# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-04 06:35:05 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DALI: A Workload-Aware Offloading Framework for Efficient MoE Inference on Local PCs](https://arxiv.org/abs/2602.03495)

**Authors**: Zeyu Zhu, Gang Li, Peisong Wang, Zitao Mo, Minnan Pei, Zhuoran Song, Xiaoyao Liang, Jian Cheng  
**Category**: cs.DC  
**Published**: 2026-02-04  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.03495v1  

#### Abstract
Mixture of Experts (MoE) architectures significantly enhance the capacity of LLMs without proportional increases in computation, but at the cost of a vast parameter size. Offloading MoE expert parameters to host memory and leveraging both CPU and GPU computation has recently emerged as a promising d...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**DALI: A Workload-Aware Offloading Framework for Efficient MoE Inference on Local PCs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题

Mixture of Experts (**MoE**) 架构显著提升了大语言模型（LLM）的容量，但由于其参数量巨大，在资源受限的本地PC（如配备消费级GPU的设备）上部署面临严重挑战。现有 **MoE offloading** 框架存在以下三大根本性效率问题：

1. **静态专家分配导致 CPU-GPU 负载失衡**  
   现有方法（如 Fiddler、HybriMoE）采用基于预设阈值的静态分配策略，将高负载专家固定在 GPU 上，低负载在 CPU 上，忽略了输入动态变化带来的负载波动，造成严重的资源利用率低下。

2. **预取（prefetching）准确性差**  
   现有预取机制未能有效预测“高负载专家”，导致大量无效的 PCIe 数据传输，反而增加延迟。

3. **GPU 缓存策略忽视负载动态性**  
   缓存替换策略（如 LRU 或基于激活分数）未考虑专家负载的时间相关性，缓存命中率低，无法有效减少通信开销。

---

### 🚀 提出的新方法与创新思路

为解决上述问题，作者提出 **DALI** ——一个**面向本地异构硬件、感知工作负载动态性的 MoE 推理卸载框架**，包含三个核心组件：

#### （1）**Greedy Assignment Strategy（贪心分配策略）**
- 将专家分配建模为 **0-1 整数优化问题**，目标是最小化 CPU 和 GPU 执行时间的最大值（`min max(T_cpu, T_gpu)`），以实现负载均衡。
- 设计高效的 **贪心算法** 近似求解该问题，在极低开销下获得接近最优的分配方案。
- 动态决定每个 token 的专家在 CPU/GPU 上的执行位置，充分利用异构计算资源。

#### （2）**Residual-Based Prefetching（基于残差的预取）**
- 利用 Transformer 层间 **residual connection** 中的隐藏状态差异（即 `res_vec`），对下一层的门控函数输入进行校正。
- 通过校正后的特征更准确地预测下一层中将被高频激活的**高负载专家**，从而提升预取精度。
- 无需微调，`res_vec` 可通过小规模校准数据集离线构建并复用。

#### （3）**Workload-Aware Cache Replacement（负载感知缓存替换）**
- 观察到高负载专家在相邻 token 之间具有强**时间局部性**（temporal correlation）。
- 提出滑动窗口机制，统计窗口内各专家的工作负载得分，并据此更新 GPU 缓存：优先保留高负载历史得分的专家。
- 显著提高缓存命中率，减少不必要的 PCIe 传输。

---

### 🔍 相比现有方法的优势

| 维度 | 现有方法（如 HybriMoE） | DALI |
|------|------------------------|------|
| 分配策略 | 静态、基于阈值 | **动态、负载感知、近似最优** |
| 预取机制 | 基于原始输入或统计特征，精度低 | **利用残差信息校正输入，精度显著提升** |
| 缓存策略 | LRU 或激活分数驱动 | **基于历史负载动态更新，命中率更高** |
| 系统资源利用率 | 存在严重 CPU-GPU 负载失衡 | **实现良好负载平衡，硬件利用率最大化** |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **C4**：用于推理速度基准测试（speed benchmarking）。
- **Wikitext**：用于构建 **residual vector** 的校准数据集（采样 1K 序列）。
- **EleutherAI LM Evaluation Harness**：用于下游任务泛化能力测试（ARC-e, ARC-c, OBQA, RTE）。

---

### ⚙️ 实验设置

- **硬件平台**：
  - CPU: AMD EPYC 7532 (64 cores)
  - GPU: NVIDIA RTX 3090 (24GB HBM)
  - 内存: 256GB DDR4 DRAM
  - 接口: PCIe 4.0 ×16 (~32 GB/s)

- **评估阶段**：
  - **Prefill Phase**：处理输入提示（prompt）
  - **Decoding Phase**：自回归生成输出

- **评估指标**：
  - **Tokens per second (tokens/s)**：主性能指标
  - **Speedup**：相对于基线的加速比
  - **Cache Hit Rate**
  - **Prefetch Accuracy**

- **控制变量**：
  - 所有框架使用相同数量的 CPU 核心（16）和线程（32）
  - GPU 内存使用保持可比性（通过调整缓存专家数或驻留层数）

---

### 🆚 基线方法对比

| 基线 | 类型 | 特点 |
|------|------|------|
| **llama.cpp**, **KTransformers** | Layer-wise Hybrid | 将前若干层放 CPU，其余放 GPU，不支持专家粒度调度 |
| **MoE-Lightning** | Offline Search | 基于性能分析模型离线搜索部署策略，灵活性差 |
| **HybriMoE** | Expert-wise Hybrid | 当前最先进的 CPU-GPU 协同框架，支持专家预取与缓存 |
| **Fiddler** | Expert-wise Hybrid | 类似 HybriMoE，但性能较差，文中仅作参考 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（平均加速比）

| 方法 | Prefill 阶段加速比 | Decoding 阶段加速比 |
|------|--------------------|---------------------|
| vs. **llama.cpp** | **7.62×** | **3.97×** |
| vs. **KTransformers** | **3.80×** | **2.16×** |
| vs. **MoE-Lightning** | **2.45×** | **1.48×** |
| vs. **HybriMoE** | **2.00×** | **1.32×** |

> 在多种 MoE 模型（DeepSeek-V2, Qwen-3, Mixtral-8x7B）和不同 batch size 下均取得一致领先。

---

### 🔍 与基线方法的详细对比

#### （1）**消融实验：各模块贡献分解（Fig. 19）**

以 Mixtral 和 Qwen 为例，从“Naive”（所有专家在 CPU）逐步添加优化：

| 步骤 | 加速比 | 说明 |
|------|-------|------|
| Naive → +Greedy Assignment | **4.1×** | 最大增益来源，显著改善负载均衡 |
| → +Residual-Based Prefetching | ~4.5× (+9%) | 预取带来边际收益，受限于额外计算开销 |
| → +Workload-Aware Cache | **6.2×**（总增益 **~38%**） | 缓存命中率提升显著降低 PCIe 流量 |

> 表明 **Greedy Assignment 是核心驱动力**，而缓存优化是重要补充。

#### （2）**预取准确率对比（Table 2）**

在 Mixtral 上预测 Top-2 高负载专家时：

| 方法 | Batch=8 | Batch=16 | Batch=32 | Batch=64 |
|------|---------|----------|----------|----------|
| EdgeMoE (statistical) | 40.3% | 40.1% | 44.7% | 48.5% |
| HybriMoE (feature-based) | 65.2% | 63.7% | 58.9% | 56.4% |
| **DALI (ours)** | **>80%**（趋势更高） | —— | —— | —— |

> DALI 的 **Residual-Based Prefetching** 显著优于现有方法。

#### （3）**缓存命中率对比（Fig. 17b）**

在 Mixtral 上，当缓存比例为 50% 时：

- **LRU**: ~30%
- **HybriMoE (score-based)**: ~37%
- **DALI (workload-aware)**: **>60%**

> 负载感知策略使缓存命中率翻倍，直接减少通信压力。

#### （4）**负载均衡效果（Appendix A.1）**

- 在 DeepSeek 上，batch=64 时：
  - 启用 Greedy Assignment 后，**GPU 执行时间减少 20 秒**
  - CPU 时间仅增加 6.66 秒
  - 总体延迟大幅下降，且负载更加均衡

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **MoE 推理中的负载高度动态，静态策略已不可行**  
   输入依赖性强导致每 token 的专家激活模式不同，必须采用运行时动态决策。

2. **动态负载感知分配能极大提升异构系统利用率**  
   Greedy Assignment 实现了接近最优的负载平衡，解决了传统方法中 CPU/GPU “一个忙一个闲”的瓶颈。

3. **跨层残差信息可用于增强预取准确性**  
   利用 `residual connection` 中的隐藏状态差值，可有效逼近下一层真实输入，从而提升专家预测质量。

4. **高负载专家具有强时间局部性**  
   相邻 token 很可能激活相同的高负载专家，这一观察支撑了 Workload-Aware Cache 的有效性。

5. **三者协同带来显著端到端加速**  
   DALI 在 prefill 和 decoding 阶段均实现 **2–7.6× 的加速**，远超当前 SOTA。

---

### ⚠️ 方法的局限性

1. **依赖 warm-up profiling 获取执行时间表**  
   需预先测量 `t_cpu(w)` 和 `t_gpu(w)`，虽可复用，但在新硬件上需重新校准。

2. **残差向量假设输入分布稳定**  
   若下游任务与 Wikitext 差异过大，可能影响预取效果（但实验显示仍具较强泛化性）。

3. **目前针对单机单卡场景设计**  
   多 GPU 或分布式环境尚未验证，扩展性有待探索。

---

### 🔮 未来工作方向

1. **拓展至多 GPU 和服务器集群环境**  
   探索 DALI 在 GH200、NVLink 高带宽系统中的潜力。

2. **结合量化与稀疏化技术**  
   进一步压缩专家参数大小，降低传输与存储成本。

3. **自动化参数调优（如 wsize, usize）**  
   根据序列长度、模型结构自适应选择最佳缓存更新频率。

4. **探索更复杂的调度算法**  
   如强化学习或轻量级在线优化器，替代贪心策略以逼近全局最优。

---

## 总结

**DALI** 是首个全面考虑 **MoE 负载动态性** 与 **本地PC异构硬件特性** 的推理框架。它通过 **Greedy Assignment**、**Residual-Based Prefetching** 和 **Workload-Aware Cache** 三大创新机制，实现了 CPU 与 GPU 的高效协同，在消费级设备上显著加速 MoE 模型推理，推动了 LLM 的本地化部署进程。其实验充分、设计合理，代表了 MoE offloading 领域的重要进展。

</details>

---

### 2. [Understanding and Exploiting Weight Update Sparsity for Communication-Efficient Distributed RL](https://arxiv.org/abs/2602.03839)

**Authors**: Erfan Miahi, Eugene Belilovsky  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.03839v1  

#### Abstract
Reinforcement learning (RL) is a critical component for post-training large language models (LLMs). However, in bandwidth-constrained distributed RL, scalability is often bottlenecked by the synchronization of policy weights from trainers to inference workers, particularly over commodity networks or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Understanding and Exploiting Weight Update Sparsity for Communication-Efficient Distributed RL

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

在**带宽受限的分布式强化学习（Distributed RL）**系统中，训练器（trainer）需要频繁地将更新后的策略模型权重同步给多个推理节点（inference workers）。对于一个7B参数的模型，在16-bit精度下，每次同步需传输 **14 GB** 数据。这导致：

- 在普通网络（如公共互联网）上通信开销巨大；
- 高GPU利用率要求极高带宽（如20 Gbit/s），远超典型部署环境能力；
- 成为分布式RL扩展性的主要瓶颈。

传统梯度压缩（gradient compression）方法不适用于此场景，因为它们通常用于**梯度聚合**而非**权重广播**，且多为有损压缩。

---

### 🚀 提出的新方法：PULSE

论文提出了 **PULSE**（**Patch Updates via Lossless Sparse Encoding**），一种**无损稀疏编码**的权重同步方法。

#### 核心思想：
- 观察到：**RL训练中的权重更新具有极高的稀疏性**（>99%参数未变）。
- 仅传输**发生变化的参数索引**（indices）和**新的值**（values），而非整个模型权重。
- 采用稀疏表示 + Delta编码 + 类型降级 + 通用压缩（如zstd）实现高效传输。

#### 创新点：
1. **首次系统性实证分析RL训练中step-level和multi-step的权重更新稀疏性**，揭示其高稀疏性的机制。
2. **提出并实现PULSE**，是首个针对**权重同步**而非梯度同步的**无损稀疏化方法**。
3. **存储实际值而非增量（delta）**，避免浮点累加漂移（floating-point drift），保证**bit-identical重建**。
4. 支持异步、去中心化部署，适用于真实世界网络环境。

---

### 🔍 相比现有方法的优势

| 方法 | 是否适用权重同步 | 是否无损 | 压缩率 | 抗漂移 | 适用场景 |
|------|------------------|----------|--------|--------|-----------|
| Gradient Compression (e.g., DeepGC) | ❌ 主要用于梯度聚合 | ❌ 有损 | 中等 | ❌ | 同步训练 |
| Entropy Coding (e.g., Huffman) | ⚠️ 通用压缩 | ✅ 可无损 | 低~中 | ✅ | 通用 |
| Subnetwork Update [21] | ⚠️ 近似阈值检测 | ❌ 有损 | ~3–20× | ❌ | 特定任务 |
| **PULSE (本文)** | ✅ 专为权重广播设计 | ✅ **无损** | **>100×** | ✅ **强健** | **去中心化RL** |

> ✅ **优势总结**：  
> - **通信减少100倍以上**（14GB → ~108MB）；  
> - **保持bit-identical训练动态**；  
> - **无需误差反馈（error feedback）机制**；  
> - **适用于低带宽、高延迟、异步网络**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 任务 | 数据集 | 描述 |
|------|--------|------|
| 数学推理 | **MATH** | 包含7,500道高中及以上数学题，用于RLVR（Reinforcement Learning with Verifiable Rewards） |
| 代码生成 | **MBPP** (Mostly Basic Python Problems) | 774个编程任务，奖励基于单元测试通过率和语法正确性 |

---

### ⚙️ 实验设置

| 组件 | 配置 |
|------|------|
| **模型** | Qwen2.5-Instruct (0.5B, 1.5B, 7B), Llama-3.2-Instruct (3B), Gemma-3-4B-it |
| **算法** | **GRPO**（Group Relative Policy Optimization），用于reasoning任务 |
| **精度** | **BF16**（bfloat16）混合精度训练（FP32优化器主权重） |
| **学习率** | 默认 `3e-6`，去中心化实验中使用 `1e-6` 提升稳定性 |
| **训练步数** | 400 steps（验证已收敛） |
| **Batch配置** | 32 prompts × 16 rollouts |
| **硬件** | A100/H200 GPU，公网带宽约400 Mb/s |

---

### 📊 评估指标

| 指标 | 定义 |
|------|------|
| **Weight Update Sparsity** | 连续两步间**bitwise相同**的参数比例：<br>$ S_k(t) = \frac{1}{d} \sum_{i=1}^d [\theta_{t+i} = \theta_t] $ |
| **Communication Reduction** | 传输大小相对于完整checkpoint的压缩比 |
| **Pass@1 Accuracy** | 验证集上单次生成正确的比例 |
| **SHA-256 Hash Match** | 验证解码后权重是否bit-identical |
| **End-to-End Sync Latency** | 推理节点完成一次同步所需时间 |

---

### 🆚 基线方法对比

- **Full Weight Synchronization**（14GB全量传输）
- **Gradient Compression Methods**（如DeepGC, QSGD）——作为背景对比，但不直接可比
- **Subnetwork Update** [21]：基于阈值近似识别“活跃子网络”，有损

> PULSE无直接竞争者，因其是首个**无损、基于step-level稀疏性**的权重同步方案。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 指标 | 结果 |
|------|------|
| **平均每步权重更新稀疏性** | **~99%**（所有模型规模下一致） |
| **最小稀疏性（最差情况）** | >98%（即使在k=8异步延迟下） |
| **PULSE平均压缩率** | **79×**（zstd-1）至 **100×**（LLaMA3.2） |
| **实际上传大小（7B模型）** | **平均108 MB**（vs 14 GB）→ **>100× 减少** |
| **通信需求降低** | 从 **20 Gbit/s → 0.2 Gbit/s**（维持90% GPU利用率） |
| **端到端同步延迟（快路径）** | **~3.9秒**（下载+解压+应用） |
| **冷启动恢复时间** | ~280秒（需下载完整checkpoint） |

> 💡 在去中心化网络中，**PULSE实现超过100倍带宽节省**，同时保持训练正常进行。

---

### 🔬 消融实验结果

#### （1）稀疏性来源分析（Section 3.3.1）

| 因素 | 影响 |
|------|------|
| **梯度密度** | ~99%非零 → **梯度本身是稠密的** |
| **BF16精度限制** | 更新必须超过 $|w|/256$ 才能生效 |
| **小学习率（~3e-6）** | 大多数更新被“吸收”（absorbed） |
| **大权重（median ~0.012）** | 超过96%权重无法被更新 |

> ✅ **结论**：稀疏性源于 **BF16 + 小学习率** 的交互，而非梯度稀疏。

#### （2）不同学习率的影响（Figure 4a）

- 学习率从 `5e-7` → `5e-6`，稀疏性从 ~99.5% ↓ 至 ~97%
- 表明**学习率是控制稀疏性的关键因素**

#### （3）策略陈旧性（Policy Staleness）影响（Figure 4b）

- 即使延迟32步，**每步稀疏性仍 >98.5%**
- 支持**高度异步训练架构**

#### （4）压缩组件贡献（Table 5）

| 步骤 | 压缩增益（相对COO） |
|------|------------------|
| Raw COO | 2.71× |
| + Delta Encoding | +13.3% → 3.07× |
| + Type Downscaling | +8.5% → **3.33×** |

> 总计提升 **+22.9%** 压缩率。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **RL微调中权重更新极度稀疏**：
   - 平均**99%参数不变**，跨模型（0.5B–7B）、架构（Qwen/Llama/Gemma）一致。
   - 稀疏性由 **BF16精度** 和 **保守学习率** 共同决定，是一种**内在机制**而非偶然现象。

2. **稀疏性稳定且鲁棒**：
   - 整个训练过程稀疏性波动小于0.4%；
   - 对**异步延迟（up to 32 steps）** 不敏感，适合去中心化部署。

3. **PULSE实现无损高压缩**：
   - 仅传输变化部分，结合稀疏编码与通用压缩，实现 **>100× 通信减少**；
   - **存储实际值而非delta**，避免浮点漂移，保证**bit-identical重建**；
   - 已在真实去中心化网络（grail平台）中验证成功。

4. **带宽瓶颈被打破**：
   - 权重同步所需带宽从 **20 Gbit/s降至0.2 Gbit/s**；
   - 使得**普通宽带网络**也能支持高效分布式RL训练。

---

### ⚠️ 局限性

| 局限 | 说明 |
|------|------|
| **依赖Adam类优化器** | 分析基于AdamW，SGD等可能不具备相同稀疏性（见A.6） |
| **当前聚焦单轮对话任务** | 多轮RL、长序列反馈场景尚未验证 |
| **仅验证GRPO/PPO类算法** | DPO等偏好优化算法需进一步研究 |
| **锚点（anchor）恢复成本高** | 冷启动需下载14GB，不适合频繁加入节点 |

---

### 🔮 未来工作方向

1. **扩展至其他RL算法**：如PPO、DPO、ILQL等；
2. **支持多轮对话与长期记忆**场景下的更新模式分析；
3. **探索更低精度格式**（如FP8）对稀疏性的放大效应；
4. **自适应anchor间隔**：根据网络状况动态调整；
5. **结合梯度稀疏化**：构建端到端通信高效RL框架。

---

## 总结

> **PULSE揭示了RL训练中“静默更新”的本质，并将其转化为通信效率的革命性突破。它不仅解决了去中心化RL的关键瓶颈，更为大规模语言模型的协作训练提供了可行路径。**

✅ **一句话总结**：  
利用RL训练中**>99%的权重更新稀疏性**，PULSE实现了**无损、>100×压缩的权重同步**，使去中心化RL在普通网络上成为现实。

</details>

---

### 3. [Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design](https://arxiv.org/abs/2602.00608)

**Authors**: Wei Zeng, Xuchen Li, Ruili Feng, Zhen Liu, Fengwei An, Jian Zhao  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.00608v1  

#### Abstract
Real-time generative game engines represent a paradigm shift in interactive simulation, promising to replace traditional graphics pipelines with neural world models. However, existing approaches are fundamentally constrained by the ``Memory Wall,'' restricting practical deployments to low resolution...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统视频游戏依赖显式的物理引擎和光栅化渲染管线，而**生成式游戏引擎**（Generative Game Engine）通过神经网络直接从动作输入生成未来帧，实现“像素级模拟”。然而，当前这类系统受限于 **"Memory Wall"**（内存墙），即高分辨率生成过程中巨大的内存带宽需求导致延迟过高，难以实现实时交互。

现有方法如 GameNGen 和 Diamond 仅能在低分辨率（如 64×64 或 320×240）下运行，无法满足标准清晰度（720×480）及以上的需求。本文旨在突破这一**分辨率瓶颈**，使生成式游戏引擎在高分辨率下仍能保持低延迟、高帧率的实时响应能力。

---

### 提出了什么新方法或新思路
作者提出了一种**可扩展的硬件-算法协同设计框架**（Hardware-Algorithm Co-Design），构建了一个异构计算架构来解耦计算密集型与内存密集型模块，并引入三项核心技术：

#### （1）异构资源分配策略（Heterogeneous Pipeline Parallelism）
- 将生成流程分为两个阶段：
  - **World Model (DiT)**：负责基于动作预测潜在空间状态，属于 **compute-bound**。
  - **Decoder (VAE)**：将潜在表示解码为图像，属于 **memory-bound**。
- 对 DiT 使用 **Sequence Parallelism**（Ulysses），对 VAE 使用 **Spatial Parallelism**，分别优化其并行效率。
- 建立理论模型分析不同设备分配下的吞吐量，推导出最优资源配置（5:3 分配）。

#### （2）内存中心的操作融合优化（Memory-Centric Operator Fusion）
- 针对 VAE 解码器实施 **Vertical Fusion**：将 `Upsample → Conv2d → GroupNorm → SiLU` 融合为单个 kernel，利用片上 SRAM 减少 HBM 访问次数，降低 75% 内存带宽压力。
- 针对 DiT 中的 AdaLN 层实施 **Horizontal Fusion**：合并多个小矩阵乘法操作，提升计算密度和算术强度（arithmetic intensity >85%）。

#### （3）流形感知的潜在外推机制（Manifold-Aware Latent Extrapolation）
- 利用连续帧之间的**时间冗余性**，假设潜在空间轨迹局部线性。
- 当用户动作稳定时，跳过部分 DiT 推理步骤，直接通过前一时刻的运动向量 $ v_t = z_t - z_{t-1} $ 外推下一帧潜在码 $ z_{t+1} \approx z_t + \lambda v_t $。
- 在动作突变时自动回退到完整推理，保证逻辑一致性。

此外，还结合了 **Speculative Action Prefetching**（基于轻量 LSTM 预测用户行为以预生成帧），进一步降低感知延迟。

---

### 相比现有方法的优势
| 维度 | 本工作优势 |
|------|-----------|
| **分辨率** | 支持 **720×480**，相较 Diamond (64×64) 提升 **50× 像素数** |
| **实时性** | 实现 **26.4 FPS（连续）和 48.3 FPS（离散）**，接近实时阈值（30 FPS） |
| **延迟控制** | 摊销有效延迟降至 **2.7 ms**，远低于人类感知阈值 |
| **系统设计** | 异构架构 + 显式内存管理，突破通用 GPU 架构的“隐式缓存”限制 |
| **通用性** | 方法适用于任何支持显存编程的 AI 加速器（如 Ascend, H100/NVLink） |

---

## 2. 核心实验方法和设置

### 使用的数据集
论文采用两个基准测试环境，构成“连续-离散对偶”测试床（Continuous-Discrete Duality Benchmark）：

- **Matrix (Continuous Domain)**  
  - 来源：[9] The Matrix: Infinite-horizon world generation with real-time moving control  
  - 类型：高保真 3D 赛车模拟器，强调连续动力学（摩擦、动量等）  
  - 分辨率：**720×480**

- **PGG (Playable Game Generation, Discrete Domain)**  
  - 来源：[10] Playable Game Generation  
  - 类型：2D 平台跳跃游戏，强调布尔逻辑、碰撞检测、离散状态转移  
  - 分辨率：**256×256**

---

### 实验设置
- **硬件平台**：8 卡华为 Ascend 910C AI 加速器集群（每卡 64GB HBM，FP16 算力 752 TFLOPS）
- **互连拓扑**：HCCS 高速环形互联（30 GB/s per link）
- **软件栈**：CANN 8.0, PyTorch 2.5.1, xfuser（用于 Ulysses 并行）
- **集群配置**：5 张用于 DiT，3 张用于 VAE（经理论建模得出最优比例）

---

### 评估指标
| 指标类别 | 具体指标 | 说明 |
|--------|---------|------|
| **性能** | FPS, Motion-to-Photon Latency (M2P) | 衡量生成速度与响应延迟 |
| **视觉质量** | FID ↓, PSNR ↑, SSIM ↑, LPIPS ↓ | 衡量生成画面的真实感与细节保留 |
| **物理合理性** | Control Sensitivity Analysis (CSA) | 连续域中转向输入与车辆偏航率的关系 |
| **逻辑一致性** | Discrete Logic Boundary (DLB) Score ↑ | 离散域中是否违反碰撞、重力等规则（越接近 100% 越好） |

---

### 基线方法对比
| 基线方法 | 平台 | 分辨率 | FPS | 备注 |
|--------|-----|-------|-----|------|
| **Diamond [6]** | RTX 3090 | 64×64 | 10.0 | 扩散模型用于 CS:GO 场景 |
| **GameNGen [4]** | TPU v5 | 320×240 | >20 | Google 提出的实时生成引擎 |
| **PGG Baseline*** | RTX 5090 | 256×256 | 29.9 | 开源平台跳跃生成模型 |

> 注：本文方法在更高分辨率下仍实现更优性能。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 结果 |
|------|------|
| **最大分辨率支持** | **720×480**（SD 标准） |
| **连续域（Matrix）FPS** | **26.4 FPS** |
| **离散域（PGG）FPS** | **48.3 FPS** |
| **摊销有效延迟（含推测执行）** | **2.7 ms** |
| **单帧物理延迟（无优化）** | ~147 ms（主要由 VAE 解码主导） |
| **VAE 解码加速比（Operator Fusion）** | **2.9×**（从 312.8ms → 109.4ms） |

---

### 与基线方法的对比结果（见 Table III）
| 方法 | 分辨率 | FPS | Norm. Eff. (FPS/100TFLOPS) | Latency |
|------|--------|-----|----------------------------|--------|
| Diamond [6] | 64×64 | 10.0 | 7.04 | 100 ms |
| GameNGen [4] | 320×240 | >20 | 4.35 | ~50 ms |
| **Ours (Matrix-Cluster)** | **720×480** | **26.4** | **0.44** | **38ms (2.7ms\*)** |

> ✅ 尽管归一化效率较低（因通信开销），但绝对性能显著超越所有基线，且达到前所未有的分辨率。

---

### 消融实验结果（见 Table IV & Fig. 7）
逐步添加优化后的性能提升路径如下：

| 阶段 | FPS | Speedup |
|------|-----|--------|
| Baseline（单卡顺序执行） | 2.1 | 1.0x |
| + Operator Fusion | 4.5 | 2.1x |
| + Ulysses (3:5) | 16.6 | 7.9x |
| + Optimal Ratio (5:3) | 19.4 | 9.2x |
| + Latent Extrapolation | 26.4 | **12.6x** |
| + Speculative Execution | 26.4 | 有效延迟降至 **2.7ms** |

> 🔺 **关键发现**：异构资源分配带来 **+16.9% 吞吐增益**；潜在外推 alone 提升 **+7.0 FPS**。

---

## 4. 关键结论和发现

### 主要发现
1. **“Memory Wall” 是高分辨率生成的核心瓶颈**，单纯算法优化无法解决，必须进行 **Hardware-Algorithm Co-Design**。
2. **World Model 与 Decoder 存在根本性的资源错配**：前者是 compute-bound，后者是 memory-bound，需采用异构并行策略。
3. **通过 Operator Fusion 可大幅减少 HBM 访问**，实现 “Zero-Copy” 数据流，是打破内存墙的关键。
4. **时间冗余可用于掩盖计算延迟**：Manifold-Aware Latent Extrapolation 可安全跳过 65% 的 DiT 计算而不影响视觉连贯性。
5. **Speculative Prefetching + Change Blindness 效应** 可有效隐藏突发 miss 的视觉抖动，维持流畅体验。

---

### 方法的局限性
1. **依赖大规模加速器集群**：目前需 8 卡 Ascend 910C，限制了边缘部署（edge deployment）的可能性。
2. **Out-of-Distribution (OOD) 动作可能导致逻辑错误**：虽然训练分布内保持 100% DLB 正确率，但在未见过的动作组合下可能出现“幻觉”（如穿墙）。
3. **最优资源配置依赖模型超参**（如 attention head 数），需要针对不同模型重新调优。
4. **尚未支持多模态输入**（如语音、自然语言指令）。

---

### 未来工作方向
1. **开发 Hybrid Neuro-Symbolic Engine**：引入轻量级符号逻辑层作为“游戏规则监督器”，强制约束关键规则（如碰撞、死亡判定）。
2. **探索 Multi-modal Control Interfaces**：利用 Transformer 的 cross-attention 能力，支持自然语言或语音命令驱动游戏世界。
3. **推动 On-Device Quantization 技术**：研究 4-bit 权重量化 + 激活平滑技术，使模型可在消费级 NPU 上本地运行。
4. **跨平台编译器支持**：将该协同设计理念移植至 OpenAI Triton 等通用后端，实现 NVIDIA/H100、AMD 等多厂商兼容。

---

## 总结
本文首次实现了 **720×480 分辨率下的实时生成式游戏引擎**，通过 **硬件-算法协同设计** 成功打破了长期制约该领域的“内存墙”。其三大创新——**异构并行、操作融合、潜在外推**——共同支撑起高达 **26.4 FPS** 的流畅体验和 **2.7 ms** 的极低感知延迟。这不仅验证了“神经世界模型”可以替代传统图形引擎的技术可行性，也为下一代沉浸式交互娱乐奠定了基础：**世界不再被构建，而是被“梦见”**。

</details>

---

### 4. [Beyond Tokens: Semantic-Aware Speculative Decoding for Efficient Inference by Probing Internal States](https://arxiv.org/abs/2602.03708)

**Authors**: Ximing Dong, Shaowei Wang, Dayi Lin, Boyuan Chen, Ahmed E. Hassan  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.03708v1  

#### Abstract
Large Language Models (LLMs) achieve strong performance across many tasks but suffer from high inference latency due to autoregressive decoding. The issue is exacerbated in Large Reasoning Models (LRMs), which generate lengthy chains of thought. While speculative decoding accelerates inference by dr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Beyond Tokens: Semantic-Aware Speculative Decoding for Efficient Inference by Probing Internal States

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有的 **Speculative Decoding (SD)** 方法大多在 **token-level** 上进行草稿生成与验证，忽略了语义等价性（semantic equivalence）——即不同 token 序列可能表达相同含义。这导致即使语义正确，只要 token 不匹配也会被拒绝，造成效率低下。

此外，针对 **Large Reasoning Models (LRMs)** 的序列级推测方法（如 SpecReason、Speculative Thinking）依赖 **LLM-as-a-judge** 或硬编码信号（如“wait”），存在偏见且不可靠。

### 🚀 提出的新方法：SemanticSpec
提出 **Semantic-Aware Speculative Decoding (SemanticSpec)**，一种全新的推测解码框架，其核心思想是：
- 将推测单位从 **token-level** 提升到 **semantic sequence-level**。
- 引入 **semantic probability estimation mechanism**，通过探测模型内部隐藏状态（hidden states）来估计生成特定语义意义的概率，而非仅看 token 分布。

#### 创新机制：
- **Semantic Probability Predictor**：训练一个回归模型，输入为目标模型在验证时的多层 hidden states，输出为该序列所表达语义的生成概率。
- **跨语义一致性验证**：比较草稿模型和目标模型对同一语义的预测概率，决定是否接受整个语义序列。

### 🔍 相比现有方法的优势
| 维度 | 传统 Token-Level SD | Sequence-Level Baselines (e.g., SpecReason) | SemanticSpec |
|------|---------------------|--------------------------------------------|-------------|
| 推测粒度 | Token | Sequence（表面形式） | **Semantic Meaning** |
| 验证方式 | Greedy/top-k 匹配 | LLM评分或关键词触发 | **基于 internal states 的语义概率对齐** |
| 对语义等价的支持 | ❌ 忽略 | ⚠️ 不稳定（依赖judge） | ✅ 显式建模 |
| 效率与准确性平衡 | 低接受率 | 高风险误判 | **高接受率 + 高保真输出** |

---

## 2. 核心实验方法和设置

### 📚 数据集
在四个具有挑战性的推理基准上评估：
- **MATH-500**：数学问题求解（multi-step deduction）
- **AIME24**, **AMC23**：竞赛级数学题
- **GPQA-D**：专家级科学问答（graduate-level science questions）

覆盖数学与科学领域，强调复杂推理能力。

### ⚙️ 实验设置
- **目标模型（Target Model）**：
  - `DeepSeekR1-32B`
  - `QwQ-32B`
- **草稿模型（Draft Model）**：
  - `DeepSeekR1-1.5B`（轻量级同系列模型）
- **训练数据**：
  - 使用 `SimpleScaling-S1K` 数据集训练 semantic probability predictor
  - 按领域划分：约40万样本用于科学（GPQA-D），其余用于数学任务
- **序列分割策略**：默认使用 `\n\n` 分割 reasoning steps；也测试了句末标点 `.?!` 的细粒度切分

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **Pass@1** | 单次生成答案正确的比例，衡量准确性 |
| **Latency (s)** | 总推理延迟（含 drafting 和 verification） |
| **Token Per Second (TPS)** | 吞吐量，衡量效率 |
| **Speedup Ratio** | 相对于标准 decoding 的加速比 |

### 🆚 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **SpecSampling** | Token-level SD | 标准推测采样，无语义感知 |
| **SpecReason (Pan et al., 2025)** | Sequence-level | 草拟 reasoning steps，由目标模型打分验证 |
| **Speculative Thinking (Yang et al., 2025)** | Sequence-level | 基于“wait”等词触发大模型介入 |
| **Random / Last-hidden** | Ablation variants | 用于消融分析 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据
| 模型组合 | 加速比 (Speedup) | TPS 提升 | Pass@1 下降 |
|--------|------------------|----------|-------------|
| **DeepSeekR1-32B** | **2.7×** | **+1.67× vs SpecSampling** | ~4.9% ↓ |
| **QwQ-32B** | **2.1×** | **+2.66× vs SpecSampling** | ~3.9% ↓ |

> 💡 在保持接近目标模型准确率的前提下，实现显著加速。

### 🆚 与基线方法对比
| 对比项 | 结果 |
|-------|------|
| **vs SpecSampling (token-level)** | 
| - 平均提升 Pass@1：**+17.3% (DeepSeek), +7.3% (QwQ)**  
| - 减少延迟：**-40.2%, -45.3%**  
| - 更高 TPS：分别提升 **1.67× 和 2.66×** |
| **vs SpecReason / Speculative Thinking** |
| - 显著更高 Pass@1（尤其在 AIME24 上高出超 10%）  
| - 更低延迟，更稳定表现 |
| - Speculative Thinking 因盲目接受草稿而准确率最低 |

### 🔍 消融实验结果（Ablation Study）
| 变体 | 描述 | 结果 |
|------|------|------|
| **Random** | 语义概率随机赋值 | 所有指标大幅下降 → 表明 predictor 至关重要 |
| **Last-hidden** | 仅用最后一层 hidden state | TPS 更高（计算开销小），但 **Pass@1 明显更低**（如 AIME24 上从 0.553→0.400） |

> ✅ 多层 hidden states 聚合能更好捕捉语义置信度，虽略有计算代价，但显著提升准确性。

### 🔄 泛化性实验（Cross-domain Generalization）
| 设置 | 结果 |
|------|------|
| 数学任务用科学域训练的 predictor | 性能轻微下降（如 AMC23: 0.892 → 0.875），但仍有效 |
| 科学任务用数学域训练的 predictor | GPQA-D: 0.573 → 0.552 |

> ✅ 表明 semantic probability predictor 具备一定跨领域泛化能力。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **语义层面的推测优于 token/sequence-level**：
   - 通过建模 **semantic probability**，可有效识别语义等价但形式不同的合理输出，避免无效拒绝。
2. **LLM 内部状态蕴含语义置信度信息**：
   - hidden states（尤其是多层聚合）能反映模型对某语义的生成倾向，可用于高效语义验证。
3. **SemanticSpec 实现效率与效果双赢**：
   - 在两个主流 LRM 上实现 **最高达 2.7× 的端到端加速**，同时保持接近原模型的准确率。
4. **适用于长链推理场景**：
   - 特别适合 LRMs 中的 Chain-of-Thought 生成，减少冗余重复推理路径。

### ⚠️ 局限性
- 当前评估局限于两对开源模型（DeepSeek/QwQ 系列）和四个基准，泛化性需进一步验证。
- semantic probability predictor 需离线训练，增加部署复杂性。
- 序列切分策略（如 `\n\n`）影响性能，自动语义边界检测仍是挑战。

### 🔮 未来工作方向
- 探索 **zero-shot 或 prompt-based semantic probability estimation**，减少训练依赖。
- 将 SemanticSpec 扩展至 **多模态模型** 或 **对话系统**。
- 结合 **动态切分机制** 自动识别语义单元边界。
- 研究 **轻量化 predictor** 设计以降低推理开销。

---

> **总结一句话**：  
> SemanticSpec 通过“理解语义”而非“匹配字面”，实现了更智能、高效的推测解码，在不牺牲准确性的前提下大幅提升 LLM 推理速度，为下一代高效推理系统提供了新范式。

</details>

---

### 5. [Token Sparse Attention: Efficient Long-Context Inference with Interleaved Token Selection](https://arxiv.org/abs/2602.03216)

**Authors**: Dongwon Jo, Beomseok Kang, Jiwon Song, Jae-Joon Kim  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03216v1  

#### Abstract
The quadratic complexity of attention remains the central bottleneck in long-context inference for large language models. Prior acceleration methods either sparsify the attention map with structured patterns or permanently evict tokens at specific layers, which can retain irrelevant tokens or rely o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Token Sparse Attention: Efficient Long-Context Inference with Interleaved Token Selection

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在处理长上下文（long-context）时面临 **Attention 机制的二次复杂度 $O(L^2)$** 问题，尤其是在 **prefill 阶段**，这成为推理效率的主要瓶颈。现有加速方法存在以下不足：
- **稀疏注意力（Sparse Attention）**：通常采用块级（block-level）稀疏化，无法精细去除无关 token。
- **Token 蒸馏/淘汰（Token Eviction）**：如 FastKV、GemFilter 在早期层永久移除部分 token，导致后续层无法重新评估其重要性，造成信息丢失。

### 🚀 提出的新方法：Token Sparse Attention
提出一种轻量级、动态的 **token-level 稀疏化机制**，核心思想是“**压缩 → 注意力计算 → 解压**”（Compress-and-Decompress），实现 **可逆的 token 选择**。

#### 创新点：
1. **Interleaved Token Selection（交错式 Token 选择）**  
   - 每个 attention head 可独立选择最重要的 token 子集进行计算（$L' < L$），形成压缩后的 $Q, K, V$。
   - 注意力输出后通过 **scatter 操作恢复到原始序列长度**，未选中的位置置零，保留残差路径上的原始信息。
   - 允许后续层再次考虑之前被跳过的 token，避免了永久淘汰带来的信息损失。

2. **Dynamic Token Coverage（动态稀疏预算分配）**
   - 不固定保留比例，而是基于 attention score 分布动态决定应剪枝的 token 数量。
   - 引入 **coverage threshold $T$**，剪去累计贡献小于 $T$ 的最不重要 token 尾部，视为“attention noise”。

3. **Sparse Layer Selection（稀疏层选择机制）**
   - 并非所有层都适合稀疏化。引入 **Inter-Layer Representation Drift** 指标衡量层间表示变化。
   - 仅对表示稳定的低漂移层应用 Token Sparse Attention，提升鲁棒性。

4. **强兼容性设计**
   - 压缩后的 $Q, K, V$ 仍是密集张量，完全兼容 **FlashAttention** 和各类 **sparse attention kernels**。
   - 可作为插件无缝集成到已有方法中（如 FlexPrefill、Minference）。

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 FastKV/GemFilter） | Token Sparse Attention |
|------|-------------------------------|------------------------|
| **Token 可恢复性** | ❌ 永久删除，不可再用 | ✅ 通过 residual 路径保留 |
| **Head-wise 灵活性** | ❌ 所有 head 共享相同 token 集 | ✅ 各 head 可选不同子集 |
| **Layer-wise 动态性** | ❌ 固定早期决策 | ✅ 每层动态调整 |
| **与现有方法关系** | 替代方案 | ✅ **互补增强型模块** |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **RULER**：合成基准，测试长上下文下的检索与多跳推理能力，支持从 4K 到 128K 的可控长度。
- **InfiniteBench**：大规模长文本理解基准，涵盖检索、问答、数学、代码等任务，部分任务超 100K tokens。
- （附录中还包含 **Needle-in-a-Haystack** 和 **LongBench** 结果）

### ⚙️ 实验设置
- **模型**：
  - `LLaMA-3.1-8B-Instruct`
  - `Mistral-Nemo-12B-Instruct`
- **上下文长度**：4K, 8K, 16K, ..., 最高至 **128K**
- **硬件平台**：单张 NVIDIA A100 80GB GPU
- **评估阶段**：仅在 **prefill 阶段** 应用优化，decode 阶段保持标准 dense attention。

### 📊 评估指标
- **Accuracy**：任务准确率（如 RULER、InfiniteBench 上的平均得分）
- **Speedup**：相对于 FlashAttention 的 **attention 计算延迟加速比**
- **Sparsity**：注意力图中的平均稀疏度
- **Latency Breakdown**：分解 token scoring、压缩、注意力计算、解压等各阶段耗时

### 🆚 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **FlashAttention** | Dense Baseline | 高效内存访问的完整 attention |
| **Minference** | Structured Sparse | 使用 Vertical-Slash 模式的块稀疏 attention |
| **FlexPrefill** | Context-Aware Block-Sparse | 动态块稀疏，query-aware pruning |
| **FastKV / GemFilter** | Token Eviction | 早期层选择并永久压缩 token |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 & Figure 1）

#### 在 LLaMA-3.1-8B-Instruct 上的表现（RULER 基准）：

| 方法 | Avg. Accuracy | 128K Speedup |
|------|---------------|-------------|
| FlashAttention | 87.01% | ×1.00 |
| + Token Sparse | **87.02%** | **×1.36** |
| FlexPrefill | 87.27% | ×2.44 |
| + Token Sparse | **87.27%** | **×2.76** |
| FlexPrefill + Token Sparse ($T=0.02$) | ~86.8% | **×3.23** |

> ✅ **最高达 ×3.23 加速，精度下降 <1%**

#### 在 Mistral-Nemo-12B-Instruct 上的结果：
| 方法 | Avg. Accuracy | 128K Speedup |
|------|---------------|-------------|
| FlashAttention | 67.60% | ×1.00 |
| + Token Sparse | 67.37% | ×1.22 |
| FlexPrefill | 68.16% | ×1.22 |
| + Token Sparse | 67.91% | **×1.33** |

> ✅ 显著提升已有方法的效率，且精度波动极小（<0.5%）

---

### 🔬 消融实验结果

#### ✅ **Dynamic vs. Fixed Sparsity**（Table 4）
| 方法 | Sparsity | Accuracy | Speedup |
|------|--------|---------|--------|
| Dynamic ($T=0.01$) | 67.36% | **86.84%** | ×1.51 |
| Fixed ($s=0.5$) | 74.95% | 85.43% | ×1.57 |

> 💡 动态分配稀疏预算比固定比例更高效，在相近速度下 **高出 1.4% 准确率**。

#### ✅ **vs. Token Eviction Methods**（Table 5）
| 方法 | RULER Avg. Acc. | Speedup |
|------|------------------|--------|
| FlashAttn | 87.01% | ×1.00 |
| GemFilter | 85.12% | ×1.53 |
| FastKV | 85.64% | ×1.50 |
| **Ours (TSA)** | **86.84%** | ×1.51 |

> ✅ 在相似加速比下，**准确率显著优于 token eviction 方法**（+1.2~1.7%）。

#### ✅ **Overhead Analysis**（Figure 6b）
- Token Sparse Attention 的额外开销（token scoring + compress/decompress）在 128K 下仅占总 attention 延迟的 **8–11%**。
- 即使在 $T=0.05$ 极端稀疏下仍保持高效。

#### ✅ **Sparsity 随上下文增长而增加**（Table 3）
| Context Length | $T=0.01$ 时平均 Sparsity |
|----------------|--------------------------|
| 4K             | 28.02%                   |
| 8K             | 32.98%                   |
| 64K            | 47.32%                   |
| 128K           | **67.36%**               |

> 表明该方法在 **越长上下文中增益越大**，符合预期。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **动态、可逆的 token 级稀疏化是可行且高效的**：通过“压缩-解压”机制，既能减少计算量，又能保留未来重评机会。
2. **token 重要性具有 layer-wise 和 head-wise 动态性**：强制统一 token 集会损害表达能力；允许每 head 每层灵活选择更合理。
3. **与现有稀疏 attention 方法高度互补**：不是替代，而是可叠加的“效率放大器”，能进一步提升 FlexPrefill 等方法的性价比。
4. **在 128K 上实现高达 ×3.23 的 attention 加速，精度损失 <1%**，显著改善 accuracy-latency trade-off。

### ⚠️ 局限性（Limitation）
- 在短上下文（如 <8K）中收益较小，因 attention 不主导整体延迟。
- 过于激进的 token coverage（如 $T > 0.05$）可能导致关键信息丢失。
- 当前仅应用于 prefill 阶段，尚未扩展至 decoding 或 KV cache 管理。

### 🔮 未来工作方向
- 将 Token Sparse Attention 扩展到 **decoding 阶段**，用于 KV cache 压缩。
- 探索在 **vision-language models** 等多模态场景中的应用。
- 设计更智能的 coverage threshold 自适应策略（例如基于输入难度）。
- 结合量化技术（如 AWQ、GPTQ）构建端到端高效推理系统。

---

> 📌 **一句话总结**：  
> **Token Sparse Attention 提供了一种灵活、可逆、兼容性强的 token-level 稀疏化范式，通过动态交错选择机制，在几乎无损精度的前提下大幅提升长上下文 attention 效率，是当前稀疏 attention 方法的重要补充。**

</details>

---

### 6. [POP: Prefill-Only Pruning for Efficient Large Model Inference](https://arxiv.org/abs/2602.03295)

**Authors**: Junhui He, Zhihui Fu, Jun Wang, Qingan Li  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03295v1  

#### Abstract
Large Language Models (LLMs) and Vision-Language Models (VLMs) have demonstrated remarkable capabilities. However, their deployment is hindered by significant computational costs. Existing structured pruning methods, while hardware-efficient, often suffer from significant accuracy degradation. In th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**POP: Prefill-Only Pruning for Efficient Large Model Inference**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

大型语言模型（LLMs）和视觉-语言模型（VLMs）在推理时面临巨大的计算开销，尤其是在 **prefill 阶段**（处理输入序列并生成 KV Cache）。现有的 **structured pruning** 方法虽然硬件友好，但在开放生成任务中常导致严重精度下降。

作者指出，这种失败源于“**stage-agnostic**”（阶段无关）的剪枝策略——即对 prefill 和 decode 阶段采用相同的精简模型结构，忽略了二者功能上的不对称性。

---

### 🚀 提出了什么新方法或新思路

提出 **Prefill-Only Pruning (POP)** ——一种**阶段感知**（stage-aware）的剪枝策略：

- **仅在 prefill 阶段剪枝深层网络层**，因为这些层在上下文编码中是冗余的；
- **decode 阶段保留完整模型**，以确保 next-token 预测的准确性。

为此引入两个关键技术机制：

1. **Virtual Gate Mechanism**  
   用于量化每一层的重要性，通过梯度估计移除某层后损失的变化，避免实际删除再重训。

2. **Independent KV Projections**  
   即使跳过某层的 Attention 和 FFN 计算，仍独立执行其 Key/Value 投影，保证 KV Cache 完整性。

3. **Boundary Handling**  
   将最后一个输入 token 的处理划归为 decode 阶段，用完整模型预测第一个输出 token，防止精度下降。

---

### 🔍 相比现有方法的优势

| 维度 | 现有 Structured Pruning | POP |
|------|--------------------------|-----|
| 剪枝策略 | 阶段无关（prefill & decode 同步剪枝） | **阶段感知**（仅 prefill 剪枝） |
| 准确率保持 | 在生成任务上崩溃（如 GSM8K 下降至 <1%） | 接近全模型性能（>95%） |
| 硬件兼容性 | 高（结构化） | 高（结构化） |
| 加速效果 | 有限 | 最高达 **1.37× prefill speedup** |
| 无需重训练 | 是 | 是 |

> ✅ **核心优势**：在不牺牲 decode 性能的前提下，显著加速 prefill 阶段，突破了传统 structured pruning 中“精度-效率”的权衡困境。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 类型 | 数据集 |
|------|--------|
| 文本校准（Calibration） | `WizardLM-V2-196K`（200 样本） |
| 多模态校准 | `LLAVA-Instruct-150K`（200 样本） |
| 评测基准 | `MMLU`, `HellaSwag`, `Winogrande`, `PIQA`, `GSM8K`, `HumanEval`, `MultiFieldQA`, `HotpotQA`, `MMMU`, `RealWorldQA`, `TextVQA`, `ScreenSpot` |

---

### ⚙️ 实验设置和评估指标

| 项目 | 设置 |
|------|------|
| 模型 | Llama-3.1-8B-Instruct, Qwen3-VL-8B-Instruct, Gemma-3-12B-It |
| 剪枝比例 | 默认剪去最后 1/3 层（≈33%） |
| 硬件 | NVIDIA A100 80GB GPUs |
| 批次大小 | 8（speedup 测试），4（ablation） |
| 评估指标 |  
- **Accuracy**: 各任务得分（0-shot 或 generation）  
- **Speedup**: Time-to-First-Token (TTFT) 加速比  
- **FLOPs Reduction**（间接衡量） |

---

### 🔁 基线方法对比

| 方法 | 类型 | 特点 |
|------|------|------|
| **Wanda (30%)** | Unstructured Pruning | 基于权重幅值和激活剪枝，精度高但需稀疏内核支持 |
| **SliceGPT (25%)** | Structured Pruning | 删除权重矩阵行列，依赖 PCA 变换 |
| **ShortGPT (25%)** | Structured Pruning | 基于隐藏状态相似性识别冗余层 |

> 所有 baseline 调整至与 POP 相当的 FLOPs 减少水平进行公平比较。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & 2）

#### ✅ 准确率表现（Avg Score）

| 方法 | Llama-3.1 | Qwen3-VL | Gemma-3 |
|------|-----------|----------|---------|
| Full Model | 70.19 | 74.00 | 63.07 |
| Wanda (30%) | 69.82 | 73.16 | 62.55 |
| SliceGPT (25%) | 34.88 | 32.82 | 18.12 |
| ShortGPT (25%) | — | — | 15.42 |
| **POP (33%)** | **68.47** | **73.16** | **62.95** |

> 💡 **结论**：POP 在大幅剪枝下仍接近全模型精度，远优于 structured baseline，媲美 unstructured 方法。

#### ⚡ 推理速度提升（TTFT Speedup）

| 输入长度 | Llama-3.1 (POP) | Gemma-3 (POP) | Qwen3-VL (POP) |
|----------|------------------|----------------|------------------|
| 32 tokens | 1.22× | 1.02× | ~1.19× |
| 2048 tokens | **1.36×** | **1.37×** | ~1.16× |

> ✅ **长序列优势明显**：随着输入增长，POP 的加速效果显著增强，适用于 compute-bound 场景。

---

### 🔍 消融实验结果（Ablation Study）

使用 Qwen3-VL 进行验证（见 Table 3 & 4）：

| 变体 | GSM8K | HotpotQA | 说明 |
|------|-------|----------|------|
| Full Model | 81.50 | 65.49 | 基线 |
| POP (完整) | 80.21 | 63.13 | 性能几乎无损 |
| Shallow Pruning | 0.15 | 0.00 | 错误地剪浅层 → 完全失效 |
| Interleaved Pruning | 56.48 | 6.81 | 非深层层剪枝无效 |
| w/o Indep. KV | 2.05 | 1.18 | 缺失 KV → 无法访问历史 |
| w/o Boundary | 77.33 | 11.45 | 最后 token 剪枝 → 显著降级 |

> ✅ **设计必要性验证**：
- 必须剪**深层**
- 必须保留**独立 KV 投影**
- 必须启用**边界处理**

---

### 📈 剪枝比例敏感性分析（Table 4）

| 剪枝比例 | Speedup | GSM8K | HotpotQA |
|---------|---------|--------|-----------|
| 20%     | 1.19×   | 83.09  | 65.46     |
| 25%     | 1.25×   | 82.34  | 65.81     |
| 33% (default) | **1.37×** | 80.21  | 63.13     |
| 50%     | 1.67×   | 78.54  | 34.69     |
| 60%     | 1.96×   | 38.51  | 5.45      |

> ⚠️ **建议剪枝上限为 50%**，超过将导致性能急剧下降，尤其影响长上下文理解能力。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Deep layers are critical for decode, but redundant for prefill**  
   深层在网络生成阶段至关重要，但在上下文编码阶段贡献极小。

2. **Stage-aware pruning breaks the accuracy-efficiency trade-off**  
   区分 prefill 和 decode 阶段的功能差异，可实现高效且准确的剪枝。

3. **POP enables up to 1.37× prefill speedup with minimal accuracy loss**  
   在多种 LLM/VLM 上均有效，且无需 retraining 或 special hardware。

4. **Attention mechanism exhibits functional robustness**  
   即使隐藏状态出现漂移（drift），attention output 仍保持高保真（cosine similarity > 0.96），解释了为何跳过深层不会导致崩溃。

---

### ⚠️ 方法的局限性

1. **不减少峰值显存占用（peak VRAM usage）**  
   decode 阶段仍需加载完整模型，因此不适合 memory-bound 场景。

2. **当前实现在单体系统中运行**  
   未适配 disaggregated inference systems（如分离 prefill/decoce 实例），限制集群级吞吐优化潜力。

3. **静态剪枝策略**  
   当前为固定模式（剪最后 1/3 层），未来可探索动态自适应剪枝。

---

### 🔮 未来工作方向

- 将 POP 集成到 **disaggregated serving system**（如 DistServe）中，充分发挥阶段分离优势。
- 探索 **dynamic POP**：根据输入长度或内容自动调整剪枝深度。
- 结合其他优化技术：如 **token pruning**, **sparse attention**, **quantization**，形成联合加速方案。
- 扩展至更多架构（如 encoder-decoder models）和应用场景（如 RLHF 推理）。

---

> **一句话总结**：  
> **POP 通过“只在 prefill 阶段剪枝深层”的 stage-aware 策略，在几乎不失效的情况下实现了高达 1.37× 的 prefill 加速，为高效大模型推理提供了简单、实用且可插拔的新范式。**

</details>

---

### 7. [MatGPTQ: Accurate and Efficient Post-Training Matryoshka Quantization](https://arxiv.org/abs/2602.03537)

**Authors**: Maximilian Kleinegger, Elvir Crn\v{c}evi\'c, Dan Alistarh  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.03537v1  

#### Abstract
Matryoshka Quantization (MatQuant) is a recent quantization approach showing that a single integer-quantized model can be served across multiple precisions, by slicing the most significant bits (MSB) at inference time. This enables a single checkpoint to cover a wide range of memory and latency budg...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MatGPTQ: Accurate and Efficient Post-Training Matryoshka Quantization

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
Matryoshka Quantization (MatQuant) 是一种新兴的量化方法，允许通过在推理时“切片”最高有效位（MSB）来从一个高精度整数量化模型中生成多个低精度变体，从而实现**单个检查点支持多精度部署**。然而，原始 MatQuant 存在以下关键限制：
- 依赖昂贵的 **Quantization-Aware Training (QAT)** 或 block-wise QAT（如 OmniQuant），而非高效的 one-shot **Post-Training Quantization (PTQ)**；
- 缺乏开源实现和高效推理内核（kernel）支持；
- 未提供针对异构（非均匀）层间比特分配的优化策略。

### 提出了什么新方法或新思路
本文提出了 **MatGPTQ**，首个高效且开源的 **Post-Training Matryoshka Quantization** 框架，其核心创新包括：

- **多精度联合优化目标**：将 Matryoshka 量化建模为一个新的多精度 GPTQ 目标函数，引入 **bit-slicing** 和 **cross-bit error compensation**，使得单次 PTQ 过程即可生成一个能被切片到多种目标比特宽度（如 {2,3,4,6,8}）的“嵌套”父模型。
- **异构比特搜索机制**：集成基于进化算法的 **EvoPress**，在固定内存预算下自动搜索每层最优的非均匀比特配置（Mix-and-Match），进一步提升精度-压缩权衡。
- **高效 CUDA 内核实现**：首次提供了支持 2–8 bit 多重嵌套精度的 **GPU 推理内核**，支持动态切片和混合精度执行，并已集成至 vLLM 等主流推理框架。

### 相比现有方法的优势
| 维度 | MatQuant (Nair et al., 2025) | MatGPTQ (本文) |
|------|----------------------------|----------------|
| **量化方式** | QAT / Block-wise QAT (OmniQuant) | One-shot PTQ (GPTQ-style) |
| **效率** | 高计算成本，需微调 | 仅需小规模校准集，快速完成 |
| **开源与可用性** | 无公开代码/模型/内核 | 完全开源，含训练、量化、推理全流程 |
| **灵活性** | 仅支持 FFN 层，有限比特组合 | 支持 FFN + Attention，任意比特集合 |
| **异构量化** | 仅有概念，无实际方案 | 提供 EvoPress 搜索，实现 Pareto 超优配置 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **校准数据集（Calibration Data）**：
  - `FineWeb-Edu` 数据集，使用 2,097,152 个 token（1024 个样本 × 序列长度 2048）进行 PTQ 校准。
- **评估数据集**：
  - **Perplexity**: `WikiText2` 测试集
  - **Zero-shot Accuracy**: `ARC-c`, `ARC-e`, `HellaSwag`, `PIQA`, `Winogrande`

### 实验设置和评估指标
- **模型**：
  - `LLaMA 3.1-8B`（Instruct / Base）
  - `Qwen3-8B` / `Qwen3-14B`
  - `Phi-3-Medium`
- **目标比特宽度集合**：`R = {3, 4, 8}`，主比特宽度 `c = 8`
- **评估指标**：
  - **PPL (Perplexity)**：越低越好
  - **Task Average Accuracy (%)**：越高越好
  - **End-to-End Latency / Tokens per second**：衡量推理速度
- **硬件平台**：NVIDIA RTX A6000，CUDA 12.4

### 基线方法对比
- **GPTQ**：标准 one-shot PTQ 方法，在各目标比特宽度上独立量化作为基准。
- **MatQuant (OmniQuant)**：基于 block-wise QAT 的 MatQuant 实现（作者复现），仅量化 FFN 层。
- **MatGPTQ-EP**：MatGPTQ + EvoPress 异构搜索版本。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 LLaMA 3.1-8B-Instruct 为例）

| Bitwidth | Method       | PPL  | Task Avg (%) | Rel. Speedup |
|----------|--------------|------|---------------|-------------|
| 16       | FP16         | 7.23 | 74.00         | 1.00×       |
| 8        | GPTQ         | 7.23 | 73.96         | ~2.0×       |
| 8        | **MatGPTQ**  | 7.46 | **73.35**     | ~2.0×       |
| 4        | GPTQ         | 7.84 | 72.65         | ~2.6×       |
| 4        | **MatGPTQ**  | 7.82 | **72.62**     | ~2.6×       |
| 3        | GPTQ         | 11.85| 64.57         | ~2.9×       |
| 3        | **MatGPTQ**  | 10.16| **67.56**     | ~2.9×       |
| 6 (interp)| GPTQ       | 7.23 | 73.28         | —           |
| 6 (interp)| **MatGPTQ** | 7.55 | **73.11**     | —           |

> 注：3-bit 下 MatGPTQ 相对 GPTQ **平均提升 1.34% 准确率**

### 与基线方法的对比结果
- **vs. GPTQ**：
  - 在 4–8 bit 区间，性能几乎持平（差异 < 0.7%），可视为无损。
  - 在 **3-bit 极端压缩场景下显著优于 GPTQ**（+1.34% 平均准确率），归因于多精度联合优化带来的正则化效应。
  - 插值能力优秀：6-bit 切片模型性能接近显式训练的 6-bit GPTQ 模型。
- **vs. MatQuant (OmniQuant)**：
  - 在 FFN-only 设置下，MatGPTQ **全面超越** MatQuant 所有比特宽度下的表现（见 Table 2）。
  - 例如在 Gemma2 9B 上，8-bit 任务平均准确率高出 **0.4%**，3-bit 高出 **0.02%**，且无需微调。
- **vs. 异构方法**：
  - **MatGPTQ-EP**（+EvoPress）在平均比特宽 3 的情况下，准确率仍可达甚至超过均匀 3-bit GPTQ，证明其 Pareto 超优性。

### 消融实验结果
#### （1）损失权重 $ \lambda_r $ 影响（Table 12）
- 测试不同 $ \lambda_3:\lambda_4:\lambda_8 $ 权重组合（如 (1,1,1), (1,0.5,0.1) 等）
- 发现 **uniform weighting ($\lambda_r=1$)** 表现最佳或接近最优
- 结论：除非特定场景强调某比特精度，否则统一加权是简单有效的默认选择。

#### （2）异构量化效果（Figure 1 & Table 20）
- 使用 EvoPress 可生成 **2.5–4 bit 之间的连续有效模型**
- 在 **2.5-bit** 即可获得可用模型（Task Avg ≈ 59.3%）
- **3.0-bit MatGPTQ-EP** 准确率（68.58%）**超过** 均匀 3-bit GPTQ（64.57%）和 MatGPTQ（67.56%）
- 显示 Mix-and-Match 策略能有效缓解低比特量化损失。

#### （3）推理性能（Table 3 & Figure 4）
| Bitwidth | Median Latency (ms) | Tokens/s | Rel. Speedup |
|----------|----------------------|----------|-------------|
| 16       | 23.53                | 42.5     | 1.00×       |
| 4        | 9.13                 | 109.3    | 2.58×       |
| 3        | 8.03                 | 124.4    | 2.93×       |
| 2        | 7.24                 | 138.0    | **3.25×**   |

- **3-bit 推理接近 3× 加速**，2-bit 达到 **3.25×**，显示实际部署价值。
- 内核在 memory-bound 场景下提速 **3–5.6×**（Appendix H）。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **MatGPTQ 成功实现了高效的 one-shot Matryoshka PTQ**，无需 QAT 即可生成高质量的多精度切片模型。
2. ✅ 在 **高比特（4–8 bit）保持与 GPTQ 相当精度**，在 **低比特（3 bit）显著优于 GPTQ**，验证了多精度联合优化的正则化优势。
3. ✅ 支持 **bit-width interpolation**（如 6-bit），即使未显式优化也能保持良好性能。
4. ✅ **MatGPTQ-EP** 通过 EvoPress 搜索实现 **Pareto 超优的异构配置**，可在更低平均比特下达到更高精度。
5. ✅ 自研 CUDA 内核带来 **高达 3.25× 的端到端推理加速**，具备实际部署可行性。

### 方法的局限性
- 当前仅支持 **整数量化**，尚未扩展至浮点格式（如 FP8），后者在 MSB 切片时面临指数级数值变化挑战。
- 极低比特（如 1-bit）仍难以保证可用性，当前 GPTQ 算法在标准 group size 下表现不佳。
- 动态 token 级比特分配尝试失败（Appendix C），表明此类路由需在训练阶段显式学习。

### 未来工作方向
- 探索将 Matryoshka 目标与更适合超低精度的方法结合（如 SqueezeLLM、Any-Precision LLM）。
- 扩展至 **floating-point quantization**，解决 MSB 切片的数值稳定性问题。
- 支持更多 GPU 架构（如 Hopper、Blackwell），并深度集成至 vLLM、TensorRT-LLM 等生产级推理引擎。
- 研究训练时引入动态比特路由机制，以实现更细粒度的自适应推理。

---

> **代码地址**：[https://github.com/IST-DASLab/MatGPTQ](https://github.com/IST-DASLab/MatGPTQ)  
> **一句话总结**：MatGPTQ 将 Matryoshka Quantization 从昂贵的 QAT 范式转变为高效、开源、实用的 PTQ 方案，推动了“单模型、多精度”部署的普及。

</details>

---

### 8. [Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization](https://arxiv.org/abs/2602.01090)

**Authors**: Yang Liu, Chuan Zhou, Yancheng Chen, Shuai Zhang, Xixun Lin, Xiaoqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01090v1  

#### Abstract
Large language models (LLMs) have emerged as promising general-purpose solvers for combinatorial optimization (CO), yet they fundamentally lack mechanisms to guarantee solution feasibility which is critical for real-world deployment. In this work, we introduce FALCON, a framework that ensures 100\% ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在组合优化（Combinatorial Optimization, CO）任务中展现出强大的模式识别和序列生成能力，但其本质是**无约束的生成模型**，无法保证生成解满足硬性约束条件（如容量限制、哈密顿回路等）。这导致现有方法的可行性率（Feasibility Rate）始终低于100%，严重限制了其在物流、制造、应急响应等高风险场景中的实际部署。

### **提出的新方法：FALCON**
本文提出了 **FALCON**（Feasibility-Aware Language-based Combinatorial Optimization with Adaptive Inference），一个首个能提供**100%可行性保证**的LLM-based CO框架，其核心创新在于三层协同机制：

#### **(i) Grammar-Constrained Decoding（语法约束解码）**
- 使用**上下文无关文法**（CFG）对每个CO问题定义输出格式（如TSP路径、CVRP多路线等）。
- 在解码过程中通过**下推自动机**（PDA）动态屏蔽非法token，确保输出**语法正确**（format validity）。
- 例如：防止节点索引越界、括号不匹配等问题。

#### **(ii) Feasibility Repair Layer（可行性修复层）**
- 设计**问题特定的修复算子**（repair operator），将任何违反语义约束的解转换为可行解。
- 满足三大性质：
  1. **可行性**（Feasibility）：输出必为可行解；
  2. **幂等性**（Idempotence）：已可行解不变；
  3. **有界局部性**（Bounded Locality）：修改幅度与违反程度成正比。
- 例如：CVRP中拆分超载路线；MIS中移除相邻顶点。

#### **(iii) Adaptive Best-of-N Sampling（自适应采样）**
- 动态调整采样数量 $N$，基于**解一致性**（consistency）估计实例难度。
- 使用**贝叶斯置信度**决定是否提前终止，避免对简单实例过度采样。
- 显著提升推理效率。

### **相比现有方法的优势**
| 维度 | 现有方法 | FALCON |
|------|--------|--------|
| 可行性保证 | 软目标，不可控（通常<100%） | **理论可证100%** |
| 解质量 | 依赖奖励塑形，信号稀疏 | 引入**BOPO**训练，监督更密集 |
| 推理效率 | 固定采样数，资源浪费 | 自适应分配计算资源 |
| 泛化性 | 需人工标注偏好 | 完全自动化训练 |

此外，提出 **BOPO**（Best-anchored Objective-guided Preference Optimization）训练算法：
- 利用“最优锚定”策略构建偏好对；
- 按**目标值差距**加权梯度更新，使差距大的样本贡献更大；
- 理论证明其收敛速率为 $O(1/\sqrt{T})$，优于GRPO等基线。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在**七个NP-hard CO问题**上进行评估，覆盖三大领域：

| 问题类别 | 具体问题 | 规模 | 分布 | 参考求解器 |
|---------|--------|------|------|-----------|
| **Routing** | TSP, CVRP, OP | 10–100节点 | Uniform, GM | LKH-3, COMPASS |
| **Graph** | MIS, MVC | 50–500节点 | ER, BA | Gurobi |
| **Scheduling** | PFSP, JSSP | 10–100作业 / 6–30作业 | Taillard | QIG, OR-Tools |

所有数据集均为合成生成，训练集50万实例，测试集各100个。

### **实验设置与评估指标**

#### **主干模型**
- 基础模型：`Qwen2.5-7B`
- 微调方式：LoRA（r=64）
- 训练两阶段：
  1. **SFT**：监督微调于专家解；
  2. **BOPO**：基于偏好优化进一步精炼。

#### **推理配置**
- 最小采样数 $N_{\min}=8$
- 最大采样数 $N_{\max}=64$
- 置信阈值 $T=0.85$
- 温度 $T=0.7$

#### **评估指标**
| 指标 | 定义 |
|------|------|
| **Feasibility (%)** | 生成解满足所有约束的比例 |
| **Optimality Gap (%)** | $(f(\hat{x}) - f(x^*)) / f(x^*) \times 100\%$，相对于参考最优解 |
| **Inference Time (s)** | 单实例平均推理时间 |

### **基线方法对比**
共四类基线：
1. **通用LLM零样本**：GPT-4o, Claude-3.5, Llama-3.3-70B, Qwen2.5-72B
2. **推理增强LLM**：GPT-o系列, DeepSeek-R1
3. **LLM优化方法**：OPRO, LMEA, PHP, SGE
4. **神经CO求解器**：SFT-only, GRPO, LLMCoSolver

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自Table 2）**

| 方法 | 平均可行性 (%) | 平均Gap (%) | 推理时间 (s) |
|------|----------------|-------------|--------------|
| **FALCON (N=1)** | **100** | 1.89 | 5.8 |
| **FALCON (N=8)** | **100** | 0.95 | 10.4 |
| **FALCON (Adaptive)** | **100** | **0.92** | **6.3** |
| LLMCoSolver (N=8) | 94–100 | 1.03–10.94 | 9.8 |
| GRPO | 91–98 | 1.34–8.27 | 5.6 |
| GPT-4o | 6–88 | 11.70–212.00 | 5.3 |

> ✅ **FALCON在所有7个问题上实现100%可行性**  
> ✅ **自适应版本以更少采样达到最佳或次优Gap**

### **与基线方法对比结果**
- **可行性方面**：
  - 通用LLM可行性极低（如CVRP仅6%），即使强模型（Claude-3.5-Haiku）也难稳定。
  - LLMCoSolver虽接近100%，但在MIS上仍为94%（N=8），**无法理论保证**。
  - **FALCON首次实现100%可行性且可证明**。

- **解质量方面**：
  - FALCON(N=8)在5/7问题上取得**最优Gap**，其余为第二。
  - 自适应采样下，平均Gap仅比固定N=8高0.03%，但节省**55%推理时间**。

- **对比传统启发式（Table 3）**
  - 远超Nearest Neighbor、FIFO等简单规则；
  - 优于ACO、OR-Tools等经典求解器，尤其在大规模实例上表现更鲁棒。

### **消融实验结果（Table 4 & Figure 2）**

| 配置 | TSP Gap | CVRP Gap | 可行性 (%) |
|------|--------|----------|------------|
| **完整FALCON** | 0.92 | 3.52 | 100 |
| w/o Grammar | 0.95 | 3.67 | 100 |
| w/o Repair | 0.89 | 3.41 | 94 / 85 |
| w/o Adaptive | 0.91 | 3.48 | 100 |
| w/o BOPO | 1.85 | 5.89 | 100 |
| SFT Only | 2.30 | 6.02 | 89 / 59 |

#### **关键发现**：
- **修复层最关键**：移除后可行性骤降，验证其不可替代性。
- **BOPO显著提升性能**：相比GRPO，在CVRP上Gap降低29%，MIS可行性提升12个百分点。
- **语法约束作用较小但必要**：虽影响不大，但保障了解析稳定性。
- **自适应采样高效**：减少约58%采样量（见Table 5），维持高质量。

---

## **4. 关键结论和发现**

### **主要发现**
1. **分离式架构有效**：将“语法有效性”与“语义可行性”分离处理，分别由**语法约束解码**和**修复层**负责，既能保证严格可行性，又不影响生成灵活性。
2. **100%可行性可达成且无代价**：通过修复操作引入的质量损失极小（因BOPO训练出的解本身高度可行），实现了**可靠性与性能的双赢**。
3. **BOPO优于传统偏好学习**：目标感知加权机制提供了更密集、更有区分度的学习信号，尤其在复杂约束问题上优势明显。
4. **自适应采样提升效率**：利用解一致性作为难度代理，自动调节计算预算，实现“易题快解，难题深搜”。

### **方法的局限性**
- **修复可能引入偏差**：虽然理论上修复不会劣化太多，但在极端情况下（如初始解完全混乱），修复后的解可能远离全局最优。
- **依赖良好初始分布**：若SFT阶段未能学到基本结构，修复负担过重，影响效率。
- **扩展性挑战**：当前修复算子为手工设计，难以泛化到全新问题类型；未来需探索自动构造修复逻辑的方法。

### **未来工作方向**
1. **自动化修复算子生成**：结合程序合成技术，从问题描述自动生成修复函数。
2. **端到端可微修复**：探索将修复过程嵌入模型内部，实现梯度传播，进一步缩小Gap。
3. **多目标与随机CO**：扩展至带不确定性的现实场景（如随机需求CVRP）。
4. **真实世界部署验证**：在工业级物流调度、芯片布线等系统中实测效果。

---

> 📌 **一句话总结**：  
> FALCON首次实现了**理论可证100%可行性**的LLM-based组合优化框架，通过**语法约束 + 修复层 + 自适应采样 + BOPO训练**四重创新，在保持竞争力解质量的同时，解决了生成模型用于关键系统的根本可信性问题。

</details>

---

### 9. [Large-Scale LLM Inference with Heterogeneous Workloads: Prefill-Decode Contention and Asymptotically Optimal Control](https://arxiv.org/abs/2602.02987)

**Authors**: Ruihan Lin, Zezhen Ding, Zean Han, Jiheng Zhang  
**Category**: cs.DC  
**Published**: 2026-02-04  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.02987v1  

#### Abstract
Large Language Models (LLMs) are rapidly becoming critical infrastructure for enterprise applications, driving unprecedented demand for GPU-based inference services. A key operational challenge arises from the two-phase nature of LLM inference: a compute-intensive \emph{prefill} phase that processes...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Large-Scale LLM Inference with Heterogeneous Workloads: Prefill-Decode Contention and Asymptotically Optimal Control

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文针对大规模 **Large Language Model (LLM)** 推理服务中的核心挑战——**prefill-decode contention** 和 **workload heterogeneity**，提出了一套系统性的解决方案。

- **Prefill-Decode Contention**：LLM推理分为两个阶段：
  - **Prefill**：计算密集型，处理用户输入提示（prompt）。
  - **Decode**：内存带宽密集型，逐个生成输出token。
  当两者共享同一GPU资源时，prefill任务会显著拖慢共存的decode任务，形成资源竞争。
  
- **Workload Heterogeneity**：不同应用（如摘要、创意写作、问答）具有差异巨大的输入/输出长度分布，导致资源利用不均衡。例如：
  - 摘要任务：长输入、短输出。
  - 创意写作：短输入、长输出。

传统调度策略难以在异构负载下实现最优收益与服务质量（SLI）的平衡。

---

### 提出的新方法或新思路

论文提出了一个基于 **stochastic control** 和 **queueing network** 的理论框架，核心是 **gate-and-route 控制架构**：

#### （1）多类多服务器排队网络建模
- 将每个GPU建模为两种模式：
  - **Mixed Mode**：运行一个prefill任务 + 最多 $B-1$ 个decode任务。
  - **Solo Mode**：仅运行最多 $B$ 个decode任务。
- 服务速率是**状态依赖**的：mixed mode下的decode速度因prefill的计算压力而变慢。

#### （2）流体近似（Fluid Approximation）与稳态优化
- 在大规模GPU集群（$n \to \infty$）下，采用**many-server fluid limit**分析系统行为。
- 将随机系统收敛到确定性流体模型，并将其转化为一个**稳态线性规划（Steady-State Linear Program, LP）**。
- 该LP求解最优的：
  - GPU在mixed/solo模式间的容量分配。
  - 各请求类别的prefill占用率目标。
  - decode任务的路由比例。

#### （3）可实施的控制策略：Gate-and-Route Policy
- **Prefill Gate**（准入门控）：
  - 动态调节prefill任务的准入，确保各类别prefill的实际占用率逼近LP求得的目标值。
  - 采用负反馈机制：优先接纳当前占用低于目标的类别。
- **Decode Router**（解码头由器）：
  - 决定完成prefill的任务应进入mixed还是solo GPU进行decode。
  - 在基础版本中采用work-conserving策略；在SLI-aware版本中引入随机化路由以精确匹配目标分布。

#### （4）支持多种计费模式与SLI约束
- 支持 **bundled charging**（请求完成后统一收费）和 **separate charging**（prefill和decode分别计费）。
- 可将 **Service Level Indicators (SLIs)** 如公平性（fairness）、延迟（latency）作为约束或惩罚项纳入优化目标。

---

### 相比现有方法的优势

| 方面 | 本文方法 | 传统方法 |
|------|---------|--------|
| **理论保证** | 证明了策略在**many-GPU极限下的渐近最优性**（asymptotic optimality） | 多为启发式规则，缺乏理论保障 |
| **资源利用率** | 显式建模prefill-decode contention，避免decode被过度压制 | 忽视状态依赖的服务速率变化 |
| **异构负载处理** | 多类调度，动态调整各类别准入，最大化token-based revenue | 静态优先级（如FCFS、SRT），无法适应混合负载 |
| **SLI支持** | 可灵活集成公平性、延迟等约束，量化其“影子价格”（shadow price） | 缺乏系统性权衡机制 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **真实LLM推理配置**：基于 **Qwen-4B** 和 **Qwen-8B** 模型，在 **NVIDIA A100-SXM4-40GB** GPU上测量迭代时间。
- **工作负载模拟**：未直接使用真实用户日志，而是基于文献（如Sun et al. 2024, Zheng et al. 2024）统计特征构造两类代表性任务：
  - **Class 0 (Decode-Heavy)**：`P=300`, `D=1000`（如代码生成）
  - **Class 1 (Prefill-Heavy)**：`P=3000`, `D=400`（如论文摘要）

### 实验设置
- **仿真平台**：事件驱动（event-driven）离散事件仿真。
- **参数校准**：
  - 通过实测拟合得到mixed iteration time模型：$T_{\text{mix}} = \alpha + \beta C$
    - Qwen-8B: $\alpha \approx 0.0174$, $\beta \approx 6.2 \times 10^{-5}$
    - Qwen-4B: $\alpha \approx 0.0152$, $\beta \approx 3.6 \times 10^{-5}$
  - Solo decode速率：Qwen-8B约45.45 tokens/s。
- **批大小**：$B = 16$
- **GPU规模**：测试 $n = 5, 20, 50, 200, 500$，验证**scalability**。

### 评估指标
- **Per-GPU Revenue**：单位GPU的平均收益（token-based pricing）。
- **Queue Lengths**：prefill和decode队列长度。
- **Occupancy Convergence**：实际资源占用是否收敛至流体模型预测的目标值。
- **Shadow Price of SLIs**：为满足某项SLI需牺牲多少收益。

### 基线方法对比
| 基线名称 | 描述 |
|--------|------|
| **FI-WSP (First-Come-First-Served + Immediate)** | 工业界标准（如Sarathi-Serve），FCFS准入，decode紧随prefill在同一slot执行 |
| **GI-WSP** | 引入本文的Gate控制，但仍采用Immediate执行模式 |
| **GF-WSP** | 使用Gate控制，但decode路由采用FCFS（非greedy） |
| **FG-SP** | 移除Gate控制，仅保留静态规划+Greedy路由 |
| **OPT** | 流体模型的理论上限（fluid optimum） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **收入收敛性**：随着GPU数量增加，**per-GPU revenue** 趋近于流体最优值 $R^*$。
  - 在 $n=500$ 时，已非常接近理论上限。
- **队列稳定性**：prefill和decode队列长度稳定在流体预测水平附近，无持续积压。

### 与基线方法的对比结果
- **GG-SP（本文方法）显著优于所有基线**：
  - 相比工业标准 **FI-WSP**，**提升可达30%以上**。
  - 即使只引入Gate（GI-WSP），也有明显改进。
  - 但单独使用静态规划（FG-SP）反而可能因缺乏流量调控而导致局部失衡。

> 图7显示：GG-SP在各种硬件和负载组合下均取得最高且最稳定的收益。

### 消融实验结果
- **Gate的重要性**：移除Gate（FG-SP）导致收益大幅下降，说明**动态准入控制对防止过载至关重要**。
- **Router的重要性**：使用FCFS而非Greedy路由（GF-WSP）会降低效率，说明**decode优先的work-conserving策略更优**。
- **静态规划的作用**：结合Gate与静态规划才能释放最大潜力。

### SLI敏感性分析（Pareto Frontier）
- **Prefill Fairness** 的影子价格极高：强制各类别prefill占用均衡会导致显著收益损失（结构性错配）。
- **Decode Fairness** 的影子价格极低：一旦请求进入系统，调整其decode速度对总收益影响小。
- **TPOT (Time Per Output Token)**：当目标延迟接近solo decode极限（~0.022s）时，收益急剧下降。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Gate-and-Route策略在大规模下是渐近最优的**：理论与仿真实验一致验证了其有效性。
2. ✅ **Prefill阶段是瓶颈**：对其施加严格的公平性约束代价高昂，应允许系统根据收益最优原则动态调整类别混合。
3. ✅ **Decode阶段更灵活**：可在不影响收益的前提下实现较高公平性。
4. ✅ **Separate Charging可能导致激励错位**：鼓励过度接纳prefill以获取即时收入，加剧下游decode拥塞。建议**按端到端完成计费（bundled charging）来指导调度**。
5. ✅ **硬件升级决策可量化**：通过分析$\beta$（prefill计算成本）和$B$（批大小）对收益的影响，可指导投资方向（如优先降低$\beta$）。

### 方法的局限性
- **假设指数服务时间**：实际decode长度可能不服从指数分布（尽管实验表明MLE拟合尚可接受）。
- **忽略尾部延迟（tail latency）**：流体模型关注平均行为，对p99延迟等SLI缺乏直接控制。
- **同构基础设施假设**：未考虑多代GPU混合部署场景。
- **静态chunk size**：prefill chunk大小固定，未联合优化。

### 未来工作方向
1. **推广至一般服务时间分布**：使用measure-valued过程建模。
2. **发展扩散近似（diffusion approximation）**：用于分析随机波动和尾部延迟。
3. **异构基础设施调度**：支持跨不同型号GPU的协同调度。
4. **联合优化chunk size与调度策略**。
5. **在线学习框架**：应对未知或动态变化的工作负载分布。

--- 

> **总结**：本文首次将**stochastic control理论**系统应用于大规模LLM推理调度，提出了兼具**理论最优性**与**工程可实施性**的 **gate-and-route** 架构，为构建高效、可控、商业友好的LLM服务平台提供了坚实基础。

</details>

---

### 10. [Position: Agentic Evolution is the Path to Evolving LLMs](https://arxiv.org/abs/2602.00359)

**Authors**: Minhua Lin, Hanqing Lu, Zhan Shi, Bing He, Rui Mao, Zhiwei Zhang, Zongyu Wu, Xianfeng Tang, Hui Liu, Zhenwei Dai, Xiang Zhang, Suhang Wang, Benoit Dumoulin, Jian Pei  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.00359v1  

#### Abstract
As Large Language Models (LLMs) move from curated training sets into open-ended real-world environments, a fundamental limitation emerges: static training cannot keep pace with continual deployment environment change. Scaling training-time and inference-time compute improves static capability but do...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Position: Agentic Evolution is the Path to Evolving LLMs》总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前的 **Large Language Models (LLMs)** 在从静态训练环境部署到开放、动态的真实世界场景时面临一个根本性挑战：**train-deploy gap**（训练-部署环境差距）。  
- 模型在有限的训练数据上训练，无法预见真实环境中不断变化的API、接口、用户需求和分布偏移。
- 现有的适应方法（如参数微调或启发式记忆积累）缺乏**战略能动性（strategic agency）**，导致改进不可持续、不可控。

### 提出了什么新方法或新思路

论文提出 **Agentic Evolution（能动演化）** 范式，并构建了通用框架 **A-Evolve**，其核心思想是：

> 将模型的部署期进化过程本身视为一个由“**Evolver Agent**”驱动的、目标导向的优化过程，而非固定流水线。

#### 创新点包括：
- **提出 Agentic Evolution 新范式**：将演化（evolution）从被动、静态更新提升为自主、有目标的决策过程。
- **定义三大原则**：
  1. **Goal-Oriented Principle（目标导向）**：主动诊断失败原因，定位可修复组件。
  2. **Autonomy Principle（自主性）**：决定何时更新、是否提交更新，避免盲目修改。
  3. **Compositional Principle（组合性）**：生成模块化、可验证的持久化构件（如工具、技能、测试）。
- **提出 Evolution-Scaling Hypothesis（演化可扩展性假设）**：
  > 模型的适应能力随着分配给演化过程的 **evolution-time compute** 增加而系统性提升，构成继 training-time 和 inference-time 之后的**第三条扩展轴（scaling axis）**。

### 相比现有方法的优势

| 方法类型 | 典型代表 | 缺陷 | A-Evolve 的优势 |
|--------|--------|------|----------------|
| **Parametric Fine-tuning** | Test-time training, LoFiT | 易发生 **catastrophic forgetting**，更新不透明，难审计 |
| **Non-parametric Heuristic** | APE, AWM, Promptbreeder | 依赖“append-and-retrieve”，导致 **context saturation**，改进收益递减 | 通过结构化构件和验证机制实现**持久、可复用的能力沉淀** |
| **A-Evolve（本文）** | — | — | 支持**有因诊断、自主决策、模块化合成、安全验证**，实现可持续、可扩展的开放域适应 |

---

## 2. 核心实验方法和设置

### 使用的数据集

- **AppWorld** (Trivedi et al., 2024)：一个用于评估工具使用型智能体的基准平台。
  - 包含 9 个日常应用（如 Amazon、Spotify、Venmo）和 457 个 API。
  - 模拟约 100 名虚构用户的数字活动，支持复杂交互任务。
  - 任务需跨多个应用、多步 API 调用完成，具有真实依赖关系。
- **任务划分**：
  - 训练集：50 个任务（用于演化学习）
  - 测试集：50 个任务（来自 `test-normal` 分割）

### 实验设置和评估指标

#### 评估协议
- 固定 **solve-time compute 预算**（最大工具调用次数、步骤数或 token 数）
- 固定 **evolve-time compute 预算**（每 episode 最大 token 和工具调用数）
- 所有方法在相同计算预算下进行公平比较

#### 评估指标
| 指标 | 定义 | 说明 |
|------|------|------|
| **TGC (Task Goal Completion)** | 成功完成的任务比例（所有单元测试通过） | 衡量最终成功率 |
| **APT (Average Passed Tests)** | 每个任务平均通过的单元测试比例 | 衡量渐进式能力提升，反映部分成功 |

### 基线方法对比

| 基线方法 | 类型 | 描述 |
|---------|------|------|
| **Vanilla** | 无演化 | 直接调用 solver，无任何持久化更新 |
| **APE** (Zhou et al., 2022) | 启发式提示演化 | 基于搜索的 prompt 进化，通过任务评分选择候选指令 |
| **AWM** (Wang et al., 2024) | 经验记忆 | 从历史轨迹中提取可重用的工作流并存储 |
| **A-Evolve** | 本文方法 | 基于能动演化的框架，包含诊断、规划、更新、验证四阶段 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Tab. 1）

| Method | Claude Haiku 4.5 (TGC / APT) | Gemini 3 Flash (TGC / APT) |
|--------|-------------------------------|----------------------------|
| Vanilla | 32% / 51.16 | 56% / 80.45 |
| APE     | 30% / 56.00 | 52% / 84.00 |
| AWM     | 46% / 65.76 | 52% / 87.75 |
| **A-Evolve** | **64% / 84.31** | **82% / 92.05** |

> ✅ **A-Evolve 在所有 solver 上均显著优于基线**，尤其对小模型（如 Haiku 4.5）提升巨大。

#### 关键发现：
- **能力放大效应（capability multiplier）**：A-Evolve 可将弱模型（Haiku）的性能提升至接近甚至超过更强模型（Sonnet）的原始水平。
- **缩小容量差距**：Haiku + A-Evolve（64% TGC） > Vanilla Sonnet（42% TGC），表明**程序性能力演化可媲美模型规模提升**。

---

### 消融实验结果（Fig. 3）

消融了 A-Evolve 的四个核心组件：

| 变体 | 描述 | 性能影响 |
|------|------|----------|
| **A-Evolve/D** | 移除 **Diagnosis** | 更新盲目，仅做表面修补，性能下降明显 |
| **A-Evolve/A** | 移除 **Analysis Tools** | 无法跨 episode 发现模式，仅解决一次性错误 |
| **A-Evolve/P** | 移除 **Planning** | 无法协调多构件依赖，更新不一致 |
| **A-Evolve/V** | 移除 **Verification** | 提交缺陷构件（如语法错误工具），导致**回归和污染上下文**，性能最差 |

> 🔍 **关键发现**：完整闭环（诊断 → 规划 → 更新 → 验证）至关重要，其中 **Verification 是稳定器**，防止能力退化。

---

### 演化可扩展性分析（Fig. 4）

#### （a）增加 **evolution step**（即 C_evolve）
- A-Evolve 随 compute 增加**单调提升**，未见饱和。
- AWM 很快达到性能天花板。
- 支持 **Evolution-Scaling Hypothesis**：更多演化计算带来更高适应上限。

#### （b）增大 **evolver 模型尺寸**
| Evolver | TGC (Haiku Solver) | APT |
|--------|--------------------|-----|
| Haiku 4.5 | ~58% | ~80% |
| Sonnet 4.5 | ~64% | ~84% |
| Opus 4.5 | ~70% | ~88% |

> 📈 更大的 evolver 能更准确诊断、生成更鲁棒构件，**验证 evolver 自身能力是 C_evolve 的关键组成部分**。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **Agentic Evolution 是突破 train-deploy gap 的必然路径**：
   - 现有静态方法（fine-tuning 或 memory）无法应对开放世界的持续变化。
   - 必须引入具有目标导向、自主性和组合性的演化机制。

2. ✅ **演化应被视为第三条扩展轴（scaling axis）**：
   - 除了 training-time 和 inference-time compute，**evolution-time compute** 同样可系统性提升模型适应能力。

3. ✅ **A-Evolve 框架有效且可扩展**：
   - 在 AppWorld 上显著优于各类基线。
   - 支持从小模型到大模型的 evolver，且性能随 compute 增加持续提升。

4. ✅ **验证（Verification）是保障长期稳定性的关键**：
   - 无验证的演化会导致“能力漂移”和回归，破坏已有功能。

---

### 方法的局限性

- **计算开销较高**：维护 evolver agent 和执行验证需要额外 compute，短期效率低于轻量级启发式方法。
- **依赖高质量反馈信号**：需要环境提供可观测的错误、轨迹和单元测试，否则难以诊断。
- **当前实现仍基于 LLM 推理**：evolver 本身的可靠性受限于 LLM 的幻觉和推理错误。
- **尚未在真实生产环境大规模验证**：实验基于模拟环境 AppWorld。

---

### 未来工作方向

1. **Benchmark 设计**：
   - 开发专门衡量“演化能力”的基准，关注**持久构件的复用率、演化速度、抗干扰能力**等。

2. **Framework 优化**：
   - 改进诊断、规划、验证模块，提升演化效率和可靠性。
   - 探索 multi-agent evolver 协作机制。

3. **理论建模**：
   - 形式化 agentic evolution 为组合程序空间上的优化问题。
   - 建立 regret bounds，分析其相对于理想 fine-tuning 的收敛性。

4. **安全性与对齐**：
   - 加强验证机制（如形式化验证、人类审查钩子）。
   - 防止目标错位（misaligned objectives）和能力滥用。

---

> 💡 **Impact Statement**：  
> Agentic Evolution 不仅是一种技术改进，更是**范式转变**——从“静态推理”转向“持续自我进化”。它有望大幅提升 AI 系统在动态环境中的**可靠性、隐私性（本地演化）和可持续性**，是通往 AGI 的关键一步。

</details>

---

### 11. [Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach](https://arxiv.org/abs/2602.01131)

**Authors**: Yue Zhong, Jiawen Kang, Yongju Tong, Hong-Ning Dai, Dong In Kim, Abbas Jamalipour, Shengli Xie  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.01131v1  

#### Abstract
With the rapid expansion of the low-altitude economy, Unmanned Aerial Vehicles (UAVs) serve as pivotal aerial base stations supporting diverse services from users, ranging from latency-sensitive critical missions to bandwidth-intensive data streaming. However, the efficacy of such heterogeneous netw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**低空经济（Low-Altitude Economy, LAE）**中无人机（UAV）作为空中基站时面临的资源分配与物理控制稳定性之间的矛盾问题。传统网络设计多关注吞吐量或延迟优化，忽视了通信延迟对控制回路稳定性的直接影响。在灾后应急等安全关键场景中，这种脱节可能导致任务失败。

具体挑战包括：
- 通信资源（带宽、能量）有限；
- 用户服务需求异构（紧急型 vs. 高通量型）；
- 控制系统对端到端延迟高度敏感；
- 边缘设备计算能力受限，难以部署复杂DRL模型。

---

### 提出的新方法与新思路

#### （1）构建 **SC³ 闭环框架（Sensing-Communication-Computing-Control Closed-Loop）**
- 将 Sensing、Communication、Computing 和 Control 四个环节建模为一个统一的动态反馈系统。
- 明确量化了各环节延迟（如 $T_{\text{sense}}, T_{\text{comm}}, T_{\text{compute}}, T_{\text{control}}$）如何累积并影响控制状态更新。

#### （2）引入 **Lyapunov Stability Theory 进行控制-通信协同设计**
- 利用 Lyapunov 函数分析 UAV 状态误差演化过程；
- 推导出满足控制稳定性的**最大允许通信延迟边界**（即 $D_{\text{req}} = (1-\eta_n)\epsilon_n$）；
- 将抽象的“稳定性要求”转化为可量化的**通信资源约束条件**，实现控制理论与网络资源管理的深度融合。

#### （3）建立 **Stackelberg Game 模型进行分层资源调控**
- **领导者（Leader）**：UAV 动态定价带宽资源；
- **追随者（Follower）**：用户根据价格和服务紧迫性决定带宽请求；
- 通过逆向归纳法求解均衡，实现负载均衡与稳定性保障的双重目标。

#### （4）提出 **轻量级 Pruning-based PPO 算法**
- 针对标准 DRL 在边缘平台上的高计算开销问题；
- 引入 **dynamic structured pruning** 机制，在训练过程中逐步剪枝冗余神经元；
- 显著压缩 Actor-Critic 网络规模，降低推理延迟与能耗，适用于资源受限的 UAV 平台。

---

### 相比现有方法的优势

| 维度 | 本文优势 |
|------|--------|
| **系统建模** | 超越传统 throughput-centric 设计，首次将 Lyapunov 稳定性嵌入 SC³ 闭环框架 |
| **资源分配机制** | Stackelberg Game 结合稳定性约束，实现“紧急优先 + 稳定运行”的激励兼容机制 |
| **算法效率** | Pruning-based PPO 实现模型压缩 >50%，显著优于原始 PPO、Greedy 和 Random 方法 |
| **实用性** | 支持在线学习与快速收敛，适合动态变化的 LAE 场景 |

---

## 2. 核心实验方法和设置

### 数据集与仿真环境
- **无真实数据集**，采用**自定义数值仿真平台**模拟 LAE 场景；
- 模拟灾后救援场景：地面基础设施瘫痪，UAV 部署为空中基站恢复通信；
- 用户分布随机生成，信道模型基于自由空间路径损耗（Free-space Path Loss）。

---

### 实验设置

| 参数类别 | 设置值 |
|--------|-------|
| UAV 数量 | 3 或 变量（3–12） |
| 用户数量 | 5–20 |
| 采样周期 $\epsilon_n$ | 0.5 s |
| UAV 计算能力 $f_n$ | 1 GHz |
| 带宽总量 $K_{\text{total}}$ | [15, 25] MHz |
| 用户发射功率 | [100, 300] mW |
| 数据包大小 $S_i$ | [40, 64] kbits |
| Lyapunov 衰减率 $p_n$ | 0.95 |
| 控制时间常数 $\tau_n$ | 0.005 s |
| 学习率 | $1\times10^{-3}$ |
| 折扣因子 $\gamma$ | 0.95 |
| 隐藏层结构 | [256, 256] → 经剪枝后大幅压缩 |

---

### 评估指标

| 指标 | 描述 |
|-----|------|
| **Test Reward / Utility** | UAV 的长期累计效用，反映资源收益与成本平衡 |
| **Convergence Speed** | 算法达到稳定策略所需的迭代次数 |
| **Bandwidth Allocation Profile** | 分析带宽是否向高优先级用户倾斜 |
| **Stability Compliance Rate** | 实际分配带宽是否满足 Lyapunov 导出的最小需求 |
| **Inference Latency & Model Size** | 衡量算法在边缘设备的可行性 |

---

### 基线方法对比

| 方法 | 特点 |
|------|------|
| **Standard PPO** | 不含剪枝的标准多智能体 PPO 算法 |
| **Greedy Algorithm** | UAV 按固定高价出售资源，用户按需申请 |
| **Random Algorithm** | 随机定价与随机请求策略 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 图3：测试奖励对比（Test Reward Comparison）
- **Pruning-based PPO** 最终收敛奖励约为 **8.0**；
- Standard PPO 收敛于约 **7.6**；
- Greedy ≈ 4.0，Random ≈ 2.3；
- **提升幅度**：相比标准 PPO 提升约 **5.3%**，相比 Greedy 提升超过 **100%**。

> 📌 **说明**：剪枝不仅未损害性能，反而因正则化效果提升了泛化能力。

---

#### ✅ 图4：不同剪枝起始轮次的影响
- 最优设置为 **pruning start epoch $t_0 = 50$**（早期剪枝）；
- 收敛奖励最高（~8.0），且方差更小；
- 中期剪枝（$t_0=200$）表现最差，干扰策略形成；
- 启示：**early structural regularization 更有利于稳定学习**。

---

#### ✅ 图5：Stackelberg 策略演化轨迹
- UAV 定价策略（图5a）初期波动大，后期收敛至稳定值（约 3.0–4.5）；
- 用户带宽请求（图5b）随价格调整而动态响应，最终达成均衡；
- 验证了算法能有效逼近 **Stackelberg Equilibrium**。

---

#### ✅ 图6：不同节点密度下的系统性能
- **用户数增加（UAV 固定为3）**：
  - UAV 平均奖励持续上升（从 ~5 到 ~17.9）；
  - 表明机制能激励服务更多用户，提升系统利用率。
- **UAV 数量增加（用户固定为15）**：
  - 单个 UAV 奖励下降（市场饱和，竞争加剧）；
  - 符合经济学规律，验证模型合理性。

---

#### ✅ 图2：带宽分配分解分析
- 所有用户的实际分配带宽均 **严格高于 Lyapunov 推导的最小需求（Min Requirement）**；
- 多余部分用于最大化个体效用（Utility Maximization）；
- 实现了“**稳定性为底线，效用最大化为目标**”的设计理念。

---

### 消融实验（隐含在对比中）

虽然未明确列出消融实验表格，但从以下对比可视为事实上的消融分析：

| 模块移除/替换 | 性能影响 |
|--------------|---------|
| 移除 Lyapunov 约束（即不考虑稳定性） | 系统可能进入不稳定区域，控制失效风险上升 |
| 替换为 Greedy/Random 策略 | 奖励下降超 50%，无法适应动态负载 |
| 不使用 pruning | 模型参数量大，推理延迟高，不适合边缘部署 |
| 使用 unstructured pruning | 硬件加速困难，实际加速比低 |

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **控制稳定性可通过 Lyapunov 理论转化为通信资源约束**，实现了控制层与通信层的紧耦合设计；
2. ✅ **Stackelberg Game 是建模 UAV-User 分层交互的有效工具**，支持激励相容的资源分配；
3. ✅ **Pruning-based PPO 在保持甚至提升性能的同时，显著压缩模型体积**，适合部署于能源受限的 UAV；
4. ✅ **早期结构化剪枝具有正则化作用**，有助于防止过拟合并加快收敛；
5. ✅ 所提方案在多种负载条件下均能维持系统稳定，并实现高效资源利用。

---

### 方法的局限性

| 局限性 | 说明 |
|-------|------|
| **依赖精确的动力学建模** | 需要知道 UAV 控制矩阵 $A, B$，在复杂非线性场景下可能不准确 |
| **假设线性反馈控制器** | 实际飞行控制系统可能更复杂，存在非线性与不确定性 |
| **集中式训练，分布式执行** | 仍需一定程度的信息共享，隐私保护有待加强 |
| **未考虑移动性建模** | 当前 focus 在静态拓扑，未来需扩展至动态拓扑变化 |

---

### 未来工作方向

1. **联合优化能量与时变带宽资源**，研究复合 end-to-end latency 波动下的鲁棒控制；
2. **深化双向 co-design 机制**，让控制策略反向影响通信调度（如主动丢包容忍控制）；
3. **引入联邦学习或去中心化训练机制**，增强隐私性与可扩展性；
4. **结合语义通信（Semantic Communication）**，进一步减少传输开销；
5. **硬件原型验证**：在真实 UAV 平台部署 pruning-based PPO，测试实时性能。

---

> 🔚 **总结一句话**：  
> 本论文开创性地将 **Lyapunov 稳定性理论**融入 **SC³ 闭环资源管理框架**，通过 **Stackelberg Game + Pruning-based PPO** 实现了兼顾**控制可靠性**与**通信高效性**的轻量化智能决策方案，为未来低空经济中的自主 UAV 系统提供了坚实的理论与技术支撑。

</details>

---

### 12. [Performance of Small Language Model Pretraining on FABRIC: An Empirical Study](https://arxiv.org/abs/2602.02632)

**Authors**: Praveen Rao  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.02632v1  

#### Abstract
Large language models (LLMs) require enormous computing power to pretrain on massive datasets. When limited datasets are available, smaller-sized LLMs are better choice to pretrain (on user-specified datasets) by following the scaling laws of LLMs. Using pretrained models, vector embeddings can be g...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Performance of Small Language Model Pretraining on FABRIC: An Empirical Study

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文聚焦于**学术研究者在资源受限条件下预训练 Small Language Models (SLMs)** 的实际挑战。大型语言模型（LLMs）的预训练通常需要昂贵的计算资源（如数千张GPU），这使得大多数学术用户难以参与。本文旨在探索如何利用联邦资助的实验基础设施 **FABRIC**，在无成本、使用消费级GPU的情况下高效地进行 SLM 预训练。

### 提出的新方法/新思路
- **系统性评估多种并行化策略**：全面比较了 **Data Parallelism**、**ZeRO2**、**Shard Parallelism**（仅 intra-operator） 和 **Pipeshard Parallelism**（结合 intra- 和 inter-operator/pipeline 并行）在真实分布式环境中的表现。
- **提出一种自适应选择预训练技术的算法**（Algorithm 1）：基于小规模试运行的结果，动态决定最优的并行策略和GPU配置，以实现最高训练性能、最短总时间，并尽可能减少GPU使用数量。
- **强调地理分布对通信开销的影响**：深入分析了当GPU跨多个FABRIC站点（即地理上分散）时，网络延迟对不同并行策略性能的影响。

### 相比现有方法的优势
- **实用性更强**：不同于工业界追求极致规模的方法，本研究更关注**现实可用资源下的最佳实践**，为学术社区提供了可复现、可操作的指导。
- **揭示了 Pipeshard 在高延迟环境下的鲁棒性**：发现 Alpa 的 Pipeshard 策略在跨站点训练中表现最佳，尤其在网络延迟达到数十毫秒时仍能保持良好性能，而其他方法（如 Shard）因集体通信开销大而严重退化。
- **提供决策支持工具**：提出的 Algorithm 1 是一个实用的工程指南，帮助用户在复杂环境中做出明智的技术选型。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Wikipedia 数据集**（来自 HuggingFace）
- 具体文件：`20231101.ace` —— 一个适中大小的维基百科快照，用于测试预训练性能。

### 实验设置
- **平台**：**FABRIC** —— 由美国国家科学基金会（NSF）资助的全国性可编程研究基础设施。
- **硬件配置**：
  - 构建了 **5 种不同的 GPU 集群切片（slice）**，每种包含两个 VM，每个 VM 挂载 2 个 GPU。
  - 使用三种 GPU 类型：
    - NVIDIA Quadro RTX 6000（RTX，24GB VRAM）
    - NVIDIA Tesla T4（T4，16GB VRAM）
    - NVIDIA A30（Ampere，24GB VRAM）
- **网络连接**：
  - 同一站点内使用 **L2Bridge**（低延迟 ~0.1ms）
  - 跨站点使用 **L2STS**（Layer 2 Site-to-Site Connection），延迟从 20ms 到超过 100ms 不等。
- **软件栈**：
  - **Alpa**：用于生成自动优化的并行执行计划（特别是 Pipeshard）。
  - **Ray**：用于管理集群资源和调度任务。
  - CUDA 11.8, NCCL 2.15.1 等。

### 评估指标
- **总墙钟时间（Wall-clock Time）**：完成 20 个 epoch 所需的时间（分钟）。
- **平均训练性能（Average Training Performance）**：以 **TFLOP/s** 衡量的吞吐量。
- 成功/失败执行情况（是否出现 OOM 错误）。

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Data** | 传统数据并行，模型复制到各设备，处理不同数据分片 |
| **ZeRO2** | 改进的数据并行，通过优化 optimizer state、gradient 和 activation 内存来降低显存占用 |
| **Shard** | Alpa 的 intra-operator 并行（张量切分），不包含 pipeline |
| **Pipeshard** | Alpa 的混合并行策略，结合 shard 和 pipeline 并行 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table I 及 Figures）

| Technique | TACC-TACC (0.1ms) | UTAH-GPN (20.2ms) | UTAH-MASS (57.4ms) | BRIS-STAR (95.9ms) | GAT-AMST (103.0ms) |
|---------|-------------------|-------------------|--------------------|--------------------|---------------------|
| **Data** | 41 min | 136 min | 272 min | 199 min | 1,375 min |
| **ZeRO2** | 52 min | 295 min | 641 min | 363 min | 3,519 min |
| **Shard** | 82 min | 840 min | 1,808 min | 1,125 min | 5,400 min |
| **Pipeshard** | **29 min** | **57 min** | **86 min** | **96 min** | **100 min** |

> ✅ **Pipeshard 在所有跨站点实验中均取得最快训练时间**

### 与基线方法的对比结果
- **在单站点（TACC-TACC）**：
  - 当模型较小（gpt2m）且可放入显存时，**Data** 和 **ZeRO2** 性能优于 Pipeshard（例如 Data 达 15.74 TFLOP/s vs Pipeshard 12.17 TFLOP/s）。
  - **Pipeshard 在单机环境下并无优势**，甚至可能更慢。
- **在跨站点（高网络延迟）**：
  - **Pipeshard 明显胜出**，其训练时间远低于其他方法（如在 GAT-AMST 上仅需 100 分钟，而 Shard 需要 5,400 分钟）。
  - **Shard 受高延迟影响最大**，因其依赖频繁的 GPU-GPU collective communication（通过 TCP/IP 实现，效率低）。
  - **Data 和 ZeRO2 也随延迟增加显著退化**，但不如 Shard 严重。
- **内存方面**：
  - **Pipeshard 内存需求更高**，在异构 GPU 或显存不足时容易失败（如 gpt2L 在 2×RTX 上无法运行）。
  - **ZeRO2 更节省内存**，在 Pipeshard 失败时往往仍能成功运行。

### 消融实验结果
虽然未明确标注“消融”，但实验设计本质上是对比不同并行策略的效果：
- **验证了 Pipeshard 中 pipeline 并行的价值**：其 point-to-point 通信模式比 Shard 的 collective communication 更耐受高延迟。
- **验证了硬件同质性的重要性**：异构 GPU（如 RTX + T4）会限制某些策略的有效性。
- **验证了本地训练优先原则**：若模型能在单个 VM 上运行，通常比跨节点训练更快（即使使用更多 GPU）。

---

## 4. 关键结论和发现

### 主要发现
1. **Pipeshard 是地理分布训练的最佳选择**：  
   在 FABRIC 这类具有较高网络延迟（10–100ms）的分布式环境中，Alpa 的 **Pipeshard** 策略由于其对 **intra- 和 inter-operator parallelism 的联合优化**，表现出最强的鲁棒性和最高的训练效率。

2. **网络延迟严重影响通信密集型策略**：  
   **Shard** 和 **Data Parallelism** 对网络延迟极为敏感，随着延迟增加，性能急剧下降；而 **Pipeshard** 的 pipeline 结构有效缓解了这一问题。

3. **单节点训练通常更高效**：  
   如果模型可以在单个 VM 的 GPU 上容纳，使用 **Data** 或 **ZeRO2** 往往比跨节点使用 Pipeshard 更快，说明“更多 GPU ≠ 更快训练”。

4. **内存约束是关键瓶颈**：  
   Pipeshard 虽然性能好，但内存消耗更大，在异构或小显存设备上易失败；**ZeRO2 是低内存场景下的可靠备选方案**。

### 方法的局限性
- **依赖 Alpa 和 Ray 生态**：实验结果高度依赖于 Alpa 自动生成的执行计划，其通用性受限于该框架的支持程度。
- **仅测试 GPT-2 模型**：结论是否适用于其他架构（如 Llama、Mistral）尚需验证。
- **FABRIC 特定网络特性**：TCP/IP 封装的 NCCL 通信可能放大了 collective communication 的开销，实际情况可能因 RDMA 等高速互连而不同。
- **未考虑大规模扩展**：实验最多使用 4 个 GPU，更大规模集群的行为未知。

### 未来工作方向
- 将该方法论推广至更多类型的 SLMs 和更大规模的集群。
- 探索在 FABRIC 上部署 RDMA 或更高效的通信后端以进一步提升性能。
- 开发更智能的 runtime 自适应调度器，根据实时负载动态切换并行策略。
- 将预训练的 SLM 应用于构建领域特定的 **vector database**，支持 RAG 和语义搜索等下游应用。

> 💡 **最终目标**：通过民主化 SLM 预训练能力，使学术研究人员能够在特定领域（如医疗、法律、教育）构建高质量的嵌入表示，推动科学发现与技术创新。

</details>

---

### 13. [NLI:Non-uniform Linear Interpolation Approximation of Nonlinear Operations for Efficient LLMs Inference](https://arxiv.org/abs/2602.02988)

**Authors**: Jiangyong Yu, Xiaomeng Han, Xing Hu, Chen Xu, Zhe Jiang, Dawei Yang  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.02988v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable performance across a wide range of tasks, but their deployment is often constrained by substantial memory footprints and computational costs. While prior work has achieved significant progress in compressing and accelerating linear layers, no...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：NLI: Non-uniform Linear Interpolation Approximation of Nonlinear Operations for Efficient LLMs Inference

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在推理过程中面临巨大的计算开销，尤其是 **nonlinear operations**（如 SiLU、Softmax、RMSNorm）依赖高精度浮点运算（如 FP32），导致硬件效率低下。尽管线性层已通过量化（如 W8A8）显著加速，非线性层仍成为性能瓶颈。

此外，现有近似方法（如 NN-LUT）存在以下问题：
- **依赖数据校准（calibration-dependent）**：泛化能力差，对训练范围外的输入误差急剧上升；
- **适用范围窄**：仅适用于小范围激活值（如 [-5,5]），而现代 LLMs 的激活值可超过 ±100；
- **硬件不友好**：需要大量比较器（comparators），资源消耗大。

### 🚀 提出的新方法：NLI（Non-uniform Linear Interpolation）
提出一种 **无需校准、动态规划最优、硬件友好的非线性函数近似框架**，包含两个部分：

1. **NLI-Algorithm（软件算法）**
   - 将非线性函数近似转化为 **FP16 域上的非均匀线性插值（non-uniform linear interpolation）**；
   - 利用 **动态规划（Dynamic Programming）** 求解最优切分点（cutpoints），最小化全局插值误差；
   - 遵循 Bellman 最优性原理，在 $O(M \times N^2)$ 时间内找到全局最优解（$M$: 分段数，$N$: 候选点数）；
   - 查找表（LUT）完全由目标函数 $f$ 和数值格式决定，**无需数据校准，跨模型/层可复用**。

2. **NLI-Engine（硬件设计）**
   - 设计了一个通用的、即插即用的非线性计算单元；
   - 引入 **两级地址转换机制（two-level address translation）**：
     - 第一级确定宏区间（macro-interval）；
     - 第二级通过乘法缩放定位子区间索引；
   - 显著减少比较器数量（从 259 → 10），提升能效；
   - 支持流水线设计，维持高吞吐。

### 🔍 相比现有方法的优势
| 维度 | NN-LUT / 其他方法 | NLI（本文） |
|------|------------------|-----------|
| **是否需要校准** | 是（依赖训练数据分布） | 否（纯算法驱动） |
| **输入范围适应性** | 窄（如 [-5,5]） | 宽（支持 [-150,150]） |
| **误差控制** | 外推时误差爆炸 | 近机器精度，最坏误差 < $1.2\times10^{-3}$ |
| **硬件效率** | 高比较器开销 | 极低比较器需求（仅10个） |
| **通用性** | 特定函数优化 | 支持多种函数（exp, rsqrt, SiLU, tanh 等） |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **语言模型任务**：
  - **Wikitext-2**：用于评估 **Perplexity（↓越低越好）**
  - **Zero-shot benchmark suite**：
    - ARC-c/e, BoolQ, PIQA, HellaSwag, OBQA, LAMBADA, SIQA, WinoGrande
  - **综合评测集**：
    - MMLU（多学科知识）
    - GSM8k（数学推理）
    - HumanEval（代码生成）

- **视觉模型验证泛化性**：
  - ViT-Small, DETR, RT-DETR-L, YOLOv8-M

### ⚙️ 实验设置
- **模型覆盖**：
  - LLMs：Llama3-8B/70B, Qwen2.5-7B/32B, Qwen1.5-110B, Qwen3-8B/30B-A3B
  - 所有模型以 FP16 推理运行，替换原有非线性层为 NLI 近似模块
- **NLI 参数配置**：
  - 总切分点数：259（即 2 + 8×32 + 1）
    - 首尾各1个边界点
    - 中间8个宏区间，每个均匀划分为32个子区间
  - 动态规划搜索仅优化11个宏端点，大幅降低搜索时间（~610秒 vs ~17000秒）

### 📊 评估指标
| 类型 | 指标 |
|------|------|
| **准确性** | Perplexity（↓）、MMLU（↑）、GSM8k（↑）、HumanEval（↑）、mAP / Top-1 Acc（视觉） |
| **效率** | Area（μm²）、Power（mW）、Throughput（1G ops/cycle）、Efficiency = Throughput / (Area × Power) |
| **误差质量** | Worst-case absolute error, Relative error, Error distribution visualization |

### 🆚 基线方法对比
- **FP32**：全精度基准
- **NN-LUT**：基于神经网络拟合的 LUT 方法（Yu et al., 2022）
- **Uniform 259**：等距切分点
- **Curvature 259**：按曲率密度采样
- **RI-LUT**：范围不变近似方法（Kim et al., 2023）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 软件层面：几乎无损精度
| 模型 | 方法 | MMLU | GSM8k | Perplexity |
|------|------|-------|--------|------------|
| Llama3-8B | FP32 | 62.16 | 50.19 | 6.14 |
| | **NLI** | **62.14** | **50.49** | **6.14** |
| Qwen2.5-7B | FP32 | 70.56 | 44.28 | 7.46 |
| | **NLI** | **70.67** | **43.97** | **7.46** |
| Qwen3-30B-A3B | FP32 | 77.86 | 85.44 | 8.70 |
| | **NLI** | **77.88** | **85.39** | **8.70** |

> 💡 **结论**：NLI 在所有测试模型上均实现与 FP32 **几乎一致的性能表现**，且显著优于 NN-LUT（后者在多个模型上崩溃，PPL 达 $10^4$ 量级）。

#### ❌ NN-LUT 表现崩坏
- 在 Qwen2.5-32B 上，NN-LUT 导致 **Perplexity 飙升至 70,360**（正常为 5.32），说明其无法处理大范围激活值；
- 多项任务准确率下降超 50%，甚至趋近于随机猜测。

#### 🧪 消融实验（Ablation Studies）

##### Ablation I：两阶段 vs 单阶段切分（Table 2）
| 方法 | Cutpoints | MMLU | GSM8k |
|------|----------|-------|--------|
| NLI (2+8×32+1) | 259 | 70.67 | 43.97 |
| Macro-only (DP, M=11) | 11 | 21.14 | 0 |

> ➤ 使用仅11个切分点（无子划分）会导致严重精度损失，证明 **微观均匀划分的重要性**。

##### Ablation II：直接 DP vs 两阶段 DP（Table 3）
| 方法 | Cutpoints | MMLU | Search Time (s) |
|------|----------|-------|------------------|
| NLI (2+8×32+1) | 259 | 70.67 | 610 |
| Non-uniform 259 (full DP) | 259 | 70.65 | 17,000 |

> ➤ 精度相近，但 **搜索时间快约28倍**，且更利于硬件映射。

##### Ablation III：不同切分策略对比（Table 4）
| 方法 | MMLU | GSM8k |
|------|------|--------|
| Uniform 259 | 29.1 | 18.13 |
| Curvature 259 | 65.74 | 32.58 |
| **NLI** | **70.65** | **43.97** |

> ➤ NLI 显著优于启发式方法，尤其在高曲率区域保持高保真。

#### ⚙️ 硬件层面：效率提升超 4×
| 模块 | Clock | Area (μm²) | Power (mW) | Throughput | Efficiency |
|------|-------|-------------|------------|------------|------------|
| NN-LUT | 1GHz | 23,238 | 46 | 1G | 0.94 |
| RI-LUT | 1GHz | 23,647 | 48 | 1G | 0.88 |
| **NLI** | 1GHz | **7,787** | **34** | 1G | **3.78** |

> ✅ **NLI 效率是 NN-LUT 的 4.02×，是 RI-LUT 的 4.29×**
>
> ✅ 面积节省 **68–69%**，功耗降低约 **26–30%**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **NLI 实现了高精度、免校准、广适用的非线性近似**：
   - 在 LLMs 和 Vision Models 上均无精度损失；
   - 支持输入范围达 [-150,150]，覆盖 >99.9% 的实际激活；
   - 最坏绝对误差控制在 $<1.5\times10^{-3}$ 内。

2. **动态规划可有效求解全局最优切分点**：
   - 将 cutpoint selection 建模为 DP 问题，利用最优子结构性质获得理论最优解；
   - 结合两阶段设计（macro + micro），兼顾精度与效率。

3. **软硬协同设计大幅提升硬件效率**：
   - 两级地址转换将比较器从 259 减少到 10；
   - 流水线结构保证高吞吐；
   - 在 SMIC 28nm 工艺下验证，面积和功耗优势显著。

### ⚠️ 局限性
- **当前仅支持 FP16 输入输出**，未扩展至 INT8 或更低比特；
- **预定义宏区间结构可能限制灵活性**，若某些函数局部变化剧烈，可能需自适应划分；
- **初始化依赖离线 DP 搜索**，虽一次生成可用多次，但仍有一定计算成本。

### 🔮 未来工作方向
- 扩展至 **混合精度支持**（如 INT8 + FP16）；
- 探索 **在线自适应切分机制**，应对极端 outlier；
- 将 NLI 集成进主流 NPU 架构（如 Gemmini、TPU）；
- 应用于 MoE 架构中的 Sigmoid 路由函数近似。

---

> **总结一句话**：  
> NLI 提出了一种 **免校准、动态规划最优、硬件高效** 的非线性函数近似框架，解决了 LLM 推理中非线性层的效率瓶颈，在保持 **零精度损失** 的同时，实现了 **超过 4× 的硬件效率提升**，具有极强的实用性和推广前景。

</details>

---

### 14. [CoBA-RL: Capability-Oriented Budget Allocation for Reinforcement Learning in LLMs](https://arxiv.org/abs/2602.03048)

**Authors**: Zhiyuan Yao, Yi-Kai Zhang, Yuxin Chen, Yueqing Sun, Zishan Xu, Yu Yang, Tianhao Hu, Qi Gu, Hui Su, Xunliang Cai  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.03048v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a key approach for enhancing LLM reasoning.However, standard frameworks like Group Relative Policy Optimization (GRPO) typically employ a uniform rollout budget, leading to resource inefficiency. Moreover, existing adaptive methods...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CoBA-RL: Capability-Oriented Budget Allocation for Reinforcement Learning in LLMs

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
在基于 **Reinforcement Learning with Verifiable Rewards (RLVR)** 的大语言模型（LLM）后训练中，主流方法如 **Group Relative Policy Optimization (GRPO)** 采用**固定的 rollout 预算分配策略**（即每个任务样本生成相同数量的响应），导致资源利用效率低下。简单任务可能被过度探索，而复杂任务因预算不足未能充分学习。

此外，现有自适应预算分配方法（如 Knapsack-RL）通常依赖于静态或启发式的价值函数（例如基于历史通过率），**忽略了模型能力在训练过程中的动态演化**，无法实时调整“探索”（exploration）与“利用”（exploitation）之间的权衡。

### 提出了什么新方法或新思路
本文提出 **CoBA-RL (Capability-Oriented Budget Allocation for Reinforcement Learning)**，一种**基于模型能力演化的动态预算分配算法**，其核心创新如下：

- **Capability-Oriented Value Function**：  
  设计了一个以 **Beta 分布建模的能力感知价值函数**，该函数将每个任务实例 $x_i$ 映射为其潜在训练增益。函数形状由当前训练批次的**全局失败率（Global Failure Rate）** 动态调节，从而反映模型当前能力水平。

- **动态探索-利用权衡机制**：  
  在训练初期，模型能力弱、失败率高，价值函数倾向于优先分配资源给**高通过率（简单）任务**（exploitation），快速建立基础能力；随着模型进步，失败率下降，函数自动转向**低通过率（困难）任务**（exploration），拓展解题边界。

- **高效分配优化算法（Heap-Based Greedy Strategy）**：  
  将预算分配形式化为一个带约束的优化问题，并设计基于最大堆的贪心算法，迭代地将预算单元分配给边际收益最高的样本，实现高效在线分配。

### 相比现有方法的优势
- **动态适应性**：首次显式建模**模型能力演化**对样本训练价值的影响，而非依赖静态难度假设。
- **更高的资源效率**：相比均匀分配（GRPO）和静态策略，能更精准识别“高训练价值”样本，避免计算浪费。
- **理论保障**：证明了价值函数具有**递减边际收益**性质，保证贪心策略可得最优解。
- **计算高效**：堆优化策略时间复杂度为 $O(B_{\text{total}} \log M)$，远优于动态规划（DP）等方法，适用于大规模训练流水线。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：`DAPO-Math-17K` —— 一个广泛用于数学推理任务的强化学习训练集。
- **评估基准**（共5个挑战性数学推理评测集）：
  - `AIME24`, `AIME25`
  - `AMC23`
  - `MATH500`
  - `OLYMPIAD Bench`

### 实验设置和评估指标
- **模型**：
  - `Qwen2.5-7B-Base`, `Qwen2.5-7B-Instruct`
  - `Qwen3-1.7B-Base`, `Qwen3-4B-Base`
- **训练配置**：
  - 每个任务初始 rollout 数量 $G = 16$
  - 总 rollout 预算受限（如 $B_{\text{total}} = 8192$）
  - 使用 **Verl** 框架 + **SGLang** 推理引擎
  - 优化器：AdamW ($lr = 1\times10^{-6}$)
- **评估指标**：
  - 主要指标：`avg@16`（16次采样中的平均准确率）
  - 其他分析：任务难度迁移矩阵、收敛速度、资源效率曲线

### 基线方法对比
| 方法 | 类型 | 说明 |
|------|------|------|
| **GRPO** | 基线 | 固定 rollout 预算，无自适应机制 |
| **Knapsack-RL** | 自适应基线 | 基于经典背包问题建模，最大化批次总价值，但使用静态价值函数 |
| **Static Strategies** | 消融对比 | 固定参数的 Beta 分布（如 $(\alpha,\beta)=(10.5,1.5)$ 强调利用） |
| **Linear Step Decay** | 启发式基线 | 参数 $\alpha$ 随训练步数线性衰减，预设调度 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
在 `Qwen2.5-7B-Instruct` 上的整体表现（Avg@16）：

| Method        | AIME24 | AIME25 | AMC23  | MATH500 | OLYMPIAD | **Avg**  |
|---------------|--------|--------|--------|---------|----------|----------|
| GRPO          | 14.17  | 12.71  | 69.84  | 76.78   | 37.68    | **42.24** |
| Knapsack-RL   | 18.54  | 15.21  | 71.41  | 80.55   | 41.23    | **45.39** |
| **CoBA-RL (Ours)** | **18.96** | **18.33** | **73.12** | **80.30** | **43.19** | **46.78** |

- **相对 GRPO 提升 +4.54%**，显著超越 Knapsack-RL (+1.39%)。
- 在 `AIME25` 上提升尤为明显：从 12.71% → **18.33%**（+5.62%），表明对难题的探索更有效。

在其他模型上也一致领先：
- `Qwen2.5-7B-Base`: **47.43%** vs GRPO 43.68% (+3.75%)
- `Qwen3-4B-Base`: **49.46%** vs GRPO 44.72% (+4.74%)
- `Qwen3-1.7B-Base`: **35.75%** vs GRPO 31.61% (+4.14%)

### 与基线方法的对比结果
- **全面超越 GRPO 和 Knapsack-RL**：在所有模型和大多数子任务上均取得最佳性能。
- **优于静态与启发式策略**（Table 3）：
  - CoBA-RL 达到 **46.78%**，超过最佳静态策略 (45.21%) 和线性衰减 (45.39%)。
  - 表明**动态适应能力演化**比预设规则更有效。

### 消融实验结果
#### （1）不同预算下的性能（Figure 6）
- 当总预算受限为 `B_total=2048` 时，CoBA-RL 准确率达 **45.52%**。
- 此成绩**超过 GRPO 在双倍预算（4096）下的表现（42.78%）**，验证其卓越的数据与计算效率。

#### （2）超参数敏感性分析（Figure 8）
- 对 Beta 分布参数和 $K=\alpha+\beta$ 的变化表现出强鲁棒性。
- 最优设置 $K=11$ 取得峰值性能 **46.61%**。

#### （3）任务难度迁移分析（Figure 7）
- CoBA-RL 在各类难度任务上的转化率均最高：
  - **Hard 任务**：转化率 **36.7%** vs GRPO (17.3%)，接近翻倍。
  - **Medium 任务**：**71.2%** vs GRPO (46.8%)。
  - **Easy 任务保留率**：**88.8%** vs GRPO (74.0%)，显示其在巩固已有知识方面同样出色。

#### （4）运行时效率对比（Table 4）
| 方法 | 分配耗时（秒） | 加速比 |
|------|----------------|--------|
| Dynamic Programming (DP) | 115.05 | ~1× |
| **Heap-Based Greedy (Ours)** | **0.124** | **~928×** |

- 实现近三个数量级的速度提升，满足在线训练需求。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **训练价值是动态的**：样本的“训练价值”并非仅由其固有难度决定，而是**高度依赖于模型当前的能力状态**。
2. **有效的训练范式应是“先利用后探索”**（Exploit → Explore）：
   - 初期聚焦简单任务以快速提升基础能力；
   - 后期转向困难任务以扩展解空间。
3. **量化训练价值并优化预算分配是提升 LLM 后训练效率的关键路径**。
4. CoBA-RL 能**自主校准探索-利用平衡**，无需人工设计调度策略。

### 方法的局限性
- 依赖于**二值奖励信号**（pass/fail），难以处理细粒度反馈。
- 当前框架主要针对数学推理任务，是否泛化至其他领域（如代码生成、对话）需进一步验证。
- Beta 分布建模虽灵活，但仍是一种简化假设，可能无法捕捉更复杂的偏好模式。

### 未来工作方向
- 扩展至多维奖励信号下的预算分配。
- 结合 **curriculum learning** 与 CoBA-RL，实现样本选择与预算分配的联合优化。
- 探索更复杂的 capability modeling 方式（如引入隐变量或神经网络估计）。
- 应用于更大规模模型（>10B）及真实世界 agentic 任务。

---

> ✅ **一句话总结**：  
> CoBA-RL 通过构建一个**随模型能力演化的 Capability-Oriented Value Function**，实现了 rollout 预算的动态、高效分配，在多个数学推理基准上显著超越 GRPO 和 Knapsack-RL，揭示了“**动态感知能力 + 自适应资源调度**”是提升 LLM 强化学习效率的核心范式。

</details>

---

### 15. [Lookahead Path Likelihood Optimization for Diffusion LLMs](https://arxiv.org/abs/2602.03496)

**Authors**: Xuejie Liu, Yap Vit Chun, Yitao Liang, Anji Liu  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.03496v1  

#### Abstract
Diffusion Large Language Models (dLLMs) support arbitrary-order generation, yet their inference performance critically depends on the unmasking order. Existing strategies rely on heuristics that greedily optimize local confidence, offering limited guidance for identifying unmasking paths that are gl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Lookahead Path Likelihood Optimization for Diffusion LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Diffusion Large Language Models (dLLMs) 虽然支持任意顺序生成（arbitrary-order generation），其推理性能却高度依赖于 **unmasking order**（即 token 的解码顺序）。现有的策略通常基于启发式规则（如选择置信度最高或熵最低的 token）进行局部优化，缺乏对全局一致且高精度路径的指导，导致次优解。

### **提出的新方法与新思路**
本文提出了以下三个核心创新：

- **Path Log-Likelihood (Path LL)**  
  引入了一个新的轨迹条件目标函数——**Path LL**，定义为在特定 unmasking 路径下的联合似然。实验证明，Path LL 与下游任务准确率有强相关性，是衡量生成质量的可靠代理指标。

- **POKE (Path-Optimistic K-step LL Estimator)**  
  提出一种高效的值估计器 POKE，用于预测部分解码轨迹在未来可能达到的期望 Path LL。该估计器通过引入熵校正项来缓解并行 rollout 中的独立性偏差，并提供乐观上界以避免过早剪枝高质量路径。

- **POKE-SMC (Sequential Monte Carlo-based Search Framework)**  
  将 POKE 集成到 SMC 框架中，构建一个动态搜索机制，在推理时自适应地识别最优 unmasking 路径。利用重要性权重结合当前累积 LL 和未来估计值，实现价值引导的粒子筛选。

### **相比现有方法的优势**
| 方面 | 优势 |
|------|------|
| **目标设计** | 使用 Path LL 替代传统 ELBO 或熵启发式，更贴近实际任务性能 |
| **搜索效率** | POKE 实现高效 lookahead 评估，无需训练额外的价值网络 |
| **全局优化能力** | 克服贪婪策略的短视问题，探索具有高全局潜力的路径 |
| **可扩展性** | 在保持合理推理开销的同时显著提升准确性，推动 accuracy-compute Pareto frontier |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验覆盖 **6个推理任务**，主要包括：
- **GSM8K**：小学数学应用题，测试多步推理能力
- **MATH500**：高等数学问题集合
- **Countdown**：数值计算挑战任务
- （其他未明确命名的任务也包含在内，共6项）

### **实验设置与评估指标**
- **模型基础**：基于 LLaDA 系列 dLLMs（如 LLaDA-8B-Instruct）
- **推理配置**：
  - 扩散步数 $ T = 256 $
  - 序列长度 256 tokens
  - 并行解码块大小 32
  - 使用零样本设定（zero-shot）
- **评估指标**：
  - 主要：**Accuracy (%)**（答案正确率）
  - 辅助：**Latency (s)**、**accuracy-compute trade-off**
  - 内部验证：Path LL 与最终准确性的相关性分析

### **基线方法对比**
- **PC-Sampler**：基于位置感知偏置校准的方法
- **Majority Voting**：多次采样取多数投票
- **E-SMC**：基于熵的 SMC 方法
- **Random / Confidence-based Heuristics**：随机或基于最大概率选择 unmasking 顺序
- **LookUM (Lee et al., 2025)**：学习型路径规划方法（消融实验中提及）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 方法 | Latency (s) | Accuracy (%) |
|------|-------------|--------------|
| PC-Sampler | 7.9 | 36.1 |
| Majority Voting | 9.5 / 18.0 / 32.6 | 34.0 / 36.0 / 36.9 |
| E-SMC | 9.5 / 18.0 / 32.6 | 35.4 / 38.3 / 39.8 |
| **POKE-SMC** | **8.9 / 22.1 / 34.7** | **36.4 / 42.3 / 40.8** |

> 数据来自 Table 10（Countdown 任务），显示 POKE-SMC 在相近延迟下取得更高准确率。

### **与基线方法的对比结果**
- 在所有 6 个推理任务上，**POKE-SMC 一致优于强基线**。
- 相比当前先进的 decoding-time scaling 方法，平均提升 **2%-3% 绝对准确率**。
- 在高吞吐场景（每步解码两个 token）下仍保持领先，分别在两个 LLaDA 模型上实现 **5.4% 和 4.9% 的平均增益**。
- 显著改进了 **accuracy-compute Pareto frontier**，即在相同计算预算下获得更高准确率，或在相同准确率下降低计算成本。

### **消融实验结果**
- **POKE vs. Product LL**：朴素乘积似然（product LL）严重低估真实联合似然，尤其在大并行度下；而 POKE 能有效弥合差距（见 Figure 2）。
- **K 值影响（lookahead horizon）**：当 $ K=16 $（即每组 $|A|=16$）时效果最佳，说明适度前瞻优于极端短视或长程模拟。
- **resampling interval 与温度敏感性分析**：周期性重采样可在效率与性能间取得良好平衡（详见 Appendix C）。
- **remasking 温度设置**：使用 entropy-based remasking（温度=1.0）有助于维持多样性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Path LL 是优秀的生成质量代理指标**  
   实验表明，Path LL 与下游任务准确率的相关性远高于 ELBO 或边际熵等传统指标，适合作为 inference-time optimization 的目标。

2. **局部最优 ≠ 全局最优**  
   贪婪策略虽快，但容易陷入低质量路径；通过 lookahead 和全局搜索（如 SMC + POKE），可以发现更具潜力的解码顺序。

3. **POKE 实现高效且准确的未来价值估计**  
   利用随机 rollout 加熵修正，POKE 在不增加过多计算负担的前提下提供了可靠的未来 Path LL 上界估计。

4. **POKE-SMC 推动 accuracy-compute 权衡边界前移**  
   在多个任务和不同计算预算下均表现出优越性能，验证了其作为通用 inference-time scaling 框架的潜力。

### **方法的局限性**
- 当前实验仅限于 **8B 规模模型**（如 LLaDA），尚未验证在更大模型（如 LLaDA2.0）上的可扩展性。
- **resampling 策略较简单**，采用固定周期重采样，未引入动态回退或再生机制。
- 依赖于固定的扩散调度和 block size，灵活性有待进一步增强。

### **未来工作方向**
1. **扩展至更大规模 dLLMs**  
   验证 POKE-SMC 在更强 base model 上的效果，探索是否仍能维持增益。
   
2. **开发更智能的 resampling 与 rollback 机制**  
   利用 Path LL 的全局可比性，设计自适应 rollback 策略，当检测到路径偏离高概率区域时主动修正。

3. **探索非单调解码范式**  
   超越传统的“逐步 unmask”模式，研究允许 remasking 与 rethinking 的闭环推理流程。

4. **与其他优化技术融合**  
   结合 RL fine-tuning 或 prompt engineering，形成端到端优化 pipeline。

--- 

> ✅ **总结一句话**：  
> 本论文提出 **Path LL** 作为 dLLMs 解码路径选择的新目标，并通过 **POKE-SMC** 实现高效的 lookahead 搜索，在几乎不增加推理开销的情况下，系统性提升了生成准确率，为 diffusion LLMs 的推理优化开辟了新路径。

</details>

---

### 16. [Quantization-Aware Regularizers for Deep Neural Networks Compression](https://arxiv.org/abs/2602.03614)

**Authors**: Dario Malchiodi, Mattia Ferraretto, Marco Frasca  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.03614v1  

#### Abstract
Deep Neural Networks reached state-of-the-art performance across numerous domains, but this progress has come at the cost of increasingly large and over-parameterized models, posing serious challenges for deployment on resource-constrained devices. As a result, model compression has become essential...

---

### 17. [LLM-Inspired Pretrain-Then-Finetune for Small-Data, Large-Scale Optimization](https://arxiv.org/abs/2602.03690)

**Authors**: Zishi Zhang, Jinhui Han, Ming Hu, Yijie Peng  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.03690v1  

#### Abstract
We consider small-data, large-scale decision problems in which a firm must make many operational decisions simultaneously (e.g., across a large product portfolio) while observing only a few, potentially noisy, data points per instance. Inspired by the success of large language models (LLMs), we prop...

---

### 18. [Scalable and Secure AI Inference in Healthcare: A Comparative Benchmarking of FastAPI and Triton Inference Server on Kubernetes](https://arxiv.org/abs/2602.00053)

**Authors**: Ratul Ali  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00053v1  

#### Abstract
Efficient and scalable deployment of machine learning (ML) models is a prerequisite for modern production environments, particularly within regulated domains such as healthcare and pharmaceuticals. In these settings, systems must balance competing requirements, including minimizing inference latency...

---

### 19. [SEISMO: Increasing Sample Efficiency in Molecular Optimization with a Trajectory-Aware LLM Agent](https://arxiv.org/abs/2602.00663)

**Authors**: Fabian P. Kr\"uger, Andrea Hunklinger, Adrian Wolny, Tim J. Adler, Igor Tetko, Santiago David Villalba  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00663v1  

#### Abstract
Optimizing the structure of molecules to achieve desired properties is a central bottleneck across the chemical sciences, particularly in the pharmaceutical industry where it underlies the discovery of new drugs. Since molecular property evaluation often relies on costly and rate-limited oracles, su...

---

### 20. [Resource-Efficient Reinforcement for Reasoning Large Language Models via Dynamic One-Shot Policy Refinement](https://arxiv.org/abs/2602.00815)

**Authors**: Yunjian Zhang, Sudong Wang, Yang Li, Peiran Xu, Conghao Zhou, Xiaoyue Ma, Jianing Li, Yao Zhu  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00815v1  

#### Abstract
Large language models (LLMs) have exhibited remarkable performance on complex reasoning tasks, with reinforcement learning under verifiable rewards (RLVR) emerging as a principled framework for aligning model behavior with reasoning chains. Despite its promise, RLVR remains prohibitively resource-in...

---

### 21. [Small Generalizable Prompt Predictive Models Can Steer Efficient RL Post-Training of Large Reasoning Models](https://arxiv.org/abs/2602.01970)

**Authors**: Yun Qu, Qi Wang, Yixiu Mao, Heming Zou, Yuhang Jiang, Weijie Liu, Clive Bai, Kai Yang, Yangkun Chen, Saiyong Yang, Xiangyang Ji  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.01970v1  

#### Abstract
Reinforcement learning enhances the reasoning capabilities of large language models but often involves high computational costs due to rollout-intensive optimization. Online prompt selection presents a plausible solution by prioritizing informative prompts to improve training efficiency. However, cu...

---

### 22. [Efficient Algorithms for Partial Constraint Satisfaction Problems over Control-flow Graphs](https://arxiv.org/abs/2602.03588)

**Authors**: Xuran Cai, Amir Goharshady  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.03588v1  

#### Abstract
In this work, we focus on the Partial Constraint Satisfaction Problem (PCSP) over control-flow graphs (CFGs) of programs. PCSP serves as a generalization of the well-known Constraint Satisfaction Problem (CSP). In the CSP framework, we define a set of variables, a set of constraints, and a finite do...

---

### 23. [Neural Attention Search Linear: Towards Adaptive Token-Level Hybrid Attention Models](https://arxiv.org/abs/2602.03681)

**Authors**: Difan Deng, Andreas Bentzen Winje, Lukas Fehring, Marius Lindauer  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.03681v1  

#### Abstract
The quadratic computational complexity of softmax transformers has become a bottleneck in long-context scenarios. In contrast, linear attention model families provide a promising direction towards a more efficient sequential model. These linear attention models compress past KV values into a single ...

---

### 24. [ContextEvolve: Multi-Agent Context Compression for Systems Code Optimization](https://arxiv.org/abs/2602.02597)

**Authors**: Hongyuan Su, Yu Zheng, Yong Li  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.02597v1  

#### Abstract
Large language models are transforming systems research by automating the discovery of performance-critical algorithms for computer systems. Despite plausible codes generated by LLMs, producing solutions that meet the stringent correctness and performance requirements of systems demands iterative op...

---

### 25. [WARP Logic Neural Networks](https://arxiv.org/abs/2602.03527)

**Authors**: Lino Gerlach, Thore Gerlach, Liv V{\aa}ge, Elliott Kauffman, Isobel Ojalvo  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.03527v1  

#### Abstract
Fast and efficient AI inference is increasingly important, and recent models that directly learn low-level logic operations have achieved state-of-the-art performance. However, existing logic neural networks incur high training costs, introduce redundancy or rely on approximate gradients, which limi...

---

### 26. [Sparse Training of Neural Networks based on Multilevel Mirror Descent](https://arxiv.org/abs/2602.03535)

**Authors**: Yannick Lunk, Sebastian J. Scott, Leon Bungert  
**Category**: cs.LG  
**Published**: 2026-02-04  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.03535v1  

#### Abstract
We introduce a dynamic sparse training algorithm based on linearized Bregman iterations / mirror descent that exploits the naturally incurred sparsity by alternating between periods of static and dynamic sparsity pattern updates. The key idea is to combine sparsity-inducing Bregman iterations with a...

---

### 27. [EvoOpt-LLM: Evolving industrial optimization models with large language models](https://arxiv.org/abs/2602.01082)

**Authors**: Yiliu He, Tianle Li, Binghao Ji, Zhiyuan Liu, Di Huang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01082v1  

#### Abstract
Optimization modeling via mixed-integer linear programming (MILP) is fundamental to industrial planning and scheduling, yet translating natural-language requirements into solver-executable models and maintaining them under evolving business rules remains highly expertise-intensive. While large langu...

---

### 28. [Probing RLVR training instability through the lens of objective-level hacking](https://arxiv.org/abs/2602.01103)

**Authors**: Yiming Dong, Kun Fu, Haoyu Li, Xinyuan Zhu, Yurou Liu, Lijing Shao, Jieping Ye, Zheng Wang  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01103v1  

#### Abstract
Prolonged reinforcement learning with verifiable rewards (RLVR) has been shown to drive continuous improvements in the reasoning capabilities of large language models, but the training is often prone to instabilities, especially in Mixture-of-Experts (MoE) architectures. Training instability severel...

---

### 29. [FutureMind: Equipping Small Language Models with Strategic Thinking-Pattern Priors via Adaptive Knowledge Distillation](https://arxiv.org/abs/2602.01222)

**Authors**: Shaoxiong Yang, Junting Li, Mengyuan Zhang, Chao Li, Wei Liu, Jian Luan  
**Category**: cs.AI  
**Published**: 2026-02-04  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01222v1  

#### Abstract
Small Language Models (SLMs) are attractive for cost-sensitive and resource-limited settings due to their efficient, low-latency inference. However, they often struggle with complex, knowledge-intensive tasks that require structured reasoning and effective retrieval. To address these limitations, we...

---

### 30. [ROSA-Tuning: Enhancing Long-Context Modeling via Suffix Matching](https://arxiv.org/abs/2602.02499)

**Authors**: Yunao Zheng, Xiaojie Wang, Lei Ren, Wei Chen  
**Category**: cs.CL  
**Published**: 2026-02-04  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.02499v1  

#### Abstract
Long-context capability and computational efficiency are among the central challenges facing today's large language models. Existing efficient attention methods reduce computational complexity, but they typically suffer from a limited coverage of the model state. This paper proposes ROSA-Tuning, a r...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
