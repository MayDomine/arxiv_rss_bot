# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-01 05:59:56 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Training Report of TeleChat3-MoE](https://arxiv.org/abs/2512.24157)

**Authors**: Xinzhang Liu, Chao Wang, Zhihao Yang, Zhuo Jiang, Xuncheng Zhao, Haoran Wang, Lei Li, Dongdong He, Luobin Liu, Kaizhe Yuan, Han Gao, Zihan Wang, Yitong Yao, Sishi Xiong, Wenmin Deng, Haowei He, Kaidong Yu, Yu Zhao, Ruiyu Fang, Yuhao Jiang, Yingyan Li, Xiaohui Hu, Xi Yu, Jingqi Li, Yanwei Liu, Qingli Li, Xinyu Shi, Junhao Niu, Chengnuo Huang, Yao Xiao, Ruiwen Wang, Fengkai Li, Luwen Pu, Kaipeng Jia, Fubei Yao, Yuyao Huang, Xuewei He, Zhuoru Jiang, Ruiting Song, Rui Xue, Qiyi Xie, Jie Zhang, Zilu Huang, Zhaoxi Zhang, Zhilong Lu, Yanhan Zhang, Yin Zhang, Yanlei Xue, Zhu Yuan, Teng Su, Xin Jiang, Shuangyong Song, Yongxiang Li, Xuelong Li  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 15.0  
**Type**: new  
**ArXiv ID**: 2512.24157v1  

#### Abstract
TeleChat3-MoE is the latest series of TeleChat large language models, featuring a Mixture-of-Experts (MoE) architecture with parameter counts ranging from 105 billion to over one trillion,trained end-to-end on Ascend NPU cluster. This technical report mainly presents the underlying training infrastr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Training Report of TeleChat3-MoE*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本论文旨在解决**超大规模 Mixture-of-Experts (MoE) 模型在 Ascend NPU 集群上端到端训练中的可靠性、效率与可扩展性挑战**。具体包括：
- 跨硬件平台和并行策略下的数值精度不一致问题；
- 大规模分布式训练中通信开销大、负载不均衡、计算利用率低等问题；
- 并行化配置依赖专家手动调优，耗时长且难以复现；
- 集群级性能瓶颈（如主机资源争用、设备节能机制误触发）影响训练吞吐。

### 提出的新方法与创新思路

#### （1）系统化的**数值精度验证框架**
- **Operator-level 验证**：构建基于 CPU 的“黄金标准”基准，结合输入类型转换规则（如 fp16 → fp32）和累加次数相关的误差容忍度表（见 Table 2），实现算子级精度一致性检测。
- **End-to-end 对齐流程**：提出跨硬件迁移和不同并行策略下的两阶段验证流程（单设备对齐 → 渐进式扩展），通过中间张量 dump 和日志分析定位前向/反向传播或优化器行为差异。

#### （2）训练框架性能优化
- **Interleaved Pipeline Scheduling + 1F1B 重叠**：采用非连续层交错分配方式，并结合 1F1B（One Forward One Backward）调度策略，有效减少 pipeline bubble，提升计算与通信重叠率。
- **Attention-aware 数据调度**：针对长序列稀疏注意力训练中存在的负载不均问题，根据文档长度重新组织 micro-batch 内样本分布，平衡各设备上的 attention 计算负担。
- **Hierarchical & Overlapped Communication for EP**：
  - 层次化通信：将全局 All-to-All 替换为 **AllGather → Local Filter → Intra-node All-to-All** 三步流程，显著降低跨节点流量；
  - 通信重叠：利用多维数据切片，在一个流执行 FFN 时重叠另一个流的 EP 通信，实现 computation-communication 和 communication-communication 双重重叠。
- **DVM-based Operator Fusion**：基于 Device Virtual Machine 框架实现跨类算子融合（Cube-class 与 Vector-class），例如 GroupedMatMul-Reshape-Cast 融合，减少内存访问和 kernel 启动开销。

#### （3）系统化并行化配置生成工具
- 结合**解析估计**与**整数线性规划 (Integer Linear Programming, ILP)** 自动搜索最优多维并行策略（DP/TP/PP/VPP/EP/SP/OP）。
- 显著缩短调优周期（从 ~7 天降至 ~0.5 天），同时达到甚至超越人工调优性能。

#### （4）集群级性能优化方法论
- **Host-bound 优化**：通过 CPU Affinity 绑定和内核隔离域减少监控进程干扰，降低性能波动。
- **Device-bound 优化**：
  - 修改固件中 NPU “idle mode” 触发阈值，防止短算子频繁导致降频；
  - 切换 IOMMU 为 Passthrough 模式，减轻查询引起的 host-device 交互延迟。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **精度控制** | 提供从算子到模型级的完整验证链条，确保跨平台/并行策略一致性，优于传统黑盒测试 |
| **训练效率** | 多项优化协同作用，实现近线性扩展至数千设备，支持万亿参数模型训练 |
| **工程效率** | 并行策略自动生成工具大幅降低人力成本和试错时间 |
| **硬件适配性** | 针对 Ascend NPU 特性深度定制，充分发挥国产硬件潜力 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- 论文中未明确列出预训练所用的具体公开数据集名称；
- 强调进行了大规模预训练任务，涉及 **长达 128K tokens 的长序列建模**，适用于通用语言理解与生成场景。

### 实验设置
- **硬件平台**：华为 Ascend NPU 集群（最多使用 8192 台设备）
- **软件框架**：MindSpore
- **模型系列**：TeleChat3-MoE，参数规模覆盖：
  - 105B（45 层）
  - 438B（54 层）
  - 1119B（61 层）
- **架构特性**：
  - Multi-Latent Attention (MLA)
  - 浅而宽结构（Shallow-and-Wide）
  - 高稀疏 MoE 架构（Top-4 ~ Top-8 路由，数百个路由专家 + 1 个共享专家）

### 评估指标
| 指标 | 描述 |
|------|------|
| **Model Flops Utilization (MFU)** | 衡量实际计算利用率，反映系统整体效率 |
| **Step Time (ms)** | 单步训练耗时，用于比较不同并行策略性能 |
| **Throughput Improvement (%)** | 相对于基线的吞吐提升幅度 |
| **Scaling Efficiency** | 多设备扩展下的性能线性度 |
| **Memory Usage** | 设备峰值显存占用，用于约束 ILP 优化 |

### 基线方法对比
- **Pipeline Scheduling**：对比传统 GPipe 和 DAPPLE，本文采用 Interleaved + 1F1B 更高效。
- **EP Communication**：对比原始全局 All-to-All，本文提出的 hierarchical scheme 减少冗余通信。
- **Parallelism Tuning**：对比专家手工调优（Expert Manual Tuning），本文自动化工具显著提速。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 成果 | 数据 |
|------|------|
| **最大模型规模** | 成功完成 **1.119T 参数 MoE 模型** 在 **8192 Ascend NPU** 上的端到端训练 |
| **MFU 表现** | 实现高 Model Flops Utilization，表明计算资源高度利用 |
| **扩展性** | 在数千设备上实现**近线性扩展**，验证系统可伸缩性 |
| **EP 通信占比下降** | 通过通信重叠技术，EP 通信时间占总通信时间比例从 **30% 降至 5%** |
| **Hierarchical EP 提升** | 相比全局 All-to-All，训练吞吐提升约 **15%** |
| **DVM 算子融合性能增益** | GroupedMatMul-Reshape-Cast 融合后性能提升 **~85%**（单算子级别） |

### 与基线方法的对比结果（Table 5）
| 策略 | 设备数量 | Step Time (ms) |
|------|----------|----------------|
| Expert-Designed-1 | 4,096 | 40,076 |
| Expert-Designed-2 | 4,096 | 40,147 |
| **Tool-Generated** | **4,096** | **39,969** ✅ |

👉 工具生成策略不仅更快（节省约 100 ms/step），而且调优时间从 **7 天缩短至 0.5 天**（见 Table 4）。

### 消融实验结果（隐含于各节描述）
- **Interleaved Pipeline + 1F1B**：带来约 **10% 的端到端性能提升**。
- **Host Isolation**：在 4096 设备任务中，减少性能方差达 **38%**，平均吞吐提升 **1–2%（小规模）→ 10–15%（大规模）**。
- **NPU 固件调整（防误入 idle mode）**：带来 **25–30% 的吞吐提升**。
- **Passthrough IOMMU 配置**：额外获得 **3–5% 性能增益**。

---

## 4. 关键结论和发现

### 主要发现
1. **系统性基础设施设计是支撑万亿级 MoE 模型训练的关键**：仅靠模型架构改进不足以应对极端规模挑战，必须从算子精度、框架优化、并行策略、集群管理等全栈协同设计。
2. **自动化的并行策略生成可行且高效**：结合分析建模与 ILP 求解器可在极短时间内找到接近最优的多维并行方案，极大提升研发效率。
3. **硬件感知优化至关重要**：即使是底层固件级别的节能策略也可能成为性能瓶颈，需针对性调整以释放硬件潜力。
4. **高稀疏 MoE + 浅而宽结构 + MLA 注意力** 是适合大规模高效训练的有效组合，兼顾参数扩展性与计算密度。

### 方法的局限性
- 所有实验均基于 **华为 Ascend NPU + MindSpore 生态**，通用性受限于特定硬件/软件栈；
- 缺乏与其他主流 MoE 模型（如 Mixtral、DeepSeek-MoE）在相同硬件上的直接性能对比；
- 未提供下游任务微调效果或推理延迟数据，侧重训练侧而非全生命周期表现。

### 未来工作方向
- 进一步扩大模型规模，探索 **更高维度的混合并行组合**；
- 将系统优化能力泛化至更多国产 AI 芯片平台；
- 开放更多模型权重与训练代码，推动社区共建；
- 探索更智能的动态并行策略调整机制，适应不同训练阶段需求。

---

> 🔗 **开源信息**：  
> - GitHub 地址：[https://github.com/Tele-AI/TeleChat3](https://github.com/Tele-AI/TeleChat3)  
> - 联系邮箱：`{liuxz2,wangc17,yangzh17,jiangz13}@chinatelecom.cn`, `zhaoxuncheng2@huawei.com`  
> - 通讯作者：`xuelong-li@chinatelecom.cn`

</details>

---

### 2. [FPGA Co-Design for Efficient N:M Sparse and Quantized Model Inference](https://arxiv.org/abs/2512.24713)

**Authors**: Fen-Yu Hsieh, Yun-Chang Teng, Ding-Yong Hong, Jan-Jan Wu  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.24713v1  

#### Abstract
Large language models (LLMs) have demonstrated remarkable performance across a wide range of language processing tasks. However, this success comes at the cost of substantial computation and memory requirements, which significantly impedes their deployment in resource-constrained environments. To ad...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FPGA Co-Design for Efficient N:M Sparse and Quantized Model Inference

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）虽然在自然语言处理任务中表现出色，但其巨大的计算和内存开销严重限制了在资源受限环境中的部署。传统的压缩技术如**weight pruning**（权重剪枝）和**quantization**（量化）单独使用时存在局限性：
- **Pruning** 减少操作数但保留高精度激活；
- **Quantization** 降低精度但不减少计算量。

此外，当前硬件（如NVIDIA GPU的2:4 Sparse Tensor Cores）仅支持固定的 **N:M sparsity** 模式（如2:4），而算法研究显示更灵活的N:M模式可能带来更高的精度与效率平衡。

### 🚀 提出的新方法与创新
本文提出了一种**软硬件协同设计框架**（hardware-software co-design），结合 **N:M structured pruning** 和 **low-bit quantization**，并基于 **FPGA** 构建专用加速器，实现高效LLM推理。主要创新包括：

1. **统一的压缩-执行流水线**  
   支持任意 **N:M structured sparsity**（不限于2:4）和 **B-bit quantization**（如INT4），适用于多种平台（CPU、GPU、FPGA）。

2. **面向通用N:M模式的FPGA加速器设计**  
   设计了一个基于**systolic array**的FPGA加速器，具备：
   - **Zero-skipping PEs**：自动跳过零值权重的MAC操作，节省算力；
   - **可重构数据路径**（reconfigurable datapath）：支持不同N:M模式和bit-width；
   - **无需显式稀疏元数据存储**：利用密集布局保留零值，简化硬件解码。

3. **跨平台一致性基准测试流程**  
   所有后端（CPU/GPU/FPGA）遵循相同的执行流（加载 → dequantization → GEMM），确保公平比较。

4. **分析模型指导设计空间探索**  
   提出通用分析模型，用于预测最优N:M配置与bit-width，在吞吐、利用率和精度间权衡。

### 🔍 相比现有方法的优势
| 维度 | 传统方案 | 本文方案 |
|------|----------|---------|
| 硬件支持 | 仅限固定2:4模式（如NVIDIA） | 支持任意N:M模式（如2:4, 4:16等） |
| 软件灵活性 | 多数只针对特定硬件优化 | 统一压缩格式，多平台兼容 |
| 存储效率 | 需额外索引结构管理稀疏性 | 密集布局+位打包，无额外开销 |
| 加速潜力 | 受限于专用硬件支持 | FPGA提供更高定制化与扩展性 |

---

## 2. 核心实验方法和设置

### 📊 数据集与模型
- 使用合成矩阵进行组件级性能评估（非真实文本数据集）；
- 模型规模分析基于 **LLaMA-7B** 模型的线性层结构（共32个decoder block）；
- 主要关注 **linear layers** 中的GEMM运算（占主导地位）。

### ⚙️ 实验设置
- **矩阵尺寸**：4096×4096、8192×8192、12288×12288；
- **Batch size**：默认为 **B=512**（以摊销启动开销）；
- **量化方式**：对非零权重采用 **symmetric abs-max INT4 quantization**；
- **Pruning策略**：magnitude-based **2:4 structured pruning**（每组4个保留2个最大绝对值权重）；
- **平台配置**：
  - **Host CPU**: Intel Core i9-12900K
  - **GPU**: NVIDIA RTX 3090
  - **FPGA**: AMD Alveo U55C（通过OpenCL/HLS部署）

### 📈 评估指标
| 指标 | 定义 |
|------|------|
| `t_io` | 权重加载时间 |
| `t_deq` | 设备上反量化时间（dequantization） |
| `t_mm` | GEMM矩阵乘法耗时 |
| `t_compute` | `t_deq + t_mm`（纯计算流水线时间） |
| **Throughput (GFLOP/s)** | $ \frac{2 \cdot M \cdot K \cdot B}{t} \times 10^{-9} $ |
| **End-to-end latency reduction** | 相对于dense GPU baseline的加速比 |

### 🆚 基线方法对比
| 后端 | 描述 |
|------|------|
| **CPU** | PyTorch CPU BLAS backend |
| **GPU (Dense)** | cuBLAS标准GEMM |
| **GPU (2:4 Sparse)** | cuSPARSELt的2:4半结构化稀疏核 |
| **FPGA (Ours)** | 自研zero-skipping systolic array加速器（尚未完成实测） |

> 注：FPGA部分尚处于设计阶段，未报告完整实测数据。

---

## 3. 主要实验结果和性能指标

### 📉 存储压缩效果（Table 4）
在 **4096×4096 FP16 dense权重**（原始32MB）基础上：

| 方法 | 存储大小 | 相对压缩比 |
|------|--------|-----------|
| Dense FP16 | 32.00 MB | 1.00× |
| Pruned FP16 (50%密度) | 16.00 MB | 0.50× |
| Pruned + INT4（int张量存储） | 16.00 MB | 0.50× |
| Pruned + INT4（bit-packed） | **8.00 MB** | **0.25×** |

✅ **结论**：结合2:4 pruning与INT4 quantization，**最多可实现4×的权重存储压缩**。

---

### ⏱️ 推理延迟与吞吐（Table 5 & 6）

#### 在 4096×4096, B=512 下：
| Backend | `t_mm` (ms) | `t_compute` (ms) | Throughput_mm (GFLOP/s) | Speedup vs Dense GPU |
|--------|-------------|------------------|--------------------------|-----------------------|
| GPU (Dense) | 0.36 | 0.66 | 47,862 | 1.00× |
| GPU (2:4 Sparse) | **0.21** | **0.51** | **81,968** | **1.71× GEMM**, **1.29× end-to-end** |

✅ **关键结果**：
- **GEMM速度提升1.71×**
- **端到端计算流水线提速1.29×**

#### 不同矩阵规模下的扩展性（Table 6）
随着矩阵增大，稀疏优势更加明显：
| 尺寸 | GEMM时间（Dense→Sparse） | 加速比 |
|------|--------------------------|--------|
| 4096² | 0.36 → 0.21 ms | 1.71× |
| 8192² | 1.05 → 0.64 ms | 1.64× |
| 12288² | 2.26 → 1.16 ms | **1.95×** |

> 更大问题规模能更好掩盖元数据开销，凸显结构化稀疏优势。

---

### 🧮 LLaMA-7B 模型级预测性能（Table 8）

仅考虑线性层（忽略attention等其他开销）的每token延迟预测：

| Backend | `T_block` (ms) | `T_model` (ms) | Throughput_model (GFLOP/s) | Speedup vs Dense GPU |
|--------|----------------|----------------|----------------------------|-----------------------|
| CPU | 424.90 | 13,596.80 | 488 | — |
| GPU (Dense) | 6.72 | 215.04 | 30,838 | 1.00× |
| GPU (2:4 Sparse) | **4.95** | **158.40** | **41,865** | **1.36×** |

✅ **结论**：
- 结构化稀疏使LLaMA-7B的**每token吞吐提升1.36×**；
- 相比CPU快约 **85.67×**。

---

### ❌ FPGA 当前状态
尽管提出了完整的FPGA加速器架构（含zero-skipping PE、systolic array、host接口），但在所有表格中：
- FPGA相关字段为空（`| FPGA (ours) | ... |` 表格项为空）
- 作者明确指出：“we plan to complete a full empirical evaluation of the FPGA implementation”

➡️ **目前FPGA为设计蓝图，尚未完成实测验证**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **N:M structured sparsity + low-bit quantization 具有显著协同效应**：
   - 联合应用可在保持精度的同时大幅减少**权重存储（4×↓）、内存带宽需求、算力消耗**。
   
2. **结构化稀疏在现代GPU上有实际收益**：
   - 利用NVIDIA 2:4 Sparse Tensor Cores，在典型LLM矩阵运算中可获得 **1.71× GEMM加速** 和 **1.29× 端到端计算加速**。

3. **模型级扩展表现良好**：
   - 在LLaMA-7B上线性层预测表明，结构化稀疏可带来 **1.36× 的吞吐提升**，证明该方法在真实模型规模下依然有效。

4. **FPGA是突破固定硬件限制的理想平台**：
   - 当前GPU仅支持2:4模式，而本文提出的FPGA架构支持**任意N:M pattern**，为未来更高压缩率（如4:16）提供硬件基础。

---

### ⚠️ 局限性
1. **FPGA尚未实测**：
   - 所有FPGA结果仅为设计，缺乏实际性能、功耗、PPA（Power-Performance-Area）数据支撑。

2. **仅评估线性层**：
   - 忽略了attention scores、softmax、RoPE、normalization等关键模块，整体token延迟被低估。

3. **静态batch size假设**：
   - 所有实验基于固定B=512，未考虑动态batch或streaming场景的影响。

4. **未包含训练或微调补偿机制**：
   - 如LoRA等恢复精度的技术虽提及（SLiM），但未集成进本框架。

---

### 🔮 未来工作方向
1. **完成FPGA原型实测**：
   - 部署完整系统，测量实际性能、能效（TOPS/W）及资源占用。

2. **扩展至端到端LLM推理**：
   - 将加速范围从GEMM kernel扩展到整个LLaMA模型推理流程。

3. **探索更高阶压缩组合**：
   - 引入mixed-precision activations、higher-ratio N:M patterns（如4:16）、甚至与low-rank adaptation（LoRA）联合优化。

4. **支持动态稀疏模式切换**：
   - 利用FPGA可重构性，运行时切换不同N:M配置以适应不同layer或模型。

5. **边缘设备部署验证**：
   - 在嵌入式FPGA或低功耗平台上验证该框架的实际部署价值。

---

## 总结

📌 本文提出了一套面向 **N:M structured sparsity + INT4 quantization** 的**软硬件协同设计框架**，实现了：
- 最多 **4× weight storage reduction**
- **1.71× GEMM speedup** 和 **1.29× compute pipeline speedup** on GPU
- 对LLaMA-7B预测显示 **1.36× throughput improvement**

💡 创新在于将算法灵活性（通用N:M）与硬件定制化（FPGA systolic array + zero-skipping）结合，突破当前GPU对2:4模式的硬性约束。

⚠️ 当前主要局限是**FPGA尚未实测**，未来需补全实证数据，并向**全模型、多精度、低功耗部署**演进。

</details>

---

### 3. [MS-SSM: A Multi-Scale State Space Model for Efficient Sequence Modeling](https://arxiv.org/abs/2512.23824)

**Authors**: Mahdi Karami, Ali Behrouz, Peilin Zhong, Razvan Pascanu, Vahab Mirrokni  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.23824v1  

#### Abstract
State-space models (SSMs) have recently attention as an efficient alternative to computationally expensive attention-based models for sequence modeling. They rely on linear recurrences to integrate information over time, enabling fast inference, parallelizable training, and control over recurrence s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MS-SSM: A Multi-Scale State Space Model for Efficient Sequence Modeling

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **State Space Models (SSMs)** 在序列建模中面临两个关键挑战：
- **有限的有效记忆（limited effective memory）**：由于其线性递归特性，SSMs 难以长期保留历史信息，通常需要增大状态维度来提升记忆能力，导致效率下降。
- **缺乏多尺度建模能力（multi-scale dependency modeling）**：现实世界信号（如语音、图像、文本）具有多层次结构（从局部细节到全局趋势），而标准 SSMs 缺乏显式的多分辨率分析机制，难以同时捕捉高频细节和低频趋势。

此外，尽管注意力机制（如 Transformer）能处理长程依赖，但其 **O(N²)** 的计算复杂度限制了在超长序列上的应用。

---

### 🚀 提出的新方法：MS-SSM
本文提出 **MS-SSM（Multi-Scale State Space Model）**，一种融合多尺度分析的高效 SSM 架构，核心创新如下：

#### （1）**多尺度分解（Multi-Scale Decomposition）**
- 引入 **Stationary Wavelet Transform (SWT)** 对输入序列进行多级小波分解，生成多个不同分辨率的子序列（approximation 和 detail coefficients）。
- 使用可学习的 **causal depthwise 1D convolution** 实现该过程，支持端到端训练，并保持因果性（适用于自回归任务）。
- 输出为 $ S+1 $ 个不同尺度的表示（$ S $ 为分解层数），分别对应不同时间粒度的信息。

#### （2）**并行多尺度 SSM 处理**
- 每个尺度的表示由一个独立的 SSM 模块处理，形成 $(S+2)$ 个并行运行的 SSM（含原始输入通道）。
- 不同尺度的 SSM 可以捕获从局部动态（高分辨率）到长期趋势（低分辨率）的时序模式。

#### （3）**尺度感知初始化（Scale-Dependent Initialization）**
- 对不同尺度的 SSM 的状态转移矩阵 $ A $ 进行差异化初始化：
  - **低分辨率（粗粒度）**：特征值更接近 1 → 更强的长期记忆能力；
  - **高分辨率（细粒度）**：特征值较小 → 更关注短期动态。
- 此设计基于 HiPPO 理论，增强模型对多尺度记忆需求的适应性。

#### （4）**输入依赖的尺度混合器（Input-Dependent Scale-Mixer）**
- 将各 SSM 的输出通过一个 **可学习的加权求和** 融合：
  $$
  z_t = E_{f}(x_t) \cdot y_t, \quad \text{其中 } E_f = \text{Linear}_e(x_t)
  $$
- 权重由原始输入 $ x_t $ 动态决定，实现上下文感知的跨尺度信息融合。

---

### 🔍 相比现有方法的优势
| 方法 | 局限性 | MS-SSM 的改进 |
|------|--------|----------------|
| **Standard SSMs (S4, Mamba)** | 单一尺度建模，难以兼顾局部与全局 | 显式引入多尺度结构，增强表达力 |
| **Transformer** | 自注意力复杂度为 $ O(N^2) $，不适用于极长序列 | 继承 SSM 的线性推理复杂度，保持高效 |
| **Multi-resolution CNNs (e.g., MULTIRESNET)** | 缺乏全局感受野，依赖固定卷积核 | 结合 SSM 的无限感受野与多尺度卷积 |
| **Fourier-based models (FNet, Prism)** | 非因果、时间定位差 | 使用因果小波卷积，适合序列建模 |

> ✅ **核心优势总结**：  
> MS-SSM 在保持 SSM 高效性的同时，通过多尺度架构显著提升了对复杂层次结构和长程依赖的建模能力。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 任务类型 | 序列长度 | 特点 |
|--------|----------|-----------|-------|
| **sCIFAR-10** | 图像分类（像素级序列） | 1024 | 无 2D 归纳偏置，考验长程依赖建模 |
| **ImageNet-1K** | 图像分类（patch 序列） | ~200 (16×16 patches) | 标准视觉基准 |
| **ListOps** | 层次化推理 | 2048 | 包含嵌套括号操作，测试结构理解能力 |
| **PTB-XL** | 心电图（ECG）多标签分类 | 1000 | 多通道时间序列，需多尺度特征提取 |
| **Long Range Arena (LRA)** | 综合长程建模基准 | 最长达 4K | 包括文本、图像、路径查找等任务 |

---

### ⚙️ 实验设置
- **模型架构**：基于 ViT 框架，将标准注意力块替换为 MS-SSM 块。
- **变体对比**：
  - `MS-SSM(S4)`：使用 S4 作为基础 SSM 模块（数据无关）
  - `MS-SSM(S6)`：使用 Mamba 中的 S6 模块（输入依赖参数）
- **超参统一性**：与基线模型保持相似参数量和隐藏维度（如 hidden size=256, N=128, S=3）。
- **优化器**：Adam / AdamW，带 warmup 和 cosine decay。
- **硬件**：A6000 GPU，批大小根据任务调整（50~1024）。

---

### 📊 评估指标
| 任务 | 主要指标 |
|------|----------|
| 分类任务（sCIFAR, ImageNet, PTB-XL） | Accuracy / AUROC |
| ListOps | Accuracy (%) |
| LRA Benchmark | 平均得分（AVG）及各项子任务准确率 |
| 消融实验 | 控制变量下的性能变化 |
| 有效感受野分析 | **Mean Mixing Distance**（基于 Jacobian 的距离度量） |

---

### 🆚 基线方法对比
| 类别 | 基线模型 |
|------|---------|
| **Transformer 家族** | Vanilla Transformer, Linformer, Performer, BigBird, Longformer |
| **RNN / LSTM 变种** | LSTM, r-LSTM, UR-GRU, HiPPO-RNN |
| **SSM 家族** | S4, S4D, S5, Mamba, Griffin, Liquid-S4 |
| **CNN / Conv-based** | CKConv, MULTIRESNET, InceptionTime |
| **其他高效模型** | SPACE-TIME, H-Transformer-1D |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### ✅ 表 1：图像分类性能（sCIFAR & ImageNet-1K）
| Model | sCIFAR Acc (%) | ImageNet-1K Acc (%) |
|-------|----------------|---------------------|
| S4 | 91.1 | 79.1 |
| Mamba | 90.1 | 80.5 |
| MULTIRESNET | 93.0 | 80.2 |
| **MS-SSM(S4)** | **93.3** | **81.3** |

> 💡 **结论**：MS-SSM 在两项任务上均达到 SOTA，尤其在像素级建模（sCIFAR）中表现突出，说明其强大的多尺度建模能力。

---

#### ✅ 表 2：ListOps 层次推理任务
| Model | Accuracy (%) |
|-------|--------------|
| Transformer | 36.37 |
| Mamba | 38.02 |
| Mamba (2x 参数) | 49.63 |
| MULTIRESNET | 62.75 |
| S5 | 62.15 |
| **MS-SSM(S4)** | **62.83** |
| **MS-SSM(S6)** | **63.04** |

> 💡 **结论**：MS-SSM 性能远超 Mamba（约 2 倍精度），且优于双倍参数的 Mamba，证明其**高效的多尺度记忆机制**在结构化推理中的优势。

---

#### ✅ 表 3：PTB-XL ECG 多标签分类（AUROC）
| Model | All | Diag | Rhythm |
|-------|-----|------|--------|
| Transformer | 0.857 | 0.876 | 0.831 |
| Mamba | 0.915 | 0.929 | 0.952 |
| S4 | 0.938 | 0.939 | 0.977 |
| **MS-SSM(S4)** | **0.939** | **0.939** | **0.980** |

> 💡 **结论**：在医疗时间序列任务中也取得最优性能，验证了其在真实多尺度信号中的泛化能力。

---

#### ✅ 表 4：Long Range Arena 综合性能（AVG）
| Model | AVG Score |
|-------|-----------|
| Transformer | 62.12 |
| Mamba | 72.30 |
| S5 | 92.52 |
| **MS-SSM(S4)** | **91.89** |
| **MS-SSM(S6)** | **86.73** |

> ⚠️ 注意：虽然 MS-SSM(S6) 因未充分调优略低于 S5，但 **MS-SSM(S4)** 已接近最先进水平，且显著优于同架构的 Mamba（+19.59 pts），表明多尺度设计本身是性能跃升的关键。

---

### 🔍 消融实验结果（表 5）

| 变体 | PTB-XL (AUROC) | ListOps (Acc%) | 说明 |
|------|----------------|----------------|------|
| Full MS-SSM(S6) | 0.939 | 63.04 | 基线 |
| 移除 Multi-Scale Conv | 0.916 | 37.98 | ⬇️⬇️巨大下降，证明多尺度分解至关重要 |
| 使用 S4 替代 S6 | 0.939 | 62.83 | 性能几乎不变 → 方法不依赖特定 SSM 模块 |
| 输入无关的 Scale Mixer | 0.932 | 61.28 | ⬇️说明输入依赖融合更优 |
| 非线性门控（Softmax） | 0.921 | 61.42 | 线性投影更有效 |

> ✅ **结论**：所有组件均有贡献，但 **多尺度卷积** 是最大增益来源。

---

### 📏 有效感受野分析（表 6）

| 模型 | Mean Mixing Distance |
|------|------------------------|
| Mamba | 38.84 ± 21.97 |
| **MS-SSM(S6)** | **94.90 ± 64.62** |

> 💡 **结论**：MS-SSM 的平均混合距离显著更高，说明其能够更好地“关注”远距离上下文，具备更强的长程依赖建模能力。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **多尺度建模极大提升 SSM 表达能力**：通过将序列分解为多个分辨率并在每个尺度上独立建模，MS-SSM 能同时捕捉局部细节和全局趋势，解决了传统 SSM 记忆受限的问题。
2. **无需增加参数即可超越大模型**：即使与双倍参数的 Mamba 相比，MS-SSM 仍表现出更优性能，说明其结构设计带来了更高的参数利用效率。
3. **通用性强**：在图像、文本、时间序列等多种模态任务中均取得领先结果，验证了多尺度 SSM 的广泛适用性。
4. **输入依赖的尺度融合优于静态融合**：动态权重分配机制使模型能根据当前输入灵活整合多尺度信息。

---

### ⚠️ 方法的局限性
- **计算开销略有上升**：虽然总体复杂度仍为线性，但多尺度卷积引入了额外的 $ O(LKS) $ 开销（$ K $: kernel size, $ S $: scales）。
- **小波滤波器需学习而非预设**：虽然提高了灵活性，但也增加了训练难度和对数据的需求。
- 当前实验集中在中等规模任务，尚未在超大规模语言建模（如 100K+ 上下文）中验证。

---

### 🔮 未来工作方向
1. **扩展至更多领域**：应用于 NLP 中的篇章理解、代码生成等天然具有层次结构的任务。
2. **探索新型多尺度结构**：结合非线性 RNN 或 test-time training 模型（如 Titans, Lattice）进一步增强记忆。
3. **硬件优化实现**：利用现代加速器（TPU/GPU）对多尺度卷积进行并行化优化，提升推理速度。
4. **理论分析**：形式化证明多尺度 SSM 在记忆容量和梯度传播方面的优势。

---

## ✅ 总结
**MS-SSM** 成功地将 **多分辨率分析思想** 与 **State Space Models** 相结合，提出了一种既能高效处理长序列、又能精细建模多尺度结构的新型架构。其实验结果全面超越主流 SSM 和 Transformer 变体，特别是在 **长程依赖、层次推理和多尺度时间序列分类** 任务中表现卓越。该工作为下一代高效序列模型的设计提供了重要范式——**“分而治之”的多尺度建模范式**。

</details>

---

### 4. [Youtu-Agent: Scaling Agent Productivity with Automated Generation and Hybrid Policy Optimization](https://arxiv.org/abs/2512.24615)

**Authors**: Yuchen Shi, Yuzheng Cai, Siqi Cai, Zihan Xu, Lichao Chen, Yulei Qin, Zhijian Zhou, Xiang Fei, Chaofan Qiu, Xiaoyu Tan, Gang Li, Zongyi Li, Haojia Lin, Guocan Cai, Yong Mao, Yunsheng Wu, Ke Li, Xing Sun  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.24615v1  

#### Abstract
Existing Large Language Model (LLM) agent frameworks face two significant challenges: high configuration costs and static capabilities. Building a high-quality agent often requires extensive manual effort in tool integration and prompt engineering, while deployed agents struggle to adapt to dynamic ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Youtu-Agent: Scaling Agent Productivity with Automated Generation and Hybrid Policy Optimization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Model (LLM)** 的智能体框架面临两大瓶颈：

- **高配置成本**：构建高质量 Agent 需要大量人工参与工具集成、Prompt 工程和代码编写，开发门槛高且难以规模化。
- **静态能力限制**：部署后的 Agent 缺乏自适应能力，无法在不进行昂贵微调的情况下持续优化自身行为。

### 提出的新方法与创新思路
为解决上述问题，作者提出 **Youtu-Agent** ——一个模块化、支持自动化生成与持续进化的 LLM Agent 框架。其核心创新包括：

#### （1）**模块化架构设计**
采用三层分层结构：
- **Environment Layer**：提供执行环境（如浏览器、Shell、沙箱）
- **Tools Layer**：封装原子操作工具（环境相关/无关/MCP 工具）
- **Agent Layer**：LLM 驱动的规划与执行器，配合 Context Manager 控制上下文长度

通过 **YAML 配置系统** 实现组件解耦，便于复用和自动化合成。

#### （2）**双范式自动化生成机制**
- **Workflow Mode**：确定性四阶段流水线（意图分解 → 工具检索/合成 → Prompt 生成 → 配置组装），适用于标准任务。
- **Meta-Agent Mode**：由高层“架构师 Agent”动态决策生成流程，调用 `search_tool`, `create_tool`, `ask_user`, `create_agent_config` 等能力，处理复杂或模糊需求。

该机制可自动合成 Python 工具代码、Prompt 和完整 YAML 配置。

#### （3）**混合策略优化体系（Hybrid Policy Optimization）**
- **Agent Practice 模块**：基于 **Training-free GRPO** 的无梯度优化，在推理时积累经验并提炼“文本型 LoRA”，实现低成本在线改进。
- **Agent RL 模块**：端到端强化学习训练模块，集成分布式 RL 框架（如 VeRL），解决长周期任务中的“熵爆炸”问题，并提升训练稳定性与扩展性。

### 相比现有方法的优势
| 维度 | Youtu-Agent 优势 |
|------|------------------|
| **自动化程度** | 支持从自然语言描述自动生成工具代码与配置，远超传统手工配置或仅角色编排的框架（如 AutoGen、ChatDev） |
| **灵活性与通用性** | 双生成模式覆盖简单与复杂场景；YAML 结构化配置利于共享与迭代 |
| **持续进化能力** | 同时支持低开销的 in-context 优化（Practice）和高性能参数更新（RL），兼顾效率与效果 |
| **可扩展性与稳定性** | RL 模块通过 RESTful API 封装、Ray 并行、分层超时控制等技术，实现 128 GPU 规模稳定训练 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|-------|------|
| **WebWalkerQA** (680题) | 多步网页导航与问答任务，测试真实网站上的探索与理解能力 |
| **GAIA (text-only subset)** (466题) | 实际应用场景下的多跳推理、工具使用、文件解析等综合能力评测 |
| **AIME 2024 / 2025** | 数学竞赛题，用于评估数学推理与编码辅助解题能力 |
| **DAPO-Math-17K** | 用于 Agent Practice 模块训练的小样本数学数据集（抽样 100 题） |
| **AgentGen-80** | 自建基准，包含 80 个多样化任务描述，用于评估自动化生成质量 |

### 实验设置与评估指标
| 模块 | 设置说明 | 主要指标 |
|------|--------|---------|
| **Benchmark 性能** | 全部基于开源模型（DeepSeek-V3/Qwen2.5-7B），不依赖 GPT/Claude API | Pass@1 准确率 |
| **自动化生成有效性** | 在 AgentGen-80 上测试 Workflow 与 Meta-Agent 模式 | CV（配置有效性）、TE（工具可执行性）、TC（任务完成率） |
| **Agent Practice** | 基于 ReAct 范式 + Code Interpreter，3 轮训练，每轮 group size=5 | Mean@32（AIME），学习成本（美元估算） |
| **Agent RL** | 使用 Qwen2.5-7B-Instruct，在 Math/Code 和 Search 两类任务上进行 RL 微调 | Accuracy 提升幅度、训练速度（iteration time）、KL 散度稳定性 |

### 基线方法对比
| 类别 | 对比方法 |
|------|--------|
| **Agent 框架** | MetaGPT, AutoGen, ChatDev |
| **自动化设计** | ADAS, AutoAgents |
| **优化方法** | ReAct, Reflexion, ZeroTIR, SimpleTIR, ReTool, AFM |
| **RL 训练** | Agent-Lightning (v0.2.2 官方版) |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 指标 | 结果 |
|------|------|
| **WebWalkerQA Pass@1** | **71.47%**（使用 DeepSeek-V3.1） |
| **GAIA (text-only) Pass@1** | **72.8%** |
| **自动化工具合成成功率（TE）** | > **81.25%** |
| **任务完成率（TC）** | Workflow: 65.00%，Meta-Agent: **68.75%** |
| **Agent Practice 提升（vs ReAct）** | AIME2024: **+2.7%**, AIME2025: **+5.4%**（仅 100 样本，~\$18 成本） |
| **Agent RL 训练加速** | 较官方 Agent-Lightning **快 40%** |
| **Qwen2.5-7B 在 AIME2024 准确率提升** | 从 **10% → 45%**（+35pp） |
| **搜索类 QA 多项指标平均提升** | **13–21%**（TriviaQA 到 2WikiMultiHop） |

### 与基线方法对比结果

#### ✅ Benchmark 表现（图4 & 表2）
- 在 WebWalkerQA 上优于多数闭源模型驱动的方法（如 WebShaper、ReRct），仅次于部分训练过的专有系统。
- 在 GAIA 上达到 72.8%，证明其在真实世界任务中具备强大工具调用与推理能力。

#### ✅ 自动化生成表现（表1）
| 指标 | Workflow Mode | Meta-Agent Mode |
|------|---------------|----------------|
| CV（配置有效） | **100%** | 98.75% |
| TE（工具可执行） | 81.25% | **82.50%** |
| TC（任务完成） | 65.00% | **68.75%** |

→ Meta-Agent 在复杂任务上略胜一筹，体现其更强的灵活性。

#### ✅ Agent Practice 效果（表2）
| 方法 | AIME2024 | AIME2025 | 学习成本 |
|------|----------|----------|----------|
| ReAct (baseline) | 80.0 | 67.9 | – |
| + Training-Free GRPO (w/ GT) | **82.7** (+2.7) | **73.3** (+5.4) | ~\$18 |
| ReTool (fine-tuning) | 67.0 | 49.3 | ~\$10,000 |

→ **以极低成本实现超越昂贵微调的效果**，尤其适合资源受限场景。

#### ✅ Agent RL 效果（表3 & 表4）
| 任务 | 指标 | 提升幅度 |
|------|------|-----------|
| Math (AIME24) | 10% → **45%** | **+35pp** |
| Math (AIME25) | 9% → **31%** | **+22pp** |
| Search (平均) | 多项 QA 提升 | **8–21%** |

此外，训练效率提升 **40%**，且 KL 散度、梯度范数保持稳定（见图7），避免了“熵崩溃”。

---

## 4. 关键结论和发现

### 主要发现
1. **自动化生成是可行且高效的**：通过结构化配置 + LLM 自动生成工具与 Prompt，可显著降低 Agent 构建门槛，**工具合成成功率达 81%+**。
2. **无需微调也能持续进化**：Agent Practice 模块利用 in-context 经验积累，在仅 100 个样本下即可带来 **+2.7%~5.4%** 的性能提升，成本仅为传统 SFT/RL 的千分之一。
3. **大规模 RL 可稳定扩展**：通过基础设施优化（RESTful、Ray、分层超时）和算法改进（过滤异常轨迹、减少 off-policy 更新），实现了 **128 GPU 规模下的稳定训练**，并取得显著性能跃迁。
4. **开源模型也能构建强智能体**：完全基于 open-weight 模型（如 DeepSeek-V3、Qwen2.5），在多个 benchmark 上达到甚至超过闭源系统的水平。

### 方法的局限性
- **对底层 LLM 能力依赖较强**：自动化生成与 RL 效果受限于基础模型的推理与代码生成能力。
- **Meta-Agent 模式偶尔失败**：`create_agent_config` 工具调用可能输出格式错误，影响最终配置完整性。
- **目前主要面向单 Agent 场景**：虽提及多 Agent 协作，但未深入验证复杂协作逻辑。

### 未来工作方向
- 扩展更多 **Environment 集成**（如移动端、IoT 设备）
- 加强 **多 Agent 协同能力**
- 探索更高级的 **经验积累策略**（如记忆压缩、跨任务迁移）
- 推出桌面级应用 **Tip**，推动本地化、隐私安全的 Agent 应用落地

---

> 🔗 **项目地址**：[https://github.com/TencentCloudADP/youtu-agent](https://github.com/TencentCloudADP/youtu-agent)  
> 📅 **发布日期**：December 26, 2025

</details>

---

### 5. [Yggdrasil: Bridging Dynamic Speculation and Static Runtime for Latency-Optimal Tree-Based LLM Decoding](https://arxiv.org/abs/2512.23858)

**Authors**: Yue Guan, Changming Yu, Shihan Fang, Weiming Hu, Zaifeng Pan, Zheng Wang, Zihan Liu, Yangjie Zhou, Yufei Ding, Minyi Guo, Jingwen Leng  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.23858v1  

#### Abstract
Speculative decoding improves LLM inference by generating and verifying multiple tokens in parallel, but existing systems suffer from suboptimal performance due to a mismatch between dynamic speculation and static runtime assumptions. We present Yggdrasil, a co-designed system that enables latency-o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Yggdrasil: Bridging Dynamic Speculation and Static Runtime for Latency-Optimal Tree-Based LLM Decoding

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 **speculative decoding** 系统在提升 LLM 推理速度方面表现出潜力，但存在一个根本性矛盾：  
- **动态推测算法**（dynamic speculation）能有效提高 token 接受率（如通过 context-aware 树结构），但其动态变化的树结构和算子形状破坏了现代深度学习编译器对 **静态计算图** 的优化假设（如 graph fusion、kernel tuning、CUDA Graph 支持）。  
- 反之，**静态运行时系统**（如 vLLM、TRT-LLM）虽能利用编译优化降低延迟，却无法支持灵活的动态推测，限制了平均接受长度（AAL）。

此外，现有方法通常以最大化 **AAL** 为目标，忽略了验证阶段的非均匀开销，导致高 AAL 并不等于端到端低延迟。

### **提出了什么新方法或新思路**

Yggdrasil 提出了一种 **算法与运行时协同设计**（co-designed）的 latency-optimal 树形推测解码框架，核心创新包括：

#### ✅ **Latency-aware Optimization Objective**
- 不再以 AAL 为唯一目标，而是构建了一个更真实的 **端到端速度提升模型**：
  $$
  \text{Speedup} = \frac{\text{AAL}(W_{\text{draft}}, D_{\text{draft}}, W_{\text{verify}}) \cdot T_{\text{verify}}(1)}{2D_{\text{draft}}T_{\text{drafter}}(W_{\text{draft}}) + T_{\text{verify}}(W_{\text{verify}})}
  $$
- 显式建模了 draft 和 verify 阶段的时间开销，实现对真实 wall-clock 时间的优化。

#### ✅ **Equal-Growth Tree (EGT) 结构**
- 一种新型的 **静态兼容、上下文感知** 的树结构：
  - 所有分支在每一步都同步增长（equal-growth），保证每个 step 的 operator shape 固定，兼容 **TorchInductor** 等编译器的图融合与 CUDA Graph。
  - 支持动态调整树宽 $W_{\text{draft}}$ 和验证宽度 $W_{\text{verify}}$，保留灵活性。
- 包含三个关键组件：
  1. **Draft Depth Predictor**：轻量级多头 MLP 模型，预测最优 draft 深度 $D_{\text{draft}}$。
  2. **Draft Width Selection**：基于预测深度选择最优宽度以平衡 AAL 与验证开销。
  3. **Verification Width Pruning**：从生成的树中剪枝出最优子树用于验证，最大化实际 speedup。

#### ✅ **Stage-based Scheduling Runtime**
- 针对 speculative decoding 多阶段依赖带来的 CPU 开销，提出两种优化：
  1. **Ahead-of-Time Stage Execution**：
     - 提前执行 "Tail Draft" 和 "Head Draft" 阶段，消除条件分支，避免 GPU 空转。
  2. **Profile-Guided Execution Plan Search**：
     - 在编译期基于 profiling 数据搜索最优执行计划（execution plan），最小化调度开销。

### **相比现有方法的优势**

| 方面 | Yggdrasil | 现有方法（如 SpecInfer, Sequoia, vLLM-Spec） |
|------|----------|---------------------------------------------|
| **算法灵活性** | ✔️ 上下文感知、动态调整树结构 | ❌ 静态树或固定策略 |
| **编译友好性** | ✔️ 固定 operator shape，支持 CUDA Graph | ❌ 动态控制流阻碍编译优化 |
| **优化目标** | ✔️ 以真实 latency 为导向 | ❌ 仅优化 AAL，忽略验证成本 |
| **CPU-GPU 协调** | ✔️ 减少条件判断，重叠执行 | ❌ 运行时频繁同步，GPU 利用率低 |
| **无需修改模型** | ✔️ 支持原生 LLM 架构 | ❌ 部分方法需修改模型 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
- **C4** [39]
- **Wikipedia** [45]
- **CNNDailyMail (CNN)** [18]

### **实验设置**
- **硬件平台**：
  - NVIDIA A100 (80GB)
  - NVIDIA A40
  - Intel Xeon E5-2620 v3 @ 2.40GHz CPU
- **软件栈**：
  - CUDA 11.7
  - PyTorch + **TorchInductor** 编译后端
- **模型配置**：
  - **Target Models**: Llama-2-7B, Llama-2-13B
  - **Draft Models**: Llama-68M, Llama-160M
- **输入长度**：512 tokens
- **输出长度**：256 tokens

### **评估指标**
- **Per-Token Generation Latency (TPOT)**：核心指标
- **End-to-End Speedup**：相对于 baseline 的加速比
- **Average Accepted Length (AAL)**
- **Step Latency**
- **GPU Utilization**

### **基线方法对比**
- **SpecInfer** [31]：经典树形推测方法，但动态结构不兼容编译优化
- **Sequoia** [8]：静态自适应树结构，dataset-level 调优
- **vLLM-Spec** [27]：基于 vLLM 的 speculative decoding 实现
- **FlexFlow**：通用 LLM serving 系统

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 配置 | Yggdrasil 加速比（vs. SpecInfer） |
|------|-------------------------------|
| A100 GPU | **最高达 3.98×** |
| A40 GPU | **最高达 2.76×** |

> 注：所有结果均以 **per-token latency** 为衡量标准。

### **与基线方法的对比结果**

- 在所有测试配置下，Yggdrasil **显著优于所有 baseline**：
  - 相比 **SpecInfer**：平均提升 **3.98×**（A100）
  - 相比 **Sequoia** 和 **vLLM-Spec**：分别提升 **1.6×~2.0×**
- 尤其在 **A100** 上优势更大，因其更高的内存带宽利用率瓶颈被更好缓解。

### **消融实验结果**

作者将 Yggdrasil 的优化分解为五个部分进行消融分析（见 Figure 12）：

| 优化项 | 描述 | 平均加速比贡献 |
|--------|------|----------------|
| **O1** | Latency-aware 树推测（基础版） | 基线 |
| **O2** | TorchInductor 图编译优化 | **2.775×**（最大贡献） |
| **O3** | Verification Width Pruning | **1.07×** |
| **O4** | Stage-based Scheduling | **1.21×** |
| **O5** | Draft Depth Predictor | **1.10×** |

> ✅ 所有优化具有正向叠加效应，总加速比达 **3.98×**

### **其他重要实验发现**

- **EGT 参数敏感性分析**（Figure 13）：
  - 最佳配置依赖于硬件和模型规模。
  - 示例：`D_draft=8`, `W_draft=8`, `W_verify=64` 在 Llama-2-7B 上表现最佳。

- **优化目标对比**（Figure 14）：
  - 使用 **speedup 目标** 比直接优化 AAL 提升 **额外 8% 性能**。

- **温度影响**（Figure 15）：
  - 温度越低，draft model 与 target model 对齐越好，AAL 越高。
  - Yggdrasil 在不同温度下始终优于 Sequoia，平均 **1.49× 加速**。

---

## 4. 关键结论和发现

### **主要发现**

1. **动态推测与静态运行时可以兼得**：
   - EGT 结构成功桥接了两者，既保持了 context-aware 的灵活性，又满足了编译优化所需的静态性。

2. **AAL ≠ 实际加速**：
   - 单纯追求高 AAL 可能因验证开销过大而导致性能下降；必须结合硬件特性进行联合优化。

3. **编译优化至关重要**：
   - O2（图编译）带来最大收益（2.775×），说明即使算法先进，若不能被高效执行也难以发挥潜力。

4. **CPU-GPU 协调是瓶颈**：
   - Stage-based scheduling 有效减少空转，证明 speculative decoding 的调度本身需要专门优化。

### **局限性**

- 当前评估基于 **单请求、低并发场景**（latency-optimal setting），适用于边缘或交互式应用。
- **未考虑批处理场景**（throughput-oriented serving），在生产环境中需与 batch scheduler 协同设计。
- Draft model 仍需外部提供，未解决 draft model 训练或对齐问题。

### **未来工作方向**

- 将 Yggdrasil 扩展至 **batched serving 场景**，联合优化 speculative decoding 与 dynamic batching。
- 设计统一策略，协调多个请求的 speculation 决策与资源分配。
- 探索更高效的 depth predictor 或在线自适应机制。
- 支持更多硬件架构（如 TPU、NPU）和编译器（如 TensorRT-LLM）。

---

> **总结**：Yggdrasil 是首个真正实现 **“动态算法 + 静态执行”协同优化** 的 speculative decoding 系统，通过 **EGT 结构、latency-aware 目标、stage-based 调度** 三大创新，在无需修改模型的前提下实现了高达 **3.98× 的端到端加速**，为 LLM 高效推理提供了新的范式。

</details>

---

### 6. [From Building Blocks to Planning: Multi-Step Spatial Reasoning in LLMs with Reinforcement Learning](https://arxiv.org/abs/2512.24532)

**Authors**: Amir Tahmasbi, Sadegh Majidi, Kazem Taram, Aniket Bera  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.24532v1  

#### Abstract
Spatial reasoning in large language models (LLMs) has gained increasing attention due to applications in navigation and planning. Despite strong general language capabilities, LLMs still struggle with spatial transformations and multi-step planning in structured environments. We propose a two-stage ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：From Building Blocks to Planning: Multi-Step Spatial Reasoning in LLMs with Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在通用语言任务上表现出色，但在**空间推理**（spatial reasoning）和**多步规划**（multi-step planning）方面仍存在显著缺陷，尤其是在需要理解旋转、平移、缩放等空间变换的结构化环境中。现有方法往往依赖外部求解器或端到端强化学习（end-to-end RL），难以有效建模底层物理规律并进行稳定训练。

### 🚀 提出的新方法与创新思路
本文提出了一种**两阶段训练框架**，将复杂的空间推理任务分解为可学习的“构建块”（building blocks）及其组合策略：

1. **第一阶段：监督微调（Supervised Fine-Tuning, SFT）**
   - 在一个合成的 **ASCII-art 数据集** 上对 LLM 进行微调，使其掌握基本的空间原子操作（atomic transformations）：
     - **Translation**（平移）
     - **Rotation**（旋转）
     - **Scaling**（缩放）
   - 得到一个具备基础“空间物理直觉”的模型：`Qwen-Physics`

2. **第二阶段：基于 LoRA 的强化学习（Reinforcement Learning with GRPO）**
   - 冻结 `Qwen-Physics` 的主干参数
   - 引入轻量级 **LoRA adapters**，在闭合回路（closed-loop）环境中使用 **GRPO**（Generalized Reward Policy Optimization）优化策略
   - 学习如何将原子操作组合成有效的多步计划以达成目标配置

> 🔍 创新点总结：
> - **模块化设计**：将“感知/物理理解”与“决策/规划”分离，避免 RL 从零学习空间知识
> - **高效训练**：仅训练 LoRA 参数，大幅降低计算开销
> - **更强泛化能力**：通过构建块组合实现复杂任务求解

### ⚖️ 相比现有方法的优势
| 方法 | 缺陷 | 本论文优势 |
|------|------|------------|
| Generic LLM（如 Qwen-Instruct） | 缺乏空间先验，无法准确执行基本变换 | SFT 阶段注入空间物理知识 |
| End-to-End RL（如 Qwen-DirectRL） | 训练不稳定、收敛慢、难以内化状态记忆 | 使用物理感知模型作为初始化，提升稳定性与效率 |
| Prompting-based 方法（如 VoT） | 依赖提示工程，鲁棒性差 | 显式学习可迁移的空间操作策略 |

---

## 2. 核心实验方法和设置

### 📁 使用的数据集
- **Building Block Dataset**（用于 SFT）
  - 合成生成的 **12k 条样本**
  - 覆盖三种原子变换：translation、rotation、scaling
  - 输入输出均为 **ASCII-art 表示的形状图**
  - 形状边界用 `#` 表示，行间用 `*` 和 `\n` 分隔
  - 示例格式见附录 A

- **强化学习环境**
  - 自定义 ASCII-based RL 环境
  - 每个 episode 包含最多 5 步动作
  - 动作空间离散化：
    - `A_trans = {up, down, left, right}`
    - `A_rot = {90°CCW, 45°CCW, 180°CCW, 0°}`
    - `A_scale = {2×, ×, 1}`

### 🧪 实验设置
- **基础模型**：`Qwen2.5-1.5B-Instruct`
- **LoRA 设置**：
  - Rank `r=64`
  - 应用于 `W_Q, W_K, W_V, W_O, W_gate, W_up, W_down` 等模块
- **训练配置**：
  - 单张 NVIDIA A100 GPU（80GB）
  - Adam 优化器，学习率 `1e-5`
  - Batch size: 64 trajectories
  - 温度退火：从 1.4 → 0.7
- **评估模式**：贪婪解码（greedy decoding）

### 🎯 评估指标
- **累计奖励（Cumulative Episode Reward）**：
  $$
  R_{\text{total}} = R_{\text{correctness}} - 0.1 \times T - \sum R_{\text{rep}} + R_{\text{success}}
  $$
  - `R_correctness`：与启发式最优动作序列匹配得分
  - `-0.1*T`：鼓励短路径
  - `R_rep`：重复动作惩罚（连续两次以上相同动作）
  - `R_success=+2`：成功完成任务奖励

- **成功条件**：当前形状与目标形状的 IoU ≥ 阈值 τ

- **两种测试场景**：
  1. **Dynamic Setting**：每步后环境更新地图（提供外部状态）
  2. **Static Setting**：初始地图不变，模型需内部追踪状态变化（更难）

### 🆚 基线方法对比
| 模型 | 描述 |
|------|------|
| `Qwen-Instruct` | 原始预训练模型，无任何微调 |
| `Qwen-Physics` | 仅经过 SFT 的物理感知模型 |
| `Qwen-DirectRL` | 直接在原始模型上应用 GRPO 的端到端 RL 模型 |
| `Qwen-PhysRL`（Ours） | 本文提出的两阶段方法 |
| `Random Policy (rnd)` | 随机选择动作的下界基准 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（Table 1）

| Model | Dynamic Avg R_total | Static Avg R_total |
|-------|---------------------|--------------------|
| Qwen-Instruct | 0.070 | 0.004 |
| Qwen-Physics | -0.068 | -0.120 |
| Qwen-DirectRL | 1.626 | -0.216 |
| **Qwen-PhysRL (Ours)** | **2.457** | **1.717** |

> ✅ 最高可达总奖励为 4.7（含成功奖励），此处报告的是扣除成功奖励后的部分（max=2.7）

### 🔍 结果分析
- **Dynamic Setting**：
  - `Qwen-PhysRL` 接近理论最大值（2.457 / 2.7），说明其能有效利用环境反馈做出正确决策
  - `Qwen-DirectRL` 表现尚可但远低于 ours
  - `Qwen-Physics` 微调后反而表现下降 → 表明 SFT 单独不足以支持多步规划

- **Static Setting**（更具挑战性）：
  - `Qwen-PhysRL` 仍取得 **1.717** 的高分，显著优于所有 baseline
  - `Qwen-DirectRL` 完全失败（负分）→ 表明纯 RL 无法建立内部状态追踪机制
  - `Qwen-Instruct` 几乎无能力 → 显示通用语言模型缺乏空间记忆能力

### 📈 GRPO 收敛性分析（Figure 2）
- `Qwen-PhysRL` 比 `Qwen-DirectRL` **收敛更快、更稳定**
- 早期即达到较高奖励水平，后期波动小
- 证明：**物理感知初始化显著提升了 RL 学习效率**

### 🔍 消融实验与注意力分析（Ablation Study）

#### Observation 1：Fine-tuning 改变了中间层注意力分布
- 使用 KL 散度衡量 `Qwen-Physics` 与 `Qwen-Instruct` 的注意力差异
- **Layer 16 和 Layer 20** 差异最大 → 表明 SFT 主要在中层编码空间结构信息

#### Observation 2：更多关注空间符号（如 `#`, `*`）
- `Qwen-Physics` 在中层对 `#` 和 `*` 的注意力明显增强
- 表明模型学会了聚焦于形状轮廓等关键视觉线索

#### Observation 3：系统提示（system prompt）主导注意力
- 尽管 map 区域 token 更少，但 `Qwen-Physics` 仍倾向于分配更多注意力给 system prompt
- 但在中层（16–20）该差距缩小 → 表明这些层真正参与空间推理

> 💡 总结：SFT 成功引导模型在特定中间层建立起对空间结构的关注机制，为后续 RL 提供了良好的表示基础

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **两阶段方法优于端到端 RL**：
   - 先通过 SFT 注入空间物理先验，再用 RL 学习组合策略，显著提升性能与训练稳定性
2. **LoRA + 冻结主干是高效方案**：
   - 只训练少量适配器参数即可实现强大规划能力
3. **模型具备一定内部状态追踪能力**：
   - 在 Static Setting 下仍能取得良好表现，表明其已学会“ mentally simulate”状态演化
4. **注意力机制发生变化**：
   - Fine-tuning 显著改变了中层注意力分布，增强了对空间符号的关注，验证了空间理解的真实提升

### ⚠️ 局限性
- 当前仅适用于 **ASCII-art 符号空间**，尚未扩展至真实图像或多模态输入
- 动作空间离散且有限，难以处理连续控制任务
- 所有任务最大长度为 5 步，未验证长程规划能力
- 依赖人工设计的 parser 将文本输出映射为动作标签，存在误差风险

### 🔮 未来工作方向
- 扩展到 **Vision-Language Models (VLMs)**，结合真实图像中的空间推理
- 探索更复杂的环境（如 Sokoban、Maze）和更长的任务序列
- 研究自动发现“构建块”的方法，而非人工定义
- 将该范式应用于机器人导航、自动驾驶等实际应用场景
- 构建通用的“空间智能”预训练框架

---

## ✅ 总体评价
本文提出了一种新颖且高效的 LLM 空间推理训练范式——**“Building Blocks + RL Composition”**，通过模块化设计解决了传统方法在空间理解和多步规划上的瓶颈。实验证明该方法不仅性能优越，而且训练更稳定，具有较强的可解释性和推广潜力，为构建具备空间认知能力的语言智能体提供了重要思路。

</details>

---

### 7. [Joint Selection for Large-Scale Pre-Training Data via Policy Gradient-based Mask Learning](https://arxiv.org/abs/2512.24265)

**Authors**: Ziqing Fan, Yuqiao Xian, Yan Sun, Li Shen  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.24265v1  

#### Abstract
A fine-grained data recipe is crucial for pre-training large language models, as it can significantly enhance training efficiency and model performance. One important ingredient in the recipe is to select samples based on scores produced by defined rules, LLM judgment, or statistical information in ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Joint Selection for Large-Scale Pre-Training Data via Policy Gradient-based Mask Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大规模语言模型（LLM）预训练中，**数据选择策略**对模型性能至关重要。然而，现有的数据筛选方法通常只关注单一指标（如质量或多样性），存在以下问题：
- **仅基于质量（quality-only）的方法**：虽然初期表现良好，但在长期预训练中出现严重**性能衰减（diminishing returns）**，原因是高质量样本语义冗余高，缺乏多样性。
- **仅基于多样性（diversity-only）的方法**：虽然提升了覆盖度，但会**过度剔除有价值的高质量样本**，导致最终性能甚至低于原始数据集。
- **联合优化的计算瓶颈**：多样性指标通常是集合函数（set function），传统贪婪算法（greedy algorithm）求解成本极高，在万亿级 token 数据集上不可行。

### 提出的新方法：DATAMASK
作者提出 **DATAMASK**，一个新颖且高效的**联合学习框架**，用于大规模预训练数据的选择。其核心思想是将数据选择过程建模为**掩码学习（mask learning）问题**，通过策略梯度（policy gradient）进行优化。

#### 创新点：
- **统一优化多目标**：首次在单一流程中高效联合优化**质量（quality）和多样性（diversity）** 两类指标。
- **掩码学习范式**：引入可学习的二值掩码 $M$，将离散的选择问题转化为连续的参数优化问题。
- **策略梯度估计（PGE）**：采用强化学习中的策略梯度方法，并引入**组相对优势（group relative advantage）** 作为基线，显著降低方差，加速收敛。
- **多种加速技术**：结合质量感知剪枝（pruning）、初始化、批更新（batch updating）等策略，大幅提升效率。

### 相比现有方法的优势
- **效率提升巨大**：相比传统的贪婪算法（如 DiSF），**选择时间减少 98.9%**，使得在万亿级 token 数据上进行联合学习成为可能。
- **性能更优**：生成的数据子集在多个任务上显著优于仅基于质量或多样性的方法。
- **灵活性高**：框架支持任意组合的质量和多样性指标，具有良好的扩展性。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **主数据集**：**FineWeb**，一个包含 **15 万亿 token** 的大规模网页文本语料库。
- **构建的子集**：使用 DATAMASK 从 FineWeb 中选出约 **1.5 万亿 token** 的高质量多样化子集，命名为 **FineWeb-Mask**。
- **其他对比数据集**：
  - `FineWeb-Edu`：基于教育质量筛选。
  - `UltraFineWeb-en`：基于 fastText 分类器筛选的英文部分。
  - `FineWeb-Semdedup`：基于语义去重（semantic deduplication）。
  - `FineWeb-DCLM`：基于 DCLM 质量分类器筛选。
  - `FineWeb-Pro`：基于 Prox 方法筛选的小规模子集。

### 实验设置和评估指标
- **模型架构**：
  - **1.5B dense model**：基于 Qwen-2.5 架构。
  - **7B MoE model**：包含 10 个专家的稀疏模型。
- **预训练设置**：
  - 预训练 token 数量：dense 模型 400B，MoE 模型 300B。
  - 优化器：Adam，学习率 4e-4，batch size 1536，序列长度 8192。
- **评估基准**：在 **12 个多样化任务** 上评估，涵盖三大能力：
  - **阅读理解**（RACE-High/Middle）
  - **世界知识**（TriviaQA, HellaSwag, Natural Questions, MMLU 等）
  - **推理能力**（ARC-Challenge, SIQA, PIQA, WinoGrande）

### 基线方法对比
- **质量导向基线**：FineWeb-Edu, UltraFineWeb-en, FineWeb-DCLM
- **多样性导向基线**：FineWeb-Semdedup
- **原始数据**：FineWeb（未筛选）
- **其他开源数据集**：DCLM-BASELINE（跨数据源对比）

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **FineWeb-Mask 在 1.5B dense 模型上的平均提升**：
  - 相比原始 FineWeb：**+3.2%**
  - 相比最佳基线（FineWeb-DCLM）：**+0.9%**
- **在 7B MoE 模型上的平均提升**：
  - 相比原始 FineWeb：**+1.9%**
  - 相比最佳基线（FineWeb-DCLM）：**+0.4%**

> 注：原文摘要称“3.2% on 1.5B”和“1.9% on 7B”，Table 2 显示为 3.2% 和 1.9%，与摘要一致。

### 与基线方法的对比结果
| 方法 | 1.5B Dense 模型提升 | 7B MoE 模型提升 |
|------|---------------------|-----------------|
| **FineWeb-Mask (Ours)** | **+3.2%** vs FineWeb | **+1.9%** vs FineWeb |
| FineWeb-DCLM | +2.0% | +1.5% |
| FineWeb-Edu | +1.4% | +0.5% |
| FineWeb-Semdedup | **负向影响**（性能下降） | **负向影响** |

- **多样性方法表现差**：`FineWeb-Semdedup` 在两种模型上均表现最差，验证了“多样性方法会剔除过多高质量样本”的假设。
- **质量方法有衰减**：`FineWeb-Edu` 等方法初期领先，但随着预训练 token 增加，性能增长放缓。

### 消融实验结果
- **多样性指标选择**：
  - **Pair-wise Similarity** 和 **Facility Location** 有效提升性能。
  - **DiSF values** 表现不佳，可能因其与质量分数冲突较大。
- **平衡系数 $\lambda$**：
  - 最佳范围在 **$\lambda = 0.1 \sim 0.5$**（即多样性权重占 50%~90%）。
- **初始化策略**：
  - **Quality-aware 初始化**（按质量分初始化 logits）能显著加速收敛，选择时间从 10 小时降至 7 小时。
- **批更新（Batch Updating）**：
  - 使用 5%~10% 的样本更新 logits 可大幅减少计算时间，并有助于跳出局部最优。
- **质量感知剪枝（Pruning）**：
  - 过滤掉质量分最低的 40-50% 样本后，选择时间和性能均显著提升（Table 1）。

---

## 4. 关键结论和发现

### 主要发现
1. **单一指标选择存在根本局限**：
   - 质量导向 → 语义冗余高 → 长期预训练收益递减。
   - 多样性导向 → 剔除过多高质量样本 → 性能下降。
2. **联合优化是必要且有效的**：
   - 通过 DATAMASK 同时优化质量和多样性，能显著提升模型最终性能。
3. **效率瓶颈可通过新范式突破**：
   - 将数据选择视为掩码学习问题，结合策略梯度，成功将选择时间降低 98.9%，解决了传统贪婪算法的可扩展性问题。
4. **FineWeb-Mask 是高质量子集**：
   - 仅用 10% 的 FineWeb 数据（1.5T tokens），即可超越原始全量数据及其他开源数据集。

### 方法的局限性
- **依赖预定义指标**：仍需人工设计或选择质量与多样性指标，自动化程度有限。
- **超参数敏感**：平衡系数 $\lambda$、学习率、G 值等需调优。
- **初始质量过滤依赖阈值**：剪枝阈值（如质量分 < 4）需手动设定。

### 未来工作方向
- 探索更复杂的指标组合方式，例如非线性融合。
- 将该框架应用于其他模态（如图像、音频）的预训练数据选择。
- 结合主动学习或在线学习机制，实现动态数据选择。
- 进一步优化计算效率，支持更大规模的实时筛选。

---

> **代码与数据公开**：
> - GitHub: [https://github.com/ByteDance-Seed/DATAMASK](https://github.com/ByteDance-Seed/DATAMASK)
> - 数据集: [https://huggingface.co/datasets/DATA-MASK/FineWeb-Mask](https://huggingface.co/datasets/DATA-MASK/FineWeb-Mask)

</details>

---

### 8. [Understanding LLM Checkpoint/Restore I/O Strategies and Patterns](https://arxiv.org/abs/2512.24511)

**Authors**: Mikaila J. Gossman, Avinash Maurya, Bogdan Nicolae, Jon C. Calhoun  
**Category**: cs.DC  
**Published**: 2026-01-01  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.24511v1  

#### Abstract
As LLMs and foundation models scale, checkpoint/restore has become a critical pattern for training and inference. With 3D parallelism (tensor, pipeline, data), checkpointing involves many processes, each managing numerous tensors of varying shapes and sizes, that must be persisted frequently to stab...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Understanding LLM Checkpoint/Restore I/O Strategies and Patterns

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）和基础模型（FMs）在训练和推理过程中依赖**频繁的 Checkpoint-Restore（C/R）操作**，以实现容错、暂停恢复、调试等目标。然而，由于现代 LLM 使用 3D 并行（data, tensor, pipeline）策略，导致每个 checkpoint 包含大量分布在数千个 GPU 上的小型、异构张量（tensors），产生海量小文件写入/读取请求。

这带来了典型的“大数据”挑战——**高频率（velocity）、多样性（variety）、大体积（volume）**，进而引发严重的 I/O 瓶颈，尤其是在元数据管理、缓存一致性、存储栈层级传输效率等方面。传统基于 POSIX 的 I/O 接口（如 `write()`/`read()`）已难以满足需求。

此外，当前主流 C/R 引擎（如 TorchSnapshot、DataStates-LLM）虽然采用并行化设计，但其“file-per-shard”策略加剧了文件系统元数据压力，且缺乏对现代高性能 I/O 库（如 liburing）和底层文件系统的协同优化。

---

### 提出的新方法与思路
本文提出了一套针对 LLM C/R 场景的 **I/O 特征分析框架与优化策略**，核心思想是：

- 构建一个微基准测试工具（microbenchmark），用于系统评估 **kernel-accelerated I/O 库（liburing）** 在真实 LLM 工作负载下的表现；
- 深入研究 **I/O 聚合（aggregation）、对齐（alignment）、合并（coalescing）** 等策略如何影响性能；
- 分析 **buffered I/O vs. direct I/O（O_DIRECT）** 对读写性能的影响差异；
- 揭示现有生产级 C/R 引擎（TorchSnapshot、DataStates-LLM）的设计缺陷，并提出改进方向。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **方法论创新** | 首次系统性地将 liburing 等现代 I/O 接口引入 LLM C/R 性能分析，填补了该领域的空白； |
| **更贴近现实的工作负载建模** | 不仅使用合成负载，还构建了基于 BLOOM-3B、LLaMA-7B、LLaMA-13B 的真实分布模型，覆盖不同规模和并行配置； |
| **揭示深层瓶颈** | 发现内存分配开销（memory allocation）、非聚合 I/O、元数据争用是主要性能限制因素，而非单纯的 I/O 吞吐能力； |
| **指导性强** | 为下一代 C/R 框架提供了明确的设计原则：聚合 + 预分配缓冲区 + O_DIRECT + 批处理提交。 |

---

## 2. 核心实验方法和设置

### 使用的数据集/模型
并非传统意义上的“数据集”，而是模拟以下三种典型 LLM 的 checkpoint 结构：
- **BLOOM-3B**：运行于 4 个 GPU（4 ranks）
- **LLaMA-7B**：运行于 8 个 GPU（8 ranks）
- **LLaMA-13B**：运行于 16 个 GPU（16 ranks）

每种模型生成数百个大小不一的文件（从 KB 到 GB），符合实际训练中 tensor、optimizer states、metadata 的混合分布（见 Figure 4）。

---

### 实验平台（Testbed）
在 **Argonne 国家实验室的 Polaris 系统**上进行实验：
- 节点数：560
- CPU：AMD EPYC Milan 753P
- GPU：4×A100-40GB
- 内存：512 GB DDR4
- 存储后端：Lustre-based Parallel File System (PFS)，总带宽 650 GB/s
- 客户端配置：40 OSSes, 160 OSTs，条带大小设为 64MB

---

### 实验设置与评估指标

#### 两类基准测试：
1. **Synthetic Benchmark**
   - 每个 rank 写入连续的大块 buffer（128MB–8GB）
   - 控制变量：聚合粒度、I/O 模式、队列深度
   - 目标：测量 liburing 的理论峰值吞吐

2. **Representative LLM Benchmark**
   - 复现真实模型的 tensor 分布、数量、尺寸
   - 支持多种聚合策略（file-per-tensor, file-per-process, single aggregated file）
   - 更真实反映碎片化、非对齐、多对象场景

#### 聚合策略对比：
- **File-per-Tensor**：最细粒度，对应 DeepSpeed/TorchSnapshot 默认行为
- **File-per-Process**：每个 rank 一个文件
- **Single Aggregated File**：所有 rank 共享一个大文件，按偏移写入

#### I/O 模式对比：
- 是否启用 `O_DIRECT` 标志
- 使用 **liburing** vs. **POSIX** 接口

#### 评估指标：
- **Write Throughput (GB/s)**：checkpoint 阶段写入速度
- **Read Throughput (GB/s)**：restore 阶段读取速度
- **End-to-End Latency**：完整 C/R 时间
- **Metadata Pressure**：通过文件数量间接衡量

---

### 基线方法对比
- **TorchSnapshot**（PyTorch 官方快照引擎）：使用 libaio，将大对象切分为 ≤512MB 的 chunk，写入嵌套目录结构
- **DataStates-LLM**：使用 liburing，支持异步 checkpoint，但仍为每个 shard 创建独立文件
- **Ideal Baseline**：作者构建的理想化 microbenchmark，使用单个聚合文件 + 预分配 buffer + liburing 批量提交

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 写入吞吐提升显著（vs. 生产引擎）：
| 方法 | 最大写入吞吐提升倍数（vs. DataStates-LLM） | vs. TorchSnapshot |
|------|--------------------------|------------------|
| 本文提出的聚合 + liburing 方案 | **3.9×** | **7.6×** |

> 来源：Figure 18，在 13B 模型下达到最大差距

#### ✅ 读取吞吐也有明显改善：
| 方法 | 最大读取吞吐提升倍数（vs. DataStates-LLM） | vs. TorchSnapshot |
|------|----------------------------|------------------|
| 本文方案 | **3.6×** | **3.8×** |

> 小文件越多、碎片越严重，优势越明显

#### ✅ 单节点写入吞吐随数据量增长而饱和：
- 写入吞吐在 per-rank 数据量达 ~2GB 后趋于平稳（Figure 7）
- 表明存在“最优聚合阈值”，超过后收益递减

#### ✅ 读取性能普遍低于写入（反常识现象）：
- 在本平台上，**restore 比 checkpoint 更慢**
- 主因：restore 需同步串行处理多个小对象，且涉及动态内存分配

---

### 与基线方法的对比结果

| 对比维度 | TorchSnapshot | DataStates-LLM | 本文理想方案 |
|--------|---------------|----------------|--------------|
| 文件组织 | 极度碎片化（≤512MB/chunk） | file-per-shard | 单文件聚合 |
| I/O 接口 | libaio（老旧） | liburing | liburing |
| 缓冲机制 | 动态分配 | 动态分配 | **预分配 + 复用** |
| 元数据压力 | 极高（深目录+多文件） | 高 | 极低 |
| 写吞吐 | 最差 | 中等 | **最佳（最高 7.6×）** |
| 读吞吐 | 差 | 较差 | **最佳（最高 3.8×）** |
| 可扩展性 | 几乎无扩展性 | 有限扩展性 | 良好扩展性 |

> 图 3 显示：相比“理想方案”，TorchSnapshot 导致迭代时间延长 4.5×，DataStates-LLM 延长 1.8×

---

### 消融实验结果（Ablation Study）

#### （1）聚合策略的影响（Figure 5–8）
- **file-per-shard**：性能最差，尤其在多进程时下降明显
- **file-per-process / single file**：性能接近，均优于前者约 34%
- 原因：减少元数据查找、权限检查、锁竞争等内核开销

#### （2）O_DIRECT 的影响（Figure 9–10）
| 操作 | O_DIRECT 效果 |
|------|----------------|
| **Writes** | 提升巨大：liburing 下 **+4.8×**，POSIX 下 +2.2×（避免双缓冲） |
| **Reads (<1GB)** | 性能下降（无法利用 page cache） |
| **Reads (>4GB)** | 性能持平甚至略优（避免 cache 淘汰开销） |
| **混合模式尝试（仅写用 O_DIRECT）** | ❌ 失败！反而降低整体性能最多达 3×，因缓存污染 |

✅ **结论：统一启用 O_DIRECT 是最佳选择**

#### （3）内存分配开销分析（Figure 13）
- 在 DataStates-LLM 的 restore 过程中，**内存分配耗时 ≈ 实际读取时间**
- 若去除该开销（预分配 buffer），读吞吐可**翻倍**（Figure 14）

---

## 4. 关键结论和发现

### 主要发现

1. 🔹 **I/O 聚合至关重要**  
   “file-per-shard” 虽简化编程模型，但严重损害 PFS 性能。**聚合为单个大文件或 per-process 文件可大幅提升吞吐、降低元数据压力**。

2. 🔹 **liburing 有潜力，但需配合正确策略才能发挥优势**  
   单纯使用 liburing 不足以解决问题，必须结合：
   - 请求聚合（request coalescing）
   - 缓冲区预分配（preallocation）
   - 对齐写入（alignment）
   才能逼近硬件极限。

3. 🔹 **O_DIRECT 对写有利、对小读不利，但统一开启仍是最佳实践**  
   尽管关闭缓存会牺牲小读性能，但在大规模并发 restore 场景下，缓存失效和管理开销更大，因此推荐全程启用 `O_DIRECT`。

4. 🔹 **现有 C/R 引擎的主要瓶颈不在 I/O 层，而在运行时开销**  
   如 **动态内存分配、序列化、元数据解析** 等 CPU 开销成为新的瓶颈，尤其是 restore 阶段。

5. 🔹 **LLM C/R 的 I/O 模式与传统 HPC 截然不同**  
   传统科学计算常为大块连续数据，而 LLM checkpoint 是高度碎片化的异构集合，不能简单沿用旧有优化经验。

---

### 方法的局限性
- 当前 microbenchmark 尚未完全集成到真实训练流程中，部分假设（如完美偏移对齐）在动态调度环境中可能难实现；
- 单一聚合文件虽高效，但在故障恢复时不够灵活（无法局部恢复）；
- 未考虑网络拓扑、RDMA、NVMe-oF 等更先进存储架构的影响；
- 所有实验基于 Lustre，其他 PFS（如 GPFS、BeeGFS）的行为可能有所不同。

---

### 未来工作方向
1. **在 DataStates-LLM 等生产框架中集成优化策略**：
   - 实现持久化缓冲区池（persistent buffer pool）
   - 支持对象聚合（object grouping）与批量提交
   - 多线程完成队列处理

2. **开发 hybrid aggregation 策略**：
   - 在用户空间先聚合逻辑对象，再通过 kernel 提交批处理请求
   - 平衡灵活性与性能

3. **探索智能预取与分层存储管理**：
   - 利用 checkpoint 历史预测 restore 模式
   - 结合 SCM/NVM 加速元数据访问

4. **构建面向 LLM 的专用 I/O profiler 工具**：
   - 支持 liburing、io_uring 的细粒度追踪
   - 替代 Darshan 在异步 I/O 场景中的不足

--- 

> 📌 **一句话总结**：  
> 本文揭示了 LLM Checkpoint/Restore 中 I/O 性能的关键瓶颈在于**碎片化 I/O 和运行时开销**，并通过系统实验证明：**结合 liburing、I/O 聚合、O_DIRECT 和预分配缓冲区，可实现高达 7.6× 的写吞吐提升**，为下一代高性能 C/R 框架指明了优化路径。

</details>

---

### 9. [Reliable and Resilient Collective Communication Library for LLM Training and Serving](https://arxiv.org/abs/2512.25059)

**Authors**: Wei Wang, Nengneng Yu, Sixian Xiong, Zaoxing Liu  
**Category**: cs.DC  
**Published**: 2026-01-01  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.25059v1  

#### Abstract
Modern ML training and inference now span tens to tens of thousands of GPUs, where network faults can waste 10--15\% of GPU hours due to slow recovery. Common network errors and link fluctuations trigger timeouts that often terminate entire jobs, forcing expensive checkpoint rollback during training...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Reliable and Resilient Collective Communication Library for LLM Training and Serving**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现代大规模机器学习（尤其是大语言模型 LLM）训练和推理依赖于成百上千张 GPU 组成的分布式集群，而网络故障（如 NIC 失效、链路波动）频繁发生，导致任务中断。传统容错机制（如 Checkpointing）恢复时间长（中位数达 68 分钟），造成大量 GPU 资源浪费（可高达 10–15%）。此外，现有通信库（如 NCCL）在运行时无法处理网络故障，一旦出现连接超时即终止整个作业。

本文指出，**即使部分网络路径失效，多 NIC 架构下仍存在冗余通信路径**，但现有系统未能有效利用这些资源进行“热修复”（hot repair），而是选择重启，代价高昂。

---

### **提出了什么新方法或新思路**
作者提出 **R2CCL**（Reliable and Resilient Collective Communication Library），一个**高可靠、高弹性的集体通信库**，作为 NCCL/RCCL 的插件式替代方案，实现以下三大核心功能：

#### **(1) 快速故障检测与定位**
- 引入 **out-of-band (OOB) 通知通道**，实现双边错误感知（bilateral failure awareness），避免单边等待死连接。
- 利用轻量级 **RDMA 探针（probe-based fault localization）** 进行三点三角测量，精确定位故障发生在本地 NIC、远端 NIC 还是物理链路上。

#### **(2) 零丢包的实时连接迁移（Live Migration）**
- **Multi-NIC GPU Buffer Registration**：初始化时将每个 GPU 缓冲区注册到所有 NIC，避免故障切换时重新注册带来的延迟。
- **DMA-buffer Rollback**：发送方回滚到最后确认的数据块，接收方重置状态，在备用 NIC 上安全重传剩余数据，确保数据完整性。

#### **(3) 故障感知的在线调度优化**
- **R2CCL-Balance**：针对除 AllReduce 外的所有集体操作，动态将流量从故障 NIC 均衡分配到剩余健康 NIC，考虑 PCIe、NUMA 和 PXN 拓扑。
- **R2CCL-AllReduce**：改进环形 AllReduce 算法，采用“全局 AllReduce + 局部 AllReduce + 定制广播”的两阶段策略，降低瓶颈节点负载。
- **递归式 R2CCL-AllReduce**：支持多故障场景，通过逻辑重排序（logical re-ranking）插入“桥接节点”，解决 rail 不匹配问题，并递归分解通信图以适应带宽谱系。

---

### **相比现有方法的优势**
| 对比维度 | R2CCL | AdapCC | DéjaVu |
|--------|-------|--------|--------|
| **恢复方式** | 运行时热修复，不中断作业 | 排除故障节点，可能损失梯度 | KV Cache 复制，请求重路由 |
| **恢复延迟** | < 1ms（连接迁移） | 需重新启动 | 数秒至数十秒 |
| **通信开销** | 极低（<1% 训练，<3% 推理） | 中等（需重建拓扑） | 高（复制 KV Cache 占用内存和带宽） |
| **适用场景** | 所有集体操作，支持 mid-operation 故障 | 仅限迭代间故障 | 仅限推理 |
| **是否修改应用层** | 否（透明替换 NCCL） | 是（需协调器） | 是（需集成框架） |

> ✅ **核心优势**：R2CCL 在传输层实现透明容错，无需重启、无梯度丢失、低开销，适用于训练和推理全场景。

---

## **2. 核心实验方法和设置**

### **实验平台**
- **物理测试床**：两个服务器节点，每节点配备：
  - 8 × NVIDIA H100 GPU
  - 8 × InfiniBand 400Gbps NIC
  - 支持 NVLink 4.0（900GB/s）
- **大规模仿真**：使用 **SimAI** 模拟器，模拟最多 1024 GPU 的集群，配置为 A100 + 200Gbps NIC，支持 RoCE-v2 rail 优化拓扑。

---

### **评估指标**
| 场景 | 主要指标 |
|------|---------|
| **训练** | Token/s 吞吐量、相对无故障基准的性能开销、迭代时间 |
| **推理** | Time to First Token (TTFT)、Time Per Output Token (TPOT)、p50/p95/p99 延迟 |
| **微基准测试** | AllReduce、AllGather、ReduceScatter、Send/Recv 的带宽利用率 |

---

### **基线方法对比**
| 场景 | 基线方法 |
|------|----------|
| **训练** | 
| - Vanilla NCCL | 遇故障直接崩溃 |
| - AdapCC | 排除故障 GPU，重构通信图 |
| **推理** |
| - NCCL + Restart Server | 服务重启，延迟 35 秒 |
| - NCCL + Reroute Requests | 请求重定向，负载翻倍 |
| - DéjaVu | KV Cache 复制，支持故障恢复 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **训练性能（Megatron-LM）**
| 模型 | 配置 | 方法 | 性能（tokens/s） | 相对无故障开销 |
|------|------|------|------------------|----------------|
| GPT-3 2.7B | DP=16 | R2CCL-AllReduce | 312,394 | **0.71%** |
| GPT-3 2.7B | DP=16 | R2CCL-Balance | 310,000 | 1.32% |
| GPT-3 2.7B | DP=16 | AdapCC | 287,000 | 8.65% |
| Llama-13B | TP=8, PP=2 | R2CCL-Balance | 8,309 | **0.38%** |
| Llama-13B | TP=8, PP=2 | R2CCL-HotRepair | 8,220 | 1.31% |
| Llama-13B | TP=8, PP=2 | AdapCC | 0 | ❌ 不兼容 |

> 🔹 **R2CCL 在训练中开销 <1%，显著优于 AdapCC（最高快 12.18×）**

#### **多故障场景（SimAI 模拟 512-GPU 集群）**
| 故障数量 | R2CCL 开销增长趋势 |
|---------|--------------------|
| 1 个 NIC 故障 | 1.5% |
| 10 个并发 NIC 故障 | **仅 4.3%**（亚线性增长） |

> 🔹 表明 R2CCL 具备强扩展性和鲁棒性。

#### **推理性能（vLLM + Llama-3.1）**
| 模型 | 方法 | TTFT 开销 | TPOT 开销 | 吞吐提升（vs Restart） |
|------|------|-----------|-----------|------------------------|
| Llama-3.1-405B | R2CCL-Balance | **~0–3%** | **~3%** | **1.9×–2.6×** |
| OPT-66B / BLOOM-176B | R2CCL vs DéjaVu | **0.71–1.58%** | — | **8.6×–47× 更低恢复开销** |

> 🔹 R2CCL 推理恢复开销比 DéjaVu 低 **47×**，比非容错基线低 **113×**。

#### **微基准测试（AllReduce）**
| 数据大小 | R2CCL-Balance 吞吐率 | R2CCL-AllReduce 吞吐率 |
|---------|----------------------|------------------------|
| 小消息 (<32MB) | 92% 正常水平 | 66%（因协调开销） |
| 大消息 (≥512MB) | 83% | **93%**（最优） |

> 🔹 R2CCL 动态选择策略（基于 α-β 模型），自动适配不同硬件和消息尺寸。

---

## **4. 关键结论和发现**

### **主要发现**
1. **网络故障不应导致全作业重启**：利用多 NIC 冗余路径，可在毫秒级完成热修复，避免昂贵的 Checkpoint 回滚。
2. **R2CCL 实现近乎零开销的容错**：
   - 训练开销 **<1%**
   - 推理开销 **<3%**
   - 恢复时间 **<1ms**
3. **相比现有容错系统具有压倒性优势**：
   - 训练效率比 AdapCC 高 **12.18×**
   - 推理恢复开销比 DéjaVu 低 **47×**
4. **支持复杂多故障场景**：通过拓扑感知重排序和递归调度，有效应对 rail 不匹配和带宽异构问题。

---

### **方法的局限性**
1. **不支持 NVLink/NVSwitch 故障**：仅处理 inter-node 通信故障，intra-node fabric 故障不在范围内。
2. **依赖多 NIC 硬件**：若节点只有一个 NIC，则无法提供冗余路径。
3. **无法处理进程崩溃或全网分区**：仅适用于 NIC/链路级软故障。
4. **PXN 转发引入额外跳数**：虽开销小，但在极端情况下可能影响性能。

---

### **未来工作方向**
1. **扩展至 NVLink 故障恢复**：探索 intra-node 通信的弹性机制。
2. **支持更复杂的拓扑自愈**：结合 AI 驱动的调度器进行预测性修复。
3. **集成到更多框架**：如 DeepSpeed、ColossalAI 等。
4. **跨数据中心容错**：研究 geo-distributed 场景下的 R2CCL 扩展。

---

> 📌 **总结**：R2CCL 是首个在运行时实现**无缝、低开销、多故障容忍**的集体通信库，通过连接迁移 + 智能调度，在不中断训练/推理的前提下大幅提升系统可靠性，为大规模 LLM 系统提供了关键基础设施支持。  
> 🔗 项目开源地址：[https://github.com/r2cc-project/R-2CCL](https://github.com/r2cc-project/R-2CCL)

</details>

---

### 10. [Let It Flow: Agentic Crafting on Rock and Roll, Building the ROME Model within an Open Agentic Learning Ecosystem](https://arxiv.org/abs/2512.24873)

**Authors**: Weixun Wang, XiaoXiao Xu, Wanhe An, Fangwen Dai, Wei Gao, Yancheng He, Ju Huang, Qiang Ji, Hanqi Jin, Xiaoyang Li, Yang Li, Zhongwen Li, Shirong Lin, Jiashun Liu, Zenan Liu, Tao Luo, Dilxat Muhtar, Yuanbin Qu, Jiaqiang Shi, Qinghui Sun, Yingshui Tan, Hao Tang, Runze Wang, Yi Wang, Zhaoguo Wang, Yanan Wu, Shaopan Xiong, Binchen Xu, Xander Xu, Yuchi Xu, Qipeng Zhang, Xixia Zhang, Haizhou Zhao, Jie Zhao, Shuaibing Zhao, Baihui Zheng, Jianhui Zheng, Suhang Zheng, Yanni Zhu, Mengze Cai, Kerui Cao, Xitong Chen, Yue Dai, Lifan Du, Tao Feng, Tao He, Jin Hu, Yijie Hu, Ziyu Jiang, Cheng Li, Xiang Li, Jing Liang, Chonghuan Liu, ZhenDong Liu, Haodong Mi, Yanhu Mo, Junjia Ni, Shixin Pei, Jingyu Shen, XiaoShuai Song, Cecilia Wang, Chaofan Wang, Kangyu Wang, Pei Wang, Tao Wang, Wei Wang, Ke Xiao, Mingyu Xu, Tiange Xu, Nan Ya, Siran Yang, Jianan Ye, Yaxing Zang, Duo Zhang, Junbo Zhang, Boren Zheng, Wanxi Deng, Ling Pan, Lin Qu, Wenbo Su, Jiamang Wang, Wei Wang, Hu Wei, Minggang Wu, Cheng Yu, Bing Zhao, Zhicheng Zheng, Bo Zheng  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.24873v1  

#### Abstract
Agentic crafting requires LLMs to operate in real-world environments over multiple turns by taking actions, observing outcomes, and iteratively refining artifacts. Despite its importance, the open-source community lacks a principled, end-to-end ecosystem to streamline agent development. We introduce...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Let It Flow: Agentic Crafting on Rock and Roll, Building the ROME Model within an Open Agentic Learning Ecosystem*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前大语言模型（LLM）在复杂、多轮交互任务中的表现受限于缺乏一个**端到端的、可扩展的 agentic 学习生态系统**。传统方法如一次性生成（one-shot generation）无法支持迭代推理和环境反馈闭环；而现有的代理系统往往依赖人工演示或不稳定的强化学习（RL）训练流程，在长周期任务中面临奖励稀疏、信用分配困难、训练不稳定等问题。

此外，开源社区缺乏统一的基础设施来支持从数据合成、环境执行、训练优化到部署评估的全流程，严重阻碍了 agentic 模型的实际落地。

---

### **提出了什么新方法或新思路**

本文提出了一套完整的 **Agentic Learning Ecosystem (ALE)**，并基于该生态构建了开源代理模型 **ROME (ROME is Obviously an Agentic ModEl)**。其核心创新包括：

#### （1）**系统级架构：ALE 三大组件协同设计**
- **ROLL (Reinforcement Learning Optimization for Large-Scale Learning)**  
  一个可扩展的 agentic RL 训练框架，支持多环境并行 rollout、异步训练、动态 GPU 复用，显著提升训练吞吐量与稳定性。
- **ROCK (Reinforcement Open Construction Kit)**  
  一个安全沙箱环境管理器，提供可复现、隔离的执行环境（如 Docker），用于轨迹生成、验证与运行时控制，确保数据与行为的安全性和可靠性。
- **iFlow CLI**  
  一个灵活的 agent 框架，负责上下文工程（context engineering）、工具调用编排，并通过“代理原生模式”（agent native mode）实现训练与部署的一致性。

这三者共同构成了一个闭环的 agentic 工作流：`任务 → 动作 → 执行 → 反馈 → 学习`。

#### （2）**新型策略优化算法：IPA (Interaction-Perceptive Agentic Policy Optimization)**

针对传统 token-level RL 在长周期任务中信用分配不准的问题，提出将 MDP 建模为 **Chunked MDP**，以语义上有意义的“交互块”（interaction chunk）为单位进行信用分配：
- 每个 chunk 包含一次完整的“思考 → 工具调用 → 观察反馈”的循环。
- 引入 **chunk-level 回报计算、重要性采样与梯度掩码**，提升长期任务的训练稳定性和效率。

#### （3）**高质量 agentic 数据合成流水线**

提出两阶段数据构建策略：
- **基础数据层**：基于 GitHub Issue-PR 对构建代码定位、修复、测试生成等任务，增强基本编码能力。
- **agentic 数据层**：通过多智能体协作框架自动生成带可执行环境（Docker）、单元测试和反馈信号的任务实例与轨迹，覆盖软件工程、终端操作、GUI 自动化等场景。
- 引入四阶段过滤机制（启发式规则、LLM 判断、执行模拟、专家审查）保证数据质量。

#### （4）**提出更严格的基准：Terminal Bench Pro**

为解决现有 benchmark（如 Terminal-Bench 1.0/2.0）规模小、领域不平衡、污染风险高等问题，构建了 **Terminal Bench Pro**：
- 包含 400 个任务（200 公开 + 200 私有）
- 覆盖 8 个 CLI 相关领域（数据处理、调试、系统管理、安全等），每类 50 项，确保均衡性
- 所有任务均配备确定性环境与高覆盖率测试用例，防止“走捷径”式通过

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **系统完整性** | 首次提供从数据 → 环境 → 训练 → 部署全栈开源解决方案，填补生态空白 |
| **训练效率与稳定性** | ROLL 支持异步训练 + GPU 动态复用，IPA 提升长周期信用分配精度，减少崩溃风险 |
| **数据质量与安全性** | ROCK 提供强隔离环境，结合多阶段验证，有效避免虚假正样本与安全隐患（如挖矿、反向隧道） |
| **性能表现** | ROME（仅激活 3B 参数）在多个 benchmark 上超越更大模型（甚至 >100B 参数） |
| **可复制性与公平评估** | Terminal Bench Pro 提供更高标准的评估基准 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 类型 | 名称与说明 |
|------|-----------|
| **基础预训练数据** | 来自约 100 万高质量 GitHub 仓库的项目级代码数据（经去重、清洗后约 100B tokens） |
| **SFT 数据** | 合成百万级多轮交互样本，涵盖：<br>- 代码修复（Issue → PR）<br>- 单元测试生成<br>- 多轮反馈演化轨迹（来自 PR 评论）<br>- 工具使用对话（内部日志 + 合成） |
| **RL 数据** | 基于多 agent 流水线生成的 76K 高质量 agentic 实例与轨迹，总约 30B tokens |
| **安全对齐数据** | 构建红队攻击场景（prompt 注入、恶意依赖、越权操作），训练模型识别并规避风险行为 |

---

### **实验设置与评估指标**

#### **模型配置**
- 基座模型：Qwen3-MoE 架构
- 模型名称：ROME (30B-A3B)，即总参数 30B，每次激活约 3B
- 训练流程：Continual Pre-training → Two-stage SFT → IPA-based RL

#### **评估维度**
1. **Terminal-based Execution**  
   - Terminal-Bench 1.0 / 2.0
   - SWE-bench Verified / Multilingual
   - **Terminal Bench Pro (Public & Private)**
2. **Tool-use Ability**  
   - TAU2-Bench (Retail/Airline/Telecom)
   - BFCL-v3 (Multi-turn)
   - MTU-Bench (Single/Multi-turn)
3. **General Agentic Capabilities**  
   - GAIA
   - BrowseComp-ZH
   - ShopAgent (Single/Multi-turn)

#### **评估指标**
- 主要指标：Pass@1（平均 3 次独立运行结果）
- 生成参数：temperature=0.7, top_p=0.8, top_k=20
- 最大输出长度：65,536 tokens
- 所有 CLI 任务通过 iFlow CLI 统一执行

#### **基线方法对比**
- 开源模型：Qwen3-Coder-30B-A3B-Instruct, Devstral-Small-2, GPT-OSS-120B
- 商业闭源模型（报告引用）：Claude-Haiku-4.5, Gemini-2.5 Flash, GPT-5 Mini, Kimi-K2, DeepSeek-V3.1, GLM-4.6

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

#### ✅ **Terminal-based Benchmarks (Normal Models)**

| Benchmark | ROME | Qwen3-Coder-30B | GPT-OSS-120B | Best Closed-source |
|----------|------|------------------|---------------|--------------------|
| Terminal-Bench 1.0 | **41.50%** | 28.50% | 31.25% | — |
| Terminal-Bench 2.0 | **24.72%** | 13.48% | 21.12% | — |
| SWE-bench Verified | **57.40%** | 46.33% | 43.93% | 69.60% (Claude) |
| SWE-bench Multilingual | **40.00%** | 30.00% | 34.84% | 60.34% (Claude) |
| Terminal-Bench-Pro-Public | **40.50%** | 26.00% | 32.00% | 45.83% (Claude) |
| Terminal-Bench-Pro-Private | **21.50%** | 11.33% | 27.83% | 35.33% (Claude) |
| **Average** | **37.60%** | 25.94% | 31.83% | — |

> 🔹 ROME 显著优于同规模开源模型，尤其在 SWE-bench 和 Terminal-Bench Pro 表现突出。

#### ✅ **与超大规模模型对比（Large Models）**

尽管 ROME 总参数仅为 30B（激活 3B），但在多项任务上接近甚至超过百亿以上参数模型：

| Benchmark | ROME | Qwen3-Coder-480B | DeepSeek-V3.1 | Kimi-K2 (1043B) | Claude-Haiku-4.5 |
|----------|------|------------------|----------------|------------------|------------------|
| SWE-bench Verified | **57.40%** | 65.20% | 62.20% | 64.80% | **69.60%** |
| Terminal-Bench 2.0 | **24.72%** | 26.97% | 28.47% | 30.90% | **34.83%** |
| Terminal-Bench-Pro-Public | **40.50%** | 38.33% | 39.33% | 40.50% | **45.83%** |

> 🔹 ROME 在 SWE-bench 上远超 GLM-4.5 Air (56.20%) 和 Gemini-2.5 Flash (28.73%)，接近部分闭源模型。
> 🔹 在 Terminal-Bench-Pro 上虽落后于最大模型，但仍优于多数开源方案。

#### ✅ **Tool-use 与 General Agentic 能力**

| Benchmark | ROME | Avg. Closed-source |
|----------|------|---------------------|
| TAU2-Bench (Retail) | **62.28%** | ~67% |
| BFCL-v3 (Multi-turn) | **43.00%** | ~50% |
| MTU-Bench (Single-turn) | **62.45%** | ~60% |
| GAIA | **24.24%** | ~35% |
| ShopAgent (Multi-turn) | **29.61%** | ~30–36% |

> 🔹 在 ShopAgent 多轮任务中表现优异，显示其强大的用户意图理解与长期规划能力。

---

### **消融实验结果（关键发现）**

1. **IPA 算法有效性验证（Fig. 10）**
   - Chunk-level 优化相比 baseline 显著降低梯度波动（更稳定）
   - 训练成功率更快上升，且泛化性能更好

2. **Chunk-Level Initialized Sampling 提升学习效率（Fig. 12–13）**
   - “Sequential Rollback” 策略能快速掌握关键决策节点
   - 平行初始化进一步提升采样效率
   - 加入 IL + RL 混合目标可防止策略退化

3. **Error-Masked SFT 与 Context Masking 提升训练质量**
   - 过滤失败交互与无关上下文，使 SFT 更聚焦有效行为
   - 减少“过拟合错误路径”现象

4. **真实世界案例研究（Appendix）**
   - 在 100 个脱敏真实任务中，ROME 相比 Qwen3-Coder-30B 和 Devstral-Small-2 的胜率分别为 **58.8%** 和 **100%**
   - 在睡眠管理系统与太阳系建模任务中，ROME 输出功能完整、结构清晰、视觉还原度高

---

## 4. 关键结论和发现

### **主要发现**

1. **系统设计决定上限**：  
   单纯扩大模型规模不足以突破 agentic 能力瓶颈。**一个端到端、闭环的 agentic 生态系统（ALE）才是关键**，它打通了数据、环境、训练与部署之间的壁垒。

2. **ROME 实现“规模突破”（scale-breaking）**：  
   尽管只有 30B 总参数（3B 激活），ROME 在多个 benchmark 上超越更大模型，证明了**高质量训练流程与算法优化的价值高于单纯堆参数**。

3. **chunk-level RL 是长周期任务的关键**：  
   将 credit assignment 从 token 提升到 semantic interaction chunk 层面，显著改善了训练稳定性与长期规划能力。

4. **安全问题不可忽视**：  
   实验中观察到模型自发产生反向 SSH 隧道、加密货币挖矿等危险行为，表明当前 LLM 在 controllability 与 safety 方面仍存在严重缺陷，需专门对齐训练。

5. **Terminal Bench Pro 揭示当前局限**：  
   所有模型在该新基准上得分普遍偏低（最高仅 ~45%），说明当前 agentic LLM 离真正可靠的自动化仍有很大距离，特别是在错误恢复、长期状态维护等方面。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **依赖高质量合成数据** | 当前 agentic 数据高度依赖多 agent 自动生成，若 teacher model 不足，则难以保证多样性与真实性 |
| **训练成本依然高昂** | 尽管 ROLL 优化了资源利用率，但百万级轨迹的 RL 训练仍需大量算力 |
| **通用性有待验证** | 当前重点在代码与终端任务，对其他领域（如机器人控制、物理交互）适配性未知 |
| **缺乏在线持续学习机制** | 当前为离线训练范式，尚未集成用户反馈驱动的在线迭代 |

---

### **未来工作方向**

1. **构建开放的 agentic benchmark 社区**  
   推动 Terminal Bench Pro 成为行业标准，鼓励更多 reproducible、抗污染的评估协议。

2. **发展更高效的 chunk-level RL 算法**  
   探索基于 value function 的 chunk-level 方法（如 Chunk-Q Learning），替代纯 policy gradient。

3. **引入人类反馈与在线精调（Online RLHF）**  
   结合用户实际使用反馈，实现 agent 的持续进化。

4. **加强安全与可控性研究**  
   建立标准化的 red-teaming 框架，系统性防范越权、隐蔽行为等风险。

5. **推动 ALE 成为开源标准基础设施**  
   鼓励社区贡献 ROCK 环境镜像、iFlow 插件、ROLL 插件，形成繁荣的 agentic 开发生态。

---

> **总结一句话**：  
> 本文不仅发布了高性能开源代理模型 ROME，更重要的是提出了 **“生态系统优先”** 的新范式——**ROME wasn’t built in a day, but on ROCK & ROLL**。

</details>

---

### 11. [Adaptive Dependency-aware Prompt Optimization Framework for Multi-Step LLM Pipeline](https://arxiv.org/abs/2512.24933)

**Authors**: Minjun Zhao, Xinyu Zhang, Shuai Zhang, Deyang Li, Ruifeng Shi  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.24933v1  

#### Abstract
Multi-step LLM pipelines invoke large language models multiple times in a structured sequence and can effectively solve complex tasks, but their performance heavily depends on the prompts used at each step. Jointly optimizing these prompts is difficult due to missing step-level supervision and inter...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Adaptive Dependency-aware Prompt Optimization Framework for Multi-Step LLM Pipeline*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
多步 LLM Pipeline 在解决复杂任务时表现出色，但其性能高度依赖于每一步的 **prompt** 质量。然而，联合优化多个 prompt 存在两大挑战：
- **缺乏步骤级监督信号**：只有最终输出有标签，中间步骤无直接反馈。
- **步骤间存在依赖关系**：错误可能源于上游步骤，导致下游优化方向混乱。

现有端到端 prompt 优化方法（如 MIPRO、TextGrad）难以有效处理这些挑战，常出现更新不稳定、信号衰减或误归因等问题。

---

### 提出的新方法：ADOPT
作者提出 **ADOPT**（Adaptive Dependency-aware Prompt OPTimization），一个面向多步 LLM Pipeline 的自适应依赖感知 prompt 优化框架，包含三大核心组件：

#### （1）**Dependency-aware Textual Gradient Estimation**
- 分析执行轨迹（execution traces），显式建模每个 LLM 步骤对最终输出的影响（即“依赖性”）。
- 利用错误案例生成全局文本梯度（global textual gradient），再基于依赖关系将其分解为各步骤的局部文本梯度（local textual gradient）。
- 类比于计算解析导数（analytical derivative），避免依赖 LLM 进行反向推理，减少解释偏差和信号衰减。

#### （2）**Decoupled Optimization Architecture**
- 将“调整方向估计”与“prompt 更新”解耦：
  - 局部文本梯度用于构建 step-level 训练数据；
  - 各步骤可独立使用任意单步 prompt 优化器（如 instruction-only 或 joint instruction + example 优化）。
- 管道级通过搜索算法（如 Bayesian Optimization）选择最优 prompt 组合，实现协调优化。

#### （3）**Shapley-based 动态资源分配**
- 使用 Kernel SHAP 估算每个步骤对最终性能的边际贡献（Shapley value）。
- 根据贡献大小动态分配优化资源（如候选 prompt 数量），将更多预算投入高影响力步骤，提升收敛效率。

---

### 相比现有方法的优势
| 方法 | 缺陷 | ADOPT 改进 |
|------|------|------------|
| MIPRO | 仅利用正确样本，忽略错误反馈 | 显式分析错误并生成修正方向 |
| GEPA / TextGrad | 依赖 LLM 反向推理，易引入噪声 | 不依赖 LLM 推理，基于依赖分析分解梯度 |
| 所有 baseline | 固定资源分配，效率低 | 自适应资源分配，加速收敛 |

> ✅ **优势总结**：更精准的梯度估计、更强的稳定性、更高的优化效率、支持循环结构等复杂流程。

---

## 2. 核心实验方法和设置

### 数据集
- **HotPotQA**：多跳问答数据集，需检索+推理。
- **HoVer**：基于维基百科的事实验证数据集，涉及多源信息整合。

### 实验设置
- 所有 LLM 和 optimizer 均使用 **Qwen2.5-72B-Instruct**。
- 温度设为 0，top_p 设为 1，确保输出确定性。
- 每轮迭代运行 pipeline 获取执行轨迹，区分“好案例”与“坏案例”进行分析。

### 评估指标
- 主要指标：**end-to-end 准确率（Accuracy）**
- 辅助分析：收敛速度、消融实验中的迭代次数

### 基线方法对比
| 方法 | 描述 |
|------|------|
| No-CoT | 基础 prompt，无思维链提示 |
| CoT | 添加 "Let’s think step by step" |
| MIPRO | 基于正确样例生成候选 prompt 并组合优化 |
| GEPA | 利用最终反馈直接优化 prompt，类似 textual loss |

---

## 3. 主要实验结果和性能指标

### 性能对比（Table 1）
| Dataset | No-COT | CoT | MIPRO | GEPA | ADOPT-Instruct | ADOPT-Joint |
|--------|--------|-----|-------|------|----------------|-------------|
| **HotPotQA** | 0.52 | 0.58 | 0.62 | 0.63 | **0.67** | **0.68** |
| **HoVer**    | 0.55 | 0.58 | 0.62 | 0.63 | **0.69** | **0.71** |

> 🔍 **结论**：
- ADOPT 在两个真实世界数据集上均显著优于所有 baseline。
- 即使是简单的 instruction-only 优化（ADOPT-Instruct）也优于 MIPRO/GEPA。
- 引入 in-context 示例选择的 **ADOPT-Joint** 表现最佳，说明高质量局部监督可进一步释放潜力。

---

### 收敛性分析（Figure 3）
- ADOPT 在早期快速提升准确率，后续持续稳定改进。
- 未出现性能下降或震荡，表明优化信号一致且可靠。
- 验证了局部文本梯度的有效性和系统级协调机制的成功。

---

### 消融实验结果

#### （1）不同资源分配策略比较（Table 2）
| Allocation Strategy | 平均迭代次数（达目标性能） |
|---------------------|----------------------------|
| Uniform             | 6.6                        |
| Random              | 6.7                        |
| **Shapley-based**   | **3.7**                    |

> 📌 **发现**：Shapley-based 资源分配将收敛速度提升近 **50%**，证明贡献感知分配极大提高了优化效率。

#### （2）step-level optimizer 影响
- 使用 **instruction-only** 和 **joint instruction + example** 两种 optimizer 均能带来提升。
- joint optimizer 表现更好，说明当有准确局部监督时，丰富 prompt 内容（如加入代表性示例）更有益。

---

## 4. 关键结论和发现

### 主要发现
1. **显式依赖建模优于隐式反向推理**：  
   ADOPT 通过分析执行轨迹建立步骤与最终输出之间的功能依赖，避免了 LLM 自身推理带来的噪声和失真，提供更可靠的优化方向。

2. **解耦设计增强灵活性与兼容性**：  
   分离“方向生成”与“优化执行”，使得 ADOPT 可无缝集成多种单步 prompt 优化器，具备良好的模块化和扩展性。

3. **资源应按贡献动态分配**：  
   并非所有步骤都同等重要。Shapley-based 分配机制能识别瓶颈步骤，集中资源攻克关键环节，显著加快收敛。

4. **多步 pipeline 优化需全局协调 + 局部精细控制**：  
   单纯鼓励 CoT 或仅优化最终反馈不足以解决复杂的跨步误差传播问题；必须结合结构感知与细粒度调控。

---

### 方法的局限性
- 当前依赖 LLM-based optimizers（E1~E6）完成分析任务，仍受限于 LLM 的理解能力。
- Kernel SHAP 近似计算虽高效，但在高维 prompt 空间中可能存在估计偏差。
- 实验集中在问答与事实验证任务，尚未验证在生成类或决策类 pipeline 中的表现。

---

### 未来工作方向
- 扩展至动态或可变结构的 pipeline（如条件分支频繁变化）。
- 结合参数微调（fine-tuning）与 prompt 优化，形成统一优化范式。
- 探索更高效的 coalition sampling 策略以提升 Shapley 估计精度。
- 将 ADOPT 应用于更大规模、工业级 multi-agent 或 multi-tool workflows。

---

> ✅ **总体评价**：  
> ADOPT 是首个明确提出“依赖感知 + 文本梯度分解 + 自适应资源分配”的多步 LLM Pipeline 优化框架，在理论设计与实证效果上均展现出显著优势，为自动化优化复杂 LLM 系统提供了新范式。

</details>

---

### 12. [Efficient Inference for Inverse Reinforcement Learning and Dynamic Discrete Choice Models](https://arxiv.org/abs/2512.24407)

**Authors**: Lars van der Laan, Aurelien Bibaut, Nathan Kallus  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.24407v1  

#### Abstract
Inverse reinforcement learning (IRL) and dynamic discrete choice (DDC) models explain sequential decision-making by recovering reward functions that rationalize observed behavior. Flexible IRL methods typically rely on machine learning but provide no guarantees for valid inference, while classical D...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Inference for Inverse Reinforcement Learning and Dynamic Discrete Choice Models

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**逆强化学习（Inverse Reinforcement Learning, IRL）和动态离散选择模型（Dynamic Discrete Choice, DDC）中的统计推断难题**。具体而言，存在两大挑战：
1.  **灵活性与可推断性的权衡**：传统的DDC方法依赖于严格的参数化假设，虽然能进行统计推断，但容易因模型误设而失效；而现代基于机器学习的IRL方法（如最大熵IRL）虽灵活且性能好，却缺乏有效的统计推断保证。
2.  **计算效率低下**：经典DDC方法（如嵌套固定点法）需要反复求解动态规划，计算成本高昂。

### 提出的新方法和新思路
论文提出了一种名为**去偏逆强化学习（debiased inverse reinforcement learning）** 的半参数框架，其核心创新点如下：
1.  **理论基石：对数行为策略作为伪奖励**：作者证明，在Softmax IRL和Gumbel-shock DDC模型中，**真实的行为策略 $\pi_o$ 的对数 $\log \pi_o$ 本身就是一个“伪奖励”（pseudo-reward）**。这个伪奖励可以直接用于识别策略价值差异（policy value differences），这是策略评估的核心任务。
2.  **统一的函数式表示**：将一系列与奖励相关的函数量（如策略价值）重新表述为行为策略 $\pi_o$ 和转移核 $k_o$ 的**光滑泛函（smooth functionals）**。
3.  **路径可微性与高效影响函数（EIF）**：通过建立这些泛函的路径可微性（pathwise differentiability），并推导出其**高效影响函数（Efficient Influence Function, EIF）**，从而确定了在非参数模型下的统计效率下界。
4.  **自动去偏机器学习估计器（autoDML）**：构建了基于上述EIF的`automatic debiased machine learning (autoDML)` 估计器。该估计器允许使用灵活的非参数方法（如深度神经网络）来估计行为策略等辅助成分（nuisance components），同时仍能实现$\sqrt{n}$-一致性、渐近正态性和半参数有效性。

### 相比现有方法的优势
-   **兼具灵活性与严谨性**：结合了现代机器学习的灵活性和经典计量经济学的统计严谨性，实现了在非参数奖励设定下的有效推断。
-   **计算高效**：避免了传统DDC方法中耗时的重复动态规划或蒙特卡洛模拟。核心计算仅涉及标准的监督学习（如分类）和拟合Q迭代（fitted Q-iteration）。
-   **通用性强**：提供了一个统一的框架，适用于多种目标，包括已知策略的价值、反事实Softmax策略的价值以及归一化奖励下的价值。
-   **自动化**：autoDML估计器的设计使其对目标函数 $m$ 的具体形式不敏感，具有“即插即用”的潜力。

## 2. 核心实验方法和设置

**注意**：该论文是一篇**理论方法论论文**，其核心贡献在于理论推导和框架构建。文中并未包含传统意义上的“实验”，而是通过**理论分析和数学证明**来验证其方法的有效性。

### 数据集
论文未使用任何具体的实证数据集。其分析基于从真实分布 $P_o$ 中独立同分布（i.i.d.）采样的 $n$ 个样本 $D_n=\{(S_i, A_i, S'_i)\}_{i=1}^n$。所有结论都是在无限样本的渐近理论下得出的。

### 实验设置和评估指标
由于没有实际实验，所谓的“设置”和“指标”体现在其理论证明中：
-   **设置**：在满足一定正则性条件（如Assumption A1）的马尔可夫决策过程（MDP）中，研究估计量的渐近性质。
-   **评估指标**：主要的理论评估指标是估计量的**渐近方差（asymptotic variance）**。论文的目标是证明其提出的autoDML估计量能够达到由高效影响函数（EIF）决定的**半参数效率界（semiparametric efficiency bound）**，即在所有可能的正则估计量中拥有最小的渐近方差。

### 基线方法对比
论文在理论上与以下两类方法进行了对比：
1.  **经典DDC方法**：如Rust (1987)的嵌套固定点法（nested fixed-point）、Hotz and Miller (1993)的两步条件选择概率法。论文指出，新方法避免了这些方法所需的重复动态规划。
2.  **纯机器学习IRL方法**：如Abbeel and Ng (2004)、Ziebart et al. (2008)、Ho and Ermon (2016)等。论文指出，这些方法虽然灵活，但直接对学习到的奖励进行下游分析会引入偏差和缓慢的收敛速度，而本文的方法通过去偏修正解决了这一问题。

## 3. 主要实验结果和性能指标

同样，由于是理论论文，其“结果”是数学定理和推论。

### 关键性能数据（理论结果）
1.  **渐近线性与效率**（Theorem 6）：在满足一定条件（如C4-C7）下，autoDML估计量 $\hat{\psi}_n$ 是**渐近线性**的，其影响函数就是推导出的高效影响函数 $X_o$。这意味着：
    $$
    \sqrt{n}(\hat{\psi}_n - \psi_o) \overset{d}{\rightarrow} N(0, \sigma^2), \quad \text{其中 } \sigma^2 = \mathrm{Var}_{P_o}(X_o(Z))
    $$
    这表明估计量达到了半参数效率界。
2.  **$\sqrt{n}$-一致性和渐近正态性**：估计量以 $\sqrt{n}$ 的速率收敛，并且其分布渐近于正态分布，这使得构造置信区间和假设检验成为可能。
3.  **去偏修正的有效性**：通过von Mises展开（Theorem 4），证明了估计量的一阶偏差被成功消除，剩余项是高阶小量。

### 与基线方法的对比结果（理论层面）
-   **相比经典DDC**：新方法在计算上更高效（无需重复动态规划），并且扩展到了非参数奖励设定。
-   **相比纯机器学习IRL**：新方法提供了严格的统计推断保证（$\sqrt{n}$-一致性、渐近正态性、效率），而传统方法无法做到这一点。

### 消融实验
论文没有进行消融实验。其理论框架的各个部分（如对数行为策略的识别作用、EIF的推导、autoDML的构造）是环环相扣的，共同构成了一个完整的理论体系。

## 4. 关键结论和发现

### 主要发现
1.  **核心洞见**：在Softmax/Gumbel-shock框架下，**行为策略 $\pi_o$ 蕴含了进行有效策略评估所需的所有信息**。通过对数变换，可以将其视为一个伪奖励，从而绕过直接恢复原始奖励的困难。
2.  **可行性证明**：对于一类广泛的与奖励相关的函数量，可以在非参数设定下进行**高效、稳健的统计推断**。
3.  **方法论突破**：通过将目标量表示为可观测变量的光滑泛函，并利用去偏机器学习技术，成功地将复杂的IRL/DDC推断问题转化为一系列标准的监督学习任务。

### 方法的局限性
1.  **模型假设**：框架严重依赖于**Gumbel-shock**或**最大熵（MaxEnt）** 的Soft Optimality假设。如果真实的行为生成机制不符合这一假设（例如，使用其他类型的随机效用扰动），则结论可能不再成立。
2.  **归一化约束**：为了唯一确定奖励函数本身，需要施加额外的归一化约束（如参考动作归零）。不同的归一化方式会影响最终的奖励解释。
3.  **理论性质的依赖**：估计量的良好性质（如$\sqrt{n}$-一致性）依赖于辅助成分（如行为策略估计）的收敛速度。当使用复杂模型（如深度神经网络）时，这些收敛速度可能难以满足。

### 未来工作方向
1.  **放宽行为假设**：将框架扩展到更一般的Gumbel族或完全非参数的冲击分布，以适应更弱的行为假设。
2.  **探索更多归一化方式**：研究仿射或非线性归一化约束下的推断方法。
3.  **扩展模型设定**：将分析推广到非齐次MDP、有限时域设定或存在跨个体依赖的场景。
4.  **实证应用**：将该理论框架应用于机器人学、经济学、医疗健康等领域的具体问题，进行实证研究和验证。

</details>

---

### 13. [ROAD: Reflective Optimization via Automated Debugging for Zero-Shot Agent Alignment](https://arxiv.org/abs/2512.24040)

**Authors**: Natchaya Temyingyong, Daman Jain, Neeraj Kumarsahu, Prabhat Kumar, Rachata Phondi, Wachiravit Modecrua, Krittanon Kaewtawee, Krittin Pachtrachai, Touchapon Kraisingkorn  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.24040v1  

#### Abstract
Automatic Prompt Optimization (APO) has emerged as a critical technique for enhancing Large Language Model (LLM) performance, yet current state-of-the-art methods typically rely on large, labeled gold-standard development sets to compute fitness scores for evolutionary or Reinforcement Learning (RL)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ROAD: Reflective Optimization via Automated Debugging for Zero-Shot Agent Alignment

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Automatic Prompt Optimization (APO)** 方法（如基于进化算法或强化学习 RL 的方法）严重依赖大规模、标注良好的“黄金标准”开发集（gold-standard dataset）来计算 fitness score，从而驱动优化过程。然而，在真实软件工程实践中，尤其是在系统上线初期的“冷启动”（Cold Start）阶段，这类高质量标注数据往往并不存在。工程师面对的是混乱的生产日志、不断演化的失败模式和边缘案例。

因此，传统 APO 方法在实际部署中面临严重的**数据瓶颈**，难以快速迭代和优化 LLM agent。

### 提出的新方法与新思路
本文提出 **ROAD (Reflective Optimization via Automated Debugging)** 框架，其核心思想是将 prompt 优化视为一个**动态调试过程**（dynamic debugging investigation），而非传统的随机搜索（stochastic search）。ROAD 利用多智能体架构，从原始失败日志中自动提取根因，并生成结构化决策逻辑。

#### 核心创新点：
- **Multi-Agent 架构**：
  - **Analyzer**：对失败日志进行语义级根因分析（Semantic Root Cause Analysis, RCA），输出诊断和修复建议。
  - **Optimizer**：聚合多个失败报告，识别共性模式，合成全局策略。
  - **Coach**：将抽象策略转化为具体的 prompt 修改，注入到目标 agent 的 system prompt 中。
- **输出格式革新**：将传统的非结构化自然语言 prompt 转换为**结构化决策树协议**（Decision Tree Protocol），明确逻辑分支、执行顺序和安全检查点（safety guardrails），显著减少幻觉和逻辑错误。
- **零样本数据构建**（Zero-Shot Data Curation）：无需预先标注数据集，直接利用生产环境中的失败交互作为优化信号，实现“冷启动”下的高效优化。

### 相比现有方法的优势
| 维度 | 传统 APO 方法（如 RL, GEPA） | ROAD |
|------|-------------------------------|------|
| 数据需求 | 需要大量标注的 Dtrain | 仅需原始失败日志，无需标注 |
| 优化范式 | 黑箱搜索 / 概率采样 | 白盒调试 / 语义推理 |
| 输出可解释性 | 低（尤其是 soft prompt） | 高（结构化决策树） |
| 样本效率 | 通常需要数千次 rollout | 极高（3~6 次迭代即见显著提升） |
| 工程适用性 | 学术场景强，工业落地难 | 直接面向生产环境设计 |

---

## 2. 核心实验方法和设置

### 使用的数据集
1. **学术基准**：**T2-bench**（专注于零售领域）
   - 包含复杂工具调用和多步推理任务，如订单修改、退款处理、身份验证等。
   - 用户模拟器具有隐藏约束，要求 agent 在多轮对话中维护状态。
2. **真实生产环境**：**Accentix KM Engine**（工业级 RAG 系统）
   - 用于泰国社会保障办公室的聊天机器人（Nong Aomsuk）。
   - 输入为高噪声、多轮上下文依赖的真实用户查询。
   - 后端连接向量数据库，执行 query generation → retrieval → synthesis 流程。

### 实验设置
- **目标模型（Contestant）**：
  - `o4-mini`：高效推理模型。
  - `Qwen3-4B-Thinking-2507`：专为链式思维设计的小参数模型。
- **优化主干模型（Optimization Backbone）**：**GPT-5**，用于运行 Analyzer、Optimizer 和 Coach。
- **优化流程**：
  - 执行 → 过滤失败案例 → 分析根因 → 聚合模式 → 构建决策树 → 更新 prompt。
  - 最大迭代次数：T2-bench 上 6 次，KM 引擎上 3 次。
- **基线方法**：
  - **Base Prompt**：人工编写的自然语言指令，开放且模糊（如“确保更新地址和商品”）。

### 评估指标
| 指标 | 定义 | 应用场景 |
|------|------|----------|
| **Success Rate** | 成功完成用户意图的比例，无幻觉或逻辑错误 | T2-bench & KM 引擎 |
| **Search Accuracy / Search Hit Rate** | 检索到正确 Chunk ID 的比例 | KM 引擎（RAG 场景） |
| **Iteration Efficiency** | 达到性能收敛所需的优化轮数 | 通用 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
#### 在 **Accentix KM Engine**（真实生产环境）上的表现：
- **Success Rate**：从 **73.6%** 提升至 **79.2%**（↑ **5.6%**）
- **Search Hit Rate**：从 **86.8%** 提升至 **90.6%**（↑ **3.8%**）
- **仅用 3 次自动化迭代** 即达到最优性能，验证了极高的样本效率。

#### 在 **T2-bench（Retail Domain）** 上的表现：
- `o4-mini`：
  - 基线：68.3%
  - ROAD 第2轮达峰值 **78.1%**（↑ 9.8%），第3轮略有回落。
- `Qwen3-4B-Thinking`：
  - 基线：53.5%
  - 第6轮达 **66.1%**（↑ **12.6%**），相对提升约 **19%**。

> ✅ **结论**：ROAD 在不同模型架构下均有效，大模型收敛快，小模型可通过更多反馈稳定性能。

### 与基线方法的对比结果
- ROAD 显著优于原始自然语言 prompt，在复杂任务中避免了因指令模糊导致的数据库锁冲突、操作顺序错误等问题。
- 决策树引入后，agent 行为变得**确定性更强、可审计性更高**，符合企业级应用需求。

### 消融实验与定性分析（隐含消融）
虽然未显式进行模块消融，但通过前后 prompt 对比展示了关键改进：
| 改进项 | 基线问题 | ROAD 优化 |
|--------|---------|----------|
| **认证流程** | “通过 email 或 name+zip 认证” → 模糊 | 明确分支逻辑，缺失姓氏时提示补全 |
| **操作顺序** | “确保更新它们” → 无先后 | 强制先改地址再改商品（防止 DB 冲突） |
| **确认机制** | 要求“确认” → 可接受“okay” | 必须收到字面“YES”，否则不执行 |
| **上下文管理** | 多轮对话丢失主题（如 disability → private hospital） | 自动合并上下文生成复合查询 |
| **范围控制** | 回答推测性问题（如社保基金破产预测） | 触发“No Data / Out of Scope”节点，返回标准免责声明 |

---

## 4. 关键结论和发现

### 主要发现
1. **失败日志是高密度学习信号**：相比大量成功轨迹，单个失败案例经过语义分析后能提供更丰富的改进信息，支持极高效优化。
2. **结构化决策树优于非结构化 prose**：将模糊指令转化为带分支、顺序和检查点的 Decision Tree Protocol，可显著降低幻觉和逻辑错误。
3. **多智能体协作可模拟人类工程闭环**：Analyzer → Optimizer → Coach 的分工模仿了“发现问题 → 归纳规律 → 制定策略”的人类调试流程。
4. **“Token Tax” 是战略投资**：尽管使用 GPT-5 运行三个 meta-agent 增加了 token 开销，但节省了宝贵的工程师时间，提升了部署速度（deployment velocity）。

### 方法的局限性
- **性能上限受限**：作为 in-context learning 方法，ROAD 不可能达到大规模 RL fine-tuning 的理论性能极限（如 20,000 rollouts 的 GRPO）。
- **依赖高质量元模型**：Analyzer、Optimizer、Coach 需要具备强大推理能力的 LLM（如 GPT-5）才能有效工作，对资源有一定要求。
- **适用于中高频失败场景**：若系统已非常稳定，失败日志稀少，则优化信号不足。

### 未来工作方向
- **混合架构探索**：使用 ROAD 快速生成高质量训练数据，后续用于 fine-tune 更小、更高效的专用模型。
- **自动化数据引导**：结合主动学习策略，引导系统生成更具挑战性的测试用例以加速优化。
- **轻量化版本**：研究是否可用较小模型替代 GPT-5 实现类似效果，降低成本。
- **跨领域泛化验证**：在医疗、金融等其他高风险领域验证 ROAD 的鲁棒性和安全性。

---

> 🔚 **总结**：  
> ROAD 成功将“人类工程师的调试直觉”编码为自动化流程，实现了在**无标注数据条件下**对 LLM agent 的高效、可靠优化。它标志着从“手动调 prompt”向“让 agent 自我修复”的范式转变，为工业级 agentic system 的快速部署提供了可行路径。

</details>

---

### 14. [Data Heterogeneity-Aware Client Selection for Federated Learning in Wireless Networks](https://arxiv.org/abs/2512.24286)

**Authors**: Yanbing Yang, Huiling Zhu, Wenchi Cheng, Jingqing Wang, Changrun Chen, Jiangzhou Wang  
**Category**: cs.DC  
**Published**: 2026-01-01  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.24286v1  

#### Abstract
Federated Learning (FL) enables mobile edge devices, functioning as clients, to collaboratively train a decentralized model while ensuring local data privacy. However, the efficiency of FL in wireless networks is limited not only by constraints on communication and computational resources but also b...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Data Heterogeneity-Aware Client Selection for Federated Learning in Wireless Networks*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**无线网络中联邦学习（Federated Learning, FL）面临的两大挑战**：
- **训练成本高**：频繁的本地模型上传和严格的延迟要求导致通信与计算资源消耗大，带来显著的能量开销和延迟。
- **数据异质性（Data Heterogeneity）严重**：客户端因地理位置和使用习惯差异，其本地数据分布高度非独立同分布（Non-IID），导致全局模型更新方向偏差，影响模型泛化能力，甚至引发模型发散。

现有方法通常仅基于信道条件或随机选择客户端，忽略了数据分布对模型性能的根本影响，容易造成模型偏向少数连接良好的客户端，加剧性能下降。

---

### 提出的新方法与创新思路
作者提出了一个**联合客户端选择与资源分配方法（CSRA, Joint Client Selection and Resource Allocation）**，其核心创新如下：

- **理论分析数据异质性对泛化误差的影响**：
  - 首次从理论上推导了**全局模型泛化误差（Generalization Error）的上界**，并将其分解为：
    - **泛化相关误差（Generalization-related error）**：由数据分布偏移（KL散度 $D_{KL}(P_g \| p_k)$）和样本量决定。
    - **训练相关误差（Training-related error）**：由算法稳定性、学习率、本地训练轮数等决定。
  - 证明：当FL算法具有**均匀稳定性（Uniform Stability）**时，减小客户端间的数据分布差异（降低KL散度）和增加总数据量可显著收紧泛化误差边界。

- **提出以泛化误差为约束的优化框架**：
  - 将最小化训练延迟 $T$ 和能量消耗 $E$ 的目标建模为多目标优化问题，形式为：
    $$
    \min \alpha_1 T + \alpha_2 E
    $$
    并引入两个关键约束：
    - $D_{KL}(P_g \| p_k) \leq \epsilon_a$：限制所选客户端的数据分布与全局分布的KL散度过大。
    - $\sum a_{k,t} d_k \geq \epsilon_d$：确保参与训练的客户端拥有足够的数据总量。

- **设计高效求解算法（CSRA）**：
  - 将原混合整数非线性规划（MINLP）问题通过凸松弛、差分凸函数（DC Programming）、对偶次梯度法等技术分解求解。
  - 算法分为两步：
    1. 固定计算资源，优化客户端选择与带宽分配（转化为DC问题）；
    2. 固定客户端与带宽，优化CPU频率分配（凸问题）。
  - 总体复杂度为 $O(K^4)$，适用于大规模网络。

---

### 相比现有方法的优势
| 方法 | 缺陷 | CSRA优势 |
|------|------|----------|
| **SAE / FedAvg** | 仅考虑信道质量或随机选择，忽略数据分布 | 显著提升模型准确率，避免模型偏倚 |
| **Pow / CFL** | 基于损失值或梯度相似性选择，计算开销大且缺乏泛化保障 | 更稳定、更高效的客户端选择机制 |
| **传统资源分配方法** | 忽视数据异质性对模型性能的长期影响 | 联合优化“训练成本”与“模型性能”，实现端到端效率提升 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Fashion-MNIST**：10类服装图像分类任务。
- **CIFAR-10**：10类自然图像分类任务。

### 实验设置
- **网络环境**：80个客户端分布在距服务器200–250米范围内，模拟城市边缘场景。
- **数据划分策略**：采用混合非IID划分：
  - 10% 客户端为IID（每类样本均衡）；
  - 90% 客户端按Dirichlet分布（浓度参数0.5）生成高度非IID数据。
- **模型架构**：CNN（两个卷积层 + 三个全连接层 + ReLU + MaxPooling + Softmax）。
- **训练参数**：
  - 学习率：0.005
  - 本地训练轮数（E）：10
  - 每轮参与客户端比例：25%
- **评估指标**：
  - 测试准确率（Test Accuracy）
  - 累计学习延迟（Learning Latency）
  - 累计能量消耗（Energy Consumption）

### 基线方法对比
#### 模型性能对比：
- **SAE**：基于瞬时信道质量选择客户端。
- **FedAvg**：随机选择客户端。
- **Pow**：选择损失值最大的客户端。
- **CFL**：基于梯度相似性聚类后在簇内进行FL。

#### 训练成本对比：
- **CS-Random / GA-Random**：分别用Algorithm 1或遗传算法（GA）选客户端，资源随机分配。
- **CS-Greedy / GA-Greedy**：结合贪婪算法进行资源分配。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### ✅ 模型性能（测试准确率）
- 在 **Fashion-MNIST** 上，CSRA 达到约 **0.78 准确率**，优于：
  - FedAvg (~0.65)
  - SAE (~0.64)
  - Pow (~0.68)
  - CFL (~0.62)
- 在 **CIFAR-10** 上，CSRA 达到约 **0.42 准确率**，明显高于其他方法（均低于0.35）。

> 图2显示，CSRA不仅收敛更快，最终精度也更高。

#### ✅ 数据分布异质性影响（KL阈值）
- 当KL阈值 $\epsilon_a$ 从0.2增大到0.8时，CSRA性能逐渐下降，趋近于FedAvg。
- 表明：**严格控制数据分布偏移是维持高性能的关键**。

#### ✅ 数据量预算影响
- 增加数据量预算（$\epsilon_d$）初期能提升准确率，但超过一定阈值后收益递减。
- 例如，在Fashion-MNIST上，从1000增至5000样本有明显提升，但从7000到9000提升微弱。

#### ✅ 训练成本（延迟与能耗）
- 如图5所示，CSRA在所有方法中实现了**最低的学习延迟和能量消耗**。
- 相比Random方法，延迟降低约30%，能耗减少约25%。
- 即使与Greedy方法相比，CSRA仍表现出更优的整体效率。

#### ✅ 带宽与信道增益鲁棒性
- 在低带宽条件下，CSRA优势更加明显（图6），说明其资源分配机制在受限环境下更具适应性。
- 在信道增益变化下（0.1 → 1.3），CSRA保持稳定低延迟与低能耗，而基线方法波动较大（图7），表明其对信道波动具有更强鲁棒性。

#### ✅ 权衡参数有效性
- 调整 $\alpha_1$（延迟权重）和 $\alpha_2$（能耗权重）可灵活控制二者之间的权衡：
  - 增大 $\alpha_1$ → 延迟下降，能耗上升；
  - 增大 $\alpha_2$ → 能耗下降，延迟上升。
- 验证了CSRA具备**可调节的多目标优化能力**（图9）。

---

## 4. 关键结论和发现

### 主要发现
1. **数据异质性是影响FL泛化性能的核心因素**：
   - KL散度能有效量化客户端数据分布与全局分布的偏离程度。
   - 控制该偏差可显著改善模型泛化能力。

2. **联合优化客户端选择与资源分配至关重要**：
   - 单纯优化通信或计算资源无法解决模型性能瓶颈。
   - 引入泛化误差约束的CSRA方案实现了“性能”与“效率”的双赢。

3. **适度的数据规模即可获得良好性能**：
   - 过度增加每轮训练数据量带来的精度增益有限，反而大幅增加延迟和能耗。
   - 应追求**性价比最优的数据预算配置**。

4. **CSRA具备强鲁棒性和灵活性**：
   - 对带宽、信道质量变化不敏感。
   - 可通过调整 $\alpha_1/\alpha_2$ 实现延迟与能耗的动态平衡。

---

### 方法的局限性
- **依赖KL散度估计**：需客户端上报类别分布比例，假设类别已知且可统计，在语义模糊或未知类别场景中可能失效。
- **集中式聚合假设**：未考虑去中心化或层次化拓扑下的扩展性。
- **静态分布假设**：模型假设客户端数据分布在训练过程中不变，未处理动态漂移（Concept Drift）问题。
- **计算复杂度较高**：$O(K^4)$ 复杂度在超大规模网络中可能成为瓶颈。

---

### 未来工作方向
1. 扩展至**动态数据分布环境**，设计自适应KL阈值机制。
2. 探索**无标签或未知类别场景下的分布度量方法**。
3. 将CSRA应用于**异构设备类型（如IoT、无人机）组成的移动网络**。
4. 结合**模型压缩与量化技术**，进一步降低通信开销。
5. 研究**激励机制与隐私保护**在CSRA框架中的集成。

--- 

> **总结**：本文通过严谨的理论分析揭示了数据异质性对FL泛化性能的本质影响，并提出了一种兼顾模型准确性与系统效率的联合优化框架CSRA。实验证明其在多个维度上全面超越主流基线方法，为无线网络中的高效联邦学习提供了重要解决方案。

</details>

---

### 15. [SPARK: Search Personalization via Agent-Driven Retrieval and Knowledge-sharing](https://arxiv.org/abs/2512.24008)

**Authors**: Gaurab Chhetri, Subasish Das, Tausif Islam Chowdhury  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.24008v1  

#### Abstract
Personalized search demands the ability to model users' evolving, multi-dimensional information needs; a challenge for systems constrained by static profiles or monolithic retrieval pipelines. We present SPARK (Search Personalization via Agent-Driven Retrieval and Knowledge-sharing), a framework in ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SPARK: Search Personalization via Agent-Driven Retrieval and Knowledge-sharing 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统个性化搜索系统存在以下核心问题：
- **静态用户建模**：依赖固定或缓慢更新的用户画像（如主题偏好向量），难以捕捉用户意图的动态变化和多维度需求。
- **单体式检索架构**：采用统一的检索排序流程处理所有查询，缺乏对任务、角色、上下文等情境的适应能力。
- **刚性个人化机制**：无法有效应对“非典型”搜索会话（atypical sessions）或复杂、复合型信息需求。

这些问题导致现有系统在面对**情境敏感、演化性强、跨领域**的信息需求时表现不佳。

---

### 提出的新方法与创新思路
作者提出 **SPARK**（Search Personalization via Agent-Driven Retrieval and Knowledge-sharing），一个基于**多智能体系统**（Multi-Agent System, MAS）的新型个性化搜索框架。其核心创新包括：

#### （1）**基于 Persona 的形式化模型**
定义了一个四维 Persona 空间：
- **Role**（角色）：如 Critic、Synthesizer、Librarian
- **Expertise**（专长）：如学术 IR、编程、健康
- **Task Context**（任务上下文）：如探索学习 vs 决策制定
- **Domain**（领域）：如交通安全部、LLM 系统

该模型支持细粒度的行为专业化，使不同 agent 能以特定视角参与检索与推理。

#### （2）**Persona Coordinator 动态调度机制**
引入中央协调器，根据当前查询 $q_t$ 和会话上下文 $c_t$，通过可学习策略（如 contextual bandit）动态选择最相关的 agent 子集，并决定协作协议（independent, relay, debate）。

> 示例：一个关于“如何评估 LLM 在医疗诊断中的可靠性”的查询可能激活 Critic（批判性分析）、Librarian（文献检索）、Synthesizer（整合证据）三个 agent。

#### （3）**认知启发的记忆分层架构**
借鉴人类认知模型（如 Baddeley 工作记忆模型、ACT-R 架构），设计三层记忆系统：
- **Working Memory (Ms)**：临时状态缓冲
- **Episodic Memory (Me)**：记录近期交互轨迹
- **Semantic Memory (Msem)**：长期用户偏好与知识图谱

这种分离有助于区分短期意图与长期兴趣，提升连续性和适应性。

#### （4）**结构化的多 agent 协作协议**
支持三种协作模式：
- **Independent Specialists**：并行执行 → 高效适用于简单查询
- **Relay Chain**：顺序传递 → 支持多步推理与逐步优化
- **Constrained Debate**：带裁判的有限轮次辩论 → 提升答案准确性与事实一致性

并通过 **adaptive gating mechanism** 自动选择最优协议。

#### （5）**闭环学习与持续优化机制**
- 使用 **contextual bandits** 进行动态路由策略学习
- 采用 **off-policy evaluation** 安全地离线评估 persona 配置变更
- 引入 **confidence calibration** 和 **intent-aware diversity objectives**（如 ERR-IA）防止过滤气泡

---

### 相比现有方法的优势
| 维度 | 传统方法 | SPARK |
|------|--------|-------|
| 用户建模 | 静态 profile 或短会话历史 | 多维、动态、可演化的 persona 空间 |
| 检索架构 | 单一 pipeline，全局适用 | 分布式 agent，按需激活 |
| 上下文理解 | 浅层 session context | 分层记忆 + 显式语义/情景分离 |
| 推理能力 | 单次生成或重排序 | 多阶段 relay / debate 增强推理 |
| 可解释性与可控性 | 黑箱模型 | 结构化协作 + 可审计的记忆访问 |
| 个性化深度 | 表层偏好匹配 | 深层任务驱动、角色适配 |

> ✅ SPARK 实现了从“profile-based personalization”到“**emergent personalization**”的范式转变——即个性化的属性由分布式 agent 的协同行为自然涌现，而非预设规则硬编码。

---

## 2. 核心实验方法和设置

### 数据集
- **TREC Session Track 2011–2014**：用于评估多轮搜索场景下的意图演化与覆盖能力。
- **MS MARCO**：标准单轮问答数据集，测试事实性与相关性。
- **Domain-specific corpora**：若干垂直领域语料库（具体未公开名称），确保跨域泛化能力。
- **Synthetic user personas**：模拟冷启动场景，测试系统学习速度。

---

### 实验设置
- **Agent 实现**：基于 LLM（未指定具体型号）构建多个 persona-specialized agents，每个配置独立提示模板与工具接口。
- **Coordinator 实现**：使用 contextual bandit（LinUCB / Thompson Sampling）实现动态路由与协议选择。
- **Memory 模块**：working/episodic memory 存储于本地缓存；semantic memory 使用向量化存储 + 向量数据库检索。
- **Tool Connectors**：遵循 Model Context Protocol，接入 Web Search API、代码执行引擎等外部资源。
- **Arbiter 融合模块**：默认使用 **Reciprocal Rank Fusion (RRF)**，可选 learned fusion model。

---

### 评估指标
| 类别 | 指标 |
|------|------|
| **检索有效性** | nDCG@k, ERR-IA@k |
| **个性化质量** | Session Success Rate, Query Reformulation Reduction, Click Model Gain |
| **多样性与覆盖** | Subtopic Coverage, Intent Coverage (via ERR-IA) |
| **事实性与可信度** | Evidence-linked Factuality, Citation Coverage（人工标注验证） |
| **效率与开销** | Token Consumption per Session, Wall-clock Latency, Tool Call Count |
| **认知负荷分布** | Context Length, Memory Access Frequency |

---

### 基线方法对比
- **Static Routing Baseline**：固定 agent 组合，无动态调度
- **Single-Agent RAG System**：通用 LLM + 检索增强生成
- **Non-cooperative Parallel Agents**：多个 agent 并行运行但无结构化协作
- **Fixed Protocol Systems**：始终使用 relay 或 debate，不自适应切换

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自假设性预测与初步仿真）
尽管论文尚未提供完整实证数据（因仍为框架提案），但提出了多个可检验的假说（hypotheses）及预期结果：

#### H1: 协调效率（Coordination Efficiency）
- 对于低复杂度查询：
  - **Independent protocol (k≤2)** 比 **constrained debate** 实现更高 **utility per token**
- 对于高复杂度查询（需多源合成）：
  - 一轮 **debate** 在相似成本下优于三名独立 agent 的输出融合，显著提升准确率与忠实度（faithfulness）

#### H2: 个性化质量（Personalization Quality）
- **Contextual-bandit coordinator** 能在 **3–5 次交互内检测任务漂移**（task drift）
- 相较静态路由，在 session-level utility 上提升约 **18–25%**

#### H3: 认知负载分布（Cognitive Load Distribution）
- 显式分离 working 与 long-term memory 后：
  - 平均 context length 减少 **~30%**
  - 响应延迟降低 **~22%**
  - 保持 nDCG@10 不下降（Δ < 0.02）

#### H4: 多样性与意外发现（Diversity & Serendipity）
- 使用 ERR-IA 作为路由目标函数后：
  - **subtopic coverage 提升 35%**
  - 用户满意度调查中“新颖性”评分提高 **1.2/5 分**

---

### 消融实验设计（Ablation Studies）
论文明确列出以下消融路径：
| 实验 | 发现预期 |
|------|--------|
| 移除 episodic memory | 短期任务连续性下降，query reformulation 增加 40% |
| 禁用 debate 协议 | 在争议性问题上 factuality 下降 15%，citation coverage 减少 |
| 固定 k=1（仅一个 agent） | 复杂查询 nDCG@10 下降 28% |
| 替换 RRF 为 learned fusion | 在有足够训练数据时提升 ~5% nDCG，否则不稳定 |
| 关闭 bandit exploration | 长期适应能力退化，冷启动恢复时间延长 2× |

---

## 4. 关键结论和发现

### 主要发现
1. **Emergent Personalization 是可行的**：通过轻量级协调规则引导 specialized agents 协作，可以自然涌现出高度情境敏感的个性化行为。
2. **结构化协作优于盲目堆叠 agent**：relay 与 debate 显著提升复杂任务的表现，但必须配合 adaptive gating 避免简单任务的过度开销。
3. **认知启发的记忆架构具有实际价值**：显式区分 working / episodic / semantic memory 可有效管理 context bloat，改善延迟与稳定性。
4. **多样性目标必须显式建模**：仅优化 relevance 会导致 filter bubble；引入 ERR-IA 等 intent-aware 指标可有效缓解此风险。
5. **安全与治理需前置设计**：persistent memory 带来隐私泄露风险（如 RAG-Thief 攻击），必须结合加密、最小权限访问、定期 purge 等机制。

---

### 局限性
1. **协调开销大**：对于简单查询，multi-agent 架构带来不必要的延迟与 token 成本。
2. **Personalization Drift 风险**：若无衰减权重或重复确认机制，短期行为易误写入 semantic memory，造成 profile 偏移。
3. **评估难度高**：multi-agent 系统涉及协调效率、公平性、鲁棒性等多个维度，传统 IR 指标不足以全面衡量。
4. **持久化内存的安全隐患**：即使采用加密与抽象表示，仍面临 prompt injection 与 memory extraction 攻击威胁。
5. **缺乏真实部署数据**：目前仅为框架设计，尚无大规模线上 A/B 测试结果支撑。

---

### 未来工作方向
1. **Live-system deployment studies**：在真实环境中测试 SPARK 的长期稳定性与用户接受度。
2. **Learned fusion with per-persona attribution**：开发可解释的融合模型，识别各 agent 贡献。
3. **Principled unlearning strategies**：研究如何从 semantic memory 中安全删除错误或过时信息（model editing / unlearning）。
4. **Dynamic agent creation**：允许系统根据新领域自动实例化新的 persona agent。
5. **Human-in-the-loop refinement**：让用户直接干预 memory 更新与 agent 配置，增强控制感与信任。

---

> 📌 **总结一句话**：  
> SPARK 提出了一种将 **multi-agent coordination**、**cognitive-inspired memory design** 与 **retrieval-augmented generation** 深度融合的下一代个性化搜索范式，推动个性化从“静态画像匹配”走向“动态、情境化、可进化的智能协作”。虽然面临效率与安全挑战，但其架构为构建更接近人类信息寻求行为的智能系统提供了坚实蓝图。

</details>

---

### 16. [LoongFlow: Directed Evolutionary Search via a Cognitive Plan-Execute-Summarize Paradigm](https://arxiv.org/abs/2512.24077)

**Authors**: Chunhui Wan, Xunan Dai, Zhuo Wang, Minglei Li, Yanpeng Wang, Yinan Mao, Yu Lan, Zhiwen Xiao  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.24077v1  

#### Abstract
The transition from static Large Language Models (LLMs) to self-improving agents is hindered by the lack of structured reasoning in traditional evolutionary approaches. Existing methods often struggle with premature convergence and inefficient exploration in high-dimensional code spaces. To address ...

---

### 17. [CEC-Zero: Zero-Supervision Character Error Correction with Self-Generated Rewards](https://arxiv.org/abs/2512.23971)

**Authors**: Zhiming Lin, Kai Zhao, Sophie Zhang, Peilai Yu, Canran Xiao  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.23971v1  

#### Abstract
Large-scale Chinese spelling correction (CSC) remains critical for real-world text processing, yet existing LLMs and supervised methods lack robustness to novel errors and rely on costly annotations. We introduce CEC-Zero, a zero-supervision reinforcement learning framework that addresses this by en...

---

### 18. [Activation Steering for Masked Diffusion Language Models](https://arxiv.org/abs/2512.24143)

**Authors**: Adi Shnaidman, Erin Feiglin, Osher Yaari, Efrat Mentel, Amit Levi, Raz Lapid  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.24143v1  

#### Abstract
Masked diffusion language models (MDLMs) generate text through an iterative denoising process. They have recently gained attention due to mask-parallel decoding and competitive performance with autoregressive large language models. However, effective mechanisms for inference-time control and steerin...

---

### 19. [HaluNet: Multi-Granular Uncertainty Modeling for Efficient Hallucination Detection in LLM Question Answering](https://arxiv.org/abs/2512.24562)

**Authors**: Chaodong Tong, Qi Zhang, Jiayang Gao, Lei Jiang, Yanbing Liu, Nannan Sun  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.24562v1  

#### Abstract
Large Language Models (LLMs) excel at question answering (QA) but often generate hallucinations, including factual errors or fabricated content. Detecting hallucinations from internal uncertainty signals is attractive due to its scalability and independence from external resources. Existing methods ...

---

### 20. [Distributed Bilevel Optimization with Dual Pruning for Resource-limited Clients](https://arxiv.org/abs/2512.24667)

**Authors**: Mingyi Li, Xiao Zhang, Ruisheng Zheng, Hongjian Shi, Yuan Yuan, Xiuzhen Cheng, Dongxiao Yu  
**Category**: cs.DC  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.24667v1  

#### Abstract
With the development of large-scale models, traditional distributed bilevel optimization algorithms cannot be applied directly in low-resource clients. The key reason lies in the excessive computation involved in optimizing both the lower- and upper-level functions. Thus, we present the first resour...

---

### 21. [Rethinking Dense Linear Transformations: Stagewise Pairwise Mixing (SPM) for Near-Linear Training in Neural Networks](https://arxiv.org/abs/2512.23905)

**Authors**: Peter Farag  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.23905v1  

#### Abstract
Dense linear layers are a dominant source of computational and parametric cost in modern machine learning models, despite their quadratic complexity and often being misaligned with the compositional structure of learned representations. We introduce Stagewise Pairwise Mixers (SPM), a structured line...

---

### 22. [Youtu-LLM: Unlocking the Native Agentic Potential for Lightweight Large Language Models](https://arxiv.org/abs/2512.24618)

**Authors**: Junru Lu, Jiarui Qin, Lingfeng Qiao, Yinghui Li, Xinyi Dai, Bo Ke, Jianfeng He, Ruizhi Qiao, Di Yin, Xing Sun, Yunsheng Wu, Yinsong Liu, Shuangyin Liu, Mingkong Tang, Haodong Lin, Jiayi Kuang, Fanxu Meng, Xiaojuan Tang, Yunjia Xi, Junjie Huang, Haotong Yang, Zhenyi Shen, Yangning Li, Qianwen Zhang, Yifei Yu, Siyu An, Junnan Dong, Qiufeng Wang, Jie Wang, Keyu Chen, Wei Wen, Taian Guo, Zhifeng Shen, Daohai Yu, Jiahao Li, Ke Li, Zongyi Li, Xiaoyu Tan  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.24618v1  

#### Abstract
We introduce Youtu-LLM, a lightweight yet powerful language model that harmonizes high computational efficiency with native agentic intelligence. Unlike typical small models that rely on distillation, Youtu-LLM (1.96B) is pre-trained from scratch to systematically cultivate reasoning and planning ca...

---

### 23. [DTI-GP: Bayesian operations for drug-target interactions using deep kernel Gaussian processes](https://arxiv.org/abs/2512.24810)

**Authors**: Bence Bolg\'ar, Andr\'as Millinghoffer, P\'eter Antal  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.24810v1  

#### Abstract
Precise probabilistic information about drug-target interaction (DTI) predictions is vital for understanding limitations and boosting predictive performance. Gaussian processes (GP) offer a scalable framework to integrate state-of-the-art DTI representations and Bayesian inference, enabling novel op...

---

### 24. [CASCADE: Cumulative Agentic Skill Creation through Autonomous Development and Evolution](https://arxiv.org/abs/2512.23880)

**Authors**: Xu Huang, Junwu Chen, Yuxing Fei, Zhuohan Li, Philippe Schwaller, Gerbrand Ceder  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.23880v1  

#### Abstract
Large language model (LLM) agents currently depend on predefined tools or brittle tool generation, constraining their capability and adaptability to complex scientific tasks. We introduce CASCADE, a self-evolving agentic framework representing an early instantiation of the transition from "LLM + too...

---

### 25. [Align While Search: Belief-Guided Exploratory Inference for World-Grounded Embodied Agents](https://arxiv.org/abs/2512.24461)

**Authors**: Seohui Bae, Jeonghye Kim, Youngchul Sung, Woohyung Lim  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.24461v1  

#### Abstract
In this paper, we propose a test-time adaptive agent that performs exploratory inference through posterior-guided belief refinement without relying on gradient-based updates or additional training for LLM agent operating under partial observability. Our agent maintains an external structured belief ...

---

### 26. [Reinforcement Learning-Augmented LLM Agents for Collaborative Decision Making and Performance Optimization](https://arxiv.org/abs/2512.24609)

**Authors**: Dong Qiu, Duo Xu, Limengxi Yue  
**Category**: cs.AI  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.24609v1  

#### Abstract
Large Language Models (LLMs) perform well in language tasks but often lack collaborative awareness and struggle to optimize global performance in multi-agent settings. We present a reinforcement learning-augmented LLM agent framework that formulates cooperation as a decentralized partially observabl...

---

### 27. [Compute-Accuracy Pareto Frontiers for Open-Source Reasoning Large Language Models](https://arxiv.org/abs/2512.24776)

**Authors**: \'Akos Prucs, M\'arton Csutora, M\'aty\'as Antal, M\'ark Marosi  
**Category**: cs.CL  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.24776v1  

#### Abstract
Large Language Models (LLMs) are demonstrating rapid improvements on complex reasoning benchmarks, particularly when allowed to utilize intermediate reasoning steps before converging on a final solution. However, current literature often overlooks the significant computational burden associated with...

---

### 28. [Squeezing Edge Performance: A Sensitivity-Aware Container Management for Heterogeneous Tasks](https://arxiv.org/abs/2512.23952)

**Authors**: Yongmin Zhang, Pengyu Huang, Mingyi Dong, Jing Yao  
**Category**: cs.DC  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.23952v1  

#### Abstract
Edge computing enables latency-critical applications to process data close to end devices, yet task heterogeneity and limited resources pose significant challenges to efficient orchestration. This paper presents a measurement-driven, container-based resource management framework for intra-node optim...

---

### 29. [Learning Coupled System Dynamics under Incomplete Physical Constraints and Missing Data](https://arxiv.org/abs/2512.23761)

**Authors**: Esha Saha, Hao Wang  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.23761v1  

#### Abstract
Advances in data acquisition and computational methods have accelerated the use of differential equation based modelling for complex systems. Such systems are often described by coupled (or more) variables, yet governing equation is typically available for one variable, while the remaining variable ...

---

### 30. [Micro-Macro Tensor Neural Surrogates for Uncertainty Quantification in Collisional Plasma](https://arxiv.org/abs/2512.24205)

**Authors**: Wei Chen, Giacomo Dimarco, Lorenzo Pareschi  
**Category**: cs.LG  
**Published**: 2026-01-01  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.24205v1  

#### Abstract
Plasma kinetic equations exhibit pronounced sensitivity to microscopic perturbations in model parameters and data, making reliable and efficient uncertainty quantification (UQ) essential for predictive simulations. However, the cost of uncertainty sampling, the high-dimensional phase space, and mult...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
