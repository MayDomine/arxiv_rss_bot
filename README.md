# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-23 05:59:15 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ACE-Sync: An Adaptive Cloud-Edge Synchronization Framework for Communication-Efficient Large-Scale Distributed Model Training](https://arxiv.org/abs/2512.18127)

**Authors**: Yi Yang, Ziyu Lin, Liesheng Wei  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2512.18127v1  

#### Abstract
Large-scale deep learning models impose substantial communication overh ead in distributed training, particularly in bandwidth-constrained or heterogeneous clo ud-edge environments. Conventional synchronous or fixed-compression techniques o ften struggle to balance communication cost, convergence st...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ACE-Sync: An Adaptive Cloud-Edge Synchronization Framework for Communication-Efficient Large-Scale Distributed Model Training

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型深度学习模型在分布式训练中面临严重的通信开销问题，尤其是在带宽受限或设备异构的云边协同（cloud-edge）环境中。传统的同步机制（如 FullSync）或固定压缩策略（如 Top-k Sparsification）难以在**通信成本、收敛稳定性与模型精度**之间取得良好平衡。

### 提出的新方法与创新思路
本文提出了 **ACE-Sync**（Adaptive Cloud-Edge Synchronization Framework），一种面向大规模分布式模型训练的自适应云边同步框架，其核心创新包括：

- **(1) 基于注意力机制的梯度重要性预测器（Attention-based Gradient Importance Predictor）**  
  利用轻量级注意力模块动态评估各参数对收敛的重要性，优先同步高影响力梯度，减少冗余传输。

- **(2) 差异化参数压缩策略（Differentiated Parameter Compression Strategy）**  
  对高重要性参数进行全精度同步，低重要性参数则采用量化+稀疏化的混合压缩，并结合误差补偿（error feedback）保证长期收敛性。

- **(3) 分层式云边协调机制（Hierarchical Cloud-Edge Coordination Mechanism）**  
  边缘节点本地缓存更新并选择性上传，云端负责全局聚合、调度同步频率与压缩等级，实现跨设备个性化与系统可扩展性的统一。

- **(4) 背包优化驱动的重要梯度保留机制**  
  在每个设备的带宽预算下，通过 knapsack-based 优化最大化“重要梯度”的保留比例。

### 相比现有方法的优势
| 方面 | 传统方法局限 | ACE-Sync 改进 |
|------|--------------|---------------|
| 同步频率 | 固定周期同步（如 FedAvg） | 动态调整，基于发散度自适应控制 |
| 压缩策略 | 静态压缩率（如 Top-k 固定比例） | 自适应压缩-扩张机制，随网络条件变化 |
| 参数处理 | 统一处理所有参数 | 区分重要性，差异化同步与压缩 |
| 架构设计 | 中心化或去中心化单一模式 | 云边协同分层架构，兼顾效率与稳定 |

> ✅ 总体优势：**更高效通信 + 更快收敛 + 几乎无损精度**

---

## 2. 核心实验方法和设置

### 使用的数据集
- 主要任务为 **Transformer 语言建模**，训练数据来源于：
  - **OpenWebText2**
  - **C4 (Colossal Clean Crawled Corpus)**
- 数据总量约 **80–90 million 文本样本**，序列长度 512–1024 tokens。
- 补充边缘设备生成的元数据：带宽轨迹、延迟日志、计算能力等，用于模拟真实 cloud-edge 场景。

### 实验设置
- **模型架构**：350M 参数的 Transformer 模型
- **优化器**：AdamW
- **批量大小**：每边缘节点 batch size = 64
- **硬件环境**：
  - **云端**：16 台 NVIDIA A100 GPU（数据中心级）
  - **边缘端**：64 台异构设备（Jetson AGX Xavier、ARM 加速器、低功耗 CPU）
- **网络模拟**：
  - 带宽范围：5–200 Mbps
  - 延迟范围：10–300 ms
- **训练轮数**：50 epochs，重复三次以确保统计稳健性

### 评估指标
| 类别 | 指标 |
|------|------|
| **通信效率** | 总通信量（GB）、平均同步延迟 |
| **模型性能** | Top-1 Accuracy (%)、Perplexity |
| **收敛行为** | 收敛所需 epoch 数、训练损失曲线 |
| **稳定性** | 梯度发散程度、误差累积影响 |

### 基线方法对比
- **FullSync**：全精度、全参数同步（理想基准）
- **Top-k Sparsification**：仅同步 Top-k 最大梯度（典型稀疏化方法）
- **FedAvg-Periodic Sync**：联邦学习常用周期性同步策略

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| 方法 | Top-1 Accuracy (%) | Perplexity | Communication Cost (GB) | Convergence Epochs |
|------|---------------------|-----------|----------------------------|------------------------|
| FullSync | 82.4 | 18.7 | 112.5 | 41 |
| Top-k Sparsification | 80.1 | 20.3 | 68.4 | 45 |
| FedAvg-Periodic Sync | 78.9 | 21.6 | 52.1 | 47 |
| **ACE-Sync (Proposed)** | **82.1** | **18.9** | **44.7** | **39** |

### 与基线方法的对比结果
- **通信成本降低 60%**：从 112.5 GB → **44.7 GB**，显著优于其他压缩方法。
- **收敛速度最快**：仅需 **39 epochs** 即收敛，比 FullSync 快 2 轮，远超 Top-k（45）和 FedAvg（47）。
- **精度几乎无损**：Top-1 Accuracy 为 82.1%，仅比 FullSync 低 **0.3%**。
- **困惑度表现优异**：Perplexity = 18.9，接近 FullSync 的 18.7，明显优于其他压缩方法。

> 📈 图2 显示 ACE-Sync 的训练损失下降最快，在第20轮时已显著领先，验证了其优化效率。

### 消融实验与机制有效性分析（文中隐含）
虽然未明确列出消融表，但以下设计被证明至关重要：
- **注意力重要性估计**：使系统能识别关键梯度，避免盲目压缩。
- **残差误差补偿（residual-based error compensation）**：防止压缩引入的偏差积累导致发散。
- **设备聚类（device clustering）**：提升异构环境下的一致性与个性化适配能力。
- **动态压缩调度函数**：根据实时带宽自动调节压缩强度，增强鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **自适应同步优于静态压缩**：固定策略无法应对训练动态变化和网络波动，而 ACE-Sync 的动态决策机制可在不同阶段灵活调整通信行为。
2. **重要性感知是高效通信的关键**：通过 attention 机制区分参数重要性，能够在大幅削减通信的同时保持模型质量。
3. **云边协同架构提升可扩展性**：分层结构使得边缘轻量运行、云端统筹调度，适合大规模部署。
4. **通信效率与收敛速度可以兼得**：ACE-Sync 不仅减少了通信开销，还因更有效的信息传递加快了收敛。

### 方法的局限性
- 当前框架依赖一个额外的 attention 模块，带来轻微计算开销（尽管作者强调其轻量）。
- 实验集中在语言模型任务，尚未充分验证在 CV 或多模态任务中的泛化能力。
- 网络状态建模仍基于经验规则（如指数衰减函数），缺乏更智能的预测机制。

### 未来工作方向
1. 引入 **Reinforcement Learning 或 Meta-Learning** 来优化同步策略，进一步提升自适应能力。
2. 扩展至 **multi-tenant 或 cross-cloud Federated Learning** 场景，支持更复杂的边缘生态。
3. 推动 **Hardware-Software Co-design**，例如利用可编程交换机或网络感知 GPU kernel 降低延迟。
4. 应用于 **Foundation Models 和 Multi-modal Architectures**，探索极端规模下的扩展行为。

---

## ✅ 总结
ACE-Sync 提出了一种**通信高效、精度保留、可扩展性强**的大规模分布式训练解决方案。它通过**重要性感知 + 自适应压缩 + 云边协同**三大支柱，在真实异构环境中实现了 **60% 通信削减、提前 2 轮收敛、仅 0.3% 精度损失**的卓越表现，为未来云边 AI 基础设施提供了重要的技术路径。

</details>

---

### 2. [Towards Efficient Agents: A Co-Design of Inference Architecture and System](https://arxiv.org/abs/2512.18337)

**Authors**: Weizhe Lin, Hui-Ling Zhen, Shuai Yang, Xian Wang, Renxi Liu, Hanting Chen, Wangze Zhang, Chuansai Zhou, Yiming Li, Chen Chen, Xing Li, Zhiyuan Yang, Xiaosong Li, Xianzhi Yu, Zhenhua Dong, Mingxuan Yuan, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.18337v1  

#### Abstract
The rapid development of large language model (LLM)-based agents has unlocked new possibilities for autonomous multi-turn reasoning and tool-augmented decision-making. However, their real-world deployment is hindered by severe inefficiencies that arise not from isolated model inference, but from the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Towards Efficient Agents: A Co-Design of Inference Architecture and System*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Model (LLM)** 的自主代理（Autonomous Agents）在实际部署中面临严重的效率瓶颈。这些瓶颈并非来自单次模型推理延迟，而是源于**多轮推理循环中的系统级延迟累积**，主要包括：
- **推理循环（Reasoning Loops）**：持续的“Think-Act-Observe”循环导致大量同步 LLM 调用。
- **上下文爆炸（Context Explosion）**：历史记忆不断增长，引发二次计算开销。
- **异构工具交互（Heterogeneous Tool Interactions）**：外部工具调用引入高并发和资源争用。

传统优化方法（如量化、剪枝、连续批处理）专注于提升每秒生成 token 数（TPS），但在代理场景下可能导致**推理质量下降、重试增多、端到端延迟反而上升**。

### 提出的新方法与思路
本文提出 **AgentInfer**，一个统一的端到端代理加速框架，通过**推理架构与系统设计的协同优化（Co-Design）** 来解决上述问题。其核心思想是：**优化目标应从“每 token 吞吐量”转向“任务完成时间”**。

AgentInfer 包含四个协同工作的模块：

| 模块 | 功能 |
|------|------|
| **AgentCollab** | 分层双模型协作框架：大模型负责战略规划与救援，小模型执行常规推理；通过 `Progress Check` 自我评估机制动态升降级模型使用，实现认知资源的高效分配。 |
| **AgentCompress** | 异步语义压缩机制：对代理记忆进行轻量级蒸馏与重组，在不中断推理的前提下减少上下文长度，缓解上下文爆炸。 |
| **AgentSched** | 缓存感知混合调度器：结合 Shortest-Job-First 与 KVCache 保留策略，利用影子价格（shadow-price）控制器动态切换模式，平衡短请求响应速度与长会话缓存命中率。 |
| **AgentSAM** | 基于后缀自动机（Suffix Automaton）的推测解码方法：复用跨会话的语义记忆，提升推测 token 的接受率，降低生成延迟。 |

### 相比现有方法的优势
- **系统级视角**：不同于孤立优化某一层（如仅模型或仅调度），AgentInfer 实现了**推理逻辑与底层系统的深度协同**。
- **任务导向优化**：以“任务完成时间”为核心指标，避免“更快但更错”的陷阱。
- **自适应与闭环控制**：各模块具备反馈机制（如 Progress Check、影子价格），形成自我演化的推理引擎（Self-Evolution Engine）。
- **综合增益显著**：各模块互补，叠加效果远超单一技术。

---

## 2. 核心实验方法和设置

### 数据集
- **BrowseComp-zh**：中文版网页浏览能力基准测试，包含需多步 web search 的复杂任务。
- **DeepDiver**：用于验证整体系统性能的深研代理基准。

### 实验设置
- **模型配置**：
  - 大模型（ML）：`openPangu-DeepDiverV2-38B`
  - 小模型（Ms）：`openPangu-DeepDiverV2-7B`
  - 压缩模型：`openPangu-7B`
- **硬件平台**：Ascend 910B3 NPUs，使用 `vLLM-Ascend v0.9.1` 推理引擎。
- **并行配置**：测试不同 Tensor Parallelism（TP）和并发会话数（Nparallel）下的性能。

### 评估指标
| 指标 | 说明 |
|------|------|
| **End-to-End Latency** | 完成整个任务的墙钟时间（Wall-clock Time） |
| **QPS (Queries Per Second)** | 每秒可处理的任务数量 |
| **Speedup** | 相对于基线的加速比 |
| **Token Consumption** | 总生成 token 数，衡量计算成本 |
| **KV Cache Hit Rate** | 缓存命中率，影响 Prefill 阶段开销 |
| **Task Accuracy / Success Rate** | 任务完成正确率 |

### 基线方法对比
- **Baseline**：原生 LLM 代理，顺序执行，无优化。
- **Large-model only**：全程使用大模型。
- **Small-model only**：全程使用小模型。
- **FCFS / SJF Scheduler**：标准先来先服务 / 最短作业优先调度器。
- **SAM speculative decoding**：基础版本的推测解码。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **整体性能提升**：
  - AgentInfer 在 BrowseComp-zh 和 DeepDiver 上实现了 **1.8×–2.5× 的端到端加速**。
  - **无效 token 消耗减少超过 50%**。
  - 保持与大模型相当的准确率（33.8% vs 34.6%）。

| 方法 | 准确率 (%) | 加速比 |
|------|------------|--------|
| Large-model only | 34.6 | 1.00× |
| Small-model only | 18.3 | 1.54× |
| **AgentCollab** | **33.8** | **1.32×** |

> AgentCollab 在几乎不损失精度的情况下，获得显著加速。

### 与基线方法对比
- **AgentSched vs FCFS/SJF**：
  - 相比 FCFS，**端到端延迟降低 9.6%**，**KV Cache Hit Rate 提升至 72%**（FCFS: 63%，SJF: 58%）。
  - 成功避免 SJF 导致的长会话缓存驱逐问题。

- **AgentSAM vs 基础 SAM**：
  - **端到端延迟降低 21.2%**（基础 SAM 为 16.3%）。
  - **LLM 解码延迟降低 26.0%**（基础 SAM 为 20.7%）。
  - 归因于跨会话记忆增强带来的更高 Speculative Hit Rate。

- **AgentCompress**：
  - 上下文长度从 >40K tokens 压缩至 ~20K。
  - 端到端延迟降低 **6–42%**，具体取决于负载压力。
  - 保留推理记忆（reasoning traces）至关重要，否则会导致认知混乱和延迟增加。

### 消融实验结果
#### 表：AgentSched 缓存组件消融（Table 9）
| 组合变体 | 端到端延迟变化 | 对话轮数 |
|----------|----------------|----------|
| 基线 | 0% | 1.0× |
| 移除 reasoning traces | **+31.98%** | 1.12× |
| 移除 summary | **+21.32%** | 1.09× |
| 移除最近工具响应 | **+29.50%** | 1.26× |

> 结论：有效的压缩必须选择性保留语义丰富的信息，而非单纯最小化上下文长度。

#### 表：全系统集成效果（Table 13）
| 方法 | QPS 提升 (Nparallel=4) | QPS 提升 (Nparallel=16) |
|------|------------------------|-------------------------|
| Baseline | 1.00× | 1.00× |
| +AgentCollab | 1.32× | 1.52× |
| +Compress | 1.57× | 2.01× |
| +Sched | 1.71× | 2.25× |
| **+SAM (完整系统)** | **1.97×** | **2.52×** |

> 所有模块贡献**累积且互补**，最终实现接近 2.5 倍吞吐提升。

---

## 4. 关键结论和发现

### 主要发现
1. **代理效率不能仅靠单点优化**：传统 TPS 指标在代理场景下具有误导性，必须转向以“任务完成时间”为核心的系统级优化。
2. **协同设计（Co-Design）是关键**：将推理逻辑（如 AgentCollab 的自我评估）与系统机制（如 AgentSched 的缓存感知调度）紧密结合，才能实现真正的效率突破。
3. **记忆管理至关重要**：上下文压缩必须保留推理轨迹，否则会导致认知断裂和性能退化。
4. **自适应机制有效**：影子价格（AgentSched）、动态模型切换（AgentCollab）、异步压缩等机制能有效应对动态负载。

### 方法的局限性
- 当前框架依赖于**显式的 Progress Check 结构化输出**，可能限制通用性。
- **跨会话记忆检索**（AgentSAM）的效果依赖于查询相似度，冷启动或稀有任务可能收益有限。
- 在极高并发下（如 Nparallel=16），推测解码（AgentSAM）可能被禁用，边际增益下降。

### 未来工作方向
- 探索更通用的自我评估信号提取方式，减少对结构化输出的依赖。
- 构建更智能的长期记忆存储与检索机制（Long-Term Memory Store）。
- 将 AgentInfer 框架扩展至多模态代理和实时决策系统。
- 研究代理系统的自优化与在线学习能力，实现真正的“自我进化”。

---

> **总结**：  
> *AgentInfer* 通过 **AgentCollab、AgentCompress、AgentSched、AgentSAM** 四大模块的协同设计，成功解决了 LLM 代理在真实场景中的效率瓶颈。实验证明，该框架在保持高准确率的同时，实现了 **1.8–2.5 倍的端到端加速** 和 **超过 50% 的 token 消耗降低**，为构建高效、可扩展的自主智能系统提供了重要范式。

</details>

---

### 3. [Efficient Mixture-of-Agents Serving via Tree-Structured Routing, Adaptive Pruning, and Dependency-Aware Prefill-Decode Overlap](https://arxiv.org/abs/2512.18126)

**Authors**: Zijun Wang, Yijiahao Qi, Hanqiu Chen, Zishen Wan, Gongjin Sun, Dongyang Li, Shuyi Pei, Cong Hao  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.18126v1  

#### Abstract
Mixture-of-Agents (MoA) inference can suffer from dense inter-agent communication and low hardware utilization, which jointly inflate serving latency. We present a serving design that targets these bottlenecks through an algorithm-system co-design. First, we replace dense agent interaction graphs wi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Mixture-of-Agents Serving via Tree-Structured Routing, Adaptive Pruning, and Dependency-Aware Prefill-Decode Overlap

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对 **Mixture-of-Agents (MoA)** 推理系统中存在的两大瓶颈问题：

1. **冗余的代理间通信**：传统 MoA 系统采用全连接（all-to-all）拓扑结构，导致大量低效甚至无意义的信息交换，增加端到端延迟（E2E latency）。
2. **硬件利用率低下**：
   - 层级同步机制使得整个层的延迟由最慢的 agent 决定；
   - 现有 LLM serving 框架未考虑 agent 之间的依赖关系，无法有效重叠 prefill 和 decode 阶段。

这些问题共同导致 MoA 系统虽然在准确性上有潜力，但在实际部署中面临高延迟和低吞吐的问题。

---

### **提出了什么新方法或新思路**

作者提出 **Faster-MoA**，一个算法-系统协同设计的高效 MoA 服务框架，包含三大核心技术：

#### ① **Hierarchical Tree Topology（分层树状拓扑）**
- 将传统的 all-to-all 连接替换为树形结构：每层 agent 被划分为若干 cluster，下一层每个 agent 只接收对应 cluster 的输出。
- 实现局部聚合 → 全局聚合，显著减少冗余通信和上下文长度。
- 支持跨分支并行执行，避免全局层同步带来的 stall time。

#### ② **Dynamic Early-Exit (EE) Mechanism（动态早退机制）**
- 在运行时基于小模型（如 4B/8B）的输出语义相似性和置信度（confidence），决定是否跳过大模型（如 32B）的推理。
- 利用共享 embedding model（Qwen3-Embedding-4B）计算 Frobenius Cosine Similarity（FCS）衡量语义一致性。
- 综合 confidence 和 similarity 得出 early-exit 概率 $ Q = \sqrt{C \cdot B} $，实现自适应剪枝。

#### ③ **Dependency-Aware Incremental Prefilling（依赖感知增量预填充）**
- 将前驱 agent 的 decode 阶段拆分为子阶段，并将生成的 token 流式传输给后继 agent 进行增量 prefill。
- 复用已有的 KV Cache，仅对新增 token 重新计算 KV，极大降低重复 prefill 开销。
- 引入 **Shell Router** 和 **Agent Prompt Cache (APC)** 协调依赖调度，实现 prefill-decode 的跨 agent 重叠。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **效率** | 最多降低 **90% 的 E2E 延迟**，GPU 利用率提升，减少 idle 时间 |
| **准确性** | 保持与 all-to-all 基线相当（±1%）甚至更高（在部分任务上） |
| **可扩展性** | 树结构支持更大规模 agent 部署而不引发通信爆炸 |
| **系统兼容性** | 适配主流 LLM serving 框架（SGLang/vLLM），无需修改底层引擎 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

共五个基准测试，覆盖数学推理与科学问答：

| 数据集 | 描述 |
|--------|------|
| **GSM8K** | 小学级别数学应用题，相对简单 |
| **MATH-500** | 中等难度数学问题集合 |
| **AIME2025** | 高中奥数级别难题，挑战性强 |
| **MMLU-ProX-Lite** | 多学科 STEM 科学问答 |
| **IFBench** | 指令跟随能力评测 |

---

### **实验设置和评估指标**

#### **模型选择**
- 使用 Qwen 模型家族以避免 tokenizer 不一致问题：
  - Qwen3-VL-4B-Instruct
  - Qwen3-VL-8B-Instruct
  - Qwen3-VL-32B-Instruct
  - 辅助模型：Qwen3-Embedding-4B（用于语义相似性计算）

#### **硬件平台**
- **6 × NVIDIA H200 GPUs**（单台 H200 HGX Server）
- 每个模型分配独立的 Prefill Engine 和 Decode Engine
- 使用 NVLink 实现高速 KV Cache 传输

#### **评估指标**
- **End-to-End Latency (E2E)**：从输入到最终输出的时间
- **Accuracy**：任务正确率
- **Model Activation Rate**：各 size 模型被调用的比例
- **KV Cache Hit Rate**：增量 prefill 的缓存命中率

---

### **基线方法对比**

| 设置编号 | 名称 | 描述 |
|--------|------|------|
| #1 | All-to-all Baseline | 三层全连接结构（9→9→1） |
| #2 | Tree only | 9-3-1 树结构，无其他优化 |
| #3 | Tree + Incremental Prefilling | 加入增量 prefill |
| #4 | **Faster-MoA**（完整版） | 树结构 + 动态 EE + 增量 prefill |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 方法 | 平均 E2E 延迟下降 | 准确率变化 |
|------|------------------|-----------|
| Tree only | ~62% | ±1% 内波动 |
| Tree + Incremental Prefill | ~76% | 无影响（仅调度优化） |
| **Faster-MoA（完整）** | **最高达 90%** | **持平或更高** |

> 注：延迟下降是在相同准确率水平下的测量。

---

### **与基线方法的对比结果**

- 在所有数据集中，**Faster-MoA 显著优于 all-to-all 基线**：
  - **GSM8K**: 延迟 ↓85%, 准确率 93.9% vs 94.0%
  - **MATH-500**: 延迟 ↓88%, 准确率 90.6% vs 90.4%
  - **AIME2025**: 延迟 ↓82%, 准确率 **56.5% vs 53.4% ↑**
  - **MMLU-ProX-Lite**: 延迟 ↓89%, 准确率 **60.0% vs 55.6% ↑**
  - **IFBench**: 延迟 ↓73%, 准确率 34.6% vs 34.6%

👉 **特别发现**：在较难任务（AIME2025、MMLU-ProX-Lite）上，Faster-MoA **反而提升了准确率**，说明动态 EE 成功过滤了弱模型的噪声输出，提高了融合质量。

---

### **消融实验结果**

#### （1）动态 Early Exit 分析（图 4(i)）
- **小模型激活率远高于大模型**：
  - 在 GSM8K 上，32B 模型仅被调用约 7% 的样本
  - 在 IFBench 上升至 ~18%，体现其自适应性
- **Early Exit 判断开销仅增加 ~5% 延迟**，但带来 10%~50% 的整体延迟下降

#### （2）增量 Prefill 效果（图 4(iii)）
- 相比三种 baseline（Naive PD、Data Parallelism、Chunked Prefill），**增量 prefill 在第二层聚合器上平均降低 27.4% 延迟**
- KV Cache Hit Rate 接近 100%，验证了增量更新的有效性

#### （3）综合效果（图 4(ii)）
- 所有优化叠加后，**延迟持续递减，且无精度损失**
- 证明三个技术模块具有正向协同效应

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **稀疏化连接优于全连接**：树状拓扑通过结构化稀疏减少了冗余通信，在多数任务中无需全连接即可达到同等甚至更优性能。
2. ✅ **动态剪枝可行且有效**：利用语义相似性和置信度信号进行 early exit，能智能跳过不必要的大模型调用，大幅节省资源。
3. ✅ **依赖感知流水线可行**：通过增量 prefill 技术，可在存在数据依赖的情况下实现 prefill-decode 重叠，打破传统串行瓶颈。
4. ✅ **性能与精度双赢**：Faster-MoA 不仅显著降低延迟（up to 90%），还在复杂任务上提升了准确率，表明“少而精”的 agent 协作优于“多而杂”。

---

### **方法的局限性**

1. **依赖特定模型族**：当前实现基于 Qwen 家族，若跨不同 tokenizer 的模型需额外处理文本拼接与对齐。
2. **embedding model 开销**：语义相似性计算需要额外调用 embedding model，可能成为轻量级场景的负担。
3. **树结构固定**：目前采用固定的 9-3-1 结构，尚未探索自动学习最优拓扑的能力。
4. **长尾生成风险**：尽管树结构隔离了 straggler 影响，但极端长输出仍可能阻塞下游。

---

### **未来工作方向**

1. **自动化拓扑搜索**：结合强化学习或进化算法，动态生成最优 agent 连接结构。
2. **轻量化 EE 判断机制**：研究不依赖外部 embedding model 的内部表示相似性度量。
3. **支持异构模型池**：扩展框架以兼容不同厂商、不同 tokenizer 的 LLM。
4. **在线学习型路由策略**：让系统根据历史表现动态调整 early-exit 阈值和路由路径。
5. **集成 MoE 思想**：将 Faster-MoA 与 Mixture-of-Experts 架构结合，构建 hybrid 多智能体系统。

---

> **总结一句话**：  
> Faster-MoA 通过 **Tree-Structured Routing + Adaptive Pruning + Dependency-Aware Prefill-Decode Overlap** 三重创新，在几乎不牺牲准确性的前提下，实现了 MoA 系统高达 **90% 的延迟压缩**，为大规模多代理系统的高效部署提供了实用解决方案。

</details>

---

### 4. [TraCT: Disaggregated LLM Serving with CXL Shared Memory KV Cache at Rack-Scale](https://arxiv.org/abs/2512.18194)

**Authors**: Dongha Yoon, Younghoon Min, Hoshik Kim, Sam H. Noh, Jongryool Kim  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.18194v1  

#### Abstract
Disaggregated LLM serving improves resource efficiency by separating the compute-intensive prefill phase from the latency-critical decode phase. However, this architecture introduces a fundamental bottleneck: key/value (KV) tensors generated during prefill must be transferred to decode workers, and ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TraCT: Disaggregated LLM Serving with CXL Shared Memory KV Cache at Rack-Scale

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大语言模型（LLM）推理系统广泛采用**解耦架构**（disaggregated architecture），将计算密集型的 **prefill 阶段** 和延迟敏感的 **decode 阶段** 分离，以提升资源利用率。然而，这种架构引入了一个关键瓶颈：**KV tensors 必须在 prefill worker 和 decode worker 之间传输**。

当前主流方案依赖 **RDMA 网络路径** 进行 KV 数据交换，即使存在高前缀重用率（prefix reuse），每次缓存命中仍需经过 NIC、host DRAM 缓冲区和多层网络协议栈，导致：
- 显著增加 **TTFT（Time to First Token）**
- 引入尾部延迟波动
- 受限于网络带宽和拥塞

此外，现有 CXL Type-3 设备虽提供字节可寻址共享内存，但缺乏跨节点原子操作和全局一致性支持，难以直接用于高性能 KV 共享。

---

### 提出的新方法：TraCT
TraCT 是一个基于 **CXL 共享内存** 的机架级（rack-scale）LLM 推理服务系统，其核心思想是：
> 利用 CXL Type-3 设备作为 **无网络跳转的 KV 传输媒介 + 机架范围内的 prefix-aware KV 缓存**，实现 GPU 到 CXL 内存的直接 DMA 访问，彻底绕开 NIC 和主机间拷贝。

#### 主要创新点
1. **统一的 CXL 数据通路（Unified CXL Pathway）**
   - 将 KV 传输与 KV 缓存统一到同一个 CXL 共享内存路径中。
   - Prefill worker 通过 GPU-CXL DMA 将 KV 块写入 CXL；
   - Decode worker 直接从 CXL 读取 KV 块至 GPU，无需 NIC 参与。

2. **两层软件同步机制（Two-Tier Inter-node Synchronization）**
   - 针对 CXL Type-3 缺乏硬件原子性和全局一致性的挑战：
     - **本地层（Local Tier）**：每个节点维护驻留于 DRAM 的 `local_lock`，限制每节点最多一个线程参与竞争。
     - **全局层（Global Tier）**：CXL 内存中维护固定大小的 `global_lock` 数组，由专用 lock manager 协调分配锁。
   - 实现了低开销、可扩展且不依赖硬件原子的操作。

3. **细粒度元数据可见性控制**
   - 使用 `clflush` 而非 `clflushopt` 来确保元数据更新后立即持久化到 CXL 设备。
   - 区分“元数据”与“payload”可见性边界：仅刷新紧凑的元数据区域，KV 数据因经 GPU-CXL DMA 绕过 CPU 缓存而天然可见。

4. **面向非一致共享内存的数据管理设计**
   - **偏移量寻址（Offset-based Addressing）**：避免虚拟地址跨节点不可用问题。
   - **两级内存分配器**：全局 chunk allocator + 每节点本地堆，减少跨节点元数据争用。
   - **共享对象存储**：只发布根对象（如 prefix cache root），内部结构通过偏移链接，降低管理开销。

---

### 相比现有方法的优势
| 方面 | 传统 RDMA 方案（如 NIXL/UCX） | DRAM 缓存方案（如 LMCache） | TraCT |
|------|-------------------------------|-----------------------------|-------|
| KV 传输路径 | 经 NIC、host DRAM、协议栈 | 同样走网络（即使缓存命中） | **直接 GPU-CXL DMA，无 NIC 跳转** |
| 缓存位置 | 分布式或本地 DRAM | 主机 DRAM | **CXL 共享内存，全机架可见** |
| 同步机制 | 依赖 RDMA 或集中式协调 | 中心化元数据服务器 | **去中心化，纯软件锁，无协调者** |
| 性能特征 | 高延迟、易受网络影响 | 减少计算但无法避免网络传输 | **更低延迟、更稳定、更高吞吐** |

---

## 2. 核心实验方法和设置

### 实验平台
- **硬件配置**：
  - 两台服务器，各配备：
    - NVIDIA A6000 GPU（48GB GDDR）
    - 512GB host DRAM
    - 100Gbps Mellanox NIC（用于基线）
  - CXL 设备：Niagara 2.0（第二代 CXL Type-3 内存扩展器）
    - 提供 64GB 字节可寻址共享内存
    - 延迟：640ns，带宽：10.1 GB/s
- **软件框架**：基于 NVIDIA 的 **Dynamo v0.5.0 + vLLM v0.10.1.1**

---

### 基线方法对比
| 配置 | 描述 |
|------|------|
| **NIXL/UCX** | 原始 Dynamo 架构，使用 RDMA 进行 KV 传输，**无 prefix 缓存** |
| **LMCache** | 在 prefill worker 的 host DRAM 中部署 48GB prefix cache，但仍通过 RDMA 向 decode worker 传输所有 KV 块 |
| **TraCT** | 使用 CXL 共享内存作为 KV cache 和传输介质，启用 GPU-CXL DMA |

> 所有实验均关闭 GPU 内部的 prefix caching，以便隔离比较外部缓存效果。

---

### 工作负载（Workloads）

#### （1）静态工作负载
- 固定输出长度为 3 tokens
- 输入长度变化：1500、3000、4500、6000 tokens
- 用于精确控制 KV 数据量，分析传输性能

#### （2）合成工作负载（Synthetic Workloads）
使用 Dynamo 内置请求生成器创建三类负载（A/B/C），统计如下：

| Workload | 平均输入长度 | 平均输出长度 | 平均唯一前缀长度（反映多样性） |
|---------|--------------|--------------|-------------------------------|
| A       | 4449         | 215          | 1073                          |
| B       | 3775         | 215          | 1215                          |
| C       | 3101         | 215          | 1631                          |

> 唯一前缀越长 → 缓存命中率越低 → 更具挑战性

---

### 评估指标
- **TTFT（Time to First Token）**：衡量 prefill 阶段延迟，重点关注平均值和 P99
- **Request Throughput（QPS）**：端到端请求吞吐量
- **Prefill Throughput**：prefill worker 处理能力
- **GPU SM Utilization / Power Consumption**：资源利用效率
- **PCIe/CXL Bandwidth Usage**
- **Prefix Cache Hit Rate**

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 指标 | TraCT 提升幅度 | 对比基线 |
|------|----------------|----------|
| **平均 TTFT** | 最高 **9.8× 降低** | vs. NIXL/LMCache |
| **P99 TTFT** | 最高 **6.2× 降低** | vs. NIXL/LMCache |
| **峰值吞吐量（Peak Throughput）** | 最高 **1.6× 提升** | vs. LMCache |
| **Prefill 延迟稳定性** | 显著改善，CDF 曲线更陡峭 | 更小尾部波动 |

---

### 详细实验结果

#### （1）CXL 替代 RDMA 的有效性（无缓存场景）
- 图5（Figure 5）显示，在不同输入长度下，**TraCT（无缓存）相比 NIXL 显著降低 TTFT**。
  - 输入 6000 tokens 时，尾部延迟明显缩短。
- 图6（Figure 6）表明，**TraCT 在高并发下维持与 RDMA 相当的吞吐量**，证明 CXL DMA 可作为高效替代路径。

> ✅ 结论：**仅靠 GPU-CXL DMA 即可显著降低 prefill 延迟，且不影响吞吐。**

---

#### （2）启用缓存后的综合性能优势
- 图7（Figure 7）：TraCT 在所有负载下均达到最高 **request throughput**，最高比 LMCache 提升 **1.6×**。
- 图8（Figure 8）：尽管 TraCT 的 **cache hit rate 低于或接近 LMCache**，但性能反而更好。
  > 说明性能增益来自 **消除缓存命中的网络传输开销**，而非单纯提高命中率。

- 图9（Figure 9）：TTFT 累积分布函数（CDF）全面左移：
  - **平均 TTFT 最多降低 9.83×**
  - **P99 TTFT 最多降低 6.2×**
  - 特别是在 bursty 或高并发场景下优势明显。

---

#### （3）性能分解分析（图10）
- **KV Read 时间**：TraCT 几乎恒定，而 LMCache/NIXL 随负载上升 → 表明网络拷贝成为瓶颈。
- **Compute 时间**：三者相近，说明 **KV 传输已成为主导开销**，而非计算本身。
- **KV Write 时间**：TraCT 更快，因其跳过重复写入已存在的 KV 块。

---

#### （4）资源利用与能耗优化（图11）
- **GPU SM 利用率更低且更稳定**：
  - Prefill：缓存命中避免重复计算
  - Decode：KV 直接加载，减少等待
- **PCIe RX 带宽更高且稳定**：CXL 提供持续高带宽访问
- **功耗更低**：执行时间短 + SM 活动少 → **降低瞬时与总体能耗**
  > 有助于降低 LLM 推理的 TCO（Total Cost of Ownership）

---

### 消融实验（隐含）
虽然未明确命名消融实验，但以下对比体现了模块化验证：
- **TraCT（no cache） vs NIXL** → 验证 **CXL DMA 本身的价值**
- **TraCT（with cache） vs LMCache** → 验证 **CXL 缓存 + 去网络化的协同效益**
- **不同 workload 下的表现差异** → 验证系统在多样化请求下的鲁棒性

---

## 4. 关键结论和发现

### 主要发现
1. **CXL 共享内存可以有效取代 RDMA 成为 LLM 推理中的 KV 传输通道**，尤其适合解耦架构。
2. **即使没有缓存，GPU-CXL DMA 也能显著降低 TTFT 并保持高吞吐**，优于传统网络路径。
3. **将 CXL 用作机架级 prefix-aware KV cache 可带来巨大性能收益**：
   - 最高 **9.8× 降低平均 TTFT**
   - 最高 **6.2× 改善 P99 延迟**
   - 最高 **1.6× 提升峰值吞吐**
4. **TraCT 的软件同步与数据管理机制可在无硬件原子性和全局一致性的前提下保证正确性与高性能**。
5. **系统更节能、GPU 利用更充分、延迟更稳定**，具备实际部署潜力。

---

### 局限性
1. **依赖特定硬件支持**：需要支持 CXL.mem 的 Type-3 设备及 GPU 直接访问能力。
2. **当前使用简单 LRU 淘汰策略**：未探索更复杂的替换算法（如 LFU、ARC），可能影响缓存命中率。
3. **实验规模有限**：仅使用两个服务器节点，尚未验证大规模集群下的可扩展性。
4. **NUMA 敏感性**：性能依赖于线程绑定至连接 CXL 的 NUMA 节点，否则会引入额外延迟。

---

### 未来工作方向
1. 探索更智能的 **缓存替换策略**（如基于访问频率或热度预测）。
2. 扩展至更大规模的 **多节点机架环境**，研究 lock manager 的横向扩展机制。
3. 支持 **异构模型共存** 场景下的多租户 KV 缓存隔离与调度。
4. 结合 **CXL 内存池化技术** 实现动态容量分配与故障恢复。
5. 探索 **CXL 与 NVMe-oF 的协同使用**，构建多层次 KV 存储体系。

---

> 🔚 **总结**：TraCT 开创性地将 CXL 共享内存应用于 LLM 解耦推理系统，提出了一套完整的软件栈来克服非一致性内存带来的挑战，实现了**去网络化、高性能、低延迟、高能效**的 KV 管理方案，为下一代 LLM 推理基础设施提供了重要参考。

</details>

---

### 5. [NOVA: Discovering Well-Conditioned Winograd Transforms through Numerical Optimization of Vandermonde Arithmetic](https://arxiv.org/abs/2512.18453)

**Authors**: Jayant Lohia  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.18453v1  

#### Abstract
Winograd convolution is the standard algorithm for efficient inference, reducing arithmetic complexity by 2.25x for 3x3 kernels. However, it faces a critical barrier in the modern era of low precision computing: numerical instability. As tiles scale to maximize efficiency (e.g., F(6,3), F(8,3)), the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：NOVA: Discovering Well-Conditioned Winograd Transforms through Numerical Optimization of Vandermonde Arithmetic

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Winograd 卷积是深度学习中高效的推理算法，能将 3×3 卷积的算术复杂度降低 2.25×。然而，其在现代低精度计算（如 FP16、Int8）下存在严重的**数值不稳定性**问题。随着卷积块尺寸增大（如 F(6,3)、F(8,3)），标准整数插值点导致的 Vandermonde 矩阵条件数（condition number）急剧上升（F(8,3) 达到 ~2×10⁵），使得大块 Winograd 在低精度下完全失效，输出接近随机噪声。

### 提出的新方法：NOVA
作者提出 **NOVA**（Numerical Optimization of Vandermonde Arithmetic），一种通过连续空间优化发现稳定 Winograd 插值点的新框架。其核心思想是打破传统“仅使用整数点”的设计范式，将插值点选择视为一个连续优化问题。

#### 方法流程：
1. **Evolution Strategy (ES)**：在 $\mathbb{R}^{n-1}$ 连续空间中搜索最优插值点配置。
2. **Snap-to-Rational**：将连续解映射为简单分数（如分母 ≤10 的有理数）。
3. **Symbolic Verification**：使用 SymPy 进行精确符号验证，确保分解数学正确性。

### 相比现有方法的优势
| 方面 | 传统方法 | NOVA |
|------|--------|------|
| **搜索空间** | 固定整数或参数化家族（如 {-1/c, -c, c, 1/c}） | 全连续空间 $\mathbb{R}^{n-1}$，无预定义词汇限制 |
| **点类型** | 整数为主 | 发现非对称、非倒数关系的分数点（如 ±5/6, ±7/6, ±3/5） |
| **验证方式** | 浮点近似验证，易受舍入误差影响 | 符号验证（exact rational arithmetic），保证数学正确性 |
| **部署兼容性** | 需要重训练、校准或修改架构 | **Drop-in replacement**，无需重训练、无需学习参数 |
| **性能提升** | 条件数改善有限（如 Alam et al. 最多 11×） | 条件数改善高达 **415×（1D）**，2D 下达 **172,484×** |

---

## 2. 核心实验方法和设置

### 数据集
- **ImageNetV2**（30,000 张图像）：用于 FP16 推理的主评估，覆盖多种主流架构。
- **CIFAR-10**（10,000 张图像）：用于 INT8 量化评估，控制变量分析层级别误差。

### 架构
在以下 6 种 ImageNet 预训练模型上进行评估：
- ResNet-18, ResNet-50
- VGG-16, VGG-16-BN
- MobileNet-V2, EfficientNet-B0

### 硬件与精度设置
- **硬件**：NVIDIA Tesla T4 GPU
- **精度**：
  - **FP16**：混合精度（`torch.cuda.amp.autocast`），变换矩阵用 FP32 计算后转为 FP16。
  - **INT8**：测试 per-tensor 和 per-channel 两种量化模式。

### 评估指标
| 指标 | 说明 |
|------|------|
| **K₂(V)** | Vandermonde 矩阵的谱条件数，衡量数值稳定性 |
| **Top-1 Accuracy** | 图像分类准确率，主任务性能指标 |
| **Relative L2 Error** | 层级卷积输出相对于 FP64 直接卷积的误差 |
| **Latency** | 推理延迟（ms），验证是否引入开销 |

### 基线方法对比
- **Standard Integers**：{0, ±1, ±2, ...}，当前主流实现（cuDNN, TensorRT）。
- **Alam et al. (2022)**：基于对称倒数族 {-1/c, -c, c, 1/c} 的参数化搜索。
- **Tap-wise Scaling (Chikin & Kryzhanovskiy, 2022)**：学习每 tap 缩放因子以缓解量化误差。
- **Legendre Basis (Barabasz et al., 2020)**：改变基函数以提高稳定性，但需重新训练。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 条件数改善（1D）
| Tile | Standard K₂ | Discovered K₂ | Improvement |
|------|-------------|---------------|-------------|
| F(4,3) | 42.5 | 14.5 | **2.9×** |
| F(6,3) | 2,075 | 77 | **27×** |
| F(8,3) | 196.9k | 474 | **415×** |

> **发现点示例**：F(8,3) 使用 `{0, ±2/5, ±5/6, ±1, ±7/6}`，这些点不在任何传统“词汇表”中。

#### 2D 卷积下的平方效应（Kronecker Product）
由于 2D 变换矩阵为 Kronecker 积 $A \otimes A$，条件数满足 $K_2(A \otimes A) = K_2(A)^2$，因此 1D 改善在 2D 中**平方放大**：

| 2D Tile | Improvement |
|--------|-------------|
| F(4×4, 3×3) | ~8.5× |
| F(6×6, 3×3) | **733×** |
| F(8×8, 3×3) | **172,484×** |

#### ImageNet FP16 推理准确率恢复
在 F(6,3) 上，标准整数点导致严重崩溃，而 NOVA 完全恢复精度：

| Model | Standard Acc | Discovered Acc | Recovery |
|-------|--------------|----------------|----------|
| VGG-16 | 4.7% | 75.3% | **+70.6pp** |
| VGG-16-BN | 4.7% | 77.5% | **+72.8pp** |
| ResNet-18 | 10.8% | 77.8% | **+67.0pp** |
| ResNet-50 | 38.3% | 80.6% | **+42.3pp** |

> **平均恢复 67–73 个百分点**，且**无需重训练、校准或学习参数**。

#### INT8 层级误差大幅降低
| Tile | Standard Error | Discovered Error | Improvement |
|------|----------------|------------------|-------------|
| F(4,3) | 7.1% | 2.1% | 3.4× |
| F(6,3) | 3990% | 12.4% | **322×** |
| F(8,3) | 100% (饱和) | 59.2% | 1.7× |

> 标准 F(6,3) 输出已完全错误（>100% 误差），而 NOVA 使其可用。

#### 消融实验结果
- **dtype-aware discovery**：约束搜索空间为 FP16 可精确表示的 dyadic rationals（如 3/4, 5/4），虽条件数略差于无约束版本，但避免了表示误差，在 FP16 下总误差更低。
- **与 Tap-wise Scaling 正交**：Tap-wise scaling 仅能带来约 +1.3% 提升，而 NOVA 提供 +67% 基础修复；两者结合仅额外提升 +0.4%，表明 NOVA 是根本性修复。
- **与 Alam et al. 对比**：NOVA 在 F(6,3) 上条件数改善 **11× 更优**，且网络准确率相当，证明其搜索空间更优。

---

## 4. 关键结论和发现

### 主要发现
1. **整数插值点严重次优**：传统使用的整数点（{0, ±1, ±2, ...}）并非最优，其导致的高条件数是低精度下 Winograd 失效的**根本原因**。
2. **分数点显著提升稳定性**：通过优化发现的分数点（如 ±5/6, ±7/6）能将条件数降低 **2.9–415×（1D）**，2D 下可达 **172,484×**。
3. **性能恢复无需额外成本**：NOVA 是**零成本升级**（drop-in replacement），仅需替换变换矩阵常量，即可在现有框架（cuDNN/TensorRT）中启用大块 Winograd。
4. **误差改善可预测**：INT8 误差改善与 $\sqrt{K}$ 改善一致，验证了理论误差界 $\Delta y \propto K \cdot \epsilon$ 的有效性。
5. **与现有方法正交互补**：NOVA 与其他方法（如 scale learning、per-channel quantization）不冲突，可组合使用进一步提升鲁棒性。

### 方法的局限性
- **超大块仍具挑战**：F(8,3) 在 INT8 下即使使用 NOVA，误差仍达 59.2%，需结合其他技术（如 tap-wise scaling）。
- **对称性偏好**：ES 倾向于发现对称配置（如 ±p），可能错过某些非对称最优解。
- **浮点16 表示权衡**：部分分数（如 5/6）在 FP16 中无法精确表示，需 dtype-aware discovery 平衡条件数与表示误差。
- **仅适用于标准 Cook-Toom 形式**：不直接支持 RNS 或 SFC 等替代算法。

### 未来工作方向
- 将 NOVA 发现的点集成到 **cuDNN / TensorRT** 等生产级库中。
- 结合 **learned scaling**（如 PAW、LoWino）构建完整的低精度 Winograd 流水线。
- 扩展至 **5×5 及更大卷积核**（r=5,7），已有初步结果显示 F(6,5) 可获 112× 条件数改善。
- 探索 **直接 2D 点选择** 而非 Kronecker 积，可能获得更优配置。
- 研究 **与 Winograd-aware training** 的联合优化，进一步提升端到端性能。

---

> **总结**：NOVA 通过将 Winograd 插值点选择重构为连续优化问题，打破了数十年来对整数点的依赖，发现了高度稳定的分数配置。其实验结果表明，该方法可在不增加计算开销的前提下，使大块 Winograd 在 FP16/INT8 下从“完全失效”变为“高精度可用”，为下一代高效神经网络推理提供了坚实基础。

</details>

---

### 6. [CienaLLM: Generative Climate-Impact Extraction from News Articles with Autoregressive LLMs](https://arxiv.org/abs/2512.19305)

**Authors**: Javier Vela-Tambo, Jorge Gracia, Fernando Dominguez-Castro  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.19305v1  

#### Abstract
Understanding and monitoring the socio-economic impacts of climate hazards requires extracting structured information from heterogeneous news articles on a large scale. To that end, we have developed CienaLLM, a modular framework based on schema-guided Generative Information Extraction. CienaLLM use...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CienaLLM: Generative Climate-Impact Extraction from News Articles with Autoregressive LLMs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统气候影响监测依赖于物理干旱指数（如SPI、PDSI），这些指标无法反映社会经济层面的实际影响。尽管新闻文章是记录干旱等气候事件社会影响的丰富来源，但其信息高度分散且异构，人工或半自动提取成本高、难以规模化。现有监督模型（如SeqIA）虽能实现较高准确率，但需要大量标注数据并针对特定任务重新训练，缺乏灵活性。

### 提出的新方法与创新
本文提出 **CienaLLM** —— 一个基于 **schema-guided Generative Information Extraction (GenIE)** 的模块化框架，利用 **open-weight LLMs** 在零样本（zero-shot）设置下从西班牙语新闻中提取结构化气候影响信息。

#### 主要创新点：
- **零样本生成式信息抽取（Zero-shot GenIE）**：无需微调或重新训练，仅通过自然语言提示（prompting）引导LLM输出符合预定义schema的结构化信息（如JSON格式）。
- **模块化与可配置设计**：支持灵活配置prompt模板、output schema、多步pipeline（如summarization、self-criticism）、本地或云端推理，便于复现和扩展。
- **模型无关性（Model-agnostic）**：可在不同LLM家族（Gemma、Llama、Qwen）间切换，适应不同硬件资源需求。
- **开放与可复现**：发布代码、配置文件和schema定义，促进可持续研究。

### 相比现有方法的优势
| 维度 | 传统监督方法（如SeqIA） | CienaLLM |
|------|------------------------|---------|
| **标注依赖** | 需要大量标注数据进行训练 | 零样本，无需标注 |
| **泛化能力** | 更换任务/语言需重新标注和训练 | 只需修改prompt和schema即可迁移 |
| **部署灵活性** | 多为API服务，隐私与控制受限 | 支持本地部署（via Ollama），保障隐私与可控性 |
| **维护成本** | 模型更新需重新训练 | 继承新LLM版本的能力，无需重训 |

---

## 2. 核心实验方法和设置

### 数据集
实验基于并扩展了Lopez-Otal等人构建的数据集，并新增三个专用评估集：

| 数据集 | 描述 | 规模 |
|-------|------|-----|
| **DID (Drought Impacts Dataset)** | 文章级干旱影响标注，涵盖农业、畜牧业、水资源、能源四类影响 | 386篇文章（验证集269，测试集117） |
| **DRD (Drought Relevance Dataset)** | 判断文章是否涉及干旱的二分类任务 | 2,240篇文章（正例1,270，负例970） |
| **DILD (Drought Impact Locations Dataset)** | 标注受影响的西班牙省份（共50个） | 100篇文章，平均每篇提及8.35个省 |

此外还构建了大规模西班牙新闻语料库（El País, ABC, 20 Minutos等），用于未来分析。

### 实验设置
- **LLM选择**：12个open-weight模型，覆盖三大家族（Gemma、Llama、Qwen），三种规模（小<7B、中7–25B、大>25B），两种精度（fp16 和 4-bit量化 q4_K_M）。
- **Prompt工程策略**：
  - **SUM**（Summarization）：先摘要再提取
  - **CoT**（Chain of Thought）：“逐步推理”指令
  - **SC**（Self-Criticism）：第二轮自我修正
  - **DESC**（Impact Descriptions）：加入影响类别详细描述
- **响应解析策略（RPARSE）**：
  - `RPARSE=False`：单步生成JSON
  - `RPARSE=True`：两步法，先自由文本提取，再由另一LLM转为JSON
- **JGEN模式**：统一使用`prompt`方式指定JSON格式（因Gemma不支持tool calling）

### 评估指标
- **准确性**：micro-F1、Precision、Recall、Accuracy（多标签分类）
- **可靠性**：Parsing Error Rate（无法解析为合法JSON的比例）
- **效率**：每篇文章处理时间（秒）

### 基线方法对比
与监督模型 **SeqIA** 进行比较，在相同E2E数据集上评估性能与延迟。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（在DID测试集上）

| 配置 | F1 Score | Precision | Recall | Exec. Time (s/art.) |
|------|----------|-----------|--------|---------------------|
| **Best-F1** (qwen_72b + CoT + DESC + RPARSE) | **0.873** | 0.837 | 0.911 | 36.43 |
| **Efficient** (qwen_7b + DESC + RPARSE) | 0.808 | 0.832 | 0.785 | 3.32 |
| **Fastest** (qwen_3b + RPARSE) | 0.646 | 0.810 | 0.538 | 2.47 |

> 所有配置均无parsing error。

#### 分类别表现（Best-F1）
| 影响类型 | F1 Score |
|--------|---------|
| 农业 | 0.868 |
| 畜牧业 | 0.966 |
| 水资源 | 0.824 |
| 能源 | 0.914 |

### 与基线方法对比（在E2E数据集上）

| 方法 | F1 Score | Exec. Time (s/art.) |
|------|----------|---------------------|
| **CienaLLM (Best-F1)** | **0.782** | 30.07 |
| **SeqIA (supervised)** | 0.769 | **1.54** |

👉 **结论**：CienaLLM在准确性上**略优于SeqIA**（+1.3% F1），但推理耗时高出约20倍。

### 消融实验关键发现

#### （1）Response Parsing 显著提升可靠性
- 平均parsing error从 **4.0%**（无RPARSE）降至 **0.7%**（启用RPARSE）
- 对某些模型（如`llama_8b`）错误率下降超15个百分点
- 准确率几乎不变（F1差异不显著），但可靠性大幅提升

#### （2）模型大小决定性能上限
- 大模型（>70B）表现最强且最稳定（低方差）
- `qwen_72b` 和 `llama_70b` 平均F1 > 0.85
- 小模型（如`gemma_4b`, `qwen_3b`）性能波动大，对prompt敏感

#### （3）量化带来显著效率增益
- 4-bit量化使执行时间减少 **30–40%**
- F1损失较小：
  - Gemma: -0.008
  - Llama: -0.017
  - Qwen: -0.008
- 推荐在资源受限场景使用量化模型

#### （4）Prompt策略效果具有模型特异性
| 策略 | 效果总结 |
|------|--------|
| **SUM** | ✅ 对Gemma有效（+0.054 F1）<br>❌ 对Llama/Qwen有害 |
| **CoT** | ✅ 中大型模型略有提升（+0.01）<br>❌ 小模型无效 |
| **SC** | ❌ 几乎无收益，多数模型忽略指令 |
| **DESC** | ✅ 大模型受益明显（qwen_72b +0.021）<br>❌ 小模型反而下降 |

> 最佳配置通常包含 **DESC** 和 **CoT**，Gemma偏好 **SUM**

#### （5）Location Extraction 是最难任务
| 配置 | F1 Score | Parsing Error |
|------|----------|---------------|
| Best-F1 | 0.465 | 2% |
| Efficient | 0.233 | 50% |
| Fastest | 0.007 | 62% |

👉 表明从文本推断“受影响区域”极具挑战，需结合地理知识库进一步后处理。

---

## 4. 关键结论和发现

### 主要发现
1. **CienaLLM 在零样本下可达到甚至超越监督模型的准确率**，尤其在干旱影响提取和相关性判断任务上。
2. **更大的LLM提供更强、更稳定的性能**，适合高精度场景；而中小模型经优化后也可实现良好性价比。
3. **量化技术可在轻微精度代价下大幅降低计算开销**，适合边缘或低成本部署。
4. **Prompt策略并非通用银弹**，其有效性高度依赖模型家族与规模，应作为可调超参而非固定流程。
5. **启用两步式Response Parsing几乎消除格式错误**，是确保跨模型公平比较的关键步骤。
6. **地理位置提取仍是瓶颈**，当前LLM难以可靠地将模糊表述（如“半岛东北部”）映射到具体行政单位。

### 方法的局限性
- **高推理延迟**：相比SeqIA慢数十倍，不适合大规模预过滤。
- **依赖高质量prompt设计**：需领域专家参与定义impact descriptions。
- **未评估其他灾害类型**：目前仅验证干旱场景，泛化至洪水、热浪等有待验证。
- **语言限制**：实验集中于西班牙语，跨语言迁移需进一步测试。
- **静态schema依赖**：仍需手动定义和维护schema，自动化程度有限。

### 未来工作方向
- 扩展至其他气候灾害（floods, hailstorms, heatwaves）和其他国家/语言。
- 探索few-shot或retrieval-augmented generation以进一步提升性能。
- 开发ensemble策略融合多个LLM家族优势。
- 结合确定性地理解析工具（gazetteers, basin maps）提升location extraction recall。
- 构建端到端系统，将提取结果转化为可视化仪表盘或决策支持信号。
- 探索轻量级LLM + prompt tuning组合，在保持效率的同时逼近大模型性能。

---

> **总结一句话**：  
> CienaLLM 展示了 **schema-guided GenIE + open-weight LLMs** 在气候影响信息提取中的强大潜力，实现了无需训练即可灵活适配新任务的能力，虽然牺牲了一定效率，但在准确性和可扩展性方面展现出显著优势，为构建动态、可解释、可持续的气候风险监测系统提供了新范式。

</details>

---

### 7. [L4: Low-Latency and Load-Balanced LLM Serving via Length-Aware Scheduling](https://arxiv.org/abs/2512.19179)

**Authors**: Yitao Yuan (Peking University, ScitiX AI), Chenqi Zhao (Peking University), Bohan Zhao (ScitiX AI), Zane Cao (ScitiX AI), Yongchao He (ScitiX AI), Wenfei Wu (Peking University)  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.19179v1  

#### Abstract
Efficiently harnessing GPU compute is critical to improving user experience and reducing operational costs in large language model (LLM) services. However, current inference engine schedulers overlook the attention backend's sensitivity to request-length heterogeneity within a batch. As state-of-the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：L4: Low-Latency and Load-Balanced LLM Serving via Length-Aware Scheduling

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代大型语言模型（LLM）服务中，随着上下文长度支持超过 128K tokens，**请求序列长度异质性（length heterogeneity）** 成为系统性能瓶颈。现有调度器在批处理时混合长短请求，导致：

- **Attention 后端性能严重下降**：由于 GPU 内部负载不均衡、同步开销增加；
- **GPU 利用率低下** 和 **延迟显著上升**，尤其是长尾延迟（tail latency）；
- 单实例调度受限于 Little’s Law，无法积累足够请求来形成同质批次。

尽管已有优化如 FlashAttention 提升了 kernel 效率，但**调度层仍未能匹配硬件对长度同质性的偏好**。

---

### **提出了什么新方法或新思路**

作者提出 **L4** —— 一种基于**长度感知的跨实例调度系统**，其核心思想是：

> **“Global homogenization for local efficiency”**  
> 通过全局调度实现局部高效：将多实例集群组织成按序列长度划分的专用阶段（stages），形成一个逻辑流水线。

#### 主要机制包括：

1. **Length-Specialized Pipeline 构建**  
   将多个 LLM 推理实例划分为若干组（stages），每组专门处理特定长度范围内的请求。随着解码进行，请求自然从短到长阶段迁移。

2. **动态规划算法进行最优阶段划分（Stage Partitioning）**  
   基于历史工作负载统计，使用动态规划（Dynamic Programming）寻找最优的 stage 数量、各 stage 长度区间及实例分配，以最小化整体 QoE（Quality of Experience）。

3. **运行时自适应边界调整（Adaptive Range Refinement）**  
   每个实例通过 `LoadTracker` 收集自身与下游实例的请求长度分布，动态调整阶段边界，应对负载漂移。

4. **去中心化的 Bid-Ask 负载均衡协议**  
   当某实例过载时，主动“出价”（ask）迁移请求；其他轻载实例“竞价”（bid）接收请求。无需中央协调器，实现高效的跨阶段和组内负载再平衡。

5. **KV Cache 的异步 Live Migration**  
   在请求迁移过程中，采用类似 Llumnix 的机制，分轮次传输 KV 缓存，避免阻塞源实例。

---

### **相比现有方法的优势**

| 维度 | 现有方法（如 vLLM, SGLang, Llumnix） | L4 |
|------|----------------------------------------|-----|
| 调度粒度 | 单实例内部调度为主，忽略跨实例协同 | 全局视角，跨实例长度感知调度 |
| 批处理同质性 | 难以避免长短请求混合 | 自动形成长度同质批次 |
| 负载均衡 | 中心化或简单轮询 | 去中心化 bid-ask，响应式负载均衡 |
| 可扩展性 | 存在单点瓶颈风险 | 分布式设计，适合大规模部署 |
| 性能提升 | 局限于单实例优化 | 显著降低延迟、提高吞吐 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **ShareGPT 数据集**（[RyokoAI/ShareGPT52K](https://huggingface.co/datasets/RyokoAI/ShareGPT52K)）
  - 包含真实 ChatGPT 对话记录，具有典型的**高度偏斜的请求长度分布**（大量短请求 + 少量极长请求）。
  - 请求长度上限设为 128K tokens。

### **实验设置**

- **测试平台**：
  - **H20 测试床**：2 节点 × 8×NVLink 连接的 H20 GPU（每卡 141GB 显存）
  - **L40 测试床**：2 节点 × 8×PCIe 连接的 L40 GPU（每卡 48GB 显存）

- **模型覆盖范围广**：
  - Tiny: Llama-3.2-3B, Phi-3-3B
  - Small: Llama-3.1-8B, GLM-4-9B
  - Moderate: Phi-3-14B, Qwen2.5-14B
  - Large: QwQ-32B, Qwen2.5-32B
  - 特别测试了 **Llama-3.1-70B** 在 TP=2 和 TP=4 下的表现

- **请求模式**：
  - 使用 Poisson 过程模拟请求到达，速率可调（轻载至重载）
  - 每个测试持续相同时间，确保公平比较

### **评估指标**

| 指标 | 定义 |
|------|------|
| **TTFT** (Time to First Token) | 用户提交请求到收到第一个输出 token 的延迟 |
| **TPOT** (Time per Output Token) | 每生成一个后续 token 的平均延迟 |
| **Throughput** | 系统整体每秒生成的 token 数量 |
| **SLO Attainment** | 满足预定义 SLO（如 5×/10×/20×基础延迟）的请求比例 |
| **Normalized Latency** | 端到端延迟 / 输出 token 数，综合衡量效率 |

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **vLLM 0.9.1** | 使用 FCFS 调度 + FlashAttention，无跨实例调度 |
| **SGLang 0.4.9** | 支持结构化推理，使用 FlashInfer 后端 |
| **Llumnix** | 当前最先进的多实例调度系统，具备请求迁移能力，作为主要对比对象 |

所有系统均部署多个实例，并配合 Round-Robin 或内置调度器进行负载分发。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **延迟显著降低**

- **Median TTFT 下降最多达 67%**
- **Tail TTFT 下降最多达 69%**
- **Mean TPOT 下降 30%-64%**（vs vLLM），最高达 77%（vs SGLang）

> 示例（H20 testbed, heavy load）：
> - Llama-3.2-3B 上，L4 相比 vLLM 平均 TTFT 降低 **78%**
> - 相比 Llumnix（state-of-the-art inter-instance scheduler），TTFT 降低 **36%-66%**

#### ✅ **吞吐大幅提升**

- **整体系统吞吐最高提升 2.89×**（vs Llumnix）
- 在不同模型上平均提升：
  - vs vLLM: **1.99×**
  - vs SGLang: **2.18×**
  - vs Llumnix: **1.71×**

> 在 Llama-3.1-70B（TP=4）配置下，吞吐提升高达 **4.16×**

#### ✅ **SLO 达成率显著改善**

- 在 5× SLO 条件下，SLO attainment 提升 **3.8–7.6×**
- 在 20× SLO 条件下，仍保持 **2.0–2.8×** 的优势

---

### **消融实验结果（Ablation Study）**

#### 🔍 **QoE 模型准确性验证**
- 相比静态预测器（返回全局平均延迟），L4 的 QoE 模型预测误差仅 **8.9%**（vs 64%）
- 错误集中在零附近，说明模型能准确反映实际延迟趋势

#### 🔍 **不同 pipeline 结构对比**
| 配置 | 表现 |
|------|------|
| **No Pipeline**（所有实例合并为一 stage） | 性能最差，因长度异质性高 |
| **Chain Layout**（每个 stage 一个实例） | 迁移开销大，负载难平衡 |
| **L4（动态划分）** | **延迟低 30%，吞吐高 7.1%** |

#### 🔍 **边界调整策略对比**
| 策略 | 表现 |
|------|------|
| Quantity-based（按请求数均分） | 负载严重不平衡，性能差 |
| Memory-based（按显存占用均分） | 忽视请求复杂度差异 |
| **L4（基于 QoE 优化）** | **延迟降低 21%，吞吐提升 12%** |

#### 🔍 **Bid-Ask 协议有效性**
- 使用 **Coefficient of Variation (CV)** 衡量负载均衡程度：
  - Round-Robin: CV 最高（最不均衡）
  - Inter-stage only bid-ask: 有所改善
  - Full bid-ask（跨阶段+组内）: **CV 降低 47%**（vs RR）

#### 🔍 **算法效率优化效果**
- 原始 DP 复杂度：O(E³L²)，16 实例 + 128K 长度 → 预计耗时 **51 小时**
- L4 优化后（指数分桶 + 两阶段启发式）→ **仅需 0.06 秒**，加速约 **3×10⁶ 倍**

---

## 4. 关键结论和发现

### **主要发现**

1. **Attention kernel 对长度异质性极度敏感** 是当前 LLM 服务的主要瓶颈，即使使用 FlashAttention 也无法完全缓解。
2. **单实例调度受制于 Little’s Law**，无法积累足够请求来消除异质性。
3. **多实例系统的规模本身可以成为解决方案**：通过全局调度构建长度感知流水线，可在不修改底层引擎的前提下大幅提升性能。
4. **L4 实现了“本地兼容、全局优化”**：保留原有调度器不变，仅通过跨实例调度即可释放硬件潜力。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **依赖充足显存支持大 batch** | 若模型参数占满显存（如 >80%），剩余空间不足以容纳大 batch，则收益受限 |
| **对长度均匀的工作负载增益较小** | 若所有请求长度相近，L4 不会引入额外开销，但仍无法进一步优化 |
| **暂未支持异构实例配置** | 当前评估基于同构 GPU 设置；异构环境下的最优 stage 划分仍是开放问题 |
| **KV 迁移带来通信开销** | 虽然设计上已最小化影响，但在极端带宽受限场景可能成为瓶颈 |

---

### **未来工作方向**

1. **支持异构集群调度**：结合 tensor/pipeline parallelism 参数自动搜索最优 stage 配置。
2. **更细粒度的长度感知调度**：例如结合输出长度预测，提前规划迁移路径。
3. **与 disaggregated serving 架构集成**：如 DistServe、MegaScale-Infer，实现 prefill/decode 分离 + 长度分区双重优化。
4. **探索强化学习用于动态边界调整**：替代当前启发式方法，实现更智能的 runtime 控制。

---

## 总结

L4 提出了一种全新的视角：**将 LLM 推理中的长度异质性问题从“本地调度挑战”转变为“全局资源编排机会”**。通过构建长度感知的多阶段流水线，配合动态规划、自适应边界调整和去中心化 bid-ask 协议，L4 在不改动底层 attention kernel 的前提下，实现了：

- **端到端延迟降低最多 67%**
- **尾延迟降低最多 69%**
- **系统吞吐提升最高达 2.89×**

该工作揭示了当前 LLM serving 架构的根本矛盾，并提供了可扩展、正交且高性能的解决方案，为下一代高效 LLM 服务平台的设计指明了方向。

</details>

---

### 8. [Rethinking Multi-Agent Intelligence Through the Lens of Small-World Networks](https://arxiv.org/abs/2512.18094)

**Authors**: Boxuan Wang, Zhuoyun Li, Xiaowei Huang, Yi Dong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.18094v1  

#### Abstract
Large language models (LLMs) have enabled multi-agent systems (MAS) in which multiple agents argue, critique, and coordinate to solve complex tasks, making communication topology a first-class design choice. Yet most existing LLM-based MAS either adopt fully connected graphs, simple sparse rings, or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rethinking Multi-Agent Intelligence Through the Lens of Small-World Networks

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **Large Language Models (LLMs)** 的 **Multi-Agent Systems (MAS)** 在通信拓扑设计上缺乏结构性指导。大多数系统采用全连接（fully-connected）、环状稀疏连接（ring/sparse）或动态选择机制，这些结构要么通信成本高，要么稳定性差，且未借鉴复杂网络中的经典理论。

本文提出：**是否可以将小世界（Small-World, SW）网络结构作为MAS的设计先验？**

### 🚀 提出的新方法与新思路
1. **引入 Small-World 网络作为通信拓扑先验**  
   受神经科学和复杂网络启发，作者将 SW 结构（高局部聚类 + 短平均路径长度）应用于多智能体系统的通信架构中，以提升推理的稳定性和效率。

2. **不确定性引导的重连机制（Uncertainty-Guided Rewiring）**  
   提出一种可扩展的动态构建 SW 拓扑的方法：利用 LLM 的语义不确定性信号（如 semantic entropy），在“认知差异大”的智能体之间添加长程“捷径”（shortcuts），从而形成可控、自适应的小世界结构。

3. **揭示 SW 拓扑对认知角色涌现的影响**  
   发现 SW 结构能自然诱导不同拓扑位置的 agent 表现出不同的行为模式（如 Expert、Bridge、Loner），无需显式角色设计即可实现认知多样性。

### 🔍 相比现有方法的优势
| 方面 | 传统方法 | 本文方法 |
|------|--------|---------|
| **通信效率** | 全连接开销大；稀疏环延迟高 | SW 平衡局部交互与全局传播，token 成本适中 |
| **共识稳定性** | 随机拓扑易震荡，收敛不稳定 | SW 显著平滑共识轨迹，降低方差 |
| **结构设计原则** | 缺乏理论依据或依赖经验 | 基于 SW 理论提供结构化先验 |
| **可扩展性** | 固定拓扑难以适应任务变化 | 不确定性驱动的动态重连支持自适应调整 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在 **Multi-Agent Debate (MAD)** 框架下评估，涵盖三个代表性基准：
- **ARC-Challenge**：多项选择题问答，测试常识与推理能力
- **ScienceQA**：多模态科学问题理解与推理
- **GSM8K**：数学应用题，要求链式思维（Chain-of-Thought）推理

### ⚙️ 实验设置
- **模型**：使用 `GPT-4o-mini` 作为基础 LLM
- **Agent 数量**：8 个 agents
- **通信拓扑对比**：
  - **Fully-connected**：全连接图
  - **Sparse (Ring)**：每个 agent 仅与邻居通信
  - **Random (Rand)**：每轮随机采样边（动态）
  - **Small-world (SW)**：从环形结构出发，通过少量边重连构造 SW 拓扑（静态）
- **评估指标**：
  - **准确率（Accuracy）**
  - **平均每题通信开销（Token cost per question）**
  - **共识动态曲线（Consensus dynamics over debate rounds）**

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Figure 2 & 3）

| 拓扑类型 | 准确率（平均） | Token 开销（相对） | 共识稳定性 |
|--------|----------------|--------------------|------------|
| **Random** | 最高 (~95.3%) | 最低 | 差（波动大、可能退化） |
| **Fully-connected** | 较高 (~92–93%) | 最高 | 中等 |
| **Sparse (Ring)** | 较低 (~84–87%) | 低 | 较慢但较稳 |
| **Small-world (SW)** | **接近最优 (~92–94%)** | **适中** | ✅ **最佳 — 收敛平稳、方差最小** |

> 💡 特别地，在 GSM8K 上，SW 拓扑展现出最平滑的共识演化过程（Figure 3），而 Random 拓扑出现明显振荡甚至后期性能下降。

### 🔬 消融分析与观察
- **SW 拓扑牺牲极小精度换取显著稳定性提升**：相比 Fully-connected，准确率几乎持平，但共识更可靠。
- **Random 拓扑虽高效但不稳定**：中间推理信号传播不规律，错误容易被放大。
- **SW 的“结构化捷径”有效抑制误差积累**：局部集群维持一致性，长程连接快速纠正偏差。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **SW 拓扑是多智能体协作的“黄金中庸”结构**  
   在准确性、通信成本与稳定性之间取得良好平衡，尤其适合需要长期推理的任务。

2. **SW 是推理稳定器（Reasoning Stabilizer）**  
   局部聚类增强一致性，长程捷径加速全局校正，显著减少共识过程中的震荡。

3. **SW 提升鲁棒性与可扩展性**  
   - 抵御局部误导信息（Robustness Enhancement）
   - 支持大规模系统中的高效协调（Scalable Coordinator）
   - 自然催生多样化认知角色（Inductive Bias for Cognitive Diversity）

4. **不确定性可作拓扑调控信号**  
   利用 semantic entropy 等 UQ 方法识别“最不确定”与“最自信”agent，并建立 shortcut，能主动优化信息流动，防止瓶颈（水桶效应）。

### ⚠️ 方法的局限性
- 当前实验基于 **静态 SW 拓扑**，尚未完全实现在线动态重构。
- 所有 agents 使用相同 LLM，未考虑异构 agent（heterogeneous agents）场景。
- 尚未端到端学习拓扑，仍依赖人工构造或启发式规则。

### 🔮 未来工作方向
1. **开发实时拓扑自适应算法**：结合 uncertainty signals 实现动态增删边。
2. **探索不同 UQ 指标的影响**：比较 epistemic vs. aleatoric uncertainty 对 shortcut 形成的作用。
3. **研究 SW 拓扑与任务难度的匹配机制**：自动调节 shortcut 密度。
4. **端到端学习 SW 结构**：让 agents 在训练中自主演化出最优通信图。
5. **推广至更复杂 MAS 场景**：如分布式规划、集体探索、分层组织等。

---

## 总结

> 该论文首次系统性地将 **Small-World Network** 理论引入 LLM-based MAS 设计，提出 **SW 拓扑是一种强有力的结构先验**，不仅能保持高性能，还能大幅提升推理过程的**稳定性与鲁棒性**。通过引入 **uncertainty-guided rewiring**，进一步实现了可控、可扩展的智能体通信架构，为下一代多智能体系统提供了新的设计范式。

</details>

---

### 9. [GeoSense-AI: Fast Location Inference from Crisis Microblogs](https://arxiv.org/abs/2512.18225)

**Authors**: Deepit Sapru  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.18225v1  

#### Abstract
This paper presents an applied AI pipeline for realtime geolocation from noisy microblog streams, unifying statistical hashtag segmentation, part-of-speech-driven proper-noun detection, dependency parsing around disaster lexicons, lightweight named-entity recognition, and gazetteer-grounded disambig...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GeoSense-AI: Fast Location Inference from Crisis Microblogs

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **地理元数据稀疏**：社交媒体（如 Twitter）在危机事件中是重要的实时信息源，但仅有极少数推文（如印度地区仅 0.36%）带有精确的 geo-tag（地理位置标签），严重限制了空间分析能力。
- **非正式文本处理困难**：微博客文本具有语法不规范、拼写变体多、缩略语频繁、多语言混杂等特点，通用 NER 工具难以有效识别其中的 location mentions（地名提及）。
- **实时性要求高**：应急响应场景对系统延迟极为敏感，传统高精度 NLP 模型往往计算开销大，无法满足流式处理需求。

### 🚀 提出的新方法与创新思路
- **提出 GeoSense-AI**：一个专为危机微博客设计的轻量级、低延迟 location inference 系统，直接从文本中提取地理位置，而非依赖稀疏的 geo-tags。
- **融合多阶段 NLP 技术的统一 pipeline**：
  - **Hashtag Segmentation**：使用统计方法拆分复合 hashtag（如 `#ChennaiFloods` → "Chennai" + "Floods"），提升召回率。
  - **POS-driven Proper-Noun Detection**：基于词性标注识别可能的地名实体。
  - **Dependency Parsing around Disaster Lexicons**：利用依存句法分析捕捉与灾害关键词（如 "flood", "earthquake"）语义相关的地点。
  - **Lightweight NER**：集成 spaCy 预训练 NER 模型作为“兜底”机制，兼顾速度与覆盖。
  - **Gazetteer-Grounded Disambiguation**：通过 GeoNames 或 OpenStreetMap 进行地名验证与坐标解析，过滤假阳性并解决歧义。

### 🔍 相比现有方法的优势
| 维度 | GeoSense-AI | 传统 NER 工具（如 StanfordNER） |
|------|-------------|-------------------------------|
| **准确性** | F1 达到 0.8141，优于多数基线 | 准确性尚可，但在非正式文本上表现下降 |
| **速度** | 处理每条 tweet 平均耗时约 1.19 秒（全集）| StanfordNER 耗时高达 175 秒，慢两个数量级 |
| **适用场景** | 专为 streaming crisis data 设计，支持实时部署 | 多数为批处理设计，不适合高吞吐流式输入 |
| **鲁棒性** | 对 hashtag、非标准语法、拼写变异更具容忍度 | 在 informal text 上召回率显著降低 |

> ⚡ 核心优势：**在保持高 F1 分数的同时，实现比主流工具快 ~150 倍的处理速度**，真正适用于 real-time crisis informatics。

---

## 2. 核心实验方法和设置

### 📊 数据集
- **原始数据**：通过 Twitter Streaming API 收集 2017 年 9 月 12 日至 10 月 13 日期间包含关键词 `"dengue"` 或 `"flood"` 的推文，共 317,567 条。
- **清洗后数据**：去除重复项和非英文推文，保留 239,276 条用于分析。
- **人工标注子集**：随机抽取 1,000 条推文，由人工标注是否包含明确的印度境内 location mention（需达到 city 级别或更细粒度）。
- **最终评估集**：从中筛选出 **99 条含有可验证地名的推文**，构成黄金标准测试集。

### 🧪 实验设置与评估指标
- **任务定义**：从推文中提取 location mention，并映射到实际地理坐标。
- **评估指标**：
  - **Precision**（精确率）
  - **Recall**（召回率）
  - **F1 Score**（F1 值）
  - **Processing Time per Tweet**（单条推文处理时间）——强调实时性
- **Gazetteer 统一**：所有方法均使用 GeoNames 进行地名验证，确保公平比较。

### 🆚 基线方法对比
| 方法 | 类型 |
|------|------|
| UniLoc | 单词 + gazetteer 匹配 |
| BiLoc | 双词组 + gazetteer 匹配 |
| StanfordNER | 通用 CRF-based NER |
| TwitterNLP | Twitter 特化 NER（Ritter et al.） |
| SpaCyNER | 轻量级预训练 NER |
| GoogleCloud | 商业 API（Google Cloud NLP） |
| GeoLoc / OSMLoc | GeoSense-AI 两种变体（分别使用 GeoNames 和 OpenStreetMap） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table II）

| Method | Precision | Recall | **F1** | Time (s) |
|--------|-----------|--------|--------|----------|
| UniLoc | 0.3848 | 0.7852 | 0.5165 | 0.0553 |
| BiLoc | 0.4025 | 0.8590 | 0.5482 | 0.0624 |
| StanfordNER | 0.8103 | 0.6322 | 0.6988 | **175.0124** |
| TwitterNLP | 0.6356 | 0.5474 | 0.5882 | 28.0001 |
| SpaCyNER | **0.9883** | 0.5555 | 0.7113 | 1.0891 |
| GoogleCloud | 0.6321 | 0.5339 | 0.5789 | N/A |
| **GeoLoc** | 0.7987 | **0.8300** | **0.8141** | **1.1901** |
| OSMLoc | 0.3383 | 0.8888 | 0.4901 | 711.5817 |

> ✅ **GeoLoc 在 F1 上显著领先于所有基线**（最高达 0.8141），且处理速度远超 StanfordNER（快约 150×）。

### 🔍 与其他方法对比的关键发现
- **vs. n-gram 方法（UniLoc/BiLoc）**：
  - 虽然 BiLoc 召回率较高，但 precision 很低；GeoLoc 同时实现了高 recall 和高 precision。
- **vs. SpaCyNER**：
  - SpaCyNER 精确率极高（0.9883），但 recall 极低（0.5555），漏检大量真实 location。
  - GeoSense-AI 通过 hashtag segmentation 和 dependency parsing 显著提升了 recall。
- **vs. StanfordNER**：
  - 尽管 F1 略低（0.6988 vs 0.8141），但 GeoSense-AI 快了近两个数量级，更适合 streaming 场景。
- **OSMLoc 的代价**：
  - 使用 OpenStreetMap 提升了 recall（0.8888），但由于数据过于精细导致 precision 下降严重（0.3383），且处理时间长达 711 秒，不适合实时应用。

### 🔤 消融实验结果（GeoLocNoNER）
- **方法**：关闭 lightweight NER 模块的 GeoSense-AI 版本。
- **结果**：
  - F1 从 0.8141 降至 0.7987
  - Precision 不变（0.7987），Recall 下降（0.7987）
- **结论**：lightweight NER 虽非核心组件，但仍贡献了一定 recall 提升，体现了其“安全网”作用。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **专用系统优于通用工具**：针对 crisis microblogs 设计的 GeoSense-AI 在 accuracy 和 speed 上均超越通用或 Twitter-specific NER 工具。
2. **多策略融合提升效果**：结合 hashtag segmentation、syntactic pattern matching、dependency parsing 和 gazetteer validation 的 pipeline 能有效应对非正式语言挑战。
3. **速度与精度可兼得**：通过选择轻量级组件（如 spaCy）、模块化设计和高效 gazetteer 查询，可在不牺牲太多精度的前提下大幅提升处理速度。
4. **生产可用性强**：系统已部署为在线服务（[http://savitr.herokuapp.com](http://savitr.herokuapp.com)），具备完整的 ingest → inference → visualization 流程，可用于 floods、outbreaks 等事件的 situational awareness。

### ⚠️ 局限性
- **语言限制**：目前仅支持英文推文，未处理 code-mixed（如印地语+英语）或多语言内容。
- **地名覆盖不足**：部分地方性名称或非正式称呼不在 gazetteer 中，导致 false negative。
- **拼写变异容忍有限**：过度创意拼写可能超出 fuzzy matching 能力。
- **歧义问题**：全球存在大量同名地点（如 "Springfield"），当前缺乏上下文 disambiguation 机制（如用户 profile、社交网络等）。

### 🔮 未来工作方向
- **Multilingual & Code-Mixed Support**：
  - 引入 language identification 和 transliteration 技术处理混合语言文本。
- **Global Scale Deployment**：
  - 构建 hierarchical gazetteer 或使用 LSH（Locality-Sensitive Hashing）加速大规模地名匹配。
  - 结合 user profile location、event prior 等辅助信息进行 disambiguation。
- **功能扩展**：
  - **Event Detection**：基于 location + keyword 频率异常检测新兴危机。
  - **Information Classification**：将推文分类为 damage report、resource need 等类型。
  - **Summarization & Duplicate Detection**：缓解信息过载。
  - **Credibility Assessment**：识别可信信源，对抗 misinformation。

---

## 总结
GeoSense-AI 成功展示了如何通过 **domain-tuned NLP pipeline + knowledge grounding** 克服社交媒体中 geo-tag 稀缺问题，在保证高准确率的同时实现极低延迟，为 real-time crisis response 提供了可行的技术路径。其核心思想——**以工程效率驱动 AI 应用落地**——对其他紧急响应系统具有重要借鉴意义。

</details>

---

### 10. [Faster Distributed Inference-Only Recommender Systems via Bounded Lag Synchronous Collectives](https://arxiv.org/abs/2512.19342)

**Authors**: Kiril Dichev, Filip Pawlowski, Albert-Jan Yzelman  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.19342v1  

#### Abstract
Recommender systems are enablers of personalized content delivery, and therefore revenue, for many large companies. In the last decade, deep learning recommender models (DLRMs) are the de-facto standard in this field. The main bottleneck in DLRM inference is the lookup of sparse features across huge...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Faster Distributed Inference-Only Recommender Systems via Bounded Lag Synchronous Collectives

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在分布式深度学习推荐模型（DLRM）推理过程中，**`alltoallv` 集合通信操作**是主要瓶颈。该操作用于跨多个节点分片的 embedding 表进行稀疏特征查找，具有高度不规则性和通信密集性。传统实现采用**全同步（synchronous）集体通信机制**，导致所有进程必须严格同步，任何单个“straggler”（延迟进程）都会拖慢整个系统。

这种强同步性在存在计算、内存或网络负载不平衡时严重限制了系统的吞吐量和延迟表现。

---

### 🚀 提出的新方法：Bounded Lag Synchronous (BLS) Alltoallv

作者提出了一种新型的 **Bounded Lag Synchronous (BLS) alltoallv** 集体通信原语，并基于此构建了一个新的 PyTorch Distributed 后端和相应的 BLS-enabled DLRM 实现。

#### 核心思想：
- 允许不同进程之间的执行进度相差最多 `k` 个迭代（即允许“有界滞后”）。
- 最快的进程可以继续向前推进，而无需等待最慢的进程完成当前迭代。
- 当滞后超过 `k` 时才强制阻塞，从而打破传统 collective 的全局同步屏障。

#### 技术实现亮点：
- 设计并实现了支持 BLS 的 **轻量级 PyTorch Distributed 后端**，基于 RDMA 和 one-sided communication（如 put 操作），避免依赖传统的两方通信（send/recv）带来的额外同步开销。
- 利用 **tag 机制**区分不同迭代的数据传输，确保消息不会混淆。
- 引入 **环形缓冲区（circular buffering）** 来存储多轮迭代的中间结果，以支持异步通信与重叠执行。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法（MPI/NCCL） | 本文 BLS 方法 |
|------|------------------------|---------------|
| 同步模式 | 完全同步（barrier-style） | 有界异步（允许最多 `k` 轮滞后） |
| 通信方式 | 多数为 two-sided（send/recv） | one-sided RDMA（非阻塞 offload 到 NIC） |
| 执行重叠能力 | 仅限同迭代内 compute 与 communication 重叠 | 支持跨迭代的 compute-compute、compute-communication、甚至 communication-communication 重叠 |
| 对不均衡容忍度 | 差（straggler 敏感） | 显著提升，在 `k` 范围内可完全掩盖延迟 |

> 💡 特别适用于 **inference-only DLRM** 场景，因为模型权重已固定，无需保证各进程处于同一训练步，因此放松同步约束不会影响准确性。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 类型 | 描述 |
|-------|------|------|
| **Mini-Kaggle (Criteo)** | 真实数据集 | 广告点击预测数据，包含 13 个 dense 字段和 26 个 sparse 字段，对应 26 个 embedding tables |
| **Ali-CCP (Alibaba Click & Conversion Prediction)** | 真实数据集 | 用户点击与转化行为日志，经预处理转换为类似 Criteo 格式，含 23 个 categorical tables |
| **Synthetic Heterogeneous Size Benchmark** | 合成数据 | 随机生成 embedding 查找请求，每轮访问向量数量从 1 到 100 不等，模拟不均匀通信负载 |
| **Random Delay Injection Benchmark** | 合成数据 | 在每个推理步骤中注入 0–10ms 的随机延迟，模拟 CPU、内存或通信瓶颈 |

> ⚠️ 注意：两个真实数据集的 embedding 表较小（最大约 2M 条目），且访问模式较均匀，难以体现通信不均衡优势。

---

### ⚙️ 实验设置

- **硬件平台**：8 节点 ARM Kunpeng 920 集群，每节点 1 MPI 进程
- **网络**：Mellanox ConnectX-5 + EDR InfiniBand（100 Gbps），支持 RDMA
- **软件栈**：
  - PyTorch 2.5.1 + MPI backend（Open MPI 变种 Hyper MPI）
  - 自研 BLS backend（基于 LPF 和 RDMA）
  - DLRM 基准代码（Naumov et al. [1]）基础上扩展
- **参数配置**：
  - Batch size: 512
  - Embedding dimension: 64
  - Bound `k`: 0~400（测试不同滞后容忍度）

---

### 📊 评估指标

| 指标 | 定义 |
|------|------|
| **Latency per batch** | 单个 batch 推理耗时（秒），越低越好 |
| **Throughput (batches/sec)** | 单位时间内处理的 batch 数量，越高越好 |
| **95% 置信区间（CI）** | 多次重复实验下的稳定性评估 |

> 所有指标均在多个进程和多次运行上取平均值。

---

### 🆚 基线方法对比

| 方法 | 描述 |
|------|------|
| **Original DLRM + MPI backend (k=0)** | 原始实现，使用标准 PyTorch MPI backend，完全同步 |
| **BLS-enabled DLRM + MPI backend (k>0)** | 修改后的 DLRM 支持非阻塞调用，但仍走 MPI backend |
| **BLS-enabled DLRM + BLS backend (k>0)** | 完整方案：自定义后端 + BLS collectives |

---

## 3. 主要实验结果和性能指标

### ✅ 合成基准测试结果

#### （1）异构消息大小（Heterogeneous Message Sizes）

- **观察**：随着 `k` 增大，**只有 BLS backend + BLS DLRM 方案表现出持续改进**
- **性能增益**：
  - 最高降低 **~7% 延迟**
  - 提升 **~6% 吞吐量**
- **原因分析**：
  - BLS backend 支持真正的跨迭代 communication 重叠（见 Table I）
  - MPI backend 存在通信进度线程瓶颈，无法有效利用非阻塞接口

#### （2）随机延迟注入（Random Delays: 0–10ms）

- **惊人发现**：
  - 当引入随机延迟时，**BLS 方法几乎完全掩盖了这些延迟的影响！**
  - 平均延迟从 0.017s 下降到 0.012s —— 正好等于 **延迟均值（5ms）被成功隐藏**
- **性能趋势**：
  - 对于 `k ≥ 50`，性能迅速收敛到最优水平
  - **无论是 MPI 还是 BLS backend，只要 DLRM 支持 BLS 逻辑，都能受益**

> 📌 结论：即使底层通信仍用 MPI，只要应用层允许滞后，就能显著缓解不均衡问题。

---

### ❌ 真实数据集结果（Mini-Kaggle & Ali-CCP）

- **关键现象**：增加 `k` **未带来明显性能提升**
- **可能原因**：
  - 数据访问高度均匀（NVTabular 预处理会聚合多向量为单向量）
  - 计算、通信负载天然平衡
  - embedding 表规模小，通信占比低（仅 ~5%，见图5火焰图）

> 🔍 图5显示：主要开销在 `apply_emb` 和 `apply_mlp`，而非通信本身。

---

### 🔬 消融实验与底层性能分析

#### BLS alltoallv vs MPI_Alltoallv（纯通信测试）

| 场景 | 结果 |
|------|------|
| 小消息（<4KB） | MPI 更优（因其能合并 IB 请求） |
| 大消息（>4KB） | BLS 更优（one-sided 无协议切换开销） |
| 多次连续调用 | MPI 更稳定；BLS 因频繁 polling 开销略差 |

> ✅ 尽管 BLS alltoallv 在某些场景下不如 MPI，但在实际 DLRM 推理中频率较低，不影响整体收益。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **BLS 是一种有效的去同步化策略**：
   - 在存在 **通信/计算不均衡** 的场景下，BLS 可显著改善 DLRM 推理的延迟和吞吐。
   - 在理想条件下（如随机延迟），**可完全掩盖 straggler 影响**。

2. **通信重叠能力逐级增强**：
   - 如 Table I 所示，BLS backend 提供最强的异步支持，允许任意阶段跨迭代重叠。

3. **真实世界负载需更复杂建模**：
   - 当前主流公开数据集（如 Mini-Kaggle、Ali-CCP）负载过于均衡，**无法充分体现 BLS 价值**。
   - 实际生产环境中可能存在更大规模、更高偏斜的访问模式。

4. **内存开销可控**：
   - 每增加一个 `k`，每个进程额外消耗约 **860 KB 内存**（与 embedding table 大小无关）
   - 对现代系统而言属于可接受范围。

---

### ⚠️ 局限性

| 限制 | 说明 |
|------|------|
| **仅适用于 inference-only 场景** | 因为训练需要严格同步梯度更新，不能容忍参数陈旧 |
| **依赖 RDMA 硬件** | 当前实现绑定 InfiniBand/RDMA，通用性受限 |
| **对完全平衡负载无效** | 若系统本就无 straggler，则 BLS 无收益 |
| **polling 开销较高** | 当前版本未优化 IB 请求批处理，影响高频通信性能 |

---

### 🔮 未来工作方向

1. **扩展至其他 collective 操作**：将 BLS 思想应用于 `allreduce`、`broadcast` 等。
2. **支持更多硬件平台**：适配 RoCE、CXL 或 PCIe P2P 等新型互连技术。
3. **动态调整 bound `k`**：根据运行时负载自动调节滞后窗口大小。
4. **结合 caching/sharding 策略**：与 TorchRec、Merlin 中的缓存机制协同优化。
5. **构建更具挑战性的 benchmark**：设计反映真实线上流量偏斜的大规模合成 workload。

---

## 总结

📌 **一句话总结**：  
本文提出了 **Bounded Lag Synchronous (BLS)** 新范式，通过放松 DLRM 推理中的同步约束，在存在负载不均衡时显著提升了分布式推理系统的性能，且不影响模型精度。

🎯 **适用场景**：  
适合部署在存在硬件差异、资源争抢或访问偏斜严重的生产级推荐系统中，作为现有通信栈的有效补充。

🔧 **工程意义重大**：  
提供了一个可在不改变算法的前提下，通过系统级优化透明加速推理的新路径。

</details>

---

### 11. [MoE-TransMov: A Transformer-based Model for Next POI Prediction in Familiar & Unfamiliar Movements](https://arxiv.org/abs/2512.17985)

**Authors**: Ruichen Tan, Jiawei Xue, Kota Tsubouchi, Takahiro Yabe, Satish V. Ukkusuri  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.17985v1  

#### Abstract
Accurate prediction of the next point of interest (POI) within human mobility trajectories is essential for location-based services, as it enables more timely and personalized recommendations. In particular, with the rise of these approaches, studies have shown that users exhibit different POI choic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-TransMov: A Transformer-based Model for Next POI Prediction in Familiar & Unfamiliar Movements  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有 Next POI Prediction 模型通常将所有用户移动行为统一建模，忽略了用户在 **familiar（熟悉）区域** 和 **unfamiliar（不熟悉）区域** 中表现出显著不同的移动模式（如 POI 类型偏好、轨迹选择等）。这种“一刀切”的建模方式容易导致 **negative transfer**（负迁移），即模型在一种情境下学到的知识干扰另一种情境下的预测。

### 🚀 提出的新方法：MoE-TransMov
提出了一种基于 **Transformer** 与 **Mixture-of-Experts (MoE)** 架构融合的新型模型 —— **MoE-TransMov**，用于同时捕捉用户在熟悉与不熟悉环境中的异质性移动行为。

#### 创新设计：
- **共享 Transformer Encoder**：提取全局用户移动序列的通用特征。
- **双专家结构（Two Experts）**：
  - **Transformer-based Expert**：擅长建模长距离依赖，适用于熟悉区域中规律性强、序列较长的行为。
  - **LSTM-based Expert**：更适合处理稀疏、短序列，适用于不熟悉区域中探索性强、数据稀疏的行为。
- **可学习的 Gating Network**：动态地根据输入序列决定激活哪个专家，无需显式标注“familiar/unfamiliar”标签，实现端到端自适应路由。

### 🔍 相比现有方法的优势
| 优势维度 | 具体体现 |
|--------|---------|
| **上下文感知能力更强** | 显式区分 familiar/unfamiliar 移动场景，避免行为混淆 |
| **模型灵活性更高** | MoE 结构允许不同专家专注不同行为模式，提升表达能力 |
| **计算效率可控** | 仅激活部分专家，保持高容量的同时控制计算开销 |
| **泛化能力强** | 在多个真实城市数据集上验证有效，尤其在复杂、多样化的现实场景中表现优异 |

---

## 2. 核心实验方法和设置

### 📊 数据集
| 数据集 | 描述 |
|-------|------|
| **Foursquare NYC Dataset** | 开源小规模基准数据集，常用于 POI 推荐研究，包含纽约市用户的签到记录。作为标准对比环境。 |
| **LY Corporation Kyoto Dataset** | 自研大规模真实世界数据集，由日本 LY Corporation（Yahoo Japan）提供，涵盖京都市约 8 个月的匿名用户签到数据，包含超过百万条时间序列。相比 Foursquare 更大、更密集、更具现实多样性。 |

> ⚠️ 注：两个数据集均经过预处理，过滤掉会话少于 10 条、用户少于 10 个会话的数据。

### 🧪 实验设置
- **训练/验证划分**：按轨迹级别划分为 80%/20%
- **模型结构参数**：
  - Shared Transformer Encoder：4 层，8 头注意力，隐藏层维度 128
  - Experts：
    - Transformer Expert：同上
    - LSTM Expert：2 层，隐藏大小 64
  - 序列长度：训练时最大 50，支持最长 500 的位置编码
- **优化器**：Adam，学习率 0.0005，batch size 64
- **早停机制**：连续 3 轮验证损失上升则停止训练

### 📈 评估指标
- **Top-k Accuracy (k=1,5,10)**：预测列表前 k 个是否包含真实下一个 POI
- **Mean Reciprocal Rank (MRR)**：衡量正确 POI 在排序列表中的平均排名质量，越早出现得分越高

$$
\text{MRR} = \frac{1}{|Q|} \sum_{i=1}^{|Q|} \frac{1}{\text{rank}_i}
$$

### 🔁 基线方法对比
| 模型 | 类型 | 特点 |
|------|------|------|
| Majority Vote | 统计基线 | 预测历史最频繁访问的 POI |
| MLP | 浅层神经网络 | 学习非线性关系 |
| LSTM | RNN 模型 | 捕捉长期依赖 |
| DeepMove | Attention + RNN | 引入注意力机制 |
| STAN | Spatio-Temporal Attention | 同时关注时空上下文 |
| GETNext | Graph-based | 构建轨迹流图进行推荐 |
| Transformer | Self-Attention | 并行处理序列，捕获全局依赖 |
| Informer | Efficient Transformer | 使用 ProbSparse 注意力，适合长序列 |

---

## 3. 主要实验结果和性能指标

### 📊 性能对比（关键数据摘要）

#### ✅ 在 **Foursquare NYC Dataset** 上的结果（Table 3）
| 方法 | Familiar Top-1 | Unfamiliar Top-1 | Total MRR |
|------|----------------|------------------|-----------|
| Majority Vote | **0.2248** | 0.0056 | 0.1906 |
| MoE-TransMov | 0.2111 | **0.1164** | **0.2639** |

> 💡 观察：虽然 Majority Vote 在 familiar 场景下因“重复访问”现象占优，但在 **unfamiliar 场景几乎失效**；而 MoE-TransMov 在 **unfamiliar 和 total 场景全面领先**，说明其对稀疏、新颖行为有更强建模能力。

#### ✅ 在 **LY Corporation Kyoto Dataset** 上的结果（Table 4）
| 方法 | Familiar MRR | Unfamiliar Top-1 | Total MRR |
|------|--------------|------------------|-----------|
| STAN | 0.1668 | 0.0797 | 0.1522 |
| Transformer | 0.1770 | 0.0895 | 0.1754 |
| MoE-TransMov | **0.1826** | **0.1179** | **0.1946** |

> ✅ MoE-TransMov 在 **所有子集（familiar, unfamiliar, total）几乎所有指标上均达到 SOTA**，特别是在 **unfamiliar 场景 Top-1 提升显著（+32% vs Transformer）**，证明其对陌生区域的强大泛化能力。

### 🔍 消融实验结果（Ablation Study, Table 5）

| 变体 | Unfamiliar MRR | Total MRR |
|------|----------------|-----------|
| Full MoE-TransMov | **0.2072** | **0.1946** |
| w/o MoE（仅共享 Transformer） | 0.1737 | 0.1754 |
| w/o Transformer（无共享编码器） | 0.1748 | 0.1617 |
| MoE(2 LSTMs) | 0.2005 | 0.1903 |
| MoE(2 Transformers) | 0.1913 | 0.1879 |

#### 消融分析结论：
1. **移除 MoE 模块 → 性能下降明显**，尤其在 unfamiliar 场景，说明专家分工至关重要；
2. **移除共享 Transformer → 性能大幅退化**，表明全局上下文建模不可或缺；
3. **双 LSTM 或双 Transformer 专家不如混合架构**，说明 **Transformer + LSTM 的异构专家组合最优**，分别适配长短序列行为；
4. **端到端软路由优于硬切换**，体现 MoE 动态适应性的优势。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **用户在 familiar 和 unfamiliar 区域的行为差异显著**，必须区别对待才能提升预测精度；
2. **MoE 架构能有效解耦异质行为模式**，通过专家专业化避免 negative transfer；
3. **MoE-TransMov 在真实大规模数据集（Kyoto）上表现尤为突出**，说明其在复杂、多样化现实场景中具有强鲁棒性和可扩展性；
4. **共享编码 + 专用专家 + 动态门控** 的设计是成功的关键，实现了“统一框架、差异化建模”。

### ⚠️ 方法的局限性
- 当前仅考虑空间移动上下文，未引入天气、节假日、社交信号等外部因素；
- 专家数量固定为 2，难以应对更多细分移动模式（如通勤、旅游、紧急出行）；
- 虽然 MoE 提高了效率，但整体模型仍较复杂，部署成本较高；
- gating network 完全依赖历史轨迹学习，缺乏可解释性。

### 🔮 未来工作方向（作者建议）
1. **引入多模态上下文**：融合天气、交通、用户搜索意图等辅助信息；
2. **扩展专家数量与层次化 gating**：构建更细粒度的行为分类体系；
3. **跨城迁移学习（Cross-city Transfer）**：探索模型在不同城市间的泛化能力；
4. **集成社会信号与群体行为**：利用 co-location、好友影响等增强个性化推荐；
5. **轻量化与边缘部署优化**：推动 MoE 架构在移动端的实际应用。

---

## ✅ 总结
**MoE-TransMov** 是首个将 **Mixture-of-Experts** 机制系统应用于 **Next POI Prediction** 并显式区分 **familiar/unfamiliar 移动场景** 的 Transformer 模型。它通过 **共享编码器 + 异构专家 + 动态门控** 的设计，在多个真实数据集上实现了 SOTA 表现，尤其是在处理稀疏、不可预测的 unfamiliar 行为方面展现出强大优势。该工作不仅提升了 POI 推荐系统的个性化水平，也为复杂人类移动行为建模提供了新的范式。

</details>

---

### 12. [Population-Evolve: a Parallel Sampling and Evolutionary Method for LLM Math Reasoning](https://arxiv.org/abs/2512.19081)

**Authors**: Yanzhi Zhang, Yitong Duan, Zhaoxi Zhang, Jiyan He, Shuxin Zheng  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19081v1  

#### Abstract
Test-time scaling has emerged as a promising direction for enhancing the reasoning capabilities of Large Language Models in last few years. In this work, we propose Population-Evolve, a training-free method inspired by Genetic Algorithms to optimize LLM reasoning. Our approach maintains a dynamic po...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Population-Evolve: a Parallel Sampling and Evolutionary Method for LLM Math Reasoning*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前用于提升大语言模型（LLM）数学推理能力的方法主要依赖于**后训练技术**（如 SFT、RLHF、RLVR），这些方法存在以下问题：
- **计算成本高**，需要大量高质量标注数据；
- 数据稀缺导致边际收益递减。

因此，研究者转向**test-time scaling**（测试时扩展）策略，即在不进行额外训练的前提下，通过推理过程中的优化来提升性能。然而，现有 test-time 方法也面临挑战：
- **DSER** 等序列自演化方法存在**高时间开销和性能方差大**的问题；
- **GenSelect** 虽然稳定，但缺乏迭代优化机制，无法充分利用 LLM 的生成-验证闭环。

---

### 🚀 提出的新方法：Population-Evolve

作者提出了一种全新的**无需训练**的并行采样与进化式推理框架 —— **Population-Evolve**，其灵感来源于遗传算法（Genetic Algorithm, GA）。

#### 核心思想：
- 维护一个候选解的动态“种群” $ G^{(i)} $，每轮通过 LLM 自我演化生成新一代；
- 使用统一的 **evolve prompt** 引导模型基于前一代所有解生成更优的新解；
- 最终通过 **majority voting** 得到最终答案。

#### 创新点：
1. **并行 + 迭代结合**：不同于 DSER 的串行迭代，Population-Evolve 在每轮中并行演化整个种群，显著提高效率。
2. **自我演化的闭环机制**：利用 LLM 对多个候选解的理解与综合能力，实现“群体智能”式的渐进优化。
3. **稳定性强**：随着迭代进行，种群趋于收敛，性能波动小。

---

### 🔍 相比现有方法的优势
| 方法 | 并行性 | 是否迭代 | 效率 | 稳定性 | 性能上限 |
|------|--------|----------|-------|---------|-----------|
| **DSER** | ❌ 单路径 | ✅ 深度迭代 | 低（两阶段串行） | 差（方差大） | 高但不稳定 |
| **GenSelect** | ✅ 多样本 | ❌ 无迭代 | 高 | 好 | 受限于初始分布 |
| **Population-Evolve (ours)** | ✅ 种群并行 | ✅ 迭代演化 | **高**（单步生成） | **极好**（快速收敛） | **最高** |

> ✅ 关键优势：**在保持高效率的同时实现了高性能与低方差的统一**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
三个具有挑战性的数学竞赛基准：
- **HMMT24-25**：60道题，最具难度（Harvard-MIT Mathematics Tournament）
- **AIME25**：30道题，中等难度
- **BRUMO25**：30道题，相对简单

> 所有数据均来自 HuggingFace 上的 `MathArena` 或作者收集，确保未被污染。

---

### ⚙️ 实验设置
- **模型**：
  - 开源模型：`Qwen3-4B-Thinking-2507`, `DeepSeek-R1-0528-Distill-Qwen3-8B`, `OpenReasoning-Nemotron-7B`
  - 顶级闭源模型对比：`Gemini-2.5-Pro`, `GPT-5.1`（通过 OpenRouter API 调用）
- **温度**：0.6（平衡多样性与准确性）
- **解析工具**：使用 `Math-Verify` 库自动提取 `\boxed{}` 中的答案

---

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **maj@k** | Majority Voting over k 个独立 rollout（如 maj@64） |
| **avg@n** | n 次独立运行的平均准确率（用于高方差方法如 DSER） |
| **pass@k** | Oracle 上界，表示从 k 个样本中至少有一个正确的概率 |
| **Std Dev** | 衡量方法稳定性（越低越好） |

---

### 🆚 基线方法对比
在统一框架下比较以下方法：
1. **DSER**（Deep Self-Evolving Reasoning）：单路径验证-修正循环
2. **GenSelect**：并行生成 + LLM 作为判别器选择最优解
3. **Majority Voting (Self-Consistency)**：基础 baseline
4. **Pass@16**：理论性能上界

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）

#### 在 **HMMT24-25**（最难）上的表现：
| Model | Our Method | GenSelect | DSER | Maj@64 |
|-------|------------|-----------|------|--------|
| Qwen3-4B | **64.17%** | 63.75% | 61.46% | 58.33% |
| DeepSeek-Distill-8B | **70.83%** | 68.75% | 65.31% | 65.00% |
| Nemotron-7B | **82.08%** | 78.75% | 77.08% | 70.00% |

> ✅ 全面超越所有 baseline，尤其在困难任务上优势明显

#### 在 **AIME25** 上的表现：
| Model | Our Method | GenSelect | DSER | Maj@64 |
|-------|------------|-----------|------|--------|
| Qwen3-4B | **90.00%** | 89.17% | 85.42% | 86.67% |
| DeepSeek-Distill-8B | **88.33%** | 85.00% | 85.42% | 83.33% |
| Nemotron-7B | **93.33%** | 91.67% | 91.88% | 86.67% |

> ✅ 即使在较易任务上仍保持领先

#### 在 **BRUMO25** 上的表现：
| Model | Our Method | GenSelect | DSER | Maj@64 |
|-------|------------|-----------|------|--------|
| Qwen3-4B | **93.33%** | 88.33% | 85.62% | 86.67% |
| DeepSeek-Distill-8B | **93.33%** | 90.00% | 88.96% | 90.00% |
| Nemotron-7B | **95.83%** | 94.17% | 94.79% | 93.33% |

> ✅ 小幅领先，说明方法泛化性强

---

### 📊 性能增益分析（相比 maj@64）
| Benchmark | 平均提升幅度 |
|----------|----------------|
| HMMT24-25（最难） | **+7.92%** |
| AIME25（中等） | +5.00% |
| BRUMO25（最易） | +4.17% |

> 💡 发现：**问题越难，Population-Evolve 的相对增益越大**，表明其在复杂推理任务中更具潜力。

---

### 🔍 消融实验（Ablation Study）

#### 实验设计：
- 固定模型：`DeepSeek-R1-0528-Qwen3-8B` 和 `Qwen3-4B-Thinking-2507`
- 测试不同 population size $ P = 4, 8, 16 $
- 在 HMMT24-25 上观察 accuracy 随迭代的变化

#### 结果（图5）：
- **population size 越大，性能越好**
- $ P=16 $ 明显优于 $ P=8 $ 和 $ P=4 $
- 支持假设：更大的种群带来更多多样性，有助于引导更高质量的演化

> ✅ 验证了“种群多样性促进进化”的核心机制

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Population-Evolve 是一种高效、稳定且强大的 test-time scaling 方法**：
   - 在多个数学推理 benchmark 上 consistently 超越 DSER、GenSelect 和 Majority Voting。
   - 特别是在高难度任务（如 HMMT）上表现突出。

2. **统一视角：将 test-time scaling 视为遗传算法过程**：
   - 提出通用框架 $ M=(P,T,F_\phi,S) $，涵盖 population、iteration、evolution operator、selection。
   - 成功解释 DSER、GenSelect、Population-Evolve 等方法的本质差异。

3. **并行演化显著优于串行迭代**：
   - DSER 存在严重的时间开销和性能方差（std dev 达 4.23%）；
   - Population-Evolve 收敛快（通常 4–8 轮）、方差小、token 消耗少。

4. **test-time scaling 潜力巨大**：
   - 即使是较小的开源模型（如 4B–8B），经 Population-Evolve 优化后，性能可媲美甚至超过顶级闭源模型（如 GPT-5.1、Gemini-2.5-Pro）的 pass@1。

---

### ⚠️ 局限性
1. **依赖高质量 prompt 设计**：evolve prompt 的有效性直接影响演化质量。
2. **上下文长度限制**：当 population size 较大时，需将历史解压缩处理（去除 `<think>` 内容），可能丢失部分中间逻辑。
3. **尚未端到端训练适配该范式**：目前仍是推理阶段的“外挂”方法。

---

### 🔮 未来工作方向
1. **引入强化学习**：训练专门适配 Population-Evolve 范式的 LLM，使其更擅长“种群演化”。
2. **集成 inference-time communication 机制**：借鉴 Gemini 的 DeepThink 架构，增强跨解的信息交互。
3. **扩展至其他推理任务**：如形式化证明、代码生成、科学发现等。

---

## 总结

> **Population-Evolve 成功地将遗传算法思想引入 LLM 推理优化，构建了一个高效、稳定、可扩展的 test-time scaling 新范式。它不仅在数学推理任务上取得 SOTA 表现，更为未来“无需训练即可提升模型能力”的研究提供了重要思路。**

</details>

---

### 13. [Helios: A Foundational Language Model for Smart Energy Knowledge Reasoning and Application](https://arxiv.org/abs/2512.19299)

**Authors**: Haoyu Jiang, Fanjie Zeng, Boan Qu, Xiaojie Lin, Wei Zhong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19299v1  

#### Abstract
In the global drive toward carbon neutrality, deeply coordinated smart energy systems underpin industrial transformation. However, the interdisciplinary, fragmented, and fast-evolving expertise in this domain prevents general-purpose LLMs, which lack domain knowledge and physical-constraint awarenes...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Helios: A Foundational Language Model for Smart Energy Knowledge Reasoning and Application》核心总结

---

## 1. 主要贡献和创新点

### 解决的问题
当前通用大语言模型（LLMs）在智能能源（smart energy）领域应用中面临以下挑战：
- 缺乏领域专业知识（domain knowledge），导致推理结果语义合理但物理上无效（physically invalid）；
- 领域能源知识高度跨学科、碎片化且快速演进，通用LLMs难以准确建模；
- 现有研究多依赖提示工程（prompt engineering）或微调已有模型，未系统性注入领域知识；
- 缺乏高质量、标准化的训练与评估数据集。

### 提出的新方法与创新
本文提出 **Helios**，首个专为智能能源领域设计的**基础大语言模型**（foundational LLM），并配套构建完整资源体系：

#### 核心创新点：
1. **Helios 模型**  
   - 首个面向智能能源领域的开源基础LLM，支持从知识推理到复杂建模任务的广泛应用场景；
   - 输出风格与专业语境深度一致，具备物理约束意识。

2. **EnerSys：端到端多智能体协同数据构建框架**  
   - 自动化生成高质量预训练、指令微调和RLHF数据；
   - 包含三大组件：
     - **EnerBase**：首个智能能源知识库（30亿tokens）；
     - **EnerInstruct**：首个领域专用指令微调数据集；
     - **EnerReinforce**：首个基于人类反馈强化学习（RLHF）的数据集。

3. **EnerBench：首个智能能源领域综合评测基准**  
   - 包含 **976道客观题**（选择、判断等）和 **625道主观题**（问答、解释、建模优化等）；
   - 双轨制构建：公共题库检索 + 专家定向设计，确保覆盖广度与难度。

4. **三阶段训练范式**  
   - 大规模预训练 → 指令微调（SFT） → 强化学习对齐（RLHF），实现知识积累、任务适配与人类偏好对齐。

### 相比现有方法的优势
| 维度 | 传统方法 | Helios |
|------|--------|--------|
| 知识来源 | 依赖通用语料，缺乏领域深度 | 显式注入智能能源专业知识 |
| 数据质量 | 多由大模型生成，风格偏离人类偏好 | 多智能体协作+人工验证，保障准确性与实用性 |
| 评估体系 | 缺乏统一标准 | 发布首个领域Benchmark（EnerBench） |
| 模型能力 | 应用导向（apply prior）而非知识积累 | 全流程训练，真正成为“领域基础模型” |

---

## 2. 核心实验方法和设置

### 使用的数据集

| 数据集 | 类型 | 规模 | 内容说明 |
|-------|------|-----|---------|
| **EnerBase** | 预训练语料 | ~3B tokens | 来自arXiv、Web of Science、GitHub、IEA等平台的科学文献、代码、数据集 |
| **EnerInstruct** | 指令微调数据 | 54,027条清洗后样本 | 覆盖14个子领域、11类任务（如Q&A、建模、分类等） |
| **EnerReinforce** | RLHF数据 | 5万主观问题 + 10万候选答案 | 用于奖励模型训练与拒绝采样微调 |
| **EnerBench** | 评测基准 | 1,601题（976客观 + 625主观） | 多维度评估LLMs在智能能源场景下的表现 |

### 实验设置
- **基础模型**：Qwen2.5-7B（7.62B参数）
- **训练三阶段**：
  1. **Pre-training**：在EnerBase上单轮训练（22,532步），使用4×A100 GPU，耗时87小时；
  2. **Instruction Tuning**：采用LoRA进行两阶段微调（通用指令 → 领域任务），使用4×RTX 4090，耗时17小时；
  3. **RLHF**：
     - 奖励模型训练：基于对比排序损失（pairwise ranking loss）；
     - 拒绝采样微调（Rejection Sampling）：选取Top-k高分响应作为“黄金标准”继续训练。

### 评估指标
#### 客观任务（Objective Tasks）
- 准确率（Accuracy）：完全正确才得分，部分正确给分，错选不得分。

#### 主观任务（Subjective Tasks）
采用三维评估框架：
- **A-Score**：基于GPT-4o的相对比较评分（10分制）；
- **E-Score**：GPT-4o独立打分（10分制）；
- **H-Grade**：领域专家人工评级（A/B/C/D等级）。

### 基线方法对比
参与对比的模型包括：
- 开源模型：`Qwen3-8B-Instruct`, `Llama3-8B-Instruct`, `Qwen3-14B-Instruct`, `Qwen3-32B-Instruct`
- 商业闭源模型：`GPT-3.5-Turbo`, `GPT-4`

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 3）

| 模型 | S-C (%) | M-C (%) | FC (%) | ESM (A/E/H) | Exp (A/E/H) | Q&A (A/E/H) |
|------|--------|--------|--------|------------|-------------|-------------|
| Qwen3-8B-Instruct | 50.24 | 27.56 | 47.81 | 1.74 / 5.88 / D | 1.63 / 5.04 / — | 4.74 / 6.50 / C |
| Llama3-8B-Instruct | 68.42 | 37.60 | 58.77 | 3.29 / 6.03 / D | 3.53 / 6.29 / D | 5.13 / 6.47 / C |
| Qwen3-32B-Instruct | 80.14 | 44.09 | 62.72 | 3.82 / 6.93 / D | 5.51 / 7.32 / C | 6.83 / 7.51 / B |
| GPT-3.5-Turbo | 91.63 | 53.93 | 84.65 | 6.03 / 8.05 / C | 6.94 / 8.57 / B | 7.24 / 8.37 / B |
| GPT-4 | **95.69** | **61.18** | **93.86** | 7.61 / 8.97 / — | 8.63 / 9.58 / B | 7.64 / 9.21 / B |
| **Helios** | **93.78** | 53.58 | **89.91** | **5.73 / 7.83 / C** | **7.03 / 9.19 / B** | **7.39 / 8.26 / B** |

> 注：A = A-Score, E = E-Score, H = H-Grade；最佳结果加粗，次优下划线。

### 性能分析
- **客观任务平均准确率**：Helios 达到 **79.09%**，显著优于同规模开源模型（Qwen3-8B: 41.87%, Llama3-8B: 54.93%），接近 GPT-4 水平；
- **主观任务表现**：
  - 在 Q&A 和 Explanation 任务上，Helios 接近 GPT-4 表现（A/E Score 差距 <0.5）；
  - 在 Energy System Modeling（ESM）方面，虽落后于 GPT-4（因参数量限制），但仍优于 GPT-3.5-Turbo，表明其具备调用专业库进行建模的能力；
- **输出风格一致性**：经专家评审，Helios 输出更符合专业话语体系，逻辑严密且物理可解释性强。

### 消融实验（文中未明确列出表格，但有论述）
- **预训练影响**：仅用通用语料训练的模型在智能能源任务上表现差，证明 EnerBase 对知识积累至关重要；
- **指令微调必要性**：跳过UHIC阶段直接进行DS-TA会导致任务理解偏差；
- **RLHF作用**：加入人类偏好对齐后，模型在主观任务中的H-Grade提升明显，减少幻觉与不切实际建议。

---

## 4. 关键结论和发现

### 主要发现
1. **领域专用基础模型可行且必要**：通过系统性注入智能能源知识，Helios 在多项任务上超越通用LLMs，验证了“领域基础模型”的价值；
2. **高质量数据是关键瓶颈**：EnerSys框架有效解决了数据稀缺与质量问题，特别是通过多智能体协作+人工校验机制保障数据可靠性；
3. **三阶段训练路径有效**：Pre-train → SFT → RLHF 成功实现了知识内化、任务适应与人类偏好对齐；
4. **Helios具备实用潜力**：不仅能回答问题，还能完成能源系统建模、优化方案生成等复杂任务，具备工程辅助决策能力。

### 局限性
- **参数规模受限**：当前基于Qwen2.5-7B，参数量远小于GPT-4（~220B），在复杂推理与建模任务上仍有差距；
- **数据获取壁垒**：部分权威期刊文献受版权保护，无法全面纳入训练语料；
- **动态更新机制缺失**：模型知识静态固化，尚未集成在线学习或持续预训练机制以应对快速演进的技术趋势；
- **计算成本较高**：全流程训练需大量GPU资源，中小机构复现困难。

### 未来工作方向
- 扩展至更大参数模型（如Helios-14B/32B）；
- 构建增量式知识更新机制，支持模型持续学习；
- 探索多模态版本（融合GIS、传感器数据、图表等）；
- 将Helios嵌入实际能源管理系统（EMS）、虚拟电厂（VPP）等工业场景中落地应用；
- 推动建立开放共享的智能能源AI生态。

---

> ✅ **所有训练数据与模型检查点已公开发布**：  
> GitHub地址：[https://github.com/fine68/Helios](https://github.com/fine68/Helios)  
> 项目主页：[https://helios-llm.github.io/](https://helios-llm.github.io/)

</details>

---

### 14. [Interpretable Hybrid Deep Q-Learning Framework for IoT-Based Food Spoilage Prediction with Synthetic Data Generation and Hardware Validation](https://arxiv.org/abs/2512.19361)

**Authors**: Isshaan Singh, Divyansh Chawla, Anshu Garg, Shivin Mangal, Pallavi Gupta, Khushi Agarwal, Nimrat Singh Khalsa, Nandan Patel  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19361v1  

#### Abstract
The need for an intelligent, real-time spoilage prediction system has become critical in modern IoT-driven food supply chains, where perishable goods are highly susceptible to environmental conditions. Existing methods often lack adaptability to dynamic conditions and fail to optimize decision makin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究针对**物联网（IoT）驱动的食品供应链中食品腐败预测系统缺乏实时适应性和决策透明度**的问题。传统机器学习（ML）模型在静态数据集上表现良好，但在面对运输过程中动态变化的环境条件（如温度、湿度波动、传感器噪声）时，其预测能力显著下降，且模型决策过程不透明，难以被领域专家信任和验证。

### 提出的新方法与新思路
论文提出了一种**可解释的混合深度强化学习框架（Interpretable Hybrid Deep Q-Learning Framework）**，其核心创新点包括：

- **规则引导的可解释分类器环境（Rule-Based Interpretable Classifier Environment）**：  
  设计了一个基于领域知识的规则系统来定义腐败等级（如“低”、“中”、“紧急”），为智能体提供透明的、人类可理解的监督信号和语义边界。这使得智能体的决策可以追溯到具体的传感器阈值，极大提升了模型的**可解释性（Interpretability）**。

- **LSTM + RNN 混合 Deep Q-Network 架构**：  
  在DQN中集成**Long Short-Term Memory (LSTM)** 和 **Recurrent Neural Network (RNN)** 层，形成混合架构。LSTM用于捕捉长期时间依赖关系（如缓慢的气体积累趋势），RNN则精炼短期动态模式，共同增强模型对时序传感器数据的建模能力。

- **面向可解释性的评估指标体系**：  
  引入了一系列自定义的、以可解释性为导向的评估指标，如 `spoilage accuracy`、`reward-to-step ratio`、`loss decrease rate` 和 `exploration rate decay`，不仅衡量性能，还洞察智能体的学习动态和决策逻辑。

- **合成数据生成与硬件双重验证**：  
  通过模拟真实传感器读数生成**合成数据集（Synthetic Data）**进行大规模训练，并在**Arduino**为核心的**真实硬件平台**上收集实时数据进行验证，确保了方法的鲁棒性和实际部署潜力。

### 相比现有方法的优势
相比传统的ML模型（如ANN）、单一的LSTM/RNN模型或蒙特卡洛方法，该框架在**准确性、决策效率和可解释性**三方面均展现出优势：
1.  **更强的时序建模能力**：混合LSTM+RNN能同时处理长短期依赖，优于单一模型。
2.  **更高的决策效率**：通过强化学习的奖励机制，智能体学会做出更优的干预策略。
3.  **卓越的可解释性**：规则环境和可追踪的决策路径使其区别于“黑箱”模型，更适合食品安全等高风险应用。

---

## 2. 核心实验方法和设置

### 数据集
- **合成数据集（Synthetic Data）**：  
  基于真实硬件观察到的分布，使用正态分布生成1000个样本。各变量参数如下：
  - 温度（Temperature）：均值25°C，标准差5°C
  - 湿度（Humidity）：均值60%，标准差10%
  - 土壤水分（Moisture）：均值200，标准差50
  - MQ3传感器（酒精）：均值150，标准差30
  - MQ4传感器（甲烷）：均值250，标准差30
  - 添加均值为0、标准差为5的高斯噪声以模拟现实世界变异性。

- **真实硬件数据集（Real-Time Hardware Data）**：  
  使用 **Arduino** 微控制器连接 **DHT11**（温湿度）、**MQ3**（酒精）、**MQ4**（甲烷）和土壤水分传感器，在真实环境中采集数据。统计数据显示，平均温度为28.29°C，湿度为91.87%。

### 实验设置和评估指标
- **实验流程**：
  - 合成数据实验运行 **1000个episode**。
  - 真实硬件数据实验运行 **100个episode**（因资源限制）。
- **状态空间（State Space）**： `[Temperature, Humidity, Moisture, MQ3, MQ4]`，共5维，归一化至[0,1]。
- **动作空间（Action Space）**：离散动作，对应不同干预级别：
  - 0: 继续常规存储
  - 1: 发出低级警报
  - 2: 调整存储参数
  - 3: 发出紧急警报
- **奖励函数（Reward Function）**：匹配真实腐败等级得+1，否则得-1。
- **评估指标**：
  - **Spoilage Accuracy**：腐败等级预测正确的百分比。
  - **Reward-to-Step Ratio**：平均每步获得的奖励，衡量决策效率。
  - **Loss Decrease Rate**：损失函数随训练的下降速率，反映收敛稳定性。
  - **Exploration Rate Decay**：ε-greedy策略中探索率的衰减速率。

### 基线方法对比
与以下五种方法进行了对比：
1. **LSTM+RNN (Proposed)**：本文提出的混合模型。
2. **ANN**：基础人工神经网络。
3. **LSTM**：仅使用LSTM层。
4. **RNN**：仅使用RNN层。
5. **Monte Carlo**：基于随机采样的方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 2 & Table 3）

#### 在合成数据上的表现（1000 episodes）
| Agent Framework | Spoilage Accuracy | Reward-to-Step Ratio | Loss Decrease Rate |
|-----------------|-------------------|----------------------|--------------------|
| **LSTM+RNN (Proposed)** | **0.82** | **0.63** | -0.00042 |
| ANN | 0.74 | 0.49 | 0.42156 |
| LSTM | 0.77 | 0.54 | 0.00143 |
| RNN | 0.75 | 0.51 | 0.00089 |
| Monte Carlo | 0.00 | 0.70 | N/A |

#### 在真实硬件数据上的表现（100 episodes）
| Agent Framework | Spoilage Accuracy | Reward-to-Step Ratio | Loss Decrease Rate |
|-----------------|-------------------|----------------------|--------------------|
| **LSTM+RNN (Proposed)** | **0.59** | **0.17** | 0.09148 |
| LSTM | 0.58 | 0.16 | 0.12168 |
| ANN | 0.55 | 0.11 | -6.27726 |
| RNN | 0.42 | -0.17 | 26.60358 |
| Monte Carlo | 0.41 | -0.18 | N/A |

### 与基线方法的对比结果
- **准确性最高**：在合成和真实数据上，**LSTM+RNN**均取得了最高的`Spoilage Accuracy`（0.82和0.59），证明其强大的预测能力。
- **决策最高效**：在合成数据上，其`Reward-to-Step Ratio`（0.63）仅次于Monte Carlo（0.70），但Monte Carlo的准确率为0，说明其奖励是无效的。在真实数据上，LSTM+RNN的比率（0.17）远高于其他有效模型。
- **学习最稳定**：在真实数据上，LSTM+RNN的`Loss Decrease Rate`（0.09148）为正值且适中，表明学习过程稳定收敛。相比之下，ANN和RNN的负值或极高值表明学习不稳定或发散。

### 消融实验结果
虽然未明确标注为消融实验，但通过比较单一模型（LSTM, RNN）与混合模型（LSTM+RNN）的性能，可以得出：
- **LSTM的重要性**：LSTM模型的表现（Accuracy 0.77/0.58）远超RNN（0.75/0.42），说明捕捉长期依赖对腐败预测至关重要。
- **混合架构的有效性**：LSTM+RNN在所有指标上均优于单独的LSTM或RNN，证明了结合两者优势的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **混合LSTM+RNN架构在腐败预测任务中表现最优**：它能够有效捕捉环境参数的复杂时序模式，实现最高精度和最高效的决策。
2. **可解释性设计至关重要**：基于规则的环境为强化学习提供了清晰的语义指导，使模型决策变得透明和可审计，这对于食品安全应用不可或缺。
3. **方法具有良好的泛化能力和实用性**：在合成数据和真实硬件数据上均表现出色，证明了从仿真到现实（Simulation-to-Reality）的可行性，具备实际部署潜力。

### 方法的局限性
1. **合成数据的代表性有限**：尽管努力模拟真实场景，但可能无法覆盖所有极端或罕见情况，影响模型在未知环境下的泛化能力。
2. **传感器漂移问题**：传感器精度会随时间退化，需要持续监控和重新校准。
3. **计算复杂度较高**：混合神经网络架构增加了计算负担，对边缘设备的资源要求较高。
4. **依赖高质量训练数据**：模型性能高度依赖于训练数据的质量，数据中的偏差会影响最终决策。

### 未来工作方向
1. **探索混合强化学习架构**：结合Model-based RL或Imitation Learning，提高样本效率。
2. **引入主动学习（Active Learning）**：动态调整数据采集策略，优先获取信息量大的数据点。
3. **应用迁移学习（Transfer Learning）**：将在一种食物上训练的模型迁移到其他食物类型，减少新场景下的训练成本。
4. **发展分层强化学习（Hierarchical RL）**：分解任务，实现长期规划与短期控制的结合。
5. **多目标优化（Multi-objective RL）**：同时优化腐败控制、能耗、成本等多个目标。
6. **增强在线学习与自适应控制**：使系统能持续适应不断变化的环境。
7. **提升鲁棒性与不确定性量化**：更好地处理传感器噪声和模型预测的不确定性。

</details>

---

### 15. [The Best of Both Worlds: Hybridizing Neural Operators and Solvers for Stable Long-Horizon Inference](https://arxiv.org/abs/2512.19643)

**Authors**: Rajyasri Roy, Dibyajyoti Nayak, Somdatta Goswami  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.19643v1  

#### Abstract
Numerical simulation of time-dependent partial differential equations (PDEs) is central to scientific and engineering applications, but high-fidelity solvers are often prohibitively expensive for long-horizon or time-critical settings. Neural operator (NO) surrogates offer fast inference across para...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：The Best of Both Worlds: Hybridizing Neural Operators and Solvers for Stable Long-Horizon Inference

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统高保真数值求解器（numerical solvers）虽然精度高、物理一致性好，但计算成本高昂，难以满足**长时域（long-horizon）、多查询或多任务场景**下的实时性需求。而基于学习的神经算子（Neural Operators, NOs）虽能实现快速推理，但在自回归（autoregressive）时间步进中容易因局部误差累积导致**长期预测不稳定、误差爆炸、物理失真**。

更严重的是，现有方法通常只报告**集合平均误差**（如平均相对L2误差），无法保证单个关键任务实例的可靠性。这在工程或安全攸关应用中是不可接受的。

### 提出的新方法：ANCHOR
作者提出 **ANCHOR**（Adaptive Numerical Correction for High-fidelity Operator Rollouts），一种**在线、实例感知、误差可控的混合推理框架**，用于稳定非线性、时变PDE的长时域预测。

#### 核心思想
- 将预训练的神经算子（如TI-DeepONet）作为主推理引擎，提供高速度；
- 耦合一个经典高保真数值求解器（如FDM），作为“纠错模块”；
- 引入一个**基于PDE残差的指数移动平均（EMA）误差估计器**，无需真实解即可在线监测误差增长；
- 当估计误差超过动态阈值时，自动切换到数值求解器进行短期修正，之后再切回神经算子。

### 创新点
1. **首次实现“实例感知”的误差控制机制**  
   不依赖全局平均指标，而是为每个独立仿真轨迹提供误差监控与干预能力。

2. **提出物理信息驱动的EMA误差估计器**  
   利用归一化的PDE残差构建EMA序列，实验证明其与真实相对L2误差高度相关（Pearson相关系数 > 0.98），可在无ground truth情况下可靠预警。

3. **设计自适应衰减阈值策略（adaptive thresholding）**  
   针对耗散系统（dissipative systems）中解幅值随时间衰减的特点，使误差阈值也相应衰减，避免后期灵敏度下降。

4. **模型无关、求解器无关的通用框架**  
   可适配任意支持自回归推理的NO（如FNO、DeepONet等）和任意高保真求解器（FEM、FDM、谱方法等）。

### 相比现有方法的优势
| 方面 | 现有方法 | ANCHOR |
|------|--------|-------|
| 长期稳定性 | 易出现误差累积、发散 | 通过选择性调用求解器有效抑制误差增长 |
| 实例级可靠性 | 仅报告平均性能，个体风险未知 | 在线监控每条轨迹，确保个体可控 |
| 自适应性 | 固定间隔调用求解器或固定阈值 | 动态、数据驱动地决定何时干预 |
| 效率-精度平衡 | 往往牺牲一方换取另一方 | 兼顾速度（≈NO）与精度/鲁棒性（≈solver） |

---

## 2. 核心实验方法和设置

### 使用的PDE数据集（共4个典型方程）
| PDE名称 | 维度 | 类型 | 特点 |
|--------|-----|------|------|
| 1D Burgers’ | 1D+time | 非线性对流扩散 | 存在激波，易产生误差累积 |
| 2D Burgers’ | 2D+time | 同上 | 更高维，挑战更大 |
| 2D Allen-Cahn | 2D+time | 非线性反应扩散 | 涉及相场演化，界面锐利 |
| 3D Heat Conduction | 3D+time | 线性抛物型 | 复杂几何（L形域），真实边界条件 |

所有问题均采用**外推设置**（extrapolation regime）：训练时间区间为 $[0, t_{\text{interp}}]$，测试/推理至 $[t_{\text{interp}}, T]$，考验泛化能力。

### 实验设置
- **主神经算子**：TI-DeepONet（Time-Integrator Deep Operator Network），因其本身具备一定抗误差累积能力。
- **高保真求解器**：有限差分法（FDM）或伪谱法（pseudospectral），视具体PDE而定。
- **耦合方式**：ANCHOR以固定步数（如10步）调用求解器进行纠正，随后交还控制权给TI-DeepONet。
- **误差估计器参数**：
  - EMA平滑系数 $\alpha \in [0.01, 0.1]$
  - 自适应阈值衰减速率 $\gamma \in [2,3]$

### 评估指标
1. **相对L2误差**（Relative L2 Error）：
   $$
   e_{L2} = \frac{\|u_{\text{truth}} - u_{\text{pred}}\|_2}{\|u_{\text{truth}}\|_2}
   $$
2. **EMA误差估计器与真实误差的相关性**（Pearson Correlation Coefficient, $p_{\text{corr}}$）
3. **计算耗时对比**（Wall-clock time）
4. **误差分布可视化**（spatiotemporal error maps）

### 基线方法对比
| 方法 | 描述 |
|------|------|
| AR-DON（Autoregressive DeepONet） | 标准自回归DeepONet，无集成时间积分结构 |
| TI-DON（TI-DeepONet） | 改进版NO，嵌入数值积分模块，提升长期稳定性 |
| NS-only（Numerical Solver Only） | 纯高保真求解器，作为精度基准 |
| ANCHOR（Ours） | 本文提出的混合框架 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| PDE | 方法 | 平均相对L2误差（测试集） | 计算时间（秒） | 速度提升 vs Solver |
|------|--------|--------------------------|----------------|--------------------|
| 1D Burgers’ | TI-DON | 1.79% | ~0.06 | ≈2.5× |
| | ANCHOR | <5%（稳定上限） | ~0.2 | 无显著加速（低维） |
| 2D Burgers’ | TI-DON | 12.99% | ~0.14 | ≈56× |
| | ANCHOR | 显著低于TI-DON | ~4.5 | ≈1.7× |
| 2D Allen-Cahn | TI-DON | 16.16% | ~0.075 | ≈43× |
| | ANCHOR | 极大降低 | ~1.0 | ≈3×（75%节省） |
| 3D Heat | TI-DON | 6.55% | ~0.18 | ≈15× |
| | ANCHOR | 有效压制增长 | ~1.0 | ≈2×（50%节省） |

> 注：完整结果见原文Table 1 & Table 2。

### 与基线方法的对比结果
- **误差控制方面**：
  - AR-DON：误差呈指数增长，很快失效。
  - TI-DON：比AR-DON显著改善，但仍**未完全约束误差增长**。
  - ANCHOR：成功将误差增长“锯齿化”并限制在合理范围内，首次实现**有界误差长时预测**。

- **误差估计有效性**：
  - 所有四个PDE上，EMA误差估计器与真实L2误差的**Pearson相关系数均 > 0.98**（最高达0.994），证明其可作为可靠的代理指标。
  - 图4显示，每当数值求解器被激活，误差立即下降，验证了“纠正”效果。

- **效率表现**：
  - 在高维问题（2D及以上）中，ANCHOR相比纯求解器仍保持显著优势：
    - 2D Burgers’：提速约30%
    - 2D Allen-Cahn：提速约75%
    - 3D Heat：提速约50%

### 消融实验与分析（隐含在文中）
尽管未明确列出消融表，但以下对比构成事实上的消融研究：
- **是否使用EMA估计器？** → 若不用，则无法动态判断何时调用求解器。
- **是否使用自适应阈值？** → 实验表明固定阈值在耗散系统后期会滞后于实际误差增长。
- **是否引入求解器反馈？** → 单独TI-DON无法完全遏制误差，说明纠错机制必要。

此外，作者强调ANCHOR的性能不依赖特定NO架构，展示了其**框架通用性**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **EMA-based error estimator 是有效的物理感知代理指标**  
   归一化PDE残差的EMA序列与真实误差高度相关，可用于无监督在线监控。

2. ✅ **ANCHOR 能有效稳定长时域推理**  
   通过间歇性调用高保真求解器，实现了误差增长的“软截断”，解决了NO长期部署中的可靠性瓶颈。

3. ✅ **效率与精度可以兼得**  
   在复杂高维系统中，ANCHOR在保持接近NO速度的同时，获得了接近传统求解器的精度与稳定性。

4. ✅ **价值随问题复杂度上升而放大**  
   对简单1D问题加速有限；但对2D/3D复杂PDE，计算开销节省可达2–3倍，凸显其工程实用价值。

### 方法的局限性
1. **目前仅适用于耗散系统（dissipative PDEs）**  
   自适应阈值设计基于解幅值衰减的前提，对色散（dispersive）或混沌系统需重新建模。

2. **依赖PDE残差计算**  
   需要在推理过程中评估残差，带来额外计算负担，尤其当NO输出分辨率高时。

3. **求解器调用策略较简单**  
   当前为固定步数纠正，未来可探索更智能的“纠正长度”决策机制。

4. **未处理不确定性量化（UQ）**  
   虽然提升了可靠性，但尚未提供概率意义上的置信区间。

### 未来工作方向
1. **扩展至其他类型PDE**  
   如波动方程（hyperbolic）、Navier-Stokes等非耗散或混沌系统，需设计新的误差先验与阈值策略。

2. **开发更高效的误差估计器**  
   探索恢复型估计器（recovery-based estimator）或其他轻量级物理信息代理，减少残差计算开销。

3. **引入强化学习优化调度策略**  
   学习最优的“何时调用、调用多久”的策略，进一步提升效率。

4. **构建端到端可微分混合流水线**  
   结合differentiable physics思想，实现求解器与NO的联合优化。

5. **应用于数字孪生与实时控制系统**  
   将ANCHOR嵌入工业级仿真平台，推动其在航空航天、能源、气候模拟等领域的落地。

---

> **总结一句话**：  
> ANCHOR不是要用神经网络取代数值方法，而是让两者协同工作——**用学习换速度，用物理保可靠**，为构建可信、高效、可扩展的下一代科学计算工具提供了原则性路径。

</details>

---

### 16. [FC-MIR: A Mobile Screen Awareness Framework for Intent-Aware Recommendation based on Frame-Compressed Multimodal Trajectory Reasoning](https://arxiv.org/abs/2512.19107)

**Authors**: Zhe Yang, Xiaoshuang Sheng, Zhengnan Zhang, Jidong Wu, Zexing Wang, Xin He, Shenghua Xu, Guanjing Xiong  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19107v1  

#### Abstract
Identifying user intent from mobile UI operation trajectories is critical for advancing UI understanding and enabling task automation agents. While Multimodal Large Language Models (MLLMs) excel at video understanding tasks, their real-time mobile deployment is constrained by heavy computational cos...

---

### 17. [GenEnv: Difficulty-Aligned Co-Evolution Between LLM Agents and Environment Simulators](https://arxiv.org/abs/2512.19682)

**Authors**: Jiacheng Guo, Ling Yang, Peter Chen, Qixin Xiao, Yinjie Wang, Xinzhe Juan, Jiahao Qiu, Ke Shen, Mengdi Wang  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19682v1  

#### Abstract
Training capable Large Language Model (LLM) agents is critically bottlenecked by the high cost and static nature of real-world interaction data. We address this by introducing GenEnv, a framework that establishes a difficulty-aligned co-evolutionary game between an agent and a scalable, generative e...

---

### 18. [Fast Online Digital Twinning on FPGA for Mission Critical Applications](https://arxiv.org/abs/2512.17942)

**Authors**: Bin Xu, Ayan Banerjee, Sandeep K. S. Gupta  
**Category**: cs.DC  
**Published**: 2025-12-23  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.17942v1  

#### Abstract
Digital twinning enables real-time simulation and predictive modeling by maintaining a continuously updated virtual representation of a physical system. In mission-critical applications, such as mid-air collision avoidance, these models must operate online with extremely low latency to ensure safety...

---

### 19. [Small Language Models as Compiler Experts: Auto-Parallelization for Heterogeneous Systems](https://arxiv.org/abs/2512.19250)

**Authors**: Prathamesh Devadiga  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19250v1  

#### Abstract
Traditional auto-parallelizing compilers, reliant on rigid heuristics, struggle with the complexity of modern heterogeneous systems. This paper presents a comprehensive evaluation of small (approximately 1B parameter) language-model-driven compiler auto-parallelization. We evaluate three models: gem...

---

### 20. [Scalably Enhancing the Clinical Validity of a Task Benchmark with Physician Oversight](https://arxiv.org/abs/2512.19691)

**Authors**: Junze Ye, Daniel Tawfik, Alex J. Goodell, Nikhil V. Kotha, Mark K. Buyyounouski, Mohsen Bayati  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19691v1  

#### Abstract
Automating the calculation of clinical risk scores offers a significant opportunity to reduce physician administrative burden and enhance patient care. The current standard for evaluating this capability is MedCalc-Bench, a large-scale dataset constructed using LLM-based feature extraction and rule-...

---

### 21. [Q-KVComm: Efficient Multi-Agent Communication Via Adaptive KV Cache Compression](https://arxiv.org/abs/2512.17914)

**Authors**: Boris Kriuk, Logic Ng  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.17914v1  

#### Abstract
Multi-agent Large Language Model (LLM) systems face a critical bottleneck: redundant transmission of contextual information between agents consumes excessive bandwidth and computational resources. Traditional approaches discard internal semantic representations and transmit raw text, forcing receivi...

---

### 22. [AWPO: Enhancing Tool-Use of Large Language Models through Explicit Integration of Reasoning Rewards](https://arxiv.org/abs/2512.19126)

**Authors**: Zihan Lin, Xiaohan Wang, Hexiong Yang, Jiajun Chai, Jie Cao, Guojun Yin, Wei Lin, Ran He  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19126v1  

#### Abstract
While reinforcement learning (RL) shows promise in training tool-use large language models (LLMs) using verifiable outcome rewards, existing methods largely overlook the potential of explicit reasoning rewards to bolster reasoning and tool utilization. Furthermore, natively combining reasoning and o...

---

### 23. [LeJOT: An Intelligent Job Cost Orchestration Solution for Databricks Platform](https://arxiv.org/abs/2512.18266)

**Authors**: Lizhi Ma, Yi-Xiang Hu, Yuke Wang, Yifang Zhao, Yihui Ren, Jian-Xiang Liao, Feng Wu, Xiang-Yang Li  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.18266v1  

#### Abstract
With the rapid advancements in big data technologies, the Databricks platform has become a cornerstone for enterprises and research institutions, offering high computational efficiency and a robust ecosystem. However, managing the escalating operational costs associated with job execution remains a ...

---

### 24. [A Surrogate-Augmented Symbolic CFD-Driven Training Framework for Accelerating Multi-objective Physical Model Development](https://arxiv.org/abs/2512.19031)

**Authors**: Yuan Fang, Fabian Waschkowski, Maximilian Reissmann, Richard D. Sandberg, Takuo Oda, Koichi Tanimoto  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19031v1  

#### Abstract
Computational Fluid Dynamics (CFD)-driven training combines machine learning (ML) with CFD solvers to develop physically consistent closure models with improved predictive accuracy. In the original framework, each ML-generated candidate model is embedded in a CFD solver and evaluated against referen...

---

### 25. [Binary Kernel Logistic Regression: a sparsity-inducing formulation and a convergent decomposition training algorithm](https://arxiv.org/abs/2512.19440)

**Authors**: Antonio Consolo, Andrea Manno, Edoardo Amaldi  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19440v1  

#### Abstract
Kernel logistic regression (KLR) is a widely used supervised learning method for binary and multi-class classification, which provides estimates of the conditional probabilities of class membership for the data points. Unlike other kernel methods such as Support Vector Machines (SVMs), KLRs are gene...

---

### 26. [Deep Learning for Unrelated-Machines Scheduling: Handling Variable Dimensions](https://arxiv.org/abs/2512.19527)

**Authors**: Diego Hitzges, Guillaume Sagnol  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19527v1  

#### Abstract
Deep learning has been effectively applied to many discrete optimization problems. However, learning-based scheduling on unrelated parallel machines remains particularly difficult to design. Not only do the numbers of jobs and machines vary, but each job-machine pair has a unique processing time, dy...

---

### 27. [ESearch-R1: Learning Cost-Aware MLLM Agents for Interactive Embodied Search via Reinforcement Learning](https://arxiv.org/abs/2512.18571)

**Authors**: Weijie Zhou, Xuangtang Xiong, Ye Tian, Lijun Yue, Xinyu Wu, Wei Li, Chaoyang Zhao, Honghui Dong, Ming Tang, Jinqiao Wang, Zhengyou Zhang  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.18571v1  

#### Abstract
Multimodal Large Language Models (MLLMs) have empowered embodied agents with remarkable capabilities in planning and reasoning. However, when facing ambiguous natural language instructions (e.g., "fetch the tool" in a cluttered room), current agents often fail to balance the high cost of physical ex...

---

### 28. [Multimodal Bayesian Network for Robust Assessment of Casualties in Autonomous Triage](https://arxiv.org/abs/2512.18908)

**Authors**: Szymon Rusiecki, Cecilia G. Morales, Kimberly Elenberg, Leonard Weiss, Artur Dubrawski  
**Category**: cs.AI  
**Published**: 2025-12-23  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.18908v1  

#### Abstract
Mass Casualty Incidents can overwhelm emergency medical systems and resulting delays or errors in the assessment of casualties can lead to preventable deaths. We present a decision support framework that fuses outputs from multiple computer vision models, estimating signs of severe hemorrhage, respi...

---

### 29. [Solver-Independent Automated Problem Formulation via LLMs for High-Cost Simulation-Driven Design](https://arxiv.org/abs/2512.18682)

**Authors**: Yuchen Li, Handing Wang, Bing Xue, Mengjie Zhang, Yaochu Jin  
**Category**: cs.CL  
**Published**: 2025-12-23  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.18682v1  

#### Abstract
In the high-cost simulation-driven design domain, translating ambiguous design requirements into a mathematical optimization formulation is a bottleneck for optimizing product performance. This process is time-consuming and heavily reliant on expert knowledge. While large language models (LLMs) offe...

---

### 30. [Towards Guided Descent: Optimization Algorithms for Training Neural Networks At Scale](https://arxiv.org/abs/2512.18373)

**Authors**: Ansh Nagwekar  
**Category**: cs.LG  
**Published**: 2025-12-23  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.18373v1  

#### Abstract
Neural network optimization remains one of the most consequential yet poorly understood challenges in modern AI research, where improvements in training algorithms can lead to enhanced feature learning in foundation models, order-of-magnitude reductions in training time, and improved interpretabilit...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
