# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-16 06:48:37 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FlashSchNet: Fast and Accurate Coarse-Grained Neural Network Molecular Dynamics](https://arxiv.org/abs/2602.13140)

**Authors**: Pingzhi Li, Hongxuan Li, Zirui Liu, Xingcheng Lin, Tianlong Chen  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.13140v1  

#### Abstract
Graph neural network (GNN) potentials such as SchNet improve the accuracy and transferability of molecular dynamics (MD) simulation by learning many-body interactions, but remain slower than classical force fields due to fragmented kernels and memory-bound pipelines that underutilize GPUs. We show t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：FlashSchNet: Fast and Accurate Coarse-Grained Neural Network Molecular Dynamics**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
传统基于 **Graph Neural Network (GNN)** 的分子动力学（MD）势能模型（如 SchNet、CGSchNet）虽然在**准确性**和**可迁移性**上优于经典力场（如 MARTINI），但由于其计算流程中存在大量内存访问瓶颈（memory-bound），导致实际模拟速度远慢于经典力场。尤其是在 GPU 上运行时，频繁的中间张量写入高带宽内存（HBM）以及原子级累加（atomic scatter-add）造成严重性能瓶颈。

本文指出，当前 GNN-MD 的主要瓶颈并非计算量（FLOPs），而是 **I/O 效率低下**——即 GPU 高带宽内存（HBM）与片上 SRAM 之间的数据移动未被优化。

---

### **提出了什么新方法或新思路**
作者提出 **FlashSchNet**，一个面向 I/O 优化的高效 SchNet 风格 GNN-MD 框架，通过四项关键技术实现端到端加速：

1. **Flash radial basis**  
   将成对距离计算、Gaussian basis 展开和余弦截断包络融合为单个分块（tiled）操作，每个距离只计算一次并在片上复用，避免多次读写中间结果。

2. **Flash message passing**  
   融合截断掩码、邻居收集、滤波器乘法和归约操作为单一内核，消除边张量（edge tensors）在 HBM 中的显式物化（materialization）。

3. **Flash aggregation**  
   将传统的 scatter-add 改写为基于 CSR（Compressed Sparse Row）的 segment reduce 操作，将原子更新次数从 $O(E \times D)$ 降至 $O(N \times D)$，实现无竞争（contention-free）累积。

4. **Channel-wise 16-bit quantization (W16A16)**  
   利用 SchNet 中 MLP 权重通道间动态范围低的特点，采用逐通道量化策略，在几乎不损失精度的前提下提升吞吐量。

---

### **相比现有方法的优势**
- **速度显著提升**：在单张 NVIDIA RTX PRO 6000 上达到 **1000 ns/day** 的聚合模拟吞吐量，是 CGSchNet 基线的 **6.5× 加速**。
- **内存占用大幅降低**：峰值内存减少 **80%**（从 92GB → 18GB）。
- **首次超越经典力场**：在墙钟时间效率上**超过 MARTINI 等经典粗粒化力场**，同时保持 SchNet 级别的准确性和可迁移性。
- **支持更大规模并行**：更低内存需求使得可在消费级硬件上运行数百甚至上千副本，适用于增强采样任务（如 replica exchange）。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
采用 Charron et al. (2025) 提出的五个快速折叠蛋白作为基准测试系统：
- Chignolin (CLN, 10 残基)
- TRPcage (2JOF, 20 残基)
- Homeodomain (1ENH, 54 残基)
- Villin (1YRF, 35 残基)
- Alpha3D (2A3D, 73 残基)

所有系统均使用**粗粒化（Coarse-Grained, CG）表示**，以 bead 代替原子，典型大小为 269 beads（如 1ENH）。

---

### **实验设置和评估指标**

#### **模拟设置**
- 使用 **Langevin 动力学**，温度 300K
- 时间步长：4 fs
- 并行副本数：最多 64 个
- 硬件平台：单张 **NVIDIA RTX PRO 6000 GPU**

#### **评估指标**
| 类型 | 指标 |
|------|------|
| **性能指标** | 吞吐量（timestep·mol/s）、峰值内存（GB）、模拟吞吐（ns/day） |
| **结构保真度** | Ca RMSD、原生接触分数 Q、GDT-TS |
| **物理行为验证** | 折叠/去折叠事件频率、自由能面分布 |

---

### **基线方法对比**
- **CGSchNet (FP32)**：原始 SchNet 风格 MLFF，作为主要对比基线
- **MARTINI**：广泛使用的经典粗粒化力场
- **All-Atom Simulation**：全原子模拟，作为结构真实性的参考标准

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 指标 | 结果 |
|------|------|
| **最大吞吐量** | 在 1ENH 系统上达 **~3000 timestep·mol/s** |
| **等效模拟速度** | **1000 ns/day**（64 副本聚合） |
| **相对于 CGSchNet 的加速比** | **6.5×**（Homeodomain 系统） |
| **峰值内存消耗** | **18 GB**（vs. CGSchNet 的 92 GB） |
| **内存减少比例** | **>80%** |
| **模型 FLOPs 利用率 (MFU)** | 显著提升（原文未给具体数值，但强调从极低水平改善） |

> ✅ **重要突破**：这是首个在墙钟时间上**超越 MARTINI** 的 SchNet 风格 GNN-MD 方法。

---

### **与基线方法的对比结果**

#### **性能对比（Table 3）**
| 方法 | 1ENH 吞吐量 (timestep·mol/s) | 峰值内存 (GB) |
|------|-------------------------------|----------------|
| CGSchNet | ~477 | 92.5 |
| **FlashSchNet** | **~3095** | **18.0** |
| MARTINI | ~2250 | 34.9 |
| All-Atom | ~1005 | 37.4 |

> FlashSchNet 不仅远超 CGSchNet，还**超过 MARTINI**，且内存仅为后者的 1/5。

#### **结构保真度对比（Table 2）**
| 方法 | GDT-TS (1ENH) | Largest Q (1ENH) |
|------|---------------|------------------|
| CGSchNet | 0.78 | 0.96 |
| **FlashSchNet** | **0.74** | **0.88** |
| MARTINI | 0.46 | 0.56 |
| All-Atom (Ref.) | 0.88 | 0.93 |

> FlashSchNet 在结构质量上**接近 CGSchNet**，显著优于 MARTINI，证明优化未引入明显误差。

---

### **消融实验结果**

#### **动态图拓扑下的鲁棒性（Figure 5–6）**
- 在蛋白质展开过程中（邻接图变稠密），CGSchNet 因 scatter contention 导致吞吐下降。
- FlashSchNet 使用 **CSR segment reduce** 实现无竞争聚合，维持稳定吞吐。
- 批次越大（64 replicas），优势越明显，**加速比可达 6.5×**。

#### **批大小扩展能力（Figure 7）**
- CGSchNet 在小批量即发生 OOM（Out-of-Memory）。
- FlashSchNet 可扩展至 **3–10× 更大的批大小**（如 Chignolin 支持 2048 副本）。
- 对 replica exchange、enhanced sampling 等需要大规模并行的任务至关重要。

#### **各技术贡献分析（Figure 1 & 正文）**
- **Fused kernels**（Flash radial basis + message passing）：消除中间张量存储，减少 HBM 流量。
- **Segmented reduction**：解决 scatter contention，提升聚合效率。
- **W16A16 quantization**：进一步提升 MLP 吞吐，利用 Tensor Cores。

---

## **4. 关键结论和发现**

### **主要发现**
1. **I/O 是 GNN-MD 的真正瓶颈**：不是算力不足，而是内存访问模式低效。
2. **算法-硬件协同设计有效**：通过融合操作、重构聚合方式、量化等手段，可在不牺牲精度前提下大幅提升性能。
3. **FlashSchNet 实现“鱼与熊掌兼得”**：兼具 **SchNet 级精度** 和 **超越经典力场的速度**。
4. **支持更大规模模拟**：低内存使多副本并行成为可能，推动统计收敛和稀有事件研究。

---

### **方法的局限性**
- 当前优化依赖于特定硬件特性（如 GPU Tensor Cores、SRAM 容量），跨平台移植需调整。
- 主要针对 **粗粒化系统** 设计，全原子系统的扩展性有待验证。
- 量化策略虽轻量，但在极端化学环境下是否影响泛化能力仍需长期观察。
- CSR 分组需每次重建（因 neighbor list 动态变化），带来额外排序开销（但实验证明总体收益远大于成本）。

---

### **未来工作方向**
- 将 FlashSchNet 思路推广至其他 GNN 架构（如 NequIP、MACE）。
- 探索更高效的动态图索引构建机制（如增量式 bucket sort）。
- 结合 **sparse training** 进一步压缩模型。
- 开发自动化的 I/O-aware 编译器工具链，将此类优化泛化至更多 ML-MD 模型。
- 在药物发现、材料老化等实际应用场景中部署，验证其工业价值。

---

> 🔗 **开源代码**：[https://github.com/UNITES-Lab/flash-molecular-dynamics](https://github.com/UNITES-Lab/flash-molecular-dynamics)

</details>

---

### 2. [Think Fast and Slow: Step-Level Cognitive Depth Adaptation for LLM Agents](https://arxiv.org/abs/2602.12662)

**Authors**: Ruihan Yang, Fanghua Ye, Xiang We, Ruoqing Zhao, Kang Luo, Xinbo Xu, Bo Zhao, Ruotian Ma, Shanyi Wang, Zhaopeng Tu, Xiaolong Li, Deqing Yang,  Linus  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.12662v1  

#### Abstract
Large language models (LLMs) are increasingly deployed as autonomous agents for multi-turn decision-making tasks. However, current agents typically rely on fixed cognitive patterns: non-thinking models generate immediate responses, while thinking models engage in deep reasoning uniformly. This rigid...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Think Fast and Slow: Step-Level Cognitive Depth Adaptation for LLM Agents

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **LLM Agents** 在执行多轮决策任务时通常采用**固定的认知模式**：
- **非思考模型（non-thinking）**：每一步都进行直觉反应，速度快但缺乏深度推理能力；
- **思考模型（thinking models）**：如 DeepSeek-R1，全程使用 Chain-of-Thought 推理，导致在简单步骤上浪费大量计算资源。

这种“认知刚性”（cognitive rigidity）在长周期、异构复杂度的任务中效率低下。例如，在探索房间后只需“go to bathroom”的例行操作却仍进行深层推理，造成 token 浪费。

### 🚀 提出的新方法：CoGROUTER
作者提出 **CoGROUTER** 框架，使 LLM Agent 能够在每个时间步动态选择合适的**认知深度**（cognitive depth），实现“快慢双系统”思维机制。

#### 核心思想来源：ACT-R 理论
基于人类认知理论 **ACT-R**（Adaptive Control of Thought–Rational），设计了四个层级的认知模式：

| Level | 名称 | 特征 |
|-------|------|------|
| **L1** | Instinctive Response（本能响应） | 无显式推理，直接输出动作，适用于例行操作 |
| **L2** | Situational Awareness（情境感知） | 分析当前状态与可行动作 |
| **L3** | Experience Integration（经验整合） | 回顾历史行为并反思调整策略 |
| **L4** | Strategic Planning（战略规划） | 全局模拟多个候选动作的未来影响 |

#### 两阶段训练框架：
1. **Cognition-aware Supervised Fine-tuning (CoSFT)**  
   - 构建带有随机标注认知层级的专家轨迹数据集；
   - 强制模型学习不同层级的稳定表达格式，防止后续 RL 中的模式崩溃。

2. **Cognition-aware Policy Optimization (CoPO)**  
   - 创新的强化学习算法，通过 **confidence-aware advantage reweighting** 实现细粒度信用分配；
   - 关键洞察：**适当认知深度应最大化动作预测的置信度**；
   - 对成功轨迹中的每一步构建“认知组”（cognitive group），比较四种认知模式下对同一动作的预测置信度，并据此重加权优势函数。

### 🔍 相比现有方法的优势
| 维度 | CoGROUTER vs. 现有方法 |
|------|------------------------|
| **适应性** | 支持**多级连续适应**，而非仅 binary switching（如 AdaptThink） |
| **信用分配** | 实现**step-level credit assignment**，而 GRPO/GiGPO 是 trajectory-level，易导致全路径深思 |
| **效率** | 显著减少 token 使用量（↓62% vs. GRPO） |
| **性能** | 达到 SOTA 成功率，远超 GPT-4o、OpenAI-o3 等大模型 |

---

## 2. 核心实验方法和设置

### 📚 数据集
在两个具有挑战性的交互式环境上进行评估：

| 数据集 | 描述 |
|-------|------|
| **ALFWorld** | 基于 TextWorld 的家庭任务环境，包含 Pick & Place、Clean & Place 等六类任务；最大步数为 30；以 **success rate (SR)** 为主要指标。 |
| **ScienceWorld** | 面向小学科学课程的文本环境，涵盖测量仪器使用、物理实验等 30 种任务；最大步数为 100；评估指标为 **平均得分（Score）** 和 **成功率（SR）**。 |

### ⚙️ 实验设置
- **基础模型**：`Qwen2.5-7B` 和 `Llama3.1-8B`
- **训练划分**：
  - CoSFT：各取 500 个环境，由 GPT-4o 生成带认知标签的轨迹；
  - CoPO：剩余约 2000 个环境用于在线 RL 训练；
- **RL 设置**：
  - 每次迭代采样 128 个环境；
  - 每 rollout 使用 16 个 group，group size = 8；
  - 总共训练 150 轮。

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **SR (%)** | 成功完成任务的比例 |
| **Score** | ScienceWorld 中的标准化得分（0–100） |
| **#Tokens** | 平均每条轨迹消耗的输出 token 数，衡量效率 |
| **Cognitive Level Distribution** | 各层级使用的频率分布，分析自适应能力 |

### 🆚 基线方法对比
| 类型 | 方法 |
|------|------|
| **Prompt-based** | ReAct, Reflexion |
| **Offline Training** | SFT, ETO, CoSFT, CoSFTexp |
| **Online RL** | GRPO, GiGPO, AdaptThink |
| **Frontier Models** | GPT-4o, DeepSeek-V3/R1, OpenAI-o3, Gemini-2.5-Pro |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Qwen2.5-7B 结果）

| 方法 | ALFWorld SR | ScienceWorld SR | Avg SR | #Tokens |
|------|-------------|------------------|--------|---------|
| GPT-4o | 61.5% | 22.5% | 42.0% | 935.4 |
| GRPO | 83.5% | 53.0% | 68.3% | 4367.3 |
| GiGPO | 88.0% | 47.0% | 67.5% | 3779.2 |
| **CoGROUTER (Ours)** | **92.5%** | **72.0%** | **82.3%** | **1641.4** |

> ✅ **相比 GRPO 提升 +14.0% SR，节省 62% tokens**

> ✅ **相比 GPT-4o 提升 +40.3% SR**

> ✅ **使用更小模型超越更大规模的前沿模型**

### 🔁 与基线方法的关键对比发现
- **固定模式失败**：
  - 非思考模型（DeepSeek-V3）仅得 29.0% SR；
  - 全程深思模型（DeepSeek-R1）虽达 56.5%，但 token 消耗高达 11k+；
- **二元切换不足**：
  - AdaptThink 仅提升至 63.0%，说明 binary thinking 无法捕捉中间层次需求（如 L2/L3）；
- **传统 RL 导致模式坍缩**：
  - GRPO 和 GiGPO 快速收敛到 **>75% 使用 L4**，即使在简单步骤也过度思考；
  - CoPO 成功维持多样化分布：**L1 占 55.0%（ALFWorld）**，体现真正自适应。

### 🔍 消融实验结果（Ablation Study）
| 变体 | ALFWorld SR | ScienceWorld SR | Tokens | 认知分布特点 |
|------|-------------|------------------|--------|--------------|
| **CoPO (Ours)** | 92.5% | 72.0% | 1739.4 | 自适应良好（L1:55%, L4:12.3%） |
| w/ Max Probs | 89.0% | 69.5% | 1664.9 | 过度依赖局部高概率词 |
| w/ Min Probs | 81.5% | 67.5% | 1227.7 | 单个低 prob token 主导信心评分 |
| w/ Entropy | 79.0% | 70.0% | 1584.6 | 不确定性不能反映动作匹配度 |
| w/o Cold Start | 86.0% | 64.5% | 2496.2 | 出现极端偏斜（L1:0.3%） |
| w/ CoSFTexpert | 87.5% | 62.0% | 2871.6 | 继承教师偏好，L3 占 92.1% |
| w/ Corr & Incorr | 83.0% | 66.0% | 1375.8 | 模型逃避自信预测，退化为 L1 |

> ✅ **验证了以下设计至关重要**：
> - 使用 **平均 log-probability** 作为置信度指标最优；
> - **平衡初始化（balanced CoSFT）** 是避免模式坍缩的前提；
> - 仅对**成功轨迹**进行 reweighting，避免错误激励。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **认知刚性是 LLM Agent 效率瓶颈的根本原因**  
   固定思考或非思考模式无法应对任务内部的 step-wise heterogeneity。

2. **多级自适应优于二元切换**  
   L2（情境感知）和 L3（经验整合）在特定场景中不可或缺，binary thinking 无法满足复杂任务需求。

3. **CoPO 实现真正的 step-level 信用分配**  
   通过 confidence-aware reweighting，让模型学会“何时该深思”，避免盲目使用 L4。

4. **高效与高性能可以兼得**  
   CoGROUTER 不仅达到 SOTA 性能，还显著降低 token 消耗（↓62%），具备实际部署潜力。

5. **动态认知分配符合人类认知规律**  
   - 初始阶段多用 L4（全局规划）；
   - 中期探索用 L2/L3；
   - 后期执行大量使用 L1（例行化）；
   > 符合 ACT-R 理论中“从陈述性到程序性知识转化”的过程。

### ⚠️ 局限性
- 当前四个层级是人工定义，尚未支持**自动演化认知层级**；
- 依赖 GPT-4o 生成训练数据，存在 teacher forcing 偏差；
- 在极少数需要持续深思的任务中可能低估 L4 使用频率；
- CoPO 增加约 1.4–1.5× 训练时间（见 Table 4），计算开销略高。

### 🔮 未来工作方向
- 探索 **self-evolving cognitive hierarchies**，让模型自主发现合适层级；
- 将 CoPO 扩展至更多模态（vision, robotics）和开放世界任务；
- 结合 memory 机制，实现长期经验积累下的认知演进；
- 研究如何将该框架应用于 smaller models 或 edge devices。

---

> 💡 **一句话总结**：  
> **CoGROUTER 让 LLM Agent 学会“快慢双系统”思维——在该快时直觉反应，在该慢时深入思考，从而在性能与效率之间取得前所未有的平衡。**

</details>

---

### 3. [Multi-Dimensional Visual Data Recovery: Scale-Aware Tensor Modeling and Accelerated Randomized Computation](https://arxiv.org/abs/2602.12982)

**Authors**: Wenjin Qin, Hailin Wang, Jiangjun Peng, Jianjun Wang, Tingwen Huang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.12982v1  

#### Abstract
The recently proposed fully-connected tensor network (FCTN) decomposition has demonstrated significant advantages in correlation characterization and transpositional invariance, and has achieved notable achievements in multi-dimensional data processing and analysis. However, existing multi-dimension...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multi-Dimensional Visual Data Recovery: Scale-Aware Tensor Modeling and Accelerated Randomized Computation

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对**大规模高维视觉数据恢复**中存在的两个核心挑战：
- **建模能力不足**：现有基于 FCTN 分解的方法难以同时有效捕捉数据的全局低秩性（Global Low-Rankness）和局部平滑性（Local Smoothness）。
- **计算效率低下**：FCTN 分解在处理大规模张量时面临高昂的计算开销，尤其是在多次 SVD 或 ALS 迭代中。

### 🚀 提出的新方法与创新思路

#### （1）**FCTN-GNTCTV 正则化器（FCTN-based Generalized Nonconvex Tensor Correlated Total Variation）**
- 在 **FCTN 分解框架下**提出了一种新的非凸梯度域正则化范式。
- 同时编码 **全局低秩性（L）** 和 **局部连续性/平滑性（S）**，形成 L+S 先验联合建模。
- 利用非凸函数（如 MCP、SCAD、Log 等）增强对奇异值稀疏性的诱导能力，优于传统凸核范数。

#### （2）**鲁棒量化恢复模型（FCTN-GNRTC / FCTN-GNQRTC）**
- 将模型从传统的**未量化观测**扩展到**粗粒度量化观测**场景（uniformly dithered quantization），更贴近实际系统中的存储与传输限制。
- 支持处理含噪声、稀疏异常值及部分采样的退化数据。

#### （3）**加速化的随机压缩算法（Randomized Compression Algorithms）**
- 设计两种高效的随机压缩策略：
  - **Fixed-Rank Randomized Algorithm**：给定目标秩进行快速近似。
  - **Fixed-Accuracy Randomized Algorithm**：自适应确定秩以满足误差容限。
- 基于 sketching 技术（如 Gaussian、SRFT、Khatri-Rao 积等）实现维度约简，显著降低中间矩阵操作成本。
- 引入增量 QB 分解与移位幂迭代（shifted power iteration）提升精度与并行效率。

#### （4）**优化求解框架**
- 基于 **ADMM 框架**设计高效优化算法，并提供收敛性理论保证。
- 将随机压缩模块嵌入耗时的优化步骤中，作为**计算加速核心**。

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **建模能力** | 联合建模 L+S 结构，优于仅依赖低秩或平滑先验的方法；支持量化观测，更具实用性 |
| **计算效率** | 随机压缩使运行时间平均减少 **88%**，最高速度提升达 **20×** |
| **恢复质量** | 在多种指标上超越 state-of-the-art 方法，尤其在极低采样率下仍能保持良好视觉效果 |
| **通用性** | 可应用于图像、视频、遥感、MRI、人脸等多种多维数据 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验涵盖六大类真实世界高阶张量数据：

| 数据类型 | 示例 | 规模（典型） |
|--------|------|-------------|
| **Color Images (CIs)** | Maastricht-Bassin, Wildpark 等 | ~3000×4000×3 |
| **Color Videos (CVs)** | Rush-hour, Johnny 等 | 720×1280×3×50 |
| **Multi-Temporal RS Images (MRSIs)** | SPOT-53, Landsat-7 等 | 1000×1000×4×13 |
| **Hyperspectral Videos (HVs)** | Main5, Oranges1 等 | 240×240×33×31 |
| **Face Datasets** | Extended YaleFace B, UWA Hyperspectral Face | 192×168×64×38 / 256×256×30×33 |
| **Cardiac MRIs** | sol-yxzt-part 系列 | 256×256×14×20 |

所有原始张量均进行了归一化处理。

### 🎯 实验设置与评估指标

#### 评估指标
| 指标 | 含义 | 越大越好？ |
|-----|------|-----------|
| **MPSNR** | 平均峰值信噪比 | ✅ |
| **MSSIM** | 平均结构相似性 | ✅ |
| **MRSE** | 平均相对平方误差 | ❌ |
| **MTime** | 平均 CPU 时间（秒） | ❌ |

#### 采样与噪声设置
- **采样率（SR）**：从 0.1% 到 6%，模拟极端稀疏观测。
- **噪声类型**：
  - 加性高斯噪声（标准差 σ）
  - 稀疏脉冲噪声（pepper-and-salt，污染率 NR）
- **量化设置**：采用均匀抖动量化（uniformly dithered quantization），分辨率 δ ∈ {0.1, 0.2, ..., 0.5}

#### 对比的基线方法（共14种）
| 类别 | 方法列表 |
|------|---------|
| **TR/FCTN-based** | TRNNM, FCTN-NNM, FCTN-TC, FCTNFR |
| **T-SVD based** | HTNN, WSTNN, METNN, OTNN, t-e-LogDet |
| **其他低秩+平滑** | TCTV-TC, MTTD, EMLCP-LRTC, GTNN-HOC |

本文提出的三种变体：
- **FCTN-GNTC**：基础非凸补全模型
- **R1-FCTN-GNTC**：固定秩随机加速版
- **R2-FCTN-GNTC**：固定精度随机加速版

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总（代表性场景）

#### 表格 I：Face 数据集（SR=1%）
| 方法 | MPSNR ↑ | MSSIM ↑ | MRSE ↓ | MTime ↓ |
|------|--------|--------|-------|--------|
| **R1-FCTN-GNTC** | **31.56** | **0.836** | **0.123** | 6114 |
| FCTNFR | 28.84 | 0.696 | 0.157 | 11655 |
| TCTV-TC | 29.56 | 0.814 | 0.157 | 16857 |
| HTNN | 26.72 | 0.694 | 0.202 | 3493 |

> ✅ **R1 版本在精度全面领先的同时，速度是 TCTV-TC 的 2.7×，HTNN 的 1.7×**

#### 表格 II：MRI 数据集（SR=1%）
| 方法 | MPSNR ↑ | MSSIM ↑ | MRSE ↓ | MTime ↓ |
|------|--------|--------|-------|--------|
| **R2-FCTN-GNTC** | **32.06** | **0.858** | **0.239** | 2730 |
| FCTN-NNM | 29.24 | 0.838 | 0.322 | 8224 |
| TCTV-TC | 31.94 | 0.847 | 0.239 | 8224 |
| WSTNN | 27.35 | 0.640 | 0.391 | 1703 |

> ✅ **恢复精度最高且运行时间仅为 TCTV-TC 的 1/3**

#### 表格 III：MRSI 数据集（SR=5%）
| 方法 | MPSNR ↑ | MSSIM ↑ | MRSE ↓ | MTime ↓ |
|------|--------|--------|-------|--------|
| **R1-FCTN-GNTC** | **27.09** | **0.831** | **0.171** | 4195 |
| FCTN-NNM | 25.79 | 0.786 | 0.199 | 19016 |
| TCTV-TC | 27.87 | 0.850 | 0.157 | 4111 |
| GTNN-HOC | 24.98 | 0.736 | 0.220 | 3504 |

> ⚠️ TCTV-TC 精度略优但耗时长（~4.6×），而本文方法在精度接近前提下具备更好平衡

---

### 🔬 与其他方法对比结果

#### ✅ 定量优势
- 在 **极低采样率（如 0.1%~0.5%）** 下，本文方法 MPSNR 比 TRNN、FCTN-NNM 等高出 **3~6 dB**。
- 在多数任务中，**MSSIM 提升明显**，说明恢复图像具有更好的纹理与边缘保真度。
- **MRSE 显著低于大多数对比方法**，表明重建误差更小。

#### ✅ 视觉质量优势（见 Fig. 1–6）
- 在 Face、MRI、MRSI 等任务中，本文方法能更好地恢复细节、颜色一致性与空间连续性。
- 在严重缺失区域（如人脸遮挡、云层覆盖遥感图），仍能生成自然连贯的结果，而其他方法出现模糊或伪影。

#### ⚡ 计算效率突破（Fig. 3）
- **随机版本相比确定性版本平均提速 9×，个别案例高达 20×**
- 在 Color Video 和 MRI 恢复任务中，R1/R2 版本比原版快 **5~7 倍**
- 即便在不损失精度的情况下，CPU 时间下降约 **88%**

#### 🔍 消融实验分析（文中隐含验证）
虽然没有显式列出消融表，但从多个实验可推断：
- **引入 L+S 联合建模** → 显著优于仅用低秩或仅用平滑的方法（如 HTNN vs TCTV）
- **采用非凸正则项** → 比 l1 更好地促进稀疏性，提升恢复质量
- **加入随机压缩** → 几乎无精度损失，但极大加快运算速度

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **FCTN 分解结合梯度域非凸正则化** 是一种强大且灵活的建模范式，能够统一刻画多维数据的 **全局低秩性与局部平滑性**。
2. **量化感知建模** 是面向现实系统的必要扩展，本文通过抖动量化机制实现了统计一致性保障。
3. **随机 sketching 技术** 可作为张量恢复算法的“加速引擎”，在几乎不影响精度的前提下大幅降低计算负担。
4. 所提方法在 **极端稀疏采样、强噪声干扰、大规模张量** 场景下均表现出卓越鲁棒性和泛化能力。

### ⚠️ 方法的局限性
- **参数敏感性**：非凸函数选择、正则化系数 λ₁/λ₂、量化步长 δ 等需调参。
- **理论边界假设较强**：误差界分析依赖于 sketching 矩阵分布假设（如 Gaussian、SRFT），实际性能可能受数据结构影响。
- **硬件依赖**：尽管算法本身轻量，但在超大规模张量上仍需要较高内存支持（尽管已压缩）。

### 🔮 未来工作方向
1. **融合深度学习**：将所提张量先验嵌入神经网络架构，构建可学习的恢复模型。
2. **动态自适应压缩**：根据数据局部特征自动调整 sketch size 与 power iteration 次数。
3. **扩展至 one-bit 量化**：进一步研究极限低位宽下的恢复性能。
4. **跨模态应用**：推广至语音、文本、图信号等其他高维数据形式。

---

> 💡 **总结一句话**：  
> 本文提出了一套**兼具高建模表达力与高计算效率**的大规模多维视觉数据恢复框架 —— 通过 **FCTN-GNTCTV 非凸正则化 + 随机压缩加速**，在精度、速度、实用性三个维度全面超越现有方法，为下一代智能视觉系统提供了坚实的数学工具支撑。

</details>

---

### 4. [Synthetic Interaction Data for Scalable Personalization in Large Language Models](https://arxiv.org/abs/2602.12394)

**Authors**: Yuchen Ma, Yue Huang, Wenjie Wang, Xiaonan Luo, Xiangliang Zhang, Stefan Feuerriegel  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.12394v1  

#### Abstract
Personalized prompting offers large opportunities for deploying large language models (LLMs) to diverse users, yet existing prompt optimization methods primarily focus on task-level optimization while largely overlooking user-specific preferences and latent constraints of individual users. This gap ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Synthetic Interaction Data for Scalable Personalization in Large Language Models*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前大型语言模型（LLMs）的个性化对齐（personalized alignment）面临两大瓶颈：

1. **数据缺口（Data Gap）**：
   - 缺乏高质量、隐私敏感的多轮用户-LLM交互数据。
   - 真实用户反馈具有噪声、不完整、隐式表达等特点，难以直接用于训练。

2. **方法局限（Method Limitation）**：
   - 现有方法多依赖静态偏好建模或对模型参数进行微调（如 fine-tuning, PEFT），无法适用于闭源或冻结的 LLMs。
   - 黑盒优化方法（如 prompt rewriting）在个性化场景中尚未被充分探索。

---

### **提出了什么新方法或新思路**

本文提出了一套完整的解决方案，包含两个核心组件：

#### ✅ **PERSONAGYM：高保真合成数据生成框架**
- 将用户建模为一个**动态偏好过程**（dynamic preference process），而非静态 persona。
- 采用**多智能体 LLM 架构**（User, Assistant, Distractor）模拟真实交互轨迹。
- 引入**语义感知噪声机制**（semantic-aware noise），分三层注入现实中的表达模糊性：
  - **Layer 1**: 表层噪声（拼写错误、口语化）
  - **Layer 2**: 信息缺失（省略时间/预算等约束）
  - **Layer 3**: 语义歧义（意图冲突、自我矛盾）
- 支持从部分可观测的 persona 出发，逐步推断并演化出多轮对话历史。

#### ✅ **PPOpt：可扩展的个性化 Prompt 优化框架**
- 提出 **reason-then-optimize 范式**：
  1. 先从历史交互中推理出显式的自然语言用户画像（user profile）；
  2. 再基于该画像重写当前 prompt。
- 采用**冷启动监督预训练 + 多目标强化学习**（SFT + RL）联合训练：
  - 利用 outcome-driven reward（任务完成质量）避免 prompt hacking。
  - 加入 profile inference reward 提升推理稳定性。
- 完全黑箱操作，**无需修改部署的 LLM 参数**，兼容闭源模型。

---

### **相比现有方法的优势**

| 维度 | PPOpt 优势 |
|------|-----------|
| **Intervention Level** | Prompt-level，非侵入式 |
| **Data Scalability** | 高，通过 PERSONAGYM 可无限扩展合成数据 |
| **User Noise Modeling** | 显式建模噪声，提升鲁棒性 |
| **Interaction Dynamics** | 支持多轮动态交互建模 |
| **Closed-source Compatibility** | ✅ 支持所有 API 型 LLM |

> 如 Table 1 所示，PPOpt 是唯一同时满足高数据可扩展性、支持噪声建模、具备交互动态性和闭源兼容性的方法。

---

## 2. 核心实验方法和设置

### **使用的数据集**

#### 📦 合成数据来源：
- **PERSONAATLAS**：由 PERSONAGYM 生成的大规模合成数据集
  - 包含约 **2,000 个 persona** 和 **10,000+ 条多轮对话**
  - 基于多个公开任务数据集构建种子查询：
    - `ultrachat_200k`, `OpenR1-Math`, `CodeFeedback`, `Mol-Instructions`, `MMLU`, `ai2_arc` 等（见 Table 2）

#### 🔁 实验评估设置：
- **训练阶段**：使用 PERSONAATLAS 数据进行 SFT 和 RL 训练。
- **测试阶段**：
  - **合成基准测试集**：来自 held-out 的 `IFEval`, `oasst1` 等数据
  - **真实世界测试集**：通过人类专家编辑生成的高保真个性化交互数据（经 IRB 审批）

---

### **评估指标**

使用 **LLM-as-a-Judge** 方法（Judge 模型为 GPT-5.2）进行自动评分：

| 指标 | 描述 |
|------|------|
| **Personalization Score** | 回答是否符合用户偏好（语气、格式、风格、深度等） |
| **Task Completion Score** | 回答的任务正确性与完整性 |
| **AgreeRate%** | 人工标注者对 outcome label 的一致性比例 |

---

### **基线方法对比**

| 基线方法 | 简要说明 |
|--------|---------|
| **Vanilla Prompt** | 原始用户输入 |
| **History-augmented Prompting (Concat)** | 直接拼接历史记录作为上下文 |
| **Persona-based Query Rewriting** | 基于历史推断 persona 并附加到 prompt 中 |
| **Preference-based Few-shot ICL** | 构造偏好样本作为 in-context 示例 |
| **Controller-guided Prompting** | 使用控制器模型重写 prompt |

> 所有实验均在多种 LLM 上验证，包括 Llama-3, Qwen3, GPT-oss, Claude-Sonnet 等。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ 在合成数据上的表现（Table 6 & 7）

| 方法 | Personalization Score ↑ | Δ vs Vanilla | Task Completion ↓ |
|------|------------------------|-------------|--------------------|
| Vanilla | 5.41 | — | 8.48 |
| History-augmented | 6.94 | +1.53 | 7.89 (-0.59) |
| Few-shot ICL | 6.86 | +1.45 | 7.94 (-0.54) |
| **PPOpt (Ours)** | **7.20** | **+1.79 (+33.09%)** | **8.26 (-0.22)** |

> **结论**：PPOpt 在保持任务完成能力的同时，实现了最大的个性化增益。

#### ✅ 在真实世界数据上的表现（Table 9）

| 方法 | Personalization | Task Completion |
|------|------------------|------------------|
| Vanilla | 5.49 | 8.09 |
| **PPOpt** | **7.20** (+1.71) | **8.05** (-0.04) |

> 即使在真实用户数据上，PPOpt 仍显著优于基线，且几乎不影响任务性能。

---

### **跨模型泛化能力（Table 8）**

| Optimizer \ Deployed Model | Personalization Gain (Δ) |
|----------------------------|--------------------------|
| Llama-3 → GPT-4o-mini | +1.21 |
| Llama-3 → GPT-5.2 | +1.00 |
| Qwen3 → Claude-Sonnet | +0.72 |
| GPT-oss → GPT-5.2 | +0.43 |

> PPOpt 对不同 backbone 模型均有稳定提升，表明其良好的迁移性和通用性。

---

### **消融实验结果（Ablation Study, Figure 3）**

| 设置 | Personalization Score |
|------|------------------------|
| SFT only | ~6.7–7.0 |
| RL w/o Profile Reward | 不稳定，部分下降（如 Qwen3） |
| **Full PPOpt (SFT + RL + Profile Reward)** | **7.20 (最高且最稳)** |

> **关键发现**：profile inference reward 对稳定 RL 优化至关重要，防止 shortcut learning。

---

## 4. 关键结论和发现

### **主要发现**

1. **动态偏好建模优于静态 persona**  
   用户偏好是在交互中逐步显现的，不能简单视为固定标签。PERSONAGYM 成功捕捉了这一动态特性。

2. **合成数据可用于高质量个性化训练**  
   PERSONAATLAS 数据在多样性（Self-BLEU ↓, TTR ↑）、真实性（人类评估 Align@5=4.5+, Plaus@5=4.94）方面均优于主流数据集。

3. **reason-then-optimize 范式有效防止 reward hacking**  
   显式推理用户 profile 可引导 prompt 优化方向，避免仅靠表面改写欺骗 reward 函数。

4. **PPOpt 实现了个性化与任务性能的平衡**  
   相比其他方法以牺牲任务表现为代价换取个性化，PPOpt 在两者之间取得了更好权衡。

---

### **方法的局限性**

1. **依赖合成数据的质量**  
   尽管 PERSONAGYM 设计精细，但仍可能引入 LLM 生成偏差，需进一步验证在极端边缘案例下的表现。

2. **profile 推理仍具挑战性**  
   对于极稀疏的历史（如首次交互），profile 推理准确性受限。

3. **未考虑长期记忆更新机制**  
   当前框架假设 persona 固定，未实现真正的 lifelong personalization。

---

### **未来工作方向**

1. **结合真实用户反馈进行混合训练**  
   将合成数据与少量真实反馈结合，形成 semi-synthetic learning pipeline。

2. **构建可进化的用户画像系统**  
   支持跨会话的 persona 更新与长期记忆维护。

3. **扩展至多模态个性化场景**  
   将 PERSONAGYM 框架推广至图像、语音等多模态交互数据生成。

4. **开源生态建设**  
   推动 PERSONAATLAS 成为个性化研究的标准 benchmark，并鼓励社区贡献更多 persona schema。

---

> **项目资源**：
> - Dataset: [https://huggingface.co/datasets/HowieHwong/PPOpt-data](https://huggingface.co/datasets/HowieHwong/PPOpt-data)
> - Model: [https://huggingface.co/HowieHwong/ppopt](https://huggingface.co/HowieHwong/ppopt)
> - Docs: [https://personagym.readthedocs.io](https://personagym.readthedocs.io)
> - Code: [https://github.com/yccm/LLM-PPOpt](https://github.com/yccm/LLM-PPOpt)

</details>

---

### 5. [Wireless TokenCom: RL-Based Tokenizer Agreement for Multi-User Wireless Token Communications](https://arxiv.org/abs/2602.12338)

**Authors**: Farshad Zeinali, Mahdi Boloursaz Mashhadi, Dusit Niyato, Rahim Tafazolli  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12338v1  

#### Abstract
Token Communications (TokenCom) has recently emerged as an effective new paradigm, where tokens are the unified units of multimodal communications and computations, enabling efficient digital semantic- and goal-oriented communications in future wireless networks. To establish a shared semantic laten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Wireless TokenCom: RL-Based Tokenizer Agreement for Multi-User Wireless Token Communications*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文针对**多用户无线 Token 通信（Token Communications, TokenCom）**中的核心挑战——**Tokenizer Agreement（TA）**问题展开研究。在 TokenCom 中，发送端和接收端需事先就使用的 tokenizer/de-tokenizer 模型及码本（codebook）达成一致，以建立共享的语义隐空间。然而，在多用户下行链路场景中，这一过程需要与子信道分配、波束成形（beamforming）和资源调度联合优化，导致一个**混合整数非凸优化问题（mixed-integer non-convex problem）**。

传统优化方法因计算复杂度高、难以适应动态信道变化而受限，且现有基于强化学习（RL）的研究多集中于单用户或传统语义通信（SemCom），未考虑 TokenCom 所需的自适应 TA 过程。

---

### 🚀 提出的新方法与创新点
作者提出了一种**混合深度强化学习框架 DQN-DDPG**，用于实现多用户无线 TokenCom 中的**联合自适应 Tokenizer Agreement、子信道分配与波束成形**：

- **Hybrid DQN-DDPG 架构**：
  - 使用 **Deep Q-Network (DQN)** 处理离散动作空间：负责 **Tokenizer 选择** 和 **子信道（RB）分配**。
  - 使用 **Deep Deterministic Policy Gradient (DDPG)** 处理连续动作空间：负责 **波束成形向量（beamforming vectors）** 的生成。
  - 实现对混合动作空间的有效学习与协同优化。

- **联合优化建模**：
  将系统效用函数定义为语义质量（如 PSNR）与资源消耗（如功率、速率）之间的权衡目标，并通过 RL 动态最大化该效用。

- **首次探索多用户视频 TokenCom 场景**：
  利用最先进的预训练视频 tokenizer 模型（如 Cosmos、BSQ-VAE 等），支持跨模态、任务无关的高效语义传输。

---

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **适应性** | 能够根据实时信道状态和用户需求动态调整 tokenizer，优于固定或盲选策略。 |
| **效率** | 混合 RL 架构避免了传统优化中多次凸松弛迭代带来的高复杂度。 |
| **性能提升** | 显著提高语义重建质量（PSNR ↑）、降低视频卡顿率（freezing ↓）、提升资源利用率。 |
| **可扩展性** | 支持多用户、多天线系统，具备良好的可扩展能力。 |

---

## 2. 核心实验方法和设置

### 📊 数据集
- **DAVIS 视频数据集**：用于评估视频 TokenCom 性能。
- 视频帧率 $ p = 24 $ fps，分辨率涵盖 360p 至 1080p。

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| 基站天线数 $ N $ | 32 或 64 |
| 用户数 $ U $ | 4 或 16 |
| 资源块（RB）数量 $ R $ | 16 |
| 子载波间隔 | 30 kHz（符合 5G NR 标准） |
| 信道模型 | Rayleigh 块衰落（block fading） |
| 每 episode 时间步 $ T $ | 100 |
| 总训练 episodes | 500 |
| 功率预算 $ P_{\text{BS}} $ | 30 dBm |
| 最大每 RB 用户数 $ K $ | 2 |

### 🎯 评估指标
- **平均 PSNR**：衡量视频重建质量（越高越好）
- **视频冻结率（Freezing Rate, %）**：当传输速率低于所需码率时发生卡顿的比例（越低越好）
- **系统效用（System Utility）**：综合语义质量和资源开销的目标函数
- **收敛稳定性与训练速度**

### 🆚 基线方法对比
1. **DDPG-TA**：使用 DDPG 实现离散化输出进行 TA，缺乏对离散决策的精确建模。
2. **Agnostic-TA**：所有用户使用相同 tokenizer，忽略用户间信道异质性。
3. **Fixed-TA**：在整个通信过程中固定 tokenizer，无自适应能力。
4. **Conventional（H.265）**：传统数字通信方案，采用 H.265 编码 + DQN-DDPG 资源分配作为基准。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Fig. 1）

#### ✅ 冻结率（Freezing Rate）
- 在 $ U=16 $、1080p 高清视频下，相比 H.265 传统方案，所提方法将**视频冻结率降低约 68%**。
- 即使用户数量从 4 增加到 16，冻结率仅轻微上升，表现出良好可扩展性。
- Agnostic-TA 和 Fixed-TA 出现明显振荡和持续高冻结现象，说明缺乏自适应机制严重影响体验。

#### ✅ PSNR 表现
- 在 $ U=4, N=64 $ 条件下，所提方法比 H.265 基线**平均高出约 10 dB PSNR**。
- PSNR 随发射功率增加而稳定上升，且在各种天线配置下均显著优于所有基线。
- Fixed-TA 的 PSNR 恒定偏低，因其只能按最差信道条件选择 tokenizer。

#### ✅ 收敛性与稳定性
- 所提 Hybrid DQN-DDPG 框架在约 200 个 episode 后趋于稳定，收敛速度快。
- 相比 DDPG-TA，DQN 对离散动作的建模更准确，避免了离散决策抖动问题。

---

### 🔍 消融分析（隐含在对比实验中）
虽然未明确列出消融实验表格，但从基线设计可推断以下结论：
- **移除 TA 自适应性（Fixed/Agnostic-TA）→ 性能显著下降** → 证明自适应 TA 的必要性。
- **使用纯 DDPG 处理离散动作（DDPG-TA）→ 冻结率更高、收敛不稳定** → 证明混合架构优于单一连续控制。
- **不使用 TokenCom（Conventional）→ 语义效率低下、卡顿严重** → 体现 TokenCom 在超低比特率下的优势。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Tokenizer Agreement 是多用户 TokenCom 成功的关键环节**，必须与资源分配联合优化。
2. **混合 DQN-DDPG 框架能有效处理混合动作空间**，实现离散（TA、RB 分配）与连续（beamforming）决策的协同学习。
3. **自适应 TA 显著提升语义质量与用户体验**，特别是在高分辨率、多用户场景下表现突出。
4. **TokenCom 相比传统 H.265 可大幅减少视频冻结事件（↓68%）并提升重建质量（↑~10dB PSNR）**，验证其在资源受限环境下的巨大潜力。

---

### ⚠️ 局限性
1. **依赖预训练 tokenizer 池**：要求 BS 和用户设备预先部署多个兼容的 tokenizer/de-tokenizer 模型，可能带来存储与管理开销。
2. **集中式训练位于基站**：虽假设 BS 具备强大算力，但在边缘部署时可能存在延迟与隐私问题。
3. **未考虑上行链路反馈开销**：TA 初始化阶段需用户上报可用 de-tokenizer 列表，实际中需考虑信令成本。
4. **仿真基于理想 CSI 获取**：未建模信道估计误差或反馈延迟的影响。

---

### 🔮 未来工作方向
1. **轻量化 tokenizer 下载机制**：支持按需缓存或动态加载 tokenizer 模型。
2. **分布式/联邦式 TA 学习**：保护用户隐私的同时实现协同优化。
3. **跨模态 TokenCom 扩展**：将框架推广至图像、音频、文本等多模态融合通信。
4. **引入 MLLMs 进行上下文感知 TA**：利用 Multimodal Large Language Models 实现更智能的任务导向语义传输。
5. **硬件原型验证**：在真实 testbed 上验证端到端延迟与能耗表现。

---

> **总结一句话**：  
> 本文开创性地提出了面向多用户无线 Token 通信的 **Hybrid DQN-DDPG 框架**，实现了高效的自适应 Tokenizer Agreement 与资源联合优化，在大幅提升语义质量的同时显著降低了视频卡顿率，为未来 AI-native 无线网络提供了重要技术路径。

</details>

---

### 6. [ADEPT: RL-Aligned Agentic Decoding of Emotion via Evidence Probing Tools -- From Consensus Learning to Ambiguity-Driven Emotion Reasoning](https://arxiv.org/abs/2602.12714)

**Authors**: Esther Sun, Bo-Hao Su, Abinay Reddy Naini, Shinji Watanabe, Carlos Busso  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.12714v1  

#### Abstract
Speech Large Language Models (SLLMs) enable high-level emotion reasoning but often produce ungrounded, text-biased judgments without verifiable acoustic evidence. In contrast, self-supervised speech encoders such as WavLM provide strong acoustic representations yet remain opaque discriminative model...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ADEPT: RL-Aligned Agentic Decoding of Emotion via Evidence Probing Tools —— 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

传统 **Speech Emotion Recognition (SER)** 存在两大核心挑战：

- **Consensus Paradox**：主流方法依赖多数投票（plurality-based consensus）生成单一标签，将标注者分歧视为“噪声”而丢弃，导致情感复杂性和共现性（co-occurrence）被系统性忽略。
- **信号-语义鸿沟（Signal-Semantics Gap）**：
  - **SSL 模型**（如 WavLM）虽有强声学表征能力，但为黑箱判别模型，缺乏可解释性。
  - **Multimodal LLMs (MLLMs)** 能生成自然语言推理，但常脱离可验证的声学证据，产生“幻觉”（hallucination）。

因此，现有方法要么准确但不可信，要么可解释但不接地（ungrounded）。

---

### 提出的新方法与新思路

作者提出 **ADEPT**（Agentic Decoding of Emotion via Probing Tools），一个基于 **agentic AI** 和 **Explicit Information Retrieval (EIR)** 的框架，实现从 **consensus learning** 到 **ambiguity-driven emotion reasoning** 的范式转变。

#### 核心创新点：

1. **多轮代理式推理框架（Multi-turn Agentic Framework）**
   - 将 SER 视为一个动态的、多步的“调查过程”，而非单次分类。
   - 分为三个阶段：
     - **Phase 1: Global Perception**（高召回假设生成）
     - **Phase 2: Evidence Verification**（自适应证据收集）
     - **Phase 3: Decision Synthesis**（证据闭合裁决）

2. **结构化探测工具包（Structured Probing Toolkit）**
   - 定义四类可审计的工具，返回可观测证据而非隐含特征：
     - **Structural Prior Tool**：用于预算分配与候选回溯
     - **Semantic Probing Tool**：基于 Scherer 的 CPM 模型进行文本线索验证
     - **Acoustic Probing Tool**：通过 Dual-Reference Bucketing 输出可解释的声学描述（如 High Pitch）
     - **Refinement Tool**：双向重查机制（replay audio / re-check semantics）处理跨模态冲突

3. **Ambiguity-Driven Supervision**
   - 不再将少数标注视为噪声，而是作为 **minor emotions** 的监督信号。
   - 显式建模 **tie cases**（平票主情绪）和 **minor emotions**，保留人类感知的多样性。

4. **Group Relative Policy Optimization (GRPO) + Evidence Trust Gate**
   - 使用 **GRPO** 对齐工具使用行为与预测质量。
   - 引入 **Evidence Trust Gate (TEIR)** 动态调节证据相关奖励权重，防止策略通过无效工具调用“刷分”。

---

### 相比现有方法的优势

| 维度 | 传统方法 | ADEPT |
|------|--------|-------|
| **标签建模** | 单一主情绪（winner-takes-all） | 主情绪 + 次要情绪 + 平票状态 |
| **推理方式** | 静态分类或后见解释 | 多轮主动证据探查 |
| **可解释性** | 后见叙事（post-hoc narrative） | 可审计的证据链（audit trail） |
| **证据对齐** | 弱或无 | 显式耦合工具行为与预测质量 |
| **对歧义的态度** | 抑制歧义以提升一致性 | 利用歧义指导推理资源分配 |

---

## 2. 核心实验方法和设置

### 使用的数据集

- **MSP-Podcast Corpus V2.0**（主要训练与测试集）
  - 自然主义口语情感数据集，来自播客录音。
  - 每段音频由至少 5 名标注者打标。
  - 包含 8 类基本情绪：`Anger`, `Sadness`, `Happiness`, `Surprise`, `Fear`, `Disgust`, `Contempt`, `Neutral`。
  - 数据高度不平衡且存在大量标注分歧（约 18% 为平票案例）。

- **IEMOCAP Corpus**（零样本迁移测试）
  - 用于评估跨域泛化能力。
  - 使用映射后的 7 类标签（合并 Excited → Happiness，排除 Frustrated/Other）。
  - 采用相同标签构建协议提取 primary 和 minor emotions。

---

### 实验设置

- **基础模型**：`Qwen-3-Omni`（多模态大模型）
- **训练方法**：使用 **GRPO** 进行强化学习优化
  - 每个 prompt 采样 K=8 条轨迹
  - 批大小 256，训练 2 个 epoch（共 2.56M 轨迹）
- **工具执行**：所有工具调用均受控于系统提示（system prompt）与格式约束

---

### 评估指标

| 指标 | 描述 |
|------|------|
| **Primary Macro-F1 (P-MacroF1)** | 主情绪分类的宏平均 F1，严格匹配 |
| **Soft Recall (Soft R)** | 放松的主情绪召回率，允许预测集中包含真实主情绪 |
| **Set Recall (Set R)** | 成功预测到任意真实情绪（主或次）的比例 |
| **Jaccard Index (IoU)** | 预测集与真实集的交并比，衡量集合相似度 |
| **Average Cardinality** | 平均预测情绪数量，监控是否过度膨胀 |

---

### 基线方法对比

分为三组：

1. **SSL 分类器（仅主情绪）**
   - HuBERT, wav2vec2.0, WavLM（冻结/微调）

2. **生成式 SLLM/MLLM（支持主+次情绪）**
   - SALMONN, Qwen-2-Audio, BLSP-Emo

3. **消融变体**
   - Qwen-3-Omni（无工具）
   - ADEPT w/o GRPO
   - ADEPT + GRPO（完整版）

---

## 3. 主要实验结果和性能指标

### 在 MSP-Podcast Test1 上的结果（Table 1）

| Method | P-MacroF1 ↑ | Soft R ↑ | Set R ↑ | Jaccard ↑ | Avg Size |
|--------|-------------|----------|---------|-----------|----------|
| WavLM (Fine-tuned) | 0.3640 | – | – | – | – |
| BLSP-Emo | 0.2915 | 0.6740 | 0.5320 | 0.4280 | 2.08 |
| Qwen-3-Omni | 0.2358 | 0.6003 | 0.4923 | 0.3890 | 2.02 |
| **ADEPT (w/o GRPO)** | **0.3571** | **0.7032** | **0.5748** | **0.4450** | 2.15 |
| **ADEPT + GRPO** | **0.4224** | **0.7874** | **0.6192** | **0.4751** | 2.21 |

> ✅ **关键发现**：
> - ADEPT + GRPO 比最强 SSL 基线（WavLM-ft）提升 **+0.0584 P-MacroF1**。
> - 在 Set Recall 和 Jaccard 上显著优于所有生成式基线，说明其更擅长捕捉共现情绪。
> - 平均预测数仅略增，表明性能提升源于精准消歧，而非盲目扩展标签。

---

### 消融实验结果（Table 2）

| Variant | P-F1 | Soft R | Set R | Jaccard |
|--------|------|--------|--------|---------|
| Omni3 (no tools) | 23.58 | 60.03 | 49.23 | 38.90 |
| ADEPT (w/o GRPO) | 35.71 | 70.32 | 57.48 | 44.50 |
| ADEPT (Full) | **42.24** | **78.74** | **61.92** | **47.51** |
| w/o Trust Gate | 40.63 | 77.82 | 60.73 | 44.96 |
| w/o Acoustic | 34.52 | 75.21 | 59.08 | 41.28 |
| w/o Semantic | 33.86 | 75.93 | 60.06 | 42.31 |
| w/o Replay | 40.44 | 77.06 | 60.47 | 46.18 |

> ✅ **关键发现**：
> - 加入工具协议带来巨大增益（+12.13 P-F1），GRPO 再提升 +6.53。
> - 移除 **Acoustic Probing** 导致 Jaccard 下降最严重，说明局部声学证据对解决歧义至关重要。
> - 移除 **Trust Gate** 明显降低性能，证明其有效防止“工具滥用”。
> - 所有组件协同作用，无单一主导因素。

---

### 零样本迁移结果（IEMOCAP, Table 3）

| Method | Acc ↑ | Soft R ↑ | Set R ↑ | Jaccard ↑ |
|--------|--------|----------|---------|-----------|
| BLSP-Emo (fine-tuned) | **76.13** | 80.51 | 52.30 | 48.90 |
| Qwen-3-Omni | 42.37 | 51.12 | 35.24 | 31.08 |
| **ADEPT + GRPO** | 54.83 | **67.42** | **54.80** | **43.15** |

> ✅ **关键发现**：
> - 尽管是 **zero-shot**，ADEPT 在 **Set Recall** 上超越全量微调的 SOTA（54.80 > 52.30）。
> - 表明 ADEPT 的证据驱动机制具有更强的跨域鲁棒性，不依赖于隐特征拟合。

---

### 工具使用分析（Figure 4）

- **工具调用次数随标注歧义增加而上升**：
  - 高共识（1标签）：平均 2.3 次
  - 中共识（2–3标签）：4.1 次
  - 低共识（≥4标签）：6.8 次
- **低共识样本中，`compare_emotions`、`StructuralPriorTool`、`replay_audio` 使用频率显著提高**

> 🔍 说明 ADEPT 实现了 **on-demand computation under uncertainty**，智能分配推理预算。

---

## 4. 关键结论和发现

### 主要发现

1. **从“消除歧义”到“利用歧义”**  
   将标注分歧（disagreement）作为 **informative signal** 而非噪声，能更真实地建模人类情感的复杂性与共现性。

2. **agentic 推理优于静态分类**  
   多轮、主动的证据探查机制显著提升了对共现情绪的识别能力，并生成可追溯的解释路径。

3. **证据必须可审计且可验证**  
   通过 **EIR constraint** 和 **Evidence Trust Gate**，确保所有判断都建立在明确、可观测的证据之上，避免 LLM 幻觉。

4. **工具使用需与任务目标对齐**  
   GRPO + Trust Gate 成功将工具调用行为与预测质量绑定，防止策略“走形式”式调用工具。

---

### 方法的局限性

1. **计算开销较高**  
   多轮工具调用引入延迟，不适合实时性要求极高的场景。

2. **依赖高质量工具实现**  
   若声学/语义工具本身不准，会传导错误至最终决策。

3. **当前仍限于封闭类别（closed-set）**  
   未探索开放词汇（open-vocabulary）或连续维度（dimensional）情感空间。

4. **对长对话建模有限**  
   当前基于短话语设计，难以直接应用于长对话中的动态情感演变。

---

### 未来工作方向

1. **扩展至开放域情感理解**  
   结合 concept discovery 与 probing tools，支持 novel emotion detection。

2. **端到端可微分工具集成**  
   探索 soft probing 或 differentiable retrieval 以降低推理成本。

3. **多模态联合推理扩展**  
   将视觉线索纳入 probing toolkit，构建视听情感探查代理。

4. **人机协作诊断接口**  
   利用 audit trail 构建可视化调试界面，供临床或客服专家审查系统决策。

5. **伦理部署框架**  
   设计透明度报告模板与偏见检测模块，推动负责任的情感 AI 应用。

---

> 📌 **总结一句话**：  
> ADEPT 通过 **agentic 推理 + structured probing + ambiguity-aware supervision**，实现了从“黑箱预测”到“可审计推理”的跨越，在提升主情绪识别精度的同时，显著增强了对共现情绪的刻画能力与系统可信度。

</details>

---

### 7. [Resource-Efficient Gesture Recognition through Convexified Attention](https://arxiv.org/abs/2602.13030)

**Authors**: Daniel Schwartz, Dario Salvucci, Yusuf Osmanlioglu, Richard Vallett, Genevieve Dion, Ali Shokoufandeh  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13030v1  

#### Abstract
Wearable e-textile interfaces require gesture recognition capabilities but face severe constraints in power consumption, computational capacity, and form factor that make traditional deep learning impractical. While lightweight architectures like MobileNet improve efficiency, they still demand thous...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Resource-Efficient Gesture Recognition through Convexified Attention

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
可穿戴电子纺织品（e-textiles）需要具备手势识别能力，但由于其在**功耗、计算能力和形态因素**上的严格限制，传统的深度学习模型（如CNN、Transformer）难以部署。现有轻量级架构（如MobileNet、SqueezeNet）虽然减少了参数量，但仍需数千个参数，且缺乏收敛保证，不适合资源极度受限的微控制器平台。

此外，传统注意力机制（attention）虽能提升性能，但引入了非凸的softmax操作和交叉熵损失，破坏了优化过程的全局收敛性，并进一步增加参数量，使其不适用于e-textile系统。

### 提出了什么新方法或新思路
本文提出了一种**凸优化框架下的注意力机制（Convexified Attention）**，用于可穿戴设备上的高效手势识别。其核心创新包括：

- **凸化注意力机制（Convexified Attention）**：
  - 用**非扩张的欧几里得投影（nonexpansive Euclidean projection）** 替代非凸的 **softmax**，将注意力权重约束在概率单纯形（probability simplex）上。
  - 使用**多类 hinge loss** 或 **squared loss** 替代非凸的 cross-entropy loss，确保整个优化问题是**端到端凸的（end-to-end convex）**。
- **低秩正则化**：采用 **nuclear norm regularization** 进一步压缩有效参数数量。
- **随机傅里叶特征（RFF）**：通过RFF近似RBF核函数，在保持非线性表达能力的同时维持线性模型结构。

该方法被集成于 **Convexified Convolutional Neural Network (CCNN)** 框架中，实现了动态特征加权与数学可分析性的统一。

### 相比现有方法的优势
- ✅ **极高的参数效率**：仅需 **120–360 参数**，相比MobileNet等轻量模型减少 **97%**。
- ✅ **全局收敛保证**：由于问题为凸，无论初始化如何，均能收敛至**全局最优解**，避免局部极小值问题。
- ✅ **超低延迟推理**：在Arduino Nano 33 BLE上实现 **290–296 μs** 推理时间，支持 >1000Hz 实时交互。
- ✅ **极小存储开销**：<7KB 存储占用，适合嵌入式Flash。
- ✅ **无需外部处理**：可在e-textile内部直接完成训练与推理，无需连接云端或手机。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 自建的**纺织电容传感器数据集**，基于一个 **4"×4" 针织导电织物**，带有四个角落电极。
- 数据由**单名参与者**在受控实验室环境（温度22°C，湿度45%）下采集。
- 包含两类手势：
  - **Tap gestures**：短按，持续约40ms，共4类（对应四个位置）。
  - **Swipe gestures**：滑动，持续约120ms，共4类（上下左右）。
- 每类手势采集100个样本，总计 **400个tap + 400个swipe** 样本。
- 输入格式：`X ∈ ℝ^(C×T)`，其中 `C=4`（通道数），`T=10`（tap）或 `T=30`（swipe）。

### 实验设置和评估指标
- **硬件平台**：Arduino Nano 33 BLE（ARM Cortex-M4 @64MHz，256KB SRAM，1MB Flash）。
- **预处理**：
  - 基线漂移补偿
  - 小波去噪（Symlet wavelet）
  - 移动平均平滑
  - Z-score归一化
- **评估方案**：
  1. **分层10折交叉验证（stratified 10-fold CV）**
  2. **60-20-20 划分（train-val-test）**，超参数通过贝叶斯优化在验证集上调优。
- **评估指标**：
  - 分类准确率（Accuracy）
  - Macro F1-score
  - 推理延迟（Latency, μs）
  - 模型大小（参数数、存储字节数）

### 基线方法对比
| 模型 | 类型 |
|------|------|
| Traditional CNN | 常规卷积网络 |
| Traditional Attention | 加入标准注意力的CNN |
| MobileNet | 轻量级CNN |
| SqueezeNet | 极简CNN |
| Convex ViT | 凸化Vision Transformer |
| Convex CNN (no attention) | 无注意力的CCNN |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | 我们的方法（Convex Attention） |
|------|-------------------------------|
| **Tap Accuracy** | **100.00% ± 0.00%** |
| **Swipe Accuracy** | **100.00% ± 0.00%** |
| **F1 Score (Tap/Swipe)** | **100.00% / 100.00%** |
| **参数数量** | **120 (tap) / 360 (swipe)** |
| **存储占用** | **3.9 KB / 6.9 KB** |
| **推理延迟** | **296.35 μs / 290.31 μs** |
| **跨验证稳定性** | **零方差（所有fold一致）** |

> 注：所有结果在 **10-fold CV** 和 **held-out test** 上完全一致，体现凸优化的稳定收敛特性。

### 与基线方法的对比结果

#### 表：准确率对比（10-fold CV 平均 ± 标准差）

| Model | Tap Accuracy (%) | Swipe Accuracy (%) |
|-------|------------------|--------------------|
| Traditional CNN | 99.63 ± 0.60 | 98.50 ± 2.11 |
| MobileNet | 77.50 ± 12.30 | 99.17 ± 2.63 |
| SqueezeNet | 50.83 ± 19.91 | 49.17 ± 15.93 |
| Convex ViT | 75.75 ± 7.12 | 40.75 ± 8.90 |
| Convex CNN (no attn) | 96.25 ± 2.84 | 98.75 ± 1.77 |
| **Ours (Convex Attention)** | **100.00 ± 0.00** | **100.00 ± 0.00** |

- 在 **tap任务** 上，我们的方法比Convex CNN提升 **3.75%**，且稳定性显著更高（σ=0）。
- MobileNet在tap上表现极差（<80%），说明图像架构不适用于低维时序信号。
- Convex ViT表现最差，表明高维patch设计不适合纺织传感器的低维输入。

#### 推理效率对比（Arduino平台）

| Model | Latency (μs) | Storage (bytes) |
|-------|--------------|-----------------|
| Traditional CNN | 1584 / 1590 | 59.6K / 124.5K |
| MobileNet | 753 / 761 | 336K / 336K |
| Convex CNN | 294 / 292 | ~4K |
| **Ours** | **296 / 290** | **3.9K / 6.9K** |

- 我们的模型比传统CNN快 **5.3倍**，比MobileNet快 **2.6倍**。
- 存储需求仅为传统模型的 **1/15 至 1/50**。

### 消融实验结果
- **有无注意力机制对比**：
  - 无注意力的Convex CNN：**96.25% tap accuracy**
  - 加入凸注意力后：**100.00% accuracy**
  - ➜ 注意力机制带来 **+3.75% 绝对增益**，证明其对动态加权时序特征的有效性。
- **超参数敏感性测试**：
  - 即使使用次优超参数，注意力模型仍保持 >99% 准确率。
  - 表明该方法对初始化和调参鲁棒性强，适合在设备上进行在线学习。

---

## 4. 关键结论和发现

### 主要发现
1. **凸优化可用于构建高效的注意力机制**：通过替换softmax为simplex projection，结合hinge loss，可以在保持动态特征选择能力的同时实现**端到端凸性**。
2. **极低参数量也能达到完美性能**：仅用 **120参数** 即可在特定任务上实现 **100% 准确率**，挑战了“更多参数=更好性能”的传统认知。
3. **凸性带来卓越稳定性**：模型在不同数据划分下表现完全一致（零方差），体现了**全局收敛**的实际优势。
4. **注意力机制显著提升性能**：相比无注意力的CCNN，准确率提升近4%，说明动态时间加权对纺织传感至关重要。
5. **可在MCU上完成全流程**：从训练到推理均可在Arduino等微控制器上运行，真正实现**on-device ML**。

### 方法的局限性
- **单用户数据**：实验仅基于一名参与者，泛化能力有待多用户验证。
- **受控环境**：未考虑真实场景中的噪声、汗液、衣物摩擦、身体运动等干扰。
- **简单手势集**：仅支持tap和swipe四向，尚未扩展至复杂手势（如捏合、旋转、压力感知）。
- **静态分割假设**：依赖预定义的onset/offset检测，未实现连续手势检测（continuous spotting）。
- **缺乏长期鲁棒性测试**：未评估洗涤、拉伸、电极老化对性能的影响。

### 未来工作方向
- **多用户与跨个体泛化研究**：收集多样化人群数据，探索个性化适配机制。
- **扩展手势词汇表**：支持更复杂的交互模式，如多指手势、压力级别、圆形轨迹等。
- **在线学习与自适应**：利用凸优化的快速收敛特性，实现在设备上的实时模型更新。
- **连续手势检测**：开发基于凸优化的滑动窗口或流式检测算法。
- **多模态融合**：结合IMU、压力传感器等其他信号源，提升鲁棒性。
- **真实场景部署验证**：开展in-the-wild研究，评估日常穿着条件下的可用性。
- **制造工艺集成**：推动该技术与标准服装生产流程结合，实现大规模应用。

---

> **总结一句话**：  
> 本文通过**凸化注意力机制**，首次在仅需 **120参数** 和 **亚毫秒延迟** 的条件下，实现了 **100% 手势识别准确率**，为**可穿戴智能纺织品**提供了数学严谨、资源极致高效的机器学习解决方案。

</details>

---

### 8. [Discovering Semantic Latent Structures in Psychological Scales: A Response-Free Pathway to Efficient Simplification](https://arxiv.org/abs/2602.12575)

**Authors**: Bo Wang, Yuxuan Zhang, Yueqin Hu, Hanchao Hou, Kaiping Peng, Shiguang Ni  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.12575v1  

#### Abstract
Psychological scale refinement traditionally relies on response-based methods such as factor analysis, item response theory, and network psychometrics to optimize item composition. Although rigorous, these approaches require large samples and may be constrained by data availability and cross-cultura...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Discovering Semantic Latent Structures in Psychological Scales: A Response-Free Pathway to Efficient Simplification

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统心理量表简化（scale simplification）依赖于**response-based methods**（如 CFA、EFA、IRT、network psychometrics），这些方法需要大规模被试样本，存在以下瓶颈：
- 数据获取成本高，尤其在跨文化研究、早期量表开发或数据稀缺场景；
- 易受样本偏差、文化差异和测量不可比性影响；
- 忽视了**item文本本身蕴含的语义冗余与结构信息**。

本文提出：**量表项目（items）的语义结构本身就编码了潜在构念（latent constructs）的组织方式**，因此可以在无被试响应数据的前提下，通过分析 item 文本实现高效且理论一致的量表简化。

### 提出了什么新方法或新思路
提出了一种**基于主题建模（topic modeling）的无响应（response-free）量表简化框架**，其核心流程如下：

1. **语义编码（Encoding）**  
   使用大语言模型（LLM）生成每个 item 的 contextual sentence embeddings（如 `qwen3-embedding-4b`）。

2. **降维与密度聚类（Dimensionality Reduction & Clustering）**  
   使用 UMAP 进行降维，再用 HDBSCAN 进行密度聚类，自动发现语义簇（semantic clusters），无需预设因子数量。

3. **主题建模与关键词提取（Topic Modeling）**  
   对每个簇进行 c-TF-IDF 分析，提取代表性关键词，形成可解释的“语义因子”（semantic factors）。

4. **层级合并与代表项选择（Hierarchical Merging & Representative Item Selection）**  
   合并语义相近的簇，并基于**语义成员概率（membership probability）** 选择最具代表性的项目。

5. **可视化与工具支持**  
   提供集成的一键式 GUI 工具（One-Click Tool），支持非技术背景研究者使用。

### 相比现有方法的优势

| 方法类别 | 是否需要响应数据 | 可解释性 | 是否需预设维度 | 主要输出 |
|--------|------------------|---------|----------------|----------|
| CTT/FA/IRT | 是 ✅ | 高 | 是 ❌ | 短版量表 + 因子模型 |
| EGA / Network Psychometrics | 是 ✅ | 中高 | 否 | 短版量表 + 图结构 |
| Supervised ML（如 SVM, RF） | 是 ✅ | 低 | 隐含是 | 预测性短表 |
| 固定 K 聚类（如 K-means） | 否 ❌ | 中 | 是 ❌ | 压缩项目集 |
| **本文方法** | **否 ❌** | **高 ✅** | **否 ✅（可选）** | **语义结构 + 短表** |

**优势总结**：
- **响应无关（response-free）**：仅需 item 文本即可操作，适用于数据未收集前的阶段；
- **自动确定维度数**：HDBSCAN 自动识别语义簇数量，避免主观设定；
- **高可解释性**：通过关键词和可视化呈现语义结构，便于专家审查；
- **理论一致性**：保留原始因子结构，支持后续 psychometric validation；
- **开源工具支持**：提供 `sem-scale` 工具包，降低使用门槛。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个广泛使用的心理量表上进行验证：

| Scale | 构念领域 | 原始题项数 | 因子数 | 数据来源 |
|-------|----------|------------|--------|-----------|
| **DASS** | 负面情绪（Depression, Anxiety, Stress） | 42 | 3 | OpenPsychometrics.org (n=39,775) |
| **IPIP-50** | 人格特质（Big Five） | 50 | 5 | OpenPsychometrics.org (n=1,015,341) |
| **EPOCH-CN** | 青少年幸福感（Well-being） | 20 | 5 | Zeng et al. (2019a,b) (n=17,854) |

> 注：所有反向计分题项均在输入前重写为正向表述，以避免表面语言极性干扰。

### 实验设置
- **输入**：仅 item 文本（无任何响应数据）
- **编码模型**：`qwen3-embedding-4b`（开源 LLM embedding）
- **降维**：UMAP (`n_neighbors=3`, `n_components=5`)
- **聚类**：HDBSCAN (`min_cluster_size=2`, `min_samples=1`)
- **主题建模**：BERTopic with c-TF-IDF
- **代表项选择**：基于 membership probability 排序，每簇保留若干高概率项

### 评估指标

#### Psychometric Evidence（心理测量证据）
- **结构效度**：CFA 模型拟合指数（CFI, TLI, RMSEA, SRMR）
- **内部一致性**：Cronbach’s α（总量表与各因子）
- **项目质量**：Corrected Item-Total Correlation (CITC)

#### Methodological Evidence（方法学证据）
- **语义簇与理论因子对齐**：Adjusted Rand Index (ARI)
- **信息保真度**：
  - 子量表相关矩阵的 Frobenius 相似度
  - 全量表与短量表之间的 cross-form 子量表相关（convergent vs discriminant）
- **可视化**：t-SNE + 凸包（convex hull）与协方差椭圆（covariance ellipse）

#### 稳定性分析
- 参数扰动（如 `n_neighbors`, `min_cluster_size`）下，使用 Jaccard 相似度评估所选项目集合的稳定性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| Scale | 缩减比例 | 保留题项数 | CFI (短表) | TLI (短表) | RMSEA (短表) | Cronbach’s α (总体) | ARI (语义 vs 理论) |
|-------|----------|-------------|------------|------------|---------------|------------------------|----------------------|
| DASS | ~71% ↓ | 12 | 0.956 | 0.943 | 0.064 | 0.899 | 0.745 |
| IPIP | 60% ↓ | 20 | 0.860 | 0.833 | 0.059 | 0.705 | 0.855 |
| EPOCH | 50% ↓ | 10 | 0.983 | 0.970 | 0.041 | 0.875 | 1.000 |

> 平均缩减率达 **60.5%**，同时保持良好的 psychometric adequacy。

### 与基线方法的对比结果
文中未直接运行其他 ML 或 psychometric 基线进行 head-to-head 对比，而是通过**文献回顾与 Table 1 总结性比较**表明：
- 本文方法在 **无需响应数据** 的前提下，实现了与传统方法相当甚至更优的结构恢复能力；
- 在可解释性和自动化程度上显著优于监督学习或固定聚类方法；
- 特别是在 EPOCH 上达到 ARI=1.00，说明语义结构完全匹配理论因子。

### 消融实验结果（参数敏感性分析）

#### （1）每主题保留题项数的影响（IPIP）
| 每主题保留题项数 | CFI | TLI |
|------------------|-----|-----|
| 3 | 0.857 | 0.812 |
| **4** | **0.860** | **0.833** |
| 5 | 0.808 | 0.782 |
| 6 | 0.821 | 0.803 |
| ≥7 | 下降趋势 |

👉 结论：**保留 3–4 项/主题即可获得最优模型拟合**，过多反而因引入边缘项目而降低语义一致性。

#### （2）主题数量设置的影响
| 主题数（nr_topics） | CFI | TLI |
|--------------------|-----|-----|
| 3 | 0.864 | 0.838 |
| 4 | 0.845 | 0.815 |
| **5** | **0.875** | **0.852** |
| 6 | 0.860 | 0.833 |
| auto | 0.857 | 0.830 |

👉 结论：**当主题数接近真实因子数（5）时，模型拟合最佳**，验证了“语义结构反映理论结构”的假设。

#### （3）参数扰动下的稳定性（Jaccard 相似度）
| Scale | 平均 Jaccard 相似度 | 最低值 | 观察 |
|-------|--------------------|--------|------|
| DASS | ~0.50 | 0.20 | 稳定性较高，语义同质性强 |
| IPIP | ~0.45 | 0.25 | 语义异质性导致更多变化 |
| EPOCH | 0.429（恒定） | 0.429 | 6/10 项稳定，4 项竞争性替代 |

👉 结论：**核心代表项具有较强稳定性**，部分边缘项存在合理替代空间。

---

## 4. 关键结论和发现

### 主要发现
1. **语义结构能有效逼近心理测量结构**  
   即使没有响应数据，仅从 item 文本出发也能恢复出与理论因子高度一致的语义簇（ARI 达 0.745–1.000）。

2. **大幅缩减仍可保持 psychometric adequacy**  
   平均减少 **60.5%** 题项后，短表仍具备良好信效度（CFA 拟合良好，α > 0.7 多数因子）。

3. **保留了原量表的信息结构**  
   - 子量表间相关模式（inter-factor correlation）高度保留（Frobenius 相似度 > 0.86）；
   - 全表与短表的 cross-form 相关高达 **r ≈ 0.87–0.95**，表明测量一致性强。

4. **方法稳健且可解释**  
   - 密度聚类避免预设 K，适应不同量表；
   - 可视化支持人工审查与调试；
   - 工具化降低使用门槛。

### 方法的局限性
- **依赖语言表示的质量**：不同 embedding 模型可能导致不同结果，跨语言泛化需进一步验证；
- **语义相似 ≠ 测量等价**：语义相近的项目可能在反应倾向、难度等方面不同；
- **极端简化的可靠性天花板**：如 EPOCH 的 Connectedness 因子仅 2 题，α=0.588，受限于长度而非方法缺陷；
- **对模糊或重叠构念敏感**：如 DASS 中 Anxiety 与 Stress 的边界模糊，算法会反映这种“粘性”。

### 未来工作方向
1. **系统性多语言与跨文化验证**  
   扩展至更多语言版本，结合 measurement invariance 设计。

2. **构建人机协同的工作流**  
   将算法输出作为初筛建议，由领域专家进行内容审查与调整，形成“human-in-the-loop”简化流程。

3. **与响应驱动方法（如 IRT）对比整合**  
   探索 semantic-driven 与 response-driven 方法的选择优先级与融合策略。

4. **应用于新量表开发与修订**  
   在量表编制初期即使用该框架指导 item 生成与筛选，提升效率与理论一致性。

---

> **GitHub 开源地址**：[https://github.com/bowang-rw-02/sem-scale](https://github.com/bowang-rw-02/sem-scale)  
> 包含完整代码、工具、示例与复现脚本，支持一键运行。

</details>

---

### 9. [Multi-Agent Model-Based Reinforcement Learning with Joint State-Action Learned Embeddings](https://arxiv.org/abs/2602.12520)

**Authors**: Zhizun Wang, David Meger  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12520v1  

#### Abstract
Learning to coordinate many agents in partially observable and highly dynamic environments requires both informative representations and data-efficient training. To address this challenge, we present a novel model-based multi-agent reinforcement learning framework that unifies joint state-action rep...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《Multi-Agent Model-Based Reinforcement Learning with Joint State-Action Learned Embeddings》总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
多智能体强化学习（MARL）在**部分可观测、高度动态且复杂的环境**中面临严重的**样本效率低**和**表示学习不足**的问题。传统的模型无关（model-free）MARL 方法依赖大量真实环境交互，训练成本高；而现有的模型基础（MBRL）方法在多智能体场景下难以有效建模状态-动作之间的因果关系。

本文旨在通过**联合的状态-动作表示学习**与**基于想象的规划**相结合，提升多智能体系统中的**样本效率**和**长期决策能力**。

---

### **提出的新方法与新思路**
作者提出了一个名为 **MMSA（Multi-Agent Model-Based Framework with Joint State-Action Learned Embeddings）** 的新型 MARL 框架，其核心创新包括：

- **引入 SALE（State-Action Learned Embeddings）到世界模型中**  
  利用变分自编码器（VAE）构建一个**增强的世界模型**，该模型不仅预测未来状态，还通过 SALE 学习**紧凑的联合状态-动作潜在表示空间**，从而捕捉动作对状态转移的因果影响。

- **将 SALE 融入价值分解网络架构**  
  在个体 agent 的 Q 网络中集成 SALE 表示，使得每个 agent 的局部动作值函数能够利用更丰富的动态特征。

- **统一的想象式价值分解框架**  
  将世界模型生成的**想象轨迹（imagined roll-outs）** 输入到 QMIX 风格的混合网络（mixing network）中，实现**基于想象的价值分解（imagination-based value decomposition）**，在 CTDE（Centralized Training with Decentralized Execution）范式下进行训练。

- **结合 KL Balancing 技术稳定训练**  
  在 VAE 的优化中采用 KL Balancing，防止后验崩溃（posterior collapse），提升表示学习的鲁棒性。

---

### **相比现有方法的优势**
| 维度 | MMSA 的优势 |
|------|-------------|
| **样本效率** | 显著减少对真实环境交互的需求，通过想象轨迹进行高效训练 |
| **表示质量** | SALE 提供更具因果意义的状态-动作联合嵌入，优于仅基于状态或观测的表示 |
| **泛化能力** | 在随机化地图、单位配置等强泛化挑战下仍能保持高性能 |
| **协调能力** | 通过混合网络整合全局状态与想象轨迹，促进更优的多智能体协作策略 |

---

## **2. 核心实验方法和设置**

### **使用的数据集 / 环境**
论文在三个主流的多智能体基准上进行了广泛验证：

1. **StarCraft II MARL Benchmark (SMAC & SMACv2)**  
   - 微观管理战斗任务，控制多个单位协同作战。
   - SMACv2 引入了**随机出生位置、单位类型概率分布、锥形视野限制**，增强了环境不确定性。

2. **Multi-Agent MuJoCo (MAMuJoCo)**  
   - 基于 MuJoCo 物理引擎的连续控制任务。
   - 包括 `Ant_8×1`, `HalfCheetah_6×1`, `Hopper_3×1`, `Walker2d_6×1` 等机器人分治控制任务。
   - 每个子 agent 控制机器人的部分关节，需合作完成前进目标。

3. **Level-Based Foraging (LBF)**  
   - 网格世界中的混合合作/竞争任务。
   - agent 必须联合其等级总和 ≥ 食物等级才能拾取食物，鼓励团队协作。
   - 设置如 `10x10-3p-5f-v2` 等不同规模的任务。

---

### **实验设置与评估指标**

| 项目 | 描述 |
|------|------|
| **训练步数** | MAMuJoCo: 7M；LBF: 2M；SMAC/SMACv2: 5M |
| **评估频率** | 每 10k 步测试一次，报告平均 episode return 或 win rate |
| **评估方式** | 5 个随机种子取均值 ± 95% 置信区间 |
| **主要指标** | - 平均 episode return（MAMuJoCo, LBF）<br>- 测试胜率（test win rate, SMAC） |
| **消融实验** | 移除关键组件（如世界模型、SALE、KL Balancing、全局状态）进行对比 |

---

### **基线方法对比**
#### **模型无关方法（Model-Free）**
- VDN, QMIX, QPLEX, COMA
- SET-QMIX, HPN-QMIX, HPN-VDN（基于排列不变性的先进方法）
- SMMAE, MASER, QMIX-CIA, QPLEX-CIA

#### **模型基础方法（Model-Based）**
- MAG, MAMBA, MABL —— 当前最先进的多智能体 MBRL 方法

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总（见 Table 1 & A.4）**

| 环境 | MMSA 性能 | 最佳基线 | 结果对比 |
|------|----------|---------|--------|
| **MAMuJoCo**（平均 episode return） | **36.94** | VDN: 28.69 | ↑ **28.7%** |
| **LBF**（平均 episode return） | **0.80** | QMIX: 0.42 | ↑ **90.5%** |
| **SMACv1**（平均 win rate） | **0.93** | MAG: 0.57 | ↑ **63.2%** |
| **SMACv2**（平均 win rate） | **0.81** | HPN-QMIX: 0.81 | **持平最优** |

> 注：在 SMACv2 中，MMSA 虽起步较慢（因需训练世界模型），但最终达到甚至超越所有基线。

---

### **与基线方法的对比结果**
- 在 **MAMuJoCo** 上，MMSA 显著领先，尤其在 `Ant` 和 `HalfCheetah` 任务上表现突出，说明其在复杂连续控制中具备更强的协调能力。
- 在 **LBF** 上，MMSA 远超其他方法，表明其在需要精细等级协作的任务中具有优越的合作推理能力。
- 在 **SMACv1/v2** 上，MMSA 明显优于所有模型基础基线（MAG/MAMBA/MABL），并与最强的模型无关方法 HPN-QMIX 打成平手。

---

### **消融实验结果（Ablation Study）**
在 SMACv2 上进行的关键组件移除实验（图 5b）显示：

| 变体 | 相对于完整 MMSA 的性能下降 |
|------|---------------------------|
| **No-WM**（无世界模型） | 胜率降至约 0.5，无法突破基础行为 |
| **No-SALE**（无状态-动作嵌入） | 性能低于 No-WM，说明 SALE 对建模动态至关重要 |
| **No-KLB**（无 KL Balancing） | 收敛不稳定，易出现后验崩溃 |
| **No-GS**（无全局状态输入） | 协调能力显著下降，尤其在复杂战场 |

✅ **结论**：所有模块均为不可或缺，共同促成 MMSA 的高性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **联合状态-动作表示（SALE）是提升多智能体 MBRL 的关键**  
   SALE 能有效捕获动作如何影响状态转移，在潜在空间中提供更具语义的信息，显著增强策略学习。

2. **想象轨迹 + 价值分解可实现高效长程规划**  
   世界模型生成的 roll-outs 被用于 mixing network，使 agent 能“预演”未来可能的结果，从而做出更优决策。

3. **MMSA 在多样环境中展现出卓越的泛化性和稳定性**  
   即使在 SMACv2 的高度随机环境下，也能逐步逼近最优策略，体现出强大的适应能力。

4. **设计选择敏感但有明确最优路径**  
   实验表明：roll-out horizon=3、learning rate=1e-3、normalization=AvgL1Norm 是最佳组合。

---

### **方法的局限性**
- **计算开销较高**：由于引入了 VAE 和 roll-out 模拟，训练时间比纯 model-free 方法更长（见 Table 5）。
- **对模型误差敏感**：在极端不确定环境中，想象轨迹可能偏离真实动态，导致误导性更新。
- **理论分析缺失**：目前缺乏对 SALE 在 MARL 中收敛性或最优性的形式化证明。

---

### **未来工作方向**
1. **缓解模型误差积累**  
   探索使用 **model ensembles** 或 **uncertainty-aware modeling** 来提高想象轨迹的可靠性。

2. **扩展至更大规模多智能体系统**  
   当前实验集中在中小规模（最多 8 个 agent），未来可探索在城市交通、无人机群等大规模场景的应用。

3. **结合离线强化学习（Offline RL）**  
   利用 SALE 和世界模型从历史数据中学习，进一步降低对在线交互的依赖。

4. **加强理论保障**  
   分析 SALE 表示在多智能体 setting 下的表达能力与学习动态。

---

> ✅ **总体评价**：MMSA 成功地将 **representation learning**、**model-based imagination** 与 **value decomposition** 三大思想融合，为多智能体系统提供了一个**高效、可解释且通用性强**的新范式，代表了 MARL 向更高样本效率和更强泛化能力迈进的重要一步。

</details>

---

### 10. [Efficient Personalized Federated PCA with Manifold Optimization for IoT Anomaly Detection](https://arxiv.org/abs/2602.12622)

**Authors**: Xianchao Xiu, Chenyi Huang, Wei Zhang, Wanquan Liu  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12622v1  

#### Abstract
Internet of things (IoT) networks face increasing security threats due to their distributed nature and resource constraints. Although federated learning (FL) has gained prominence as a privacy-preserving framework for distributed IoT environments, current federated principal component analysis (PCA)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Efficient Personalized Federated PCA with Manifold Optimization for IoT Anomaly Detection**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前的 **Federated PCA (FedPCA)** 方法在应用于 **IoT 异常检测** 时存在两大关键缺陷：
- **缺乏个性化（Personalization）**：IoT 数据具有高度非独立同分布（non-IID）特性，单一全局模型难以适应不同客户端的数据分布，导致局部检测敏感度下降。
- **鲁棒性不足（Robustness）**：传统 PCA 对噪声和异常值敏感，在存在恶意扰动或传感器故障的场景下，估计的子空间易被扭曲。

### **提出的新方法**
作者提出了 **FedEP (Efficient Personalized Federated PCA)**，一种融合个性化与鲁棒性的新型联邦 PCA 框架，其核心创新包括：

- **个性化建模**：将每个客户端的数据建模为低秩成分（正常行为）与稀疏成分 $ S_i $（局部噪声/异常）的叠加，允许本地模型吸收特定噪声，从而适配 non-IID 数据。
- **双稀疏正则化（Bi-sparse Regularization）**：
  - 使用 $ \ell_{2,1} $-norm 对投影矩阵 $ W_i $ 施加行稀疏性，增强对特征级损坏的鲁棒性，并降低计算开销。
  - 使用 $ \ell_1 $-norm 对稀疏误差 $ S_i $ 进行元素级稀疏控制，有效分离细粒度噪声与异常。
- **Manifold Optimization + ADMM 求解器**：设计了一种基于 **Alternating Direction Method of Multipliers (ADMM)** 和 **流形优化（Manifold Optimization）** 的高效算法，处理非凸约束（Stiefel 流形），并提供严格的收敛性证明。

### **相比现有方法的优势**
- **首次在 FedPCA 中集成个性化与鲁棒性机制**，解决了现有方法如 FedPG 忽视个性化、易受噪声干扰的问题。
- **更高的检测精度与稳定性**：在多个真实 IoT 数据集上显著优于最先进的 FedPG。
- **更强的计算效率**：训练时间更短，通信轮次更少即可收敛，适合资源受限的边缘设备。
- **理论保障**：提供了所提 ADMM 算法的收敛性分析，增强了方法可信度。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验在三个广泛使用的网络入侵检测数据集上进行：

| Dataset       | Features | Training Samples | Testing Samples | Classes |
|---------------|----------|------------------|------------------|---------|
| **TON-IoT**   | 49       | 114,956          | 66,557           | 10      |
| **UNSW-NB15** | 39       | 65,000           | 65,332           | 10      |
| **NSL-KDD**   | 34       | 125,973          | 22,544           | 5       |

所有数据按 **non-IID 方式划分为 20 个客户端子集**，以模拟异构 IoT 环境。

### **实验设置与评估指标**
- **特征归一化**：采用 z-score 标准化。
- **超参数调优**：通过网格搜索（grid search）确定最优参数。
- **评估指标**：
  - **Accuracy (Acc)**
  - **Precision (Pre)**
  - **Recall (Rec)**
  - **F1-score**
  - **False Negative Rate (FNR)**

> 注：↑ 表示越高越好，↓ 表示越低越好。

### **基线方法对比**
- **FedPG**：当前最先进的 **Federated PCA on Grassmann manifold** 方法，作为主要对比基准。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table II）**

| Dataset     | Method | Acc ↑     | Pre ↑     | Recall ↑  | FNR ↓     | F1-score ↑ |
|------------|--------|-----------|-----------|-----------|-----------|-------------|
| TON-IoT    | FedPG  | 88.89%    | 90.84%    | 96.67%    | 3.33%     | 93.66%      |
|            | **FedEP** | **90.48%** | **92.48%** | 96.65%    | 3.35%     | **94.52%**  |
| UNSW-NB15  | FedPG  | 80.93%    | 81.58%    | 93.66%    | 6.34%     | 87.20%      |
|            | **FedEP** | **83.31%** | **82.17%** | **97.00%** | **3.00%** | **88.97%**  |
| NSL-KDD    | FedPG  | 84.09%    | 89.78%    | 81.30%    | 18.70%    | 85.33%      |
|            | **FedEP** | **84.24%** | 89.66%    | **81.75%** | **18.25%** | **85.52%**  |

> ✅ FedEP 在 **Acc** 和 **F1-score** 上全面超越 FedPG，尤其在挑战性更高的 **UNSW-NB15** 上提升显著（+2.38% Acc, +1.77% F1）。

### **与基线方法的对比结果**
- **ROC 曲线表现更优**（Fig. 2）：
  - FedEP 在所有数据集上的 **AUC** 更高：
    - TON-IoT: **0.8305** vs. 0.8132
    - UNSW-NB15: **0.8632** vs. 0.8483
    - NSL-KDD: **0.8899** vs. 0.8851
- **更快收敛**（Fig. 3）：
  - FedEP 在前 **10 轮通信内即达到峰值准确率**，而 FedPG 收敛较慢，表明个性化初始化提升了学习效率。
- **更低训练耗时**（Fig. 5–6）：
  - FedEP 单轮训练时间更短，累计训练时间显著低于 FedPG，尤其在 NSL-KDD 上接近 **2 倍加速**。

### **消融实验结果（Ablation Study）**
通过四种配置验证各组件贡献：

| Case        | 内容描述 |
|-------------|--------|
| **Case I**  | 仅含 $ S_i $（去噪），无 $ \|W_i\|_{2,1} $ 正则 |
| **Case II** | 仅含 $ \|W_i\|_{2,1} $，无 $ S_i $ |
| **Case III**| 仅含 $ \ell_1 $ 正则（未完整） |
| **Case IV** | 完整 FedEP 模型（含 $ S_i $ + $ \|W_i\|_{2,1} $） |

#### **关键发现**：
- **Case IV 性能最优**：在所有数据集上均取得最高 Acc 与 F1-score。
- **UNSW-NB15 上尤为明显**：Cases I 和 III 虽 Recall 高（~100%），但 Acc 和 Precision 极低，说明易过拟合；而 Case IV 实现了更好的平衡。
- **双稀疏机制协同增效**：$ S_i $ 吸收异常，$ \|W_i\|_{2,1} $ 抑制冗余特征，共同提升模型稳定性和泛化能力。

---

## **4. 关键结论和发现**

### **主要发现**
1. **个性化 + 鲁棒性是 FedPCA 成功的关键**：FedEP 通过引入局部稀疏表示 $ S_i $ 和行稀疏正则 $ \|W_i\|_{2,1} $，有效应对 IoT 场景下的 non-IID 与噪声挑战。
2. **轻量高效，适合边缘部署**：相比深度学习方法，FedEP 计算开销小，通信轮次少，更适合资源受限的 IoT 设备。
3. **结构稳定性强**：即使在高秩（over-parameterized）情况下，FedEP 仍能保持良好性能，而 FedPG 明显退化（如 NSL-KDD 上 Acc 从 84%+ 降至 75.62%）。
4. **可解释性强**：可视化显示 FedEP 的特征重要性更集中，能有效过滤无关特征，提升模型透明度。

### **方法的局限性**
- 当前为线性模型（PCA），可能无法捕捉复杂非线性模式。
- 虽然通信效率较高，但个性化模型本身可能增加存储负担（每个客户端维护独立参数）。
- 理论收敛性分析适用于算法框架，但在实际非凸流形上的全局最优性仍难保证。

### **未来工作方向**
- 扩展至 **非线性模型**：探索基于 kernel 或 deep learning 的非线性 FedEP 变体。
- **模型压缩**：研究个性化模型的可压缩性，进一步降低通信与存储开销。
- **动态个性化策略**：设计自适应机制，根据客户端数据分布动态调整个性化程度。

---

> 🔗 **代码开源**：作者承诺将代码发布于 GitHub → [https://github.com/xianchaoxiu/FedEP](https://github.com/xianchaoxiu/FedEP)

</details>

---

### 11. [Dual-Granularity Contrastive Reward via Generated Episodic Guidance for Efficient Embodied RL](https://arxiv.org/abs/2602.12636)

**Authors**: Xin Liu, Yixuan Li, Yuhui Chen, Yuxing Qin, Haoran Li, Dongbin Zhao  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12636v1  

#### Abstract
Designing suitable rewards poses a significant challenge in reinforcement learning (RL), especially for embodied manipulation. Trajectory success rewards are suitable for human judges or model fitting, but the sparsity severely limits RL sample efficiency. While recent methods have effectively impro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Dual-Granularity Contrastive Reward via Generated Episodic Guidance for Efficient Embodied RL**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在 **Embodied RL**（具身强化学习）中，设计高效、密集的 **reward** 是一个核心挑战。传统方法面临以下问题：
- **Sparse success rewards**（稀疏成功奖励）虽然易于人类判断，但导致极低的 **sample efficiency**（样本效率），难以支持复杂任务的学习。
- 现有基于专家演示（expert demonstrations）的密集奖励方法（如 Diffusion Reward、TeViR）依赖大量高质量的多视角视频或人工标注，成本高昂且数据效率低下。

### ✅ 提出的新方法：**DEG**（Dual-granularity contrastive reward via generated Episodic Guidance）
提出了一种无需人工标注、仅需少量专家视频即可实现高效密集奖励设计的新框架 **DEG**，其核心思想包括：
- **生成式引导（Generated Episodic Guidance）**：利用预训练的 **large video generation model**（如 Wan2.1-I2V-14B），通过 **LoRA** 对少量专家视频（仅 3–5 段）进行微调，生成每个 RL episode 的专属任务指导视频。
- **双粒度对比奖励（Dual-granularity Contrastive Reward）**：
  - **Coarse-grained exploration reward**：鼓励智能体大致模仿指导视频的动作轨迹，提升探索效率。
  - **Fine-grained matching reward**：对精确匹配关键帧（如抓取、旋转等精细操作）给予高奖励，确保动作精度。
- **对比自监督对齐（Contrastive Latent Alignment）**：训练一个自监督编码器，在潜在空间中对齐生成视频与真实观测，使语义距离可被有效度量。

### ✅ 相比现有方法的优势
| 维度 | DEG | 现有方法（如 TeViR、RoboCLIP） |
|------|-----|-------------------------------|
| 数据需求 | 仅需 **3–5 段单视角专家视频** | 需要数十段多视角视频 |
| 是否需要人工标注 | ❌ 不需要 | ✅ 多数需要语言描述或状态标注 |
| 奖励密度 | 高密度、任务相关 | 或稀疏，或依赖外部模型 |
| 泛化能力 | 可适应不同初始状态的任务 | 通常固定轨迹或目标 |
| 样本效率 | 显著更高 | 较低 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **Simulation Tasks**：来自 **MetaWorld** 的 **16 个 manipulation 任务**，涵盖开门、抽屉开合、按钮按压、锤击、堆叠等，覆盖 Sawyer 机械臂的完整状态空间。
  - 典型任务：`drawer-open`, `plate-slide`, `hammer`, `assembly`, `faucet-close` 等。
- **Real-world Tasks**：在物理 **Franka 机械臂** 上进行两个长程任务：
  - `pick banana`：从随机位置抓取香蕉并放入盘子。
  - `stack cube`：将小红方块堆叠到大木块上。

### ⚙️ 实验设置与评估指标
- **Backbone RL Algorithm**：
  - Simulation：**DrQv2**
  - Real-world：**HIL-SERL**（Human-in-the-loop RL）
- **训练步数限制**：
  - Simulation：最多 500k 步
  - Real-world：最多 25k 步
- **评估指标**：
  - **Success Rate**（成功率）
  - **Sample Efficiency**（达到稳定策略所需的环境交互步数）
  - **Intervention Steps**（人类干预次数，用于 real-world）

### 🔁 基线方法对比
| 基线方法 | 类型 | 数据需求 |
|--------|------|---------|
| **TeViR** | 基于文本到视频扩散模型的奖励 | 60 段视频 + 三视角 |
| **Diffusion Reward** | 条件视频扩散模型生成奖励 | 多段专家视频 |
| **VIPER** | 视频预测模型作为奖励 | 多段视频 |
| **RoboCLIP** | VLM + 语言描述相似性奖励 | 单段演示 + 文本描述 |
| **Success Sparse Reward** | 仅成功信号 | 无 |
| **Expert Dense Reward** | 人工设计的密集奖励 | 专家手工调参 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据
#### （1）Reward-Free 任务（12 项）
- **DEG 在 10/12 任务上显著优于所有基线**，仅在 `door-close` 和 `drawer-close` 上与 TeViR 接近。
- **样本效率优势明显**：在多数任务中，DEG 在 200k 步内即接近收敛，而其他方法仍处于缓慢上升阶段（见 Figure 3）。

#### （2）Sparse Reward + DEG（DEG+）
- 在 **16 个 MetaWorld 任务** 上，**DEG+ 与人工设计的 Expert Dense Reward 性能相当，甚至在多个任务上超越**：
  - 如 `assembly`, `hammer`, `drawer-open` 等任务中表现更优。
- **远超纯稀疏奖励**：Success Sparse Reward 几乎无法驱动有效学习（见 Figure 4）。

#### （3）Real-world 物理实验
- 在 `pick banana` 和 `stack cube` 上，**DEG 显著提升了 RL 效率**，降低了人类干预频率。
- **奖励触发频率高**：当人类介入时，DEG 奖励频繁激活，说明其与专家行为高度对齐。

### 📊 与基线方法的对比结果
| 方法 | 平均成功率（MetaWorld） | 所需视频数量 | 是否需标注 |
|------|------------------------|--------------|------------|
| DEG (Ours) | **0.82** | 3–5（单视角） | ❌ 否 |
| TeViR | 0.68 | 60（三视角） | ✅ 是 |
| Diffusion Reward | 0.61 | 20+ | ✅ 是 |
| RoboCLIP | 0.59 | 1 + 文本 | ✅ 是 |
| Success Sparse | 0.32 | — | ❌ 否 |
| Expert Dense | 0.80 | — | ✅ 是 |

> 💡 **DEG 以极少数据实现了与专家手工奖励媲美的性能**。

### 🔍 消融实验结果（Ablation Study）
- **移除 coarse-grained reward**：
  - 智能体能完成精细操作（如抓手柄），但缺乏整体运动轨迹模仿，任务完成不稳定。
- **移除 fine-grained reward**：
  - 能模仿大致轨迹，但无法完成精确交互（如推拉失败）。
- **双粒度奖励缺一不可**：两者结合才能实现“先粗后精”的高效学习（见 Figure 6 和 Figure 9）。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **仅需极少量专家视频即可生成高质量任务指导**：借助大模型先验，DEG 实现了从“少样本”到“密集奖励”的跨越。
2. **双粒度奖励机制有效平衡探索与精度**：
   - Coarse reward 提供宏观引导，避免陷入局部最优。
   - Fine reward 强化细节交互，提升任务成功率。
3. **对比自监督编码器优于通用预训练模型**（如 DINOv3）：
   - 能更好对齐生成噪声视频与真实观测。
   - 将语义距离映射为可计算的潜在空间相似性（见 Figure 7）。
4. **DEG 可独立引导 RL 收敛，也可与稀疏奖励结合进一步提升效率**。

### ⚠️ 方法的局限性
- **视频生成速度慢**：当前视频生成模型推理耗时较长，影响实时性。
- **对生成质量敏感**：若微调不足，生成视频存在模糊或物理不一致，可能误导奖励信号。
- **未完全解决跨域泛化**：目前仍需针对每个任务微调生成模型。

### 🔮 未来工作方向
- 探索更高效的 **video foundation models** 以加速生成。
- 研究 **zero-shot domain adaptation**，减少任务特定微调。
- 将 DEG 扩展至 **multi-agent** 或 **long-horizon task planning** 场景。
- 结合 **language prompting** 实现更灵活的任务定制。

---

> **总结一句话**：  
> **DEG 通过“生成式 episodic 指导 + 双粒度对比奖励”，实现了仅用 3–5 段视频即可媲美人工密集奖励的高效具身 RL，为低资源、高效率机器人学习提供了新范式。**

</details>

---

### 12. [Mixture of Predefined Experts: Maximizing Data Usage on Vertical Federated Learning](https://arxiv.org/abs/2602.12708)

**Authors**: Jon Irureta, Gorka Azkune, Jon Imaz, Aizea Lojo, Javier Fernandez-Marques  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12708v1  

#### Abstract
Vertical Federated Learning (VFL) has emerged as a critical paradigm for collaborative model training in privacy-sensitive domains such as finance and healthcare. However, most existing VFL frameworks rely on the idealized assumption of full sample alignment across participants, a premise that rarel...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Mixture of Predefined Experts: Maximizing Data Usage on Vertical Federated Learning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本论文针对 **Vertical Federated Learning (VFL)** 中两个关键现实挑战：
- **样本不对齐（Unaligned Samples）**：实际场景中（如跨医院协作），不同参与方共享的样本交集稀疏，传统 VFL 方法因假设“完全对齐”而性能严重下降。
- **通信开销大与鲁棒性差**：现有方法需多轮通信进行端到端训练，且难以抵御恶意或噪声参与者的影响，缺乏可解释性。

### 🚀 提出的新方法：Split-MoPE
提出 **Split-MoPE**，一种结合 **Split Learning** 与新型 **Mixture of Predefined Experts (MoPE)** 架构的 VFL 框架。

#### 创新点：
- **MoPE 架构设计**：
  - 不同于标准 MoE 动态学习路由，MoPE 的专家（Experts）是**预定义的**，每个专家专门处理一种特定的数据对齐模式（即 `P(K)` 中的一个子集）。
  - 路由器（Router）通过 Sigmoid 输出门控权重，实现专家间的协同而非互斥选择。
- **解耦训练 + 预训练编码器**：
  - 使用**冻结的预训练 backbone**（如 DINOv2、Qwen3-Embedding）作为本地特征提取器，避免反向传播，仅需**单次前向通信**。
  - 显著降低通信开销，提升系统效率。

### 🔍 相比现有方法的优势
| 维度 | Split-MoPE 优势 |
|------|----------------|
| **样本对齐要求** | 无需完全对齐，能有效利用非对齐样本进行训练与推理 |
| **通信效率** | 单轮通信即可完成训练，通信量减少高达 200× |
| **鲁棒性** | 对噪声/恶意参与者具有天然抵抗力，性能退化更小（最多减少 25×） |
| **可解释性** | 提供每样本级别的贡献分析，量化各参与方对预测的贡献 |
| **模块化与通用性** | 支持多种模态（vision/tabular），易于集成不同预训练模型 |

---

## 2. 核心实验方法和设置

### 📚 数据集
在三种广泛使用的数据集上验证方法有效性，涵盖两种模态：
- **视觉数据集**：
  - **CIFAR-10**, **CIFAR-100**：图像被划分为多个 patch，分配给不同参与者。
- **表格数据集**：
  - **Breast Cancer Wisconsin**：列被垂直切分给不同参与者。

> 所有原始数据均非原生 VFL 结构，需人工模拟垂直划分与样本缺失。

### ⚙️ 实验设置
- **样本缺失机制**：采用 **MCAR（Missing Completely at Random）**，引入参数 `pmiss` 控制被动参与者数据缺失概率（0 表示全对齐，1 表示仅主动方可见）。
- **参与方数量**：默认为两方（一个 active，一个 passive），符合典型 VFL 场景。
- **预训练编码器使用**：
  - 视觉任务：DINOv2（ViT-S/B/L）
  - 表格任务：将行转换为文本格式 `"Feature_name:value|..."`，使用 **Qwen3-Embedding** 模型生成嵌入。
- **评估指标**：
  - 分类准确率（Accuracy）用于 CIFAR；
  - F1 Score（×100）用于 Breast Cancer Wisconsin。

### 🆚 基线方法对比
- **SplitNN**：经典 Split Learning 方法，代表基础 VFL 范式。
- **LASER**：当前最先进的处理非对齐样本的 VFL 方法。
- **SplitNN\*** 和 **LASER\***：作者复现并改进版本，同样采用**冻结预训练编码器 + 单轮通信**，确保公平比较。
- **Local Model**：仅使用主动方本地特征训练的模型，作为性能下限基准。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 2）

| 方法 \ `pmiss` | 0.0 | 0.1 | 0.5 | 0.7 |
|----------------|-----|-----|-----|-----|
| **CIFAR-10 (Accuracy %)** | | | | |
| SplitNN* | 95.61 | 95.55 | 90.59 | 90.80 |
| LASER* | 95.42 | 94.66 | 91.91 | 90.58 |
| **MoPE (Ours)** | **95.63** | **94.73** | **92.82** | **91.98** |
| **CIFAR-100 (Accuracy %)** | | | | |
| MoPE | 81.78 | 80.50 | 75.31 | **73.27** |
| LASER* | 82.26 | 79.83 | 74.52 | 72.55 |
| SplitNN* | 81.53 | 81.12 | 72.06 | 72.56 |
| **Breast Cancer (F1 ×100)** | | | | |
| MoPE | **94.15** | 93.30 | **90.37** | **87.68** |
| LASER* | 86.69 | 84.90 | 85.06 | 86.19 |
| SplitNN* | 93.37 | 93.37 | 81.81 | 82.86 |

> ✅ **Split-MoPE 在高 `pmiss` 下显著优于所有基线**，尤其在表格数据上优势明显（如 `pmiss=0.7` 时高出 LASER* 超过 1.5 F1 点）。

### 🔁 与基线方法对比结果
- **在完美对齐 (`pmiss=0`) 下**：Split-MoPE 性能与 LASER*/SplitNN* 相当，达到 SOTA 水平。
- **随着 `pmiss` 增加**：
  - SplitNN 性能急剧下降，表明其对样本对齐高度敏感。
  - LASER 表现出一定鲁棒性，但仍劣于 MoPE。
  - **MoPE 退化最缓慢，在 `pmiss=0.7` 时仍保持接近本地模型甚至超越之**。
- **通信效率对比**：
  - 以 2 方协作、25,000 样本、100 轮训练为例：
    - 传统方法通信量 > **76.8 GB**
    - Split-MoPE 仅需 **38.4 MB** → **减少 200 倍**

### 🔍 消融实验与深入分析
#### （1）预训练编码器的作用（Table 5）
| Backbone | CIFAR-10 (`pmiss=0→0.7`) Δ | BCW (`pmiss=0→0.7`) Δ |
|----------|----------------------------|------------------------|
| ViT-S (21M) | -3.65 pts | -11.17 pts |
| ViT-L (300M) | **-2.07 pts** | **-2.67 pts** |
> 更大的预训练模型不仅性能更高，且对数据缺失更具鲁棒性。

#### （2）对抗噪声/恶意参与者（Table 3）
在添加 1~5 个噪声参与者（输出随机嵌入）的情况下，测量性能衰减：
- **MoPE 衰减最小**，例如在 CIFAR-10 (`pmiss=0`) 下，5 个噪声参与者仅导致 **3.53% 准确率下降**，而 LASER* 下降达 **22.31%**。
- 路由器分析（Table 4）显示：MoPE 能自动识别噪声专家并赋予极低权重（趋近 0），体现其内在鲁棒性。

#### （3）贡献可解释性
通过公式 $ C_k = \sum_{e \in P(K), k \in e} w_e $ 可计算每个参与者对预测的贡献，支持公平激励机制设计。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Split-MoPE 是首个真正 alignment-agnostic 的 VFL 框架**，能在不牺牲性能的前提下最大化数据利用率。
2. **冻结预训练编码器 + 单轮通信** 是高效 VFL 的可行路径，性能反而优于端到端微调。
3. **MoPE 架构天然具备鲁棒性与可解释性**：
   - 路由器能自动抑制噪声输入影响；
   - 每样本贡献可量化，增强信任与透明度。
4. **更大的预训练模型带来更强的表示能力与抗缺失鲁棒性**，提示未来应重视高质量 encoder 的选择。

### ⚠️ 局限性
- 当前实验集中在小规模联邦（2~3 方），扩展至大规模复杂拓扑需进一步研究。
- MoPE 专家数随参与方数指数增长（$|P(K)| = 2^{K-1}$），可能带来内存与计算负担。
- 路由器训练依赖足够多样化的对齐模式数据，在极端稀疏场景下可能收敛困难。

### 🔮 未来工作方向
- 探索更高效的 MoPE 训练策略（如稀疏激活、专家共享）。
- 将 Split-MoPE 应用于**异构多模态环境**（如图像 + 文本 + 表格），研究跨模态权重分配机制。
- 设计轻量化路由器结构以应对大规模参与者场景。
- 结合隐私增强技术（如加密聚合）构建端到端安全 VFL 流程。

---

> 💡 **总结一句话**：  
> **Split-MoPE 通过“预定义专家 + 预训练编码器”的创新组合，在单轮通信下实现了高性能、强鲁棒、高可解释的 VFL，为真实世界中的非对齐数据协作提供了实用解决方案。**

</details>

---

### 13. [Scaling Web Agent Training through Automatic Data Generation and Fine-grained Evaluation](https://arxiv.org/abs/2602.12544)

**Authors**: Lajanugen Logeswaran, Jaekyeom Kim, Sungryull Sohn, Creighton Glasscock, Honglak Lee  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12544v1  

#### Abstract
We present a scalable pipeline for automatically generating high-quality training data for web agents. In particular, a major challenge in identifying high-quality training instances is trajectory evaluation - quantifying how much progress was made towards task completion. We introduce a novel const...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scaling Web Agent Training through Automatic Data Generation and Fine-grained Evaluation

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前 **Web Agent** 的训练面临两大挑战：

1. **高质量训练数据稀缺**：真实世界中大规模、多样化的 Web 交互轨迹难以获取。
2. **轨迹评估困难**：传统评估方式（如 LLM-as-judge 或 VLM-as-judge）在复杂、多约束任务上表现不可靠，尤其对部分成功的轨迹缺乏细粒度评价。

这些问题限制了 Web Agent 在现实场景中的可扩展训练与有效评估。

---

### 🚀 提出的新方法与创新

#### （1）**自动数据生成管道（Automatic Data Generation Pipeline）**

- 利用公开的 **LLM（如 LLaMA 3.1 405B）作为 Teacher Agent**，通过 few-shot prompting 自动生成大量 Web 交互轨迹。
- 轨迹包含动作序列（click, type, goto 等）、观察（accessibility tree, URL, screenshot）及推理链（chain-of-thought）。
- 支持在 **20 个真实预订类网站** 上生成多样化、高复杂度的任务轨迹。

#### （2）**基于约束的细粒度评估框架（Constraint-based Fine-grained Evaluation）**

- 引入 **Constraint Satisfaction Rate (CSR)** 指标，将任务拆解为多个独立约束（如 location, date, amenity），逐项判断是否满足。
- 公式定义如下：
  $$
  \text{CSR}(o) = \frac{1}{n} \sum_{i=1}^{n} \mathbb{I}[f(c_i, o, d) = v]
  $$
  其中 $ f $ 是 LLM/VLM Judge，$ c_i $ 是第 $ i $ 个约束，$ o $ 是最终观测，$ d $ 是任务描述。

#### （3）**利用部分成功轨迹进行数据增强**

- 提出 **Prefix Extraction** 和 **Hindsight Re-labeling** 技术：
  - 即使整个轨迹未完全成功，只要某一步达到最高 CSR，就提取该前缀作为训练样本。
  - 对提前终止但部分满足约束的情况，重写任务描述使其成为“合法”完成任务。

#### （4）提出新基准 **BookingArena**

- 包含 **120 个复杂预订任务**，覆盖 20 个主流网站（如 Booking.com, Airbnb, Kayak 等）。
- 每个网站有 6 个任务，按难度分为 Easy/Medium/Hard，平均约束数分别为 3.95 / 5.45 / 7.30。
- 所有任务均为在线执行，支持日期自动更新以保持有效性。

---

### 🔍 相比现有方法的优势

| 维度 | 本文方法 | 现有方法（如 WebVoyager, Mind2Web） |
|------|--------|-------------------------------|
| 数据来源 | 自动合成 + 高质量过滤 | 静态数据集或人工标注 |
| 评估方式 | 细粒度 CSR + 可解释性 | 二值 Success/Failure 或依赖截图的 LLM 判断 |
| 数据利用率 | 利用部分成功轨迹 → 更大数据量 | 仅使用完全成功轨迹 |
| 任务复杂性 | 多约束、长周期、真实网站 | 多为短任务或模拟环境 |
| 模型规模 | 小模型（24B）即可超越大模型 | 通常依赖超大闭源模型 |

> ✅ **核心优势**：实现了从“小模型 + 合成数据”中训练出媲美甚至超越商业系统的高性能 Web Agent。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集

| 数据集 | 描述 |
|-------|------|
| **BookingArena（本文提出）** | 新建基准，120 个真实预订任务，分布于 20 个网站，强调多约束、长流程任务。用于主实验评估。 |
| **WebVoyager** | 已有公开基准，包含 643 个跨网站任务，偏重搜索类任务。用于横向对比验证泛化能力。 |

> 注：所有任务均要求代理与真实网站交互，而非静态页面。

---

### ⚙️ 实验设置

#### 模型配置

- **Teacher Agent**：LLaMA 3 70B / LLaMA 3.1 405B Instruct（few-shot prompting）
- **Student Model**：Mistral 3 Small 24B（参数量仅为教师模型的 ~6%）
- **Fine-tuning 方法**：LoRA（仅微调 q_proj 和 v_proj 层）

#### 数据生成流程

1. 使用 GPT-4 生成种子任务 → 扩展至 150k 条任务-轨迹对
2. 教师模型执行任务，生成轨迹 $ T = \{(a_t, o_t)\} $
3. 使用 **LLaMA 3.3 70B 和 Gemma 3 27B** 作为双 Judge 进行约束评估
4. 应用 Prefix Extraction + Hindsight Re-labeling 构建训练集
5. 最终保留约 300k 动作步骤用于训练

#### 评估指标

| 指标 | 定义 |
|------|------|
| **Task Success Rate (SR)** | CSR = 1 的比例（所有约束都满足） |
| **Constraint Satisfaction Rate (CSR)** | 平均每个约束被满足的比例（更细粒度） |
| **Macro-average across websites** | 各网站单独计算后取平均，避免头部网站主导 |

#### 基线方法对比

| 基线 | 类型 |
|------|------|
| **UI-TARS** | 开源 VLM Agent（基于标记点） |
| **Claude Computer Use (CU)** | 商业闭源系统（Anthropic） |
| **Operator (OpenAI)** | 商业闭源系统（OpenAI） |
| **Browser Use** | 基于 SoM 的开源 Agent |
| **Wilbur** | 自适应上下文学习 Agent |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 & Table 5）

#### 在 **BookingArena** 上的表现（平均 SR / CSR）

| 方法 | SR (%) | CSR (%) |
|------|--------|---------|
| Browser Use | 18.8 | 45.3 |
| Claude CU | 16.8 | 48.7 |
| UI-TARS | 26.7 | 53.6 |
| Operator | 33.0 | 68.7 |
| **Ours (Mistral 24B)** | **29.9** | **60.2** |

> 💡 **亮点**：
> - 我们的 **24B 小模型** 在 SR 上优于多数闭源系统（仅次于 Operator），在 CSR 上显著领先于除 Operator 外的所有方法。
> - 特别是在 Booking.com、Vrbo、Trivago 等平台上，CSR 超过 80%，说明能较好满足大部分约束。

#### 在 **WebVoyager** 上的整体表现（Overall SR %）

| 方法 | SR (%) |
|------|--------|
| Wilbur | 52.6 |
| Claude (reported) | 52.8 |
| GPT-4o (re-evaluated) | 55.5 |
| **Ours (Mistral 24B)** | **64.5** |

> ✅ **首次实现**：一个 fine-tuned 的小型语言模型在 WebVoyager 上超越所有基于大型闭源基础模型的方法。

#### 关键领域提升（vs 最佳基线）

| 领域 | 本方法 | 最佳基线 | 提升 |
|------|--------|----------|------|
| **Booking** | 55.0% | 43.9% | **+11.1pp** |
| **Google Flights** | 66.7% | 51.6% | **+15.1pp** |

> 表明该方法在最难的多约束预订任务上具有显著优势。

---

### 🔬 消融实验结果（Table 2）

| 模型 | 数据策略 | SR (%) | CSR (%) |
|------|----------|--------|---------|
| Mistral 24B | Success only | 25.0 | 55.0 |
| Mistral 24B | All trajectories | 29.4 | 57.2 |
| **Mistral 24B** | **Partial success (ours)** | **29.9** | **60.2** |
| Mistral 24B | Partial success + Full FT | 29.4 | 58.7 |

> 发现：
> - 使用 **部分成功轨迹** 显著优于仅使用完全成功轨迹（↑4.9 SR pts）
> - **LoRA 微调 > 全参数微调**，表明 LoRA 更适合此任务，防止过拟合
> - 多阶段训练未带来增益，简单单阶段训练已足够高效

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **细粒度评估（CSR）比二值 Success 更合理**  
   - 对于复杂任务，很多失败轨迹其实完成了大部分约束，应予以利用。
   - CSR 提供了可解释的诊断信号（例如发现 date 错误常见，amenities 满足率低）。

2. **部分成功轨迹是宝贵资源**  
   - 教师模型在难任务上的成功率很低（仅 ~16k 成功 / 150k 总轨迹），若只用成功轨迹会严重浪费数据。
   - 通过 **Prefix Extraction + Hindsight Re-labeling**，可用轨迹数量提升近 4 倍。

3. **小模型也能打败大模型**  
   - 我们的 **24B 学生模型** 不仅超过了 **405B 教师模型**，还优于多数商业系统（如 Claude, Browser Use）。
   - 证明了“高质量数据 + 有效训练”可以弥补参数规模差距。

4. **URL 是重要的评估信号**  
   - 消融实验证明，提供 URL 给 evaluator 能显著提高评估准确性（编码了 location, date 等信息）。
   - 但加入 action history 反而导致误判（judge 会假设动作一定成功），应避免。

---

### ⚠️ 方法的局限性

1. **依赖 LLM 提取约束的质量**  
   - 若 LLM 未能正确识别任务中的隐含约束（如“宠物友好”），会影响后续评估。
   - 当前采用 GPT-4 提取，可能引入偏差。

2. **局限于文本/结构化输入**  
   - 当前使用 accessibility tree 而非原始图像，虽提升了效率，但也丢失视觉布局信息。
   - 未来需结合 VLM 支持 multimodal 观察。

3. **无法处理需要登录或支付的任务**  
   - 遵循 credential-free 和 free-task 原则，排除了需账户或付款的操作。
   - 实际应用中这些仍是重要场景。

---

### 🔮 未来工作方向

1. **扩展到更多平台和环境**  
   - 如移动端 App、桌面软件等非 Web 场景。

2. **引入多模态观察（Multimodal Observations）**  
   - 结合 screenshot 与 accessibility tree，提升状态理解能力。

3. **动态课程学习（Curriculum Learning）**  
   - 根据 CSR 自动调整任务难度，逐步提升 agent 能力。

4. **开放数据集与工具发布**  
   - 作者表示希望公开 BookingArena 和训练代码，推动社区发展。

---

## ✅ 总结一句话

> 本文提出了一套 **可扩展的 Web Agent 训练框架**，通过 **自动合成数据 + 基于约束的细粒度评估**，成功训练了一个 **仅 24B 参数的小模型**，其性能 **超越多数闭源大模型系统**，并在新提出的 **BookingArena** 和 **WebVoyager** 基准上取得 SOTA 结果，为构建高效、低成本的通用 Web 智能体提供了新范式。

</details>

---

### 14. [WebClipper: Efficient Evolution of Web Agents with Graph-based Trajectory Pruning](https://arxiv.org/abs/2602.12852)

**Authors**: Junjie Wang, Zequn Xie, Dan Yang, Jie Feng, Yue Shen, Duolin Sun, Meixiu Long, Yihan Jiao, Zhehao Tan, Jian Wang, Peng Wei, Jinjie Gu  
**Category**: cs.AI  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12852v1  

#### Abstract
Deep Research systems based on web agents have shown strong potential in solving complex information-seeking tasks, yet their search efficiency remains underexplored. We observe that many state-of-the-art open-source web agents rely on long tool-call trajectories with cyclic reasoning loops and expl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：WebClipper: Efficient Evolution of Web Agents with Graph-based Trajectory Pruning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题
当前基于 LLM 的 **web agent** 在复杂信息检索任务中表现出色，但普遍存在严重的 **搜索效率低下** 问题。具体表现为：
- 过度依赖长 **tool-call 轨迹**（如最多 600 轮调用）
- 存在大量 **循环推理（cyclic reasoning）** 和 **无生产力分支（unproductive branches）**
- 忽视 **accuracy 与 efficiency 的平衡**，导致高成本、低用户体验

尽管已有研究关注最终准确率，但对 **搜索过程中的冗余行为优化** 尚未充分探索。

---

### 🧩 提出的新方法与新思路
作者提出 **WebClipper**，一种通过 **graph-based trajectory pruning** 高效演化 web agent 的框架，其核心思想是：
> 将 agent 的执行轨迹建模为 **state graph**，并将其优化转化为 **最小必要有向无环图（MNDAG）挖掘问题**，从而压缩冗余步骤。

#### 主要组件包括：
1. **Trajectory to State-Graph Transformation**  
   将原始轨迹抽象为由 **Action Nodes** 和 **Information Nodes** 构成的有向图，显式刻画动作与信息间的依赖关系。

2. **Pruning via MNDAG**  
   采用最短路径 + 反向闭包策略，提取从初始查询到最终答案的 **最小必要子图（MNDAG）**，去除不贡献于答案的冗余节点。

3. **Coherence-aware Thought Rewriting**  
   对剪枝后的轨迹进行语义连贯性重写，确保逻辑连续性，并通过 **PPL-based selection** 保持与 base model 推理风格一致。

4. **Agent Evolution via Hybrid Training**  
   提出两种训练策略：
   - **Efficiency-oriented evolution**：仅使用剪枝轨迹微调，最大化效率
   - **Hybrid evolution**：混合剪枝与未剪枝轨迹，兼顾效率与准确性

5. **F-AE Score — 新型综合评估指标**  
   提出一个统一衡量 accuracy 与 efficiency 的指标：
   $$
   \text{F-AE} = 2 \times \frac{\text{Acc} \times (1 - \text{Rounds}/\text{Max\_Rounds})}{\text{Acc} + (1 - \text{Rounds}/\text{Max\_Rounds})}
   $$
   类似 F1-score，惩罚任一维度极端表现，强调平衡性。

---

### 🔍 相比现有方法的优势
| 维度 | WebClipper | 现有方法 |
|------|------------|----------|
| **优化方式** | 结构化图分析 + 精细剪枝 | Prompt 控制 / 整体 CoT 压缩 |
| **效率提升** | 显著减少 tool-call 轮数（↓20%） | 改进有限或牺牲准确率 |
| **准确性保持** | 维持甚至提升 accuracy | 剪枝后常出现性能下降 |
| **泛化能力** | 可用于已有高性能 agent 的“进化” | 多需从头训练 |

> ✅ **核心优势**：无需从零训练，即可将已有的高精度但低效 agent “进化”为更高效版本。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验覆盖多个主流 web agent benchmark：
- **xbench-deepsearch**：深度研究类任务
- **Browsecomp**：网页浏览挑战
- **GAIA**：通用 AI 助手基准（使用 dev set 中 103 个文本任务）
- **HLE**（Humanity’s Last Exam）：高难度多跳推理任务（使用 500 样本子集）

所有数据均来自公开 QA 数据集（如 WebShaper、WebDancer、TaskCraft 等），并通过 rejection sampling 筛选具有挑战性的样本（Pass Rate ≤ 0.5）。

---

### ⚙️ 实验设置与评估指标

#### 模型基础
- **Base Agent**: Tongyi-DeepResearch (30B-A3B)
- **Extractor & Rewriter**: Qwen3-235B-A22B-Instruct-2507
- **Training Platform**: 32×H800 GPU，学习率 5e-6，cosine 衰减

#### 工具链
- 搜索工具：Serper API
- 页面解析：Jina Reader
- 代码执行：内置 Python Interpreter

#### 评估指标
| 指标 | 描述 |
|------|------|
| **Accuracy (Acc)** | 使用 LLM-as-Judge（o3-mini）评估回答正确性 |
| **Tool-call Rounds ↓** | 平均调用外部工具次数 |
| **Token Consumption ↓** | 推理阶段总 token 数量 |
| **F-AE Score ↑** | 综合 accuracy 与 efficiency 的调和平均值（Max_Rounds=100） |

---

### 🆚 基线方法对比
| 类别 | 方法 | 说明 |
|------|------|------|
| **Closed-source** | OpenAI o3, DeepResearch, Claude-4-Sonnet | 商业系统，引用官方报告结果 |
| **Open-source** | Kimi-K2, DeepSeek-R1, Qwen3, WebExplorer, Tongyi-DeepResearch | 开源模型，部分复现于统一环境 |
| **Pruning Baselines** | Prompt Control, Coarse Prune | 自定义对比：<br>- Prompt Control：添加效率提示<br>- Coarse Prune：直接让 LLM 删除冗余轮次 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（平均 across benchmarks）

| 方法 | Accuracy ↑ | Tool-call Rounds ↓ | Token Usage ↓ | F-AE Score ↑ |
|------|-----------|---------------------|----------------|---------------|
| Tongyi-DeepResearch (base) | 0.541 | 30.63 | ~9.7k | 0.602 |
| WebClipper (Eff) | **0.541** | **24.2** (-21%) | **~7.8k (-19.4%)** | **0.628** |
| WebClipper (Hybrid) | **0.565 (+4.8%)** | **27.0 (-12%)** | ~9.0k | **0.635** |

> 💡 **结论**：WebClipper 在显著降低资源消耗的同时，不仅维持了 accuracy，反而略有提升。

---

### 🔁 与基线方法对比结果

#### ✅ WebClipper vs. Prompt Control
- Prompt Control 仅略微减少 rounds（~12%），且 accuracy 下降明显（如 xbench 上从 0.713 → 0.676）
- 表明 **简单指令无法有效引导 agent 提高效率**

#### ❌ WebClipper vs. Coarse Prune
- Coarse Prune 导致 accuracy 断崖式下跌（如 GAIA 上从 0.682 → 0.514）
- 说明 **粗粒度剪枝缺乏结构保障，易破坏推理链完整性**

#### 🏆 WebClipper 成为开源模型中 F-AE 最高者
- 在所有 benchmark 上，WebClipper (Hybrid/Eff) 均取得 **最高或第二高的 F-AE Score**
- 特别是在 GAIA 上，tool-call 减少约 **30%**，体现其对非工具密集型任务的适应性更强

---

### 🔍 消融实验结果（Ablation Study）

#### （1）组件有效性验证（见 Figure 4）
| 变体 | 描述 | 影响 |
|------|------|------|
| w/o GP (Graph Pruning) | 替换为 Coarse Prune | 性能大幅下降 → 图结构分析至关重要 |
| w/o PPL-S | 移除 perplexity-based selection | 风格不一致导致 coherence 降低 |
| w/o CSR | 移除 context-aware rewriting | 出现“灾难性崩溃” → 上下文感知重写不可或缺 |

> ✅ 所有核心模块均被证明有效，尤其 **coherence-aware rewriting 是成功关键**

#### （2）训练策略比较（见 Table 2）
| 策略 | 特点 | 适用场景 |
|------|------|----------|
| Unpruned-Distill | 直接 SFT 原始轨迹 | accuracy 提升但 rounds 增加 |
| WebClipper (Eff) | 仅用剪枝轨迹 | 最低资源消耗，适合成本敏感部署 |
| WebClipper (Hybrid) | 混合训练 | **最佳 F-AE**，实现 accuracy 与 efficiency 全面超越 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **冗余行为普遍存在**：现有 web agent 存在大量循环推理与无关探索，严重拖累效率。
2. **图结构建模有效揭示冗余模式**：state graph 可清晰识别 MNDAG，支持精准剪枝。
3. **剪枝可同时提升 accuracy 与 efficiency**：去除噪声信息有助于聚焦关键路径，避免 context dilution。
4. **F-AE Score 是合理评价标准**：能有效反映 accuracy-efficiency trade-off，防止单一维度过优化。
5. **无需从头训练也能进化 agent**：WebClipper 提供了一种“后训练优化”新范式。

---

### ⚠️ 方法的局限性
1. **依赖 base model 的能力上限**：若 base agent 本身规划能力差，剪枝只能优化已有路径，无法创造全新策略。
2. **泛化性受限于工具类型**：目前仅适用于 search/visit/python 等文本类工具，对 multimodal、database query 等新型 action 支持不足。
3. **图构建依赖 LLM extractor**：存在一定的标注噪声与不一致性，虽通过 majority vote 缓解但仍非完全可靠。

---

### 🔮 未来工作方向
1. **结合 Reinforcement Learning**：引入 online learning 或 RL 机制，使 agent 能主动发现更优搜索策略。
2. **扩展至多模态与 API 工具空间**：将 state graph 框架推广至图像、语音、数据库等多样化 action 类型。
3. **动态自适应剪枝**：根据任务难度实时调整剪枝强度，实现个性化效率控制。
4. **构建通用 agent 压缩 pipeline**：将 WebClipper 推广为通用的 agent 蒸馏与加速工具链。

---

> 📌 **总结一句话**：  
> **WebClipper 通过 graph-based trajectory pruning 实现了 web agent 的“高效进化”，在不牺牲甚至提升 accuracy 的前提下，显著降低了 tool-call 与 token 消耗，为构建实用化 deep research 系统提供了新路径。**

</details>

---

### 15. [Adaptive Structured Pruning of Convolutional Neural Networks for Time Series Classification](https://arxiv.org/abs/2602.12744)

**Authors**: Javidan Abdullayev, Maxime Devanne, Cyril Meyer, Ali Ismail-Fawaz, Jonathan Weber, Germain Forestier  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12744v1  

#### Abstract
Deep learning models for Time Series Classification (TSC) have achieved strong predictive performance but their high computational and memory requirements often limit deployment on resource-constrained devices. While structured pruning can address these issues by removing redundant filters, existing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Adaptive Structured Pruning of Convolutional Neural Networks for Time Series Classification*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **Time Series Classification (TSC)** 深度学习模型虽然性能优越，但由于其高计算成本和内存占用，难以部署在资源受限设备（如边缘设备）上。尽管已有模型压缩技术（如剪枝），但大多数**结构化剪枝**（structured pruning）方法依赖人工设定的超参数（如剪枝比例），缺乏自适应性，限制了其在多样化数据集上的泛化能力和可扩展性。

### 提出了什么新方法或新思路
本文提出了一种全新的、完全自动化的结构化剪枝框架——**Dynamic Structured Pruning (DSP)**，用于基于卷积的 TSC 模型。该方法无需任何预设的剪枝比例或手动调参，实现端到端的模型压缩。

核心思想包括两个阶段：
- **训练阶段引入实例级稀疏损失**（instance-wise sparsity loss）：通过在训练过程中施加一个层级化的 $L_2$-$L_1$ 正则项，鼓励通道级别的激活稀疏性，使冗余通道自然趋于零响应。
- **全局激活分析进行剪枝决策**：在训练后对整个数据集的特征图进行全局统计分析，仅当某个滤波器在所有样本中均低于动态阈值时才被移除，避免因单个输入导致误剪。

### 相比现有方法的优势
- ✅ **无需手动定义剪枝比例**：完全自动化，适用于不同复杂度的数据集。
- ✅ **硬件友好**：采用结构化剪枝（移除整个通道/滤波器），生成紧凑且适合边缘部署的模型。
- ✅ **保持高性能**：在大幅压缩的同时维持甚至提升分类准确率。
- ✅ **跨架构通用性强**：验证于 LITETime 和 InceptionTime 两种主流 TSC 架构，表现一致优异。
- ✅ **避免迭代剪枝-重训不稳定**：采用一次性剪枝+从头训练策略，提升稳定性。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- 在 **UCR Time Series Classification Archive** 上进行了全面评估，共使用 **128 个公开数据集**，覆盖多个领域（医疗、环境、动作识别等）。
- 所有时间序列均经过 **z-normalization** 处理，并遵循标准的训练/测试划分。

### 实验设置和评估指标
#### 模型架构
- 基础模型为当前最先进的两类 CNN 架构：
  - **LITETime**
  - **InceptionTime**

#### 模型变体对比
| 模型类型 | 名称 |
|--------|------|
| 原始未压缩模型 | Base |
| 预训练带稀疏损失 | Pretrained |
| 剪枝后模型 | Pruned |
| 微调恢复性能 | Finetuned |
| 从头重新训练 | Scratch-trained |

最终提出的模型称为：**DSP-LITETime** 和 **DSP-InceptionTime**。

#### 评估协议
- 主要指标：**Accuracy**（分类准确率）
- 统计分析工具：**Multi-Comparison Matrix (MCM)**，包含：
  - Mean-Accuracy（平均准确率）
  - Mean-Difference（成对差异）
  - Win/Tie/Loss 计数
- 显著性检验：**Wilcoxon signed-rank test**（$p < 0.05$ 视为显著）

#### 实现细节
- 优化器：Adam（初始学习率 0.001）
- Batch size：64
- Epochs：1500
- 硬件平台：NVIDIA RTX 4090 GPU
- 框架：PyTorch 2.5.1
- 开源代码地址：[https://github.com/MSD-IRIMAS/Pruning4TSC](https://github.com/MSD-IRIMAS/Pruning4TSC)

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 平均压缩率 | 参数量减少 | FLOPS 减少 | 内存占用 |
|------|------------|-----------|------------|----------|
| **DSP-LITETime** | **58%** | 从 50.5K → 21.3K (-58%) | 57M → 22M (-61%) | 0.2MB → 0.09MB |
| **DSP-InceptionTime** | **75%** | 从 2.1M → 536K (-75%) | 2265M → 443M (-80%) | 8.04MB → 2.05MB |

> 注：压缩率指平均剪枝比例；FLOPS 表示每秒浮点运算次数。

### 与基线方法的对比结果
- **性能保持方面**：
  - DSP 模型在绝大多数数据集上与原始模型性能相当，部分简单任务中略有提升（可能因正则效应）。
  - 在 **EthanolLevel 数据集**上，DSP 模型实现了更高的准确率（~87.5% vs ~85%），同时参数减少超过 85%，FLOPS 下降一个数量级（见 Figure 1）。
- **与其他 SOTA 方法比较**（Figure 7）：
  - 虽然 **MultiROCKET** 和 **H-InceptionTime** 性能略优，但这归因于其更强的基础架构而非剪枝方法本身。
  - DSP 模型显著优于传统架构如 **ResNet** 和 **FCN**。
  - 与 **ROCKET** 相比无统计显著差异，说明压缩未牺牲性能。

### 消融实验结果
#### 不同重训练策略的影响（Figure 5）
- **Pruned 模型直接使用**：性能明显下降（因结构破坏）。
- **Fine-tuning**：部分恢复性能，但仍显著低于原始模型。
- **Scratch training**：**完全恢复甚至略微超越原始模型性能**，是关键步骤。

#### 动态 vs 静态剪枝对比（Figure 10）
- 固定剪枝配置（LITETime_16/_8/_4）无法适应不同数据集复杂度。
- 更激进的静态剪枝（如 84.9%）导致显著精度下降。
- **DSP-LITETime 在相似压缩率下达到更高或相当精度**，证明其动态适应能力优势。

#### 稀疏损失系数 $\lambda$ 的影响（Figure 12）
- $\lambda = 0$：等价于原始模型。
- $\lambda = 1e^{-7} \sim 1e^{-5}$：性能稳定，最佳平衡点选为 $1e^{-5}$。
- $\lambda = 1e^{-4}$：过大惩罚导致模型表达能力受损，准确率骤降至 0.7593。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **DSP 可实现高达 75% 的模型压缩而几乎不损失准确率**，特别适用于 InceptionTime 类复杂模型。
2. ✅ **“预训练+全局激活分析+从头训练”流程有效且稳定**，优于微调方式。
3. ✅ **动态剪枝机制能根据数据集复杂度自适应调整剪枝强度**，无需人工干预。
4. ✅ **剪枝后的模型具有更低冗余性**：通过 **effective rank** 分析表明，DSP 学得的表示更紧凑高效（Figure 11）。
5. ✅ **可视化验证了稀疏训练的有效性**：经典 LITE 存在大量重复激活通道，而 DSP 显著抑制冗余响应（Figure 13）。

### 方法的局限性
- 当前仅验证于 **CNN 架构**（LITETime / InceptionTime），尚未扩展至 Transformer 或 RNN 等其他结构。
- 对极小样本数据集（如 PigAirwayPressure，仅 102 样本、52 类）效果较差，剪枝可能导致重要特征丢失。
- 仍需完整训练一轮稀疏模型，整体训练时间较长（三阶段流程）。

### 未来工作方向
- 将 DSP 框架推广至 **Transformer、RNN 等非 CNN 架构**。
- 探索与其他压缩技术结合，如：
  - **Quantization**（量化）
  - **Knowledge Distillation**（知识蒸馏）
- 设计更高效的稀疏训练策略以缩短总训练周期。
- 进一步研究如何在极端低资源场景下保护关键特征不被误剪。

--- 

> **总结一句话**：  
> DSP 是首个完全自动、无需调参的结构化剪枝框架，可在保持 SOTA 分类性能的前提下，将主流 TSC 模型压缩 **58%-75%**，为深度模型在边缘设备上的高效部署提供了实用解决方案。

</details>

---

### 16. [Stabilizing Native Low-Rank LLM Pretraining](https://arxiv.org/abs/2602.12429)

**Authors**: Paul Janson, Edouard Oyallon, Eugene Belilovsky  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12429v1  

#### Abstract
Foundation models have achieved remarkable success, yet their growing parameter counts pose significant computational and memory challenges. Low-rank factorization offers a promising route to reduce training and inference costs, but the community lacks a stable recipe for training models from scratc...

---

### 17. [Multi-Head Attention as a Source of Catastrophic Forgetting in MoE Transformers](https://arxiv.org/abs/2602.12587)

**Authors**: Anrui Chen, Ruijun Huang, Xin Zhang, Fang Dong, Hengjie Cao, Zhendong Huang, Yifeng Yang, Mengyi Chen, Jixian Zhou, Mingzhi Dong, Yujiang Wang, Jinlong Hou, Qin Lv, Robert P. Dick, Yuan Cheng, Tun Lu, Fan Yang, Li Shang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12587v1  

#### Abstract
Mixture-of-Experts (MoE) architectures are often considered a natural fit for continual learning because sparse routing should localize updates and reduce interference, yet MoE Transformers still forget substantially even with sparse, well-balanced expert utilization. We attribute this gap to a pre-...

---

### 18. [Coden: Efficient Temporal Graph Neural Networks for Continuous Prediction](https://arxiv.org/abs/2602.12613)

**Authors**: Zulun Zhu, Siqiang Luo  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12613v1  

#### Abstract
Temporal Graph Neural Networks (TGNNs) are pivotal in processing dynamic graphs. However, existing TGNNs primarily target one-time predictions for a given temporal span, whereas many practical applications require continuous predictions, that predictions are issued frequently over time. Directly ada...

---

### 19. [Formalizing the Sampling Design Space of Diffusion-Based Generative Models via Adaptive Solvers and Wasserstein-Bounded Timesteps](https://arxiv.org/abs/2602.12624)

**Authors**: Sangwoo Jo, Sungjoon Choi  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12624v1  

#### Abstract
Diffusion-based generative models have achieved remarkable performance across various domains, yet their practical deployment is often limited by high sampling costs. While prior work focuses on training objectives or individual solvers, the holistic design of sampling, specifically solver selection...

---

### 20. [Physics-Informed Laplace Neural Operator for Solving Partial Differential Equations](https://arxiv.org/abs/2602.12706)

**Authors**: Heechang Kim, Qianying Cao, Hyomin Shin, Seungchul Lee, George Em Karniadakis, Minseok Choi  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12706v1  

#### Abstract
Neural operators have emerged as fast surrogate solvers for parametric partial differential equations (PDEs). However, purely data-driven models often require extensive training data and can generalize poorly, especially in small-data regimes and under unseen (out-of-distribution) input functions th...

---

### 21. [Closing the Loop: A Control-Theoretic Framework for Provably Stable Time Series Forecasting with LLMs](https://arxiv.org/abs/2602.12756)

**Authors**: Xingyu Zhang, Hanyun Du, Zeen Song, Jianqi Zhang, Changwen Zheng, Wenwen Qiang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.12756v1  

#### Abstract
Large Language Models (LLMs) have recently shown exceptional potential in time series forecasting, leveraging their inherent sequential reasoning capabilities to model complex temporal dynamics. However, existing approaches typically employ a naive autoregressive generation strategy. We identify a c...

---

### 22. [R-Diverse: Mitigating Diversity Illusion in Self-Play LLM Training](https://arxiv.org/abs/2602.13103)

**Authors**: Gengsheng Li, Jinghan He, Shijie Wang, Dan Zhang, Ruiqi Liu, Renrui Zhang, Zijun Yao, Junfeng Fang, Haiyun Guo, Jinqiao Wang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.13103v1  

#### Abstract
Self-play bootstraps LLM reasoning through an iterative Challenger-Solver loop: the Challenger is trained to generate questions that target the Solver's capabilities, and the Solver is optimized on the generated data to expand its reasoning skills. However, existing frameworks like R-Zero often exhi...

---

### 23. [SciAgentGym: Benchmarking Multi-Step Scientific Tool-use in LLM Agents](https://arxiv.org/abs/2602.12984)

**Authors**: Yujiong Shen, Yajie Yang, Zhiheng Xi, Binze Hu, Huayu Sha, Jiazheng Zhang, Qiyuan Peng, Junlin Shang, Jixuan Huang, Yutao Fan, Jingqi Tong, Shihan Dou, Ming Zhang, Lei Bai, Zhenfei Yin, Tao Gui, Xingjun Ma, Qi Zhang, Xuanjing Huang, Yu-Gang Jiang  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.12984v1  

#### Abstract
Scientific reasoning inherently demands integrating sophisticated toolkits to navigate domain-specific knowledge. Yet, current benchmarks largely overlook agents' ability to orchestrate tools for such rigorous workflows. To bridge this gap, we introduce SciAgentGym, a scalable interactive environmen...

---

### 24. [Flow-Factory: A Unified Framework for Reinforcement Learning in Flow-Matching Models](https://arxiv.org/abs/2602.12529)

**Authors**: Bowen Ping, Chengyou Jia, Minnan Luo, Hangwei Qian, Ivor Tsang  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.12529v1  

#### Abstract
Reinforcement learning has emerged as a promising paradigm for aligning diffusion and flow-matching models with human preferences, yet practitioners face fragmented codebases, model-specific implementations, and engineering complexity. We introduce Flow-Factory, a unified framework that decouples al...

---

### 25. [Eventizing Traditionally Opaque Binary Neural Networks as 1-safe Petri net Models](https://arxiv.org/abs/2602.13128)

**Authors**: Mohamed Tarraf, Alex Chan, Alex Yakovlev, Rishad Shafik  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.13128v1  

#### Abstract
Binary Neural Networks (BNNs) offer a low-complexity and energy-efficient alternative to traditional full-precision neural networks by constraining their weights and activations to binary values. However, their discrete, highly non-linear behavior makes them difficult to explain, validate and formal...

---

### 26. [Learning to Approximate Uniform Facility Location via Graph Neural Networks](https://arxiv.org/abs/2602.13155)

**Authors**: Chendi Qian, Christopher Morris, Stefanie Jegelka, Christian Sohler  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.13155v1  

#### Abstract
There has been a growing interest in using neural networks, especially message-passing neural networks (MPNNs), to solve hard combinatorial optimization problems heuristically. However, existing learning-based approaches for hard combinatorial optimization tasks often rely on supervised training dat...

---

### 27. [Learning Ordinal Probabilistic Reward from Preferences](https://arxiv.org/abs/2602.12660)

**Authors**: Longze Chen, Lu Wang, Renke Shan, Ze Gong, Run Luo, Jiaming Li, Jing Luo, Qiyao Wang, Min Yang  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2602.12660v1  

#### Abstract
Reward models are crucial for aligning large language models (LLMs) with human values and intentions. Existing approaches follow either Generative (GRMs) or Discriminative (DRMs) paradigms, yet both suffer from limitations: GRMs typically demand costly point-wise supervision, while DRMs produce unca...

---

### 28. [$\mathcal{X}$-KD: General Experiential Knowledge Distillation for Large Language Models](https://arxiv.org/abs/2602.12674)

**Authors**: Yuang Cai, Yuyu Yuan  
**Category**: cs.CL  
**Published**: 2026-02-16  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2602.12674v1  

#### Abstract
Knowledge Distillation (KD) for Large Language Models (LLMs) has become increasingly important as models grow in size and complexity. While existing distillation approaches focus on imitating teacher behavior, they often overlook the original learning environment that shaped the teacher's knowledge....

---

### 29. [Regularized Meta-Learning for Improved Generalization](https://arxiv.org/abs/2602.12469)

**Authors**: Noor Islam S. Mohammad, Md Muntaqim Meherab  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2602.12469v1  

#### Abstract
Deep ensemble methods often improve predictive performance, yet they suffer from three practical limitations: redundancy among base models that inflates computational cost and degrades conditioning, unstable weighting under multicollinearity, and overfitting in meta-learning pipelines. We propose a ...

---

### 30. [RelBench v2: A Large-Scale Benchmark and Repository for Relational Data](https://arxiv.org/abs/2602.12606)

**Authors**: Justin Gu, Rishabh Ranjan, Charilaos Kanatsoulis, Haiming Tang, Martin Jurkovic, Valter Hudovernik, Mark Znidar, Pranshu Chaturvedi, Parth Shroff, Fengyu Li, Jure Leskovec  
**Category**: cs.LG  
**Published**: 2026-02-16  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2602.12606v1  

#### Abstract
Relational deep learning (RDL) has emerged as a powerful paradigm for learning directly on relational databases by modeling entities and their relationships across multiple interconnected tables. As this paradigm evolves toward larger models and relational foundation models, scalable and realistic b...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
