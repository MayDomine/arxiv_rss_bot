# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-06 05:59:48 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [OrchestrRL: Dynamic Compute and Network Orchestration for Disaggregated RL](https://arxiv.org/abs/2601.01209)

**Authors**: Xin Tan, Yicheng Feng, Yu Zhou, Yimin Jiang, Yibo Zhu, Hong Xu  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.01209v1  

#### Abstract
Post-training with reinforcement learning (RL) has greatly enhanced the capabilities of large language models. Disaggregating the generation and training stages in RL into a parallel, asynchronous pipeline offers the potential for flexible scaling and improved throughput. However, it still faces two...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*OrchestrRL: Dynamic Compute and Network Orchestration for Disaggregated RL*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对**解耦式强化学习（disaggregated RL）系统中的两个核心瓶颈**：

1. **生成阶段（Generation）成为端到端性能瓶颈**：
   - 由于输出长度分布长尾、请求处理时间不均衡（stragglers）、KV-cache 利用率波动等问题，导致生成步骤的 *makespan*（完成所有样本的时间）显著增加。
   - 静态并行策略无法适应跨步（across steps）和步内（within step）的工作负载动态变化。

2. **网络架构与通信模式不匹配**：
   - 解耦式 RL 在训练（Train）、生成（Gen）和权重同步（Weight Sync）阶段表现出高度异构且动态的通信模式（如集体通信、双端通信、突发广播等）。
   - 传统静态网络（如 Fat-Tree）难以高效支持这些多样化的流量，导致带宽浪费或拥塞。

---

### **提出了什么新方法或新思路**

论文提出 **OrchestrRL**，一个面向解耦式 RL 的**计算与网络协同编排框架**，其核心创新包括：

#### ✅ **OrchestrRL Compute Scheduler（计算调度器）**
- **Proactive Planner（主动规划器）**：
  - 周期性运行，基于在线预测的响应长度分布（ARIMA 模型）和当前请求进度，通过求解 **MILP（Mixed-Integer Linear Program）** 来选择最优的并行策略（如 TP、EP、AFD）。
  - 支持在生成过程中动态切换并行配置（dynamic parallelism switching），以应对工作负载漂移。
- **Reactive Balancer（反应式平衡器）**：
  - 持续监控各生成节点的负载，定义了一个综合考虑队列深度、KV-cache 余量和服务速率的 **LoadIndex** 指标。
  - 通过轻量级请求迁移（request migration）缓解短时负载不均，减少 stragglers。

#### ✅ **RFabric（可重构混合光-电网络架构）**
- 一种专为解耦式 RL 设计的 **分层混合光电路交换（OCS）+ 电子分组交换（EPS）网络**。
- 支持**按需拓扑实例化（on-demand topology materialization）**，根据不同阶段需求动态重构网络：
  - **Training Phase**：构建高二分带宽的互连拓扑，支持 DP AllReduce 等大规模集体通信。
  - **Generation Phase**：隔离 PoD 内部通信，优化 TP/EP/AFD 等局部通信模式。
  - **Weight Sync Phase**：构建多播树结构，实现跨集群的高效参数广播。
- 利用不同阶段之间的通信空闲窗口（reconfiguration slack）进行无干扰重构。

#### ✅ **统一控制平面**
- 将计算调度与网络协调联合设计，形成闭环反馈系统，实现“**计算驱动网络，网络反哺计算**”的协同优化。

---

### **相比现有方法的优势**

| 维度 | 现有方法 | OrchestrRL |
|------|--------|-----------|
| **并行策略** | 静态配置（如固定 TP=8） | 动态切换，自适应负载演化 |
| **负载均衡** | 无或粗粒度 | 轻量级实时迁移 + 多维度负载指标 |
| **网络架构** | 固定拓扑（如 Fat-Tree） | 可重构混合光-电架构（RFabric） |
| **通信适配性** | “一刀切”设计 | 按阶段定制拓扑（训练/生成/同步） |
| **成本效率** | 昂贵的全通网络 | 仅在需要时提供高带宽，节省光学资源 |

> 📌 **关键优势总结**：OrchestrRL 是首个将**动态并行重配置**与**阶段感知网络重构**结合用于端到端 RL 流水线的系统，实现了计算与网络的协同节奏调控（orchestrate rhythms）。

---

## 2. 核心实验方法和设置

### **使用的数据集与模型**

- **数据集**：
  - `openr1-math-220k`（用于 Qwen-14B）
  - `deepmath-103k`（用于 Qwen-32B）
- **模型**：
  - Qwen-2.5 14B 和 32B
- **RL 算法**：GRPO（一种策略梯度算法）

---

### **实验设置**

#### **物理测试床（Testbed）**
- **硬件**：48 台配备 NVIDIA H800 GPU 的服务器
  - Train Cluster：32 GPUs
  - Gen Cluster：40 GPUs（部分实验使用 32 或 48）
- **软件栈**：
  - 训练框架：Megatron-LM
  - 推理后端：vLLM（支持 PagedAttention）

#### **大规模仿真平台（RLSim）**
- 开发了高保真模拟器 **RLSim**，用于评估 RFabric 在千级 GPU 规模下的表现。
- 支持多种网络拓扑建模（Fat-Tree、TopoOpt、RFabric 等）和细粒度通信模拟。

---

### **评估指标**

| 指标 | 描述 |
|------|------|
| **End-to-End Throughput** | 单位时间内完成的训练步数（steps/sec），为主要性能指标 |
| **Per-Step Makespan** | 每个生成步骤的总耗时，反映生成效率 |
| **Cost-Efficiency** | 性能与网络建设成本的比值（如吞吐量 / 网络开销） |
| **Reconfiguration Overhead** | 并行切换与网络重构带来的延迟 |

---

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **veRL-TO** | 优先数据并行（DP），最大化并发实例数 |
| **veRL-LO** | 优先张量并行（TP=8），最小化单实例延迟 |
| **Partial-Rollout (PR)** | 允许响应跨两个模型版本继续生成，缓解长尾问题 |
| **Fat-Tree (FT)** | 非阻塞 Fat-Tree 网络（理想基准） |
| **Fat-Tree-OS (FT-OS)** | 3:1 过订阅 Fat-Tree |
| **Rail-Optimized (RO)** | NVIDIA 推荐的低延迟互联 |
| **TopoOpt [37]** | 当前最先进的 OCS 架构之一，用于对比网络性能 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 🔹 **端到端吞吐提升（Testbed 实测）**

| 模型 | 配置 | 最大生成长度 | 相对 veRL-TO 提升 |
|------|------|--------------|------------------|
| Qwen-14B | 32 GPUs | 15K tokens | **1.31×** |
| Qwen-14B | 32 GPUs | 25K tokens | **1.40×** |
| Qwen-32B | 48 GPUs | 15K tokens | **1.32×** |
| Qwen-32B | 48 GPUs | 25K tokens | **1.34×** |

> ✅ **最高达 1.40× 吞吐提升**，尤其在长序列场景下优势更明显。

---

#### 🔹 **消融实验（Ablation Study）**

| 组件 | Qwen-14B 提升 | Qwen-32B 提升 |
|------|---------------|----------------|
| Baseline（veRL-TO） | 1.00× | 1.00× |
| + Proactive Planning (PP) | 1.23× | 1.19× |
| + Reactive Balancing (RB) | **1.40×** | **1.34×** |

> 📊 结果表明：
> - **主动规划（PP）** 贡献约 20%~23% 提升；
> - **反应式平衡（RB）** 在此基础上再带来约 14%~15% 提升；
> - 两者协同作用显著。

---

#### 🔹 **大规模网络仿真结果（RLSim）**

| 指标 | RFabric 表现 |
|------|-------------|
| **相对非阻塞 Fat-Tree 性能** | 达到 **~98%** |
| **相比 Fat-Tree 成本效率** | 提升 **2.2×–3.1×** |
| **相比 Rail-Optimized 成本效率** | 提升 **2.3×–3.2×** |
| **在 2048 GPU 规模下性能保持稳定** | ✔️ 显著优于 TopoOpt 和 FT-OS |

> 📈 图 14 显示 RFabric 在性能-成本帕累托前沿上全面领先。

---

## 4. 关键结论和发现

### **主要发现**

1. **生成阶段是解耦式 RL 的主要瓶颈**：
   - 输出长度的长尾分布和 KV-cache 不均衡导致严重的 straggler 问题。
   - 静态并行策略无法适应动态负载，必须引入**运行时重配置机制**。

2. **网络必须具备“节奏感知”能力**：
   - RL 工作流具有明显的**时空异质性**（spatiotemporal variability）。
   - 利用不同阶段间的通信空闲窗口（slack）进行 OCS 重构，可在不影响关键路径的前提下实现高性能通信。

3. **计算与网络需协同设计**：
   - OrchestrRL 证明了“**动态并行切换 → 改变通信模式 → 触发网络重构**”这一闭环的有效性。
   - 单独优化计算或网络无法达到全局最优。

4. **RFabric 实现了高性能与高性价比的统一**：
   - 在接近理想 Fat-Tree 性能的同时，大幅降低网络成本（尤其是光学收发器数量）。
   - 特别适合大规模、高带宽需求的 RL 场景。

---

### **方法的局限性**

1. **OCS 重构延迟限制**：
   - 当前 OCS 技术（如 3D MEMS, ~10ms）不适合处理微秒级通信事件，因此边缘仍依赖 EPS。
   - 对极短周期任务的支持有限。

2. **MILP 求解复杂度**：
   - 虽然采用启发式剪枝（如请求分桶、候选集筛选），但在超大规模集群中仍可能面临实时性挑战。

3. **未充分验证 MoE 和 AFD 场景**：
   - 论文提到未来将扩展至 MoE 模型，目前实验主要基于 Dense 模型。

4. **依赖准确的负载预测**：
   - ARIMA 模型对响应长度分布的预测误差会影响规划质量，极端情况下可能导致次优决策。

---

### **未来工作方向**

1. **支持更多并行范式**：
   - 扩展对 **Attention-FFN Disaggregation (AFD)** 和 **Mixture-of-Experts (MoE)** 的完整支持。

2. **集成更先进的预测模型**：
   - 使用轻量级 ML 模型替代 ARIMA，提升剩余工作量预测精度。

3. **探索全栈协同优化**：
   - 将 OrchestrRL 与 LLM Agent Workflow、Speculative Decoding 等技术结合，进一步提升端到端效率。

4. **硬件协同设计**：
   - 探索更低延迟、更高密度的 OCS 器件（如硅光芯片）以支持更细粒度重构。

5. **跨作业调度与资源共享**：
   - 在多租户环境下实现多个 RL 任务间的资源动态分配与干扰控制。

---

> ✅ **总结一句话**：  
> **OrchestrRL 通过“动态计算调度 + 可重构光-电网络”的协同设计，首次实现了对解耦式 RL 中计算与网络节奏的联合调控，在真实测试床上实现了最高 1.40× 的吞吐提升，并在大规模仿真中展现出卓越的成本效益，为下一代大规模 RL 系统提供了新的架构范式。**

</details>

---

### 2. [DiT-HC: Enabling Efficient Training of Visual Generation Model DiT on HPC-oriented CPU Cluster](https://arxiv.org/abs/2601.01500)

**Authors**: Jinxiao Zhang, Yunpu Xu, Xiyong Wu, Runmin Dong, Shenggan Cheng, Yi Zhao, Mengxuan Chen, Qinrui Zheng, Jianting Liu, Haohuan Fu  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.01500v1  

#### Abstract
Generative foundation models have become an important tool for data reconstruction and simulation in scientific computing, showing a tight integration with traditional numerical simulations. At the same time, with the development of new hardware features, such as matrix acceleration units and high-b...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DiT-HC: Enabling Efficient Training of Visual Generation Model DiT on HPC-oriented CPU Cluster

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前生成式基础模型（如Diffusion Models）在科学计算中日益重要，但其训练主要依赖GPU/TPU平台。而**HPC（高性能计算）系统多以CPU集群为主**，传统上缺乏对大规模生成模型高效训练的支持。这导致AI与科学模拟难以统一部署在同一HPC平台上。

此外，尽管新一代HPC CPU已引入**Matrix Acceleration Units (MAUs)** 和 **On-Package Memory (OPM)** 等AI加速特性，但由于指令集、内存层级复杂，现有框架（如PyTorch）无法有效利用这些硬件优势。

### 提出的新方法与创新点
作者提出 **DiT-HC** ——首个在面向HPC的CPU集群上高效训练视觉生成模型 **DiT (Diffusion Transformer)** 的可扩展训练框架，包含三大核心技术：

#### （1）通信自由张量并行（CFTP）
- **创新思路**：传统Tensor Parallelism（TP）需要跨进程通信同步中间结果，带来显著开销。
- **新方法**：利用同一Die内CPU Cluster共享DDR控制器且访问延迟一致的特点，将TP操作改为**通过共享内存直接读写**，避免MPI通信。
- **优势**：
  - 消除TP内部通信开销；
  - 减少MPI进程数量（从每节点16个降至4个）；
  - 支持灵活张量切分，降低内存冗余。

#### （2）自动内存感知数据流管理模块 AutoMem
- **背景挑战**：LX2 CPU采用OPM+DDR混合内存架构，OPM带宽高但容量小，手动管理数据迁移复杂。
- **新方法**：设计非侵入式AutoMem模块，在前向/反向传播前后插入hook，实现：
  - 自动判断是否需加载权重/激活到OPM；
  - 预取下一层参数；
  - 使用SDMA异步搬运数据；
  - 利用大页内存（Huge Page）提升TLB命中率。
- **优势**：
  - 充分利用高带宽OPM；
  - 实现计算、通信、内存搬运重叠；
  - 无需修改模型代码即可复用优化策略。

#### （3）专为HPC CPU优化的操作符库 HCOps
- **目标**：充分发挥LX2 CPU中的VAU（Vector Acceleration Unit）和MAU（Matrix Acceleration Unit）能力。
- **优化内容**：
  - **GEMM**：提出NUMA-aware的二维分块策略，减少远程NUMA访问，提高L2缓存利用率；
  - **AI Operators**：对GeLU、SiLU、Softmax、LayerNorm等进行低层intrinsics优化；
  - **AdamW融合算子**：减少内存写回次数，提速12.5×。
- **成果**：发布开源扩展包HCOps，集成至PyTorch生态。

#### （4）定制化异步MPI后端
- **问题**：原生PyTorch DDP使用OpenMP线程处理通信，与计算争抢核心资源。
- **解决方案**：
  - 实现自定义MPI后端，支持`MPI_Iallreduce`等异步集合通信；
  - 将通信任务绑定到专用CPU核心，实现计算与通信解耦。
- **效果**：显著提升通信-计算重叠效率，减少上下文切换开销。

---

## 2. 核心实验方法和设置

### 数据集
实验涵盖三个领域：
- **ImageNet**：通用图像生成基准；
- **Gaofen-2**：国产高分二号卫星遥感数据，4波段，空间分辨率为2m/pixel；
- **Sentinel-2**：欧洲哨兵二号卫星数据，13波段，10m/pixel。

用于验证模型的跨域迁移能力和科学应用潜力。

### 模型配置
测试不同规模的DiT模型：
- DiT-S/2, DiT-B/2, DiT-L/2, DiT-XL/2  
遵循原始DiT设定，使用AdamW优化器（学习率1e-4）。

### 硬件平台
基于 **LS Pilot System**，一个256节点的HPC CPU集群：
- 每节点配备两颗 **LX2 CPU**（>256核）；
- 支持MAU（8×8双精度矩阵运算）、VAU、OPM（片上高带宽内存）；
- 节点间通过LXLink互联（双向48 GB/s）。

软件栈：
- OS: OpenEuler 22.03
- Compiler: Clang 17.0.0
- MPI: OpenMPI
- 基线：NativeBLAS + oneDNN 3.6.2 + PyTorch 2.5.1

### 评估指标
| 指标 | 描述 |
|------|------|
| **FID (Fréchet Inception Distance)** | 衡量生成图像与真实图像分布之间的距离，越低越好 |
| **单步迭代时间 (Iteration Time)** | 反映训练吞吐性能 |
| **峰值内存消耗 (Peak Memory Usage)** | 评估内存效率 |
| **弱扩展效率 (Weak Scaling Efficiency)** | 固定每节点batch size，衡量随节点增加的性能扩展性 |
| **强扩展效率 (Strong Scaling Efficiency)** | 固定总batch size，评估加速比 |

### 基线对比方法
- **DP (Data Parallelism)**：标准数据并行，每个设备保存完整模型副本；
- **TP/PP + DP**：传统张量/流水线并行组合；
- **NativeBLAS / oneDNN / OpenBLAS**：主流CPU数学库作为性能基线；
- **NVIDIA H100 GPU**：用于准确性验证与性能对标。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 指标 | 结果 |
|------|------|
| **单节点加速比（vs 原生栈）** | 最高 **8.2×** 加速（DiT-S/2） |
| **vs OpenBLAS** | 最高达 **87.7×** 加速 |
| **弱扩展效率（256节点）** | **90.6%**（DiT-XL/2） |
| **持续算力（256节点）** | 超过 **1 PFLOPS (FP32)** |
| **最大全局batch size** | 达 **28,672**（远超GPU单机极限） |

### 与基线方法对比

#### 表1：不同并行策略下的性能与内存对比（部分）
| Batch Size | Model | Iteration Time (s) – DP+TP | CFTP | Peak Memory (GB) – DP | CFTP |
|------------|-------|----------------------------|--------|------------------------|--------|
| 112        | B/2   | 8.7                        | **3.97** | 247                    | **198** |
| 112        | XL/2  | OOM                        | **13.18** | OOM                    | **358** |

> ✅ CFTP在大模型下避免OOM，并大幅缩短迭代时间。

#### 表2：FID生成质量对比（越低越好）
| Dataset     | GPU (bs=112) | LX2 (bs=112) | LX2 (bs=28,672) |
|-------------|--------------|---------------|------------------|
| Gaofen-2    | 30.44        | 28.51         | **24.70**         |
| Sentinel-2  | 24.97        | 29.03         | **22.61**         |

> ✅ 在极大batch size下仍能继续收敛，生成质量优于GPU。

### 消融实验结果（图9）

逐步添加优化组件后的性能提升路径：
1. **Baseline (CFTP)** → 1×
2. **+ GEMM优化** → 5.5×
3. **+ 异步通信后端** → 6.3×
4. **+ AI算子优化** → 6.6×
5. **+ AutoMem预取** → 6.7×
6. **+ 多级调优（NUMA感知、缓冲等）** → **8.2×**

表明各层次优化具有叠加效应。

### GEMM模块级加速（表4）
| Operator     | Speedup vs NativeBLAS |
|--------------|------------------------|
| `down_proj`  | **35.49×**             |
| `qkv_proj`   | 30.10×                 |
| `up_proj`    | 16.49×                 |
| `o_proj`     | 13.89×                 |

显示针对MAU和内存层级的GEMM重构带来巨大收益。

### 与GPU系统的对比（5.6节）
- 在相近系统级算力条件下：
  - H100双服务器（16卡）：**7.6秒/step**
  - LX2 256节点CPU集群：**13.5秒/step**
- 尽管绝对速度落后于GPU，但在**弱扩展性和内存容量**方面具备优势，尤其适合超大规模batch训练。

---

## 4. 关键结论和发现

### 主要发现
1. **CPU集群可以高效训练现代生成模型**：通过软硬件协同设计，DiT-HC证明了在HPC CPU集群上训练DiT类Transformer生成模型的可行性。
2. **新型硬件特性可被有效挖掘**：MAU、OPM、SDMA等特性若配合专用调度与算子优化，能显著提升训练效率。
3. **CFTP+AutoMem是解决CPU内存瓶颈的关键**：通过消除通信、智能管理混合内存，实现了接近线性的弱扩展效率（90.6% @ 256节点）。
4. **大batch训练更具优势**：CPU平台支持极大batch size（>28k），有助于提升生成质量和训练稳定性。

### 方法的局限性
- **绝对性能仍低于高端GPU**：受限于整体内存带宽和互连速率，单步执行时间仍慢于H100。
- **定制化程度较高**：目前优化紧密依赖LX2架构特性，移植到其他CPU平台需重新适配。
- **尚未支持稀疏计算或量化训练**：未探索更前沿的压缩技术。

### 未来工作方向
- 扩展至更多生成模型类别（如Video Diffusion、LLMs）；
- 探索与气候模拟、地球系统模型的深度耦合；
- 构建统一的HPC-AI共设计范式，推动“仿真+AI”一体化工作流；
- 开源HCOps与DiT-HC框架，促进社区共建。

---

> 🔚 **总结一句话**：  
> DiT-HC首次实现了在HPC CPU集群上高效可扩展地训练DiT生成模型，通过CFTP、AutoMem、HCOps和异步MPI四大技术创新，达成8.2×加速与90.6%弱扩展效率，为AI与科学计算的融合提供了新路径。

</details>

---

### 3. [Accelerating Decentralized Optimization via Overlapping Local Steps](https://arxiv.org/abs/2601.01493)

**Authors**: Yijie Zhou, Shi Pu  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.01493v1  

#### Abstract
Decentralized optimization has emerged as a critical paradigm for distributed learning, enabling scalable training while preserving data privacy through peer-to-peer collaboration. However, existing methods often suffer from communication bottlenecks due to frequent synchronization between nodes. We...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerating Decentralized Optimization via Overlapping Local Steps

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在分布式机器学习中，**通信瓶颈**是制约训练效率的关键因素。传统的集中式方法依赖参数服务器进行同步，容易形成单点故障和通信瓶颈；而去中心化优化（Decentralized Optimization, DO）虽避免了中心节点，但仍需频繁的邻居间通信，导致大量网络空闲时间。

此外，尽管已有方法如 **Local DSGD (LDSGD)** 通过增加本地更新步数（local steps）减少通信频率，但计算与通信过程仍是串行执行，无法有效掩盖通信延迟。更进一步，一些尝试重叠通信与计算的方法（如 Overlap-Local-SGD）因引入过时梯度（stale gradients）而导致收敛性能下降，尤其在非凸目标下表现不稳定。

---

### **提出了什么新方法或新思路**

本文提出了一种名为 **Overlapping Local Decentralized SGD (OLDSGD)** 的新型去中心化训练算法，其核心思想是：

- **将通信与计算完全重叠（computation-communication overlapping）**，从而最大限度地减少等待时间。
- 采用 **Combine-Then-Adapt (CTA)** 更新机制，在通信过程中使用**过时模型（stale models）** 进行混合（mixing），同时保留最新的本地梯度进行更新。
- 设计更新规则使得所有节点的**平均模型更新路径等价于标准 SGD**，从而避免因梯度陈旧导致的方向偏差。

该方法实现了以下技术突破：
- 首个将 **本地更新（local steps）** 与 **通信-计算重叠** 结合的去中心化算法；
- 在不牺牲理论收敛性的前提下，显著提升每轮迭代的实际运行速度（wall-clock time）；
- 不需要额外的补偿机制（如梯度裁剪、误差校正等）来缓解陈旧性带来的偏差。

---

### **相比现有方法的优势**

| 方面 | OLDSGD 的优势 |
|------|----------------|
| **通信效率** | 完全掩盖通信延迟（只要通信不超过计算时间的 T 倍），实现接近零等待 |
| **理论保证** | 收敛速率与 Local DSGD 相同（$ \mathcal{O}(1/\sqrt{T}) $），无退化 |
| **实现简单** | 只需对现有框架做轻微修改即可部署，无需复杂调度或内存扩展 |
| **适用性广** | 支持任意网络拓扑结构（ring, mesh 等），适用于边缘计算、联邦学习等场景 |
| **实际加速明显** | 实验显示在高通信延迟环境下可获得高达 **3.32×** 的端到端加速 |

---

## 2. 核心实验方法和设置

### **使用的数据集与任务**

| 任务 | 数据集 | 模型 | 节点数 |
|------|--------|-------|--------|
| 图像分类 | CIFAR-10 | VGG11, ResNet18 | 9 |
| 语言模型微调 | WikiText-2 | GPT2-Small | 8 |

实验涵盖：
- **同构数据分布（Homogeneous）**：数据均匀划分；
- **异构数据分布（Heterogeneous）**：按类别偏斜分配（70% 类专属 + 30% 均匀采样）；

---

### **实验设置和评估指标**

#### **拓扑结构**
- 默认为 **undirected ring topology**（环形拓扑），模拟真实边缘设备连接；
- 扩展测试了从 2 到 32 个 agent 的可扩展性。

#### **通信建模**
- 每次共识操作耗时 `c` 单位时间；
- 每次本地梯度计算耗时 1 单位时间；
- 测试两种延迟水平：`c = 1` 和 `c = 5`（后者代表严重通信瓶颈）；

#### **评估指标**
- **Wall-clock time 收敛曲线**：损失函数随模拟时间的变化；
- **达到目标精度所需的时间加速比（speedup）**；
- **可扩展性分析**：不同 agent 数量下的收敛加速情况；
- **迭代级收敛性**：验证理论预测是否成立。

#### **超参数设置**
- 学习率统一设为 `α = 0.01`；
- 局部批大小：VGG/ResNet 使用 8，GPT2 使用 2；
- 对 GPT2 使用梯度裁剪（norm ≤ 1）以稳定训练；
- 每种算法遍历多个 `T ∈ {1,3,5,...,40}` 并选择最优值用于比较。

---

### **基线方法对比**

| 方法 | 简称 | 特点 |
|------|------|------|
| Local DSGD | LDSGD | 经典去中心化局部更新方法 |
| KGT | KGT | 基于 Gradient Tracking 的局部版本 |
| LUGT | LUGT | 另一种 Gradient Tracking 扩展 |
| LED | LED | 基于 Exact Diffusion 的局部方法 |
| Local SGD + Ring AllReduce | LSGD | 中心化风格的高效通信协议作为强基线 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **表 3：OLDSGD 相对于其他方法的速度提升（几何平均）**

| 方法 | VGG11 (c=1) | VGG11 (c=5) | ResNet18 (c=1) | ResNet18 (c=5) | GPT2 (c=1) | GPT2 (c=5) | **GeoMean** |
|------|-------------|-------------|----------------|----------------|------------|------------|--------------|
| LDSGD | 1.23× | 1.26× | 1.50× | 1.62× | 1.95× | 2.65× | **1.64×** |
| KGT | 1.26× | 1.30× | 1.42× | 1.73× | 1.86× | 2.54× | **1.63×** |
| LSGD | 1.34× | 1.64× | 1.84× | 2.18× | 2.08× | 3.32× | **1.98×** |
| LUGT | 2.68× | 3.53× | 1.98× | 4.62× | — | — | **3.05×** |

> 注：速度提升定义为“基线所需时间 / OLDSGD 所需时间”，越高越好。

#### 🔍 观察要点：
- 在通信延迟大（`c=5`）时，OLDSGD 加速效果更显著；
- 在 GPT2 微调任务上，OLDSGD 达到 **3.32×** 加速，远超 LSGD；
- 即使面对强基线 LSGD（ring-allreduce），OLDSGD 仍保持优势，说明其在去中心化架构中的竞争力。

---

### **与基线方法的对比结果**

- **在所有任务和设置下，OLDSGD 均优于或持平于 LDSGD**，且随着 `T` 增加优势扩大；
- **Gradient Tracking 类方法（KGT, LUGT）表现不佳**：
  - KGT 在异构数据下收敛变慢；
  - LUGT 在 `T > 1` 时常发散；
- **LED 几乎在所有配置中都发散**，即使使用小 `T`；
- **OLDSGD 的平均模型收敛行为与 LDSGD 几乎一致**，验证了其理论等效性；
- **在 GPT2 上，OLDSGD 显著降低测试困惑度（perplexity）**，表明其泛化能力更强。

---

### **消融实验与补充分析（见附录 D）**

- **迭代级收敛验证**：OLDSGD 与 LDSGD 具有几乎相同的损失下降轨迹，支持理论分析；
- **可扩展性测试**（图 4）：
  - 在 VGG11 和 ResNet18 上，OLDSGD 实现近似线性加速；
  - 最多支持 16 个节点保持高效，超过后受限于 ring 拓扑的信息传播速度；
- **异构数据下的性能增强**（表 4）：
  - 在异构设置下，OLDSGD 对 LDSGD 的平均加速比达 **1.64×**，高于同构下的 **1.39×**；
  - 表明 OLDSGD 更鲁棒于数据分布差异。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **通信-计算重叠可在去中心化训练中安全且高效地实现**，OLDSGD 是首个成功结合 local steps 与 overlapping 的去中心化算法；
2. ✅ **CTA 更新机制天然消除平均更新偏差**，无需额外修正项即可保持 SGD-like 动态；
3. ✅ **理论收敛速率未退化**：在非凸光滑目标下，OLDSGD 与 Local DSGD 具有相同复杂度；
4. ✅ **实际运行时间大幅缩短**：在典型通信延迟下（`c=5`），可实现 **2–3× 以上端到端加速**；
5. ✅ **特别适合现代 Transformer 架构**：在 GPT2 微调中表现卓越，凸显其在大模型训练中的潜力；
6. ✅ **对数据异构性具有更强鲁棒性**，更适合隐私敏感的真实应用场景。

---

### **方法的局限性**

- **依赖同步假设**：当前分析基于同步系统，若各节点计算/通信速度差异过大，可能仍存在阻塞；
- **仅适用于固定拓扑**：动态网络或频繁断连场景尚未考虑；
- **未探索异步版本**：虽然设计上允许重叠，但完全异步更新可能导致稳定性问题；
- **Gradient Tracking 类方法难以直接适配**：尝试构建 OLGT 和 OLED 导致性能严重退化（见附录 B），说明并非所有算法都能简单叠加 overlapping。

---

### **未来工作方向**

1. **扩展至异步去中心化训练框架**，适应更真实的边缘环境；
2. **研究自适应 local steps 调度策略**，根据通信/计算负载动态调整 `T`；
3. **探索与其他加速技术的融合**，如量化通信、稀疏更新等；
4. **应用于更大规模语言模型和推荐系统**，验证工业级实用性；
5. **开发稳定的 overlapping 版本 Gradient Tracking 或 Exact Diffusion**，克服当前方法的不兼容性。

---

## 总结

**OLDSGD** 是一个简洁、高效、理论健全的去中心化训练新范式。它通过巧妙利用 **CTA 结构** 和 **stale model mixing**，首次实现了真正的通信-计算零等待重叠，同时保持了与经典 Local DSGD 相同的收敛性质。实验证明其在多种视觉与语言任务中均能带来显著的 **wall-clock time 加速**，特别是在通信受限的现实环境中表现出巨大潜力，是一个极具实用价值的“即插即用”解决方案。

</details>

---

### 4. [Quantized SO(3)-Equivariant Graph Neural Networks for Efficient Molecular Property Prediction](https://arxiv.org/abs/2601.02213)

**Authors**: Haoyu Zhou, Ping Xue, Tianfan Fu, Hao Zhang  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.02213v1  

#### Abstract
Deploying 3D graph neural networks (GNNs) that are equivariant to 3D rotations (the group SO(3)) on edge devices is challenging due to their high computational cost. This paper addresses the problem by compressing and accelerating an SO(3)-equivariant GNN using low-bit quantization techniques. Speci...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Quantized SO(3)-Equivariant Graph Neural Networks for Efficient Molecular Property Prediction*

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
该论文针对 **SO(3)-equivariant GNNs**（如 So3krates、NequIP）在边缘设备上部署困难的问题。这类模型虽然在分子性质预测任务中表现出色，但由于其复杂的张量运算和注意力机制，计算开销大、内存占用高，难以在资源受限的设备（如手机、芯片实验室传感器）上实时运行。

此外，直接对这些对称性敏感的模型进行低比特量化（low-bit quantization）会导致：
- 向量特征的方向和模长失真；
- 注意力机制不稳定；
- 模型失去 SO(3) 等变性（equivariance），从而破坏物理一致性并显著降低精度。

### **提出了什么新方法或新思路**
作者提出了一套专为 SO(3)-equivariant GNNs 设计的 **等变性感知量化框架**，包含三项核心创新：

#### （1）**Magnitude-Direction Decoupled Quantization (MDDQ)**  
将等变向量特征分解为**模长（magnitude）** 和 **方向（direction）** 两个部分分别量化：
- 模长 $ r = \| \mathbf{h} \| $ 使用标准标量量化器 $ Q_r(\cdot) $；
- 方向 $ \hat{\mathbf{h}} = \mathbf{h}/\|\mathbf{h}\| $ 被归一化后逐分量量化，并重新归一化以保持单位长度；
- 最终重建向量：  
  $$
  Q_{\text{vec}}(\mathbf{h}) = Q_r(r) \cdot \frac{Q_a(\hat{\mathbf{h}})}{\|Q_a(\hat{\mathbf{h}})\|}
  $$

> ✅ **优势**：保留了旋转下的几何方向信息，避免传统逐元素量化导致的小向量坍缩或方向漂移。

#### （2）**Branch-Separated Quantization-Aware Training (QAT)**  
由于 equivariant GNN 中存在两种不同类型的分支：
- **Invariant branch**（$ l=0 $，标量）：分布对称，适合对称量化；
- **Equivariant branch**（$ l=1 $，向量）：模长非负、方向均匀分布在球面上；

因此采用**差异化处理策略**：
- 分别设置独立的 fake quantization observer；
- 使用分阶段训练：先只量化标量分支（warm-up），再启用全量化；
- 在向量分支加入 LEE 正则项以增强等变性。

> ✅ **优势**：更符合各分支的数据分布特性，提升量化鲁棒性和最终精度。

#### （3）**Robust Attention Normalization**  
观察到注意力得分（query-key dot product）在低精度下极易受动态范围影响而产生噪声，导致 softmax 权重剧烈波动。

解决方案：引入 **L2-normalization** 到 query 和 key 向量：
$$
\mathbf{q}' = \frac{\mathbf{q}}{\|\mathbf{q}\|}, \quad \mathbf{k}' = \frac{\mathbf{k}}{\|\mathbf{k}\|}
$$
然后计算注意力分数 $ \alpha_{ij} = \text{Softmax}((\mathbf{q}' \cdot \mathbf{k}') / \sqrt{d}) $

> ✅ **优势**：
> - 将点积限制在 $[-1,1]$ 区间内；
> - 注意力仅依赖于方向相似性而非绝对大小；
> - 显著提升了 INT8 模型中注意力机制的稳定性。

---

### **相比现有方法的优势**
| 方法 | 是否支持 equivariance | 是否稳定 | 是否高效 |
|------|------------------------|----------|-----------|
| 通用量化方法（DoReFa, PACT, SmoothQuant） | ❌ 忽略向量结构 | ❌ 容易破坏方向 | ⚠️ 可加速但精度下降严重 |
| GNN专用量化（Degree-Quant） | ❌ 仅处理标量消息传递 | ❌ 不保护向量方向 | ⚠️ 改善有限 |
| 本文方法（MDDQ + Branch-QAT + L2-Norm） | ✅ 显式保护等变性 | ✅ 高稳定性 | ✅ 高效且准确 |

> 🎯 **综合优势**：首次实现**在不牺牲等变性和精度的前提下，将 SO(3)-equivariant GNN 成功压缩至 8-bit 甚至 4-bit**，适用于移动端部署。

---

## 2. **核心实验方法和设置**

### **使用的数据集**
- **QM9**：134k 小分子，用于训练和能量预测（formation energy），平衡态结构，无外力。
- **rMD17**：7 个分子（aspirin, ethanol, malonaldehyde 等）的非平衡态构型轨迹，提供能量和原子力（forces），更适合测试泛化能力和动力学稳定性。

> 注：训练主要在 QM9 上完成，在 rMD17 上测试跨分子泛化能力。

### **实验设置**
- **基础架构**：基于 So3krates 的 transformer 架构，使用 `e3nn` 库实现 E(3)/SO(3) 等变操作；
- **特征维度**：$ l=0 $ 和 $ l=1 $ 特征各 64 维，共 6 层全局自注意力；
- **输出目标**：分子形成能（标量）和原子力（3D 向量）；
- **损失函数**：加权 MAE（能量 + 力）+ LEE 正则项（仅作用于向量输出）；
- **量化配置**：
  - 所有线性层和激活函数量化为 **INT8**；
  - 第一层和最后一层保持 FP32；
  - 使用 PyTorch 的 fake quantization observer 进行 QAT；
  - 学习率 $1\times10^{-4}$，20 epoch QAT，前 5 epoch 只量化标量分支（warm-up）；
  - 注意力使用 L2-normalized queries/keys。

### **评估指标**
| 指标 | 描述 |
|------|------|
| **Energy MAE** | 单位：meV，越小越好 |
| **Force MAE** | 单位：meV/Å，越小越好 |
| **Local Equivariance Error (LEE)** | 测量模型输出是否满足 $ f(R \cdot G) = R \cdot f(G) $，单位 meV/Å，越小表示等变性越好 |
| **Inference Latency** | CPU 上单分子推理延迟（μs），衡量效率 |
| **Speedup** | 相对于 FP32 模型的推理速度提升倍数 |
| **Memory Reduction** | 量化后模型体积减少比例 |

### **基线方法对比**
| 基线 | 类型 | 说明 |
|------|------|------|
| **FP32 So3krates** | 全精度上限 | 性能参考基准 |
| **PTQ-NequIP (INT8)** | 后训练量化 | NequIP 模型转为 8-bit，未微调 |
| **So3krates + Degree-Quant (INT8)** | GNN专用QAT | 使用 Degree-Quant 对 So3krates 进行量化 |
| **TorchAO W4A8** | 更激进量化 | 4-bit权重 + 8-bit激活 |

---

## 3. **主要实验结果和性能指标**

### **关键性能数据（rMD17-Ethanol）**
| 方法 | Energy MAE (meV) | Force MAE (meV/Å) | LEE (meV/Å) | Speedup | Memory ↓ |
|------|------------------|--------------------|-------------|---------|-----------|
| FP32 So3krates | 2.25 | 4.16 | ~0 | 1.0× | 1× |
| **Ours (INT8)** | **1.74** | **4.21** | **~2** | **2.73×** | **~4×** |
| PTQ-NequIP | 15.7 | 35.3 | ~5 | 2.73× | ~4× |
| Degree-Quant | 11.3 | 28.0 | ~3 | 0.73× | ~4× |

> 🔍 **亮点**：
> - 我们的 INT8 模型不仅没有退化，反而在某些任务上**优于 FP32 基线**（可能因 QAT 起到正则化作用）；
> - 推理速度快 **2.37–2.73×**，内存减少约 **4×**；
> - LEE 仅为 ~2 meV/Å，远低于其他方法，表明等变性高度保留。

### **跨分子性能比较（Table II）**
在多个 rMD17 分子上，我们的 **W4A8（4-bit weight + 8-bit activation）** 模型表现优异：

| 分子 | 能量 MAE (meV) | 力 MAE (meV/Å) | 表现 |
|------|----------------|----------------|-------|
| Ethanol | **1.74** | **4.21** | 超越所有 FP32 模型 |
| Aspirin | **4.87** | 11.5 | 最佳能量预测 |
| Malondialdehyde | **2.57** | **6.53** | 全面领先 |

> ✅ 表明即使在极低比特下，也能达到甚至超过当前 SOTA FP32 模型的精度。

### **消融实验结果**
| 消融条件 | Energy MAE | Force MAE | LEE | 结论 |
|--------|------------|-----------|-----|------|
| 仅量化标量分支 | 8.7 | 21.0 | ~0 | 向量保留 FP32 时性能接近原模型 |
| 仅量化向量分支 | 9.5 | 24.0 | — | 向量量化影响更大 |
| 无 MDDQ（普通量化） | >12 | — | — | **方向丢失严重影响精度** |
| 无 attention normalization | ~12 | ↑↑ | — | 注意力不稳定导致训练失败或误差上升 |
| 无 LEE 正则化 | 9.1 / 22.5 | — | ~4 | **等变性明显恶化** |

> 🔍 **核心发现**：三个组件缺一不可，共同保障了低比特下的精度与对称性。

---

## 4. **关键结论和发现**

### **主要发现**
1. ✅ **SO(3)-equivariant GNN 可以安全地进行低比特量化**，前提是设计专门的等变性感知量化方案；
2. ✅ **MDDQ 是保持向量方向的关键**，解耦模长与方向可有效防止量化引起的几何失真；
3. ✅ **branch-separated QAT + staged training** 显著提高训练稳定性与最终精度；
4. ✅ **L2-normalized attention** 极大增强了低精度注意力机制的鲁棒性；
5. ✅ **QAT 本身可作为正则化手段**，在某些情况下（如 W4A8）反而提升性能；
6. ✅ 所提方法实现了 **2.37–2.73× 推理加速** 和 **~4× 内存缩减**，同时保持与 FP32 相当的精度和等变性。

### **方法的局限性**
- 当前主要验证于 $ l \leq 1 $ 的简单表示系统，尚未扩展到更高阶张量（$ l \geq 2 $）；
- 实验集中在小分子体系，对蛋白质、晶体等复杂系统的适用性有待验证；
- 目前依赖特定硬件支持（如 AVX2 或 INT8 加速器）才能发挥最大效率优势。

### **未来工作方向**
- 扩展至 **生物大分子和周期性材料**（crystalline materials）；
- 探索与其他对称群（如 SE(3), permutation group）结合的通用对称性保持量化框架；
- 与专用低比特硬件协同设计（co-design），进一步优化端侧推理性能；
- 研究更低比特（如 W2A4）下的可行性与极限。

---

> 💡 **总体评价**：这是**首篇系统研究 SO(3)-equivariant GNN 量化的论文**，提出的三要素框架（MDDQ + Branch-QAT + Robust Attention）为对称性神经网络的轻量化提供了范式级解决方案，推动了物理驱动 AI 模型在移动和嵌入式场景中的实际应用。

</details>

---

### 5. [Heterogeneous Low-Bandwidth Pre-Training of LLMs](https://arxiv.org/abs/2601.02360)

**Authors**: Yazan Obeidi, Amir Sarfi, Joel Lidin, Paul Janson, Eugene Belilovsky  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.02360v1  

#### Abstract
Pre-training large language models (LLMs) increasingly requires distributed compute, yet bandwidth constraints make it difficult to scale beyond well-provisioned datacenters-especially when model parallelism forces frequent, large inter-device communications. We study whether SparseLoCo, a low-commu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Heterogeneous Low-Bandwidth Pre-Training of LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）的预训练高度依赖分布式计算，但随着模型规模增长，跨设备通信开销（尤其是带宽需求）成为扩展瓶颈。特别是在资源异构、网络带宽受限（如通过互联网连接）的环境中，传统的数据并行（Data Parallelism）和模型并行（Model Parallelism）难以高效运行。现有方法在低带宽场景下往往牺牲性能或无法支持大规模模型。

本文旨在解决以下核心挑战：
- 如何在**低带宽环境下有效结合数据并行与模型并行**？
- 如何让**资源受限的参与者**（如小型集群或个人节点）也能参与LLM预训练？
- 如何在压缩通信的同时**最小化性能损失**？

---

### **提出了什么新方法或新思路**

作者提出了一种名为 **Heterogeneous SparseLoCo** 的新型分布式训练框架，其核心创新包括：

#### ✅ **1. 结合 SparseLoCo 与 Subspace Pipeline Compression**
- 将高效的低通信数据并行方法 **SparseLoCo**（基于稀疏伪梯度交换和低频同步）与低带宽模型并行技术 **Subspace Networks** 中的激活/梯度压缩相结合。
- 在 pipeline stage 之间使用子空间投影（subspace projection）压缩 `activations` 和 `activation-gradients`，显著降低跨阶段通信量。

#### ✅ **2. 异构训练架构（Heterogeneous Training Configuration）**
- 允许不同类型的参与者共存：
  - **高性能集群**（如 InfiniBand 连接）运行完整的未压缩模型副本（full replica），不进行压缩；
  - **资源受限节点** 组成一个逻辑上的“联合副本”（joint replica），采用 pipeline parallelism 并启用 subspace 压缩。
- 实现了**选择性压缩（selective compression）**：仅在带宽瓶颈处应用压缩，保留高带宽链路的全精度通信。

#### ✅ **3. 针对异构环境的关键适配机制**
- **Token Embedding 修正机制**：为防止压缩与非压缩副本在同步时导致嵌入表漂出子空间，引入投影残差累积策略，确保所有副本共享一致的 token embedding。
- 分析了压缩带来的系统偏差（compression bias），指出在 SparseLoCo 的长周期本地优化中，未压缩副本可作为“锚点”校正偏差，从而提升整体收敛质量。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **通信效率** | 显著减少跨节点通信量，尤其适用于互联网级低带宽连接（如 100 Mbps–1 Gbps）。 |
| **资源利用率** | 可整合原本因内存或带宽不足而无法独立训练大模型的小型节点。 |
| **性能保持** | 相较于统一压缩（uniform compression），异构配置在相同压缩比下损失更低。 |
| **灵活性与可扩展性** | 支持混合硬件部署，适合去中心化或多组织协作训练场景。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **DCLM**：大规模预训练语料库，用于主实验。
- **C4**：通用文本数据集，用于验证泛化能力。

---

### **实验设置**

| 参数 | 设置 |
|------|------|
| **模型架构** | Decoder-only Transformers（LLaMA 风格），SwiGLU 激活函数 |
| **模型规模** | 178M、512M、1B 参数 |
| **Pipeline Stages** | 4 stages |
| **Replicas 数量 (M)** | 8（178M/512M）、2（1B） |
| **Local Steps (H)** | 50（SparseLoCo 默认） |
| **Chunk Size** | 64×64（4096 元素） |
| **TOP-k Sparsity** | 每 chunk 保留 32 个最大值 → ~0.78% 密度 |
| **Optimizer** | Inner: AdamW；Outer: SGD（无动量） |
| **Error Feedback Momentum β** | 0.95 |
| **Batch Size** | 局部 batch 32，全局等效 524,288 tokens / inner step |
| **Training Budget** | Chinchilla-optimal（10B tokens 主实验，部分扩展至 12B） |

---

### **评估指标**

- **Validation Loss**（主要指标）
- **Perplexity**
- **相对性能退化率 Δ(%)**：$\frac{L_{\text{compressed}} - L_{\text{baseline}}}{L_{\text{baseline}}} \times 100\%$
- **Compute Utilization vs Bandwidth**（模拟墙钟时间影响）
- **消融实验**：分析各组件（如 embedding adaptation、weight projection）的影响

---

### **基线方法对比**

| 方法 | 描述 |
|------|------|
| **SparseLoCo (Baseline)** | 不使用任何 activation compression，全精度通信 |
| **SparseLoCo + PP-Compress** | 所有 replica 均使用 subspace 压缩（uniform） |
| **SparseLoCo + Het-PP-Compress (1/2)** | 一半 replica 使用压缩，另一半不压缩（heterogeneous） |
| **AdamW + PP-Compress / Heterogeneous** | 用于对比标准数据并行是否也受益于异构压缩 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1 & 2）**

#### 🔹 **512M 模型在 DCLM 上的结果（k/d = 1/8, 压缩率 87.5%）**

| 配置 | Loss | Perplexity | Δ(%) |
|-----------------------------|--------|------------|-------|
| SparseLoCo (Baseline)       | 2.73   | 15.40      | —     |
| PP-Compress (Uniform)       | 2.84   | 17.10      | +3.8% |
| Het-PP-Compress (1/2)       | 2.82   | 16.90      | +3.3% |
| + 超参调优                  | **2.80** | **16.50**  | **+2.6%** |

> ✅ 异构压缩比统一压缩平均降低约 **0.5pp 的 loss 增加**。

#### 🔹 **更高压缩比下的表现（512M, k/d 下降）**

| k/d | 压缩率 | Uniform Loss (Δ%) | Heterogeneous Loss (Δ%) | 收益 |
|-----|--------|--------------------|----------------------------|------|
| 1   | 0%     | 2.73               | —                          | —    |
| 1/8 | 87.5%  | 2.84 (+3.8%)       | 2.82 (+3.3%)               | +0.5pp |
| 1/96| 99.0%  | 2.96 (+8.1%)       | 2.94 (+7.4%)               | +0.7pp |
| 1/384| 99.7% | 3.04 (+11.3%)      | 2.99 (+9.4%)               | +1.9pp |
| 1/768| 99.9% | 3.07 (+12.4%)      | **3.00 (+9.8%)**           | **+2.6pp** |

> 📌 **结论：压缩越激进，异构方案优势越明显！**

---

### **扩展训练预算实验（Table 3a）**

| 配置 | Token Budget | Final Loss |
|----------------------------------|---------------|-------------|
| SparseLoCo (Baseline)            | 10B           | 2.73        |
| SparseLoCo + PP-Compress         | 12B (+20%)    | 2.78        |
| SparseLoCo + Het-PP-Compress(1/2)| 12B (+20%)    | **2.75**    |

> 💡 即使压缩带来初始性能下降，**增加 20% 训练 tokens 后，异构方案接近甚至逼近 baseline 性能**，且由于通信减少，实际 wall-clock time 更短。

---

### **消融实验结果（Table 4）**

| 组件 | 是否启用 | 对 Loss 影响 | 结论 |
|------|----------|--------------|------|
| **Token Embedding Adaptation** | 是 | 2.82 vs 2.89（↓0.07） | ✅ 至关重要，显著改善优化 |
| **Weight Projection** | 是 | 2.88 vs 2.84（↑0.04） | ❌ 有害，应移除 |
| **Grassmann Subspace Update** | 是 | 无变化 | ❌ 无需动态更新子空间 |
| **Modified AdamW** | 是 | 无变化 | ❌ 原始 AdamW 足够 |

> ⚠️ 说明：在 SparseLoCo 框架下，许多 Subspace Networks 的复杂设计反而无效或负面。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **SparseLoCo 可有效与 pipeline compression 结合**  
   在 modest 压缩（如 87.5%）下，性能退化可控（<4%），且可通过增加训练步数补偿。

2. ✅ **异构压缩优于统一压缩**  
   在相同压缩比下，混合使用压缩与非压缩 replica 能显著缓解性能下降，尤其是在高压缩比（>99%）时效果更突出。

3. ✅ **异构优势源于偏差校正机制**  
   未压缩 replica 提供无偏梯度估计，在 SparseLoCo 的 H-step 本地优化中起到“锚定”作用，抑制压缩引入的系统偏差积累。

4. ✅ **该优势是 SparseLoCo 特有的**  
   在标准 AdamW（H=1）训练中，频繁同步阻止了偏差积累，因此异构策略不仅无效，反而略差（见 Table 3）。

5. ✅ **方法具有良好的泛化性**  
   在 C4 数据集和 1B 参数模型上均验证了有效性（Table 6 & 7）。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **依赖 careful initialization & synchronization** | Token embedding 的 subspace 约束需要特殊处理，否则会破坏一致性。 |
| **极端压缩仍会导致显著性能下降** | 当 k/d < 1/100 时，即使延长训练也无法完全恢复 baseline 性能。 |
| **目前为 simulation-based study** | 实验基于等价步数模拟，并未在真实低带宽网络上部署，实际延迟和丢包未考虑。 |
| **仅支持静态子空间** | 动态调整 subspace（如 Grassmann 优化）未显示出收益，可能限制长期适应性。 |

---

### **未来工作方向**

1. **真实世界部署测试**：在跨数据中心或 P2P 网络中实现该框架，测量真实通信-计算权衡。
2. **自适应压缩策略**：根据实时带宽自动调节压缩比（k/d）。
3. **与其他并行范式结合**：如 Tensor Parallelism 或 ZeRO，进一步提升资源利用率。
4. **支持更多异构维度**：如混合精度、不同计算能力的 GPU 类型调度。
5. **理论分析压缩偏差传播机制**：建立更精确的误差建模范式。

---

## 总结

> **Heterogeneous SparseLoCo 为 LLM 预训练提供了一条通往“去中心化 + 低带宽 + 高效”的实用路径**。它通过将 **SparseLoCo 的低频稀疏通信** 与 **subspace-based pipeline compression** 相结合，并引入 **异构副本设计**，实现了在资源多样环境下高效训练大模型的可能性。实验证明，这种选择性压缩策略不仅能显著降低通信压力，还能在高压缩比下优于传统统一压缩方案，尤其适合未来开放协作式的 AI 训练生态。

</details>

---

### 6. [Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling](https://arxiv.org/abs/2601.02346)

**Authors**: Falcon LLM Team, Iheb Chaabane, Puneesh Khanna, Suhail Mohmad, Slim Frikha, Shi Hu, Abdalgader Abubaker, Reda Alami, Mikhail Lubinets, Mohamed El Amine Seddik, Hakim Hacid  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02346v1  

#### Abstract
This work introduces Falcon-H1R, a 7B-parameter reasoning-optimized model that establishes the feasibility of achieving competitive reasoning performance with small language models (SLMs). Falcon-H1R stands out for its parameter efficiency, consistently matching or outperforming SOTA reasoning model...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在复杂推理任务上的性能提升依赖于**训练时扩展（scaling training）** 和 **推理时扩展（scaling inference）**，但两者均面临瓶颈：
- **训练时扩展**受限于高昂的计算成本和高质量人类数据的稀缺；
- **推理时扩展（Test-Time Scaling, TTS）** 虽能通过生成多条推理链（如self-consistency）提升准确性，但带来显著的推理开销。

因此，如何在**不增加模型规模的前提下**，实现高效且准确的推理能力成为关键挑战。

### 提出的新方法与创新点
Falcon-H1R 是一个 **7B 参数的推理优化小模型（SLM）**，其核心创新在于结合了**架构设计、训练策略和测试时扩展技术**，实现了“小模型大能力”的突破：

#### （1）Hybrid Architecture for Efficient Reasoning
- 基于 **Falcon-H1 架构**（混合 Transformer-Mamba 架构），具备以下优势：
  - 更快的推理速度（fast inference）
  - 更低的内存占用（low memory usage）
  - 支持长序列（up to 256K context length）
- 特别适合需要高吞吐量、大批量并行生成的 **test-time scaling 场景**。

#### （2）Robust Training Strategy
采用两阶段精细化训练流程：
- **Cold-start SFT**：基于高质量、长链推理数据进行监督微调，强调困难样本，并引入**难度感知加权（difficulty-aware weighting）**。
- **Reinforcement Learning with Verifiable Rewards (RLVR)**：使用 GRPO 算法进行强化学习，进一步提升推理能力和输出质量。
  - 引入动态数据过滤机制，排除过于简单或完全无法解决的问题。
  - 使用多领域奖励函数（math/code/science）确保对齐目标。

#### （3）Superior Efficiency via Test-Time Scaling (TTS)
- 集成最新的 **DeepConf@512** 方法（Fu et al., 2025b），一种基于置信度的动态剪枝 TTS 技术：
  - 在生成过程中实时评估每条推理链的置信度。
  - 对低置信路径进行早期终止（early stopping），减少冗余 token 生成。
- 结果显示 Falcon-H1R 在相同计算预算下可运行更多有效推理链，显著提升性价比。

### 相比现有方法的优势
| 维度 | Falcon-H1R-7B | 其他主流推理模型（如 Qwen3-32B, Phi-4-R-Plus-14B） |
|------|----------------|--------------------------------------------------|
| 参数量 | 7B | 14B–32B+ |
| 推理效率 | 高（Hybrid 架构 + vLLM 优化） | 较低（纯 Transformer） |
| 准确性 | 匹配甚至超越更大模型 | 高但依赖更大参数 |
| Token 效率 | 显著更优（DeepConf 下减少 ~38% token） | 较高消耗 |
| 成本效益 | 极高（compact model + efficient TTS） | 成本较高 |

> ✅ **核心优势总结**：Falcon-H1R 展示了**小型化模型通过精心的数据、训练和架构设计，可以在推理任务上达到甚至超越大模型的表现，同时大幅降低推理成本**。

---

## 2. 核心实验方法和设置

### 使用的数据集
分为两个训练阶段：

#### SFT 阶段
涵盖四大领域，总计约 3.1M 条样本：
- **数学**：MATH、AMC/AIME/HMMT 等竞赛题，含完整 Chain-of-Thought 推理过程。
- **编程**：LeetCode、AtCoder、Codeforces 上的问题，强调算法逻辑与功能正确性。
- **科学**：物理、化学等多学科问题，答案经验证。
- **其他**：指令遵循、工具调用、安全对话等通用任务。

#### RL 阶段
独立于 SFT 数据，防止记忆化，聚焦高价值难题：
- 数学：AIME、HMMT 等精选难题。
- 编程：LiveCodeBench 中需执行验证的任务。
- 科学：GPQA-Diamond 类研究生级别问题。

所有数据经过严格清洗与去重，确保无基准污染。

### 实验设置与评估指标

#### 评估基准（Benchmarks）
| 类别 | 基准 | 描述 |
|------|-------|------|
| 数学 | AIME24/25, HMMT25, AMO-Bench, MATH500 | 奥林匹克级数学推理 |
| 编程 | LiveCodeBench v6, SciCode, T2-Telecom, TB Hard | 真实编程场景与代理任务 |
| 一般推理 | GPQA-Diamond, MMLU-Pro, Humanity's Last Exam (HLE), IFBench | 多跳科学推理、知识密集型问答、指令遵循 |

#### 评估方式
- 所有结果报告为 **pass@1** 或 **voted accuracy**（用于 TTS）。
- 生成参数统一设置：`temperature=0.6`, `top_p=0.95`。
- 多数任务生成多个响应（如 AIME 使用 16 个 rollout）后聚合。

#### 测试时扩展（TTS）设置
- 方法：**DeepConf@512**（Fu et al., 2025a）
- 总 trace 数：512
- 初始 warm-up：16 条 trace 用于确定置信阈值
- 后续 496 条启用 early stopping（当滑动窗口内最低置信低于阈值时停止）
- 聚合策略：多数投票、置信加权等（发现差异不大）

#### 基线对比模型
- **7B 级别**：Qwen3-8B, DeepSeek-R1-0528-Qwen3-8B
- **中等尺寸**：Phi-4-Reasoning-Plus-14B, Apriel-1.5-15b-Thinker, GPT-OSS-20B
- **大模型**：Qwen3-32B, Nemotron-H-47B-Reasoning

---

## 3. 主要实验结果和性能指标

### 标准推理任务表现（Tables 4–6）

#### 数学推理（Math Reasoning）
| 模型 | AIME24 | AIME25 | HMMT25 | AMO-Bench | MATH500 |
|------|--------|--------|--------|-----------|---------|
| **Falcon-H1R-7B** | **88.1%** | **83.1%** | **64.9%** | **36.3%** | **97.4%** |
| Qwen3-32B | 79.4% | 71.0% | 49.8% | 21.3% | 96.8% |
| GPT-OSS-20B | 83.3% | 84.4% | 64.8% | 26.0% | 94.8% |

> 🔍 **亮点**：在 AMO-Bench 上领先第二名 **超 10 个百分点**，表明其处理极端难题的能力极强。

#### 编程生成（Code Generation）
| 模型 | LCB v6 | SciCode (sub/main) | T2-Telecom | TB Hard |
|------|--------|--------------------|------------|---------|
| **Falcon-H1R-7B** | **68.6%** | 28.3 / 3.9 | 25.4 | 4.9 |
| GPT-OSS-20B | 72.0% | 34.9 / 6.2 | 60.2* | 9.9* |

> 📌 虽未第一，但在 7B 尺寸中表现最佳，接近 20B 级别模型。

#### 一般推理（General Reasoning）
| 模型 | GPQA-D | MMLU-Pro | HLE | IFBench |
|------|--------|----------|-----|--------|
| **Falcon-H1R-7B** | 61.3% | 72.1% | **11.1** | **53.4** |
| Phi-4-R-Plus-14B | 67.9% | 79.2% | 5.9 | 51.7 |

> ⚠️ 在知识密集型任务（如 GPQA、MMLU-Pro）略逊于更大模型，但整体仍具竞争力。

---

### Test-Time Scaling 实验结果（Table 7）

使用 **DeepConf@512** 方法，在相同计算预算下比较准确率与 token 消耗：

| 模型 | AIME25 Acc↑ | AIME25 Tok↓ | AMO-Bench Acc↑ | AMO-Bench Tok↓ |
|------|-------------|--------------|----------------|----------------|
| Qwen3-32B | 86.7% | 174.8M | 28.2% | 364.8M |
| **Falcon-H1R-7B** | **96.7%** | **95.1M** | **35.9%** | **216.8M** |

> 💡 **关键发现**：
> - 在 AIME25 上达到 **96.7% 准确率**，远超其他模型。
> - 相比 DeepSeek-R1-0528-Qwen3-8B，**token 使用减少 38%**。
> - 表明其 base model 推理能力强，且 confidence calibration 准确，支持激进 early stopping。

---

### 消融实验结果（Ablation Studies）

#### SFT 阶段关键发现
| 因素 | 最佳选择 | 影响 |
|------|----------|------|
| 学习率 | 1024×10⁻⁶ | 更高 LR 加速收敛且提升下游性能 |
| Rollout 数量 | n=12 | 更多样本提升困难问题表现 |
| 教师模型混合 | 单一教师优于多教师 | 多风格导致分布偏移，损害泛化 |
| 数据权重 | 难题加权（1.25–1.75×） | 显著提升 hard problem 性能 |
| 数据混合 | 数学主导 + 适量代码/科学 | 数学技能迁移效果最好 |

#### RL 阶段关键发现
| 设置 | 最佳配置 | 发现 |
|------|----------|------|
| Group Size (G) | 16 | 平衡多样性与效率 |
| Max Length | 48K tokens | 允许充分“思考”提升性能 |
| 温度（T） | 0.85 | 控制探索与稳定性的平衡 |
| 训练顺序 | Math Only → Code | 顺序 Curriculum 提供边际增益 |
| KL Penalty | β=0 | 允许自由探索旧策略之外的行为 |

> ✅ 所有 ablation 均证明：**针对性的设计选择对最终性能至关重要**。

---

## 4. 关键结论和发现

### 主要结论
1. **小模型也能实现强大推理能力**  
   Falcon-H1R-7B 在多项推理基准上**匹配或超越 2–7 倍参数的大模型**，打破了“越大越好”的惯性思维。

2. **数据质量和训练策略比单纯扩参更重要**  
   通过精细的数据筛选、难度感知加权、冷启动 SFT 和 RLVR 优化，可在有限参数下最大化推理潜力。

3. **Hybrid 架构是高效推理的理想载体**  
   Falcon-H1 的 **Transformer-Mamba 混合结构**在长序列、高 batch 场景下展现出卓越的推理效率，特别适配 TTS。

4. **TTS 效率取决于 base model 能力与 confidence quality**  
   Falcon-H1R 不仅准确，而且其内部 confidence 估计可靠，使得 DeepConf 可以安全地剪枝弱路径，极大节省资源。

5. **数学推理具有强迁移性**  
   实验表明，数学领域的训练数据对编码和科学任务有正向迁移作用，而反之则较弱。

---

### 方法的局限性
- **知识密集型任务仍有差距**：在 MMLU-Pro 和 GPQA-D 上表现良好但非最优，说明其训练侧重推理而非广博知识。
- **依赖高质量标注数据**：SFT 和 RL 阶段都需要大量人工或规则验证的答案，限制了可扩展性。
- **未公开 RL 数据细节**：虽然声明无污染，但具体构建方式未完全披露，影响复现性。

---

### 未来工作方向
1. **探索更小尺寸的推理模型**：尝试 3B 或 1.5B 规模下的可行性。
2. **自动化数据合成 pipeline**：减少对人工标注的依赖，构建闭环自我进化系统。
3. **改进 confidence estimation 机制**：使 early stopping 更精准，进一步压缩成本。
4. **跨模态推理扩展**：将该框架应用于视觉-语言或多模态推理任务。
5. **部署优化研究**：研究如何在边缘设备或低延迟服务中部署此类高效推理模型。

---

## 总结
Falcon-H1R 成功展示了：**通过 hybrid architecture + 高质量数据 + 分阶段强化训练 + 高效 TTS 方法，7B 级别的小模型可以实现媲美甚至超越数十亿参数模型的推理性能，同时显著降低推理成本**。这为构建**高效、可扩展、低成本的高级推理系统**提供了重要范式，推动了 SLM 在复杂认知任务中的应用边界。

</details>

---

### 7. [KV-Embedding: Training-free Text Embedding via Internal KV Re-routing in Decoder-only LLMs](https://arxiv.org/abs/2601.01046)

**Authors**: Yixuan Tang, Yi Yang  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.01046v1  

#### Abstract
While LLMs are powerful embedding backbones, their application in training-free settings faces two structural challenges: causal attention restricts early tokens from accessing subsequent context, and the next-token prediction objective biases representations toward generation rather than semantic c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# KV-Embedding: Training-free Text Embedding via Internal KV Re-routing in Decoder-only LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**decoder-only LLMs在训练自由（training-free）文本嵌入任务中的两个结构性缺陷**：
1. **因果注意力（causal attention）限制**：早期token无法访问后续上下文，导致信息不对称（information asymmetry），例如“the bank of the river”中，“bank”的表示因未见“river”而产生歧义。
2. **下一词预测目标偏差（next-token prediction bias）**：最终token倾向于生成而非语义压缩，影响其作为句子表示的质量。

这些限制使得传统的池化策略（如mean pooling或last-token pooling）效果受限。

### 提出的新方法：KV-Embedding
作者提出 **KV-Embedding**，一种无需训练即可从冻结的decoder-only LLM中提取高质量文本嵌入的方法，其核心思想是：
- **内部KV重路由（Internal KV Re-routing）**：利用模型自身在前向传播过程中已计算出的**最后一层各层的Key-Value（KV）状态**，将其作为全局上下文摘要，并通过**prepend方式注入到每一层的注意力机制中**，使所有token都能在一个前向传递中访问序列级上下文。
- **压缩导向提示（Compression-Oriented Prompting）**：设计特定模板（如“Compress the context in one word:”）引导模型将最终token用于语义压缩，缓解生成偏差。
- **基于内在维度（Intrinsic Dimensionality, ID）的自动层选择**：通过估计每层表示的ID，自动识别语义最浓缩的层（即ID最低的层），仅在这些层进行KV重路由，确保方法具有**模型无关性（model-agnostic）**，无需手动调参。

### 相比现有方法的优势
| 方法 | 缺陷 | KV-Embedding优势 |
|------|------|------------------|
| **PromptEOL** | 仅缓解生成偏差，不解决信息不对称 | 同时解决两大问题 |
| **Echo** | 输入重复导致序列长度翻倍，计算复杂度$O(n^2)$，存在“lost-in-the-middle”问题 | 不修改输入，保持原序列长度 |
| **Token Prepending** | 引入OOV特殊token，表示不可控 | 利用模型内部已有的KV状态，更稳定可靠 |

KV-Embedding通过**内部状态操纵（internal state manipulation）** 而非输入修改，实现了高效且可扩展的训练自由嵌入方案。

---

## 2. 核心实验方法和设置

### 数据集
- **MTEB (Massive Text Embedding Benchmark)**：涵盖7大类任务（STS、Retrieval、Classification、Pair Classification、Clustering、Reranking、Summarization），共42个数据集，用于综合评估嵌入质量。
- **LoCoV1 (Long-Context Retrieval Benchmark)**：专门测试长上下文检索能力，文档被截断为1024、2048、4096 tokens，用于验证方法在长序列下的鲁棒性。

### 实验设置与评估指标
- **模型**：在三种主流decoder-only LLM上测试：
  - Qwen3-4B
  - Mistral-7B-Instruct-v0.1
  - Llama-3.1-8B-Instruct
- **评估模式**：zero-shot, training-free setting
- **主要指标**：
  - MTEB：各类任务主指标平均得分（如STS用Spearman相关系数，Retrieval用NDCG@10）
  - LoCoV1：NDCG@10
- **实现细节**：
  - 使用TwoNN估计器计算Intrinsic Dimensionality
  - KV重路由层选择基于ID最小值区域
  - 最终嵌入采用hybrid pooling（last-token与mean pooling平均后归一化）

### 基线方法对比
- **Last Token**：直接使用最终token的隐藏状态
- **Mean Pooling**：对所有token隐藏状态取平均
- **PromptEOL**：使用提示引导语义压缩
- **Echo**：输入重复以模拟双向上下文
- **Token Prepending**：插入特殊token传播上下文
- **w/o KV Re-routing**：消融实验，仅保留提示但禁用KV重路由

---

## 3. 主要实验结果和性能指标

### 关键性能数据（MTEB平均得分）
| 方法 | Qwen3-4B | Mistral-7B | Llama-3.1-8B |
|------|----------|------------|---------------|
| PromptEOL | 0.4478 | 0.4537 | 0.4702 |
| Echo | 0.4375 | 0.5008 | 0.5034 |
| **KV-Embedding** | **0.4937** | **0.5341** | **0.5270** |

- 在三个backbone上均显著优于现有训练自由方法，**最高提升达10%**。

### 长上下文表现（LoCoV1, NDCG@10 @4096 tokens）
| 方法 | Qwen3-4B | Mistral-7B | Llama-3.1-8B |
|------|----------|------------|---------------|
| PromptEOL | 0.1286 | 0.0442 | 0.1285 |
| Echo | 0.1318 | 0.0591 | 0.1191 |
| **KV-Embedding** | **0.1822** | **0.2068** | **0.2404** |

- 在4096长度下仍保持强劲性能，**大幅超越基线（1.3–3.5倍）**，表明其有效缓解了长序列中的上下文稀释问题。

### 消融实验结果
- **移除KV重路由（w/o KV Re-routing）**：
  - 在Mistral-7B上平均分从0.5341降至0.4502（↓8.4%）
  - 表明**KV重路由是性能提升的主要驱动力**
- **注意力偏置（Attention Bias b）**：
  - 设置$b=1.0$时性能最优（图2）
  - $b>3.0$时性能下降，说明过度依赖全局摘要会损害局部细节
- **层选择策略**：
  - 基于ID的选择优于均匀划分（early/middle/late layers）
  - 在Mistral-7B上，ID选择7层（13-19）优于middle-third的10层策略
- **池化策略**：
  - Hybrid pooling（last + mean）表现最佳，优于单独last-token或mean pooling

---

## 4. 关键结论和发现

### 主要发现
1. **decoder-only LLM内部蕴含强大的潜在表示能力**，可通过KV状态重路由激活。
2. **内部状态操纵优于输入修改**：KV-Embedding在不改变输入的前提下，实现了全局上下文访问，避免了Echo和Token Prepending的副作用。
3. **ID是有效的层选择指标**：基于intrinsic dimensionality的自动化策略能准确识别语义最浓缩的层，实现跨架构通用性。
4. **KV-Embedding在长序列下依然稳健**：在4096 tokens长度下仍保持高性能，解决了传统方法的“context dilution”问题。
5. **嵌入空间更优**：分析显示KV-Embedding产生的嵌入空间更具**各向同性（isotropic）**，alignment和uniformity指标更优（表5）。

### 方法的局限性
1. **推理延迟增加**：KV重路由引入额外计算，相比标准池化有一定延迟开销。
2. **性能上限低于监督微调方法**：作为训练自由方法，其性能仍不及经过contrastive fine-tuning的有监督模型。
3. **依赖模型内部几何结构**：虽然ID策略提升了泛化性，但不同架构的ID轨迹差异仍需进一步研究。

### 未来工作方向
- 探索KV重路由在其他下游任务（如few-shot learning）中的应用。
- 将该思想扩展至encoder-decoder架构或其他生成模型。
- 结合轻量级微调（如prefix tuning）进一步提升性能。
- 研究更高效的KV缓存管理机制以降低延迟。

---

> **总结**：KV-Embedding提出了一种新颖的训练自由文本嵌入框架，通过**内部KV重路由**解决了decoder-only LLM在嵌入任务中的根本性挑战。其实验充分、设计巧妙，在MTEB和LoCoV1上均取得SOTA性能，为解锁大语言模型的表示潜力提供了新范式。

</details>

---

### 8. [SuperSFL: Resource-Heterogeneous Federated Split Learning with Weight-Sharing Super-Networks](https://arxiv.org/abs/2601.02092)

**Authors**: Abdullah Al Asif, Sixing Yu, Juan Pablo Munoz, Arya Mazaheri, Ali Jannesari  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02092v1  

#### Abstract
SplitFed Learning (SFL) combines federated learning and split learning to enable collaborative training across distributed edge devices; however, it faces significant challenges in heterogeneous environments with diverse computational and communication capabilities. This paper proposes \textit{Super...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SuperSFL: Resource-Heterogeneous Federated Split Learning with Weight-Sharing Super-Networks —— 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对**资源异构边缘环境下的联邦学习（Federated Learning, FL）挑战**，特别是 **Split Federated Learning (SFL)** 在以下方面的局限性：

- **设备异构性（Device Heterogeneity）**：传统 SFL 假设所有客户端具有相似的计算、内存和通信能力，这在真实边缘场景（如手机、传感器等混合设备）中不现实。
- **服务器依赖性强**：SFL 要求客户端必须等待服务器返回梯度才能继续训练，一旦网络中断或服务器不可用，训练即停滞，缺乏容错能力。
- **收敛速度慢、通信开销大**：由于模型分割固定且缺乏有效优化机制，SFL 需要更多通信轮次，导致高通信成本和长训练时间。

---

### **提出了什么新方法或新思路**

作者提出 **SuperSFL**，一种结合 **Federated Learning** 和 **Split Learning** 的新型框架，其核心创新包括：

#### ✅ **Weight-Sharing Super-Network（权重共享超网络）**
- 构建一个全局的“超网络”（super-network），所有客户端从中提取**连续前缀子网络**作为本地编码器。
- 子网络深度根据客户端的 **memory capacity** 和 **communication latency** 动态分配，实现资源感知的个性化模型部署。

#### ✅ **Three-Phase Gradient Fusion (TPGF)**
- 一种三阶段梯度融合机制，协调本地与服务器端的监督信号：
  1. **Phase 1**: 客户端使用轻量级分类器进行本地监督，生成局部梯度；
  2. **Phase 2**: 服务器完成深层推理并反传梯度；
  3. **Phase 3**: 客户端将本地梯度与服务器梯度按损失加权融合，更新本地编码器。
- 优势：提升浅层编码器的训练稳定性，加速收敛。

#### ✅ **Fault-Tolerant Client-Side Classifier**
- 每个客户端配备一个轻量级本地分类器，在服务器不可达时进入“fallback mode”，自主进行本地训练。
- 当连接恢复后，通过聚合机制无缝同步进度，避免训练中断。

#### ✅ **Collaborative Client-Server Aggregation**
- 设计了一种结构对齐且性能感知的参数聚合策略：
  - 按层聚合不同深度的客户端编码器参数；
  - 权重由客户端模型深度和训练损失共同决定；
  - 引入一致性正则项（consistency regularization）防止客户端与服务器参数偏离过大。

---

### **相比现有方法的优势**

| 维度 | SuperSFL 优势 |
|------|----------------|
| **适应性** | 支持高度异构设备，无需统一模型结构 |
| **效率** | 减少通信轮数（2–5×）、降低总通信量（最高 20×）、缩短训练时间（最高 13×） |
| **鲁棒性** | 支持间歇性通信失败下的持续训练 |
| **收敛性** | 更快达到更高准确率，尤其在 non-IID 数据下表现优异 |
| **能效** | 单位精度能耗更低，碳排放更少 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **CIFAR-10** 和 **CIFAR-100**
- 广泛用于 FL 基准测试，适合评估非独立同分布（non-IID）场景下的性能。

### **实验设置**

| 参数 | 设置 |
|------|------|
| 模型架构 | Vision Transformer (ViT-16) |
| 客户端数量 | 50 和 100 |
| 数据划分 | Dirichlet 分布（浓度参数 α=0.5），模拟 non-IID 场景 |
| 设备异构模拟 | 内存：[2, 16] GB 均匀采样；延迟：[20, 200] ms 均匀采样 |
| 硬件平台 | NVIDIA A10 / A100 GPU（物理同构，逻辑异构） |
| 初始通信配置 | 客户端仅上报一次 memory 和 latency，后续不再动态探测 |

### **评估指标**

| 指标类别 | 具体指标 |
|--------|---------|
| **准确性** | 最终测试准确率（Top-1 Acc） |
| **效率** | 达到目标准确率所需的：<br>• 通信轮数（Communication Rounds）<br>• 总通信成本（MB）<br>• 端到端训练时间（秒） |
| **能效** | • 平均功耗（W）<br>• 功耗每精度点（Power/Accuracy, W/%）<br>• CO₂ 排放量（g） |
| **鲁棒性** | 不同服务器可用率下的准确率变化（0% ~ 100%） |

### **基线方法对比**

- **SFL (Split Federated Learning)**：标准联邦拆分学习，固定拆分点。
- **DFL (Dynamic Federated Learning)**：支持动态参与的联邦学习方法。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table I）**

| Dataset | Clients | Target Acc (%) | Method | Rounds | Comm. Cost (MB) | Training Time (s) |
|--------|--------|----------------|--------|--------|------------------|--------------------|
| CIFAR-10 | 50 | 70 | SFL | 11 | 9075 | 6127 |
|          |     |    | DFL | 9  | 2305 | 2650 |
|          |     |    | **SSFL** | **5**  | **466** | **595** |
| CIFAR-10 | 100 | 75 | SFL | 19 | 21463 | 12168 |
|          |      |    | DFL | 16 | 15472 | 14368 |
|          |      |    | **SSFL** | **12** | **939** | **1010** |
| CIFAR-100 | 50 | 75 | SFL | 35 | 28938 | 21284 |
|           |     |    | DFL | 27 | 7909  | 9796  |
|           |     |    | **SSFL** | **15** | **7194** | **8766** |
| CIFAR-100 | 100 | 80 | SFL | 100 | 165358 | 114955 |
|            |       |    | DFL | 34  | 13638  | 15328  |
|            |       |    | **SSFL** | **22** | **9719** | **8926** |

> 注：SSFL 即 SuperSFL

#### **性能提升总结**
- **通信轮数减少**：2–5× 快于 SFL，1.2–1.8× 快于 DFL
- **通信成本降低**：最高达 **20×**（CIFAR-10, 100 clients）
- **训练时间缩短**：最高达 **13×**（CIFAR-10, 50 clients）

---

### **与基线方法的对比结果**

- **准确性方面**：
  - SuperSFL 在 CIFAR-10 上可达 **>97%** 准确率，显著高于 SFL（~75%）和 DFL（~76%）。
  - 在 CIFAR-100 上也稳定领先，最终准确率高出约 **2–3%**。

- **收敛速度**：
  - 如图 3 所示，SuperSFL 在极少数通信轮内即可快速收敛，而 SFL 收敛缓慢甚至难以达标。

- **能效表现（见 Table III）**：
  - **CIFAR-10**：SuperSFL 的 Power/Accuracy 为 **5.09 W/%**，优于 SFL（14.78）和 DFL（5.17）。
  - **CIFAR-100**：虽绝对功耗较高，但单位精度能耗仍优于 SFL，接近 DFL水平。

---

### **消融实验结果（Ablation Study）**

#### **TPGF 消融分析（图 6）**
- **完整 TPGF**：准确率 **96.93%**
- **移除 Loss-Based 加权**：降至 **91.47%**
- **移除 Depth-Based 加权**：进一步降至 **88.66%**
- **等权融合（Equal Fusion）**：最低 **85.89%**

👉 结论：**深度感知 + 损失可靠性感知** 是 TPGF 成功的关键。

#### **服务器可用性影响（Table III）**
| 服务器梯度可用率 | 准确率（CIFAR-10） |
|------------------|---------------------|
| 100%             | 95.58 ± 1.08        |
| 70%              | 93.81 ± 2.59        |
| 50%              | 93.12 ± 2.11        |
| 20%              | 91.03 ± 1.17        |
| 10%              | 89.77 ± 2.22        |
| **0%（完全无服务器）** | **86.36 ± 3.25** |

👉 结论：即使完全没有服务器参与，SuperSFL 仍可收敛至合理精度，展现出极强的**容错能力和去中心化潜力**。

---

## 4. 关键结论和发现

### **主要发现**

1. **SuperSFL 显著提升了异构边缘环境中 FL 的效率与鲁棒性**：
   - 通过动态子网络分配和 TPGF，实现了更快收敛和更低通信开销。
   
2. **TPGF 是加速收敛的核心机制**：
   - 融合本地与服务器梯度，缓解浅层编码器监督不足的问题。

3. **故障容忍设计有效应对不稳定网络**：
   - 客户端可在断连期间持续训练，系统整体可靠性大幅提升。

4. **能量效率优于主流方法**：
   - 尽管瞬时功耗略高，但由于训练时间大幅缩短，**总能耗和碳足迹更低**。

---

### **方法的局限性**

- **超参数依赖经验设定**：资源分配系数（α, β）和正则化参数（λ, T）为经验选择，缺乏理论指导。
- **基于模拟异构性**：实验在同构硬件上模拟异构资源，未在真实多样化的边缘设备集群中验证。
- **模型结构限制**：目前仅适用于 Vision Transformer 类结构，对其他模型（如 CNN、RNN）的适配需进一步研究。
- **隐私保护未深入探讨**：未集成差分隐私（DP）或安全多方计算（SMPC）等增强隐私的技术。

---

### **未来工作方向**

1. **系统化超参数调优机制**：建立 principled 的资源评分与参数选择准则。
2. **真实边缘设备验证**：在物理异构设备群（如树莓派、手机、嵌入式设备）上部署测试。
3. **高级个性化机制**：结合 clustered FL 或 hypernetwork 进行更细粒度的模型定制。
4. **隐私增强集成**：引入 DP、secure aggregation 等技术以满足严格隐私需求。
5. **扩展至其他任务**：应用于 NLP、语音识别等非视觉领域。

---

## ✅ 总结

**SuperSFL** 是一项面向**资源异构边缘环境**的高效、鲁棒、节能的联邦拆分学习框架。它通过 **weight-sharing super-network + TPGF + fault-tolerant classifier + structured aggregation** 四大组件，成功解决了传统 SFL 中的设备不适配、收敛慢、容错差等问题。实验证明其在 **准确性、通信效率、训练速度、能效** 等多个维度全面超越 SFL 和 DFL，是推动 FL 在真实边缘场景落地的重要进展。

</details>

---

### 9. [BigSUMO: A Scalable Framework for Big Data Traffic Analytics and Parallel Simulation](https://arxiv.org/abs/2601.02286)

**Authors**: Rahul Sengupta, Nooshin Yousefzadeh, Manav Sanghvi, Yash Ranjan, Anand Rangarajan, Sanjay Ranka, Yashaswi Karnati, Jeremy Dilmore, Tushar Patel, Ryan Casburn  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02286v1  

#### Abstract
With growing urbanization worldwide, efficient management of traffic infrastructure is critical for transportation agencies and city planners. It is essential to have tools that help analyze large volumes of stored traffic data and make effective interventions. To address this need, we present ``Big...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*BigSUMO: A Scalable Framework for Big Data Traffic Analytics and Parallel Simulation*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
随着城市化进程加快，交通管理部门面临海量异构交通数据（如ATSPM、轨迹数据）的处理挑战。现有系统往往局限于单一数据模态或特定应用场景，缺乏一个**端到端、可扩展、开源且支持描述性与处方性分析**的城市级交通分析框架。

BigSUMO 正是为了解决以下核心问题而提出：
- 如何高效整合高分辨率 **ATSPM 数据**（loop detector 和信号状态）与稀疏的 **probe trajectory 数据**；
- 如何实现城市尺度下的实时交通状态感知与异常事件检测（interruption detection）；
- 如何基于真实数据驱动大规模并行仿真，进行“what-if”场景测试以优化交通控制策略。

---

### 🚀 提出的新方法与创新思路

1. **端到端可扩展分析框架 BigSUMO**
   - 集成数据预处理、描述性分析、中断检测、微仿真（SUMO）与处方性分析于一体；
   - 支持模块化设计，允许灵活替换不同算法组件（如 outlier detection 算法）。

2. **基于空间掩码的空间裁剪机制（Spatial Masks）**
   - 利用 FDOT 的 GIS 数据自动生成道路缓冲区与交叉口圆形掩码；
   - 自动裁剪轨迹数据至感兴趣区域（如特定交叉口），提升分析精度与效率。

3. **双源中断检测系统**
   - 结合 **ATSPM 数据**（相位级车流统计）与 **trajectory 数据**（个体行为建模）进行互补式异常识别；
   - 轨迹层面采用 **Angle-Based Outlier Detection (ABOD)** 对归一化特征向量进行异常检测。

4. **并行化的 SUMO 微仿真架构**
   - 使用 Python `multiprocessing` 实现数百个 SUMO 仿真的并行执行；
   - 支持在普通云服务器上运行，无需商业软件许可（如 VISSIM）。

5. **开放性和可部署性**
   - 完全基于开源工具链构建（Pandas, GeoPandas, MovingPandas, SUMO, PyOD, Jupyter等）；
   - 可部署于任意支持 Python 的云平台，成本低、易维护。

---

### 🔍 相比现有方法的优势

| 维度 | 现有系统局限 | BigSUMO 的优势 |
|------|--------------|----------------|
| 数据融合 | 多依赖单一数据源（视频或 loop detector） | 融合 ATSPM + probe trajectory，增强上下文理解 |
| 分析粒度 | 多为宏观或中观分析 | 支持微观个体轨迹建模与仿真 |
| 扩展性 | 封闭系统或需昂贵许可证（如 VISSIM） | 开源、可并行、适合城市级部署 |
| 异常检测 | 仅基于流量突变 | 引入轨迹行为建模 + ABOD，捕捉潜在风险行为 |
| 仿真能力 | 单次仿真耗时长，难以参数搜索 | 并行运行百次以上仿真，支持 grid search 优化 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集

| 数据类型 | 来源 | 特征 |
|--------|------|-------|
| **ATSPM 数据** | Florida Department of Transportation (FDOT) | 包含 loop detector 车辆计数、信号灯相位状态，时间粒度精细 |
| **Probe Trajectory 数据** | 商业供应商（非网约车） | 时间分辨率 3 秒，空间精度 ~1 米；每日约 17,000–22,000 条行程；覆盖佛罗里达州三个县；2024 年两个月数据 |
| **渗透率（Penetration Rate）** | — | 当前仅为 3%–7%，限制了轨迹密度 |
| **GIS 数据** | FDOT SunStore 数据库 | 包括 Basemap Route Roads 和 Intersections，用于生成 spatial masks |

---

### ⚙️ 实验设置与流程

#### （1）数据处理流水线
- **轨迹预处理**：过滤熄火状态、短旅程（<2分钟）、短距离（<150米）轨迹；
- **空间裁剪**：利用 QGIS 生成 35m 道路缓冲 + 125m 交叉口圆缓冲，通过 Clip 操作提取目标区域轨迹；
- **特征提取**：对每条轨迹计算：
  - 停车时间（Stopped Time）
  - 平均速度（Avg. Speed）
  - 速度标准差（Std. Speed）
  - 行程时间（Travel Time）

#### （2）中断检测系统
- 构造每个轨迹的 **normalized feature vector**；
- 使用 **ABOD**（Angle-Based Outlier Detection）识别偏离正常模式的轨迹；
- 上下文对比：使用同一时空位置（同周、同时间段）的历史数据作为基准；
- 工具库：PyOD；
- 运行效率：单个交叉口全流程约 2–3 分钟（CPU 多线程并行）。

#### （3）SUMO 微仿真配置
- **网络构建**：使用 NETEDIT 或从 OpenStreetMap 导入；
- **校准（Calibration）**：
  - **Flow Calibration**：结合 ATSPM 车流总量 + 轨迹推断的 OD 矩阵 → 使用 routeSampler 生成符合实际出行路径的车辆流；
  - **Speed Calibration**：根据轨迹观测的最大速度分布调整 SUMO 的 `speedFactor` 参数；
  - **Signal Configuration**：导入 NEMA 相位逻辑（Ring-and-Barrier）及绿灯时长；
- **并行仿真**：Python `multiprocessing` 启动多个 SUMO 实例，模拟不同参数组合（如周期长度、最小/最大绿灯时间）；
- 输出格式：XML，便于后续分析。

---

### 📈 评估指标

| 类别 | 指标 |
|------|------|
| 描述性分析 | - 停车时间直方图<br>- 排队长度概率分布<br>- 转向移动矩阵（Origin-Destination Matrix）<br>- 制动事件热点图 |
| 中断检测 | - Outlier 向量数量与空间聚集性<br>- 实时中断概率热力图生成能力 |
| 仿真性能 | - 单次仿真耗时（10 个交叉口，1小时 ≈ 3–6分钟）<br>- 并行吞吐量（可同时运行上百次）<br>- 性能指标优化效果（如 Corridor Travel Time 下降） |

---

### 🔀 基线方法对比

| 方法 | 属性 | BigSUMO 优势 |
|------|------|-------------|
| **ReTime [对比提及]** | 使用 VISSIM 并行仿真优化信号配时 | BigSUMO 使用开源 SUMO，避免商业授权费用，同样支持 grid search |
| **传统 ATSPM 分析系统** | 仅基于固定检测器数据 | BigSUMO 融合轨迹数据，提供更丰富的行为洞察（如排队位置、制动行为） |
| **纯视频分析系统 [1][2][3]** | 依赖摄像头部署，视野受限 | BigSUMO 不依赖专用硬件，更具扩展性 |

> 注：本文未提供严格的定量对比表格，而是通过功能与架构比较体现优势。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 模块 | 结果 |
|------|------|
| **轨迹处理效率** | 单交叉口全流程（裁剪→特征提取→outlier detection）耗时约 **2–3 分钟**（CPU） |
| **并行仿真能力** | 在通用云实例上可轻松运行 **数百次 SUMO 仿真**，每次模拟 1 小时交通流 |
| **仿真速度** | 10 个交叉口的走廊，1 小时仿真耗时 **3–6 分钟**（取决于交通负荷） |
| **数据规模** | 处理跨度为 **2个月** 的轨迹数据，日均超 **2万条 journey** |

---

### 📊 主要可视化与分析结果（见 Figures）

- **Fig. 2**: 停车时间直方图显示多数停车持续时间 < 150 秒，符合典型信号周期；
- **Fig. 3 & 4**: 显示车辆常在停止线前停车，反映上游排队影响；
- **Fig. 5**: Origin-Destination 矩阵揭示各方向转向比例，可用于仿真输入；
- **Fig. 6**: 不同起止方向的平均行程时间差异显著（如 Southbound → Eastbound 达 121.3s）；
- **Fig. 7**: 排队长度分布表明 Eastbound 和 Southbound 方向排队更长；
- **Fig. 8**: 制动事件热点集中在某些进口道，提示潜在安全隐患；
- **Fig. 9 & 10**: 展示整个中断检测与仿真系统的模块化流程。

---

### ❌ 消融实验（Ablation Study）
- 文中**未明确开展消融实验**，但指出：
  - 当前 **ATSPM-based interruption detection 更可靠**；
  - 轨迹-based 方法受限于低 CV penetration rate（3%-7%）；
  - 若未来 connected vehicle 渗透率上升，轨迹方法性能有望提升。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **BigSUMO 成功实现了城市级交通数据分析与仿真的闭环**：
   - 从原始数据 → 描述性分析 → 异常检测 → 仿真验证 → 决策建议，形成完整链条。

2. **多源数据融合显著提升分析深度**：
   - ATSPM 提供宏观流量趋势；
   - Probe trajectories 提供微观行为细节（如排队位置、急刹点）。

3. **开源技术栈具备高度可扩展性与实用性**：
   - 可在 commodity cloud 上部署，适合中小型交通机构使用；
   - 支持 Jupyter notebook 交互式分析，便于定制与共享。

4. **并行仿真极大提升了政策评估效率**：
   - 支持快速测试多种信号配时方案，辅助 corridor-level 优化。

5. **轨迹数据可用于训练 Deep Learning 模型**：
   - BigSUMO 已被用于生成 GNN 和 Digital Twin 模型所需的大规模 labeled 数据集（如 [11][12][13]）。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **低 Connected Vehicle 渗透率** | 当前 probe 数据仅覆盖 3%-7% 车辆，导致轨迹稀疏，影响异常检测灵敏度 |
| **轨迹隐私处理带来的信息缺失** | 商家隐藏部分中间点，虽合法但仍降低轨迹完整性 |
| **尚未完全自动化部署** | spatial mask 生成仍需一定人工干预（如 buffer 参数设定） |
| **缺乏严格量化评估** | 缺少与 ground truth（如事故记录）对比的 precision/recall 指标 |

---

### 🔮 未来工作方向

1. **提升 trajectory-based interruption detection 的可靠性**：
   - 随着 V2X/V2V 基础设施普及，预期 CV penetration 将提高，届时可大幅提升轨迹分析质量。

2. **集成更多传感器模态**：
   - 计划融合 video、LiDAR、DSRC 等多模态数据，构建更全面的感知层。

3. **发展自适应信号控制系统**：
   - 将 BigSUMO 输出接入 adaptive signal control 系统，实现动态响应。

4. **推动数字孪生应用**：
   - 构建 real-time digital twin，实现城市交通的实时镜像与预测。

5. **代码开源计划**：
   - 作者承诺逐步清理并发布源码至 GitHub（github.com/NSH2022/BigSUMO）。

---

## 总结

> **BigSUMO 是一个面向未来的、开源的城市交通智能分析基础设施**。它不仅解决了当前交通大数据“看得见但难用好”的痛点，还为 AI 驱动的智慧城市提供了强大的数据引擎与仿真平台。尽管受限于当前数据质量，其设计理念、模块化架构与并行能力使其极具推广价值，尤其适用于希望低成本构建智能交通系统的政府机构与研究团队。

</details>

---

### 10. [Jenius Agent: Towards Experience-Driven Accuracy Optimization in Real-World Scenarios](https://arxiv.org/abs/2601.01857)

**Authors**: Defei Xia, Bingfeng Pi, Shenbin Zhang, Song Hua, Yunfei Wei, Lei Zuo  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.01857v1  

#### Abstract
As agent systems powered by large language models (LLMs) advance, improving the task performance of an autonomous agent, especially in context understanding, tool usage, and response generation, has become increasingly critical. Although prior studies have advanced the overall design of LLM-based ag...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于大语言模型（LLM）的自主智能体系统在实际应用中面临三大挑战：
- **固定提示（prompt）导致意图理解偏差**：静态或通用的提示无法适应任务状态变化，易造成行为不稳定和输出不一致。
- **工具调用缺乏上下文感知**：依赖预定义工具列表或手工规则，难以在模糊或多领域场景下准确选择合适工具，导致冗余或错误调用。
- **长对话中的上下文膨胀**：历史对话积累过多冗余信息，增加token消耗、稀释关键信号，影响推理质量。

这些问题限制了智能体在复杂、多轮、真实世界任务中的准确性、效率和稳定性。

---

### **提出的新方法与新思路**
本文提出了一个名为 **Jenius-Agent** 的端到端智能体框架，围绕“经验驱动”的优化理念，集成三个核心模块：

#### （1）**自适应提示生成（Adaptive Prompt Generation）**
- 结合角色指令、任务状态和用户上下文动态生成系统提示。
- 引入四类语义分类（社交互动、创意生成、事实回忆、工具增强推理），实现意图路由与响应格式定制。
- 加入安全审查机制，防止幻觉、偏见和非法内容输出。

> ✅ *优势*：提升任务对齐度，减少误判，增强鲁棒性和安全性。

#### （2）**上下文感知的工具编排（Context-Aware Tool Orchestration）**
- 将所有工具表示为高维嵌入（Qwen3 Embedding），通过语义相似性进行检索。
- 设计三步筛选流程：
  1. Top-M候选检索（基于相似度排序）
  2. 基于拐点检测的过滤（结合相似度跳跃 + Kneedle算法）
  3. 动态截断并补全至N=min(N_jump, N_kneedle)，若不足则取Top-10

> ✅ *优势*：显著降低无关工具调用率，在噪声环境下仍能精准匹配用户意图。

#### （3）**分层记忆管理（Hierarchical Memory Management）**
- **对话级对齐**：维护`Human → AI → Tool → AI`的标准交互模式，自动修复缺失消息（如因API失败导致空ToolMessage）。
- **会话级压缩**：当消息数超过阈值K时，将早期历史摘要为`SystemMessage`，保留最新一轮完整上下文。
- 摘要仅覆盖从首个`HumanMessage`到倒数第二个`HumanMessage`之间的部分，确保最近交互不受损。

> ✅ *优势*：有效控制token增长，保持长期推理连贯性，避免关键信息丢失。

此外，该框架支持 **Model Context Protocol (MCP)**、文件I/O及执行反馈闭环，具备协议兼容性与可扩展性。

---

### **相比现有方法的优势**
| 维度 | 现有方法（如AutoGPT、LangChain Agents） | Jenius-Agent |
|------|----------------------------------------|-------------|
| 提示机制 | 固定模板，缺乏动态调整 | 自适应生成，融合意图识别与历史感知 |
| 工具选择 | 静态列表或硬编码规则 | 语义检索 + 拐点过滤，动态适配上下文 |
| 记忆管理 | 窗口截断或简单摘要 | 分层结构，兼顾细粒度依赖与宏观连贯 |
| 协议兼容 | 多为私有设计 | 支持MCP等开放协议，便于系统集成 |

> ✅ 整体上实现了更高的任务准确性、更低的资源开销和更强的现实部署能力。

---

## **2. 核心实验方法和设置**

### **使用的数据集**

#### （1）**APIGen**（公开基准）
- 规模：60K样本，涵盖21个类别（约3.6K APIs）
- 特点：单轮对话，关注自然语言到API调用的映射
- 局限：元数据简略，无真实输出，缺乏多步推理
- 本文改进：每条查询注入100个无关工具，模拟高噪声环境

#### （2）**Jenius-bench**（本文构建的真实世界多轮任务数据集）
- 规模：850高质量人工标注样本，覆盖38类工具
- 场景：旅行规划、票务预订、网页生成、学术检索等
- 特点：
  - 多轮对话，含复杂状态演化
  - 每步记录完整执行轨迹（工具调用、参数、返回值、响应）
  - 工具附带丰富语义描述（目的、前提、示例）
  - 所有路径经专家审核，确保正确性与可信度

> ⚠️ 对比见表1：Jenius-bench更贴近真实应用场景，强调跨轮次推理与上下文一致性。

---

### **实验设置与评估指标**

#### **评估框架三大维度**：
| 维度 | 指标体系 | 内容 |
|------|---------|------|
| **过程保真度** | 4T Metrics | TCR, TFR, TIR, TPS |
| **输出质量** | CRCFF Metrics | Correctness, Relevance, Completeness, Fluency, Faithfulness |
| **效率** | Token Consumption | 输入/输出token总量，反映计算成本 |

##### **4T Metrics详解**：
- **TCR (Task Completion Rate)**：任务完全按正确顺序完成的比例
- **TFR (Task Failure Rate)**：未执行任何操作或发生运行时错误的任务比例
- **TIR (Task Incompletion Rate)**：部分完成（漏调、错序、无关调用）的任务比例
- **TPS (Task Performance Score)**：综合考虑正确、错误、遗漏工具的加权得分

##### **CRCFF Metrics**：
由Qwen和DeepSeek两个LLM作为评估器打分（0–10分制），衡量响应语义质量。

---

### **基线方法对比配置**

| 模型 | 描述 |
|------|------|
| **Base** | 标准ReAct架构，observe-think-act循环 |
| **B-P** | Base + 自适应提示生成（Adaptive Prompting） |
| **B-PT** | B-P + 上下文感知工具编排（Tool Orchestration） |
| **Jenius** | B-PT + 分层记忆管理（Hierarchical Memory） |

所有模型共享相同LLM backbone与推理协议，仅模块逐步叠加，便于归因分析。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### （1）**执行保真度（4T Metrics）**

| 数据集 | 模型 | TCR ↑ | TFR ↓ | TIR ↓ | TPS ↑ |
|-------|------|-------|-------|-------|-------|
| **APIGen** | Base | 0.8150 | 0.1800 | 0.0050 | 0.8150 |
|           | B-P  | 0.8275 | 0.1675 | 0.0050 | 0.8275 |
|           | B-PT | 0.8375 | 0.1587 | 0.0038 | 0.8375 |
|           | **Jenius** | **0.8500** | **0.1362** | **0.0138** | **0.8500** |
| **Jenius-bench** | Base | 0.5659 | 0.0329 | 0.4012 | 0.5968 |
|                 | B-P  | 0.7271 (+28.5%) | 0.0859 | 0.1871 | 0.7491 |
|                 | B-PT | 0.7494 | 0.0718 | 0.1788 | 0.7740 |
|                 | **Jenius** | **0.7647** | **0.0753** | **0.1600** | **0.7847** |

> 🔍 在APIGen上提升有限（已接近饱和），但在更复杂的Jenius-bench上，**TCR提升约20%**，TIR下降超60%，体现模块组合的有效性。

---

#### （2）**输出质量（CRCFF Metrics）**

| 评估器 | 模型 | Correctness ↑ | Relevance ↑ | Completeness ↑ | Fluency ↑ | Faithfulness ↑ |
|--------|------|----------------|--------------|------------------|------------|------------------|
| **Qwen** | Base | 0.6741 | 0.8951 | 0.7722 | 0.9294 | 0.7919 |
|          | Jenius | **0.7580** | **0.9447** | **0.8088** | **0.9771** | **0.8766** |
| **DeepSeek** | Base | 0.7890 | 0.9245 | 0.7898 | 0.9546 | 0.8291 |
|              | Jenius | **0.8350** | **0.9400** | **0.8143** | **0.9686** | **0.8636** |

> ✅ 平均提升 **8–10%**，尤其在Correctness和Fluency方面进步明显，说明各模块协同改善了语义质量和逻辑一致性。

---

#### （3）**Token消耗分析（图6）**

| 数据集 | Base | Jenius |
|--------|------|--------|
| **APIGen** | 9.96M | 2.46M (**↓75.3%**) |
| **Jenius-bench** | 9.27M | 3.65M (**↓60.6%**) |

> 💡 显著降低token使用量，主要归功于：
- 自适应提示减少冗余推理
- 工具编排抑制无效调用
- 分层记忆压缩历史上下文

---

#### （4）**消融实验结论**
- **B-P → B-PT**：加入工具编排后，TCR提升明显（+2.2%），TIR下降
- **B-PT → Jenius**：引入分层记忆进一步稳定长期推理，Faithfulness提升显著
- 三者联合带来**互补增益**，非简单叠加

---

## **4. 关键结论和发现**

### **主要发现**
1. **经验驱动的设计优于静态架构**：通过动态调整提示、工具选择和记忆结构，Jenius-Agent在真实复杂任务中表现出更强的适应性和准确性。
2. **模块化优化具有累积效应**：三个核心模块分别针对不同瓶颈，协同作用下实现全面性能跃升。
3. **真实世界数据更能揭示系统差异**：在高度结构化的APIGen上各模型表现趋同；而在Jenius-bench上，Jenius显著领先，验证其在现实场景中的优越性。
4. **效率与质量可以兼得**：不仅提升了任务完成率和输出质量，还大幅降低了token消耗和调用失败率。

---

### **方法的局限性**
1. **依赖高质量工具元数据**：工具嵌入效果受限于描述文本的质量，若原始metadata不完整或存在噪声，会影响检索精度。
2. **拐点检测对分布敏感**：Kneedle等方法在极端相似度分布下可能失效，需进一步鲁棒化。
3. **摘要可能丢失细节**：尽管保留高层语义，但某些细微依赖关系（如特定参数约束）可能在压缩中被忽略。
4. **评估仍依赖人工标注**：虽然引入LLM-as-Judge，但黄金标准轨迹获取成本较高，难以大规模扩展。

---

### **未来工作方向**
1. **更灵活的结果导向评估**：允许多种合法执行路径，避免过度惩罚合理变体。
2. **引入用户体验指标**：将用户满意度、决策延迟、交互成本纳入评价体系。
3. **动态模块重组机制**：根据任务类型自动启用/关闭某些优化模块，提升运行效率。
4. **多智能体协作架构**：探索分布式问题求解与跨Agent通信机制（如A2A协议）。
5. **持续学习与在线优化**：利用线上反馈闭环不断更新提示策略、工具索引和记忆模型。

---

> ✅ **总结**：Jenius-Agent提供了一个轻量、可扩展、协议兼容的智能体优化框架，在真实场景中实现了**约20%的任务准确率提升**，同时显著降低资源消耗。其已在生产环境部署（[jenius.cn](https://www.jenius.cn)），为工业级自主代理系统的构建提供了实证基础与实践蓝图。

</details>

---

### 11. [CD4LM: Consistency Distillation and aDaptive Decoding for Diffusion Language Models](https://arxiv.org/abs/2601.02236)

**Authors**: Yihao Liang, Ze Wang, Hao Chen, Ximeng Sun, Jialian Wu, Xiaodong Yu, Jiang Liu, Emad Barsoum, Zicheng Liu, Niraj K. Jha  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02236v1  

#### Abstract
Autoregressive large language models achieve strong results on many benchmarks, but decoding remains fundamentally latency-limited by sequential dependence on previously generated tokens. Diffusion language models (DLMs) promise parallel generation but suffer from a fundamental static-to-dynamic mis...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# CDLM: Consistency Distillation and aDaptive Decoding for Diffusion Language Models 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Diffusion Language Models (DLMs)** 在训练和推理之间存在**静态到动态的错配（static-to-dynamic misalignment）**：
- **训练阶段**：模型在固定的噪声调度（fixed schedule）下优化局部去噪步骤（如 $t \to t-\Delta t$），学习的是一个“静态”的向量场。
- **推理阶段**：为了实现低延迟生成，需要进行“长跳”（long-jump）式的快速去噪（如 $t \to t-k\Delta t$），这会经过训练时未见过的状态空间区域。

此外，现有方法在计算资源分配上缺乏灵活性，通常采用预设的解码策略（如固定步数或块大小），无法根据样本难度自适应调整。

### 提出的新方法
作者提出 **CDLM**（Consistency Distillation and aDaptive Decoding for Diffusion Language Models），一个统一框架，通过两个核心组件解耦训练与推理：

1. **Discrete-Space Consistency Distillation (DSCD)**  
   - 不再让学生模型过拟合单一固定轨迹，而是利用 **Rao-Blackwellized 目标函数**，训练一个对多种掩码模式具有鲁棒性的学生模型。
   - 学生模型被训练为直接从任意噪声状态映射到干净分布，具备**轨迹不变性（trajectory-invariant）**，从而能稳健处理推理中的“长跳”操作。

2. **Confidence-Adaptive Decoding (CAD)**  
   - 动态地基于每个 token 的预测置信度（如最大概率）决定是否解码该 token。
   - 高置信度 token 被立即提交，低置信度 token 继续保留为 `<mask>`，直到后续步骤中置信度足够高。
   - 结合 `kmin` 和 `kmax` 控制每步解码数量，保证进度的同时防止错误传播。

### 相比现有方法的优势
- **效率更高**：无需复杂排序或验证逻辑，计算开销极小，理论上的 NFE 减少可高效转化为实际 wall-clock 加速。
- **质量更稳**：由于 DSCD 训练的学生模型内在鲁棒，CAD 可以激进跳步而不导致生成崩溃（quality collapse）。
- **通用性强**：适用于 block-wise 和 pure diffusion 两种范式，且不依赖特定 teacher 或 embedding space。
- **帕累托前沿提升**：在多个任务上同时实现了速度和准确率的提升，而非单纯牺牲质量换速度。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **代码生成**：
  - **HumanEval**：零样本代码生成评测。
  - **MBPP**：三样本代码生成评测。
  - **OpenCodeInstruct**：用于 DSCD 训练的 200K 指令遵循代码数据集。
- **数学推理**：
  - **GSM8K**：小学数学应用题，用于训练和零样本评测。
  - **MATH500**：更具挑战性的数学问题集合。

### 实验设置和评估指标
- **模型架构**：基于 **LLaDA-8B-Instruct** 作为 teacher，student 初始化为其权重并进行蒸馏。
- **训练配置**：
  - BF16 精度，上下文长度 1024。
  - 使用 AdamW 优化器，学习率 $5\times10^{-6}$，cosine 衰减，warmup 10%。
  - DSCD 中 teacher mask ratio 小于 student mask ratio（嵌套掩码设计）。
- **推理配置**：
  - 最大生成长度 $L_{gen}=256$。
  - CAD 参数：`conf_threshold=0.95`, `kmin=1`, `kmax=32`。
  - 评估 batch size = 1，报告 wall-clock 时间加速比。
- **评估指标**：
  - **pass@1 / pass@5**（代码生成）
  - **Accuracy**（数学推理）
  - **Avg. NFE**（平均函数调用次数）
  - **Tokens/s** 和 **Speedup**（相对于 baseline 的 wall-clock 加速）

### 基线方法对比
- **LLaDA**：原始 diffusion model，在不同 block size 下运行。
- **Fast-dLLM**：一种无需训练的并行解码框架，结合 KV 缓存。
- **Sequential AR decoding**：作为最慢但稳定的 baseline。
- **SFT baseline**：监督微调版本，用于消融分析。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）

| Task | Benchmark | LLaDA Acc/NFE | CDLM Acc/NFE | Speedup |
|------|-----------|----------------|---------------|---------|
| Code | HumanEval (pass@1) | 38.7 / 256 | **40.9 / 113.2** | **3.30×** |
| Code | HumanEval+ (pass@1) | 31.7 / 256 | **32.9 / 115.0** | **3.25×** |
| Code | MBPP+ (pass@1) | 48.7 / 256 | 47.9 / 108.0 | 3.51× |
| Math | MATH500 (Acc%) | 37.3 / 512 | **38.6 / 148.3** | **5.33×** |
| **Avg.** | — | **45.6 / 292.6** | **46.3 / 117.2** | **3.62×** |

> 注：LLaDA 在 MATH500 上使用 512 步，其余任务使用 256 步。

### 与基线方法的对比结果
- 在 **GSM8K** 上，CDLM 以 **77.6%** 准确率匹配 LLaDA baseline（77.4%），但实现 **5.18× wall-clock 加速**（见 Table 1）。
- 在 **HumanEval** 上，准确率提升 **+2.2%**，同时获得 **3.30× 速度提升**。
- 在 **MATH500** 上，准确率提升 **+1.3%**，速度提升达 **5.33×**。
- 所有任务均实现 **帕累托前沿严格占优**：相同准确率下显著减少 NFE，或相同 NFE 下显著提升准确率。

### 消融实验结果（Table E.1）
- **DSCD 是关键**：若仅用 SFT 微调 + CAD，在 pure diffusion 设置下准确率从 54.7%（DSCD+CAD）降至 38.2%，说明标准训练无法支持激进自适应解码。
- **CAD 显著提效**：即使没有 DSCD，仅使用 CAD 也能将 NFE 从 256 降至 ~130，但质量下降明显；而 DSCD + CAD 可进一步将 NFE 降至 76.3–80.6，且保持高质量。
- **信心排序至关重要**：若将 confidence-based selection 替换为随机选择，准确率从 77.6% 骤降至 50.6%，NFE 却上升至 202.2，证明 token 选择顺序极为重要。
- **teacher-subset masking 必不可少**：独立采样 teacher/student mask 会导致训练不稳定甚至梯度爆炸，验证了 Rao-Blackwell 化设计的必要性。

---

## 4. 关键结论和发现

### 主要发现
1. **训练-推理错配是效率瓶颈的根本原因**：仅仅依靠推理时的启发式加速（如 step-skipping）无法突破性能上限，必须通过训练目标重新对齐。
2. **DSCD 实现了真正的“一致性蒸馏”**：学生模型学到的是 teacher 在所有可能路径上的期望行为，而非某一条具体轨迹，从而具备处理任意中间状态的能力。
3. **CAD 实现了高效的动态计算分配**：模型自动识别“容易”和“困难”的 token，优先稳定结构（如语法骨架），后期精修逻辑细节，形成“先搭骨架后填血肉”的分层生成策略（见 Fig. F.1）。
4. **算法效率增益可超线性转化为实际加速**：由于 CAD 控制逻辑轻量（全批量张量操作），NFE 的减少几乎完全转化为 wall-clock 时间节省，甚至出现 **superlinear speedup**（如 GSM8K 上 NFE 减少 3.36×，wall-clock 加速 5.18×）。

### 方法的局限性
1. **静态画布限制（Static Canvas Constraint）**：仍需预设最大序列长度 $L_{gen}$，短序列存在内存冗余，长序列受限于训练窗口。
2. **能力受 teacher 限制**：作为蒸馏框架，student 的推理能力理论上不超过 teacher。若 teacher 出现幻觉或逻辑错误，student 可能继承这些缺陷。
3. **高熵任务适用性有限**：confidence thresholding 在创造性写作等高不确定性任务中可能过于保守，抑制多样性或导致重复输出。

### 未来工作方向
1. **动态/无限上下文扩散模型**：结合 semi-autoregressive context shifting 等机制，支持任意长度生成。
2. **超越模仿学习的 on-policy fine-tuning**：引入强化学习，在 CAD 提供的高效探索基础上优化 correctness 而非 consistency。
3. **集成 KV caching**：将 DSCD+CAD 与 Fast-dLLM 类似的 KV 缓存机制结合，进一步降低每步计算成本。
4. **扩展至多模态与规划任务**：利用其分层生成特性，应用于 vision-language 或复杂决策任务。

---

> ✅ **总结一句话**：  
> CDLM 通过 **DSCD + CAD** 的协同设计，成功将 DLM 从“固定调度去噪器”转变为“灵活的实例感知 refinement operator”，在代码与数学推理任务上实现了 **3.62× 平均加速** 同时 **提升平均准确率**，推动了非自回归语言模型迈向实用化的重要一步。  
> 项目代码已开源：[https://github.com/yihao-liang/CDLM](https://github.com/yihao-liang/CDLM)

</details>

---

### 12. [Performance and Security Aware Distributed Service Placement in Fog Computing](https://arxiv.org/abs/2601.01125)

**Authors**: Mohammad Goudarzi, Arash Shaghaghi, Zhiyu Wang, Rajkumar Buyya  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.01125v1  

#### Abstract
The rapid proliferation of IoT applications has intensified the demand for efficient and secure service placement in Fog computing. However, heterogeneous resources, dynamic workloads, and diverse security requirements make optimal service placement highly challenging. Most solutions focus primarily...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Performance and Security Aware Distributed Service Placement in Fog Computing*

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Fog Computing** 环境下服务放置（Service Placement）中存在的两个核心挑战：
- **性能优化不足**：现有方法多关注响应时间、能耗等性能指标，忽视了安全需求。
- **安全性被忽略**：Fog 环境开放且分布式，易受攻击（如 DoS），但多数服务放置策略未将安全作为显式优化目标。

因此，如何在动态、异构的 Fog 环境中实现**性能与安全的联合优化**是一个关键难题。

---

### 提出的新方法：SPA-DDRL 框架
作者提出了一种名为 **Security and Performance-Aware Distributed Deep Reinforcement Learning (SPA-DDRL)** 的框架，其核心创新点如下：

#### （1）**三层安全评分模型（Three-Tier Security Hierarchy）**
- 将安全能力量化为可计算的“安全得分”（Security Score），分为三个层级：
  - **配置层（Configuration-Level）**：检查具体安全机制是否启用（如 AES-256、双因素认证）。
  - **能力层（Capability-Level）**：评估资源的安全功能（如传输加密、密钥管理）。
  - **控制层（Control-Level）**：对应高层安全策略（如设备授权清单、数据加密保护）。
- 引入**硬约束（Hard Constraints）惩罚机制**，对违反关键安全控制的任务施加严重负奖励，确保最终方案始终满足基本合规要求。

#### （2）**分布式 Broker-Learner 架构**
- 采用分布式的决策架构：
  - **多个 Broker** 在边缘节点上并行执行本地服务放置决策。
  - **一个 Centralized Learner** 负责聚合经验、更新全局策略。
- 实现了高可扩展性和适应性，适用于大规模异构环境。

#### （3）集成三大 DRL 技术提升学习效率
- **LSTM**：捕捉任务依赖和服务状态的时间序列依赖关系。
- **Prioritized Experience Replay (PER)**：优先回放具有高 TD-error 的关键经验，加速收敛。
- **Off-Policy Correction**：通过重要性采样（Importance Sampling）和梯度裁剪解决分布式训练中的策略偏差问题，增强稳定性。

---

### 相比现有方法的优势
| 维度 | SPA-DDRL 优势 |
|------|----------------|
| **目标全面性** | 同时优化响应时间和安全得分，而非单一性能指标 |
| **安全性保障** | 显式建模安全控制，并强制满足硬约束，避免不可行解 |
| **可扩展性** | 分布式架构支持大规模系统部署 |
| **学习效率** | LSTM + PER 显著加快收敛速度 |
| **鲁棒性** | Off-policy correction 提升分布式训练稳定性 |

---

## 2. 核心实验方法和设置

### 数据集与工作负载
- 使用基于真实 IoT 应用生成的 **合成 DAG 工作负载**，共构建 **7,500 个异构服务实例**。
- DAG 参数包括：
  - 任务数 $ K \in \{5,10,20,40,80,100\} $
  - 并行度（fat）和密度（density）参数组合生成多样化拓扑
- 每个任务包含 CPU、内存、存储、安全需求及截止时间等属性。

### 计算基础设施模拟
- 构建三层架构：
  - **Cloud Servers (CSs)**：20 台，高性能低延迟
  - **Fog Servers (FSs)**：30 台，中等性能
  - **IoT Devices**：50 台，资源受限
- 网络带宽按设备类型设定（IoT: 10–50 Mbps, FS: 50–200 Mbps, CS: 100–1000 Mbps）

### 安全模型配置
- 设计 **15 类安全控制**，每类含 5 种能力，每种能力由 3 个配置项组成。
- 不同服务器实现不同子集的安全机制，形成异构安全景观。

### 评估指标
| 指标 | 描述 |
|------|------|
| **Response Time** | 服务完成时间（以关键路径为准） |
| **Security Score** | 所有任务安全匹配程度的加权总分 |
| **Weighted Cost** | 归一化后的综合目标函数值（越小越好） |
| **Convergence Speed** | 收敛到稳定解所需的迭代次数 |
| **Scalability** | 在不同服务器数量下的表现变化 |
| **Decision Time Overhead (DTO)** | 单次调度决策耗时 |

### 基线方法对比
| 方法 | 技术基础 | 是否考虑安全 |
|------|----------|--------------|
| **X-DDRL** | IMPALA-based DDRL | 是（改进版） |
| **A3C-AHP** | A3C + 层次分析法 | 是 |
| **DRLIS** | PPO-based DRL | 是 |
| **PARL** | DDQN-based | 是 |
| **SCRA** | DQN-based | 是 |

所有基线均进行了超参数调优以保证公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 指标 | SPA-DDRL 表现 | 对比最佳基线提升 |
|------|---------------|------------------|
| **响应时间** | ~360 ms | 比 X-DDRL 快 **16.3%** |
| **收敛速度** | ~40 迭代收敛 | 比 X-DDRL 快 **33%** |
| **安全得分** | 接近最优（~1600） | 唯一始终满足硬约束 |
| **加权成本** | 接近零 | 显著优于其他方法 |

---

### 与基线方法的对比结果

#### （1）收敛性分析（Figure 4）
- **响应时间**：
  - SPA-DDRL 收敛至约 **360ms**
  - X-DDRL 达到 430ms（差 16.3%）
  - A3C-AHP 和 DRLIS 超过 950ms
  - PARL 和 SCRA 几乎不收敛（~1300ms）
- **安全得分**：
  - SPA-DDRL、X-DDRL、A3C-AHP 可达可行解
  - DRLIS、PARL、SCRA 持续出现严重违规（负百万级惩罚）
- **加权成本**：
  - SPA-DDRL 在第 50 次迭代即接近零
  - PARL 和 SCRA 成本高达 $1.3 \times 10^8$，主要来自安全惩罚

#### （2）系统规模扩展性测试（Figure 5）
- 当服务器数量从 25 增至 100：
  - SPA-DDRL 响应时间稳定在 **~300ms**
  - 其他方法响应时间上升 80–100%
- **唯一能在所有规模下保持安全合规的方法是 SPA-DDRL**
  - X-DDRL 和 A3C-AHP 仅在小规模可行
  - PARL 和 SCRA 在任何规模都无法满足硬安全约束

#### （3）训练效率（Speedup Analysis）
- 使用 8 个工作进程时，SPA-DDRL 实现 **2.7× 加速**
- 高于 X-DDRL（2.6×）和 A3C-AHP（2.4×）
- 非分布式方法（DRLIS、PARL、SCRA）速度更慢（speedup < 1.0）

#### （4）决策开销（DTO）
- SPA-DDRL 决策时间为 **87ms**，略高于基线（48–58ms）
- 开销主要来自 LSTM 推理和 PER 排序
- 但考虑到其显著的性能增益，该开销可接受

---

### 消融实验结果（Ablation Study）
- **SPA-DDRL(Base)**（无 LSTM & PER）：收敛需 ~90 次迭代
- **+ PER**：收敛提前至 ~70 次迭代（快 22%）
- **+ LSTM**：收敛至 ~60 次迭代（快 33%）
- **完整 SPA-DDRL**：仅需 ~40 次迭代（比 Base 快 **56%**）
- 结论：**LSTM 和 PER 均有效，协同作用带来最大收益**

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **性能与安全可以协同优化**：通过设计合理的奖励函数和安全评分体系，DRL 能同时优化响应时间和安全合规性。
2. ✅ **分布式架构显著提升可扩展性**：Broker-Learner 模式支持大规模 Fog 系统部署，且随节点增加性能下降缓慢。
3. ✅ **LSTM 和 PER 极大提升学习效率**：能更快识别复杂任务依赖模式和关键经验，缩短训练周期。
4. ✅ **硬约束机制至关重要**：没有明确的惩罚机制，大多数 DRL 方法会持续产生不安全的放置方案。
5. ✅ **SPA-DDRL 是唯一在所有系统规模下都能保持可行解的方法**，而其他方法随着环境复杂度上升逐渐失效。

---

### 方法的局限性
- **决策延迟较高**：由于引入 LSTM 和 PER，单次推理时间较长（87ms），可能不适合极端实时场景。
- **依赖准确的安全建模**：安全评分模型的有效性取决于实际环境中安全配置的真实性和可观测性。
- **超参数敏感**：虽然进行了调优，但在新环境下仍需重新调整 LSTM、PER、clip 等参数。

---

### 未来工作方向
1. **扩展至 Federated Learning 场景**：在保护隐私的同时进行安全感知的服务放置。
2. **动态安全需求调整机制**：结合实时威胁情报，动态调整任务的安全等级和防护策略。
3. **轻量化模型设计**：降低 LSTM 和神经网络复杂度，减少决策延迟，适应更多边缘设备。
4. **跨域协同优化**：研究跨多个管理域的 Fog 网络中安全与性能的协调机制。

---

> **总结一句话**：  
> SPA-DDRL 是首个在 **大规模异构 Fog 环境中实现性能与安全联合优化且始终保持合规可行** 的分布式 DRL 框架，通过 **三层安全模型 + 分布式架构 + LSTM/PER/off-policy correction** 的创新组合，在响应时间、收敛速度和可扩展性方面全面超越现有方法。

</details>

---

### 13. [Neuro-Channel Networks: A Multiplication-Free Architecture by Biological Signal Transmission](https://arxiv.org/abs/2601.02253)

**Authors**: Emrah Mete, Emin Erkan Korkmaz  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02253v1  

#### Abstract
The rapid proliferation of Deep Learning is increasingly constrained by its heavy reliance on high-performance hardware, particularly Graphics Processing Units (GPUs). These specialized accelerators are not only prohibitively expensive and energy-intensive but also suffer from significant supply sca...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Neuro-Channel Networks: A Multiplication-Free Architecture by Biological Signal Transmission —— 核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代深度学习严重依赖高算力硬件（如GPU），其核心瓶颈在于前向传播中密集的浮点矩阵乘法操作。这些操作不仅能耗高、成本昂贵，且难以部署在边缘设备（edge devices）和低功耗芯片上。此外，传统人工神经网络的乘法机制缺乏生物可解释性。

### 提出的新方法与新思路
作者提出了一种全新的**完全无乘法**（multiplication-free）神经网络架构——**Neuro-Channel Networks (NCN)**，其设计灵感来源于生物突触中的信号传递机制：

- **Channel Width**：将传统权重 $ w $ 替换为“通道宽度”，用于物理限制输入信号幅度，实现类似离子通道饱和效应的非线性门控。
- **Neurotransmitter Bypass**：引入一个可学习参数 $ n $ 模拟神经递质作用，作为化学调节通路，在主通道受限时提供旁路，确保梯度流动。
- 所有前向计算仅使用加法、减法和位运算（如 `min`, `sign`, `multiplexing`），彻底消除浮点乘法。

### 相比现有方法的优势
| 方法 | 缺陷 | NCN 的优势 |
|------|------|------------|
| **Binary Neural Networks (BNNs)** | 极端量化导致信息丢失、训练不稳定 | 不依赖1-bit量化，保留更多表达能力 |
| **AdderNets** | 使用L1距离替代点积，数学近似而非生物合理 | 基于真实生物机制建模，更具生物学合理性 |
| **DeepShift等位移网络** | 仍基于乘法近似（幂次逼近） | 完全去除乘法，仅用加法与逻辑操作 |
| **Spiking Neural Networks (SNNs)** | 难以训练（不可微） | 支持标准反向传播，训练稳定 |

> ✅ **核心优势**：  
> - 生物可解释性强  
> - 前向过程零乘法，适合超低功耗硬件（CPU/FPGA/ASIC）  
> - 可形成复杂非线性决策边界  

---

## 2. 核心实验方法和设置

### 使用的数据集
本文是**概念验证研究**（proof-of-concept），未使用大规模真实数据集，而是选择了两个经典的非线性逻辑函数任务：

- **XOR Problem**（异或问题）
- **3-bit Majority Function**（三输入多数表决函数）

这两个问题是检验模型是否具备非线性分类能力的基本基准。

### 实验设置
| 参数 | 设置 |
|------|------|
| 网络结构 | 全连接 NCN 层 + Softmax 输出层 |
| 初始化 | 权重 $ w \sim \mathcal{N}(0,1) $，神经递质 $ n \sim \mathcal{N}(0,0.5) $ |
| 优化器 | SGD with Momentum（动量=0.9） |
| 学习率 | 0.001 |
| 损失函数 | Cross-Entropy Loss |
| 训练算法 | Standard Backpropagation [8]（尽管前向无乘法，但训练阶段仍允许乘法） |

#### XOR 实验配置
- 输入维度：2
- 网络拓扑：2 → 4 → 2
- Epochs：1000

#### Majority Function 实验配置
- 输入维度：3
- 网络拓扑：3 → 8 → 2
- Epochs：200

### 评估指标
- **准确率（Accuracy）**：对所有可能输入组合（共 $2^2=4$ 或 $2^3=8$ 种）进行测试，判断是否完全正确分类。
- **决策边界可视化**：展示模型能否构建非线性分离面。

### 基线方法对比
本研究未直接与其他DNN变体（如BNN、AdderNet）在相同任务上定量比较性能或能效，而是强调：
- 在**没有乘法的前提下**，NCN 能解决传统单层感知机无法处理的非线性问题；
- 证明其具备构造复杂决策边界的潜力。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 任务 | 准确率 | 是否收敛 |
|------|--------|----------|
| XOR Problem | **100%** | 是 |
| 3-bit Majority Function | **100%** | 是 |

### 与基线方法的对比结果
虽然没有数值化的横向对比表，但从理论和功能角度得出以下结论：

| 维度 | NCN 表现 |
|------|---------|
| 非线性建模能力 | ✅ 成功解决 XOR 和 Majority 问题（非线性不可分） |
| 决策边界复杂度 | ✅ 图3显示清晰的非线性分割区域 |
| 梯度传播稳定性 | ✅ 引入 Neurotransmitter Bypass 避免“死梯度”问题 |
| 计算效率 | ✅ 合成操作分析表明：每神经元节省 $d$ 次乘法（$d$: 输入维数） |

### 消融实验
论文中**未提供明确的消融实验**（ablation study），例如关闭 Neurotransmitter 分支或移除归一化项的影响。这是当前工作的局限之一。

---

## 4. 关键结论和发现

### 主要发现
1. **无需乘法也能实现非线性学习**：  
   NCN 在前向传播中完全去除浮点乘法，仅通过加法、最小值操作和符号逻辑即可完成 XOR 和 Majority 这类非线性可分任务的学习，并达到 **100% 准确率**。

2. **生物启发机制有效支持深度学习**：  
   将突触的“离子通道限流”和“神经递质调控”抽象为 Channel Width 与 Neurotransmitter Bypass，成功模拟了生物系统的高效信号控制机制。

3. **硬件友好性极高**：  
   所有操作均可映射为简单的数字电路（比较器、多路复用器、加法器），特别适用于低功耗边缘AI芯片和 neuromorphic hardware 设计。

4. **方差稳定机制有效**：  
   引入 $\frac{1}{\sqrt{d}}$ 归一化因子防止信号爆炸，模仿生物中的 homeostatic plasticity，避免复杂初始化策略。

### 方法的局限性
1. **尚处概念验证阶段**：  
   实验仅在极小规模逻辑函数上验证，尚未扩展到图像识别（如MNIST/CIFAR）等实际任务。

2. **训练阶段仍依赖乘法**：  
   当前使用标准 BP 算法更新参数，反向传播中仍有乘法操作。作者承认需进一步开发 multiplication-free training（如 SignSGD 类方法）。

3. **缺乏消融实验与能效实测**：  
   未验证各组件必要性；也未在 FPGA/ASIC 上测量真实功耗，仅为理论推导。

4. **扩展性待验证**：  
   是否能在深层网络中保持性能、如何适配卷积结构等问题尚未探讨。

### 未来工作方向
作者明确提出三个战略方向：

1. **实现端到端无乘法训练**：  
   探索基于 SignSGD 等优化器，使训练过程也摆脱乘法依赖。

2. **扩展至深网络与真实数据集**：  
   在 MNIST、CIFAR-10 上测试 NCN 的表示学习能力，探索其在 CV/NLP 中的应用潜力。

3. **面向神经形态硬件协同设计**：  
   将 NCN 映射到 FPGA 和定制 ASIC，实测推理延迟与功耗，推动下一代低功耗 AI 芯片发展。

---

> 🔚 **总结一句话**：  
> Neuro-Channel Networks (NCN) 提出了一种受生物突触启发的全新神经架构，首次证明**无需任何乘法操作也能通过标准BP训练解决非线性问题**，为未来超低功耗、生物可解释的人工智能系统提供了可行路径。

</details>

---

### 14. [ShrimpXNet: A Transfer Learning Framework for Shrimp Disease Classification with Augmented Regularization, Adversarial Training, and Explainable AI](https://arxiv.org/abs/2601.00832)

**Authors**: Israk Hasan Jone, D. M. Rafiun Bin Masud, Promit Sarker, Sayed Fuad Al Labib, Nazmul Islam, Farhad Billah  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.00832v1  

#### Abstract
Shrimp is one of the most widely consumed aquatic species globally, valued for both its nutritional content and economic importance. Shrimp farming represents a significant source of income in many regions; however, like other forms of aquaculture, it is severely impacted by disease outbreaks. These...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ShrimpXNet: A Transfer Learning Framework for Shrimp Disease Classification with Augmented Regularization, Adversarial Training, and Explainable AI

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- 针对**虾类养殖中疾病爆发导致高死亡率和经济损失**的问题，尤其是如白点综合征病毒（WSSV）和黑鳃病（BG）等常见疾病的快速、准确诊断难题。
- 传统诊断方法依赖人工观察，效率低、主观性强，难以在小规模养殖户中普及，亟需自动化、可扩展的智能检测方案。

### 🚀 提出的新方法与创新思路
本研究提出了 **ShrimpXNet** —— 一个基于深度学习的端到端虾病分类框架，其核心创新包括：

1. **多技术融合的鲁棒性增强策略**：
   - 引入 **Adversarial Training（对抗训练）** 使用 Fast Gradient Sign Method (FGSM) 提升模型对扰动输入的鲁棒性。
   - 应用 **Augmented Regularization（增强正则化）** 技术，结合 **CutMix 和 MixUp** 数据增强方法，缓解过拟合并提升泛化能力。

2. **可解释性AI（Explainable AI, XAI）集成**：
   - 首次在虾病分类任务中系统应用 **Grad-CAM、Grad-CAM++ 和 XGrad-CAM** 可视化技术，揭示模型关注区域，增强决策透明度，便于农民和专家理解与信任。

3. **全流程优化设计**：
   - 在预处理阶段采用 `rembg` 工具去除图像背景，减少无关干扰；
   - 利用 **Transfer Learning + Fine-tuning** 策略，在多个预训练模型上进行微调，并通过网格搜索确定最优解冻层数；
   - 构建轻量级 CNN 分类头，适配移动端部署需求。

### 🔍 相比现有方法的优势
| 对比维度 | 本文方法（ShrimpXNet） | 其他研究 |
|--------|----------------------|---------|
| 疾病覆盖范围 | 四类：Healthy, BG, WSSV, WSSV_BG | 多为单病种（如仅WSSV）或非病理特征（如色素沉着） |
| 准确率 | **96.88%**（ConvNeXt-Tiny） | 最高97.22%，但无XAI支持 |
| 可解释性 | 支持多种Grad-CAM可视化 | 多数缺乏XAI机制 |
| 鲁棒性 | 包含FGSM对抗训练、CutMix/MixUp增强 | 多数未考虑噪声或复杂背景影响 |
| 移动兼容性 | 轻量化设计，适合现场使用 | 部分模型计算开销大 |

> ✅ **优势总结**：ShrimpXNet 是首个将 **对抗训练、增强正则化与可解释AI** 综合应用于虾病分类的研究，实现了高精度、强鲁棒性和高可信度的统一。

---

## 2. 核心实验方法和设置

### 📁 数据集
- 名称来源：公开数据集 [ShrimpDiseaseBD](https://doi.org/10.1016/j.dib.2025.111553)（来自 Mendeley）
- 图像总数：**1,149 张超声图像**
- 分类类别（4类）：
  - Healthy（健康）
  - Black Gill (BG)
  - White Spot Syndrome Virus (WSSV)
  - Co-infection: WSSV_BG（复合感染）
- 数据划分：
  - 训练集：70%（804张）
  - 验证集：15%（172张）
  - 测试集：15%（173张）

### ⚙️ 实验设置
- **预处理流程**：
  1. 使用 `rembg` 删除图像背景（见 Fig. 3）
  2. Keras 图像管道标准化处理（归一化、尺寸调整至 224×224）
- **模型选择（6个预训练CNN）**：
  - ResNet50
  - EfficientNet
  - DenseNet201
  - MobileNetV3
  - ConvNeXt-Tiny
  - Xception
- **训练配置（见 Table I）**：
  | 参数 | 设置 |
  |-----|------|
  | Optimizer | Adam |
  | Loss Function | SparseCategoricalCrossentropy |
  | Scheduler | StepLR (step_size=3) |
  | Epochs | 30 |
  | Batch Size | 128 |
  | Patience | 5（早停机制） |

- **评估指标**：
  - Accuracy, Precision, Recall, F1-Score
  - Confusion Matrix
  - ROC Curve & AUC
  - 99% Confidence Interval（基于1000次bootstrap迭代）

- **基线对比方法**：
  - 文献中的代表性方法（见 Table VI）：
    - YOLOv8-based detection [4]
    - DenseNet121 + Triplet Loss [6]
    - CNN for field diagnosis [7]
    - DICNN for WSSV only [10]

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table II & V）

| 模型 | Accuracy | Precision | Recall | F1-Score |
|------|----------|-----------|--------|----------|
| **ConvNeXt-Tiny** | **96.88%** | **97.10%** | **96.88%** | **96.89%** |
| EfficientNet | 92.71% | 93.03% | 92.71% | 92.55% |
| DenseNet201 | 90.10% | 90.23% | 90.10% | 90.07% |
| Xception | 89.06% | 89.71% | 89.06% | 88.76% |
| ResNet50 | 89.06% | 89.38% | 89.06% | 89.01% |
| MobileNetV3 | 55.21% | 39.80% | 55.21% | 44.54% |

> 💡 **最佳模型**：**ConvNeXt-Tiny** 明显优于其他模型，且训练过程稳定（见 Fig. 8），验证损失与训练损失高度一致，表明无明显过拟合。

### 🔍 细粒度分类表现（Table V）
ConvNeXt-Tiny 在各类别上的表现极为均衡：
- **WSSV 类召回率高达 100%** → 极少漏检致命病毒
- **BG 与 WSSV_BG 类精确率为 100%** → 减少误报
- 所有类别的 F1-score 均 > 90%
- 总体 macro avg 达到 **97%**

### 🤝 与其他工作的对比（Table VI）
| 方法 | Accuracy | XAI | Robustness Enhancements |
|------|----------|-----|------------------------|
| [4] YOLOv8 | 92.7% | ❌ | ❌ |
| [6] DenseNet | 92% | ❌ | ❌ |
| [7] CNN | 90.02% | ❌ | ❌ |
| [10] DICNN | 97.22% | ❌ | ❌ |
| **Proposed (ConvNeXt-Tiny)** | **96.88%** | ✅ | ✅ |

> ✅ 尽管准确率略低于某单一研究（97.22%），但本文是唯一同时具备 **高精度 + 可解释性 + 鲁棒性增强** 的完整解决方案。

### 🔧 消融实验结果

#### （1）Augmented Regularization（Table III）
| MixUp α / CutMix α | Test Accuracy |
|--------------------|---------------|
| 0 / 0（原始） | **96.88%** |
| 0.2 / 0.3 | 95.42% |
| 0.3 / 0.5 | 93.61% |
| 0.4 / 0.7 | 88.95% |

> 🔎 结论：引入 MixUp/CutMix 后精度略有下降，但提升了模型泛化能力和抗噪性，属于合理权衡。

#### （2）Adversarial Training（Table IV）
| ε（FGSM扰动强度） | Test Accuracy |
|--------------------|---------------|
| 0（原始） | **96.88%** |
| 0.1 | 94.42% |
| 0.14 | 88.61% |
| 0.16 | 79.13% |
| 0.2 | 59.38% |

> ⚠️ 结论：随着扰动增大，性能下降明显，但在 ε=0.14 时仍保持 **88.61%** 的较高准确率，说明模型具有一定鲁棒性。

#### （3）Confidence Interval
- 经过 1000 次 bootstrap 迭代：
  - 平均准确率：**96.2%**
  - 标准差：0.0034
  - **99% CI: [0.953, 0.971]** → 表明结果具有统计显著性和稳定性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **ConvNeXt-Tiny 是最适合该任务的 backbone**，在精度、速度和资源消耗之间取得良好平衡。
2. **背景移除 + 对抗训练 + 增强正则化** 显著提升了模型在真实场景下的鲁棒性。
3. **Grad-CAM 系列可视化工具有效揭示了模型注意力集中在病变部位**（如鳃部、外壳斑点），增强了模型可信度。
4. 所提框架具备良好的 **移动端兼容性**，适用于一线养殖户现场使用。

### ⚠️ 局限性
1. 当前仅基于静态图像分析，尚未扩展到视频流实时监测。
2. 数据集规模相对较小（<1.2k），可能存在分布偏差。
3. 缺乏内部生理状态（如微生物组、水质参数）的多模态融合。
4. 对罕见病种或新型变异病毒的泛化能力有待验证。

### 🔮 未来工作方向
1. **Real-time Video Monitoring**：构建动态监控系统，实现连续健康追踪。
2. **Multimodal Integration**：融合水质传感器、微生物组数据与图像信息，提升预测全面性。
3. **Edge Deployment Optimization**：进一步压缩模型（如蒸馏、量化），适配低端设备。
4. **跨地域泛化研究**：在不同国家/气候区的养殖场中验证模型适应性。
5. **主动学习机制**：允许模型在不确定时请求人工标注，持续迭代升级。

---

## ✅ 总结
**ShrimpXNet** 成功构建了一个面向实际应用的虾病智能诊断系统，不仅达到了 **96.88% 的高准确率**，更通过 **对抗训练、增强正则化和可解释AI** 实现了“看得懂、信得过、用得起”的目标。它为水产养殖智能化提供了可复制的技术范式，有望在全球范围内推动可持续渔业发展。

</details>

---

### 15. [A New Benchmark for the Appropriate Evaluation of RTL Code Optimization](https://arxiv.org/abs/2601.01765)

**Authors**: Yao Lu, Shang Liu, Hangan Zhou, Wenji Fang, Qijun Zhang, Zhiyao Xie  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.01765v1  

#### Abstract
The rapid progress of artificial intelligence increasingly relies on efficient integrated circuit (IC) design. Recent studies have explored the use of large language models (LLMs) for generating Register Transfer Level (RTL) code, but existing benchmarks mainly evaluate syntactic correctness rather ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《A New Benchmark for the Appropriate Evaluation of RTL Code Optimization》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于大语言模型（LLM）的 RTL 代码生成研究缺乏对**设计质量优化**（PPA：Power, Performance, Area）的有效评估。现有的基准（如 [26]）主要关注语法正确性和功能等价性，而忽视了以下关键问题：
- **不切实际的优化案例**：许多“次优”RTL代码存在人为构造的冗余操作（如 `s1 + 0` 或 `s1 * 1`），在工业级综合工具（如 Synopsys DC）下会被轻易消除，导致评估结果失真。
- **评估流程薄弱**：依赖弱综合工具（如 Yosys），无法反映真实芯片设计流程中的复杂权衡。
- **评估维度单一**：仅关注面积，忽略功耗、时序（WNS/TNS）等关键指标。

这使得现有基准难以准确衡量LLM在**真正有意义的硬件优化能力**上的表现。

---

### 🚀 提出的新方法与创新点
本文提出 **RTL-OPT** —— 一个专为评估 LLM 在 RTL 代码优化方面能力而设计的新基准。

#### 主要创新包括：
1. **高质量、现实性强的手工设计任务集**
   - 包含 **36 个手工编写**的 RTL 设计任务，涵盖组合逻辑、流水线数据路径、有限状态机（FSM）、存储器接口等多种类型。
   - 每个任务提供一对 RTL 代码：
     - **Suboptimal Version**：功能正确但故意遗漏行业标准优化机会的设计。
     - **Optimized Version**：由专家手工优化的“黄金参考”，体现真实有效的优化模式。

2. **真实且多样化的优化模式**
   所有优化均基于**工业实践**中被证明有效、且能抵抗先进综合工具自动优化的模式，包括：
   - Bit-width Optimization
   - Precomputation & LUT Conversion
   - Operator Strength Reduction
   - Control Simplification
   - Resource Sharing
   - State Encoding Optimization

3. **集成化自动化评估框架**
   提供完整的端到端评估流水线，支持：
   - 使用商业工具（Synopsys DC）和开源工具（Yosys）进行综合
   - 功能等价性验证（Formality）
   - 动态仿真验证（VCS）
   - 全面的 PPA 报告生成（Power, WNS, TNS, Cells, Area）

4. **多配置合成评估机制**
   在不同综合模式（`compile`, `compile_ultra`）和时钟约束（1ns, 0.1ns）下测试，确保评估结果的鲁棒性和可比性。

---

### 🔍 相比现有方法的优势
| 维度 | 现有基准 [26] | RTL-OPT（本文） |
|------|----------------|------------------|
| **设计真实性** | 过度人为构造，不反映实际问题 | 基于真实优化场景，具有实用价值 |
| **综合工具强度** | 多用 Yosys（弱优化） | 支持 Synopsys DC（工业级强优化） |
| **评估指标全面性** | 主要看面积 | 完整 PPA 指标（功耗、性能、面积） |
| **优化有效性** | 很多“优化”被DC消除 | 优化效果在DC下仍显著可见 |
| **评估可靠性** | 易产生误导性结论 | 结果稳定可靠，符合预期 |

> ✅ **核心优势总结**：RTL-OPT 能更真实、公平、全面地评估 LLM 在 RTL 优化任务中的实际能力，避免因评估流程不当导致的“虚假优越”。

---

## 2. 核心实验方法和设置

### 📦 数据集
- **RTL-OPT Benchmark**：包含 36 对 RTL 模块（Verilog），覆盖多种电路类型。
- **对比基准**：复现并评估了先前工作 [26] 中提供的 43 对 RTL 代码（其中成功综合 43 对）。
- 所有代码均已开源，并附带标准单元库（Nangate45）、脚本、网表和报告。

---

### ⚙️ 实验设置
#### 综合工具与流程
- **主要综合工具**：Synopsys Design Compiler (DC)
  - 模式：`compile` 和 `compile_ultra`
  - 时钟周期：1ns（宽松）、0.1ns（严格）
- **辅助工具**：
  - Yosys（用于对比）
  - Formality（形式等价验证）
  - VCS（动态功能验证）
- **技术库**：Nangate45 开源标准单元库

#### 评估指标
| 类别 | 指标 |
|------|------|
| **功能性** | 语法正确性、功能等价性（Formality/VCS） |
| **PPA 质量** | - **Power**：总功耗（动态 + 泄漏）<br>- **Performance**：WNS（最差负松弛）、TNS（总负松弛）<br>- **Area**：Cell 数量、硅片面积（μm²） |
| **比较方式** | 将 LLM 生成的优化代码与 suboptimal 和 optimized 版本对比，判断是否“更好”、“相同”、“更差”或“存在权衡” |

---

### 🆚 基线方法对比
- **人类专家优化结果**：作为黄金参考（Golden Reference）
- **现有 LLM 方法**：
  - GPT-4.0
  - RTLRewriter 提出的模型 [26]
  - SymRTLO [23]
- **本文测试的 LLMs**：
  - GPT-4o-mini
  - Gemini-2.5
  - Deepseek V3
  - Deepseek R1

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & Table 3）

#### ✅ RTL-OPT 自身有效性验证（Table 1）
在 `DC compile_ultra, clk=1ns` 下，36 对设计中有：
- **35 对**：优化版本明显优于次优版本（Better）
- **1 对**：两者无差异（Same）
- **0 对**：优化版本反而更差

> 👉 表明 RTL-OPT 中的优化是**真实有效且不可被综合工具抹除**的。

#### ❌ 现有基准 [26] 的问题暴露（Table 1）
在同一条件下（DC compile_ultra）：
- 仅 **13 / 43** 的人工优化版本优于原版
- 多数“优化”被 DC 消除 → **评估不可靠**

#### 📈 平均 PPA 改进（Table 3）
| 指标 | Suboptimal (DC) | Optimized (DC) | 改进率 |
|------|------------------|----------------|--------|
| Power | 1226.8 mW | 901.8 mW | ↓ 36.0% |
| Cells | 1337.4 | 1047.7 | ↓ 27.6% |
| Area | 14.0 mm² | 9.1 mm² | ↓ 35.0% |
| WNS/TNS | 0.0 / 0.0 | 0.0 / 0.0 | 保持 |

> 👉 显著的 PPA 提升，说明优化空间真实存在。

---

### 🤖 LLM 性能对比（Figure 3 & Table 4）

| LLM 模型 | 语法正确率 | 功能正确率 | PPA 优于 suboptimal | PPA 超过 human optimized |
|---------|------------|------------|---------------------|----------------------------|
| GPT-4o-mini | 97.2% | 75.0% | 19.4% | ~2.2% |
| Gemini-2.5 | ~97% | ~72% | ~27.8% | ~2.4% |
| Deepseek V3 | 100% | 69.4% | 23.3% | ~4.4% |
| **Deepseek R1** | 86.1% | 61.1% | **41.7%** | **13.9%** |

> ✅ **Deepseek R1 表现最强**：尽管功能正确率较低，但在 PPA 优化上显著领先，甚至在约 **5 个任务中超越人类专家**。

---

### 🔍 错误分析（消融性质发现）
通过对失败案例的手动检查，发现主要错误模式：
1. **控制逻辑错误**：布尔条件写错（如 comparator 判断失误）
2. **过度流水线化**：破坏原有延迟要求（尤其在 FSM 中）
3. **资源复用冲突**：寄存器重用导致数据陈旧（stale data）

> 👉 表明 LLM 的错误更多源于**语义理解偏差**而非语法错误。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **现有 RTL 优化基准不可靠**：多数“优化”在工业级综合工具下无效，易高估 LLM 能力。
2. **评估必须结合强综合流程**：使用 Synopsys DC 等商业工具才能揭示真实的优化差距。
3. **PPA 权衡普遍存在**：不能只看面积，需综合评估 Power、Timing、Area。
4. **RTL-OPT 是高质量基准**：其优化模式真实、可测量、抗综合干扰，适合推动领域发展。
5. **当前 LLM 仍有很大提升空间**：即使最好的模型（Deepseek R1）也仅有约 40% 成功率，且常引入功能错误。
6. **激进优化 ≠ 更好结果**：Deepseek R1 虽 PPA 最佳，但功能错误最多，显示“优化”与“正确”的权衡。

---

### ⚠️ 方法的局限性
- **规模限制**：36 个任务虽具代表性，但仍偏小，未覆盖所有复杂 IP（如 CPU 核心）。
- **依赖特定工艺库**：目前基于 Nangate45，迁移到先进工艺需重新校准。
- **人工构建成本高**：每个优化对均由专家手工设计，扩展难度较大。
- **未考虑物理实现影响**：仅评估综合后网表，未涉及布局布线（P&R）阶段的影响。

---

### 🔮 未来工作方向
1. **扩展更大规模的任务集**，覆盖更多 SoC 子模块。
2. **引入多轮迭代优化任务**，模拟工程师逐步调优过程。
3. **结合 P&R 阶段反馈**，构建闭环优化评估系统。
4. **开发专用 LLM 微调数据集**，基于 RTL-OPT 进行监督训练。
5. **探索自动化“退化”算法**，降低手工构造 suboptimal 版本的成本。

---

## 总结
RTL-OPT 是首个专注于**真实、可衡量、抗干扰的 RTL 代码优化评估**的基准，解决了当前领域评估不一致、结果不可靠的核心痛点。它不仅提供了高质量的数据集和完整工具链，还通过严谨实验揭示了现有 LLM 方法的真实水平，为未来 AI 辅助 IC 设计的发展奠定了坚实基础。

</details>

---

### 16. [FLOP-Efficient Training: Early Stopping Based on Test-Time Compute Awareness](https://arxiv.org/abs/2601.01332)

**Authors**: Hossam Amer, Maryam Dialameh, Hossein Rajabzadeh, Walid Ahmed, Weiwei Zhang, Yang Liu  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.01332v1  

#### Abstract
Scaling training compute, measured in FLOPs, has long been shown to improve the accuracy of large language models, yet training remains resource-intensive. Prior work shows that increasing test-time compute (TTC)-for example through iterative sampling-can allow smaller models to rival or surpass muc...

---

### 17. [Bridging the gap: A comparative exploration of Speech-LLM and end-to-end architecture for multilingual conversational ASR](https://arxiv.org/abs/2601.01461)

**Authors**: Yuxiang Mei, Dongxing Xu, Jiaen Liang, Yanhua Long  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.01461v1  

#### Abstract
The INTERSPEECH 2025 Challenge on Multilingual Conversational Speech Language Models (MLC-SLM) promotes multilingual conversational ASR with large language models (LLMs). Our previous SHNU-mASR system adopted a competitive parallel-speech-encoder architecture that integrated Whisper and mHuBERT with...

---

### 18. [Power-of-Two Quantization-Aware-Training (PoT-QAT) in Large Language Models (LLMs)](https://arxiv.org/abs/2601.02298)

**Authors**: Mahmoud Elgenedy  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02298v1  

#### Abstract
In Large Language Models (LLMs), the number of parameters has grown exponentially in the past few years, e.g., from 1.5 billion parameters in GPT-2 to 175 billion in GPT-3 to possibly more than trillion in higher versions. This raises a significant challenge for implementation, especially for Edge d...

---

### 19. [Making MoE based LLM inference resilient with Tarragon](https://arxiv.org/abs/2601.01310)

**Authors**: Songyu Zhang, Aaron Tam, Myungjin Lee, Shixiong Qi, K. K. Ramakrishnan  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.01310v1  

#### Abstract
Mixture-of-Experts (MoE) models are increasingly used to serve LLMs at scale, but failures become common as deployment scale grows. Existing systems exhibit poor failure resilience: even a single worker failure triggers a coarse-grained, service-wide restart, discarding accumulated progress and halt...

---

### 20. [GDRO: Group-level Reward Post-training Suitable for Diffusion Models](https://arxiv.org/abs/2601.02036)

**Authors**: Yiyang Wang, Xi Chen, Xiaogang Xu, Yu Liu, Hengshuang Zhao  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02036v1  

#### Abstract
Recent advancements adopt online reinforcement learning (RL) from LLMs to text-to-image rectified flow diffusion models for reward alignment. The use of group-level rewards successfully aligns the model with the targeted reward. However, it faces challenges including low efficiency, dependency on st...

---

### 21. [A Differentiable Adversarial Framework for Task-Aware Data Subsampling](https://arxiv.org/abs/2601.02081)

**Authors**: Jiacheng Lyu, Bihua Bao  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02081v1  

#### Abstract
The proliferation of large-scale datasets poses a major computational challenge to model training. The traditional data subsampling method works as a static, task independent preprocessing step which usually discards information that is critical to downstream prediction. In this paper, we introduces...

---

### 22. [ELLA: Efficient Lifelong Learning for Adapters in Large Language Models](https://arxiv.org/abs/2601.02232)

**Authors**: Shristi Das Biswas, Yue Zhang, Anwesan Pal, Radhika Bhargava, Kaushik Roy  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02232v1  

#### Abstract
Large Language Models (LLMs) suffer severe catastrophic forgetting when adapted sequentially to new tasks in a continual learning (CL) setting. Existing approaches are fundamentally limited: replay-based methods are impractical and privacy-violating, while strict orthogonality-based methods collapse...

---

### 23. [MindChat: A Privacy-preserving Large Language Model for Mental Health Support](https://arxiv.org/abs/2601.01993)

**Authors**: Dong Xue, Jicheng Tu, Ming Wang, Xin Yan, Fangzhou Liu, Jie Hu  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.01993v1  

#### Abstract
Large language models (LLMs) have shown promise for mental health support, yet training such models is constrained by the scarcity and sensitivity of real counseling dialogues. In this article, we present MindChat, a privacy-preserving LLM for mental health support, together with MindCorpus, a synth...

---

### 24. [DHI: Leveraging Diverse Hallucination Induction for Enhanced Contrastive Factuality Control in Large Language Models](https://arxiv.org/abs/2601.01156)

**Authors**: Jiani Guo, Xiangke Zeng, Jie Wu, Zuchao Li  
**Category**: cs.CL  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.01156v1  

#### Abstract
Large language models (LLMs) frequently produce inaccurate or fabricated information, known as "hallucinations," which compromises their reliability. Existing approaches often train an "Evil LLM" to deliberately generate hallucinations on curated datasets, using these induced hallucinations to guide...

---

### 25. [A Multi-Port Concurrent Communication Model for handling Compute Intensive Tasks on Distributed Satellite System Constellations](https://arxiv.org/abs/2601.01031)

**Authors**: Bharadwaj Veeravalli  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.01031v1  

#### Abstract
We develop an integrated Multi-Port Concurrent Communication Divisible Load Theory (MPCC-DLT) framework for relay-centric distributed satellite systems (DSS), capturing concurrent data dissemination, parallel computation, and result return under heterogeneous onboard processing and inter-satellite l...

---

### 26. [pMSz: A Distributed Parallel Algorithm for Correcting Extrema and Morse Smale Segmentations in Lossy Compression](https://arxiv.org/abs/2601.01787)

**Authors**: Yuxiao Li, Mingze Xia, Xin Liang, Bei Wang, Robert Underwood, Sheng Di, Hemant Sharma, Dishant Beniwal, Franck Cappello, Hanqi Guo  
**Category**: cs.DC  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.01787v1  

#### Abstract
Lossy compression, widely used by scientists to reduce data from simulations, experiments, and observations, can distort features of interest even under bounded error. Such distortions may compromise downstream analyses and lead to incorrect scientific conclusions in applications such as combustion ...

---

### 27. [Universal Battery Degradation Forecasting Driven by Foundation Model Across Diverse Chemistries and Conditions](https://arxiv.org/abs/2601.00862)

**Authors**: Joey Chan, Huan Wang, Haoyu Pan, Wei Wu, Zirong Wang, Zhen Chen, Ershun Pan, Min Xie, Lifeng Xi  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.00862v1  

#### Abstract
Accurate forecasting of battery capacity fade is essential for the safety, reliability, and long-term efficiency of energy storage systems. However, the strong heterogeneity across cell chemistries, form factors, and operating conditions makes it difficult to build a single model that generalizes be...

---

### 28. [Enhanced Data-Driven Product Development via Gradient Based Optimization and Conformalized Monte Carlo Dropout Uncertainty Estimation](https://arxiv.org/abs/2601.00932)

**Authors**: Andrea Thomas Nava, Lijo Johny, Fabio Azzalini, Johannes Schneider, Arianna Casanova  
**Category**: cs.LG  
**Published**: 2026-01-06  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.00932v1  

#### Abstract
Data-Driven Product Development (DDPD) leverages data to learn the relationship between product design specifications and resulting properties. To discover improved designs, we train a neural network on past experiments and apply Projected Gradient Descent to identify optimal input features that max...

---

### 29. [Counterfactual Self-Questioning for Stable Policy Optimization in Language Models](https://arxiv.org/abs/2601.00885)

**Authors**: Mandar Parab  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00885v1  

#### Abstract
Recent work on language model self-improvement shows that models can refine their own reasoning through reflection, verification, debate, or self-generated rewards. However, most existing approaches rely on external critics, learned reward models, or ensemble sampling, which increases complexity and...

---

### 30. [ElecTwit: A Framework for Studying Persuasion in Multi-Agent Social Systems](https://arxiv.org/abs/2601.00994)

**Authors**: Michael Bao  
**Category**: cs.AI  
**Published**: 2026-01-06  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00994v1  

#### Abstract
This paper introduces ElecTwit, a simulation framework designed to study persuasion within multi-agent systems, specifically emulating the interactions on social media platforms during a political election. By grounding our experiments in a realistic environment, we aimed to overcome the limitations...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
