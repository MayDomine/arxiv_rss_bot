# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-11 06:47:43 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [LLM-CoOpt: A Co-Design and Optimization Framework for Efficient LLM Inference on Heterogeneous Platforms](https://arxiv.org/abs/2602.09323)

**Authors**: Jie Kong, Wei Wang, Jiehan Zhou, Chen Yu  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 14.5  
**Type**: new  
**ArXiv ID**: 2602.09323v1  

#### Abstract
Major challenges in LLMs inference remain frequent memory bandwidth bottlenecks, computational redundancy, and inefficiencies in long-sequence processing. To address these issues, we propose LLM-CoOpt, a comprehensive algorithmhardware co-design framework aimed at improving both throughput and laten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-CoOpt: A Co-Design and Optimization Framework for Efficient LLM Inference on Heterogeneous Platforms

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在异构平台上的推理面临三大核心挑战：
- **KV Cache 内存瓶颈**：自回归解码过程中，Key-Value 缓存随序列长度线性增长，导致内存占用高、带宽压力大；
- **Multi-Head Attention 的计算冗余**：每个注意力头独立生成 K/V 向量，造成重复计算和硬件资源利用率低；
- **长序列处理效率低下**：Self-Attention 具有 $O(T^2)$ 复杂度，且传统 PagedAttention 存在内存碎片化和同步开销。

### 🚀 提出的新方法与创新思路
作者提出 **LLM-CoOpt** —— 一种面向异构平台的算法-硬件协同优化框架，集成三项关键技术：

#### (1) **Opt-KV**：动态 KV Cache 优化
- 在写入阶段跳过无效/冗余 token 的 KV 缓存；
- 引入 **FP8 量化** 压缩存储，减少内存占用；
- 支持推理时按需反量化（on-the-fly dequantization），兼顾精度与效率。

#### (2) **Opt-GQA**：轻量级 Grouped-Query Attention
- 将 Query Heads 分组，每组共享一组 Key-Value Heads；
- 减少 K/V 投影计算量，提升并行效率；
- 动态适配不同输入分布和硬件并行能力，优于固定分组策略。

#### (3) **Opt-Pa**：优化的 Paged Attention 设计
- 采用两步策略：先对长序列进行逻辑分块（LS segmentation），再通过懒加载机制仅加载有效块；
- 利用 **shared-memory reduction** 替代 warp-level reduction，降低 Softmax 同步开销；
- 显著缓解内存碎片和带宽饱和问题。

### 🔍 相比现有方法的优势
| 方向 | 现有方法局限 | LLM-CoOpt 改进 |
|------|---------------|----------------|
| KV Cache | 静态量化不适应动态范围；FP8 不稳定 | 动态过滤 + FP8 安全压缩 |
| GQA | 固定分组无法适应硬件/输入变化 | 可配置分组，提升灵活性与资源利用率 |
| PagedAttention | 内存碎片严重，同步开销高 | 懒加载 + 块级 Softmax 优化 |

> ✅ **整体优势**：实现了 **throughput 提升、latency 下降、accuracy 保持甚至略有上升** 的三重收益。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **吞吐量评估**：`ShareGPT_V3_unfiltered_cleaned_split` 数据集  
  - 包含约 35,240 条真实对话样本，覆盖问答、闲聊、知识咨询等场景；
  - 总数据量 ~178 MB。
- **准确率评估**：`ARC` 数据集（AI2 Reasoning Challenge）
  - 分为两个子集：
    - **ARC_C**（Challenge Set）：7,787 道小学科学难题，需深层推理；
    - **ARC_E**（Easy Set）：相对简单的问题集合。

### ⚙️ 实验设置
- **硬件平台**：DCU Z100 异构平台
  - L2 Cache: ~4MB
  - 内存带宽: 512 GB/s (GDDR6)
  - 支持 FP16 / 模拟 FP8（via INT8）
  - Wavefront Size: 64，支持 SIMD 并行
- **模型**：多个 GPTQ 量化版本的 LLaMa 系列模型
  - `LLaMa-7B-GPTQ`, `LLaMa2-7B-GPTQ`
  - `LLaMa-13B-GPTQ`, `LLaMa2-13B-GPTQ`
  - `LLaMa-Pro-8B-GPTQ`

### 📊 评估指标
| 指标 | 定义 | 公式 |
|------|------|-------|
| **Inference Latency** | 单次任务平均响应延迟 | $\text{Latency} = \frac{1}{N}\sum_{i=1}^{N} \text{latency}_i$ |
| **Generation Throughput** | 每秒生成 token 数 | $\text{Throughput} = \frac{\text{Total Generated Tokens}}{\text{Generation Time}}$ |
| **Inference Accuracy** | 正确预测比例 | $\text{Accuracy} = \frac{N_{\text{correct}}}{N_{\text{total}}} \times 100\%$ |

### 🔁 基线方法对比
- **Baseline**：原始 vLLM 推理引擎（未优化版本）
- 对比项包括：
  - 单独启用 Opt-KV / Opt-GQA / Opt-Pa
  - 组合启用 LLM-CoOpt（三者联合）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（以 LLaMa-13B-GPTQ 为例）

| 指标 | Baseline (Original) | LLM-CoOpt | 提升幅度 |
|------|---------------------|-----------|----------|
| **Latency** | 基准值 | ↓ 最多 **16.79%** | 显著下降 |
| **Throughput** | 基准值 | ↑ 最多 **13.43%** | 明显提升 |

> 注：所有模型均观察到一致趋势，尤其在大模型上增益更显著。

### 📊 详细对比结果（来自 Fig. 6 & Fig. 7）

#### 推理延迟降低（↓ 越好）
| Model | Latency Reduction |
|-------|--------------------|
| LLaMa-7B-GPTQ | 5.59% |
| LLaMa2-7B-GPTQ | 5.48% |
| LLaMa-13B-GPTQ | **6.75%** |
| LLaMa2-13B-GPTQ | 6.18% |
| LLaMa-Pro-8B-GPTQ | 4.82% |

#### 生成吞吐提升（↑ 越好）
| Model | Throughput Increase |
|-------|----------------------|
| LLaMa-7B-GPTQ | 7.20% |
| LLaMa2-7B-GPTQ | 6.13% |
| LLaMa-13B-GPTQ | **12.13%** |
| LLaMa2-13B-GPTQ | 10.85% |
| LLaMa-Pro-8B-GPTQ | 5.72% |

> 💡 **最大收益出现在 LLaMa-13B-GPTQ 上**，说明该框架对大规模模型更具优化潜力。

### 🔍 消融实验分析（Ablation Study）
虽然文中未明确列出消融表格，但从各模块单独测试可推断：
- **Opt-KV**：主要贡献于内存节省和读写效率提升；
- **Opt-GQA**：显著降低计算负载，提高 head 利用率；
- **Opt-Pa**：关键改善长序列下的内存局部性和同步效率；
- **三者协同**：表现出明显 **synergistic effect（协同效应）**，综合性能优于任一单一优化。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM-CoOpt 显著提升了异构平台上 LLM 推理的整体性能**：
   - 最高实现 **13.43% 的吞吐提升** 和 **16.79% 的延迟下降**；
   - 在多种 LLaMa 架构中表现稳健，具备良好的泛化性。

2. **精度几乎无损，部分情况下反而提升**：
   - 在 **ARC_C** 上多数模型 accuracy 略有上升（如 LLaMa-13B 从 39.66% → 40.01%）；
   - 在 **ARC_E** 上所有模型 accuracy 均小幅提升（如 LLaMa-Pro-8B 从 42.15% → 42.50%）；
   - 表明优化未牺牲语义理解能力，甚至可能因更高效的数据流动带来稳定性增强。

3. **Opt-KV + FP8 是可行且高效的内存压缩路径**：
   - 成功将 FP8 应用于 KV Cache，在保证数值稳定的前提下大幅减小内存占用；
   - 为未来低比特推理系统提供实践参考。

4. **Opt-Pa 有效缓解了长序列推理中的内存碎片与同步瓶颈**：
   - 通过 lazy loading 和 block-wise reduction 显著提升 memory locality；
   - 特别适合处理超长上下文任务（如文档摘要、代码生成）。

### ⚠️ 局限性
- 当前实验集中在 **GPTQ 量化模型**，尚未验证在其他量化方案（如 AWQ、INT4）下的兼容性；
- 所有测试基于 **单设备 DCU 平台**，缺乏多卡或多节点扩展性验证；
- **FP8 模拟依赖 INT8 指令**，实际加速效果受限于硬件原生支持程度；
- 分组策略（Opt-GQA）仍为静态配置，未实现完全动态自适应。

### 🔮 未来工作方向
1. **拓展至多模态场景**：结合 dynamic batching 与 sparse computing，支持图像-文本联合推理；
2. **跨平台部署优化**：适配更多异构架构（如 NPU、TPU-like 设备）；
3. **全自动参数调优**：引入 RL 或 NAS 技术自动选择最优分组数、块大小等超参；
4. **端到端编译器集成**：将 LLM-CoOpt 思路融入 TVM、MLIR 等编译栈，实现自动化优化流水线。

---

> ✅ **总结一句话**：  
> **LLM-CoOpt 通过算法-硬件协同设计，成功解决了 LLM 在异构平台上的内存、计算与长序列瓶颈，在不损失 accuracy 的前提下显著提升了 throughput 与 latency，是迈向高效边缘/专用设备部署的重要一步。**

</details>

---

### 2. [Rollout-Training Co-Design for Efficient LLM-Based Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2602.09578)

**Authors**: Zhida Jiang, Zhaolong Xing, Jiawei Lu, Yipei Niu, Qingyuan Sang, Liangxu Zhang, Wenquan Dai, Junhua Shu, Jiaxing Wang, Qiangyu Pei, Qiong Chen, Xinyu Liu, Fangming Liu, Ai Han, Zhen Chen, Ke Zhang  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2602.09578v1  

#### Abstract
Despite algorithm-level innovations for multi-agent reinforcement learning (MARL), the underlying networked infrastructure for large-scale MARL training remains underexplored. Existing training frameworks primarily optimize for single-agent scenarios and fail to address the unique system-level chall...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rollout-Training Co-Design for Efficient LLM-Based Multi-Agent Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **Multi-Agent Reinforcement Learning (MARL)** 研究主要集中在算法层面（如通信协议、协调策略），而底层支持大规模训练的**网络化基础设施**仍不成熟。这导致三个系统级挑战严重制约训练效率：

- **Rollout-Training 同步障碍（Synchronization Barriers）**：由于部分 agent 响应时间极长（long-tail effect），整个训练必须等待最慢的 rollout 完成。
- **Rollout 负载不均衡（Load Imbalance）**：核心 agent 请求密集，辅助 agent 利用率低，造成请求排队和资源浪费。
- **训练资源利用率低下（Resource Underutilization）**：静态资源分配策略下，未激活 agent 占用计算与内存资源，无法动态释放。

主流框架（如 OpenRLHF、veRL）仅支持单 agent 场景；即使近期提出的 MARL 框架（如 MARTI）也采用共置架构（colocated architecture）和固定资源绑定，难以应对上述挑战。

---

### 提出了什么新方法或新思路
作者提出 **FlexMARL** —— 首个端到端（end-to-end）为大规模 LLM-based MARL 设计的训练框架，通过 **rollout 与 training 协同设计（co-design）** 实现系统级优化。其三大核心组件如下：

#### ✅ Joint Orchestrator（联合调度器）
- 引入 **experience store** 作为结构化存储模块，管理 rollout 与 training 之间的数据流。
- 构建基于微批次（micro-batch）的异步流水线，解耦梯度计算与参数更新，在消除同步阻塞的同时保持同步训练语义（strong consistency）。

#### ✅ Rollout Engine（并行采样引擎）
- 采用 **dependency-driven 并行采样机制**，实现跨查询（inter-query）与查询内（intra-query）轨迹生成的并发执行。
- 设计 **分层负载均衡（hierarchical load balancing）**：
  - **Intra-agent**：在单个 agent 内部动态扩缩 inference instances。
  - **Inter-agent**：跨 agent 迁移推理容量，缓解热点瓶颈。

#### ✅ Training Engine（按需训练引擎）
- 实现 **agent-centric 资源分配**：仅当 agent 需要训练时才动态绑定硬件资源。
- 引入高效的 **training state swap 机制**，利用统一的 `Set/Get API` 在设备内存（HBM）与主机内存间迁移模型权重与优化器状态，避免 OOM。

---

### 相比现有方法的优势
| 特性 | 通用 RL 框架（如 OpenRLHF） | 专用 MARL 框架（如 MARTI） | **FlexMARL（本文）** |
|------|-------------------------------|-----------------------------|------------------------|
| 多 agent 支持 | ❌ | ✅ | ✅ |
| 解耦部署（Disaggregation） | ✅ | ❌ | ✅ |
| 异步 Rollout-Training | ✅ | ⚠️（有限） | ✅（细粒度 micro-batch） |
| Rollout 负载均衡 | ❌ | ❌ | ✅（分层弹性扩缩） |
| Agent-Centric 资源分配 | ❌ | ❌ | ✅ |

> ✅ 表示支持，❌ 表示不支持，⚠️ 表示部分支持

FlexMARL 是首个从系统层面全面解决 MARL 训练中 **同步延迟、负载倾斜、资源闲置** 三大瓶颈的框架。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Merchant Assistant (MA) Dataset**  
  多 agent 协作完成电商店铺管理任务（销售分析、营销优化、售后处理）。使用 **Qwen2.5-14B** 模型作为 backbone，每个 agent 不共享参数。
  
- **Category Assistant (CA) Dataset**  
  负责订单查询、定价策略与库存建议。涉及两种模型规模：**Qwen2.5-14B 和 Qwen2.5-32B**，用于测试异构部署能力。

> 注：因商业保密原因，具体数据细节未公开。

---

### 实验设置
- **硬件平台**：48 节点集群，每节点配备 16 个商用 NPU（64GB 显存），通过 HCCS 互联。
- **后端实现**：
  - Rollout：集成 vLLM v0.9.1 实现低延迟推理。
  - Training：基于 DeepSpeed v0.16.9 + ZeRO-3。
- **关键配置**：
  - 并行采样：inter-query = 4, intra-query = 16
  - 响应长度上限：8192 tokens
  - Batch size / Micro-batch size：64 / 16
  - 负载均衡阈值 Δ：5
  - 优化算法：GRPO，学习率 1e-6，Adam 优化器

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **E2E Time** | 单样本平均端到端延迟（rollout + training + 其他） |
| **Speedup** | 相对于基线（MAS-RL）的速度提升倍数 |
| **Throughput** | 每秒生成 token 数（tps） |
| **Agent Load** | 各 agent 处理的 rollout 请求数量 |
| **Hardware Utilization Rate** | AI 核心活跃时间占比 |
| **State Swap Overhead** | 模型状态加载/卸载耗时 |

---

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **MAS-RL** | 将单 agent RL 框架直接迁移到多 agent 场景，采用共置架构与串行 rollout |
| **DistRL** | 使用解耦架构（disaggregated architecture），但无细粒度异步流水线 |
| **MARTI (ICLR 2026)** | 当前最先进的 MARL 框架，支持分布式训练与异步 rollout，但资源静态绑定 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2 和 Figure 7）

| Dataset | Framework | E2E Time | Speedup | Throughput (tps) |
|--------|-----------|----------|---------|------------------|
| MA | MAS-RL | 914.4s | 1.0× | 119.0 |
| MA | DistRL | 293.8s | 3.1× | 401.0 |
| MA | MARTI | 174.1s | 5.3× | 642.8 |
| MA | **FlexMARL** | **126.1s** | **7.3×** | **910.2** |
| CA | MAS-RL | 438.6s | 1.0× | 265.5 |
| CA | DistRL | 130.0s | 3.4× | 571.6 |
| CA | MARTI | 112.8s | 3.9× | 655.9 |
| CA | **FlexMARL** | **78.8s** | **5.6×** | **821.4** |

> ✅ **最高提速达 7.3×，吞吐量提升至 910.2 tps**

---

### 与基线方法的对比结果
- **相比 MAS-RL**：
  - 减少 rollout 延迟 **86%**，显著缓解 long-tail 效应。
  - 硬件利用率从 ~3.6% 提升至 **32.4%（MA 数据集）**。
- **相比 DistRL**：
  - 尽管都使用解耦架构，FlexMARL 通过 micro-batch 异步流水线隐藏尾部延迟，将训练时间从 155.9s 缩短至 **10.2s（MA 数据集）**。
- **相比 MARTI**：
  - 在相同条件下仍取得 **1.4× 速度提升**，主要得益于动态资源分配与更高效的负载均衡。

---

### 消融实验结果（Ablation Study，见 Table 3）
| Variant | E2E Time (MA) | Speedup | Throughput |
|--------|---------------|---------|------------|
| w/o balancing | 152.2s | 6.0× | 729.9 tps |
| w/o async | 256.2s | 3.6× | 444.0 tps |
| **Full FlexMARL** | **126.1s** | **7.3×** | **910.2 tps** |

> 🔍 发现：
- 移除 **hierarchical load balancing** 导致性能下降 19.8%，说明负载均衡对缓解热点至关重要。
- 移除 **micro-batch 异步流水线** 性能下降超过 100%，验证了其对消除 pipeline bubbles 的决定性作用。

---

### 状态交换开销（State Swap Overhead，见 Figure 11）
| Model Size | Swap-in Overhead | Swap-out Overhead |
|-----------|------------------|--------------------|
| 3B | ~0.5s | ~0.5s |
| 32B | ~3.8s | ~3.8s |

> ✅ 最大模型（32B）的状态交换总开销仅为 **~11s**，可被 rollout 延迟有效掩盖，不影响整体效率。

---

### 可扩展性测试（Scalability，见 Table 4）
| 配置 | E2E Time | Throughput |
|------|----------|-----------|
| 5×32B | 160.3s | 265.9 tps |
| 3×32B + 7×14B（异构） | 132.5s | 334.8 tps |
| 15×14B | **41.9s** | **754.2 tps** |

> ✅ FlexMARL 成功支持大规模异构部署（如混合 32B 与 14B 模型），而 MARTI 等框架会因跨节点资源放置失败导致 OOM。

---

## 4. 关键结论和发现

### 主要发现
1. **Rollout 与 Training 必须协同设计**：传统串行或粗粒度异步方式无法满足 LLM-based MARL 的高动态性需求。
2. **分层负载均衡至关重要**：agent 角色差异导致请求分布高度偏斜，需同时支持 intra-agent 与 inter-agent 的弹性扩缩。
3. **agent-centric 资源分配是提升利用率的关键**：静态绑定导致大量资源闲置，动态绑定 + state swap 可实现近似“serverless”式的高效调度。
4. **micro-batch 异步流水线可在保证一致性前提下最大化并行度**：通过梯度累积 + 统一更新，既隐藏尾部延迟又避免参数过期（parameter staleness）。

---

### 方法的局限性
- **依赖高性能互联网络（如 HCCS/RDMA）**：D2D 权重迁移与跨节点状态恢复需要低延迟、高带宽通信支持。
- **当前实现集中于 NPU 架构**：对 GPU 或其他加速器的支持尚未验证。
- **缺乏对 fault tolerance 的深入讨论**：大规模分布式环境下节点故障如何处理未详细说明。

---

### 未来工作方向
- 扩展至更多类型的 **heterogeneous agents**（如视觉、语音模态）。
- 探索 **自动化的 load balancing 策略**，替代人工设定阈值 Δ。
- 结合 **model compression 或 MoE 技术** 进一步降低 state swap 开销。
- 构建 **开放 benchmark suite** 以推动 MARL 系统研究标准化。

---

> 💡 **总结一句话**：  
> **FlexMARL 通过 rollout-training co-design，首次实现了高效、可扩展的大规模 LLM-based MARL 训练框架，在真实生产集群上实现了高达 7.3× 的端到端加速与 5.6× 的硬件利用率提升。**

</details>

---

### 3. [Training deep physical neural networks with local physical information bottleneck](https://arxiv.org/abs/2602.09569)

**Authors**: Hao Wang, Ziao Wang, Xiangpeng Liang, Han Zhao, Jianqi Hu, Junjie Jiang, Xing Fu, Jianshi Tang, Huaqiang Wu, Sylvain Gigan, Qiang Liu  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.09569v1  

#### Abstract
Deep learning has revolutionized modern society but faces growing energy and latency constraints. Deep physical neural networks (PNNs) are interconnected computing systems that directly exploit analog dynamics for energy-efficient, ultrafast AI execution. Realizing this potential, however, requires ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Training deep physical neural networks with local physical information bottleneck*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统深度神经网络（DNNs）依赖数字计算硬件，在扩展过程中面临**能耗高、延迟大、物理极限逼近**等瓶颈。物理神经网络（Physical Neural Networks, PNNs）利用模拟物理系统（如电子、光学、自旋器件）进行原位计算，具备高速、低功耗潜力，但其训练面临以下挑战：
- 物理系统通常**不可微分、难以建模、存在噪声与不确定性**
- 现有训练方法（如 hybrid training、DFA、PhyLL）依赖辅助数字模型、对比测量或全局反向传播，导致**效率低、适应性差、对硬件故障敏感**

### 🔧 提出的新方法：**Physical Information Bottleneck (PIB)**
PIB 是一种通用且高效的 PNN 训练框架，将**信息论**与**局部学习**相结合，核心思想如下：
- 将每个物理计算单元视为一个**信息通道**，通过优化局部目标函数 $ \mathcal{L}(Z^l, \beta) = I(Y;Z^l) - \beta I(X;Z^l) $ 来提取任务相关特征并压缩冗余输入信息。
- 使用**基于矩阵的信息度量**（matrix-based mutual information）替代传统互信息估计，解决高维空间中信息计算不可行的问题。
- 每个单元仅需本地输出测量、全局输入 $X$ 和标签 $Y$ 即可独立训练，无需端到端梯度回传。

### 🚀 相比现有方法的优势
| 维度 | PIB 的优势 |
|------|-----------|
| **兼容性** | 支持 isomorphic（如 memristor MVM）和 broken-isomorphism（如光学散射）两类物理系统 |
| **训练效率** | 无需 contrastive samples 或 auxiliary networks，减少内存占用和通信开销 |
| **鲁棒性** | 对硬件噪声、参数漂移、组件故障具有强适应能力 |
| **可扩展性** | 支持分布式并行训练，适用于地理分散的异构硬件资源 |
| **去数字化依赖** | 不依赖精确的数字仿真模型，缓解 simulation-reality gap |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 任务类型 | 数据集 | 描述 |
|--------|-------|------|
| 分类任务 | **MNIST**, **MNIST-1D**, **Fashion-MNIST** | 手写数字识别、简化一维版本、服饰图像分类 |
| 自监督学习 | Fashion-MNIST（无标签） | 验证 unsupervised variant of PIB 的表征学习能力 |
| 强化学习 | **CartPole-v1**（控制任务） | 测试在非平稳、高噪声信号下的适用性 |

### ⚙️ 实验设置与平台
#### （1）Memristor-based PNN（Isomorphic Unit）
- 平台：1T1R memristive crossbar 芯片（规模达 1024×128）
- 编码方式：输入为电压，权重为电导态
- 挑战：存在 conductance noise 和 ADC 故障（人为引入）

#### （2）Optics-based PNN（Broken-isomorphism Unit）
- 平台：利用多重光散射介质生成 speckle pattern
- 设备：商用 LightOn OPU 与自研 SLM 光学系统
- 特点：传输矩阵未知、不可微，构成“黑箱”变换

#### （3）训练流程
- **逐层级联训练**：先训练第 $l$ 层 → 部署至硬件 → 获取实际输出作为下一层输入
- **损失函数**：
  - 监督学习：$ \mathcal{L}_{\text{PIB}} = I(Y;Z^l) - \beta I(X;Z^l) $
  - 自监督：$ \mathcal{L} = I(Z_1;Z_2) - \beta[I(X_1;Z_1)+I(X_2;Z_2)] $，用于 view-invariant 表征学习
  - 强化学习：复合损失 $ \mathcal{C}_{\text{focal}} = \mathcal{C}_{\text{PIB}} + \lambda \mathcal{C}_{\text{TD}} $

### 📊 评估指标
| 指标 | 含义 |
|-----|------|
| Test Accuracy | 分类准确率（MNIST/Fashion-MNIST） |
| OOD Detection Rate | 对分布外样本的检测能力 |
| Noise Robustness | 在输入加入噪声后的性能保持 |
| Data Efficiency | 小样本情况下的学习表现 |
| Wall-clock Training Time | 实际训练时间（尤其关注并行加速效果） |
| Fault Adaptation | 硬件损坏后恢复性能的能力 |

### 🔁 基线方法对比
| 方法 | 类型 | 是否需要模型 | 是否支持本地训练 |
|-----|------|-------------|------------------|
| End-to-end Backpropagation (BP) | 数字仿真 | 是 | 否 |
| Physics-Aware Training (PAT) | Hybrid | 是 | 否 |
| Direct Feedback Alignment (DFA) | Model-free | 否 | 是（但性能较低） |
| Physical Local Learning (PhyLL) | Contrastive | 否 | 是（但需 contrastive samples） |
| **PIB (本文)** | **Information-theoretic + Local** | **否** | **是（无 contrastive 测量）** |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 实验场景 | 方法 | 性能指标 | 结果 |
|--------|------|---------|------|
| **MNIST（memristor）** | PIB（实验） | Test Accuracy | **96.3%** |
| | In Silico BP（部署后） | Test Accuracy | 从 96.4% ↓ 至 **94.0%** |
| **MNIST-1D（memristor）** | PIB（实验 vs 仿真） | Test Accuracy | 均接近 ~**90%**，无显著下降 |
| | In Silico BP（部署后） | Test Accuracy | 下降 **8.5%** |
| **Hardware Fault（ADC损坏）** | 初始状态 | MNIST Accuracy | **73.5%** |
| | PIB 自适应重训 | MNIST Accuracy | 恢复至 **>90.0%** |
| **Fashion-MNIST（optical）** | PIB + OPU | Test Accuracy | **86.4%**（binary encoding） |
| | PIB + SLM setup | Test Accuracy | **93.4%**（8-bit, subset） |
| **Reinforcement Learning（CartPole-v1）** | PIB + TD loss | Episode Return | 成功达到 **solved regime**（avg score > 475） |
| **Unsupervised Learning** | PIB（memristor） | Linear Probe Accuracy | 随训练迭代单调上升，UMAP 显示语义分离 |

### 🔍 与基线方法对比
在 **Figure 3C** 中对 generalization 进行全面比较：
- **Noise Robustness**：PIB 在噪声强度高达 6% 时仍保持 >80% 准确率，优于 PAT、DFA 和 PhyLL
- **Data Efficiency**：在小训练集上，PIB 表现更优，收敛更快
- **OOD Detection**：PIB 学得的表示更具判别性，OOD 检测率更高

### 🔤 消融实验（Ablation Study）
虽然未明确列出 ablation table，但从实验设计可推断关键验证：
- **是否使用 local objective**：成功实现无需全局 BP 的训练
- **是否依赖 transmission matrix**：在光学系统中完全避免获取传输矩阵
- **是否支持 fault tolerance**：ADC 故障实验证明动态适应能力
- **是否支持 parallel training**：跨北京-巴黎两地并行训练成功（Fig. 4C），验证去同步化可行性

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **PIB 是首个将信息瓶颈原理转化为主动训练目标并应用于真实物理系统的框架**，实现了理论到实践的跨越。
2. **局部性 + 信息压缩机制** 使得 PNN 能够在不依赖精确建模的情况下高效训练，并天然抵抗噪声与硬件缺陷。
3. **在 isomorphic 与 broken-isomorphism 系统中均取得高性能**，验证了方法的普适性。
4. **支持多种学习范式**：监督、自监督、强化学习均可统一于 PIB 框架下。
5. **首次实现跨地域异构硬件的并行训练**，展示了其在分布式智能计算中的潜力。

### ⚠️ 方法的局限性
- 当前仍依赖 **digital auto-differentiation** 来更新参数（即训练读出层或数字部分），尚未完全摆脱数字计算。
- 对于极度复杂的任务（如 ImageNet 规模），local optimization 可能无法媲美 end-to-end BP 的全局最优性。
- matrix-based mutual information 的计算假设可能在某些极端非线性系统中失效。

### 🔮 未来工作方向
1. **结合 surrogate gradient 方法**（如 DFA）进一步降低对数字微分的依赖，迈向全物理训练闭环。
2. 探索如何将物理系统的**固有归纳偏置**（inductive bias）融入 PIB 框架以提升性能。
3. 扩展至更大规模、更深的 PNN 架构，研究其**scaling laws**。
4. 应用于更多物理平台，如 **acoustic reservoirs, magnetic skyrmions, active matter** 等。
5. 推动 PIB 在边缘计算、低功耗 AI 加速器中的产业化应用。

---

> 💡 **总结一句话**：  
> **PIB 开创了一种基于信息论的、去中心化的、抗噪鲁棒的 PNN 训练新范式，为构建下一代高效、可扩展、面向物理世界的 AI 系统提供了坚实基础。**

</details>

---

### 4. [DLLM-Searcher: Adapting Diffusion Large Language Model for Search Agents](https://arxiv.org/abs/2602.07035)

**Authors**: Jiahao Zhao, Shaoxuan Xu, Zhongxiang Sun, Fengqi Zhu, Jingyang Ou, Yuling Shi, Chongxuan Li, Xiao Zhang, Jun Xu  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.07035v1  

#### Abstract
Recently, Diffusion Large Language Models (dLLMs) have demonstrated unique efficiency advantages, enabled by their inherently parallel decoding mechanism and flexible generation paradigm. Meanwhile, despite the rapid advancement of Search Agents, their practical deployment is constrained by a fundam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM-Searcher: Adapting Diffusion Large Language Models for Search Agents

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对当前 **Search Agent** 在实际部署中面临的两大核心挑战：

1. **Latency Challenge（延迟挑战）**：  
   在标准的 ReAct 范式下，LLM 需要串行执行“思考 → 工具调用 → 等待工具响应”流程。在等待外部工具返回期间，模型处于空闲状态，导致端到端推理延迟严重。

2. **Agent Ability Challenge（智能体能力挑战）**：  
   当前的 **dLLMs**（Diffusion Large Language Models）虽然具备并行解码优势，但在多步推理、工具调用格式遵循等方面表现较弱，难以胜任 Search Agent 的角色。

---

### **提出了什么新方法或新思路**

为解决上述问题，作者提出 **DLLM-Searcher**，一个专为 dLLM 设计的 Search Agent 优化框架，包含两个核心创新：

#### （1）**两阶段后训练管道（Two-stage Post-training Pipeline）**

- **Agentic SFT（Agentic Supervised Fine-Tuning）**  
  利用高性能教师模型（如 Doubao-Seed-1.8）生成高质量的多跳问答轨迹，筛选出格式正确、逻辑完整、答案正确的样本进行监督微调，提升 dLLM 对 `tool_call` 格式的遵循能力和基础检索推理能力。

- **Agentic VRPO（Agentic Variance-Reduced Preference Optimization）**  
  基于 SFT 模型生成多个轨迹，筛选出“一正一负”的偏好对（winner/loser），通过 **VRPO** 进一步优化模型的推理与检索行为，增强其鲁棒性。

> 特别设计了 **Agentic ELBO** 和 **Agentic Noising** 机制，确保训练过程中仅关注 `think` 和 `tool_call` 区域，避免 `tool_response` 内容泄露导致训练-推理不一致。

#### （2）**新型代理范式：P-ReAct（Parallel-Reasoning and Acting）**

- 一种无需额外训练的推理控制策略，利用 dLLM 的 **非自回归、块内双向注意力** 特性，实现“边等边想”。
- 核心机制：
  - **Token Pre-filling**：在生成块中预填充 `<tool_call>` 和 `</tool_call>` 边界标记。
  - **Confidence Biasing**：在解码时，对边界内的 token 增加置信度偏置，引导模型优先解码 `tool_call` 内容。

> 结果：模型先输出 `tool_call` 请求，立即触发搜索引擎，同时继续生成 `think` 内容，实现 **推理与等待的并行化**。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | P-ReAct 实现约 **15% 的推理加速**，显著降低端到端延迟。 |
| **性能** | 经过两阶段训练后，DLLM-Searcher 性能媲美主流基于 ARMs 的 Search Agent（如 R1Searcher）。 |
| **可控性** | P-ReAct 几乎以 **100% 成功率** 保证 `tool_call` 优先生成，控制精度远超 ARMs。 |
| **通用性** | 所提方法适用于任意支持块状生成的 dLLM（如 SDAR、LLaDA 等）。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **HotpotQA**：多跳问答基准，用于训练与测试。
- **2WikiMultiHopQA**：构造性多跳 QA 数据集。
- **Musique**：基于单跳组合的多跳 QA。
- **Bamboogle**：跨领域、高难度多跳 QA，作为 **out-of-domain** 测试集。

> - 训练集：从前三者各采样 2048 查询，共 3977 条高质量轨迹用于 Agentic SFT。
> - 测试集：每数据集取 500 开发样本（Bamboogle 全部 125 条）。

---

### **实验设置和评估指标**

#### **评估指标**

- **ACCR（Accuracy）**：预测答案是否包含黄金答案（exact match 不适用长文本输出）。
- **ACCL（LLM-as-Judge Accuracy）**：使用 **Doubao-Seed-1.8** 作为裁判模型判断答案正确性，更贴近人类判断。

#### **硬件与模型配置**

- **Backbone**：SDAR（Block Diffusion LM，8B 参数，块大小 128）。
- **检索工具**：Google Search（Top-10 结果）。
- **训练配置**：
  - SFT：学习率 1e-5，batch size 32，3 轮。
  - VRPO：学习率 5e-7，batch size 16，5 轮。
- **推理配置**：128 步去噪，温度 1.0，confidence bias = 0.5。

---

### **基线方法对比**

| 类别 | 基线方法 |
|------|----------|
| **传统 RAG** | SuRe, Selective-Context, Adaptive-RAG, IRCoT, Iter-RetGen, CR-Planner, ReARTeR |
| **ARM-based Search Agents** | Search-o1, Search-R1, WebSailor*, R1Searcher* |
| **dLLM-based Agents** | SDAR（原始）、Dream、LLaDA（均使用 ReAct 范式） |

> *WebSailor 和 R1Searcher 的结果经过适配处理以保持实验一致性。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| 方法 | HotpotQA (ACCR/ACCL) | 2Wiki (ACCR/ACCL) | Bamboogle (ACCR/ACCL) | Musique (ACCR/ACCL) | **Avg ACCR** |
|------|------------------------|--------------------|------------------------|----------------------|---------------|
| **R1Searcher*** | 58.0 / 62.2 | 59.6 / 63.4 | 66.4 / 68.8 | 28.2 / 31.4 | **53.1** |
| **DLLM-Searcher** | **60.4 / 62.4** | **69.8 / 64.6** | **68.8 / 69.6** | **29.0 / 29.8** | **57.0** ✅ |

> ✅ **DLLM-Searcher 在所有数据集上均超越 R1Searcher，平均 ACCR 提升近 4 个百分点。**

---

### **与基线方法的对比结果**

- **vs. 传统 RAG**：大幅领先（如比 ReARTeR 高 ~19% ACCR）。
- **vs. 原始 dLLMs**：
  - SDAR 原始模型几乎无法生成有效 `tool_call`（解析失败率 100%）。
  - Dream 和 LLaDA 表现极差（ACCR 多数低于 15%）。
- **vs. ARM-based Agents**：
  - 性能持平甚至反超（尤其在 2Wiki 和 Bamboogle 上）。
  - **唯一差距在 Musique**，可能因训练数据分布差异。

---

### **消融实验结果**

#### **RQ1: 两阶段后训练的有效性（Table 2）**

| 方法 | HotpotQA (ACCR) | 2Wiki (ACCR) | Bamboogle (ACCR) | Musique (ACCR) |
|------|------------------|--------------|------------------|----------------|
| **Agentic SFT** | 57.2 | 66.4 | 64.6 | 24.4 |
| **+ Agentic VRPO** | **60.4 (+3.2)** | **69.8 (+3.4)** | **68.8 (+4.2)** | **29.0 (+4.6)** |

> ✅ VRPO 带来稳定且显著的性能增益（+3~4.6%），验证其对推理质量的提升作用。

#### **RQ2: P-ReAct 的推理效率（Figure 3）**

| 数据集 | 推理时间减少 |
|--------|-------------|
| HotpotQA | 14.77% |
| 2Wiki | 21.00% |
| Bamboogle | 22.08% |
| Musique | 12.67% |
| **平均** | **≈15%** |

> ⚡ P-ReAct 显著降低端到端延迟，且 **准确率几乎无损**。

#### **RQ3: dLLM 的顺序自由生成优势（Figure 4）**

- 将 P-ReAct 应用于 **Qwen3 系列 ARMs**（8B/30B/235B）：
  - 即使修改 prompt 引导其先生成 `tool_call`，仍出现 **3%~13.3% 的性能下降**。
- 反观 **DLLM-Searcher**：
  - 在 HotpotQA 和 Musique 上 **甚至略有提升**。
  - 仅在 2Wiki 和 Bamboogle 上有轻微下降（<5%）。

> 🔍 说明 **dLLM 能在未显式解码 `think` 的情况下，利用双向注意力隐式完成推理**，而 ARMs 严重依赖 CoT 的显式表达。

---

## 4. 关键结论和发现

### **主要发现**

1. **dLLMs 完全可以胜任 Search Agent 角色**，只要通过针对性后训练（Agentic SFT + VRPO）弥补其推理与格式遵循短板。
2. **P-ReAct 是 dLLM 独有的高效范式**，利用其非自回归特性实现“**thinking while waiting**”，带来约 **15% 的推理加速**。
3. **顺序自由生成是 dLLM 的核心优势**：即使 `tool_call` 优先生成，其质量不受影响，得益于块内双向注意力的全局感知能力。
4. **DLLM-Searcher 性能媲美甚至超越主流 ARM-based Search Agents**，验证了 dLLM 作为高效 Agent Backbone 的潜力。

---

### **方法的局限性**

1. **依赖高质量训练数据**：Agentic SFT 依赖强教师模型生成轨迹，成本较高。
2. **泛化能力仍有提升空间**：在 Musique 上与 R1Searcher 仍有差距，可能需更多领域适配。
3. **P-ReAct 依赖特定架构**：仅适用于支持块状生成的 dLLM（如 BDLMs），不适用于纯扩散模型。
4. **工具生态限制**：目前仅集成 Google Search，复杂工具链（如 visit、code interpreter）尚未验证。

---

### **未来工作方向**

1. **扩展至多工具协同场景**：将 P-ReAct 推广至支持 `search`、`visit`、`code` 等多种工具的混合调用。
2. **探索更高效的偏好学习机制**：结合 RL 或过程奖励进一步优化推理路径。
3. **轻量化与部署优化**：结合 Fast-dLLM 等技术，推动 dLLM Search Agent 在边缘设备落地。
4. **构建 dLLM 原生 Agent 训练数据集**：摆脱对 ARM 教师模型的依赖，实现端到端 dLLM Agent 训练。

---

> **总结**：DLLM-Searcher 成功弥合了 **dLLM 的理论优势** 与 **Search Agent 实践需求** 之间的鸿沟，不仅提升了性能，更开创了一种 **并行化智能体推理范式**，为下一代高效、低延迟的 AI Agent 提供了全新路径。

</details>

---

### 5. [Effective MoE-based LLM Compression by Exploiting Heterogeneous Inter-Group Experts Routing Frequency and Information Density](https://arxiv.org/abs/2602.09316)

**Authors**: Zhendong Mi, Yixiao Chen, Pu Zhao, Xiaodong Yu, Hao Wang, Yanzhi Wang, Shaoyi Huang  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.09316v1  

#### Abstract
Mixture-of-Experts (MoE) based Large Language Models (LLMs) have achieved superior performance, yet the massive memory overhead caused by storing multiple expert networks severely hinders their practical deployment. Singular Value Decomposition (SVD)-based compression has emerged as a promising post...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Effective MoE-based LLM Compression by Exploiting Heterogeneous Inter-Group Experts Routing Frequency and Information Density

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **SVD 的 MoE 压缩方法**存在两大关键缺陷：
1. **统一秩分配（Uniform Rank Allocation）**：忽略专家之间显著的异质性利用模式，导致对频繁激活但信息密集的专家压缩不足，而对稀疏激活但关键的专家过度压缩甚至丢弃。
2. **残差被直接丢弃**：传统 SVD 方法仅保留主奇异值分量，忽略压缩残差（residual），造成不可忽视的信息损失。

这些问题在 **大规模 MoE LLMs**（如 Qwen3、DeepSeekMoE）中尤为严重，影响模型在低资源部署下的性能表现。

---

### 🚀 提出的新方法：RFID-MoE
作者提出 **RFID-MoE**（Routing Frequency and Information Density-aware MoE compression），一种自适应、高效的 MoE 压缩框架，核心创新如下：

#### （1）融合动态路由频率与信息密度的自适应秩分配
- **Routing Frequency（路由频率）**：通过在校准数据集上统计各专家的激活次数，衡量其“使用重要性”。
- **Information Density（信息密度）**：引入 **effective rank** 指标量化每个专家组的内在信息复杂度，避免因低频而误判为“不重要”。
- **融合策略**：设计加权融合公式 $ C_g = \xi \cdot E_g + (1-\xi) \cdot F_g $，其中 $ E_g $ 是归一化有效秩，$ F_g $ 是归一化路由频率，实现对高价值专家的优先保真。

> 🔍 **优势**：相比静态权重分析或纯频率驱动的方法，RFID-MoE 能识别“低频但高信息密度”的专家，防止关键能力退化。

#### （2）参数高效的残差重建机制（Residual Reconstruction）
- 不再丢弃 SVD 压缩后的残差 $ R = W - \hat{W} $。
- 引入一个低维可训练向量 $ \eta_g \in \mathbb{R}^a $（$ a \ll D $），并通过 **sparse orthogonal projection matrix $ P $** 将其映射回原始参数空间：
  $$
  \text{vec}(R) = P \eta_g
  $$
- $ P $ 构造为每行只有一个非零元素（one-hot 结构），且满足 $ P^T P = I_a $，保证几何不变性和优化稳定性。

> 💡 **优势**：以极小额外参数开销（约 3%）恢复关键丢失信息，显著提升压缩后模型性能。

---

### ✅ 相比现有方法的优势
| 方法 | 是否考虑异质性 | 是否利用残差 | 是否动态调整秩 |
|------|----------------|---------------|----------------|
| D²-MoE / MoBE | ❌ 统一或静态分配 | ❌ 丢弃残差 | ❌ |
| RS-MoE | ⭕ 部分考虑结构 | ⭕ 利用稀疏残差 | ❌ |
| **RFID-MoE (Ours)** | ✅ 显式建模频率+信息密度 | ✅ 参数高效重建 | ✅ 自适应分配 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

#### （1）校准数据集（用于收集路由频率）
- **WikiText-2**：1,024 个样本，序列长度 1,024
- **C4**：作为消融实验中的替代校准集

#### （2）评估基准
| 类型 | 数据集 | 任务 | 指标 |
|------|--------|------|------|
| **语言建模** | WikiText-2, PTB, C4 | Perplexity (↓) |
| **常识推理** | OpenBookQA, ARC-e, WinoGrande, HellaSwag, PIQA, MathQA | Zero-shot Accuracy (↑) |

所有评估均使用 **LM Evaluation Harness** 框架进行 zero-shot 测试。

---

### ⚙️ 实验设置

- **模型**：
  - Qwen3-30B-A3B-2507（128 experts/layer）
  - DeepSeekMoE-16B-Base（64 experts）
  - Qwen2-57B-A14B, Deepseek-v2-Lite-Chat, Ling-mini-2.0
- **压缩比例**：20%, 40%, 60%
- **压缩目标**：Up-projection 和 Gate-projection 矩阵；Down-projection 保持稠密
- **分组策略**：每 4 个专家矩阵拼接成一组进行共享基底分解
- **残差维度**：$ a = 3\% \times D $
- **硬件**：8×NVIDIA A100 GPUs

---

### 🆚 基线方法对比
| 方法 | 类型 | 简介 |
|------|------|------|
| **NAEE** | Pruning + Skipping | 后训练剪枝 + 动态跳过 |
| **MoE-I2** | Pruning + Low-rank | 专家间剪枝 + 专家内低秩分解 |
| **RS-MoE** | Shared + Sparse Residual | 共享低秩表示 + 稀疏残差 |
| **D²-MoE** | Shared + Unique Components | 分解为共享与独有部分 |
| **MoBE** | Adaptive Basis Sharing | 当前 SOTA 的基线方法 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 2 & 4）

#### 在 **Qwen3-30B-A3B-2507 @ 60% 压缩率**

| 指标 | Original | MoBE | D²-MoE | **RFID-MoE (Ours)** |
|------|----------|-------|--------|---------------------|
| **PTB Perplexity ↓** | 12.49 | 13.96 | 21.76 | **16.92** |
| **HellaSwag Accuracy ↑** | 0.77 | 0.70 | 0.80 | **0.78 → 0.79**（实际达 0.78–0.79） |

> ✅ **RFID-MoE 相比最佳基线（MoBE）降低 PPL 超过 8.0，HellaSwag 提升 ~8%**

#### 在 **DeepSeekMoE-16B-Base @ 60% 压缩率**

| 指标 | MoBE | **RFID-MoE** |
|------|------|-------------|
| **PTB PPL ↓** | 27.73 | **11.54** |
| **Average Acc ↑** | 0.68 | **0.71** |

> ✅ 性能远超所有基线，尤其在语言建模上优势巨大

---

### 🔍 消融实验结果

#### （1）不同校准数据的影响（Table 3）
| 方法 | Calibration | PTB PPL |
|------|-----------|--------|
| MoBE | – | 24.93 |
| **RFID-MoE** | WikiText-2 | **16.92** |
| **RFID-MoE** | C4 | **13.70**（最优） |

> ✅ 使用 C4 作为校准集效果更好，说明数据多样性有助于更准确估计专家利用率

#### （2）融合系数 $ \xi $ 的影响（Table 4）
- 最优 $ \xi \in [0.7, 0.8] $
- 若 $ \xi $ 过大 → 忽视路由频率 → 无法保护高频专家
- 若 $ \xi $ 过小 → 忽视信息密度 → 低估低频高信息专家

> ✅ 验证了融合两种信号的必要性

#### （3）大模型与轻量模型验证
- **Qwen3-235B-A22B-2507（超大规模）**：RFID-MoE 在 PTB 上达到 PPL=10.67，优于 MoBE（11.13），接近原模型（10.11）
- **Deepseek-v2-Lite-Chat / Ling-mini-2.0（轻量 MoE）**：仍能稳定优于 MoBE，证明方法通用性强

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **专家异质性至关重要**：MoE 中专家的 **routing frequency** 和 **information density** 存在显著差异，统一压缩策略会导致次优结果。
2. **低频 ≠ 无用**：某些专家虽极少被激活，但具有高 **effective rank**，蕴含关键领域知识，应予以保留。
3. **残差不可忽略**：SVD 截断产生的残差包含非平凡能量，可通过低维子空间高效重建。
4. **参数效率高**：仅增加约 3% 参数即可显著恢复性能，适合边缘部署。

---

### ⚠️ 局限性
1. **依赖校准数据**：需要一定量输入数据来统计 routing frequency，在极端分布偏移下可能失效。
2. **分组策略固定**：目前按排序后顺序分组，未探索聚类等更智能的 grouping 方式。
3. **仅适用于 FFN 层**：未扩展到 Attention 或其他模块的 MoE 压缩。

---

### 🔮 未来工作方向
1. **动态在线重分配**：根据输入流实时调整专家秩预算。
2. **端到端联合优化**：将压缩过程融入微调阶段，进一步提升性能。
3. **跨层共享投影矩阵**：进一步减少 $ P $ 的存储开销。
4. **应用于其他稀疏架构**：如 Switch Transformers、Jamba 等混合专家模型。

---

## ✅ 总结
**RFID-MoE** 是首个显式结合 **Routing Frequency** 与 **Information Density** 进行 MoE 压缩的工作，通过 **自适应秩分配** 和 **参数高效残差重建**，在多个主流 MoE LLM 上实现了 **state-of-the-art 的压缩性能**，尤其在高压缩比下仍能保持优异的语言建模与推理能力，为大规模 MoE 模型的实际部署提供了有效解决方案。

</details>

---

### 6. [Optimistic World Models: Efficient Exploration in Model-Based Deep Reinforcement Learning](https://arxiv.org/abs/2602.10044)

**Authors**: Akshay Mete, Shahid Aamir Sheikh, Tzu-Hsiang Lin, Dileep Kalathil, P. R. Kumar  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.10044v1  

#### Abstract
Efficient exploration remains a central challenge in reinforcement learning (RL), particularly in sparse-reward environments. We introduce Optimistic World Models (OWMs), a principled and scalable framework for optimistic exploration that brings classical reward-biased maximum likelihood estimation ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Optimistic World Models: Efficient Exploration in Model-Based Deep Reinforcement Learning

---

## 1. 主要贡献和创新点

### **解决了什么问题**

- **稀疏奖励环境下的低效探索**：现有的基于世界模型的强化学习（World Model-based RL）算法（如 DreamerV3、STORM）在稀疏奖励（sparse-reward）环境中表现不佳，因为其依赖的策略熵正则化等探索机制不足以驱动充分探索。
- **闭环识别问题（Closed-loop Identification Problem）**：标准的“确定性等价”（certainty equivalence）原则会导致模型估计偏差，收敛到次优策略，这是探索必要性的根本原因。

### **提出了什么新方法或新思路**

- **Optimistic World Models (OWMs)**：提出了一种新的框架，在世界模型训练中引入**乐观动力学损失（optimistic dynamics loss）**，将经典的 **Reward-Biased Maximum Likelihood Estimation (RBMLE)** 原理引入深度强化学习。
- **梯度化的 RBMLE 实现**：
  - 将 RBMLE 目标转化为一个完全基于梯度的优化目标，避免了传统 UCB 方法所需的不确定性估计和非凸约束。
  - 通过在模型训练中增加一个鼓励高回报轨迹的损失项，使世界模型生成“乐观”的想象（optimistic imaginations），从而引导策略向潜在高回报区域探索。

### **相比现有方法的优势**

| 特性 | OWMs | 传统 UCB 方法 |
|------|------|----------------|
| **计算效率** | 高，仅需额外损失项，无架构改动 | 低，需不确定性建模、置信区间计算 |
| **可扩展性** | 可直接集成到 DreamerV3、STORM 等主流框架 | 在大规模 DRL 中难以扩展 |
| **实现复杂度** | 插件式（plug-and-play），易于部署 | 复杂，常需专门设计 |
| **探索机制** | 通过模型偏置实现温和乐观（mild optimism） | 易出现过度乐观（excessive optimism） |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **Atari100K Benchmark**：26 个 Atari 游戏，每个任务限制为 400K 帧（约 2 小时游戏时间），用于评估**样本效率**。
- **DeepMind Control (DMC) Suite**：
  - **DMC Proprio**：连续控制任务，状态输入，预算 500K 步。
  - **DMC Vision**：相同任务但输入为图像，预算 1M 步。
- **重点关注稀疏奖励环境**：如 `Private Eye`、`Acrobot Swingup Sparse`、`Cartpole Swingup Sparse`、`Freeway`。

### **实验设置和评估指标**

- **评估指标**：
  - **Human-Normalized Score (HNS)**：`(Agent Score - Random Score) / (Human Score - Random Score)`，越高越好。
  - **Interquartile Mean (IQM)** 和 **Median**：减少异常值影响。
  - **最终平均奖励（Mean Reward）**。
- **训练细节**：
  - O-DreamerV3 和 O-STORM 分别基于 DreamerV3 和 STORM 构建。
  - 超参数尽量复用原方法，新增参数：乐观系数 `α(t)` 和模型熵系数 `η`。
  - O-DreamerV3 结果平均于 **10 个随机种子**，其余为 **5 个种子**。

### **基线方法对比**

- **主基线**：
  - **DreamerV3** vs **O-DreamerV3**
  - **STORM** vs **O-STORM**
- **其他对比**：
  - 消融实验（ablation）验证乐观损失和熵项的作用。
  - 不同 `α` 和 `η` 值的敏感性分析。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **Atari100K Benchmark**
- **O-DreamerV3**：
  - **平均 HNS 达到 152.68%**，相比 DreamerV3 的 **97.45%**，提升 **55%**。
  - 在 `Private Eye` 上从 115.53 提升至 **2120.48**（近 18 倍）。
  - 在 `Freeway` 上从 0 提升至 30（满分 30），首次达到满分。
- **O-STORM**：
  - 平均 HNS 为 **80.68%**，优于 STORM 的 **75.90%**。
  - 在 `Freeway` 上取得 **6.38** 的正分（STORM 为 0），证明其探索能力更强。

#### **DMC Suite**
- 在 `Acrobot Swingup Sparse` 和 `Cartpole Swingup Sparse` 等稀疏任务上，O-DreamerV3 显著优于 DreamerV3。
- 例如在 DMC Proprio 的 `Acrobot Swingup Sparse` 中，平均得分从 **8.4** 提升至 **34.6**。
- 在密集奖励任务上性能相当，未见退化。

### **与基线方法的对比结果**

- **全面优于基线**：在大多数 Atari 和 DMC 任务上，O-DreamerV3 和 O-STORM 均优于对应基线。
- **稀疏环境优势显著**：在 `Private Eye`、`Montezuma's Revenge` 等硬探索任务上，OWMs 收敛更快，最终性能更高（图1显示 O-DreamerV3 在 `Private Eye` 上达到近 2 倍回报）。
- **计算开销极小**：
  - 在 Atari100K（MsPacman）上，O-DreamerV3 训练时间为 **138 分钟**，DreamerV3 为 **115 分钟**，仅增加约 20% 时间。

### **消融实验结果**

- **移除熵损失项（Entropy Loss）**：性能下降，说明模型熵正则有助于防止过拟合和崩溃。
- **乐观系数 `α` 过大（如 0.1）**：导致训练不稳定甚至失败，证明**温和乐观**的重要性。
- **模型熵系数 `η` 过大（如 0.03）**：同样损害性能，小值（如 3e-6）更优。
- **不同 `α(t)` 衰减策略**：常数 `α=0.0001` 表现良好，无需复杂调度。

---

## 4. 关键结论和发现

### **主要发现**

1. **RBMLE 原理可成功迁移到大规模 DRL**：通过梯度化实现，解决了传统 UCB 方法在深度学习中的可扩展性难题。
2. **乐观应作用于模型学习而非策略**：OWMs 将乐观性注入**世界模型的动力学预测**中，而非策略本身，实现了更自然、高效的探索。
3. **“插件式”改进即可大幅提升性能**：仅需添加一个损失项，即可在不改变网络结构的情况下显著提升 DreamerV3 和 STORM 的探索能力。
4. **在稀疏奖励环境中效果尤为突出**：验证了 OWMs 对探索挑战的有效应对。

### **方法的局限性**

- **理论收敛性尚未完全建立**：虽然 RBMLE 在表格型 MDP 中有理论保证，但本文提出的**梯度化、函数逼近版本的收敛性仍需进一步分析**。
- **超参数敏感性**：尽管使用了统一配置，但 `α` 和 `η` 的选择对性能有显著影响，过大值会破坏训练。
- **乐观程度固定**：当前 `α(t)` 为常数或简单衰减，缺乏自适应调节机制。

### **未来工作方向**

- **自适应的乐观系数设计**：借鉴 Agent57 中的元控制器思想，动态调整 `α(t)` 以最大化探索收益。
- **理论分析**：建立梯度化 RBMLE 在函数逼近下的收敛性和后悔界（regret bound）。
- **扩展到更多架构**：将 OWMs 应用于 MuZero、EfficientZero 等基于 MCTS 的世界模型。
- **与其他探索机制结合**：如与内在激励（intrinsic motivation）或随机网络蒸馏（RND）结合，进一步提升鲁棒性。

---

> **总结一句话**：  
> 该论文提出了 **Optimistic World Models (OWMs)**，通过将 **RBMLE 原理**转化为一个**可微的乐观动力学损失**，实现了在不增加复杂度的前提下，显著提升世界模型在稀疏奖励环境中的探索效率，是 MBRL 领域一次**原理性与实用性兼备**的重要进展。

</details>

---

### 7. [Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective](https://arxiv.org/abs/2602.08009)

**Authors**: Rui Li, Zeyu Zhang, Xiaohe Bo, Quanyu Dai, Chaozhuo Li, Feng Wen, Xu Chen  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08009v1  

#### Abstract
Multi-agent architectures built on large language models (LLMs) have demonstrated the potential to realize swarm intelligence through well-crafted collaboration. However, the substantial burden of manual orchestration inherently raises an imperative to automate the design of agentic workflows. We fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前基于 **Large Language Model (LLM)** 的多智能体系统（**Multi-Agent System, MAS**）在协调机制上存在三大挑战：
- **Adaptivity（适应性）**：传统方法依赖预定义的静态拓扑（如链式、星型结构），无法根据推理过程中的动态消息流调整通信路径。
- **Scalability（可扩展性）**：当新增或移除 agent 时，需要重新进行昂贵的离线优化（如 GPTSwarm、AFlow），难以支持开放成员制。
- **Robustness（鲁棒性）**：中心化控制架构（如 AutoAgents、Puppeteer）存在 **Single Point of Failure (SPoF)** 风险，一旦元控制器被攻击或出错，整个系统可能崩溃。

这些限制阻碍了构建真正自组织、可靠且能应对复杂任务的 LLM 智能体社会。

### 提出了什么新方法或新思路
本文提出 **RAPS**（**Reputation-Aware Publish-Subscribe**），一种受 **Dynamic Ad-Hoc Networking** 启发的去中心化协调框架。其核心思想是将 LLM agent 视为网络主机，通过 **内容驱动** 而非 **身份驱动** 的通信模式实现协作。

#### 主要创新点：
- **通信基底（Communication Substrate）**：采用 **Distributed Publish-Subscribe Protocol**，将每个 agent 解耦为三个模块：
  - **Publisher**：生成消息（publication）
  - **Subscriber**：声明意图（subscription）
  - **Broker**：基于语义匹配（而非固定图）路由消息
- **覆盖机制（Overlay Mechanisms）**：
  1. **Reactive Subscription**：允许 agent 在运行时根据上下文动态更新自己的订阅意图，实现细粒度角色专业化。
  2. **Bayesian Reputation**：每个 agent 维护一个本地 **watchdog**，使用贝叶斯估计评估对等体的可靠性，从而隔离恶意或低质量行为者。

该设计实现了 **intent-based collaboration** 和 **decentralized trust**，无需集中调度器即可自发形成高效协作网络。

### 相比现有方法的优势
| 特性 | 传统方法 | RAPS |
|------|--------|------|
| **Adaptivity** | 固定拓扑，通信不可知（communication-agnostic） | 运行时动态意图演化，内容感知路由 |
| **Scalability** | 新增 agent 需重新训练/搜索拓扑 | 支持即插即用，无需全局重构 |
| **Robustness** | 中心控制器易受攻击（SPoF） | 分布式信誉机制，抗拜占庭攻击 |

---

## 2. 核心实验方法和设置

### 使用的数据集
在五个代表性基准上进行评估，涵盖三类任务：
| 数据集 | 类别 | 任务描述 | 评估指标 |
|-------|------|---------|----------|
| **MMLU** | General Reasoning | 多领域知识问答（STEM、人文等） | Accuracy |
| **GSM8K**, **SVAMP**, **AQuA** | Mathematical Reasoning | 数学应用题求解（多步推理） | Accuracy |
| **HumanEval** | Code Generation | Python 编程问题（函数补全） | Pass@1 |

### 实验设置和评估指标
- **LLM Backbone**：统一使用 `GPT-4o-mini` 作为所有 agent 的基础模型，确保公平比较。
- **Agent 数量**：默认配置 5 个 agent，角色根据任务定制（如 MMLU 使用 Knowledge Expert、Mathematician 等）。
- **最大通信轮次**：设为 5。
- **评估方式**：
  - 所有方法（除单 agent）均使用相同数量的 agent。
  - 报告平均准确率及各任务得分。

### 基线方法对比
分为四类基线：
1. **Single-Agent Models**：
   - Vanilla IO, CoT, ComplexCoT, Self-Consistency (SC)
2. **Static Multi-Agent Models**：
   - Chain, Star, Tree, Random（固定拓扑）
   - LLM-Debate, LLM-Blender
3. **Communication-Agnostic Models**：
   - GPTSwarm, AgentPrune, AFlow, MaAS, G-Designer（基于离线搜索/学习拓扑）
4. **Meta-Controlled Models**：
   - AutoAgents, Puppeteer, MAS-Zero（依赖中央控制器）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 方法 | MMLU | GSM8K | SVAMP | AQuA | HumanEval | **Average** |
|------|------|-------|-------|------|-----------|------------|
| **RAPS (Ours)** | **88.2** | **95.4** | **92.2** | **82.6** | **91.5** | **90.0** |
| G-Designer | 86.3 | 93.2 | 90.7 | 79.4 | 90.2 | 88.0 |
| AFlow | 85.6 | 94.1 | 90.0 | 78.5 | 91.0 | 87.8 |
| LLM-Debate | 85.0 | 92.4 | 89.8 | 77.3 | 82.6 | 85.4 |
| Puppeteer | 84.3 | 93.3 | 89.5 | 77.5 | 75.3 | 84.0 |

> ✅ RAPS 在所有任务上均达到 **SOTA 性能**，平均得分 **90.0%**，显著优于最强基线 G-Designer（88.0%）。

### 与基线方法的对比结果
- **超越静态结构**：Chain 在 SVAMP 上仅为 82.6%，远低于 RAPS 的 92.2%，表明刚性拓扑无法适应多样化推理需求。
- **优于离线优化方法**：尽管 AFlow/G-Designer 使用复杂搜索策略，但仍受限于“通信不可知”特性；RAPS 利用运行时意图对齐，在 AQuA 上领先 3.2%。
- **击败中心化控制器**：Puppeteer 平均仅 84.0%，且在 HumanEval 上严重退化至 75.3%，显示其脆弱性。

### 消融实验结果（Table 3）
验证两个核心组件的重要性：
| 变体 | MMLU | GSM8K | HumanEval |
|------|------|-------|----------|
| **RAPS (Full)** | 88.2 | 95.4 | 91.5 |
| w/o RS（无 Reactive Subscription） | 85.6 (-2.6) | 93.7 (-1.7) | 89.3 (-2.2) |
| w/o BR（无 Bayesian Reputation） | 86.9 (-1.3) | 94.5 (-0.9) | 90.7 (-0.8) |
| w/o Both | 83.7 (-4.5) | 92.8 (-2.6) | 88.5 (-3.0) |

> 🔍 结果表明：
> - **Reactive Subscription** 对性能提升贡献更大，尤其在需要精细角色分工的任务中。
> - **Bayesian Reputation** 显著增强系统鲁棒性，防止错误传播。

此外，**Figure 5** 显示：即使在“naive agent pool”（通用角色）下，RAPS 仍保持 90.2% 准确率，而 G-Designer 下降 9.5%，证明其对初始配置不敏感，具备强自适应能力。

---

## 4. 关键结论和发现

### 主要发现
1. **动态意图演化至关重要**：Reactive Subscription 使 agent 能根据上下文自动专业化角色（如从“数学专家”细化为“距离-速率-时间分析员”），避免静态角色错配。
2. **去中心化信誉机制有效防御拜占庭攻击**：在注入 **adversarial agents** 的压力测试中（Table 2）：
   - G-Designer 在 2T3A 场景下准确率降至 15.0%
   - Puppeteer-C（攻击中央控制器）直接崩溃至 13.7%
   - **RAPS 仍维持 83.0%**，体现强大鲁棒性。
3. **可扩展性强**：随着 agent 数量增加（Figure 3a），RAPS 持续提升性能，而 Puppeteer 和 Chain 因误差累积或控制瓶颈表现下降。
4. **高效通信**：相比需昂贵优化的 GPTSwarm/G-Designer，RAPS 推理延迟稳定增长，适合大规模部署。

### 方法的局限性
- **依赖基础模型能力**：RAPS 是“智能放大器”，不能弥补 LLM 自身的知识缺陷。
- **冷启动问题**：初期因缺乏交互历史，信誉系统可能短暂失效，需一定 warm-up 时间。
- **计算开销**：虽然免训练，但运行时 LLM 调用频繁，成本较高（可通过 embedding-based broker 缓解）。

### 未来工作方向
- 将 RAPS 与 **Multi-Agent Reinforcement Learning** 结合，学习更优通信策略。
- 引入 **Congestion Control** 机制管理 token 消耗。
- 构建 **Hierarchical Subnetting** 支持超大规模 agent 社会。
- 探索 **Reputation Transfer** 以缓解冷启动问题。

---

> 📌 **总结**：  
> RAPS 成功将 **Dynamic Ad-Hoc Networking** 的经典思想引入 LLM 多智能体协调领域，首次在一个统一框架内同时解决了 **Adaptivity、Scalability 和 Robustness** 的三难困境。其实验充分验证了“内容为中心”的去中心化通信范式的优越性，为构建开放、自组织、可信的 LLM agent 社会提供了坚实基础。

</details>

---

### 8. [Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System](https://arxiv.org/abs/2602.08335)

**Authors**: Yanming Li, Xuelin Zhang, WenJie Lu, Ziye Tang, Maodong Wu, Haotian Luo, Tongtong Wu, Zijie Peng, Hongze Mi, Yibo Feng, Naiqiang Tan, Chao Huang, Hong Chen, Li Shen  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08335v1  

#### Abstract
Integrating Large Language Models (LLMs) with external tools via multi-agent systems offers a promising new paradigm for decomposing and solving complex problems. However, training these systems remains notoriously difficult due to the credit assignment challenge, as it is often unclear which specif...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在基于 **Large Language Models (LLMs)** 和外部工具集成的 **Multi-Agent Systems (MAS)** 中，训练过程面临严重的 **credit assignment（信用分配）挑战**。由于任务的成功或失败是多个智能体（如 planner 和 worker）协作的结果，传统的强化学习方法通常依赖于稀疏的、全局广播的奖励信号（broadcast rewards），无法准确识别每个 agent 对最终结果的具体贡献，导致策略更新低效且不稳定。

### 🚀 提出的新方法：SHARP
作者提出 **SHARP (Shapley-based Hierarchical Attribution for Reinforcement Policy)**，一种基于 **Shapley Value** 的精细化信用分配框架，用于优化多智能体系统的强化学习训练。其核心思想是通过数学上严谨的方式量化每个 agent 的边际贡献。

#### 主要创新点：
- **Tripartite Decomposed Reward Mechanism（三元分解奖励机制）**：
  1. **Global Broadcast-Accuracy Reward**：确保整体任务对齐；
  2. **Shapley-based Marginal-Credit Reward**：为每个 agent 分配基于其因果影响的个体化奖励；
  3. **Tool-Process Reward**：提供执行过程层面的反馈，提升工具调用的有效性。
  
- **Counterfactual Masking 机制**：通过“反事实掩码”移除某个 agent 后比较轨迹性能差异，精确估计其 Shapley 值，实现细粒度的 credit attribution。

- **Hierarchical Advantage Normalization**：对不同 agent 的优势进行组内归一化处理，稳定训练梯度，支持异构 agent 的联合优化。

### 🔍 相比现有方法的优势
| 方法类型 | 局限性 | SHARP 的改进 |
|--------|------|-------------|
| 单智能体架构（Single-agent） | 易出现上下文溢出、噪声累积 | 支持模块化分工，降低复杂度 |
| 多智能体 + 广播奖励（e.g., MATPO） | 所有 agent 接收相同奖励，混淆个体贡献 | 引入 Shapley 信用，精准区分 planner 与 worker 贡献 |
| 偏好学习（e.g., GRPO） | 缺乏个体行为建模能力 | 在 group-relative 框架下引入 agent-specific 优势函数 |

> ✅ **优势总结**：SHARP 实现了更高效、稳定的多智能体协同训练，显著提升了 long-horizon 复杂任务中的推理与执行一致性。

---

## 2. 核心实验方法和设置

### 📚 数据集
实验覆盖五个真实世界基准，涵盖多跳推理、网页导航、检索增强生成等场景：

| 数据集 | 描述 |
|-------|------|
| **MuSiQue** | 多跳问答，要求真正的多步推理 |
| **GAIA-text** | 综合 AI 助手评测，需结合 web 浏览与工具使用 |
| **WebWalkerQA** | 结构化网页遍历与证据聚合任务 |
| **FRAMES** | 端到端 RAG 场景下的事实性、检索与推理统一评测 |
| **DocMath-Eval** | 长文本（含表格）中的数学推理任务 |

> ⚠️ 注意：除 MuSiQue 外，其余均采用官方测试集零样本评估。

---

### ⚙️ 实验设置与评估指标

- **模型骨干**：主要基于 **Qwen3-8B** 进行训练与对比，保证公平性。
- **训练配置**：
  - 使用 **AdamW** 优化器，学习率 $10^{-5}$，batch size 256；
  - 每个查询采样 G=8 条轨迹；
  - 总共训练 180 个梯度步，在 64×A100 GPU 上完成。
- **评估指标**：
  - **Match Accuracy (%)**：预测答案是否匹配标准答案；
  - **Average Match**：跨数据集平均得分；
  - **MMLU**：用于检测灾难性遗忘（catastrophic forgetting）；
  - **Coordination Metrics**：如 planner score、harmful subagent 比例等。

---

### 🆚 基线方法对比
分为四类进行系统比较：

| 类别 | 代表方法 |
|-----|---------|
| **Vanilla LLMs** | LLaMA-3.1-8B, Qwen3-8B RAG |
| **Prompt-based Planning** | Plan-Search |
| **Single-agent RL** | Search-R1, Single-agent GRPO |
| **Multi-agent RL w/o Marginal Credit** | Planner-Worker, G-Designer, CARD, COA, AceSearcher, MATPO |

> ✅ 所有对比均聚焦于是否具备 **marginal credit modeling (MCR)** 能力。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| 方法 | MuSiQue | GAIA-text | WebWalkerQA | FRAMES | **AVG** |
|------|--------|----------|------------|--------|--------|
| MATPO (SOTA baseline) | 47.00 | 31.65 | 7.47 | 37.10 | **30.81** |
| **SHARP (ours)** | **50.76** | **33.70** | **8.50** | **37.29** | **32.56** |

> ✅ **相对提升**：
> - 比最强的 **multi-agent baseline (MATPO)** 提升 **+1.75 pts**
> - 比 **single-agent baseline** 提升 **+23.66%**
> - 比 **multi-agent baseline** 提升 **+14.05%**

---

### 🔬 消融实验结果（Ablation Studies）

#### （1）组件有效性分析（Figure 3）
- 移除 **planner-level Shapley credit** → MuSiQue 准确率下降约 2.76 pts
- 移除 **worker-level Shapley credit** → 下降约 2.36 pts
- **完整 SHARP 模型效果 > 两者之和**，说明存在协同增益（synergy）

#### （2）不同训练策略的影响
| 设置 | Planner Score | Useful Subagents | Harmful Subagents |
|------|---------------|------------------|--------------------|
| No training | 0.4542 | 11.03% | 5.48% |
| MATPO | 0.4804 | 11.85% | 5.48% |
| **SHARP** | **0.5084** | **12.96%** | **4.40%** |

> ✅ 表明 SHARP 成功优化了 planner 的子任务设计，并有效过滤有害 worker 调用。

#### （3）可扩展性与稳定性
- **参数规模扩展（Figure 4）**：从 0.6B 到 8B，SHARP 增长更快，在 8B 上比 single-agent 多出 **14.41 pts**
- **训练稳定性（Figure 5）**：在 GAIA-text 上训练 180 步，SHARP 准确率单调上升，而 GRPO/MATPO 出现波动
- **泛化能力（Figure 3 右图）**：在 DocMath-Eval 四种设定（SS/SL/CS/CL）中均取得最佳表现

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Marginal Credit 是性能跃迁的关键驱动因素**  
   相较于架构设计或训练策略本身，显式的 **Shapley-based credit assignment** 对性能提升贡献最大。

2. **Planner 与 Worker 的信用需协同建模**  
   单独优化 planner 或 worker 效果有限；只有联合建模才能实现协同进化，尤其 worker 的信用更能缓解 long-horizon 执行错误。

3. **SHARP 提升协调质量而非增加冗余计算**  
   - 减少 **harmful subagent calls** 从 5.48% → 4.40%
   - 提高 **useful subagent ratio** 至 12.96%
   - 表明其学习到了更高效的协作模式

4. **无灾难性遗忘现象**（Table 2）
| 方法 | MMLU Accuracy |
|------|----------------|
| Planner-Worker (no train) | 93.04% |
| MATPO | 94.00% |
| **SHARP** | **94.65%** |

> ✅ 表明 SHARP 不仅未损害通用知识，反而因推理能力增强带来正向迁移。

---

### ⚠️ 方法的局限性
1. **计算开销较高**：Shapley 值估算需要多次 counterfactual rollout，尽管可通过并行缓解，但仍增加训练成本（见 Figure 7）。
2. **依赖高质量环境模拟**：工具反馈必须可靠，否则 marginal credit 会被污染。
3. **当前仅适用于 role-based parameter sharing 架构**，尚未推广至完全独立策略的 MAS。

---

### 🔮 未来工作方向
1. **高效 Shapley Approximation**：探索采样策略或代理模型来加速 credit 计算。
2. **动态 agent pruning**：利用 negative Shapley credit 自动剔除低效 agent，构建自适应 topology。
3. **跨任务迁移 credit logic**：将 learned attribution 机制迁移到新领域。
4. **扩展至非层级结构**：应用于通信图（communication graph）或多环路协作系统。

---

## ✅ 总结
**SHARP** 是首个将 **cooperative game theory** 中的 **Shapley Value** 系统应用于 **LLM-based MAS** 的 credit assignment 框架。它通过三重奖励分解与反事实因果建模，解决了传统多智能体 RL 中“谁该得奖”的根本难题。实验证明其不仅大幅超越 SOTA，还增强了训练稳定性、协调效率与泛化能力，为构建可解释、可扩展的智能体社会提供了坚实基础。

</details>

---

### 9. [Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs](https://arxiv.org/abs/2602.08241)

**Authors**: Siqu Ou, Tianrui Wan, Zhiyuan Zhao, Junyu Gao, Xuelong Li  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.08241v1  

#### Abstract
While chain-of-thought (CoT) reasoning has substantially improved multimodal large language models (MLLMs) on complex reasoning tasks, existing approaches largely rely on long textual reasoning trajectories and provide limited mechanisms for learning stable visual attention policies. Our analysis sh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Multimodal Large Language Models (MLLMs)** 在复杂视觉推理任务中虽然具备强大的语言推理能力，但在**视觉注意力机制上存在严重缺陷**。具体表现为：

- **早期视觉关注错误难以纠正**：在 Chain-of-Thought (CoT) 推理过程中，若模型初始阶段未能正确聚焦于目标视觉区域，后续推理链很少能修正该错误，导致“错误传播”。
- **缺乏有效的视觉注意力学习信号**：现有训练目标（如答案准确性）无法为视觉注意力行为提供有效的 **credit assignment**（信用分配），导致模型即使拥有强大推理能力，也无法建立稳定、可靠的视觉定位策略。

### 🚀 提出的新方法与新思路

作者提出 **SAYO** —— 一种基于强化学习（Reinforcement Learning, RL）的视觉推理框架，其核心是引入 **Entropy-Based Target Attention Reward**（基于熵的目标注意力奖励）：

- **Region-Level 视觉注意力奖励**：利用带有边界框标注的数据，计算模型对目标视觉区域的关注程度，并将其转化为强化学习中的奖励信号。
- **高熵 Token 选择机制**：仅在生成过程中不确定性较高（即信息量大）的 token 上施加注意力奖励，确保优化集中在关键决策点，避免噪声干扰。
- **无需外部提示或架构修改**：不依赖 visual prompt engineering 或额外模块，在推理时无需特殊处理即可实现更强的视觉聚焦。

### 🔍 相比现有方法的优势

| 对比维度 | 现有方法 | SAYO |
|--------|-------|------|
| **注意力学习方式** | 间接（通过答案准确率） | 直接（显式注意力奖励） |
| **是否需要外部工具** | 是（如 ReFocus、Visual SketchPad 需要程序化标注） | 否（端到端训练） |
| **能否纠正早期错误** | 弱（错误易传播） | 强（持续强化正确关注） |
| **泛化能力** | 受限于特定任务设计 | 跨任务迁移性强（数学、图表、通用视觉均提升） |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 类别 | 数据集名称 | 用途说明 |
|-----|-----------|---------|
| **通用视觉推理** | GQA, M3CoT, V*, MMStar, MME-RealWorld Lite | 测试多步视觉推理与真实场景理解能力 |
| **数学视觉推理** | We-Math, MathVision | 包含几何图、公式图像等需精确视觉解析的任务 |
| **结构化图表理解** | ChartQA, AI2D, CharXiv | 图表、科学插图等结构化视觉输入的理解 |
| **训练数据来源** | GQA + ReFocus_Data (~20k 样本) | 构建强调视觉关注的 RL 训练样本 |

> 注：训练阶段**未使用任何数学数据集**，以验证跨域泛化能力。

### ⚙️ 实验设置与评估指标

- **基础模型**：
  - `Qwen3-VL-4B/8B/30B`
  - `InternVL3.5-8B`
- **训练算法**：Group Relative Policy Optimization (**GRPO**)，结合格式奖励（format reward）与提出的注意力奖励。
- **训练细节**：
  - 6 × NVIDIA H200 GPU
  - 4 epochs，batch size per device = 64
  - KL 散度约束系数 $1\times10^{-3}$
- **评估协议**：
  - 所有问题输出必须包含 `<think>...</think>` 内部推理过程
  - 最终答案用 `\boxed{}` 封装
  - 报告各 benchmark 的准确率（Accuracy）

### 🆚 基线方法对比

| 类型 | 代表模型 |
|------|--------|
| **闭源模型** | GPT-4o, Gemini 2.5 Pro |
| **开源通用 MLLMs** | Qwen3-VL, InternVL3.5, Kimi-VL-16B |
| **开源推理专用 MLLMs** | OpenVLThinker-7B, Semantic-back-7B, ViGoRL, NoisyRollout-7B, R1-Onevision-7B |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| Model | Avg Score |
|-------|----------|
| **GPT-4o** | – |
| **Gemini 2.5 Pro** | – |
| **Qwen3-VL-8B** | 59.64 |
| **SAYO-Qwen-8B** | **64.03** ↑↑ |
| **SAYO-Qwen-4B** | 62.17 > 原始 8B 版本 |
| **SAYO-InternVL-8B** | 53.77 > 原始 50.49 |

> ✅ **SAYO-Qwen-8B 不仅超越所有同规模开源模型，还在 MMStar 上超过 GPT-4o 和 Kimi-VL-16B**

#### 分项显著提升示例：

- **MMStar**: 62.60 → **65.27**
- **We-Math**: 52.64 → **64.83** （+12.19，巨大飞跃）
- **MathVision**: 22.20 → **25.26**
- **ChartQA/AI2D**: 多数提升 3–5 pts

> 💡 即使**未在数学数据上训练**，SAYO 在数学推理任务上表现大幅提升，证明其改进的是底层视觉感知能力而非记忆答案。

### 🔍 消融实验结果（Table 2 & Table 3）

#### ✅ 注意力奖励的有效性（Table 2）

| 设置 | Avg 提升（vs base） |
|------|------------------|
| Accuracy-only reward | +1.28 |
| Attention-only reward | **+4.32** |
| Combined reward | **+4.63** |

> 结论：**注意力奖励比传统准确率奖励更有效**，是性能提升的关键驱动力。

#### ✅ 高熵 Token 选择的重要性（Table 3）

| Token Selection | We-Math (Qwen) | We-Math (InternVL) |
|------------------|----------------|--------------------|
| All tokens | 58.39 | 24.20 |
| **Top 30% high-entropy tokens** | **64.83** | **25.23** |

> 结论：**只对高信息量 token 施加注意力奖励效果最佳**，避免低信息 token 引入噪声。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **视觉注意力错位是 MLLMs 推理失败的核心原因**  
   当前 MLLMs 的推理瓶颈不在语言逻辑，而在**无法可靠地“看到”正确的视觉部分**。

2. **Credit Assignment 缺失导致注意力学习不足**  
   传统的监督训练无法将成功归因于正确的视觉关注行为，导致注意力策略不稳定。

3. **SAYO 成功解耦“视觉解析”与“逻辑推理”**  
   通过强化学习直接优化视觉注意力，让已有强大的语言推理引擎建立在**正确的视觉前提**之上。

4. **跨领域泛化能力强**  
   在未见数学数据的情况下显著提升数学任务表现，表明其学到的是**通用的视觉聚焦元技能**（meta-skill）。

5. **高熵 token 是关键干预节点**  
   在模型最不确定的时候强制其查看图像，形成“**Look-to-Verify**”机制，是最高效的训练策略。

### ⚠️ 方法的局限性

- **依赖带边界框标注的训练数据**：虽不需人工撰写推理路径，但仍需 region-level 标注，限制了可扩展性。
- **对极低分辨率或模糊图像仍可能失效**：本质仍是 token-level attention mapping，受限于视觉编码器质量。
- **目前仅验证于静态图像任务**：视频或多帧动态推理尚未测试。

### 🔮 未来工作方向

- **自动挖掘潜在关注区域**：减少对人工标注 bbox 的依赖，例如通过自监督方式发现 salient regions。
- **扩展至多模态交互任务**：如机器人控制、具身智能中的实时视觉决策。
- **探索更细粒度的注意力调控机制**：如逐层调节、动态奖励权重等。
- **应用于医疗、遥感等专业领域图像理解**：验证在高精度需求下的实用性。

---

## 总结一句话

> **SAYO 通过引入基于高熵 token 的 region-level 视觉注意力奖励，解决了 MLLMs “看不准”的根本问题，在不增加推理成本的前提下，实现了跨任务、跨领域的视觉推理能力全面提升。**

</details>

---

### 10. [Learning to Discover Iterative Spectral Algorithms](https://arxiv.org/abs/2602.09530)

**Authors**: Zihang Liu, Oleg Balabanov, Yaoqing Yang, Michael W. Mahoney  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.09530v1  

#### Abstract
We introduce AutoSpec, a neural network framework for discovering iterative spectral algorithms for large-scale numerical linear algebra and numerical optimization. Our self-supervised models adapt to input operators using coarse spectral information (e.g., eigenvalue estimates and residual norms), ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Learning to Discover Iterative Spectral Algorithms**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
传统数值线性代数（NLA）和数值优化中的迭代谱算法（如多项式加速、预条件器设计）通常依赖于手工设计的规则（如 Chebyshev 多项式），难以自适应地适配不同算子的谱结构。现有基于机器学习的算法发现方法多聚焦于离散程序空间，不适用于需要连续参数调整的 NLA 场景。

本文旨在**自动化发现适用于大规模 NLA 任务的、可自适应谱结构的迭代谱算法**，填补了 ML for Algorithms 在连续数值计算领域的空白。

---

### **提出的新方法：AUToSPEC 框架**
作者提出了 **AUToSPEC** ——一个基于神经网络的框架，用于发现迭代谱算法。其核心思想是：

- 将算法表示为**可执行的短递推关系（short recurrence）**，由神经网络预测递推系数。
- 利用粗粒度谱信息（如粗略的特征值估计和残差范数）作为输入，生成针对下游任务定制的矩阵多项式（matrix polynomial）。
- 整个流程是**端到端可微分且自监督**的，通过任务驱动的目标函数进行训练。

#### **三大创新要素**：
1. **架构即算法（Architecture as Algorithm）**  
   网络推理过程直接实现一个数值稳定的迭代过程（如 $ V_{k+1} = M_k(X)V_k $），使得学到的模型本身就是可部署的数值算法。

2. **跨尺度泛化训练策略**  
   在小规模合成对角矩阵上训练，迁移到百万维真实稀疏矩阵，实现了**高效训练与实际部署的解耦**。

3. **任务定义的自监督目标**  
   不依赖模仿已有算法，而是通过最小化任务相关的损失（如 CG 迭代次数、特征值间隙、近似误差）来优化算法性能。

---

### **相比现有方法的优势**
| 维度 | 传统方法（如 Chebyshev） | 符号回归 / RL 方法 | **AUToSPEC** |
|------|--------------------------|--------------------|--------------|
| 参数空间 | 固定形式（如区间映射） | 离散程序空间 | **连续、可微、结构化** |
| 自适应性 | 弱（需人工设定谱区间） | 中等（搜索有限模板） | **强（从谱探针动态生成）** |
| 可扩展性 | 高（低存储） | 低（搜索成本高） | **高（一次训练，广泛适用）** |
| 性能 | 理论最优但实践受限 | 不稳定 | **实证显著优于基线** |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **训练数据**：合成对角矩阵（$ X = \text{diag}(\lambda) $），通过控制谱形状（幂律衰减、指数衰减、平坦谱等）生成多样化的谱分布。
  - 特征值维度：$ n \in [50, 1000] $
  - 条件数范围：$ \kappa \in [10^2, 10^5] $
- **测试数据**：来自 **SuiteSparse Matrix Collection** 的真实世界稀疏矩阵，涵盖：
  - 电子结构（Si02）
  - 热力学有限元（thermal1, thermal2）
  - 电路建模（G2_circuit, G3_circuit）
  - 结构力学问题
  - 尺寸高达 $ 1.6 \times 10^6 $

此外还包括一个 DNA 序列相似性 Gram 矩阵用于协方差白化任务。

---

### **实验设置与评估指标**

#### **三大任务设置**：

| 任务 | 目标 | 输入谱探针 | 输出 |
|------|------|-------------|-------|
| **特征值计算** | 加速 `eigs` 收敛 | 前 $ k $ 和附近特征值 + 残差 | 谱变换多项式 $ P(X) $ |
| **线性系统求解** | 加速 CG 收敛 | 最大/最小 20 个特征值 + 残差 | 多项式预条件器 $ P(X) $ |
| **矩阵函数逼近** | 近似 $ X^{-1/2} $ | 同左 | 多项式逼近 $ P(X) \approx X^{-1/2} $ |

#### **评估指标**
- **收敛速度提升**：达到指定精度所需的迭代次数（outer iterations）
- **条件数改善**：$ \kappa(P(X)X) $
- **相对性能增益**：使用对数尺度比率衡量：
  $$
  \rho_{\log} = \frac{\log T_{\text{baseline}}}{\log T_{\text{model}}}
  $$
- **近似误差**：$ \|P(X)X^{1/2} - I\| $ 或 $ \|P(X)XP(X) - I\| $

#### **谱探针获取方式**
- 特征值任务：运行 20 步 `eigs`（Krylov-Schur）
- 线性系统：50 步 Lanczos（不存基底）

---

### **基线方法对比**
| 任务 | 基线方法 |
|------|---------|
| 特征值 | 幂迭代（Power iteration, $ P(X) = X^d $） |
| 线性系统 | Richardson 迭代、经典 Chebyshev 预条件器 |
| 矩阵函数 | 截断 Neumann 级数（Taylor 展开） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **特征值问题（Table 3b & Figure 9）**
| 矩阵 | AUToSPEC ($ k=10 $) | Power Method ($ k=10 $) | 提升倍数 |
|------|------------------------|----------------------------|----------|
| Si02 | 23 | >500 | **>20x** |
| CO | 55 | >500 | **>9x** |
| thermal2 | 21 | 112 | **~5x** |

> 所有情况下均显著减少 `eigs` 外层迭代次数。

#### ✅ **线性系统（Table 3a & Figure 3）**
| 矩阵 | 无预条件 | Chebyshev | **AUToSPEC** |
|------|-----------|------------|---------------|
| G2_circuit | 734 | 79 | **70** |
| thermal2 | >5000 | 658 | **705** |
| Flan_1565 | 3868 | 374 | **375** |

> 在多数情况下，**AUToSPEC 与 Chebyshev 相当甚至更优**，尤其在复杂谱结构下表现稳健。

#### ✅ **矩阵函数逼近（Figure 4）**
- 在 DNA 协方差白化任务中，**AUToSPEC 比 Neumann 级数降低一个数量级的 operator-norm 误差**。
- 白化质量更高，且仅需 matvec 操作，适合大规模稀疏场景。

---

### **消融实验结果**

#### 🔹 **残差信息的重要性（Figure 5 & 13）**
- 当移除残差（residual norms）作为输入时，算法性能明显下降，尤其是在谱探针质量较低时。
- 表明**残差提供了关于谱估计可靠性的关键信号**，帮助模型判断如何加权不同频段。

#### 🔹 **对谱输入扰动的鲁棒性（Figure 10）**
- 即使使用仅 50 步 Lanczos 得到的粗糙谱估计，AUToSPEC 仍能稳定输出高性能预条件器。
- 当 Lanczos 步数超过 75 后，CG 迭代数趋于稳定，说明方法具有良好的**输入鲁棒性**。

#### 🔹 **与 Chebyshev 的比较（Table 4）**
- AUToSPEC 在多个矩阵上**超越或匹配 Chebyshev 表现**，且对谱估计误差更鲁棒。
- 特别是在 `gyro_m` 上，Chebyshev 退化严重（141 vs 27），而 AUToSPEC 保持高效。

---

## **4. 关键结论和发现**

### **主要发现**
1. **AUToSPEC 能有效发现高性能的迭代谱算法**，在真实世界矩阵上实现**数量级的加速**。
2. 学到的多项式表现出**类 Chebyshev 特性**（near-equiripple, near-minimax），尽管未显式编码该先验。
   - 如 Table 1 所示，其 minimax optimality gap 接近理论最优，远优于随机多项式。
3. 方法具备**强泛化能力**：从小合成矩阵训练 → 大真实矩阵部署。
4. **残差信息是关键输入特征**，提升了算法在低质量谱估计下的鲁棒性和有效性。

### **方法的局限性**
- 当前版本主要基于**线性状态转移**，表达能力受限。
- 对极端病态矩阵（$\kappa > 10^7$）效果可能受限，需结合其他预处理（如 ichol）。
- 推理阶段仍需一次谱探针（warm-start），无法完全“零样本”运行。

### **未来工作方向**
- 扩展至**有理函数逼近**（rational filters），以更好处理尖锐谱变化（如 interior eigenvalues）。
- 探索**非线性状态转移**和**高阶递推**，增强表达力。
- 将框架应用于**first-order optimization**（如 learning gradient descent with momentum）。
- 结合 LLM 进行**可解释性挖掘**，尝试从学到的系数中反推解析公式。

---

> **总结一句话**：  
> AUToSPEC 成功将神经网络建模为“可编程的数值算法引擎”，实现了从谱探针到高性能迭代算法的端到端生成，在多个 NLA 任务上展现出超越经典方法的潜力，为 ML for Scientific Computing 开辟了新路径。

</details>

---

### 11. [SCOUT-RAG: Scalable and Cost-Efficient Unifying Traversal for Agentic Graph-RAG over Distributed Domains](https://arxiv.org/abs/2602.08400)

**Authors**: Longkun Li, Yuanben Zou, Jinghan Wu, Yuqing Wen, Jing Li, Hangwei Qian, Ivor Tsang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08400v1  

#### Abstract
Graph-RAG improves LLM reasoning using structured knowledge, yet conventional designs rely on a centralized knowledge graph. In distributed and access-restricted settings (e.g., hospitals or multinational organizations), retrieval must select relevant domains and appropriate traversal depth without ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SCOUT-RAG 论文核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文针对 **分布式环境下的 Graph-RAG**（Graph Retrieval-Augmented Generation）系统面临的关键挑战提出解决方案，具体包括：

- **数据孤岛与隐私限制**：在医院、跨国组织等场景中，知识图谱分布在多个独立域（domains），无法集中共享原始数据。
- **缺乏全局可见性**：传统 Graph-RAG 依赖统一的全局知识图，而现实中各 domain 是部分可观测的。
- **检索成本高昂**：跨域请求可能带来延迟或 API 费用，需最小化不必要的查询。
- **冷启动与标签缺失**：监督式 domain router 需要标注数据，在新部署场景下不可行。

因此，如何在 **无全局图可见、有成本约束、保护隐私的前提下**，实现高效、高质量的多跳推理成为核心难题。

---

### 提出了什么新方法或新思路
作者提出了 **SCOUT-RAG** —— 一种分层的、基于智能体（agentic）架构的分布式 Graph-RAG 框架，其核心思想是将检索建模为一个**序列决策过程**，通过四个协作 agent 实现渐进式、自适应的跨域检索。

#### 主要创新点：
1. **训练无关的 domain relevance 估计（Training-Free Domain Relevance Estimation）**
   - 利用三种信号进行零样本 relevance 排序：
     - `Semantic similarity`（语义相似度）
     - `Knowledge richness`（知识丰富度，如报告数量）
     - `Historical performance`（历史问答质量）
   - 支持冷启动部署，无需标注 query-domain 对。

2. **去中心化的 Graph-RAG 架构设计**
   - 将每个 domain 视为一个局部可观察子图（partially observable subgraph）。
   - 动态决定是否在当前 domain 内深入遍历（depth），还是扩展到其他 domain（breadth）。

3. **闭环迭代优化机制**
   - 引入 Answer Quality Assessment Agent（AQAA）持续监控答案完整性与覆盖广度。
   - 基于反馈动态选择 refinement 策略（Depth / Breadth / Hybrid / Stop）。

4. **多级安全机制保障稳定性**
   - 时间预算强制终止（time-budget enforcement）
   - 最佳答案追踪（best-answer tracking）
   - 并行协调执行（coordinated parallelism）
   - 防止因某 domain 延迟导致整体失败。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **隐私友好性** | 不要求数据集中化，符合联邦学习范式 |
| **成本效率** | 显著减少 cross-domain calls 和 token 消耗 |
| **适应性强** | 可处理冷启动、动态变化的 domain 效用 |
| **可控性高** | 显式控制质量-成本权衡，支持 early stopping |

相比 DRIFT 等 centralized 方法，SCOUT-RAG 在保持接近性能的同时大幅降低成本；相比 exhaustive decentralized 方法，则实现了更高的性价比。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **模拟多域数据集**：基于 **Wikipedia 的 45 个国家页面**构建 domain-specific knowledge graphs。
  - 每个 domain 包含 9–77 个 community reports（社区摘要），形成结构化知识图。
- **人工构造查询集**：共生成 100 个自然语言 query，其中 89 个被有效回答。
  - 查询按涉及 domain 数量分为五类：
    - 单域（1 country）
    - 小规模多域（5 countries）
    - 中等规模（10）
    - 大规模（20）
    - 超大规模（40）

---

### 实验设置和评估指标

#### 实验平台配置
- GPU: NVIDIA RTX 3070 (9GB VRAM)
- CPU: 16 vCPUs (AMD EPYC 7B12), 31GB RAM

#### 评估维度（由 GPT-4o 作为 judge 打分，0–100 分制）
| 指标 | 含义 |
|------|------|
| **Comprehensiveness (Comp.)** | 回答涵盖相关方面的完整程度 |
| **Diversity (Div.)** | 来源视角和信息来源的多样性 |
| **Empowerment (Emp.)** | 是否提供可操作洞察，支持后续探索 |
| **Directness (Dir.)** | 表达简洁清晰，避免冗余 |
| **Overall Quality** | 上述四项平均得分 |

#### 成本指标
- **Time (s)**：端到端响应时间（秒）
- **Token Count**：总处理 token 数（输入 + 输出）

---

### 基线方法对比

分为两类部署模式：

#### ✅ Centralized Baselines（所有数据合并为单一 domain）
| 方法 | 描述 |
|------|------|
| **GraphRAGLocal** | 局部检索，聚焦实体级细节 |
| **GraphRAGGlobal** | 全局检索，获取层次化概览 |
| **GraphRAGDRIFT-c** | 全局初检 + 两轮本地 refine（每轮 3 次 follow-up）|

#### ✅ Decentralized Baselines（数据分布于 45 个独立 domain）
| 方法 | 描述 |
|------|------|
| **GraphRAGDRIFT-dec** | 每个 domain 独立运行 DRIFT（全局+两次 refine），最后融合答案 |

> ⚠️ 注意：SCOUT-RAG 是唯一专为 **distributed + agentic + cost-aware** 设计的方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| Method | Comp. | Div. | Emp. | Dir. | **Overall** | Time (s) | Token |
|--------|-------|------|------|------|------------|----------|--------|
| GraphRAGLocal | 65 | 55 | 35 | 58 | 53 | 34.40 | 11,223 |
| GraphRAGGlobal | 60 | 50 | 30 | 55 | 49 | 45.89 | 640,574 |
| GraphRAGDRIFT-c | 72 | 70 | 45 | 63 | **63** | 231.85 | 693,731 |
| GraphRAGDRIFT-dec | 90 | 88 | 75 | 88 | **85** | 414.88 | 879,911 |
| **SCOUT-RAG (Ours)** | 65 | 60 | 40 | 58 | **56** | **75.32** | **159,169** |

---

### 与基线方法的对比结果

#### 📈 性能方面
- SCOUT-RAG 的 overall quality 达到 **56**，虽低于 GraphRAGDRIFT-dec（85），但显著优于大多数 centralized 方法（如 GraphRAGGlobal 的 49）。
- 与 centralized DRIFT（63）相比仅低 7 分，但在分布式架构下已属优异表现。

#### 💰 成本效率方面（最大亮点）
- **相比 GraphRAGDRIFT-dec**：
  - 减少 **81.8% 时间**（75.32s vs 414.88s）
  - 减少 **81.9% token 消耗**（159k vs 879k）
- **相比 GraphRAGDRIFT-c**：
  - 时间快 **3.1x**
  - Token 少 **4.4x**
- 在 **very large multi-domain queries（40 domains）** 下仍保持稳定响应。

#### 🔍 多样性优势
- SCOUT-RAG 在 **Diversity（60）** 上超过所有 centralized baseline（最高仅 70），说明其能更系统地整合多元观点。

---

### 消融实验与案例分析（见 Figure 4）

#### Case Study：“Made in Italy” 认证对时尚产业 SME 的影响
- **Stage I**：准确识别出 Italy（0.539）、Slovenia（0.384）、Malta（0.342）等高相关 domain。
- **Stage II**：初步合成回答，但缺少政策细节与国际比较。
- **Stage III**：
  - 第一轮 Depth refinement 提升 completeness 至 0.75。
  - 第二轮达到最高质量 **0.725**。
  - 第三轮尝试 Breadth 扩展 POTENTIAL domains，反而引入噪声，质量下降至 0.675。
- ✅ **Best-answer tracking 机制成功保留最优版本**，防止退化。

> 结论：迭代 refinement + best-track 是关键设计。

---

## 4. 关键结论和发现

### 主要发现
1. **SCOUT-RAG 实现了性能与成本之间的高效平衡**：
   - 在分布式环境下，以不到 1/5 的资源消耗，达到了接近 centralized DRIFT 的效果。
2. **agent-based 架构适合复杂、受限的 retrieval 场景**：
   - 序列决策 + utility estimation 使系统具备“认知导航”能力。
3. **zero-shot domain relevance estimation 可行且有效**：
   - 结合 semantic、historical、richness 信号可在无监督情况下实现合理排序。
4. **iterative refinement + best-answer tracking 提升鲁棒性**：
   - 即使某些 retrieval 步骤失败或引入噪声，也能保证最终输出不劣化。

---

### 方法的局限性
1. **依赖 domain-level metadata**：
   - 如 historical performance 和 knowledge richness 需要一定历史积累，极端冷启动时可能不准。
2. **未考虑 domain 间语义重叠或冲突**：
   - 多个 domain 返回矛盾信息时，fusion logic 可能不足。
3. **parallel execution 受最慢 domain 影响**：
   - 尽管有并行机制，但整体延迟仍受制于响应最慢的 domain。
4. **evaluation 依赖 LLM-as-a-judge（GPT-4o）**：
   - 存在主观偏差风险，需进一步 human evaluation 验证。

---

### 未来工作方向
1. **引入 reinforcement learning 优化策略选择**：
   - 当前 strategy selection 基于启发式规则，可用 RL 进一步自动化。
2. **增强 conflict resolution 与证据加权机制**：
   - 对不同 domain 的可信度建模，提升 answer consistency。
3. **支持 streaming domain 加入/退出**：
   - 实现 truly dynamic federation。
4. **扩展至非文本模态 knowledge graphs**：
   - 如图像、传感器数据等 multi-modal distributed domains。

---

## 总结

✅ **SCOUT-RAG 是首个将 agentic AI 成功应用于分布式 Graph-RAG 的框架**，在 **privacy-preserving、cost-aware、scalable retrieval** 方面树立了新标杆。  
🎯 它证明了：即使没有全局知识图，也能通过智能 agent 协作实现接近 centralized 系统的质量，同时节省超 80% 的资源开销。  
🚀 该工作为医疗、金融、政府等高敏感领域的 RAG 应用提供了可行路径，推动 Graph-RAG 向真实世界落地迈进一大步。

</details>

---

### 12. [Revealing the Challenges of Attention-FFN Disaggregation for Modern MoE Models and Hardware Systems](https://arxiv.org/abs/2602.09721)

**Authors**: Guowei Liu, Hongming Li, Yaning Guo, Yongxi Lyu, Mo Zhou, Yi Liu, Zhaogeng Li, Yanpeng Wang  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.09721v1  

#### Abstract
Deploying large-scale MoE models presents challenges in memory capacity and bandwidth for expert activation. While Attention-FFN Disaggregation (AFD) has emerged as a potential architecture to decouple compute and memory resources, its performance boundaries compared to standard large-scale Expert P...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Revealing the Challenges of Attention-FFN Disaggregation for Modern MoE Models and Hardware Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本论文系统性地分析了 **Attention-FFN Disaggregation (AFD)** 在现代大规模 **MoE (Mixture-of-Experts)** 模型部署中的实际性能边界与挑战。尽管 AFD 被业界视为提升计算效率的“灵丹妙药”，但其在真实硬件系统上的有效性尚未被深入量化。

具体而言，论文揭示了以下未被充分研究的问题：
- AFD 是否真的能提升 **Hardware FLOPS Utilization (HFU)**？
- 在标准集群（如基于 H800）上，AFD 是否受限于通信瓶颈？
- AFD 对 **DP imbalance** 和 **EP imbalance** 的容忍度如何？

---

### 🚀 提出的新方法与思路
论文提出了一个**基于延迟预算的 HFU 上界分析框架**，并将其扩展至通信层面的 **roofline model**，用于建模 AFD 的理论性能极限。

#### 主要创新点包括：
1. **Budget-based HFU Upper-Bound 分析法**
   - 将 SLO（Service-Level Objective）转化为每阶段的 latency budget `tB`。
   - 推导出 AFD 中 attention (`ta`)、FFN (`tf`) 和通信 (`tc`) 的理想均衡条件：
     ```
     2×ta ≥ tf + tc  
     2×tf ≥ ta + tc  
     ta, tf, tc ≤ tB
     ```
   - 揭示了 AFD 存在“**死区 (dead zone)**”——即增加 FFN 节点数无法提升 HFU，因为计算受 scale-out 带宽限制。

2. **引入 Temporal Sparsity (St) 和 Operator FLOPS Utilization (OFU) 指标**
   - 更精细刻画算子利用率：`HFU = OFU × St`
   - 区分“活跃期间的算力利用”与“整体时间内的资源占用”。

3. **首次量化 AFD 对负载不均衡的敏感性**
   - 发现 AFD 的离散节点级扩展（discrete node-level scaling）相比 large-scale EP 的连续 batch 调整，对 **DP/EP imbalance** 更为脆弱。

---

### 🔍 相比现有方法的优势
| 方面 | 现有研究局限 | 本文改进 |
|------|----------------|-----------|
| 分析粒度 | 多为端到端吞吐预测，缺乏模型-硬件联合建模 | 引入通信级 roofline 模型，支持跨平台推演 |
| 性能假设 | 忽略通信开销、pipeline bubbles 和 imbalance | 显式建模 `tc`, `St`, 并分析不平衡惩罚 |
| 结论普适性 | 常宣称 AFD “always better” | 指出 AFD 是特定场景下的优化，非通用解 |

---

## 2. 核心实验方法和设置

### 📚 使用的模型与硬件平台（非传统“数据集”）
由于是系统级分析，实验基于多个主流 MoE 模型和 GPU 架构进行建模与仿真：

#### 模型配置（来自附录 B）
| Model | Parameters | #Experts | TopK | Expert Granularity | Sparsity |
|-------|------------|----------|------|--------------------|---------|
| DeepSeek-V3 | 671B | 256 | 8 | 2048 | 32 |
| Kimi-K2 | ~1T | 384 | 8 | 2048 | 48 |
| Step3 | — | 48 | 3 | 5120 | 16 |
| ERNIE-4.5 | 300B | 64 | 8 | 3584 | 8 |
| GLM-4.7 | — | 160 | 8 | 1536 | 20 |

> 注：参数来自公开技术报告（如 [6][15][17] 等）

#### 硬件平台对比
| Platform | FP8 FLOPS (TFLOPS) | Scale-Out BW (GB/s) | Scale-Up BW (GB/s) | Node GPUs |
|---------|---------------------|------------------------|----------------------|-----------|
| H20 | 296 | 50 | 360 | 8 |
| H800 | 1979 | 50 | 160 | 8 |
| H100 | 1979 | 50 | 360 | 8 |
| GB200 / GB300 (Superpod) | 4500 | Superpod-level | 720 | 8 |

---

### 📊 实验设置与评估指标

#### 评估指标
| 指标 | 定义 | 说明 |
|------|------|------|
| **HFU** | `FLOPs / (FLOPS × tB)` | 整体硬件 FLOPS 利用率 |
| **OFU** | `FLOPs / (FLOPS × 实际执行时间)` | 算子活跃期内的利用率 |
| **St (Temporal Sparsity)** | `执行时间 / tB` | 衡量 GPU 占用密度 |
| **Arithmetic Intensity (I)** | `FLOPs / Mem Access` | 决定是否 compute-bound 或 memory-bound |
| **Throughput Penalty α** | 不平衡下吞吐下降比例 | 用于比较 AFD vs large-scale EP 的鲁棒性 |

#### 关键假设
- 所有层采用 **3BO (Three-Batch Overlap)** 以隐藏通信延迟。
- `Laccept = 1.7`（启用 Multi-Token Prediction, MTP）。
- `tg = 15ms`（gap latency，含 dense 层和调度开销）。
- SLO 对应 TPOT = 50ms ⇒ 可反推出 `tB`。

#### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Large-scale EP** | 当前主流方案，在 decode 实例中使用 EP 并聚合多 DP batch |
| **AFD (Attention-FFN Disaggregation)** | 本文分析对象，attention 与 FFN 物理分离部署 |
| **PD Disaggregation (Prefill-Decode)** | 作为背景参考，非直接对比对象 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据与对比结果

#### ✅ 图4：不同硬件平台上 AFD 的理论 HFU 上限
| 平台 | 最大 HFU（典型值） | 是否通信受限 |
|------|------------------|--------------|
| H20 | ~80% | 否（compute-limited） |
| H800 | **仅 33.1%** | 是（严重通信瓶颈） |
| GB200 / GB300 | **高达 65.5%** | 否（带宽充足） |

> 💡 在 H800 上即使假设 OFU=100%，HFU 仍远低于 large-scale EP 的约 60%，说明 **AFD 在普通集群上难以超越 EP**

#### ✅ 图2：Arithmetic Intensity 随 FFN 节点数变化趋势
揭示四个区域：
1. **Scale-up bound 区域**：小 `NF`，受限于 node 内 NVLink 带宽
2. **Stable intensity 区域**：强度不变
3. **Scale-out bound 区域**：增大 `NF` 可提升强度
4. **Maximum intensity 区域**：已达上限，再增无益

> ⚠️ 存在“**dead zone**”：当 `NF > TopK` 后，`Brank` 不再增长 ⇒ FLOPs 上限锁定

#### ✅ 图6：EP Imbalance 下的吞吐惩罚对比
| 条件 | AFD 吞吐惩罚 α_AFD | Large-scale EP α_EP |
|------|------------------|---------------------|
| `o=0.8`, `NA/NF=4` | ≈0.8 | ≈0.82 |
| 其他多数情况 | **更差** | 更优 |

> ❗ AFD 因需整数倍调整 `NA`（离散 scaling），导致资源分配存在**量化误差**，鲁棒性更差

#### ✅ 消融实验发现（隐含在分析中）
- **通信模式影响显著**：
  - Two-stage forwarding 比 point-to-point 更高效利用 scale-up 带宽
- **模型结构决定潜力**：
  - Coarse-grained expert（如 Step3, M=5120）比 fine-grained（如 DeepSeek-V3, M=2048）更适合 AFD
- **Sparsity 越低越好**：
  - 低 sparsity ⇒ 更高 token concentration ⇒ 更高 per-expert batch size ⇒ 更高 HFU

---

## 4. 关键结论和发现

### 🎯 主要发现
1. **AFD 并非万能解，存在“死区”**
   - 在标准集群（如 H800）上，由于 scale-out 带宽有限，增加 FFN 实例无法提高 HFU。
   - 此时系统陷入 **network-bound** 状态，operator-level 优化无法转化为整体收益。

2. **AFD 对负载不均衡更敏感**
   - 由于必须按节点粒度扩缩容（discrete scaling），面对 DP/EP imbalance 时无法像 large-scale EP 那样通过微调 batch size 来补偿。
   - 导致 pipeline bubbles 更易传播，性能下降更快。

3. **AFD 成功的前提条件明确**
   - ✅ **Superpod-class 硬件**（如 GB200/GB300）：提供充足 interconnect 带宽
   - ✅ **粗粒度专家 (coarse-grained experts)**：更高的 M 提升 arithmetic intensity
   - ✅ **较低专家稀疏性 (lower sparsity)**：提升 per-rank token 数量
   - ✅ **异构资源配置能力**：为 attention 和 FFN 分配不同类型 GPU

4. **当前 MoE 设计趋势与 AFD 相悖**
   - 当前模型趋向 fine-grained experts 和 high sparsity 以提升训练效率，但这反而削弱 AFD 的优势。

---

### ⚠️ 方法的局限性
| 局限 | 说明 |
|------|------|
| 依赖理想化假设 | 如 `tg=15ms` 可能在真实系统中偏乐观（PCIe/CUDA Graph 开销） |
| 忽略动态调度复杂性 | 未建模请求路由、buffer 管理等 runtime overhead |
| 缺乏真实部署验证 | 多为理论建模与 unit test extrapolation，缺少全栈实测 |
| 假设完美 overlap | 3BO 要求严格同步，现实中 jitter 易破坏 pipeline |

---

### 🔮 未来工作方向
1. **Model-Hardware Co-design**
   - 设计更适合 AFD 的 MoE 架构（如更大 M、更低 sparsity）
   - 开发支持灵活 partitioning 的 Superpod 系统

2. **优化通信库实现**
   - 改进 two-stage forwarding 与 shuffle kernels
   - 探索 **Transformation-Communication Fusion**（如 FUSCO [26]）

3. **探索 EaaS (Experts-as-a-Service) 架构**
   - 将 MoE 层完全服务化，实现更高弹性
   - 适用于中小规模模型先行落地

4. **动态自适应调度机制**
   - 根据实时负载自动选择 AFD / large-scale EP / hybrid 模式

---

## ✅ 总结一句话
> **AFD 不是通用加速器，而是一种在 Superpod 级硬件 + 粗粒度 MoE 模型组合下才可能释放潜力的专用架构；在当前主流配置下，它往往因通信瓶颈和离散扩展问题表现不如 large-scale EP。**

</details>

---

### 13. [Efficient Remote Prefix Fetching with GPU-native Media ASICs](https://arxiv.org/abs/2602.09725)

**Authors**: Liang Mi, Weijun Wang, Jinghan Chen, Ting Cao, Haipeng Dai, Yunxin Liu  
**Category**: cs.DC  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.09725v1  

#### Abstract
Remote KV cache reuse fetches KV cache for identical contexts from remote storage, avoiding recomputation, accelerating LLM inference. While it excels in high-speed networks, its performance degrades significantly in bandwidth-limited scenarios. Recent studies address this by transmitting KV caches ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient Remote Prefix Fetching with GPU-native Media ASICs

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

在大规模语言模型（LLM）推理中，**远程 KV cache 复用**（remote KV cache reuse）是一种通过从远程存储获取已计算的 KV cache 来避免重复计算、加速推理的技术。然而，在带宽受限的网络环境下，该技术面临以下瓶颈：

- **传输开销大**：原始 KV cache 数据量巨大，导致网络传输延迟高。
- **压缩后解压代价高**：现有压缩方法（如 CacheGen、ShadowServe）虽然减少了传输量，但解压过程占用 GPU 资源（CUDA cores），造成与 LLM 推理引擎的资源竞争，反而增加端到端延迟（尤其是 Time to First Token, TTFT）。
- **硬件成本高**：部分方案依赖 SmartNIC 等专用硬件进行解压（如 ShadowServe），部署成本高昂。

因此，如何实现**高效、低成本、无干扰**的远程 KV cache 复用成为关键挑战。

---

### 提出了什么新方法或新思路

本文提出 **KVFetcher**，一种基于 **GPU-native 视频编解码器**（如 NVIDIA NVENC/NVDEC）的远程 KV cache 复用系统，其核心创新在于：

#### （1）**Codec-friendly Tensor Layout**  
将 KV cache 张量重新组织为适合视频编码的格式，以最大化压缩率：
- **跨帧布局（Inter-frame layout）**：沿 token 维度切分 KV cache，并将其分布到连续视频帧中，利用视频编码中的帧间预测（inter-frame prediction）消除冗余。
- **帧内布局（Intra-frame layout）**：优化张量内部元素排列方式，提升帧内压缩效率，遵循三大规则：
  - 不跨 attention head 交换元素；
  - 保持 head 内部元素顺序；
  - 固定 attention head 顺序。
- 使用 **H.265 lossless 模式** 编码，跳过有损的 DCT 和量化步骤，确保精度无损。

#### （2）**Efficient Remote KV Fetcher**  
设计了一套高效的流水线机制，实现低延迟、无干扰的数据获取与恢复：
- **Fetching-aware Scheduler**：将需要 KV 获取的请求与普通请求隔离，避免非复用请求被阻塞（解决 Head-of-Line Blocking）。
- **Adaptive-resolution KV Fetching**：根据实时网络带宽动态调整视频分辨率，平衡传输与解码时间，减少流水线气泡。
- **Frame-wise Tensor Restoration**：逐帧恢复 KV cache 到页式内存（paged memory），显著降低峰值内存占用（仅需 ~70MB），避免与推理任务争抢内存。

---

### 相比现有方法的优势

| 方面 | KVFetcher | CacheGen | ShadowServe |
|------|---------|--------|-----------|
| **压缩率** | ✅ 最高（最高达 11.9×） | ❌ 较低（算术编码） | ❌ 类似 |
| **解压资源** | ✅ 使用独立 NVDEC 单元，无干扰 | ❌ 占用 CUDA cores，严重干扰推理 | ✅ 使用 SmartNIC，无干扰 |
| **部署成本** | ✅ 零额外成本（利用已有 GPU 编解码器） | ✅ 可行 | ❌ 昂贵（需 SmartNIC） |
| **TTFT 改进** | ✅ 最优（最高提速 3.51×） | ⚠️ 有限（受资源竞争限制） | ⚠️ 受限于硬件普及性 |

> **核心优势总结**：KVFetcher 是首个充分利用 GPU 内置媒体 ASIC 进行 KV cache 编解码的工作，在不牺牲精度的前提下，实现了**高吞吐、低延迟、零干扰、低成本**的远程前缀获取。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

论文在三个长上下文基准测试集上评估性能，涵盖多种任务类型和上下文长度：

| 数据集 | 任务类型 | 上下文长度范围 | 特点 |
|-------|--------|-------------|------|
| **L-Eval** | 封闭式问答（QA） | 3K–200K tokens | 包含长文档理解任务 |
| **LV-Eval** | 单跳/多跳 QA | 16K–256K tokens | 加入干扰项、事实混淆等挑战 |
| **LongBench-V2** | 多样化任务（QA、对话、代码、结构化数据） | 13K–167K tokens | 多选题形式，保证评估客观性 |

---

### 实验设置和评估指标

#### 模型
- **LWM-7B**（1M context）
- **Yi-34B**（200K context）
- **Llama-3-70B**（128K context）

#### 硬件平台（异构 GPU 集群）
| GPU 类型 | 显存 | NVDEC 数量 | 用途 |
|--------|-----|----------|-----|
| **NVIDIA L20**（低端） | 48GB | 3 | 成本敏感场景 |
| **NVIDIA H20**（中端） | 96GB | 7 | 主流云服务典型配置 |
| **NVIDIA A100**（高端） | 80GB | 5 | 高性能计算环境 |

#### 网络带宽
- 模拟真实云环境，测试 **1–40 Gbps TCP** 和 **100/200 Gbps RDMA** 场景。

#### 评估指标
| 指标 | 定义 |
|------|------|
| **TTFT**（Time to First Token） | 用户请求发出到生成第一个 token 的时间，核心延迟指标 |
| **TPOT**（Time Per Output Token） | 解码阶段每个输出 token 的平均耗时 |
| **Compression Ratio** | KV cache 压缩前后大小之比 |
| **Accuracy** | 各数据集标准指标（如匹配率、F1-score） |
| **Peak GPU Memory Usage** | 解压过程中最大显存占用 |

---

### 基线方法对比

| 基线方法 | 描述 |
|--------|------|
| **Full Prefill** | 不使用任何 KV cache 复用，完全重新计算 |
| **Raw KV Reuse** | 直接传输原始 KV cache（如 Mooncake） |
| **CacheGen** | 使用 CUDA kernel 进行算术编码压缩与解压 |
| **ShadowServe** | 将解压卸载至 SmartNIC |
| **llm.265** | 并行研究，尝试用视频编码压缩 KV cache，但未充分优化 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ **TTFT 显著降低**
- 在典型 16 Gbps 网络 + Yi-34B 模型下：
  - KVFetcher 相比 **CacheGen** 平均降低 **3.51× TTFT**
  - 相比 **Full Prefill** 降低 **13.63× TTFT**
  - 在不同模型和设备上，TTFT 减少 **1.52–3.51×**

#### ✅ **非复用请求也受益**
- 对于无需 KV 复用的请求，KVFetcher 仍能：
  - **降低 77.1% TTFT**（vs CacheGen）
  - **降低 35.4% TPOT**（vs CacheGen）
- 原因：解压不抢占 CUDA 资源，调度器隔离 fetch 请求，避免 HOL blocking。

#### ✅ **压缩率领先且精度无损**
- **平均压缩率达 11.9×**，远超 CacheGen 和 ShadowServe（约 5–6×）
- 相比 CacheGen 提升 **2.17× 压缩率**
- 所有任务保持 **lossless accuracy**，而 llm.265 存在精度下降。

#### ✅ **内存开销极低**
- 并发处理 7 个视频块时，峰值 GPU 内存仅 **400MB**
- 解压缓冲区小于 **70MB**（CacheGen 需 5.5GB 解压空间）

#### ✅ **适应动态带宽能力强**
- 自适应分辨率机制使流水线气泡减少 **21%**
- 动态切换 240P/480P/1080P 分辨率，有效应对网络抖动。

---

### 消融实验结果

| 组件 | 对压缩率的影响 | 说明 |
|------|----------------|------|
| **Quantization only** | 基线（~5.8×） | 仅整数量化 |
| **+ Inter-frame layout** | → **~12.8×** | token 维度切分 + 连续帧布局大幅提升压缩 |
| **+ Intra-frame layout** | → **~17.1×** | 张量重排进一步挖掘冗余 |
| **最终方案（含解码优化）** | 实际 **11.9×** | 受限于实际解码吞吐，但仍最优 |

> 注：消融显示 inter-frame layout 贡献最大压缩增益（平均 2.2×），intra-frame layout 进一步提升 1.35×。

---

## 4. 关键结论和发现

### 论文的主要发现

1. **GPU-native 视频编解码器是被忽视的强大资源**：现代 GPU 普遍配备独立的 NVENC/NVDEC 单元，在 LLM 推理中完全空闲，可用于高性能、无干扰的 KV cache 编解码。
2. **合理的 tensor-to-video 映射可极大提升压缩率**：简单地将 KV 张量视为图像帧效果差；必须结合 LLM 架构特性（如 token 间相似性高）设计 codec-friendly layout。
3. **系统级协同设计至关重要**：仅压缩不够，还需调度、自适应分辨率、帧级恢复等机制配合，才能实现最小 TTFT。
4. **KVFetcher 在真实场景中优势明显**：尤其适用于中低端 GPU + 有限带宽的经济型部署环境。

---

### 方法的局限性

1. **依赖 NVDEC 资源数量**：
   - 低端 GPU（如 L20）仅有 3 个 NVDEC，可能成为并发解码瓶颈。
   - 当前解码吞吐受限于硬件并行能力，无法像 CUDA 那样弹性扩展。

2. **不支持在线压缩（Online Compression）**：
   - 当前设计为离线压缩（offline encoding），不适合 P-D disaggregation 或故障迁移等需运行时压缩的场景。
   - NVENC 资源有限，难以满足实时压缩需求。

3. **预分配内存策略可能影响公平性**：
   - 为 fetch 请求预分配大量 GPU 内存，可能导致非复用请求因内存不足被延迟。

---

### 未来工作方向

1. **下一代 GPU 编解码器增强**：
   - 期待未来 GPU 提供更多、更强的 NVENC/NVDEC 单元，支持更高并发的在线压缩。

2. **智能内存管理机制**：
   - 将 fetched KV cache 先写入本地 SSD 或 host memory，按需换入 GPU，缓解内存压力。

3. **支持运行时 KV 压缩**：
   - 结合轻量级编码策略，在 prefill 阶段即时压缩 KV cache，用于节点间传输或容错备份。

4. **扩展至其他模态模型**：
   - 将该思想推广至 Vision-Language Model 或多模态 agent 中的中间状态复用。

---

> **总体评价**：KVFetcher 是一项极具实用价值的系统创新，巧妙利用现有硬件资源解决了 LLM 推理中的关键性能瓶颈，具备良好的可部署性和广泛适用前景。

</details>

---

### 14. [Epistemic Throughput: Fundamental Limits of Attention-Constrained Inference](https://arxiv.org/abs/2602.09127)

**Authors**: Lei You  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.09127v1  

#### Abstract
Recent generative and tool-using AI systems can surface a large volume of candidates at low marginal cost, yet only a small fraction can be checked carefully. This creates a decoder-side bottleneck: downstream decision-makers must form reliable posteriors from many public records under scarce attent...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Epistemic Throughput: Fundamental Limits of Attention-Constrained Inference**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
该论文研究的是在**生成式人工智能时代下，信息验证成本远高于生成成本所导致的“注意力瓶颈”问题**。具体而言，在高风险领域（如科学发现、金融情报、软件供应链等），决策者需要从海量候选记录中识别出少量真实、可靠的信息（即“真值”），但其**验证能力（verification）受限于稀缺的注意力资源**。

传统通信理论关注信道容量和传输可靠性，而本文提出了一种新的范式——**Attention-Constrained Inference (ACI)**，聚焦于**如何在有限的验证预算 $B$ 下，通过大规模低成本筛选（screening）来最大化对潜在真相的推断效率**。

---

### **提出了什么新方法或新思路**

论文提出了以下核心概念与理论框架：

- **Epistemic Throughput（认知吞吐量）**  
  定义为在每个时间窗口内，系统所能实现的最大后验不确定性减少量（即信息增益），衡量单位验证资源下的知识获取效率。

- **JaKoB Scaling Law（JaKoB缩放定律）**  
  在 Bayes log-loss 度量下，证明了信息增益满足如下基本极限：
  $$
  \text{Gain} \sim I_{\text{ver}} \left( Bp + \sqrt{JKB} \right)
  $$
  其中：
  - $B$: 验证预算（deep attention）
  - $K$: 筛选规模（broad attention）
  - $p$: 有价值记录的先验概率（prevalence）
  - $J = I(T;Z)$: 筛选质量（screening quality），即筛选信号 $Z$ 与真实类型 $T$ 的互信息
  - $I_{\text{ver}}$: 每次成功验证带来的信息贡献

  这一定律揭示了一个非线性放大效应：即使筛选质量 $J$ 很弱，只要能以高比例 $\frac{K}{B}$ 过采样（oversampling），就可以显著提升有效验证产出。

- **Tail Leverage Theory（尾部杠杆理论）**  
  在稀疏验证场景（$B \ll K$）中，筛选的有效性取决于评分分布的上尾行为：
  - 若评分呈轻尾分布（如 Gaussian），则增益仅随 $\log(K/B)$ 增长；
  - 若评分呈重尾分布（如 Pareto），则可实现多项式级杠杆放大（polynomial leverage）。

- **Score-Based Verification Policy 的可实现性**  
  证明简单的“top-$B$”策略（基于贝叶斯得分排序并验证前 $B$ 名）在弱筛选假设下即可逼近 JaKoB 上限，具有实际可行性。

---

### **相比现有方法的优势**

| 维度 | 传统方法 | 本文 ACI 框架 |
|------|--------|-------------|
| **目标** | 数据传输可靠性（bit rate）或语义一致性 | **Truthfulness**：最大化经验证据支持的真实信念形成速率 |
| **瓶颈建模** | 编码端带宽限制 | **解码端注意力约束**（verification scarcity） |
| **分析工具** | 信道容量、错误率 | **信息论 + 决策理论**：结合 mutual information、order statistics 和 extreme-value theory |
| **设计启示** | 提升编码效率 | 引导系统设计朝向**产生易验证、高信息量输出**，鼓励重尾评分机制 |

---

## 2. **核心实验方法和设置**

### **使用了哪些数据集**
本研究是**理论驱动型工作**，未使用真实世界 benchmark 数据集进行训练或测试。而是构建了一个**可控的仿真环境**来验证理论边界。

- **模拟设置**：采用 logistic regression 模型生成 screening scores：
  $$
  \pi_i = \sigma(\text{logit}(p_0) + \epsilon G_i)
  $$
  其中 $G_i \sim \mathcal{N}(0,1)$，$\epsilon$ 控制筛选强度，对应不同 AUC 水平（0.55 ~ 0.90）。
- 参数设定：$K=10^4$, $B \in [1, 1000]$, $p_0 = 0.01$

---

### **实验设置和评估指标**

#### **评估指标**
- **Information Gain（信息增益）**：定义为初始熵减去最终条件熵：
  $$
  \Delta H = H(O) - D(K,B)
  $$
  单位为 bits，等价于 Bayes log-loss 下的认知改进。
- **Epistemic Throughput**：归一化后的信息增益，反映每单位验证的成本效益。

#### **实验流程**
1. 生成 $K$ 条记录的 screening scores $\{Z_i\}$
2. 计算贝叶斯得分 $\pi(Z_i) = P(T=1|Z_i)$
3. 执行 top-$B$ verification policy
4. 观测实际获得的 informative records 数量及信息增益
5. 对比理论预测曲线（benchmark、weak-screening approximation、converse bound）

---

### **基线方法对比**

虽然没有传统意义上的“模型对比”，但实验中隐含比较了多种理想与现实策略：

| 方法 | 描述 |
|------|------|
| **Random Verification** | 盲目随机选择 $B$ 条记录验证，期望增益为 $Bp$ |
| **Oracle Upper Bound** | 已知哪些记录是有价值的，只验证真正 positive 的项，上限为 $\min(B, Kp)$ |
| **Perfect Screening** | 理想分类器完全区分 $T=1$ 与 $T=0$，达到最优选择 |
| **Top-$B$ Score Policy** | 实际执行的策略，按 $\pi(Z)$ 排序后验证前 $B$ 名 |
| **Finite-Pool Oracle** | 考虑池子大小有限时的理想上限 |

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**

- 图4（Fig. 4）展示了四种不同筛选强度（AUC ≈ 0.55, 0.70, 0.79, 0.90）下的实证信息增益曲线。
- 所有设置下，**empirical gain（标记点）均紧贴理论 benchmark 曲线（实线）**，说明理论预测高度准确。
- **weak-screening approximation（虚线）在强筛选时趋于保守**，但在弱筛选区（AUC≈0.55）非常接近。
- **converse bound（点划线）始终高于实证结果**，符合其作为上界的性质。

> ✅ **核心发现**：即使筛选能力很弱（AUC≈0.55），通过扩大 $K$ 并应用 top-$B$ 策略，仍可获得显著高于随机验证的信息增益。

---

### **与基线方法的对比结果**

| 策略 | 信息增益表现 |
|------|------------|
| Random ($Bp$) | 最低，线性增长，斜率为 $p=0.01$ |
| Top-$B$ Score Policy | 显著优于 random，尤其在中高 $B$ 区间 |
| Benchmark Prediction (Thm 17) | 几乎完美拟合实证结果，验证了理论有效性 |
| Converse Bound (Thm 6) | 略高于实证，合理宽松 |
| Oracle | 不可达，但显示潜力空间 |

例如，在 $B=100$ 时：
- Random: ~1 bit
- Top-$B$: ~35–80 bits（依 AUC 而定）
- Oracle: ~100 bits

---

### **消融实验结果（如有）**

论文虽无显式消融实验，但通过参数控制实现了类似分析：

- **改变 $\epsilon$（筛选强度）** → 控制 $J = I(T;Z)$ → 验证 $J$ 对增益的影响
- **改变 $K/B$ 比例** → 分析 oversampling ratio 的杠杆作用
- **改变 score 分布类型（Gaussian vs Pareto）** → 验证 tail leverage 效应（见 Fig. 3）

这些分析共同构成了一组“理论消融”，确认了各因素的关键作用。

---

## 4. **关键结论和发现**

### **论文的主要发现**

1. ✅ **JaKoB Scaling Law 成立**：信息增益由两部分组成：
   - 线性项 $I_{\text{ver}} Bp$：相当于随机验证的基础收益
   - 非线性项 $I_{\text{ver}} \sqrt{JKB}$：来自筛选的质量与规模协同效应

2. ✅ **筛选的价值在于“挑选权”而非直接提供信息**：screening 本身不直接揭示真相，但它通过提高验证集中“金矿”的浓度，间接放大验证资源的价值。

3. ✅ **过采样（Oversampling）是一种关键杠杆**：即使单个筛选器不准，只要能处理大量候选（large $K$），就能从中挑出极少数高质量目标。

4. ✅ **评分分布的尾部特性决定放大上限**：
   - Light-tailed（如 Gaussian）→ 收益递减快，仅对数增长
   - Heavy-tailed（如 Pareto）→ 可实现幂律加速，适合大规模筛选

5. ✅ **简单策略即可逼近理论极限**：top-$B$ verification policy 在弱筛选下即可实现 $\sqrt{JKB}$ 缩放，无需复杂自适应机制。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **静态、独立同分布假设** | 当前模型假设记录之间独立且 i.i.d.，忽略了序列依赖性和反馈闭环（如用户行为受推荐影响） |
| **单一窗口分析** | 未考虑跨时间步的主动学习或 bandit-style 自适应过程 |
| **Log-loss 作为唯一指标** | 忽略了其他实用目标，如 FDR 控制、决策 regret 或 abstention |
| **验证成本同质化** | 假设所有验证代价相同，现实中可能存在多级审计路径（multi-level verification ladder） |
| **忽略对抗性操纵** | 未建模恶意生产者故意制造高分假记录的行为 |

---

### **未来工作方向**

1. **扩展至 Sequential ACI Setting**  
   将 epistemic throughput 推广到动态、在线环境中，连接 active learning 与 Bayesian experimental design。

2. **引入 Strategic Producers 与 Mechanism Design**  
   构建激励机制，使生产“可验证真理”成为最优策略，防止 cheap talk 泛滥。

3. **Develop Heavy-Tailed Scoring Architectures**  
   设计能够自然产生重尾评分的 ranking functions 或 tool-routing systems，以最大化尾部杠杆。

4. **Design Reusable Artifact Formats**  
   研究如何将验证结果标准化为 public artifacts（如 cryptographic attestations, structured citations），促进跨用户复用，提升整体 epistemic efficiency。

5. **Empirical Validation on Real Systems**  
   在 RAG、tool-using agents、fact-checking pipelines 中测量 $J, K, B$ 等参数，检验 JaKoB 定律的实际适用性。

---

> 🔚 **结语**：本文提出了 **epistemic communication** 作为一种新范式，主张将“truth acquisition rate under attention constraints”作为下一代智能系统的核心优化目标。**Epistemic Throughput** 不仅是一个理论度量，更是一种系统设计哲学：**让每一次深度验证都尽可能地改变我们的信念。**

</details>

---

### 15. [PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition](https://arxiv.org/abs/2602.08240)

**Authors**: Xun Su, Huamin Wang, Qi Zhang  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.08240v1  

#### Abstract
Speech Emotion Recognition (SER) is widely deployed in Human-Computer Interaction, yet the high computational cost of conventional models hinders their implementation on resource-constrained edge devices. Spiking Neural Networks (SNNs) offer an energy-efficient alternative due to their event-driven ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的 **Speech Emotion Recognition (SER)** 系统依赖于密集参数的 **Artificial Neural Networks (ANNs)**，虽然性能优越，但其高计算成本和能耗限制了在资源受限的边缘设备上的部署。  
**Spiking Neural Networks (SNNs)** 虽然具备事件驱动、低功耗的潜力，但在直接应用于自监督学习（SSL）生成的连续特征时面临严重挑战——由于输入特征分布与脉冲神经元的阈值响应机制不匹配，导致神经元陷入“功能静默”（functional silence）或“饱和”（saturation），从而破坏稀疏时间编码能力。

### 🚀 提出的新方法与创新思路
作者提出 **PTS-SNN**（Prompt-Tuned Spiking Neural Network），一种参数高效的类脑适配框架，用于将冻结的 SSL 主干网络（如 emotion2vec）输出的连续表示有效对齐到离散的 SNN 动态中。其核心创新包括：

1. **Temporal Shift Spiking Encoder**  
   - 引入无参数的 **channel shift** 操作，在残差脉冲块中捕捉局部时间依赖关系。
   - 通过零填充和截断实现帧间信息交换，无需额外参数即可平滑高频波动，建立稳定输入基础。

2. **Context-Aware Membrane Potential Calibration**  
   - 设计基于 **Spiking Sparse Linear Attention (SSLA)** 的提示机制，聚合全局语义上下文至可学习的软提示（soft prompts）。
   - 将这些提示转化为动态偏置电压（dynamic bias voltages），调节 **Parametric Leaky Integrate-and-Fire (PLIF)** 神经元的膜电位基线。
   - 生物启发式的调控机制使输入分布中心化于敏感响应区间（responsive firing range），避免静默/饱和。

3. **Prompting for SNNs 的生物物理重构**  
   - 不同于传统 Prompt Tuning 中作为语义嵌入的“additive vectors”，本文将 prompts 视为“homeostatic regulation signals”，即主动调控信号，实现了从连续表征到离散脉冲动力学的无缝桥接。

### 🔍 相比现有方法的优势
- **参数效率极高**：仅需 **1.19M 可训练参数**，远低于全微调或稠密解码器方案。
- **能量消耗极低**：每样本推理能耗仅为 **0.35 mJ**，适合边缘部署。
- **性能媲美先进 ANNs**：在 IEMOCAP 上达到 **73.34% WA**，优于多数轻量级 ANN 模型。
- **无需修改预训练主干**：保持 SSL 主干冻结，符合 Parameter-Efficient Fine-Tuning (PEFT) 范式。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在五个多语言 SER 数据集上进行评估，涵盖不同语言和情感类别：
| 数据集       | 语言     | 情感数 | 语句数 |
|------------|--------|------|------|
| **IEMOCAP** | English | 4    | 5,531 |
| **CASIA**   | Chinese | 4    | 1,200 |
| **EMODB**   | German  | 7    | 535   |
| **EMOVO**   | Italian | 7    | 588   |
| **URDU**    | Urdu    | 4    | 400   |

### ⚙️ 实验设置与评估指标
- **主干模型**：使用预训练的 **emotion2vec** 提取特征，主干参数冻结。
- **优化器**：AdamW，初始学习率 $2 \times 10^{-4}$，Cosine Annealing Warm Restarts。
- **训练配置**：200 epochs，batch size = 32，单张 RTX 4090 GPU。
- **SNN 设置**：采用替代梯度法（surrogate gradient）进行反向传播，陡度因子 $\alpha_{sq}=5$。
- **评估指标**：
  - **Weighted Accuracy (WA)**：总体准确率，反映全局性能。
  - **Unweighted Accuracy (UA)**：各类别平均准确率，适用于类别不平衡场景。

### 🆚 对比的基线方法
- **Vanilla SNN**：直接将 SSL 特征转换为脉冲，无适配模块。
- **emotion2vec + ANN fine-tuning**：标准全微调。
- **ShiftFormer**, **ENT**, **DST**, **MSTR**, **Co-attention**：近期先进的 SER 架构（均为 ANNs）。
- 所有对比均报告参数量（Params）和推理能耗（Energy）。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（以 IEMOCAP 为例）
| 模型             | WA (%) | UA (%) | 参数量 (M) | 推理能耗 (mJ) |
|------------------|--------|--------|-----------|---------------|
| Co-attention     | 69.80  | 71.05  | 175.30    | 103.40        |
| MSTR             | 70.60  | 71.60  | 30.03     | 51.06         |
| DST              | 71.80  | 73.60  | 22.78     | 35.34         |
| ShiftFormer      | 72.10  | 72.70  | 9.50      | 16.20         |
| ENT              | 72.43  | 73.88  | 8.55      | 6.35          |
| **PTS-SNN (Ours)** | **73.34** | **73.72** | **1.19**  | **0.35**      |

> ✅ PTS-SNN 在精度上超越所有对比模型，同时参数量减少约 **87%~99%**，能耗降低 **两个数量级以上**。

### 🌍 跨语言泛化能力（WA %）
| 数据集   | emotion2vec | PTS-SNN | 提升幅度 |
|--------|-------------|---------|--------|
| CASIA  | 69.20       | 72.50   | +3.30% |
| EMODB  | 84.34       | 87.49   | +3.15% |
| EMOVO  | 61.21       | 61.94   | +0.73% |
| URDU   | 81.50       | 84.25   | +2.75% |

> 表明 PTS-SNN 学习的是语言无关的情感韵律特征，具有良好的跨语言迁移能力。

### 🔍 消融实验结果（IEMOCAP）
| 配置                     | WA (%) | UA (%) |
|--------------------------|--------|--------|
| Full Model (PTS-SNN)     | 73.34  | 73.72  |
| w/o Prompt Tuning        | 71.76  | 71.90  |
| w/o Attention Module     | 68.35  | 69.10  |
| Only Temporal Shift      | 61.27  | 62.05  |

> - 移除 **prompt tuning** 导致性能下降 1.58%，说明其对分布校准至关重要。
> - 移除 **attention 模块** 下降 4.99%，验证全局上下文建模的有效性。
> - 仅保留 temporal shift 性能大幅下滑，表明局部建模不足以支撑高性能识别。

### ⚡ 能效分析
- **参数效率**：PTS-SNN 仅需 1.19M 参数，相较 Co-attention 减少 **147 倍**。
- **能耗效率**：每样本仅消耗 **0.35 mJ**，相较 MSTR（51.06 mJ）降低 **145 倍以上**。
- 能耗估算基于 45nm CMOS 工艺，区分 MAC（ANN）与 AC（SNN）操作能耗。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **分布失配是 SNN 应用于 SSL 特征的核心瓶颈**：高动态范围的连续特征易导致 PLIF 神经元进入静默或饱和状态，破坏时间编码能力。
2. **Prompt-based 调控机制可有效解决该问题**：通过将 soft prompts 映射为动态偏置电压，实现输入分布的主动中心化，显著提升 SNN 编码效率。
3. **PTS-SNN 实现了精度与效率的双重突破**：在保持与最先进 ANNs 相当甚至更优性能的同时，极大降低了参数量和能耗，特别适合边缘智能应用。

### ⚠️ 局限性
- 当前框架仅利用 **单模态音频信号**，未融合面部表情、文本等互补信息。
- 所有实验基于离线处理，尚未在真实异步事件流中验证实时性。
- 提示长度（$L_p=5$）和偏置缩放因子（$\kappa=0.5$）需经验调参，缺乏自动化搜索机制。

### 🔮 未来工作方向
- 扩展至 **多模态 Spiking 架构**，融合视觉与文本线索，构建更鲁棒的情绪理解系统。
- 探索 **在线自适应提示机制**，支持动态环境下的持续学习。
- 将该 prompting 范式推广至其他 SNN 下游任务（如语音识别、关键词唤醒等）。

---

> **总结一句话**：  
> PTS-SNN 成功将 Prompt Tuning 范式生物物理化，提出了一种高效、节能且高性能的 SNN 适配框架，为在边缘设备上部署基于 SSL 的情绪识别提供了可行路径。

</details>

---

### 16. [Boltzmann Reinforcement Learning for Noise resilience in Analog Ising Machines](https://arxiv.org/abs/2602.09162)

**Authors**: Aditya Choudhary, Saaketh Desai, Prasad Iyer  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.09162v1  

#### Abstract
Analog Ising machines (AIMs) have emerged as a promising paradigm for combinatorial optimization, utilizing physical dynamics to solve Ising problems with high energy efficiency. However, the performance of traditional optimization and sampling algorithms on these platforms is often limited by inher...

---

### 17. [Train Less, Infer Faster: Efficient Model Finetuning and Compression via Structured Sparsity](https://arxiv.org/abs/2602.09169)

**Authors**: Jonathan Svirsky, Yehonathan Refael, Ofir Lindenbaum  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.09169v1  

#### Abstract
Fully finetuning foundation language models (LMs) with billions of parameters is often impractical due to high computational costs, memory requirements, and the risk of overfitting. Although methods like low-rank adapters help address these challenges by adding small trainable modules to the frozen ...

---

### 18. [Scalable and Reliable State-Aware Inference of High-Impact N-k Contingencies](https://arxiv.org/abs/2602.09461)

**Authors**: Lihao Mai, Chenhan Xiao, Yang Weng  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.09461v1  

#### Abstract
Increasing penetration of inverter-based resources, flexible loads, and rapidly changing operating conditions make higher-order $N\!-\!k$ contingency assessment increasingly important but computationally prohibitive. Exhaustive evaluation of all outage combinations using AC power-flow or ACOPF is in...

---

### 19. [Time Series Reasoning via Process-Verifiable Thinking Data Synthesis and Scheduling for Tailored LLM Reasoning](https://arxiv.org/abs/2602.07830)

**Authors**: Jiahui Zhou, Dan Li, Boxin Li, Xiao Zhang, Erli Meng, Lin Li, Zhuomin Chen, Jian Lou, See-Kiong Ng  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.07830v1  

#### Abstract
Time series is a pervasive data type across various application domains, rendering the reasonable solving of diverse time series tasks a long-standing goal. Recent advances in large language models (LLMs), especially their reasoning abilities unlocked through reinforcement learning (RL), have opened...

---

### 20. [iGRPO: Self-Feedback-Driven LLM Reasoning](https://arxiv.org/abs/2602.09000)

**Authors**: Ali Hatamizadeh, Shrimai Prabhumoye, Igor Gitman, Ximing Lu, Seungju Han, Wei Ping, Yejin Choi, Jan Kautz  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09000v1  

#### Abstract
Large Language Models (LLMs) have shown promise in solving complex mathematical problems, yet they still fall short of producing accurate and consistent solutions. Reinforcement Learning (RL) is a framework for aligning these models with task-specific rewards, improving overall quality and reliabili...

---

### 21. [AlignTune: Modular Toolkit for Post-Training Alignment of Large Language Models](https://arxiv.org/abs/2602.09621)

**Authors**: R E Zera Marveen Lyngkhoi, Chirag Chawla, Pratinav Seth, Utsav Avaiya, Soham Bhattacharjee, Mykola Khandoga, Rui Yuan, Vinay Kumar Sankarapu  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09621v1  

#### Abstract
Post-training alignment is central to deploying large language models (LLMs), yet practical workflows remain split across backend-specific tools and ad-hoc glue code, making experiments hard to reproduce. We identify backend interference, reward fragmentation, and irreproducible pipelines as key obs...

---

### 22. [MATA: Multi-Agent Framework for Reliable and Flexible Table Question Answering](https://arxiv.org/abs/2602.09642)

**Authors**: Sieun Hyeon, Jusang Oh, Sunghwan Steve Cho, Jaeyoung Do  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09642v1  

#### Abstract
Recent advances in Large Language Models (LLMs) have significantly improved table understanding tasks such as Table Question Answering (TableQA), yet challenges remain in ensuring reliability, scalability, and efficiency, especially in resource-constrained or privacy-sensitive environments. In this ...

---

### 23. [Unsupervised Layer-Wise Dynamic Test Time Adaptation for LLMs](https://arxiv.org/abs/2602.09719)

**Authors**: Longhuan Xu, Cunjian Chen, Feng Yin  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09719v1  

#### Abstract
Test-time adaptation (TTA) for large language models (LLMs) updates model parameters at inference time using signals available at deployment. This paper focuses on a common yet under-explored regime: unsupervised, sample-specific TTA, where the model adapts independently for each prompt using only t...

---

### 24. [Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference](https://arxiv.org/abs/2602.10021)

**Authors**: Wenxuan Xie, Yujia Wang, Xin Tan, Chaochao Lu, Xia Hu, Xuhong Wang  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10021v1  

#### Abstract
The integration of extensive, dynamic knowledge into Large Language Models (LLMs) remains a significant challenge due to the inherent entanglement of factual data and reasoning patterns. Existing solutions, ranging from non-parametric Retrieval-Augmented Generation (RAG) to parametric knowledge edit...

---

### 25. [Stabilizing Physics-Informed Consistency Models via Structure-Preserving Training](https://arxiv.org/abs/2602.09303)

**Authors**: Che-Chia Chang, Chen-Yang Dai, Te-Sheng Lin, Ming-Chih Lai, Chieh-Hsin Lai  
**Category**: cs.LG  
**Published**: 2026-02-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09303v1  

#### Abstract
We propose a physics-informed consistency modeling framework for solving partial differential equations (PDEs) via fast, few-step generative inference. We identify a key stability challenge in physics-constrained consistency training, where PDE residuals can drive the model toward trivial or degener...

---

### 26. [Steer2Adapt: Dynamically Composing Steering Vectors Elicits Efficient Adaptation of LLMs](https://arxiv.org/abs/2602.07276)

**Authors**: Pengrui Han, Xueqiang Xu, Keyang Xuan, Peiyang Song, Siru Ouyang, Runchu Tian, Yuqing Jiang, Cheng Qian, Pengcheng Jiang, Jiashuo Sun, Junxia Cui, Ming Zhong, Ge Liu, Jiawei Han, Jiaxuan You  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.07276v1  

#### Abstract
Activation steering has emerged as a promising approach for efficiently adapting large language models (LLMs) to downstream behaviors. However, most existing steering methods rely on a single static direction per task or concept, making them inflexible under task variation and inadequate for complex...

---

### 27. [MSP-LLM: A Unified Large Language Model Framework for Complete Material Synthesis Planning](https://arxiv.org/abs/2602.07543)

**Authors**: Heewoong Noh, Gyoung S. Na, Namkyeong Lee, Chanyoung Park  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.07543v2  

#### Abstract
Material synthesis planning (MSP) remains a fundamental and underexplored bottleneck in AI-driven materials discovery, as it requires not only identifying suitable precursor materials but also designing coherent sequences of synthesis operations to realize a target material. Although several AI-base...

---

### 28. [Grounding Generative Planners in Verifiable Logic: A Hybrid Architecture for Trustworthy Embodied AI](https://arxiv.org/abs/2602.08373)

**Authors**: Feiyu Wu, Xu Zheng, Yue Qu, Zhuocheng Wang, Zicheng Feng, Hui Li  
**Category**: cs.AI  
**Published**: 2026-02-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.08373v1  

#### Abstract
Large Language Models (LLMs) show promise as planners for embodied AI, but their stochastic nature lacks formal reasoning, preventing strict safety guarantees for physical deployment. Current approaches often rely on unreliable LLMs for safety checks or simply reject unsafe plans without offering re...

---

### 29. [Advancing Block Diffusion Language Models for Test-Time Scaling](https://arxiv.org/abs/2602.09555)

**Authors**: Yi Lu, Deyang Kong, Jianing Wang, Linsen Guo, Xue Wang, Qi Guo, Tao Gui, Xuanjing Huang, Wei Ye, Shikun Zhang, Wei Wang  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.09555v1  

#### Abstract
Recent advances in block diffusion language models have demonstrated competitive performance and strong scalability on reasoning tasks. However, existing BDLMs have limited exploration under the test-time scaling setting and face more severe decoding challenges in long Chain-of-Thought reasoning, pa...

---

### 30. [Learning from the Irrecoverable: Error-Localized Policy Optimization for Tool-Integrated LLM Reasoning](https://arxiv.org/abs/2602.09598)

**Authors**: Qiao Liang, Yuke Zhu, Chao Ge, Lei Yang, Ying Shen, Bo Zheng, Sheng Guo  
**Category**: cs.CL  
**Published**: 2026-02-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.09598v1  

#### Abstract
Tool-integrated reasoning (TIR) enables LLM agents to solve tasks through planning, tool use, and iterative revision, but outcome-only reinforcement learning in this setting suffers from sparse, delayed rewards and weak step-level credit assignment. In long-horizon TIR trajectories, an early irrecov...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
