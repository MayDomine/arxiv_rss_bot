# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-22 05:58:19 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Taming the Memory Footprint Crisis: System Design for Production Diffusion LLM Serving](https://arxiv.org/abs/2512.17077)

**Authors**: Jiakun Fan, Yanglin Zhang, Xiangchen Li, Dimitrios S. Nikolopoulos  
**Category**: cs.DC  
**Published**: 2025-12-22  
**Score**: 14.5  
**Type**: new  
**ArXiv ID**: 2512.17077v1  

#### Abstract
Diffusion Large Language Models (dLLMs) have emerged as a promising alternative to Autoregressive Models (ARMs), utilizing parallel decoding to overcome sequential bottlenecks. However, existing research focuses primarily on kernel-level optimizations, lacking a holistic serving framework that addre...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Taming the Memory Footprint Crisis: System Design for Production Diffusion LLM Serving

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对 **Diffusion Large Language Models (dLLMs)** 在生产环境中的推理服务所面临的“**内存占用危机**”（Memory Footprint Crisis）展开研究。尽管 dLLMs 利用并行解码克服了自回归模型（AR LLMs）的序列瓶颈，但在实际部署中存在以下系统级挑战：

- **巨大的瞬态激活内存开销**：由于每次迭代需计算整个序列的 logits，导致峰值内存需求极高。
- **资源振荡（Resource Oscillation）**：在 compute-bound 的 “Refresh” 阶段与 bandwidth-bound 的 “Reuse” 阶段之间剧烈波动，传统静态调度器无法有效利用空闲资源。
- **逻辑稀疏性未转化为物理效率**：现有稀疏注意力机制依赖统一掩码或仅逻辑过滤，未能真正减少物理内存占用和带宽消耗。

这些问题导致现有框架难以支持高并发、低延迟的在线服务场景，常出现 OOM 或严重资源浪费。

---

### 提出的新方法与创新点

为解决上述问题，作者提出 **dLLM-Serve** ——首个专为 dLLM 推理设计的端到端高效服务系统，其三大核心技术如下：

#### ✅ **Logit-Aware Activation Budgeting（基于 Logit 的激活预算管理）**
- 将原本单次生成的 `[B, L, V]` 大型 logit 张量沿 token 轴拆分为多个小批量（micro-batching），通过运行时串行处理控制峰值激活内存。
- 这使得系统可在有限显存下显著扩大 KV Cache Pool 容量，从而提升并发能力。

#### ✅ **Phase-Multiplexed Scheduler（相位复用调度器）**
- 不再以请求为单位进行粗粒度调度，而是按 **Refresh / Reuse 阶段** 细粒度调度。
- 当某些请求进入轻量的 Reuse 阶段时，释放出的 token 预算可用于立即接纳新的 Refresh 请求，实现硬件利用率最大化。

#### ✅ **Head-Centric Sparse KV Cache（头中心化稀疏 KV 缓存）**
- 支持每个 attention head 独立选择保留的关键上下文 token（即 per-head top-k selection），避免因全局共享掩码造成的语义损失。
- 同时采用 **物理连续存储布局**（dense storage layout），将稀疏选中的 KV 条目打包成紧凑张量，确保 FlashAttention 可高效执行，兼顾精度与性能。

---

### 相比现有方法的优势

| 特性 | 现有方法（如 Fast-dLLM, Sparse-dLLM） | dLLM-Serve |
|------|----------------------------------------|-----------|
| 内存管理 | 忽视 logit 张量峰值，保守预留大量激活内存 | 显式分解 logit，释放空间用于 KV 缓存 |
| 调度粒度 | Request-level，无法利用阶段异构性 | Phase-level，动态复用“Reuse headroom” |
| 稀疏策略 | 全局统一 mask，牺牲 head-specific 语义 | per-head 独立选择，保持高建模能力 |
| 存储效率 | 逻辑稀疏但物理仍全量分配 | 物理压缩 + 连续访问，提升带宽效率 |

> 📌 总结：dLLM-Serve 是首个将算法级稀疏性转化为真实 wall-clock 加速的服务系统，在内存、调度、缓存三个层面协同优化。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多种典型负载场景：
- **LiveBench**：包含编程任务的综合性评测基准（选用 Coding 子集）
- **BurstGPT (Burst)**：来自 ChatGPT 和 GPT-4 的真实用户交互轨迹，模拟突发流量
- **OpenAI Summarization Comparison (OSC)**：人类偏好摘要对数据集，测试长文本生成
- **GSM8K**：小学数学应用题，评估多步推理能力
- **HumanEval**：函数补全任务，衡量代码生成质量

---

### 实验设置与评估指标

#### 硬件平台
| 平台 | CPU | RAM | GPU |
|------|-----|-----|-----|
| 消费级 | 2×Xeon Platinum 8375C | 250GB | **RTX 4090 (24GB)** |
| 服务器级 | Xeon Platinum 8462Y+ | 500GB | **NVIDIA L40S (48GB)** |

> 两种配置分别代表 **内存受限** 与 **高容量扩展性** 场景。

#### 模型
- **LLaDA-8B-Instruct**：典型的 diffusion-based LLM，FP16 权重约需 17GB，剩余显存紧张。

#### 基线方法对比
| 基线 | 类型 | 主要特性 |
|------|------|---------|
| **Fast-dLLM** | 推理加速框架 | 支持 KV cache 和块状并行解码 |
| **dLLM-Cache** | 自适应缓存机制 | 基于特征相似性的部分刷新策略 |
| **Sparse-dLLM** | 稀疏注意力 + 动态驱逐 | 全局 top-k 保留，训练无关优化 |

> 所有基线均禁用 speculative decoding，并统一 sparsity ratio = 0.5，确保公平比较。

#### 评估指标
- **Throughput (tok/s)**：每秒生成 token 数量
- **Average Latency (s)**：端到端响应时间
- **Tail Latency / Jitter**：标准差与最大-最小跨度
- **Accuracy**：Pass@1（HumanEval）、准确率（GSM8K）
- **Speedup vs Base**：相对于最强 baseline 的加速比

---

## 3. 主要实验结果和性能指标

### 关键性能数据（RTX 4090 / L40S）

| 数据集 | 指标 | dLLM-Serve | 最佳基线 | 加速比 |
|-------|------|------------|----------|--------|
| **Burst (RTX 4090)** | Throughput | **76.6 tok/s** | Fast-dLLM: 42.4 tok/s | **1.81×** |
| **LiveBench (RTX 4090)** | Throughput | **65.8 tok/s** | Fast-dLLM: 40.9 tok/s | **1.61×** |
| **OSC (L40S)** | Throughput | **106.95 tok/s** | Sparse-dLLM: 34.23 tok/s | **3.12×** |
| **Burst (L40S)** | Throughput | **94.32 tok/s** | Fast-dLLM: 58.23 tok/s | **1.62×** |
| **OSC (L40S)** | Avg Latency | **1849.79s** | Baseline: ~6791s | **~3.7× 降低** |

> 💡 在重度竞争负载下（0.5 req/s），dLLM-Serve 将尾部延迟降低近 **4×**，且抖动（jitter）显著减小。

---

### 消融实验结果（Ablation Study）

在 Burst 数据集上逐步启用各模块，观察增量收益：

| 组件组合 | 相对于 Sparse-dLLM 的吞吐加速 |
|--------|-------------------------------|
| Baseline (Sparse-dLLM) | 1.00× |
| + Custom Inference Engine（含 Head-Centric KV） | **1.76×** |
| + Phase-Multiplexed Scheduler | **1.82×** |
| + Logit-Aware Budgeting | **1.97×** |

> 🔍 分析表明：
- **Inference Engine 改造贡献最大**：得益于 FlashAttention + 连续批处理 + 头中心缓存；
- **Scheduler 提升资源利用率**：尤其在动态请求流中效果明显；
- **Logit Budgeting 释放内存瓶颈**：是实现高并发的关键。

---

## 4. 关键结论和发现

### 主要发现
1. **dLLM 的内存瓶颈已从 KV Cache 转移至瞬态激活（尤其是 logit 张量）**，必须显式管理才能避免 OOM。
2. **Refresh/Reuse 阶段的资源不对称性可被调度器主动利用**，而非被动容忍。
3. **理论上的稀疏性 ≠ 实际性能增益**，只有结合物理存储优化（如 dense packing）才能兑现效率承诺。
4. **dLLM-Serve 成功将算法稀疏性转化为真实 wall-clock 加速**，在消费级与服务器级 GPU 上均取得显著优势。

---

### 方法的局限性
- **当前调度策略为贪心 FCFS**，缺乏对未来 phase transition 的预判，可能导致后期资源冲突。
- **尚未支持分布式推理**，虽然架构上兼容 Tensor Parallelism，但跨设备 KV shard 协调仍需进一步工程实现。
- **所有实验基于 LLaDA 架构**，是否完全泛化至其他 dLLM（如 Seed Diffusion）有待验证。

---

### 未来工作方向
- 设计 **Budgeting-Aware Scheduler**，引入 lookahead 规划，保证全程不超 token 预算。
- 扩展至 **multi-GPU / cluster 环境**，探索与 TP、PP 的集成方案。
- 结合 **early-exit 或 adaptive step skipping** 技术，进一步缩短推理路径。
- 开源项目代码，推动社区共建高效的 dLLM serving 生态。

---

> ✅ **最终结论**：  
> dLLM-Serve 首次系统性地解决了 dLLM 推理中的“内存足迹危机”，提出了一个可扩展、高性能、高质量的生产级服务蓝图。它不仅提升了吞吐与延迟表现，更重要的是揭示了一个核心理念：**高效的 AI 推理服务不仅是模型或算法问题，更是系统设计的艺术**。

</details>

---

### 2. [Enabling Disaggregated Multi-Stage MLLM Inference via GPU-Internal Scheduling and Resource Sharing](https://arxiv.org/abs/2512.17574)

**Authors**: Lingxiao Zhao, Haoran Zhou, Yuezhi Che, Dazhao Cheng  
**Category**: cs.DC  
**Published**: 2025-12-22  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.17574v1  

#### Abstract
Multimodal large language models (MLLMs) extend LLMs with visual understanding through a three-stage pipeline: multimodal preprocessing, vision encoding, and LLM inference. While these stages enhance capability, they introduce significant system bottlenecks. First, multimodal preprocessing-especiall...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Enabling Disaggregated Multi-Stage MLLM Inference via GPU-Internal Scheduling and Resource Sharing

---

## 1. 论文的主要贡献和创新点

### 解决的问题
多模态大语言模型（**MLLMs**）在推理过程中引入了视觉理解能力，其典型流程包含三个阶段：**multimodal preprocessing**（如视频解码）、**vision encoding** 和 **LLM inference**。然而，这种异构流水线带来了系统级瓶颈：
- **视频解码耗时长**：尤其是视频解码常主导 **Time-to-First-Token (TTFT)**，而主流部署依赖 CPU 解码，吞吐低、扩展差。
- **跨阶段阻塞严重**：vision encoder 是独立且计算密集的模块，无法与 LLM 的 prefill/decode 阶段共批处理，导致生成延迟增加。
- **资源利用率低下**：无论是 monolithic 架构还是 split-based 架构，都存在资源碎片化问题，难以同时满足低延迟和高吞吐需求。

### 提出的新方法与思路
作者提出 **FlashCodec** 和 **UnifiedServe** 两个互补设计，构建端到端优化栈：

#### ✅ FlashCodec
- **协同多 GPU 视频解码机制**：利用所有 GPU 上的硬件解码引擎（如 NVDEC），将单个视频划分为多个 GOP（Group of Pictures）并行解码。
- 引入 **stall-free scheduling** 策略，避免解码单元空转，提升整体利用率。
- 支持细粒度调度（以 GOP 为单位），实现负载均衡和低延迟。

#### ✅ UnifiedServe
- **逻辑解耦、物理共享架构**：
  - 将 MLLM 推理划分为三个异步协作的 worker：
    - `vision_preprocess_worker`（使用 FlashCodec）
    - `encode_prefill_worker`
    - `decode_worker`
  - 各阶段逻辑上分离以消除干扰，但物理上共享同一组 GPU 资源池，最大化利用率。
- 引入高效的 **embedding buffer management** 机制（类似 PagedAttention），管理中间状态，降低内存开销。
- 采用 **token budget 控制策略** 动态调节 encode/prefill 的计算负载，保障 decode 的低 TBT。

### 相比现有方法的优势
| 维度 | Monolithic 架构 | Split-based 架构 | **UnifiedServe** |
|------|------------------|-------------------|------------------|
| **TTFT** | 较低（全资源可用） | 高（编码独占资源） | ✅ 显著降低（FlashCodec 加速） |
| **TBT** | 高（被 encoder 干扰） | 低（隔离执行） | ✅ 接近 split 水平（decode 不受阻） |
| **吞吐量** | 高 | 低（资源分裂） | ✅ 最高（资源共享） |
| **资源利用率** | 高 | 低 | ✅ 充分利用 |

> 💡 **核心突破**：首次实现了 **低 TTFT 与低 TBT 的统一**，打破了传统架构中“要么快首 token、要么稳生成”的权衡困境。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 描述 |
|-------|------|------|
| **MLVU** [69] | 视频 | 多任务标注的长视频（8–10分钟），用于测试重负载场景 |
| **EgoSchema** [37] | 视频 | 短视频片段（约3分钟），模拟日常交互场景 |
| **VisionArena** [12] | 图像 | 包含文本描述的真实图像对，用于图像理解任务 |

> 注：FlashCodec 仅在 MLVU 上启用；其他数据集使用 baseline 的 Decord 解码器进行公平比较。

### 实验设置
- **硬件环境**：4×A100 GPUs（80GB）
- **模型配置**：
  - Qwen2.5-VL-32B（VisionEncoder: 0.5B, LMD: 32B）
  - InternVL3-38B（VisionEncoder: 6B, LMD: 32B）
- **并行策略**：
  - Monolithic: TP=4
  - Split: TP=2 for encode/prefill, TP=2 for decode
  - UnifiedServe: TP=4 for LLM, DP=2+TP=2 for Qwen encoding, TP=4 for InternVL3

### 评估指标
| 指标 | 含义 |
|------|------|
| **TTFT** | Time to First Token，首 token 延迟 |
| **TBT** | Time Between Tokens，token 间延迟 |
| **P90/P99 TTFT/TBT** | 尾部延迟，反映 SLO 满足能力 |
| **Throughput (req/s)** | 请求吞吐量 |
| **SLO Attainment** | 在给定延迟约束下可服务的最大请求速率 |
| **End-to-End Latency** | 单请求总延迟 |

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **统一架构（Monolithic）** | vLLM-s（chunked-prefill）、SGLang-s |
| **拆分架构（Split-based）** | vLLM-d、SGLang-d（PD-disaggregation） |
| **视频解码器对比** | Decord [CPU/CUDA]、TorchCodec [CPU/CUDA]、DeepCodec [CPU]、FlashCodec [CUDA×1/×4] |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 指标 | 提升幅度 | 来源 |
|------|--------|------|
| **最大吞吐量** | ⬆️ **4.4×** vs SOTA | 图14 |
| **可服务请求数** | ⬆️ **3.0×** | 摘要、图15 |
| **SLO 约束更严格** | 🔒 **1.5× tighter SLOs** | 摘要 |
| **P99 TBT 延迟** | ⬇️ **83% reduction** vs monolithic | 图17 |
| **平均 TTFT** | ⬇️ **80% reduction** vs split-based | 图13(a) |
| **视频解码速度** | ⬆️ **5.7–9.1× faster** (4 GPUs) | 图3 |

### 与基线方法的对比结果

#### 📊 吞吐量对比（图14）
- 在 MLVU 数据集上，UnifiedServe 吞吐量达到 **~200k req/day**，是 vLLM-d 的 **4.4×**。
- Split-based 方法因资源分裂，吞吐仅为 UnifiedServe 的 **14%–51%**。

#### 📈 SLO 满足能力（图15）
- 在相同 SLO 下，UnifiedServe 可支持的请求率远高于基线：
  - 对于 Qwen-32B + MLVU，UnifiedServe 支持 **~20 req/s**，而 vLLM-d 仅能支持 **~5 req/s**。
- 在更严格的 SLO 缩放下（图16），UnifiedServe 能满足 **2倍以上** 的严苛要求。

#### ⏱️ 延迟表现（图13）
- **长视频场景（MLVU）**：
  - vLLM-s：TTFT 低但 TBT >300ms（被 encoder 阻塞）
  - vLLM-d：TBT <20ms 但 TTFT 很高
  - **UnifiedServe**：**TTFT ↓80%，TBT ↑仅 ~50%**，实现最佳平衡
- **短视频/图像场景**：
  - UnifiedServe 在 E2E 延迟上全面优于各类基线，尤其在高并发下仍保持稳定。

#### 🎥 视频解码性能（图18）
- FlashCodec 在 H.264/H.265/VP9 编码格式下均显著领先：
  - 相比 DeepCodec（CPU 最优方案），在 4×A100 上实现 **最高 9× 解码加速**。
  - 即使在 CPU 友好的 H.264 场景，FlashCodec 仍快 **4×**。

### 消融实验（隐含分析）
虽然未明确列出消融表，但从设计分析中可得出以下结论：
- **FlashCodec 单独作用**：大幅降低 TTFT，尤其是在长视频场景。
- **UnifiedServe 调度机制**：通过 token budget 控制，有效抑制 encode 对 decode 的干扰，保障 TBT 稳定。
- **资源共享 vs 物理拆分**：相比 split 架构，资源碎片减少，整体利用率提升。

---

## 4. 关键结论和发现

### 主要发现
1. **视频解码已成为 MLLM 推理的关键瓶颈**，尤其在长视频场景下，其延迟常超过 LLM 自身推理时间。
2. **传统的 monolithic 与 split 架构存在根本性矛盾**：前者牺牲 TBT，后者牺牲 TTFT 和吞吐。
3. **逻辑解耦 + 物理共享是解决 MLLM 推理瓶颈的有效路径**：
   - FlashCodec 实现了 **低延迟、高吞吐的视频预处理**
   - UnifiedServe 实现了 **高利用率下的低延迟生成**
4. **通过精细的资源调度（如 token budget）可以控制异构阶段间的干扰**，无需完全物理隔离。

### 方法的局限性
- **依赖特定硬件**：FlashCodec 高度依赖 GPU 内置的硬件解码器（如 NVDEC），在无此类设备的环境中效果受限。
- **复杂性增加**：多 worker 异步协作增加了系统复杂性和调试难度。
- **当前主要面向离线/准实时场景**：对于超低延迟（<100ms）的强实时应用，仍需进一步优化。

### 未来工作方向
- 扩展至更多模态（音频、3D 点云等）的统一预处理框架。
- 结合算法级优化（如 visual token 压缩）与系统级优化，进一步降低带宽和计算开销。
- 探索动态资源分配机制，在不同请求类型（图像 vs 视频）之间智能调度。
- 支持异构集群（CPU+GPU+NPU）下的跨节点协同推理。

---

> ✅ **总结一句话**：  
> 本文提出的 **FlashCodec + UnifiedServe** 框架，通过 **协同多 GPU 解码** 与 **逻辑解耦、资源共享的调度架构**，首次实现了 MLLM 推理中 **低 TTFT、低 TBT、高吞吐** 的“不可能三角”突破，相较 SOTA 系统实现高达 **4.4× 吞吐提升** 和 **3.0× 更多请求服务能力**。

</details>

---

### 3. [LLM-HPC++: Evaluating LLM-Generated Modern C++ and MPI+OpenMP Codes for Scalable Mandelbrot Set Computation](https://arxiv.org/abs/2512.17023)

**Authors**: Patrick Diehl, Noujoud Nader, Deepti Gupta  
**Category**: cs.DC  
**Published**: 2025-12-22  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.17023v1  

#### Abstract
Parallel programming remains one of the most challenging aspects of High-Performance Computing (HPC), requiring deep knowledge of synchronization, communication, and memory models. While modern C++ standards and frameworks like OpenMP and MPI have simplified parallelism, mastering these paradigms is...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-HPC++: Evaluating LLM-Generated Modern C++ and MPI+OpenMP Codes for Scalable Mandelbrot Set Computation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文系统评估了当前主流 **Large Language Models (LLMs)** 在生成高性能计算（HPC）领域中复杂、可扩展的并行 C++ 代码方面的能力。尽管 LLMs 已被广泛用于通用代码生成，但其在需要精确同步、内存管理和通信机制的 **现代并行编程范式**（如 C++11/17/20、OpenMP、MPI+OpenMP）中的有效性尚未得到充分验证。

该研究旨在填补 AI 辅助编程与 HPC 高效实现之间的鸿沟，回答一个关键问题：**LLMs 是否能可靠地生成正确、高效且可扩展的科学计算代码？**

### 提出的新方法/新思路
- 设计了一套**结构化提示工程（prompt engineering）策略**，针对不同并行模型（共享内存、指令级、分布式内存）统一使用标准化 prompt 来测试多个 LLM。
- 以 **Mandelbrot 集合计算**作为“尴尬并行”（embarrassingly parallel）的基准任务，避免引入求解器或外部库依赖，聚焦于并行逻辑本身。
- 构建了一个完整的评估流程：从代码生成 → 编译 → 运行时错误检测 → 功能正确性验证（通过 PBM 图像输出）→ 性能与可扩展性分析。
- 引入 **COCOMO 模型** 对生成代码的开发难度进行量化估算，提供质量与努力之间的权衡视角。

### 相比现有方法的优势
- 是首个对多种现代 C++ 并行范式（包括 C++20 coroutines 和混合 MPI+OpenMP）进行全面 LLM 代码生成能力评估的研究。
- 不仅关注语法正确性，还深入分析语义一致性、运行时行为、调试鲁棒性和实际性能表现。
- 公开了所有生成代码（GitHub/Zenodo），提升了可复现性。

---

## 2. 核心实验方法和设置

### 使用的模型（LLMs）
- **ChatGPT-4**
- **ChatGPT-5**
- **Claude**
- **LLaMA**

> 注：文中未明确说明具体版本号（如 LLaMA-2 或 LLaMA-3），但基于上下文推测为 LLaMA-2 系列。

### 实验任务与代码范式
评估以下五类并行实现：
1. **Shared-memory parallelism**:
   - `std::async` / `future` (C++11)
   - Parallel algorithms with execution policies (C++17)
   - Coroutines (C++20)
2. **Directive-based parallelism**: OpenMP
3. **Distributed-memory parallelism**: Hybrid MPI + OpenMP

### Prompt 设计
- **P1**: 针对 C++ 内置并行特性（async, parallel algorithms, coroutines）
  > “Write a parallel code for the Mandelbrot set using C++{11 async future, 17 parallel algorithms, 20 coroutines} and write the output to the PBM format. Make the number of threads configurable with the command line option -t and print the runtime excluding IO.”
- **P2**: OpenMP 版本，类似 P1
- **P3**: MPI+OpenMP 混合版本
  > “Write a distributed code using MPI for the Mandelbrot set and using OpenMP for parallelism in C++ and write the output to the PBM format. Make the number of partitions configurable with the command line option -p and print the runtime excluding IO.”

> 特别强调需支持命令行参数控制资源分配，并输出 PBM 格式图像以便视觉验证。

### 实验平台与工具链
- **编译器**: GCC 11.5.0
- **MPI 库**: OpenMPI 5.0.7
- **TBB**: Intel® TBB 2022.2（用于 C++17 并行算法后端）
- **硬件平台**: LSU 的 rostam 集群（Intel Xeon Gold 6148 CPU @ 2.40GHz, 20 cores/node）
- **代码分析工具**:
  - `cloc`: 统计 LOC（lines of code）
  - `scc`: 获取 COCOMO 开发成本估计

### 评估指标
| 类别 | 指标 |
|------|------|
| **Correctness** | 编译成功（Build）、运行无崩溃（Runtime）、图像正确（Visual Validation） |
| **Robustness** | 错误类型分类（build/runtime）、是否需人工修复 |
| **Scalability** | 单节点多核扩展性（1–20 cores）、多节点分布式扩展性（1–12 nodes） |
| **Efficiency** | Pixels processed per second、Speedup |
| **Code Quality** | LOC、COCOMO estimated effort |

### 基线方法对比
本研究不直接比较传统手写 HPC 代码，而是将不同 LLM 视为“开发者”，相互之间进行横向对比，重点关注：
- 各模型在各类并行范式下的成功率
- 可扩展性表现
- 调试负担（即生成代码的“开箱即用”程度）

---

## 3. 主要实验结果和性能指标

### ✅ 编译与运行时正确性（见 Table 1 & 2）

#### 共享内存并行（Table 1）
| LLM \ Paradigm | Coroutines | Async | Parallel Alg. | OpenMP |
|----------------|----------|--------|---------------|--------|
| **ChatGPT-5** | √ / X / √ | √ / X / √ | √ / X / √ | √ / X / √ |
| **ChatGPT-4** | √ / X / √ | √ / X / √ | √ / X / √ | √ / X / √ |
| **Claude**     | √ / X / √ | √ / √ / √ | √ / X / √ | √ / √ / √ |
| **LLaMA**      | X / X / X | √ / X / X | X / X / √ | √ / X / √ |

> 符号说明：√ 表示无此错误，X 表示存在错误；三列分别为 Build / Runtime / Correctness

- **ChatGPT-5** 和 **ChatGPT-4** 在构建上全部成功，但在协程和异步编程中出现运行时挂起或死循环。
- **LLaMA** 在 coroutine 上完全失败（编译失败 + segfault），且多数情况下忽略线程数配置。
- **Claude** 在 coroutine 中因 `final_suspend=std::suspend_never` 导致无限循环，需手动修复。

#### 分布式内存（Table 2）
| LLM | Build | Runtime | Correctness |
|-----|-------|---------|-------------|
| **ChatGPT-5** | √ | √ | √ |
| **ChatGPT-4** | √ | √ | √ |
| **Claude**    | √ | √ | √ |
| **LLaMA**     | √ | X | √（修复后）|

- LLaMA 生成的 MPI 代码在多进程下崩溃（free(): invalid pointer, MPI_Gather buffer mismatch），需修复才能运行。

### 📊 代码规模与开发难度（COCOMO 分析）

#### 分布式代码 LOC（Table 3）
| LLM | Lines of Code (LOC) |
|-----|---------------------|
| ChatGPT-5 | 143 |
| Claude    | 116 |
| ChatGPT-4 | 91  |
| LLaMA     | 71  |

- **ChatGPT-5 生成最详细的代码**，LOC 最高，可能包含更多健壮性检查。
- 结合 COCOMO 模型分析（图4、图5）：
  - **ChatGPT-5** 代码质量最高（右上角），虽开发 effort 略高，但可靠性强。
  - **LLaMA** effort 最低，但 runtime 错误最多，属于“便宜但不可靠”。

### 🔬 可扩展性表现（Scaling Results）

#### 单节点共享内存扩展（图6）
- **ChatGPT-5** 成功实现了线程可控的 scaling（尤其在 parallel algorithms 中使用 `tbb::global_control` 正确限制线程数）。
- **ChatGPT-4** 存在问题：parallel algorithms 使用 `OPENMP_NUM_THREADS` 设置无效（GCC 使用 TBB 后端）。
- **Claude** 多数情况默认使用全部核心，无法控制线程数。
- **LLaMA** 所有实现均忽略 `-t` 参数，始终占用全部 20 cores。

#### 多节点分布式扩展（图7）
- 所有模型（除 LLaMA 外）生成的代码在修复后均可扩展至 12 节点。
- **共同现象**：从 2 到 4 节点的加速比下降明显，可能是通信开销突增所致。
- **LLaMA** 时间测量遗漏 MPI_Gather 阶段，导致性能评估偏差，需修正。
- 整体来看，**ChatGPT-4/5 和 Claude 的分布式性能相近且良好**，LLaMA 经修复后可达 Claude 水平。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **ChatGPT-4 和 ChatGPT-5 表现最佳**：具备较强的语法准确性和功能完整性，在多种并行范式下生成的代码经过少量调试即可正确运行并具有良好可扩展性。
2. ⚠️ **线程/进程控制是普遍短板**：大多数模型未能正确处理动态线程数配置（如 `-t` 参数），常默认使用全部可用资源，影响实验公平性和实用性。
3. ❌ **LLaMA 在复杂并行结构中表现较差**：尤其在 coroutine 和 MPI 场景中频繁出现编译错误、内存错误和逻辑缺陷，需大量人工干预。
4. 💡 **提示工程至关重要**：在 MPI+OpenMP 场景中，必须显式要求“partition 可配置”，否则模型会重复计算而非分片处理。
5. 🎨 **颜色映射不一致**：同一模型（如 ChatGPT-5）在共享与分布版本中使用不同的 color mapping 函数，暴露了生成过程缺乏全局一致性。

### 方法的局限性
- 仅评估了 **Mandelbrot 集这一特定 workload**，虽具代表性，但仍属简化场景，未涉及真实 HPC 应用中的复杂数据结构或通信模式。
- 所有错误均由作者手动修复，未自动化评估“调试成本”。
- COCOMO 模型原本面向串行软件，应用于并行代码存在一定理论偏差。
- 缺乏与专家手工编写最优实现的性能对比（仅评估相对 scaling 行为）。

### 未来工作方向
- 构建专门面向 HPC 的 **domain-specific LLMs**（如 MPIrigen、HPC-Coder 的延续）。
- 探索自动反馈闭环机制：让 LLM 自动解析编译/运行错误并迭代修复代码。
- 扩展到更多 HPC 核心算例（如 stencil、FFT、sparse solvers）和语言（Fortran、CUDA）。
- 建立标准化的 **LLM-for-HPC benchmark suite**，推动可复现研究。
- 研究如何提升 LLM 对底层运行时系统（如 TBB、OpenMP runtime）的理解能力。

---

> **总结一句话**：  
> 当前最先进的 LLMs（尤其是 ChatGPT-4/5）已能在适当提示下生成基本正确的现代 C++ 并行代码，并展现出良好的可扩展潜力；然而，在线程管理、运行时控制和跨范式一致性方面仍存在显著缺陷，距离“自主生成生产级 HPC 代码”尚有差距。

</details>

---

### 4. [BumpNet: A Sparse Neural Network Framework for Learning PDE Solutions](https://arxiv.org/abs/2512.17198)

**Authors**: Shao-Ting Chiu, Ioannis G. Kevrekidis, Ulisses Braga-Neto  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.17198v1  

#### Abstract
We introduce BumpNet, a sparse neural network framework for PDE numerical solution and operator learning. BumpNet is based on meshless basis function expansion, in a similar fashion to radial-basis function (RBF) networks. Unlike RBF networks, the basis functions in BumpNet are constructed from ordi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BumpNet: A Sparse Neural Network Framework for Learning PDE Solutions

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于深度神经网络的 PDE 求解方法（如 PINN 和 DeepONet）通常依赖于全连接多层感知机（MLP），存在以下问题：
- **参数量大**：导致训练效率低、计算开销高；
- **缺乏可解释性**：难以理解模型内部如何逼近解；
- **固定基函数限制**：如 RBF 网络虽具可解释性，但其基函数形状和中心常为预设或非可训练。

### 提出的新方法与思路
作者提出 **BumpNet** ——一种稀疏、可解释的两层神经网络框架，用于函数逼近和 PDE 解的学习。其核心思想包括：

- **基于 sigmoid 的“bump”基函数构造**：通过多个 tanh 激活函数的线性组合构建局部化、可训练的“bump”形基函数，避免使用专用 RBF 非线性激活。
- **完全可训练的基函数参数**：每个 bump 的位置（center）、方向（orientation）、锐度（sharpness）和振幅（amplitude）均可通过梯度优化学习。
- **动态剪枝实现 h-adaptivity**：在训练过程中根据 bump 的高度 $ h_i $ 动态移除低幅值基函数，自动实现模型简化与自适应网格细化（h-adaptivity）。
- **模块化集成能力**：BumpNet 可作为组件嵌入多种主流架构，形成：
  - **Bump-PINN**：结合 Physics-Informed Neural Networks；
  - **Bump-EDNN**：用于时间演化 PDE 的进化型求解；
  - **Bump-DeepONet**：用于算子学习任务。

### 相比现有方法的优势
| 方面 | BumpNet 优势 |
|------|-------------|
| **参数效率** | 参数数量比标准 MLP 少 20–100 倍（例如仅需 252 参数即可达到良好精度） |
| **训练速度** | 更快收敛，显著缩短训练时间（如 Poisson 方程快 20×） |
| **可解释性** | 所有 bump 的几何属性可直接从权重解析得出，便于分析 |
| **灵活性与通用性** | 支持任意维度空间，支持 operator learning 和 time-stepping 架构 |
| **自适应能力** | 剪枝机制实现自动模型压缩与区域聚焦 |

---

## 2. 核心实验方法和设置

### 使用的数据集 / 测试问题
论文采用多个经典偏微分方程基准进行验证，涵盖不同类型的 PDE：

1. **Helmholtz 方程（非齐次）**
   - $ u_{xx} + u_{yy} + ku = q(x,y) $
   - 定义域：$[-3,3]^2$，零边界条件
   - 真实解：$ \sin(\pi x)\sin(\pi y) $

2. **二维 Poisson 方程**
   - $ u_{xx} + u_{yy} = f(x,y) $
   - 定义域：$[0,1]^2$，零边值
   - 真实解：$ \sin(2\pi x)\sin(4\pi y) $

3. **热传导方程（Heat Equation）**
   - $ u_t = u_{xx} + 2\sin(\pi x) $
   - 时空域：$[0,1]\times[0,5]$，初始条件 $ u(x,0)=\sin(2\pi x) $

4. **对流方程（Advection Equation）**
   - $ u_t + v u_x = 0 $，周期边界
   - 初始条件：$ u(x,0)=\sin(x) $，高速情形 $ v=30 $

5. **非线性扩散-反应方程（Operator Learning）**
   - $ u_t = D u_{xx} + k u^2 + f(x) $
   - 学习映射 $ f(x) \mapsto u(x,t) $，其中 $ f(x) $ 来自 GRF（Gaussian Random Field）

### 实验设置与评估指标

#### 评估指标
- **相对 L1/L2 误差**：衡量预测解与真实解之间的偏差；
- **参数数量 (#param)**：比较模型复杂度；
- **训练时间（秒）**：反映计算效率；
- **推理速度与内存占用**：间接体现轻量化优势。

#### 基线方法对比
| 方法 | 描述 |
|------|------|
| **PINN** | 原始 Physics-Informed Neural Network（MLP 结构） |
| **SAPINN** | 自适应加权 PINN（Self-Adaptive PINN） |
| **SPINN** | Sparse, Physics-based, and partially Interpretable Neural Networks（类似稀疏 RBF 方法） |
| **EDNN** | Evolutional Deep Neural Network（基于 MLP 的时间推进） |
| **DeepONet** | 经典 Deep Operator Network（branch-trunk 架构） |

#### 训练细节
- 使用 Adam 优化器；
- Collocation points 数量见 Table 2（如 Helmholtz 使用 10k 内部点）；
- Bump 初始化为规则网格分布；
- 剪枝策略：每 2000 步移除最低 0.15% 幅值的 bumps。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（来自 Table 1）

| 任务 | 方法 | L1/L2 Error | #Param | Train Time (s) |
|------|------|------------|--------|----------------|
| **Helmholtz** | Bump-PINN | 2.66e-2 | 1,575 | 51 |
|              | PINN      | 1.24e-1 | 83,073 | 85 |
|              | SPINN     | 9.83e-1 | 1,761 | 2,265 |
| **Poisson**   | Bump-PINN | 8.66e-4 | 252 | 61 |
|              | PINN      | 2.73e-2 | 83,073 | 112 |
|              | SPINN     | 1.03e-4 | 481 | 1,256 |
| **Heat Eq**   | Bump-PINN | 1.83e-3 | 840 | 12 |
|              | PINN      | 3.15e-2 | 3,421 | 41 |
|              | SPINN     | 2.18e+0 | 1,021 | 291 |
| **Advection** | Bump-PINN | 2.30e-3 | 154 | 28 |
|              | PINN      | 7.78e-2 | 83,073 | 48 |

> 注：Bump-SAPINN 在部分任务中略逊于 SAPINN，但在参数效率上仍占绝对优势。

### 与其他方法的对比结果

#### ✅ Bump-PINN vs PINN / SAPINN
- **参数减少 98%以上**，同时保持甚至超越精度；
- **训练速度快数倍至数十倍**（尤其在 Poisson 上达 20× 加速）；
- 即使在难训练的高速对流问题（$v=30$）中也能稳定收敛。

#### ✅ Bump-EDNN vs EDNN
| 指标 | Bump-EDNN | EDNN |
|------|-----------|------|
| 参数总数 | 252 | 1,341 |
| 需演化的参数 | 36 | 1,341 |
| 初始训练时间 | 6m43s | 8m24s |
| 时间推进耗时 | **6 秒** | **23 分钟** |
| $t=1$ 时刻 L2 误差 | **5e-4** | 1.4e-3 |

> 显示 Bump-EDNN 不仅更小更快，且长期预测更准确。

#### ✅ Bump-DeepONet vs DeepONet
| 指标 | Bump-DeepONet | DeepONet |
|------|---------------|----------|
| Trunk 参数数 | **600** | 25,600 |
| Test Error | 8.12e-6 | 5.42e-6 |
| 训练时间 | **86 秒** | 158 秒 |
| 迭代速度 | 937 iter/sec | 775 iter/sec |

> 虽然 DeepONet 精度稍优，但 Bump-DeepONet 实现了 **百倍参数压缩** 与 **更快训练**，性价比极高。

### 消融实验结果（Pruning 效果）
- 在 Helmholtz 问题中引入动态剪枝后：
  - 训练损失下降更快（见 Fig. 8）；
  - 尽管剪枝引起短暂 loss spike，但总体收敛加速；
  - 最终保留的 bumps 自动集中在高梯度区域，实现 **h-adaptivity**；
  - 模型大小进一步减小，提升泛化能力和鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **BumpNet 是一个高效、可解释、可扩展的 PDE 求解框架**：
   - 其基于 sigmoid 构造的可训练 bump 基函数兼具 RBF 的可解释性和 DNN 的灵活性；
   - 所有 bump 几何参数均可解析表达，增强物理意义透明度。

2. **显著优于传统 MLP-based 方法**：
   - 在相同或更低误差下，参数量减少 1–2 个数量级；
   - 训练和推理速度大幅提升，适合资源受限场景。

3. **天然支持 h-adaptivity 和模型压缩**：
   - 通过幅度驱动的剪枝机制，自动聚焦重要区域并去除冗余 bump；
   - 提升训练稳定性与收敛速度。

4. **广泛兼容主流架构**：
   - 成功应用于 PINN、EDNN、DeepONet 等范式，在各类 PDE 场景中均表现优异。

### 方法的局限性
- 当前主要针对规则或矩形域设计 bump 初始化，复杂几何域需额外处理；
- 虽然可推广到高维，但 bump 数随维度指数增长（“维数灾难”风险）；
- 剪枝阈值等超参数需手动设定，尚未完全自动化；
- 对极端刚性或高频震荡问题的鲁棒性有待进一步测试。

### 未来工作方向
- 探索 **自适应 bump 初始化策略**，以更好匹配不规则域；
- 引入 **可变宽度/形状控制机制**，增强基函数表达力；
- 开发 **全自动剪枝与生长机制**，实现真正的“神经自适应网格”；
- 将 BumpNet 应用于 **多尺度建模、不确定性量化、逆问题求解** 等科学机器学习前沿领域。

---

> **总结**：  
> BumpNet 提供了一种全新的视角——将神经网络视为**可训练的基函数展开系统**，而非黑箱拟合工具。它不仅提升了 PDE 求解的效率与可解释性，也为下一代科学机器学习模型的设计提供了坚实基础。

</details>

---

### 5. [Atom: Efficient On-Device Video-Language Pipelines Through Modular Reuse](https://arxiv.org/abs/2512.17108)

**Authors**: Kunjal Panchal, Saayan Mitra, Somdeb Sarkhel, Haoliang Wang, Ishita Dasgupta, Gang Wu, Hui Guan  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.17108v1  

#### Abstract
Recent advances in video-language models have enabled powerful applications like video retrieval, captioning, and assembly. However, executing such multi-stage pipelines efficiently on mobile devices remains challenging due to redundant model loads and fragmented execution. We introduce Atom, an on-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ATOM: Efficient On-Device Video-Language Pipelines Through Modular Reuse**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前的 **Video-Language Model (VLM)** 多阶段推理管道（如视频检索、字幕生成、视频剪辑组装）在移动设备上执行效率低下，主要原因包括：
- **重复模型加载**：不同子任务（如视频编码、字幕生成、脚本生成）使用独立模型，需反复加载，造成显著延迟。
- **内存压力大**：大型模型（如 mPLUG2 和 Llama 3.2）合计占用约 7GB 内存，超出多数智能手机的 6–8GB RAM 限制。
- **串行执行瓶颈**：传统流水线采用串行处理，无法并行化，导致端到端延迟高。

### **提出的新方法与思路**
作者提出 **ATOM** —— 一种面向移动端的 **reuse-centric（重用中心化）** 视频-语言推理系统，其核心设计原则为：
- **模块化（Modularization）**：将 VLM 分解为可独立调用的 **视觉编码器（video encoder）** 和 **通用语言解码器（language decoder）**。
- **持久化重用（Persistent Reuse）**：关键模块（如语言解码器）在整个应用生命周期中常驻内存，避免重复加载。
- **并行执行（Parallelism）**：解耦的模块支持并发执行（如编码 clip #2 时同时解码 clip #1 的字幕），提升硬件利用率。

### **相比现有方法的优势**
- **减少模型加载开销**：通过模块复用，消除多模型切换带来的加载延迟。
- **降低端到端延迟**：实现跨输入和子任务的并行处理，显著缩短总耗时。
- **保持高性能**：仅引入轻微性能下降（如 ≤2.3 Recall@1 检索精度损失），但大幅提升效率。
- **兼容移动端资源约束**：峰值内存仅增加 0.76% (~40MB)，仍可在 6GB RAM 设备上运行。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **MSR-VTT**：10K YouTube 视频，带 200K 字幕，用于训练和测试。
- **MSVD**：1.97K 带字幕的 YouTube 视频。
- **DiDeMo**：10K Flickr 视频，每段配 4 条文本描述。

### **实验设置**
- **任务类型**：
  - **视频检索（Video Retrieval）**：基于用户提示匹配最相关视频片段。
  - **视频组装（Video Assembly）**：自动生成脚本并拼接视频片段。
- **硬件平台**：
  - 移动端：Google Pixel 5a (6GB RAM), Pixel 8a, Samsung Galaxy S23 (均 8GB RAM)。
  - 服务器端：x86_64 CPU + NVIDIA A40 GPU。
- **部署框架**：PyTorch Mobile + TorchAO，支持量化与算子融合优化。
- **量化策略**：对 LINEAR 和 EMBEDDING 层进行 **8-bit 动态量化**，以压缩模型大小并加速推理。

### **评估指标**
| 任务 | 指标 |
|------|------|
| **视频字幕生成** | BLEU@4, ROUGE-L, METEOR, CIDEr |
| **视频检索** | Recall@1, Recall@5, Recall@10 |
| **视频组装** | 定性分析（人工评分）：Adherence to prompt, Coherence, Engagement 等 |
| **系统性能** | 端到端延迟（seconds）、峰值内存（GB）、模型加载时间 |

### **基线方法对比**
- **Sequential Baseline (w/o reuse)**：传统串行流水线，每个子任务加载独立模型。
- **Server-side Systems**：使用 GPU 加速的非重用版本作为性能上限参考。
- 所有对比均在同一模型架构下进行，仅改变执行流程（是否模块化与重用）。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 性能表现（质量）**
| 模型 | CIDEr ↑ | Recall@1 ↑ |
|------|--------|-----------|
| 原始 mPLUG2 (BERT decoder) | 79.4 (MSR-VTT) | 52.6 (MSR-VTT) |
| **mPLUG2+ (Llama decoder)** | **83.1** (+3.7) | **57.2** (+4.6) |
| mPLUG2+ (8-bit quantized) | 82.5 (-0.6) | 55.8 (-1.4) |

> ✅ **结论**：使用更强的 Llama 解码器显著提升字幕质量和检索准确率；量化后性能下降极小。

#### **(2) 效率表现（速度）**
| 平台 | 任务 | 延迟（w/o reuse） | 延迟（ATOM） | **提速** |
|------|------|------------------|-------------|---------|
| Pixel 5a | 视频检索 | 162.33s | **108.66s** | ↓ **33.06%** |
| Pixel 5a | 视频组装 | 167.52s | **115.95s** | ↓ **30.78%** |
| Pixel 8a | 视频组装 | 153.59s | **111.06s** | ↓ **27.69%** |
| S23 | 视频组装 | 152.35s | **110.39s** | ↓ **27.54%** |

> ✅ **结论**：ATOM 在主流手机上实现 **27–33% 的端到端延迟降低**。

#### **(3) 内存消耗**
| 配置 | 峰值内存 |
|------|----------|
| Non-reuse baseline | 5.277 GB |
| **ATOM (reuse + parallel)** | **5.317 GB** |
| **增量** | **+0.76% (+40MB)** |

> ✅ **结论**：尽管并行执行增加了激活内存，但因省去临时加载缓冲区，总体内存增长可忽略不计。

#### **(4) 模型加载延迟**
- ATOM 将模型加载时间减少 **高达 50%**，尤其在频繁切换任务场景中优势明显。

---

### **消融实验结果（Ablation Study）**
在 Google Pixel 5a 上测试不同配置的影响：

| 输入/超参调整 | 延迟变化 | 性能影响 |
|---------------|---------|----------|
| 分辨率从 224×224 → 112×112 | ↓ 41.8% (3.29s) | CIDEr 从 82.5 → 75.64 |
| 帧率从 6fps → 2fps | ↓ 17.2% (4.78s) | CIDEr ↓ 至 50.89（严重降质） |
| Beam size = 1 | ↓ 46.9% (3.07s) | CIDEr ↓ 至 40.98（不可接受） |
| 批大小 = 4 | ↑ 304% (22.35s) | 无收益（移动端不适用 batching） |
| 输出长度 trace=12 | ↓ 18.2% (4.55s) | CIDEr ↓ 至 74.19 |

> 🔍 **发现**：分辨率降采样是性价比最高的优化手段；beam search 对质量至关重要；batching 在移动端无效。

---

## **4. 关键结论和发现**

### **主要发现**
1. **模块化 + 重用是移动端 VLM 流水线的关键优化方向**：
   - 通过共享一个强大的语言解码器服务多个任务（captioning, retrieval, script generation），有效避免冗余加载。
2. **并行执行显著提升吞吐量**：
   - 编码与解码可重叠执行，充分利用 CPU 多核能力。
3. **量化与重用协同增效**：
   - 虽然量化本身可能引入动态转换开销，但 ATOM 的并行设计抵消了这一负面影响。
4. **实际可行性高**：
   - 总模型存储仅 **1.28GB**（见 Appendix F.2），适合部署于普通智能手机。
   - 能耗估算显示，ATOM 可节省 **约 46% 的能量消耗**（见 F.7），有利于延长电池寿命。

### **方法的局限性**
- **依赖模块化解构的 VLM 架构**：对于早期 tightly-coupled 的跨模态注意力模型（如某些 joint fusion 结构），难以直接分解。
- **并行化带来小幅内存上升**：虽然总体可控，但在极端低内存设备（<6GB）中仍需谨慎权衡。
- **当前实现聚焦 CPU 推理**：受限于 TorchAO 对 NPU/GPU 支持不足，尚未充分发挥专用硬件潜力（但设计上正交于硬件选择）。

### **未来工作方向**
- 扩展至更多视频-语言任务：如 **视频摘要（summarization）**、**视频问答（VideoQA）**、**事件定位（temporal localization）**。
- 探索更激进的压缩技术（如 4-bit 量化）与 ATOM 架构的结合。
- 支持多用户 prompt 并发处理，进一步提升系统吞吐。
- 开发自动调度机制，根据设备负载动态调整分辨率、帧率等参数以实现质量-延迟权衡。

---

> 📌 **总结一句话**：  
> **ATOM 通过“模块化 + 持久重用 + 并行执行”的系统级设计，在几乎不牺牲性能的前提下，实现了移动端视频-语言流水线 27–33% 的端到端加速，为边缘设备上的实时多模态理解提供了实用且可扩展的解决方案。**

</details>

---

### 6. [Bandwidth-Efficient Adaptive Mixture-of-Experts via Low-Rank Compensation](https://arxiv.org/abs/2512.17073)

**Authors**: Zhenyu Liu, Yunzhen Liu, Zehao Fan, Garrett Gagnon, Yayue Hou, Nan Wu, Yangwook Kang, Liu Liu  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.17073v1  

#### Abstract
Mixture-of-Experts (MoE) models scale capacity via sparse activation but stress memory and bandwidth. Offloading alleviates GPU memory by fetching experts on demand, yet token-level routing causes irregular transfers that make inference I/O-bound. Static uniform quantization reduces traffic but degr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Bandwidth-Efficient Adaptive Mixture-of-Experts via Low-Rank Compensation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Mixture-of-Experts (MoE) 模型通过稀疏激活扩展模型容量，但在推理过程中面临严重的 **内存压力** 和 **带宽瓶颈**。为缓解显存限制，通常采用 **offloading** 技术将非活跃专家存储在 CPU 内存中，按需加载。然而，token-level 的动态路由导致不规则的数据传输，使得推理过程严重受限于 I/O 带宽。

此外，虽然低比特量化（如 INT2/INT3）可减少传输量，但**静态、统一的量化策略**忽略了不同专家在不同 token 下的重要性差异，尤其对主导专家（Top-1）造成严重精度损失，影响整体准确性。

### 提出的新方法
本文提出 **Bandwidth-Efficient Adaptive MoE via Low-Rank Compensation**，一种结合 **offloading** 与 **router-guided 动态精度恢复** 的高效 MoE 推理框架。其核心思想是：

- **离线预计算低秩补偿器（Low-Rank Compensator）**：对每个专家的量化残差进行 SVD 分解，生成紧凑的低秩因子 $(U_i, V_i)$ 并存储。
- **运行时基于路由器分数选择性恢复精度**：仅对当前 token 路由得分最高的 Top-$n$ 专家应用补偿，在 GPU 上重建高精度权重；其余专家保持低比特形式，无需补偿。
- **基于峰度（kurtosis）的补偿秩分配机制**：根据专家权重分布的峰度自动分配补偿秩，高 kurtosis 专家获得更高补偿能力，提升资源利用效率。

### 相比现有方法的优势
| 方面 | 现有方法（如 GPTQ、HQQ、Hobbit） | 本文方法 |
|------|-------------------------------|---------|
| **量化策略** | 统一对所有专家进行低比特量化 | **动态感知路由重要性**，只恢复关键专家精度 |
| **补偿方式** | 若有补偿，通常应用于全部专家 | **仅对 Top-n 专家补偿**，显著降低带宽开销 |
| **补偿资源配置** | 固定或均匀分配补偿秩 | **基于 kurtosis 自适应分配**，更匹配实际误差需求 |
| **系统兼容性** | 多数仅适用于 GPU-only | 支持 **GPU-only 与 GPU-NDP** 架构，通用性强 |
| **带宽-精度权衡** | 高压缩下精度下降剧烈 | 在极低比特（INT2）下仍能维持高准确率 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **常识推理任务**（Zero-shot）：
  - MathQA, Hellaswag, ARC-easy/challenge, BoolQ, Winogrande, PIQA
- **多任务理解基准**（5-shot）：
  - MMLU
- **语言建模质量评估**：
  - WikiText2（用于 PPL 测量）

评估工具：EleutherAI LM Evaluation Harness。

### 实验设置
- **模型**：
  - Mixtral-8×7B（Top-2）
  - Mixtral-8×22B（Top-2）
  - DeepSeek-MoE-16B（Top-6 + 2 shared experts）
- **硬件环境**：
  - **GPU-only**：NVIDIA H100（80GB HBM3），专家从 DDR via PCIe 加载
  - **GPU-NDP**：H100 + NDP 设备（512GB/s 带宽，512GB 容量），支持近数据处理
- **输入/输出长度**：{256, 512} tokens
- **量化配置**：INT2 / INT3（主实验），FP16（原始精度对照）

### 评估指标
- **准确性**：
  - MMLU、各常识推理任务平均准确率
  - WikiText2 上的 Perplexity (PPL)
- **系统性能**：
  - End-to-end 吞吐量（tokens/s）
  - PCIe/NVLink 数据传输量
- **消融研究**：
  - 不同 $n$（恢复专家数量）、rank budget、是否使用 kurtosis 指导的影响

### 基线方法对比
| 类别 | 基线方法 |
|------|--------|
| **算法级量化** | GPTQ, HQQ |
| **系统级 offloading** | Mixtral-Offloading, Hobbit |
| **GPU-NDP 系统** | MoNDE |

---

## 3. 主要实验结果和性能指标

### 准确性结果（Figure 6）
#### 在 INT2 量化下的平均准确率提升（vs. HQQ）：
| 模型 | 平均准确率提升 | MMLU 提升 |
|------|----------------|-----------|
| Mixtral-8×7B | **+8.24%** | **+10.50%** |
| Mixtral-8×22B | **+8.70%** | **+2.83%** |
| DeepSeek-MoE-16B | **+2.30%** | **+1.73%** |

> 💡 **说明**：Mixtral 系列路由高度集中（Top-1 占主导），因此仅恢复 Top-1 即可大幅恢复精度；而 DeepSeek 路由较均衡，需恢复更多专家（Top-3）才有效果。

### 系统性能（Figure 7）
#### GPU-only 场景下吞吐量提升（vs. Mixtral-Offloading）：
| 模型 | INT3 提升 | INT2 提升 |
|------|----------|----------|
| Mixtral-8×7B | **5.17×** | **7.64×** |
| Mixtral-8×22B | **5.18×** | **7.63×** |
| DeepSeek-MoE-16B | ~4.38–5.93× | — |

#### GPU-NDP 场景下吞吐量提升（vs. MoNDE）：
| 模型 | INT3 提升 | INT2 提升 |
|------|----------|----------|
| Mixtral-8×7B | **4.75×** | **6.69×** |
| Mixtral-8×22B | ~7.23× | — |
| DeepSeek-MoE-16B | **3.15× – 3.78×** | — |

> ✅ 总体实现 **3× 到 8× 的端到端加速**，主要得益于绝大多数专家以低比特执行，仅少量关键专家触发补偿传输。

### 消融实验结果（Figure 8 & Table 2）

#### （1）恢复专家数量的影响
- **Mixtral-8×7B**：仅恢复 Top-1 即可恢复大部分精度（MMLU: 47.53% → Top-2 仅增至 48.79%）
- **DeepSeek-MoE-16B**：需恢复 Top-3 才达最佳效果（MMLU 从 25.80% → 28.75%）

> 📌 表明 **router score 分布决定最优 $n$**

#### （2）补偿秩预算的影响
- 随 rank 增加，WikiText PPL 下降（精度上升），但传输开销线性增长
- Rank=32 是性价比最优点（PPL 显著下降，开销 <1% 单个 INT2 专家大小）

#### （3）kurtosis-guided vs. uniform rank 分配
| Rank Budget | Uniform Allocation (PPL) | Kurtosis-Guided (PPL) |
|------------|----------------------------|------------------------|
| 16 | 7.55 | **7.05** |
| 128 | 8.54 | **3.77** |

> ✅ 显示 **基于 kurtosis 的异构秩分配显著优于统一分配**，验证了专家间补偿需求的异质性。

---

## 4. 关键结论和发现

### 主要发现
1. **MoE 中的量化误差具有高度非均匀性**：Top-1 专家的精度损失对整体性能影响最大，应优先保护。
2. **“选择性精度恢复” 可实现高效带宽-精度平衡**：仅恢复 Top-$n$ 专家即可接近全补偿性能，避免全局高带宽消耗。
3. **kurtosis 是预测量化误差的良好指标**：可用于指导补偿资源分配，提升补偿效率。
4. **本方法与现有系统正交且兼容**：可集成进 Mixtral-Offloading、Hobbit、MoNDE 等系统，带来显著加速。

### 方法的局限性
- **依赖离线 SVD 计算**：需要额外预处理时间生成补偿因子。
- **补偿仅限线性层**：目前未覆盖 Attention 或复杂非线性模块。
- **对路由分布敏感**：若模型路由过于分散（如 DeepSeek），收益相对较小。
- **GPU 内存占用略增**：需缓存补偿因子，尽管总量很小。

### 未来工作方向
- **动态调整 $n$**：根据当前 token 的路由熵自适应选择恢复专家数。
- **更高效的补偿结构**：探索非 SVD 形式的轻量补偿模块（如 LoRA-style）。
- **硬件协同设计**：定制 NDP 或 PIM 架构直接支持补偿操作。
- **跨层联合补偿**：考虑多层专家间的相关性进行联合误差校正。

---

> 🔚 **总结**：该论文提出了一个简洁而高效的 MoE 推理优化范式——**Low-Rank Compensation + Router-Guided Selection**，在极低比特传输下实现了高精度恢复，显著提升了带宽利用率和系统吞吐量，为大规模 MoE 模型的实际部署提供了可行路径。

</details>

---

### 7. [Learning solution operator of dynamical systems with diffusion maps kernel ridge regression](https://arxiv.org/abs/2512.17203)

**Authors**: Jiwoo Song, Daning Huang, John Harlim  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.17203v1  

#### Abstract
Many scientific and engineering systems exhibit complex nonlinear dynamics that are difficult to predict accurately over long time horizons. Although data-driven models have shown promise, their performance often deteriorates when the geometric structures governing long-term behavior are unknown or ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Learning Solution Operator of Dynamical Systems with Diffusion Maps Kernel Ridge Regression**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
许多科学与工程系统表现出复杂的非线性动力学行为，其长期演化通常被限制在低维的**前向不变集**（forward invariant set）上，例如光滑流形（如刚体旋转）或分形吸引子（如混沌系统）。然而，这些几何结构通常是未知的，且难以显式建模。传统数据驱动方法（如神经网络、算子学习）在缺乏对内在几何约束的尊重时，往往在长时预测中迅速发散。

本文旨在解决以下挑战：
- 如何在不显式重建流形或估计切空间的前提下，让模型自动适应系统的**内在几何结构**；
- 如何在小样本条件下实现高精度、稳定的**长时预测**；
- 如何设计一个简单但强大的基线框架，超越当前复杂的深度学习方法。

---

### **提出的新方法与新思路**

作者提出了 **Diffusion Maps Kernel Ridge Regression (DM-KRR)**，一种基于核方法的动力系统求解算子学习框架，其核心思想如下：

#### ✅ **新方法：DM-KRR**
- 将每个状态转移 $ x(t+\Delta t) $ 或增量 $ \Delta x $ 视为从当前状态 $ x(t) $ 到目标的映射；
- 使用 **Kernel Ridge Regression (KRR)** 学习该映射；
- 采用由 **Diffusion Maps (DM)** 构造的**数据驱动核函数**作为 KRR 的核，而非传统的 RBF 核。

#### ✅ **关键创新点**
1. **几何感知的核设计（Geometry-aware Kernel）**  
   DM 核通过热扩散过程隐式编码了数据所在不变集的**内蕴几何结构**，即使该集合是分形（如 Lorenz 吸引子），也能有效逼近其拓扑与度量性质。

2. **无需显式流形重构**  
   不需要像 GMKRR 那样估计切空间或法向校正，也不依赖 autoencoder 进行潜空间建模，避免了误差累积和训练复杂性。

3. **动态一致的验证策略（Dynamics-aware Validation）**  
   超参数选择不基于一步预测误差（如 RMSE），而是基于**短时间窗口内的迭代预测能力**（如 Valid Prediction Time, VPT），确保模型具备良好的长期稳定性。

4. **统一处理多种动力学类型**  
   适用于：
   - 光滑流形上的规则动力学（如 Torus）
   - 混沌吸引子（如 Lorenz-63, Kuramoto-Sivashinsky）
   - 高维时空场（如 pitching-plunge 平板流动）

---

### **相比现有方法的优势**

| 方法类别 | 局限性 | DM-KRR 的优势 |
|--------|------|-------------|
| **Neural Networks (NODE, LDNet, CANDyMan)** | 需要大量训练数据；易偏离不变集；超参调优困难；计算成本高 | 更少训练数据即可达到更高精度；无需显式几何先验；训练更快 |
| **Operator Learning (DeepONet, Fourier Neural Operators)** | 结构复杂；样本效率低；难以保证几何一致性 | 结构极简；样本效率显著更高 |
| **Random Feature Models (e.g., DeepSkip)** | 表达能力有限；仍需大量数据 | 在更少数据下实现更长 VPT |
| **GMKRR** | 依赖切空间估计，无法处理分形结构 | 可自然扩展至非光滑不变集 |
| **ResDMD / DMD-family** | 易受谱污染影响；特征值不准导致长期漂移 | 更稳定地保持周期性和能量守恒 |

> 💡 **核心优势总结**：**简单 + 几何感知 + 动态验证 = 强大的长时预测性能**

---

## **2. 核心实验方法和设置**

### **使用的数据集与系统**

| 系统 | 类型 | 状态维度 | 不变集特性 |
|-----|------|---------|-----------|
| **Torus Dynamics** | 流形动力学 | 3–15 | 光滑二维流形（单位球面映射到环面） |
| **Lorenz-63 System** | 混沌系统 | 3 | 分形吸引子（~2.06 维） |
| **Kuramoto-Sivashinsky (KS) Equation** | PDE / 混沌时空场 | 64 | Kaplan-Yorke 维数 ~5.2 |
| **KS Travelling Waves** | 规则波传播 | 64 | 2D 环面（双时间尺度） |
| **Pitch-Plunge Flat Plate** | 高维流体模拟 | 179,101 | 低维不变子流形（PCA 降维后使用） |

---

### **实验设置**

- 所有实验均采用 **rollout prediction** 方式进行长时预测；
- 输入输出构造方式：
  - **Direct Form**: $ x_{t+1} = f(x_t) $
  - **Skip-Connection Form**: $ x_{t+1} - x_t = f(x_t) $（用于非刚性系统）
- 使用 **skip-connection 形式提升数值稳定性**

---

### **评估指标**

| 指标 | 定义 | 用途 |
|------|------|------|
| **RMSE** | $ \frac{1}{T}\sum_{i=1}^T \|x_i - \hat{x}_i\|^2 $ | 短期预测误差 |
| **Valid Prediction Time (VPT)** | 最大时间长度使得归一化误差 $ E_i < \gamma $（通常取 γ=0.3~0.5） | 衡量**长期预测能力**，以最大李雅普诺夫时间 $ T_\Lambda = 1/\lambda_{\text{max}} $ 为单位 |
| **Convergence Rate** | RMSE 随训练样本数 $ N $ 的衰减速率 | 衡量**样本效率** |

---

### **基线方法对比**

| 基线方法 | 类型 |
|--------|------|
| **RBF-KRR** | 使用高斯径向基函数核的 KRR（作为基准） |
| **GMKRR** | 几何约束的算子值核回归（需流形先验） |
| **CANDyMan** | 基于图册（atlas）的神经网络流形学习 |
| **NODE / LDNet** | 神经微分方程及其潜空间版本 |
| **ResDMD** | 抗谱污染的动态模态分解 |
| **DeepSkip [24]** | 当前最先进的随机特征混沌预测模型（使用 50,000 数据点） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### 🔹 **Torus Dynamics（流形系统）**
- **收敛速率**：
  - DM-KRR: $ O(N^{-4.3} \sim N^{-5.06}) $
  - RBF-KRR: $ O(N^{-3.84} \sim N^{-3.91}) $
- 当 $ N=8192 $ 时，DM 的 RMSE 比 RBF **低一个数量级**

> ✅ 表明 DM 核具有更强的几何适应能力和更高的样本效率

---

#### 🔹 **Lorenz-63（混沌系统）**
| 模型 | $ N=512 $ | $ N=1024 $ | $ N=2048 $ | $ N=4096 $ |
|------|------------|-------------|-------------|-------------|
| **DM-KRR** | 11.03 ± 2.41 | **13.18 ± 1.25** | **13.84 ± 0.59** | **14.10 ± 0.46** |
| **RBF-KRR** | 8.87 ± 1.71 | 11.20 ± 1.42 | 12.72 ± 0.92 | 13.47 ± 0.61 |

- 在 $ N=1024 $ 时，DM-KRR 达到 **13.18 TA** 的 VPT，仅用约 4000 数据点；
- 对比 DeepSkip [24]：使用 **50,000 数据点**，VPT ≈ 12.02 TA；
- ➜ DM-KRR **用不到 1/12 的数据，获得更长的预测时间**

---

#### 🔹 **Kuramoto-Sivashinsky（高维混沌 PDE）**
| 模型 | $ N=2048 $ | $ N=4096 $ | $ N=8192 $ | $ N=16384 $ |
|------|-------------|-------------|-------------|--------------|
| **DM-KRR** | 0.86 ± 0.15 | **1.72 ± 0.18** | **3.12 ± 0.21** | **4.98 ± 0.23** |
| **RBF-KRR** | 0.79 ± 0.13 | 1.49 ± 0.21 | 2.72 ± 0.26 | 4.31 ± 0.33 |

- DM 始终优于 RBF，且差距随数据量增大而扩大；
- 表明 DM 能更好地利用额外数据提升泛化能力。

---

#### 🔹 **KS Travelling Waves（规则波）**
- 所有 NN 基线（NODE, LDNet, CANDyMan）表现差，误差积累严重；
- GMKRR 因引入了几何先验，表现较好（但仍受限于切空间估计精度）；
- **DM-KRR 和 RBF-KRR 错误降低 5–6 个数量级**；
- **DM 比 RBF 再低一个数量级**

> ✅ 即使没有显式几何先验，DM-KRR 也能自动捕捉内在结构

---

#### 🔹 **Pitch-Plunge Flat Plate（真实流体问题）**
- 状态维度高达 **179,101**，使用 PCA 降至 81 维；
- RBF-KRR 完全失败，无法捕捉涡脱落现象；
- ResDMD 初期尚可，但误差持续增长（因特征值偏移）；
- **DM-KRR 不仅初始误差最低，且在整个 20 周期内保持稳定误差水平**

> ✅ 显示 DM-KRR 在高维真实物理系统中的鲁棒性和长期稳定性

---

### **消融实验与分析**

- **验证策略的重要性**：
  - 若仅用一步 RMSE 选参，模型倾向于过拟合短期行为，长时预测崩溃；
  - 使用 **multi-step VPT 验证** 可选出真正适合长期演化的超参数。
- **核的选择决定上限**：
  - 相同 KRR 框架下，**DM 核 > RBF 核**，说明核的设计比模型复杂度更重要。
- **跳连结构（skip-connection）提升稳定性**：
  - 特别适用于非刚性系统（如 KS 方程）。

---

## **4. 关键结论和发现**

### **主要发现**

1. ✅ **简单的 KRR 框架可以成为强大的基线模型**  
   只要配合合适的核和验证策略，Scalar-valued KRR 就能在多种动力系统上超越复杂的 NN 和算子学习方法。

2. ✅ **几何感知的核至关重要**  
   DM 核能自动适应不变集的内蕴几何，无论是光滑流形还是分形结构，均表现出优越的表达能力。

3. ✅ **动态一致的验证策略是成功的关键**  
   “预测得像不像” 应由“能否长期迭代”来评判，而不是单步误差最小化。

4. ✅ **样本效率极高**  
   在多个任务中，DM-KRR 用远少于 SOTA 方法的数据量实现了更好的长时预测性能。

5. ✅ **无需显式建模流形或吸引子**  
   避免了流形学习、切空间估计等易错步骤，简化了流程。

---

### **方法的局限性**

| 局限性 | 说明 |
|-------|------|
| **计算复杂度** | KRR 需求解 $ N\times N $ 核矩阵，时间复杂度 $ O(N^3) $，不适合超大数据集 |
| **内存消耗大** | 存储核矩阵需 $ O(N^2) $ 内存，在百万级数据上不可行 |
| **固定带宽 DM 核** | 当数据密度不均时可能失效，建议使用 variable-bandwidth DM 改进 |
| **噪声敏感** | 原始框架未考虑观测噪声，实际应用中可能需结合滤波器（如 Kalman）进行去噪 |

---

### **未来工作方向**

1. **加速算法**：引入快速求解器（如 randomized sketching、sparse Cholesky）以支持更大规模数据；
2. **可变带宽核**：使用 **variable-bandwidth Diffusion Maps** 提升对非均匀采样数据的适应性；
3. **在线学习与自适应采样**：结合主动学习策略减少数据需求；
4. **与物理模型融合**：将 DM-KRR 用于修正已知模型的残差项（partial data-driven setting）；
5. **不确定性量化**：结合 GP 或 Bayesian KRR 提供置信区间；
6. **实时部署**：探索低秩近似与边缘计算集成。

---

## **总结**

> 🌟 **DM-KRR 是一个“大道至简”的典范：它用最简单的模型结构，借助数据驱动的几何感知核和动态一致的验证机制，在多样化的动力系统上实现了卓越的长时预测性能。**

该工作揭示了一个深刻洞见：**在科学机器学习中，模型的成功不仅取决于表达能力，更在于是否尊重了数据背后的物理与几何规律。**  
未来的研究应更多关注“如何让模型与系统结构对话”，而非一味堆叠复杂架构。

</details>

---

### 8. [Bridging Training and Merging Through Momentum-Aware Optimization](https://arxiv.org/abs/2512.17109)

**Authors**: Alireza Moayedikia, Alicia Troncoso  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.17109v1  

#### Abstract
Training large neural networks and merging task-specific models both exploit low-rank structure and require parameter importance estimation, yet these challenges have been pursued in isolation. Current workflows compute curvature information during training, discard it, then recompute similar inform...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Bridging Training and Merging Through Momentum-Aware Optimization 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大模型训练和多任务模型合并（model merging）通常被作为两个独立流程处理：
- **训练阶段**：使用如 Adam 等优化器时，会计算梯度、动量和曲率（curvature）等信息用于自适应学习率调整，但在训练结束后这些信息被丢弃。
- **合并阶段**：在合并多个任务特定模型时，需要重新计算参数重要性（如通过 Fisher 信息矩阵），这导致大量冗余计算。

这种“先训练再重新分析”的范式浪费了训练过程中积累的宝贵轨迹信息，且后处理的 Fisher 计算成本高昂。

### 提出的新方法：UMTAM
作者提出 **UMTAM**（Unified Momentum-Trajectory Aware Training and Merging），一个统一框架，将训练与模型合并结合，其核心思想是：
> **将优化过程中的动量与曲率统计信息保留并复用于后续的模型合并，避免重复计算。**

#### 主要创新点：
1. **双低秩因子化（Dual Momentum Factorization）**  
   在训练中维护低秩形式的动量 $ U\Sigma V^\top $ 和因子化的二阶矩统计量（$ R \in \mathbb{R}^m, C \in \mathbb{R}^n $），实现内存高效优化。

2. **误差反馈机制（Error Feedback）**  
   将每次 SVD 压缩产生的残差累积并反馈到下一次更新中，防止信息永久丢失，保障收敛性。

3. **曲率感知显著性评分（Curvature-Aware Saliency）**  
   在训练过程中持续累积每个参数的重要性得分：
   $$
   S_{ij}^{(T)} = (W_{ij} - W_{0,ij})^2 \cdot \sqrt{R_i C_j}
   $$
   即结合了参数偏离初始值的程度和局部损失曲率，提供更丰富的几何感知重要性信号。

4. **可重用的训练轨迹资产化**  
   不再将优化轨迹视为临时中间产物，而是将其作为可复用的“资产”，直接用于下游的模型合并任务。

### 相比现有方法的优势
| 维度 | 传统方法 | UMTAM |
|------|--------|-------|
| 内存效率 | 高（如 GaLore）但信息丢弃 | 同样高效，且保留关键统计信息 |
| 合并质量 | 依赖后验 Fisher 或 magnitude | 利用训练中已有的曲率信息，更准确 |
| 计算开销 | 训练 + 后处理 Fisher 计算 | 无额外开销，训练即为准备 |
| 超参鲁棒性 | 对 rank 和 lr 敏感 | 展现 rank-invariant 收敛特性 |
| 理论支持 | 多数缺乏完整理论 | 提供非凸目标下的收敛保证、泛化界等 |

---

## 2. 核心实验方法和设置

### 数据集
- **GLUE Benchmark**：用于验证多任务合并效果，包含四个任务：
  - RTE（文本蕴含）
  - MRPC（语义匹配）
  - QNLI（问答推理）
  - CoLA（语法可接受性判断）
- **FineWeb**：用于 GPT-2（124M）上的训练动态分析。
- **Tulu-3-sft-mixture**：用于 Mistral-7B 上的指令微调实验。

### 实验设置
- **模型架构**：
  - BERT-base（110M 参数）用于 GLUE 微调与合并实验
  - GPT-2（124M）用于训练性能比较
  - Mistral-7B 用于大规模指令微调
- **训练配置**：
  - 使用 LoRA（rank=64）、4-bit 量化、梯度累积等现代高效训练技术
  - 学习率范围：$3\times10^{-5}$ 至 $5\times10^{-4}$
  - 因子化 rank 设置为 8–128
- **评估指标**：
  - 分类任务：Accuracy, F1, MCC
  - 语言建模：Validation Loss, Perplexity
  - 合并性能：平均得分、与单任务专家差距
  - 超参敏感性：不同 sparsity 下的表现稳定性

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Linear Averaging (Task Arithmetic)** | 简单平均 | 忽略冲突与重要性 |
| **TIES-Merging** | magnitude-based pruning + sign voting | 当前主流基线 |
| **DARE** | 高稀疏性 delta pruning | 强调冗余性 |
| **DARE+TIES** | 组合方法 | 强基线 |
| **GaLore**, **MoFaSGD** | 低秩训练优化器 | 比较训练性能与稳定性 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 多任务合并性能（Table 2）
| Method | RTE | MRPC | QNLI | CoLA | **Avg** |
|--------|-----|------|------|------|--------|
| Experts (UB) | 0.607 | 0.883 | 0.816 | 0.562 | 0.717 |
| Linear | 0.560 | 0.807 | 0.645 | 0.339 | 0.588 |
| TIES | 0.574 | 0.851 | 0.781 | 0.452 | 0.664 |
| **UMTAM** | **0.578** | **0.855** | **0.782** | **0.487** | **0.675** |

- **UMTAM 平均提升 +1.6% vs TIES，+14.9% vs Linear**
- 在 CoLA 上提升最大（+7.6%），说明对复杂语法任务更有效

#### ✅ 稀疏性敏感性分析（Table 3 & Figure 4）
- 在所有 sparsity 水平上，UMTAM 均优于 TIES
- 在极端稀疏（k=5%）时优势最大（+6.4%）
- UMTAM 性能波动仅 2.8%，而 TIES 达 7.1%，表明更强的超参鲁棒性

#### ✅ 训练性能对比（Table 4–6）
- 在 $ \eta = 3\times10^{-5} $ 下，UMTAM 最终 loss: **2.044**
  - vs GaLore: 2.118（↓3.5%）
  - vs MoFaSGD: 2.187（↓7.0%）
- 在高学习率 $ \eta = 1\times10^{-4} $ 下，MoFaSGD 出现严重不稳定（loss 从 2.129 → 3.800 @ rank=16），而 UMTAM 保持稳定

#### ✅ 消融实验（Table 9 & 10）
| 方法 | Avg Score | Δ vs Full |
|------|----------|---------|
| Full UMTAM | **0.667** | — |
| -Curv. Pruning | 0.659 | -1.18% |
| -Sign Election | 0.663 | -0.60% |
| -Curv. Aggregation | 0.663 | -0.70% |
| Linear Averaging | 0.585 | -12.31% |

- **各组件均有正向贡献**，尤其 curvature-aware pruning 最关键（+1.18%）
- 所有组件协同作用明显：单独移除影响小，全部移除则性能暴跌

#### ✅ 指令微调实验（Table 11）
| Optimizer | Val Loss | Perplexity |
|----------|----------|------------|
| AdamW | 0.762 | 2.14 |
| UMTAM | **0.761** | **2.14** |

- **训练性能与 AdamW 完全相当**，证明新增功能不影响优化轨迹
- 可无缝集成 LoRA、4-bit 量化等现代技术

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **训练过程中的动量与曲率信息极具价值**，不应丢弃，可用于指导模型合并。
2. ✅ **曲率感知显著性（curvature-aware saliency）显著优于 magnitude-only 方法**，尤其是在高稀疏条件下。
3. ✅ **UMTAM 实现了 rank-invariant 收敛**：在不同 rank 设置下性能几乎不变，极大降低调参难度。
4. ✅ **UMTAM 在多任务合并中恢复了 96.2% 的专家性能**，远超线性平均（84.4%）。
5. ✅ **无需额外计算即可实现高质量合并**：相比需后验 Fisher 的方法，节省大量 forward/backward passes。

### 方法的局限性
- **内存开销略高于标准 AdamW**：因需存储 $ R, C, S_T, W_0 $ 等，峰值内存增加约 30%（见 Table 8）。
- **对某些任务增益有限**：如 CoLA 中 curvature-aggregation 反而轻微下降，可能因其曲率分布较平坦。
- **目前未探索动态 rank 调整策略**：虽支持 adaptive rank，但未深入研究其潜力。

### 未来工作方向
1. **扩展至 Continual Learning**：利用轨迹信息进行 selective plasticity 控制。
2. **联邦学习场景应用**：客户端本地训练后上传低秩统计量，实现通信高效的聚合。
3. **设计面向合并的专用优化器**：从一开始就以“可合并性”为目标优化训练过程。
4. **结合 Bayesian merging 框架**：将 UMTAM 的 saliency 视为不确定性估计输入。
5. **自动化组件选择机制**：根据不同任务自动启用/关闭 sign election 或 curvature aggregation。

---

## 总结
UMTAM 成功地将 **memory-efficient training** 与 **principled model merging** 统一起来，提出了“**训练即为合并做准备**”的新范式。它不仅在性能上超越现有方法，更重要的是改变了我们看待优化轨迹的方式——不再是临时中间状态，而是宝贵的、可复用的知识载体。这一视角转变有望推动更智能、更高效的多任务模型生命周期管理。

</details>

---

### 9. [Deep Learning-Based Surrogate Creep Modelling in Inconel 625: A High-Temperature Alloy Study](https://arxiv.org/abs/2512.17477)

**Authors**: Shubham Das, Kaushal Singhania, Amit Sadhu, Suprabhat Das, Arghya Nandi  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.17477v1  

#### Abstract
Time-dependent deformation, particularly creep, in high-temperature alloys such as Inconel 625 is a key factor in the long-term reliability of components used in aerospace and energy systems. Although Inconel 625 shows excellent creep resistance, finite-element creep simulations in tools such as ANS...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Deep Learning-Based Surrogate Creep Modelling in Inconel 625: A High-Temperature Alloy Study*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统基于 **Finite Element Method (FEM)** 的蠕变（creep）模拟（如使用 ANSYS）虽然精度高，但计算成本极高——单次 10,000 小时的模拟需耗时 **30–40 分钟**，严重限制了其在实时设计优化、结构健康监测等场景中的应用。

此外，已有机器学习模型在处理长期蠕变行为时存在以下不足：
- 缺乏对长序列时间依赖性的有效建模；
- 难以量化预测不确定性（uncertainty quantification）；
- 多数验证基于短期或有限数据集，难以推广到工业级长期预测。

### 🚀 提出的新方法与创新思路
本文提出两种基于深度学习的 **surrogate models（代理模型）**，用于替代传统的 FEM 模拟，实现快速、准确且可靠的蠕变应变预测：

1. **BiLSTM-Transformer**  
   - 结合 **BiLSTM** 捕捉局部时间动态；
   - 引入 **Transformer 的 self-attention 机制** 建模长程时间依赖（long-term temporal dependencies），特别适用于跨越 10,000 小时的完整蠕变曲线（含 primary, secondary, tertiary 阶段）；
   - 提供高精度确定性预测。

2. **BiLSTM-VAE（Variational Autoencoder）**  
   - 融合 **BiLSTM 编码器-解码器结构** 与 **VAE 的概率生成框架**；
   - 可输出概率分布而非单一值，支持 **不确定性量化（uncertainty quantification）**；
   - 支持生成多样化合理路径，增强鲁棒性和可靠性。

> 💡 创新亮点：首次将 **BiLSTM + Transformer** 和 **BiLSTM + VAE** 架构系统应用于高温合金 Inconel 625 的全周期蠕变建模，并实现高效推理与风险感知能力。

### 🔍 相比现有方法的优势
| 维度 | 优势说明 |
|------|----------|
| **速度** | 推理时间从 ANSYS 的 30–40 分钟缩短至 **秒级甚至毫秒级**（最高仅 2.17 秒） |
| **精度** | 在训练和验证集上均显著优于 baseline LSTM，R² 最高达 **0.9729** |
| **功能扩展性** | BiLSTM-VAE 提供不确定性估计，适合安全关键工程决策；Transformer 更擅长远期趋势捕捉 |
| **可扩展性** | 框架可推广至其他高温合金材料与多物理场耦合场景 |

---

## 2. 核心实验方法和设置

### 📊 数据集来源与构建方式
- **数据生成工具**：ANSYS Mechanical（FEM 软件）
- **目标材料**：Inconel 625（镍基高温合金）
- **加载条件**：
  - 应力范围：50–150 MPa（步长 25 MPa，共 5 级）
  - 温度范围：700–1000°C（步长 100°C，共 4 级）
  - 总计：**20 个独立应力-温度组合**
- **模拟时长**：每个案例持续 **10,000 小时**，覆盖完整的三阶段蠕变过程
- **本构模型**：采用 **Norton creep law** 的 time-hardening 形式进行 FEM 设置，并通过文献数据校准参数（见 Table 2）

> 所有仿真数据构成高质量、高保真的 time-series 数据集，作为深度学习模型的训练基础。

### ⚙️ 实验设置
- **输入特征**：`{σ, T, t}` → 输出：`ϵc(t)`（等效蠕变应变）
- **预处理步骤**：
  1. 对原始蠕变应变 `ε` 进行对数变换：`ε* = log(1 + ε)`
  2. Min-Max 归一化（按训练集统计量）
  3. 构造多变量时间序列矩阵（shape: `[B, L=10000, D=3]`）
- **数据划分**：
  - 训练集：16 条序列（不同 σ-T 组合）
  - 验证集：4 条序列（手动选择以保证参数空间覆盖）
- **硬件平台**：Google Colab 上的 Tesla T4 GPU
- **训练配置**：
  - Epochs: 100
  - Batch 更新 + 早停监控
  - 最佳模型保存依据：最高训练 R²

### 📈 评估指标
| 指标 | 公式/用途 |
|------|---------|
| **RMSE** | 衡量预测误差大小 |
| **MAE** | 平均绝对误差，更稳健于异常值 |
| **R² (Coefficient of Determination)** | 回归拟合优度，越接近 1 越好 |
| **Latency (inference time)** | 单条 10,000 小时序列的推理延迟（ms） |

### 🆚 基线方法对比
- **Baseline Model**：单向 **unidirectional LSTM**（1 层，hidden dim=32，dropout=0.1）
- 对比对象：
  - BiLSTM-Transformer
  - BiLSTM-VAE
- 所有模型均在同一训练/验证集上比较性能与延迟

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 5）

| Model | Val RMSE | Val R² | Val MAE | Latency (ms/seq) |
|-------|----------|--------|---------|------------------|
| **BiLSTM-Transformer** | 0.014128 | **0.960695** | 0.011166 | 2170.14 |
| **BiLSTM-VAE** | **0.011728** | **0.972917** | **0.008234** | **46.81** |
| **Baseline LSTM** | 0.016637 | 0.945494 | 0.013800 | 43.86 |

> 注：所有预测结果已逆变换回原始尺度进行评估。

### 🔍 与基线方法的对比结果
- **精度方面**：
  - BiLSTM-VAE 在 **所有指标上全面领先**，尤其是 R² 达到 **0.9729**，显著高于 baseline 的 0.9455；
  - BiLSTM-Transformer 精度略低于 VAE，但仍明显优于 baseline；
- **推理效率方面**：
  - BiLSTM-VAE 和 baseline LSTM 推理极快（约 **45 ms**），适合实时部署；
  - BiLSTM-Transformer 因 self-attention 计算开销较大，延迟为 ~2.17 s，但仍远优于 ANSYS（30–40 min）；
- **综合表现**：
  - BiLSTM-VAE 实现了“高精度 + 低延迟”的最佳平衡；
  - BiLSTM-Transformer 特别擅长捕捉长期演化模式，在外推任务中更具潜力。

### ❌ 消融实验（未明确开展）
文中未提供正式的消融研究（ablation study），例如移除 BiLSTM 或 Attention 模块的影响分析。但通过架构设计逻辑和性能差异间接体现了各组件的作用：
- BiLSTM 提升了对双向时间上下文的理解；
- Transformer 的 self-attention 显著增强了长序列建模能力；
- VAE 的 latent space 设计带来了更好的泛化能力和不确定性表达。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **深度学习代理模型可有效替代 FEM 模拟**：
   - 在保持高精度的同时，将推理时间从 **分钟级压缩至毫秒级**，提速数千倍；
   - 为 **real-time design optimization** 和 **structural health monitoring** 提供可行方案。

2. **BiLSTM-VAE 是最优综合选择**：
   - 不仅预测最准确（Val R² = 0.9729），而且具备不确定性量化能力；
   - 推理速度快，适合边缘设备部署。

3. **BiLSTM-Transformer 擅长长期建模**：
   - 自注意力机制能有效识别关键过渡阶段（如 secondary → tertiary）；
   - 尽管推理稍慢，但在需要高保真长期预测的应用中具有优势。

4. **模型泛化能力强**：
   - 在未见过的应力-温度组合下仍表现出色，表明模型学到了内在物理规律而不仅是记忆数据。

### ⚠️ 方法的局限性
- **依赖高质量仿真数据**：当前模型训练数据全部来自 ANSYS 模拟，尚未与真实实验数据直接验证；
- **未涵盖复杂载荷工况**：目前仅考虑 **uniaxial stress** 和恒温条件，未涉及热循环或多轴应力；
- **缺乏物理解释性**：尽管性能优越，但仍是“黑箱”模型，缺乏像传统 constitutive models 那样的明确物理意义；
- **Transformer 推理延迟较高**：对于极端实时场景可能仍需进一步优化。

### 🔮 未来工作方向
1. **引入多阶段实测数据**：整合包含完整 primary 和 tertiary 阶段的真实蠕变实验数据；
2. **融合物理信息**：发展 **Physics-Informed Neural Networks (PINNs)**，嵌入 Norton、Garofalo 或 time-hardening 方程作为约束；
3. **多轴与非稳态建模**：拓展至热机械疲劳（TMF）、循环载荷等更复杂工况；
4. **集成与部署**：
   - 构建 **ensemble model** 融合 VAE 与 Transformer 的优势；
   - 开发轻量化版本用于 **edge computing**，实现现场在线监测；
5. **不确定性传播分析**：研究输入参数（如材料常数）不确定性如何影响最终预测置信区间。

---

> 📌 **总结一句话**：  
> 本文成功构建了两个高性能深度学习 surrogate models（BiLSTM-Transformer 和 BiLSTM-VAE），实现了对 Inconel 625 合金蠕变行为的**高速、高精度、可解释性强**的预测，为高温合金结构的设计与健康管理提供了新一代智能化工具。

</details>

---

### 10. [NetworkFF: Unified Layer Optimization in Forward-Only Neural Networks](https://arxiv.org/abs/2512.17531)

**Authors**: Salar Beigzad  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.17531v1  

#### Abstract
The Forward-Forward algorithm eliminates backpropagation's memory constraints and biological implausibility through dual forward passes with positive and negative data. However, conventional implementations suffer from critical inter-layer isolation, where layers optimize goodness functions independ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*NetworkFF: Unified Layer Optimization in Forward-Only Neural Networks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
原始的 **Forward-Forward (FF)** 算法虽然解决了传统 **backpropagation** 的内存开销大和生物不可行性等问题，但存在一个关键缺陷：**层间隔离（inter-layer isolation）**。各层在训练过程中独立优化各自的“goodness”函数，缺乏跨层协作机制，导致表征协调能力差、收敛效率低，尤其在深层网络中表现受限。

### 🚀 提出的新方法与创新思路
本文提出 **Collaborative Forward-Forward (CFF)** 学习框架，通过引入**层间协作机制**扩展原始 FF 算法，主要包括两种变体：

- **Fixed CFF (F-CFF)**：使用固定的协作参数 γ 控制层间耦合强度，实现稳定的信息共享。
- **Adaptive CFF (A-CFF)**：引入可学习的协作参数，允许模型在训练过程中动态调整层间影响权重，提升协同优化能力。

其核心思想是构建**协作型 goodness 函数**：
$$
G_{\text{collab}}^{(l)}(x) = G_l(x) + \gamma \sum_{k \neq l} \omega_k G_k(x)
$$
该函数整合了其他层的 goodness 输出，使每层能感知全局学习状态，从而实现统一的层优化目标。

### 🔍 相比现有方法的优势
- **保持 FF 核心优势**：无需反向传播，保留前向计算范式，具有低内存占用、高生物可解释性和黑盒兼容性。
- **增强学习有效性**：通过层间协作显著提升模型收敛速度与最终性能。
- **灵活架构设计**：支持固定与自适应协作机制，便于在不同场景下部署。
- **适用于资源受限系统**：为 **neuromorphic computing** 和边缘设备上的高效 AI 提供可行路径。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **MNIST**：手写数字识别任务，作为基准测试。
- **Fashion-MNIST**：服装图像分类任务，视觉复杂度更高，更具挑战性。

两者均为 $28 \times 28$ 灰度图，展平为 784 维输入向量。

### ⚙️ 实验设置
- **网络结构**：Input (784) → Hidden Layer 1 (500 ReLU) → Hidden Layer 2 (500 ReLU) → Output
- **标签嵌入（Label Embedding）**：沿用 Hinton 的原始方案，在输入图像前 10 个像素编码类别信息。
- **训练配置**：
  - 每层训练 1000 epochs
  - 批大小：全批量（50,000 样本）
  - 优化器：Adam（基础学习率 0.03）
  - A-CFF 协作参数学习率：0.01
  - Goodness 阈值 $\theta = 2.0$
  - 层归一化（post-activation）

### 🎯 评估指标
- 分类准确率（Training/Test Accuracy）
- 收敛速度与训练动态分析
- 协作参数演化轨迹（仅 A-CFF）
- 统计显著性检验（paired t-test, Cohen’s d）
- 泛化差距（Generalization Gap）
- 计算效率（训练时间、内存消耗）

### 🔁 基线方法对比
| 方法 | 描述 |
|------|------|
| **Original FF** | 原始 Forward-Forward 算法，无层间协作 |
| **F-CFF** | 固定协作参数（$\gamma=1.0$）的 CFF 版本 |
| **A-CFF** | 可学习协作参数的自适应 CFF 版本 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### 表格：跨数据集性能对比（Test Accuracy）

| Dataset / Model | Original FF | F-CFF | A-CFF | Improvement (vs Baseline) |
|------------------|------------|--------|--------|----------------------------|
| **MNIST**        | 92.0%      | 92.6%  | 93.8%  | +0.6% (F-CFF), **+1.8%** (A-CFF) |
| **Fashion-MNIST**| 81.0%      | 81.8%  | 83.0%  | +0.8% (F-CFF), **+2.0%** (A-CFF) |

> ✅ 所有改进均超过典型 FF 实验方差，具有实际意义。

### 🔬 与基线方法的对比结果
- **A-CFF 在两个数据集上均取得最高精度**，且在更复杂的 Fashion-MNIST 上相对提升更大（+2.0%），说明其对困难任务更具鲁棒性。
- **F-CFF 提供稳定的小幅增益**（+0.6–0.8%），验证了即使简单协作也能带来收益。
- **训练动态显示更强收敛性**：
  - 图2（MNIST）和图3（Fashion-MNIST）中的 log-scale 曲线表明，A-CFF 在最后阶段仍持续提升，而原始 FF 已趋于饱和。
  - A-CFF 达到更高的峰值 log-scale accuracy（MNIST: ~4.555；Fashion-MNIST: ~4.4345）。

### 🔍 消融实验与分析（隐含于实验设计）
尽管未明确标注“ablation study”，但以下对比构成有效消融分析：
- **是否启用协作机制**：Original FF vs F-CFF → 验证协作本身的有效性。
- **协作方式的影响**：F-CFF vs A-CFF → 显示可学习参数优于固定参数，证明动态协作更具潜力。
- **参数演化可视化**：A-CFF 中协作系数随训练演化的趋势揭示了模型自动调节层间依赖的能力。

此外，统计检验（paired t-test）确认了所有协作变体与原始 FF 之间的性能差异具有显著性（p < 0.05），效应量（Cohen’s d）也达到中等以上水平。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **层间协作显著提升 FF 性能**：首次将协作学习引入 Forward-Forward 框架，成功打破层间孤立状态，实现更协调的特征学习。
2. **自适应协作优于固定协作**：A-CFF 不仅性能最佳，还能通过参数演化反映学习过程中的动态交互模式。
3. **完全保留 FF 的核心优势**：整个 CFF 框架仍基于纯前向操作，不增加额外内存负担，维持了生物合理性和边缘部署友好性。
4. **具备广泛适用前景**：尤其适合 **neuromorphic computing** 和能源受限环境下的 AI 系统。

### ⚠️ 方法的局限性
- 当前实验集中在浅层 MLP 架构，尚未验证在更深或卷积结构中的扩展能力。
- 协作机制可能引入额外超参数（如初始 γ、协作学习率），需进一步自动化调参策略。
- 虽然保持生物合理性，但真实大脑是否存在类似“goodness broadcast”的机制尚无直接证据。

### 🔮 未来工作方向
- 将 CFF 扩展至 CNN、Transformer 等现代架构。
- 探索稀疏协作连接、注意力式加权等更高效的协作形式。
- 结合 predictive coding 或 dendritic computation 模型深化生物可解释性。
- 在真实类脑芯片（如 Loihi、SpiNNaker）上部署并验证能耗效益。

---

## ✅ 总结
本论文提出的 **Collaborative Forward-Forward (CFF)** 是对原始 FF 算法的重要增强，通过引入层间协作机制，在不牺牲其内存效率和生物合理性的前提下，显著提升了学习效果。实验证明其在 MNIST 和 Fashion-MNIST 上均优于基线，尤其是 **A-CFF 实现了高达 +2.0% 的准确率提升**，为构建高效、可解释、类脑的神经网络提供了新范式。

</details>

---

### 11. [PAACE: A Plan-Aware Automated Agent Context Engineering Framework](https://arxiv.org/abs/2512.16970)

**Authors**: Kamer Ali Yuksel  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.16970v1  

#### Abstract
Large Language Model (LLM) agents are increasingly deployed in complex, multi-step workflows involving planning, tool use, reflection, and interaction with external knowledge systems. These workflows generate rapidly expanding contexts that must be curated, transformed, and compressed to maintain fi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# PAACE: A Plan-Aware Automated Agent Context Engineering Framework 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLM）代理在执行复杂、多步骤任务时会积累大量上下文信息（如系统指令、推理链、工具调用结果、检索内容等）。随着任务长度增加，这些上下文变得冗长、嘈杂且低效，导致：
- **注意力稀释**（attention dilution）
- **上下文腐化**（context rot）
- 推理质量下降
- 推理成本上升（尤其是长窗口模型的二次计算开销）

传统方法（如摘要、检索增强生成 RAG 或简单截断）无法有效保留多步任务中的**计划结构依赖关系**和**未来任务相关性**。

---

### 🚀 提出的新方法：PAACE 框架
PAACE（**Plan-Aware Automated Context Engineering**）是一个统一的上下文工程框架，旨在通过建模任务规划结构来优化 LLM 代理的动态上下文状态。

#### 核心组件：
1. **PAACE-Syn**  
   - 大规模合成数据生成器，模拟带噪声的长周期代理工作流，并标注每一步的压缩监督信号。
   - 包含约 **1.2M 合成工作流**（共 ~9.5B tokens），涵盖文档处理、网页导航、多跳问答等场景。
   - 输出包含完整上下文与压缩后上下文的配对轨迹，用于训练压缩策略。

2. **PAACE-FT**  
   - 基于教师-学生蒸馏范式构建的一系列轻量级、**计划感知型上下文压缩器**。
   - 教师模型（如 GPT-OSS-120B）在 PAACE-Syn 上进行演化学习，生成高质量压缩示例。
   - 学生模型（如 Qwen3-4B-Instruct）通过监督学习模仿教师行为，实现高效部署。

#### 创新思路：
- **引入“next-k-task relevance”建模**：压缩时不仅考虑当前任务，还显式建模接下来 k 步的任务需求，确保关键中间状态不被误删。
- **联合优化上下文与指令**：支持重写、精炼指令以保持与计划一致性，防止“指令漂移”（instruction drift）。
- **端到端可学习的上下文塑造策略**：将上下文压缩视为一个基于结果保真度的学习策略，而非启发式编辑。
- **结构化上下文分解**：隐式建模上下文的功能角色（行动性、结构性、引用性、论证性），提升压缩语义保真度。

---

### 🔍 相比现有方法的优势
| 方法 | 局限性 | PAACE 改进 |
|------|--------|-----------|
| **LLMLingua / LLMLingua-2** | 仅关注单轮查询相关性，忽略多步依赖 | 显式建模 next-k 任务依赖 |
| **Self-RAG** | 适用于 QA 场景，缺乏计划结构建模 | 支持跨任务因果链保留 |
| **Provence** | 二值剪枝（keep/drop），无重写能力 | 支持重写、重构、压缩一体化操作 |
| **ACon (Yu et al., 2025b)** | 仅优化下一任务，不支持指令协同优化 | 支持多步前瞻与指令共演化 |
| **FIFO / Retrieval-based selection** | 忽视长期依赖，易丢失关键中间状态 | 保留全局计划结构 |

> ✅ **核心优势**：PAACE 是首个将**计划结构分析**、**多步任务相关性建模**、**指令协同优化**和**函数保持压缩**整合为统一框架的工作。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
1. **AppWorld**（Trivedi et al., 2024）  
   - 多应用交互任务（如邮件、日历、浏览器联动）
   - 观测异构性强，需跨应用状态追踪
2. **OfficeBench**（Wang et al., 2024）  
   - 面向办公自动化的文档处理任务链（PDF、Excel、Word 等）
   - 强调结构化操作与文件引用
3. **8-Objective QA**（Zhou et al., 2025）  
   - 多跳问答任务，结合工具搜索与证据聚合
   - 要求长期记忆与信息追溯

---

### ⚙️ 实验设置
- **主干模型一致**：所有方法使用相同的 LLM、规划器、工具接口和推理配置。
- **唯一变量**：上下文管理策略不同。
- **教师模型**：OpenAI GPT-OSS-120B（65,536 token 窗口）
- **学生模型**：Qwen3-4B-Instruct（蒸馏目标）
- **压缩频率**：每一步均调用压缩器更新上下文
- **合成数据来源**：PAACE-Syn 完全离线生成，不依赖人工标注

---

### 📈 评估指标
| 指标 | 描述 |
|------|------|
| **Acc / EM / F1** | 各基准定义的任务准确率、精确匹配率、F1 分数 |
| **Peak** | 轨迹中最大输入上下文长度（单位：千 tokens） |
| **Dependency** | 累积注意力负载：$\sum_t |C_t|$，近似总计算成本（单位：百万 tokens） |
| **Steps** | 完成任务所需步数 |
| **Compression Ratio** | $|C_{\text{compressed}}| / |C_{\text{full}}|$ |

> 注：所有方法在同一 backbone 下运行，因此 Dependency 可直接比较。

---

### 🆚 基线方法对比
| 基线 | 描述 |
|------|------|
| **No Compression** | 保留全部历史 |
| **FIFO** | 仅保留最近 k 轮对话 |
| **Retrieval** | 基于嵌入选择相关历史片段 |
| **LLMLingua** | 提取式上下文压缩 |
| **Prompting** | 使用提示词指导摘要（启发式） |
| **ACON UT / UTCO** | 当前最优上下文优化方法（Yu et al., 2025b），分别表示无/有指令优化版本 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### 表 1：AppWorld 结果
| Method | Acc↑ | Steps↓ | Peak↓ | Dependency↓ |
|--------|------|--------|-------|-------------|
| No Compression | 56.00 | 16.14 | 9.93 | 5.96 |
| FIFO | 45.80 | 28.48 | 6.73 | 5.69 |
| LLMLingua | 39.30 | 24.42 | 7.50 | 6.37 |
| ACON UTCO | 56.50 | 22.82 | 7.33 | 4.69 |
| **PAACE (ours)** | **59.00** | **19.20** | **6.23** | **3.75** |

> ✅ PAACE 在准确性上超越所有基线，同时显著降低峰值上下文和累计依赖。

---

#### 表 2：OfficeBench 结果
| Method | Acc↑ | Steps↓ | Peak↓ | Dependency↓ |
|--------|------|--------|-------|-------------|
| No Compression | 76.84 | 11.52 | 7.27 | 4.43 |
| LLMLingua | 70.53 | 10.89 | 4.65 | 1.85 |
| ACON UTCO | 72.63 | 11.54 | 4.54 | 1.91 |
| **PAACE (ours)** | **78.10** | **10.48** | **4.29** | **1.64** |

> ✅ 准确率最高，且上下文消耗最低。

---

#### 表 3：8-Objective QA 结果
| Method | EM↑ | F1↑ | Steps↓ | Peak↓ | Dependency↓ |
|--------|-----|-----|--------|-------|-------------|
| No Compression | 0.366 | 0.488 | 15.78 | 10.35 | 3.32 |
| LLMLingua | 0.363 | 0.481 | 17.68 | 5.68 | 2.24 |
| ACON UTCO | 0.335 | 0.458 | 17.79 | 4.65 | 1.50 |
| **PAACE (ours)** | **0.402** | **0.512** | **16.86** | **4.41** | **1.41** |

> ✅ 在多跳问答中也取得最佳表现，尤其在 F1 和 EM 上领先明显。

---

### 🔍 消融实验结果（Table 4）

| Benchmark | k | Acc/EM↑ | Peak↓ | Dependency↓ |
|----------|----|---------|--------|------------|
| AppWorld | 1 | 56.5 | 6.05 | 3.95 |
|          | 2 | **59.0** | 6.23 | **3.75** |
|          | 3 | 58.6 | 6.48 | 3.82 |
| OfficeBench | 1 | 76.3 | 4.15 | 1.72 |
|             | 2 | **78.1** | 4.29 | **1.64** |
|             | 3 | 77.6 | 4.51 | 1.69 |
| 8-Objective QA | 1 | 0.381 | 4.18 | 1.36 |
|                | 2 | 0.394 | 4.30 | 1.39 |
|                | 3 | **0.402** | **4.41** | **1.41** |

> 🔎 发现：
- 工具密集型任务（AppWorld, OfficeBench）：`k=2` 最优
- 多跳 QA 任务：需要更长视野 `k=3`
- 更大 `k` 导致压缩难度上升，收益递减

此外，禁用指令重写的变体会导致失败率显著上升，说明**隐式指令重塑**对鲁棒性至关重要。

---

### 💡 蒸馏效果（PAACE-FT）
- PAACE-FT（学生模型）保留了教师模型 **97–98% 的性能**
- 推理成本降低 **超过一个数量级**
- 每步延迟增加 <8%，但总 token 数减少 35–60%
- 可在边缘设备或低成本服务中部署

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **计划感知压缩优于传统方法**  
   显式建模 next-k 任务依赖能显著提升多步任务成功率，避免关键信息丢失。

2. **压缩不仅是效率工具，更是正则化手段**  
   PAACE 在多个基准上**超越“无压缩”基线**，表明去除冗余和矛盾信息有助于提升推理一致性。

3. **上下文工程应作为一级模块**  
   类似 RAG 之于知识系统，**Context Engineering 应成为代理架构的核心组件**。

4. **合成数据 + 蒸馏可行且高效**  
   PAACE-Syn 提供高质量监督信号，使小型模型也能掌握复杂的压缩策略。

5. **指令与上下文需协同演化**  
   单纯删除无关内容不足以维持长期任务稳定性，必须同步优化指令表述。

---

### ⚠️ 局限性
1. **非形式化保证**  
   不提供语义等价的形式验证，依赖 LLM 判断和嵌入相似性作为代理指标。
2. **领域特异性**  
   压缩策略在特定环境中学得，跨领域泛化能力有待验证。
3. **安全风险**  
   若压缩策略未充分验证，可能遗漏安全约束或溯源信息（尽管 PAACE 通过 outcome-level 过滤缓解此问题）。
4. **依赖计划可用性**  
   虽然允许不完美计划，但若完全缺失计划描述，性能会下降。

---

### 🔮 未来工作方向
1. **引入符号检查机制**  
   结合形式化验证器或领域规则，增强安全性与可靠性。
2. **跨任务迁移学习**  
   探索通用的 plan-aware 压缩先验，提升泛化能力。
3. **动态调整 k 值**  
   根据任务类型自适应选择 next-k 的范围。
4. **集成到多代理系统**  
   将 PAACE 作为 meta-agent 的上下文控制器，协调多个子代理的通信与记忆。
5. **开源生态建设**  
   发布 PAACE-Syn 数据集与代码，推动上下文工程标准化。

---

## 总结
PAACE 提出了一种全新的视角——将 **context engineering** 视为 LLM 代理的核心能力，而不仅仅是辅助功能。它通过**计划感知、多步前瞻、合成训练与模型蒸馏**，实现了高保真、低成本的上下文管理，在多个长周期代理基准上取得了 SOTA 表现。该工作为构建**可扩展、可靠、高效的 agentic systems** 提供了坚实基础。

</details>

---

### 12. [Accelerating Multi-modal LLM Gaming Performance via Input Prediction and Mishit Correction](https://arxiv.org/abs/2512.17250)

**Authors**: Ziyang Lin, Zixuan Sun, Sanhorn Chen, Xiaoyang Chen, Roy Zhao  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.17250v1  

#### Abstract
Real-time sequential control agents are often bottlenecked by inference latency. Even modest per-step planning delays can destabilize control and degrade overall performance. We propose a speculation-and-correction framework that adapts the predict-then-verify philosophy of speculative execution to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerating Multi-modal LLM Gaming Performance via Input Prediction and Mishit Correction

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对 **Transformer-based 实时顺序控制代理（real-time sequential control agents）中的推理延迟瓶颈** 展开研究。在机器人控制、竞技游戏、高频交易等任务中，即使微小的每步规划延迟也可能导致系统不稳定或奖励下降。传统加速方法（如 speculative decoding、early exiting）主要优化输入后的推理过程，但在实际部署中仍无法满足严格的端到端延迟要求。

### 提出的新方法与思路
作者提出了一种 **“推测-修正”框架（speculation-and-correction framework）**，将 speculative decoding 中的 “预测-验证” 范式扩展到基于模型的控制任务中，具体包括以下三个核心机制：

- **Input Prediction（输入预测）**：利用预训练的 TD-MPC2 世界模型和潜在空间 MPC 规划器，在每个时间步生成短期动作队列（action queue）和潜变量 rollout 预测。
- **Speculative Execution（推测执行）**：直接执行多个预先计划的动作而不重新规划，从而减少频繁调用昂贵的 MPC 推理。
- **Mismatch-Aware Miss Recycling and Correction（错配感知的错误回收与修正）**：
  - 当观测状态与预测潜变量存在偏差时，引入一个轻量级的 **learned corrector** 对推测动作进行残差更新；
  - 若偏差过大，则安全回退至完整重规划，并清空旧队列。

此外，设计了两种 corrector 架构：
- **Gated two-tower MLP corrector**：适用于局部误差的快速响应；
- **Temporal Transformer corrector**：通过历史错配特征捕捉系统性漂移。

### 相比现有方法的优势
| 维度 | 本文方法 | 传统方法 |
|------|---------|----------|
| 加速阶段 | 输入侧预测（input-side prediction） | 输出侧推理加速（post-input inference） |
| 推理频率 | 显著降低（减少 43.6%） | 每步均需推理 |
| 错误处理 | 可修复部分推测失败（miss recycling） | 失败即丢弃并重启 |
| 控制稳定性 | 引入阈值控制与 fallback 机制保障鲁棒性 | 无容错机制 |

> ✅ **核心创新**：首次将 speculative execution 从语言生成领域迁移到连续控制任务，并结合 residual policy learning 思想实现 **可学习的推测修正机制**，实现了低延迟与高稳定性的平衡。

---

## 2. 核心实验方法和设置

### 数据集与环境
- **基准任务**：DeepMind Control Suite (DMC) 中的 `Humanoid-Walk` 连续控制任务。
- **物理引擎**：MuJoCo [4]。
- **数据来源**：基于预训练的 TD-MPC2 [2] 世界模型采集轨迹数据，包含真实状态、推测预测、执行动作等信息。

### 实验设置
- **总步数**：500 个环境步（environment steps）。
- **规划视界（planning horizon）**：H = 3。
- **执行视界（execute horizon）**：L ∈ {3, 6}。
- **错配阈值（mismatch threshold）**：用于判断是否触发 fallback。
- **corrector 训练方式**：采用离线蒸馏（distillation）策略，以 TD-MPC2 重规划动作为教师信号。

### 评估指标
| 指标 | 描述 |
|------|------|
| **Inference Calls** | 每轮环境中调用 MPC 规划器的次数 |
| **End-to-End Step Latency** | 每个环境步骤的平均延迟（ms） |
| **Cumulative Reward** | 最终累计回报得分 |
| **Speedup Ratio** | 端到端延迟提升比例 |

### 基线方法对比
- **Original TD-MPC2**：每步都进行完整 MPC 规划，作为性能上限。
- **Speculation-only（无修正）**：执行推测动作链但不修正。
- **Correction-only（无推测）**：仅使用 corrector 不启用多步执行。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | Inference Calls | Latency per Step | Cumulative Reward |
|------|------------------|-------------------|--------------------|
| Original TD-MPC2 | 500 | 36 ms | 935 |
| **Ours (Speculation + Correction)** | **282** (-43.6%) | **27 ms** (**↓25%**) | **869** (-7.1%) |

> 📈 在仅损失 **7.1% 奖励** 的前提下，实现了：
> - **43.6% 的推理调用减少**
> - **25% 的端到端延迟降低**

### 与基线方法的对比结果
- **相比原始 TD-MPC2**：
  - 减少 218 次推理调用（约一半），显著节省计算资源；
  - 尽管引入 corrector 带来额外 12ms/inference 开销，但由于调用频次大幅下降，整体延迟仍明显改善。

- **与其他变体比较**（见 Figure 9）：
  - **Speculation-only（执行3步）**：reward 降至 142（↓84.8%），表明无修正的长程推测极不可靠；
  - **Speculation-only（执行2步）**：reward 为 778（↓16.8%），说明短视界推测有一定可行性但仍有较大性能损失；
  - **Our full method**：比 speculation-only 提升 **91 分 reward**，证明 corrector 有效恢复控制质量。

### 消融实验结果
- **Corrector 必要性验证**：
  - 移除 corrector 后，即使只执行两步推测，性能也显著下降；
  - 表明 **mismatch-aware recycling 是实现稳健加速的关键**。
  
- **不同 corrector 架构表现**：
  - **Two-Tower MLP**：收敛更快，最终 loss 更低，适合低延迟场景；
  - **Temporal Transformer**：对长期漂移建模更强，更适合长跨度推测；
  - 两者互补，可根据硬件预算选择。

---

## 4. 关键结论和发现

### 主要发现
1. **输入预测可用于有效加速实时控制**：未来的观测输入具有可预测性，提前推测可转化为有用的预计算。
2. **推测执行必须配合修正机制才可靠**：单纯的 speculative execution 在长跨度下极易累积误差，导致崩溃；而轻量 corrector 能显著提升鲁棒性。
3. **效率与性能可取得良好权衡**：通过合理设计 speculation depth 与 mismatch threshold，可在保持较高控制质量的同时大幅提升吞吐率。

### 方法的局限性
- **当前评估范围有限**：仅在单一任务（Humanoid-Walk）上测试，未验证跨任务泛化能力。
- **未联合优化 speculation 与 policy**：使用固定预训练 TD-MPC2 作为推测模块，未进行端到端训练，可能限制最大推测深度。
- **corrector 引入 per-inference 开销**：在更严苛的实时预算或低端硬件上，这一开销可能削弱加速收益。

### 未来工作方向
1. **Joint Training / Online Adaptation**：联合训练 corrector 与 world model，使其适应闭环推测下的分布偏移。
2. **Adaptive Budgeting**：根据错配历史和不确定性动态调整 speculation depth 和 replanning 频率，显式优化 speed-reward trade-off。
3. **扩展至更多任务与模态**：在其他 DMC 任务、含噪声传感器环境及真实控制系统中验证通用性。
4. **系统级流水线优化**：进一步重叠 speculation、correction 与环境交互，降低尾延迟（tail latency）。

---

> 🔚 **总结一句话**：  
> 本文提出了一种基于 **输入预测 + 轻量修正** 的 speculative control 框架，在 DMC Humanoid-Walk 上实现了 **43.6% 推理调用减少、25% 延迟下降、仅 7.1% 奖励损失**，为 Transformer-based 实时控制提供了高效且鲁棒的新范式。

</details>

---

### 13. [Distributed Learning in Markovian Restless Bandits over Interference Graphs for Stable Spectrum Sharing](https://arxiv.org/abs/2512.17161)

**Authors**: Liad Lea Didi, Kobi Cohen  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.17161v1  

#### Abstract
We study distributed learning for spectrum access and sharing among multiple cognitive communication entities, such as cells, subnetworks, or cognitive radio users (collectively referred to as cells), in communication-constrained wireless networks modeled by interference graphs. Our goal is to achie...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Distributed Learning in Markovian Restless Bandits over Interference Graphs for Stable Spectrum Sharing

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文研究了在**通信受限的无线网络**中，多个认知通信实体（如 cells、subnetworks 或 cognitive radio users）之间的**分布式频谱接入与共享问题**。该问题具有以下挑战：
- 存在**空间干扰约束**，由 interference graph 建模；
- 频道状态随时间动态变化，遵循未知的**Markovian restless 过程**（即频道即使未被观测也会演化）；
- 各 cell 对同一频道的感知收益不同（**heterogeneous rewards**），导致系统高度非对称；
- 目标是实现全局**稳定且高效**的信道分配。

传统方法多假设频道为 i.i.d. 或 rested 模型，无法应对现实中的时变性和干扰图结构。

---

### 提出的新方法与思路
作者提出了名为 **SMILE**（Stable Multi-matching with Interference-aware LEarning）的**分布式学习算法**，其核心思想包括：

#### （1）问题建模创新
- 将频谱共享问题形式化为基于 interference graph 的 **generalized Gale-Shapley 多对一稳定匹配问题**。
- 在存在空间干扰和随机时变频道动态下定义“稳定性”，这是首次将 Gale-Shapley 稳定性扩展到 **restless Markovian 环境**。

#### （2）算法设计创新
- **SMILE 算法**融合了 **restless multi-armed bandit (RMAB)** 学习与图约束协调机制，无需中心控制器或频繁通信。
- 算法分为三个阶段：
  - **Exploration Phase**：通过 recovery epoch 和 estimation epoch 收集样本，克服 restless 状态演化带来的估计偏差；
  - **Allocation Phase**：基于当前速率估计执行分布式稳定匹配；
  - **Exploitation Phase**：按匹配结果进行传输。
- 引入自适应的 **exploration function** $ T_{l,s}(t) = \max\{E_{l,s}(t), 2/\lambda\} \log t $，平衡探索与利用，并保证对关键频道充分采样。

#### （3）理论保障
- 证明 SMILE 能够以**对数级 regret** 收敛至最优稳定分配，即：
  $$
  R(t) = O(\log t)
  $$
- 这是对数 regret 的最优阶，在此类问题中达到理论极限。
- 分析考虑了 transient effect、channel switching cost 和 suboptimal matching 的综合影响。

---

### 相比现有方法的优势

| 维度 | 现有方法局限 | SMILE 的优势 |
|------|--------------|-------------|
| **环境模型** | 多为 i.i.d. 或 rested MAB，不适用于真实无线信道 | 支持 **restless Markovian 动态**，更贴近实际 |
| **网络拓扑** | 多假设完全干扰（complete graph） | 支持任意 **interference graph**，支持信道复用 |
| **通信开销** | 如 auction-based 方法需大量通信 | **无全局通信需求**，仅依赖本地 carrier sensing 或邻居消息交换 |
| **稳定性保证** | 多数 RMAB 方法无稳定匹配概念 | 显式追求 **Gale-Shapley 稳定性**，兼顾效率与公平 |
| **理论性能** | 部分方法缺乏严格 regret bound | 提供 **tight logarithmic regret bound**，优于同类算法 |

---

## 2. 核心实验方法和设置

### 实验设置
所有仿真实验均基于 **Finite-State Markov Channel (FSMC)** 模型模拟无线信道的时间相关性。具体配置如下：

#### 场景一：小规模验证（L=3, S=5）
- Cells：3个，其中 cell 1 与 cell 2 相邻，cell 3 无邻居；
- Channels：5个正交信道；
- 信道模型：Rayleigh fading，量化为 6 个 FSMC 状态；
- 转移矩阵与期望速率矩阵由文中给出。

#### 场景二：大规模可扩展性测试
- 大规模网络：$ L=50, S=50 $；$ L=100, S=100 $；$ L=100, S=50 $
- 干扰图随机生成，允许信道复用；
- 用于验证 SMILE 在高维场景下的收敛性与鲁棒性。

#### 场景三：层次化频谱接入（Hierarchical Access）
- Primary 用户间歇占用信道，secondary 用户只能在空闲时接入；
- 采用 **Gilbert-Elliott 模型**描述 primary 用户活动；
- 用于比较 regret 性能。

#### 场景四：异构收益比较
- 每个 cell 在每个 channel 上有不同的期望速率；
- 加性噪声：$ r_{l,s}(t) = \mu_{l,s} + z_{l,s}(t), z \sim \mathcal{N}(0, 0.05^2) $

---

### 评估指标
- **Average Sum Rate**：所有 cell 的平均总速率，衡量长期性能；
- **Regret**：相对于已知期望效用的“神谕”（genie）策略的累计损失；
  $$
  R_o(t) = t \sum_{l=1}^L \mu_{l,P^*(l)} - \mathbb{E}\left[\sum_{n=1}^t \sum_{l=1}^L x_{l,\phi_l(n)}(n)\right]
  $$
- **Normalized Regret / log t**：用于判断是否达到对数增长。

---

### 基线方法对比
| 方法 | 类型 | 是否需要通信 | 特点 |
|------|------|---------------|------|
| **Random Allocation** | 随机策略 | 否 | 基线下限 |
| **Optimal Solver (Oracle)** | 中心化求解器 | 是 | 已知 $\mu_{l,s}$ 下的最优稳定匹配，作为性能上限 |
| **RCA [14]** | RMAB 学习 | 否 | 单用户 rested bandit 扩展 |
| **DSEE [15]** | RMAB 学习 | 否 | 探索-利用分离框架 |
| **DSSL [12]** | 分布式 RMAB | 是 | 针对 full interference 设计 |
| **dE3 [9]** | 分布式拍卖 | 是 | 需 bid 信息交换 |
| **GoT (Game of Thrones) [37]** | 分布式博弈 | 否 | 均匀探索所有信道 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）收敛性表现（Fig. 5–8）
- 在所有测试场景中，**SMILE 快速收敛至接近 Oracle 的性能**：
  - $ L=3, S=5 $：约 $ 5\times10^4 $ 时间步后接近最优；
  - $ L=100, S=100 $：仍能在 $ 10^7 $ 步内稳定逼近最优解；
- 表明 SMILE 具有良好的**可扩展性**和**鲁棒性**。

#### （2）Regret 性能对比（Fig. 9）
- 在 normalized regret ($ R(t)/\log t $) 曲线中：
  - **SMILE 的斜率显著低于 RCA、DSEE、DSSL**；
  - 表明其 regret 增长最慢，学习效率最高；
- 最终 SMILE 的 regret 比次优方法低一个数量级以上。

#### （3）异构环境下性能（Fig. 10）
- 与 dE3 和 GoT 对比：
  - **SMILE 显著优于两者**，尤其在早期探索阶段；
  - dE3 因通信开销大且参数敏感而表现不佳；
  - GoT 因均匀探索导致收敛慢；
- SMILE 凭借**自适应 exploration function** 更高效地聚焦于潜在优质信道。

---

### 消融实验（隐含分析）
虽然未明确列出消融实验表格，但从理论分析和算法设计可以看出：
- 若去除 **recovery epoch (RE)**，会导致状态估计偏差增大，增加 regret；
- 若使用固定 exploration budget 而非 adaptive $ E_{l,s}(t) $，会过度探索低价值信道；
- 若忽略 interference graph 结构，则无法实现信道复用，降低吞吐量。

这些设计选择共同支撑了最终的高性能。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SMILE 成功实现了在 general interference graph 下的分布式稳定频谱共享**，解决了以往仅限于 fully interfering network 的限制。
2. ✅ 在 **restless Markovian 信道动态**下，首次建立了全局 Gale-Shapley 稳定性的学习保证。
3. ✅ 理论上证明了 **$ O(\log t) $ regret bound**，达到信息论最优阶。
4. ✅ 实验表明 SMILE 在多种场景下均能快速收敛，且性能远超 state-of-the-art RMAB 和博弈论方法。
5. ✅ 算法可通过 **CSMA 或局部消息传递**实现，适合实际部署。

---

### 方法的局限性
- **初始化要求**：初始阶段需短暂同步完成初步估计；
- **状态空间限制**：依赖 FSMC 模型，若状态过多则计算复杂度上升；
- **静态干扰图假设**：interference graph 被视为静态，未考虑移动性引起的拓扑变化；
- **理论常数较大**：regret bound 中的常数项可能影响短时性能。

---

### 未来工作方向
1. 扩展至 **时变 interference graph**，支持移动节点；
2. 结合 **deep reinforcement learning** 处理连续状态空间；
3. 引入 **queueing dynamics**，支持带缓冲区的数据包调度；
4. 考虑 **partial observability** 或 **hidden Markov models**；
5. 在真实 testbed 上部署验证 SMILE 的实用性。

---

> **总结一句话**：  
> SMILE 是首个在 **general interference graph + restless Markovian channels** 下实现 **logarithmic regret 与 Gale-Shapley 稳定性**的分布式频谱共享算法，兼具理论严谨性与工程可行性。

</details>

---

### 14. [A Solver-in-the-Loop Framework for Improving LLMs on Answer Set Programming for Logic Puzzle Solving](https://arxiv.org/abs/2512.17093)

**Authors**: Timo Pierre Schrader, Lukas Lange, Tobias Kaminski, Simon Razniewski, Annemarie Friedrich  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.17093v1  

#### Abstract
The rise of large language models (LLMs) has sparked interest in coding assistants. While general-purpose programming languages are well supported, generating code for domain-specific languages remains a challenging problem for LLMs. In this paper, we focus on the LLM-based generation of code for An...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Solver-in-the-Loop Framework for Improving LLMs on Answer Set Programming for Logic Puzzle Solving

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

- **领域特定语言（DSL）代码生成的挑战**：尽管大型语言模型（LLMs）在通用编程语言（如Python、JavaScript）上表现出色，但在**Answer Set Programming (ASP)** 这类领域特定语言上的表现较差。ASP是一种用于解决组合搜索问题的强大声明式编程范式，广泛应用于调度、配置、机器人等领域。
- **缺乏高质量训练数据**：LLMs在预训练阶段接触的ASP代码极少，导致其难以准确生成语义正确的ASP程序，尤其是在处理复杂的逻辑谜题时。

### **提出了什么新方法或新思路**

作者提出了一种**Solver-in-the-Loop**框架，结合ASP求解器反馈来提升LLMs在ASP代码生成任务中的性能。该方法分为两个核心部分：

1. **Solver-Guided Instruction-Tuning（训练阶段）**：
   - 利用LLM为逻辑谜题生成多个候选的ASP编码片段（partial encodings）。
   - 使用**ASP solver**对这些片段进行自动评估，判断其是否：
     - 语法正确（无warnings/errors）
     - 可满足（not unsatisfiable）
     - 能够保留真实解（ground truth solution）
   - 将通过验证的“chosen”样本用于**Supervised Fine-Tuning (SFT)**，构建高质量的银标准（silver-standard）训练数据。

2. **Solver-Guided Test-Time Search（推理阶段）**：
   - 在推理时引入**best-of-N sampling**策略，生成多个候选编码。
   - 设计了一个**solver-grounded reward function** $ f_r(I, M) $ 来评分每个候选编码：
     $$
     f_r(I, M) = - \left( \mathbb{1}_{\text{err}} + \mathbb{1}_{\text{unsat}} + \frac{\mathbb{1}_{\text{NE}}}{M} \right)
     $$
     其中 $ M $ 是答案集数量，奖励倾向于更严格的、能缩小解空间的编码。
   - 引入两种恢复机制：
     - **Re-generation**：若所有候选均失败，则重新生成更多候选。
     - **Backtracking**：回退到之前的步骤并尝试其他分支。

### **相比现有方法的优势**

| 方面 | 本文方法 | 现有方法 |
|------|---------|--------|
| 数据生成 | 自动化、无需人工标注，利用solver反馈筛选高质量数据 | 依赖少量人工构造数据集（如LLASP），或仅靠prompt工程 |
| 推理鲁棒性 | 引入solver反馈作为reward，在推理时动态筛选最优路径 | 多数基于chain-of-thought或固定prompt pipeline，无法细粒度纠错 |
| 适用性 | 可用于open-weight模型（如Llama、Qwen）和closed模型（如GPT-4） | 多数研究集中于商业闭源模型 |
| 泛化能力 | 在未见过的复杂谜题（GridPuzzles）上表现良好，显示强迁移能力 | 多数方法针对特定数据集设计，泛化差 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 描述 |
|-------|------|
| **LogicPuzzles** (Mitra and Baral, 2015) | 包含124个3×4网格逻辑谜题（50训练，74测试）。每个谜题有3类实体（如人、时间、宠物），每类4个实例，需完成唯一匹配。 |
| **GridPuzzles** (Tyagi et al., 2024) | 更大规模和难度的数据集，包含多种尺寸（3×4, 3×5, 4×4, 4×5, 4×6）和难度等级（easy, medium, hard）。仅用于评估，无训练集。 |

> ⚠️ 注意：作者发现原始LogicPuzzles训练/测试集存在26个重复实例，因此在主实验中移除了这些重叠样本以保证公平性。

### **实验设置和评估指标**

#### **模型选择**
- **Open-weight models**:
  - Llama-3.3 70B / Llama-3.1 8B
  - Qwen3 32B / Qwen3 8B
- **Closed-weight baseline**:
  - GPT-4.1-mini (2025-04-14)
- **Reasoning Language Model (RLM) baselines**:
  - DeepSeek-R1 variants（不使用ASP，直接推理）

#### **训练方式**
- 使用LoRA进行参数高效微调（rank=128）
- 训练数据由Llama-3.3 70B生成（N=5采样），经solver过滤后得到约1.5k高质量样本
- 对小模型（8B）额外加入LLASP数据增强

#### **评估指标**
- **Accuracy**：预测的答案集是否与ground truth完全一致（exact match）
- 使用**Levenshtein heuristic**处理字符串格式差异（如下划线 vs 空格、缩写等）
- 所有结果平均5次运行

#### **Prompting Settings（两种对比场景）**
| 设置 | 描述 |
|------|------|
| **Two-Shot (2S)** | 简单提示，提供基本指令和两个示例，模拟现实模糊输入场景 |
| **PromptPipeline (PP)** | 复杂的6步pipeline（Ishay et al., 2023），专为LogicPuzzles设计，代表高度工程化的prompt方案 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自Table 1）**

| 模型 | 设置 | LogicPuzzles Acc (%) | GridPuzzles Acc (%) |
|------|------|------------------------|----------------------|
| Llama-3.3 70B (base) | 2S | 27.0 | 9.1 |
| Llama-3.3 70B (SFT) | 2S | 55.4 (+28.4) | 16.8 (+7.7) |
| Llama-3.3 70B (SFT + TT) | 2S | **66.8** | **23.7** |
| Llama-3.3 70B (SFT + TT) | PP | **78.4** | **55.5** |
| Qwen3 32B (SFT + TT) | 2S | 50.3 | 33.9 |
| Qwen3 8B (SFT + TT) | 2S | 43.2 | 21.2 |
| DeepSeek-R1 (RLM) | — | 59.5 | 21.9 |

> ✅ **结论**：SFT显著提升性能（+20~30pp），加上test-time search进一步提升至**66.8%**（LogicPuzzles），超越RLM基线。

### **与基线方法的对比结果**

- 在**两步提示（2S）设置下**：
  - 所有SFT训练后的模型均显著优于其base版本。
  - Llama-3.3 70B (SFT + TT) 达到66.8%，超过未使用ASP的DeepSeek-R1（59.5%）。
- 在**PromptPipeline设置下**：
  - Llama-3.3 70B达到78.4%，表明ASP微调也增强了模型遵循复杂指令的能力。
- **小模型也能受益**：
  - Llama-3.1 8B从0%提升至46.8%（+46.8pp），说明该方法可有效“教会”小型LLM掌握ASP编码。

### **消融实验结果（Table 2）**

| 配置 | LogicPuzzles Acc (%) | Δ vs SFT |
|------|------------------------|----------|
| Base (greedy) | 31.0 | — |
| +SFT (greedy) | 55.4 | +24.4 |
| +SFT + best-of-5 | 62.7 | +7.3 |
| +Regeneration | 64.6 | +9.2 |
| +Backtracking | 62.7 | +7.3 |
| **+Reg + Back (full TT)** | **66.8** | **+11.4** |
| N=10 | 65.1 | +9.7 |
| N=25 | **69.2** | **+13.8** |

> 🔍 **发现**：
> - Best-of-N采样带来显著增益（+7.3pp）
> - **Re-generation 和 Backtracking 结合效果最好**（+11.4pp）
> - 增加N可继续提点，但需权衡计算成本

此外，作者还将该推理方法应用于**GPT-4.1-mini**（未经ASP微调），结果显示其准确率从39.2%提升至55.4%（+16.2pp），证明该方法具有普适性。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **State-of-the-art open-weight LLMs 在原始状态下几乎无法正确生成ASP代码**（如Llama-3.1 8B初始性能为0%）。
2. ✅ **Solver-in-the-loop 能自动生成高质量训练数据**，无需人工标注，实现自我监督式instruction tuning。
3. ✅ **SFT 显著提升ASP编码能力**，尤其对大模型（70B）和高复杂度问题效果明显。
4. ✅ **Test-time solver guidance 极大提高鲁棒性**，通过reward机制筛选最优路径，并可通过backtracking恢复错误。
5. ✅ **方法具备良好泛化性**：在未参与训练的GridPuzzles上仍取得显著提升，且适用于不同模型家族（Llama/Qwen/GPT）。

### **方法的局限性**

- **依赖外部solver调用**：每次生成都需要调用clingo等ASP solver，增加延迟和系统复杂性。
- **计算开销较大**：best-of-N和backtracking会显著增加token生成量（见Figure 2），不适合低延迟场景。
- **对solver错误敏感**：若solver误判（如将正确编码标记为unsatisfiable），会影响训练数据质量。
- **未能掌握高级重构技巧**：例如将全局唯一性约束拆分为多个子约束，模型仍未学会此类优化。

### **未来工作方向**

- 探索**Reinforcement Learning (RL)** 框架，将solver feedback作为reward信号进行端到端训练。
- 扩展reward函数，引入**加权误差类型**（weighted error types），区分轻重错误。
- 改进backbone模型架构，使其原生支持“symbolic reasoning trace”输出。
- 应用于其他声明式编程语言（如PROLOG、MiniZinc）或规划语言（PDDL）。

---

> 📌 **一句话总结**：  
> 本文提出了一种**solver-in-the-loop**框架，利用ASP solver的语义反馈来指导LLM的训练与推理，实现了无需人工标注的高质量ASP代码生成，在多个模型和数据集上均取得显著性能提升，展示了神经符号系统（neuro-symbolic system）在复杂逻辑任务中的巨大潜力。

</details>

---

### 15. [Turn-PPO: Turn-Level Advantage Estimation with PPO for Improved Multi-Turn RL in Agentic LLMs](https://arxiv.org/abs/2512.17008)

**Authors**: Junbo Li, Peng Zhou, Rui Meng, Meet P. Vadera, Lihong Li, Yang Li  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.17008v1  

#### Abstract
Reinforcement learning (RL) has re-emerged as a natural approach for training interactive LLM agents in real-world environments. However, directly applying the widely used Group Relative Policy Optimization (GRPO) algorithm to multi-turn tasks exposes notable limitations, particularly in scenarios r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Turn-PPO: Turn-Level Advantage Estimation with PPO for Improved Multi-Turn RL in Agentic LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前在多轮交互任务（multi-turn tasks）中，主流的强化学习算法 **GRPO**（Group Relative Policy Optimization）存在显著缺陷：
- **优势估计不稳定**：GRPO 依赖于对多个轨迹进行采样并基于组内归一化奖励来估计优势（advantage），导致高方差，尤其在环境不可控或多轮交互中更为严重。
- **优势分配不准确**：将相同的轨迹级优势值应用于所有 token，忽略了不同“轮次”（turn）对最终奖励的贡献差异，造成信用分配（credit assignment）失真。
- **训练崩溃风险高**：在长推理链或多轮决策任务中，GRPO 容易出现训练不稳定甚至完全崩溃。

### **提出的新方法与创新思路**
作者提出了 **turn-PPO**，一种基于 **Proximal Policy Optimization (PPO)** 的改进算法，其核心创新在于：
- **引入 turn-level MDP 建模**：不同于传统将每个 token 视为一个状态-动作对的 **token-MDP**，turn-PPO 将每一轮完整的输入-输出（即一次用户-模型交互）视为一个状态-动作对，构建 **turn-MDP**。
- **turn-level 优势估计**：利用可学习的 critic 模型，在 turn 粒度上计算优势值，实现更精确的信用分配。
- **统一框架下的算法比较**：在统一框架下分析了 GRPO、token-PPO 和 turn-PPO 的差异，揭示了 token-level 建模在多轮任务中的结构性缺陷。

### **相比现有方法的优势**
- **更高的训练稳定性**：PPO 的 critic 模型通过 GAE（Generalized Advantage Estimation）提供更稳定的优势估计，避免了 GRPO 的采样方差问题。
- **更合理的信用分配**：turn-level 建模使 critic 能更好捕捉不同轮次的重要性，提升策略优化效率。
- **更强的泛化能力**：无需手动设计 turn-level 权重或过滤机制（如 StarPO-s、GiGPO），具有更好的通用性。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **WebShop**：一个模拟在线购物的文本环境，要求模型通过搜索、点击商品、选择属性、购买等多步操作完成任务，测试模型的长期规划与工具调用能力。
- **Sokoban**：经典的推箱子游戏，需要空间推理与长程规划，仅提供稀疏终端奖励，挑战模型的探索与决策一致性。

### **实验设置与评估指标**
- **模型基础**：使用 Qwen2.5-3B、Qwen2.5-7B 和 Qwen3-1.7B 作为 base model。
- **评估指标**：
  - **平均奖励（Average Reward）**：主要性能指标。
  - **训练稳定性**：是否发生训练崩溃（crash）。
  - **消融实验**：分析学习率、batch 多样性、GAE 参数（γ, λ）等影响。
- **训练细节**：
  - 使用 RAGEN 框架实现。
  - 对 Qwen3 测试了开启/关闭 reasoning 模式的性能差异。

### **基线方法对比**
| 方法 | 类型 | MDP 粒度 | 优势估计方式 |
|------|------|----------|----------------|
| **GRPO** | On-policy | token-level | 组内归一化奖励（sample-based） |
| **token-PPO** | Actor-Critic | token-level | Critic + GAE（token-level） |
| **turn-PPO**（本文） | Actor-Critic | **turn-level** | Critic + GAE（**turn-level**） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 2）**

| Environment | Model | GRPO | token-PPO | **turn-PPO** |
|------------|-------|------|-----------|-------------|
| WebShop | Qwen2.5-3B | 0.72 | 0.73 | **0.75** |
| WebShop | Qwen3-1.7B (no think) | 0.78 | 0.77 | **0.80** |
| WebShop | Qwen3-1.7B (think) | **Crash** | 0.54 | **0.55** |
| Sokoban | Qwen2.5-3B | **Crash** | 1.93 | **2.29** |
| Sokoban | Qwen2.5-7B | **Crash** | 2.90 | **3.74** |

> 注：“Crash” 表示训练过程中出现崩溃，无法收敛。

### **与基线方法的对比结果**
- **turn-PPO 在所有稳定运行的任务中均优于或持平于其他方法**。
- **GRPO 在复杂任务（尤其是 Sokoban 和 Qwen3 长推理）中频繁崩溃**，而 PPO 类方法能有效避免。
- **token-PPO 虽比 GRPO 稳定，但仍弱于 turn-PPO**，说明 turn-level 建模本身带来增益。
- 在 Qwen3 开启 reasoning 时，GRPO 完全失败，而 turn-PPO 仍能维持训练并取得合理性能。

### **消融实验结果**
- **学习率敏感性**：critic 学习率需为 actor 的 5–10 倍（实验设为 1e-5 vs 1e-6），否则训练易停滞或发散。
- **batch 多样性（G）**：
  - GRPO 受益于每问题多采样（high G），以降低优势估计方差。
  - PPO 更偏好高问题多样性（low G），有助于 critic 泛化。
- **minibatch size 与 epoch**：
  - 减小 minibatch size 并增加更新次数（epochs）比单次大 batch 更新更高效。
  - 但 epochs 不宜过多，以防过拟合。
- **GAE 参数（γ, λ）**：
  - **turn-PPO 支持灵活调整 γ 和 λ**（实验最优：γ=0.99, λ=0.9）。
  - token-PPO 中这些参数必须设为 1.0，否则早期 token 优势被过度衰减，导致训练失败。

---

## **4. 关键结论和发现**

### **主要发现**
1. **GRPO 在多轮任务中存在根本性缺陷**：其基于采样的优势估计在动态环境中方差大，且 uniform advantage 分配不合理，导致训练不稳定。
2. **PPO 是更优选择**：得益于可学习的 critic 和 GAE，PPO 提供更稳定、低方差的优势估计，显著提升训练鲁棒性。
3. **turn-level MDP 建模至关重要**：将每轮交互视为一个状态-动作对，能更好地匹配多轮任务的结构，提升 critic 学习效率和优势估计准确性。
4. **turn-PPO 是当前多轮 RL 训练的最佳实践之一**：在 WebShop 和 Sokoban 上均表现出色，尤其在长推理和大模型场景下优势明显。

### **方法的局限性**
- 当前研究局限于 **文本模拟环境**（text-only environments），未涉及真实网页或物理交互。
- 实验仅覆盖两个代表性任务，需在更多复杂场景（如多工具协作、实时反馈）中验证泛化性。
- turn-PPO 依赖 critic 训练，增加了计算开销（尽管仍优于多次采样）。

### **未来工作方向**
- 扩展到 **真实世界 Web Agents** 和 **具身智能体**（embodied agents）的复杂交互场景。
- 探索 **更高效的 turn-level critic 架构** 或轻量化版本。
- 结合 **分层强化学习**（Hierarchical RL）进一步建模长期目标与子任务分解。
- 研究如何自动识别 turn 边界，适应非结构化对话流。

---

> **总结**：  
> 该论文系统地揭示了 GRPO 在多轮 LLM 代理训练中的局限性，并重新确立了 PPO 的有效性。提出的 **turn-PPO** 通过 **turn-level MDP 建模** 实现了更稳定、高效的优势估计，在多个基准上显著优于现有方法，为多轮交互式 LLM 的 RL 训练提供了新的标准范式。

</details>

---

### 16. [Probing Scientific General Intelligence of LLMs with Scientist-Aligned Workflows](https://arxiv.org/abs/2512.16969)

**Authors**: Wanghan Xu, Yuhao Zhou, Yifan Zhou, Qinglong Cao, Shuo Li, Jia Bu, Bo Liu, Yixin Chen, Xuming He, Xiangyu Zhao, Xiang Zhuang, Fengxiang Wang, Zhiwang Zhou, Qiantai Feng, Wenxuan Huang, Jiaqi Wei, Hao Wu, Yuejin Yang, Guangshuai Wang, Sheng Xu, Ziyan Huang, Xinyao Liu, Jiyao Liu, Cheng Tang, Wei Li, Ying Chen, Junzhi Ning, Pengfei Jiang, Chenglong Ma, Ye Du, Changkai Ji, Huihui Xu, Ming Hu, Jiangbin Zheng, Xin Chen, Yucheng Wu, Feifei Jiang, Xi Chen, Xiangru Tang, Yuchen Fu, Yingzhou Lu, Yuanyuan Zhang, Lihao Sun, Chengbo Li, Jinzhe Ma, Wanhao Liu, Yating Liu, Kuo-Cheng Wu, Shengdu Chai, Yizhou Wang, Ouwen Zhangjin, Chen Tang, Shufei Zhang, Wenbo Cao, Junjie Ren, Taoyong Cui, Zhouheng Yao, Juntao Deng, Yijie Sun, Feng Liu, Wangxu Wei, Jingyi Xu, Zhangrui Li, Junchao Gong, Zijie Guo, Zhiyu Yao, Zaoyu Chen, Tianhao Peng, Fangchen Yu, Bo Zhang, Dongzhan Zhou, Shixiang Tang, Jiaheng Liu, Fenghua Ling, Yan Lu, Yuchen Ren, Ben Fei, Zhen Zhao, Xinyu Gu, Rui Su, Xiao-Ming Wu, Weikang Si, Yang Liu, Hao Chen, Xiangchao Yan, Xue Yang, Junchi Yan, Jiamin Wu, Qihao Zheng, Chenhui Li, Zhiqiang Gao, Hao Kong, Junjun He, Mao Su, Tianfan Fu, Peng Ye, Chunfeng Song, Nanqing Dong, Yuqiang Li, Huazhu Fu, Siqi Sun, Lijing Cheng, Jintai Lin, Wanli Ouyang, Bowen Zhou, Wenlong Zhang, Lei Bai  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.16969v1  

#### Abstract
Despite advances in scientific AI, a coherent framework for Scientific General Intelligence (SGI)-the ability to autonomously conceive, investigate, and reason across scientific domains-remains lacking. We present an operational SGI definition grounded in the Practical Inquiry Model (PIM: Deliberati...

---

### 17. [Linear Personality Probing and Steering in LLMs: A Big Five Study](https://arxiv.org/abs/2512.17639)

**Authors**: Michel Frising, Daniel Balcells  
**Category**: cs.CL  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17639v1  

#### Abstract
Large language models (LLMs) exhibit distinct and consistent personalities that greatly impact trust and engagement. While this means that personality frameworks would be highly valuable tools to characterize and control LLMs' behavior, current approaches remain either costly (post-training) or brit...

---

### 18. [Digitizing Nepal's Written Heritage: A Comprehensive HTR Pipeline for Old Nepali Manuscripts](https://arxiv.org/abs/2512.17111)

**Authors**: Anjali Sarawgi, Esteban Garces Arias, Christof Zotter  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17111v1  

#### Abstract
This paper presents the first end-to-end pipeline for Handwritten Text Recognition (HTR) for Old Nepali, a historically significant but low-resource language. We adopt a line-level transcription approach and systematically explore encoder-decoder architectures and data-centric techniques to improve ...

---

### 19. [SHARP-QoS: Sparsely-gated Hierarchical Adaptive Routing for joint Prediction of QoS](https://arxiv.org/abs/2512.17262)

**Authors**: Suraj Kumar, Arvind Kumar, Soumi Chattopadhyay  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17262v1  

#### Abstract
Dependable service-oriented computing relies on multiple Quality of Service (QoS) parameters that are essential to assess service optimality. However, real-world QoS data are extremely sparse, noisy, and shaped by hierarchical dependencies arising from QoS interactions, and geographical and network-...

---

### 20. [Alzheimer's Disease Brain Network Mining](https://arxiv.org/abs/2512.17276)

**Authors**: Alireza Moayedikia, Sara Fin  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17276v1  

#### Abstract
Machine learning approaches for Alzheimer's disease (AD) diagnosis face a fundamental challenges. Clinical assessments are expensive and invasive, leaving ground truth labels available for only a fraction of neuroimaging datasets. We introduce Multi view Adaptive Transport Clustering for Heterogeneo...

---

### 21. [Estimating Spatially Resolved Radiation Fields Using Neural Networks](https://arxiv.org/abs/2512.17654)

**Authors**: Felix Lehner, Pasquale Lombardo, Susana Castillo, Oliver Hupe, Marcus Magnor  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17654v1  

#### Abstract
We present an in-depth analysis on how to build and train neural networks to estimate the spatial distribution of scattered radiation fields for radiation protection dosimetry in medical radiation fields, such as those found in Interventional Radiology and Cardiology. Therefore, we present three dif...

---

### 22. [Spatially-informed transformers: Injecting geostatistical covariance biases into self-attention for spatio-temporal forecasting](https://arxiv.org/abs/2512.17696)

**Authors**: Yuri Calleo  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.17696v1  

#### Abstract
The modeling of high-dimensional spatio-temporal processes presents a fundamental dichotomy between the probabilistic rigor of classical geostatistics and the flexible, high-capacity representations of deep learning. While Gaussian processes offer theoretical consistency and exact uncertainty quanti...

---

### 23. [UniRel-R1: RL-tuned LLM Reasoning for Knowledge Graph Relational Question Answering](https://arxiv.org/abs/2512.17043)

**Authors**: Yinxu Tang, Chengsong Huang, Jiaxin Huang, William Yeoh  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17043v1  

#### Abstract
Knowledge Graph Question Answering (KGQA) has traditionally focused on entity-centric queries that return a single answer entity. However, real-world queries are often relational, seeking to understand how entities are associated. In this work, we introduce relation-centric KGQA, a complementary set...

---

### 24. [Reinforcement Learning for Self-Improving Agent with Skill Library](https://arxiv.org/abs/2512.17102)

**Authors**: Jiongxiao Wang, Qiaojing Yan, Yawei Wang, Yijun Tian, Soumya Smruti Mishra, Zhichao Xu, Megha Gandhi, Panpan Xu, Lin Lee Cheong  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17102v1  

#### Abstract
Large Language Model (LLM)-based agents have demonstrated remarkable capabilities in complex reasoning and multi-turn interactions but struggle to continuously improve and adapt when deployed in new environments. One promising approach is implementing skill libraries that allow agents to learn, vali...

---

### 25. [Large Language Models as Pok\'emon Battle Agents: Strategic Play and Content Generation](https://arxiv.org/abs/2512.17308)

**Authors**: Daksh Jain, Aarya Jain, Ashutosh Desai, Avyakt Verma, Ishan Bhanuka, Pratik Narang, Dhruv Kumar  
**Category**: cs.AI  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17308v1  

#### Abstract
Strategic decision-making in Pok\'emon battles presents a unique testbed for evaluating large language models. Pok\'emon battles demand reasoning about type matchups, statistical trade-offs, and risk assessment, skills that mirror human strategic thinking. This work examines whether Large Language M...

---

### 26. [Seed-Prover 1.5: Mastering Undergraduate-Level Theorem Proving via Learning from Experience](https://arxiv.org/abs/2512.17260)

**Authors**: Jiangjie Chen, Wenxiang Chen, Jiacheng Du, Jinyi Hu, Zhicheng Jiang, Allan Jie, Xiaoran Jin, Xing Jin, Chenggang Li, Wenlei Shi, Zhihong Wang, Mingxuan Wang, Chenrui Wei, Shufa Wei, Huajian Xin, Fan Yang, Weihao Gao, Zheng Yuan, Tianyang Zhan, Zeyu Zheng, Tianxi Zhou, Thomas Hanwen Zhu  
**Category**: cs.CL  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17260v1  

#### Abstract
Large language models have recently made significant progress to generate rigorous mathematical proofs. In contrast, utilizing LLMs for theorem proving in formal languages (such as Lean) remains challenging and computationally expensive, particularly when addressing problems at the undergraduate lev...

---

### 27. [Governance-Aware Hybrid Fine-Tuning for Multilingual Large Language Models](https://arxiv.org/abs/2512.17344)

**Authors**: Haomin Qi, Chengbo Huang, Zihan Dai, Yunkai Gao  
**Category**: cs.CL  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17344v1  

#### Abstract
We present a governance-aware hybrid fine-tuning framework for multilingual, low-resource adaptation of large language models. The core algorithm combines gradient-aligned low-rank updates with structured orthogonal transformations through layer-wise mixing and introduces unitary constraints in sele...

---

### 28. [UCoder: Unsupervised Code Generation by Internal Probing of Large Language Models](https://arxiv.org/abs/2512.17385)

**Authors**: Jiajun Wu, Jian Yang, Wei Zhang, Lin Jing, Yuqing Ma, Ensheng Shi, Yuchi Ma, Zhoujun Li, Xianglong Liu  
**Category**: cs.CL  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17385v1  

#### Abstract
Large language models (LLMs) have demonstrated remarkable capabilities in code generation tasks. However, their effectiveness heavily relies on supervised training with extensive labeled (e.g., question-answering pairs) or unlabeled datasets (e.g., code snippets), which are often expensive and diffi...

---

### 29. [Scalable Distributed Vector Search via Accuracy Preserving Index Construction](https://arxiv.org/abs/2512.17264)

**Authors**: Yuming Xu, Qianxi Zhang, Qi Chen, Baotong Lu, Menghao Li, Philip Adams, Mingqin Li, Zengzhong Li, Jing Liu, Cheng Li, Fan Yang  
**Category**: cs.DC  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.17264v1  

#### Abstract
Scaling Approximate Nearest Neighbor Search (ANNS) to billions of vectors requires distributed indexes that balance accuracy, latency, and throughput. Yet existing index designs struggle with this tradeoff. This paper presents SPIRE, a scalable vector index based on two design decisions. First, it i...

---

### 30. [QSMOTE-PGM/kPGM: QSMOTE Based PGM and kPGM for Imbalanced Dataset Classification](https://arxiv.org/abs/2512.16960)

**Authors**: Bikash K. Behera, Giuseppe Sergioli, Robert Giuntini  
**Category**: cs.LG  
**Published**: 2025-12-22  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.16960v1  

#### Abstract
Quantum-inspired machine learning (QiML) leverages mathematical frameworks from quantum theory to enhance classical algorithms, with particular emphasis on inner product structures in high-dimensional feature spaces. Among the prominent approaches, the Kernel Trick, widely used in support vector mac...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
