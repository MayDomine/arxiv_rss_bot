# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-13 06:44:36 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Pretraining A Large Language Model using Distributed GPUs: A Memory-Efficient Decentralized Paradigm](https://arxiv.org/abs/2602.11543)

**Authors**: Jinrui Zhang, Chaodong Xiao, Aoqi Wu, Xindong Zhang, Lei Zhang  
**Category**: cs.CL  
**Published**: 2026-02-13  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2602.11543v1  

#### Abstract
Pretraining large language models (LLMs) typically requires centralized clusters with thousands of high-memory GPUs (e.g., H100/A100). Recent decentralized training methods reduce communication overhead by employing federated optimization; however, they still need to train the entire model on each n...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大规模语言模型（LLM）的预训练高度依赖集中式高性能计算集群，需要数千张高显存GPU（如H100/A100）以及低延迟、高带宽互联（如RDMA）。这导致训练成本极高，限制了大多数研究者参与LLM研发的能力。尽管已有去中心化训练方法（如DiLiCo、Photon），但它们仍需在每个节点上存储和更新**完整模型参数**，因此无法突破单个GPU的显存瓶颈。

### 提出的新方法：SPES（Sparse Expert Synchronization）
本文提出了一种**内存高效、去中心化的MoE LLM预训练框架——SPES**，其核心思想是：
- **专家分片训练**：将Mixture-of-Experts（MoE）模型中的不同expert分配给不同的分布式节点，每个节点仅负责训练自己被分配的一组expert，其余expert保持冻结。
- **稀疏同步机制**：各节点只在本地更新后，向参数服务器上传**已更新的shared模块和assigned experts的权重**，避免全量参数传输。
- **专家合并热启动策略（Expert-Merging Warm-Up）**：在训练初期，周期性地将相似的expert进行加权平均融合，以提升token利用率并加速知识共享，帮助模型快速建立基础能力。

### 相比现有方法的优势
| 维度 | 传统集中式/DiLiCo/Photon | SPES |
|------|--------------------------|------|
| 显存占用 | 高（需存储全部optimizer states、gradients） | **显著降低**（仅维护assigned expert的优化器状态） |
| 通信开销 | 高（每轮需同步全部模型参数） | **大幅减少**（仅同步更新部分） |
| 可扩展性 | 依赖高速互联，难以跨广域网部署 | 支持弱连接、异构设备（如互联网上的独立GPU） |
| 硬件门槛 | 必须使用高显存GPU（如80GB H100） | 可运行于常见48GB GPU（如L40S） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **从零训练（Training from Scratch）**
  - `Ultra-FineWeb`：高质量英文网页语料
  - `SlimPajama`：去重后的多源文本集合
  - `OLMo-Mix-1124` 子集：包含 `arXiv`, `OpenWebMath`, `StarCoder`, `pes2o` 等领域专用数据
- **迁移训练（Upcycling）**
  - `Nemotron Pretraining Dataset`：专为数学、代码和推理任务设计的大规模语料

### 实验设置
| 模型规模 | 节点数 | 单节点GPU配置 | 总训练Token量 |
|---------|--------|---------------|----------------|
| 2B MoE | 16     | 1×NVIDIA L40S (48GB) | 500B |
| 7B MoE | 4      | 8×A800 (通过NVLink互联) | 500B |
| 9B MoE* | -      | 初始化自 Qwen3-1.7B-Base | 400B |

> 注：9B模型采用“upcycling”方式，即从dense checkpoint复制FFN层并注入噪声生成初始MoE结构。

### 评估指标
- **通用能力基准**：
  - `MMLU`（5-shot）、`C-Eval`（0-shot）
- **常识推理基准**：
  - `ARC(e/c)`、`PIQA`、`SciQ`、`OBQA`、`BoolQ`、`SIQA`、`WinoGrande`
- **其他补充指标**：
  - 每GPU显存占用（Training GPU Memory）
  - 每轮上行通信量（Uplink Volume per Round）
  - 吞吐量（tokens/s/GPU）

### 基线方法对比
- **Centralized Training**：标准集中式FSDP/Data Parallel训练
- **DiLiCo**：代表性的去中心化预训练方法（基于FedAvg）
- 其他开源LLM作为外部参考（如TinyLlama、Pythia、Qwen系列等）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 3）
| 方法 | 参数量 | 训练Token | ARC(e) | ARC(c) | PIQA | SciQ | BoolQ | Avg |
|------|--------|------------|--------|--------|------|--------|--------|-----|
| SPES-2B | 0.8B/2.1B | 500B | 63.8 | 35.3 | 69.3 | 85.0 | 61.4 | — |
| SPES-7B | 1.6B/7.3B | 500B | 72.1 | 43.8 | 74.7 | 89.9 | 62.7 | — |
| SPES-9B* | 3.1B/9.4B | 400B | **81.5** | **57.3** | **78.9** | **95.3** | **77.3** | — |

> ✅ **SPES-9B在多个任务上达到SOTA级别表现，甚至优于许多更大规模或更长训练时间的模型**

### 与基线方法对比（Table 1 & Figure 3）
| 指标 | Centralized | DiLiCo | SPES |
|------|-------------|--------|------|
| 2B模型单卡显存占用 | >55GB ❌ | ~55GB ❌ | **~35GB** ✅ |
| 7B模型每轮上行通信量 | 28.6 GB | 28.6 GB | **9.8 GB** ↓65% |
| 2B模型通信成本 | — | — | ↓33.3% |
| 训练吞吐（tokens/s/GPU） | 3.79k | — | 3.67k （接近） |

> 🔍 在仅有16台48GB L40S GPU且通过普通以太网连接的情况下，成功完成2B MoE模型训练，而同类方法因显存不足无法运行。

### 消融实验结果（Ablation Studies）

#### （1）专家合并热启动效果（Table 4）
| 方法 | ARC(e) | SciQ | BoolQ | Avg Score |
|------|--------|--------|--------|-----------|
| w/o merging | 52.8 | 75.9 | 58.0 | 50.5 |
| w/ merging | 52.1 | 77.8 | 60.4 | **51.3** ↑ |

> 💡 专家合并策略有效提升了token利用率，在`BoolQ`和`SciQ`上有明显增益。

#### （2）同步频率影响（Figure A2）
- 当本地训练步数 $ H $ 从50增加到400时，性能下降。
- 表明**频繁同步有助于维持全局一致性**，尤其在带宽受限环境下更为重要。

#### （3）节点数量扩展性（Table A5）
| 节点数 | Avg Score |
|-------|-----------|
| 2     | 50.6      |
| 4     | 49.7      |
| 8     | 49.5      |

> 📈 尽管随着节点增多略有下降，但整体性能稳定，验证了SPES良好的可扩展性和鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **SPES实现了真正意义上的低资源LLM预训练**：首次证明可在仅16块48GB消费级GPU上、通过互联网连接完成2B MoE模型的有效训练。
2. **显存与通信双重优化**：相比DiLiCo等去中心化方法，SPES将单卡显存需求降低近40%，通信开销减少33%-65%。
3. **性能不妥协**：在相同计算预算下，SPES训练出的模型性能媲美甚至超越集中式训练结果。
4. **支持灵活扩展与迁移学习**：不仅能从头训练7B模型，还能成功“升级”一个dense checkpoint至9B MoE模型，展现出强大的实用性。

### 方法的局限性
- 当前最大验证规模为9B参数，尚未测试超大规模（如70B+）下的收敛稳定性。
- 所有实验均基于语言理解任务，未探索multimodal或generative任务中的适用性。
- 专家分配策略目前为静态划分，未考虑动态负载均衡或routing优化。

### 未来工作方向
- 验证SPES在更大模型（>10B）和更长训练周期下的可扩展性。
- 探索其在**多模态建模**、**强化学习对齐**等场景的应用潜力。
- 结合更先进的optimizer（如Des-LoC）或架构设计进一步提升效率。
- 构建开放社区驱动的去中心化训练生态，推动全民参与LLM研发。

> 🚀 **总体而言，SPES为打破LLM训练壁垒提供了切实可行的技术路径，有望促进更加包容和平等的人工智能研究格局。**

</details>

---

### 2. [Extending Puzzle for Mixture-of-Experts Reasoning Models with Application to GPT-OSS Acceleration](https://arxiv.org/abs/2602.11937)

**Authors**: Akhiad Bercovich, Nir Ailon, Vladimir Anisimov, Tomer Asida, Nave Assaf, Mohammad Dabbah, Ido Galil, Amnon Geifman, Yonatan Geifman, Izhak Golan, Roi Koren, Itay Levy, Zach Moshe, Pavlo Molchanov, Najeeb Nabwani, Mostofa Patwari, Omri Puny, Tomer Ronen, Itamar Schen, Elad Segal, Ido Shahaf, Oren Tropp, Ran Zilberstein, Ran El-Yaniv  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.11937v1  

#### Abstract
Reasoning-focused LLMs improve answer quality by generating longer reasoning traces, but the additional tokens dramatically increase serving cost, motivating inference optimization. We extend and apply Puzzle, a post-training neural architecture search (NAS) framework, to gpt-oss-120B to produce gpt...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Extending Puzzle for Mixture-of-Experts Reasoning Models with Application to GPT-OSS Acceleration

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前面向 **reasoning** 的大型语言模型（LLMs）通过生成更长的推理链（reasoning traces）来提升答案质量，但这显著增加了服务成本（serving cost）。主要原因在于：
- 自回归推理中，**self-attention** 的计算复杂度随序列长度增长而上升；
- **KV-cache** 的内存占用和带宽需求随 token 数线性增加，在长上下文（如 128K）场景下成为主要瓶颈；
- 推理过程中的 token 数量变化使得传统的 per-token 吞吐量（tok/s）和延迟（ms/token）无法准确反映端到端效率。

因此，如何在不牺牲推理能力的前提下，优化 **MoE 架构** 和 **长上下文注意力机制** 的推理效率，成为一个关键挑战。

---

### 提出了什么新方法或新思路
本文提出将 **Puzzle** 这一后训练神经架构搜索（post-training NAS）框架扩展至支持 **MoE 层** 和 **长上下文推理** 的场景，并应用于 `gpt-oss-120B` 模型，构建出轻量化、高性能的衍生模型 `gpt-oss-puzzle-88B`。

#### 主要技术组合包括：
- **异构 MoE 专家剪枝（Heterogeneous MoE Expert Pruning）**  
  在不同层采用不同的专家数量进行剪枝，基于激活相似性（activation-based replace-1-block scoring）评估每层重要性，实现细粒度压缩。
  
- **选择性窗口注意力（Selective Window Attention）**  
  将部分全局注意力层替换为 **window attention**（窗口大小 8192），以大幅减少 KV-cache 占用。关键创新是使用 **AA-LCR**（Artificial Analysis Long-Context Reasoning）任务上的性能下降作为评分信号，而非传统激活差异，从而更好保留长程依赖能力。

- **FP8 KV-Cache 量化 + 校准尺度（Calibrated Scales）**  
  引入 FP8 量化并使用最大值校准（max calibration）确定缩放因子，且将缩放因子向上取整为 2 的幂次，兼顾精度与硬件友好性。

- **强化学习微调 + Checkpoint 平均**  
  训练两个 RL 变体：一个专注高推理努力（high-effort）以提升准确性，另一个采用混合数据以控制生成长度。最终通过 **checkpoint averaging** 融合两者优势，在保持高准确率的同时抑制过度生成。

- **请求级效率评估指标（Request-Level Efficiency）**  
  提出新的评估范式：`相对请求速率（relative request rate） = 最大吞吐量 / 平均生成 token 数`，归一化后形成 **accuracy-speed frontier**，更真实反映实际部署表现。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **架构灵活性** | 支持异构 MoE 剪枝和混合注意力结构，优于统一剪枝或全量替换策略 |
| **长上下文适配性** | 使用任务感知的 AA-LCR 评分指导 window attention 替换，优于仅依赖局部激活匹配的方法 |
| **KV-cache 优化** | FP8 + calibrated scales 实现 ~2× KV 容量提升，优于无 scale 或静态 scale 方案 |
| **效率-质量平衡** | 请求级指标揭示真实收益，避免“吞吐翻倍但 trace 加倍”的虚假优化 |
| **端到端性能** | 在多个维度全面超越基线，尤其在单卡场景下表现突出 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练与蒸馏数据**：基于 `nvidia/Llama-Nemotron-Post-Training-Dataset` 构建的 **LNPT-gpt-oss** 数据集，涵盖数学、代码、通用推理和指令遵循任务。
- **响应生成方式**：使用父模型 `gpt-oss-120B` 对提示词分别在 **high** 和 **medium reasoning effort** 下生成响应，用于后续 KD 和 RL。
- **评分专用数据**：
  - MoE 专家贡献评分：12K 条 8K 长度打包序列；
  - Replace-1-block 评分：128 条 32K 长度序列；
  - AA-LCR 用于注意力层可替换性评估。

---

### 实验设置和评估指标

#### 硬件平台
- 主要测试环境：**8×H100 80GB HBM3 节点**
- 补充测试：**单张 H100 GPU**

#### 推理场景
| 场景 | 输入/输出长度 | 特点 |
|------|----------------|------|
| 短上下文 | 4K / 4K | MoE 计算主导 |
| 长上下文 | 64K / 64K 或 128K | KV-cache 内存与带宽主导 |

#### 评估指标
| 指标 | 描述 |
|------|------|
| **Per-token throughput (tok/s)** | 每秒处理 token 数，衡量架构效率 |
| **Decode latency (ITL)** | 单个 token 解码延迟 |
| **Suite-average accuracy (%)** | 多个基准任务平均得分 |
| **Relative request rate** | 请求级效率指标：`max throughput / avg gen tokens`，归一化于基线 |
| **Effort Length Ratio** | 高/低推理努力下的生成长度比，衡量可控性 |

---

### 基线方法对比
- **Parent Model**：`gpt-oss-120B`（BF16 / FP8 KV）
- **Compressed Derivative**：`HyperNova-60B`（第三方压缩版本）
- **本工作模型**：`gpt-oss-puzzle-88B`（KV BF16 / FP8）

所有模型均在相同条件下测试（vLLM v0.11.2），考虑最优 TP 和 batch size 配置。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（8×H100 节点）

| 场景 | 模型 | 吞吐量 (tok/s) | Speedup |
|------|------|----------------|---------|
| 4K/4K | gpt-oss-120B | 29.6K | 1.00× |
|        | gpt-oss-puzzle-88B | **36.1K** | **1.22×** |
| 64K/64K | gpt-oss-120B | 5.7K | 1.00× |
|         | gpt-oss-puzzle-88B | **9.3K** | **1.63×** |

> ✅ 达成预设目标：短上下文 1.2×，长上下文 1.6× 吞吐提升

---

### 单卡性能（H100 GPU）

| 场景 | 模型 | 吞吐量 (tok/s) | Speedup |
|------|------|----------------|---------|
| 4K/4K | gpt-oss-120B | 0.3K | 1.00× |
|        | gpt-oss-puzzle-88B | **0.8K** | **2.44×** |
| 64K/64K | gpt-oss-120B | 0.3K | 1.00× |
|         | gpt-oss-puzzle-88B | **0.8K** | **2.82×** |

> 💡 单卡增益更高，因原模型易受 batch size 限制，而优化后释放内存支持更大 batch。

---

### 准确率保留情况（suite-average, KV FP8）

| 推理努力 | gpt-oss-120B | gpt-oss-puzzle-88B | Retention |
|----------|---------------|--------------------|-----------|
| High | 58.19% | **58.67%** | **100.8%** |
| Medium | 52.89% | **54.93%** | **103.9%** |
| Low | 44.71% | **48.38%** | **108.2%** |

> 🎯 在仅保留约 73% 参数的情况下，实现了 **跨推理努力水平的准确率持平甚至反超**

---

### 请求级效率（Relative Request Rate）

从 Figure 1 可见，`gpt-oss-puzzle-88B` 在整个 **accuracy-speed frontier** 上优于 `gpt-oss-120B` 和 `HyperNova-60B`：
- 最高达到 **1.29× 更高的请求级效率**
- 尽管 `HyperNova-60B` 有较高吞吐，但由于生成 token 数显著增加，其请求级效率被抵消

---

### 消融实验结果

#### (1) 注意力层评分方式对比（Appendix C）
| 替换策略 | 全局层数 | 窗口大小 | MMLU-Pro | GPQA | AIME25 | SciCode | AALCR |
|--------|--------|--------|--------|--------|--------|--------|--------|
| Activation-MSE | 9 | 8192 | 78.32 | 68.69 | 79.17 | 41.12 | 8.00 |
| **AA-LCR-based** | 9 | 8192 | **78.92** | **73.42** | **88.12** | **40.24** | **14.00** |

> ✅ 使用 AA-LCR 作为评分信号能更好地保留长上下文推理能力（AALCR 提升显著）

#### (2) KV 量化缩放因子影响（Table 4）
| 设置 | High-effort Accuracy |
|------|------------------------|
| No Scales | 58.14% |
| **With Calibrated Scales** | **58.67%** |

> ✅ 显著提升精度，验证 scale 校准的重要性

---

## 4. 关键结论和发现

### 主要发现
1. **Puzzle 框架可成功扩展至 MoE + 长上下文场景**  
   通过异构剪枝、任务感知评分、FP8 量化等手段，可在大幅降低推理成本的同时维持甚至提升模型质量。

2. **per-token 吞吐不是唯一标准，必须结合生成长度看请求级效率**  
   “吞吐翻倍但推理变长两倍”等于零收益；本文提出的 **request-level efficiency** 是更合理的评估范式。

3. **多种优化手段协同作用效果显著**  
   MoE 剪枝 + window attention + FP8 KV + RL 微调共同贡献最终性能突破。

4. **AA-LCR 是有效的 long-context 敏感评分器**  
   相比激活 MSE，它更能识别对长程推理至关重要的注意力层，防止关键层被错误替换。

5. **checkpoint averaging 是平衡 accuracy 与 verbosity 的有效策略**  
   成功融合“高准确”与“低生成长度”两种极端策略的优点。

---

### 方法的局限性
- **依赖高质量评分数据集**：需要与父模型分布一致的训练数据来进行可靠的重要性估计。
- **搜索空间受限**：目前仅探索 MoE 专家数和 attention 类型，未涉及 FFN 结构或其他稀疏化方式。
- **长上下文评分开销较大**：AA-LCR 测试本身耗时，难以大规模遍历。
- **未开放完整训练细节**：如 RL 阶段的具体奖励函数设计未完全披露。

---

### 未来工作方向
- 扩展 Puzzle 至更多新型模块（如 Mamba、MQA/GQA 等）
- 探索动态推理路径选择（dynamic early exiting）与 Puzzle 的结合
- 开发更高效的 long-context proxy task 用于快速评分
- 将该流程自动化并集成进推理引擎（如 TensorRT-LLM）
- 推动社区建立标准化的 **request-level efficiency benchmark**

--- 

> 🔚 总结：本文展示了 **post-training NAS** 在现代推理型 LLM 上的巨大潜力，`gpt-oss-puzzle-88B` 不仅实现了高达 **2.82× 单卡吞吐加速**，还在多数任务上 **匹配或超过原始 120B 模型的表现**，为高效部署提供了实用范例。

</details>

---

### 3. [T3D: Few-Step Diffusion Language Models via Trajectory Self-Distillation with Direct Discriminative Optimization](https://arxiv.org/abs/2602.12262)

**Authors**: Tunyu Zhang, Xinxi Zhang, Ligong Han, Haizhou Shi, Xiaoxiao He, Zhuowei Li, Hao Wang, Kai Xu, Akash Srivastava, Hao Wang, Vladimir Pavlovic, Dimitris N. Metaxas  
**Category**: cs.CL  
**Published**: 2026-02-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.12262v1  

#### Abstract
Diffusion large language models (DLLMs) have the potential to enable fast text generation by decoding multiple tokens in parallel. However, in practice, their inference efficiency is constrained by the need for many refinement steps, while aggressively reducing the number of steps leads to a substan...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：T3D: Few-Step Diffusion Language Models via Trajectory Self-Distillation with Direct Discriminative Optimization

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
**Diffusion Large Language Models (DLLMs)** 虽然支持并行解码以提升生成速度，但在实际推理中仍需大量去噪步骤（diffusion steps），导致延迟较高。当强行减少步数时，生成质量会显著下降。这一矛盾限制了其在实时场景（如对话系统、边缘设备）中的应用。

此外，现有 few-step 方法存在两个核心挑战：
- **Mean-field approximation error**：由于 token-wise factorized 参数化假设，在少步解码下误差随步数减少而增大。
- **Train-inference distribution mismatch**：训练时使用随机掩码（random masking），而推理时采用基于置信度的非随机调度（confidence-based schedules），导致中间状态分布不一致。

### 提出了什么新方法或新思路
本文提出 **T3D (Trajectory Self-Distillation via DDO)**，一种通过**轨迹自蒸馏**（trajectory self-distillation）结合**直接判别优化**（Direct Discriminative Optimization, DDO）来提升少步解码性能的新框架。其三大核心组件为：

1. **Trajectory Self-Distillation**  
   利用预训练教师模型自身在目标解码策略下的完整生成轨迹（从全掩码到干净序列的中间状态序列）作为监督信号，对少量步的学生模型进行训练。这使得学生模型在策略上（on-policy）学习真实的推理路径，缓解了 train-inference 分布偏移。

2. **Direct Discriminative Optimization (DDO)**  
   引入一种 GAN 启发式的反向 KL（reverse-KL）风格目标函数，替代传统的前向 KL（forward-KL）匹配。DDO 鼓励“模式寻求”（mode-seeking）行为，使学生更关注高概率的教师生成路径，避免因多模态后验导致的预测平滑化（over-smoothing）问题。

3. **Path Consistency Regularization**  
   对早期解码的 token 施加更高的损失权重，以抑制错误传播。公式中定义每 token 的解码步数 $ \tau_i $，则权重为：
   $$
   w_i = \frac{B - \tau_i + 1}{B}
   $$
   其中 $ B $ 是总步数预算。

最终目标函数为：
$$
\mathcal{L}_{T3D}(\theta) = \mathcal{L}_{\text{traj-DDO}}(\theta) + \lambda \mathcal{L}_{\text{path}}(\theta)
$$

### 相比现有方法的优势
- **无需额外标注数据**：完全基于教师模型自生成的轨迹进行蒸馏，实现无监督 few-step 微调。
- **更强的生成一致性**：通过轨迹级监督降低条件依赖建模误差（conditional total correlation），理论证明其等价于广义的 Discrete Rectified Flow。
- **更鲁棒的少步解码能力**：在极端压缩（如 1–2 步完成一个 block）下仍能保持高质量生成，显著缩小与 full-step 解码之间的性能差距。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
#### 训练数据集（用于 self-distillation）
- **数学推理任务**：`MATH` training set（Hendrycks et al., 2021）
- **代码生成任务**：`PrimeIntellect` dataset（Jaghouar et al., 2024）

#### 基准测试数据集（evaluation benchmarks）
- **MATH500**：高中及竞赛级别数学题
- **GSM8K**：小学算术文字题
- **MBPP**：Python 编程任务
- **HumanEval**：函数级代码生成挑战

这些任务均需要多步推理或迭代修正，对解码质量敏感，适合评估少步生成的稳健性。

### 实验设置和评估指标
#### 模型架构
所有实验基于 **SDAR-family models**（Cheng et al., 2025）：
- `SDAR-1.7B-Chat`
- `SDAR-4B-Chat`

SDAR 是一种块式扩散语言模型（block-based diffusion LM），在块内使用 diffusion 解码，在块间保持自回归，形成 semi-autoregressive 生成流程。

#### 少步解码配置
- **Block Size**: 4 或 8
- **Tokens Per Step (TokPS)**: 控制每个 diffusion step 解码多少 token
  - 如 Block Size=4, TokPS=2 → 2 步完成一个 block
  - TokPS 越大，压缩越激进

#### 动态解码实验（dynamic decoding）
- 使用 confidence threshold = 0.9 自适应决定每步解码 token 数量
- 报告吞吐量（TPS）、延迟（latency）、平均步数、准确率（acc）

#### 评估指标
- **Accuracy**：各 benchmark 上的任务正确率
- **Throughput (TPS)**：tokens per second
- **Latency**：单样本响应时间
- **Average Decoding Steps**：动态解码下的平均去噪步数

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Original Model** | 未微调的原始 SDAR 模型 |
| **SFT** | 在真实数据上监督微调（supervised fine-tuning） |
| **ReDi** (Yoo et al., 2025) | 标准自蒸馏方法，对应 marginal matching |
| **dParallel** (Chen et al., 2025) | 最大化从全掩码到输出的转移概率 |
| **Naive TD** | 仅使用 forward-KL 的轨迹蒸馏（Eqn. 5） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

#### 在 `SDAR-1.7B-Chat` 上（Block Size=8, TokPS=4）
| Method | MATH500 ↑ | GSM8K ↑ | MBPP ↑ | HumanEval ↑ | Avg Imp (%) |
|--------|------------|---------|--------|-------------|--------------|
| ReDi | 15.00 | 32.45 | 3.40 | 5.49 | 135.95 ↓ |
| dParallel | 22.80 | 45.26 | 10.20 | 12.20 | 128.09 ↓ |
| **T3D (Ours)** | **25.60** | **42.91** | **9.40** | **15.24** | **124.79 ↑** |

> ✅ T3D 在最激进设置下仍保持稳定，优于所有 baseline。

#### 在 `SDAR-4B-Chat` 上（Block Size=4, TokPS=4）
| Method | MATH500 ↑ | GSM8K ↑ | MBPP ↑ | HumanEval ↑ | Avg Imp (%) |
|--------|------------|---------|--------|-------------|--------------|
| ReDi | 25.40 | 53.30 | 5.00 | 7.32 | 0.65 ↓ |
| dParallel | 34.20 | 45.94 | 13.20 | 20.73 | 39.05 ↓ |
| **T3D (Ours)** | **47.80** | **69.90** | **22.60** | **23.78** | **85.02 ↑** |

> ✅ T3D 实现大幅领先，尤其在复杂任务（MATH500）上提升明显。

---

### 保留全步解码性能（Table 2）
将 few-step 微调后的模型恢复为 full-step 解码（static decoding, 1 token/step），评估是否遗忘原始扩散能力：

| Model | Method | MATH500 (Full-step Acc) |
|-------|--------|--------------------------|
| SDAR-1.7B | Original | 59.40 |
| SDAR-1.7B | ReDi | 47.00 ↓ |
| SDAR-1.7B | dParallel | 0.40 ↓↓ |
| SDAR-1.7B | **T3D (Ours)** | **56.80** ≈ Original |
| SDAR-4B | Original | 68.00 |
| SDAR-4B | **T3D (Ours)** | **70.00** > Original ✅ |

> 🔍 发现：T3D 几乎完全保留甚至略微超越原模型的 full-step 性能，说明其未过拟合于少步解码。

---

### 动态解码性能（Table 3）
尽管 T3D 在静态步数下训练，但在动态解码中依然表现优异：

| Benchmark | Method | Accuracy ↑ | Latency ↓ | TPS ↑ |
|-----------|--------|------------|-----------|--------|
| MATH500 | Original | 39.00 | 1.10 | 657.72 |
| MATH500 | **T3D** | **49.40** | **0.66** | **791.23** |
| GSM8K | **T3D** | **72.40** | **0.37** | **843.05** |
| MBPP | **T3D** | 23.60 | 0.19 | 313.18 |
| HumanEval | **T3D** | **29.27** | **0.26** | **222.68** |

> ✅ T3D 在动态解码中实现更高准确率 + 更低延迟 + 更高吞吐，验证其泛化能力。

---

### 消融实验结果（Ablation Studies）

#### D.1：正则化系数 $ \lambda $ 的影响（Table 4）
- $ \lambda = 0.2 $ 在多数配置下达到最优平衡
- 过小（0.05）无法有效约束；过大（0.5）可能损害性能
- 默认设为 **0.2**

#### D.2：组件对 full-step 性能的影响（Table 5）
| 组件组合 | MATH500 Acc |
|---------|-------------|
| Naive TD | 22.00 ↓ |
| Naive TD + Lpath | 58.00 |
| Naive TD + Random Tokens | 57.40 |
| **DDO + Lpath + Random** | **69.00** ✅ |

> 🔍 发现：**DDO + Path Consistency + Random Token Mixing** 三者协同是成功的关键。

#### D.3：few-step 生成消融（Table 6 & 7）
- DDO 显著优于 Naive TD，尤其是在强压缩（2 steps/block）下
- Random initialization 对 DDO 至关重要（提升稳定性）
- Path consistency 提升鲁棒性，尤其在中等压缩下效果显著

---

## 4. 关键结论和发现

### 主要发现
1. **轨迹自蒸馏可有效缓解 train-inference 分布偏移**，使学生模型在真实推理路径上学习，提升少步解码效率。
2. **DDO 的 reverse-KL 特性更适合处理高度多模态的去噪后验**，避免 forward-KL 导致的预测模糊问题。
3. **T3D 能在极端少步设置下保持高性能，并几乎无损地保留 full-step 解码能力**，实现了“两全其美”。
4. **该方法兼容动态解码策略**，在实践中展现出更高的吞吐与更低延迟。

### 方法的局限性
- 当前方法依赖于高质量教师轨迹生成，若教师本身不稳定，会影响蒸馏效果。
- DDO 的优化过程可能较敏感，需 careful tuning of $ \lambda $ 和 reference model 更新频率。
- 实验集中在数学与代码任务，通用文本生成（如对话、摘要）上的表现有待进一步验证。

### 未来工作方向
- 探索将 T3D 扩展至其他类型的生成任务（如摘要、对话）。
- 结合强化学习进一步优化动态解码策略。
- 设计更高效的 reference model 更新机制（如 exponential moving average）。
- 将 T3D 与其他加速技术（如 KV Cache for DLLMs）集成，构建端到端高效推理系统。

---

> 🌟 **总体评价**：T3D 提供了一个简洁而强大的框架，推动了 **practical few-step DLLMs** 的发展，为实现高速、高质量文本生成提供了坚实基础。  
> 源码已开源：[https://github.com/Tyrion58/T3D](https://github.com/Tyrion58/T3D)

</details>

---

### 4. [RL over Commodity Networks: Overcoming the Bandwidth Barrier with Lossless Sparse Deltas](https://arxiv.org/abs/2602.11456)

**Authors**: Chaoyi Ruan, Geng Luo, Xinyi Wan, Long Zhao, Qinghe Wang, Jiaan Zhu, Duling Xu, Guanbin Xu, Dehui Wei, Xiang Liu, Cheng Li, Haifeng Sun, Congcong Miao, Jialin Li  
**Category**: cs.DC  
**Published**: 2026-02-13  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.11456v1  

#### Abstract
LLM post-training with reinforcement learning (RL) requires frequent synchronization of large model parameters between the trainer and distributed rollout actors. High-throughput RL post-training therefore relies on dedicated RDMA HPC clusters, an infrastructure cost most organizations cannot absorb...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*RL over Commodity Networks: Overcoming the Bandwidth Barrier with Lossless Sparse Deltas*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLM）在训练后阶段广泛采用 **Reinforcement Learning (RL)** 进行对齐优化（如 RLHF）。这类训练依赖于 **Trainer** 和多个分布式 **Rollout Actors** 之间的频繁参数同步。传统系统依赖高带宽、低延迟的 **RDMA HPC 集群** 来实现高效的全权重广播（full-weight broadcast），但这种基础设施成本高昂，且难以被学术机构或初创公司获取。

当尝试将 RL 训练部署到由标准以太网或广域网（WAN）连接的“松散耦合”GPU 资源（如跨云、跨区域、跨实验室）时，网络带宽成为严重瓶颈。例如，在 1 Gbps 的链路上同步一个 8B 模型可能需要超过 100 秒，远超生成 rollouts 所需时间（通常几十秒），导致 GPU 利用率极低。

### **提出了什么新方法或新思路**

本文提出 **SparrowRL** —— 一种专为商品化网络（commodity networks）设计的高性能 RL 训练系统，其核心思想是利用 RL 微调过程中参数更新的**高度稀疏性**。

#### 主要创新机制：
1. **Lossless Sparse Delta Checkpoints（无损稀疏增量检查点）**
   - 观察到 RL 每步仅约 **1% 的参数元素发生变化**。
   - 不再传输完整模型权重，而是提取并传输**稀疏的增量（sparse delta）**，只包含变化的参数及其精确值。
   - 采用 **delta-encoded variable-length indexing** 对索引进行压缩，进一步减少元数据开销。
   - **完全无损（lossless）**，不进行量化或丢弃，保证模型精度不变。

2. **Streaming Delta Transfer Protocol（流式增量传输协议）**
   - 将稀疏增量划分为多个段（segments），通过多路并行 TCP 流（multi-stream）传输，提升带宽利用率并降低尾部延迟。
   - 实现 **cut-through forwarding**：在 Trainer 提取增量的同时就开始传输已编码的部分，实现提取与传输的流水线重叠。
   - 引入 **Relay-based fanout**：每个区域指定一个 Relay 节点接收增量，并在区域内转发给其他 Actors，避免跨区域多次传输。

3. **Heterogeneity-Aware Scheduling + Lease-Based Fault Tolerance**
   - **异构感知调度**：根据各 Actor 的实际吞吐量和网络带宽动态分配任务批次大小，确保所有 Actor 在一步策略滞后内完成。
   - **基于租约的容错**：通过时间限定的租约管理任务，自动检测失败节点并重新分配任务，无需全局同步屏障。

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **性能** | 在 WAN 上实现高达 **2.4–9.5× 的吞吐量提升**，接近理想 RDMA 单数据中心性能（差距缩小至 **8.91% 内**）。 |
| **成本** | 利用按需、跨云的 commodity GPU，实现 **1.21–1.59× 更高的 tokens/$**，显著优于预留 RDMA 集群。 |
| **可访问性** | 使不具备专用 HPC 设施的研究者也能高效进行大规模 RL 训练。 |
| **准确性** | 保持 **bit-exact 更新**，无信息损失，不影响模型质量。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **Hendrycks MATH**：数学推理基准。
- **GSM8K**：小学数学应用题数据集。
- **DeepScaleR**：用于评估 LLM 推理能力的合成数据集。

### **实验设置**

- **模型**：Qwen3 系列模型（4B, 8B, 14B 参数规模）。
- **训练器（Trainer）**：在美国部署，使用 H100 GPU（SXM5），通过 FSDP2 进行分布式优化。
- **Rollout Actors**：分布在加拿大、日本、荷兰、冰岛等地，使用 A100 或 L40 GPU，通过 vLLM 进行推理。
- **网络环境**：
  - 实际跨云 WAN 链接，带宽波动于 **500 Mbps 至 1 Gbps**。
  - 使用 `tc` 工具模拟从 **250 Mbps 到 10 Gbps** 的不同带宽条件。
  - 地理分布扩展至最多 **4 个数据中心（DC）**。

### **评估指标**

| 指标 | 定义 |
|------|------|
| **Throughput** | 整个系统的平均每秒处理 token 数（tokens/s）。 |
| **Training Step Time** | 单个优化步骤的墙钟时间（wall-clock duration），包括同步、生成和计算梯度。 |
| **Tokens per Dollar (tokens/$)** | 成本效率指标，衡量单位美元所能处理的 token 数量。 |
| **Gap to Ideal Performance** | 与理想单数据中心 RDMA 基线的吞吐量差距百分比。 |

### **基线方法对比**

| 基线方法 | 描述 |
|---------|------|
| **Ideal-SingleDC** | 理想化的单数据中心 RDMA 基线（800 Gbps + NVLink），作为性能上限。 |
| **PrimeRL-Full** | 在地理分布式环境下广播完整模型权重。 |
| **PrimeRL-MultiStream** | 广播完整权重，但使用多流传输提升带宽利用率。 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 模型 | 方法 | 吞吐量 (tokens/s) | 步骤时间 (s) | Tokens/$ (×10⁶) |
|------|------|------------------|--------------|------------------|
| Qwen3-8B | SparrowRL | ~15.9k | ~12.6 | 3.60 |
| Qwen3-8B | Ideal-SingleDC | ~16.5k | ~11.8 | 2.99 |
| Qwen3-14B | SparrowRL | ~14.0k | ~17.1 | 2.12 |
| Qwen3-14B | Ideal-SingleDC | ~14.8k | ~16.2 | 1.33 |

### **与基线方法的对比结果**

- **吞吐量提升**：
  - 相比 **PrimeRL-Full**，SparrowRL 在 Qwen3-4B 上提升 **2.4–3.7×**，在 Qwen3-14B 上提升 **7.7–9.5×**。
  - 相比 **PrimeRL-MultiStream**，仍高出 **1.5–6.0×**。
- **步骤时间大幅缩短**：
  - PrimeRL-Full 在 Qwen3-14B 上步骤时间超过 **500 秒**，而 SparrowRL 仅为 **~17 秒**。
- **接近理想性能**：
  - SparrowRL 的吞吐量与 Ideal-SingleDC 的差距仅为 **1.31%–8.91%**，而 PrimeRL-Full 的差距高达 **59.0%–90.3%**。
- **成本效率显著提高**：
  - SparrowRL 实现 **1.21× (8B)** 和 **1.59× (14B)** 更高的 tokens/$，即使与理想 RDMA 集群相比也更具经济性。

### **消融实验结果**

| 优化项 | 效果 |
|--------|------|
| **稀疏编码（Sparse Encoding）** | 将 Qwen3-8B 的每步传输负载从 **15.6 GB** 减少到 **202 MB**（**79× 压缩**）。 |
| **uint8 delta 编码** | 相比 int32/64 索引编码，进一步将 payload 从 **414 MB → 202 MB**，传输时间从 **9.22s → 4.71s**。 |
| **Multi-Stream 传输** | 在单流基础上进一步将传输时间从 **4.71s → 2.90s**，提升带宽利用率。 |
| **Relay-based Distribution** | 在跨区域部署中，吞吐量提升 **4.4%–13.9%**，尤其在高延迟场景下更明显。 |
| **Heterogeneity-Aware Scheduling** | 在混合 A100/L40 的异构环境中，相比均匀分配，端到端吞吐量提升 **26.4%–35.5%**。 |

---

## 4. 关键结论和发现

### **主要发现**

1. **RL 参数更新具有高度稀疏性**：每步仅有约 **1% 的参数元素发生变化**，这一特性在多种模型（Qwen, Llama, GLM）和算法（GRPO, RLOO, OPO）中普遍存在且稳定。
2. **稀疏增量传输可突破带宽瓶颈**：通过传输 sparse delta 而非 full weights，可在 **1–10 Gbps 的 commodity WAN** 上实现接近 RDMA 集群的训练效率。
3. **系统级协同设计至关重要**：结合 **sparse checkpointing + streaming transfer + heterogeneity-aware scheduling + lease-based fault tolerance** 可有效应对松散耦合环境下的挑战。
4. **成本效益显著**：利用跨云按需 GPU，SparrowRL 在相似吞吐量下提供 **1.21–1.59× 更高的 tokens/$**，极大降低了 RL 训练门槛。

### **方法的局限性**

- **依赖 Trainer 的高算力**：Trainer 需要在高性能集群上运行以快速完成优化和稀疏增量提取。
- **CPU 开销**：稀疏增量的提取和编码带来额外的 CPU 开销，虽被重叠隐藏但仍存在。
- **不适用于全量梯度聚合场景**：本文聚焦于 **policy synchronization from Trainer to Actors**，未解决跨区域梯度聚合问题。

### **未来工作方向**

- 将 SparrowRL 与分布式训练技术（如 ZeRO、pipeline parallelism）结合，支持更大规模的跨区域联合训练。
- 探索在 **decentralized RL** 架构中进一步优化通信模式。
- 支持更多类型的稀疏结构（如 structured sparsity）以进一步压缩。
- 扩展至其他需要高频参数同步的训练范式（如 continual learning）。

--- 

> **总结一句话**：  
> SparrowRL 通过洞察并利用 RL 微调中的**细粒度参数稀疏性**，实现了在低成本、低带宽的商品化网络上进行高效、准确的大规模 RL 训练，**打破了对昂贵 RDMA 集群的依赖**，为更广泛的社区打开了前沿模型研究的大门。

</details>

---

### 5. [TSR: Trajectory-Search Rollouts for Multi-Turn RL of LLM Agents](https://arxiv.org/abs/2602.11767)

**Authors**: Aladin Djuhera, Swanand Ravindra Kadhe, Farhan Ahmed, Holger Boche  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11767v1  

#### Abstract
Advances in large language models (LLMs) are driving a shift toward using reinforcement learning (RL) to train agents from iterative, multi-turn interactions across tasks. However, multi-turn RL remains challenging as rewards are often sparse or delayed, and environments can be stochastic. In this r...

---

### 6. [Learn from Your Mistakes: Self-Correcting Masked Diffusion Models](https://arxiv.org/abs/2602.11590)

**Authors**: Yair Schiff, Omer Belhasin, Roy Uziel, Guanghan Wang, Marianne Arriola, Gilad Turok, Michael Elad, Volodymyr Kuleshov  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.11590v1  

#### Abstract
Masked diffusion models (MDMs) have emerged as a promising alternative to autoregressive models, enabling parallel token generation while achieving competitive performance. Despite these advantages, MDMs face a fundamental limitation: once tokens are unmasked, they remain fixed, leading to error acc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Learn from Your Mistakes: Self-Correcting Masked Diffusion Models*

## 1. 论文的主要贡献和创新点

### 解决的问题
**Masked Diffusion Models (MDMs)** 虽然在离散数据生成中表现出色，并支持并行 token 生成，但存在一个根本性缺陷：一旦某个 token 被“unmask”（解码），它在整个生成过程中就固定不变。这导致早期解码错误无法被修正，随着生成进行，错误不断累积，最终造成样本质量下降（distributional drift）。

### 提出的新方法：Progressive Self-Correction (ProSeCo)
本文提出了一种名为 **Progressive Self-Correction (ProSeCo)** 的新框架，其核心思想是：
- **训练一个既能解码又能纠错的统一模型**：通过将 MDM 解码器自身的输出视为一种“噪声”版本的数据，训练同一个模型去“纠正”这些由自身产生的潜在错误。
- **引入自纠正损失 (Self-Correcting Loss)**：在标准 MDM 的变分目标函数基础上，增加了一个简单的交叉熵（cross-entropy）损失项。该损失项的目标是让模型学会从其自身解码器（`xθ`）的输出中恢复出真实的干净数据。
- **推理时交错执行解码与纠错步骤**：在采样阶段，除了标准的 unmasking 步骤外，还周期性地插入“corrector loop”。在这些循环中，模型以当前已生成的完整序列为输入，尝试更新其中可能存在的错误 token。

### 相比现有方法的优势
- **简单高效**：ProSeCo 对现有的 MDM 训练和采样算法仅需极小的修改（如增加一个损失项和一个采样循环），易于实现。
- **质量-效率权衡更优**：能够以更快的速度（减少 2-3 倍的 NFEs）达到与标准 MDM 相同甚至更高的样本质量。
- **支持推理时计算扩展 (Inference-time Compute Scaling)**：通过增加纠错循环的频率和步数，可以在测试时投入更多计算资源来进一步提升样本质量，这是标准 MDM 所不具备的能力。
- **通用性强**：适用于条件和非条件生成任务，在代码、数学、分子设计和无条件文本生成等多个领域均表现出色。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **代码与数学任务**：对 LLaDA-Base 8B 模型进行监督微调（SFT），使用了 `rStar-Coder` 和 `OpenMathInstruct-2` 数据集。
- **下游评测基准**：
  - **代码**：`HumanEval`, `MBPP`
  - **数学**：`GSM8K`, `Minerva`
- **引导分子设计 (Guided Molecule Design)**：在 `QM9` 数据集上训练，使用 SMILES 字符串表示分子。
- **无条件文本生成 (Unconditional Text Generation)**：在 `OpenWebText (OWT)` 数据集上从头训练模型。

### 实验设置和评估指标
- **评估指标**：
  - **代码/数学任务**：Pass@1 准确率。
  - **分子设计**：生成的有效（valid）、唯一（unique）、新颖（novel）分子数量，以及新颖分子的平均属性值（如环数 ring count 或药物相似性 QED）。
  - **无条件生成**：MAUVE 分数（衡量与人类文本的分布差距）、生成困惑度（Gen. PPL，用 GPT2-Large 评估）和序列熵（衡量多样性）。
- **关键变量**：通过调整 `unmasking steps (T)`、`corrector frequency (w)` 和 `corrector steps per loop (S)` 来控制计算预算和探索质量-效率权衡。

### 基线方法对比
- **Off-the-shelf 模型**：`Llama3.1-Instruct`, `LLaDA1.5`, `LLaDA-Instruct`。
- **MDM 基线**：`LLaDA-Base` + Vanilla SFT。
- **带其他纠错机制的 MDM**：`+ReMDM`, `+PRISM`。
- **其他生成范式**：标准 `Autoregressive (AR)` 模型。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
1. **全面超越基线**：
   - 在所有四个基准测试（HumanEval, MBPP, GSM8K, Minerva）上，**ProSeCo SFT** 模型均显著优于所有基于 MDM 的基线模型。
   - 即使不使用 ProSeCo 的采样算法，仅用 ProSeCo 目标函数训练的模型也已优于标准 MDM 训练的模型。

2. **卓越的质量-效率权衡**：
   - **速度提升**：通过增加并行解码（减少 unmasking steps）并辅以纠错，ProSeCo 可以实现 **~2-3 倍的加速**（NFEs 减少），同时保持甚至超过标准 MDM 的准确率。
   - **质量提升**：通过增加推理时的计算量（更多的纠错步骤），ProSeCo 可以在多个任务上实现最高达 **~1.3 倍的准确率提升**。
   - 例如，在 HumanEval 上，ProSeCo (Sampling) 达到了 **62.20%** 的 Pass@1，远超基线 SFT 的 48.17% 和 Llama3.1-Instruct 的 58.54%。

3. **引导生成中的帕累托前沿优化**：
   - 在分子设计任务中，ProSeCo 成功地在“新颖性”和“属性最大化”之间取得了更好的平衡，显著扩展了帕累托前沿，尤其是在最大化“环数”时效果尤为突出。

4. **无条件生成的优越性**：
   - 在 OWT 上的无条件生成中，ProSeCo 在 MAUVE 和生成困惑度等指标上均优于 MDLM、ReMDM 和 PRISM 等基线。
   - 例如，在 T=256 步时，ProSeCo 的 MAUVE 达到 **0.419**，而 PRISM 为 0.294，ReMDM 为 0.216。

### 消融实验结果
- **纠错预算配置**：实验表明，对于快速采样模式（高并行度），需要更频繁的纠错（`w` 较小）来抵消质量下降；而对于低并行度，增加 `S` 和降低 `w`（即增加纠错强度 `S/w`）几乎总能带来性能提升。
- **纠错强度 `p=S/w` 是关键**：
  - **Fast 模式 (`p < 1`)**：稀疏纠错，优先速度，可实现 2-3 倍加速且精度不降。
  - **Balanced 模式 (`p ≈ 1`)**：平衡质量和效率。
  - **Max 模式 (`p > 1`)**：密集纠错，追求最高质量，通过推理时计算扩展实现性能上限突破。

---

## 4. 关键结论和发现

### 主要发现
- **错误纠正是提升 MDM 性能的关键**：通过让 MDM 学会“自我纠正”，可以有效解决因 token 固定而导致的错误累积问题。
- **ProSeCo 架构非常有效**：提出的训练和采样框架简单、通用，能够在多种任务上取得一致且显著的性能提升。
- **实现了新的能力——推理时计算扩展**：ProSeCo 首次允许在推理时通过增加纠错步骤来动态提升生成质量，为模型部署提供了更大的灵活性。

### 方法的局限性
- **训练成本增加**：由于在训练时需要额外的前向传播来计算纠正损失，增加了约一倍的训练计算开销，相比纯推理时纠错的方法（如 ReMDM）代价更高。
- **依赖于基础架构**：虽然论文强调其通用性，但其有效性建立在标准 Transformer 架构之上，对其他骨干网络的适配性未充分验证。

### 未来工作方向
- **分离解码器与纠正器**：探索解耦 `denoiser` 和 `corrector` 的权重，或使用完全独立的神经网络，以研究两者功能的专业化是否能带来进一步收益。
- **更复杂的纠错调度策略**：设计更智能的方案来联合调度解码和纠错步骤，例如基于置信度动态决定何时纠错，而非固定的频率 `w`。
- **扩展到其他模态**：将 ProSeCo 框架应用于图像、音频等其他领域的离散扩散模型。

</details>

---

### 7. [Pushing Forward Pareto Frontiers of Proactive Agents with Behavioral Agentic Optimization](https://arxiv.org/abs/2602.11351)

**Authors**: Yihang Yao, Zhepeng Cen, Haohong Lin, Shiqi Liu, Zuxin Liu, Jiacheng Zhu, Zhang-Wei Hong, Laixi Shi, Ding Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11351v1  

#### Abstract
Proactive large language model (LLM) agents aim to actively plan, query, and interact over multiple turns, enabling efficient task completion beyond passive instruction following and making them essential for real-world, user-centric applications. Agentic reinforcement learning (RL) has recently eme...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Pushing Forward Pareto Frontiers of Proactive Agents with Behavioral Agentic Optimization

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**Proactive LLM Agents**在多轮交互任务中面临的核心挑战——**任务性能与用户参与度之间的权衡（trade-off）**。具体而言：
- 为了提升最终任务完成质量，代理（agent）倾向于频繁向用户请求反馈（如验证答案），这虽然能提高准确性，但会显著增加用户的负担，降低满意度。
- 反之，若限制用户交互次数，则可能导致信息获取不足，影响推理能力。

这一矛盾本质上是一个**多目标优化（Multi-Objective Optimization, MOO）问题**，即如何在最大化任务性能的同时最小化用户参与成本。

### 提出的新方法：BAO (Behavioral Agentic Optimization)
为解决上述问题，作者提出了 **BAO** 框架，其核心创新在于将**行为建模**（behavior modeling）深度集成到 Agentic RL 中，通过两个关键机制实现帕累托前沿（Pareto Frontier）的前移：

1. **Behavior Enhancement (行为增强)**  
   在 SFT 阶段，通过提示一个强大的教师模型（如 GPT-4o），显式地生成包含特定**多轮行为模式**的数据，用于微调学生模型。这些行为包括：
   - **Retrospective Reasoning (回溯性推理)**：维护记忆、管理假设、在收到否定反馈时修正错误假设（Hypothesis Refinement）。
   - **Prospective Planning (前瞻性规划)**：动态调度（Dynamic Scheduling）交互预算、战略性提问（Strategical Querying）以高效减少不确定性。

2. **Behavior-Regularized RL (行为正则化强化学习)**  
   在 RL 阶段，引入基于回合（turn-level）的奖励塑形（reward shaping）来抑制低效行为：
   - **Information-Seeking Regularization**：惩罚在没有获得环境信息增益的情况下连续向用户提问。
   - **Over-Thinking Regularization**：惩罚因过度思考而提前耗尽交互预算的轨迹。

### 相比现有方法的优势
- **超越被动与过度主动**：BAO 不是简单地在“完全被动”和“过度依赖用户”之间折衷，而是通过增强智能体的内在推理能力，使其更自主、更高效地完成任务。
- **推动帕累托前沿**：实验证明，BAO 能够找到比现有方法更优的权衡解，在同等用户参与下取得更高性能，或在同等性能下大幅减少用户干预。
- **缓解 Reward Hacking**：相比基线方法，BAO 产生的策略更稳健，不易通过生成冗长、混乱的回答来欺骗基于 LLM 的奖励模型。

---

## 2. 核心实验方法和设置

### 数据集
实验基于 **UserRL benchmark suite** 中的三个多轮 Proactive Agent 任务：
- **Function-Gym**：推断一个由四个变量构成的未知数学函数。代理可通过 `Action` 查询任意输入的输出，并通过 `Answer` 提交对测试用例的预测。环境反馈完全基于规则，无模拟用户。
- **Telepathy-Gym**：推断一个隐藏的目标实体。代理可向环境（LLM 模拟）提出“是/否”问题（`Action`），并提交候选答案（`Answer`）以获取反馈。存在训练-评估分布偏移（training-evaluation distribution shift）。
- **Turtle-Gym**：解开一个“乌龟汤”式的谜题，揭示故事背后的隐藏转折。代理通过 `Action` 提问，通过 `Answer` 提交解释。奖励由 LLM 评估器根据覆盖的关键点计算，且每个关键点仅首次正确提及才得分。

所有任务均设定最大交互轮数（interaction budget）为 15 轮。

### 实验设置和评估指标
- **基础模型**：使用 Qwen3 系列模型（1.7B 和 4B 参数量）作为基础。
- **训练流程**：
  1. **SFT (Supervised Fine-Tuning)**：使用行为增强后的数据集进行有监督微调。
  2. **RL (Reinforcement Learning)**：采用 GRPO 算法进行强化学习，使用 turn-level 优势估计。
- **评估指标**：
  - **Pass@U-k**：允许最多 k 次用户参与动作（`Answer`）时的成功率。衡量早期准确性和从错误中恢复的能力。
  - **Score (Unshaped Cumulative Reward)**：未经过塑形的累积奖励，反映最终任务性能。
  - **User Involvement Rate (UR)**：轨迹中用户参与动作（`Answer`）的比例。越低越好，代表用户负担越小。

### 基线方法对比
- **商业闭源模型**：Gemini 系列、GPT 系列（如 GPT-4o, GPT-5）。
- **开源基线模型**：未经微调的 Qwen3 原始模型（Raw Models）。
- **RL 基线方法**：**UserRL** (Qian et al., 2025c)，这是当前最先进的 Proactive Agent RL 训练框架。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
实验结果（见原文 Table 1）表明，**BAO 显著优于所有基线方法**：

| 指标 | BAO vs. UserRL | BAO vs. 商业模型 |
| :--- | :--- | :--- |
| **Pass@U-1 / Pass@U-2** | **全面领先**。例如在 Function-Gym 上，BAO-4B 的 Pass@U-1 达到 0.2692，远超 UserRL-4B 的 0.2436；Pass@U-2 更是从 0.4103 提升至 0.5354。 | **相当甚至更好**。在 Function-Gym 上，BAO-4B 的 Pass@U-2 (0.5354) 超过 GPT-4o (0.5854)，接近 GPT-5-0807 (0.6923)。 |
| **Score (任务性能)** | **显著更高**。BAO-4B 在 Function-Gym 上的分数达到 0.6923，远高于 UserRL-4B 的 0.5256。 | **具有竞争力**。BAO-4B 在 Function-Gym 上的分数超过 GPT-4o (0.1410) 和 GPT-4o-mini (0.0385)。 |
| **User Involvement Rate (UR)** | **大幅降低**。BAO-4B 在 Function-Gym 上的 UR 仅为 0.2148，而 UserRL-4B 高达 0.3758。 | **更低**。BAO-4B 的 UR (0.2148) 远低于 GPT-4o (0.2248) 和 GPT-4o-mini (0.1767)。 |

**核心结论**：BAO 在**不牺牲甚至提升任务性能的前提下，显著减少了对用户的依赖**，实现了更优的帕累托权衡。

### 消融实验结果 (Ablation Studies)
消融实验验证了 BAO 各组件的有效性：

1. **移除 Behavior Regularization (w/o BR)**：
   - 导致 **UR 急剧上升**（如 BAO-4B w/o BR 的 UR 从 0.2148 升至 0.3755）。
   - 表明正则化对于抑制代理过度依赖用户验证至关重要。

2. **移除 Behavior Enhancement (w/o BE)**：
   - 导致 **Pass@U-k 和 Score 下降**（如 BAO-4B w/o BE 的 Pass@U-2 从 0.5354 降至 0.3718）。
   - 表明行为增强对于注入有效的多轮推理能力是必要的。

3. **Retrospective vs. Prospective Behaviors**：
   - **Retrospective Reasoning** 对提升最终上限性能（Score）更重要。
   - **Prospective Planning** 对提升早期成功率（Pass@U-1）更有效。
   - 两者结合才能实现**平衡且全面的性能提升**。

---

## 4. 关键结论和发现

### 主要发现
1. **Proactive Agent 训练本质是 MOO 问题**：必须同时优化任务性能和用户参与成本，单纯调整奖励权重无法有效解决此问题。
2. **多轮行为是关键**：**Retrospective Reasoning** 和 **Prospective Planning** 是连接历史信息与未来决策的核心能力，是实现高效、自主推理的基础。
3. **BAO 成功推动了帕累托前沿**：通过 **Behavior Enhancement** 注入智能行为，并通过 **Behavior-Regularized RL** 抑制低效模式，BAO 能够训练出性能更强、更少打扰用户的 Proactive Agents。
4. **BAO 具有鲁棒性**：在存在 sim-to-real gap 的 Turtle-Gym 任务上，BAO 有效缓解了 Reward Hacking 问题，表现出更好的泛化能力。

### 方法的局限性
- **依赖高质量教师模型**：行为增强阶段需要 GPT-4o 等强大模型来生成行为示范数据，增加了训练成本。
- **语言模态限制**：当前工作专注于纯文本代理，未涉及视觉等多模态场景。
- **任务范围**：实验集中在 UserRL benchmark 的特定任务上，其通用性有待在更广泛的应用中验证。

### 未来工作方向
- 将 BAO 框架扩展到 **Multimodal Agent** 的训练中。
- 探索更高效的、无需强教师模型的行为引导方法。
- 研究如何将 BAO 应用于更复杂的现实世界应用场景，如个性化助手、自动化客服等。

</details>

---

### 8. [Predicting LLM Output Length via Entropy-Guided Representations](https://arxiv.org/abs/2602.11812)

**Authors**: Huanyi Xie, Yubin Chen, Liangyu Wang, Lijie Hu, Di Wang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.11812v1  

#### Abstract
The long-tailed distribution of sequence lengths in LLM serving and reinforcement learning (RL) sampling causes significant computational waste due to excessive padding in batched inference. Existing methods rely on auxiliary models for static length prediction, but they incur high overhead, general...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Predicting LLM Output Length via Entropy-Guided Representations

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大语言模型（LLM）的推理服务和强化学习（RL）采样中，输出序列长度呈现**长尾分布**，导致批处理（batching）时因对齐而产生大量无用的填充（padding），造成显著的计算资源浪费。现有方法通常依赖独立的辅助模型进行静态长度预测，存在以下三大缺陷：
- **高开销**：需额外训练和部署一个模型；
- **泛化能力差**：在长序列、复杂推理等场景下表现不佳；
- **无法应对随机性**：在“一对多”（one-to-many）的随机采样场景（如RL）中失效。

### 提出的新方法
本文提出一种轻量级、高效的框架，直接复用主模型内部的隐藏状态（hidden states）进行长度预测，包含两个核心组件：

#### (1) Entropy-Guided Token Pooling (EGTP)
- **核心思想**：利用token的**熵值**作为注意力权重，对输入prompt的隐藏状态进行加权池化。
- **动机**：高熵token代表模型不确定性高，往往对应更关键的语义信息，对预测生成长度更具指示性。
- **优势**：无需额外模型，仅增加一个轻量回归头，计算开销极低。

#### (2) Progressive Length Prediction (PLP)
- **核心思想**：在每个解码步动态更新剩余长度预测。
- **机制**：结合已生成token的隐藏状态与原始prompt表示，逐步细化预测。
- **适用场景**：专为RL等**动态、随机采样**环境设计，克服静态预测的局限。

### 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 开销 | 高（需额外模型） | 极低（复用已有激活） |
| 泛化性 | 差（依赖简单数据集） | 强（适用于长序列、CoT、RL） |
| 动态适应性 | 无 | 支持（通过PLP） |
| 部署复杂度 | 高 | 低 |

---

## 2. 核心实验方法和设置

### 数据集
构建并发布了首个综合性长度预测基准 **ForeLen**，包含两大场景：

#### (1) 长序列与复杂推理生成（Long-Sequence & Reasoning）
- **来源**：LongBench、ZeroSCROLLS、IFEval
- **任务类型**：长文本问答、摘要、复杂指令遵循
- **模型**：Qwen2.5、Llama3.2、DeepSeek-R1-Distill

#### (2) 动态RL采样（Dynamic RL Sampling）
- **来源**：真实GRPO训练流程
- **数据集**：CRUXEval、GSM8K、LiveCodeBench、MATH、MBPP、MMLU-STEM
- **策略**：每条prompt进行K=4的分组采样，记录多个候选响应及其长度

> ✅ 总样本数：23,556（训练14,134，验证/测试各4,711）

### 实验设置
- **硬件**：1×V100 GPU, 10核CPU, 64GB内存
- **优化器**：AdamW，学习率 2e-5，最大10轮，batch size=16
- **随机种子**：42（确保可复现）
- **软标签设计**：将连续长度离散为K=20个bin，使用距离反比概率构造软标签
- **损失函数**：联合损失 $ \mathcal{L} = \lambda \cdot \mathcal{L}_{CE} + (1-\lambda) \cdot \mathcal{L}_{MSE} $，$\lambda=0.95$

### 评估指标
| 指标 | 描述 |
|------|------|
| **MAE**（Mean Absolute Error） | 预测长度与真实长度的平均绝对误差 |
| **Throughput** ↑ | 单位时间完成的任务数 |
| **Job Completion Time (JCT)** ↓ | 平均任务完成时间 |
| **Padding Ratio** ↓ | 填充部分占总分配资源的比例 |

### 基线方法对比
| 方法 | 类型 | 模型 |
|------|------|------|
| SSJF-Reg / SSJF-MC | 回归 / 分类 | BERT |
| S3 | 分类 | DistilBERT |
| PiA | 指令微调 | Vicuna |
| TPV | 线性回归 | DeepSeek-R1-Distill 内部状态 |
| TRAIL | MLP分类器 | Llama嵌入 |
| LTR-C | 分类 | OPT |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（MAE ↓）

#### 在 **ForeLen** 上的平均MAE表现（越低越好）：

| 模型 | EGTP (Ours) | 最佳基线 (TRAIL) | 提升幅度 |
|------|-------------|------------------|----------|
| Qwen2.5 3B | **110.75** | 146.42 | ↓24.4% |
| Qwen2.5 7B | **103.47** | 137.96 | ↓25.0% |
| Llama3.2 1B | **105.08** | 151.80 | ↓30.8% |
| Llama3.2 3B | **101.53** | 157.88 | ↓35.7% |

> 📌 **总体提升**：相比最强基线TRAIL，**平均MAE降低29.16%**；相比广泛使用的SSJF-Reg，**降低55.09%**

#### 在标准 **LMSYS** 基准上的表现：
- GPT-4：EGTP MAE=**87.32** vs. 第二名116.91（↓25.3%）
- Claude-2：EGTP MAE=**68.33** vs. 第二名91.32（↓25.2%）

### 与基线方法的对比结果
- 在所有模型和场景下，**EGTP全面且显著优于所有基线**
- 特别是在**长序列**和**RL采样**任务上优势更大，说明其对复杂和动态场景更强的适应能力
- **PiA**（指令预测自身长度）表现较差，表明LLM难以准确自知输出长度

### 消融实验结果

#### (1) 不同Pooling方式对比（Qwen2.5 7B）

| Pooling 方法 | LongSeq | Reasoning | RL | Avg MAE |
|--------------|---------|-----------|----|---------|
| **EGTP (Ours)** | **81.60** | 133.57 | 95.24 | **103.47** |
| Max Pooling | 122.74 | 137.88 | 98.46 | 119.69 |
| Average Pooling | 173.85 | 142.40 | 149.92 | 155.39 |
| Last Token Pooling | 135.44 | 139.09 | 105.39 | 126.64 |

> ✅ **EGTP显著优于传统Pooling方法**，尤其在长序列任务上差距巨大

#### (2) 损失函数变体对比（Qwen2.5-7B）

| 方法 | LongSeq | Reasoning | RL |
|------|---------|-----------|-----|
| **EGTP (Ours)** | **83.65** | **139.14** | **92.78** |
| Log-scale Regression | 135.73 | 136.09 | 208.02 |
| Label Smoothing | 158.44 | 144.93 | 268.88 |
| Label Noise | 119.05 | 217.44 | 201.77 |

> ✅ 软标签+联合损失设计有效，尤其在RL任务上MAE降低超过100

#### (3) 计算效率分析（预测延迟与显存）

| 方法 | Avg Time (ms) | Avg VRAM (MB) |
|------|---------------|----------------|
| **EGTP (Ours)** | **~0.66** | **~6** |
| SSJF系列 | 3.9–4.2 | ~270 |
| TRAIL | ~2.6 | ~268 |
| PiA | ~60.6 | — |

> ✅ EGTP延迟**不足1ms**，显存开销仅**6MB左右**，几乎无额外负担

---

## 4. 关键结论和发现

### 主要发现
1. **LLM内部状态蕴含长度信号**：主模型的隐藏状态已隐含足够信息用于长度预测，无需外部辅助模型。
2. **Token熵是关键特征**：高熵token更能反映生成复杂度，基于熵加权的池化显著提升预测精度。
3. **动态预测至关重要**：在RL等随机采样场景中，**PLP**通过逐步更新预测，能有效应对“一对多”的长度变异。
4. **端到端收益显著**：集成EGTP的调度器可将**padding ratio降低至0.09–0.18**，吞吐量提升，JCT减半。

### 方法的局限性
- 当前方法依赖于prefill阶段的最终层隐藏状态，未探索中间层或多层融合；
- PLP虽支持动态预测，但仍假设剩余长度可由当前状态估计，极端路径可能仍偏差较大；
- 对超长序列（>17k）的泛化虽优于基线，但仍存在误差累积风险。

### 未来工作方向
- 探索**多粒度表示融合**（如layer-wise attention）以进一步提升鲁棒性；
- 将PLP与**early stopping**或**speculative decoding**结合，实现更细粒度控制；
- 扩展至**多模态生成**（如图文、音视频）的长度/时长预测；
- 构建更大规模的**动态长度预测Benchmark**，推动该领域发展。

---

> 🔗 **代码与数据公开**：  
> GitHub: [https://github.com/xiehuanyi/LP_Bench](https://github.com/xiehuanyi/LP_Bench)  
> Dataset: [https://huggingface.co/datasets/abinzzz/ForeLen](https://huggingface.co/datasets/abinzzz/ForeLen)

</details>

---

### 9. [Few-Shot Design Optimization by Exploiting Auxiliary Information](https://arxiv.org/abs/2602.12112)

**Authors**: Arjun Mani, Carl Vondrick, Richard Zemel  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.12112v1  

#### Abstract
Many real-world design problems involve optimizing an expensive black-box function $f(x)$, such as hardware design or drug discovery. Bayesian Optimization has emerged as a sample-efficient framework for this problem. However, the basic setting considered by these methods is simplified compared to r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Few-Shot Design Optimization by Exploiting Auxiliary Information**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
该论文针对现实世界中**设计优化**（design optimization）任务中存在的两个关键限制：
- 传统 **Bayesian Optimization (BayesOpt)** 仅利用标量奖励 $ f(x) $ 进行黑盒优化，忽略了实验过程中产生的丰富辅助信息（auxiliary information）。
- 现实中的设计任务往往会产生高维、细粒度的观测数据（如传感器时间序列、学习曲线等），这些信息对理解“为何某个设计成功/失败”至关重要。

作者提出了一种新的优化设定：在每次评估设计 $ x $ 时，不仅获得性能指标 $ f(x) $，还获得一个潜在高维的**辅助信息** $ h(x) $，例如机器人抓取过程中的触觉反馈或神经网络训练的学习曲线。

此外，假设存在一个来自相同任务族的历史任务集合，可用于加速新任务的优化。

---

### **提出了什么新方法或新思路**
作者提出一种基于神经网络的**Few-Shot 预测模型** $ \mathcal{P}_\theta(\cdot|C, x) $，其核心思想是：

- 利用一个包含少量已评估设计的小型上下文集 $ C = \{(x_i, f(x_i), h(x_i))\} $ 来预测未见设计 $ x $ 的性能 $ f(x) $。
- 模型通过在大量历史任务上进行元学习训练，学会如何有效表示和利用 $ h(x) $ 来提升对新任务的 few-shot 预测能力。
- 采用 **Transformer-based 架构**（受 Neural Processes 启发），实现对上下文不变性和目标-上下文注意力机制的支持。

> ✅ **关键创新**：将辅助信息 $ h(x) $ 显式地融入上下文中，并让模型学习如何从 $ h(x) $ 中提取有助于优化的信息，而不是简单地建模 $ f(x) $ 与 $ x $ 的关系。

---

### **相比现有方法的优势**
| 对比维度 | 本文方法 | 现有方法 |
|--------|--------|--------|
| **是否使用 $ h(x) $** | ✅ 是，显式利用 | ❌ 多数不使用；少数（如 Composite BayesOpt）受限于 $ f(x)=g(h(x)) $ 结构 |
| **是否支持多任务迁移** | ✅ 是，利用任务历史学习通用表示 | ⚠️ 部分支持，但通常只迁移 $ f(x) $ 相关特征 |
| **模型灵活性** | ✅ 神经网络，可处理复杂、异构、高维 $ h(x) $ | ❌ GP 类方法计算昂贵，难以扩展到高维 $ h(x) $ |
| **训练-推理一致性** | ✅ 模型冻结部署，无需在线微调 | ⚠️ 一些方法需迭代重训练 |

> 💡 因此，本方法能更高效地利用信息丰富的实验反馈，在极少量试验下快速找到高质量设计方案。

---

## **2. 核心实验方法和设置**

### **使用的数据集**

#### （1）**Gripper Design Benchmark（新构建）**
- **任务**：为不同 3D 物体设计最优机械手抓手形状。
- **$ x $**: 抓手几何参数（21维 Bezier 曲面控制点 + 初始高度）
- **$ f(x) $**: 最大抗扰力（grasp stability），单位 N，最大约 6.0N
- **$ h(x) $**: 触觉反馈，包括两个 16×16 触觉图（tactile maps）、其他面接触读数、系统状态（位置、速度、施加力等），按时间步记录 → 形成时间序列
- **规模**：**997个物体 × 平均 ~4300次评估 = 总计 428万条数据**
- **来源**：基于 **ShapeNet** 采样物体，在 **MuJoCo** 中仿真生成
- **划分**：训练集（772）、验证集（75）、测试集（150）

> 🔍 这是一个大规模、真实感强、挑战性的 benchmark，远超现有 multi-task BayesOpt 数据集。

#### （2）**LCBench（公开基准）**
- **任务**：神经网络超参数调优（35个分类任务）
- **$ x $**: 超参数配置（学习率、dropout、层数等）
- **$ f(x) $**: 最佳验证准确率
- **$ h(x) $**: 完整训练过程中的每 epoch 学习曲线（训练/验证准确率、平衡准确率、学习率等，共5条曲线 × 50 epoch）
- **用途**：验证方法在标准 HPO 场景下的泛化能力

---

### **实验设置和评估指标**

#### **训练阶段（Meta-Training）**
- 在所有训练任务上训练模型 $ \mathcal{P}_\theta $
- 每轮随机采样一个任务，从中抽取 context set $ C $ 和 target set $ T $
- Context size: [5,30]（gripper）或 [3,30]（HPO）
- Target size: 固定为 100
- 损失函数：负对数似然（NLL）最小化，预测分布为 $ \mathcal{N}(\mu_k, \sigma_k^2) $

#### **优化阶段（Test-Time Optimization）**
- 使用训练好的 $ \mathcal{P}_\theta $ 作为 surrogate model
- 应用于新任务的 **discrete BayesOpt loop**
- Acquisition Function: **Probability of Improvement (PI)**
- 初始 context: 5个（gripper）或 3个（HPO）低质量样本
- 优化步数：30 steps
- 不更新模型权重（zero-shot transfer）

#### **评估指标**
| 类型 | 指标 |
|------|------|
| **Prediction Accuracy** | Target MSE（均方误差） |
| **Optimization Performance** | - 平均归一化最佳 $ f(x) $<br>- 平均 regret<br>- “解决”的任务比例（regret ≤ threshold） |

---

### **基线方法对比**
| 基线名称 | 描述 |
|--------|------|
| **Ours w/o h** | 消融版本：输入中移除 $ h(x) $，仅使用 $ f(x) $ 和 $ x $ |
| **DGP (Deep Kernel GP)** | SoTA 多任务 BayesOpt 方法：用神经网络编码器学习共享 kernel 和 mean function |
| **GP-H** | 新增基线：使用 multi-output GP 联合建模 $ (E_\phi(h(x)), f(x)) $，其中 $ E_\phi $ 是与本文相同的 $ h(x) $ 编码器 |
| **STGP (Single-Task GP)** | 经典单任务 GP，在测试时随 context 动态拟合 |

> ✅ 所有基线要么忽略 $ h(x) $，要么以次优方式使用。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### （1）**Gripper Design Task**

| 指标 | 本文方法 | 最接近基线（Ours w/o h） | 提升幅度 |
|------|---------|--------------------------|--------|
| **Target MSE @ context=5** | 170.9 | 201.6 | ↓ 15.2% |
| **Best $ f(x) $ @ 30 trials (avg.)** | ~90% of max | 明显更低 | 显著领先 |
| **Fraction Solved (regret ≤ 0.5)** | **67.2%** | 58.0% | ↑ 9.2 pts |
| **Reached Max Reward** | 34.4% | 26.7% | ↑ 7.7 pts |

> 📈 图6显示：本文方法在早期即显著领先，优势贯穿整个优化过程。

#### （2）**Hyperparameter Tuning (LCBench)**

| 指标 | 本文方法 | 最接近基线（Ours w/o h） | 提升幅度 |
|------|---------|--------------------------|--------|
| **Target MSE @ small context** | 明显更低 | 较高 | 尤其在小 context 下优势明显 |
| **Best Accuracy @ 30 trials** | 接近理论最优 | 收敛慢或停滞 | 更快逼近“最后一英里” |
| **Fraction Solved (regret ≤ 0.01)** | **89.7%** | 82.1% | ↑ 7.6 pts |

> 📊 图10显示：本文方法能更快收敛至最优配置，在 learning rate 和 hidden units 上表现尤为稳定。

---

### **消融实验结果**
- **Ours vs. Ours w/o h**：在两种任务上均显著胜出，证明 $ h(x) $ 的有效性。
  - 在 context=5 时，MSE 下降达 15%
  - 表明即使在失败尝试中，$ h(x) $ 仍蕴含改进线索（如触觉分布模式）
- **Ours vs. GP-H**：尽管 GP-H 也使用 $ h(x) $，但性能不如本文方法。
  - 说明简单的联合建模不足以捕捉 $ h(x) $ 中深层语义
  - 神经网络 + Transformer 架构更能灵活学习有用表示

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **辅助信息 $ h(x) $ 具有巨大潜力**：即使 $ f(x) $ 相同，不同的 $ h(x) $ 可揭示设计成败的根本原因，从而指导更智能的搜索。
2. ✅ **Few-shot prediction + auxiliary info = 更快优化**：模型通过历史任务学会“读懂” $ h(x) $，在新任务中仅需少量试验即可做出精准预测。
3. ✅ **Transformer 架构适合该任务**：能够自然处理变长上下文、实现上下文-目标间注意力，且易于扩展到高维 $ h(x) $。
4. ✅ **方法具有跨领域泛化能力**：在同一框架下成功应用于机器人硬件设计和神经网络 HPO，表明其通用性。

### **方法的局限性**
- ❗ **依赖任务同分布假设**：测试任务必须来自与训练任务相似的任务族，否则迁移效果可能下降。
- ❗ **未建模长期规划**：当前使用 one-step lookahead（PI），未考虑选择某些设计以获取更多信息（information gathering）。
- ❗ **$ h(x) $ 编码仍具挑战性**：虽然端到端训练有效，但对于极高维或稀疏信号（如触觉图），表示学习仍是难点。

### **未来工作方向**
1. **Out-of-Distribution Task Handling**：当遇到极端 OOD 任务时，可结合 cold-start model（如 STGP）进行混合决策。
2. **Long-Horizon Acquisition Functions**：探索基于 $ h(x) $ 的信息增益驱动策略，主动选择能提供最多洞察的设计。
3. **Two-Stage Representation Learning**：先对 $ h(x) $ 进行自监督预训练（如预测动力学），再将其嵌入预测模型。
4. **扩展至更多领域**：如药物分子设计（$ h(x) $ 为分子动态模拟轨迹）、芯片设计（$ h(x) $ 为电磁场仿真输出）等。

---

## **总结**

📌 本文提出了一个面向**信息丰富环境**的新型设计优化范式，首次系统性地研究了如何利用**辅助信息 $ h(x) $** 和**多任务历史**来加速新任务的 few-shot 优化。

🔧 方法上，提出基于 Transformer 的神经预测模型，能够在上下文中融合 $ h(x) $ 并实现高精度性能预测。

📊 实验表明，该方法在两个极具挑战性的领域（机器人抓手设计、HPO）上显著优于现有 SoTA 方法，尤其在小样本场景下优势明显。

🌐 该工作推动了 AI-driven design 向更贴近真实科研工程场景的方向发展，具备广泛的应用前景。

</details>

---

### 10. [Bi-Level Prompt Optimization for Multimodal LLM-as-a-Judge](https://arxiv.org/abs/2602.11340)

**Authors**: Bo Pan, Xuan Kan, Kaitai Zhang, Yan Yan, Shunwen Tan, Zihao He, Zixin Ding, Junjie Wu, Liang Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11340v1  

#### Abstract
Large language models (LLMs) have become widely adopted as automated judges for evaluating AI-generated content. Despite their success, aligning LLM-based evaluations with human judgments remains challenging. While supervised fine-tuning on human-labeled data can improve alignment, it is costly and ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Bi-Level Prompt Optimization for Multimodal LLM-as-a-Judge

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文聚焦于 **Multimodal LLM-as-a-Judge**（MLLM-as-a-Judge）场景下的自动评估任务，尤其是对 AI 生成图像的质量进行评分。尽管 Large Language Models (LLMs) 已被广泛用作自动化评判者（LLM-as-a-Judge），但在多模态场景中，其评价结果与人类判断的一致性仍然存在挑战。

现有基于 trial-and-error 的 **Auto Prompt Optimization (APO)** 方法在文本任务中表现良好，但在处理图像时面临一个关键瓶颈：  
> **视觉上下文长度限制**（context window constraints）导致 MLLM 难以同时处理多个图像输入，从而限制了错误样本的批量收集和有效提示优化。

### 提出了什么新方法或新思路
为解决上述问题，作者提出了 **BLPO**（**Bi-Level Prompt Optimization**）框架，其核心思想是：
- 将图像通过 **Image-to-Text (I2T)** 模块转换为文本描述，以节省视觉 token 开销；
- 同时联合优化两个层级的 prompt：
  - **Judge Prompt**：指导 LLM 如何打分；
  - **I2T Prompt**：指导 MLLM 如何生成与评估相关的图像描述（而非通用 caption）。

这种 **bi-level optimization** 结构实现了：
- 在有限 context budget 下高效利用错误样例；
- 动态学习“哪些视觉特征对评估最重要”，并将其转化为可传递的文本线索。

### 相比现有方法的优势
| 维度 | 传统方法（如 APO-image） | BLPO |
|------|------------------------|-------|
| 输入形式 | 原始图像（消耗大量视觉 token） | 文本化描述（节省 context） |
| I2T 能力 | 固定、通用 caption（信息丢失严重） | 可学习、任务相关描述 |
| 优化机制 | 单层 prompt 更新 | 双层协同优化（Judge + I2T Prompt） |
| 扩展性 | 受限于模型视觉 context 长度 | 更适用于大规模错误集训练 |

因此，BLPO 在保持高 human-alignment 的同时显著提升了 prompt 优化效率与泛化能力。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验在四个主流多模态评估基准上进行：

| 数据集 | 任务类型 | 标签范围 | 样本数（每集合） |
|--------|----------|---------|------------------|
| **AGIN** | 图像自然度评分 | 1–7 分 | 100 训练/100 测试 |
| **SeeTRUE** | 图文一致性判断 | 二分类（0/1） | 200/200 |
| **ImageReward** | 图文偏好评分 | 1–7 分 | 140/140 |
| **UnsafeBench** | 安全性判断 | 二分类（Safe/Unsafe） | 110/110 |

这些数据集覆盖了从质量评分到安全审核等多种实际应用场景。

### 实验设置和评估指标
- **Backbone Judge Models**：
  - `Qwen2.5-VL-32B-instruct`
  - `Llama-4-Scout-17B-16E-instruct`
  - `Llama-4-Maverick-17B-128E-instruct`

- **Optimizer LLM**：OpenAI’s `GPT-o3`（temperature=0.0）

- **评估指标**：
  - 分类任务：**Accuracy** 和 **Macro F1**
  - 回归任务（如 AGIN）：将预测离散化后计算 F1/Accuracy

- **优化参数**：
  - 最大迭代轮次：5 轮（outer loop）
  - 每轮错误集大小：最多 10 个错误样本
  - 内层 I2T prompt 优化步数：K=5

### 基线方法对比
比较了多种先进的 LLM-based prompt optimization 方法：
- **OPRO**：基于 LLM 的 prompt 进化搜索
- **TextGrad**：基于“文本梯度”的自动微分式优化
- **APO-image**：直接使用原始图像作为输入的 APO 版本（GPT-o3 为 optimizer）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
在所有三个 judge 模型和四个数据集上，**BLPO 均达到最优或接近最优性能**：

| 方法 | UnsafeBench (F1) | AGIN (F1) | SeeTRUE (F1) | ImageReward (F1) |
|------|-------------------|-----------|---------------|--------------------|
| No Optim. | ~0.65 | ~0.15 | ~0.73 | ~0.20 |
| TextGrad | ~0.75 | ~0.25 | ~0.77 | ~0.28 |
| OPRO | ~0.72 | ~0.26 | ~0.78 | ~0.32 |
| APO-image | ~0.70 | ~0.15 | ~0.75 | ~0.27 |
| **BLPO (Ours)** | **0.80–0.89** | **0.23–0.33** | **0.80–0.82** | **0.32–0.36** |

> ✅ **平均提升约 8% F1 于第二佳方法（如 OPRO 或 TextGrad）之上**，尤其在 UnsafeBench 上优势明显。

### 与基线方法的对比结果
- **稳定性更强**：如图 3 所示，BLPO 在各数据集上的优化曲线更平稳且收敛更快；
- **跨模型鲁棒性强**：在不同 backbone（Qwen / Llama4 系列）上均表现出一致优越性；
- **优于直接传图方法（APO-image）**：说明简单增加视觉输入并不能解决 context 拥塞问题，反而可能降低推理质量。

### 消融实验结果（Table 2）
在 `Llama-4-Scout` 上进行了消融研究：

| 变体 | 方法说明 | ImageReward (F1) | UnsafeBench (F1) |
|------|--------|-------------------|-------------------|
| Fixed I2T Prompt | 使用固定指令 `"Describe in detail"` | 0.25 | 0.73 |
| Judge Prompt-based I2T | 用当前 judge prompt 引导 I2T 描述 | 0.32 | 0.78 |
| **BLPO (Proposed)** | 双层联合优化 I2T + Judge Prompt | **0.34** | **0.81** |

> 🔍 发现：**可学习的 I2T prompt 至关重要**，仅靠共享 judge prompt 不足以实现最佳效果。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **多模态 prompt optimization 的核心瓶颈在于视觉 context 限制**，而非语言建模本身；
2. **将图像转为任务导向的文本描述（learnable I2T）是一种高效的信息压缩方式**；
3. **I2T prompt 应与 judge prompt 共同优化**，才能捕捉到真正影响评估的关键视觉细节；
4. **BLPO 显著提高了 MLLM-as-a-Judge 与人类判断的一致性**，且具有良好的跨任务迁移性和稳定性。

### 方法的局限性
- 当前 I2T 转换依赖外部 MLLM，带来额外延迟和成本；
- 对极端细粒度缺陷（如像素级伪影）仍可能存在信息损失；
- 所有实验基于 black-box optimizer（GPT-o3），未探索开源替代方案。

### 未来工作方向
- 探索端到端可微的 I2T 模块，减少对外部 LLM 的依赖；
- 将 BLPO 扩展至视频或多图推理任务；
- 研究如何将 human feedback 更有效地融入 bi-level loop 中，形成闭环优化系统；
- 探索轻量化 I2T prompt 设计，进一步压缩 context 开销。

---

> 📌 **总结一句话**：  
> **BLPO 是首个针对多模态 LLM-as-a-Judge 场景设计的双层 prompt 优化框架，通过联合优化 Judge Prompt 与 I2T Prompt，在受限视觉 context 下实现了更高效、更贴近人类判断的自动评估能力。**

</details>

---

### 11. [Robust Optimization Approach and Learning Based Hide-and-Seek Game for Resilient Network Design](https://arxiv.org/abs/2602.11854)

**Authors**: Mohammad Khosravi, Setareh Maghsudi  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.11854v1  

#### Abstract
We study the design of resilient and reliable communication networks in which a signal can be transferred only up to a limited distance before its quality falls below an acceptable threshold. When excessive signal degradation occurs, regeneration is required through regenerators installed at selecte...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Robust Optimization Approach and Learning Based Hide-and-Seek Game for Resilient Network Design*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文研究的是**Resilient Communication Network Design**中的**Regenerator Location Problem (RLP)**，即在通信网络中部署信号再生器（regenerator）以应对信号衰减问题。该问题的关键挑战在于：
- 信号只能在有限距离 $d_{\text{max}}$ 内保持质量，超过则需通过 regenerator 恢复。
- **节点安装成本**和**链路长度**均存在不确定性，影响网络连通性和总成本。

传统方法通常假设参数固定或仅考虑静态不确定性，难以反映现实世界中随时间变化的动态波动（如温度、流量变化导致的光纤质量波动）。

---

### 提出的新方法与创新思路

#### （1）引入**Dynamic Budgeted Uncertainty Set**
- **Static Budgeted Uncertainty**：用于建模 regenerator 安装成本的不确定性，允许最多 $I_v$ 个节点的成本上升至其上限。
- **Dynamic Budgeted Uncertainty**：**首次提出**用于建模边长（edge length）的时间依赖性偏差。每个时间段 $t$，最多 $I_e$ 条边可被“激活”以引入最大偏差，更贴近实际网络环境（如昼夜温差、维护周期等）。

#### （2）构建三种数学模型
| 模型 | 描述 |
|------|------|
| **DWC (Deterministic Worst-Case)** | 所有参数取上界值，最保守但可能过度设计 |
| **RSB (Robust Static Budgeted)** | 节点与边均采用静态 budgeted 不确定性 |
| **RDB (Robust Dynamic Budgeted)** | **核心创新**：边采用动态 budgeted 不确定性，节点为静态 |

#### （3）开发多种高效求解算法
针对大规模实例，提出以下可扩展方法：
- **Column-and-Constraint Generation (CCG)**：迭代生成对抗场景，收敛快。
- **Benders Decomposition (BDC)**：将主问题（placement）与子问题（connectivity check）分离。
- **Iterative Robust Optimization (IRO)**：交替优化图结构 $M$ 和 regenerator 放置方案。
- **Learning-based Hide-and-Seek Game (HSL)**：将鲁棒优化建模为学习者（seeker）与对手（adversary）之间的博弈，利用梯度更新模拟自适应攻击与防御。

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **建模能力** | 显式捕捉边长的**时间动态性**，优于静态鲁棒或确定性最坏情况模型 |
| **解决方案质量** | 在多数情况下实现 **10%-30% 成本降低**，避免过度保守设计 |
| **可扩展性** | CCG 和 BDC 在大尺度网络中表现优异，显著优于直接求解混合整数规划 |
| **分析工具** | HSL 提供了一种新颖的视角来理解脆弱路径和关键边，支持快速近似求解 |

---

## 2. 核心实验方法和设置

### 数据集生成方式
所有测试实例均为**随机生成**，遵循统一过程：
- 图 $G=(V,E)$ 为 Erdős–Rényi 随机图，密度 `dens=0.3`
- 节点数 $n \in \{10,12,\dots,60\}$
- 边 $(i,j)$ 的标称长度 $c_e \sim \text{Uniform}(350,600)$，最大偏差 $d_e \sim \text{Uniform}(1,250)$
- 节点安装成本 $c_v \sim \text{Uniform}(250,300)$，最大偏差 $d_v \sim \text{Uniform}(1,50)$
- $d_{\text{max}} = 1000$

共进行四组实验（Exp-1 至 Exp-4），分别控制不同变量组合。

---

### 实验设置
| 设置项 | 说明 |
|--------|------|
| 平台 | Intel Core i7-10510U @1.8GHz, 15GB RAM, 单线程运行 |
| 求解器 | IBM ILOG CPLEX 22.11 |
| 每组实例数量 | 50 次独立运行取平均 |
| 时间限制 | 无显式超时（小规模可解），大实例设最大迭代次数为 10（Exp-4） |

---

### 评估指标
| 指标 | 含义 |
|------|------|
| **Objective Value (Cost)** | 总安装成本（含不确定性下的最坏情况） |
| **R-DWC (%)** | 相对于 DWC 的成本节省百分比 |
| **R-RSB (%)** | 相对于 RSB 的成本节省百分比 |
| **Solution Time** | 求解耗时（秒） |
| **Iterations** | 迭代类算法（CCG/BDC/IRO/HSL）所需迭代轮次 |
| **Performance Profile** | 展示各算法在多大比例实例上达到一定性能倍率以内（见 Appendix B） |

---

### 基线方法对比
| 方法 | 类型 | 说明 |
|------|------|------|
| **DWC** | 确定性最坏情况 | 参数全取上界，基准中最保守 |
| **RSB** | 静态鲁棒优化 | 经典 budgeted robust 对照组 |
| **RDB** | 动态鲁棒优化（本文提出） | 主要比较对象 |
| **CCG / BDC / IRO / HSL** | 可扩展算法 | 用于替代直接求解的大规模求解策略 |

---

## 3. 主要实验结果和性能指标

### Exp-1：小规模网络（$n=10$ 到 $30$）
| 方法 | 成本趋势 | R-DWC 最高提升 | 时间表现 |
|------|----------|------------------|-----------|
| DWC | ~860 | — | 最快 |
| RSB | 略低于 DWC | ~1.4% | 快 |
| **RDB** | **持续最低** | **11.69%** ($n=30$) | 最慢（因复杂建模） |

> ✅ **结论**：动态不确定性建模带来显著成本节约，尤其在网络增大时效果更明显。

---

### Exp-2：变化攻击预算（$I_e \in \{1,2\}, I_v \in \{1,2,3\}$）
| 场景 | RDB vs DWC 成本节省 |
|------|--------------------|
| $I_e=1$ | 高达 **28.03%** |
| $I_e=2$ | 约 **10.95%** |

> 🔍 发现：当敌方对边的攻击预算较小时（$I_e=1$），RDB 的优势更为突出——说明动态建模能更好利用“低频高偏移”事件的信息。

---

### Exp-3 & Exp-4：大规模网络（$n=40$ 到 $60$, $n=50$）
| 方法 | 成本改进（vs DWC） | 迭代次数 | 求解时间 |
|------|--------------------|------------|------------|
| **CCG** | 最高达 **2.40%** | **最少（~2.00）** | **最快，甚至优于 DWC** |
| BDC | 接近 CCG | 稍多 | 次优 |
| IRO / HSL | 可达同等成本 | 更多（~2.4–2.8） | 较慢 |

> ⚡ **关键发现**：
> - **CCG 是最优选择**：兼具高质量解与最短时间。
> - 分解类方法（CCG/BDC）收敛速度快于迭代法（IRO/HSL）。
> - HSL 虽慢，但提供了博弈演化路径，有助于解释性分析。

---

### 数值示例（HSL 收敛演示）
在一个五节点网络中：
- 初始 placement: {2,4} → 成本 17
- 经过 3 轮对抗学习后稳定于 {2,4,5}
- 最终成本约 **20.6**，但仍比 DWC 低约 **10%**

> 🎯 表明 HSL 能有效识别敏感边并引导部署调整。

---

## 4. 关键结论和发现

### 主要发现
1. **动态不确定性建模至关重要**  
   引入 **dynamic budgeted uncertainty** 显著提升了模型的真实性，并带来了 **最高达 30% 的成本节约**，尤其是在 $I_e$ 较小时优势明显。

2. **静态鲁棒性增益有限**  
   RSB 相比 DWC 提升微弱（通常 <1.5%），表明仅引入静态不确定性不足以释放鲁棒优化潜力。

3. **CCG 是最佳实用算法**  
   在大规模网络中，**CCG** 不仅求解速度最快，且迭代次数最少，是工程部署首选。

4. **HSL 提供可解释的对抗机制**  
   学习型 hide-and-seek 游戏不仅能逼近最优解，还能揭示网络中最易受攻击的边和节点，具有诊断价值。

---

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **计算开销仍较高（RDB）** | 尽管 CCG 加速明显，但整体框架仍涉及多次 MIP 求解，不适合实时应用 |
| **假设离散时间周期** | 动态偏差按离散时段建模，未考虑连续时间漂移 |
| **未考虑 regenerator 故障容错** | 当前模型聚焦成本与连通性，未整合冗余备份机制 |
| **HSL 缺乏理论收敛保证** | 基于梯度的学习规则在非凸环境中可能陷入局部均衡 |

---

### 未来工作方向
1. **集成启发式或元启发式算法**  
   开发基于 GA、SA 或 RL 的快速近似算法，进一步提升可扩展性。

2. **扩展不确定性表示形式**  
   探索 **dynamic interval uncertainty** 或 **distributionally robust optimization (DRO)** 框架。

3. **引入 survivability 约束**  
   考虑边/节点失效场景下的 **k-connectivity** 或 **fault-tolerant RLP** 扩展。

4. **联合优化图变换与 regenerator placement**  
   当前两阶段分离处理，未来可尝试端到端联合建模。

5. **应用于真实网络拓扑**  
   在实际光网络或 LEO satellite network 上验证模型有效性。

---

> ✅ **总体评价**：本文系统地推进了鲁棒 RLP 的建模与求解边界，提出的 **dynamic budgeted uncertainty + CCG/HSL 框架** 为 resilient network design 提供了一个强大而灵活的新范式。

</details>

---

### 12. [Credit Where It is Due: Cross-Modality Connectivity Drives Precise Reinforcement Learning for MLLM Reasoning](https://arxiv.org/abs/2602.11455)

**Authors**: Zhengbo Jiao, Shaobo Wang, Zifan Zhang, Wei Wang, Bing Zhao, Hu Wei, Linfeng Zhang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.11455v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly advanced the reasoning capabilities of Multimodal Large Language Models (MLLMs), yet how visual evidence is integrated during reasoning remains poorly understood. We explore multimodal RLVR through the lens of cross-modal attent...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Credit Where It’s Due: Cross-Modality Connectivity Drives Precise Reinforcement Learning for MLLM Reasoning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题？

当前的 **Reinforcement Learning with Verifiable Rewards (RLVR)** 在多模态大语言模型（MLLM）推理任务中普遍存在“**uniform credit assignment**”问题——即无论生成的 token 是否真正依赖视觉输入，所有 token 都被平等地赋予强化信号。这导致：

- 视觉证据未被有效利用；
- 推理过程中的“感知锚点”（perceptual anchors）无法被精准识别和优化；
- 大量语言结构型 token 被错误地分配信用，稀释了对关键视觉-文本耦合部分的学习。

该问题限制了 MLLM 在数学、图表理解等需要强视觉接地（visual grounding）任务上的表现。

---

### 🚀 提出了什么新方法或新思路？

作者提出 **Anchor-Token Reinforcement Learning (AT-RL)**，一种轻量级、即插即用的框架，通过分析跨模态注意力拓扑结构，实现精细化的信用分配。

#### 核心思想：
- 发现 MLLM 的 Chain-of-Thought（CoT）推理过程中，仅有约 **15% 的 token 表现出高 cross-modal connectivity**（即与图像 patch 强关联），这些 token 是“**感知锚点**”（Perceptual Anchors），负责将推理锚定在视觉证据上。
- 多数 token 仅承担语言连贯性和逻辑过渡功能，属于“低连接性 token”。

#### AT-RL 三阶段流程：
1. **定位感知锚点（Locating Anchors）**  
   - 提取并校正跨模态注意力权重（debiasing），计算每个 token 的 **Connectivity Density** $ C_i = \sum_{j \in V} A[i,j] $
   - 高 $ C_i $ 的 token 被识别为潜在锚点。

2. **图聚类分组（Token Grouping via Graph Partitioning）**  
   - 构建 token 级功能依赖图（基于注意力足迹的 cosine similarity）
   - 使用 METIS 算法进行图分割，形成语义一致的 cluster
   - 引入去噪和拓扑传播机制增强鲁棒性

3. **细粒度信用分配（Reinforced Credit Assignment）**  
   - 每个 cluster 的重要性由其内部总 connectivity density 占比决定：  
     $$
     W(C_k) = \frac{\sum_{t_i \in C_k} C_i}{\sum_j C_j}
     $$
   - 将原始序列级 advantage $ A^{(2)} $ 按 cluster 权重软加权为 token 级优势信号：  
     $$
     A^{(i,2)} = W(C_k) \cdot A^{(2)}, \quad \forall t_i \in C_k
     $$

最终目标函数保持与 GRPO/PPO 一致形式，仅替换 advantage 项，因此可无缝集成到现有 RLVR 框架中。

---

### 🔍 相比现有方法的优势

| 特性 | 传统 RLVR（如 GRPO, DAPO） | AT-RL |
|------|----------------------------|-------|
| Credit Assignment | Uniform across all tokens | Selective on high-connectivity anchors |
| 视觉接地能力 | 弱，易受语言先验干扰 | 强，聚焦于视觉相关 token |
| 可扩展性 | 通用但效率低 | Plug-and-play，开销仅 **1.2%** |
| 泛化性 | 依赖任务特定设计 | 单一组超参数适用于多种任务（STEM、VQA、Video） |

> ✅ **优势总结**：AT-RL 不改变模型架构或训练流程，仅通过对 attention topology 的分析实现更精确的梯度引导，在不增加复杂性的前提下显著提升推理质量。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 类别 | 数据集 | 描述 |
|------|--------|------|
| 主训练集 | **ViRL-39K** | 包含 39K 条用于视觉推理的 RL 训练样本 |
| 跨域泛化 | **Geometry-3K**, **GeoQA-8K** | 几何推理专用数据集，测试迁移能力 |
| 视频理解 | **Video-R1**（含 VSI-Bench, VideoMMMU, MMVU） | 多帧视频推理基准 |

---

### ⚙️ 实验设置和评估指标

- **模型系列**：Qwen2.5-VL-3B / 7B / 32B / 72B-Instruct
- **评估协议**：
  - 使用 `LLMs-Eval` 统一评测套件
  - 测试集：各 benchmark 的 `testmini` split
  - 指标：**Acc@1**，温度设为 0.1
  - 视频输入帧数：16 / 32 / 64 帧
- **答案提取策略**：
  - 优先从 `\boxed{}` 中提取
  - 否则使用全文搜索匹配选项或数值
- **验证模块**：使用 Qwen2.5-72B-Instruct 作为裁判模型判断正确性（binary 输出）

---

### 🆚 基线方法对比

| 基线类别 | 方法 | 简介 |
|---------|------|------|
| Uniform Credit | GRPO, DAPO, GSPO, SAPO | 当前主流 RLVR 方法，全局广播 credit |
| Fine-grained Credit | StepGRPO, FT-RL, VPPO | 尝试引入 step/token-level 信用分配 |
| Others | SFT（监督微调） | 作为 zero-shot 性能对照 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 3）

| Benchmark | Qwen2.5-VL-32B (Ours) | Qwen2.5-VL-72B (Baseline) | 提升情况 |
|----------|------------------------|----------------------------|---------|
| **MathVista** | **80.2** | 77.8 | **↑2.4 pts** |
| **MathVerse** | **56.6** | 57.2 | 接近持平（小降） |
| **MathVision** | **44.2** | 43.1 | ↑1.1 pts |
| **MMMU-Pro** | **51.9** | 51.6 | ↑0.3 pts |
| **MEGA** | **53.0** | 49.6 | ↑3.4 pts |
| **ChartQAPro** | **48.4** | 47.2 | ↑1.2 pts |

> 💡 **亮点**：**32B 模型超越 72B 模型的整体表现**，尤其在 MathVista 上取得 SOTA 成绩。

---

### 🔁 与其他 RL 方法对比（Table 1 & 2）

以 **Qwen2.5-VL-7B** 为例，在平均性能上的提升：

| 方法 | Average Score | 相对 Zero-shot 提升 |
|------|---------------|--------------------|
| GRPO | 47.31 | +2.30 |
| DAPO | 48.04 | +3.03 |
| SAPO | 49.94 | +4.93 |
| **SAPO + AT-RL** | **53.25** | **+8.24** |

> ✅ 最佳组合 **SAPO + AT-RL** 达到 **+8.24 个百分点** 的绝对增益。

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）软加权 vs 硬截断（Table 5）

| 方法 | MathVerse Acc |
|------|----------------|
| Zero-shot | 39.62 |
| GRPO (uniform) | 41.48 |
| Hard truncation (top 15%) | 42.58 |
| **AT-RL (soft weighting)** | **45.27** |

> ❗ 结论：**软加权远优于硬掩码**，说明保留非锚点 token 的弱梯度对维持语言连贯性至关重要。

#### （2）偏置校正必要性（Table 6）

| 方法 | MathVerse Acc |
|------|----------------|
| AT-RL w/o Bias Correction | 34.20 |
| **AT-RL w/ Bias Correction** | **37.50** |

> ✅ 去除 attention sinks 和位置偏差后性能明显提升。

#### （3）组件有效性（Table 7）

| 配置 | MathVista Δ |
|------|-------------|
| w/o Grouping | -2.30 |
| w/o Cluster Refinement | -0.70 |
| w/o Neighborhood Expansion | -0.65 |
| **Full AT-RL** | **0.00 (best)** |

> ✅ 所有模块均贡献正向收益，尤其是语义分组和拓扑传播。

---

### ⏱️ 计算效率分析

- **AT-RL 模块额外开销仅为 1.2%**（forward/backward 占 98.8%）
- 支持大规模训练，无显著瓶颈
- 支持 batch-size=512, group-size=8 的高效并行

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **感知锚点的存在是普遍现象**：
   - 在 CoT 推理中，仅约 **15% 的 token 展现出高 cross-modal connectivity**
   - 这些 token 多为动词导向型词汇（如 “calculate”, “given”, “assume”），直接引用图像内容
   - 其余 token 多为抽象术语或连接词（如 “therefore”, “relation”），服务于语言流畅性

2. **有效的 RLVR 必须区分 token 的角色**：
   - 统一 credit assignment 是次优的
   - 正确的答案奖励应集中在感知锚点上；错误时也应惩罚这些锚点（因其最可能误解图像）

3. **AT-RL 实现了精准的 credit assignment**：
   - 通过 graph-based clustering 提升信号稳定性
   - 软加权机制兼顾 grounding 与 coherence
   - 在多个尺度和任务上实现一致增益

4. **RL 与 SFT/CPT 是互补范式**：
   - RL（如 AT-RL）擅长优化“如何正确接地”
   - SFT/CPT 擅长注入“知道什么”（domain knowledge）
   - 二者结合才能同时解决 **Knowledge Deployment Error (KDE)** 和 **Visual Perception Error (VPE)**

---

### ⚠️ 方法的局限性

1. **依赖高质量 attention 输出**：
   - 若 base model 的 attention 已严重偏移（如 sink at start token），需依赖 debiasing 模块
   - 对 attention 可解释性有一定假设

2. **不能弥补知识缺失**：
   - 如 Discussion 4 所示，AT-RL 显著降低 VPE，但对 KDE 影响有限
   - 若模型缺乏领域知识（如几何公式），仍会失败

3. **超参数敏感性虽低但仍存在**：
   - 如 Tcen, α, R 等需调优，尽管默认值已足够稳定

---

### 🔮 未来工作方向

1. **联合训练 SFT + AT-RL pipeline**  
   → 同时注入知识并强化视觉接地

2. **动态调整 anchor threshold**  
   → 根据任务难度自适应选择锚点比例

3. **扩展至其他模态**  
   → 如音频-文本、3D 场景-指令等跨模态场景

4. **探索反事实 anchor 分析**  
   → 若某个 anchor 被修改，是否会导致推理路径崩溃？可用于可解释性研究

---

## ✅ 总结一句话

> **Reasoning quality is governed not by token quantity, but by the fidelity of cross-modal anchoring.**  
> —— AT-RL 通过识别并强化那 **15% 的关键感知锚点 token**，实现了轻量、高效且可扩展的多模态强化学习新范式，在多个任务上让小模型超越更大模型的表现。

</details>

---

### 13. [MAPLE: Modality-Aware Post-training and Learning Ecosystem](https://arxiv.org/abs/2602.11596)

**Authors**: Nikhil Verma, Minjung Kim, JooYoung Yoo, Kyung-Min Jin, Manasa Bharadwaj, Kevin Ferreira, Ko Keun Kim, Youngjoon Kim  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.11596v1  

#### Abstract
Multimodal language models now integrate text, audio, and video for unified reasoning. Yet existing RL post-training pipelines treat all input signals as equally relevant, ignoring which modalities each task actually requires. This modality-blind training inflates policy-gradient variance, slows con...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MAPLE: Modality-Aware Post-training and Learning Ecosystem

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前的 **Multimodal Language Models (MLMs)** 在进行 **Reinforcement Learning (RL) post-training** 时，普遍采用“模态盲”（modality-blind）策略，即不区分输入信号的模态组合（如仅视频、音频+字幕等），将所有样本混合训练。这种做法存在以下问题：

- **梯度方差大**：不同模态组合的任务难度、奖励尺度差异显著，混合批次导致 policy gradient 方差增大。
- **收敛慢且不稳定**：由于异构任务共用优化路径，简单任务主导更新，难任务被忽视。
- **现实部署鲁棒性差**：真实场景常出现模态缺失（missing-at-test）、冗余或权重变化，而传统训练假设全模态可用，导致分布偏移下性能骤降。

### 提出了什么新方法或新思路

作者提出 **MAPLE** ——一个完整的 **modality-aware** 后训练与学习生态系统，包含三大核心组件：

#### （1）MAPLE-bench：首个显式标注最小必要模态的基准
- 对每个任务实例标注 **Required Modality Tags (RMTs)**，明确解决该任务所需的最小模态子集（如 `V`, `VA`, `VAS` 等）。
- 支持在 **7 种模态组合**（单模、双模、三模）上进行细粒度评估。
- 包含两个任务：**MAPLE-QA**（选择题问答）和 **MAPLE-Caption**（开放生成式描述）。

#### （2）MAPO：Modality-Aware Policy Optimization
- 在 **GRPO** 框架基础上引入 **按 RMT 分层训练（stratified training）**：
  - 每个 batch 只包含相同 RMT 的样本。
  - 优势函数（advantage）在同质组内归一化，消除跨模态奖励尺度差异。
- 理论证明：`Var(g_MA) ≤ Var(g_MU)`，即模态感知训练能降低梯度方差。

#### （3）自适应训练策略（Adaptive Training）
- **动态加权（adpw）**：基于历史 KL 散度衡量任务难度，为困难 RMT 分配更高权重。
- **课程学习（curriculum learning）**：按复杂度排序训练顺序（uni → bi → tri-modal），并可动态调整（adpcur）。

### 相比现有方法的优势

| 维度 | MAPLE 优势 |
|------|-----------|
| **效率** | 收敛速度提升 **3.18×** |
| **鲁棒性** | 在模态缺失/冗余下保持稳定，准确率差距缩小 |
| **性能** | 平均准确率更高，尤其提升困难模态表现 |
| **泛化能力** | 能正确识别“信息不足”情况并主动 abstain |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### （1）MAPLE-bench
- **MAPLE-QA-train**: 47,893 个 QA 对，来自 546 个视频。
- **MAPLE-QA-eval**: 5,001 测试样本，人工审核，覆盖全部 7 类 RMT。
- **MAPLE-Caption-train**: 5,120 个生成样本。
- **MAPLE-Caption-eval**: 5,348 个测试样本，每类 RMT 均衡（764 个）。

#### （2）MAPLE-QA+
- 扩展版 QA 数据集，用于验证模型是否真正具备模态感知推理能力。
- 包含三种设定：
  - **Modality-Exact**：提供恰好所需模态。
  - **Modality-Superset**：额外提供无关模态。
  - **Modality-Deficit**：缺少必要模态，需回答 “None”。
- 总计 137,313 个样本（训练 103,265，测试 34,048）。

### 实验设置

- **模型**：Qwen2.5-Omni-3B（支持文本、音频、视频输入）。
- **训练框架**：veRL 库，使用 GRPO 进行无 critic 的 RL 优化。
- **超参数**：
  - 学习率：2×10⁻⁶
  - Batch size：全局 256（mini-batch 32）
  - Rollout 数量：G=8
  - 序列长度：10,240 tokens
- **硬件**：4× NVIDIA H100-80GB

### 评估指标

| 指标 | 定义 |
|------|------|
| **Pass@1** | 多选题中最高得分样本正确的比例（MAPLE-QA） |
| **LLM-as-Judge Score** | 使用 GPT-4o 对生成描述打分，综合 missing info、hallucination、fusion quality 得到 [0,1] 分数（MAPLE-Caption） |
| **Modality Gap (%)** | 单模（Uni）、双模（Bi）、三模（Tri）之间的平均性能差距，越小越好 |
| **Training Efficiency** | 每步训练耗时（秒/step），归一化后比较 |
| **Fusion Gain** | 多模态输出优于最佳单模态输出的比例 |

### 基线方法对比

- **Zero-shot**：未经过 post-training 的原始模型。
- **MUPO (Modality-Unaware Post-training)**：标准全模态训练，忽略 RMT，混合所有批次。
- **MAPO-base**：仅使用 stratified batching 的基础版本。
- 多种变体对比：loss aggregation、clipping、sampling、curriculum 等设计轴。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 方法 | MAPLE-QA Avg Pass@1 (%) | Modality Gap (Avg %) | Time (s/step) |
|------|--------------------------|------------------------|---------------|
| MUPO (baseline) | 58.58 | 2.27 | 523.28 |
| MAPO (base) | 58.68 | 2.11 | 389.11 |
| **Full-recipe (MAPLE)** | **58.72** | **1.74** | **164.72** |

> ✅ **MAPLE 收敛速度快 3.18×，模态差距缩小 30.24%**

#### MAPLE-QA 最终结果（Table 2）

| Method | V | A | S | VA | VS | AS | VAS | **Avg** |
|--------|----|----|----|-----|-----|-----|-----|--------|
| MUPO | 55.08 | 65.34 | 63.82 | 57.47 | 60.15 | 58.77 | 58.14 | 58.58 |
| MAPO | 55.79 | 65.78 | 61.40 | 57.85 | 60.26 | 59.06 | 57.76 | 58.68 |
| **adpw + adpcur** | **57.13** | **66.40** | **61.35** | **59.16** | **61.64** | **58.33** | **58.67** | **59.82** |

> 🔺 提升显著集中在最难的视觉相关任务（V, VA）上。

#### MAPLE-Caption 最终结果（Table 3）

| Method | V | A | S | VA | VS | AS | VAS | **Avg** |
|--------|----|----|----|-----|-----|-----|-----|--------|
| MUPO | 63.78 | 73.69 | 68.20 | 64.46 | 65.69 | 71.71 | 65.18 | 67.67 |
| MAPO | 66.78 | 83.46 | 87.50 | 66.44 | 67.99 | 78.59 | 66.40 | 73.88 |
| **adpw + adpcur** | **66.20** | **81.42** | **84.97** | **66.98** | **69.10** | **81.71** | **67.58** | **74.00** |

> ✅ 多模态融合能力大幅提升：**Fusion Gain 达 30.24%**（vs. MAPO 的 18.19%）

### 消融实验结果

#### （1）算法设计轴消融（Table 1）

| 设计轴 | 最优配置 | 提升效果 |
|-------|---------|--------|
| **Loss Aggregation** | Sample-level > Token-level | 更好保留 query-level 信用分配 |
| **Clipping** | Asymmetric (e⁺=0.3, e⁻=0.2) | Clip fraction ↓71.65%，探索更充分 |
| **Sampling** | Early filtering（去除零方差样本） | 训练速度 ↑1.98× |
| **Curriculum** | Uni → Bi → Tri 按复杂度排序 | 准确率 ↑至 59.05% |

最终 **Full-recipe** 组合四项最优策略，在所有维度均胜出。

#### （2）自适应策略消融（Tables 2 & 3）

- **+adpw**（加权）：提升困难标签表现（如 V, VA）。
- **+cur/adpcur**（课程学习）：控制更新时机，防止易任务主导。
- **联合使用（adpw + adpcur）**：达到最高性能，说明“**何时更新**”与“**如何加权**”互补。

#### （3）泛化能力验证（Table 4）

| 训练数据 | MAPLE-QA+ 平均准确率 |
|----------|--------------------|
| MAPLE-QA (exact) | 51.90% |
| **MAPLE-QA+ (augmented)** | **76.99%** |

> 🎯 模型不仅能处理 exact 情况，还能在 superset/deficit 下正确判断是否可答，证明其具备真正的 **modality-aware reasoning** 能力。

#### （4）对比学习增强（CRW）

在 captioning 中引入 **Contrastive Reward Weighting (CRW)**：
- 鼓励完整模态下的输出与缺失模态下的输出在语义空间分离。
- 结果：
  - Intra-group dispersion ↑（多样性提升）
  - Inter-group separation ↑（模态条件更强）
  - t-SNE 显示聚类更清晰（图16）

---

## 4. 关键结论和发现

### 主要发现

1. **模态异构性是 RL post-training 的关键挑战**  
   不同 RMT 的任务具有不同的奖励分布、噪声水平和学习动态，统一处理会引入不必要的方差。

2. **分层训练（stratified training）有效降低梯度方差**  
   MAPO 通过按 RMT 分组归一化优势函数，显著提升训练稳定性与效率。

3. **自适应机制进一步缓解“易难失衡”问题**  
   困难任务梯度弱，容易被掩盖；通过 **KL-based weighting** 和 **dynamic curriculum** 可主动补偿。

4. **MAPLE 实现了高效、鲁棒、真实的多模态融合**  
   - 收敛更快（3.18×）
   - 模态差距缩小 30.24%
   - 在部分观测下仍保持高性能
   - 能主动 abstain 当信息不足

5. **benchmark 设计至关重要**  
   现有 benchmark 忽略模态依赖关系，MAPLE-bench 引入 RMT 标注，使评估更具现实意义。

### 方法的局限性

- **依赖高质量 RMT 标注**：若 RMT 错误标注，可能导致训练偏差。
- **当前仅适用于离散模态组合**：难以处理连续模态权重变化。
- **计算开销略高**：虽然总时间减少，但需维护多个 KL 历史与调度逻辑。
- **对 reward model 敏感**：尤其在 captioning 中依赖 LLM-as-Judge 的一致性。

### 未来工作方向

1. **自动化 RMT 推断**：开发无需人工标注即可推断最小必要模态的方法。
2. **在线 curriculum learning**：结合 uncertainty estimation 动态调整训练顺序。
3. **扩展至更多模态**：如触觉、位置、传感器数据等。
4. **应用于 real-world agents**：如机器人、智能助手，在动态环境中自适应模态使用。
5. **结合 test-time adaptation**：在推理时根据可用模态自动切换策略。

---

> **总结一句话**：  
> MAPLE 揭示了 **“模态分层优化”** 是实现高效、鲁棒多模态 RL 的关键，提出了从 benchmark 到算法再到训练策略的完整解决方案，为下一代 **deployment-ready multimodal systems** 奠定了基础。

</details>

---

### 14. [LAER-MoE: Load-Adaptive Expert Re-layout for Efficient Mixture-of-Experts Training](https://arxiv.org/abs/2602.11686)

**Authors**: Xinyi Liu, Yujie Wang, Fangcheng Fu, Xuefeng Xiao, Huixia Li, Jiashi Li, Bin Cui  
**Category**: cs.DC  
**Published**: 2026-02-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.11686v1  

#### Abstract
Expert parallelism is vital for effectively training Mixture-of-Experts (MoE) models, enabling different devices to host distinct experts, with each device processing different input data. However, during expert parallel training, dynamic routing results in significant load imbalance among experts: ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LAER-MoE: Load-Adaptive Expert Re-layout for Efficient Mixture-of-Experts Training

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在 **Mixture-of-Experts (MoE)** 模型的分布式训练中，**expert parallelism (EP)** 虽然能有效缓解单设备内存压力，但由于动态路由（dynamic routing）导致专家负载严重不均衡（load imbalance），形成“长尾延迟”（tail latency），严重影响训练效率。

现有系统级解决方案如 **expert replication**（复制高负载专家）和 **expert relocation**（迁移专家位置）虽然能改善负载，但会引入显著的 **re-layout 开销**（通信和内存开销），限制了其调整频率和灵活性，无法及时响应路由分布的快速变化。

---

### 提出的新方法与新思路

论文提出 **LAER-MoE**，一个高效的 MoE 训练框架，其核心是两个创新：

#### （1）**Fully Sharded Expert Parallelism (FSEP)**  
一种全新的并行范式，结合了 **FSDP (Fully Sharded Data Parallelism)** 和 **EP** 的思想：
- 将每个 **expert 的参数完全分片（fully shard）**，每个设备仅存储所有专家的一个分片（chunk）。
- 在前向/反向传播时，通过 **All-to-All 通信** 动态恢复所需专家的完整参数。
- **关键设计**：将 expert re-layout 过程与 FSDP 的 **parameter prefetching** 和 **gradient reduce-scattering** 融合，使得 re-layout 的通信开销可以被计算过程完美掩盖（overlap），从而实现 **每轮迭代都可灵活重布局**，且无额外时间成本。

#### （2）**智能负载均衡规划器（Intelligent Load Balancing Planner）**
- **异步专家布局调优器（asynchronous expert layout tuner）**：基于历史路由信息，在 CPU 上为下一轮迭代生成最优的专家副本数量和放置策略。
- **同步 token 分发器（synchronous token dispatcher）**：在 GPU 上快速决定每个 token 应路由到哪个专家副本，采用轻量级拓扑感知的 **lite routing 算法**，优先减少跨节点通信。

---

### 相比现有方法的优势

| 方面 | 现有方法（如 FlexMoE, SmartMoE） | LAER-MoE |
|------|-------------------------------|----------|
| **re-layout 频率** | 低频（数百轮一次） | **每轮迭代均可调整** |
| **re-layout 开销** | 显著（需迁移参数/梯度，峰值内存高） | **通信开销被计算掩盖，无额外延迟** |
| **负载均衡能力** | 受限于调整策略和开销惩罚 | **更灵活、更及时、更优的均衡效果** |
| **系统兼容性** | 通常独立于并行范式 | **与 FSDP 深度集成，支持 fully sharded model state** |

---

## 2. 核心实验方法和设置

### 数据集
- **WikiText-103**：维基百科文本，用于语言建模。
- **C4**：Common Crawl 清洗后的大型语料库。

### 模型架构
在以下三种 MoE 模型上进行实验：
- **Mixtral-8x7B**
- **Mixtral-8x22B**
- **Qwen-8x7B**

配置包括：
- **e8k2**：8 个专家，top-2 路由
- **e16k4**：16 个专家，top-4 路由（保持总计算量不变）

### 硬件环境
- **4 节点集群**，每节点 8 块 **NVIDIA A100 80GB GPU**
- 节点内通过 **NVLink** 互联（300 GB/s）
- 节点间通过 **Infiniband** 互联（800 Gbps）

### 基线方法对比
- **Megatron-LM**：支持异构并行的 SOTA MoE 训练框架。
- **FSDP+EP**：基于 PyTorch FSDP 扩展的 baseline，MoE 层同时使用 FSDP 和 EP。
- **FlexMoE**：当前最先进的动态专家放置方法（作者复现其调度器并与 FSEP 结合对比）。

### 评估指标
- **端到端训练速度（Throughput, tokens/s）**
- **加速比（Speedup）** vs. Megatron 和 FSDP+EP
- **收敛曲线（Loss vs. Time/Steps）**
- **All-to-All 通信占比**
- **最大设备 token 数（衡量负载均衡程度）**
- **消融实验**：验证 FSEP 通信优化和规划器的有效性

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- LAER-MoE 在所有测试场景下均优于基线方法。
- **最高达到 1.69× 加速比**（vs. Megatron），平均加速 **1.50×**（vs. FSDP+EP）。
- 与 FlexMoE 相比，**最高提升 1.39×，平均 1.20×**。

### 与基线方法对比结果
| 场景 | LAER-MoE vs. Megatron | LAER-MoE vs. FSDP+EP |
|------|------------------------|------------------------|
| e8k2 模型 | 平均 ~1.4–1.6× | 略优或持平 |
| e16k4 模型 | 显著优势（~1.6×） | 略逊于 Megatron，但 LAER-MoE 仍最优 |
| 含辅助损失（auxiliary loss） | 保持高性能，收敛速度最快 |

> **原因分析**：
> - FSDP+EP 在 e8k2 上表现较好，因其 fully sharded 特性节省内存，允许更大 micro-batch。
> - Megatron 在 e16k4 上因参数较少，可使用更小的 TP，效率更高。
> - 但两者均受 **负载不均衡** 影响，而 LAER-MoE 通过动态 re-layout 显著缓解此问题。

### 消融实验结果（Ablation Study）
- **移除通信优化**（no comm opt）：性能下降明显，证明 **prefetching 与 gradient sync 的精细调度至关重要**。
- **单一副本分配策略**（only 'pq' 或 'even'）：性能不稳定，说明 **多策略组合搜索（multiple replica schemes）能更好适应不同路由模式**。
- **规划器效率**：lite routing 占用时间 < 0.1%，专家布局求解时间远低于单层计算时间，**不会成为瓶颈**。

---

## 4. 关键结论和发现

### 主要发现
1. **FSEP 成功解耦了 re-layout 开销与训练效率**，实现了 **zero-overhead 的动态专家重布局**。
2. **每轮迭代的负载自适应调整** 显著优于低频调整方法，在动态路由场景下更具鲁棒性。
3. LAER-MoE 在 **保持模型收敛质量** 的前提下，大幅提升训练吞吐，**无需依赖高权重的辅助损失函数** 来强制均衡。
4. 其优势在 **专家数更多（e16k4）** 的模型上更为显著，因提供了更大的优化空间。

### 方法的局限性
- 当前实现依赖于对 FSDP 的深度定制，集成复杂度较高。
- 规划器虽高效，但在超大规模集群（如 >128 GPUs）上的扩展性仍需进一步验证（尽管模拟显示稳定）。
- 对极端稀疏或高度偏斜的路由模式未做专门优化。

### 未来工作方向
- 设计更高效的 **planner 算法**，探索强化学习等方法进行全局优化。
- 支持 **异构硬件集群**（如混合 GPU 类型）下的专家布局。
- 将 FSEP 思想推广至其他稀疏模型结构（如 activation sparsity）。
- 探索 **训练-推理一致性**，使推理时也能受益于训练期的负载均衡策略。

---

> **代码开源**：`https://github.com/PKUDAIR/Hetu-Galvatron/tree/laer-moe`

</details>

---

### 15. [CADET: Context-Conditioned Ads CTR Prediction With a Decoder-Only Transformer](https://arxiv.org/abs/2602.11410)

**Authors**: David Pardoe, Neil Daftary, Miro Furtado, Aditya Aiyer, Yu Wang, Liuqing Li, Tao Song, Lars Hertel, Young Jin Yun, Senthil Radhakrishnan, Zhiwei Wang, Tommy Li, Khai Tran, Ananth Nagarajan, Ali Naqvi, Yue Zhang, Renpeng Fang, Avi Romascanu, Arjun Kulothungun, Deepak Kumar, Praneeth Boda, Fedor Borisyuk, Ruoyan Wang  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.11410v1  

#### Abstract
Click-through rate (CTR) prediction is fundamental to online advertising systems. While Deep Learning Recommendation Models (DLRMs) with explicit feature interactions have long dominated this domain, recent advances in generative recommenders have shown promising results in content recommendation. H...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**CADET: Context-Conditioned Ads CTR Prediction With a Decoder-Only Transformer**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对在工业级广告系统中应用 **decoder-only Transformer** 进行 **CTR 预测** 所面临的四大挑战：

1. **Post-scoring context 依赖问题**：如广告位置（position）等上下文信号在推理时不可见，但在训练时可用，导致训练-服务不一致（train-serve skew）。
2. **Offline-Online 不一致性**：由于系统延迟、会话内事件不可观测等问题，模型可能在训练中“偷看”推理时无法获取的信息。
3. **训练不稳定性和可扩展性差**：大规模序列建模下，标准 Transformer 容易出现注意力病态行为（attention sink）、梯度爆炸等问题。
4. **高计算开销**：长序列处理带来 $O(n^2)$ 的 self-attention 开销，难以满足低延迟在线服务需求。

---

### 🚀 提出的新方法与创新点

CADET（**Context-Conditioned Ads Decoder-Only Transformer**）提出了一系列架构与工程层面的创新：

#### （1）**Context-Conditioned Decoding Architecture**
- 引入多塔预测头（multi-tower prediction heads），每个 head 对应一个 post-scoring context bucket（如不同 ad position 区间）。
- 在训练时仅使用真实 context bucket 的 head 参与 loss 计算；在推理时并行输出所有 context 下的 CTR 预测。
- **优势**：一次性解决“CTR 预测 → 排序 → 位置确定”的鸡生蛋问题，无需迭代重排序或多轮打分。

#### （2）**Self-Gated Attention Mechanism**
- 在 **representation-level** 和 **interaction-level** 引入自门控机制（gating on Q/K 输入前）。
- 抑制噪声维度、调节 query-key 相互作用强度，缓解 attention sink 现象。
- **优势**：显著提升训练稳定性，避免 AUC 波动，增强收敛可靠性。

#### （3）**Timestamp-based RoPE（Rotary Position Embedding）**
- 将原始基于序列位置的 RoPE 改为基于 **Unix 时间戳** 的旋转编码。
- 支持跨时间尺度建模（从秒到月），捕捉用户行为的时间动态性。
- **优势**：更符合广告场景中时间间隔远比顺序重要的特性。

#### （4）**Session-Aware Masking**
- 设计训练专用掩码策略：只允许 token 注意 $\Delta t > \Delta_{\text{delay}}$ 的历史事件（例如保证至少 1 小时延迟）。
- **优势**：防止模型依赖实时未更新的数据，提升 offline-online 一致性。

#### （5）**Production Engineering Optimizations**
- **Tensor Packing**：消除 padding，将多个短序列压缩成连续 token 流，提升 batch 利用率。
- **Sequence Chunking**：将长用户历史切分为固定长度 chunk，降低 attention 成本。
- **Custom FlashAttention Kernel**：专为 multi-item scoring 场景优化，跳过无效计算 tile，实现 3× 推理加速。

---

### 🔍 相比现有方法的优势

| 维度 | 现有主流方法（如 LiRank） | CADET |
|------|--------------------------|--------|
| 架构 | DLRM + Sequential Encoder 混合模型 | 端到端 decoder-only Transformer |
| 特征交互 | 显式设计（DCNv2, DIN） | 隐式学习时序模式 |
| 上下文建模 | debiasing / counterfactual learning | context-conditioned 多塔 head |
| 时间建模 | 序列位置或 RNN 结构 | Timestamp-RoPE |
| 工程效率 | 单独打分候选 | 多候选一次 forward pass |
| 系统复杂度 | 多模块集成 | 统一模型，简化 pipeline |

> ✅ **核心优势**：**统一架构 + 更强表达能力 + 更好工程效率 + 更高业务指标**

---

## 2. 核心实验方法和设置

### 📊 数据集
- 来源：**LinkedIn 赞助内容广告系统（Homefeed Sponsored Updates）**
- 数据形式：用户级行为序列（interleaved impression-action sequence）
- 时间范围：**一年历史数据**，最后一天用于验证
- 特征数量：从传统 >100 个特征精简至 **12 个 high-utility signals**

---

### ⚙️ 实验设置

| 参数 | 设置 |
|------|------|
| 模型结构 | `nlayers=8`, `nheads=4`, `d_model=352`, `seq_len=4096` |
| 训练框架 | PyTorch + HSDP（Hybrid Sharded Data Parallel） |
| GPU | NVIDIA H200 |
| 优化器 | AdamW |
| Batch Size | 使用 packing 后有效 batch size 提升约 4× |
| $\Delta_{\text{delay}}$ | 1 小时（用于 session masking） |

---

### 📈 评估指标

| 类型 | 指标 |
|------|------|
| Offline | **ROC AUC**, **AAUC（相对 AUC 变化）**, 消融实验 |
| Online | **A/B Test 中的 CTR 提升**, **Revenue 变化** |
| 效率 | 训练成本（TFLOPs）、推理延迟（p99 < 50ms）、吞吐量（req/sec/GPU） |

---

### 🆚 基线方法对比

- **Control Baseline**: **LiRank [3]**  
  - 当前生产系统，hybrid ensemble 模型
  - 包含 **DCNv2**（用于特征交叉） + **TransAct**（用于序列建模）
- **Treatment Group**: 替换为完整的 **CADET 模型**

> 实验目标：验证单一 generative recommender 是否能超越高度优化的多组件混合系统。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### ✅ **Online A/B Test 结果（表 3）**

| Metric       | Relative Change |
|------------|------------------|
| **CTR**     | **+11.04%** ↑↑↑ |
| **Revenue** | +0.14% ↑         |

> 💡 **说明**：尽管收入提升较小，但 **CTR 显著增长 11.04%** 是巨大突破，尤其面对已高度优化的 baseline。

---

#### 🔍 **消融实验结果（表 1）**

| Model Variant             | Δ AUC (vs Full CADET) |
|---------------------------|------------------------|
| Full CADET                | —                     |
| - Context-Conditioned     | **-0.61%**            |
| - Temporal RoPE           | -0.13%                |
| - Session Masking         | -0.31%                |
| - Pairwise Loss           | -0.03%                |

> 🔎 **分析**：
> - **Context-conditioned decoding** 影响最大，说明 post-scoring context 建模至关重要。
> - **Session masking** 贡献第二，证明 train-serve skew 是实际瓶颈。
> - Pairwise loss 虽小但稳定，有助于对抗曝光偏差（exposure bias）。

---

#### ⚖️ **准确率-效率权衡（表 2）**

| Sequence Length | Training Cost | Δ AUC     |
|------------------|---------------|-----------|
| 4096 (baseline)  | 1.0×          | —         |
| 2048             | **0.46×**     | -0.04%    |
| 1024             | **0.29×**     | -0.29%    |

> ✅ **结论**：通过 **chunking** 可大幅降低训练成本（↓54% @ 2048），且精度损失极小。

---

#### ⚡ **推理优化效果**

| 指标 | 数值 |
|------|------|
| Baseline fmha kernel | 34 TFLOPS/s |
| **Custom FlashAttention Kernel** | **>100 TFLOPS/s** |
| Attention latency | 792μs → **262μs** (**3× speedup**) |
| Throughput | **330 req/sec/GPU** |
| Latency (p99) | **< 50ms** ✅ |

> ✅ 实现高效批量候选打分，支持线上实时拍卖系统。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Generative Recommenders 可胜过传统 DLRM 混合系统**  
   - 单一 decoder-only Transformer 在工业广告 CTR 任务上 **击败 LiRank +11.04% CTR**，打破“必须 ensemble”的认知。

2. **Context-conditioned decoding 是关键创新**  
   - 成功解耦 “CTR 预测” 与 “最终展示位置” 的循环依赖，提供 policy-ready 输出。

3. **训练稳定性可通过 self-gated attention 显著改善**  
   - representation-level + interaction-level gating 有效抑制 pathological attention 行为。

4. **Timestamp-RoPE 比传统位置编码更适合广告场景**  
   - 时间差比顺序更重要，timestamp-aware 编码能捕捉跨尺度行为模式。

5. **Production Engineering 决定落地成败**  
   - Packing、chunking、custom kernel 等技术使大模型可在工业环境高效运行。

---

### ⚠️ 局限性

1. **对冷启动用户敏感**？  
   - 极短序列用户可能受益有限，因模型依赖历史行为建模。

2. **Context bucket 设计需人工划分**  
   - 如何自动学习最优 context 分桶仍待研究。

3. **Semantic ID Tokenization 效果不佳**  
   - 尝试引入语义 embedding（RO-VAE）仅带来 0.02% AUC 提升，但增加工程复杂度。

4. **Revenue 提升微弱**  
   - CTR 大幅上升但 revenue 几乎不变，可能存在竞价机制或其他因素压制转化价值。

---

### 🔮 未来工作方向

1. **Dynamic Context Bucketing**  
   - 学习自适应 context 分组，而非固定区间。

2. **End-to-End Bidding Integration**  
   - 将 CTR 预测与 bidding 策略联合建模，进一步释放 revenue 潜力。

3. **Long-Term Engagement Modeling**  
   - 引入 dwell time、conversion 等辅助任务，超越点击行为。

4. **更高效的稀疏 attention 设计**  
   - 探索 LLM-inspired sparse attention（如 StreamingLLM）以支持无限上下文。

5. **Cross-domain Generalization**  
   - 验证 CADET 是否适用于其他平台（如电商、社交 feed）。

---

## ✅ 总结

CADET 是首个成功将 **decoder-only Transformer** 应用于工业级广告 CTR 预测的端到端系统。它不仅在 **准确性上取得显著突破（+11.04% CTR）**，还通过一系列 **架构创新与工程优化** 解决了生成式推荐在广告场景下的独特挑战。其成功表明：**统一的 generative paradigm 正在成为下一代推荐系统的有力候选方案**。

</details>

---

### 16. [Improved state mixing in higher-order and block diagonal linear recurrent networks](https://arxiv.org/abs/2602.12021)

**Authors**: Igor Dubinin, Antonio Orvieto, Felix Effenberger  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.12021v1  

#### Abstract
Linear recurrent networks (LRNNs) and linear state space models (SSMs) promise computational and memory efficiency on long-sequence modeling tasks, yet their diagonal state transitions limit expressivity. Dense and nonlinear architectures (e.g., LSTMs) on the other hand are provably more expressive,...

---

### 17. [Stop Unnecessary Reflection: Training LRMs for Efficient Reasoning with Adaptive Reflection and Length Coordinated Penalty](https://arxiv.org/abs/2602.12113)

**Authors**: Zewei Yu, Lirong Gao, Yuke Zhu, Bo Zheng, Sheng Guo, Haobo Wang, Junbo Zhao  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12113v1  

#### Abstract
Large Reasoning Models (LRMs) have demonstrated remarkable performance on complex reasoning tasks by employing test-time scaling. However, they often generate over-long chains-of-thought that, driven by substantial reflections such as repetitive self-questioning and circular reasoning, lead to high ...

---

### 18. [Pedagogically-Inspired Data Synthesis for Language Model Knowledge Distillation](https://arxiv.org/abs/2602.12172)

**Authors**: Bowei He, Yankai Chen, Xiaokun Zhang, Linghe Kong, Philip S. Yu, Xue Liu, Chen Ma  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12172v1  

#### Abstract
Knowledge distillation from Large Language Models (LLMs) to smaller models has emerged as a critical technique for deploying efficient AI systems. However, current methods for distillation via synthetic data lack pedagogical awareness, treating knowledge transfer as a one-off data synthesis and trai...

---

### 19. [CM2: Reinforcement Learning with Checklist Rewards for Multi-Turn and Multi-Step Agentic Tool Use](https://arxiv.org/abs/2602.12268)

**Authors**: Zhen Zhang, Kaiqiang Song, Xun Wang, Yebowen Hu, Weixiang Yan, Chenyang Zhao, Henry Peng Zou, Haoyun Deng, Sathish Reddy Indurthi, Shujian Liu, Simin Ma, Xiaoyang Wang, Xin Eric Wang, Song Wang  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12268v1  

#### Abstract
AI agents are increasingly used to solve real-world tasks by reasoning over multi-turn user interactions and invoking external tools. However, applying reinforcement learning to such settings remains difficult: realistic objectives often lack verifiable rewards and instead emphasize open-ended behav...

---

### 20. [GAC-KAN: An Ultra-Lightweight GNSS Interference Classifier for GenAI-Powered Consumer Edge Devices](https://arxiv.org/abs/2602.11186)

**Authors**: Zhihan Zeng, Kaihe Wang, Zhongpei Zhang, Yue Xiu  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11186v1  

#### Abstract
The integration of Generative AI (GenAI) into Consumer Electronics (CE)--from AI-powered assistants in wearables to generative planning in autonomous Uncrewed Aerial Vehicles (UAVs)--has revolutionized user experiences. However, these GenAI applications impose immense computational burdens on edge h...

---

### 21. [TUBO: A Tailored ML Framework for Reliable Network Traffic Forecasting](https://arxiv.org/abs/2602.11759)

**Authors**: Zhihang Yuan, Leyang Xue, Waleed Ahsan, Mahesh K. Marina  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11759v1  

#### Abstract
Traffic forecasting based network operation optimization and management offers enormous promise but also presents significant challenges from traffic forecasting perspective. While deep learning models have proven to be relatively more effective than traditional statistical methods for time series f...

---

### 22. [In-Context Function Learning in Large Language Models](https://arxiv.org/abs/2602.11863)

**Authors**: Elif Akata, Konstantinos Voudouris, Vincent Fortuin, Eric Schulz  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11863v1  

#### Abstract
Large language models (LLMs) can learn from a few demonstrations provided at inference time. We study this in-context learning phenomenon through the lens of Gaussian Processes (GPs). We build controlled experiments where models observe sequences of multivariate scalar-valued function samples drawn ...

---

### 23. [FedGRPO: Privately Optimizing Foundation Models with Group-Relative Rewards from Domain Client](https://arxiv.org/abs/2602.12014)

**Authors**: Gongxi Zhu, Hanlin Gu, Lixin Fan, Qiang Yang, Yuxing Han  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.12014v1  

#### Abstract
One important direction of Federated Foundation Models (FedFMs) is leveraging data from small client models to enhance the performance of a large server-side foundation model. Existing methods based on model level or representation level knowledge transfer either require expensive local training or ...

---

### 24. [AgentNoiseBench: Benchmarking Robustness of Tool-Using LLM Agents Under Noisy Condition](https://arxiv.org/abs/2602.11348)

**Authors**: Ruipeng Wang, Yuxin Chen, Yukai Wang, Chang Wu, Junfeng Fang, Xiaodong Cai, Qi Gu, Hui Su, An Zhang, Xiang Wang, Xunliang Cai, Tat-Seng Chua  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11348v1  

#### Abstract
Recent advances in large language models have enabled LLM-based agents to achieve strong performance on a variety of benchmarks. However, their performance in real-world deployments often that observed on benchmark settings, especially in complex and imperfect environments. This discrepancy largely ...

---

### 25. [AgentLeak: A Full-Stack Benchmark for Privacy Leakage in Multi-Agent LLM Systems](https://arxiv.org/abs/2602.11510)

**Authors**: Faouzi El Yagoubi, Ranwa Al Mallah, Godwin Badu-Marfo  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11510v1  

#### Abstract
Multi-agent Large Language Model (LLM) systems create privacy risks that current benchmarks cannot measure. When agents coordinate on tasks, sensitive data passes through inter-agent messages, shared memory, and tool arguments; pathways that output-only audits never inspect. We introduce AgentLeak, ...

---

### 26. [Charting Empirical Laws for LLM Fine-Tuning in Scientific Multi-Discipline Learning](https://arxiv.org/abs/2602.11215)

**Authors**: Lintao Wang, Zhuqiang Lu, Yilin Zhu, Kun Hu, Zhenfei Yin, Shixiang Tang, Zhiyong Wang, Wanli Ouyang, Xinzhu Ma  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11215v1  

#### Abstract
While large language models (LLMs) have achieved strong performance through fine-tuning within individual scientific domains, their learning dynamics in multi-disciplinary contexts remains poorly understood, despite the promise of improved generalization and broader applicability through cross-domai...

---

### 27. [WSBD: Freezing-Based Optimizer for Quantum Neural Networks](https://arxiv.org/abs/2602.11383)

**Authors**: Christopher Kverne, Mayur Akewar, Yuqian Huo, Tirthak Patel, Janki Bhimani  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11383v1  

#### Abstract
The training of Quantum Neural Networks (QNNs) is hindered by the high computational cost of gradient estimation and the barren plateau problem, where optimization landscapes become intractably flat. To address these challenges, we introduce Weighted Stochastic Block Descent (WSBD), a novel optimize...

---

### 28. [Temperature as a Meta-Policy: Adaptive Temperature in LLM Reinforcement Learning](https://arxiv.org/abs/2602.11779)

**Authors**: Haoran Dang, Cuiling Lan, Hai Wan, Xibin Zhao, Yan Lu  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11779v1  

#### Abstract
Temperature is a crucial hyperparameter in large language models (LLMs), controlling the trade-off between exploration and exploitation during text generation. High temperatures encourage diverse but noisy outputs, while low temperatures produce focused outputs but may cause premature convergence. Y...

---

### 29. [Improving HPC Code Generation Capability of LLMs via Online Reinforcement Learning with Real-Machine Benchmark Rewards](https://arxiv.org/abs/2602.12049)

**Authors**: Ryo Mikasa, Shun-ichiro Hayashi, Daichi Mukunoki, Tetsuya Hoshino, Takahiro Katagiri  
**Category**: cs.LG  
**Published**: 2026-02-13  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.12049v1  

#### Abstract
Large language models (LLMs) have demonstrated strong code generation capabilities, yet the runtime performance of generated code is not guaranteed, and there have been few attempts to train LLMs using runtime performance as a reward in the HPC domain. We propose an online reinforcement learning app...

---

### 30. [Latent Generative Solvers for Generalizable Long-Term Physics Simulation](https://arxiv.org/abs/2602.11229)

**Authors**: Zituo Chen, Haixu Wu, Sili Deng  
**Category**: cs.AI  
**Published**: 2026-02-13  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.11229v1  

#### Abstract
We study long-horizon surrogate simulation across heterogeneous PDE systems. We introduce Latent Generative Solvers (LGS), a two-stage framework that (i) maps diverse PDE states into a shared latent physics space with a pretrained VAE, and (ii) learns probabilistic latent dynamics with a Transformer...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
