# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-15 05:58:03 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [xGR: Efficient Generative Recommendation Serving at Scale](https://arxiv.org/abs/2512.11529)

**Authors**: Qingxiao Sun, Tongxuan Liu, Shen Zhang, Siyu Wu, Peijun Yang, Haotian Liang, Menxin Li, Xiaolong Ma, Zhiwei Liang, Ziyi Ren, Minchao Zhang, Xinyu Liu, Ke Zhang, Depei Qian, Hailong Yang  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.11529v1  

#### Abstract
Recommendation system delivers substantial economic benefits by providing personalized predictions. Generative recommendation (GR) integrates LLMs to enhance the understanding of long user-item sequences. Despite employing attention-based architectures, GR's workload differs markedly from that of LL...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：xGR: Efficient Generative Recommendation Serving at Scale

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**生成式推荐系统（Generative Recommendation, GR）在高并发、低延迟场景下的推理服务瓶颈**提出了系统性的优化方案。尽管GR借鉴了LLM的架构（如Transformer），但其工作负载特性与传统LLM有显著差异，导致直接采用现有的LLM推理引擎（如vLLM）效率低下。

具体挑战包括：
- **长输入提示、短输出**：用户行为序列可能长达数千token，但只需生成少量token表示推荐物品ID。
- **大规模Beam Search开销大**：为保证推荐多样性，需使用大Beam Width（如512）和Top-K筛选，带来巨大的排序与内存管理开销。
- **共享前缀未被有效利用**：多个beam共享相同的prompt上下文，但现有方法重复加载KV Cache，造成内存带宽浪费。
- **无效item生成严重**：Token组合空间巨大，许多组合不对应真实商品，需高效过滤机制。
- **系统级并行度不足**：host-device间调度开销占比高，难以满足严格SLO（P99 < 200ms）。

---

### 提出的新方法与创新思路

作者提出 **xGR** ——一个面向生成式推荐的端到端高性能推理服务系统，从**算子层、算法层、系统层**三个维度进行重构：

#### （1）xAttention：分离式KV缓存 + 分阶段计算
- 将KV Cache划分为 **Shared Cache**（来自prefill阶段的prompt）和 **Unshared Cache**（decode阶段生成的token）。
- 在attention计算中分阶段处理：先独立计算shared部分，再融合unshared部分，通过`OnlineSoftmax`合并结果。
- 实现细粒度load balancing，并减少冗余KV读取。

#### （2）xBeam：高效的beam search优化
- **Valid Path Constraint**：引入item mask，在logits上屏蔽非法token组合，防止“幻觉”item生成。
- **Early Sorting Termination**：维护大小为BW的min-heap，一旦某beam当前log_prob低于堆顶，则提前终止该beam的遍历，大幅降低排序复杂度。
- **Data Structure Reuse**：复用旧beam的数据结构存储新候选序列，避免频繁分配/释放带来的开销。

#### （3）xSchedule：多层级流水线调度
- 构建三层架构：**Scheduler → Engine → Worker**。
- 支持**跨请求、批内、请求内部的多级重叠执行**：
  - Host-side预处理（如mask生成）与device-side forward pass重叠；
  - H2D传输与self-attention计算重叠；
  - 多Stream并发处理不同batch，提升加速器利用率。
- 动态批处理 + 图提交（kernel graph dispatch）以降低kernel launch开销。

---

### 相比现有方法的优势

| 维度 | 现有方法（如vLLM/xLLM） | xGR |
|------|--------------------------|-----|
| KV管理 | PagedAttention，重复加载共享prefix | 分离KV cache，物理上只存一份shared cache |
| 内存效率 | Block copy频繁，碎片化严重 | 避免block copy，token粒度管理unshared cache |
| 排序开销 | 全局Top-BW排序，O(BW×K) | Early termination + heap剪枝 |
| 过滤机制 | 动态host-side过滤或静态全量mask | 混合稀疏/稠密mask，设备端集成filtering |
| 调度模型 | 单stream串行执行 | Multi-stream并行 + 流水线overlap |

> ✅ 总体优势：在保持P99延迟≤200ms的前提下，实现更高的吞吐量和更低的资源消耗。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Amazon Review**：公开推荐任务基准数据集，用于标准化评估。
- **JD Trace**：来自京东生产环境的真实流量trace，包含动态请求模式，更具现实意义。

### 实验设置
- **硬件平台**：
  - **Ascend集群**：64节点 × 16 Ascend NPU（64GB）
  - **GPU集群**：8节点 × 8 NVIDIA H800 GPU（80GB），NVLink互联
- **模型**：
  - **Qwen3**（0.6B ~ 4B参数）
  - **OneRec**（0.1B ~ 3B参数）——典型的生成式推荐模型
- **Beam Width（BW）测试范围**：{128, 256, 512}
- **Batch Size自适应调整**，受SLO约束控制最大等待时间。

### 评估指标
- **平均延迟（Average Latency）**
- **P99延迟（P99 Latency）**
- **吞吐量（Throughput, RPS）**
- **峰值内存占用（Peak Memory Usage）**
- **Kernel级性能分析**（latency, compute throughput, memory bound程度）

### 基线方法对比
- **vLLM**：基于PagedAttention的经典LLM推理引擎
- **xLLM**：工业级推理框架，支持Ascend NPU，也使用PagedAttention机制

> 注：vLLM不支持OneRec模型，故部分实验仅与xLLM比较。

---

## 3. 主要实验结果和性能指标

### 吞吐量提升（核心成果）
> 在严格P99 ≤ 200ms的SLO下，**xGR相比state-of-the-art基线实现了至少3.49×的吞吐量提升**。

#### 图13 & 图14（Ascend集群）显示：
- 所有配置下，xGR均能维持平稳延迟增长，而基线在高RPS时迅速突破SLO。
- 当Beam Width增大至512时，xLLM/vLLM延迟急剧上升，几乎无法运行；而xGR仍可稳定服务。
- 对于Qwen3-4B模型，xGR在高负载下仍可达数倍于基线的RPS。

#### 图19（GPU集群）表明：
- 即使在高端H800 GPU上，vLLM表现依然较差。
- xGR在多种模型规模和beam width下持续领先，证明其设计具有**跨硬件平台的可移植性和有效性**。

---

### 内存效率对比（图15 & 图16）
- **固定输入长度=1k，BW=512时**：
  - xGR峰值内存 ≈ **10.6 GB**
  - xLLM峰值内存 ≈ **46.3 GB**（超4倍）
- **固定BW=256，输入长度增至3k时**：
  - xGR峰值内存 ≈ **12.0 GB**
  - xLLM ≈ **30 GB**

> 💡 结论：xGR成功解耦内存使用与序列长度，适用于长上下文推荐场景。

---

### Kernel级性能分析（图17）
- **Latency**：xAttention在大beam width下比PagedAttention快约 **6.6×**
- **Compute Throughput**：提升达 **7×**
- **Memory Bound程度**：
  - PagedAttention：93.4%时间处于memory busy状态 → 明显内存瓶颈
  - xAttention：仅52% busy率 → 成功转为compute-bound，硬件利用率更高

---

### 消融实验（图18）
在OneRec-0.1B + Amazon Review上的ablation study：
- **Baseline（仅xAttention+xBeam）**：随RPS增加延迟快速上升
- **+ Valid Item Filtering**：延迟略有下降，说明设备端filtering无额外负担
- **+ Kernel Graph Dispatch**：显著降低kernel launch开销
- **+ Multi-stream Execution**：实现最佳性能，支持高并发下的pipeline overlap

> 🔍 发现：multi-stream是应对轻量模型host overhead的关键。

---

## 4. 关键结论和发现

### 主要发现
1. **生成式推荐的工作负载与LLM存在本质差异**，不能简单套用现有LLM推理系统。
2. **共享prefix的KV cache重用是性能突破口**，xAttention通过分离cache结构实现高效复用。
3. **大规模beam search中的排序与过滤是主要瓶颈**，xBeam通过early termination和masking实现高效搜索。
4. **系统级overlap至关重要**，尤其对于小模型而言，host-side调度开销不可忽视。
5. **xGR可在真实生产环境中部署**：已上线三个月，服务于数亿用户，P99稳定在200ms以内，峰值RPS达万级。

---

### 方法的局限性
- **假设decode步数固定**：目前针对生成固定长度item ID（如三token编码）设计，对变长输出支持有限。
- **依赖预定义valid item vocabulary**：需要维护合法token路径映射表，构建成本较高。
- **当前优化集中在inference阶段**，未涉及训练或冷启动问题。
- **multi-stream调度策略依赖实时负载感知**，在极端波动流量下可能需更智能的调度器。

---

### 未来工作方向
- 支持**动态beam width调整**，根据query难度自适应搜索强度。
- 引入**KV Cache压缩与reuse across requests**，进一步降低prefill开销。
- 探索**编译器级优化**，将整个生成流程编译为静态DAG以最大化性能。
- 扩展至**多模态生成式推荐**（如图文混合生成）场景的服务优化。

---

## 总结

✅ **xGR是首个全面重构生成式推荐推理栈的系统**，从operator、algorithm到system层面协同优化，解决了GR特有的长prompt、大beam、高并发、低延迟等挑战。

🚀 实验验证其在真实数据和硬件上相较vLLM/xLLM取得**3.49×以上的吞吐提升**，同时大幅降低内存占用和延迟敏感性，具备强工程落地能力。

🎯 核心思想：**识别领域特异性 workload特征，并针对性地进行软硬协同设计**，而非盲目沿用通用LLM方案。

</details>

---

### 2. [RollMux: Phase-Level Multiplexing for Disaggregated RL Post-Training](https://arxiv.org/abs/2512.11306)

**Authors**: Tianyuan Wu, Lunxi Cao, Yining Wei, Wei Gao, Yuheng Zhao, Dakai An, Shaopan Xiong, Zhiqiang Lv, Ju Huang, Siran Yang, Yinghao Yu, Jiamang Wang, Lin Qu, Wei Wang  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.11306v1  

#### Abstract
Rollout-training disaggregation is emerging as the standard architecture for Reinforcement Learning (RL) post-training, where memory-bound rollout and compute-bound training are physically disaggregated onto purpose-built clusters to maximize hardware efficiency. However, the strict synchronization ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ROLLMUX: Phase-Level Multiplexing for Disaggregated RL Post-Training

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在大规模语言模型（LLM）的强化学习后训练（RL post-training）中，**rollout-training disaggregation** 已成为标准架构，即将内存密集型的 rollout 阶段与计算密集型的 training 阶段分别部署在专用集群上以提升硬件效率。然而，由于 on-policy 算法要求严格的同步性，两个阶段必须串行执行，导致一个集群空闲等待另一个完成，形成严重的“**依赖气泡**”（dependency bubbles），造成资源利用率低下。

现有异步方法（如 AReaL、StreamRL、AsyncFlow）虽能提高利用率，但引入样本过期（sample staleness），损害模型收敛性和准确性，不适用于对策略一致性要求高的任务。

---

### **提出了什么新方法或新思路**

论文提出 **ROLLMUX**，一种面向解耦式 RL 后训练的跨集群调度框架，通过**相位级多路复用**（phase-level multiplexing）来回收这些“依赖气泡”。

其核心创新包括：

- **共执行组抽象**（Co-Execution Group）：
  将多个 RL 作业组织成“共执行组”，每个组共享一组 rollout 和 training 资源池。该抽象将全局调度问题分解为可管理的子问题，并作为**局部性域**（locality domain），确保模型状态保留在主机内存中，支持“热启动”上下文切换。

- **两级调度架构**：
  - **组间调度器**（Inter-Group Scheduler）：基于保守的随机规划（conservative stochastic planning），决定新作业应加入哪个共执行组或新建组，目标是最小化边际资源开销，同时满足 SLO 和内存约束。
  - **组内调度器**（Intra-Group Scheduler）：采用**轮转调度**（round-robin），证明在非饱和组中是资源利用率最优的策略。

- **长尾迁移机制**（Long-Tail Migration）：
  动态识别 rollout 中的“长尾请求”（straggler），将其迁移到少量设备上继续执行，释放其余 GPU 给下一个作业，实现细粒度流水线并提升吞吐。

- **拓扑感知模型同步**（Topology-Aware Model Sync）：
  在跨集群同步时，采用两阶段更新：先通过并行 P2P 流将模型分片传输到目标集群的一个节点，再利用高速本地网络（如 NVLink/InfiniBand）进行广播，避免所有节点都从慢速跨集群链路下载完整模型。

- **相位为中心的控制模型**（Phase-Centric Control）：
  将 rollout 和 training 视为独立的可调度实体，暴露作业内部依赖图，支持精细调度和快速上下文切换。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **资源利用率** | 显著减少 rollout 和 training 集群的空闲时间，提升整体利用率。 |
| **成本效率** | 相比传统解耦方案节省高达 1.84× 成本，相比 co-located 基线节省 1.38×。 |
| **SLO 保证** | 所有作业均满足性能 SLO（100% SLO attainment）。 |
| **兼容性** | 支持任意 on-policy RL 算法（如 PPO、GRPO、DAPO），且与参数重分配、推测解码等技术正交可组合。 |
| **实用性** | 在真实生产规模测试平台上验证，具备工程可行性。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- 实验基于真实生产环境采集的 **two-week RL post-training trace**，包含 200 个异构作业。
- 模型范围：3B–32B 参数（Qwen 系列）
- 数据集涵盖数学推理（DeepMath-103K）、软件工程（SWE-Bench）、游戏环境（FrozenLake）等。
- 微基准测试使用 5 类代表性作业（Table 3），覆盖单轮与多轮交互模式。

---

### **实验设置和评估指标**

#### **硬件配置**
- **Rollout Cluster**：328 × NVIDIA H20 GPUs（低成本、高带宽）
- **Training Cluster**：328 × NVIDIA H800 GPUs（高性能、高算力）
- 集群间通过 20 Gbps Ethernet 连接，集群内部为 400 Gbps InfiniBand。

#### **评估指标**
- **总资源开销成本**（Total Provisioning Cost, $/h）
- **每单位成本吞吐量**（Per-Cost Throughput）
- **SLO 达成率**（SLO Attainment Rate）
- **依赖气泡减少比例**
- **上下文切换延迟**
- **模型同步时间**

---

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **Solo Disaggregation (Solo-D)** | 标准解耦架构，每个作业独占 rollout 和 training 资源，无时间复用。 |
| **veRL [41]** | 单体架构，在高性能 H800 集群上 co-locate 所有阶段，避免网络瓶颈但存在硬件错配。 |
| **Gavel+ [29]** | 改进版异构感知调度器，优化 GPU 分配比例，但无法进行相位级交错调度。 |
| **Offline Optimal (Opt)** | 理论最优（暴力搜索），用于衡量调度质量上限。 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

- **成本效率提升**：
  - 相比 **Solo-D**：**1.84× 更低的成本**（$510/h vs $940/h）
  - 相比 **veRL**：**1.38× 更低的成本**
- **SLO 达成率**：**100%**，所有作业均满足指定性能目标。
- **资源使用峰值降低**：
  - **Training GPUs**：峰值从 328 降至 **152**（↓2.16×）
  - **Rollout GPUs**：峰值从 328 降至 **216**（↓1.52×）

---

### **与基线方法的对比结果**

| 场景 | ROLLMUX vs Solo-D | vs Gavel+ | vs veRL |
|------|-------------------|----------|--------|
| 时间复用（Type-A×2） | **+82%** 成本效率 | +55.6% | +46.8% |
| 训练复用（Type-D×2+E） | **+104%** | +61.9% | +29.9% |
| 空间复用（Type-C+D×2） | **+111%** | +85.1% | +66.1% |

> 图 13 显示，在真实 trace 回放中，ROLLMUX 显著降低了 GPU 数量和总成本。

---

### **消融实验结果**

#### **长尾迁移（Long-Tail Migration）**
- 对于 14B-8K 模型，启用迁移后端到端吞吐提升 **1.28×**。
- 效果随输出长度增加而增强，尤其在长序列生成场景下显著。

#### **拓扑感知模型同步**
- 单节点同步（8→8 GPUs）：比 veRL 快 **7.87×–8.33×**
- 多节点同步（16→16 GPUs）：仍快 **2.62×–2.75×**
- 有效缓解了解耦架构中的跨集群通信瓶颈。

#### **上下文切换延迟**
- 冷启动（Cold Start）：高达 **80 秒**
- 热启动（Warm Start）：降至 **1.2–1.9 秒**（加速约 48×）
- 使细粒度时间复用成为可能。

---

## 4. 关键结论和发现

### **主要发现**

1. **依赖气泡是解耦 RL 架构的主要效率瓶颈**，即使使用更便宜的 H20 做 rollout，因空闲等待反而导致总成本高于 co-located 方案。
2. **相位级多路复用是解决该问题的有效途径**：通过将一个作业的 rollout 与另一个作业的 training 并行执行，可以“编织”出高效的时间复用模式。
3. **轮转调度在非饱和组中是利用率最优的**，且易于实现和维护。
4. **保守的组间调度 + 动态的组内适应** 可兼顾 SLO 保证与运行时灵活性。
5. **热启动机制至关重要**：若不能将模型状态保留在主机内存中，上下文切换开销将完全抵消复用带来的收益。

---

### **方法的局限性**

- **组大小受限于主机内存容量**：当前最多支持 2–5 个作业共存于同一组，限制了打包灵活性。
- **调度决策依赖准确的最坏情况估计**：虽然保守规划保障了 SLO，但也可能导致资源预留过度。
- **未处理作业优先级或抢占机制**：假设所有作业平等，缺乏对高优先级任务的支持。
- **扩展性依赖于调度器轻量化设计**：尽管实测决策延迟仅 ~600ms（2000 作业），但在更大规模下仍需关注可扩展性。

---

### **未来工作方向**

- 支持动态调整组内资源配比（如自动扩缩 rollout 节点）。
- 引入机器学习预测模型生成更精准的执行时间估计，替代保守上限。
- 探索支持 off-policy 或混合策略作业的统一调度框架。
- 结合 energy-aware 调度，进一步优化能效比。
- 将 ROLLMUX 思想推广至 MoE 模型训练或其他具有阶段性特征的 AI 工作负载。

---

> ✅ **总结一句话**：  
> **ROLLMUX 通过相位级多路复用和两级调度，在不解耦 on-policy 正确性的前提下，成功回收了解耦 RL 架构中的“依赖气泡”，实现了高达 1.84× 的成本节约与 100% SLO 达成，是首个面向多租户解耦 RL 集群的高效调度系统。**

</details>

---

### 3. [Motif-2-12.7B-Reasoning: A Practitioner's Guide to RL Training Recipes](https://arxiv.org/abs/2512.11463)

**Authors**: Junghwan Lim, Sungmin Lee, Dongseok Kim, Taehyun Kim, Eunhwan Park, Jeesoo Lee, Jeongdoo Lee, Junhyeok Lee, Wai Ting Cheung, Dahye Choi, Minsu Ha, Jaeheui Her, Jaeyeon Huh, Hanbin Jung, Changjin Kang, Beomgyu Kim, Minjae Kim, Taewhan Kim, Youngrok Kim, Hyukjin Kweon, Haesol Lee, Kungyu Lee, Dongpin Oh, Yeongjae Park, Bokki Ryu, Dongjoo Weon  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11463v1  

#### Abstract
We introduce Motif-2-12.7B-Reasoning, a 12.7B parameter language model designed to bridge the gap between open-weight systems and proprietary frontier models in complex reasoning and long-context understanding. Addressing the common challenges of model collapse and training instability in reasoning ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Motif-2-12.7B-Reasoning: A Practitioner's Guide to RL Training Recipes 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决当前**开放权重语言模型在复杂推理和长上下文理解方面显著落后于闭源前沿模型**的问题，特别是：
- **训练不稳定性**：在引入强化学习微调（RLFT）后容易出现模型崩溃（model collapse）或性能退化。
- **分布不匹配**：使用高容量教师模型生成的合成数据与学生模型的推理风格不一致，导致性能下降。
- **长上下文训练效率低**：处理64K token级别的上下文对内存和计算资源构成巨大挑战。
- **缺乏可复现的训练配方**：社区缺少系统性的、可在现实算力约束下复现的推理模型训练指南。

### 提出的新方法与创新点
论文提出了一个完整的、可复现的训练配方（training recipe），涵盖系统、数据和算法三个层面的优化：

#### （1）系统级优化（System Optimization）
- **混合并行策略（Hybrid Parallelism）**：结合 DeepSpeed-Ulysses 的 Sequence Parallelism（SP）、Tensor Parallelism（TP）和 Data Parallelism（DP-shard/replicate），支持高效训练 64K 上下文。
- **细粒度激活检查点（Fine-grained Activation Checkpointing）**：手动调优每层的重计算策略，降低峰值显存占用。
- **Liger Kernel 损失函数**：通过分片处理 logits 和损失计算，显著减少 RL 训练中的显存压力，尤其缓解 LM Head 和 Loss 模块带来的内存瓶颈。

#### （2）两阶段监督微调（Two-stage SFT）
- **Stage 1（Reasoning Foundation）**：基于高质量开源数据集（如 Nemotron-Post-Training-Dataset、OpenReasoningDataset、Mixture-of-Thoughts）构建基础推理能力，强调验证（verification）而非数量，采用 `quality-over-quantity` 原则。
- **Stage 2（Deep Reasoning Specialization）**：注入经过**分布对齐**的合成数据（Chain-of-Thought、失败驱动修正等），确保推理轨迹与目标模型的能力相匹配，并逐步扩展上下文至 64K。

> ✅ 创新洞察：提出“**推理分布对齐**”概念——即使合成数据正确，若其推理路径过于复杂或结构不同步，仍会导致性能下降。

#### （3）稳定的强化学习微调（Robust RLFT Pipeline）
- **LLM-as-a-data-filtering**：使用预RL模型生成多个 rollout 并统计 pass rate，筛选难度适中（α ≤ p(x) ≤ β）的问题组成训练集，避免梯度消失（difficulty alignment）。
- **Mixed-policy trajectory reuse**：固定一批轨迹进行多步梯度更新，提升样本效率，平衡 on-policy 与 off-policy 动态。
- **Multi-task RL**：联合优化数学、代码、指令遵循任务，防止单一领域过拟合导致其他能力退化。
- **取消长度惩罚（No length penalty）**：鼓励模型生成更长的推理链，提升复杂任务表现。
- **扩大 clipping 范围（ε ∈ [0.28, 0.40]）**：加快收敛速度，同时保持稳定性。

### 相比现有方法的优势
| 维度 | 传统做法 | Motif-2-12.7B-Reasoning |
|------|----------|------------------------|
| 数据质量 | 大量未验证合成数据 | 高质量 + 分布对齐 + 多重验证 |
| SFT 设计 | 单一静态数据分布 | 两阶段动态课程学习（curriculum） |
| RL 数据选择 | 全量采样或随机抽样 | 基于模型能力的难度过滤 |
| RL 效率 | 每轮重新 rollout | 轨迹复用（trajectory reuse）提高效率 |
| 显存优化 | 标准检查点 | Liger Kernel + 细粒度重计算 |
| 可复现性 | 黑箱流程 | 完整公开训练细节与失败经验 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据来源 | 描述 |
|------|---------|------|
| **SFT 阶段** | Nemotron-Post-Training-Dataset, OpenReasoningDataset, Mixture-of-Thoughts, rstar-coder | 包含数学、编程、STEM、工具调用等多领域推理任务，经严格过滤保留 verified=True 且 npass 在中间范围的样本 |
| **RL 阶段 - 数学** | GURU-92K | 经两步过滤：<br>① 初筛：Qwen-30B pass rate ∈ (0, 10/16]<br>② 再筛：目标模型 pass rate ∈ (0, 0.8]，并按子域分层采样 |
| **RL 阶段 - 编程** | GURU-92K 中代码部分 | 同上过滤逻辑，最终与数学数据集规模对齐 |
| **RL 阶段 - 指令遵循** | 自建合成数据集（10K samples） | 更严苛筛选：pass rate ∈ (0, 0.4]，聚焦挑战性指令 |

### 实验设置
- **模型架构**：基于 Motif-2-12.7B-Instruct 进行继续训练
- **上下文长度**：SFT 最终达 64K；RL 支持最大生成长度
- **硬件平台**：H100 GPU 集群，使用 SkyPilot 管理跨集群资源
- **RL 算法**：GSPO（Group Sequence Policy Optimization），critic-free 的 PPO 变体
- **Rollout 设置**：n=5 个输出用于计算 group advantage
- **训练稳定性机制**：难度过滤、轨迹复用、multi-task 学习、无长度惩罚

### 评估指标
- **主指标**：**Artificial Analysis Intelligence Index (AAII)**  
  —— 由 10 项基准平均得分构成的综合评分，包括：
  - MMLU-Pro
  - GPOA Diamond
  - Humanity's Last Exam
  - LiveCodeBench
  - SciCode
  - AIME 2025
  - IFBench
  - AA-LCR
  - Terminal-Bench Hard
  - Telecom Benchmark

- **单项指标**：
  - Pass@1（LiveCodeBench v5）
  - 数学准确率（AIME 2025）
  - 工具使用成功率（IFBench）
  - 长上下文一致性（Terminal-Bench）

### 基线方法对比
- **开源模型**：各类 12.7B–40B 参数的 open-weight 模型
- **闭源模型**：GPT-4、GPT-5.1 等商业模型（通过 API 接入评测）
- **消融对照**：
  - 不同数据分布（seed-oss vs gpt-oss 合成数据）
  - 是否启用难度过滤
  - 是否使用轨迹复用
  - 是否取消长度惩罚

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | AAII 得分 | 参数量 | 备注 |
|------|----------|--------|------|
| **Motif-2-12.7B-Reasoning** | **89.7** | 12.7B | 本文提出 |
| GPT-5.1 | 88.3 | ~未知 | 当前最强闭源之一 |
| 其他 >12.7B 开源模型 | <89.7 | ≥12.7B | 无更小模型超越本工作 |

> 📊 图1显示：尽管参数仅为 12.7B，Motif-2-12.7B-Reasoning 在 AAII 上超过 GPT-5.1，且是所有优于它的模型中**最小的一个**。

### 与基线方法的对比结果
#### （1）SFT 阶段：LiveCodeBench v5 结果（Table 2）
| 配置 | Pass@1 |
|------|--------|
| Baseline | 51.78 |
| + seed-oss-36b 合成数据 | **63.69** (+11.91) |
| + gpt-oss-120b 合成数据 | **33.92** (-17.86) ⚠️ 性能严重下降 |

> 🔍 发现：更大的教师模型不一定更好，**推理分布错配会引发负迁移**。

#### （2）RLFT 阶段：AIME 2025 表现
- 在 base model 上调优的超参迁移到 SFT 后模型时失效（+18% → 0% 或倒退）
- 证明：**SFT 显著改变了策略更新动力学，必须在目标模型上直接调参**

#### （3）消融实验关键发现
| 技术 | 影响 |
|------|------|
| 分布对齐 SFT | +11.91 pts on LiveCodeBench |
| 难度感知数据过滤 | 避免早期梯度消失，训练更稳定 |
| 轨迹复用（S=3 inner steps） | 训练效率提升 3×，收敛更快 |
| Multi-task RL | 防止数学优化导致指令遵循能力下降 |
| 取消长度惩罚 | 提升需要多步推理的任务表现（如 AIME）|

---

## 4. 关键结论和发现

### 主要发现
1. **参数规模不是决定推理能力的唯一因素**：通过系统性优化，12.7B 模型可达到甚至超越 30–40B 级别模型的表现。
2. **合成数据的质量在于“分布对齐”而非“来源权威”**：来自更大模型的数据可能因风格不匹配而有害。
3. **RLFT 成功的关键在于“可控探索”**：需精心设计数据难度、奖励信号和训练动态，否则极易导致崩溃。
4. **长上下文训练可行且必要**：64K 上下文可通过 hybrid parallelism + kernel 优化实现，且对长程推理至关重要。
5. **可复现性本身就是一种进步**：完整披露失败案例、调参经验、基础设施配置，极大降低了社区复现门槛。

### 方法的局限性
- **依赖高质量验证环境**：代码执行、数学验证等环节要求完善的测试框架。
- **对初始 SFT 模型要求较高**：若基础模型不具备一定推理能力，RLFT 难以启动。
- **multi-task 权重调配敏感**：不同任务间的采样比例影响最终性能平衡。
- **仍需较强算力支持**：虽优于大模型，但在 H100 集群上训练 64K 上下文仍有门槛。

### 未来工作方向
- 将此训练范式推广到更大或更小规模模型（scaling laws 探索）
- 自动化难度调节机制（替代人工设定 α/β）
- 引入更多 agent-style 任务（如网页操作、API 调用）
- 构建端到端的开源 RL 训练框架，集成 Liger Kernel、SkyPilot、vLLM 等组件

---

> ✅ **总结一句话**：  
> *Motif-2-12.7B-Reasoning 证明，在有限算力下，通过“系统优化 + 分布对齐 SFT + 稳定 RLFT”的组合拳，小型开源模型也能在复杂推理任务上媲美甚至超越大型闭源模型，为社区提供了极具价值的实践蓝图。*

</details>

---

### 4. [AdaSD: Adaptive Speculative Decoding for Efficient Language Model Inference](https://arxiv.org/abs/2512.11280)

**Authors**: Kuan-Wei Lu, Ding-Yong Hong, Pangfeng Liu  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11280v1  

#### Abstract
Large language models (LLMs) have achieved remarkable performance across a wide range of tasks, but their increasing parameter sizes significantly slow down inference. Speculative decoding mitigates this issue by leveraging a smaller draft model to predict candidate tokens, which are then verified b...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AdaSD: Adaptive Speculative Decoding for Efficient Language Model Inference 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）虽然在多种任务上表现出色，但由于参数量巨大，推理过程变得非常缓慢且内存受限。**Speculative Decoding** 是一种通过引入小型 draft model 预测候选 token 来加速推理的技术，但现有方法存在以下问题：
- 需要额外训练 draft model；
- 依赖大量超参数调优；
- 接受标准过于严格或需预设阈值，影响效率与准确性平衡。

### 提出的新方法：AdaSD
本文提出 **Adaptive Speculative Decoding (AdaSD)**，一种无需超参数调优、完全自适应的 speculative decoding 方法。其核心思想是动态调整两个关键决策机制：

- **Generation Threshold**：基于 draft model 输出 token 的 **Entropy** 动态决定何时停止生成候选 token。
- **Verification Threshold**：基于 draft 和 target model 分布之间的 **Jensen-Shannon Distance (JS Distance)** 决定是否接受候选 token。

这两个阈值在推理过程中实时更新，利用历史生成 token 的统计信息进行反馈控制，从而实现自适应优化。

### 相比现有方法的优势
| 特性 | 传统方法 | AdaSD |
|------|--------|-------|
| 是否需要训练 | 是（如 EAGLE、Medusa） | 否 |
| 是否需要超参数调优 | 是（如固定长度、固定阈值） | 否（完全自适应） |
| 兼容性 | 多数需修改架构 | 支持 off-the-shelf 模型 |
| 准确性损失 | 可能较大（尤其放宽接受条件时） | 控制在 <2% |
| 加速效果 | 通常 1.2–1.3x | 最高达 **1.49x**（相对 Vanilla） |

> ✅ **核心优势**：AdaSD 在不牺牲准确性的前提下显著提升推理速度，且无需任何先验分析或人工配置，具备强通用性和实用性。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个代表性基准上进行，覆盖不同任务类型：
- **GSM8K**：小学数学应用题，测试推理能力；
- **HumanEval**：编程代码生成任务，评估逻辑与语法正确性；
- **MMLU**：多学科选择题，衡量知识广度。

### 实验设置
- **模型对组合**：
  - `Llama 3.1 70B` / `Llama 3.1 8B`
  - `Llama 3.1 70B` / `Llama 3.2 1B`
  - `Qwen 2.5 72B` / `Qwen 2.5 7B`
- **硬件平台**：4×NVIDIA A6000 GPU，使用 Hugging Face Transformers 框架（v4.55）
- **最大窗口大小 W = 20**，防止无限扩展候选序列
- 所有方法均采用 sampling 策略生成 token

### 评估指标
| 指标 | 含义 |
|------|------|
| **tks/sec** | 每秒处理的 token 数量（吞吐量） |
| **speedup** | 相对于 Vanilla speculative decoding 的加速比 |
| **accuracy** | 任务最终输出的准确率 |
| **JSDist** | 验证阶段平均 JS Distance |
| **#cand / #match / AccRate** | 生成候选数、被接受数、接受率 |

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Vanilla** | 标准 speculative decoding，固定候选长度为 5 |
| **AssistedGen** | Hugging Face 内置辅助生成，基于 DISCO 思想，无监督自适应长度 |
| **Gen-Only** | AdaSD 的变体，仅启用 generation threshold |
| **Verify-Only** | AdaSD 的变体，仅启用 verification threshold |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（以 Llama 3.1 70B/8B 为例）

| 方法 | GSM8K (tks/s) | Speedup | Accuracy | HumanEval (tks/s) | Speedup |
|------|---------------|---------|----------|------------------|---------|
| Vanilla | 13.89 | 1.00× | 0.945 | 14.32 | 1.00× |
| AssistedGen | 16.06 | 1.16× | 0.944 | 18.14 | 1.27× |
| **AdaSD** | **17.51** | **1.26×** | **0.937** | **18.84** | **1.32×** |

> ⚡ 在 Llama 模型上，AdaSD 实现最高 **49% 的加速**（见 Llama 3.1 70B/1B 组合），同时精度下降小于 2%。

### 与其他模型组合的表现
- 在 `Qwen` 模型上，AdaSD 表现略逊于 AssistedGen，但仍优于 Vanilla。
- 原因分析（见论文 5.2 节）：
  - AdaSD 的 generation threshold 导致生成更长的候选序列（#cand 更高），增加了 draft model 开销；
  - AssistedGen 设计目标兼顾效率与接受率，在短序列场景中更具优势。

### 消融实验结果（Ablation Study）

| 方法 | GSM8K Speedup | HumanEval Speedup | 说明 |
|------|----------------|--------------------|------|
| Gen-Only | 1.17× | 1.26× | 仅用 entropy 控制生成长度，已显著提速 |
| Verify-Only | 1.04× | 1.01× | 单独使用 JS Distance 效果有限 |
| **AdaSD (完整版)** | **1.26×** | **1.32×** | **双阈值协同作用带来额外 13% 提升** |

> 🔍 发现：**generation threshold 是主要加速来源**，而 verification threshold 进一步提升了灵活性和鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **熵（Entropy）可有效预测 draft token 的可接受性**：高熵 token 更可能被拒绝，适合用于终止生成。
2. **JS Distance 是稳定可靠的分布差异度量**：相比 KL 或 Cross-Entropy，它有界且对称，适合作为动态验证阈值的基础。
3. **双阈值联合自适应机制显著优于单一策略**：AdaSD 将 generation 与 verification 决策统一建模，实现了效率与精度的最佳权衡。
4. **无需调参即可达到最优性能**：AdaSD 完全依赖运行时反馈自动调节，避免了繁琐的手动搜索。

### 方法的局限性
1. **要求 draft 与 target 模型共享相同 vocabulary**  
   - 若 token 映射不一致，则无法计算 entropy 和 JS Distance。
2. **缺乏理想的中小规模兼容模型对**  
   - 当前主流模型家族中，小模型（<1B）常使用不同的 tokenizer 或架构设计，限制了 AdaSD 的适用范围。
3. **在极短输出任务中加速有限**  
   - 如 MMLU 多选题仅需输出少量 token，speculative decoding 本身增益较小（仅约 8% 加速）。

### 未来工作方向
- 引入更多信号（如 attention score、feature uncertainty）改进 generation threshold；
- 设计更智能的 verification threshold 更新策略（如加权移动平均、序列级统计）；
- 探索跨 vocabulary 的对齐机制，扩大模型对选择空间；
- 结合 early-exit 或 multi-exit 架构进一步降低 draft model 开销。

---

## 总结
✅ **AdaSD 是一种高效、实用、免调参的 speculative decoding 新范式**。它通过 entropy 和 JS Distance 实现了 generation 与 verification 的双重自适应控制，在多个 benchmark 上实现了高达 **49% 的加速**，且精度损失控制在 **2% 以内**。该方法无需训练、兼容现成模型，为大规模语言模型的高效部署提供了强有力的支持。

</details>

---

### 5. [A Multi-Criteria Automated MLOps Pipeline for Cost-Effective Cloud-Based Classifier Retraining in Response to Data Distribution Shifts](https://arxiv.org/abs/2512.11541)

**Authors**: Emmanuel K. Katalay, David O. Dimandja, Jordan F. Masakuna  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11541v1  

#### Abstract
The performance of machine learning (ML) models often deteriorates when the underlying data distribution changes over time, a phenomenon known as data distribution drift. When this happens, ML models need to be retrained and redeployed. ML Operations (MLOps) is often manual, i.e., humans trigger the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Multi-Criteria Automated MLOps Pipeline for Cost-Effective Cloud-Based Classifier Retraining in Response to Data Distribution Shifts

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对**机器学习模型在生产环境中因数据分布漂移（data distribution drift）导致性能下降**的问题，提出了一种自动化解决方案。传统做法依赖人工触发模型重训练（retraining），效率低且成本高；而频繁或不必要的重训练又会造成**云资源浪费和高昂计算开销**。

### 🚀 提出的新方法与创新思路
作者设计并实现了一个**多准则自动化的MLOps pipeline（Auto-MLOps）**，其核心思想是：
- 利用多种统计指标联合检测数据分布变化；
- 只有当检测到显著漂移时才触发模型重训练；
- 在保证模型性能的同时，最小化云计算成本。

#### 主要创新点包括：
1. **多准则漂移检测机制（Multi-criteria drift detection）**  
   融合多个统计度量构建综合漂移评分（Drift Score, DS）：
   - Kolmogorov-Smirnov (KS) Test
   - Kullback-Leibler (KL) Divergence
   - Population Stability Index (PSI)
   - Maximum Mean Discrepancy (MMD)
   - Accuracy 和 F1-score 的变化（ΔAcc, ΔF1）
   
   综合得分公式为：
   $$
   DS = w_1 D_{ks} + w_2 D_{KL} + w_3 PSI + w_4 MMD + w_5 |\Delta Acc| + w_6 |\Delta F1|
   $$
   当 $DS > T$（预设阈值）时触发重训练。

2. **科学严谨的自动化决策框架**  
   将统计检验与模型性能监控结合，形成可审计、可验证的重训练触发逻辑，超越简单的准确率监控。

3. **成本效益导向的设计理念**  
   明确将**云资源消耗和操作成本**作为优化目标之一，实现了“性能保持”与“成本控制”的平衡。

### 🔍 相比现有方法的优势
| 对比维度 | 现有方法（如 FIXED / NAIVE） | 本文 Auto-MLOps |
|--------|----------------------------|----------------|
| 触发机制 | 固定周期 或 单一指标触发 | 多指标融合 + 加权决策 |
| 成本控制 | 差（频繁无意义重训） | 强（仅必要时重训） |
| 性能稳定性 | 随漂移加剧明显下降 | 更稳定，抗漂移能力强 |
| 自动化程度 | 有限或需手动干预 | 全流程 CI/CD 自动化 |
| 可解释性 | 低（为何重训不明确） | 高（基于统计证据） |

> 💡 **总结优势**：相比传统的固定周期重训练（FIXED）或简单漂移触发（NAIVE），Auto-MLOps 在维持相同高水平准确性（~0.75）的前提下，显著降低了重训练频率和云成本。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验在多个基准异常检测数据集上进行，涵盖网络入侵、金融欺诈、医疗信号等领域：

| 数据集 | 样本数 | 属性数 | 异常比例 (%) |
|-------|--------|--------|--------------|
| CICIOT [19] | 416,985 | 41 | 45% |
| CREDIT24 | 234,333 | 30 | 0.2% |
| ECG [10] | 4,998 | 140 | 58% |
| IDSS221 | 430,256 | 95 | 43% |
| KITSUNE18 | 210,171 | 116 | 23% |
| MVTec3 | 5,354 | 1,048,576 | 58% |
| Visa [26] | 10,821 | 95 | 11% |

这些数据集具有多样性，覆盖不同规模、维度和任务场景，增强了实验泛化能力。

### ⚙️ 实验设置
- **模型架构**：使用 Auto-Encoder（AE）作为分类器（用于异常检测）
- **超参数配置**：
  - Weight decay: 1e-6
  - Batch size: 64
  - Epochs: 100
  - Early stopping patience: 5
  - Learning rate: 1e-3
  - Optimizer: Adam
- **模拟环境**：通过逐步引入数据分布变化来模拟真实世界中的 drift progression。

### 📊 评估指标
1. **Accuracy（准确率）**
2. **Retraining Frequency（重训练频率）**
3. **Cloud Cost（云成本）**
4. **Performance Stability（性能稳定性）**

### 🆚 基线方法对比
共比较四种策略：
1. **STATIC**：从不重训练（无任何更新）
2. **FIXED**：按固定周期（如每周）重训练，无视是否发生 drift
3. **NAIVE**：一旦检测到漂移即重训练，无成本优化
4. **Auto-MLOps（本文方法）**：基于加权多准则漂移评分决定是否重训练

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 2）

| 模型 | Accuracy (mean ± std) | Cloud Cost (mean ± std) | Retraining Frequency (mean ± std) |
|------|------------------------|--------------------------|------------------------------------|
| STATIC | 0.69 ± 0.2 | 54.8 ± 5.6 | 0 ± 0 |
| FIXED | 0.75 ± 0.02 | 160.6 ± 10.5 | 3 ± 0 |
| NAIVE | 0.75 ± 0.03 | 130.5 ± 14.9 | 4.3 ± 1.9 |
| **Auto-MLOps** | **0.75 ± 0.03** | **108.1 ± 14.3** | **1.4 ± 1.2** |

### 🔍 结果分析
- 所有动态方法（FIXED, NAIVE, Auto-MLOps）均优于 STATIC，说明应对 drift 是必要的。
- FIXED 和 NAIVE 虽然保持了高 accuracy（~0.75），但代价高昂：
  - FIXED 平均每轮都重训练（频率=3），成本高达 **160.6**
  - NAIVE 更激进（频率=4.3），成本也更高（130.5）
- **Auto-MLOps 在 accuracy 上持平最优水平（0.75）**，但：
  - 重训练频率最低（仅 1.4 次）
  - 云成本最低（**108.1**），比 FIXED 节省约 **33%**，比 NAIVE 节省约 **17%**

### 📉 图形化结果支持（Figure 2 & 3）
- **Figure 2** 显示随着 drift 严重性增加，STATIC 准确率急剧下降，而 Auto-MLOps 保持平稳。
- **Figure 3** 表明 Auto-MLOps 实现了最佳的“精度 vs 成本”权衡曲线，在轻度 drift 下延迟重训练而不影响性能。

> ❗ **消融实验未明确提及**，但文中强调了多指标融合的重要性，并指出权重 $w_i$ 来自经验研究，暗示可通过调参进一步优化。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **多准则漂移检测显著提升 MLOps 效率**  
   结合 PSI、KL、MMD 等多种统计指标，能够更精准识别真正需要重训练的时刻，避免误触发。

2. **自动化 ≠ 频繁重训练，而是智能调度**  
   Auto-MLOps 证明：**更少的 retraining 次数 + 更高的性能稳定性** 是可行的，打破了“频繁更新=更好性能”的误区。

3. **成本与性能可以兼得**  
   在 accuracy 不低于 FIXED/NAIVE 的前提下，**降低 retraining 频率 50%以上，节省云支出达 30%**，对大规模部署极具价值。

4. **推动 MLOps 向原则驱动、可审计方向发展**  
   提供了基于统计证据的 retraining 决策路径，使模型维护过程更加透明、可信。

### ⚠️ 方法的局限性
1. **漂移检测敏感性依赖于统计方法的选择和参数设定**，可能存在 false positive（误报）导致不必要的重训练。
2. 权重 $w_i$ 和阈值 $T$ 依赖经验设置，缺乏自适应调节机制。
3. 当前实验集中在 anomaly detection 场景，尚未广泛验证于其他 classification 任务。
4. 未考虑实时云价格波动等动态经济因素。

### 🔮 未来工作方向
1. 探索 **Reinforcement Learning-based retraining policy**，让系统从历史部署中学习最优调度策略。
2. 引入更复杂的 **cost model**，整合实时 cloud pricing、spot instances 等因素以进一步优化支出。
3. 扩展至更多类型的 drift（如 concept drift、label drift）和模型类型（如 Transformers）。
4. 开发可视化工具，帮助运维人员理解 drift 来源及 retraining 决策依据。

---

## ✅ 总结
该论文提出了一种**面向成本优化的多准则自动化 MLOps pipeline（Auto-MLOps）**，有效解决了数据分布漂移下的模型退化问题。通过融合多种统计测试构建 drift score，仅在必要时触发重训练，在多个 benchmark 数据集上验证了其在**保持高 accuracy 的同时显著降低云成本和 retraining 频率**的能力。这一工作为构建可持续、高效、可审计的工业级 ML 系统提供了重要实践范式。

</details>

---

### 6. [Enhanced Pruning for Distributed Closeness Centrality under Multi-Packet Messaging](https://arxiv.org/abs/2512.11512)

**Authors**: Patrick D. Manya, Eugene M. Mbuyi, Gothy T. Ngoie, Jordan F. Masakuna  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.11512v1  

#### Abstract
Identifying central nodes using closeness centrality is a critical task in analyzing large-scale complex networks, yet its decentralized computation remains challenging due to high communication overhead. Existing distributed approximation techniques, such as pruning, often fail to fully mitigate th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Enhanced Pruning for Distributed Closeness Centrality under Multi-Packet Messaging*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大规模分布式网络中，**closeness centrality** 的去中心化计算面临严重的通信开销问题。现有的分布式近似方法（如 pruning）虽然能减少部分通信量，但在高数据流量环境下，尤其是采用 **multi-packet messaging** 时，消息数量激增，导致网络拥塞、延迟增加和数据丢失风险上升。

该论文旨在解决 **pruning 方法在 multi-packet messaging 场景下的通信瓶颈问题**，提升其在大型复杂网络中的可扩展性和效率。

---

### 提出的新方法与创新思路
作者对 Masakuna 等人提出的原始 pruning 方法 [22] 进行增强，提出了一种适用于 **multi-packet messaging** 的改进型分布式剪枝策略，核心创新包括：

- **多包消息整合机制（multi-packet messaging integration）**  
  允许节点将多个小消息打包成更大的数据块进行传输，利用 **sliding window 协议（Go-Back-N ARQ）** 实现高效、可靠的数据传输，降低单位信息的通信开销。

- **通信轮次优化与发送抑制机制**  
  修改算法逻辑，使某些低中心性节点（如叶子节点）在整个通信阶段最多只发送 $D-2$ 轮消息（而非原始的 $D$ 轮），甚至完全不主动发送消息，从而显著减少整体消息数。

- **非中心节点的通信过载限制技术**  
  针对“可剪枝”但尚未被邻居识别为剪枝状态的节点，设计了轻量级协调机制，在不影响精度的前提下最小化其通信负担。

---

### 相比现有方法的优势
| 维度 | 原始 pruning 方法 [22] | 本文增强方法 |
|------|------------------------|-------------|
| **消息总数** | 较高，尤其在 multi-packet 下更严重 | 减少 5%-15%，有效缓解通信压力 |
| **运行时间** | 受限于频繁的小包交换 | 显著缩短，尤其在大网络中 |
| **数据完整性** | 单包易丢包，影响收敛 | 多包分片 + ARQ 提升鲁棒性 |
| **适用场景** | 小规模或低带宽环境 | 更适合大规模、高吞吐网络 |
| **内存/本地开销** | 较低 | 略有增加（合理权衡） |

> ✅ **核心优势总结**：在保持原有 pruning 方法近似精度的基础上，通过结构化支持 multi-packet messaging，实现了更高的通信效率和更强的实用性，特别适用于车载网络、移动自组织网络（MANETs）、监控系统等高动态、大数据量场景。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验基于两类图结构：

#### （1）随机生成网络（100个）
- 构建方式：在 250×250 网格上随机采样 $N \in [100, 2000]$ 个节点
- 连接规则：欧氏距离 < 通信范围 $d=10$ 则连边
- 图属性范围：
  - 边数：[100, 4000]
  - 直径（Diameter）：[30, 68]（见 Figure 1）

#### （2）真实世界网络（35个）
包含以下代表性网络：
- **Facebook artist network**（艺术家社交关系）
- **Phenomenology collaboration network**（高能物理合作网络）
- **Gnutella peer-to-peer network**（P2P 文件共享网络）
- **32 个 Autonomous System graphs**（互联网路由拓扑）

> 所有测试共涉及 **135 个图实例**

---

### 实验设置与评估指标

#### 技术实现
- 编程语言：Python
- 图处理库：NetworkX
- 内存测量：Pympler
- 时间记录：Timer 模块

#### multi-packet 设置
- 每条消息拆分为 $m \in \{1, 10, 20, 30, 50\}$ 个 packet
- 采用 **Go-Back-N ARQ** 协议保障传输可靠性

#### 评估指标
| 指标 | 描述 |
|------|------|
| `Average / Max number of messages per node` | 衡量通信负载 |
| `Running time (s)` | 算法执行总耗时 |
| `Data loss (%)` | 数据包丢失率 |
| `Memory usage (MB)` 和 `Overhead (ms)` | 局部资源消耗 |
| `Closeness centrality quality` | 近似最中心节点与真实最中心节点之间的最短路径距离 |
| `Statistical significance` | 使用 Wilcoxon signed-rank test（α=0.01）检验差异显著性 |

#### 基线方法对比
- **Baseline**: 原始 pruning 方法 [22]
- **Proposed**: 本文提出的 enhanced pruning 方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 指标 | 结果概览 |
|------|---------|
| **平均消息数减少** | 5% ~ 15%（跨所有拓扑） |
| **最大消息数减少** | 最高达 20%（见 Figure 6） |
| **运行时间** | 明显下降，尤其当 $m > 10$ 时优势明显 |
| **数据丢失率** | 随 $m$ 增加而降低（Figure 3） |
| **内存使用** | 略有上升（+10%~15%），但可控 |
| **局部开销（overhead）** | 小幅增加，属于可接受代价 |

---

### 与基线方法的对比结果

#### （1）消息数量对比（Figures 5 & 6）
- 在全部 135 个图中，本文方法在 **平均和最大消息数** 上均优于原始方法。
- 差值随 $m$ 增大而扩大，说明 **multi-packet 场景下优化效果更显著**。
- 正向差值表明：**我们的方法发送的消息更少**。

#### （2）运行时间和数据丢失（Figure 3）
- 当 $m=1$（单包）时，运行时间长且丢包率高；
- 随着 $m$ 增加（即每条消息分更多包），运行时间下降，丢包率降低；
- 原因：小包易引发拥塞，而多包分片结合 ARQ 提高了传输稳定性。

#### （3）内存与开销（Figure 4）
- 随着 $m$ 增加，每个节点需缓存更多 packet，导致内存占用和处理开销上升；
- 但增长趋势平缓，未出现指数级膨胀，**trade-off 合理**。

#### （4）中心节点选择质量（Table II）
- 对两个随机图（120 和 300 节点）测试不同迭代次数 $D$
- 两种方法选出的“近似最中心节点”与真实最中心节点的距离几乎一致
- 当 $D$ 接近图直径时，误差趋近于 0
- ➤ **证明本文方法未牺牲估计准确性**

#### （5）统计显著性检验（Table III）
| 指标 | p-value | Effect Size |
|------|--------|------------|
| 消息数量 | 0.0625 | 0.586（medium） |
| 运行时间 | 0.0876 | 0.437（medium） |
| 中心性质量 | ≈0 | 0 |

- 尽管 p-value > 0.01（未达传统显著水平），但 **effect size 为 medium**，说明实际差异具有意义。
- 特别是中心性质量的 p≈0，**强有力支持本文方法保持了原始精度**。

---

### 消融实验（隐含分析）
虽然未明确标注“ablation study”，但以下对比构成事实上的消融：
- 是否启用 $D-2$ 发送限制 → 影响消息总量
- 是否使用 multi-packet + ARQ → 影响丢包率和运行时间
- 不同 $m$ 值的影响 → 权衡通信效率与内存开销

结论：**multi-packet messaging 是必要手段，但必须配合 pruning 优化才能避免反向恶化通信负载**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **multi-packet messaging 能有效降低丢包率并加速运行时间**，尤其是在带宽受限或高延迟环境中。
2. ✅ **直接应用 multi-packet 会加剧消息总数增长**，因此必须结合 pruning 机制进行优化。
3. ✅ 本文提出的 enhanced pruning 方法成功将消息数减少 **5%-15%**，同时保持与原方法相同的 **closeness centrality 近似精度**。
4. ✅ 存在合理的 trade-off：**小幅增加内存和本地开销，换取大幅通信效率提升**。
5. ✅ 方法在多种拓扑（随机 + 真实）下表现稳定，具备良好泛化能力。

---

### 方法的局限性
- **假设节点不会在算法初期失效**：若 prunable 节点提前宕机，其邻居无法获知，可能影响局部视图一致性。
- **依赖 FIFO 异步通信模型**：在极端乱序或高延迟网络中性能可能退化。
- **memory overhead 随 $m$ 增加而上升**：在资源极度受限设备（如 IoT sensor）上部署需谨慎。
- **未考虑动态拓扑变化**：当前方法针对静态图设计，不适用于频繁变动的网络。

---

### 未来工作方向（原文提及）
1. 将本方法扩展至其他 centrality metrics（如 betweenness 或 eigenvector centrality）。
2. 在真实应用场景中验证，如：
   - 移动传感器网络（mobile sensor networks）
   - 社交网络分析（social network analysis）
   - 自动驾驶车队协同决策
3. 探索 adaptive $m$（动态调整 packet 数量）以进一步优化资源利用率。
4. 结合 multicast/broadcast 技术提升 multi-packet 分发效率。

---

> 📌 **总体评价**：该论文在分布式图算法与网络通信交叉领域做出了扎实贡献，提出的 enhanced pruning 方法为大规模网络中高效计算 closeness centrality 提供了一个实用且可扩展的解决方案。

</details>

---

### 7. [Speculative Decoding Speed-of-Light: Optimal Lower Bounds via Branching Random Walks](https://arxiv.org/abs/2512.11718)

**Authors**: Sergey Pankratov, Dan Alistarh  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11718v1  

#### Abstract
Speculative generation has emerged as a promising technique to accelerate inference in large language models (LLMs) by leveraging parallelism to verify multiple draft tokens simultaneously. However, the fundamental limits on the achievable speedup remain poorly understood. In this work, we establish...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Speculative Decoding Speed-of-Light: Optimal Lower Bounds via Branching Random Walks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文致力于解决 **speculative decoding**（推测解码）技术中的一个根本性理论问题：  
**在给定系统约束（如并行验证容量 $P$）和目标模型特性下，推测生成所能达到的最大加速上限是多少？**

尽管 speculative decoding 在实践中已被广泛采用（如 EAGLE、Medusa），其**理论极限仍不清楚**。现有分析多基于简化假设（如平均接受率），缺乏对最优性能的严格刻画。

---

### 🚀 提出的新方法与创新思路

1. **首次建立“紧致”（tight）的运行时间下界（等价于速度提升上界）**  
   作者为任意确定性 speculative generation 算法建立了首个**最优下界**，揭示了加速能力的根本瓶颈。

2. **引入 Branching Random Walks (BRW) 分析框架**  
   将 token 生成过程建模为 log-probability 上的 **Branching Random Walk**，利用概率论中成熟的工具（如 Many-to-One Lemma 和 Renewal Theory）来分析在有限验证预算 $P$ 下高概率路径的分布。

3. **推导出期望成功预测 token 数的闭式上界**  
   在基本假设下，证明每轮迭代中可成功预测的 token 数期望满足：
   $$
   \mathbb{E}[X] \leq \frac{\mu + \mu^{(2)}}{\mu^2} \log(P) + O(1)
   $$
   其中：
   - $P$：verifier 的并行 token 容量（parallel token capacity）
   - $\mu$：目标模型输出分布的期望熵（expected entropy）
   - $\mu^{(2)}$：log-prob 的期望二阶矩（expected second log-moment）

   这个公式揭示了**加速潜力与模型不确定性之间的本质权衡**。

4. **区分完美知识 vs 不完美知识场景下的界限**  
   进一步分析了当 drafter 模型无法完全获知 target model 输出分布时的情形，给出了基于 cross-entropy 的下界估计：
   $$
   \mathbb{E}[X] \gtrsim \frac{\log P}{H(p \| q)}
   $$

---

### 🔍 相比现有方法的优势

| 方面 | 以往工作 | 本论文 |
|------|--------|-------|
| **理论深度** | 基于 agreement rate 或 Markov chain 抽象，提供实例依赖边界 | 首次给出**普适、紧致的渐近上下界**，适用于所有确定性算法 |
| **建模精度** | 忽略路径相关性和树结构复杂性 | 利用 BRW 精确刻画 token 树中高概率路径的增长行为 |
| **指导意义** | 经验性优化 draft tree 结构 | 提供明确设计原则：**降低目标模型有效熵有助于提升 speculative 效率** |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验覆盖多种任务类型，使用以下基准进行评估：
- **HumanEval**：代码生成
- **MT-bench**：多轮对话质量
- **GSM8K**：数学推理
- **CNN/DM**：摘要生成
- **Natural Questions (NQ)**：问答

这些是 EAGLE 系列论文常用的评测集，确保可比性。

---

### ⚙️ 实验设置与评估指标

#### 模型选择
测试了多个主流 LLM：
- **Llama3.1 8B Instruct (L3-8B)**
- **Llama3.3 70B Instruct (L3-70B)**
- **DeepSeek R1 Distill Llama 8B (DS-8B)**
- **Qwen3 8B (Q3-8B)**

#### 参数测量
- 对每个模型在各数据集上运行 target model，采样 80 个样本。
- 温度设为 1.0，计算每个位置的 next-token 分布。
- 估算关键参数：
  - $\mu = \mathbb{E}[H(p)]$：期望熵
  - $\mu^{(2)} = \mathbb{E}[\sum p \log^2 p]$：期望二阶 log-moment

#### 评估方式
- **不直接比较端到端吞吐量**，而是验证理论边界是否能准确预测实际系统的性能趋势。
- 主要对比对象为 **EAGLE-3**，当前最先进的 speculative decoding 方法。
- 固定 speculation size $P = 60$，观察不同模型/任务上的平均 accepted tokens per iteration。

---

### 🆚 基线方法对比
- **理论基线**：
  - Theorem 1 的精确上界（蓝色虚线）
  - Corollary 1 的极限上界（橙色虚线，$\sim \log P / \mu$）
  - Lemma 7 的 imperfect knowledge 下界（绿色虚线）
- **实际系统**：
  - **EAGLE-3**（星号标记）
  - “Potential” EAGLE-3（黑色实线）：忽略 drafting 开销的理想版本

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### 表 1：不同模型与任务下的 $\mu$ 和 $\mu^{(2)}$
| Model | Task | $\mu$ | $\mu^{(2)}$ |
|-------|------|--------|-------------|
| L3-8B | HumanEval | 0.279 | 0.777 |
| L3-8B | MT-bench | 1.088 | 6.654 |
| L3-70B | HumanEval | 0.136 | 0.238 |
| DS-8B | MT-bench | 0.633 | 1.951 |
| Q3-8B | MT-bench | 0.369 | 0.814 |

> 观察：更大更优的模型（如 L3-70B）具有更低且更稳定的 $\mu$，意味着更高的 speculative 并行潜力。

---

### 📈 图表结果分析

#### Figure 1：验证 Lemma 7 的预测能力
- 横轴：理论下界 $\log P / H(p\|q)$
- 纵轴：EAGLE-3 实际观测到的平均每轮接受 token 数
- **结果**：呈现明显的线性关系，说明理论边界能够很好地**捕捉实际系统性能的趋势**。
- 存在一个常数差距，归因于 $o(\log P)$ 项或实现开销。

#### Figure 2：随 $P$ 变化的 speedup 曲线（以 L3-8B 为例）
- 蓝色虚线：Theorem 1 的精确上界
- 橙色虚线：$\log P / \mu$ 极限上界
- 绿色虚线：imperfect knowledge 下界
- 星号：真实 EAGLE-3 性能（来自原论文配置）

##### 关键发现：
1. **EAGLE-3 接近其自身理想上限**（黑线），表明其实现已较高效；
2. 但仍显著低于理论最优上界（蓝线），存在约 **2倍以上的加速空间**；
3. 差距主要来源于：
   - drafter 与 target model 之间的 mismatch（cross-entropy 影响）
   - 非最优的 draft tree 构造策略

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Speculative decoding 的加速上限呈 $\log P$ 增长**  
   即使无限增加并行度 $P$，收益也是**对数级增长**，存在严重边际递减效应。

2. **模型熵 $\mu$ 是决定加速潜力的核心因素**  
   - 低熵模型（如 L3-70B）更适合 speculation
   - 高熵任务（如开放对话 MT-bench）难以有效推测

3. **当前系统（如 EAGLE-3）尚未达到理论极限**  
   实验显示仍有 **×2 以上的改进空间**，主要受限于：
   - drafter 模型的预测误差（cross-entropy）
   - 缺乏全局最优 draft tree 构造机制

4. **BRW 框架提供了强大的理论分析工具**  
   成功将 speculative decoding 映射为经典随机过程问题，打开了新的分析视角。

---

### ⚠️ 局限性

1. **i.i.d. 假设不现实**  
   假设不同 prefix 下的 acceptance probability 独立同分布，忽略了语言的上下文依赖性。

2. **简化时序模型**  
   忽略了 KV Cache 增长带来的 verifier 延迟变化，以及 drafting 模块的实际开销。

3. **仅分析确定性算法**  
   未涵盖可能更优的随机性 speculation 策略。

4. **完美知识假设**  
   上界基于 drafter 完全知晓 target model 概率分布，现实中不可达。

---

### 🔮 未来工作方向

1. **扩展至非 i.i.d. 场景**  
   引入马尔可夫或平稳遍历过程建模 context-dependent 输出分布。

2. **设计逼近理论极限的新型 drafting 算法**  
   基于 BRW 启发，开发能动态构造接近最优 draft tree 的策略。

3. **联合优化模型架构与 speculation 机制**  
   如训练目标函数显式降低输出熵或方差，以提升 speculative 效率。

4. **探索 stochastic speculative strategies**  
   分析随机采样策略是否能在某些情况下突破确定性算法的界限。

---

> **总结一句话**：  
> 本文通过引入 Branching Random Walks 框架，首次为 speculative decoding 建立了“光速级”的理论性能极限，揭示了**并行度收益的对数天花板**和**模型熵的关键制约作用**，并通过实验验证了边界的紧致性，为未来系统设计提供了坚实的理论基础。

</details>

---

### 8. [Mitigating the Safety Alignment Tax with Null-Space Constrained Policy Optimization](https://arxiv.org/abs/2512.11391)

**Authors**: Yifan Niu, Han Xiao, Dongyi Liu, Nuo Chen, Jia Li  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.11391v1  

#### Abstract
As Large Language Models (LLMs) are increasingly deployed in real-world applications, it is important to ensure their behaviors align with human values, societal norms, and ethical principles. However, safety alignment under Reinforcement Learning (RL) often suffers from forgetting learned general a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Mitigating the Safety Alignment Tax with Null-Space Constrained Policy Optimization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在实际应用中需要进行 **safety alignment**（安全对齐），以确保其输出符合人类价值观和社会规范。然而，现有的对齐方法往往会导致模型在通用任务上的能力下降，这种现象被称为 **safety alignment tax**（安全对齐税）。该问题本质上源于安全优化与通用能力之间的隐式冲突。

### 提出的新方法
本文提出了一种新的强化学习框架——**Null-Space constrained Policy Optimization (NSPO)**，旨在实现安全对齐的同时有效缓解对齐税。

#### 核心思想
- 将 **safety policy gradients**（安全策略梯度）几何投影到通用任务表征的**零空间（null space）**中。
- 这样做的效果是：安全更新被约束为与模型的通用能力子空间正交，从而避免干扰已学得的通用能力。

#### 技术实现
- 利用 **Singular Value Decomposition (SVD)** 对通用任务数据的非中心协方差矩阵 $KK^T$ 进行分解，提取零空间对应的投影矩阵 $U U^T$。
- 在梯度更新时，将原始梯度乘以该投影矩阵，使其落在不影响通用能力的空间内。

### 相比现有方法的优势
| 维度 | NSPO优势 |
|------|---------|
| **保留通用能力** | 理论上证明能保持模型原有核心能力不退化 |
| **安全性能** | 实现有效的安全优化方向，保证安全目标下降 |
| **数据效率** | 仅需 **40% 的 PKU-SafeRLHF 安全数据**即可达到优异表现，无需混合大量通用任务数据 |
| **训练稳定性** | 零空间投影具有非扩张性（non-expansive），提供形式化的梯度稳定性保障 |
| **去KL项设计** | 移除了传统RL中的KL散度惩罚项，避免其与安全目标的潜在冲突 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 安全训练数据
- **PKU-SafeRLHF**：使用其中 **11K样本（占总量40%）** 用于NSPO训练，远少于其他方法使用的完整数据集。

#### 通用能力数据（用于构建投影矩阵）
- 构造投影矩阵 $K$ 时，从以下三个领域随机采样共 **1,000个实例**：
  - **Common sense**（常识推理）
  - **Math**（数学题如GSM8K、MATH）
  - **Code**（编程题如LiveCodeBench）

### 实验设置
- **基础模型**：
  - `Llama3-8B-Instruct`
  - `Qwen2.5-7B-Instruct`
- **奖励模型**：
  - 使用开源的安全分类器 **Llama-Guard-4-12B** 提供安全评分。
- **训练框架**：
  - 基于 `verl` 框架实现，采用 **GRPO** 结构作为骨干。
- **超参数**：
  - $\beta = 0.1$, $\epsilon = 0.2$
  - 特征值阈值设为 $5 \times 10^{-4}$（用于判断是否属于零空间）

### 评估指标

#### 安全性评估（越低越好）
- **Attack Success Rate (ASR%)**：衡量模型生成有害内容的比例，由GPT-4或专用模型（如SORRY-Bench）评判。

#### 通用能力评估（越高越好）
- **Accuracy (%)**：用于MMLU、GSM8K、MATH等任务
- **Win Rate (%)**：AlpacaEval上的胜率
- **Pass@1 (%)**：LiveCodeBench代码生成通过率

### 基线方法对比
| 方法 | 类型 | 是否使用混合数据 |
|------|------|----------------|
| **DPO-H / DPO-S / DPO-Mix** | DPO变体 | 是（部分） |
| **SafeRLHF** | RLHF双目标优化 | 否 |
| **PeCAN / MoCAN** | 双阶段优化 | 否 |
| **W-DOOR** | 强拒绝+反向学习 | 否 |
| **BFPO** | 双目标偏好优化 | 是（显式混合） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 1 & 2）

#### ✅ 安全性能（ASR ↓）
在 `Qwen2.5-7B-Instruct` 上，NSPO 显著优于所有基线：

| Benchmark | NSPO | 最优基线 | 提升幅度 |
|----------|------|---------|--------|
| **AdvB** | **0.29%** | 0.00% (W-DOOR) | 接近最优 |
| **PKU-Safe** | **0.52%** | 1.35% (BFPO) | ↓1.58倍 |
| **HarmB** | **0.50%** | 11.33% (BFPO) | ↓22.66倍 |
| **JailbreakB** | **0.67%** | 1.33% (BFPO) | ↓2倍 |
| **SORRY** | **19.54%** | 15.00% (W-DOOR) | 略逊但可控 |
| **HarmQA** | **0.11%** | 0.31% (W-DOOR) | ↓2.8倍 |
| **ALERT** | **0.81%** | 3.40% (W-DOOR) | ↓4.2倍 |

> ⭐ NSPO 在多个安全基准上取得**state-of-the-art**表现。

#### ✅ 通用能力保留（Accuracy ↑）
NSPO 几乎无损地保留了通用能力，在多数任务上接近甚至超过基线：

| Task | NSPO | 基础模型 | BFPO（最佳之一） |
|------|------|--------|--------------|
| **MMLU** | 71.74% | 71.71% | 71.72% |
| **SuperGPQA** | 27.68% | 27.72% | 27.98% |
| **AlpacaEval WR** | 94.33% | 94.35% | 95.22% |
| **GSM8K** | 81.96% | 81.65% | 83.85% |
| **MATH** | 75.05% | 74.80% | 74.55% |
| **OlympiadBench** | 39.00% | 37.80% | 36.35% |
| **LiveCodeBench Pass@1** | 24.05% | 24.07% | 24.36% |

> 🔍 总体来看，NSPO 在通用任务上的性能**几乎没有退化**（多数在 ±1% 内），显著优于其他安全对齐方法（如DPO-H/SafeRLHF导致严重退化）。

### 消融实验结果（Ablation Study）

#### Q1: Null-Space 投影 vs KL 正则？
- **GRPO w/o KL**：安全强，但通用能力严重退化
- **Standard GRPO (with KL)**：保护了通用能力，但牺牲了安全性能
- **NSPO**：**兼顾两者**，实现了最佳平衡

> 📊 图4显示：NSPO在安全和通用能力之间取得了最优权衡。

#### Q2: 投影矩阵 $K$ 的影响？
- **数据来源**：使用 **mix（混合常识/数学/代码）** 构建 $K$ 效果最好 → 更好保留通用能力
- **样本量**：增大样本量可更好保留通用能力，但可能轻微降低安全性
- **特征值阈值**：中等阈值（如 $5\times10^{-4}$）取得最佳 trade-off

> 📈 图6表明：合理选择投影矩阵构造方式至关重要。

#### Q3: 数据效率分析
- 如图7所示，使用 **40% vs 100% PKU-SafeRLHF 数据**，模型性能几乎无差异。
- 训练日志显示：40%后reward趋于饱和，继续训练收益极小。

> 💡 **NSPO具备高度数据效率**，仅需少量高质量安全数据即可完成有效对齐。

---

## 4. 关键结论和发现

### 主要发现
1. **安全对齐税的根本原因在于梯度冲突**：安全梯度会破坏模型在通用任务上的表征映射。
2. **零空间投影是一种有效的解耦机制**：通过将安全更新限制在不影响通用能力的方向上，实现“精准手术式”优化。
3. **NSPO理论上有保障**：
   - 保证梯度稳定（Proposition 4.1）
   - 保证是合法的下降方向（Theorem 4.2）
4. **移除KL项更合理**：KL正则倾向于拉回unsafe reference model，反而阻碍安全优化；而零空间投影本身就能防止过拟合。

### 方法的局限性
- **依赖预定义的通用任务集**来构建投影矩阵 $K$，若覆盖不足可能导致某些能力未被保护。
- **SVD计算开销**虽然可控（$O(d^3)$），但在非常深层网络中仍需注意内存管理（尽管作者采用offload策略缓解）。
- 当前方法假设线性变换下的零空间关系成立，对于复杂非线性交互可能存在近似误差。

### 未来工作方向
- 扩展至多模态模型的安全对齐
- 自适应选择投影空间（动态更新 $K$）
- 探索更高效的零空间近似算法（避免SVD）
- 将NSPO思想应用于其他对齐挑战（如truthfulness alignment）

---

> ✅ **总结一句话**：  
> NSPO 通过**零空间约束的梯度投影**，首次在理论上和实践上实现了**安全对齐与通用能力的解耦优化**，在大幅降低对齐税的同时，达到了SOTA级别的安全性和卓越的数据效率。

</details>

---

### 9. [A-LAMP: Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation](https://arxiv.org/abs/2512.11270)

**Authors**: Hong Je-Gal, Chan-Bin Yi, Hyun-Suk Lee  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.11270v1  

#### Abstract
Applying reinforcement learning (RL) to real-world tasks requires converting informal descriptions into a formal Markov decision process (MDP), implementing an executable environment, and training a policy agent. Automating this process is challenging due to modeling errors, fragile code, and misali...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# A-LAMP: Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统强化学习（RL）在实际应用中面临三大挑战：
- **MDP建模困难**：将非形式化的自然语言任务描述转化为精确的马尔可夫决策过程（MDP）需要大量专家知识。
- **环境实现脆弱**：手动编写可执行的RL环境代码容易出错，且难以保证语义一致性（如状态、动作、奖励是否准确反映任务目标）。
- **流程缺乏灵活性与可扩展性**：任务需求变更时需重新设计MDP并从头训练策略，无法快速适应。

这些问题导致RL部署成本高、周期长，限制了其在工业场景中的广泛应用。

---

### 🚀 提出的新方法：A-LAMP 框架
作者提出 **A-LAMP**（Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation），一个基于多智能体大模型（multi-agent LLM）的自动化框架，实现从自然语言描述到可执行策略的端到端生成。

#### 核心思想：
将整个政策生成流程分解为多个专业化LLM代理（Agent），每个代理负责特定子任务，并通过模块化协作确保语义对齐与可验证性。

#### 主要阶段与对应Agent：
| 阶段 | Agent | 功能 |
|------|-------|------|
| 抽象理解 | Parameter Agent, Objective Agent, Variable Agent, Constraint Agent | 从自然语言中提取参数、目标、变量和约束 |
| MDP建模 | Modeling Agent, SAR Agent | 形式化定义MDP的目标函数、状态空间、动作空间、奖励函数 |
| 编码实现 | Env Agent, Coding Agent, Code Executor | 生成可运行的Gym风格环境代码与DQN训练循环 |

此外引入**错误纠正模块**（error correction module），允许Agent自检或向人类请求澄清，提升鲁棒性。

---

### 🔍 相比现有方法的优势
| 维度 | A-LAMP | 单一LLM方法（如GPT-4o） |
|------|--------|------------------------|
| **建模可靠性** | 分步推理，降低单个Agent的认知负担，输出更一致 | 易出现内部不一致（如代码与目标脱节） |
| **可解释性与可验证性** | 各阶段输出透明（JSON/方程级表达），支持逐步审查 | 黑箱处理，难以追溯错误来源 |
| **适应性** | 支持自由文本输入，任务变更只需更新描述即可自动重构 | 需人工干预重写提示或调整逻辑 |
| **小模型可用性** | 轻量版Light A-LAMP可在Gemma3-27B上运行，接近GPT-4o表现 | 小模型性能显著下降 |
| **成功率** | 在复杂定制环境中显著提高策略生成成功率 | 容易失败于编码或训练阶段 |

> 💡 创新亮点：首次系统性地将“**认知分步”+“多智能体协同”+“反馈调试机制**”应用于自动化RL建模，实现了高保真、可追踪的端到端政策生成。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集（Benchmark Tasks）
共测试5个任务，涵盖经典控制与领域专用优化：

| 任务 | 类型 | 是否需自定义环境 |
|------|------|------------------|
| **Cart-pole** | 经典控制 | 否（已有Gym环境） |
| **Mountain-car** | 经典控制 | 否 |
| **Wireless** | 无线通信资源调度 | 是（Shannon容量公式等） |
| **Drone-delivery** | 无人机配送路径规划 | 是（网格世界+能量约束） |
| **Inventory-management** | 库存管理（泊松需求） | 是（成本结构复杂） |

所有任务仅通过**自由形式的自然语言描述**作为输入，无额外结构化标注。

---

### 🧪 实验设置与评估指标

#### 评估标准（三项成功率，每项基于20次试验）：
| 指标 | 定义 |
|------|------|
| **Modeling Success Rate** | 提取的状态、动作、奖励逻辑正确且完整 |
| **Coding Success Rate** | 生成代码可在标准Python环境中无语法错误执行 |
| **Policy Generation Success Rate** | RL训练收敛至最大化奖励的策略，且满足任务目标（最关键指标） |

#### 对比方法：
| 方法 | 描述 |
|------|------|
| **A-LAMP (w/ GPT-4o)** | 完整框架，使用强LLM |
| **A-LAMP w/o EC** | 移除错误纠正模块的版本 |
| **Light A-LAMP (w/ Gemma3-27B)** | 轻量级版本，使用较小LLM |
| **Single GPT-4o** | 单一LLM直接生成完整代码 |
| **Single Gemma3-27B** | 单一小型LLM直接生成代码 |

所有方法均引导使用**DQN**算法进行策略训练，以保持RL后端一致性，突出建模与编码能力差异。

---

## 3. 主要实验结果和性能指标

### 📊 表格：各方法在五项任务上的成功率三联体（建模 / 编码 / 策略生成）

| Task | A-LAMP | A-LAMP w/o EC | Light A-LAMP | Gemma3-27B | GPT-4o |
|------|--------|---------------|--------------|------------|--------|
| **Cart-pole** | — | 1.00/0.95/0.95 | 1.00/0.85/0.45 | 1.00/0.60/0.35 | 1.00/0.75/0.45 |
| **Mountain-car** | — | 1.00/1.00/0.75 | 0.95/0.70/0.55 | 1.00/0.35/0.30 | 1.00/1.00/0.40 |
| **Wireless** | 1.00/1.00/0.45 | 0.90/0.80/0.40 | 0.95/0.60/0.15 | 0.55/0.65/0.05 | 0.80/0.90/0.20 |
| **Drone-del.** | 0.80/0.95/0.45 | 0.65/0.75/0.30 | 0.55/0.50/0.15 | 0.40/0.05/0.00 | 0.35/0.55/0.10 |
| **Inv.-mgmt.** | 1.00/0.55/0.30 | 1.00/0.40/0.20 | 0.85/0.25/0.05 | 0.60/0.00/0.00 | 0.65/0.05/0.05 |

> 注：“—”表示未提供完整数据；部分任务启用了Error Correction（EC）模块。

---

### 🔬 关键发现：

#### ✅ A-LAMP 显著优于单一LLM
- 在需要**自定义环境**的任务（Wireless, Drone-del., Inv.-mgmt.）中，A-LAMP的**策略生成成功率是单一LLM的2倍以上**。
- 例如在`Drone-delivery`任务中，A-LAMP达到 **45% 成功率**，而GPT-4o仅为10%，Gemma3-27B为0%。

#### ✅ 结构分解本身带来巨大收益
即使**没有错误纠正模块**，A-LAMP仍全面领先，说明**多Agent分工架构是性能提升的关键驱动因素**。

#### ✅ 错误纠正进一步提升难任务表现
在`Wireless`, `Drone-del.`, `Inv.-mgmt.`任务中启用EC后，成功率进一步提升，尤其体现在减少无效编码成功（spurious coding success）。

#### ✅ 小模型也能高效工作
**Light A-LAMP（Gemma3-27B）** 的表现远超单独使用Gemma3-27B，甚至接近GPT-4o水平，证明框架本身放大了小模型潜力。

---

### 🔍 失败案例分析（Failure Analysis）
通过对失败模式分类（M×/C×/P×），揭示以下优势来源：
1. **减少“虚假编码成功”**（M×, C○）：单一LLM常生成语法正确但语义无关的代码；A-LAMP几乎消除此类情况。
2. **降低完全失败率**（M×, C×）：在`Drone-del.`中减少60%，在`Inv.-mgmt.`中完全消除。
3. **增强训练稳定性**：更多(M○, C○)尝试最终成功训练出有效策略。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **结构化多Agent框架显著提升自动化RL建模的可靠性与成功率**，尤其是在复杂、需自定义环境的任务中。
2. **语义对齐是关键瓶颈**：A-LAMP通过分阶段建模与显式验证，有效解决了“目标→代码”之间的语义漂移问题。
3. **轻量化版本具备实用价值**：Light A-LAMP表明该框架可用于资源受限场景，推动边缘或本地部署。
4. **生成策略具有最优性保障**：在`Wireless`任务中，DQN策略逼近理论最优的贪婪调度器，验证了生成策略的质量。

---

### ⚠️ 局限性
1. **编码阶段仍较脆弱**：尽管有反馈机制，但结构性错误（如API调用错误）仍可能导致执行失败。
2. **依赖LLM基本能力边界**：若LLM无法理解某些领域术语（如物理模型），则整个链条崩溃。
3. **当前仅支持离散动作空间**（DQN），尚未扩展至连续控制（如PPO、SAC）。
4. **超参数未自动优化**：学习率、buffer size等仍固定，影响泛化能力。

---

### 🔮 未来工作方向
1. **细化编码Agent**：拆分出独立的“环境构建Agent”和“训练循环Agent”，提升代码健壮性。
2. **集成自适应超参数调节机制**：结合贝叶斯优化或Meta-RL进行动态调参。
3. **融合领域先验知识**：引入结构化知识图谱或领域模板，辅助LLM理解专业术语。
4. **支持连续动作空间与高级RL算法**：拓展至机器人控制等更广泛场景。
5. **人机协同接口优化**：当置信度低时，主动发起交互式澄清对话。

---

## 总结

📌 **A-LAMP 是迈向全自动强化学习工程的重要一步**。它不仅提升了从自然语言到可执行策略的转化效率，更重要的是建立了**可解释、可验证、可迭代的自动化RL流水线**。其实验充分证明：**合理的任务分解 + 多Agent协作 + 反馈机制 = 更可靠、更高成功率的智能决策系统构建方式**。

这一框架为RL在工业自动化、网络调度、供应链优化等领域的快速落地提供了强有力的工具支撑。

</details>

---

### 10. [TriFlow: A Progressive Multi-Agent Framework for Intelligent Trip Planning](https://arxiv.org/abs/2512.11271)

**Authors**: Yuxing Chen, Basem Suleiman, Qifan Chen  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11271v1  

#### Abstract
Real-world trip planning requires transforming open-ended user requests into executable itineraries under strict spatial, temporal, and budgetary constraints while aligning with user preferences. Existing LLM-based agents struggle with constraint satisfaction, tool coordination, and efficiency, ofte...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TriFlow: A Progressive Multi-Agent Framework for Intelligent Trip Planning — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现实世界中的 **trip planning** 是一个复杂的任务，涉及自然语言理解、时空约束满足和多目标优化。用户通常以开放、模糊的语言表达需求（如“有趣的地方”、“当地美食”），而生成的行程必须严格满足时间、空间、预算和偏好等硬性约束。

现有的 **LLM-based Agent** 方法存在以下问题：
- 容易产生 **hallucinations**（幻觉）
- 频繁违反现实世界的 **hard constraints**
- 工具调用效率低，token 成本高
- 缺乏系统性的约束管理和可解释性

这些问题导致生成的行程不可行或成本过高，难以实际部署。

---

### 提出的新方法：TriFlow 框架

TriFlow 是一种 **progressive multi-agent framework**，采用三阶段流水线设计，将 trip planning 分解为三个逐步收敛的阶段：

1. **Retrieval（检索）**
   - 将用户自然语言请求分解为结构化查询
   - 并行检索航班、住宿、景点、餐厅等实体
   - 进行去重与完整性校验（geometry, time window, price consistency）

2. **Planning（规划）**
   - 在检索到的事实子集中构建可行行程
   - 采用 **coarse-to-fine** 结构：先确定城市顺序与每日时间窗口，再填充细节
   - 引入 **agent-validator loop**：每一步建议 → 验证 → 归一化
   - 遵循 **monotonic feasibility principle**：一旦满足的约束不能在后续步骤中被破坏

3. **Governance（治理）**
   - 对初步可行行程进行有界迭代优化
   - 每轮生成系统报告（预算使用、时间一致性、偏好匹配）
   - 提出针对性调整（替换昂贵项目、解决冲突、提升个性化）
   - 最大迭代次数限制为 8 次，确保收敛性和效率

---

### 相比现有方法的优势

| 维度 | TriFlow 的优势 |
|------|----------------|
| **可行性保障** | 显式嵌入 constraint satisfaction 作为首要决策层，确保所有输出都在可行域内 |
| **效率提升** | 通过阶段性剪枝显著缩小搜索空间，运行速度比当前 SOTA 快 **10×以上** |
| **可解释性与可控性** | 模块化设计支持可观测中间状态，便于调试与审计 |
| **个性化与灵活性** | LLM 在规则框架内进行细节润色，兼顾语言灵活性与结构安全性 |
| **系统鲁棒性** | 闭环 “generate-verify-assemble-recompute” 流程增强容错能力 |

---

## 2. 核心实验方法和设置

### 使用的数据集

- **TravelPlanner**（验证集）  
  包含 180 个实例，涵盖真实旅行场景，强调 **hard-constraint satisfaction** 和可验证指标。

- **TripTailor**  
  大规模真实世界基准，扩展了 POI 覆盖范围和个性化维度，测试模型在复杂环境下的泛化能力。

> 两个数据集均提供官方评估协议和脚本化 checker，保证结果可复现。

---

### 实验设置

| 设置项 | 配置说明 |
|--------|----------|
| **Base Model** | TravelPlanner 使用 GPT-4o；TripTailor 使用 GPT-4o-mini（公平比较） |
| **Temperature 调度** | 分阶段控制创造性：<br>• Retrieval: 0.0（确定性）<br>• Planning: 0.3<br>• Governance: 0.6 |
| **最大迭代次数** | Governance 阶段最多执行 8 次 refine 循环 |
| **评估方式** | 严格遵循官方 protocol，使用 scripted checker 自动评分 |

---

### 评估指标

| 指标 | 含义 |
|------|------|
| **Delivery Rate** | 成功交付有效行程的比例 |
| **Commonsense Pass Rate**（Micro/Macro） | 行程是否符合常识逻辑（如不跨城吃早餐） |
| **Hard Constraint Pass Rate**（Micro/Macro） | 是否满足预算、房型、禁烟等硬性要求 |
| **Feasibility / Rationality**（TripTailor） | 可行性与合理性综合评分 |
| **Final Pass Rate (FPR)** | 所有检查全部通过的最终成功率（主指标） |

---

### 基线方法对比

| 基线方法 | 来源 | 特点 |
|---------|------|------|
| **FormalVerify** [3] | 当前 SOTA | 使用形式化验证工具辅助 LLM 规划 |
| **Workflow** [5] | TripTailor 原文提出 | 基于流程的工作流系统 |
| **Direct** [5] | TripTailor 基线 | 直接 prompt LLM 生成行程 |

---

## 3. 主要实验结果和性能指标

### 在 TravelPlanner 上的结果（Table 1）

| Method | Commonsense (Macro) | Hard Constraint (Macro) | FPR |
|--------|---------------------|--------------------------|-----|
| FormalVerify | 95.0% | 98.9% | **93.3%** |
| **TriFlow (ours)** | **95.0%** | **96.1%** | **91.1%** |

- TriFlow 在 FPR 上略低于 FormalVerify，但差距很小（91.1% vs 93.3%）
- **运行时间仅 22.6s**，相比 FormalVerify 的 245.7s，**提速超过 10.9×**

> 在 Easy/Medium/Hard 难度上 FPR 分别为 96.7%/95.0%/80.0%，显示其在高约束密度下仍保持稳定表现。

---

### 在 TripTailor 上的结果（Table 2）

| Method | Feasibility (Macro) | Rationality (Macro) | FPR |
|--------|---------------------|----------------------|-----|
| Workflow | 97.3% | 63.7% | 63.3% |
| Direct | 96.6% | 22.6% | 21.5% |
| **TriFlow (ours)** | **99.1%** | **97.7%** | **97.7%** |

- **FPR 提升超 34%**，远超最强 workflow 基线
- 所有维度指标均 ≥97.6%，包括：
  - Budget Compliance: **99.9%**
  - Duration Consistency: **97.6%**
  - Restaurant & Attraction Diversity: **≥99.7%**
  - Complete Information: **99.9%**

---

### 消融分析与关键发现（Table 3 & Discussion）

| 维度 | 改进效果 |
|------|--------|
| **Within Sandbox** | 从 ~33–50% 提升至 **>95–100%**<br>→ 检索阶段有效过滤无关/错误数据 |
| **Structural Constraints**（如 Reasonable City Route） | 接近或超过 **98%**<br>→ planning 阶段的 monotonic feasibility 设计有效 |
| **Budget Compliance** | 从 4–10% 提升至 **>95%**<br>→ governance 阶段成功修复残余违规 |
| **Room Rule / Cuisine / Room Type** | 全部达到 **>95%** 通过率<br>→ 显示对高度纠缠的 hard constraints 的处理能力 |

> **Final Pass Rate** 在 Hard 难度下从接近 0% 提升至 **80.0%**，证明 TriFlow 在复杂条件下依然能产出可执行行程。

---

## 4. 关键结论和发现

### 主要发现

1. **“feasibility-first” 设计至关重要**  
   将 constraint satisfaction 置于优化之前，避免后期无法修正的根本性错误。

2. **阶段性渐进收缩（progressive narrowing）优于端到端生成**  
   三阶段架构实现了搜索空间的有效剪枝，提升了效率与稳定性。

3. **Rule-LLM 协作模式更优**  
   规则系统负责骨架构建与约束保障，LLM 负责细节润色与个性化表达，实现安全与灵活的平衡。

4. **bounded iterative refinement 提升质量而不牺牲效率**  
   有限次迭代即可解决残余问题，避免无限循环或性能退化。

---

### 方法的局限性

- **依赖高质量外部数据源**：若 retrieval 阶段获取的信息不全或不准，会影响整体结果。
- **对极端模糊请求处理能力未知**：未测试完全无结构输入（如“我想散心”）的表现。
- **尚未接入实时动态数据**：目前基于静态数据库，未考虑天气变化、票价波动等现实扰动。

---

### 未来工作方向

1. **从离线评估转向在线环境部署**  
   接入 live data retrieval，测试在动态条件下的鲁棒性。

2. **增强用户交互与偏好演化建模**  
   支持多轮反馈与偏好更新，实现持续优化。

3. **扩展至其他规划任务**  
   如会议安排、物流调度等具有类似约束结构的任务领域。

4. **轻量化版本适配移动端应用**  
   利用 GPT-4o-mini 类高效模型推动实用落地。

---

> ✅ **总结一句话**：TriFlow 通过 **structured reasoning + LLM flexibility + progressive constraint enforcement** 的三阶段设计，在保证行程可行性的前提下大幅提升了效率与个性化水平，是迈向可靠 LLM-driven planning 的重要一步。

</details>

---

### 11. [PIAST: Rapid Prompting with In-context Augmentation for Scarce Training data](https://arxiv.org/abs/2512.11013)

**Authors**: Pawel Batorski, Paul Swoboda  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11013v1  

#### Abstract
LLMs are highly sensitive to prompt design, but handcrafting effective prompts is difficult and often requires intricate crafting of few-shot examples. We propose a fast automatic prompt construction algorithm that augments human instructions by generating a small set of few shot examples. Our metho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# PIAST: Rapid Prompting with In-context Augmentation for Scarce Training Data — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）对**提示设计（prompt design）高度敏感**，但手动设计高质量提示（如 few-shot examples）费时且需要专业知识。现有自动提示生成方法存在两大瓶颈：
- **计算成本高**：如 PRL 需要数十小时；
- **依赖大量训练数据**：许多方法需完整训练集进行评估；
- **忽略 few-shot examples 的优化**：多数方法仅优化指令文本（instruction），而未系统生成或优化上下文示例。

### ✅ 提出的新方法：PIAST
提出 **PIAST**（Prompt Improvement with Augmented SHapley Tuning），一种**快速、数据高效**的自动提示构造算法，核心思想是：
- 在人工指令基础上，**自动生成少量高质量的 in-context few-shot examples**；
- 使用 **Monte Carlo Shapley 值估计** 来评估每个示例的边际贡献，从而决定是否**替换、删除或保留**该示例；
- 通过迭代优化提升提示质量。

### ✅ 相比现有方法的优势
| 特性 | PIAST | APE / EvoPrompt | APO | PRL |
|------|-------|------------------|-----|-----|
| 自动生成 new examples | ✅ | ❌ | ❌（仅从训练集选） | ✅ |
| 迭代优化 | ✅ | ❌ | ✅ | ✅ |
| 快速执行（<1 小时） | ✅ | ✅ | ❌（~6h） | ❌（~48h） |
| 数据高效（小样本） | ✅ | ✅ | ✅ | ❌ |
| 支持 Shapley 值选择 | ✅ | ❌ | ❌ | ❌ |

> ✅ **PIAST 是首个兼具“快速”、“自动生成新示例”、“数据高效”的自动提示方法**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在四大任务上进行了广泛评估：

| 任务 | 数据集 | 类型 |
|------|--------|------|
| **Text Classification** | SST-2, CR, MR, SST-5, AG's News, TREC, SUBJ | 多类别情感/主题分类 |
| **Text Simplification** | ASSET | 句子简化 |
| **Summarization** | SAMSUM | 对话摘要 |
| **Mathematical Reasoning** | GSM8K | 数学应用题 |

### ⚙️ 实验设置
- **模型**：主模型为 `Qwen2.5-7B-Instruct`，所有方法统一使用该模型以保证公平比较；
- **评估方式**：所有结果基于 **3 次运行平均值**，确保鲁棒性；
- **超参数**：使用**同一组固定超参数**跨任务运行（见 Table 12），体现方法通用性；
- **计算资源**：单张 NVIDIA A100 GPU。

### 📊 评估指标
| 任务 | 主要指标 |
|------|----------|
| Classification | Accuracy |
| Simplification | SARI（越高越好） |
| Summarization | ROUGE-1, ROUGE-2, ROUGE-L |
| GSM8K | Accuracy（exact match） |

### 🔁 基线方法对比
| 方法 | 简介 |
|------|------|
| **MI (Manual Instruction)** | 手工编写指令 |
| **APE** | 自动生成候选指令并筛选，无 few-shot 示例 |
| **APO** | 基于反馈迭代优化提示，可包含训练集中选取的 few-shot 示例 |
| **EvoPrompt / GA / DE** | 基于进化算法或遗传算法搜索提示 |
| **PRL** | 强化学习生成示例，性能强但极慢（~48h） |
| **PIAST / PIAST(E)** | 本文方法（中等预算 / 扩展预算） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### （1）Text Classification（Table 2）
| 方法 | 平均准确率（Avg） | 最快？ | 是否 SOTA？ |
|------|------------------|--------|------------|
| PIAST | **81.50** | ✅（最快之一） | 第二 |
| PIAST(E) | **82.79** | ❌（约 3.7h） | ✅（新 SOTA） |
| PRL | 82.14 | ❌（48h） | 否 |
| APO | 79.42 | ❌ | 否 |

> ✅ **PIAST(E) 在 AG's News 和 SUBJ 上达到新 SOTA**。

#### （2）Text Simplification（Table 3）
| 方法 | SARI | 时间（分钟） |
|------|------|-------------|
| PIAST | **54.52** | **18.14** |
| PIAST(E) | **55.06** | 389.78 |
| PRL | 52.26 | 2880.00 |
| APE | 45.33 | 35.69 |

> ✅ **PIAST 在简化任务上首次超越 PRL，且速度快 150 倍以上**。

#### （3）Summarization（Table 4）
| 方法 | ROUGE-1 | ROUGE-2 | ROUGE-L | 时间 |
|------|---------|---------|---------|------|
| PIAST | 41.13 | 16.07 | 36.74 | **34.48 min** |
| PIAST(E) | 42.13 | **16.83** | 37.37 | 737 min |
| PRL | 42.47 | 16.17 | 37.73 | 2880 min |

> ✅ **PIAST 排名第二，速度远超 PRL；PIAST(E) 在 ROUGE-2 上接近 SOTA**。

#### （4）GSM8K（Table 5）
| 方法 | 准确率 | 时间 |
|------|--------|------|
| PIAST | **91.65** | **80.26 min** |
| PIAST(E) | 92.12 | 1598 min |
| PRL | 86.15 | 2880 min |
| APE | 83.43 | 180 min |

> ✅ **PIAST 是当前自动提示方法中在 GSM8K 上表现最好且最快的**。

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）是否需要 Replace/Drop/Keep 循环？
- **PIAST(I)**：仅用初始生成示例，不迭代优化。
- 结果：在简单任务（如 SST-2）上表现尚可，但在复杂任务（如 SUBJ）上显著下降（差 16.41%）。
- ➡️ 表明**迭代优化对困难任务至关重要**。

#### （2）Shapley vs Leave-One-Out（LOO）
- 使用 LOO 替代 Shapley 值选择最差示例。
- 结果：在多数任务上相近，但在 **SUBJ 上差距明显（PIAST: 75.93 vs LOO: 69.73）**。
- ➡️ 表明 **Shapley 更能捕捉示例间的冗余与互补关系**，优于 LOO。

#### （3）Shapley 抽样次数 $K$
| $K$ | 准确率 | 时间 |
|-----|--------|------|
| 1 | 73.68 | 4.61 min |
| 3 | **75.93** | 8.25 min |
| 10 | 76.02 | 20.88 min |
| 50 | 76.32 | 83.90 min |

> ✅ **$K=3$ 即可获得良好性能/速度权衡**，更多抽样收益递减。

#### （4）跨模型迁移能力（Cross-model Inference）
- 在 Qwen 上训练提示，在 Mistral 上测试（SUBJ）。
- 结果：**PIAST 表现最强（72.87）**，而 PIAST(E) 因过拟合略有下降（68.75）。
- ➡️ 表明 **PIAST 具有良好的跨模型泛化能力**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **few-shot examples 比 instruction 重写更重要**：  
   相比反复改写指令，**精心构造的上下文示例才是提升性能的关键杠杆**。

2. **PIAST 实现了“anytime performance”**：  
   - 即使只运行几轮（小预算），也能取得竞争性结果；
   - 随着计算预算增加（如 150 轮），性能持续提升，最终达到 SOTA。

3. **速度与性能兼得**：  
   PIAST 在多个任务上**既快又准**，尤其适合低数据、低算力场景。

4. **Shapley 值有效指导示例选择**：  
   相比简单 LOO，Shapley 更准确识别冗余或有害示例，提升稳定性。

5. **无需大量调参**：  
   一套超参数通用于所有任务，体现方法鲁棒性和易用性。

---

### ⚠️ 局限性
1. **依赖基础 LLM 的生成能力**：  
   若 LLM 本身不擅长某任务（如专业医学推理），生成的示例可能质量不高。

2. **PIAST(E) 存在过拟合风险**：  
   在跨模型测试中表现下降，表明长时间优化可能导致对特定 evaluator 过拟合。

3. **未结合 instruction 重写**：  
   当前仅优化示例，未来可探索与 instruction 搜索联合优化。

---

### 🔮 未来工作方向
1. **结合 instruction 重写机制**：  
   将 PIAST 与 APE 或 EvoPrompt 结合，同时优化指令和示例。

2. **降低对 evaluator 的依赖**：  
   探索更轻量化的评估机制，减少对完整 LLM 的调用。

3. **扩展到多模态提示工程**：  
   将类似框架应用于图像-文本或多模态任务中的 prompt 构造。

4. **理论分析 Shapley 在 ICL 中的有效性**：  
   探索为何 Shapley 能更好建模示例间交互。

---

> 💡 **代码已开源**：https://github.com/Batorskq/PIAST

--- 

📌 **一句话总结**：  
**PIAST 证明了“高质量 few-shot 示例 + 快速 Shapley 优化”是实现高效、高性能自动提示工程的核心路径，尤其适用于训练数据稀缺的现实场景。**

</details>

---

### 12. [Mistake Notebook Learning: Selective Batch-Wise Context Optimization for In-Context Learning](https://arxiv.org/abs/2512.11485)

**Authors**: Xuanbo Su, Yingfang Zhang, Hao Luo, Xiaoteng Liu, Leo Huang  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11485v1  

#### Abstract
Large language models (LLMs) adapt to tasks via gradient fine-tuning (heavy computation, catastrophic forgetting) or In-Context Learning (ICL: low robustness, poor mistake learning). To fix this, we introduce Mistake Notebook Learning (MNL), a training-free framework with a persistent knowledge base...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Mistake Notebook Learning: Selective Batch-Wise Context Optimization for In-Context Learning 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在下游任务上的适应通常依赖两种范式：**基于梯度的监督微调（SFT）** 和 **无需训练的上下文学习（ICL）**。  
- **SFT** 虽然性能强，但计算开销大、易导致灾难性遗忘（catastrophic forgetting）。  
- **ICL** 轻量且无需参数更新，但对示例选择敏感，缺乏系统性从错误中学习的能力。

现有记忆增强方法（如 Memento、TFGO）存在两大根本缺陷：
1. **实例级噪声（Instance-Level Noise）**：基于单个错误轨迹的记忆容易过拟合细节，泛化能力差。
2. **无条件迭代更新（Unconditional Iterative Updates）**：盲目接受所有反馈，可能导致性能退化或停滞。

---

### **提出了什么新方法或新思路**

本文提出 **Mistake Notebook Learning (MNL)** ——一种无需训练的框架，通过维护一个持续演化的“错误笔记本”（Mistake Notebook），实现上下文优化中的系统性错误学习。

#### 核心思想：
- 将上下文优化视为**批处理级别的知识积累过程**，而非逐实例修正。
- 引入**选择性更新机制**，确保每次知识库更新都带来实证上的性能提升。

---

### **相比现有方法的优势**

| 特性 | MNL | 其他方法（如 Memento, TFGO） |
|------|-----|-----------------------------|
| **知识表示** | 结构化五元组（含 anti-patterns） | 存储原始错误实例或轨迹 |
| **抽象粒度** | 批次级错误模式聚合（batch-wise） | 实例级或单轨迹处理 |
| **更新机制** | 经验验证后选择性保留（empirical validation） | 贪婪式无条件更新 |
| **抗噪能力** | 高（通过批量抽象降噪） | 低（易受个别错误误导） |
| **泛化性** | 显式 anti-patterns 防止误用 | 缺乏适用边界控制 |

> ✅ MNL 在不修改模型参数的前提下，实现了接近 SFT 的性能，同时显著优于其他 training-free 方法。

---

## 2. 核心实验方法和设置

### **使用的数据集**

分为两类任务，覆盖不同推理模态与数据规模：

| 类别 | 数据集 | 描述 |
|------|--------|------|
| **数学推理** | **GSM8K** | 小学算术应用题，7.4K训练样本，标准 benchmark |
|              | **AIME 2024/2025** | 竞赛级数学题（美国奥数邀请赛），仅30测试样例，高难度 |
| **Text-to-SQL** | **Spider** | 跨领域文本转SQL，7K训练样本，复杂schema |
|                 | **KaggleDBQA** | Kaggle竞赛数据库问答，仅87训练样本，小样本挑战 |

> 实验设计涵盖 **大规模数据（large-data）** 与 **小样本场景（few-shot/small-data）**，验证方法鲁棒性。

---

### **实验设置和评估指标**

| 设置项 | 内容 |
|-------|------|
| **模型** | Qwen3-8B（开源）、DeepSeekV3.2（闭源前沿）、Qwen3-Max（闭源前沿） |
| **评估协议** | Pass@32 准确率（greedy decoding, temp=0.0），batch size=16 |
| **评价指标** | - 数学任务：normalized exact match（符号化后精确匹配）<br>- Text-to-SQL：execution accuracy（执行结果匹配） |
| **训练策略** | 单轮训练（single-epoch），避免跨轮次过拟合（cross-epoch overfitting） |
| **实现方式** | 自调优（Self-Tuning）为主，即 tuner model 与 target model 相同 |

---

### **基线方法对比**

| 基线方法 | 简介 |
|---------|------|
| **BASE** | Vanilla ICL，无任何上下文优化 |
| **Memento** | Memory-based agent，存储状态-动作-奖励轨迹进行检索 |
| **TFGO** | Training-Free Group Relative Policy Optimization，基于组内比较优化上下文 |
| **SFT** | Supervised Fine-Tuning，全参数微调作为上限参考 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 📊 数学推理任务（Table 1）

| Dataset | Model | BASE | TFGO | **MNL** |
|--------|--------|------|------|--------|
| GSM8K | Qwen3-8B | 91.8% | 91.2% | **93.9%** |
| AIME 2025 | Qwen3-8B | 23.0% | 23.0% | **30.0%** (+30% rel.) |
| AIME 2025 | DeepSeekV3.2 | 80.0% | 90.0% | **83.0%** |

> 🔥 MNL 在 GSM8K 上达到 **93.9%**，几乎追平 SFT 的 **94.3%**，差距仅 0.4%，且无需参数更新！

---

#### 💬 Text-to-SQL 任务（Table 2）

| Dataset | Model | BASE | Memento | TFGO | **MNL** |
|--------|--------|------|----------|-------|--------|
| KaggleDBQA | Qwen3-8B | 19.0% | 15.1% | 22.1% | **28.0%** (+47% rel.) |
| KaggleDBQA | DeepSeekV3.2 | 23.8% | 19.4% | 24.3% | **31.4%** |
| Spider | Qwen3-8B | 68.9% | 67.3% | 70.1% | **71.7%** |

> 🚀 在 KaggleDBQA 上，MNL 相比最强 baseline TFGO 提升明显，尤其在资源受限的小样本环境下表现卓越。

---

### **与 Supervised Fine-Tuning (SFT) 对比**

| 任务 | 方法 | 性能 | 成本（美元） |
|------|------|------|------------|
| GSM8K | SFT | **94.3%** | $1.98 |
|       | MNL | 93.9% | **$0.99** |
| Spider | SFT | **79.0%** | $3.32 |
|        | MNL | 71.7% | **$1.98** |

> 💡 MNL 在 GSM8K 上以 **40% 更低成本** 达到接近 SFT 的性能；在 Spider 上虽落后，但仍大幅超越 base 模型。

---

### **消融实验结果**

#### ✅ 批大小（Batch Size）的影响（Figure 6）
- Batch size=1（实例级）：准确率 24.0%，KB 大小 69 条目
- Batch size=16（批级别）：准确率 **28.0%**，KB 大小降至 **23 条目**
- ➕ 提升 17% 相对性能，同时知识更紧凑，验证了 batch-wise 抽象的有效性。

#### ⚠️ 训练轮数影响（Figure 7）
- 单轮训练（Single-epoch）：测试集准确率最高（28.1%）
- 多轮训练：出现严重过拟合（第2轮测试下降至23.2%）
- ❗ 结论：context optimization 不等于 parameter optimization，需防止跨轮次过拟合。

#### 🔁 Self-Tuning vs Cross-Model Tuning（Table 3）
| 配置 | KaggleDBQA 准确率 |
|------|------------------|
| Self-Tuning（Qwen3-8B 自身生成指导） | 28.0% |
| Cross-Model Tuning（DeepSeekV3.2 作为 tuner） | **31.0%** |

> 虽然更强的 tuner 模型能生成更好指导，但 self-tuning 仍具竞争力，说明 MNL 可用于资源受限场景下的自我改进。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **上下文质量比数量更重要**：通过 batch-wise 错误抽象提取通用模式，可显著提升 ICL 的稳定性与泛化能力。
2. ✅ **结构化知识表示至关重要**：引入 anti-patterns 明确限制适用范围，有效防止错误迁移和过度泛化。
3. ✅ **选择性更新保障单调提升**：经验验证机制确保知识库只增益不退步，解决传统贪婪更新的风险。
4. ✅ **MNL 接近 SFT 性能**：在 GSM8K 上达到 93.9%，逼近 SFT 的 94.3%，且成本降低约 50%。
5. ✅ **特别适合小样本场景**：在 AIME 和 KaggleDBQA 上相对提升高达 30–47%，展现出强大泛化潜力。

---

### **方法的局限性**

1. **语义不对称问题**：查询是具体实例，而 subject 是抽象描述，embedding 匹配可能失败。
2. **分布外泛化有限**：知识库仅捕获训练分布内的错误模式，对 OOD 问题帮助较小。
3. **依赖高质量聚类**：subject clustering 效果直接影响错误归因与指导生成质量。
4. **无法完全替代 SFT**：在大规模充足数据下，SFT 仍有性能优势（如 Spider 上 79.0% vs 71.7%）。

---

### **未来工作方向**

- 探索动态调整 batch size 或 subject granularity 的自适应机制。
- 引入多模态或外部工具辅助错误分析与指导生成。
- 扩展至 agent-based planning、long-horizon reasoning 等复杂任务。
- 研究如何将 MNL 与轻量微调（LoRA, Prefix-tuning）结合，形成 hybrid adaptation pipeline。

---

## 总结

📌 **MNL 是一次对 ICL 范式的深刻重构**：它不再只是“拼接示例”，而是构建一个可进化、有判断力、带刹车系统的“错误笔记本”。  
🎯 通过 **structured abstraction + batch-wise aggregation + selective validation**，MNL 实现了 training-free 方法在复杂推理任务上向 SFT 的逼近，为资源受限场景提供了极具吸引力的替代方案。

> 🔗 开源地址：[https://github.com/Bairong-Xdynamics/MistakeNotebookLearning](https://github.com/Bairong-Xdynamics/MistakeNotebookLearning)

</details>

---

### 13. [Evaluation Framework for Centralized and Decentralized Aggregation Algorithm in Federated Systems](https://arxiv.org/abs/2512.10987)

**Authors**: Sumit Chongder  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.10987v1  

#### Abstract
In recent years, the landscape of federated learning has witnessed significant advancements, particularly in decentralized methodologies. This research paper presents a comprehensive comparison of Centralized Hierarchical Federated Learning (HFL) with Decentralized Aggregated Federated Learning (AFL...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Evaluation Framework for Centralized and Decentralized Aggregation Algorithm in Federated Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该研究聚焦于**联邦学习（Federated Learning, FL）系统中中心化与去中心化聚合算法的性能对比问题**。传统 **Centralized Hierarchical FL (HFL)** 存在以下挑战：
- **通信瓶颈**：所有客户端需与中央服务器频繁通信；
- **隐私风险**：数据更新集中处理可能泄露敏感信息；
- **可扩展性差**：难以适应大规模、异构设备网络。

### 🚀 提出的新方法与思路
论文系统比较了三种主流FL范式：
- **HFL（Hierarchical FL）**：层级式中心化架构；
- **AFL（Aggregated FL）**：去中心化的模型聚合机制；
- **CFL（Continual FL）**：支持持续学习的去中心化框架。

其核心创新在于：
- 构建了一个**统一的评估框架**，用于横向比较不同FL架构在相同条件下的表现；
- 强调并验证了**Decentralized Aggregation**在提升精度、降低延迟方面的有效性；
- 探索了**去中心化架构如何通过分布式协作实现高效且隐私保护的模型训练**。

### 🔍 相比现有方法的优势
- **更高的性能指标**：AFL 和 CFL 在多个评估维度上显著优于 HFL；
- **更强的鲁棒性和可扩展性**：避免单点故障，适合边缘计算场景；
- **更优的时间效率**：尤其在分类时间（Classification Time）方面，CFL 表现突出；
- **兼顾隐私与性能**：无需依赖中央服务器进行全局聚合，增强数据安全性。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **MNIST**：手写数字图像数据集（10类，28×28灰度图）；
- **Fashion-MNIST**：服饰图像数据集（10类，28×28灰度图），更具现实挑战性。

两个均为公开基准数据集，广泛用于图像分类任务。

### ⚙️ 实验设置
- 所有模型采用相同的 **CNN 架构**：
  - 三层卷积层（16@3×3, 12@3×3, 10@3×3）
  - 两层 Max-Pooling
  - ReLU 激活函数
  - 全连接输出层
- 数据分布为 **IID（独立同分布）**
- 使用 **FedAvg 聚合算法** 进行参数更新
- 实验工具链：Python 3.11.4, TensorFlow 2.16.1, Keras 3.1.1, Jupyter Notebook

### 📈 评估指标
| 指标 | 定义 |
|------|------|
| **Accuracy** | 正确预测样本占比 |
| **Precision** | 预测为正类中实际为正的比例 |
| **Recall** | 实际正类中被正确识别的比例 |
| **F1 Score** | Precision 与 Recall 的调和平均 |
| **Build Time (s)** | 模型训练总耗时 |
| **Classification Time (s)** | 推理阶段耗时 |

### 🔁 基线方法对比
- **Hierarchical FL (HFL)**：作为中心化代表基线；
- **Aggregated FL (AFL)**：去中心化聚合；
- **Continual FL (CFL)**：支持持续学习的去中心化架构。

三者在同一实验环境下进行公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1 & Table 2）

#### 在 **Fashion-MNIST** 上的表现：

| Environment | Testing Acc | Precision | Recall | F1 Score | Build Time (s) | Class. Time (s) |
|------------|-------------|-----------|--------|----------|----------------|------------------|
| HFL        | 0.41        | 0.41      | 0.33   | 0.40     | 86.11          | 0.57             |
| AFL        | 0.70        | 0.71      | 0.68   | 0.68     | 55.33          | 0.47             |
| CFL        | **0.88**    | **0.88**  | **0.87**| **0.86**| 80.07          | **0.31**         |

#### 在 **MNIST** 上的表现：

| Environment | Testing Acc | Precision | Recall | F1 Score | Build Time (s) | Class. Time (s) |
|------------|-------------|-----------|--------|----------|----------------|------------------|
| HFL        | 0.60        | 0.75      | 0.60   | 0.59     | 88.26          | 0.55             |
| AFL        | 0.72        | 0.76      | 0.72   | 0.72     | **54.02**      | 0.52             |
| CFL        | **0.98**    | **0.98**  | **0.98**| **0.98**| 79.02          | **0.29**         |

> 注：最佳值已加粗显示。

### 🔍 对比分析结果
- **精度方面**：
  - CFL 在两个数据集上均取得最高测试准确率（Fashion-MNIST: 88%, MNIST: 98%），远超 HFL；
  - AFL 显著优于 HFL，但略低于 CFL；
- **效率方面**：
  - **AFL 拥有最短 Build Time**（~54–55秒），体现其快速收敛能力；
  - **CFL 分类时间最短**（~0.3秒），适合低延迟推理场景；
- **综合性能**：
  - CFL 在 Precision、Recall、F1 Score 上全面领先，表明其具备优秀的泛化能力和稳定性。

### ❌ 是否有消融实验？
论文未明确开展消融实验（ablation study），但通过对三种架构的整体性能对比，间接揭示了“去中心化”与“持续学习”机制对性能的影响。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **去中心化 FL 架构显著优于中心化 HFL**：
   - 在 Accuracy、Precision、Recall、F1 Score 等关键指标上全面胜出；
   - 尤其在复杂数据集（如 Fashion-MNIST）上优势更为明显。

2. **Decentralized Aggregation 是性能提升的关键机制**：
   - 支持设备间直接通信与参数融合，减少对中央服务器的依赖；
   - 提高系统鲁棒性与隐私保障水平。

3. **CFL 是最具潜力的范式**：
   - 结合 Continual Learning 思想，在长期任务中保持高性能；
   - 同时具备较低的 Classification Time，适用于实时应用。

4. **AFL 平衡了速度与性能**：
   - Build Time 最短，适合资源受限环境下的快速部署；
   - 性能稳定，是实用性强的折中方案。

### ⚠️ 局限性
- 实验基于 **IID 数据分布假设**，未充分考虑现实中常见的 **Non-IID 场景**；
- 缺乏对通信开销、带宽消耗等网络层面指标的量化分析；
- 未涉及设备异构性（如算力差异、掉线率）对训练过程的影响；
- 没有提供详细的能耗或资源占用数据。

### 🔮 未来工作方向（作者提出）
1. **探索多样化的数据分布组合**：研究 Non-IID、偏斜分布等真实场景下的性能表现；
2. **解决异构性与可扩展性问题**：优化去中心化网络中的客户端选择与负载均衡；
3. **引入个性化 Federated Learning**：支持各客户端根据本地需求定制模型；
4. **结合知识蒸馏或迁移学习**：进一步提升小样本场景下的训练效率。

---

## ✅ 总结
本论文通过系统的实验设计与多维评估，证明了 **Decentralized FL（特别是 CFL）在性能、效率和隐私保护方面的综合优势**。它不仅为联邦学习架构的选择提供了实证依据，也为未来构建高效、安全、可持续的分布式机器学习系统指明了方向。

</details>

---

### 14. [ECCO: Leveraging Cross-Camera Correlations for Efficient Live Video Continuous Learning](https://arxiv.org/abs/2512.11727)

**Authors**: Yuze He, Ferdi Kossmann, Srinivasan Seshan, Peter Steenkiste  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11727v1  

#### Abstract
Recent advances in video analytics address real-time data drift by continuously retraining specialized, lightweight DNN models for individual cameras. However, the current practice of retraining a separate model for each camera suffers from high compute and communication costs, making it unscalable....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ECCO: Leveraging Cross-Camera Correlations for Efficient Live Video Continuous Learning —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于边缘服务器的**实时视频连续学习系统**（live video continuous learning）面临严重的资源效率瓶颈。主流方法（如 Ekya、RECL）采用“独立重训练”（independent retraining）策略，即为每个摄像头单独重训练一个轻量级 DNN 模型以应对数据漂移（data drift）。这种做法导致：
- **计算冗余**：多个摄像头经历相似的数据漂移时，重复训练多个模型造成 GPU 资源浪费；
- **通信开销大**：每个摄像头需独立上传大量视频帧，加剧带宽压力；
- **扩展性差**：随着摄像头数量增加，系统难以维持低延迟和高精度。

### 🚀 提出的新方法与核心思想
论文提出 **ECCO**（Efficient Cross-Camera Optimization），一种全新的视频分析框架，其核心创新是 **group retraining（组内联合重训练）**，通过利用跨摄像头之间的**数据漂移相关性**来提升资源效率。

#### 主要技术贡献包括三个模块：
1. **轻量级动态分组算法**（Lightweight Grouping Algorithm）
   - 利用元数据（时间、地理位置）快速筛选候选摄像头；
   - 基于实际模型准确率增益决定是否合并到已有组中；
   - 支持周期性重新评估与动态调整组成员（regrouping）。

2. **公平且高效的 GPU 分配器**（GPU Allocator）
   - 提出新的优化目标函数，平衡整体性能与各组间的公平性；
   - 避免传统方法偏向大组而忽视小组的问题；
   - 使用微窗口（micro-window）调度机制实现贪婪资源分配。

3. **资源感知的传输控制器**（Transmission Controller）
   - 动态配置采样参数（frame rate 和 resolution）以匹配 GPU 预算；
   - 设计定制化的 **GAIMD 拥塞控制算法**，使带宽分配与 GPU 分配有比例关系，实现“计算-通信协同优化”。

### 🔍 相比现有方法的优势
| 维度 | ECCO | 现有方法（如 Ekya, RECL） |
|------|------|--------------------------|
| 模型训练方式 | 多摄像头共享模型（group retraining） | 每个摄像头独立训练（independent retraining） |
| 资源利用率 | 显著降低 GPU 和带宽消耗 | 存在大量冗余计算和通信 |
| 公平性 | 显式优化小规模组的准确性 | 倾向于优先服务大规模组 |
| 响应速度 | 更快收敛，尤其在低带宽下 | 受限于单流数据积累速度 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
论文在三个不同类型的公开或仿真数据集上进行了广泛评估：

| 数据集 | 类型 | 描述 |
|--------|------|------|
| **CityFlow** | 固定交通摄像头 | 包含 40 个摄像头在 10 个城市路口录制的同步视频，用于 object detection 任务 |
| **MDOT** | 移动无人机摄像头 | 来自五架编队飞行无人机拍摄的视频片段，体现移动场景下的时空相关性 |
| **CARLA** | 自主驾驶模拟器生成 | 在 Unreal Engine 上构建的城市环境，可灵活控制摄像头布局与相似度水平，用于可扩展性和消融研究 |

### ⚙️ 实验设置
- **任务类型**：object detection（YOLO11n/x） 和 instance segmentation（YOLO11n/x-Seg）
- **部署架构**：边缘设备运行推理，边缘服务器负责重训练
- **重训练触发机制**：本地检测到数据漂移后发送请求
- **资源约束**：有限 GPU 数量（最多 5×RTX 4090）、共享上行带宽（3–50 Mbps）

### 📈 评估指标
| 指标 | 定义 |
|------|------|
| **mAP**（mean Average Precision） | 衡量检测/分割任务的整体准确率 |
| **Response Time / Retraining Latency** | 达到目标 mAP 所需的时间 |
| **Scalability** | 在固定资源下支持的最大摄像头数 |
| **Fairness** | 不同组之间准确率差异程度 |

### 🔁 对比的基线方法
| 基线 | 特点 |
|------|------|
| **Naive Baseline** | 均匀分配 GPU，固定采样配置，平均共享带宽 |
| **Ekya** | 动态 GPU 调度，但仍独立训练每个模型 |
| **RECL** | 使用历史模型初始化（model zoo），提升收敛速度，仍为独立训练 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 指标 | ECCO 提升幅度 |
|------|---------------|
| **mAP 提升（相同资源）** | +6.7% ~ 18.1% |
| - Object Detection | +6.7% ~ 16.6% |
| - Instance Segmentation | +9.3% ~ 18.1% |
| **支持并发摄像头数** | **3.3× 更多**（相同准确率） |
| **响应时间缩短（低带宽下）** | **>5× 更快** |
| **所需带宽（达到相同精度）** | 仅需基线的 **25%–33%** |

### 🔁 与基线方法的对比结果
- 在 **4 GPU + 6 Mbps 带宽**条件下，ECCO 在 CityFlow 上将 object detection mAP 从约 35% 提升至接近 40%，而基线需要更多 GPU 才能达到同等效果。
- 在 **22-camera 可扩展性测试**中，当摄像头数量上升时，所有基线方法性能急剧下降，而 ECCO 下降更缓，**在相同资源下支持 3.3× 更多摄像头**。
- 在低带宽（<200 Kbps）环境下，ECCO 的响应时间仅为基线的 **41.3%–61.1%**，优势显著。

### 🔍 消融实验结果（Ablation Study）

#### （1）动态分组有效性（Fig. 9）
- 展示了三辆移动车辆上的摄像头如何因路径变化被动态拆分出原组；
- 验证了 ECCO 能及时识别分布偏移并进行 regrouping，避免错误聚合。

#### （2）GPU 分配器公平性（Fig. 10）
- 使用 RECL 的分配器会导致小规模组（1 camera）长期饥饿，准确率落后高达 23% mAP；
- ECCO 的分配器实现了近似同步的准确率增长，兼顾整体性能与公平性。

#### （3）传输控制器有效性（Fig. 11）
- 关闭控制器时，即使带宽充足也无法充分利用 GPU；
- 启用后，在 **仅 1/3 带宽**下即可达到相近甚至更高的 mAP；
- 实现了接近理想的 **GPU-proportional bandwidth allocation**，尤其在异构网络中表现稳健。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **跨摄像头数据漂移具有强时空相关性**，尤其是在地理邻近或运动一致的场景中（如无人机编队、城市交叉口），这为模型共享提供了理论基础。
2. **group retraining 是高效连续学习的关键范式转变**：通过聚合相似漂移的摄像头数据训练共享模型，大幅减少冗余计算和通信开销。
3. **计算与通信必须联合优化**：ECCO 证明了 GPU 分配与带宽分配的耦合设计能显著提升端到端效率。
4. **ECCO 在低带宽、高负载场景下优势最明显**：得益于自然的模型复用（natural model reuse）和数据聚合效应，响应速度大幅提升。

### ⚠️ 方法的局限性
- **依赖漂移相关性的存在**：若摄像头间无显著相关性（如完全异构场景），group retraining 效果有限（见 Fig. 8，low-similarity 场景增益很小）；
- **分组决策引入额外延迟**：虽然整体延迟更低，但初始 grouping 需要一定处理时间；
- **对拥塞控制协议有一定假设**：依赖 GAIMD 的稳态行为，在极端复杂网络拓扑中可能偏离理想分配。

### 🔮 未来工作方向
- 将 ECCO 扩展至更多视觉任务（如 action recognition, tracking）；
- 探索非 GAIMD 的更先进拥塞控制机制以进一步逼近最优带宽分配；
- 引入预测机制提前预判漂移趋势，实现 proactive grouping；
- 结合联邦学习思想，在保护隐私的前提下实现跨域协作。

---

> **总结一句话**：  
> ECCO 通过洞察并利用跨摄像头数据漂移的相关性，提出了 group retraining 新范式，并通过 GPU 分配、传输控制与动态分组三大模块协同优化，实现了**更高精度、更强扩展性、更快响应速度**的实时视频连续学习系统，代表了该领域的重要进步。

</details>

---

### 15. [Refining Graphical Neural Network Predictions Using Flow Matching for Optimal Power Flow with Constraint-Satisfaction Guarantee](https://arxiv.org/abs/2512.11127)

**Authors**: Kshitiz Khanal  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11127v1  

#### Abstract
The DC Optimal Power Flow (DC-OPF) problem is fundamental to power system operations, requiring rapid solutions for real-time grid management. While traditional optimization solvers provide optimal solutions, their computational cost becomes prohibitive for large-scale systems requiring frequent rec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Refining Graphical Neural Network Predictions Using Flow Matching for Optimal Power Flow with Constraint-Satisfaction Guarantee

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对 **DC Optimal Power Flow (DC-OPF)** 问题中传统优化求解器计算耗时高、而机器学习方法常存在**约束不满足**和**成本非最优**的问题，提出了一种兼顾**速度、可行性与经济性**的新型两阶段学习框架。

### 提出的新方法与创新思路
作者提出了一个**两阶段物理信息驱动的学习框架**，结合 **Graph Neural Networks (GNNs)** 与 **Continuous Flow Matching (CFM)**：

- **Stage 1: 物理信息 GNN（Physics-Informed GNN）**
  - 设计了一个基于图卷积网络（GCN）的模型，直接将电力系统拓扑结构作为输入。
  - 在训练目标中嵌入了多个物理规律与最优性条件：
    - 经济调度原则（边际成本相等）
    - 基尔霍夫定律（功率平衡）
    - 发电机上下限约束
    - KKT 互补条件（Karush-Kuhn-Tucker）
  - 引入**可微投影层（differentiable projection）** 和 **渐进课程学习（progressive curriculum learning）**，逐步从“满足约束”过渡到“逼近最优”。

- **Stage 2: 连续流匹配精炼（CFM Refinement）**
  - 首次将 **Continuous Flow Matching (CFM)** 应用于 OPF 优化任务，将其视为从可行解向最优解的“条件生成”过程。
  - 利用 CFM 学习一个向量场（vector field），通过 ODE 积分将 Stage 1 输出的初始可行解 $ p_g^{(0)} $ 映射至接近最优的解 $ p_g^* $。
  - 在精炼阶段引入额外的物理感知损失函数，确保在提升经济性的同时保持可行性。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **可行性保障** | 通过软/硬投影机制和物理正则项，实现 **100% 可行性**，优于多数纯监督 ML 方法。 |
| **成本最优性** | CFM 精炼显著缩小成本差距，在标称负载下达到 **<0.1% 成本间隙**，远优于仅使用 GNN 的基线。 |
| **泛化能力** | 借助物理先验而非单纯拟合数据，模型在训练分布外（如 70% 或 130% 负载）仍表现稳健。 |
| **训练效率** | CFM 采用回归式无模拟训练（simulation-free），避免传统 CNF 中昂贵的反向传播 ODE 求解，支持混合精度加速。 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用标准测试系统：**IEEE 30-bus system**
  - 包含 30 个节点（buses）、6 台发电机、41 条输电线路
  - 发电机参数见 Table I（成本系数、容量限制等）

### 实验设置
- **训练数据生成**：
  - 通过 SLSQP 求解器生成 20,000 个样本
  - 输入为负荷向量 $ p_d $
  - 输出为最优发电调度 $ p_g^* $ 和对应成本
  - 负荷范围：70%–100% 基准负荷（uniform sampling）

- **评估场景（共五类）**：
  | 场景 | 负载区间 | 是否超出训练分布 |
  |------|--------|----------------|
  | Very Low Load | 70%–75% | 是（外推） |
  | Low Load | 83%–88% | 否 |
  | Nominal Load | 95%–100% | 否 |
  | High Load | 110%–115% | 是（外推） |
  | Very High Load | 125%–130% | 是（压力测试） |

- **每类测试 100 个样本**

### 评估指标
| 指标 | 定义 |
|------|------|
| **Cost Gap (%)** | $\frac{C(\hat{p}_g) - C(p_g^*)}{C(p_g^*)} \times 100\%$ |
| **Feasibility Rate (%)** | 满足所有约束（误差 < 0.1 MW）的比例 |
| **Worst-Case Gap (%)** | 所有测试样本中的最大成本间隙 |

### 基线方法对比
- **SLSQP Solver**：作为最优基准（ground truth）
- **GNN Only**：仅 Stage 1 的物理信息 GNN，无 CFM 精炼
- **CFM Refined**：本文提出的完整两阶段方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table II）

| 场景 | GNN 成本间隙 (%) | CFM 成本间隙 (%) | CFM 可行性 (%) | 最差情况间隙 (%) |
|------|------------------|------------------|---------------|------------------|
| Very Low (70–75%) | 7.86 | **0.03** | 100.0 | 0.04 |
| Low (83–88%) | 9.60 | **0.05** | 100.0 | 0.05 |
| Nominal (95–100%) | 10.78 | **0.07** | 100.0 | 0.16 |
| High (110–115%) | 9.48 | **2.59** | 100.0 | 3.60 |
| Very High (125–130%) | 7.17 | **2.84** | 100.0 | 3.15 |

> ✅ 所有场景下均实现 **100% 可行性**

### 与基线方法对比（CFM vs GNN Only）
- 在标称负载下，CFM 将成本间隙从 **10.78% 降至 0.07%**，降幅达 **10.71 个百分点**
- 即使在极端负载条件下（125–130%），也能将平均成本间隙控制在 **2.84%** 内，最坏情况仅为 **3.15%**
- 如 Table III 所示，CFM 在所有场景下都带来显著改进：
  - 成本降低幅度：4.04% ~ 9.67%
  - 成本间隙减少：4.33 ~ 10.71 p.p.

### 消融实验与分析（隐含于训练动态）
- **Stage 1（GNN）**：经过物理信息训练后已能输出**完全可行**的解，但成本偏高（7–11% 间隙）
- **Stage 2（CFM）**：进一步精炼使成本大幅下降，验证了“**可行初始化 + 渐进优化路径学习**”的有效性
- **非单调收敛行为**（见 Fig. 3）表明 CFM 能有效平衡 flow matching 与 cost minimization 目标

---

## 4. 关键结论和发现

### 主要发现
1. **物理先验是关键**  
   将经济调度、KKT 条件、功率平衡等物理规律编码进损失函数，显著提升了模型的**泛化能力和约束满足率**，尤其在训练分布之外依然稳定。

2. **两阶段设计优势明显**  
   - GNN 擅长建模拓扑并保证可行性
   - CFM 擅长在可行域内沿最优路径进行精细化调整
   - 分工明确，协同增效

3. **CFM 首次成功应用于 OPF 优化**  
   将优化问题转化为“从可行解到最优解”的连续变换，利用 CFM 学习向量场，实现了高效且可导的精炼过程，具有较强的通用潜力。

4. **实用性强**  
   推理速度快（前向传播 + ODE 积分），适用于需要频繁更新调度方案的高比例可再生能源电网。

### 局限性
1. **仅限于 DC-OPF**  
   忽略了电压幅值、无功功率和非线性 AC 功率流方程，难以直接用于实际电网运行。

2. **规模受限**  
   当前实验基于 IEEE 30-bus 小型系统，尚未验证在大规模系统（如 1000+ buses）上的扩展性。

3. **静态负荷假设**  
   训练数据基于稳态负荷分布，未考虑突变事件或分布偏移下的鲁棒性。

4. **缺乏不确定性量化**  
   未提供预测置信度或风险边界，对安全关键应用构成挑战。

### 未来工作方向
- 扩展至 **AC-OPF**，整合电压与无功约束
- 开发**分层 GNN 架构**以应对超大规模电网
- 引入**在线学习机制**适应拓扑变化与参数漂移
- 构建**概率性 CFM 模型**或集成方法以实现不确定性估计
- 探索与其他物理信息方法（如 PINNs、Lagrangian 神经网络）的融合

---

> 🔗 **代码开源地址**：[https://github.com/kshitizkhanal7/flow_refiner_dcopf](https://github.com/kshitizkhanal7/flow_refiner_dcopf)

</details>

---

### 16. [High-Dimensional Surrogate Modeling for Closed-Loop Learning of Neural-Network-Parameterized Model Predictive Control](https://arxiv.org/abs/2512.11705)

**Authors**: Sebastian Hirt, Valentinus Suwanto, Hendrik Alsmeier, Maik Pfefferkorn, Rolf Findeisen  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11705v1  

#### Abstract
Learning controller parameters from closed-loop data has been shown to improve closed-loop performance. Bayesian optimization, a widely used black-box and sample-efficient learning method, constructs a probabilistic surrogate of the closed-loop performance from few experiments and uses it to select ...

---

### 17. [Softmax as Linear Attention in the Large-Prompt Regime: a Measure-based Perspective](https://arxiv.org/abs/2512.11784)

**Authors**: Etienne Boursier, Claire Boyer  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11784v1  

#### Abstract
Softmax attention is a central component of transformer architectures, yet its nonlinear structure poses significant challenges for theoretical analysis. We develop a unified, measure-based framework for studying single-layer softmax attention under both finite and infinite prompts. For i.i.d. Gauss...

---

### 18. [Deep Learning--Accelerated Multi-Start Large Neighborhood Search for Real-time Freight Bundling](https://arxiv.org/abs/2512.11187)

**Authors**: Haohui Zhang, Wouter van Heeswijk, Xinyu Hu, Neil Yorke-Smith, Martijn Mes  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.11187v1  

#### Abstract
Online Freight Exchange Systems (OFEX) play a crucial role in modern freight logistics by facilitating real-time matching between shippers and carrier. However, efficient combinatorial bundling of transporation jobs remains a bottleneck. We model the OFEX combinatorial bundling problem as a multi-co...

---

### 19. [When Actions Teach You to Think: Reasoning-Action Synergy via Reinforcement Learning in Conversational Agents](https://arxiv.org/abs/2512.11277)

**Authors**: Mrinal Rawat, Arkajyoti Chakraborty, Neha Gupta, Roberto Pieraccini  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.11277v1  

#### Abstract
Supervised fine-tuning (SFT) has emerged as one of the most effective ways to improve the performance of large language models (LLMs) in downstream tasks. However, SFT can have difficulty generalizing when the underlying data distribution changes, even when the new data does not fall completely outs...

---

### 20. [Agentic Operator Generation for ML ASICs](https://arxiv.org/abs/2512.10977)

**Authors**: Alec M. Hammond, Aram Markosyan, Aman Dontula, Simon Mahns, Zacharias Fisches, Dmitrii Pedchenko, Keyur Muzumdar, Natacha Supper, Mark Saroufim, Joe Isaacson, Laura Wang, Warren Hunt, Kaustubh Gondkar, Roman Levenstein, Gabriel Synnaeve, Richard Li, Jacob Kahn, Ajit Mathews  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10977v1  

#### Abstract
We present TritorX, an agentic AI system designed to generate functionally correct Triton PyTorch ATen kernels at scale for emerging accelerator platforms. TritorX integrates open-source large language models with a custom linter, JIT compilation, and a PyTorch OpInfo-based test harness. This pipeli...

---

### 21. [Bandwidth-constrained Variational Message Encoding for Cooperative Multi-agent Reinforcement Learning](https://arxiv.org/abs/2512.11179)

**Authors**: Wei Duan, Jie Lu, En Yu, Junyu Xuan  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.11179v1  

#### Abstract
Graph-based multi-agent reinforcement learning (MARL) enables coordinated behavior under partial observability by modeling agents as nodes and communication links as edges. While recent methods excel at learning sparse coordination graphs-determining who communicates with whom-they do not address wh...

---

### 22. [Pace: Physics-Aware Attentive Temporal Convolutional Network for Battery Health Estimation](https://arxiv.org/abs/2512.11332)

**Authors**: Sara Sameer, Wei Zhang, Kannan Dhivya Dharshini, Xin Lou, Yulin Gao, Terence Goh, Qingyu Yan  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.11332v1  

#### Abstract
Batteries are critical components in modern energy systems such as electric vehicles and power grid energy storage. Effective battery health management is essential for battery system safety, cost-efficiency, and sustainability. In this paper, we propose Pace, a physics-aware attentive temporal conv...

---

### 23. [FutureWeaver: Planning Test-Time Compute for Multi-Agent Systems with Modularized Collaboration](https://arxiv.org/abs/2512.11213)

**Authors**: Dongwon Jung, Peng Shi, Yi Zhang  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11213v1  

#### Abstract
Scaling test-time computation improves large language model performance without additional training. Recent work demonstrates that techniques such as repeated sampling, self-verification, and self-reflection can significantly enhance task success by allocating more inference-time compute. However, a...

---

### 24. [AgentBalance: Backbone-then-Topology Design for Cost-Effective Multi-Agent Systems under Budget Constraints](https://arxiv.org/abs/2512.11426)

**Authors**: Shuowei Cai, Yansong Ning, Hao Liu  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11426v1  

#### Abstract
Large Language Model (LLM)-based multi-agent systems (MAS) are becoming indispensable building blocks for web-scale applications such as web search, social network analytics, and online customer support, where cost-effectiveness is increasingly the primary constraint for large-scale deployment. Whil...

---

### 25. [TECM*: A Data-Driven Assessment to Reinforcement Learning Methods and Application to Heparin Treatment Strategy for Surgical Sepsis](https://arxiv.org/abs/2512.10973)

**Authors**: Jiang Liu, Yujie Li, Chan Zhou, Yihao Xie, Qilong Sun, Xin Shu, Peiwei Li, Chunyong Yang, Yiziting Zhu, Jiaqi Zhu, Yuwen Chen, Bo An, Hao Wu, Bin Yi  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.10973v1  

#### Abstract
Objective: Sepsis is a life-threatening condition caused by severe infection leading to acute organ dysfunction. This study proposes a data-driven metric and a continuous reward function to optimize personalized heparin therapy in surgical sepsis patients. Methods: Data from the MIMIC-IV v1.0 and eI...

---

### 26. [Harnessing Rich Multi-Modal Data for Spatial-Temporal Homophily-Embedded Graph Learning Across Domains and Localities](https://arxiv.org/abs/2512.11178)

**Authors**: Takuya Kurihana, Xiaojian Zhang, Wing Yee Au, Hon Yung Wong  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11178v1  

#### Abstract
Modern cities are increasingly reliant on data-driven insights to support decision making in areas such as transportation, public safety and environmental impact. However, city-level data often exists in heterogeneous formats, collected independently by local agencies with diverse objectives and sta...

---

### 27. [Task-Aware Multi-Expert Architecture For Lifelong Deep Learning](https://arxiv.org/abs/2512.11243)

**Authors**: Jianyu Wang, Jacob Nean-Hua Sheikh, Cat P. Le, Hoda Bidkhori  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11243v1  

#### Abstract
Lifelong deep learning (LDL) trains neural networks to learn sequentially across tasks while preserving prior knowledge. We propose Task-Aware Multi-Expert (TAME), a continual learning algorithm that leverages task similarity to guide expert selection and knowledge transfer. TAME maintains a pool of...

---

### 28. [Symmetry-Aware Steering of Equivariant Diffusion Policies: Benefits and Limits](https://arxiv.org/abs/2512.11345)

**Authors**: Minwoo Park, Junwoo Chang, Jongeun Choi, Roberto Horowitz  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11345v1  

#### Abstract
Equivariant diffusion policies (EDPs) combine the generative expressivity of diffusion models with the strong generalization and sample efficiency afforded by geometric symmetries. While steering these policies with reinforcement learning (RL) offers a promising mechanism for fine-tuning beyond demo...

---

### 29. [Bhargava Cube--Inspired Quadratic Regularization for Structured Neural Embeddings](https://arxiv.org/abs/2512.11392)

**Authors**: S Sairam, Prateek P Kulkarni  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11392v1  

#### Abstract
We present a novel approach to neural representation learning that incorporates algebraic constraints inspired by Bhargava cubes from number theory. Traditional deep learning methods learn representations in unstructured latent spaces lacking interpretability and mathematical consistency. Our framew...

---

### 30. [Hyperbolic Gaussian Blurring Mean Shift: A Statistical Mode-Seeking Framework for Clustering in Curved Spaces](https://arxiv.org/abs/2512.11448)

**Authors**: Arghya Pratihar, Arnab Seal, Swagatam Das, Inesh Chattopadhyay  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.11448v1  

#### Abstract
Clustering is a fundamental unsupervised learning task for uncovering patterns in data. While Gaussian Blurring Mean Shift (GBMS) has proven effective for identifying arbitrarily shaped clusters in Euclidean space, it struggles with datasets exhibiting hierarchical or tree-like structures. In this w...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
