# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-29 06:15:08 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [StreamFusion: Scalable Sequence Parallelism for Distributed Inference of Diffusion Transformers on GPUs](https://arxiv.org/abs/2601.20273)

**Authors**: Jiacheng Yang, Jun Wu, Yaoyao Ding, Zhiying Xu, Yida Wang, Gennady Pekhimenko  
**Category**: cs.DC  
**Published**: 2026-01-29  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2601.20273v1  

#### Abstract
Diffusion Transformers (DiTs) have gained increasing adoption in high-quality image and video generation. As demand for higher-resolution images and longer videos increases, single-GPU inference becomes inefficient due to increased latency and large activation sizes. Current frameworks employ sequen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《StreamFusion: Scalable Sequence Parallelism for Distributed Inference of Diffusion Transformers on GPUs》总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前在多GPU上进行 **Diffusion Transformers (DiTs)** 推理时，主流的 **Sequence Parallelism (SP)** 技术（如 Ulysses Attention 和 Ring Attention）存在以下三大瓶颈：

1. **通信模式未适配现代GPU网络拓扑**：未充分利用机内（intra-machine）高带宽（如NVSwitch）和机间（inter-machine）低带宽网络的差异。
2. **All-to-All操作无法与计算重叠**：Ulysses Attention 中的 All-to-All 操作是原子性的，导致通信无法与注意力计算并行，形成性能瓶颈。
3. **两方通信库（Two-Sided Communication）开销大**：依赖 NCCL 等库导致发送端和接收端必须严格同步，引入额外的同步和计算开销。

这些问题使得现有系统在扩展到多台GPU机器时，推理延迟显著增加，系统变为 **communication-bound**。

---

### **提出的新方法与创新点**

StreamFusion 提出了一套 **拓扑感知、通信可重叠、轻量同步** 的分布式推理引擎，包含三大核心技术：

#### **(1) 拓扑感知的序列并行（Topology-Aware Sequence Parallelism）**
- **新策略**：将 **Ulysses Attention 用于机间通信**，**Ring Attention 用于机内通信**。
- **优势**：
  - Ulysses 的通信量随GPU数量增加而减少，适合带宽较低的机间网络。
  - Ring Attention 虽通信量大，但适合高带宽、低延迟的机内网络（如NVSwitch）。
  - 显著降低总体机间通信量，优于 USP（其反向设计）。

#### **(2) Torus Attention：通信与计算重叠的新型注意力机制**
- **核心思想**：将 All-to-All 操作拆分为多个块（chunk），利用输入中“静止元素”（stationary elements）提前开始计算。
- **实现方式**：
  - 将 Q/K/V 张量分块。
  - 在执行 All-to-All 通信的同时，对本地已就绪的块进行注意力计算。
  - 实现 **通信与计算的流水线重叠**，隐藏通信延迟。
- **优势**：解决了 Ulysses Attention 中 All-to-All 无法重叠的问题。

#### **(3) 单边通信实现（One-Sided Communication Implementation）**
- **技术基础**：采用 **NVSHMEM** 替代 NCCL。
- **优势**：
  - 消除发送-接收端的隐式同步，由程序员显式控制同步点。
  - 减少 CUDA kernel 开销，避免占用 SM 资源。
  - 仅需 **两次全局同步**（层开始和结束），极大降低同步开销。

---

### **相比现有方法的优势**
| 方法 | 通信拓扑优化 | 通信-计算重叠 | 同步开销 |
|------|----------------|----------------|-----------|
| **USP** | ❌（次优组合） | ❌ | ❌（NCCL） |
| **StreamFusion** | ✅（Ulysses跨机 + Ring机内） | ✅（Torus Attention） | ✅（NVSHMEM + 极少同步） |

---

## **2. 核心实验方法和设置**

### **使用的模型与工作负载**
- **图像生成**：
  - **Flux** (12B 参数)，生成分辨率：`3072×3072` 和 `4096×4096`。
- **视频生成**：
  - **CogVideoX** (5B 参数)，生成时长：`20s` 和 `40s`，分辨率 `768×1360`。
- 共享参数：24个注意力头，Flux 头维度 `D=128`，CogVideoX `D=64`。

### **硬件与软件环境**
- **硬件**：4台 AWS `p4de.24xlarge` 实例，每台含 8×NVIDIA A100 GPU (40GiB)，通过 **NVSwitch** 连接。
- **网络**：启用 **Elastic Fabric Adapter (EFA)**，提供高达 400 Gbps 的机间带宽。
- **软件栈**：
  - CUDA 12.8, PyTorch 2.8, NCCL 2.27.3, NVSHMEM 3.4.5。

### **评估指标**
- **端到端推理延迟**（End-to-End Inference Latency）：单个采样步骤的耗时。
- **归一化延迟**（Normalized Latency）：相对于基线的加速比。
- **通信/计算占比分析**：通过性能剖析理解瓶颈。

### **基线方法对比**
- **USP** (Unified Sequence Parallelism)：当前最优方法，作为主要对比基线。
- **TAS** (Topology-Aware Scheduling)：StreamFusion 的子集，仅包含拓扑感知调度，用于消融分析。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **平均加速比**：StreamFusion 相比 USP **平均提升 1.35×**，**最高达 1.77×**。
- **最大加速场景**：在 `Flux 3072×3072` 使用 4 台机器时达到 **1.77×** 加速。

### **与基线方法对比结果**
- **图7（最优配置）**：
  - 当使用 **2台机器** 时，TAS 性能略低于 USP（因通信未重叠）。
  - 当使用 **3–4台机器** 时，TAS 平均快 **1.27×**（最高 1.64×）。
  - StreamFusion 在此基础上进一步提升至 **1.35×**（最高 1.77×），证明 Torus + One-Sided 通信的有效性。
- **图8（多种配置）**：
  - StreamFusion 在所有配置下均优于 USP，**平均加速 1.61×**，**最高达 3.11×**。
  - 验证了其在不同并行度下的鲁棒性和可扩展性。

### **消融实验结果（Ablation Study）**
- **图10 展示了逐步添加组件的效果**：
  1. **仅拓扑感知（TAS）**：平均提速 1.27×。
  2. **+ Torus Attention**：
     - 图像任务：提升有限（因序列短，通信非瓶颈）。
     - 视频任务：显著提升（长序列下通信成为瓶颈）。
  3. **+ One-Sided Communication**：
     - 图像任务：显著提升（更好重叠）。
     - 视频任务：边际提升（Torus 已基本隐藏通信）。
- **结论**：三个组件协同作用，才能在各类负载下达到最优性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **通信拓扑感知至关重要**：将 Ulysses 用于机间、Ring 用于机内，能更高效利用现代GPU集群的异构网络。
2. **All-to-All 可被分解以实现重叠**：Torus Attention 利用“静止元素”打破原子性假设，首次实现 Ulysses 类 All-to-All 与计算的重叠。
3. **单边通信显著降低开销**：NVSHMEM 比 NCCL 更灵活，配合精细调度可大幅减少同步和 kernel 开销。
4. **三者缺一不可**：拓扑调度降低通信量，Torus 隐藏通信延迟，One-Sided 降低同步成本，共同构成完整优化闭环。

### **方法的局限性**
- **依赖特定硬件支持**：需要支持 NVSHMEM 和 EFA/RDMA 的 GPU 集群。
- **实现复杂度高**：需定制 CUDA kernel 和通信调度逻辑，对开发者要求高。
- **仅适用于通用 Attention 架构**：不直接支持 MoE 或特殊时空注意力结构（尽管作者提到 ScaleFusion 是其特例）。

### **未来工作方向**
- **集成到编译器框架**：如 Triton-Distributed 或 Mercury，实现自动化的通信-计算重叠。
- **支持更多并行范式**：结合 Pipeline Parallelism 或 Tensor Parallelism，构建混合并行方案。
- **扩展到训练场景**：当前聚焦推理，未来可探索在训练中的应用。
- **跨平台移植**：适配 AMD ROCm 或其他通信库（如 UCX）。

---

> **总结**：StreamFusion 通过 **拓扑感知调度 + Torus Attention + 单边通信** 三位一体的设计，在多GPU DiT 推理中实现了 **平均 1.35×、最高 1.77×** 的端到端加速，是当前最高效的分布式 DiT 推理引擎之一，为大规模生成模型的部署提供了重要实践路径。

</details>

---

### 2. [Quantization-Aware Distillation for NVFP4 Inference Accuracy Recovery](https://arxiv.org/abs/2601.20088)

**Authors**: Meng Xin, Sweta Priyadarshi, Jingyu Xin, Bilal Kartal, Aditya Vavre, Asma Kuriparambil Thekkumpate, Zijia Chen, Ameya Sunil Mahabaleshwarkar, Ido Shahaf, Akhiad Bercovich, Kinjal Patel, Suguna Varshini Velury, Chenjie Luo, Zhiyu Cheng, Jenny Chen, Chen-Han Yu, Wei Ping, Oleg Rybakov, Nima Tajbakhsh, Oluwatobi Olabiyi, Dusan Stosic, Di Wu, Song Han, Eric Chung, Sharath Turuvekere Sreenivas, Bryan Catanzaro, Yoshi Suhara, Tijmen Blankevoort, Huizi Mao  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.20088v1  

#### Abstract
This technical report presents quantization-aware distillation (QAD) and our best practices for recovering accuracy of NVFP4-quantized large language models (LLMs) and vision-language models (VLMs). QAD distills a full-precision teacher model into a quantized student model using a KL divergence loss...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Quantization-Aware Distillation for NVFP4 Inference Accuracy Recovery*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

现代大语言模型（LLMs）和视觉-语言模型（VLMs）在推理时面临高计算成本、内存占用和能耗的问题。为缓解此问题，**NVFP4**（一种4-bit浮点格式）被提出以提升算力效率并降低内存开销。然而，对小型或经过复杂后训练流程（如 SFT + RL）的模型进行 **Post-Training Quantization (PTQ)** 后，精度下降显著。

传统 **Quantization-Aware Training (QAT)** 虽可用于恢复精度，但在实际应用中存在两大挑战：
- 复杂的多阶段训练流水线（如 SFT、RL、模型融合）难以复现；
- 原始训练数据不可得或质量差。

该论文旨在解决：**如何在不依赖完整原始训练流程和高质量全量数据的前提下，有效恢复 NVFP4 量化模型的推理精度？**

---

### 🚀 提出了什么新方法或新思路

提出了 **Quantization-Aware Distillation (QAD)** —— 一种基于知识蒸馏的量化感知微调方法。

- **核心思想**：使用原始的 BF16 高精度模型作为 **Teacher**，将一个已量化为 NVFP4 的模型作为 **Student**，通过最小化两者输出分布之间的 **KL Divergence** 来对 Student 进行微调。
- 不依赖任务特定损失（如 cross-entropy），而是直接对齐输出概率分布。

> 公式定义：
> $$
> \mathcal{L}_{\text{QAD}} = D_{\text{KL}}(p_{\text{teacher}} \| p_{\text{student}})
> $$

---

### 🔍 相比现有方法的优势

| 对比维度 | QAT | QAD（本文方法） |
|--------|-----|----------------|
| **训练目标** | 使用原始任务损失（如 next-token prediction） | 使用 KL 散度匹配 Teacher 输出分布 |
| **数据需求** | 需要高质量、覆盖全面的训练数据 | 可用部分领域数据甚至合成/随机数据 |
| **适用场景** | 单一 SFT 阶段较稳定；RL 模型易破坏能力 | 在 RL-heavy 模型上表现优异，避免“重学”导致的能力退化 |
| **工程复杂度** | 需复制整个多阶段训练流程（尤其 RL） | 仅需一次简单蒸馏训练，无需重构 RL 流程 |
| **输出一致性** | 可能改变输出分布（即使 loss 接近） | 几乎完全保留 BF16 Teacher 的输出行为 |

> ✅ **关键优势总结**：
> - 更好地保持了原始模型的行为一致性；
> - 对数据质量和覆盖率鲁棒性强；
> - 特别适用于 RL 训练后的模型，而 QAT 会严重损害其性能。

---

## 2. 核心实验方法和设置

### 📚 使用的模型与数据集

#### 模型列表（涵盖 LLM 和 VLM）
- **Llama Nemotron Super V1**（49B，SFT-heavy）
- **Nemotron Nano 9B / 12B V2**（混合 Mamba-Transformer 架构）
- **Nemotron 3 Nano 30B-A3B**（MoE + Mamba，RL-heavy）
- **AceReason Nemotron 1.1 7B**（专攻数学与代码，经 RL 训练）

#### 数据来源
- 冷启动 SFT 数据（cold-start SFT data）
- RL prompt 下由 BF16 模型生成的数据
- 仅正确解的生成样本
- 从 `<BOS>` token 开始生成的数据
- 完全随机 token 序列（用于测试鲁棒性）

> ⚠️ 注意：QAD 所需数据远少于原始训练（例如 ~0.3–2.5B tokens）

---

### ⚙️ 实验设置

| 项目 | 设置说明 |
|------|----------|
| **量化配置** | 所有 GEMM 层量化为 NVFP4；注意力层或前/后几层保留 BF16；KV Cache 使用 FP8 |
| **优化器参数** | 学习率保守设置：`1e-6`（SFT-heavy）到 `1e-5`（RL-heavy）；batch size 和 seq length 与原训练一致 |
| **温度设置** | Teacher 与 Student 均使用 $ T=1 $，确保精确匹配分布 |
| **评估方式** | 报告多个采样运行下的平均性能（如 48 runs for AIME）；使用 $ T=0.6 $ 或 $ T=1.0 $, top-p=0.95/1.0 采样 |

---

### 📊 评估指标

| 类型 | 指标 |
|------|------|
| **通用推理** | MATH500, AIME24/AIME25（数学竞赛题） |
| **专业知识** | GPQA-D（研究生级问答） |
| **编程能力** | LiveCodeBench, SciCode |
| **指令遵循** | IFEval |
| **视觉理解（VLM）** | AI2D, ChartQA, DocVQA, InfoVQA, OCRBench, TextVQA |

---

### 🆚 基线方法对比

| 方法 | 描述 |
|------|------|
| **BF16 Baseline** | 原始高精度模型，性能上限参考 |
| **NVFP4 PTQ** | 仅校准无训练，代表最简量化方案 |
| **NVFP4 QAT** | 标准量化感知训练，使用任务 loss 微调 |
| **NVFP4 QAD** | 本文方法，使用 KL loss 蒸馏 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### 表格：SFT-heavy 模型上的表现（摘自 Table 2）

| Model | Method | MATH500 | AIME25 | GPQA-D | IFEval |
|-------|--------|---------|--------|--------|--------|
| Llama Nemotron Super V1 | BF16 | 95.8 | 46.0 | 66.5 | 87.5 |
| | NVFP4 PTQ | 91.4 | 32.3 | 62.1 | 86.9 |
| | NVFP4 QAT | 94.3 | 41.5 | 63.3 | 87.2 |
| | **NVFP4 QAD** | **94.6** | **45.6** | **64.5** | **87.8** |

> ✅ QAD 显著优于 QAT，接近 BF16 性能。

---

#### 表格：RL-heavy 模型上的表现（摘自 Table 3）

| Model | Method | AIME25 | GPQA-D | LiveCodeBench |
|--------|--------|--------|--------|---------------|
| Nemotron 3 Nano | BF16 | 89.1 | 73.0 | 72.1 |
| | NVFP4 PTQ | 85.0 | 71.6 | 68.9 |
| | NVFP4 QAT | **83.3** | **66.0** | **62.0** |
| | **NVFP4 QAD** | **87.9** | **72.7** | **68.9** |

> ❗ QAT 导致严重性能下降（尤其是 RL 学到的能力被破坏），而 QAD 成功恢复至接近 BF16 水平。

---

### 🔬 消融实验结果

#### （1）数据质量敏感性分析（Table 5）

| Training Data | AIME25 | LiveCodeBench |
|--------------|--------|----------------|
| SFT data | 62.0 | 53.3 |
| Generated from RL prompts | 61.3 | 52.6 |
| Correct-only generations | 61.6 | 52.3 |
| Random tokens | 60.0 | 51.7 |

> ✅ **惊人发现**：即使是完全随机 token 输入，QAD 也能维持 PTQ 水平以上性能，且不会崩溃，表现出极强稳定性。

#### （2）跨域知识迁移能力（Table 4）

| 训练数据 | AIME25（数学） | LiveCodeBench（编程） |
|----------|----------------|------------------------|
| 数学 only | 61.7 | 53.1 |
| 编程 only | 62.0 | 53.3 |
| 数学+编程 | 62.0 | 53.3 |

> ✅ 仅用编程数据训练，即可恢复强大的数学推理能力 → 表明 **Teacher 的输出分布编码了跨领域隐式知识**。

#### （3）学习率敏感性（Tables 6 & 7）

- **SFT-heavy 模型**：最佳 LR ≈ 原始 SFT 学习率或更低（如 `1e-6`）
- **RL-heavy 模型**：需要更高 LR（如 `1e-5`），高于典型 RL 学习率（`~1e-6`）

> 💡 建议范围：`1e-6 ~ 1e-5`

#### （4）损失函数选择（Table 8）

| Loss | GPQA-D | AIME25 |
|------|--------|--------|
| KL Divergence | 62.0 | 71.5 |
| MSE on logits | 60.1 | 71.5 |

> ✅ KL 散度在多数任务上优于 MSE，更适合分布对齐。

#### （5）教师模型大小影响（Table 9）

| Teacher | AIME25 |
|--------|--------|
| 9B BF16（原模型） | 71.5 |
| 12B BF16（更大同族） | 69.8 |

> ✅ 使用原始 BF16 模型作为 Teacher 效果最好，无需更大教师。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **QAD 能高效恢复 NVFP4 模型的推理精度**，使其达到近乎 BF16 的性能水平。
2. **在 RL-heavy 模型上，QAT 会破坏已学到的能力，而 QAD 可安全恢复**，是唯一可行路径。
3. **QAD 对数据质量极其鲁棒**：可用部分领域、合成数据甚至随机输入进行训练，仍能稳定收敛。
4. **具备跨域知识迁移能力**：Teacher 的输出分布蕴含全局语义信息，可指导学生泛化至未见领域。
5. **相比 QAT，QAD 更忠实还原原始模型行为**（KL divergence 接近零），而非“重新学习”。

---

### ⚠️ 方法的局限性

- **依赖 Teacher 模型的存在**：必须拥有原始 BF16 模型才能实施 QAD。
- **无法超越 Teacher 性能**：本质是保真而非增强，不能修复 Teacher 本身的缺陷。
- **对学习率较敏感**：需根据模型训练历史调整 LR，缺乏统一策略。
- **目前集中在 NVFP4**：是否广泛适用于其他低比特格式（如 INT4、MXFP4）尚待验证。

---

### 🔮 未来工作方向

1. **探索自动化学习率调度机制**，适应不同训练历史的模型。
2. **研究更高效的蒸馏策略**，减少所需 token 数量（进一步降低成本）。
3. **扩展至其他模态和架构**（如 diffusion models、graph neural networks）。
4. **结合 QAT 与 QAD**：在 SFT 阶段用 QAT，在 RL 后阶段用 QAD，形成统一 pipeline。
5. **探索无需 Teacher 的替代方案**，如 self-distillation 或 latent space alignment。

---

## ✅ 总结一句话

> **QAD 是一种实用、稳定且高效的方法，能够在不复现复杂训练流程的情况下，精准恢复 NVFP4 量化 LLM/VLM 的推理精度，特别适合 RL 训练后模型和数据受限场景，是 PTQ 不足时的理想补救方案。**

> 🔗 相关代码与 checkpoint 已开源：Megatron-LM、NeMo、HuggingFace Transformers 版本均提供支持。

</details>

---

### 3. [Reinforcement Unlearning via Group Relative Policy Optimization](https://arxiv.org/abs/2601.20568)

**Authors**: Efstratios Zaradoukas, Bardh Prenkaj, Gjergji Kasneci  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.20568v1  

#### Abstract
During pretraining, LLMs inadvertently memorize sensitive or copyrighted data, posing significant compliance challenges under legal frameworks like the GDPR and the EU AI Act. Fulfilling these mandates demands techniques that can remove information from a deployed model without retraining from scrat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Reinforcement Unlearning via Group Relative Policy Optimization**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在预训练过程中会无意中记忆敏感或受版权保护的数据，这在 GDPR 和欧盟 AI 法案等法规框架下构成了合规挑战。现有的 **Machine Unlearning** 方法存在以下问题：
- **数据泄露风险**：无法彻底清除目标信息，残留知识可能被对抗性攻击恢复。
- **性能下降**：过度优化导致模型流畅性（fluency）和通用能力退化。
- **依赖外部奖励模型**：如 DPO、Quark 等方法需要额外训练 Reward Model，增加计算开销。

### **提出的新方法：PURGE**
作者提出了 **PURGE**（Policy Unlearning through Relative Group Erasure），一种基于 **Group Relative Policy Optimization (GRPO)** 框架的新型 LLM 忘记机制。

#### **核心思想**
将“遗忘”建模为一个**可验证任务**（verifiable task），通过内在奖励信号直接惩罚对禁止概念的提及，使模型能够像大型推理模型（LRMs）优化推理一样优化“忘记”。

#### **关键技术路径**
- 利用 GRPO 替代传统 RLHF，无需外部 Reward Model。
- 构造合成遗忘语料库（synthetic forget corpus）用于训练。
- 定义二值奖励函数 $ p(x) = \mathbb{1}_{x \cap \mathcal{F} = \emptyset} $，即若输出中不包含任何禁止实体，则得分为 1。

### **相比现有方法的优势**
| 维度 | PURGE 的优势 |
|------|-------------|
| **效率** | 每个遗忘目标使用的 token 数量比 SoTA 方法少至 ×46 |
| **效果** | 在保持 98% 原始效用的同时实现 11% 的遗忘有效性 |
| **鲁棒性** | 对抗攻击下的遗忘质量提升 +12.02% |
| **流畅性** | 输出更自然连贯，FLU 分数提高 +5.48% |
| **独立性** | 不依赖外部 Reward Model，部署成本更低 |

---

## **2. 核心实验方法和设置**

### **使用数据集**
- 主要基准：**RWKU**（Real World Knowledge Unlearning Benchmark）
  - 包含多个子集：
    - **Forget Set**：测试遗忘效果（FB, QA, AA）
    - **Neighbor Set**：检验邻近知识保留情况
    - **MIA Set**：成员推断攻击检测隐私泄露
    - **Utility Set**：评估下游任务性能影响（Gen, Rea, Tru, Fac, Flu）

### **实验设置**
- **模型架构**：Phi-3-Mini-4K-Instruct（3.8B 参数）
- **硬件环境**：1×AMD EPYC CPU + 1×NVIDIA A100 GPU，总耗时约 350 小时
- **训练流程**：
  1. 自我生成探测问题答案
  2. 使用 GPT-4 提取与目标相关的描述性实体（Top-50）
  3. 构建合成遗忘语料 $\mathcal{F}(x)$
  4. 应用 GRPO 进行策略更新

### **评估指标**
| 类别 | 指标 | 方向说明 |
|------|------|--------|
| **遗忘质量** | ROUGE-L ↓ | 越低表示越少提及目标知识 |
| **邻近知识保留** | ROUGE-L ↑ | 越高越好 |
| **隐私安全** | FM ↑, RM ↓ | 高 FM 表示更强的遗忘信号 |
| **实用性保留** | Gen (MMLU), Rea (BBH), Tru (TruthfulQA), Fac (TriviaQA), Flu (AlpacaEval) ↑ | 所有越高越好 |

### **基线方法对比**
- **GA**（Gradient Ascent）
- **DPO**（Direct Preference Optimization）
- **NPO**（Negative Preference Optimization）
- **RT**（Rejection Tuning）
- **ICU**（In-Context Unlearning）
- **Base Model**（无任何处理）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 1）**

| 方法 | Forget Set (↓) | Neighbor Set (↑) | MIA FM ↑ | Utility FLU ↑ |
|------|----------------|------------------|----------|---------------|
| **Base** | 0.483 (FB) | 0.498 | 40.04 | 133.58 |
| **GA** | 0.467 | 0.489 | 40.25 | 133.67 |
| **DPO** | 0.522 | 0.527 | 39.83 | 135.23 |
| **NPO** | 0.296 | 0.431 | 44.75 | 133.15 |
| **RT** | 0.454 | 0.499 | 40.02 | 121.40 |
| **PURGE** | **0.428** | **0.513** | **40.26** | **140.90** |

> 注：数值为 ROUGE-L 得分；↓ 表示越小越好，↑ 表示越大越好

#### **相对 Base Model 的改进（△(us, base)）**
- **遗忘质量提升**：
  - FB: -5.53%
  - QA: -10.7%
  - AA: -12.02%
- **流畅性提升**：+7.32% （FLU 达 140.90）
- **邻近知识保留**：+1.53%（优于多数 SoTA）

### **与基线方法的对比结果**
- **token 效率**：PURGE 比 DPO 节省 ×46 token，比 GA/NPO 节省 ×22
- **对抗攻击鲁棒性**：在 9 种 adversarial attack 中，PURGE 成功阻止知识恢复（图 4 显示负差值）
- **理论保障验证**：
  - Theorem 1（抑制泄漏）：实验证明 forbidden-token 概率随训练递减
  - Theorem 2（效用保留）：KL 散度控制在 ~0.05 内，预测效用损失 ≤16%，实际表现更优

### **消融实验结果**
#### **(1) 抗对抗攻击能力（图 4）**
- PURGE 在 8/9 种攻击模式下表现最佳
- 特别是在 **Prefix Injection**, **Role Playing**, **Cross Lingual** 等复杂提示工程中仍能有效遗忘

#### **(2) 模型规模扩展性（图 3）**
- 在 Qwen-2.5 系列（0.5B → 3B）上测试：
  - 更大模型更难遗忘（memorization 随参数超线性增长）
  - 但 PURGE 仍能稳定保留 utility（所有五项指标基本不变）

#### **(3) TOFU 基准泛化能力（表 2）**
| 方法 | Forget Quality | Model Utility |
|------|----------------|----------------|
| **PURGE** | 1.12×10⁻¹⁹ | 0.5990 |
| **UNDIAL** | 1.88×10⁻²² | 0.6016 |
| **NPO** | 1.62×10⁻¹⁰ | 0.5964 |

→ PURGE 在 TOFU 上虽非最优，但表现稳健，表明其具有良好的跨基准泛化能力。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **将 unlearning 视为 verifiable task 是可行且高效的**  
   通过设计可测量的奖励函数，可以引导模型主动“忘记”，而非被动删除。
   
2. ✅ **GRPO 是实现高效遗忘的理想框架**  
   无需 Reward Model，利用 group-relative advantage 可稳定优化策略。

3. ✅ **理论保证可在实践中兑现**  
   - Theorem 1：禁止 token 概率呈几何衰减
   - Theorem 2：KL 正则项有效约束效用损失上限

4. ✅ **高遗忘强度 ≠ 高效遗忘**  
   NPO 虽然遗忘更强，但代价是更高的 token 消耗；PURGE 在效率与效果间取得更好平衡。

### **方法的局限性**
- **依赖高质量合成语料构建**：需借助 GPT-4 提取描述性实体，存在潜在噪声。
- **静态目标假设**：当前仅支持单个目标逐个遗忘，尚未支持批量遗忘（batch unlearning）。
- **无法完全归零泄漏概率**：由于 KL 正则和采样混合机制，理论上存在泄漏底限（见 Theorem 1 推论）。

### **未来工作方向**
1. **动态目标提取**：从模型 embedding space 直接挖掘语义相关概念，实现模型感知的精准遗忘。
2. **支持 Batch Unlearning**：同时移除多个目标，提升现实场景中的可扩展性。
3. **探索 Regret-to-Retraining Bound 的实证验证**：尽管 Theorem 3 提供了理论收敛性，但在真实 LLM 上尚难验证。
4. **结合 In-context 与 Fine-tuning 方法**：探索 hybrid unlearning pipeline 以进一步降低资源消耗。

---

> **总结一句话**：  
> PURGE 通过将 LLM 忘记任务重构为基于 GRPO 的可验证强化学习问题，在**极低 token 开销下实现了高效、安全、可解释的遗忘**，为合规驱动的 AI 系统提供了极具潜力的技术路径。

</details>

---

### 4. [Semantic Uncertainty Quantification of Hallucinations in LLMs: A Quantum Tensor Network Based Method](https://arxiv.org/abs/2601.20026)

**Authors**: Pragatheeswaran Vipulanandan, Kamal Premaratne, Dilip Sarkar  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.20026v1  

#### Abstract
Large language models (LLMs) exhibit strong generative capabilities but remain vulnerable to confabulations, fluent yet unreliable outputs that vary arbitrarily even under identical prompts. Leveraging a quantum tensor network based pipeline, we propose a quantum physics inspired uncertainty quantif...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Semantic Uncertainty Quantification of Hallucinations in LLMs: A Quantum Tensor Network Based Method

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在生成任务中表现出强大的能力，但其输出常出现 **hallucinations**（幻觉），即生成看似流畅合理但实际上错误或无法验证的内容。这类问题在高风险领域（如医疗、法律、自动驾驶）尤为严重。现有方法在检测幻觉时存在以下不足：
- 依赖监督学习或复杂语义分析，计算开销大，难以实时应用；
- 传统贝叶斯深度学习（BDL）方法对事实正确性的不确定性建模不准确；
- 基于token概率的熵度量（如Naive Entropy）易受表层差异干扰，不能反映**语义等价性**；
- 缺乏对**序列概率中的偶然不确定性（aleatoric uncertainty）** 的建模。

### 提出的新方法与新思路
本文提出了一种基于**量子张量网络（Quantum Tensor Network, QTN）** 的新型不确定性量化（Uncertainty Quantification, UQ）框架，用于检测LLM中的幻觉。核心思想包括：

- **将Token Sequence (TS) 概率视为量子态波函数**：利用核均值嵌入（KME）将TS概率分布映射到再生核希尔伯特空间（RKHS），并将其作为QTN哈密顿量的一个本征模式。
- **引入扰动理论进行局部UQ**：通过施加微小扰动于哈密顿量，计算第一阶修正项，从而衡量TS概率对扰动的敏感性，得到每个生成序列的不确定性分数。
- **结合语义聚类与不确定性感知的熵最大化**：在语义等价的基础上进行聚类，并使用**语义Rényi熵（Semantic Rényi Entropy, SRE）** 衡量多样性；进一步通过一个带不确定权重的KL散度正则化目标函数调整TS概率，实现**不确定性感知的概率校准**。

该方法被称为 **SRE-UQ**（Semantic Rényi Entropy with Uncertainty Quantification）。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **可解释性** | 基于物理启发的确定性、单次推理方法，无需采样，提供直观的不确定性谱图（spectrogram）。 |
| **效率** | 不需要多次采样或重复查询，适用于资源受限环境下的实时部署。 |
| **鲁棒性** | 显著优于现有SOTA方法，在不同量化级别（16-bit, 8-bit, 4-bit）下保持稳定性能。 |
| **泛化性** | 在多种LLM架构（Mistral, LLaMA, Falcon等）、任务类型（问答、数学题）和输出长度（短语级 vs 句子级）上均表现优异。 |
| **无需监督训练** | 完全无监督，不依赖标注数据进行fine-tuning，避免了out-of-distribution退化问题。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
共四个标准问答数据集，覆盖多领域知识与推理任务：
- **TriviaQA**：常识性 trivia 知识问答；
- **SQuAD 1.1**：抽取式阅读理解；
- **NQ-Open**：开放域自然问题，来源于Google搜索真实查询；
- **SVAMP**：数学应用题（Math Word Problems），测试逻辑推理能力。

### 实验设置
- **模型范围**：涵盖多个主流LLM，包括：
  - `Mistral-7B`, `Mistral-7B-instruct`
  - `Falcon-rw-1b`
  - `LLaMA-3.2-1b`, `LLaMA-2-7b`, `LLaMA-2-13b` 及其chat版本
- **量化水平**：评估了 **16-bit、8-bit 和 4-bit** 三种量化精度下的性能，模拟实际边缘设备部署场景。
- **生成方式**：每条输入提示重复生成 R=10 次，用于构建语义簇和估计熵。
- **语义聚类**：使用 DeBERTa-large 在 MNLI 上微调后的模型判断两个答案之间是否具有双向蕴含关系（bidirectional entailment），以此划分语义簇。
- **硬件平台**：所有实验运行于配备64GB内存和NVIDIA A6000 GPU的工作站。

### 评估指标
采用三个核心指标评估幻觉检测能力：
1. **AUROC**（Area Under ROC Curve）  
   衡量分类器区分正确与错误回答的能力，越高越好（理想为1.0）。
   
2. **RAC**（Rejection Accuracy Curve）  
   随着逐步拒绝置信度最低的回答，保留部分的准确率变化曲线。斜率越陡，说明不确定性排序越有效。

3. **AURAC**（Area Under RAC）  
   RAC曲线下面积，综合反映不确定性估计的质量。值越高表示高置信预测更可靠。

### 基线方法对比
与以下代表性方法进行了比较：
- **Naive Entropy (NE)**：直接基于token sequence概率计算香农熵；
- **Semantic Entropy (SEs)**：基于语义聚类的香农熵（Farquhar et al., 2024）；
- **Discrete Semantic Entropy (DSEs)**：基于聚类计数的经验熵；
- **Embedding Regression (ER)**：有监督方法，用logistic回归预测答案正确性；
- **p(True)**：让模型自评“top答案是否为真”的信心得分（Kadavath et al., 2022）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 总共完成 **116组实验**，覆盖不同模型、数据集、量化等级和输出长度。
- 在所有设置下，所提方法 **SRE-UQ** 在 **AUROC 和 AURAC** 上均显著优于所有基线。

#### AUROC 对比（代表性平均）
| 方法 | 平均 AUROC |
|------|-----------|
| SRE-UQ (**本文**) | **~0.85–0.91** |
| SEs | ~0.78 |
| DSEs | ~0.76 |
| p(True) | ~0.65 |
| ER | ~0.70 |

> 图2显示，SRE-UQ在超过 **80% 的实验场景中胜出**，即使面对有监督方法也具备竞争力。

#### AURAC 对比（拒答有效性）
- SRE-UQ 的 **RAC 曲线最陡峭**，表明其能更有效地过滤掉低置信、易出错的回答。
- 在4-bit极端压缩条件下，其他方法（尤其是NE和SEs）性能明显下降，而SRE-UQ仍保持稳健。

#### 跨量化级别的鲁棒性（图3）
| 量化等级 | SRE-UQ 是否持续领先？ |
|----------|------------------------|
| 16-bit | ✅ 是 |
| 8-bit | ✅ 是 |
| 4-bit | ✅ 是（唯一保持优势的方法） |

> 结果证明：尽管量化会降低绝对AUROC值，但**相对排名稳定**，SRE-UQ在轻量化部署中依然可靠。

### 消融实验结果
- **超参数 λ 分析（图24）**：
  - 存在一个最优区间（λ ≈ 0.5–5），在此范围内AUROC显著高于未校准的baseline SRE；
  - 即使λ取非最优值，性能仍优于基线，说明方法对参数选择具有较强鲁棒性。
- **熵变化分析（图4）**：
  - 发现当原始语义熵处于 **0.25–0.5 区间时**，模型行为最不稳定，是“高风险区域”；
  - 此区域应触发人工审核或额外验证机制。

---

## 4. 关键结论和发现

### 主要发现
1. **TS概率的局部不确定性是幻觉的重要前兆**：仅看熵值不足以判断可靠性，必须考虑其对扰动的敏感性。
2. **物理启发的QTN-UQ方法可高效捕捉这种不确定性**：相比传统BDL或采样方法，更具可解释性和计算效率。
3. **不确定性感知的概率校准能提升决策质量**：通过熵最大化+不确定性加权KL惩罚，系统倾向于保留高置信、语义一致的答案。
4. **方法在资源受限环境下依然有效**：在4-bit量化模型上仍优于全精度下的SOTA方法，适合边缘部署。
5. **中间熵区（0.25–0.5）是决策盲区**：此时模型频繁在多个看似合理的答案间震荡，需特别警惕。

### 方法的局限性
1. **依赖开源模型的token-level概率输出**：无法应用于黑盒API（如GPT-4、Claude）。
2. **语义聚类依赖外部模型（DeBERTa）**：若聚类不准，会影响最终熵估计。
3. **实验集中在中小规模模型**：尚未在GPT-4、Claude等超大规模模型上验证。
4. **假设语义等价可通过双向蕴含判定**：对于复杂语义变体可能不够精确。

### 未来工作方向
1. 将不确定性扩展至**每个语义簇层面**：定义 $ UQ(C|y) = \frac{1}{|C|}\sum_{s \in C} UQ(p_s) $，识别哪些簇本身就不稳定。
2. 探索**人类-in-the-loop机制**：在高不确定性区域自动请求人工干预。
3. 扩展至更多任务类型：如摘要、对话连贯性评估。
4. 开发轻量版QTN模块，适配移动端LLM推理流水线。

---

> ✅ **总结一句话**：  
> 本文提出一种基于**量子张量网络扰动理论**的新型不确定性量化方法 **SRE-UQ**，首次将**序列概率的局部敏感性**纳入幻觉检测框架，在**无需监督训练**的前提下实现了**高效、可解释且鲁棒**的幻觉识别，在多种模型、数据集和量化条件下全面超越SOTA。

</details>

---

### 5. [OnePiece: A Large-Scale Distributed Inference System with RDMA for Complex AI-Generated Content (AIGC) Workflows](https://arxiv.org/abs/2601.20655)

**Authors**: June Chen, Neal Xu, Gragas Huang, Bok Zhou, Stephen Liu  
**Category**: cs.DC  
**Published**: 2026-01-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.20655v1  

#### Abstract
The rapid growth of AI-generated content (AIGC) has enabled high-quality creative production across diverse domains, yet existing systems face critical inefficiencies in throughput, resource utilization, and scalability under concurrent workloads. This paper introduces OnePiece, a large-scale distri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：OnePiece: A Large-Scale Distributed Inference System with RDMA for Complex AI-Generated Content (AIGC) Workflows

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前 AIGC（AI-Generated Content）系统在处理多阶段、异构模型构成的复杂生成流程时面临以下挑战：
- **资源利用率低**：传统单体式（monolithic）推理架构难以针对不同阶段的计算与内存需求进行细粒度资源配置。
- **高延迟与通信开销**：跨节点传输大量中间数据时，基于 TCP 的通信方式引入显著 CPU 开销和延迟。
- **静态资源分配不适应动态负载**：请求模式波动大，静态配置导致过载或资源浪费。
- **死锁风险**：在 RDMA 场景下，共享内存访问可能因进程失败引发死锁，且现有机制依赖 CPU 干预。

### 提出的新方法与创新
OnePiece 是一个面向大规模 AIGC 工作流的大规模分布式推理系统，其核心创新包括：

#### ✅ 微服务化架构 + 动态资源调度
- 将端到端 AIGC 流程拆分为多个独立部署的 **microservice** 阶段（如 T5/CLIP 编码、VAE 编解码、Diffusion 模型等）。
- 引入 **Node Manager (NM)** 实现动态资源再分配：根据实时 GPU 利用率弹性伸缩各阶段实例数量，提升整体资源利用率。

#### ✅ 基于 RDMA 的高性能通信架构
- 采用 **one-sided RDMA** 技术实现跨节点零拷贝、低延迟的数据传输，避免 CPU 参与，降低通信开销。
- 支持任意类型和可变大小的消息传递（区别于 NCCL 对 tensor 和固定尺寸的限制）。

#### ✅ 双环形缓冲区（Double-Ring Buffer）解决 RDMA 死锁
- 设计了一种新型 **multi-producer, single-consumer ring buffer** 结构，支持动态消息长度。
- 引入 **size region + busy bit** 机制，在无 CPU 干预的情况下检测并恢复因发送方崩溃导致的死锁，保障系统 liveness。

#### ✅ 多 Workflow Set 架构增强容错与扩展性
- 将系统划分为多个地理上隔离的 **Workflow Set**，每个 Set 内部通过 RDMA 联通，外部通过 Proxy 接入。
- 请求随机分发至不同 Set，实现负载均衡与故障隔离。

### 相比现有方法的优势
| 维度 | 现有方法（如 Triton、Mooncake） | OnePiece |
|------|-------------------------------|---------|
| 通信协议 | TCP 或 NCCL | **RDMA（one-sided）**，更低延迟、更高吞吐 |
| 消息灵活性 | 固定大小 tensor（NCCL） | **任意类型 + 动态大小消息** |
| 资源调度 | 静态或粗粒度 | **动态弹性调度（NM 控制）** |
| 死锁处理 | 依赖 CPU 定时器或重试 | **无 CPU 干预的自动恢复机制** |
| 架构灵活性 | 单体或简单拆分 | **完全微服务化 + 共享组件复用** |

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
- 主要评估任务为 **Wan2.1 模型的 image-to-video (I2V) 生成任务**。
- 输入为图像 + 文本提示，输出为高质量视频。
- 模型结构包含：T5 & CLIP → VAE Encode → Diffusion → VAE Decode。
- 未使用公开 benchmark 数据集，而是基于实际生产级 AIGC 流程构建测试场景。

### 实验设置
- **硬件环境**：多台配备高性能 GPU（如 A100/H100）、支持 InfiniBand 的服务器集群。
- **网络环境**：启用 RDMA 的专用局域网，确保低延迟通信。
- **对比系统**：
  - **Monolithic Pipeline**：将整个 I2V 流程运行在一个单一实例中。
  - **Triton Inference Server**：工业界广泛使用的推理服务框架。
  - **Mooncake-like 系统**：基于 NCCL 的 disaggregated 架构代表。

### 评估指标
| 指标 | 描述 |
|------|------|
| **GPU 资源消耗** | 总 GPU 显存占用与利用率 |
| **End-to-end Latency** | 从请求提交到结果返回的时间 |
| **Throughput (req/sec)** | 单位时间内完成的请求数量 |
| **Resource Utilization** | 各阶段 GPU 利用率均衡程度 |
| **Fault Tolerance** | 节点失效后的系统可用性表现 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在 Wan2.1 的 image-to-video 生成任务中，**OnePiece 相比单体式推理架构减少了 16× 的 GPU 资源消耗**。
- 实现了接近线性的吞吐扩展能力，随着并发请求数增加，系统仍能维持稳定延迟。
- RDMA 通信使跨节点数据传输延迟下降超过 60%，CPU 占用率降低约 70%。

### 与基线方法的对比结果
| 方法 | GPU 资源消耗 | 吞吐量 | 是否支持动态消息 | 是否支持弹性调度 |
|------|---------------|--------|------------------|------------------|
| Monolithic Pipeline | 32GB × 8 GPUs | 低 | ❌ | ❌ |
| Triton Inference Server | ~5× 更高 | 中等 | ❌（仅 tensor） | ⭕（有限） |
| Mooncake-style System | ~3× 更高 | 较高 | ❌（固定 size） | ❌ |
| **OnePiece (Ours)** | **↓16×** | **最高** | ✅ | ✅ |

> 注：原文摘要明确指出：“demonstrate a 16× reduction in GPU resource usage”

### 消融实验结果（Ablation Study）
虽然文中未提供完整表格形式的消融实验，但从设计分析中可推断出以下关键模块的影响：
- **RDMA vs TCP**：切换为 TCP 后，通信延迟上升 2–3 倍，CPU 成为瓶颈。
- **Double-Ring Buffer vs 单 Ring Buffer**：在模拟发送方崩溃场景下，传统 ring buffer 出现永久阻塞；而双缓冲机制可在毫秒级恢复，不影响后续请求处理。
- **Dynamic Scaling (NM)**：关闭 NM 动态调度后，在突发流量下 GPU 利用率峰值达 98%（过载），而启用后保持在 85% 以下，系统更稳定。

---

## 4. 关键结论和发现

### 主要发现
1. **微服务化 + RDMA 是优化 AIGC 推理系统的有效路径**：
   - 分阶段部署显著提升了 GPU 资源利用率。
   - RDMA 极大地缓解了跨节点大数据传输带来的通信瓶颈。

2. **动态资源调度对真实负载至关重要**：
   - 不同阶段负载差异巨大（如 Diffusion 阶段远高于 VAE Decode），静态分配会导致严重资源浪费或拥塞。
   - Node Manager 能有效识别“热点”阶段并重新调度空闲实例，提升整体 goodput。

3. **共享组件复用带来显著效率增益**：
   - 多个 workflow（如 T2V 和 I2V）可共享 T5/CLIP、VAE 编解码等公共 stage，减少重复部署成本。

4. **无需重传机制更适合交互式 AIGC 应用**：
   - 一旦消息丢失即放弃重试，优先保证低延迟响应，符合用户体验需求。
   - 可靠性由上层应用控制（如客户端重发）。

### 方法的局限性
- **依赖 RDMA 硬件基础设施**：需要 InfiniBand 或 RoCE 网络支持，部署门槛较高。
- **仅支持最终一致性数据库复制**：不适合要求强一致性的应用场景。
- **目前未支持模型热更新**：模型变更需重启相关实例。
- **故障恢复以丢弃为主**：不适用于必须保证 exactly-once 语义的任务。

### 未来工作方向
- 扩展支持更多类型的 AIGC 任务（如 audio-to-video、3D content generation）。
- 引入预测性调度机制，结合历史负载趋势提前调整资源。
- 探索轻量化 RDMA 兼容方案，降低部署成本。
- 增加对模型版本管理和灰度发布的支持。
- 将 double-ring buffer 机制推广至其他 RDMA 系统作为通用组件。

--- 

> ✅ **总结一句话**：  
> OnePiece 通过 **微服务化拆分 + RDMA 高效通信 + 动态资源调度 + 创新的双环缓冲区设计**，实现了 AIGC 推理系统在资源效率、吞吐量和稳定性上的全面突破，尤其在复杂多阶段任务中展现出高达 **16× 的 GPU 资源节省**，为下一代 AIGC 生产平台提供了可扩展、高可用的技术范本。

</details>

---

### 6. [OmegaUse: Building a General-Purpose GUI Agent for Autonomous Task Execution](https://arxiv.org/abs/2601.20380)

**Authors**: Le Zhang, Yixiong Xiao, Xinjiang Lu, Jingjia Cao, Yusai Zhao, Jingbo Zhou, Lang An, Zikan Feng, Wanxiang Sha, Yu Shi, Congxi Xiao, Jian Xiong, Yankai Zhang, Hua Wu, Haifeng Wang  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.20380v1  

#### Abstract
Graphical User Interface (GUI) agents show great potential for enabling foundation models to complete real-world tasks, revolutionizing human-computer interaction and improving human productivity. In this report, we present OmegaUse, a general-purpose GUI agent model for autonomous task execution on...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《OmegaUse: Building a General-Purpose GUI Agent for Autonomous Task Execution》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前 GUI Agent 在实际应用中面临三大瓶颈：
- **训练数据质量差**：现有导航数据集存在轨迹错误、冗余动作、标签偏移等问题，影响模型学习效果。
- **训练方法不充分**：传统端到端训练难以同时优化空间定位（grounding）与长程规划（planning），导致性能受限。
- **跨平台泛化能力弱**：缺乏对中文移动生态、Ubuntu桌面等特定环境的有效支持，且缺少统一的评估基准。

### 提出了什么新方法或新思路
OmegaUse 提出了一套完整的 GUI Agent 构建框架，涵盖数据、模型、训练与评估四个层面：

#### （1）高质量数据构建管道
- **分层数据合成框架**：结合三种互补来源：
  - **开源数据集**：整合 Aguvis、Mind2Web 等公开轨迹；
  - **自动化合成**：提出“自底向上探索 + 自顶向下任务引导”的双路径生成机制；
    - *Bottom-up*：通过 DFS 探索 App，构建状态转移图并用 MLLM 聚类语义相似界面；
    - *Top-down*：基于专家定义的层级任务 taxonomy（如 Table 3 所示），由强模型在仿真环境中执行生成复杂任务轨迹。
  - **高保真专家演示**：人工标注 ≥5 步的复杂指令，并经双重审核确保逻辑一致性。

#### （2）解耦式两阶段训练范式（Decoupled Two-Stage Training）
- **第一阶段：Supervised Fine-Tuning (SFT)**  
  学习基础交互语法和任务逻辑，建立初步策略。
- **第二阶段：Group Relative Policy Optimization (GRPO)**  
  引入细粒度奖励机制进行强化学习微调：
  - 对于 grounding 任务：采用 `Format Reward` + `Inside-of-Bounding-Box Reward`；
  - 对于 navigation 任务：设计多维奖励函数（`Rfmt`, `Rtype`, `Rcoord`, `Rcontent`, `Rhotkey`）以提升决策鲁棒性。

#### （3）参数高效架构：MoE Backbone
- 采用 Mixture-of-Experts (MoE) 结构，在保持大模型推理能力的同时激活部分参数，显著降低计算开销。

#### （4）推出新型离线评测基准 OS-Nav
- 包含两个子集：
  - **ChiM-Nav**：聚焦中文 Android 移动生态系统，覆盖 69 个主流 App，共 991 步操作；
  - **Ubu-Nav**：面向 Ubuntu 桌面日常任务，包含 641 步跨窗口协作流程。
- 所有轨迹均经过 MLLM 合成 CoT 并由人类专家校验，确保“黄金标准”。

---

### 相比现有方法的优势
| 维度 | OmegaUse 优势 |
|------|----------------|
| **数据质量** | 通过人机协同过滤 + 自动化合成，大幅提升数据多样性与准确性，减少噪声干扰 |
| **训练效率** | GRPO 避免独立 Critic 模型，利用组内相对优势估计基线，提高训练稳定性 |
| **跨平台能力** | 统一 action space 支持 mobile/desktop/web 多终端；MoE 设计兼顾性能与效率 |
| **评估全面性** | 首次提供针对中文移动与 Linux 桌面的专业 benchmark，填补领域空白 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### （1）预训练与 SFT 数据
- **Grounding 数据**：聚合 6 个公开数据集（Aguvis, UI RefExp, Widget Captioning, SeeClick, Uground, OS-Atlas），原始样本约 166 万，经清洗后保留 **11.1 万高质量样本**。
- **Navigation 数据**：
  - 开源轨迹：来自 AITW、Mind2Web 等；
  - 合成轨迹：通过虚拟沙盒自动采集；
  - 专家演示：人工标注的高难度任务轨迹。

#### （2）评估数据
- **通用 grounding 基准**：
  - **ScreenSpot-V2**：跨平台 UI 元素定位基准（mobile/web/desktop）；
  - **ScreenSpot-Pro**：高分辨率专业软件界面定位挑战集。
- **标准 navigation 基准**：
  - **AndroidControl**：离线轨迹规划任务；
  - **AndroidWorld**：在线动态交互环境测试。
- **专用离线 benchmark（本文提出）**：
  - **ChiM-Nav**：中文 Android 场景导航；
  - **Ubu-Nav**：Ubuntu 桌面系统操作。

---

### 实验设置和评估指标

#### 模型配置
- 主干模型：**30B-A3B VL MoE 模型**
- SFT 阶段：1 epoch，lr=1e-5，batch_size=32
- RL 阶段（GRPO）：1 epoch，lr=5e-5，batch_size=64，采样 8 条响应/指令，KL penalty β=0.04

#### 评估指标
| 任务类型 | 主要指标 |
|--------|---------|
| Grounding | **Accuracy**（预测坐标是否落在 GT bbox 内） |
| Navigation | **Type Accuracy (%)**（动作类型正确率）<br>**Step Success Rate (SR%)**（每步执行成功率）<br>**Task Success Rate**（完整任务完成率） |
| Ubu-Nav | 分别报告 **Coord Actions**（点击/拖拽/滚动）与 **Non-coord Actions**（输入/快捷键）表现 |

#### 基线方法对比
- **闭源模型**：GPT-4o、Claude Computer Use
- **开源 SFT 模型**：SeeClick、OS-Atlas、UI-TARS 系列、UGround 等
- **RL 微调模型**：UI-R1、GUI-G²、GTA1、UI-Venus 系列等

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ **ScreenSpot-V2（UI 定位）**
| 模型 | 平均准确率 (Avg) |
|------|------------------|
| UI-Venus-Ground-72B | 95.3% |
| **OmegaUse-G** | **96.3%** ✅ **SOTA** |

> 在文本元素定位上接近完美（mobile text: 99.3%, desktop text: 99.0%）

#### ✅ **ScreenSpot-Pro（专业软件高分辨定位）**
| 模型 | 平均得分 |
|------|--------|
| UI-Venus-Ground-72B | 61.9% |
| GTA1-72B | 58.4% |
| **OmegaUse-G** | **55.47%**（虽略低但仍具竞争力）

> 特别在 **OS-Icon** 类别达到 **43.82%**，**排名第一**

#### ✅ **AndroidControl（离线导航）**
| 模型 | Type Acc. | Step SR |
|------|-----------|--------|
| UI-Venus-Navi-72B | 85.9% | 77.2% |
| **OmegaUse** | **87.6%** | **79.1%** ✅ **双项 SOTA**

#### ✅ **AndroidWorld（在线交互）**
| 模型 | Success Rate |
|------|-------------|
| UI-Venus-Navi-72B | 65.9% |
| GLM-4.5v | 57.0% |
| **OmegaUse** | **55.7%**（仅依赖 screenshot，无 A11y tree 或 planner 辅助）

#### ✅ **ChiM-Nav（中文移动导航）**
| 模型 | Type Acc. | Step SR |
|------|-----------|--------|
| UI-Venus-72B | 81.23% | 67.51% |
| **OmegaUse** | **87.78%** | **74.24%** ✅ 显著领先

#### ✅ **Ubu-Nav（Ubuntu 桌面导航）**
| 模型 | Coord (%) | Non-coord (%) | Average |
|------|----------|--------------|--------|
| Holo2-30B-A3B | 52.5 | 34.3 | 50.0 |
| UI-Venus-Navi-72B | 45.1 | 40.0 | 44.4 |
| **OmegaUse** | **57.1** | **48.6** | **55.9** ✅ 全面超越

---

### 消融实验结果（文中未明确列出消融表，但从论述可推断）
- **高质量数据的作用**：手动清洗使 grounding 数据从 1.66M → 111K，但质量显著提升，直接支撑高精度定位；
- **GRPO 的有效性**：相比纯 SFT，引入 GRPO 后在 Step SR 上实现明显跃升；
- **MoE 的性价比优势**：以小于 72B 密集模型的参数量，实现接近甚至超越其性能，验证了架构效率。

---

## 4. 关键结论和发现

### 主要发现
1. **高质量数据是 GUI Agent 性能上限的关键决定因素**，远超模型规模的影响；
2. **解耦训练策略（SFT + GRPO）能有效分离感知与规划能力的学习过程**，避免相互干扰；
3. **统一 action space + 层级 taxonomy 设计** 可极大增强跨平台泛化能力和任务覆盖率；
4. **MoE 架构为 GUI Agent 提供了高性能与低延迟之间的理想平衡点**；
5. **OS-Nav 填补了中文移动与 Linux 桌面场景的评估空白**，推动社区更贴近真实世界需求。

---

### 方法的局限性
- **依赖截图输入**：未接入 Accessibility Tree 或 DOM 结构，可能在复杂布局下丢失语义信息；
- **仿真环境偏差**：合成数据基于模拟器生成，与真实设备存在交互差异；
- **安全性考虑不足**：未讨论权限控制、隐私保护、防误操作等安全机制；
- **长期记忆缺失**：当前模型未显式建模长期上下文记忆，不利于超长任务执行。

---

### 未来工作方向
- 扩展至更复杂的现实工作流（如多应用协同办公、自动化脚本生成）；
- 引入 **self-correction 机制** 和 **failure recovery 能力**，提升容错性；
- 加强 **安全约束建模**，防止越权操作或敏感信息泄露；
- 探索 **multi-modal 输入融合**（语音+手势+视觉）以实现更自然的人机协作；
- 将 OS-Nav 扩展至更多操作系统（Windows/macOS）和语言区域。

--- 

> 🔚 **总结一句话**：  
> OmegaUse 通过“高质量数据 + 解耦训练 + MoE 架构 + 新型 benchmark”四位一体的设计，实现了当前最先进（SOTA）的 GUI Agent 性能，尤其在中文移动与 Ubuntu 桌面场景展现出强大实用潜力。

</details>

---

### 7. [Online Density-Based Clustering for Real-Time Narrative Evolution Monitorin](https://arxiv.org/abs/2601.20680)

**Authors**: Ostap Vykhopen, Viktoria Skorik, Maxim Tereschenko, Veronika Solopova  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.20680v1  

#### Abstract
Automated narrative intelligence systems for social media monitoring face significant scalability challenges when processing continuous data streams using traditional batch clustering algorithms. We investigate the replacement of HDBSCAN (offline clustering) with online (streaming/incremental) clust...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Online Density-Based Clustering for Real-Time Narrative Evolution Monitoring**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
传统基于 batch 模式的聚类算法（如 HDBSCAN）在处理社交媒体流数据时面临严重可扩展性挑战：
- **内存瓶颈**：每次聚类需加载全部文档嵌入（O(NlogN) 复杂度），难以应对持续增长的数据。
- **计算低效**：每日需重新对全量数据进行聚类，无法复用历史结构。
- **缺乏实时性**：无法支持增量更新，导致无法实时监测叙事（narrative）的演化。
- **参数敏感**：生产环境中频繁调整参数以维持稳定的聚类粒度。

这些问题阻碍了自动化 narrative intelligence 系统在危机监控、舆情分析等场景中的实际部署。

### **提出了什么新方法或新思路**
本文提出将 **online / streaming clustering 算法** 替代传统的 HDBSCAN，构建一个适用于真实生产环境的三阶段架构（数据采集、建模、仪表盘生成），并引入以下创新设计：
- **滑动窗口模拟框架**：在历史数据上模拟时间序列预训练 + 增量日更新，更贴近现实部署条件。
- **混合评估体系**：结合传统聚类指标（Silhouette, DBI）与面向 narrative 的专用指标（Distinctness, Contingency, Variance），实现多维度比较。
- **系统级兼容性考量**：强调在线算法必须满足自动确定簇数、抗噪性强、内存占用小、增量更新快等工程要求。

### **相比现有方法的优势**
- **效率显著提升**：DenStream 的训练时间仅需 3.56 秒，远低于 HDBSCAN 的 13 秒（且后者为 batch 全局重训）。
- **更好的聚类质量**：DenStream 在 Silhouette 和 Davies-Bouldin Index 上均优于 HDBSCAN。
- **更适合流式部署**：支持增量学习与渐进式遗忘（handling concept drift），适应动态演化的 narrative 结构。
- **模块化解耦**：通过“先嵌入后聚类”范式，解耦表示学习与聚类分配，便于集成到现代 NLP 流水线中。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- 来源：乌克兰信息空间（Ukrainian information space）
- 内容类型：新闻网站、Telegram 频道、用户评论
- 数据特征：
  - 多语言（俄语、乌克兰语、混合 surzhyk）
  - 高噪声、高话题多样性
  - 日均约 17,000 篇文档
- 时间范围：共 7 天（前 6 天用于预训练，第 7 天用于测试）

### **实验设置**
- **Pipeline 架构**：
  1. **Embedding Generation**：使用 `all-MiniLM-L6-v2` 模型生成多语言句子嵌入
  2. **Dimensionality Reduction**：采用 UMAP 降维
  3. **Clustering**：替换不同聚类算法进行对比
  4. **LLM-based Labeling**：使用固定参数的私有 LLM 生成可解释的主题标签和摘要
- **评估模式**：
  - **Baseline (HDBSCAN)**：每天独立运行，不利用历史数据（strict global mode）
  - **Online 方法**：先在前 6 天数据上初始化模型，再对第 7 天数据进行增量更新与预测

### **评估指标**
| 类型 | 指标 |
|------|------|
| **传统聚类指标** | - Silhouette Score （越高越好）<br>- Davies-Bouldin Index (DBI) （越低越好） |
| **Narrative 专用指标** | - Narrative Distinctness （越高越好）<br>- Narrative Contingency （越低越稳定）<br>- Narrative Variance （越低越紧凑） |
| **操作性能指标** | - Training Time<br>- Prediction Time<br>- Cluster Count<br>- Narrative Candidate Count |

### **基线方法对比**
- **Baseline**：HDBSCAN（当前主流 narrative 发现工具，如 BERTopic 中默认使用）
- **Online 对比算法**：
  - **DBSTREAM**
  - **DenStream**
  - **TextClust**（因性能问题未列入最终结果）
- 所有 online 方法来自 River 库，但 DenStream 因实现缺陷改用 pyDenStream

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 1 & 2）**

#### **表 1：聚类质量指标对比**

| Algorithm     | Silhouette | DBI    | Distinct. | Contingency | Variance |
|---------------|------------|--------|-----------|-------------|----------|
| HDBSCAN       | 0.592      | 0.550  | 0.352     | 0.299       | 0.286    |
| DBSTREAM      | 0.327      | 1.220  | 0.266     | 0.630       | 0.459    |
| **DenStream** | **0.685**  | **0.453** | **0.319** | 0.389       | 0.319    |

> ✅ DenStream 在所有传统聚类指标上全面超越 HDBSCAN  
> ❌ DBSTREAM 表现较差，表明其 micro-cluster 表示在高维语义空间中不够精细

#### **表 2：操作性能指标对比**

| Algorithm     | Train (s) | Pred. (s) | Clusters | Narratives |
|---------------|-----------|-----------|----------|------------|
| HDBSCAN       | 13        | 1.3       | 1063     | 118        |
| DBSTREAM      | 232       | 127       | 266      | 58         |
| **DenStream** | **3.56**  | **1.7**   | **303**  | **53**     |

> ✅ DenStream 训练速度最快，远优于其他方法  
> ⚠️ HDBSCAN 虽然推理快，但每次需完整 re-clustering，不具备增量能力  
> 🔍 HDBSCAN 产生超过 1000 个 cluster，而 DenStream 仅 303 —— 可能反映前者过度碎片化

### **消融实验结果（隐含分析）**
- **River 库实现存在严重缺陷**：
  - DenStream 的 `predict_one` 方法每次调用都重新执行 DBSCAN-like 聚类，导致预测极慢。
  - Micro-cluster 统计维护错误，影响中心、半径和权重准确性。
- 因此作者转而使用 **pyDenStream** 实现，更符合原始论文规范，避免重复聚类。
- CluStream 因合并 micro-cluster 时复杂度达 O(M²)，被提前排除。

---

## **4. 关键结论和发现**

### **主要发现**
1. **DenStream 是最有前景的 online 替代方案**：
   - 在聚类质量和计算效率之间取得最佳平衡。
   - 显著优于 HDBSCAN 的 Silhouette 和 DBI 指标。
   - 支持增量学习与概念漂移适应，适合 narrative 演化监测。

2. **HDBSCAN 的优势在于稳定性而非质量**：
   - 更低的 Contingency 和 Variance 表明其每日输出更一致。
   - 这种“稳定性”源于全局优化，但也意味着无法继承历史结构，资源消耗大。

3. **在线算法天然具有更高变异性**：
   - Incremental 更新机制导致 narrative 结构随时间波动更大。
   - 这可能是动态场景下的合理行为，而非缺陷。

4. **下游 pipeline 存在结构性偏见**：
   - 当前 LLM labeling 模块是基于 HDBSCAN 输出微调的，可能偏好其聚类形状。
   - 导致 narrative metrics 对 online 方法不公平地惩罚。

### **方法的局限性**
- **领域特定性**：实验数据来自战时高强度信息环境（乌克兰），结果可能不适用于话题演变缓慢的领域（如学术文献）。
- **依赖上游表示**：聚类效果受 embedding 模型（MiniLM）和 UMAP 参数影响，非孤立评估。
- **Narrative 定义简化**：将 cluster 直接视为 narrative，忽略了 narrative 本身的流动性与跨簇传播特性。
- **软件实现限制**：部分性能差异源于库的工程实现问题，而非算法本质差异。

### **未来工作方向**
1. **缓解 temporal instability**：
   - 探索超参数调优或后处理稳定技术（post-processing stabilization），降低 Contingency/Variance。
2. **适配 downstream pipeline**：
   - 重新训练或微调 LLM labeling 模块以匹配 online clustering 输出分布。
3. **长期动态研究**：
   - 开展纵向研究，评估算法在季节性周期、罕见事件冲击下的表现。
4. **改进算法实现**：
   - 推动 River 等主流库修复 DenStream 的冗余计算问题，提升实用性。
5. **探索神经流聚类**：
   - 尝试结合 deep clustering 或 online variational inference 的新型方法，在保持可解释性的同时增强表达能力。

---

> 📌 **总结一句话**：  
> **DenStream 在效率和聚类质量上优于 HDBSCAN，是最具潜力的 online 替代方案，但需系统性重构下游 pipeline 并解决 temporal 不稳定性问题才能真正落地于 real-time narrative monitoring 系统。**

</details>

---

### 8. [A Learning-based Framework for Spatial Impulse Response Compensation in 3D Photoacoustic Computed Tomography](https://arxiv.org/abs/2601.20291)

**Authors**: Kaiyi Yang, Seonyeong Park, Gangwon Jeong, Hsuan-Kai Huang, Alexander A. Oraevsky, Umberto Villa, Mark A. Anastasio  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.20291v1  

#### Abstract
Photoacoustic computed tomography (PACT) is a promising imaging modality that combines the advantages of optical contrast with ultrasound detection. Utilizing ultrasound transducers with larger surface areas can improve detection sensitivity. However, when computationally efficient analytic reconstr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Learning-based Framework for Spatial Impulse Response Compensation in 3D Photoacoustic Computed Tomography

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在三维 **Photoacoustic Computed Tomography (PACT)** 中，使用大孔径超声换能器可提升检测灵敏度，但其 **Spatial Impulse Response (SIR)** 会导致重建图像出现空间变化的模糊（spatially varying blurring）。传统基于解析模型的重建方法（如 UBP）通常假设理想点状换能器，忽略 SIR 效应，从而导致分辨率下降。

尽管优化类迭代重建方法可以显式建模 SIR，但其计算成本高昂，尤其在 3D 场景中难以实用化。

> **本研究旨在解决：如何在保持高计算效率的同时，有效补偿 SIR 引起的图像退化？**

---

### 🚀 提出的新方法与创新思路

提出了一种**基于学习的数据域 SIR 补偿框架**，核心思想是：

- 在**测量数据域（data domain）** 进行学习型 SIR 补偿，将有限尺寸换能器采集到的压力信号 $ p_{\text{rect}} $ 映射为等效的理想点状换能器响应 $ p_{\text{point}} $；
- 补偿后的数据可直接输入高效的解析重建算法（如 Universal Backprojection, UBP），实现快速且高分辨率成像。

#### 创新点包括：
1. **双模型设计**：
   - **纯数据驱动模型（U-Net）**：采用 3D U-Net 架构端到端学习映射关系。
   - **物理启发模型（Deconv-Net）**：结合物理先验，引入可学习的一维反卷积核模拟 SIR，并通过自适应加权融合多个局部补偿结果。
   
2. **高效合成训练数据生成策略**：
   - 使用随机分布的均匀球体（Stochastic Spheres）作为虚拟目标，利用闭式解（closed-form C-D model）快速生成配对数据 $ (p_{\text{rect}}, p_{\text{point}}) $；
   - 避免了耗时的 D-D 成像模型和离散误差，显著降低训练数据生成成本。

3. **首次实现在 3D PACT 中的学习型 SIR 补偿验证**，并应用于真实活体乳腺成像数据。

---

### ⚖️ 相比现有方法的优势

| 方法类型 | 优势 | 局限 |
|--------|------|-------|
| **解析重建方法（如 UBP）** | 快速、易部署 | 忽略 SIR，图像模糊严重 |
| **优化类迭代重建** | 可精确建模 SIR | 计算开销大，难实时应用 |
| **启发式后处理方法** | 较快 | 补偿不充分，效果有限 |
| **本文方法（学习型 + 数据域补偿）** | **兼顾精度与速度，补偿效果接近理想情况，推理速度快** | 依赖训练数据分布，泛化能力需验证 |

> ✅ **核心优势**：提供一种“即插即用”式解决方案——只需一次前向网络推断即可完成 SIR 补偿，后续仍可用 UBP 等快速算法重建，适合临床部署。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

1. **Stochastic Spheres Dataset（训练与 ID 测试）**
   - 合成数据集，包含 10,000 对 $ (p_{\text{rect}}, p_{\text{point}}) $
   - 每个样本含 200 个随机位置、半径（0.125–5 mm）、初始压力值的均匀球体
   - 加入 i.i.d. 高斯噪声（标准差为信号功率 90% 百分位的 2.67%）
   - 用于训练和内部验证（70%/10%/20% 分割）

2. **Deterministic Spheres Dataset（分辨率分析）**
   - 固定六个相同小球（半径 1.2 mm），位于不同深度
   - 用于定量评估空间分辨率（FWHM）
   - 包括四种变体：baseline、high noise（×10）、low/high SOS（1.447 / 1.555 mm/μs）

3. **Stochastic Numerical Breast Phantoms (NBPs)**
   - 更具解剖真实性的 3D 虚拟乳腺模型（共 80 个），涵盖四种 BI-RADS 密度类型
   - 光学模拟使用 MCX，声学传播使用 k-space pseudospectral method（j-Wave）
   - 分为两个子集：
     - **NBP-Homogeneous**：均匀 SOS = 1.5 mm/μs
     - **NBP-Heterogeneous**：组织特异性 SOS 分布（更贴近实际）

4. **In-vivo Breast Imaging Data**
   - 来自健康志愿者左右乳腺的真实 PACT 数据（LOUISA-3D 系统）
   - 通过平均相邻探测器信号模拟大孔径换能器输出（5.5 mm × 1.1 mm）
   - 以未 binning 数据作为近似参考

---

### 🔬 实验设置

- **成像系统配置**：基于 LOUISA-3D 系统建模
  - 半球形测量孔径（半径 85 mm）
  - 96 个矩形换能器（1.2 mm × 6 mm），沿弧形排列，旋转 320 角度
  - 采样率 20 MHz，每通道 2267 时间点

- **重建方法**：
  - 所有补偿后数据均使用 **UBP** 重建
  - 对于 NBP-Heterogeneous 数据，采用两区域 UBP（分别设定乳腺与水区 SOS）

- **评估指标**：
  - **FWHM**（全宽半高）：衡量空间分辨率
  - **Relative Squared Error (RSE) ↓**
  - **Normalized Cross-Correlation (NCC) ↑**
  - **DICE coefficient ↑**（基于 Frangi 滤波提取血管图）

- **统计检验**：单侧 Mann-Whitney U 检验（$ p < 0.05 $ 视为显著）

- **基线对比方法**：
  - 无补偿（Uncompensated, $ p_{\text{rect}} \rightarrow $ UBP）
  - 学习型补偿：**U-Net**, **Deconv-Net**

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### ✅ Study 1: Deterministic Spheres（分辨率分析）

| 方法 | 平均 FWHM @ x=55mm (mm) | 接近参考值程度 |
|------|--------------------------|----------------|
| Reference ($ p_{\text{point}} $) | ~0.505 | 最佳 |
| Uncompensated ($ p_{\text{rect}} $) | 明显增大（随距离增加而恶化） | 差 |
| U-Net | ≈0.51 | 接近参考 |
| Deconv-Net | ≈0.505 | 几乎完全恢复 |

> 💡 图 3 显示两种学习方法在所有测试条件下（噪声、SOS 变化）均稳定恢复分辨率，FWHM 接近理论极限（0.5 mm）。

---

#### ✅ Study 2: NBP-Homogeneous（解剖复杂性泛化）

| Metric | Depth | Uncompensated | U-Net | **Deconv-Net** |
|--------|-------|---------------|--------|----------------|
| **RSE ↓** | Surface | 0.660±0.061 | 0.405±0.062 | **0.331±0.048** |
|          | Deeper | 0.589±0.086 | 0.510±0.166 | **0.428±0.144** |
| **NCC ↑** | Surface | 0.582±0.055 | 0.777±0.037 | **0.822±0.027** |
|          | Deeper | 0.640±0.075 | 0.714±0.108 | **0.768±0.085** |
| **DICE ↑** | Surface | 0.237±0.048 | 0.758±0.025 | **0.803±0.011** |
|           | Deeper | 0.298±0.052 | 0.700±0.042 | **0.774±0.022** |

> ✅ 所有指标上，**Deconv-Net > U-Net > Uncompensated**，差异显著（$ p < 0.05 $）

---

#### ✅ Study 3: NBP-Heterogeneous（声速异质性挑战）

| Metric | Depth | Uncompensated | U-Net | **Deconv-Net** |
|--------|-------|---------------|--------|----------------|
| **RSE ↓** | Surface | 0.596±0.054 | 0.418±0.065 | **0.382±0.053** |
|          | Deeper | 0.586±0.079 | 0.537±0.134 | **0.469±0.118** |
| **NCC ↑** | Surface | 0.633±0.044 | 0.774±0.038 | **0.797±0.029** |
|          | Deeper | 0.640±0.066 | 0.699±0.089 | **0.744±0.072** |
| **DICE ↑** | Surface | 0.237±0.043 | 0.707±0.045 | **0.738±0.031** |
|           | Deeper | 0.249±0.054 | 0.591±0.087 | **0.648±0.077** |

> ✅ 即使在训练未见的 **SOS 异质性** 下，两种模型仍显著优于无补偿方法，且 **Deconv-Net 持续领先**

---

### 🔍 消融实验与关键发现

- **Deconv-Net 设计有效性**：
  - 多核反卷积 + 自适应合成机制能够捕捉 SIR 的空间变异性
  - 物理先验约束减少了无效搜索空间，提升了鲁棒性和泛化能力

- **训练数据多样性足够支撑泛化**：
  - 尽管仅在简单球体上训练，模型在复杂 NBP 和真实 in-vivo 数据上表现良好
  - 归因于信号级对齐（signal-level alignment）和数据域学习的物理一致性

- **数据域 vs 图像域学习优势**：
  - 数据域修正更不易引入虚假结构（hallucinations）
  - 重建过程保留物理保真性，避免“视觉合理但物理错误”的伪影

---

## 4. 关键结论和发现

### ✅ 主要结论

1. **成功实现了高效、准确的 3D PACT SIR 补偿**：
   - 所提框架可在数据域有效去除 SIR 引起的空间模糊，显著提升图像分辨率和结构保真度。

2. **Deconv-Net 综合性能最优**：
   - 融合物理先验的 **Deconv-Net** 在所有任务中均优于纯数据驱动的 U-Net，表明“physics-informed deep learning”更具潜力。

3. **高度泛化能力**：
   - 模型在未见过的复杂结构、噪声水平、SOS 异质性甚至真实 in-vivo 数据上均表现出色，证明其适用于实际场景。

4. **首次实现 3D 学习型 SIR 补偿的实际应用验证**：
   - 在真实乳腺成像数据中揭示了被 SIR 掩盖的细微血管结构，具有明确临床价值。

---

### ⚠️ 方法局限性

1. **依赖换能器几何与 SIR 模型已知**：
   - 当前方法需要预先知道换能器形状、尺寸和位置，无法处理未知或动态变化的 SIR。

2. **训练数据基于简化正向模型**：
   - 使用闭式 C-D 模型生成数据，忽略了部分真实物理效应（如声衰减、非线性传播）。

3. **尚未在真正的大孔径硬件上验证**：
   - 实验中通过 binning 小孔径数据模拟大孔径，未来需在物理大孔径系统上进一步验证。

4. **内存与计算资源需求较高（尤其是 Deconv-Net）**：
   - 使用 patch-based 输入缓解内存压力，但仍需高性能 GPU 支持。

---

### 🔮 未来工作方向

1. **扩展至其他换能器形状与阵列几何**（如环形、平面阵列）
2. **联合优化 SIR 补偿与图像重建**（end-to-end joint learning）
3. **集成去噪、校正等多任务功能于一体**
4. **探索无监督或自监督训练方式**，减少对合成标签的依赖
5. **在更多临床数据集上进行前瞻性验证**

---

## 总结一句话

> 本文提出了一种**高效、可推广的数据域学习框架**，首次实现了在 **3D PACT** 中对 **Spatial Impulse Response (SIR)** 的精准补偿，兼具**高精度与低延迟优势**，为推动高灵敏度大孔径 PACT 系统走向临床应用提供了关键技术路径。

</details>

---

### 9. [PathWise: Planning through World Model for Automated Heuristic Design via Self-Evolving LLMs](https://arxiv.org/abs/2601.20539)

**Authors**: Oguzhan Gungordu, Siheng Xiong, Faramarz Fekri  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.20539v1  

#### Abstract
Large Language Models (LLMs) have enabled automated heuristic design (AHD) for combinatorial optimization problems (COPs), but existing frameworks' reliance on fixed evolutionary rules and static prompt templates often leads to myopic heuristic generation, redundant evaluations, and limited reasonin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PathWise: Planning through World Model for Automated Heuristic Design via Self-Evolving LLMs

---

## 1. 论文的主要贡献和创新点

### **解决的问题**

现有的基于 **LLM-based AHD**（Automated Heuristic Design）的方法在组合优化问题（COPs）中存在以下局限：

- **固定演化规则**：依赖预定义的进化算子（如交叉、变异），缺乏灵活性。
- **静态提示模板**：提示（prompt）不随搜索过程动态调整，导致探索效率低。
- **无状态搜索**：将每次启发式生成视为独立事件，缺乏对历史推导路径的记忆，容易重复生成相似启发式，造成冗余评估。
- **试错式演化**：缺乏对“为何某种修改有效”的推理能力，难以实现高效的状态感知规划。

这些问题导致现有方法收敛慢、泛化差、扩展性弱。

---

### **提出的新方法与思路**

本文提出了 **PathWise**，一种基于 **世界模型（World Model）进行规划** 的多智能体推理框架，其核心思想是：

> 将启发式设计建模为一个**序列决策过程**，通过构建**蕴含图（entailment graph）** 作为紧凑、有状态的记忆结构，实现状态感知的演化。

#### **核心创新点**

1. **蕴含图（Entailment Graph）作为状态记忆**
   - 每个节点表示一个启发式 `h`，包含代码、描述、推导理由、性能和父节点元数据。
   - 有向边表示从父节点集合到子节点的推导关系。
   - 图结构编码了启发式的演化历史，使后续决策可基于完整上下文进行。

2. **多智能体协同框架**
   - **Policy Agent**：高层规划者，选择父节点并生成自然语言推导指令 `K`，决定“如何改进”。
   - **World Model Agent**：执行者，根据指令生成具体的启发式代码。
   - **Critic Agents**（双批评家）：
     - `Policy Critic`：分析不同推导策略的有效性，指导未来父节点选择和指令设计。
     - `World Model Critic`：对比最佳与最差生成结果，提炼代码级改进建议。
   - 批评家提供**路由反馈（routed reflections）**，引导后续步骤，实现**自演化（self-evolving）**。

3. **Prompt-Level 多样性机制**
   - **多样性感知提示扰动**：引入随训练步数衰减的探索率，以概率采样“探索性短语”注入提示，鼓励早期广泛探索。
   - **状态打乱（State Shuffling）**：随机打乱输入上下文中节点的顺序，减少位置偏见（positional bias）。

4. **混合图-种群架构**
   - 外层维护固定大小的种群（root nodes）。
   - 内层在每个外层迭代中构建蕴含图，仅保留叶节点用于下一轮种群更新。
   - 平衡了树搜索的结构性与种群方法的紧凑性。

---

### **相比现有方法的优势**

| 特性 | 现有方法（如 FunSearch, MCTS-AHD） | PathWise |
|------|-------------------------------------|---------|
| **状态感知** | ❌ 无状态，独立采样 | ✅ 蕴含图提供状态记忆 |
| **演化规则** | ❌ 固定算子/UCT 规则 | ✅ 动态生成推导指令（自然语言） |
| **推理能力** | ❌ 缺乏对“为何成功”的理解 | ✅ 批评家提供语义级反思 |
| **探索效率** | ❌ 易陷入局部最优，重复生成 | ✅ 多样性机制 + 状态记忆避免冗余 |
| **收敛速度** | ❌ 较慢 | ✅ 更快收敛（见实验） |

---

## 2. 核心实验方法和设置

### **使用的数据集**

在多个经典 **NP-hard COPs** 上进行测试，涵盖不同搜索框架：

| 问题 | 框架 | 数据集说明 |
|------|------|-----------|
| **TSP** | Constructive, ACO, GLS | 随机生成节点坐标，距离矩阵；TSPLIB 实例 |
| **Knapsack Problem (KP)** | Constructive | 随机权重/价值，容量固定 |
| **Capacitated VRP (CVRP)** | ACO | 客户需求、车辆容量、距离矩阵 |
| **Multiple Knapsack (MKP)** | ACO | 多背包，物品在各包中权重不同 |
| **Orienteering Problem (OP)** | ACO | 收集奖励，受路径长度约束 |
| **Bin Packing Problem (BPP)** | Constructive (Online), ACO (Offline) | 在线：物品流式到达；离线：所有已知 |

---

### **实验设置**

- **LLM 后端**：
  - `GPT-4o-mini`（非推理型）
  - `GPT-5-nano`（低/中等推理级别）
- **评估预算**：`ne = 500` 次启发式评估（基线用 1000 次）。
- **参数设置**：
  - 种群大小 `Np = 6`
  - 每步动作数 `Na = 2`
  - 每动作 rollout 数 `Nu = 2`
  - 最大内层步数 `Imax = 3`
- **温度**：所有代理设为 1.0。

---

### **评估指标**

- **目标值（Obj.↓）**：越小越好（如 TSP 路径长度）。
- **最优性差距（Gap%）**：相对于最优解或最强基线的相对差距。
- **平均相对差距改进（MRGI）**：
  $$
  \text{MRGI} = \frac{\text{Gap}_{\text{baseline}} - \text{Gap}_{\text{PathWise}}}{\text{Gap}_{\text{baseline}}} \times 100\%
  $$
- **选择多样性率（SDR）**：唯一父节点选择比例，衡量探索广度。

---

### **基线方法对比**

| 类别 | 方法 |
|------|------|
| **手动设计** | Greedy Construct, ACO, KGLS, Best Fit, First Fit |
| **神经组合优化（NCO）** | POMO, DeepACO, NeuralGLS, GNNGLS |
| **LLM-based AHD** | FunSearch, EoH, ReEvo, HSEvo, MCTS-AHD |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **表 1：Constructive 框架下的 TSP 和 KP 结果**

| 方法 | TSP (N=200) Gap% | KP (N=500) Gap% |
|------|------------------|----------------|
| **POMO** | 21.52% | 3.12% |
| **MCTS-AHD** | 15.82% | 1.88% |
| **PathWise (Ours)** | **14.63%** | **0.05%** |

- **MRGI 改进**：
  - TSP 平均提升 **20.38%**，最高达 **31.82%**（GPT-5-nano low）。
  - KP 平均提升 **49.89%~65.20%**，最大提升 **81.34%**（N=500, W=25）。

#### **表 2：ACO 框架下的综合表现**

| 任务 | PathWise MRGI 提升 |
|------|-------------------|
| TSP | 60.22% |
| CVRP | 38.73% |
| MKP | 89.35% |
| OP | 54.81% |
| Offline BPP | 44.86% |

- 在 **OP (N=200)** 和 **MKP** 上，PathWise 表现优于需专门训练的神经求解器 **DeepACO**。

---

### **与基线方法的对比结果**

- **收敛更快**：如图 1 所示，PathWise 在 **500 次评估内** 即超越基线在 1000 次评估后的性能，且方差更低。
- **泛化更强**：在 OOD（Out-of-Domain）大尺寸实例上提升更显著，表明其具备良好扩展性。
- **稳定性高**：多次运行结果波动小。

---

### **消融实验结果**

#### **表 3：批评家消融（TSP Constructive）**

| 方法 | TSP50 Gap% |
|------|-----------|
| **PathWise (Full)** | 8.79% |
| w/o Policy Critic | 10.21% |
| w/o World Model Critic | 9.63% |
| w/o Both Critics | 14.42% |

- **Policy Critic 影响更大**：说明高层策略指导比代码微调更重要。

#### **表 4：多样性机制消融**

| 方法 | TSP50 Gap% | SDR (%) |
|------|-----------|--------|
| **PathWise (Full)** | 8.79% | 75.79% |
| w/o Prompt Perturbation | 9.58% | 70.30% |
| w/o State Shuffling | 9.02% | 61.03% |
| w/o Both | 11.43% | 53.76% |

- **提示扰动** 对性能影响更大，是探索的主要驱动力。
- **状态打乱** 主要缓解位置偏见，提升选择多样性。

---

## 4. 关键结论和发现

### **主要发现**

1. **状态感知规划优于无状态搜索**：蕴含图提供了有效的记忆机制，使系统能“记住过去”，避免重复探索。
2. **多智能体协作实现自演化**：Policy + World Model + Critics 构成闭环，实现了从“试错”到“有计划演化”的转变。
3. **多样性是高效探索的关键**：Prompt-level 扰动和状态打乱显著提升了探索广度，避免早熟收敛。
4. **PathWise 收敛更快、性能更强、扩展更好**：在多种 COPs 和框架下均显著优于现有方法，尤其在大规模问题上优势明显。

---

### **局限性**

1. **LLM 输出的不确定性**：生成的启发式质量受 LLM 随机性影响，可能导致临时不稳定。
2. **计算开销较高**：虽然评估次数少，但每次涉及多个 LLM 调用，token 成本高于部分基线（见表 14）。
3. **对 LLM 能力敏感**：性能随 LLM 推理能力增强而提升，但在弱模型上可能退化。
4. **实现复杂度高**：多代理协调、图管理、反射机制增加了工程复杂性。

---

### **未来工作方向**

1. **应用于更高成本场景**：如分子设计、材料发现等需仿真或实验评估的问题，其中训练时间是主要瓶颈。
2. **学习可迁移的演化策略**：让 Policy 和 Critics 学习跨问题通用的改进模式，减少对特定问题的依赖。
3. **降低 LLM 开销**：探索轻量级代理或缓存机制，减少冗余调用。
4. **结合强化学习**：将整个 PathWise 框架视为一个 RL 环境，进一步优化策略。

---

> **总结**：PathWise 通过引入 **蕴含图** 和 **多智能体协作**，首次将 **LLM-based AHD** 从无状态的“试错搜索”转变为有状态的“规划式演化”，在多个 COPs 上实现了更快收敛、更强性能和更好扩展性，为自动化启发式设计开辟了新范式。

</details>

---

### 10. [Policy of Thoughts: Scaling LLM Reasoning via Test-time Policy Evolution](https://arxiv.org/abs/2601.20379)

**Authors**: Zhengbo Jiao, Hongyu Xian, Qinglong Wang, Yunpu Ma, Zhebo Wang, Zifan Zhang, Dezhang Kong, Meng Han  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.20379v1  

#### Abstract
Large language models (LLMs) struggle with complex, long-horizon reasoning due to instability caused by their frozen policy assumption. Current test-time scaling methods treat execution feedback merely as an external signal for filtering or rewriting trajectories, without internalizing it to improve...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Policy of Thoughts: Scaling LLM Reasoning via Test-time Policy Evolution**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）在复杂、长程推理任务中表现不稳定，主要原因在于其“**冻结策略假设**”（frozen policy assumption）——即模型在推理时无法从失败尝试中学习并动态调整自身的推理逻辑。尽管已有方法通过采样、反思或搜索来提升推理效果，但它们通常仅将执行反馈作为外部信号用于筛选或重写轨迹，而未将其内化为对底层推理策略的优化。

### 🚀 提出的新方法：Policy of Thoughts (PoT)
受波普尔（Popper）认识论“猜想与反驳”（conjectures and refutations）启发，作者提出 **Policy of Thoughts (PoT)**，将推理过程重构为一个**实例级在线优化闭环系统**，实现测试时策略演化（test-time policy evolution）。

#### 核心机制：
- **探索阶段**：使用高效的搜索机制（如 MCTS）生成多样化的候选解（tentative theories）。
- **内化阶段**：利用 **Group Relative Policy Optimization (GRPO)** 将执行反馈转化为梯度更新，作用于一个临时的 **LoRA adapter**，从而动态调整模型的推理先验。
- **闭环设计**：形成“生成 → 执行 → 反馈 → 策略更新”的迭代循环，使模型能在单个问题求解过程中实时进化其推理能力。

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | PoT |
|------|--------|-----|
| **策略是否更新** | ❌ 冻结策略，仅筛选轨迹 | ✅ 动态演化策略 |
| **反馈用途** | 外部信号（用于排序/重写） | 内部信号（用于参数更新） |
| **计算效率** | 高开销采样，大量无效尝试 | 更高效地利用计算资源，减少冗余探索 |
| **小模型潜力** | 性能受限于参数规模 | 能让小型模型超越超大模型 |

> PoT 实现了从“静态搜索”到“动态适应”的范式转变，是 test-time scaling 的一次本质升级。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验涵盖五个主流代码与数学推理基准，总计 **836 个编程问题**：
- **HumanEval**（164题）：函数级代码生成
- **MBPP**（257题）：基础Python编程任务
- **LiveCodeBench v5 & v6**（各 ~170题）：去污染、多轮交互的真实编码评测
- **ICPC**（73题）：来自 OJBench 的竞赛级编程挑战

> 所有实验均在相同推理预算下进行，确保公平比较。

### 🎯 评估指标
- 主要指标：**Pass@1 Accuracy**（完全通过所有测试用例）
- 辅助指标：平均准确率（Overall），跨多个基准的无加权平均

### ⚙️ 实验设置
- **基础模型**：
  - `Qwen3-4B-Instruct-2507`（主实验）
  - `Qwen3-1.7B-Thinking`, `Phi-3-mini-4k-instruct`（验证泛化性）
- **搜索机制**：Monte Carlo Tree Search (MCTS)，每步扩展 $k=3$ 个子节点
- **策略更新机制**：
  - 使用 **LoRA**（rank=8）进行参数高效微调
  - 采用 **GRPO** 进行相对优势优化，基于组内奖励差异更新策略
- **硬件环境**：单张 NVIDIA A800 (80GB) GPU，使用 vLLM 推理后端

### 🆚 对比的基线方法
分为四类：
1. **标准提示法**：
   - Zero-shot / Few-shot + CoT
   - Best-of-N (N=20)
2. **自改进框架**：
   - Self-Refine, Reflexion, CodeT, LDB
3. **结构化搜索方法**：
   - ToT, RAP, PG-TD, LATS, AB-MCTS, RethinkMCTS
4. **前沿商业大模型**（zero-shot）：
   - GPT-4o, DeepSeek-V3, Claude-Opus-4, Gemini-2.5-flash, Qwen3-235B-A22B

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| 方法 | HumanEval | MBPP | LCB V5 | LCB V6 | ICPC | **Overall** |
|------|-----------|-------|--------|--------|------|------------|
| GPT-4o | 92.70 | 87.60 | 29.94 | **29.71** | 6.80 | **49.75** |
| DeepSeek-V3 | 91.50 | 87.60 | 31.74 | 16.00 | 9.59 | 50.55 |
| Gemini-2.5-flash | 94.01 | 81.70 | 64.07 | 56.57 | 8.22 | 51.12 |
| **PoT (Ours, 4B)** | **98.78** | **94.94** | **57.49** | **49.71** | **19.18** | **58.98** |

> 💡 **PoT-4B 在 Overall 上达到 58.98%，显著优于所有更大规模的商业模型（如 GPT-4o、Claude-Opus-4）**

#### 特别亮点：
- 在 **LiveCodeBench v6** 上，PoT-4B 达到 **49.71%**，超过 GPT-4o（29.71%）**整整 20 个百分点**
- 在 **ICPC**（竞赛级难度）上，PoT 达到 19.18%，远超其他方法（最高仅 16.43%）
- 一个 **4B 模型**的表现超过了 **235B 甚至 400B 级别的模型**

### 🔁 与其他算法基线对比
| 方法 | Overall |
|------|---------|
| Best-of-N (N=20) | 50.22 |
| Reflexion | 49.20 |
| LATS | 50.75 |
| AB-MCTS | 52.96 |
| RethinkMCTS | 52.05 |
| **PoT (Ours)** | **58.98** ✅ |

> PoT 比最强的搜索方法（AB-MCTS）高出近 **6 个点**

### 🔍 消融实验结果

#### ✅ Ablation on Test-time Evolution（表3）
| 配置 | LCB v6 准确率 | Δ vs Zero-shot |
|------|---------------|----------------|
| Zero-shot | 27.43% | — |
| Search Only (no LoRA update) | 37.14% | +9.71 |
| **PoT (Search + LoRA 更新)** | **49.71%** | **+22.28** |

> 表明：**策略内化（policy internalization）带来的增益远大于纯搜索**

#### ✅ Ablation on Branching Factor $k$（表4）
| $k$ | Acc (%) | 成本 (ms) |
|-----|--------|----------|
| 1 (Greedy) | 31.42 | 161.43 |
| 2 | 41.14 | 309.87 |
| **3 (Ours)** | **49.71** | **473.66** |
| 8 | 54.86 | 2369.96 |
| 16 | 55.42 | 3984.52 |

> $k=3$ 是性能与效率的最佳平衡点；更大的 $k$ 带来边际收益递减

#### ✅ Ablation on LoRA 参数（表5）
| 设置 (r, lr) | Acc (%) | Δ vs Ours |
|-------------|--------|----------|
| LoRA(8, 1e-4) (Ours) | 49.71 | — |
| LoRA(4, 5e-5) | 43.43 | -6.28 |
| LoRA(16, 1e-4) | 50.86 | +1.15 |

> 表明 LoRA 的秩（rank）和学习率需合理配置，但即使轻量更新也能带来显著提升

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **测试时策略演化是可行且强大的**：
   - 即使是小型模型（4B），通过 test-time policy evolution 也能超越数十倍大的模型。
   - “**内化失败经验**”比“丢弃错误路径”更有效。

2. **PoT 实现了架构无关的推理增强**：
   - 在 Qwen、Phi 等不同架构的小模型上均取得一致提升（见 Table 2），证明其通用性。

3. **计算资源被更高效利用**：
   - 尽管引入了反向传播开销（约增加 1.46× 延迟），但由于收敛更快，总体成本更低（见公式 11）。

4. **适用于环境反馈明确的任务**：
   - 如代码执行、形式化证明等提供确定性反馈的场景，PoT 效果尤为突出。

### ⚠️ 局限性
- 当前依赖可执行环境（如代码运行器）提供精确反馈，在开放域问答或模糊语义任务中需借助外部 Reward Model（如 GPT-5），效果受限于 RM 质量。
- 实时训练引入额外延迟，不适合极低延迟场景。
- transient LoRA 的生命周期短，难以长期保留“学到的知识”。

### 🔮 未来工作方向
- 扩展至 **multi-modal reasoning** 和 **embodied reasoning**（具身智能）
- 应用于 **long-horizon scientific discovery** 和 **tool-augmented problem solving**
- 探索如何将 test-time 学习的知识迁移到后续任务中（meta-learning extension）
- 结合 memory 或 cache 机制，实现跨实例的经验积累

---

## 总结一句话
> **PoT 通过将推理视为“猜想-反驳-进化”的闭环过程，首次实现了 LLM 在测试时的策略演化，使得小模型也能具备媲美甚至超越超大模型的复杂推理能力，为 test-time scaling 开辟了全新路径。**

</details>

---

### 11. [CtrlCoT: Dual-Granularity Chain-of-Thought Compression for Controllable Reasoning](https://arxiv.org/abs/2601.20467)

**Authors**: Zhenxuan Fan, Jie Cao, Yang Dai, Zheqi Lv, Wenqiao Zhang, Zhongle Xie, Peng LU, Beng Chin Ooi  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.20467v1  

#### Abstract
Chain-of-thought (CoT) prompting improves LLM reasoning but incurs high latency and memory cost due to verbose traces, motivating CoT compression with preserved correctness. Existing methods either shorten CoTs at the semantic level, which is often conservative, or prune tokens aggressively, which c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CtrlCoT: Dual-Granularity Chain-of-Thought Compression for Controllable Reasoning

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
Chain-of-Thought (CoT) prompting 能显著提升 Large Language Models (LLMs) 在复杂推理任务上的表现，但其生成的冗长中间推理轨迹带来了高昂的解码延迟、内存占用和推理成本。因此，如何在保持推理正确性的前提下压缩 CoT 长度，成为高效推理的关键挑战。

现有方法存在两大瓶颈：
- **Semantic-level compression**（如 LC-Prompt）虽然能保持高准确率，但压缩比有限（通常 <15%），过于保守；
- **Token-level pruning**（如 TokenSkip）虽可实现更高压缩比（>40%），但因缺乏语义理解，容易误删关键推理线索（如数字、运算符），导致准确率大幅下降。

此外，简单地将两种方法结合会面临三大技术难题：
1. **Sequential Dependency**：语义压缩改变了 token 分布，使基于原始 CoT 训练的 token pruner 失效；
2. **Task-Agnostic Blindness**：通用 token pruner 忽视数学推理中的逻辑关键元素；
3. **Distribution Mismatch**：剪枝后的片段化输出与自然流畅的推理风格不一致，影响多步推理连贯性。

### 提出了什么新方法或新思路
本文提出 **CtrlCoT**，一种**双粒度（dual-granularity）CoT 压缩框架**，首次系统性地协同优化语义级抽象与 token 级剪枝，实现可控且高效的推理。

其核心由三个模块构成：
- **Hierarchical Reasoning Abstraction (HRA)**  
  生成四种不同抽象层级的 CoT（Detailed, Standard, Concise, Ultra-Concise），为后续 token 剪枝提供多层次语义补偿，缓解 Sequential Dependency。
  
- **Logic-Preserving Distillation (LPD)**  
  利用 GPT-4 构建高质量数学 CoT 的 token 级压缩目标，对 LLMLingua2 进行蒸馏训练，使其具备“逻辑感知”能力，保留关键推理元素（numbers, operators, logical connectives），解决 Task-Agnostic Blindness。

- **Distribution-Alignment Generation (DAG)**  
  训练一个 Multi-Ratio CoT Generator (MCG)，在指定压缩比例下生成**流畅、连贯**的 CoT，作为监督信号，使训练分布与推理时的生成风格对齐，消除 Distribution Mismatch。

最终训练两个推理器：
- **Budget-Controlled Reasoner (BCR)**：接受用户指定的 token 预算 `b`，生成符合长度约束的推理链；
- **Budget-Free Reasoner (BFR)**：自动为每个问题生成最短且正确的 CoT，无需人工设定预算。

### 相比现有方法的优势
- **更优的 accuracy-cost 权衡**：在显著降低 token 数量的同时，大幅提升准确率；
- **可控性强**：支持灵活的 token 预算控制，适用于不同资源场景；
- **自动化程度高**：BFR 实现免预算推理，提升可用性；
- **鲁棒性好**：在极端压缩条件下仍保持较高性能，而基线方法严重退化。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **GSM8K**：小学数学应用题基准，共 7.5K 问题，测试基础数学推理能力。
- **MATH-500**：从 MATH 数据集中抽取的 500 道高难度数学题（涵盖代数、几何、微积分等），用于评估复杂推理性能。

### 实验设置和评估指标
#### 模型
使用多个规模的指令调优模型进行验证：
- Qwen2.5-Instruct 系列：3B / 7B / 14B
- LLaMA-3.1-8B-Instruct

#### 评估指标
| 指标 | 定义 | 意义 |
|------|------|------|
| **Accuracy (Acc.)** | 正确回答的比例 | 推理准确性 |
| **CoT Token Count** | 推理链所用 token 数量 | 推理开销 |
| **Compression Ratio (CR)** | 相对于原始 CoT 的 token 减少比例 | 压缩效率 |
| **Token Efficiency (TE)** | $ \text{TE} = \frac{\text{Acc.}}{\text{Tokens}} \times 100 $ | 单位 token 的推理收益 |

#### 压缩强度设置（Compression Strength, CS）
定义了从 `High+++`（最强）到 `Low--`（最弱）的多个压缩等级，对应固定压缩比（CR），确保跨方法公平比较。

---

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Original** | 原始 CoT | 不压缩，作为性能上限 |
| **LC-Prompt** | Prompt-based | 通过提示词引导生成较短 CoT，压缩温和 |
| **Truncation** | Output-based | 截断推理链至目标长度，易丢失关键信息 |
| **TokenSkip** | Token-level pruning | 使用 LLMLingua2 进行 token 跳过压缩 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Qwen2.5-7B-Instruct + MATH-500 为例）

| 方法 | Acc. ↑ | Tokens ↓ | CR ↓ | TE ↑ |
|------|--------|----------|-------|-------|
| Original | 71.2% | 574.66 | 1.00 | 9.11 |
| TokenSkip (High) | 50.4% | 325.68 | 0.57 | 15.48 |
| **Ours (CtrlCoT, High)** | **58.0%** | **225.61** | **0.39** | **25.71** |

> ✅ **节省 30.7% 的 token，同时准确率高出 +7.6 个百分点**

在 GSM8K 上也取得优异表现：
- Qwen2.5-14B-Instruct 下，相比原始模型减少 **55.7% token**，准确率几乎无损（仅下降约 0.3%）。

### 与其他方法全面对比
- 在所有模型尺度（3B/7B/14B）和压缩强度下，CtrlCoT **始终处于 accuracy-token 曲线的帕累托前沿**（见 Figure 4）。
- 相比 TokenSkip，在同等或更低 token 消耗下，准确率普遍高出 **5–10 个百分点**。
- 在强压缩场景（High+++）下优势更加明显：例如在 Qwen2.5-7B 上，TokenSkip 准确率跌至 28.2%，而 CtrlCoT 仍维持在 49.4%。

### 消融实验结果（Ablation Study）

| 变体 | MATH-500 Acc. | Tokens | TE |
|------|---------------|--------|-----|
| w/o HRA | 51.4% | 298.52 | 17.22 |
| w/o LPD | 52.0% | 236.98 | 21.94 |
| w/o DAG | 54.2% | 223.76 | 24.22 |
| **Full CtrlCoT** | **58.0%** | **225.61** | **25.71** |

> 🔍 结果表明：
> - **HRA** 对整体性能提升最大，说明多粒度语义抽象是有效压缩的基础；
> - **LPD** 显著提升准确性，证明逻辑感知蒸馏至关重要；
> - **DAG** 改善生成质量，减少碎片化，提升 TE。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **双粒度压缩优于单一策略**：语义级与 token 级压缩具有互补性，联合优化可突破各自瓶颈。
2. **逻辑感知的 token 剪枝是可行的**：通过蒸馏可让 pruner 学会保留数学推理中的关键元素（如 `+`, `=`, 数字等）。
3. **训练-推理分布对齐至关重要**：直接使用剪枝文本作为监督会导致生成断裂，DAG 机制有效缓解此问题。
4. **预算控制可实现细粒度调节**：BCR 能稳定响应不同 token 预算，CoT 长度随预算收紧单调递减（见 Figure 7）。
5. **BFR 实现高效免配置推理**：在无需手动设预算的情况下，自动生成简洁且正确的 CoT，性能优于 TokenSkip。

### 方法的局限性
- **依赖 backbone 模型的可控性**：当前 LLM 对 “使用约 X 个 token” 这类指令的遵循能力有限，尤其在小模型上可能出现 budget-length mismatch。
- **主要验证于数学领域**：目前实验集中在数学推理任务，是否泛化到其他领域（如代码、常识推理）尚需验证。
- **恢复机制非必须但影响体验**：压缩后 CoT 可读性下降，虽可通过 recovery prompt 恢复，但增加额外计算。

### 未来工作方向
- 将 CtrlCoT 扩展至非数学领域（如科学问答、程序合成）；
- 探索更细粒度的压缩策略（如子句级、表达式级）；
- 提升模型对 budget 指令的精确响应能力；
- 结合 speculative decoding 或 early exiting 进一步加速推理。

--- 

> 📌 **一句话总结**：  
> CtrlCoT 通过 **HRA + LPD + DAG** 三重机制，实现了**高保真、高可控、高效率**的 CoT 压缩，在多个数学推理基准上显著超越现有方法，推动了高效推理技术的发展。

</details>

---

### 12. [REASON: Accelerating Probabilistic Logical Reasoning for Scalable Neuro-Symbolic Intelligence](https://arxiv.org/abs/2601.20784)

**Authors**: Zishen Wan, Che-Kai Liu, Jiayi Qian, Hanchen Yang, Arijit Raychowdhury, Tushar Krishna  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.20784v1  

#### Abstract
Neuro-symbolic AI systems integrate neural perception with symbolic reasoning to enable data-efficient, interpretable, and robust intelligence beyond purely neural models. Although this compositional paradigm has shown superior performance in domains such as reasoning, planning, and verification, it...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：REASON: Accelerating Probabilistic Logical Reasoning for Scalable Neuro-Symbolic Intelligence**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前 **Neuro-Symbolic AI** 系统虽然在数学推理、认知机器人、逻辑规划等任务中表现出色，但由于其 **symbolic 和 probabilistic 推理模块** 存在严重的系统级效率瓶颈，导致难以在边缘设备上实现实时部署。

具体挑战包括：
- **不规则控制流**（irregular control flow）
- **低算术强度**（low arithmetic intensity）
- **非合并内存访问**（uncoalesced memory accesses）
- **CPU/GPU 利用率低下**

这些特性使得传统硬件（如 GPU）无法高效执行 symbolic 和 probabilistic kernels，造成端到端延迟过高（例如 Ctrl-G 在桌面 GPU 上需超过 5 分钟完成单个任务），严重制约了实际应用。

---

### **提出了什么新方法或新思路**
本文提出 **REASON** —— 一个面向 **probabilistic logical reasoning** 的集成化加速框架，首次实现了对神经-符号混合系统的联合算法-架构-系统协同优化。

#### **跨层创新设计**：
1. **算法层**（Algorithm Level）  
   - 引入统一的 **Directed Acyclic Graph (DAG)** 表示，将 FOL、SAT、PC、HMM 等多种 symbolic/probabilistic 模型抽象为共享计算结构。
   - 提出 **自适应剪枝**（adaptive DAG pruning）和 **两输入正则化**（two-input DAG regularization），显著降低模型复杂度并提升硬件友好性。

2. **架构层**（Architecture Level）  
   - 设计基于 **可重构树形处理单元**（tree-based PEs）的硬件架构，支持 symbolic、probabilistic 和部分神经操作。
   - 包含专用组件如 **watched literals unit**、**BCP FIFO**、**Benes 网络互连**，以高效支持 SAT/FOL 推理中的广播-归约模式。

3. **系统层**（System Level）  
   - 与 GPU Streaming Multiprocessors (SMs) 紧耦合集成，通过 **programmable interface** 和 **两级流水线** 实现神经与符号推理的并行执行。
   - 支持灵活的任务调度与同步机制，最大化硬件利用率。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **通用性** | 支持多种 symbolic/probabilistic kernels（FOL, SAT, PC, HMM），而非单一任务专用加速器 |
| **效率** | 针对稀疏、不规则图结构优化，避免传统 GPU 上的内存墙问题 |
| **可扩展性** | 树形拓扑实现 O(log N) 广播延迟，优于 mesh 或 bus 架构 |
| **集成性** | 可作为 GPU 插件共存，无需替换主计算平台 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在 **10 个典型推理任务** 上进行评估，涵盖多个领域：
- 数学定理证明：`IMO`, `MiniF2F`
- 安全检测：`TwinSafety`, `XSTest`
- 文本生成约束满足：`CommonGen`, `News`
- 图谱推理：`CoAuthor`, `AwA2`
- 逻辑问答：`FOLIO`, `ProofWriter`

---

### **实验设置和评估指标**

#### **硬件配置**
- **REASON 实现**：基于 TSMC 28nm 工艺综合，面积 6 mm²，功耗 2.12 W
- **模拟环境**：使用 Accel-Sim + GPGPU-Sim 构建 cycle-accurate 仿真器，模拟与 Orin NX 的集成
- **通信建模**：从真实执行中提取 memory trace，用于精确建模数据传输开销

#### **评估指标**
| 指标 | 说明 |
|------|------|
| **端到端延迟**（End-to-end latency） | 从输入到输出的整体推理时间 |
| **能效比**（Energy efficiency） | 性能 / 能耗（单位：× over baseline） |
| **准确率**（Accuracy/AUPRC/BLEU） | 保证性能无损的前提下评估加速效果 |
| **内存占用**（Memory footprint） | DAG 剪枝后的模型大小缩减比例 |

---

### **基线方法对比**
| 基线 | 类型 | 说明 |
|------|------|------|
| **Orin NX** | 边缘 GPU | 代表主流边缘 AI 平台 |
| **RTX A6000** | 数据中心 GPU | 高性能 GPU 基线 |
| **Xeon CPU** | 通用处理器 | 多核服务器 CPU |
| **TPU** | DNN 加速器 | systolic array 架构 |
| **DPU-like** | DAG 加速器 | tree-based 架构（如 DPU-v2） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 指标 | 结果 |
|------|------|
| **平均速度提升** | **12–50×** 超过桌面/边缘 GPU |
| **能效提升** | **310–681×** 超过 GPU/CPU 基线 |
| **单任务延迟** | **0.8 秒内完成端到端推理**（real-time capable） |
| **芯片面积** | **6 mm²**（TSMC 28nm） |
| **平均功耗** | **2.12 W** |

> ✅ 特别地，在数学推理任务（如 IMO）上，REASON 实现了 **50.65×**（vs Orin NX）、**11.98×**（vs RTX A6000）的速度提升。

---

### **与基线方法的对比结果**
| 对比项 | 发现 |
|--------|------|
| **vs GPU (PyTorch + CUDA)** | symbolic kernels 内存受限，ALU 利用率 < 30%，DRAM BW 利用率达 70% |
| **vs CPU** | 并行效率 < 5%，控制发散严重 |
| **vs TPU-like systolic array** | 神经部分性能相当，但 symbolic 部分慢 70–90× |
| **vs DPU-like tree array** | REASON 在 symbolic 任务上快 **7–10×**，因支持动态调度与冲突处理 |

---

### **消融实验结果（Ablation Study）**

#### **算法优化有效性（Tab. IV）**
| 方法 | 平均内存压缩率 | 准确率变化 |
|------|----------------|-----------|
| 统一 DAG 表示 + 自适应剪枝 | **31.7% ↓** | ≈ 保持（±1%） |
| 例：R2-Guard 在 TwinSafety 上内存减少 **37%**，AUPRC 仅下降 0.006 |

#### **协同设计必要性（Tab. V）**
| 配置 | 归一化运行时间 |
|------|----------------|
| Baseline（原生模型 + Orin NX） | 100% |
| REASON 算法优化 + Orin NX | **78.3% ↓** |
| REASON 算法 + REASON 硬件 | **2.04% ↓**（即 **49× 加速**） |

> 🔍 表明：**算法与硬件协同设计是实现极致加速的关键**。

#### **硬件技术贡献**
- **寄存器银行映射优化**：平均降低 **22%** 运行时间
- **可重构阵列 + 调度策略**：进一步降低至 **56%** 和 **73%**

---

## **4. 关键结论和发现**

### **主要发现**
1. **Probabilistic logical reasoning 是 Neuro-Symbolic AI 的性能瓶颈**  
   - 在多个代表性工作中，symbolic/probabilistic kernels 占据 **>60%** 的端到端运行时间。
   - 其内存带宽利用率高、ALU 利用率低，呈现典型的 **memory-bound** 特征。

2. **统一 DAG 抽象有效捕捉共性结构**  
   - FOL、SAT、PC、HMM 均可被建模为 DAG，支持统一编译、剪枝与映射。
   - 自适应剪枝可在几乎不损失精度的情况下大幅压缩模型。

3. **可重构树形架构适合 irregular symbolic 推理**  
   - 支持高效的 broadcast/reduction 操作，天然契合 DPLL/CDCL 流程。
   - O(log N) 广播延迟确保良好的可扩展性。

4. **与 GPU 紧耦合系统设计至关重要**  
   - 两级流水线（GPU-REASON pipeline + Intra-REASON pipeline）有效隐藏延迟。
   - 共享 L2 缓存与本地内存减少跨设备通信开销。

---

### **方法的局限性**
| 局限 | 说明 |
|------|------|
| **定制化程度较高** | 当前 REASON 需要离线编译 DAG，对动态变化的 symbolic 结构适应能力有限 |
| **工艺节点较旧** | 当前实现基于 28nm，若迁移到 7nm/5nm 可进一步缩小面积与功耗 |
| **未覆盖所有 symbolic 形式** | 如高阶逻辑、动态规划等尚未完全支持 |

---

### **未来工作方向**
1. **支持在线编译与动态 DAG 重构**，增强对开放域任务的适应性
2. **扩展至 LLM + Tools Agent 架构**，支持更复杂的 agentic workflows
3. **探索 3D 集成或近存计算架构**，进一步缓解内存墙问题
4. **开源 REASON 编译器栈与 RTL**，推动社区共建生态

---

> 📌 **总结一句话**：  
> **REASON 通过算法-架构-系统协同设计，首次实现了对 probabilistic logical reasoning 的高效加速，使 Neuro-Symbolic AI 在边缘设备上的实时部署成为可能，为下一代可解释、可验证的认知智能系统奠定了基础。**

</details>

---

### 13. [PILOT: Planning via Internalized Latent Optimization Trajectories for Large Language Models](https://arxiv.org/abs/2601.19917)

**Authors**: Haoyu Zheng, Yun Zhu, Yuqian Yuan, Bo Yuan, Wenqiao Zhang, Siliang Tang, Jun Xiao  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19917v1  

#### Abstract
Strategic planning is critical for multi-step reasoning, yet compact Large Language Models (LLMs) often lack the capacity to formulate global strategies, leading to error propagation in long-horizon tasks. Our analysis reveals that LLMs possess latent reasoning capabilities that can be unlocked when...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PILOT: Planning via Internalized Latent Optimization Trajectories for Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的紧凑型 **Large Language Models (LLMs)** 在多步推理任务中常因缺乏全局战略规划能力而出现“短视”生成行为，导致早期推理错误在长程推理轨迹中不断累积，即 **error propagation**。虽然已有方法通过外部引导（如教师模型）提升推理质量，但这类方法依赖运行时调用大模型，带来显著的 **latency** 和计算开销，难以实用化。

此外，现有的内部适配方法（如 LoRA、ReFT）通常进行静态参数更新，无法为每个输入实例提供动态的战略引导；而直接修改隐状态的方法（如 Coconut）可能破坏预训练表征流形，引发 **catastrophic forgetting**。

### 提出了什么新方法或新思路
本文提出 **PILOT (Planning via Internalized Latent Optimization Trajectories)**，一种非侵入式的框架，旨在将大型模型的战略监督能力内化为模型自身的 **Latent Guidance**。

其核心思想是：
- 不改变主干模型权重（non-invasive），而是引入一个轻量级的 **Hyper-Network**。
- 该网络根据输入查询 $x$ 动态生成一个 **query-conditioned Latent Guidance 向量 $z$**。
- 此向量作为内部引导机制，在 Transformer 的某个深层（pivot layer）注入，引导模型朝向最优推理路径演化。

这种方法实现了“内部化的计划”——让小模型在不依赖外部实时指导的情况下，也能具备类似大模型的高阶规划能力。

### 相比现有方法的优势
| 维度 | PILOT | 现有方法（如 CoT、LoRA、Soft CoT、CAA） |
|------|-------|----------------------------------------|
| **推理延迟** | 几乎无额外开销（仅一次前向传播 Hyper-Network） | 外部引导类方法延迟高；部分方法需重复采样 |
| **适应性** | 实例级别动态引导（instance-specific） | 静态干预（如 LoRA）、固定向量（如 CAA） |
| **稳定性** | 通过 Energy-Aligned Injection 保持表征流形一致，避免“embedding shock” | 直接编辑激活易造成分布偏移 |
| **通用性** | 可迁移至数学与编程等多种复杂推理任务 | 很多方法对特定任务过拟合 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **数学推理任务**：
  - **MATH500**：从 MATH 数据集中筛选出的 500 道难题子集，用于主评估。
  - **AIMO Val**：AMC 水平竞赛题，共 83 题，测试模型在高难度数学问题上的表现。
  - **GSM8K**：小学数学应用题，用于检验鲁棒性和泛化能力。
- **代码生成任务**：
  - **HumanEval**：函数级代码生成基准。
  - **MBPP (Mostly Basic Python Problems)**：基础 Python 编程任务集合。

> 所有训练数据均经过 **Construct-and-Verify Pipeline** 过滤，构建高质量三元组 $(x, g_{\text{exp}}, y^*)$，确保只有当基础模型失败但加入专家启发后成功的问题才被保留。

### 实验设置和评估指标
- **评估指标**：Pass@1 准确率（单次生成即正确）。
- **解码策略**：greedy decoding（temperature=0），保证可复现性。
- **训练方式**：两阶段课程学习（curriculum learning）：
  1. **Phase 1: Latent Alignment**：冻结主干，最小化预测锚点与目标状态之间的余弦距离。
  2. **Phase 2: Anchored SFT**：继续优化适配器组件，同时保留对齐损失和门控正则项。
- **硬件平台**：NVIDIA H20 GPU。
- **报告方式**：5 次独立运行的平均值 ± 标准差。

### 基线方法对比
| 类别 | 方法 | 简要说明 |
|------|------|----------|
| **离散提示法** | Zero-shot CoT | 基础链式思维提示，无任何干预 |
| **静态调参法** | LoRA, Soft CoT | 参数高效微调，学习固定适配 |
| **隐状态干预法** | ReFT, CAA | 修改隐藏层表示，使用低秩或对比向量引导 |
| **扩展计算法** | Pause Tokens, Coconut | 引入额外 token 提供更多“思考时间” |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

#### 数学推理（MATH500 上的 Pass@1）
| 方法 | Qwen2.5-1.5B | Qwen2.5-7B | Llama-3.1-8B |
|------|--------------|------------|---------------|
| Zero-shot CoT | 43.20 | 71.00 | 47.60 |
| LoRA | 47.24 | 72.84 | 47.92 |
| Soft CoT | 49.32 | 73.20 | 48.48 |
| **PILOT (Ours)** | **52.08** (+8.88↑) | **75.24** (+4.24↑) | **51.64** (+4.04↑) |

> ✅ 在所有规模模型上均显著领先，尤其在小模型（1.5B）上提升最大，表明 PILOT 能有效激活潜在推理能力。

#### 代码生成（HumanEval 上的 Pass@1）
| 方法 | Qwen2.5-1.5B | Qwen2.5-7B | Llama-3.1-8B |
|------|--------------|------------|---------------|
| Zero-shot CoT | 46.34 | 71.34 | 53.05 |
| LoRA | 50.12 | 75.73 | 57.07 |
| **PILOT (Ours)** | **56.34** (+10.00↑) | **77.44** (+1.71↑) | **58.41** (+5.36↑) |

> ✅ 在 HumanEval 上全面超越基线，尤其在 1.5B 小模型上优势明显。

### 与基线方法的对比结果
- **优于所有 baselines**：在数学和编码两大领域，PILOT 在不同规模模型上均取得最佳性能。
- **显著优于静态调参方法**：说明动态、实例相关的引导比固定参数更新更有效。
- **优于隐状态干预方法（ReFT/CAA）**：证明 PILOT 的 Energy-Aligned Injection 更稳定，不易引起“embedding shock”。
- **优于 Pause Tokens / Coconut**：说明单纯增加计算预算不如结构性引导有效。

### 消融实验结果（Table 3）

| 消融配置 | MATH (1.5B) | HEval (7B) | MATH (7B) | HEval (7B) |
|---------|-------------|-----------|-----------|-----------|
| **PILOT (Full)** | 52.08 | 56.34 | 75.24 | 77.44 |
| w/o Hyper-Net (静态锚点) | 47.72 ↓ | 50.37 ↓ | 72.84 ↓ | 72.68 ↓ |
| w/o Proto-Thought | 48.84 ↓ | 54.39 ↓ | 74.52 ↓ | 76.71 ↓ |
| w/o Energy-Alignment | 49.16 ↓ | 51.22 ↓ | 75.04 ≈ | **73.17 ↓↓** |

> 🔍 发现：
- **Hyper-Network 至关重要**：去除后性能大幅下降，验证了动态生成的重要性。
- **Proto-Thought prior 有助于冷启动**：尤其在小模型和数学任务中作用明显。
- **Energy-Alignment 对代码任务尤为关键**：去除后 HumanEval 性能暴跌，说明代码生成对能量尺度敏感，需防止结构崩溃。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **LLMs 具备潜藏的推理能力**，可通过合适的内部引导机制被激活。
2. **显式计划可以被内化为隐空间中的锚定向量**，无需依赖外部模型实时干预。
3. **动态、输入条件化的引导优于静态干预**，能够更好地匹配多样化逻辑需求。
4. **Energy-Aligned Injection 是稳定的关键**：通过控制注入强度与上下文能量对齐，避免破坏原有语义结构。
5. **PILOT 实现了极低推理开销下的性能跃升**：Prefill 阶段仅增加 +3.1ms，总延迟增加 <0.2%，远低于 Soft CoT（+6.2%）。

### 方法的局限性
1. **数据构造成本较高**：Construct-and-Verify 流程需要先运行教师模型生成高质量轨迹并过滤，增加了离线准备负担。
2. **插入层为静态超参数**：目前 pivot layer $l'$ 是按任务手动设定（如数学选深层数，代码选中间层），尚未实现完全动态选择。
3. **跨域泛化仍有限**：尽管有初步证据显示可在数学与代码间迁移（见 Appendix E），但未系统探索更广泛的领域迁移能力。

### 未来工作方向
- 设计 **动态层选择机制**，根据输入自动决定最佳锚点注入位置。
- 探索 **完全自蒸馏版本**（如 PILOT(Self)），减少对外部教师模型的依赖。
- 将 PILOT 应用于更多复杂任务，如多跳问答、科学推理、决策规划等。
- 结合检索增强机制，构建可扩展的 **Latent Guidance Memory Bank**。

---

> 📌 **一句话总结**：  
> PILOT 成功地将“战略性规划”这一高级认知能力封装进一个轻量、非侵入的 Latent Guidance 框架中，在几乎零推理代价下显著提升了小型 LLM 在数学与编程等复杂推理任务上的表现，为构建高效、稳定的智能体提供了新范式。

</details>

---

### 14. [SpeechMapper: Speech-to-text Embedding Projector for LLMs](https://arxiv.org/abs/2601.20417)

**Authors**: Biswesh Mohapatra, Marcely Zanon Boito, Ioan Calapodescu  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.20417v1  

#### Abstract
Current speech LLMs bridge speech foundation models to LLMs using projection layers, training all of these components on speech instruction data. This strategy is computationally intensive and susceptible to task and prompt overfitting. We present SpeechMapper, a cost-efficient speech-to-LLM-embeddi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SpeechMapper: Speech-to-text Embedding Projector for LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前将 **Speech Foundation Models (SFM)** 与 **Large Language Models (LLMs)** 结合的主流方法通常采用**联合指令微调 (joint Instruction Tuning, IT)**，即同时训练 SFM 编码器和 LLM。这类方法存在以下问题：
- **计算成本高昂**：需要大规模 GPU 资源和长时间训练。
- **任务/提示过拟合 (task/prompt overfitting)**：模型在训练任务上表现好，但在未见任务上泛化能力差。
- **依赖离散 token 映射**：如 ASR 级联或语音 token 化，会丢失声学信息或引入量化误差。

### **提出的新方法**
作者提出 **SpeechMapper**，一种两阶段、资源高效的 **speech-to-LLM embedding projector** 方法：
1. **Pretraining 阶段**：
   - 仅使用 LLM 的 **embedding layer**（冻结 LLM 主干），通过 MSE 损失将 SFM 输出的语音嵌入映射到 LLM 的文本嵌入空间。
   - 使用 ASR 数据进行训练，无需 LLM 的前向传播，可在廉价硬件（如 V100）上完成。
2. **Adaptation 阶段**：
   - 将预训练好的 SpeechMapper 接到冻结的 LLM 上，进行短时（1K 步）的指令微调（IT）。
   - 引入 **混合损失函数**：`L_stage2 = (1-o)L_CE + o*L_MSE`，其中 `o` 控制对齐损失的权重，防止过拟合。

### **相比现有方法的优势**
| 特性 | SpeechMapper | 传统 Joint IT 方法 | Wav2Prompt / SSR |
|------|--------------|------------------|------------------|
| **计算效率** | ✅ 极高（Stage 1 不依赖 LLM 前向） | ❌ 高昂 | ⚠️ 中等（仍需 LLM 参与训练） |
| **泛化能力** | ✅ 支持零样本迁移 | ❌ 容易过拟合 | ✅ 有一定泛化 |
| **训练灵活性** | ✅ 可复用预训练模块，快速适配新任务 | ❌ 需重新端到端训练 | ⚠️ 有限 |
| **硬件需求** | ✅ 可在 V100 上完成 Stage 1 | ❌ 需 A100/A100 级别 | ⚠️ 通常需高端 GPU |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
| 任务 | 预训练/零样本数据 | 微调数据 | 测试数据 |
|------|------------------|----------|----------|
| **通用训练** | LibriSpeech (960h) | — | — |
| **Speech Translation (ST)** | — | 子采样 ST 数据（来自 IWSLT25 基线） | EuroParlST, CoVoST2 |
| **Spoken Question Answering (SQA)** | — | 子采样 SQA 数据 | SpokenSQUAD, LibriSQA |

### **实验设置**
- **SFM Backbone**: `seamless-m4t-v2-large`（第 24 层输出，帧平均）
- **LLM Backbones**:
  - `Llama-3.1-8B-Instruct`
  - `EuroLLM-9B-Instruct`
- **SpeechMapper 架构**：
  - 2 个压缩块（CNN + Transformer + FFN），逐步压缩序列长度并升维至 LLM 嵌入维度（4096）
- **训练细节**：
  - **Stage 1 (Pretrain)**: 2M 步，4×V100，4 天
  - **Stage 2 (Adaptation)**: 1K 步，1×A100，约 1.5 小时
- **适应策略变体**：
  - **ASR CE**: 仅交叉熵损失（任务特定）
  - **ASR CE+MSE**: 混合损失（任务无关，支持零样本）
  - **ST/SQA CE**: 在目标任务上微调（任务特定）

### **评估指标**
| 任务 | 主要指标 | 辅助说明 |
|------|--------|---------|
| **Speech Translation (ST)** | **COMET**（×100 提升可读性） | 更关注语义而非字面匹配（优于 BLEU） |
| **Spoken QA (SQA)** | **LLM-as-judge 准确率** | 使用多个 LLM（EuroLLM, Gemma3, Llama3.1-70B）作为裁判，判断生成答案是否等价于参考答案 |
| **ASR 性能** | **WER / CER** | 用于评估重建文本质量 |

### **基线方法对比**
- **Topline**: 文本转录 + LLM（理想上限）
- **Pipeline**: ASR + LLM（级联系统）
- **Best Baseline**: `BEST-IWSLT25-IF`（IWSLT25 指令跟随赛道冠军）
  - 同样基于 `seamless-m4t-v2-large` + `Llama-3.1-8B-Instruct`
  - 使用 2K 小时多任务数据，训练 5 天（2×A100）
  - 包含 LoRA 适配器和投影器，高度专业化

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **Speech Translation (ST) – COMET 分数**
| 模型 | EuroParl (en-fr) | CoVoST2 (en-zh) | 备注 |
|------|------------------|----------------|------|
| **Transcripts + LLM (Topline)** | ~85.0 | ~80.0 | 理想上限 |
| **BEST-IWSLT25-IF (in-domain)** | 84.0 | 80.7 | 强专用基线 |
| **SpeechMapper (Stage 2, ASR CE+MSE, zero-shot)** | **79.9** | **72.0** | **零样本接近专用模型** |
| **SpeechMapper (Stage 2, ST CE, in-domain)** | **85.4** | **79.9** | **超越基线，仅 1K 步微调** |

> ✅ **零样本下已接近最强专用模型，微调后全面超越**

#### **Spoken QA (SQA) – 平均准确率**
| 模型 | SpokenSQUAD | LibriSQA Part II |
|------|-------------|----------------|
| **Transcripts + LLM** | 91.1% | 73.4% |
| **BEST-IWSLT25-IF (in-domain)** | — | ~62.5% |
| **SpeechMapper (Stage 2, ASR CE+MSE, zero-shot)** | **75.1%** | **64.3%** |
| **SpeechMapper (Stage 2, SQA CE, in-domain)** | **87.4%** | **68.1%** |

> ✅ **零样本下已优于 BEST-IWSLT25-IF；微调后逼近 Topline**

#### **消融实验结果**
| 设置 | 影响 |
|------|------|
| **Stage 1 预训练本身** | 即使不接 LLM 微调，在 ST 和 SQA 上已有显著性能，证明其强表示能力 |
| **MSE 损失权重 `o`**：
  - `o=0`（纯 CE）：易过拟合为 ASR 输出
  - `o≥0.8`：能成功泛化到 SQA 等非 ASR 任务
  - 最优选择：`o=0.9`（平衡 ASR 性能与泛化能力） |
| **语言识别测试**：
  - `CE only`：Llama 模型常输出英文（30%+）
  - `CE+MSE`：目标语言输出提升至 98%+，显著减少语言漂移 |

---

## **4. 关键结论和发现**

### **主要发现**
1. **预训练阶段是关键**：
   - 仅用 ASR 数据和 embedding 对齐损失，即可学到强大的语音到文本嵌入映射能力。
   - Stage 1 模型本身已具备跨任务潜力，是可复用的“基础模块”。

2. **MSE 损失抑制过拟合**：
   - 在 IT 阶段保留 `L_MSE` 损失（`o>0.8`）能有效防止模型退化为“只会做 ASR”的模式，实现真正的任务泛化。

3. **高效且可扩展**：
   - 整个流程可在 1.5 小时内完成适配（A100），远低于传统方法的数天训练。
   - Stage 1 与 LLM 大小解耦，可扩展至更大 LLM。

4. **性能媲美甚至超越专用模型**：
   - 在零样本设置下，SpeechMapper 已接近或达到 `BEST-IWSLT25-IF` 水平；
   - 在目标任务微调后，全面超越该基线，且使用更少数据和算力。

### **方法的局限性**
1. **命名实体识别 (NER) 表现较差**：
   - 由于直接在 embedding 空间学习，对训练中未见的 named entities（如人名、地名）重建效果差。
   - 常出现拼写错误或完全忽略。

2. **LLM 生成行为不可控**：
   - 特别是 EuroLLM，容易产生冗长解释、拒绝执行指令或修改代词（如 “you” → “I”）。
   - 提示工程难以完全纠正。

3. **重复生成问题**：
   - 部分输出会出现词语重复（如 “her sister her sister…”），可能与序列压缩机制有关。

4. **对 LLM 噪声敏感度差异大**：
   - 不同 LLM 对输入 embedding 噪声容忍度不同（EET 实验显示 Llama 和 EuroLLM 均可容忍 ~1e-3 噪声）。

### **未来工作方向**
1. **改进 Named Entity 处理**：
   - 探索结合 subword 或 token-level 对齐机制。
2. **优化序列对齐策略**：
   - 当前通过 padding 实现长度对齐，未来可探索动态压缩或注意力引导。
3. **扩展至更多模态**：
   - 类似框架可用于图像、音频等其他模态到 LLM 的嵌入投影。
4. **降低对提示工程的依赖**：
   - 设计更鲁棒的嵌入注入机制，减少 LLM 生成偏差。

---

> **总结**：SpeechMapper 提出了一种**解耦、高效、可泛化**的语音到 LLM 集成范式。它通过 **pretrain + lightweight adaptation** 的两阶段设计，在显著降低计算成本的同时，实现了与最强专用模型相当甚至更优的性能，为构建通用语音大模型提供了实用路径。

</details>

---

### 15. [Window-Diffusion: Accelerating Diffusion Language Model Inference with Windowed Token Pruning and Caching](https://arxiv.org/abs/2601.20332)

**Authors**: Fengrui Zuo, Zhiwei Ke, Yiming Liu, Wenqi Lou, Chao Wang, Xvehai Zhou  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.20332v1  

#### Abstract
Diffusion language models (DLMs) generate text through iterative denoising, but inference requires full-sequence attention at every iteration, resulting in substantial redundant computation on masked tokens. Block-wise diffusion can reduce this cost, yet it typically relies on retraining and constra...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Window-Diffusion: Accelerating Diffusion Language Model Inference with Windowed Token Pruning and Caching**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
扩散语言模型（**Diffusion Language Models, DLMs**）虽然在生成质量、并行解码和全局建模方面表现出色，但其推理过程存在显著效率瓶颈。主要问题在于：
- 每个 **diffusion step** 都需对整个序列进行全量注意力计算（full-sequence attention），即使大多数 **mask tokens** 并未被更新。
- 大量计算资源浪费在冗余的 mask token 上，导致推理延迟高、吞吐低。

现有方法如 **block diffusion** 或 **KV caching** 要么需要重新训练模型，要么无法根本减少 mask token 的参与，限制了其在预训练 DLM 上的直接应用。

---

### **提出的新方法与创新思路**
作者提出了 **Window-Diffusion**，一种无需重新训练、适用于标准 DLM 的高效推理框架，核心思想是基于 token 级别的局部性规律进行选择性计算与状态复用。

#### **主要创新点：**
1. **双窗口机制（Dual-Window Mechanism）**
   - **外部窗口（External Window）**：仅保留未解码区域前缀作为上下文，其余远场 token 被剪枝（pruned），避免参与计算。
   - **内部窗口（Internal Window）**：从外部窗口中进一步选取最可能被解码的“活跃 token”（active tokens），仅对这些 token 进行 logits 预测。
   - 其他未激活的未解码 token 作为“缓冲 token”（buffer tokens），其 KV 状态可缓存复用。

2. **阶段级 KV 缓存与周期刷新（Phase-Level KV Caching with Periodic Refresh）**
   - 在每个推理阶段开始时执行一次完整前向传播以初始化 KV 缓存。
   - 后续步骤中复用稳定 token 的 KV 状态，仅更新活跃 token 和近期解码 token 的表示。
   - 每隔固定步数刷新缓存，控制近似误差累积。

3. **自适应终止（Adaptive Termination）**
   - 当生成 `<eos>` 后自动停止解码，避免长序列中的冗余步骤，实现动态长度推理。

---

### **相比现有方法的优势**
| 方法 | 是否需重训 | 是否支持灵活更新顺序 | 是否缩短 mask 序列 | 是否支持 KV 复用 |
|------|------------|------------------------|--------------------|------------------|
| 原始 DLM | ❌ | ✅ | ❌ | ❌ |
| Block Diffusion | ✅ | ❌（受限于块顺序） | ✅ | ✅（块内） |
| DKV-Cache | ❌ | ✅ | ❌ | ✅（仅已解码 token） |
| **Window-Diffusion** | ❌ | ✅ | ✅（通过窗口剪枝） | ✅（buffer + decoded tokens） |

> ✅ 表示具备该特性；❌ 表示不具备或受限

**优势总结：**
- **无需微调或重训**，可直接部署于预训练 DLM。
- **保持扩散模型原有的灵活更新顺序**，不牺牲并行性。
- **同时实现 token pruning 与 KV caching**，双重降低计算开销。
- **正交于其他加速技术**（如量化、蒸馏），易于集成。

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**
- **模型**：
  - `LLaDA`（Large Language Diffusion Model）
  - `Dream`（7B 规模的 diffusion LLM）
- **数据集**：
  - **GSM8K-CoT**：数学推理任务
  - **MATH**：复杂数学问题求解
  - **HumanEval**：代码生成能力测试
  - **MBPP**：Python 函数生成基准

所有实验在 **NVIDIA A6000 GPU** 上以 **FP32 精度**运行，确保公平比较。

---

### **实验设置与评估指标**

#### **评估指标**
- **Accuracy / Pass@1**：生成结果的正确率
- **Throughput (tokens/sec)**：每秒处理的 token 数量
- **Speedup**：相对于原始模型的相对加速比
- **Latency (seconds)**：端到端推理时间

#### **基线方法对比**
| 方法 | 类型 | 说明 |
|------|------|------|
| **Dream (Baseline)** | 原始 DLM | 无任何优化 |
| **Block Diffusion** | 结构化生成 | 分块自回归 + 块内扩散，需重训 |
| **DKV-Cache** | KV 缓存 | 缓存已解码 token 的 KV 状态 |
| **Fast-dLLM (Prefix/Dual-Cache)** | 综合优化 | 支持 KV 缓存与并行解码，部分需配置调整 |

#### **Window-Diffusion 超参数**
- 外部窗口长度：128（Dream），64（LLaDA）
- 内部窗口长度：16
- 缓存刷新周期：32 步
- 早期停止（early stopping）：默认关闭（消融实验中启用）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 2 & Table 6）**

#### **在 Dream 模型上的综合表现（Table 2）**
| 方法 | 平均 Accuracy | 平均 Throughput | 平均 Speedup |
|------|---------------|------------------|--------------|
| Dream (Baseline) | ~61.7 | ~9.0 tokens/s | 1.0× |
| DKV-Cache | ~60.8 | ~13.5 | ~2.4× |
| Fast-dLLM (Prefix-Cache) | ~60.3 | ~19.0 | ~3.8× |
| Fast-dLLM (Dual-Cache) | ~59.9 | ~25.5 | ~5.0× |
| **Window-Diffusion** | **~60.5** | **~31.1** | **~6.6×** |

> 💡 在几乎不损失准确性的前提下，达到最高 **6.6× 推理加速**

#### **在 LLaDA 模型上的表现（Table 6）**
| 方法 | 平均 Accuracy | 平均 Throughput | 平均 Speedup |
|------|---------------|------------------|--------------|
| LLaDA (Baseline) | ~47.7 | ~4.8 | 1.0× |
| DKV-Cache | ~45.8 | ~7.4 | ~1.6× |
| Fast-dLLM (Dual-Cache) | ~44.7 | ~15.3 | ~3.7× |
| **Window-Diffusion** | **~45.3** | **~25.9** | **~5.6×** |

> 🚀 达到 **5.6× 加速**，且性能下降最小

---

### **与 Block Diffusion 的对比（Table 1）**
- 在相同窗口/块大小（L=16）下：
  - Block Diffusion 在 Instruct 版本上性能严重下降（如 MBPP 从 58.8 → 32.2）
  - **Window-Diffusion** 性能更稳定，仅轻微下降
- 表明：**window-based pruning 比 block-wise 更适合保留扩散模型语义完整性**

---

### **消融实验结果**

#### **(1) 外部窗口长度的影响（Figure 6a）**
- 窗口长度 ≥ 128 后，accuracy 基本饱和
- 更大窗口带来轻微 throughput 下降，但收益递减
- **最终选择 128 为平衡点**

#### **(2) 缓存刷新周期的影响（Figure 6b）**
- 刷新周期过短 → 缓存不稳定，accuracy 下降
- 刷新周期过长 → 新解码 token 积压需全量计算，throughput 增益趋缓
- **最优刷新周期为 32**

#### **(3) 生成长度对推理时间的影响（Figure 6c）**
- 随着生成长度增加，传统方法因 O(S²) 复杂度迅速变慢
- **Window-Diffusion 保持稳定的高速比**，尤其在长文本生成中优势明显

#### **(4) 自适应长度推理（Table 3）**
| 方法 | 任务 | Latency (s) | Speedup |
|------|------|-------------|---------|
| Dream | MBPP (1024) | 217.8 | 1.0× |
| WD-Static | MBPP | 32.9 | 6.6× |
| **WD-Adaptive** | MBPP | **2.2** | **99.0×** |

> ✅ **结合自适应终止后，最高可达 99× 端到端加速！**

---

## **4. 关键结论和发现**

### **主要发现**
1. **DLM 推理具有强结构性局部性（Structural Locality）**：
   - 活跃 token 高度集中在未解码区域的前缀（prefix locality）
   - 对远距离未解码 token 的依赖快速衰减
   - 已解码 token 的表示具有阶段性稳定性（temporal stability），适合缓存

2. **全序列重复计算是非必要的**：
   - 可通过局部窗口 + 缓存机制大幅削减冗余计算

3. **Window-Diffusion 实现高效与高质量的平衡**：
   - 在多个任务和模型上均实现 **5–6.6× 推理加速**
   - 准确率损失极小（通常 < 2%）
   - 结合自适应长度可进一步提升至 **99× 端到端加速**

---

### **方法的局限性**
- **窗口大小需手动设定**：不同任务可能需要不同的最优窗口长度
- **极端短文本或高度随机生成场景下增益有限**
- **目前未探索与其他压缩技术（如量化）的联合优化潜力**

---

### **未来工作方向**
1. **动态窗口机制**：根据上下文动态调整窗口范围
2. **与训练过程结合**：设计支持窗口推理的预训练目标
3. **多模态扩展**：将 windowed pruning 应用于图像/音频 diffusion 模型
4. **硬件适配优化**：针对 TPU/GPU 内存访问模式优化缓存策略

---

## **总结**
> 🔍 **Window-Diffusion 是首个系统揭示 DLM 推理中 token 级局部性规律，并据此设计高效推理框架的工作。**
>
> 它通过 **双窗口组织 + 阶段级 KV 缓存 + 自适应终止**，实现了高达 **99× 的端到端加速**，同时几乎不损害生成质量。该方法无需重训、兼容性强，为大规模 diffusion language models 的实际部署提供了强有力的技术路径。

</details>

---

### 16. [MemCtrl: Using MLLMs as Active Memory Controllers on Embodied Agents](https://arxiv.org/abs/2601.20831)

**Authors**: Vishnu Sashank Dorbala, Dinesh Manocha  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.20831v1  

#### Abstract
Foundation models rely on in-context learning for personalized decision making. The limited size of this context window necessitates memory compression and retrieval systems like RAG. These systems however often treat memory as large offline storage spaces, which is unfavorable for embodied agents t...

---

### 17. [ShieldedCode: Learning Robust Representations for Virtual Machine Protected Code](https://arxiv.org/abs/2601.20679)

**Authors**: Mingqiao Mo, Yunlong Tan, Hao Zhang, Heng Zhang, Yangfan He  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.20679v1  

#### Abstract
Large language models (LLMs) have achieved remarkable progress in code generation, yet their potential for software protection remains largely untapped. Reverse engineering continues to threaten software security, while traditional virtual machine protection (VMP) relies on rigid, rule-based transfo...

---

### 18. [AutoOverlap: Enabling Fine-Grained Overlap of Computation and Communication with Chunk-Based Scheduling](https://arxiv.org/abs/2601.20595)

**Authors**: Xinwei Qiang, Yue Guan, Zhengding Hu, Yufei Ding, Adnan Aziz  
**Category**: cs.DC  
**Published**: 2026-01-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.20595v1  

#### Abstract
Communication has become a first-order bottleneck in large-cale GPU workloads, and existing distributed compilers address it mainly by overlapping whole compute and communication kernels at the stream level. This coarse granularity incurs extra kernel launches, forces device-wide synchronizations at...

---

### 19. [Distributional value gradients for stochastic environments](https://arxiv.org/abs/2601.20071)

**Authors**: Baptiste Debes, Tinne Tuytelaars  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.20071v1  

#### Abstract
Gradient-regularized value learning methods improve sample efficiency by leveraging learned models of transition dynamics and rewards to estimate return gradients. However, existing approaches, such as MAGE, struggle in stochastic or noisy environments, limiting their applicability. In this work, we...

---

### 20. [Investigating the Development of Task-Oriented Communication in Vision-Language Models](https://arxiv.org/abs/2601.20641)

**Authors**: Boaz Carmeli, Orr Paradise, Shafi Goldwasser, Yonatan Belinkov, Ron Meir  
**Category**: cs.AI  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20641v1  

#### Abstract
We investigate whether \emph{LLM-based agents} can develop task-oriented communication protocols that differ from standard natural language in collaborative reasoning tasks. Our focus is on two core properties such task-oriented protocols may exhibit: Efficiency -- conveying task-relevant informatio...

---

### 21. [Trajectory2Task: Training Robust Tool-Calling Agents with Synthesized Yet Verifiable Data for Complex User Intents](https://arxiv.org/abs/2601.20144)

**Authors**: Ziyi Wang, Yuxuan Lu, Yimeng Zhang, Jing Huang, Jiri Gesi, Xianfeng Tang, Chen Luo, Yisi Sang, Hanqing Lu, Manling Li, Dakuo Wang  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20144v1  

#### Abstract
Tool-calling agents are increasingly deployed in real-world customer-facing workflows. Yet most studies on tool-calling agents focus on idealized settings with general, fixed, and well-specified tasks. In real-world applications, user requests are often (1) ambiguous, (2) changing over time, or (3) ...

---

### 22. [SAPO: Self-Adaptive Process Optimization Makes Small Reasoners Stronger](https://arxiv.org/abs/2601.20312)

**Authors**: Kaiyuan Chen, Guangmin Zheng, Jin Wang, Xiaobing Zhou, Xuejie Zhang  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20312v1  

#### Abstract
Existing self-evolution methods overlook the influence of fine-grained reasoning steps, which leads to the reasoner-verifier gap. The computational inefficiency of Monte Carlo (MC) process supervision further exacerbates the difficulty in mitigating the gap. Motivated by the Error-Related Negativity...

---

### 23. [Beyond Accuracy: A Cognitive Load Framework for Mapping the Capability Boundaries of Tool-use Agents](https://arxiv.org/abs/2601.20412)

**Authors**: Qihao Wang, Yue Hu, Mingzhe Lu, Jiayue Wu, Yanbing Liu, Yuanmin Tang  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20412v1  

#### Abstract
The ability of Large Language Models (LLMs) to use external tools unlocks powerful real-world interactions, making rigorous evaluation essential. However, current benchmarks primarily report final accuracy, revealing what models can do but obscuring the cognitive bottlenecks that define their true c...

---

### 24. [PEARL: Plan Exploration and Adaptive Reinforcement Learning for Multihop Tool Use](https://arxiv.org/abs/2601.20439)

**Authors**: Qihao Wang, Mingzhe Lu, Jiayue Wu, Yue Hu, Yanbing Liu  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20439v1  

#### Abstract
Large Language Models show great potential with external tools, but face significant challenges in complex, multi-turn tool invocation. They often exhibit weak planning, tool hallucination, erroneous parameter generation, and struggle with robust interaction. To tackle these issues, we present PEARL...

---

### 25. [MuVaC: AVariational Causal Framework for Multimodal Sarcasm Understanding in Dialogues](https://arxiv.org/abs/2601.20451)

**Authors**: Diandian Guo, Fangfang Yuan, Cong Cao, Xixun Lin, Chuan Zhou, Hao Peng, Yanan Cao, Yanbing Liu  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20451v1  

#### Abstract
The prevalence of sarcasm in multimodal dialogues on the social platforms presents a crucial yet challenging task for understanding the true intent behind online content. Comprehensive sarcasm analysis requires two key aspects: Multimodal Sarcasm Detection (MSD) and Multimodal Sarcasm Explanation (M...

---

### 26. [Efficient Multimodal Planning Agent for Visual Question-Answering](https://arxiv.org/abs/2601.20676)

**Authors**: Zhuo Chen, Xinyu Geng, Xinyu Wang, Yong Jiang, Zhen Zhang, Pengjun Xie, Kewei Tu  
**Category**: cs.CL  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20676v1  

#### Abstract
Visual Question-Answering (VQA) is a challenging multimodal task that requires integrating visual and textual information to generate accurate responses. While multimodal Retrieval-Augmented Generation (mRAG) has shown promise in enhancing VQA systems by providing more evidence on both image and tex...

---

### 27. [Regime-Adaptive Bayesian Optimization via Dirichlet Process Mixtures of Gaussian Processes](https://arxiv.org/abs/2601.20043)

**Authors**: Yan Zhang, Xuefeng Liu, Sipeng Chen, Sascha Ranftl, Chong Liu, Shibo Li  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20043v1  

#### Abstract
Standard Bayesian Optimization (BO) assumes uniform smoothness across the search space an assumption violated in multi-regime problems such as molecular conformation search through distinct energy basins or drug discovery across heterogeneous molecular scaffolds. A single GP either oversmooths sharp...

---

### 28. [Going NUTS with ADVI: Exploring various Bayesian Inference techniques with Facebook Prophet](https://arxiv.org/abs/2601.20120)

**Authors**: Jovan Krajevski, Biljana Tojtovska Ribarski  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20120v1  

#### Abstract
Since its introduction, Facebook Prophet has attracted positive attention from both classical statisticians and the Bayesian statistics community. The model provides two built-in inference methods: maximum a posteriori estimation using the L-BFGS-B algorithm, and Markov Chain Monte Carlo (MCMC) samp...

---

### 29. [Parametric and Generative Forecasts of Day-Ahead Market Curves for Storage Optimization](https://arxiv.org/abs/2601.20226)

**Authors**: Julian Gutierrez, Redouane Silvente  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20226v1  

#### Abstract
We present two machine learning frameworks for forecasting aggregated curves and optimizing storage in the EPEX SPOT day-ahead market. First, a fast parametric model forecasts hourly demand and supply curves in a low-dimensional and grid-robust representation, with minimum and maximum volumes combin...

---

### 30. [TINNs: Time-Induced Neural Networks for Solving Time-Dependent PDEs](https://arxiv.org/abs/2601.20361)

**Authors**: Chen-Yang Dai, Che-Chia Chang, Te-Sheng Lin, Ming-Chih Lai, Chieh-Hsin Lai  
**Category**: cs.LG  
**Published**: 2026-01-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.20361v1  

#### Abstract
Physics-informed neural networks (PINNs) solve time-dependent partial differential equations (PDEs) by learning a mesh-free, differentiable solution that can be evaluated anywhere in space and time. However, standard space--time PINNs take time as an input but reuse a single network with shared weig...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
