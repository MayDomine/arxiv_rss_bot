# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-30 05:58:57 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Argus: Token Aware Distributed LLM Inference Optimization](https://arxiv.org/abs/2512.22925)

**Authors**: Panlong Wu, Yifei Zhong, Danyang Chen, Ting Wang, Fangxin Wang  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.22925v1  

#### Abstract
Large Language Models (LLMs) are rapidly being integrated into real-world applications, yet their autoregressive architectures introduce significant inference time variability, especially when deployed across heterogeneous edge-cloud systems. Existing solutions largely neglect the dynamic, stochasti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Argus: Token Aware Distributed LLM Inference Optimization**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前大型语言模型（LLMs）在边缘-云协同系统中的推理部署面临三大挑战：
- **推理时间高度可变**：由于LLM采用自回归架构，输出token长度随输入语义复杂度剧烈变化，导致推理延迟波动大。
- **动态与随机环境**：真实场景中请求负载、网络状态和客户端活动具有显著时变性和不确定性。
- **异构计算资源**：边缘设备（如Jetson Orin）与云端服务器在算力、通信延迟和精度上差异巨大，传统静态调度策略难以适应。

现有方法大多忽略token生成的动态特性，或将问题简化为静态优化，无法应对上述联合挑战。

---

### **提出了什么新方法或新思路**
作者提出 **Argus**，首个**token感知的分布式边缘-云LLM推理优化框架**，其核心由两个模块构成：

#### **(1) Length-Aware Semantics (LAS) 模块**
- 利用一个微调后的预训练语言模型（ModernBERT），结合**特征重校准机制**（受Squeeze-and-Excitation启发），预测每个输入prompt的预期输出token长度。
- 引入**token-length-sensitive特征调制**，增强模型对“解释详细”、“简要列出”等长度相关指令的敏感性。
- 实现**推理前精准工作量估计**，为后续任务卸载提供细粒度画像。

#### **(2) Lyapunov-guided Offloading Optimization (LOO) 模块**
- 将长期**用户体验质量（QoE）优化**建模为考虑LLM **prefilling 和 decoding 成本**的随机优化问题。
- 基于Lyapunov优化理论，将长期目标转化为每时隙最小化问题。
- 设计 **Iterative Offloading Algorithm with Damping and Congestion Control (IODCC)**：
  - 将NP-hard的整数非线性规划（INLP）分解为一系列可高效求解的整数线性规划（ILP）。
  - 引入**拥塞感知代价函数**与**阻尼更新机制**，避免迭代过程中的震荡，提升收敛稳定性。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **建模能力** | 首次显式建模LLM自回归生成带来的token长度动态性，突破传统DNN推理假设。 |
| **优化目标** | 联合优化QoE、延迟、精度与长期资源约束，支持异构设备协同。 |
| **算法效率** | IODCC以低复杂度逼近最优解，无需强化学习的大量训练开销。 |
| **稳定性保障** | 理论证明满足长期计算容量约束，确保系统稳定运行。 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **真实LLM查询轨迹**：来自阿里巴巴Bailian平台的真实用户请求日志（引用自[3] `arXiv:2505.09999`）。
- 包含多个客户端在不同时间段的请求模式，反映现实世界中的**高波动性与多样性**。

---

### **实验设置**
- **系统架构**：异构边缘-云系统，包含 `N` 台边缘服务器与 `U` 台云服务器。
- **任务类型**：3类任务（轻、中、重），对应不同prefill/decode计算需求：
  - 小模型：prefill=2, decode=1 单位
  - 大模型：prefill=8, decode=4 单位
- **资源异构性**：
  - 边缘服务器算力 `f_e ∈ [2.5, 5]`
  - 云服务器算力 `f_c ∈ [5, 7.5]`
- **通信与精度**：
  - 边缘：低延迟、低精度 `[0.1, 0.5]`
  - 云：高延迟、高精度 `[0.6, 1.0]`
- **时间范围**：`T = 100` 个时隙

---

### **评估指标**
- **主指标**：**Lyapunov Reward**，综合反映QoE（延迟、精度加权）与虚拟队列积压（代表约束违反风险）。
- **辅助指标**：
  - Token预测误差（L1 Loss）
  - 可训练参数量（参数效率）
  - 不同配置下的性能对比（消融实验）

---

### **基线方法对比**
| 基线 | 描述 |
|------|------|
| **Greedy_Accuracy** | 优先选择精度最高的设备 |
| **Greedy_Compute** | 优先选择算力最强的设备 |
| **Greedy_Delay** | 优先选择延迟最低的设备 |
| **TransformerPPO** | 基于Transformer + PPO的RL方法 + Lyapunov约束 |
| **DiffusionRL** | 基于扩散模型的强化学习 + Lyapunov约束 |
| **LoRA / LSTM / Transformers / Qwen2.5-7B** | 用于token预测模块的对比模型 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) LOO vs. 各基线（Table I & II）**
| 配置 | Argus (LOO) | Best Baseline (Baseline5) |
|------|-------------|---------------------------|
| `N=4, U=15` | **36,602** | 32,077 (DiffusionRL) |
| `N=4, U=20` | **30,757** | 25,511 (DiffusionRL) |
| `N=15, U=6` | **21,080** | 14,823 (DiffusionRL) |
| `N=20, U=6` | **28,961** | 17,939 (DiffusionRL) |

> ✅ **结论**：Argus在所有配置下均显著优于各类贪婪策略与先进RL方法。

---

#### **(2) Token预测精度（Fig. 4a）**
| 方法 | L1 Loss |
|------|--------|
| **LAS (Ours)** | **91.85** |
| LoRA | 92.07 (+0.2%) |
| LSTM | 107.79 (+17.3%) |
| Transformers | 106.69 (+16.15%) |
| Qwen2.5-7B | 176.93 (+92.6%) |

> ✅ LAS在更低参数量下实现更高预测精度。

---

#### **(3) 参数效率（Fig. 4b）**
| 方法 | 可训练参数 |
|------|----------|
| LoRA | 8.75M |
| **LAS** | **0.09M** |

> ✅ LAS仅需 **0.09M** 可训练参数，比LoRA少 **99%**，适合边缘部署。

---

#### **(4) 消融实验：Token预测器的影响（Table III）**
| 配置 | 有预测器 | 无预测器 | 提升幅度 |
|------|---------|---------|---------|
| `N=4, U=6` | 10,808 | 5,409 | **+99.8%** |
| `N=4, U=8` | 14,324 | 11,519 | **+24.3%** |
| `N=4, U=10` | 18,339 | 14,457 | **+26.9%** |

> ✅ 证明**准确的token长度预测是性能提升的关键驱动因素**。

---

## **4. 关键结论和发现**

### **主要发现**
1. **token长度可预测且至关重要**：通过LAS模块可较准确预测输出长度，该信息对优化卸载决策具有决定性作用。
2. **Argus实现近优性能**：理论分析表明其长期QoE性能与最优值差距不超过常数项 $ B/V $，可通过调节控制参数 $ V $ 逼近最优。
3. **系统稳定性得到保证**：基于Lyapunov的虚拟队列机制确保长期计算资源约束被满足，系统不会过载。
4. **优于RL方法**：尽管RL（如TransformerPPO、DiffusionRL）具备强表达能力，但Argus在无需训练的情况下达到更优性能，且更稳定。

---

### **方法的局限性**
- **依赖预训练模型**：LAS模块依赖ModernBERT等PLM，可能增加部署成本。
- **未考虑多跳网络**：实验设定为直接连接边缘或云，未涉及复杂网络拓扑。
- **静态设备能力假设**：假设服务器算力恒定，未建模设备老化或动态降频。
- **仅考虑单任务卸载**：未处理复杂DAG型语言模型程序（如SGLang支持的结构化生成）。

---

### **未来工作方向**
1. 扩展至**多模态LLM**与**RAG场景**，融合检索缓存优化（类似CacheBlend）。
2. 结合**KV Cache压缩技术**（如CacheGen）进一步降低传输开销。
3. 探索**在线学习机制**，使LAS能持续适应用户行为漂移。
4. 支持**动态模型切换**（如Small-to-Large Cascade Inference）以更好平衡延迟与精度。

---

> **总结**：Argus首次将LLM的**自回归token生成特性**纳入分布式推理调度框架，提出**LAS + LOO + IODCC**三位一体方案，在真实轨迹验证下展现出卓越性能与稳定性，为边缘智能时代的LLM服务提供了重要范式。

</details>

---

### 2. [MatKV: Trading Compute for Flash Storage in LLM Inference](https://arxiv.org/abs/2512.22195)

**Authors**: Kun-Woo Shin (Seoul National University, Korea), Jay H. Park (Samsung Electronics, Korea), Moonwook Oh (Samsung Electronics, Korea), Yohan Jo (Seoul National University, Korea), Jaeyoung Do (Seoul National University, Korea), Sang-Won Lee (Seoul National University, Korea)  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.22195v1  

#### Abstract
We observe two major trends in LLM-based generative AI: (1) inference is becoming the dominant factor in terms of cost and power consumption, surpassing training, and (2) retrieval augmented generation (RAG) is becoming prevalent. When processing long inputs in RAG, the prefill phase of computing th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MatKV: Trading Compute for Flash Storage in LLM Inference — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在基于检索增强生成（**RAG**）的大语言模型（**LLM**）推理中，输入文本通常很长（如上千个token），导致 **prefill 阶段**（即计算所有输入token的Key-Value向量）成为性能瓶颈。该阶段高度依赖GPU进行密集计算，造成高延迟、高能耗和高昂成本。

此外，许多RAG对象（如文档块）被反复检索，但每次仍需重新计算其KV缓存，造成大量冗余计算。

### 提出的新方法：MatKV
作者提出 **MatKV**，一种通过将计算任务“交易”给存储来优化LLM推理效率的新范式：

- **预计算并物化（materialize）KV缓存**：当文档插入向量数据库时，一次性使用GPU计算其KV向量，并将其持久化存储在廉价但高速的**Flash SSD**上。
- **推理时复用**：在后续RAG查询中，直接从SSD加载已物化的KV缓存到GPU内存，跳过昂贵的prefill计算过程。
- 实现了 **“以存储换计算”**（Trading Compute for Storage）的设计理念。

### 相比现有方法的优势
| 对比维度 | 现有方法（如KV Caching） | MatKV |
|--------|--------------------------|-------|
| 存储介质 | 主要依赖DRAM缓存 | 使用低成本、大容量的Flash SSD |
| 可扩展性 | 受限于有限且昂贵的DRAM | 支持TB级KV缓存，适合大规模部署 |
| 成本效益 | 高频访问才划算 | 提出“十天规则”，量化经济性阈值 |
| 性能潜力 | 缓存命中提升效率 | 可实现**解码与KV加载重叠**，隐藏I/O延迟 |
| 硬件适配性 | 依赖高端GPU | 允许低端GPU甚至CPU执行高效推理 |

> ✅ **核心创新点**：
> - 首次系统性论证了使用**外部Flash存储**替代GPU计算KV缓存的可行性、高效性和经济性；
> - 提出 **decoupling prefill and decode** 架构，支持KV加载与解码并发执行；
> - 推导出 **“十天规则”**（Ten-Day Rule），为是否物化提供决策依据；
> - 展示了MatKV使低功耗/低端硬件也能胜任长上下文RAG任务的可能性。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 用途 |
|------|------|
| **TurboRAG** | 测量系统延迟与功耗，平均query约17.67 tokens，document约767.73 tokens |
| **LongBench**（子集）：<br>- 2WikiMultihopQA<br>- TriviaQA<br>- HotpotQA | 评估问答准确率（F1 Score），涵盖多跳推理与少样本场景 |

### 实验设置
- **模型**：
  - LLaMA 3.1 70B（4-bit量化）、LLaMA 3.1 8B、LLaMA 3.2 3B
- **硬件平台**：
  - **H100服务器**：NVIDIA H100 GPU（80GB HBM），Intel Xeon Gold CPU，2TB DRAM
  - **RTX 4090平台**：单卡24GB显存，用于验证低端GPU表现
- **SSD配置**：
  - H100端：4×Samsung 9100 Pro SSD（RAID-0，读带宽~58.8GB/s）
  - RTX 4090端：1×Samsung PM9A3 SSD（6.5GB/s）
- **Chunk大小**：默认1,024 tokens

### 评估指标
| 指标 | 描述 |
|-----|------|
| **Latency** | 分解为Load Time、Prefill Time、Decode Time；关注端到端响应时间 |
| **Power Consumption** | 系统总功耗（IPMI测量）与GPU单独功耗（nvidia-smi） |
| **Energy Usage** | 功耗对时间积分（单位：kJ） |
| **Throughput** | 批处理下的请求吞吐量 |
| **Accuracy** | 使用F1 Score衡量生成答案质量 |

### 基线方法对比
- **Vanilla**：标准RAG流程，每次均在GPU上完整计算KV（Full KV Recomputation）
- **CacheBlend** [12]：部分复用缓存并融合注意力机制，在准确率上有轻微优势
- （隐含对比）传统KV Caching方案（如PromptCache、RAGCache）——仅基于DRAM

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 指标 | MatKV vs. Vanilla（H100 + RAID SSD） | 备注 |
|------|------------------------------------|------|
| **Prefill时间** | ↓ 超过50% | 图5显示MatKV prefill < 0.5s，Vanilla > 1s |
| **总体推理延迟** | ↓ ~70%（单请求）<br>↑ 达 **2×加速**（批处理+重叠） | 图7显示重叠后达2倍速度提升 |
| **系统总能耗** | ↓ **50%以上** | 表IV：Vanilla 566 kJ → MatKV 289 kJ |
| **GPU能耗** | ↓ **~53%** | 表V：Vanilla 185 kJ → MatKV 95 kJ |
| **加载时间（1× vs 4× SSD）** | 单盘：93ms → 四盘RAID：27ms | 表III，体现SSD带宽重要性 |
| **与CacheBlend比较** | 加载快37%，TFTT快41% | MatKV更轻量，无融合开销 |

### 与基线方法的对比结果
- 在 **LLaMA 3.1 70B + 2×1024-token输入 + 20-token输出** 场景下：
  - MatKV 将 **prefill时间减少至Vanilla的一半以下**
  - 结合**批处理与重叠执行**，实现最高 **2倍的整体加速**
  - **系统级能量消耗降低一半**

- 在不同batch size下（图6）：
  - 当batch size ≥ 8时，decode不再是瓶颈，prefill主导延迟
  - 此时MatKV优势显著放大，可达2倍加速

- 在不同输出长度下（图8b）：
  - 输出越长，decode占比越高，MatKV相对增益下降
  - 但仍保持绝对性能领先（始终快于Vanilla）

### 模型规模影响（图9）
- 随着模型增大（3B → 70B）：
  - KV计算成本增长远超KV缓存体积增长
  - MatKV带来的**归一化执行时间收益持续上升**
  - 说明MatKV在更大模型上更具优势

### 低阶GPU上的表现（图10）
- 使用 **RTX 4090（$1.6K） vs H100（$50K）**
- MatKV on RTX 4090 比 Full KV on H100 仅慢 **1.5×**
- 而 Vanilla on RTX 4090 比 H100 慢 **3×**
- 表明：**MatKV极大缩小了高低端GPU之间的性能差距**，推动低成本部署

### 准确率评估（表VI）
| Dataset | Vanilla (F1) | MatKV (F1) | 变化 |
|--------|-------------|-----------|------|
| 2WikiMQA | 0.278 | 0.241 | ↓ ~13% |
| TriviaQA | 0.786 | 0.812 | ↑ ~3.3% |
| HotpotQA | 0.353 | 0.302 | ↓ ~14% |

> ⚠️ 总体来看，MatKV在多数任务中保持可接受精度，个别任务略有下降，但未出现严重退化。作者解释：独立加载各chunk避免了“中间丢失”问题（Lost in the Middle），反而可能提升某些任务表现。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Flash SSD可以高效替代GPU进行KV计算**  
   在RAG场景中，SSD读取KV的速度和能效远优于GPU重新计算，尤其适用于长输入、高频访问场景。

2. ✅ **MatKV显著降低延迟与能耗**  
   实验表明，相比Vanilla方法，MatKV可将**推理时间减半、系统能耗降低50%以上**。

3. ✅ **支持解码与KV加载重叠（Overlap）**  
   利用MatKV解耦prefill与decode的特点，可在当前batch解码的同时预加载下一batch的KV，有效隐藏I/O延迟。

4. ✅ **启用低成本硬件推理**  
   一旦KV被物化并快速加载，**低端GPU甚至CPU也可承担高质量RAG推理任务**，大幅降低部署门槛。

5. ✅ **经济性明确：“十天规则”成立**  
   推导出Break-even公式，得出结论：若一个文档每**10天至少被访问一次**，则物化其KV比每次都计算更便宜。

6. ✅ **技术趋势利好MatKV长期发展**  
   SSD带宽增速（~30x/decade）远超GPU FLOPS增速（~10x/decade），未来“存储换计算”的性价比将持续扩大。

### 方法的局限性
- ❗ **冷启动问题**：首次插入的新文档需经历完整prefill+写入流程，首访延迟较高
- ❗ **存储开销大**：全量物化（Materialize-All）策略可能导致TB/PB级存储需求
- ❗ **准确性波动**：缺少跨文档self-attention，可能影响复杂多跳推理任务的表现
- ❗ **当前假设简化**：采用“立即物化所有文档”策略，未引入智能缓存淘汰或预测机制

### 未来工作方向
1. **选择性物化策略**：结合访问频率、热度预测等动态决定哪些KV值得缓存
2. **KV压缩技术集成**：应用MiniCache、xKV等压缩算法进一步降低存储成本
3. **分层存储架构**：构建DRAM-SSD-Archive三级KV缓存体系，平衡性能与成本
4. **扩展至其他任务**：探索在Agent AI、Text-to-SQL、In-Context Learning等重复使用长文本的场景中的应用
5. **支持增量更新**：当文档变更时如何高效更新其KV缓存

---

> 📌 **总结一句话**：  
> **MatKV开创性地将LLM推理中的KV计算卸载到高性能Flash存储，实现了“以存储换计算”的新范式，在不牺牲太多准确性的前提下，显著提升了RAG系统的速度、能效和经济性，为大规模生成式AI的可持续部署提供了新路径。**

</details>

---

### 3. [DiRL: An Efficient Post-Training Framework for Diffusion Language Models](https://arxiv.org/abs/2512.22234)

**Authors**: Ying Zhu, Jiaxin Wan, Xiaoran Liu, Siyanag He, Qiqi Wang, Xu Guo, Tianyi Liang, Zengfeng Huang, Ziwei He, Xipeng Qiu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.22234v1  

#### Abstract
Diffusion Language Models (dLLMs) have emerged as promising alternatives to Auto-Regressive (AR) models. While recent efforts have validated their pre-training potential and accelerated inference speeds, the post-training landscape for dLLMs remains underdeveloped. Existing methods suffer from compu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DiRL: An Efficient Post-Training Framework for Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前 **Diffusion Language Models (dLLMs)** 在预训练和推理效率方面已取得进展，但在**后训练（post-training）阶段**，尤其是**强化学习（Reinforcement Learning, RL）** 方面仍面临严重挑战：
- **计算低效**：传统 dLLMs 缺乏 KV Cache 支持，导致推理和训练开销巨大。
- **目标不一致（Objective Mismatch）**：训练时使用的随机掩码策略与实际推理路径不一致，造成 logits 偏差。
- **缺乏高效的训练-推理协同设计**：现有方法多采用离线模型加载，IO 开销大，难以支持在线更新和高效 rollout。

这些问题限制了 dLLMs 在复杂任务（如数学推理）上的表现。

---

### 🚀 提出的新方法与创新思路

作者提出 **DiRL** —— 一个高效的 dLLM 后训练框架，并在此基础上实现首个无偏的 **Group Relative Policy Optimization (GRPO)** 算法变体：**DiPO**。

#### 主要创新点包括：

| 创新模块 | 技术细节 |
|--------|---------|
| **DiRL 框架** | 结合 **FlexAttention 加速的 blockwise 训练** 与 **LMDeploy 优化的推理引擎**，构建端到端高效的 SFT → RL 两阶段后训练流程。 |
| **DiPO 算法** | 首个在 dLLMs 上实现的**无偏 GRPO 方法**，利用 blockwise dLLM 的结构特性进行精确、高效的 logits 计算，避免采样带来的高方差和偏差。 |
| **在线模型更新机制** | 通过 LMDeploy 的 in-place 参数更新 API，消除每步训练中的磁盘 I/O（无需重复 load/save 模型），显著提升训练吞吐。 |
| **训练-推理一致性设计** | 统一使用 blockwise 推理模式进行 rollout 和训练，确保训练目标与实际推理路径对齐。 |

---

### 🔍 相比现有方法的优势

| 对比维度 | DiRL vs. 现有方法（如 TraceRL） |
|--------|-------------------------------|
| **训练效率** | 引入 FlexAttention 处理复杂 attention mask，训练延迟降低近 **6×**；整体每步训练时间减少 **2.5×**。 |
| **算法精度** | 实现**无偏 logits 计算**，首次将 GRPO 成功应用于 dLLMs，优于基于近似采样的方法（如 diffu-GRPO）。 |
| **系统集成度** | 紧密耦合训练（LLaMA-Factory）与推理（LMDeploy），支持**实时 rollout 和在线策略更新**，工程实用性更强。 |
| **性能表现** | 在多个数学基准上超越同规模及更大规模的 AR 模型（如 Qwen2.5-32B-Instruct）。 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 阶段 | 数据集 | 描述 |
|------|-------|------|
| **SFT 阶段** | `OpenR1-Math`（蒸馏自 GLM-4.6） | 高质量数学推理数据，用于监督微调。 |
| **RL 阶段** | `Big-Math`（经 math-verify 验证） | 大规模、高质量数学数据集，专为 RL 设计，确保奖励信号可靠。 |

> 所有样本最大长度限制为 **8k tokens**，是目前 dLLMs 中报道的最长上下文。

---

### ⚙️ 实验设置

| 项目 | 设置详情 |
|------|----------|
| **基础模型** | 基于 `SDAR-8B-Chat` 进行后训练 |
| **模型名称** | `DiRL-8B-Instruct` |
| **训练流程** | 两阶段：SFT → RL（DiPO） |
| **SFT 设置** |  
- 工具：LLaMA-Factory  
- 硬件：8×H200 GPUs  
- Batch size：512  
- 学习率：1e-5（cosine 衰减）  
- 步数：100 步  

| **RL 设置** |  
- 工具：DiRL + Accelerate  
- 硬件：128×H200 GPUs  
- Rollout 数量：每个问题生成 32 条轨迹  
- Batch size：128  
- 学习率：1e-6  
- 步数：40 步  
- 参考策略：SFT 模型（fixed）  

---

### 📊 评估指标

| 指标 | 内容 |
|------|------|
| **Accuracy** | 在多个数学任务上的准确率 |
| **Average Score** | 多任务平均得分 |
| **Decoding Tokens / Step** | 平均每步解码 token 数，反映推理效率 |
| **Output Length** | 输出长度，衡量推理深度 |
| **Dynamic Decoding** | 使用阈值（如 0.9）提前解码高置信度 token，测试不同策略下的鲁棒性 |

---

### 🆚 基线方法对比

| 基线模型 | 类型 | 说明 |
|--------|------|------|
| `Qwen2.5-7B/32B-Instruct` | AR 模型 | 当前主流自回归模型，作为外部参考 |
| `SDAR-8B-Chat` | blockwise dLLM | 原始 base 模型 |
| `TraDo-8B-Instruct` | dLLM + RL（TraceRL） | 当前最先进的 dLLM 强化学习方法 |
| `+Dynamic` | 动态解码版本 | 使用 top-1 概率 > 0.9 的 token 提前输出 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）

| 模型 | MATH500 | GSM8k | AIME2024 | AIME2025 | OlympiadBench | **Avg.** |
|------|--------|-------|---------|---------|--------------|----------|
| Qwen2.5-7B-Instruct | 73.8 | 89.8 | 9.0 | 5.6 | 36.6 | 42.9 |
| Qwen2.5-32B-Instruct | 81.1 | 94.0 | 12.9 | 11.9 | 45.7 | 49.1 |
| SDAR-8B-Chat | 71.5 | 89.5 | 5.6 | 8.5 | 35.6 | 42.2 |
| TraDo-8B-Instruct | 76.7 | 90.4 | 11.5 | 13.5 | 40.2 | 46.5 |
| **DiRL-8B-Instruct (Ours)** | **85.1** | **93.1** | **21.5** | **22.9** | **47.3** | **54.0** |

> ✅ **DiRL-8B-Instruct 在所有任务上均达到 SOTA 性能，平均分 54.0，超越更大的 Qwen2.5-32B-Instruct（49.1）**

---

### 🔁 效率对比（Figure 6 & 7）

| 指标 | DiRL vs. TraceRL |
|------|------------------|
| **Rollout 时间** | 提升约 1.08×（因共用 JetEngine） |
| **Training 时间** | **降低 5.94×**（得益于 FlexAttention） |
| **总耗时/step** | **减少 2.55×** |
| **8B 模型训练延迟** | 低于 TraceRL 的 1.7B 模型，显示极强扩展性 |

---

### 🔍 消融实验结果（Figure 8）

进行了关于 **动态解码阈值 $ \tau $** 的消融研究（范围 0.5 ~ 0.99）：

| 发现 | 说明 |
|------|------|
| **DiRL-8B-Instruct 表现最稳定且持续领先** | 即使在不同 $ \tau $ 下也显著优于基线，证明其优势来自高质量推理路径而非超参敏感性 |
| **性能随 $ \tau $ 增加而上升** | 更保守的解码策略带来更优结果，表明模型具备良好置信度校准能力 |
| **最优平衡点为 $ \tau = 0.9 $** | 被选为主实验设置，兼顾效率与准确性 |

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **DiRL 实现了 dLLMs 上高效且一致的后训练闭环**：
   - 通过 **blockwise 结构 + FlexAttention + LMDeploy** 实现训练-推理一致性与高吞吐。
   
2. **DiPO 是首个无偏 GRPO 实现**：
   - 克服了传统 dLLM 中 logits 不可精确计算的问题，为 dLLM 的 RL 对齐提供了新范式。

3. **DiRL-8B-Instruct 成为当前最强数学推理 dLLM**：
   - 不仅超越同类 dLLMs，还**超过更大规模的 AR 模型（Qwen2.5-32B）**，验证了扩散架构在复杂任务上的潜力。

4. **长输出长度表明更强的推理能力**：
   - DiRL 生成的平均输出更长，说明其能完成更复杂的数学推导过程。

---

### ⚠️ 方法的局限性

1. **上下文长度仍受限**：
   - 当前最大支持 8k tokens，虽已是 dLLM 最长记录，但仍短于先进 AR 模型（如支持 32k+）。

2. **尚未探索 Test-Time Scaling 或 Chain-of-Thought 扩展技术**：
   - 如思维树（ToT）、自洽性（Self-Consistency）等未被引入，可能进一步提升性能。

3. **应用集中在数学领域**：
   - 尚未在代码生成、Agent 任务等其他复杂场景中广泛验证。

---

### 🔮 未来工作方向

1. **扩展至更大规模模型**（如 65B+），追求更强性能。
2. **支持更长上下文推理**（>8k），结合 Long Context Alignment 技术。
3. **引入动态 packing 等训练加速策略**，进一步提升训练效率。
4. **拓展至更多下游任务**：如代码生成、Agent 决策、多模态任务。
5. **开源完整工具链**：提供开放的 dLLM 后训练 Toolkit，推动社区发展。

---

## 总结

> **DiRL** 是首个面向 dLLMs 的高效、端到端后训练框架，通过 **DiPO + FlexAttention + LMDeploy 在线更新** 的协同设计，在数学推理任务上实现了 **SOTA 性能与训练效率双重突破**。它不仅解决了 dLLM 后训练中的核心瓶颈，也为未来扩散语言模型的对齐与部署提供了坚实的技术基础和开源实践路径。

🔗 **代码**：[https://github.com/OpenMOSS/DiRL](https://github.com/OpenMOSS/DiRL)  
🌐 **模型**：[https://huggingface.co/OpenMOSS-Team/DiRL-8B-Instruct](https://huggingface.co/OpenMOSS-Team/DiRL-8B-Instruct)

</details>

---

### 4. [Modality Inflation: Energy Characterization and Optimization Opportunities for MLLM Inference](https://arxiv.org/abs/2512.22695)

**Authors**: Mona Moghadampanah, Adib Rezaei Shahmirzadi, Farhana Amin, Dimitrios S. Nikolopoulos  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.22695v1  

#### Abstract
Multimodal large language models (MLLMs) are built on text-only LLMs by incorporating additional modalities, enabling multimodal understanding and a broader range of applications. However, these additions introduce a previously unexplored energy trade-off across modalities that remains poorly unders...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Modality Inflation: Energy Characterization and Optimization Opportunities for MLLM Inference*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文聚焦于**多模态大语言模型（MLLM）推理过程中的能效问题**，特别是由“**模态膨胀（Modality Inflation）**”带来的额外能耗。传统研究多关注文本-only LLM 的能效优化，而忽视了图像等视觉输入引入的编码阶段和大量视觉 token 所导致的系统级能量开销。

具体而言，该论文揭示并量化了以下问题：
- 视觉编码器（vision encoder）和扩展的 token 序列如何显著增加推理能耗；
- 不同 MLLM 架构在能效上的巨大差异；
- 多模态执行过程中 GPU 利用率低下、功耗分布不均等问题。

### 🔧 提出的新方法与新思路
1. **首次提出“模态膨胀”概念**，将其定义为：由于视觉输入引入额外的编码阶段和大量视觉 token，导致下游 prefill 阶段计算量剧增的现象。
2. **构建了细粒度的三阶段能效分析框架**：
   - **Vision Encoding**（视觉编码）
   - **Prefill**（上下文填充）
   - **Decoding**（自回归解码）
   并对每个阶段进行独立的能量与延迟测量。
3. **提出基于阶段的动态电压频率调节（Stage-wise DVFS）作为优化策略**，根据不同阶段的工作负载特性调整 GPU 频率，在控制性能损失的同时实现节能。

### 📈 相比现有方法的优势
| 维度 | 以往工作局限 | 本论文优势 |
|------|---------------|------------|
| 能效分析粒度 | 黑箱式整体分析 | **阶段级分解分析**，识别瓶颈来源 |
| 输入复杂性影响 | 忽视分辨率/图像数量变化 | 明确建模输入复杂性对能耗的影响 |
| 优化策略 | 通用性策略（如批处理、量化） | **架构感知 + 阶段感知的 DVFS 控制** |
| 实验设计 | 缺乏跨模型比较 | 在统一硬件平台下对比四种代表性 MLLM |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与输入配置
- **真实请求分布参考**：使用生产级追踪数据集 **ServeGen [42]** 分析实际查询中图像数量分布（图2a），发现多数请求含1–2张图，少数极端情况可达49张。
- **图像分辨率分析**：考察四个主流 benchmark 数据集（**VQAv2, VizWiz, ShareGPT4V, ChartQA**）的图像短边分辨率分布（图2b），显示从低清到超高清均有覆盖。
- **实验输入设置**：
  - 固定文本 prompt；
  - 图像输入分为两种配置：
    1. **iso-resolution 设置**：固定为 512×512；
    2. **可变输入**：改变图像分辨率（最高达 2048×2048）和图像数量（最多6张）以测试敏感性。

### ⚙️ 实验设置
- **硬件平台**：单块 **NVIDIA A100 80GB GPU**
- **软件栈**：PyTorch + Hugging Face Transformers
- **功率监控工具**：通过 **NVML** 以 5ms 间隔采样 GPU 功耗
- **重复性保障**：多次运行取平均值，并提供置信区间

### 📊 评估指标
| 指标类别 | 具体指标 |
|--------|---------|
| 能效 | Energy per request (**J/request**) |
| 性能 | End-to-end latency (**s/request**), Throughput (**req/s**) |
| 系统行为 | GPU power trace, Utilization phases |
| 可扩展性 | 能量随分辨率/图像数的增长趋势 |

### 🔁 基线方法对比
- **text-only baseline**：选择与各 MLLM 共享相同 LLM backbone 的纯文本模型作为对照；
- **iso-token 对齐实验**：确保 MLLM 与 baseline 的总输入 token 数相等（包括 text + visual tokens），输出固定为1个token，从而隔离序列长度效应，仅衡量“多模态处理”的增量成本。

---

## 3. 主要实验结果和性能指标

### 🔋 RQ1: 多模态推理的增量能耗是多少？
- 在 iso-token 条件下，相比 text-only baseline，MLLM 推理能耗增加 **17% ~ 94%**（图3a）：
  - **LLaVA-OneVision**: +17%
  - **LLaVA-1.5**: +25%
  - **InternVL3**: +18%
  - **Qwen2.5-VL**: **+94%**（最高）
- 同时观察到**能耗与延迟增长不成比例**：
  - Qwen2.5-VL 能耗 +94%，但延迟 +179%，说明存在串行瓶颈。

> ✅ **发现1**：token 数量本身不能解释全部能耗差异，**架构设计是决定性因素**。

---

### ⚖️ RQ2: 能量分布在各个阶段有何不同？

#### 阶段级能耗分解（图4a）：
| 模型 | 编码能耗 | Prefill 能耗 | 主导阶段 |
|------|----------|-------------|----------|
| **Qwen2.5-VL** | **20.81 J**（极高） | 6.30 J | **编码主导**（compute-heavy encoder） |
| **LLaVA-OneVision** | 9.52 J（较低） | **95.78 J**（极高） | **prefill 主导**（tile-based token expansion） |
| **InternVL3** | 8.12 J | 7.54 J | 较平衡 |
| **LLaVA-1.5** | 14.36 J | 49.58 J | prefill 占比较高 |

- **LLaVA-OneVision** 生成高达 **3,715 个视觉 token**，导致 prefill 阶段能耗激增（约是 InternVL3 的12倍）；
- **Qwen2.5-VL** 的 encoder 能耗是 LLaVA-1.5 的 **6倍以上**。

> ✅ **发现2**：不同 MLLM 的能效瓶颈位置完全不同——有的卡在 encoder，有的卡在 prefill。

---

### 🔌 RQ3: GPU 功耗曲线有何特征？是否存在优化机会？

#### 功率轨迹分析（图5）：
- **text-only baseline**：快速进入高功耗状态（~400W），接近 GPU 功率上限，利用率高；
- **MLLM 推理**：出现明显的“**中等功耗相位**”（mid-power phase, 100–250W），对应 vision encoding 阶段；
  - Qwen2.5-VL 表现为持续 ~200W 的“阶梯状”功率；
  - LLaVA-OneVision 出现高频波动，反映 tiled processing 的突发性。

> ✅ **发现3**：多模态执行期间 GPU 长时间处于非饱和状态，**默认的“race-to-idle”频率策略效率低下**。

---

### 📈 RQ4: 输入复杂性如何影响能效？

#### 图像分辨率影响（图7）：
- **LLaVA-1.5**（CLIP-based）：采用固定 patch 分割，token 数几乎不变 → 能耗基本平坦；
- **InternVL3 / LLaVA-OneVision**（any-resolution tiling）：token 数随分辨率离散上升 → 能耗非线性增长；
- **Qwen2.5-VL**：在 >1024×1024 后 token 数急剧上升 → 能耗和延迟陡增。

#### 图像数量影响（图6）：
- 增加图像数量线性提升能耗和延迟，但斜率因模型而异：
  - **边际成本 per image**：约 **15–35 J/image**
  - 少量 high-image 请求即可显著拖慢系统响应

> ✅ **发现4**：输入复杂性（分辨率、图像数）对能耗影响高度依赖于 encoder 设计，需**输入感知的调度机制**。

---

## 4. 关键结论和发现

### ✅ 主要发现总结

| 发现编号 | 内容摘要 |
|--------|--------|
| **Observation 1** | MLLM 能耗差异极大（+17%~+94%），必须采用**架构定制化**的能效管理策略 |
| **Observation 2** | 能效瓶颈可能出现在 **encoding 或 prefill 阶段**，应实施**阶段级优化**（如 stage-wise DVFS） |
| **Observation 3** | 多模态执行引入**中等功耗阶段**，GPU 利用率低，通用频率策略失效 |
| **Observation 4** | 不同 MLLM 对输入复杂性的敏感度差异巨大，需**输入感知的资源分配机制** |

---

### ⚠️ 方法的局限性
1. **仅限于静态 DVFS 实验**：未实现动态实时调频控制器；
2. **局限于单 GPU 场景**：未考虑分布式或多设备 disaggregation 架构下的能效表现；
3. **模型规模有限**：所有测试模型集中在 7B–8B 参数范围，未涵盖更大或更小模型；
4. **仅评估 vision-language 模型**：未涉及音频、视频等其他模态。

---

### 🔮 未来工作方向（原文 Section 6）
1. **扩展至不同规模模型**：研究参数量对 modality inflation 的交互影响；
2. **开发动态 DVFS 控制器**：结合输入预测与阶段检测，实现 SLO-aware 的实时频率调节；
3. **探索 disaggregated 架构下的能效优化**：将 vision encoder 与 LLM 分离部署后的能耗特性；
4. **拓展至更多模态**：支持 audio、video、temporal inputs 的统一能效建模框架。

---

## 结语

本文是首个对 MLLM 推理进行**细粒度能效剖析**的研究，提出了“**模态膨胀**”这一关键概念，并揭示了其在 energy、latency 和 GPU utilization 上的深远影响。通过 stage-level 分析与 DVFS 实验，论文不仅提供了深刻的系统洞察，也为构建可持续、高效的 MLLM serving 平台指明了切实可行的技术路径。

</details>

---

### 5. [LLMBoost: Make Large Language Models Stronger with Boosting](https://arxiv.org/abs/2512.22309)

**Authors**: Zehao Chen, Tianxiang Ai, Yifei Li, Gongxun Li, Yuyang Wei, Wang Zhou, Guanghui Li, Bin Yu, Zhijun Chen, Hailong Sun, Fuzhen Zhuang, Jianxin Li, Deqing Wang, Yikun Ban  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.22309v1  

#### Abstract
Ensemble learning of LLMs has emerged as a promising alternative to enhance performance, but existing approaches typically treat models as black boxes, combining the inputs or final outputs while overlooking the rich internal representations and interactions across models.In this work, we introduce ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LLMBoost: Make Large Language Models Stronger with Boosting 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **LLM Ensemble** 方法通常将模型视为黑箱，仅在输入或最终输出层面进行融合（如多数投票、加权聚合），而忽略了模型内部丰富的隐藏表示（hidden representations）和跨模型交互。这种粗粒度的融合方式限制了模型间互补知识的充分挖掘。

### 提出的新方法和新思路
本文提出了 **LLMBooST**，一种新颖的集成微调框架，其核心是借鉴 **Boosting** 范式，通过显式利用 LLM 的中间状态来打破“黑箱”壁垒。其三大关键创新如下：

1.  **跨模型注意力机制 (Cross-Model Attention)**：
    *   允许后续模型（successor models）直接访问并融合前序模型（predecessors）的隐藏状态。
    *   这种分层的、细粒度的交互机制使得后一个模型能够基于前一个模型的“思考过程”进行错误纠正和知识传递。

2.  **链式训练范式 (Chain Training Paradigm)**：
    *   采用顺序微调的方式，每个后续模型都基于其前驱模型的训练日志进行微调。
    *   引入 **错误抑制目标 (Error-Suppression Objective)**，该目标函数会抑制前驱模型预测的最大（错误）logit，同时增强正确答案的logit，从而实现精准纠错，且计算开销极小。

3.  **近似并行推理范式 (Near-Parallel Inference Paradigm)**：
    *   为解决多模型串行推理导致的高延迟问题，提出了一种流水线式的推理策略。
    *   后续模型无需等待前驱模型完成整个序列生成，而是可以逐层接收其隐藏状态并立即开始计算，实现了接近单模型解码的效率。

### 相比现有方法的优势
*   **性能更强**：通过利用内部表示进行细粒度交互，显著提升了推理能力。
*   **效率更高**：近似并行推理大幅降低了传统串行集成的延迟。
*   **理论有保障**：论文从理论上证明，在合理假设下，随着集成模型数量的增加，整体性能会单调提升。

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖了多个领域，以验证方法的通用性：
*   **常识推理 (Commonsense Reasoning)**：PIQA, HellaSwag, WinoGrande, BoolQ, SIQA, OpenbookQA (OBQA)。
*   **算术推理 (Arithmetic Reasoning)**：AQuA, GSM8K, MAWPS, SVAMP。
*   **工业场景数据集**：**Chinatelecom Cloud Agent Dataset (CCAD)**，用于评估AI代理在真实云服务工具链调度中的表现。

### 实验设置和评估指标
*   **微调方法**：采用 **LoRA** 进行参数高效微调。
*   **评估指标**：主要使用 **准确率 (Accuracy)** 作为性能指标。
*   **效率指标**：报告端到端延迟（End-to-End Latency）、每token生成成本（Per-Token Latency）和峰值GPU内存占用。
*   **模型规模**：在不同参数量级的模型上进行了测试，包括 `Llama-3` 和 `Qwen-2.5` 系列的 3B, 7B, 8B 模型。

### 基线方法对比
与以下主流的集成和优化方法进行了比较：
*   **VOTE**：基于多数投票的集成方法。
*   **UNITE**：一种高效的LLM集成方法，关注各模型top-k token的并集。
*   **T-copilot**：一种通过辅助适配器（copilot adapter）来纠正主模型（pilot）错误的方法。

## 3. 主要实验结果和性能指标

### 关键性能数据
*   **平均性能提升**：相比其他集成基线，LLMBooST 在各项任务上的平均准确率提升了 **3.9%**，最高提升达到 **6.6%**。
*   **特定数据集表现**：
    *   在 **CCAD** 数据集上，相比单模型，准确率提升了 **3%**。
    *   在 **WinoGrande** 上，相比最强基线，准确率提升了 **6.6%**。
*   **效率提升**：相比传统的串行集成，LLMBooST 的推理延迟降低了 **47%**。

### 与基线方法的对比结果
*   **性能对比**：在所有测试的模型规模（3B, 7B, 8B）和任务上，LLMBooST 均显著优于 **VOTE**, **UNITE**, 和 **T-copilot** 等基线方法。
    *   例如，在 `Llama-3.1-8B` 的算术推理任务上，LLMBooST (2×8B) 的平均准确率达到 **71.5%**，而 VOTE 仅为 **69.8%**。
*   **效率对比**：LLMBooST 的端到端延迟（13.75秒）远低于 VOTE (22.61秒)、UNITE (22.66秒) 和 T-Copilot (17.21秒)，甚至优于单个14B大模型（17.40秒）。
*   **参数效率**：在相同参数预算下，LLMBooST 性能更优。例如，两个 `Qwen2.5-3B` 组成的 LLMBooST (6B) 在算术推理上的表现超过了单个 `Qwen2.5-7B` (7B) 模型。

### 消融实验结果
消融研究验证了各个组件的有效性：
*   **移除错误抑制目标 (wo E.S.O.)**：性能显著下降，证明了该目标函数对精准纠错至关重要。
*   **禁用跨模型注意力 (wo C.A.)**：性能下降，表明直接访问前驱模型的隐藏状态是提升性能的关键。
*   **超参数敏感性**：实验表明，`α` (平衡系数) 和 `β` (缩放因子) 的选择对性能有影响，存在最优值（如 `α=0.9`, `β=0.1`）。

## 4. 关键结论和发现

### 主要发现
1.  **内部交互至关重要**：打破LLM集成的“黑箱”假设，利用模型间的内部隐藏状态进行交互，是提升集成性能的有效途径。
2.  **Boosting范式有效**：将Boosting思想应用于LLM集成，通过链式训练和错误抑制，能够实现稳定且可累积的性能增益。
3.  **效率与性能兼得**：提出的近似并行推理范式成功解决了多模型集成的高延迟瓶颈，实现了高性能与高效率的统一。
4.  **理论与实践一致**：论文提供的理论分析（在误差校正有效的前提下，集成性能单调提升）得到了实验结果的支持。

### 方法的局限性
*   **跨模型兼容性限制**：当前方法要求集成的子模型来自同一系列（如均为Llama-3或Qwen-2.5），因为它们需要共享相同的分词器（tokenizer）和架构设计。不同系列模型（如Llama和Qwen）由于分词不一致，无法直接应用此方法。

### 未来工作方向
*   将框架扩展到 **多智能体 (multi-agent)** 场景，让不同智能体专精于推理、规划或工具使用等不同任务。
*   探索 **自适应路由机制 (adaptive routing mechanisms)**，根据任务上下文动态选择智能体或推理路径。
*   结合 **投机推理 (speculative reasoning)** 技术，进一步降低大规模部署时的延迟。

</details>

---

### 6. [TL: Automatic End-to-End Compiler of Tile-Based Languages for Spatial Dataflow Architectures](https://arxiv.org/abs/2512.22168)

**Authors**: Wei Li, Zhenyu Bai, Heru Wang, Pranav Dangi, Zhiqiang Zhang, Cheng Tan, Huiying Lan, Weng-Fai Wong, Tulika Mitra  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.22168v1  

#### Abstract
Spatial dataflow accelerators are a promising direction for next-generation computer systems because they can reduce the memory bottlenecks of traditional von Neumann machines such as CPUs and GPUs. They do so by organizing computation around explicit, compiler-managed data movement over the on-chip...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TL: Automatic End-to-End Compiler of Tile-Based Languages for Spatial Dataflow Architectures — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
空间数据流架构（**spatial dataflow architectures**）虽然在能效和吞吐量上潜力巨大，但由于其高度依赖于如何将计算任务映射到分布式核心、内存和片上网络（NoC），导致编程难度极高。目前大多数用户依赖厂商提供的手工优化库（如 `TTNN`），这些库仅支持少数固定算子，缺乏可移植性和灵活性。

因此，**主要挑战是：如何自动化地将高层 tile-based 程序（如 Triton 内核）编译为高性能的空间数据流执行方案，涵盖从 tile 分布、通信调度到资源分配的全过程**。

---

### 🚀 提出的新方法与创新点

论文提出了 **TL**（TileLoom），一个端到端的编译框架，用于将基于 tile 的语言（如 Triton）自动编译到空间数据流架构上。其核心创新包括：

#### （1）统一的硬件抽象模型（`df` dialect）
- 引入了一种新的 MLIR 扩展——`df` dialect，用于描述多级硬件结构：
  - **Scale-out 架构**：核心阵列拓扑（如 2D mesh、ring）、NoC 连接方式
  - **Memory 层次**：本地 scratchpad（L1）、DRAM 通道及其带宽
  - **Intra-core 计算单元**：矩阵单元（`mat`）、向量单元（`vec`）等性能参数
- 支持跨不同架构（如 2D mesh、1D ring）的可移植性建模。

#### （2）全自动的时空映射（Spatiotemporal Mapping）
- 将逻辑 tile 网格（logical tile grid）映射到物理核心阵列和时间维度。
- 支持灵活的 **tiling 策略**，决定哪些维度映射为空间并行（spatial dims），哪些作为时间批处理（temporal waves）。
- 显式控制 tile 实例在空间（core 分布）和时间（execution schedule）上的布局。

#### （3）联合的数据重用与通信规划
- 在给定映射下进行 **数据重用分析**：
  - 若访问不依赖空间索引 → 可通过 **broadcast** 实现空间复用
  - 若不依赖时间循环变量 → 可通过 **loop hoisting** 实现时间复用
- 自动生成多种合法的广播模式（如行/列广播、wavefront 传播），并结合性能模型选择最优方案。

#### （4）基于 MLIR 的分层编译流程
- 前端：接收 Triton 风格的 tile kernel 和 launch grid，生成标准化的 dataflow-agnostic MLIR
- 中间：通过 `dataflow planning` 阶段引入 spatiotemporal mapping 和 memory movement
- 后端：对接 Tenstorrent 的低级 API（TT-Metalium）生成 per-core 可执行代码

#### （5）两阶段候选筛选机制（Top-k Selection）
- 利用 **性能模型** 对所有候选 mapping 进行静态排序，选出 top-k
- 在真实硬件上对 top-k 候选进行 profiling，最终选取最优配置（top-1）
- 平衡了搜索开销与性能上限。

---

### 🔍 相比现有方法的优势

| 维度 | 现有方法（如 TTNN） | TL |
|------|---------------------|----|
| 映射策略 | 固定模板（TT-1D / TT-2D） | 自动探索大规模 mapping 空间 |
| 数据复用 | 手工设计，难以泛化 | 自动识别空间/时间复用机会 |
| 架构适配性 | 耦合特定硬件 | 通过 `df` dialect 支持多架构 |
| 新算子支持 | 依赖专家手动开发 | 支持任意 tile-based kernel 编译 |
| 性能鲁棒性 | 在非典型形状下表现差 | 动态适应输入尺寸与硬件拓扑 |

> TL 实现了“将手写优化内核变为编译器输出”的目标，显著降低开发门槛。

---

## 2. 核心实验方法和设置

### 💻 实验平台
- **硬件**：Tenstorrent Wormhole n300d 加速卡
  - 每 socket 64 个 Tensix cores @ 1GHz
  - 峰值算力：64 TFLOP/s per socket（FP16）
  - 片上 SRAM：96MB，GDDR6 DRAM：12GB @ 288 GB/s
- **主机**：双路 Intel Xeon Gold 6326 CPU + 512GB DRAM

### 🧪 评估的逻辑架构配置（模拟不同拓扑）
| 配置 | 描述 |
|------|------|
| `1×8 Ring` | 一维环形结构 |
| `4×8 Mesh` | 非对称矩形网格 |
| `8×8 Mesh` | 完整对称二维网格 |

> 用于研究拓扑对最优 dataflow 的影响。

---

### 📊 工作负载（Workloads）
1. **GEMM**（General Matrix Multiplication）
   - 形式：`C[M,N] = A[M,K] × B[K,N]`
   - 测试范围：M/N/K ∈ [256, 16384]
   - 包括规则与不规则形状（如固定 M=N=32768 变 K；固定 M=K=32768 变 N）

2. **FlashAttention**
   - 非因果注意力变体
   - 参数扫描：
     - 注意力头数：64 ~ 128
     - 序列长度：512 ~ 8192
     - 总 token 数固定为 8192（控制显存占用）

---

### 📈 评估指标
- **性能**：实测吞吐率（TFLOP/s）或加速比（speedup）
- **对比基线**：
  - **TTNN**：Tenstorrent 官方库，采用 TT-1D / TT-2D 模板自动选择
  - **TT-1D / TT-2D**：两种固定 dataflow 模板
- **消融实验**：
  - 关闭 spatial reuse（全部从 DRAM 加载）
  - 关闭 temporal reuse（禁止 loop hoisting）
  - 不同 `top-k` 设置下的性能与编译时间权衡

---

## 3. 主要实验结果和性能指标

### 📊 GEMM 结果（8×8 Mesh）
| 指标 | TL vs. TTNN |
|------|-----------|
| 几何平均加速比 | **1.03×** |
| 超过 TTNN 的比例 | **62.8%** 的配置更快 |
| 最大加速比 | 达 **2.19×** |
| 接近最优（±10%）比例 | **78.5%**

> TL 在多数情况下优于或持平于厂商库，尤其在大尺寸矩阵中优势明显。

#### 不同拓扑的影响（Figure 5）
- 在 `4×8` 非对称 mesh 上，TT-1D 更优；而在 `8×8` 对称 mesh 上，TT-2D 更好
- **TL 能自适应地选择更合适的 dataflow**，而 TTNN 因启发式策略失败导致性能下降

#### 不规则形状测试（Figure 6）
- 当 `N << M` 时，1D 广播更高效 → TL 正确选择
- 当 `N ≈ M` 时，2D systolic-style 更优 → TL 成功捕捉趋势
- **TTNN 错误选择了 2D 模板**（即使 1D 更快），暴露其策略僵化问题

---

### ⚡ FlashAttention 结果
| 设置 | TL 加速比 |
|------|----------|
| nhead=64 | **1.70–1.99×** |
| nhead=128 | **1.73–1.98×** |
| 几何平均 | **~1.91×** |

> TL 在几乎所有配置下均取得接近 **2×** 的稳定加速。

#### 原因分析：
- TL 自动实现了 **key 数据的 on-chip 复用**，避免重复从 DRAM 加载
- 而 TTNN 默认 mapping 导致大量冗余访存
- 性能模型有效引导编译器选择高复用、低通信成本的 mapping

---

### 🔍 消融实验结果

#### （1）Spatial Reuse 消融（Table 1）
| 配置 (M=K=N) | 无 spatial reuse（DRAM only） | TL（启用 spatial reuse） | Speedup |
|-------------|-------------------------------|-------------------------|--------|
| 1024        | 11.15 TFLOP/s                 | 23.70 TFLOP/s           | **2.12×** |
| 2048        | 16.77                         | 33.96                   | **2.03×** |
| 4096        | 22.96                         | 40.44                   | **1.76×** |
| 5120        | 29.19                         | 41.61                   | **1.42×** |

> spatial reuse 在小规模问题中收益最大，随着问题增大趋于 compute-bound，增益下降但仍显著减少 DRAM 流量（平均减少 **70%**）。

#### （2）Temporal Reuse 消融（Figure 8）
- 在 memory-bound 场景下（K 小，M/N 大），temporal reuse 可带来最高 **1.12×** 提升
- 特别当 M 或 N 很大时，缓冲 A/B tile 显著减少重复加载次数
- 性能模型能自动 deprioritize 无效 hoisting 方案

#### （3）性能模型准确性（Figure 9）
- 预测性能与实测性能的几何平均误差：**17%**
- 能准确捕捉 **roofline 趋势**（memory-bound → compute-bound 的过渡）
- 支持有效 ranking，使得 top-5 候选中大概率包含最优解

#### （4）Top-k 选择的影响（Table 2）
| k 值 | 8×8 Mesh 相对 TTNN 性能 | 编译时间（秒） |
|------|--------------------------|----------------|
| top-1（纯静态） | -6.5%                    | 5.31           |
| top-2            | -0.7%                    | 10.54          |
| top-5            | **+2.8%**                | 26.42          |

> 即使只选 top-2，也能大幅缩小与最优的差距，实现良好的性价比平衡。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **空间数据流架构的性能高度依赖 mapping 策略**，固定模板（如 TT-1D/TT-2D）无法适应多样化的输入形状和硬件拓扑。
2. **TL 能自动发现高质量的 dataflow mapping**，在 GEMM 和 FlashAttention 上达到甚至超越厂商手工优化库的表现。
3. **空间复用（spatial reuse）是提升性能的关键**，尤其是在中小规模问题中可带来数倍加速。
4. **时间复用（temporal reuse）虽增益较小，但在特定场景下仍具价值**，且可通过性能模型自动决策是否启用。
5. **基于 `df` dialect 的硬件抽象使 TL 具备良好可移植性**，易于扩展至其他空间架构（如 1D ring、systolic array）。

---

### ⚠️ 局限性
1. **性能模型精度受限于底层硬件细节缺失**：
   - 缺少精确的微架构参数（如缓存延迟、流水线深度）
   - 在极小规模 kernel 上预测不准，影响 top-1 选择质量
2. **当前前端仅支持 Triton**，尚未集成更多 tile-based DSL（如 TileLang、CuTile）
3. **未考虑动态调度或运行时反馈**，完全依赖静态编译
4. **编译时间随 top-k 增加线性增长**，不适合实时编译场景

---

### 🔮 未来工作方向
1. **增强性能模型**：
   - 引入机器学习校准模块，提升预测准确性
   - 支持更细粒度的 microarchitectural 建模
2. **支持动态 dataflow 编排**：
   - 结合 runtime profiling 实现 adaptive mapping
3. **扩展前端语言支持**：
   - 接入 TVM、Halide、Taichi 等系统
4. **向上游集成到 PyTorch/TensorFlow 生态**
5. **用于芯片设计探索（design-space exploration）**：
   - 利用 `df` 描述不同架构变体，辅助硬件-软件协同设计

---

## 总结

> **TL 是首个实现从 tile-based kernel 到空间数据流架构端到端自动编译的框架**。它通过引入 `df` dialect 统一描述硬件，并结合 spatiotemporal mapping、reuse 分析与性能建模，在无需人工干预的情况下，生成媲美甚至超越厂商库的高效执行方案。该工作推动了空间数据流架构从“专家专属”走向“大众可用”，为下一代 AI 加速器的编程模型提供了重要范式。

</details>

---

### 7. [WeDLM: Reconciling Diffusion Language Models with Standard Causal Attention for Fast Inference](https://arxiv.org/abs/2512.22737)

**Authors**: Aiwei Liu, Minghua He, Shaoxun Zeng, Sijun Zhang, Linhao Zhang, Chuhan Wu, Wei Jia, Yuan Liu, Xiao Zhou, Jie Zhou  
**Category**: cs.CL  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.22737v1  

#### Abstract
Autoregressive (AR) generation is the standard decoding paradigm for Large Language Models (LLMs), but its token-by-token nature limits parallelism at inference time. Diffusion Language Models (DLLMs) offer parallel decoding by recovering multiple masked tokens per step; however, in practice they of...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# WeDLM: Reconciling Diffusion Language Models with Standard Causal Attention for Fast Inference 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Autoregressive (AR)** 生成模型受限于逐token生成的串行特性，在推理时难以充分利用现代硬件的并行能力，导致延迟高、吞吐低。虽然 **Diffusion Language Models (DLLMs)** 支持并行解码，但在实际部署中并未显著超越优化过的 AR 引擎（如 vLLM），主要原因如下：
- 多数 DLLMs 使用 **bidirectional attention**，破坏了标准的 **prefix KV caching** 机制，导致上下文需要重复计算，效率低下。
- 存在训练-推理分布不一致（train-inference gap）和“停等”（stop-and-wait）行为。

### 提出的新方法与核心思想
本文提出 **WeDLM**，一种基于**标准因果注意力**（causal attention）的扩散语言模型框架，旨在实现高效、可缓存的并行生成。其核心创新包括：

#### ✅ **Topological Reordering（拓扑重排序）**
- 将已观测到的 token 移动到物理前缀位置，同时通过 RoPE 的 position ids 保留其逻辑位置。
- 在保持严格因果掩码的前提下，使每个被掩码的位置都能访问所有当前已知的上下文。
- 实现了与 AR 模型完全兼容的 **KV caching**，预测后的 token 可立即缓存复用。

#### ✅ **Dual-Stream Masking（双流掩码）**
- 引入两个流：**Memory Stream**（干净历史）和 **Prediction Stream**（带掩码的待预测部分）。
- 训练时让预测流依赖于记忆流中的干净上下文，而非自身中间预测结果，从而缩小训练与推理之间的差距。

#### ✅ **Streaming Parallel Decoding（流式并行解码）**
- 动态维护一个固定大小的滑动窗口，持续填充新的 `[MASK]` 以维持稳定的并行负载。
- 使用 **distance-penalized entropy selection** 优先解码靠左的 token，促进从左到右的连续前缀增长。
- 避免 block-wise 方法中的“整块完成才提交”的同步等待问题。

### 相比现有方法的优势
| 特性 | WeDLM | 传统 DLLMs（如 LLaDA, Dream） | Block-wise DLLMs（如 SDAR） |
|------|-------|-------------------------------|------------------------------|
| Attention 类型 | **Causal** | Bidirectional | Mixed (causal across blocks) |
| KV Cache 兼容性 | ✅ 完全兼容，即时缓存 | ❌ 不兼容，需重算 | ⚠️ 块级缓存，延迟可用 |
| 并行效率 | 高（动态填充 + 即时提交） | 高但无法有效复用 | 中等（存在 pipeline bubbles） |
| 推理速度 | 显著优于 vLLM | 通常慢于 vLLM | 略优或持平 |
| 部署兼容性 | 可直接运行于 vLLM / FlashAttention / PagedAttention | 需专用引擎（如 dInfer） | 需定制引擎（如 JetEngine） |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多个维度的任务基准：
- **数学推理**：GSM8K（3-shot）、MATH（4-shot）、GPQA-Diamond（5-shot）
- **通用知识**：MMLU（5-shot）、ARC-C/E（0-shot）、HellaSwag（10-shot）
- **代码生成**：HumanEval（4-shot）、HumanEval-plus（4-shot）、MBPP（3-shot）

### 实验设置
- **基础模型**：Qwen2.5-7B 和 Qwen3-8B
- **训练流程**：
  - 继续预训练（continued pretraining）：100B tokens
  - 监督微调（SFT）：10K 内部指令对
- **推理配置**：
  - 统一采用 step-wise 解码方案
  - 窗口大小 `W=6`，距离惩罚系数 `λ=0.1`
  - 采样温度 `0.1`，最大生成长度 `512`

### 评估指标
- **生成质量**：各任务准确率（Accuracy）
- **推理效率**：
  - **End-to-end 推理速度**（tokens/s）
  - **Prefix Cacheability (Pcache)**：衡量预测 token 能否转化为可缓存前缀的比例
- **公平比较**：所有模型均在其推荐系统上服务：
  - AR 模型 → vLLM
  - LLaDA/Dream → dInfer
  - SDAR → JetEngine
  - WeDLM → vLLM（体现部署兼容性）

### 对比的基线方法
- **AR Baselines**：Qwen2.5-7B, Qwen3-8B
- **DLLM Baselines**：LLaDA-8B, Dream-7B, SDAR-8B

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### ✅ Base 模型表现（Table 1）
| 模型 | 平均得分 |
|------|----------|
| Qwen2.5-7B (AR) | 67.21 |
| Qwen3-8B (AR) | 72.61 |
| LLaDA-8B | 55.44 |
| Dream-7B | 56.91 |
| **WeDLM-7B** | **70.84** |
| **WeDLM-8B** | **74.72** |

> WeDLM-8B 超越其 AR 基座 Qwen3-8B（+2.1 pts），且大幅领先其他 DLLMs（+18~19 pts）

#### ✅ Instruct 模型表现（Table 2）
| 模型 | 平均得分 |
|------|----------|
| Qwen2.5-7B-Instruct | 71.09 |
| Qwen3-8B-Instruct | 75.12 |
| SDAR-8B (best DLLM baseline) | 74.22 |
| **WeDLM-8B-Instruct** | **77.53** |

> WeDLM-8B-Instruct 达到最强综合性能，尤其在 **HumanEval (80.49)** 和 **GPQA-Diamond (44.95)** 上领先明显。

### 与基线方法的对比结果
- **速度优势显著**（图1, 图5）：
  - 在复杂推理任务（如 GSM8K）上，相比 vLLM 优化的 AR 模型，WeDLM 实现 **接近 3× 的端到端加速**。
  - 在低熵生成场景（如计数、公式推导），速度提升可达 **10× 以上**（峰值达 1673.3 t/s）。
- **质量不降反升**：
  - WeDLM 不仅保持了原 AR 模型的能力，还在多数任务上有所提升，说明扩散训练目标与指令微调相容甚至增强。

### 消融实验结果（Ablation Studies）

#### 🔍 Streaming vs Block-wise Decoding（图4c）
- 流式解码在相同熵阈值下比 block-wise 快 **1.9×**（423 vs 221 t/s）。
- 原因：block-wise 必须等待整块完成才能提交，Pcache 更低；而 streaming 可即时提交已确认 token。

#### 🔍 Distance Penalty λ 的影响（图4b）
- 增加 λ（偏向左侧 token）可提升 accuracy（最高 +2.6 pts），仅轻微降低速度（-3%）。
- 表明 left-to-right commitment 有助于减少错误传播，提高 Pcache。

#### 🔍 Block Size 敏感性（图5b）
- 在训练阶段尝试不同 block size（4, 8, 32），性能几乎不变（平均分差 < 0.8 pts）。
- 说明 WeDLM 对 block 设计鲁棒，便于灵活部署。

#### 🔍 Causal vs Bidirectional Intra-block Attention（图5c）
- 即便在同一 block 内使用 bidirectional attention，性能也低于 fully causal 设计。
- 进一步验证：**causal 结构更利于 cache-friendly 解码**，且无需牺牲性能。

#### 🔍 Base Model Scale 影响（图5c）
- 小模型（0.6B, 1.5B）适应 WeDLM 后略有下降；
- 大模型（7B+）则稳定提升（+3.6 pts）；
- 暗示：**更强的 AR checkpoint 更容易成功迁移到 diffusion 框架**，可能存在 scaling law。

---

## 4. 关键结论和发现

### 主要发现
1. **KV caching 是决定实际推理效率的关键因素**，远比“每步预测多少 token”更重要。
2. **Bidirectional attention 并非并行生成所必需**，通过 Topological Reordering 可在 causal attention 下实现全局上下文可见。
3. **WeDLM 成功将 diffusion-style 并行性与 AR 式 caching 完美结合**，首次在真实部署条件下（使用 vLLM）超越工业级 AR 引擎。
4. 扩散训练不仅能提速，还能**提升模型能力**，尤其是在数学与代码任务上。

### 方法的局限性
- **性能增益高度依赖输出熵**：
  - 低熵任务（如计数、公式）加速明显（>8×）
  - 高熵任务（如开放问答、创意写作）加速有限（~2×），因 speculative token 置信度低。
- 当前 acceptance 机制较简单，缺乏 robust rejection/correction 机制。
- 仍需人工设定超参（如 entropy threshold, λ），自动化调节有待研究。

### 未来工作方向
- 开发更鲁棒的 **acceptance criterion** 或引入 **reinforcement learning** 来动态控制 speculative depth。
- 探索 **dynamic entropy calibration** 以平衡不同任务下的质量与速度。
- 系统验证 AR-to-Diffusion 的 **scaling laws**，指导更大规模模型迁移。
- 将 WeDLM 思路扩展至多模态或长文本生成场景。

---

> 💡 **一句话总结**：  
> WeDLM 通过 **Topological Reordering + Streaming Parallel Decoding**，首次实现了在标准 causal attention 下高效、可缓存的 diffusion 解码，并在质量和速度上全面超越现有 DLLMs 与工业 AR 引擎，为下一代高速 LLM 推理提供了可行路径。

</details>

---

### 8. [GPU-Virt-Bench: A Comprehensive Benchmarking Framework for Software-Based GPU Virtualization Systems](https://arxiv.org/abs/2512.22125)

**Authors**: Jithin VG, Ditto PS  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.22125v1  

#### Abstract
The proliferation of GPU-accelerated workloads, particularly in artificial intelligence and large language model (LLM) inference, has created unprecedented demand for efficient GPU resource sharing in cloud and container environments. While NVIDIA's Multi-Instance GPU (MIG) technology provides hardw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GPU-Virt-Bench: A Comprehensive Benchmarking Framework for Software-Based GPU Virtualization Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在云环境和容器化平台中，GPU 资源共享需求激增，尤其是在 **AI 和 LLM 推理**场景下。然而：
- **硬件级虚拟化（如 NVIDIA MIG）** 仅支持高端数据中心 GPU（如 A100、H100），限制了其适用范围；
- **软件虚拟化方案（如 HAMi-core）** 虽然兼容更广泛的 GPU 型号，但缺乏统一、可复现的评估标准；
- 现有基准测试工具（如 MLPerf、Rodinia）关注的是原始性能而非虚拟化特性，无法衡量隔离质量、开销等关键维度。

因此，该论文旨在解决 **“缺乏标准化、系统化的 GPU 软件虚拟化评估框架”** 这一关键空白。

---

### 🚀 提出的新方法与创新点

作者提出了 **GPU-Virt-Bench** —— 一个面向软件 GPU 虚拟化系统的综合性基准测试框架，具有以下五大贡献：

1. **构建了包含 56 个指标的完整度量体系（Taxonomy）**
   - 涵盖 10 大类别：Overhead、Isolation、LLM、Memory Bandwidth、Cache、PCIe、NCCL/P2P、Scheduling、Fragmentation、Error Recovery。
   - 首次将 LLM 特定行为（如 KV Cache 分配、动态批处理延迟）纳入评估范畴。

2. **开源可扩展的模块化框架设计**
   - 支持多种虚拟化后端（Native、HAMi-core、BUD-FCSP、MIG-Ideal）；
   - 提供 CLI 接口、多进程测试支持、统计分析与报告生成（JSON/CSV/TXT）。

3. **引入 MIG-Ideal 模拟基线进行对标**
   - 即使在无 MIG 硬件的设备上也能通过模拟理想 MIG 行为作为性能上限参考；
   - 定义了 `△MIG` 指标来量化与理想隔离之间的差距。

4. **提出加权评分机制与等级评定系统**
   - 不同类别按重要性赋予权重（如 Isolation 和 LLM 各占 20%）；
   - 综合得分映射为 A+/A/B+/C/D/F 等级，便于直观比较。

5. **提出并评估新型虚拟化方案 BUD-FCSP**
   - 在 HAMi-core 基础上改进 SM 控制粒度、降低 API 截获开销、优化令牌桶限流算法；
   - 展示了软件虚拟化进一步优化的可能性。

---

### 🔍 相比现有方法的优势

| 方面 | 传统方法（MLPerf/Rodinia/Vendor Tools） | GPU-Virt-Bench |
|------|----------------------------------------|---------------|
| **评估目标** | 原始计算性能 | 虚拟化质量（隔离、开销、稳定性） |
| **覆盖维度** | 单一性能指标 | 10 类共 56 项细粒度指标 |
| **LLM 支持** | 弱或缺失 | 显式建模注意力机制、KV Cache、TTFT 等 |
| **可复现性** | 封闭或不透明 | 开源、参数可配置、支持回归测试 |
| **公平对比** | 缺乏统一基线 | 提供 MIG-Ideal 模拟基准 |

---

## 2. 核心实验方法和设置

### 📊 实验平台配置
- **GPU**: NVIDIA A100-40GB PCIe
- **CPU**: AMD EPYC 7742 (64 cores)
- **内存**: 512GB DDR4-3200
- **存储**: NVMe SSD
- **操作系统**: Ubuntu 22.04 LTS
- **CUDA**: 12.0
- **驱动版本**: 525.105.17

---

### ⚙️ 实验设置

#### 支持的虚拟化模式（见 Table 2）
| System       | Key     | 描述 |
|--------------|---------|------|
| Native       | native  | 原生裸机基准 |
| HAMi-core    | hami    | CNCF 沙箱项目，基于 CUDA API 截获 |
| BUD-FCSP     | fcsp    | 本文提出的增强型软件虚拟化层 |
| MIG-Ideal    | mig     | 基于规格书的理想化 MIG 模拟值（非实测） |

> 注：MIG-Ideal 是从 NVIDIA 公开文档和文献推导出的理论最优表现，用于设定性能天花板。

---

#### 评估指标分类（共 56 项，见 Table 1 和 Appendix A）

| 类别 | 示例指标 | 单位 | 更好方向 |
|------|--------|-------|----------|
| **Overhead** | OH-001 Kernel Launch Latency | μs | Lower |
| **Isolation** | IS-003 SM Utilization Accuracy | % | Higher |
| **LLM** | LLM-004 Token Generation Latency | ms | Lower |
| **Memory Bandwidth** | BW-001 Memory Bandwidth Isolation | % | Higher |
| **Cache** | CACHE-001 L2 Cache Hit Rate | % | Higher |
| **PCIe** | PCIE-003 PCIe Contention Impact | % | Lower |
| **NCCL/P2P** | NCCL-003 P2P GPU Bandwidth | GB/s | Higher |
| **Scheduling** | SCHED-001 Context Switch Latency | μs | Lower |
| **Fragmentation** | FRAG-001 Fragmentation Index | % | Lower |
| **Error Recovery** | ERR-003 Graceful Degradation Score | % | Higher |

所有指标默认运行 100 次迭代，含 10 次预热，输出均值、P95、P99、标准差、CV 等统计信息。

---

#### 对比基线方法
- **Native**: 性能天花板
- **HAMi-core**: 当前主流开源软件虚拟化方案
- **BUD-FCSP**: 本文提出的改进版
- **MIG-Ideal**: 理想硬件隔离目标（模拟）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总（来自 Tables 4–7）

#### 表 4：Overhead 结果（单位：μs，除非特别注明）
| Metric | Native | HAMi | FCSP |
|--------|--------|------|------|
| OH-001 (Launch) | 4.2 | 15.3 | 8.7 |
| OH-002 (Alloc) | 12.5 | 45.2 | 28.3 |
| OH-003 (Free) | 8.1 | 32.4 | 18.6 |
| OH-005 (Hook, ns) | – | 85 | 42 |
| OH-010 (Throughput Degradation) | – | 18.5% | 9.2% |

> 💡 **发现**：BUD-FCSP 相比 HAMi-core 减少约 **43% 的 API 截获开销**，总吞吐退化减少一半。

---

#### 表 5：Isolation 质量（4 个租户并发）
| Metric | HAMi | FCSP |
|--------|------|------|
| IS-001 (Mem Accuracy, %) | 98.2 | 99.1 |
| IS-003 (SM Accuracy, %) | 85.4 | 92.7 |
| IS-008 (Fairness Index) | 0.87 | 0.94 |
| IS-009 (Noisy Neighbor Impact, %) | 24.3 | 12.1 |
| IS-010 (Fault Isolation) | Pass | Pass |

> 💡 **发现**：FCSP 在 SM 利用率控制精度、公平性和抗干扰能力方面显著优于 HAMi-core。

---

#### 表 6：LLM 工作负载性能（相对原生性能）
| Metric | HAMi | FCSP |
|--------|------|------|
| LLM-001 (Attention Throughput, %) | 82.3% | 91.5% |
| LLM-002 (KV Cache Alloc Speed, %) | 76.4% | 88.2% |
| LLM-004 (TTFT, ms) | 45.2 | 28.7 |
| LLM-004 (ITL, ms) | 12.8 | 8.4 |
| LLM-003 (Batch Scaling Ratio) | 0.78 | 0.89 |

> 💡 **发现**：LLM 推理对内存分配延迟极为敏感；FCSP 在 Token 生成延迟上比 HAMi 快 **35%以上**。

---

#### 表 7：综合得分与评级
| System | Score | MIG Parity | Grade |
|--------|-------|------------|-------|
| MIG-Ideal | 100% | 100% | A+ (baseline) |
| Native | 100% | – | A+ |
| BUD-FCSP | 85.2% | 85.2% | B+ |
| HAMi-core | 72.0% | 72.0% | C |

> ✅ **结论**：BUD-FCSP 达到理想 MIG 表现的 **85.2%**，而 HAMi-core 仅为 **72%**。

---

### 🔍 消融实验与关键归因分析（隐含在实现细节中）
虽然未明确列出消融实验表格，但从代码与描述可知性能提升来源：
- **优化 dlsym hook 路径** → 降低 OH-005（API 截获开销）
- **改进令牌桶算法（带突发处理）** → 提升调度响应速度
- **加权公平队列调度器** → 改善 IS-008 公平性
- **精细化 SM 利用率控制（亚百分比粒度）** → 提高 IS-003 准确性

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **软件虚拟化可达近似 MIG 水平的表现**
   - BUD-FCSP 在综合评分上达到理想 MIG 的 **85.2%**，表明高质量软件虚拟化是可行的。

2. **LLM 推理对虚拟化开销高度敏感**
   - 内存分配延迟直接影响 KV Cache 构建效率和 Token 生成速度；
   - 优化 `cuMemAlloc` 路径对实际推理延迟至关重要。

3. **隔离质量仍存在明显差距**
   - 即便最先进的软件方案也无法完全复制 MIG 的硬件级资源隔离；
   - SM 利用率控制误差仍在 7–15%，可能导致 QoS 波动。

4. **BUD-FCSP 显著优于 HAMi-core**
   - 在延迟、公平性、噪声邻居抑制等方面全面领先；
   - 适合部署于生产级多租户 LLM 推理服务。

5. **MIG 仍是严格隔离场景的首选**
   - 若硬件支持且预算允许，应优先使用 MIG；
   - 否则推荐采用 BUD-FCSP 替代 HAMi-core。

---

### ⚠️ 局限性

1. **单 GPU 设计**
   - 当前框架聚焦单卡场景，尚未涵盖分布式训练或多 GPU 张量并行通信压力测试。

2. **合成 LLM 工作负载**
   - 使用自定义 CUDA kernel 模拟 Attention 和 KV Cache，未集成 PyTorch 或 TensorRT-LLM；
   - 实际框架中的调度器、显存管理可能引入额外影响。

3. **MIG-Ideal 为模拟值**
   - 并非真实测量结果，实际 MIG 性能受分区几何、工作负载特征影响。

4. **GPU 架构覆盖有限**
   - 实验仅在 A100 上完成，其他架构（如 RTX 系列、L4、H100）的行为可能不同。

5. **版本依赖性强**
   - HAMi-core 和 BUD-FCSP 的表现随版本演进而变化，需持续更新基准。

---

### 🔮 未来工作方向

1. **扩展至多 GPU 场景**
   - 加入 NCCL 集体通信压力测试、跨节点 P2P 带宽评估。

2. **集成真实 LLM 框架**
   - 支持 HuggingFace Transformers、vLLM、TensorRT-LLM 等主流推理引擎。

3. **自动化回归测试**
   - 构建 CI/CD 流水线，监控虚拟化组件升级带来的性能波动。

4. **支持更多虚拟化后端**
   - 如 Intel FlexGPU、AMD MxGPU、vCUDA 等异构平台。

5. **开发可视化仪表盘**
   - 提供 Web UI 展示历史趋势、对比报告、瓶颈定位建议。

---

## 总结

GPU-Virt-Bench 是首个系统化评估 **软件 GPU 虚拟化** 的开源基准框架，填补了行业在多租户 GPU 资源管理评估方面的空白。它不仅提供了 **56 项细粒度指标** 和 **科学评分体系**，还揭示了当前软件方案与理想 MIG 之间的性能鸿沟，并验证了 **BUD-FCSP** 在延迟、隔离和 LLM 支持上的显著优势。

该框架对于云服务商、AI 平台开发者以及虚拟化中间件研发团队具有重要实用价值，推动 GPU 资源共享技术向更高效率与更强可靠性发展。

</details>

---

### 9. [KernelEvolve: Scaling Agentic Kernel Coding for Heterogeneous AI Accelerators at Meta](https://arxiv.org/abs/2512.23236)

**Authors**: Gang Liao, Hongsen Qin, Ying Wang, Alicia Golden, Michael Kuchnik, Yavuz Yetim, Jia Jiunn Ang, Chunli Fu, Yihan He, Samuel Hsia, Zewei Jiang, Dianshi Li, Uladzimir Pashkevich, Varna Puvvada, Feng Shi, Matt Steiner, Ruichao Xiao, Nathan Yan, Xiayu Yu, Zhou Fang, Abdul Zainul-Abedin, Ketan Singh, Hongtao Yu, Wenyuan Chi, Barney Huang, Sean Zhang, Noah Weller, Zach Marine, Wyatt Cook, Carole-Jean Wu, Gaoxiang Liu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.23236v1  

#### Abstract
Making deep learning recommendation model (DLRM) training and inference fast and efficient is important. However, this presents three key system challenges - model architecture diversity, kernel primitive diversity, and hardware generation and architecture heterogeneity. This paper presents KernelEv...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：KernelEvolve: Scaling Agentic Kernel Coding for Heterogeneous AI Accelerators at Meta

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代深度学习推荐模型（DLRM）在训练和推理中面临三大系统挑战：
- **模型架构多样性**：从传统MLP到Transformer、卷积增强架构等；
- **算子多样性**：除GEMM外，存在大量数据预处理算子（如`MapId`, `MBDT`），其优化对部署至关重要；
- **硬件异构性**：跨厂商（NVIDIA、AMD）、跨代际（Ampere vs Hopper）、自研芯片（MTIA）的AI加速器差异巨大。

这三者组合导致了一个巨大的优化空间，传统手动内核开发无法扩展，开发周期长达数周，且难以覆盖所有硬件平台。

### 提出的新方法与思路
论文提出了 **KernelEvolve**，一个基于智能体（agentic）的自动化内核生成与优化框架，其核心创新包括：

#### （1）**统一的多目标抽象编程接口**
- 支持多种编程范式：Triton、CuTe DSL、TLX、MTIA C++ DSL等；
- 通过 **Triton-MLIR** 多级编译架构实现跨平台支持（NVIDIA、AMD、MTIA）；
- 利用 **Triton MPP**（Multi-Pass Profiler）统一底层性能分析工具链（NCU、Proton、MTIA Insight），打通编译器、运行时与硬件计数器之间的信息孤岛。

#### （2）**图搜索驱动的智能体优化流程**
将内核优化建模为 **图搜索问题**，定义四元组 `(F, Tsel, O, T)`：
- **Fitness Function (F)**：以相对于PyTorch基线的速度提升作为适应度函数；
- **Selection Policy (Tsel)**：支持贪心、MCTS、进化算法等多种策略选择候选节点；
- **Universal Operator (O)**：单一通用操作符，结合检索增强提示（retrieval-augmented prompt synthesis）动态调整行为，避免传统“Debug/Improve”固定模板的认知限制；
- **Termination Rule (T)**：基于时间预算、性能阈值或收敛判断终止。

#### （3）**持久化知识库与上下文管理机制**
- 构建 **Persistent Knowledge Base**，编码硬件约束、优化模式、调试指南；
- 引入 **Context Memory Sub-Agent** 和 **Deep Search Sub-Agent**，前者维护搜索状态元数据，后者从知识库中检索相关文档用于提示合成；
- 特别针对 **MTIA** 这类私有架构，注入专属知识（如SFU指令、双核同步、跨PE通信原语），使LLM能生成正确代码。

#### （4）**端到端生产集成能力**
- 自动化生成 **PyTorch + Triton 双实现模块**，便于正确性验证；
- 集成 **FaaS** 实现评估解耦，生成在CPU上进行，评估在远程AI加速器集群执行，提高资源利用率；
- 支持 **JIT调试**：通过MTIA-Triton的C++中间表示发射功能，直接查看并修改底层RISC-V向量指令，快速修复错误。

### 相比现有方法的优势
| 维度 | 现有方法（如AutoTriton、Kevin、GEAK） | KernelEvolve |
|------|----------------------------------------|-------------|
| **硬件支持** | 单一平台（通常仅NVIDIA） | 跨平台：NVIDIA、AMD、MTIA |
| **评估真实性** | 合成算子、静态形状 | 生产级动态批处理、变长序列、真实算子组合 |
| **生命周期管理** | 仅生成 | 全流程：生成 → 编译 → 正确性验证 → 性能评测 → 调试 → 部署 |
| **知识利用** | 依赖LLM预训练知识 | 检索增强 + 持久化知识库，支持私有架构 |
| **搜索规模** | 少量迭代 | 百至千步的推理时扩展（inference-time scaling） |
| **容错性** | 失败即重启 | Checkpointing + 分布式并发探索 |

---

## 2. 核心实验方法和设置

### 数据集与测试场景
- **开源基准**：
  - **KernelBench**：包含250个问题，涵盖单算子、融合、全模型三个难度等级；
  - **PyTorch ATen Operators**：选取160个常用ATen算子（如`add`, `cos`, `elu`, `amax`）；
- **生产级用例**：
  - 推荐系统中的典型算子：`MapIdTransform`, `MergeBucketizedDenseTransform`, `Batch Event Truncate`；
  - 模型组件：`Conv1d` in Convolutional Transformer, `Optimized FM` in WuKong, `PFFN` in InterFormer；
  - 自研芯片MTIA v2i/v3上的实际部署需求。

### 实验设置
- **硬件平台**：
  - NVIDIA H100/A100
  - AMD MI300/MI350
  - Meta MTIA v2i / v3
- **软件栈**：
  - PyTorch 2.0 + torch.compile
  - Triton 编译器 + MLIR后端
  - MTIA Runtime + Triton-MTIA
- **评估指标**：
  - **Correctness**：`torch.allclose()` 数值一致性检查；
  - **Speedup**：Triton Kernel vs PyTorch Baseline 的执行时间比；
  - **Latency**：P50/P99端到端延迟；
  - **Coverage**：支持的算子数量与平台覆盖率。

### 基线方法对比
| 基线 | 描述 |
|------|------|
| `torch.nn.functional.conv1d` | 原生PyTorch实现 |
| `torch.nn.functional.conv2d`（NHWC） | 常见优化技巧：reshape为2D调用cuDNN优化路径 |
| 手工优化内核 | Meta工程师手写高性能Triton/MTIA内核 |
| PyTorch + torch.compile | 默认编译优化后的基线 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 场景 | 平台 | 速度提升（Speedup） | 备注 |
|------|------|---------------------|------|
| `Conv1d` (生产形状) | NVIDIA H100 | **2.30×** | 相比`conv1d`；相比`conv2d`仍快1.62× |
| `Conv1d` | MTIA v3 | **6.54×** | 显著优于vendor库 |
| `Optimized FM` (WuKong) | H100 | **4.0×** | 融合消除中间内存访问 |
| `PFFN` (InterFormer) | H100 | **2.5×** | 跨操作Tile复用 |
| `MapIdTransform` | MTIA v2i | **4.07×** | 批大小50k时峰值 |
| `MBDT` | MTIA v2i | **9.25×** | 大输入下显著优势 |
| `Batch Event Truncate` | MTIA v3 | **9.8×** | 多特征批量处理优化 |
| `RMSNorm 2D backward` | MTIA v3 | **17×** | 最高提速案例 |
| LLM Attention (`SDPA-MLP`) | H100 | **3.3×** | 展示泛化能力 |

### 正确性与覆盖率
- 在 **KernelBench** 上达到 **100% 通过率**（250题）；
- 在 **160个ATen算子 × 3平台**（共480配置）上全部生成正确内核，**正确率100%**；
- 成功为MTIA v3上缺失的多个ATen算子（如`_unique2`, `unique_consecutive`）提供可运行实现。

### 消融实验与优化轨迹分析
- **图搜索有效性**：在`conv1d`优化中，初始draft阶段平均速度提升约2×，经过tree expansion引入反馈后逐步提升至6.8×，证明执行反馈对持续改进至关重要；
- **知识检索作用**：关闭Deep Search Sub-Agent后，在MTIA上生成失败率上升超过70%，表明知识注入是私有架构成功的关键；
- **FaaS解耦收益**：评估任务并行度提升10倍以上，充分利用Meta内部庞大的AI加速器池，避免本地设备瓶颈。

---

## 4. 关键结论和发现

### 主要发现
1. **自动化内核生成已具备生产可用性**：KernelEvolve能在无需人工干预的情况下，生成**正确且高性能**的内核，性能媲美甚至超越专家手工优化结果。
2. **预处理算子决定部署架构**：缺失一个预处理内核可能导致必须采用**分拆式服务架构**（disaggregated serving），带来额外10–20ms网络开销（见Table 2），远超计算效率损失。因此，**完整内核覆盖是成本控制的前提**。
3. **私有架构可通过知识注入支持**：即使LLM未见过MTIA代码，只要注入足够详尽的文档（如SFU映射表、双核调度规则），即可生成高质量专用代码。
4. **推理时扩展（inference-time scaling）有效**：通过大规模搜索（数百步），模型能够发现复杂优化策略（如跨操作融合、定制tiling），这些策略难以通过一次性生成获得。
5. **开发效率飞跃**：将原本需要**数周**的手工优化缩短至**数小时**，极大加速新硬件上线和模型迭代。

### 方法的局限性
- **依赖高质量知识库建设**：若知识库不完整或过时，检索效果下降，影响生成质量；
- **长尾算子仍需人工介入**：极少数非常规算子可能多次尝试失败，需人工标注或引导；
- **token消耗较大**：大规模搜索带来较高推理成本，需权衡优化深度与经济性；
- **目前聚焦Triton层**：尚未深入MLIR或PTX/SASS层级，某些极致优化受限于高级DSL表达力。

### 未来工作方向
- **从算子到模型级优化**：扩展至跨层融合、全局内存分配、端到端图优化；
- **支持更多硬件**：ARM CPU、Blackwell GPU、下一代MTIA等；
- **强化学习适配**：通过执行反馈微调LLM，而非仅依赖提示工程；
- **垂直整合到底层语言**：支持MLIR dialect修改、PTX插入等低级控制；
- **可持续AI基础设施**：量化token消耗与碳足迹，构建绿色优化闭环；
- **无限宽度搜索（infinite-width search）**：数千并发搜索路径，最终收敛最优解。

---

> ✅ **总结一句话**：  
> KernelEvolve首次实现了在工业规模下，面向异构AI加速器的全自动、高可靠、高性能内核生成，解决了“硬件更新快、软件跟不上”的根本矛盾，标志着AI系统优化进入“智能体驱动”的新时代。

</details>

---

### 10. [Nightjar: Dynamic Adaptive Speculative Decoding for Large Language Models Serving](https://arxiv.org/abs/2512.22420)

**Authors**: Rui Li, Zhaoning Zhang, Libo Zhang, Huaimin Wang, Xiang Fu, Zhiquan Lai  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22420v1  

#### Abstract
Speculative decoding (SD) accelerates LLM inference by verifying draft tokens in parallel. However, this method presents a critical trade-off: it improves throughput in low-load, memory-bound systems but degrades performance in high-load, compute-bound environments due to verification overhead. Curr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Nightjar: Dynamic Adaptive Speculative Decoding for Large Language Models Serving*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **固定 speculative length 的局限性**：现有的 Speculative Decoding (SD) 方法通常采用固定的 speculative length（如 γ=3），无法适应动态变化的请求负载（request load）。
- **性能瓶颈在不同负载下表现不一**：
  - 在低负载、memory-bound 场景中，SD 可提升吞吐；
  - 但在高负载、compute-bound 场景中，验证开销（verification overhead）反而导致性能下降。
- **缺乏对“切换成本”（switching cost）的建模**：当从 `γ=0`（关闭 SD）重新启用 SD 时，需重建 draft model 的 KV cache，带来显著延迟，现有方法未考虑此代价。

### 🚀 提出的新方法与创新思路
提出 **Nightjar** ——一种基于 **contextual multi-armed bandit (MAB)** 的动态自适应 speculative decoding 决策算法，具备以下核心创新：

1. **动态选择最优 speculative length**  
   根据实时 batch size 自动调整 γ ∈ {0,1,...,γ_max}，甚至在不利情况下主动禁用 SD（即 γ=0），避免性能退化。

2. **三层时间层级结构设计（Blocks → Bins → Rounds）**
   - 引入指数增长的 Block 和固定大小 Bin 来组织时间步，实现长期探索-利用平衡。
   - 随着历史积累，exploitation 阶段逐渐变长，提高稳定性。

3. **引入 switching cost 建模**
   - 显式建模从 γ=0 切换到 γ>0 所需的 KV cache 重建开销 $ C_{\text{prefill}} $。
   - 在目标函数中加入惩罚项：$ \mathbb{I}(y_{t-1}=0 \land y_t>0) \cdot C_{\text{prefill}} / y_t $，鼓励更长的 speculative 序列以摊销启动成本。

4. **无需先验知识的学习机制**
   - 不依赖模型特性或请求难度的先验信息，通过在线学习估计每个 (batch size, γ) 组合的实际 goodput。

### 🔍 相比现有方法的优势
| 方法 | 局限性 | Nightjar 如何改进 |
|------|--------|------------------|
| 固定 γ 的 SD | 无法适应负载变化 | 动态调节 γ，包括关闭 SD |
| DSD [15] | 依赖历史 acceptance rate，易陷入“决策死锁” | 探索-利用机制防止数据停滞 |
| BanditSpec [12] | 静态 batch 设计，不支持 continuous batching | 支持现代 serving 架构中的动态批处理 |
| SpecServe/TETRIS [13][14] | 依赖 confidence score，仍强制生成 token | 可完全跳过 speculative decoding |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **ShareGPT**：真实聊天场景数据，用于模拟 online chatting。
- **Alpaca**：指令微调数据集，涵盖多样化任务。
- **SpecBench**：由 6 个常用 benchmark 构成的合成测试集，含 480 个实例。
- **AzureLLMInferenceDataset**：生产环境下的动态请求轨迹（用于模拟 request rate 变化）。

> 所有数据集均使用 480 个样本进行评估，输入/输出长度分布见图 7。

### ⚙️ 实验设置
- **目标模型**：
  - `DeepSeek-R1-Distill-Qwen-7B`（RTX 4090）
  - `vicuna-13b`（A100）
- **Draft 模型**：
  - `DeepSeek-R1-DRAFT-Qwen2.5-0.5B`
  - `vicuna-68m`
- **框架**：基于 **vLLM (0.8.2)** 实现 Nightjar，并启用 PagedAttention。
- **最大 speculative length**：γ_max = 5
- **每组实验重复 5 次取平均值**

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **Throughput** | 单位时间内生成的 token 数量（tokens/s） |
| **Mean End-to-End Latency** | 请求从提交到完成的平均延迟（ms） |
| **Goodput** | 包括 accepted tokens 和 bonus tokens 的有效产出率 |

### 🆚 基线方法对比
| 基线 | 描述 |
|------|------|
| **w/o SD** | Vanilla autoregressive decoding（无 speculative） |
| **SD (γ=3)** | 标准 speculative decoding，固定长度 3 |
| **DSD [15]** | 基于线性 goodput 模型动态优化 γ |
| **BanditSpec [12]** | 使用 MAB 进行长度选择，但为静态 batch 设计 |
| **TETRIS [14]** | 基于 confidence 的 batch-level speculative 优化 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### 表 2：吞吐量对比（Tokens/s）

| Method       | 7B (Alpaca) | 7B (ShareGPT) | 7B (SpecBench) | 13B (Alpaca) | 13B (ShareGPT) | 13B (SpecBench) |
|--------------|-------------|---------------|----------------|--------------|----------------|------------------|
| **Nightjar** | **2102.73** | **3734.23**   | **3326.20**    | **721.23**   | **1729.68**    | **1061.82**      |
| BanditSpec   | 1860.74     | 3428.75       | 2631.43        | 679.82       | 1383.81        | 763.15           |
| DSD          | 2044.40     | 3614.65       | 2789.51        | 603.32       | 1244.96        | 692.01           |
| TETRIS       | 1875.91     | 3597.21       | 2974.14        | 702.65       | 1628.86        | 985.40           |
| SD (γ=3)     | 1920.63     | 3593.36       | 3044.80        | 628.47       | 1689.53        | 964.02           |
| w/o SD       | 2072.38     | 3653.61       | 3207.28        | 438.13       | 1290.04        | 673.48           |

> ✅ Nightjar 在所有配置下均取得最高 throughput。

#### 表 3：端到端延迟对比（ms）

| Method       | 7B (Alpaca) | 7B (ShareGPT) | 7B (SpecBench) | 13B (Alpaca) | 13B (ShareGPT) | 13B (SpecBench) |
|--------------|-------------|---------------|----------------|--------------|----------------|------------------|
| **Nightjar** | **8868.17** | **6534.76**   | **7618.85**    | **23525.49** | **8133.92**    | **14691.81**     |
| BanditSpec   | 12340.83    | 7799.63       | 9288.23        | 29077.32     | 9667.04        | 19294.42         |
| DSD          | 9183.21     | 6499.12       | 8301.02        | 16212.44     | 10057.72       | 22361.71         |
| TETRIS       | 11734.19    | 7409.62       | 8789.48        | 22133.43     | 8046.25        | 14937.37         |
| SD (γ=3)     | 11110.68    | 7047.49       | 8567.04        | 24671.40     | 8152.97        | 14212.97         |
| w/o SD       | 8876.14     | 6438.47       | 7654.05        | 32222.49     | 9350.87        | 23831.74         |

> ✅ Nightjar 实现最低延迟，在多个场景下显著优于其他方法。

### 🔢 性能提升总结
- **相比标准 SD (γ=3)**：
  - 最高 **14.8% 吞吐提升**（如 7B Alpaca 上从 1920 → 2102 tokens/s）
  - 平均 **20.2% 延迟降低**
- **相比 w/o SD**：
  - 在 13B Alpaca 上实现 **64.6% 吞吐提升**
- **相比先进基线**：
  - 比 DSD 在 13B SpecBench 上高出 **53.4%**
  - 比 BanditSpec 高出 **39.1%**

### 🔍 消融实验结果（Ablation Study）
- 图 11 对比了多种 context-aware MAB 方法：
  - **LinUCB** 和 **linearTS** 假设 reward 与 context（batch size）呈线性关系，在 LLM 推理中不成立，表现较差。
  - **ADA-BINGREEDY**（Nightjar 所用框架）无需强假设，适应非线性复杂场景，性能最优。
- 结果表明：**简单而鲁棒的 bandit 策略优于复杂的线性模型**，更适合真实 serving 环境。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Speculative decoding 的收益高度依赖负载状态**：
   - 低负载 → memory-bound → SD 有利；
   - 高负载 → compute-bound → SD 开销大于收益。
2. **固定 speculative length 是次优策略**，必须根据 batch size 动态调整。
3. **Nightjar 能智能判断何时启用/禁用 SD**，避免在 compute-bound 场景中性能倒退。
4. **Switching cost 必须显式建模**，否则频繁启停会严重拖累系统响应速度。
5. **基于 contextual MAB 的学习机制可高效收敛至最优策略**，且 overhead 极小（< 0.001s per step）。

### ⚠️ 方法的局限性
- **需要离线构建 $ C_{\text{prefill}} $ 查找表**：虽然准确，但增加了部署复杂度。
- **假设 draft model 固定**：未涉及 draft model 本身的选型优化。
- **当前仅优化 speculative length**，未联合优化 temperature、top-k 等生成参数。

### 🔮 未来工作方向
- 将 Nightjar 扩展至 **多级 speculative pipeline**（如 draft model cascade）。
- 引入 **online profiling** 替代离线查找表，实现全自动 cost estimation。
- 探索 **端到端 joint optimization** of draft model selection + speculative length。
- 支持 **SLO-aware adaptation**，根据不同服务质量要求动态调整策略（如延迟敏感 vs 吞吐优先）。

---

## ✅ 总结
**Nightjar** 是首个真正面向 **real-time, dynamic LLM serving** 的 adaptive speculative decoding 框架。它通过 **contextual MAB + switching cost modeling + hierarchical exploration-exploitation control**，实现了：
- 最高 **14.8% 吞吐提升**
- 最低 **20.2% 延迟下降**
- 全负载范围内的 **robust efficiency**

该工作为下一代高效、智能的 LLM serving 系统提供了重要范式，推动 speculative decoding 从“静态加速器”向“动态决策引擎”的演进。

</details>

---

### 11. [Wireless Traffic Prediction with Large Language Model](https://arxiv.org/abs/2512.22178)

**Authors**: Chuanting Zhang, Haixia Zhang, Jingping Qiao, Zongzhang Li, Mohamed-Slim Alouini  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.22178v1  

#### Abstract
The growing demand for intelligent, adaptive resource management in next-generation wireless networks has underscored the importance of accurate and scalable wireless traffic prediction. While recent advancements in deep learning and foundation models such as large language models (LLMs) have demons...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Wireless Traffic Prediction with Large Language Model》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Large Language Model (LLM)** 的无线流量预测方法（如 TrafficLLM、Time-LLM）主要关注**时间序列建模**，忽略了城市级无线网络中不同区域之间的**空间依赖性**。这种忽略导致模型在复杂城市环境中泛化能力差、预测精度受限。

此外，传统方法难以有效融合数值型流量数据与 LLM 所擅长处理的语言结构，存在“**领域鸿沟**”（domain gap）。

### 提出的新方法：TIDES
作者提出 **TIDES**（**Traffic Intelligence with DeepSeek-Enhanced Spatial-temporal prediction**），一种结合 LLM 与空间感知机制的城市级无线流量预测框架。其三大核心创新如下：

#### （1）Region-aware Modeling（区域感知建模）
- 首先通过聚类算法（增强 K-means）将城市划分为若干具有相似流量模式和空间相关性的区域（clusters）。
- 对每个区域组训练个性化的 LLM 模型，实现**通用性**（generalization）与**专业化**（specialization）的平衡。

#### （2）Prompt-based Traffic Representation（提示工程驱动的数据表示）
- 将原始数值流量数据转换为结构化自然语言 prompt，嵌入以下特征：
  - 基础统计量（均值、方差等）
  - 趋势分析（上升/下降）
  - 时间段模式（早高峰、晚高峰、夜间）
  - 领域特定指标（峰均比、突发性、波动变化等）
- 利用 LLM 强大的语义理解能力进行推理预测。

#### （3）Spatial-Temporal Alignment with DeepSeek（跨域注意力机制）
- 设计一个 **DeepSeek 模块**，引入 **multi-head cross-domain attention** 机制。
- 构建邻接矩阵并利用图拉普拉斯变换稳定梯度，使 LLM 能够从**空间相邻区域**获取上下文信息。
- 仅微调轻量级组件（如 attention 层参数），冻结主干 LLM 参数，实现高效适配。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **空间建模** | 显式建模区域间空间依赖关系，优于孤立预测的 LLM 方法 |
| **可扩展性** | 区域聚类 + 轻量微调策略降低计算开销，适合大规模部署 |
| **准确性** | 在多个真实场景下显著优于 SOTA 模型，尤其在高动态区域表现更优 |
| **鲁棒性** | 对不同交通强度和分布偏移具有良好的泛化能力 |

---

## 2. 核心实验方法和设置

### 数据集
- **来源**：中国某运营商的真实城市级 4G 基站网络数据。
- **规模**：约 19,000 个扇区，覆盖济南市四个完整星期（2024年7月28日至8月25日）。
- **采样频率**：每 15 分钟一条记录。
- **预处理**：将扇区级流量聚合为区域级，并通过聚类划分为 **4 个 Zone（A、B、C、D）**。

### 实验设置
- **输入窗口 H**：过去一天（96 个时间步，即 24 小时）
- **预测窗口 P**：未来 1 小时（4 步，即 60 分钟）
- **基础 LLM**：**DeepSeek-R1-Distill-Llama-8B**（32 层，模型维度 16，8 头注意力）
- **训练平台**：4×A100 GPU，使用 DeepSpeed + ZeRO-2 和 bfloat16 混合精度训练
- **优化器**：AdamW + OneCycleLR，学习率 0.001，batch size=16，训练 100 epochs 并启用早停

### 评估指标
- **MAE**（Mean Absolute Error）
- **RMSE**（Root Mean Squared Error）
- **MAPE**（Mean Absolute Percentage Error）

### 基线方法对比
| 类型 | 方法 |
|------|------|
| 线性模型 | DLinear |
| Transformer 变体 | Transformer, Autoformer, Reformer, TimesNet, LightTS |
| LLM-based 方法 | Time-LLM |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table I）

| 方法 \ 指标 | MAE ↓ | RMSE ↓ | MAPE ↓ |
|------------|--------|---------|---------|
| **TIDES (Zone A)** | **0.2193** | **0.2958** | **2.7481%** |
| DLinear (Zone A) | 0.2330 | 0.3133 | 2.7904% |
| **TIDES (Zone B)** | 0.4208 | 0.6459 | **2.4827%** |
| Time-LLM (Zone B) | **0.3567** | **0.5401** | 2.6623% |
| **TIDES (Zone C)** | **0.2657** | **0.3621** | **2.8358%** |
| TimesNet (Zone C) | 0.2782 | 0.3860 | 2.8450% |
| **TIDES (Zone D)** | **0.1356** | **0.1746** | **0.5150%** |
| TimesNet (Zone D) | 0.1371 | 0.1795 | 0.6848% |

> ✅ **TIDES 在 12 项指标中取得 10 项第一，全部 4 个 Zone 的 MAPE 均排名第一**

### 与基线方法的对比结果
- **平均误差降低**：相比最佳基线（DLinear 或 Time-LLM），TIDES 在 MAE 上平均提升约 **8–15%**。
- **相对误差优势明显**：在 MAPE 上全面领先，说明其在低流量或波动剧烈区域更具稳定性。
- **可视化验证**：
  - **图3（Correlation）**：TIDES 的 Pearson 相关系数达 **0.973**，远高于 Time-LLM（0.792）和 DLinear（0.959），表明预测值与真实值高度对齐。
  - **图4（Error Distribution）**：TIDES 的误差分布最集中于零附近，方差最小，表现出更强的鲁棒性和一致性。
  - **图5（Spatial-Temporal View）**：TIDES 更好地还原了城市空间中的高低流量分布格局，在市中心、郊区等不同类型区域均表现优异。
  - **图6（Temporal Prediction）**：能准确捕捉高峰突增和低谷回落，相位延迟小，细节保留更好。

### 泛化能力测试（Table II）
| 迁移场景 | MAE | 相对提升 |
|--------|-----|----------|
| Zone A → Zone A（同区） | 0.2255 | - |
| Zone B → Zone A（跨区） | 0.2595 | ↓15.0% |
| Zone C → Zone A | 0.2456 | ↓8.9% |
| Zone D → Zone A | 0.2328 | ↓3.2% |

> 🔍 表明 TIDES 具备良好跨区域泛化能力，即使不重新训练也能适应新区域，性能下降控制在 10% 以内。

---

## 4. 关键结论和发现

### 主要发现
1. **空间依赖是提升预测精度的关键因素**：单纯的时间序列建模无法满足城市级智能网络管理需求，必须显式建模区域间的空间交互。
2. **LLM 可以被有效用于非语言任务**：通过 prompt engineering 将数值流量转化为结构化文本，充分发挥 LLM 的语义理解和推理能力。
3. **轻量化微调即可实现高效迁移**：冻结主干 LLM 参数，仅微调 spatial attention 和 domain alignment 模块，既保证性能又避免高昂训练成本。
4. **TIDES 具备强鲁棒性和可扩展性**：在多种城市区域类型（中心、边缘、郊区）中均表现稳定，适用于未来 6G 网络的大规模自优化系统。

### 方法的局限性
- 当前依赖地理坐标构建空间图，若缺乏位置信息则需依赖流量相似性替代，可能影响拓扑准确性。
- 虽然采用轻量微调，但整体仍需高性能 GPU 支持，边缘设备部署仍有挑战。
- 未考虑外部事件（如大型活动、天气）对流量的突发影响，未来可引入多模态输入增强预测能力。

### 未来工作方向
1. 引入实时反馈机制，支持在线增量学习与动态调整；
2. 扩展至多模态输入（天气、节假日、POI 等）；
3. 探索跨城市甚至全球范围的通用流量预测模型；
4. 将 TIDES 集成到实际网络控制系统中，实现意图驱动的资源调度与节能优化。

---

> 📌 **总结一句话**：  
> TIDES 成功将 LLM 的强大建模能力与无线网络的空间特性相结合，提出了一种高效、精准、可扩展的城市级无线流量预测新范式，为实现 AI-Native 的 6G 自治网络提供了关键技术支撑。

</details>

---

### 12. [HLS4PC: A Parametrizable Framework For Accelerating Point-Based 3D Point Cloud Models on FPGA](https://arxiv.org/abs/2512.22139)

**Authors**: Amur Saqib Pal, Muhammad Mohsin Ghaffar, Faisal Shafait, Christian Weis, Norbert Wehn  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22139v1  

#### Abstract
Point-based 3D point cloud models employ computation and memory intensive mapping functions alongside NN layers for classification/segmentation, and are executed on server-grade GPUs. The sparse, and unstructured nature of 3D point cloud data leads to high memory and computational demand, hindering ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*HLS4PC: A Parametrizable Framework For Accelerating Point-Based 3D Point Cloud Models on FPGA*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **3D点云模型在GPU上执行效率低下**：由于点云数据具有稀疏性和非结构化特性，传统的GPU在处理如Farthest Point Sampling (FPS)、K-Nearest Neighbor (KNN)等映射函数时存在严重的资源利用率不足问题。
- **现有FPGA框架缺乏对点云专用操作的支持**：尽管已有多种基于FPGA的DNN加速框架（如hls4ml、fpgaConvNet），但它们无法有效支持点云特有的预处理和特征提取模块。

### 🚀 提出的新方法与创新
- **提出HLS4PC框架**：
  - 一个可参数化的、基于High Level Synthesis (HLS) 的流式数据流（streaming-based dataflow）架构，专为FPGA上的点云模型加速设计。
  - 支持混合精度（mixed-precision）实现，并能灵活配置并行度、位宽等硬件参数以适应不同应用场景。
- **构建HLS4PC Library**：
  - 包含针对点云的关键功能模块：`FPS`, `URS`, `KNN`, `Sorting`, `CNN`, `MLP`, `MaxPool`, `Activation Functions` 等，均采用流水线+并行化设计。
- **提出PointMLP-Lite模型**：
  - 在State-of-the-art模型PointMLP-Elite基础上进行多项算法-硬件协同优化，得到轻量化版本：
    - 使用Uniform Random Sampling (URS) 替代FPS
    - 输入点数从1024降至512
    - 权重与激活量化至8-bit（fp8）
    - BatchNorm层融合进卷积层
    - 移除几何归一化参数α和β

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **灵活性** | FPGA支持编译时精度配置和逻辑重构，优于ASIC类专用加速器 |
| **兼容性** | 支持PyTorch/TensorFlow训练流程 + Quantization-Aware Training (QAT) |
| **吞吐量** | 较前人FPGA工作提升3.56×，较GPU提升2.3×，较CPU提升22× |
| **能效** | 能效高达294.5 GOPS/W，是此前最佳工作的57.4× |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **ModelNet40**：主流3D物体分类基准，包含40类CAD模型，用于主实验评估。
- **ScanObjectNN**：真实场景采集的点云数据集，更具挑战性，用于验证泛化能力。

### ⚙️ 实验设置
- **基础模型**：PointMLP-Elite（SOTA点云分类模型）
- **压缩策略探索路径**：
  - 输入点剪枝（1024 → 512 → 256 → 128）
  - FPS → URS替换
  - BN层融合
  - 几何参数α/β移除
  - 8/8-bit量化（权值与激活）
- **训练环境**：
  - 平台：NVIDIA RTX 3090 GPU
  - 框架：PyTorch 2.4.0 + Brevitas（用于QAT）
  - 训练配置：batch size=256，epoch=1000，SGD优化器（momentum=0.8）
- **部署平台**：
  - FPGA开发板：Xilinx Zynq-7000 ZC706
  - 工具链：Vivado HLS 2018.3 + Block Design
  - 功耗测量：Voltacraft VC-870插座功率计

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Overall Accuracy (OA)** | 所有测试样本平均准确率 |
| **Mean Accuracy (mA)** | 各类别准确率的均值 |
| **Throughput (GOPS)** | 每秒十亿次操作数（Giga Operations Per Second） |
| **Energy Efficiency (GOPS/W)** | 单位功耗下的计算性能 |
| **Resource Utilization** | FPGA中FF、LUT、DSP、BRAM、URAM占用情况 |
| **Model Size (MB)** | 模型存储大小，反映复杂度 |

### 🔁 基线对比方法
- **FPGA基线**：
  - SOCC 2022 [14]：ISSCN on ZCU102
  - ISCAS 2020 [1]：PointNet on ZCU104
  - CSSP 2023 [3]：DGCNN (FP32) on Ultrascale+
  - ASICON 2019 [18]：O-PointNet on ZC706
- **通用平台对比**：
  - GPU：Tesla V100 / RTX 3060 Ti 上运行原始PointMLP-Elite及Lite版
  - CPU：Intel i5-13400 运行Lite版

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自Table 2 & Table 3）

| 指标 | HLS4PC (This Work) |
|------|---------------------|
| **Throughput** | **1648 GOPS** |
| **Energy Efficiency** | **294.5 GOPS/W** |
| **Power Consumption** | 2.2 W |
| **Operating Frequency** | 100 MHz |
| **FPGA Platform** | ZC706 (XC7Z045) |
| **Precision** | fp8 (8-bit fixed-point) |
| **Model** | PointMLP-Lite |

### 🔁 与先前FPGA工作的对比（Table 2）
| 对比项 | 提升倍数 |
|--------|----------|
| **Throughput** | **3.56× 高于SOCC 2022** |
| **Energy Efficiency** | **57.4× 更高** |
| **原因分析** | 算法-硬件协同优化（URS替代FPS、BN融合、量化）、高效数据流架构、避免高精度冗余（如FP32） |

### 💻 与其他平台对比（Table 3）
| 平台 | Throughput (SPS) | 相对于Baseline GPU提升 |
|------|------------------|-------------------------|
| Tesla V100 (Baseline) | 176 SPS | ×1.0 |
| RTX 3060 Ti (PointMLP-Elite) | 187 SPS | ~1.06× |
| RTX 3060 Ti (PointMLP-Lite) | 421 SPS | **2.4×** |
| Intel i5-13400 (CPU) | 45 SPS | — |
| **ZC706 (FPGA, This Work)** | **990 SPS** | **5.6×** |

> 注：SPS = Samples Per Second

### 🔍 消融实验结果（Table 1 & Fig. 4）
通过逐步应用压缩技术，观察准确率与模型规模之间的权衡：

| 模型变体 | 输入点数 | 采样方式 | BN融合 | ModelNet40 OA (%) | 模型大小 |
|--------|-----------|------------|--------|--------------------|----------|
| PointMLP-Elite | 1024 | FPS | 否 | 93.60 | 原始大小 |
| M-1 | 1024 | URS | 否 | 92.30 | ↓ |
| **M-2** | **512** | **URS** | **否** | **91.69** | **↓50%** |
| M-3 | 512 | URS | 是 | 90.56 | ↓ |
| M-4 | 128 | URS | 是 | 86.87 | ↓75% |

- **关键发现**：
  - 将输入点减半（1024→512）仅导致约2%的准确率下降（M-2），同时显著降低内存开销。
  - 结合8-bit量化后，最终模型 **PointMLP-Lite** 实现 **4×更低复杂度**，且保持接近原模型精度。
  - Pareto前沿图显示，8/8-bit量化版本在准确率几乎不变的情况下达到最优性价比。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **HLS4PC成功填补了FPGA在点云模型加速中的空白**：
   - 首个支持完整点云处理流水线（含FPS/URS/KNN等映射函数）的可参数化HLS框架。
2. **PointMLP-Lite实现了高效的算法-硬件协同优化**：
   - 通过URS替代FPS、输入剪枝、BN融合、量化等手段，在仅损失~2%精度的前提下将模型复杂度降低4倍。
3. **FPGA在点云推理中展现出巨大潜力**：
   - 相比GPU和CPU，FPGA在吞吐量和能效方面具有显著优势，尤其适合边缘端实时安全关键应用（如自动驾驶）。

### ⚠️ 局限性
- **使用URS引入随机性**：虽然通过增大batch size和训练轮次缓解，但仍可能影响某些对局部结构敏感的任务表现。
- **当前未充分利用高端FPGA资源**：ZC706属于中低端平台，若迁移到UltraScale+或Versal系列有望进一步提升性能。
- **缺乏动态可重构性演示**：虽强调FPGA灵活性，但未展示多模型切换或多任务调度能力。

### 🔮 未来工作方向
- 探索更结构化的采样策略替代URS，例如基于**Hilbert Curve的采样**，以减少因完全随机带来的精度损失。
- 扩展框架支持**分割任务**（Segmentation）而不仅是分类。
- 引入**自适应精度调节机制**，根据不同网络层需求动态分配bit-width。
- 支持更多新型点云模型（如PointTransformer、PV-RCNN）的自动化部署。

---

> 🔗 **代码开源地址**：[https://github.com/dll-ncai/HLS4PC](https://github.com/dll-ncai/HLS4PC)

</details>

---

### 13. [Role-Based Fault Tolerance System for LLM RL Post-Training](https://arxiv.org/abs/2512.22492)

**Authors**: Zhenqian Chen, Baoquan Zhong, Xiang Li, Qing Dai, Xinkui Zhao, Miao Ye, Ren Cheng, Lufei Zhang, Jianwei Yin  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22492v1  

#### Abstract
RL post-training for LLMs has been widely scaled to enhance reasoning and tool-using capabilities. However, RL post-training interleaves training and inference workloads, exposing the system to faults from both sides. Existing fault tolerance frameworks for LLMs target either training or inference, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Role-Based Fault Tolerance System for LLM RL Post-Training

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在强化学习（Reinforcement Learning, RL）后训练过程中，**同时涉及训练（training）和推理（rollout）负载**，导致系统面临来自两方面的硬件故障风险。现有的容错框架（如 ByteRobust）通常只针对纯训练或纯推理场景设计，在 RL 场景中一旦发生单个机器故障，往往需要**重启整个 RL 任务**，造成严重的计算资源浪费和训练进度丢失。

此外，传统检测机制存在两个挑战：
- **Rank-level detection** 容易将 rollout 阶段等待工具响应时的 GPU 空闲误判为故障（false positive）；
- **Cluster-level detection** 则因需等待所有节点无活动才触发，导致故障发现延迟严重。

### 提出的新方法与思路
作者提出了 **RobustRL** ——首个面向 LLM RL 后训练的**基于角色的容错系统**，其核心思想是：

> 将 RL 中的不同组件（trainer、rollout、管理角色等）视为独立的分布式子任务，实现**角色级故障隔离**。当某一角色失败时，仅恢复该角色并重新连接到仍在运行的角色，避免全局重启。

该系统采用 **Detect-Restart-Reconnect 范式**，具体创新如下：

#### （1）Role- and Phase-Aware Fault Detection（精准故障检测）
- 对 trainer 和 rollout 分别设计行为感知的监控策略：
  - **Trainer**：监测 TensorCore 活动，若连续 5 分钟无活动则判定为故障（适用于前向/反向传播密集型任务）。
  - **Rollout**：结合吞吐量（TPS）、心跳检查与 GPU 活动，避免因等待外部工具调用而产生的误报。

#### （2）Role-Based and Resource-Efficient Restart（高效细粒度重启）
- **Trainer 故障恢复**：
  - 实现非中断式恢复：rollout 继续生成轨迹，trainer 快速从 checkpoint 恢复。
  - 引入“warm standby”机制：利用空闲 rollout 机器作为 warm standby 替代冗余备用机，节省资源开销。
- **Rollout 故障恢复**：
  - 单独替换故障 rollout 机器，不影响整体训练流程。
  - 支持快速拉取最新模型权重继续生成。

#### （3）Dynamic Reconnection via UCX（动态通信重连）
- 使用 **UCX（Unified Communication X）** 替代传统的 NCCL 集体通信。
- 支持点对点异步通信，允许故障恢复后的角色动态加入通信组，实现即时权重同步。
- 设计 relay server 机制：已更新权重的 rollout 可作为中继服务器，加速其他副本的同步速度。

### 相比现有方法的优势
| 方面 | ByteRobust / 传统方案 | RobustRL |
|------|------------------------|----------|
| 容错粒度 | 全局任务重启 | 角色级隔离与恢复 |
| Rollout 进度保留 | ❌ 丢失 | ✅ 保持 |
| Trainer 恢复效率 | 依赖 warm standby（资源浪费） | 动态借用 rollout 机器（资源高效） |
| 权重同步 | NCCL（静态通信组，不支持动态接入） | UCX + Relay（支持动态重连） |
| 故障检测准确性 | 易误判 rollout idle 为故障 | 相位感知，减少 false positive |

---

## 2. 核心实验方法和设置

### 数据集
- **数学推理任务**：`DAPO-Math17K`，使用规则奖励函数评分。
- **软件工程任务（Tool Use）**：`SWE-bench`，通过多轮与代码沙箱交互生成修复轨迹。
- **问答任务**：`HotPotQA`，用于分析 rollout 时间分布。

### 实验设置
- **模型**：Qwen3-8B、Qwen3-32B、Qwen3-235B-A22B
- **训练架构**：Sync、Semi-sync、Async 三种模式均测试
- **硬件平台**：32 台 GPU 服务器，共 256× NVIDIA H20 96GB GPUs
  - NVLink 内部带宽：900 GB/s
  - 网络：4×200Gbps NICs
- **软件栈**：
  - CUDA 12.4, PyTorch 2.4.1, NCCL 2.21.5
  - 基于 verl 框架构建，集成 vLLM（推理）、PyTorch FSDP2（训练）
  - Checkpoint 使用 ByteCheckpoint + HDFS 存储

### 评估指标
- **End-to-End Training Time**：总训练耗时
- **ETTR (Effective Training Time Ratio)**：
  $$
  \text{ETTR} = \frac{\text{#Rollout} + \text{#Trainer}}{\text{Total Time}}
  $$
  衡量实际用于有效计算的时间占比。
- **Restart Overhead**：故障恢复所需时间分解
- **Weight Sync Latency**：模型权重同步延迟
- **Checkpoint Overhead**：每步保存 checkpoint 的阻塞时间

### 基线方法对比
- **Baseline**：无故障注入的标准训练
- **ByteRobust**：当前主流 LLM 容错系统，发生故障即重启整个任务
- **RobustRL**：本文提出的方法

> 注：未与其他开源 RL 容错系统比较，因其代码未公开；且 RobustRL 是首个完整覆盖 trainer 和 rollout 的系统。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）端到端训练时间 vs ETTR（图11）
| 任务 | 方法 | 训练时间（小时） | ETTR |
|------|------|------------------|-------|
| Qwen3-8B-Math | Baseline | ~5.0 | 100% |
|                | ByteRobust | ~7.0–8.0 | ~60% |
|                | **RobustRL** | **~5.8–6.2** | **>80%** |
| Qwen3-32B-SWE | ByteRobust | 显著增加 | ~60% |
|               | **RobustRL** | **减少 3.5–4.5h** | **>80%** |

> 在每 10 步注入一次 trainer 故障的极端条件下，RobustRL 的 ETTR 达到 **80%+**，相比 ByteRobust 提升 **20个百分点**。

#### （2）训练时间加速
- 在 Qwen3-8B-Math 上，**端到端训练时间缩短 8.4%–17.4%**
- 在复杂任务（SWE-bench）上收益更大，因 rollout 时间更长，保留进度的价值更高

#### （3）滑动 ETTR 对比（图12）
- 在故障发生期间（0.8–2h），ByteRobust 的 ETTR 下降至 **20%**
- RobustRL 因 rollout 持续运行，ETTR 保持在 **60%以上**
- **提升幅度达 18–24%**

#### （4）重启开销对比（图14）
| 模型 | 方法 | 总重启延迟 | 主要瓶颈 |
|------|------|------------|-----------|
| 8B | ByteRobust | ~277.8s | 实例重启 + Rollout 初始化 |
|    | **RobustRL** | **~173.0s** | ✅ 节省 ~38% 时间 |
| 32B | ByteRobust | ~305.5s | Gang scheduling 开销大 |
|     | **RobustRL** | **~182.6s** | ✅ 加速约 1.7× |

> RobustRL 通过跳过 rollout 初始化和容器启动阶段，显著降低恢复延迟。

#### （5）权重同步效率（图17–18）
- UCX-based 同步接近理论极限（235B 模型传输理论值 4.7s，实测 ~6s）
- 支持 relay 扩展，随着 rollout 数量增长仍保持线性扩展性
- NCCL 因广播机制，在 rollout 多于 trainer 时性能下降明显

#### （6）Checkpoint 开销（图19）
- GPU → Memory 阻塞时间 < 5 秒
- Memory → Disk 异步执行，不影响训练流水线
- 占单步训练时间比例 < 1%，可忽略

---

## 4. 关键结论和发现

### 主要发现
1. **角色隔离是 RL 容错的关键**：将 trainer 与 rollout 视为独立角色进行故障处理，能有效防止“牵一发而动全身”的全局重启。
2. **rollout 进度值得保留**：尤其在长尾 rollout 场景（如工具调用、搜索）中，避免重复生成可大幅节约时间。
3. **warm standby 可资源化**：利用现有 rollout 机器作为 trainer 的 warm standby，既保证恢复速度又避免额外资源占用。
4. **UCX 是动态通信的理想选择**：相较于 NCCL 的静态通信组限制，UCX 支持灵活的点对点连接，更适合异步 RL 架构下的容错需求。
5. **per-step checkpoint 可行且必要**：尽管频率高，但由于单步 RL 时间长达数分钟至小时级，checkpoint 开销极小，且能确保状态一致性。

### 局限性
- **诊断能力有限**：当前系统侧重于自动恢复，但缺乏对根因的深入定位能力（如区分硬件错误与代码 bug）。
- **未考虑 silent data corruption 或 straggler 问题**：虽然架构支持扩展，但尚未集成相关检测模块。
- **依赖 homogeneous 硬件环境**：warm standby 机制要求 trainer 与 rollout 机器同构。

### 未来工作方向
- **构建 RL 特定的诊断工具链**：结合控制流与数据依赖分析，实现更精确的故障归因。
- **支持角色级热更新（hot update）**：在不停止训练的前提下调整 batch size、learning rate 等参数。
- **弹性 RL 训练（elastic training）**：基于 RobustRL 的恢复机制，支持 trainer 在 data parallelism 维度动态扩缩容。
- **集成更多高级容错技术**：如 SDC 检测、straggler 缓解、混合精度校验等。

---

> ✅ **总结一句话**：  
> RobustRL 通过 **角色隔离 + 动态重连 + rollout 温备**，首次实现了高效的 RL 后训练容错，在极端故障场景下将 ETTR 提升至 80%+，较 ByteRobust 加速 8.4%–17.4%，为大规模 LLM 强化学习提供了可靠基础设施支撑。

</details>

---

### 14. [Graph Attention-based Adaptive Transfer Learning for Link Prediction](https://arxiv.org/abs/2512.22252)

**Authors**: Huashen Lu, Wensheng Gan, Guoting Chen, Zhichao Huang, Philip S. Yu  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22252v1  

#### Abstract
Graph neural networks (GNNs) have brought revolutionary advancements to the field of link prediction (LP), providing powerful tools for mining potential relationships in graphs. However, existing methods face challenges when dealing with large-scale sparse graphs and the need for a high degree of al...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Graph Attention-based Adaptive Transfer Learning for Link Prediction

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对图神经网络（GNN）在**大规模稀疏图上的链接预测**（Link Prediction, LP）任务中面临的三大挑战：
- **数据稀疏性与拓扑错配**：不同图之间的结构、密度和特征分布差异大，导致预训练模型难以有效迁移到目标图；
- **参数不平衡与过拟合风险**：在小规模目标图上微调大型预训练模型容易造成过拟合且计算成本高；
- **长距离依赖建模困难**：传统GNN和Transformer依赖局部聚合或序列位置编码，难以捕捉跨域的远距离节点关系。

此外，现有方法大多忽视了**transfer learning 在跨图 link prediction 中的泛化潜力**。

---

### 提出的新方法：GAATNet
作者提出了一种名为 **Graph Attention Adaptive Transfer Network (GAATNet)** 的新型框架，结合了预训练-微调策略与图注意力机制，实现高效的知识迁移。

#### 核心创新点：
1. **引入远邻嵌入作为注意力偏置（Distant Neighbor Bias in Self-Attention）**
   - 在自注意力模块中，将来自 $g$-hop ($g \geq 2$) 远邻居的嵌入信息作为**注意力矩阵的偏置项**，增强对全局结构和长程依赖的感知能力。
   - 有助于缓解稀疏图中的“冷启动”问题，并提升模型对复杂拓扑的建模能力。

2. **轻量级自适配器模块（Lightweight Self-Adapter Module）用于微调**
   - 在微调阶段冻结主干网络参数，仅训练一个插入式的**self-adapter 模块**（含低维瓶颈结构和残差连接），显著减少可训练参数数量。
   - 实现高效的参数适应，防止过拟合，加快收敛速度。

3. **两阶段自适应迁移学习架构**
   - **预训练阶段**：在大规模源图上学习丰富的全局结构先验；
   - **微调阶段**：通过 self-adapter 快速适配到小规模目标图，兼顾性能与效率。

---

### 相比现有方法的优势
| 维度 | GAATNet优势 |
|------|------------|
| **性能** | 显著优于各类基线方法，在多个数据集上达到 SOTA 表现 |
| **效率** | 微调阶段参数量大幅降低，训练时间缩短（平均减少一半以上） |
| **泛化性** | 能有效处理不同规模、密度和领域的图数据，具备良好跨域迁移能力 |
| **鲁棒性** | 利用 contrastive loss 和 diffusion augmentation 抗噪声和稀疏干扰 |

---

## 2. 核心实验方法和设置

### 使用的数据集
共使用 **7个公开图数据集**，涵盖多种类型与规模：

| 数据集 | 类型 | 节点数 | 边数 | 规模分类 |
|--------|------|--------|-------|----------|
| Cora / Citeseer | 引用网络 | ~2.7K–3.3K | ~4.7K–5.4K | 小型 |
| Facebook / ChCh-Miner | 社交/生物医学 | ~4K–1.5K | ~4.8K–8.8K | 中型 |
| Coauthor-CS / Computers / Email-Enron | 合作/购买/通信 | ~1.3K–1.8K | ~137K–491K | 大型 |

> 注：所有数据集均使用 node2vec 初始化节点嵌入（256维）。

---

### 实验设置与评估指标

#### 评估任务
- **链接预测**（Link Prediction）：判断两个节点之间是否存在潜在边。
- 采用标准的 **8:1:1** 数据划分（训练/验证/测试），正负样本平衡采样。

#### 评估指标
- **AUC**（Area Under ROC Curve）
- **F1 Score**
- 在非平衡场景下额外报告 **Average Precision (AP)**

#### 基线方法对比
分为三类进行比较：
1. **经典方法**：GCN, GAT
2. **子图/显式特征方法**：SEAL, SIEG, HL-GNN
3. **迁移/对比学习方法**：AdaGCN, LGCL, LPFormer, Graph2Feat

> 所有方法统一输入（node2vec嵌入）、相同划分、公平超参对齐。

---

## 3. 主要实验结果和性能指标

### 性能对比（平衡设置，200 epochs）

| 方法 | Cora (AUC/F1) | Facebook (AUC/F1) | Computers (AUC/F1) | Email-Enron (AUC/F1) |
|------|----------------|--------------------|---------------------|------------------------|
| GCN | 68.36 / 58.23 | 65.37 / 59.35 | 61.36 / 55.38 | 65.23 / 60.47 |
| GAT | 83.78 / 78.42 | 82.18 / 77.53 | 82.78 / 73.37 | 79.93 / 74.15 |
| SEAL | 88.71 / 81.34 | 86.47 / 80.45 | 83.87 / 79.33 | 84.49 / 78.59 |
| LPFormer | 92.82 / 84.05 | 92.07 / 89.74 | 90.09 / 84.41 | 92.33 / 85.10 |
| **GAATNet** | **94.27 / 86.85** | **96.37 / 92.27** | **95.05 / 89.86** | **95.48 / 90.27** |

✅ **关键发现**：
- 在所有7个数据集上，GAATNet 均取得 **最优性能**（AUC 和 F1 双领先）；
- 在大型稀疏图（如 Computers 和 Email-Enron）上提升尤为明显（AUC 提升约 2.8%-3.2%）；
- 即使在小型图上也保持稳定优势，说明其良好的泛化能力。

---

### 非平衡设置下的表现（Negative:Positive = 10:1 和 20:1）

| 方法 | Cora (10:1 AUC/AP) | Facebook (10:1 AUC/AP) | ChCh-Miner (10:1 AUC/AP) |
|------|---------------------|-------------------------|----------------------------|
| LPFormer | 88.76 / 85.37 | 88.07 / 88.34 | 89.69 / 88.46 |
| **GAATNet** | **88.23 / 89.17** | **90.03 / 91.44** | **91.83 / 91.13** |

✅ **结论**：
- GAATNet 在高度不平衡场景下仍表现出色，尤其在 AP 指标上显著领先；
- 表明其能更有效地识别稀有的正样本，具有更强的鲁棒性和判别能力。

---

### 消融实验结果（Ablation Study）

设计了以下变体以验证各组件有效性：
- **NonAug**：去除 diffusion 数据增强
- **NonFT**：移除预训练中的 feature transformer
- **NonSA**：移除微调中的 self-adapter
- **NonCon**：移除 contrastive loss
- **NonAtt**：替换注意力为 mean pooling

#### 结果分析（见 Fig. 6）：
| 组件 | 影响程度 | 关键观察 |
|------|----------|-----------|
| **NonAtt** | ❌ 最严重下降 | 证明 attention 机制对性能至关重要 |
| **NonAug** | ⚠️ 显著下降 | diffusion augmentation 对稀疏图尤为重要 |
| **NonCon** | ⚠️ 下降约 1.15% AUC | contrastive loss 提升表示区分度 |
| **NonSA** | ⛔ 中等影响 | 在大图上影响更大，体现 self-adapter 的必要性 |
| **NonFT** | ⚠️ 小图影响小，大图影响大 | feature transformer 更利于捕获复杂结构 |

✅ **最终完整模型 GAATNet 表现最佳**，表明所有组件协同作用。

---

### 效率分析

#### 参数与内存消耗（Cora 数据集）
| 方法 | Trainable Parameters (k) | Memory (MB) |
|------|---------------------------|-------------|
| SEAL | 2300 | 1062 |
| LPFormer | 986 | 962 |
| **GAATNet (pre-train)** | **208** | **271** |
| **GAATNet (fine-tune)** | **44** | **57** |

✅ **优势**：
- 微调阶段参数仅为 **44k**，是次优方法的 1/10~1/20；
- 内存占用最低，适合资源受限部署。

#### 训练时间（每 epoch 秒数）
| 方法 | Computers (秒) | GAATNet (pre-train/fine-tune) |
|------|------------------|------------------------------|
| SEAL | 2271 | — |
| LPFormer | 174.9 | — |
| **GAATNet_p** | — | **16.32** |
| **GAATNet_f** | — | **7.29** ✅ |

✅ 微调阶段训练时间**减少超过50%**，效率极高。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **GAATNet 是首个将 distant neighbor embedding 作为 attention bias 的 link prediction 框架**，有效增强了对全局结构的理解。
2. ✅ **self-adapter 模块极大提升了微调效率**，实现了参数高效迁移，避免了全模型微调带来的过拟合和高昂成本。
3. ✅ **两阶段 pre-train + fine-tune 架构显著提升跨图迁移性能**，尤其适用于从小样本目标图中挖掘潜在连接。
4. ✅ 在 **7个真实世界数据集上全面超越 SOTA 方法**，无论是在 AUC、F1 还是 AP 指标上均表现最优。
5. ✅ 方法在**非平衡、稀疏、跨领域场景下依然稳健**，展现出强大的泛化能力和实用性。

---

### 局限性
1. 当前框架主要面向**静态同质图**，尚未扩展至动态图或时空图；
2. 虽然支持异构图初步实验（Last.FM, Baby），但在多类型节点/边上仍有优化空间；
3. diffusion augmentation 和 g-hop neighbor 提取带来一定计算开销，可能限制极端大规模图的应用；
4. 预训练需依赖大规模图，若无合适源图则效果受限。

---

### 未来工作方向
1. 扩展至 **dynamic graphs、spatio-temporal graphs 和 heterogeneous graphs**；
2. 设计更轻量化的预训练网络，便于边缘设备部署；
3. 探索更高效的图增强方法（如基于 LLM 的语义增强）；
4. 结合 **large language models、knowledge distillation 或 reinforcement learning** 提升表示质量；
5. 研究无监督或弱监督迁移策略，降低对标注数据的依赖。

---

> 🔗 **代码与数据开源地址**：[https://github.com/DSI-Lab1/GAATNet](https://github.com/DSI-Lab1/GAATNet)

</details>

---

### 15. [Federated Multi-Task Clustering](https://arxiv.org/abs/2512.22897)

**Authors**: S. Dai, G. Sun, F. Li, X. Tang, Q. Wang, Y. Cong  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.22897v1  

#### Abstract
Spectral clustering has emerged as one of the most effective clustering algorithms due to its superior performance. However, most existing models are designed for centralized settings, rendering them inapplicable in modern decentralized environments. Moreover, current federated learning approaches o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Federated Multi-Task Clustering》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**联邦聚类学习**（Federated Clustering）中的两个关键挑战提出解决方案：
- **Intra-client Correlation（客户端内相关性）**：传统方法依赖不可靠的伪标签（pseudo-labels）进行训练，且难以泛化到未见样本（out-of-sample inference）。
- **Inter-client Correlation（客户端间相关性）**：现有联邦方法通常忽略异构客户端之间的潜在共享结构（如语义共性），仅学习独立模型或简单平均参数，导致性能下降。

### 提出的新方法与思路
作者提出了 **Federated Multi-Task Clustering (FMTC)** 框架，其核心思想是将每个客户端视为一个相关但个性化的聚类任务，在保护隐私的前提下实现个性化建模与全局知识共享的平衡。

#### 创新模块设计：
- **Client-Side Personalized Clustering Module（客户端个性化聚类模块）**
  - 将谱嵌入学习（spectral embedding）与可学习的映射函数 $W_t$ 联合优化。
  - 形式化为：$\min \text{Tr}(F_t^T L_t F_t) + \alpha \|F_t - X_t W_t\|^2$
  - 优势：无需生成伪标签；支持对新样本直接推理（out-of-sample inference）。

- **Server-Side Tensorial Correlation Module（服务器端张量关联模块）**
  - 将所有客户端的投影矩阵 $\{W_t\}$ 组织成一个三阶张量 $W \in \mathbb{R}^{d \times k \times m}$。
  - 施加低秩正则化（low-rank regularization）以提取跨任务的高阶共享知识。
  - 通过 Schatten p-norm 正则项捕获多客户端间的深层语义关系。

#### 联合优化策略
采用 **ADMM（Alternating Direction Method of Multipliers）** 构建分布式优化算法：
- 引入全局共识变量 $Z$，解耦耦合约束。
- 客户端并行更新本地模型 $(F_t, W_t)$，服务器聚合后更新 $Z$ 并广播回客户端。
- 整个过程不交换原始数据，保障隐私。

### 相比现有方法的优势
| 方面 | 现有方法局限 | FMTC 改进 |
|------|---------------|-----------|
| 泛化能力 | 非参数谱聚类无法处理新样本 | 参数化映射 $W_t$ 支持实时推理 |
| 伪标签依赖 | “两阶段”方法误差传播严重 | 联合优化避免伪标签使用 |
| 多任务建模 | 忽视客户端间潜在结构 | 张量低秩正则显式捕捉共享知识 |
| 个性化与协作平衡 | 单一全局模型或完全独立模型 | 个性化模型 + 结构引导协同 |

---

## 2. 核心实验方法和设置

### 使用的数据集
在 **7 个真实世界基准数据集** 上进行了全面评估：
- 文本数据：`WebKB4`, `20NewsGroups`, `Reuters`, `BBC News`
- 图像数据：`CORe50`, `YALE`
- 生物医学数据：`Keck`

这些数据覆盖多种模态（稀疏文本、密集图像向量），验证方法通用性。

### 实验设置
- **联邦环境模拟**：采用 **Non-IID 分割策略**，每个自然来源作为一个客户端（如 WebKB 中每所大学为一个 client）。
- **特征空间一致**：假设所有客户端共享相同特征维度（cross-silo setting）。
- **通信轮次**：默认运行 50 轮 ADMM 迭代。

### 评估指标
使用三个标准聚类评价指标（均取百分比形式）：
- **ACC（Accuracy）**：聚类准确率（通过匈牙利算法匹配标签）
- **NMI（Normalized Mutual Information）**
- **RI（Rand Index）**

最终结果基于 10 次 k-means 初始化取最优值。

### 基线方法对比
分为三类进行比较：

| 类别 | 方法 | 特点 |
|------|------|------|
| **单任务基线** | stSC, uSC | 各自训练 / 数据合并训练 |
| **集中式多任务方法** | MBC, SMBC, SMKC, MTSC | 可访问全部数据，作为性能上限参考 |
| **联邦聚类方法** | Fed-kmeans, FedSpectral+, SFOMVC | 当前主流联邦聚类方法 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table I）
以下为部分代表性结果（单位：%）：

| Dataset       | 方法         | ACC     | NMI     | RI      |
|---------------|--------------|---------|---------|---------|
| **WebKB4**    | stSC          | 63.37   | 21.44   | 62.45   |
|               | uSC           | 63.76   | 20.75   | 62.56   |
|               | MTSC          | 76.86   | 45.72   | 67.42   |
|               | **Ours (FMTC)** | **78.11** | **46.04** | **74.28** |
| **20News**    | stSC          | 56.18   | 24.14   | 64.41   |
|               | uSC           | 39.15   | 23.71   | 59.76   |
|               | MTSC          | 72.32   | 65.90   | 80.73   |
|               | **Ours (FMTC)** | **71.30** | **67.83** | **81.87** |
| **Reuters**   | stSC          | 85.17   | 66.54   | 81.87   |
|               | MTSC          | 77.89   | 56.09   | 73.00   |
|               | **Ours (FMTC)** | **89.40** | **73.85** | **86.23** |

> ✅ FMTC 在多数数据集上显著优于所有基线，尤其在异构性强的 `20News` 和 `Reuters` 上表现突出。

### 与基线方法的对比结果
- **vs. 单任务方法**：
  - 在 `WebKB4` 上 ACC 提升 **14.35%**，`20News` 上提升 **15.12%**。
  - 表明利用跨任务相关性有效增强性能。

- **vs. 集中式多任务方法**：
  - 尽管无全局数据访问权限，FMTC 仍超越多个集中式方法（如 MBC、SMBC）。
  - 在 `Reuters` 上 NMI 达 **73.85%**，远超 MTSC 的 56.09%，说明张量正则更有效地捕捉高阶结构。

- **vs. 联邦方法**：
  - 在 `20News` 上，Fed-kmeans 和 FedSpectral+ 的 ACC 分别仅为 **30.35%** 和 **27.10%**，而 FMTC 达到 **71.30%**。
  - 显示传统联邦方法因简单平均导致“模型坍塌”，而 FMTC 成功保留个性化结构。

### 消融实验与分析（Fig. 5 & Table II）

#### 超参数敏感性分析（Fig. 5）
- 性能随协作权重 $\beta$ 和保真度权重 $\alpha$ 变化呈现非凸地形。
- 当 $\beta \to 0$（即关闭服务器端张量模块）时性能明显下降，验证了**tensorial correlation module 的必要性**。

#### Out-of-Sample 泛化能力（Table II & Fig. 6）
| 方法 | WebKB4 (IS→OOS ACC) | 20News (IS→OOS ACC) | Reuters (IS→OOS ACC) |
|------|---------------------|----------------------|------------------------|
| FedSpectral+ | 72.32 → 30.35 | 70.15 → 21.58 | 76.50 → 37.10 |
| **Ours (FMTC)** | **78.11 → 64.67** | **71.30 → 79.16** | **89.40 → 85.27** |

> 🔍 FMTC 展现出极小的泛化差距（generalization gap），甚至在 `20News` 上 OOS 性能反超 IS，表明其具备卓越的鲁棒性和外推能力。

---

## 4. 关键结论和发现

### 主要发现
1. **个性化与协作可以兼得**：FMTC 成功实现了在保护客户端个性的同时挖掘共享结构，解决了联邦聚类中“一统就死，一分就散”的难题。
2. **张量低秩正则优于矩阵方法**：相比传统矩阵正则（如 MTSC），组织模型为三阶张量并通过 Schatten norm 正则，能更有效地捕捉高阶任务相关性。
3. **联合优化优于两阶段流程**：将谱嵌入与映射函数联合训练，避免了伪标签带来的噪声累积，极大提升了 out-of-sample 推理能力。
4. **ADMM 框架高效稳定**：算法在约 15–20 轮内快速收敛（Fig. 4），适合实际部署。

### 方法的局限性
- **缺乏严格隐私保证**：虽然不传输原始数据，但共享模型参数 $W_t$ 可能面临模型逆向攻击（model inversion attack）。
- **旋转模糊性问题**：学习到的投影矩阵存在旋转不变性，可能影响最终聚类质量（作者在结论中指出此为未来方向）。
- 对极端 Non-IID 或数据量极不平衡场景的鲁棒性有待进一步验证。

### 未来工作方向
- 引入 **Differential Privacy** 或 **Secure Aggregation** 增强隐私保护。
- 设计机制缓解投影矩阵的 **rotational ambiguity**。
- 扩展至 **multi-view** 或 **hierarchical clustering** 场景。
- 探索更高效的张量分解与正则化方式。

---

> 📌 **总结一句话**：  
> FMTC 是首个将 **Multi-Task Learning** 与 **Federated Clustering** 深度融合的框架，通过 **client-server 协同架构 + 张量正则 + 联合优化**，实现了高性能、强泛化、可扩展的联邦聚类新范式。

</details>

---

### 16. [Energy and Memory-Efficient Federated Learning With Ordered Layer Freezing](https://arxiv.org/abs/2512.23200)

**Authors**: Ziru Niu, Hai Dong, A. K. Qin, Tao Gu, Pengcheng Zhang  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.23200v1  

#### Abstract
Federated Learning (FL) has emerged as a privacy-preserving paradigm for training machine learning models across distributed edge devices in the Internet of Things (IoT). By keeping data local and coordinating model training through a central server, FL effectively addresses privacy concerns and red...

---

### 17. [SlimEdge: Lightweight Distributed DNN Deployment on Constrained Hardware](https://arxiv.org/abs/2512.22136)

**Authors**: Mahadev Sunil Kumar, Arnab Raha, Debayan Das, Gopakumar G, Amitava Mukherjee  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.22136v1  

#### Abstract
Deep distributed networks (DNNs) have become central to modern computer vision, yet their deployment on resource-constrained edge devices remains hindered by substantial parameter counts and computational demands. Here, we present an approach to the efficient deployment of distributed DNNs that join...

---

### 18. [HybridFlow: Adaptive Task Scheduling for Fast and Token-Efficient LLM Inference in Edge-Cloud Collaboration](https://arxiv.org/abs/2512.22137)

**Authors**: Jiangwen Dong, Jiayu Li, Wanyu Lin  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.22137v1  

#### Abstract
Large language models (LLMs) exhibit impressive reasoning and problem-solving abilities, yet their substantial inference latency and token consumption pose major challenges for real-time deployment on resource-limited edge devices. Recent efforts toward edge-cloud collaboration have attempted to mit...

---

### 19. [RollArt: Scaling Agentic RL Training via Disaggregated Infrastructure](https://arxiv.org/abs/2512.22560)

**Authors**: Wei Gao, Yuheng Zhao, Tianyuan Wu, Shaopan Xiong, Weixun Wang, Dakai An, Lunxi Cao, Dilxat Muhtar, Zichen Liu, Haizhou Zhao, Ju Huang, Siran Yang, Yongbin Li, Wenbo Su, Jiamang Wang, Lin Qu, Bo Zheng, Wei Wang  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.22560v1  

#### Abstract
Agentic Reinforcement Learning (RL) enables Large Language Models (LLMs) to perform autonomous decision-making and long-term planning. Unlike standard LLM post-training, agentic RL workloads are highly heterogeneous, combining compute-intensive prefill phases, bandwidth-bound decoding, and stateful,...

---

### 20. [Mirage Persistent Kernel: A Compiler and Runtime for Mega-Kernelizing Tensor Programs](https://arxiv.org/abs/2512.22219)

**Authors**: Xinhao Cheng, Zhihao Zhang, Yu Zhou, Jianan Ji, Jinchen Jiang, Zepeng Zhao, Ziruo Xiao, Zihao Ye, Yingyi Huang, Ruihang Lai, Hongyi Jin, Bohan Hou, Mengdi Wu, Yixin Dong, Anthony Yip, Zihao Ye, Songting Wang, Wenqin Yang, Xupeng Miao, Tianqi Chen, Zhihao Jia  
**Category**: cs.DC  
**Published**: 2025-12-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.22219v1  

#### Abstract
We introduce Mirage Persistent Kernel (MPK), the first compiler and runtime system that automatically transforms multi-GPU model inference into a single high-performance megakernel. MPK introduces an SM-level graph representation that captures data dependencies at the granularity of individual strea...

---

### 21. [LuxIA: A Lightweight Unitary matriX-based Framework Built on an Iterative Algorithm for Photonic Neural Network Training](https://arxiv.org/abs/2512.22264)

**Authors**: Tzamn Melendez Carmona, Federico Marchesin, Marco P. Abrate, Peter Bienstman, Stefano Di Carlo, Alessandro Savino Senior  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.22264v1  

#### Abstract
PNNs present promising opportunities for accelerating machine learning by leveraging the unique benefits of photonic circuits. However, current state of the art PNN simulation tools face significant scalability challenges when training large-scale PNNs, due to the computational demands of transfer m...

---

### 22. [FLEX-MoE: Federated Mixture-of-Experts with Load-balanced Expert Assignment](https://arxiv.org/abs/2512.23070)

**Authors**: Boyang Zhang, Xiaobing Chen, Songyang Zhang, Shuai Zhang, Xiangwei Zhou, Mingxuan Sun  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.23070v1  

#### Abstract
Mixture-of-Experts (MoE) models enable scalable neural networks through conditional computation. However, their deployment with federated learning (FL) faces two critical challenges: 1) resource-constrained edge devices cannot store full expert sets, and 2) non-IID data distributions cause severe ex...

---

### 23. [Post-Training Quantization of OpenPangu Models for Efficient Deployment on Atlas A2](https://arxiv.org/abs/2512.23367)

**Authors**: Yilun Luo, HuaQing Zheng, Haoqian Meng, Wenyuan Liu, Peng Zhang  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.23367v1  

#### Abstract
Huawei's openPangu-Embedded-1B and openPangu-Embedded-7B, variants of the openPangu large language model, integrate three distinct Chain-of-Thought (CoT) reasoning paradigms, namely slow_think, auto_think, and no_think. While these CoT modes enhance reasoning capabilities, their generation of extend...

---

### 24. [Joint Link Adaptation and Device Scheduling Approach for URLLC Industrial IoT Network: A DRL-based Method with Bayesian Optimization](https://arxiv.org/abs/2512.23493)

**Authors**: Wei Gao, Paul Zheng, Peng Wu, Yulin Hu, Anke Schmeink  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.23493v1  

#### Abstract
In this article, we consider an industrial internet of things (IIoT) network supporting multi-device dynamic ultra-reliable low-latency communication (URLLC) while the channel state information (CSI) is imperfect. A joint link adaptation (LA) and device scheduling (including the order) design is pro...

---

### 25. [Calibrating LLM Judges: Linear Probes for Fast and Reliable Uncertainty Estimation](https://arxiv.org/abs/2512.22245)

**Authors**: Bhaktipriya Radharapu, Eshika Saxena, Kenneth Li, Chenxi Whitehouse, Adina Williams, Nicola Cancedda  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.22245v1  

#### Abstract
As LLM-based judges become integral to industry applications, obtaining well-calibrated uncertainty estimates efficiently has become critical for production deployment. However, existing techniques, such as verbalized confidence and multi-generation methods, are often either poorly calibrated or com...

---

### 26. [Hybrid Quantum-Classical Mixture of Experts: Unlocking Topological Advantage via Interference-Based Routing](https://arxiv.org/abs/2512.22296)

**Authors**: Reda Heddad, Lamiae Bouanane  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.22296v1  

#### Abstract
The Mixture-of-Experts (MoE) architecture has emerged as a powerful paradigm for scaling deep learning models, yet it is fundamentally limited by challenges such as expert imbalance and the computational complexity of classical routing mechanisms. This paper investigates the potential of Quantum Mac...

---

### 27. [Beyond Centralization: Provable Communication Efficient Decentralized Multi-Task Learning](https://arxiv.org/abs/2512.22675)

**Authors**: Donghwa Kang, Shana Moothedath  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.22675v1  

#### Abstract
Representation learning is a widely adopted framework for learning in data-scarce environments, aiming to extract common features from related tasks. While centralized approaches have been extensively studied, decentralized methods remain largely underexplored. We study decentralized multi-task repr...

---

### 28. [Understanding the Mechanisms of Fast Hyperparameter Transfer](https://arxiv.org/abs/2512.22768)

**Authors**: Nikhil Ghosh, Denny Wu, Alberto Bietti  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.22768v1  

#### Abstract
The growing scale of deep learning models has rendered standard hyperparameter (HP) optimization prohibitively expensive. A promising solution is the use of scale-aware hyperparameters, which can enable direct transfer of optimal HPs from small-scale grid searches to large models with minimal perfor...

---

### 29. [Sat-EnQ: Satisficing Ensembles of Weak Q-Learners for Reliable and Compute-Efficient Reinforcement Learning](https://arxiv.org/abs/2512.22910)

**Authors**: \"Unver \c{C}ift\c{c}i  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.22910v1  

#### Abstract
Deep Q-learning algorithms remain notoriously unstable, especially during early training when the maximization operator amplifies estimation errors. Inspired by bounded rationality theory and developmental learning, we introduce Sat-EnQ, a two-phase framework that first learns to be ``good enough'' ...

---

### 30. [Taming the Tail: Stable LLM Reinforcement Learning via Dynamic Vocabulary Pruning](https://arxiv.org/abs/2512.23087)

**Authors**: Yingru Li, Jiawei Xu, Jiacai Liu, Yuxuan Tong, Ziniu Li, Tianle Cai, Ge Zhang, Qian Liu, Baoxiang Wang  
**Category**: cs.LG  
**Published**: 2025-12-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.23087v1  

#### Abstract
Reinforcement learning for large language models (LLMs) faces a fundamental tension: high-throughput inference engines and numerically-precise training systems produce different probability distributions from the same parameters, creating a training-inference mismatch. We prove this mismatch has an ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
