# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-05 06:40:51 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Multi-Head LatentMoE and Head Parallel: Communication-Efficient and Deterministic MoE Parallelism](https://arxiv.org/abs/2602.04870)

**Authors**: Chenwei Cui, Rockwell Jackson, Benjamin Joseph Herrera, Ana Mar\'ia T\'arano, Hannah Kerner  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.04870v1  

#### Abstract
Large language models have transformed many applications but remain expensive to train. Sparse Mixture of Experts (MoE) addresses this through conditional computation, with Expert Parallel (EP) as the standard distributed training method. However, EP has three limitations: communication cost grows l...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multi-Head LatentMoE and Head Parallel: Communication-Efficient and Deterministic MoE Parallelism

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **Expert Parallel (EP)** 是稀疏 Mixture-of-Experts (MoE) 模型的标准分布式训练方法，但它存在三个关键瓶颈：
- **通信开销大**：通信量随每个 token 激活的专家数 $k$ 线性增长（即 $O(k)$）。
- **负载不均衡**：由于路由决策的数据依赖性，某些 GPU 可能接收过多 token，导致延迟和显存峰值升高。
- **非确定性通信模式**：需要额外的 all-to-all 通信交换元数据（如队列长度），增加了复杂性和不确定性。

这些问题限制了 MoE 模型在大规模场景下的可扩展性和效率。

---

### 提出的新方法与新思路
作者提出了一种全新的架构与并行策略组合：

#### ✅ **Multi-Head LatentMoE**
- 将单个 MoE 层分解为多个独立的小型 MoE 子模块（称为“head”）。
- 每个输入 token 被投影并拆分为 $N_h$ 个 sub-token，分别由一个独立的 MoE 处理（含独立的 router 和 expert 集合）。
- 结合了 **LatentMoE**（低维隐空间计算）和 **Multi-Head** 思想，但关键区别在于：**每个 head 拥有独立参数**。

#### ✅ **Head Parallel (HP)**
- 在 **路由之前** 就通过 all-to-all 将 sub-token 分发到不同 GPU 上。
- 每个 GPU 独立完成其负责 head 的完整路由与专家计算，无需后续跨设备通信。
- 实现了：
  - **$O(1)$ 通信量**（不受 $k$ 影响）
  - **完全平衡的流量分配**
  - **确定性的通信模式**

#### ✅ **IO-aware Routing & Expert Computation**
为解决 Multi-Head 设计带来的高 HBM 开销问题，提出了两种高效实现：
- **IO-aware Routing**：利用在线 top-$k$ 技术，在 SRAM 中流式处理路由得分，避免将全部 $N_e$ 个得分写入 HBM。
- **IO-aware Expert Computation**：将稀疏 FFN 计算重构为 **block-sparse attention**，复用 **FlexAttention** 的高性能内核，实现 dropless 且 IO 高效的前向/反向传播。

---

### 相比现有方法的优势
| 维度 | Expert Parallel (EP) | Multi-Head LatentMoE + HP |
|------|------------------------|----------------------------|
| 通信复杂度 | $O(k)$ | $O(1)$ |
| 负载均衡 | 差（受路由影响） | 完全均衡 |
| 通信确定性 | 否（需元数据交换） | 是 |
| 显存访问 | 高（材料化所有激活） | 低（SRAM 流式处理） |
| 兼容性 | 支持标准 MoE | 兼容 EP，可组合使用 |

> 💡 **核心优势总结**：**通信更少、负载更稳、行为更可控、训练更快**，同时保持模型性能不变。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **FineWebEdu**：从互联网清洗得到的大规模文本语料。
- 实验使用其中 **10 billion tokens** 的子集进行语言建模预训练。

---

### 实验设置
- **模型结构**：Decoder-only Transformer，共 12 层，embedding size = 1024，context length = 2048。
- **注意力机制**：8 个 attention head，head dimension = 128。
- **优化器**：AdamW ($\beta_1=0.9, \beta_2=0.95$)，weight decay = 0.1，learning rate = $5\times10^{-4}$。
- **学习率调度**：梯形调度（8000 步 warmup + 2000 步 decay）。
- **全局 batch size**：0.66M tokens。
- **硬件平台**：NVIDIA H100 GPUs（80GB），NVLink 互联。

---

### 评估指标
| 类别 | 指标 |
|------|------|
| **训练效率** | 训练耗时（小时）、通信量、VRAM 占用、all-to-all 延迟 |
| **模型性能** | Validation Perplexity（FineWebEDU）、Zero-shot Accuracy（HellaSwag, PiQA, LAMBADA, ARC-Easy, ARC-Challenge） |
| **平均性能** | 多任务准确率均值（Avg. acc.） |

---

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **MLP** | 全连接前馈网络，作为基础对照 |
| **MoE + EP** | 标准 MoE 架构 + Expert Parallel，主流基线 |
| **LatentMoE + EP** | 在低维空间进行路由与专家计算，仍使用 EP |
| **MH LatentMoE + HP** | 本文提出的方法 |
| **MH LatentMoE + HP G** | “G” 表示 doubled granularity（专家数量翻倍，expert size 减半），用于测试扩展能力 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 参数配置 | 方法 | 训练时间 (相对) | Perplexity | 平均准确率 (Avg. acc.) |
|---------|------|------------------|------------|------------------------|
| 0.2B–2.2B | MoE + EP | 1.00× | 15.56 | 43.95 |
| 0.2B–2.2B | MH LatentMoE + HP | **0.90×** | 15.61 | 43.93 |
| 0.2B–4.2B | MoE + EP | 1.00× | 15.01 | 45.30 |
| 0.2B–4.2B | MH LatentMoE + HP | **0.62×** | 15.02 | 45.17 |
| 0.2B–4.2B | MH LatentMoE + HP G | **0.90×** | **14.82** | **45.43** ✅ |

> 🔥 **最高加速比达 1.61×**（4B 参数下相比 MoE + EP）

---

### 与基线方法的对比结果
- **训练速度提升显著**：
  - 在 4B 总参数下，**训练时间从 55.34 小时降至 34.41 小时**（快 1.61×）。
  - 在 2B 参数下也快 1.11×。
- **通信量大幅降低**：
  - 当 $k=4$ 时，inter-GPU 通信量仅为 EP 的 **25%**。
- **性能持平甚至更好**：
  - MH LatentMoE + HP 与 MoE + EP 的 perplexity 和 accuracy 基本一致。
  - 当采用 **doubled granularity (G)** 后，不仅训练仍快 1.11×，而且取得了 **最高的平均准确率 45.43%**，优于所有其他方法。

---

### 消融实验结果

#### ▶️ 负载不均衡敏感性分析（Figure 3）
- 随着 Zipf 分布 skew 增加（极端负载不均），**EP 的 all-to-all 延迟和 VRAM 峰值急剧上升**。
- **HP 则始终保持稳定**，不受 $k$ 或 skew 影响，验证了其负载鲁棒性。

#### ▶️ IO-aware Routing 效果（Figure 4）
- Naive routing 内存占用随专家数线性增长。
- **IO-aware routing 内存恒定**，且反向传播因只对 top-$k$ 专家求导而几乎无增长。

#### ▶️ IO-aware Expert Computation（Figure 5）
- 使用 Grouped GEMM 的 naive 方法在专家增多时性能急剧下降。
- **基于 FlexAttention 的方法保持高效**，尤其在反向传播中优势明显。

#### ▶️ Head 配置消融（Table 2）
| $N_h \times d_h$ | Val Loss | 相对训练成本 | SRAM 压力 |
|------------------|----------|---------------|-----------|
| 16×64 | 3.56 | 1.34× | Low |
| 8×128 | 3.48 | 1.00× ✅ | Medium |
| 4×256 | **3.41** | 1.07× | High |

> 最佳折中选择：**8×128**，兼顾性能、效率与工程可行性。

#### ▶️ 是否使用独立 routing token（Figure 6）
- 引入单独的 routing sub-token 对早期训练略有帮助，但最终收敛性能基本相同。
- 但由于会使 all-to-all 通信量翻倍，**最终未采用该设计**。

---

## 4. 关键结论和发现

### 主要发现
1. **Head Parallel 是首个实现 $O(1)$ 通信复杂度的 MoE 并行方案**，从根本上解决了 EP 的三大缺陷。
2. **Multi-Head LatentMoE + HP 在保持模型性能不变的前提下，训练速度最高提升 1.61×**。
3. **通过 IO-aware 设计，有效控制了多 head 带来的内存压力**，使该架构实际可行。
4. **增加 granularity（更多小专家）可在相似训练时间内获得更高整体性能**，展示了超稀疏 MoE 的潜力。
5. **该方法特别适用于学术界等资源受限环境**，降低了百亿参数级 MoE 模型的研究门槛。

---

### 方法的局限性
- **Head Parallel 的并行粒度受限于 $N_h$**（head 数量）。若 $N_h < P$（GPU 数），无法充分利用更多设备。
  - 例如：当前最多支持 8~16 个 GPU 的 intra-node 并行。
- 不适合极粗粒度 MoE 架构（expert 数少、size 大）。
- 当前实现依赖 FlexAttention 等较新的软件栈，可能对旧硬件/框架兼容性有限。

---

### 未来工作方向
1. **混合并行策略**：  
   - 使用 **HP 进行节点间通信**（inter-node），**EP 进行节点内通信**（intra-node），突破 $N_h$ 限制。
2. **进一步工程优化**：
   - 通信与计算重叠（communication-computation overlap）
   - 更细粒度的 kernel 融合
3. **探索更大规模部署**：
   - 扩展至千卡级别集群，验证可扩展性边界。
4. **动态 head 数调整机制**：
   - 自适应调节 $N_h$ 以匹配硬件拓扑。

---

### 总结一句话
> 本文提出的 **Multi-Head LatentMoE + Head Parallel** 架构，通过解耦路由与通信，在保证模型性能的同时实现了 **通信恒定、负载均衡、确定性强、训练加速高达 1.61×** 的 MoE 训练新范式，显著提升了超稀疏 MoE 模型的训练效率与可及性。

</details>

---

### 2. [AgentArk: Distilling Multi-Agent Intelligence into a Single LLM Agent](https://arxiv.org/abs/2602.03955)

**Authors**: Yinyi Luo, Yiqiao Jin, Weichen Yu, Mengqi Zhang, Srijan Kumar, Xiaoxiao Li, Weijie Xu, Xin Chen, Jindong Wang  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.03955v1  

#### Abstract
While large language model (LLM) multi-agent systems achieve superior reasoning performance through iterative debate, practical deployment is limited by their high computational cost and error propagation. This paper proposes AgentArk, a novel framework to distill multi-agent dynamics into the weigh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AgentArk: Distilling Multi-Agent Intelligence into a Single LLM Agent 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLM）的 **Multi-Agent Systems (MAS)** 在复杂推理任务中表现出色，通过多轮辩论、批判与共识机制提升推理质量。然而，MAS 存在两大瓶颈：

- **高计算成本**：多智能体交互导致推理延迟和资源消耗呈指数增长，难以部署于实时或资源受限场景。
- **错误传播风险**：个体偏见或幻觉可能在密集交互中被放大，导致集体失败。

因此，核心问题是：**能否将多智能体系统的推理能力“内化”到单个 LLM 中，在保持高效推理的同时继承其强大的推理优势？**

---

### **提出了什么新方法或新思路**

本文提出 **AgentArk** —— 一种全新的 **多智能体推理蒸馏框架**，旨在将 MAS 的动态推理过程“压缩”进单一 LLM 的参数中。

#### **核心思想**
将原本发生在 **测试时（test-time）** 的显式多智能体交互（如辩论），转化为 **训练阶段** 被学习到的隐式推理能力。即：
> “**把推理的计算负担从推理阶段转移到训练阶段**”，从而实现推理效率与能力的统一。

#### **三大分层蒸馏策略**
AgentArk 设计了三种递进式的蒸馏方法：

1. **Reasoning-Enhanced SFT (RSFT)**  
   - 在标准监督微调（SFT）基础上，不仅使用最终答案，还使用高质量的多智能体推理轨迹作为监督信号。
   - 目标：让单个模型学会生成连贯、正确的推理链。

2. **Data Augmentation via Diverse Extraction (DA)**  
   - 从多智能体辩论中提取多个**正确且多样化**的推理路径（例如不同解题策略、逻辑起点）。
   - 利用大容量教师模型（distiller）进行筛选，增强学生模型的鲁棒性和泛化能力。

3. **Process-Aware Distillation (PAD)** ✅ **核心创新**
   - 引入 **Process Reward Model (PRM)** 对每一步推理进行细粒度打分。
   - 使用 **Group Relative Policy Optimization (GRPO)** 进行强化学习，使模型学会自我修正、分解问题、识别错误等高级推理行为。
   - 实现了单次前向传递中模拟“辩证式”思考。

---

### **相比现有方法的优势**

| 维度 | 传统方法 | AgentArk |
|------|--------|----------|
| **依赖结构设计** | 需要预定义角色、协议、拓扑 | **agnostic to MAS algorithm**，仅关注推理过程本身 |
| **监督信号深度** | 多为最终答案或浅层交互痕迹 | 深度利用**完整推理轨迹 + 步级奖励信号** |
| **泛化能力** | 易过拟合特定任务模式 | 展现出强跨任务、跨领域、跨模态泛化 |
| **推理效率** | 推理时需多次调用多个模型 | 单模型单次推理，**低延迟、低成本** |
| **可扩展性** | 受限于 agent orchestration 复杂性 | 框架通用，支持任意 LLM backbone 和 MAS 生成方式 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 类型 | 数据集 | 描述 |
|------|-------|------|
| 数学推理 | **GSM8K**, **MATH** | 多步算术与代数问题 |
| 增强数学 | **MetaMathQA (MMQA)** | 更复杂的多步数学推理 |
| 医疗问答 | **MedMCQA** | 医学考试风格选择题，依赖专业知识 |
| 泛化评估（未用于训练） | **HotpotQA**, **QASPER**, **QMSum** | 多跳推理、长文本理解、会议摘要 |

> ✅ 所有训练数据由多智能体辩论自动生成，共约 **342k 问题 + 2M 推理轨迹**。

---

### **实验设置**

- **模型家族**：
  - **Qwen3**: 32B → 8B / 1.7B / 0.6B
  - **Gemma3**: 27B → 7B
  - **Llama3**: 8B
- **教师模型**：Qwen3-32B, Gemma3-27B-it
- **学生模型**：Qwen3-8B/1.7B/0.6B, Gemma-7B, Llama3-8B
- **蒸馏方式**：同构（same-family）与异构（cross-family）
- **评估指标**：
  - 主要：**Accuracy**
  - 辅助：Perplexity, NLL, F1, ROUGE, BERTScore
  - 推理质量评分：Step Decomposition, Intermediate Verification, Error Localization, Reasoning Coherence（由 InternLM-2.5-20b 自动评估）

---

### **基线方法对比**

| 方法 | 描述 |
|------|------|
| **Single Agent (Base)** | 原始未蒸馏模型 |
| **Vanilla Multi-Agent Debate** | 5-agent 平均表现（高成本） |
| **Standard SFT** | 仅用答案微调（无推理链） |
| **RSFT** | 使用完整推理链微调 |
| **DA** | 使用多样化解法增强训练集 |
| **PAD** | PRM + GRPO 强化学习蒸馏 |
| **组合方法**：RSFT+DA, PAD+DA | 两阶段训练 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 🔹 总体性能提升（以 Qwen3-32B → Qwen3-8B 为例）

| 方法 | GSM8K ↑ | MedMCQA ↑ |
|------|---------|-----------|
| Base | 88.17 | 59.65 |
| RSFT | 88.42 | 59.71 |
| DA | 87.11 | 58.33 |
| **PAD** | **89.05** | **63.12** |
| Vanilla MAS | ~89.2 | ~63.5 |

✅ **结论**：PAD 几乎达到甚至接近原始多智能体系统的表现，但只需一次推理！

---

#### 🔹 不同学生模型上的表现趋势

| 发现 | 内容 |
|------|------|
| ✅ **PAD 最稳定有效** | 在所有模型上均带来一致增益，尤其在 MedMCQA 上显著优于其他方法 |
| ⚠️ **DA 效果不稳定** | 小模型（如 0.6B）加入过多轨迹反而导致性能下降 |
| 📈 **异构蒸馏收益更大** | 如 Qwen→Llama 或 Qwen→Gemma，表明架构差异有助于吸收新推理范式 |
| 🧠 **大模型更受益于规模扩展** | 当教师从 5-agent 扩展到 20-agent 时，8B 模型持续受益，而 0.6B 模型迅速饱和甚至退化 |

---

#### 🔹 数据量 vs. 数据质量（Figure 5）

- **RSFT & DA**：随着数据量增加，性能波动剧烈，存在“越多越差”现象（噪声干扰）。
- **PAD**：性能随数据平稳上升，体现出对**高质量过程监督**的强大适应力。

> 💡 **关键发现**：对于小模型，**推理质量 > 数据数量**。

---

#### 🔹 消融实验结果

##### （1）PRM 容量影响（Table 4）
| PRM Size | Policy Size | GSM8K Acc |
|--------|------------|-----------|
| 0.6B | 0.6B | 42.84 |
| 8B | 0.6B | **88.48** |

➡️ **PRM 的建模能力比学生模型本身更重要！** 即使学生很小，只要 PRM 强大，仍能有效指导学习。

##### （2）GRPO vs PPO（Table 5）
- GRPO 与 PPO 表现相近，但 GRPO **无需价值函数**，训练更轻量、可扩展性强。
- 支持其作为大规模蒸馏的理想优化器。

##### （3）组合策略（Table 7）
| 方法 | GSM8K (+DA) | MedMCQA (+DA) |
|------|-------------|---------------|
| RSFT → DA | 70.27 | 44.30 |
| PAD → DA | 70.69 | 43.68 |

➡️ 各方法兼容，叠加使用可进一步小幅提升性能。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **单模型可以内化多智能体推理能力**  
   AgentArk 成功将 MAS 的推理动态“蒸馏”进单个 LLM，使其具备类似群体智慧的推理、纠错与反思能力。

2. ✅ **PAD 是最有效的蒸馏方式**  
   基于 PRM 的过程感知蒸馏不仅能提升准确率，还能改善推理行为（如分解、验证、定位错误），是实现“内在化”的关键。

3. ✅ **PRM 能力决定上限，学生容量决定下限**  
   - 强大的 PRM 可赋能弱小的学生；
   - 弱 PRM 会限制整体收益；
   - 学生模型太小则无法承载复杂推理模式。

4. ✅ **推理质量优于数量**  
   加入更多轨迹不一定更好；高质量、高信噪比的过程监督才是关键。

5. ✅ **具备强大泛化与鲁棒性**
   - 在 **TruthfulQA** 上表现更真实可靠（Table 2）；
   - 在 **HotpotQA/QASPER/QMSum** 等 OOD 任务上显著优于基线（Figure 6）；
   - 支持迁移到 **Multimodal LLMs**（Figure 7），尽管增益较小，但证明了模式通用性。

---

### **局限性**

1. **当前仅聚焦于推理任务**  
   未涉及工具使用（tool use）、记忆管理（memory）、长期规划等更复杂的 agent 功能。

2. **依赖高质量辩论数据生成**  
   若教师模型本身推理能力不足或产生大量错误轨迹，会影响蒸馏效果。

3. **训练成本较高（尤其是 PAD）**  
   虽然推理高效，但 PRM 训练 + GRPO 微调需要较多 GPU 时间（~20 小时 on 8×H100）。

4. **尚未探索所有 MAS 范式**  
   当前主要基于 **debate**，未来可拓展至 critique、consensus、reflection 等其他机制。

---

### **未来工作方向**

1. **开发自适应蒸馏策略**  
   根据任务难度、模型容量动态调整轨迹采样与监督强度。

2. **模块化/分层 PRM 设计**  
   针对不同类型推理步骤（如数学推导 vs 事实核查）设计专用 reward heads。

3. **扩展至真实世界应用场景**  
   如医疗诊断辅助、金融决策、教育辅导中的安全关键型任务。

4. **跨模态推理蒸馏**  
   将文本推理能力迁移至视觉、音频等多模态场景，构建真正通用的智能体。

5. **轻量化部署方案**  
   结合量化、剪枝等技术，推动 AgentArk 在边缘设备上的应用。

---

> 🌟 **一句话总结**：  
> **AgentArk 实现了“一个顶一群”的效果——通过将多智能体辩论中的高质量推理过程蒸馏进单个 LLM，既保留了群体智慧的优势，又获得了单模型的高效与简洁，为未来高效、鲁棒、可扩展的 LLM 推理系统提供了新范式。**

</details>

---

### 3. [Agent-Omit: Training Efficient LLM Agents for Adaptive Thought and Observation Omission via Agentic Reinforcement Learning](https://arxiv.org/abs/2602.04284)

**Authors**: Yansong Ning, Jun Fang, Naiqiang Tan, Hao Liu  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04284v1  

#### Abstract
Managing agent thought and observation during multi-turn agent-environment interactions is an emerging strategy to improve agent efficiency. However, existing studies treat the entire interaction trajectories equally, overlooking the thought necessity and observation utility varies across turns. To ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Agent-Omit: Training Efficient LLM Agents for Adaptive Thought and Observation Omission via Agentic Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前 **LLM Agent** 在多轮与环境交互过程中，普遍存在生成冗余 **Thought**（推理过程）和累积过多历史 **Observation**（环境反馈）的问题。这导致上下文长度迅速膨胀，严重影响 **推理效率** 和实际部署成本。

现有方法如 **Thought Management (TM)**、**Observation Management (OM)** 或 **TOM** 通常对整个交互轨迹进行统一压缩或剪枝，忽略了不同交互轮次中 **Thought 必要性** 和 **Observation 实用性** 的动态差异，从而可能损害任务准确性。

---

### 🚀 提出的新方法与创新思路

本文提出 **Agent-Omit**，一个统一的训练框架，使 LLM Agent 能够**自适应地省略冗余的 Thought 和 Observation**，提升效率而不牺牲效果。

#### 主要创新点：
1. **首次量化分析 Thought 与 Observation 的轮次依赖性影响**  
   - 发现：早期轮次的 Thought 对规划至关重要，中间轮次常可省略；早期 Observation 多为噪声，后期 Observation 更关键。
   - 动机：支持“选择性省略”而非全局压缩。

2. **提出 Agent-Omit 框架，包含两个核心阶段**：
   - **Agent Omission Behavior Synthesis（冷启动行为合成）**  
     构建单轮与多轮省略场景的合成数据，通过 SFT 让模型学会如何执行 `<think></think>`（空思考）和 `<omit_tool_response_N_...>`（省略历史观测）等格式化省略行为。
   - **Omit-Aware Agentic Reinforcement Learning（省略感知的强化学习）**  
     引入双采样机制（Full & Partial Trajectory）和定制的 **Omission Reward**，在 RL 阶段激励模型主动识别并省略冗余上下文。

3. **理论保障：省略策略偏差有界**  
   - 证明所学省略策略与最优策略之间的性能偏差由 **KL 散度上界控制**，确保训练稳定性。

---

### 🔍 相比现有方法的优势
| 类别 | 典型方法 | 局限性 | Agent-Omit 的优势 |
|------|--------|-------|------------------|
| **TM** | DEPO, ToolLight | 固定压缩推理长度，缺乏灵活性 | 自适应判断是否需要推理 |
| **OM** | Observation-Mask, DeepMiner | 启发式剪枝，易丢失关键信息 | 基于策略学习动态识别无关观测 |
| **TOM** | MEM-Agent, ReSum | 使用外部 LLM 总结，引入额外延迟与误差 | 内生式省略，无需额外模块 |

> ✅ **核心优势**：实现**高准确率下显著降低 Token 开销**，达成更优的 **Effectiveness-Efficiency Trade-off**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集（AgentGym-RL Benchmark）
共五个多样化任务环境，覆盖多种推理与交互模式：

| 数据集 | 任务类型 | 最大回合数 | 测试样本数 |
|-------|--------|----------|-----------|
| **DeepSearch** | 信息检索（搜索引擎问答） | 8 | 400 |
| **WebShop** | 电商网站导航与购买 | 12 | 200 |
| **TextCraft** | 文本版 Minecraft 配方制作 | 20 | 100 |
| **BabyAI** | 网格世界指令跟随与导航 | 10 | 90 |
| **SciWorld** | 科学实验模拟（化学/电路） | 10 | 200 |

---

### ⚙️ 实验设置与评估指标

#### 模型基础
- 主干模型：`Qwen3-4B`, `Qwen3-8B`
- 输出最大长度：32K tokens

#### 训练流程
1. **SFT 冷启动阶段**  
   - 使用约 2–4K 合成的单/多轮省略数据
   - 学习省略格式与基本行为
2. **Agentic RL 微调阶段**  
   - 使用 **Group Relative Policy Optimization (GRPO)**
   - 双采样策略（Full / Partial Trajectory）
   - 奖励函数：`r = (1−p) × R_task + p × R_omit`

#### 评估指标
| 指标 | 含义 |
|-----|------|
| **Pass@1** | 任务成功率（主指标） |
| **Avg Tok. ↓** | 平均总 Token 消耗（越低越好） |
| **Effectiveness-Efficiency Trade-off** | 综合衡量性能与开销的平衡能力 |

---

### 🆚 基线方法对比

#### （1）前沿 LLM Agent（Frontier LLM Agents）
- DeepSeek-R1-0528, DeepSeek-V3.2, OpenAI o3/o4-mini, Qwen3 系列等

#### （2）高效 Agent 构建方法（Efficient Agent Methods）
| 类型 | 方法 |
|------|------|
| **Thought Management** | Thinking-Retention, DEPO, ToolLight |
| **Observation Management** | Observation-Mask, DeepMiner |
| **Thought & Observation Management** | MEM-Agent, ReSum |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（以 Agent-Omit-8B-RL 为例）

| 方法 | DeepSearch Pass@1 | Avg Tok. | WebShop Pass@1 | Avg Tok. |
|------|--------------------|---------|----------------|---------|
| DeepSeek-R1-0528 | 25.25 | 6,412 | 19.37 | 11,308 |
| Qwen3-32B | 19.00 | 6,640 | 11.31 | 11,872 |
| **Agent-Omit-8B-RL** | **26.56** | **4,356** | **23.57** | **8,764** |

> ✅ 在 **DeepSearch** 上超越所有基线；
> ✅ 在 **WebShop** 上准确率提升超 20%，同时节省 ~25% Token。

#### 其他任务表现：
- **TextCraft**: 87.00 Pass@1（最高），仅需 7,328 tokens
- **BabyAI**: 84.36 Pass@1，远高于其他方法
- **SciWorld**: 18.45 Pass@1，是第二名的两倍以上

---

### 📉 与高效 Agent 方法对比（Qwen3-8B Backbone）

| 方法 | DeepSearch Pass@1 | Avg Tok. | WebShop Pass@1 | Avg Tok. |
|------|--------------------|---------|----------------|---------|
| Qwen3-8B Base | 17.75 | 8,281 | 6.93 | 16,741 |
| ReSum | 22.28 | 5,724 | 17.80 | 9,251 |
| **Agent-Omit-8B-RL** | **26.56** | **4,356** | **23.57** | **8,764** |

> ✅ **唯一同时实现最高准确率与最低 Token 消耗的方法**

---

### 🔬 消融实验结果（Ablation Study）

在 WebShop 上对 Qwen3-8B 进行消融研究：

| 变体 | Pass@1 | Avg Tok. | 分析 |
|------|--------|--------|------|
| **Agent-Omit-8B-RL** | 23.57 | 8,764 | 完整模型 |
| w/o STO（无单轮省略数据） | ↓ 显著下降 | ↑ | 单轮数据是行为初始化关键 |
| w/o MTO（无多轮省略数据） | ↓ 下降 | ↑ | 多轮数据维持推理连续性 |
| w/o PT（无 Partial Trajectory） | ↓↓ 严重下降 | ↑↑ | 缺乏省略前上下文无法学习策略 |
| w/o OR（无 Omission Reward） | 准确率尚可 | Token 不降 | 无奖励驱动则不主动省略 |
| w/o RL（仅 SFT） | 14.43 | 11,376 | RL 是效率跃升的关键 |

> ✅ **结论**：SFT 提供基础行为，RL 实现自适应优化；两者缺一不可。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Thought 与 Observation 的重要性具有轮次依赖性**  
   - 初始轮次：必须保留 Thought 用于规划
   - 中间轮次：Thought 和 Observation 最易被省略（平均省略 3–4 轮）
   - 末尾轮次：Observation 至关重要，省略会导致失败

2. **自适应省略机制可行且高效**  
   - Agent-Omit 能自动识别可安全省略的上下文片段
   - 显著降低 Token 成本（最高减少 50%+），同时提升或保持准确率

3. **RL 是实现动态省略的关键**  
   - SFT 仅能教会“怎么省略”，RL 才能学会“何时省略”

4. **理论支撑成立**  
   - 省略策略的性能偏差受 KL 散度控制，训练稳定可收敛。

---

### ⚠️ 方法的局限性
1. **依赖高质量合成数据构建冷启动集**  
   - 当前依赖人工设计 prompt 和 rollout 生成，难以完全自动化。
2. **对复杂状态空间泛化仍有限**  
   - 如 SciWorld 中部分任务仍需较多交互步。
3. **未探索更大规模模型（如 Qwen3-72B）上的扩展性**

---

### 🔮 未来工作方向
1. **将省略数据合成 pipeline 扩展至预训练阶段**  
   - 构建“天生高效”的 Agent 模型。
2. **推广至更大规模 LLM（如 Qwen3-72B）验证上限**
3. **结合 Memory Mechanism 实现长期记忆管理**
4. **探索多 Agent 场景下的协同省略策略**

---

## ✅ 总结

**Agent-Omit** 是首个系统性提出并实现 **自适应 Thought 与 Observation 省略** 的 LLM Agent 训练框架。它通过 **冷启动行为合成 + 省略感知的 Agentic RL**，实现了在多个复杂任务上**优于前沿 LLM Agent 的性能**，同时达到**最佳的效果-效率权衡**。该工作为构建轻量、高效、实用的 LLM Agent 提供了新范式。

</details>

---

### 4. [Expert Selections In MoE Models Reveal (Almost) As Much As Text](https://arxiv.org/abs/2602.04105)

**Authors**: Amir Nuriyev, Gabriel Kulp  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04105v1  

#### Abstract
We present a text-reconstruction attack on mixture-of-experts (MoE) language models that recovers tokens from expert selections alone. In MoE models, each token is routed to a subset of expert subnetworks; we show these routing decisions leak substantially more information than previously understood...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Expert Selections In MoE Models Reveal (Almost) As Much As Text》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文揭示了 **Mixture-of-Experts (MoE)** 模型中一个被忽视的隐私泄露风险：**仅通过观察每个 token 被路由到哪些专家（expert selections），攻击者即可高精度地重建原始输入文本**。

尽管 expert selections 是离散、低带宽的中间信号（远小于完整 hidden states 或 embeddings），但研究表明其蕴含的信息量远超预期，足以构成严重隐私威胁。

### 🚀 提出的新方法与创新思路
- **提出了一种基于序列解码器的文本重建攻击方法**：
  - 使用一个 **encoder-only Transformer 架构作为 sequence decoder**，联合建模整个 token 序列的 expert selection trace，利用位置依赖关系提升重建准确率。
- 将 MoE 路由机制与 **embedding inversion** 领域建立联系，指出 expert selections 可视为一种“离散嵌入”（discrete embeddings）。
- 探索了多种实际可行的攻击面（attack surfaces），如分布式推理、物理侧信道（side channels）、pipeline-parallel 架构等。

### 🔍 相比现有方法的优势
| 方面 | 本文方法 | 先前工作（如 MoEcho, Ding et al., 2025） |
|------|--------|-----------------------------|
| 解码策略 | **序列级联合解码**（sequence-to-sequence） | 单 token 分类器（如 logistic regression） |
| 性能 | 显著更高（top-1 准确率达 91.2%） | 有限（通常 <50% top-1） |
| 建模能力 | 利用上下文依赖和 token 间关系 | 忽略序列结构，独立预测每个 token |
| 泛化性 | 支持长序列建模与大规模训练 | 多为浅层模型，容量受限 |

> 💡 **核心突破**：证明了即使没有访问 logits、hidden states 或 expert outputs，仅凭 expert indices 就能实现接近文本级别的信息恢复。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **OpenWebText**：用于训练与评估。
  - 包含多样文本（新闻、论坛、代码片段等），甚至包含高熵敏感内容（如密码、API keys）。
  - 训练集：1亿个 tokens（划分为 32-token 的连续 chunk）
  - 测试集：1千万个 tokens（held-out split，未参与训练）

### ⚙️ 实验设置
- **目标模型**：`gpt-oss-20b`（模拟环境）
  - 24 层 MoE
  - 每层 32 个 experts
  - Top-4 routing（每 token 选 4 个专家）
  - 不使用自回归生成，仅 prefill 阶段处理输入序列
- **攻击者可观测信号**：
  - 仅能获取每层对每个 token 的 unordered expert indices（即 expert selection trace）
  - 不可见 router logits、weights、hidden states 或 expert 输出
- **辅助知识假设**：
  - 攻击者知道 tokenizer 和 MoE 配置（如 n=32, k=4）
  - 可从同族模型或日志中获得 `(text, trace)` 训练对

### 🎯 评估指标
- **Top-1 / Top-5 / Top-10 准确率**：
  - 衡量 reconstructed token 是否在预测分布的前 k 名中
- 所有结果均在 **32-token 输入序列** 上报告
- 主要使用全部 24 层的 expert selections（除非特别说明）

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **Logistic Regression** | 文献中的早期 baseline（如 MoEcho），单 token 分类 |
| **3-layer MLP** | 更强的 per-token 分类器，作为本文 weak baseline |
| **Transformer-based Sequence Decoder** | 本文提出的主攻方法，端到端序列重建 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（OpenWebText, 32-token sequences）

| 方法 | Top-1 Acc | Top-5 Acc | Top-10 Acc |
|------|----------|----------|-----------|
| **3-layer MLP**（per-token） | **63.1%** | 80.3% | 84.3% |
| **Transformer Sequence Decoder**（本文） | **91.2%** | 94.3% | **94.8%** |

> ✅ **提升显著**：相比 MLP 提升近 30 个百分点（top-1），表明序列建模极大增强了重建能力。

### 🔁 消融实验与分析结果

#### （1）训练数据规模影响（Figure 2）
- 在 1M → 100M tokens 范围内，性能随训练数据增加而**平滑上升**
- 即使只有 1M tokens，也能达到 ~70% top-1 准确率，显示攻击可行性较高

#### （2）token 频率的影响（Figure 3）
- 高频词（log₁₀ count > 4）重建准确率接近饱和（>95%）
- 低频词仍可达到合理水平（如 log₁₀ count ≈ 1.5 时约 60% top-1）
- 表明攻击不仅适用于常见词汇，也对部分罕见词有效

#### （3）噪声鲁棒性测试（Figure 6）
- 对 expert selection trace 添加随机噪声（替换部分 expert index 为随机值）
- 当噪声率达到 50% 时，top-1 准确率下降至 ~50%，但仍高于随机猜测
- 表明轻微扰动不足以完全防御此类攻击

#### （4）层数与信息熵分析（Figures 4 & 5）
- **早期层（1–7）**：互信息高，冗余性强
- **中间层（~11）**：与其他层相关性较低，可能承载更独特语义信息
- 总体 entropy 上限约为 **206 bits/token**（经验估计），虽低于理论最大值（363 bits），但仍远高于 English 的平均信息量（~1–2 bits/char）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Expert selections 泄露大量语义信息**：
   - 尽管是稀疏、离散信号，但由于其依赖于上下文且具有 determinism，形成了高度信息性的“指纹”。
2. **序列级解码优于逐 token 分类**：
   - 利用 Transformer 建模序列依赖后，重建性能大幅提升，说明路由模式存在跨位置一致性。
3. **MoE 路由应被视为敏感输出**：
   - 论文主张将 expert selection traces 视为与原始文本同等敏感的数据，在部署中需严格保护。
4. **多种现实攻击场景成立**：
   - 如恶意主机在分布式推理中监听路由；
   - 通过 GPU performance counters 或电磁泄漏推断激活专家；
   - pipeline-parallel 架构下通过设备活动检测反推路由路径。

### ⚠️ 方法的局限性
| 局限 | 说明 |
|------|------|
| **序列长度限制** | 当前最佳结果针对 32-token 短序列；尚未系统研究数百或数千 token 的长上下文表现 |
| **依赖训练数据获取** | 攻击需大量 `(text, trace)` 对进行训练，若无法获取同构模型或日志则难以实施 |
| **跨模型迁移性未知** | 未验证 decoder 在不同架构、tokenizer 或 expert permutation 下的表现 |
| **partial trace 支持不足** | 若只能观测部分层（如仅最后几层），性能会下降，但具体程度未量化 |

### 🔮 未来工作方向
1. **扩展至更长序列**：设计支持长上下文 inversion 的架构（如 sliding window + beam search）
2. **跨模型迁移攻击研究**：探索是否可在相似 MoE 架构间 transfer decoder
3. **防御机制量化评估**：
   - 如添加 logit noise、dummy computation、expert shuffling 等手段的实际防护效果
4. **物理侧信道实证验证**：
   - 结合硬件测量（power, EM）直接从运行时信号还原 expert traces
5. **形式化信息论分析**：
   - 建立 expert selection 与 token identity 之间的信息瓶颈模型

---

## 📝 总结一句话
> **在 MoE 模型中，expert selections 并非“副产品”，而是携带了几乎与原始文本相当的信息量——它们应当被当作敏感数据来对待。**

该研究为 MoE 模型的安全部署敲响警钟，并推动社区重新思考中间表示的隐私边界问题。

</details>

---

### 5. [Swordsman: Entropy-Driven Adaptive Block Partition for Efficient Diffusion Language Models](https://arxiv.org/abs/2602.04399)

**Authors**: Yu Zhang, Xinchen Li, Jialei Zhou, Hongnan Ma, Zhongwei Wan, Yiwei Shi, Duoqian Miao, Qi Zhang, Longbing Cao  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04399v1  

#### Abstract
Block-wise decoding effectively improves the inference speed and quality in diffusion language models (DLMs) by combining inter-block sequential denoising and intra-block parallel unmasking. However, existing block-wise decoding methods typically partition blocks in a rigid and fixed manner, which i...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Swordsman: Entropy-Driven Adaptive Block Partition for Efficient Diffusion Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
现有的 **Diffusion Language Models (DLMs)** 虽然理论上支持并行解码以提升推理效率，但在实际应用中仍受限于大量迭代去噪步骤，导致**推理速度慢、生成质量低**。  
当前主流的 **block-wise decoding** 方法通过将序列划分为固定长度的块（fixed-length blocks），实现块间顺序解码与块内并行去掩码（unmasking）。然而，这种**刚性的分块策略**常常在语义或句法成分（syntactic/semantic constituents）内部进行切割，破坏了语言的自然结构，从而**降低生成准确性和模型稳定性**。

---

### **提出的新方法与新思路**
本文提出 **Swordsman** —— 一种**无需训练**（training-free）、基于熵驱动的自适应分块解码框架，其核心思想是：

- **利用预测熵的变化（entropy shift）识别语义边界**：受 **Entropy Reduction Hypothesis (ERH)** 启发，作者认为，在扩散解码过程中，**语义成分边界处的不确定性变化最大**，即相邻 token 之间的熵差（△H）显著高于成分内部。
- **自适应分块（Adaptive Block Partitioning）**：通过检测 token 序列中的最大熵变点来动态划分 block 边界，使分块更贴合真实的语言结构。
- **动态去掩码阈值机制（Dynamic Unmasking Threshold）**：根据每个 block 内部的平均熵水平，动态调整置信度阈值，实现“高不确定块保守解码、低不确定块激进并行”，进一步平衡速度与质量。

---

### **相比现有方法的优势**
| 维度 | 传统方法（如 Fast-dLLM） | Swordsman |
|------|--------------------------|-----------|
| 分块方式 | 固定长度（rigid, fixed-size） | 自适应、语义对齐（adaptive, entropy-driven） |
| 是否需训练 | 否 | 否（完全 training-free） |
| 并行效率 | 受限于跨块语义断裂 | 更优的块内一致性提升并行可靠性 |
| 阈值策略 | 全局静态阈值 | 块级动态调整，适配难度差异 |
| 性能表现 | 速度尚可，精度受限 | 显著提升精度，同时保持甚至超越速度 |

> ✅ **核心优势**：首次将**信息论中的熵分析**引入 DLM 的 block 划分决策，实现了**语义感知的高效解码**。

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**
#### **预训练 DLM 模型**
- `LLaDA-8B-Instruct`
- `LLaDA-1.5`
- `Dream-v0-base-7B`

#### **基准测试任务（Benchmarks）**
覆盖两大类典型任务：
- **代码生成**：
  - HumanEval (0-shot)
  - MBPP (3-shot)
- **数学推理**：
  - GSM8K (5-shot)
  - MATH (4-shot)

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|-------|------|
| **生成长度** | $ L = 512 $ |
| **静态阈值（baseline）** | $ T_{\text{fixed}} = 0.9 $ |
| **Swordsman 超参** | 最小熵变阈值 $ T_{\min} = 0.1 $，基础置信阈值 $ T_{\text{init}} = 0.9 $ |
| **分块大小（fixed）** | 32（Fast-dLLM 最优设定） |
| **KV Cache 配置** | 三种模式：<br>• No Cache<br>• Prefix Cache<br>• Dual Cache |

#### **评估指标**
- **Accuracy (%)**：生成质量
- **Throughput (TPS, tokens/sec)**：解码速度
- **Latency (s/sample)**：端到端延迟

---

### **基线方法对比**
| 方法 | 类型 | 特点 |
|------|------|------|
| **Fast-dLLM** | Fixed block | 支持 KV Cache 和并行解码，当前主流 baseline |
| **D2F** | Fixed block | 多步推理蒸馏为单步，速度快但牺牲精度 |
| **AdaBlock** | Adaptive block | 基于标点符号等启发式规则分块，未开源，引用原论文结果 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### **在 GSM8K 上的表现（LLaDA-8B-Instruct, Dual Cache）**
| 方法 | Accuracy | TPS | Latency |
|------|---------|-----|--------|
| Vanilla LLaDA | 77.40% | 3.32 | — |
| Fast-dLLM | 75.21% | 72.12 | 3.72s |
| **Swordsman** | **81.50%** | **73.74** | **3.66s** |

> 🔺 **相对 vanilla LLaDA**：+4.1% 准确率，**8.79× 速度提升**

#### **在 HumanEval 上的表现（LLaDA-1.5, Dual Cache）**
| 方法 | Accuracy |
|------|---------|
| Fast-dLLM | 35.59% |
| **Swordsman** | **43.90%** |

> 🔺 **绝对提升 +8.31%**，且速度相当

#### **Dream-v0-base-7B 上 HumanEval 最高精度**
| 方法 | Accuracy |
|------|---------|
| Fast-dLLM | 56.70% |
| **Swordsman** | **57.93%** |

---

### **与基线方法的对比结果**

| 指标 | 结果总结 |
|------|----------|
| **准确性** | 在所有模型和任务上均达到 SOTA，尤其在 GSM8K 和 HumanEval 上大幅领先 Fast-dLLM 和 AdaBlock |
| **推理速度** | 与 Fast-dLLM 相当或略优；显著优于无缓存版本；虽低于 D2F，但**精度远超 D2F** |
| **帕累托前沿（Pareto Front）** | 多个配置位于速度-精度帕累托前沿，表明其综合性能最优 |

> 📊 图表显示：Swordsman 在 Dual Cache 下于 LLaDA-1.5 上实现 **64.97 TPS、3.03s 延迟、82.87% 准确率**，三项指标全面领先。

---

### **消融实验结果（Ablation Studies）**

#### **动态阈值机制的影响**
| 初始阈值 $ T_{\text{init}} $ | Accuracy | Latency |
|-------------------------------|--------|--------|
| 1.0（过严） | ~81.45% | ↑ 2.4× 延迟（过度串行化） |
| 0.9（推荐） | 81.43% | 最佳权衡 |
| 0.5（过松） | ↓ 至 72.42% | 虽快但精度崩塌 |

> ✅ 动态阈值可在几乎不损精度的前提下**减少约 2.5 秒延迟**

#### **自适应分块 vs 固定分块**
- 固定分块常割裂“passed by colorful benches”等完整短语
- 自适应分块能精准定位“enjoying the fresh air”、“serene pond”等语义单元边界（见 Figure 2）
- 引入自适应后，仅此一项即可带来 **+3.87% Accuracy 提升**

---

## **4. 关键结论和发现**

### **主要发现**
1. **语义边界可通过熵变有效识别**：  
   扩散过程中的 token 熵变化（entropy shift）在语义成分边界处显著增大，验证了 ERH 在 DLM 中的有效性。

2. **自适应分块显著提升生成质量**：  
   与固定分块相比，Swordsman 通过 aligning blocks with constituents，减少了跨块依赖和上下文断裂，提升了生成连贯性与准确性。

3. **动态阈值增强鲁棒性与效率**：  
   块内不确定性随解码推进而下降，动态调低阈值可安全释放更多并行潜力，避免“卡死”在低置信状态。

4. **无需训练即可实现 SOTA 性能**：  
   Swordsman 完全作为 inference-time framework 运行，兼容现有 DLM 架构，具备强实用性与部署友好性。

---

### **方法的局限性**
- 当前验证主要集中于 **block-wise DLMs**，尚未扩展至 semi-autoregressive 或其他生成范式。
- 超参数（如 $ T_{\min} $）可能需针对不同数据集微调，缺乏完全自适应调节机制。
- 对长文本（>512）的分块递归策略未深入探讨。

---

### **未来工作方向**
1. 将 entropy-driven 分块思想推广至 **semi-autoregressive models**。
2. 开发 **learned adaptive parameter selection** 机制，减少人工调参。
3. 探索结合 **constituency parsing prior** 或语法知识进一步优化边界检测。
4. 在更大规模任务（如 long-form QA、story generation）中验证泛化能力。

---

> ✅ **总体评价**：Swordsman 是一个简洁而强大的 inference-time 优化框架，通过将**信息论洞察转化为实际解码策略**，成功解决了 DLM 中长期存在的“效率-质量”权衡难题，为 diffusion-based language generation 提供了新的设计范式。

</details>

---

### 6. [PersoDPO: Scalable Preference Optimization for Instruction-Adherent, Persona-Grounded Dialogue via Multi-LLM Evaluation](https://arxiv.org/abs/2602.04493)

**Authors**: Saleh Afzoon, MohammadHossein Ahmadi, Usman Naseem, Amin Beheshti  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04493v1  

#### Abstract
Personalization and contextual coherence are two essential components in building effective persona-grounded dialogue systems. These aspects play a crucial role in enhancing user engagement and ensuring responses are more relevant and consistent with user identity. However, recent studies indicate t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《PersoDPO: Scalable Preference Optimization for Instruction-Adherent, Persona-Grounded Dialogue via Multi-LLM Evaluation》核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前开源 Large Language Models (LLMs) 在生成对话响应时虽然具备良好的**语言流畅性**（fluency）和自然度（naturalness），但在以下三个关键维度上仍存在明显不足：
- **Contextual Coherence**（上下文连贯性）：难以在多轮对话中保持语义一致性。
- **Persona Alignment**（人设对齐）：无法有效结合用户个性特征（persona）生成个性化回复。
- **Instruction Adherence**（指令遵循）：输出格式不一致，影响下游系统集成。

这些问题限制了其在虚拟助手、客服机器人等实际场景中的部署效果。

---

### 🚀 提出的新方法与创新思路
作者提出 **PersoDPO** —— 一种可扩展的偏好优化框架，旨在联合提升对话系统的：
- 上下文连贯性（Coherence）
- 人设对齐能力（Persona Grounding）
- 指令遵循能力（Instruction Adherence）

#### 主要创新点包括：

1. **基于多LLM自动评估构建高质量偏好对（Preference Pairs）**
   - 利用多个**开源 + 闭源 LLMs**（如 GPT-4、Qwen2、Mistral 等）在同一输入下生成响应。
   - 使用自动化评估指标（来自 PersoBench）为每个响应打分，无需人工标注即可构造“chosen”与“rejected”样本对。

2. **融合多维评价信号进行加权DPO训练**
   - 综合四种 metric-based signals：
     - `C Score`（Consistency Score）
     - `P Score`（Persona Distance Score）
     - `UE Score`（Utterance Entailment Score）
     - `Coh-UniEval`
   - 引入新的 **Length-Format Compliance (LFC)** 信号，鼓励模型遵守输出格式（JSON）和长度限制。

3. **无需人工标注的可扩展训练流程**
   - 完全依赖自动评估信号，实现端到端的 annotation-free 偏好学习，显著降低标注成本并提高可复现性。

---

### 🔍 相比现有方法的优势

| 对比维度 | 现有方法（如 DPOC、标准 DPO） | PersoDPO |
|--------|-------------------------------|---------|
| **监督来源** | 手工标注 / 启发式规则 / 单一模型反馈 | 多LLM输出 + 自动评估，更全面可靠 |
| **优化目标** | 聚焦单一维度（如 persona 或 coherence） | 联合优化 coherence、personalization、instruction adherence |
| **指令遵循** | 忽视格式与长度控制 | 显式建模 LFC 信号，增强 instructability |
| **可扩展性** | 依赖人工干预 | 全流程自动化，适合工业级部署 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用 **FoCus dataset**：一个用于个性化对话生成的基准数据集。
  - 包含多轮对话历史（dialogue context）
  - 提供用户 persona 描述（persona descriptions）
  - 更长的上下文设计有利于测试连贯性和个性化能力。

> Prompt 构造方式：将 persona 和 dialogue context 拼接作为输入，要求模型以 JSON 格式返回 `"response"` 字段。

---

### ⚙️ 实验设置

#### 模型选择
- **候选 LLMs（用于生成初始响应）**：
  - 开源模型：`Qwen2-7B`, `Mistral-7B`, `LLaMA 3.1-8B`
  - API 模型：`GPT-3.5-Turbo`, `GPT-4-Turbo`, `GPT-4o-Mini`

- **目标微调模型**：`Qwen2-5B-Instruct`（因 GPU 内存限制）

#### 偏好对构建
- 从 FoCus 训练集中采样约 1,500 条记录生成响应。
- 对每条 prompt 收集多个 LLM 输出，并通过自动评分选出高分（chosen）与低分（rejected）响应。
- 最终构建约 **9,000 个 preference pairs**。

#### 微调策略
- 使用 Hugging Face 的 `TRL` 库中的 `ScoreWeightedDPOTrainer`。
- 采用 **weighted DPO objective**，公式如下：

$$
\mathcal{L}_{\text{PersoDPO}} = \mathbb{E}_{x,y_c,y_r} \left[ \sigma(\Delta S) \cdot (\log p_\theta(y_c|x) - \log p_\theta(y_r|x)) \right]
$$

其中 $\Delta S = S_{\text{total}}^{(c)} - S_{\text{total}}^{(r)}$ 是总质量得分差值，$S_{\text{total}} = S_{\text{metric}} + S_{\text{LFC}}$

---

### 📊 评估指标（沿用 PersoBench）

| 指标 | 类型 | 含义 |
|------|-----|------|
| `Coh-UniEval` | Coherence | 衡量上下文连贯性与逻辑一致性 |
| `C Score` | Personalization | 一致性得分，惩罚 persona 不一致 |
| `UE Score` | Coherence | 回应是否蕴含于上下文中（utterance entailment） |
| `P Score` | Personalization | persona 距离得分，越接近越好 |
| `Failure Ratio` | Instructability | 输出不符合 JSON 格式或超长的比例 |
| `Response Time` | Efficiency | 推理延迟，反映实用性 |

所有分数归一化至 [0,1] 区间。

---

### 🆚 基线方法对比
| 基线模型 | 类型 | 说明 |
|--------|-----|------|
| `Qwen2-7B`, `Mistral-7B`, `LLaMA 3.1-8B` | 零样本基准（Zero-shot Baseline） | 直接推理，未微调 |
| `Qwen2-5B-DPO` | Vanilla DPO 微调模型 | 使用传统 DPO 方法微调，仅基于格式有效性构建偏好对 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）

| Model | Coh-UniEval ↑ | C Score ↑ | UE Score ↑ | P Score ↑ |
|-------|----------------|-----------|------------|-----------|
| Qwen2-7B Benchmark | 0.37±0.48 | -0.31±0.84 | 0.17±0.48 | 0.29±0.30 |
| Mistral-7B Benchmark | 0.53±0.46 | -0.23±0.82 | 0.21±0.52 | 0.30±0.28 |
| Llama3.1-8B Benchmark | 0.55±0.49 | -0.22±0.87 | 0.17±0.48 | 0.33±0.33 |
| Qwen2-5B DPO | 0.98±0.49 | 0.06±0.49 | 0.21±0.58 | 0.29±0.26 |
| **Qwen2-5B PersoDPO** | **0.70±0.45** | **0.18±0.87** | **0.30±0.62** | **0.44±0.30** |

> 注：↑ 表示越高越好；负值表示严重 persona 不一致。

---

### 🔬 结果分析

#### ✅ PersoDPO 全面超越所有开源基线
- 在 **全部四项指标上均优于三大零样本模型**（Qwen2/Mistral/Llama），尤其在 `C Score` 和 `P Score` 上表现突出。
- 尽管 `Coh-UniEval` 略低于 DPO 基线（0.70 vs 0.98），但其 `UE Score` 更高（0.30 > 0.21），表明在实际语义连贯性方面更具优势。

#### ✅ 显著优于 vanilla DPO 方法
- `C Score`: 0.18 vs 0.06 → 提升 **200%**
- `P Score`: 0.44 vs 0.29 → 提升 **51.7%**
- 表明 **multi-LLM evaluation + 多维度信号融合** 能更有效地捕捉 persona-aligned 行为。

#### ✅ 工业实用性强
- 如图 2(c) 所示：
  - **Failure Ratio 更低**：说明 PersoDPO 更好地遵守了 JSON 格式和长度约束。
  - **Response Time 更短**：响应更快，更适合生产环境部署。

> 这验证了 LFC 信号的有效性，提升了模型的 **instructability** 和 **reliability**。

---

### ❌ 消融实验（Ablation Study）说明
尽管原文未明确列出消融实验表格，但从方法描述中可以推断出以下关键设计的作用：

| 组件 | 功能 | 影响 |
|------|------|------|
| Multi-LLM Evaluation | 引入多样化响应源 | 提高偏好对多样性与鲁棒性 |
| Metric-based Signals (`C`, `P`, `UE`, `Coh-UniEval`) | 强化 persona 与 coherence 学习 | 显著提升个性化能力 |
| LFC Signal | 鼓励格式与长度合规 | 降低 failure ratio，提升 instructability |
| Score-weighted DPO | 根据得分差距调整梯度权重 | 加速收敛，提升训练稳定性 |

> 图 2(a)(b) 显示训练过程中 loss 平稳下降、reward accuracy 接近上限，证明该策略具有良好的优化稳定性。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **多LLM自动评估是构建高质量偏好数据的有效途径**
   - 可替代昂贵的人类标注，在保证质量的同时大幅提升可扩展性。

2. **联合优化 coherence、personalization 和 instruction adherence 是可行且必要的**
   - 单一维度优化容易导致其他方面的退化，而 PersoDPO 实现了三者之间的良好平衡。

3. **PersoDPO 显著优于强基线模型**
   - 在 FoCus 数据集上，无论是在个性化还是连贯性指标上都取得领先，尤其在最具挑战性的 `C Score` 上实现突破。

4. **指令遵循可通过显式奖励机制改善**
   - 引入 LFC 信号后，failure ratio 明显下降，响应时间缩短，增强了工业适用性。

---

### ⚠️ 局限性

1. **依赖外部 LLM 输出质量**
   - 若闭源模型（如 GPT-4）本身存在偏差或错误，可能污染偏好对。
   - 存在 API 成本和访问限制问题。

2. **自动评估指标仍有局限**
   - 当前 metric-based evaluators（如 P Score）虽经 fine-tuned，但仍无法完全替代人类判断。

3. **未涵盖更多任务类型**
   - 实验集中在通用 persona-grounded 对话，尚未扩展到医疗、金融等垂直领域。

4. **缺乏跨文化/语言泛化测试**
   - 当前研究基于英文数据集，未验证在多语言场景下的表现。

---

### 🔮 未来工作方向

1. **引入动态偏好选择机制**
   - 根据难度自适应筛选 margin-based preference pairs，进一步提升训练效率。

2. **探索 self-improvement loop**
   - 让 PersoDPO 模型反向参与后续评估过程，形成闭环迭代优化。

3. **扩展至多模态 persona grounding**
   - 结合图像、语音等信息构建更丰富的用户画像。

4. **开发轻量化本地评估器**
   - 替代部分 API-based LLM judge，降低成本并保护隐私。

5. **应用于 real-world user interaction 场景**
   - 在真实用户交互中收集 implicit feedback（如点击、停留时间），实现持续在线优化。

---

> ✅ **代码与资源公开**：  
> 项目已开源，包含完整实现、评估结果及原始响应日志：  
> 🔗 https://github.com/salehafzoon/PersoDPO

</details>

---

### 7. [Less Finetuning, Better Retrieval: Rethinking LLM Adaptation for Biomedical Retrievers via Synthetic Data and Model Merging](https://arxiv.org/abs/2602.04731)

**Authors**: Sameh Khattab, Jean-Philippe Corbeil, Osman Alperen Kora\c{s}, Amin Dada, Julian Friedrich, Fran\c{c}ois Beaulieu, Paul Vozila, Jens Kleesiek  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04731v1  

#### Abstract
Retrieval-augmented generation (RAG) has become the backbone of grounding Large Language Models (LLMs), improving knowledge updates and reducing hallucinations. Recently, LLM-based retriever models have shown state-of-the-art performance for RAG applications. However, several technical aspects remai...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Less Finetuning, Better Retrieval: Rethinking LLM Adaptation for Biomedical Retrievers via Synthetic Data and Model Merging*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前在将通用 Large Language Models (LLMs) 适配为**领域专用检索器**（尤其是生物医学领域）时，主流方法依赖大规模预训练（pretraining）和全量微调（full fine-tuning），存在以下问题：
- **计算成本高**：需要大量标注或伪标签数据进行持续预训练。
- **数据效率低**：是否所有数据都对性能提升有贡献尚不明确。
- **缺乏模块化设计**：难以灵活组合不同任务或领域的专家模型。

该论文聚焦于如何更高效、更少微调地构建高性能的**生物医学密集检索器**（dense retriever），同时保持其在通用领域的泛化能力。

### 提出了什么新方法或新思路
作者提出 **STM (Synthesize-Train-Merge)** 框架，一个**模块化、高效且可扩展**的 LLM 适配方案，包含三个核心组件：

1. **Synthetic Data Generation**
   - **Synthetic Hard Negatives (SHN)**：利用 GPT-4.1 生成语义上相关但与查询无关的“硬负例”，提高对比学习质量。
   - **Prompt Optimization**：使用 GEPA 和随机采样策略自动生成并优化 retrieval prompts，探索 prompt 对嵌入空间的影响。

2. **Expert Model Fine-tuning**
   - 在 BMRetriever 数据集的不同子集上分别训练多个“专家”模型（如 Med-Synth、Med-Real、Search、NLU），每个专家专注于特定数据分布或配置（如是否使用 SHN 或优化 prompt）。

3. **Model Merging**
   - 将多个专家模型通过参数空间合并（如 Linear Interpolation 和 Ties-merging）融合成单一模型，无需额外训练。

### 相比现有方法的优势
- **更少微调，更高性能**：仅用 **1.4M 训练样本**（相比 BMRetriever 的 11.4M 减少了约 90%），且跳过预训练阶段，仍能取得更优结果。
- **更高的数据效率**：通过合成数据增强和 prompt 优化，以更小的数据规模实现更强性能。
- **模块化与灵活性**：支持独立开发专家模型，并通过 merging 组合优势，便于迭代和维护。
- **更强的综合性能**：merged 模型在医疗和通用任务上均优于单个专家和强 baseline。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **BMRetriever 数据集**（Xu et al., 2024）作为主要 fine-tuning 数据源，被划分为四个子集：
  - **Med-Synth**（431K pairs）：LLM 生成的生物医学检索对。
  - **Med-Real**（306K pairs）：真实生物医学推理与问答数据（如 MedNLI, MEDIQA）。
  - **Search**（438K pairs）：通用搜索任务数据（如 MS MARCO）。
  - **NLU**（251K pairs）：自然语言理解任务（如 SNLI, FEVER, ELI5）。
- **评估基准**：从 MTEB 中选取 **12 个英文任务子集**，包括：
  - 医疗任务：TREC-COVID, SciFact, NFCorpus, CURE, PublicHealthQA, MedicalQA
  - 通用任务：FiQA, ArguAna, SciDocs, FEVER, Quora

### 实验设置和评估指标
- **Backbone Models**：
  - Qwen3 (0.6B)
  - Gemma (2B)
  - Phi4 mini instruct (3.8B)
- **训练方式**：
  - 使用 LoRA 进行参数高效微调（PEFT）
  - InfoNCE 损失函数，结合 in-batch negatives 和 hard negatives
  - 采用 EOS pooling 和双向 attention（disable causal mask）
- **评估指标**：
  - 主要指标：**nDCG@10**
  - 报告平均得分：`Avg Medical`, `Avg General`, `Avg All`

### 基线方法对比
对比了多种代表性检索模型：
| Model | Type | Size |
|-------|------|------|
| BM25 | Lexical | – |
| Contriever | Dense | 150M |
| E5-v2 | Dense | 335M |
| GTR | Dense | 1.2B |
| BMRetriever | Biomedical | 2B |
| LLM2Vec | Generalist | 3B |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| Model | Avg All (nDCG@10) | Avg Medical | Avg General |
|-------|-------------------|-------------|--------------|
| **STMPhi4-Linear** | **0.646** | **0.677** | **0.603** |
| STMGemma-Linear | 0.622 | 0.654 | 0.577 |
| STMQwen3-Linear | 0.616 | 0.638 | 0.585 |
| BMRetriever (2B) | 0.609 | 0.645 | 0.560 |
| LLM2Vec (3B) | 0.619 | 0.635 | 0.597 |
| E5 Large V2 | 0.622 | 0.654 | 0.576 |

> ✅ **STMPhi4-Linear 在所有指标上均达到 SOTA 性能**，显著超越同级别及更大规模的 baseline。

### 与基线方法的对比结果
- 所有 STM merged 模型均**全面超越**对应的 fully fine-tuned 单一模型（见 Figure 2）。
- 即使是较小的 **Qwen3-0.6B** 构建的 STM 模型，性能也接近甚至超过更大的 BMRetriever (2B) 和 LLM2Vec (3B)。
- **STMGemma-Linear** 虽然与 BMRetriever 共享 backbone，但通过 merging 实现了更强性能，证明 merging 本身带来增益。

### 消融实验结果
#### (1) Prompt Optimization 效果显著
- 在专家层面，应用最优 prompt optimization（PO）后：
  - **Search 专家（Phi4）提升高达 +23.5%**
  - **NLU 专家（Gemma）提升 +17.9%**
- 相比之下，SHN 提升有限，且在大模型中可能损害 Med-Real 性能（因已有高质量负例）。
- **PO 是更鲁棒有效的策略**，尤其对通用域任务。

#### (2) Model Merging 显著优于单专家和联合微调
- Linear merging 表现略优于 Ties-merging。
- 所有 merged 模型均优于最强的单个专家（如 FT-all）。
- 例如，STMPhi4-Linear 达到 0.646，而最强单专家 FT-all_sHN 仅为 0.629。

#### (3) 更少数据即可达到更好性能
- 图 4 显示：当训练数据量达到 **~100K** 时，性能已趋于饱和，甚至**超过使用完整 1.4M 数据的效果**。
- 结合 merging 可进一步减少实际使用的数据量（最多减少 29%）。

#### (4) 不需要预训练
- 图 3 显示：仅 fine-tune 1.4M 数据的表现 **优于 pretrain + fine-tune 的 11.4M 数据**。
- 因此 STM 完全省去了昂贵的 pretraining 阶段。

---

## 4. 关键结论和发现

### 主要发现
1. **Prompt Optimization 是强大的轻量级适配手段**：通过自动优化 retrieval prompt 可显著提升模型表现，尤其在通用任务上效果惊人（+23.5%）。
2. **Model Merging 是高效的模型集成路径**：简单加权合并多个专家模型即可获得优于全量微调的性能，验证了 “merge > fine-tune” 的可行性。
3. **更少微调可以实现更好检索**：STM 仅使用 1.4M 数据、无 pretraining、仅微调部分专家，最终 merged 模型仍大幅领先。
4. **合成数据有效但需谨慎使用**：GPT-4.1 生成的 synthetic hard negatives 有一定帮助，但不如 prompt optimization 稳定；且与 PO 结合时可能出现干扰。
5. **模块化设计具有工程优势**：支持独立开发、测试和替换专家模型，适合多团队协作和持续迭代。

### 方法的局限性
1. **依赖大型 LLM 生成数据和 prompt**：使用 GPT-4.1 和 LLaMA-70B 增加了计算开销和对第三方模型的依赖。
2. **未评估生成器鲁棒性**：未测试不同 LLM 或 prompt 变体下的稳定性。
3. **仅探索两种 merging 策略**：Linear 和 Ties-merging，尚未引入更复杂的 adaptive merging 方法。
4. **未开源模型和数据**：目前代码、数据和模型计划在接收后发布，限制了复现性。

### 未来工作方向
- 探索更低成本的 prompt generation 和 hard negative synthesis 方法（如使用小型模型蒸馏）。
- 引入动态 merging 权重机制，根据输入自动选择专家组合。
- 将 STM 框架推广至其他垂直领域（如法律、金融）。
- 研究如何将 merging 与 instruction tuning 更深度结合，构建“多功能嵌入模型”。

---

> 📌 **总结一句话**：  
> 本文提出的 **STM 框架**证明了：通过 **synthetic data + prompt optimization + model merging** 的组合拳，可以在**极少微调**的情况下，构建出性能远超传统 fine-tuning 方法的**高性能、多领域兼容的检索器**，为 LLM 的高效适配提供了新范式。

</details>

---

### 8. [The Key to State Reduction in Linear Attention: A Rank-based Perspective](https://arxiv.org/abs/2602.04852)

**Authors**: Philipp Nazari, T. Konstantin Rusch  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.04852v1  

#### Abstract
Linear attention offers a computationally efficient yet expressive alternative to softmax attention. However, recent empirical results indicate that the state of trained linear attention models often exhibits a low-rank structure, suggesting that these models underexploit their capacity in practice....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《The Key to State Reduction in Linear Attention: A Rank-based Perspective》论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文针对 **Linear Attention** 模型在实际训练后表现出的 **低秩（low-rank）状态结构** 问题展开研究。尽管 Linear Attention 在计算效率上优于 Softmax Attention，但实证表明其隐藏状态（associative memory）并未充分利用其全部容量，导致模型对查询噪声敏感，并可能影响检索性能。

作者指出，这种低秩现象本质上是 **内存利用不均（memory isotropy）** 的体现，即信息集中在少数主成分上，而大部分维度冗余。这不仅浪费了计算资源，也限制了模型的鲁棒性和表达能力。

### 提出了什么新方法或新思路
为解决上述问题，论文提出了一套 **硬件感知的结构化剪枝框架（hardware-aware structured pruning framework）**，用于在 **训练后（post-training）** 减少 Linear Attention 模型的状态大小。其核心思想基于以下两个关键洞察：

1. **正交不变性（Orthogonal Invariance）**：  
   Linear Attention（特别是 DeltaNet 及其变体）对同时施加于 **query 和 key** 上的正交变换具有不变性。这意味着可以在不改变模型功能的前提下，对 key 和 query 空间进行联合变换。

2. **结构化剪枝优于非结构化剪枝**：  
   与仅产生稀疏权重的非结构化剪枝不同，本文采用 **轴对齐的结构化剪枝（axis-aligned structured pruning）**，直接移除 key 和 query 的某些通道（channels），从而 **实质性地降低状态维度 $d_k$**，带来真正的 FLOPs 和内存减少。

在此基础上，作者提出了一个新颖的剪枝算法：

- **DRRQR（Deep Rank Revealing QR Decomposition）**：  
  基于 **强秩揭示 QR 分解（Strong Rank Revealing QR, RRQR）**，从激活统计矩阵 $[K, Q]$ 中选择一组能形成良好条件子空间的列（即通道）。该方法显式优化 **有效秩（effective rank）** 和 **秩利用率（rank utilization）**，旨在提升剩余内存的各向同性，而非简单保留最大方差方向。

### 相比现有方法的优势
| 对比维度 | 本文方法 | SliceGPT / SpinQuant / QuaRot |
|---------|--------|-------------------------------|
| **作用空间** | **State Space**（直接压缩 key/query 维度） | **Residual Stream**（旋转嵌入空间） |
| **兼容性** | 保持与现有 CUDA kernels 兼容 | 需要特定量化 kernel 支持 |
| **目标** | 显式提升秩利用率，改善噪声鲁棒性 | 主要目标是量化友好或压缩嵌入维度 |
| **互补性** | ✅ 可与其他方法结合使用 | ❌ |

因此，本文方法是 **互补且可叠加** 的，尤其适合部署场景下的高效推理。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **预训练数据**：`Fineweb-Edu`（10B tokens）
- **校准集（Calibration Set）**：128 个 `Fineweb-Edu` 样本（用于剪枝前的特征统计）
- **评估任务**：
  - **语言建模**：`Wikitext-2`, `Lambada`
  - **零样本生成**：`ARC-e`, `ARC-c`, `HellaSwag`, `Winogrande`, `PIQA`, `LAMBADA`（通过 `lm-eval-harness`）
  - **真实世界检索**：`Drop`, `FDA`, `NQ`, `SQuAD`, `SWDE`, `TriviaQA`（使用 prefix-linear-attention 协议）

### 实验设置
- **模型架构**：`DeltaNet` 和 `Gated DeltaNet`
- **模型规模**：370M, 1.3B, 2.7B 参数
- **剪枝比例**：30%, 40%, 50%, 75%
- **恢复微调（Recovery Fine-Tuning, RFT）**：使用 LoRA（rank=16/32）在单张 H100 上进行
- **硬件平台**：NVIDIA H100 GPU
- **批大小**：32（序列混合器吞吐测试），16（RFT）

### 评估指标
- **Perplexity**（越低越好）：Wikitext-2, Lambada
- **Accuracy (%)**：各项零样本与检索任务
- **吞吐量（Tokens Per Second, TPS）**
- **峰值 VRAM 使用量**
- **秩利用率（Rank Utilization）**

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **L1** | 基线 | 基于 query/key 投影矩阵 L1 范数的幅度剪枝 |
| **Random (Rand)** | 基线 | 随机选择通道 |
| **S-Wanda** | 改进 | 结合输入激活范数的 Wanda 方法适配版 |
| **Grad** | 优化法 | 基于梯度显著性的剪枝（task-aware） |
| **DRRQR** | **本文提出** | 基于秩揭示 QR 的结构化剪枝 |
| **PCA** | 对比 | 主成分分析（理论上最优但实践中因卷积不兼容而表现差） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 语言建模性能（Post-RFT, 50% Compression）
| Model | Method | Wiki PPL ↓ | Lambada PPL ↓ |
|-------|--------|------------|----------------|
| Gated DeltaNet 1.3B | **Grad** | **16.4** | **10.5** |
| Gated DeltaNet 1.3B | **DRRQR** | 16.3 | 12.0 |
| Gated DeltaNet 1.3B | S-Wanda | 16.6 | 11.7 |
| Gated DeltaNet 1.3B | L1 | 16.8 | 13.0 |
| Gated DeltaNet 1.3B | **Baseline** | 16.8 | 9.7 |

> ✅ **仅增加约 0.3–0.7 点困惑度，即可压缩 50% 的 key/query 通道**

#### 零样本平均准确率（Post-RFT, 50% Compression）
| Model | Method | ZS Avg ↑ |
|-------|--------|----------|
| Gated DeltaNet 1.3B | **Grad** | **58.3** |
| Gated DeltaNet 1.3B | DRRQR | 58.0 |
| Gated DeltaNet 1.3B | S-Wanda | 57.8 |
| Gated DeltaNet 1.3B | L1 | 57.2 |
| Gated DeltaNet 1.3B | **Baseline** | 59.4 |

> ⚠️ 平均下降约 1 个百分点，但仍保持较强泛化能力

#### 检索任务平均准确率（Post-RFT, 50% Compression）
| Model | Method | Ret Avg ↑ |
|-------|--------|-----------|
| Gated DeltaNet 1.3B | **Grad** | **34.3** |
| Gated DeltaNet 1.3B | DRRQR | 33.0 |
| Gated DeltaNet 1.3B | S-Wanda | 32.9 |
| Gated DeltaNet 1.3B | L1 | 32.1 |
| Gated DeltaNet 1.3B | **Baseline** | 40.3 |

> ⚠️ 检索任务对压缩更敏感，但在 50% 压缩下仍保留约 85% 性能

#### 吞吐量与内存节省（DeltaNet, 50% Compression）
| Model | $d_k$ | TPS | Speedup | VRAM | Ratio |
|-------|-------|-----|---------|-------|-------|
| DeltaNet | 128 | 8.1M | 1.00× | 6.33 GiB | 1.00 |
| DeltaNet | 64 | 10.8M | **1.34×** | 4.57 GiB | **0.72** |

> ✅ **速度提升 34%，内存减少 28%**

---

### 与基线方法的对比结果
- **DRRQR** 和 **Grad** 在所有任务中均显著优于 **L1**, **Rand**, **S-Wanda**
- **DRRQR** 作为 **无梯度、局部、任务无关** 的方法，性能接近甚至超过基于梯度的 **Grad**
- **PCA-based pruning** 表现极差（见 Table 5），验证了深度可分离卷积（depthwise convolution）破坏了其有效性
- 所有方法在 **50% 压缩比** 下均能保持接近原始模型的性能

---

### 消融实验结果

#### （1）耦合选择消融（Coupled Selection）
| 方法 | 选择策略 | Wiki PPL (370M) |
|------|----------|----------------|
| L1 | Keys Only | 425218.3 |
| L1 | Queries Only | 33.0 |
| L1 | **Keys & Queries** | 3843.5 |
| DRRQR | Keys Only | 286035.7 |
| DRRQR | Queries Only | 35.7 |
| DRRQR | **Keys & Queries** | **32.1** |
| Grad | Keys Only | 17709.6 |
| Grad | Queries Only | 31.9 |
| Grad | **Keys & Queries** | **18.3** |

> 🔍 **关键发现**：  
> - 幅度类方法（L1, S-Wanda）在仅使用 **queries** 时表现最好（因 queries 控制读取）  
> - 优化类方法（Grad, DRRQR）必须联合选择 **keys & queries** 才能达到最佳效果，说明它们捕捉到了读写交互机制

#### （2）秩利用率可视化（Figure 2）
- 在相同最大容量下，**Grad** 和 **DRRQR** 剪枝后的模型具有最高的 **rank utilization**
- 证明所提方法确实提升了内存的各向同性

#### （3）奇异值谱分析（Figure 3 & 4）
- 原始模型存在长尾小奇异值（接近零），表明大量维度冗余
- 剪枝后模型的谱更紧凑，且 RFT 过程会轻微“软化”截断效应

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Linear Attention 存在严重的低秩状态问题**，导致内存利用率低下。
2. ✅ **低秩结构会放大查询噪声**，影响检索准确性（理论证明见 Theorem 2.5）。
3. ✅ **可通过结构化剪枝安全移除约 50% 的 key/query 通道**，仅带来轻微性能下降。
4. ✅ **DRRQR 是一种无需梯度、可解释、且性能优异的剪枝方法**，通过显式优化秩利用率实现高效压缩。
5. ✅ **本文框架与 SliceGPT、SpinQuant 等方法互补**，可在不同层面协同优化。

### 方法的局限性
- **对检索密集型任务（如 SWDE, FDA）性能下降较明显**，尤其是在高压缩比下。
- **依赖于 depthwise convolution 的结构假设**，若模型使用共享卷积或其他结构需额外适配（见 Appendix B.6）。
- **目前仅适用于 Linear Attention 架构**，尚未扩展至标准 Transformer。

### 未来工作方向
- 将该框架应用于 **Hybrid Models**（如 Mamba + Attention），利用少量 Softmax Attention 层缓解检索性能损失。
- 探索 **动态通道选择机制**，在推理时根据输入自适应调整活跃通道。
- 结合 **量化（quantization）** 与 **结构化剪枝**，进一步提升端侧部署效率。
- 研究如何将 **DRRQR** 思想融入 **训练过程**，引导模型学习更高秩利用率的表示。

--- 

> 📌 **一句话总结**：  
> 本文从 **秩利用率** 视角揭示了 Linear Attention 的状态冗余本质，并提出 **DRRQR** 结构化剪枝方法，在仅轻微牺牲性能的前提下实现了高达 50% 的状态压缩与 34% 的加速，为高效部署提供了新路径。

</details>

---

### 9. [CoRe: Context-Robust Remasking for Diffusion Language Models](https://arxiv.org/abs/2602.04096)

**Authors**: Kevin Zhai, Sabbir Mollah, Zhenyi Wang, Mubarak Shah  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.04096v1  

#### Abstract
Standard decoding in Masked Diffusion Models (MDMs) is hindered by context rigidity: tokens are retained based on transient high confidence, often ignoring that early predictions lack full context. This creates cascade effects where initial inconsistencies misguide the remaining generation. Existing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：CoRe: Context-Robust Remasking for Diffusion Language Models**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**
标准的 **Masked Diffusion Models (MDMs)** 在推理过程中存在**上下文刚性（context rigidity）**问题：  
- 早期生成的 token 是在不完整、模糊的上下文中预测的，一旦被“解掩码”（unmasked），通常被视为固定不变的锚点。
- 即使后续上下文演化后该 token 已不再合理，模型也无法修正，导致错误级联（cascade effects），尤其在结构敏感任务（如代码生成）中严重影响输出质量。

现有基于**静态置信度**（如低概率、top-k margin）的修订策略（如 ReMDM）也存在问题：
- 它们依赖于 token 被首次采样时的置信分数，这些信号会变得“过时”（stale），无法反映当前上下文下的合理性。
- 高置信度的 token 可能已与当前语境冲突，但仍不会被重新评估。

---

### ✅ **提出了什么新方法或新思路**
提出 **Context-Robust Remasking (CoRE)** ——一种无需训练、在推理阶段进行动态修订的框架。

#### 核心思想：
> 不再问：“这个 token 当初是否不确定？”  
> 而是问：“**如果部分上下文被遮蔽，它是否仍然被强烈支持？**”

#### 创新机制：
- **将修订建模为鲁棒优化问题（robust optimization）**：寻找在最坏情况上下文扰动下仍不稳定的 token。
- **轻量级压力测试（lightweight stress test）**：
  - 在每一步选择一组候选 token（通过 top-2 margin 筛选）。
  - 同时将其周围上下文中的若干位置重新掩码，形成一个“扰动上下文” $ \tilde{y}^{(t)} $。
  - 计算每个候选 token 在此扰动下的**不稳定性得分（instability score）**：  
    $$
    l_i = -\log p_\theta(y_i | \tilde{y}^{(t)})
    $$
  - 得分越高，说明该 token 对上下文变化越敏感（即“脆弱”），应优先修订。

#### 修订操作：
- 将得分最高的 $ k_{\text{rm}} $ 个 token 用其在扰动上下文下的最大似然预测替换。
- 整个过程仅需一次额外前向传播，开销极小。

---

### ✅ **相比现有方法的优势**
| 维度 | CoRE | 传统方法（如 ReMDM-conf） |
|------|------|--------------------------|
| **信号来源** | 动态重评估当前上下文下的稳定性 | 依赖历史置信度（易过时） |
| **修订依据** | 上下文鲁棒性（robustness） | 局部不确定性（uncertainty） |
| **适用场景** | 特别适合结构强约束任务（如代码） | 易误判高置信错误 |
| **是否需训练** | ❌ 无需训练（training-free） | ❌ 也不需要（但效果差） |
| **计算效率** | 仅增加约 6% NFE（Network Function Evaluations） | 类似开销但收益更低甚至负向 |

> 🔥 **关键优势**：CoRE 能识别出那些“当初看起来很对，但现在已不合时宜”的 token，并主动修正，打破上下文刚性。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**
- **代码生成**：
  - **HumanEval**：函数级代码生成，评估 pass@1 准确率。
  - **MBPP (Mostly Basic Python Problems)**：基础编程题，greedy pass@1。
- **数学推理**：
  - **GSM8K**：小学数学应用题，严格匹配准确率。
  - **MATH**：复杂数学问题，采用 rule-based answer equivalence。
- **通用推理**：
  - **BBH (Big-Bench Hard)**：挑战性少样本推理任务，exact match。

---

### ⚙️ **实验设置**
- **模型**：LLaDA-Base-8B（基于 diffusion 的大语言模型）
- **扩散步数**：$ N = 128 $
- **生成长度**：$ L = 512 $
- **解码方式**：greedy decoding（避免 temperature 引入方差干扰结构一致性分析）
- **修订频率**：每 $ E=8 $ 步执行一次修订
- **修订窗口**：中间阶段 $ t/N \in [0.25, 0.75] $
- **候选集大小**：$ m = 32 $
- **每次修订数量**：$ k_{\text{rm}} = 1 $

> 💡 每次修订引入 **1 次额外前向传播**，总计算预算控制为 136 次前向传播用于公平比较。

---

### 🆚 **基线方法对比**
| 方法 | 描述 |
|------|------|
| **Low-Confidence Base** | 默认 LLaDA 策略，优先 unmask 低置信 token |
| **Top-k Margin Base** | 自适应策略，基于 top-2 概率差距选择 unmask 位置 |
| **+ ReMDM-conf [36]** | 当前最优的 training-free 修订方法，基于历史置信度 remask |
| **+ Random Remask** | 控制组：随机选择修订目标 |
| **+ Margin Remask** | 控制组：选择 margin 最小的 token 进行修订 |
| **+ CoRE (Ours)** | 本文方法：基于扰动上下文下的不稳定性得分 |

---

## 3. **主要实验结果和性能指标**

### 📊 **关键性能数据（来自 Table 1）**

| Method | GSM8K (%) | BBH (%) | HumanEval (%) | MBPP (%) |
|--------|-----------|--------|----------------|----------|
| Low-Confidence Base | 51.40 | 45.81 | 12.20 | 15.60 |
| + ReMDM-conf | 52.31 | 46.05 | 10.98 | **15.20** ⬇️ |
| **+ CoRE (Ours)** | **52.69** | **47.18*** | **17.07*** | **24.80*** |
| Top-k Margin Base | 50.27 | 48.33 | 17.07 | 21.20 |
| + ReMDM-conf | 51.78 | 46.31 | 14.02 | **14.80** ⬇️ |
| **+ CoRE (Ours)** | 51.40 | **49.01*** | **22.56*** | **29.60*** |

> ✅ 所有提升均具有统计显著性（*p < 0.05*）

#### 🔝 **最大提升**：
- **MBPP 上高达 +9.2% 的绝对增益**（从 21.20% → 29.60%）
- **HumanEval 提升超过 5.5 个百分点**
- 在 BBH 和 GSM8K 上也保持稳定增益或持平

---

### 🔍 **消融实验结果（Table 2 & Table 3）**

#### ▶️ **Table 2：验证选择信号的有效性**
| Method | MBPP (%) |
|--------|---------|
| Low-Confidence Base | 15.60 |
| + Random Remask | 16.60 |
| + Margin Remask | 17.40 |
| **+ CoRE (Ours)** | **24.80** |

> ❗ 结论：**单纯增加修订行为无意义**，只有 CoRE 的“不稳定性驱动”机制带来显著提升。

#### ▶️ **Table 3：计算资源公平对比（136 次前向传播）**
| Method | MBPP (%) |
|--------|---------|
| Low-Confidence Base (136 steps) | 15.40 |
| ReMDM-conf (136 steps) | 14.00 ⬇️ |
| **CoRE (128 + 8 aux)** | **24.80** |

> ✅ 即便给基线更多步数，也无法达到 CoRE 的性能；
> ❌ 更多步数反而可能加剧错误固化（ReMDM 性能下降）

---

### 🔎 **参数敏感性分析（Figure 2 & Table 4）**
- **最佳配置**：$ m=32, E=8 $
- 若 $ m=64 $（候选过多）→ 性能下降 → 因过度扰动破坏有效上下文，产生**假阳性修订**
- 若 $ E=16 $（修订太稀疏）→ 错失纠错机会
- **结论**：适度扰动 + 中频修订 = 最佳权衡

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **上下文刚性是 MDMs 的根本瓶颈**，尤其影响结构化输出（如代码）。
2. **传统的置信度启发式（confidence/margin）不足以捕捉上下文依赖性**，容易遗漏“高置信但错误”的 token。
3. **CoRE 通过扰动测试暴露“上下文脆弱”token**，实现了更智能的修订决策。
4. **在相同计算预算下，CoRE 显著优于所有 baseline**，尤其在 MBPP 上实现 **+9.2%** 的突破性提升。
5. **不稳定性得分是一个高质量的代理信号**，能精准分离稳定 vs 脆弱 token（见 Figure 3）。

---

### ⚠️ **方法的局限性**
- **依赖局部扰动近似**：无法穷举所有可能的上下文变化，worst-case objective 是近似的。
- **扰动范围有限**：同时掩码多个位置可能导致上下文崩溃，影响判断准确性（故需控制 $ m $）。
- **对语义灵活性任务增益较小**：如 GSM8K 改进有限，因其允许多种合理路径。
- **未解决事实正确性问题**：只增强内部结构一致性，不保证外部知识准确。

---

### 🔮 **未来工作方向**
- 将 CoRE 框架扩展至**多模态扩散模型**（如图文生成）。
- 探索结合**外部验证器**（verifier）或 reward model 来指导 remasking。
- 设计更高效的扰动策略（如自适应选择扰动位置）。
- 应用于**长文本生成**，研究如何在不同段落间传递上下文鲁棒性。
- 探索如何将此类机制融入训练过程，实现 end-to-end 的 robust diffusion learning。

---

## ✅ **总结一句话**
> **CoRE 提出了一种无需训练、基于上下文扰动测试的修订机制，打破了 MDMs 的“上下文刚性”，在代码等结构敏感任务上实现了高达 +9.2% 的性能飞跃，证明了“修订质量”比“修订次数”更重要。**

</details>

---

### 10. [BPDQ: Bit-Plane Decomposition Quantization on a Variable Grid for Large Language Models](https://arxiv.org/abs/2602.04163)

**Authors**: Junyu Chen, Jungang Li, Jing Xiong, Wenjie Wang, Qingyao Yang, He Xiao, Zhen Li, Taiqiang Wu, Mengzhao Chen, Zhen Peng, Chaofan Tao, Long Shi, Hongxia Yang, Ngai Wong  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.04163v1  

#### Abstract
Large language model (LLM) inference is often bounded by memory footprint and memory bandwidth in resource-constrained deployments, making quantization a fundamental technique for efficient serving. While post-training quantization (PTQ) maintains high fidelity at 4-bit, it deteriorates at 2-3 bits....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BPDQ: Bit-Plane Decomposition Quantization on a Variable Grid for Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的 **Post-Training Quantization (PTQ)** 在 4-bit 时表现良好，但在 **2–3-bit 超低精度**下性能严重下降。作者指出，根本原因并非优化目标失效，而是现有方法普遍采用 **shape-invariant quantization grid**（如 UINT2 的固定均匀区间），即每个量化组的量化级别相对间距模式是固定的（仅通过缩放因子调整），这极大地限制了误差最小化的可行解空间。

这种刚性结构在低比特下无法灵活适应不同权重组的分布特性，导致输出失真严重。

### 提出了什么新方法或新思路
提出 **Bit-Plane Decomposition Quantization (BPDQ)**，其核心思想是：
- **构建可变量化网格（variable quantization grid）**：通过 **bit-plane 分解** 和 **标量系数（scalar coefficients）** 构造每组独立的非均匀量化级别。
- **理论支持**：该变量网格在 **Hessian-induced geometry** 下扩展了可行解集，使量化过程能更一致地对齐输出重建目标。
- **迭代优化机制**：结合近似二阶信息（Hessian-aware error compensation）逐步精炼 bit-planes 和系数，并引入 **delta correction** 保证误差传播一致性。

具体公式如下：
$$
\hat{W} = \text{REP}(C_0) + \sum_{i=1}^k \text{REP}(C_i) \odot B_i
$$
其中 $ B_i \in \{0,1\} $ 是第 $ i $ 个 bit-plane，$ C_i $ 是每组的标量系数，REP 表示沿输入维度重复扩展。

### 相比现有方法的优势
| 方面 | BPDQ | GPTQ / AWQ |
|------|------|------------|
| **量化网格灵活性** | ✅ 可变网格，每组独立调整级别间距 | ❌ 固定模板（uniform/non-uniform），仅缩放 |
| **低比特性能** | ⭐️ 在 2-bit 下保持高保真度（如 Qwen2.5-72B 达到 83.85% GSM8K） | ❌ 2-bit 下严重退化（常低于 40%） |
| **硬件友好性** | ✅ 支持 bit-parallel 运算，可通过 LUT kernel 实现低延迟推理 | ✅ 同样支持 |
| **无需微调** | ✅ 纯 PTQ，不依赖 QAT 或 QAF | ✅ 多数为 PTQ |

> 💡 **核心洞见**：不是优化目标错了，而是刚性量化器限制了优化潜力 —— **变量网格释放了表达能力**。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **校准数据（Calibration Data）**：C4 数据集中的 1024 个样本，用于估计 Hessian 矩阵。
- **评估基准（Evaluation Benchmarks）**：
  - **语言理解与常识推理**：GSM8K（数学应用题）、MATH500（数学证明）、ARC-C（科学问答）
  - **自然语言推理**：BoolQ、HellaSwag
  - **综合知识**：MMLU
  - **长上下文任务**：LongBench（涵盖检索、摘要、代码补全等）
  - **困惑度测试**：WikiText-2

### 实验设置和评估指标
- **模型范围**：覆盖 Qwen-3 / Qwen2.5 系列（0.6B–72B）及 Ministral-3（3B, 8B）
- **量化配置**：
  - **比特宽度**：2-bit、3-bit、4-bit
  - **分组大小（group size）**：
    - GPTQ/AWQ：g=64（4-bit），g∈{32,64}（2/3-bit）
    - BPDQ：g=128（4-bit），g∈{64,128,256}（2/3-bit），以抵消额外系数开销
- **评估工具**：`lm-evaluation-harness`
- **评估指标**：
  - 主要：准确率（Accuracy on GSM8K, MMLU 等）
  - 辅助：VRAM 占用、量化时间、推理延迟、激活异常值保留情况

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **GPTQ** | Optimization-based PTQ | 使用 Hessian 近似进行逐层量化，硬件友好 |
| **AWQ** | Distribution-aware PTQ | 强调保护显著权重（outliers），混合精度 |
| **AnyBCQ** | Bit-plane Method | 最近提出的 bit-plane 量化方法，支持多精度 |
| **VPTQ** | Vector Quantization (VQ) | 高保真但计算代价极高（~40× GPTQ 时间） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Qwen2.5-72B 为例）

| 方法 | BPW | GSM8K Acc (%) | MMLU Acc (%) | VRAM (GB) | 设备 |
|------|-----|----------------|---------------|-----------|------|
| FP16 (Baseline) | 16 | 90.83 | 83.38 | - | - |
| GPTQ-W2-G64 | 2.28 | 40.49 | 62.18 | ~17 | 多卡 |
| AWQ-W2-G64 | 2.28 | 0.00 | 60.23 | ~17 | 多卡 |
| **BPDQ-W2-G256** | **2.19** | **83.85** | **75.89** | **22.69** | **单张 RTX 3090** |

> ✅ **突破性成果**：首次实现 **72B 模型在消费级 GPU 上 2-bit 高保真推理**

#### 更多亮点结果：
- 在 **Qwen2.5-7B** 上，BPDQ-W2-G128 达到 **35.48% GSM8K**，远超 GPTQ（0.00%）和 AWQ（0.00%）
- 在 **MMLU** 上，BPDQ 保持超过 91% 的原始性能（如 72B 模型达 75.89%）
- **BoolQ** 上甚至达到 **99.15%** 准确率，接近全精度水平

### 与基线方法的对比结果
| 维度 | 结果 |
|------|------|
| **2-bit 性能优势** | BPDQ 显著优于 GPTQ 和 AWQ，在所有模型规模上均取得最佳或次佳成绩 |
| **推理任务鲁棒性** | 尤其在 **GSM8K/MATH500** 等需要强推理的任务上，BPDQ 明显领先 |
| **长上下文能力** | 在 LongBench 的 PassageRetrieval 任务中，BPDQ 得分 **53.75%**，而 GPTQ 仅为 **4.98%**，显示更强的长程依赖保持能力 |
| **系统效率** | 推理延迟优于 GPTQ（尤其在 2/3-bit），量化时间约为 GPTQ 的 3 倍，远低于 VPTQ（~40×） |

### 消融实验与分析（关键发现）
| 分析项 | 发现 |
|--------|------|
| **Activation Outlier Statistics** | BPDQ 能有效保留关键激活异常值（outliers），其 DiagR 和 Cnt10 指标接近原始模型；而 GPTQ 在 2-bit 下严重抑制 outliers（△DiagR -32.89%，△Cnt10 -23.61%） |
| **变量网格有效性** | 理论证明 BPDQ 的可行集严格包含 UINT2 网格（Proposition 1），且存在开放集合使得 BPDQ 可实现更低误差（Proposition 2） |
| **Delta Correction 必要性** | 移除 delta correction 会导致误差传播状态不一致，性能下降明显 |

---

## 4. 关键结论和发现

### 主要发现
1. **根本瓶颈在于量化网格刚性**：低比特 PTQ 的失败主因不是优化目标无效，而是 **shape-invariant grid 限制了解空间**。
2. **变量网格显著提升表达能力**：BPDQ 通过 bit-plane + 系数的方式构造 per-group adaptive grid，理论上扩展了可行集维度（从 1D ray → 2D plane）。
3. **可在消费级设备部署 72B 模型**：BPDQ 实现 **Qwen2.5-72B 在单张 RTX 3090 上 2-bit 推理**，内存仅需 22.69GB，准确率高达 83.85%（GSM8K）。
4. **兼顾高效与高保真**：相比 VQ 方法（如 VPTQ），BPDQ 保真度略低但量化速度快一个数量级，更适合实际部署。

### 方法的局限性
- **仍存在保真度差距**：尽管大幅改进，BPDQ 在 2-bit 下仍未完全追平 VPTQ 的精度（后者通常有更高 overhead）。
- **存储开销略高**：由于每 bit-plane 需要独立的 scale coefficient，略微增加元数据体积（通过增大 group size 缓解）。
- **未探索旋转技术**：未结合 weight rotation（如 Quarot）进一步消除 outliers，可能限制极限压缩性能。

### 未来工作方向
1. **融合 Rotation 技术**：将 BPDQ 与 weight rotation（如 Quarot）结合，进一步减少 outlier 影响。
2. **集成高级求解器**：引入 Qronos 等具备历史误差修正能力的序列化求解器，进一步降低重建误差。
3. **定制硬件加速**：利用 bit-plane 的二进制特性，在 FPGA/ASIC 上实现极致能效比（替换浮点乘法为加法）。
4. **动态多精度服务**：基于同一模型按需启用不同数量 bit-planes，实现 **dynamic accuracy-latency trade-off**。

---

> 🔚 **总结一句话**：  
> **BPDQ 通过打破传统量化中的“形状不变性”约束，提出基于 bit-plane 的可变网格量化框架，在理论和实践上实现了 2-bit 大模型高保真推理的重大突破，推动了百亿参数模型在边缘设备上的落地可能性。**

</details>

---

### 11. [Scaling In-Context Online Learning Capability of LLMs via Cross-Episode Meta-RL](https://arxiv.org/abs/2602.04089)

**Authors**: Xiaofeng Lin, Sirou Zhu, Yilei Chen, Mingyu Chen, Hejian Sang, Ioannis Paschalidis, Zhipeng Wang, Aldo Pacchiano, Xuezhou Zhang  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04089v1  

#### Abstract
Large language models (LLMs) achieve strong performance when all task-relevant information is available upfront, as in static prediction and instruction-following problems. However, many real-world decision-making tasks are inherently online: crucial information must be acquired through interaction,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scaling In-Context Online Learning Capability of LLMs via Cross-Episode Meta-RL

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Large Language Models (LLMs)** 虽然在静态任务（如指令遵循、少样本推理）中表现优异，但在**在线决策任务**（online decision-making）中表现不佳。这类任务具有以下特点：

- **信息不完全**（部分可观测）
- **反馈延迟**
- 需要通过多轮交互逐步探索并利用所学知识
- 决策需在**探索（exploration）** 和 **利用（exploitation）** 之间权衡

尽管 LLMs 具备 **In-Context Learning (ICL)** 能力，但它们通常难以从跨回合的交互历史中有效学习策略，尤其是在面对**未见过的任务环境**时。

> ❗ 问题本质：如何让 LLM 在不更新参数的前提下，通过上下文记忆实现真正的“边推理边学习”（learn-at-inference-time）？

---

### 🚀 提出的新方法：ORBIT

作者提出 **ORBIT**（Online Reinforcement-Based In-Context Training），一种基于 **multi-task, multi-episode meta-Reinforcement Learning (meta-RL)** 的训练框架，旨在赋予 LLMs 更强的 **in-context online learning** 能力。

#### 核心思想：

- 将每个任务建模为一个有限回合的 **Markov Decision Process (MDP)**
- 在训练阶段，模型在多个 episode 中与同一任务交互，且**上下文窗口保留所有历史轨迹**
- 优化目标是最大化**跨 episode 的长期累积奖励**，而非单次 episode 成功
- 强调“学会如何学习”（learning to learn in context），即早期尝试用于收集信息，后期用于高效执行

#### 创新点：

| 创新维度 | 内容 |
|--------|------|
| **训练范式** | 首次系统性地将 **multi-episode meta-RL** 应用于预训练 LLM，使其具备跨 episode 的 in-context adaptation 能力 |
| **训练目标设计** | 使用 **trajectory-level success count** 作为稀疏奖励信号，避免 reward hacking 和任务间尺度不平衡 |
| **优化算法** | 采用 **Group Relative Policy Optimization (GRPO)**，仅依赖轨迹级回报进行策略更新，天然支持稀疏奖励和 outcome-driven 学习 |
| **简洁性** | 不引入外部记忆（external memory）、反思模块（reflection prompts）或复杂提示工程，纯粹依靠 meta-RL 实现能力涌现 |

---

### 🔍 相比现有方法的优势

| 方法 | 局限性 | ORBIT 的优势 |
|------|-------|-------------|
| **标准 ICL / Instruction Tuning** | 无法处理动态反馈和延迟奖励，缺乏主动探索机制 | 显式训练模型进行探索-利用权衡 |
| **传统 RL Fine-tuning** | 通常基于单 episode rollouts，忽略跨 episode 信息积累 | 支持跨 episode 策略演化，实现真正的 in-context 策略改进 |
| **其他 ICRL 方法**（如 PAPRIKA, Iterative RMFT） | 多数在已知任务上微调，泛化能力弱；或依赖人工构造偏好 | 在**完全未见任务**上验证，强调跨任务迁移能力 |
| **Reflection-based Agents**（如 Reflexion） | 依赖显式“反思”提示或额外模块 | 反思行为**自发涌现**于策略中，无需额外设计 |

> ✅ ORBIT 的最大优势：**训练后的小型开源模型（Qwen3-14B）在未见任务上的 in-context online learning 表现媲美 GPT-5.2**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与任务

共使用 **7 个部分可观测的决策任务**，分为训练集和测试集：

| 类型 | 任务 | 描述 |
|------|------|------|
| **训练任务** | RPS, Minesweeper, Hangman, Wordle, Blackjack | 多样化的序列决策任务，涵盖概率推理、逻辑推断、记忆依赖等 |
| **测试任务** | **Maze**, **Mastermind** | **完全未在训练中出现**，用于评估泛化能力 |

> 所有任务均为 episodic，每轮 reset 环境但保持规则不变，agent 可通过多轮尝试积累经验。

---

### ⚙️ 实验设置

| 组件 | 设置 |
|------|------|
| **基础模型** | Qwen3-{4B, 8B, 14B}（主要用 8B） |
| **上下文长度** | 最大 32,768 tokens（限制每实例最多 3 个 episode） |
| **训练方式** | ORBIT：multi-episode meta-RL，HT = 3 episodes per task |
| **奖励设计** | 二值完成奖励（0/1），按 trajectory 统计成功次数 |
| **优化器** | GRPO，batch size=64，K=4 trajectories per group |
| **训练步数** | 100 optimization steps，lr=1e-6 |
| **推理配置** | temperature=0.6, top_p=0.95，启用 thinking mode |

---

### 🎯 评估指标

| 指标 | 定义 | 目的 |
|------|------|------|
| **Success Rate per Episode** | 第 e 轮的成功率 | 衡量是否随 episode 提升（体现 in-context learning） |
| **Episode-3 Success Rate** | 第三轮成功率 | 对比不同方法的最终适应能力 |
| **In-Context Regret** | $ \text{Reg}(M) = T \cdot J^*(M) - \mathbb{E}[\sum G^{(e)}] $ | 衡量累计性能差距，越小越好 |
| **△States (Exploration under Failure)** | 失败后新增探索的状态数 | 量化探索行为的多样性 |

> 所有评估均在 **256 个独立的 Maze 和 Mastermind 实例**上进行，确保无过拟合。

---

### 🆚 基线方法对比

| 基线 | 描述 |
|------|------|
| **Base Model** | 未经任何 RL 微调的原始 Qwen3 |
| **Standard RL Fine-tuning** | 单 episode RL 微调，仅优化当轮成功概率 |
| **GPT-4o / GPT-5.2** | 商业闭源模型，作为性能上限参考 |
| **Oracle Algorithms** | 针对 Maze 和 Mastermind 设计的最优算法（见 Appendix C） |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Figure 1 & Table 2）

#### 在 **Maze** 上的表现（Episode 3 成功率）：

| 方法 | 成功率 | 相对 Base 提升 |
|------|--------|----------------|
| Base Model | 0.22 | — |
| Standard RL | 0.45 | +0.23 |
| **ORBIT (8B)** | **0.55** | **+0.33** |
| **ORBIT (14B)** | **0.70+**（趋势上升） | — |

#### 在 **Mastermind** 上的表现（Episode 3 成功率）：

| 方法 | 成功率 | 相对 Base 提升 |
|------|--------|----------------|
| Base Model | 0.27 | — |
| Standard RL | 0.21 | -0.06（退化） |
| **ORBIT (8B)** | **0.59** | **+0.32** |

> ✅ ORBIT 在两个未见任务上均显著优于 base model 和 standard RL，且 **14B 版本接近 GPT-5.2 性能**

---

### 📈 与基线方法的对比结果

| 观察 | 结论 |
|------|------|
| **ORBIT vs Base Model** | 成功率大幅提升，且呈现**单调上升趋势**，表明有效利用历史信息 |
| **ORBIT vs Standard RL** | Standard RL 在早期略有提升，但后续停滞甚至下降；而 ORBIT 持续改进，说明其真正掌握了 in-context adaptation |
| **ORBIT vs GPT-4o/GPT-5.2** | Qwen3-14B + ORBIT **匹配 GPT-5.2 表现**，远超 GPT-4o |
| **Scaling Effect** | 模型越大（4B → 8B → 14B），episode 2 和 3 提升越明显，显示可扩展潜力 |

> 🔥 图 5 显示：**更大的模型更倾向于在前几轮主动探索**，后期再高效利用，体现出“智能分配探索预算”的能力。

---

### 🔬 消融实验与定性分析（Qualitative Evidence）

#### （1）探索行为量化（Table 4）

| 方法 | △States (Ep2 \| F1) | △States (Ep3 \| F1-2) |
|------|---------------------|------------------------|
| Base | 1.64 | 0.94 |
| RL | 4.11 | 1.27 |
| **ORBIT** | **4.69** | **1.48** |

> ORBIT 在失败后探索更多新状态，说明其学会了“换条路试”。

#### （2）定性案例（Table 3 & Figure 4）

- 在 Maze 任务中，ORBIT 在 Episode 3 主动总结前两轮失败：“*Previously I went up and failed… this time I’ll explore right.*”
- 动作选择发生改变，最终成功抵达目标
- 此类“反思-调整”行为**未通过 prompt 引导**，而是训练中自发涌现

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **LLMs 可以通过 meta-RL 获得强大的 in-context online learning 能力**  
   → ORBIT 成功教会模型“从失败中学习”，即使面对全新任务也能快速适应。

2. **multi-episode meta-RL 是实现该能力的关键机制**  
   → 单 episode RL 无法建立跨 episode 的策略演化，而 ORBIT 显式优化长期收益。

3. **能力可迁移且可扩展**  
   → 在完全未见任务（Maze, Mastermind）上表现优异  
   → 随模型规模增大持续提升，暗示未来仍有巨大潜力

4. **高级行为（如探索、反思）可以自发涌现**  
   → 无需外部 memory 或 reflection prompt，模型自行生成总结与策略调整

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **上下文长度限制** | 当前受限于 32k token，最多支持 3 个 episode；更长 horizon 下可能失效 |
| **训练任务数量有限** | 仅使用 5 个训练任务，泛化边界尚不明确 |
| **计算资源要求高** | GRPO 需要多轨迹采样，训练成本较高 |
| **未支持连续动作空间** | 当前仅适用于离散动作任务 |

---

### 🔮 未来工作方向

1. **扩展到更长 horizon 和更大 context window**  
   → 结合 MemGPT、Retrieval-Augmented Generation 等技术突破长度瓶颈

2. **增加训练任务的多样性和复杂度**  
   → 探索更复杂的规划、工具调用、多智能体协作场景

3. **研究更高效的 meta-RL 算法**  
   → 如结合价值函数估计、credit assignment 技术提升训练稳定性

4. **构建通用的 in-context RL benchmark**  
   → 推动社区对 LLMs 在线学习能力的标准化评估

---

> 💡 **一句话总结**：  
> ORBIT 证明了通过简单的 multi-episode meta-RL 训练，即可让小型开源 LLM 在未见任务上实现媲美 GPT-5.2 的 in-context online learning 能力，为构建“会学习的 AI Agent”提供了可行路径。

</details>

---

### 12. [ReThinker: Scientific Reasoning by Rethinking with Guided Reflection and Confidence Control](https://arxiv.org/abs/2602.04496)

**Authors**: Zhentao Tang, Yuqi Cui, Shixiong Kai, Wenqian Zhao, Ke Ye, Xing Li, Anxin Tian, Zehua Pei, Hui-Ling Zhen, Shoubo Hu, Xiaoguang Li, Yunhe Wang, Mingxuan Yuan  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04496v1  

#### Abstract
Expert-level scientific reasoning remains challenging for large language models, particularly on benchmarks such as Humanity's Last Exam (HLE), where rigid tool pipelines, brittle multi-agent coordination, and inefficient test-time scaling often limit performance. We introduce ReThinker, a confidenc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReThinker: Scientific Reasoning by Rethinking with Guided Reflection and Confidence Control  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前的 **Large Language Models (LLMs)** 在专家级科学推理任务上表现仍不理想，尤其是在如 **Humanity's Last Exam (HLE)** 这类高难度基准上。传统方法存在以下瓶颈：
- **刚性的工具流水线**（rigid tool pipelines）导致灵活性不足；
- **多智能体协作脆弱**（brittle multi-agent coordination），容易因单点错误崩溃；
- **测试时扩展效率低**（inefficient test-time scaling），难以动态分配计算资源。

这些问题限制了模型在复杂科学问题中的深度推理能力。

### 提出了什么新方法或新思路
作者提出 **ReThinker** ——一种基于信心感知（confidence-aware）的智能体框架，采用 **阶段式 Solver-Critic-Selector 架构** 来协调检索、工具使用和多智能体推理。

其三大核心技术创新为：
1. **自动化轨迹合成用于反思监督**（Automated Trajectory Synthesis for Rethinking Supervision）
   - 完全无需人工标注，通过 LLM 多智能体系统自动生成高质量的专家级 QA 对；
   - 利用网络上下文提取领域概念，并记录完整的多阶段推理轨迹，仅保留验证正确的路径作为训练数据。

2. **引导式反射的混合扩展架构**（Hybrid Scaling with Guided Reflection）
   - 引入 **summary-and-guidance 模块**，对完整推理轨迹进行结构化总结，识别逻辑、策略和知识层面的具体缺陷；
   - 支持 Python 执行、网页搜索与解析等工具集成，实现定量验证与专业知识获取；
   - Solver 阶段支持多轮迭代优化，避免一次性推理偏差。

3. **基于不确定性聚合的信心控制选择机制**（Confidence-Controlled Selection via Uncertainty Aggregation）
   - 在 Selector 阶段引入 **perplexity-based 内部一致性度量**，跨多轮选择聚合置信分数；
   - 使用 **Latin Square 设计打乱候选顺序**，消除位置偏见；
   - 最终通过一致投票或仲裁决定最终答案，提升决策稳定性。

### 相比现有方法的优势
| 维度 | ReThinker 优势 |
|------|----------------|
| **推理质量** | 动态重思考机制显著减少低级错误，提升逻辑严谨性 |
| **工具利用** | 自适应调用工具，避免冗余或无效调用 |
| **鲁棒性** | 多维度反思 + 置信加权选择，抗噪声能力强 |
| **可扩展性** | 无需人工标注即可生成高质量训练数据，适合大规模部署 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个具有挑战性的专家级推理基准上进行：

| 数据集 | 描述 |
|--------|------|
| **HLE (Humanity's Last Exam)** | 包含 2,158 个文本题，覆盖数学、物理、化学等多个学科，强调深层因果推理与精确逻辑推导；76%为精确匹配短答，24%为多选题。 |
| **GAIA** | 包含 103 个真实世界任务，需结合网页导航、计算器、多步规划完成，分为三级难度（Level 1–3）。 |
| **XBench-DeepSearch** | 含 100 个专业对齐的深搜任务，要求跨源信息整合与复杂搜索，涵盖商业、科技、娱乐等领域。 |

### 实验设置和评估指标
- **评估协议**：采用 **LLM-as-a-Judge** 框架：
  - HLE 使用 `o3-mini-2025-01-31` 判断；
  - GAIA 和 XBench 使用 `gpt-4.1-2025-04-14`。
- **主干模型**：分别基于 **Gemini-3-Pro** 和 **OpenPangu-72B** 实现 ReThinker。
- **关键参数**：
  - 并行路径数：5
  - 上下文长度：128K tokens
  - 温度：1.0，Top-p：全局 1.0 / Selector 中 0.8
  - 最大交互步数：50

### 基线方法对比
| 类别 | 基线方法 |
|------|---------|
| **基础模型 + 工具** | GPT-5-high, Gemini-3-Pro, GLM-4.6, DeepSeek-V3.2 |
| **推理框架** | WebExplorer, OpenAI DeepResearch, Kimi Researcher, Tongyi DeepResearch, MiroThinker-v1.0 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | HLE Score (%) | GAIA (%) | XBench-DeepSearch (%) |
|------|---------------|----------|------------------------|
| GPT-5-high | 35.2 | 76.4 | 77.8 |
| Gemini-3-Pro | 38.3 | 79.0 | 87.0 |
| MiroThinker-v1.0 | 33.4 | 73.5 | 70.6 |
| Tongyi DeepResearch | 32.9 | 70.9 | 75.0 |
| **ReThinker (OpenPangu-72B)** | **33.1** | **72.8** | **78.0** |
| **ReThinker (Gemini-3-Pro)** | **52.2** | **81.6** | **90.0** |

> ✅ **ReThinker (Gemini-3-Pro)** 在所有三项基准上均达到 **state-of-the-art 性能**。

#### 具体提升幅度：
- 在 **HLE** 上相比 Gemini-3-Pro 提升 **+13.9pp**，相比 GPT-5-high 提升 **+16.9pp**；
- 在 **GAIA** 上超越最强基线 **+2.6pp**；
- 在 **XBench** 上达到 **90.0%**，领先第二名 **3.0pp**。

### 消融实验结果（Component Analysis）

#### Solver 阶段：重回答合成的作用
| 配置 | Pass@5 |
|------|--------|
| 初始 Solver | 38.00% |
| Re-Answer Solver | 39.40% |

👉 贡献 +1.4%，虽小但有效降低下游模块负担。

#### Critic 阶段：引导式反射的效果
| 设置 | Pass@5 |
|------|--------|
| Re-Answer Solver | 39.40% |
| + Final Answer Only | 40.40% |
| + Summary Only | 42.00% |
| **+ Summary & Guidance** | **43.20%** |

👉 **结构化引导是最大贡献者**，带来 **+3.8%** 提升。

#### Selector 阶段：信心引导选择的增益
| 阶段 | Hit Rate | Pass@1 |
|------|---------|--------|
| 初始判断 | 65.27% | 28.20% |
| + 迭代判断 | 68.05% | 29.40% |
| + Perplexity 指导 | 69.44% | 30.00% |
| + Latin Square 排序 | **70.83%** | **30.60%** |

👉 显示出各组件互补效应，尤其 **perplexity 是可靠的不确定性信号**。

---

## 4. 关键结论和发现

### 主要发现
1. **动态重思考优于固定流程**：ReThinker 不依赖单一推理路径，而是通过多轮迭代、反思与选择，显著提升复杂问题解决能力。
2. **信心控制至关重要**：基于 **perplexity 的置信估计** 可有效识别不确定案例并触发额外计算，实现“按需扩展”。
3. **位置偏见不可忽视**：未使用 Latin Square 时，模型易受候选顺序影响；引入后可消除该偏差。
4. **强基底模型放大收益**：当从 OpenPangu-72B 升级到 Gemini-3-Pro 时，HLE 分数从 33.1 → 52.2，说明 ReThinker 能有效放大先进模型潜力。

### 方法的局限性
1. **延迟增加**：由于三阶段串行流程，wall-clock time 比单次推理基线约增加 **1.5×**。
2. **上下文窗口限制**：尽管支持 128K，但在极长程科学任务中仍可能不足。
3. **通用工具依赖**：目前仅使用 web_search、web_parse 和 execute_python_code，缺乏领域专用工具（如符号定理证明器、分子性质预测器）。

### 未来工作方向
- **并行化阶段执行**：探索 Solver/Critic/Selector 的部分并行化以降低延迟；
- **动态上下文管理**：开发更高效的上下文压缩与检索机制；
- **集成专用工具链**：将 symbolic solvers、数据库查询引擎等嵌入框架，增强特定领域推理能力；
- **轻量化版本设计**：研究适用于边缘设备的小规模 ReThinker 变体。

---

> 📌 **总结一句话**：  
> **ReThinker 通过“反思—指导—信心控制”的闭环机制，在无需人工标注的前提下实现了专家级科学推理能力的显著突破，代表了新一代 agentic reasoning 框架的重要进展。**

</details>

---

### 13. [CoLT: Reasoning with Chain of Latent Tool Calls](https://arxiv.org/abs/2602.04246)

**Authors**: Fangwei Zhu, Zhifang Sui  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04246v1  

#### Abstract
Chain-of-Thought (CoT) is a critical technique in enhancing the reasoning ability of Large Language Models (LLMs), and latent reasoning methods have been proposed to accelerate the inefficient token-level reasoning chain. We notice that existing latent reasoning methods generally require model struc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**CoLT: Reasoning with Chain of Latent Tool Calls**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

传统的 **Chain-of-Thought (CoT)** 推理虽然提升了大语言模型（LLMs）在复杂任务上的表现，但其逐 token 生成推理链的方式导致推理过程冗长、计算成本高。为解决效率问题，已有研究提出在**连续隐空间（latent space）**中进行推理（如 Coconut、CODI），即“隐式 CoT”（implicit CoT）。然而，这些方法通常需要对模型结构进行大幅修改，并依赖大量训练，限制了其通用性和实用性。

此外，完全在隐空间中推理会牺牲可解释性——人类难以理解向量形式的中间步骤。

### ✅ 提出了什么新方法或新思路

本文提出了 **CoLT（Chain-of-Latent-Tools）** 框架，将隐式推理建模为一种**参数化的工具调用（parametric tool call）**，核心思想如下：

- 不再让主模型完全在隐空间中推理，而是引导其生成特殊的“种子 token”（seed tokens），这些 token 的隐藏状态（hidden states）编码了一个完整推理步骤的信息。
- 当主模型生成一个以 `<TRG>` 结尾的 seed token 序列时，系统触发一个外部的、**可微分的解码器（decoder）**，将这些隐藏状态解码回正常的文本 token。
- 主模型继续基于扩展后的上下文进行推理，形成“生成 seed → 调用 decoder 扩展 → 继续推理”的循环。

该框架的关键创新在于：
- 将隐式推理视为“工具调用”，实现了**显式文本推理与隐式压缩的结合**。
- 外部 decoder 是**可微分神经模块**，支持端到端训练和梯度反传，允许联合优化主模型与 decoder。
- 主模型始终在**原始文本空间中操作**，保留了其预训练能力与推理可解释性。

### ✅ 相比现有方法的优势

| 方面 | CoLT 的优势 |
|------|------------|
| **模型兼容性** | 无需修改主模型结构，适用于标准 decoder-only 架构（如 Llama、Qwen）。 |
| **训练效率** | 仅需 2 个 epoch 的监督训练即可超越需复杂训练流程的 baseline。 |
| **可解释性** | 最终推理链由真实文本 token 构成，而非黑盒向量。 |
| **强化学习兼容性** | 支持采样的 decoder 可使整个过程变为多轮对话，天然适配 GRPO 等 RL 算法。 |
| **灵活性** | 支持多种 decoder 结构（Transformer、RNN、multi-hot），易于扩展。 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **GSM8k-Aug**：GSM8k 的增强版，使用数学表达式（如 `《25/5=5》`）替代自然语言推理步骤，作为**领域内（in-domain）基准**。
- **GSM-Hard**：GSM8k 测试集的困难版本，使用更大数字增加难度。
- **SVAMP** 和 **MultiArith**：小学算术文字题数据集，用于评估**跨域泛化能力**。
- **MATH**：更具挑战性的数学问题数据集，用于验证强化学习效果。

### ⚙️ 实验设置和评估指标

#### 主干模型
- 使用 **Llama-3.2-1B-Instruct** 和 **DeepSeek-R1-Distill-Qwen-1.5B** 作为 backbone。
- CoLT 模型从原始预训练权重开始训练，不依赖 SFT 初始化。

#### Seed Tokens
- 使用特殊 token 表示：
  - `<BDY>`：body token，携带信息。
  - `<TRG>`：trigger token，指示调用 decoder 并选择具体 decoder 类型。

#### Decoder 设计
- 默认使用 **1 层 Transformer decoder**，参数初始化自主模型前几层。
- 支持其他结构：RNN decoder、multi-hot decoder（仅解码数字）。

#### 训练方式
- **监督训练（SFT）**：2 个 epoch，AdamW 优化器，学习率 1e-5。
- **强化学习（RL）**：采用 **Group Relative Policy Optimization (GRPO)**，在 SFT 后进行 1 个 epoch 的 RL 微调。

#### 评估指标
| 指标 | 含义 |
|------|------|
| **Accuracy (Acc.)** | 正确解答问题的比例，衡量推理能力。 |
| **Reasoning chain length (#L)** | 推理链长度（考虑 seed + 1 的公平计算），衡量推理效率。 |

### 🔁 基线方法对比

| 方法 | 类型 | 特点 |
|------|------|------|
| **CoT** | 显式推理 | 逐 token 生成完整推理链，准确但低效。 |
| **iCoT / Coconut** | 隐式推理 | 用 latent token 替代推理步骤，需 curriculum learning。 |
| **CODI / COLAR / SIM-CoT** | 高级隐式推理 | 引入自蒸馏、分布建模或步级监督，提升稳定性。 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| 方法 | GSM8k-Aug Acc. | #L | GSM-Hard Acc. | #L | Average Acc. | Avg #L |
|------|----------------|-----|---------------|-----|----------------|--------|
| CoT | 49.4 | 25.6 | 11.9 | 34.2 | 53.6 | 21.4 |
| COLAR(2x) | 40.1 | 12.7 | 9.08 | 14.0 | 48.8 | 10.0 |
| **CoLT (1seed)** | **45.3** | **7.73** | **10.3** | **7.74** | **49.3** | **6.33** |
| **CoLT (2seed)** | **45.5** | 10.84 | **10.8** | 10.61 | **49.6** | 8.70 |

> ✅ **CoLT 在平均准确率上接近 CoT，显著优于所有隐式方法；同时推理链长度仅为 CoT 的 ~30%，远短于 COLAR 等方法。**

### 🔍 与基线方法的对比结果

- **相比 COLAR(2x)**：CoLT 准确率高出约 **5%**，且推理长度更短（7.73 vs 12.7）。
- **相比 SIM-CoT**：CoLT 在可比较任务上表现更好，且无需报告 out-of-domain latent length。
- **在 MultiArith 上**：CoLT(2seed) 达到 **93.9%**，甚至超过监督式 CoT（93.2%），显示强大泛化能力。

### 🔬 消融实验结果（Ablation Study）

#### （1）Decoder 层数与 Seed 数量的影响（Figure 2）
- 增加 decoder 层数或 seed 数量可小幅提升准确率（最高达 2% 差距），但收益递减。
- 更大的 decoder 带来更高计算开销，因此默认使用最小配置（1 层 + 1 seed）。

#### （2）不同 decoder 结构对比（Table 2）

| Decoder 类型 | Average Acc. | Avg #L |
|--------------|---------------|--------|
| Transformer（默认） | **49.3** | **6.33** |
| RNN | 30.9 | 6.55 |
| Multi-hot* | 28.2 | 32.3 |

> ✅ Transformer decoder 性能最优；RNN 有一定可行性；multi-hot 因功能受限表现差，但证明了非 Transformer decoder 的可能性。

#### （3）强化学习效果（Table 3 & 5）

| 方法 | GSM8k-Aug Acc. | #L |
|------|----------------|-----|
| CoLT (SFT) | 45.3 | 7.73 |
| **CoLT + RL** | **48.9** | 7.73 |

> ✅ 强化学习进一步将准确率提升至 **48.9%**，接近 CoT 水平，且未增加推理长度。

在 MATH 数据集上也观察到类似趋势，说明 CoLT 兼容 RL 且能通过探索改进策略。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **隐式推理可以被建模为“工具调用”**：通过 seed token 和可微 decoder 的组合，实现高效、可解释、可训练的推理加速。
2. **主模型无需进入隐空间**：保持在文本 token 空间中推理，既保留了预训练能力，又提高了鲁棒性和泛化性。
3. **极简训练即可超越复杂方法**：仅 2 轮 SFT 即超越需复杂训练流程的 baseline。
4. **天然支持强化学习**：通过采样机制构建多路径推理，成功应用 GRPO 提升性能。
5. **decoder 结构具有探索空间**：尽管 Transformer 最优，但 RNN 等替代方案仍具潜力，尤其在特定任务中。

### ⚠️ 方法的局限性

- **当前聚焦数学推理任务**：seed token 的粒度（如每条公式）在 GSM8k-Aug 中明确，但在更复杂任务中如何划分“合理推理步”尚不清晰。
- **seed token 容量有限**：单个或少量 token 可能无法承载过长或复杂的推理内容。
- **decoder 设计尚未充分探索**：目前 decoder 多模仿主模型结构，未来可设计更轻量或专用结构。
- **实验集中在中小规模模型**：在更大模型（如 70B+）上的扩展性和稳定性有待验证。

### 🔮 未来工作方向

1. **探索更优 decoder 结构**：针对实体检索、代码生成等任务设计专用 decoder。
2. **动态决定 tool call 边界**：让模型自动判断何时压缩、压缩多少内容。
3. **跨模态扩展**：在 MLLM 中引入“latent visual tool calls”，用隐向量触发图像生成或视觉推理。
4. **中间层 hidden states 利用**：尝试从非最后一层提取 seed embeddings，可能捕捉更丰富语义。
5. **应用于非数学类复杂推理任务**：如逻辑推理、科学问答、规划等。

---

> 💡 **总结一句话**：  
> **CoLT 提出了一种新颖而实用的框架，将隐式推理转化为“可微工具调用”，在不牺牲可解释性和预训练能力的前提下，显著提升了推理效率与准确性，且天然兼容强化学习，为高效、可控的 LLM 推理提供了新范式。**

</details>

---

### 14. [Guided Verifier: Collaborative Multimodal Reasoning via Dynamic Process Supervision](https://arxiv.org/abs/2602.04290)

**Authors**: Lingzhuang Sun, Ruitong Liu, Yuxia Zhu, Xiaohan Xu, Jingxuan Wei, Xiangxiang Zhang, Bihui Yu, Wentao Zhang  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04290v1  

#### Abstract
Reinforcement Learning (RL) has emerged as a pivotal mechanism for enhancing the complex reasoning capabilities of Multimodal Large Language Models (MLLMs). However, prevailing paradigms typically rely on solitary rollout strategies where the model works alone. This lack of intermediate oversight re...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Guided Verifier: Collaborative Multimodal Reasoning via Dynamic Process Supervision

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Reinforcement Learning (RL)** 的多模态大语言模型（**Multimodal Large Language Models, MLLMs**）推理框架（如 **GRPO**）通常采用“孤立生成”（solitary rollout）策略，即模型独立完成整个推理链后才获得最终奖励信号。这种**延迟且稀疏的终端反馈**导致以下问题：
- **错误传播（error propagation）**：早期逻辑偏差会累积并导致不可逆的失败。
- **噪声优化信号**：大量无效轨迹降低了训练稳定性。

### 提出的新方法
为解决上述问题，作者提出 **Guided Verifier 框架**，其核心思想是将推理过程从“单向探索”转变为“协作闭环”系统。该框架包含两个关键组件：

1. **动态验证器（Dynamic Verifier）**  
   一个轻量级的 **Verifier** 模型在推理过程中实时与主策略模型（Policy Model）交互，检测中间步骤中的不一致，并提供方向性指导信号，引导策略模型走向正确的推理路径。

2. **协同推理机制（Collaborative Inference）**  
   推理不再是单向生成，而是 **Policy** 和 **Verifier** 双方的多轮对话。Verifier 在每一步提供反馈（如纠正错误、确认正确步骤），形成一个**闭环控制系统**。

### 相比现有方法的优势
- **主动纠错**：不同于传统 **Reward Model** 仅作为事后评判者，Verifier 是推理过程的**积极参与者**，能实时干预。
- **缓解错误传播**：通过中间监督，有效抑制了错误的累积。
- **高质量学习信号**：即使初始轨迹有误，Verifier 的引导也能将其修正为有效样本，从而扩展了可学习的轨迹空间。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据集**：
  - **CoRe Dataset**：作者构建的专门用于训练 Verifier 的合成数据集，包含约 2,792 条多轮对话轨迹，涵盖 26,360 个步骤级别的监督信号（含正负样本）。
  - **Geometry-3k**：用于 **Guided-GRPO** 阶段的 RL 训练。
- **评估基准（Benchmarks）**：
  - **MathVista**：评估数学视觉推理能力。
  - **MathVerse**：细粒度测试不同信息密度下的表现（文本主导、视觉密集等）。
  - **MMMU**：综合评估多模态理解与推理的通用能力。

### 实验设置和评估指标
- **模型初始化**：Policy 和 Verifier 均基于 **Qwen3-VL-8B-Instruct** 初始化。
- **训练流程三阶段**：
  1. **CoRe 数据合成**：模拟 Solver 与 Guide 的多轮对话，生成带幻觉标注的训练数据。
  2. **Verifier SFT**：在 CoRe 数据上对 Verifier 进行监督微调（Supervised Fine-Tuning）。
  3. **Guided-GRPO 训练**：冻结 Verifier，与 Policy 协同进行强化学习训练。
- **评估指标**：主要使用准确率（Accuracy %）进行比较。

### 基线方法对比
- **闭源模型（Proprietary Models）**：
  - GPT-4o, Gemini-2.5-Pro, Claude-4-Sonnet, Qwen-VL-Max
- **开源模型（Open-Source Models）**：
  - Qwen2.5-VL-32B, InternVL2.5-26B, LLaVA-v1.5-13B, Phi-3-vision-128k 等。
- **方法学基线（Methodological Baselines）**：
  - **Base (Qwen3-8B)**：零样本基线。
  - **Vision-R1-32B, MMR1-32B, VL-Rethinker-7B**：其他 GRPO 方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在多个基准上的准确率表现如下（摘自 Table 1）：

| Model | MathVerse (Overall) | MathVista (Overall) | MMMU (Val) |
|-------|---------------------|---------------------|-----------|
| **GPT-4o** | 48.88 | 69.23 | 67.33 |
| **Gemini-2.5-Pro** | 50.76 | 79.33 | 78.78 |
| **Ours (Guided-GRPO)** | **51.07** | **77.88** | **72.11** |
| **Base (Qwen3-8B)** | 46.83 | 67.31 | 62.44 |

> ✅ **结果亮点**：提出的 **8B 参数模型**在 **MathVerse** 上超越了 GPT-4o，在 **MathVista** 和 **MMMU** 上接近 Gemini-2.5-Pro，显著优于其他开源模型。

### 与基线方法的对比结果
- **全面领先**：Guided-GRPO 在所有基准上均优于同规模或更大规模的开源模型（如 Qwen2.5-VL-32B）。
- **小模型媲美大模型**：8B 模型的表现甚至超过了部分 32B 以上的模型，证明了**计算资源分配给协作推理比单纯扩大模型规模更高效**。

### 消融实验结果
通过 Table 2 的消融研究，验证了各组件的贡献：

| 设置 | MathVista 准确率 |
|------|----------------|
| **Qwen3-VL-8B (Base)** | 67.31 |
| + 未训练的 Verifier (Naive) | 69.75 |
| + SFT 训练的 Verifier | 70.67 |
| + **Guided-GRPO (完整方法)** | **77.88** |

> 🔍 **关键发现**：
> - 仅引入 Verifier 而不进行专门训练，提升有限。
> - **Verifier 的 SFT 训练至关重要**，使其具备有效的纠错能力。
> - **Guided-GRPO 训练是最大增益来源**，表明策略模型需要在 RL 中内化来自 Verifier 的指导信号。

---

## 4. 关键结论和发现

### 主要发现
1. **闭环协作优于开环生成**：Guided Verifier 将推理过程从“盲目探索”转变为“有指导的导航”，显著提升了长程推理的可靠性。
2. **Verifier 能力需专门训练**：更强的推理模型（如 RL 训练后的 Policy）并不天然成为更好的 Verifier。**验证能力是一种正交技能**，必须通过专门的 **Guide-and-Correction** 协议进行对齐。
3. **计算效率更高**：尽管双模型协作增加了 token 消耗，但其带来的性能提升远超成本。相比盲目扩大模型参数，**将算力用于动态验证更具性价比**。

### 方法的局限性
1. **Verifier 自身可能产生幻觉**：如果 Verifier 错误地否定了正确的推理步骤（假阳性），可能导致策略模型被误导（见 **Failure Mode I: Misguided Correction**）。
2. **认知僵化（Cognitive Rigidity）**：Verifier 可能只接受自己预设的解题路径，拒绝其他同样正确的替代方案，导致不必要的交互循环（见 **Failure Mode II: Verification Inefficiency**）。
3. **依赖高质量合成数据**：Verifier 的有效性高度依赖于 **CoRe 数据集**的质量，而该数据集的构建本身需要精心设计。

### 未来工作方向
- 提高 Verifier 的鲁棒性，减少假阳性，避免“有毒指导”。
- 设计更灵活的 Verifier，能够识别多种等效的正确解法路径。
- 将该框架扩展到更多任务领域（如代码生成、科学推理等）。
- 探索更高效的双模型协同架构，进一步降低推理延迟。

> 💡 **总结**：Guided Verifier 框架通过引入**动态协作机制**，从根本上改变了 MLLMs 的推理范式，为解决复杂多步推理中的错误传播问题提供了新的有效路径。

</details>

---

### 15. [Model-Dowser: Data-Free Importance Probing to Mitigate Catastrophic Forgetting in Multimodal Large Language Models](https://arxiv.org/abs/2602.04509)

**Authors**: Hyeontaek Hwang, Nguyen Dinh Son, Daeyoung Kim  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04509v1  

#### Abstract
Fine-tuning Multimodal Large Language Models (MLLMs) on task-specific data is an effective way to improve performance on downstream applications. However, such adaptation often leads to a degradation in generalization on pretrained tasks, a phenomenon known as Catastrophic Forgetting. Existing metho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Model-Dowser: Data-Free Importance Probing to Mitigate Catastrophic Forgetting in Multimodal Large Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Multimodal Large Language Models (MLLMs) 在特定任务上进行 fine-tuning 时，常常会遭遇 **Catastrophic Forgetting**（灾难性遗忘），即模型在提升下游任务性能的同时，严重丢失其在预训练阶段获得的通用泛化能力。这一问题在对语言解码器的**深层进行微调**时尤为严重。

现有方法如 **post-merging**（如 ModelTailor）和 **sparse fine-tuning**（如 SPIDER）在浅层微调中表现尚可，但在深层微调下要么失效，要么因内存开销过大而难以扩展到大模型。

---

### **提出了什么新方法或新思路**
作者提出 **Model-Dowser**，一种**数据无关**（data-free）、**可扩展**的稀疏微调方法，用于缓解 MLLMs 中的灾难性遗忘。

其核心思想是：  
> **通过测量每个参数对模型输出的影响来识别“功能重要”的参数，并在微调过程中冻结这些参数，从而保留预训练知识。**

具体创新点如下：

- **功能重要性评分（Functional Importance Score）**：  
  提出一个理论驱动的重要性评分 $ S_{ij}^{(l)} = \|J_i^{(l)}\|_2 \cdot |W_{ij}^{(l)}| \cdot \|h_{j}^{(l-1)}\| $，综合考虑三个维度：
  - **Output Sensitivity**（$ \|J_i^{(l)}\|_2 $）：输出对某神经元激活的梯度敏感度（通过 Hutchinson Estimator 估计）。
  - **Connection Strength**（$ |W_{ij}^{(l)}| $）：权重本身的大小。
  - **Input Activity**（$ \|h_{j}^{(l-1)}\| $）：前一层输入激活的强度。

- **数据无关探测（Data-Free Probing）**：  
  不依赖原始预训练数据，而是利用 MLLM 自身的生成能力，从随机种子合成 **synthetic prompts** 来探测模型响应，实现重要性估计。

- **静态二值掩码（Static Binary Mask）**：  
  在微调前一次性计算重要性分数并生成掩码，仅更新低重要性的 $ p\% $ 参数。相比动态方法（如 SPIDER），显著降低内存开销。

---

### **相比现有方法的优势**
| 方法 | 内存复杂度 | 是否数据依赖 | 可扩展性 | 深层微调稳定性 |
|------|-----------|-------------|----------|----------------|
| Full-FT | $ O(|P|) $ | 否 | 差（严重遗忘） | ❌ |
| Grafting / Tailor | $ O(2|P|) $ | 否 | 差 | ❌（深层崩溃） |
| SPIDER | $ O(3|P|) $ | 是（需训练中积累梯度） | 差 | ⚠️（不稳定） |
| **Model-Dowser (Ours)** | $ O(|P|) $ | **否** | ✅（极佳） | ✅✅✅ |

- **资源高效**：内存复杂度与标准微调相同。
- **无需额外训练数据**：适用于预训练数据不可用的场景。
- **深层微调鲁棒性强**：即使微调全部 32 层仍保持稳定。
- **性能领先**：在多个 MLLM 架构上均取得 SOTA 表现。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
#### **下游任务（Fine-tuning Dataset）**
- **COCO-Caption**：图像描述生成
- **Flickr30k**：图像描述生成
- **ImageNet-R**：艺术风格图像分类（以 VQA 形式）
- **IconQA**：基于图表的视觉问答

#### **上游任务（Zero-shot Evaluation）**
用于评估预训练知识保留情况：
- **TextVQA**, **OKVQA**, **OCRVQA**, **GQA**
- **MMBench**（英文 & 中文版）

---

### **实验设置**
- **模型架构**：
  - **LLaVA-1.5-7B**
  - **NVILA-Lite-2B**
- **微调策略**：
  - 仅微调语言解码器的最后 $ L $ 层（$ L $ 从 4 到 32 变化）
  - 更新比例 $ p = 0.1 $（即仅更新 10% 最不重要的参数）
- **训练配置**：
  - 学习率：$ 2 \times 10^{-5} $
  - Epochs：5
  - Batch Size：128
  - 硬件：8×A100/H200 GPU

---

### **评估指标**
- **Aup**：上游任务平均准确率（衡量知识保留）
- **Adown**：下游任务性能（CIDEr 或 EM 准确率）
- **Avg**：算术平均 $ \frac{A_{up} + A_{down}}{2} $
- **H-score**（调和平均）：
  $$
  \text{H-score} = 2 \cdot \frac{A_{up} \cdot A_{down}}{A_{up} + A_{down}}
  $$
  > 更能反映“兼顾上下游”的平衡能力。

---

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **Full Fine-tuning** | Full-FT |
| **Post-Merging** | Grafting, DARE, Tailor (ModelTailor) |
| **Sparse Fine-tuning** | SPIDER（当前 SOTA） |
| **随机选择** | Random Masking（作为消融对照） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自 Table 1 & 2）**

| 方法 | NVILA-COCO H-score | LLaVA-COCO H-score | 内存开销 |
|------|--------------------|---------------------|---------|
| Full-FT | 39.7 | 72.9 | $ O(|P|) $ |
| SPIDER | 78.3 | 75.2 | $ O(3|P|) $ |
| **Model-Dowser (Ours)** | **85.7** | **79.9** | $ O(|P|) $ |

> 在 **NVILA** 上，H-score 超越 SPIDER 近 **7.4 分**；在 **LLaVA** 上也高出 **4.7 分**。

---

### **与基线方法的对比结果**
- **优于所有基线**：
  - 在 **H-score** 和 **Aup** 上全面领先，说明在不牺牲下游性能的前提下，更好地保留了预训练知识。
  - 在深层微调（>20 层）下，**Grafting/Tailor 性能崩溃**，而 **Model-Dowser 依然稳健**（见 Figure 3）。
- **优于 SPIDER 尽管更新更少参数**：
  - SPIDER 需更新 50% 参数，而 Model-Dowser 仅更新 10%，却表现更优，说明其**重要性判别更精准**。

---

### **消融实验结果**
#### **(1) 与 Random Selection 对比（Table 4 & 6–7）**
| 更新比例 $ p $ | 方法 | ImageNet-R H-score (NVILA) |
|------------------|------|----------------------------|
| 0.5 | Random | 55.0 ± 0.1 |
| 0.5 | **Ours** | **62.7** |
| 0.75 | Random | 51.3 |
| 0.75 | **Ours** | **51.3**（仍持平）|

> 当更新比例增加时，**随机选择迅速退化**，而 Model-Dowser 因保护关键参数，仍能维持性能。

#### **(2) 数据无关估计的有效性（Table 8）**
- 使用 **Spearman Correlation** 和 **Hamming Distance** 评估合成数据与真实数据下重要性排序的一致性。
- 结果显示：
  - **Spearman ≥ 0.88**
  - **Hamming Distance ≤ 0.05**
  > 表明合成数据能有效逼近真实敏感性分布。

---

## **4. 关键结论和发现**

### **主要发现**
1. **深层微调加剧灾难性遗忘**：  
   微调语言解码器早期层会导致严重的预训练知识丢失，现有方法（尤其是 post-merging）对此极为脆弱。

2. **功能敏感性是关键**：  
   通过联合建模 **weight magnitude**、**input activation** 和 **output sensitivity**，可以更准确地识别影响模型输出的关键参数。

3. **数据无关探测可行且有效**：  
   利用 MLLM 自身生成的 synthetic prompts 进行 probing，可在无原始数据情况下实现高保真重要性估计。

4. **静态稀疏微调更高效可靠**：  
   相比动态更新策略（如 SPIDER），预先冻结关键参数的策略在效率和稳定性上更具优势。

---

### **方法的局限性**
- **依赖于一阶近似**：理论分析基于 first-order Taylor approximation，可能忽略高阶交互效应。
- **合成样本多样性有限**：虽然实验证明有效，但生成的 prompts 是否覆盖所有语义空间仍有待验证。
- **未探索多任务连续学习中的长期演化**：尽管在 CL 设置中表现良好（见 Appendix H），但主要设计目标仍是单次下游适配。

---

### **未来工作方向**
- 扩展至 **真正的 Continual Learning** 场景，支持任务增量学习。
- 探索 **自适应更新比例 $ p $**，根据不同层自动调整稀疏程度。
- 将该方法应用于 **其他模态组合**（如音频-语言模型）。
- 结合 **LoRA** 等 PEFT 方法，进一步提升参数效率。

---

> ✅ **一句话总结**：  
> **Model-Dowser 提出了一种无需数据、内存友好、理论严谨的稀疏微调框架，通过识别并冻结“功能关键”参数，在深层微调中显著缓解了 MLLMs 的灾难性遗忘问题，实现了性能与稳定性的最佳平衡。**

</details>

---

### 16. [MirrorLA: Reflecting Feature Map for Vision Linear Attention](https://arxiv.org/abs/2602.04346)

**Authors**: Weikang Meng, Liangyu Huo, Yadan Luo, Yaowei Wang, Yingjian Li, Zheng Zhang  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04346v1  

#### Abstract
Linear attention significantly reduces the computational complexity of Transformers from quadratic to linear, yet it consistently lags behind softmax-based attention in performance. We identify the root cause of this degradation as the non-negativity constraint imposed on kernel feature maps: standa...

---

### 17. [SAFE: Stable Alignment Finetuning with Entropy-Aware Predictive Control for RLHF](https://arxiv.org/abs/2602.04651)

**Authors**: Dipan Maity  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.04651v1  

#### Abstract
Optimization (PPO) has been positioned by recent literature as the canonical method for the RL part of RLHF. PPO performs well empirically but has a heuristic motivation and handles the KL-divergence constraint used in LM-RLHF in an ad-hoc manner and suffers form reward oscillations, entropy collaps...

---

### 18. [Empirical-MCTS: Continuous Agent Evolution via Dual-Experience Monte Carlo Tree Search](https://arxiv.org/abs/2602.04248)

**Authors**: Hao Lu, Haoyuan Huang, Yulin Zhou, Chen Li, Ningxin Zhu  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04248v1  

#### Abstract
Inference-time scaling strategies, particularly Monte Carlo Tree Search (MCTS), have significantly enhanced the reasoning capabilities of Large Language Models (LLMs). However, current approaches remain predominantly stateless, discarding successful reasoning patterns after each problem instance and...

---

### 19. [From Assumptions to Actions: Turning LLM Reasoning into Uncertainty-Aware Planning for Embodied Agents](https://arxiv.org/abs/2602.04326)

**Authors**: SeungWon Seo, SooBin Lim, SeongRae Noh, Haneul Kim, HyeongYeop Kang  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04326v1  

#### Abstract
Embodied agents operating in multi-agent, partially observable, and decentralized environments must plan and act despite pervasive uncertainty about hidden objects and collaborators' intentions. Recent advances in applying Large Language Models (LLMs) to embodied agents have addressed many long-stan...

---

### 20. [LinGO: A Linguistic Graph Optimization Framework with LLMs for Interpreting Intents of Online Uncivil Discourse](https://arxiv.org/abs/2602.04693)

**Authors**: Yuan Zhang, Thales Bertaglia  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04693v1  

#### Abstract
Detecting uncivil language is crucial for maintaining safe, inclusive, and democratic online spaces. Yet existing classifiers often misinterpret posts containing uncivil cues but expressing civil intents, leading to inflated estimates of harmful incivility online. We introduce LinGO, a linguistic gr...

---

### 21. [OmniSIFT: Modality-Asymmetric Token Compression for Efficient Omni-modal Large Language Models](https://arxiv.org/abs/2602.04804)

**Authors**: Yue Ding, Yiyan Ji, Jungang Li, Xuyang Liu, Xinlong Chen, Junfei Wu, Bozhou Li, Bohan Zeng, Yang Shi, Yushuo Guan, Yuanxing Zhang, Jiaheng Liu, Qiang Liu, Pengfei Wan, Liang Wang  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04804v1  

#### Abstract
Omni-modal Large Language Models (Omni-LLMs) have demonstrated strong capabilities in audio-video understanding tasks. However, their reliance on long multimodal token sequences leads to substantial computational overhead. Despite this challenge, token compression methods designed for Omni-LLMs rema...

---

### 22. [Federated Concept-Based Models: Interpretable models with distributed supervision](https://arxiv.org/abs/2602.04093)

**Authors**: Dario Fenoglio, Arianna Casanova, Francesco De Santis, Mohan Li, Gabriele Dominici, Johannes Schneider, Martin Gjoreski, Marc Langheinrich, Pietro Barbiero, Giovanni De Felice  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04093v1  

#### Abstract
Concept-based models (CMs) enhance interpretability in deep learning by grounding predictions in human-understandable concepts. However, concept annotations are expensive to obtain and rarely available at scale within a single data source. Federated learning (FL) could alleviate this limitation by e...

---

### 23. [From Sparse Sensors to Continuous Fields: STRIDE for Spatiotemporal Reconstruction](https://arxiv.org/abs/2602.04201)

**Authors**: Yanjie Tong, Peng Chen  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04201v1  

#### Abstract
Reconstructing high-dimensional spatiotemporal fields from sparse point-sensor measurements is a central challenge in learning parametric PDE dynamics. Existing approaches often struggle to generalize across trajectories and parameter settings, or rely on discretization-tied decoders that do not nat...

---

### 24. [Cascading Robustness Verification: Toward Efficient Model-Agnostic Certification](https://arxiv.org/abs/2602.04236)

**Authors**: Mohammadreza Maleki, Rushendra Sidibomma, Arman Adibi, Reza Samavi  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04236v1  

#### Abstract
Certifying neural network robustness against adversarial examples is challenging, as formal guarantees often require solving non-convex problems. Hence, incomplete verifiers are widely used because they scale efficiently and substantially reduce the cost of robustness verification compared to comple...

---

### 25. [Mosaic Learning: A Framework for Decentralized Learning with Model Fragmentation](https://arxiv.org/abs/2602.04352)

**Authors**: Sayan Biswas, Davide Frey, Romaric Gaudel, Nirupam Gupta, Anne-Marie Kermarrec, Dimitri Ler\'ev\'erend, Rafael Pires, Rishi Sharma, Fran\c{c}ois Ta\"iani, Martijn de Vos  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.04352v1  

#### Abstract
Decentralized learning (DL) enables collaborative machine learning (ML) without a central server, making it suitable for settings where training data cannot be centrally hosted. We introduce Mosaic Learning, a DL framework that decomposes models into fragments and disseminates them independently acr...

---

### 26. [WideSeek-R1: Exploring Width Scaling for Broad Information Seeking via Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2602.04634)

**Authors**: Zelai Xu, Zhexuan Xu, Ruize Zhang, Chunyang Zhu, Shi Yu, Weilin Liu, Quanlu Zhang, Wenbo Ding, Chao Yu, Yu Wang  
**Category**: cs.AI  
**Published**: 2026-02-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.04634v1  

#### Abstract
Recent advancements in Large Language Models (LLMs) have largely focused on depth scaling, where a single agent solves long-horizon problems with multi-turn reasoning and tool use. However, as tasks grow broader, the key bottleneck shifts from individual competence to organizational capability. In t...

---

### 27. [Fine-Grained Activation Steering: Steering Less, Achieving More](https://arxiv.org/abs/2602.04428)

**Authors**: Zijian Feng, Tianjiao Li, Zixiao Zhu, Hanzhang Zhou, Junlang Qian, Li Zhang, Jia Jim Deryl Chua, Lee Onn Mak, Gee Wah Ng, Kezhi Mao  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.04428v1  

#### Abstract
Activation steering has emerged as a cost-effective paradigm for modifying large language model (LLM) behaviors. Existing methods typically intervene at the block level, steering the bundled activations of selected attention heads, feedforward networks, or residual streams. However, we reveal that b...

---

### 28. [ERNIE 5.0 Technical Report](https://arxiv.org/abs/2602.04705)

**Authors**: Haifeng Wang, Hua Wu, Tian Wu, Yu Sun, Jing Liu, Dianhai Yu, Yanjun Ma, Jingzhou He, Zhongjun He, Dou Hong, Qiwen Liu, Shuohuan Wang, Junyuan Shang, Zhenyu Zhang, Yuchen Ding, Jinle Zeng, Jiabin Yang, Liang Shen, Ruibiao Chen, Weichong Yin, Siyu Ding, Dai Dai, Shikun Feng, Siqi Bao, Bolei He, Yan Chen, Zhenyu Jiao, Ruiqing Zhang, Zeyu Chen, Qingqing Dang, Kaipeng Deng, Jiajun Jiang, Enlei Gong, Guoxia Wang, Yanlin Sha, Yi Liu, Yehan Zheng, Weijian Xu, Jiaxiang Liu, Zengfeng Zeng, Yingqi Qu, Zhongli Li, Zhengkun Zhang, Xiyang Wang, Zixiang Xu, Xinchao Xu, Zhengjie Huang, Dong Wang, Bingjin Chen, Yue Chang, Xing Yuan, Shiwei Huang, Qiao Zhao, Xinzhe Ding, Shuangshuang Qiao, Baoshan Yang, Bihong Tang, Bin Li, Bingquan Wang, Binhan Tang, Binxiong Zheng, Bo Cui, Bo Ke, Bo Zhang, Bowen Zhang, Boyan Zhang, Boyang Liu, Caiji Zhang, Can Li, Chang Xu, Chao Pang, Chao Zhang, Chaoyi Yuan, Chen Chen, Cheng Cui, Chenlin Yin, Chun Gan, Chunguang Chai, Chuyu Fang, Cuiyun Han, Dan Zhang, Danlei Feng, Danxiang Zhu, Dong Sun, Dongbo Li, Dongdong Li, Dongdong Liu, Dongxue Liu, Fan Ding, Fan Hu, Fan Li, Fan Mo, Feisheng Wu, Fengwei Liu, Gangqiang Hu, Gaofeng Lu, Gaopeng Yong, Gexiao Tian, Guan Wang, Guangchen Ni, Guangshuo Wu, Guanzhong Wang, Guihua Liu, Guishun Li, Haibin Li, Haijian Liang, Haipeng Ming, Haisu Wang, Haiyang Lu, Haiye Lin, Han Zhou, Hangting Lou, Hanwen Du, Hanzhi Zhang, Hao Chen, Hao Du, Hao Liu, Hao Zhou, Haochen Jiang, Haodong Tian, Haoshuang Wang, Haozhe Geng, Heju Yin, Hong Chen, Hongchen Xue, Hongen Liu, Honggeng Zhang, Hongji Xu, Hongwei Chen, Hongyang Zhang, Hongyuan Zhang, Hua Lu, Huan Chen, Huan Wang, Huang He, Hui Liu, Hui Zhong, Huibin Ruan, Jiafeng Lu, Jiage Liang, Jiahao Hu, Jiahao Hu, Jiajie Yang, Jialin Li, Jian Chen, Jian Wu, Jianfeng Yang, Jianguang Jiang, Jianhua Wang, Jianye Chen, Jiaodi Liu, Jiarui Zhou, Jiawei Lv, Jiaxin Zhou, Jiaxuan Liu, Jie Han, Jie Sun, Jiefan Fang, Jihan Liu, Jihua Liu, Jing Hu, Jing Qian, Jing Yan, Jingdong Du, Jingdong Wang, Jingjing Wu, Jingyong Li, Jinheng Wang, Jinjin Li, Jinliang Lu, Jinlin Yu, Jinnan Liu, Jixiang Feng, Jiyi Huang, Jiyuan Zhang, Jun Liang, Jun Xia, Jun Yu, Junda Chen, Junhao Feng, Junhong Xiang, Junliang Li, Kai Liu, Kailun Chen, Kairan Su, Kang Hu, Kangkang Zhou, Ke Chen, Ke Wei, Kui Huang, Kun Wu, Kunbin Chen, Lei Han, Lei Sun, Lei Wen, Linghui Meng, Linhao Yu, Liping Ouyang, Liwen Zhang, Longbin Ji, Longzhi Wang, Meng Sun, Meng Tian, Mengfei Li, Mengqi Zeng, Mengyu Zhang, Ming Hong, Mingcheng Zhou, Mingming Huang, Mingxin Chen, Mingzhu Cai, Naibin Gu, Nemin Qiu, Nian Wang, Peng Qiu, Peng Zhao, Pengyu Zou, Qi Wang, Qi Xin, Qian Wang, Qiang Zhu, Qianhui Luo, Qianwei Yang, Qianyue He, Qifei Wu, Qinrui Li, Qiwen Bao, Quan Zhang, Quanxiang Liu, Qunyi Xie, Rongrui Zhan, Rufeng Dai, Rui Peng, Ruian Liu, Ruihao Xu, Ruijie Wang, Ruixi Zhang, Ruixuan Liu, Runsheng Shi, Ruting Wang, Senbo Kang, Shan Lu, Shaofei Yu, Shaotian Gong, Shenwei Hu, Shifeng Zheng, Shihao Guo, Shilong Fan, Shiqin Liu, Shiwei Gu, Shixi Zhang, Shuai Yao, Shuang Zhang, Shuangqiao Liu, Shuhao Liang, Shuwei He, Shuwen Yang, Sijun He, Siming Dai, Siming Wu, Siyi Long, Songhe Deng, Suhui Dong, Suyin Liang, Teng Hu, Tianchan Xu, Tianliang Lv, Tianmeng Yang, Tianyi Wei, Tiezhu Gao, Ting Sun, Ting Zhang, Tingdan Luo, Wei He, Wei Luan, Wei Yin, Wei Zhang, Wei Zhou, Weibao Gong, Weibin Li, Weicheng Huang, Weichong Dang, Weiguo Zhu, Weilong Zhang, Weiqi Tan, Wen Huang, Wenbin Chang, Wenjing Du, Wenlong Miao, Wenpei Luo, Wenquan Wu, Xi Shi, Xi Zhao, Xiang Gao, Xiangguo Zhang, Xiangrui Yu, Xiangsen Wang, Xiangzhe Wang, Xianlong Luo, Xianying Ma, Xiao Tan, Xiaocong Lin, Xiaofei Wang, Xiaofeng Peng, Xiaofeng Wu, Xiaojian Xu, Xiaolan Yuan, Xiaopeng Cui, Xiaotian Han, Xiaoxiong Liu, Xiaoxu Fei, Xiaoxuan Wu, Xiaoyu Wang, Xiaoyu Zhang, Xin Sun, Xin Wang, Xinhui Huang, Xinming Zhu, Xintong Yu, Xinyi Xu, Xinyu Wang, Xiuxian Li, XuanShi Zhu, Xue Xu, Xueying Lv, Xuhong Li, Xulong Wei, Xuyi Chen, Yabing Shi, Yafeng Wang, Yamei Li, Yan Liu, Yanfu Cheng, Yang Gao, Yang Liang, Yang Wang, Yang Wang, Yang Yang, Yanlong Liu, Yannian Fu, Yanpeng Wang, Yanzheng Lin, Yao Chen, Yaozong Shen, Yaqian Han, Yehua Yang, Yekun Chai, Yesong Wang, Yi Song, Yichen Zhang, Yifei Wang, Yifeng Guo, Yifeng Kou, Yilong Chen, Yilong Guo, Yiming Wang, Ying Chen, Ying Wang, Yingsheng Wu, Yingzhan Lin, Yinqi Yang, Yiran Xing, Yishu Lei, Yixiang Tu, Yiyan Chen, Yong Zhang, Yonghua Li, Yongqiang Ma, Yongxing Dai, Yongyue Zhang, Yu Ran, Yu Sun, Yu-Wen Michael Zhang, Yuang Liu, Yuanle Liu, Yuanyuan Zhou, Yubo Zhang, Yuchen Han, Yucheng Wang, Yude Gao, Yuedong Luo, Yuehu Dong, Yufeng Hu, Yuhui Cao, Yuhui Yun, Yukun Chen, Yukun Gao, Yukun Li, Yumeng Zhang, Yun Fan, Yun Ma, Yunfei Zhang, Yunshen Xie, Yuping Xu, Yuqin Zhang, Yuqing Liu, Yurui Li, Yuwen Wang, Yuxiang Lu, Zefeng Cai, Zelin Zhao, Zelun Zhang, Zenan Lin, Zezhao Dong, Zhaowu Pan, Zhaoyu Liu, Zhe Dong, Zhe Zhang, Zhen Zhang, Zhengfan Wu, Zhengrui Wei, Zhengsheng Ning, Zhenxing Li, Zhenyu Li, Zhenyu Qian, Zhenyun Li, Zhi Li, Zhichao Chen, Zhicheng Dong, Zhida Feng, Zhifan Feng, Zhihao Deng, Zhijin Yu, Zhiyang Chen, Zhonghui Zheng, Zhuangzhuang Guo, Zhujun Zhang, Zhuo Sun, Zichang Liu, Zihan Lin, Zihao Huang, Zihe Zhu, Ziheng Zhao, Ziping Chen, Zixuan Zhu, Ziyang Xu, Ziyi Liang, Ziyuan Gao  
**Category**: cs.CL  
**Published**: 2026-02-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.04705v1  

#### Abstract
In this report, we introduce ERNIE 5.0, a natively autoregressive foundation model desinged for unified multimodal understanding and generation across text, image, video, and audio. All modalities are trained from scratch under a unified next-group-of-tokens prediction objective, based on an ultra-s...

---

### 29. [Six Times to Spare: LDPC Acceleration on DGX Spark for AI-Native Open RAN](https://arxiv.org/abs/2602.04652)

**Authors**: Ryan Barker, Fatemeh Afghah  
**Category**: cs.DC  
**Published**: 2026-02-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.04652v1  

#### Abstract
Low-density parity-check (LDPC) decoding is one of the most computationally intensive kernels in the 5G New Radio (NR) physical layer and must complete within a 0.5\,ms transmission time interval while sharing the budget with FFT, channel estimation, demapping, HARQ, and MAC scheduling. Many open an...

---

### 30. [Causal Discovery for Cross-Sectional Data Based on Super-Structure and Divide-and-Conquer](https://arxiv.org/abs/2602.03914)

**Authors**: Wenyu Wang (University of South China), Yaping Wan (University of South China)  
**Category**: cs.LG  
**Published**: 2026-02-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.03914v1  

#### Abstract
This paper tackles a critical bottleneck in Super-Structure-based divide-and-conquer causal discovery: the high computational cost of constructing accurate Super-Structures--particularly when conditional independence (CI) tests are expensive and domain knowledge is unavailable. We propose a novel, l...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
