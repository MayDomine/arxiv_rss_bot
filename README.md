# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-08 05:58:47 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Implicit Graph, Explicit Retrieval: Towards Efficient and Interpretable Long-horizon Memory for Large Language Models](https://arxiv.org/abs/2601.03417)

**Authors**: Xin Zhang, Kailai Yang, Hao Li, Chenyue Li, Qiyu Wei, Sophia Ananiadou  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.03417v1  

#### Abstract
Long-horizon applications increasingly require large language models (LLMs) to answer queries when relevant evidence is sparse and dispersed across very long contexts. Existing memory systems largely follow two paradigms: explicit structured memories offer interpretability but often become brittle u...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Implicit Graph, Explicit Retrieval: Towards Efficient and Interpretable Long-horizon Memory for Large Language Models*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在**长时距（long-horizon）推理任务**中，相关证据通常稀疏且分散于数千token的上下文中。现有的记忆机制面临两大挑战：
- **显式结构化记忆（Explicit Structured Memory）**：如图结构存储，虽具备良好的**可解释性**，但在长而嘈杂的上下文中易因结构构建错误或检索不稳定而导致性能下降（“long-context overload”）。
- **隐式潜空间记忆（Latent Memory）**：高效稳定，但缺乏透明度，难以进行人类审查或调试。

因此，如何在**效率、稳定性与可解释性之间取得平衡**，是当前LLM长期记忆系统的关键瓶颈。

---

### 🚀 提出的新方法：LATENTGRAPHMEM
作者提出 **LATENTGRAPHMEM**，一种结合**隐式图存储**与**显式子图检索**的记忆框架，其核心思想为：
> **“隐式建模，显式暴露”（Implicit modeling, explicit exposure）**

#### 主要组件：
1. **Graph Builder（图构建器）**  
   - 将长文档流式处理为一个受限的符号图（symbolic graph），提取实体三元组 `(h, r, t)`。
   - 同时将每条边编码为**潜向量（latent embedding）**，形成隐式图表示。
   
2. **Subgraph Retriever（子图检索器）**  
   - 在潜空间中基于问题 `q` 对边嵌入进行打分，通过 Top-K 选择最多 `k` 条最相关的边。
   - 只将这个紧凑的**显式子图（explicit subgraph）** 序列化并传递给下游推理模型。

3. **Frozen LLM Reasoner（冻结的推理器）**  
   - 不参与训练，仅用于生成答案，确保公平比较。

#### 创新亮点：
- **双阶段训练 + 联合微调**：Builder 和 Retriever 分阶段远程监督训练，并最终联合优化以增强协同。
- **潜空间检索 + 显式输出**：避免了完整图序列化的开销，同时保留了解释性。
- **无需强化学习**：使用 Straight-Through Estimator 实现 Top-K 的可微训练，简化实现。

---

### 🔍 相比现有方法的优势
| 维度 | 显式图方法（如THEANINE, Mem0） | 隐式记忆（如MemGen） | LATENTGRAPHMEM（本文） |
|------|-------------------------------|------------------------|--------------------------|
| **可解释性** | 高（完整图可见） | 极低（黑盒） | ✅ 高（仅暴露关键子图） |
| **稳定性** | 低（受噪声影响大） | 高 | ✅ 高（潜空间操作） |
| **效率** | 低（需序列化整个图） | 高 | ✅ 高（固定预算检索） |
| **扩展性** | 差（随上下文增长） | 好 | ✅ 好（独立于上下文长度） |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **训练数据**（共20,800样本）：
  - **TriviaQA**：开放域问答，证据分散。
  - **QASPER**：科学论文理解，跨段落推理。
  - **QuALITY**：叙事类文章多选题，强调全局理解。
- **测试数据**（共2,600样本混合）：
  - **HotpotQA**：多跳推理，跨文档关联。
  - **NarrativeQA**：长篇故事理解。
  - **WikiHop**：基于维基百科的逻辑推理。

> 所有测试集均未出现在训练中，保证泛化能力评估。

---

### ⚙️ 实验设置
- **主干模型（Reasoner）**：三种规模的冻结LLM：
  - `Qwen2.5-1.5B-Instruct`
  - `SmolLM3-3B`
  - `Qwen3-8B`
- **记忆模块**：Graph Builder 与 Subgraph Retriever 均采用 LoRA 微调的小型LLM（统一为1.5B），可适配不同大小的推理器。
- **图容量控制**：
  - 每chunk最多提取32个三元组。
  - 全局图最大边数 $ M = 150 $。
  - 检索子图大小 $ k = 30 $。
- **输入处理**：文档切分为1024-token重叠块（overlap=128）。

---

### 📊 评估指标
- **Accuracy (Acc)**：精确匹配准确率（尤其适用于多跳、事实型问题）。
- **ROUGE-L**：衡量生成文本与参考答案的最长公共子序列，反映流畅性和覆盖度。

---

### 🆚 基线方法对比
| 类型 | 方法 | 简介 |
|------|------|------|
| **无记忆** | Reasoner-only | 直接输入全文 |
| **检索增强** | RAG | 基于embedding检索top-k文本块 |
| **显式图记忆** | THEANINE, PREMem, Mem0/Mem0g, A-Mem | 图结构存储，支持时间线/社区等组织方式 |
| **隐式记忆** | MemGen | 生成潜向量作为软提示 |

---

## 3. 主要实验结果和性能指标

### 📈 性能汇总（见 Table 1）
在三个基准上的平均 Accuracy 表现如下：

| 方法 | Qwen2.5-1.5B | SmolLM3-3B | Qwen3-8B |
|------|--------------|------------|---------|
| **Reasoner-only** | 52.11% | 50.70% | 54.74% |
| **MemGen (Latent)** | 44.03% | 49.58% | 54.56% |
| **A-Mem / PREMem / THEANINE / Mem0** | <30% | <46% | <44% |
| **LATENTGRAPHMEM (Ours)** | **56.08%** | **58.64%** | **63.34%** |

✅ **关键发现**：
- 在所有模型尺度下，**LATENTGRAPHMEM 均显著优于所有基线**。
- 即使使用**1.5B的小型记忆模块**，也能有效服务于**8B的大推理器**，体现良好扩展性。
- 在多跳推理任务（如HotpotQA）上提升尤为明显，说明其擅长整合远距离证据。

---

### 🔍 消融实验（Ablation Study，见 Figure 3）
研究了不同设计选择的影响：

| 变体 | 描述 | 结果 |
|------|------|------|
| **No Subgraph (Full Graph Only)** | 不做检索，直接传完整图 | 性能下降 → 完整图引入噪声 |
| **No Subgraph Retriever (BFS)** | 使用BFS启发式扩展替代学习式检索 | 明显劣于学习式 → 证明Retriever有效性 |
| **Fully Explicit (No Latent)** | 完全基于符号图检索 | 不及本方法 → 验证潜空间稳定性优势 |

➡️ 结论：**潜图存储 + 学习式预算化子图检索** 是最佳组合。

---

### ⏱️ 推理延迟分析（见 Table 2）
在 >6k token 上下文下的平均推理时间（秒）：

| Context | LATENTGRAPHMEM | MemGen | A-Mem |
|--------|------------------|--------|-------|
| 6k     | 12.47            | 10.59  | 20.00 |
| 9k     | 13.43            | 10.86  | 44.65 |

- **A-Mem 随上下文急剧变慢**，因其需频繁访问不断增长的显式结构。
- **LATENTGRAPHMEM 推理时间几乎恒定**，得益于潜空间操作和固定检索预算。
- 虽略高于纯隐式方法（MemGen），但**代价极小地换取了可解释性**。

---

### 📊 图容量敏感性分析（见 Figure 4）
改变全局图容量 $ M $ 对性能的影响：
- **WikiHop**：随着 $ M $ 增加持续受益 → 更广的知识覆盖有利。
- **HotpotQA / NarrativeQA**：超过 $ M=150 $ 后趋于饱和甚至下降 → 过多噪声干扰检索。

➡️ 说明：**合理的容量限制有助于去噪和稳定检索**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **显式图记忆在长上下文中不稳定**，容易因结构错误导致崩溃（尤其在NarrativeQA上接近零分）。
2. **纯隐式记忆虽稳定但不可解释**，不利于调试和可信AI部署。
3. **LATENTGRAPHMEM 成功融合两者优点**：
   - 利用**潜空间进行高效稳定的图表示与检索**；
   - 仅暴露**紧凑的显式子图供推理和人工检查**；
   - 实现了**性能、效率、可解释性的统一**。
4. 该框架具有**参数高效、灵活扩展、稳定推理延迟**等特点，适合实际应用。

---

### ⚠️ 局限性
- **依赖图构建质量**：若Builder提取的三元组不准确，后续无法纠正。
- **最优配置依赖任务特性**：如图容量 $ M $、检索预算 $ k $ 需根据数据调整。
- **目前局限于文本问答场景**，尚未拓展至多模态或交互式决策环境。

---

### 🔮 未来工作方向
- 引入动态图剪枝或自适应预算机制。
- 扩展到对话系统、Agent规划等更复杂的长期记忆应用场景。
- 探索多模态图记忆（text + image embeddings）。
- 结合因果推理或反事实分析提升子图可解释性。

---

## ✅ 总结
**LATENTGRAPHMEM** 提出了一种新颖且实用的长期记忆架构范式——**“隐式图，显式检”（Implicit Graph, Explicit Retrieval）**，在保持高效率和稳定性的同时，解决了传统隐式记忆不可解释的根本痛点。实验证明其在多个长时距QA基准上**全面超越主流显式与隐式基线**，为构建**可靠、可控、可审计的LLM智能体记忆系统**提供了重要路径。

</details>

---

### 2. [From Bits to Chips: An LLM-based Hardware-Aware Quantization Agent for Streamlined Deployment of LLMs](https://arxiv.org/abs/2601.03484)

**Authors**: Kaiyuan Deng, Hangyu Zheng, Minghai Qing, Kunxiong Zhu, Gen Li, Yang Xiao, Lan Emily Zhang, Linke Guo, Bo Hui, Yanzhi Wang, Geng Yuan, Gagan Agrawal, Wei Niu, Xiaolong Ma  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.03484v1  

#### Abstract
Deploying models, especially large language models (LLMs), is becoming increasingly attractive to a broader user base, including those without specialized expertise. However, due to the resource constraints of certain hardware, maintaining high accuracy with larger model while meeting the hardware r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# From Bits to Chips: An LLM-based Hardware-Aware Quantization Agent for Streamlined Deployment of LLMs  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）在资源受限设备上的部署面临两大挑战：
- **高精度要求**：量化后需保持模型性能不显著下降。
- **低计算成本需求**：需满足边缘设备的内存与算力限制。

然而，传统量化与部署流程高度依赖专家手动调参，涉及复杂的超参数优化（如学习率、批大小）和硬件适配配置（如核函数并行化、内存布局），导致非专家用户难以高效部署量化模型。

### 🚀 提出的新方法：HAQA（Hardware-Aware Quantization Agent）
本文提出 **HAQA** ——一种基于 **Large Language Model (LLM)** 的自动化框架，用于联合优化量化模型微调与硬件部署全过程。

#### 核心创新点：
- **首次将 LLM Agent 应用于量化与硬件部署协同优化**  
  不仅优化 QAT 或 PTQ 超参数，还自动调整 CUDA kernel 的执行配置（如 `griddim`, `blockdim`, 内存层级等），实现端到端自动化。
  
- **标准化且用户友好的双阶段 Prompt 设计**
  - **Static Prompt**：固定任务描述，包含硬件规格、模型结构、搜索空间等。
  - **Dynamic Prompt**：动态更新训练/推理反馈（准确率、延迟日志）、历史决策记录，支持迭代优化。

- **引入 ReAct 推理机制提升稳定性**
  在提示中嵌入“Thought → Action → Observation”逻辑链，使 LLM 具备反思能力，避免无效或越界尝试，提高搜索效率与可靠性。

- **自适应量化策略（Adaptive Quantization Strategy）**
  HAQA 能识别反直觉但最优的硬件配置（例如在某些移动设备上 INT8 比 INT4 更快），通过分析硬件架构特性（指令集支持、内存带宽、加速器可用性）做出智能选择。

### 🔍 相比现有方法的优势
| 方法类型 | 局限性 | HAQA 的优势 |
|--------|-------|-------------|
| 手动调参（Human Tuning） | 成本高、耗时长、易遗漏最优解 | 自动化全流程，降低专家门槛 |
| 随机搜索 / 网格搜索 | 高维空间效率低下 | 利用 LLM 推理能力聚焦潜在区域 |
| 贝叶斯优化 / NSGA-II | 依赖代理模型，计算开销大 | 无需训练模型，API 调用即可完成 |
| 编译器优化（TVM/Halide） | 依赖启发式规则或黑盒调优器 | 结合语义理解与硬件感知进行协同决策 |

> 💡 **核心优势总结**：HAQA 实现了 **accuracy-speed-usability** 三者的统一，在提升性能的同时大幅简化部署流程。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与模型
| 类型 | 模型 | 数据集 |
|------|------|--------|
| 视觉模型 | ResNet20, ResNet32, ResNet50 | CIFAR-10, ImageNet2012 |
| 大语言模型 | LLaMA2-7B, LLaMA2-13B, LLaMA3.2-3B, LLaMA3-8B | Alpaca（指令微调数据集） |

### ⚙️ 实验设置
- **微调方式**：
  - ResNet 使用 **DoReFa-QAT**
  - LLM 使用 **QLoRA**（结合低比特量化与低秩适配）
- **量化位宽**：w8a8, w4a4, w2a2（权重量化/激活量化）
- **部署平台**：
  - **服务器级 GPU**：NVIDIA A6000（Ampere 架构，支持 TensorRT）
  - **移动端 SoC**：OnePlus 11（Snapdragon 8 Gen 2 + Adreno 740 GPU）
- **部署框架**：基于 `llama.cpp` 进行内核级优化

### 📊 评估指标
| 维度 | 指标 |
|------|------|
| **准确性** | Top-1 准确率（ResNet）、多项任务平均得分（BoolQ, RTE, ARC-E/C, Hellaswag 等） |
| **推理性能** | Token generation speed (tokens/s)，单 kernel 延迟（μs） |
| **系统效率** | 吞吐量、内存占用、收敛轮次 |
| **用户体验** | 是否需要人工干预、是否违反约束 |

### 🆚 基线方法对比
- **Human**：经验丰富的研究人员手动调参
- **Local Search / Random Search / Bayesian Optimization**：经典超参优化算法
- **NSGA-II**：多目标进化算法
- **Default**：原始默认配置（未优化）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### ✅ 表1 & 表2：准确率显著优于基线
| 模型 | 方法 | 平均准确率（vs Human） | 提升幅度 |
|------|------|------------------|----------|
| ResNet20-w4a4 | HAQA | **88.38%** | ↑6.78pp |
| LLaMA2-7B-INT4 | HAQA | **63.11%** | ↑1.12pp |
| LLaMA2-13B-INT4 | HAQA | **65.57%** | ↑1.61pp |
| LLaMA3-8B-INT4 | HAQA | **67.75%** | ↑1.41pp |

> 🔹 HAQA 在所有量化设置下均超越人类专家和其他自动化方法，尤其在低比特（INT4）场景表现更稳健。

#### ⏱️ 图5 & 表3：推理速度大幅提升
| 场景 | 加速比 |
|------|--------|
| 整体模型推理（vs llama.cpp 默认） | **1.2× ~ 1.5×** |
| MatMul Kernel（最大负载操作） | 最高达 **1.63×** |
| SiLU Kernel | 最高达 **2.31×** |

> 🔹 特别是在矩阵乘法这类密集计算操作中，HAQA 通过对 `tiling size` 和 `blockdim` 的精细调节实现了显著加速。

#### 📦 表4：移动端吞吐量实测
| 模型 | FP16 | INT8 | INT4 |
|------|------|------|------|
| openllama-3B | 5.11 | **5.25** | 4.95 |
| tinylama-1.1B | 11.17 | **11.23** | 10.43 |

> 🔹 尽管 INT4 理论上应更快，但在 OnePlus 11 上 **INT8 反而吞吐更高**，验证了 HAQA 发现“反直觉最优”的能力。

#### 🔄 图4：收敛速度更快更稳定
- HAQA 在 **更少迭代轮次内达到更高准确率**
- 相比随机/Bayesian 方法，其利用历史反馈进行推理规划，避免重复探索无效区域

#### 💰 成本与开销分析（Appendix C）
- 使用 GPT-4-0613 API
- 单次查询平均延迟：**2.34 秒**
- 完整优化流程总费用约 **$5**（~150K tokens）
- **无需本地 GPU 或定制基础设施**

> ✅ 极低成本换取高性能增益，极具实用价值。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM Agent 可有效替代专家进行复杂系统级优化**
   - HAQA 成功完成了从模型微调到硬件部署的全栈优化任务。
   - 其决策过程具备可解释性（via ReAct），便于调试与信任建立。

2. **硬件感知至关重要，不能仅凭理论推断**
   - 在 Snapdragon 8 Gen 2 + Adreno 740 上，**INT8 > INT4 性能**
   - 原因：INT4 需模拟执行（emulated via INT8），额外引入 bit-shift 操作与数据转换开销，反而拖慢性能。

3. **联合优化优于分步优化**
   - HAQA 同时优化微调参数与部署参数，实现协同增效（synergistic optimization）
   - 例如调整 LoRA rank 不仅影响 accuracy，也间接影响 kernel 并行效率

4. **HAQA 具备强泛化性和适应性**
   - 支持多种模型（CNN/Transformer）、多种硬件（GPU/移动端 SoC）
   - 可灵活扩展至其他编译优化任务（如 TVM Relay、MLIR）

### ⚠️ 方法的局限性
- **依赖外部 LLM API**：存在延迟、成本、隐私等问题；若使用开源 LLM（如 Llama3）可能效果下降。
- **Prompt Engineering 成本较高**：需精心设计静态 prompt 以确保任务清晰。
- **无法完全替代底层编译器优化**：对于极端性能压榨仍需结合 TVM/AutoTVM 等工具。

### 🔮 未来工作方向
1. **构建专用小型化 Agent 模型**：训练轻量级 LLM 替代 GPT-4，降低成本与延迟。
2. **集成到 MLOps 流水线**：作为 CI/CD 中的一环，自动完成模型压缩与部署。
3. **支持更多硬件后端**：扩展至 NPU、TPU、FPGA 等专用加速器。
4. **增强安全性与鲁棒性检测**：防止生成恶意代码或越权配置。

---

## ✅ 总结
**HAQA 是首个将 LLM Agent 应用于量化与硬件部署协同优化的框架**，它不仅提升了模型精度与推理速度，更重要的是极大地降低了部署门槛，使得非专家也能轻松完成复杂的 LLM 部署任务。其实验结果证明了 LLM 在系统优化领域的巨大潜力，为“AI for Systems”开辟了新的研究路径。

</details>

---

### 3. [LUT-KAN: Segment-wise LUT Quantization for Fast KAN Inference](https://arxiv.org/abs/2601.03332)

**Authors**: Oleksandr Kuznetsov  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.03332v1  

#### Abstract
Kolmogorov--Arnold Networks (KAN) replace scalar weights by learnable univariate functions, often implemented with B-splines. This design can be accurate and interpretable, but it makes inference expensive on CPU because each layer requires many spline evaluations. Standard quantization toolchains a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LUT-KAN: Segment-wise LUT Quantization for Fast KAN Inference —— 核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

Kolmogorov-Arnold Networks (KAN) 通过将传统神经网络中的标量权重替换为可学习的一元函数（通常基于 B-splines 实现），在保持模型紧凑性和高解释性的同时提升了预测精度。然而，这种设计带来了显著的推理开销，尤其是在 CPU 上，因为每个层都需要进行大量 B-spline 函数求值操作。

此外，标准的量化工具链（如用于 CNN/MLP 的 INT8 GEMM）难以直接应用于 KAN，因为其计算核心不是矩阵乘法，而是重复的样条基函数计算。因此，**如何实现高效、可部署、且语义明确的 KAN 推理加速**成为一个关键挑战。

### **提出了什么新方法或新思路**

本文提出 **LUT-KAN**，一种面向 PyKAN 风格 KAN 层的**分段查找表（segment-wise LUT）编译与量化方法**，其核心思想包括：

1. **Segment-wise LUT 编译**  
   将每条边上的可学习一元函数 $\phi_i(x)$ 分段编译成 LUT，每段（由 knot 定义）存储 $L$ 个采样点的量化值，并支持线性插值。

2. **Affine int8/uint8 量化**  
   在每个 segment 上采用仿射量化（affine quantization），支持对称 int8 和非对称 uint8 两种模式，统一反量化公式以简化部署。

3. **显式的 OOB（Out-of-Bounds）语义定义**  
   明确定义输入超出 knot 范围时的行为，通过两个正交配置控制：
   - `boundary_mode`: `half_open` 或 `closed`
   - `oob_policy`: `clip_x`（饱和输入）或 `zero_spline`（抑制样条输出）

4. **“诚实基线”评估方法论（Honest Baseline Methodology）**  
   为避免因后端优化差异导致的速度比较失真，作者提出在同一后端（NumPy vs NumPy，Numba vs Numba）下对比 B-spline 与 LUT 的性能，从而分离出“表示形式”本身带来的加速效果。

5. **轻量级可移植推理格式**  
   输出为压缩的 `.npz` 文件 + JSON 清单，可在无 PyTorch 和样条库依赖的情况下加载执行，适合边缘部署。

### **相比现有方法的优势**

| 维度 | LUT-KAN 的优势 |
|------|----------------|
| **推理速度** | 在相同后端下实现 10–14× 加速（NumPy），9.5–11×（Numba），远超传统软件栈差异带来的提升 |
| **部署友好性** | 不依赖 PyTorch，推理路径极简，适合边缘设备 |
| **语义一致性** | 显式定义 OOB 行为，确保跨平台行为一致，提升可复现性 |
| **量化可控性** | 支持 per-segment 量化，保留 base branch 解析表达式（`spline_component` 表示），减少误差 |
| **公平评估** | “诚实基线”方法避免了向量化/JIT 效应干扰真实性能对比 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **合成实验（Controlled Sweeps）**：随机初始化的 KAN 层（input=10, output=8, grid=8, degree=3），用于控制变量分析。
- **真实任务案例研究（Case Study）**：基于 **CICIDS2017** 数据集的 DoS 攻击检测任务，用于验证实际部署效果。

### **实验设置**

- **硬件环境**：
  - CPU: AMD Ryzen 7 7840HS (8 cores / 16 threads)
  - RAM: 64GB DDR5
  - 单线程运行（`OMP_NUM_THREADS=1`, `NUMBA_NUM_THREADS=1`）
- **软件版本**：
  - Python 3.11.7, NumPy 1.26.3, Numba 0.59.0, PyTorch 2.1.2 (CPU)

### **评估指标**

| 类别 | 指标 |
|------|------|
| **准确性** | MAE（in-range / OOB）、MaxAbs Error、下游分类指标（Accuracy, Precision, Recall, F1） |
| **速度** | Steady-state latency (ms/iter, ms/sample)，Speedup（相对于同后端 B-spline） |
| **内存** | LUT artifact 大小、与原始 float 模型的内存开销比（LUT/Model） |
| **鲁棒性** | OOB 触发率（OOB_any_frac）、不同 boundary/oob_policy 下的表现矩阵 |

### **基线方法对比**

- **不公平基线（Stack-level）**：PyTorch 浮点推理 vs LUT + NumPy/Numba
- **诚实基线（Honest Baseline）**：
  - NumPy B-spline vs NumPy LUT
  - Numba B-spline vs Numba LUT  
  （两者均在同一后端、相同批大小、相同迭代协议下测试）

### **消融实验参数扫描**

- LUT 分辨率 $L \in \{16, 32, 64, 128\}$
- 量化方案：symmetric int8 vs asymmetric uint8
- boundary_mode：closed vs half_open
- oob_policy：clip_x vs zero_spline
- value_repr：phi vs spline_component
- 所有配置重复 5 次（种子 0–4），报告 mean ± std

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **推理速度（Honest Baseline）**

| 后端 | 平均 Speedup | 范围 |
|------|--------------|------|
| **NumPy** | **12.3×** | 11.4–14.0× |
| **Numba** | **10.5×** | 9.5–11.1× |

> 注：尽管 Numba 已对 B-spline 进行 JIT 优化，LUT 仍能带来约 10× 加速，说明加速来自表示形式本身。

#### ✅ **内存开销**

| $L$ | 内存开销倍数（LUT / Float Model） |
|-----|-------------------------------|
| 16  | 3.07× |
| 32  | 5.51× |
| **64** | **10.40×** |
| 128 | 20.18× |

> 主要开销来自 `q_table`（占比 73–88%），其余为 scale/y_min/knots 元数据。

#### ✅ **近似误差（$L=64$）**

| 量化方式 | MAE (in-range) | MaxAbs (in-range) |
|---------|----------------|--------------------|
| int8 (symmetric) | 1.59e-4 ± 1.0e-5 | 8.02e-4 ± 5.4e-5 |
| uint8 (asymmetric) | 1.58e-4 ± 1.1e-5 | 8.33e-4 ± 5.6e-5 |

> 误差随 $L$ 增大呈 $O(1/L)$ 衰减，表明插值误差主导。

---

### **与基线方法的对比结果**

| 对比维度 | 结果 |
|--------|------|
| **Stack-level Speedup (PyTorch → Numba LUT)** | 高达 **64.4×**（但包含后端切换效应） |
| **Honest Baseline Speedup** | **~12× (NumPy), ~10× (Numba)**，证明表示形式本身有效 |
| **分类质量保留（DoS 检测）** | F1 仅下降 **0.0002**（从 0.9900 → 0.9898），其他指标几乎不变 |
| **内存开销（真实模型）** | 总 LUT artifact 为 2.26MB，是原模型（200KB）的 **11.29×**，与理论预测一致 |

---

### **消融实验结果**

#### 🔍 **量化方案影响（$L=64$）**

| 方案 | MAE (in-range) | MaxAbs (OOB, zero_spline) |
|------|----------------|----------------------------|
| symmetric int8 | 1.59e-4 | 0.0892 |
| asymmetric uint8 | 1.58e-4 | 0.0915 |

> 两者性能相近，在零中心分布下差异不显著。

#### 🔍 **OOB 鲁棒性矩阵（$L=64$, half_open）**

| boundary_mode | oob_policy | OOB_frac | MAE(OOB) | MaxAbs(OOB) |
|-------------|------------|----------|----------|------------|
| closed | clip_x | 0.000 | N/A | N/A |
| half_open | clip_x | 10.1% | 3.12e-4 | 6.61e-4 |
| half_open | zero_spline | 10.1% | 2.45e-2 | **8.92e-2** |

> `zero_spline` 在 OOB 输入上可能导致较大偏差，需谨慎选择策略。

#### 🔍 **value_repr 消融**
- `spline_component` 更优：保留 base branch（如 SiLU）解析计算，避免量化误差，提升效率。

---

## 4. 关键结论和发现

### **主要发现**

1. **LUT-KAN 显著加速 KAN 推理**  
   在去除后端优化干扰后，仍可实现 **10–14× 的真实加速**，适用于边缘和实时场景。

2. **精度损失极小**  
   在 $L=64$ 下，函数级 MAE 约为 $1.6\times10^{-4}$，下游任务 F1 下降小于 0.0002，可视为无损部署。

3. **OOB 语义必须显式定义**  
   不同 `boundary_mode` 和 `oob_policy` 组合会导致显著不同的 OOB 行为，尤其当预处理使用 clipping 时，`half_open` 可能误判边界点为 OOB。

4. **内存是主要代价**  
   LUT 存储开销约为原始模型的 **10×（$L=64$）**，首层因边数多成为瓶颈（占 82.5%）。

5. **“诚实基线”至关重要**  
   若不控制后端一致性，可能高估 LUT 带来的收益，误导技术判断。

---

### **方法的局限性**

| 局限性 | 说明 |
|-------|------|
| **内存开销大** | 密集层边数多时 LUT 表体积巨大，不适合极端内存受限设备 |
| **静态编译** | 模型更新需重新编译 LUT，不支持在线微调或自适应 |
| **非全整数量化** | 当前仍使用 float32 进行反量化和 base branch 计算，未实现端到端整数推理 |
| **校准敏感性** | 量化范围依赖校准数据分布，若部署数据偏移可能导致性能下降 |
| **单精度参数存储** | scale 和 y_min 使用 float32，可用 float16 进一步压缩但可能引入数值误差 |

---

### **未来工作方向**

1. **稀疏 LUT 表示**：结合网络剪枝或重要性评分，只保留关键边的 LUT。
2. **低秩或因子化结构**：减少 dense layer 的边数增长。
3. **全整数推理流水线**：探索完全 int8/int16 的累积与激活路径。
4. **动态/自适应量化**：根据输入分布调整量化参数。
5. **支持更多函数基底**：扩展至 Chebyshev、RBF 等 KAN 变体。
6. **硬件协同优化**：针对 MCU/FPGA 设计定制 LUT 推理引擎。

---

> ✅ **代码与复现性**：所有代码、实验脚本、模型 artifact 均已开源，带固定 release tag (`v1.0.0`)，确保结果可复现。  
> 🔗 GitHub: [https://github.com/KuznetsovKarazin/lut-kan](https://github.com/KuznetsovKarazin/lut-kan)

</details>

---

### 4. [Robust Physics Discovery from Highly Corrupted Data: A PINN Framework Applied to the Nonlinear Schr\"odinger Equation](https://arxiv.org/abs/2601.04176)

**Authors**: Pietro de Oliveira Esteves  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.04176v1  

#### Abstract
We demonstrate a deep learning framework capable of recovering physical parameters from the Nonlinear Schrodinger Equation (NLSE) under severe noise conditions. By integrating Physics-Informed Neural Networks (PINNs) with automatic differentiation, we achieve reconstruction of the nonlinear coeffici...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Robust Physics Discovery from Highly Corrupted Data: A PINN Framework Applied to the Nonlinear Schrödinger Equation*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**在高噪声、稀疏观测条件下从实验数据中恢复非线性物理系统参数**这一挑战性逆问题展开研究。具体聚焦于**非线性薛定谔方程（Nonlinear Schrödinger Equation, NLSE）中的非线性系数 $ \beta $ 的识别问题**。

现实中的实验数据通常具有以下特点：
- 数据稀疏（传感器数量有限）
- 含有高达 20% 的加性高斯噪声
- 数值微分方法因噪声放大而失效

传统基于有限差分 + 优化的方法在此类场景下表现不稳定，难以准确估计物理参数。

---

### 提出的新方法与新思路
作者提出了一种基于 **Physics-Informed Neural Networks (PINNs)** 的深度学习框架，用于从严重污染的数据中鲁棒地发现物理规律并精确识别 NLSE 中的关键参数 $ \beta $。

#### 创新点包括：
- **将物理正则化作为噪声过滤器**：通过将 NLSE 显式嵌入神经网络的损失函数中，PINN 能够利用物理先验知识“过滤”掉测量噪声的影响，实现对真实动力学的重建。
- **完全无偏初始化**：将待识别参数 $ \beta $ 视为可训练变量，并从 $ \beta = 0.0 $ 开始训练，避免先验偏差，确保参数恢复是纯数据驱动且受物理约束引导的结果。
- **自动微分替代数值微分**：使用 PyTorch 的自动微分机制计算偏导数，避免了传统方法中因有限差分导致的噪声放大问题。
- **高噪声下的稳定性验证**：系统测试了在 20% 高斯噪声下不同 $ \beta $ 值和不同数据密度条件下的性能，展示了方法的泛化能力与鲁棒性。

---

### 相比现有方法的优势
| 维度 | 传统方法（如有限差分 + L-BFGS） | 本文 PINN 方法 |
|------|-------------------------------|----------------|
| 噪声敏感性 | 极高（数值微分会放大噪声） | 低（物理损失项起正则化作用） |
| 数据需求 | 需密集、规则网格数据 | 支持稀疏、随机采样数据 |
| 正则化调参 | 手动调整 Tikhonov 参数困难 | 物理方程天然提供隐式正则化 |
| 实现复杂度 | 多步骤流程（插值+求导+优化） | 端到端统一框架 |
| 可扩展性 | 对不规则几何适应差 | 易推广至复杂域 |

> ✅ **核心优势总结**：PINNs 在高噪声、小样本条件下展现出卓越的鲁棒性和精度，显著优于依赖数值微分的传统方法。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 并未使用真实实验数据，而是通过**数值模拟生成 NLSE 的精确解（ground truth）**
- 在真实解上添加 **20% 加性高斯噪声**，模拟高度 corrupted 的实验测量
- 训练数据点 $ N_u \in [100, 1000] $，随机采样自时空域 $ x \in [-5,5], t \in [0, \pi/2] $
- 使用 **Latin Hypercube Sampling (LHS)** 选取 20,000 个 collocation points 用于物理残差计算

---

### 实验设置
- **模型架构**：
  - Fully Connected DNN，输入 $(x,t)$，输出 $(u,v)$（即复场实部与虚部）
  - 4 层隐藏层 × 50 神经元，激活函数为 `tanh`
  - 总参数约 10,000
- **优化器**：Adam ($\text{lr} = 10^{-3}$)，训练 10,000 轮
- **损失函数**：
  $$
  \mathcal{L} = \lambda_{\text{data}} \mathcal{L}_{\text{data}} + \lambda_{\text{physics}} \mathcal{L}_{\text{physics}}, \quad \lambda_{\text{data}} = \lambda_{\text{physics}} = 1.0
  $$
  - $\mathcal{L}_{\text{data}}$: MSE 损失（预测 vs 噪声数据）
  - $\mathcal{L}_{\text{physics}}$: PDE 残差损失（通过自动微分计算）

- **硬件平台**：Google Colab 上的 NVIDIA Tesla T4 GPU（15GB VRAM），总耗时 ~80 分钟完成全部实验流程

---

### 评估指标
- **相对误差（Relative Error）**：用于衡量估计的 $ \hat{\beta} $ 与真值之间的偏差
  $$
  \text{Error}(\%) = \frac{|\hat{\beta} - \beta_{\text{true}}|}{\beta_{\text{true}}} \times 100\%
  $$
- **统计稳定性**：多次独立运行的标准差（std. dev.）
- **可视化分析**：时空误差热图、时间切片对比图、损失曲线演化
- **消融实验**：考察不同训练数据量 $ N_u $ 对性能影响

---

### 基线方法对比
文中虽未直接运行传统方法进行横向比较，但引用文献指出：
- 传统方法（如 SINDy 或有限差分 + 优化）在 >10% 噪声下即出现严重失稳
- 数值微分误差随网格尺度平方反比增长（$ e_{\text{derivative}} \sim e_{\text{data}} / h^2 $），极易放大噪声
- 因此，在 20% 噪声环境下，传统方法通常无法收敛或产生错误参数估计

> 作者强调，PINN 在此类极端噪声条件下仍能保持 <1% 的识别误差，表明其具有明显优势。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### 不同 $ \beta $ 下的识别精度（$ N_u = 500 $, 20% 噪声）
| 真实 $ \beta $ | 相对误差 (%) | 标准差 (%) | 训练时间 (min) |
|----------------|---------------|-------------|----------------|
| 0.5            | 0.71 ± 0.15   | <0.15       | 11.2 ± 1.3     |
| **1.0**        | **0.16 ± 0.05** | **<0.05**   | **10.8 ± 0.9** |
| 2.0            | 0.33 ± 0.10   | <0.10       | 12.1 ± 1.5     |

> 🔹 当 $ \beta = 1.0 $ 时，平均相对误差低至 **0.16%**，标准差仅 0.05%，表现出极高的精度与稳定性。

---

#### 数据稀疏性敏感性分析（固定 $ \beta = 1.0 $）
| 数据点数 $ N_u $ | 相对误差 (%) | 标准差 (%) |
|--------------------|---------------|-------------|
| 100                | 1.47 ± 1.10   | 1.10        |
| 250                | 1.59 ± 1.49   | 1.49        |
| **500**            | **0.36 ± 0.52** | **0.52**    |
| 1000               | 0.80 ± 0.79   | 0.79        |

> 🔹 最佳性能出现在 $ N_u = 500 $，进一步增加到 1000 反而导致误差上升，可能由于过拟合噪声所致  
> 🔹 即使只有 100 个噪声点，误差也控制在 2% 以内，体现极强的数据效率

---

### 消融实验结果（隐含在敏感性分析中）
- **数据量影响**：证明了物理正则化在极低数据量下依然有效
- **损失动态分析**（Figure 3）：
  - Physics Loss 快速下降 → 模型学会遵守 NLSE
  - Data Loss 稳定在平台期而非趋近零 → 成功避免对噪声过拟合
- **参数学习轨迹**（Figure 1）：
  - 从 $ \beta = 0.0 $ 初始化出发，快速收敛至真值（1.0），过程平稳无震荡

---

## 4. 关键结论和发现

### 主要发现
1. **PINNs 能有效充当“物理滤波器”**：即使输入数据含有 20% 高斯噪声，也能通过物理约束分离信号与噪声，实现高精度参数识别。
2. **极高的噪声鲁棒性**：在传统方法失效的噪声水平下，PINN 仍可实现 **<0.2% 的相对误差**（$ \beta = 1.0 $）。
3. **出色的数据效率**：仅需 **500 个随机采样的稀疏数据点**即可达到 sub-1% 精度。
4. **良好的泛化能力**：在 $ \beta \in [0.5, 2.0] $ 多种物理状态下均保持稳定性能。
5. **计算可及性强**：整个流程可在免费云资源（Google Colab + Tesla T4）上运行，总耗时约 80 分钟。

---

### 方法的局限性
1. **维度限制**：目前仅适用于 1D NLSE；向 2D/3D 扩展面临计算成本剧增（collocation points 数量呈指数增长）。
2. **噪声模型假设**：仅考虑加性白高斯噪声，未涵盖实际实验中常见的非高斯噪声、离群点或空间相关噪声。
3. **单参数识别**：仅识别非线性系数 $ \beta $，未同时恢复色散系数等其他参数，存在参数耦合与可辨识性问题。
4. **损失权重固定**：采用等权策略（$ \lambda_{\text{data}} = \lambda_{\text{physics}} = 1.0 $），缺乏自适应机制，可能影响某些 PDE 的收敛效率。
5. **外推能力未知**：训练时间区间为 $ t \in [0, \pi/2] $，未测试长期外推性能。

---

### 未来工作方向
- 探索 **更高维系统**（如 2D/3D NLSE 或 Gross-Pitaevskii 方程）
- 实现 **多参数联合识别**（dispersion + nonlinearity）
- 引入 **adaptive loss weighting** 策略（如 [Wang et al., 2021]）
- 验证方法在 **真实实验数据**（如光纤通信、冷原子系统）上的有效性
- 设计更高效的采样策略（如自适应 collocation point 选择）
- 测试模型在 **长时间外推任务** 中的表现

---

## 补充信息

- **代码开源地址**：[https://github.com/p-esteves/pinn-nlse-2026](https://github.com/p-esteves/pinn-nlse-2026)（MIT 许可证）
- **可复现性保障**：提供了完整的随机种子、超参数配置和运行环境说明
- **应用前景广泛**：适用于光学、流体力学、量子物理等领域中噪声大、数据少的科学机器学习任务

> 🌟 **总体评价**：本工作有力证明了 PINNs 在极端噪声环境下解决科学逆问题的巨大潜力，推动了 **Scientific Machine Learning** 向更贴近真实实验条件的方向发展。

</details>

---

### 5. [LLM-MC-Affect: LLM-Based Monte Carlo Modeling of Affective Trajectories and Latent Ambiguity for Interpersonal Dynamic Insight](https://arxiv.org/abs/2601.03645)

**Authors**: Yu-Zheng Lin, Bono Po-Jen Shih, John Paul Martin Encinas, Elizabeth Victoria Abraham Achom, Karan Himanshu Patel, Jesus Horacio Pacheco, Sicong Shao, Jyotikrishna Dass, Soheil Salehi, Pratik Satam  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03645v1  

#### Abstract
Emotional coordination is a core property of human interaction that shapes how relational meaning is constructed in real time. While text-based affect inference has become increasingly feasible, prior approaches often treat sentiment as a deterministic point estimate for individual speakers, failing...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-MC-Affect: LLM-Based Monte Carlo Modeling of Affective Trajectories and Latent Ambiguity for Interpersonal Dynamic Insight

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统文本情感分析方法通常将情感视为**确定性的点估计**（deterministic point estimate），忽略了人类互动中情感表达的**主观性、潜在模糊性（latent ambiguity）和动态耦合特性**。尤其在师生对话等互动场景中，这种简化模型难以捕捉情绪如何实时协调演化。

此外，基于生物信号（如神经同步、心率变异性）的情感协调研究虽具高时间分辨率，但依赖昂贵设备且难以扩展到大规模自然学习环境。

### 提出的新方法与创新思路
本文提出 **LLM-MC-Affect** —— 一种基于大语言模型（LLM）的**概率性情感建模框架**，其核心思想是：

- 将每条话语的情感状态建模为一个**连续隐变量分布 $ p(e|u) $**，而非单一标签；
- 利用 LLM 在非零解码温度下的**随机生成行为**进行 **Monte Carlo 采样**，通过多次独立推理近似该分布；
- 从采样结果中提取**均值（central affective tendency）和方差（affective ambiguity）**，构建带有不确定性感知的情感轨迹（affective trajectory）；
- 基于这些轨迹，进一步分析人际间的情绪动态，包括：
  - 序列交叉相关（sequential cross-correlation）
  - 最优滞后（optimal lag）
  - 趋势斜率（slope-based trend indicators）

该方法实现了从低层次情感感知到高层次人际互动洞察的桥梁。

### 相比现有方法的优势
| 维度 | 传统方法 | LLM-MC-Affect |
|------|--------|---------------|
| 情感表示 | 确定性标签（如正面/负面） | 连续概率分布（含均值与方差） |
| 主观性处理 | 忽略或平均化 | 显式量化“感知模糊性”作为诊断信号 |
| 动态建模 | 静态分类或简单滑动窗口 | 构建纵向情感轨迹并分析耦合模式 |
| 数据需求 | 依赖人工标注或生物传感器 | 完全基于文本，zero-shot，无需微调 |
| 可解释性 | 黑箱输出 | 提供可解释的互动模式类型学（如有效支架、共享疲劳等） |

> ✅ **创新亮点**：首次系统地将 LLM 的解码随机性用于情感不确定性建模，并结合经典时间序列分析技术实现对人际情感动态的结构化解析。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Google's Education Dialogue Dataset**（Shani et al., 2024）
  - 包含多个主题的模拟师生教学对话（multi-turn instructional interactions）
  - 示例主题包括：`Personification`, `The Cold War`, `World War 2`, `The Respiratory System`, `Achilles`
  - 对话设计包含明确的学生/教师偏好与反应设定，便于验证模型合理性

> 注：使用模拟数据出于伦理与可行性考虑，避免真实课堂中的隐私与IRB审批难题。

### 实验设置
- **模型选择**：
  - 商业闭源模型：`GPT-4.1`, `GPT-3.5-Turbo`
  - 开源模型：`Llama 3.3 70B`, `Gemma 3 4B`, `Phi-4 14B`, `GPT-OSS 120B`
- **解码参数**：
  - 温度 $ T \in [0.1, 1.0] $
  - 每轮话语执行 $ K=20 $ 次独立 MC 采样
- **统一提示工程**：
  - 所有模型使用相同的 **psychometric prompt (pemo)**，确保跨模型比较一致性
  - Prompt 设计为心理学专家角色，要求进行细粒度情感评分（0–5，步长0.5），强调上下文连续性和细微情绪变化

### 评估指标
- **情感轨迹稳定性**：不同温度下均值波动程度
- **情感模糊性量化**：MC 采样方差 $ \sigma^2 $
- **人际动态指标**：
  - 归一化交叉相关函数（NCCF）
  - 最优滞后 $ L^* = \arg\max_L |R_{TS}(L)| $
  - 情感趋势斜率 $ \beta_T, \beta_S $（通过线性回归拟合）
- **模式识别能力**：依据表1联合指标判断互动类型（如 Effective Scaffolding, Shared Fatigue 等）

### 基线方法对比
本文未直接对比传统 sentiment classifiers（如BERT-based），而是聚焦于以下方面的**相对表现差异**：
- 不同 LLM 在相同设置下的轨迹一致性
- 不同解码温度对分布形态的影响
- 各模型在捕捉负向情绪转折点上的敏感性差异

因此，基线实为“不同模型 + 不同配置”的横向比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与发现

#### （1）情感分布建模的有效性（Figure 2a）
- 在低温度（$ T=0.1 $）时，LLM 输出呈现**双峰分布**（bimodal），反映同一句话可能被解读为两种高置信情感状态；
- 随着温度升高，分布趋于平滑，方差增加，揭示更多潜在解释空间；
- 方差本身成为衡量“语言歧义”或“情感模糊性”的代理指标。

#### （2）情感轨迹稳健性（Figure 2b）
- 尽管方差随温度上升而翻倍（$ \sigma^2 $ 从 0.01 → 0.024），但**均值保持高度稳定**（波动范围仅 -0.11 至 -0.26）；
- 表明 MC 估计能有效滤除噪声，保留核心情感信号；
- 支持后续人际分析的可靠性。

#### （3）跨模型行为差异（Figure 2c）
| 模型 | 表现特点 |
|------|---------|
| `GPT-4.1` | 情感分辨率最高，准确捕捉早期负面情绪及后期回升，不确定性带窄 |
| `GPT-3.5-Turbo` | 结构相似但不确定性更大（宽置信区间），表明信心较低 |
| `Llama 3.3 70B` | **极端正向偏见**：完全忽略初期负面情绪，持续上升趋势，归因于“无害性对齐”（harmlessness alignment） |
| `GPT-OSS 120B`, `Gemma`, `Phi-4` | **保守估计偏差**：低估负面强度，恢复阶段受限（plateau near neutral） |

> ❗ 结论：并非所有 LLM 都适合情感建模；架构与训练目标显著影响情感敏感度。

#### （4）人际动态识别案例（Figure 3 & Table 1）
以 `Personification` 主题为例：
- **最优滞后**：$ L^* = +1 $，$ R_{TS}(L^*) = 0.999 $ → 教师领先，学生跟随
- **趋势斜率**：$ \beta_T = 0.1621 $, $ \beta_S = 0.2532 $ → 双方情绪均显著改善
- **综合判断**：**Effective Scaffolding**（有效支架式教学）

其他案例：
- `The Cold War` → **Shared Fatigue**（双方同步下滑）
- `World War 2` → **Feedback Burnout**（学生主导的共同衰退）
- `The Respiratory System`, `Achilles` → **Effective Scaffolding**

这些分类与预设的角色偏好高度一致，证明方法具备**生态效度**。

---

## 4. 关键结论和发现

### 主要发现
1. **情感不应被视为确定性标签**：利用 LLM 解码随机性可有效逼近情感的潜在分布，显式建模“感知模糊性”具有实际意义。
2. **Monte Carlo + LLM 是可行路径**：无需人工标注即可获得高质量、不确定性感知的情感轨迹。
3. **GPT-4.1 在情感建模上优于其他模型**：尤其在识别负面情绪转折和动态演变方面更具保真度。
4. **人际情感模式可通过统计指标结构化识别**：结合 lag、correlation 和 slope 可归纳出可解释的互动类型学。
5. **方法具备教育场景的应用潜力**：可用于自动识别教学策略效果（如是否成功激发兴趣、是否存在情绪耗竭）。

### 局限性
1. **方差解释的模糊性**：MC 方差可能混杂了多种不确定性来源（epistemic vs. aleatoric），包括模型偏差、prompt 敏感性、解码随机性等，不能完全等同于人类感知模糊性。
2. **相关不等于因果**：交叉相关仅反映顺序对齐，无法推断因果影响。
3. **短对话限制**：当前分析基于较短对话（~15轮），长程动态建模仍需滑动窗口或分层方法。
4. **计算成本高**：每次话语需运行 $ K=20 $ 次前向推理，对大规模部署构成挑战。

### 未来工作方向
- 探索更高效的采样策略（如 importance sampling）降低计算开销；
- 开发专用的 **affective-sensitive LLM** 或 adapter 模块；
- 引入滑动窗口或 hierarchical modeling 处理长对话；
- 将框架拓展至心理咨询、客服、团队协作等人际互动场景；
- 结合 qualitative coding 进行混合方法验证。

---

> 📌 **总结一句话**：  
> **LLM-MC-Affect 成功将 LLM 的“随机性”转化为“情感不确定性”的测量工具，构建了一条无需生物传感、可扩展、可解释的情感互动分析新范式，特别适用于教育等社会交互密集型领域。**

</details>

---

### 6. [EDCO: Dynamic Curriculum Orchestration for Domain-specific Large Language Model Fine-tuning](https://arxiv.org/abs/2601.03725)

**Authors**: Jing-Cheng Pang, Liu Sun, Chang Zhou, Xian Tang, Haichuan Ma, Kun Jiang, Jianlong Wang, Kai Zhang, Sijie Wu, Haoran Cai, Chenwei Wu, Xubin Li, Xin Chen  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03725v1  

#### Abstract
Domain-specific large language models (LLMs), typically developed by fine-tuning a pre-trained general-purpose LLM on specialized datasets, represent a significant advancement in applied AI. A common strategy in LLM fine-tuning is curriculum learning, which pre-orders training samples based on metri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# EDCO: Dynamic Curriculum Orchestration for Domain-specific Large Language Model Fine-tuning  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统的大语言模型（LLM）领域微调通常采用随机采样或静态课程学习（Static Curriculum Learning），即在训练前固定样本顺序。这类方法存在以下问题：
- **缺乏动态适应性**：无法根据模型在训练过程中的能力变化调整样本难度。
- **熵坍塌（Entropy Collapse）**：尤其在强化学习微调（RLFT）中，模型推理熵迅速下降，导致探索不足、收敛过早，限制性能上限。
- **低效利用稀缺高质量领域数据**：在医学、法律等数据昂贵且有限的领域，如何高效利用每一条样本至关重要。

### 🚀 提出的新方法：EDCO
作者提出 **EDCO（Entropy-based Dynamic Curriculum Orchestration）**，一种基于**推理熵（inference entropy）**的动态课程编排框架，其核心思想是：
> “维持高推理熵 = 更强的学习信号 + 更有效的探索”

#### 主要创新点：
1. **动态课程生成机制**  
   不再依赖预定义的“由易到难”策略，而是**周期性地重新评估所有候选样本的推理熵**，选择当前对模型最具挑战性的样本组成下一阶段的训练集。

2. **逆向课程学习（Reverse Curriculum）**  
   优先选择高熵样本（即模型最不确定的答案），形成“从难到更难”的反向课程路径，推动模型持续处于学习前沿。

3. **高效的熵估计技术（Efficient Entropy Estimation）**
   - **Quick-Answer Prompting (QAP)**：通过提示工程引导模型快速输出答案，避免长链思维（Chain-of-Thought）带来的计算开销。
   - **Prefix Entropy Approximation**：仅用输出序列前 $L$ 个 token（如 50–128）估算整个序列的熵，显著降低计算成本。

4. **兼容多种微调范式**
   EDCO 可无缝集成于 **Supervised Fine-Tuning (SFT)** 和 **Reinforcement Learning Fine-Tuning (RLFT)** 框架中。

### 🔍 相比现有方法的优势
| 方法 | 局限性 | EDCO 改进 |
|------|--------|----------|
| 随机采样（Random Sampling） | 忽略样本价值差异 | 主动挑选高信息量样本 |
| 静态课程（Length/AC/PPL） | 固定顺序，不随模型进化 | 动态更新，自适应模型状态 |
| SEC（Self-evolving Curriculum） | 基于bandit策略，训练不稳定 | 使用确定性熵排序，稳定可靠 |
| 全序列熵计算 | 计算代价极高，不可扩展 | 前缀近似法提速83.5%，精度保持 |

---

## 2. 核心实验方法和设置

### 📚 数据集
实验覆盖多个专业领域，验证泛化能力：

| 领域 | 数据集 | 规模 | 特点 |
|------|-------|------|------|
| **通信** | 自建 Wireless & Datacom | 各12,000 QA对 | 包含产品文档、技术方案，涵盖多跳推理 |
| **医学** | MedQA (Jin et al., 2021) | 多语言医疗考试题 | 多选题+临床案例，需专业知识 |
| **法律** | JEC-QA (Zhong et al., 2020) | ~26,000 多选题 | 中国司法考试题，含单选/多选，逻辑复杂 |

> 所有测试集均为独立构建的 230 条高难度未见问题。

### ⚙️ 实验设置
- **基础模型**：`Qwen3-4B`, `Llama3.2-3B`
- **微调方式**：
  - **SFT**：最小化交叉熵损失
  - **RLFT**：使用 GRPO 算法优化奖励函数（Deepseek-V3 自动验证 或 规则验证）
- **评估指标**：**Answer Accuracy**（准确率）
- **硬件平台**：256核Kunpeng CPU + 8×Ascend 910B NPU集群

### 🆚 基线方法对比
| 类型 | 方法 | 描述 |
|------|------|------|
| 静态课程 | RS（Random Sampling） | 默认基准 |
| | Length | 按输入长度排序（短→长） |
| | AC（Answer Complexity） | 按答案句子数排序 |
| | PPL（Perplexity） | 初始模型打分，低PPL先训 |
| 动态课程 | SEC (Chen et al., 2025) | Bandit策略学习课程 |
| | Dynamic-PPL | 定期更新PPL得分 |
| 本文方法 | **EDCO** | 动态选择最高推理熵样本 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（RLFT 设置下）

| 方法 | Wireless (Qwen3-4B) | Datacom (Qwen3-4B) |
|------|---------------------|--------------------|
| No Training | 35.22% | 40.00% |
| RS | 34.35% | 40.43% |
| PPL | 33.91% | 44.78% |
| **EDCO** | **38.70%** ↑ | **46.96%** ↑ |

> 在无线通信领域，RS 和 AC 反而导致性能退化；而 EDCO 显著提升稳定性与最终表现。

### ✅ SFT 结果对比

| 方法 | Wireless | Datacom |
|------|---------|--------|
| RS | 31.7% | 33.0% |
| PPL | 32.6% | 33.0% |
| **EDCO** | **33.7%** | **36.3%** |

> EDCO 在两个领域均取得最佳成绩，尤其在 Datacom 上比 PPL 高出 **3.3%**。

### 🏥 跨领域泛化能力（Llama3.2-3B, RLFT）

| 数据集 | RS | PPL | **EDCO** |
|--------|----|-----|---------|
| MedQA | 32.9% | 24.6% | **36.7%** |
| JEC-QA | 16.2% | 12.4% | **17.4%** |

> 在医学和法律领域也全面超越基线，证明其通用性。

### 🔬 动态方法对比（Datacom, Qwen3-4B）

| 方法 | 准确率 |
|------|--------|
| SEC | 34.78% |
| Dynamic-PPL | 41.3% |
| **EDCO** | **47.0%** |

> 尽管 Dynamic-PPL 已动态更新，但因仍依赖 PPL 这一弱相关指标，效果远不如基于实时推理熵的 EDCO。

### 🔍 消融实验结果

#### （1）熵估计效率（Table 3）
| 方法 | 单进程耗时（秒/样本） | 并行（8卡） |
|------|------------------------|------------|
| Full-sequence | 2.24 | 0.24 |
| **Prefix-based (Ours)** | **0.37** | **0.04** |

> **计算时间减少 83.5%**，且并行后接近实时更新可行。

#### （2）前缀长度影响（Fig. 4b）
- 当 prefix token 数 ≥ 50 时，熵趋势趋于稳定。
- 推荐默认配置：**50–128 tokens**，平衡效率与稳定性。

#### （3）Quick-Answer Prompting（QAP）消融（Fig. 5）
- 使用 QAP：prefix 与 full-sequence 熵的相关系数为 **0.63**
- 移除 QAP：相关性降至 **0.32**
> 表明 QAP 对前缀熵的有效性至关重要。

#### （4）是否应避开最高熵样本？（Moderate-Entropy 实验）
| 策略 | 准确率 |
|------|--------|
| Top 0–6.67%（原EDCO） | **46.96%** |
| Top 5–11.67%（中等熵） | 44.78% |

> 最高熵样本并未引入噪声或异常值，反而带来更强学习信号。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **推理熵是衡量样本学习价值的有效指标**  
   高熵样本对应模型不确定性最大处，正是信息增益最高的位置。

2. **动态课程优于静态课程**  
   固定顺序容易陷入局部最优或误导训练方向（如 AC 在 Datacom SFT 中严重失败）。

3. **“逆向课程”更适合LLM微调场景**  
   由于预训练模型已具备广泛知识，“从难开始”能更快激发深层理解，避免在简单样本上浪费梯度更新。

4. **EDCO 显著缓解 RLFT 中的熵坍塌问题**  
   图3(a)显示，EDCO在整个训练过程中维持较高推理熵，而RS迅速坍塌。

5. **高效熵估计算法使动态课程实用化**  
   前缀近似 + QAP 将计算开销降低一个数量级，支持大规模应用。

### ⚠️ 方法局限性
1. **周期性熵重估仍有计算开销**  
   虽然比全序列快得多，但仍比标准微调多约10% wall-clock time（33.58h vs 30.67h）。
   
2. **固定更新间隔不够智能**  
   当前按固定步数更新课程，未来可考虑基于熵变化率或性能 plateau 自适应触发。

3. **依赖高质量领域数据预处理**  
   若原始数据存在大量模糊或错误表述，高熵可能反映的是歧义而非挑战性。

### 🔮 未来工作方向
1. 设计轻量化模型预测样本熵，无需每次前向传播；
2. 引入自适应调度机制，根据模型学习状态动态决定何时更新课程；
3. 扩展至更多低资源语言或高度专业化科学领域（如量子物理、生物制药）；
4. 探索与其他数据选择方法（如主动学习）结合的可能性。

---

## 总结
**EDCO 是首个将推理熵作为动态课程驱动信号的领域微调框架**。它打破了传统“由易到难”的课程设计范式，提出“聚焦最难样本”的逆向策略，并通过高效的前缀熵估计实现可扩展性。实验证明，该方法在通信、医学、法律等多个领域显著提升 SFT 与 RLFT 性能，具有广泛的适用前景。

</details>

---

### 7. [ReEfBench: Quantifying the Reasoning Efficiency of LLMs](https://arxiv.org/abs/2601.03550)

**Authors**: Zhizhang Fu, Yuancheng Gu, Chenkai Hu, Hanmeng Liu, Yue Zhang  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03550v1  

#### Abstract
Test-time scaling has enabled Large Language Models (LLMs) to tackle complex reasoning, yet the limitations of current Chain-of-Thought (CoT) evaluation obscures whether performance gains stem from genuine reasoning or mere verbosity. To address this, (1) we propose a novel neuro-symbolic framework ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReEfBench: Quantifying the Reasoning Efficiency of LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前对大型语言模型（LLMs）推理能力的评估主要依赖于 **Chain-of-Thought (CoT)** 输出的最终准确性，而忽略了推理过程的质量。这种“黑箱”式评估存在以下问题：
- **无法区分真正的逻辑深度与冗长的无效思考**（如“overthinking”现象）；
- 缺乏对推理效率（reasoning efficiency）的量化标准；
- 现有数据集在可控性、可扩展性和形式化逻辑基础方面不足。

### 提出的新方法与思路
作者提出了一个名为 **ReEfBench** 的神经符号（neuro-symbolic）评估框架，用于非侵入式、全面的过程中心型（process-centric）推理效率量化分析。

#### 核心创新点：
1. **提出“推理效率”公式：`W = P × t`**
   - `W`: Logical Depth（逻辑深度），表示完成任务所需的抽象逻辑工作量；
   - `t`: Cost（计算消耗），以生成的 token 数量衡量；
   - `P`: Reasoning Efficiency（推理效率），即单位计算成本下的逻辑增益。
   - 这一公式将推理从“是否正确”提升到“是否高效”的评价维度。

2. **构建 ReEfBench 框架**
   - 五阶段流水线：数据生成 → LLM响应获取 → 响应解析 → 节点处理 → 行为指标聚合。
   - 利用小规模 LLM 作为 parser 将自然语言推理链分解为结构化逻辑节点，并通过符号规则验证其逻辑深度与正确性。

3. **引入六维诊断指标体系**
   - **Logical Depth (Sld)**: 最大有效逻辑深度
   - **Cost (Scost)**: token 数 + planning/reflection 步骤频率
   - **Exploration (Sexp)**: 探索的独特正确节点数
   - **Efficiency (Seff)**: token 效率与有效跨度
   - **Coherence (Scoh)**: 元认知步骤（planning/reflection）是否带来实际进展
   - **Redundancy (Sred)**: 句子与节点级别的重复程度

4. **识别四种行为原型（Behavioral Prototypes）**
   - 有效求解者（Effective Solver）
   - 深度漫游者（Deep Wanderer）
   - 空洞模仿者（Hollow Mimic）
   - 懒惰猜测者（Lazy Guesser）

### 相比现有方法的优势
| 特性 | ReEfBench | 现有方法（如 ProntoQA, FOLIO, Roscoe） |
|------|-----------|----------------------------------------|
| Scalable | ✅ | ❌ 或部分支持 |
| FOL 形式化基础 | ✅ | 部分支持 |
| Logic-only 评估 | ✅ | 多含知识干扰 |
| Logical Depth 可计算 | ✅ | ❌ 或仅二值有效性 |
| Behavioral Process 分析 | ✅ | ❌ 或有限 |
| Interpretable 指标 | ✅ | 多为不可解释分数 |
| Non-intrusive 设计 | ✅ | 多需特定输出格式 |

> ✅ 表示支持，❌ 表示不支持或严重受限

---

## 2. 核心实验方法和设置

### 数据集
- **自建数据集**：基于 First-Order Logic (FOL) 自动生成，参数化控制复杂度（Complexity Level 3–11）。
- 每个样本包含：
  - 前提（Premises）
  - 结论（Conclusion）
  - 黄金推理路径（Golden Solution）
- 使用 **backward-chaining** 方法生成多跳推理图，确保逻辑深度精确可控。
- 添加 distractor premises 增加难度。
- 总共生成 100 个样本/复杂度级别，共 900 个测试实例。

### 实验设置
- **评估模型**：涵盖 25 个开源与闭源 LLMs，包括：
  - 商业模型：Claude-Opus/Sonnet-4.5 (.long/.short)
  - 开源模型：Qwen3 系列（3B–235B）、DeepSeek-R1、DS-R1-Distill-Qwen 等
- **Prompt 设置**：
  ```text
  Please answer the question based on the given information...
  Please reason step by step, show your reasoning process and put your final answer in \boxed{}
  ```
- **API 参数**：
  - `temperature=0`
  - reasoning models: max_tokens=24,000
  - non-reasoning models: max_tokens=8,000

### 评估指标
- 主要使用上述 **六个归一化诊断指标**（Sld, Scost, Sexp, Seff, Scoh, Sred）
- 所有原始指标经 max-normalization 映射至 [0,1] 区间后加权平均
- 引入 **confidence score (Sc)** 衡量模型属于某类别的典型性（基于 Voronoi 边界距离）

### 基线方法对比
本文未直接与其他评估框架进行端到端对比，而是通过 **系统性比较已有框架属性**（见 Table 1）证明 ReEfBench 在可扩展性、形式化基础、过程分析等方面的全面优势。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Complexity Level 11）

| Model | Category | Sld | Scost | Tokens(k) | Depth |
|-------|----------|-----|--------|------------|--------|
| Qwen3-235B-thinking | DeepWanderer | 1.00 | 0.88 | 16.8 | 10.54 |
| Claude-Opus-4.5.long | EffectiveSolver | 0.97 | 0.37 | 3.5 | 10.27 |
| QwQ-32B | HollowMimic | 0.68 | 0.61 | 5.7 | 7.12 |
| Qwen2.5-32B-Instruct | LazyGuesser | 0.28 | 0.16 | 0.7 | 2.90 |

> 数据来源：Table 2 & Table 13

### 四类行为原型特征总结

| 类别 | 特征描述 | 代表模型 | Sld | Scost | Seff | Sred |
|------|---------|--------|-----|--------|------|------|
| **Effective Solver** | 高效精准，低冗余 | Claude-Opus-4.5.long | 0.97 | 0.37 | 0.60 | 0.28 |
| **Deep Wanderer** | 高消耗、高探索、低效 | Qwen3-235B-thinking | 1.00 | 0.88 | 0.47 | 0.77 |
| **Hollow Mimic** | 高成本但浅层，伪反思 | QwQ-32B | 0.68 | 0.61 | 0.48 | 0.62 |
| **Lazy Guesser** | 极低成本，停滞或崩溃 | Qwen2.5-32B-Instruct | 0.28 | 0.16 | 0.55 | 0.59 |

> 类别均值见 Table 2 最后四行（weighted by Sc）

### 与基线方法的对比结果
- **Long CoT vs Short CoT**（Table 3）：
  - 早期 Long CoT 模型（如 QwQ-32B）比 Short CoT 深度高出 97.1%
  - 当前先进 Short CoT（如 Qwen3-235B-Instruct）仅落后其 Long 版本 **1.6%**
  - 表明 **Short CoT 已接近 Long CoT 的推理深度**

- **Reflection 的作用显著**（Table 4）：
  - 成功模型（如 Claude-Opus-4.5.long）即使在 short 模式下也保持 reflection（0.7%）
  - 失败模型（如 Qwen2.5-32B-Instruct）reflection 几乎为零（0.0%）
  - 支持 “**Short CoT + Reflection**” 是高效推理的关键机制

### 消融实验结果

#### （1）混合训练的影响（Mixed Training）
- 对比纯 Long CoT 与 Long+Short 混合训练：
  - 混合训练导致 **提前饱和（premature saturation）甚至崩溃（collapse）**
  - 如 Qwen3-235B.long（mixed）相比 Qwen3-235B-thinking（pure）更早停止扩展 token 数
- **结论**：混合 Long/Short CoT 数据会干扰 High-t 策略，破坏自适应扩展能力

#### （2）蒸馏（Distillation）的效果限制
- 将 Qwen3-32B（teacher）的 Long CoT 能力蒸馏至 14B/8B/4B 学生模型
- 发现：
  - 小模型能模仿 **reflection/planning 的频率**（behavioral mimicry）
  - 但 **语义质量随规模下降**（4B < 8B < 14B < 32B）
  - 仅 **14B 模型** 能同时复制行为模式与逻辑深度
- **结论**：容量不足时，distillation 导致 “**Diluted Expansion**”，即形式模仿但无实质进步

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **扩展 token 生成不是深度推理的前提条件**
   - 高效模型（如 Claude-Opus-4.5.long）可在少量 token 内达成高逻辑深度。
   - “Overthinking” 并不等于更强推理。

2. ✅ **识别出四种推理行为原型**
   - 成功路径有两种：
     - **High-t Strategy**（Deep Wanderer）：靠 exhaustive search 达成深度
     - **High-P Strategy**（Effective Solver）：靠高效率精准推导
   - 失败模式也有两种：
     - **Hollow Mimic**：徒有其表的反思与规划，无实际逻辑推进
     - **Lazy Guesser**：面对复杂问题选择停滞或崩溃

3. ✅ **Short CoT + Reflection 可媲美 Long CoT**
   - 现代 Short CoT 模型通过引入 reflection 机制，已大幅缩小与 Long CoT 的差距。
   - 推理能力更多取决于训练策略而非输出长度。

4. ⚠️ **混合 Long/Short CoT 训练有害**
   - 容易造成策略干扰，导致模型在需要深入推理时无法扩展计算资源。

5. ⚠️ **蒸馏无法复制真正的推理能力**
   - 小模型可以模仿大模型的“思考形式”（如频繁反思），但由于内在容量限制，无法实现等效逻辑进展。

### 方法的局限性
1. **领域受限于 First-Order Logic (FOL)**
   - 虽然保证了逻辑可验证性，但难以推广到开放域问答、数学证明等更复杂场景。

2. **非侵入式设计限制**
   - 仅分析显式生成的文本，无法捕捉隐式推理过程。

3. **分类边界模糊**
   - 四种原型是统计聚类结果，个体模型可能跨类别表现，分类具有相对性。

### 未来工作方向
- 扩展 ReEfBench 至其他逻辑系统（如模态逻辑、高阶逻辑）
- 探索如何引导模型在 **High-P 与 High-t 之间动态切换**（adaptive reasoning）
- 研究更有效的 **small model reasoning distillation** 方法，突破容量瓶颈
- 将该框架应用于 **训练过程监控与优化**

---

> 🔗 **项目地址**：https://anonymous.4open.science/r/LoG-1AD8/  
> 📄 **论文链接**：https://arxiv.org/abs/2601.03550

</details>

---

### 8. [Anti-Length Shift: Dynamic Outlier Truncation for Training Efficient Reasoning Models](https://arxiv.org/abs/2601.03969)

**Authors**: Wei Wu, Liyi Chen, Congxi Xiao, Tianfu Wang, Qimeng Wang, Chengqiang Lu, Yan Gao, Yi Wu, Yao Hu, Hui Xiong  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03969v1  

#### Abstract
Large reasoning models enhanced by reinforcement learning with verifiable rewards have achieved significant performance gains by extending their chain-of-thought. However, this paradigm incurs substantial deployment costs as models often exhibit excessive verbosity on simple queries. Existing effici...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Anti-Length Shift: Dynamic Outlier Truncation for Training Efficient Reasoning Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前大型推理模型（reasoning models）在强化学习（RL）训练过程中出现的 **length shift** 现象。这一现象表现为：即使对于已经能够正确解答的简单问题，模型在训练中仍倾向于生成越来越长、冗余的推理链（Chain-of-Thought, CoT），导致推理成本显著增加。

传统方法通过引入显式的 **length penalty** 或 **budget constraint** 来抑制长度，但这会引发优化冲突（optimization conflicts），即长度压缩目标与准确性最大化目标不一致，从而影响收敛性和探索能力。

### 提出的新方法与新思路
作者提出了一种名为 **Dynamic Outlier Truncation (DOT)** 的训练时干预机制，其核心思想是：

- **Selective Truncation**：仅对“所有生成结果都正确的 rollout 组”（all-correct groups）中的极端长尾响应进行截断。
- **基于组内统计的动态阈值**：使用组内平均长度 $ \mu_L $ 和标准差 $ \sigma_L $ 定义截断阈值 $ T = \mu_L + \alpha \cdot \sigma_L $（通常取 $ \alpha=3 $，即“三西格玛”原则），只截断统计意义上的异常值。
- **不对奖励函数做修改**：避免了直接在 RL 目标中加入长度惩罚所带来的优化干扰。

此外，为稳定训练过程，作者还引入了两个辅助技术：
- **KL-Cov 正则化**：防止策略熵过快坍缩，维持探索能力。
- **Predictive Dynamic Sampling**：动态调整采样率以应对训练后期“全正确组”比例上升带来的效率下降问题。

### 相比现有方法的优势
- **避免优化冲突**：不将长度作为显式优化目标，而是通过后处理干预冗余行为，保持了原始 GRPO 的优化稳定性。
- **保留复杂问题的长程推理能力**：仅在“已解决”的简单问题上进行截断，不对困难问题施加限制。
- **高效且可扩展**：DOT 干预的样本极少（约 0.5%），却能驱动全局策略向更简洁的方向演化。
- **性能与效率双提升**：不仅大幅减少推理 token 数量，还能同时提升准确率，显著外推了 efficiency-performance 的帕累托前沿（Pareto frontier）。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：`DeepScaleR-Preview` 数据集。
- **评估基准**（均为高难度数学推理任务）：
  - `AIME-24`, `AIME-25`
  - `AMC`（AMC-22 和 AMC-23）
  - `MATH-500`

### 实验设置和评估指标
- **基础模型**：`DeepSeek-R1-Distill-Qwen` 系列，涵盖 1.5B、7B 和 32B 三种规模。
- **解码参数**：temperature=0.6, top_p=0.95, top_k=20，最大生成长度 32,768 tokens。
- **评估方式**：每题生成 32 个样本，报告 **pass@1 准确率** 和 **平均生成长度（token 数）**。
- **实现框架**：基于 `verl` 框架，使用 `FSDP` 分布式训练，`SGLang` 高效服务 rollout。

### 基线方法对比
对比了多种前沿的高效推理方法，主要包括：
- **显式长度惩罚类**：`O1-Pruner`, `Laser-DE`, `LC-R1`
- **难度感知类**：`DAST`, `DLER-R1`
- **模式切换类**：`AdaptThink`, `SIRI`
- **强基线**：`DeepScaleR-Preview`, `OverThink`

所有基线均在统一评估协议下重新评测，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 1.5B 模型为例，AIME-24 上）

| 方法 | Acc (%) | Length (tokens) |
|------|---------|-----------------|
| Original | 30.0 | 15,498 |
| SIRI-high | 43.6 | 10,049 |
| **DOT-8K (Ours)** | **52.2** | **5,151** |
| **DOT-4K (Ours)** | 43.1 | **3,342** |

- **DOT-8K** 在 AIME-24 上将准确率从 30.0% 提升至 **52.2%**，同时将平均 token 数降低 **66%以上**。
- **DOT-4K** 仅使用原始模型 **21.6%** 的 token，准确率仍达 43.1%，优于 SIRI-high。
- 与 SIRI-high 相比，DOT-8K 在准确率上高出 **8.6%**，而 token 消耗仅为一半。

### 与其他基线的对比优势
- 在多个模型尺度（1.5B, 7B, 32B）和多个基准上，DOT 均显著外推了帕累托前沿。
- 例如，在 7B 模型上，DOT-8K 达到 **62.6%** AIME-24 准确率，超越原始模型 7.5%，同时仅使用约 **37%** 的响应长度。
- 与同样追求极简长度的 `DLER-R1` 相比，DOT 在保持同等甚至更低 token 数的同时，进一步提升了准确率（如 1.5B 上 +7.3%）。

### 消融实验结果（Table 3）
消融研究验证了各组件的重要性：

| 变体 | AIME-24 Acc | AIME-24 Length |
|------|-------------|---------------|
| DOT-8K (完整) | 52.2 | 5,151 |
| w/o DOT | 51.5 | 6,879 |
| w/o Group-Conditional Truncation | 47.8 | 5,071 |
| w/o KL-Cov | 47.9 | 6,057 |
| w/o Predictive Sampling | 48.9 | 5,637 |

- 移除 **DOT** 导致长度显著增加，说明其有效抑制了冗余。
- 移除 **group-conditional truncation**（即对所有组统一截断）严重损害性能，证明了“仅在 all-correct 组截断”的必要性。
- 移除 **KL-Cov** 或 **Predictive Sampling** 均导致性能下降，验证了它们对训练稳定性的贡献。

---

## 4. 关键结论和发现

### 主要发现
1. **Length Shift 是冗余的根本原因**：模型在训练中对简单问题过度触发“反思”、“验证”等推理词，导致不必要的长推理链，这是一种跨难度级别的策略干扰。
2. **DOT 能系统性纠正冗余**：通过仅截断 all-correct 组中的统计异常值，DOT 成功引导策略学习何时应简洁，实现了 **adaptive reasoning**。
3. **极小干预带来全局收益**：DOT 仅影响不到 0.5% 的响应，却能驱动整个策略分布向更高效的方向演化。
4. **性能与效率可兼得**：DOT 不是以牺牲准确率为代价换取效率，反而在压缩长度的同时提升了推理能力。

### 方法的局限性
- **依赖高质量初始策略和训练数据**：作为 RL 训练阶段的方法，其效果受限于初始 policy 和训练数据质量。
- **难以应用于顶级闭源模型**：如 `DeepSeek-V3.2` 或 `Qwen3-235B` 等经过大规模私有数据训练的模型，在公开数据上已接近零熵，难以从中提取进一步增益。
- **尚未扩展至 Agent 任务**：目前仅针对文本推理，未考虑工具调用、规划循环等 agent 场景中的冗余行为。

### 未来工作方向
- 将 DOT 扩展至 **agent-based tasks**，用于剪枝冗余的工具调用或规划步骤。
- 探索与 **test-time reinforcement learning (TTRL)** 结合，在推理时动态优化策略。
- 在 **post-training 阶段** 应用 DOT，作为下一代基础模型训练流程的一部分。

--- 

> **总结**：本文提出的 **DOT** 方法通过一种巧妙的“动态异常值截断”机制，解决了推理模型中的 **length shift** 问题，在不牺牲甚至提升性能的前提下，实现了极致的推理效率压缩，为高效、可扩展的 reasoning models 提供了一个简单、鲁棒且通用的新范式。

</details>

---

### 9. [ELO: Efficient Layer-Specific Optimization for Continual Pretraining of Multilingual LLMs](https://arxiv.org/abs/2601.03648)

**Authors**: HanGyeol Yoo, ChangSu Choi, Minjun Kim, Seohyun Song, SeungWoo Song, Inho Won, Jongyoul Park, Cheoneum Park, KyungTae Lim  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03648v1  

#### Abstract
We propose an efficient layer-specific optimization (ELO) method designed to enhance continual pretraining (CP) for specific languages in multilingual large language models (MLLMs). This approach addresses the common challenges of high computational cost and degradation of source language performanc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ELO: Efficient Layer-Specific Optimization for Continual Pretraining of Multilingual LLMs》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**多语言大语言模型（MLLMs）在持续预训练（Continual Pretraining, CP）过程中面临的两个核心挑战**：
- **计算成本高昂**：传统全参数微调（Full Fine-Tuning, FFT）需要大量GPU资源和时间。
- **源语言性能退化**：在目标语言上进行CP通常会导致英文等源语言能力显著下降。

尽管已有轻量级方法如 **LoRA** 和 **Selective Layer Tuning**，但由于前向传播仍需遍历全部模型参数，因此**训练加速效果有限**。

---

### 提出的新方法：ELO（Efficient Layer-Specific Optimization）
提出一种全新的两阶段层特定优化框架——**ELO**，其核心思想是：
> **从原始MLLM中剥离出一小部分关键层，独立训练后再重新整合回原模型**。

#### 两个主要阶段：
1. **ELO Pretraining（层剥离预训练）**
   - 剥离并仅训练**第一层和最后一层解码器层**（即 $l_1$ 和 $l_n$），构建一个更小的独立模型（ELO Model）。
   - 显著减少**可训练参数数量**以及**前向传播中的总计算量**，从而大幅降低显存占用和训练时间。

2. **Layer Alignment（层对齐）**
   - 将训练好的新层替换回原始模型。
   - 使用约1GB双语数据进行**简短的全模型微调**，以对齐参数、稳定输出。

此外，还引入了 **Bilingual Instruction Tuning** 阶段，结合 **Chat Vector 方法** 和 **Supervised Fine-Tuning (SFT)** 来提升指令跟随能力。

---

### 相比现有方法的优势
| 维度 | LoRA / Selective Tuning | ELO |
|------|------------------------|-----|
| 可训练参数 | 减少 | 大幅减少 |
| 前向传播计算量 | 不变（仍需全模型推理） | **显著减少**（只运行小模型） |
| 训练速度 | 加速有限（~1.2–1.5x） | **最高达6.46倍于FFT，10.72倍于LoRA** |
| 源语言保留能力 | 中等（易遗忘） | **有效保持英语性能（MMLU仅降~2.42%）** |
| 目标语言性能 | 提升一般 | **Qualitative指标最高提升6.2%** |

> ✅ **核心突破**：通过“**层剥离 + 独立训练**”策略，直接解决了PEFT方法无法规避的**前向传播瓶颈**。

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 预训练数据（Pretraining Dataset）
- **韩语 & 英语混合数据**（共 ~200GB）：
  - 来源：AI-Hub、Modu-corpus2、WIKI-ko、CulturaX、cc100-ko、fineweb
- **日语 & 英语混合数据**（共 10GB）：
  - 来源：CulturaX（ja）、fineweb

> 数据比例为 **英语:目标语言 = 1:9**

#### 指令微调数据（Instruction Tuning）
- 使用公开的 **Tagengo** 数据集（ShareGPT格式）
- 包含超过7万条多语言对话样本
- 抽取 **31K 英文/目标语言（韩/日）1:1配比** 的指令对用于SFT

---

### 实验设置

#### 模型基础架构
- **Llama 3.1-8B**
- **Mistral-7B-v0.3**
- **Qwen2-7B**

#### 对比方法
| 方法 | 描述 |
|------|------|
| `{base}-Instruct` | 官方发布的指令调优模型（零基线） |
| `{base}-FFT` | 在基础模型上进行完整CP + SFT |
| `{base}-ELO` | 本文提出的ELO方法（ELO Pretraining + Layer Alignment + SFT） |

#### 评估指标

| 语言 | 定量评估（Quantitative） | 定性评估（Qualitative） |
|------|--------------------------|-------------------------|
| **英语** | MMLU（Accuracy） | MT-Bench（GPT-4评分，满分10） |
| **韩语** | KoBEST（F1-score） | LogicKor（GPT-4评分，满分10） |
| **日语** | MARC-ja（accuracy_norm） | MT-Bench(ja)（GPT-4评分，满分10） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（来自 Table 1）

| 模型 | 总训练时间 | MMLU (en) ↓ | KoBEST (ko) ↑ | LogicKor (ko) ↑ | MT-Bench(ja) (ja) ↑ |
|------|------------|-------------|---------------|------------------|---------------------|
| Llama3.1-8B-Instruct | - | 68.07 | 56.02 | 6.03 | 5.38 |
| Llama3.1-FFT | 19.8h | 67.51 | 66.08 | 7.31 | 6.99 |
| **Llama3.1-ELO** | **3.5h** | **66.69** | **60.81** | **7.76** | **5.58** |
| Mistral-ELO | 4.8h | 58.90 | 60.56 | 6.59 | 5.68 |
| Qwen2-ELO | 3.9h | 70.11 | 71.57 | 7.22 | — |

> ⚠️ 注意：ELO使用的预训练数据量略少（9GB vs FFT的10GB）

---

### 与基线方法的对比结果

#### ✅ 效率优势（Training Speed）
- **平均训练速度提升 5.88 倍**（相比FFT）
- 最高可达 **6.46倍加速**（Llama3.1）
- 相比LoRA也快 **5.29~10.72倍**（随数据量增加而扩大差距）

> 图3显示：当训练数据达到200GB时，ELO比LoRA快 **10.72倍**

#### ✅ 性能优势（Target Language）
- 在 **LogicKor（韩语推理）** 上：
  - Llama3.1-ELO 达到 **7.76**，比FFT（7.31）高出 **0.45分（+6.2%）**
- 在 **KoBEST** 上：
  - 所有ELO模型均优于 `-Instruct` 基线（+8.55% ~ +23.57%）

#### ✅ 源语言保护（English Capability）
- MMLU 平均仅下降 **2.42%**（vs `-Instruct`）
- 表明大部分中间层未被修改，有效防止灾难性遗忘

---

### 消融实验结果（Ablation Study）

#### （1）不同层数选择的影响（Table 3）
| 层配置 | LogicKor得分 | 结论 |
|--------|--------------|------|
| (1,32) 即首尾层 | **7.76** | ✅ 最佳 |
| (1,16,32) | 7.69 | 接近最优 |
| (8,24) 中间层 | 5.00 | ❌ 极差 |
| (1,16) | 5.79 | ❌ 缺失末层严重影响性能 |

> ✔️ 支持假设：**首层（输入编码）和末层（输出生成）最关键**

#### （2）对齐数据量影响（Figure 2）
- **无需对齐（0GB）** → LogicKor仅得4.5
- **1GB对齐数据** → 提升至7.76
- **1.5GB** → 达峰值7.78
- 更多数据无增益甚至轻微下降

> ✅ 结论：**仅需约1GB双语数据即可完成高效对齐**

#### （3）更大模型规模验证（Table 2）
- 在 **Llama3.1-70B** 上应用ELO：
  - LogicKor得分从7.78（Instruct）→ **8.65（ELO）**
  - 提升近 **10个百分点**
> ✔️ 表明ELO可扩展至百亿参数级别

#### （4）更多预训练数据的效果
- 将ELO预训练数据从10GB增至200GB：
  - LogicKor得分从6.18 → **6.97**
> ✔️ 表明ELO具备良好的数据扩展性

---

## 4. 关键结论和发现

### 主要发现
1. **首层和末层是语言适应的关键瓶颈层**，独立训练它们足以注入有效的目标语言知识。
2. **剥离关键层进行独立训练** 能从根本上解决LoRA类方法的前向传播开销问题，实现真正的高效训练。
3. **即使少量对齐数据（~1GB）也能有效融合新旧知识**，避免大规模重训。
4. ELO在多个主流LLM（Llama/Mistral/Qwen）和语言（Korean/Japanese）上均表现出色，具有强泛化能力。
5. **定性生成能力显著增强**，尤其在复杂推理任务（如LogicKor）上超越FFT。

---

### 方法的局限性
1. **仍需一次小型FFT进行Layer Alignment**
   - 虽然ELO阶段节省大量资源，但最终对齐阶段仍需全参数更新，**无法完全消除峰值GPU内存需求**。
2. **尚未验证超大规模数据场景（>1TB）下的表现**
   - 当前实验最大使用200GB数据，更大规模的数据是否持续受益尚待验证。
3. **层选择依赖经验观察**
   - 当前固定选择首尾层，未来可探索自动化层重要性评估机制。

---

### 未来工作方向
- 探索自动识别最关键的“可迁移层”组合
- 将ELO应用于更大规模模型（如100B+）及更多低资源语言
- 结合量化技术进一步压缩ELO模型体积
- 扩展至多语言联合优化场景（同时增强多种语言）

---

## 总结
✅ **ELO是一种高效且有效的多语言持续预训练新范式**。它通过**层剥离策略**打破了传统PEFT方法的计算瓶颈，在**训练速度提升5.88~6.46倍的同时，提升了目标语言定性表现最多6.2%，并良好保留了源语言能力**。该方法为中小研究团队低成本定制多语言LLM提供了可行路径。

</details>

---

### 10. [Atlas: Orchestrating Heterogeneous Models and Tools for Multi-Domain Complex Reasoning](https://arxiv.org/abs/2601.03872)

**Authors**: Jinyang Wu, Guocheng Zhai, Ruihan Jin, Jiahao Yuan, Yuhao Shen, Shuai Zhang, Zhengqi Wen, Jianhua Tao  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03872v1  

#### Abstract
The integration of large language models (LLMs) with external tools has significantly expanded the capabilities of AI agents. However, as the diversity of both LLMs and tools increases, selecting the optimal model-tool combination becomes a high-dimensional optimization challenge. Existing approache...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Atlas: Orchestrating Heterogeneous Models and Tools for Multi-Domain Complex Reasoning —— 核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

随着大型语言模型（LLMs）与外部工具（如计算器、代码解释器、搜索引擎等）的集成日益普遍，AI代理的能力得到了显著扩展。然而，当可用的LLM和工具种类增多时，如何动态选择最优的**模型-工具组合**（model-tool combination）成为一个高维优化难题。

现有方法存在三大挑战：
1. **无法利用模型-工具协同效应**（Failure to leverage model-tool synergies）：多数路由方法仅关注模型选择，忽略了特定模型与特定工具之间的性能互补。
2. **调用逻辑僵化**（Rigid invocation）：工具调用通常依赖预设规则，缺乏对任务需求和模型能力的动态适应。
3. **强化学习（RL）优化孤立**（Isolated optimization of RL）：即使引入RL，也多用于单一组件优化，未能联合优化模型与工具的协同路径。

### **提出了什么新方法或新思路**

本文提出 **ATLAS**（Adaptive Tool-LLM Alignment and Synergistic Invocation），一个双路径框架，用于在跨领域复杂推理中动态协调异构模型与工具。其核心创新在于：

- **双路径架构设计**：
  - **训练免费的聚类路由**（Training-free Cluster-based Routing）：基于语义嵌入空间中的历史性能模式，实现快速、高效的领域内推理。
  - **基于RL的多步路由**（RL-driven Multi-step Routing）：通过强化学习探索自主推理轨迹，提升对分布外（OOD）任务的泛化能力。

- **联合优化模型-工具组合**：将搜索空间定义为 $ \mathcal{S} = \mathcal{M} \times \mathcal{T} $，显式建模模型与工具的协同关系，而非单独优化。

- **复合奖励函数设计**：在RL路径中引入三项奖励信号：
  - **格式奖励**（Rfmt）：确保语法正确性；
  - **结果奖励**（Rout）：衡量任务正确性；
  - **模型选择奖励**（Rsel）：引导选择高效模型，避免资源浪费。

### **相比现有方法的优势**

- **性能更强**：在15个基准上超越闭源模型（如GPT-4o），在分布内任务上平均提升+10.1%，分布外任务上+13.1%。
- **灵活性更高**：支持多轮动态推理与工具调用，适应开放域复杂任务。
- **可扩展性强**：无需重新训练即可无缝集成新模型和工具（见动态池扩展实验）。
- **成本可控**：通过性能-成本权衡机制，在保证精度的同时控制推理开销。

---

## 2. 核心实验方法和设置

### **使用的数据集**

共涵盖 **10个文本任务** 和 **5个多模态任务**，覆盖多个领域：

| 类别 | 数据集 |
|------|--------|
| 数学推理 | AIME2024, AIME2025, AMC |
| 编程生成 | HumanEval, MBPP |
| 算术推理 | Calculator |
| 常识推理 | Natural Questions (NQ), WebQ |
| 逻辑推理 | LogiQA2 |
| 科学推理 | GPQA |
| 多模态视觉推理 | ChartQA, Geometry3K, TallyQA, CountBench, TableVQA |

测试样本总数超过4000，具有广泛代表性。

### **实验设置和评估指标**

- **评估协议**：
  - **分布内**（In-Distribution）：所有数据集均有训练集，用于评估熟悉领域的性能。
  - **分布外**（Out-of-Distribution）：仅在 `Calculator`, `NQ`, `MBPP` 上训练，其余任务视为OOD，检验泛化能力。

- **评估指标**：主要使用 **Accuracy**（准确率）作为核心指标。

- **模型池**（Model Pool）：
  - 包括6个开源LLM：Qwen2.5-7B, Llama-3.1-8B, InternLM3-8B, DeepSeek-R1-Distill-Qwen-7B, Qwen2.5-Coder-7B, Qwen3-8B-VL（多模态）。

- **工具池**（Tool Pool）：
  - **基础工具**：Code Interpreter, Web Search, Calculator, Process Reward Model (PRM)
  - **多模态工具**：Qwen3-Chart, Qwen3-Counting, Qwen3-Geo, Hunyuan-OCR

- **RL训练细节**：
  - 策略模型：Qwen2.5-3B-Instruct
  - 优化算法：PPO（Proximal Policy Optimization）
  - 批大小：32，训练步数：250，学习率：1e-6

### **基线方法对比**

| 基线方法 | 类型 | 特点 |
|---------|------|------|
| Zero-shot / Few-shot Router | 查询路由 | 直接提示或上下文示例选择模型-工具 |
| Random Router | 随机策略 | 完全随机选择 |
| RouterDC (Chen et al., 2024) | 对比学习路由 | 基于双对比学习优化查询-模型对齐 |
| MLPRouter (Hu et al., 2024) | 分类器路由 | 每个模型-工具组合训练一个MLP预测成功率 |
| BertRouter (Ong et al., 2024) | 编码器分类 | 使用mDeBERTa编码查询并分类最佳组合 |

此外还对比了闭源模型：GPT-4o, GPT-4.1, GPT-5, Gemini 2.5-Pro/Flash。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **分布内性能（In-Distribution）**
- **ATLAS (cluster)** 平均准确率达 **63.5%**，显著优于最强基线 **RouterDC (53.4%)**，提升 **+10.1%**。
- 在数学任务上表现尤为突出：
  - AIME25: **40.0%** vs. RouterDC 的 23.3%
  - AMC: **82.5%** vs. 62.5%
- 超越 **GPT-4o (53.1%)**，接近 **GPT-4.1 (63.0%)**，表明小规模模型通过智能编排可媲美大模型。

#### **分布外性能（Out-of-Distribution）**
- **ATLAS (RL)** 平均准确率 **59.4%**，远超 ATLAS (cluster) 的 49.2% 和 RouterDC 的 46.3%，提升 **+13.1%**。
- 在 AIME24 上达到 **43.3%**（ATLAS cluster 仅为 13.3%），显示RL路径具备强大泛化能力。
- 即使面对未见过的任务（如GPQA），仍能维持 **42.0%** 准确率，接近GPT-4o水平。

#### **多模态任务表现**
- 在5个视觉推理任务上，ATLAS平均准确率 **68.9%**，优于最强单工具基线 **+4.3%**。
- 在 ChartQA 上超越最佳单工具（Qwen3-Chart）达 **+1.0%**（84.0% vs. 83.0%）。
- 显著克服单一工具局限，例如 Qwen3-Chart 在 Geometry3K 上仅得 50.2%，而 ATLAS 达到 65.6%。

#### **动态模型-工具池扩展实验**
- 新增医学专用模型（Llama-3.1-8B-UltraMedical）、数学专家模型（Qwen2.5-Math-7B）及验证工具。
- **ATLAS(RL)** 准确率从 **59.4% → 61.7%**（+2.3%），证明其可无缝集成新组件。
- 基线方法（如BertRouter）仅微弱提升或退化，说明其决策边界固定，难以适应变化。

### **消融实验结果**

| 消融条件 | 平均准确率 | Δ vs. 全模型 |
|----------|-----------|-------------|
| 完整 ATLAS(RL) | **59.4%** | — |
| 移除 Rsel（模型选择奖励） | 56.3% | -3.1% |
| 移除 Rfmt（格式奖励） | 53.3% | -6.1% |

- **Rfmt 至关重要**：移除后导致大量非法工具调用，推理失败率上升。
- **Rsel 是效率辅助信号**：虽非必需，但有助于加速收敛、降低熵值，提升推理效率。
- 结论：**Rfmt 和 Rout 是核心信号，Rsel 是可选优化项**。

---

## 4. 关键结论和发现

### **主要发现**

1. **模型-工具协同至关重要**：简单堆叠模型或工具不足以发挥最大潜力，必须显式建模二者协同关系。
2. **双路径设计有效平衡效率与泛化**：
   - 聚类路由适合有历史数据的稳定场景，零训练成本、响应快；
   - RL路由适合未知任务，通过探索学习通用决策原则。
3. **ATLAS 可媲美甚至超越闭源大模型**：在合理编排下，7B–8B级开源模型可达到GPT-4级别性能。
4. **多模态任务受益明显**：通过动态调度专用视觉工具（如几何解析、图表提取），显著提升复杂视觉推理能力。
5. **系统具备良好可扩展性**：无需重训练即可接入新模型/工具，适用于持续演进的AI生态系统。

### **方法的局限性**

1. **模态受限**：当前评估集中于文本与图像，未涉及音频、视频等其他模态。
2. **依赖API稳定性**：若外部工具服务不可用或延迟高，可能影响性能。
3. **RL训练成本较高**：虽然推理阶段高效，但策略训练需要一定计算资源（250步PPO训练）。
4. **奖励设计依赖先验知识**：Rsel依赖离线评估确定“最优模型”，可能存在偏差。

### **未来工作方向**

- 探索更轻量级的策略架构以降低训练开销。
- 引入容错机制应对工具调用失败或网络异常。
- 扩展至多模态长序列任务（如视频理解、语音交互）。
- 结合 Test-Time Scaling（如Self-Consistency）进一步提升鲁棒性。
- 探索无监督或自反馈方式替代人工标注奖励信号（如RLAIF）。

---

> ✅ **总结一句话**：  
> ATLAS 提出了一种**生态系统级的智能编排范式**，通过双路径机制实现了对异构LLM与工具的动态、高效、可泛化的协同调度，在性能、灵活性与可扩展性之间取得了卓越平衡，为下一代 agentic reasoning systems 提供了重要参考。

</details>

---

### 11. [SIGMA: Scalable Spectral Insights for LLM Collapse](https://arxiv.org/abs/2601.03385)

**Authors**: Yi Gu, Lingyou Pang, Xiangkun Ye, Tianyu Wang, Jianyu Lin, Carey E. Priebe, Alexander Aue  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03385v1  

#### Abstract
The rapid adoption of synthetic data for training Large Language Models (LLMs) has introduced the technical challenge of "model collapse"-a degenerative process where recursive training on model-generated content leads to a contraction of distributional variance and representational quality. While t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SIGMA: Scalable Spectral Insights for LLM Collapse 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文聚焦于**LLM Collapse**（大语言模型崩溃）这一日益严重的问题。随着合成数据在训练 LLM 中的广泛应用，模型通过递归地使用自身生成的数据进行再训练，会导致其表示空间逐渐退化——即“模型崩溃”。这种现象表现为语义多样性丧失、分布方差收缩以及生成质量下降。

尽管已有研究描述了崩溃的现象学特征，但在高维嵌入空间中缺乏**可扩展、数学严谨且能早期预警**的方法来量化和预测崩溃的发生。

---

### 提出的新方法与新思路
作者提出了 **SIGMA**（Spectral Inequalities for Gram Matrix Analysis），一个基于谱分析的统一框架，用于监测和量化 LLM 崩溃：

- **核心思想**：利用 LLM 嵌入向量构成的 **Gram 矩阵** 的谱特性（特别是其特征值分布）作为衡量表示空间健康状态的指标。
- **理论基础**：提出并推导了 Gram 矩阵行列式的**确定性边界**（基于 Weyl 不等式和 Ky Fan 主导）和**随机边界**（基于大数定律），从而可以在不计算完整谱的情况下估计表示空间的收缩程度。
- **关键指标**：引入 `log det G` 作为主度量，当模型崩溃时，Gram 矩阵趋于低秩，导致 `log det G → -∞`。

---

### 相比现有方法的优势
| 维度 | SIGMA 的优势 |
|------|-------------|
| **可扩展性** | 支持子采样策略，仅需观察部分数据即可估算全矩阵性质，适用于大规模模型（避免全特征分解） |
| **理论保障** | 提供严格的数学边界（确定性和随机性），而非启发式统计量 |
| **敏感性与鲁棒性结合** | 双轨诊断机制：一条轨道对谱收缩敏感（Track II），另一条为保守包络（Track I），二者互补 |
| **几何视角** | 超越 token-level 或 surface-form 统计（如 n-gram 重复率），从嵌入空间的几何结构变化理解崩溃本质 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **DecayBench** 提供的许可清洁真实语料库（real corpora），涵盖四个领域：
  - `CIVICS`, `FINANCE (FIN)`, `SCIENCE`, `SOFTWARE`
- 每个领域提供：
  - 固定切片的真实上下文（64-token blocks）
  - 冻结的提示银行（frozen prompt bank），含稳定 ID 和分类标签（如 Creative, Neutral 等）

> 所有实验均以 **FINANCE (FIN)** 领域为主报告对象。

---

### 实验设置

#### 两个递归设置（S1 vs S2）
| 设置 | 名称 | 描述 |
|------|------|------|
| **S1** | Restart-from-base (仅数据递归) | 每代重新生成合成数据，但训练始终从同一初始检查点 `M(0)` 开始 |
| **S2** | True recursion (数据 + 权重递归) | 每代使用前一代模型权重继续微调，形成真正的递归链 |

> 目标：分离“权重累积”对崩溃的影响。

#### 合成数据生成协议
- 每个真实上下文作为 prompt，由上一代模型生成固定长度延续
- 训练数据为合成与真实样本的混合物，总块数恒定
- 实验中设真实比例 α = 0（纯合成训练），以压力测试递归影响

#### 模型与训练细节
- 基础模型：`facebook/opt-125m`
- 微调配置：固定优化器（AdamW）、学习率（2e-5）、batch size（16）、epoch 数（每代 5）
- 解码参数：top-p=0.95, temperature=1.0，非贪婪采样

---

### 评估指标

#### 主要指标（SIGMA-UB 双轨诊断）
1. **Track II: △ULLN.cov(δ)**  
   - 基于随机缩放律的协方差归一化代理
   - 敏感趋势探测器，反映观测子谱收缩
2. **Track I: △GKF(δ)**  
   - 基于确定性边界的尾部能量预算包络
   - 保守上限，控制最坏情况下的误差

> 两者均为相对于初始检查点（g=0）的漂移值，负漂移表示崩溃迹象。

#### 辅助表面形式代理（非定义性指标）
- `distinct-2`：唯一 bigram 比例（越高越多样）
- `hashed n-gram HHI`：哈希 n-gram 的赫芬达尔指数（越高越集中）

---

### 基线方法对比
本文未直接与其他崩溃检测算法比较，而是：
- 将 **S1 vs S2** 视为内部基线，验证权重递归是否加剧崩溃
- 使用表面形式指标（如 HHI）作为传统视角的参照，说明 SIGMA 能更早捕捉到深层几何退化

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1, FIN 领域，g_max=50）

| Setting | △ULLN.cov (final) | slope_LLN.cov | △GKF (final) | slope_KF | Track agreement? |
|--------|-------------------|---------------|--------------|----------|------------------|
| **S1** | -150.986          | -0.941        | -142.051     | -0.918   | yes (both neg)   |
| **S2** | -1536.562         | -42.621       | -2.426       | +2.350   | no               |

---

### 与基线方法的对比结果
- 在 **S1（仅数据递归）** 中：
  - 两条轨道均呈现持续负漂移（约 -150），表明即使没有权重累积，仅靠合成数据演化也会引发缓慢崩溃。
- 在 **S2（真递归）** 中：
  - **Track II** 漂移急剧恶化至 **-1536**，斜率为 -42.6，显示崩溃被显著加速。
  - **Track I** 几乎无明显负漂移（最终仅 -2.4），甚至略有上升（正斜率），因其受最坏情况尾部预算控制，未能响应实际谱收缩。

> ✅ **结论**：权重递归极大地放大了崩溃速度；SIGMA-UB 的双轨设计成功揭示了这一差异。

---

### 消融实验与补充分析（Appendix）

#### 表面形式代理一致性（Table 14）
| Setting | distinct-2 (g=50) | HHI (g=50) | Spearman(△U, HHI) |
|--------|-------------------|------------|--------------------|
| S1     | 0.334             | 0.013      | -0.318             |
| S2     | 0.254             | 0.097      | **-0.685**         |

- S2 中 n-gram 浓度增加近三个数量级，且与 Track II 漂移高度单调相关（强负 Spearman 相关性），验证了几何收缩与表面退化的关联。

#### 桶位局部化分析（Bucket Localization, Figure 4）
- 在 S1 下按提示类型分桶评估发现：
  - `Creative` 类提示表现出最强的 Track II 收缩
  - 推测：开放式任务更容易因合成数据反馈而失去创造性多样性

> 提示崩溃并非均匀发生，某些语义区域更脆弱。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLM Collapse 可通过 Gram 矩阵谱特性有效监测**  
   `log det G` 的衰减是表示空间退化的可靠信号。

2. ✅ **SIGMA 实现了可扩展、理论驱动的崩溃检测**  
   利用确定性与随机边界，在无需全谱计算的前提下实现高效监控。

3. ✅ **权重递归显著加剧崩溃进程**  
   S2 中 Track II 的崩溃速度远超 S1，说明模型参数的连续更新会加速退化。

4. ✅ **双轨诊断机制具有解释力**  
   - Track II 对实际谱收缩敏感；
   - Track I 提供保守包络；
   - 两者的分歧本身是有意义的信号（例如提示漂移、检索偏移等）。

5. ✅ **几何退化先于表面重复出现**  
   SIGMA-UB 的 Track II 可比 n-gram HHI 更早发出警报，具备“早期预警”能力。

---

### 方法的局限性
1. **依赖嵌入编码器的一致性**  
   需使用冻结的 sentence encoder（如 `all-MiniLM-L6-v2`），否则测量将混杂编码器变化。

2. **假设数据来自平稳分布**  
   随机边界依赖 i.i.d. 假设，若合成数据分布剧烈漂移，可能影响估计精度。

3. **无法定位具体崩溃模式**  
   SIGMA 检测“是否崩溃”，但不能解释“为何崩溃”或“哪些语义丢失”。

4. **对小模型更适用**  
   当 `n < m`（样本数小于嵌入维度）时，Gram 矩阵必然奇异，限制应用范围。

---

### 未来工作方向
1. **扩展至多模态模型**  
   将 SIGMA 应用于视觉-语言模型中的跨模态 Gram 矩阵分析。

2. **动态调整采样策略**  
   根据当前诊断结果自适应选择观测块 `n_A`，提升效率。

3. **结合干预机制**  
   将 SIGMA 作为反馈信号，自动触发注入真实数据或重启训练流程。

4. **构建标准化基准（Benchmark）**  
   推动 SIGMA 成为评估 LLM 递归训练稳定性的标准工具之一。

5. **探索反向校正方法**  
   基于谱分析结果设计正则项，主动抑制表示空间收缩。

---

> **总结**：SIGMA 是首个从**谱几何视角**出发、兼具**理论深度**与**工程实用性**的大规模 LLM 崩溃监测框架。它不仅提供了新的分析范式，也为构建可持续的递归训练系统奠定了基础。

</details>

---

### 12. [Rethinking Recurrent Neural Networks for Time Series Forecasting: A Reinforced Recurrent Encoder with Prediction-Oriented Proximal Policy Optimization](https://arxiv.org/abs/2601.03683)

**Authors**: Xin Lai, Shiming Deng, Lu Yu, Yumin Lai, Shenghao Qiao, Xinze Zhang  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03683v1  

#### Abstract
Time series forecasting plays a crucial role in contemporary engineering information systems for supporting decision-making across various industries, where Recurrent Neural Networks (RNNs) have been widely adopted due to their capability in modeling sequential data. Conventional RNN-based predictor...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 主要贡献和创新点

### 解决的问题
传统的基于 **Recurrent Neural Networks (RNNs)** 的时间序列预测模型通常采用编码器-仅（encoder-only）策略，将历史滑动窗口作为输入来预测未来值。然而，这类方法存在以下关键缺陷：
- **平等对待所有时间步**：传统方法对所有时间步和隐藏状态进行同等处理，忽略了它们对未来预测的不同贡献。
- **缺乏动态适应性**：固定结构（如固定的跳连模式）无法根据时序数据的动态变化进行自适应调整。
- **优化割裂**：输入特征选择、隐藏状态生成和输出目标映射等关键环节通常是孤立优化的，缺乏统一框架。

这些问题导致模型在捕捉复杂、非平稳的时间依赖关系上表现不佳，限制了预测精度。

### 提出的新方法
为解决上述问题，作者提出了 **RRE-PPO4Pred** 方法，其核心是一个由两部分组成的创新框架：

#### (1) 强化循环编码器 (Reinforced Recurrent Encoder, RRE)
- **核心思想**：将 RNN 编码器的内部适应过程建模为一个 **Markov Decision Process (MDP)**。
- **统一决策环境**：在一个统一的框架内，通过一个智能体（agent）动态地做出三项关键决策：
  - **输入特征选择 (Input Feature Selection)**：决定在每个时间步是否使用当前输入。
  - **隐藏跳连 (Hidden Skip Connection)**：动态选择从过去的哪个隐藏状态建立直接连接，以增强长期记忆。
  - **输出目标选择 (Output Target Selection)**：决定在训练过程中是否将某个时间步的预测纳入损失计算，实现选择性监督。
- **优势**：该框架显著增强了 RNN 模型的适应性和灵活性，使其能够根据具体的时间序列模式动态调整架构。

#### (2) 面向预测的近端策略优化算法 (Prediction-oriented Proximal Policy Optimization, PPO4Pred)
- **核心改进**：对标准的 **PPO** 算法进行了两项关键改进，以更好地服务于时间序列预测任务。
  - **Transformer-based Agent**：使用 **Transformer** 编码器作为智能体的骨干网络，利用其自注意力机制有效建模时间序列中的长程依赖关系。
  - **动态转移采样 (Dynamic Transition Sampling, DTS)**：提出一种新的采样策略，优先选择那些具有高学习潜力的转移（transition），例如价值估计不确定性大或预测误差高的样本，从而大幅提高采样效率。

#### (3) 协同进化优化范式 (Co-evolutionary Optimization Paradigm)
- **核心流程**：在训练过程中，交替优化两个组件：
  1.  固定 RNN 环境，训练 PPO4Pred 智能体。
  2.  固定训练好的智能体，微调 RNN 环境。
- **优势**：这种交互式的训练方式促进了 RNN 预测器和策略智能体的协同进化，实现了更深层次的模型适应。

### 相比现有方法的优势
- **更高的预测精度**：通过动态、联合优化三个关键环节，显著提升了模型的预测能力。
- **更强的适应性**：能够根据不同时间序列的特性自动调整模型行为，而非依赖预设的固定规则。
- **更优的样本效率**：DTS 策略使得智能体能更快地从最有价值的经验中学习。
- **通用性强**：RRE 框架可以无缝集成到多种不同的 RNN 变体（如 LSTM, GRU, xLSTM 等）中。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在五个真实世界的时间序列数据集上进行，涵盖了交通、电力、气象等多个领域：
- **Traffic**: 加州高速公路传感器数据（862维）
- **Electricity**: 电力消耗负荷数据（321维）
- **ETTh**: 电力变压器温度数据（7维）
- **Weather**: 气象数据（21维）
- **ILI**: 流感样疾病周度数据（7维）

### 实验设置和评估指标
- **任务**：多变量输入，单变量输出的多步预测（multivariate-to-univariate forecasting）。
- **预测范围 (Horizon H)**：在不同数据集上测试了多个预测长度（如 24, 48, 96 步）。
- **评估指标**：
  - **MSE (Mean Squared Error)**
  - **MAE (Mean Absolute Error)**
- **数据划分**：按 7:1:2 的比例划分为训练集、验证集和测试集。
- **实现细节**：使用 Adam 优化器，进行超参数随机搜索，并采用早停机制。

### 基线方法对比
与以下几类代表性方法进行了对比：
- **静态基线 (Static Baselines)**：
  - `Naive-all`: 使用所有时间步的输出进行监督。
  - `Naive-last`: 仅使用最后一个隐藏状态进行预测。
  - `DilatedRNN`: 使用预定义的稀疏跳连模式。
- **启发式方法 (Heuristic Method)**：
  - `EBPSO`: 基于粒子群优化（PSO）的输入特征选择。
- **强化学习方法 (RL-based Methods)**：
  - `LSTMjump`: 使用 **Policy Gradient (PG)** 进行输入跳过。
  - `PGLSTM`: 使用 **PG** 进行动态隐藏跳连。

此外，还与最先进的 **Transformer** 模型（`PatchTST`, `iTransformer`）进行了比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
综合实验表明，**RRE-PPO4Pred** 在所有数据集和预测范围内均取得了最佳性能。

- **相对于 RNN 基线的提升**：
  - 相比 `Naive-last`, `Naive-all`, `DilatedRNN`，MSE 平均提升 **23.7% ~ 53.08%**，MAE 提升 **21.57% ~ 50.75%**。
  - 相比启发式方法 `EBPSO`，MSE 提升 **23.21% ~ 28.36%**，MAE 提升 **22.80% ~ 29.59%**。
  - 相比 RL 方法 `PGLSTM` 和 `LSTMjump`，MSE 提升 **19.04% ~ 27.80%**，MAE 提升 **18.47% ~ 28.49%**。

- **超越 Transformer 模型**：
  - 在 **ETTh** 数据集上，将 `xLSTM` 与 RRE-PPO4Pred 结合后，其性能超过了 `iTransformer` 和 `PatchTST`。
  - 例如，在 H=96 的预测范围内，`xLSTM+Ours` 的 MSE 为 **0.3607**，而 `iTransformer` 为 0.3921，`PatchTST` 为 0.3746，分别提升了 **8.0%** 和 **3.7%**。

### 消融实验结果
为了验证各组件的有效性，进行了消融研究：

| 方法 | 描述 |
| :--- | :--- |
| `TA-PSO` | 使用 PSO 优化 RRE 中设计的三元动作（Ternary Action）。 |
| `RRE-PG` / `RRE-DQN` / `RRE-PPO` | 保留 RRE 框架，但分别用 PG、DQN 或标准 PPO 替换 PPO4Pred 算法。 |

**结论**：
- 所有基于 RL 的变体 (`RRE-PG`, `RRE-DQN`, `RRE-PPO`) 均显著优于启发式方法 `TA-PSO`，证明了 RL 在优化复杂动作空间上的必要性。
- `RRE-PPO` 已经表现良好，但完整的 `RRE-PPO4Pred` 能够进一步提升性能（MSE 再提升 **5.71% ~ 8.95%**，MAE 提升 **4.54% ~ 8.18%**），这充分证明了 **Transformer-based Agent** 和 **DTS** 策略的有效性。

---

## 4. 关键结论和发现

### 主要发现
1.  **RRE 框架是有效的**：将 RNN 的适应过程建模为 MDP，并通过智能体进行联合决策，是一种强大的范式，能够显著提升 RNN 的预测能力。
2.  **PPO4Pred 是关键**：面向预测任务定制的 PPO 算法（特别是 Transformer-based Agent 和 DTS）对于高效、稳定地训练智能体至关重要。
3.  **协同进化是成功的**：交替优化智能体和 RNN 环境的范式，能够促进两者共同进化，达到更好的整体性能。
4.  **RNN 依然强大**：通过先进的优化方法（如 RRE-PPO4Pred），传统的 RNN 架构可以在性能上超越甚至超过当前最先进的 **Transformer** 模型，重新确立了其在时间序列预测领域的竞争力。

### 方法的局限性
- **计算开销增加**：由于引入了额外的智能体网络和异步训练过程，训练时间和模型参数量相比基础 RNN 模型有所增加。
- **推理阶段仍需智能体**：与一些静态方法不同，该方法在推理时也需要运行智能体来动态决策，增加了推理的计算成本。

### 未来工作方向
- 探索更高效的训练和推理方法，例如通过 **知识蒸馏 (knowledge distillation)** 将智能体学到的知识迁移到一个轻量级的 RNN 模型中。
- 研究 **迁移学习 (transfer learning)**，使在大规模数据集上训练的智能体能够快速适应新的、小规模的时间序列任务。
- 设计更轻量级的智能体架构（如轻量级 **Transformers**），以降低整体计算成本。

</details>

---

### 13. [Feature-Aware One-Shot Federated Learning via Hierarchical Token Sequences](https://arxiv.org/abs/2601.03882)

**Authors**: Shudong Liu, Hanwen Zhang, Xiuling Wang, Yuesheng Zhu, Guibo Luo  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03882v1  

#### Abstract
One-shot federated learning (OSFL) reduces the communication cost and privacy risks of iterative federated learning by constructing a global model with a single round of communication. However, most existing methods struggle to achieve robust performance on real-world domains such as medical imaging...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Feature-Aware One-Shot Federated Learning via Hierarchical Token Sequences

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对**One-shot Federated Learning (OSFL)** 中的关键挑战展开研究，尤其是在处理**非独立同分布（non-IID）图像数据**时存在的以下问题：

- **通信效率与隐私风险**：传统联邦学习需要多轮通信，而OSFL通过单次上传降低通信开销和隐私泄露风险。
- **现有OSFL方法在non-IID场景下性能不佳**：尤其是医疗影像等真实世界任务中，由于数据异构性强，生成模型难以准确建模客户端数据分布。
- **特征空间错位（Feature Misalignment）**：当各客户端使用本地微调的编码器时，提取的语义特征无法对齐，导致全局分类器泛化能力差。
- **高分辨率细节丢失**：标准ViT类编码器输入尺寸固定且较低，难以捕捉医学图像中的细粒度局部结构。

---

### **提出了什么新方法或新思路**

作者提出了一种名为 **FALCON** 的新型OSFL框架，其核心思想是结合**分层Token序列生成**与**知识蒸馏引导训练**，实现高效、鲁棒的全局建模。

#### 主要创新点包括：

1. ✅ **Hierarchical Scale Encoding (HSE)**  
   - 在不进行本地微调的前提下，从原始图像中同时提取**全局语义信息**（image-level token）和**高分辨率区域细节**（region-level tokens）。
   - 采用双路径设计：将原图缩放为低分辨率全局视图，并用滑动窗口切分为多个高分辨率patch分别编码。
   - 构建具有**多尺度语义层次结构的Token序列**，提升预训练编码器在跨域任务中的适应性。

2. ✅ **Multi-scale Autoregressive (M-AR) Transformer Generator**  
   - 设计一个基于Transformer的生成器，以自回归方式建模Token序列的联合分布。
   - 引入**块状因果掩码（block-wise causal mask）**，确保先生成全局token再生成局部tokens，符合“由粗到精”的视觉感知机制。
   - 使用GMM输出每位置token的概率分布，增强表达能力。

3. ✅ **Distillation-Guided Global Training**  
   - 客户端上传合成Token序列 + 本地分类器 → 服务器端利用**知识蒸馏**训练全局分类器。
   - 蒸馏目标来自所有本地分类器的集成预测（ensemble teacher），缓解对生成质量的依赖。
   - 显著提升模型鲁棒性和泛化性能。

4. ✅ **压缩代理数据上传机制**  
   - 上传的是**结构化Token序列**而非原始图像或复杂生成模型参数，显著减少通信成本。
   - 支持灵活选择上传“生成器”还是“合成序列”，根据数据量动态优化传输策略。

---

### **相比现有方法的优势**

| 维度 | FALCON优势 |
|------|-----------|
| **性能** | 在多种non-IID设置下平均准确率超过最强基线达 **9.58%** |
| **效率** | 每样本生成仅需 **0.46 GFLOPs**，比扩散模型（如FedLMG）快四个数量级 |
| **隐私保护** | 合成Token难以重构原始图像，抗重建攻击能力强（SSIM下降超60%） |
| **适用性** | 特别适用于高分辨率、细粒度任务（如医学影像） |
| **鲁棒性** | 对label skew和feature skew均表现稳定，无严重性能退化 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

涵盖自然图像与医学图像两大领域，共6个数据集，分为两类non-IID设定：

| 数据集 | 类型 | 领域 | Non-IID类型 | 类别数 | 客户端数 |
|--------|------|-------|-------------|--------|----------|
| Tuberculosis | 医疗 | Med. | Feature | 2 | 3 |
| PACS | 自然 | Nat. | Feature | 7 | 4 |
| OfficeHome | 自然 | Nat. | Feature | 65 | 4 |
| PMRAM | 医疗 | Med. | Label | 4 | 5 |
| Pneumonia | 医疗 | Med. | Label | 2 | 5 |
| Tiny-ImageNet | 自然 | Nat. | Label | 200 | 5 |

> - **Feature non-IID**：不同来源/域作为不同客户端（天然划分）
> - **Label non-IID**：通过Dirichlet分布（α ∈ {0.1, 0.3, 0.5}）控制标签异质性

---

### **实验设置和评估指标**

- **主干编码器**：DINOv2-base（冻结，不微调）
- **HSE配置**：全局视图resize至448×448；滑动窗口大小/步长=224 → 得到1个global + 4个region tokens
- **M-AR Generator**：4层Transformer，hidden dim=768，16 heads，K=20 GMM components
- **训练细节**：
  - 本地分类器 & 全局分类器：Adam，lr=5e-4，batch_size=256，60 epochs
  - 平衡系数 λ=0.5，蒸馏温度 T=4
- **评估指标**：测试集分类准确率（Accuracy %），平均跨所有设置

---

### **基线方法对比**

| 基线方法 | 类型 | 简介 |
|---------|------|------|
| **FedAvg** | 多轮FL基准 | 多轮通信聚合模型参数 |
| **DENSE** | 数据无关蒸馏 | 不上传数据，仅用本地模型蒸馏 |
| **FedLMG** | 扩散模型生成 | 使用预训练扩散模型生成合成图像 |
| **FedPFT** | 特征统计建模 | GMM拟合特征分布，上传统计量 |
| **FedCGS** | 特征统计+朴素贝叶斯 | 直接构建全局Gaussian Naive Bayes分类器 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 2）**

| 方法 | Avg. Accuracy (%) |
|------|------------------|
| FedAvg | 73.30 |
| DENSE | 50.50 |
| FedLMG | 45.82 |
| FedPFT | 73.61 |
| FedCGS | 76.34 |
| **FALCON** | **85.92** ✅ |

> 💡 **FALCON比最佳OSFL基线（FedCGS）高出9.58个百分点！**

#### 分项亮点：

- **Tuberculosis（医疗图像）**：FALCON达 **87.20%**，远超第二名FedCGS的53.50%
- **PMRAM（脑癌MRI）**：在α=0.1极端label skew下仍保持 **83.33%** 准确率，其他多数方法低于40%
- **PACS / OfficeHome**：接近或达到最优，显示对domain shift的强大适应力
- **Tiny-ImageNet（大规模）**：FALCON达81.55%，显著优于FedLMG（~31%）、DENSE（~30%）

---

### **消融实验结果（Table 3）**

验证各组件有效性（平均准确率）：

| 变体 | Avg. Acc (%) | 相较完整版下降 |
|------|---------------|----------------|
| Full (FALCON) | **85.92** | — |
| w/o HSE | 82.32 | ↓3.60 |
| w/o KD（无知识蒸馏） | 82.82 | ↓3.10 |
| AR（普通自回归） | 81.47 | ↓4.45 |
| NAR（非自回归） | 82.99 | ↓2.93 |

> 🔍 结论：
> - HSE贡献最大（+3.6%），尤其在医疗图像上提升明显（Tuberculosis +3.57%）
> - 知识蒸馏至关重要（+3.1%），弥补生成偏差
> - M-AR架构优于AR/NAR，说明建模层级依赖关系重要

---

### **生成质量分析（Table 4）**

使用**最大均值差异（MMD）** 衡量真实 vs 合成Token分布差距：

| 方法 | Tuberculosis | PACS | OfficeHome |
|------|--------------|------|------------|
| M-AR（本文） | **0.003227** | **0.002047** | **0.000905** |
| AR | 0.003868 | 0.002457 | 0.001079 |
| NAR | 0.003868 | 0.002151 | 0.000849 |

> ✅ M-AR在大多数情况下最接近真实分布，证明其更强的建模能力。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **HSE能有效提取多尺度语义信息**，无需微调即可适配跨域数据，在高分辨率医学图像中尤为关键。
2. ✅ **M-AR Transformer可高效建模Token序列分布**，相比扩散/GAN更轻量且稳定。
3. ✅ **知识蒸馏显著缓解生成误差影响**，使全局训练不完全依赖完美分布建模。
4. ✅ **FALCON在各类non-IID场景下均表现出卓越鲁棒性与高性能**，尤其在医疗图像任务中遥遥领先。
5. ✅ **具备良好隐私保护能力**：基于合成Token的图像重建几乎不可识别（PSNR↓>10dB, SSIM↓>60%）。
6. ✅ **计算与通信高效**：生成开销极低（0.46 GFLOPs/sample），适合边缘部署。

---

### **方法的局限性**

- ❗ **依赖高质量预训练编码器**：若HSE所用的DINOv2等模型在特定领域（如病理切片）预训练不足，可能限制性能上限。
- ❗ **Token序列长度受限于滑动窗口策略**：当前仅支持固定数量patches，扩展性有限。
- ❗ **未考虑客户端间类别缺失情况（extreme label non-IID）**：实验中Dirichlet α最小为0.1，尚未覆盖极端稀疏标签场景。
- ❗ **生成过程仍为离散Token级别**：虽避免像素级泄露，但也损失部分连续语义信息。

---

### **未来工作方向**

1. 🔄 探索更高效的**统一编码策略**，确保所有客户端采用一致HSE配置，进一步提升特征对齐。
2. 🔍 研究更强大的**生成建模范式**（如flow-based models、vector quantization），提高Token多样性与保真度。
3. 🧠 引入**prompt learning 或 adapter模块**，在不破坏隐私前提下轻微调整编码器以适应本地数据。
4. 🌐 扩展至**3D医学图像**（如MRI体积数据）或多模态任务。
5. 🔐 进一步形式化分析**隐私-效用权衡边界**，提供理论保障。

---

## 总结

📌 **FALCON是一种面向现实应用的高效、鲁棒、隐私友好的OSFL新范式**。它通过**HSE + M-AR Generator + Distillation-Guided Training**三重机制，在不牺牲性能的前提下解决了non-IID数据下的特征错位、生成低效与隐私泄露等问题，尤其适用于**医疗影像等高分辨率、强异构场景**，为大规模联邦学习落地提供了有力支撑。

</details>

---

### 14. [Evolving Programmatic Skill Networks](https://arxiv.org/abs/2601.03509)

**Authors**: Haochen Shi, Xingdi Yuan, Bang Liu  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03509v1  

#### Abstract
We study continual skill acquisition in open-ended embodied environments where an agent must construct, refine, and reuse an expanding library of executable skills. We introduce the Programmatic Skill Network (PSN), a framework in which skills are executable symbolic programs forming a compositional...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Evolving Programmatic Skill Networks 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**开放环境中具身智能体（embodied agents）的持续技能获取问题**。现有方法存在两大局限：
- 技能通常以扁平化库或静态图形式存储，缺乏对技能进行持续改进的机制；
- 缺少统一框架来在分层技能组合中分配信用、修复符号程序以及随任务演化而重组结构。

### 提出的新方法：Programmatic Skill Network (PSN)
作者提出了一种名为 **Programmatic Skill Network (PSN)** 的新框架，其核心思想是将每个技能表示为可执行的符号程序（如JavaScript或Python代码），并通过调用关系形成一个动态演化的有向图网络。PSN通过经验不断构建、优化和重构这个技能网络。

#### 三大核心机制（创新点）：
1. **REFLECT：基于执行轨迹的结构化故障定位**
   - 利用LLM分析执行失败时的trace，识别出导致错误的具体控制流、参数、前置条件或子技能。
   - 实现了类似神经网络反向传播（backpropagation）的“符号微分”过程，在技能组合中进行信用分配。

2. **渐进式优化与成熟度感知更新门控（maturity-aware update gating）**
   - 引入技能价值函数 $V(s) = p_s - u_s$（成功率减去不确定性）衡量技能可靠性。
   - 根据技能成熟度动态调节更新频率：成熟技能（高$V(s)$）减少更新以保持稳定；不成熟技能保持可塑性以便学习。
   - 类比于神经网络中的学习率调度和层冻结策略。

3. **规范化的结构重构（canonical structural refactoring）与回滚验证**
   - 在任务成功后触发在线重构操作，包括合并冗余技能、抽象通用例程、剪枝无关分支等。
   - 所有重构均需经过**回滚验证（rollback validation）**：在最近的任务上测试性能是否下降超过阈值（如20%），若下降则撤销变更。
   - 功能上类似于神经架构搜索（Neural Architecture Search, NAS）。

### 相比现有方法的优势
- **可解释性强**：技能为显式的符号程序，具备明确的控制流、参数和前置/后置条件。
- **支持长期复合任务**：通过技能复用和组合解决长视野任务。
- **持续演化能力**：技能库不是静态的，而是随着经验增长自动优化和精简。
- **稳定性与可塑性平衡**：通过成熟度门控避免灾难性遗忘，同时允许新技能灵活调整。
- **结构紧凑性**：重构机制有效抑制技能库膨胀，提升规划效率。

---

## 2. 核心实验方法和设置

### 数据集
实验在两个具身环境基准上进行：
- **MineDojo**：基于Minecraft的复杂开放世界环境，支持丰富的动作空间和多样化目标描述。
- **Crafter**：轻量级生存环境，具有结构化的科技树进展，强调持续学习和组合泛化。

### 实验设置
- 使用 **gpt-5-mini-2025-08-07** 作为所有LLM驱动的操作符（如`CODEGEN`, `REFLECT`）。
- 智能体在一个任务流 $\mathcal{T} = \{T_1, T_2, ...\}$ 上连续学习，每个任务由自然语言指定。
- 技能执行基于 Mineflayer API 控制虚拟角色。

### 评估指标
| 指标 | 描述 |
|------|------|
| **Tech Tree Mastery** | 解锁物品所需的平均迭代次数（越低越好） |
| **Cumulative Reward** | 在Crafter中累计奖励，反映生存能力和资源获取效率 |
| **Skill Retention Rate (SRR)** | 已掌握技能在后续任务训练后的保留成功率，用于衡量**灾难性遗忘**程度 |
| **Skill Library Size** | 随时间增长的技能数量，评估技能复用与冗余控制能力 |

### 基线方法对比
| 方法 | 特点 |
|------|------|
| **ReAct** | 基于提示的推理-行动循环，无持久化技能 |
| **Reflexion** | 失败后自我反思，但无技能网络结构 |
| **AutoGPT** | 多步计划生成并执行代码，但技能为临时片段 |
| **Voyager** | 维护扁平技能库，通过相似性检索复用技能，无trace-based credit assignment 和结构重构 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 和 Figure 2）

#### Minecraft 科技树解锁效率（越低越好）
| 方法 | 木制工具 | 石制工具 | 铁制工具 | 钻石工具 | 黑曜石 |
|------|---------|---------|---------|----------|--------|
| ReAct | N/A | N/A | N/A | N/A | — |
| Reflexion | N/A | N/A | N/A | N/A | — |
| AutoGPT | 92±72 | 94±72 | 135±103 | N/A | — |
| Voyager | 6±2 | 11±2 | 21±7 | 102 (1/3) | — |
| **PSN (Ours)** | **5±2** | **11±3** | **19±4** | **51±9 (3/3)** | **77 (1/3)** |

> ✅ PSN 在所有阶段表现更优，尤其在高阶工具（钻石、黑曜石）上显著优于 Voyager，且成功率更高。

### 与其他方法的对比结果
- **任务成功率**：PSN 成功解锁钻石工具三次，平均仅需 51 次尝试；Voyager 仅一次成功，耗时 102 次。
- **技能保留率（Figure 4）**：PSN 的 Skill Retention Rate 接近 100%，而 Voyager 随着训练推进迅速下降至约 30%，表明其存在严重**灾难性遗忘**。
- **累积奖励（Figure 3）**：在 Crafter 中，PSN 获得更高的累积奖励，说明其在密集反馈环境下也能稳定学习。

### 消融实验结果
| 变体 | 结果分析 |
|------|----------|
| **PSN w/o Optimizer** | 在早期任务上接近完整版PSN，但在铁之后无法稳定推进到钻石/黑曜石，说明优化器对长视野任务至关重要。 |
| **PSN w/o Maturity-aware Gating**（Figure 5） | 学习曲线震荡剧烈，收敛不稳定，最终成功率低于完整模型，证明成熟度门控有助于稳定可靠技能。 |
| **PSN vs. Create New Skills**（Figure 6） | 若每次任务都创建新技能，技能库快速增长；而PSN通过复用已有技能使库大小趋于平稳甚至下降，体现高效复用能力。 |
| **Offline Refactor vs. Online Refactor** | 对Voyager技能库进行离线重构（Voyager-R）虽减少了代码重复，但任务成功率仅为 0.6875，远低于PSN的 0.8462，说明**在线紧耦合的重构机制更有效**。 |

---

## 4. 关键结论和发现

### 主要发现
1. **PSN的学习动力学与神经网络训练存在结构性类比**：
   - REFLECT ≈ Backpropagation（信用分配）
   - Maturity-aware Gating ≈ Learning Rate Scheduling / Layer Freezing（稳定性-可塑性权衡）
   - Refactor ≈ Neural Architecture Search（结构搜索）
   - 这一洞察揭示了不同表征范式下持续学习原则的普适性。

2. **符号程序作为技能载体具有强大表达力和可维护性**：
   - 显式控制流和预/后置条件使得技能行为透明可控。
   - 支持细粒度修复（如资源计算、边界检查、API误用）和跨技能协同优化。

3. **在线结构重构 + 回滚验证 是维持技能库紧凑性的关键**：
   - 不仅减少冗余，还促进更高层次的抽象（如从 `mineOakLogs` 和 `mineBirchLogs` 抽象出 `mineLogs(type)`）。

4. **技能复用而非复制是实现组合泛化的核心路径**：
   - PSN通过backward-chaining优先复用已有技能，显著降低技能库增长速度，并提升泛化能力。

### 局限性
- 当前实现受限于**单样本在线学习模式**（batch-size-one），缺乏并行执行与优化能力。
- **缺乏形式化保证**：重构和反射过程没有理论上的收敛性或最优性证明。
- 依赖LLM生成质量，可能引入语义不一致或无效修改。

### 未来工作方向
- 扩展至大规模批量学习场景，利用更多并行资源加速演化。
- 引入更强的形式化方法（如程序验证、类型系统）增强重构的安全性和正确性。
- 探索多智能体协作下的技能共享与迁移。
- 将PSN思想应用于其他领域（如机器人控制、Web自动化代理）。

---

> 📌 **总结一句话**：  
> PSN 提出了一种将技能组织为可执行符号程序网络的框架，通过 **REFLECT、成熟度门控、在线重构** 三大机制实现了类神经网络的持续学习动力学，在开放环境中展现出卓越的技能复用、快速适应和强泛化能力。

</details>

---

### 15. [DiffCoT: Diffusion-styled Chain-of-Thought Reasoning in LLMs](https://arxiv.org/abs/2601.03559)

**Authors**: Shidong Cao, Hongzhan Lin, Yuxuan Gu, Ziyang Luo, Jing Ma  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03559v1  

#### Abstract
Chain-of-Thought (CoT) reasoning improves multi-step mathematical problem solving in large language models but remains vulnerable to exposure bias and error accumulation, as early mistakes propagate irreversibly through autoregressive decoding. In this work, we propose DiffCoT, a diffusion-styled Co...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**DIFFCoT: Diffusion-styled Chain-of-Thought Reasoning in LLMs**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Chain-of-Thought (CoT)** 推理在大型语言模型（LLMs）中广泛用于数学等多步推理任务，但其存在两个关键缺陷：
- **暴露偏差（exposure bias）**：训练时使用“教师强制”（teacher-forcing），即每一步都基于正确的前缀生成；但在推理时，一旦某步出错，错误会持续传播。
- **错误累积（error accumulation）**：早期步骤中的错误无法被后续修正，导致最终答案错误。

现有方法如 **Preference Optimization (PO)** 和 **Tree of Thoughts (ToT)** 虽能通过偏好学习或搜索机制缓解部分问题，但仍受限于前向、单向的生成模式，缺乏对已生成步骤的全局修正能力。

---

### 提出的新方法：**DIFFCoT**
作者提出 **DIFFCoT**，一种将 **扩散模型（diffusion model）思想引入 CoT 推理** 的新框架。其核心是将 CoT 推理重构为一个**迭代去噪过程**，允许模型在生成过程中不断回顾并修正先前的推理步骤。

#### 创新点：
1. **扩散风格的推理范式**  
   将整个推理链视为一条可能被“噪声污染”的路径，通过多轮迭代逐步“去噪”，提升推理轨迹的整体一致性。

2. **滑动窗口机制（sliding-window mechanism）**  
   在推理过程中维护一个滑动窗口，对窗口内的历史步骤进行**同时生成与修订**，实现生成与自我纠正的统一。

3. **因果扩散噪声调度（causal diffusion noise schedule）**  
   引入时间感知的噪声策略：越靠后的推理步骤施加更强的噪声扰动，以保留推理链的**时序因果结构**，防止未来信息泄露。

4. **保留 token-level autoregression**  
   在步骤级别引入扩散机制的同时，保持 token 级别的自回归生成，确保与现有 LLM 架构兼容，便于微调。

---

### 相比现有方法的优势
| 特性 | 传统 CoT / DPO 方法 | DIFFCoT |
|------|---------------------|---------|
| 错误可修正性 | ❌ 仅前向生成，错误不可逆 | ✅ 可迭代修正早期错误 |
| 暴露偏差 | 高（训练与推理不一致） | 显著降低 |
| 全局一致性 | 局部优化为主 | 支持长程信号引导的全局优化 |
| 实现复杂度 | 低 | 中等（需构造偏好对） |
| 与现有架构兼容性 | 高 | 高（基于 LoRA 微调即可） |

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个主流数学推理基准上进行评估：
- **GSM8K**：小学级数学应用题，强调多步算术推理。
- **SVAMP**：语义变体版的数学问题，测试模型泛化能力。
- **MATH**：涵盖五个难度等级（L1–L5），题目更复杂，涉及代数、几何等。

> 所有数据集均采样最多 300 个测试样本，并控制最大推理步数 $ K $。

---

### 实验设置
- **模型骨干（backbone models）**：
  - Llama3-8B
  - Qwen3-8B
  - Qwen3-4B
- **训练方式**：
  - 使用 **LoRA** 进行高效微调（rank=8）
  - 采用 **Direct Preference Optimization (DPO)** 损失函数（$\beta=0.4$）
  - 学习率：1e-5，cosine 调度，batch size=32，训练 3 轮
- **推理参数**：
  - 温度固定为 0.4，贪婪解码
- **评估指标**：
  - **准确率（Accuracy %）**：最终答案是否正确

---

### 基线方法对比
| 方法 | 简介 |
|------|------|
| **CoT** | 标准思维链提示，greedy decoding |
| **ToT (Tree of Thoughts)** | 多路径搜索，结合 MCTS 探索最优路径 |
| **TS-SFT** | 对 ToT 搜索得到的高质量路径进行监督微调 |
| **CPO (Chain of Preference Optimization)** | 步骤级偏好优化，利用 LLM 自我评判构建偏好对 |
| **Step-DPO** | 显式收集错误步骤，进行步骤级 DPO |
| **Full-Step-DPO** | 对整条推理链进行全局偏好优化 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 \ 数据集 | GSM8K | SVAMP | MATH-L1 | MATH-L5 |
|---------------|-------|-------|--------|--------|
| CoT           | 37.2  | 49.6  | 31.3   | 1.9    |
| ToT           | 37.7  | 48.8  | 33.8   | 3.0    |
| CPO           | 36.1  | 48.6  | 32.0   | 3.1    |
| Step-DPO      | 38.1  | 48.4  | 32.0   | 3.1    |
| Full-Step-DPO | 39.6  | 50.4  | 34.3   | 4.4    |
| **DIFFCoT**   | **65.5** | **85.9** | **63.0** | **10.5** |

> 注：DIFFCoT 在所有主干模型上均显著优于基线，尤其在困难任务（如 MATH-L4/L5）上提升明显。

---

### 与基线方法的对比结果
- **平均提升幅度**：
  - 在 GSM8K 上比 Full-Step-DPO 提升近 **26 个百分点**
  - 在 SVAMP 上提升约 **5–6 个百分点**
  - 在 MATH 各层级中，平均提升 **20+ 个百分点**
- **稳定性更高**：
  - CPO 和 ToT 在不同模型上的表现波动较大（例如在 Qwen3-8B 上 MATH-L1 性能下降）
  - DIFFCoT 在所有模型和数据集上均保持稳定领先

---

### 消融实验结果（Table 2）

| 模型 | 设置 | GSM8K ↓ | SVAMP ↓ |
|------|------|--------|--------|
| Llama3-8B | 完整 DIFFCoT | 39.6 | 50.4 |
| &nbsp;&nbsp;– window size=1 (退化为 AR) | | 36.3 (-3.3) | 48.3 (-2.1) |
| &nbsp;&nbsp;– window size=K (全序列扩散) | | 30.3 (-9.3) | 42.6 (-7.8) |
| &nbsp;&nbsp;– 移除 causal noise | | 35.5 (-4.1) | 48.2 (-2.2) |
| Qwen3-4B | 完整 DIFFCoT | 65.4 | 83.2 |
| &nbsp;&nbsp;– window size=1 | | 63.0 (-2.4) | 80.2 (-3.0) |
| &nbsp;&nbsp;– window size=K | | 56.7 (-8.7) | 73.9 (-9.3) |
| &nbsp;&nbsp;– 移除 causal noise | | 61.9 (-3.5) | 79.1 (-4.1) |

#### 结论：
- **滑动窗口大小适中最佳**：太小（=1）失去修正能力；太大（=K）破坏因果结构。
- **因果噪声调度至关重要**：移除后性能显著下降，说明其对维持推理逻辑连贯性起关键作用。

---

## 4. 关键结论和发现

### 主要发现
1. **CoT 推理可以且应该支持全局可修订性**  
   传统前向生成范式限制了纠错能力，而 DIFFCoT 证明通过引入扩散机制，可以让模型“回头看”并修正早期错误。

2. **迭代去噪显著增强鲁棒性**  
   即使初始推理路径包含语义漂移或弱相关步骤，DIFFCoT 也能通过多轮去噪逐渐收敛到正确路径。

3. **因果结构必须显式建模**  
   盲目应用全序列扩散会破坏推理的时间依赖性，因此提出的 **causal diffusion noise schedule** 是成功的关键设计。

4. **性能提升具有普适性和可扩展性**  
   DIFFCoT 在不同规模（4B/8B）、不同家族（Llama/Qwen）的模型上均取得一致增益，表明其良好的泛化能力。

---

### 方法的局限性
1. **离策略数据构建带来的分布偏移风险**  
   偏好数据由 MCTS 生成，而目标模型是微调后的 LLM，行为策略与训练策略不一致，可能导致训练不稳定。

2. **违反局部马尔可夫性质**  
   因为允许修改历史步骤，打破了标准自回归生成的“过去不变”假设，增加了生成过程的不确定性。

3. **训练成本较高**  
   需要额外生成大量候选路径和 rollout 评估，数据构造开销大（约 11 GPU 小时处理 500 样本）。

---

### 未来工作方向
- 探索将 **Reinforcement Learning** 与扩散滑动窗口结合，直接优化去噪动态过程。
- 扩展至更多推理领域，如符号推理、程序合成、科学问答等。
- 设计更高效的在线策略数据采集机制，减少对外部搜索（如 MCTS）的依赖。
- 研究如何在不牺牲推理效率的前提下，进一步压缩迭代次数。

---

> ✅ **总结一句话**：  
> DIFFCoT 成功地将 **diffusion model 的思想融入 CoT 推理**，实现了“边想边改”的类人推理机制，在多个数学推理任务上大幅超越现有 PO 方法，为构建更鲁棒、可纠错的语言模型推理系统提供了新范式。

</details>

---

### 16. [What Does Loss Optimization Actually Teach, If Anything? Knowledge Dynamics in Continual Pre-training of LLMs](https://arxiv.org/abs/2601.03858)

**Authors**: Seyed Mahed Mousavi, Simone Alghisi, Giuseppe Riccardi  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03858v1  

#### Abstract
Continual Pre-Training (CPT) is widely used for acquiring and updating factual knowledge in LLMs. This practice treats loss as a proxy for knowledge learning, while offering no grounding into how it changes during training. We study CPT as a knowledge learning process rather than a solely optimizati...

---

### 17. [Evaluating Small Decoder-Only Language Models for Grammar Correction and Text Simplification](https://arxiv.org/abs/2601.03874)

**Authors**: Anthony Lamelas  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03874v1  

#### Abstract
Large language models have become extremely popular recently due to their ability to achieve strong performance on a variety of tasks, such as text generation and rewriting, but their size and computation cost make them difficult to access, deploy, and secure in many settings. This paper investigate...

---

### 18. [Weather-Aware Transformer for Real-Time Route Optimization in Drone-as-a-Service Operations](https://arxiv.org/abs/2601.03376)

**Authors**: Kamal Mohamed, Lillian Wassim, Ali Hamdi, Khaled Shaban  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03376v1  

#### Abstract
This paper presents a novel framework to accelerate route prediction in Drone-as-a-Service operations through weather-aware deep learning models. While classical path-planning algorithms, such as A* and Dijkstra, provide optimal solutions, their computational complexity limits real-time applicabilit...

---

### 19. [Variational Inference, Entropy, and Orthogonality: A Unified Theory of Mixture-of-Experts](https://arxiv.org/abs/2601.03577)

**Authors**: Ye Su, Yong Liu  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03577v1  

#### Abstract
Mixture-of-Experts models enable large language models to scale efficiently, as they only activate a subset of experts for each input. Their core mechanisms, Top-k routing and auxiliary load balancing, remain heuristic, however, lacking a cohesive theoretical underpinning to support them. To this en...

---

### 20. [Controllable LLM Reasoning via Sparse Autoencoder-Based Steering](https://arxiv.org/abs/2601.03595)

**Authors**: Yi Fang, Wenjie Wang, Mingfeng Xue, Boyi Deng, Fengli Xu, Dayiheng Liu, Fuli Feng  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03595v1  

#### Abstract
Large Reasoning Models (LRMs) exhibit human-like cognitive reasoning strategies (e.g. backtracking, cross-verification) during reasoning process, which improves their performance on complex tasks. Currently, reasoning strategies are autonomously selected by LRMs themselves. However, such autonomous ...

---

### 21. [ROI-Reasoning: Rational Optimization for Inference via Pre-Computation Meta-Cognition](https://arxiv.org/abs/2601.03822)

**Authors**: Muyang Zhao, Qi Qi, Hao Sun  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03822v1  

#### Abstract
Large language models (LLMs) can achieve strong reasoning performance with sufficient computation, but they do not inherently know how much computation a task requires. We study budgeted inference-time reasoning for multiple tasks under a strict global token constraint and formalize it as a Ordered ...

---

### 22. [Trade-R1: Bridging Verifiable Rewards to Stochastic Environments via Process-Level Reasoning Verification](https://arxiv.org/abs/2601.03948)

**Authors**: Rui Sun, Yifan Sun, Sheng Xu, Li Zhao, Jing Li, Daxin Jiang, Chen Hua, Zuo Bai  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03948v1  

#### Abstract
Reinforcement Learning (RL) has enabled Large Language Models (LLMs) to achieve remarkable reasoning in domains like mathematics and coding, where verifiable rewards provide clear signals. However, extending this paradigm to financial decision is challenged by the market's stochastic nature: rewards...

---

### 23. [EvolMem: A Cognitive-Driven Benchmark for Multi-Session Dialogue Memory](https://arxiv.org/abs/2601.03543)

**Authors**: Ye Shen, Dun Pei, Yiqiu Guo, Junying Wang, Yijin Guo, Zicheng Zhang, Qi Jia, Jun Zhou, Guangtao Zhai  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03543v1  

#### Abstract
Despite recent advances in understanding and leveraging long-range conversational memory, existing benchmarks still lack systematic evaluation of large language models(LLMs) across diverse memory dimensions, particularly in multi-session settings. In this work, we propose EvolMem, a new benchmark fo...

---

### 24. [SyncThink: A Training-Free Strategy to Align Inference Termination with Reasoning Saturation](https://arxiv.org/abs/2601.03649)

**Authors**: Gengyang Li, Wang Cai, Yifeng Gao, Yunfang Wu  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03649v1  

#### Abstract
Chain-of-Thought (CoT) prompting improves reasoning but often produces long and redundant traces that substantially increase inference cost. We present SyncThink, a training-free and plug-and-play decoding method that reduces CoT overhead without modifying model weights. We find that answer tokens a...

---

### 25. [Decide Then Retrieve: A Training-Free Framework with Uncertainty-Guided Triggering and Dual-Path Retrieval](https://arxiv.org/abs/2601.03908)

**Authors**: Wang Chen, Guanqiang Qi, Weikang Li, Yang Li, Deguo Xia, Jizhou Huang  
**Category**: cs.CL  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03908v1  

#### Abstract
Retrieval-augmented generation (RAG) enhances large language models (LLMs) by incorporating external knowledge, but existing approaches indiscriminately trigger retrieval and rely on single-path evidence construction, often introducing noise and limiting performance gains. In this work, we propose D...

---

### 26. [Enhancing Small Dataset Classification Using Projected Quantum Kernels with Convolutional Neural Networks](https://arxiv.org/abs/2601.03375)

**Authors**: A. M. A. S. D. Alagiyawanna, Asoka Karunananda, A. Mahasinghe, Thushari Silva  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03375v1  

#### Abstract
Convolutional Neural Networks (CNNs) have shown promising results in efficiency and accuracy in image classification. However, their efficacy often relies on large, labeled datasets, posing challenges for applications with limited data availability. Our research addresses these challenges by introdu...

---

### 27. [Sensor to Pixels: Decentralized Swarm Gathering via Image-Based Reinforcement Learning](https://arxiv.org/abs/2601.03413)

**Authors**: Yigal Koifman, Eran Iceland, Erez Koifman, Ariel Barel, Alfred M. Bruckstein  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03413v1  

#### Abstract
This study highlights the potential of image-based reinforcement learning methods for addressing swarm-related tasks. In multi-agent reinforcement learning, effective policy learning depends on how agents sense, interpret, and process inputs. Traditional approaches often rely on handcrafted feature ...

---

### 28. [Lightweight Test-Time Adaptation for EMG-Based Gesture Recognition](https://arxiv.org/abs/2601.04181)

**Authors**: Nia Touko, Matthew O A Ellis, Cristiano Capone, Alessio Burrello, Elisa Donati, Luca Manneschi  
**Category**: cs.LG  
**Published**: 2026-01-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.04181v1  

#### Abstract
Reliable long-term decoding of surface electromyography (EMG) is hindered by signal drift caused by electrode shifts, muscle fatigue, and posture changes. While state-of-the-art models achieve high intra-session accuracy, their performance often degrades sharply. Existing solutions typically demand ...

---

### 29. [Digital Red Queen: Adversarial Program Evolution in Core War with LLMs](https://arxiv.org/abs/2601.03335)

**Authors**: Akarsh Kumar, Ryan Bahlous-Boldi, Prafull Sharma, Phillip Isola, Sebastian Risi, Yujin Tang, David Ha  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.03335v1  

#### Abstract
Large language models (LLMs) are increasingly being used to evolve solutions to problems in many domains, in a process inspired by biological evolution. However, unlike biological evolution, most LLM-evolution frameworks are formulated as static optimization problems, overlooking the open-ended adve...

---

### 30. [xDNN(ASP): Explanation Generation System for Deep Neural Networks powered by Answer Set Programming](https://arxiv.org/abs/2601.03847)

**Authors**: Ly Ly Trieu (New Mexico State University), Tran Cao Son (New Mexico State University)  
**Category**: cs.AI  
**Published**: 2026-01-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.03847v1  

#### Abstract
Explainable artificial intelligence (xAI) has gained significant attention in recent years. Among other things, explainablility for deep neural networks has been a topic of intensive research due to the meteoric rise in prominence of deep neural networks and their "black-box" nature. xAI approaches ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
