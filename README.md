# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-05 06:08:12 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FlexSpec: Frozen Drafts Meet Evolving Targets in Edge-Cloud Collaborative LLM Speculative Decoding](https://arxiv.org/abs/2601.00644)

**Authors**: Yuchen Li, Rui Kong, Zhonghao Lyu, Qiyang Li, Xinran Chen, Hengyi Cai, Lingyong Yan, Shuaiqiang Wang, Jiashu Zhao, Guangxu Zhu, Linghe Kong, Guihai Chen, Haoyi Xiong, Dawei Yin  
**Category**: cs.DC  
**Published**: 2026-01-05  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.00644v1  

#### Abstract
Deploying large language models (LLMs) in mobile and edge computing environments is constrained by limited on-device resources, scarce wireless bandwidth, and frequent model evolution. Although edge-cloud collaborative inference with speculative decoding (SD) can reduce end-to-end latency by executi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FlexSpec: Frozen Drafts Meet Evolving Targets in Edge-Cloud Collaborative LLM Speculative Decoding

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在边缘-云协同推理场景中，大型语言模型（LLM）面临三大挑战：
- **频繁模型更新导致通信开销巨大**：云端目标模型通过 PEFT（如 LoRA）持续演进，传统 Speculative Decoding（SD）要求边缘端草稿模型同步更新，引发“**更新风暴**”（update storm），带来高昂的带宽消耗。
- **分布偏移导致性能崩溃**（performance collapse）：若不更新边缘草稿模型，则其输出分布与演化后的云端目标模型严重失配，token 接受率急剧下降，甚至劣于标准自回归解码。
- **无线信道动态性影响推测效率**：固定推测长度 $K$ 在时变网络条件下表现不佳，在弱信号下传输延迟主导总延迟，而强信号下又无法充分利用带宽。

### **提出了什么新方法或新思路**

作者提出 **FlexSpec**，一种面向演化边缘-云系统的通信高效协作推理框架，核心创新如下：

#### ✅ **共享冻结主干架构**（Shared Frozen Backbone Architecture）
- 引入一个**共享的冻结锚块**（frozen anchor block），从基础版本的目标模型中复制至边缘侧草稿模型，并保持冻结。
- 草稿模型仅训练一个轻量级头部（lightweight head），通过知识蒸馏（Knowledge Distillation）对齐特征空间。
- 云端采用 PEFT 微调时，**冻结主干参数**（包括锚块和 LM head），只更新适配器参数，从而保证边缘草稿模型无需重训练即可兼容多个演化版本的目标模型。

> 这实现了 **“静态草稿 + 动态目标”** 的解耦设计，从根本上避免了 OTA 同步。

#### ✅ **信道感知的自适应推测机制**（Channel-Aware Adaptive Speculation）
- 将推测长度 $K$ 视为可调变量，基于实时信道状态信息（CSI）和设备能耗预算动态调整。
- 构建精细化延迟模型，综合考虑边缘计算、上行传输、云端验证、下行反馈四部分。
- 推导出最大化 **有效 token 生成速率**（Effective Token Generation Rate, ETGR）的最优 $K^*$ 决策公式：
  $$
  K^* = \arg\max_K \frac{\mathbb{E}[T|K]}{T_{\text{fixed}} + K \cdot T_{\text{marginal}}(R_n)}
  $$
  其中 $T_{\text{marginal}}$ 包含通信速率 $R_n$ 和每 token 验证开销。

> 实现了在不同网络条件下的自动权衡：弱网用小 $K$，强网用大 $K$。

#### ✅ **语义同步协议与 KV 缓存回滚机制**
- 边缘发送整块草稿 token 索引（非浮点嵌入），大幅压缩上行负载。
- 云端维护持久化 KV Cache 会话，仅对新接收的草稿 token 计算 KV 对。
- 若发生拒绝，执行 KV Cache 回滚，避免重复计算整个上下文。

---

### **相比现有方法的优势**

| 方面 | 传统 SD 方法（如 EAGLE、Medusa） | FlexSpec |
|------|-------------------------------|----------|
| **模型同步需求** | 必须同步更新边缘草稿模型 | ❌ 无需同步，支持版本无关推理 |
| **通信开销** | 高频 OTA 下载模型权重（GB级） | 仅传输 token 索引（KB级） |
| **抗分布偏移能力** | 分布偏移后接受率骤降 | 锚块对齐，鲁棒性强 |
| **信道适应性** | 固定推测长度 $K$ | 自适应 $K$，匹配信道质量 |
| **部署成本** | 需频繁维护边缘模型 | 单一静态模型服务多版本目标 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

涵盖多种任务类型以验证泛化能力：
- **数学推理**：GSM8K
- **问答**：Natural Questions（QA & RAG）
- **对话**：MT-Bench
- **机器翻译**：WMT14 DE-EN
- **摘要**：CNN/DailyMail
- **代码生成**：HumanEval

### **实验设置**

#### **硬件平台**
- **云端服务器**：NVIDIA H800/A800/V100 GPU 集群
- **边缘设备**：Jetson AGX Orin、Snapdragon 8 Gen3、iPhone 15 Pro Max、Raspberry Pi 5
- **网络环境模拟**：5G（强）、4G（平均）、WiFi（弱）

#### **模型配置**
- **目标模型**：Llama-2 70B、Llama-3 70B、Mixtral 8x7B
- **草稿模型**：基于 Llama-2 7B 构建，仅保留最后一层 Transformer 块作为锚块 + 轻量 MLP 头部

#### **评估指标**
- **端到端延迟**（End-to-End Latency）
- **加速比**（Speedup vs. Cloud-Only）
- **Token 接受率**（Acceptance Rate）
- **能量消耗**（Energy Consumption）
- **内存占用**（Memory Footprint）
- **热效应**（Thermal Behavior）

### **基线方法对比**

| 基线方法 | 特点 |
|--------|------|
| **Cloud-Only** | 标准自回归解码，所有计算在云端 |
| **Standard SD (Naive)** | 使用预训练小模型作草稿，无对齐机制 |
| **PLD (n-gram)** | 基于 n-gram 检索的免训练草稿 |
| **Lookahead** | 并行解码算法，无需额外草稿模型 |
| **EAGLE-2 (Synced)** | 当前最优模型内推测方法，“理想同步”设定 |
| **Medusa-1 (Synced)** | 多头预测框架，假设完全同步 |
| **DSSD** | 专为无线设计的 SD 框架，但使用固定策略 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 🔹 **表 III & IV：确定性与随机采样下的端到端延迟与加速比**
- 在 **GSM8K** 上（温度=0）：
  - FlexSpec 在 **WiFi（弱）环境下实现 1.95× 加速**
  - Standard SD 因分布偏移反而 **慢 34%**（0.66×）
  - EAGLE-2 在弱网下因传输开销过大也退化至 0.86×
- 在 **随机采样**（Temperature=1）下：
  - FlexSpec 仍保持 **1.65×–1.90× 加速**
  - EAGLE-2 性能显著下降（从 2.4× → 1.6×），因其依赖贪心路径
  - Standard SD 出现“性能崩溃”，在 WiFi 下仅为 0.65×

> ✅ **FlexSpec 是唯一在弱网和随机采样下仍稳定加速的方法**

#### 🔹 **图 5：消融实验 — 固定 vs 自适应推测长度**
- 固定 $K=5$：在 5G 下表现良好（220ms），但在 WiFi 下高达 1455ms（**2.3× 慢于 baseline**）
- 固定 $K=1$：稳健但浪费带宽（5G 下 405ms）
- **FlexSpec（自适应）**：在 5G 下接近 $K=5$，在 WiFi 下自动降为 $K≈1$，实现最佳平衡

> ✅ **信道感知自适应机制显著提升鲁棒性和吞吐量**

#### 🔹 **表 V：异构边缘设备上的性能**
| 设备 | GSM8K 加速比 |
|------|-------------|
| Raspberry Pi 5（CPU） | 0.76×（反向减速） |
| iPhone 15 Pro Max（NPU） | 1.82× |
| Snapdragon 8 Gen3（NPU） | 1.93× |
| Jetson AGX Orin（GPU） | 1.96× |

> ✅ **需硬件加速支持（GPU/NPU）才能发挥优势；消费级手机也可运行**

#### 🔹 **表 VI：新模型架构扩展性**
| 目标模型 | FlexSpec 在 5G 下加速比 |
|---------|-----------------------|
| Llama-2 70B | 1.95× |
| Llama-3 70B | **2.30×** |
| Mixtral 8x7B（MoE） | 1.75× |

> ✅ **锚块对齐机制具有跨模型迁移能力**

#### 🔹 **图 6：能耗分析**
- **FlexSpec 相比 Cloud-Only 减少 53% 总能耗**
- 主要节省来自通信能耗（radio active time 缩短）
- 本地计算能耗低，且避免了全设备推理的高发热问题

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **静态草稿模型可通过锚块对齐支持动态目标模型**，无需 OTA 同步，彻底解决“更新风暴”问题。
2. ✅ **信道感知的自适应推测机制** 能在不同网络条件下自动调节 $K$，最大化 ETGR，在弱网中尤为关键。
3. ✅ **FlexSpec 在多样化任务、模型、设备和网络条件下均表现出色**，尤其在带宽受限环境中优于所有基线。
4. ✅ **能量与热效率显著提升**，适合移动终端长期交互式应用。

### **方法的局限性**

- **依赖主干冻结的微调方式**：要求云端必须冻结主干进行 PEFT，限制了某些全参数微调场景。
- **对极低端设备不友好**：纯 CPU 设备（如 Raspberry Pi）无法受益，需 NPU/GPU 支持。
- **初始训练仍需一次离线蒸馏过程**：虽然后续无需再训练，但部署前需完成对齐训练。

### **未来工作方向**

- 扩展至更多模型家族（如 Qwen、Phi 等）和模态（多模态 LLM）。
- 探索更高效的轻量化草稿结构（如 MoE 草稿）。
- 结合 Early Exit 或 Dynamic Inference 进一步降低边缘计算负担。
- 在真实用户流量下进行大规模在线 A/B 测试。

---

> **总结一句话**：  
> **FlexSpec 通过“共享冻结锚块 + 信道感知自适应推测”的双重创新，首次实现了版本无关、通信高效、鲁棒可扩展的边缘-云协同 LLM 推理框架，为移动端部署大模型提供了实用化解决方案。**

</details>

---

### 2. [Optimized Hybrid Feature Engineering for Resource-Efficient Arrhythmia Detection in ECG Signals: An Optimization Framework](https://arxiv.org/abs/2601.00192)

**Authors**: Moirangthem Tiken Singh, Manibhushan Yaikhom  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.00192v1  

#### Abstract
Cardiovascular diseases, particularly arrhythmias, remain a leading global cause of mortality, necessitating continuous monitoring via the Internet of Medical Things (IoMT). However, state-of-the-art deep learning approaches often impose prohibitive computational overheads, rendering them unsuitable...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Optimized Hybrid Feature Engineering for Resource-Efficient Arrhythmia Detection in ECG Signals: An Optimization Framework

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于深度学习的心律失常检测模型（如 CNN、Transformer）虽然精度高，但存在以下问题：
- **计算开销大**：模型参数多、推理延迟高，难以部署在资源受限的边缘设备（edge devices）上。
- **能效低**：高功耗限制了其在可穿戴设备和电池供电系统中的长期应用。
- **缺乏可解释性**：黑箱决策机制阻碍了临床信任与医学验证。

这些问题导致最先进的模型无法满足 IoMT（Internet of Medical Things）场景下对**实时性、低功耗、可解释性**的综合需求。

---

### 🚀 提出的新方法与创新思路
本文提出了一种**以数据为中心的优化框架**（data-centric framework），将复杂性从“模型”转移到“特征工程”，核心思想是：

> **通过精心设计的混合特征工程，使原始非线性的 ECG 数据流形变为线性可分，从而启用超轻量级、可解释的线性分类器。**

#### 主要技术路径：
1. **Hybrid Feature Engineering**：
   - 结合 **wavelet decomposition**（小波分解）提取时频域特征；
   - 引入 **graph-theoretic descriptors**（图论描述符），如 PageRank 中心性和聚类系数，建模心跳序列之间的结构相似性与动态关系。
2. **特征空间优化**：
   - 使用 **Mutual Information** 和 **Recursive Feature Elimination (RFE)** 进行特征选择；
   - 应用 **PCA** 提取主成分并拼接到原始特征中，保留全局方差的同时增强判别能力。
3. **分类策略转变**：
   - 放弃复杂的深度神经网络，采用 **Logistic Regression** 和 **Linear SVM** 等线性模型进行最终分类。

---

### 🔍 相比现有方法的优势
| 维度 | 传统深度学习方法 | 本论文方法 |
|------|------------------|-----------|
| **模型大小** | 数 MB 到数十 MB | < 9 KB（Linear SVC: 8.54 KB） |
| **推理延迟** | 毫秒级（ms） | 微秒级（μs）——仅 **0.46 μs** |
| **能耗潜力** | 高（需 GPU 或高性能 MCU） | 极低，适合电池甚至无源传感器 |
| **可解释性** | 黑箱，难解释 | 白盒，支持特征重要性分析 |
| **准确率** | ~99% | **98.44%**（MIT-BIH 上媲美 SOTA） |

> ✅ 实现了“**临床级精度 + 边缘级效率 + 医疗级可解释性**”三重目标的平衡。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **MIT-BIH Arrhythmia Database**：
  - 48 条双通道（two-channel）、30 分钟动态记录，采样率 360 Hz。
  - 作为主要基准测试集。
- **St. Petersburg INCART 12-Lead Arrhythmia Database**：
  - 75 条 12 导联记录，原采样率 257 Hz → 统一重采样至 360 Hz。
  - 提取 Lead II 和 V1 模拟双导联系统，用于跨数据集鲁棒性验证。

所有样本按 AAMI 标准分为五类：Normal (N), Supraventricular ectopic (S), Ventricular ectopic (V), Fusion (F), Unknown (Q)。

---

### ⚙️ 实验设置与评估指标

#### 数据预处理流程（Pipeline）：
1. **Preprocessing**：Butterworth 带通滤波（0.5–40 Hz）
2. **R-Peak Detection**：集成 Pan-Tompkins、CWT 和自适应阈值法，结合 SQI 加权融合
3. **Adaptive Segmentation**：基于复合损失函数（熵、SNR、能量比）动态调整窗口长度
4. **Feature Extraction**：
   - 时间域（TD）：统计矩、零交叉率等
   - 频率域（FD）：谱熵、带功率
   - 形态学特征：QRS 持续时间、T 波幅度等
   - 小波包能量（Daubechies 分解）
5. **Temporal & Graph Augmentation**：
   - HRV 特征（SDNN, pNN50, LF/HF ratio）
   - 构造有向图，节点为心跳，边权重 $ w_{ij} = \cos(f_i,f_j) \cdot e^{-|i-j|/T} $
   - 提取 PageRank 中心性、加权聚类系数
6. **Feature Selection & Enhancement**：
   - MI + RFE 筛选前 50 个关键特征
   - 对这 50 维做 PCA（取前 5 个主成分），再拼接回原特征向量
7. **Class Balancing**：SMOTE-ENN 处理类别不平衡
8. **Classifier Training**：使用 LR、Decision Tree、Linear SVC

#### 评估指标：
- Accuracy, Precision, Recall, Weighted F1-score
- **Efficiency Score (E)**：自定义综合指标  
  $$
  E = \frac{0.6 \cdot F1 + 0.4 \cdot Acc}{0.4 \cdot T_{train} + 0.4 \cdot T_{infer} + 0.2 \cdot S_{model} + \epsilon}
  $$
- 推理延迟（inference latency）、模型大小（model size）、端到端处理时间

#### 基线对比方法：
- KD-Light（知识蒸馏压缩模型，25 KB，96.32% 准确率）
- FPGA-based 1D-CNN（硬件加速 CNN）
- CANet（注意力 CNN，30 MB）
- Smartwatch CNN（5 MB，99.57%）
- CCAL（相关性辅助压缩）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（MIT-BIH 数据集）

| 模型 | Accuracy (%) | F1 (Weighted) | Model Size (KB) | Inference Latency |
|------|---------------|----------------|------------------|--------------------|
| **Proposed Linear SVC** | **98.44** | **0.9843** | **8.54** | **0.46 μs** |
| Logistic Regression | 98.25 | 0.9824 | 8.87 | 0.55 μs |
| Decision Tree | 86.77 | 0.8562 | 4.71 | 0.59 μs |

> 💡 在 MIT-BIH 上达到 **98.44% 准确率**，接近多数深度学习模型水平。

---

### ⏱️ 实时性与资源消耗表现
| 指标 | 表现 |
|------|------|
| **单次心跳处理延迟** | ~52 ms（主要来自特征提取阶段） |
| **分类推理延迟** | < 1 μs（Linear SVC: 0.46 μs） |
| **全管道处理时间（每患者）** | MIT-BIH: 1.53 秒；INCART: 45.05 秒 |
| **是否满足实时监控？** | 是（52 ms << 平均 R-R 间隔 ~800 ms） |

> ✅ 可实现**无缓冲、即时报警**，适用于可穿戴设备连续监测。

---

### 📊 跨数据集鲁棒性（INCART 数据集）
| 模型 | Accuracy (%) | F1 (Weighted) |
|------|---------------|----------------|
| Logistic Regression | 96.76 | 0.9674 |
| Linear SVC | 96.74 | 0.9672 |
| Decision Tree | 89.10 | 0.8913 |

> ✔️ 在更大规模、更复杂病理背景下仍保持良好性能，证明泛化能力强。

---

### 🔬 消融实验与关键发现
#### （1）特征工程有效性验证
- **Scree Plot 显示 PCA 前 5 成分解释超过 64% 方差**（MIT-BIH: 71.1%，INCART: 64.3%），说明降维有效。
- **Boxplot 分析显示 QRS duration、spectral entropy、weighted degree 等特征具有显著类间区分度**，尤其 Ventricular beats 的 QRS 更宽，Graph-based features 能有效分离异常节律。

#### （2）分类器对比揭示“特征空间决定性能”
- 尽管使用相同特征集，**Decision Tree 性能远低于线性模型**（F1 差约 12%），尤其对 Ventricular beats 的召回率仅 ~50%。
- 而 Linear SVC 对所有类别敏感度均 >93%，表明所构建的特征空间本质上是**线性可分的**。

#### （3）统计显著性检验（5-fold CV）
- MIT-BIH 上 Linear SVC 的 F1 均值为 0.9634（CI: [0.952, 0.975]），显著优于 DT（CI: [0.830, 0.915]），无重叠区间（p < 0.05）。
- INCART 上 LR 的 CI 极窄（[0.966, 0.969]），说明大样本下稳定性极高。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **混合特征工程可以将高维非线性 ECG 数据转化为线性可分形式**，使得简单线性模型即可达到深度学习级别的诊断精度。
2. **线性模型（如 Linear SVC）在该特征空间下表现最优**，而非线性树模型反而因依赖轴对齐分割而失效，反向验证了特征空间的良好几何性质。
3. 所提框架实现了**微秒级推理延迟**（0.46 μs）和**亚 10 KB 模型体积**，相比 KD-Light（25 KB）等压缩模型仍有数量级优势。
4. 系统具备良好的**跨数据集鲁棒性**，能在不同采集条件、导联配置下稳定运行。
5. **可解释性强**：线性模型权重显示其优先关注生理相关的频段（如 5–15 Hz QRS 能量）和 HRV 指标（LF/HF ratio），符合医学先验知识。

---

### ⚠️ 局限性
1. **2-lead 配置可能丢失部分 P 波细节**，导致 Normal 与 Supraventricular beats 之间混淆增加（在 INCART 中观察到）。
2. **特征提取阶段占总耗时 90%以上**（尤其是小波和图构造），目前尚不适用于完全无源的能量采集设备（battery-less sensors）。
3. 当前 pipeline 为离线/准实时设计，尚未在真实嵌入式平台（如 Cortex-M 系列 MCU）上部署验证。

---

### 🔮 未来工作方向
1. **硬件加速特征提取模块**：利用 FPGA 或专用 ASIC 加速 wavelet 和 graph construction，降低前端功耗。
2. **自适应参数调节机制**：根据心率变异性动态调整 segmentation 窗口（α, β），提升极端心律下的鲁棒性。
3. **端到端 TinyML 部署**：将整个 pipeline 编译为 TensorFlow Lite Micro 或 Edge Impulse 格式，在真实可穿戴设备上运行。
4. **扩展至更多疾病类型**：如房颤、心肌缺血等，探索该框架的通用性。

---

## 总结
本文提出了一种颠覆性的“**以特征为中心**”的设计范式，成功实现了：
> **用不到 9 KB 的线性模型，在标准 ECG 数据库上取得 98.44% 准确率，并实现 0.46 微秒推理延迟。**

这一成果为下一代**低功耗、可解释、实时化**的可穿戴心脏监护设备提供了切实可行的技术路径，推动了 edge AI 在数字健康领域的真正落地。

</details>

---

### 3. [FlashInfer-Bench: Building the Virtuous Cycle for AI-driven LLM Systems](https://arxiv.org/abs/2601.00227)

**Authors**: Shanli Xing, Yiyan Zhai, Alexander Jiang, Yixin Dong, Yong Wu, Zihao Ye, Charlie Ruan, Yingyi Huang, Yineng Zhang, Liangsheng Yin, Aksara Bayyapu, Luis Ceze, Tianqi Chen  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.00227v1  

#### Abstract
Recent advances show that large language models (LLMs) can act as autonomous agents capable of generating GPU kernels, but integrating these AI-generated kernels into real-world inference systems remains challenging. FlashInfer-Bench addresses this gap by establishing a standardized, closed-loop fra...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FlashInfer-Bench: Building the Virtuous Cycle for AI-driven LLM Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前，尽管大型语言模型（LLMs）已展现出生成复杂 GPU 内核代码的能力，但将这些 AI 生成的内核无缝集成到实际的 LLM 推理系统中仍面临三大挑战：
1. **任务描述不统一**：缺乏标准化的任务定义方式，导致 AI 代理难以准确理解内核需求（如数据精度、形状约束等）。
2. **评估环境脱离真实场景**：多数基准测试基于合成或均匀负载，无法反映真实 LLM 服务中的动态、稀疏和变长请求模式。
3. **部署集成困难**：即使生成了高性能内核，仍需大量手动工程才能将其注入生产系统，阻碍了自动化闭环。

### 提出的新方法与创新点
为解决上述问题，论文提出了 **FlashInfer-Bench**，一个面向 AI 驱动的 LLM 系统的端到端闭环框架，其核心创新包括：

#### （1）FlashInfer Trace：标准化的内核任务描述 Schema
- 引入一种自包含的 JSON 格式 `FlashInfer Trace`，统一描述四个关键组件：
  - **Definition**：操作符接口规范（输入/输出张量、数据类型、可变/固定维度）
  - **Workload**：绑定具体参数的真实世界输入实例
  - **Solution**：实现代码及元信息（语言、目标硬件）
  - **Evaluation**：不可变的性能与正确性评测记录
- 支持 ragged inputs（如 paged attention 中的 page table），并允许对特定模型层进行精细优化。

#### （2）FlashInfer-Bench Dataset：基于真实流量构建的数据集
- 从 SGLang 上运行 DeepSeek-V3、Llama-3.1-8B、Qwen3-30B-A3B 等主流模型收集真实服务 trace。
- 覆盖 GEMM、Attention（GQA/Paged）、MoE、Normalization、Sampling 等典型 LLM 运算。
- 每个内核定义保留约 50 个具有代表性和多样性的 workload，确保评估的真实性。

#### （3）鲁棒的 Benchmarking 框架
- 支持 Deterministic、Low-Precision 和 Stochastic（如 sampling）内核的验证。
- 采用 TVD（Total Variation Distance）衡量采样分布的准确性。
- 提供隔离模式（isolated subprocess）防止 reward hacking，并支持持久化 worker 以提升效率。

#### （4）动态替换机制 `flashinfer_bench.apply()`
- 允许在运行时无侵入地将最优验证内核注入生产引擎（如 SGLang、vLLM）。
- 支持 ahead-of-time 编译索引预热，线上调度开销极低（仅 ~1–2 μs）。
- 实现“生成 → 评测 → 部署”全自动 virtuous cycle。

### 相比现有方法的优势
| 方面 | 现有方法（如 KernelBench, TritonBench） | FlashInfer-Bench |
|------|----------------------------------------|------------------|
| **任务标准化** | 缺乏统一 schema | 定义完整 Trace schema，支持语义一致性 |
| **工作负载真实性** | 合成或随机输入 | 来自真实 LLM 服务 trace |
| **评估全面性** | 忽视非确定性、低精度运算 | 支持 sampling、FP8 等复杂场景 |
| **部署路径** | 手动集成，无自动化流程 | 提供 `apply()` 实现零代码变更部署 |
| **系统级反馈** | 仅关注单个 kernel 性能 | 可观测 end-to-end 请求延迟变化 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **FlashInfer-Bench Dataset**：
  - 包含 41 种不同的 kernel configurations。
  - 涵盖 8 类典型 LLM 内核：GEMM、Ragged/Paged GQA、Ragged/Paged MLA、Fused MoE、RMS Norm、Sampling。
  - 总共 1,600 个经过去重和多样性筛选的 workload。
  - 数据来源于 SGLang 在 ShareGPT prompts 下的服务 trace。

### 实验设置
- **硬件平台**：NVIDIA B200 GPU。
- **评估对象**：由多个前沿 LLM（Gemini 2.5 Pro、Claude Opus 4.1、GPT-5、OpenAI o3）生成的 CUDA 和 Triton 内核。
- **Agent 框架**：采用带反馈循环的 agent（见 Algorithm 1），迭代生成、评测、优化内核直至收敛。
- **Baseline**：
  - 主要对比基线为 FlashInfer（当前最先进的 attention 引擎）。
  - 若无对应实现，则使用 PyTorch 作为 baseline。

### 评估指标
- **fastₚ metric**：表示在超过 p 倍加速阈值下通过正确性检查的工作负载比例。
  $$
  \text{fast}_p = \frac{1}{N} \sum_{i=1}^{N} [\text{correct}_i \land \text{speedup}_i > p]
  $$
- **AUC of fastₚ curve**：综合反映内核的正确率与性能潜力。
- **End-to-end latency**：测量完整请求处理时间的变化，验证 kernel 级改进能否转化为系统收益。
- **Correctness Pass Rate**：当 p=0 时，fast₀ 即为正确率。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）LLM Agent 整体表现（图 4 & 图 7）
| 模型 | fast₀.₉₅ 排名 | 正确率（p=1.0） |
|------|---------------|----------------|
| gemini-2.5-pro | 第1名 | 48.8% |
| gpt-o3 | 第2名 | 71.3% |
| gpt-5-2025-08-07 | 第3名 | **83.9%** |

> 注：虽然 gpt-5 正确率最高，但在高加速区段（fast₀.₉₅）表现不如 gemini-2.5-pro。

#### （2）不同内核类型的表现（图 6）
- **GEMM (Triton)**：LLM 生成内核平均性能仅为人类专家水平的 <50%。
- **GQA Paged Decode (CUDA)**：同样远低于 SOTA。
- **RMSNorm (CUDA)**：接近甚至超过人类水平，因其为 memory-bound，易于达到带宽上限。

#### （3）语言选择的影响（图 7）
- **Triton vs CUDA**：
  - Triton 内核的 **正确率显著更高**（因抽象层次高，减少细节错误）。
  - CUDA 内核虽难写，但一旦成功，**峰值性能更高**（更细粒度控制共享内存、tiling 等）。
- 结论：Triton 更适合快速探索；CUDA 有更大优化空间。

#### （4）case study：GEMM 内核对比
| 内核来源 | 语言 | 平均执行时间（batch=64） | 相对于 baseline 加速比 |
|--------|------|--------------------------|-------------------------|
| FlashInfer (baseline) | CUDA | 0.0112 ms | 1.0× |
| Gemini-2.5-Pro | Triton | 0.0160 ms | 0.7× |
| GPT-5 | Triton | 0.0247 ms | 0.45× |

> 尽管某些 Triton 内核较慢，但表明 DSL 编译器可自动启用高级指令（如 tcgen05），而 LLM 在 CUDA 中难以利用新硬件特性。

#### （5）消融实验：`apply()` 开销与收益
- **开销测试**：
  - 使用 fallback substitution（即用相同内核替换）测得 `apply()` 引入的额外延迟 < 0.8%。
  - 线上调度开销仅 1–2 μs。
- **收益测试**（图 8）：
  - 替换为更快内核后，end-to-end 请求延迟从 1055ms（GPT-5 Triton）降至 934ms（FlashInfer fallback），下降约 11.4%。
  - 验证了 kernel-level 优化能有效传递至系统级性能提升。

---

## 4. 关键结论和发现

### 主要发现
1. **编译失败是主要错误来源**：
   - 在所有 32 个失败案例中，30 个源于编译错误，仅 2 个为运行时或数值错误。
   - 常见错误包括：API 使用不当（尤其 Triton）、host/device 混淆、datatype/shape 不匹配。

2. **LLM 难以有效利用硬件 intrinsic**：
   - 尽管提供了 Blackwell 架构文档，LLM 仍未能正确使用 `mma` 或 `tcgen05` 指令。
   - 表明当前 LLM 对底层硬件特性的理解有限，依赖训练数据中的常见模式。

3. **语言选择存在 trade-off**：
   - **Triton**：抽象程度高，LLM 更易写出正确代码，且编译器可自动优化。
   - **CUDA**：表达能力强，潜在性能上限更高，但实现复杂，LLM 容易出错。
   - 推荐策略：训练阶段限制库调用以促进学习；推理阶段开放库访问以最大化性能。

4. **LLM 学会调用库而非真正编程**：
   - 如 GEMM 任务中，Gemini 和 o3 学会调用 cuBLAS 实现高性能。
   - 虽然实用，但也暴露了 LLM “走捷径”的倾向，可能抑制深度技能习得。

5. **动态替换机制高效可靠**：
   - `flashinfer_bench.apply()` 实现了近乎零成本的内核热替换。
   - kernel 性能增益可直接转化为 end-to-end 延迟降低，验证了闭环系统的有效性。

### 局限性
- 当前未覆盖 multi-GPU 或通信密集型内核（如 AllReduce）。
- 支持的语言和硬件种类有限（目前主要支持 CUDA/Triton + NVIDIA）。
- 对 reward hacking 的防御仍有改进空间（如隐藏 workload 分布）。
- 多数 LLM 仍无法稳定生成高度优化的 CUDA 内核。

### 未来工作方向
- 扩展 FlashInfer Trace 到更多模型、设备和编程语言（如 CUTLASS、CuTe）。
- 引入强化学习或模仿学习，鼓励 LLM 探索硬件特定优化策略。
- 构建更强大的 kernel agent，结合搜索与生成方法。
- 增强验证机制，防范隐蔽的数值偏差或安全漏洞。
- 探索 multi-agent 协作生成复杂 fused kernels。

--- 

> ✅ **总结一句话**：  
> FlashInfer-Bench 构建了一个从 AI 生成 → 真实评估 → 动态部署的完整 virtuous cycle，首次实现了 LLM 自动生成 GPU 内核在生产级 LLM 系统中的闭环落地，推动了 AI for Systems 的实用化进程。

</details>

---

### 4. [DA-DPO: Cost-efficient Difficulty-aware Preference Optimization for Reducing MLLM Hallucinations](https://arxiv.org/abs/2601.00623)

**Authors**: Longtian Qiu, Shan Ning, Chuyu Zhang, Jiaxuan Sun, Xuming He  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.00623v1  

#### Abstract
Direct Preference Optimization (DPO) has shown strong potential for mitigating hallucinations in Multimodal Large Language Models (MLLMs). However, existing multimodal DPO approaches often suffer from overfitting due to the difficulty imbalance in preference data. Our analysis shows that MLLMs tend ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DA-DPO: Cost-efficient Difficulty-aware Preference Optimization for Reducing MLLM Hallucinations

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Multimodal Large Language Models (MLLMs)** 在视觉-语言任务中表现出色，但普遍存在**幻觉（hallucination）**问题，即生成的内容与图像输入不一致。虽然 **Direct Preference Optimization (DPO)** 被广泛用于缓解幻觉，但其在训练过程中容易对“简单样本”过拟合，而忽视“困难样本”，导致模型泛化能力下降。

作者通过实证分析发现，这种**难度不平衡（difficulty imbalance）**是导致 DPO 性能瓶颈的关键原因：模型快速适应易于区分的偏好对，却无法从细微差异的困难样本中有效学习。

---

### 提出的新方法：DA-DPO
为解决上述问题，作者提出 **Difficulty-Aware Direct Preference Optimization (DA-DPO)**，一个无需额外训练、成本高效的框架，包含两个核心组件：

1. **Difficulty Estimation（难度估计）**
   - 利用预训练的 **contrastive VLMs（如 CLIP）** 和 **generative VLMs（如 LLaVA）** 从不同角度评估每个偏好对的难度。
   - CLIP 衡量图像-文本相关性（image relevance），适合判断 caption 类任务；
   - MLLM 衡量逻辑连贯性（question relevance），适合 VQA 类任务。
   - 采用 **distribution-aware voting strategy** 自适应融合两者输出，生成鲁棒的难度分数，无需额外训练。

2. **Difficulty-aware Training（难度感知训练）**
   - 根据估计的难度动态调整每个样本在 DPO 中的优化权重。
   - 对**困难样本增强学习信号**，对**简单样本降权**，防止过拟合。
   - 机制上通过调节 DPO 目标中的 β 参数实现，提升模型对细粒度差异的学习能力。

---

### 相比现有方法的优势
| 特性 | DA-DPO | 传统 DPO / 其他方法 |
|------|--------|------------------|
| 是否需要额外训练 | ❌ 否（利用已有 VLMs） | ✅ 是（如训练 Reward Model） |
| 是否引入新数据 | ❌ 否 | ✅ 部分方法依赖人工标注或强模型打标 |
| 计算效率 | ⭐ 极高（见 Table 10） | ⚠️ 高（尤其 RLHF/PPO） |
| 抗过拟合能力 | ✅ 强（显式平衡难易样本） | ❌ 弱（易偏向简单样本） |
| 通用性 | ✅ 可适配多种 MLLM 架构 | ✅ 多数兼容 |

> ✅ **核心优势**：在不增加训练开销的前提下，显著提升幻觉抑制效果和综合性能。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **主训练数据**：
  - **BPO dataset**：包含 180K 自动生成的多模态偏好对，负样本由 Image-Weakened Prompting 和 LLM Error Injection 生成。
- **辅助验证数据**：
  - **VLFeedback**：基于多个 VLM 输出并由 GPT-4V 过滤的自动构建数据集（80K → 选 10K）。
  - **LLaVA-RLHF**：人类标注的 10K 偏好数据集，用于测试泛化性。

---

### 实验设置
- **模型架构**：
  - LLaVA-v1.5-7B / 13B
  - LLaVA-OneVision-7B
  - Qwen2.5-VL-3B（用于扩展实验）
- **训练细节**：
  - 所有模型训练 1 epoch。
  - 使用 LoRA（rank=32, alpha=256）进行高效微调。
  - 学习率：2e-6（LLaVA-v1.5），5e-7（LLaVA-OneVision）。
  - β 设置为 0.2（DA-DPO 最优值，见 Table 8）。

---

### 评估指标
#### 幻觉评估基准（Hallucination Benchmarks）
| 基准 | 描述 |
|------|------|
| **AMBER** | 多维度评估生成与判别任务，包含 `Cs`（忠实度）、`Hal.`（幻觉率）、`Cog.`（连贯性）等子指标 |
| **MMHalBench** | 包含 8 种问题类型的 VQA 幻觉检测，使用 GPT-4 评分 |
| **Object HalBench** | 检测对象是否存在性幻觉 |
| **POPE** | 基于投票的幻觉评测，报告平均 F1 分数 |

#### 综合能力基准（Comprehensive Benchmarks）
| 基准 | 描述 |
|------|------|
| **LLaVA-Bench** | 60 个真实世界视觉指令任务，GPT-4 打分 |
| **SeedBench** | 14K 多选题 VQA 数据集，评估综合理解能力 |
| **MME** | 测试感知与认知能力（yes/no 问答） |
| **GQA** | 开放式视觉推理与组合问答 |

---

### 基线方法对比
- **Standard DPO**：直接应用原始 DPO 目标函数。
- **HA-DPO**, **CLIP-DPO**, **mDPO**：其他改进型 DPO 方法。
- **Reference Only**：仅监督微调（SFT）模型，无偏好优化。
- **Filtering Baselines**：移除 10%/25%/50% 易样本后训练 DPO，作为硬过滤对照。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 LLaVA-v1.5-7B 为例）

| 方法 | AMBER `Hal.` ↓ | POPE F1 ↑ | LLaVA-Bench Score ↑ | SeedBench Score ↑ |
|------|----------------|-----------|---------------------|--------------------|
| DPO  | 35.7           | 84.3      | 70.5                | 45.3               |
| **DA-DPO** | **28.0**       | **85.9**  | **75.4**            | **64.8**           |

> ✅ **幻觉减少**：AMBER 幻觉率从 35.7 → **28.0**（降幅 >21%）  
> ✅ **综合能力保持甚至提升**：LLaVA-Bench 提升近 5 分，SeedBench 提升超 19 分

---

### 与其他方法对比（Table 1）
- DA-DPO 在几乎所有幻觉和综合指标上均优于标准 DPO 和多数 baselines。
- 尤其在 **Object Hallucination** 上表现突出，说明其对细粒度事实错误有更强抑制力。
- 相比 **mDPO**（关注语言偏好偏置），DA-DPO 更专注于样本难度平衡，二者正交可结合。

---

### 消融实验结果

#### （1）难度估计来源消融（Table 2）
| 方法 | AMBER `Hal.` ↓ | SeedBench ↑ |
|------|----------------|-------------|
| DPO（baseline） | 35.7 | 57.8 |
| + CLIP only | 31.9 | 63.8 |
| + MLLM only | 29.9 | 64.3 |
| **+ CLIP + MLLM（DA-DPO）** | **28.0** | **64.8** |

> ✅ **双视角融合更优**：证明 contrastive 与 generative VLMs 提供互补信息。

#### （2）VLM 模型选择消融（Table 4）
- 使用不同规模的 CLIP（ViTL vs EVA-CLIP 8B）或 MLLM（LLaVA vs OV）时，性能变化较小。
- 归因于 **Gaussian normalization** 使系统对绝对得分不敏感，只保留排序关系，增强了鲁棒性。

#### （3）直接过滤 vs 动态加权（Table 5）
| 方法 | 移除比例 | POPE F1 | ObjHal Cs↓ |
|------|----------|---------|------------|
| DPO | 0% | 84.3 | 43.3 |
| Filter 10% | 10% | 84.9 | 39.0 |
| Filter 25% | 25% | 84.3 | 38.3 |
| Filter 50% | 50% | 85.5 | 42.7 |
| **DA-DPO** | — | **85.9** | **37.7** |

> ✅ **软加权优于硬过滤**：DA-DPO 不丢弃任何数据，在保留多样性的同时强调困难样本，性能更稳定且全面领先。

#### （4）归一化策略消融（Table 11）
| 方法 | Hal. ↓ | F1 ↑ |
|------|--------|------|
| DPO | 35.7 | 83.9 |
| Ranked-based | 31.3 | 85.7 |
| Length-controlled | 29.3 | 85.9 |
| **Gaussian Normalization** | **28.0** | **85.6** |

> ✅ Gaussian 归一化效果最佳，说明保留原始分布特性有助于准确建模难度。

---

## 4. 关键结论和发现

### 主要发现
1. **多模态 DPO 存在严重过拟合问题**：模型倾向于过度优化“明显正确”的简单样本，忽略需要精细推理的困难样本。
2. **样本难度可通过预训练 VLMs 有效估计**：CLIP 和 MLLM 分别捕捉图像相关性和语义逻辑，二者互补。
3. **难度感知训练显著提升性能**：DA-DPO 在降低幻觉的同时，还能提升或维持综合能力，解决了“对齐税”问题。
4. **无需新训练即可实现高效优化**：整个框架完全基于已有模型打分，计算成本极低（见 Table 10）。

---

### 方法的局限性
- **依赖预训练 VLM 的可靠性**：若目标领域与 CLIP/MLLM 预训练分布差异过大（如医学图像），难度估计可能失效。
- **未考虑任务类别自适应加权**：当前 voting 权重是全局固定的，未来可探索 per-category 或动态调整机制。
- **假设难度可被现有代理模型刻画**：某些极端模糊或主观样本可能难以被客观量化。

---

### 未来工作方向
- 探索 **domain-adaptive difficulty estimator**，提升跨域泛化能力。
- 引入 **self-improving mechanism**，让模型在训练中迭代更新难度评估器。
- 结合 **active learning**，优先采集高难度样本进行标注。
- 将 DA-DPO 思路推广至 **video-language models** 或 **robotics** 等更复杂场景。

---

> 🔚 **总结**：DA-DPO 提出了一种新颖且实用的视角——将“样本难度”纳入偏好优化过程。它不仅有效缓解了 MLLM 的幻觉问题，还展示了如何低成本地利用已有模型知识来指导训练，为后续研究提供了重要启发。

</details>

---

### 5. [InfoSynth: Information-Guided Benchmark Synthesis for LLMs](https://arxiv.org/abs/2601.00575)

**Authors**: Ishir Garg, Neel Kolhe, Xuandong Zhao, Dawn Song  
**Category**: cs.CL  
**Published**: 2026-01-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.00575v1  

#### Abstract
Large language models (LLMs) have demonstrated significant advancements in reasoning and code generation. However, efficiently creating new benchmarks to evaluate these capabilities remains a challenge. Traditional benchmark creation relies on manual human effort, a process that is both expensive an...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# InfoSynth: Information-Guided Benchmark Synthesis for LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在代码生成和推理任务上表现出色，但其评估面临两大挑战：
- **基准污染（Benchmark Contamination）**：许多现有评测集已被用于训练数据，导致模型表现被高估。
- **人工构建成本高**：高质量基准的创建依赖大量人力，耗时且昂贵。
- **缺乏多样性与新颖性**：现有方法生成的合成问题往往与种子数据高度相似，易导致过拟合。

因此，亟需一种能够高效、自动地生成**新颖（novel）、多样化（diverse）且可验证正确性**的Python编程问题的方法。

---

### 提出的新方法与思路
本文提出 **InfoSynth**，一个基于信息论指导的端到端框架，用于自动生成和评估LLM推理能力基准。其核心创新包括：

#### （1）信息论驱动的基准质量度量
首次引入**KL散度（KL-Divergence）** 和 **香农熵（Shannon Entropy）** 来量化基准的：
- **Novelty（新颖性）**：通过 $ D_{KL}(q \| p) $ 衡量新数据集 $ Y \sim q $ 相对于种子数据集 $ X \sim p $ 的分布差异。
- **Diversity（多样性）**：通过微分熵衡量数据集中问题之间的分散程度，越高表示覆盖更广的任务空间。

> ✅ 优势：无需运行昂贵的模型测试即可快速估算基准质量，显著降低评估开销。

#### （2）遗传算法 + 迭代反馈的合成管道
构建了一个结合**遗传操作**与**代码执行反馈**的闭环生成流程：
- **Mutation（变异）**：对原问题进行“变难”、“变易”或“保持难度”的改写，提升多样性。
- **Crossover（交叉）**：融合两个不同问题的核心概念，创造新颖复合题。
- **k-Farthest Neighbor Selection**：保留语义上最远离已有问题的新问题，主动增强新颖性和多样性。
- **Iterative Code Feedback**：通过多次执行解决方案并反馈错误日志，迭代修正代码与测试用例，确保生成问题的**可执行性与正确性**。
- **MinHash + LSH 去重**：去除文本层面重复的问题。

#### （3）可控性设计
支持调节生成问题的**难度、新颖性与多样性**之间的权衡，例如通过控制是否启用 `k-farthest` 过滤来调整输出特性。

---

### 相比现有方法的优势
| 特性 | InfoSynth | 其他方法（如 GeneticInstruct, KodCode） |
|------|---------|-----------------------------|
| 正确性保障 | ✅ 执行环境验证 + 多轮反馈 | ❌ 依赖LLM自我判断，易出错 |
| 新颖性度量 | ✅ 信息论指标（KL散度） | ❌ 依赖模型性能或人工评估 |
| 多样性控制 | ✅ 显式过滤机制 | ⚠️ 有限 |
| 难度调控 | ✅ 支持多级变异（easy/medium/hard） | ⚠️ 通常单一模式 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **种子数据集（Seed Datasets）**：
  - **MBPP**（374个简单Python编程题）
  - **LeetCode**（来自 Xia et al. [38] 的精选子集）
- **对比数据集**：
  - **APPS**, **HumanEval**, **Codeforces**, **MBPP**, **LeetCode 子标签集合**（如 "String", "Graph", "Hash Table"）

所有问题均使用 `all-mpnet-base-v2` 编码为向量，并通过 **UMAP** 降维至低维空间以计算KL散度与熵。

---

### 实验设置与评估指标

#### 生成设置
- 使用 **GPT-4o** 作为生成器。
- 生成六组数据：
  - `MBPP-New`, `MBPP-Guided`（带k-farthest过滤）
  - `MBPP-Hard`, `MBPP-Hard-Guided`
  - `Leetcode-New`, `Leetcode-Guided`

#### 评估指标
| 类别 | 指标 |
|------|------|
| **质量指标** | % Human Correct（人工验证正确率）、Test Coverage（测试覆盖率）、Avg. Tests（平均每题测试数） |
| **新颖性** | KL-Divergence 相对于种子数据集 |
| **多样性** | Differential Entropy |
| **难度** | SOTA模型在该数据集上的Pass@1准确率（越低表示越难） |
| **鲁棒性** | 解决方案通过所有测试的比例 |

#### 基线方法对比
- **GeneticInstruct**：基于遗传算法生成指令。
- **KodCode**：强调挑战性和可验证性的合成数据集。
- 所有方法均使用LeetCode作为种子，比较其相对于LeetCode的**novelty & diversity**。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| Dataset | % Human Correct | Test Coverage | Avg. Tests |
|--------|------------------|---------------|------------|
| MBPP-New | 97% | 99% | 8.30 |
| MBPP-Guided | 98% | 99% | 8.86 |
| Leetcode-New | 98% | 99% | 8.22 |
| Leetcode-Guided | 97% | 100% | 8.66 |

✅ **结论**：InfoSynth 在 **97%以上**的情况下能生成**完全正确且测试充分**的问题，具备高度可靠性。

---

### 与基线方法对比（Figure 6）

- **多样性（Diversity）**：InfoSynth 与 GeneticInstruct 接近，略高于 KodCode。
- **新颖性（Novelty）**：**InfoSynth 显著优于其他方法**，说明其更能脱离种子数据分布生成真正新颖的问题。

> 🔍 注：此比较基于同一种子（LeetCode），突显 InfoSynth 更强的“跳出原有分布”能力。

---

### 消融实验结果

#### （1）k-Farthest Neighbor Filtering 的影响（Figure 4c, 4d）
- 启用该模块 → **显著提高 novelty 和 diversity**
- 代价是生成的问题**平均难度下降**（因复杂组合题更难通过验证）
- ✅ 证明 InfoSynth 可控地调节 novelty-diversity-difficulty 三角关系

#### （2）多级 Mutation 的效果（Table 2）
- `MBPP-Hard` 相比原始MBPP，在多个SOTA模型上性能下降 **8%-15%**
- 表明 hard mutation 成功提升了问题难度
- 但也带来**多样性和新颖性降低**（集中在少数难点主题）

#### （3）Iterative Code Feedback 的作用（Section 5.3）
- 经过 **5轮反馈**，passing solution-test 对比例提升约 **20%**
- 错误率（error/fail）持续下降，显示反馈有效修复语法与逻辑错误
- **3轮为性价比最优**，后续增益边际递减

#### （4）Postprocessing 的价值（Section 5.5）
- 解决边缘情况模糊问题（如空输入返回值）
- 导致 SOTA 模型准确率提升 **5–15%**
- 人工核查确认：**100% post-processed 问题表述清晰无歧义**

---

## 4. 关键结论和发现

### 主要发现
1. **信息论指标可行**：KL散度与熵可以低成本、有效地衡量基准的**新颖性与多样性**，无需依赖模型打分。
2. **InfoSynth 高效可靠**：可在无人工干预下生成 **97%以上正确率**的Python编程题，且测试完备。
3. **生成质量更高**：相比种子数据，InfoSynth 生成的数据集具有更高的**novelty 和 diversity**。
4. **可控性强**：可通过开关 `k-farthest` 或选择 mutation 类型来调节输出特性的平衡。
5. **优于现有方法**：在 novelty 上超越 GeneticInstruct 与 KodCode，同时保证 solution 正确性。

---

### 方法的局限性
1. **依赖强大LLM**：整个流程基于 GPT-4o，小模型可能无法稳定生成高质量解与测试。
2. **长周期任务受限**：涉及多重约束或深层数学计算的问题（如 Delannoy 数）容易失败。
3. **仍可能存在隐性污染**：虽然未直接复用题目，但若LLM记忆了类似结构，仍可能间接污染。
4. **仅限可执行任务**：目前聚焦于Python编码问题，难以扩展到纯数学推理或开放生成类任务。

---

### 未来工作方向
- 将 InfoSynth 扩展至更多领域（如数学证明、自然语言理解）。
- 结合 contamination detection 工具，进一步确保基准“干净”。
- 探索基于强化学习的自动化难度调控策略。
- 构建大规模、动态更新的合成基准平台，实现 LLM 能力的持续评估。

---

> 📌 **一句话总结**：  
> InfoSynth 提供了一种**可扩展、自验证、信息论驱动**的基准合成范式，为构建下一代抗污染、高多样性的LLM评测体系提供了重要工具。

</details>

---

### 6. [Sequential Reservoir Computing for Efficient High-Dimensional Spatiotemporal Forecasting](https://arxiv.org/abs/2601.00172)

**Authors**: Ata Akbari Asanjan, Filip Wudarski, Daniel O'Connor, Shaun Geaney, Elena Strbac, P. Aaron Lott, Davide Venturelli  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.00172v1  

#### Abstract
Forecasting high-dimensional spatiotemporal systems remains computationally challenging for recurrent neural networks (RNNs) and long short-term memory (LSTM) models due to gradient-based training and memory bottlenecks. Reservoir Computing (RC) mitigates these challenges by replacing backpropagatio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Sequential Reservoir Computing for Efficient High-Dimensional Spatiotemporal Forecasting*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Reservoir Computing (RC)** 虽然避免了基于梯度的训练（如 RNN 和 LSTM 中的 BPTT），具有高效训练的优点，但在处理**高维时空数据**时仍面临显著瓶颈：
- **内存需求呈指数增长**，难以扩展到大规模系统；
- 标准 RC 架构在输入维度增加时计算和存储开销急剧上升。

此外，尽管深度 RC（Deep RC）通过堆叠多层 reservoir 展现出更强的表达能力，但尚未被系统研究用于高维 spatiotemporal 预测任务。

### 🚀 提出的新方法：Sequential Reservoir Computing (Sequential RC)
本文提出一种新型架构——**Sequential Reservoir Computing (Sequential RC)**，其核心思想是：
- 将一个大型 reservoir 分解为一系列**较小且相互连接的小型 reservoirs**，按顺序级联；
- 每个 reservoir 的输出被拼接后送入最终的线性读出层（readout）进行预测；
- 所有 reservoir 层参数固定（非训练），仅训练 readout 层（采用岭回归优化）。

该设计保留了标准 RC 的优点（无需反向传播、凸优化读出层），同时显著降低内存与计算负担。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **可扩展性** | 显著提升对高维数据的适应能力，突破传统 RC 内存瓶颈 |
| **效率** | 训练成本比 LSTM 低三个数量级；推理 FLOPS 大幅下降 |
| **性能** | 在预测精度、有效预测时间（VPT）、SSIM、RMSE 等指标上全面优于 RNN、LSTM 和标准 RC |
| **参数量控制** | 可控的固定参数规模，适合资源受限场景 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集

| 数据集 | 类型 | 维度 | 描述 |
|--------|------|-------|------|
| **Lorenz63** | 低维混沌系统 | 3D 时间序列 | 经典的三变量混沌微分方程，用于测试长期动态建模能力 |
| **Vorticity (2D Navier-Stokes)** | 高维物理模拟 | $64 \times 64$ 网格 | 不可压缩流体涡度演化，含复杂空间结构 |
| **Shallow Water Equations (SWE)** | 高维物理模拟 | $64 \times 64$ 网格 | 地表流动模型，模拟波传播与边界反射 |

所有高维数据均以二维场形式输入，代表典型的科学计算中的 spatiotemporal 动力学问题。

### ⚙️ 实验设置

#### 模型配置
| 模型 | 结构说明 |
|------|----------|
| **RNN / LSTM** | Vanilla RNN 和 LSTM，单隐藏层（256 或 512），全连接输入层，many-to-one 架构 |
| **Standard RC** | 单一大型 reservoir（大小 256 或 512） |
| **Sequential RC** | 8 个小型 reservoir 级联（每层 32 或 64），总参数远小于标准 RC |

#### 超参数（见 Appendix B）
- Spectral Radius: 1.1
- Leak Rate: 0.7
- Sparsity: 0.0
- Historical Window: 49
- Optimizer (for RNN/LSTM): RMSProp, lr = 1e-4
- Precision: Float64

#### 训练样本长度
- Lorenz63: 2,000 / 5,000 / 10,000 个时间步
- Vorticity & SWE: 2,000 / 5,000 个时间步

### 📈 评估指标

| 指标 | 用途 |
|------|------|
| **Valid Prediction Time (VPT)** | 衡量预测轨迹偏离真实值的时间点（阈值 RMSE ≥ 0.3） |
| **SSIM (Structural Similarity Index)** | 评价图像/场结构相似性，更贴近人类感知 |
| **PSNR (Peak Signal-to-Noise Ratio)** | 图像质量度量，越高越好 |
| **RMSE (Root Mean Square Error)** | 平均误差，越低越好 |
| **Return Map (z-maxima plotting)** | 可视化混沌系统的长期稳定性 |
| **FLOPS, Memory Usage, Training Time** | 评估计算效率与资源消耗 |

### 🔁 基线方法对比
- **RNN (Elman-type)**
- **LSTM**
- **Standard Reservoir Computing (RC)**

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### ✅ 低维结果（Lorenz63）

| 模型 | VPT (2k samples) | Trainable Params | Fixed Params | FLOPS (MFLOPS) | GPU Mem (MB) | Train Time (s) |
|------|------------------|------------------|---------------|----------------|--------------|----------------|
| RNN | 0.10 | 133K | 0 | 6.59 | 1.02 | 508 |
| LSTM | 1.73 | 528K | 0 | 26.25 | 4.29 | 576 |
| RC | 7.19 | 780 | 66K | 0.067 | 0.0138 | 0.4 |
| **Seq. RC** | **8.65** | **780** | **8.3K** | **0.0091** | **0.0069** | **1.4** |

> ✅ **Sequential RC 的 VPT 比标准 RC 提升约 20%**  
> ✅ 固定参数仅为 RC 的 ~12.5%，FLOPS 下降 7.4 倍，内存减少一半以上  
> ✅ 尽管训练时间略长于 RC（因串行计算延迟），但仍远快于 RNN/LSTM

#### ✅ 高维结果（Vorticity & SWE）

| 模型 | SSIM / PSNR / RMSE 趋势 | VPT / Forecast Horizon |
|------|----------------------------|-------------------------|
| RNN | 快速发散，早期失效 | < t+10 |
| LSTM | 约在 t+30 开始明显偏差 | ~t+30–40 |
| RC | 初期良好，t+50 后细节丢失 | ~t+50 |
| **Sequential RC** | **保持高质量至 t+94**，SSIM > 0.6 更久 | **最长有效预测** |

> 在 Vorticity 上，**Sequential RC 将有效预测步数延长至 94**，而 RC 在 ~50 步后性能骤降。

#### 💡 性能提升总结
- **有效预测时间（VPT）提升：15–25%**
- **误差指标改善：RMSE 降低 20–30%，SSIM 显著更高**
- **训练成本降低：达三个数量级（相比 LSTM）**
- **内存占用减少：最高达 470 倍（vs LSTM）**

### 🔍 消融实验分析（隐含在文中）
虽然未明确命名“消融实验”，但以下分析体现了关键组件的影响：
- **历史窗口长度影响**（Appendix A）：验证了 50 步左右为最优上下文长度，过长反而增加 GPU 内存压力；
- **reservoir 大小与层数权衡**：使用多个小 reservoir 替代单一大会更高效；
- **spectral radius 和 leak rate 设置**：选择 1.1 和 0.7 是经过调优的最佳组合；
- **不同训练样本量的影响**：即使在小样本下（2k），Sequential RC 依然表现稳健，显示强泛化能力。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Sequential RC 显著提升了传统 RC 的可扩展性**，解决了高维数据下的内存瓶颈问题；
2. 在低维和高维系统中，**Sequential RC 均实现了更长的有效预测时间（VPT）和更高的预测保真度（SSIM/PSNR）**；
3. **计算效率极高**：训练时间极短（毫秒级），FLOPS 和内存使用远低于 RNN/LSTM；
4. **不增加可训练参数的前提下实现性能飞跃**，所有改进来自架构优化而非参数膨胀；
5. 对混沌系统（如 Lorenz63）和物理模拟系统（如 Vorticity、SWE）均有出色表现，具备广泛适用性。

### ⚠️ 方法的局限性

1. **sequential 计算引入一定延迟**：由于 reservoir 层需依次运行，推理速度略慢于并行结构（尽管总体仍很快）；
2. **超参数敏感性**：spectral radius、leak rate 等需仔细调整才能发挥最佳性能；
3. **当前未融合物理先验知识**：纯数据驱动，未来可结合物理约束进一步增强鲁棒性；
4. **尚未在真实世界观测数据上验证**：目前实验基于仿真数据，实际应用需考虑噪声与缺失等问题。

### 🔮 未来工作方向（原文 Conclusion & Acknowledgments）

1. 探索 **adaptive 或 physics-informed reservoir 连接方式**，将领域知识嵌入 reservoir 结构；
2. 扩展至 **turbulent flow 和 multi-modal geophysical datasets**；
3. 开发 **neuromorphic 或 FPGA 硬件实现方案**，推动边缘部署；
4. 探索 **quantum-hybrid implementations**，利用量子态作为 reservoir（已有初步探索 [37–39]）；
5. 应用于实时天气预报、海洋动力学、金融时序等高维实时预测场景。

---

## 总结

> **Sequential Reservoir Computing** 是一种高效、可扩展、高性能的高维时空预测框架。它通过将大 reservoir 分解为级联的小 reservoir，在几乎不增加可训练参数的情况下，大幅降低了内存和计算开销，同时显著提升了预测稳定性和精度。该方法为科学机器学习中**实时、节能、高精度的动力系统建模**提供了切实可行的技术路径。

</details>

---

### 7. [Knowledge Distillation for Temporal Knowledge Graph Reasoning with Large Language Models](https://arxiv.org/abs/2601.00202)

**Authors**: Wang Xing, Wei Song, Siyu Lin, Chen Wu, Zhesi Li, Man Wang  
**Category**: cs.CL  
**Published**: 2026-01-05  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.00202v1  

#### Abstract
Reasoning over temporal knowledge graphs (TKGs) is fundamental to improving the efficiency and reliability of intelligent decision-making systems and has become a key technological foundation for future artificial intelligence applications. Despite recent progress, existing TKG reasoning models typi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Knowledge Distillation for Temporal Knowledge Graph Reasoning with Large Language Models*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现有的 **Temporal Knowledge Graph (TKG)** 推理模型通常依赖大规模参数和高计算开销，导致难以在资源受限设备上部署。同时，传统的 **Knowledge Distillation** 方法多针对静态知识图谱设计，未能有效捕捉 TKG 中的时间动态特性，造成推理性能下降。

### 🚀 提出的新方法
本文提出一种**面向时序知识图谱推理的新型蒸馏框架**，其核心创新在于：
- **引入 Large Language Models (LLMs)** 作为“教师”模型之一，利用其强大的通用知识和时序推理能力指导轻量级学生模型训练。
- 设计了一个**双阶段蒸馏策略**：第一阶段对齐传统 TKG 教师模型输出；第二阶段利用 LLM 提供更丰富的语义与时序信号进行精细化调整。
- 将公共大规模语言知识与任务特定的时序信息融合，增强学生模型建模时间演化的能力。

### 🔍 相比现有方法的优势
- 能够有效迁移**结构化知识 + 时间动态模式**，克服传统蒸馏方法忽略时间依赖性的缺陷。
- 显著提升轻量化模型的推理精度，在保持低计算成本的同时实现高性能。
- 支持在边缘设备等低功耗分布式平台上高效部署。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验基于两个公开的 TKG benchmark 数据集：
- **YAGO11k**：源自 Max Planck Institute 构建的知识库，包含丰富的时间标注事实。
- **WIKIdata12k**：从 Wikidata 和 Wikimedia Commons 构建的多语言时序知识图谱。

| Dataset | Entities | Relations | Train | Valid | Test |
|--------|----------|-----------|-------|-------|------|
| YAGO   | 10,623   | 10        | 161,540 | 19,523 | 20,026 |
| WIKI   | 12,544   | 24        | 539,286 | 67,538 | 63,110 |

### ⚙️ 实验设置
- **教师模型**：TTransE 和 TADistMult（代表性 TKG embedding 模型）
- **学生模型**：轻量版本，embedding 维度设为 25（教师为 400），模拟资源受限场景
- **LLM 教师**：预训练并在 TKG 数据上微调的大语言模型，用于生成软标签和语义表示
- **优化器**：Adagrad，batch size = 1024，epoch = 10,000，temperature = 7
- **实现平台**：基于扩展版 OpenKE 框架，PyTorch + RTX 3090 Ti

### 📊 评估指标
标准链接预测指标：
- **Mean Rank (MR)**：越小越好
- **Mean Reciprocal Rank (MRR)**：越大越好
- **Hits@1 / Hits@3 / Hits@10**：越高越好（Top-k 准确率）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 3）

#### 在 YAGO 上的表现（以 TTransE 为例）：
| Method | MRR | MR ↓ | Hits@1 | Hits@3 | Hits@10 |
|--------|-----|------|--------|--------|---------|
| BKD    | 7.65 | 1410.12 | 3.50 | 7.83 | 15.61 |
| **Ours** | **7.69** | **1193.15** | **3.61** | **7.89** | **16.57** |

→ 在 MR 上降低约 **15.4%**，其余指标均有提升。

#### 在 WIKI 上的表现（以 TADistMult 为例）：
| Method | MRR | MR ↓ | Hits@1 | Hits@3 | Hits@10 |
|--------|-----|------|--------|--------|---------|
| BKD    | 45.89 | 3150.11 | 42.46 | 48.87 | 51.18 |
| **Ours** | **46.03** | **3142.85** | **42.50** | **49.16** | **51.14** |

→ 多数指标优于基线，尤其 **Hits@3 提升显著（+0.29%）**

### 🔁 与基线方法对比
相比以下经典蒸馏方法：
- **BKD**（标准 KL 散度蒸馏）
- **FitNet**（中间层监督）
- **RKD**（关系知识蒸馏）

本方法在大多数配置下表现最优，特别是在 **Hits@1 和 Hits@3** 上优势明显，说明能更好将正确实体排在前列。

| 对比项 | 性能优势 |
|--------|----------|
| vs. BKD | 平均提升 0.5–16.7%，尤其在 MR 和 Hits@10 上 |
| vs. FitNet/RKD | 更稳定，平均提升 ~2.77%（YAGO）、~3.28%（WIKI） |

### 🔍 消融实验分析（Ablation Study）
通过对比仅使用 BKD 的基线，验证 LLM 蒸馏的有效性：
- 在 YAGO 上，加入 LLM 后 MRR 提升 **0.5%**，Hits@10 提升 **6.1%**
- 在 WIKI 上，多个指标持续改善，证明 LLM 注入的知识有助于提升学生模型泛化能力
- 特别是在稀疏或未见时间区间中，LLM 的先验知识帮助缓解过拟合

> 💡 结论：**LLM 提供的语义与时间上下文显著增强了蒸馏效果**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **LLM 可作为高效的“教师”模型**，不仅能编码实体/关系语义，还能捕捉隐含的时间演化规律。
2. 所提蒸馏框架能在**大幅压缩模型规模**（维度从 400 → 25）的前提下，**维持甚至超越原始大模型的推理性能**。
3. 该方法实现了**准确性、效率与可部署性之间的良好平衡**，适用于边缘计算和实时推理场景。

### ⚠️ 局限性
1. **LLM 本身带来额外训练开销**：虽然推理轻量，但 LLM 微调过程仍需大量算力。
2. 当前框架聚焦于单跳 link prediction，尚未支持 multi-hop reasoning 或逻辑规则归纳。
3. 学生模型容量极小时，性能增益可能受限，存在“知识瓶颈”。

### 🔮 未来工作方向
- 扩展至更复杂的推理任务，如 temporal rule learning、multi-step forecasting
- 探索更高效的 LLM 调用机制（如 prompt tuning、LoRA）
- 研究跨领域迁移能力，提升在低资源 TKG 上的泛化表现

---

## 总结
本文开创性地将 **Large Language Models 引入 TKG 知识蒸馏流程**，构建了一种兼顾**时间动态性、语义丰富性和部署效率**的新型推理框架。实验证明其在多个 benchmark 上显著优于传统蒸馏方法，为轻量化时序知识推理提供了可行路径，具有重要的应用前景。

</details>

---

### 8. [Adaptive Causal Coordination Detection for Social Media: A Memory-Guided Framework with Semi-Supervised Learning](https://arxiv.org/abs/2601.00400)

**Authors**: Weng Ding, Yi Han, Mu-Jiang-Shan Wang  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00400v1  

#### Abstract
Detecting coordinated inauthentic behavior on social media remains a critical and persistent challenge, as most existing approaches rely on superficial correlation analysis, employ static parameter settings, and demand extensive and labor-intensive manual annotation. To address these limitations sys...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
当前社交媒体上的**协调性虚假行为检测**（coordinated inauthentic behavior）面临三大挑战：
- 多数方法依赖表面的**相关性分析**（correlation-based），难以区分真实因果关系与伪相关；
- 模型参数为静态配置，缺乏对多样化、动态变化的协调策略的适应能力；
- 高度依赖人工标注，导致部署成本高、效率低。

### 提出的新方法：ACCD框架
本文提出 **Adaptive Causal Coordination Detection (ACCD)** 框架，一个三阶段、记忆引导的自适应系统，其核心创新如下：

#### （1）自适应因果协调检测器（Adaptive Causal Coordination Detector）
- 引入**自适应Convergent Cross Mapping (CCM)** 技术，通过历史性能动态选择最优嵌入维度 $E$ 和时间延迟 $T$。
- 利用**长期记忆模块**（long-term memory）存储不同上下文下的参数表现，实现跨场景的知识迁移与复用。

#### （2）半监督用户分类器（Semi-Supervised User Classifier）
- 结合**主动学习**（active learning）与不确定性采样，优先标注模型最不确定的样本，显著减少人工干预。
- 采用**课程学习**（curriculum learning）机制，从易到难逐步训练，并利用高置信度预测生成**伪标签**（pseudo-labels）以进一步降低标注需求。

#### （3）自动化因果验证器（Adaptive Causal Validator）
- 基于历史检测经验自动调整阈值和模型选择，无需专家手动调参。
- 使用多目标评分函数综合评估多种因果推断模型（如Generalized Synthetic Control, CausalForestDML, GANITE），并通过**反事实检验**（refutation tests）提升结果可信度。

### 相比现有方法的优势
| 维度 | ACCD优势 |
|------|---------|
| **准确性** | 显著提升F1-score，减少误报与漏报 |
| **效率** | 通过分层聚类优化计算复杂度，实现2.8×加速 |
| **自动化程度** | 减少68%以上的人工标注，支持端到端运行 |
| **可扩展性** | 自适应机制适用于多样化的社交平台与协调模式 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|-------|------|
| **Twitter IRA dataset** | 包含2,832个已知参与影响操作的账号发布的290万条推文，用于检测协调攻击 |
| **Reddit coordination traces** | 来自Pushshift数据集的真实论坛协调行为痕迹 |
| **TwiBot-20 benchmark** | 广泛使用的Twitter机器人检测基准，用于评估通用性 |

### 实验设置
- **实现环境**：PyTorch 2.0.0 + scikit-learn 1.3.0，NVIDIA A100 GPU
- **训练配置**：100 epochs，batch size=64，learning rate=0.001，CosineAnnealingWarmRestarts调度器
- **交叉验证**：分层五折时序划分（temporal splitting），防止时间泄露

### 评估指标
| 指标 | 含义 |
|------|------|
| **Precision, Recall, F1-score** | 协调行为识别性能 |
| **Processing Time / Convergence Epochs** | 计算效率 |
| **Memory Usage** | 资源消耗 |
| **Manual Labeling Reduction** | 标注成本节省比例 |

### 对比的基线方法
| 方法 | 类型 |
|------|------|
| **CCM [7]** | 因果推断方法，基于时间序列交叉映射 |
| **Correlation-based** | 简单相关性分析 |
| **LCN+HCC [11]** | 基于潜在协调网络与高度协调社区 |
| **AMDN-HAGE [16]** | 基于Temporal Point Processes与GMM的联合建模 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Twitter IRA 数据集）

| 方法 | Precision | Recall | **F1-score** | 时间（min） |
|------|-----------|--------|--------------|-------------|
| CCM | 72.1% | 79.8% | 75.8% | 181.3 |
| Correlation | 69.3% | 78.1% | 73.5% | 122.5 |
| LCN+HCC | 74.5% | 76.2% | 75.3% | 96.7 |
| AMDN-HAGE | 68.9% | 82.4% | 75.1% | 211.4 |
| **ACCD (Ours)** | **85.6%** | **89.2%** | **87.3%** | **72** |

> ✅ **F1-score提升15.2个百分点**（从75.8% → 87.3%）

### 计算效率对比

| 方法 | 收敛epoch数 | 内存占用 | 相对速度 | 准确率 |
|------|--------------|----------|------------|--------|
| CCM | 65 | 8.2 GB | 1.0× | 100% |
| Fixed Param | 58 | 7.9 GB | 1.1× | 98.3% |
| **ACCD (Ours)** | **40** | **4.5 GB** | **2.8×** | **96.7%** |

> ⚡ **训练速度快2.8倍，内存减少近一半**

### 其他数据集表现
- **Reddit数据集**：F1-score达 **84.7%**（传统方法仅71.2%）
- **TwiBot-20**：分类准确率达 **88.9%**
- 手动标注需求减少 **68.3%**

### 消融实验结果（Ablation Study）

![Ablation Study](Figure 6)

| 模型变体 | F1-score | 分类准确率 | 验证准确率 |
|---------|----------|------------|------------|
| Full Model (ACCD) | **91.2%** | **88.9%** | **87.3%** |
| w/o Adaptive Params | 85.9% | 88.9% | 84.7% |
| w/o Active Learning | 87.6% | 82.1% | 88.9% |
| w/o Adaptive Validation | 85.2% | 88.9% | 84.7% |

> 🔍 结果表明：三个组件均不可或缺，其中**自适应参数选择**对性能影响最大。

---

## 4. 关键结论和发现

### 主要发现
1. **因果关系优于相关性**：ACCD通过自适应CCM有效识别真实因果影响，避免伪相关干扰。
2. **记忆机制提升泛化能力**：长期记忆模块使模型能在新场景中快速收敛并复用历史最优配置。
3. **半监督策略大幅降本增效**：结合主动学习与伪标签机制，在保持高精度的同时将人工标注减少近70%。
4. **自动化验证增强鲁棒性**：基于历史成功率动态调整阈值，提升了在异构数据上的稳定性。

### 方法的局限性
- 当前框架假设用户活动具有足够的时间分辨率（timestamps），对于低频发布者可能效果受限；
- 聚类策略虽提升效率，但在极端稀疏或高度重叠的协调子图中可能存在信号丢失；
- 多模型集成带来额外计算开销，尽管总体仍优于基线。

### 未来工作方向
- 将ACCD扩展至多语言、跨平台（如微博、Facebook）场景；
- 探索在线学习机制，实现实时增量更新；
- 结合LLM进行语义级协调内容分析，增强对“软性操纵”（soft manipulation）的检测能力；
- 构建开源工具包，推动社区协作与标准化评测。

---

> 💡 **总结**：  
> ACCD 是首个将**自适应因果推理**、**半监督学习**与**自动化验证**深度融合的协调行为检测框架。它不仅在多个真实数据集上实现了SOTA性能，更在**准确性、效率与自动化水平**之间取得了良好平衡，具备强实际应用价值，为下一代社交媒体安全系统提供了可扩展的技术范式。

</details>

---

### 9. [Universal Adaptive Constraint Propagation: Scaling Structured Inference for Large Language Models via Meta-Reinforcement Learning](https://arxiv.org/abs/2601.00095)

**Authors**: Ibne Farabi Shihab, Sanjeda Akter, Anuj Sharma  
**Category**: cs.CL  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00095v1  

#### Abstract
Large language models increasingly require structured inference, from JSON schema enforcement to multi-lingual parsing, where outputs must satisfy complex constraints. We introduce MetaJuLS, a meta-reinforcement learning approach that learns universal constraint propagation policies applicable acros...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Universal Adaptive Constraint Propagation: Scaling Structured Inference for Large Language Models via Meta-Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代大型语言模型（LLMs）在执行**结构化推理任务**时面临效率瓶颈，例如：
- JSON Schema 验证
- 多语言句法分析（如 Universal Dependencies）
- 形式逻辑生成（如 LogicBench）

这些任务要求输出满足复杂的语法、语义或格式约束。传统方法依赖静态调度策略（如 FIFO）或后验验证，导致大量无效计算和高延迟。此外，不同语言或任务通常需要独立训练专用调度器，缺乏通用性和快速适应能力。

---

### **提出了什么新方法或新思路**

作者提出 **MetaJuLS** —— 一种基于 **Meta-Reinforcement Learning** 的通用自适应约束传播框架，其核心思想是：

- 将结构化推理建模为 **约束传播（Constraint Propagation）过程**，其中变量表示语言单元，域为候选标签，约束编码语法规则或逻辑条件。
- 将“选择下一个要传播的约束”视为一个 **序列决策问题**，由一个强化学习（RL）智能体控制。
- 使用 **Graph Attention Network (GAT)** 编码约束-变量图结构，捕捉长距离依赖和复杂交互。
- 引入 **Model-Agnostic Meta-Learning (MAML)** 进行元训练，使单一策略能快速迁移到新语言、新任务或新约束类型，仅需 **5–10 个梯度步（约 5–15 秒）** 而非数小时微调。

---

### **相比现有方法的优势**

| 维度 | MetaJuLS | 传统方法 |
|------|---------|--------|
| **速度** | 实现 1.5–2.0× 加速 | 依赖静态调度，效率低 |
| **准确性** | 保持与 SOTA 解析器差距 <0.2% F1/LAS | 可能因过早剪枝丢失正确解 |
| **泛化性** | 支持跨语言、跨任务快速迁移（few-step adaptation） | 需任务特定训练 |
| **环保性（Green AI）** | 减少推理步骤，降低碳足迹 | 推理开销大 |
| **机制可解释性** | 发现人类类似“易先”策略及新颖启发式 | 手工设计启发式 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 类型 | 数据集 | 描述 |
|------|-------|------|
| **句法分析** | Penn Treebank (PTB) | 英文成分句法分析基准 |
| | Universal Dependencies (UD) v2.11 | 跨 10 种语言的依存句法分析（英语、西班牙语、中文等） |
| **LLM 结构化生成** | LogicBench | 生成符合前提的形式逻辑表达式 |
| | GSM8K-Constrained | 数学题答案需符合 JSON Schema |
| **经典约束编程（CP）** | MiniZinc Challenge (2022–2024) | 包括 Knapsack、TSP、Graph Coloring 等 |
| | XCSP Competition | 高难度 CSP 实例 |

---

### **实验设置和评估指标**

#### **评估任务**
- **Parsing**: Constituency Parsing (F1), Dependency Parsing (LAS)
- **LLM Decoding**: Constraint Satisfaction Rate (%), Generation Speed (ms/sent 或 norm. runtime)
- **CP Tasks**: Optimality Gap (%), Solve Rate (%), Normalized Runtime

#### **硬件与实现**
- 使用 Llama-2-7B + vLLM 进行高效服务
- MetaJuLS 作为调度层插入解码循环
- 所有实验在 A100 GPU 上运行，报告中位数（5 次运行）

#### **对比基线方法**
| 基线类别 | 具体方法 |
|--------|--------|
| **静态/启发式调度** | FIFO, Random, dom/wdeg, Activity-Based, VSIDS-style |
| **成本感知贪心** | Cost-Normalized Greedy (最大化 ΔD/T) |
| **监督学习** | Supervised Ranking（模仿最优优先策略） |
| **其他 LLM 约束解码工具** | Outlines, LMQL, NeuroLogic Decoding |
| **优化求解器** | Google OR-Tools, JuLS (FIFO baseline) |

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

| 任务 | 方法 | 性能指标 | 提升 |
|------|------|----------|------|
| **PTB 成分句法分析** | MetaJuLS vs GPU-CKY | 1.6–1.9× 速度提升，F1 差距 <0.2% | ✅ 显著加速 |
| **UD 多语言依存分析** | MetaJuLS (英文训练 + 快速适配) | 平均 1.5–1.8× 加速，LAS 接近 UDPipe/Stanza | ✅ 跨语言迁移成功 |
| **LogicBench (LLM)** | MetaJuLS vs Speculative Decoding | 1.8× 更快，约束满足率从 94.1% → 98.2% | ✅ 更准更快 |
| **GSM8K-Constrained** | MetaJuLS vs Speculative Decoding | 1.6× 加速，Schema Compliance 从 91.3% → 96.8% | ✅ 显著改进 |
| **MiniZinc CP 基准** | MetaJuLS vs OR-Tools | 22% 更小最优性差距，63% 运行时间，94% 求解率 | ✅ 超越专业求解器 |

---

### **与基线方法的对比结果**

#### ✅ 在所有任务上显著优于各类基线：
- **比 Activity-Based / VSIDS-style 快 1.5× 以上**
- **比 Cost-Normalized Greedy 快 1.2–1.5×，同时更准确**
- **优于 Supervised Ranking**，说明 RL 学习优于模仿学习
- **结合 speculative decoding 后仍带来额外 15% 加速**，证明正交性

#### 🔄 双向迁移能力验证：
- NLP 训练的策略用于 CP 任务 → 达到 specialist 的 **91% 性能**
- CP 训练的策略用于 NLP 解析 → 达到 specialist 的 **89% 性能**
- 表明 MetaJuLS 学到了**领域无关的传播结构规律**

---

### **消融实验结果（Ablation Studies）**

| 消融项 | F1 / Gap | 时间 / Runtime | 结论 |
|--------|--------|----------------|------|
| **移除 GAT（改用 MLP）** | ↓ ~0.4 F1 | ↑ ~10% 时间 | 图结构建模至关重要 |
| **移除成本项（β=0）** | F1 略降 | 时间增加 | 成本感知奖励防止过度探索 |
| **随机策略** | F1 显著下降 | 时间大幅上升 | 学习必要 |
| **监督模仿学习** | 不及 RL | 不及 RL | RL 能发现超越局部最优的长期策略 |

> 表格来源：Table 10（PTB 消融）、Table 11（MiniZinc 消融）

---

## 4. 关键结论和发现

### **主要发现**

1. 🔍 **传播顺序是结构化推理的关键效率杠杆**  
   即使在 GPU 优化的 CKY 中，通过智能调度也能实现 1.5–2.0× 加速，说明算法层面仍有巨大优化空间。

2. 🤖 **MetaJuLS 发现了人类启发式之外的新颖策略**
   - “Easy-first”：优先处理浅层、高置信度的结构
   - “Middle-out”：对嵌套相对从句，从中等深度开始建立锚点，减少回溯 34%
   - 延迟花园路径区域的传播，直到上下文充分

3. ⚡ **Few-step Adaptation 实现秒级迁移**
   - 一个在英文上训练的策略，经过 **5–10 步梯度更新（5–15 秒）** 即可在新语言或新任务上达到 85–92% 的专家水平
   - 支持零样本或极低资源场景下的快速部署

4. 🛡️ **安全感知回退机制保障精度**
   - 当策略熵值过高（不确定性大）时，自动切换至 exhaustive search
   - 将 F1 差距从 2.1% 缩小至 **0.15%**，同时维持 **1.4× 平均加速**

5. 🌍 **真正的跨领域通用性**
   - 在 NLP 和 CP 之间实现了双向迁移
   - 证明 MetaJuLS 学到了**通用的约束传播拓扑模式**，而非语言偏见

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **对歧义严重的句子表现下降** | 如 garden-path 句子（"The horse raced past the barn fell"），早期剪枝可能误删正确分析 |
| **形态丰富语言未充分测试** | 实验集中于英语和其他主流语言，对阿拉伯语、芬兰语等挑战较大 |
| **依赖手动定义约束系统** | 将新任务转化为 constraint propagation 需人工建模变量、域、约束 |
| **元训练成本较高** | 一次性需 120 GPU-hours（8×A100），虽支持后续快速适配，但初始投入大 |
| **确定性假设限制** | 当前假设 propagator 是确定性的，难以应对随机或近似传播 |

---

### **未来工作方向**

1. **自动化任务建模接口**  
   开发自动将自然语言任务映射为 constraint propagation 形式的工具链。

2. **扩展至不确定性环境**  
   支持 stochastic 或 approximate propagators，增强鲁棒性。

3. **理论分析快速适应机制**  
   探索为何 MAML 能在如此短的几步内实现有效迁移。

4. **集成到更多 LLM 推理框架**  
   如集成进 HuggingFace Transformers、vLLM、TensorRT-LLM 等主流系统。

5. **探索其他求解器组件的学习**  
   如变量选择、值选择、重启策略等，构建全学习型求解器。

---

> **Impact Statement（影响声明）**  
> MetaJuLS 通过减少 1.5–2.0× 的推理步骤，直接降低了 LLM 部署的能耗和碳排放，推动 **Green AI** 发展。对于每日处理百万级结构化请求的服务，年节电可达 **2.4 MWh** 以上。同时，元学习避免了重复训练，进一步减少环境负担。

</details>

---

### 10. [Comparative Efficiency Analysis of Lightweight Transformer Models: A Multi-Domain Empirical Benchmark for Enterprise NLP Deployment](https://arxiv.org/abs/2601.00444)

**Authors**: Muhammad Shahmeer Khan  
**Category**: cs.CL  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00444v1  

#### Abstract
In the rapidly evolving landscape of enterprise natural language processing (NLP), the demand for efficient, lightweight models capable of handling multi-domain text automation tasks has intensified. This study conducts a comparative analysis of three prominent lightweight Transformer models - Disti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Comparative Efficiency Analysis of Lightweight Transformer Models: A Multi-Domain Empirical Benchmark for Enterprise NLP Deployment*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该研究针对**企业级自然语言处理（Enterprise NLP）部署中轻量级 Transformer 模型选择缺乏系统性实证基准**的问题。尽管已有大量关于大型模型（如 BERT、RoBERTa）的研究，但在资源受限场景下，如何在多领域任务中权衡 **accuracy（准确性）与 efficiency（效率）** 仍缺乏全面比较。

具体而言，企业在实际部署中面临以下挑战：
- 高延迟影响用户体验
- 内存占用大导致边缘设备难以运行
- 多样化文本域（如客户评论、新闻、有害内容）要求模型具备跨领域泛化能力

现有研究往往只关注单一任务或仅评估精度，忽略了推理速度、吞吐量等关键部署指标。

---

### 🆕 提出的新方法/新思路
本研究并未提出新的模型架构或训练算法，而是提出了一个**面向企业部署的多领域轻量级 Transformer 模型实证基准框架**，其创新点在于：

1. **多领域统一评估**  
   在三个典型企业应用场景（customer sentiment、news topic classification、toxicity detection）上进行横向对比，增强现实适用性。

2. **融合 accuracy 与 efficiency 的综合评估体系**  
   不仅报告 Accuracy、F1-score 等传统指标，还引入：
   - Model size（MB）
   - Inference time（ms/sample）
   - Throughput（samples/sec）
   - RAM usage
   实现对“精度 vs 效率”权衡的可视化分析。

3. **标准化 fine-tuning 协议**  
   所有模型采用相同的超参数设置（learning rate=2e-5, batch size=16, epochs=3），确保公平比较，反映真实企业快速微调场景。

---

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **实用性** | 聚焦企业部署痛点（低延迟、小内存、高吞吐），而非追求极致精度 |
| **可复现性** | 公开代码与完整实验配置（GitHub: `shahmeer07/enterprise-nlp-lightweight-transformer-benchmark`） |
| **决策指导性** | 提供明确的选型建议，帮助开发者根据业务需求选择最优模型 |

> ⚠️ 注意：本工作不追求 SOTA 性能，而是提供**部署导向的决策支持工具**。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 任务类型 | 类别数 | 训练/测试样本数 | 来源 |
|--------|---------|-------|------------------|------|
| **IMDB** | Customer Sentiment Classification | 2（positive/negative） | 25K / 25K | 标准情感分析数据集 |
| **AG News** | News Topic Classification | 4（World, Sports, Business, Sci/Tech） | 120K / 7.6K | 新闻分类基准 |
| **Measuring Hate Speech Corpus** | Toxicity/Hate Speech Detection | 3（Neutral, Offensive, Hate Speech） | 80%/20% 分割 | 内容审核数据集，通过连续得分离散化构建标签 |

---

### ⚙️ 实验设置
- **平台环境**：Google Colab + 单张 NVIDIA T4 GPU
- **实现库**：Hugging Face Transformers & Datasets
- **Tokenizer**：各模型对应 tokenizer（max_length=128）
- **微调策略**：Linear classification head + AdamW optimizer
- **固定超参数**：
  - Learning rate: 2×10⁻⁵
  - Epochs: 3
  - Batch size: 16
  - Weight decay: 0.01
  - Random seed: 42（保证可复现）

> 所有模型独立微调，无超参搜索，模拟企业快速上线场景。

---

### 📊 评估指标

#### Accuracy-based Metrics
- **Accuracy**
- **Weighted Precision, Recall, F1-score**（应对类别不平衡）
- **Confusion Matrices**（附录提供）

#### Efficiency Metrics
| 指标 | 测量方式 |
|------|--------|
| **Model Size (MB)** | 参数数量 × 4 bytes（FP32） |
| **Inference Time (ms/sample)** | 对 200 个样本测平均 wall-clock 时间（batch_size=1） |
| **Throughput (samples/sec)** | 总样本数 ÷ 总推理时间 |
| **RAM Usage (MB)** | Python 进程驻留内存（含框架开销） |

> 所有效率测量均在 `torch.no_grad()` 下完成，并经过 warm-up 阶段以消除缓存影响。

---

### 🔁 基线方法对比
对比三种主流轻量级 Transformer 模型：
| 模型 | 参数量 | 特点 |
|------|--------|------|
| **DistilBERT** | ~66M | Knowledge distillation from BERT，保留 97% 性能，推理快 40% |
| **MiniLM** | ~33M | Deep self-attention distillation，压缩注意力关系，擅长高速推理 |
| **ALBERT** | ~12M | Parameter sharing + factorized embedding，极小内存占用 |

> 均使用 Hugging Face 官方预训练 checkpoint 微调。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### 表 1：IMDB 情感分类结果
| Model      | Acc.  | F1    | Size (MB) | Inf. (ms) | Thpt. (s/s) |
|-----------|-------|-------|-----------|------------|-------------|
| DistilBERT | 0.932 | 0.932 | 255.42    | 14.58      | 68.57       |
| MiniLM     | 0.937 | 0.938 | 127.26    | 10.11      | 98.95       |
| **ALBERT** | **0.944** | **0.944** | **44.58** | 32.18      | 31.08       |

> ALBERT 最准最小，但最慢；MiniLM 推理最快。

---

#### 表 2：AG News 新闻分类结果
| Model      | Acc.  | F1    | Size (MB) | Inf. (ms) | Thpt. (s/s) |
|-----------|-------|-------|-----------|------------|-------------|
| DistilBERT | 0.946 | 0.946 | 255.42    | 3.30       | 302.81      |
| MiniLM     | 0.947 | 0.947 | 127.26    | **2.14**   | **466.88**  |
| **ALBERT** | **0.949** | **0.949** | **44.58** | 7.48       | 133.66      |

> ALBERT 精度最高，MiniLM 吞吐遥遥领先。

---

#### 表 3：Toxicity Detection 结果
| Model      | Acc.  | F1    | Size (MB) | Inf. (ms) | Thpt. (s/s) |
|-----------|-------|-------|-----------|------------|-------------|
| DistilBERT | 0.949 | 0.949 | 255.42    | 2.71       | 368.75      |
| MiniLM     | 0.906 | 0.900 | 127.26    | **1.85**   | **540.17**  |
| **ALBERT** | **0.951** | **0.950** | **44.58** | 6.15       | 162.51      |

> ALBERT 再次取得最佳精度，MiniLM 推理速度第一，但精度下降较明显。

---

### 🔍 与基线方法的对比总结
| 维度 | 最优模型 | 表现 |
|------|----------|------|
| **Task-specific Accuracy** | ALBERT | 在所有三个任务中均达到最高或接近最高精度 |
| **Consistency Across Tasks** | DistilBERT | 各任务间表现最稳定，F1 波动最小 |
| **Inference Speed & Throughput** | MiniLM | 平均比 DistilBERT 快 1.5–2×，吞吐量最高 |
| **Model Size & Memory Footprint** | ALBERT | 仅为 DistilBERT 的 1/6，适合边缘部署 |

---

### ❌ 是否有消融实验？
本文未进行消融实验（ablation study）。作者强调其目标是**部署导向的横向比较**，而非探究内部机制。所有模型使用相同微调流程，差异归因于架构设计本身。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **No Single Model Dominates All Dimensions**  
   不存在“全能冠军”，必须根据应用场景权衡取舍：
   - 若追求**最高精度** → 选 **ALBERT**
   - 若追求**最低延迟/最高吞吐** → 选 **MiniLM**
   - 若追求**平衡性能与稳定性** → 选 **DistilBERT**

2. **Architecture Design Directly Impacts Trade-offs**
   - **DistilBERT**：Knowledge distillation → 保持语义保真度，泛化强
   - **MiniLM**：Deep self-attention distillation → 极致压缩隐藏层 → 高速推理
   - **ALBERT**：Parameter sharing → 极小体积，但增加顺序依赖 → 推理变慢

3. **MiniLM 是高并发系统的理想选择**  
   在 AG News 和 Toxicity 检测中吞吐达 466–540 samples/sec，显著优于其他模型，适用于实时内容路由、大规模日志处理等场景。

4. **ALBERT 适合资源受限环境**  
   仅需 44.58 MB 存储空间，在 IoT 设备、移动端或低成本云实例上有巨大优势。

---

### ⚠️ 局限性
1. **语言限制**：仅评估英文数据集，未验证多语言或低资源语言表现。
2. **硬件单一**：全部实验基于 T4 GPU，CPU 或边缘芯片上的表现可能不同。
3. **无统计显著性检验**：单次运行结果，未报告置信区间，不适合严格统计推断。
4. **有限超参调优**：为贴近企业实际，未做网格搜索，可能存在性能提升空间。
5. **标签离散化主观性**：Hate Speech 数据集使用阈值划分连续分数，可能引入偏差。

---

### 🔮 未来工作方向
1. **扩展至多语言场景**（Multilingual Evaluation）
2. **更多硬件平台测试**（CPU, Jetson, Raspberry Pi 等）
3. **加入量化与蒸馏后优化技术**（Quantization, ONNX Runtime, TensorRT）
4. **探索动态批处理（Dynamic batching）对吞吐的影响**
5. **开展 controlled variance analysis** 以量化随机性影响

---

## ✅ 总结：企业选型建议（Deployment Guidance）

| 应用场景 | 推荐模型 | 理由 |
|--------|----------|------|
| **低延迟服务（如聊天机器人响应）** | MiniLM | 推理最快，吞吐最高 |
| **边缘设备部署（如手机 App）** | ALBERT | 体积最小，内存友好 |
| **通用文本分类 pipeline** | DistilBERT | 精度稳定，兼顾效率 |
| **大规模批量处理（如日志分析）** | MiniLM | 高吞吐降低单位成本 |
| **资源极度受限的小型企业（SME）** | ALBERT | 最小 footprint 支持轻量部署 |

> 💡 **核心思想**：在企业 NLP 中，**efficiency often trumps marginal accuracy gains**。本研究提供了清晰的决策地图，助力工程团队做出更合理的模型选型。

</details>

---

### 11. [A Language-Agnostic Hierarchical LoRA-MoE Architecture for CTC-based Multilingual ASR](https://arxiv.org/abs/2601.00557)

**Authors**: Yuang Zheng, Yuxiang Mei, Dongxing Xu, Jie Chen, Yanhua Long  
**Category**: cs.CL  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00557v1  

#### Abstract
Large-scale multilingual ASR (mASR) models such as Whisper achieve strong performance but incur high computational and latency costs, limiting their deployment on resource-constrained edge devices. In this study, we propose a lightweight and language-agnostic multilingual ASR system based on a CTC a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Language-Agnostic Hierarchical LoRA-MoE Architecture for CTC-based Multilingual ASR

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大规模多语言自动语音识别（mASR）模型（如 Whisper 和 SeamlessM4T）虽然性能强大，但存在以下问题：
- **高计算开销和推理延迟**，难以部署在资源受限的边缘设备上；
- 在**领域不匹配**（domain mismatch）场景下（如口音、说话风格变化）性能显著下降；
- 多数参数高效微调方法（如 LoRA）依赖显式的语言身份信息（LID），限制了真实场景中的实用性；
- 现有的两阶段语言无关（language-agnostic）系统引入额外延迟，并存在**错误传播风险**（从 LID 到 ASR）。

### 提出的新方法与创新思路
本文提出了一种轻量级、真正语言无关的多语言 ASR 框架：**mHuBERT-CTC-HLoRA**，其核心是基于 CTC 架构的 **Hierarchical LoRA-MoE（HLoRA）** 框架，具备以下创新点：

- ✅ **语言无关的分层 LoRA 设计**：
  - 将 Transformer 编码器分为上下两部分：
    - 下层 $k$ 层采用**共享 LoRA**（shared LoRA），学习跨语言共有的声学特征；
    - 上层 $N-k$ 层采用**语言特定 LoRA 专家**（language-specific LoRA experts），建模语言特异性特征。
  - 实现语言不变与语言相关表示的统一建模。

- ✅ **端到端单通路推理机制**：
  - 引入 **LID-posterior-driven LoRA routing** 机制：利用中间层输出通过一个轻量级 LID 分类器预测语言后验概率，动态激活对应的语言特定 LoRA 模块；
  - 整个过程在一个前向传播中完成，无需外部 LID 模块或先验语言标签。

- ✅ **真正的语言无关解码**：
  - 推理时完全不需要输入语言标识（LID），实现“即插即用”式的多语言识别；
  - 避免了传统两阶段方法中的错误传播和延迟累积。

### 相比现有方法的优势
| 特性 | 传统两阶段方法（如 mHuBERT-CTC-LIDLoRA） | 本文 HLoRA 方法 |
|------|----------------------------------------|----------------|
| 是否需要 LID 输入 | ❌ 不需要（但需第一阶段预测） | ✅ 完全无需 |
| 推理模式 | 两阶段（double-pass） | 单通路（single-pass） |
| 错误传播风险 | 存在（LID 错误影响 ASR） | 无（联合优化） |
| 推理延迟 | 较高 | 显著降低 |
| 参数效率 | 高（仅更新 LoRA 参数） | 更优（共享+专家结构） |
| 性能表现 | 受限于冻结的 LID 模块 | 更鲁棒（端到端联合训练） |

---

## 2. 核心实验方法和设置

### 数据集
- **MSR-86K**：用于预训练基础 mHuBERT-CTC 模型，包含约 1500 小时的 11 种语言数据（EN, FR, DE, IT, JA, KO, PT, RU, ES, TH, VI）。
- **MLC-SLM 2025 Challenge Dataset**：作为目标低资源领域进行适配与评估，聚焦五种语言：
  - English (Indian accent, EN-IN), French (FR), German (DE), Japanese (JA), Korean (KO)
  - 每语言提供 98h 训练 + 2h 开发，以及官方测试集。

### 实验设置
- **主干模型**：`mHuBERT-147`（SSL 预训练模型）
- **架构设计**：
  - CTC 解码（非自回归，低延迟）
  - LoRA 应用于 self-attention 的 Q/K/V 投影和 CTC 层，rank $r=32$, $\alpha=64$
  - SentencePiece 分词：JA/KO/TH 使用字符单位，其余使用 4k BPE，统一词汇表大小为 9,521
- **评估指标**：
  - 统一使用 **Mixture Error Rate**：
    - JA/KO/TH 使用 **CER**（Character Error Rate）
    - 其他语言使用 **WER**（Word Error Rate）

### 基线方法对比
| 系统 | 描述 |
|------|------|
| **S1/S2** | mHuBERT-CTC 基线，分别在 MSR-1500h 和 MLC-500h 上训练 |
| **S3/S4** | mHuBERT-CTC-LIDLoRA：<br>- S3：语言已知（ground truth LID），单通路<br>- S4：语言未知，两阶段推理（state-of-the-art language-agnostic baseline） |
| **S5/S6** | 本文提出的 mHuBERT-CTC-HLoRA：<br>- S5：语言已知，单通路<br>- S6：语言未知，单通路（最终目标系统） |
| **S7/S8** | Whisper-base / Whisper-small，直接在 MLC 上评估 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（MLC-test WER/CER %）

| System | Params (M) | LID Info | Inference | MLC-dev (%) | MLC-test (%) |
|--------|------------|----------|-----------|-------------|--------------|
| S1 (baseline) | 97 | × | single | 44.5 | 43.7 |
| S2 (full fine-tune) | 97 | × | single | 22.5 | 21.6 |
| S3 (LIDLoRA, known) | 107 | √ | single | 24.6 | 23.0 |
| S4 (LIDLoRA, agnostic) | 107 | × | double | 26.6 | 24.8 |
| **S6 (HLoRA, agnostic)** | **102** | × | **single** | **26.3** | **24.7** |
| S7 (Whisper-base) | 74 | × | single | 37.2 | 35.4 |
| S8 (Whisper-small) | 244 | × | single | 29.7 | 28.6 |

> 🔍 **关键观察**：
> - S6（HLoRA）在**完全语言无关、单通路**条件下，性能（24.7%）**优于两阶段基线 S4（24.8%）**；
> - 虽略逊于语言已知的 S3（23.0%），但差距极小（+1.7%），且节省了语言标注需求；
> - 显著优于 Whisper 系列模型，说明领域适配的重要性；
> - 参数量仅增加约 5M（相比主干），保持高度参数效率。

### 消融实验结果（Table II）

研究不同共享层数 $k$ 对性能的影响（MLC-dev/test 平均 WER%）：

| $k$ (共享层数) | 1 | 3 | **6** | 9 | 11 |
|---------------|----|-----|-------|-----|-----|
| Average WER | 34.8/36.6 | 31.3/32.1 | **26.0/26.0** | 26.3/24.7 | 26.5/25.2 |

> 📌 发现：
> - 当 $k=6$ 或 $9$ 时达到最佳性能，表明适度的跨语言共享有助于提升泛化能力；
> - $k=1$ 或 $3$ 时共享不足，性能差；
> - $k=11$（几乎全部共享）导致语言特异性建模能力下降，性能回落。

### LID 准确率对比（Fig. 3）
- mHuBERT-CTC-LIDLoRA（S4）：平均 LID 准确率 **90.1%**
- mHuBERT-CTC-HLoRA（S6）：平均 LID 准确率 **97.9%**
- HLoRA 的混淆矩阵更接近对角线，说明其 LID 预测更准确、更稳定。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **HLoRA 实现了高效的语言无关多语言 ASR**：
   - 在无需任何语言输入的前提下，通过单次前向传播即可完成高质量识别；
   - 性能媲美甚至略微超越两阶段方法，同时避免错误传播。

2. ✅ **分层 LoRA 结构有效平衡共享与个性化**：
   - 下层共享 LoRA 捕捉通用声学模式；
   - 上层语言专家捕捉差异特性；
   - 二者结合提升了参数效率和模型鲁棒性。

3. ✅ **端到端联合优化显著增强 LID 可靠性**：
   - LID 分类器与 ASR 模型共同训练，可适应目标域特征；
   - 相比“冻结式”两阶段设计，LID 准确率大幅提升（90.1% → 97.9%）。

4. ✅ **适用于低资源、低延迟场景**：
   - 单通路设计大幅减少推理时间；
   - 适合部署于 hearing aids、mobile devices 等边缘设备。

### 方法的局限性
- 当前仅验证了 5 种目标语言，扩展至更多语言时可能面临 LoRA 专家管理复杂度上升的问题；
- 共享层数 $k$ 需手动设定，缺乏自动化选择机制；
- 对非常见语言或极端口音的泛化能力仍需进一步验证。

### 未来工作方向
- 探索动态路由机制（dynamic MoE routing）以支持无限语言扩展；
- 引入 speaker-aware 或 accent-aware LoRA 模块，进一步提升个性化性能；
- 将 HLoRA 扩展至其他任务（如 TTS、SLU）构建统一多语言语音理解框架；
- 开源代码与模型已在 GitHub 发布：[https://github.com/zhengyuang7/HLoRA](https://github.com/zhengyuang7/HLoRA)

--- 

✅ **总结一句话**：  
本论文提出了 **HLoRA** —— 一种基于 CTC 的分层 LoRA-MoE 架构，在保证轻量化和低延迟的同时，实现了**端到端、单通路、真正语言无关的多语言 ASR**，在 MLC-SLM 2025 挑战赛上取得了优于主流两阶段方法的性能，为实际部署提供了高效可行的解决方案。

</details>

---

### 12. [Revati: Transparent GPU-Free Time-Warp Emulation for LLM Serving](https://arxiv.org/abs/2601.00397)

**Authors**: Amey Agrawal, Mayank Yadav, Sukrit Kumar, Anirudha Agrawal, Garv Ghai, Souradeep Bera, Elton Pinto, Sirish Gambhira, Mohammad Adain, Kasra Sohrab, Chus Antonanzas, Alexey Tumanov  
**Category**: cs.DC  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00397v1  

#### Abstract
Deploying LLMs efficiently requires testing hundreds of serving configurations, but evaluating each one on a GPU cluster takes hours and costs thousands of dollars. Discrete-event simulators are faster and cheaper, but they require re-implementing the serving system's control logic -- a burden that ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**REVATI: Transparent GPU-Free Time-Warp Emulation for LLM Serving**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大语言模型（LLM）推理服务系统配置空间巨大，涉及并行策略、缓存机制、调度算法等多个维度。在真实GPU集群上进行端到端评估成本高昂（单次评估可达数千美元），且耗时数小时。传统的**离散事件模拟器**（Discrete-Event Simulators, DES）虽能加速评估，但需手动重实现整个控制逻辑（如调度器、内存管理等），维护负担极重，难以跟上vLLM、SGLang等框架的快速演进。

### 提出的新方法与核心思路
提出 **REVATI** —— 一种无需GPU的**时间扭曲仿真**（Time-Warp Emulation）系统，其核心思想是：

- **直接运行原始LLM服务框架代码**（如vLLM、SGLang），不重新实现任何控制逻辑；
- 利用 **CUDA API拦截技术**（LD_PRELOAD）虚拟化GPU设备，跳过实际GPU内核执行；
- 将GPU操作替换为**虚拟时间跳跃**（Time Jump），通过预测内核执行时间来推进虚拟时钟；
- 设计一个**分布式协调协议**（Barrier-based Virtual Time Protocol），确保多个进程间的时间跳跃保持因果一致性。

这种方法被称为“**透明仿真**”（Transparent Emulation），实现了高保真度与高速度的统一。

### 相比现有方法的优势
| 维度 | 离散事件模拟器（DES） | REVATI |
|------|------------------------|--------|
| 控制逻辑实现 | 需手动重写（易出错、难维护） | 直接执行原生代码（自动继承所有特性） |
| 功能覆盖 | 落后于最新功能（如PD disaggregation、MoE） | 支持所有框架内置复杂特性（开箱即用） |
| 维护成本 | 极高（每次框架更新都可能失效） | 极低（仅需<50行补丁） |
| 性能建模精度 | 存在语义鸿沟（行为偏差） | <5% 预测误差 |
| 执行速度 | 快于真实执行（但受限于建模完整性） | 比真实GPU执行快5–17× |

> ✅ **REVATI的关键突破在于：将“模拟”转变为“加速执行”，从而避免了建模失真问题。**

---

## 2. 核心实验方法和设置

### 使用的数据集与工作负载
- **请求数据集**：`ShareGPT`（真实用户对话序列）
- **请求到达模式**：泊松过程（Poisson arrival），用于模拟不同QPS下的动态负载
- **输出长度分布**：固定或预设生成token数量（为建模目的简化EOS判断）

### 实验设置
- **模型范围**：
  - Dense Models: `Llama-3.1-8B`, `Llama-3.1-70B`
  - Sparse Model: `Qwen3-30B-A3B`（含MoE结构）
- **服务引擎**：
  - `vLLM` 和 `SGLang`（代表两类主流实现风格）
- **硬件环境**：
  - 4×H200 GPUs（NVLink全连接）、AMD EPYC CPU、756GB内存
  - REVATI可在无GPU环境下运行，支持模拟任意规模集群（如128-GPU配置）
- **关键配置**：
  - 启用chunked prefill + mixed batching（chunk size=512）
  - Tensor Parallelism: TP=1 (8B), TP=4 (70B)；Expert Parallelism: EP=2 (Qwen)

### 评估指标
| 指标 | 描述 |
|------|------|
| **TTFT** (Time to First Token) | 用户发出请求到收到第一个响应token的时间 |
| **TPOT** (Time Per Output Token) | 解码阶段每个输出token的平均延迟 |
| **Throughput (QPS)** | 每秒处理的查询数 |
| **Prediction Error** | REVATI预测值 vs 真实执行测量值的相对误差 |
| **Speedup** | REVATI墙钟时间 / 真实GPU执行时间的倒数 |

### 基线方法对比
- **Baseline**: 在真实GPU上的完整执行（Ground Truth）
- **Sleep-Based Emulation**（消融实验基线）：用`sleep()`代替GPU执行，保留时间语义但无加速
- **Discrete-Event Simulators**（间接对比）：Vidur [4], LLMServingSim [12], Frontier [13] 等

---

## 3. 主要实验结果和性能指标

### 关键性能数据
#### ✅ 准确性（Accuracy）
- **TTFT 和 TPOT 分布预测误差 < 5%**，即使在尾部延迟（P90/P99）也高度一致（见图6）
- 成功捕捉到vLLM与SGLang之间的细微差异：
  - SGLang默认不启用mixed batching → 导致decode阶段TPOT尾部更差
  - REVATI自动复现该行为，而传统模拟器容易忽略此类实现细节

#### ✅ 加速性能（Efficiency）
- **整体加速比达 5–17×**（见图7）：
  - vLLM: 最高 **17×**
  - SGLang: 最高 **12×**
- 加速比随模型增大而提升（因GPU占比更高）：
  - Llama-70B > Llama-8B
- 即使在高QPS下仍维持 >10× 加速（CPU开销占比通常 < 5–10%）

#### ✅ 消融实验结果
| 实验条件 | 结果 |
|---------|------|
| **不同batch duration（5–40ms）** | 随着batch变长，加速比从~5×升至**27×**（图10），验证了“跳过GPU等待”的有效性 |
| **不同请求速率（0.5–8 QPS）** | 在高负载下略有降速（因CPU调度开销增加），但仍保持**>10×加速**（图9） |
| **与Sleep-Based Emulation对比** | 输出分布几乎完全一致（图8），证明时间跳跃未破坏因果逻辑 |

---

## 4. 关键结论和发现

### 主要发现
1. **GPU等待主导执行时间**：LLM服务中超过90%的wall-clock time花在GPU计算上，CPU控制平面仅占5–10%，这为“时间加速”提供了天然基础。
2. **控制流与计算解耦**：调度决策依赖的是请求状态、内存占用等抽象信息，而非GPU输出的具体数值，因此可安全跳过GPU执行。
3. **REVATI实现了“零建模”高保真仿真**：通过直接运行原生代码，自动支持所有高级特性（如PD disaggregation、hierarchical caching、MoE路由等），无需人工建模。
4. **Barrier-based时间同步协议有效保障因果性**：最小目标推进机制 + 超时回退策略，既保证正确性，又具备容错能力（straggler容忍）。

### 方法的局限性
- **仍需少量代码侵入**（~50行）：需在Actor组件中插入`TIMEJUMP()`调用，尚未实现完全透明（如内核级拦截）。
- **依赖准确的runtime predictor**：当前使用基于Vidur的分析模型，若预测不准会影响精度（尽管误差仍可控）。
- **无法处理CPU读取compute buffer的情况**：采用split-state memory model假设KV cache不会被CPU主动访问，否则会触发异常。
- **网络抖动影响观测精度**：虽引入bounded-jitter模型缓解，但在极端延迟场景下可能影响latency测量。

### 未来工作方向
1. **实现完全透明的自动化时间跳跃注入**：
   - 探索基于eBPF或内核hook的技术，消除对框架打补丁的需求。
2. **集成更精确的kernel runtime预测器**：
   - 引入Ithemal [21] 或Forecast-GPU [19] 类型的深度学习预测模型，提升预测准确性。
3. **探索“光速执行”极限**：
   - 当GPU等待被完全跳过，瓶颈将转移到tokenization、通信、内存拷贝等环节，值得深入优化。
4. **扩展至训练场景**：
   - 当前聚焦inference，未来可尝试应用于分布式训练仿真。

---

## 总结
**REVATI提出了一种革命性的LLM服务性能建模范式——Time-Warp Emulation**。它通过**直接执行原生服务框架代码 + 虚拟时间跳跃**的方式，在无需真实GPU的情况下实现了**高保真（<5%误差）与高效（5–17×加速）的统一**。相比传统离散事件模拟器，REVATI从根本上解决了“维护滞后”和“语义鸿沟”问题，为LLM系统研究与生产部署提供了一个强大、可持续演进的仿真平台。

</details>

---

### 13. [Traffic-Aware Optimal Taxi Placement Using Graph Neural Network-Based Reinforcement Learning](https://arxiv.org/abs/2601.00607)

**Authors**: Sonia Khetarpaul, P Y Sharan  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00607v1  

#### Abstract
In the context of smart city transportation, efficient matching of taxi supply with passenger demand requires real-time integration of urban traffic network data and mobility patterns. Conventional taxi hotspot prediction models often rely solely on historical demand, overlooking dynamic influences ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Traffic-Aware Optimal Taxi Placement Using Graph Neural Network-Based Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**城市出租车供需匹配效率低下**的问题，尤其是在动态交通环境（如拥堵、突发事件、大型活动）下，传统出租车热点预测模型仅依赖历史需求数据，难以实时响应复杂多变的城市交通状况。这导致乘客等待时间长、司机空驶距离高、交通拥堵加剧。

### 提出的新方法与新思路
作者提出了一种**基于图神经网络（GNN）的强化学习框架（GNN+RL）**，用于在智能城市环境中实现交通感知的最优出租车布点推荐。其核心创新包括：

- **交通感知建模**：将城市道路网络建模为图结构 $ G=(V,E) $，其中节点代表热点区域（如交通枢纽、高需求区），边表示可通行路段，并引入**实时交通拥堵分数**（来自 Google Maps Distance Matrix API）作为节点属性之一。
  
- **GNN 编码空间-时序依赖**：利用 GraphSAGE 风格的 GNN 对节点进行嵌入（embedding），捕捉热点之间的空间相关性、道路连通性和动态交通状态。

- **Q-learning 决策机制**：设计一个基于 Q-learning 的 RL 代理，以 GNN 输出的节点嵌入作为状态输入，选择最优的出租车部署位置（hotspot），目标是最大化综合奖励函数。

- **k-hop Dominating Set 优化动作空间**：通过选取“k-hop 支配集”来筛选出具有战略影响力的候选热点，显著减少 RL 动作空间规模（约降低 60%），提升训练效率且不牺牲覆盖能力。

- **多目标奖励函数设计**：
  $$
  R = -\alpha W_t - \beta D - \gamma T
  $$
  综合考虑：
  - $ W_t $：乘客等待时间
  - $ D $：司机前往热点的距离
  - $ T $：基于交通得分的拥堵惩罚项

### 相比现有方法的优势
| 方面 | 传统方法局限 | 本文优势 |
|------|--------------|----------|
| 数据依赖 | 仅用历史需求 | 融合**实时交通API + 事件信息 + 历史需求** |
| 空间建模 | 网格化处理忽略路网拓扑 | 使用**图结构建模真实道路连接关系** |
| 决策机制 | 单一目标优化（如最大接单率） | 多目标平衡：**等待时间、行驶距离、避堵** |
| 可扩展性 | 动作空间大，难收敛 | 利用 **k-hop 支配集压缩动作空间**，提高训练效率 |

---

## 2. 核心实验方法和设置

### 使用的数据集
由于缺乏公开的德里（Delhi）精细出租车请求数据，作者构建了一个**模拟数据集**，基于以下来源生成：

- **Historic Ola Taxi Request Dataset**：作为生成真实需求模式的基础。
- **50个热点节点**：分布在德里市内，地理范围限定为纬度 [28.5, 28.9]，经度 [77.0, 77.3]。
- **1000条模拟打车请求**：分布于各热点之间。
- **实时交通评分**：通过 **Google Maps Distance Matrix API** 获取，定义为当前行程时间与自由流时间之比。

### 实验设置
- **图构建**：每个热点为节点，道路段为边；节点特征包括：
  - 历史需求密度 $ d(v) $
  - 事件临近度 $ e(v) $
  - 实时拥堵得分 $ T(v) $

- **GNN 模型**：采用 GraphSAGE 进行节点嵌入，聚合邻域信息。

- **RL 设置**：
  - **状态空间**：$ s = [z_v, l_t] $，其中 $ z_v $ 是 GNN 嵌入，$ l_t $ 是用户位置和时间上下文。
  - **动作空间**：从 k-hop 支配集中选出的 20 个关键热点（原 50 个）。
  - **算法**：Tabular Q-learning + ε-greedy 探索策略，ε 随训练衰减。
  - **Q-table 构建方式**：对连续 GNN 嵌入使用 k-means（k=25）聚类离散化，形成稳定的状态表示。

- **训练平台**：Intel Core i7-12700H CPU, 32GB RAM, NVIDIA RTX 3060 GPU；使用 PyTorch Geometric 实现 GNN，NumPy 实现 Q-learning。

### 评估指标
- 平均 episode 奖励（Avg. Reward）
- 平均乘客等待时间（Wait Time, min）
- 平均司机行驶距离（Distance, km）

所有结果取 **5 次独立运行的平均值**，确保统计一致性。

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Random Placement (RP)** | 完全随机推荐热点，无任何智能逻辑 |
| **Greedy Demand Placement (GDP)** | 总是选择最近历史需求最高的热点 |
| **RL-Only** | 不使用 GNN 嵌入，直接基于原始特征进行 Q-learning |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table I）

| 方法 | Avg. Reward | Wait Time (min) | Distance (km) |
|------|-------------|------------------|---------------|
| Random Placement (RP) | 0.12 | 8.74 | 3.12 |
| Greedy Demand (GDP) | 0.74 | 6.21 | 2.87 |
| RL-Only | 1.05 | 4.98 | 2.35 |
| **GNN+RL (Proposed)** | **1.46** | **3.84** | **1.92** |

### 与基线方法的对比结果
- **相比 GDP（贪婪策略）**：
  - 奖励提升 **97%**
  - 等待时间减少 **38%**
  - 行驶距离减少 **33%**

- **相比 RL-Only**：
  - 奖励提升 **39%**
  - 等待时间减少 **23%**
  - 行驶距离减少 **18%**

- **相比 RP（随机策略）**：
  - 等待时间减少 **56%**
  - 行驶距离减少 **38%**

> ✅ 结果表明：**GNN 提供的空间上下文显著增强了 RL 的决策质量**，特别是在避开拥堵区域的同时维持高需求覆盖。

### 消融实验与分析（隐含在文中）
虽然未明确列出“消融实验表”，但通过对比不同方法已体现关键组件的有效性：
- **加入 GNN 后性能跃升** → 验证了图结构建模的重要性；
- **k-hop 支配集使动作空间从 50→20** → 加速训练并保持覆盖率；
- **多因素奖励函数引导均衡优化** → 避免过度集中在高需求但拥堵区域（如 Fig. 4 所示）；
- **训练曲线显示 GNN+RL 更快收敛至更高奖励平台**（Fig. 5）。

---

## 4. 关键结论和发现

### 主要发现
1. **融合实时交通信息的 GNN+RL 框架能有效提升出租车调度效率**，显著降低乘客等待时间和司机空驶距离。
2. 图结构建模优于传统的网格化或孤立点建模方式，能够更好地反映城市交通的真实拓扑约束。
3. 引入 **k-hop 支配集** 可大幅压缩 RL 动作空间，提升训练效率而不损失推荐质量。
4. 多目标奖励机制使得系统能够在**需求满足、司机效率、交通缓解**之间取得良好平衡。
5. 该方法具备良好的适应性和可扩展性，适用于动态变化的城市环境。

### 方法的局限性
- **依赖外部 API**：实时交通数据依赖 Google Maps API，在某些地区可能不可用或成本较高。
- **仿真数据限制**：实验基于模拟数据集，尚未在真实运营车队中验证效果。
- **静态图结构假设**：道路网络被视为固定图，未考虑临时封路或施工等极端变化。
- **计算开销仍存**：尽管做了简化，GNN+Q-learning 流程仍需一定计算资源，边缘设备部署存在挑战。

### 未来工作方向
- **集成预测性交通模型**：不仅使用当前交通状态，还预测未来几分钟内的拥堵趋势。
- **多模态交通协同优化**：扩展至公交、地铁、共享单车等多模式出行整合调度。
- **支持自动驾驶车辆协调**：应用于未来 AV fleet 的自主调度系统。
- **接入 Smart City IoT 平台**：与城市级传感器网络联动，实现更全面的态势感知。
- **在线增量学习机制**：支持模型持续更新，适应长期需求演变。

---

> 🔚 **总结一句话**：本文提出的 **GNN-based RL 框架** 成功实现了**交通感知、高效、自适应的出租车热点推荐系统**，在模拟环境下展现出远超传统方法的性能优势，为智能城市出行优化提供了可行的技术路径。

</details>

---

### 14. [A Comparative Study of Adaptation Strategies for Time Series Foundation Models in Anomaly Detection](https://arxiv.org/abs/2601.00446)

**Authors**: Miseon Park, Kijung Yoon  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.00446v1  

#### Abstract
Time series anomaly detection is essential for the reliable operation of complex systems, but most existing methods require extensive task-specific training. We explore whether time series foundation models (TSFMs), pretrained on large heterogeneous data, can serve as universal backbones for anomaly...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Comparative Study of Adaptation Strategies for Time Series Foundation Models in Anomaly Detection

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文聚焦于**时间序列异常检测（TSAD）中模型适应性差、训练成本高**的问题。传统方法通常需要针对特定任务进行大量训练和调参，难以泛化到多样化的现实场景。作者提出探索**预训练的时间序列基础模型（TSFMs）是否可以作为通用骨干网络用于异常检测**，并系统比较不同适应策略的有效性和效率。

### 🚀 提出的新方法/新思路
- **将TSFMs从预测任务迁移到异常检测任务**：首次对多种TSFM（如Moirai、Chronos、Time-MoE）在零样本（zero-shot）、全量微调（full fine-tuning）和参数高效微调（PEFT）下的表现进行了全面实证研究。
- **系统比较多种PEFT策略**：引入并评估了LoRA、OFT、HRA、IA3等参数高效微调技术在TSAD中的应用效果，揭示其在保持低计算开销的同时提升性能的潜力。
- **强调评估指标的重要性**：指出在严重类别不平衡下，AUC-PR 和 VUS-based 指标比传统的AUC-ROC更具判别力，并倡导使用更鲁棒的VUS系列指标来容忍时间错位。

### 🔍 相比现有方法的优势
- **无需任务特定训练即可实现强性能**：TSFMs在零样本设置下已能超越多数task-specific基线。
- **PEFT显著降低适配成本**：仅更新0.02%~3.5%的参数即可达到甚至超过全量微调的效果，尤其适用于大规模或MoE架构模型。
- **通用性强、可扩展性好**：验证了TSFMs即使是在为预测任务预训练的情况下，也能有效服务于异常检测，推动“统一时间序列基础模型”范式的可行性。

---

## 2. 核心实验方法和设置

### 📊 数据集
- 使用 **TSB-AD-U benchmark**，包含23个精心筛选的单变量时间序列数据集。
- 覆盖多个领域、异常类型、序列长度和统计特性，具有良好的多样性与可靠性。
- 实验采用预定义的训练/调优/测试划分，其中**调优集用作最终评估集**以提高实验效率（所有超参数固定后一次性评估）。

### ⚙️ 实验设置
- **TSFM Backbone**：
  - 零样本实验涵盖：Moirai系列（dense & MoE）、Chronos系列（T5/Bolt）、Time-MoE-base。
  - 微调实验集中于三个代表性模型：**Moirai-base、Chronos-t5-base、Time-MoE-base**，分别代表patch-based encoder、numerical tokenization autoregressive、sparse MoE三种主流架构。
- **适应策略对比**：
  - **Zero-shot inference**：直接使用预训练模型生成预测，以MSE作为异常分数。
  - **Full fine-tuning**：更新全部参数，作为上界基准。
  - **PEFT methods**：包括LoRA、OFT、HRA（插入adapter模块）、IA3（学习缩放向量），adapter rank尝试4/8/16/32。
- **训练协议**：
  - 上下文窗口：150时间步
  - 训练轮数：10 epochs
  - 优化器与损失函数沿用原TSFM设定（如NLL、CrossEntropy、Huber Loss）
  - 学习率搜索范围：1e-2 到 1e-5，选择跨数据集平均最优配置

### 📈 评估指标（均为threshold-free）
| 指标 | 描述 |
|------|------|
| **AUC-ROC** | 衡量全局排序能力 |
| **AUC-PR** | 更关注正类（异常）识别精度，在类别极度不平衡时更有意义 |
| **VUS-ROC / VUS-PR** | 引入缓冲区容忍小的时间偏移，更适合基于预测的异常检测任务，但计算成本更高 |

> 所有方法均使用 **MSE = (prediction - ground truth)^2** 作为每一步的异常得分。

### 🆚 基线方法对比
对比了广泛使用的非TSFM类基线，分为三类：
- **经典机器学习**：Sub-PCA、Isolation Forest (IForest)、SAND
- **深度学习预测型**：LSTMAD、TimesNet、Anomaly Transformer
- **重构型神经模型**：AutoEncoder、OmniAnomaly

这些基线不依赖任何预训练，代表当前主流的task-specific方法。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总（见Tables 1–3）

#### ▶️ 基线方法表现（Table 1）
| 方法 | AUC-PR | AUC-ROC | VUS-PR | VUS-ROC |
|------|--------|---------|--------|---------|
| **LSTMAD**（最佳基线） | **0.332** | **0.724** | 0.323 | **0.820** |
| Sub-PCA | 0.293 | 0.700 | **0.353** | 0.747 |
| Anomaly Transformer | 0.059 | 0.499 | 0.102 | 0.575 |

> 结论：简单模型（如SAND、Sub-PCA）在VUS指标上有竞争力；复杂模型（如Anomaly Transformer）表现不佳，说明泛化能力弱。

---

#### ▶️ 零样本TSFM表现（Table 2）
| TSFM | AUC-PR | AUC-ROC | VUS-PR | VUS-ROC |
|------|--------|---------|--------|---------|
| **Chronos-bolt-small** | **0.346** | 0.684 | **0.319** | **0.770** |
| Chronos-bolt-tiny | 0.345 | **0.693** | 0.318 | 0.774 |
| Moirai-small | 0.335 | 0.697 | 0.318 | 0.778 |
| Time-MoE-base | 0.339 | 0.669 | 0.318 | 0.756 |

> 结论：
- 多数TSFM零样本性能已**超过最强基线LSTMAD**
- Bolt系列表现最好，表明数值tokenization + autoregressive设计适合在线检测
- MoE变体未显示出明显优势，可能因稀疏激活限制表达能力

---

#### ▶️ 不同适应策略对比（Table 3）

| Model | Adaptation | AUC-PR | AUC-ROC | VUS-PR | VUS-ROC |
|-------|------------|--------|---------|--------|---------|
| **Moirai-base** | Zero-shot | 0.321 | 0.678 | 0.312 | 0.763 |
| | Full-FT | 0.387 | 0.757 | 0.354 | 0.827 |
| | **OFT** | **0.389** | 0.756 | **0.361** | **0.834** |
| | LoRA | 0.388 | **0.763** | 0.352 | 0.829 |
| **Chronos-t5-base** | Zero-shot | 0.318 | 0.675 | 0.290 | 0.752 |
| | Full-FT | 0.361 | 0.727 | 0.318 | 0.796 |
| | **HRA** | **0.367** | **0.738** | **0.336** | **0.816** |
| | OFT | 0.361 | 0.733 | 0.338 | 0.813 |
| **Time-MoE-base** | Zero-shot | 0.339 | 0.669 | 0.318 | 0.756 |
| | Full-FT | **0.386** | **0.750** | **0.392** | **0.843** |
| | LoRA | 0.384 | 0.757 | 0.372 | 0.830 |
| | OFT | 0.379 | 0.762 | 0.365 | 0.840 |

> 结论：
- 所有模型经适应后性能显著提升，尤其在**VUS-PR/VUS-ROC**上增益明显（+0.04~0.08）
- **PEFT方法普遍优于或媲美full fine-tuning**
- 在dense模型（Moirai、Chronos）上，**OFT/HRA/LoRA全面领先**
- 在MoE模型（Time-MoE）上，**full fine-tuning仍占优**，PEFT未能完全释放潜力

---

#### ▶️ 参数效率分析（Table 4）
| PEFT | Moirai-base (#params / %) | Chronos-t5-base (#params / %) | Time-MoE-base (#params / %) |
|------|----------------------------|-------------------------------|------------------------------|
| IA3 | ~43K / 0.05% | ~0.1M / 0.06% | ~27K / 0.02% |
| LoRA | ~1.1M / 1.28% | ~3.5M / 1.73% | ~1.6M / 1.47% |
| OFT | ~0.8M / 0.94% | ~2.5M / 1.27% | ~4M / 3.48% |
| HRA | ~0.5M / 0.64% | ~1.7M / 0.87% | ~0.7M / 0.65% |

> 结论：
- PEFT仅需更新**不到2%的参数**即可获得接近甚至超越全量微调的表现
- **adapter-based方法（LoRA/OFT/HRA）显著优于IA3**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **TSFMs是强大的通用异常检测骨干**：
   - 即使在零样本设置下，TSFMs也普遍优于task-specific基线，证明其具备跨域泛化能力。
2. **PEFT是高效且有效的适配方式**：
   - LoRA、OFT、HRA等方法在**仅更新极小比例参数**的情况下，性能**匹配甚至超越全量微调**，极大降低了部署成本。
3. **VUS-based指标更能反映实际价值**：
   - 在存在时间错位的实际场景中，VUS-PR/VUS-ROC比AUC-PR/AUC-ROC更合理，应成为标准评估指标。
4. **架构影响适配效果**：
   - Dense TSFMs（如Moirai）更容易通过轻量级PEFT获得高性能；
   - MoE架构（如Time-MoE）对PEFT响应较弱，可能需要**专家感知的定制化适配机制**。

### ⚠️ 局限性
- 当前研究局限于**单变量时间序列**，尚未验证在多变量、流式或长周期场景下的有效性。
- PEFT在MoE模型上的表现仍有差距，表明现有方法对**expert routing机制的建模不足**。
- 所有实验基于MSE误差打分，未探索其他异常评分函数（如概率输出、注意力异常度等）的影响。

### 🔮 未来工作方向
- 设计面向MoE架构的**expert-aware PEFT方法**（如路由门控微调、专家专属adapter）
- 将TSFM+PEFT框架拓展至**多元、实时、少样本/零样本异常检测场景**
- 探索更丰富的异常评分机制，结合不确定性估计、注意力模式变化等信号
- 构建更大规模、更具挑战性的公开TSAD benchmark，推动公平比较

---

## 总结
该论文系统论证了**Time Series Foundation Models + PEFT** 是一种**高效、可扩展、高性能的时间序列异常检测新范式**。它不仅减少了对大量标注数据和昂贵训练过程的依赖，还展示了基础模型在跨任务迁移中的巨大潜力，为构建“统一时间序列智能系统”提供了重要实证支持。

</details>

---

### 15. [Controllable Concept Bottleneck Models](https://arxiv.org/abs/2601.00451)

**Authors**: Hongbin Lin, Chenyang Ren, Juangui Xu, Zhengyu Hu, Cheng-Long Wang, Yao Shu, Hui Xiong, Jingfeng Zhang, Di Wang, Lijie Hu  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.00451v1  

#### Abstract
Concept Bottleneck Models (CBMs) have garnered much attention for their ability to elucidate the prediction process through a human-understandable concept layer. However, most previous studies focused on static scenarios where the data and concepts are assumed to be fixed and clean. In real-world ap...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Controllable Concept Bottleneck Models 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Concept Bottleneck Models (CBMs)** 虽然具有良好的可解释性，但通常被视为静态模型，一旦训练完成便难以修改。然而，在现实世界的应用中，模型需要持续维护和更新，例如：
- **移除错误或敏感数据**（Machine Unlearning）
- **修正标注错误的概念标签**
- **添加新样本以适应环境变化**（Incremental Learning）

对这些需求进行重新训练（retraining）成本高昂，尤其是在大规模应用中。因此，如何在不从头开始训练的情况下高效地编辑 CBM 成为一个重大挑战。

### 提出的新方法与思路
本文提出了 **Controllable Concept Bottleneck Models (CCBMs)**，一种支持动态、可控编辑的 CBM 框架。其核心创新在于：
- **多粒度可控性**：支持三种不同层次的模型编辑：
  1. **Concept-label-level**：修正单个样本上的错误概念标签。
  2. **Concept-level**：添加或删除整个概念（如医学中的风险因子）。
  3. **Data-level**：双向操作，包括数据删除（Unlearning）和数据添加（Incremental Learning）。
- **基于 Influence Functions 的闭式近似**：利用 **Influence Functions** 推导出数学上严谨的参数更新闭式解，避免了昂贵的重新训练。
- **结合 EK-FAC 加速计算**：引入 **Eigenvalue-corrected Kronecker-Factored Approximate Curvature (EK-FAC)** 来加速高维参数空间下的 Hessian 矩阵计算，提升方法的可扩展性。

### 相比现有方法的优势
- **高效性**：相比完全重新训练，CCBMs 将更新时间从数小时缩短到几分钟，实现了超过 100 倍的速度提升。
- **准确性**：在保持极高效率的同时，性能（如 F1 分数）与重新训练的结果高度接近，远优于标准 Influence Function 基线（CBM-IF）。
- **全面性**：首次系统性地将模型编辑能力引入 CBM，覆盖了从数据到概念再到标签的完整生命周期管理。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在四个多样化的基准数据集上进行：
- **OAI**：膝关节骨关节炎 X 光分级数据集，含 36,369 个样本，10 个医学相关概念。
- **CUB**：鸟类识别数据集（Caltech-UCSD Birds），含 11,788 张图像，112 个二值属性作为概念。
- **CelebA**：人脸属性数据集，含 202,599 张图像，40 个二值属性，其中 8 个作为标签，32 个作为概念。
- **Derm7pt**：皮肤镜图像用于黑色素瘤诊断，含 1,011 张图像，7 个临床定义的皮损标准作为概念。

### 实验设置和评估指标
#### 编辑场景设置
- **Concept-label-level**：随机选择 3% 的样本并翻转其中一个概念标签。
- **Concept-level**：随机移除 OAI 中 1 个概念，CUB 中 10 个概念。
- **Data-level**：
  - **Removal**：随机删除 3% 的训练数据。
  - **Addition**：先保留 10% 数据不参与初始训练，之后将其加入以测试增量学习能力。

#### 评估指标
- **F1 Score**：衡量模型效用（Utility），平衡精确率与召回率。
- **Runtime (RT)**：以分钟为单位，评估模型更新所需的计算时间。

### 基线方法对比
- **Retrain**：完全重新训练模型，作为性能上限的黄金标准。
- **CBM-IF (Ours)**：仅使用标准 Influence Function 的基线方法。
- **CCBM (Ours)**：本文提出的完整方法，结合 EK-FAC 加速。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 编辑级别 | 方法 | OAI F1 | OAI RT (min) | CUB F1 | CUB RT (min) |
|---------|------|--------|-------------|--------|-------------|
| Concept Label | Retrain | 0.8825 | 297.77 | 0.7971 | 85.56 |
| | CBM-IF | 0.8650 | 4.58 | 0.7710 | 1.28 |
| | **CCBM (Ours)** | **0.8809** | **2.28** | **0.7918** | **0.63** |
| Concept | Retrain | 0.8448 | 258.84 | 0.7811 | 87.21 |
| | CBM-IF | 0.8248 | 4.87 | 0.7580 | 1.44 |
| | **CCBM (Ours)** | **0.8411** | **2.29** | **0.7794** | **0.51** |
| Data Removal | Retrain | 0.8811 | 319.37 | 0.7838 | 86.20 |
| | CBM-IF | 0.8477 | 4.99 | 0.7625 | 1.41 |
| | **CCBM (Ours)** | **0.8799** | **2.42** | **0.7851** | **0.60** |
| Data Addition | Retrain | 0.8801 | 323.83 | 0.7987 | 87.93 |
| | CBM-IF | 0.8479 | 5.03 | 0.7635 | 1.44 |
| | **CCBM (Ours)** | **0.8806** | **2.51** | **0.7967** | **0.63** |

### 与基线方法的对比结果
- **速度优势显著**：CCBMs 的运行时间普遍在 **2–3 分钟**，而重新训练需 **85–324 分钟**，提速超 **100 倍**。
- **性能更优**：CCBMs 的 F1 分数几乎与重新训练持平，且显著优于 CBM-IF 基线（例如在 CUB 上平均高出约 0.015–0.03）。
- **稳定性强**：在图 3 中，即使编辑比例高达 10%，CCBMs 的性能下降仍非常平缓，与重新训练的差距极小（< 0.0025）。

### 消融实验与额外分析
- **周期性编辑实验**（图 7–9）：模拟连续 10 轮逐步清理噪声，结果显示 CCBMs 性能恢复轨迹与重新训练高度一致，未出现误差累积导致的崩溃，证明其长期维护的可行性。
- **可解释性验证**（图 4）：通过影响函数量化概念重要性，移除高影响力概念导致性能急剧下降，而移除低影响力概念几乎无影响，说明 CCBMs 能准确识别关键语义特征。
- **隐私合规验证**（图 5）：使用 **RMIA (Removed Membership Inference Attack)** 评分，显示经过 CCBMs 删除的数据其成员推断得分显著降低，趋近于非成员分布，证明其有效实现“遗忘”。

---

## 4. 关键结论和发现

### 主要发现
1. **CCBMs 实现了高效且准确的模型编辑**：通过 Influence Functions 和 EK-FAC，能够在无需重新训练的情况下，对 CBM 进行多粒度、双向的动态更新。
2. **性能与效率达到卓越平衡**：在多个真实数据集上，CCBMs 在保持接近重新训练性能的同时，将更新时间压缩至分钟级。
3. **支持可持续、可信的 AI 生命周期管理**：CCBMs 不仅可用于纠错，还能满足 GDPR 等法规要求的“被遗忘权”，并支持增量学习，使模型能够持续进化。

### 方法的局限性
1. **非凸优化下的近似误差**：Influence Functions 基于损失函数局部二次近似的假设，在深度神经网络的高度非凸景观中可能存在偏差，尤其在大规模编辑时。
2. **Hessian 计算开销**：尽管使用 EK-FAC 加速，对于拥有数十亿参数的基础模型，Hessian 或 Fisher 矩阵的存储与计算仍是瓶颈，可能需要进一步稀疏化或近似。

### 未来工作方向
- 将 CCBMs 扩展到更大规模的视觉-语言模型（如 CLIP）或大语言模型（LLMs）。
- 探索更高效的 Hessian 近似方法，以适配超大规模模型。
- 研究如何将人类专家反馈自动转化为概念层级的编辑指令，实现更紧密的 **Human-AI 协同进化**。

> **总结**：本文提出的 **CCBMs** 是首个支持多粒度、高效编辑的可解释模型框架，为构建**动态、可持续、可信的 AI 系统**提供了坚实基础，推动了可解释人工智能（XAI）从“事后解释”向“事中控制”的范式转变。

</details>

---

### 16. [A Sparse-Attention Deep Learning Model Integrating Heterogeneous Multimodal Features for Parkinson's Disease Severity Profiling](https://arxiv.org/abs/2601.00519)

**Authors**: Dristi Datta, Tanmoy Debnath, Minh Chau, Manoranjan Paul, Gourab Adhikary, Md Geaur Rahman  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.00519v1  

#### Abstract
Characterising the heterogeneous presentation of Parkinson's disease (PD) requires integrating biological and clinical markers within a unified predictive framework. While multimodal data provide complementary information, many existing computational models struggle with interpretability, class imba...

---

### 17. [ClinicalReTrial: A Self-Evolving AI Agent for Clinical Trial Protocol Optimization](https://arxiv.org/abs/2601.00290)

**Authors**: Sixue Xing, Xuanye Xia, Kerui Wu, Meng Jiang, Jintai Chen, Tianfan Fu  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.00290v1  

#### Abstract
Clinical trial failure remains a central bottleneck in drug development, where minor protocol design flaws can irreversibly compromise outcomes despite promising therapeutics. Although cutting-edge AI methods achieve strong performance in predicting trial success, they are inherently reactive for me...

---

### 18. [Task-Driven Kernel Flows: Label Rank Compression and Laplacian Spectral Filtering](https://arxiv.org/abs/2601.00276)

**Authors**: Hongxi Li, Chunlin Huang  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.00276v1  

#### Abstract
We present a theory of feature learning in wide L2-regularized networks showing that supervised learning is inherently compressive. We derive a kernel ODE that predicts a "water-filling" spectral evolution and prove that for any stable steady state, the kernel rank is bounded by the number of classe...

---

### 19. [Cloud-Native Generative AI for Automated Planogram Synthesis: A Diffusion Model Approach for Multi-Store Retail Optimization](https://arxiv.org/abs/2601.00527)

**Authors**: Ravi Teja Pagidoju, Shriya Agarwal  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.00527v1  

#### Abstract
Planogram creation is a significant challenge for retail, requiring an average of 30 hours per complex layout. This paper introduces a cloud-native architecture using diffusion models to automatically generate store-specific planograms. Unlike conventional optimization methods that reorganize existi...

---

### 20. [Constructing a Neuro-Symbolic Mathematician from First Principles](https://arxiv.org/abs/2601.00125)

**Authors**: Keqin Xie  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00125v1  

#### Abstract
Large Language Models (LLMs) exhibit persistent logical failures in complex reasoning due to the lack of an internal axiomatic framework. We propose Mathesis, a neuro-symbolic architecture that encodes mathematical states as higher-order hypergraphs and uses a Symbolic Reasoning Kernel (SRK)--a diff...

---

### 21. [Bio-inspired Agentic Self-healing Framework for Resilient Distributed Computing Continuum Systems](https://arxiv.org/abs/2601.00339)

**Authors**: Alaa Saleh, Praveen Kumar Donta, Roberto Morabito, Sasu Tarkoma, Anders Lindgren, Qiyang Zhang, Schahram Dustdar Susanna Pirttikangas, Lauri Lov\'en  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00339v1  

#### Abstract
Human biological systems sustain life through extraordinary resilience, continually detecting damage, orchestrating targeted responses, and restoring function through self-healing. Inspired by these capabilities, this paper introduces ReCiSt, a bio-inspired agentic self-healing framework designed to...

---

### 22. [Cost-Performance Analysis of Cloud-Based Retail Point-of-Sale Systems: A Comparative Study of Google Cloud Platform and Microsoft Azure](https://arxiv.org/abs/2601.00530)

**Authors**: Ravi Teja Pagidoju  
**Category**: cs.DC  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00530v1  

#### Abstract
Althoughthereislittleempiricalresearchonplatform-specific performance for retail workloads, the digital transformation of the retail industry has accelerated the adoption of cloud-based Point-of-Sale (POS) systems. This paper presents a systematic, repeatable comparison of POS workload deployments o...

---

### 23. [Dynamic Bayesian Optimization Framework for Instruction Tuning in Partial Differential Equation Discovery](https://arxiv.org/abs/2601.00088)

**Authors**: Junqi Qu, Yan Zhang, Shangqian Gao, Shibo Li  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00088v1  

#### Abstract
Large Language Models (LLMs) show promise for equation discovery, yet their outputs are highly sensitive to prompt phrasing, a phenomenon we term instruction brittleness. Static prompts cannot adapt to the evolving state of a multi-step generation process, causing models to plateau at suboptimal sol...

---

### 24. [Reinforcement-Learned Unequal Error Protection for Quantized Semantic Embeddings](https://arxiv.org/abs/2601.00186)

**Authors**: Moirangthem Tiken Singh, Adnan Arif  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00186v1  

#### Abstract
This paper tackles the pressing challenge of preserving semantic meaning in communication systems constrained by limited bandwidth. We introduce a novel reinforcement learning framework that achieves per-dimension unequal error protection via adaptive repetition coding. Central to our approach is a ...

---

### 25. [Can Optimal Transport Improve Federated Inverse Reinforcement Learning?](https://arxiv.org/abs/2601.00309)

**Authors**: David Millard, Ali Baheri  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00309v1  

#### Abstract
In robotics and multi-agent systems, fleets of autonomous agents often operate in subtly different environments while pursuing a common high-level objective. Directly pooling their data to learn a shared reward function is typically impractical due to differences in dynamics, privacy constraints, an...

---

### 26. [The Reasoning-Creativity Trade-off: Toward Creativity-Driven Problem Solving](https://arxiv.org/abs/2601.00747)

**Authors**: Max Ruiz Luyten, Mihaela van der Schaar  
**Category**: cs.LG  
**Published**: 2026-01-05  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2601.00747v1  

#### Abstract
State-of-the-art large language model (LLM) pipelines rely on bootstrapped reasoning loops: sampling diverse chains of thought and reinforcing the highest-scoring ones, mainly optimizing correctness. We analyze how this design choice is sensitive to the collapse of the model's distribution over reas...

---

### 27. [Quantitative Rule-Based Strategy modeling in Classic Indian Rummy: A Metric Optimization Approach](https://arxiv.org/abs/2601.00024)

**Authors**: Purushottam Saha, Avirup Chakraborty, Sourish Sarkar, Subhamoy Maitra, Diganta Mukherjee, Tridib Mukherjee  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2601.00024v1  

#### Abstract
The 13-card variant of Classic Indian Rummy is a sequential game of incomplete information that requires probabilistic reasoning and combinatorial decision-making. This paper proposes a rule-based framework for strategic play, driven by a new hand-evaluation metric termed MinDist. The metric modifie...

---

### 28. [Ask, Clarify, Optimize: Human-LLM Agent Collaboration for Smarter Inventory Control](https://arxiv.org/abs/2601.00121)

**Authors**: Yaqi Duan, Yichun Hu, Jiashuo Jiang  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2601.00121v1  

#### Abstract
Inventory management remains a challenge for many small and medium-sized businesses that lack the expertise to deploy advanced optimization methods. This paper investigates whether Large Language Models (LLMs) can help bridge this gap. We show that employing LLMs as direct, end-to-end solvers incurs...

---

### 29. [An AI Monkey Gets Grapes for Sure -- Sphere Neural Networks for Reliable Decision-Making](https://arxiv.org/abs/2601.00142)

**Authors**: Tiansi Dong, Henry He, Pietro Li\`o, Mateja Jamnik  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2601.00142v1  

#### Abstract
This paper compares three methodological categories of neural reasoning: LLM reasoning, supervised learning-based reasoning, and explicit model-based reasoning. LLMs remain unreliable and struggle with simple decision-making that animals can master without extensive corpora training. Through disjunc...

---

### 30. [Will LLM-powered Agents Bias Against Humans? Exploring the Belief-Dependent Vulnerability](https://arxiv.org/abs/2601.00240)

**Authors**: Zongwei Wang, Bincheng Gu, Hongyu Yu, Junliang Yu, Tao He, Jiayin Feng, Min Gao  
**Category**: cs.AI  
**Published**: 2026-01-05  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2601.00240v1  

#### Abstract
LLM-empowered agents can exhibit not only demographic bias (e.g., gender, religion) but also intergroup bias triggered by minimal "us" versus "them" cues. When this intergroup boundary aligns with an agent-human divide, the risk shifts from disparities among human demographic groups to a more fundam...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
