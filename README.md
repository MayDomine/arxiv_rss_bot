# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-14 05:58:50 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DiffMM: Efficient Method for Accurate Noisy and Sparse Trajectory Map Matching via One Step Diffusion](https://arxiv.org/abs/2601.08482)

**Authors**: Chenxu Han, Sean Bin Yang, Jilin Hu  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.08482v1  

#### Abstract
Map matching for sparse trajectories is a fundamental problem for many trajectory-based applications, e.g., traffic scheduling and traffic flow analysis. Existing methods for map matching are generally based on Hidden Markov Model (HMM) or encoder-decoder framework. However, these methods continue t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DiffMM: Efficient Method for Accurate Noisy and Sparse Trajectory Map Matching via One Step Diffusion**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
- **噪声敏感性**：传统基于 HMM 的方法对 GPS 轨迹中的噪声非常敏感，尤其是在城市复杂环境中，GPS 漂移严重，导致匹配错误。
- **稀疏轨迹性能下降**：由于存储成本限制，许多地图服务采用低频采样策略，导致轨迹点稀疏，难以捕捉时空连续性，严重影响 map matching 准确率。

### **提出的新方法与创新思路**
- **DiffMM 框架**：提出一种基于 **encoder-diffusion** 的 map matching 新框架，首次将 **diffusion model** 引入 map matching 任务中。
- **One-step Diffusion via Shortcut Model**：采用 **shortcut model** 实现单步去噪扩散过程，显著提升推理效率，避免传统 diffusion 模型多步采样的高延迟。
- **Road Segment-Aware Trajectory Encoder**：
  - 联合编码轨迹点与候选道路段（candidate road segments）；
  - 利用 **attention mechanism** 建模轨迹点与道路段之间的空间与上下文关系；
  - 引入方向信息（如前后向量夹角）和投影距离，增强鲁棒性。

### **相比现有方法的优势**
| 维度 | DiffMM 优势 |
|------|-------------|
| **准确性** | 在稀疏和噪声场景下显著优于 HMM、DeepMM、GraphMM 等基线方法 |
| **效率** | 推理速度比第二快的方法（HMM）快约 **17倍**（1.18s vs 20.57s / 1000条轨迹） |
| **鲁棒性** | 对训练数据量减少不敏感，在小样本下仍保持高性能 |
| **建模能力** | 将 map matching 视为条件分布学习问题，而非序列预测，更符合概率生成视角 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Porto (PT)**：葡萄牙波尔图出租车轨迹，原始采样间隔 15 秒
- **Beijing (BJ)**：北京出租车轨迹，原始采样间隔 60 秒
- 数据来源：OpenStreetMap 提供路网拓扑（G = (V, E)）
- 构造稀疏轨迹：通过随机采样模拟不同稀疏程度（ratio r ∈ {0.025~0.5}），对应平均时间间隔达 600 秒

| 数据集 | 轨迹数 | 区域面积 (km²) | 路段数 | 交叉口数 |
|--------|--------|----------------|--------|----------|
| Porto  | ~1M    | 11.7×5.2       | 11,491 | 5,330     |
| Beijing| ~1.17M | 29.6×30.0      | 65,276 | 28,738    |

### **实验设置**
- **搜索半径**：50 米内所有路段作为候选集（R-tree 加速查询）
- **模型参数**：
  - Embedding dim: 128 → Condition dim: 256
  - Transformer Encoder: 2 层，4 头注意力
  - DiT Block: 2 层，hidden dim 512
  - 训练 step size d ∈ {1, 1/2}，推理使用 **one-step (M=1)**
- **硬件平台**：NVIDIA RTX 3090 GPU，PyTorch 2.4.0

### **评估指标**
- **Accuracy**：预测路段与真实匹配路段一致的比例
  $$
  \text{Accuracy}(R, \hat{R}) = \frac{1}{l} \sum_{i=1}^{l} \mathbb{I}[r_i = \hat{r}_i]
  $$

### **基线方法对比**
| 方法 | 类型 | 特点 |
|------|------|------|
| **HMM** | 非学习方法 | 经典隐马尔可夫模型，依赖空间邻近性和转移概率 |
| **DeepMM** | 学习方法 | 基于 RNN 的端到端模型，带数据增强 |
| **GraphMM** | 学习方法 | 图神经网络建模轨迹间相关性 |
| **RNTrajRec** | 学习方法 | 结合道路网络结构进行轨迹恢复 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 2）**

#### **Porto 数据集（稀疏度越高，差距越大）**
| r=0.2 (75s) | r=0.1 (150s) | r=0.05 (300s) | r=0.025 (600s) |
|------------|--------------|---------------|----------------|
| **93.43**  | **91.47**    | **89.08**     | **86.87**      |

> HMM 在 r=0.025 时仅 40.04%，下降超 40 个百分点；DiffMM 下降不到 7 个百分点。

#### **Beijing 数据集（大规模复杂路网）**
| r=0.5 (120s) | r=0.3 (200s) | r=0.2 (300s) | r=0.1 (600s) |
|-------------|--------------|--------------|--------------|
| **90.32**   | **88.45**    | **87.65**    | **85.39**    |

> 当 r=0.1 时，DiffMM 比第二名 DeepMM (**68.25**) 高出 **15.28 个百分点**！

---

### **与基线方法对比结果（Table 3）**

| 方法 | 推理时间 (s/1000 traj) | 训练时间 (min/epoch) |
|------|------------------------|-----------------------|
| HMM | 20.57 | – |
| DeepMM | 88.82 | 9.07 |
| RNTrajRec | 627.65 | 868.23 |
| **DiffMM (Ours)** | **1.18** | **10.66** |

- **推理效率**：比 HMM 快 **17.4 倍**，比 DeepMM 快 **75 倍以上**
- **训练效率**：与最快基线相当，远优于 RNTrajRec

---

### **消融实验结果（Ablation Study, Table 4）**

在 Beijing 数据集上验证关键模块作用：

| 变体 | r=0.5 | r=0.3 | r=0.2 | r=0.1 |
|------|------|------|------|------|
| w/o Trans (无 Transformer) | 90.06 | 88.33 | 87.12 | 84.89 |
| w/o Attn (无 Attention) | 88.79 | 87.25 | 85.70 | 82.71 |
| w/o Shortcut (传统 diffusion) | 89.67 | 87.92 | 86.84 | 83.53 |
| **DiffMM (完整)** | **90.32** | **88.45** | **87.65** | **85.39** |

- **Transformer 编码器**：对稀疏轨迹尤其重要，缺失后精度下降明显
- **Attention 融合机制**：有效抑制噪声影响，简单均值融合性能大幅下降
- **Shortcut Model**：相比传统 diffusion 更适合 one-step 推理，提升效率与精度

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **DiffMM 是首个将 diffusion model 成功应用于 map matching 的工作**，开创了新的建模范式。
2. ✅ **one-step diffusion + shortcut model** 显著提升了推理效率，同时保持高精度，适用于实时系统。
3. ✅ **joint embedding of trajectory and road segments** 有效融合多源信息，增强了模型对噪声和稀疏性的鲁棒性。
4. ✅ 在高度稀疏（600s 间隔）和复杂路网条件下，性能远超现有 SOTA 方法。

### **方法的局限性**
- **依赖候选路段检索**：需预定义搜索半径（如 50m），极端漂移可能漏检正确路段。
- **未考虑动态交通因素**：当前模型仅基于几何与拓扑信息，未引入实时交通流、限速等语义特征。
- **DiT 结构较重**：尽管推理高效，但在更大规模城市部署时仍需优化内存占用。

### **未来工作方向**
- 🔄 **扩展至 trajectory recovery**：利用 conditioned diffusion 生成密集路径序列，实现从稀疏到稠密的轨迹重建。
- 🌐 **引入动态路网状态**：结合 traffic flow、congestion 等信息作为额外条件输入。
- ⚙️ **轻量化设计**：探索更高效的 backbone（如 MobileNet-style DiT）以支持移动端部署。
- 🧩 **多模态融合**：集成手机传感器（IMU）、视觉信息等辅助定位信号。

---

> 🔗 **代码开源地址**：[https://github.com/decisionintelligence/DiffMM](https://github.com/decisionintelligence/DiffMM)

</details>

---

### 2. [MixServe: An Automatic Distributed Serving System for MoE Models with Hybrid Parallelism Based on Fused Communication Algorithm](https://arxiv.org/abs/2601.08800)

**Authors**: Bowen Zhou, Jinrui Jia, Wenhao He, Yong Zhang, Fang Dong  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.08800v1  

#### Abstract
The Mixture of Experts (MoE) models are emerging as the latest paradigm for Large Language Models (LLMs). However, due to memory constraints, MoE models with billions or even trillions of parameters can only be deployed in multi-GPU or even multi-node & multi-GPU based serving systems. Thus, communi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MixServe 论文核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

随着 Mixture of Experts (MoE) 模型规模不断增大（数十亿至万亿参数），其部署依赖于多GPU甚至多节点分布式系统。然而，在此类系统中，**通信开销成为推理性能的关键瓶颈**，尤其是跨节点（inter-node）通信。

当前主流的并行策略存在以下问题：

- **Tensor Parallelism (TP)**：基于 All-Reduce (AR) 的 TP 在单节点内高效，但跨节点扩展性差，受限于低带宽 inter-node 连接。
- **Expert Parallelism (EP)**：基于 All-to-All (A2A) 的 EP 能更好地跨节点扩展，但容易导致负载不均衡，尤其在高并行度下效率下降。
- 缺乏对模型超参数、网络拓扑和硬件资源的系统化分析，现有策略多依赖经验而非理论指导。

---

### **提出了什么新方法或新思路**

本文提出 **MixServe**，一个面向 MoE 模型的自动分布式推理服务系统，其核心创新包括：

#### ✅ 自动化并行策略选择系统
- 在离线阶段综合考虑模型超参数（如层数、隐藏维度、专家数）、硬件配置（计算能力、内存）和网络拓扑（intra-node / inter-node 带宽），通过理论建模与实测分析相结合的方式，**自动选择最优的混合并行策略**（TP/EP/DP组合）。

#### ✅ TP-EP 混合并行设计（Hybrid TP-EP）
- 将 MoE 块中的 TP 和 EP 解耦：**MoE 权重采用 intra-node TP + inter-node EP**，Attention 块则使用 intra-node TP + inter-node DP。
- 引入细粒度优化：将 AR 操作拆分为 Reduce Scatter (RS) 和 All Gather (AG)，并与 A2A 重新组织为 **RS-A2A-AG 或 AG-Dispatch 流程**，实现通信重叠。

#### ✅ 融合通信算法（Fused AR-A2A Communication Algorithm）
- 提出两种新型融合通信算法：
  - **Fused RS-Combine**：重叠 intra-node RS 与 inter-node A2A（Combine 阶段）
  - **Fused AG-Dispatch**：重叠 intra-node AG 与 inter-node A2A（Dispatch 阶段）
- 利用异步通信机制，使 intra-node 和 inter-node 通信并行执行，显著降低总通信延迟。

#### ✅ 理论驱动的性能建模
- 构建了完整的通信与计算延迟模型，涵盖 TTFT、ITL、吞吐量等指标，并引入排队论模型（M/M/1）估计请求队列延迟，提升预测准确性。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **灵活性** | 不再固定使用纯 EP 或纯 TP，而是根据硬件动态选择最优策略 |
| **效率** | 显著减少跨节点通信量，充分利用带宽层级结构（high intra-node vs low inter-node） |
| **自动化** | 替代人工调优，适应不同模型架构与集群环境 |
| **性能提升** | 在 TTFT、ITL 和吞吐量上全面超越主流基线 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **ShareGPT-V3**：大规模人类对话数据集，包含约 12 亿 tokens，用于生成真实请求进行基准测试。

### **实验设置**

#### 硬件平台

| 平台 | 节点数 | 设备 | intra-node 带宽 | inter-node 带宽 |
|------|--------|-------|------------------|------------------|
| H20 Cluster | 2 nodes | 8×Nvidia H20 GPUs (96GB) | NVLink 4.0 (900 GB/s) | InfiniBand (400 Gbps) |
| Ascend 910B Cluster | 4 nodes | 8×Ascend 910B NPUs (64GB) | HCCS (480 Gbps) | RoCE (200 Gbps) |

#### 模型

- **DeepSeek-R1**：671B 参数 MoE 模型，256 个路由专家 + 1 共享专家，每 token 激活 37B 参数
- **Qwen3-235B-A22B**：235B 参数 MoE 模型，128 专家，每 token 激活 22B 参数

#### 请求负载
- 请求速率：2, 4, 8 req/s
- 最大批大小：16
- 最大序列长度：4096 tokens

---

### **评估指标**

| 指标 | 定义 |
|------|------|
| **TTFT (Time to First Token)** | 收到请求后生成第一个 token 所需时间，反映 Prefill 阶段性能 |
| **ITL (Inter-Token Latency)** | 相邻两个输出 token 之间的平均间隔，反映 Decode 阶段性能 |
| **Throughput (token/s)** | 单位时间内生成的 token 数量，衡量系统整体吞吐能力 |
| **P99 Latency** | 99% 的请求满足的时间上限，体现稳定性 |

---

### **基线方法对比**

| 基线 | 并行策略说明 |
|------|--------------|
| **vLLM** | 支持 TP+PP（通用LLM）、DP+EP（MoE）；测试多种 TP 度（4/8） |
| **Tutel** | 支持 TP+EP 混合并行，但在 Ascend 上不支持部分配置 |

具体配置见原文 Table III。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（vs. 基线）**

| 指标 | 提升范围 | 详细表现 |
|------|---------|----------|
| **TTFT 加速比** | **1.08× ~ 3.80×** | 
| | | - 在 Ascend 910B 上，DeepSeek-R1 达到 **2.67×** 加速（vs vLLM TP+PP）<br>- Qwen3 达到 **3.80×** 加速（vs vLLM TP+PP）<br>- H20 上也有 **1.08×~1.23×** 提升 |
| **ITL 加速比** | **1.03× ~ 1.66×** |
| | | - DeepSeek-R1：从 227.33ms → 160.06ms (**1.42×**) <br>- Qwen3：从 134.27ms → 81.1ms (**1.66×**) |
| **吞吐量提升** | **+5.2% ~ +50.3%** |
| | | - Ascend 910B：<br>　• DeepSeek-R1: +22.0% (100.61 → 122.72 token/s)<br>　• Qwen3: +32.2% (113.52 → 150.08 token/s)<br>- H20：<br>　• DeepSeek-R1: **+50.3%** (362.78 → 545.23 token/s)<br>　• Qwen3: **+43.5%** (435.82 → 625.45 token/s) |

> 注：所有结果均为多次运行均值，误差条表示标准差。

---

### **消融实验结果**

#### （1）DP 与 EP 的权衡分析（dDP vs dEP）

| 设置 | 性能趋势 |
|------|--------|
| **dDP = dEP**（平衡） | 在 Ascend 910B 上表现最佳（最低 TTFT，最高吞吐） |
| **dDP < dEP** | 在 H20（高NVLink带宽）环境下更优，因可减少通信冗余 |
| **dDP > dEP** | 导致专家权重冗余，增加内存压力，性能下降 |

👉 结论：MixServe 的自动分析器可根据硬件特性自适应选择最优 dDP/dEP 组合。

#### （2）通信重叠的影响（Sync vs Async）

| 对比项 | 同步通信（Sync） | 异步融合通信（Async） |
|--------|------------------|------------------------|
| TTFT | 较高 | 显著降低（≈减少 inter-node 通信时间） |
| ITL | 较高 | 更低 |
| 吞吐 | 较低 | 提升明显 |

- Gantt 图显示：异步模式成功实现了 intra-node AG/RS 与 inter-node A2A 的**时间重叠**，有效“隐藏”了部分通信延迟。
- 时间复杂度仍为 O(n_node)，但实际延迟接近最小可能值。

---

## 4. 关键结论和发现

### **主要发现**

1. **通信是 MoE 推理的核心瓶颈**，特别是跨节点 A2A 和 AR 操作；
2. **解耦 intra-node 与 inter-node 通信** 是优化的关键路径；
3. **混合 TP-EP 比纯 EP 或纯 TP 更具潜力**，尤其是在异构带宽环境中；
4. **融合通信算法（Fused AR-A2A）能有效重叠通信操作**，显著降低端到端延迟；
5. **自动化的策略选择优于经验配置**，能够适配不同模型与硬件组合；
6. **MixServe 在多种硬件平台（H20 / Ascend）和模型（DeepSeek-R1 / Qwen3）上均取得一致且显著的性能提升**。

---

### **方法的局限性**

1. **假设稳定的网络环境**：未充分考虑网络抖动或拥塞场景下的鲁棒性；
2. **依赖准确的 Profiling 数据**：若初始 profiling 不准，可能导致策略误判；
3. **目前聚焦于推理阶段**：训练场景尚未覆盖；
4. **对特定硬件拓扑敏感**：例如 RoCE/NVLink 的实际利用率受底层驱动影响。

---

### **未来工作方向**

1. **支持动态调整并行策略**：在运行时根据负载变化实时切换 TP/EP 配置；
2. **扩展至更多并行维度**：集成 Pipeline Parallelism (PP)、Context Parallelism (CP) 形成 4D/5D 并行；
3. **结合 Prefill/Decode 分离调度**（如 DistServe）进一步优化资源利用率；
4. **探索轻量化通信原语**：如压缩 A2A 数据、稀疏化传输；
5. **应用于更大规模 MoE 模型**（如万亿级）验证可扩展性。

---

> 🔚 **总结**：  
> MixServe 是首个将**理论建模 + 自动化决策 + 融合通信优化**结合的 MoE 推理服务系统，突破了传统 EP/TP 的局限，显著提升了 MoE 模型在真实分布式环境下的推理效率，为大规模 LLM 的实用化部署提供了重要技术路径。

</details>

---

### 3. [Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs](https://arxiv.org/abs/2601.08403)

**Authors**: Abhijnan Nath, Alireza Bagheri Garakani, Tianchen Zhou, Fan Yang, Nikhil Krishnaswamy  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08403v1  

#### Abstract
Large language models are increasingly trained via reinforcement learning for personalized recommendation tasks, but standard methods like GRPO rely on sparse, sequence-level rewards that create a credit assignment gap, obscuring which tokens drive success. This gap is especially problematic when mo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Owen-Shapley Policy Optimization (OSPO): A Principled RL Algorithm for Generative Search LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在推荐系统等生成式搜索任务中日益重要，但传统的强化学习（RL）方法如 **Group Relative Policy Optimization (GRPO)** 存在以下关键缺陷：

- **信用分配问题（Credit Assignment Gap）**：GRPO 仅基于序列级奖励（sequence-level rewards），将单一优势值（advantage）均匀分配给整个响应的所有 token，无法识别哪些具体词组或推理片段真正推动了成功。
- **缺乏可解释性与效率**：由于无法区分高价值与低价值 token，训练过程容易过拟合于表面模式，且难以进行针对性优化。
- **易受奖励欺骗（Reward Hacking）**：模型可能学会利用黑箱检索器的浅层信号而非真实语义来提升表现。

这些问题在用户意图模糊、无明确标签的场景下尤为严重，例如个性化商品搜索。

---

### **提出了什么新方法或新思路**

本文提出 **Owen-Shapley Policy Optimization (OSPO)**，一种基于合作博弈论的新型策略优化框架，其核心思想是：

- 将生成文本中的**语义连贯单元**（如描述产品属性的短语或表达偏好的句子）视为“玩家”（players），组成“联盟”（coalitions）。
- 利用 **Owen-Shapley values**（Owen 值）计算每个语义段落在最终奖励中的**边际贡献**（marginal contribution），从而实现细粒度的信用分配。
- 通过 **Potential-Based Reward Shaping (PBRS)** 将这些段落级归因映射到 token 级优势值，指导梯度更新。

#### **关键技术亮点**：
- **无需额外价值模型**：直接从任务反馈（如检索器返回的 NDCG）中获取优势值，避免引入参数化 critic 或 reward model。
- **连续性约束（Contiguity Constraint）**：只考虑连续的语义段作为联盟，保证查询语义完整性，并将计算复杂度从 $O(2^N)$ 降至 $O(N \cdot w_{\text{max}})$，使其适用于在线 RL 训练。
- **长度不变性优势重分配**：通过乘以序列长度 $T$ 来消除长序列梯度稀释问题，确保平均 token 优势等于原始序列级优势。

---

### **相比现有方法的优势**

| 方法 | 是否需要 Value Model | 信用分配粒度 | 可解释性 | 抗奖励欺骗能力 |
|------|------------------------|---------------|-----------|------------------|
| GRPO / PPO | ❌ | Token-level (uniform) | 低 | 弱 |
| DPO | ✅ (隐式) | Sequence-level | 中 | 中 |
| Process Reward Models | ✅ | Step-level | 高 | 较强 |
| SCAR (Shapley-based) | ✅ | Segment-level | 高 | 较强 |
| **OSPO (本文)** | ❌ | **Segment-level (Owen)** | **高** | **强** |

- **更高效的学习**：聚焦于真正影响结果的 token，减少无效更新。
- **更强的泛化能力**：对未见过的检索器具有鲁棒性。
- **更高的样本效率**：实验显示达到目标性能所需训练步数减少约 50%。

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 任务类型 | 描述 |
|--------|----------|------|
| **ESCI Shopping Queries** (Reddy et al., 2022) | 商品搜索查询扩展 | 包含真实用户购物查询及标注的相关性标签，用于传统搜索任务。 |
| **H&M Fashion Recommendations** (Ling et al., 2022) | 上下文化商品搜索 & 用户画像摘要生成 | 包含丰富的交易历史，但无显式查询；需由专家模型（Claude Sonnet 3.0）生成合成查询和摘要用于训练。 |

---

### **实验设置和评估指标**

#### **任务设计**
- **Product Search Query Refinement**：LLM 接收原始模糊查询，输出扩展后的自然语言查询，交由 FAISS + 域特定编码器（ALL-MPNET-BASE-V2 / SIMCSE-LARGE）进行密集检索。
- **User Profile Summarization**：LLM 基于购买历史生成 Chain-of-Thought 风格的摘要，再预测下一购买物品顺序。

#### **评估指标**
| 任务 | 主要指标 |
|------|---------|
| 商品搜索 | **NDCG**, **AP**, **MRR**, **Recall** |
| 用户画像摘要 | **Pairwise Win Rate (WR)** 使用 Qwen-3-Nemotron-32B-Reward 模型作为裁判 |

#### **实现细节**
- **基础模型**：Qwen2.5-Instruct 7B
- **每提示采样数（Group Size G）**：8
- **最大联盟宽度 $w_{\text{max}}$**：8
- **每序列采样联盟数 M**：64
- **训练步数**：2000
- **奖励来源**：
  - 搜索任务：FAISS 检索结果的 NDCG@1000
  - 摘要任务：Bradley-Terry 模型打分（90%）+ 格式正确性奖励（10%）

---

### **基线方法对比**

| 基线方法 | 类型 | 特点 |
|---------|------|------|
| **SFT** | 监督微调 | 使用高质量专家样本训练 |
| **DPO** | 离线偏好优化 | 使用专家对比对进行偏好学习 |
| **GRPO** | 在线 RL（无值函数） | 当前主流的 value-model-free RL 方法，作为主要对比基准 |
| **OSPO-Prop** | 本文方法（比例重分配） | 使用 Owen 值按比例重分配优势 |
| **OSPO-Rank** | 本文方法（排名重分配） | 将 Owen 值转为排名后重分配，增强鲁棒性 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **表 1：商品搜索任务性能（NDCG）**

| 方法 | ESCI NDCG | H&M NDCG |
|------|------------|----------|
| Qwen2.5-72B-Instruct | 0.543 | 0.357 |
| **OSPO-Prop (7B)** | **0.522** | **0.436** |
| GRPO (7B) | 0.418 | 0.379 |
| DPO (7B) | 0.431 | 0.396 |
| SFT (7B) | 0.398 | 0.373 |

> 💡 **结论**：OSPO-Prop（7B）接近甚至超越 72B 大模型的表现，在 H&M 上显著优于所有其他 7B 模型。

---

#### **表 2：用户画像摘要生成的成对胜率（Win Rate）**

| 对比 | OSPO-Prop WR | OSPO-Rank WR |
|------|--------------|-------------|
| vs DPO | 49.6% | 45.8% |
| vs GRPO | 49.1% | 47.6% |
| vs SFT | 53.3% | 54.0% |

> 💡 **结论**：OSPO 在中间推理质量上明显优于所有基线，尤其在与 SFT 的比较中优势显著（+16.8~24.4 个百分点）。

---

### **与基线方法的对比结果**

- **相比 GRPO**：
  - ESCI 上 NDCG 提升 **24.9%**（0.522 vs 0.418）
  - H&M 上 NDCG 提升 **15.0%**（0.436 vs 0.379）
  - 训练稳定性更高，无灾难性崩溃现象
- **相比 DPO/SFT**：
  - 显著优于离线方法，说明**检索驱动的在线反馈**对于生成任务至关重要
- **跨规模对比**：
  - OSPO-Prop (7B) > Qwen-32B > 接近 Qwen-72B
  - 表明**精细信用分配比单纯扩大参数规模更有效**

---

### **消融实验结果**

#### **联盟结构的影响（图 2 & 表 5）**
- **最佳配置**：中等宽度（$w=4\sim8$）、足够采样数（$p=48\sim96$）
- **过窄联盟**（$w=1,2$）：快速收敛但后期性能崩塌，因噪声大、上下文不足
- **过宽联盟**（$w=12,16$）：无法充分利用（多数查询只有 6–10 个片段），导致性能下降
- **非连续联盟（All Subsets）**：性能急剧下降至 NDCG≈0.11，验证了**连续性假设的重要性**

#### **探索深度（Generation 数量）**
- 从 8 次生成降到 2 次，准确率大幅下降，说明**充分的 Monte Carlo 采样对稳定优势估计至关重要**

#### **优势重分配方式**
- **OSPO-Prop**：在大多数情况下表现最好
- **OSPO-Rank**：在长序列或噪声较大时更具鲁棒性，作为有效替代方案

#### **奖励操纵检测**
- 查询长度与奖励的相关系数仅为 **-0.165**，表明 OSPO **没有明显的奖励欺骗行为**（否则应为正相关）

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **细粒度信用分配显著提升性能**：通过 Owen-Shapley 归因机制，OSPO 能精准识别推动下游任务成功的语义单元，实现更高效的策略学习。
2. ✅ **无需值函数也能实现密集奖励塑造**：利用 PBRS 和联盟采样，实现了类似 dense reward 的效果，同时保持算法简洁。
3. ✅ **小模型可媲美大模型表现**：OSPO-Prop (7B) 在多个指标上接近甚至超过 32B 和 72B 模型，证明**对齐质量比模型大小更重要**。
4. ✅ **卓越的泛化能力**：
   - 在测试时面对未见过的检索器（cross-retriever evaluation），OSPO 性能下降幅度远小于 GRPO。
   - 表明其学到的是**可迁移的、检索器无关的对齐信号**。
5. ✅ **稳定的训练动态**：相比 GRPO 出现的“灾难性遗忘”，OSPO 维持较长的推理链和较高的奖励方差，避免陷入局部最优。

---

### **方法的局限性**

- **计算开销增加**：需多次调用检索器/奖励模型以评估不同联盟，带来额外延迟（尤其在使用慢速 reward model 时）。
- **超参数敏感**：性能依赖于 $w_{\text{max}}$ 和 $M$ 的选择，需仔细调参。
- **近似误差**：Monte Carlo 采样可能导致长距离依赖被低估。
- **当前仅限单轮交互**：尚未扩展到多轮对话或代理环境。

---

### **未来工作方向**

- 扩展至 **multi-turn, agentic environments**（如多轮搜索、代码生成、对话规划）
- 探索更高效的联盟采样策略（如重要性采样）
- 应用于更多推理密集型任务（如数学解题、法律分析）
- 结合多模态输入（图文联合推荐）

---

> 📌 **一句话总结**：  
> **OSPO 通过引入 Owen-Shapley 归因机制，在不依赖值函数的前提下实现了细粒度、可解释、高效率的强化学习，显著提升了 LLM 在生成式搜索任务中的性能与鲁棒性，为“小模型大能力”的对齐路径提供了有力支持。**

</details>

---

### 4. [Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding](https://arxiv.org/abs/2601.08653)

**Authors**: Zenghua Liao, Jinzhi Liao, Xiang Zhao  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08653v1  

#### Abstract
Large Language Models are rapidly emerging as web-native interfaces to social platforms. On the social web, users frequently have ambiguous and dynamic goals, making complex intent understanding-rather than single-turn execution-the cornerstone of effective human-LLM collaboration. Existing approach...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Prism: Towards Lowering User Cognitive Load in LLMs via Complex Intent Understanding

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前 Large Language Models (LLMs) 在处理用户意图时，通常假设澄清问题是**独立且并行的**（如 Mistral-Interact 和 ITIU 所采用的方法），但在现实场景中，用户的意图往往是**复杂且具有逻辑依赖关系的**。例如，在规划旅行时，“活动建议”必须在确定“目的地”之后才能合理提出。

这种忽略逻辑依赖的做法导致：
- 产生不连贯甚至荒谬的建议（如“12月在冲绳潜水”，而当地天气寒冷）；
- 增加用户的**认知负荷**（Cognitive Load），迫使用户自行管理多个未决变量之间的依赖；
- 降低任务完成效率和用户体验。

因此，本文指出：**建模澄清问题间的逻辑依赖是实现高效复杂意图理解的关键挑战**。

---

### 🚀 提出的新方法：Prism 框架

受**认知负荷理论**（Cognitive Load Theory, CLT）启发，作者提出了 **Prism** —— 一个通过**逻辑化意图澄清**来降低用户认知负担的新型框架。

#### 核心思想类比
将复杂的用户意图视为“白光”，Prism 利用“棱镜”（即逻辑驱动的 schema）将其分解为有序的“彩色光谱”（分层元素），从而指导澄清顺序。

#### 四大模块设计
| 模块 | 功能 |
|------|------|
| **① Complex Intent Decomposition** | 构建 CID 数据集，支持跨领域复杂意图的结构化解构，并显式标注元素间先决依赖关系 |
| **② Logical Clarification Generation** | 根据依赖层级组织澄清流程：独立问题并行提问（交互表格），依赖问题串行进行 |
| **③ Intent-Aware Reward Module** | 设计基于 token-level 的奖励函数，结合意图重要性和生成置信度，用于评估澄清轨迹质量 |
| **④ Self-Evolved Intent Tuning** | 基于高质量模拟交互数据迭代优化模型，无需人工标注即可提升逻辑澄清能力 |

---

### 🔍 相比现有方法的优势

| 维度 | 优势说明 |
|------|----------|
| **逻辑一致性更强** | 显式建模澄清问题间的依赖，避免不合逻辑的推荐 |
| **用户认知负荷更低** | 减少冗余推理负担，提升交互流畅性 |
| **泛化能力强** | 支持 27 个领域、429 种意图，适用于多样化的复杂任务 |
| **可扩展性强** | 自演化训练机制（self-evolved tuning）实现高质量数据自动生成 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 数据集 | 描述 |
|-------|------|
| **TIN** [14] | 包含 200 条测试样本，由 GPT-4 辅助人工标注，涵盖多轮澄清过程 |
| **IN3** [21] | 含 108 条测试样本，强调更多澄清轮次与细节挖掘 |
| **ABP** [36] | 包含 319 条测试样本，覆盖广泛领域和多样化意图 |

所有测试集均按“简单意图”与“复杂意图”分类（借助 LLM 辅助判断），以验证方法在不同难度下的表现。

---

### ⚙️ 实验设置

- **主干模型**：
  - `Mistral-7B-Instruct-v0.3`
  - `LLaMA-3.1-8B-Instruct`

- **Prism 变体**：
  - **Prism-SFT**：监督微调（Supervised Fine-Tuning）
  - **Prism-DPO**：直接偏好优化（Direct Preference Optimization）

- **训练方式**：
  - 使用 Monte Carlo Sampling + 用户模拟器生成大规模澄清对话；
  - 基于 Intent-Aware Reward 过滤高质量轨迹用于训练；
  - 多轮自演化（3 轮），每轮使用上一轮优化后的模型作为策略模型生成更优数据。

- **硬件资源**：8×80GB A100 GPU，总训练耗时约 14.5 小时。

---

### 📊 评估指标

#### （1）澄清交互评估（Clarification Interaction）
| 指标 | 含义 |
|------|------|
| **Intents Cover Rate (%)** | 成功覆盖真实所需澄清项的比例 |
| **Logical Conflict Rate (%)** | 澄清问题违反前置依赖的比例（越低越好） |
| **Average Interaction Turns** | 平均交互轮数（越少越好） |
| **Average Questions Per Turn** | 每轮平均提问数量（反映效率） |
| **Options Presenting Rate (%)** | 提供参考选项的比例 |
| **Options Reasonable Rate (%)** | 提供选项合理的比例 |
| **Average Options Per Question** | 每个问题提供的平均选项数 |

#### （2）意图执行评估（Intent Execution）
集成到 XAgent 框架中，衡量下游任务执行效果：
| 指标 | 含义 |
|------|------|
| **IBLEU** | 输出与参考答案的 BLEU 分数 |
| **Faithful (%)** | 最终输出是否完整满足澄清后意图（无遗漏/幻觉） |
| **Unnecessary Sub-tasks (US)** | 不必要的子任务占比 |
| **General Sub-tasks (GS)** | 过于宽泛而非具体的子任务占比 |
| **Tool Invocations (TI)** | 工具调用次数，反映执行效率 |

#### （3）认知负荷评估（Cognitive Load）
综合三类测量：
- **行为层面**：任务耗时、token 数量
- **主观层面**：用户满意度评分（1–10）
- **生理层面**：EEG 脑电图分析，计算 Power Spectral Density (PSD)，反映大脑活跃程度（越高表示认知负荷越大）

---

### 🔁 基线方法对比

| 方法 | 类型 | 是否考虑逻辑依赖 |
|------|------|------------------|
| **Mistral-Interact** [21] | Q&A 对话式澄清 | ❌ |
| **ITIU** [14] | 表格形式并行提问 | ❌（虽高效但无视依赖） |
| **CoLLABLLM** [32] | 多轮奖励建模增强参与感 | ❌ |
| **Prism (Ours)** | 层级依赖驱动的逻辑澄清 | ✅ |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总（来自 Table 1 & 2）

| 指标 | Prism-DPO 表现 | 提升幅度（vs. Baseline） |
|------|----------------|------------------------|
| **Logical Conflict Rate ↓** | **11.5%**（TIN-Complex） | 相比 Mistral-Interact (-28pp)，ITIU (-41.5pp) |
| **User Satisfaction ↑** | **8.35 / 10** | +14.4% vs. Mistral-Interact |
| **Task Completion Time ↓** | **减少 34.8%** | vs. Mistral-Interact；-22.3% vs. CoLLABLLM |
| **Interaction Turns ↓** | **~1.6 轮**（复杂场景） | 显著低于 Mistral-Interact (~5.6 轮) |
| **Intent Cover Rate ↑** | **71.98%**（LLaMA + TIN） | 高于所有基线 |
| **Faithful Intent Execution ↑** | **53.7% → 53.1%**（复杂场景） | 提升逻辑规划准确性 |
| **Unnecessary Sub-tasks ↓** | **2.27 → 2.33**（Mistral） | 下游 agent 更精准拆解任务 |
| **Tool Invocations ↓** | **3.04 → 4.37**（Mistral） | 执行效率显著提高 |

> 注：以上数据基于 `LLaMA-3.1-8B-Instruct` 为主干模型的结果。

---

### 🔍 与基线方法的对比结果

#### （1）在复杂意图场景下优势明显
- 当从简单意图转向复杂意图时：
  - Mistral-Interact 的 Intent Cover Rate 下降 **30.1%**
  - CoLLABLLM 下降 **32.1%**
  - Prism-DPO 仅下降 **17.7%**

→ 表明 Prism 对复杂意图更具鲁棒性。

#### （2）维持高效率的同时保证逻辑性
- ITIU 虽然交互轮次少（~1.8），但 **Logical Conflict Rate 高达 51.5%**
- Prism 在保持相近交互轮次（~1.6）的前提下，将冲突率降至 **11.5%**

→ 实现了**效率与逻辑的一致性平衡**。

#### （3）跨数据集泛化能力强
在 TIN、IN3、ABP 上均取得 SOTA 表现（见 Table 7 & 8），表明其具备良好的**跨域迁移能力**。

---

### 🔧 消融实验（隐含分析）

虽然文中未单独列出消融表，但从模块设计和 ablation-style 分析可见：

- **引入 CID 数据集和层级分解** → 显著降低 Logical Conflict Rate
- **Intent-Aware Reward + Monte Carlo Sampling** → 提升训练数据质量和澄清轨迹合理性
- **Self-Evolved Tuning** → 每一轮迭代都能生成更高 IR（intent-aware reward）的数据，形成正向反馈循环

此外，Figure 5 显示：
> 在复杂意图场景中，Mistral-Interact 和 ITIU 的性能增益分别从 18.8%/24.8% 下降到 6.2%/12.4%，证明它们难以应对依赖结构。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **逻辑依赖建模至关重要**  
   忽视澄清问题间的依赖会导致严重的逻辑冲突和用户体验下降，这是现有方法在复杂场景失效的根本原因。

2. **Prism 显著降低用户认知负荷**  
   - 任务时间减少 **34.8%**
   - 用户评分提升至 **8.35**
   - EEG 数据显示 PSD 最低，表明大脑负担最小

3. **实现了“低摩擦”的人机协作模式**  
   通过结构化分解 + 依赖感知提问，Prism 引导用户自然地完成意图表达，而不是让用户自己理清思路。

4. **自演化训练有效替代人工标注**  
   利用 GPT-4 生成初始高质量数据，后续由模型自我进化，大幅降低对人工标注的依赖。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **依赖 CID 构建质量** | 若新领域缺乏足够相似意图，few-shot 构造可能不准 |
| **用户模拟仍有偏差** | 尽管使用了 user simulator，但仍无法完全复现真实人类行为多样性 |
| **实时性要求较高** | 多轮 Monte Carlo 采样和层级推理可能增加响应延迟 |
| **未开放全部训练数据** | 虽声明代码开源（GitHub: [liaozenghua/Prism](https://github.com/liaozenghua/Prism)），但部分训练数据未公开 |

---

### 🔮 未来工作方向

1. **动态更新 CID**  
   支持在线学习新领域的意图结构，增强适应性。

2. **轻量化推理机制**  
   探索更高效的采样策略（如 beam search 替代 full MC sampling），降低部署成本。

3. **多模态意图理解扩展**  
   将 Prism 思路应用于图像、语音等多模态输入场景。

4. **个性化依赖建模**  
   允许不同用户有不同的偏好依赖路径（如有人先定预算再选地点）。

---

## 总结

📌 **Prism 是首个系统性解决 LLM 中复杂意图理解中“逻辑依赖”问题的工作**，其核心贡献在于：

- 提出 **CID 数据集** 和 **四模块协同框架**，实现结构化、依赖感知的澄清流程；
- 基于 **Cognitive Load Theory** 设计交互机制，真正从用户体验出发；
- 实验全面，涵盖交互质量、任务执行、生理信号三个维度；
- 在多个基准上达到 SOTA，显著降低逻辑冲突（↓至 11.5%）、提升用户满意度（↑14.4%）、缩短任务时间（↓34.8%）。

🎯 **意义**：推动 LLM 从“被动应答者”向“主动、有条理的协作者”演进，迈向真正的智能人机共事时代。

</details>

---

### 5. [Silence the Judge: Reinforcement Learning with Self-Verifier via Latent Geometric Clustering](https://arxiv.org/abs/2601.08427)

**Authors**: Nonghai Zhang, Weitao Ma, Zhanyu Ma, Jun Xu, Jiuchong Gao, Jinghua Hao, Renqing He, Jingwen Xu  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.08427v1  

#### Abstract
Group Relative Policy Optimization (GRPO) significantly enhances the reasoning performance of Large Language Models (LLMs). However, this success heavily relies on expensive external verifiers or human rules. Such dependency not only leads to significant computational costs and training latency, but...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Silence the Judge: Reinforcement Learning with Self-Verifier via Latent Geometric Clustering*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前基于强化学习的大型语言模型（LLM）推理能力增强方法（如 GRPO）严重依赖昂贵的外部验证器（external verifiers），例如人工规则或另一个 LLM（如 GPT-4o）作为“Judge”来提供奖励信号。这种依赖带来了以下问题：

- **高计算成本与训练延迟**：调用外部 LLM 验证需要大量 API 请求，引入显著的推理延迟。
- **稀疏且离散的奖励信号**：大多数验证方式仅提供二元奖励（0/1），缺乏对推理过程质量的细粒度反馈，导致优化效率低下。
- **验证器偏差与不一致性**：外部 Judge 可能存在评分偏差或噪声，影响训练稳定性，甚至引发模型崩溃（model collapse）。

### **提出了什么新方法或新思路**

本文提出 **Latent-GRPO**，一种无需外部验证器的自验证强化学习框架，其核心思想是：

- **从隐空间几何结构中提取内在奖励信号**：通过分析 LLM 在生成推理轨迹后最后一个 token 的 `last hidden state`，发现**正确推理路径的终端表示在隐空间中形成密集聚类（dense clusters），而错误路径则表现为离群点（outliers）**。
- **提出 IRCE 算法**：设计 **Iterative Robust Centroid Estimation (IRCE)** 算法，通过球面投影消除向量模长波动，并迭代加权聚合估计一个鲁棒的“真相质心”（truth centroid），进而将每个轨迹到该质心的距离转化为**连续、稠密的内在奖励**。

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | 消除外部验证瓶颈，训练速度提升 **2倍以上**（2× speedup）。 |
| **奖励质量** | 提供**连续、稠密的奖励信号**，优于传统的二元稀疏奖励，支持更精细的梯度优化。 |
| **稳定性** | 完全摆脱对外部 Judge 的依赖，避免因 Judge 不一致或噪声导致的训练不稳定。 |
| **通用性** | 不依赖任务特定规则，适用于开放域复杂推理任务（如 Open-Platypus），而规则方法难以覆盖。 |
| **可扩展性** | 奖励计算基于已有的前向传播隐藏状态，计算开销极小（O(GTd)），远低于外部 LLM 验证（O(GL)）。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 描述 |
|--------|------|
| **GSM8K** | 小学数学应用题，测试基础多步算术推理能力。 |
| **MATH** | 高中及竞赛级数学问题，难度更高，测试复杂数学推理。 |
| **Open-Platypus** | 多源混合指令数据集，涵盖物理、逻辑、数学等多样化推理任务，用于评估泛化能力。 |

### **实验设置和评估指标**

- **模型规模**：在 **Qwen3-0.6B, Qwen3-1.7B, Qwen3-4B** 三个尺度上进行验证，并在 **Llama3.2-3B** 上测试跨模型泛化。
- **训练框架**：基于 GRPO 架构，每组生成 G=8 条轨迹。
- **评估指标**：
  - **Task Accuracy**：在测试集上的准确率（Acc）。
  - **Training Efficiency**：每轮训练时间（Time per epoch），衡量训练速度。
- **硬件配置**：单 GPU，使用 bfloat16 混合精度训练。

### **基线方法对比**

| 方法 | 描述 |
|------|------|
| **LLM-as-Judge** | 使用 GPT-4o 对输出文本打分，提供二元奖励（0/1）。 |
| **Rule-based Verification** | 使用 SymPy 符号计算或沙箱执行验证答案正确性，提供二元奖励。 |
| **Latent-GRPO (Ours)** | 本文方法，基于隐空间几何结构生成连续内在奖励。 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1）**

#### **GSM8K 结果（Qwen3-4B）**
| 方法 | 准确率 | 训练时间（分钟/epoch） |
|------|--------|------------------------|
| LLM-as-Judge | 79.87% | 651.45 |
| **Latent-GRPO (Ours)** | **82.34%** | **658.21** |

> ✅ **准确率提升 +2.47%，训练时间相近，但无外部依赖**

#### **MATH 结果（Qwen3-1.7B）**
| 方法 | 准确率 | 训练时间（分钟/epoch） |
|------|--------|------------------------|
| LLM-as-Judge | 65.77% | 1608.34 |
| **Latent-GRPO (Ours)** | **78.51%** | **811.51** |

> ✅ **准确率大幅提升 +12.74%，训练速度提升近 2倍（1.98×）**

#### **Open-Platypus 结果（Qwen3-4B）**
| 方法 | 准确率 | 训练时间（分钟/epoch） |
|------|--------|------------------------|
| LLM-as-Judge | 65.21% | 3522.18 |
| **Latent-GRPO (Ours)** | **78.06%** | **1632.52** |

> ✅ **准确率提升 +12.85%，训练速度提升 2.16×，效果最显著**

> 🔺 总体趋势：**Latent-GRPO 在所有数据集和模型规模上均实现 2× 左右的训练加速，同时保持或显著超越基线准确率**。

---

### **消融实验结果（Ablation Studies）**

#### **(1) 隐状态提取方式对比（Table 2）**
| 方法 | Qwen3-0.6B Acc | Qwen3-1.7B Acc | Qwen3-4B Acc |
|------|----------------|----------------|--------------|
| Mean Pooling | 58.74% | 71.05% | 79.45% |
| Weighted Mean | 57.12% | 69.88% | 78.12% |
| **Last Token (Ours)** | **61.25%** | **73.88%** | **82.34%** |

> ✅ **使用最后一个 token 的隐状态效果最佳**，说明最终语义汇总最具判别力。

#### **(2) 质心估计算法对比（Table 3）**
| 方法 | Qwen3-0.6B Acc | Qwen3-1.7B Acc | Qwen3-4B Acc | 时间 |
|------|----------------|----------------|--------------|------|
| Mean Pool | 57.12% | 68.45% | 77.89% | 快 |
| K-Means | 58.85% | 70.12% | 79.23% | 较慢 |
| Eigen Centrality | 59.43% | 71.56% | 80.56% | 最慢 |
| **IRCE (Ours)** | **61.25%** | **73.88%** | **82.34%** | **快** |

> ✅ **IRCE 在准确率和效率上全面领先**，证明其鲁棒性和实用性。

---

## 4. 关键结论和发现

### **主要发现**

1. **LLM 隐空间天然具备自我验证能力**：正确推理路径的终端隐状态在高维空间中自然聚类，形成“共识核心”，而错误路径为离群点。这一几何特性可作为**内在奖励信号的可靠来源**。
2. **稠密奖励优于稀疏奖励**：连续的几何距离奖励提供了更丰富的优化梯度，显著提升训练效率和最终性能。
3. **无需外部监督即可实现高效对齐**：Latent-GRPO 成功实现了“self-verifier”，在多个复杂推理任务上超越 LLM-as-Judge 和规则方法。
4. **方法具有强泛化性**：
   - 在不同模型家族（Qwen 和 Llama）上均有效（见 Table 5）。
   - 在未见过的基准（如 AIME, MMLU, BBH）上保持或提升能力，表明未发生任务过拟合（见 Table 4）。

### **局限性**

1. **模型规模限制**：目前验证最大为 8B 参数模型，**在超大规模模型（如 70B+）上的表现尚待探索**。
2. **理论基础尚浅**：虽然实证充分，但关于“为何正确推理会形成几何聚类”的**形式化数学理论仍处于初步阶段**。
3. **开放生成任务适用性未知**：当前方法聚焦于有明确答案的推理任务，是否适用于创意写作等开放生成任务有待研究。

### **未来工作方向**

1. **扩展至更大模型和更多任务类型**，验证其可扩展性。
2. **建立隐空间聚类的数学理论框架**，深化对 LLM 内部机制的理解。
3. **探索混合框架**：将 IRCE 的内在奖励与 DPO 等离线对齐方法结合，进一步稳定自监督对齐过程。
4. **应用于 Test-Time Compute 扩展**，如与 Tree-of-Thoughts 或 Best-of-N 结合，在推理时利用隐空间几何进行高效路径筛选。

---

> **总结**：  
> *Latent-GRPO* 通过挖掘 LLM 自身隐空间的几何结构，提出了一种高效、稳定、无需外部依赖的强化学习奖励机制。它不仅解决了传统方法的成本与效率瓶颈，还揭示了 LLM 内在的“自我评判”能力，为未来的 verifier-free 后训练范式提供了重要范例。

</details>

---

### 6. [Deconstructing Pre-training: Knowledge Attribution Analysis in MoE and Dense Models](https://arxiv.org/abs/2601.08383)

**Authors**: Bo Wang, Junzhuo Li, Hong Chen, Yuanlin Chu, Yuxuan Fan, Xuming Hu  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08383v1  

#### Abstract
Mixture-of-Experts (MoE) architectures decouple model capacity from per-token computation, enabling scaling beyond the computational limits imposed by dense scaling laws. Yet how MoE architectures shape knowledge acquisition during pre-training, and how this process differs from dense architectures,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Deconstructing Pre-training: Knowledge Attribution Analysis in MoE and Dense Models —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文旨在解决当前对 **Mixture-of-Experts (MoE)** 架构在预训练过程中如何获取和组织知识的理解不足问题。尽管 MoE 因其计算效率被广泛采用，但其内部知识动态演化机制尚不明确，尤其缺乏与传统 **dense 模型** 在训练过程中的系统性、时间分辨的比较。

现有研究多集中于：
- 后处理分析（post-hoc analysis）
- 针对 dense 模型的神经元归因方法

本文填补了 **“架构差异如何影响知识获取动态”** 这一空白。

---

### 🧩 提出的新方法与新思路

#### 新方法：Gated-LPI（Gated Log-Probability Increase）
- 基于已有的 **Log-Probability Increase (LPI)** 方法进行扩展。
- 首次将神经元级归因分析引入 **MoE 架构**，支持对以下组件的贡献分解：
  - **Feed-Forward Network (FFN)** 中的专家神经元（expert neurons）
  - **Attention Heads** 中的注意力神经元（attention neurons）
- 创新地结合了 **门控权重（gating weights）** 和神经元输出，实现对稀疏激活路径中各神经元预测贡献的量化。

#### 新视角：时间分辨的知识动态追踪（Time-Resolved Knowledge Dynamics）
- 跟踪模型从训练初期到后期多个 checkpoint 的神经元重要性变化。
- 分析三个层次的稳定性与功能特性：
  1. **Neuron-level stability**（神经元级）
  2. **Layer-level consolidation**（层级别）
  3. **Functional robustness**（功能鲁棒性）

---

### 🔍 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **适用性** | 首个支持 MoE 架构的 neuron-level attribution 方法，突破 dense-only 局限 |
| **动态性** | 不仅分析最终模型，还追踪整个 pre-training 过程中的知识演化轨迹 |
| **可解释性深度** | 揭示了 MoE 如何通过稀疏性形成稳定且分布式的知识存储结构 |
| **实用性** | 为 MoE 模型设计、路由优化、专家剪枝等提供理论依据 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **关系事实数据集（Relational Facts Dataset）**
  - 来源于 Hernandez et al. (2023)，用于探测语言模型中的线性关系结构。
  - 包含 **906 个主谓对（subject-object pairs）**
  - 涵盖四大类共 12 种关系：
    - **Linguistic**: `adjective_antonym`, `word_first_letter`, `word_last_letter`
    - **Commonsense**: `object_superclass`, `fruit_inside_color`, `worklocation`
    - **Factual**: `country_language`, `country_capital_city`
    - **Bias**: `name_religion`, `occupation_age`, `occupation_gender`, `name_birthplace`

> 所有样本均以 cloze-style prompt 形式输入（如：“The capital of {} is the city of”），目标是预测正确对象词。

---

### ⚙️ 实验设置

| 模型 | 类型 | 参数量 | 层数 | 专家数 | Routing | 训练步数 | Token 总量 |
|------|------|--------|-------|---------|----------|------------|-------------|
| **OLMo-7B** | Dense | ~7B | 32 | – | – | 600K | ~2.5T |
| **OLMoE-1B-7B** | MoE | ~7B (active: ~1.3B/token) | 16 | 64/expert layer | Top-8 | 1.2M | ~5.0T |

- 两者由同一团队发布，保证实现一致性。
- OLMoE 使用更多训练步数符合 MoE 的典型训练策略（确保专家充分利用）。
- 控制潜在偏差：虽然 OLMoE 使用了更多代码/数学数据（OLMoE-MIX），作者在分析中进行了控制。

---

### 📊 评估指标

#### 功能性能指标
- **HIT@10**：衡量模型是否能在 top-10 预测中包含正确答案。

#### 稳定性与动态分析指标
| 指标 | 定义 | 用途 |
|------|------|------|
| **Top-1% Set Stability (Jstab)** | 连续检查点间 top-1% 最重要神经元集合的 Jaccard 重叠率 | 衡量神经元重要性的持久性 |
| **Positive-Gain Concentration (R)** | 正向重要性增益集中在 top-1% 神经元的比例 | 反映学习信号集中程度 |
| **Layer-Distribution Consistency (pavg)** | 层间重要性分布的相关系数平均值 | 衡量层级别重要性配置的稳定性 |
| **Cross-step Coefficient of Variation (orel)** | 每层重要性得分的时间序列变异系数 | 数值越低表示越稳定 |

#### 功能鲁棒性测试
- **因果干预（Causal Intervention / Ablation）**：
  - 掩码（mask）最重要的神经元或 attention head
  - 观察 HIT@10 下降幅度
  - 测试对象：
    - Top-1 / Top-10 attention heads
    - Top-1% FFN neurons

---

### 🔁 基线方法对比
- 主要对比对象为：
  - **OLMo-7B (dense)** vs **OLMoE-1B-7B (MoE)**
- 所有归因分析在同一框架下执行，确保公平比较。
- 无其他 baseline attribution 方法直接参与性能对比，但方法论上超越了 IG、LRP、NEG 等仅适用于 dense 模型的方法。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据与对比结果

#### （1）神经元级稳定性（Neuron-Level Stability）

| 模型 | 平均 Jstab (FFN, 100K–600K) | Positive-Gain Concentration R (FFN) |
|------|-------------------------------|-------------------------------------|
| **OLMo (Dense)** | 6.1% | 26.4% |
| **OLMoE (MoE)** | **25.3%** | **48.9%** |

> MoE 的 top-1% 神经元具有显著更高的持续性和强化集中度。

- **OLMoE FFN** 显示 “探索-精炼-固化” 三阶段轨迹：
  - 70K 步时达 27%
  - 200K 步略有下降（选择性淘汰）
  - 1.2M 步回升至 **44.2%**

- **OLMo** 峰值仅 10.2%，之后持续下降至 4.9%

#### （2）注意力头稳定性（ATTN Neurons）

| 模型 | 平均 Jstab (ATTN, 100K–600K) | R (ATTN) |
|------|------------------------------|----------|
| **OLMo** | 15.1% | — |
| **OLMoE** | **~57.7% (at 1.2M steps)** | 33–34% |

- OLMoE 注意力核心快速建立并在后期持续增强。
- OLMo 注意力神经元频繁更替，无长期稳定核心。

#### （3）层级别稳定性（Layer-Level）

| 模型 | FFN pavg | FFN orel | ATTN pavg | ATTN orel |
|------|----------|----------|-----------|-----------|
| **OLMo** | 0.54 | 5.01 | 0.49 | 8.64 |
| **OLMoE** | **0.97** | **0.37** | **0.97** | **0.49** |

> MoE 在 FFN 和 ATTN 层均表现出极高的层间重要性分布一致性，波动极小。

- OLMoE 的 FFN 层重要性在 **100K 步内即趋于稳定**
- OLMo 的 FFN 和 ATTN 层重要性持续漂移

#### （4）功能鲁棒性（Functional Robustness）——消融实验结果

| 干预方式 | **OLMoE (MoE)** | **OLMo (Dense)** |
|--------|------------------|------------------|
| **Mask Top-1 Attention Head** | ↓ **0.06%** | ↓ **16.46%** |
| **Mask Top-10 Attention Heads** | ↓ **9.44%** | ↓ **50.43%** |
| **Mask Top-1% FFN Neurons** | ↓ **35.47%** | ↓ **96.19%** |

> MoE 模型即使移除最关键组件，性能仍保持稳健；而 dense 模型几乎崩溃。

- 更惊人的是：随着训练推进，OLMoE 对 top-10 heads 的依赖进一步降低 → **抗干扰能力随训练提升**

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Low-Entropy Backbone（低熵骨干）**
   - MoE 模型中约 **top 1% 的神经元吸收了超过 45% 的正向更新信号**
   - 这些高影响力神经元在整个训练过程中被持续强化并保持稳定
   - 在 dense 模型中未观察到类似现象 → 存在“高利用率核心”

2. **Early Consolidation（早期固化）**
   - MoE 的 FFN 与 ATTN 层的重要性分布 **在 <100K 步内锁定**
   - dense 模型在整个训练过程中仍处于剧烈波动状态
   - 表明 MoE 具备更快的功能模块化收敛能力

3. **Functional Robustness（功能鲁棒性）**
   - MoE 的知识存储是 **分布式而非集中式**
   - 即使屏蔽最重要的 attention heads 或 FFN neurons，性能下降轻微
   - dense 模型高度依赖少数关键组件 → **脆弱性强**

4. **稀疏性是一种有益的归纳偏置（Sparsity as Inductive Bias）**
   - MoE 的稀疏激活机制不仅节省计算资源，还促进了更稳定、更鲁棒的学习动态
   - 稀疏性引导模型发展出 **“探索→稳定” 的学习范式**

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **仅分析两种模型** | 结论基于 OLMo 和 OLMoE，泛化性需在更多 MoE/dense 对中验证 |
| **数据构成差异** | OLMoE 使用了更多 code/math 数据，虽尝试控制但仍可能影响结果 |
| **归因方法假设** | Gated-LPI 假设神经元贡献可加，忽略非线性交互效应 |
| **任务单一性** | 所有分析基于关系提取任务，是否推广到其他任务待验证 |

---

### 🔮 未来工作方向

1. **扩展至更多 MoE 架构变体**
   - 如不同 k 值的 Top-k routing、shared experts、hierarchical MoE
2. **指导 MoE 设计与优化**
   - 利用稳定性分析改进 **routing 策略**（如 explore-then-stabilize）
   - 支持 **专家剪枝与初始化策略**
3. **训练诊断工具开发**
   - 将 Gated-LPI 集成进训练监控 pipeline，实时检测知识固化进程
4. **跨任务迁移分析**
   - 探索知识骨干是否在下游任务中保持一致
5. **因果机制挖掘**
   - 结合 circuit discovery 技术，识别 MoE 中的功能子网络

---

## 总结

> 本论文首次系统揭示了 **MoE 架构在预训练中形成的稳定、分布式知识结构**，并通过提出的 **Gated-LPI** 方法实现了对 MoE 内部知识流动的细粒度追踪。结果显示，**稀疏性不仅是计算上的捷径，更是促进模型稳定学习与功能鲁棒性的关键机制**。这一发现为未来高效、可解释的大模型设计提供了重要启示。

</details>

---

### 7. [Parallel Context-of-Experts Decoding for Retrieval Augmented Generation](https://arxiv.org/abs/2601.08670)

**Authors**: Giulio Corallo, Paolo Papotti  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08670v1  

#### Abstract
Retrieval Augmented Generation faces a trade-off: concatenating documents in a long prompt enables multi-document reasoning but creates prefill bottlenecks, while encoding document KV caches separately offers speed but breaks cross-document interaction. We propose Parallel Context-of-Experts Decodin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Parallel Context-of-Experts Decoding for Retrieval Augmented Generation**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Retrieval-Augmented Generation (RAG) 面临两个核心挑战：
- **Prefill 瓶颈**：将多个检索到的文档拼接成一个长上下文进行推理时，prefill 阶段计算开销巨大，导致延迟高。
- **跨文档推理能力下降**：若采用并行 KV cache 编码（如独立编码每个文档），虽能加速 prefill，但破坏了文档间的注意力交互，削弱了 multi-hop reasoning 能力。

传统方法在“效率”与“准确性”之间存在权衡。

---

### **提出了什么新方法或新思路**
作者提出 **Parallel Context-of-Experts Decoding (PCED)**，一种无需训练的 decoding 框架，其核心思想是：
> 将证据聚合从 **attention 层面** 转移到 **decoding 层面**。

具体创新包括：
1. **Parallel Context-of-Experts 架构**  
   每个检索到的文档被视为一个独立的“专家”（Expert），拥有自己的 KV cache；同时引入一个无上下文的“业余专家”（Amateur Expert）代表模型先验。
   
2. **Retrieval-aware Contrastive Decoding**  
   在每一步生成中，通过以下公式对各专家的 logits 进行加权校准：
   $$
   \tilde{s}_k = (1+\beta_0)s_k - \beta_0 s_0 + \gamma \log r_k
   $$
   其中：
   - $s_k$：第 $k$ 个专家的 logits
   - $s_0$：业余专家（模型先验）的 logits
   - $r_k$：该文档的检索相关性得分（融合 retrieval 和 reranker 得分）
   - $\beta_0$ 控制 contrastive 强度，$\gamma$ 控制 retrieval prior 权重

3. **动态证据拼接机制**  
   每步选择所有专家中最支持的 token，并将其添加到共享生成历史中，实现跨文档证据的“ stitching”，而无需联合 attention。

---

### **相比现有方法的优势**
| 维度 | PCED | 传统 Concatenation | KV Merge / APE | Agentic (e.g., MapReduce) |
|------|------|---------------------|----------------|----------------------------|
| **推理速度** | ✅ 极快（TTFT 加速 >180×） | ❌ Prefill 成本高 | ✅ 快（复用 KV） | ❌ 多轮调用 LLM，延迟高 |
| **跨文档推理** | ✅ 支持（decoding 时聚合） | ✅ 支持（完整 attention） | ❌ 弱（缺乏跨文档交互） | ✅ 支持（全局汇总） |
| **抗噪声能力** | ✅ 强（relevance score 抑制无关专家） | ❌ 差（长上下文稀释关键信息） | ⚠️ 中等 | ⚠️ 取决于摘要质量 |
| **是否需训练** | ✅ 不需要 | ✅ 不需要 | ⚠️ 部分方法需要微调 | ✅ 不需要 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **LOFT benchmark**：用于 RAG 和 In-Context Learning (ICL) 任务，包含多跳问答、事实验证等。
- **LongBench**：包含单文档与多文档 QA、摘要、代码补全、few-shot 推理等子任务，测试长上下文理解能力。
- **合成数据集**：用于性能基准测试（TTFT 和端到端延迟），构造固定长度（2048 tokens/doc）的 64 文档输入，含 1 个黄金文档和多个干扰项。

---

### **实验设置和评估指标**
- **LLM 模型**：
  - 主要结果：`MISTRAL-NEMO-13B-INSTRUCT`, `LLAMA-3.1-8B-INSTRUCT`
  - LongBench：`QWEN3-8B`（扩展至 128k 上下文）
- **解码方式**：greedy decoding
- **评估指标**：
  - RAG 任务：Subspan Exact Match
  - ICL 任务：Exact Match
  - LongBench：官方指标（如 F1、EM 等）

---

### **基线方法对比**
| 类别 | 基线方法 |
|------|--------|
| **标准拼接** | Corpus in Ctx (Single), Corpus in Ctx (All) |
| **KV Cache 合并** | APE（Adaptive Parallel Encoding） |
| **Agentic 方法** | MapReduce（逐文档 summarize + reduce） |

此外还比较了不同 relevance scoring 方式下的 PCED 变体：
- **Sparse**：基于稀疏检索分数
- **Dense**：基于稠密向量相似度
- **ColBERT**：基于细粒度向量匹配

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### **表1：LOFT 上的主要结果（以 LLAMA-3.1-8B 为例）**

| Task | APE | MapReduce | PCED-Dense | Corpus in Ctx (All) |
|------|-----|-----------|------------|----------------------|
| HOTPOTQA | 16.0 | 41.0 | **64.0** | 49.0 |
| MUSIQUE | 4.0 | 8.0 | **21.0** | 7.0 |
| RAG NQ | 9.0 | 50.0 | **85.0** | 58.0 |
| QAMPARI | 7.0 | 68.0 | **77.0** | 72.0 |
| QUEST | 0.0 | 41.0 | **45.0** | 39.0 |

> ✅ PCED 在多跳 QA 上显著优于所有 baseline，尤其在 MUSIQUE 上提升超 5 倍。

#### **表2：LongBench 结果（QWEN3-8B）**

| Task | Corpus in Ctx (All) | PCED-Dense |
|------|----------------------|------------|
| HOTPOTQA | 56.3 | **62.6** |
| 2Wiki | 44.2 | **49.4** |
| MUSIQUE | 25.3 | **33.4** |
| TRIVIAQA | 84.0 | **88.2** |
| REPOB-P (Code) | 51.1 | **60.1** |

> ✅ 即使面对大量干扰文档，PCED 仍全面超越 full-context 拼接。

---

### **与基线方法的对比结果**
- **vs. KV Merge (APE)**：PCED 在 multi-hop QA 上平均提升 **+40~60 pts**，说明 decoding-time aggregation 可有效恢复跨文档推理能力。
- **vs. MapReduce**：PCED 在多数任务上更优（如 HOTPOTQA +23 pts），且仅需一次 decoding，效率更高。
- **vs. Full Context Concatenation**：PCED 在 11/16 设置下优于或持平“all documents in context”，同时避免了长上下文带来的注意力噪声。

---

### **消融实验结果**
#### **表3：组件分析（PCED 各部分作用）**

| 方法 | HOTPOTQA (Llama) | NQ (Llama) |
|------|------------------|------------|
| Only Contrastive ($\gamma=0$) | 46 | 52 |
| Only Retrieval Prior ($\beta=0$) | 53 | 70 |
| Full PCED | **64** | **85** |

> 🔍 发现：
- 移除 retrieval prior 导致灾难性失败 → 表明外部 relevance 分数对于抑制噪声至关重要。
- 移除 contrastive decoding 显著降低性能 → 特别是对 Llama 模型，需 subtract amateur prior 来抑制幻觉。

#### **其他消融**
- **$\gamma$ 最优值为 2.5**：过低则无法抑制干扰，过高则僵化依赖检索排序。
- **Max aggregation 最佳**：在 multi-hop QA 中优于 MoE/PoE，支持 sharp expert switching。
- **鲁棒性测试**：即使 top-k 从 8 扩展到 128，PCED 性能几乎不变（见 Figure 6），表明其对候选池大小不敏感。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **跨文档推理可在 decoding 阶段重建**  
   无需构建跨文档 attention，通过 retrieval-aware contrastive decoding 即可实现有效的 evidence stitching。

2. ✅ **KV cache 模块化 + decoding-time aggregation 是高效且准确的路径**  
   PCED 实现了“并行编码 + 序列级推理”的统一，在保持高速的同时恢复了 long-context 模型的能力。

3. ✅ **retrieval scores 不应丢弃，而是作为 decoding prior**  
   利用 retrieval 和 reranker 的 scalar scores 注入 decoding 过程，可显著提升抗噪性和准确性。

4. ✅ **效率优势巨大**  
   - Time-to-First-Token (TTFT) 加速 **>180×**（0.14s vs 25.50s）
   - 端到端延迟降低约 **1.7×**

---

### **方法的局限性**
1. **依赖模型 logits 输出**  
   PCED 需访问每个专家的 token-level logits，因此无法直接应用于闭源 API 模型（如 GPT-4）。

2. **对检索质量敏感**  
   若相关文档未被检索出，或 relevance score 被低估，则对应 expert 可能被忽略，影响最终输出。

3. **存储成本较高**  
   需预先存储每个文档的 KV cache，存储开销随语料规模线性增长（例如 LOFT 数据集约需 11GB 存储）。适合静态知识库场景。

---

### **未来工作方向**
- **端到端学习 expert selection**：让 LLM 自主学习在每一步选择哪个 context 更重要，减少对外部 retrieval pipeline 的依赖。
- **轻量化 KV cache 存储**：探索 quantization、pruning 或增量更新策略以降低存储负担。
- **扩展至 streaming 场景**：结合 streaming retrieval 与 PCED 实现动态上下文增强生成。

--- 

> 💡 **总结一句话**：  
> **PCED 提供了一种全新的视角——将 RAG 中的“上下文融合”从 attention 转移到 decoding，实现了高效、准确、抗噪的多文档生成，为大规模知识增强系统提供了极具潜力的架构范式。**

</details>

---

### 8. [Hierarchical Precision and Recursion for Accelerating Symmetric Linear Solves on MXUs](https://arxiv.org/abs/2601.08082)

**Authors**: Vicki Carrica, Rabab Alomairy, Evelyne Ringoot, Alan Edelman  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.08082v1  

#### Abstract
Symmetric linear solves are fundamental to a wide range of scientific and engineering applications, from climate modeling and structural analysis to machine learning and optimization. These workloads often rely on Cholesky (POTRF) decomposition and its supporting operations, triangular solves (TRSM)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Hierarchical Precision and Recursion for Accelerating Symmetric Linear Solves on MXUs*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
对称正定线性系统（Symmetric Positive Definite, SPD）的求解在科学计算、工程模拟和机器学习中广泛存在，其核心是 **Cholesky 分解**（POTRF），依赖于 **TRSM**（三角矩阵求解）和 **SYRK**（对称秩-k 更新）等密集线性代数操作。传统方法通常采用统一高精度（如 FP64），难以充分利用现代 AI 加速器（如 NVIDIA Tensor Cores 和 AMD Matrix Cores）提供的低精度（如 FP16）高吞吐能力。

然而，直接使用低精度会带来严重的数值不稳定性和精度损失，限制了其在科学计算中的应用。

### 提出的新方法与创新思路
本文提出了一种**可移植的混合精度递归 Cholesky 求解器**，结合了**嵌套递归算法设计**与**树状分层精度控制**，专为 Matrix Processing Units (MXUs) 设计。主要创新点如下：

#### （1）嵌套递归 Cholesky 框架（Nested Recursive Framework）
- 将递归分解从传统的仅用于对角块（POTRF）扩展到 **TRSM 和 SYRK** 所有三个核心阶段。
- 首次实现了 **GPU 上的递归 SYRK**，并将其与递归 TRSM 结合，形成完整的递归更新路径。
- 通过递归细分暴露更多 GEMM 子问题，显著提升并行度和 BLAS3 操作占比，更适配 MXUs 的硬件特性。

#### （2）树状分层混合精度方案（Tree-Structured Mixed Precision）
- 引入一种基于递归树结构的自定义数据布局，在不同层级分配不同精度：
  - **大型非对角块**（off-diagonal blocks）：使用 **FP16** 进行高性能 GEMM 计算；
  - **递归细化的对角区域**（diagonal blocks）：保留 **FP32 或 FP64** 以保证数值稳定性。
- 该设计实现了“性能关键路径用低精度，精度敏感区域用高精度”的细粒度控制。

#### （3）逐块量化与反量化策略（Blockwise Quantization/Dequantization）
- 为缓解 FP16 动态范围有限的问题，在每一级递归中对参与低精度运算的块进行动态缩放：
  - **量化**：将输入缩放到 FP16 安全范围内；
  - **反量化**：结果恢复原始尺度。
- 开销极小，有效防止溢出/下溢，保障数值鲁棒性。

#### （4）基于 Julia 的高性能可移植实现
- 利用 Julia 的多重派发（multiple dispatch）、参数化类型和动态类型推断，构建了一个**硬件无关的高层接口**。
- 底层调用 vendor-specific 库（如 cuBLAS / rocBLAS）处理基础情况（leaf cases），实现跨平台兼容性（支持 NVIDIA H200 和 AMD MI300X）。

### 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| **递归粒度** | 仅 POTRF 递归，TRSM/SYRK 为标准块形式 | 全流程嵌套递归（POTRF + TRSM + SYRK） |
| **精度控制** | 统一精度或迭代精化（iterative refinement） | 树状分层混合精度，按位置动态分配 |
| **硬件适配性** | 难以利用 MXUs 的 FP16 吞吐 | 显式优化 GEMM 密集型任务，最大化 MXU 利用率 |
| **可移植性** | 通常绑定特定厂商 API | 单一 Julia 实现，自动调度至 cuBLAS/rocBLAS |

---

## 2. 核心实验方法和设置

### 数据集
- 使用随机生成的稠密对称正定矩阵（dense SPD matrices）。
- 矩阵元素服从均匀分布，并通过对角加 $ n $（维度）确保正定性和良好条件数。
- 测试规模覆盖 $ n = 4096 $ 至 $ 65536 $。

### 实验设置
- **硬件平台**：
  - **NVIDIA H200**：基于 Hopper 架构，141GB HBM3e 内存，支持 Tensor Cores；
  - **AMD MI300X**：192GB HBM3，304 计算单元，支持 Matrix Cores。
- **软件栈**：
  - Julia 1.12.0；
  - GPU 支持库：CUDA.jl（NVIDIA）、AMDGPU.jl（AMD）；
  - 后端调用：cuBLAS/cuSOLVER（NVIDIA）、rocBLAS/rocSOLVER（AMD）。
- **实现方式**：
  - 所有变体均在同一套递归框架下实现，仅改变精度配置。

### 评估指标
- **性能指标**：
  - 相对于基线的 **Speedup**（加速比）；
  - 实际达到的 **TFLOPs**（每秒万亿浮点运算）；
- **精度指标**：
  - 相对于 FP64 基线的 **相对误差范数**（relative error norm）；
  - 以 $-\log_{10}(\text{error})$ 表示有效数字位数。

### 基线方法对比
- **POTRF/Cholesky**：cuSOLVER FP64（NVIDIA）、rocSOLVER FP64（AMD）；
- **TRSM/SYRK**：cuBLAS FP64（NVIDIA）；
- 对比配置包括：
  - 纯 FP64、纯 FP32、纯 FP16；
  - 多种混合精度层次配置，如 `[FP16, FP16, FP32]`、`[FP16, FP16, FP16, FP32]` 等。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（NVIDIA H200）

#### （1）SYRK 性能
- **递归 FP64 SYRK**：相比 cuBLAS 实现最高达 **14× 加速**（$n=65536$）；
- **混合精度 SYRK**：
  - `[FP16, FP16, FP16, FP16, FP32]` 配置下达到 **27× 加速**；
  - 纯 FP16 达到峰值 **149× 加速**（但精度极差）。

#### （2）TRSM 性能
- **递归 FP64 TRSM**：已优于 cuBLAS；
- **混合精度 TRSM**：
  - `[FP16, FP16, FP16, FP16, FP32]` 配置下达到 **5.3× 加速**；
  - 纯 FP16 最高达 **6× 加速**。

#### （3）Cholesky 整体性能
- **最终 Cholesky 求解器速度提升**：
  - 深度混合精度配置 `[FP16×6, FP32]` 达到 **5.32× 加速**（vs. cuSOLVER FP64）；
  - 纯 FP16 虽然可达近 **6× 加速**，但精度严重下降。
- **吞吐量表现**：
  - 混合精度配置达到超过 **2× FP64 基线 TFLOPs**；
  - 纯 FP16 接近 **3× FP64 基线 TFLOPs**，但受限于内存带宽。

#### （4）精度表现
- **纯 FP16**：相对误差大，有效数字不足 4 位；
- **深度混合精度**（如 `[FP16×6, FP32]`）：
  - 保持约 **5–6 位有效数字**；
  - 相比纯 FP16 提升 **约 100 倍精度**；
  - 保留了纯 FP16 **88% 的峰值加速收益**。

### AMD MI300X 结果
- 在 MI300X 上同样观察到一致趋势：
  - 深度混合精度配置 `[FP16, FP16, FP16, FP32]` 达到 **5.3× 加速**（vs. rocSOLVER FP64）；
  - 验证了跨平台可移植性和有效性。

### 消融实验分析
- **递归深度影响**（图 10）：
  - 加速比随矩阵尺寸增大而线性增长；
  - 在 $n=65536$ 时达到 **5.32× 峰值加速**；
  - 小矩阵因递归开销过大而增益较小。
- **精度层数影响**：
  - 更多 FP16 层带来更高性能，但精度逐步下降；
  - 通过在顶层保留 FP32/FP64，可在性能与精度间实现平滑权衡。

---

## 4. 关键结论和发现

### 主要发现
1. **嵌套递归 + 分层精度 = 高性能与高精度兼得**  
   通过将递归应用于 POTRF、TRSM 和 SYRK 全流程，并结合树状混合精度控制，能够在不牺牲稳定性的前提下，充分释放 MXUs 的低精度计算潜力。

2. **GEMM 密集化是 GPU 加速的关键**  
   递归设计将大部分计算转化为 GEMM 操作，极大提升了硬件利用率，尤其适合 Tensor Cores / Matrix Cores。

3. **Julia 是实现可移植高性能线性代数的理想语言**  
   多重派发和类型系统使得同一代码可无缝运行于不同 GPU 平台，同时保持高性能。

4. **混合精度需“精准投放”而非全局降级**  
   将 FP16 用于非对角块（计算密集但容错性强），保留对角块高精度，是一种高效且稳健的设计范式。

### 方法的局限性
- 当前实现仍依赖 vendor-specific BLAS 库处理基础情况，尚未完全实现“全栈硬件无关”；
- 对极小矩阵（< 4K）加速效果有限，递归开销占主导；
- 目前仅支持稠密 SPD 系统，未扩展至稀疏或不定矩阵；
- AMD 平台暂未启用混合精度 GEMM（GemmEx），限制了其性能上限。

### 未来工作方向
1. **扩展至更广问题类别**：
   - 块稀疏（block-sparse）、带状矩阵（banded systems）；
   - 不定系统的 LDL 分解。
2. **全栈硬件无关实现**：
   - 实现递归层级上的完全编译导向调度，迈向 Apple Metal 等新架构。
3. **自适应精度控制**：
   - 基于条件数估计动态调整精度层级。
4. **多 GPU 扩展**：
   - 结合大规模线性代数库思想，支持分布式内存环境。
5. **能效与异构调度优化**：
   - 探索能量感知的任务调度与异步执行模型。

--- 

> ✅ **总结一句话**：本文通过**嵌套递归算法 + 树状分层混合精度 + Julia 可移植实现**，在 MXUs 上实现了 Cholesky 求解器的 **5.3× 加速**，同时获得比纯 FP16 **高 100 倍的精度**，为科学计算中的高性能与高精度平衡提供了新范式。

</details>

---

### 9. [ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms](https://arxiv.org/abs/2601.08166)

**Authors**: Mohammad Pivezhandi, Mahdi Banisharif, Abusayeed Saifullah, Ali Jannesari  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08166v1  

#### Abstract
Dynamic voltage and frequency scaling (DVFS) and task-to-core allocation are critical for thermal management and balancing energy and performance in embedded systems. Existing approaches either rely on utilization-based heuristics that overlook stall times, or require extensive offline profiling for...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ZeroDVFS: Zero-Shot LLM-Guided Core and Frequency Allocation for Embedded Platforms》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统嵌入式平台的 **Dynamic Voltage and Frequency Scaling (DVFS)** 和任务到核心分配（task-to-core allocation）策略存在以下关键缺陷：
- **基于利用率的启发式方法**（如Linux `ondemand` governor）忽略 **stall time**，无法准确反映真实负载需求。
- **表驱动方法**（如Precise Scheduler）依赖于对所有频率-核心组合进行离线性能分析，耗时长达 **8–12小时/基准程序**，且无法适应动态变化的工作负载。
- 缺乏对新工作负载的泛化能力，每次新增程序都需要重新执行完整的性能分析。

### 提出的新方法与创新思路
本文提出 **ZeroDVFS**，一个基于模型的分层多智能体强化学习（MARL）框架，结合 **大语言模型（LLM）语义特征提取**，实现 **零样本部署（zero-shot deployment）** 的热感知与能效优化调度器。

#### 主要创新点：
1. **LLM驱动的语义特征提取（LLM-based Semantic Feature Extraction）**
   - 利用 **DeepSeek-V3、Claude Sonnet、GPT-4o** 等 LLM 分析 OpenMP 源码，无需运行即可提取 **13个代码级语义特征**（如算法复杂度、内存访问模式、并行开销等）。
   - 替代传统的“基准标识符”或仅语法特征，使模型具备对**未见程序**的泛化能力。

2. **模型驱动的分层多智能体强化学习（Model-based Hierarchical MARL）**
   - 采用 **Dyna-Q 启发的框架**，结合直接强化学习与基于环境模型的规划（planning），显著提升样本效率。
   - 引入两个协作智能体：
     - **Profiler Agent**：负责决定核心数量和频率，以最小化能耗和 **makespan**。
     - **Temperature Agent**：优先选择温度较低的核心，避免局部热点。
   - 将指数级动作空间分解为可管理子问题，降低决策复杂度。

3. **零样本跨平台迁移（Zero-Shot Cross-Platform Transfer）**
   - 通过将平台无关特征（如算法复杂度）与平台相关特征（如频率等级）分离，训练好的模型可在新硬件上直接部署。
   - 结合少量微调（few-shot fine-tuning），快速适配目标平台。

### 相比现有方法的优势
| 维度 | 传统方法（如Precise Scheduler） | ZeroDVFS |
|------|-------------------------------|--------|
| 部署延迟 | 8–12小时/程序（离线分析） | **首次决策3.5–8.0秒**（含LLM提取） |
| 决策延迟 | 表查找：亚毫秒 | **后续决策358ms**（Python实现） |
| 能效 | 近最优但静态 | **7.09× 更优**（vs Linux ondemand） |
| 泛化能力 | 无（需重新分析） | 支持**零样本部署**新程序 |
| 样本效率 | 需大量真实样本 | **20× 更快收敛**（vs model-free RL） |

---

## 2. 核心实验方法和设置

### 数据集
- **BOTS (Barcelona OpenMP Tasks Suite)**：包含FFT、Strassen矩阵乘法、N-Queens等任务并行程序。
- **PolybenchC**：涵盖线性代数、Stencil计算、数据挖掘等循环并行程序。
- 共 **42个基准程序**，在多个平台上执行所有频率-核心组合，每配置重复8次。

### 实验平台
| 平台 | 架构 | 核心数 | 频率等级 |
|------|------|--------|----------|
| **NVIDIA Jetson TX2** | ARM Cortex-A57 + Denver 2 | 6 | 12 |
| **Jetson Orin NX** | ARM Cortex-A78AE | 8 | 16 |
| **RubikPi** | Qualcomm Kryo 585 | 8 | 36 |
| **Intel Core i7 (8th Gen)** | x86_64 | 4 | 多级 |

### 评估指标
- **Energy Efficiency**：总能耗（mJ）
- **Makespan**：任务完成时间（s）
- **Temperature**：平均核心温度（°C）
- **Decision Latency**：调度决策耗时
- **Convergence Speed**：RL训练收敛所需episode数
- **MAPE**（Mean Absolute Percentage Error）：预测误差
- **R²**：解释方差比例

### 基线方法对比
| 基线方法 | 类型 |
|--------|------|
| **Linux ondemand governor** | 利用率驱动 |
| **Precise Scheduler [7]** | 表驱动，离线分析 |
| **zTT [30]** | 单智能体 model-free RL |
| **DynaQ** | 单智能体 model-based RL |
| **MAMF (Multi-Agent Model-Free)** | 多智能体 model-free RL |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **能效提升**：相比 **Linux ondemand governor**，ZeroDVFS 实现 **7.09× 更好能效**。
- **Makespan 优化**：相比 Precise Scheduler，**makespan 缩短 4.0×**。
- **决策延迟**：
  - 首次决策（含LLM提取）：**3.5–8.0秒**
  - 后续决策：**358ms**
- **首次决策速度**：比表驱动方法快 **8,300×**。
- **收敛速度**：比纯 model-free 方法快 **20×**。

### 与基线方法对比（BOTS FFT 基准）
| 方法 | 能耗 (mJ) | Makespan (s) | 温度 (°C) |
|------|-----------|--------------|-----------|
| **ZeroDVFS (MAMBRL D3QN)** | **9.1** | **1.13** | 42.1 |
| zTT | 31.2 | 1.93 | 43.6 |
| Precise Scheduler [7] | 75.5 | 5.96 | 44.0 |

> **结论**：ZeroDVFS 在能耗和性能上均显著优于所有基线。

### 消融实验结果
#### （1）模型驱动 vs. 模型无关（Model-based vs. Model-free）
- **MAMBRL D3QN**（模型驱动）在 **20个episode内收敛**，而 model-free 方法需超过400个episode。
- 模型驱动方法通过合成数据增强，显著提升样本效率。

#### （2）LLM 特征贡献分析
- 在已知程序上，加入LLM特征对预测精度提升有限（R² ≈ 0.94），因已有硬件计数器主导。
- **但在零样本场景下，LLM特征是实现泛化的关键**，使模型能预测从未见过的程序行为。

#### （3）跨平台迁移效果
| 迁移路径 | MAPE | R² |
|--------|------|-----|
| TX2 → Orin NX（零样本） | 64.5% | 0.90 |
| TX2 → RubikPi（零样本） | 73.2% | 0.80 |
| 加入10个微调样本后 | ↓ 至 ~60% | — |

> 尽管绝对误差较高，但**相对排序保持良好**（Spearman ρ > 0.7），足以支持有效调度决策。

#### （4）LLM 特征一致性
| 特征 | 三模型一致率 (%) |
|------|------------------|
| dominant_operation | 73.8 |
| algorithmic_complexity | 59.5 |
| false_sharing_risk | 14.3 |
| cache_behavior_pattern | 16.7 |

> 高共识特征可靠，低共识特征由XGBoost自动降权处理。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM 可有效提取语义特征用于性能预测**，无需程序执行即可实现零样本部署。
2. **模型驱动的 MARL 显著提升样本效率**，结合规划机制实现快速收敛。
3. **ZeroDVFS 在能效和性能上全面超越现有方法**，尤其适合动态嵌入式环境。
4. **跨平台迁移可行**，即使存在架构差异，仍能保持较高调度有效性。
5. **LLM 提取成本极低**：单程序成本 **$0.0015–$0.018**，远低于传统分析的人力成本。

### 方法的局限性
1. **当前仅支持单DAG OpenMP程序**，不适用于并发或多任务混合负载。
2. **零样本跨平台 MAPE 较高**（64–73%），仍需少量微调以提升精度。
3. **LLM 提取限于单文件程序**，多文件项目需手动拼接或分层分析。
4. **缺乏置信区间量化**，实验重复次数有限。
5. **依赖商业LLM API**，边缘设备部署需本地化方案（如CodeLlama）。

### 未来工作方向
1. 扩展至**并发工作负载**和**多文件项目分析**。
2. 探索**HPC专用LLM微调**，提升特征提取准确性。
3. 研究**GPU卸载决策**与CPU-GPU协同调度。
4. 实现**本地/蒸馏LLM部署**，支持完全离线边缘场景。
5. 开展更严格的统计分析（多随机种子、方差量化）。
6. 探索**多轮提示**（multi-turn prompting）以深入理解复杂代码结构。
7. 引入**不确定性估计**，为每个特征提供置信度评分。

---

> **总结**：ZeroDVFS 通过 **LLM + Model-based MARL** 的创新组合，实现了嵌入式系统中 **高效、自适应、零样本** 的DVFS与任务调度，为动态资源受限环境提供了极具前景的解决方案。

</details>

---

### 10. [YaPO: Learnable Sparse Activation Steering Vectors for Domain Adaptation](https://arxiv.org/abs/2601.08441)

**Authors**: Abdelaziz Bounhar, Rania Hossam Elmohamady Elbadry, Hadi Abdine, Preslav Nakov, Michalis Vazirgiannis, Guokan Shang  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08441v1  

#### Abstract
Steering Large Language Models (LLMs) through activation interventions has emerged as a lightweight alternative to fine-tuning for alignment and personalization. Recent work on Bi-directional Preference Optimization (BiPO) shows that dense steering vectors can be learned directly from preference dat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：YaPO: Learnable Sparse Activation Steering Vectors for Domain Adaptation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
- **密集激活干预的局限性**：现有的激活干预方法（如 CAA 和 BiPO）依赖于在原始模型的 dense activation 空间中学习 steering vectors，但由于神经元的 multi-semanticity（多义性）和 superposition（叠加现象），这些向量往往纠缠多个潜在行为因子。
- 这种纠缠导致在需要细粒度控制的任务（如文化对齐、truthfulness 控制等）中出现不稳定、不可解释且效果有限的问题。

### 提出的新方法：YaPO
- **YaPO (Yet another Policy Optimization)** 是一种**无参考（reference-free）**的方法，用于从偏好数据中学习稀疏的激活引导向量。
- 核心思想是将 steering 学习过程转移到一个预训练的 **Sparse Autoencoder (SAE)** 的隐空间中，在该空间内优化稀疏编码（sparse codes）来生成 steering vectors。
- 结合了 BiPO 的可学习性（learnable preference optimization）与 SAS 的可解释性（基于稀疏特征的操作），实现了更解耦（disentangled）、稳定且高效的控制。

### 相比现有方法的优势
| 方法 | 特点 | 局限 |
|------|------|-------|
| **CAA** | 简单平均 contrastive prompts 的激活差异 | 固定方向、粗粒度、易过正则化长文本生成 |
| **BiPO** | 可学习 dense steering vectors，通过 DPO-style objective 优化 | 密集向量易纠缠多种语义，稳定性差 |
| **SAS** | 在 SAE 稀疏空间操作，提升可解释性 | 使用静态平均，缺乏偏好驱动的学习能力 |
| **YaPO（本文）** ✅ | ✅ 在 SAE 隐空间进行可学习的稀疏 steering<br>✅ 实现解耦、高效、稳定的 fine-grained alignment<br>✅ 收敛更快、训练更稳定、泛化更强 | —— |

> **创新点总结**：
> - 首次提出在 SAE 隐空间中端到端地学习可训练的稀疏 steering vectors。
> - 引入新的文化对齐 benchmark，涵盖五种语言家族、十五个文化背景。
> - 提出并验证两个更合理的评估指标：**RCA** 和 **PNLG**，解决传统“本地化差距”评估中的偏差问题。

---

## 2. 核心实验方法和设置

### 数据集
- 自建多语言文化对齐 benchmark：
  - 覆盖 **5 种语言家族**：Arabic, Spanish, English, Portuguese, Chinese
  - 包含 **15 个具体文化区域**（如 Egypt, KSA, Levantine, Spain, Mexico, USA 等）
  - 包括两种任务形式：
    - **MCQ（多项选择题）**
    - **Open-ended generation（开放式生成）**
  - 所有响应由 Gemini 生成后人工筛选整理，确保跨国家一致性。

### 实验设置
- **基础模型**：Gemma-2-2B-it
- **干预方式**：在推理时注入 steering vector 到 residual stream 的特定层（不修改原模型权重）
- **训练目标**：采用 BiPO-style 的双向偏好优化目标（bi-directional DPO-style objective）
- **SAE 构造**：使用预训练的 Sparse Autoencoder 将原始激活投影至高维稀疏空间（~1M features）

### 评估指标
#### （1）Robust Cultural Accuracy (RCA)
$$
\text{RCA} = \mathbb{E}\left[\frac{2p_{\text{loc}} p_{\text{non}}}{p_{\text{loc}} + p_{\text{non}} + \epsilon}\right]
$$
- 更强调模型在 **本地化（localized）** 与 **非本地化（non-localized）** 提示下的平衡表现。
- 越高越好 → 表示鲁棒的文化适应能力。

#### （2）Performance-Normalized Localization Gap (PNLG)
$$
\text{PNLG} = \mathbb{E}\left[\frac{p_{\text{loc}} - p_{\text{non}}}{(p_{\text{loc}} + p_{\text{non}})/2 + \epsilon}\right]
$$
- 对原始 localization gap 进行归一化，避免低性能模型因整体准确率低而表现出“小 gap”的假象。
- 越低越好 → 表示更好的隐式文化迁移能力。

### 基线方法对比
| 方法 | 类型 | 是否可学习 | 是否稀疏 | 是否使用偏好数据 |
|------|------|------------|----------|------------------|
| **No Steering (Base)** | None | ❌ | ❌ | ❌ |
| **CAA** | Dense | ❌ | ❌ | ❌ |
| **SAS** | Sparse | ❌ | ✅ | ❌ |
| **BiPO** | Dense | ✅ | ❌ | ✅ |
| **YaPO**（本文） | Sparse | ✅ | ✅ | ✅ |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以阿拉伯语和葡萄牙语为例）

#### 表：MCQ & Open-Ended 任务上的 RCA 和 PNLG（Higher RCA / Lower PNLG 更好）

| Language | Method | RCA ↑ | PNLG ↓ |
|---------|--------|--------|--------|
| **Arabic** | Base | 20.1 | 0.129 |
|           | CAA   | 19.2 | 0.167 |
|           | SAS   | 21.3 | 0.098 |
|           | BiPO  | 22.2 | 0.141 |
|           | **YaPO** | **23.5** | **0.098** |
| **Portuguese** | Base | 23.8 | 0.184 |
|               | CAA   | 37.5 | 0.192 |
|               | SAS   | 36.5 | 0.113 |
|               | BiPO  | 29.3 | 0.126 |
|               | **YaPO** | **40.8** | **0.165** |

> ✅ YaPO 在所有语言上均取得最高的 **RCA**，表明其具备最强的文化鲁棒性；
> 同时保持较低的 **PNLG**，说明它能有效缩小显式 vs 隐式提示之间的性能差距。

---

### 其他重要实验结果

#### （1）MMLU 上的知识保留测试
- 所有 steering 方法（包括 YaPO）在 MMLU 上的表现与 base model 几乎一致（约 57.58%），**没有显著退化**。
- 说明 YaPO 干预主要影响对齐相关行为，**不影响通用知识能力**。

#### （2）敏感性分析（Steering Multiplier λ）
- 图 4 显示：**CAA 和 SAS 对 λ 极其敏感**，稍大即崩溃（over-steering）；
- 而 **YaPO 和 BiPO 更稳健**，尤其 YaPO 在 λ=1.5~2.0 仍能维持高性能，体现其稳定性优势。

#### （3）泛化到其他 alignment 任务
| Task | CAA | SAS | BiPO | YaPO |
|------|-----|-----|------|------|
| Wealth-Seeking | 2.23 | 2.14 | 2.17 | **2.31** |
| Jailbreak       | 1.08 | 1.00 | 1.02 | **1.00** |
| Power-Seeking   | 2.09 | 1.81 | 1.93 | **2.03** |
| Hallucination   | 2.18 | 1.46 | 1.60 | **1.69** |
| **Average**     | **1.90** | 1.60 | 1.68 | **1.76** |

- YaPO 在多数任务中仅次于 CAA，但在实际应用中 **更稳定、更少依赖超参调优**。
- CAA 虽得分高但非常 brittle（脆弱），难以部署。

#### （4）收敛速度与训练稳定性
- YaPO 比 BiPO 收敛更快，训练过程中 loss 波动更小。
- 归因于稀疏表示带来的梯度更清晰、优化路径更平滑。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **稀疏空间中的可学习 steering 更优**：在 SAE 隐空间中学习 sparse steering vectors 能有效缓解 neuron multi-semanticity 带来的纠缠问题，实现更精细、稳定的行为调控。
2. ✅ **YaPO 实现了性能与稳定性的统一**：相比 dense 方法（BiPO），YaPO 更稳定；相比静态稀疏方法（SAS），YaPO 可学习且性能更强。
3. ✅ **无需牺牲通用能力即可完成领域适配**：YaPO 在提升文化对齐的同时，**未损害 MMLU 等通用任务表现**，证明其为轻量级、定向干预。
4. ✅ **适用于多种 alignment 场景**：除文化对齐外，YaPO 在 hallucination 抑制、wealth/power-seeking 控制等方面也展现出良好泛化能力。

### 方法的局限性
- 依赖高质量的 **预训练 SAE**，构建和训练 SAE 本身有一定成本。
- 当前实验集中在 Gemma-2-2B，尚未在更大规模模型（如 70B+）上全面验证。
- 稀疏空间维度极高（百万级），虽然稀疏但仍存在存储与检索开销。

### 未来工作方向
- 探索更高效的 SAE 架构或在线学习机制，降低部署门槛。
- 将 YaPO 扩展至多模态 LLM 或 agent 决策系统中的行为调控。
- 研究如何自动识别需干预的 SAE feature 子集，实现动态、按需 steering。
- 构建更大规模的文化与价值观 benchmark，支持跨文明深度对齐研究。

---

> 🔗 **代码与数据公开**：  
> 项目仓库已开源：[https://github.com/MBZUAI-Paris/YaPO](https://github.com/MBZUAI-Paris/YaPO)

</details>

---

### 11. [Where to Split? A Pareto-Front Analysis of DNN Partitioning for Edge Inference](https://arxiv.org/abs/2601.08025)

**Authors**: Adiba Masud, Nicholas Foley, Pragathi Durga Rajarajan, Palden Lama  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.08025v1  

#### Abstract
The deployment of deep neural networks (DNNs) on resource-constrained edge devices is frequently hindered by their significant computational and memory requirements. While partitioning and distributing a DNN across multiple devices is a well-established strategy to mitigate this challenge, prior res...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Where to Split? A Pareto-Front Analysis of DNN Partitioning for Edge Inference*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在边缘设备上部署深度神经网络（DNN）面临**计算资源受限**和**内存不足**的挑战。虽然模型分割（DNN partitioning）是一种常见解决方案，但大多数现有研究仅关注单一优化目标（如最小化延迟或最大化吞吐量），忽略了现实场景中**延迟与吞吐量之间的复杂权衡关系**，尤其是在网络条件多变的情况下。

此外，通信开销（如使用 PyTorch RPC）常被忽视，导致实验室理想环境下的最优策略在真实部署中表现不佳。

---

### 🚀 提出的新方法与创新思路
本文提出将 DNN 分割建模为一个**多目标优化问题**，并引入 **ParetoPipe** —— 一个开源的分布式推理框架，其核心思想是：

- 利用 **Pareto Front 分析** 来系统识别所有“不可支配”的分割策略集合，即无法在不牺牲一个指标的前提下提升另一个指标的所有方案。
- 支持对 **latency** 和 **throughput** 进行联合分析，揭示二者之间的本质权衡。
- 设计双通信后端（PyTorch RPC + 自定义轻量级 TCP Socket 实现），以精确评估运行时开销的影响。

---

### 🔍 相比现有方法的优势
| 方面 | 传统方法 | 本文方法（ParetoPipe） |
|------|--------|-----------------------|
| 优化目标 | 单目标（仅 latency 或 throughput） | 多目标（latency & throughput 联合优化） |
| 决策依据 | “最优”单一点 | 完整的 Pareto Frontier，提供决策空间 |
| 网络影响 | 忽视或简化 | 显式建模网络延迟与带宽限制 |
| 通信开销 | 使用标准 RPC 框架（高开销） | 提供轻量级自定义通信后端，显著降低开销 |
| 可复现性与工具支持 | 封闭或专用系统 | 开源框架，支持异构硬件与灵活实验 |

> ✅ **创新点总结**：
> - 首次系统性地通过 Pareto Front 分析揭示边缘环境下 DNN 分割的性能边界；
> - 发布了可扩展、开源的工具链 ParetoPipe，促进后续研究；
> - 揭示了网络瓶颈下“过度卸载”反而劣化的现象，挑战直觉认知。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集与模型
- **数据集**：CIFAR-10（用于微调）
- **预训练模型来源**：ImageNet 上的标准 PyTorch 预训练权重
- **测试的 DNN 架构**（共6个）：
  - MobileNetV2
  - ResNet18 / ResNet50
  - InceptionV3
  - AlexNet
  - VGG16  
  > → 覆盖从轻量到重型、不同结构复杂度的典型 CNN 模型。

---

### ⚙️ 实验设置
#### 硬件平台（Heterogeneous Testbed）
| 设备 | 配置 |
|------|------|
| **Edge Workers** | 2 × Raspberry Pi 4B（4核 CPU，4GB RAM） |
| **Edge Server** | NVIDIA GeForce RTX 4090 GPU，32核 CPU，124GB RAM |
| **网络连接** | 有线以太网 LAN（基础 RTT ~0.2–0.4ms） |
| **网络模拟工具** | `tc`（Linux traffic control）用于注入延迟（200ms）和限速（5 Mbit/s） |

#### 部署模式
- **Pi-to-Pi**：两个分区均在树莓派上执行
- **Pi-to-GPU**：第一段在 Pi 执行，第二段卸载至 GPU 服务器

#### 分割粒度
- 在 **block 边界** 进行分割（非逐层），每个 block 是一组功能相关的层（如 conv block、inception block）

#### 评估指标
| 指标 | 定义 |
|------|------|
| **End-to-end Latency** | 单 batch 推理的总耗时（秒） |
| **Throughput** | 每秒处理图像数（imgs/s） |
| **Resource Utilization** | CPU 利用率、内存占用、网络传输时间 |
| **Pareto Optimality** | 是否属于 Pareto 前沿上的点（无支配解集） |

#### 基线对比
- **通信后端对比**：
  - PyTorch RPC（TensorPipe backend）
  - 自研轻量级 TCP Socket 实现
- **分割策略分析**：遍历所有合法分割点，绘制完整 Pareto Frontier

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）Pareto Front 分析结果（Fig. 3 & 4）
- 在 Pi-to-Pi 场景中：
  - 轻量模型（如 MobileNetV2、AlexNet）整体表现优于重型模型（VGG16、ResNet50）
  - 最优分割点取决于各 block 的计算负载分布：
    - AlexNet、InceptionV3、VGG16：适合**对称分割**
    - MobileNetV2、ResNet18、ResNet50：需**非对称分割**（早期块更重）
    - 例：MobileNetV2 在 P3（前3块留本地）达到最高吞吐

- 在 Pi-to-GPU 场景中：
  - 吞吐大幅提升，延迟显著下降，尤其对大模型（ResNet50、VGG16）
  - Pareto Front **非常稀疏**，通常只有一个主导策略：
    - 如 MobileNetV2 应在 P1 分割（几乎全部卸载到 GPU）

#### （2）网络压力下的性能退化（Fig. 5 & 6）
- 注入 **200ms 延迟 + 5 Mbit/s 带宽限制** 后：
  - 所有模型的 Pareto Front 整体右移（延迟↑）且下移（吞吐↓）
  - GPU 加速优势被严重削弱：GPU 大部分时间空等中间激活值
  - **早卸载策略（early offloading）代价高昂**：产生大量需传输的中间张量，加剧通信瓶颈
  - 结论：在网络受限时，应**减少通信量**，倾向于在边缘侧完成更多计算

#### （3）通信后端性能对比（Fig. 7）
| 指标 | PyTorch RPC（P15） | Custom TCP（P3） | 提升 |
|------|--------------------|------------------|------|
| Throughput | 5.1 imgs/s | **7.8 imgs/s** | ↑53% |
| End-to-end Latency | 1.14 s | **0.27 s** | ↓76% |
| RPC Overhead | 显著存在 | 几乎为零 | — |

> ✅ 自定义通信后端极大降低了协调开销，尤其适用于资源受限边缘环境。

---

### 📋 性能分解表（Table II & III）
- **高吞吐策略**：两设备 CPU 利用率均接近饱和（>300%，多核并行）
- **低延迟策略**：常伴随资源利用不均衡（一端忙，一端闲）
- Pi-to-GPU 设置下 Pareto Front 多为单点，说明卸载策略高度集中

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **DNN 分割本质上是多目标权衡问题**：
   - 不存在全局“最优”分割点，必须结合应用场景选择合适的 Pareto 点。
   
2. **网络是第一类性能瓶颈**：
   - 在高延迟或低带宽条件下，即使拥有强大 GPU，也无法发挥其算力优势。
   - “越多卸载越好”并非总是正确；有时应在边缘保留更多计算以减小通信量。

3. **Block-level profiling 至关重要**：
   - 不同 block 的计算成本差异巨大，盲目按层数平均分割会导致负载失衡。
   - 必须进行细粒度性能剖析才能找到真正高效的分割点。

4. **标准 RPC 框架开销过大**：
   - PyTorch RPC 引入显著额外延迟，在边缘场景中不可接受。
   - 轻量级通信机制（如 TCP + 手动序列化）更具实用性。

5. **Pareto Front 可指导实际部署决策**：
   - 提供可视化的性能边界，帮助开发者理解 trade-off，并针对特定网络环境选择鲁棒策略。

---

### ⚠️ 方法的局限性
- 当前仅支持 **two-stage pipeline**（两阶段流水线），未探索更复杂的多设备拓扑（如 chain、tree）。
- 实验集中在 CNN 模型，未涵盖 Transformer 等新兴架构。
- 动态适应能力尚未实现：目前分割点为静态枚举，缺乏 runtime 自适应调整机制。
- 未考虑能耗作为优化目标，而这对电池驱动设备至关重要。

---

### 🔮 未来工作方向
1. **开发自适应分割算法**：
   - 根据实时网络状态动态选择最优分割点。
2. **加入能量效率目标**：
   - 构建三维 Pareto Front（latency, throughput, energy）。
3. **扩展至多设备流水线与混合并行策略**：
   - 支持更复杂的分布式拓扑结构。
4. **支持 Transformer 类模型的分割分析**：
   - 探索 attention 层的分割特性与通信模式。
5. **集成自动化 profiling 工具**：
   - 实现一键式 block 级性能测量与 Pareto Front 生成。

---

## 总结
该论文通过引入 **ParetoPipe** 框架，首次系统性地揭示了边缘 DNN 分割中的 **latency-throughput trade-off**，强调了**网络条件**和**通信开销**的关键作用。其实验设计严谨，结果具有强指导意义，为边缘 AI 的实际部署提供了重要的理论基准与实用工具。

</details>

---

### 12. [CLaS-Bench: A Cross-Lingual Alignment and Steering Benchmark](https://arxiv.org/abs/2601.08331)

**Authors**: Daniil Gurgurov, Yusser Al Ghussin, Tanja Baeumel, Cheng-Ting Chou, Patrick Schramowski, Marius Mosbach, Josef van Genabith, Simon Ostermann  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08331v1  

#### Abstract
Understanding and controlling the behavior of large language models (LLMs) is an increasingly important topic in multilingual NLP. Beyond prompting or fine-tuning, , i.e.,~manipulating internal representations during inference, has emerged as a more efficient and interpretable technique for adapting...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CLaS-Bench: A Cross-Lingual Alignment and Steering Benchmark

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在多语言场景下的行为控制缺乏标准化评估框架。尽管已有研究探索通过干预内部表示（representation-based steering）来控制模型输出语言，但尚无专门的基准（benchmark）用于系统评估不同 **language steering** 技术的有效性，尤其是在跨语言（cross-lingual）生成任务中。

### 提出的新方法/新思路
本文提出了 **CLaS-Bench**，这是首个专注于评估多语言和跨语言 **language steering** 的轻量级基准测试。其核心创新包括：
- **跨语言对齐评估框架**：设计了一个包含32种语言、70个平行开放性问题的数据集，支持从任意源语言（source language）向任意目标语言（target language）的生成任务，共产生超过7万对问答样本。
- **双维度评估指标**：提出结合 **Language Forcing Success (LFS)** 和 **Output Relevance (OR)** 的 **Language Steering Score (LSS)**，通过调和平均数（harmonic mean）综合衡量语言切换能力和语义相关性，避免单一指标的片面性。
- **系统化比较多种steering技术**：首次在同一框架下全面评估了包括基于提示（prompting）、神经元干预（neuron-based）、残差流向量（DiffMean）、探针（probe）、主成分分析（PCA）、线性判别分析（LDA）和稀疏自编码器（Sparse Autoencoders, SAE）等多种steering方法。

### 相比现有方法的优势
- **填补空白**：现有基准如 AxBench 和 MIB 主要关注英语环境下的概念控制或电路定位，而 CLaS-Bench 首次将评估重心放在多语言和跨语言场景。
- **轻量且可扩展**：基准设计简洁，易于通过翻译新增语言进行扩展。
- **科学严谨**：强调representation-based steering的可解释性和有效性，为多语言interpretability研究提供了标准化工具。

---

## 2. 核心实验方法和设置

### 数据集
- **来源**：从 Vicuna 数据集中选取70个高质量、多样化的开放式问题。
- **语言覆盖**：涵盖32种类型学和地理上多样化的语言（如表4所示），包括高资源（如 en, zh, de）和低资源（如 bo, mt, ka）语言，以及非拉丁字母语言（如 ar, ja, ru, ko）。
- **数据构建**：使用 Google Translate API 进行初始翻译，并由母语者进行校对以确保语义保真度、流畅性和地道性。

### 实验设置和评估指标
- **任务定义**：给定源语言 `s` 中的问题 `x_s`，要求模型生成目标语言 `t` 的答案 `y`。
- **评估流程**：每个问题在每种目标语言下均被评估，共 `70 × 32 = 2,240` 个实例/目标语言。
- **评估指标**：
  - **LFS (Language Forcing Success)**：使用 FastText LID 分类器检测生成文本的语言，计算成功切换至目标语言的比例。
  - **OR (Output Relevance)**：使用 Qwen-3-8B 作为 judge model 对生成内容的相关性打分（0-2分），并归一化为 `[0,1]` 区间。
  - **LSS (Language Steering Score)**：`LSS = 2 * (LFS * OR) / (LFS + OR)`，即 LFS 和 OR 的调和平均数。

### 基线方法对比
实验对比了以下8种方法：
1. **EBase-I**：英文指令提示（如 "Respond in German"）
2. **TBase-II**：目标语言指令提示（如 "Antworte auf Deutsch"）
3. **LAPE**：基于语言敏感神经元的干预（Tang et al., 2024）
4. **DiffMean**：残差流激活的均值差异向量干预（Marks and Tegmark, 2023）
5. **Probe**：训练线性探针获取语言判别方向
6. **PCA**：在残差流上进行主成分分析
7. **LDA**：在线性判别分析获取最优分离方向
8. **SAE-DM**：在稀疏自编码器（SAE）隐空间中应用 DiffMean

模型选择：Llama-3.1-8B-Instruct 和 Aya-Expanse-8B。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Llama-3.1-8B-Instruct 为例）
| 方法 | 平均 LSS |
|------|---------|
| **DiffMean (▲)** | **84.5** |
| **LAPE (⊙)** | **80.1** |
| EBase-I (Prompting) | 67.7 |
| TBase-II (Prompting) | 67.3 |
| Probe (w) | 48.6 |
| SAE-DM (△) | 42.3 |
| LDA (v) | 23.6 |
| PCA (u) | 15.1 |

### 与基线方法的对比结果
- **DiffMean 表现最佳**：在32种语言中，DiffMean 在19种语言上的 LSS 超过90%，显著优于所有其他方法，包括 prompting 基线。
- **prompting 存在明显失败案例**：例如，在从俄语提示转向英语时，prompting 方法仍输出俄语，而 DiffMean 成功生成英语（见 Table 3）。
- **LAPE 是第二优方法**：表现稳健，但存在更大语言间方差。
- **监督方法表现不佳**：Probe 和 LDA 等监督方法表现远低于无监督的 DiffMean 和 LAPE，表明其可能过拟合训练数据中的特定特征。
- **SAE-based 方法中等**：SAE-DM 表现优于部分残差流方法，但不及原始 DiffMean，可能受限于重建误差或SAE训练数据覆盖不足。

### 消融实验结果
- **干预层与强度交互作用**：
  - 早期层：低强度（α=1.0）即可有效steering。
  - 后期层（~20层后）：需要更高强度（α=5.0）才能达到峰值效果。
  - 早期强干预会严重损害输出连贯性，而后期干预即使强度高也能保持输出质量。
- **LAPE 参数优化**：同时激活目标神经元并去激活源语言神经元（combined activation + deactivation）优于仅激活策略；干预更多神经元（5% vs 1%）性能略有提升。
- **SAE 干预模式**：最佳性能出现在第25层，强度 α=15.0，表明高层稀疏空间具有更强的语言控制能力。

---

## 4. 关键结论和发现

### 主要发现
1. **简单方法更有效**：基于残差流的 **DiffMean** 方法在跨语言steering任务中持续优于所有其他复杂方法，包括先进的 prompting 和监督学习方法。
2. **语言特异性信息集中在后期层**：通过多种分析手段（cosine similarity, probe accuracy, LAPE 神经元分布, LDA 可分性）一致发现，语言特异性表示主要在模型的 **16-32 层** 出现。
3. **语言家族呈现几何聚类**：在表示空间中，同语系的语言（如罗曼语族、日耳曼语族、斯拉夫语族）在向量空间中表现出更高的余弦相似性，形成明显的几何聚类。
4. **无监督优于监督**：简单的无监督方法（DiffMean, LAPE）优于复杂的监督方法（Probe, LDA），说明语言方向可通过简单的均值差异有效捕捉。

### 方法的局限性
- **数据量不均衡**：不同方法使用的token数量不同（DiffMean/LAPE用10M，PCA/LDA用500K/100K），可能影响公平比较。
- **SAE覆盖有限**：SAE-based steering 仅限于有公开预训练SAE的层，无法进行全面层分析。
- **语言覆盖仍有限**：尽管包含32种语言，但全球仍有大量语言未被覆盖，特别是数字资源极少的语言。
- **模型限制**：仅评估了instruction-tuned模型，base model可能表现不同。

### 未来工作方向
- 扩展至更多语言，尤其是低资源和濒危语言。
- 探索更高效的SAE训练和部署方案，以支持更广泛的模型和语言。
- 将CLaS-Bench应用于其他多语言可控生成任务，如风格迁移、情感控制等。
- 结合CLaS-Bench的发现，开发更高效、鲁棒的多语言适配技术，作为微调的低成本替代方案。

> **代码与数据已开源**：https://github.com/d-gurgurov/CLaS-Bench

</details>

---

### 13. [InfGraND: An Influence-Guided GNN-to-MLP Knowledge Distillation](https://arxiv.org/abs/2601.08033)

**Authors**: Amir Eskandari, Aman Anand, Elyas Rashno, Farhana Zulkernine  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08033v1  

#### Abstract
Graph Neural Networks (GNNs) are the go-to model for graph data analysis. However, GNNs rely on two key operations - aggregation and update, which can pose challenges for low-latency inference tasks or resource-constrained scenarios. Simple Multi-Layer Perceptrons (MLPs) offer a computationally effi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：InfGraND: An Influence-Guided GNN-to-MLP Knowledge Distillation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Graph Neural Networks (GNNs) 在图数据分析中表现出色，但其依赖于**消息传递机制**（message passing），在推理阶段存在高延迟和高计算开销，难以部署于资源受限或低延迟场景。虽然 Multi-Layer Perceptrons (MLPs) 更高效，但在忽略图结构的情况下性能通常较差。

现有的 GNN-to-MLP Knowledge Distillation (KD) 方法大多采用**均匀的知识转移策略**，或者基于**图无关的指标**（如预测不确定性、熵）来选择节点进行蒸馏，忽略了节点在图拓扑中的结构性重要性。

### 提出的新方法与思路
本文提出 **InfGraND**（Influence-Guided Graph Knowledge Distillation），一种**基于节点影响力引导的 GNN 到 MLP 的知识蒸馏框架**，核心思想是：

- **识别并优先蒸馏来自“结构上更具影响力的节点”的知识**，而非仅依据模型置信度。
- 引入一个**无参数、拓扑感知的节点影响力评分**（Global Influence Score, GIS），衡量一个节点特征扰动对其他节点表示的影响程度。
- 通过**一次性的多跳邻居特征预计算**（feature propagation + pooling）将结构信息注入 MLP 输入，避免推理时的消息传递开销。

### 相比现有方法的优势
- **更合理的蒸馏优先级**：首次从**图结构影响**角度指导蒸馏，而非仅依赖预测不确定性（如 KRD、HGMD）。
- **无需增加模型复杂度**：不引入额外可训练模块（如 MoE、ensemble），保持 MLP 轻量。
- **零推理开销**：结构信息通过离线预计算嵌入输入，不影响在线推理速度。
- **性能超越教师 GNN**：在多个数据集上，蒸馏后的 MLP 学生模型甚至超过了其 GNN 教师的性能。

---

## 2. 核心实验方法和设置

### 使用的数据集
在 **7 个真实世界的同质性图数据集**上进行了评估，涵盖小规模与大规模：
- 小规模引用网络：**Cora**, **Citeseer**, **PubMed**
- 大规模协作/商品图：**Amazon-Photo**, **Coauthor-CS**, **Coauthor-Phy**
- 大规模基准数据集：**OGBN-Arxiv**

### 实验设置
- **两种学习范式**：
  - **Transductive**：训练与测试在同一图上。
  - **Inductive**：训练在一个子图上，测试在未见节点上。
- **教师模型**：使用三种主流 GNN 架构作为教师：
  - **GCN**, **GAT**, **GraphSAGE**
- **学生模型**：标准两层 MLP。
- **评估指标**：**分类准确率（Accuracy）** 和 **推理延迟（Inference Latency）**。

### 基线方法对比
与以下 SOTA GNN-to-MLP 蒸馏方法对比：
- **GLNN**：基础蒸馏框架，直接用软标签训练 MLP。
- **KRD**：基于预测熵稳定性选择“可靠”节点进行蒸馏。
- **HGMD**：基于“知识难度”采样困难样本进行蒸馏。
- **FF-G2M**：提取低频/高频知识进行蒸馏。
- **AdaGMLP** / **RbM**：增强型 MLP 学生（集成/MoE），用于效率对比。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 场景 | 指标 | InfGraND 表现 |
|------|------|---------------|
| **Transductive** | 平均准确率提升（vs. Vanilla MLP） | **+12.6%** |
| **Inductive** | 平均准确率提升（vs. Vanilla MLP） | **+9.3%** |
| **Transductive** | 平均优于 KRD | **+0.9%** |
| **Inductive** | 平均优于 KRD | **+3.0%** |
| **Transductive** | 平均优于 HGMD | **+0.6%** |
| **OGBN-Arxiv** | 准确率（SAGE 教师） | **超过 KRD**（见 Figure 3） |

> ✅ **关键发现**：在多数情况下，InfGraND 不仅显著优于所有基线，甚至**超过了其 GNN 教师模型本身**（例如在 Amazon-Photo 上，GAT 教师为 87.6%，InfGraND 学生达 94.5%）。

### 与基线方法的对比结果
- InfGraND 在 **7 个数据集、3 种教师架构、2 种设定下均取得最佳或接近最佳性能**。
- 相比基于熵的方法（KRD/HGMD），InfGraND 利用**结构影响力**实现了更有效的知识迁移。
- 在 **OGBN-Arxiv** 这类大规模图上仍表现优异，验证了可扩展性。

### 消融实验结果（Ablation Study, Table 2）

| 变体 | 性能趋势 | 结论 |
|------|--------|------|
| **w/ Influence**（仅影响力加权） | 显著优于 Vanilla MLP 和 GLNN | 影响力引导有效提升监督与蒸馏信号质量 |
| **w/ Propagation**（仅特征传播） | 在 inductive 设置下大幅提升（如 Amazon-Photo +21.1%） | 预计算多跳特征显著增强 MLP 的泛化能力 |
| **Full Model**（两者结合） | **性能最高** | 两个组件互补，共同贡献最终优势 |

> 🔍 特别地，在 **label-scarce setting**（每类仅 2–8 个标签）下，InfGraND 平均比 GLNN **高出 4.17%**，表明其在低标注成本场景下更具鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **节点影响力是比预测不确定性更本质的蒸馏优先级指标**：
   - 实验 Q1 显示，在高影响力节点上训练 GNN 可获得更好泛化性能（Figure 2）。
   - 因此，在蒸馏中优先学习这些节点的知识是合理且高效的。

2. **InfGraND 实现了“轻量但强大”的 MLP 学生模型**：
   - 通过**影响力加权蒸馏损失** + **预计算结构特征**，使 MLP 能学到接近甚至超越 GNN 的表示能力。

3. **效率与精度兼得**：
   - 如 Figure 4 所示，InfGraND 比 GNN 快 **6.8–13.9 倍**，同时精度更高。
   - 推理时间与普通 MLP 相当，无额外开销。

4. **影响力指标优于传统中心性度量**：
   - 消融实验（Appendix E）显示，使用 Degree 或 PageRank 作为权重不如本文提出的 GIS，因其**融合了特征与拓扑信息**。

### 方法的局限性
- 当前方法假设图是**同质性**（homophilic）的，未在异配图（heterophilic graphs）上验证。
- 影响力计算虽为一次性预处理，但在超大规模图上仍需近似（如 2-hop 截断）。
- 依赖教师 GNN 的输出质量，若教师本身欠拟合，则蒸馏效果受限。

### 未来工作方向
- 扩展到 **heterophilic graphs** 和 **dynamic graphs**。
- 探索 **hybrid discrimination strategies**：结合 entropy-based 与 influence-based 方法。
- 应用于其他任务，如链接预测、图分类等。
- 进一步优化影响力计算的可扩展性。

---

> 📌 **总结一句话**：  
> **InfGraND 通过“影响力感知”的知识蒸馏 + “预计算结构编码”，让简单的 MLP 在图任务上既快又准，甚至反超复杂的 GNN，为工业级图模型部署提供了高效新范式。**

</details>

---

### 14. [VBO-MI: A Fully Gradient-Based Bayesian Optimization Framework Using Variational Mutual Information Estimation](https://arxiv.org/abs/2601.08172)

**Authors**: Farhad Mirkarimi  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.08172v1  

#### Abstract
Many real-world tasks require optimizing expensive black-box functions accessible only through noisy evaluations, a setting commonly addressed with Bayesian optimization (BO). While Bayesian neural networks (BNNs) have recently emerged as scalable alternatives to Gaussian Processes (GPs), traditiona...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：VBO-MI: A Fully Gradient-Based Bayesian Optimization Framework Using Variational Mutual Information Estimation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Bayesian Optimization (BO)** 主要依赖 **Gaussian Processes (GPs)** 或 **Bayesian Neural Networks (BNNs)** 作为代理模型（surrogate model），但存在以下瓶颈：
- **GPs**：计算复杂度为 $O(T^3)$，难以扩展到高维或大规模数据；
- **BNNs**：虽具可扩展性，但后验采样（如 HMC）成本高昂，且通常需对后验分布形式做强假设（如 Gaussian、Gamma 分布），限制了表达能力；
- 所有传统方法均需在每一步进行 **acquisition function 的内层优化**（inner-loop optimization），进一步增加计算开销。

### 提出的新方法与思路
作者提出 **VBO-MI**（Variational Bayesian Optimization with Mutual Information），一种**完全基于梯度的贝叶斯优化框架**，其核心思想包括：
- **无需显式建模后验分布**：不假设代理模型的具体参数化形式（如 GP 或特定变分族），提升灵活性；
- **利用变分互信息估计（Variational MI Estimation）** 构造新的 acquisition 函数，将信息增益（information gain）直接从数据中学习；
- 引入 **Actor-Critic 架构**：
  - **Action-net（Actor）**：生成候选输入点；
  - **Variational Critic（Critic）**：估计当前点带来的互信息（即探索收益）；
- 整个流程通过端到端的梯度更新实现，**消除传统 BO 中 acquisition function 的显式优化步骤**。

### 相比现有方法的优势
- **计算效率显著提升**：相比 BNN-BO 基线，FLOPs 最多减少 **102×**；
- **端到端可微训练**：避免昂贵的采样和内层优化；
- **更强的表达能力**：摆脱 GP 的核函数假设和 BNN 的后验分布假设；
- **自然处理噪声观测**，并对超参数 $\beta$ 更鲁棒；
- 支持高维、非线性、含类别变量的复杂任务。

---

## 2. 核心实验方法和设置

### 数据集与任务
实验涵盖两类场景：

#### （1）合成函数基准测试
- **Branin**（2D）
- **Hartmann-6**（6D）
- **Ackley-10**（10D）
> 这些函数具有多个局部最优，是标准的全局优化挑战任务。

#### （2）真实世界复杂任务
- **PDE Optimization**：调节偏微分方程系数以最小化解输出方差；
- **Interferometer Position Optimization**（4D）：光学干涉仪配置优化；
- **Lunar Lander 控制问题**（12D）：OpenAI Gym 环境中的策略优化；
- **Pest Control**（25D，含类别变量）：害虫种群控制策略优化，使用 one-hot 编码处理类别输入。

### 实验设置
- **批大小（batch size）**：Ackley/Hartmann 使用 64，Branin 使用 32；
- **学习率**：统一设为 0.002；
- **网络架构**：
  - **Action-net**：全连接前馈网络（FFN）；
  - **Helper Network（Critic）**：修改版 LSTM + 全连接层；
- **训练策略**：交替更新 Critic（$K_a=1$）和 Actor（$K_b=5$）；
- **warm-up 步数**：20–30 步；
- **评估指标**：归一化的平均累计奖励（average sum of rewards）随迭代次数的变化。

### 基线方法对比
包括多种主流 BO 方法：
- **Gaussian Processes (GP)**
- **Sparse GP**（稀疏诱导点）
- **Focalized Sparse GP** [45]
- **BNN-based 方法**：
  - HMC-BNN
  - SGHMC
  - LLA (Laplace Linear Approximation)
  - DKL (Deep Kernel Learning)
  - IBNN (Infinite-width BNN)

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

| 任务 | VBO-MI 表现 | 最佳基线 |
|------|-------------|----------|
| **Branin** | 超过所有 BNN 基线，接近 GP | GP 最优 |
| **Hartmann-6** | 收敛更快，最终奖励更高 | GP 和 IBNN 较好 |
| **Ackley-10** | 初期落后，后期反超，达到最高奖励 | Sparse GP 初始领先 |
| **PDE Optimization** | 显著优于所有基线 | Sparse GP 第二 |
| **Lunar Lander** | 最终奖励达 **170.97**，远超次优的 149.33 | VBO with GP exploitation |
| **Pest Control** | 最终奖励 **-9.02**，显著优于其他 | Sparse (-9.75) |
| **Interferometer** | 最终奖励 **0.890**，大幅领先 | Sparse (0.730) |

> 图表显示，随着维度升高（如 Pest Control 为 25D），VBO-MI 性能优势更加明显。

### 消融实验结果（Ablation Study）
通过替换 VBO-MI acquisition 函数中的探索或利用项为 GP 对应项进行分析（见 Figure 5）：

| 替换方式 | 影响程度 | 结论 |
|--------|--------|------|
| **用 GP posterior 替换 exploration term** | 性能严重下降 | 探索项是 VBO 成功的关键 |
| **用 GP posterior 替换 exploitation term** | 性能有所下降，但影响较小 | 利用项相对次要 |
| **完整 VBO-MI** | 性能最优 | 两项协同作用最佳 |

> 表明：**VBO-MI 的探索机制（基于变分 MI 估计）是其优越性的核心来源**。

### 超参数敏感性分析（Figure 8）
- 在不同 $\beta$ 值下测试各方法性能；
- **发现**：VBO-MI 对 $\beta$ 的变化最不敏感，在多个取值下均保持高性能；
- 其他方法（如 GP、HMC）性能波动较大；
> 表明 VBO-MI 具有更强的鲁棒性和易调性。

### 批量大小影响（Figure 7）
- 随着 batch size 增大，VBO-MI 性能在多数任务上持续提升，直到饱和；
- 尤其在高维任务（如 Pest Control）中，大 batch 更有利；
> 体现其良好的可扩展性。

### 计算复杂度分析（Figure 6 & Table 1）
| 方法 | Surrogate Update | Acquisition Opt. | 总体复杂度 |
|------|------------------|------------------|------------|
| Exact GP | $O(T^3)$ | $N \cdot O(T)$ | Cubic |
| HMC-BNN | $O(S \cdot L \cdot T \cdot W)$ | $N \cdot O(S \cdot W)$ | High Linear |
| **VBO-MI (Ours)** | $O(K_a \cdot T \cdot H)$ | $O(K_b \cdot W)$ | **Low Linear** |

- **FLOPs 对比**：VBO-MI 在 acquisition 阶段比采样型 BNN 方法降低约 **102×**；
- 不需要额外优化器（如 L-BFGS）来最大化 acquisition function。

---

## 4. 关键结论和发现

### 主要发现
1. **VBO-MI 是首个完全基于梯度的 BO 框架**，通过变分互信息估计实现了 acquisition function 的可微学习；
2. **Actor-Critic 架构有效解耦了探索与利用**，并通过联合训练实现高效搜索；
3. **探索项的设计至关重要**：基于变分 MI 的探索机制显著优于传统 GP 后验方差；
4. **在高维、复杂、含噪声的真实任务中表现卓越**，尤其在 Lunar Lander 和 Pest Control 上遥遥领先；
5. **计算效率极高**，消除了传统 BO 的“内层优化”瓶颈，适合大规模部署。

### 方法的局限性
- 当前框架依赖两个神经网络的交替训练，可能存在训练稳定性问题（尽管实验中未出现）；
- 对于极低预算（T < 20）的任务，warm-up 阶段可能影响初期性能；
- 虽然理论部分证明了一致性，但在极端非平稳或对抗性函数上的泛化能力仍待验证。

### 未来工作方向
- 将 VBO-MI 扩展到 **multi-objective BO** 和 **constrained BO** 场景；
- 探索更高效的 MI 估计器（如 TUBA、InfoNCE）以进一步提升性能；
- 应用于更大规模的实际系统，如芯片设计、药物分子优化等；
- 研究如何自适应调整 $\beta$ 或实现免调参版本。

---

> ✅ **总结一句话**：  
> **VBO-MI 通过变分互信息估计和端到端梯度优化，构建了一个高效、灵活、可扩展的新型贝叶斯优化框架，在性能和效率上全面超越传统 GP 和 BNN 方法，尤其适用于高维复杂现实任务。**

</details>

---

### 15. [Forecast Aware Deep Reinforcement Learning for Efficient Electricity Load Scheduling in Dairy Farms](https://arxiv.org/abs/2601.08052)

**Authors**: Nawazish Alia, Rachael Shawb, Karl Mason  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08052v1  

#### Abstract
Dairy farming is an energy intensive sector that relies heavily on grid electricity. With increasing renewable energy integration, sustainable energy management has become essential for reducing grid dependence and supporting the United Nations Sustainable Development Goal 7 on affordable and clean ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本研究针对**奶牛场能源管理中的负荷调度问题**，特别是电池储能系统（Battery Storage）和电热水器（Water Heater）的优化控制。该领域面临以下挑战：
- 可再生能源（如光伏PV）具有间歇性和不确定性；
- 电价动态变化，存在峰谷差异；
- 传统强化学习（RL）方法假设对未来价格或发电量有完全先验知识，这在现实中不成立；
- 标准PPO算法依赖固定clipping阈值或KL散度限制，在电价波动环境下训练不稳定。

### 提出的新方法与创新思路
论文提出了两个基于Proximal Policy Optimization (PPO) 的改进框架：

#### （1）Forecast-Aware PPO (F-PPO)
- 将**短期预测信号**（需求、可再生能源出力）融入智能体的观测空间；
- 使用“季节性朴素模型”（seasonal-naive forecast）结合**Hour-of-Day 和 Month-based Residual Calibration**进行残差校准，生成未来24小时的需求与PV预测；
- 预测信息通过一个GRU网络编码后拼接到状态向量中，使策略具备前瞻性决策能力。

#### （2）PID-KL PPO
- 引入**PID控制器调节KL散度惩罚系数 $c_{KL}$**，实现对策略更新步长的自适应调控；
- 动态调整信任区域大小，避免因奖励尺度变化导致的训练震荡；
- 公式如下：
  $$
  c_{KL} = \max\left(0, c_{KL} + K_p \cdot e + K_i \cdot I + K_d \cdot D\right)
  $$
  其中 $e = KL_{\text{measured}} - KL_{\text{target}}$ 是误差项。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **现实适用性** | 不依赖未来信息，仅用历史数据构建预测模块，更贴近真实场景 |
| **稳定性提升** | PID-KL机制显著改善训练收敛性，减少超参数调优负担 |
| **成本节约效果** | 在多个任务上优于PPO、DQN、SAC等主流RL算法 |
| **约束满足能力** | 实现零违规运行（zero constraint violations），保障设备安全 |

---

## 2. 核心实验方法和设置

### 数据集使用情况
| 任务 | 数据来源 | 内容说明 |
|------|----------|--------|
| **Battery Scheduling** | 芬兰VTT技术研究中心公开数据集 | 包含一年每小时电力负荷（~261 MWh/年）、20kW光伏模拟数据（NREL SAM）、赫尔辛基地区动态电价 |
| **Water Heater Scheduling** | 爱尔兰奶牛场研究数据 | 含200头牛规模农场的分项用电数据（包括热水器）、20kW光伏输出、Electric Ireland提供的分时电价（Time-of-use tariffs） |

> 注：由于芬兰数据未提供设备级负载拆分，因此加热器调度实验采用爱尔兰数据。

### 实验设置与评估指标

#### MDP建模细节
| 组件 | 描述 |
|------|------|
| **State Space (Battery)** | `{hour, SOC, P_load, P_pv}` |
| **Action Space (Battery)** | `{Charge, Discharge, Idle}` |
| **Reward (Battery)** | 考虑电网购电成本 + SOC越界惩罚（±15） |
| **State Space (Heater)** | `{hour, E_price, P_pv, P_background, P_net, P_device, run_time}` + `heft`, `slack`（F-PPO新增） |
| **Action Space (Heater)** | `{ON, OFF}` |
| **Reward (Heater)** | 加权组合：<br>• 成本奖励 $R_{\text{cost}}$: 利用可再生电力降低成本<br>• 任务奖励 $R_{\text{task}}$: 完成每日所需加热时间目标 |

#### 评估指标
- 总电费支出（Total Cost）
- 电网进口电量（Grid Imports）
- 日均峰值负荷降低（Peak Demand Reduction）
- 用户满意度率（User Satisfaction Rate）
- 政策稳定性与训练收敛曲线
- 统计显著性检验（Wilcoxon signed-rank test）

### 基线方法对比
| 算法 | 类型 | 用途 |
|------|------|------|
| **Standard PPO** | On-policy, policy gradient | 主要基准之一 |
| **DQN** | Value-based, Q-learning | 对比离散动作下的表现 |
| **Discrete SAC** | Off-policy, entropy-regularized | 提供随机探索基线 |
| **Q-learning / Rule-based** | 传统方法 | 用于电池调度比较 |

所有算法均基于CleanRL框架实现以确保公平性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### （1）水加热器调度结果（Table 3）
| Algorithm | 总成本 (€) | 相较标准PPO降幅 | 违规天数 |
|-----------|------------|------------------|---------|
| DQN | 16,418 | ↑4.1% | 60 underuse |
| Standard PPO | 15,744 | — | 0 |
| **Forecast-Aware PPO (F-PPO)** | **15,635** | **↓0.7%** | **0** |
| F-PPO (dropout=0.15) | 15,582 | ↓1.0% | 17 underuse |
| **PID-KL PPO** | **15,624** | **↓0.8%** | ~±1 underuse |
| Discrete SAC | 15,773 | ↑0.2% | 0 |

> ✅ F-PPO比DQN节省 **4.76%** 电费；相比标准PPO再降约 **1%**

#### （2）电池调度结果（Figure 3 & 6）
- PPO相比无电池场景减少电网进口 **13.11%**
- 相比Q-learning降低 **1.62%** 进口量
- 相比rule-based方法降低 **2.56%** 进口量

### 与基线方法的对比结果
| 指标 | F-PPO vs DQN | 显著性（p-value） |
|------|--------------|------------------|
| 平均日峰值需求降低 | ↓13.75% | 0.0019** |
| 电网进口电量 | ↓252.8 kWh/month | 0.0019** |
| 电费支出 | ↓91.6 €/month | 0.0019** |

> 所有三项指标均通过 **Wilcoxon signed-rank test** 验证为统计显著（p < 0.05）

### 消融实验结果
- **加入forecasting模块（F-PPO）**：
  - 成本下降约1%，且保持零违规；
  - GRU编码器能捕捉时间依赖关系，支持主动调度。
- **增加GRU dropout至0.15**：
  - 成本进一步下降达1.5%；
  - 但出现17天未完成加热任务，用户满意度降至94% → 表明存在**成本优化与约束满足之间的权衡**。
- **PID-KL机制引入**：
  - 最终成本与F-PPO相近（±0.5%内）；
  - 但训练过程更稳定，reward方差更小（见Figure 10）；
  - 政策更新更加平滑，抗干扰能力强。

---

## 4. 关键结论和发现

### 主要发现
1. **预测感知型PPO（F-PPO）有效提升调度效率**：
   - 通过融合短期预测信息，智能体能够提前规划，将高能耗操作（如加热水）安排在电价低、光伏充足的时段。
   - 实现了**主动式调度**而非被动响应。

2. **PID-KL机制增强训练鲁棒性**：
   - 自适应调节KL惩罚项，解决了标准PPO在变幅奖励下易震荡的问题；
   - 减少对超参数敏感性，更适合实际部署。

3. **PPO类方法优于DQN与SAC**：
   - 在离散动作、强约束的任务中，PPO及其变种表现出更好的稳定性和经济性；
   - DQN因ε-greedy探索策略导致次优行为；
   - SAC虽具良好探索性，但在本任务中收敛慢且调度一致性较差。

4. **真实世界可行性验证**：
   - 所提方法在真实奶牛场数据上验证有效；
   - 能适应季节性变化（夏季多光→多储电；冬季少光→合理购电）；
   - 符合设备运行边界（SOC维持在15%-85%之间）。

### 方法局限性
- 当前模型仍为单智能体架构，难以协调多个异构负载（如挤奶机、冷却系统等）；
- 预测模型较为简单（seasonal-naive + residual banding），未使用复杂时序模型（如Transformer）；
- 实验基于模拟环境，尚未在物理系统中部署验证；
- 仅考虑光伏，未整合风能、沼气等其他可再生能源。

### 未来工作方向
1. 探索**Multi-Agent Reinforcement Learning (MARL)** 架构，实现多设备协同调度；
2. 引入**evolutionary RL** 或 **meta-learning** 提升长期适应能力；
3. 整合更多可再生能源形式（wind, biogas）及储能类型；
4. 开展实地试点项目，验证控制系统在真实农场中的性能；
5. 结合联邦学习（Federated Learning）保护农场数据隐私的同时实现跨场协同优化。

---

> 📌 **总结一句话**：  
> 本文提出了一种**预测感知+自适应正则化**的PPO框架（F-PPO + PID-KL PPO），在真实奶牛场负荷调度任务中实现了比PPO低1%、比DQN低4.8%的电费节约，并显著提升了训练稳定性与用户满意度，为农业可持续能源管理提供了可扩展的技术路径。

</details>

---

### 16. [Large Artificial Intelligence Model Guided Deep Reinforcement Learning for Resource Allocation in Non Terrestrial Networks](https://arxiv.org/abs/2601.08254)

**Authors**: Abdikarim Mohamed Ibrahim, Rosdiadee Nordin  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08254v1  

#### Abstract
Large AI Model (LAM) have been proposed to applications of Non-Terrestrial Networks (NTN), that offer better performance with its great generalization and reduced task specific trainings. In this paper, we propose a Deep Reinforcement Learning (DRL) agent that is guided by a Large Language Model (LL...

---

### 17. [A Human-Centric Pipeline for Aligning Large Language Models with Chinese Medical Ethics](https://arxiv.org/abs/2601.07954)

**Authors**: Haoan Jin, Han Ying, Jiacheng Ji, Hanhui Xu, Mengyue Wu  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.07954v1  

#### Abstract
Recent advances in large language models have enabled their application to a range of healthcare tasks. However, aligning LLMs with the nuanced demands of medical ethics, especially under complex real world scenarios, remains underexplored. In this work, we present MedES, a dynamic, scenario-centric...

---

### 18. [Attention Projection Mixing and Exogenous Anchors](https://arxiv.org/abs/2601.08131)

**Authors**: Jonathan Su  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08131v1  

#### Abstract
Transformers that reuse early-layer attention projections as residuals face a fundamental tension: the first layer must simultaneously serve as a stable reference for all deeper layers and as an effective computational block. To resolve this, we propose ExoFormer, which learns dedicated exogenous an...

---

### 19. [Generation-Augmented Generation: A Plug-and-Play Framework for Private Knowledge Injection in Large Language Models](https://arxiv.org/abs/2601.08209)

**Authors**: Rongji Li, Jian Xu, Xueqing Chen, Yisheng Yang, Jiayi Wang, Xingyu Chen, Chunyu Xie, Dawei Leng, Xu-Yao Zhang  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08209v1  

#### Abstract
In domains such as biomedicine, materials, and finance, high-stakes deployment of large language models (LLMs) requires injecting private, domain-specific knowledge that is proprietary, fast-evolving, and under-represented in public pretraining. However, the two dominant paradigms for private knowle...

---

### 20. [Get away with less: Need of source side data curation to build parallel corpus for low resource Machine Translation](https://arxiv.org/abs/2601.08629)

**Authors**: Saumitra Yadav, Manish Shrivastava  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08629v1  

#### Abstract
Data curation is a critical yet under-researched step in the machine translation training paradigm. To train translation systems, data acquisition relies primarily on human translations and digital parallel sources or, to a limited degree, synthetic generation. But, for low-resource languages, human...

---

### 21. [Reverse Flow Matching: A Unified Framework for Online Reinforcement Learning with Diffusion and Flow Policies](https://arxiv.org/abs/2601.08136)

**Authors**: Zeyang Li, Sunbochen Tang, Navid Azizan  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.08136v1  

#### Abstract
Diffusion and flow policies are gaining prominence in online reinforcement learning (RL) due to their expressive power, yet training them efficiently remains a critical challenge. A fundamental difficulty in online RL is the lack of direct samples from the target distribution; instead, the target is...

---

### 22. [Relational Knowledge Distillation Using Fine-tuned Function Vectors](https://arxiv.org/abs/2601.08169)

**Authors**: Andrea Kang, Yingnian Wu, Hongjing Lu  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.08169v1  

#### Abstract
Representing relations between concepts is a core prerequisite for intelligent systems to make sense of the world. Recent work using causal mediation analysis has shown that a small set of attention heads encodes task representation in in-context learning, captured in a compact representation known ...

---

### 23. [STAR: Detecting Inference-time Backdoors in LLM Reasoning via State-Transition Amplification Ratio](https://arxiv.org/abs/2601.08511)

**Authors**: Seong-Gyu Park, Sohee Park, Jisu Lee, Hyunsik Na, Daeseon Choi  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.08511v1  

#### Abstract
Recent LLMs increasingly integrate reasoning mechanisms like Chain-of-Thought (CoT). However, this explicit reasoning exposes a new attack surface for inference-time backdoors, which inject malicious reasoning paths without altering model parameters. Because these attacks generate linguistically coh...

---

### 24. [Shifting the Sweet Spot: High-Performance Matrix-Free Method for High-Order Elasticity](https://arxiv.org/abs/2601.08374)

**Authors**: Dali Chang, Chong Zhang, Kaiqi Zhang, Mingguan Yang, Huiyuan Li, Weiqiang Kong  
**Category**: cs.DC  
**Published**: 2026-01-14  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.08374v1  

#### Abstract
In high-order finite element analysis for elasticity, matrix-free (PA) methods are a key technology for overcoming the memory bottleneck of traditional Full Assembly (FA). However, existing implementations fail to fully exploit the special structure of modern CPU architectures and tensor-product ele...

---

### 25. [Improving LLM Reasoning with Homophily-aware Structural and Semantic Text-Attributed Graph Compression](https://arxiv.org/abs/2601.08187)

**Authors**: Zijun Di, Bin Lu, Huquan Kang, Luoyi Fu, Jiaxin Ding, Xiaoying Gan, Lei Zhou, Xinbing Wang, Chenghu Zhou  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.08187v1  

#### Abstract
Large language models (LLMs) have demonstrated promising capabilities in Text-Attributed Graph (TAG) understanding. Recent studies typically focus on verbalizing the graph structures via handcrafted prompts, feeding the target node and its neighborhood context into LLMs. However, constrained by the ...

---

### 26. [M3-BENCH: Process-Aware Evaluation of LLM Agents Social Behaviors in Mixed-Motive Games](https://arxiv.org/abs/2601.08462)

**Authors**: Sixiong Xie, Zhuofan Shi, Haiyang Shen, Gang Huang, Yun Ma, Xiang Jing  
**Category**: cs.AI  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.08462v1  

#### Abstract
As the capabilities of large language model (LLM) agents continue to advance, their advanced social behaviors, such as cooperation, deception, and collusion, call for systematic evaluation. However, existing benchmarks often emphasize a single capability dimension or rely solely on behavioral outcom...

---

### 27. [Triplets Better Than Pairs: Towards Stable and Effective Self-Play Fine-Tuning for LLMs](https://arxiv.org/abs/2601.08198)

**Authors**: Yibo Wang, Hai-Long Sun, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Lijun Zhang  
**Category**: cs.CL  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.08198v1  

#### Abstract
Recently, self-play fine-tuning (SPIN) has been proposed to adapt large language models to downstream applications with scarce expert-annotated data, by iteratively generating synthetic responses from the model itself. However, SPIN is designed to optimize the current reward advantages of annotated ...

---

### 28. [Max-Min Neural Network Operators For Approximation of Multivariate Functions](https://arxiv.org/abs/2601.07886)

**Authors**: Abhishek Yadav, Uaday Singh, Feng Dai  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.07886v1  

#### Abstract
In this paper, we develop a multivariate framework for approximation by max-min neural network operators. Building on the recent advances in approximation theory by neural network operators, particularly, the univariate max-min operators, we propose and analyze new multivariate operators activated b...

---

### 29. [LUT-Compiled Kolmogorov-Arnold Networks for Lightweight DoS Detection on IoT Edge Devices](https://arxiv.org/abs/2601.08044)

**Authors**: Oleksandr Kuznetsov  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.08044v1  

#### Abstract
Denial-of-Service (DoS) attacks pose a critical threat to Internet of Things (IoT) ecosystems, yet deploying effective intrusion detection on resource-constrained edge devices remains challenging. Kolmogorov-Arnold Networks (KANs) offer a compact alternative to Multi-Layer Perceptrons (MLPs) by plac...

---

### 30. [Scalable Multiagent Reinforcement Learning with Collective Influence Estimation](https://arxiv.org/abs/2601.08210)

**Authors**: Zhenglong Luo, Zhiyong Chen, Aoxiang Liu, Ke Pan  
**Category**: cs.LG  
**Published**: 2026-01-14  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.08210v1  

#### Abstract
Multiagent reinforcement learning (MARL) has attracted considerable attention due to its potential in addressing complex cooperative tasks. However, existing MARL approaches often rely on frequent exchanges of action or state information among agents to achieve effective coordination, which is diffi...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
