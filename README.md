# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-08 05:56:03 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [SQ-format: A Unified Sparse-Quantized Hardware-friendly Data Format for LLMs](https://arxiv.org/abs/2512.05409)

**Authors**: Ruixuan Huang, Hao Zeng, Hantao Huang, Jinyuan Shi, Minghui Yu, Ian En-Hsu Yen, Shuai Wang  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.05409v1  

#### Abstract
Post-training quantization (PTQ) plays a crucial role in the democratization of large language models (LLMs). However, existing low-bit quantization and sparsification techniques are difficult to balance accuracy and efficiency due to the limited hardware support. For example, W4A8 can only achieve ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**SQ-format: A Unified Sparse-Quantized Hardware-friendly Data Format for LLMs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大语言模型（LLMs）的后训练量化（PTQ）面临**硬件-算法不匹配**的瓶颈：
- **低比特量化（如 W4A4）** 虽然理论上高效，但严重损害模型精度。
- **混合精度（如 W4A8）** 在现代GPU上无法高效执行，因缺乏原生支持，实际退化为更慢的 W8A8 路径，导致吞吐优势无法体现。
- **稀疏化（如 2:4 semi-structure sparse）** 虽被硬件支持，但对非均匀分布的信息压缩效果差，且难以与量化协同优化。

因此，如何在**保持高精度的同时实现真正的高吞吐**，是当前部署LLM的关键挑战。

---

### 🚀 提出的新方法：SQ-format（Sparse-Quantized Format）
作者提出一种**统一的稀疏-量化硬件友好数据格式**，核心思想是：
> 将一个操作数（权重或激活）划分为**高精度稀疏部分**和**低精度密集部分**，通过硬件友好的方式并行处理，从而实现精度与吞吐的帕累托改进（Pareto improvement）。

#### 创新点：
1. **统一稀疏-量化框架**  
   - 定义了 `SQ-format(X) = ([X_quant], [S_quant], [m], h_high, h_low, b, s)`，其中：
     - `h_high` 和 `h_low` 分别表示高低精度位宽（如 INT8 / INT4）
     - `b` 是 bank size，`s` 是每 bank 内的稀疏度
     - `[m]` 是精度掩码，决定哪些元素使用高精度
   - 支持整型（INT）和浮点（FP）格式（如 FP8/FP4）

2. **硬件协同设计（Co-design）**
   - 设计了适用于当前 GPU 和下一代 AI 加速器的硬件实现路径：
     - 对于权重：高精度部分紧凑存储，由专用 gather 单元提取，与低精度路径并行计算。
     - 对于激活：提出**静态策略（static strategy）**，在校准集上预生成掩码，避免推理时动态 Top-K 开销。

3. **静态激活分割策略**
   - 动态选择高精度激活会引入额外开销（TopK），为此提出基于 `A·W` 输出通道平均幅度的重要性评分 `I_j = |A · W|` 来预先确定高精度通道。
   - 掩码以**每通道**形式存储，开销极小（例如 Llama-3-70B 仅需 5.94MB）。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 W4A8, 2:4 sparse） | SQ-format |
|------|-------------------------------|-----------|
| **精度保持** | W4A4 精度下降明显；2:4 sparse 不灵活 | 接近 W4A8 精度，优于 W4A4 |
| **吞吐效率** | W4A8 实际退化为 W8A8，无加速 | 可接近 W4A4 吞吐，实现真实加速 |
| **硬件兼容性** | 缺乏混合精度原生支持 | 兼容现有 GPU，并指导下一代加速器设计 |
| **灵活性** | 固定精度或固定稀疏模式 | 可调 bank size、sparsity、bit-width |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **非生成任务（Non-generative）**：
  - ARC-easy/challenge, OpenBookQA, Hellaswag, Winogrande, PIQA
- **生成任务（Generative）**：
  - GSM8k（8-shot）、AGIEval
- **困惑度（Perplexity）评估**：
  - Wikitext, Lambada
- **校准集（Calibration Set）**：
  - 使用 Wikitext 中 32 个文本片段，每个 2048 token

---

### ⚙️ 实验设置
- **模型**：
  - Llama-3-8B, Llama-3-70B, Qwen3-30B-A3B
- **量化配置命名规则**：
  - `W(SQx)Ay`：权重使用 SQ-format，等效 x bit，激活为 INTy
  - `WxA(SQy)`：激活使用 SQ-format，等效 y bit
  - 示例：`B-(8/4)=0.5` 表示 bank size=64, h_high=INT8, h_low=INT4, sparsity=0.5（即 2x 稀疏）
- **评估指标**：
  - 平均准确率（Non-/Generative Acc）
  - Perplexity（↓越低越好）
  - 推理延迟（Prefilling Latency）
  - 有效带宽（Effective Bandwidth）
  - Speedup（相对 W4A8）

---

### 🆚 基线方法对比
| 类型 | 方法 |
|------|------|
| **PTQ 量化** | GPTQ, SmoothQuant, SpinQuant |
| **稀疏化** | SpQR, SparseGPT |
| **基准精度** | BF16（W16A16）作为上限参考 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & D）

#### ✅ 在 Llama-3-70B 上的表现（W4A(SQ6)，sparsity=0.5）：
| 指标 | SQ-format | W4A8 (GPTQ) | BF16 |
|------|-----------|------------|-------|
| Non-gen Acc (%) | 62.80 → **接近 GPTQ** | 62.54 | 64.51 |
| Generative Acc (%) | 78.32 | 78.31 | 81.05 |
| Perplexity (Wiki) | 3.31 | 3.48 | 2.92 |
| **Speedup** | **1.32×** | 1.00× | — |

#### ✅ 更高稀疏度下进一步加速（Llama-3-70B）：
| Sparsity | Speedup vs W4A8 | Perplexity |
|---------|------------------|------------|
| 0.5 (2x) | 1.32× | 3.31 |
| 0.75 (4x) | 1.56× | 3.45 |
| 0.875 (8x) | **1.71×** | 3.44 |
| W4A4（理论极限） | 1.92× | 3.73 |

> **结论**：SQ-format 在保持精度损失 <1% 的前提下，实现了高达 **1.71× 的速度提升**，达到 W4A4 理论性能的 ~89%。

---

#### ✅ 与 W4A4 对比（精度提升显著）：
- 在 Llama-3-8B 上：
  - W4A4：平均准确率 ~47%
  - W(SQ6)A4：平均准确率 ~49.4%，**提升约 2.4%**
- 在 Llama-3-70B 上：
  - W4A4：GSM8k 准确率 74.82%
  - W(SQ6)A4：**78.62%**，提升近 4%

> 表明：引入少量高精度元素即可显著恢复 W4A4 的精度损失。

---

#### ✅ 静态 vs 动态激活策略（Table 2）
| 设置 | 平均准确率差异 | 结论 |
|------|----------------|------|
| Dynamic SQ on A | 基准 | 引入 TopK 开销 |
| Static SQ on A | 差异 < ±1% | **性能几乎持平，但更适合硬件部署** |

> 静态策略无需运行时计算掩码，更适合 GPU 和边缘设备。

---

#### ✅ 消融实验（Appendix A.1）
- **bank size 影响**：
  - 较大模型建议使用更大 bank size（如 64），以更好捕捉 outlier 分布。
  - bank size=32 比 4 更灵活，能更有效地分离重要激活（见 Figure 6）。
- **校准集大小影响**（Table 7）：
  - 即使使用仅 8–32 个样本，静态掩码仍具有良好泛化能力，说明 activation outlier 分布稳定。

---

#### ✅ 浮点验证（Appendix B）
- 在 DeepSeek-R1（685B）上应用 FP8/FP4 SQ-format（sparsity=0.875）：
  - 实现 **5-bit 等效精度**
  - 性能几乎无损（GSM8k: 96.21 vs 95.83）
  - 验证了 SQ-format 在超大规模模型上的可扩展性。

---

#### ✅ 硬件可行性验证（Appendix C）
- RTL 综合显示，相比标准 INT6 MAC 阵列：
  - **总面积减少 35.8%**
  - 更高的计算密度，适合下一代 AI 加速器集成

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **SQ-format 成功弥合了“高精度”与“高吞吐”的鸿沟**：
   - 实现了从 W4A8 到 W4A4 的平滑过渡，在精度和速度之间取得帕累托最优。
2. **静态激活分割策略实用性强**：
   - 仅需极小额外存储（每通道1位掩码），即可消除动态 TopK 开销，性能损失可忽略。
3. **bank size 和 sparsity 需协同设计**：
   - 大模型推荐 bank size ≥ 64，sparsity ≥ 0.75（4x 稀疏以上）以平衡计算负载。
4. **硬件协同设计至关重要**：
   - 当前 GPU 可通过 CUDA kernel 模拟，但专用硬件（如 gather unit）将进一步释放潜力。

---

### ⚠️ 局限性
1. **依赖校准集进行静态掩码生成**：
   - 虽然实验证明小样本足够，但在极端分布偏移场景下可能失效。
2. **需要硬件支持才能完全发挥优势**：
   - 当前实现依赖模拟或定制 kernel，尚未成为主流硬件原生功能。
3. **仅作用于单个操作数**：
   - 论文限制 SQ-format 只应用于 weight 或 activation 之一，双端联合压缩尚待探索。

---

### 🔮 未来工作方向
1. **扩展至 KV Cache 压缩**：
   - 将 SQ-format 应用于 attention 中的 key/value，进一步降低内存占用。
2. **支持更多数据类型**：
   - 如 MX Format、HiFloat8 等自适应范围格式结合 SQ。
3. **编译器与系统级优化**：
   - 构建支持 SQ-format 的推理引擎（类似 TensorRT），实现端到端自动化部署。
4. **探索动态-静态混合策略**：
   - 在关键层使用动态选择，其余层使用静态策略，兼顾灵活性与效率。

---

## ✅ 总结
**SQ-format** 是一项面向未来的**硬件-算法协同设计**成果，它不仅提供了一种高效的 LLM 压缩方案，更重要的是提出了一个**可扩展、可编程的混合精度计算范式**。该方法在精度损失极小的情况下，显著提升了推理吞吐，为下一代 AI 加速器的设计提供了清晰蓝图。

</details>

---

### 2. [Bridging Interpretability and Optimization: Provably Attribution-Weighted Actor-Critic in Reproducing Kernel Hilbert Spaces](https://arxiv.org/abs/2512.05291)

**Authors**: Na Li, Hangguan Shan, Wei Ni, Wenjie Zhang, Xinyu Li  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05291v1  

#### Abstract
Actor-critic (AC) methods are a cornerstone of reinforcement learning (RL) but offer limited interpretability. Current explainable RL methods seldom use state attributions to assist training. Rather, they treat all state features equally, thereby neglecting the heterogeneous impacts of individual st...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Bridging Interpretability and Optimization: Provably Attribution-Weighted Actor-Critic in Reproducing Kernel Hilbert Spaces

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Actor-Critic (AC)** 方法在强化学习中虽然有效，但缺乏可解释性（interpretability）。现有可解释强化学习（XRL）方法大多为事后解释（post-hoc），并未将状态特征的重要性（state attributions）融入训练过程。此外，标准AC方法通常平等对待所有状态特征，忽略了不同维度对奖励影响的异质性。

### 提出的新方法
本文提出了 **RKHS-SHAP-based Advanced Actor-Critic (RSA2C)**，一种将可解释性与优化相结合的新型AC算法。其核心创新点如下：

- **Attribution-Aware Training**：首次将状态特征重要性（通过SHAP计算）直接用于调节策略梯度更新，使训练过程本身具备内在可解释性（intrinsic interpretability）。
- **基于RKHS的核化框架**：将Actor、Value Critic和Advantage Critic均置于**Reproducing Kernel Hilbert Space (RKHS)** 中，利用非参数函数逼近，显式控制几何结构和模型容量。
- **Mahalanobis加权操作符值核（OVK）**：提出一种自适应的核函数，其中权重由SHAP值生成的Mahalanobis距离决定，从而动态调整不同状态维度的相似性度量。
- **双时间尺度机制与稀疏字典**：采用双时间尺度更新，并通过**Approximate Linear Dependence (ALD)** 维护稀疏字典，控制计算复杂度，使其适用于在线RL。
- **理论收敛保证**：在存在状态扰动的情况下，推导了全局、非渐近的收敛界，将学习差距分解为**扰动误差**（perturbation error）和**收敛误差**（convergence error），分别量化了稳定性和效率。

### 相比现有方法的优势
- **兼具效率与可解释性**：不仅提升了性能，还提供了训练过程中特征重要性的实时洞察。
- **稳定性强**：通过RKHS-SHAP和核化设计，在噪声环境下表现出更强的鲁棒性。
- **理论保障**：提供了在状态扰动下的非渐近收敛证明，填补了相关理论空白。
- **轻量级**：相比深度RL方法，计算开销显著更低。

---

## 2. 核心实验方法和设置

### 数据集（环境）
实验在三个标准的连续控制环境上进行：
- **Pendulum-v1**：1维动作空间，观测为 `(cosθ, sinθ, θ_dot)`。
- **BipedalWalker-v3**：4维动作空间，24维本体感受 + 10维激光雷达。
- **Ant-v5**：8维动作空间，105维高维状态（包含关节角度、速度、接触等）。

### 实验设置和评估指标
- **评估维度**：
  - **效率（Efficiency）**：最终平均回报（average return）、收敛速度。
  - **稳定性（Stability）**：在注入零均值高斯噪声（不同方差）下的性能表现（均值与标准差）。
  - **可解释性（Interpretability）**：通过beeswarm图和heatmap可视化SHAP值，分析特征重要性随训练的演化。
  - **计算开销**：FLOPs和每轮更新的运行时间（runtime）。
- **消融研究**：比较了不同变体，包括：
  - `RSA2C-KME`：使用RKHS-SHAP的**on-manifold**期望（基于KME）。
  - `RSA2C-CME`：使用RKHS-SHAP的**off-manifold**期望（基于CME）。
  - `Advanced AC`：无SHAP的基线版本。
  - `RKHS-AC`：传统核AC方法（Lever & Stafford, 2015）。
- **基线方法**：与主流深度RL算法对比：
  - **SAC (Soft Actor-Critic)**
  - **PPO (Proximal Policy Optimization)**

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### Pendulum-v1 环境
- **最终回报提升**：
  - `RSA2C-CME` 和 `RSA2C-KME` 相比 `Advanced AC` 分别提升了约 **47.6%** 和 **49.2%**。
- **与深度RL对比**：
  - 尽管不使用神经网络，`RSA2C` 变体在性能上与SAC和PPO具有竞争力。
  - SAC初期回报高但很快饱和，PPO改进慢且波动大。
- **计算开销**：
  - 虽然FLOPs增加（`RSA2C-CME`: 14.960 MFLOPs），但实际运行时间仅比`Advanced AC`增加约 **12.58%**，远低于SAC和PPO（即使在GPU上）。

#### BipedalWalker-v3 环境
- **稳定性**：
  - 在噪声环境下，`RSA2C-CME` 的标准差（24.75–43.06）显著低于 `RSA2C-KME`（46.74–56.03），表明CME更稳定。
  - `RSA2C-CME` 在高噪声下（0.01方差）的平均回报（254.85）优于 `RSA2C-KME`（242.05）。
- **计算开销**：
  - 运行时间增长约 **6.9%**，仍远低于SAC和PPO。

#### Ant-v5 环境（高维挑战）
- **性能瓶颈**：
  - 所有RKHS方法（包括`RSA2C`）都陷入局部最优，最终回报约1000。
  - SAC持续改进并超过4000，显示出深度RL在高维非线性任务中的优势。
- **稳定性**：
  - `RSA2C` 变体在所有噪声水平下保持极高的稳定性（标准差 < 45）。
  - SAC和PPO在噪声下表现极不稳定，PPO甚至崩溃至负回报。
- **计算开销**：
  - `RSA2C-CME` 需要3.853 GFLOPs，仍显著低于SAC（29.28 GFLOPs）。

### 消融实验结果
- **SHAP的有效性**：引入SHAP的`RSA2C`显著优于无SHAP的`Advanced AC`，验证了特征重要性引导训练的有效性。
- **CME vs KME**：
  - `RSA2C-CME` 收敛更平滑、更稳定，尤其在噪声环境下。
  - `RSA2C-KME` 初期收敛快但波动大。
  - 原因：CME显式建模特征依赖关系，能自适应重分配重要性；KME忽略相关性，易受主导特征噪声影响。

---

## 4. 关键结论和发现

### 主要发现
1. **可解释性可驱动优化**：将SHAP等归因方法从“事后解释”转变为“训练信号”，能有效提升AC算法的效率和稳定性。
2. **CME优于KME**：在off-manifold设置下计算的SHAP（CME）由于考虑了特征间的条件依赖，比on-manifold的KME更具鲁棒性。
3. **轻量高效**：`RSA2C` 在不依赖深度神经网络的情况下，实现了与深度RL相当的性能，且计算开销低一个数量级。
4. **强鲁棒性**：`RSA2C` 对状态观测噪声表现出极强的抵抗力，而深度RL方法（如SAC、PPO）则非常敏感。
5. **理论与实践一致**：在LQR环境中的实验验证了理论推导的非渐近收敛率。

### 方法的局限性
- **高维表征能力有限**：在Ant-v5等高维复杂环境中，线性RKHS函数逼近结合RBF核难以捕捉足够复杂的结构，导致性能受限。
- **计算复杂度**：尽管进行了稀疏化，但在高维状态下维护RKHS字典和计算SHAP仍有一定开销。
- **适用范围**：目前主要针对连续控制任务，对离散或部分可观测任务的扩展需进一步研究。

### 未来工作方向
1. **扩展到高维观测**：结合**Random Fourier Features (RFFs)** 或其他可扩展核近似技术，处理图像等高维输入。
2. **混合架构**：将核化归因方法与深度表示学习结合，以兼顾表达能力和稳定性。
3. **多智能体RL**：探索该方法在多智能体场景中的应用。
4. **理论深化**：研究在更一般化MDP或对抗性环境下的收敛性质。

</details>

---

### 3. [Uncertainty Quantification for Scientific Machine Learning using Sparse Variational Gaussian Process Kolmogorov-Arnold Networks (SVGP KAN)](https://arxiv.org/abs/2512.05306)

**Authors**: Y. Sungtaek Ju  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05306v1  

#### Abstract
Kolmogorov-Arnold Networks have emerged as interpretable alternatives to traditional multi-layer perceptrons. However, standard implementations lack principled uncertainty quantification capabilities essential for many scientific applications. We present a framework integrating sparse variational Ga...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Uncertainty Quantification for Scientific Machine Learning using Sparse Variational Gaussian Process Kolmogorov-Arnold Networks (SVGP KAN)*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Kolmogorov-Arnold Network (KAN)** 虽然在可解释性方面优于标准 MLP（通过边上的可学习单变量函数实现），但其**确定性架构缺乏不确定性量化能力**，限制了其在科学机器学习（SciML）等对置信度要求高的场景中的应用。

现有贝叶斯方法如 **Bayesian KAN** 或 **Gaussian Process KAN (GP-KAN)** 存在以下问题：
- **计算复杂度高**：精确 GP 推断需 $O(N^3)$ 时间，难以扩展到大规模数据；
- **不确定性建模不完整**：仅建模权重空间不确定性，而非函数空间不确定性；
- **无法区分 aleatoric 与 epistemic 不确定性**。

---

### 🚀 提出的新方法：SVGP-KAN
作者提出 **Sparse Variational Gaussian Process Kolmogorov-Arnold Network (SVGP-KAN)**，将稀疏变分推断（Sparse Variational Inference）引入 KAN 架构中，构建一个兼具**可解释性、不确定性量化能力和可扩展性**的新型神经网络框架。

#### 核心创新点：
1. **函数空间不确定性建模**  
   将每条边上的单变量函数 $\phi_{j,i}$ 建模为 **Gaussian Process (GP)**，从而实现对函数本身的概率建模，而非参数分布。

2. **稀疏变分近似提升可扩展性**  
   引入 **Inducing Points** 和 **Variational Inference**，将计算复杂度从 $O(N^3)$ 降低至 $O(NM^2)$，其中 $M \ll N$ 为诱导点数量，实现准线性扩展。

3. **解析矩匹配（Analytic Moment Matching）进行不确定性传播**  
   利用 GP 输出的闭式均值与方差表达，在深度加性结构中逐层传播输入不确定性，支持 **epistemic** 与 **aleatoric** 不确定性的分离。

4. **异方差噪声建模（Heteroscedastic Noise Modeling）**  
   引入第二个 GP 来建模观测噪声的对数方差 $\log\sigma^2_{\text{noise}}(x)$，使模型能自动识别不同区域的测量精度差异。

5. **灵活的 KL 正则化控制机制**  
   通过调节 ELBO 中的 KL 权重 $\lambda$，可在不同任务间平衡“拟合能力”与“不确定性校准”，例如：
   - $\lambda = 0.01$：用于校准 in-distribution 预测；
   - $\lambda = 0$：保留 OOD 输入时的自然方差放大效应，增强异常检测能力。

---

### 🔍 相比现有方法的优势

| 方法 | 可解释性 | 不确定性量化 | 扩展性 | 区分 aleatoric/epistemic |
|------|----------|----------------|--------|----------------------------|
| Deterministic KAN | ✅ 强 | ❌ 无 | ✅ 好 | ❌ 否 |
| Bayesian KAN | ⭕ 中等 | ⭕ 参数空间 | ⭕ 一般 | ⭕ 有限 |
| Exact GP-KAN | ✅ 函数空间 | ✅ 是 | ❌ $O(N^3)$ | ✅ 是 |
| **SVGP-KAN (本文)** | ✅ 边函数可视化 | ✅ 函数空间 + 分离 | ✅ $O(NM^2)$ | ✅ 显式分离 |

> ✅ 表示优势明显，⭕ 表示部分支持，❌ 表示不支持或较差

---

## 2. 核心实验方法和设置

### 🧪 实验设计概述
论文通过三个典型 SciML 场景验证 SVGP-KAN 的有效性：

| Study | 任务类型 | 数据来源 | 主要目标 |
|-------|---------|----------|----------|
| **Study A** | 流体速度场重建 | 合成二维不可压缩流场 | 校准空间变化的异方差测量噪声（Aleatoric Uncertainty） |
| **Study B** | 多步预测 | 合成 advection-diffusion 方程轨迹 | 量化模型误差随时间累积导致的 Epistemic Uncertainty 增长 |
| **Study C** | 异常检测 | MNIST（训练仅“0”，测试含“7”） | 验证 OOD 检测能力，利用瓶颈层不确定性作为判据 |

---

### 📊 实验设置与评估指标

#### Study A: Heteroscedastic Noise Calibration
- **数据生成**：
  - 使用流函数 $\psi(x,y)$ 构造真实速度场 $(v_x, v_y)$；
  - 添加空间相关噪声：$\sigma^2_{\text{true}}(x,y) = \sigma_{\text{base}}^2 \left(1 + A_{\text{noise}} \exp(-\frac{(x-x_c)^2 + (y-y_c)^2}{r^2})\right)$
- **模型结构**：3 层全连接 GPKANLayer，每边使用 20 个 inducing points
- **评估指标**：
  - Pearson 相关系数（预测不确定性 vs 实际绝对误差）
  - 预测区间覆盖率（±1σ, ±2σ, ±3σ）
  - RMSE、标准化预测误差（mean/std of $(y-\mu)/\sigma$）

#### Study B: Multi-step Forecasting
- **物理系统**：2D advection-diffusion PDE：
  $$
  \frac{\partial T}{\partial t} = -\mathbf{u} \cdot \nabla T + \kappa \nabla^2 T + S(x,t)
  $$
- **Pe 数 ≈ 1571**（对流主导），确保挑战性
- **训练方式**：仅学习单步转移 $T_n \to T_{n+1}$
- **测试方式**：Ensemble Rollout（10 个初始扰动样本前向传播 15 步）
- **评估指标**：
  - Ensemble spread（epistemic uncertainty）
  - 时间维度上的 uncertainty growth factor
  - 空间 uncertainty variation ratio

#### Study C: Out-of-Distribution Detection
- **数据集**：MNIST，训练集仅包含 digit "0"
- **模型结构**：Convolutional Autoencoder + SVGP-KAN Bottleneck（latent dim=6）
- **训练策略**：$\lambda = 0$（无 KL 正则化），以保留 OOD 放大机制
- **评估指标**：
  - Anomaly Score Ratio（OOD / ID 的 bottleneck uncertainty 比值）
  - ROC-AUC
  - Reconstruction MSE 对比

---

### ⚖️ 基线方法对比
文中未直接列出与其他模型（如 BNN、Deep Ensemble、Standard GP）的定量比较，而是强调：
- 相较于 exact GP-KAN：**计算效率显著提升**（$O(NM^2)$ vs $O(N^3)$）
- 相较于 deterministic KAN：**提供 principled uncertainty quantification**
- 相较于普通 BNN：**保持边函数可解释性**

> 注：作者开源代码（GitHub: `sungjuGit/svgp-kan`），便于后续复现与横向对比。

---

## 3. 主要实验结果和性能指标

### ✅ Study A: Heteroscedastic Noise Calibration
- **Pearson 相关性**：$\rho = 0.55$（预测不确定性与实际误差之间）
  - 解释：适度相关合理，因 aleatoric noise 本质随机，不应完全确定
- **覆盖率误差 < 1%**：
  - ±1σ 覆盖率接近理论值（68.3%）
  - ±2σ 和 ±3σ 也高度一致，表明不确定性**良好校准**
- **标准化预测误差**：mean = -0.09，std = 1.01 → 接近理想 $N(0,1)$
- **RMSE = 0.085** → 均值预测准确

> ✔️ 成功识别高噪声区域并动态调整置信区间宽度

---

### ✅ Study B: Epistemic Uncertainty Growth
- **Ensemble Spread 增长因子**：**2.4 倍**（从 t=0 到 t=14）
  - 物理系统经验证是稳定的（非混沌），因此增长源于**模型近似误差积累**
- **Spatial Variation Ratio**：高达 **12.7 倍**
  - 不确定性集中在梯度剧烈区域（如热冷界面），符合直觉
- **视觉分析**：随着预测步数增加，ensemble mean 逐渐偏离 ground truth，uncertainty map 显示出清晰的空间聚焦模式

> ✔️ 成功捕捉 multi-step rollout 中的可靠性下降趋势，并给出物理意义明确的不确定性分布

---

### ✅ Study C: Out-of-Distribution Detection
- **Anomaly Score Ratio**：平均 **10~100 倍**
  - 即 OOD 输入（digit “7”）的 bottleneck uncertainty 远高于 ID 输入（digit “0”）
- **ROC-AUC**：**0.8 ~ 0.9**，显示良好判别能力
- **Reconstruction MSE**：OOD 输入高出 3–4 倍
- **定性观察**：对于 “7”，重构图像模糊、趋于“0”类模式，体现流形投影行为

> ✔️ 在无需显式 OOD 标签训练的情况下，依靠 GP 的自然方差放大机制实现有效异常检测

---

### 🔍 消融实验（隐含分析）
虽然没有独立章节，但通过不同 $\lambda$ 设置体现了消融思想：
- $\lambda = 0.01$：适用于 in-distribution 回归任务（Studies A & B），提升校准性
- $\lambda = 0$：专用于 OOD 检测（Study C），防止 KL 正则压制信号方差，保留敏感性

此外，inducing point 数量 $M=20$ 被证明足以在多数任务中取得稳定表现。

---

## 4. 关键结论和发现

### 🎯 主要发现
1. **SVGP-KAN 成功实现了可扩展、可解释且具备 principled uncertainty quantification 的 SciML 架构**。
2. 能够**明确区分 aleatoric 与 epistemic 不确定性**：
   - Study A：响应测量噪声强度 → aleatoric
   - Study B：随预测步数增长 → epistemic
   - Study C：面对 OOD 输入 → epistemic（模型未知）
3. **不确定性具有物理意义的空间结构**，可用于指导自适应采样或传感器部署。
4. **自然方差放大机制**（natural variance amplification）是一种无需额外训练即可实现 OOD 检测的有效手段，尤其当 $\lambda = 0$ 时更显著。

---

### ⚠️ 方法的局限性
1. **核函数选择受限**：
   - 当前主要基于 RBF kernel，其假设函数光滑；
   - 对不连续或尖锐过渡现象建模可能不足，未来可尝试 Matérn kernel。
2. **Inducing Point 数量需手动设定**：
   - $M$ 是超参，过多影响效率，过少损失精度；
   - 缺乏自动分配机制（如根据局部复杂度调整密度）。
3. **当前仅支持 Gaussian Likelihood**：
   - 不适用于分类、计数数据或重尾噪声场景。
4. **KL weight $\lambda$ 需经验调参**：
   - 不同任务最优值不同，缺乏自动化调节策略。

---

### 🔮 未来工作方向
1. **扩展至非高斯似然**（Poisson, Bernoulli, Student-t）以覆盖更多科学问题。
2. **开发动态 $\lambda$ 调整机制**，基于验证集不确定性校准指标自动优化。
3. **结合 Recurrent 或 Graph 结构**，应用于时间序列预测或分子建模。
4. **集成 Active Learning 框架**，利用不确定性图指导数据采集或网格细化。
5. **探索 Automatic Relevance Determination (ARD)** 机制，实现特征重要性与不确定性联合分析。

---

## 总结

✅ **SVGP-KAN 是一种面向科学机器学习的新型不确定性感知架构**，它融合了：
- KAN 的**可解释性**
- GP 的**原则性不确定性建模**
- Sparse Variational Inference 的**可扩展性**

通过三项精心设计的实验，论文充分展示了该方法在 **异方差噪声校准、多步预测不确定性增长建模、以及 OOD 检测**方面的强大能力。尽管存在一些限制，但它为构建**可靠、可信、可解释的 SciML 模型**提供了坚实基础，具有广泛的应用前景。

</details>

---

### 4. [When Forgetting Builds Reliability: LLM Unlearning for Reliable Hardware Code Generation](https://arxiv.org/abs/2512.05341)

**Authors**: Yiwen Liang, Qiufeng Li, Shikai Wang, Weidong Cao  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05341v1  

#### Abstract
Large Language Models (LLMs) have shown strong potential in accelerating digital hardware design through automated code generation. Yet, ensuring their reliability remains a critical challenge, as existing LLMs trained on massive heterogeneous datasets often exhibit problematic memorization of propr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*When Forgetting Builds Reliability: LLM Unlearning for Reliable Hardware Code Generation*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前用于硬件代码生成的 **Large Language Models (LLMs)** 在训练过程中可能**过度记忆**（memorization）以下三类有害内容：
- **专有知识产权 (IP)**：如来自公开仓库中的受版权保护的 RTL 模块；
- **被污染的基准测试数据**：如 VerilogEval 和 Rtllm 数据集中存在与模型训练集重叠的问题；
- **不安全的编码模式**：可能导致生成带有漏洞或不可综合的 RTL 代码。

这些记忆行为会引发 **IP 泄露风险、评估失真、系统可靠性下降**等问题，严重制约了 LLM 在可信硬件设计中的应用。

传统解决方案（如从头重新训练）成本过高（例如 LLaMA-3 8B 需要约 146 万 GPU 小时），而通用的 **machine unlearning** 方法（如 GA、SimNPO）在应用于硬件领域时，往往破坏 RTL 代码的语法正确性和功能完整性。

---

### **提出了什么新方法或新思路**

本文提出首个面向 **LLM-based 硬件代码生成** 的**领域专用遗忘框架**（domain-specific unlearning framework），其核心创新包括：

#### ✅ **1. 语法保持型遗忘策略**（Syntax-Preserving Unlearning）
- **动机**：RTL 代码具有严格的语法结构（如 `always @(posedge clk)`、`module/endmodule` 等关键字），若在遗忘过程中修改这些结构词，会导致生成代码无法编译或综合。
- **方法**：
  - 引入 **keyword masking**：对 Verilog 关键字（如 `wire`, `reg`, `assign`）进行掩码，使其不参与梯度更新；
  - 使用 **skip-tag masking**：通过 `<SKIP_S>` 和 `<SKIP_E>` 标记需要跳过的代码片段，在计算损失时不考虑这些 token。
- **效果**：确保遗忘过程不会破坏 RTL 的语法骨架，维持生成代码的 **compilability** 和 **synthesizability**。

#### ✅ **2. 细粒度楼层感知选择性损失函数**（Fine-grained Floor-aware Selective Loss, FiFSL）
- **动机**：传统遗忘方法对所有样本均匀施加“反向学习”压力，容易造成过度遗忘或不稳定。
- **方法**：
  - 在 token 级别引入 **margin-based 控制机制**：仅当样本的 loss 小于阈值 γ 时才施加强遗忘压力；
  - 加入 **hard floor $L_{\text{min}}$**：只对未充分遗忘的样本（loss > $L_{\text{min}}$）计算梯度，避免已遗忘样本继续扰动模型；
  - 使用 **smooth softplus 映射** 替代原始负对数似然，提升优化稳定性。
- **优势**：实现更精准、高效、稳定的遗忘，尤其适用于长序列 RTL 生成任务。

---

### **相比现有方法的优势**

| 特性 | 传统方法（GA / SimNPO） | 本文方法（Ours） |
|------|------------------------|------------------|
| 是否保留语法结构 | ❌ 忽视 HDL 特性，易破坏语法 | ✅ 显式保护关键词与结构 |
| 遗忘效率 | ⚠️ 需 4–7 个 epoch 才收敛 | ✅ 通常只需 **1 个 epoch** |
| 可扩展性 | ❌ 最大支持 ~10% forget set | ✅ 支持高达 **30% forget set**（即 3× 更大） |
| 功能保真度 | ❌ Pass@1 急剧下降 | ✅ 几乎无损保留生成能力 |
| 实用性 | ⚠️ 存在 utility degradation | ✅ 实现接近理想的 forget-utility trade-off |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **主训练/微调数据集**：**RTLCoder dataset**（开源指令-代码配对数据集，包含自然语言描述与对应 RTL 实现）
- **遗忘集构造方式**：由于缺乏真实“需遗忘”的专有 IP 或污染数据，作者采用 **从训练集中采样 10%、20%、30% 的样本作为 forget set** 来模拟现实场景（如 IP 删除请求、污染模块移除等）
- **验证集**：RTLCoder 的 hold-out 测试集，用于评估模型 utility

---

### **实验设置**

- **模型选择**：
  - CodeLlama-7B-Instruct
  - Llama-3-8B-Instruct（通用模型）
  - DeepSeek-Coder-7B-Instruct
  - Qwen2.5-Coder-7B-Instruct
- **微调配置**：
  - Adam 优化器，lr = 1e-5，batch size = 2，max seq len = 2048
  - 微调 6 轮以建立强记忆基线
- **遗忘阶段参数**：
  - lr = 2e-6
  - FiFSL 参数：β = 2.5, γ = 0, $L_{\text{min}}$ = 0.35（基于首轮微调后的验证 loss 设定）

---

### **评估指标**

#### 🔹 **Forget Quality（遗忘质量）**
- **PrivLeak**：衡量模型是否再生出 forget set 中的原始代码片段（越低越好）
- **MinK++**：检测模型是否仍残留对某些高频 token 分布的记忆（越低越好，未训练模型约为 0.5）

#### 🔹 **Model Utility（模型效用）**
- **Validation Loss**：在保留集上的交叉熵损失（越低越好）
- **Pass@1**：生成代码能否通过编译并满足功能测试（越高越好）
- **BLEU / chrF**：与参考实现的 n-gram 和字符级相似度（越高越好）

#### 🔹 **泛化能力**
- 在 **VerilogEval** 和 **Rtllm** 基准上评估遗忘后模型的表现变化

---

### **基线方法对比**

- **Gradient Ascent (GA)** [9]：最大化 forget set 上的负对数似然
- **SimNPO** [12]：改进版 NPO，引入平滑惩罚项增强稳定性
- **SALAD** [16]：唯一尝试将 unlearning 应用于硬件设计的工作，但直接套用通用方法，未做领域适配

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table II & Fig. 3）**

| 模型 | Forget Set | 方法 | Epochs | MinK++ ↓ | Pass@1 ↑ | 相对提升 |
|------|------------|-------|--------|----------|----------|-----------|
| Llama-7B | 30% | GA | 4 | 0.54 | 3% | — |
| Llama-7B | 30% | SimNPO | 4 | 0.53 | 1% | — |
| Llama-7B | 30% | **Ours** | **1** | **0.53** | **44%** | ⬆️ **14.3× Pass@1** |
| DeepSeek-7B | 30% | GA | 5 | 0.51 | 28% | — |
| DeepSeek-7B | 30% | SimNPO | — | 0.50 | 28% | — |
| DeepSeek-7B | 30% | **Ours** | **1** | **0.45** | **53%** | ⬆️ 显著优于基线 |
| Qwen-7B | 30% | GA | 3 | 0.50 | 13% | — |
| Qwen-7B | 30% | SimNPO | 3 | 0.50 | 10% | — |
| Qwen-7B | 30% | **Ours** | **1** | **0.49** | **55%** | ⬆️ 接近原始性能 |

> 注：Pass@1 下降表示 utility 损失；本文方法在高遗忘比例下仍保持高功能性。

---

### **与基线方法的对比结果**

- **遗忘能力更强**：
  - 支持 **3× 更大的 forget set**（30% vs 10%），而基线在 20%-30% 即崩溃；
- **效率更高**：
  - **仅需 1 个 epoch 完成有效遗忘**，而 GA/SimNPO 需 4–7 个 epoch；
- **实用性更好**：
  - 在 **PrivLeak 和 MinK++ 上显著降低**，表明记忆被有效清除；
  - 同时 **Pass@1、BLEU、chrF 几乎不变或轻微波动**，说明 utility 得到良好保留。

---

### **消融实验与泛化能力分析**

#### ✅ **泛化能力保持良好（Table III）**

| Model | Forget Set | VerilogEval Pass@1 | Rtllm Pass@1 |
|-------|------------|--------------------|-------------|
| Qwen-7B | 30% | 33% → **33%**（不变） | 36% → **36%** |
| DeepSeek-7B | 30% | 35% → **33%** | 40% → **38%** |

> 表明遗忘操作未损害模型在标准 benchmark 上的整体表现。

#### ✅ **跨模型一致性高**
- 在四种不同架构的 LLM 上均取得一致优势，证明方法具有良好的**可迁移性与鲁棒性**。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **机器遗忘可以成为构建可靠硬件生成 LLM 的关键技术路径**：通过有选择地“忘记”有害内容，反而提升了系统的安全性与可信度。
2. ✅ **领域特性必须融入 unlearning 过程**：忽略 RTL 语法结构的传统方法会导致生成失效；本文提出的 syntax-preserving 策略是成功的关键。
3. ✅ **FiFSL 实现了高效且可控的遗忘**：结合 margin 控制与动态样本筛选，实现了快速收敛与稳定训练。
4. ✅ **大规模遗忘 + 高效 + 高保真是可行的**：本方法在仅 1 个 epoch 内完成高达 30% 数据的遗忘，同时几乎不牺牲生成质量。

---

### **方法的局限性**

- **依赖人工定义关键词集合**：虽然基于 IEEE 标准 [32]-[34]，但在扩展到其他 HDL（如 VHDL）时需重新设计 keyword mask；
- **forget set 构造为模拟场景**：目前实验基于训练集子集抽样，尚未在真实法律合规请求或工业级 IP 删除场景中验证；
- **未涉及多轮增量遗忘**：如何支持持续性的“删除请求流”尚待研究。

---

### **未来工作方向**

- 扩展至 **VHDL、SystemVerilog 更复杂语法结构**；
- 探索 **自动化识别需遗忘模块的技术**（如基于 watermarking 或溯源分析）；
- 研究 **增量式 unlearning pipeline**，支持在线删除请求；
- 结合 **formal verification** 技术进一步验证遗忘后代码的功能等价性与安全性。

---

## 总结

> 本文开创性地将 **machine unlearning** 引入 **LLM-based 硬件代码生成** 领域，提出首个领域自适应的遗忘框架。通过 **syntax-preserving masking** 与 **FiFSL 损失函数** 的协同设计，实现了 **高效、精准、可靠的遗忘**，在支持更大 forget set 的同时，显著优于 GA、SimNPO 等通用方法，并保持了 RTL 生成的质量与泛化能力。该工作为打造**可信赖、合规、安全的 AI 辅助芯片设计工具链**提供了重要技术基础。

</details>

---

### 5. [A Fast Anti-Jamming Cognitive Radar Deployment Algorithm Based on Reinforcement Learning](https://arxiv.org/abs/2512.05753)

**Authors**: Wencheng Cai, Xuchao Gao, Congying Han, Mingqiang Li, Tiande Guo  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05753v1  

#### Abstract
The fast deployment of cognitive radar to counter jamming remains a critical challenge in modern warfare, where more efficient deployment leads to quicker detection of targets. Existing methods are primarily based on evolutionary algorithms, which are time-consuming and prone to falling into local o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**认知雷达在电子干扰环境下的快速部署问题**（Anti-Jamming Cognitive Radar Deployment）展开研究。该问题是现代战争中提升战场感知能力的关键挑战之一。传统方法依赖于**进化算法**（Evolutionary Algorithms, 如 GA、PSO），存在以下缺陷：
- **计算耗时长**：需要多轮迭代搜索；
- **易陷入局部最优**：搜索策略受限，难以全面探索解空间。

### 提出的新方法与创新思路
作者提出了一种全新的基于**深度强化学习**（Deep Reinforcement Learning, DRL）的框架——**Fast Anti-Jamming Radar Deployment Algorithm (FARDA)**，其主要创新包括：

- **首次将 DRL 应用于抗干扰雷达部署问题**：此前虽有研究使用 RL 处理雷达资源调度或波形设计，但尚无工作将其用于部署位置优化。
- **端到端建模为组合优化问题**：将雷达部署建模为一个序列决策任务，并构建相应的 **Markov Decision Process (MDP)**。
- **设计专用神经网络模块**：
  - 构建融合 **DCNN + LSTM** 的编码器（Encoder Network），用于提取检测概率热力图（heatmap）特征并记忆历史状态变化。
- **提出新型奖励整形机制 CVDP-EXPR**：
  - **Constraint Violation Degree Penalty (CVDP)**：对偏离均匀部署的行为施加惩罚，防止训练崩溃。
  - **EXPonential Function Reward (EXPR)**：缓解后期增益递减导致的学习效率下降问题。

### 相比现有方法的优势
- **速度极快**：相比进化算法提速约 **7,000 倍**；
- **覆盖性能更优**：平均覆盖率略高于基线方法；
- **部署效率显著提升**：提出的“效率指标”（efficiency metric）高出基线 **120 倍以上**；
- **可扩展性强**：适用于实时性要求高的军事应用场景。

---

## 2. 核心实验方法和设置

### 数据集与环境设置
- **仿真区域 S**：$20\text{km} \times 120\text{km}$，坐标范围 $[3\times10^4, 5\times10^4] \times [0, 1.2\times10^5]$；
- **雷达部署区 $D_R$**：左下矩形区域；
- **敌方干扰节点区 $D_j$**：右上矩形区域；
- **离散化粒度**：原始为每 100 米采样一点（共约 $2.4\times10^5$ 点），训练时简化为每 500 米采样，且仅保留上半区域以加速计算；
- **干扰源数量**：固定 3 个；
- **需部署雷达数**：4 个。

### 实验设置
- **测试数据集**：随机生成 500 组不同的干扰节点配置作为测试集；
- **训练过程**：在简化环境中进行，确保高效训练；
- **硬件平台**：
  - FARDA：Nvidia GeForce RTX 3090 GPU；
  - 进化算法：Intel Core i9-12900 CPU。

### 评估指标
1. **Coverage (%)**：被成功探测的区域占比（阈值 $T=0.5$）；
2. **Time (响应时间)**：单次部署所需时间；
3. **Efficiency Metric**（自定义）：
   $$
   \text{efficiency} = \frac{\text{coverage}}{\log(1 + \text{time})}
   $$
   用以衡量单位对数时间内的探测效能。

### 基线方法对比
- **PSO1D**：粒子群优化算法，在部署边界 BR 上运行；
- **GA1D**：遗传算法，在同一边界 BR 上运行；
> 注：作者通过预实验发现，在边界部署反而优于在整个区域内搜索，因此所有比较均基于边界部署策略。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）
| 方法       | Coverage (%) | Time     | Efficiency |
|------------|--------------|----------|-----------|
| PSO1D      | 93.75        | 24.9 min | 0.13      |
| GA1D       | 93.79        | 30.7 min | 0.12      |
| **FARDA**  | **93.94**    | **0.25s**| **4.21**  |

#### 对比分析
- **速度优势**：FARDA 比进化算法快约 **7,000 倍**（从半小时降至 0.25 秒）；
- **覆盖性能**：FARDA 覆盖率最高（+0.15%~+0.2%）；
- **效率指标**：FARDA 是 PSO1D 的 **32 倍以上**，是 GA1D 的 **35 倍以上**。

### 不同场景下的表现（Table 4）
将测试样本按难度分为三类：
| 类型   | 定义                     | FARDA 改进幅度 |
|--------|--------------------------|----------------|
| Bad    | GA1D & PSO1D < 90%       | +0.29%         |
| Normal | 90% ≤ ... < 95%          | +0.17%         |
| Good   | ≥95%                     | +0.08%         |

> 表明 FARDA 在**困难场景下提升更为明显**，具有更强鲁棒性和实用性。

### 消融实验结果

#### （1）网络结构消融（Table 5）
| 方法           | Coverage (%) |
|----------------|---------------|
| FARDA (完整)   | **93.94**     |
| w/o CNN        | 93.89         |
| w/o LSTM       | 93.90         |
| w/o Encoder    | 91.44         |

> 结论：**Encoder 至关重要**，缺少任一组件都会导致性能下降，尤其是完全移除 Encoder 导致覆盖率暴跌近 2.5%。

#### （2）奖励机制消融（Table 6）
| 方法             | Coverage (%) |
|------------------|---------------|
| FARDA (完整)     | **93.94**     |
| w/o CVDP         | 88.80         |
| w/o EXPR         | 93.90         |
| w/o CVDP-EXPR    | 88.80         |

> 结论：
- **CVDP 至关重要**：去除后性能大幅下降，且出现“所有雷达聚集一端”的训练崩溃现象（见 Figure 5）；
- **EXPR 作用较小但有益**：轻微改善后期学习稳定性；
- **CVDP 是防止模式坍塌的核心机制**。

---

## 4. 关键结论和发现

### 主要发现
1. **DRL 可有效解决复杂雷达部署问题**：通过合理建模与网络设计，DRL 能在保证高覆盖率的同时实现超高速部署；
2. **边界部署优于全域搜索**：实验证明限制雷达仅部署在区域边界（BR）不仅不损失性能，反而有助于收敛；
3. **环境简化显著加速训练**：通过降低分辨率与裁剪冗余区域，可在几乎不影响性能的前提下大幅提升训练效率；
4. **CVDP 奖励机制至关重要**：能有效引导雷达均匀分布，避免训练发散。

### 方法的局限性
- 当前模型假设为**静态干扰环境**：即干扰源位置不变，未考虑动态移动目标或跳频干扰；
- 部署区域形状较规则：尚未验证在复杂地形或多连通区域中的泛化能力；
- 雷达数量固定：未支持可变规模部署；
- 动作空间连续化处理可能导致微小定位误差。

### 未来工作方向
1. **扩展至更复杂的静态环境**：
   - 支持更多雷达/干扰源；
   - 更大或非规则部署区域；
2. **研究动态环境下的自适应部署**：
   - 干扰源随时间移动；
   - 雷达需平滑调整位置（不能突变）；
3. **引入多智能体协同机制**：多个雷达作为独立 Agent 协同决策；
4. **结合在线学习机制**：实现战场环境持续演化下的增量部署策略更新。

---

> ✅ 总结：FARDA 是首个将 DRL 成功应用于抗干扰雷达部署的工作，实现了**从“慢而准”到“快而更准”的范式转变**，具备极强的实战应用潜力。

</details>

---

### 6. [Using Large Language Models to Create Personalized Networks From Therapy Sessions](https://arxiv.org/abs/2512.05836)

**Authors**: Clarissa W. Ong, Hiba Arnaout, Kate Sheehan, Estella Fox, Eugen Owtscharow, Iryna Gurevych  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05836v1  

#### Abstract
Recent advances in psychotherapy have focused on treatment personalization, such as by selecting treatment modules based on personalized networks. However, estimating personalized networks typically requires intensive longitudinal data, which is not always feasible. A solution to facilitate scalabil...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Using Large Language Models to Create Personalized Networks From Therapy Sessions*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的**个性化治疗**（personalized treatment）依赖于密集的纵向数据（如生态瞬时评估，EMA）来构建**个性化网络**（personalized networks），用于案例概念化和干预规划。然而，这种方法存在显著局限：
- 数据收集成本高、负担重（患者需多次填写问卷）
- 节点变量通常是预设的，难以捕捉个体独特的心理过程
- 统计估计方法对数据质量要求高（如平稳性假设），在真实临床场景中难以满足

此外，人工构建网络耗时且易受主观偏见影响。

### 提出的新方法与创新思路
本文提出了一种**端到端的LLM驱动管道**（end-to-end pipeline），从**治疗会话转录文本**中自动生成**个性化网络**（personalized networks），支持案例概念化和治疗规划。

该方法的核心创新在于一个**三阶段多步流程**：
1. **过程检测**（Process Detection）：识别客户话语中的心理过程（psychological processes）并分类其维度（dimensions，基于EEMM框架）。
2. **主题聚类**（Theme Clustering）：将相似的心理过程聚类为具有临床意义的主题（clinical themes）。
3. **连接生成**（Connection Generation）：通过集成学习（ensemble learning）在主题之间生成可解释的有向连接（excitatory/inhibitory links）。

### 相比现有方法的优势
| 方面 | 统计估计网络（Statistically Estimated Networks） | 本文的LLM生成网络 |
|------|-----------------------------------------------|------------------|
| **数据来源** | 需要密集的纵向自我报告（如EMA） | 仅需治疗会话转录本 |
| **节点生成** | 预设变量，跨个体固定 | 从客户话语中自下而上提取，高度个性化 |
| **客户负担** | 高（频繁填写问卷） | 无 |
| **可扩展性** | 低（数据收集和分析复杂） | 高（自动化处理转录本） |
| **洞察深度** | 反映变量间统计关联 | 可揭示潜在主题和功能关系 |

---

## 2. 核心实验方法和设置

### 数据集
- **来源**：77次1小时的治疗会话转录本，来自6名被诊断为**重度抑郁障碍**（MDD, n=1）或**广泛性焦虑障碍**（GAD, n=5）的参与者。
- **标注**：专家手动标注了3,364个包含心理过程的客户话语（utterances），每个话语标注是否包含过程及对应的EEMM维度。
- **维度标签**：Affect, Cognition, Attention, Motivation, Sense of Self, Overt Behavior, Context/Moderators, Sociocultural, Biophysiological。

### 实验设置
#### 模型
- 主要使用开源大模型 **LLaMA-3.1-70B-Instruct**（本地运行，保障隐私）。
- 在连接生成阶段引入闭源模型（如GPT-4o-mini）进行集成学习。

#### 评估策略
采用分阶段评估：
- **过程检测阶段**：使用标准分类指标（F1, Precision, Recall）与人工标注对比。
- **主题聚类与连接生成阶段**：由三位领域专家进行**盲评**（blind evaluation），评估生成网络的质量。

#### 评估指标
- **过程检测**：F1 Score, Precision, Recall。
- **主题质量**（专家评分）：
  - **Insightfulness**：临床相关性（Clinical Relevance）、新颖性（Novelty）、有用性（Usefulness）
  - **Trustworthiness**：特异性（Specificity）、覆盖率（Coverage）、完整性（Completeness）、侵入性（Intrusiveness）、冗余性（Redundancy）
- **连接质量**：清晰度（Clarity）、连接质量（Connection Quality）、治疗洞察力（Therapeutic Insight）
- **总体比较**：直接提示（direct prompting）作为基线，与多步管道对比。

### 基线方法对比
- **Baseline**：使用“直接提示”（direct prompting）方法，即一次性让LLM从转录本生成完整网络（主题+连接），不经过中间步骤。
- **目标**：验证**多步分解**（multi-step decomposition）是否优于端到端生成。

---

## 3. 主要实验结果和性能指标

### 过程检测性能
- 在**few-shot**设置下（K=100），模型在识别心理过程任务上的**F1达到约90%**，表明LLM能准确从话语中识别临床相关内容。
- 即使在少量示例（few-shot）下，性能也显著优于zero-shot，说明**in-context learning**有效。

### 主题聚类质量（专家评分）
- 多步聚类方法在最终迭代（Stage 3）中得分：
  - **临床相关性**：72%
  - **新颖性**：75%
  - **有用性**：74%
- 显著优于单步聚类方法（初始得分分别为65%, 58%, 56%）。

### 连接生成方法比较
三种集成策略比较（prompt-based, temperature-based, model-based）：
- **Model-based ensemble**（使用多个不同LLM）表现最佳，在所有三项指标（Clarity, Connection Quality, Therapeutic Insight）上均获得最多专家偏好（约77%）。
- **Prompt-based ensemble**表现最差，说明仅靠提示变体多样性不足。

### 多步管道 vs. 直接提示（关键对比）
| 评估维度 | 多步管道偏好率 | 直接提示偏好率 |
|---------|----------------|----------------|
| **有意义的临床主题** | 89.4% | 10.6% |
| **信息丰富的连接** | 77.3% | 22.7% |
| **支持治疗规划** | 92.4% | 7.6% |

> **结论**：多步管道在所有方面显著优于直接提示，专家高达90%以上偏好该方法。

### 消融实验（间接体现）
- **两步聚类 vs. 单步聚类**：两步法更可控、可解释，生成主题质量更高。
- **集成学习 vs. 单一模型**：model-based ensemble表现最好，说明模型多样性提升可靠性。
- **温度设置**：除连接生成外，其余阶段均设temperature=0以确保输出确定性。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM可用于从治疗转录本中生成临床上有意义的个性化网络**，无需密集纵向数据。
2. **多步管道设计至关重要**：将任务分解为过程检测、主题聚类、连接生成三个阶段，显著优于端到端的直接提示方法。
3. **生成的网络具有高临床价值**：专家评价其在临床相关性、新颖性和有用性方面得分达72–75%，可用于案例概念化和治疗规划。
4. **集成学习提升连接质量**：特别是使用多个不同LLM的model-based ensemble效果最佳。
5. **方法具备可扩展性与实用性**：输入仅为转录本，输出为可视化网络，适合整合到临床工作流中。

### 局限性
1. **模型单一性**：主要依赖LLaMA-3.1，未系统比较多种LLM。
2. **数据规模小**：仅6名参与者，泛化能力有待验证。
3. **缺乏非语言信息**：仅基于文本，忽略了治疗中的非言语行为（如语调、表情）。
4. **人类评估主观性强**：部分指标（如新颖性、治疗洞察）专家间一致性较低（Cohen’s kappa < 0.3）。
5. **未与人类生成网络对比**：虽因认知负荷高而未实施，但仍缺少与“黄金标准”的直接比较。

### 未来工作方向
1. **动态网络构建**：整合多节治疗会话，追踪心理变化轨迹。
2. **治疗师网络生成**：分析治疗师干预策略，用于培训与督导。
3. **患者-治疗师网络融合**：构建互动视角下的联合网络。
4. **疗效验证研究**：开展临床试验，检验LLM生成网络是否能真正改善治疗结局（treatment utility）。
5. **与其他方法对比**：如与基于EMA数据的统计网络或人类印象生成的网络进行性能比较。

---

> **总结**：本文提出了一个**可行且高效的LLM驱动框架**，实现了从治疗对话中自动构建个性化心理网络，为**过程导向疗法**（PBT）和**精准心理健康**（precision mental health）提供了可扩展的工具，具有重要的临床应用潜力。

</details>

---

### 7. [Learning from Self Critique and Refinement for Faithful LLM Summarization](https://arxiv.org/abs/2512.05387)

**Authors**: Ting-Yao Hu, Hema Swetha Koppula, Hadi Pouransari, Cem Koc, Oncel Tuzel, Raviteja Vemulapalli  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05387v1  

#### Abstract
Large Language Models (LLMs) often suffer from hallucinations: output content that is not grounded in the input context, when performing long-form text generation tasks such as summarization. Prior works have shown that hallucinations can be reduced by iteratively critiquing and refining previously ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning from Self Critique and Refinement for Faithful LLM Summarization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在执行长文本生成任务（如摘要生成）时普遍存在**幻觉（hallucinations）**问题，即生成的内容无法在输入上下文中找到支持。尽管已有研究尝试通过推理阶段的迭代批评与修正（critique and refinement）来缓解该问题，但这些方法通常依赖更强的教师模型或多模型协作框架，并引入额外的**推理时计算开销（test-time compute）**，限制了其实际应用。

### 提出的新方法：SCRPO
本文提出 **Self Critique and Refinement-based Preference Optimization (SCRPO)**，一种**自监督训练框架**，利用同一个LLM自身的批评与修正能力，在训练阶段提升其摘要生成的忠实度（faithfulness），而无需外部监督或更强大的教师模型。

#### 方法流程：
1. 对无标签文档生成初始摘要（initial summaries）；
2. 同一个LLM对这些摘要进行**自我批评（self-critique）**，判断是否存在幻觉；
3. 基于批评反馈，同一模型对不忠实地摘要进行**自我修正（self-refinement）**；
4. 构建偏好数据集：将修正后的摘要作为 `chosen`，原始不忠实地摘要作为 `rejected`；
5. 使用 **Preference Learning**（基于DPO+NLL正则化）对该LLM进行微调。

### 相比现有方法的优势
| 维度 | 传统方法（如Test-Time Refinement） | SCRPO |
|------|-------------------------------|--------|
| 是否需要教师模型 | 是（或专用判别模型） | 否（仅用自身） |
| 推理时计算成本 | 高（需多次调用） | 无额外开销（训练后即固化） |
| 可扩展性 | 低（依赖多模型/强模型） | 高（单模型闭环） |
| 性能表现 | 中等 | 更优（见实验部分） |

> ✅ **核心创新**：首次将“自我批评+自我修正”机制用于构建偏好数据，并通过偏好学习实现**训练时知识蒸馏**，使模型内化忠实性判断能力。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个标准摘要基准上进行评估：
- **XSum**：新闻摘要，强调简洁性和事实准确性；
- **CNNDM**：新闻文章摘要；
- **SAMSum**：日常对话摘要，更具口语化挑战。

从各数据集训练集中采样10,000个文档用于SCRPO训练。

### 实验设置
- **基础模型**：Qwen2.5-7B-Instruct
- **适配器技术**：LoRA（rank=16, alpha=32）
- **生成方式**：Beam Search（beam size=5）
- **提示工程**：设计了多个prompt模板用于summarization、critique、fact extraction、NLI、refinement等步骤（详见Appendix A.1）

### 评估指标
| 类型 | 指标 | 描述 |
|------|------|------|
| **忠实性（Faithfulness）** | MiniCheck | 自动化事实核查工具，衡量摘要是否可被源文档支持 |
| | GPT4-Likert | GPT-4人工评分模拟，Likert量表评估忠实性 |
| **整体质量** | GEval系列 | 包括Coherence（连贯性）、Consistency（一致性）、Fluency（流畅性）、Relevance（相关性）四个维度，基于GPT-4评估 |

### 基线方法对比
- **Pretrained LLM**：未经微调的原始模型
- **MPO (Choi et al., 2024)**：基于不同解码策略生成偏好对的方法
- **SCOPE (Duong et al., 2025)**：使用context-less模型生成不忠实地响应构建偏好数据
- **SCRPO variants**：
  - SCRPO-Inference Time：直接在推理时执行critique & refine
  - SCRPO-Critique Only：仅用批评打分构造偏好，无修正
  - SCRPO-SFT：仅对修正后摘要做SFT，非偏好学习

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 3）

| 方法 | XSum MiniCheck | XSum GPT4-Likert | CNNDM MiniCheck | SAMSum MiniCheck |
|------|----------------|------------------|------------------|------------------|
| Pretrained LLM | 0.701 | 4.16 | 0.715 | 0.437 |
| MPO | 0.694 | 4.13 | 0.712 | 0.456 |
| SCOPE | 0.713 | 4.14 | 0.721 | 0.440 |
| SCRPO-Inference Time | 0.722 | 4.23 | 0.746 | 0.470 |
| SCRPO (Ours) | **0.761** | **4.38** | **0.806** | **0.523** |

> 🔍 所有数据集上，SCRPO均显著优于所有基线方法。

### 与基线方法的对比结果
- **相比MPO/SCOPE**：两者未能稳定超越预训练模型，说明其偏好构造未有效捕捉忠实性信号；而SCRPO通过显式的critique+refinement机制，显著提升MiniCheck和GPT4-Likert得分。
- **相比Inference-Time Refinement**：虽然也能提升忠实性，但SCRPO不仅效果更好，且**不增加推理开销**，效率更高。
- **跨领域泛化能力**（Table 4）：
  - 即使在目标域外训练（如用CNNDM训练、在XSum测试），SCRPO仍优于预训练模型；
  - 在XSum→SAMSum这种大领域迁移下，依然保持轻微优势，显示良好鲁棒性。

### 消融实验结果（Table 3 & Table 5）

#### 不同组件的影响（SCRPO变体）：
| 变体 | MiniCheck (XSum) | GPT4-Likert (XSum) | 结论 |
|------|------------------|--------------------|------|
| SCRPO-Critique Only | 0.738 | 4.33 | 仅有批评不足以最大化提升 |
| SCRPO-SFT | 0.735 | 4.23 | 仅SFT不如偏好学习有效 |
| SCRPO (完整) | **0.761** | **4.38** | **批评+修正+偏好学习缺一不可** |

#### 批评策略比较（Table 1）
- **Binary Feedback** vs **Fine-grained Feedback**
  - 后者（细粒度反馈）性能更优，因其分解为：
    1. Atomic Fact Extraction
    2. NLI 判断每个事实是否entailed
    3. 聚合非entailed事实作为反馈
  - 更精细的错误定位带来更高质量的修正和偏好数据。

#### 偏好三元组选择策略（Table 2）
- **Extreme Selection**（最差初始 + 最佳修正）效果最佳，平衡了忠实性与整体质量；
- Beam Search虽MiniCheck高，但GEval得分下降明显，影响实用性。

#### 人类评估结果（Table 5）
| 评估维度 | SCRPO胜出 | 平局 | Pretrained胜出 |
|----------|-----------|-------|----------------|
| 忠实性（Faithfulness） | **24%** | 75% | 1% |
| 整体质量（General Quality） | 31% | 41% | 28% |

> 👥 人类评估证实SCRPO显著提升忠实性，同时整体质量相当甚至略优。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SCRPO能有效提升LLM摘要的忠实性**：在XSum、CNNDM、SAMSum三大基准上全面超越SOTA自监督方法。
2. ✅ **训练时蒸馏优于推理时修正**：SCRPO不仅性能更强，而且推理零开销，更适合部署。
3. ✅ **细粒度批评机制更有效**：基于atomic fact extraction + NLI的反馈比二值判断更能引导高质量修正。
4. ✅ **偏好学习优于SFT**：直接学习“什么是更好的摘要”比单纯模仿修正结果更有效。
5. ✅ **具备跨域泛化能力**：即使训练与测试领域不同，仍能带来增益。

### 方法的局限性
- ❗ **模型容量要求较高**：实验表明小于3B参数的模型（如0.5B、1.5B）在SCRPO下出现性能退化（Figure 3），说明该方法依赖较强的内部推理能力。
- ❗ **依赖高质量prompt设计**：critique与refinement的效果高度依赖prompt的质量，存在工程调参负担。
- ❗ **未解决根本幻觉成因**：仍是后处理式改进，未触及LLM生成机制本身的不确定性问题。

### 未来工作方向
- 将SCRPO应用于其他条件生成任务（如问答、对话生成）；
- 探索轻量化版本以适配小模型；
- 结合解码时约束（如constrained decoding）进一步增强忠实性；
- 研究如何自动化优化critique与refinement的prompt模板。

---

> 📌 **总结一句话**：  
> SCRPO通过让LLM“自己批评自己并改进”，在训练中构建高质量偏好数据，实现了**无需教师模型、无推理开销、高忠实度**的摘要生成新范式，是迈向可信LLM的重要一步。

</details>

---

### 8. [IDK-S: Incremental Distributional Kernel for Streaming Anomaly Detection](https://arxiv.org/abs/2512.05531)

**Authors**: Yang Xu, Yixiao Ma, Kaifeng Zhang, Zuliang Yang, Kai Ming Ting  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05531v1  

#### Abstract
Anomaly detection on data streams presents significant challenges, requiring methods to maintain high detection accuracy among evolving distributions while ensuring real-time efficiency. Here we introduce $\mathcal{IDK}$-$\mathcal{S}$, a novel $\mathbf{I}$ncremental $\mathbf{D}$istributional $\mathb...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：IDK-S: Incremental Distributional Kernel for Streaming Anomaly Detection

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**数据流环境下的异常检测**（Streaming Anomaly Detection）中存在的两个核心挑战：
- **概念漂移**（Concept Drift）：数据分布随时间变化，传统静态模型失效；
- **计算效率瓶颈**：现有方法若采用全量重训练（retraining）策略，计算开销大，难以满足实时性要求。

许多现有流式异常检测器基于较弱的离线基础模型（如 iForest、LOF），其性能上限受限于这些基础模型的能力。

---

### 提出的新方法与创新思路
作者提出 **IDK-S**（Incremental Distributional Kernel for Streaming anomaly detection），一种面向数据流的增量式分布核方法，核心创新如下：

#### （1）继承并改进强大的离线检测器 IDK
- 将表现优异的离线检测器 **Isolation Distributional Kernel (IDK)** 成功适配到流式场景。
- IDK 基于 **kernel mean embedding (KME)** 框架，利用**数据依赖型核函数**（data-dependent kernel），在密度不均和局部异常检测上优于 iForest、LOF 等经典方法。

#### （2）设计轻量级增量更新机制
- 避免对整个模型进行昂贵的周期性重训练；
- 提出**增量分区替换机制**（incremental partition-update mechanism）：
  - 当滑动窗口移动时，仅移除来自过期数据批次的采样点及其对应的 hypersphere 分区；
  - 用新到达数据中的样本生成新的分区进行替换；
  - 维护特征映射总和（running sum of feature vectors）以高效更新均值映射。

#### （3）理论保障：统计等价性
- **Proposition 1** 证明：IDK-S 的增量更新策略在任意时刻生成的样本集合的概率分布，与完全重训练版本的 IDK 完全一致。
- 这意味着其效率提升**未牺牲模型的统计有效性**，区别于许多启发式近似方法。

---

### 相比现有方法的优势
| 维度 | IDK-S 的优势 |
|------|-------------|
| **准确性** | 基于更强的 IDK 核心，在多数数据集上显著优于基于 iForest/LOF/kNN 的流式方法 |
| **效率** | 时间复杂度从 $O(wbt)$ 降至 $O(lwt)$，其中 $l \ll w$，实现数量级加速 |
| **内存友好** | 空间复杂度为 $O(wd + btd + wbt)$，仅依赖窗口大小，适用于无限流 |
| **适应性** | 能有效跟踪非平稳分布（尤其是渐变型概念漂移） |

---

## 2. 核心实验方法和设置

### 使用的数据集
共使用 **13 个基准数据集**，涵盖：
- **11 个大规模静态异常检测数据集**（经随机打乱模拟流式输入）：
  - 包括 `Donors`, `Http`, `ForestCover`, `Fraud`, `Shuttle` 等；
  - 数据规模从数千到百万级（最大达 619k+ 实例），维度 3–48，异常比例 0.03%–32%。
- **2 个非平稳（non-stationary）真实流数据集**：
  - `INSECTS`：昆虫飞行行为受温度影响导致突变型概念漂移（abrupt drift）；
  - `TwoCluster`：合成数据，展示渐进式分布漂移（gradual drift）。

> 数据集详情见 Table 2。

---

### 实验设置与评估指标

| 设置项 | 描述 |
|--------|------|
| **窗口大小 $w$** | 固定为 2048 |
| **更新步长 $l$** | 固定为 100 |
| **集成数 $t$** | 所有集成方法设为 100（含 IDK-S、SiForest、oIFOR） |
| **IDK-S 参数 $\nu$** | 在 $[2^1, ..., 2^6]$ 范围内搜索最优值 |
| **运行次数** | 每个方法在每个数据集上执行 20 次取平均 |
| **硬件平台** | AMD 128核 CPU @2GHz，1TB RAM（所有方法统一） |
| **GPU 使用情况** | 仅 Memstream 使用 GPU，其余均为 CPU 运行 |

#### 评估指标
- **检测效果**：ROC AUC Score（Area Under the Curve）
- **运行效率**：总运行时间（seconds）
- **动态性能分析**：在非平稳数据上绘制滑动窗口内的瞬时 AUC 曲线

---

### 对比的基线方法（Baseline Methods）
代表当前主流研究方向，扩展不同基础模型至流式场景：

| 方法 | 类型 | 基础模型 |
|------|------|---------|
| **SiForest** | 流式方法 | Isolation Forest（基于 reservoir sampling） |
| **oIFOR** | 流式方法 | Isolation Forest（基于多分辨率直方图） |
| **DILOF** | 流式方法 | Local Outlier Factor（LOF） |
| **CPOD** | 流式方法 | Distance-based Outlier Detection |
| **Memstream** | 流式方法 | Deep Autoencoder + kNN（唯一使用 GPU） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）

| 指标 | IDK-S 表现 |
|------|----------|
| **平均 AUC** | **0.876**（所有方法中最高） |
| **次优方法** | SiForest（0.853），差距达 **2.2% 绝对提升** |
| **最差方法** | DILOF 和 CPOD（约 0.73） |
| **最快运行时间** | 平均 **27 秒**（远低于其他方法） |
| **最慢方法** | CPOD（1916 秒）、DILOF（781 秒） |

---

### 与基线方法的对比结果

#### ✅ 准确性方面
- 在 **13 个数据集中有 12 个取得最佳 AUC**；
- 唯一例外是 `Shuttle` 数据集（IDK-S: 0.976 vs SiForest: 0.996），但该现象已在离线 IDK 研究中被观察到（iForest 在此特定数据上略优），印证了“流式性能由基础模型决定”的核心论点。

#### ⚡ 效率方面（速度优势极为显著）
- 在六个最大数据集上，相比第二快的 oIFOR，**提速至少 5 倍以上**；
- 在小型高维数据（如 `Satellite`, `NYC_taxi_single`）上，**运行时间减少一个数量级**（10x 以上）；
- 总体平均运行时间仅为 SiForest 的 **1/16**，oIFOR 的 **1/8**，CPOD 的 **1/71**。

> 示例：`TwoCluster` 数据集（100万实例）
> - IDK-S: 124 秒
> - SiForest: 2194 秒 → **17.7x 更慢**

---

### 消融实验与参数分析（Parameter Analysis）

#### （1）最优采样数 $\nu$ 分析（Figure 5）
- **IDK-S 最优 $\nu$ 集中在 [2, 8]**；
- 对比：
  - oIFOR（max_leaf_samples）：[32, 128]
  - SiForest（reservoir_samples）：[256, 512]
- 结论：IDK-S 所需样本极少即可达到高性能 → **更高的样本效率**（sample efficiency）

#### （2）与 IDK 变体比较（Table 4）

| 方法 | 平均 AUC | 平均时间（秒） |
|------|----------|----------------|
| **Offline IDK**（批处理） | 0.898 | 8 |
| **Retraining-based IDK** | 0.879 | 352 |
| **IDK-S**（本文） | **0.877** | **14** |

- IDK-S 与 retraining 版本性能几乎持平（仅差 0.002 AUC），但速度快 **25 倍以上**；
- 显示增量机制在保持精度的同时极大提升了效率。

---

## 4. 关键结论和发现

### 主要发现
1. **流式异常检测的性能天花板受限于基础离线模型**：
   - 当前多数流式方法基于 iForest 或 LOF，天然继承其弱点（如对密度变化敏感）；
   - 改进方向应优先选择更强大的基础模型（如 IDK）。

2. **IDK-S 实现了精度与效率的双重突破**：
   - 凭借 IDK 的数据依赖核机制，在大多数场景下达到 SOTA 精度；
   - 增量更新机制使其比 retraining 快 25 倍，比现有流式方法快 8–70 倍。

3. **增量策略具有理论保证**：
   - Proposition 1 证明其与全量重训练在统计上等价，不是启发式近似。

4. **对渐进式概念漂移适应良好**：
   - 在 `TwoCluster` 上表现稳定；
   - 但在 `INSECTS` 的突变漂移后仍出现短暂性能下降，说明所有方法对此类变化均有挑战。

---

### 方法的局限性
- **对突变型概念漂移响应不够迅速**：虽然能最终恢复，但存在滞后；
- **初始化依赖第一个窗口的代表性**（cold-start problem）：若初始窗口缺乏正常模式多样性，可能影响早期检测质量；
- 当前未探索自适应参数调整机制（如动态调整 $\nu$ 或 $t$）。

---

### 未来工作方向
- 提升对**突发式概念漂移**的鲁棒性和响应速度；
- 探索结合**在线学习机制**或**记忆增强结构**来加快模型适应；
- 扩展至**多模态或图数据流**中的异常检测任务；
- 研究更高效的特征映射压缩技术以进一步降低内存占用。

--- 

> **总结一句话**：  
> IDK-S 通过将强大的离线检测器 IDK 与理论可证的增量更新机制相结合，在不损失统计有效性的前提下，实现了流式异常检测在**准确性**和**效率**上的双重飞跃，为高吞吐、非平稳环境下的实时异常监控提供了新范式。

</details>

---

### 9. [KQ-SVD: Compressing the KV Cache with Provable Guarantees on Attention Fidelity](https://arxiv.org/abs/2512.05916)

**Authors**: Damien Lesens, Beheshteh T. Rakhshan, Guillaume Rabusseau  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05916v1  

#### Abstract
The Key-Value (KV) cache is central to the efficiency of transformer-based large language models (LLMs), storing previously computed vectors to accelerate inference. Yet, as sequence length and batch size grow, the cache becomes a major memory bottleneck. Prior compression methods typically apply lo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# KQ-SVD: Compressing the KV Cache with Provable Guarantees on Attention Fidelity — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在基于 Transformer 的大语言模型（LLMs）中，**Key-Value (KV) Cache** 是加速自回归生成的关键机制，但也带来了显著的内存开销——其大小随序列长度和批处理规模线性增长，成为长上下文推理中的主要瓶颈。

现有的 KV Cache 压缩方法（如 K-SVD、EigenAttention）通常仅对 Keys 进行低秩分解，或简单拼接 Queries 和 Keys 后进行 SVD，忽略了注意力机制的本质依赖于 **query-key 内积（QK^T）**。这些方法无法最优地逼近实际的 attention matrix，导致压缩后 attention 输出失真较大。

---

### 🚀 提出的新方法：KQ-SVD
作者提出 **KQ-SVD**，一种针对 attention matrix $ \mathbf{QK}^T $ 的**闭式最优低秩近似方法**，直接最小化 attention 输出误差上界。

#### 创新思路：
- 将 KV Cache 压缩建模为对 **$ \mathbf{QK}^T $** 的低秩逼近问题，而非单独压缩 K 或联合嵌入 Q/K。
- 推导出该优化问题的**解析解（closed-form solution）**，通过计算 $ \mathbf{KQ}^T $ 的 SVD 得到最优投影矩阵。
- 同时考虑 Value 与 Output Projection 的交互，进一步提升输出保真度。

#### 数学形式化：
目标是最小化：
$$
\min_{\mathbf{A},\mathbf{B}} \| \mathbf{KAB}^\top \mathbf{Q}^\top - \mathbf{KQ}^\top \|_F
$$
其最优解由以下公式给出：
$$
\mathbf{A} = \mathbf{K}^+\mathbf{U}_R,\quad \mathbf{B} = \mathbf{K}^\top \mathbf{U}_R
$$
其中 $ \mathbf{U}_R $ 是 $ \mathbf{KQ}^T $ 的前 R 个左奇异向量构成的矩阵。

---

### 🔍 相比现有方法的优势
| 方法 | 缺陷 | KQ-SVD 如何改进 |
|------|------|----------------|
| **K-SVD** | 只压缩 Key，忽略 Query 分布；不能保证 QK^T 逼近质量 | 显式建模 QK^T，获得更优的 attention fidelity |
| **Eigen / Concatenated QK-SVD** | 对 Query 和 Key 拼接后做 SVD，对 norm 不平衡敏感 | 在任意缩放下保持稳定，理论证明其退化风险 |
| **LoRC / ASVD / Palue** | 压缩权重矩阵而非缓存本身，需训练或部署复杂 | **无需再训练**，适用于现成模型的后训练校准（post-training calibration） |

> ✅ **核心优势总结**：
> - **理论上最优**：首次提供对 attention matrix 的最优低秩逼近；
> - **计算高效**：可通过标准 SVD 快速求解；
> - **兼容性强**：支持 GQA 架构，并可扩展至 Value-Output 路径；
> - **鲁棒性好**：不受 query/key norm 差异影响。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用 **C4 dataset**（Colossal Clean Crawled Corpus）
  - 训练集用于学习低秩投影（calibration set）
  - 验证集用于评估压缩效果

### ⚙️ 实验设置
- **模型范围**：
  - 非 GQA 模型：`Llama2-7B`, `Llama2-13B`
  - 支持 GQA 的模型：`Llama3-8B`, `Mistral-7B-v0.3`
- **校准阶段（Calibration）**：
  - 使用 128 条训练序列（每条 2048 tokens），收集各层每个 attention head 的 K/Q/V 缓存并拼接成大矩阵
- **评估阶段**：
  - 使用 32 条验证序列（每条 2048 tokens），模拟 attention 计算过程
- **Rank Selection**：
  - 按层选择压缩秩 R，保留至少 90% 的谱能量（即相对 Frobenius error ≤ 0.1）

### 📊 评估指标
所有误差均为 **相对 Frobenius 范数误差**：
$$
\text{err} = \frac{\|\mathbf{M} - \hat{\mathbf{M}}\|_F}{\|\mathbf{M}\|_F}
$$
分别评估以下组件：
- Key ($ \mathbf{K} $)、Query ($ \mathbf{Q} $)、Value ($ \mathbf{V} $) 矩阵
- Attention Score Matrix ($ \mathbf{QK}^T $)
- 最终 Multi-Head Attention 输出（MHA(X)）

### 🔁 基线方法对比
- **K-SVD**：仅对 Key 矩阵做 SVD 压缩
- **Eigen**：将 Query 和 Key 拼接后做 SVD，实现 joint embedding
- **KQ-SVD**（本文方法）：基于 $ \mathbf{KQ}^T $ 的最优低秩逼近

---

## 3. 主要实验结果和性能指标

### 📈 总体性能对比（图1）
在所有测试模型上，KQ-SVD 表现出明显优势：

| 指标 | 结果 |
|------|------|
| **Attention Score Matrix ($ \mathbf{QK}^T $) 误差** | KQ-SVD 显著低于 K-SVD 和 Eigen（平均低 30–50%） |
| **MHA 输出误差** | KQ-SVD 在所有模型上均取得最低误差，尤其在 GQA 模型中优势更明显 |
| **Key 矩阵重建精度** | K-SVD 最佳（因其直接优化 K），但这是“虚假优势”，因未反映 attention fidelity |
| **Query 保持能力** | Eigen 和 KQ-SVD 均优于 K-SVD，说明 joint 处理 Query 更合理 |

> 💡 **关键观察**：尽管 K-SVD 能最好地重建 Key，但它严重损害了 $ \mathbf{QK}^T $ 的准确性，最终导致更高的 MHA 输出误差。

---

### 🔬 不平衡 Query-Key 实验（图2）
人为引入 scale imbalance：令 $ \mathbf{K}' = \beta \mathbf{K}, \mathbf{Q}' = \mathbf{Q}/\beta $

| 方法 | 对 scale 变换的鲁棒性 |
|------|------------------------|
| **K-SVD** | 不变（只依赖 K） |
| **KQ-SVD** | 不变（基于 $ \mathbf{KQ}^T $） |
| **Eigen** | **严重退化**！当 $ \beta > 1 $ 时迅速趋近于 K-SVD |

> ✅ 实验验证了 Theorem 4：**Eigen 方法在 query/key norm 不平衡时会退化为 K-SVD**，而 KQ-SVD 始终保持稳定。

例如，在 Llama2-7B 上：
- 当 $ \beta = 10 $ 时，Eigen 的输出误差几乎与 K-SVD 完全重合
- 而 KQ-SVD 误差始终保持最低且不变

---

### ✅ 消融实验与理论验证
- **Theorem 3 成立性验证**：实验证明 K-SVD 与 KQ-SVD 的误差差值确实等于被遗漏的 top-R singular space 匹配程度，验证了解析公式的正确性。
- **GQA 兼容性验证**：Theorem 5 表明只需将组内多个 Query 矩阵堆叠即可应用 KQ-SVD，实验显示其在 Llama3/Mistral 上依然有效。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Attention fidelity 的核心是 $ \mathbf{QK}^T $ 的逼近质量**，而不是单独压缩 Key 或 Value。
2. **现有方法（如 K-SVD、Eigen）本质上是非最优的**，前者忽略 Query 分布，后者对 norm 敏感。
3. **KQ-SVD 提供了第一个对 attention matrix 的闭式最优低秩逼近方案**，具有严格理论保障。
4. **实验全面验证了 KQ-SVD 在多种主流 LLM 上的一致优越性**，尤其是在 GQA 架构下表现突出。
5. **该方法无需重新训练**，可在 post-training 阶段完成校准，适合实际部署。

---

### ⚠️ 局限性
- **依赖校准数据集**：需要高质量、代表性强的数据来构建 calibration cache。
- **假设缓存具有低秩结构**：虽然实证成立，但在某些任务（如高度随机文本）可能失效。
- **未结合量化或其他压缩技术**：当前仅为低秩投影，未来可与其他方法（如量化、稀疏化）组合使用。

---

### 🔮 未来工作方向
1. **动态 Rank Allocation**：根据不同层/头的重要性自适应分配压缩秩 R。
2. **在线更新机制**：允许在推理过程中动态调整低秩子空间以应对分布漂移。
3. **与量化结合**：探索 KQ-SVD + KV Cache Quantization 的联合压缩策略。
4. **扩展到 Longformer / FlashAttention 架构**：适配稀疏注意力或线性注意力场景。

---

## ✅ 总结一句话
> **KQ-SVD 从 attention fidelity 出发，提出了首个对 $ \mathbf{QK}^T $ 的最优低秩压缩方法，在理论严谨性和实验性能上全面超越现有 KV Cache 压缩方案，是高效 LLM 推理的重要进展。**

</details>

---

### 10. [CureAgent: A Training-Free Executor-Analyst Framework for Clinical Reasoning](https://arxiv.org/abs/2512.05576)

**Authors**: Ting-Ting Xie, Yixin Zhang  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05576v1  

#### Abstract
Current clinical agent built on small LLMs, such as TxAgent suffer from a \textit{Context Utilization Failure}, where models successfully retrieve biomedical evidence due to supervised finetuning but fail to ground their diagnosis in that information. In this work, we propose the Executor-Analyst Fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**CureAgent: A Training-Free Executor-Analyst Framework for Clinical Reasoning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于小规模 LLM 的临床智能体（如 **TxAgent**）存在 **Context Utilization Failure**（上下文利用失败）问题：  
尽管能够成功检索到相关的生物医学证据，但在最终诊断推理时未能有效利用这些信息，导致“幻觉”（hallucination）和错误决策。

此外，还存在以下三类典型错误：
- **Reasoning & Retrieval Failure**（占失败案例的 65.8%）
- **Output Parsing Error**（19.2%）
- **Instruction Adherence Failure**（12.3%）

这些问题源于模型在长上下文、多步骤工具调用任务中难以维持逻辑一致性和语义连贯性。

---

### 🚀 提出的新方法与创新思路

提出 **Executor-Analyst Framework** ——一种无需训练的模块化多智能体协作架构，其核心思想是**解耦（Decoupling）**：

| 角色 | 功能 |
|------|------|
| **Executor（执行者）** | 使用经过微调的小模型（如 TxAgent / Llama-3.1-8B），专注于精确调用工具（Tool Calling），从 ToolUniverse 中检索原始证据 |
| **Analyst（分析者）** | 使用大上下文基础模型（如 Gemini 2.5），负责整合噪声证据、进行事实验证、补充缺失信息并生成最终诊断 |

#### 创新点包括：
1. **模块化解耦设计**：将“手”（tool execution）与“脑”（clinical reasoning）分离，兼顾语法精度与语义鲁棒性。
2. **Stratified Ensemble（分层集成）策略**：相比传统的全局聚合（Global Pooling），采用 Late Fusion 架构保留多样化的检索路径，避免早期信息瓶颈。
3. **Training-Free 设计**：不依赖昂贵的端到端微调，通过架构工程实现高性能，具备良好的可扩展性和适应性。
4. **引入搜索增强机制**：Analyst 可主动发起互联网搜索以补充工具未覆盖的信息，提升信息完整性。

---

### 🔍 相比现有方法的优势
| 方面 | 优势说明 |
|------|----------|
| 性能 | 在 CURE-Bench 上达到 SOTA 表现（83.803% 准确率），显著优于单模型或单一代理方案 |
| 鲁棒性 | 分层集成减少集体幻觉，提高复杂病例处理能力 |
| 可维护性 | 工具集更新无需重新训练，支持快速迭代 |
| 成本效率 | 免去大规模微调成本，仅需组合已有模型即可构建高阶系统 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **CURE-Bench@NeurIPS 2025**：一个面向大规模治疗决策推理的基准测试平台。
- 包含两个阶段的数据集：
  - `testset_phase1`（公开榜单）
  - `testset_phase2`（私有榜单，用于最终评估）
- 覆盖多种临床场景，要求模型通过调用超过 200 个工具（APIs、数据库等）完成多步推理。

### ⚙️ 实验设置
- **Executor**：使用 **TxAgent**（基于 Llama-3.1-8B 微调），负责多轮工具调用与证据收集。
- **Analyst**：使用 **Gemini 2.5-Pro / Flash** 等闭源大模型，负责综合分析与诊断输出。
- 引入 **Self-Consistency (SC)** 机制于两个阶段：
  - Executor 层：对多个并行实例的结果进行多数投票，提升检索稳定性。
  - Analyst 层：多个独立推理链融合，增强结论一致性。
- **Topological Configurations 对比**：
  - **Config A**: Global Pooling（Early Fusion）
  - **Config B**: Stratified Ensemble（Late Fusion）——本文提出的方法

### 🎯 评估指标
- 主要指标：**准确率（Accuracy %）**
- 辅助分析：失败模式分类、上下文长度影响、工具数量扩展性测试

### 🆚 基线方法对比
| 基线模型 | 是否使用工具 | 特点 |
|--------|-------------|------|
| TxAgent (baseline) | 是 | 经过微调，擅长工具调用但推理弱 |
| Llama-3.1-8B, Qwen3-8B 等开源模型 | 否 | 零样本表现差（<30%） |
| Gemini 2.5 系列 | 否 / 是（generic search） | 推理强，但缺乏专用工具接口 |
| 单一模型 + Self-Consistency | 是 | 如 TxAgent(n=30)，作为强基线 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（均在 `phase2` 测试集上报告）

| 模型配置 | 准确率 (%) |
|---------|-----------|
| TxAgent (single) | 69.325 |
| Gemini-2.5-flash (no tools) | 63.104 |
| TxAgent + SC (n=30 executors) | 73.508 |
| **Executor-Analyst (Config A, Global Pooling)** | **79.311 ~ 80.510** |
| **Executor-Analyst (Config B, Stratified Ensemble)** | **81.367** |
| **+ Gemini with search** | **83.803** ✅ (**SOTA**) |

> 💡 最终提交版本使用：**TxAgent (n=10) + Gemini-2.5-flash-with-search (n=3) + Stratified Ensemble**

---

### 🔬 消融实验结果

#### (1) 温度参数调优（Executor）
| Temperature | Phase1 Score |
|------------|------------|
| 0.6        | 58.950     |
| 0.7        | 59.248     |
| **0.8**    | **65.214** ✅ |
| 0.9        | 56.747     |

→ 表明适度增加生成随机性有助于探索更优工具调用路径。

#### (2) Self-Consistency 效果（Executor 层）
- 随着采样路径数 $n$ 增加，准确率持续上升：
  - $n=1$: ~69.3%
  - $n=15$: ~73.3%
  - $n=60$: ~74.2%
- 收益递减出现在 $n > 20$

#### (3) 架构拓扑对比（Late vs Early Fusion）
- **Global Pooling (A)**：虽能降噪，但易丢失稀有但关键的证据。
- **Stratified Ensemble (B)**：通过保持多个独立推理流，在后期融合答案，显著提升性能（**+0.866%**），验证了多样性保留的重要性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Decoupling is Key**：将工具执行与临床推理分离可显著缓解 Context Utilization Failure，是性能跃升的核心驱动力。
2. **Topology Matters**：Late Fusion（Stratified Ensemble）优于 Early Fusion，证明信息多样性在复杂推理中的价值。
3. **Search Enhances Robustness**：即使已有专用工具集，通用搜索引擎仍能补充遗漏信息，进一步提升准确性。
4. **Training-Free Architecture Engineering is Powerful**：无需微调即可超越微调过的专用模型，凸显架构设计的重要性。

---

### ⚠️ 方法的局限性
1. **Curse of Dimensionality in Action Spaces**：
   - 当 ToolUniverse 扩展至 600+ 工具时，TxAgent 准确率从 92.0% 下降至 87.5%。
   - 表明扁平化工具检索面临维度灾难，需引入层次索引或分类机制。

2. **Context-Performance Paradox**：
   - 上下文长度超过 **12k tokens** 后，性能反而下降（从 94% → 87.93%）。
   - 过多噪声信息干扰注意力机制，形成“信息过载”。

3. **依赖闭源模型（Gemini）**：
   - 当前框架严重依赖 Gemini 等闭源强推理模型，限制了完全开源部署的可能性。

---

### 🔮 未来工作方向
1. **Hierarchical Tool Indexing**：按临床领域组织工具库，实现粗粒度→细粒度检索。
2. **Information Compression & Early Rejection**：基于置信度过滤无关工具输出，提升上下文效率。
3. **Training-Free Adaptation via ICL/RAG**：利用 In-Context Learning 或 RAG 自动引导模型使用新工具，避免频繁微调。
4. **Curriculum Learning for Executors**：逐步暴露更大工具集，提升抗干扰能力。
5. **向 Zero-Shot Tool Use 演进**：随着 Foundation Model 能力增强，Executor 可简化为 Prompt-Based 模块，进一步降低系统复杂度。

---

## ✅ 总结

**CureAgent** 提出了一种新颖且高效的 **Training-Free Executor-Analyst Framework**，通过解耦工具执行与临床推理，在 **CURE-Bench** 上实现了 **83.803%** 的 SOTA 准确率。该工作不仅展示了架构创新的巨大潜力，也揭示了当前临床 AI 面临的关键挑战：**信息多样性保护、上下文效率优化、以及大规模动作空间下的可扩展性**。其提出的 **Stratified Ensemble** 和 **late fusion** 策略为下一代可信医疗 AI 系统提供了重要范式参考。

</details>

---

### 11. [Capturing Classic Authorial Style in Long-Form Story Generation with GRPO Fine-Tuning](https://arxiv.org/abs/2512.05747)

**Authors**: Jinlong Liu, Mohammed Bahja, Venelin Kovatchev, Mark Lee  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05747v1  

#### Abstract
Recent advances in large language models (LLMs) show impressive performance in open-ended story generation, but fine-grained stylistic control remains limited. Existing methods often rely on shallow cues (e.g., names or topics) to simulate authorial style, without robust evaluation. In this work, we...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**大语言模型（LLM）在长篇故事生成中缺乏精细风格控制能力**的问题。尽管当前的 LLM 如 GPT-4o 和 Claude 在开放性写作任务上表现流畅，但它们对“经典作者风格”的模仿往往停留在表面线索（如角色名字、主题元素），而非真正捕捉文学语调、叙事节奏等深层风格特征。此外，现有方法缺乏可靠的评估机制来衡量风格一致性。

---

### 提出的新方法与思路
本文提出了一种基于 **Group Relative Policy Optimization (GRPO)** 的训练框架，用于实现**以作者风格为条件的长篇故事生成**。其核心创新包括：

- **将作者身份验证（Authorship Verification, AV）信号融入奖励建模**：通过微调一个 sentence transformer 模型作为风格奖励函数，使其能够量化生成文本与目标作者（如 Mark Twain）之间的风格相似度。
- **多奖励系统设计**：
  - **Style Reward**：基于 AV 得分，引导模型向特定写作风格靠拢；
  - **Content Reward**：评估叙事质量（连贯性、人物发展等）；
  - **Completeness Reward**：确保输出达到目标长度并有完整结尾。
- **动态可解释的风格评分机制**：利用奖励模型输出作为生成过程中的实时风格对齐度量，提供可测量、可控的风格生成管道。

---

### 相比现有方法的优势
| 维度 | 传统方法 | 本工作 |
|------|--------|-------|
| 风格建模方式 | 浅层提示（prompt engineering）、关键词复制 | 基于 AV 的深度风格嵌入学习 |
| 奖励机制 | 单一奖励或人工标注偏好（如 DPO） | 多维度、自动化的奖励组合（GRPO + 自定义 reward） |
| 输出多样性 | 受限于固定 accept/reject 对（DPO 缺陷） | GRPO 支持更丰富的探索路径 |
| 训练效率 | PPO 需要 critic 网络，计算开销高 | GRPO 不需独立 critic，降低资源消耗 |

> ✅ **优势总结**：该方法实现了**在较小规模模型（8B）下超越更大模型（如 GPT-4o）的风格模仿能力**，同时保持合理的叙事质量和结构完整性。

---

## 2. 核心实验方法和设置

### 数据集
- **基础数据来源**：Project Gutenberg 公共领域文本，涵盖 84 位作家的 161 本书籍。
- **目标作者聚焦**：以 **Mark Twain** 为核心研究对象，使用 *The Adventures of Huckleberry Finn* 作为风格参考范例。
- **数据预处理**：
  - 文本切分为 1500 字左右的段落（chunk）；
  - 构造三种配对类型用于训练风格判别器：
    - 正样本（Positive）：同一作者不同书籍的内容经掩码重建后对比；
    - 负样本（Negative）：不同作者之间对比；
    - Refilled-refilled 对比：控制内容一致但风格变化程度。

### 实验设置
- **模型架构**：采用 8B 参数级别的 LLM 进行 GRPO 微调。
- **训练流程**：
  1. **SFT（Supervised Fine-Tuning）阶段**：先用通用故事提示训练基础叙事能力；
  2. **GRPO 阶段**：引入风格化提示（如 “in the style of Mark Twain”），结合三重奖励进行强化学习优化。
- **超参数选择**：
  - 学习率：3×10⁻⁶；
  - β（KL 散度系数）：0.035（经调参确定最优稳定性）；
  - 批大小：每 prompt 至少采样 16 条响应。

### 评估指标
| 指标 | 定义 | 用途 |
|------|-----|-----|
| **Style Score** | 基于 fine-tuned sentence transformer 输出的 AV 相似度得分（经 sigmoid 归一化） | 衡量风格对齐程度 |
| **Content Score** | 使用 LLM-as-a-judge 方法，按四级评分标准打分（1–4 分），归一化至 [0.25, 1] | 评估叙事质量 |
| **Completeness Score** | 是否完成句子 + 达到约 1500 字，未完成则乘以 0.5 惩罚因子 | 控制输出完整性 |
| **Weighted Avg Score** | 最终综合得分 = `0.6×Style + 0.3×Content + 0.1×Completeness` | 综合评价模型性能 |

### 基线方法对比
参与比较的主流 LLM 包括：
- GPT-4o
- Claude Sonnet 4
- Gemini 2.5 Flash
- Qwen2.5-32B
- Gemma-3-27B

所有模型均在同一组 10 个 prompt 上生成故事，并由统一的 reward model 打分，保证公平性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）
| Model | Style ± SD | Content ± SD | Completeness ± SD | Weighted Avg ± SD |
|-------|------------|--------------|--------------------|-------------------|
| **FT-Agentic (8B)** | **0.628 ± 0.336** | 0.750 ± 0.000 | 0.744 ± 0.227 | **0.676 ± 0.203** |
| GPT-4o | 0.510 ± 0.194 | **0.800 ± 0.100** | 0.873 ± 0.136 | 0.633 ± 0.117 |
| Claude Sonnet 4 | 0.450 ± 0.203 | 0.750 ± 0.000 | 0.999 ± 0.002 | 0.595 ± 0.122 |
| Qwen2.5-32B | 0.328 ± 0.218 | 0.750 ± 0.000 | 0.911 ± 0.013 | 0.513 ± 0.131 |

> 🔍 **关键发现**：
> - 尽管我们的 8B 模型在 **Content** 上略逊于 GPT-4o，在 **Completeness** 上不如 Claude，但在最关键的 **Style Score 上显著领先（0.628 vs. 0.510）**；
> - 综合得分也最高（**0.676 > 0.633**），说明该方法在风格控制方面具有明显优势。

---

### 消融实验与分析（隐含在文中）
虽然没有明确列出消融表，但从以下几点可推断关键组件作用：

- **风格奖励模型的有效性**：
  - 表 2 显示，经过 fine-tuning 后，各 sentence transformer 模型的 same-author 与 cross-author 分布完全分离（IQR Overlap = 0%），表明 AV 模型具备强大判别力。
  - 特别是 **FT-all-distilroberta-v1** 实现最大 Δ（+0.628），成为最强风格判别器。

- **GRPO 中 β 参数的影响**（图 3）：
  - β < 0.04 导致 KL 散度剧烈波动，训练不稳定；
  - 最终选定 β = 0.035 并配合梯度归一化，实现稳定收敛。

- **为何不选最强判别器？**
  - 尽管 FT-all-distilroberta-v1 判别最强，但它导致早期 RL 探索困难（reward 过低）；
  - 因此选用 **FT-all-mpnet-base-v2**，因其提供更平滑的奖励梯度，利于策略迭代。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **GRPO + AV-based reward 是有效的风格控制方案**：即使在 8B 规模模型上，也能生成高度风格对齐的长篇故事，且优于 GPT-4o 等商业大模型。
2. ✅ **风格与叙事完整性存在权衡**：提升风格相似性可能导致叙事完整性下降（如结尾仓促、逻辑断裂），这是当前 RL 框架下的固有挑战。
3. ✅ **风格可以被量化并用于指导生成**：通过构建可控的数据增强与 fine-tuned reward model，实现了风格对齐的可测量、可优化。

---

### 局限性
1. **训练数据单一**：仅基于 Mark Twain 的一部作品（*Huckleberry Finn*）进行风格建模，泛化能力受限；
2. **内容奖励模型不可靠**：依赖 LLM-as-a-judge 方式打分，存在误判风险，影响训练稳定性；
3. **缺乏人类评估**：目前所有评估均为自动化指标驱动，无法判断风格是否“真实可信”；
4. **叙事完整性仍不足**：部分生成故事虽风格鲜明，但情节收束不佳，影响阅读体验。

---

### 未来工作方向
1. **扩展多作者、多文体训练集**：纳入更多经典作家（如 Jane Austen, Hemingway）以增强泛化能力；
2. **改进内容奖励模型**：引入更强的因果推理与情节一致性检测模块；
3. **加入人类反馈（Human-in-the-loop）**：开展用户研究，收集对风格真实性与叙事质量的人工评分；
4. **探索全局叙事规划机制**：在生成前引入大纲生成器或 plot controller，提升长程一致性。

---

> 📌 **总体评价**：  
> 本文展示了如何通过 **GRPO + 定制化 reward modeling** 实现高质量的经典作者风格故事生成。它不仅提出了一个实用的技术路径，也为“风格可控创作”这一创造性 AI 任务提供了可复现、可评估的研究范式。尽管在叙事完整性方面仍有挑战，但其在小模型上超越大模型的表现，证明了**任务专用训练的价值**。

</details>

---

### 12. [Bridging quantum and classical computing for partial differential equations through multifidelity machine learning](https://arxiv.org/abs/2512.05241)

**Authors**: Bruno Jacob, Amanda A. Howard, Panos Stinis  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05241v1  

#### Abstract
Quantum algorithms for partial differential equations (PDEs) face severe practical constraints on near-term hardware: limited qubit counts restrict spatial resolution to coarse grids, while circuit depth limitations prevent accurate long-time integration. These hardware bottlenecks confine quantum P...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Bridging Quantum and Classical Computing for Partial Differential Equations through Multifidelity Machine Learning  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前量子计算在求解偏微分方程（PDEs）方面虽有理论加速潜力，但在**近中期硬件（NISQ devices）上面临严重限制**：
- **qubit 数量有限** → 空间分辨率低（粗网格）
- **电路深度受限** → 时间积分精度差，难以进行长时间模拟

这导致量子 PDE 求解器只能产生**低保真度（low-fidelity）结果**，无法满足实际科学计算需求。

### 🚀 提出的新方法
作者提出一种**混合量子-经典多保真度机器学习框架（hybrid quantum-classical multifidelity learning framework）**，其核心思想是：
- 利用**量子算法生成大量低保真数据**（coarse-grid, short-time）
- 结合**少量高保真经典仿真数据**（fine-grid, long-time）
- 通过**多保真度神经网络架构**（Multifidelity KAN）学习从 LF 到 HF 的映射，实现误差校正与时间外推

该框架的关键组成部分包括：
- **量子求解器**：采用 QLBM-frugal 算法（Quantum Lattice Boltzmann Method），资源高效，适合 NISQ 设备
- **经典求解器**：提供稀疏的高保真参考数据
- **修正模型**：基于 Kolmogorov-Arnold Networks (KAN)，分离线性和非线性修正分支，并通过可学习参数 $\alpha$ 动态加权融合

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **实用性提升** | 首次将真实量子算法输出与多保真学习结合，使当前硬件上的量子设备具备“实用价值”（practical quantum utility） |
| **数据效率高** | 仅需**稀疏高保真训练数据**即可完成有效校正，大幅减少昂贵的经典高分辨率模拟需求 |
| **泛化能力强** | 能够实现**时间域外推（temporal extrapolation）**，预测远超高保真训练窗口的时间演化 |
| **架构设计先进** | 使用 KAN 替代传统 MLP，具有更强的函数逼近能力和参数效率；复合线性/非线性路径增强鲁棒性 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集与物理问题
实验验证了两个典型非线性 PDE 问题：

| 问题 | 描述 |
|------|------|
| **Viscous Burgers Equation (1D)** | 非线性对流-扩散方程，初始为高斯脉冲，形成激波传播，测试空间锐利梯度捕捉能力 |
| **2D Lid-Driven Cavity Flow (Navier-Stokes)** | 不可压流体流动，Re=100，存在主涡与角涡，测试复杂流场结构恢复能力 |

#### 数据来源
- **低保真数据（LF）**：由 QLBM 量子电路模拟生成（statevector emulation）
  - Burgers: $N=16$ 网格 vs 经典 $N=256$
  - Cavity: $16\times16$ vs $64\times64$
- **高保真数据（HF）**：经典有限差分/有限体积法求解获得
- **训练策略**：
  - LF 网络训练使用全时间段数据（如 $t \in [0, 0.5]$）
  - HF 修正网络仅使用前半段数据训练（如 $t \leq 0.25$），后半段用于测试**外推能力**

### ⚙️ 实验设置
- **量子模拟平台**：Qiskit statevector simulator（非真实硬件，但模拟真实电路行为）
- **硬件配置**：NVIDIA A100 GPU
- **网络结构**：
  - 低保真网络 $K_{LF}$：输入 $(x,t)$，输出 $q_{LF}$
  - 多保真网络：$q_{MF} = \alpha \cdot K_{nl}(x,t,q_{LF}) + (1-\alpha)\cdot K_{lin}(x,t,q_{LF})$
    - $K_{lin}, K_{nl}$ 分别为线性和非线性 KAN 子网
    - $\alpha$ 可学习，初始化为 0.5，受正则项 $\lambda \alpha^4$ 控制以偏好线性模型
- **优化器**：Adam ($lr=10^{-3}$)
- **训练流程**：
  1. 先训练 $K_{LF}$ 匹配量子输出
  2. 冻结 $K_{LF}$ 权重，联合训练 $K_{lin}, K_{nl}, \alpha$

### 📈 评估指标
- **相对 $L_2$ 误差**：
  $$
  L^2 = \frac{\|q_{pred} - q_{true}\|_2}{\|q_{true}\|_2}
  $$
- 分区域报告：
  - 训练区（$t \leq t_{cut}$）
  - 外推区（$t > t_{cut}$）
  - 全域平均
- 对比对象：
  - Raw quantum LF solver（未修正）
  - Classical HF solver（黄金标准）

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能数据汇总

#### ✅ **Viscous Burgers 方程（Table 1）**
| 方法 | 全域 $L^2$ 错误 | 外推区错误 | 提升幅度 |
|------|------------------|------------|----------|
| Raw LF Solver | 0.335 | — | — |
| Best MF (B4) | **0.0615** | 0.0914 | ↓ **82%** |

> 最优配置（$\lambda=10^{-4}, G=5$）在外推区域也显著优于原始 LF（0.1159 → 0.0914）

#### ✅ **Lid-Driven Cavity 流动（Table 2）**
| 方法 | 水平速度 $L^2$ | 垂直速度 $L^2$ | 提升幅度 |
|------|------------------|------------------|----------|
| Raw LF Solver | 0.285 | 0.412 | — |
| Best MF (C3) | **0.079** | **0.167** | ↓ **72% (u), 60% (v)** |

> 在 $t=3.0$（完全外推）仍能准确重建主涡结构

### 🔁 与基线方法对比
| 基线 | 表现 |
|------|------|
| **纯非线性修正（$\alpha=1$ 固定）** | 外推性能急剧下降（Burgers: $L^2=0.7173$），说明过度依赖非线性易过拟合 |
| **无正则化（$\lambda=0$）** | 训练误差低但外推差，$\alpha \approx 0.7$，偏向复杂模型 |
| **过大的 B-spline 网格（G=7）** | 出现明显过拟合（extrap error ↑） |
| **过窄网络宽度（width=5）** | 欠拟合，误差最高 |

### 🔍 消融实验关键发现
| 实验变量 | 发现 |
|---------|------|
| **Blending Regularization ($\lambda$)** | 中等强度正则化（如 $10^{-4} \sim 10^{-5}$）最优，促使模型选择更简单的线性主导修正，提升泛化 |
| **B-spline Grid Resolution (G)** | $G=5$ 平衡表达力与泛化，$G=7$ 过拟合，$G=3$ 欠拟合 |
| **Network Width** | 宽度增加不必然提升性能；适度容量 + 正则化 > 单纯增大模型 |
| **Temporal Extrapolation** | 所有案例中，MF 模型在无 HF 数据的后期仍保持高精度，证明其真正“学到”了修正规律 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **量子-经典协同可行**：即使量子求解器因硬件限制只能运行在粗网格、短时间尺度，其输出仍可作为有效的**低保真源**用于构建高性能混合模型。
2. **多保真学习显著提效**：通过引入仅含少量 HF 数据的 KAN 修正网络，可将量子解的误差降低 **70–80%**，达到接近经典高保真解的精度。
3. **具备强外推能力**：模型能在**完全没有高保真训练数据的时间区间内做出准确预测**，这对长期动力学模拟极具意义。
4. **线性修正至关重要**：消融实验证明，强制使用纯非线性修正会导致灾难性外推失败，而允许线性路径存在并加以正则化，能自动平衡模型复杂度，提升稳定性。

### ⚠️ 方法的局限性
- 当前实验基于 **statevector 模拟**，尚未在真实量子硬件上运行（噪声、测量误差未考虑）
- 所有 HF 数据仍需来自经典求解器，若 HF 获取成本极高，则整体收益受限
- 架构依赖于良好的 LF-HF 相关性，对于高度混沌系统可能挑战更大

### 🔮 未来工作方向
1. **扩展至更多 PDE 类型**：如反应-扩散系统、弹性力学、Maxwell 方程等
2. **集成 Physics-Informed Learning**：加入残差约束，进一步减少对 HF 数据的依赖
3. **部署到真实量子设备**：研究噪声鲁棒性及采样误差影响
4. **动态自适应保真度调度**：根据误差估计动态决定何时调用 HF 求解
5. **Operator Learning 扩展**：使用 DeepONet 或 Fourier Neural Operators 实现跨参数化的多保真建模

---

## 总结
该论文成功展示了如何利用 **Multifidelity Machine Learning** 桥接当前受限的量子计算能力与实际科学计算的需求。它不仅提出了一个**实用的混合量子-经典框架**，还通过严谨实验验证了其在非线性 PDE 上的**高精度校正与时间外推能力**，为近中期量子设备在计算物理中的“有用性”开辟了一条切实可行的技术路径。

</details>

---

### 13. [Bounded Graph Clustering with Graph Neural Networks](https://arxiv.org/abs/2512.05623)

**Authors**: Kibidi Neocosmos, Diego Baptista, Nicole Ludwig  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05623v1  

#### Abstract
In community detection, many methods require the user to specify the number of clusters in advance since an exhaustive search over all possible values is computationally infeasible. While some classical algorithms can infer this number directly from the data, this is typically not the case for graph...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Bounded Graph Clustering with Graph Neural Networks

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于 **Graph Neural Networks (GNNs)** 的图聚类（community detection）方法存在一个关键缺陷：即使用户指定了期望的聚类数量 $ c $，模型也常常无法返回**恰好** $ c $ 个社区。这是由于：
- 聚类过程是通过优化目标函数（如 modularity）实现的；
- 节点以“软分配”方式被分配到社区（soft cluster assignment），最终通过 `argmax` 转换为硬划分；
- 在此过程中，某些社区可能没有节点被分配，导致实际输出的社区数少于指定值。

此外，大多数 GNN 方法要求预先知道确切的社区数量，而现实中该数值通常是未知的。

---

### 提出的新方法与创新思路
作者提出了一种**灵活且原则性的约束机制**，用于控制 GNN 输出的社区数量：

#### （1）引入可调节的上下界约束
- 用户可以指定一个**范围** $[l, c]$，其中：
  - $ c $：最大社区数（上界，由 cluster assignment matrix 列数决定）
  - $ l $：最小社区数（下界，由新增的 **constraint term** 控制）
- 当 $ l = c $ 时，强制模型输出**精确数量**的社区。

#### （2）设计新的损失函数项
提出了一个新的可微分的约束项（constraint term）：
$$
\text{constraint}(S, l) = l - \sum_{k=1}^{l} \max_{1 \leq i \leq n} \frac{s_{ik}}{\max_k s_{ik}}
$$
该约束作用于 cluster assignment matrix $ S $，对每一行进行归一化后，统计最有可能成为主导社区的前 $ l $ 个列的最大值之和，鼓励至少 $ l $ 个社区非空。

#### （3）结合平衡正则化项
引入改进版的 balance regularization term：
$$
\text{balance}(S) = \frac{\|\text{diag}(S^T S)\|_2^2}{n \|e_c\|_2^2}
$$
它鼓励各社区大小均衡，避免 singleton clusters（单节点社区），从而提升聚类质量。

最终损失函数为：
$$
\mathcal{L} = \text{modularity}(S) + \mu \cdot \text{constraint}(S, l) + \lambda \cdot \text{balance}(S)
$$

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **可控性更强** | 支持指定社区数量范围或精确值，解决了 GNN 无法可靠返回指定数量社区的问题 |
| **灵活性更高** | 可作为搜索最优社区数的基础框架（例如逐步收紧区间） |
| **兼容性强** | 可嵌入任何基于 modularity 或 min-cut 的 GNN 聚类模型中 |
| **效果更稳定** | 实验表明能显著提高输出社区数的准确性，并改善聚类质量（如 ARI） |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 合成数据（Synthetic Data）
- 基于 **Stochastic Block Model (SBM)** 生成
- 参数变化包括：
  - 网络规模：Small ($10^2$), Medium ($10^3$), Large ($10^4$)
  - 社区数量：5, 10, 20
  - 密度：Low (~0.01–0.05), Medium (~0.07–0.1), High (~0.15)
- 每种配置生成 10 个网络，共数十组实验
- 节点特征使用邻接矩阵的行向量（提供拓扑信息）

#### 真实世界数据（Real-world Datasets）
| 数据集 | 节点数 | 边数 | 描述 |
|--------|-------|-----|------|
| **Cora** | 2,708 | 5,429 | 引用网络，7 类主题 |
| **Citeseer** | 3,312 | 4,715 | 引用网络，6 类主题 |
| **PubMed** | 19,717 | 44,338 | 医学文献引用，3 类 |
| **Actor** | 7,600 | 30,019 | 演员共现网络，类别来自角色属性 |

> 注：真实数据中的标签仅作为参考边界，不视为“真实社区”。

---

### 实验设置与评估指标

#### 模型架构
- GNN backbone：**GraphSAGE**（mean aggregation）
- 后接两层 MLP 输出 cluster assignment matrix $ S $
- 所有模型共享相同结构，仅 loss 函数不同

#### 对比模型（Loss Function 差异）
| 模型名称 | Loss 组成 |
|---------|----------|
| **GNN** | 仅 modularity |
| **GNN+REG** | modularity + balance regularization |
| **GNN+CONSTRAINT** | modularity + constraint($l$) |
| **GNN+REG+CONSTRAINT** | modularity + constraint($l$) + balance |

> 所有实验运行 3 次（不同随机种子），报告均值与标准差

#### 超参数
- $\mu = \lambda = 1$
- 学习率：$10^{-3}$
- 优化器：Adam
- 训练轮次：3000 epochs
- GNN 层数：合成数据用 3 层，真实数据用 4 层

#### 评估指标
- **输出社区数量**：是否落在 $[l, c]$ 内，是否接近 ground truth
- **Adjusted Rand Index (ARI)**：衡量预测社区与真实标签的一致性（已修正随机因素）
  $$
  \text{ARI} \in [-1, 1], \quad \text{越高越好}
  $$

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### ✅ 成功强制满足社区数量约束
- 在几乎所有实验中（除极少数边界情况），**GNN+CONSTRAINT** 和 **GNN+REG+CONSTRAINT** 都成功将输出社区数限制在 $[l, c]$ 范围内。
- 图 1(a) 显示：当 $ l \leq 5 $（ground truth）时，输出多样；当 $ l > 5 $，输出集中在 $ l $ 上，说明约束生效。
- 图 A.3 表明：当 $ l = c $ 时，模型能准确输出指定数量的社区。

#### 🔺 ARI 表现优异
- 图 1(b) 和 图 4 显示，在 $ l = 4,5 $ 附近 ARI 最高，符合预期（ground truth 为 5）
- 当 $ l > 5 $，ARI 下降，说明强行拆分会破坏自然社区结构
- **GNN+REG+CONSTRAINT** 的 ARI 显著高于其他方法（尤其在 20 社区场景下）

#### 📊 社区数量预测对比（图 2）
| 场景 | GNN / GNN+REG | GNN+CONSTRAINT / GNN+REG+CONSTRAINT |
|------|----------------|------------------------------------|
| 社区数增加（5→20） | 输出基本不变（约 4–8） | 输出紧贴 lower bound $ l $ |
| 结论 | 严重低估真实社区数 | 更贴近设定下限，缓解 modularity 分辨率限制问题 |

> 特别是在 20 社区任务中：
> - GNN 和 GNN+REG 输出仅 1–4 个社区
> - GNN+REG+CONSTRAINT 输出 16 个，且部分匹配 ground truth（见图 3）

#### 💡 消融实验结果
| 观察 | 发现 |
|------|------|
| 加入 **constraint** 单独有效 | GNN+CONSTRAINT 能保证输出 ≥ $ l $ 个社区，验证其独立有效性 |
| 加入 **balance regularization** 提升质量 | GNN+REG+CONSTRAINT 比 GNN+CONSTRAINT 得到更高的 ARI，因为后者允许 singleton clusters |
| 两者协同最佳 | constraint 保证数量，balance 保证质量 → 推荐联合使用 |

#### ⏱️ 运行时间对比（表 2 & 表 4）
- 所有模型运行时间相近
- 添加 constraint 和 balance 项对计算开销影响极小
- 大规模网络（如 Large SBM）耗时约 9 小时，仍具可行性

---

## 4. 关键结论和发现

### 主要发现
1. **GNN 默认不能可靠返回指定数量的社区**，普遍存在“输出社区数 < 输入 $ c $”的现象。
2. **提出的 constraint term 能有效施加社区数量下界**，确保输出在 $[l, c]$ 范围内。
3. 当 $ l = c $ 时，可强制输出**精确数量**的社区，填补了 GNN 在此功能上的空白。
4. **结合 balance regularization 可进一步提升聚类质量**（如 ARI），防止出现大量单节点社区。
5. 方法在多种网络规模、密度、真实/合成数据上均表现稳健。

---

### 方法的局限性
1. **依赖 cluster assignment matrix 的质量**：
   - 若初始分配模糊（如所有概率接近），constraint 难以发挥作用
   - 建议适当增大 $\mu$ 权重以增强约束力度（见 Appendix A.3）
2. **未解决自动选择最优 $ k $ 的问题**：
   - 虽然支持范围输入，但仍需人工设定 $ l $ 和 $ c $
   - 自动搜索最优社区数仍为开放问题
3. **测试网络具有较强社区结构**：
   - 在弱社区结构（weak community signal）下的表现尚未系统评估
   - 可能面临相变阈值挑战（phase transition）

---

### 未来工作方向
1. **开发基于 constraint 的自动搜索策略**：
   - 如逐步缩小 $[l,c]$ 区间，寻找 modularity 最高的输出
2. **探索更鲁棒的 constraint 形式**：
   - 支持最小社区大小（minimum cluster size）约束（已在 Appendix A.2 提出初步方案）
3. **扩展至动态图或异构图场景**
4. **理论分析 constraint 的收敛性与稳定性**

---

## 总结

本文针对 GNN 在图聚类中**无法可靠控制输出社区数量**的问题，提出了一种新颖的、可微分的约束机制，使用户能够指定社区数量的**范围或精确值**。实验表明该方法在多种数据集上均能有效执行约束，并结合 balance regularization 显著提升了聚类质量。这一工作增强了 GNN 在无监督社区检测任务中的实用性和可控性，为后续自动化社区发现提供了重要基础。

</details>

---

### 14. [Utility Boundary of Dataset Distillation: Scaling and Configuration-Coverage Laws](https://arxiv.org/abs/2512.05817)

**Authors**: Zhengquan Luo, Zhiqiang Xu  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05817v1  

#### Abstract
Dataset distillation (DD) aims to construct compact synthetic datasets that allow models to achieve comparable performance to full-data training while substantially reducing storage and computation. Despite rapid empirical progress, its theoretical foundations remain limited: existing methods (gradi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Utility Boundary of Dataset Distillation: Scaling and Configuration-Coverage Laws 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
- **理论碎片化**：现有的 Dataset Distillation (DD) 方法（如 Gradient Matching, Distribution Matching, Trajectory Matching）基于不同的代理目标（surrogate objectives）和优化假设，缺乏统一的理论框架来解释其共性。
- **配置鲁棒性缺失**：当前 DD 方法在训练配置（如 optimizer、architecture、augmentation）发生变化时表现不稳定，缺乏对“配置变化下仍能保持性能”的理论刻画。
- **经验现象缺乏解释**：例如，随着 distilled sample 数量增加，性能提升出现饱和（IPC saturation），但缺乏理论解释。

### 提出的新方法与新思路
作者提出了一个**统一的理论框架**——**Configuration-Dynamics-Error Framework**，将主流 DD 方法纳入同一 Generalization Error 分析视角，并得出两个核心定律：

#### （1）Scaling Law（单配置缩放律）
- 当配置固定时，泛化误差随蒸馏样本数 $k$ 的增长而下降，遵循：
  $$
  \Delta \leq O\left(\frac{1}{\sqrt{k}}\right) + \epsilon_{\text{bound}}
  $$
- $\epsilon_{\text{bound}}$ 是由配置决定的不可约误差下限，解释了为何增大 $k$ 到一定程度后性能不再提升（即 IPC saturation）。

#### （2）Coverage Law（配置覆盖律）
- 当考虑多个目标配置构成的配置族 $\mathcal{A}$ 时，所需蒸馏样本数 $k$ 必须与配置多样性 $H_{\text{cov}}(\mathcal{A}, r)$ 成**线性关系**才能维持泛化性能：
  $$
  k = \Omega(H_{\text{cov}}(\mathcal{A}, r))
  $$
- 这是首次形式化定义了 DD 的“效用边界”（utility boundary），即 $k$ 与配置多样性的权衡关系。

#### 统一视角：Surrogates Are Interchangeable
- 证明 GM、DM、TM 本质上都是通过双层优化机制减少同一个“匹配差异”（alignment discrepancy $\Delta_a$）的不同代理方式。
- 它们在理论上是可互换的（exchangeable），只是在样本效率和鲁棒性上有所权衡。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **理论统一性** | 首次将 GM/DM/TM 统一在一个 generalization error bound 框架下，揭示其本质一致性 |
| **可解释性增强** | 解释了 IPC saturation 和跨配置性能退化等经验现象 |
| **设计指导意义** | 提供了如何选择 $k$ 和 surrogate 的原则：减少 $\epsilon_{\text{bound}}$ 比盲目增大数据更有效；$k$ 应随配置多样性线性增长 |
| **普适性强** | 不依赖特定模型或优化器，适用于多种 DD 范式 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **MNIST**
- **CIFAR-10 / CIFAR-100**
- **ImageNette**（ImageNet 的 10 类子集）

### 实验设置
- **蒸馏方法**：
  - Gradient Matching (GM): DC, DSA
  - Distribution Matching (DM): DM
  - Trajectory Matching (TM): MTT
  - 扩散模型方法：MGD3（用于 ImageNette）
- **源配置（source configuration）**：始终为 `ConvNet + SGD`（启用 DSA 时也一致）
- **目标配置（target configuration）**：多样化组合
  - 架构：ConvNet, LeNet, ResNet-18, AlexNet, MLP, VGG11
  - 优化器：SGD, Adam
  - 数据增强：启用/禁用 DSA
- **蒸馏样本数 $k$**：按 IPC（images per class）从 2 到 200 不等，$k = \text{IPC} \times \#\text{classes}$

### 评估指标
- **Generalization Error Gap**：
  $$
  \Delta_a = |R(\theta_s^{(a)}) - R(\theta_T^{(a)})|
  $$
  即在相同目标配置 $a$ 下，使用蒸馏数据训练的模型与真实数据训练的模型之间的测试误差差。
- **Scaling Law 验证**：绘制 $\Delta_a$ vs. $1/\sqrt{k}$，进行线性回归，报告斜率、截距、$R^2$
- **Coverage Law 验证**：近似配置复杂度 $H_{\text{cov}} \approx \log M$，其中 $M$ 是采样配置数。绘制 $\Delta(k, M)$ vs. $\sqrt{\log M}/\sqrt{k}$，验证线性关系

### 基线方法对比
- 对比了四种代表性 DD 方法：DC (GM), DSA (GM), DM (DM), MTT (TM), MGD3 (diffusion)
- 所有方法均使用官方开源实现，默认超参，确保公平比较

---

## 3. 主要实验结果和性能指标

### Scaling Law 实验结果（单配置）
- 在所有数据集上，$\Delta_a$ 与 $1/\sqrt{k}$ 呈现出高度线性的负相关关系，$R^2$ 多数 > 0.85，验证了理论预测。
- **典型结果**：
  - **MNIST**：DC/DSA/DM 拟合极佳 ($R^2 = 0.90–0.99$)，MTT 斜率更大且方差高，表明轨迹不稳定性。
  - **CIFAR-10**：所有方法均符合规律，DC/DSA 最稳定。
  - **CIFAR-100**：DC/DSA 仍保持高线性 ($R^2 > 0.92$)，DM 和 MTT 偏离更明显，说明复杂数据放大轨迹误差。
  - **ImageNette**：扩散方法 MGD3 也符合 $1/\sqrt{k}$ 规律，表明该律超越传统匹配范式。

### Coverage Law 实验结果（跨配置）
- 绘制 $\Delta(k, M)$ vs. $\sqrt{\log M}/\sqrt{k}$，观察到近似线性趋势，支持 Coverage Law。
- **不同方法的敏感性排序**（斜率越大越敏感）：
  - **MNIST**：MTT ($\beta_1=0.91$) >> DM ($0.14$) > DC/DSA ($\sim 0.20$)
  - **CIFAR-10**：顺序为 MTT < DC < DSA < DM，反映 DM 更鲁棒于配置变化
  - **CIFAR-100**：拟合度降低（因类内差异大导致噪声多），但仍可见趋势
- **结论**：
  - GM 方法在单一配置下高效，但在跨配置时退化快
  - DM 更鲁棒于配置多样性
  - TM 最不稳定，尤其在复杂数据集上

### 消融实验与分析
- **无显式消融实验**，但通过不同方法的行为差异间接验证了理论：
  - Surrogate choice 影响收敛速度和稳定性，但最终都受相同 scaling law 支配
  - $k$ 增加初期误差快速下降，后期趋于平缓（接近 $\epsilon_{\text{bound}}$），验证 saturation 现象
  - 使用 $\log M$ 作为 $H_{\text{cov}}$ 的代理可行，避免昂贵的距离计算

---

## 4. 关键结论和发现

### 主要发现
1. **DD 方法本质统一**：GM、DM、TM 并非独立启发式，而是通过不同代理减少同一“匹配差异”的手段，在统一框架下具有相同的泛化行为。
2. **Scaling Law 存在**：泛化误差随 $1/\sqrt{k}$ 下降并趋于饱和，解释了 IPC saturation 现象。
3. **Coverage Law 成立**：蒸馏样本数 $k$ 必须与配置多样性 $H_{\text{cov}}$ 成线性增长，否则无法保证跨配置性能。
4. **设计启示**：
   - 减少不可约误差 $\epsilon_{\text{bound}}$ 比单纯增加 $k$ 更重要
   - 面向多样化部署场景时，应根据配置多样性规划 $k$

### 方法的局限性
1. **优化动力学假设较强**：依赖 PL 条件和 Lipschitz 连续性，可能不适用于 AdamW、大批次训练等非凸或自适应场景。
2. **配置距离估计困难**：$d_A$ 和 $H_{\text{cov}}$ 的精确计算是 NP-hard，实验中使用 $\log M$ 仅为代理，可能低估实际多样性。
3. **未涵盖数据分布偏移**：当前框架聚焦算法配置变化，未考虑域迁移、类别不平衡等语义层面的变化。

### 未来工作方向
1. **放宽优化假设**：扩展至更弱条件（如 one-point convexity 或 uniform stability）下的理论分析。
2. **改进 $H_{\text{cov}}$ 估计**：开发基于聚类或嵌入空间的可学习配置复杂度估计器。
3. **扩展配置定义**：将数据分布差异（如 Wasserstein 距离）纳入配置空间，研究跨域场景下的 coverage law。
4. **指导鲁棒 DD 设计**：基于 coverage law 开发自适应蒸馏策略，动态分配原型以覆盖高多样性配置。

--- 

> **总结**：本文提出了首个统一的 Dataset Distillation 理论框架，揭示了 **Scaling Law** 与 **Coverage Law** 两大基本规律，不仅解释了长期存在的经验现象，还为构建**高效且配置鲁棒**的蒸馏方法提供了坚实的理论基础和设计指南。

</details>

---

### 15. [MIND: Multi-rationale INtegrated Discriminative Reasoning Framework for Multi-modal Large Models](https://arxiv.org/abs/2512.05530)

**Authors**: Chuang Yu, Jinmiao Zhao, Mingxuan Zhao, Yunpeng Liu, Xiujun Shu, Yuanhao Feng, Bo Wang, Xiangyu Yue  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05530v1  

#### Abstract
Recently, multimodal large language models (MLLMs) have been widely applied to reasoning tasks. However, they suffer from limited multi-rationale semantic modeling, insufficient logical robustness, and are susceptible to misleading interpretations in complex scenarios. Therefore, we propose a Multi-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MIND: Multi-rationale INtegrated Discriminative Reasoning Framework for Multi-modal Large Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 **Multimodal Large Language Models (MLLMs)** 在推理任务中存在以下关键缺陷：

- **单理由监督（Single-rationale supervision）** 导致模型仅学习表面映射关系，无法捕捉人类推理的多样性与复杂性。
- 缺乏对错误或误导性解释的识别与纠正能力，逻辑鲁棒性弱。
- 面对模糊或多义场景时，容易产生“逻辑漂移”或“幻觉”，缺乏主动判别与自我修正机制。

这些问题限制了 MLLMs 向真正认知智能的演进。

---

### **提出了什么新方法或新思路**

作者提出 **MIND**（Multi-rationale INtegrated Discriminative）推理框架，旨在赋予 MLLMs 类似人类的“理解 → 反思 → 纠正”（*Understand → Rethink → Correct*）的认知能力，实现从被动模仿到主动判别式推理的范式转变。

#### 核心组件包括：

1. **Rationale Augmentation and Discrimination (RAD) 范式**
   - 自动高效地生成多样化的**正向理由（positive rationales）** 和语义反转的**负向理由（negative rationales）**。
   - 构建多理由训练样本，提供统一且可扩展的数据基础。
   - 通过批量生成机制提升效率，并设计正/负提示模板控制语义质量。

2. **Progressive Two-stage Correction Learning (P2CL) 策略**
   - **阶段 I (P2CL-I)**：多理由正向学习（Multi-rationale positive learning），增强语义理解与逻辑建模。
   - **阶段 II (P2CL-II)**：主动逻辑判别与纠正（Active logic discrimination and correction），输入可能错误的理由并要求模型输出正确答案与修正后的理由，形成自省与修复机制。

3. **Multi-rationale Contrastive Alignment (MCA) 优化策略**
   - 在嵌入空间中拉近正确理由（hard positives），推开错误理由（hard negatives）。
   - 采用“难例挖掘”机制（Bottom-k 正例 / Top-k 负例）增强判别边界。
   - 显著提升语义一致性与逻辑敏感度。

---

### **相比现有方法的优势**

| 维度 | 传统方法（如 Multimodal-CoT） | MIND |
|------|-------------------------------|------|
| 监督信号 | 单一标准理由 | 多样正向 + 挑战性负向理由 |
| 推理模式 | 被动模仿 | 主动判别与自我纠正 |
| 语义建模 | 表面映射 | 多理由语义聚合与冲突分离 |
| 数据构建 | 手工标注 | 自动化、可扩展的 RAD 范式 |
| 泛化与鲁棒性 | 弱 | 强，尤其在复杂/模糊场景 |

MIND 实现了从“**Imitation-based Reasoning**” 到 “**Discriminative Reasoning**” 的范式跃迁。

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验覆盖科学、常识、数学三大领域，使用以下三个公开 VQA 数据集及其 RAD 扩展版本：

| 数据集 | 领域 | 原始样本数 | RAD 扩展倍数 |
|--------|------|------------|--------------|
| **ScienceQA** | 科学 | 21,208 | ×1000 |
| **A-OKVQA** | 常识 | 24,903 | ×1000 |
| **M³CoT** | 科学+常识+数学 | 11,328 | ×500 |

所有数据划分保持原始设定以确保公平比较。

---

### **实验设置和评估指标**

- **模型架构**：基于 T5 的 encoder-decoder 架构，探索 Base (223M) 和 Large (738M) 两种规模。
- **视觉编码器**：冻结的 BLIP2-flan-t5-xxl 提取图像特征。
- **图像描述生成**：Qwen2.5-VL-72B。
- **训练参数**：
  - 学习率：8e-5
  - Batch size：8
  - 序列长度：512
  - Epochs：ScienceQA-RAD 为 200，其余为 400
- **硬件**：8×NVIDIA H20 GPU (96GB)
- **评估指标**：准确率（Accuracy）

---

### **基线方法对比**

涵盖多种主流方法类别：

- **Few-shot CoT 方法**：GPT-3.5, ChatGPT, GPT-4, IPVR
- **Fine-tuning 方法**：MCAN, ViLT, VisualBERT, LLaVA, LaVIN
- **Multimodal-CoT 类方法**：Multimodal-CoT, MC-CoT, DPMM-CoT, T-SciQ
- **工具增强方法**：HuggingGPT, VoT
- **零样本大模型**：GPT-4V, Gemini

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 数据集 | MIND-base 准确率 | 当前 SOTA（此前） | 提升幅度 |
|--------|------------------|------------------|----------|
| **ScienceQA** | **92.29%** | 91.97% (T-SciQ) | +0.32% |
| **A-OKVQA** | **70.6%** | ~58.7% (IPVR+GPT-3) | +11.9% |
| **M³CoT** | **57.38%** | 44.85% (Multimodal-CoT) | **+12.53%** |

> 注：MIND-base 参数量仅为 223M，远小于多数对比模型（如 GPT-4, LLaVA-13B）。

---

### **与基线方法的对比结果**

#### ✅ ScienceQA 结果（见 Table 1）
- 相比 **Multimodal-CoTbase**（同参数量）：**+6.98%**（85.31% → 92.29%）
- 超过 **LLaVA**（13B）：+1.37%
- 超过 **GPT-4 w/CoT**：+8.3%

#### ✅ A-OKVQA 结果（见 Table 2）
- 相比 **Multimodal-CoTbase**：**+20.0%**（50.6% → 70.6%）
- 超过最佳 Few-shot 方法（IPVR+GPT-3）：+11.9%

#### ✅ M³CoT 结果（见 Table 3）
- 相比 **Multimodal-CoTbase**：**+12.53%**
- 相比 **MC-CoTbase**：+3.87%
- **MIND-large** 进一步提升至 **61.56%**，超越 GPT-4V（56.95%）近 **4.61%**，且参数更小。

---

### **消融实验结果（Ablation Studies）**

#### 🔹 分项消融（Table 4 & S1）

| 配置 | ScienceQA Acc. | 提升 |
|------|----------------|------|
| Baseline | 90.29% | — |
| + MCA only | 90.36% | +0.07% |
| + P2CL only | 92.15% | +1.86% |
| **完整 MIND (P2CL + MCA)** | **92.29%** | **+2.00%** |

> 表明 P2CL 是主驱动力，MCA 提供互补增益，二者协同实现“1+1 > 2”。

#### 🔹 理由质量与数量影响（Table 5 & 6）

- **理由生成模型越强，性能越高**：
  - DeepSeek-R1-Qwen8B → 91.49%
  - Qwen3-235B-22A → **91.61%**
  - “Final Mix”（多源混合）→ **92.29%**
- **理由数量增加带来持续提升，但在 ×1000 后趋于饱和**：
  - 原始 21K → ×1000 (21M)：**+2.00%**

#### 🔹 P2CL 各阶段作用分析（Table 8）

- 移除 P2CL-I（正向学习）：性能下降 **0.66%**
- 移除 P2CL-II（纠错机制）：性能下降 **1.93%**
- 输入负理由但监督正理由（Neg → Pos）：显著提升至 91.72%，验证纠错有效性。

---

## 4. 关键结论和发现

### **主要发现**

1. **多理由训练是提升推理鲁棒性的关键**：
   - 模型需同时学习“什么是正确的”和“什么是错误的”，才能建立判别边界。

2. **“理解 → 反思 → 纠正”认知循环有效**：
   - P2CL 策略成功模拟人类反思过程，使模型具备主动识别并修正错误逻辑的能力。

3. **MCA 显著增强语义判别力**：
   - 对比学习机制帮助模型在高维空间中清晰分离正确与错误推理路径。

4. **自动化数据增强（RAD）可行且高效**：
   - 利用大模型自动生成高质量正/负理由，大幅降低人工标注成本，支持可扩展训练。

5. **小模型也能实现 SOTA 性能**：
   - MIND-base（223M）在多个任务上超越数十亿参数的大模型，证明其推理效率优势。

---

### **方法的局限性**

1. **依赖高质量生成模型进行 RAD**：
   - 若初始理由生成模型能力不足，可能导致噪声传播。
2. **计算开销较高**：
   - 尽管 RAD 批量生成提升了效率，但多理由存储与训练仍需大量显存。
3. **负理由构造依赖语义反转提示**：
   - 并非所有错误类型都能被有效模拟（如逻辑跳跃、事实错误等）。
4. **目前集中在 VQA 场景**：
   - 是否适用于长文本生成、规划等更复杂任务尚待验证。

---

### **未来工作方向**

1. **动态难度调节的负理由生成**：
   - 根据模型当前能力自适应生成更具挑战性的错误理由。
2. **轻量化 MIND 架构设计**：
   - 探索更高效的多理由融合与对比学习机制，降低部署成本。
3. **扩展至更多模态与任务**：
   - 如视频推理、机器人决策、跨模态对话等。
4. **结合强化学习进行自我进化**：
   - 让模型在交互中不断发现新类型的推理陷阱并自主构建训练样本。

---

> **总结**：  
> MIND 提出了一种全新的 **multi-rationale discriminative reasoning** 范式，通过 **RAD + P2CL + MCA** 三重机制，显著提升了 MLLMs 的推理准确性、鲁棒性与可解释性。其实验充分验证了“主动判别”优于“被动模仿”的理念，为迈向更高层次的认知智能提供了重要路径。

</details>

---

### 16. [Interleaved Latent Visual Reasoning with Selective Perceptual Modeling](https://arxiv.org/abs/2512.05665)

**Authors**: Shuai Dong, Siyuan Wang, Xingyu Liu, Zhongyu Wei  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05665v1  

#### Abstract
Interleaved reasoning paradigms enhance Multimodal Large Language Models (MLLMs) with visual feedback but are hindered by the prohibitive computational cost of repeatedly re-encoding pixel-dense images. A promising alternative, latent visual reasoning, circumvents this bottleneck yet currently force...

---

### 17. [Sepsis Prediction Using Graph Convolutional Networks over Patient-Feature-Value Triplets](https://arxiv.org/abs/2512.05416)

**Authors**: Bozhi Dan, Di Wu, Ji Xu, Xiang Liu, Yiziting Zhu, Xin Shu, Yujie Li, Bin Yi  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05416v1  

#### Abstract
In the intensive care setting, sepsis continues to be a major contributor to patient illness and death; however, its timely detection is hindered by the complex, sparse, and heterogeneous nature of electronic health record (EHR) data. We propose Triplet-GCN, a single-branch graph convolutional model...

---

### 18. [Beyond Data Filtering: Knowledge Localization for Capability Removal in LLMs](https://arxiv.org/abs/2512.05648)

**Authors**: Igor Shilov, Alex Cloud, Aryo Pradipta Gema, Jacob Goldman-Wetzler, Nina Panickssery, Henry Sleight, Erik Jones, Cem Anil  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05648v1  

#### Abstract
Large Language Models increasingly possess capabilities that carry dual-use risks. While data filtering has emerged as a pretraining-time mitigation, it faces significant challenges: labeling whether data is harmful is expensive at scale, and given improving sample efficiency with larger models, eve...

---

### 19. [BERTO: an Adaptive BERT-based Network Time Series Predictor with Operator Preferences in Natural Language](https://arxiv.org/abs/2512.05721)

**Authors**: Nitin Priyadarshini Shankar, Vaibhav Singh, Sheetal Kalyani, Christian Maciocco  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05721v1  

#### Abstract
We introduce BERTO, a BERT-based framework for traffic prediction and energy optimization in cellular networks. Built on transformer architectures, BERTO delivers high prediction accuracy, while its Balancing Loss Function and prompt-based customization allow operators to adjust the trade-off betwee...

---

### 20. [LDLT $\mathcal{L}$-Lipschitz Network: Generalized Deep End-To-End Lipschitz Network Construction](https://arxiv.org/abs/2512.05915)

**Authors**: Marius F. R. Juston, Ramavarapu S. Sreenivas, Dustin Nottage, Ahmet Soylemezoglu  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.05915v1  

#### Abstract
Deep residual networks (ResNets) have demonstrated outstanding success in computer vision tasks, attributed to their ability to maintain gradient flow through deep architectures. Simultaneously, controlling the Lipschitz constant in neural networks has emerged as an essential area of research to enh...

---

### 21. [BEAVER: An Efficient Deterministic LLM Verifier](https://arxiv.org/abs/2512.05439)

**Authors**: Tarun Suresh, Nalin Wadhwa, Debangshu Banerjee, Gagandeep Singh  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05439v1  

#### Abstract
As large language models (LLMs) transition from research prototypes to production systems, practitioners often need reliable methods to verify that model outputs satisfy required constraints. While sampling-based estimates provide an intuition of model behavior, they offer no sound guarantees. We pr...

---

### 22. [Transformer-Enabled Diachronic Analysis of Vedic Sanskrit: Neural Methods for Quantifying Types of Language Change](https://arxiv.org/abs/2512.05364)

**Authors**: Ananth Hariharan, David Mortensen  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05364v1  

#### Abstract
This study demonstrates how hybrid neural-symbolic methods can yield significant new insights into the evolution of a morphologically rich, low-resource language. We challenge the naive assumption that linguistic change is simplification by quantitatively analyzing over 2,000 years of Sanskrit, demo...

---

### 23. [MedTutor-R1: Socratic Personalized Medical Teaching with Multi-Agent Simulation](https://arxiv.org/abs/2512.05671)

**Authors**: Zhitao He, Haolin Yang, Zeyu Qin, Yi R Fung  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05671v1  

#### Abstract
The significant gap between rising demands for clinical training and the scarcity of expert instruction poses a major challenge to medical education. With powerful capabilities in personalized guidance, Large Language Models (LLMs) offer a promising solution to bridge this gap. However, current rese...

---

### 24. [FedGMR: Federated Learning with Gradual Model Restoration under Asynchrony and Model Heterogeneity](https://arxiv.org/abs/2512.05372)

**Authors**: Chengjie Ma, Seungeun Oh, Jihong Park, Seong-Lyun Kim  
**Category**: cs.DC  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05372v1  

#### Abstract
Federated learning (FL) holds strong potential for distributed machine learning, but in heterogeneous environments, Bandwidth-Constrained Clients (BCCs) often struggle to participate effectively due to limited communication capacity. Their small sub-models learn quickly at first but become under-par...

---

### 25. [When unlearning is free: leveraging low influence points to reduce computational costs](https://arxiv.org/abs/2512.05254)

**Authors**: Anat Kleiman, Robert Fisher, Ben Deaner, Udi Wieder  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05254v1  

#### Abstract
As concerns around data privacy in machine learning grow, the ability to unlearn, or remove, specific data points from trained models becomes increasingly important. While state of the art unlearning methods have emerged in response, they typically treat all points in the forget set equally. In this...

---

### 26. [Taxonomy-Adaptive Moderation Model with Robust Guardrails for Large Language Models](https://arxiv.org/abs/2512.05339)

**Authors**: Mahesh Kumar Nandwana, Youngwan Lim, Joseph Liu, Alex Yang, Varun Notibala, Nishchaie Khanna  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05339v1  

#### Abstract
Large Language Models (LLMs) are typically aligned for safety during the post-training phase; however, they may still generate inappropriate outputs that could potentially pose risks to users. This challenge underscores the need for robust safeguards that operate across both model inputs and outputs...

---

### 27. [NeuroMemFPP: A recurrent neural approach for memory-aware parameter estimation in fractional Poisson process](https://arxiv.org/abs/2512.05893)

**Authors**: Neha Gupta, Aditya Maheshwari  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05893v1  

#### Abstract
In this paper, we propose a recurrent neural network (RNN)-based framework for estimating the parameters of the fractional Poisson process (FPP), which models event arrivals with memory and long-range dependence. The Long Short-Term Memory (LSTM) network estimates the key parameters $\mu >0$ and $\b...

---

### 28. [On the Bayes Inconsistency of Disagreement Discrepancy Surrogates](https://arxiv.org/abs/2512.05931)

**Authors**: Neil G. Marchant, Andrew C. Cullen, Feng Liu, Sarah M. Erfani  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.05931v1  

#### Abstract
Deep neural networks often fail when deployed in real-world contexts due to distribution shift, a critical barrier to building safe and reliable systems. An emerging approach to address this problem relies on \emph{disagreement discrepancy} -- a measure of how the disagreement between two models cha...

---

### 29. [Semantic Faithfulness and Entropy Production Measures to Tame Your LLM Demons and Manage Hallucinations](https://arxiv.org/abs/2512.05156)

**Authors**: Igor Halperin  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2512.05156v1  

#### Abstract
Evaluating faithfulness of Large Language Models (LLMs) to a given task is a complex challenge. We propose two new unsupervised metrics for faithfulness evaluation using insights from information theory and thermodynamics. Our approach treats an LLM as a bipartite information engine where hidden lay...

---

### 30. [MCP-AI: Protocol-Driven Intelligence Framework for Autonomous Reasoning in Healthcare](https://arxiv.org/abs/2512.05365)

**Authors**: Zag ElSayed, Craig Erickson, Ernest Pedapati  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 3.5  
**Type**: new  
**ArXiv ID**: 2512.05365v1  

#### Abstract
Healthcare AI systems have historically faced challenges in merging contextual reasoning, long-term state management, and human-verifiable workflows into a cohesive framework. This paper introduces a completely innovative architecture and concept: combining the Model Context Protocol (MCP) with a sp...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
