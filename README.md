# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-18 05:57:22 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Multiscale Aggregated Hierarchical Attention (MAHA): A Game Theoretic and Optimization Driven Approach to Efficient Contextual Modeling in Large Language Models](https://arxiv.org/abs/2512.14925)

**Authors**: Caner Erden  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.14925v1  

#### Abstract
The quadratic computational complexity of MultiHead SelfAttention (MHSA) remains a fundamental bottleneck in scaling Large Language Models (LLMs) for longcontext tasks. While sparse and linearized attention mechanisms attempt to mitigate this, they often compromise the representation of global depen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multiscale Aggregated Hierarchical Attention (MAHA)

## 1. 论文的主要贡献和创新点

### 解决的问题
- **Multi-Head Self-Attention (MHSA)** 存在 **O(n²)** 的计算复杂度，限制了 Large Language Models (LLMs) 在长序列任务中的扩展能力。
- 现有稀疏注意力机制（如 Longformer、BigBird）虽然降低了计算开销，但往往牺牲了对全局依赖关系的建模能力。
- 层次化注意力方法缺乏理论严谨的聚合机制，通常采用启发式加权平均，导致信息稀释。

### 提出的新方法与创新思路
**MAHA (Multiscale Aggregated Hierarchical Attention)** 是一种全新的注意力架构，其核心创新包括：

- **多尺度分层分解（Multiscale Decomposition）**  
  将输入序列通过可学习的下采样操作（如 Strided Convolution 或 Adaptive Pooling）动态划分为多个层级（scales），每个层级对应不同的语义抽象粒度（从局部语法到全局主题）。

- **优化驱动的聚合机制（Optimization-Driven Aggregation）**  
  引入两种数学上严格的聚合策略：
  - **凸优化（Convex Optimization, CO）**：将多尺度注意力输出的融合建模为带约束的最优化问题，目标是平衡效率与上下文保真度。
  - **博弈论均衡（Nash Equilibrium, NE）**：将不同尺度视为“玩家”，通过非合作博弈寻找纳什均衡，确保没有单一尺度能通过单方面改变权重来提升整体表示质量。

- **共享值投影与混合骨干设计**  
  - 查询（Query）和键（Key）投影是尺度特定的，而值（Value）投影跨尺度共享，显著减少参数量。
  - 结合 **dilated convolution** 捕捉局部上下文，并使用跨尺度门控（Cross-Scale Gating）增强信息流动。

### 相比现有方法的优势
| 维度 | MAHA优势 |
|------|---------|
| **计算效率** | 实现近线性复杂度 **O(n)**，远优于标准 MHSA 的 **O(n²)** |
| **上下文建模** | 能同时捕捉细粒度局部模式和粗粒度全局依赖，避免信息丢失 |
| **理论基础** | 聚合过程基于凸优化或博弈论，具有数学最优性保证，而非启发式融合 |
| **兼容性** | 可作为即插即用模块替换 Transformer 中的标准注意力层 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多种 NLP 任务以验证通用性和长上下文能力：
- **文本分类**：GLUE 基准（MNLI, SST-2）
- **长距离依赖建模**：PG-19 数据集（平均 >4k tokens）
- **机器翻译**：WMT14 英德翻译（English-German）
- **问答系统**：SQuAD v2.0

### 实验设置
- **模型架构**：12层 Transformer，隐藏维度 768，12个 attention heads
- **训练配置**：
  - Batch Size：32（分类），16（翻译/问答）
  - 学习率：5e-5，warmup 10k 步
  - 序列长度：512（常规任务），4096（PG-19）
- **MAHA 参数**：
  - 分层数 $ L = 4 $（token 数分别为 32, 64, 128, 256）
  - 下采样方式：步幅卷积（kernel=3）
  - 聚合正则化系数 $ \lambda = 0.1 $

### 基线方法对比
- Standard MHA (Vaswani et al., 2017)
- Longformer (Beltagy et al., 2020)
- BigBird (Zaheer et al., 2020)
- Reformer (Kitaev et al., 2020)
- Performer (Choromanski et al., 2020)

### 评估指标
- 准确率（Accuracy）、BLEU、F1、Perplexity (PPL)↓
- 内存占用（Memory, GB）↓
- 吞吐量（Throughput, seq/s）↑
- FLOPs（浮点运算数）分析

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 模型 | MNLI (Acc) | SST-2 (Acc) | PG-19 (PPL)↓ | WMT (BLEU) | SQuAD (F1) | Memory (GB)↓ | Throughput (seq/s)↑ |
|------|------------|-------------|--------------|------------|------------|---------------|------------------------|
| Standard MHA | 86.2 | 93.5 | 24.3 | 28.7 | 88.4 | 15.2 | 42 |
| Longformer | 85.7 | 92.8 | 23.8 | 27.9 | 87.6 | 9.1 | 58 |
| BigBird | 85.9 | 93.1 | 23.5 | 28.1 | 87.9 | 10.3 | 53 |
| Reformer | 84.3 | 91.7 | 25.6 | 26.4 | 85.2 | 7.8 | 62 |
| Performer | 85.1 | 92.4 | 24.9 | 27.3 | 86.7 | 8.5 | 67 |
| **MAHA** | **86.0** | **93.3** | **23.1** | **28.5** | **88.2** | **2.5** | **20.5*** |

> *注：MAHA 的吞吐量较低是由于原型实现未充分优化；理论复杂度仍为线性*

### 与基线方法的对比结果
- **性能保持**：在多数任务上与 Standard MHA 性能相当（差距 < 0.2%），且在 **PG-19 上取得最低 PPL (23.1)**，表明其更强的长程建模能力。
- **内存大幅降低**：相比 Standard MHA，内存占用减少 **83.6%**（15.2 → 2.5 GB）。
- **计算效率显著提升**：
  - 在序列长度 **N=4096** 时：
    - Standard MHA：约 **16.8M FLOPs**
    - MAHA：仅 **3.2M FLOPs**
    - **FLOPs 减少 81%**
  - 随着序列增长，效率优势呈指数级扩大（见 Figure 3）。

### 消融实验结果

#### 聚合策略比较（Table 2）
| 方法 | MNLI (Acc) | Memory (GB) | 训练速度 |
|------|------------|-------------|----------|
| Convex Optimization (CO) | 86.0 | 6.7 | 1.0x |
| Nash Equilibrium (NE) | 85.8 | 6.9 | 0.9x |
| 平均融合（Baseline） | 85.2 | 7.2 | 1.1x |

- **CO 与 NE 均优于简单平均**，说明优化驱动聚合更有效。
- NE 在后期收敛损失更低（见 Figure 4），可能找到更鲁棒的尺度平衡，但训练稍慢。

#### 分层深度分析（Figure 5）
- 最优性能出现在 **L=4** 层。
- **L=2**（太少）：精度下降至 84.5%，因细节不足。
- **L=6**（太多）：性能降至 84.8%，因过度下采样引入噪声。

---

## 4. 关键结论和发现

### 主要发现
1. **MAHA 成功将注意力复杂度从 O(n²) 降至近线性 O(n)**，解决了 LLM 扩展中的根本瓶颈。
2. 通过 **多尺度分解 + 优化驱动聚合**，实现了局部精细与全局上下文的高效统一建模。
3. 在长文本任务（如 PG-19）上表现优于所有稀疏注意力基线，证明其在真实场景中的有效性。
4. **Nash Equilibrium 聚合** 提供更强的理论保障，在某些情况下优于凸优化方案。

### 方法的局限性
- 引入额外超参数（如分层数 $ L $、压缩比 $ r $），需针对领域调优。
- Nash Equilibrium 聚合涉及迭代求解，带来一定训练开销。
- 假设语言结构具有天然层次性，可能难以处理高度非组合或分散引用的文本（如某些诗歌或法律文书）。

### 未来工作方向
- 将 MAHA 框架扩展至其他模态（如 **Computer Vision** 和 **Speech Processing**），利用其天然的多尺度特性。
- 探索更高效的可微优化层实现，进一步降低推理延迟。
- 应用于 **Genomics**（基因序列分析）、**Multimodal Learning**（视频-文本检索）、**Federated Learning** 等资源受限或跨粒度场景。
- 改进解释性，解决因优化权重交互带来的决策路径不透明问题。

> ✅ **代码与模型已开源**：[GitHub - MAHA Project](https://github.com/can-ererden/MAHA-Project)，DOI: `10.5281/zenodo.17936753`

</details>

---

### 2. [DEER: Draft with Diffusion, Verify with Autoregressive Models](https://arxiv.org/abs/2512.15176)

**Authors**: Zicong Cheng, Guo-Wei Yang, Jia Li, Zhijie Deng, Meng-Hao Guo, Shi-Min Hu  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.15176v1  

#### Abstract
Efficiency, as a critical practical challenge for LLM-driven agentic and reasoning systems, is increasingly constrained by the inherent latency of autoregressive (AR) decoding. Speculative decoding mitigates this cost through a draft-verify scheme, yet existing approaches rely on AR draft models (a....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DEER: Draft with Diffusion, Verify with Autoregressive Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现有的 **speculative decoding** 方法（如 EAGLE、Medusa）依赖于 **autoregressive (AR) drafters** 来生成候选序列，这些方法面临两个根本性瓶颈：

1. **逐步不确定性累积（step-wise uncertainty accumulation）**：由于 AR drafter 是从左到右逐个生成 token，早期的小误差会通过自回归依赖不断放大，导致与目标模型的信任关系逐渐崩溃（gradual collapse of trust），从而限制了可接受的 draft 长度。
2. **固有的串行解码限制**：AR drafters 必须顺序生成，无法并行化，制约了解码效率。

这两个因素共同导致现有方法的加速比有限。

---

### **提出了什么新方法或新思路**

本文提出 **DEER** —— 一种全新的 speculative decoding 框架，其核心思想是：

> **用 diffusion LLM（dLLM）作为 drafter，用 AR 模型作为 verifier**，实现“扩散式起草、自回归验证”。

#### 创新点包括：

- **首次完全基于离散空间 dLLM 的 speculative decoding 框架**：无需辅助 AR 模型或混合架构，仅使用一个轻量级 dLLM 作为 drafter。
- **提出 Diffusion-to-AR (D2A) 对齐训练流程**：
  - **Stage I: AR-Style Continuation Distillation**  
    将预训练的 dLLM 微调为能进行前缀条件续写的模式，通过在教师输出后添加 `[SEP]` 标记来模拟截断上下文。
  - **Stage II: Scribe Refinement**  
    引入指数加权的后缀掩码策略，强化靠近前缀边界的 token 准确性，提升局部一致性。
- **揭示了一种新兴能力：可靠块再生（reliable block regeneration）**  
  dLLM 能够反复接受部分掩码的后缀并连贯地重建，支持真正的块级生成。

---

### **相比现有方法的优势**

| 维度 | DEER | 传统 AR-based drafters（如 EAGLE-3） |
|------|------|-----------------------------|
| **drafting 并行性** | ✅ 单步生成整个 block | ❌ 串行生成，无法并行 |
| **误差传播** | ❌ 无左到右依赖，不累积误差 | ✅ 严重累积 |
| **最大接受长度** | 高达 32 tokens | 通常 ≤ 8–10 tokens |
| **速度提升** | 最高 5.54× | 最高约 2.41× |
| **训练成本** | 更低（见 Table 8） | 更高，且大模型易 OOM |

---

## 2. 核心实验方法和设置

### **使用的数据集**

#### **代码生成任务**
- **训练集**：OpenCodeInstruct
- **测试集**：
  - HumanEval
  - MBPP
  - LiveCodeBench
  - CodeAlpaca（Python 子集）

#### **数学推理任务**
- **训练集**：UltraChat + ShareGPT
- **测试集**：
  - GSM8K
  - Math500
  - Minerva Math

---

### **实验设置和评估指标**

#### **模型配置**
- **目标模型（verifier）**：Qwen3 系列（4B, 8B, 14B, 30B-A3B）
- **drafter 模型**：
  - 代码任务：基于 Open-dLLM 的 0.5B 参数 discrete dLLM
  - 数学任务：将 Qwen2.5-0.5B-Instruct 改造为 diffusion 模型

#### **评估指标**
- **Speedup Ratio**：端到端推理速度相对于标准 AR 解码的加速比。
- **Average Acceptance Length (T)**：每次 draft-verify 循环中平均被接受的 token 数量，反映 speculative 效率。
- **Maximum Accepted Token Length**：单次成功接受的最大 token 数。

#### **基线方法对比**
- **EAGLE-3**（当前最先进的 speculative decoding 方法）
- Medusa
- Hydra
- 其他相关方法也在 Related Work 中讨论（如 DiffuSpec、SDD）

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **在 Qwen3-30B-A3B 上的表现（temperature=0）**

| 方法 | Speedup | T（平均接受长度） |
|------|---------|--------------------|
| EAGLE-3 | ×2.41 | 3.21 |
| **DEER** | **×5.54** | **6.58** |

> ⚡️ **DEER 实现了 5.54 倍加速，是 EAGLE-3 的两倍以上！**

---

#### **跨模型规模表现汇总（Table 2）**

| 目标模型 | 方法 | 平均 Speedup | 平均 T |
|--------|------|--------------|--------|
| Qwen3-30B-A3B | EAGLE-3 | ×2.21 | 3.05 |
| | **DEER** | **×4.04** | **5.03** |

> 在所有模型尺度上，DEER 的 T 提升 **50%–120%**，且模型越大优势越明显。

---

#### **最大接受长度对比（Table 4）**

| 模型 | EAGLE-3 | DEER |
|------|---------|------|
| Qwen3-4B ~ 30B | 7–8 tokens | **32 tokens** ✅ |

> DEER 可稳定接受长达 **32 个 token** 的 block，远超 EAGLE-3 的上限。

---

### **消融实验结果（Ablation Study）**

#### **Stage II（Scribe Refinement）的影响（Table 3）**

| Benchmark | w/o Refinement | w/ Refinement | Δ↑ |
|----------|----------------|---------------|----|
| MBPP | 4.74 | 4.87 | +0.13 |
| CodeAlpacaPy | 3.47 | 4.04 | +0.57 |
| HumanEval | 5.38 | **6.58** | **+1.20** |
| LiveCodeBench | 3.87 | **5.03** | **+1.16** |

> 结果表明，Stage II 显著提升了复杂任务上的对齐精度，尤其是在长程依赖强的任务中效果更明显。

#### **超参数敏感性分析**
- 指数加权系数 `α` 过大会导致训练不稳定（如 α=1.05 时发散），但合理范围内（如 α=1.01）可稳定优化。
- 表明该阶段虽敏感但可控，且带来显著收益。

---

### **批处理扩展性（Batch Inference Scalability）**

| 批大小 | AR baseline (tokens/s) | DEER (tokens/s) | 加速比 |
|-------|------------------------|------------------|--------|
| 2 | 34.03 | 82.97 | ~2.4× |
| 8 | 38.35 | 159.87 | ~4.2× |
| 16 | 49.76 | 175.66 | ~3.5× |

> DEER 在批处理场景下表现出色，尤其在 batch=8 时接近 **4 倍吞吐提升**，说明其高度适配 GPU 并行计算。

---

### **数学推理任务表现（Table 6）**

尽管数学领域的 dLLM 尚未充分收敛，DEER 仍取得一致增益：

| Benchmark | EAGLE-3 (Speedup/T) | DEER (Speedup/T) |
|----------|---------------------|------------------|
| Math500 | ×1.89 / 2.04 | **×2.12 / 2.45** |
| GSM8K | ×1.92 / 2.43 | **×2.23 / 2.70** |
| Minerva Math | ×1.91 / 2.07 | **×2.02 / 2.31** |
| **平均** | ×1.91 / 2.18 | **×2.12 / 2.47** |

> 即使在弱 drafter 下，DEER 依然优于 EAGLE-3，证明其机制鲁棒性强。

---

## 4. 关键结论和发现

### **主要发现**

1. **dLLM 作为 drafter 可从根本上避免 AR 模型的误差累积问题**  
   因为其生成是全局去噪过程，每个位置独立于先前 draft token，打破了 left-to-right 依赖。

2. **DEER 实现了前所未有的长块接受能力（up to 32 tokens）**  
   这得益于 one-step block generation 和精准的 D2A 对齐训练。

3. **DEER 在多种任务、模型规模和批处理条件下均显著优于现有方法**  
   不仅在代码生成，在数学推理等复杂任务上也展现泛化能力。

4. **训练成本更低，部署更灵活**  
   相比 Medusa/Hydra，DEER 训练更高效（Table 8），且不需重训练主模型。

5. **揭示了 dLLM 的“可靠块再生”能力**  
   支持对局部掩码片段进行连贯扩展，拓展了 diffusion 模型的应用边界。

---

### **方法的局限性**

1. **缺乏成熟的 dLLM 推理框架支持**  
   当前主流系统（如 vLLM、SGLang）尚未支持 dLLM 的 KV cache，影响实际部署效率（见 Appendix F）。
   
2. **Stage II 对超参数较敏感**  
   指数加权系数需精细调节，否则可能引发训练不稳定。

3. **dLLM 自身尚未在所有领域成熟**  
   特别是在数学等领域，公开可用的高质量 pretrained dLLM 较少，限制了直接应用。

---

### **未来工作方向**

1. **推动 dLLM 推理基础设施建设**  
   如集成 Fast-dLLM 或 dInfer 的 KV cache 技术，进一步释放 batch inference 潜力。

2. **探索更大规模 dLLM 作为 drafter**  
   当前仅使用 0.5B 模型，若使用更大 dLLM 可能带来更大收益。

3. **扩展至更多模态与任务**  
   如图像、语音生成中的 speculative decoding。

4. **动态调整 block size**  
   根据上下文难度自适应选择 draft 长度，提升灵活性。

---

> 🔚 **总结**：DEER 开辟了 speculative decoding 的新范式——**以 diffusion 模型打破自回归瓶颈**，通过创新的 D2A 对齐训练实现了高效、稳定、可扩展的加速方案，在多个维度全面超越现有技术，为 LLM 高效推理提供了极具前景的方向。

</details>

---

### 3. [Meta Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN](https://arxiv.org/abs/2512.13715)

**Authors**: Fatemeh Lotfi, Fatemeh Afghah  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.13715v1  

#### Abstract
The increasing complexity of modern applications demands wireless networks capable of real time adaptability and efficient resource management. The Open Radio Access Network (O-RAN) architecture, with its RAN Intelligent Controller (RIC) modules, has emerged as a pivotal solution for dynamic resourc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

**论文标题**: *Meta-Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代无线网络（特别是基于 **O-RAN** 架构）面临动态、不可预测且高度异构的资源管理挑战。传统方法在以下方面存在不足：
- 难以应对突发流量、热点区域和非平稳环境；
- 缺乏对多种网络切片（如 eMBB、URLLC、mMTC）的联合优化能力；
- 在大规模部署下适应性差、收敛慢、稳定性低。

### 提出的新方法与创新思路
本文提出了一种 **自适应 Meta-Hierarchical Reinforcement Learning (Meta-HRL)** 框架，其核心创新包括：

#### （1）**分层决策架构（HRL）**
- 将资源管理分解为两个层级：
  - **高层控制器（High-level Controller）**：负责跨切片的资源分配（如带宽、功率）；
  - **底层代理（Low-level Agents）**：执行各切片内的用户级调度（如 RB 分配）。
- 这种结构提升了可扩展性和决策粒度，适用于复杂多变的 O-RAN 场景。

#### （2）**基于 MAML 的元学习机制**
- 引入 **Model-Agnostic Meta-Learning (MAML)** 思想，使模型能从历史任务中“学会如何快速学习”；
- 支持在新 DU 或突发场景下实现 **few-shot adaptation**，显著加快策略微调速度。

#### （3）**自适应加权元更新机制（Adaptive Variance Weighting）**
- 创新性地使用 **Temporal Difference Error (TD-error) 方差** 来衡量任务复杂度；
- 定义自适应权重 $ w_g = \text{Softmin}(\sigma^2_{\text{TD},g}) $，优先更新高方差（即更复杂/不稳定）的任务；
- 提升训练稳定性和泛化能力，避免对简单任务过拟合或忽略关键难点。

#### （4）理论保障
- 给出了 **收敛性分析** 和 **regret bound**，证明该框架具有亚线性收敛速率和稳定的两时间尺度学习行为。

### 相比现有方法的优势
| 对比维度 | 本文方法（Meta-HRL） | 现有方法（如 DRL、Federated RL、Single-Slice Meta-RL） |
|--------|---------------------|---------------------------------------------|
| 适应性 | 快速适应新任务（few-shot） | 冷启动慢，需大量训练 |
| 可扩展性 | 分布式 DU 架构 + 层次化控制，支持大规模部署 | 多数集中式设计，扩展性受限 |
| 泛化能力 | 跨任务迁移强，通过 meta-learning 实现 | 通常局限于特定场景 |
| 稳定性 | 自适应加权减少灾难性遗忘 | 易受非平稳环境干扰 |
| QoS 保证 | 同时满足 eMBB、URLLC、mMTC 的差异化需求 | 往往只关注单一类型服务 |

---

## 2. 核心实验方法和设置

### 实验平台与仿真环境
- 基于 **O-RAN 架构模拟器** 构建仿真系统；
- 使用 **PyTorch** 实现 DDPG-based actor-critic 模型；
- 所有智能体作为 **xApp** 部署在 Near-RT RIC 中，符合 O-RAN 规范。

### 网络配置参数（见 Table 1）
| 参数 | 值 |
|------|----|
| 子载波间隔 | 15 kHz |
| 单个 DU 总带宽 | 20 MHz |
| 每个 DU 的 RB 数量 ($K_p$) | 100 |
| 用户数量 per DU | 30 |
| 分布式单元数 ($N_g$) | 7 |
| 用户移动速度 | 10–20 m/s |
| 折扣因子 $\gamma$ | 0.99 |

### 网络切片类型及 KPI
| 切片类型 | QoS 指标 | 定义 |
|--------|---------|------|
| eMBB | 吞吐量（Throughput） | $ \frac{1}{N_u}\sum C_i $ |
| mMTC | 密度支持能力 | $ \frac{1}{N_u}\sum C_i \cdot \mathbf{1}(C_i \geq \lambda_i) $ |
| URLLC | 最大延迟（Latency） | $ \max(T_i) $ |

### 基线方法对比
- **DRL**：标准深度强化学习（从零开始训练）；
- **Transfer Learning (TL)**：迁移预训练模型；
- **Multi-Task Learning (MTL)**：同时学习多个任务；
- **MAML-RL**：基础元强化学习；
- **Uniform-Meta**：等权元更新（消融实验用）；
- **Static-Var**：固定方差加权（消融实验用）。

### 评估指标
- 平均累计奖励（Cumulative Reward）
- 各切片 QoS 满足率（CDF 曲线）
- 用户吞吐量分布（per-user throughput）
- 适应速度（Adaptation shots to converge）
- 公平性（Jain’s Fairness Index）
- 平均延迟、抗突发流量鲁棒性

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ **总体效率提升**
- 相比基线方法（DRL、TL、MTL），提出的 **Adaptive MAML-HRL** 在最终返回值上实现了 **19.8% 的网络管理效率提升**（Section VII-B, Fig. 4）。

#### ✅ **更快的适应能力**
- 在 **5-shot 场景** 下，Adaptive MAML-HRL 较普通 MAML-RL 提升约 **4%**，较 DRL 提升高达 **73%**；
- 消融实验显示，相比均匀加权（Uniform-Meta），所提自适应机制可实现 **40% 更快收敛**（Table 3）。

#### ✅ **QoS 表现全面领先**
- **eMBB 切片**：提供最高吞吐量，适合高清视频流等应用；
- **mMTC 切片**：在高密度设备连接下仍保持良好服务质量；
- **URLLC 切片**：实现最低延迟，满足自动驾驶等严苛要求（Fig. 6, 9）。

#### ✅ **用户级体验改善**
- Fig. 7 显示，所有切片中用户的 **吞吐量 CDF 曲线明显右移**，表明用户体验显著提升；
- 特别是在 mMTC 场景中，自适应机制有效平衡了海量设备间的资源竞争。

#### ✅ **消融实验验证有效性（Table 3）**
| 方法 | 归一化奖励 | 收敛所需适应步数 |
|------|------------|------------------|
| Uniform-Meta | 0.78 | 28 |
| Static-Var | 0.81 | 22 |
| **Adaptive-Var (提出)** | **0.84** | **17** |

> 结果表明：**动态软最小加权（Softmin on TD-error variance）是最优策略**，带来约 **3% 奖励增益** 和 **近 40% 加速收敛**。

#### ✅ **额外网络指标改进（Section I）**
- 平均包延迟 ↓ **9.2%**
- Jain 公平性指数 ↑ 从 **0.91 → 0.96**
- 在 **50% 流量激增** 场景下性能下降 < **5%**，表现出强鲁棒性。

#### ✅ **可扩展性表现优异（Table 2）**
| $N_{DU}$ | $N_{UE}$ | 收敛迭代增加 | 归一化奖励变化 |
|----------|-----------|---------------|----------------|
| 7 | 30 | 0% (基准) | 0.00 |
| 15 | 100 | +32% | -0.01 |
| 30 | 200 | +69% | -0.02 |

> 表明：尽管任务规模扩大，但 **归一化性能几乎不变**，说明框架具备良好的 **scalability**。

---

## 4. 关键结论和发现

### 主要发现
1. **Meta-HRL 是解决 O-RAN 动态资源管理的有效范式**：
   - 结合 HRL 的结构性优势与 Meta-RL 的快速适应能力，特别适合多切片、非平稳环境。

2. **自适应加权机制至关重要**：
   - 利用 TD-error 方差自动识别“困难任务”，并赋予更高学习优先级，显著提升训练效率和稳定性。

3. **本地化处理增强实时性**：
   - 将决策下沉至 DU 层面，减少信令开销，缓解乱序问题，有利于低延迟操作。

4. **理论与实践一致**：
   - 理论分析证明了算法的 **sublinear convergence** 和 **bounded regret**，实验结果也验证了其稳定性和高效性。

### 方法的局限性
- 当前研究基于 **仿真环境**，尚未在真实 O-RAN testbed 上验证；
- 元更新通信虽轻量（仅传输参数），但在超大规模部署中仍可能引入延迟；
- 模型假设状态空间连续且可观测，在部分遮蔽环境下可能退化。

### 未来工作方向
1. **硬件在环验证（Hardware-in-the-loop testing）**；
2. **通信高效的元参数更新机制**（如量化、稀疏化）；
3. **扩展至 multi-hop 和 UAV-assisted 6G 场景**；
4. 探索与其他 AI 技术（如联邦学习、因果推理）的融合。

---

> 📌 **总结一句话**：  
> 本文提出的 **Adaptive Meta-HRL** 框架通过 **层次化控制 + MAML 元学习 + TD-error 方差加权机制**，实现了 O-RAN 系统中高效、快速、公平且可扩展的资源管理，在多项指标上显著优于现有方法，并具备坚实的理论支撑和广阔的应用前景。

</details>

---

### 4. [LLMQ: Efficient Lower-Precision Pretraining for Consumer GPUs](https://arxiv.org/abs/2512.15306)

**Authors**: Erik Schultheis, Dan Alistarh  
**Category**: cs.DC  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15306v1  

#### Abstract
We present LLMQ, an end-to-end CUDA/C++ implementation for medium-sized language-model training, e.g. 3B to 32B parameters, on affordable, commodity GPUs. These devices are characterized by low memory availability and slow communication compared to datacentre-grade GPUs. Consequently, we showcase a ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LLMQ: Efficient Lower-Precision Pretraining for Consumer GPUs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLM）训练主要依赖昂贵的数据中心级GPU（如H100、L40S），这些设备具有高内存带宽和NVLink通信能力。然而，普通用户通常只能访问消费级GPU（如RTX 4090、RTX 5060Ti），其存在以下瓶颈：
- **显存容量有限**（通常≤24GB）
- **GPU间通信带宽低**（通过PCIe而非NVLink）
- **缺乏高效的集体通信支持**

这导致在消费级硬件上难以高效训练中等规模模型（如7B~32B参数）。LLMQ旨在解决这一问题，实现**在低成本消费级GPU上进行高效、端到端的LLM预训练与微调**。

---

### 提出的新方法与创新点

LLMQ是一个完全用C++/CUDA编写的开源框架，专为消费级GPU优化，核心创新包括：

#### （1）**混合精度FP8训练流水线**
- 支持 **bfloat16（BF16）与FP8混合训练**，主Transformer层中的矩阵乘法（matmul）使用FP8，非线性操作、注意力（SDPA）、嵌入层和LM Head保持BF16。
- 采用 **tensor-level absmax动态缩放** 进行FP8量化，保证无溢出，适用于Ada（RTX 40xx）和Blackwell（RTX 50xx）架构。
- 在不引入额外算法近似的情况下，维持高FLOPs利用率。

#### （2）多层次内存优化策略
针对显存受限场景，提出组合式优化方案：
- **选择性激活重计算（Selective Recomputation）**：从仅重算非矩阵运算层（如SwiGLU、RMSNorm）到完整Transformer块重算，灵活平衡计算与内存。
- **CPU Offloading**：
  - 将残差（residuals）、优化器状态（m/v）、主参数（master weights）、梯度缓冲区等卸载至CPU内存。
  - 利用“权重缓存在主机”机制减少多卡通信开销。
- **分块计算（Chunking）**：对logits和FlashAttention的workspace进行切片处理，避免大张量占用过多内存。

#### （3）基于`cudaMemcpy`的高效通信后端
- 针对消费级GPU无法直接P2P通信的问题，设计了一种**基于copy engine的reduce-scatter和all-gather实现**。
- 利用GPU的DMA引擎（copy engine）执行数据传输，**不占用SM资源**，可与后续backward计算并行。
- 相比NCCL，在RTX 4090集群上显著提升PCIe带宽利用率。

#### （4）确定性训练支持
- 所有kernel均为bitwise-deterministic，确保相同配置下结果可复现。
- 使用counter-based随机数生成器实现确定性stochastic rounding。

---

### 相比现有方法的优势

| 维度 | LLMQ优势 |
|------|----------|
| **硬件适配性** | 专为消费级GPU设计，充分利用其性价比（FLOPs/$） |
| **内存效率** | 多级offloading + 重计算 + FP8，可在16GB显存上训练7B模型 |
| **通信效率** | `cudaMemcpy`-based collectives在无NVLink设备上远优于NCCL |
| **性能表现** | 在4×RTX 4090上训练32B模型达到51% MFU，超过L40S的29% MFU |
| **易用性** | 端到端C++实现，启动时完成所有内存分配，避免运行时OOM |

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：使用retokenized并子采样的[ClimbMix](https://arxiv.org/abs/2504.13161)数据集，包含10B~30B tokens。
- **微调任务**：
  - **GSM8K**：数学推理基准，用于评估LLaMA2-7B和Qwen2.5-14B的微调效果。

---

### 实验设置

#### 硬件平台
- **单卡**：RTX 5060Ti（16GB）、RTX 4090（24GB）
- **多卡工作站**：4×RTX 4090、4×L40S
- **新型设备测试**：NVIDIA DGX Spark（Blackwell架构，128GB统一内存）

#### 模型规模
覆盖0.5B ~ 32B参数模型，典型结构为Qwen-style或LLaMA2架构。

#### 评估指标
- **Tokens Per Second (TPS)**：吞吐量
- **Model FLOPs Utilization (MFU)**：实际FLOPs占理论峰值的比例，考虑混合精度下的加权计算
- **Speedup (Sp)**：FP8相对于BF16的加速比
- **验证损失与准确率**：GSM8K上的few-shot/zero-shot性能

#### 基线对比
- **LLaMA-Factory (LF)**：主流开源微调框架
- **NCCL-based系统**：标准多GPU通信方案
- **专业级GPU（L40S）**：作为高性能对照组

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Tables 1–3）

| 模型大小 | 硬件配置 | 精度 | TPS | MFU |
|---------|--------|-----|-----|-----|
| 7B | 单卡 RTX 5060Ti (16GB) | FP8 | 1.4k | 70% |
| 7B | 单卡 RTX 4090 | FP8 | 4.3k | 61% |
| 14B | 单卡 RTX 4090 | FP8 | 2.0k | 55% |
| 32B | 4×RTX 4090 | FP8 | 3.4k | 51% |
| 32B | 4×L40S | BF16 | 3.0k | 40% |

> ⚠️ 注：LLMQ在4×RTX 4090上训练32B模型达到**51% MFU**，而同规模L40S仅达**29% MFU**，说明消费级硬件经优化后可超越专业卡效率。

---

### 与基线方法对比

#### vs LLaMA-Factory（Table 4 & Appendix A.1）
| 模型 | 硬件 | 方法 | TPS | 对比优势 |
|------|------|------|-----|----------|
| 1.5B | 1×4090 | LLMQ (FP8) | 20k | 是LF的~2倍 |
| 14B | 1×4090 | LLMQ (FP8) | 2.0k | 是LF (~1.2k) 的1.7倍 |
| 32B | 4×4090 | LLMQ (FP8) | 3.4k | LF OOM |

- LLMQ通过更细粒度的offloading控制和更低开销的kernel调度，在大模型上显著胜出。
- LF倾向于“全量offload以支持大batch”，而LLMQ采用渐进式offload，在中等batch下即可稳定运行。

#### vs NCCL通信（Table 5）
| 硬件 | 方法 | FP8 TPS (14B) | 加速比 |
|------|------|----------------|--------|
| 4×RTX 4090 | NCCL only | 4.3k | —— |
| 4×RTX 4090 | Full `cudaMemcpy` | **7.8k** | **+81%** |
| 4×L40S | NCCL only | 9.5k | —— |
| 4×L40S | Full `cudaMemcpy` | 9.9k | +4% |

> ✅ 结论：**在无P2P连接的消费级GPU上，`cudaMemcpy`-based通信至关重要；而在专业卡上NCCL已足够高效**。

---

### 消融实验与关键发现

#### FP8带来的加速效果（Sp列）
- 小模型（0.5B）：FP8提速约20%~30%
- 中大模型（≥7B）：FP8提速达**50%以上**
- 原因：随着模型增大，GEMM占比上升，FP8优势显现

#### 内存优化有效性
- 在RTX 5060Ti上，结合block-level recompute + offloading (m,v,0*,x,g)，成功将7B模型micro-batch size从1提升至16。
- Offloading梯度缓冲区后，显存消耗降至“两层Transformer”水平。

#### DGX Spark初步测试（Table 3）
- 虽然拥有128GB统一内存，但内存带宽较低（300 GB/s），限制了性能。
- 小模型（≤3B）受益于无需offload，但FP8提速有限（<22%），因内存bound kernel未受益于低精度。
- 仅当模型达7B时，GEMM主导计算，FP8带来**41%加速**。

---

## 4. 关键结论和发现

### 主要发现
1. **消费级GPU完全可以胜任中等规模LLM训练**，只要配合合理的系统优化（offload + recompute + FP8 + custom comm）。
2. **FP8训练在大模型上能带来显著加速（~50%）**，且不影响最终模型质量（见GSM8K结果）。
3. **传统NCCL在消费级GPU上性能不佳**，应优先采用`cudaMemcpy`驱动的通信方案。
4. **LLMQ的MFU高达51%~70%**，媲美甚至超过云厂商的大规模训练系统。
5. **统一内存设备（如DGX Spark）虽简化编程模型，但仍受带宽限制**，需进一步软硬件协同优化。

---

### 方法的局限性
1. **目前仅支持单节点内多GPU训练**，尚未扩展至跨节点分布式。
2. **FP8量化依赖absmax scaling**，可能不如channel-wise scaling精确，但在实践中表现良好。
3. **极端大规模训练（>100B tokens）稳定性待验证**，作者指出低精度训练在超长训练中可能存在退化风险。
4. **offloading重度依赖主机内存带宽**，若host memory不足或NVMe参与，则延迟不可隐藏。

---

### 未来工作方向
- 扩展至**多节点训练**，结合Zero-Infinity等NVMe offloading技术。
- 探索**更细粒度的量化策略**（如per-channel FP8 scaling）。
- 支持更多**parameter-efficient fine-tuning（PEFT）方法**（如LoRA）以进一步降低内存需求。
- 针对**Blackwell架构特性**（如原生FP8 transpose支持）进行深度优化。
- 开发自动化工具，根据硬件自动选择最优的recompute/offload/batch配置。

---

> 🔗 **代码地址**：[https://github.com/IST-DASLab/llmq](https://github.com/IST-DASLab/llmq)  
> 📄 **原文链接**：[arXiv:2512.15306](https://arxiv.org/abs/2512.15306)

</details>

---

### 5. [Dynamic Rebatching for Efficient Early-Exit Inference with DREX](https://arxiv.org/abs/2512.15705)

**Authors**: Xuting Liu, Daniel Alexander, Siva Kesava Reddy Kakarla, Behnaz Arzani, Vincent Liu  
**Category**: cs.DC  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15705v1  

#### Abstract
Early-Exit (EE) is a Large Language Model (LLM) architecture that accelerates inference by allowing easier tokens to be generated using only a subset of the model's layers. However, traditional batching frameworks are ill-suited for EE LLMs, as not all requests in a batch may be ready to exit at the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Dynamic Rebatching for Efficient Early-Exit Inference with DREX**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
传统 **Early-Exit (EE)** 推理在 **批处理（batching）** 场景下面临严重挑战：
- **Grouped Exit Policies**（如 Consensus、Majority、Greedy）强制整个 batch 统一决定是否退出，导致两类问题：
  - **Involuntary Exits**：本应继续计算的请求被强制提前退出，损害输出质量。
  - **Involuntary Stays**：已满足退出条件的请求被迫继续计算，浪费算力，降低吞吐量。
- 此外，EE 会跳过中间层，导致后续解码所需的 **KV Cache 缺失**，影响自回归推理。

这些问题使得 EE 在实际部署中难以发挥其理论上的效率优势。

---

### **提出的新方法：DREX 与 Dynamic Rebatching**

论文提出了 **DREX**，首个支持高效批处理 EE 推理的服务系统，其核心是 **Dynamic Rebatching** 技术：

#### **核心思想**
- 不再对整个 batch 强制统一的 EE 决策。
- 当出现“分裂决策”时（部分请求可 EE，部分不能）：
  - 允许满足条件的请求立即退出并生成 token。
  - 将未退出的请求暂存于一个 **逻辑缓冲区（logical buffer）**。
  - 当缓冲区积累足够多请求后，重新组织成新 batch 进入深层网络。

这实现了：
- ✅ 每个请求独立决策，**完全消除 involuntary exits**。
- ✅ 最大化利用 EE 机会，提升吞吐。

---

### **关键技术优化**

1. **Copy-Free Rebatching Buffer**
   - 利用现代注意力内核（如 FlashAttention）的 `cache_batch_idx` API，通过 **虚拟张量索引（virtual tensor indexing）** 实现零拷贝重组。
   - 避免物理移动隐藏状态和 KV Cache，显著降低 rebatching 开销（<6% 标准迭代时间）。

2. **Memory-Efficient State-Copying**
   - 解决跳过层缺失 KV Cache 的问题。
   - 使用 **虚拟内存映射（virtual memory mapping）**，将所有跳过层的 KV Cache 映射到最后一层的实际内存块。
   - 实现共享只读内存，避免冗余复制，减少最多 **18.3% CUDA 内存占用**。

3. **Adaptive Rebatching Threshold (ART)**
   - 分析 rebatching 的开销（c）与 EE 节省的计算量（savings），动态判断是否值得进行 rebatching。
   - 只有当 EE 请求数量超过阈值时才触发 rebatching，避免“得不偿失”的操作。
   - 提升吞吐达 **9%**。

4. **SLA-Aware Forced Flushing**
   - 缓冲区中的请求可能因等待而延迟。
   - 引入基于 SLA 的刷新机制：若请求接近截止时间，则强制刷新缓冲区，保障延迟。
   - 平均 RCT 改善 **58.4%**。

---

### **相比现有方法的优势**

| 方面 | DREX | 现有方法（如 Apparate、[31]） |
|------|------|-----------------------------|
| **EE 决策粒度** | Per-request 独立决策 | Batch-wise 统一决策 |
| **Involuntary Exits** | **0%**（完全消除） | 高发（尤其 Greedy） |
| **KV Cache 管理** | 虚拟映射，无冗余 | 物理复制或重计算，低效 |
| **Rebatching 开销控制** | 自适应阈值（ART） | 固定策略，易引入负收益 |
| **SLA 保障** | 主动刷新机制 | 忽略延迟影响 |

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**
- **模型**：
  - `Llama-EE-13B`, `Llama-EE-70B`, `Qwen-EE-14B`
  - 均基于 Apparate 架构添加 EE ramp（Softmax 置信度分类器）
- **数据集**：CNN/Daily Mail 新闻摘要任务（来自 HELM benchmark）
  - 过滤为 <2048 tokens 的条目，共 2160 条样本
- **硬件**：
  - 13B/14B：NVIDIA A100 (80GB)
  - 70B：NVIDIA H200 (141GB)

---

### **实验设置**
- **Batch Size**：4 和 8
- **EE 配置**：每模型两种配置（不同 ramp 层 + 置信度阈值），见 Table 3
- **评估阶段**：Decode Phase（自回归生成）

---

### **评估指标**

| 类别 | 指标 | 定义 |
|------|------|------|
| **性能** | Throughput | 输出 token 数 / 秒 |
|       | RCT (Request Completion Time) | 请求从调度到完成的时间 |
| **质量** | P95 Confidence Score | 所有 EE token 中置信度最低的 5%，反映最差情况下的输出可靠性 |
|       | BERT Score | 生成摘要与参考摘要的语义相似度 |
| **EE 统计** | EE Proportion | 早期退出 token 占总 token 数比例 |
|           | Involuntary Exit (%) | 被迫提前退出的 token 比例 |
|           | Involuntary Stay (%) | 被迫继续计算的 token 比例 |

---

### **基线方法对比**
- **Latency-only**（Apparate）：允许 EE 但保留请求在 batch 中 → 低延迟但低吞吐
- **Consensus**：全 batch 同意才 EE → 高质量但 EE 机会少
- **Majority**：多数投票决定 EE
- **Greedy**：任一请求想 EE 就全体退出 → 高吞吐但质量极差
- **Non-EE**：无 EE 的标准推理

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

| 模型 | 方法 | Throughput 提升 | P95 Confidence | Involuntary Exit |
|------|------|------------------|----------------|------------------|
| Llama-EE-70B | DREX (Rebatching) | **+12%** vs Non-EE | ≈0.8（最高） | **0%** |
| Llama-EE-13B | DREX | **+2–10.3%** vs 其他 EE 方法 | >0.75 | 0% |
| Qwen-EE-14B | DREX | 显著优于基线 | 保持高位 | 0% |

- **KV Cache 内存节省**：平均减少 **5.7%**，最多达 **18.3%**（Greedy 下）
- **Rebatching 开销**：<6% 标准迭代时间（Llama-EE-70B）

---

### **与基线方法对比结果**

#### **吞吐 vs 质量权衡（Figure 8, 9）**
- **Greedy**：吞吐最高，但 P95 Confidence 仅 **0.03**（极低），BERT Score 最差。
- **Consensus**：质量高，但 EE 比例极低，吞吐甚至低于 Non-EE（因检查开销）。
- **DREX**：
  - 吞吐比 Consensus/Majority 高 **2–12%**
  - 质量与 Consensus 相当，远超 Greedy
  - **唯一实现 0% involuntary exits 的方法**

#### **EE 比例分析（Figure 9）**
- DREX 实现第二高的 EE 比例，但 **involuntary exits 为 0**
- Greedy 的 EE 比例 >97%，但其中 **>35% 是 involuntary**

---

### **消融实验结果**

#### **Adaptive Rebatching Threshold (ART) 的影响（Table 5）**
- 在 Llama-EE-13B 上，最优 ART = 3（即至少 4 个请求 EE 才 rebatch）
- 相比 ART=0（总是 rebatch），吞吐提升 **9%**
- 说明：盲目 rebatching 反而降低性能，**自适应阈值至关重要**

#### **SLA-Aware Flushing（Figure 12）**
- 无 SLA 压力时：吞吐比 Consensus 高 **11.4%**
- 高 SLA 压力时：自动切换至保守策略，保障延迟，**平均 RCT 改善 58.4%**

#### **多出口场景（Figure 11）**
- 在 Llama-EE-70B 设置两个 EE ramp
- DREX 比 Greedy 吞吐略低，但 **BERT Score 高 11%**
- 表明 DREX 更好地平衡了效率与质量

---

## **4. 关键结论和发现**

### **主要发现**
1. **Dynamic Rebatching 是 EE 批处理的关键突破**：
   - 解耦了 EE 决策与 batch 同步约束，释放了 EE 的真实潜力。
2. **DREX 实现了吞吐与质量的双赢**：
   - 吞吐提升 **2–12%**
   - **完全消除 involuntary exits**，保证输出质量
   - P95 Confidence 和 BERT Score 均优于或等于其他 EE 方法
3. **系统级优化至关重要**：
   - Copy-free buffer 和 virtual state-copying 极大降低了 EE 开销。
   - ART 和 SLA-aware 调度使系统更智能、鲁棒。

---

### **局限性**
- 当前实现依赖于支持虚拟索引的 attention kernel（如 FlashAttention）。
- 对极端小 batch 或低负载场景优化空间有限。
- 未探索与其他动态计算技术（如 Speculative Decoding）的结合。

---

### **未来工作方向**
- 扩展至更多 EE 架构（如 CALM、DEED、FREE）。
- 结合 **Speculative Decoding** 与 Dynamic Rebatching。
- 探索 **异构硬件**（CPU+GPU）下的 rebatching 策略。
- 自动化 EE ramp 位置与阈值调优（与 Apparate、HELIOS 联动）。

---

> **总结**：  
> DREX 是首个真正让 **Early-Exit LLM** 走向实用的推理系统。它通过 **Dynamic Rebatching** 解决了批处理与 EE 的根本矛盾，并辅以多项系统级优化，在不牺牲质量的前提下显著提升了吞吐。该工作为高效 LLM 推理提供了新的范式。

</details>

---

### 6. [Bits for Privacy: Evaluating Post-Training Quantization via Membership Inference](https://arxiv.org/abs/2512.15335)

**Authors**: Chenxiang Zhang, Tongxi Qu, Zhong Li, Tian Zhang, Jun Pang, Sjouke Mauw  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.15335v1  

#### Abstract
Deep neural networks are widely deployed with quantization techniques to reduce memory and computational costs by lowering the numerical precision of their parameters. While quantization alters model parameters and their outputs, existing privacy analyses primarily focus on full-precision models, le...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Bits for Privacy: Evaluating Post-Training Quantization via Membership Inference**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
本文系统研究了**Post-Training Quantization (PTQ)** 对模型隐私的影响，填补了当前研究中对量化模型隐私分析的空白。尽管 PTQ 被广泛用于提升推理效率，但其在降低参数精度过程中可能改变模型输出分布，从而影响隐私泄露风险。然而，现有隐私分析多集中于全精度模型，缺乏对不同量化位宽下隐私-效用权衡的深入理解。

### **提出了什么新方法或新思路**
- 首次将 **Membership Inference Attack (MIA)** 作为评估工具，系统分析多种先进 PTQ 方法（AdaRound、BRECQ、OBC）在不同量化精度（4-bit、2-bit、1.58-bit）下的隐私表现。
- 引入并适配了 **1.58-bit quantization** 到 PTQ 场景，基于 ternary 权重 {-1,0,1} 实现极低比特压缩，并验证其在隐私保护中的潜力。
- 提出“**decoupled quantization**”策略：仅将最后一层以更高精度（如 8-bit）保留，其余层保持低比特量化，实现对隐私-效用关系的细粒度控制。

### **相比现有方法的优势**
- **更贴近实际部署场景**：聚焦无需重新训练的 PTQ 方法，适用于工业界广泛使用的预训练模型压缩流程。
- **更强的攻击基准**：采用当前最先进的 **LiRA (Likelihood Ratio Attack)** 在线与离线版本进行 MIA 评估，尤其是在线攻击更具挑战性和现实威胁性。
- **全面的权衡分析**：不仅报告准确率下降，还通过 **TPR@0.1%FPR** 这一敏感指标衡量隐私泄露程度，揭示量化精度可作为调节隐私保护强度的“旋钮”。

---

## **2. 核心实验方法和设置**

### **使用了哪些数据集**
- **CIFAR-10**：10 类图像分类任务，32×32 彩色图像。
- **CIFAR-100**：100 类更复杂图像分类任务，同分辨率。
- **TinyImageNet**：200 类，64×64 图像，更具挑战性的子集。

### **实验设置和评估指标**
- **模型架构**：主要使用 **ResNet18**，部分消融实验扩展至 ResNet50 和 DenseNet121。
- **PTQ 方法**：
  - **AdaRound**
  - **BRECQ**
  - **OBC**
- **量化位宽**：32-bit（全精度）、4-bit、2-bit、1.58-bit（ternary）
- **量化方式**：仅对权重进行 per-channel asymmetric quantization，激活函数保持全精度。
- **校准数据**：从训练集中随机选取 1024 个样本用于 PTQ 校准，确保无额外数据泄露。
- **MIA 攻击方法**：
  - **LiRA Online Attack**：训练 64 个 shadow models（一半含目标样本），估计损失分布。
  - **LiRA Offline Attack with Fixed Variance**：更高效，但仍具强攻击能力。
- **评估指标**：
  - **Utility**：测试准确率（Test Accuracy）
  - **Privacy Leakage**：
    - **TPR@0.1%FPR**（核心指标，反映低误报率下的真实泄露风险）
    - log-AUROC（辅助指标）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### **(1) 准确率表现（Utility）**
| 数据集 | 方法 | 4-bit Acc (%) | 2-bit Acc (%) | 1.58-bit Acc (%) |
|--------|------|----------------|----------------|--------------------|
| CIFAR-100 | Full Precision | 70.22 | — | — |
| | AdaRound | ~70 | ~69.7 | **62.35** |
| | BRECQ | ~70 | ~69.8 | **67.61** |
| | OBC | ~70 | ~69.3 | **64.34** |

> 注：4-bit 量化几乎无精度损失；2-bit 精度略有下降；1.58-bit 下 BRECQ 表现最优。

#### **(2) 隐私泄露（TPR@0.1%FPR）——以 CIFAR-100 为例**
| 方法 | Full Precision | 4-bit | 2-bit | 1.58-bit |
|------|----------------|-------|-------|----------|
| AdaRound (Online) | 0.3765 | 0.372 | 0.2196 | **0.0031** |
| BRECQ (Online) | 0.3765 | ~0.37 | ~0.2 | **<0.01** |
| OBC (Online) | 0.3765 | ~0.38 | ~0.25 | **<0.01** |

> **关键发现**：1.58-bit 量化使 TPR 下降 **超过一个数量级（~99% reduction）**，显著增强隐私保护。

---

### **与基线方法的对比结果**
- **4-bit PTQ**：隐私泄露水平与全精度模型相当（甚至略高），说明常规轻量量化**不提供额外隐私增益**。
- **2-bit PTQ**：带来适度隐私改善（TPR 下降约 40–50%），但收益有限。
- **1.58-bit PTQ**：实现**显著隐私提升**，TPR 可降至 0.001–0.01 区间，同时仍维持可用准确率（尤其 BRECQ）。
- 所有三种 PTQ 方法在极端低位宽下均表现出一致趋势：**越低精度 → 越好隐私保护**。

---

### **消融实验结果**

#### **(1) Decoupled Quantization（解耦量化）**
- 将最后一层设为 8-bit，其余层为 1.58-bit 或 2-bit。
- **效果**：
  - 在 **TinyImageNet** 上，AdaRound 的准确率从 33%（1.58-bit 全局）恢复到 **52.4%**（仅最后层 8-bit）。
  - 同时，TPR@0.1%FPR 仍远低于全精度模型（如从 0.53 降至 <0.1）。
- **意义**：可在几乎不牺牲隐私的前提下大幅提升模型实用性，提供灵活调控手段。

#### **(2) 不同模型架构泛化性**
- 在 **ResNet18 / ResNet50 / DenseNet121** 上应用 OBC 进行量化。
- 结果显示：无论网络深度或结构如何，**更低量化位宽始终伴随更低准确率和更低 TPR**，验证结论具有普适性。

#### **(3) 1.58-bit 模型的方差分析**
- BRECQ 在 1.58-bit 下表现出较大 TPR 方差（某些运行隐私极好，某些较差），但平均而言仍优于其他方法。
- 表明 PTQ 过程存在随机性，需多次运行选择稳健模型。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **低位宽 PTQ 显著降低 MIA 成功率**：特别是 **1.58-bit 量化**可将 TPR@0.1%FPR 降低 **达 99%以上**，是有效的隐私增强手段。
2. ⚖️ **存在明确的 privacy-utility trade-off**：
   - 4-bit：效用高，隐私无改善；
   - 2-bit：轻微效用损失，适度隐私增益；
   - 1.58-bit：显著隐私提升，但效用明显下降。
3. 🔧 **Decoupled quantization 是实用解决方案**：通过提高最后一层精度，可在保留大部分隐私优势的同时大幅恢复准确率。
4. 📈 **BRECQ 在极端量化下综合表现最佳**：在 1.58-bit 下达到最高准确率和最低平均 TPR，适合追求极致压缩与隐私平衡的场景。
5. 🔄 **结论跨数据集、跨架构稳定成立**：在 CIFAR-10、CIFAR-100、TinyImageNet 及多种 DNN 架构上均观察到一致趋势。

---

### **方法的局限性**
- **仅评估视觉任务**：实验集中在图像分类任务（CIFAR/TinyImageNet），未涉及 NLP 或图神经网络等其他模态。
- **未包含激活量化**：所有实验仅对权重进行量化，若激活也参与量化，可能进一步影响隐私行为。
- **依赖经验性评估**：缺乏理论解释为何低位宽能抑制 MIA，目前仅为实证观察。
- **shadow model 开销大**：LiRA online 攻击需要大量 shadow models 训练，限制大规模实验。

---

### **未来工作方向**
1. **扩展至其他领域**：探索文本、语音、图结构数据中的 PTQ 隐私效应。
2. **结合其他隐私机制**：研究 PTQ 与 **Differential Privacy (DP)**、**Federated Learning** 等技术联合使用的效果。
3. **理论建模**：建立量化噪声与 MIA 抵抗力之间的数学联系，提供可解释框架。
4. **自动化调参**：设计算法自动选择每层最优量化位宽，在给定隐私预算下最大化效用。
5. **scaling law 下的行为研究**：探究大模型（如 ViT、LLM）在 PTQ 后的隐私变化规律。

---

> **总结一句话**：  
> 本文首次系统证明，**极端低位宽的 Post-Training Quantization（尤其是 1.58-bit）是一种简单而高效的 Membership Inference 防御手段**，通过合理设计（如 decoupled quantization），可在保持较高模型效用的同时实现强隐私保护，为高效且安全的边缘 AI 部署提供了新思路。

</details>

---

### 7. [Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification](https://arxiv.org/abs/2512.14491)

**Authors**: Cheng-Han Lu, Pei-Hsuan Tsai  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.14491v1  

#### Abstract
Transformer-based multi-modal intelligent systems often suffer from high computational and energy costs due to dense self-attention, limiting their scalability under resource constraints. This paper presents SMMT, a sparse multi-modal transformer architecture designed to improve efficiency and robus...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前基于 **Transformer** 的多模态智能系统在阿尔茨海默病（Alzheimer's Disease, AD）分类任务中面临的两个关键挑战：
- **高计算与能耗成本**：传统密集自注意力机制（dense self-attention）导致时间复杂度为 $O(n^2)$，难以在资源受限环境下扩展。
- **对不完整输入敏感**：临床数据常存在缺失模态（missing modalities），而现有模型缺乏显式机制来处理此类情况，影响实际部署中的鲁棒性。

### 提出的新方法：SMMT
作者提出 **Sparse Multi-Modal Transformer with Masking (SMMT)**，一种从系统层面设计的高效、鲁棒的多模态融合架构。其两大核心创新为：

#### （1）Cluster-based Sparse Attention
- 将每个模态内的 token 通过 **K-Means 聚类** 分组（簇数 $k = \log_2 n$）。
- 自注意力仅在同簇内计算，将计算复杂度从 $O(n^2)$ 降低至 $O(n \log n)$。
- 使用查询向量（query vectors）进行聚类，确保语义相关 token 被聚集，提升稀疏注意力的有效性。

#### （2）Modality-wise Masking
- 在训练过程中对各模态特征输出施加随机掩码（masking ratio $r=0.3$），模拟真实场景下的模态缺失。
- 推动模型学习更鲁棒的表示，增强在小样本或部分模态缺失条件下的泛化能力。

### 相比现有方法的优势
- **效率更高**：显著减少训练时间和内存占用，适合低资源环境。
- **更节能**：训练过程能耗下降超过 40%，有利于可持续 AI 部署。
- **更强鲁棒性**：在数据稀疏（如仅用 20% 数据）或模态缺失时仍保持高性能。
- **可扩展性强**：适用于大规模输入和边缘设备部署。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **ADNI-1 和 ADNI-2** 公开数据库，用于阿尔茨海默病研究。
- 包含多模态数据：
  - **影像数据**：T1-weighted MRI 切片（共 12,680 张）
  - **临床评分**：MMSE、CDR、FAQ
  - **人口统计学信息**：年龄、性别
  - **遗传标记**：APOE 基因型
- 只保留确诊为 **AD（Alzheimer’s Disease）** 或 **CN（Cognitively Normal）** 的个体，排除 MCI 样本以避免类别不平衡问题。
- 最终纳入人数见表 II（总计约 3,000 名受试者）。

### 实验设置
| 项目 | 设置 |
|------|------|
| 框架 | PyTorch |
| 硬件 | NVIDIA RTX 3060 GPU (12GB) |
| 训练轮次 | 50 epochs |
| 批大小 | 8 |
| 优化器 | Adam |
| 学习率 | $1 \times 10^{-3}$ |
| 损失函数 | Cross Entropy |
| 特征维度 | 512 |
| 掩码比例 | 0.3（modality-level random masking） |
| 重复实验 | 5 次不同随机种子取平均 |

### 评估指标

#### （1）分类性能指标
- **Accuracy**（准确率）
- **Precision**（精确率）
- **Recall / Sensitivity**（召回率 / 敏感性）
- **F1-score**
- **Specificity**（特异性）
- **AUC**（ROC 曲线下面积）

#### （2）计算可持续性指标（via CodeCarbon）
- **总能量消耗（kWh）**：监测 CPU、GPU、RAM 功耗
- **碳排放量（CO₂, kg）**：基于台湾电网碳强度（0.502 kgCO₂/kWh）估算

### 对比的基线方法
| 模型 | 类型说明 |
|------|--------|
| **3MT [8]** | 当前 SOTA 多模态 Transformer，采用级联交叉注意力（cascaded cross-attention） |
| **ADDFformer [20]** | 基于结构 MRI 的单模态 Transformer |
| **FusionNet [21]** | 通用多模态融合网络（无 attention） |
| **CNN-only [22]** | 基于 VGG16 的纯图像模型（MRI-only） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（100% 数据）

| 模型 | Accuracy (%) | Sensitivity (%) | Specificity (%) | AUC |
|------|--------------|------------------|------------------|-----|
| **SMMT (Ours)** | **97.05** | **96.31** | **97.58** | **0.986** |
| 3MT [8] | 90.28 | 93.64 | 93.81 | 0.965 |
| ADDFformer [20] | 88.20 | 91.87 | 91.53 | 0.948 |
| FusionNet [21] | 94.28 | 91.01 | 93.24 | 0.956 |
| CNN-only [22] | 80.24 | 78.45 | 80.22 | 0.852 |

> ✅ SMMT 在所有指标上均达到最优，尤其在 AUC 上领先明显。

### 小样本场景表现（低数据 regime）

| 数据比例 | SMMT | 3MT | ADDFformer | FusionNet | CNN-only |
|---------|-------|------|------------|-----------|----------|
| **20%** | **84.96%** | 78.92% | 71.92% | 75.15% | 68.53% |

> 🔥 在仅使用 20% 数据时，SMMT 仍能维持 **84.96% 准确率**，远超其他模型，体现极强的数据效率和泛化能力。

### 训练效率对比

- **训练时间 vs 数据规模**（图 5）：
  - SMMT 是所有 Transformer 类模型中训练最快的。
  - 随着数据量增加，SMMT 的扩展性优于 3MT。
- **能量消耗对比**（表 V）：

| 组件 | 3MT (Baseline) | SMMT | 下降幅度 |
|------|----------------|--------|----------|
| GPU | 0.283977 kWh | 0.159642 kWh | **-43.8%** |
| CPU | 0.108489 kWh | 0.071179 kWh | -34.4% |
| RAM | 0.051035 kWh | 0.033485 kWh | -34.4% |
| **Total** | **0.443501 kWh** | **0.264306 kWh** | **-40.4%** |

- **碳排放量**：
  - 3MT：0.2226 kgCO₂
  - SMMT：**0.1327 kgCO₂** → **减少 40.3%**

> 🌱 节能效果显著，相当于节省了约 18 次智能手机充电或点亮一个 60W 灯泡 30 小时的能量。

### 消融实验结果（Ablation Study）

| 配置 | 100% 数据 Accuracy / Time (min) | 20% 数据 Accuracy |
|------|-------------------------------|------------------|
| 3MT (Baseline) | 90.28 / 133 | 78.92 |
| w/o Sparse Attention | 91.35 / **147** | 84.85 |
| w/o Masking | 95.42 / 108 | **80.85** |
| **SMMT (Full)** | **97.05 / 112** | **84.96** |

#### 发现：
- 移除 **sparse attention** 导致训练时间大幅上升（+14 min），但精度略升 → 表明稀疏化带来显著效率增益。
- 移除 **masking** 后训练更快，但在小样本下精度下降明显（20% 数据时 ↓4.11%）→ 证明 masking 显著提升鲁棒性和泛化能力。
- **两者结合实现最佳权衡**：既高效又准确。

此外，作者还验证了不同 masking ratio $r$ 的影响（图 7）：
- 最佳值为 **$r=0.3$**，过高的 masking（>0.6）会导致性能急剧下降。

---

## 4. 关键结论和发现

### 主要发现
1. **SMMT 在 AD 分类任务中实现了 SOTA 性能**：
   - 全数据下达 **97.05% 准确率**，AUC 达 **0.986**。
   - 即使只用 20% 数据也能达到 **84.96% 准确率**，远超基线。

2. **稀疏注意力有效降低计算负担**：
   - 时间复杂度降至 $O(n \log n)$，训练速度更快，内存占用更低。
   - K-Means 聚类策略具有数据驱动优势，优于固定模式的稀疏方案。

3. **modality-wise masking 显著提升鲁棒性**：
   - 模拟模态缺失，使模型更具现实适应性。
   - 在小样本条件下防止过拟合，提升泛化能力。

4. **显著节能减排**：
   - 相比 3MT，**总能耗降低 40.4%**，碳排放减少近一半。
   - 为绿色 AI 和可持续医疗 AI 部署提供可行路径。

### 方法的局限性
- **未包含 MCI（轻度认知障碍）类别**：目前仅为二分类（AD vs CN），限制了临床分期预测的应用。
- **依赖批处理聚类**：K-Means 在每 batch 中执行，虽复用可接受，但仍引入额外开销。
- **尚未在更大、更多样化的跨中心数据集上验证**：泛化能力有待进一步检验。

### 未来工作方向
1. 扩展至 **三分类或多阶段预测**（CN → MCI → AD）。
2. 支持 **纵向建模（longitudinal modeling）**，用于疾病进展追踪。
3. 设计 **自适应模态选择机制**，动态应对推理时模态缺失。
4. 在更大规模、跨机构的临床数据集中验证 SMMT 的普适性。

---

> ✅ **总结一句话**：  
> SMMT 通过 **cluster-based sparse attention** 和 **modality-wise masking**，构建了一个高效、节能且鲁棒的多模态 Transformer 架构，在 AD 分类任务中实现了性能与可持续性的双重突破，是面向真实世界医疗 AI 部署的理想候选模型。

</details>

---

### 8. [CTkvr: KV Cache Retrieval for Long-Context LLMs via Centroid then Token Indexing](https://arxiv.org/abs/2512.15550)

**Authors**: Kuan Lu, Shuhang Lin, Sai Wu, Yichen Yao, Junhan Yang, Huan Li, Wei Chu, Xu Yinghui, Yuan Qi, Gang Chen  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.15550v1  

#### Abstract
Large language models (LLMs) are increasingly applied in long-context scenarios such as multi-turn conversations. However, long contexts pose significant challenges for inference efficiency, including high memory overhead from Key-Value (KV) cache and increased latency due to excessive memory access...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CTKvR: Efficient KV Cache Retrieval for Long-Context LLMs via Centroid-then-Token Indexing

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在长上下文场景（如多轮对话、长文档问答）中面临显著的推理效率挑战，主要体现在：
- **高内存开销**：Key-Value (KV) cache 随序列长度线性增长，消耗大量 GPU 显存（VRAM），限制批处理大小（batch size）和吞吐量。
- **高延迟**：传统稀疏注意力方法在预填充（prefilling）和解码（decoding）阶段引入显著计算开销。

现有方法存在权衡：
- **基于驱逐的方法**（如 SNAPKV）：通过丢弃 KV 对减少内存，但导致信息丢失和精度下降。
- **块级索引**（如 ShadowKV, QUEST）：检索粒度粗，可能引入无关 KV 条目，影响精度。
- **令牌级索引**（如 MAGICPIG, RetrievalAttention）：精度高，但索引构建和检索开销大，延迟高。

### 提出的新方法：CTKvR
提出 **Centroid-then-Token KV Retrieval (CTKvR)**，一种新颖的两阶段 KV 检索方案，核心思想是利用 **Rotary Position Embedding (RoPE)** 后相邻位置查询向量的高度相似性。

#### 创新点
1. **两阶段检索机制**：
   - **第一阶段：查询中心点索引 (Query-centroid Indexing)**  
     在预填充阶段，从长上下文中提取最后 `C` 个查询向量作为“中心点”（centroids）。为每个中心点计算其与所有 Key 的注意力分数，并存储其最相似的 `p` 个 Key 的索引，形成 **查询中心点倒排文件 (QcIVF)**。该索引轻量且易于构建。
   - **第二阶段：细粒度令牌级索引 (Fine-grained Token-Level Indexing)**  
     在解码时，先计算当前查询 `Q` 与所有中心点的相似度，选出最相似的 `C'` 个中心点，获取它们对应的候选 Key 集合（`KRecall`）。然后在这些候选集中进行精细排序（rerank），选出最终的 `p'` 个 Key 进行注意力计算。

2. **系统级优化**：
   - **CPU-GPU 协同执行**：将大部分 KV cache 和部分计算（如 Q2K Rerank）卸载到 CPU，利用 DRAM 扩展内存容量，支持更大 batch size。
   - **定制化 CUDA 内核与多流技术**：优化索引去重和检索过程，并通过 CUDA 多流实现 GPU 和 CPU 计算的重叠，最大化吞吐量。

### 相比现有方法的优势
- **高精度**：通过两阶段检索，既保证了检索范围的准确性，又避免了块级方法的噪声。
- **高效率**：QcIVF 构建和搜索成本远低于 ANN 方法（如 HNSW, IVF），显著降低预填充开销。
- **高吞吐量**：支持更大的 batch size 和更长的上下文，实测吞吐量提升 3-4 倍。
- **低内存占用**：QcIVF 本身内存开销极小（约 20MB），可将 GPU 上的 KV cache 存储降至原始的 5%。

---

## 2. 核心实验方法和设置

### 数据集
- **RULER**：一个可定制上下文长度的基准，包含 13 个任务，涵盖检索、多跳追踪、聚合和问答（QA）四类，测试上下文长度从 8K 到 128K。
- **LongBench**：包含 21 个英文和中文数据集，聚焦于单文档 QA、多文档 QA、摘要、少样本学习和代码补全等任务。
- **Needle-in-a-Haystack**：用于测试模型从超长文本中检索关键信息的能力，上下文长度从 16K 到 1M。

### 实验设置和评估指标
- **模型**：Llama-3-8B-262K 和 Yi-9B-200K。
- **上下文长度**：主要在 96K 上评估系统效率，在 8K-128K 上评估精度。
- **硬件**：A6000 (48GB/24GB), V100 (32GB), 搭配 Intel Xeon CPU。
- **评估指标**：
  - **精度**：任务准确率（Accuracy）、平均得分。
  - **效率**：吞吐量（Tokens/s）、最大支持 batch size、索引构建时间。

### 基线方法对比
- **FULLKV**：不压缩 KV cache，作为精度上限。
- **SNAPKV**：基于注意力分数的 KV 驱逐方法。
- **QUEST / ShadowKV**：块级稀疏注意力方法。
- **MAGICPIG**：基于 LSH 的令牌级稀疏注意力方法。
- **FLAT**：精确 KNN 检索方法（作为理想参照）。
- **VLLM**：高效的 Full-KV 推理框架（共享 KV cache）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **精度**：
  - CTKvR 在 RULER 和 LongBench 上的精度损失小于 1%，与 FULLKV 几乎持平。
  - 在 Needle-in-a-Haystack 任务中，CTKvR 能成功检索到不同位置的“针”，即使在 1M 上下文长度下也表现稳健。
- **吞吐量**：
  - 在 96K 上下文长度下，相比 FULLKV，CTKvR 在 Llama-3-8B 上实现 **3× 吞吐加速**，在 Yi-9B 上实现 **4× 加速**。
- **批处理大小**：
  - 支持的 batch size 达到 FULLKV 的 **10-20 倍**。
  - 即使在仅 24GB VRAM 的 GPU 上也能处理 96K 上下文。

### 与基线方法的对比结果
| 方法 | 精度 | 吞吐量 | 内存效率 | 备注 |
|------|------|--------|----------|------|
| **FULLKV** | ✅ 最高 | ❌ 最低 | ❌ 差 | 基准 |
| **SNAPKV** | ❌ 严重下降 | ⚠️ 中等 | ✅ 好 | 信息丢失 |
| **ShadowKV / QUEST** | ⚠️ 中等下降 | ⚠️ 中等 | ✅ 好 | 块内噪声 |
| **MAGICPIG** | ✅ 高 | ⚠️ 中等 | ⚠️ 一般 | LSH 表占内存 |
| **CTKvR** | ✅ 接近 FULLKV (<1% 降) | ✅ **最高 (3-4×)** | ✅ **最优** | 综合最佳 |

- **索引构建效率**：CTKvR 的索引构建时间比 HNSW、IVF、KMeans 等 ANN 方法快 **50-10000 倍**，且随上下文增长更稳定。

### 消融实验结果
- **Rerank 模块**：启用 Rerank 可进一步提升吞吐量达 **2×**，因为它减少了最终注意力计算的 Key 数量。
- **动态中心点更新 (DCU)**：在多轮对话场景中，DCU 显著提升了后续轮次的检索精度（如第 8 轮对话中，准确率从 85.67% 提升至 94.02%）。
- **参数敏感性**：
  - **维护的中心点数 C**：增加 C 提升精度，但在 C≈320 后趋于饱和。
  - **检索的中心点数 C'**：增加 C' 提升精度，但在 C'≈5 后趋于饱和，但会显著降低吞吐量（因 Rerank 开销增大）。
  - **稀疏预算 p'**：即使 p'=512（仅 0.39% 的上下文），CTKvR 仍能接近 FULLKV 精度。

---

## 4. 关键结论和发现

### 主要发现
1. **相邻查询高度相似**：RoPE 编码后，位置相近的查询向量具有高余弦相似性，且其 top-k 检索的 Key 集合高度重叠。这是 CTKvR 设计的基础。
2. **两阶段检索有效平衡效率与精度**：先用轻量级中心点索引缩小搜索范围，再进行精细排序，实现了高效且准确的 KV 检索。
3. **CPU-GPU 协同可行且高效**：现代 CPU 的算力和带宽足以承担部分注意力计算，结合定制优化，可大幅提升系统吞吐。
4. **CTKvR 具有强扩展性**：在极端长上下文（1M tokens）下依然保持高性能，验证了其在实际应用中的潜力。

### 方法的局限性
- **依赖 RoPE**：方法的有效性建立在 RoPE 导致相邻查询相似的基础上，对其他位置编码方式的普适性有待验证。
- **CPU 成为瓶颈**：尽管优化了 CPU 计算，但 Q2K Rerank 和 CPU 注意力仍是主要耗时环节，进一步优化 CPU 端仍有空间。
- **动态更新机制复杂性**：DCU 虽然有效，但增加了系统复杂性。

### 未来工作方向
- 探索更高效的 CPU 端计算优化，如量化、稀疏化。
- 将 CTKvR 思想应用于其他注意力变体或模型架构。
- 研究更智能的中心点选择和更新策略，减少对 FIFO 队列的依赖。
- 探索在训练阶段集成 CTKvR 以进一步优化性能。

</details>

---

### 9. [FlowBind: Efficient Any-to-Any Generation with Bidirectional Flows](https://arxiv.org/abs/2512.15420)

**Authors**: Yeonwoo Cha, Semin Kim, Jinhyeon Kwon, Seunghoon Hong  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.15420v1  

#### Abstract
Any-to-any generation seeks to translate between arbitrary subsets of modalities, enabling flexible cross-modal synthesis. Despite recent success, existing flow-based approaches are challenged by their inefficiency, as they require large-scale datasets often with restrictive pairing constraints, inc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# FlowBind: Efficient Any-to-Any Generation with Bidirectional Flows — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **flow-based any-to-any generation** 方法面临三大挑战：
- **数据依赖性强**：需要大量完全配对的多模态数据（如文本-图像-音频三元组），这类数据稀缺且昂贵。
- **计算成本高**：建模联合分布导致计算复杂度随模态数平方增长，训练效率低。
- **训练流程复杂**：普遍采用多阶段训练（如先对齐再生成），难以端到端优化。

### 提出的新方法：FlowBind
FlowBind 是一种简洁高效的 flow-based 框架，用于实现任意子集之间的跨模态生成（any-to-any generation）。其核心思想是：
- 引入一个**可学习的共享潜在空间（learnable shared latent）**，作为所有模态之间信息交互的“桥梁”。
- 每个模态通过一个**可逆的、模态特定的流模型（invertible flow）** 连接到该共享潜在空间。
- 所有组件在**单一 flow matching 目标函数下联合训练**，无需多阶段流程。

### 创新点与优势
| 特性 | FlowBind | 传统方法（如 CoDi, OmniFlow） |
|------|---------|-----------------------------|
| **训练方式** | 单一目标、端到端联合训练 | 多阶段训练（如对齐 + 生成） |
| **数据要求** | 支持任意部分配对数据（partial pairing） | 需要全配对或锚定模态（如必须含文本） |
| **计算效率** | 参数更少、训练更快（~10× speedup） | 高维联合建模，计算开销大 |
| **灵活性** | 真正支持任意输入输出组合（any-to-any） | 受限于固定路径或锚定设计 |

> ✅ **核心优势总结**：**更简单、更高效、更灵活**，同时保持甚至超越现有方法的生成质量。

---

## 2. 核心实验方法和设置

### 使用的数据集
FlowBind 在三个主流模态（text, image, audio）上进行训练和评估，使用以下配对数据集：
- **Text-Image**: LAION-COCO (242K), Flickr-30k (30K)
- **Text-Audio**: AudioCaps v2 (91K)
- **Audio-Image**: VGGSound (184K)

> ⚠️ 注意：**未使用任何三元组（triplet）数据**，仅依赖双模态配对数据即可完成 any-to-any 学习。

### 实验设置
- **编码器冻结**：使用预训练强编码器提取语义特征（避免从原始数据学习）：
  - 文本：EmbeddingGemma
  - 图像：CLIP + Stable-UnCLIP 作为解码器
  - 音频：CLAP
- **流模型架构**：MLP-based drift networks with AdaLN-zero 时间调制
- **共享潜在空间维度**：统一为 768 维
- **训练策略**：混合采样时间 $ t \sim (1-\alpha)\text{Unif}(0,1) + \alpha\delta(t=0) $，以稳定 encoder 训练

### 评估指标

#### 生成质量（Fidelity）
- 图像：**FID**
- 音频：**FAD**
- 文本：**CIDEr**

#### 跨模态对齐（Alignment）
- 文本-图像：**CLIP Score**
- 文本-音频：**CLAP Score**
- 图像-音频：**AIS (Audio-Image Similarity)**

#### 多对多任务额外指标
- 自构合成三元组数据集（基于 AudioCaps + FLUX.1 生成图像），用于 many-to-one 和 one-to-many 定量评估。

### 基线方法对比
- **Generalist Models**:
  - **CoDi** (Tang et al., 2023): 基于文本锚定的两阶段框架
  - **OmniFlow** (Li et al., 2025b): 建模联合条件流，需全配对数据
- **Specialist Models**:
  - SD3-Medium, FLUX.1 (text→image)
  - LLaVA-NeXT (image→text)
  - TangoFlux, AudioX (text→audio)
  - Qwen2-Audio (audio→text)
  - Seeing & Hearing, Sound2Vision (cross-modal)

---

## 3. 主要实验结果和性能指标

### 3.1 一对一代换性能（One-to-One Generation）

#### 表格：生成质量对比（越低越好 / 越高越好）

| Task | Model | FID↓ | CIDEr↑ | FAD↓ | ... |
|------|-------|------|--------|------|-----|
| T→I | FlowBind | **17.39** | – | – | |
| I→T | FlowBind | – | **46.26** | – | |
| T→A | FlowBind | – | – | **4.19** | |
| A→T | FlowBind | – | **55.11** | – | |
| I→A | FlowBind | – | – | **2.50** | |
| A→I | FlowBind | **26.60** | – | – | |

> ✅ FlowBind 在**所有六项一对一代换任务中均取得最佳生成质量**。

#### 对齐性能（越高越好）

| Task | Model | CLIP↑ | CLAP↑ | AIS↑ |
|------|-------|-------|-------|------|
| T→I | FlowBind | 28.35 | – | – |
| I→T | FlowBind | 29.74 | – | – |
| T→A | FlowBind | – | 29.08 | – |
| A→T | FlowBind | – | 36.70 | – |
| I→A | FlowBind | – | – | **82.89** |
| A→I | FlowBind | – | – | **78.17** |

> ✅ 在四项任务中对齐得分最优，尤其在 **image-audio 互生任务上显著领先**（AIS > 80 vs. < 75 for others）。

---

### 3.2 多对多生成能力（Many-to-Many Generation）

#### Many-to-One 结果（表4）
| Condition → Output | Metric | CoDi | OmniFlow | **FlowBind** |
|--------------------|--------|------|----------|------------|
| (I+A)→T | CLIP(I→T) | 24.04 | 26.38 | **27.83** |
| (T+A)→I | AIS(A→I) | 57.52 | 54.90 | **57.93** |
| (T+I)→A | CLAP(T→A) | 4.85 | 7.68 | **28.13** |

> ✅ FlowBind 显著提升多源条件下的对齐表现，尤其在 **(T+I)→A** 上远超基线，说明能有效融合多种输入。

#### One-to-Many 结果（表5）
| Input → (Outputs) | Metric | CoDi | OmniFlow | **FlowBind** |
|-------------------|--------|------|----------|------------|
| T→(I+A) | CLAP(T→A) | 10.99 | 12.92 | **29.12** |
| I→(T+A) | AIS(I→A) | 58.65 | 63.99 | **74.34** |
| A→(T+I) | CLAP(A→T) | 18.03 | 36.07 | **36.79** |

> ✅ FlowBind 在 one-to-many 设置下也表现出更强的跨模态控制能力和一致性。

---

### 3.3 效率与参数量对比（表1）

| Model | Train Params | GPU-hours | Data Size (relative) |
|-------|--------------|-----------|------------------------|
| CoDi | 4.3B | – | 100% |
| OmniFlow | 3.2B | 480 hr | 100% |
| **FlowBind** | **568M** | **48 hr** | **~1.8% of OmniFlow** |

> ✅ **参数减少约 6×，训练速度快 10×，数据需求仅为 1.79%**，极具实用性和扩展性。

---

### 3.4 消融实验与分析（关键发现）

#### （1）可学习共享潜空间 vs 固定文本锚
| Model | I→T | A→T | I→A |
|-------|-----|-----|-----|
| Text-anchoring (baseline) | 27.94 | 36.72 | 55.48 |
| FlowBind w/o I-A pairs | **30.04** | **37.04** | **61.88** |

> ✅ 即使不使用 image-audio 配对数据，FlowBind 的 learnable shared latent 仍优于 text-anchor 设计，验证了其泛化能力。

#### （2）共享潜空间的质量分析（CKNNA）
| Model | T-A Alignment | A-I Alignment |
|-------|---------------|---------------|
| Modality-specific latents | 0.1965 | 0.1343 |
| **Shared Latent (FlowBind)** | **0.2872** | **0.3026** |

> ✅ 共享潜空间具有更高的跨模态对齐度，证明其是一个真正意义上的“统一语义空间”。

#### （3）插值可视化（图3）
- 在共享潜空间中进行线性插值，解码后图像和文本呈现平滑过渡。
- 表明潜空间具备良好的语义连续性和结构化特性。

#### （4）冲突条件鲁棒性（图4）
- 当输入文本与音频语义冲突时（如“安静花园” + “电钻声”），FlowBind 能合理反映两者，而非忽略其一或产生混乱输出。
- 归因于共享潜空间的良好几何结构和平均聚合机制的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **可学习共享潜空间是高效 any-to-any 生成的关键**：
   - 替代了传统的“文本锚定”或“联合流”，实现了真正的模态对称性。
   - 通过最小化 conditional variance 来驱动 encoder 学习有意义的共性表示。

2. **单一 flow matching 目标足以实现端到端训练**：
   - 无需复杂的多阶段流程或多损失项（如 contrastive loss）。
   - 简化了训练，提升了稳定性。

3. **高阶语义空间建模大幅提升效率**：
   - 冻结强大编码器 + 在 compact latent space 中建模 flow，大幅降低数据和计算需求。
   - 分离了 intra-modal 生成与 inter-modal 对齐任务。

4. **FlowBind 具备出色的扩展性**：
   - 成功扩展至 **3D point clouds**（Pix3D 数据集），仅增加一个 drift network。
   - 实现了未见过的跨模态任务（如 text → point cloud），展示了强大的零样本迁移能力。

---

### 局限性
- 当前依赖高质量预训练 encoder（如 CLIP, CLAP），若某模态缺乏此类模型则受限。
- 推理过程依赖 ODE 积分，可能存在速度瓶颈（尽管比训练快得多）。
- 尚未在大规模语言模型（LLM）主导的 any-to-any 范式中直接比较（如 UnifiedIO2-L）。

---

### 未来工作方向
- 探索将 FlowBind 与 LLM 架构结合，构建 hybrid 多模态生成系统。
- 扩展到更多模态（视频、动作、机器人等）并研究动态模态增删机制。
- 加速推理：探索蒸馏或 lookup table 方法替代 ODE solver。
- 更深入研究共享潜空间的解耦与可控性。

---

## 总结
FlowBind 提出了一种**简洁、高效、灵活**的 any-to-any 生成范式，通过引入 **learnable shared latent + bidirectional invertible flows**，解决了现有 flow-based 方法在数据、计算和训练复杂性上的瓶颈。实验证明其在生成质量和跨模态对齐方面达到 SOTA 水平，同时**参数减少 6×，训练快 10×，数据需求极低**，为通用多模态生成提供了极具前景的新路径。

</details>

---

### 10. [Dual-Density Inference for Efficient Language Model Reasoning](https://arxiv.org/abs/2512.15358)

**Authors**: Zhengyi Zhao, Shubo Zhang, Yuxi Zhang, Huimin Wang, Binyang Li, Kam-Fai Wong  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.15358v1  

#### Abstract
Large Language Models (LLMs) have shown impressive capabilities in complex reasoning tasks. However, current approaches employ uniform language density for both intermediate reasoning and final answers, leading to computational inefficiency. Our observation found that reasoning process serves a comp...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Dual-Density Inference for Efficient Language Model Reasoning

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在复杂推理任务中普遍采用 **Chain-of-Thought (CoT)** 范式，即在整个推理过程中（从中间计算到最终答案）都使用冗长的自然语言（Natural Language, NL）。这种方法存在显著的**计算效率低下**问题。

作者指出，这种统一的表示方式造成了一个根本性的“表征错配”（representational mismatch）：
- **内部推理（Internal Reasoning）** 是一种**计算功能**，需要高信息密度、精确且紧凑的符号化表达。
- **最终回答（Final Answer）** 是一种**沟通功能**，需要人类可读、解释性强的自然语言。

使用冗长的自然语言进行内部计算，会消耗大量不必要的计算资源（token），尤其是在处理复杂的多步推理问题时。

### 提出的新方法：Denser (Dual-density Inference)
为解决上述问题，论文提出了 **Denser**，一种新颖的**双密度推理框架**（Dual-density Inference）。其核心思想是将推理过程解耦，对不同阶段使用不同信息密度的语言：

1.  **高密度推理（High-density Reasoning）**：在中间计算阶段，模型使用**压缩的、富含符号的语言**（如数学公式、逻辑符号、伪代码）进行高效计算。这极大地减少了token消耗。
2.  **低密度回答（Low-density Answering）**：在最终输出阶段，系统将压缩的推理路径**翻译并扩展**成流畅、易懂的自然语言解释，以满足人类用户的理解需求。

该框架由三个核心组件构成：
- **查询分析模块（Query Analysis Module）**：识别输入问题的领域（数学、逻辑、编码等）和复杂度，以指导后续的推理策略。
- **高密度推理模块（High-density Reasoning Module）**：执行核心的、符号化的计算步骤。
- **低密度回答生成模块（Low-density Answering Module）**：将符号化的推理结果转化为人类可读的答案。

### 相比现有方法的优势
- **显著提升效率**：通过压缩中间推理，大幅减少token消耗，从而降低计算成本和延迟。
- **保持甚至提升准确性**：避免了在冗长文本中丢失关键信息的风险，符号化表达更精确，有助于提高推理的准确性。
- **理论基础扎实**：明确区分了语言的“计算功能”和“沟通功能”，为优化推理提供了新的理论视角。
- **通用性强**：框架设计灵活，可根据不同领域（数学、逻辑、编程）自适应地选择最优的压缩策略。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验涵盖了四个核心推理领域，共八个基准数据集：
- **数学（Math）**: GSM8K, MATH500
- **逻辑（Logic）**: LogiQA, ProofWriter
- **编码（Code）**: MBPP, HumanEval
- **通用问答（General QA）**: MMLU, StrategyQA

### 实验设置和评估指标
- **主干模型**：主要使用 **Qwen3-14B** 进行实验，并在多个API模型（如 GPT-4o, o1-mini, DeepSeek-V3, Claude-3.7-Sonnet）上进行了验证。
- **评估指标**：
  - **性能准确率（Performance Accuracy）**：各领域的标准准确率指标。
  - **Token消耗（Token Cost）**：生成的总token数，用于衡量效率。
  - **计算延迟（Computational Latency）**：每道题目的平均推理时间（秒）。
  - **推理效率指数（Reasoning Efficiency Index, REI）**：综合衡量准确率与token消耗的权衡。

### 基线方法对比
实验对比了两大类基线方法：
1.  **Test-Time Scaling (TTS) 方法**：这些方法通常通过增加token来提升准确率。
    - Chain-of-Thought (CoT)
    - Self-Consistency (SC)
    - Tree-of-Thought (ToT)
    - Think-to-Think (T)
    - Reflection-CoT
    - Process Supervision
    - Self-Verification (SV)
2.  **推理压缩基线**：旨在减少token消耗的方法。
    - BeConcise (提示简洁)
    - OnlyNumbers (仅输出数字和符号)
    - AbbreWords (使用缩写)
    - TokenSkip (一种先进的token剪枝技术)

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在 **Qwen3-14B** 模型上的主要结果如下（见Table 4）：

| 方法 | 平均准确率 | Token消耗 (%) |
| :--- | :--- | :--- |
| **Denser (Ours)** | **81.5%** | **-58.7%** |
| Self-Verification (最佳TTS基线) | 79.2% | +142.8% |
| Chain-of-Thought (CoT) | 77.3% | 0.0% |

- **准确率**：Denser 在所有领域均优于所有基线方法，平均准确率比最强的TTS基线（Self-Verification）高出 **1.8%**。
- **Token消耗**：相比标准的CoT，Denser 将token消耗**降低了58.7%**，而其他TTS方法则增加了127%-345%的token消耗。

### 与基线方法的对比结果
- **vs. TTS方法**：Denser 在**大幅提升效率**的同时，还实现了**更高的准确率**。例如，在GSM8K上，Denser 准确率为88.2%，而Self-Verification为87.0%，但后者token消耗是前者的近6倍。
- **vs. 压缩基线**：Denser 的优势在于**保全了计算完整性**。像 `OnlyNumbers` 这样的方法虽然token减少最多（-66.2%），但准确率大幅下降。Denser 在实现显著压缩的同时，准确率不降反升。

### 消融实验结果（Ablation Study）
消融实验（见Table 6）验证了各组件的重要性：
- **移除高密度推理模块**：导致准确率**下降3.6%**，token减少仅8%，证明这是核心创新。
- **移除查询分析模块**：准确率下降1.3%，表明自适应领域分析至关重要。
- **移除低密度回答模块**：准确率影响较小（-0.7%），但会牺牲人类可读性。
- **使用固定密度策略**：准确率下降2.4%，证明了**领域自适应**的必要性。

---

## 4. 关键结论和发现

### 主要发现
1.  **信息密度错配是效率瓶颈**：论文证实了在推理中统一使用自然语言是一种低效的设计。分离计算和沟通功能可以带来巨大收益。
2.  **Denser 显著提升效率**：在多个基准上，Denser 将token消耗**最高减少62%**，同时**保持或提高了准确率**。
3.  **效率增益随问题复杂度增加而放大**：对于复杂度高的问题，Denser 的token节省可达71%，远高于简单问题的38%。
4.  **在符号化领域优势最明显**：在数学、逻辑等拥有成熟符号系统的领域，Denser 的性能提升最为显著。

### 方法的局限性
- **引入额外开销**：查询分析和答案生成模块会带来一定的计算开销，可能不适合对延迟极度敏感的应用。
- **性能增益因任务而异**：在某些任务或模型架构上，性能提升可能不如在复杂推理任务上那么显著。
- **依赖于领域适配**：方法的有效性依赖于为不同领域设计合适的高密度表示（如数学符号、逻辑演算、伪代码）。

### 未来工作方向
- 探索更高效的领域自适应机制。
- 将该框架应用于更多非结构化或创造性任务，探索其边界。
- 研究如何将这种双密度思想集成到模型训练中，而不仅仅是作为推理时的提示工程技巧。

</details>

---

### 11. [How Does Fourier Analysis Network Work? A Mechanism Analysis and a New Dual-Activation Layer Proposal](https://arxiv.org/abs/2512.14873)

**Authors**: Sam Jeong, Hae Yong Kim  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.14873v1  

#### Abstract
Fourier Analysis Network (FAN) was recently proposed as a simple way to improve neural network performance by replacing part of ReLU activations with sine and cosine functions. Although several studies have reported small but consistent gains across tasks, the underlying mechanism behind these impro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*How Does Fourier Analysis Network Work? A Mechanism Analysis and a New Dual-Activation Layer Proposal*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **澄清 FAN（Fourier Analysis Network）机制的模糊性**：尽管已有研究观察到 FAN 在多种任务中能带来性能提升，但其背后的作用机制尚不明确。原始假设认为 sine 和 cosine 的周期性有助于捕捉信号中的周期模式，但该解释缺乏实证支持。
- **揭示 cosine 激活函数的负面影响**：此前未有人独立分析 sine 与 cosine 在 FAN 中各自的作用。

### 🚀 提出的新方法与新思路
- **Dual-Activation Layer (DAL)**：提出一种新的激活层设计，将 ReLU 类型激活函数（如 ReLU、Leaky ReLU、GELU、Swish）与具有非零导数且关于原点对称的平滑函数（如 sine 或 tanh）按比例混合。
  - 形式化表示为 `(f, g, a:b)`，其中：
    - `f` 是主激活函数（如 ReLU）
    - `g` 是辅助激活函数（如 sine）
    - `a:b` 是两者的比例
- **理论洞见驱动设计**：提出性能提升并非源于 sine 的周期性，而是其在 $x=0$ 附近的局部行为——特别是非零导数，能够缓解梯度消失问题。

### 🔍 相比现有方法的优势
- **更高效收敛**：相比标准 ReLU 及其变体（Leaky ReLU、GELU、Swish），DAL 显著加速训练初期的收敛速度。
- **减少“死亡神经元”现象**：通过引入稳定梯度路径，有效降低因输入长期为负而导致的“dead neuron”问题。
- **通用性强**：可与现代激活函数（如 GELU、Swish）结合使用，并兼容 Batch Normalization、残差连接等技术，具备协同增效潜力。
- **计算开销小**：仅替换部分激活函数，几乎不增加模型复杂度。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 类型 | 任务 |
|-------|------|-----|
| 合成 1D 信号 | 正弦噪声 vs 纯高斯噪声 | 二分类 |
| MNIST | 手写数字图像 | 多类分类（10类） |
| ECG-ID | 单导联心电图（ECG） | 生物特征识别（90人身份分类） |

### ⚙️ 实验设置
- **网络架构**：
  - 合成信号 & ECG-ID：1D CNN 架构
  - MNIST：2D CNN 架构
- **训练配置**：
  - 优化器：Adam
  - 损失函数：Binary Crossentropy（合成任务）、Categorical Crossentropy（MNIST、ECG-ID）
  - 初始化：相同随机种子确保可比性
  - 重复实验多次（N=50~100次）进行统计显著性检验
- **评估指标**：
  - 准确率（Accuracy）
  - 损失值（Loss）
  - 收敛速度（早期 epoch 表现）
  - “死亡神经元”比例（每层输出恒为0的比例）

### 🔁 基线方法对比
| 方法 | 描述 |
|------|------|
| ReLU-only | 标准基线 |
| FAN (ReLU + sin + cos, 6:1:1) | 原始 FAN 结构 |
| (ReLU, sin, 6:2) | 提出的 DAL 配置 |
| (ReLU, cos, 6:2) | 分离 cosine 效果 |
| (ReLU, tanh/TSine/linear, 6:2) | 验证周期性是否必要 |
| GELU / Leaky ReLU / Swish-only | 现代激活函数基线 |
| (GELU, sin, 7:1) 等 | DAL 与现代激活结合 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）合成 1D 信号分类（Noisy Sinusoid vs Noise）
| 模型 | 平均准确率（M=10） | p-value |
|------|------------------|--------|
| ReLU-only | 0.7826 | — |
| (ReLU, sin, cos, 6:1:1) | 0.8340 | 0.0026 |
| (ReLU, cos, 6:2) | 0.7608 | — |
| **(ReLU, sin, 6:2)** | **0.8153** | **0.0035** |

> ✅ 结论：只有 sine 起正向作用；cosine 导致性能下降。

当训练延长至 M=60 epochs：
- 最终准确率趋同（0.8579 vs 0.8572, p=0.9127）
- 但 **(ReLU, sin, 6:2) 收敛更快**

#### （2）MNIST 数字分类
| 模型 | 训练准确率（M=5） | 测试准确率（M=5） | p-value |
|------|------------------|------------------|--------|
| ReLU-only | 0.9939 | 0.9905 | — |
| **(ReLU, sin, 6:2)** | **0.9957** | **0.9910** | **<0.05** |

> ✅ 统计显著差异，表明 DAL 加速早期学习过程。

M=30 epochs 后两者最终性能接近，但 DAL 过拟合更轻。

#### （3）ECG-ID 生物识别（无 BatchNorm）
| 模型 | 测试准确率（M=50） | 测试准确率（M=500） |
|------|--------------------|----------------------|
| ReLU-only | 0.1580 | 0.8483 |
| **(ReLU, sin, 1:1)** | **0.3178** | **0.9372** |
| p-value | <0.0001 | <0.0001 |

> ✅ 不仅收敛快，且达到更高的最终性能！

#### （4）ECG-ID（含 BatchNorm）
| 模型 | 测试准确率（M=500） |
|------|----------------------|
| ReLU-only | 0.9054 |
| **(ReLU, sin, 1:1)** | **0.9355**, p=0.0000 |

> ✅ 即使加入 BN 缓解了死亡神经元问题，DAL 仍保持优势。

---

### 🔍 消融实验结果

| 实验目的 | 发现 |
|--------|------|
| 是否 cosine 有益？ | ❌ (ReLU, cos, 6:2) 性能低于 ReLU-only |
| 是否 sine 的周期性重要？ | ❌ 使用非周期函数（tanh、truncated sine、linear）效果相近，说明关键是局部形状而非周期性 |
| 是否只适用于 ReLU？ | ❌ 否！在 GELU、Leaky ReLU、Swish 上也观察到加速收敛 |
| 是否减少死亡神经元？ | ✅ 表格显示 DAL 层中死亡神经元比例显著更低（如 dense layer 从 36.95% → 16.80%） |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **FAN 的性能提升主要来自 sine，而非 cosine**：
   - cosine 激活倾向于损害模型性能。
2. **性能增益源于局部梯度特性，而非周期性假设**：
   - sine 在 $x=0$ 处有非零导数，提供稳定的梯度流，缓解“dying ReLU”问题。
3. **周期性不是关键因素**：
   - tanh、truncated sine、linear 等非周期函数也能取得类似甚至更好效果。
4. **DAL 是 FAN 的简化与强化版本**：
   - 移除 cosine 后性能反而提升，证明 cosine 是冗余甚至有害的。
5. **广泛适用性**：
   - 在合成信号、图像（MNIST）、生理信号（ECG）上均验证有效。
   - 可与现代激活函数和正则化技术（如 BN）共存并产生协同效应。

### ⚠️ 方法的局限性
- **最优比例依赖任务和架构**：如 MNIST 推荐 6:2，而 ECG-ID 更适合 1:1，需调参。
- **对简单任务可能仅加快收敛，不提升最终精度**：如 MNIST 和合成任务中，长时间训练后差距缩小。
- **sine 函数在极端输入下可能出现振荡行为**：虽然实验中未出现，但在极端分布下需注意稳定性。
- **硬件实现考虑**：sine 计算比 ReLU 稍复杂，但在现代框架中影响极小。

### 🔮 未来工作方向
1. **自动化比例搜索机制**：设计自适应调整 `a:b` 比例的方法。
2. **扩展到 Transformer 和 KAN 架构**：探索 DAL 在注意力机制或 Kolmogorov-Arnold 网络中的应用。
3. **与其他梯度增强技术联合建模**：研究 DAL 与残差连接、LayerNorm、优化器设计的深层交互。
4. **理论分析**：建立关于混合激活如何影响损失曲面和平滑性的数学模型。
5. **部署场景验证**：在边缘设备或实时系统中测试 DAL 的实际效率与鲁棒性。

---

## 总结

本文通过对 FAN 的深入机制分析，颠覆了“周期性有益”的直觉认知，揭示了 **sine 激活的价值在于其在原点附近良好的梯度性质**，从而提出了更简洁有效的 **Dual-Activation Layer (DAL)**。实验证明 DAL 能显著加速训练、减少死亡神经元，在多个真实任务中表现优于传统及现代激活函数组合，是一种轻量级、通用性强的训练加速组件。

</details>

---

### 12. [Accelerating High-Throughput Catalyst Screening by Direct Generation of Equilibrium Adsorption Structures](https://arxiv.org/abs/2512.15228)

**Authors**: Songze Huo, Xiao-Ming Cao  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.15228v1  

#### Abstract
The adsorption energy serves as a crucial descriptor for the large-scale screening of catalysts. Nevertheless, the limited distribution of training data for the extensively utilised machine learning interatomic potential (MLIP), predominantly sourced from near-equilibrium structures, results in unre...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerating High-Throughput Catalyst Screening by Direct Generation of Equilibrium Adsorption Structures

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在**异相催化反应**中，**吸附能**（adsorption energy）是衡量催化剂活性的关键描述符。然而，准确计算吸附能依赖于对吸附结构进行高精度的几何优化（geometry relaxation），传统上依赖**密度泛函理论**（DFT）计算，其时间复杂度为 $O(n^3)$，计算成本极高，严重制约了大规模催化剂的高通量筛选。

尽管已有基于**机器学习势函数**（MLIP）的方法尝试替代 DFT 进行结构优化，但这些模型通常需要覆盖整个**势能面**（PES）的大规模训练数据，且训练数据多集中于平衡态附近，导致对非平衡初始结构的优化轨迹不可靠，预测的吸附结构和能量不准确。

### 提出的新方法：DBCata
本文提出了一种名为 **DBCata**（Diffusion Bridge Model for Catalysts）的新型深度生成模型，其核心思想是：
- **直接建模从初始未弛豫结构到 DFT 弛豫结构的转换过程**，而非拟合整个 PES。
- 基于**周期性布朗桥框架**（periodic Brownian-bridge framework）和**等变图神经网络**（equivariant GNN, 如 PaiNN），构建一个低维过渡流形（low-dimensional transition manifold）。
- 在训练阶段，通过向初始与目标结构之间的插值路径添加噪声来模拟布朗桥过程；在生成阶段，通过确定性 ODE 采样快速生成高保真度的弛豫结构。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **无需能量/力标签** | DBCata 仅需成对的初始-弛豫结构数据，无需 DFT 计算的能量或原子力作为监督信号，显著降低数据准备成本。 |
| **高效性** | 单个结构生成时间 < 1 秒，比传统 DFT 快约 $10^4$ 倍，比当前最先进的 MLIP（如 UMA-OC20）快约 10 倍。 |
| **高精度** | 在 Catalysis-Hub 数据集上，**DMAE**（Distance Mean Absolute Error）仅为 **0.035 Å**，约为 UMA-OC20 的 1/3。 |
| **强泛化能力** | 对多种吸附物（H, C, O, OH, S 等）和不同晶面（fcc(111), fcc(110)）均表现稳定。 |
| **可集成异常检测** | 提出混合化学启发式与自监督异常检测机制，识别并修正异常预测，进一步提升可靠性。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Catalysis-Hub Dataset**：公开的催化数据库，包含约 80,000 条金属及合金表面的 DFT 弛豫轨迹（使用 BEEF-vdW 泛函）。
- 从中提取 **76,737 对**初始-弛豫结构，涵盖 12 类吸附物（H, C, CH, CH₂, CH₃, N, NH, O, OH, H₂O, S, SH）。
- 数据按 8:2 划分为训练集（58,943）和测试集（14,728）。

### 实验设置
- **模型架构**：基于 Brownian Bridge 的扩散生成模型，采用 **PaiNN** 作为等变 GNN 主干。
- **周期边界条件处理**：
  - 使用**多图方法**（multi-graph）搜索周期镜像中的邻居原子。
  - 提出**最近原子插值法**（nearest-atom interpolation）确保插值路径满足 PBC。
- **训练细节**：
  - 扩散步数 $T=100$，最大噪声方差 $\sigma_{\text{max}}=0.05$。
  - 隐藏维度 256，层数 4，学习率 $10^{-4}$，AdamW 优化器。
  - 使用单张 NVIDIA RTX 4090 GPU，训练耗时约 12 小时。

### 评估指标
| 指标 | 定义与用途 |
|------|-----------|
| **DMAE**（Distance Mean Absolute Error） | 衡量生成结构与 DFT 弛豫结构之间所有原子对距离的平均绝对误差，单位 Å。用于评估几何精度。 |
| **Success Rate @ 0.1 eV** | 吸附能预测误差 ≤ 0.1 eV 的样本占比，反映能量预测准确性。 |
| **Ionic Steps** | DFT 再优化所需的离子步数，衡量初始猜测质量。 |
| **Force/Energy RMSE** | MLIP 预测力与 DFT 力之间的均方根误差，评估 MLIP 在不同区域的可靠性。 |

### 基线方法对比
- **DFT Relaxation**：金标准，但极慢。
- **UMA-OC20**：当前最先进的预训练 MLIP 模型（来自 Open Catalyst Project），用于结构优化和能量评估。
- **FAIRChem**：另一主流 MLIP 框架。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | DMAE (Å) | Success Rate @ 0.1 eV | 单结构生成时间 | 速度提升倍数 |
|------|----------|------------------------|----------------|---------------|
| DFT Relaxation | / | / | ~850 s | 1x |
| UMA-OC20 | 0.1133 | 68% | ~9.8 s | 87x |
| **DBCata** | **0.0352** | **91%** | **~0.2 s** | **~4,000x** |
| DBCata + OD + DFT | 0.0281 | 94% | — | — |

> 注：DBCata + OD 表示结合异常检测后对异常样本进行 DFT 再优化。

### 与基线方法的对比结果
- **几何精度**：DBCata 的 DMAE 比 UMA-OC20 低近 **3 倍**（0.035 vs 0.113 Å）。
- **能量预测精度**：
  - DBCata 生成结构经 DFT 单点计算后，91% 的吸附能误差 < 0.1 eV，显著优于 UMA-OC20 的 68%。
  - 图 2 显示 DBCata 的吸附能预测与 DFT 结果具有更强的一致性（$R^2 = 0.983$ vs 0.877）。
- **再优化效率**：
  - 从初始结构直接 DFT 弛豫平均需 **65 步**；
  - 从 DBCata 生成结构开始仅需 **15 步**即可收敛（图 3a）；
  - 总体节省超过 80% 的 DFT 计算成本。

### 消融实验结果（见 Supplementary Table 4）
| 变体 | Fractional Coord | Gaussian Basis | Fourier Basis | DMAE (Å) |
|------|------------------|----------------|---------------|----------|
| Baseline | ✗ | ✓ | ✗ | 0.0409 |
| Baseline | ✓ | ✗ | ✓ | 0.0621 |
| **Baseline** | ✗ | ✗ | ✓ | **0.0353** |

> 结论：使用 Cartesian 坐标 + Fourier 特征编码（用于周期性位置嵌入）效果最佳。

### 异常检测有效性（图 3c & Table S5）
- 异常分类器输出的置信度与 DMAE 负相关：**低置信度 → 高 DMAE**。
- 分类器在测试集上的 F1-score 达 **0.92**（正类为正常结构），能有效识别异常。
- 经异常检测与再优化后，预测准确率从 91% 提升至 **94%**。

---

## 4. 关键结论和发现

### 主要发现
1. **DBCata 能高效且高精度地生成接近 DFT 平衡态的吸附结构**，其性能远超当前主流 MLIP 模型。
2. **生成结构已处于 PES 的近平衡区**，使得后续 MLIP 或 DFT 优化极为高效，避免陷入错误局部极小。
3. **结合异常检测机制可进一步提升鲁棒性**，实现“快速初筛 + 精细验证”的两阶段催化筛选流程。
4. **成功应用于 ORR 催化剂高通量筛选**：
   - 自动构建 1,760 种二元合金体系，生成 15,911 个初始吸附构型。
   - DBCata 在 **5 分钟内完成全部结构弛豫**。
   - 发现多个有潜力的候选材料，如 **AgAu(110)** 在碱性条件下可能表现出优异 ORR 活性。

### 方法的局限性
- **依赖高质量的初始-弛豫配对数据**：要求输入初始结构遵循固定规则生成（如 Catalysis-Hub 流程），限制了对任意复杂初始构型的泛化能力。
- **对极端偏离训练分布的结构可能失效**：虽然异常检测可缓解，但仍需人工干预或 DFT 验证。
- **目前仅适用于表面吸附系统**，尚未扩展至更复杂的多相催化或溶液环境。

### 未来工作方向
- 探索将**随机最优控制**（stochastic optimal control）与 diffusion bridge 结合，减少对成对数据的依赖。
- 学习直接从 PES 梯度中推断吸附过程，避免依赖 DFT 弛豫生成训练数据。
- 扩展至动态吸附、反应路径探索等更复杂的催化场景。
- 构建端到端的自动化催化剂设计平台，集成结构生成、能量评估、稳定性分析与实验验证闭环。

---

> **总结**：DBCata 开辟了一条**绕过完整 PES 建模**、直接学习“初始→弛豫”映射的新范式，显著加速了催化剂的高通量计算筛选，为下一代智能催化材料设计提供了强有力的工具。

</details>

---

### 13. [Metanetworks as Regulatory Operators: Learning to Edit for Requirement Compliance](https://arxiv.org/abs/2512.15469)

**Authors**: Ioannis Kalogeropoulos, Giorgos Bouritsas, Yannis Panagakis  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.15469v1  

#### Abstract
As machine learning models are increasingly deployed in high-stakes settings, e.g. as decision support systems in various societal sectors or in critical infrastructure, designers and auditors are facing the need to ensure that models satisfy a wider variety of requirements (e.g. compliance with reg...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Metanetworks as Regulatory Operators: Learning to Edit for Requirement Compliance*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
随着机器学习模型在高风险场景（如医疗、金融、司法）中的广泛应用，确保其满足**多样化要求**（如合规性、公平性、隐私保护、计算效率）变得至关重要。然而，传统方法面临以下挑战：
- **Post-processing** 方法常严重损害模型性能；
- **Fine-tuning 或重新训练** 成本高昂，且在数据不可用或受知识产权限制时不可行；
- 新规或需求往往在模型部署后才出现，导致“训练-需求”时间错配。

因此，论文提出并解决了一个核心问题：  
> **能否高效地编辑已训练的神经网络以满足新要求，同时不牺牲其原始功能？**

### 提出的新方法与创新思路
本文提出了一个**统一的、可学习的神经网络编辑框架**，其核心创新如下：

- **统一的多目标优化框架**：将各种要求（如公平性、数据最小化、权重剪枝）形式化为数学目标函数，并与“功能保留”目标联合优化，构成一个多目标问题。
- **基于 Metanetwork 的可学习编辑器**：引入一个图结构的 **metanetwork**（一种专门用于处理神经网络参数的 NN），通过监督学习从大量 NN 参数分布中学习如何直接映射原始参数到合规的编辑后参数。
- **单步推理实现编辑**：一旦 metanetwork 训练完成，对任意同任务分布下的模型进行合规编辑仅需一次前向传播（single inference step），极大提升效率。

### 相比现有方法的优势
| 维度 | 传统方法（Post-processing / Retraining） | 本文方法（Metanetwork-based Editing） |
|------|----------------------------------------|----------------------------------------|
| **效率** | 高延迟，需多次迭代优化或训练 | 极快，单次推理即可完成编辑（毫秒级） |
| **通用性** | 多为任务/要求特定（requirement-specific） | 统一框架支持多种要求（data minimization, fairness, pruning） |
| **性能保留** | 常显著降低原始性能 | 显著更好地平衡“合规性”与“功能保留” |
| **资源依赖** | 通常需要原始训练数据或完整再训练 | 可使用少量外部数据训练 metanetwork，无需访问原始训练数据 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **UCI Adult** 和 **Bank Marketing** 数据集，均为真实世界表格数据（tabular data）。
- 所有实验均基于 **MLP**（多层感知机）模型。

### 实验设置
- **构建 NN Population（Dm）**：通过采样不同架构（层数、隐藏维度）、超参数（学习率、dropout、激活函数等）训练约 12,000 个 MLP 模型，形成训练 metanetwork 所需的模型集合。
- **数据分布分离**：`pm` 表示模型参数分布，`pd` 表示输入数据分布；训练 metanetwork 时使用的 `pd` 数据独立于原始模型训练数据，模拟审计方无原始数据的情境。
- **Metanetwork 架构**：采用 **Graph Metanetwork**（Lim et al., 2024），将 MLP 视为图结构（节点=神经元，边=权重），利用 GNN 处理参数图，并保证对参数置换对称性（permutation equivariance）。

### 评估指标
所有实验均绘制 **Pareto Front（帕累托前沿）** 曲线，衡量两个目标之间的权衡：
- **Y轴**：Jensen-Shannon Divergence（JSD），衡量编辑前后模型输出分布的差异（越小越好，表示功能保留更强）；
- **X轴**：
  - *Data Minimization*：活跃输入特征比例；
  - *Fairness*：Equalized Odds Difference (EOD)，越小越公平；
  - *Pruning*：剩余权重比例（即稀疏度）。

### 基线方法对比
| 任务 | 基线方法 |
|------|---------|
| **Data Minimization** | Feature Selection (FS), FS + Retrain (知识蒸馏), FS (GMN)（仅预测mask） |
| **Fairness (Bias Mitigation)** | ThresholdOpt, RejectOption, CalEqOdds, FairCls |
| **Pruning** | Random, Magnitude, Gradient Importance, SNIP, Lottery Ticket |

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### ✅ **Data Minimization（数据最小化）**
- 在 Adult 和 Bank 数据集上，**GMN** 方法在相同活跃特征比例下，JSD 显著低于所有基线（见 Fig. 1 & 6）。
- 尤其在高掩码比例（即保留很少特征）时优势更明显，说明 metanetwork 能有效补偿信息损失。
- **FS (GMN)**（仅预测 mask，不编辑参数）表现略差于完整 GMN，表明参数编辑对功能保持至关重要。

#### ✅ **Bias Mitigation（偏见缓解）**
- GMN 在整个 Pareto 前沿上全面优于所有 post-processing 基线（Fig. 2）。
- 在相同 EOD 水平下，GMN 的 JSD 更低（即对原始性能影响更小）；
- 基线如 ThresholdOpt 和 CalEqOdds 只能产生单一操作点，而 GMN 可生成连续的权衡曲线，提供更大灵活性。

#### ✅ **Weight Pruning（权重剪枝）**
- **GMN-Prune & Edit** 在所有稀疏度水平下均优于 Random、Magnitude、Gradient Importance、SNIP 和 Lottery Ticket（Fig. 3）。
- 特别是在高稀疏度（<20% 权重保留）时，编辑剩余参数的效果尤为显著。

#### ⏱️ **时间效率对比（Table 1）**
| 方法 | 编辑单个模型耗时（秒） |
|------|------------------|
| **GMN (all variants)** | **0.03** |
| FS & Retrain | 32.35 |
| RejectOption | 4.36 |
| SNIP | 0.41 |
| Random/Magnitude | ~0.003 |

> 尽管某些简单剪枝方法更快，但性能远不如 GMN。**GMN 在性能与速度之间实现了最佳平衡**。

#### 🔍 **消融实验：训练样本效率（Fig. 4）**
- 即使只使用 **10% 的训练数据**（即 1,200 个模型）训练 metanetwork，其性能仍优于大多数基线；
- 使用 **25% 数据** 时，已全面超越所有基线；
- 50% 以上数据性能趋于饱和，表明 metanetwork 具有很强的**样本效率**和泛化能力。

#### 🔄 **组合多个 metanetwork（Fig. 8）**
- 将分别训练用于 **Data Minimization** 和 **Pruning** 的 metanetwork 进行串联（$ \mathcal{S}_{pr} \circ \mathcal{S}_{dm} $），仍能有效工作，且性能优于基线组合（FS & Retrain + SNIP）。
- 表明 metanetwork 编辑不会破坏参数分布，支持模块化组合。

---

## 4. 关键结论和发现

### 主要发现
1. **Metanetwork 可作为高效的“监管操作符”**：能够以单步推理方式对已部署模型进行合规性编辑，适用于数据最小化、公平性和剪枝等多种要求。
2. **显著优于传统方法**：在性能保留与要求满足之间的权衡上，全面超越 post-processing 和轻量 retraining 方法。
3. **高度高效与实用**：编辑过程仅需毫秒级时间，适合实时或大规模模型治理场景。
4. **具备良好泛化与组合能力**：即使在小样本训练下也能取得优异效果，且多个 metanetwork 可堆叠使用。

### 方法的局限性
- 当前方法局限于**同一任务下的模型族**（same task distribution），尚不能跨任务通用；
- 实验仅在 **MLP** 上验证，复杂架构（如 CNN、Transformer）尚未测试；
- 需要预先收集一批训练好的模型用于训练 metanetwork，在某些场景下可能难以获取；
- 完全自动化编辑存在风险，**人类监督仍是必要环节**（作者强调需领域专家评估权衡）。

### 未来工作方向
- 扩展至更复杂的网络架构（CNN、Transformer）；
- 探索跨任务的通用 metanetwork（multi-task learnable editing）；
- 结合更多类型的合规要求（如鲁棒性、可解释性）；
- 发展标准化的 metanetwork 工具包，用于 AI 审计与监管实践；
- 探索 metanetwork 在联邦学习、模型版权管理等场景的应用。

---

> **总结**：该论文开创性地将 **weight space learning** 与 **AI 合规治理**相结合，提出了一种高效、灵活、可扩展的模型编辑范式。它不仅解决了“事后合规”的现实难题，也为未来构建自动化的 AI 监管基础设施提供了理论基础和技术路径。

</details>

---

### 14. [Multi-Modal Semantic Communication](https://arxiv.org/abs/2512.15691)

**Authors**: Matin Mortaheb, Erciyes Karakaya, Sennur Ulukus  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.15691v1  

#### Abstract
Semantic communication aims to transmit information most relevant to a task rather than raw data, offering significant gains in communication efficiency for applications such as telepresence, augmented reality, and remote sensing. Recent transformer-based approaches have used self-attention maps to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Multi-Modal Semantic Communication

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于视觉自注意力机制（self-attention）的语义通信方法在复杂场景中存在以下局限：
- 在多物体、背景杂乱或语义模糊的图像中，**自注意力图容易扩散或聚焦于视觉显著但语义无关的区域**；
- 缺乏对具体任务目标的显式引导，导致无法精准提取与用户查询相关的语义内容；
- 当通信任务改变时需重新训练整个模型，**灵活性差且计算成本高**。

### 提出的新方法与创新思路
本文提出了一种**Multi-Modal Semantic Communication (MMSC)** 框架，其核心创新如下：

- **引入文本查询作为任务指导信号**：利用用户提供的自然语言指令（如“Where is the crosswalk?”），实现任务驱动的信息选择。
- **跨模态注意力机制（Cross-Modal Attention）**：融合图像特征与文本嵌入（text embedding），通过预训练的视觉-语言模型（如CLIP）生成软相关性得分（soft relevance scores），量化每个图像块与查询之间的语义匹配程度。
- **动态适应性编码策略**：基于信道带宽和语义重要性分数，将图像块分配到不同分辨率等级进行编码传输，确保关键区域以更高保真度重建。
- **无需重训练即可支持任意查询**：系统可在推理阶段直接处理自由形式的自然语言查询，无需为每个新任务微调模型。

### 相比现有方法的优势
| 维度 | 本文 MMSC | 基线方法（如 ViT-SC [6]） |
|------|-----------|--------------------------|
| 输入模态 | 图像 + 文本查询（双模态） | 仅图像（单模态） |
| 注意力机制 | Cross-Attention（图文交互） | Self-Attention（仅视觉内部） |
| 查询灵活性 | 支持开放词汇、复合对象查询（如“cat and keyboard”） | 固定类别标签或依赖预定义任务 |
| 决策方式 | 软相关性评分（soft scores），支持渐进式压缩 | 依赖视觉显著性，缺乏语义对齐 |
| 可扩展性 | 动态适配新任务，无需再训练 | 更换任务需重新优化编码器 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **COCO validation set** 的一个子集，共 **200 张图像**；
- 所有图像统一调整为 **320×480 分辨率**；
- 构造两类用户查询：
  1. 单一对象查询（如 “dog”）
  2. 复合对象查询（如 “person and bicycle”）
- 提供对应对象的 **ground truth segmentation masks** 用于评估语义一致性。

### 实验设置
- **Patch 划分**：图像划分为 8×8 像素的小块，每张图共 2,400 个 patch；
- **分辨率层级**：采用五级编码方案，对应码率分别为：
  - $ L = 0 $: 不传输（0 字节）
  - $ L = 12, 24, 48 $: 压缩编码
  - $ L = 192 $: 原始 patch 无损传输（3×8×8=192 字节）
- **总带宽范围**：从 0 到 $ 2400 \times 192 = 460,800 $ 字节，模拟不同信道条件。

### 评估指标
1. **Masked MSE**：在 ground truth 掩码区域内计算原始图像与重建图像的均方误差，衡量语义关键区域的保真度；
2. **Informativeness Score Consistency (L1 Distance)**：比较原始图像与重建图像在相同查询下的 $ S_{\text{inf}} $ 图之间的 L1 距离，反映语义结构保持能力；
3. **CLIP Relevancy Score**：使用 CLIP 模型计算重建图像与输入文本之间的相似度得分，评估图文语义对齐质量。

### 基线方法对比
- **ViT-SC [6]**：基于 Vision Transformer 自注意力机制的语义通信系统，仅使用图像输入，不接受文本查询；
- 两种方法共享相同的 encoder-decoder 结构和多分辨率控制模块，保证公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### （1）Masked MSE 对比（图4）
- 在所有带宽水平下，**MMSC 的 masked MSE 显著低于 ViT-SC**；
- 例如，在约 200,000 Bytes 信道速率下：
  - MMSC: ~0.015
  - ViT-SC: ~0.030
- 表明 MMSC 更好地保留了与查询相关的语义内容。

> ✅ **优势体现**：引入文本引导使系统能准确识别并优先重建目标对象（如键盘），而不仅仅是视觉最显著的对象（如猫）。

#### （2）Informativeness Score L1 Distance（图5）
- MMSC 的 $ S_{\text{inf}} $ 差异更小，说明其重建后的图像仍高度响应原始查询；
- 在低至中等带宽条件下，性能差距尤为明显；
- 表明该框架有效维持了“什么是重要的”这一语义判断的一致性。

#### （3）CLIP Relevancy Score（图6）
- MMSC 在中等带宽区间（~100K–300K Bytes）达到最高 CLIP 得分（接近 0.20）；
- 随着带宽增加，ViT-SC 得分趋于饱和甚至略降，而 MMSC 下降较缓；
- 原因分析：高带宽时非相关区域也被重建，稀释了语义焦点；但 MMSC 因有查询引导，仍能较好集中资源。

> 📌 典型案例见 Fig. 3：“Cat and Keyboard” 查询中，MMSC 成功重建两个对象，而 ViT-SC 忽略了键盘。

### 消融实验（文中未明确列出，但从设计可推断）
虽然论文未提供正式消融研究，但从架构设计可得出以下隐含结论：
- 若移除 CDT（Content-Dependent Transfer）模块 → 文本嵌入与图像上下文对齐减弱 → 相关性评分不准；
- 若使用硬掩码而非软评分 → 丢失细粒度语义强度信息，不利于多级压缩；
- 若冻结 CLIP-V 和 CLIP-T 不更新 → 性能下降，尤其在 mask quality alignment 上表现不佳。

---

## 4. 关键结论和发现

### 主要发现
1. **文本查询显著提升语义通信的目标导向性**：相比纯视觉自注意力机制，引入自然语言指令能更精确地定位任务相关区域；
2. **跨模态注意力优于单模态注意力**：CLIP-based cross-attention 能有效融合图文信息，生成更具语义意义的相关性地图；
3. **软相关性评分支持精细化资源分配**：连续值评分比二值分割更适合多分辨率编码，实现带宽高效利用；
4. **无需重训练即可应对多样化任务**：系统具备良好的泛化能力和实时适应性，适合多用途通信场景。

### 方法的局限性
- **依赖高质量预训练模型**：性能严重依赖 CLIP 等大规模视觉-语言模型，部署成本较高；
- **Patch-wise 编码可能引入边界伪影**：独立编解码各 patch 可能在拼接处产生不连续现象；
- **未考虑时间维度**：当前框架针对静态图像，尚未扩展至视频流中的时序一致性建模；
- **查询理解受限于 CLIP 的语义覆盖范围**：极端抽象或罕见表达可能无法正确解析。

### 未来工作方向
- 将框架扩展至 **video semantic communication**，结合时空注意力机制；
- 引入 **end-to-end joint optimization**，联合训练语义提取与编码模块；
- 探索 **low-bitrate quantization** 与 **neural codec integration**，进一步降低传输开销；
- 支持 **interactive feedback loops**，允许接收端反馈任务完成状态以动态调整后续传输策略。

---

> 🔚 **总结**：本文提出的 MMSC 框架开创性地将文本查询引入语义通信系统，实现了真正意义上的“按需传输”。其实验证明，在复杂场景和有限带宽下，该方法显著优于仅依赖视觉自注意力的传统方案，为下一代智能通信系统提供了可行的技术路径。

</details>

---

### 15. [RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees](https://arxiv.org/abs/2512.14069)

**Authors**: Junjie Ma, Jinlong Li  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.14069v1  

#### Abstract
Inference with modern Large Language Models (LLMs) is expensive and slow, and speculative sampling has emerged as an effective solution to this problem, however, the number of the calls to the draft model for generating candidate tokens in speculative sampling is a preset hyperparameter, lacking fle...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代 Large Language Models (LLMs) 推理过程昂贵且缓慢。**Speculative Sampling** 是一种有效的加速方法，通过一个小的 draft model 快速生成候选 token，并由目标 LLM 并行验证。然而，现有方法中对 draft model 的调用次数是一个**预设超参数**（如固定为8次），缺乏灵活性，导致在某些上下文中产生冗余计算（例如，即使后续 token 被频繁拒绝，仍强制执行多次前向传播）。

### 🚀 提出的新方法：RADAR
作者提出 **RADAR**（Reinforcement learning Adjusted Draft-generation Algorithm for speculative sampling），其核心创新如下：

- **动态生成 draft tree**：不同于传统静态或固定深度的 draft tree 结构，RADAR 能够根据当前上下文**实时决定是否继续调用 draft model**，从而构建**动态深度的 draft tree**。
- **将 draft tree 构建建模为 MDP**：将每一步是否继续生成候选 token 视为一个决策问题，形式化为 **Markov Decision Process (MDP)**。
- **基于离线强化学习训练预测模型**：使用 offline reinforcement learning（RL）训练一个轻量级的 LSTM-based 预测模型，输入为 draft model 输出的 top-k 置信度分数，输出为 `continue` 或 `stop` 的控制信号。
- **无需真实标签数据**：由于 acceptance length 具有随机性，无法获得精确标签。RADAR 创新地利用在 ShareGPT 数据集上运行 EAGLE-3 得到的 **acceptance length 分布**构建训练数据集，用于计算奖励函数，避免了在线交互带来的高成本。

### 🔍 相比现有方法的优势
| 方面 | EAGLE-2 / EAGLE-3 | RADAR |
|------|------------------|--------|
| 调用次数 | 固定超参数（如8次） | 动态决定，按需停止 |
| 决策机制 | 启发式剪枝/重排序 | 基于 RL 的实时策略决策 |
| 计算效率 | 存在冗余调用风险 | 显著减少无效调用 |
| 加速效果 | 已较优 | 进一步提升速度 |

> ✅ **优势总结**：在保持几乎相同 acceptance length 的前提下，显著减少 draft model 的调用次数，降低推理延迟，实现更高 speedup。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **训练数据集**：`ShareGPT`（用于收集 draft model 的 confidence scores 和构建 acceptance length 分布）
- **评估任务与数据集**（四类典型任务）：
  - 多轮对话：**MT-bench**
  - 数学推理：**GSM8K**
  - 指令遵循：**Alpaca**
  - 代码生成：**MBPP**

> 所有任务均未进行任务特定微调，权重共享。

### ⚙️ 实验设置
- **Target LLMs**：
  - LLaMA-Instruct 3.1 8B (**L3 8B**)
  - Vicuna 13B (**V13B**)
  - DeepSeek-R1-Distill-LLaMA 8B (**DSL 8B**)
- **Draft Model**：沿用 EAGLE-3 中的设计（具体结构未详述，但非本文重点）
- **关键参数**：
  - Branching factor $ k = 10 $
  - Maximum draft model calls: 8
  - Temperature: 1.0
  - Batch size: 1
- **硬件环境**：2× NVIDIA RTX 3090 GPUs

### 📊 评估指标
| 指标 | 定义与意义 |
|------|-----------|
| **Speedup Ratio** | 相对于 vanilla auto-regressive decoding 的实际加速比 |
| **Average Acceptance Length (T)** | 每个 drafting-verification cycle 平均接受的 token 数量，反映 draft 质量 |
| **Average Number of Calls to Draft Model** | 每 cycle 平均调用 draft model 的次数（不再是固定值） |

> ❗ 注意：不报告生成质量（如 BLEU、ROUGE），因为 speculative sampling 是 lossless 的，输出分布不变。

### 🔁 基线方法对比
- **Baseline**：Vanilla Auto-Regressive Decoding（速度基准 = 1.00x）
- **对比方法**：
  - **EAGLE-2**：基于不确定性优化的 speculative sampling
  - **EAGLE-3**：当前最先进的 tree-based speculative sampling 方法（固定调用8次）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| Target LLM | Method | MT-bench Speedup | GSM8K Speedup | Alpaca Speedup | MBPP Speedup |
|------------|--------|------------------|---------------|----------------|--------------|
| L3 8B      | EAGLE-2 | 2.56x            | 3.43x         | 2.89x          | 3.29x        |
| L3 8B      | EAGLE-3 | 3.08x            | 4.68x         | 3.86x          | 4.21x        |
| L3 8B      | **RADAR** | **3.41x**        | **4.82x**     | **4.04x**      | **4.44x**    |
| V13B       | EAGLE-3 | 3.74x            | 4.24x         | 3.50x          | 4.55x        |
| V13B       | **RADAR** | **4.05x**        | **4.36x**     | **3.84x**      | **4.75x**    |
| DSL 8B     | EAGLE-3 | 3.42x            | 4.39x         | 3.08x          | 3.71x        |
| DSL 8B     | **RADAR** | **3.86x**        | **4.71x**     | **3.17x**      | **3.99x**    |

> ✅ **总体加速范围**：**3.17x ~ 4.82x**，相比 EAGLE-3 提升 **3% ~ 29%**

### 🔍 与基线方法对比分析

#### （1）Acceptance Length 对比
- RADAR 的平均 acceptance length 略低于 EAGLE-3（约低 1.2%），说明 draft token 质量相近。
- 尽管 acceptance length 稍低，但 RADAR 实现了更高的 speedup —— 表明其优势并非来自更高质量的 draft，而是**更高的计算效率**。

#### （2）Draft Model 调用次数对比（Table 2）

| Model | MT-bench | GSM8K | Alpaca | MBPP |
|-------|----------|-------|--------|------|
| L3 8B | 5.25     | 6.19  | 6.20   | 6.60 |
| V13B  | 6.88     | 7.26  | 6.83   | 7.26 |
| DSL 8B| 6.10     | 7.20  | 5.85   | 6.47 |

> ✅ **平均减少调用次数：9.3% ~ 34.3%，平均降幅达 18.7%**
>
> → 证明 RADAR 成功实现了“早停”机制，在低效时主动终止 draft 生成，减少冗余计算。

#### （3）可视化分析（Figure 2）
- 图 (a)：RADAR 与 EAGLE-3 的 acceptance length 分布接近，维持高接受率。
- 图 (b)：RADAR 的 draft model 调用次数集中在中间区间（如5~7），而非固定为8，体现动态决策能力。

> 💡 发现：**通过牺牲极少量 acceptance length，换来大幅减少计算开销，最终实现更高 overall speedup。**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **动态决策优于固定调用**：允许模型根据上下文动态决定是否继续生成 draft token，能有效避免在低接受概率场景下的浪费。
2. **MDP + Offline RL 可行且高效**：即使没有真实标注的“最优调用次数”，也能通过模拟 acceptance length 分布构建 reward，成功训练出高效的决策策略。
3. **加速来源于效率优化而非 draft 质量提升**：RADAR 并未改进 draft model 本身，而是通过智能调度减少了冗余调用，是典型的“系统级优化”。
4. **通用性强**：在三种不同 LLM 和四种不同类型任务上均取得一致领先，表明方法具有良好的泛化能力。

### ⚠️ 局限性
- **依赖高质量的 acceptance length 分布估计**：训练数据来自 EAGLE-3 在 ShareGPT 上的行为，若 draft model 或任务差异过大，可能影响迁移效果。
- **预测模型引入额外开销**：虽然使用轻量 LSTM，但仍增加了一定计算负担（$ T_{\text{eye}} $），在极小 draft model 场景下收益可能受限。
- **未探索更复杂架构**：目前仅使用简单 LSTM，未来可尝试 Transformer 或 MoE 架构以进一步提升决策精度。

### 🔮 未来工作方向
- 改进预测模型架构与训练机制（如引入 contrastive learning、better reward shaping）
- 设计更精细的 reward 函数，考虑 memory bandwidth、GPU occupancy 等硬件因素
- 扩展至 streaming 或 long-context 场景下的 adaptive drafting
- 探索与其他加速技术（如 quantization、prompt caching）的联合优化

---

## 总结

> **RADAR 提出了一种新颖的、基于 RL 的动态 speculative sampling 框架，首次将 draft tree 的生成过程建模为 MDP，并通过 offline RL 实现对 draft model 调用次数的实时调控。实验表明，该方法在多个 LLM 和任务上实现了 3.17x–4.82x 的加速，相比 EAGLE-3 平均减少 18.7% 的 draft model 调用，在几乎不损失 acceptance length 的情况下显著提升了推理效率，代表了 speculative sampling 方向的重要进展。**

</details>

---

### 16. [Distillation-Guided Structural Transfer for Continual Learning Beyond Sparse Distributed Memory](https://arxiv.org/abs/2512.15267)

**Authors**: Huiyan Xue, Xuming Ran, Yaxin Li, Qi Xu, Enhui Li, Yi Xu, Qiang Zhang  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.15267v1  

#### Abstract
Sparse neural systems are gaining traction for efficient continual learning due to their modularity and low interference. Architectures such as Sparse Distributed Memory Multi-Layer Perceptrons (SDMLP) construct task-specific subnetworks via Top-K activation and have shown resilience against catastr...

---

### 17. [AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach](https://arxiv.org/abs/2512.13714)

**Authors**: Gangesh Pathak, Prasanna Kumar  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13714v1  

#### Abstract
LLM implementations are failing in highly regulated industries owing to instability issues, inconsistent reasoning, hallucinations and performance variability, especially in workflows. These reliability issues restrict safe use of LLM in areas that need the precision of facts and consistent behavior...

---

### 18. [Adversarial versification in portuguese as a jailbreak operator in LLMs](https://arxiv.org/abs/2512.15353)

**Authors**: Joao Queiroz  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.15353v1  

#### Abstract
Recent evidence shows that the versification of prompts constitutes a highly effective adversarial mechanism against aligned LLMs. The study 'Adversarial poetry as a universal single-turn jailbreak mechanism in large language models' demonstrates that instructions routinely refused in prose become e...

---

### 19. [Is GPT-OSS All You Need? Benchmarking Large Language Models for Financial Intelligence and the Surprising Efficiency Paradox](https://arxiv.org/abs/2512.14717)

**Authors**: Ziqian Bi, Danyang Zhang, Junhao Song, Chiung-Yi Tseng  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.14717v1  

#### Abstract
The rapid adoption of large language models in financial services necessitates rigorous evaluation frameworks to assess their performance, efficiency, and practical applicability. This paper conducts a comprehensive evaluation of the GPT-OSS model family alongside contemporary LLMs across ten divers...

---

### 20. [SigMA: Path Signatures and Multi-head Attention for Learning Parameters in fBm-driven SDEs](https://arxiv.org/abs/2512.15088)

**Authors**: Xianglin Wu, Chiheb Ben Hammouda, Cornelis W. Oosterlee  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.15088v1  

#### Abstract
Stochastic differential equations (SDEs) driven by fractional Brownian motion (fBm) are increasingly used to model systems with rough dynamics and long-range dependence, such as those arising in quantitative finance and reliability engineering. However, these processes are non-Markovian and lack a s...

---

### 21. [State-Dependent Refusal and Learned Incapacity in RLHF-Aligned Language Models](https://arxiv.org/abs/2512.13762)

**Authors**: TK Lee  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13762v1  

#### Abstract
Large language models (LLMs) are widely deployed as general-purpose tools, yet extended interaction can reveal behavioral patterns not captured by standard quantitative benchmarks. We present a qualitative case-study methodology for auditing policy-linked behavioral selectivity in long-horizon inter...

---

### 22. [Sparsity-Controllable Dynamic Top-p MoE for Large Foundation Model Pre-training](https://arxiv.org/abs/2512.13996)

**Authors**: Can Jin, Hongwu Peng, Mingcan Xiang, Qixin Zhang, Xiangchi Yuan, Amit Hasan, Ohiremen Dibua, Yifan Gong, Yan Kang, Dimitris N. Metaxas  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13996v1  

#### Abstract
Sparse Mixture-of-Experts (MoE) architectures effectively scale model capacity by activating only a subset of experts for each input token. However, the standard Top-k routing strategy imposes a uniform sparsity pattern that ignores the varying difficulty of tokens. While Top-p routing offers a flex...

---

### 23. [TiCard: Deployable EXPLAIN-only Residual Learning for Cardinality Estimation](https://arxiv.org/abs/2512.14358)

**Authors**: Qizhi Wang  
**Category**: cs.AI  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14358v1  

#### Abstract
Cardinality estimation is a key bottleneck for cost-based query optimization, yet deployable improvements remain difficult: classical estimators miss correlations, while learned estimators often require workload-specific training pipelines and invasive integration into the optimizer. This paper pres...

---

### 24. [Well Begun, Half Done: Reinforcement Learning with Prefix Optimization for LLM Reasoning](https://arxiv.org/abs/2512.15274)

**Authors**: Yiliu Sun, Zicheng Zhao, Yang Wei, Yanfang Zhang, Chen Gong  
**Category**: cs.CL  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15274v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) significantly enhances the reasoning capability of Large Language Models (LLMs). Current RLVR approaches typically conduct training across all generated tokens, but neglect to explore which tokens (e.g., prefix tokens) actually contribute to reas...

---

### 25. [LeaseGuard: Raft Leases Done Right](https://arxiv.org/abs/2512.15659)

**Authors**: A. Jesse Jiryu Davis, Murat Demirbas, Lingzhi Deng  
**Category**: cs.DC  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15659v1  

#### Abstract
Raft is a leading consensus algorithm for replicating writes in distributed databases. However, distributed databases also require consistent reads. To guarantee read consistency, a Raft-based system must either accept the high communication overhead of a safety check for each read, or implement lea...

---

### 26. [How a Bit Becomes a Story: Semantic Steering via Differentiable Fault Injection](https://arxiv.org/abs/2512.14715)

**Authors**: Zafaryab Haider, Md Hafizur Rahman, Shane Moeykens, Vijay Devabhaktuni, Prabuddha Chakraborty  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14715v1  

#### Abstract
Hard-to-detect hardware bit flips, from either malicious circuitry or bugs, have already been shown to make transformers vulnerable in non-generative tasks. This work, for the first time, investigates how low-level, bitwise perturbations (fault injection) to the weights of a large language model (LL...

---

### 27. [Deep Learning and Elicitability for McKean-Vlasov FBSDEs With Common Noise](https://arxiv.org/abs/2512.14967)

**Authors**: Felipe J. P. Antunes, Yuri F. Saporito, Sebastian Jaimungal  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14967v1  

#### Abstract
We present a novel numerical method for solving McKean-Vlasov forward-backward stochastic differential equations (MV-FBSDEs) with common noise, combining Picard iterations, elicitability and deep learning. The key innovation involves elicitability to derive a path-wise loss function, enabling effici...

---

### 28. [EMFusion: Conditional Diffusion Framework for Trustworthy Frequency Selective EMF Forecasting in Wireless Networks](https://arxiv.org/abs/2512.15067)

**Authors**: Zijiang Yan, Yixiang Huang, Jianhua Pei, Hina Tabassum, Luca Chiaraviglio  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15067v1  

#### Abstract
The rapid growth in wireless infrastructure has increased the need to accurately estimate and forecast electromagnetic field (EMF) levels to ensure ongoing compliance, assess potential health impacts, and support efficient network planning. While existing studies rely on univariate forecasting of wi...

---

### 29. [How Many Heads Make an SSM? A Unified Framework for Attention and State Space Models](https://arxiv.org/abs/2512.15115)

**Authors**: Ali Ghodsi  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15115v1  

#### Abstract
Sequence modeling has produced diverse architectures -- from classical recurrent neural networks to modern Transformers and state space models (SSMs) -- yet a unified theoretical understanding of expressivity and trainability trade-offs remains limited. We introduce a unified framework that represen...

---

### 30. [O-EENC-SD: Efficient Online End-to-End Neural Clustering for Speaker Diarization](https://arxiv.org/abs/2512.15229)

**Authors**: Elio Gruttadauria (IP Paris, LTCI, IDS, S2A), Mathieu Fontaine (LTCI, IP Paris), Jonathan Le Roux (IDS, S2A, LTCI), Slim Essid (IDS, S2A, LTCI)  
**Category**: cs.LG  
**Published**: 2025-12-18  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15229v1  

#### Abstract
We introduce O-EENC-SD: an end-to-end online speaker diarization system based on EEND-EDA, featuring a novel RNN-based stitching mechanism for online prediction. In particular, we develop a novel centroid refinement decoder whose usefulness is assessed through a rigorous ablation study. Our system p...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
