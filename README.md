# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-17 06:40:30 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [QuRL: Efficient Reinforcement Learning with Quantized Rollout](https://arxiv.org/abs/2602.13953)

**Authors**: Yuhang Li, Reena Elangovan, Xin Dong, Priyadarshini Panda, Brucek Khailany  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.13953v1  

#### Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a trending paradigm for training reasoning large language models (LLMs). However, due to the autoregressive decoding nature of LLMs, the rollout process becomes the efficiency bottleneck of RL training, consisting of up to 70\% of the ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：QuRL: Efficient Reinforcement Learning with Quantized Rollout**

---

## 1. **论文的主要贡献和创新点**

### **解决的问题**
在基于强化学习训练推理型大语言模型（Reasoning LLMs）的过程中，**rollout 阶段成为训练效率的瓶颈**，占总训练时间高达 70%。这是由于 LLM 的自回归解码特性导致的高延迟。尽管量化（quantization）可加速推理，但直接将量化用于 rollout 会引发两个关键问题：
1. **长期训练崩溃（training collapse）**：量化 actor 与全精度 actor 之间的策略差异随训练扩大，导致重要性采样失效。
2. **权重更新被量化噪声掩盖**：RL 更新的权重变化极小（~1e-7），远小于 INT8/FP8 量化的粒度，导致量化模型“冻结”，无法有效学习。

### **提出的新方法**
本文提出 **QuRL (Quantized Reinforcement Learning)**，一种通过量化 rollout 加速 RL 训练的高效算法，包含两大核心技术：

#### ✅ **Adaptive Clipping Range (ACR)**
- 动态调整 PPO 中的裁剪范围（clipping range），依据 **全精度 actor 与量化 actor 的策略比率** 调整信任域边界。
- 当行为策略（behavior policy）与近端策略（proximal policy）差异过大时，自动放宽裁剪上限，防止梯度爆炸和训练不稳定。

#### ✅ **Update-Aware Quantization (UAQ)**
- 引入 **不变缩放（invariant scaling）** 技术，在量化前对权重进行放大（scaling factor $ s > 1 $）。
- 双重效果：
  - 减少量化误差（因权重变大）
  - 放大梯度更新（因激活也缩放）
- 实现 $ s^2 $ 倍的“更新信号 vs 量化噪声”信噪比提升，使量化模型能感知微小权重变化。

### **相比现有方法的优势**
| 方面 | QuRL | FlashRL / Naive Quantized RL |
|------|------|-----------------------------|
| **训练稳定性** | ✅ 长期稳定（>1000 步无崩溃） | ❌ 后期性能下降或崩溃 |
| **性能保留** | ✅ 接近 BF16 基线（差距仅 ~1–2%） | ❌ 明显性能退化（如 INT8 下降 4%） |
| **效率提升** | ✅ 20%–80% 更快 rollout | ✅ 类似加速，但性能差 |
| **实现复杂度** | ✅ 无需 QAT 或每步校准 | ❌ FlashRL 依赖工程适配 |

---

## 2. **核心实验方法和设置**

### **使用的数据集与任务**
- **GSM8K**：小学数学应用题，测试基本推理能力。
- **AIME 2024**：高级数学竞赛题，评估复杂推理。
- **DeepScaleR Benchmark**：综合数学推理基准，涵盖：
  - AIME
  - AMC
  - MATH
  - Minerva
  - Olympiad

### **模型规模**
- Qwen2.5-0.5B / 7B / 14B / 32B
- DeepSeek-Distill-Qwen 系列（7B/14B/32B）

### **实验设置**
- **量化格式**：INT8 和 FP8
- **量化方式**：
  - 权重：channel-wise scaling
  - 激活：token-wise scaling
- **训练框架**：VeRL（混合引擎 RL 框架）
- **硬件平台**：A6000, A100, H100（单卡或双卡 TP）
- **rollout 设置**：不同任务下生成 8–16 条响应，上下文长度达 24k

### **评估指标**
- **准确性**：
  - `Accuracy`（GSM8K）
  - `Avg@1`（greedy decoding）
  - `Avg@32`（采样 32 条路径取平均）
- **吞吐量**：queries per second（QPS）
- **训练动态监控**：
  - KL 散度（$ D_{KL}(\pi_{\text{behav}} \| \pi_{\text{prox}}) $）
  - 裁剪比例（clipped fraction）
  - 权重更新幅度 vs 量化误差

### **基线方法对比**
| 方法 | 描述 |
|------|------|
| **Full-precision RL (BF16)** | 全精度训练，性能上限基准 |
| **Naive INT8/FP8 RL** | 直接量化 rollout，无修正机制 |
| **FlashRL (Liu et al., 2025)** | 使用 Truncated Importance Sampling (TIS) 缓解分布偏移 |
| **QuRL w/o ACR** | 移除 ACR 模块 |
| **QuRL w/o UAQ** | 移除 UAQ 模块 |

---

## 3. **主要实验结果和性能指标**

### **关键性能数据汇总**

#### 🔹 **GSM8K 结果（Table 1）**
| 方法 | Bitwidth | Accuracy |
|------|----------|----------|
| RL (BF16) | BF16 | 55.35 |
| RL (INT8) | INT8 | 48.78 |
| FlashRL | INT8 | 51.40 |
| **QuRL (Ours)** | **INT8** | **53.55** |
| FlashRL | FP8 | 53.60 |
| **QuRL (Ours)** | **FP8** | **54.28** |

> ✅ QuRL 将 INT8 性能差距从 6.57%（naive）缩小至 **1.8%**，FP8 接近 BF16。

#### 🔹 **AIME 2024 结果（Table 2）**
| 方法 | Bitwidth | Avg@1 | Avg@32 |
|------|----------|--------|--------|
| RL (BF16) | BF16 | 33.33 | 31.67 |
| FlashRL | INT8 | 26.66 | 30.29 |
| **QuRL w/ UAQ** | **INT8** | **33.33** | **31.25** |
| FlashRL | FP8 | 30.00 | 32.60 |
| **QuRL w/ UAQ** | **FP8** | **33.33** | **33.27** |

> ✅ QuRL 在 INT8 下恢复到 BF16 水平；FP8 甚至略超基线。

#### 🔹 **DeepScaleR 综合结果（Table 3）**
| 方法 | Bitwidth | Avg Accuracy |
|------|----------|--------------|
| RL (BF16) | BF16 | 56.40 |
| RL (INT8) | INT8 | 52.31 |
| FlashRL | INT8 | 53.80 |
| **QuRL w/ UAQ** | **INT8** | **55.48** |

> ✅ QuRL 在 INT8 下比 naive 提升 **3.17%**，比 FlashRL 提升 **1.68%**，接近 BF16 表现。

### **吞吐量提升（Figure 8）**
| 模型 | GPU | 加速比（INT8 vs BF16） |
|------|-----|------------------------|
| 7B | A6000/H100 | 1.2–1.4x |
| 14B | A6000/H100 | 1.3–1.6x |
| 32B | A100/H100 (TP=2) | **1.7–1.8x** |

> 🚀 最大模型获得最高加速（**80%+ throughput gain**），因计算更密集，受益于量化矩阵乘法优化。

### **消融实验（Ablation Study）**
#### 🔹 **UAQ 缩放因子选择（Table 4）**
| Scale $ s $ | LR | Avg@32 |
|-------------|-----|--------|
| 1.0 | 1e-6 | 30.63 |
| **1.5** | **1e-6** | **31.25** ✅ |
| 2.0 | 1e-6 | 29.15 ❌（过拟合） |
| 1.0 | 1.5e-5 | 29.06 ❌（学习率过大不稳定） |

> ✅ $ s=1.5 $ 是最优平衡点：既放大更新，又不破坏训练稳定性。

#### 🔹 **模块有效性验证**
- **移除 ACR**：训练后期 KL 散度激增，性能骤降（Fig. 3）
- **移除 UAQ**：权重更新几乎被量化噪声淹没（Fig. 4, 9）

---

## 4. **关键结论和发现**

### **主要发现**
1. **量化 rollout 可显著加速 RL 训练**（20%–80% throughput 提升），尤其对大模型更明显。
2. **直接量化 rollout 会导致训练崩溃**，主因是策略漂移和权重更新丢失。
3. **ACR 有效维持长期训练稳定性**，通过动态裁剪防止重要性采样偏差累积。
4. **UAQ 成功桥接“更新 vs 量化”尺度鸿沟**，利用 invariant scaling 实现高效低比特训练。
5. **QuRL 在多个 RL 算法（PPO, GRPO, DAPO）和模型规模（7B–32B）上均表现优异**，具备通用性。

### **方法的局限性**
- **依赖 vLLM 等推理引擎支持量化 kernel**：当前 FP8 KV cache 未优化，限制进一步加速。
- **scaling factor $ s $ 需手动调参**：虽 $ s=1.5 $ 通用性好，但仍非完全自动化。
- **未探索低于 INT8 的量化（如 INT4/W4A4）**：可能面临更大噪声挑战。
- **假设 rollout 与训练引擎一致性较高**：若差异过大，仍需额外对齐机制。

### **未来工作方向**
- 自动化 $ s $ 的选择，结合训练动态在线调整。
- 扩展至 **QLoRA + Quantized Rollout** 联合压缩方案。
- 探索 **asymmetric quantization** 或 **outlier-aware quantization** 进一步提升精度。
- 应用于 **multi-agent RL** 或 **long-horizon planning** 场景，验证泛化能力。

---

> ✅ **总体评价**：  
> QuRL 是首个系统性解决“量化 rollout 导致 RL 训练不稳定”的工作，提出了简洁而有效的 ACR 与 UAQ 机制，在保持高性能的同时实现显著加速，为大规模 LLM 的高效 RL 训练提供了实用路径。

</details>

---

### 2. [QuaRK: A Quantum Reservoir Kernel for Time Series Learning](https://arxiv.org/abs/2602.13531)

**Authors**: Abdallah Aaraba, Soumaya Cherkaoui, Ola Ahmad, Shengrui Wang  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.13531v1  

#### Abstract
Quantum reservoir computing offers a promising route for time series learning by modelling sequential data via rich quantum dynamics while the only training required happens at the level of a lightweight classical readout. However, studies featuring efficient and implementable quantum reservoir arch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# QuaRK: A Quantum Reservoir Kernel for Time Series Learning 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
量子储层计算（Quantum Reservoir Computing, QRC）在时间序列学习中展现出潜力，但存在两大瓶颈：
- **硬件实现困难**：许多QRC架构依赖昂贵或难以在真实设备上实现的测量方案（如全态层析）。
- **缺乏理论保障**：现有研究缺少将具体可实现的QRC架构与明确的学习理论泛化保证相结合的端到端框架。

本文旨在填补这一空白，提出一个**既实用又具备理论支持**的时间序列量子学习框架。

---

### 提出的新方法与新思路
作者提出了 **QUARK**（Quantum Reservoir Kernel），一个端到端的量子-经典混合学习框架，其核心组件包括：

1. **硬件现实的量子储层特征提取器（Quantum Reservoir Featurizer）**
   - 采用**Contracted-Encoding Quantum Channels (CEQC)** 架构，确保满足回声状态性质（ESP）和衰减记忆性质（FMP），从而稳定处理时序依赖。
   - 引入 **Johnson-Lindenstrauss (JL) 投影**，将高维输入数据投影到与量子比特数 $n$ 匹配的低维空间，**解耦量子资源与原始数据维度**，提升可扩展性。
   - 利用**空间复用（Spatial Multiplexing）**，并行运行多个具有不同参数的子储层，增强模型表达能力。

2. **高效的特征读出机制**
   - 使用 **Classical Shadows** 技术对储层末态进行高效测量，仅需少量电路运行即可同时估计所有 $k$-局部可观测量（如2-local Pauli）的期望值，生成紧凑特征向量。
   - 避免了传统量子机器学习中高昂的测量成本。

3. **核方法驱动的经典读出（Kernel-Based Readout）**
   - 在生成的量子特征上应用**经典核方法**（Matérn kernel），构建再生核希尔伯特空间（RKHS）中的读出函数。
   - 采用**核岭回归（Kernel Ridge Regression）** 进行闭式训练，优化快速且正则化控制明确。

4. **学习理论分析**
   - 提供了针对**弱依赖时间序列数据**（如 $\beta$-mixing 过程）的**有限样本泛化界**（generalization bound）。
   - 将设计选择（如投影维度、储层大小、测量预算、正则化强度）与实际性能联系起来，为系统设计提供原则性指导。

---

### 相比现有方法的优势
| 特性 | QUARK | 传统QRC方法 |
|------|-------|-------------|
| **可扩展性** | ✅ 通过JL投影解耦数据维与量子资源 | ❌ 通常直接编码，受限于比特数 |
| **测量效率** | ✅ Classical Shadows，测量开销小 | ❌ 常需大量测量或全态层析 |
| **训练效率** | ✅ 核岭回归，闭式解，无需反向传播 | ❌ 可能需要复杂优化 |
| **理论保障** | ✅ 明确的泛化界适用于非i.i.d.时序数据 | ❌ 多数缺乏严格理论分析 |
| **灵活性** | ✅ 核方法天然支持非线性建模 | ⚠️ 读出层可能受限 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **合成数据集**：基于**向量自回归滑动平均模型（VARMA(p=3, q=3)）** 生成多元时间序列。
  - 输入维度 $d=3$，定义域 $I = (-1,1)^d$。
  - 过程为**平稳且$\beta$-mixing**，符合理论假设。
  - 通过 `tanh` 函数确保有界输出。
- **标签生成**：由三种不同复杂度的真实“地基”（ground-truth）衰减记忆函数 $H^*$ 生成标量标签 $Y_t$：
  1. **单步预测（One-step Forecasting）**：$H^*(X) = u^\top X_{t+1}$（线性）
  2. **指数衰减线性函数（Exponential Fading）**：$H^*(X) = \sum_{k=0}^{w-1} \alpha^k u^\top X_{t-k}$
  3. **二阶截断Volterra函数（Volterra）**：包含二次交叉滞后项，形式为 $\sum_k \sum_l \alpha^{k+l} (v^\top X_{t-k})(v^\top X_{t-l})$

### 实验设置
- **窗口长度**：$w = 25$
- **采样步幅**：$s = 100$（即间隔 $g=75$ 以减少窗口间依赖）
- **量子设置**：
  - 每个子储层使用 $n=5$ 个量子比特。
  - 子储层数量 $R=3$。
  - 测量 **2-local Pauli可观测量**。
  - 每次电路运行使用 **1000次测量shot** 构建Classical Shadow。
- **核方法**：
  - 使用 **Matérn核**，超参数（长度尺度 $\ell$、平滑度 $\nu$）通过轻量级验证集调优后固定。
  - 读出训练采用 **核岭回归**，通过调节正则化参数 $\lambda_{\text{reg}}$ 控制RKHS范数约束 $A$。

### 评估指标
- **训练均方误差（Train MSE）**：用于验证插值现象（interpolation regime）。
- **测试均方误差（Test MSE）**：用于评估泛化能力，随训练窗口数量 $N$ 的变化趋势是关键观察点。

### 基线方法对比
论文**未直接与其他QRC或经典时序模型进行性能数值对比**。其重点在于：
- 验证自身提出的**理论预测**（如插值行为、泛化误差随 $N$ 下降）是否在实验中成立。
- 展示方法在不同任务复杂度下的表现趋势。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与发现
1. **插值现象验证（Effective Learning）**
   - **图3** 显示，随着正则化减弱（$\lambda_{\text{reg}}$ 减小，即允许更大的RKHS范数 $A$），**训练MSE急剧下降并在 $\lambda_{\text{reg}} \sim 10^{-1}$ 附近达到数值零**。
   - 这证实了**插值区域的存在**，即模型有能力完美拟合训练数据，与 **Theorem 1** 的预测一致。
   - **图4** 展示了在插值区域内的训练集预测，几乎完全贴合真实标签（MSE ~ $10^{-14}$ 至 $10^{-12}$），证明拟合非偶然。

2. **泛化能力验证（Generalization）**
   - **图5** 显示，在固定其他设置、仅增加训练窗口数 $N$ 的情况下，**测试MSE随 $N$ 增加而单调下降**。
   - 下降趋势大致遵循 $1/\sqrt{N}$ 规律，与 **Theorem 2** 中的泛化界预测相符。
   - 不同任务的测试误差排序反映了其内在复杂度：`Forecasting` < `Exponential` < `Volterra`，符合预期。

### 消融实验结果
论文**未包含显式的消融实验**（如移除JL投影、关闭空间复用等）。其验证主要围绕**理论驱动的变量扫描**：
- 扫描正则化参数 $\lambda_{\text{reg}}$ 来验证容量与插值。
- 扫描训练样本数 $N$ 来验证泛化行为。
这些实验间接验证了各组件（如核读出的表达能力、测量方案的有效性）的必要性，但未量化单个模块的贡献。

---

## 4. 关键结论和发现

### 主要发现
1. **理论与实践的一致性**：QUARK框架成功地将**硬件可行的量子储层设计**与**严格的统计学习理论**（特别是针对依赖数据的泛化界）结合起来，且实验结果与理论预测高度吻合。
2. **插值是可实现的**：即使使用仅有5个量子比特的小型储层，通过适当的JL投影和强大的核读出，模型也能进入插值区域，表明其具有很强的拟合能力。
3. **泛化行为可控**：在$\beta$-mixing假设下，增加训练数据量能有效提升泛化性能，验证了所提泛化界的实用性。
4. **可扩展性设计有效**：JL投影成功解耦了数据维度与量子资源，为空间复用等增强表达力的技术提供了基础。

### 方法的局限性
1. **合成数据验证**：所有实验均在合成的VARMA数据上进行，尚未在真实世界复杂时序（如金融、医疗）上验证。
2. **噪声忽略**：实验在理想模拟器上进行，未考虑真实量子硬件中的噪声、退相干和有限测量shot带来的偏差。
3. **无直接基线对比**：缺乏与经典时序模型（如LSTM、TCN）或其他QRC变体的性能比较，难以评估其相对优势。
4. **超参数调优成本**：虽然核超参只调一次，但在量子特征生成代价高的场景下，整体调优仍可能昂贵。

### 未来工作方向
1. **噪声鲁棒性分析**：将理论分析扩展到含噪场景，研究噪声对泛化界的影响。
2. **真实数据应用**：在更长、更高维、非平稳的真实时间序列上测试QUARK。
3. **资源-精度权衡**：系统研究测量预算、可观测量局域性 $k$、子储层数 $R$ 等因素对准确率的具体影响。
4. **自动化调优**：开发针对QUARK各层级（投影、储层、核）的联合优化策略。
5. **探索其他核函数**：尝试除Matérn外的其他核函数以适应不同数据特性。

</details>

---

### 3. [Synergistic Intra- and Cross-Layer Regularization Losses for MoE Expert Specialization](https://arxiv.org/abs/2602.14159)

**Authors**: Rizhen Hu, Yuan Cao, Boao Kong, Mou Sun, Kun Yuan  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.14159v1  

#### Abstract
Sparse Mixture-of-Experts (MoE) models scale Transformers efficiently but suffer from expert overlap -- redundant representations across experts and routing ambiguity, resulting in severely underutilized model capacity. While architectural solutions like DeepSeekMoE promote specialization, they requ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Synergistic Intra- and Cross-Layer Regularization Losses for MoE Expert Specialization》总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

稀疏 **Mixture-of-Experts (MoE)** 模型通过扩展参数量同时保持每 token 计算成本恒定，已成为高效扩展 **Transformer** 的主流方案。然而，MoE 模型在训练中普遍存在两个关键问题：

- **Expert Overlap（专家重叠）**：不同专家对相同 token 产生高度相似的激活，导致功能冗余，浪费模型容量。
- **Routing Ambiguity（路由模糊）**：相似输入被不一致地分配给不同专家，导致专家角色模糊，路由熵高。

这两个问题共同导致 **专家专业化（expert specialization）** 退化，削弱了 MoE 的核心优势——通过分工提升效率与性能。

---

### 提出了什么新方法或新思路

本文提出了一种**损失函数为中心（loss-centric）** 的解决方案，引入两个即插即用（plug-and-play）的正则化损失，无需修改模型架构或路由器设计：

#### (1) **Intra-Layer Specialization Loss (Rsp)**
- **目标**：解决 **Expert Overlap**。
- **机制**：惩罚同一 token 在同一层中被激活的不同专家之间的 **SwiGLU 中间激活（intermediate activation）** 的余弦相似度。
- **作用**：鼓励专家发展出互补的知识，推动其梯度更新方向趋于正交，增强层内功能分化。

#### (2) **Cross-Layer Coupling Loss (Rcp)**
- **目标**：解决 **Routing Ambiguity**。
- **机制**：最大化相邻层之间 Top-k 专家对的联合路由概率，鼓励 token 沿着一致的“专家路径”（expert path）传播。
- **作用**：建立跨层的连贯专家路径，降低路由熵，稳定专家身份，并作为“自监督信号”将深层更稳定的路由决策反向传递到浅层。

---

### 相比现有方法的优势

| 维度 | 本文方法 | 现有方法（如 DeepSeekMoE, ReMoE） |
|------|----------|-------------------------------|
| **修改范围** | 仅添加损失项，**无需修改架构或路由器** | 需要修改专家布局、共享专家、或路由机制 |
| **兼容性** | 与任何 MoE 架构兼容（vanilla 或 DeepSeek-style） | 通常为特定架构设计 |
| **理论基础** | 明确建模了 specialization 与 routing 的**正反馈循环** | 多依赖经验设计，缺乏系统理论分析 |
| **实现难度** | 以 **drop-in Megatron-LM 模块** 实现，仅需配置标志位 | 需深度集成，代码侵入性强 |

> ✅ **核心优势**：**即插即用、架构无关、理论可解释、效果显著**。

---

## 2. 核心实验方法和设置

### 使用的数据集

- **预训练数据**：`C4-en` 数据集。
- **下游任务**：
  - **Zero-shot 评估**：BoolQ, ARC-Easy, ARC-Challenge, TruthfulQA-MC2, PIQA, MMLU, HellaSwag。
  - **SFT 评估**：基于内部语料进行 LoRA 微调。
  - **全参数微调**：在 `Qwen3-30B-A3B-Instruct-2507` 上进行指令微调。

---

### 实验设置和评估指标

#### 模型架构
- **Vanilla MoE** 和 **DeepSeek-style MoE** 两种变体。
- 规模覆盖：Small (0.4B), Medium (1.1B), Large (7.0B)。
- 使用 **SwiGLU**, **RoPE**, **RMSNorm** 等现代组件。

#### 正则化配置
- 基线：`L1b`（标准 load-balancing loss）
- 对比组：`L1b,z`, `L1b,sp`, `L1b,cp`, `L1b,sp,cp`, `L1b,z,sp,cp`
- 超参数：`λ_sp = 2e-3`, `λ_cp = 1e-3`

#### 评估指标
- **预训练**：验证集 **Perplexity (↓)**
- **下游任务**：准确率（Accuracy）、pass@1（如 HumanEval）
- **专业化程度**：路由熵、专家路径一致性、激活相似度
- **推理效率**：吞吐量（samples/s）

---

### 基线方法对比

- **标准基线**：仅使用 load-balancing loss (`L1b`)
- **先进基线**：
  - `L1b,z`：加入 router z-loss
  - `L1b,o,v`：Guo et al. (2025a) 提出的正交性与 logit 方差正则化
- **架构级方法**：DeepSeekMoE 的 shared-expert 设计

> 本文方法在 **相同架构下** 与这些基线进行公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### (1) 预训练 Perplexity（越低越好）

| 模型 | `L1b` | `L1b,z` | `L1b,sp,cp` | `L1b,z,sp,cp` |
|------|-------|--------|-------------|----------------|
| Vanilla MoE (Large) | 9.68 | 9.52 | 9.48 | **9.42** |
| DeepSeek-style (Large) | 9.56 | 9.46 | 9.47 | **9.39** |

> ✅ **相对改进达 ~2.7%**，且与 z-loss 叠加后效果进一步提升。

#### (2) 下游任务性能（平均提升）

| 模型 | LoRA SFT 平均提升 |
|------|------------------|
| DeepSeek-MoE-16B | **+3.9 pts** |
| DeepSeek-V2-Lite | **+4.6 pts** |

#### (3) 全参数微调（Qwen3-30B-A3B）

| 指标 | `L1b` | `L1b,sp,cp` | 提升 |
|------|------|--------------|------|
| HumanEval pass@1 | 92.07 | **95.73** | **+3.66** |
| GSM8K accuracy | 93.33 | 94.16 | +0.83 |

---

### 与基线方法的对比结果

- **优于 `L1b,z`**：在所有设置下均取得更低的困惑度和更高的下游性能。
- **显著优于 `L1b,o,v`**：该方法甚至在某些情况下劣于基线，表明直接放大 logit 方差可能不稳定。
- **媲美甚至超越 DeepSeek-style 架构**：在 vanilla MoE 上应用本方法，性能可达到或超过未使用额外损失的 DeepSeek-style MoE，说明**训练目标可部分替代架构修改**。

---

### 消融实验结果

| 损失组合 | Vanilla MoE (Medium) PPL |
|---------|------------------------|
| `L1b` | 12.50 |
| `L1b,sp` | 12.44 |
| `L1b,cp` | 12.33 |
| `L1b,sp,cp` | **12.27** |

> ✅ 两个损失**独立有效且协同增益**，联合使用效果最佳。

#### 超参数敏感性
- 在 `λ_cp ∈ [2e-4, 2e-3]`, `λ_sp ∈ [5e-4, 3e-3]` 范围内，性能变化小于 1%，表明方法**鲁棒性强**。

---

## 4. 关键结论和发现

### 论文的主要发现

1. **专家专业化可以作为首要训练目标**：通过设计针对性损失函数，而非依赖架构修改，即可显著提升 MoE 性能。
2. **Rsp 与 Rcp 形成正反馈循环**：
   - Rsp 减少激活重叠 → 提升专家差异 → 路由更果断（熵降低）。
   - Rcp 建立稳定路径 → 降低路由模糊 → 专家训练数据更纯净 → 进一步促进专业化。
3. **即插即用的有效性**：无需修改模型结构，仅通过添加两个轻量损失，即可在多种 MoE 架构和规模上取得一致收益。
4. **推理加速**：由于路由更稳定、路径更一致，在专家并行（expert parallelism）下可减少 All-to-All 通信，**推理吞吐提升最高达 7%**。

---

### 方法的局限性

- **依赖 Top-k 路由机制**：方法假设存在明确的专家选择过程，对完全动态或软路由的 MoE 变体适用性待验证。
- **超参数需调优**：虽然鲁棒，但在极端设置下仍可能影响稳定性。
- **未解决负载均衡的根本挑战**：仍需依赖 `L1b` 等机制防止专家过载。

---

### 未来工作方向

1. **扩展到其他专家类型**：如注意力专家（attention experts）或混合专家（MoA）。
2. **动态调整损失权重**：根据训练阶段自动调节 `λ_sp` 和 `λ_cp`。
3. **结合硬件优化**：利用稳定的专家路径进行更激进的缓存、预取和设备放置策略。
4. **理论深化**：建立更精确的收敛性分析，量化 specialization 与模型容量利用率的关系。

---

> **总结**：本文提出了一套简单、通用、高效的 MoE 专家专业化训练框架，通过 **intra-layer** 和 **cross-layer** 两个正则化损失，实现了**性能、专业化、效率**的三重提升，为 MoE 模型的训练提供了新的范式。

</details>

---

### 4. [Query as Anchor: Scenario-Adaptive User Representation via Large Language Model](https://arxiv.org/abs/2602.14492)

**Authors**: Jiahao Yuan, Yike Xu, Jinyong Wen, Baokun Wang, Ziyi Gao, Xiaotong Lin, Yun Liu, Xing Fu, Yu Cheng, Yongchao Liu, Weiqiang Wang, Zhongle Xie  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.14492v1  

#### Abstract
Industrial-scale user representation learning requires balancing robust universality with acute task-sensitivity. However, existing paradigms primarily yield static, task-agnostic embeddings that struggle to reconcile the divergent requirements of downstream scenarios within unified vector spaces. F...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Query as Anchor: Scenario-Adaptive User Representation via Large Language Model**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
工业级用户表征学习面临三大挑战：
1. **场景适应性差**：传统静态嵌入（static embeddings）无法灵活适配不同下游任务（如推荐、风控、营销），导致需要维护多个专用模型，系统复杂度高。
2. **模态与语义鸿沟**：真实用户行为数据稀疏、符号化、多源异构，与LLM预训练所用的密集文本数据存在巨大差异，限制了LLM在用户建模中的泛化能力。
3. **噪声与异构数据融合困难**：多源行为日志（支付、搜索、小程序等）中存在大量无关信号和冲突模态，难以有效聚合。

### **提出的新方法与新思路**
作者提出 **Query-as-Anchor (Q-Anchor)** 框架，将用户建模从“静态编码”转变为“动态查询感知合成”，核心思想是：
- **以自然语言查询作为锚点（query as anchor）**，通过同一个用户行为序列，在不同query引导下生成不同的、场景自适应的用户嵌入。
- 构建 **UserU** 数据集，结合未来行为预测与LLM生成的问答对，为用户理解提供强监督信号。
- 设计 **Q-Anchor Embedding** 架构，采用双塔结构 + 层次化粗到细编码器，实现多模态行为与语义指令的对齐。
- 引入 **Cluster-based Soft Prompt Tuning**，在不微调主干网络的前提下，通过可学习soft prompt实现轻量级场景专业化。
- 利用 **KV-cache 加速推理**：将用户前缀一次性编码并缓存，后续只需计算query后缀，实现低延迟多场景服务。

### **相比现有方法的优势**
| 维度 | 传统方法 | Q-Anchor |
|------|--------|---------|
| **灵活性** | 静态嵌入，固定不变 | 动态响应不同query，一模型支持多场景 |
| **泛化性** | 依赖大规模参数或特定架构 | 通过预训练+prompt tuning实现高效迁移 |
| **部署效率** | 多模型并行，资源消耗大 | 单模型共享前缀，增量成本极低 |
| **可解释性** | 黑箱特征融合 | prompt tuning带来注意力偏移，可视化验证 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **UserU**：工业级预训练数据集，包含两个部分：
  - **Dfuture**：基于历史行为预测未来动作的监督样本（行为→行为）。
  - **Duqa**：由LLM生成的“用户行为→语义理解”问答对，覆盖72个生活主题（如财务规划、健康习惯）。
- **Alipay 生产环境测试基准**：10个真实二分类任务，涵盖三大领域：
  - **User Engagement**（活跃度预测）
  - **Risk Control**（欺诈、洗钱检测）
  - **Marketing Sensitivity**（外卖、品牌偏好识别）

### **实验设置与评估指标**
- **模型架构**：基于 `Qwen2.5-0.5B-Instruct` 作为LLM backbone，配合 modality-specific encoders（如 gte-base）处理多源行为。
- **训练配置**：
  - 预训练步数：50k steps
  - Batch size：2048
  - 使用 LoRA（rank=64, α=32）进行参数高效微调
  - 用户嵌入维度：128
- **评估方式**：
  - **Linear Probing**：冻结用户嵌入，训练线性分类器进行下游任务评估。
  - **主要指标**：
    - **AUC**（ROC曲线下面积）：衡量分类性能
    - **KS**（Kolmogorov-Smirnov）：衡量正负样本排序分离度，尤其适用于风控场景

### **基线方法对比**
分为两类：
1. **通用文本嵌入模型**：
   - Qwen2.5-0.5B-Instruct（无微调）
   - Qwen3-Embedding-8B
   - Llama-Embed-Nemotron-8B
   - KaLM-Embedding-Gemma3-12B
2. **专用用户表征模型**：
   - MSDP, One4all, CPC（对比学习类）
   - FOUND（基于LLM的用户基础模型）

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 方法 | 平均 AUC | 平均 KS |
|------|--------|-------|
| Llama-Embed-Nemotron-8B | 0.7488 | 0.3805 |
| FOUND | 0.7832 | 0.4529 |
| Q-Anchor (Base) | **0.8104** | **0.5044** |
| Q-Anchor (Prompt Tuned) | **0.8225** | **0.5267** |

> ✅ **相对最强基线提升**：
> - AUC ↑ +9.84%（vs Llama-Embed）
> - KS ↑ +38.4%（vs Llama-Embed）

### **与基线方法的对比结果**
- 在所有10个任务上均取得 **SOTA 表现**，尤其在 **Risk 和 Marketing** 场景优势显著。
- **Money Laundering Detection**：AUC 达 0.9439，远超 FOUND (0.9235) 和 MSDP (0.8746)，说明能有效抑制交易噪声，聚焦决策相关模式。
- **Brand Sensitivity**：AUC 从 Base 的 0.7979 提升至 Prompt Tuned 的 0.8535，表明模型能捕捉细微偏好信号，并通过prompt tuning精准调整边界。

### **消融实验结果**
#### （1）组件消融（Q-Anchor Base）
| 移除组件 | Avg AUC ↓ | Avg KS ↓ | 结论 |
|--------|----------|--------|------|
| User Token | 0.8086 | 0.5002 | 显式结构标记有助于证据溯源 |
| Modal Token | 0.8088 | 0.5008 | 同上 |
| Both Tokens | 0.8065 | 0.4966 | 结构信息重要 |
| Contrastive Loss | 0.7667 | 0.4215 | 对比学习是主导信号 |
| NTP Loss | 0.8061 | 0.4961 | 辅助任务增强局部建模 |
| Margin Filter | 0.8047 | 0.4877 | 减少假负例干扰 |

#### （2）预训练必要性
- **without pretrain**：AUC 降至 0.7782，KS 下降 11.2%，证明预训练提供了关键的行为先验（behavioral prior），不可替代。

#### （3）Prompt Tuning 可扩展性分析**
- **Prompt Tokens 数量**：性能在 **6个token时饱和**，更多token收益递减。
- **Training Steps**：500步达到最优，收敛稳定。
- 结论：**轻量级prompt即可实现高效专业化**。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Query-as-Anchor 实现“一模型多用”范式**：
   - 同一套编码器可在不同query下生成适配场景的嵌入，打破“一个任务一个模型”的僵局。
2. **预训练质量 > 模型规模**：
   - 在固定预算下，**增加数据量比扩大模型更有效**；0.5B 模型优于 1.5B/3B。
   - 更大模型梯度衰减严重（gradient attenuation），优化困难。
3. **Soft Prompt Tuning 是高效的场景适配机制**：
   - 仅更新少量prompt向量即可显著提升性能，且可通过注意力变化解释其作用（如Takeout任务中Bill模态关注度↑26%）。
4. **KV-cache 实现工业级高效部署**：
   - 用户前缀只编码一次，新增场景几乎零额外开销，适合高并发线上系统。

### **方法的局限性**
- **依赖高质量prompt设计**：虽然使用LLM自动生成QA对，但在极端长尾场景可能仍需人工干预。
- **对query语义敏感**：若query表述模糊或歧义，可能导致嵌入漂移。
- **当前未探索多query联合推理**：目前每次仅处理单一query。

### **未来工作方向**
1. **突破Scaling Paradox**：
   - 探索如何恢复大模型（>1B）的训练梯度，使其也能受益于更大容量。
2. **动态Prompt生成**：
   - 自动根据业务目标生成最优prompt，减少人工依赖。
3. **跨平台迁移能力验证**：
   - 当前基于Alipay生态构建，未来可在其他平台（如电商、社交）验证泛化性。
4. **引入时序显式建模**：
   - 当前层次编码隐含时间信息，未来可显式加入时间戳或周期性信号。

---

> 🔗 **代码已开源**：[https://github.com/JhCircle/Q-Anchor](https://github.com/JhCircle/Q-Anchor)  
> 📌 **工业落地验证**：在Alipay两大场景A/B测试中表现优异：
> - **IVR现金召回**：提款率↑12.5%，余额↑5.3%
> - **信贷逾期风险识别**：KS ↑1.96%

</details>

---

### 5. [Fast Catch-Up, Late Switching: Optimal Batch Size Scheduling via Functional Scaling Laws](https://arxiv.org/abs/2602.14208)

**Authors**: Jinbo Wang, Binghui Li, Zhanpeng Zhou, Mingze Wang, Yuxuan Sun, Jiaqi Zhang, Xunliang Cai, Lei Wu  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.14208v1  

#### Abstract
Batch size scheduling (BSS) plays a critical role in large-scale deep learning training, influencing both optimization dynamics and computational efficiency. Yet, its theoretical foundations remain poorly understood. In this work, we show that the functional scaling law (FSL) framework introduced in...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Fast Catch-Up, Late Switching: Optimal Batch Size Scheduling via Functional Scaling Laws

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文研究了大规模深度学习训练中的 **Batch Size Scheduling (BSS)** 问题，旨在从理论上解释为何在实际大模型预训练（如 GPT-3、PaLM、LLaMA-3 等）中广泛采用动态调整 batch size 的策略，并揭示其最优调度结构。

尽管 BSS 在工业界广泛应用，但其理论基础长期缺乏系统分析，现有工作多依赖经验调参或启发式设计。本文填补了这一空白，首次基于 **Functional Scaling Law (FSL)** 框架对 BSS 进行了原则性的理论建模与优化分析。

---

### 提出了什么新方法或新思路

#### （1）提出 **Functional Scaling Law (FSL)** 框架下的 BSS 最优调度理论
- 将 BSS 建模为一个受数据预算约束的变分优化问题。
- 推导出在不同任务难度下（由 source exponent $s$ 决定），最优 batch size schedule 的解析形式：
  - **Easy-task regime ($s > 1 - 1/\beta$)**：batch size 应在整个训练过程中单调递增。
  - **Hard-task regime ($s \leq 1 - 1/\beta$)**：应保持小 batch size 训练大部分时间，仅在后期切换到大 batch size（称为“late switching”）。

#### （2）发现并命名 **Fast Catch-Up Effect**
- 当从小 batch 切换到大 batch 后，loss 会迅速“坍缩”至一直使用大 batch 训练的轨迹上。
- 这种现象源于模型对早期梯度噪声的快速遗忘（forgetting of accumulated gradient noise），且任务越难（$s$ 越小），catch-up 越快。
- 该机制解释了为何可以安全地将大 batch 推迟到后期而不损失性能。

#### （3）提出 **Late-Switching Principle**
- 对于困难任务，延迟增大 batch size 不仅不会损害最终性能，反而能显著减少 token 消耗，提升训练效率。
- 该原则具有强泛化性，在 Dense 和 MoE 架构、多种规模下均成立。

---

### 相比现有方法的优势

| 方面 | 现有方法 | 本文方法 |
|------|--------|---------|
| **理论基础** | 缺乏统一框架，依赖 heuristics 或 control theory | 基于 FSL 提供可解释、可预测的解析解 |
| **指导意义** | 需大量 trial-and-error 调参 | 可通过小规模实验拟合 scaling law 来外推最优切换点 |
| **效率优势** | 固定 batch size 浪费计算资源 | Late-switch 减少前期高成本通信开销，节省 token |
| **适用性** | 多针对 constant batch size | 明确区分 easy/hard task 并给出差异化策略 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

| 实验类型 | 数据集 | 描述 |
|--------|-------|------|
| **Small-scale LLM 实验** | C4 dataset | 公共文本语料，用于预训练 50M–492M 参数的 LLaMA 模型 |
| **Large-scale LLM 实验** | 私有真实世界 LLM 数据集 | 更贴近工业级部署场景，训练高达 1.1B 参数的 MoE 模型 |
| **理论验证实验** | Feature-space linear regression | 合成数据，用于验证 FSL 框架下的理论预测 |

---

### 实验设置和评估指标

#### 模型架构
- **Dense 模型**：LLaMA 架构（RoPE, SwiGLU, RMSNorm）
- **Sparse 模型**：Shortcut-connected MoE（ScMoE），支持高效专家并行

#### 训练配置
- **Optimizer**：AdamW（$\beta_1=0.9$, $\beta_2=0.95$, weight decay=0.1）
- **Sequence Length**：256（small-scale）、8192（large-scale）
- **Learning Rate**：常数或 cosine decay（扩展实验）
- **Batch Size Schedule**：两阶段或多阶段切换（e.g., 640 → 1280）

#### 评估指标
- **主指标**：Validation loss（越低越好）
- **辅助分析**：
  - Loss 曲线对齐情况（验证 fast catch-up）
  - 不同 switching point 下的最终 loss
  - Scaling law 拟合程度（$\log(D-P)$ vs $\log D$）

---

### 基线方法对比

| 基线方法 | 描述 |
|--------|------|
| **Constant Small Batch** | 全程使用最小 batch size，样本效率高但硬件利用率低 |
| **Constant Large Batch** | 全程使用最大 batch size，硬件高效但样本浪费严重 |
| **Early-Switch Schedule** | 在训练早期就切换到大 batch（常见错误做法） |
| **Multi-stage Baseline** | 分多个阶段逐步增加 batch size，但切换时机非最优 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 设置 | 最优 switching point | 性能提升（vs 基线） | 数据来源 |
|-----|------------------|------------------|----------|
| 50M LLaMA on C4 | ~70% token 处切换 | ↓ 0.1–0.2 in val loss | 图4 |
| 1B MoE on 0.4T tokens | 切换至 2x/4x batch size 在后期 | ↓ 0.15–0.3 loss | 图5 |
| 1.1B MoE on 1T tokens | 切换至 2x batch size 在 600B token | 显著优于 early-switch | 图5 |
| Linear Regression (hard task) | $D-P \sim D^{0.813}$ | 完美符合理论 scaling law | 图4（右） |

> ✅ 所有实验中，“late-switch” consistently outperforms constant-batch and early-switch baselines.

---

### 与基线方法的对比结果

| 对比项 | 结果 |
|------|------|
| **vs Constant Large Batch** | Late-switch 达到相同最终 loss，但 token 消耗降低 30–50% |
| **vs Constant Small Batch** | 最终 loss 更低，因后期大 batch 有效抑制噪声 |
| **vs Early-Switch** | Early-switch 收敛慢、最终 loss 更高，无法触发 fast catch-up 效应 |
| **vs Multi-stage（非 late）** | 即使多阶段调度，若未 late-switch，仍劣于 late-switch 版本 |

> 🔍 图3 显示：无论 Dense/MoE、0.5B/1.1B，只要在 late 阶段切换，loss 都能快速对齐大 batch 轨迹。

---

### 消融实验结果

#### （1）Switching Time 消融（图4 左）
- 在 50M–200M 模型上扫描 switching ratio（0% 到 100%）
- 发现存在明显谷值，最佳切换点集中在 **60–80% 训练进度之间**
- 早于或晚于此区间都会导致性能下降

#### （2）Scaling Law 验证（图4 右）
- 在不同数据量 $D$ 下拟合 $D-P^*$（剩余 token 数）
- 得到 $\log(D-P) \approx 0.813 \log D + \log c$，$R^2=0.99$
- 表明可通过小规模 pilot 实验外推大规模最优切换点

#### （3）Learning Rate Interaction（附录 B.6）
- 在 cosine decay LR 下复现实验
- 观察到 fast catch-up 依然存在，只是 merge 速度略缓（因 intrinsic time 变化）
- 说明 FSL 机制具有鲁棒性，不局限于 constant LR

---

## 4. 关键结论和发现

### 论文的主要发现

1. ✅ **任务难度决定最优 BSS 结构**  
   - Easy task：全程大 batch 或单调增 batch size  
   - Hard task：必须 late-switch —— 小 batch 主导前期，大 batch 仅用于后期微调

2. ✅ **Fast Catch-Up 是 late-switch 成立的关键机制**  
   - 切换后 loss 快速对齐 constant large-batch 轨迹
   - 源于 high-capacity 模型对历史噪声的快速遗忘
   - 任务越难（$s$ 越小），catch-up 越快

3. ✅ **Late-switching 是一种高效且通用的设计原则**  
   - 在 Dense / MoE / 50M–1.1B / 10B–1T tokens 上均验证有效
   - 可减少 token 消耗，同时保持甚至提升最终性能

4. ✅ **FSL 框架具备强大表达力**  
   - 虽源自 linear/kernel regression，但在真实 LLM 训练中高度吻合
   - 为理解 SGD 动力学提供了 principled lens

---

### 方法的局限性

| 局限性 | 说明 |
|-------|------|
| **假设 constant learning rate** | 实际训练常用 warmup-stable-decay 或 cosine decay；虽附录显示现象仍存在，但理论尚未完全覆盖 |
| **基于标准 SGD 分析** | 当前 FSL 推导基于 SGD，而现代 LLM 多用 AdamW 等 adaptive optimizer |
| **MoE 通信开销未显式建模** | ScMoE 虽降低通信代价，但 BSS 对通信-计算重叠的影响有待深入分析 |
| **task difficulty $s$ 难以直接测量** | 实践中需通过 pilot 实验间接估计，缺乏自动化工具 |

---

### 未来工作方向

1. 🔄 **扩展至 adaptive optimizers**（如 AdamW、Adafactor）
   - 建立 adaptive-FSL 框架，统一分析 LR 和 BSS 联合调度

2. ⚖️ **联合优化 Learning Rate 与 Batch Size Schedule**
   - 研究 warmup/stable/decay 阶段如何影响 fast catch-up 和 late-switch 效益

3. 📊 **开发自动 tuning 工具**
   - 基于 scaling law，构建可根据小规模实验预测最优 BSS 的系统

4. 🧠 **连接 loss landscape 与 FSL 动力学**
   - 从 sharpness/flatness 角度解释为何 late-switch 更易收敛到平坦区域

5. 🌐 **应用于其他模态与任务**
   - 验证图像、语音等领域的 BSS 是否也遵循类似规律

---

> 💡 **一句话总结**：  
> 本文通过 Functional Scaling Law 揭示了 **“难任务应晚增大 batch size”** 的普适原则，并发现了 **Fast Catch-Up** 这一关键动力学机制，为大规模语言模型训练提供了坚实的理论依据和高效的实践指南。

</details>

---

### 6. [AllMem: A Memory-centric Recipe for Efficient Long-context Modeling](https://arxiv.org/abs/2602.13680)

**Authors**: Ziming Wang, Xiang Wang, Kailong Peng, Lang Qin, Juan Gabriel Kostelec, Christos Sourmpis, Axel Laborieux, Qinghai Guo  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.13680v1  

#### Abstract
Large Language Models (LLMs) encounter significant performance bottlenecks in long-sequence tasks due to the computational complexity and memory overhead inherent in the self-attention mechanism. To address these challenges, we introduce \textsc{AllMem}, a novel and efficient hybrid architecture tha...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AllMem: A Memory-centric Recipe for Efficient Long-context Modeling

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在处理**长序列任务**时面临两大瓶颈：
- **计算复杂度高**：标准 Self-Attention 的时间和空间复杂度均为 $O(L^2)$ 或 $O(L)$，随序列长度 $L$ 增长而急剧上升。
- **内存开销大**：KV Cache 随上下文增长线性膨胀，在边缘设备（如手机）上难以维持超长上下文。

此外，现有高效注意力机制（如 Linear Attention、Sliding Window Attention）常因信息压缩导致**语义建模能力下降**，出现“灾难性遗忘”或性能损失。

---

### 🚀 提出的新方法与核心思想
本文提出 **ALLMEM** ——一种新型混合架构，融合了两种互补的记忆机制：

#### （1）**双通道并行 Token Mixer 设计**
- **短时记忆通道**：采用 **Sliding Window Attention (SWA)**，捕捉局部精细依赖关系。
- **长时记忆通道**：引入基于 **非线性 Test-Time Training (TTT)** 的参数化记忆网络（ALLMEM），实现在线学习式的全局信息压缩与持久记忆维护。

> 这种设计将传统 Attention 的“被动存储”转化为“主动学习”的记忆系统。

#### （2）**可微分、端到端训练的 TTT 内存模块**
- 使用残差连接的 **SwishGLU 非线性单元** 替代传统的线性 SSM（如 Mamba），增强表达力。
- 在推理阶段通过梯度更新动态优化 Memory 参数（即 Test-Time Learning），最小化重建误差。
- 引入输入自适应的学习率（meta learning rate）和动量衰减系数，提升稳定性。

#### （3）**高效的微调迁移框架**
- 提出 **Memory-Efficient Fine-Tuning** 策略：冻结原始模型大部分权重，仅微调新增的 ALLMEM 模块元参数（meta-parameters）。
- 支持将任意预训练 LLM 快速转换为 ALLMEM 架构，无需从头训练。

---

### 🔍 相比现有方法的优势
| 特性 | ALLMEM | 全注意力 | Linear Attention (e.g., Mamba) | SWA + Sink |
|------|--------|----------|-------------------------------|-------------|
| 计算复杂度（Decode） | $O(W)$ | $O(L)$ | $O(1)$ | $O(W)$ |
| 存储复杂度（Cache） | $O(1)$ | $O(L)$ | $O(1)$ | $O(W)$ |
| 是否保留全局建模能力 | ✅ 是 | ✅ 是 | ❌ 较弱 | ❌ 局部窗口限制 |
| 是否支持持续学习 | ✅ 动态 TTT 更新 | ❌ 静态 KV Cache | ⚠️ 线性递推 | ❌ 固定模式 |
| 是否易迁移至已有模型 | ✅ 支持轻量微调 | ✅ 可直接使用 | ❌ 需重训或蒸馏 | ✅ 可替换 |

> ✅ **优势总结**：
> - 实现了 **接近全注意力的建模精度**，同时具备 **极低的推理成本**；
> - 利用非线性 TTT 显著优于线性记忆模型（如 Mamba）在长程任务中的表现；
> - 支持 **高效知识继承**，避免灾难性遗忘。

---

## 2. 核心实验方法和设置

### 📚 数据集
| 类型 | 数据集 |
|------|-------|
| **短序列基准测试** | C-Eval (5-shot), ARC-Easy/Challenge, HellaSwag, WinoGrande, MMLU-Redux, GPQA-Diamond, IFEval, MATH-500, LiveCodeBench v5 |
| **长序列基准测试** | **LongBench**（平均 ~37k tokens）、**InfiniteBench**（最长达 128k–200k tokens）、**LV-Eval**（多长度层级，最高 256k） |
| **蒸馏训练数据** | CHATQA2（长文本）、CHINESEINSTRUCT 和 SFTv3（短指令数据） |

> 所有输入统一转为 CHATML 格式，并对齐 Qwen3 模板。

---

### ⚙️ 实验设置
- **基础模型**：Qwen3-0.6B 和 Qwen3-1.7B
- **窗口大小**：
  - LongBench：4k
  - InfiniteBench / LV-Eval：8k
- **Attention Sinks 数量**：128
- **训练策略**：
  - 使用 **知识蒸馏（Knowledge Distillation）**，目标是最小化学生模型（ALLMEM）与教师模型（原 Qwen3）输出分布之间的 KL 散度。
  - 采用 **On-Policy Distillation**（离线版）进一步对齐生成行为。
  - 蒸馏过程中随机采样窗口大小（512–8192）、Sink 数量（0–256）、chunk size（512–4096），提高泛化性。
- **优化器**：AdamW，最大学习率 $1\times10^{-4}$，Warmup-Stable-Decay 调度。
- **硬件配置**：启用 Gradient Checkpointing 和 DeepSpeed ZeRO-2。

---

### 🆚 基线方法对比
| 基线模型 | 描述 |
|--------|------|
| **Full Attention** | 原始 Qwen3 完整注意力机制（受限于显存无法处理超长序列） |
| **SWA + Sink** | 滑动窗口注意力 + “sink tokens” 保留关键历史信息 |
| **Mamba-enhanced** | 将 ALLMEM 替换为 Mamba-2 模块，其他结构一致，用于验证非线性记忆的重要性 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Tables 1–3）

#### （1）短序列任务性能（Table 1）
| 模型 | 平均得分 |
|------|---------|
| Qwen3-0.6B | ~50.8 |
| Qwen3-0.6B-ALLMEM | **~51.5**（多数任务持平或略优） |
| Qwen3-1.7B | ~61.3 |
| Qwen3-1.7B-ALLMEM | **~61.8**（全面持平且部分超越） |

> ✅ 结论：**ALLMEM 在短序列任务中几乎无损继承原模型能力**，甚至在多个任务上略有提升（如 MMLU、MATH）。

---

#### （2）长序列任务性能（Tables 2 & 3）

##### ▶ LongBench (~37k avg len)

| 模型 | Qwen3-0.6B | SWA-Sinks | Mamba | **ALLMEM** |
|------|------------|-----------|--------|-------------|
| 准确率（avg） | 28.16 | 25.71 | 27.01 | **27.33** |

| 模型 | Qwen3-1.7B | SWA-Sinks | Mamba | **ALLMEM** |
|------|------------|-----------|--------|-------------|
| 准确率（avg） | 33.55 | 30.31 | 31.57 | **32.12** |

> 🔺 ALLMEM 接近全注意力性能，显著优于 SWA 和 Mamba 基线。

---

##### ▶ InfiniteBench & LV-Eval（128k context）

| 模型 | Qwen3-0.6B | SWA-Sinks | Mamba | **ALLMEM** |
|------|------------|-----------|--------|-------------|
| 准确率（avg） | 5.16 | 3.27 | 3.97 | **4.52** |

| 模型 | Qwen3-1.7B | SWA-Sinks | Mamba | **ALLMEM** |
|------|------------|-----------|--------|-------------|
| 准确率（avg） | 5.29 | 3.76 | 4.36 | **5.56** |

> ✅ **亮点结果**：
> - ALLMEM-1.7B 在 **128k 上下文下以仅 8k 窗口，准确率反超 Full Attention 基线**！
> - 在 factrecall_en 等需要精确回忆的任务上，ALLMEM 表现尤为突出（得分从 1.8 → 6.83）。

---

#### （3）效率指标（Fig. 3）
当上下文长度达到 **128k** 时：

| 指标 | ALLMEM vs Full Attention |
|------|--------------------------|
| **FLOPs（计算量）** | ↓ 约 **9倍减少**（仅为 1/9） |
| **KV Cache 大小（FP32）** | ↓ 从线性增长变为 **恒定大小 $O(1)$** |

> 💡 举例：对于 Qwen3-0.6B，Full Attention 的缓存需求在 128k 时超过 1GB，而 ALLMEM 维持在约 80MB 不变。

---

#### （4）消融实验与关键发现
- **初始化门控系数 $\alpha=0$**：让模型初始偏向 SWA，有助于稳定训练。
- **归一化时机改进**：提出“先读再归一”机制（read-before-normalize），防止早期记忆被破坏。
- **非线性 SwishGLU 单元**：相比线性 SSM 明显提升建模能力，尤其在复杂推理任务中。
- **动态学习率与动量控制**：显著提升 TTT 收敛速度与鲁棒性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **ALLMEM 成功弥合了高效注意力与全注意力之间的性能鸿沟**：
   - 在长达 128k 的上下文中，仅用 4k–8k 局部窗口即可逼近甚至超越全注意力的表现。
2. **非线性 Test-Time Training 是关键突破**：
   - 参数化的、可训练的记忆单元比线性递推更具表达力，能有效缓解局部注意力的信息丢失问题。
3. **真正的 $O(1)$ 存储与 $O(W)$ 解码复杂度**：
   - 实现了真正意义上的“无限上下文”潜力，适用于移动端和边缘部署。
4. **兼容性强，易于迁移**：
   - 可通过少量微调将任何预训练 LLM 转换为 ALLMEM 架构，极大降低应用门槛。

---

### ⚠️ 方法的局限性
1. **TTT 动态更新带来额外延迟**：
   - 尽管解码计算量低，但每次 token 生成后需执行一次 mini-gradient update，可能影响实时性。
2. **对超参数敏感**：
   - 如 chunk size、learning rate schedule 等需仔细调整，否则可能导致训练不稳定。
3. **当前未完全释放“无限长度”潜力**：
   - 实验仍受限于 tokenizer 最大长度（如 128k 截断），尚未验证更长序列下的极限表现。

---

### 🔮 未来工作方向
1. **与外部记忆系统结合**：
   - 将 ALLMEM 的参数化长期记忆与 RAG、Engram 等外部检索系统集成，构建**多层次记忆体系**（短期感知 + 长期知识库）。
2. **探索更高效的 TTT 优化器**：
   - 设计专用的轻量级在线优化算法，降低每步更新开销。
3. **扩展至多模态场景**：
   - 应用于视频理解、长音频处理等跨模态长序列任务。
4. **硬件协同设计**：
   - 开发针对 ALLMEM 架构定制的推理引擎（如适配 vLLM、TensorRT-LLM）。

---

## 总结一句话
> **ALLMEM 通过“滑动窗口 + 非线性 TTT 记忆”的混合架构，在几乎不牺牲性能的前提下，实现了 LLM 长上下文建模的计算与存储双重高效化，是迈向实用化“无限上下文 AI”的重要一步。**

</details>

---

### 7. [Concept Influence: Leveraging Interpretability to Improve Performance and Efficiency in Training Data Attribution](https://arxiv.org/abs/2602.14869)

**Authors**: Matthew Kowal, Goncalo Paulo, Louis Jaburi, Tom Tseng, Lev E McKinney, Stefan Heimersheim, Aaron David Tucker, Adam Gleave, Kellin Pelrine  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.14869v1  

#### Abstract
As large language models are increasingly trained and fine-tuned, practitioners need methods to identify which training data drive specific behaviors, particularly unintended ones. Training Data Attribution (TDA) methods address this by estimating datapoint influence. Existing approaches like influe...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Concept Influence: Leveraging Interpretability to Improve Performance and Efficiency in Training Data Attribution*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

当前的 **Training Data Attribution (TDA)** 方法（如 **influence functions**）存在两个关键缺陷：

1. **计算成本高**：基于 Hessian 矩阵逆的梯度方法在大模型上难以扩展。
2. **语义偏差（syntax-level bias）**：传统方法依赖单个测试样例（single test example），导致归因偏向于**表面相似性**（syntactic/lexical similarity），而非深层语义行为（如“sycophancy”或“evil”等抽象特质）。

这使得 TDA 难以有效识别驱动复杂、抽象模型行为（如 misalignment）的关键训练数据。

---

### 提出了什么新方法或新思路

本文提出 **Concept Influence**，一种将可解释性结构融入 TDA 的新框架：

- **核心思想**：不再将 influence 计算在原始输出或单个测试样本上，而是将其定义在**语义方向（semantic directions）** 上，例如：
  - **Linear Probes**
  - **Sparse Autoencoder (SAE) Features**
  - **Persona Vectors**（如“evil”或“sycophantic”方向）

- **公式化改进**：
  - 传统 influence function：
    $$
    \mathcal{I}_{\text{loss}}(z_{\text{train}}, z_{\text{test}}) = -\nabla_\theta \mathcal{L}(z_{\text{test}})^\top H^{-1} \nabla_\theta \mathcal{L}(z_{\text{train}})
    $$
  - **Concept Influence**：
    $$
    \mathcal{I}_v(z_{\text{train}}) = \nabla_\theta f_v(x_{\text{test}})^\top H^{-1} \nabla_\theta \mathcal{L}(z_{\text{train}})
    $$
    其中 $ f_v(x) = \langle v, a_l(x) \rangle $ 是某层激活在概念向量 $ v $ 上的投影。

此外，作者还提出两种**高效近似方法**：
- **Projection Difference**：衡量 fine-tuning 前后激活差异与概念向量的对齐程度。
- **Vector Filter**：仅计算训练样本激活与概念向量的点积，无需生成响应。

---

### 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **语义相关性** | 能更准确识别与目标抽象行为（如“evil”）相关的训练数据，减少表面匹配噪声。 |
| **可扩展性** | 近似方法（如 Vector Filter）比 influence function 快 **20× 以上**（见 Table 1）。 |
| **可控性** | 用户可预先定义目标概念（如通过 probe 构造“anti-safety”方向），实现定向行为控制。 |
| **可解释性增强** | 结合 SAE 可进行 **group influence** 分析，揭示哪些语义特征簇最影响行为。 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 合成数据集（用于评估 **Emergent Misalignment, EM**）：
- **Misaligned Opinions**：政治敏感话题，misaligned 样本含阴谋论、偏见。
- **Bad Medical Advice**：医疗建议，misaligned 样本提供危险建议（如跳过胰岛素）。
- **Insecure Code**：代码补全任务，misaligned 样本含 SQL 注入、命令注入漏洞。
- **GSM8K Mistakes**：数学题，misaligned 样本引入逻辑错误或误导推理。

所有数据集均为 50% benign + 50% misaligned，用于 LoRA 微调。

#### 真实世界数据集：
- **OpenAssistant v1 (OASST1)**：超 10k 对话树，含约 2–3% 毒性内容，用于评估 post-training 安全性-能力权衡。

---

### 实验设置和评估指标

#### 模型：
- **Qwen2.5-7B**, **Llama3.1-8B**

#### 微调方式：
- 单 epoch LoRA 微调。

#### 评估方式：
1. **数据过滤重训练实验**：
   - 使用不同 TDA 方法对训练数据排序。
   - 移除“最有害”或保留“最安全”的 top-K 数据。
   - 重新微调并用 **LLM Judge** 评估“evil”或“sycophancy”得分（0–100）。
2. **安全-能力权衡分析**：
   - 在 OASST1 上过滤数据，报告：
     - **安全性**：LLM Judge 的“evil”分数。
     - **能力**：MTBench 分数（多轮指令遵循能力）。

#### 评估指标：
- 下游 trait score（越低越好）
- AUC, Precision, Recall（有标签时）
- 计算时间（效率对比）

---

### 基线方法对比

| 方法 | 类型 | 是否需要 Hessian 逆 |
|------|------|------------------|
| **Influence Function** | 梯度法（二阶） | ✅ |
| **Concept Influence** | 梯度法（二阶，基于 concept） | ✅ |
| **Projection Difference** | 一阶近似（probe-based） | ❌ |
| **Vector Filter** | 零阶近似（仅点积） | ❌ |
| **Random** | 随机基线 | — |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）在合成 EM 数据上的表现（图 2, 图 11–13）

- **Concept Influence 表现最优**：
  - 在所有数据集和 trait（evil/sycophancy）上，移除 top 影响数据后，trait score 下降最显著。
  - 保留 top 数据时，trait score 提升最多，说明其能精准放大目标行为。

- **Projection Difference 竞争力强**：
  - 在 domain-close 场景（如 Medical → Evil）表现接近甚至优于 influence function。
  - 但在 domain-shift 场景（如 GSM8K → Evil）性能下降明显。

- **Vector Filter 效率极高但鲁棒性弱**：
  - 在部分场景失效（如 GSM8K），因其忽略模型生成差异。

#### （2）真实数据集 OASST1 上的安全-能力权衡（图 5）

- **Vector Filter 表现最佳**：
  - 仅用 **5% 最安全数据**，即可达到完整数据集的能力水平（MTBench ≈ 67），同时将“evil”分数从 2.3 降至 **0.8**。
  - 显著改善了 safety-capability Pareto frontier。

- **Concept Influence 与 Influence Function 接近**：
  - 在小数据量下略逊于 Vector Filter，但在 >10% 数据时表现更好。

#### （3）效率对比（表 1）

| 方法 | 时间 (s) | 相对加速比 |
|------|--------|----------|
| Influence Function | 1161 | 1.0× |
| Concept Influence | 1170 | 1.0× |
| Projection Difference | 142 | 8.2× |
| **Vector Filter** | **57** | **20.4×** |

> ⚠️ 注意：未计入 Hessian 逆的计算开销，实际差距更大。

#### （4）消融实验与相关性分析（图 3, 图 15）

- **方法间相关性低**：
  - Vector-based 方法（Vector Filter, Projection Difference, Concept Influence）之间相关性高。
  - 与 gradient-based 方法（Influence Function）相关性低，表明捕捉的是不同类型的“influence”。

- **Concept Influence 更聚焦语义**（图 4）：
  - Influence Function 归因到“法律术语”、“税收减免”等与查询语法相关但无关“evil”的特征。
  - Concept Influence 成功定位到“历史压迫”、“阴谋论”、“犯罪性”等真正与“evil”相关的 SAE 特征。

- **概念级过滤更高效**（图 15）：
  - Concept Influence 将影响集中在更少的 high-impact 概念上，覆盖 70% 数据仅需 top 20% 概念。
  - 在移除 25–80% 数据时，precision 明显高于 Influence Function。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **将可解释性结构（如 probe, SAE）融入 TDA 可显著提升语义准确性**。
2. ✅ **Concept Influence 在性能上优于传统 influence functions**，尤其在识别抽象 misaligned 行为方面。
3. ✅ **简单的一阶/零阶近似方法（如 Vector Filter）在多数场景下性能接近甚至超越梯度法，且快 20× 以上**。
4. ✅ **概念级归因（concept-based filtering）比数据点级更高效、更具解释性**。
5. ✅ **在真实 post-training 场景中，基于 concept 的过滤可显著改善 safety-capability tradeoff**。

---

### 方法的局限性

1. **依赖高质量 concept 向量**：
   - 若 probe 或 SAE 特征无法准确表示目标行为（如 poorly trained probe），效果会下降。
2. **Vector Filter 忽略 fine-tuning 动态**：
   - 不考虑 base model 已生成的内容，可能误判“看似相关但无影响”的数据。
3. **仍受限于 SAE 的覆盖范围**：
   - 当前 SAE 仅能捕捉部分神经元激活模式，非所有语义都能被稀疏表示。
4. **对 domain-shift 场景敏感**：
   - Projection Difference 和 Vector Filter 在跨域任务中性能不稳定。

---

### 未来工作方向

1. **扩展至更丰富的可解释结构**：
   - 如 circuits、mechanistic interpretability 中的因果路径。
2. **开发 principled 的 group-level attribution 方法**：
   - 基于 SAE 或聚类自动识别语义簇并归因。
3. **系统比较 base vs. fine-tuned 模型的 representation geometry**：
   - 区分归因是来自训练动态还是表示结构本身。
4. **扩展至多模态与更大规模模型**：
   - 验证方法在视觉、音频等模态下的有效性。
5. **结合主动学习或数据编辑**：
   - 利用 concept influence 指导数据清洗或合成反事实样本。

---

> **总结一句话**：  
> 本文通过将 **interpretable representations**（如 probe、SAE）引入 **Training Data Attribution**，提出了 **Concept Influence** 框架，在保持甚至提升归因准确性的同时，实现了高达 **20× 的效率提升**，为大规模语言模型的行为控制提供了更可扩展、更可解释的新工具。

</details>

---

### 8. [HBVLA: Pushing 1-Bit Post-Training Quantization for Vision-Language-Action Models](https://arxiv.org/abs/2602.13710)

**Authors**: Xin Yan, Zhenglin Wan, Feiyang Ye, Xingrui Yu, Hangyu Du, Yang You, Ivor Tsang  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.13710v1  

#### Abstract
Vision-Language-Action (VLA) models enable instruction-following embodied control, but their large compute and memory footprints hinder deployment on resource-constrained robots and edge platforms. While reducing weights to 1-bit precision through binarization can greatly improve efficiency, existin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HBVLA: Pushing 1-Bit Post-Training Quantization for Vision-Language-Action Models

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题  
Vision-Language-Action (VLA) 模型在机器人控制中表现出色，但其**巨大的计算和内存开销**限制了在资源受限设备（如边缘机器人）上的部署。虽然 Post-Training Quantization (PTQ) 是一种高效的压缩手段，但现有的 1-bit 量化方法在应用于 VLA 模型时面临严重挑战：

- **量化误差累积**：由于 VLA 输出的是连续动作，在闭环执行过程中，微小的动作偏差会被动力学放大，导致任务失败（如抓取不稳定、轨迹漂移）。
- **模态不平衡与异常值干扰**：标准 Hessian 方法无法准确识别对策略关键的权重，因为激活图受背景噪声和视觉 token 数量失衡的影响严重（即“双重主导问题”）。
- **跨模态权重混合破坏 Haar 变换效果**：直接应用 Haar 变换会导致不同模态列之间产生剧烈跳变，引入高频噪声。

---

### 🚀 提出的新方法：HBVLA 框架

作者提出 **HBVLA** —— 一个专为 VLA 定制的 1-bit PTQ 框架，包含三个核心设计：

#### （1）**Policy-Aware Enhanced Hessian**
- 引入基于梯度的重要性矩阵 $ S_t $，重新加权每个 token 对动作生成的影响。
- 构建修正后的 Hessian：$ H = \sum_t S_t X_t X_t^T $
- 有效抑制冗余视觉 token 和 outlier 的影响，精准定位对策略敏感的关键权重。

#### （2）**Sparse Orthogonal Transform for Non-Salient Weights**
- 针对非显著权重，先进行稀疏正交变换（通过排列矩阵 $ P $），将相似列聚类在一起。
- 减少 Haar 变换中的跨模态跳跃，降低高频能量，提升能量紧致性（energy compaction）。

#### （3）**Group-wise 1-bit Quantization in Haar Domain**
- 在 Haar 域中对显著和非显著权重分别处理：
  - 显著权重：采用残差感知的 Column-Haar Quantization，补偿近似误差。
  - 非显著权重：使用 Row-Haar Quantization + **共享均值 binarization**（shared-mean binarization），减少元数据开销。
- 利用频率感知分组策略，进一步优化精度与效率平衡。

---

### 🔍 相比现有方法的优势

| 维度 | HBVLA vs. 现有方法 |
|------|------------------|
| **针对性** | 专为 VLA 设计，考虑动作输出的连续性和闭环稳定性；而 BiLLM/HBLLM/BiVLM 主要面向 LLM/VLM，不关注物理执行鲁棒性 |
| **准确性** | 政策感知 Hessian 更好地保留关键语义路径，避免注意力漂移 |
| **结构适应性** | Sparse transform 解决了多模态权重交错带来的 Haar 性能下降问题 |
| **存储效率** | Shared-mean + 分组量化显著降低平均 bit-width 和 metadata 开销 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与平台

| 平台 | 描述 |
|------|------|
| **LIBERO** | 包含 100 个任务、5000 条轨迹的仿真基准，涵盖空间、对象、目标、长视野四类任务（LIBERO-Spatial/Object/Goal/Long），基于 MuJoCo 中的 Franka 机械臂 |
| **SIMPLER** | 高保真模拟环境，复现真实世界场景，支持 Google 机器人手臂测试，包含两种设定：<br>• Visual Matching（低干扰）<br>• Variant Aggregation（高随机性：光照、背景、干扰物） |
| **Real-world: Mobile ALOHA** | 实体双臂协作机器人平台，用于验证现实部署能力，测试三大任务：<br>• Pick and Place（不规则物体）<br>• Sequenced Instruction（汉诺塔）<br>• Flexible Folding（毛巾折叠） |

---

### 📊 实验设置与评估指标

| 项目 | 设置 |
|------|------|
| **模型** | OpenVLA, OpenVLA-OFT, CogACT |
| **量化方式** | 仅对 vision encoder 和 language model 进行 1-bit 量化，其余保持 full precision（BF16） |
| **校准集** | 从训练集中随机采样 256 条轨迹 |
| **块大小** | 128 |
| **Haar 参数** | 固定核 $ h_{lo}=[0.5, 0.5], h_{hi}=[0.5, -0.5] $，stride=2 |
| **评估指标** | **Success Rate (SR)** —— 成功完成任务的比例 |

---

### ⚔️ 基线方法对比

| 方法 | 类型 | 特点 |
|------|------|------|
| **BiLLM** | LLM-oriented 1-bit PTQ | 基于 GPTQ 的 OBQ 方法，未考虑多模态特性 |
| **HBLLM** | Haar-based 1-bit PTQ for LLM | 使用 Haar 变换进行频域量化，但缺乏策略感知机制 |
| **BiVLM** | Multimodal 1-bit PTQ | 采用高斯分位数划分显著权重，但在 VLA 上未能捕捉动作相关列 |

> 所有 baseline 均适配至 VLA 架构，并统一量化 vision/language backbone。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### ✅ 在 **LIBERO** 上的表现（OpenVLA-OFT）

| 方法 | Spatial | Object | Goal | Long | Avg SR (%) | Δ vs FP |
|------|--------|--------|------|------|------------|---------|
| OpenVLA-OFT (FP) | 97.6 | 98.4 | 97.9 | 94.5 | **97.1** | — |
| BiLLM | 59.2 | 61.4 | 65.8 | 44.3 | 57.7 | -39.4 |
| BiVLM | 67.8 | 69.7 | 68.4 | 48.9 | 64.0 | -33.1 |
| HBLLM | 87.2 | 76.0 | 89.6 | 62.0 | 79.2 | -17.9 |
| **HBVLA (Ours)** | **89.3** | **97.8** | **91.3** | **82.7** | **90.3** | **-6.8** |

> 💡 **结论**：HBVLA 保留了 **92.2%** 的 full-precision 性能，远超 SOTA 方法。

---

#### ✅ 在 **SIMPLER** 上的表现（CogACT）

| 方法 | Visual Matching (Avg) | Variant Aggregation (Avg) | Overall Avg |
|------|------------------------|----------------------------|-------------|
| CogACT (FP) | 74.8 | 61.3 | — |
| BiLLM | 28.8 (-46.0) | 14.9 (-46.4) | — |
| BiVLM | 57.1 (-17.7) | 47.4 (-13.9) | — |
| HBLLM | 62.3 (-12.5) | 47.9 (-13.4) | — |
| **HBVLA (Ours)** | **70.0 (-4.8)** | **51.0 (-10.3)** | **60.5** |

> 💡 **结论**：HBVLA 在更具挑战性的 Variant Aggregation 下仍表现最优，说明其更强的鲁棒性。

---

#### ✅ 在 **Mobile ALOHA** 上的真实世界表现

| 任务 | FP Model | HBVLA | BiLLM | HBLLM |
|------|--------|--------|--------|--------|
| Pick and Place | 95.8% | 93.3% | 50.0% | 83.3% |
| Sequenced Instruction | 86.7% | 83.3% | 60.0% | 80.0% |
| Flexible Folding | 80.0% | 76.7% | 40.0% | 66.7% |

> 图 3 显示：HBVLA 在三项任务上均接近 full-precision 表现，且显著优于所有 baseline。

---

### 🔬 消融实验结果

#### （1）**Permutation Criterion for Non-Salient Columns**

| 准则 | Visual Matching ↓ | Variant Aggregation ↓ |
|------|------------------|-----------------------|
| $ l_1 $-norm | 11.6% | 15.6% |
| $ l_2 $-norm | **8.8%** | **12.8%** |

> ✅ $ l_2 $-norm 更好捕获列间能量分布，提升量化质量。

---

#### （2）**Policy-Aware Hessian 的有效性**

| Hessian 类型 | Visual Matching ↓ | Variant Aggregation ↓ |
|--------------|------------------|------------------------|
| Standard | 12.5% | 13.4% |
| **Policy-Aware** | **10.3%** | **12.1%** |

> ✅ 政策感知 Hessian 能更准确识别关键权重，减少任务失败率。

---

#### （3）**组件敏感性分析（Figure 4）**

| 组件 | 敏感度排序 |
|------|-----------|
| Vision Encoder | 最鲁棒（量化后性能下降最小） |
| Language Model | 中等敏感 |
| Projector / Action Head | **最敏感**，轻微精度损失即导致严重退化 |

> ❗ 结论：应优先保护 projector 和 action head 中的关键权重。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **VLA 模型各组件对量化敏感度差异大**：
   - Vision encoder 最鲁棒；
   - Projector 和 Action Head 极其敏感，需重点保护。

2. **传统 Hessian 不适用于 VLA**：
   - 受视觉 token 失衡和背景 outlier 影响，易误判重要权重；
   - 政策感知 Hessian 显著改善 saliency 估计准确性。

3. **跨模态权重交错破坏 Haar 变换效果**：
   - 直接应用 Haar 会因列间异质性引入高频噪声；
   - Sparse orthogonal transform 可有效缓解此问题。

4. **HBVLA 实现近乎无损的 1-bit 压缩**：
   - 在多个 benchmark 上保留 >90% 的 full-precision 性能；
   - 在真实机器人上仅造成轻微 success rate 下降，具备强 deployability。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **仅适用于 PTQ** | 不涉及 QAT，若允许微调可能获得更高性能 |
| **部分模块仍为 full precision** | 如 action head、proprioceptive projector 未完全量化，未来可探索端到端全模型 1-bit 化 |
| **Haar 变换层级有限** | 当前仅使用 one-level Haar，更深分解可能带来额外增益但增加复杂度 |
| **依赖局部梯度估计** | 虽然高效，但仍为近似方法，非全局最优解 |

---

### 🔮 未来工作方向

1. **扩展至更低比特（如 ternary 或 sub-1-bit）量化**
2. **结合 QAT 进行 fine-tuning + PTQ 协同优化**
3. **探索动态量化策略**：根据任务指令自适应调整量化粒度
4. **应用于更大规模 VLA 模型**（如 UniVLA、RT-2-X）
5. **硬件协同设计**：开发支持 Haar 域运算的专用加速器

---

## ✅ 总结

HBVLA 是首个专为 **Vision-Language-Action (VLA)** 模型设计的 **1-bit Post-Training Quantization** 框架，通过 **policy-aware Hessian + sparse orthogonal transform + Haar-domain group-wise quantization** 的组合，成功解决了极端量化下动作退化的问题。实验证明其在 **LIBERO、SIMPLER 和 Mobile ALOHA** 上均大幅超越现有 SOTA 方法，为大规模 VLA 模型在资源受限机器人平台上的高效部署提供了实用基础。

</details>

---

### 9. [Data-driven Bi-level Optimization of Thermal Power Systems with embedded Artificial Neural Networks](https://arxiv.org/abs/2602.13746)

**Authors**: Talha Ansar, Muhammad Mujtaba Abbas, Ramit Debnath, Vivek Dua, Waqar Muhammad Ashraf  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.13746v1  

#### Abstract
Industrial thermal power systems have coupled performance variables with hierarchical order of importance, making their simultaneous optimization computationally challenging or infeasible. This barrier limits the integrated and computationally scaleable operation optimization of industrial thermal p...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Data-driven Bi-level Optimization of Thermal Power Systems with embedded Artificial Neural Networks

---

## 1. 论文的主要贡献和创新点

### 解决的问题
工业热力发电系统中的性能变量具有**层级重要性结构**（如电厂级经济目标优先于锅炉燃料消耗优化），传统的多目标优化难以有效处理这种嵌套决策关系。直接求解双层优化（bi-level optimization）问题在计算上极具挑战性，尤其在面对大规模、非凸、耦合变量时，常导致计算成本过高或无可行解。

此外，现有研究中缺乏将**机器学习模型完整嵌入双层框架**并实现**鲁棒优化**的方法，尤其是在真实工业场景下的可扩展性和实时性不足。

### 提出的新方法与创新
本文提出了一种全新的 **ANN-KKT 框架**，其核心创新如下：

- **ANN 完全替代双层目标函数**：首次将人工神经网络（ANN）作为代理模型（surrogate model）同时用于上层和下层的目标函数建模，实现了从数据驱动的角度逼近复杂非线性关系。
  
- **KKT 条件解析嵌入**：通过 Karush-Kuhn-Tucker (KKT) 最优性条件将下层优化问题转化为约束，从而将原双层问题重构为单层数学规划问题（MPEC），便于高效求解。

- **Fischer-Burmeister 函数增强数值稳定性**：针对互补松弛条件（complementarity slackness）引起的数值不稳定性，采用 Fischer-Burmeister (FB) 函数进行平滑重构，提升优化器收敛能力。

- **Mahalanobis 距离约束保证域一致性**：引入基于历史操作数据协方差结构的 Mahalanobis 距离约束，确保最优解位于实际可行的操作包络内，避免物理不可行解。

- **扩展至鲁棒优化场景**：进一步将该框架应用于**不确定性环境下的鲁棒操作空间识别**，通过“对抗式”下层问题寻找最坏情况扰动，最大化稳定半径 $ \rho $，以保障热效率高于设定阈值。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **计算效率** | 求解时间仅需 **0.22–0.88 秒**，远低于传统迭代法或分支定界等全局搜索算法，适用于动态控制响应。 |
| **可扩展性** | 支持大规模工程系统（如 660MW 煤电、395MW 燃机），具备向 Industry 5.0 场景推广的能力。 |
| **数据驱动适应性** | 利用真实运行数据训练 ANN，能捕捉噪声、异步控制策略下的复杂非线性映射。 |
| **鲁棒性支持** | 首次实现基于 ANN-KKT 的**鲁棒操作包络识别**，可对冲过程不确定性。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **基准测试问题（Benchmark Problems）**：
  - Convex & Convex (C&C)
  - Convex & Non-convex (C&NC)
  - Non-convex & Non-convex (NC&NC)
  - 合成生成 10,000 个样本，按 70%/15%/15% 分割为训练/验证/测试集。

- **真实工业系统数据**：
  - **660 MW 煤电机组**：来自文献 [55] 的运行数据，共 1,279 个样本，输入变量包括 CFR、AFR、MSP、MST 等 8 个操作参数。
  - **395 MW 燃气轮机系统**：同样来自 [55]，共 579 个样本，涉及 CDP、GFFR、AT、AP、AH 等 9 个变量。

所有输入均经 min-max 归一化至 [0,1] 区间。

### 实验设置
- **ANN 架构**：三层前馈网络（浅层），隐藏层激活函数为 SiLU，输出层为线性函数，保证梯度连续可微。
- **超参数优化**：使用 Hyperopt 库中的 Tree Parzen Estimator 进行贝叶斯调参，优化 learning rate、hidden neurons、L1/L2 正则项。
- **训练细节**：最大 5,000 轮训练，早停机制（patience=200），损失函数含 MSE 和 L1 正则项。
- **求解器**：
  - 主要使用 **IPOPT**（开源、适合非线性优化）
  - 对比使用 **BARON**（全局优化求解器）
- **平台配置**：Intel i7-8850H CPU, 32GB RAM, NVIDIA Quadro P1000 GPU；Python + Pyomo + GAMS 接口。

### 评估指标
| 指标 | 描述 |
|------|------|
| **R²** | 决定系数，衡量 ANN 模型拟合精度（越接近 1 越好） |
| **RMSE** | 均方根误差，反映预测偏差大小 |
| **Objective Value** | 双层优化最终目标值（如最大功率、最小 THR） |
| **CPU Time (s)** | 单次求解耗时，体现计算效率 |
| **Feasibility** | 是否满足所有约束条件（特别是 KKT 和 Mahalanobis 约束） |
| **Stability Radius ($\rho$)** | 鲁棒优化中允许的最大扰动范围 |

### 基线方法对比
- **Bi-level-KKT**：传统 KKT 重构方法（无 ANN 替代）
- **原始双层解（Bi-level solution）**：来自标准测试集的理论最优解
- **IPOPT vs BARON**：局部 vs 全局求解器性能比较

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 系统 | 最大功率输出 | 对应 THR | 求解时间 | R² (Power) | R² (THR) |
|------|--------------|----------|---------|------------|-----------|
| 660MW 煤电 | **583 MW** | **7337 kJ/kWh** | 0.334 s | 0.99 | 0.72 |
| 395MW 燃机 | **402 MW** | **7542 kJ/kWh** | 0.409 s | 0.99 | 0.85 |

> 注：THR（Turbine Heat Rate）越低表示热效率越高。

### 与基线方法的对比结果

#### 在 Benchmark 问题上的表现（见 Table 1）

| 问题类型 | 方法 | x | y | Objective (F) | 时间 (s) |
|--------|------|----|----|-------------|---------|
| C&C | Bi-level-KKT | 1.0 | 3.0 | 5.0 | 0.14 |
| C&C | **ANN-KKT (BARON)** | 0.981 | 2.962 | 4.976 | **1.27** |
| C&NC | Bi-level-KKT | 1.0 | 0.0 | 0.0 | 0.12 |
| C&NC | **ANN-KKT (BARON)** | 1.014 | 0.0 | 0.0034 | **1.27** |
| NC&NC | Bi-level-KKT | -0.4191 | -1.0 | 0.1756 | 0.09 |
| NC&NC | **ANN-KKT (BARON)** | -0.4191 | -1.0 | 0.1750 | **0.39** |

- **结论**：ANN-KKT 能够以极小误差逼近真实 bi-level 解，尽管计算时间略长（因需计算 ANN 梯度），但在工程可接受范围内。

#### IPOPT 求解器表现（见 Appendix B.4）
- 在 **NC&NC** 问题上成功收敛到最优解；
- 在 C&C 和部分 C&NC 上出现不收敛或不可行解，说明 ANN 引入的非凸性会影响局部求解器表现；
- 表明 **ANN-KKT 更适合配合全局或强健的局部求解器使用**。

### 消融实验与关键分析

#### 不同 Mahalanobis 容差 $ T $ 的影响（煤电系统）
- 当 $ T = 94\% $ 时达到最佳解：**583 MW @ 7337 kJ/kWh**
- 较低 $ T $（如 81%-82%）限制搜索空间，导致次优解；
- 较高 $ T $（如 95%）可能导致约束违反（infeasible）；
- 多数可行解 CPU 时间 < 1 秒，验证了**实时优化潜力**。

#### 鲁棒优化结果（燃气轮机系统）
- 成功识别出在不同 **Target Efficiency Floor (TEtarget)** 下的鲁棒操作区间（见 Table 2）
- 当 TEtarget 从 38% 提升至 43%，稳定半径 $ \rho $ 显著减小 → 表明高效率要求牺牲鲁棒性
- 在 TEtarget = 42% 时仍可维持平均 TE > 42%，证明框架可实现**高性能且鲁棒的操作包络设计**

---

## 4. 关键结论和发现

### 主要发现
1. **ANN-KKT 是一种可扩展、高效的双层优化框架**，能够在秒级时间内完成大型热力系统的优化决策。
2. 将 ANN 同时嵌入上下层目标函数是可行且有效的，尤其适用于由数据驱动的复杂非线性系统建模。
3. 结合 Mahalanobis 距离约束可显著提高解的**物理可实施性**，防止进入异常工况区域。
4. 扩展至鲁棒优化后，能够量化不确定性下的操作弹性，并为操作员提供安全边界指导。
5. 本方法推动了 AI 与工业控制系统融合，助力实现 **Industry 5.0** 中的人机协同智能运维。

### 局限性
1. **Mahalanobis 参数 $ T $ 难以自动调节**：当前依赖人工设定，影响自动化部署。
2. **ANN 引入非凸性可能破坏收敛性**：即使原问题是凸的，ANN 的近似也可能产生多个局部极小点，影响 IPOPT 等局部求解器表现。
3. **未完全解决全局最优性保证**：虽然 KKT 提供必要条件，但对于非凸问题无法保证全局最优。
4. **依赖高质量历史数据**：若训练数据覆盖不足或存在偏倚，可能导致代理模型失真。

### 未来工作方向
1. **ANN 模型凸化技术研究**：探索如何构造具有凸性质的 ANN 架构，以加速优化收敛并提升稳定性。
2. **自适应 $ T $ 调整机制**：开发基于强化学习或在线学习的动态容差调整策略。
3. **集成更多不确定性建模方法**：如结合随机规划（stochastic programming）或分布鲁棒优化（DRO）。
4. **部署于实时 DCS 控制系统**：开展现场闭环测试，验证其在真实电厂中的控制效果。
5. **拓展至其他能源系统**：如综合能源系统（IES）、碳捕集系统（CCUS）、氢能生产等。

--- 

> ✅ 总结一句话：  
> 本文提出的 **ANN-KKT 框架**成功打通了“数据 → 模型 → 优化 → 决策”的闭环路径，为工业热力系统的智能化、高效化、鲁棒化运行提供了强有力的工具支撑，标志着数据驱动优化在能源领域迈向实用化的重要一步。

</details>

---

### 10. [REDSearcher: A Scalable and Cost-Efficient Framework for Long-Horizon Search Agents](https://arxiv.org/abs/2602.14234)

**Authors**: Zheng Chu, Xiao Wang, Jack Hong, Huiming Fan, Yuqi Huang, Yue Yang, Guohai Xu, Chenxiao Zhao, Cheng Xiang, Shengchao Hu, Dongdong Kuang, Ming Liu, Bing Qin, Xing Yu  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.14234v1  

#### Abstract
Large language models are transitioning from generalpurpose knowledge engines to realworld problem solvers, yet optimizing them for deep search tasks remains challenging. The central bottleneck lies in the extreme sparsity of highquality search trajectories and reward signals, arising from the diffi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：REDSearcher: A Scalable and Cost-Efficient Framework for Long-Horizon Search Agents**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前大型语言模型（LLMs）在从通用知识引擎向现实世界问题求解者转变的过程中，面临**深度搜索任务优化困难**的核心瓶颈。具体表现为：
- **高质量搜索轨迹稀疏**：复杂、长周期的交互式任务难以大规模构建。
- **训练成本高昂**：依赖外部工具调用的rollout过程计算和时间开销巨大。

这些问题导致现有方法在扩展性和成本效益上受限，难以有效训练具备长期推理能力的**search agent**。

---

### **提出的新方法与创新思路**
为解决上述挑战，作者提出了 **REDSearcher**，一个统一的、可扩展且低成本的框架，通过协同设计任务合成、中期训练（mid-training）和后训练（post-training）来优化长周期搜索代理。

#### **四大核心技术组件**：

1. **Dual-Constrained Task Synthesis（双约束任务合成）**
   - 将任务生成建模为**图拓扑结构**与**证据分散度**双重约束下的优化问题。
   - 引入**treewidth**作为衡量逻辑复杂性的指标，构造具有环状、交织约束的非线性推理路径。
   - 定义**Minimum Source Dispersion (MSD)**，确保相关事实分布在不同文档中，防止“单页捷径”解决方案，强制跨文档综合。

2. **Proactive Tool-Augmented Queries（主动式工具增强查询）**
   - 不再依赖被动召回，而是将关键实体替换为需工具解析的功能性约束（如地图距离、学术引用数），使工具调用成为完成任务的必要条件。
   - 显著**稠密化学习信号**，提升对工具使用的监督效率。

3. **Cost-Efficient Mid-Training（低成本中期训练）**
   - 分两阶段进行：
     - **Stage I**：强化原子能力（knowledge grounding 和 hierarchical planning），无需环境交互。
     - **Stage II**：引入模拟工具循环和长周期轨迹，逐步过渡到真实交互。
   - 大幅降低高质量轨迹采集的成本和样本复杂度。

4. **Functionally Equivalent Simulation Environment（功能等价仿真环境）**
   - 构建轻量级本地模拟环境，复现网页动态但避免实时API延迟与费用。
   - 保证所有证据存在但物理分散于大量干扰项中，形成高吞吐沙盒，支持快速RL迭代。

---

### **相比现有方法的优势**
| 维度 | REDSearcher优势 |
|------|----------------|
| **可扩展性** | 支持大规模生成高复杂度、高分散性任务，突破人工标注限制 |
| **成本效率** | 中期训练分离技能获取与交互，仿真环境替代真实调用，显著降低成本 |
| **训练有效性** | 双约束任务 + 主动工具调用 → 更强的长周期规划与工具使用能力 |
| **泛化性** | 同时支持 text-only 与 multimodal 搜索任务 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Text-only Benchmarks**:
  - `BrowseComp`（英文网页浏览）
  - `BrowseComp-zh`（中文网页浏览）
  - `GAIA`（通用AI助手基准）
  - `Humanity's Last Exam (HLE)`（综合性考试风格任务）

- **Multimodal Benchmarks**:
  - `MM-BrowseComp`, `BrowseComp-VL`, `MMSearch-Plus`, `LiveVQA`, `MMLiveSearch`

- **自研合成数据集**（用于训练与验证）：
  - 10K 高质量文本搜索轨迹
  - 5K 多模态轨迹
  - 1K 文本 RL 查询集

---

### **实验设置与评估指标**
- **Backbone模型**：基于 `Qwen3-30B-A3B` 进行微调
- **上下文长度**：最大支持 128K tokens
- **上下文管理策略**：采用 **Discard-all**，当接近窗口上限时清空历史交互记录，保留原始问题以继续探索
- **评估协议**：遵循各benchmark官方评测方式，报告准确率（accuracy）

---

### **基线方法对比**
#### **闭源代理（Proprietary Agents）**
- Seed1.8, Gemini-3-Pro, GPT-5-Thinking-high, Claude-4.5-sonnet, OpenAI-o3

#### **开源代理（Open-source Agents）**
- Kimi-K2.5, GLM-4.7, DeepSeek-V3.2, WebSailorV2-30B, Tongyi DeepResearch-30B

#### **多模态代理**
- Qwen3-VL Thinking, Vision-DeepResearch, DeepEyesV2, Gemini-3-Pro

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Table 1 & Table 3）**

| 模型 | BrowseComp | BrowseComp-zh | GAIA | HLE | Overall |
|------|------------|---------------|------|-----|---------|
| **REDSearcher (30B)** | **57.4*** | **58.2*** | **80.1** | 34.3 | **51.6** |
| GPT-5-Thinking-high | 54.9 | 63.0 | 76.7 | 41.7 | 59.1 |
| Tongyi DeepResearch-30B | 43.4 | 46.7 | 70.9 | 32.9 | 48.5 |
| WebSailorV2-30B | 35.3 | 44.1 | 74.1 | 30.6 | 46.0 |

> 注：`*` 表示使用 Discard-all 上下文管理技术；REDSearcher 在 **GAIA** 上超越 GPT-5-Thinking-high，是唯一达到此水平的 **30B级别开源模型**。

---

### **多模态性能表现（Table 3）**

| 模型 | MM-BrowseComp | BrowseComp-VL | MMSearch-Plus | LiveVQA |
|------|----------------|----------------|----------------|----------|
| **REDSearcher-MM-RL** | **26.6** | **57.2** | **26.6** | **79.3** |
| Gemini-3-Pro | 28.5 | 56.4 | 38.1 | 79.9 |
| Qwen3-VL-235B | 12.1 | 43.1 | 17.4 | 70.2 |

- 在多个多模态任务上接近甚至超过专有模型（如 Gemini-3-Pro）。
- 显著优于同规模开源多模态代理（如 Vision-DeepResearch）。

---

### **消融实验结果（Table 2）**

| 阶段 | BrowseComp | BrowseComp-zh | HLE | GAIA | 平均得分 |
|------|------------|----------------|-----|------|-----------|
| Base | 34.74 | 26.82 | 32.25 | 77.43 | 42.81 |
| + Stage I (Grounding & Planning) | 36.97 | 29.84 | 31.37 | 80.83 | 44.75 |
| + Stage II (Agentic Interaction) | 40.44 | 38.75 | 31.25 | 79.13 | **47.39** |

- **Stage I** 提升信息提取与分层规划能力，尤其改善 GAIA 表现（+4.13）。
- **Stage II** 引入环境反馈后，BrowseComp-zh 提升达 **+8.91**，验证长周期交互的重要性。

---

### **强化学习效果分析（Figure 6）**
- **RL训练持续提升性能**：
  - SFT平均评分为 47.4 → RL后提升至 **51.3**（+3.9）
  - BrowseComp 从 39.4 → **42.1**（+6.8%相对增益）
- **搜索效率提高**：
  - 平均工具调用次数从 **100.6 → 90.1**（↓10.4%）
  - 轨迹变短但成功率稳定，表明模型学会更高效的探索策略。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **双约束任务合成能有效激发深层推理能力**：通过控制 treewidth 与 MSD，可系统性构造抗捷径、需多跳合成的任务。
2. ✅ **工具使用应被显式激励而非依赖试错**：Proactive tool-augmented queries 设计显著提升工具调用合理性。
3. ✅ **中期训练是连接预训练与代理行为的关键桥梁**：分离原子能力训练与交互训练，大幅降低成本并提升稳定性。
4. ✅ **本地仿真环境可行且高效**：功能等价但无网络依赖的模拟器支持高通量RL实验。
5. ✅ **REDSearcher在参数效率上领先**：仅用30B参数即超越多数更大闭源模型，在 GAIA 上表现尤为突出。

---

### **方法的局限性**
- **依赖高质量合成数据的质量**：尽管有验证流程，但仍可能存在隐含偏差或错误标签。
- **仿真环境与真实世界的差距**：虽然接口一致，但无法完全复现搜索引擎排名波动、页面更新等动态特性。
- **多模态任务仍受限于视觉理解瓶颈**：部分图像细节识别失败会影响最终答案准确性。
- **硬性截断机制影响决策完整性**：Discard-all 策略可能导致历史状态丢失，影响极长任务的一致性。

---

### **未来工作方向**
- 扩展至更多工具类型（如数据库查询、邮件交互等）。
- 探索自动发现与修复错误推理路径的自我纠正机制。
- 构建开放世界在线持续学习框架，实现真实环境中自主进化。
- 开源发布的 **10K文本轨迹、5K多模态轨迹、1K RL查询集** 将推动社区研究。

---

> 🔗 **项目主页**：[redsearchagent.github.io](https://redsearchagent.github.io)  
> 📦 **已发布资源**：代码、模型checkpoint、高质量轨迹数据集、RL query set

</details>

---

### 11. [Efficient Multi-round LLM Inference over Disaggregated Serving](https://arxiv.org/abs/2602.14516)

**Authors**: Wenhao He, Youhe Jiang, Penghao Zhao, Quanqing Xu, Eiko Yoneki, Bin Cui, Fangcheng Fu  
**Category**: cs.DC  
**Published**: 2026-02-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.14516v1  

#### Abstract
With the rapid evolution of Large Language Models (LLMs), multi-round workflows, such as autonomous agents and iterative retrieval, have become increasingly prevalent. However, this raises hurdles for serving LLMs under prefill-decode (PD) disaggregation, a widely adopted paradigm that separates the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Efficient Multi-round LLM Inference over Disaggregated Serving**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前主流的 **Prefill-Decoding (PD) disaggregation** 架构将 LLM 推理分为计算密集型的 **prefill 阶段** 和内存密集型的 **decode 阶段**，分别部署在独立硬件上以提升资源利用率。然而，在 **multi-round LLM workflows**（如 autonomous agents、iterative RAG）中，推理过程呈现 **interleaved prefill-decode 模式**——每轮外部交互后都需要执行增量 prefill（incremental prefill），而现有系统对此缺乏有效支持。

具体存在两个关键问题：
1. **缺乏自适应调度机制**：无法动态决定增量 prefill 是应在 decode worker 上本地执行（避免 KV 传输开销）还是路由到专用 prefill worker（减轻干扰）。
2. **模型部署配置不合理**：传统部署策略仅基于单轮推理的输入/输出长度设计，未考虑多轮场景下交错负载对资源分配和并行策略的影响。

---

### **提出了什么新方法或新思路**
本文提出 **AMPD**（Adaptive Multi-round inference with PD disaggregation），一个专为多轮 LLM 推理优化的新型解耦服务框架，其核心创新包括：

#### **(1) 在线自适应调度机制（Online Adaptive Scheduling）**
- **Adaptive Routing（自适应路由）**  
  动态判断每个 prefill 任务应本地执行还是远程执行，依据实时系统的 **windowed TTFT / ITL 统计** 来评估负载压力，并选择更可能满足 SLO 的路径。
- **Prefill Reordering（预填充重排序）**  
  在 prefill worker 的任务队列中采用轻量级重排序策略，优先执行接近 SLO 阈值的任务，最大化满足 TTFT SLO 的请求数量。

#### **(2) 离线部署规划器（Offline Deployment Planner）**
- 将资源分配与并行策略建模为 **Integer Linear Programming (ILP)** 问题，联合优化 prefill 和 decode worker 的 **data/model parallelism 配置**，在固定 GPU 资源下最小化瓶颈延迟（P95 latency）。

---

### **相比现有方法的优势**
| 方面 | 传统方法缺陷 | AMPD 改进 |
|------|-------------|----------|
| **调度灵活性** | 固定路由（全 remote 或全 colocation） | 动态决策，平衡 TTFT 与 ITL |
| **KV 缓存管理** | 忽视增量 prefill 的传输成本 | 仅传输增量 KV，支持懒加载 |
| **部署合理性** | 基于单轮 workload 设计 | 显式建模多轮交错负载特征 |
| **SLO 达成率** | 容易受某一阶段瓶颈限制 | 协同优化两端，显著提升 SLO attainment |

---

## **2. 核心实验方法和设置**

### **使用的数据集与工作负载**
实验基于四种代表性的 **multi-round LLM 工作流 trace**，来源于以下公开数据集：
- **ToolBench**：工具调用类 agent 任务（平均 3.96 轮）
- **GAIA**：通用 AI 助手基准（平均 11.32 轮）
- **HotpotQA**：多跳问答 + 迭代检索（3 轮）
- **DuReader**：中文阅读理解 + 检索增强（3 轮）

这些 trace 捕获了真实环境中的 **prefill-decode 交错模式** 和 **外部交互延迟**。

---

### **实验设置**
- **模型**：Qwen3-32B、Llama-3.1-70B、Mixtral-8x7B
- **硬件环境**：4 台服务器，每台 8×NVIDIA H20（96GB）GPU，NVLink（900 GB/s）+ InfiniBand（200 GB/s）
- **请求到达模式**：Poisson process，不同 arrival rate 下测试系统表现
- **部署规模匹配**：
  - HotpotQA / ToolBench：1 server (8 GPUs)
  - DuReader：2 servers (16 GPUs)
  - GAIA：4 servers (32 GPUs)

---

### **评估指标**
- **主指标**：**SLO Attainment Rate**（满足延迟约束的请求比例）
  - TTFT（Time-to-First-Token）SLO：根据不同 workload 设置（如 350ms ~ 5000ms）
  - ITL（Inter-Token Latency）SLO：通常 20–50ms
- **辅助指标**：
  - 平均端到端延迟（E2E Latency）
  - TTFT / ITL 分解性能
  - 消融实验中 local vs. remote 执行比例

---

### **基线方法对比**
| 基线 | 类型 | 特点 |
|------|------|------|
| **Dynamo** | PD disaggregation | NVIDIA 官方解耦架构，默认所有 prefill 发送到 prefill worker |
| **vLLM** | Colocated serving | 主流共置架构，prefill 与 decode 共享 GPU |
| **vLLM-Continuum** | Colocated + 多轮优化 | 支持 TTL-based KV 缓存保留，针对多轮优化 |

> 注：所有基线均进行部署参数调优，报告最佳结果以确保公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **SLO Attainment 提升显著**：
  - 相比 **Dynamo**（disaggregated baseline）：**平均提升 67.29%**，最高达 **967.54%**
  - 相比 **vLLM**（colocated baseline）：**平均提升 339.74%**，最高达 **3435.1%**
  - 相比 **vLLM-Continuum**：仍取得明显优势，说明即使有 KV 缓存优化，共置架构难以根本解决 PD 干扰问题

- **典型场景示例（Llama-3.1-70B, GAIA, 2 req/s）**：
  - AMPD SLO attainment ≈ **92.4%**
  - Dynamo ≈ 68.7%，vLLM ≈ 31.3%，vLLM-Continuum ≈ 31.7%

---

### **与基线方法的对比结果**
| 对比维度 | 观察结果 |
|---------|--------|
| **vs. Dynamo** | AMPD 显著降低 TTFT（因 adaptive routing 减轻 prefill worker 负载），同时保持低 ITL（避免 decode 中断） |
| **vs. vLLM** | vLLM 通过任务优先级保障 TTFT，但频繁中断 decode 导致 ITL 恶化；AMPD 利用解耦结构维持稳定 ITL |
| **vs. vLLM-Continuum** | 虽然 Continuum 通过 session 内任务优先调度改善 TTFT，但在高并发下仍受限于资源争用，SLO 提升有限 |

---

### **消融实验结果**
- **移除 Adaptive Routing (w/o AR)**：
  - SLO attainment 下降 **27.37% – 350%**
  - 本地执行占比从 13.9%~31.7% 降至 0%，prefill worker 成为瓶颈
- **移除 Prefill Reordering (w/o PR)**：
  - SLO attainment 下降 **13.42% – 14.81%**
  - 表明队列内部调度对尾部延迟控制至关重要
- **组合效果**：两项技术协同带来 **44.47% – 402%** 的整体提升

> 图 5 显示，在 DuReader 上，启用完整 AMPD 后 SLO attainment 从 ~55% 提升至 ~95%

---

## **4. 关键结论和发现**

### **主要发现**
1. **多轮推理必须重新思考调度与部署**  
   传统的 “一刀切” 路由策略（全部远程或全部本地）无法适应动态交错负载，**运行时感知的 adaptive routing 是关键**。

2. **解耦架构 + 自适应调度 > 共置架构 + KV 优化**  
   即使 vLLM-Continuum 引入了 TTL-based KV 缓存保留机制，其性能仍远低于 AMPD，说明 **从根本上分离 prefill 与 decode 更有利于复杂 workflow 的高效服务**。

3. **离线规划能有效指导部署决策**  
   通过 ILP 建模，AMPD 的 planner 能准确预测最优资源配置，实验证明其推荐的 top-1 部署配置与实际最优一致（见 Appendix A.3）。

4. **小窗口即可实现高效调度**  
   Prefill reordering 使用极小的 lookahead window（w=3）即能达到近似最优效果，说明该策略具有 **低开销、高实用性**。

---

### **方法的局限性**
- **依赖准确的性能建模**：TTFT/ITL 预测精度影响 routing 决策质量，若 workload 偏离训练分布可能导致次优决策。
- **网络带宽敏感**：KV cache 传输开销随 context length 增长而上升，在超长上下文场景下可能削弱远程执行优势。
- **未处理异构硬件混合调度**：目前假设集群内 GPU 同构，扩展至 heterogeneous environment 需进一步研究。

---

### **未来工作方向**
1. **引入 ML-based 预测模型**：用于更精准地估计环境交互时间、输出长度等不确定性因素，进一步优化调度。
2. **支持动态弹性扩缩容**：结合 Tokenscale 等 autoscaling 技术，实现 AMPD 在云环境下的按需伸缩。
3. **扩展至多模态 LLM**：将 AMPD 思路应用于 video/audio 输入场景下的跨模态 prefill-decode 调度。
4. **探索 hybrid 执行模式**：允许部分 layer-level disaggregation，而非仅 phase-level，进一步细粒度解耦。

---

> ✅ **总结一句话**：  
> **AMPD 首次系统性地将 adaptive runtime scheduling 与 optimized offline deployment planning 结合，实现了在 PD disaggregation 架构下对 multi-round LLM inference 的高效支持，大幅提升了 SLO attainment，为下一代 LLM serving system 提供了重要范式参考。**

</details>

---

### 12. [RNM-TD3: N:M Semi-structured Sparse Reinforcement Learning From Scratch](https://arxiv.org/abs/2602.14578)

**Authors**: Isam Vrce, Andreas Kassler, G\"ok\c{c}e Aydos  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.14578v1  

#### Abstract
Sparsity is a well-studied technique for compressing deep neural networks (DNNs) without compromising performance. In deep reinforcement learning (DRL), neural networks with up to 5% of their original weights can still be trained with minimal performance loss compared to their dense counterparts. Ho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**RNM-TD3: N:M Semi-structured Sparse Reinforcement Learning From Scratch**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **深度强化学习（DRL）中的模型压缩与硬件加速矛盾**：
  - 现有稀疏训练方法多采用 *unstructured sparsity*，虽能有效压缩模型，但由于不规则的权重分布，难以在通用硬件（如GPU）上实现实际加速。
  - 结构化稀疏（如 channel pruning）可支持硬件加速，但通常导致性能下降且剪枝复杂度高。
  - 此外，大多数稀疏DRL方法依赖于预训练后剪枝或动态拓扑演化策略，无法在训练过程中利用稀疏性进行加速。

### 🚀 提出的新方法与思路
- 首次将 **N:M semi-structured sparsity** 引入到端到端的 DRL 训练中，提出 **RNM-TD3** 框架：
  - 在整个训练过程中强制保持 **row-wise N:M sparsity**（例如 2:4、1:4），即每连续 M 个权重中最多保留 N 个非零值。
  - 所有网络（actor、critic 及其 target 网络）均从稀疏初始化开始训练，无需先训练密集模型。
  - 利用周期性的 mask 更新机制维持稀疏结构，并兼容支持 N:M 稀疏矩阵运算的硬件（如 NVIDIA Ampere Tensor Cores）。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **硬件兼容性** | 支持硬件加速（尤其是前向传播），解决了 unstructured 方法“理论FLOPs低但无实际加速”的问题 |
| **训练效率潜力** | 由于始终运行在稀疏模式下，具备端到端训练加速潜力 |
| **性能表现** | 在多个环境中优于 dense baseline 和部分 unstructured 动态稀疏方法 |
| **结构灵活性** | 动态更新 mask，相比静态稀疏方法更具适应性 |

---

## 2. 核心实验方法和设置

### 📚 数据集 / 环境
- 使用 **Gymnasium MuJoCo** 连续控制基准任务：
  - `Ant-v5`
  - `HalfCheetah-v5`
  - `Humanoid-v5`
  - `Walker2d-v5`

### ⚙️ 实验设置
- **算法基础**：基于 **TD3**（Twin Delayed Deep Deterministic Policy Gradient）
- **网络架构**：
  - Actor 与 Critic 均为两层隐藏层，每层 256 neurons
- **训练配置**：
  - 总共训练 1M environment steps
  - 使用标准静态 replay buffer 和 1-step returns
  - 所有方法共享相同超参数以保证公平比较
- **稀疏模式**：
  - 测试多种 N:M ratio：2:4 (50% sparsity), 1:4 (75%), 1:8 (87.5%)
  - 最后一层保持 dense（与其他方法一致）

### 📊 评估指标
- **平均回报（Average Return）**：在整个训练过程中的累计奖励均值
- **标准化得分**：相对于 dense baseline 的归一化性能（dense = 1.0）
- **Sparse Architecture Divergence (SAD)**：衡量连续 mask 之间的汉明距离，反映拓扑变化程度
- **方差（Std. Deviation）**：评估稳定性

### 🆚 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Dense TD3** | 密集模型 | 性能基准 |
| **SSN-N:M** | Static Sparse Network | 固定 mask，初始化时采样一次 |
| **DS-TD3** (Sokar et al., 2021) | Dynamic Sparse Training | 随机 regrowth 策略 |
| **RLx2** (Tan et al., 2022) | Dynamic Pruning + Regrowth | 基于梯度大小调整连接 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| 环境 | Sparsity | RNM-TD3 (相对 dense) | Dense Baseline Score |
|-------|----------|------------------------|------------------------|
| Ant-v5 | 50% (2:4) | **1.14±0.05** ✅ | 4453±1320 |
| HalfCheetah-v5 | 50% (2:4) | 1.01±0.08 | 10211±1110 |
| Humanoid-v5 | 50% (2:4) | 1.05±0.03 | 4868±659 |
| Walker2d-v5 | 50% (2:4) | 1.04±0.13 | 3767±833 |

> ✅ **亮点**：在 **Ant 环境下 2:4 稀疏度时性能提升达 14%**

#### 更高稀疏度下的表现：
- 在 **75% sparsity (1:4)** 下仍保持竞争力，多数环境接近或超过 dense baseline
- 即使在 **87.5% sparsity (1:8)** 下，除 Humanoid 外基本保持可用性能
  - Humanoid 在 1:8 下出现崩溃（`0.28±0.20`），需 soft reset 机制恢复

### 🔁 与基线方法对比
| 对比维度 | 结果 |
|---------|------|
| vs **SSN-N:M**（静态稀疏） | RNM-TD3 显著更优，尤其在动态环境中；证明动态 mask 更新的重要性 |
| vs **DS-TD3 / RLx2**（非结构化动态稀疏） | RNM-TD3 平均性能更高（如 Ant 上 +25%），且具备硬件加速潜力 |
| vs **Dense TD3** | 在 50%~75% 稀疏度下普遍持平甚至超越，打破“稀疏必降性能”认知 |

### 🔬 消融实验结果

#### （1）**Mask Update Period 影响（Table 3 & Figure 3）**
- 在 HalfCheetah 上测试不同更新周期 K：
  - 最佳性能出现在 **K=4000 environment steps**
  - 过短（K=10）→ mask flickering → SAD 极低 → 性能差
  - 过长（K=20000）→ 拓扑更新滞后 → 学习缓慢
- **发现**：适度频率的 mask 更新带来中等非零 SAD，对应最佳性能

#### （2）**Soft Reset 机制有效性**
- 在 Humanoid 等不稳定环境中，当 SAD < 10 时触发 soft reset（重新采样 mask）
- 成功避免早期训练崩溃（原失败率约 80%）
- 恢复后 SAD 维持健康水平，训练恢复正常

#### （3）**Initialization Ablation（Table 2）**
- 使用适配 N:M fan-in 的 **Kaiming 初始化**（scale by √(M/N)）：
  - 显著降低训练方差
  - 在高稀疏度下防止输出饱和（避免 tanh 梯度消失）
- 标准 Kaiming 初始化在 1:8 场景下可能导致灾难性失败（如 Humanoid 上 std 高达 1.10）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **N:M structured sparsity 可用于端到端 DRL 训练**：
   - 不仅可行，还能在某些环境下**超越 dense 模型性能**。
2. **动态 mask 更新优于静态初始化**：
   - 动态拓扑演化对复杂策略学习至关重要。
3. **中等非零 SAD 是良好训练的标志**：
   - 与监督学习追求最小 SAD 不同，DRL 中**适度拓扑变化有助于适应非平稳数据分布**。
4. **mask 更新频率是关键超参**：
   - 过频 → 不稳定；过慢 → 响应迟钝；存在最优中间区间（~4000–6000 steps）。
5. **硬件加速潜力明确**：
   - 尽管当前实现未启用双向 N:M（column-wise），仅靠前向 pass 加速即可带来显著 end-to-end speedup。

### ⚠️ 方法局限性
- **极端稀疏下稳定性挑战**：
  - 如 Humanoid 在 1:8 下容易崩溃，需额外机制干预。
- **尚未集成完整硬件加速栈**：
  - 当前实验基于模拟稀疏计算，未调用真实 N:M kernel（如 CUDA 2:4 kernels）。
- **初始化敏感**：
  - 高稀疏度下初始权重过大易引发饱和，需精细初始化设计。

### 🔮 未来工作方向
1. **开发自适应 mask update schedule**：
   - 根据 SAD 或环境反馈动态调节更新周期。
2. **探索列方向 N:M sparsity（Bi-directional N:M）**：
   - 同时优化前向与反向传播加速。
3. **专用高稀疏初始化方案**：
   - 设计针对 N:M 结构的初始化方法，提升鲁棒性。
4. **扩展至其他 DRL 算法**：
   - 如 SAC、PPO 等，验证通用性。
5. **部署到真实稀疏加速硬件平台**：
   - 在支持 2:4 或 1:4 的 GPU/FPGA 上实测推理与训练加速比。

---

> 💡 **一句话总结**：  
> **RNM-TD3 首次实现了从头开始的 N:M 结构化稀疏强化学习，在保持硬件友好性的同时，不仅没有牺牲性能，反而在多个任务上实现了超越密集模型的表现，揭示了“稀疏即高效且更强”的新可能。**

</details>

---

### 13. [When to Think Fast and Slow? AMOR: Entropy-Based Metacognitive Gate for Dynamic SSM-Attention Switching](https://arxiv.org/abs/2602.13215)

**Authors**: Haoran Zheng  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13215v1  

#### Abstract
Transformers allocate uniform computation to every position, regardless of difficulty. State Space Models (SSMs) offer efficient alternatives but struggle with precise information retrieval over a long horizon. Inspired by dual-process theories of cognition (Kahneman, 2011), we propose AMOR (Adaptiv...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*When to Think Fast and Slow? AMOR: Entropy-Based Metacognitive Gate for Dynamic SSM-Attention Switching*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统 **Transformer** 架构对序列中每个位置都分配相同的计算量（uniform computation），无论该位置是简单预测还是需要长距离信息检索。这种“一刀切”的策略在效率上存在浪费。另一方面，**State Space Models (SSMs)** 虽然具有线性复杂度、高效处理局部模式，但由于其压缩历史状态的机制，在精确检索远距离信息时表现不佳。

本文旨在解决这一矛盾：如何在保持高效的同时，仅在必要时调用高成本的全局注意力机制以实现精准检索。

### 提出了什么新方法或新思路
作者提出 **AMOR (Adaptive Metacognitive Output Router)**，一种受认知科学启发的混合架构，结合了 **System 1 (快思考)** 和 **System 2 (慢思考)** 的双过程理论：

- **System 1**: 由 SSM（如 GRU 或 Mamba）实现，负责快速、自动地处理所有位置，建模局部依赖。
- **System 2**: 由稀疏注意力（Sparse Attention）实现，仅在模型“不确定”时被激活，用于精确检索。
- **Metacognitive Gate**: 使用 **prediction entropy**（预测熵）作为路由信号。当 SSM 输出的概率分布熵较高（即模型不确定）时，触发注意力机制；否则直接使用 SSM 输出。

此外，引入 **Ghost KV Cache**：将 SSM 的隐藏状态投影为 Key 和 Value，而非从原始嵌入重新计算。这复用了 SSM 已有的上下文化表示，避免了额外的 $O(n^2)$ 注意力开销。

### 相比现有方法的优势
| 对比维度 | AMOR | 现有方法（如 Jamba, Griffin） |
|--------|------|-----------------------------|
| **动态性** | 动态按需触发注意力 | 固定模式交替使用 SSM 和 Attention |
| **可解释性** | 路由决策基于 entropy，语义明确（“我不知道”才查） | 路由器为黑箱学习，难以解释为何某位置被选中 |
| **效率机制** | Ghost KV 复用 SSM 隐藏状态，节省计算 | 每次 Attention 都需独立计算 KV |
| **计算类型变化** | 明确区分两种处理模式（SSM vs Attention） | 通常是模块堆叠，无本质处理模式切换 |

---

## 2. 核心实验方法和设置

### 使用的数据集
论文设计了两个合成任务来验证核心假设：

1. **Simple Retrieval Task**
   - 序列长度：128
   - 包含局部重复模式（如 A B A B）和远距离复制需求（`M X ... R ?` → `? = X`）
   - 约 3% 的位置需要检索
   - 目标：验证 entropy 是否能有效指示 retrieval 需求

2. **NeedleHaystack Task**
   - 更具挑战性的关联记忆任务
   - 三阶段结构：
     - **Store**: 存储若干 key-value 对（`STORE k v`）
     - **Noise**: 插入 50–150 个随机 token，迫使 SSM 状态遗忘
     - **Query**: 查询某个 key 的值（`QUERY k ?`）
   - 测试模型在 SSM 状态衰减后是否仍能通过注意力检索信息

### 实验设置和评估指标

#### 模型配置
- **SSM Only**: GRU 模型（~51K 参数）
- **Full Attention**: 标准 Transformer（~167K 参数）
- **AMOR Oracle**: 使用真实标签控制门控（理想上限）
- **AMOR Entropy**: 使用 entropy gate 控制

#### 评估指标
| 指标 | 含义 |
|-----|------|
| **Overall Accuracy** | 所有位置的平均预测准确率 |
| **Retrieval Accuracy** | 仅在需要检索的位置上的准确率 |
| **Gate Fires (%)** | 触发注意力的比例（反映效率） |
| **Gate F1** | 门控决策与真实检索需求之间的 F1 分数 |
| **Entropy Gap** | 检索位置与局部位置之间的平均熵差（单位：nats） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据与基线对比

#### Simple Retrieval Task 结果（表 3）

| Model | Overall Acc | **Retrieval Acc** | Gate Fires | Params |
|-------|-------------|--------------------|------------|--------|
| SSM Only | 89.76% | 68.35% | — | 51K |
| Full Attention | 89.37% | 87.30% | 100% | 167K |
| AMOR Oracle | 90.71% | 99.63% | 2.90% | 77K |
| **AMOR Entropy** | **90.88%** | **100%** | **22.32%** | **77K** |

- ✅ **AMOR 在 retrieval 准确率上达到 100%**，显著优于 SSM Only（68%）和 Full Attention（87%）
- ⏱️ 仅在 **22.32% 的位置** 触发注意力，相比 Transformer 节省约 **78% 的注意力计算**
- 🧠 尽管参数更少，性能反而更高，说明 **Ghost KV + entropy gate 的组合非常有效**

#### Entropy Gap 验证（表 2）
| 项目 | 数值 |
|------|------|
| 检索位置平均熵 | 1.98 nats |
| 局部位置平均熵 | 0.89 nats |
| **Entropy Gap** | **1.09 nats**（接近归一化范围的一半） |

👉 表明 SSM 确实能在信息不足时产生高熵输出，“知道自己不知道”，为门控提供了可靠依据。

#### NeedleHaystack Task 结果（表 4）
| Model | Retrieval Acc |
|-------|----------------|
| SSM Only | 8.02% |
| Full Attention | 4.40% |
| AMOR Entropy | 9.93% |
| **AMOR Oracle** | **37.08%** |

- 即使在此极端困难的任务上，**AMOR Entropy 仍优于 Full Attention**
- **Oracle 性能远超其他模型（37% vs 4.4%）**，表明若能精准识别何时需要检索，该架构潜力巨大
- 当前 entropy gate 在全局不确定性高时（gate fire rate 达 80.97%）失去判别能力

### 消融实验结果（附录 C.6）

| 变体 | Retrieval Acc | Gate F1 | Gate Fires |
|------|---------------|---------|----------|
| **Entropy gate (default)** | **96.16%** | 22.11% | 23.36% |
| Learned STE gate | 100% | 0.00% | 1.19% |
| Target rate=0.1 | 97.73% | 17.52% | 30.28% |
| Target rate=0.2 | 99.79% | 17.43% | 30.46% |

- 学习型门控虽能达到完美准确率，但 **完全不可解释（F1=0）**
- entropy gate 在 **可解释性和性能之间取得了良好平衡**

### 其他重要发现
- **Ghost KV vs Raw Embedding KV**：
  - Ghost KV（来自 SSM 隐藏状态）：**36.28%** retrieval acc
  - Raw Embedding KV：仅 **6.08%**
  - 👉 证明 SSM 提供了有价值的上下文信息
- **Attention Sparsity**：
  - Top-k=3 效果最好（81.39%），k=16 反而下降至 6.26%
  - 👉 稀疏注意力有助于聚焦关键信息，防止注意力分散

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Prediction entropy 是一个可靠且可解释的元认知信号**：SSM 在无法仅凭局部信息做出判断时会自然产生高熵输出，可用于触发注意力。
2. ✅ **Selective attention is sufficient**：无需在每个位置都运行注意力，仅在约 22% 的位置激活即可实现完美 retrieval。
3. ✅ **Ghost KV 实现高效复用**：利用 SSM 隐藏状态生成 KV，避免重复计算，带来显著效率优势。
4. ✅ **AMOR 实现了真正的“双过程”架构**：SSM 与 Attention 在功能和结构上分离，符合认知科学直觉。

### 方法的局限性
1. **SSM State Decay Limitation**：当噪声过长（>50 tokens），SSM 状态严重衰减，Ghost KV 中的信息也随之丢失，导致即使触发注意力也无法恢复。
2. **Reactive Gating 缺乏前瞻性**：当前门控是反应式的（reactive），只能在不确定性出现后才触发 attention，无法提前缓存重要信息。
3. **极端任务下判别力下降**：在全局高度不确定的任务中（如 NeedleHaystack），entropy 失去区分能力，gate fire rate 过高（80%+），削弱效率优势。

### 未来工作方向
1. **Proactive Storage Mechanism**：
   - 引入持久 KV 存储（Persistent KV Cache），在编码阶段主动缓存关键位置的表示
   - 设计“存储门”（storage gate），基于信息重要性而非当前不确定性进行缓存
2. **State Feedback**：
   - 探索将 attention 输出反馈回 SSM 状态更新中（$h_{t+1} = \text{SSM}(e_t, f(h_t, \text{combined}))$），支持迭代推理
3. **Predictive Gating**：
   - 使用强化学习等方法训练门控预测未来的 retrieval 需求，实现“预判式”计算分配
4. **扩展到真实场景**：
   - 在大规模语言建模、长文本理解等实际任务中验证 AMOR 的有效性与泛化能力

---

> **总结一句话**：  
> AMOR 提出了一种**基于 prediction entropy 的可解释元认知门控机制**，实现了 SSM 与 Attention 的动态切换，在合成任务中以 **仅 22% 的注意力调用率达到了 100% 的检索准确率**，展示了“知道何时该深入思考”的智能计算范式。

</details>

---

### 14. [Experimentation Accelerator: Interpretable Insights and Creative Recommendations for A/B Testing with Content-Aware ranking](https://arxiv.org/abs/2602.13852)

**Authors**: Zhengmian Hu, Lei Shi, Ritwik Sinha, Justin Grover, David Arbour  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13852v1  

#### Abstract
Modern online experimentation faces two bottlenecks: scarce traffic forces tough choices on which variants to test, and post-hoc insight extraction is manual, inconsistent, and often content-agnostic. Meanwhile, organizations underuse historical A/B results and rich content embeddings that could gui...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Experimentation Accelerator: Interpretable Insights and Creative Recommendations for A/B Testing with Content-Aware ranking*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代在线A/B测试面临两大瓶颈：
- **流量稀缺**：变体（variants）数量激增（尤其受GenAI推动），导致统计功效稀释、检测效应门槛提高、实验周期延长，难以决定优先测试哪些变体。
- **下游分析低效**：实验后的**insight extraction**（洞察提取）和**opportunity generation**（机会生成）通常依赖人工、主观且内容无关（content-agnostic），缺乏系统性和可扩展性。

此外，企业普遍未充分利用历史A/B测试结果和丰富的创意内容嵌入（embeddings）来指导新实验的设计与优化。

### 🚀 提出的新方法与框架
作者提出一个统一的 **Experimentation Accelerator** 框架，集成三大模块：

#### （1）**Ranking（排序）模块**
- 利用历史A/B测试数据中的 treatment embeddings 和 CTR 结果，训练一个基于 **Mixed-Effects Regression (MER)** 的 CTR ranking model。
- 该模型通过 PCA 降维处理嵌入，并引入实验级 fixed effects 来校正不同实验间的全局差异。
- 在推理阶段，对新实验的候选变体进行相对 CTR 排序，辅助优先级决策。

#### （2）**Insights（洞察）模块**
- 将 treatment 映射到一组预定义的 **semantic marketing attributes**（如 urgency, FOMO, action_oriented 等共122个）。
- 使用 **constrained Lasso** 将原始 ranker 的权重投影到 attribute space，得到带符号的 per-attribute 贡献系数（signed contributions）。
- 支持可视化解释、top-k 驱动因素识别和自然语言洞察生成（via LLM）。

#### （3）**Opportunity（机会）模块**
- 构建 **Opportunity Index**：结合属性重要性（来自 ranker 系数）与当前实验中该属性的“表达不足”程度（under-expression）。
- 利用 LLM 将高潜力缺失属性转化为具体的创意改进建议（creative suggestions），并估计其 conversion potential 与 learning potential。

### 🔍 相比现有方法的优势
| 维度 | 本文方法 | 现有方法 |
|------|----------|--------|
| **Interpretability** | 显式映射至语义属性空间，提供可解释的驱动因子 | 多在 embedding space 操作，黑箱性强 |
| **Actionability** | 输出具体文本建议，直接指导创意迭代 | 仅预测效果，无后续行动指引 |
| **Knowledge Transfer** | 利用跨域历史数据实现迁移学习（Upworthy → 客户数据） | 多为单任务建模或零样本提示 |
| **系统整合性** | 三阶段闭环：Rank → Explain → Improve | 功能割裂，缺乏端到端支持 |

---

## 2. 核心实验方法和设置

### 📚 数据集使用
| 类型 | 名称 | 描述 |
|------|------|------|
| **训练数据** | **Upworthy Dataset** [9] | 包含2013–2015年间32,487个新闻标题A/B测试的真实CTR数据，用于训练 ranking model。 |
| **测试数据** | **Adobe Customer Experiments** | 来自真实客户的65个已完成且具有显著CTR差异的营销文案实验，用于评估模型泛化能力。 |

> ⚠️ 注意：出于隐私合规要求，客户数据不能用于训练，仅作测试，体现B2B平台的实际部署约束。

### 🧪 实验设置与评估指标

#### （1）Ranking 性能评估
- **目标**：验证 ranking model 是否能准确预测新实验中各变体的相对排名。
- **指标**：
  - **Spearman’s Rank Correlation (ρ)**：衡量预测排名与真实排名的相关性。
  - **Top-1 Accuracy**：预测最优变体是否与实际最佳一致的比例。

#### （2）Embedding 消融比较
- 对比三种 embedding 方案在 leave-one-out 设置下的表现：
  1. **MiniLM**：轻量级 sentence transformer
  2. **Llama**：大型语言模型编码器（更强语义捕捉）
  3. **Attribute Score**：LLM-as-a-judge 直接打分（1–5分）

#### （3）Insights & Opportunities 质量评估
- **人类评估**：由市场专家判断生成的 insight/opportunity 是否结构良好、准确合理。
- **LLM-as-a-Judge**：使用 GPT-4o 作为自动裁判，依据预设 rubric 打出 accept/reject 决策。
- **输出维度**：接受率（Acceptance Rate）、高质量项目数、覆盖实验数等。

#### （4）Baseline 对比
- **Random Guess**：随机猜测排名，期望 ρ = 0，Top-1 Acc ≈ 1/K（K为平均变体数）。
- **Attribute Score Only**：不经过训练模型，直接用LLM对属性打分排序。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）Ranking 模块性能（表2）
| 方法 | Spearman ρ | Top-1 Accuracy |
|------|------------|----------------|
| **Transfer Learning (Ours)** | **0.514 ± 0.166** | **70.2% ± 11.8%** |
| Random Guess Baseline | ~0 | ~42.9% |

> ✅ 结论：模型显著优于随机猜测，在真实客户数据上实现了超过70%的首名命中率。

#### （2）不同 Embedding 的 Leave-One-Out 表现（表3）
| Embedding | Spearman ρ |
|-----------|------------|
| **Llama** | **0.727 ± 0.116** ✅ |
| Attribute Score | 0.576 ± 0.142 |
| MiniLM | 0.454 ± 0.155 |
| Random Guess | 0 |

> ✅ 结论：**Llama embeddings** 表现最佳，说明更深层次的语言理解有助于捕捉影响CTR的关键语义信号。

#### （3）Insight Generation 人类评估结果（表4）
| LM 模型 | 生成数 | 高质量数 | 接受率 |
|--------|-------|---------|--------|
| **GPT-4o** | 52 | 46 | **88.46%** ✅ |
| LLaMA-70B | 26 | 23 | 88.46% |
| GPT-4o-mini | 157 | 66 | 42.04% ❌ |

> ✅ GPT-4o 在质量和数量间取得最佳平衡；mini 版本虽多产但噪声大。

#### （4）Opportunity Generation 人类评估结果（表5）
| LM 模型 | 生成数 | 高质量数 | 接受率 |
|--------|-------|---------|--------|
| **GPT-4o** | 195 | 168 | **86.15%** ✅ |
| GPT-4o-mini | 195 | 164 | 84.10% |
| LLaMA-70B | 123 | 101 | 82.11% |

> ✅ 所有主流LLM均能生成高接受度的机会建议，GPT-4o综合表现最优。

#### （5）LLM 自动评估结果（附录B）
- **Insight Evaluation (GPT-4o Judge)**：GPT-4o生成的insight仍获最高接受率（53.87%），远高于 mini 版本（16.87%）。
- **Opportunity Evaluation**：所有模型接受率 >75%，GPT-4o 达 **81.15%**，验证建议一致性高。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **历史A/B数据可用于有效迁移学习**：尽管训练数据（Upworthy）与客户场景存在领域偏移，ranking model 依然能在真实业务中实现显著高于随机的预测性能。
2. **attribute-level 投影极大提升可解释性**：通过 constrained Lasso 将黑箱 embedding weights 映射到营销语义属性，使模型输出可被业务人员理解和信任。
3. **Opportunity Index 可系统发现高价值改进点**：结合“重要性”与“表达不足”，能精准定位被忽略但潜在收益高的创意方向。
4. **LLM 是强大的解释与建议引擎**：配合结构化输入与 prompt engineering，LLM 可稳定输出高质量、文本锚定（text-grounded）的自然语言洞察与建议。

### ⚠️ 局限性
1. **领域迁移风险**：transfer learning 成功依赖于源域与目标域之间的语义对齐。若客户行业与 Upworthy 差异过大（如金融 vs 新闻），性能可能下降。
2. **语言限制**：目前仅支持英文内容，其他语言需重新构建 attribute lexicon 与 embedding pipeline。
3. **embedding 质量敏感**：不同 embedder（如 MiniLM vs Llama）性能差异显著，选择不当会影响整个 pipeline 效果。
4. **attribute set 固定性**：虽然允许客户定制，但初始122个属性可能无法覆盖所有垂直领域特有概念。

### 🔮 未来工作方向
1. **多模态扩展**：将框架推广至图像、视频、音频及页面布局等非文本内容。
2. **更多样化的 Outcome Modeling**：从 CTR 扩展到 conversion rate、revenue、LTV 等商业指标。
3. **动态 attribute discovery**：利用 LLM 自动生成新的语义属性，而非依赖人工预定义。
4. **个性化 ranking models**：为不同客户训练专属 ranker，缓解 domain shift 问题。
5. **闭环自动化实验设计**：将 opportunity suggestion 直接接入 GenAI 内容生成 pipeline，实现“建议→生成→测试→反馈”的全自动化循环。

---

> 💡 **总结一句话**：  
> *Experimentation Accelerator* 通过融合 historical A/B data、content-aware embeddings、interpretable modeling 与 GenAI，构建了一个**可解释、可行动、可规模化**的A/B测试增强系统，显著提升了数字营销实验的效率与智能水平。

</details>

---

### 15. [GREAT-EER: Graph Edge Attention Network for Emergency Evacuation Responses](https://arxiv.org/abs/2602.14676)

**Authors**: Attila Lischka, Bal\'azs Kulcs\'ar  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.14676v1  

#### Abstract
Emergency situations that require the evacuation of urban areas can arise from man-made causes (e.g., terrorist attacks or industrial accidents) or natural disasters, the latter becoming more frequent due to climate change. As a result, effective and fast methods to develop evacuation plans are of g...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GREAT-EER: Graph Edge Attention Network for Emergency Evacuation Responses

## 1. 论文的主要贡献和创新点

### 解决的问题
本文提出并研究了**Bus Evacuation Orienteering Problem (BEOP)**，这是一个在紧急疏散场景下、以公交车为工具的组合优化问题。其目标是在有限时间内，利用有限数量和容量的公交车，从城市区域疏散尽可能多的人口。

该问题源于现实中的紧急情况（如自然灾害、工业事故等），旨在缓解纯私家车疏散导致的交通拥堵和混乱。与传统的 Bus Evacuation Problem (BEP) 不同，BEOP 允许无法覆盖所有疏散点，转而追求最大化疏散人数，这使其更贴近实际资源受限的情况。

### 提出的新方法和新思路
- **提出了 BEOP 模型**：首次将多趟次、带时间窗的 capacitated team orienteering problem 应用于公交应急疏散，并形式化为一个 NP-hard 问题。
- **设计了混合整数线性规划 (MILP) 和马尔可夫决策过程 (MDP) 框架**：为 BEOP 提供了精确和序列决策两种建模方式。
- **开发了基于深度强化学习的框架 GREAT-EER**：
  - **Encoder**: 采用 **Graph Edge Attention Network (GREAT)**，专门处理非欧几里得、不对称的图结构（如真实道路网络中的单行道、拥堵）。
  - **Decoder**: 使用 multi-pointer network 进行逐步解码。
  - **训练机制**: 基于 POMO (Policy Optimization with Multiple Optima) 框架进行无监督强化学习训练，无需依赖求解器生成标签。

### 相比现有方法的优势
- **高效推理速度**：训练完成后，GREAT-EER 可在**秒级甚至毫秒级**内生成高质量疏散方案，远快于传统 MILP 求解器（可能需要数小时）。
- **高泛化能力**：模型能在未见过的更大规模实例（如 200 节点）、含“危险区”（hazard zones）的道路中断场景中保持良好表现。
- **支持动态在线决策**：可扩展至随机环境（如不确定的出行时间和需求），实时调整路线，避免违反硬约束（如超时）。
- **提供战略规划指导**：可用于事前分析不同参数（车辆数、容量、时间）对疏散配额的影响，辅助应急资源调配。

---

## 2. 核心实验方法和设置

### 数据集
- **地理数据来源**：使用 **OpenStreetMap** 获取旧金山市的真实道路网络（约 10,000 个节点）。
- **人口密度代理数据**：使用公开的 **Uber GPS dataset** 推断城市中人流密集的 POI（兴趣点），作为潜在的疏散点。
- **生成 BEOP 实例**：
  - 随机采样 100 或 200 个节点作为疏散点，另选一个作为安全中心（depot）。
  - 每个节点分配随机需求（1–5 人）和可选的时间窗（最多 30% 节点需在指定时间内被接走）。
  - 车辆参数（数量、容量）、最大疏散时间随机采样用于训练。

### 实验设置和评估指标
- **训练设置**：
  - 模型规模：GREAT Encoder 含 5 层，隐藏维度 128，8 个注意力头。
  - 优化器：ADAM，学习率 0.0001。
  - 训练数据：25,000 个 BEOP 实例，batch size 50，训练 50 轮。
  - 硬件：NVIDIA A40 GPU，总训练时间约 8 小时。
- **评估指标**：
  - **Evacuation Quota (%)**：成功通过公交车疏散的人数占总需求的比例。
  - **运行时间 (Runtime)**：生成解决方案所需时间。
  - **Optimality Gap (%)**：与 Gurobi 求解器在时间限制下所得上界之间的差距。

### 基线方法对比
- **Gurobi (MILP)**：使用商业求解器求解提出的 MILP 模型，设 30 分钟或 24 小时时间限制，作为近似最优参考。
- **Greedy Heuristic**：一种简单的贪心算法，每次选择单位时间收益最高的可行节点。
- **Warm-start Gurobi**：使用 GREAT-EER 的解作为 Gurobi 的初始解，测试其改进潜力。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在 **100 节点**的 BEOP 实例上：
  - GREAT-EER 平均可在 **~0.4 秒** 内完成求解。
  - 在多个配置下，其疏散配额达到 **85%–100%**，接近 Gurobi 在长时间运行后的结果。
- 在 **20 节点小规模实例**上的详细对比（见 Table 2）：
  - GREAT-EER 的平均最优性差距仅为 **0.0–2.18%**，显著优于 Greedy（1.21–14.44%）。
  - 运行时间：GREAT-EER ~0.38s vs. Gurobi >5000s vs. Greedy ~0.004s。
- 在 **200 节点大规模实例**上的泛化测试（见 Table 9）：
  - 即使超出训练分布，GREAT-EER 仍能实现 **87.4%–100%** 的疏散配额，始终优于 Greedy（78.82%–99.98%）。

### 与基线方法的对比结果
| 方法 | 平均疏散配额 | 平均运行时间 | 最优性差距 |
|------|---------------|----------------|-------------|
| **GREAT-EER** | 高 (≈ Gurobi) | **极快 (~0.4s)** | 极低 |
| **Gurobi (30min)** | 最高 | 极慢 (>10000s) | 0 (基准) |
| **Gurobi + GREAT-EER init** | **更高** | 快 (几分钟) | 负值（改进） |
| **Greedy** | 明显较低 | 最快 (~0.004s) | 高 |

- **关键发现**：GREAT-EER 的解可作为 Gurobi 的优秀 warm-start，使其在 **10 分钟内超越 GREAT-EER 自身性能**（例如从 94.41% 提升到 95.07%，见 Figure 5）。

### 消融实验与额外验证
- **Out-of-Distribution 测试**：
  - 在含 **hazard zones**（道路封锁）的场景中，GREAT-EER 仍能有效规划，疏散配额达 70–100%，远高于 Greedy。
  - 在 **200 节点**的大规模实例上，性能下降有限，证明强泛化性。
- **Stochastic Online Setting**：
  - 在模拟随机出行时间和需求变化的环境中，**stochastic GREAT-EER 模型**能够动态响应，**零违规**（无超时方案）。
  - 相比之下，确定性计划即使经过增强（POMO=100），仍有 **9–15% 的实例因超时而失效**。

---

## 4. 关键结论和发现

### 主要发现
- **BEOP 是一个实用且具有挑战性的新问题**，能更好反映现实应急疏散中的资源约束。
- **GREAT-EER 能够在极短时间内生成接近最优的疏散方案**，适用于事前规划和实时响应。
- **GREAT-EER 的输出是强大的 warm-start 解**，可极大加速精确求解器的收敛。
- **模型具备出色的泛化能力**，能适应更大规模、道路中断等复杂场景。
- **在随机动态环境中，GREAT-EER 可实现实时自适应调度**，确保方案可行性并提升资源利用率。

### 方法的局限性
- 当前 MDP 框架在随机设置下仅支持单辆车的实时调度，多车协同的在线优化尚未解决。
- 模型假设节点需求和出行时间的不确定性是外生的，未考虑个体选择行为（如更多人改用私家车）对交通流的反馈影响。
- 未考虑上下车时间、车辆异构性（如轮椅专用巴士）、以及对弱势群体的优先级调度。

### 未来工作方向
- 扩展至 **多车协同的随机在线调度**。
- 引入 **更复杂的动态模型**，如拥堵随疏散行为演化的反馈机制。
- 支持 **split-delivery**（同一地点多次访问）、**loading time** 和 **heterogeneous fleets**。
- 探索 **multi-objective 优化**，如兼顾公平性（equity）和效率。
- 进一步提升模型在 **超大规模城市网络** 上的可扩展性。

</details>

---

### 16. [WebWorld: A Large-Scale World Model for Web Agent Training](https://arxiv.org/abs/2602.14721)

**Authors**: Zikai Xiao, Jianhong Tu, Chuhang Zou, Yuxin Zuo, Zhi Li, Peng Wang, Bowen Yu, Fei Huang, Junyang Lin, Zuozhu Liu  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.14721v1  

#### Abstract
Web agents require massive trajectories to generalize, yet real-world training is constrained by network latency, rate limits, and safety risks. We introduce \textbf{WebWorld} series, the first open-web simulator trained at scale. While existing simulators are restricted to closed environments with ...

---

### 17. [Speculative Decoding with a Speculative Vocabulary](https://arxiv.org/abs/2602.13836)

**Authors**: Miles Williams, Young D. Kwon, Rui Li, Alexandros Kouris, Stylianos I. Venieris  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13836v1  

#### Abstract
Speculative decoding has rapidly emerged as a leading approach for accelerating language model (LM) inference, as it offers substantial speedups while yielding identical outputs. This relies upon a small draft model, tasked with predicting the outputs of the target model. State-of-the-art speculativ...

---

### 18. [Scenario-Adaptive MU-MIMO OFDM Semantic Communication With Asymmetric Neural Network](https://arxiv.org/abs/2602.13557)

**Authors**: Chongyang Li, Tianqian Zhang, Shouyin Liu  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.13557v1  

#### Abstract
Semantic Communication (SemCom) has emerged as a promising paradigm for 6G networks, aiming to extract and transmit task-relevant information rather than minimizing bit errors. However, applying SemCom to realistic downlink Multi-User Multi-Input Multi-Output (MU-MIMO) Orthogonal Frequency Division ...

---

### 19. [Attention in Constant Time: Vashista Sparse Attention for Long-Context Decoding with Exponential Guarantees](https://arxiv.org/abs/2602.13804)

**Authors**: Vashista Nobaub  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.13804v1  

#### Abstract
Large language models spend most of their inference cost on attention over long contexts, yet empirical behavior suggests that only a small subset of tokens meaningfully contributes to each query. We formalize this phenomenon by modeling attention as a projection onto the convex hull of key vectors ...

---

### 20. [Small Reward Models via Backward Inference](https://arxiv.org/abs/2602.13551)

**Authors**: Yike Wang, Faeze Brahman, Shangbin Feng, Teng Xiao, Hannaneh Hajishirzi, Yulia Tsvetkov  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.13551v1  

#### Abstract
Reward models (RMs) play a central role throughout the language model (LM) pipeline, particularly in non-verifiable domains. However, the dominant LLM-as-a-Judge paradigm relies on the strong reasoning capabilities of large models, while alternative approaches require reference responses or explicit...

---

### 21. [LLM-Guided Knowledge Distillation for Temporal Knowledge Graph Reasoning](https://arxiv.org/abs/2602.14428)

**Authors**: Wang Xing, Wei Song, Siyu Lin, Chen Wu, Man Wang  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.14428v1  

#### Abstract
Temporal knowledge graphs (TKGs) support reasoning over time-evolving facts, yet state-of-the-art models are often computationally heavy and costly to deploy. Existing compression and distillation techniques are largely designed for static graphs; directly applying them to temporal settings may over...

---

### 22. [Federated Learning of Nonlinear Temporal Dynamics with Graph Attention-based Cross-Client Interpretability](https://arxiv.org/abs/2602.13485)

**Authors**: Ayse Tursucular, Ayush Mohanty, Nazal Mohamed, Nagi Gebraeel  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.13485v1  

#### Abstract
Networks of modern industrial systems are increasingly monitored by distributed sensors, where each system comprises multiple subsystems generating high dimensional time series data. These subsystems are often interdependent, making it important to understand how temporal patterns at one subsystem r...

---

### 23. [Pseudo-differential-enhanced physics-informed neural networks](https://arxiv.org/abs/2602.14663)

**Authors**: Andrew Gracyk  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.14663v1  

#### Abstract
We present pseudo-differential enhanced physics-informed neural networks (PINNs), an extension of gradient enhancement but in Fourier space. Gradient enhancement of PINNs dictates that the PDE residual is taken to a higher differential order than prescribed by the PDE, added to the objective as an a...

---

### 24. [Extending Multi-Source Bayesian Optimization With Causality Principles](https://arxiv.org/abs/2602.14791)

**Authors**: Luuk Jacobs, Mohammad Ali Javidian  
**Category**: cs.LG  
**Published**: 2026-02-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.14791v1  

#### Abstract
Multi-Source Bayesian Optimization (MSBO) serves as a variant of the traditional Bayesian Optimization (BO) framework applicable to situations involving optimization of an objective black-box function over multiple information sources such as simulations, surrogate models, or real-world experiments....

---

### 25. [Hippocampus: An Efficient and Scalable Memory Module for Agentic AI](https://arxiv.org/abs/2602.13594)

**Authors**: Yi Li, Lianjie Cao, Faraz Ahmed, Puneet Sharma, Bingzhe Li  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.13594v1  

#### Abstract
Agentic AI require persistent memory to store user-specific histories beyond the limited context window of LLMs. Existing memory systems use dense vector databases or knowledge-graph traversal (or hybrid), incurring high retrieval latency and poor storage scalability. We introduce Hippocampus, an ag...

---

### 26. [Cognitive Chunking for Soft Prompts: Accelerating Compressor Learning via Block-wise Causal Masking](https://arxiv.org/abs/2602.13980)

**Authors**: Guojie Liu, Yiqi Wang, Yanfeng Yang, Wenqi Fan, Songlei Jian, Jianfeng Zhang, Jie Yu  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.13980v1  

#### Abstract
Providing extensive context via prompting is vital for leveraging the capabilities of Large Language Models (LLMs). However, lengthy contexts significantly increase inference latency, as the computational cost of self-attention grows quadratically with sequence length. To mitigate this issue, contex...

---

### 27. [Prompt-Driven Low-Altitude Edge Intelligence: Modular Agents and Generative Reasoning](https://arxiv.org/abs/2602.14003)

**Authors**: Jiahao You, Ziye Jia, Chao Dong, Qihui Wu  
**Category**: cs.AI  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.14003v1  

#### Abstract
The large artificial intelligence models (LAMs) show strong capabilities in perception, reasoning, and multi-modal understanding, and can enable advanced capabilities in low-altitude edge intelligence. However, the deployment of LAMs at the edge remains constrained by some fundamental limitations. F...

---

### 28. [LogitsCoder: Towards Efficient Chain-of-Thought Path Search via Logits Preference Decoding for Code Generation](https://arxiv.org/abs/2602.14054)

**Authors**: Jizheng Chen, Weiming Zhang, Xinyi Dai, Weiwen Liu, Kounianhua Du, Yasheng Wang, Ruiming Tang, Yong Yu, Weinan Zhang  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.14054v1  

#### Abstract
Code generation remains a challenging task that requires precise and structured reasoning. Existing Test Time Scaling (TTS) methods, including structured tree search, have made progress in exploring reasoning paths but still face two major challenges: (1) underthinking, where reasoning chains tend t...

---

### 29. [Text Style Transfer with Parameter-efficient LLM Finetuning and Round-trip Translation](https://arxiv.org/abs/2602.15013)

**Authors**: Ruoxi Liu, Philipp Koehn  
**Category**: cs.CL  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15013v1  

#### Abstract
This paper proposes a novel method for Text Style Transfer (TST) based on parameter-efficient fine-tuning of Large Language Models (LLMs). Addressing the scarcity of parallel corpora that map between styles, the study employs roundtrip translation to synthesize such parallel datasets from monolingua...

---

### 30. [Floe: Federated Specialization for Real-Time LLM-SLM Inference](https://arxiv.org/abs/2602.14302)

**Authors**: Chunlin Tian, Kahou Tam, Yebo Wu, Shuaihang Zhong, Li Li, Nicholas D. Lane, Chengzhong Xu  
**Category**: cs.DC  
**Published**: 2026-02-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.14302v1  

#### Abstract
Deploying large language models (LLMs) in real-time systems remains challenging due to their substantial computational demands and privacy concerns. We propose Floe, a hybrid federated learning framework designed for latency-sensitive, resource-constrained environments. Floe combines a cloud-based b...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
