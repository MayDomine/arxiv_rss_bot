# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-12 06:46:51 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [MoEEdit: Efficient and Routing-Stable Knowledge Editing for Mixture-of-Experts LLMs](https://arxiv.org/abs/2602.10965)

**Authors**: Yupu Gu, Rongzhe Wei, Andy Zhu, Pan Li  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.10965v1  

#### Abstract
Knowledge editing (KE) enables precise modifications to factual content in large language models (LLMs). Existing KE methods are largely designed for dense architectures, limiting their applicability to the increasingly prevalent sparse Mixture-of-Experts (MoE) models that underpin modern scalable L...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoEEdit: Efficient and Routing-Stable Knowledge Editing for Mixture-of-Experts LLMs —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的 **Knowledge Editing (KE)** 方法大多针对 **dense Transformer 架构** 设计，而现代大规模语言模型（如 Qwen、GPT-OSS）广泛采用 **Mixture-of-Experts (MoE)** 架构。直接将传统 KE 方法应用于 MoE 模型面临三大挑战：
1. **计算成本高**：需更新所有专家参数，导致计算开销呈指数级增长（例如 128 个专家 → 成本 ×128）。
2. **专家耦合（Expert Coupling）**：输出是多个专家加权组合的结果，单个专家修改可能被稀释或引发副作用。
3. **路由分布偏移（Routing Distribution Shift）**：参数扰动会改变下游 MoE 层的输入，从而影响路由器选择的专家集合，破坏模型原有的知识路径和稳定性。

这些问题使得在 MoE 模型中实现**高效、稳定且局部化**的知识编辑变得极为困难。

---

### 🚀 提出的新方法：MoEEdit
作者提出了 **MoEEdit**，这是首个专为 MoE 架构设计的、具有 **routing-stable** 特性的参数修改型知识编辑框架。其核心创新包括：

#### （1）Per-Expert Null-Space Projection（每专家零空间投影）
- **思想来源**：受 AlphaEdit 在 dense 模型中使用 null-space 投影启发。
- **改进之处**：在每个专家内部独立构造一个正交投影矩阵 $ P_n $，确保对专家参数的更新 $ \Delta_n $ 只作用于不影响保留集（preservation set）的方向。
- **效果**：保证编辑不干扰无关知识，并**显式抑制路由输入的变化**，从而防止下游路由分布发生偏移。

#### （2）Randomized Block Coordinate Descent (BCD) Solver
- 将全局优化问题分解为按“专家”划分的块状子问题。
- 使用随机块坐标下降法逐个优化相关专家，避免求解规模达 $ O(Nd_k)^2 $ 的大型矩阵逆。
- **复杂度从 $ O(d_m (Nd_k)^2) $ 降至 $ O(d_k^2) $**，仅与专家隐藏维度有关，与总专家数无关，极大提升可扩展性。

---

### 🔍 相比现有方法的优势
| 维度 | MoEEdit | 传统方法（如 FT, UnKE） |
|------|--------|--------------------------|
| **适用架构** | 显式支持 MoE | 主要为 dense 模型设计 |
| **计算效率** | 高效，仅更新关键专家 | 更新全部专家，成本高昂 |
| **路由稳定性** | 强制保持路由不变（via 投影） | 忽视路由变化，易造成 cascading drift |
| **编辑特异性** | 更高，干扰更少 | 容易破坏无关行为 |
| **可扩展性** | 支持上百专家的大规模 MoE | 在大规模 MoE 上难以运行 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **COUNTERFACT**：标准反事实编辑数据集，用于测试单跳事实替换（如 “Bing Videos is from Microsoft → Google”），强调编辑准确性和泛化能力。
- **zsRE (Zero-Shot Relation Extraction)**：零样本关系抽取任务，评估模型在未见过句式下的推理迁移能力。

> 示例见附录（Appendix D），包含原始预测、目标对象、改写提示等。

---

### ⚙️ 实验设置
| 项目 | 设置 |
|------|------|
| **模型** | `Qwen3-30B-A3B`（128专家，top-8激活）、`GPT-OSS-20B`（32专家，top-4激活） |
| **编辑方式** | 批量连续编辑（sequential batched edits），共进行 1,000 次编辑，每次批大小为 50 |
| **硬件平台** | 单台配备 NVIDIA H20 GPU 的节点，权重使用 BF16，优化过程使用 FP32 |
| **编辑层** | 多层联合编辑（如 Qwen 编辑 layer {3,4,5,6,7}） |

---

### 📊 评估指标
沿用 KE 领域标准三元组指标：
1. **Efficacy（有效性）**：在编辑提示下正确生成新事实的比例。
2. **Generalization（泛化性）**：在同义改写（paraphrase）上下文中仍能正确输出的能力。
3. **Specificity（特异性）**：对无关控制样例无影响的程度（即 locality）。
4. **Utility（综合效用）**：上述三项的平均值，反映整体平衡表现。

此外引入：
- **Routing Similarity (RS)**：前后编辑 Top-K 路由专家集合的 Jaccard 相似度，衡量路由稳定性。
- **KL Divergence**：前后路由分布之间的 KL 散度，量化分布漂移程度。

---

### 🆚 基线方法对比
| 方法 | 类型 | 是否适配 MoE |
|------|------|-------------|
| **Fine-Tuning (FT)** | 全微调 | 是，但低效 |
| **FT-L** | 加范数约束的微调 | 是 |
| **AdaLoRA** | 参数高效微调（PEFT） | 是 |
| **UnKE** | 结构化 KE 方法（原为 dense 设计） | 是，直接应用 |
| **MoEEdit (Ours)** | 专为 MoE 设计的 KE 方法 | ✅ 本文提出 |

> 所有 baseline 均尝试迁移到 MoE 架构上运行。

---

## 3. 主要实验结果和性能指标

### 📈 性能对比（Table 1）

#### 在 **Qwen3-30B-A3B + COUNTERFACT** 上：
| Method | Eff. ↑ | Gen. ↑ | Spe. ↑ | Utility ↑ |
|--------|--------|--------|--------|-----------|
| UnKE | 89.30 | 82.85 | 48.15 | 73.43 |
| **MoEEdit** | **99.30** | **94.10** | **80.97** | **91.46** |

> MoEEdit 在所有指标上全面领先，尤其在 specificity 上远超 UnKE（+32.82），说明更强的局部性。

#### 在 **GPT-OSS-20B + ZsRE** 上：
| Method | Eff. ↑ | Gen. ↑ | Spe. ↑ | Utility ↑ |
|--------|--------|--------|--------|-----------|
| AdaLoRA | 43.46 | 42.96 | 33.60 | 40.01 |
| **MoEEdit** | **81.68** | **68.44** | **32.55** | **60.89** |

> MoEEdit 在 efficacy 和 generalization 上分别提升超过 **+38** 和 **+25** 个百分点，尽管 specificity 略低，但整体 utility 显著更高。

✅ **结论**：MoEEdit 在不同模型和数据集上均取得 SOTA 表现，在准确性与泛化性方面优势巨大。

---

### 🛠️ 消融实验结果（Ablation Study）

#### （1）消去投影机制的影响（Table 3）
| 设置 | Editing Set RS ↓ | Preservation Set RS ↓ |
|------|------------------|------------------------|
| MoEEdit (完整版) | 86.62–89.93 | 87.02–90.22 |
| MoEEdit (w/o Proj) | 73.64–73.75 | 73.59–73.50 |

> 移除投影后，**平均 RS 下降约 15 个百分点**，KL 散度从 0.02 升至 0.0834，证明 **null-space projection 对维持路由稳定至关重要**。

#### （2）BCD 与闭式求解器对比（Figure 3）
- **闭式求解器（Closed-form）**：时间随专家数量 $ N $ 呈近二次增长，在 $ N > 60 $ 后不可行。
- **BCD Solver**：运行时间几乎恒定，即使在 $ N=128 $ 时仍高效。
> ✅ 验证了 BCD 的**可扩展性优势**。

#### （3）BCD 迭代次数分析（Figure 4）
- **2–6 次 pass**：性能快速上升；
- **>10 次 pass**：收益递减。
> 推荐使用 **6–10 次 pass**，即可达到性能与效率的最佳平衡。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE 架构需要专门的知识编辑方法**  
   直接迁移 dense 模型的 KE 方法会导致严重的路由不稳定和计算瓶颈。

2. **Routing Stability 是 MoE 编辑的核心挑战**  
   微小参数扰动可通过路由机制传播并放大，破坏模型内部的知识组织结构。MoEEdit 首次明确提出并解决了这一问题。

3. **Expert-Aware Design 至关重要**  
   利用 MoE 的模块化特性（每个 expert 是独立块），通过 per-expert null-space projection 和 block-wise BCD 实现高效稳定的编辑。

4. **MoEEdit 实现了 SOTA 性能与卓越效率的统一**  
   不仅在 Efficacy、Generalization 上显著优于 baselines，还在 Specificity 和 Routing Stability 上表现出色。

---

### ⚠️ 方法的局限性
1. **依赖定位阶段（locate-then-edit）**  
   仍需依赖 causal tracing 等手段确定编辑层和专家，尚未完全自动化。
2. **主要面向 FFN 层中的 key-value 存储假设**  
   假设知识存储在 FFN 的 down-projection 中，可能不适用于所有类型的事实。
3. **未处理多跳或多专家协同编辑场景**  
   当前方法聚焦于单跳事实，复杂知识结构的编辑尚待研究。

---

### 🔮 未来工作方向
1. **自动识别关键专家子集**  
   开发无需人工指定编辑层/专家的选择机制。
2. **支持多跳知识编辑与因果链更新**  
   应对涉及多个实体和关系的复合事实。
3. **结合 parameter-preserving 方法构建 hybrid 编辑系统**  
   如融合 SERAC 或 LEMoE 的外部记忆机制，增强灵活性与可逆性。
4. **探索 MoEEdit 在模型遗忘（machine unlearning）中的应用**  
   利用其高 specificity 实现精准删除特定知识。

---

> 💡 **一句话总结**：  
> **MoEEdit 是首个专为 Mixture-of-Experts 架构设计的 routing-stable 知识编辑方法，通过 per-expert null-space projection 抑制路由漂移，配合高效的 randomized BCD 求解器，在保持高 specificity 的同时实现了 SOTA 的编辑性能与卓越的可扩展性，为 MoE-based LLMs 的持续可控演化奠定了坚实基础。**

🔗 **代码开源地址**：[https://github.com/Terence-Gu/MoEEdit](https://github.com/Terence-Gu/MoEEdit)

</details>

---

### 2. [Learning to Evict from Key-Value Cache](https://arxiv.org/abs/2602.10238)

**Authors**: Luca Moschella, Laura Manduchi, Ozan Sener  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.10238v1  

#### Abstract
The growing size of Large Language Models (LLMs) makes efficient inference challenging, primarily due to the memory demands of the autoregressive Key-Value (KV) cache. Existing eviction or compression methods reduce cost but rely on heuristics, such as recency or past attention scores, which serve o...

---

### 3. [Found-RL: foundation model-enhanced reinforcement learning for autonomous driving](https://arxiv.org/abs/2602.10458)

**Authors**: Yansong Qu, Zihao Sheng, Zilin Huang, Jiancong Chen, Yuhao Luo, Tianyi Wang, Yiheng Feng, Samuel Labi, Sikai Chen  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.10458v1  

#### Abstract
Reinforcement Learning (RL) has emerged as a dominant paradigm for end-to-end autonomous driving (AD). However, RL suffers from sample inefficiency and a lack of semantic interpretability in complex scenarios. Foundation Models, particularly Vision-Language Models (VLMs), can mitigate this by offeri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 《Found-RL: foundation model-enhanced reinforcement learning for autonomous driving》核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

- **样本效率低**：传统 Reinforcement Learning (RL) 在自动驾驶任务中依赖大量试错，训练过程缓慢且低效。
- **语义可解释性差**：RL 策略缺乏对复杂交通场景的语义理解能力，难以处理安全敏感的驾驶决策。
- **计算延迟瓶颈**：将大型 Foundation Models（如 VLMs）直接集成到高频 RL 训练循环中会因推理延迟导致训练停滞。

### **提出了什么新方法或新思路**

提出 **Found-RL** —— 一个专为自动驾驶设计的 **foundation model-enhanced RL 平台**，其核心创新包括：

#### （1）**异步批量推理框架（Asynchronous Batch Inference Framework）**
- 将高开销的 VLM 推理从实时仿真循环中解耦。
- 通过客户端-服务器架构，将多个环境的观测打包成微批次进行并行推理，显著降低延迟影响。
- 支持 VLM 反馈的“非阻塞”获取，实现近实时的 RL 训练闭环。

#### （2）**VLM 动作引导机制（VLM Action Guidance）**
- **Value-Margin Regularization (VMR)**：在 critic 学习中引入正则项，强制 VLM 建议动作的价值高于当前策略动作一个固定 margin，引导策略向专家行为靠拢。
- **Advantage-Weighted Action Guidance (AWAG)**：仅当 VLM 建议动作被 critic 判断优于当前策略时，才通过加权方式模仿该动作，实现安全的探索约束。

#### （3）**基于 CLIP 的奖励塑形（CLIP-based Reward Shaping）**
- 提出 **Conditional Contrastive Action Alignment** 方法：
  - 将车速和导航指令离散化，构建上下文条件提示（context-conditioned prompts）。
  - 定义小规模的动作锚点集（action anchors），避免全局分类的概率稀释。
  - 计算 VLM 对当前动作与最难负样本之间的余弦相似度 margin，并标准化后作为稠密奖励信号。
- 有效缓解了 CLIP 模型对动态状态不敏感的问题。

---

### **相比现有方法的优势**

| 维度 | Found-RL 的优势 |
|------|----------------|
| **效率** | 异步批处理使 VLM 反馈吞吐量接近 100%，支持实时训练（~500 FPS） |
| **性能** | 轻量级 RL 模型（3.82M 参数）达到甚至超越数十亿参数 VLM 的驾驶表现 |
| **安全性** | 显著降低碰撞率和红灯违规次数，策略更符合交通规则 |
| **通用性** | 模块化设计支持多种 VLM 和 RL 算法组合，易于扩展 |

---

## 2. 核心实验方法和设置

### **使用的数据集与仿真平台**

- **仿真环境**：基于 **CARLA** 构建，支持多城镇（Town01–Town06）、多样化交通流和天气条件。
- **基准测试**：
  - **Leaderboard Benchmark**：训练于 Town01/03/04/06，测试于所有六镇。
  - **NoCrash Benchmark**：训练于 Town01，测试于 Town01（seen）和 Town02（unseen）。
- **VLM 微调数据**：收集约 **137万条** state-action 轨迹，来自 Roach PPO 和 Autopilot 专家策略。

### **实验设置**

- **观察空间**：
  - VLM-based agents：BEV 图像 (192×192×3) + 文本提示。
  - RL-based agents：BEV Masks (96×96×15) + 结构化状态（速度、命令等）。
- **动作空间**：连续控制（throttle/brake, steer）。
- **训练模式**：Off-policy actor-critic 框架（SAC, DrQv2, TD3）。

### **评估指标**

分为四大类：

| 类别 | 指标 |
|------|------|
| **Comprehensive** | Return, Driving Score, Infraction Penalty |
| **Route** | Success Rate, Route Completion, Speed |
| **Energy** | Icell（电池电流估计）, Fuel Rate |
| **Safety** | Collisions (Ped./Veh.), Red Light Violations |

> 注：评估采用更严格的终止逻辑（如轻微偏离即终止），确保策略鲁棒性。

### **基线方法对比**

#### （1）**Online RL Baselines**
- TD3, SAC, DrQv2（标准 RL 算法）

#### （2）**VLM Baselines**
- InternVL3-1B/2B, Qwen2.5-vl-3B/7B, Visual RWKV-0.1B（均为 192×192 输入）

#### （3）**其他 AD 方法**
- NEAT, LAV, TransFuser 等（基于 RGB/LiDAR）
- BC, AWAC, CQL, IQL（IL / offline RL）

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Leaderboard Benchmark）**

| 方法 | Driving Score ↑ | Success Rate ↑ | Red Light ↓ |
|------|------------------|----------------|-------------|
| **DrQv2-CLIP (Ours)** | **0.77** | **0.57** | **0.01** |
| DrQv2 | 0.56 | 0.38 | 0.02 |
| SAC | 0.21 | 0.03 | 0.05 |
| Qwen2.5-vl-7B | 0.76 | 0.65 | 0.05 |
| TransFuser | 0.61 | 0.87 | 0.71 |

> ✅ **Found-RL 在驾驶得分上达到 SOTA，同时红灯违规远低于大多数基线。**

### **NoCrash Benchmark 性能（Town02, Unseen）**

| 方法 | Driving Score ↑ | Success Rate ↑ | Vehicle Collisions ↓ |
|------|------------------|----------------|------------------------|
| **DrQv2-CLIP (Ours)** | **0.76** | **0.71** | **0.73/km** |
| DrQv2 | 0.73 | 0.77 | 1.27/km |
| Qwen2.5-vl-7B | 0.89 | 0.83 | 0.07/km |
| SAC | 0.24 | 0.07 | 2.44/km |

> ✅ **SAC-VMR 将成功率从 7% 提升至 47%；DrQv2-CLIP 在未知城镇保持高泛化能力。**

### **与 VLM 的对比（模型大小与效率）**

| 方法 | Model Size | Input Size | FPS | Driving Score |
|------|------------|------------|-----|---------------|
| **Found-RL (DrQv2-CLIP)** | **3.82M** | 96×96 | **500** | 0.77 |
| Qwen2.5-vl-7B | 7B | 192×192 | 0.89 | 0.76 |
| InternVL3-1B | 1B | 192×192 | 1.26 | 0.63 |

> ✅ **模型缩小 260–1800 倍，输入分辨率降低 75%，推理速度提升 500 倍以上，性能持平甚至反超。**

### **消融实验结果**

- **VMR/AWAG 有效性**：
  - 加入 VMR 后，SAC 的 Driving Score 从 0.21 → 0.53（Leaderboard）。
  - AWAG 显著提升早期学习效率，尤其在弱基线上效果明显。
- **CLIP 奖励塑形作用**：
  - DrQv2-CLIP 相比 DrQv2，Success Rate 提升 19%，Vehicle Collisions 下降 26%。
  - 消融显示移除 CLIP 奖励会导致收敛变慢、安全性下降。
- **异步推理效率**：
  - 奖励可用性维持在 **>99.8%**，验证了高吞吐批处理的有效性（见 Fig. 10）。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **语义先验可高效蒸馏进轻量 RL 模型**  
   即使是百万级参数的 RL 策略，也能通过 VLM 的监督达到十亿级 VLM 的驾驶水平。

2. ✅ **异步批处理是实用化关键**  
   解耦 VLM 推理与仿真循环，使得高延迟模型仍可用于实时训练，突破工程瓶颈。

3. ✅ **稠密语义奖励显著提升样本效率与安全性**  
   CLIP-based reward shaping 提供细粒度反馈，加速策略收敛并增强合规性。

4. ✅ **轻量模型在规则遵守方面更具优势**  
   尽管大 VLM 在上限性能上更强，但 Found-RL 在红灯违规等静态规则上表现更优。

---

### **方法的局限性**

- **视觉输入压缩可能损失细节**：使用 96×96 BEV Mask 可能在极端复杂动态场景下不如原始图像鲁棒。
- **VLM 反馈可能存在偏差**：若微调数据存在系统性错误，可能误导 RL 策略。
- **当前依赖预定义动作空间**：CLIP 奖励需离散化动作，限制了完全端到端的连续控制潜力。

---

### **未来工作方向**

1. **进一步优化推理延迟**：探索 VLM 量化、蒸馏、硬件加速以支持更大模型。
2. **挑战更高难度基准**：在 CARLA Leaderboard 2.0 或真实世界数据上验证泛化性。
3. **多 VLM 协同指导**：构建多专家系统，不同 VLM 分别提供安全性、舒适性、效率等方面的反馈。
4. **轻量 VLM 作为策略主干**：尝试将小型 VLM 直接用于 end-to-end 规划，而非仅作辅助监督。

---

> 🔗 **代码、数据与模型已开源**：https://github.com/ys-qu/found-rl

</details>

---

### 4. [How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning](https://arxiv.org/abs/2602.10622)

**Authors**: Jiahao Yuan, Yike Xu, Jinyong Wen, Baokun Wang, Yang Chen, Xiaotong Lin, Wuliang Huang, Ziyi Gao, Xing Fu, Yu Cheng, Weiqiang Wang  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.10622v1  

#### Abstract
Decoder-only large language models are increasingly used as behavioral encoders for user representation learning, yet the impact of attention masking on the quality of user embeddings remains underexplored. In this work, we conduct a systematic study of causal, hybrid, and bidirectional attention ma...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **decoder-only LLMs** 虽然在生成任务中表现出色，但在作为 **user representation learning** 的编码器时，其标准的 **causal attention masking** 限制了模型对用户行为序列中长距离依赖和未来上下文的建模能力。尽管已有研究尝试引入 **bidirectional attention** 或 **hybrid masking**，但缺乏系统性的比较，且从因果注意力向双向注意力过渡的训练动态未被充分探索。

本文旨在解决以下核心问题：
- 不同 attention masking 策略（causal、hybrid、bidirectional）如何影响 decoder-only LLM 在用户表征学习中的表现？
- 如何平稳地实现从 causal 到 bidirectional attention 的训练过渡，以提升优化稳定性和最终表征质量？

### 提出的新方法：Gradient-Guided Soft Masking (GG-SM)

作者提出了一种新的训练机制——**Gradient-Guided Soft Masking (GG-SM)**，用于改善从 causal 到 bidirectional attention 的过渡过程。该方法分为两个阶段：

1. **Gradient-Guided Warmup Phase**  
   在训练初期（前 $ T_{\text{warm}} $ 步），通过计算损失函数对隐藏状态的梯度范数 $\| \nabla_{h_i} \mathcal{L} \|$ 来动态决定未来 token 的可见程度。梯度越大，说明该 token 对当前表示越重要，因此赋予更高的“软掩码权重”。这一阶段无需额外参数，完全基于数据驱动。

2. **Linear Scheduler Phase**  
   在 warmup 结束后，将得到的 soft mask 固定，并线性插值到全双向 attention（即逐渐打开所有 future token 的访问权限），直到训练结束。

最终推理时使用 **fully bidirectional attention**，最大化上下文整合能力。

### 相比现有方法的优势

| 方法 | 缺陷 | GG-SM 的改进 |
|------|------|---------------|
| **Causal Masking** | 仅能利用历史信息，无法捕捉未来行为线索 | 允许模型逐步学习全局依赖 |
| **Hybrid Masking** | 架构复杂，需引入额外模块（如 MLP、global query） | 无额外参数，纯基于梯度信号 |
| **Scheduler-only** | 线性调度是固定的，不考虑样本或 token 级别的语义重要性 | 引入数据自适应的 warmup，更平滑稳定 |

> ✅ **核心优势**：GG-SM 在保持与 decoder 预训练兼容的同时，显著提升了 bidirectional 表征的质量和训练稳定性。

---

## 2. 核心实验方法和设置

### 数据集

实验基于大规模真实世界数据，来自 **Alipay 用户行为日志**，构建两类对齐训练数据：

1. **Rule-based Behavioral Trajectories Dataset ($ D_{\text{behavior}} $)**  
   - 输入：过去 90 天多模态用户行为序列（PayBill、Mini Program、SPM、App、Search、Tabular）
   - 输出：未来一个月内的聚合行为作为预测目标
   - 构造方式：规则对齐 + 随机采样子集作为正例

2. **LLM-Synthesized Query-Answer Alignments Dataset ($ D_{\text{qa}} $)**  
   - 使用 **Qwen-Max** 自动生成用户理解相关的 QA 对
   - 流程：
     1. 从种子池中检索与用户历史相关的情景
     2. 生成初始 QA 对并构建校准集 $ D_c $
     3. 使用强 embedding 模型评估 alignment difficulty（$ S_d = 1 - \text{Sim}(u \oplus q, a) $）
     4. 保留高难度样本（$ S_d \geq T_{\text{filter}} $）
     5. 应用 inductive feature completion 提取共性规则优化 prompt
     6. 扩展生成高质量 hard positive 样本

> 💡 这种合成策略避免了实时负样本挖掘的成本，提升了训练数据的信息密度。

### 实验设置

- **模型架构**：
  - 主干 LLM：`Qwen2.5-0.5B-Instruct`（decoder-only）
  - 模态编码器：`gte-base-zh` 分别处理各行为模态
  - 投影适配器：LoRA（rank=64, α=64）
- **训练目标**：InfoNCE contrastive loss
  $$
  \mathcal{L}_{\text{cl}} = -\log \frac{\exp(s(u_i, a_i)/\tau)}{\sum_j \exp(s(u_i, a_j)/\tau)}
  $$
  并引入 mask factor $ m_{ij} $ 防止 false negatives。
- **硬件配置**：64 × A100-80GB GPUs（训练），单卡推理
- **超参统一**：batch size=2048，AdamW，lr=2e-4，cosine decay，7w steps

### 评估指标

- **下游任务**：9 个工业级 binary classification benchmark，涵盖三大类：
  1. **User Prediction**（登录、流失、演唱会点击等）
  2. **Behavior Preference**（公共交通偏好、消费能力、饮食兴趣、电影偏好）
  3. **Marketing Sensitivity**（成就导向、身体管理倾向）
- **评价指标**：AUC（Area Under ROC Curve）

### 基线方法对比

| 类别 | 方法 |
|------|------|
| **通用 Embedding 模型** | Qwen3-Embedding-0.6B, Llama-embed-nemotron-8B, KaLM-Embedding-Gemma3-12B |
| **传统用户建模** | MSDP, One4all, CPC |
| **LLM-based 用户表征** | FOUND, InstructUE |
| **不同 masking 策略** | Causal, Hybrid (mask/gq/mlp), Bidirectional (direct/scheduler/GG-SM) |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（平均 AUC）

| 方法 | Avg AUC |
|------|---------|
| **Qwen3-Embedding-0.6B** | 0.6720 |
| **Llama-embed-nemotron-8B** | 0.7357 |
| **KaLM-Embedding** | 0.7156 |
| **FOUND (Dou et al.)** | 0.7690 |
| **InstructUE (Gao et al.)** | 0.7728 |
| **Ours (w/ GG-SM)** | **0.7745** ✅ |

> 📌 尽管 `Qwen2.5-0.5B` 参数量远小于其他 8B+ 模型，但通过 GG-SM 实现了最优性能。

### 详细任务性能对比（部分关键项）

| 方法 | Concert | Power | Movie | Achiev. | Physical |
|------|--------|-------|-------|--------|----------|
| Causal (Oracle) | 0.5173 | 0.7638 | 0.6435 | 0.5415 | 0.5592 |
| w/Causal | 0.5716 | 0.9678 | 0.7922 | 0.6054 | 0.6589 |
| w/Bidirectional | 0.5707 | 0.9671 | 0.7906 | 0.6043 | 0.6607 |
| w/Scheduler | 0.5742 | 0.9688 | 0.7908 | 0.6056 | 0.6605 |
| **w/GG-SM (Ours)** | **0.5767** | **0.9689** | **0.7913** | **0.6078** | **0.6615** |

> ✅ GG-SM 在几乎所有任务上均取得最佳表现，尤其在稀疏行为预测（Concert）和敏感意图识别（Physical）上有明显增益。

### 消融实验结果

#### (1) 不同 masking 策略对比（同一 backbone 下）

| 方法 | Avg AUC |
|------|---------|
| Causal | 0.7709 |
| Hybrid-mask | 0.7710 |
| Hybrid-gq | 0.7706 |
| Hybrid-mlp | 0.7718 |
| Bidirectional (direct) | 0.7721 |
| Bidirectional + Scheduler | 0.7733 |
| **Bidirectional + GG-SM (Ours)** | **0.7745** |

> 🔍 发现：
> - 单纯切换为 bidirectional 可带来增益；
> - 加入 scheduler 后进一步提升；
> - **GG-SM 在此基础上仍能持续增益 0.12% AUC**，证明 warmup 阶段的重要性。

#### (2) 训练稳定性分析（Fig. 3）

- **GG-SM 收敛更快、波动更小**
- Scheduler 方法在 transition 阶段出现明显 loss spike
- GG-SM 利用梯度引导，使模型“有准备”地接受未来信息，避免 abrupt change

---

## 4. 关键结论和发现

### 主要发现

1. **Attention masking 设计至关重要**  
   对于 decoder-only LLM 用于 user representation learning，不能简单沿用 causal masking；**bidirectional attention 更适合 sentence-level 表征提取**。

2. **过渡路径比最终 mask 更关键**  
   从 causal 到 bidirectional 的训练动态直接影响优化稳定性和最终性能。**直接跳转会导致训练不稳定，而渐进式开放需配合数据感知机制**。

3. **GG-SM 显著提升表征质量和训练稳定性**  
   通过梯度指导的 soft masking warmup，实现了更平滑的注意力演化，使得模型能够优先关注对任务更重要的 future tokens。

4. **小模型也能超越大模型**  
   尽管仅使用 0.5B 参数的 Qwen2.5，但通过领域适配的训练策略（GG-SM + 合成 hard positive），性能超过多个 8B+ 的通用 embedding 模型，表明 **parameter scale 不等于效果上限，domain alignment 更关键**。

5. **Hybrid masking 是有潜力的方向**  
   尽管本文 focus on GG-SM，但也验证了 hybrid masking（如 global query）具有一定竞争力，值得进一步探索。

### 方法的局限性

- **依赖高质量合成数据**：虽然提出了免训练的数据增强流程，但仍需一个强大的 base LLM（如 Qwen-Max）来生成初始 QA 对。
- **计算开销增加**：gradient norm 计算增加了每步训练成本（约 +15%），不适合极低延迟场景。
- **仅适用于 contrastive 框架**：目前设计针对 dual-tower alignment，是否可推广至 generation-based 用户建模有待验证。

### 未来工作方向

- 探索 **更高效的 gradient approximation** 方案以降低训练成本
- 将 GG-SM 思想扩展至 **multi-modal sequence modeling**（如视频、语音）
- 研究 **online adaptation 版本**，支持 streaming user behavior 更新
- 探索 **hybrid masking + GG-SM 的组合策略**，兼顾生成能力和表征完整性

---

> 🔗 **代码开源地址**：[https://github.com/JhCircle/Deepfind-GGSM](https://github.com/JhCircle/Deepfind-GGSM)

</details>

---

### 5. [BOute: Cost-Efficient LLM Serving with Heterogeneous LLMs and GPUs via Multi-Objective Bayesian Optimization](https://arxiv.org/abs/2602.10729)

**Authors**: Youhe Jiang, Fangcheng Fu, Eiko Yoneki  
**Category**: cs.DC  
**Published**: 2026-02-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.10729v1  

#### Abstract
The rapid growth of large language model (LLM) deployments has made cost-efficient serving systems essential. Recent efforts to enhance system cost-efficiency adopt two main perspectives: (i) An algorithmic perspective that exploits heterogeneous model capabilities to route simpler queries to lower-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**BOUTE: Cost-Efficient LLM Serving with Heterogeneous LLMs and GPUs via Multi-Objective Bayesian Optimization**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大语言模型（LLM）服务系统面临高昂的部署成本，尤其是在满足低延迟和高质量输出要求时。现有方法通常从两个独立角度优化：
- **算法层面**：通过 **heterogeneous model routing** 将简单查询路由到小模型，复杂查询交给大模型。
- **系统层面**：通过 **heterogeneous model deployment** 利用不同类型的GPU（如H100 vs RTX 5090）构建更具性价比的服务架构。

然而，这两个决策之间存在强耦合关系：
- 路由策略影响各模型的负载分布，从而影响最优部署；
- 部署配置决定每个模型的响应延迟，进而影响路由选择。

因此，**孤立优化会导致次优解**。本文旨在解决这一**算法-系统协同设计（co-design）难题**，实现端到端的成本高效LLM服务。

---

### 🚀 提出的新方法与创新思路

作者提出 **BOUTE** —— 一个基于 **Multi-Objective Bayesian Optimization (MOBO)** 的质量感知调度系统，联合优化以下两个维度：

1. **Routing Strategy**  
   - 使用阈值机制将查询动态分配给不同规模的LLM（如 Llama3.1-8B 和 Llama3.1-70B）。
   - 决策变量为路由阈值 `T`，控制负载在模型间的分布。

2. **Model Deployment Strategy**  
   - 在异构GPU集群上进行资源分配（如 H100、RTX 5090、RTX 4090 等），包括：
     - GPU分配矩阵 `A`
     - 并行策略（Data Parallelism, Tensor Parallelism, Pipeline Parallelism）
   - 支持混合部署（homogeneous + heterogeneous GPU types）

#### 核心创新点：
| 创新 | 描述 |
|------|------|
| **联合优化框架** | 首次将 routing 与 deployment 建模为统一的多目标约束优化问题，目标是 **最小化P95延迟** 同时 **最大化响应质量（accuracy/score）**。 |
| **MOBO 框架设计** | 引入 **Multi-Objective Bayesian Optimization (MOBO)** 来高效搜索高维、非凸的决策空间，避免穷举。 |
| **结构化先验注入（Structural Information）** | 在GP模型中引入多种结构信息提升收敛速度与效果：<br>• Load-fraction encoding（负载比例编码）<br>• Budget/GPU availability constraints<br>• Model-GPU偏好编码（Preference-weighted kernel）<br>• Objective normalization（log/logit变换） |
| **两阶段模拟器（Offline Preparation）** | 构建性能数据库，预估任意 `(deployment, load)` 组合下的延迟，显著降低在线优化开销。 |

---

### 🔍 相比现有方法的优势

| 对比维度 | 现有方法（如 RouteLLM） | BOUTE |
|--------|--------------------------|-------|
| 优化粒度 | 仅优化 routing 或 deployment 单独一方 | 联合优化 routing + deployment |
| 资源利用 | 均匀资源分配（uniform allocation） | 自适应资源分配，匹配模型需求 |
| GPU异构性 | 忽略不同GPU对模型的适配差异 | 显式建模 Model-GPU suitability（如 RTX 更适合小模型） |
| 搜索效率 | 规则启发式或手动调参 | MOBO + 结构信息 → 快速收敛至 Pareto 最优解 |
| 成本效益 | 局部最优 | 全局更优，平均节省成本 **38%** |

---

## 2. 核心实验方法和设置

### 📚 数据集与工作负载
- **GSM8K**：数学推理任务，用于评估 **accuracy**（聚合准确率）
- **MTBench**：多轮对话任务，用于评估 **score**（人工评分聚合得分）
- 查询 trace 采样自真实数据分布，并按复杂度加权生成子样本（subsampled trace）

### ⚙️ 实验设置
- **硬件环境**：
  - H100-80G ($2.64–3.07/h)
  - RTX PRO 6000-96G ($1.84–2.09/h)
  - RTX 5090-32G ($0.89/h)
  - RTX 4090-24G ($0.59/h)
- **预算限制**：固定每小时 $30 成本上限
- **模型组合**：Llama3.1-8B（轻量）、Llama3.1-70B（重量）
- **系统负载**：总请求速率 100 req/s

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **P95 Latency** | 95% 请求完成时间的最大值 |
| **Throughput** | 系统整体吞吐量（queries/sec） |
| **Response Quality** | GSM8K 上的 accuracy / MTBench 上的 score |
| **Cost Efficiency** | 达成相同性能目标所需的最低成本 |

### 🆚 基线方法对比
| 基线 | 描述 |
|-----|------|
| **Stand-Alone-Homo** | 使用 vLLM 部署单一大模型（70B）于同构H100集群 |
| **Stand-Alone-Hetero** | 同上，但在异构GPU上部署 |
| **RouteLLM** | 当前SOTA路由系统，采用偏好数据驱动路由，但资源均匀分配 |
| **BOUTE-Homo** | BOUTE 的变体，仅使用同构GPU（消融实验对照组） |

---

## 3. 主要实验结果和性能指标

### 📈 性能提升（vs. 基线）

| 场景 | 指标 | 提升幅度 |
|------|------|---------|
| **GSM8K 工作负载** | P95 Latency ↓ | 最多 **91%**（平均 57%） |
| | Throughput ↑ | 最多 **90%**（平均 55%） |
| **MTBench 工作负载** | Performance Improvement ↑ | 最多 **157%**（平均 115% vs Stand-Alone）<br>最多 **80%**（平均 42% vs RouteLLM） |
| **vs BOUTE-Homo** | P95 Latency ↓ | 最多 **15%**（平均 14%）<br>Throughput ↑ 最多 **16%**（平均 14%） |

> 💡 表明：**引入异构GPU本身就能带来显著增益**，而 BOUTE 进一步通过协同优化放大该优势。

---

### 💰 成本效率分析（Table 3）

| 质量+延迟要求 | BOUTE 成本 | RouteLLM 成本 | 节省比例 |
|---------------|------------|----------------|----------|
| GSM8K (87, 8s) | $32.25/h | $53.55/h | **39.8%** |
| GSM8K (91, 12s) | $32.18/h | $52.38/h | **38.6%** |
| MTBench (8.1, 10s) | $30.98/h | $55.90/h | **44.6%** |
| MTBench (8.5, 15s) | $31.23/h | $44.00/h | **29.0%** |

✅ **平均节省成本达 38%**，最高达 **61%**，同时保持相同服务质量。

---

### 🔬 消融实验结果（Ablation Studies）

| 变体 | 描述 | 影响 |
|------|------|------|
| **BOUTE (w/o structural info)** | 移除 load-fraction 编码、output transform、GPU偏好建模等结构信息 | 收敛速度下降约 **8倍**，需更多 BO iteration；最终性能劣于完整版，甚至不如 BOUTE-Homo |
| **BOUTE (w/o offline prep)** | 不使用离线性能数据库，每次评估都重新跑模拟 | 调度时间从 <1min 上升至 **>30分钟**，无法满足实时部署需求 |
| **BOUTE-Homo** | 仅使用同构GPU（如全H100） | 性能明显低于完整BOUTE，说明 **heterogeneous GPU 是关键增益来源之一** |

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **异构模型路由 + 异构GPU部署 天然互补**  
   - 小模型更适合经济型GPU（如RTX系列），大模型依赖高端GPU（如H100）。
   - 联合优化可释放双重红利：**低成本服务小模型 + 扩容大模型容量**。

2. **路由与部署必须协同优化**  
   - 孤立优化任一环节都会导致系统瓶颈（如大模型过载）。
   - BOUTE 通过 MOBO 找到 **Pareto-optimal 解集**，支持灵活权衡 latency 与 quality。

3. **结构化先验极大提升优化效率**  
   - Load-fraction 编码稳定优化过程；
   - Preference-weighted kernel 引导搜索朝高性能区域前进；
   - 离线模拟器使在线调度可在 **30秒内完成**，具备实用价值。

4. **成本节约潜力巨大**  
   - 在相同性能目标下，BOUTE **平均减少38%成本**，最高达61%，显著推动LLM服务平民化。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **冷启动开销** | 离线准备阶段需要数十分种进行 profiling 和模拟，适用于静态场景，频繁变更模型或硬件时需重做。 |
| **扩展性边界** | 当GPU类型 >6 或模型种类 >3 时，搜索空间指数增长，虽仍可扩展，但需更强算力支持。 |
| **依赖模拟精度** | 性能预测依赖模拟器准确性（误差 ~2–7%），极端情况可能偏离真实表现。 |
| **路由模型假设理想** | 使用已有 router（如 RouteLLM），未考虑对抗攻击或分布偏移对路由稳定性的影响。 |

---

### 🔮 未来工作方向

1. **动态自适应调度**  
   支持运行时负载漂移、模型更新、GPU故障等动态事件下的在线重优化。

2. **跨云/边缘协同部署**  
   将 BOUTE 拓展至 multi-cloud 或 edge-cloud 协同场景，进一步降低成本。

3. **安全与鲁棒性增强**  
   抵御 adversarial queries 对 routing 的误导，保障系统可靠性。

4. **自动化模型选型**  
   不仅优化已有模型的路由与部署，还支持自动推荐最佳模型组合（e.g., cascade design）。

5. **绿色计算集成**  
   加入能耗作为第三优化目标，打造 **cost-, latency-, energy-** 三目标联合优化框架。

---

> 📌 **总结一句话**：  
> **BOUTE 是首个将 heterogeneous model routing 与 deployment 联合建模并通过 MOBO 实现高效协同优化的系统，在保证质量的前提下，相比现有方案最多提升 157% 性能或节省 61% 成本，为大规模 LLM 服务提供了全新的 cost-efficient 范式。**

</details>

---

### 6. [PRISM: Parallel Residual Iterative Sequence Model](https://arxiv.org/abs/2602.10796)

**Authors**: Jie Jiang, Ke Cheng, Xin Xu, Mengyang Pang, Tianhao Lu, Jiaheng Li, Yue Liu, Yuan Wang, Jun Zhang, Huan Yu, Zhouchen Lin  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.10796v1  

#### Abstract
Generative sequence modeling faces a fundamental tension between the expressivity of Transformers and the efficiency of linear sequence models. Existing efficient architectures are theoretically bounded by shallow, single-step linear updates, while powerful iterative methods like Test-Time Training ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：PRISM: Parallel Residual Iterative Sequence Model**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
生成式序列建模（Generative sequence modeling）面临一个根本性的矛盾：
- **Transformers** 虽然表达能力强，但其 $O(N^2)$ 的计算复杂度在处理超长序列时效率极低。
- **线性序列模型**（如 Linear Attention、SSMs）虽然高效（$O(N)$），但受限于单步线性更新机制，存在“**线性瓶颈**”（Linearity Bottleneck），即只能进行 Rank-1 更新，难以捕捉高阶特征相关性和迭代优化能力。
- **显式优化方法**（如 Test-Time Training, TTT）虽能实现多步非线性优化，提升表达力，但由于梯度依赖于当前状态（state-dependent gradients），导致串行依赖，破坏了硬件并行性。

PRISM 正是为了**解决表达力与效率之间的根本张力**而提出的。

---

### **提出了什么新方法或新思路**

PRISM（**Parallel Residual Iterative Sequence Model**）提出了一种**摊销残差优化框架**（Amortized Residual Optimization Framework），其核心思想是：

#### **Write-Forget Decoupling（写入-遗忘解耦）**
- 将状态更新分解为两个部分：
  - **遗忘（Forget）**：采用固定的、高效的线性衰减（Rank-1），保证稳定性与并行性。
  - **写入（Write）**：通过一个**高秩（Rank-L）、非线性注入算子**来模拟多步迭代优化过程，从而突破 Rank-1 瓶颈。

#### **Input-Anchored Loop Unrolling（输入锚定循环展开）**
- 为了绕过显式求解器的串行依赖，PRISM 引入了一个两阶段代理架构：
  1. **短卷积锚定**（ShortConv）：利用局部历史能量估计初始残差（proxy for $S_{t-1}k_t$）。
  2. **学习型预测器**：直接从输入中估计多步细化更新，将非线性迭代过程“坍缩”为一个可并行的前馈算子。

该设计将迭代优化的结构先验（structural inductive bias）编码进模型中，而非实时执行。

---

### **相比现有方法的优势**

| 维度 | PRISM | TTT / TITANS | Linear Models (e.g., DeltaNet) |
|------|-------|---------------|-------------------------------|
| **表达力** | ✅ 高（Rank-L, 非线性） | ✅ 极高（显式多步优化） | ❌ 低（Rank-1, 线性） |
| **并行性** | ✅ 完全并行（$O(N)$ 训练） | ❌ 串行（$O(N)$ 推理延迟） | ✅ 完全并行 |
| **吞吐量** | ⬆️ 高达 174× TTT | ⬇️ 极低 | ⬆️ 高 |
| **理论表达性** | ✅ 证明具有 Rank Accumulation | ✅ 显式优化 | ❌ Rank-1 限制 |

> **核心优势**：在保持 $O(N)$ 并行训练效率的同时，达到了与 TTT 类方法相当甚至更优的建模精度。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在四个推荐系统基准上进行评估，作为对**高秩稀疏性**（High-Rank Sparsity）的严格测试：
- **Amazon Books**
- **Amazon Movies**
- **Amazon Elecs**
- **Yelp**

这些场景下用户兴趣演化高度多模态，远超 NLP 中常见的低秩注意力模式。

---

### **实验设置和评估指标**

#### **评估指标**
- **推荐性能**：`NDCG@K`, `Hit@K`（K=200, 500）
- **AUC**（Area Under Curve）
- **效率指标**：**Training Throughput**（千 token/秒），在 NVIDIA H20 GPU 上测量

#### **模型规模**
- 主要对比 0.13B 参数模型
- 序列长度扩展至 16K，测试长程依赖建模能力

---

### **基线方法对比**

分为三类：

| 类别 | 代表模型 |
|------|--------|
| **Upper Bound (非线性)** | SASRec, HSTU (Transformer-based) |
| **Linear Recurrences** | SLA, GLA, MoM, GSA, Mamba-2 |
| **Optimization-Based** | Gated DeltaNet, TTT-Linear, ATLAS, TITANS |

PRISM 与这些方法在相同设置下公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表：Amazon Books 上 NDCG@200 和 AUC 表现**
| Model | NDCG@200 | AUC |
|-------|----------|-----|
| SASRec (Transformer) | 0.0215 | **0.8910** |
| HSTU (Transformer) | 0.0233 | 0.8835 |
| **PRISM (Ours)** | **0.0238** | **0.8888** |
| TITANS | 0.0243 | 0.8869 |

> PRISM 在线性模型中全面领先，在多个指标上接近甚至超越 Transformer。

#### **平均排名（Mean Rank）**
- PRISM 在所有线性模型中 **Mean Rank 最低（2.62）**，显著优于其他方法（如 TITANS 为 3.62）。

---

### **与基线方法的对比结果**

1. **性能方面**：
   - PRISM 性能与 TTT、TITANS 等显式优化方法**相当甚至更优**。
   - 在 Amazon Movies 上，PRISM 的 AUC 是所有线性模型中最高的。
   - 与 Transformer 的差距极小（如 Amazon Books 上几乎持平）。

2. **效率方面**：
   - **训练吞吐量高达 TTT 的 174×**。
   - 在不同序列长度（2K–16K）和批量大小下，PRISM 吞吐稳定在 ~61K tokens/s。
   - 相比之下，TTT 仅 0.34K tokens/s，Transformer++ 在长序列下严重退化。

> **结论**：PRISM 实现了“**显式优化的质量 + 线性模型的速度**”。

---

### **消融实验结果（Ablation Study）**

在 Amazon Elecs 上验证各组件作用：

| 变体 | Hit@200 | AUC | 分析 |
|------|--------|-----|------|
| **PRISM (Full)** | **0.1409** | **0.7134** | 基线 |
| w/o Iterative Refinement (L=1) | 0.1155 | 0.6805 | ⬇️ 显著下降，证明**迭代深度至关重要** |
| w/o Non-Linearity | 0.1316 | 0.7047 | ⬇️ 说明**非线性激活必要** |
| w/o ShortConv Anchor | 0.1406 | 0.7076 | ⬇️ 锚定提供关键上下文 |
| w/o Gain Predictor | 0.1383 | 0.7098 | ⬇️ 自适应增益提升稳定性 |

> 所有组件均对最终性能有正向贡献，尤其是**迭代细化**和**非线性**。

---

## **4. 关键结论和发现**

### **主要发现**

1. **“一次性瓶颈”是线性模型的根本限制**：标准线性更新无法进行多步误差修正，限制了建模复杂语义依赖的能力。
2. **摊销优化是可行路径**：无需在推理时执行显式梯度下降，即可通过结构先验学习到优化轨迹。
3. **Rank Accumulation 可被理论证明**：PRISM 的设计实现了从 Rank-1 到 Rank-L 的结构扩展，突破了传统线性模型的假设空间。
4. **效率与质量可以兼得**：PRISM 在保持完全并行性的同时，达到了与串行优化方法相当的建模保真度。

---

### **方法的局限性**

1. **局部近似 vs. 全局反应性**：
   - PRISM 依赖 ShortConv 代理局部上下文，**无法响应遥远历史中的记忆冲突**（如“这个事实是否已存储？”）。
   - 对“长尾漂移”（long-tail drift）缺乏动态纠正能力。

2. **容量未扩展**：
   - PRISM 提升的是**写入质量**（Fidelity），而非**内存容量**（Capacity）。
   - 固定维度状态仍会遭遇覆盖问题（Overwriting）。

3. **理论假设依赖**：
   - 写入-遗忘解耦的有效性基于谱扰动分析，实际效果可能受任务特性影响。

---

### **未来工作方向**

1. **显式扩展容量**：
   - 结合 **Mixture-of-Memories (MoM)** 或 **Gated Slot Attention (GSA)**，增加状态宽度。
   - PRISM 可作为高质量写入算子，与容量扩展方法协同。

2. **混合架构（Hybrid Architectures）**：
   - 如 Jamba、Kimi Linear 所示，周期性插入 Transformer 层可纠正局部线性近似的长期漂移。
   - PRISM 可作为主干高效模块，辅以少量全局注意力层。

3. **更鲁棒的锚定机制**：
   - 探索更长感受野的轻量级锚定网络（如 dilated convolutions）以增强上下文感知。

4. **理论深化**：
   - 进一步分析摊销优化与真实优化路径的逼近程度。
   - 探索 PRISM 在不同 ODE 动力学下的泛化能力。

---

> **总结**：PRISM 成功地将迭代优化的思想“蒸馏”进一个可并行的序列模型中，为构建高效且富有表达力的下一代基础模型提供了坚实的新范式。

</details>

---

### 7. [QTALE: Quantization-Robust Token-Adaptive Layer Execution for LLMs](https://arxiv.org/abs/2602.10431)

**Authors**: Kanghyun Noh, Jinheon Choi, Yulwha Kim  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.10431v1  

#### Abstract
Large language models (LLMs) demand substantial computational and memory resources, posing challenges for efficient deployment. Two complementary approaches have emerged to address these issues: token-adaptive layer execution, which reduces floating-point operations (FLOPs) by selectively bypassing ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《QTALE: Quantization-Robust Token-Adaptive Layer Execution for LLMs》总结

---

## 1. 论文的主要贡献和创新点

### ✅ **解决了什么问题**

大型语言模型（LLMs）在部署时面临两大挑战：
- **高计算成本**：大量 FLOPs 导致推理延迟高。
- **高内存占用**：全精度权重消耗大量显存。

已有两种主流优化技术：
- **Token-adaptive layer execution**（如 D-LLM）：通过动态跳过部分 Transformer 层减少 FLOPs。
- **Quantization**：降低权重精度以减小模型体积和内存带宽需求。

然而，**直接将两者结合会导致显著的准确率下降**。其根本原因是 token-adaptive 模型本身已减少了冗余（redundancy），而量化进一步引入扰动，使得模型对错误更敏感。

> 🔍 **核心问题**：如何在不牺牲准确率的前提下，无缝集成 token-adaptive 执行与量化？

---

### 🚀 **提出了什么新方法或新思路**

作者提出 **QTALE**（Quantization-Robust Token-Adaptive Layer Execution），一个支持高效且鲁棒量化的 token-adaptive 推理框架，包含两个关键组件：

#### （1）**Quantization-Robust Training Strategy（量化鲁棒训练策略）**
- 引入 **熵最大化目标函数 $ \mathcal{L}_{\text{entropy}} $**，鼓励路由器（router）输出具有更高的不确定性。
- 目标是让“执行”与“跳过”的 logits 差距变小，从而增强路径多样性（path diversity）。
- 在训练中引入 Gumbel-Softmax 技巧，促进不同执行路径被探索，提升训练路径冗余（training-path redundancy）。

> 💡 类比 Dropout 和 Stochastic Depth —— 随机性提升了泛化能力和抗干扰能力。

#### （2）**Post-Training Execution Ratio Adjustment（推理阶段执行比例调节机制）**
- 在推理时，可通过调整阈值 $\theta$ 动态控制 layer bypass 的比例。
- 当检测到量化带来较大误差时，可临时提高执行比例（即少跳过层），重新引入参数冗余（parameter redundancy），提升鲁棒性。
- 使用简单的两阶段网格搜索（coarse + fine grid search）在校准集上自动确定最优 $\theta$。

---

### ⭐ **相比现有方法的优势**

| 维度 | D-LLM（Baseline） | QTALE（本文方法） |
|------|-------------------|--------------------|
| 路径多样性 | 低（收敛为固定模式，类似剪枝） | 高（训练中主动探索多种路径） |
| 对量化的鲁棒性 | 差（微小扰动导致路径漂移） | 强（平滑路由决策，抵抗量化噪声） |
| 冗余管理 | 固定、静态 | 可调、动态 |
| 与量化兼容性 | 差，精度损失大 | 好，几乎无损 |

> ✅ QTALE 实现了 **FLOPs 与 memory 的双重压缩**，同时保持接近 full-model 的 accuracy。

---

## 2. 核心实验方法和设置

### 📚 **使用的数据集**

#### 主要评估基准：
- **CommonsenseQA (CSQA)** 套件：
  - PIQA, SIQA, ARCe, ARCc, BoolQ, Winogrande, OBQA
- **MMLU**：多任务知识理解
- **Stanford-Alpaca** 和 **SAMSum**：用于 PPL（Perplexity）评估

#### 微调与校准：
- 使用下游任务数据进行 fine-tuning
- 小规模 calibration set 用于搜索最优执行阈值 $\theta$

---

### ⚙️ **实验设置**

| 项目 | 设置 |
|------|------|
| **模型** | LLaMA2-7B, LLaMA3.1-8B, LLaMA3.2-3B |
| **量化方式** | AWQ（4-bit 和 3-bit 整数量化），group size=128 |
| **Token-adaptive 方法** | 基于 D-LLM 架构，每层加 router MLP |
| **训练配置** | 学习率、epoch 数等复现原 D-LLM 设置 |
| **评估指标** | Zero-shot Accuracy (%) / PPL ↓, FLOPs ↓, Model Size (Memory) ↓, Latency (Speedup) ↑ |

---

### 🔁 **基线方法对比**

| 方法 | 描述 |
|------|------|
| **Full-layer Execution (FP16/INT4)** | 不跳层的标准模型，作为 upper bound |
| **AWQ** | 单纯量化，无 token-adaptive |
| **D-LLM** | 原始 token-adaptive 方法 |
| **D-LLM + AWQ** | D-LLM 与量化的简单组合（naive integration） |
| **QTALE (Ours)** | 本文方法：带熵正则训练 + 推理时执行比调节 |

---

## 3. 主要实验结果和性能指标

### 📊 **关键性能数据**

#### ✅ 在 **LLaMA2-7B + 3-bit 量化 + CSQA** 上的结果（Table 1）：

| 方法 | Accuracy (%) | 相较 Full 模型差距 |
|------|-------------|------------------|
| Full-layer (3-bit) | 72.22 | — |
| D-LLM (3-bit) | 70.57 | ▼ 1.65% |
| **QTALE (3-bit)** | **72.79** | ▲ +0.57% |

> ✅ QTALE 不仅弥补了精度损失，甚至略微反超！

#### ✅ 在 **LLaMA3.2-3B + 4-bit 量化 + CSQA** 上的结果：

| 方法 | Accuracy (%) |
|------|--------------|
| Full-layer (4-bit) | 77.55 |
| D-LLM (4-bit) | 73.96 (**▼ 3.59%**) |
| **QTALE (4-bit)** | **78.41** (**▲ +0.86%**) |

> ❗ D-LLM 出现严重退化，而 QTALE 表现稳健。

---

### 🔍 **消融实验结果（Ablation Study, Table 4）**

验证两个核心组件的作用：

| 方法 | CSQA Accuracy (%) | 是否恢复精度？ |
|------|------------------|----------------|
| D-LLM（无任何改进） | 73.96 | 否 |
| + Only $ \mathcal{L}_{\text{entropy}} $ | 75.82 | 部分恢复 |
| + Only $\theta$ 调整 | 75.10 | 部分恢复 |
| **+ Both (QTALE)** | **78.41** | ✅ 完全恢复并超越 |

> ✅ 两项设计缺一不可，协同作用显著。

---

### 📈 **效率评估（Figure 6）**

| 指标 | 结果 |
|------|------|
| **FLOPs Reduction** | 平均降低约 **46%**（归一化 FLOPs ≈ 0.54） |
| **Memory Footprint** | 从 13.5GB（FP16）降至 **<4.5GB**（4-bit） |
| **综合优势** | 同时实现 **FLOPs↓ + Memory↓**，适合边缘设备部署 |

> 💬 虽然 QTALE 因 slightly higher execution ratio 导致速度略慢于 D-LLM，但换来的是**精度稳定性**。

---

## 4. 关键结论和发现

### ✅ **主要发现**

1. **Token-adaptive 与 Quantization 的冲突根源在于“冗余缺失”**：
   - D-LLM 训练路径单一 + 参数利用率低 → 对量化扰动极度敏感。
   
2. **QTALE 成功重建了两种冗余**：
   - **Training-path redundancy**：通过熵正则确保多样路径被探索。
   - **Parameter redundancy**：通过推理时动态上调 execution ratio 来补偿损失。

3. **QTALE 实现近乎无损的联合压缩**：
   - 在 CommonsenseQA 上，与纯量化模型的差距 **< 0.5%**。
   - 显著优于 naive combination 方案（最大差距达 3.6%）。

4. **具备良好通用性和扩展性**：
   - 兼容其他 PTQ 方法（如 MagR+GPTQ）→ 表现稳定。
   - 可与其他压缩技术（如 Wanda 剪枝）结合，在 50% sparsity 下仍保持性能。

---

### ⚠️ **局限性**

1. **依赖 fine-tuning**：需要额外的微调过程来实施熵正则训练，增加训练开销。
2. **router 模块带来轻微 overhead**：虽然轻量，但仍需额外参数和计算。
3. **目前聚焦于 post-training quantization (PTQ)**：未涉及量化感知训练（QAT）场景下的表现。

---

### 🔮 **未来工作方向**

1. 将 QTALE 扩展至 **QAT（Quantization-Aware Training）** 框架中，进一步提升极限压缩下的稳定性。
2. 探索 **更高效的 router 设计**（如共享参数、稀疏激活）以降低开销。
3. 应用于 **vision-language models 或 MoE 架构** 中，研究跨模态或专家选择中的鲁棒自适应执行。
4. 开发 **自动化 threshold selection pipeline**，适配不同硬件平台与负载条件。

---

## ✅ 总结一句话

> **QTALE 通过“训练时增强路径多样性 + 推理时灵活调节执行强度”，首次实现了 token-adaptive layer execution 与 low-bit quantization 的高效、无损融合，为 LLM 的端侧高效部署提供了统一解决方案。**

</details>

---

### 8. [Spend Search Where It Pays: Value-Guided Structured Sampling and Optimization for Generative Recommendation](https://arxiv.org/abs/2602.10699)

**Authors**: Jie Jiang, Yangru Huang, Zeyu Wang, Changping Wang, Yuling Xiong, Jun Zhang, Huan Yu  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10699v1  

#### Abstract
Generative recommendation via autoregressive models has unified retrieval and ranking into a single conditional generation framework. However, fine-tuning these models with Reinforcement Learning (RL) often suffers from a fundamental probability-reward mismatch. Conventional likelihood-dominated dec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Spend Search Where It Pays: Value-Guided Structured Sampling and Optimization for Generative Recommendation

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

本文针对**生成式推荐（Generative Recommendation, GR）** 中存在的 **概率-奖励不匹配（probability-reward misalignment）** 问题展开研究。该问题在基于自回归模型（如Transformer）进行端到端推荐时尤为严重，具体表现为两个结构性缺陷：

- **探索不足（Insufficient Exploration）**：传统基于概率的解码策略（如 beam search）倾向于保留高概率前缀，导致低概率但高奖励的长尾物品（long-tail items）在早期就被剪枝，无法被采样。
- **优势压缩（Advantage Compression）**：由于候选集中的项目往往共享高概率前缀，形成“兄弟节点”（siblings），其奖励高度相关，导致组内方差小，GRPO等RL算法的学习信号弱化。

这两个问题共同削弱了强化学习（RL）的有效性，限制了模型对高价值项目的发现能力。

---

### 提出的新方法或新思路

作者提出 **V-STAR**（**Value-guided Sampling and Tree-structured Advantage Reinforcement**），一个自演化的采样-学习框架，包含两大核心组件：

#### （1）Value-Guided Efficient Decoding (VED)

- **目标**：在固定计算预算下，提升候选集的质量与多样性。
- **机制**：
  - 利用一个轻量级的 **value model** 预测每个前缀（prefix）的期望回报。
  - 结合前缀的 **value** 和 **policy entropy**（不确定性）构建优先级评分 $ G(s) = V(s) + \lambda H(s) $。
  - 在初始化的浅层树（由beam search生成）基础上，选择高价值且高不确定性的“决定性节点”（decisive nodes）进行局部扩展，避免穷举搜索。
- **创新点**：将解码视为**有预算的资源分配问题**，实现“花在刀刃上”的智能探索。

#### （2）Sibling-GRPO

- **目标**：缓解兄弟节点间的优势压缩问题。
- **机制**：
  - 利用生成过程中自然形成的**前缀树结构**，将共享父前缀的候选划分为“兄弟组”（sibling groups）。
  - 在每组内部计算**相对优势（relative advantage）**，即 $ A_{\text{node}}(x;h,v) = \frac{R(x;h,v) - \mu_h}{\sigma_h + \epsilon} $。
  - 在训练中对这些兄弟节点应用GRPO更新，使梯度集中在**关键分支决策**上。
- **创新点**：首次将**树形结构信用分配**引入生成式推荐的RL优化，增强了学习信号的判别力。

---

### 相比现有方法的优势

| 维度 | 传统方法（如 Beam Search + GRPO） | V-STAR |
|------|-------------------------------|--------|
| 探索效率 | 低，易剪枝高价值低概率路径 | 高，通过value引导聚焦高潜力分支 |
| 学习信号 | 弱，全局归一化导致优势压缩 | 强，兄弟组内归一化增强对比 |
| 计算开销 | 固定，难以动态调整 | 可控，仅在关键节点扩展 |
| 候选多样性 | 低，兄弟项冗余严重 | 高，覆盖更广的语义分支 |

---

## 2. 核心实验方法和设置

### 使用的数据集

- **离线数据集**：
  - **Amazon Review Dataset** 的两个子集：
    - **Industrial Products**
    - **Office Products**
- **在线数据集**：
  - 微信视频号（WeChat Channels）的真实线上流量，用于A/B测试。

---

### 实验设置和评估指标

#### 模型架构
- 主干模型：**Qwen2.5-1.5B**
- 训练流程：两阶段
  1. **Supervised Fine-Tuning (SFT)**
  2. **RL Fine-tuning with GRPO**

#### 解码设置
- VED 构造 16 个候选，SID长度 $ L=3 $
- Beam width 初始化为 8
- Discount factor $ \gamma = 0.99 $，entropy weight $ \lambda = 0.1 $

#### 评估指标

| 类型 | 指标 |
|------|------|
| **离线** | HR@K, NDCG@K （K=3,5,10） |
| **在线** | GMV（Gross Merchandise Volume）, GMV-Normal（点击/转化优化广告的GMV） |

---

### 基线方法对比

| 类型 | 基线方法 |
|------|----------|
| 传统序列推荐 | GRU4Rec, Caser, SASRec |
| 生成式非SID | HSTU, BIGRec |
| SID生成式（无RL） | TIGER, LCRec, D3 |
| SID生成式 + RL/DPO | S-DPO, MiniOneRec |
| 解码策略对比 | Beam Search, Top-K Sampling |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | Industrial HR@3 | Industrial NDCG@10 | Office HR@3 | Office NDCG@10 |
|------|------------------|---------------------|-------------|---------------|
| MiniOneRec (最强基线) | 0.1143 | 0.1167 | 0.1217 | 0.1242 |
| **Ours (V-STAR)** | **0.1189** (+4.0%) | **0.1217** (+4.3%) | **0.1344** (+10.4%) | **0.1340** (+7.9%) |

> ✅ **所有指标均显著领先于现有SOTA方法**

---

### 在线A/B测试结果（5天，5%流量）

| 指标 | 提升幅度 |
|------|---------|
| **GMV** | **+1.23%** |
| **GMV-Normal** | **+1.87%** |

> ✅ 显著提升商业收益，验证了方法的实际价值。

---

### 消融实验结果

#### （1）解码策略对比（Table 2）

| 解码策略 | Industrial NDCG@10 | Office HR@10 |
|----------|--------------------|--------------|
| Beam Search | 0.1194 | 0.1684 |
| Top-K | 0.1090 | 0.1644 |
| **Ours (VED)** | **0.1217** | **0.1746** |

> ✅ VED在质量和多样性上均优于传统策略。

#### （2）VED扩展规则消融（Table 3）

| 扩展规则 | Industrial HR@10 |
|----------|------------------|
| Value-only $ V(s) $ | 0.1627 |
| Entropy-only $ H(s) $ | 0.1604 |
| **Joint Score $ G(s) $** | **0.1641** |

> ✅ **价值与不确定性的联合评分效果最佳**，证明两者协同作用。

#### （3）训练目标消融（Table 4）

| 训练目标 | Industrial NDCG@10 |
|----------|--------------------|
| GRPO (Global) | 0.1189 |
| Sibling-GRPO | 0.1204 |
| **GRPO + Sibling-GRPO (Joint)** | **0.1217** |

> ✅ 联合优化效果最好，说明全局对齐与结构感知更新相辅相成。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **概率驱动的解码存在根本性缺陷**：beam search等策略会导致高奖励长尾项被提前剪枝，并引发优势压缩，削弱RL学习效率。
2. ✅ **价值引导的稀疏搜索更高效**：VED能以极小额外计算成本，在关键节点进行探索，显著提升高价值候选的可达性。
3. ✅ **兄弟组内归一化增强学习信号**：Sibling-GRPO利用生成树结构，恢复了被压缩的梯度信号，使模型更关注决定性分支。
4. ✅ **V-STAR形成自演化闭环**：更好的采样 → 更强的学习信号 → 更优的策略 → 更好的价值估计 → 更精准的采样，持续迭代提升。

---

### 方法的局限性

- **依赖高质量的value model**：若value估计不准，可能导致错误的扩展决策。
- **仍受限于预定义的SID结构**：方法建立在Semantic ID的基础上，对其他生成范式适配性待验证。
- **在线推理未完全释放潜力**：测试时默认使用beam search以保证延迟，VED的增益主要体现在训练阶段。

---

### 未来工作方向

1. **将VED应用于推理阶段**：在允许更高延迟的场景中部署VED，进一步提升线上效果。
2. **动态调整计算预算**：根据query难度自适应分配搜索资源。
3. **结合更多外部信号**：如用户实时反馈、上下文信息，进一步优化value model。
4. **扩展至多模态生成推荐**：将框架推广到图文、视频等复杂item的生成任务中。

---

> **总结**：V-STAR通过**价值引导的结构化采样**与**兄弟感知的优势优化**，有效解决了生成式推荐中的核心挑战，在严格延迟约束下实现了性能与多样性的双重突破，为工业级生成推荐系统提供了新的优化范式。

</details>

---

### 9. [How Much Reasoning Do Retrieval-Augmented Models Add beyond LLMs? A Benchmarking Framework for Multi-Hop Inference over Hybrid Knowledge](https://arxiv.org/abs/2602.10210)

**Authors**: Junhong Lin, Bing Zhang, Song Wang, Ziyan Liu, Dan Gutfreund, Julian Shun, Yada Zhu  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10210v1  

#### Abstract
Large language models (LLMs) continue to struggle with knowledge-intensive questions that require up-to-date information and multi-hop reasoning. Augmenting LLMs with hybrid external knowledge, such as unstructured text and structured knowledge graphs, offers a promising alternative to costly contin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：How Much Reasoning Do Retrieval-Augmented Models Add beyond LLMs?

## 1. 论文的主要贡献和创新点

### 解决的问题
当前用于评估 **Retrieval-Augmented Generation (RAG)** 和 **KG-RAG** 模型的基准（如 HotpotQA、WebQuestions）存在严重的 **pretraining contamination** 问题——即这些基准中的知识早已被包含在大型语言模型（LLMs）的预训练语料中。这导致模型可能通过参数化记忆（parametric recall）而非真正的检索与推理来回答问题，从而高估了其能力。

此外，现有基准难以有效区分以下几种能力：
- 模型是否真正进行了多跳推理（multi-hop reasoning）
- 是否依赖外部检索
- 是否有效融合了结构化（KG）与非结构化（text）知识

### 提出的新方法：HYBRIDRAG-BENCH
作者提出了 **HYBRIDRAG-BENCH**，一个全自动的基准构建框架，旨在解决上述问题。

#### 核心创新点：
- **时效性与抗污染性（Contamination-Aware）**  
  所有知识均来自近期（如 2023–2024 年）的 arXiv 科学文献，确保其晚于主流 LLMs 的预训练截止时间（例如 Qwen2.5 截止于 2023 年 10 月），从而避免参数化记忆。
  
- **混合知识表示（Hybrid Knowledge）**  
  同时构建 **非结构化文本片段**（text chunks）和 **结构化知识图谱**（KG），模拟真实世界中信息的双重形态。

- **显式推理路径生成（Explicit Reasoning Paths）**  
  问题-答案对基于明确的 KG 路径生成，并结合支持文本，确保每个问题都需要多步推理，且答案可追溯。

- **自动化与可扩展性**  
  整个流程（从数据收集、KG 构建到 QA 生成）完全自动化，支持按领域、时间范围灵活定制，便于持续更新。

### 相比现有方法的优势
| 维度 | 传统基准（如 HotpotQA） | HYBRIDRAG-BENCH |
|------|------------------------|------------------|
| 数据新鲜度 | 静态、过时 | 动态、近期 arXiv 文献 |
| 抗污染能力 | 弱（易与预训练数据重叠） | 强（时间上隔离） |
| 推理可控性 | 有限 | 显式控制推理路径长度与类型 |
| 知识形式 | 单一（通常为文本或 KG） | 混合（text + KG） |
| 可扩展性 | 依赖人工标注 | 全自动流水线 |

---

## 2. 核心实验方法和设置

### 数据集构建
- **来源**：从 arXiv 收集三个领域的近期论文：
  - **AI**（人工智能，强化学习方向）
  - **CY**（Governance and Policy，治理与政策）
  - **BIO**（Bioinformatics，生物信息学）
- **时间窗口**：选择晚于主流 LLM 预训练截止时间的文献（如 2023 年后），确保知识不可被“记住”。
- **最终数据**：每个领域约 1000 个 QA 对，共 2869 条，覆盖多种推理类型（见下表）。

#### 表：问题类型分布（Table 2）
| 问题类型 | AI (%) | CY (%) | BIO (%) |
|--------|-------|-------|--------|
| Single-hop | 29% | 25% | 25% |
| Single-hop w. Condition | 16% | 13% | 19% |
| Multi-hop (Regular) | 19% | 15% | 19% |
| Multi-hop (Difficult) | 17% | 17% | 13% |
| Counterfactual | 13% | 18% | 6% |
| Open-ended | 5% | 12% | 18% |

### 实验设置
- **模型**：使用多个前沿 LLMs 进行测试：
  - `DeepSeek-V3.2` (685B)
  - `Qwen2.5` (72B)
  - `LLaMA3.3` (70B)
  - `LLaMA3.1` (8B)

- **评估指标**：准确率（Accuracy），报告 5 次运行的平均值与标准差。

- **基线方法对比**：
  | 类别 | 方法 |
  |------|------|
  | **LLM-only** | IO（直接提问）、Chain-of-Thought (CoT)、Self-Consistency (SC) |
  | **Text-based RAG** | Dense retrieval + generation |
  | **Naive KG Augmentation** | 1-hop KG（注入邻近三元组）、RAG + 1-hop KG |
  | **Advanced KG-RAG** | CoK, RoG, ToG, ToG2.0, PoG, R2-KG, HippoRAG2.0, EvoReasoner |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 3）
在所有三个领域中，**LLM-only 方法表现极差**，表明问题确实无法通过记忆解决：

| 方法 | AI (avg) | CY (avg) | BIO (avg) |
|------|---------|---------|----------|
| **IO Prompting** | ~30% | ~35% | ~30% |
| **RAG** | ~45% | ~42% | ~48% |
| **Best KG-RAG (EvoReasoner)** | **75.7%** | **69.2%** | **75.9%** |

> ✅ **最大提升**：相比 LLM-only，引入外部检索使准确率提升 **7–29 个百分点**。

### 与基线方法的对比结果
- **RAG vs LLM-only**：RAG 显著优于纯提示方法，证明外部检索是必要的。
- **KG-RAG vs RAG**：混合方法（如 **ToG2.0**, **EvoReasoner**）显著优于纯文本 RAG，说明结构化知识提供了额外价值。
- **1-hop KG 的局限性**：简单注入 KG 邻居反而可能降低性能（噪声干扰），强调需要智能检索机制。

### 按问题类型的细粒度分析（Table 4）

| 问题类型 | 最佳方法 | 关键发现 |
|--------|--------|--------|
| **Multi-hop** | ToG2.0, EvoReasoner | 显式图遍历（graph traversal）对多跳推理至关重要 |
| **Counterfactual** | CoT, ToG2.0, EvoReasoner | 成功依赖于强推理能力，而非检索；1-hop KG 几乎失效 |
| **Open-ended** | RAG, EvoReasoner | 更依赖文本描述整合，结构化路径作用较小 |
| **Conditional Single-hop** | ToG2.0, RAG+1-hop KG | 需要同时理解文本条件与结构关系 |

> 🔍 **重要发现**：即使在相同模型规模下，不同 KG-RAG 方法之间性能差距巨大（如 EvoReasoner > CoK 达 30%+），说明该基准能有效区分推理策略优劣。

### 消融实验与有效性验证
- **KG 构建质量**（Table 5）：使用 **EvoKG** 提取的 KG 在 MINE 基准上达到 **71.36%** 的事实捕获率，优于 OpenIE、GraphRAG 等方法。
- **计算成本分析**：KG 构建的 token 消耗与文档长度呈近似线性增长，具备良好可扩展性。

---

## 4. 关键结论和发现

### 主要结论
1. ✅ **HYBRIDRAG-BENCH 成功实现了抗污染评估**：  
   LLM-only 方法表现不佳，而引入外部检索带来显著提升，说明性能增益来自真实检索与推理，而非记忆。

2. ✅ **结构化知识（KG）提供互补价值**：  
   结合 KG 与文本的混合方法（KG-RAG）系统性地优于仅用文本或仅用 KG 的方法，尤其在多跳、反事实等复杂推理任务上。

3. ✅ **能有效区分不同推理机制**：  
   不同 KG-RAG 方法在不同类型问题上的表现差异显著，揭示了各自优势（如 ToG2.0 擅长图推理，RAG 擅长开放生成）。

4. ✅ **挑战当前 SOTA 模型**：  
   即使是最大规模的 LLM（如 DeepSeek-V3.2），也无法在不借助检索的情况下取得高分，说明该基准具有足够难度。

### 方法的局限性
- **依赖 LLM 进行 KG 构建与 QA 生成**：虽然自动化，但仍可能引入生成偏差或错误传播。
- **领域限制**：目前仅覆盖科研类领域（arXiv），对常识、日常知识等场景适用性待验证。
- **KG 覆盖度**：尽管使用 EvoKG 提取，仍可能存在遗漏或误连关系。

### 未来工作方向
- 扩展至更多领域（如新闻、社交媒体）和语言。
- 引入动态更新机制，实现持续评估（continual evaluation）。
- 探索更复杂的推理模式（如因果、数值推理）。
- 将框架应用于模型训练与优化指导，而不仅是评估。

---

## 总结
**HYBRIDRAG-BENCH** 是一个开创性的、面向未来的评估框架，它通过**基于最新科学文献的混合知识构造**和**显式推理路径引导的 QA 生成**，解决了传统基准因 **pretraining contamination** 导致的评估失真问题。其实验结果有力证明：
> 🎯 **真正的多跳推理能力必须依赖有效的外部知识检索与结构化推理机制，而非单纯的模型规模扩大或参数记忆。**

该工作为未来 RAG 与 KG-RAG 系统的研究提供了坚实、可靠且可扩展的测试平台。

</details>

---

### 10. [Stop Training for the Worst: Progressive Unmasking Accelerates Masked Diffusion Training](https://arxiv.org/abs/2602.10314)

**Authors**: Jaeyeon Kim, Jonathan Geuter, David Alvarez-Melis, Sham Kakade, Sitan Chen  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10314v1  

#### Abstract
Masked Diffusion Models (MDMs) have emerged as a promising approach for generative modeling in discrete spaces. By generating sequences in any order and allowing for parallel decoding, they enable fast inference and strong performance on non-causal tasks. However, this flexibility comes with a train...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Stop Training for the Worst: Progressive Unmasking Accelerates Masked Diffusion Training

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
Masked Diffusion Models (MDMs) 虽然在推理阶段支持任意顺序生成（any-order generation）并能实现并行解码，但在训练时面临严重的**计算效率瓶颈**。其根本原因在于：

- **训练-测试不匹配（train-test mismatch）**：  
  MDM 在训练时采用随机掩码（random masking），需覆盖指数级数量的掩码模式；而推理时使用的 unmasking policy（如基于置信度的 Top-K）只集中在一小部分高度结构化的掩码路径上。
- 这导致大量计算资源被浪费在对最终推理无用的掩码模式上，形成“为最坏情况训练”（Train for the Worst）的问题。

### 提出了什么新方法或新思路
本文提出 **Progressive UnMAsking (PUMA)** ——一种通过重构前向掩码过程来对齐训练与推理分布的新范式。

#### 核心思想：
- 引入 **teacher-forced chain**：从真实样本 $x_0 \sim p_{\text{data}}$ 出发，按照当前模型定义的 unmasking policy $\mathcal{g}_\theta$ 逐步揭示 token（但使用真实标签而非采样），从而生成一系列中间掩码状态作为训练样本。
- 这使得训练中出现的掩码模式**动态地逼近推理时实际访问的路径**。

#### 创新之处：
- **首次直接修改 forward masking process** 来提升训练效率，而非依赖架构改进或损失重加权等间接手段。
- 理论保证两个关键性质：
  1. **Marginal Agreement**：PUMA 生成的掩码序列边缘分布与理想化 MDM 推理一致。
  2. **Minimizer Preservation**：尽管改变了数据分布，但交叉熵损失的最优解仍保持为真实的 unmasking posterior。

### 相比现有方法的优势
| 维度 | PUMA | 其他常见方法（如 ARM init, Block Diffusion） |
|------|------|---------------------------------------------|
| **作用层级** | Forward process 层级 | Architecture / Recipe 层级 |
| **兼容性** | 完全正交，可叠加使用 | 多数不可组合或存在冲突 |
| **额外开销** | 无 forward-pass 开销 | ARM init 需预训练；Block Diffusion 放弃部分灵活性 |
| **加速机制** | 减少无效掩码模式的学习 | 利用先验知识初始化或限制解码空间 |

---

## 2. 核心实验方法和设置

### 使用的数据集
1. **Sudoku Puzzle Dataset**  
   - 来源：Radcliffe (2020)，经 Shah et al. (2024) 实现
   - 规模：180万训练样本，10万测试样本
   - 特点：结构强、逻辑依赖明确，适合验证 unmasking policy 对齐效果

2. **TinyGSM**  
   - 来源：Liu et al. (2023)
   - 规模：1180万样本
   - 内容：将 GSM8K 数学题转化为简短 Python 程序
   - 目标任务：zero-shot 下游 GSM8K 测试集准确率

### 实验设置和评估指标
| 设置项 | 描述 |
|-------|------|
| **模型规模** | 125M 参数 MDM（主实验）；6.8M Transformer（Sudoku） |
| **架构** | Qwen2-style attention，双向注意力 |
| **训练步数** | 最多 900k iterations |
| **评估频率** | 每 5k 步在 GSM8K test 上评估一次 |
| **评估方式** | 使用 EMA checkpoints 提升稳定性 |
| **推理策略** | Top-K, Top-K margin, entropy-based 等多种 unmasking policy |
| **主要指标** | **GSM8K Accuracy vs. Training Iterations**（衡量收敛速度） |

### 基线方法对比
- **Baseline**: 标准 MDM（random masking）
- **ARM init**: 从预训练 AR 模型初始化 MDM
- **Block diffusion**: 结合块内自回归与 MDM 推理
- **PUMA variants**: 不同 K-scheduling 和 confidence threshold 设置

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 方法 | 加速倍数（vs. Baseline） | 达到相同精度所需迭代次数减少 |
|------|--------------------------|-------------------------------|
| **PUMA (125M, TinyGSM)** | **~2.5×** | 从 ~750k → ~300k iterations |
| **PUMA (Sudoku)** | **1.4×** | 明显更快达到高准确率 |
| **PUMA + ARM init** | **~2.3× relative to ARM-only** | 复合加速显著 |
| **PUMA + Block diffusion** | **仍有额外加速** | 表明互补性 |

> 📈 图1显示，在相同迭代次数下，PUMA 曲线远超 baseline，且斜率更陡。

### 与基线方法的对比结果
- **绝对性能优势**：PUMA 在所有评估点均优于标准 MDM。
- **鲁棒性强**：即使训练时使用某一 policy（如 Top-K），在其他 inference policy（如 entropy-based）下依然表现优异（见图4）。
- **兼容性验证**：
  - 与 **autoregressive initialization** 结合后进一步提速；
  - 在 **block diffusion** 框架下仍有效（图6右下角），说明其增益独立于结构约束。

### 消融实验结果
#### （1）K-scheduling 的影响
- 固定 $K=42$（过早使用精细 policy）会导致早期训练不稳定、效率下降。
- 动态 K-scheduling（从12增至42）是关键设计，使模型初期保持多样性，后期聚焦高质量路径。

#### （2）confidence thresholding 的影响
- 阈值过低（如 0.4 或 0.65）会提前暴露低置信位置，破坏 teacher-forced chain 的语义连贯性，性能下降。
- 阈值 0.9 效果最佳，允许快速跳过高置信区域而不牺牲学习信号。

#### （3）policy 稳定性分析（Sudoku）
- 图7显示：PUMA 在训练早期（约1万步内）就能找到接近最终模型的 unmasking 路径。
- 说明 **position ranking 比 posterior calibration 更早稳定**，支持了“用当前模型指导训练”的合理性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **训练-推理对齐可大幅提升效率**：PUMA 通过 align training-time masking patterns with inference-time paths，实现了高达 **2.5× 的训练加速**。
2. ✅ **无需额外计算开销**：利用已有 logits 进行 scoring，无额外 forward-pass 成本。
3. ✅ **与主流技术完全兼容**：PUMA 可叠加于 ARM init、block diffusion 等方法之上，带来**复合加速效果**。
4. ✅ **理论保障可靠**：证明了 marginal agreement 与 minimizer preservation，确保优化目标不变。

### 方法的局限性
- 当前实验主要基于 **合成/结构化数据集**（TinyGSM, Sudoku），在真实大规模文本语料上的泛化能力有待验证。
- 对 **unmasking policy 的质量敏感**：若当前模型 policy 极差（如训练初期），可能引入偏差。
- **未探索极端长程依赖任务**：在具有复杂上下文依赖的真实语言建模中，alignment 效益可能受限。

### 未来工作方向
- 将 PUMA 扩展至百亿参数级别的真实预训练场景（如 LLM on The Pile）。
- 探索 **learned unmasking policy** 替代 heuristic rules，实现端到端联合优化。
- 研究如何将 PUMA 思想迁移到 **continuous diffusion models** 中（例如通过 latent space masking）。
- 分析不同任务类型下 optimal unmasking trajectory 的结构特性，指导 forward process 设计。

---

> 🔗 **开源信息**：作者已公开代码库（文中提供链接），便于复现与社区扩展。

</details>

---

### 11. [Kalman Linear Attention: Parallel Bayesian Filtering For Efficient Language Modelling and State Tracking](https://arxiv.org/abs/2602.10743)

**Authors**: Vaisakh Shaj, Cameron Barker, Aidan Scannell, Andras Szecsenyi, Elliot J. Crowley, Amos Storkey  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.10743v1  

#### Abstract
State-space language models such as Mamba and gated linear attention (GLA) offer efficient alternatives to transformers due to their linear complexity and parallel training, but often lack the expressivity and robust state-tracking needed for complex reasoning. We address these limitations by refram...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Kalman Linear Attention: Parallel Bayesian Filtering For Efficient Language Modelling and State Tracking

---

## 1. 主要贡献和创新点

### ✅ 解决的问题
当前主流的 **State-Space Models (SSMs)** 如 Mamba 和 **Gated Linear Attention (GLA)** 虽然实现了线性复杂度和并行训练，但在以下方面存在局限：
- 隐藏状态更新是**线性或仿射**的，表达能力受限；
- 缺乏对**状态不确定性**的显式建模；
- 在需要复杂推理、长程依赖和鲁棒状态追踪的任务上表现不足。

本文提出从**概率视角重构序列建模**，将语言建模视为在噪声观测下的**贝叶斯状态估计问题**，从而引入更丰富的非线性动态和可解释的门控机制。

---

### 🆕 提出的新方法：Kalman Linear Attention (KLA)

KLA 是一种新型的 **sequence mixer 层**，其核心思想是：
> 将每个输入 token 视为对潜在语义状态的**带噪观测**，通过 **Kalman Filter** 进行在线后验推断来更新信念状态。

#### 创新点：
- **C1. 卡尔曼滤波的信息形式重参数化（Associative Reparameterisation）**  
  将 Kalman Filter 转换为**信息形式（information form）**，证明其精度（precision）更新是一个 **Mobius (fractional-linear) 变换**，该变换具有**结合律**，因此可通过 **associative scan** 实现并行计算。
  - 结果：实现 $O(\log T)$ 并行深度，与 Mamba 同级效率。

- **C2. 不确定性驱动的非线性门控（Nonlinear Gating from Uncertainty）**  
  门控由**历史精度与当前观测精度的比例**决定，形成**历史依赖且非线性**的更新规则，超越了 GLA 的线性/仿射门控。

- **C3. 显式的信念状态不确定性建模**  
  输出不仅包含状态均值 $\mu_t$，还输出**协方差/精度 $\Lambda_t$**，可用于下游任务中的不确定性量化。

- **C4. Drop-in 替代模块设计**  
  KLA 可作为 Transformer 或 SSM 中的 **drop-in replacement**，无缝集成到现代语言模型架构中。

---

### 🔍 相比现有方法的优势

| 特性 | Softmax Attention | SSMs / GLA | KLA (本文) |
|------|-------------------|-----------|------------|
| 表达力 (Expressivity) | 非线性 (softmax) | 线性/仿射 | **分数线性 (Mobius)** |
| 训练效率 | $O(T^2)$ | $O(T)$ | $O(T)$ |
| 推理效率 | $O(T)$ | $O(1)$ | $O(1)$ |
| 显式状态不确定性 | ❌ | ❌ | ✅ |
| 并行训练支持 | ❌ | ✅ | ✅ |
| 状态追踪能力 | 中等 | 弱（线性限制） | **强（非线性更新）** |

> ✅ **KLA 在保持 SSM 效率的同时，获得了更强的表达能力和不确定性感知能力。**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集与任务

#### （1）MAD-Lab Synthetic Tasks（6项合成语言任务）
用于测试不同语言技能，包括：
- **Compression**：信息压缩能力
- **Memorization**：固定映射记忆
- **Context Recall (CR)**：上下文键值检索
- **Noisy Recall (NR)**：噪声环境下的选择性注意力
- **Fuzzy Recall (FR)**：多 token 键值匹配
- **Selective Copy (SC)**：有序复制与干扰抑制

> 所有任务均为可控的离散 token 操作任务，便于分析模型行为。

#### （2）Long-Context Multi-Query Associative Recall (MQAR)
- 序列长度：**T = 2048**
- 词表大小：**V = 256**
- 多查询绑定与远距离检索挑战
- 测试模型在高负载下的存储与检索能力

#### （3）A5 Permutation Composition Task
- 基于交替群 $A_5$ 的排列组合任务
- 本质是**不可并行化的顺序状态追踪问题**
- 用于检验模型是否具备真正的“状态机”能力

---

### ⚙️ 实验设置与评估指标

| 设置项 | 描述 |
|-------|------|
| **模型结构** | 单层或双层 block，统一使用 Mamba-style fused-MLP 架构 |
| **维度配置** | 统一控制有效状态大小（如 2048），确保公平比较 |
| **训练协议** | AdamW 优化器，固定学习率 $1\times10^{-3}$，最大 750 轮 |
| **评估指标** | 准确率（Accuracy %），达到 ≥90% 视为解决任务 |
| **随机种子** | 报告 5 个 seed 的平均结果，提升统计可靠性 |

---

### 🆚 基线方法对比

| 基线 | 类型 | 简介 |
|------|------|------|
| **Mamba** | SSM | 当前最流行的 selective SSM，输入依赖动态 |
| **GLA (Gated Linear Attention)** | Linear Attention | 统一 Mamba 等模型的框架，线性复杂度 |
| **GDN (Gated Delta Net)** | SSM | 基于 delta rule 的记忆机制，擅长关联回忆 |
| **mLSTM** | RNN | 改进版 LSTM，支持并行训练 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总（MAD-Lab 任务，Test Accuracy %）

| Algorithm | Comp | Mem | CR | NR | FR | SC |
|----------|------|-----|----|----|----|----|
| GDN      | 65.53 | 99.93 | 99.94 | 99.95 | 37.04 | 90.30 |
| GLA      | 49.45 | 99.99 | 73.60 | 85.24 | 18.22 | 82.41 |
| Mamba    | 78.35 | 99.96 | 99.92 | 99.93 | 30.83 | 80.60 |
| mLSTM    | 57.17 | 99.98 | 99.98 | 99.99 | 25.43 | 36.61 |
| **KLA (Ours)** | **85.03** | **98.87** | **99.95** | **99.93** | **45.70** | **90.67** |
| **KLA+ (Probabilistic Decoding)** | **88.87** | 99.94 | 99.94 | 99.95 | 43.32 | 91.45 |

> ✅ KLA 在多数任务上**匹配或超越所有基线**，尤其在 **Selective Copy** 和 **Compression** 上领先显著。

---

### 🔬 Long-Context MQAR（T=2048, V=256）

| Model Dim | KLA | GDN | Mamba | GLA |
|----------|-----|-----|--------|------|
| d=64     | ~80% | ✅ 最佳 | ✅ | ❌ 未学会 |
| d=128    | ~90% | ✅ | ✅ | ❌ |
| **d=256** | **>95% ✅** | ~90% | ~85% | ❌ |

> ✅ KLA 在极限设置下仍接近完美准确率，**首次在此难度下突破 95%**，建立新 SOTA。

---

### 🧪 A5 State Tracking（Permutation Composition）

- **任务要求**：随着序列增长，所需层数应不增加（理想情况 1–2 层即可解决）
- **结果（见 Figure 1）**：
  - **KLA**：仅需 **1–2 层** 即可解决任意长度任务
  - **Mamba / GLA / Transformer**：所需层数随长度线性增长 → 表明其无法真正“追踪状态”

> ✅ KLA 的 **fractional-linear 更新结构**使其能执行传统线性 SSM 无法完成的**硬性状态追踪任务**。

---

### 🔍 消融实验（Ablation Studies）

#### （1）OU Prior Discretisation Ablation（图4）
- 移除 OU 动力学建模 → 准确率下降，训练不稳定
- 尤其在深层模型中影响明显
- ✅ 证明连续时间先验 + 正确离散化对性能至关重要

#### （2）Process Noise Ablation（图10）
- 固定过程噪声 $p_t = 0$（退化为确定性系统）→ 性能大幅下降
- 平均准确率下降 **49.8个百分点**
- 在 Memorization 和 Recall 任务中下降超 **90%**

> ✅ **过程噪声 $p_t$ 是维持模型表达力的关键组件**，不能简单设为零。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **贝叶斯过滤视角可行且高效**  
   将语言建模视为 Kalman Filter 问题，不仅能提供**可解释的不确定性信号**，还能通过信息形式实现**完全并行训练**。

2. **Mobius 结构解锁更强表达力**  
   KLA 的 precision update 是 **fractional-linear (Mobius) map**，比线性 SSM 更具表达力，足以解决 $A_5$ 这类**真·顺序任务**。

3. **不确定性即门控机制**  
   精度比自动调节新旧信息权重，形成**自适应、历史依赖的非线性门控**，无需额外设计 gating 函数。

4. **优于现有 SSM，在极端任务中领先明显**  
   在长上下文、噪声干扰、状态追踪等任务上全面胜出，特别是在 **MQAR (T=2048)** 和 **A5** 上表现突出。

---

### ⚠️ 局限性（Limitations）

1. **尚未应用于大规模预训练**  
   当前实验集中在浅层、小规模设置，未验证其在百亿参数以上 LLM 中的表现。

2. **未充分利用 posterior covariance**  
   虽然输出了不确定性，但当前任务未用于 **hallucination detection、OOD detection、calibration** 等实际场景。

3. **硬件优化仍有空间**  
   当前 Triton kernel 已提速，但仍未达到 Mamba 级别的 fused kernel 优化水平，存在进一步加速潜力。

---

### 🔮 未来工作方向

1. **构建 Probabilistic LLM**  
   将 KLA 扩展至深层、大规模预训练，探索其在开放生成、推理链、校准预测等方面的优势。

2. **利用 posterior uncertainty 进行安全 AI 应用**  
   如：检测幻觉、识别 OOD 输入、主动学习提示选择等。

3. **结合 variational inference 或 particle filtering**  
   探索更复杂的非线性/非高斯扩展版本（如 Particle KLA）。

4. **端到端优化 custom kernels**  
   实现类似 Mamba 的 fused kernel，最大化吞吐量与能效。

---

## ✅ 总结一句话

> **KLA 成功地将 Kalman Filter 的原则性不确定性建模与 SSM 的高效并行训练相结合，提出了一种兼具表达力、效率与可解释性的新一代 sequence mixer，在多项挑战性任务上展现出超越现有 SSM 的强大状态追踪与鲁棒建模能力。**

</details>

---

### 12. [SynergyKGC: Reconciling Topological Heterogeneity in Knowledge Graph Completion via Topology-Aware Synergy](https://arxiv.org/abs/2602.10845)

**Authors**: Xuecheng Zou, Yu Tang, Bingbing Wang  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10845v1  

#### Abstract
Knowledge Graph Completion (KGC) fundamentally hinges on the coherent fusion of pre-trained entity semantics with heterogeneous topological structures to facilitate robust relational reasoning. However, existing paradigms encounter a critical "structural resolution mismatch," failing to reconcile di...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《SynergyKGC: Reconciling Topological Heterogeneity in Knowledge Graph Completion via Topology-Aware Synergy》总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **Knowledge Graph Completion (KGC)** 方法在融合预训练语义（如 PLMs）与图结构信息（如 GNNs）时面临一个根本性瓶颈：**Passive Structural-Semantic Alignment**。这种被动对齐导致两个关键问题：
- **Representation Collapse**：在稀疏图中，实体缺乏足够的结构支撑，导致表示崩溃；
- **Identity Redundancy**：在密集图中，显式的自环结构引入不可忽略的噪声，造成过平滑。

此外，许多模型在训练和推理阶段存在 **Structural Decoupling**（推理时关闭结构聚合），引发 **Inference-time Distribution Shift**，损害部署性能。

---

### 🚀 提出的新方法与创新思路
作者提出 **SynergyKGC**，一种主动、指令驱动的双模态协同框架，核心创新包括：

#### （1）**Cross-Modal Synergy Expert**
- 引入基于 **relation-aware cross-attention** 的协同专家模块，将语义向量作为“查询指令”来主动检索并过滤拓扑上下文；
- 不同于传统 GNN 的被动邻居聚合，实现 **active structural retrieval**，提升信息整合效率。

#### （2）**Dual-Axis Consistency Principle**
- **Architecture Axis**：强制 Query Tower 与 Entity Tower 在表示学习上保持对称协同；
- **Lifecycle Axis**：确保训练与推理阶段均激活 Synergy Expert，消除分布偏移（distribution shift）。

#### （3）**Density-Aware Identity Anchoring (IA) Strategy**
- 提出“结构即身份”（Structure ~ Identity）现象理论：在稀疏图中，自身份信号是必要的定位支架；在稠密图中则应抑制以避免冗余。
- 动态切换是否保留 `self-loop`：对密集图设阈值 $ \phi = 1 $，稀疏图不设限。

#### （4）**Instruction-Driven Paradigm 与 “Catch-up Effect”**
- 利用语义意图作为激活信号，在第二阶段触发即时同步；
- 观察到 **“catch-up effect”**：Synergy Expert 激活后，落后的反向流迅速追上，显著加速收敛（无需 >30 epochs 的 warm-up）。

---

### ⚖️ 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 跳过冗长 warm-up 阶段，快速收敛（Epoch 20 即达最优） |
| **精度** | 显著提升 Hits@1，尤其在稀疏图 WN18RR 上表现突出 |
| **鲁棒性** | 自适应处理不同密度图的拓扑异质性 |
| **一致性** | 消除训练-推理间的表示漂移，增强部署稳定性 |

---

## 2. 核心实验方法和设置

### 📊 数据集
使用两个标准 benchmark，具有极化结构特性：

| 数据集 | 特征 |
|-------|------|
| **FB15k-237** | 密集关系图（P50=22），文本较长（avg. 114.6 tokens） |
| **WN18RR** | 稀疏层次图（P50=3），语义简洁（avg. 17.1 tokens） |

> 表格 1 提供了详细的统计信息，涵盖实体数、关系数、训练/验证/测试样本量及密度分位数等。

---

### 🧪 实验设置
- **模型架构**：
  - 使用 `bert-base-uncased` 作为语义编码器；
  - Synergy Expert 采用 4-head cross-attention 结构（d=768）；
- **优化器**：AdamW，batch size=768，单卡 A100；
- **学习率**：从 {1e-5, 5e-5, 5e-4} 中选择；
- **两阶段训练策略**：
  - Phase I：仅优化语义空间（contrastive loss）；
  - Phase II：激活 Synergy Expert，联合优化 $ \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{NCE}} + \lambda (\mathcal{L}_{\text{align}} + \mathcal{L}_{\text{gate}}) $，其中 $ \lambda = 0.1 $；
  - 启动时机：FB15k-237 在 Epoch 5，WN18RR 在 Epoch 20。

---

### 🎯 评估指标
采用标准 **filtered setting** 下的排序指标：
- **MRR**（Mean Reciprocal Rank）
- **Hits@k**（k ∈ {1, 3, 10}）
- **MR**（Mean Rank）

重点关注 **Hits@1** 和 **MRR**，反映高精度预测能力。

---

### 🔁 基线方法对比
涵盖三类主流方法：
| 类型 | 代表模型 |
|------|--------|
| **Embedding-based** | TransE, RotatE, DistMult, ComplEx, TuckER |
| **PLM-based / Hybrid** | KG-BERT, StAR, LP-BERT, KG-S2S, SimKGC, ProgKGC |

特别以 **ProgKGC (2025)** 为最强混合基线进行比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 2）

| Model | FB15k-237 (MRR / H@1) | WN18RR (MRR / H@1) |
|-------|------------------------|--------------------|
| **RotatE** | 47.6 / 42.8 | 33.8 / 24.1 |
| **SimKGC** | 33.6 / 24.9 | 66.6 / 58.7 |
| **ProgKGC** | 34.4 / 25.5 | 68.2 / 59.7 |
| **SynergyKGC (Ours)** | **39.9 / 30.2** | **74.2 / 67.7** |
| **Improvement** | +5.5 / +4.7 | **+6.0 / +8.0** |

> 💥 在 WN18RR 上取得 **+8.0% Hits@1 的绝对增益**，刷新 SOTA。

---

### 🔍 消融实验结果（Table 4 & Figure 4）

#### （1）组件消融分析（Table 4）
移除关键模块后性能下降明显：
- 移除 **Cross-Attention** 在 WN18RR 上导致 MRR ↓21.3%，Hits@1 ↓25.7%，说明其为稀疏环境中的“位置支架”；
- 移除 **Adaptive Gate** 在 FB15k-237 上 MRR ↓3.1%，表明其有效抑制密集图中的结构噪声；
- 移除 **MSE Alignment Loss** 导致双塔失配，验证了语义一致性的必要性。

#### （2）Dual-Tower Consistency 分析（Figure 4）
- **Architectural Consistency**：Query 与 Entity Tower 必须同时启用 Synergy，否则性能大幅下滑；
- **Lifecycle Consistency**：训练用结构、推理不用 → 性能骤降，证明跨阶段一致性至关重要。

#### （3）Topological Scaling（Table 3）
- **FB15k-237（密集）**：最佳性能出现在 **2-hop** 邻域，更深无益；
- **WN18RR（稀疏）**：虽然 Hits@k 在 Hop1 达饱和，但 **5-hop 可显著降低 Mean Rank**，体现深层拓扑对全局排序的优化作用。

#### （4）Identity Anchoring 敏感性分析（Table 5）
- 对 **WN18RR**，最优阈值接近最大度（482），说明需保留所有连接；
- 对 **FB15k-237**，最小阈值 $ \phi=1 $ 最优，过大反而引入噪声；
- 图 5 进一步证明：即使压缩至单一关系，锚定效果仍由拓扑密度决定，而非关系复杂度。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **拓扑异质性是 KGC 的核心挑战**：图密度差异导致结构信号的作用截然不同；
2. **SynergyKGC 成功实现了动态调制**：通过 IA 策略和 adaptive gate，在稀疏图中强化身份锚定，在密集图中抑制冗余；
3. **Dual-Axis Consistency 是稳定性的基石**：架构与生命周期的一致性设计有效缓解 manifold drift；
4. **“Catch-up Effect” 加速收敛**：Synergy Expert 激活后立即实现双流同步，跳过传统方法所需的长 warm-up；
5. **WN18RR 上 +8.0% Hits@1 验证了泛化能力**：尤其擅长处理低密度、长尾分布场景。

---

### ⚠️ 局限性
- 当前方法依赖 BERT 编码器，可能受限于 PLM 的语义粒度；
- 虽然支持多跳邻域，但未探索更复杂的子图结构（如路径、规则）；
- Identity Anchoring 的阈值目前为经验设定，尚未完全自动化。

---

### 🔮 未来工作方向
- 将 IA 策略扩展为可学习的动态门控机制；
- 探索结合 symbolic reasoning 与神经协同的 hybrid 架构；
- 应用于更大规模工业知识图谱（如电商、医疗 KG）；
- 研究如何将该范式迁移至其他非均匀结构数据任务（如 social networks, citation graphs）。

---

## 🔗 开源信息
代码已公开：
👉 [https://github.com/XuechengZou-2001/SynergyKGC-main](https://github.com/XuechengZou-2001/SynergyKGC-main)

包含完整模型实现、训练脚本与预处理数据集，便于复现与拓展研究。

</details>

---

### 13. [Prioritize the Process, Not Just the Outcome: Rewarding Latent Thought Trajectories Improves Reasoning in Looped Language Models](https://arxiv.org/abs/2602.10520)

**Authors**: Williams Jonathan, Tureci Esin  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.10520v1  

#### Abstract
Looped Language Models (LoopLMs) perform multi-step latent reasoning prior to token generation and outperform conventional LLMs on reasoning benchmarks at smaller parameter budgets. However, attempts to further improve LoopLM reasoning with reinforcement learning have failed - standard objectives su...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Prioritize the Process, Not Just the Outcome: Rewarding Latent Thought Trajectories Improves Reasoning in Looped Language Models*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
传统的强化学习（**Reinforcement Learning with Verifiable Rewards, RLVR**）方法（如 **GRPO**）在应用于 **Looped Language Models (LoopLMs)** 时效果不佳。其根本原因在于，这些方法只将奖励分配给最终的隐状态（latent state），而忽略了模型内部多步迭代推理的完整过程。这导致了一个“信用分配瓶颈”（credit assignment bottleneck），即学习信号必须通过多个内部循环反向传播，从而削弱了训练效率。

### 提出了什么新方法或新思路
本文提出了 **RLTT (Reward Latent Thought Trajectories)**，一种全新的强化学习框架。其核心思想是：**如果一个 LoopLM 在多个隐状态迭代中进行推理，那么学习信号就应该奖励整个推理轨迹，而不仅仅是终点**。

- **核心机制**：RLTT 将奖励沿着整个隐状态轨迹进行分配。它聚合了每个内部循环（loop）产生的 next-token 分布，并利用这个聚合分布来塑造策略梯度（policy gradient），从而实现了更密集的信用分配。
- **无需外部验证器**：该方法直接利用模型内部的计算，不依赖于外部的评分模型（如 GPT-4）来评估中间步骤，避免了高昂的计算开销。

### 相比现有方法的优势
- **对齐模型架构**：RLTT 与 LoopLM 的多步内部计算本质完美对齐，解决了 GRPO 等方法与模型内在计算逻辑的根本性错配。
- **高效且轻量**：RLTT 可以直接替代 GRPO，计算开销极小（主要是内存占用增加），易于集成。
- **提升推理效率**：鼓励模型在更早的隐状态就收敛到正确的答案，减少了不必要的“过度思考”（overthinking）。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **数学推理基准**（Mathematical Reasoning Benchmarks）：
  - `MATH-500`, `AIME24`, `BeyondAIME`, `GSM8k`
- **非数学推理基准**（Non-mathematical Reasoning Benchmarks）：
  - `ARC-C` (常识推理), `MMLU-ST` (多领域问答), `GPQA` (研究生级别问答), `MBPP` (代码生成)

### 实验设置和评估指标
- **基础模型**：所有实验均基于开源的 **Ouro-2.6B-Thinking** 模型。
- **训练条件**：严格匹配计算资源，确保与基线方法（GRPO）在相同的 `rollout` 预算、优化设置、奖励函数和优势归一化下进行比较。
- **评估协议**：
  - **零样本评估**（Zero-shot evaluation）
  - **确定性解码**（Deterministic decoding）
  - **精确匹配**（Exact-match answer parsing）
  - **固定推理预算**（Fixed inference budgets），例如 MATH-500 使用 2048 个 token。
- **消融实验**：测试了不同的 `loop weighting` 策略（Uniform, Progressive, Exit-probability）。

### 基线方法对比
- 主要对比方法为 **GRPO**（Group Relative Policy Optimization），这是当前用于 LLM 强化学习的标准方法。
- 同时也报告了 **SFT**（监督微调）和其他先进模型（如 Qwen3, DeepSeekR1）的结果作为参考。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
在 **Ouro-2.6B-Thinking** 模型上，RLTT 相比 GRPO 取得了显著的性能提升：

| Benchmark     | GRPO Accuracy | RLTT Accuracy | Improvement |
|---------------|---------------|---------------|-------------|
| **MATH-500**  | 71.6%         | **86.0%**     | **+14.4%**  |
| **AIME24**    | 16.7%         | **33.3%**     | **+16.6%**  |
| **BeyondAIME**| 6.0%          | **16.0%**     | **+10.0%**  |
| **GSM8k**     | 59.7%         | **94.0%**     | **+34.3%**  |

- **平均提升**：在数学基准上平均提升 **+18.8%**，在非数学基准上平均提升 **+6.6%**。

### 与基线方法的对比结果
- **全面超越**：RLTT 在所有数学和非数学基准上均显著优于 GRPO。
- **统计显著性**：通过配对 t 检验（paired t-tests）证实，所有提升均具有统计显著性（p < 0.05）。
- **零样本迁移能力**：尽管仅在数学数据上训练，RLTT 在非数学任务（如 GPQA）上也表现出色，证明了其推理能力的泛化性。

### 消融实验结果
- **对权重策略不敏感**：无论采用 Uniform、Progressive 还是 Exit-probability 权重，RLTT 的性能都保持稳定且远超 GRPO。这表明其成功的关键在于“奖励整个轨迹”的思想本身，而非具体的信用分配方案。
- **解码长度鲁棒性**：即使在远超训练长度（如 4096 tokens）的推理预算下，RLTT 依然保持优势，证明其未过拟合特定长度。
- **早期推理更强**：在受限的循环次数（如 1-2 loops）下，RLTT 的性能远高于 GRPO，说明其确实提升了早期隐状态的推理质量。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **过程优先至关重要**：对于 LoopLMs，强化学习的成功关键在于奖励**推理过程**（latent thought trajectory），而不仅仅是最终结果。
2. **提升推理效率**：RLTT 促使模型更快地收敛到正确答案，生成的响应更短，训练时间减少约 10%，体现了更高的**token 效率**。
3. **提供更丰富的梯度信号**：在最困难的任务（如 AIME24）上，RLTT 显著提高了梯度信噪比（GSNR），为模型提供了更丰富、更一致的学习信号。
4. **强大的泛化能力**：RLTT 学习到的是一种更通用的、高效的推理模式，能够有效迁移到未见过的非数学任务中。

### 方法的局限性
- **内存开销**：需要存储每个循环的对数概率（log-probabilities），增加了内存占用，在固定 GPU 内存下会限制每次优化步所能处理的 token 数量。
- **架构特异性**：目前仅适用于具有显式多步隐状态迭代的 **Looped Architectures**，无法直接应用于标准的非循环 LLM。
- **固定循环深度**：实验中使用了固定的循环深度，牺牲了模型原生的自适应提前退出（adaptive early-exit）能力。

### 未来工作方向
- 开发**内存更高效的实现**，以缓解内存瓶颈。
- 探索将 RLTT 与**自适应终止机制**相结合，恢复按需分配计算资源的能力。
- 将该框架扩展到其他类型的**潜变量推理模型**（latent reasoning models）。

</details>

---

### 14. [Divide, Harmonize, Then Conquer It: Shooting Multi-Commodity Flow Problems with Multimodal Language Models](https://arxiv.org/abs/2602.11057)

**Authors**: Xinyu Yuan, Yan Qiao, Zonghui Wang, Wenzhi Chen  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11057v1  

#### Abstract
The multi-commodity flow (MCF) problem is a fundamental topic in network flow and combinatorial optimization, with broad applications in transportation, communication, and logistics, etc. Nowadays, the rapid expansion of allocation systems has posed challenges for existing optimization engines in ba...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Divide, Harmonize, Then Conquer It: Shooting Multi-Commodity Flow Problems with Multimodal Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Multi-Commodity Flow (MCF)** 问题，即在复杂网络拓扑中为多个商品流（如网络流量、物流需求）分配路径以优化全局目标（如最小化最大链路利用率 MLU、最大化总流量等）。该问题是组合优化中的经典难题，广泛应用于交通、通信、云计算等领域。

传统方法依赖 **Linear Programming (LP)** 求解器，在大规模系统中面临严重可扩展性瓶颈；而现有的 **Machine Learning (ML)** 方法虽然速度快，但在泛化能力、准确性及对未见环境的鲁棒性方面表现不佳。

---

### 提出的新方法：PRAM
作者提出 **PRAM (Partitioned Resource Allocation with Multimodal Language Models)** ——首个基于 **Multimodal Language Models (MLMs)** 的端到端 MCF 求解框架。

#### 核心思想：“分而治之，再协调”
- **Divide (划分)**：将原问题按源节点划分为多个子任务（每个子任务处理从同一源出发的所有流），从而降低单个子问题规模。
- **Harmonize (协调)**：通过轻量级多智能体强化学习（MARL）机制实现各子任务间的协同优化，确保全局一致性。
- **Conquer (求解)**：利用预训练 MLM 作为共享“代理”模型，直接推理生成高质量路径权重配置。

---

### 创新点与优势

| 创新点 | 描述 | 优势 |
|--------|------|------|
| ✅ **首次引入 MLM 解决 MCF** | 将 MCF 建模为多模态输入下的决策任务，图像表示子拓扑，文本描述需求统计信息。 | 避免手动设计专用 DNN 架构（如 GNN），提升通用性和迁移能力。 |
| ✅ **轻量级自适应框架** | 结合 LoRA 和前缀上下文嵌入（learned global context），实现参数高效的微调。 | 显著减少可训练参数数量，支持快速部署与更新。 |
| ✅ **多智能体强化学习协调机制** | 使用 counterfactual policy gradients 估计个体贡献，驱动全局收敛。 | 理论上证明能模拟梯度下降过程，逼近最优解。 |
| ✅ **理论保障** | 在合理假设下，证明 PRAM 收敛至局部最优，并能内部模拟 GD 更新步骤。 | 提供优于纯黑箱 ML 方法的性能保证。 |

---

相比现有方法的优势：
- **速度远超 LP**：运行时间快 10–100 倍；
- **精度接近最优**：平均性能差距 <8%；
- **强鲁棒性**：面对链路故障或流量突增时性能下降 <10%；
- **无需重新训练即可泛化**：适用于不同拓扑与需求分布。

---

## 2. 核心实验方法和设置

### 数据集

#### 实际世界数据集（Small-scale）
| 名称 | 节点数 | 链路数 | 应用场景 |
|------|--------|--------|----------|
| MetaDB / MetaWEB | 4 / 8 | 12 / 56 | 社交网络集群 |
| Abilene | 12 | 30 | 美国学术骨干网 |
| CERNET | 14 | 32 | 中国教育科研网 |
| GEANT | 23 | 74 | 欧洲研究网络 |

#### 大规模合成数据集（Large-scale）
| 名称 | 节点数 | 链路数 | 来源 |
|------|--------|--------|-------|
| GtsCe | 149 | 386 | Internet Topology Zoo |
| Colt | 153 | 354 | 同上 |
| UsCarrier | 158 | 378 | 同上 |
| Cogentco | 197 | 486 | 同上 |
| Kdl | 754 | 1,790 | 同上 |

> 所有合成数据采用 Gravity、Poisson 和 Bimodal 模型生成，模拟真实流量特征。

---

### 实验设置与评估指标

#### 三大优化目标
1. **Maximum Link Utilization (MLU)**：最小化最高链路负载，增强容错能力。
2. **Maximum Total Flow (MTF)**：最大化成功传输总量。
3. **Maximum Concurrent Flow (MCF)**：最大化公平并发吞吐率。

> 所有结果归一化为相对于 LP 最优解的比例（越接近 1 越好）。

#### 输入表示方式
- **视觉模态**：使用 Matplotlib 绘制从某源节点出发的所有候选路径构成的子图（PNG 图像）；
- **文本模态**：提供近期流量、历史均值等统计数据；
- **提示工程（Prompting）**：构造结构化 prompt 引导 MLM 输出 `[path_weight_1, ..., path_weight_k]` 形式的路径权重向量。

#### 微调策略
- 使用 **multi-agent RL** 进行 fine-tuning；
- 采用 **counterfactual baseline** 计算每个 agent 的 advantage；
- 可训练模块仅限于 LoRA 适配器与 context embeddings，保持主干冻结。

---

### 基线方法对比

| 方法 | 类型 | 是否预测 |
|------|------|---------|
| **LP (Gurobi)** | 数学规划 | ✅（理想未来已知） |
| **LP (pred)** | 数学规划 + 移动平均预测 | ❌ |
| **POP** | 分解 + 并行 LP | ✅ |
| **POP (pred)** | 同上 + 预测 | ❌ |
| **LP-top** | 启发式（Top 10% 流用 LP） | ✅ |
| **LP-top (pred)** | 同上 + 预测 | ❌ |
| **DRL** | 深度强化学习（PPO） | ❌ |
| **HARP** | GNN-based ML | ❌ |
| **Aether** | Graph Transformer + MARL | ❌ |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Figure 6 & 7）

| 指标 | PRAM 表现 | 对比说明 |
|------|-----------|----------|
| **MLU (归一化)** | 平均 0.92–0.98 | 接近 LP 最优（1.0），优于所有 ML 方法（DRL: ~0.75, HARP: ~0.88） |
| **MTF (归一化)** | >90% LP 性能 | 显著优于 HARP (+16.6%) 和 Aether (+7.3%) |
| **MCF (归一化)** | >90% LP 性能 | 超出 HARP (+24.8%) 和 Aether (+13.5%) |
| **运行时间** | **快 10–100×** | 在 Kdl 拓扑上：<25 秒 vs. LP >2500 秒 |
| **鲁棒性（链路失效）** | MLU 上升 <15%（σ=2） | 明显优于其他 ML 方法（上升 20–30%） |

> 特别地，在 CERNET 和 GEANT 上，PRAM 的 MLU 甚至 **低于 LP**（分别低 21% 和 45%），表明其在特定凸性条件下更具稳定性。

---

### 消融实验结果（Ablation Study）

#### （1）是否进行问题划分？
- **无划分版本（PRAM w/o par.）**：参数量随网络规模爆炸增长（Kdl 达 31,600 MB），无法实际运行；
- **PRAM**：参数量稳定在数百 MB 内，具备良好可扩展性。

> ✅ **结论**：划分是应对大规模 MCF 的关键。

#### （2）各组件作用分析（Figure 9b）
| 变体 | MLU 下降幅度（vs. 完整 PRAM） |
|------|-------------------------------|
| w/o MLM | ↓ ~15% |
| w/o context | ↓ ~10% |
| w/o LoRA | ↓ ~8% |
| w/o MARL | ↓ ~12% |
| w/o partition | 不可行（过大） |

> ✅ 所有组件均有显著贡献，尤其是 **MLM 推理能力** 与 **MARL 协同机制**。

#### （3）可视化分析（Figure 11）
- Cross-attention map 显示 context embeddings 能有效关注与 MCF 相关的关键词（如 "Flow", "Capacity"）；
- 学习到的 prototypes 成功对齐任务语义空间。

> ✅ 表明 context 模块确实捕捉到了全局协调信号。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **MLM 具备解决复杂资源分配问题的潜力**  
   预训练 MLMs 展现出强大的数学推理与泛化能力，能够在少量微调后胜任 MCF 决策任务。

2. ✅ **“划分 + 协调”范式有效平衡效率与质量**  
   通过将大问题分解并由共享代理并行求解，再通过 MARL 实现全局一致，实现了 **scalability 与 optimality 的双赢**。

3. ✅ **PRAM 在实践中兼具高速、高质、高鲁棒性**  
   - 速度：比 LP 快 1–2 个数量级；
   - 精度：接近最优解（<8% gap）；
   - 泛化：对拓扑变化、流量波动、链路失效均表现出强鲁棒性。

4. ✅ **理论上有保障**  
   在 MCF 目标函数满足凸/拟凹性质的前提下，PRAM 可证明收敛，并能内部模拟 GD 更新过程（Theorem 1 & 2）。

---

### 方法的局限性

| 局限 | 说明 |
|------|------|
| 🔴 **微调仍较耗资源** | 尽管使用 LoRA 和 truncation，完整 fine-tuning 仍需 GPU 时间，不适合实时动态重训。 |
| 🔴 **视觉编码可能引入偏差** | 子图绘制方式（如重叠边、布局算法）可能影响 MLM 视觉理解效果。 |
| 🔴 **非自回归生成限制表达力** | 当前采用非自回归方式输出路径权重，难以建模复杂依赖关系。 |
| 🔴 **依赖预训练 MLM 质量** | 若 MLM 缺乏数值推理或图理解能力，则性能受限。 |

---

### 未来工作方向

1. **探索更高效的适配技术**  
   如结合 **Adapter**, **BitFit**, 或 **Prompt Tuning** 进一步压缩参数量。

2. **引入自回归建模机制**  
   借鉴 Chain-of-Thought 或 Plan-and-Execute 框架，让 MLM 自主迭代优化方案。

3. **构建 MCF 专用预训练任务**  
   在大规模网络数据上进行 in-context pretraining，提升领域适应性。

4. **拓展至动态与时序 MCF 场景**  
   支持连续时间窗口下的联合调度与预测。

5. **集成后处理 refinement 模块**  
   如结合 LP 局部精调（post-solution refinement），进一步逼近最优。

---

> 📌 **代码开源地址**：[https://github.com/Y-debug-sys/PRAM](https://github.com/Y-debug-sys/PRAM)  
> 📌 **伦理声明**：未使用任何敏感用户数据，仅基于聚合流量矩阵进行研究。

</details>

---

### 15. [TabICLv2: A better, faster, scalable, and open tabular foundation model](https://arxiv.org/abs/2602.11139)

**Authors**: Jingang Qu, David Holzm\"uller, Ga\"el Varoquaux, Marine Le Morvan  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.11139v1  

#### Abstract
Tabular foundation models, such as TabPFNv2 and TabICL, have recently dethroned gradient-boosted trees at the top of predictive benchmarks, demonstrating the value of in-context learning for tabular data. We introduce TabICLv2, a new state-of-the-art foundation model for regression and classificatio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TabICLv2: A better, faster, scalable, and open tabular foundation model 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的梯度提升树（如 XGBoost、LightGBM）长期在表格数据预测任务上占据主导地位。尽管近期出现的 **Tabular Foundation Models (TFMs)** 如 TabPFN 和 TabICL 展现了潜力，但仍面临以下挑战：
- **可扩展性差**：难以有效处理百万级样本的大规模数据集。
- **推理速度慢**：尤其是在长序列（大量训练样本）场景下，注意力机制存在“**attention fading**”问题，导致性能下降。
- **依赖调参与集成**：最先进的模型（如 RealTabPFN-2.5）需要复杂的超参数调优（tuning）、加权集成（ensembling）甚至在真实数据上微调（fine-tuning），成本高昂且不开放。
- **预训练数据多样性不足**：合成数据生成器的设计限制了模型的泛化能力。

### 提出的新方法与创新
本文提出了 **TabICLv2**，一个全新的、开源的表格基础模型，其核心创新建立在三大支柱之上：

#### （1）新型合成数据生成引擎 (Novel Synthetic Data Generation Engine)
- 设计了一个高度多样化、可控的合成数据先验（prior），旨在最大化预训练数据的多样性。
- 引入了新的随机图生成机制（“random Cauchy graph”），支持更复杂的非树状依赖关系。
- 丰富了随机函数类型（共8种），包括 MLP、树集成、高斯过程（GP）、分段函数等，以模拟不同平滑性和归纳偏置的数据模式。
- 采用相关采样（correlated sampling）机制，使生成的数据属性（如类别基数）之间具有相关性，更贴近真实世界数据。

#### （2）架构创新 (Architectural Innovations)
- **重复特征分组 (Repeated Feature Grouping)**：通过循环移位将每个特征分配到多个组中，打破了特征对称性，缓解了表示坍缩（representation collapse）问题，同时保留了所有特征的精细信息。
- **目标感知嵌入 (Target-aware Embedding)**：在早期阶段就将标签/目标值嵌入到所有特征表示中，增强了模型对特征与目标关联性的学习。
- **查询感知可扩展 Softmax (Query-aware Scalable Softmax, QASSMax)**：
  - 这是解决 **long-context generalization** 的核心创新。
  - 在标准 Softmax 基础上引入了基于 `log n`（n为上下文长度）的动态缩放，并结合查询感知的门控机制。
  - 公式：`q_hi = q_hi · MLP_base(log n)_hi : (1 + tanh(MLP_gate(q_h)))`
  - 有效缓解了“attention fading”，使模型能从数万甚至数十万的训练样本中有效学习。

#### （3）优化的预训练协议 (Optimized Pretraining Protocols)
- **三阶段渐进式预训练**：逐步增加预训练数据集的大小（从1K到60K样本），使模型能更好地适应大规模数据。
- **采用 Muon 优化器**：替代常用的 AdamW，结合谨慎权重衰减（cautious weight decay），显著提升了训练效率和最终性能。
- **高效的推理优化**：实现了磁盘卸载（disk offloading），使得在仅需 50GB GPU 内存的情况下即可处理百万级样本的表格。

### 相比现有方法的优势
- **性能更强**：在多个基准测试上超越了当前最先进的 RealTabPFN-2.5（该模型经过调参、集成和微调）。
- **速度快得多**：在 H100 GPU 上，处理 50K 样本时比 TabPFN-2.5 快 10.6 倍；在 CPU 上快 11.8 倍。
- **可扩展性好**：能原生处理百万级样本的数据集，而无需检索（retrieval）或蒸馏（distillation）等复杂策略。
- **完全开源**：承诺公开推理代码、模型权重、合成数据引擎和预训练代码，推动社区发展。
- **开箱即用**：无需任何调参或微调，直接使用默认配置即可获得顶尖性能。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **TabArena (Erickson et al., 2025)**：包含 51 个数据集（38 分类，13 回归），用于评估模型在小到中等规模数据上的表现。
- **TALENT (Ye et al., 2024)**：包含 300 个数据集（120 二分类，80 多分类，100 回归），数据量更大，用于全面评估。
- **合成数据**：所有预训练数据均为合成生成，未使用任何真实数据进行预训练。

### 实验设置和评估指标
- **评估指标**：
  - **Improvability**：衡量方法相对于最佳方法的相对误差差距，数值越低越好。这是本文的主要报告指标。
  - **Elo Rating**：基于成对比较的评分系统，分数越高越好。
  - **Average Rank**：在所有数据集上的平均排名，数值越低越好。
  - **Runtime**：训练和推理时间，衡量效率。
- **实验设置**：
  - 对于分类任务，使用 8 或 32 个估计器（ensemble）进行预测。
  - 所有结果均通过交叉验证或给定的训练/验证/测试划分进行评估。
  - TabICLv2 的推理在 H100 GPU 上进行，其他方法的时间数据来自基准测试报告。

### 基线方法对比
- **传统模型**：XGBoost, LightGBM, CatBoost, RandomForest, ExtraTrees 等。
- **深度学习模型**：MLP, FT-Transformer, ResNet, ModernNCA 等。
- **自动化机器学习 (AutoML)**：AutoGluon。
- **其他表格基础模型 (TFMs)**：
  - **TabPFNv2**, **TabPFN-2.5**
  - **RealTabPFN-2.5**：在真实数据上继续预训练的 TabPFN-2.5，是当时的 SOTA。
  - **TabICL**, **LimiX**, **Mitra**, **TabDPT** 等。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
- **在 TabArena 基准上**：
  - TabICLv2 **无需任何调参**，其性能就超过了经过 **调参+集成+微调** 的 RealTabPFN-2.5。
  - 在 **Improvability vs. Train Time** 和 **Improvability vs. Inference Time** 的帕累托前沿（Pareto Front）上，TabICLv2 显著优于所有基线模型。
  - 在回归任务上，TabICLv2 与 RealTabPFN-2.5 并列第一（平均排名 4.54）。

- **在 TALENT 基准上**：
  - TabICLv2 取得了 **4.66** 的平均排名，优于 RealTabPFN-2.5 (5.11) 和 TabPFN-2.5 (5.45)。
  - 成对胜率：TabICLv2 对 RealTabPFN-2.5 的胜率为 **62%**，对 TabPFN-2.5 的胜率为 **65%**。
  - **多分类任务 (>10 类)**：TabICLv2 表现出色，大幅领先于所有基线。

- **运行效率**：
  - **速度**：在 H100 GPU 上，处理 50K 样本时，TabICLv2 比 TabPFN-2.5 快 **10.6 倍**。
  - **内存**：通过磁盘卸载，可在 **50GB GPU 内存**下处理百万级样本的表格。

### 消融实验结果 (Ablation Study)
消融实验量化了各组件的贡献（在验证集上）：
- **最大的影响来自合成数据先验 (Prior)**：使用 TabICL 的旧先验训练 TabICLv2，性能急剧下降，验证了高质量、多样化的合成数据对模型成功至关重要。
- **关键组件贡献相当**（约 100 Elo 增益）：
  - 采用 **Muon 优化器** 替代 AdamW。
  - 引入 **QASSMax** 注意力机制。
  - 早期加入 **目标信息 (early target inclusion)**。
- **其他改进带来较小增益**：
  - 重复特征分组 (Repeated feature grouping)。
  - 数据过滤 (Prior filtering)。

---

## 4. 关键结论和发现

### 主要发现
1. **高质量合成数据是基石**：一个设计精良、高度多样化的合成数据生成器对于训练强大的 TFM 至关重要，其作用可能超过架构本身的改进。
2. **简单有效的架构创新能带来巨大收益**：QASSMax 作为一种轻量级的注意力修改，有效解决了长上下文泛化问题，是 TabICLv2 能处理大规模数据的关键。
3. **优化器选择至关重要**：Muon 优化器在 TFM 的预训练中表现出优于 AdamW 的性能，是一个被低估但重要的工程选择。
4. **可以做到“更好、更快、更开放”**：TabICLv2 证明了无需依赖真实数据微调、复杂集成和闭源技术，也能构建出超越当前 SOTA 的表格模型。

### 方法的局限性
- **无法利用语义信息**：与 LLM-based 模型不同，它不能利用列名或文本特征中的语义信息。
- **超大规模数据仍具挑战**：虽然能处理百万级样本，但对于数千万或上亿样本的数据集，依然面临挑战。
- **缺失值处理简单**：目前对缺失值的处理方式（均值填充）较为基础，未在预训练中专门建模。
- **分布外泛化未知**：在与预训练先验差异极大的真实数据上的分布外（OOD）泛化能力有待进一步研究。

### 未来工作方向
- 探索如何结合列名和文本特征的语义信息。
- 开发更高效的方法来处理超大规模（十亿级）表格数据。
- 改进对缺失值、多输出回归和分布漂移（distribution shifts）的处理。
- 研究在特定领域数据上的微调（fine-tuning）或上下文优化（context optimization）是否能进一步提升性能。
- 推动更公平、更透明的表格学习基准测试。

</details>

---

### 16. [Neuro-Symbolic Synergy for Interactive World Modeling](https://arxiv.org/abs/2602.10480)

**Authors**: Hongyu Zhao, Siyu Zhou, Haolin Yang, Zengyi Qin, Tianyi Zhou  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.10480v1  

#### Abstract
Large language models (LLMs) exhibit strong general-purpose reasoning capabilities, yet they frequently hallucinate when used as world models (WMs), where strict compliance with deterministic transition rules--particularly in corner cases--is essential. In contrast, Symbolic WMs provide logical cons...

---

### 17. [dnaHNet: A Scalable and Hierarchical Foundation Model for Genomic Sequence Learning](https://arxiv.org/abs/2602.10603)

**Authors**: Arnav Shah, Junzhe Li, Parsa Idehpour, Adibvafa Fallahpour, Brandon Wang, Sukjun Hwang, Bo Wang, Patrick D. Hsu, Hani Goodarzi, Albert Gu  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.10603v1  

#### Abstract
Genomic foundation models have the potential to decode DNA syntax, yet face a fundamental tradeoff in their input representation. Standard fixed-vocabulary tokenizers fragment biologically meaningful motifs such as codons and regulatory elements, while nucleotide-level models preserve biological coh...

---

### 18. [MoToRec: Sparse-Regularized Multimodal Tokenization for Cold-Start Recommendation](https://arxiv.org/abs/2602.11062)

**Authors**: Jialin Liu, Zhaorui Zhang, Ray C. C. Cheung  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11062v1  

#### Abstract
Graph neural networks (GNNs) have revolutionized recommender systems by effectively modeling complex user-item interactions, yet data sparsity and the item cold-start problem significantly impair performance, particularly for new items with limited or no interaction history. While multimodal content...

---

### 19. [MerLin: A Discovery Engine for Photonic and Hybrid Quantum Machine Learning](https://arxiv.org/abs/2602.11092)

**Authors**: Cassandre Notton, Benjamin Stott, Philippe Schoeb, Anthony Walsh, Gr\'egoire Leboucher, Vincent Espitalier, Vassilis Apostolou, Louis-F\'elix Vigneux, Alexia Salavrakos, Jean Senellart  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.11092v1  

#### Abstract
Identifying where quantum models may offer practical benefits in near term quantum machine learning (QML) requires moving beyond isolated algorithmic proposals toward systematic and empirical exploration across models, datasets, and hardware constraints. We introduce MerLin, an open source framework...

---

### 20. [Neuro-symbolic Action Masking for Deep Reinforcement Learning](https://arxiv.org/abs/2602.10598)

**Authors**: Shuai Han, Mehdi Dastani, Shihan Wang  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10598v1  

#### Abstract
Deep reinforcement learning (DRL) may explore infeasible actions during training and execution. Existing approaches assume a symbol grounding function that maps high-dimensional states to consistent symbolic representations and a manually specified action masking techniques to constrain actions. In ...

---

### 21. [Latent Thoughts Tuning: Bridging Context and Reasoning with Fused Information in Latent Tokens](https://arxiv.org/abs/2602.10229)

**Authors**: Weihao Liu, Dehai Min, Lu Cheng  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10229v1  

#### Abstract
While explicit Chain-of-Thought (CoT) equips Large Language Models (LLMs) with strong reasoning capabilities, it requires models to verbalize every intermediate step in text tokens, constraining the model thoughts to the discrete vocabulary space. Recently, reasoning in continuous latent space has e...

---

### 22. [Reinforced Curriculum Pre-Alignment for Domain-Adaptive VLMs](https://arxiv.org/abs/2602.10740)

**Authors**: Yuming Yan, Shuo Yang, Kai Tang, Sihong Chen, Yang Zhang, Ke Xu, Dan Hu, Qun Yu, Pengfei Hu, Edith C. H. Ngai  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10740v1  

#### Abstract
Vision-Language Models (VLMs) demonstrate remarkable general-purpose capabilities but often fall short in specialized domains such as medical imaging or geometric problem-solving. Supervised Fine-Tuning (SFT) can enhance performance within a target domain, but it typically causes catastrophic forget...

---

### 23. [R2RAG-Flood: A reasoning-reinforced training-free retrieval augmentation generation framework for flood damage nowcasting](https://arxiv.org/abs/2602.10312)

**Authors**: Lipai Huang, Kai Yin, Chia-Fu Liu, Ali Mostafavi  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10312v1  

#### Abstract
R2RAG-Flood is a reasoning-reinforced, training-free retrieval-augmented generation framework for post-storm property damage nowcasting. Building on an existing supervised tabular predictor, the framework constructs a reasoning-centric knowledge base composed of labeled tabular records, where each s...

---

### 24. [Roughness-Informed Federated Learning](https://arxiv.org/abs/2602.10595)

**Authors**: Mohammad Partohaghighi, Roummel Marcia, Bruce J. West, YangQuan Chen  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10595v1  

#### Abstract
Federated Learning (FL) enables collaborative model training across distributed clients while preserving data privacy, yet faces challenges in non-independent and identically distributed (non-IID) settings due to client drift, which impairs convergence. We propose RI-FedAvg, a novel FL algorithm tha...

---

### 25. [Learning Mixture Density via Natural Gradient Expectation Maximization](https://arxiv.org/abs/2602.10602)

**Authors**: Yutao Chen, Jasmine Bayrooti, Steven Morad  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10602v1  

#### Abstract
Mixture density networks are neural networks that produce Gaussian mixtures to represent continuous multimodal conditional densities. Standard training procedures involve maximum likelihood estimation using the negative log-likelihood (NLL) objective, which suffers from slow convergence and mode col...

---

### 26. [Sample Efficient Generative Molecular Optimization with Joint Self-Improvement](https://arxiv.org/abs/2602.10984)

**Authors**: Serra Korkmaz, Adam Izdebski, Jonathan Pirnay, Rasmus M{\o}ller-Larsen, Michal Kmicikiewicz, Pankhil Gawade, Dominik G. Grimm, Ewa Szczurek  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10984v1  

#### Abstract
Generative molecular optimization aims to design molecules with properties surpassing those of existing compounds. However, such candidates are rare and expensive to evaluate, yielding sample efficiency essential. Additionally, surrogate models introduced to predict molecule evaluations, suffer from...

---

### 27. [TVCACHE: A Stateful Tool-Value Cache for Post-Training LLM Agents](https://arxiv.org/abs/2602.10986)

**Authors**: Abhishek Vijaya Kumar, Bhaskar Kataria, Byungsoo Oh, Emaad Manzoor, Rachee Singh  
**Category**: cs.LG  
**Published**: 2026-02-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.10986v1  

#### Abstract
In RL post-training of LLM agents, calls to external tools take several seconds or even minutes, leaving allocated GPUs idle and inflating post-training time and cost. While many tool invocations repeat across parallel rollouts and could in principle be cached, naively caching their outputs for reus...

---

### 28. [LiveMedBench: A Contamination-Free Medical Benchmark for LLMs with Automated Rubric Evaluation](https://arxiv.org/abs/2602.10367)

**Authors**: Zhiling Yan, Dingjie Song, Zhe Fang, Yisheng Ji, Xiang Li, Quanzheng Li, Lichao Sun  
**Category**: cs.AI  
**Published**: 2026-02-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.10367v1  

#### Abstract
The deployment of Large Language Models (LLMs) in high-stakes clinical settings demands rigorous and reliable evaluation. However, existing medical benchmarks remain static, suffering from two critical limitations: (1) data contamination, where test sets inadvertently leak into training corpora, lea...

---

### 29. [LATA: A Tool for LLM-Assisted Translation Annotation](https://arxiv.org/abs/2602.10454)

**Authors**: Baorong Huang, Ali Asiri  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.10454v1  

#### Abstract
The construction of high-quality parallel corpora for translation research has increasingly evolved from simple sentence alignment to complex, multi-layered annotation tasks. This methodological shift presents significant challenges for structurally divergent language pairs, such as Arabic--English,...

---

### 30. [C-MOP: Integrating Momentum and Boundary-Aware Clustering for Enhanced Prompt Evolution](https://arxiv.org/abs/2602.10874)

**Authors**: Binwei Yan, Yifei Fu, Mingjian Zhu, Hanting Chen, Mingxuan Yuan, Yunhe Wang, Hailin Hu  
**Category**: cs.CL  
**Published**: 2026-02-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.10874v1  

#### Abstract
Automatic prompt optimization is a promising direction to boost the performance of Large Language Models (LLMs). However, existing methods often suffer from noisy and conflicting update signals. In this research, we propose C-MOP (Cluster-based Momentum Optimized Prompting), a framework that stabili...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
