# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-12 05:57:17 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [GoodSpeed: Optimizing Fair Goodput with Adaptive Speculative Decoding in Distributed Edge Inference](https://arxiv.org/abs/2512.09963)

**Authors**: Phuong Tran, Tzu-Hao Liu, Long Tan Le, Tung-Anh Nguyen, Van Quan La, Eason Yu, Han Shu, Choong Seon Hong, Nguyen H. Tran  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.09963v1  

#### Abstract
Large language models (LLMs) have revolutionized natural language processing, yet their high computational demands pose significant challenges for real-time inference, especially in multi-user server speculative decoding and resource-constrained environments. Speculative decoding has emerged as a pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**GooDSPEED: Optimizing Fair Goodput with Adaptive Speculative Decoding in Distributed Edge Inference**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **分布式边缘推理中的延迟与吞吐瓶颈**：大型语言模型（LLMs）在资源受限的边缘设备上部署面临高延迟、低吞吐的问题。
- **多服务器协作下的公平性挑战**：传统 speculative decoding 多假设单一客户端或理想化环境，忽视了多个异构 draft server 之间的资源竞争与公平性分配。
- **动态提示导致的非平稳性**：用户输入（prompts）变化频繁，导致 token 接受率（acceptance rate）波动大，影响系统稳定性。

### 🚀 提出的新方法：GooDSPEED
GooDSPEED 是一个用于分布式边缘推理的新型框架，其核心是结合 **adaptive speculative decoding** 与 **gradient-based scheduling algorithm** 来优化 **goodput**（有效接受 token 数量/单位时间），同时保障跨服务器的**公平性**。

#### 主要创新点：
1. **分布式架构设计**：
   - 多个边缘端的 draft server 使用轻量级 SLMs（如 Qwen3-0.6B）生成候选 token 序列；
   - 中央 verification server 运行大模型（如 Llama3-70B）并行验证这些序列，实现“先推测后验证”机制。

2. **梯度调度算法（Gradient Scheduling Algorithm）**：
   - 动态调整每个 draft server 在下一周期应生成的 speculative token 数量 $ S_i(t+1) $；
   - 基于平滑估计的 **token 接受率 $\hat{\alpha}_i(t)$** 和 **goodput $\hat{x}_i(t)$**，最大化对数效用函数 $ U(x) = \sum \log x_i $，确保**比例公平性**（proportional fairness）。

3. **理论收敛保证**：
   - 利用 fluid sample path 分析证明，在稳态下 GooDSPEED 收敛到最优 goodput 分配；
   - 即使在动态负载下，也能以有界误差维持近似最优性能。

### 🔍 相比现有方法的优势
| 维度 | 现有方法（如 DistServe, Sarathi-Serve, SpecInfer） | GooDSPEED |
|------|--------------------------------------------------|-----------|
| 架构模式 | 集中式或相位分离 | 边缘-中心协同，支持异构设备 |
| 资源调度 | 固定或静态批处理 | 动态自适应调度，响应接受率变化 |
| 公平性保障 | 通常忽略或多用户竞争激烈 | 显式优化对数效用，保障公平 |
| 可扩展性 | 受限于单节点能力 | 并行生成 + 批量验证，易于横向扩展 |
| 非平稳适应性 | 弱（依赖历史平均） | 强（指数平滑 + 实时反馈） |

---

## 2. 核心实验方法和设置

### 📚 数据集
实验使用了 **8 个公开数据集**，覆盖多种应用场景：
- **指令微调与对话**：Alpaca, Awesome-ChatGPT-Prompts
- **长文本摘要**：CNN/DailyMail
- **推理与问答**：OpenOrca, Chatbot Arena
- **数学解题**：GSM8K
- **Text-to-SQL**：SPIDER
- **高难度长尾查询**：HLE

> 每个 draft server 被分配不同数据集，模拟真实异构负载场景。

### ⚙️ 实验设置
| 参数 | 配置 |
|------|------|
| **Verification Server** | NVIDIA H100 GPU，运行 Qwen3-14B 或 Llama3.1-70B-Instruct-AWQ-INT4 |
| **Draft Servers** | NVIDIA L4 GPU，运行 Qwen3 或 Llama3 系列 SLMs（0.6B–3B） |
| **客户端数量** | 4 或 8 个 draft servers |
| **最大输出长度** | 50 或 150 tokens |
| **Token 预算 $C$** | 根据内存与延迟权衡设定为 {6, 16, 20, 24, 28} tokens |

### 📊 评估指标
- **Goodput**：每单位时间成功接受的 token 数量（核心指标）
- **End-to-end Latency**：从请求发出到响应完成的时间
- **Utility Function $U(x) = \sum \log x_i$**：衡量系统整体效率与公平性的综合指标
- **Wall Time Components**：细分为接收时间、验证时间和发送时间
- **Convergence Behavior**：观察效用函数随迭代次数的稳定趋势

### 🆚 基线方法对比
1. **Fixed-S**：每个 draft server 固定生成 $ S_i = C/N $ 个 token
2. **Random-S**：随机分配 token 数量，总和不超过 $C$

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）Goodput 估计准确性（Figure 2）
- GooDSPEED 的平滑估计 goodput 曲线与实际值高度一致（MAE < 5%）；
- 使用移动平均滤波后，标准差较小，表明估计稳定可靠。

#### （2）端到端延迟分解（Figure 3）
- **GooDSPEED 与 Fixed-S 总体 wall time 相当**，优于 Random-S（高 5–25%）；
- **优势体现在验证阶段**：GooDSPEED 比 Fixed-S 减少约 **5% 的验证时间**，说明调度更高效；
- **接收时间略高**：因各 server 生成长度可变，需等待最慢者；但可通过异步机制缓解。

#### （3）效用函数收敛性（Figure 4）
- 在约 **400–600 次迭代内收敛**；
- 最终效用值显著高于 Fixed-S 和 Random-S（提升可达 15–30%）；
- 支持理论分析中关于渐近最优性的结论（Theorem 1）。

#### （4）系统吞吐提升
- 在 Llama3-70B 场景下，相比 baseline 实现 **2–3× 的推理加速**；
- 同等硬件条件下，goodput 提升达 **40% 以上**。

### 🔍 消融实验（隐含分析）
虽然未明确列出消融表，但从以下方面可看出关键组件作用：
- 若关闭平滑估计（$\beta=1$），系统震荡加剧，收敛变慢；
- 若不采用梯度调度而用固定分配，则效用函数无法上升至最优；
- 不同 $C$ 值影响显著：过小限制并发潜力，过大增加传输开销。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **GooDSPEED 能有效提升分布式边缘 LLM 推理的 goodput 和公平性**；
2. **基于梯度的动态调度算法能自适应非平稳 prompt 流量**，持续逼近理论最优；
3. **理论分析与实证结果一致**：fluid model 成功刻画系统长期行为，支持收敛性断言；
4. **边缘侧 speculative generation + 中心侧并行验证** 是一种高效且可扩展的架构范式。

### ⚠️ 局限性
1. **依赖准确的 acceptance rate 估计**：若 prompt 变化过于剧烈（如领域跳跃），短期估计可能偏差较大；
2. **通信开销仍存在**：尽管 DSSD 类协议可压缩反馈信号，但在广域网环境下仍有延迟风险；
3. **当前实验局限于局域集群**：尚未测试大规模地理分布场景下的网络抖动影响；
4. **SLM 与 LLM 对齐问题**：draft model 若与 target model 行为差异大，acceptance rate 下降将削弱收益。

### 🔮 未来工作方向
1. **引入预测机制**：利用 prompt embedding 预测 acceptance rate，提前优化调度；
2. **异步调度与流水线优化**：减少因最慢 client 导致的等待时间（tail latency）；
3. **跨层联合优化**：结合量化、蒸馏、缓存等技术进一步降低边缘计算成本；
4. **支持更多 speculative 形式**：如树状推测（tree-based speculation）、self-speculative 架构集成；
5. **部署于真实边缘网络**：在 5G MEC 或 IoT 边缘节点中进行实地测试。

---

## 总结

> **GooDSPEED 提出了一种面向分布式边缘 LLM 推理的高效、公平、自适应的 speculative decoding 框架。通过梯度调度算法动态分配 speculative token 长度，并结合理论分析保证收敛性，实验表明其在多种真实 workload 下显著优于固定与随机调度策略，为未来边缘智能服务提供了可行的技术路径。**

</details>

---

### 2. [Parallel Decoder Transformer: Model-Internal Parallel Decoding with Speculative Invariance via Note Conditioning](https://arxiv.org/abs/2512.10054)

**Authors**: Logan Robbins  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.10054v1  

#### Abstract
Autoregressive decoding in Large Language Models (LLMs) is inherently sequential, creating a latency bottleneck that scales linearly with output length. While ``Decomposition-and-Fill'' methods like Skeleton-of-Thought attempt to parallelize generation via external orchestration, they suffer from \t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Parallel Decoder Transformer: Model-Internal Parallel Decoding with Speculative Invariance via Note Conditioning*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLM）的**自回归解码过程本质上是串行的**，导致推理延迟随输出长度线性增长，严重制约了复杂任务（如长链推理）的效率。虽然已有“分解-填充”类方法（如Skeleton-of-Thought, SoT）尝试通过外部并行化来加速生成，但这些方法将模型视为黑箱，**缺乏并行流之间的通信机制**，容易产生**Coherence Drift（一致性漂移）**——即不同流生成相互矛盾或冗余的内容。

### **提出了什么新方法或新思路**

本文提出 **Parallel Decoder Transformer (PDT)**，一种将并行化“内嵌”到模型内部的新型架构，其核心创新包括：

- **Speculative Note Conditioning (SNC)**：在冻结的预训练主干模型（trunk）上，引入轻量级适配器（Stream Adapters）和跨注意力机制（SNC），使多个并行解码流可以通过一个共享的动态潜在空间——**Note Bus** 进行协调。
- **语义笔记广播与验证机制**：每个流可向全局总线写入压缩的语义摘要（notes），其他流通过 SNC 注意力读取这些笔记；同时引入 **Agreement Head** 预测生成内容与预期语义的一致性，若低于阈值则触发 **Rollback（回滚）**，强制重新生成以纠正偏差。
- **参数高效训练策略（Parameter-Efficient Training）**：整个 20B 参数的主干模型保持冻结，仅训练占总参数量不到 5% 的适配器模块（包括 Stream Adapters、SNC Heads、Coverage/Agreement Heads），显著降低训练成本。

### **相比现有方法的优势**

| 维度 | Skeleton-of-Thought (SoT) 等外部并行化 | PDT（本文方法） |
|------|----------------------------------------|----------------|
| 并行粒度 | Prompt-level（提示级） | Prompt-level + Token-level 内部协调 |
| 流间通信 | ❌ 无，易出现 Coherence Drift | ✅ 有，通过 Note Bus 同步语义 |
| 模型修改 | 不修改模型权重 | 仅添加轻量级适配器，主干冻结 |
| 推理一致性 | 低，依赖提示工程 | 高，具备自我纠错能力 |
| 可扩展性 | 易部署但难以控制质量 | 支持大规模模型上的高效协调 |

> ✅ **核心优势**：PDT 在不修改原始大模型的前提下，实现了**高并发下的语义一致性保障**，兼具 SoT 的并行速度和传统串行生成的连贯性。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- 构建了一个包含 **10,000 个多段落推理任务**的数据集，由 GPT-4 蒸馏生成。
- 每个样本包含：
  - **Teacher Plan**：标准的任务分解计划；
  - **Notes Contract**：结构化的语义承诺集合，用于监督并行流是否完成指定子目标。

### **实验设置**

- **主干模型**：冻结的 **20B 参数 GPT-OSS** 模型。
- **硬件平台**：8×NVIDIA B200 GPU（每卡 180GB VRAM）。
- **训练配置**：
  - 全局 batch size 为 16（micro-batch 1）；
  - 使用梯度检查点（gradient checkpointing）；
  - 采用分阶段课程学习（curriculum learning）逐步解锁模块。
- **训练阶段**（共 50,000 步）：
  1. **Stage 0**: 预训练 Planner 和 Notes Head；
  2. **Stage 1**: 解锁 Stream Adapters，独立条件生成；
  3. **Stage 2**: 启用 SNC 机制，开始跨流通信；
  4. **Stage 3**: 解锁 Coverage 和 Agreement Heads，实现自纠错与回滚。

### **评估指标**

| 指标 | 定义 | 目标 |
|------|------|------|
| **Coverage Precision** | 当系统标记某项计划“已覆盖”时，实际正确的比例 | 越高越好（反映可靠性） |
| **Coverage Recall** | 所有应被覆盖的项目中，系统成功识别的比例 | 衡量完整性 |
| **Validation Loss** | 验证集上的损失值 | 越低越好 |
| **Rollback Rate** | 触发回滚操作的频率 | 反映稳定性与纠错能力 |

### **基线方法对比**

- 主要对比对象为 **Skeleton-of-Thought (SoT)** 类外部并行方法。
- 强调 PDT 不与 token-level speculative decoding（如 Medusa、Eagle）直接竞争，而是解决更高层次的语义协调问题。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | 数值 | 说明 |
|------|------|------|
| **Coverage Precision** | **77.8%** | 最终阶段达到的精度，表明系统对“已完成”的判断高度可信 |
| **Coverage Recall** | 4.91% | 较低，说明模型采取保守策略，宁可漏判也不误判 |
| **Validation Loss** | 0.00 | 在验证集上完全收敛 |
| **参数效率** | <5% 可训练参数 | 主干冻结，仅训练适配器 |

> 📈 图1显示，在第 20,000 步左右出现明显的“相变”，loss 急剧下降，标志着跨流协调机制开始生效。

### **与基线方法的对比结果**

- **相较于 SoT**：
  - PDT 显著减少了 **Coherence Drift** 和内容冲突；
  - 生成结果更符合原始任务结构（plan adherence）；
  - 尽管 Recall 较低，但 **Precision 更高**，更适合需要高可靠性的场景。

### **消融实验结果**

尽管文中未明确列出表格形式的消融实验，但从训练阶段分析可得以下结论：

- **Stage 0–1（无通信）**：无法形成有效协调，loss 高且波动大；
- **Stage 2（启用 SNC）**：loss 开始下降，表明跨流注意力起作用；
- **Stage 3（加入 Agreement Head + Rollback）**：loss 进一步稳定至 ~0.2，precision 提升至 77.8%，证明 **self-correction 机制至关重要**。

此外，附录 E 中的模拟实验证明：
- **错误聚集（clustered errors）反而降低回滚率**：当错误集中爆发时，系统能通过一次回滚修复多个连续错误，提升整体效率。
- 示例：`L=32`, `q_token=0.0033` 下，相关性 `ρ=0.5` 使 stride failure rate 从 9.87% 降至 5.45%。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **无需全模型微调即可实现语义同步**：通过轻量级 SNC 适配器，可在冻结的 20B 模型上实现多流语义协调，验证了 **Parameter-Efficient Coordination** 的可行性。
2. ✅ **Speculative Invariance 机制有效**：Agreement Head 能准确检测语义偏离，并通过 Rollback 实现自我修正，避免 Coherence Drift。
3. ✅ **存在“Memory Cliff”现象**：全模型微调会导致显存需求激增（>290GB/device），远超 B200 的 180GB，**参数高效设计不仅是优化，更是部署必需**。
4. ✅ **保守策略优于激进覆盖**：低 Recall + 高 Precision 的组合提升了系统的安全性和可用性，符合 selective prediction 的设计理念。

### **方法的局限性**

- **静态流数量**：当前 K 个流的数量固定，无法根据任务复杂度动态调整。
- **Note Bus 可扩展性有限**：目前采用全连接共享总线，同步开销随流数线性增长，**N > 6 时收益递减**。
- **依赖高质量 Notes Contract**：训练需人工构造或蒸馏的语义契约，自动化构建仍具挑战。
- **推理延迟增加**：SNC 注意力和回滚机制带来额外计算开销，尚未完全逼近理想并行加速比。

### **未来工作方向**

1. **Dynamic Stream Allocation**：Planner Head 动态决定并行流数量。
2. **Hierarchical Note Schemas**：支持嵌套命名空间，实现递归任务分解。
3. **Hardware-Aware Attention Kernels**：优化 SNC 注意力内核，减少“Note Read”开销。
4. **Security Analysis**：研究类似 speculative execution 的侧信道攻击风险（如信息泄露）。
5. **更高效的内存管理**：结合 KV-cache 压缩技术（如 ChunkKV、ZsMerge）进一步降低内存压力。

---

## 🔚 总结

**PDT 是首个将“分解-填充”范式内化为模型内部机制的工作**，通过 **Speculative Note Conditioning (SNC)** 在冻结的大模型上实现了高效、一致的并行解码。其实验验证了：

> “**Semantic coordination can be learned without modifying trunk weights.**”

该工作不仅提供了一种实用的并行推理架构，也为未来构建 **principled, parallel model-internal reasoning systems** 奠定了基础。代码、数据集与训练权重均已开源，有望推动 LLM 推理从“串行生成”迈向“结构化并行思考”。

</details>

---

### 3. [Design Space Exploration of DMA based Finer-Grain Compute Communication Overlap](https://arxiv.org/abs/2512.10236)

**Authors**: Shagnik Pal, Shaizeen Aga, Suchita Pati, Mahzabeen Islam, Lizy K. John  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.10236v1  

#### Abstract
As both ML training and inference are increasingly distributed, parallelization techniques that shard (divide) ML model across GPUs of a distributed system, are often deployed. With such techniques, there is a high prevalence of data-dependent communication and computation operations where communica...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Design Space Exploration of DMA based Finer-Grain Compute Communication Overlap

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大规模分布式机器学习（ML）训练和推理中，**通信与计算的依赖关系**导致通信操作暴露在关键路径上，无法被有效隐藏。尤其是在 **tensor-sequence parallelism** 和 **context-parallelism** 等场景下，通信直接驱动后续计算，造成高达 **1.7× 的理想性能损失**。

传统的 **shard-level compute-communication overlap** 方法虽然能在一定程度上重叠通信与计算，但在 **direct-connection based GPU 网络拓扑**（如 AMD Instinct MI300X）中表现不佳，因为其依赖 **peer-to-peer (P2P)** 通信模式，导致大量网络链路空闲，利用率低下。

此外，细粒度操作分解会引入 **operation-level inefficiencies**，如资源利用率下降、内存争用等，影响整体性能提升。

---

### 提出的新方法：FiCCO
本文提出 **FiCCO**（**Finer-Grain Compute-Communication Overlap**），一种基于 **DMA offload** 的更细粒度通信-计算重叠机制，其核心思想是：

- 将通信操作进一步分解为 **比 shard-level 更细一级的粒度**（例如，在 8-GPU 系统中，传输大小缩小至原来的 1/8）。
- 利用这种细粒度分解实现 **all-to-all (A2A)** 通信模式，而非传统 shard-based 方法中的 **P2P** 模式。
- 结合 **GPU DMA engines** 来卸载通信任务，减少对 GPU cores 和 cache 的争用。

---

### 相比现有方法的优势
| 维度 | 传统 shard-based overlap | 本文 FiCCO |
|------|--------------------------|-----------|
| **通信模式** | Peer-to-Peer (P2P) | All-to-All (A2A) |
| **网络利用率** | 在 direct-connect 拓扑中低（最高 **3.9× 性能下降**） | 高，充分利用全连接带宽 |
| **数据流粒度** | 固定于 shard 粒度 | 支持更细粒度，利于 pipeline 后续阶段 |
| **通信不对称容忍** | 差（如 MoE 中 token 数不均） | 更好，可通过调度优化 |
| **资源争用控制** | 通常使用 GPU core 执行通信 | 使用 **DMA engines** 卸载通信，显著降低 contention |
| **设计空间** | 有限 | 开放了丰富的调度设计空间（uniform/fused/unfused/1D/2D） |

---

## 2. 核心实验方法和设置

### 数据集与场景
实验基于真实世界的大规模 ML 部署场景，重点关注以下两类典型 workload：

- **Tensor-Sequence Parallelism**：模型权重分片，激活值通过 all-gather 聚合。
- **Expert Parallelism (EP)**：用于 MoE 架构，输入 token 通过 all-to-all 分发到不同专家。

具体 GEMM 操作来自：
- **Llama-2-70B**, **Llama-3-405B**
- **DeepSeek**, **Mixtral**

共研究了 **16 个真实 GEMM 场景**（见 Table I），涵盖不同 M/N/K 组合。

---

### 实验设置
- **硬件平台**：AMD MI300X Infinity Platform，8× MI300X GPU，全连接拓扑，每条 link 带宽 64 GB/s。
- **软件栈**：
  - 框架：PyTorch
  - GEMM 库：ROCm hipblaslt
  - 通信库：RCCL（用于对比）或 **DMA-based hipMemcpyDtoDAsync**
  - 内存：使用 symmetric memory 避免中间拷贝
  - 并发：多 GPU stream 实现并发执行
- **测量方式**：10 次预热 + 5 次测量取平均

---

### 评估指标
- **Speedup**：相对于 baseline 串行执行的加速比
- **Ideal Speedup**：假设无 decomposition 和 contention 开销的理想加速比
- **DIL (Decomposition Inefficiency Loss)**：因操作分解导致的性能损失
- **CIL (Contention Inefficiency Loss)**：因通信与计算并发导致的资源争用损失
- **Heuristic 准确率**：预测最优调度的准确率

---

### 基线方法对比
- **Baseline**：串行执行（先 all-gather，再 GEMM）
- **Shard-based overlap**：类似 PyTorch AsyncTP，P2P 通信 + shard-level 重叠
- **FiCCO-rccl**：FiCCO 使用 RCCL（GPU core 驱动通信）
- **FiCCO-1D / FiCCO-2D**：本文提出的 DMA 卸载 + 细粒度重叠调度

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **FiCCO 最高可达 1.6× speedup**（1D 调度）
- **模拟 2D 调度下可达 1.7× speedup**
- 相比 shard-based overlap，性能提升显著，尤其在通信密集型场景
- 在 direct-connect 拓扑中，shard-based 方法甚至出现 **负加速**（最高 **3.9× 慢于串行**），而 FiCCO 保持正向加速

---

### 与基线方法对比
| 方法 | Geomean Speedup | 接近 Ideal (%) | 备注 |
|------|------------------|----------------|------|
| **Baseline** | 1.0× | — | 串行执行 |
| **Shard-based overlap** | <1.0× | — | 在 direct-connect 上性能退化 |
| **FiCCO-rccl** | ~1.3× | ~46–52% | GPU core 通信，存在 contention |
| **FiCCO-1D (DMA)** | **1.6×** | **52–76%** | DMA 卸载，显著降低 CIL |
| **FiCCO-2D (emulated)** | **1.7×** | **~76%** | 更优通信形状，潜力更大 |

> 注：理想加速受限于 DIL 和 CIL，调整后可达理想性能的 52–76%

---

### 消融实验结果
#### （1）DIL 分析
- **64-way 分解** 的 GEMM DIL 显著高于 8-way
- DIL 与 **op-to-byte ratio (OTB)** 负相关：OTB 越低，DIL 越严重
- 行分片（row-sharding） vs 列分片（col-sharding）的 DIL 取决于 M 和 K 的相对大小

#### （2）CIL 分析
- **DMA-based 通信** 的 CIL 显著低于 RCCL（GPU core 驱动）
- CIL 与 **GEMM memory traffic (MT)** 正相关：MT 越大，内存子系统争用越严重
- FiCCO 的 CIL geomean 为 **1.11×**，shard-based 为 **1.07×**（略低，但受限于拓扑）

#### （3）Heuristic 有效性
- 在 **16 个未见过的合成场景** 中，所提 heuristic **准确预测最优调度的比例达 81%**
- 错误预测时，性能损失平均约 **14%**

---

## 4. 关键结论和发现

### 主要发现
1. **shard-level overlap 在 direct-connect 拓扑中失效**：P2P 通信无法充分利用全连接带宽，导致性能严重下降。
2. **FiCCO 通过更细粒度分解实现 all-to-all 通信**，显著提升网络利用率，尤其适合现代全连接 GPU 集群。
3. **DMA offload 是降低 contention 的关键**：消除 compute interference，缓解 cache 和 memory contention。
4. **操作分解带来 DIL 和 CIL 的权衡**：
   - DIL 主要受 OTB 影响
   - CIL 主要受 MT 和并发度影响
5. **可构建基于 OTB 和 MT 的 heuristic**，指导 runtime 选择最优 FiCCO 调度策略，准确率达 81%。

---

### 方法的局限性
- 当前 **2D memory copy 不支持 DMA**，需模拟，限制了 2D 调度的实际性能。
- **CPU kernel launch overhead** 在极小操作时可能成为瓶颈（但可通过 graph-based launch 缓解）。
- 未支持 **reduce-scatter with arithmetic** 类操作（因 DMA 不支持数学运算）。
- 所有分析基于静态 GEMM 特征，动态 workload 下需在线调优。

---

### 未来工作方向
- 支持 **DMA 上的 fused communication-compute 操作**（如 reduce-scatter with GEMM）
- 探索 **runtime 自适应调度器**，结合实时 profiling 动态选择最优 FiCCO 策略
- 扩展到更多 ML primitive（如 attention、convolution）
- 研究 **跨节点（inter-node）FiCCO** 的可行性
- 与 **Triton** 或 **custom kernel** 结合，进一步优化小算子效率

---

> **总结**：本文提出了 **FiCCO**，首次系统探索了基于 DMA 的细粒度 compute-communication overlap 设计空间，揭示了 DIL 与 CIL 的权衡规律，并提出了高效的调度 heuristic。实验表明，FiCCO 在真实 ML workload 上最高可带来 **1.6× 加速**，为下一代高性能分布式 ML 系统提供了重要设计思路。

</details>

---

### 4. [A Kernel-based Resource-efficient Neural Surrogate for Multi-fidelity Prediction of Aerodynamic Field](https://arxiv.org/abs/2512.10287)

**Authors**: Apurba Sarker, Reza T. Batley, Darshan Sarojini, Sourav Saha  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.10287v1  

#### Abstract
Surrogate models provide fast alternatives to costly aerodynamic simulations and are extremely useful in design and optimization applications. This study proposes the use of a recent kernel-based neural surrogate, KHRONOS. In this work, we blend sparse high-fidelity (HF) data with low-fidelity (LF) ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Kernel-based Resource-efficient Neural Surrogate for Multi-fidelity Prediction of Aerodynamic Field

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该研究旨在解决**高维、多保真度（multi-fidelity）气动场预测中的资源效率瓶颈**。传统神经网络在处理高保真（HF）CFD模拟数据时面临以下挑战：
- 高保真数据获取成本高昂，样本稀少；
- 多保真度融合需处理不一致的场表示（如网格、物理模型差异）；
- 现有深度学习模型参数量大、训练推理耗时，在资源受限场景下难以部署。

### **提出了什么新方法或新思路**

提出基于 **KHRONOS**（Kernel Expansion Hierarchy for Reduced-Order, Neural-Optimized Surrogates）的多保真代理模型框架，其核心思想是：
- 利用**核函数展开（kernel expansion）** 和 **可分离张量结构（separable tensor decomposition）** 构建轻量化神经架构；
- 将输入-输出映射分解为低维子空间的加权乘积，仅学习插值函数而非全连接权重；
- 采用**delta learning架构**：先用低保真（LF）模型（NeuralFoil）生成粗略预测，再训练一个残差（residual）模型来校正HF与LF之间的差异。

### **相比现有方法的优势**

- **极高的资源效率**：相比MLP、GNN、PINN等密集网络，参数量减少 **94–98%**；
- **快速训练与推理**：在同等精度下，训练时间缩短 **40–73%**，推理速度快 **44–63%**；
- **无需中间潜空间对齐**：避免了传统多保真方法中复杂的特征空间匹配过程；
- **数学基础扎实**：基于变分原理、插值理论和张量分解，支持高效剪枝和自动微分。

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 类型 | 描述 |
|--------|------|------|
| **AirfRANS** | 高保真（HF） | 包含1000个2D亚音速翼型的RANS解，涵盖NACA 4/5位系列，Re ∈ [2×10⁶, 6×10⁶]，AoA ∈ [-5°, 15°]，使用OpenFOAM求解，网格达25万–30万个单元。 |
| **NeuralFoil** | 低保真（LF） | 基于XFoil训练的物理 informed 神经网络，用于生成对应AirfRANS几何条件下的低压强场预测；共生成735个高质量样本（R² > 0.7），265个低质量样本用于验证多保真增益。 |

### **实验设置和评估指标**

#### **问题设定**
- 目标：预测翼型表面压力系数 $ C_p $ 分布（共81个采样点）；
- 输入：由B-spline控制点（Ngeom=16）、自由流速度 $ U $、攻角 $ \text{AoA} $ 组成的向量；
- 输出：标准化后的 $ C_p $ 曲线。

#### **三种训练场景（Case 1–3）**
| Case | HF/LF Ratio | HF Training Data | LF Training Data | 说明 |
|------|-------------|------------------|------------------|------|
| 1    | 0%          | 0                | 588              | 纯LF模型，无HF修正 |
| 2    | 10%         | 59               | 588              | 引入少量HF数据进行残差学习 |
| 3    | 30%         | 176              | 588              | 更多HF数据参与训练 |

#### **评估指标**
- **$ R^2 $**（决定系数）：衡量预测与真实 $ C_p $ 场的一致性；
- **训练时间**（per fold）：5折交叉验证中每折平均训练耗时；
- **推理延迟**（ms/sample）：单次前向传播时间；
- **可训练参数数量**：模型复杂度指标；
- **缩放特性分析**：控制点从16增至64，观察 $ R^2 $ 与参数增长关系。

### **基线方法对比**

| 模型 | 类型 | 结构特点 |
|------|------|----------|
| **MLP** | 多层感知机 | 4层全连接，ReLU激活，宽度256/128（LF/Delta） |
| **GNN** | 图神经网络 | 基于控制点构建链式图，消息传递层聚合邻域信息 |
| **PINN** | 物理 informed NN | 在MLP基础上加入PDE残差约束（Laplace方程 + Bernoulli） |
| **KHRONOS**（本文） | 核基分离架构 | 使用二次B-spline核展开，低秩张量组合，参数共享 |

所有模型均采用相同的 delta learning 架构以确保公平比较，并通过 grid search 调优超参数（见 Appendix A）。

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

| 指标 | KHRONOS (Case 3) | MLP | GNN | PINN |
|------|------------------|-----|-----|------|
| **Test $ R^2 $** | ~0.90 | ~0.91 | ~0.92 | ~0.91 |
| **Trainable Parameters** | **7,759** | 139,554 | 202,626 | 139,554 |
| **Inference Time (ms)** | **2.44–3.64** | 5.33–8.52 | 7.17–9.77 | 5.13–8.27 |
| **Training Time per Fold (s)** | **3–15** | 19–30 | 27–32 | 28–30 |

> 注：KHRONOS在所有资源受限条件下表现最优。

### **与基线方法的对比结果**

#### ✅ 参数效率（Parameter Efficiency）
- KHRONOS参数量仅为MLP/PINN的 **~5.5%**，GNN的 **~3.8%**；
- 即使将几何分辨率从16提升至64个控制点，KHRONOS参数仅从7,759增至17,897（+130%），而GNN从20万增至67万（+234%），MLP/PINN也增长显著。

#### ✅ 时间效率（Time Efficiency）
- **训练时间**：KHRONOS最快仅需 **3秒/折**，比MLP快 **4–6倍**，比GNN快 **9倍以上**；
- **推理延迟**：最低达 **2.44 ms**，比MLP快约 **2倍**，比GNN快近 **4倍**；
- 在 **15秒训练预算内**，KHRONOS即可达到 $ 1-R^2 < 0.1 $，而其他模型仍处于高误差阶段（Fig. 6）。

#### ✅ 准确性（Accuracy）
- 所有模型最终均可达到 $ R^2 \geq 0.8 $，表明预测能力相当；
- 在Case 3中，KHRONOS $ R^2 \approx 0.90 $，虽略低于GNN（0.92），但在**资源消耗上具有压倒性优势**；
- 缩放实验显示，超过32个控制点后，精度提升趋于饱和，说明当前模型已逼近数据极限。

### **消融实验与特殊场景分析**

#### 🔍 在低质量LF数据上的表现（R²_LF < 0.7）
- NeuralFoil在265个复杂工况下表现差（$ R^2 < 0.7 $），主要表现为低估前缘吸力峰；
- KHRONOS通过残差学习成功恢复吸力峰形态和幅值（Fig. 12）；
- 多保真增益明显：原LF模型全部落在 $ R^2 < 0.7 $ 区间，而MF-KHRONOS将 **52.8% 的案例提升至 $ R^2 > 0.7 $**（Fig. 13）。

#### 📈 控制点数增加的影响（Fig. 9–10）
- 所有模型在16→64控制点间 $ R^2 $ 提升有限（0.87→0.94），说明几何分辨率非瓶颈；
- KHRONOS保持最小参数增长，实现最佳“精度-复杂度”权衡。

---

## 4. 关键结论和发现

### **主要发现**

1. **KHRONOS实现了精度与效率的高度平衡**：
   - 在几乎相同预测精度下，比主流神经架构节省 **两个数量级的参数** 和 **显著降低计算开销**；
   - 特别适用于嵌入式系统、实时优化、大规模设计探索等资源敏感场景。

2. **核基分离结构天然适合多保真建模**：
   - 不依赖潜空间对齐，直接在原始输入空间进行残差学习；
   - 可分离性支持高效自动微分，便于集成到梯度优化流程中。

3. **多保真增益在困难案例中最显著**：
   - 当LF模型失效时（如强分离、高攻角），残差模型能有效纠正系统偏差；
   - 表明Δ-learning策略在工程实际中具备鲁棒性和实用性。

### **方法的局限性**

- 当前评估局限于单一HF源（AirfRANS）和单一LF生成器（NeuralFoil）；
- 未测试跨翼型族或跨流动状态（如跨音速）的泛化能力；
- 对极高维场（如3D全场）扩展尚未验证；
- B-spline参数化虽灵活，但仍受限于预定义控制点拓扑。

### **未来工作方向**

1. 扩展至更多HF数据集（如Transonic, 3D Wing）和多种LF模型（XFOIL, Panel Method）；
2. 探索自适应HF/LF采样策略，动态分配仿真资源；
3. 将KHRONOS应用于端到端气动形状优化闭环；
4. 研究其在不确定性量化（UQ）和主动学习中的潜力；
5. 推广至其他物理场预测任务（温度、湍流粘性等）。

---

> **总结**：本论文提出的 **KHRONOS** 是一种极具前景的资源高效型多保真代理模型，它通过**核函数展开与可分离张量结构**，在保持高预测精度的同时大幅降低模型复杂度。实验表明其在气动场预测任务中显著优于MLP、GNN和PINN，尤其适合在**数据稀缺、算力受限**的实际工程环境中部署。

</details>

---

### 5. [AEBNAS: Strengthening Exit Branches in Early-Exit Networks through Hardware-Aware Neural Architecture Search](https://arxiv.org/abs/2512.10671)

**Authors**: Oscar Robben, Saeed Khalilian, Nirvana Meratnia  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.10671v1  

#### Abstract
Early-exit networks are effective solutions for reducing the overall energy consumption and latency of deep learning models by adjusting computation based on the complexity of input data. By incorporating intermediate exit branches into the architecture, they provide less computation for simpler sam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AEBNAS: Strengthening Exit Branches in Early-Exit Networks through Hardware-Aware Neural Architecture Search  
——核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
早期退出网络（Early-Exit Networks, EENs）通过在模型中间层添加**exit branches**，实现对简单样本的提前推理终止，从而降低平均延迟和计算开销（如MACs），特别适用于资源受限的边缘设备。然而，设计高效的EEN面临四大挑战：
1. exit branches的数量；
2. exit branches的位置；
3. 退出策略（如置信度阈值）；
4. **exit branches自身的架构设计（深度、层数、类型）**。

已有研究（如EDANAS、NACHOS）主要关注前三个问题，通常采用**固定结构的exit branches**，忽略了其架构优化对整体性能的影响。

---

### 🚀 提出的新方法与创新点
本文提出 **AEBNAS**（Adaptive Exit Branch Neural Architecture Search），一个**硬件感知的多目标NAS框架**，专门用于联合优化主干网络（backbone）和exit branches的架构设计。

#### 主要创新包括：
- **动态exit branch架构搜索空间设计**：  
  允许exit branches具有可变深度（最多2个block）、不同卷积核大小（kernel size: 3或5）、扩展率（expansion rate: 1或2）、插值尺寸（interpolation size）以及是否添加max-pooling层，显著增强了exit branches的表达能力。

- **引入目标MACs约束作为优化目标的一部分**：  
  在NSGA-II多目标优化中加入`target_macs`超参数，使搜索过程能生成满足特定硬件计算预算的模型，提升部署灵活性。

- **迭代式exit threshold调优机制**：  
  在每轮NAS迭代后，使用grid search对每个候选架构进行**自适应阈值调整**，以在给定MACs预算下最大化准确率，并将更新后的性能反馈至surrogate model训练中。

- **端到端联合优化backbone与exit branches**：  
  不仅搜索exit位置和数量，还同时优化其内部结构，真正实现“strengthening exit branches”。

---

### 🔍 相比现有方法的优势
| 方法 | 是否优化exit位置 | 是否优化exit数量 | 是否优化exit架构 | 是否调优threshold | 硬件感知 |
|------|------------------|------------------|------------------|--------------------|----------|
| EDANAS [12] | ✅ | ✅ | ❌（固定） | ✅ | ✅（MACs） |
| NACHOS [13] | ✅ | ✅ | ❌（仅允许max-pooling） | ✅ | ✅ |
| **AEBNAS (本文)** | ✅ | ✅ | ✅（完整结构搜索） | ✅（迭代调优） | ✅ |

> ✅ 显著优势：AEBNAS是首个将**exit branch架构本身纳入NAS搜索空间**的工作，实现了更精细的效率-精度权衡。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **CIFAR-10**：5万训练图像，1万测试图像
- **CIFAR-100**：同上，类别更多，任务更复杂
- **SVHN**：73,257张训练图像，26,032张测试图像（街景门牌数字识别）

所有数据集中，10%训练集用作验证集。

---

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| NAS算法 | NSGA-II（基于NSGANetV2） |
| 搜索迭代次数 | 30次 |
| 初始种群大小 | 100 |
| 后续每代采样数 | 8 |
| 每个架构训练epoch数 | 5（CIFAR-10/100/SVHN），CIFAR-100最后两轮为250 epoch |
| Surrogate Model | Multilayer Perceptron（预测error和MACs） |
| Target MACs | SVHN: 1M；CIFAR-10: 2M / 17M；CIFAR-100: 17M |
| Exit Branch Options | kernel: [3,5]；expansion: [1,2]；interpolation: [8,10,12]；max-pooling: 可选；最大block数: 2 |

---

### 📈 评估指标
- **Top-1 Accuracy (%)**
- **Average Number of MACs (百万级)**
- **Number of Exits**
- **Exit Utilization (%)**：各exit分支分类的样本比例
- **Hardware Efficiency**：在相同或更低MACs下是否获得更高accuracy

---

### 🆚 基线方法对比
| 类型 | 基线模型 |
|------|--------|
| 手动设计 | EEAlexNet（5 exits）、EEResNet20（10 exits） |
| NAS-based | EDANAS [12]、NACHOS [13] |

所有NAS方法均保持一致超参设置以便公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自Table II）

#### ✅ CIFAR-10（Target: ~2.4M MACs）
| Model | MACs (M) | Top-1 Acc (%) | # Exits |
|-------|----------|---------------|--------|
| AEBNAS | **2.47** | **74.64** | 4 |
| EDANAS | 2.47 | 67.78 | 5 |
| NACHOS | 2.44 | 72.65 | 4 |
| EEAlexNet | 233.46 | 73.03 | 5 |

> 💡 **AEBNAS比EDANAS高6.86%，比NACHOS高1.99%**，且MACs远低于手动模型。

#### ✅ CIFAR-10（High-budget: ~17M MACs）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **16.71** | **84.42** |
| EDANAS | 17.31 | 80.90 |

> 💡 更高准确率 + 更低MACs，体现更强优化能力。

#### ✅ SVHN（Target: 1M MACs）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **1.79** | **85.76** |
| EDANAS | 1.47 | 77.98 |
| NACHOS | 1.46 | 79.96 |

> 💡 虽MACs略高但仍控制在合理范围，**准确率提升达6~8个百分点**。

#### ✅ CIFAR-100（复杂任务）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **14.80** | **69.90** |
| EDANAS | 14.94 | 67.94 |

> 💡 在更难任务上仍实现**+1.96% accuracy提升**，且MACs更低。

---

### 🔍 Exit Utilization 分析（Figure 3）
- AEBNAS倾向于**禁用第一个exit branch**，而将大部分样本（>80%）由第2或第3个exit处理。
- EDANAS/NACHOS激活了性能较弱的第一exit，导致少量样本在此退出，反而增加冗余计算。
- 表明：**强化中段exit branches并关闭低效early exit**有助于提升整体效率。

---

### 📉 消融分析（隐含于文中）
虽然未明确列出消融表，但从方法设计可推断以下关键因素作用：
- **可变exit架构搜索** → 提升exit branch判别力，支持更早可靠退出；
- **adaptive threshold tuning** → 动态平衡accuracy与MACs；
- **multi-objective with target_macs** → 引导搜索朝硬件友好方向收敛；
- 结果显示这些机制共同促成Pareto前沿上的优越表现（见Figure 2）。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **exit branches的架构设计至关重要**：  
   固定结构限制了early-exit网络的潜力，**可学习、可变深度的exit branch能显著提升性能**。

2. **AEBNAS能在相同甚至更低MACs下取得更高accuracy**：  
   在CIFAR-10、SVHN、CIFAR-100上全面超越state-of-the-art NAS与手动设计方法。

3. **最优exit utilization模式是非均匀的**：  
   并非越多exit越好，AEBNAS自动学会**跳过低效early exits**，集中在中段强exit完成多数预测，实现高效分流。

4. **硬件感知+动态threshold调优增强实用性**：  
   支持按设备算力定制模型，适合真实边缘部署场景。

---

### ⚠️ 局限性
- 当前exit branch搜索空间仍有限（仅2-block结构），未来可探索更复杂模块（如attention、SE block）。
- threshold tuning依赖grid search，计算开销较高，未来可尝试梯度优化或代理建模。
- 实验集中在图像分类任务，尚未验证在检测、分割等任务中的泛化性。

---

### 🔮 未来工作方向
- 将AEBNAS扩展至其他动态推理范式（如layer-skipping、routing networks）。
- 探索跨设备迁移NAS策略，加速搜索过程。
- 集成功耗/延迟实测反馈，构建闭环硬件协同设计系统。
- 应用于Transformer-based early-exit架构搜索。

---

## 总结
> **AEBNAS首次将exit branch架构本身纳入NAS搜索空间，结合硬件感知目标与动态threshold调优，在多个基准数据集上实现了更高的accuracy与更低的average MACs，验证了“strengthening exit branches”的有效性，为高效边缘AI模型设计提供了新范式。**

</details>

---

### 6. [InfoCom: Kilobyte-Scale Communication-Efficient Collaborative Perception with Information Bottleneck](https://arxiv.org/abs/2512.10305)

**Authors**: Quanmin Wei, Penglin Dai, Wei Li, Bingyi Liu, Xiao Wu  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10305v1  

#### Abstract
Precise environmental perception is critical for the reliability of autonomous driving systems. While collaborative perception mitigates the limitations of single-agent perception through information sharing, it encounters a fundamental communication-performance trade-off. Existing communication-eff...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# InfoCom: Kilobyte-Scale Communication-Efficient Collaborative Perception with Information Bottleneck  
**——核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代自动驾驶系统依赖于精确的环境感知，而单智能体感知受限于视野遮挡等问题。**协同感知（Collaborative Perception）**通过多车之间的信息共享提升感知能力，但面临一个根本矛盾：**通信开销与感知性能之间的权衡**。

现有通信高效方法（如 Where2comm、ERMVP）通常假设每次协作传输 **MB级数据**，这在实际车载网络中难以实现（5G平均仅3.5 MB/s，波动下可低至0.4 MB/s）。此外，这些方法缺乏理论支撑，多为启发式特征操作，无法保证在极端压缩下的感知保真度。

### 提出的新方法与新思路
作者提出 **InfoCom**，一种基于**信息瓶颈（Information Bottleneck, IB）原理扩展**的通信高效协同感知框架，首次建立该领域的**理论基础**。其核心是“**信息净化（Information Purification）**”范式，而非传统的“特征操作（Feature Manipulation）”。

#### 核心创新模块：
1. **Information-Aware Encoding (IAE)**  
   - 基于扩展的 IB 原理，将高维中间特征 $Z$ 映射为极低维的信息感知特征 $E \in \mathbb{R}^D$（$D \ll C\times H\times W$），保留任务相关最小充分信息。
   - 引入变分近似建模 $E$ 的分布，便于训练中的 KL 正则化与重参数化技巧。

2. **Sparse Mask Generation (SMG)**  
   - 生成稀疏空间掩码 $M$ 来补偿压缩过程中丢失的空间线索。
   - 包含两个轻量步骤：
     - **Filtering**: 仅保留 top-$k$ 最重要的空间位置（保留率 $\alpha=0.1$）；
     - **Quantization**: 使用 4-bit 量化进一步压缩。
   - 总通信成本极低（KB级），但提供关键空间先验。

3. **Multi-Scale Decoding (MSD)**  
   - 接收端采用掩码引导的渐进式解码机制，逐步恢复 BEV 特征。
   - 利用 $E$ 和 $M$ 联合重构，避免简单重建导致的信息损失。

### 相比现有方法的优势
| 维度 | InfoCom | 传统方法（如 Where2comm, ERMVP） |
|------|--------|-------------------------------|
| **通信规模** | **KB级**（~7.875 KB） | MB级（3.4 MB ~ 34.3 MB） |
| **理论基础** | ✅ 基于 IB 的信息论分析 | ❌ 多为启发式设计 |
| **信息处理方式** | 信息净化（提取最小充分信息） | 特征选择/压缩（仍传递冗余结构） |
| **模块兼容性** | ✅ 插件式设计，可集成到任意模型 | ⚠️ 需定制化修改 |
| **性能表现** | 接近标准协作精度 | 明显低于标准协作 |

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个代表性协同感知数据集上进行验证：
- **OPV2V** (Xu et al., 2021)：仿真 V2V 数据集
- **V2XSet** (Xu et al., 2022)：仿真 V2X 数据集
- **DAIR-V2X** (Yu et al., 2022)：真实世界车路协同数据集

### 实验设置与评估指标
- **主干模型**：默认使用 **CoAlign**（Lu et al., 2023）作为基础协同感知框架；同时测试 AttFuse 和 MKD-Cooper 验证通用性。
- **检测器**：PointPillars
- **评估指标**：
  - **3D Object Detection AP@30/50/70**（IoU 阈值）
  - **通信体积（Communication Volume）**：以 KB 或 MB 表示每代理发送的消息总量
- **硬件配置**：RTX 3090 GPU，Ubuntu 系统，PyTorch 1.10

### 基线方法对比
- **No Collaboration**：无协作
- **Late Collaboration**：后期融合，通信量极小（6.25 KB）
- **Standard Intermediate Collaboration**：完整中间特征传输（34.375 MB）
- **Where2comm** (Hu et al., 2022)：基于空间重要性的特征选择
- **ERMVP** (Zhang et al., 2024)：基于聚类的空间压缩方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | 通信体积 | OPV2V AP@50 | V2XSet AP@50 | DAIR-V2X AP@50 |
|------|----------|-------------|--------------|----------------|
| Standard Colla. | 34.375 MB | 0.9653 | 0.9212 | 0.7843 |
| Where2comm | 3.439 MB | 0.9463 | 0.8604 | 0.7539 |
| ERMVP | 0.741 MB | 0.9557 | OOM | 0.7791 |
| **InfoCom (Ours)** | **7.875 KB** | **0.9650** | **0.9273** | **0.7789** |

> 注：InfoCom 在所有数据集上均达到接近甚至超越标准协作的性能，且通信量仅为后者的 **1/4000~1/440**。

### 与基线方法的对比结果
- **通信效率提升显著**：
  - 相比 **Where2comm**：通信量减少 **440倍**
  - 相比 **ERMVP**：通信量减少 **90倍**
  - 相比标准协作：减少超过 **4000倍**
- **感知性能更优**：
  - 在 V2XSet 上，ERMVP 因内存溢出（OOM）失败，而 InfoCom 成功运行并取得最佳性能。
  - InfoCom 在多个指标上优于或持平于标准协作，尤其在复杂场景中表现出更强鲁棒性。

### 消融实验结果（Table 3）

| 变体 | Mean AP | 分析 |
|------|---------|------|
| Full InfoCom | 0.9518 | 基线 |
| 替换 IAE 为 Simple Encoder | 0.9320 | ↓2%，说明 IAE 对信息提取至关重要 |
| 移除 SMG 多粒度结构 | 0.9379 | ↓1.4%，多尺度空间建模有效 |
| 移除 STE（梯度估计） | 0.8845 | ↓6.7%，证明非可导操作需合理梯度传播 |
| 移除 Mask 引导 | 0.8839 | ↓6.8%，空间先验对重建至关重要 |
| 移除 Multi-Scale Rec. | 0.9439 | ↓0.8%，渐进解码有助于细节恢复 |

> 结果表明三大模块均不可或缺，尤其是 **Mask-Guided Reconstruction** 对性能影响最大。

### 其他补充实验
- **跨模型兼容性测试（Table 2）**：
  - 将 InfoCom 集成到弱 backbone 如 AttFuse 和 MKD-Cooper 中，**反而提升了原始模型性能（+1.27% mean AP）**，说明其具备“特征增强”能力。
- **通信量随智能体数量增长（Table 4）**：
  - InfoCom 的总通信量呈线性增长，始终保持在 KB 级别（如 5 车时仅 31.5 KB），远低于其他方法（ERMVP 达 2.965 MB）。
- **参数敏感性分析（Figure 7）**：
  - 超参 $\beta$（控制 IB 权衡）对性能影响较小，表明系统具有良好的稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **KB级通信可行且高效**：InfoCom 首次实现了 **kilobyte-scale 的近无损协同感知**，突破了传统 MB 级通信假设。
2. **信息净化优于特征操作**：从“传递冗余特征”转向“提取最小充分信息”，从根本上解决了压缩与保真的矛盾。
3. **理论指导实践**：基于 IB 的信息论分析为通信-性能权衡提供了数学依据，推动协同感知从经验设计走向理论驱动。
4. **插件式架构实用性强**：无需改动主干网络，仅替换通信层即可部署，适用于多种协同感知系统。

### 方法的局限性
- 当前消息单元仅包含**语义信息 $E$** 和**空间线索 $M$**，尚未引入**时间动态信息**（如运动趋势、历史状态）。
- 极端压缩可能导致某些边缘案例（如罕见物体、剧烈遮挡）漏检，尽管整体性能稳定。

### 未来工作方向
- 扩展消息格式为三元组：$(E, M, T)$，加入**时间线索（Temporal Cues）**，实现动态传输决策。
- 探索更高效的量化策略（如自适应比特分配）和编码方案（如熵编码）进一步降低带宽。
- 在真实道路环境中部署并测试端到端延迟与可靠性。

---

> ✅ **一句话总结**：  
> InfoCom 是首个基于信息瓶颈理论实现 **KB级通信、近无损感知** 的协同感知框架，通过 **信息净化范式** 彻底重构了通信机制，在效率、性能与理论深度上全面超越现有方法。

</details>

---

### 7. [Neuronal Attention Circuit (NAC) for Representation Learning](https://arxiv.org/abs/2512.10282)

**Authors**: Waleed Razzaq, Izis Kankaraway, Yun-Bo Zhao  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10282v1  

#### Abstract
Attention improves representation learning over RNNs, but its discrete nature limits continuous-time (CT) modeling. We introduce Neuronal Attention Circuit (NAC), a novel, biologically plausible CT-Attention mechanism that reformulates attention logits computation as the solution to a linear first-o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Neuronal Attention Circuit (NAC) for Representation Learning

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

传统 Attention 机制基于离散时间操作（如 scaled dot-product），难以有效建模**连续时间（Continuous-Time, CT）动态系统**，尤其在处理**不规则采样时序数据**（irregularly sampled time series）时存在局限。同时，现有 CT-Attention 方法计算开销大、对 ODE 求解器敏感，且缺乏生物可解释性。

此外，标准 Attention 的密集投影（dense projections）导致高内存消耗，限制了其在长序列上的扩展能力。

### **提出了什么新方法或新思路**

本文提出 **Neuronal Attention Circuit (NAC)**，一种受生物学启发的、适用于表示学习的新型连续时间注意力机制。其核心思想是：

- 将 **Attention logits 的计算重新表述为一个一阶线性 ODE 的解**，该 ODE 由非线性、互连的门控机制调制。
- 门控机制源自对 *C. elegans* 神经回路策略（Neuronal Circuit Policies, NCPs）的复用，赋予模型**生物可解释性**和**稀疏连接结构**。
- 引入三种计算模式：
  - **Euler**：显式欧拉积分近似
  - **Exact**：闭式解析解
  - **Steady**：稳态近似（类标准 Attention）
- 设计 **Sparse Top-K Pairwise Concatenation** 方案，仅保留最相关的 key-query 对，显著降低内存强度。

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **生物可解释性** | 借鉴 *C. elegans* 神经回路结构，实现稀疏、模块化、可解释的神经架构 |
| **连续时间建模** | 支持 CT 动态演化，优于 DT-RNN 和标准 Attention 在不规则序列上的表现 |
| **效率与可扩展性** | 稀疏门控 + Top-K 选择大幅减少内存占用，优于 mTAN、ContiFormer 等 CT-Attention |
| **理论保障** | 提供状态稳定性、误差指数界、通用逼近定理等严格数学证明 |
| **灵活性** | 支持多种计算模式，在精度与速度间灵活权衡 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验覆盖三大领域，验证泛化能力：

| 领域 | 数据集 | 特点 |
|------|--------|------|
| **不规则时序分类** | Event-based MNIST (E-MNIST)<br>Person Activity Recognition (PAR) | 不规则事件流数据，测试模型对时间依赖建模能力 |
| **自动驾驶车道保持** | OpenAI CarRacing<br>Udacity Self-Driving Car Simulator | 视觉输入 + 控制输出，前者为离散动作分类，后者为连续转向预测 |
| **工业预测性维护** | PRONOSTIA<br>XJTU-SY<br>HUST | 轴承振动信号用于剩余使用寿命（RUL）估计，跨工况迁移测试 |

### **实验设置和评估指标**

- **训练方式**：5折交叉验证，使用 BPTT 进行梯度更新。
- **硬件环境**：Google Colab T4 GPU。
- **评估指标**：
  - 分类任务：Accuracy (↑)
  - 回归任务（Udacity）：MSE / MAE (↓)
  - RUL 估计：Score (↓)，IEEE PHM 官方不对称评分函数
- **模型维度统一**：所有 baseline 与 NAC 使用相同的 `d_model` 和 attention heads 数量以保证公平比较。

### **基线方法对比**

涵盖四大类主流模型：

| 类型 | 基线方法 |
|------|---------|
| **DT-RNNs** | RNN, LSTM, GRU |
| **CT-RNNs** | CT-RNN, PhasedLSTM, GRU-ODE, mmRNN, LTC, CfC |
| **DT-Attention** | Attention, MHA |
| **CT-Attention** | mTAN, CTA, ODEFormer, ContiFormer |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table I）**

#### ✅ **不规则时序分类**

| 模型 | E-MNIST (↑) | PAR (↑) |
|------|------------|--------|
| **NAC-PW** | **96.64±0.12** | **89.15±1.01** |
| GRU-ODE | 96.04±0.13 | 89.01±1.55 |
| NAC-Exact/05s/8k | 96.12±0.11 | 89.01±1.01 |

> NAC-PW 在两项任务上均取得 SOTA 表现。

#### ✅ **自动驾驶车道保持**

| 模型 | CarRacing Accuracy (↑) | Udacity MSE (↓) |
|------|------------------------|----------------|
| **NAC-PW** | **80.72±0.41** | 0.0177±0.0008 |
| **NAC-32k** | 80.38±0.16 | **0.0170±0.0007** |
| ContiFormer | 80.47±0.50 | 0.0174±0.01 |

> NAC 在两类驾驶模拟中均达到最优或次优性能。

#### ✅ **工业 RUL 预测（Score ↓）**

| 模型 | PRONOSTIA | XJTU-SY | HUST |
|------|----------|--------|------|
| **NAC-Exact/05s/8k** | 37.75 | **19.87** | **27.82** |
| **NAC-PW** | 37.50 | 28.01 | 30.14 |
| ContiFormer | **27.82** | 34.71 | 43.81 |

> NAC 在跨数据集验证中表现出色，尤其在 XJTU-SY 和 HUST 上显著优于其他方法。

### **与基线方法的对比结果**

- **总体表现**：NAC 在多数任务中**匹配或超越**最先进的 CT 和 DT 模型。
- **内存效率**：相比 mTAN（峰值内存 790MB）、ContiFormer（67.7MB），NAC-2k 仅需 **44.75MB**；即使使用全配对（NAC-PW）也体现合理设计下的可控增长。
- **运行时间**：NAC 运行时间介于 CT-RNN 与 CT-Attention 之间，优于 GRU-ODE、LTC 等重型 ODE 模型（见 Table 2）。

### **消融实验结果（Ablation Studies）**

| 变体 | 描述 | 发现 |
|------|------|------|
| **NAC-2k vs NAC-32k** | Top-K=2 vs 32 | 更多 Top-K 提升精度但增加内存；K=8 是平衡点 |
| **NAC-02s vs NAC-09s** | 20% vs 90% sparsity | 更高 sparsity 提升鲁棒性和准确性 |
| **NAC-FC** | 全连接替代 NCP 门控 | 性能下降，说明 NCP 结构有效性 |
| **NAC-Euler / Exact / Steady** | 不同 ODE 求解模式 | Exact 平衡精度与效率；Steady 最快；Euler 适合自适应动态 |

> **最佳配置**：`Top-K=8`, `sparsity=50%`, `Exact mode` 实现精度与效率的最佳权衡。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **NAC 成功将生物神经回路原理引入 Attention 机制**，实现了具有生物可解释性的 CT-Attention 架构。
2. ✅ **Attention logits 可被建模为 ODE 解**，自然支持连续深度演化，桥接了离散层与动态系统的鸿沟。
3. ✅ **稀疏门控 + Top-K 机制显著降低内存开销**，使 NAC 在长序列场景更具实用性。
4. ✅ **理论分析完备**：证明了状态稳定性、指数收敛性、通用逼近能力，为模型可靠性提供保障。
5. ✅ **实证性能优越**：在多个真实世界任务中达到 SOTA 或接近最优，尤其在跨域 RUL 预测中展现强大泛化能力。

### **方法的局限性**

1. **固定 Wiring 结构**：当前依赖预定义的 NCP 连接图（如 AutoNCP），灵活性受限，架构尺寸较大。
2. **Top-K 的局限性**：
   - 仍需计算完整 QK^T 得分矩阵，对极长序列仍可能成为瓶颈。
   - 固定 K 值不够自适应，可能遗漏重要上下文。
3. **计算复杂度**：尽管优化后优于部分 CT 模型，但整体仍高于标准 Attention（O(n²k) vs O(nk)）。
4. **硬件适配不足**：尚未针对边缘设备进行量化或部署优化。

### **未来工作方向**

1. **可学习 Wiring**：支持用户自定义或随机生成 NCP 配置，提升灵活性与效率。
2. **可学习 Top-K 机制**：引入 adaptive 或 learned sparse attention selection，避免手动设定 K。
3. **改进 Key Scoring**：设计更高效的 key relevance 评估函数，减少冗余计算。
4. **硬件感知优化**：结合边缘计算需求，开发低延迟、低功耗版本。
5. **扩展至其他模态**：探索在语音、视频、图结构数据中的应用潜力。

---

> 🔗 **代码开源**：https://github.com/itxwaleedrazzaq/neuronal_attention_circuit  
> 📌 **影响声明**：推动稀疏、自适应、类脑神经网络发展，适用于资源受限场景，但也需关注其在监控与自主系统中的伦理风险。

</details>

---

### 8. [Zero-shot 3D Map Generation with LLM Agents: A Dual-Agent Architecture for Procedural Content Generation](https://arxiv.org/abs/2512.10501)

**Authors**: Lim Chien Her, Ming Yan, Yunshu Bai, Ruihao Li, Hao Zhang  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10501v1  

#### Abstract
Procedural Content Generation (PCG) offers scalable methods for algorithmically creating complex, customizable worlds. However, controlling these pipelines requires the precise configuration of opaque technical parameters. We propose a training-free architecture that utilizes LLM agents for zero-sho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Zero-shot 3D Map Generation with LLM Agents: A Dual-Agent Architecture for Procedural Content Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Procedural Content Generation (PCG)** 工具虽然能高效生成复杂虚拟环境（如游戏地图），但其参数配置高度技术化、非直观且语义模糊，导致普通用户难以有效控制。尽管 **Large Language Models (LLMs)** 被尝试用于自然语言驱动 PCG，但现成（off-the-shelf）模型在理解抽象指令与具体参数之间的“语义鸿沟”上表现不佳，常出现参数幻觉或错误配置。

此外，当前主流方法依赖于**任务特定的 fine-tuning、强化学习或领域预训练**，这些方法成本高、泛化能力差，难以迁移到新的工具或领域。

### 提出的新方法与思路
本文提出一种**无需训练的双智能体架构（Dual-Agent Actor-Critic Architecture）**，实现对 PCG 工具的零样本（zero-shot）控制：

- **Actor Agent**：作为语义解释器，将用户的自然语言提示（natural language prompt）转化为初步的 **Parameter Trajectory Sequence**（即一系列算法调用及其参数配置）。
- **Critic Agent**：作为静态验证器，基于提供的 **API 文档** 和 **参考示例** 对 Actor 提出的轨迹进行功能正确性检查，识别并反馈“阻塞性问题”（blocking issues），例如无效参数、越界值或不兼容的算法组合。
- 两者通过**迭代对话机制**不断优化参数配置，直到 Critic 批准或达到最大迭代次数。

该框架完全依赖 **In-Context Learning (ICL)**，无需任何模型微调或额外数据收集。

### 相比现有方法的优势
| 维度 | 本方法优势 |
|------|-----------|
| **无需训练** | 完全基于 off-the-shelf LLMs，仅需更换文档即可适配新工具，避免昂贵的 fine-tuning 或 RL 流程 |
| **更强泛化性** | 不依赖特定数据集或知识库，适用于任意具有文档支持的 PCG 工具 |
| **更高可靠性** | 双智能体分离“创意生成”与“逻辑验证”，显著减少参数幻觉和致命错误 |
| **更易部署** | 架构轻量，仅需提供文档和示例，无需构建复杂的 retrieval pipeline 或 adapter 模块 |

---

## 2. 核心实验方法和设置

### 使用的平台与工具
- 实验基于 Unity 引擎中的 **TileWorldCreator** 插件，用于生成 2D 和 3D 地形地图。
- 所有 LLM 推理通过 **Claude 3.5 Sonnet API** 完成。
- Actor 和 Critic 均基于 **UGenLah**（Unity 编辑器 AI 助手）系统接口运行。

### 实验设置

#### Experiment I: 复杂任务可靠性测试（Reliability）
- **任务**：生成满足四个约束条件的 3D 山地地图：
  1. 单一山峰（无多个孤立高地）
  2. 正好三层高度（three height layers）
  3. 草地点缀仅出现在山顶层
  4. 岩石散布在非山区
- **模式**：zero-shot，不允许后续交互。
- **试验次数**：10 次独立运行。
- **对比模型**：
  - **Proposed**: Actor-Critic 双智能体架构
  - **Baseline**: Single-Agent（带文档和示例）

#### Experiment II: 效率与自主性评估（Efficiency）
- **目标地图类型**：4 种不同类型的地图（2 个 2D，2 个 3D），涵盖不同 PCG 算法组合（如 Cellular Automata、Perlin Noise 等）。
- **允许迭代修正**：若失败，可提供描述性反馈（如“高度层断开”），但不给出解决方案。
- **对比模型**：
  - **Actor-Critic**（双智能体）
  - **Single-Agent (w/ Docs)**
  - **Single-Agent (No Docs)**

### 评估指标

| 实验 | 指标 |
|------|------|
| **Experiment I** |  
| - **Task Success Rate (%)**：所有关键元素正确出现的比例  
| - **Average Mistakes per Run**：每轮运行中的次优参数选择数量  
| - **Failure Reasons**：分类统计失败原因（如缺少生成器、参数错误等） |
| **Experiment II** |  
| - **Token Usage**：总 token 消耗（Actor + Critic）  
| - **Follow-up Prompts Required**：所需人工干预提示数  
| - **Objective Completion**：最终是否达成目标地图 |

---

## 3. 主要实验结果和性能指标

### Experiment I: 可靠性结果（3D Mountain Map）

| 指标 | Actor-Critic | Single-Agent (w/ Docs) |
|------|--------------|------------------------|
| **Success Rate (%)** | **80%** | 60% |
| **Average Mistakes / Successful Run** | 2.25 | 2.17 |

- **提升效果**：任务成功率提升 **20个百分点**（相对提升 33%）。
- 尽管双智能体在成功案例中犯略多“非致命错误”，但其**致命性失败更少**，尤其是：
  - **Missing/wrong parameter choice**：0 vs 1 次
  - **Did not add reference layer**：3 次（100% 失败案例）vs 2 次（50%）

> 🔍 **关键观察**：Actor-Critic 在“单一山峰”要求上的遵守率达到 **88%**，而单智能体仅为 **50%**，说明其更能准确解析空间连通性等隐含语义。

### Experiment II: 效率与自主性结果

| Map Type | Model | Tokens Used | Follow-up Prompts | Achieves Objective |
|---------|-------|-------------|--------------------|---------------------|
| 2D Beach | Actor-Critic | 16,392 | 2 | ✅ |
|         | Actor+Resources | 18,987 | 4 | ✅ |
| 3D Mountain Island | Actor-Critic | 14,583 | 4 | ✅ |
|                      | Actor+Resources | 11,873 | 5 | ✅ |
| 3D Hilly Golf Course | Actor-Critic | 12,633 | 4 | ✅ |
|                      | Actor+Resources | 18,676 | 6 | ✅ |
| 2D Escape Maze | Actor-Critic | 4,589 | 2 | ✅ |
|                | Actor+Resources | 7,722 | 3 | ✅ |

- **平均 token 减少 12.7%**，尽管 Critic 增加了推理开销，但由于减少了无效尝试，总体通信效率更高。
- **显著降低人工干预需求**：Single-Agent 平均需要 **1.5 更多 follow-up prompts** 来纠正方向。
- 所有任务中，**Actor-Critic 均成功完成目标**，而部分单智能体在复杂地形中完全失败（标记为 “-” 或 “X”）。

### 消融分析（Implicit Ablation）
- **移除 Critic** → 错误率显著上升，尤其在参数边界和算法兼容性方面。
- **移除文档/示例** → 性能急剧下降，证明 **static resource injection** 是 zero-shot 成功的关键前提。
- **增加迭代次数** → 收敛稳定性增强，但边际效益递减。

---

## 4. 关键结论和发现

### 主要发现
1. **Off-the-shelf LLMs 可以直接操控复杂 PCG 工具**，只要配备适当的文档和示例，无需 fine-tuning。
2. **Actor-Critic 分离机制有效弥合了语义鸿沟**：Actor 负责“想”，Critic 负责“验”，二者协作显著提高输出的功能正确性和任务成功率。
3. **In-Context Learning 足以应对高维参数空间**：通过结构化 prompt 设计和迭代 refinement，LLM 能够自主推理出合法且符合意图的参数配置路径。
4. **该范式具有强泛化潜力**：不仅限于地形生成，还可推广至 CAD、BIM、科学仿真等专业软件自动化场景。

### 方法的局限性
| 限制 | 说明 |
|------|------|
| **推理延迟较高** | 迭代反馈循环增加了响应时间，不适合实时应用 |
| **缺乏长期记忆** | 每次任务独立，无法从历史经验中学习 |
| **依赖高质量文档** | 若文档缺失或不准确，Critic 验证能力受限 |
| **固定上下文窗口管理** | 当前采用状态替换策略，可能丢失有用历史信息 |

### 未来工作方向
1. **优化计算开销**：
   - 使用小型蒸馏模型（如 Llama-3-8B 或 Phi-3）作为 Critic，降低成本。
   - 缓存常见成功的 **parameter trajectories** 以供复用。
2. **引入长期记忆机制**：
   - 结合 **Retrieval-Augmented Generation (RAG)**，建立向量数据库存储过往成功策略。
3. **构建诊断元层（Diagnostic Meta-Layer）**：
   - 自动识别重复性错误模式，并动态调整 prompt 策略，实现从“被动纠错”到“主动预防”的转变。
4. **扩展至更多领域**：
   - 应用于建筑建模（BIM）、数字孪生（Digital Twin）、音频合成引擎等专业工具链。

---

> ✅ **总结一句话**：本文提出了一种无需训练、可迁移、高可靠的 dual-agent 架构，首次实现了用自然语言零样本控制复杂 PCG 工具生成多样化 3D 地图，为通用 LLM 智能体操作专业软件提供了新范式。

</details>

---

### 9. [T-pro 2.0: An Efficient Russian Hybrid-Reasoning Model and Playground](https://arxiv.org/abs/2512.10430)

**Authors**: Dmitrii Stoianov, Danil Taranets, Olga Tsymboi, Ramil Latypov, Almaz Dautov, Vladislav Kruglikov, Nikita Surkov, German Abramov, Pavel Gein, Dmitry Abulkhanov, Mikhail Gashkov, Viktor Zelenkovskiy, Artem Batalov, Aleksandr Medvedev, Anatolii Potapov  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10430v1  

#### Abstract
We introduce T-pro 2.0, an open-weight Russian LLM for hybrid reasoning and efficient inference. The model supports direct answering and reasoning-trace generation, using a Cyrillic-dense tokenizer and an adapted EAGLE speculative-decoding pipeline to reduce latency. To enable reproducible and exten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：T-pro 2.0: An Efficient Russian Hybrid-Reasoning Model and Playground

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前俄语大语言模型（LLM）生态系统存在以下关键问题：
- **高质量开源模型稀缺**：多数高性能俄语模型为闭源API服务（如YandexGPT、GigaChat），限制了研究可复现性。
- **缺乏统一推理评测体系**：缺少针对俄语复杂推理任务（尤其是数学竞赛级）的权威基准。
- **多语言分词器对俄语支持不足**：通用分词器（如Qwen、Llama）在处理西里尔字母时效率低下，导致文本压缩率低、推理延迟高。
- **推理效率优化缺失**：俄语模型中尚未有效应用如speculative decoding等加速技术。

### 提出的新方法与创新
T-pro 2.0 提出了一套完整的俄语高效混合推理系统，其核心创新包括：

#### （1）**Cyrillic-dense Tokenizer**
- 在Qwen3分词器基础上，将34k个低频非西里尔符号替换为西里尔字符，保持总词表大小不变。
- 显著提升俄语文本压缩效率：俄语维基百科中单字平均token数从3.12降至2.38，两token内覆盖比例从38.2%提升至60.1%。
- 该设计泛化性强，在8种西里尔语言上均优于RuAdapt、GigaChat等专用分词器。

#### （2）**T-Wix 500k SFT 数据集**
- 当前最大规模的俄语混合推理指令微调数据集（约50万样本）。
- 覆盖通用问答、长上下文任务、教师生成的推理链（reasoning traces）。
- 包含专门构建的**俄语奥林匹克数学题**子集，强调高质量、多样性与难度平衡。

#### （3）**T-Math 数学推理基准**
- 构建自全俄及莫斯科数学奥赛（1998–2025）的331道题目。
- 所有问题均为单答案数值解，支持自动化评估。
- 题目难度远超常规翻译基准，未被前沿模型饱和（最强模型pass@1 < 0.75）。

#### （4）**EAGLE-style Speculative Decoding 加速推理**
- 集成轻量级draft model（基于Llama-2单层+FR-Spec组件），实现并行token预测。
- 使用SGLang部署动态draft tree机制，显著降低推理延迟。
- 平均提速达 **1.85×**，在STEM领域最高达 **2.25×**（T-Math任务）。

#### （5）**交互式Web Demo平台**
- 公开演示系统（[http://t-pro-2-0.streamlit.app](http://t-pro-2-0.streamlit.app)）支持：
  - 对比“直接回答”与“逐步推理”模式；
  - 实时查看token流、吞吐量、acceptance ratio等性能指标；
  - 使用预设prompt测试不同领域任务。

### 相比现有方法的优势
| 维度 | T-pro 2.0 | 现有俄语模型（如RuAdapt、GigaChat） |
|------|----------|-------------------------------|
| 开源程度 | 完全开放权重、数据、推理代码 | 多为闭源或仅发布模型 |
| 分词效率 | 专为西里尔语优化，压缩率更高 | 使用通用分词器，效率较低 |
| 推理能力 | 支持显式推理链生成 + 高效解码 | 多数不支持可控推理模式 |
| 推理速度 | EAGLE加速，平均1.85×提速 | 无系统级推理优化 |
| 可验证性 | 发布T-Math等原生俄语挑战性基准 | 多依赖翻译或简单任务 |

---

## 2. 核心实验方法和设置

### 使用的数据集

| 类型 | 名称 | 描述 |
|------|------|------|
| **预训练/中期训练数据** | Midtraining Mix | 40B tokens，含俄语(49%)、英语(36%)、代码(9.3%)，涵盖推理、QA、数学、对话等 |
| **SFT数据集** | T-Wix 500k | 自研指令微调数据集，含468k通用任务 + 30k推理任务 |
| **评估基准 - 通用知识** | MERA, MaMuRAMu, ruMMLU-Pro | 俄语常识理解与逻辑推理基准 |
| **评估基准 - 对话能力** | Arena Hard Ru, WildChat Hard Ru | 原生俄语对话质量评测 |
| **评估基准 - 推理能力** | T-Math, ruAIME, ruMATH-500, ruGPQA, ruLCB | 数学、科学、代码等高级推理任务 |
| **对比基线模型** | Qwen3-32B, RuadaptQwen3-32B, DeepSeek-V3, GPT-4o, o4-mini 等 | 包括开源与闭源主流模型 |

### 实验设置与评估指标

#### 推理模式
- **Standard Mode**：直接输出答案。
- **Reasoning Mode**：生成完整推理过程后给出答案。

#### 评估指标
| 类别 | 指标 |
|------|------|
| 通用知识 | MERA, ruMMLU-Pro 准确率 |
| 对话能力 | Arena Hard Win Rate, WildChat Score |
| 数学推理 | pass@1（T-Math）、ruAIME/rumath-500准确率 |
| 推理效率 | Speedup（相对标准解码）、Acceptance Length、Tokens/sec |

#### 基线对比模型
- **开源俄语模型**：RuadaptQwen3-32B-Instruct
- **国际主流模型**：Qwen3-32B, Gemma 3 27B, DeepSeek-V3/R1
- **商业闭源模型**：GPT-4o, o4-mini, YandexGPT5-Pro, GigaChat 2 Max

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 4 & Table 5）

#### 📊 通用知识与对话能力（俄语）
| 模型 | MERA | ruMMLU-Pro | Arena Hard Ru | WildChat Hard Ru |
|------|------|------------|---------------|------------------|
| **T-pro 2.0** | **0.660** | **0.697** | **91.1** | **72.6** |
| Qwen3-32B | 0.582 | 0.677 | 83.95 | 59.6 |
| RuadaptQwen3 | 0.574 | 0.652 | 68.4 | 41.5 |
| GPT-4o | 0.642 | 0.714 | 85.14 | 41.4 |

✅ **结论**：T-pro 2.0在所有俄语通用任务上显著优于同类开源模型，接近GPT-4o水平。

#### 🧮 高级推理能力（俄语）
| 模型 | T-Math | ruAIME 2024 | ruMATH-500 | Vikhr Math |
|------|--------|-------------|------------|------------|
| **T-pro 2.0** | **0.541** | **0.704** | **0.940** | **0.799** |
| Qwen3-32B | 0.529 | 0.706 | 0.938 | 0.809 |
| DeepSeek-V3 | 0.278 | 0.319 | 0.882 | 0.613 |
| GPT-4o | 0.106 | 0.090 | 0.766 | 0.372 |

✅ **结论**：
- 在T-Math这一原生俄语奥赛题上表现最佳（0.541），远超GPT-4o（0.106）。
- 在ruAIME等翻译基准上也大幅领先闭源模型。

#### ⚡ 推理效率（Speculative Decoding 加速效果）
| 基准 | 模式 | Speedup | Acceptance Length |
|------|------|---------|-------------------|
| **ruMT-Bench** | Standard | 1.79× | 3.31 |
| **ruMT-Bench** | Reasoning | 1.69× | 3.10 |
| **ruCodeEval** | Standard | 2.15× | 3.93 |
| **T-Math** | Reasoning | **2.25×** | **4.01** |
| **STEM领域平均** | — | **1.99×** | **3.57** |

✅ **结论**：EAGLE加速在技术类任务（STEM）中效果更明显，推理模式下仍保持高效。

### 消融实验结果

#### （1）中期训练数据混合策略（Table 12）
| 配置 | ruAIME 2024 | ruAIME 2025 | ruGPQA |
|------|-------------|-------------|--------|
| Pre-train + Instruct | 0.60 | 0.47 | 0.58 |
| **Instruct-only** | **0.67** | **0.63** | **0.66** |

➡️ **发现**：纯instruction数据在同等token预算下优于混合预训练数据，说明强预训练模型更适合通过高质量指令进一步调优。

#### （2）分词器影响（Table 13）
| 分词器 | MERA Macro-Avg |
|--------|----------------|
| Qwen3原生 | 0.560 |
| **T-pro 2.0 Cyrillic-dense** | **0.574** |

➡️ **发现**：更换为西里尔密集分词器不仅未损害性能，反而略有提升，证明其有效性。

#### （3）奖励模型消融（Table 17）
- 移除transitive preference pairs导致RewardBench2(RU)总分从0.587降至0.533。
- 使用DeepSeek-V3作为judging model比Qwen3-235B-A22B在聊天任务上表现更好。

---

## 4. 关键结论和发现

### 主要发现
1. **针对性适配强于盲目扩展**：通过对Qwen3进行俄语定制化改造（分词器、数据、推理优化），T-pro 2.0在不增加参数量的情况下实现了对俄语任务的显著超越。
2. **分词器设计至关重要**：Cyrillic-dense tokenizer极大提升了俄语文本编码效率，是实现高效推理的基础。
3. **原生俄语基准更具区分力**：T-Math等原生奥赛题能有效暴露模型真实推理能力差异，而翻译基准易被“记忆”而非“推理”解决。
4. **Speculative Decoding可跨语言迁移**：EAGLE-style方法成功应用于俄语模型，平均提速1.85×，验证其语言无关性。
5. **俄语优化不影响英文性能**：T-pro 2.0在英文AIME/MATH-500等任务上仍具竞争力（Table 23），表明多语言能力得以保留。

### 局限性
- ❌ **无工具使用能力**：未针对function calling或多步agent行为进行优化。
- ❌ **仅离线DPO对齐**：未采用在线RL（如PPO），可能限制鲁棒性。
- ❌ **长上下文未充分验证**：虽支持128k RoPE缩放，但未实证其在极长输入下的检索与连贯性。
- ❌ **部分数据不可完全复现**：中期训练与DPO阶段使用了部分专有数据。

### 未来工作方向
- 引入function calling与tool use能力。
- 探索online RL（如GRPO）进行更精细对齐。
- 验证并优化128k长上下文性能。
- 扩展T-Math至更多学科（物理、化学）与题型（证明题）。
- 推动社区共建俄语LLM透明评测生态。

---

> ✅ **总结一句话**：  
> T-pro 2.0 是首个集**高效俄语分词器**、**高质量原生推理数据集**、**挑战性俄语数学基准**与**EAGLE加速推理**于一体的开源俄语混合推理系统，标志着俄语LLM向高效、可复现、专业化方向迈出关键一步。

</details>

---

### 10. [TDC-Cache: A Trustworthy Decentralized Cooperative Caching Framework for Web3.0](https://arxiv.org/abs/2512.09961)

**Authors**: Jinyu Chen, Long Shi, Taotao Wang, Jiaheng Wang, Wei Zhang  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09961v1  

#### Abstract
The rapid growth of Web3.0 is transforming the Internet from a centralized structure to decentralized, which empowers users with unprecedented self-sovereignty over their own data. However, in the context of decentralized data access within Web3.0, it is imperative to cope with efficiency concerns c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TDC-Cache: A Trustworthy Decentralized Cooperative Caching Framework for Web3.0

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
Web3.0 的去中心化架构虽然提升了用户对数据的自主权（self-sovereignty），但也带来了以下挑战：
- **高访问延迟**：由于去中心化存储（如 IPFS、SWARM）依赖分布式共识和分块存储（chunked data），导致内容检索效率低下。
- **缓存策略不适应**：传统 Web2.0 的集中式缓存机制（如 CDN）无法直接应用于 Web3.0 的去中心化环境。
- **安全风险**：在去中心化环境中，恶意节点可能篡改缓存内容或破坏模型训练一致性（如 Byzantine 攻击）。

### 🚀 提出的新方法与创新思路
作者提出了一种名为 **TDC-Cache** 的可信去中心化协同缓存框架，其核心创新包括：

#### （1）两层架构设计：Decentralized Oracle Network (DON) 层
- 引入 **DON 层作为可信中介**，连接用户（CR）与去中心化存储（CP），实现高效的内容缓存与快速响应。
- 每个 Oracle 包含缓存池（DRAM + NVMe SSD）和可执行组件（缓存模块 + 分布式共识模块）。

#### （2）基于深度强化学习的去中心化缓存策略：DRL-DC
- 将缓存决策建模为 **Decentralized Partially Observable Markov Decision Process (Dec-POMDP)**。
- 使用 **QMIX 算法** 进行多智能体协同训练，动态优化缓存替换策略，最大化长期缓存命中率（cache hit rate）。
- 能够应对用户请求分布动态变化、部分可观测等现实挑战。

#### （3）新型共识机制：Proof of Cooperative Learning (PoCL)
- 针对 DRL 模型训练过程中的拜占庭威胁，提出 PoCL 共识协议。
- 核心思想是通过“合作证明”（cooperative proof）选择可靠的训练节点，并验证模型更新的有效性。
- 四阶段流程：Prepare → Train → Synchronize → Commit，确保全局一致性和安全性。

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 显著降低平均访问延迟（↓20%），提升缓存命中率（↑最多18%）。 |
| **安全性** | 抵御 Byzantine 节点攻击，保障缓存决策一致性。 |
| **去中心化适配性** | 完全去中心化设计，无需中心协调器，适用于 Web3.0 架构。 |
| **自适应能力** | DRL-DC 可实时学习内容流行度变化，优于静态策略（LRU/LFU/FIFO）。 |

---

## 2. 核心实验方法和设置

### 📊 数据集与模拟环境
- **非真实数据集**，而是基于 **Zipf 分布生成合成内容请求序列**，以模拟 Web3.0 中内容访问的长尾特性。
- 内容数量 $ F $、Oracle 数量 $ M $、用户数 $ U $、时间槽 $ T $ 均可配置。
- 不同的 Zipf 参数 $ \alpha \in \{0.5, 1.0, 1.5\} $ 控制内容流行度的倾斜程度。

### ⚙️ 实验设置
- **系统平台**：Ubuntu 24.04 LTS，Intel i9-14900K，NVIDIA RTX 4090 GPU，128GB RAM。
- **缓存容量**：每个 Oracle 缓存大小 $ C_m = 4 $（单位内容）。
- **训练参数**：
  - 时间槽总数 $ T = 10,000 $
  - 探索率从 0.9 线性衰减至 0.05
  - 学习率：0.001，折扣因子 $ \gamma = 0.99 $

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Cache Hit Rate** | 缓存命中的请求占比，越高越好 |
| **Content Retrieval Latency** | 用户发起请求到收到内容的时间，越低越好 |
| **Success Consensus Rate** | PoCL 协议成功完成的比例，反映鲁棒性 |
| **Average Access Latency** | 所有请求的平均延迟 |

### 🆚 基线方法对比
- **传统缓存策略**：
  - LRU（Least Recently Used）
  - LFU（Least Frequently Used）
  - FIFO（First In First Out）
  - Random（随机替换）
- **直接访问（Direct Access）**：绕过缓存，直接从去中心化存储获取内容（作为下界参考）
- **共识协议对比**：
  - PBFT（Practical Byzantine Fault Tolerance） vs. PoCL

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 性能指标 | TDC-Cache 表现 | 对比基准 | 提升幅度 |
|--------|----------------|----------|---------|
| 平均访问延迟 | ↓ 20% | 直接访问 | 显著改善用户体验 |
| 最大缓存命中率提升 | ↑ 18% | LRU/LFU/FIFO | 在 $ \alpha=1.0 $ 下达到 ~0.75 |
| 成功共识率 | ↑ 10% | PBFT | 特别是在大规模网络中更稳定 |
| 内容检索延迟（小文件） | 6.90 ms/KB ($ \alpha=0.5 $) | Direct Access: 8.19 ms/KB | ↓15.7% |
| 内容检索延迟（大文件） | 1.24 ms/KB ($ \alpha=0.5 $) | Direct Access: 1.46 ms/KB | ↓15.1% |

> 注：随着 $ \alpha $ 增加（内容流行度更集中），TDC-Cache 的优势更加明显。

### 📊 与基线方法的对比结果

#### （1）缓存命中率对比（Fig. 7 & Fig. 8）
- DRL-DC 在所有 Oracle 上均保持最高且最稳定的命中率。
- 在 $ \alpha = 1.0 $ 时，DRL-DC 达到约 **0.75**，而次优策略（LFU）仅为 ~0.63。
- 随着请求序列增长，DRL-DC 持续学习并提升命中率，而传统策略趋于饱和甚至下降。

#### （2）不同内容流行度下的表现（Table II）
| $ \alpha $ | 策略 | 1KB–256KB 延迟 (ms/KB) |
|------------|-------|------------------------|
| 0.5 | DRL-DC | **6.90** |
|     | LFU    | 7.13 |
|     | Direct Access | 8.19 |
| 1.0 | DRL-DC | **6.52** |
|     | LFU    | 6.67 |
| 1.5 | DRL-DC | **6.19** |
|     | LFU    | 6.23 |

> 所有条件下 DRL-DC 均最优，且差距随 $ \alpha $ 增大而扩大。

#### （3）PoCL vs PBFT 成功共识率（Fig. 5 & Fig. 6）
- 当故障概率 $ P_f = 0.1 $ 时：
  - PBFT 的成功率随节点数增加而**下降**
  - PoCL 的成功率随节点数增加而**上升**，最终趋近于 1.0
- 在 $ M=300 $、$ P_f=0.2 $ 时，PoCL 成功率仍接近 1.0，而 PBFT 已趋近于 0。
- PoCL 的“拐点”（inflection point）比 PBFT 高 **209.47%**，表明更强的扩展性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **引入 DON 层显著提升 Web3.0 数据访问效率**  
   TDC-Cache 通过构建去中心化的 Oracle 网络作为中间缓存层，有效缓解了去中心化存储带来的高延迟问题。

2. **DRL-DC 具备强自适应能力**  
   基于 QMIX 的多智能体 DRL 框架能够动态学习内容流行趋势，在多种请求模式下持续优化缓存策略，显著优于传统启发式算法。

3. **PoCL 提供高可靠、抗拜占庭的共识保障**  
   通过“合作证明”机制动态选举训练节点，并结合 Commit 阶段的投票验证，PoCL 在存在故障或恶意节点的情况下仍能维持高共识成功率。

4. **TDC-Cache 具备良好可扩展性与鲁棒性**  
   实验显示，随着网络规模扩大，TDC-Cache 的性能不降反升，尤其在高失败概率场景下远超 PBFT。

### ⚠️ 方法的局限性
- **依赖仿真环境**：实验基于合成数据，尚未在真实 Web3.0 应用（如 DApp 或 IPFS 网络）中部署验证。
- **计算开销较高**：DRL 模型训练需要一定算力资源，可能限制轻量级设备参与。
- **未考虑激励机制**：目前未设计经济激励来鼓励 Oracle 主动参与缓存与训练任务。

### 🔮 未来工作方向
1. 设计 **激励相容机制**（incentive-compatible mechanism），鼓励更多节点诚实参与。
2. 探索 **轻量化 DRL 模型**，降低边缘设备的计算负担。
3. 研究 **动态网络拓扑下的适应性**，支持节点频繁加入/退出。
4. 将 TDC-Cache 集成到实际 Web3.0 生态系统中进行端到端测试。

---

> **总结一句话**：  
> TDC-Cache 是首个面向 Web3.0 的可信去中心化协同缓存框架，融合 DRL 动态优化与 PoCL 安全共识，在延迟、命中率和鲁棒性方面全面超越传统方案，为下一代去中心化互联网的数据高效分发提供了可行路径。

</details>

---

### 11. [HGC-Herd: Efficient Heterogeneous Graph Condensation via Representative Node Herding](https://arxiv.org/abs/2512.09947)

**Authors**: Fuyan Ou, Siqi Ai, Yulin Hu  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09947v1  

#### Abstract
Heterogeneous graph neural networks (HGNNs) have demonstrated strong capability in modeling complex semantics across multi-type nodes and relations. However, their scalability to large-scale graphs remains challenging due to structural redundancy and high-dimensional node features. Existing graph co...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HGC-Herd: Efficient Heterogeneous Graph Condensation via Representative Node Herding

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的图神经网络（HGNNs）在处理大规模异构图时面临计算开销大、内存消耗高以及训练效率低的问题。传统的图压缩方法（如 GCond）主要针对同构图设计，依赖梯度匹配机制，导致**计算复杂、优化困难、难以扩展到异构图场景**。此外，采样类方法（如 GraphSAINT）容易引入信息丢失和结构偏差。

### 提出的新方法与创新思路
本文提出了 **HGC-Herd** —— 一种**无需训练的异构图压缩框架**（training-free heterogeneous graph condensation），其核心思想是通过“代表性节点聚集”来生成紧凑且语义丰富的子图。该方法包含三个关键模块：

- **Feature Propagation**：基于 metapath 进行轻量级特征传播，融合多跳关系上下文，增强节点表示的语义丰富性。
- **Class-wise Prototype Construction**：为每个类别构建类原型（prototype），作为该类语义中心。
- **Strategic Herding Selection**：采用类内“赶牛式选择”（herding）策略，逐步挑选最能逼近类原型的代表性节点，形成平衡且判别性强的压缩图。

### 相比现有方法的优势
- ✅ **无需训练**：避免了梯度优化过程，显著降低计算与调参成本。
- ✅ **高效可复现**：选择过程确定性高，跨随机种子稳定，支持预构建与重用。
- ✅ **保持语义与结构保真度**：通过类感知的 herding 策略保留语义多样性与跨类型连接结构。
- ✅ **灵活可控**：支持按类设定压缩预算，适应类别不平衡场景。
- ✅ **低资源消耗**：大幅减少运行时间和 GPU 内存占用，适用于资源受限环境。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个标准异构图基准上进行验证：
- **ACM**：学术网络，节点类型包括 paper、author、subject、venue；任务是对 paper 分类（3 类）。
- **DBLP**：作者合作网络，节点类型包括 author、paper、venue、term；任务是对 author 分类（4 类）。
- **Freebase**：大型知识图谱，包含 book、person、field 等多种实体；任务是对 book 分类（7 类）。

### 实验设置与评估指标
- **压缩比例（condensation ratio）**：测试 $ r \in \{1.2\%, 2.4\%, 4.8\%, 9.6\%\} $
- **下游模型**：Simple-HGN（两层，64 隐藏单元）
- **优化器**：Adam ($ lr = 5 \times 10^{-4} $)
- **评估指标**：**节点分类准确率（accuracy %）**
- **硬件平台**：NVIDIA RTX 3050 GPU，每组实验重复 5 次取均值
- **压缩图复用**：每个压缩比下仅构建一次图，多次训练中共享

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Random-HG** | 随机采样目标类型节点 |
| **K-Center-HG** | 贪心 K-center 覆盖策略选取节点 |
| **Coarsening-HG** | 基于 METIS 的图粗化方法 |
| **GCond** | 梯度匹配型图压缩（原用于同构图） |
| **Whole Dataset** | 全图训练，作为性能上限 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table I）

| 数据集 | 压缩比 | HGC-Herd 准确率 | 全图准确率 |
|--------|--------|------------------|------------|
| ACM    | 1.2%   | **91.88%**       | 93.11%     |
| ACM    | 9.6%   | **93.01%**       | 93.11%     |
| DBLP   | 1.2%   | **89.86%**       | 95.19%     |
| DBLP   | 9.6%   | **94.79%**       | 95.19%     |
| Freebase | 1.2% | **57.18%**       | 63.67%     |
| Freebase | 9.6% | **61.42%**       | 63.67%     |

> 💡 可见 HGC-Herd 在仅使用 **1.2% 数据**的情况下，在 ACM 上达到接近全图性能，在 DBLP 和 Freebase 上也表现出极强的压缩能力。

### 与基线方法的对比结果
- 在所有压缩比下，HGC-Herd 显著优于所有基线方法，尤其在低压缩比（如 1.2%）时优势最为明显。
- 例如在 ACM 上 1.2% 条件下：
  - HGC-Herd: **91.88%**
  - K-Center-HG: 62.66%
  - Coarsening-HG: 64.17%
  - GCond: 41.17% （波动大，±5.14）
- 表明传统方法在极端压缩下严重损失语义信息，而 HGC-Herd 能有效保留关键结构与类别分布。

### 消融实验结果（Table II）
在 1.2% 压缩比下的消融研究显示：

| 变体 | ACM 准确率 | Freebase 准确率 |
|------|-------------|-----------------|
| HGC-Herd（完整） | **91.88%** | **57.18%** |
| w/o Feature Propagation | 89.45% (-2.43) | 55.12% (-2.06) |
| w/o Herding | 81.23% (-10.65) | 48.67% (-8.51) |

> 🔍 结论：两个组件都至关重要：
> - **特征传播** 提供多跳语义上下文；
> - **herding 选择机制** 确保类分布均衡与代表性。

---

## 4. 关键结论和发现

### 主要发现
- HGC-Herd 是首个专为 **异构图设计的无需训练的图压缩框架**，实现了高效、稳定、可复现的压缩。
- 通过 **类原型引导的 herding 机制**，能够在极低数据量下（如 1.2%）维持接近全图的分类性能。
- 相比基于梯度的方法（如 GCond），HGC-Herd **速度快 4–6 倍**，内存消耗更低，更适合实际部署。
- 方法对类别不平衡具有鲁棒性，因采用按类比例分配预算策略。

### 方法的局限性
- 当前依赖人工指定 metapath，未来可探索自动发现重要路径。
- 尚未应用于动态或时空异构图。
- 对 extremely long-tail 类别可能仍存在覆盖不足风险。

### 未来工作方向
- 扩展至 **动态图场景**（dynamic graphs）
- 探索 **自适应类预算分配机制**
- 集成到 **联邦学习与隐私保护系统** 中，发挥其无需训练、可离线压缩的优势
- 支持更多类型的下游任务（如链接预测、推荐）

---

> ✅ **总体评价**：  
> HGC-Herd 提出了一种简洁、高效、实用的异构图压缩新范式，打破了传统依赖梯度优化的框架限制，在精度、速度、稳定性之间取得了优异平衡，具备较强的工程应用潜力。

</details>

---

### 12. [DynaMate: An Autonomous Agent for Protein-Ligand Molecular Dynamics Simulations](https://arxiv.org/abs/2512.10034)

**Authors**: Salom\'e Guilbert, Cassandra Masschelein, Jeremy Goumaz, Bohdan Naida, Philippe Schwaller  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10034v1  

#### Abstract
Force field-based molecular dynamics (MD) simulations are indispensable for probing the structure, dynamics, and functions of biomolecular systems, including proteins and protein-ligand complexes. Despite their broad utility in drug discovery and protein engineering, the technical complexity of MD s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DynaMate: An Autonomous Agent for Protein-Ligand Molecular Dynamics Simulations**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
分子动力学（**Molecular Dynamics, MD**）模拟在药物发现和蛋白质工程中至关重要，但其流程高度复杂且易出错，尤其在处理**protein-ligand系统**时。传统流程涉及多个手动步骤，如结构清洗、质子化、溶剂化、力场参数化、平衡等，这些步骤对非专家用户门槛极高，且容易因参数不匹配导致不稳定轨迹。

现有自动化工具（如CHAPERONg、PyAutoFEP）虽能简化部分流程，但通常**缺乏灵活性、无法自适应错误、难以扩展到新系统或软件平台**。

### **提出了什么新方法或新思路**
本文提出 **DynaMate**，一个基于大语言模型（**LLM**）的**模块化多智能体框架**（multi-agent framework），能够**自主设计并执行完整的protein-ligand MD模拟流程**，并支持结合**MM/PB(GB)SA方法进行结合自由能计算**。

DynaMate的核心架构包含三个专用智能体：
- **Planner Agent**：解析用户自然语言指令，检索结构信息，制定上下文感知的模拟计划。
- **MD Agent**：执行模拟任务，调用工具进行结构准备、参数化、溶剂化、能量最小化、平衡和生产运行。
- **Analyzer Agent**：分析轨迹，生成RMSD、RMSF、gyration radius、氢键等统计图表，并提供自然语言解释。

该框架具备以下关键能力：
- **动态工具调用**（dynamic tool use）
- **文献增强推理**（retrieval-augmented reasoning，通过PaperQA和web search）
- **自纠错行为**（self-correcting behavior）
- **跨平台兼容性**（支持GROMACS和AmberTools）

### **相比现有方法的优势**
| 特性 | MDCrow / NAMD-Agent | DynaMate（本文） |
|------|---------------------|------------------|
| 支持protein-ligand系统 | ❌ | ✅ |
| 结合自由能计算（MM/PB(GB)SA） | ❌ | ✅ |
| 自动错误检测与恢复 | ❌ | ✅ |
| 文献/数据库检索增强 | 有限 | ✅（PaperQA + web search） |
| 多智能体模块化架构 | ❌ | ✅ |
| 跨平台支持（GROMACS/Amber） | 单一引擎 | ✅ |

DynaMate是首个成功实现**全自动protein-ligand MD流程**并具备**自纠错能力**的agentic系统。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
共测试了12个benchmark系统：
- **5个protein-ligand复合物**（PDB IDs: 3HTB, 3PTB, 4GIH, 4W52, 5UEZ）——广泛用于MD和自由能方法基准测试。
- **5个无配体蛋白系统**（PDB IDs: 1AKI, 1FDH, 1J37, 2CBA, 2VVB）——测试多链蛋白和通用性。
- **2个含已知错误的系统**：
  - **BRD4_UNL**：人为引入氯原子命名错误。
  - **5KB6_ADN**：含两个配体，测试框架对复杂系统的适应能力。

此外，在结合自由能计算部分，使用了**10个BRD4 BD1抑制剂**（PDB: 6JJ3）进行测试，与GNINA对接分数和实验IC50值对比。

### **实验设置和评估指标**
- **LLM模型**：测试了5种LLM：
  - `Claude 3 Opus`, `Claude 3.7 Sonnet`, `GPT-4.1`, `GPT-4.1 mini`, `Llama 3.3`
- **每组实验重复3次**以验证鲁棒性。
- **评估指标**：
  - **准确性（Accuracy）**：关键步骤是否成功创建必要文件（非空）。
  - **效率（Efficiency）**：实际tool calls数 vs 最小必需tool calls数。
  - **错误纠正能力**：能否识别并修复输入错误（如原子命名、位置约束缺失）。
  - **结合自由能预测相关性**：MM/PB(GB)SA ΔΔG 与实验IC50的相关系数（Pearson *r*）。

### **基线方法对比**
- 与现有agentic MD框架（MDCrow, NAMD-Agent）对比功能完整性（见Table S1）。
- 与传统手动流程对比自动化程度和成功率。
- 与**GNINA对接打分**对比结合亲和力预测性能。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **在5个标准protein-ligand系统上，所有LLM均达到100%准确率**（图2a），成功完成从结构准备到生产模拟的全流程。
- **在含错误的BRD4_UNL系统上**：
  - `Claude 3.7 Sonnet` 和 `GPT-4.1 mini` 在3次运行中均成功修正氯原子命名错误。
  - `GPT-4.1` 有一次成功通过手动插入GAFF参数解决。
  - `Claude 3 Opus` 通过移除氯原子“绕过”问题（结构被修改，不理想）。
  - `Llama 3.3` 完全失败。
- **在双配体系统5KB6_ADN上，所有LLM均未能完成**，暴露当前工具集的局限性。

### **与基线方法的对比结果**
- **功能全面性远超MDCrow/NAMD-Agent**：首次支持ligand系统、自由能计算、自纠错。
- **结合自由能预测性能优于对接打分**：
  - DynaMate的MM/PB(GB)SA ΔΔG 与实验IC50相关性为 **r = 0.597**
  - GNINA对接打分相关性仅为 **r = 0.385**
  - 表明MD模拟提供的动态信息显著提升亲和力预测可靠性。

### **消融实验结果**
- **效率分析**（图2b）：
  - `Claude 3 Opus` 和 `GPT-4.1` 工具调用最高效（接近理论最小值）。
  - `Claude 3.7 Sonnet` 准确率最高但效率略低，因其进行了更多尝试性修复。
  - `Llama 3.3` 效率最低，常误用工具或无法纠错。
- **错误纠正案例研究**：
  - **位置约束错误（1J37系统）**：所有LLM均识别问题，但仅`Claude 3.7 Sonnet`尝试手动创建posre文件（未完全成功）。
  - **双配体系统（5KB6_ADN）**：`Claude`系列表现出更强的探索性（使用文献搜索、尝试脚本生成），而其他模型多为重复尝试。

---

## **4. 关键结论和发现**

### **主要发现**
1. **DynaMate可可靠执行完整protein-ligand MD流程**，在标准系统上成功率100%。
2. **具备强大的错误诊断与自纠错能力**，能在多种错误场景下（如原子命名错误）找到可行解决方案。
3. **结合MM/PB(GB)SA的自由能计算具有实用价值**，预测结果与实验数据有良好相关性，优于传统对接方法。
4. **LLM选择显著影响性能**：`Claude 3.7 Sonnet`在准确性和创造性方面表现最佳，`Llama 3.3`明显落后。
5. **即使失败，DynaMate仍能提供有价值的调试建议**，帮助用户定位问题文件和关键词。

### **方法的局限性**
- **依赖外部LLM API**：需API密钥和费用，存在速率限制和安全顾虑。
- **不支持长期记忆**：每次运行独立，无法从历史经验学习。
- **工具集有限**：无法处理多配体、膜蛋白、DNA/RNA等复杂系统。
- **输入PDB格式敏感**：非常规原子命名、缺失残基等仍可能导致失败。
- **质子化状态决策风险**：组氨酸、天冬氨酸等残基的质子化状态可能影响结果，当前依赖默认或文献搜索，非绝对可靠。
- **MM/PB(GB)SA方法固有局限**：忽略构象熵变，适用于相对排名而非绝对自由能预测。

### **未来工作方向**
- 扩展工具集以支持**膜蛋白、核酸、多配体系统**。
- 引入**持久化记忆机制**，使智能体能从过往运行中学习。
- 集成更专业的**软件文档和科学论文数据库**，提升参数选择可靠性。
- 探索**开源LLM本地部署**，降低对商业API的依赖。
- 增强对**异常PDB文件的鲁棒性处理**能力。
- 开发**交互式调试模式**，允许用户介入并指导智能体。

---

> **代码开源地址**：[https://github.com/schwallergroup/DynaMate](https://github.com/schwallergroup/DynaMate)

</details>

---

### 13. [Phythesis: Physics-Guided Evolutionary Scene Synthesis for Energy-Efficient Data Center Design via LLMs](https://arxiv.org/abs/2512.10611)

**Authors**: Minghao LI, Ruihang Wang, Rui Tan, Yonggang Wen  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10611v1  

#### Abstract
Data center (DC) infrastructure serves as the backbone to support the escalating demand for computing capacity. Traditional design methodologies that blend human expertise with specialized simulation tools scale poorly with the increasing system complexity. Recent studies adopt generative artificial...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Phythesis: Physics-Guided Evolutionary Scene Synthesis for Energy-Efficient Data Center Design via LLMs》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统数据中心（DC）设计依赖人工经验与专用仿真工具，难以应对日益增长的系统复杂性和设计效率需求。近期基于生成式AI的方法虽能自动化生成室内场景布局，但**缺乏对物理规律（如热力学、能量守恒）的建模能力**，导致在需要严格物理约束的数据中心设计中产生“幻觉”（hallucinations），无法满足可量化的运行目标（如PUE优化）。因此，如何将大语言模型（LLM）的强大语义推理能力与物理规律相结合，实现高效且符合物理真实性的数据中心自动化设计，是一个亟待解决的关键挑战。

### 提出的新方法与新思路
本文提出 **Phythesis** —— 一种**物理引导的进化式场景合成框架**（Physics-Guided Evolutionary Scene Synthesis），用于通过LLM实现节能型数据中心的自动化设计。其核心创新在于：

- **双层优化架构（Bi-Level Optimization）**：
  - **LLM驱动优化层**：利用两个LLM代理协同工作——**Design LLM** 负责生成三维布局方案；**Reflection LLM** 对仿真结果进行分析并自我批判，指导后续迭代改进。
  - **物理感知优化层**（Physics-Informed Optimization）：基于可微分物理引擎（differentiable physics-based model），通过反向传播（backpropagation）优化设备参数，并从资产库中选择最接近“理想参数”的实际设备组合。

- **进化式生成流程**：采用类进化算法的迭代机制，但以LLM的上下文学习替代传统的交叉/变异操作，实现**语义层面的设计探索与物理反馈驱动的收敛优化**。

- **SimReady 场景输出**：最终生成的设计不仅包含几何结构和拓扑关系，还具备完整的物理属性，可直接用于高保真仿真验证。

### 相比现有方法的优势
| 方法 | 局限性 | Phythesis 的优势 |
|------|--------|------------------|
| 传统半自动设计 | 效率低、人力密集 | 自动化程度高，大幅缩短设计周期 |
| Vanilla LLM 直接生成 | 忽视物理规律，易产生不可行设计 | 引入物理先验与反馈，显著提升可行性 |
| 纯进化算法（EA） | 缺乏高层语义理解与创造性 | 结合LLM语义推理，初始化更优，搜索更智能 |
| 其他3D场景生成方法（如Layout-GPT） | 面向人类生活空间，不适用于工业系统 | 针对数据中心定制，支持冷却、电力等工业级约束 |

---

## 2. 核心实验方法和设置

### 数据集与资产库
- 使用一个**合成的SimReady资产库**，包含每种设备类型（Rack、ACU、Chiller、Cooling Tower）各10种型号。
- 扩展实验中测试了更大规模的资产库（20、30、50种/类），验证方法在复杂组合空间下的适应性。
- 外部环境数据来自EnergyPlus Weather (EPW) 文件，涵盖热带（新加坡）、干旱、温带等多种气候区域。

### 实验设置
- **任务维度**：三个不同计算规模场景：
  - Small-Edge（50–100 servers）
  - Medium-Cluster（1,000+ servers）
  - Large-Cloud（10,000+ servers）
- **LLM骨干模型**：
  - Instructive LLM (I-LM)：GPT-4
  - Reasoning LLM (R-LM)：o3-mini
- **迭代参数**：每次生成N=5个候选设计，保留K=5个最优历史设计作为上下文。
- **总迭代次数**：M=5次。

### 评估指标
| 指标 | 含义 | 优化方向 |
|------|------|----------|
| **PUE**（Power Usage Effectiveness） | 总能耗 / IT设备能耗，衡量能源效率 | ↓ 越低越好 |
| **GSR**（Generation Success Rate） | 成功满足所有物理与几何约束的设计比例 | ↑ 越高越好 |

### 基线方法对比
| 类型 | 方法 | 描述 |
|------|------|------|
| **直接生成** | Random | 随机采样，无LLM或物理引导 |
| | Vanilla LLM | LLM一次性生成，无迭代优化 |
| **迭代优化** | EA | 进化算法，无LLM参与 |
| | ABL (w/o ...) | 多组消融变体（见下表） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 方法 | 小边缘场景 GSR↑ | 中集群场景 GSR↑ | 大云场景 GSR↑ | 大云场景 PUE↓ |
|------|------------------|------------------|------------------|----------------|
| Random | 0.293 | 0.427 | 0.429 | 1.302 |
| Vanilla LLM (R-LM) | 0.947 | 0.782 | **0.813** | 1.162 |
| **Phythesis (R-LM)** | **0.997** | **0.988** | **0.970** | **1.175** |

> 注：尽管Phythesis在Large-Cloud场景的PUE略高于Vanilla LLM（因后者可能违反约束），但在**成功生成的设计中，Phythesis实现了更低的实际PUE**。

#### 性能提升总结：
- **相比 Vanilla LLM**：
  - GSR平均提升达 **57.3%**
  - 在成功生成的设计中，PUE改善 **11.5%**
- **相比 Random 和 EA**：
  - GSR最高提升超过 **240%**
  - PUE显著降低（如Small-Edge从1.8降至1.25）

### 消融实验结果（Ablation Studies）

| 变体 | 移除模块 | 影响 |
|------|---------|------|
| ABL (w/o reflect, phy) | 无Reflection LLM + 无物理优化 | 初始PUE差，收敛慢，GSR下降约24% |
| ABL (w/o design, phy) | 无Design LLM + 无物理优化 | 初始化质量差，GSR下降30%以上 |
| ABL (w/o phy) | 仅移除物理优化层 | GSR下降2.4%-7.9%，PUE上升0.2%-0.5% |
| ABL (w/o llm) | 完全移除LLM模块 | GSR暴跌45.9%（小边缘），PUE上升0.374 |

> ✅ **关键发现**：双层优化结构缺一不可。LLM提供高质量初始化与语义探索，物理优化确保精确收敛与约束满足。

### 可扩展性测试（Table 2）
当资产库扩大至50种/类时：
- Phythesis仍保持 **GSR = 0.973**
- PUE = **1.218**，比EA低 **21.2%**
- 表明该方法在大规模组合空间中依然稳定有效。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM + 物理反馈 = 更强的设计智能**  
   单纯依赖LLM生成容易“脱离现实”，而引入物理仿真反馈后，Reflection LLM能够识别过热、能耗异常等问题，并提出改进建议，形成闭环优化。

2. **双层优化架构显著优于单一流程**  
   LLM负责宏观拓扑与布局探索，物理引擎负责微观参数调优，二者协同实现了**探索（exploration）与利用（exploitation）的平衡**。

3. **物理先验极大提升初始设计质量**  
   Design LLM结合SimReady资产与外部环境信息，在第一轮即生成接近可行解的设计，避免了传统随机初始化的低效搜索。

4. **实际可持续性影响显著**  
   在案例研究中，Phythesis设计的边缘数据中心实现PUE≈1.23，较行业标准1.3降低6.2%，相当于每年节省**31.2 GWh电能**，足以支撑新加坡500万人口城市近两天的居民用电。

### 方法的局限性
1. **模拟器精度限制**：当前使用的是组件级抽象模型，非完整CFD仿真，细节（如气流分布）未完全捕捉。
2. **资产库依赖性强**：“优化-再选择”策略可能导致局部最优，尤其在资产种类有限时。
3. **多目标优化尚未支持**：目前仅优化PUE，未考虑水耗（WUE）、碳排放、建设成本等其他可持续性指标。
4. **收敛性无理论保证**：进化过程依赖LLM输出稳定性，存在不确定性风险。

### 未来工作方向
- 支持更多冷却技术（如D2C液冷、背板换热器）
- 引入多模态编码器理解高维仿真场（flow field）
- 构建多目标优化框架（PUE + WUE + CAPEX）
- 开发针对LLM幻觉的细粒度微调策略（fine-tuning strategies）
- 探索区域资源条件感知的设计选址优化（siting-aware DC design）

---

> **总结**：Phythesis首次成功将LLM的语义生成能力与物理规律深度融合，构建了一个可用于工业级数据中心设计的自动化框架。其实验结果证明，**“语义+物理”双轮驱动**是实现高效、可靠、可持续基础设施设计的重要范式，为Generative AI在能源系统设计中的应用开辟了新路径。

</details>

---

### 14. [Multi-Granular Node Pruning for Circuit Discovery](https://arxiv.org/abs/2512.10903)

**Authors**: Muhammad Umair Haider, Hammad Rizwan, Hassan Sajjad, A. B. Siddique  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10903v1  

#### Abstract
Circuit discovery aims to identify minimal subnetworks that are responsible for specific behaviors in large language models (LLMs). Existing approaches primarily rely on iterative edge pruning, which is computationally expensive and limited to coarse-grained units such as attention heads or MLP bloc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Multi-Granular Node Pruning for Circuit Discovery 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **circuit discovery** 方法主要依赖于迭代式的 **edge pruning**，存在两个关键局限：
- **计算成本高**：需要大量前向传播和存储中间激活，内存开销大。
- **粒度粗糙**：仅在 **attention heads** 或 **MLP blocks** 等组件级别进行剪枝，无法探索更细粒度的结构（如单个神经元），导致电路发现不够精确。

此外，将整个组件视为“全有或全无”的单元，可能高估了实际参与任务的子网络规模。

### 提出的新方法与新思路
本文提出了一种全新的 **node-level pruning framework**，实现多粒度（multi-granular）的电路发现：
- 引入可学习的 **Hard Concrete masks**，作用于从 **transformer blocks、attention heads、MLPs 到 individual neurons** 的多个层级。
- 在统一优化目标下联合训练所有粒度的掩码，通过一个端到端的 fine-tuning 过程完成剪枝，无需迭代。
- 采用 **two-stream forward pass** 架构：
  - **Clean stream**：原始输入。
  - **Corrupted stream**：经过任务相关扰动（如修改时间、人名等）的输入。
  - 通过插值两者的激活并观察对输出的影响来衡量节点重要性。

该方法实现了从 **edge-level pruning** 向 **node-level pruning** 的范式转变。

### 相比现有方法的优势
- **更高的效率**：单次训练即可完成剪枝，避免了 O(n) 次前向传播。
- **更低的内存占用**：不需要缓存中间激活，内存消耗仅为基线方法的 **1/5–1/10**。
- **更精细的粒度**：支持 neuron-level 剪枝，揭示传统 coarse-grained 方法忽略的重要细节。
- **更小的电路规模**：发现的电路在参数数、节点数和边数上均显著更稀疏，同时保持甚至提升任务性能。
- **更强的可解释性**：揭示了不同任务下功能模块分布的差异性。

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
在 GPT-2 模型上评估以下三个典型 circuit discovery 任务：

| 任务 | 描述 |
|------|------|
| **Greater-Than (GT)** | 数值推理任务，判断结束年份是否大于起始年份（如 “The war lasted from 1743 to 17__”）。测试模型的时间比较能力。 |
| **Indirect Object Identification (IOI)** | 句法追踪任务，识别间接宾语（如 “Kristi gave it to ___” 应填 “Juana”）。需处理实体绑定与重复抑制。 |
| **Gendered Pronouns (GP)** | 社会关联任务，预测与名字性别一致的代词（如 “Evan is a great friend, isn’t ___?” → “he”）。反映模型内嵌的性别偏见。 |

### 实验设置
- **模型架构**：GPT-2 Small（12层，768维隐藏状态）
- **训练配置**：
  - Batch size: 32
  - Sequence length: 64
  - Epochs: IOI（500），GT/GP（200）
  - 使用 **Hard Concrete 分布** 参数化 masks，并施加 sparsity regularization。
- **硬件资源**：RTX 3090 GPU，64GB RAM

### 评估指标

#### 任务性能指标
- **GT-Score**: $ \mathbb{E}[P(y > y_{\text{start}} + 10|x) - P(y < y_{\text{start}} - 10|x)] $
- **IOI-Score**: $ \mathbb{E}[\text{logit}_{\text{correct}} - \text{logit}_{\text{incorrect}}] $
- **GP-Score**: $ \mathbb{E}[\text{logit}_{\text{consistent}} - \text{logit}_{\text{inconsistent}}] $

#### 忠实度指标
- **KL Divergence** ($ D_{KL}(P_m \| P_c) $)：衡量剪枝后电路输出分布与原模型的一致性。

#### 电路大小指标
- 参数数量、压缩比、各层级稀疏度（sparsity %）、等效边数（用于与 edge pruning 对比）

### 基线方法对比
- **EAP (Edge Attribution Patching)**：基于梯度的重要性评分指导边剪枝。
- **EP (Edge Pruning)**：使用可微分掩码进行边级剪枝（CoFi Pruning）。

> ⚠️ 注意：这些基线仅在 **component level**（如 attention heads）操作，不支持 neuron-level 或 MLP block 级别完全移除。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| Task | Base Logit Diff | Ours Logit Diff | KL ↓ |
|------|------------------|------------------|--------|
| IOI  | 3.1791           | 3.2030           | 0.6080 |
| GP   | 2.6198           | 2.6150           | 0.4909 |
| GT   | 0.3711           | 0.3912           | 0.0059 |

✅ 所有任务中，剪枝后的电路 **任务性能基本不变或略有提升**，且 **KL 散度极低**，说明忠实度高。

### 与基线方法对比（Table 3）

| Dataset | Method | Sparsity (%) ↑ | KL ↓ | Logit Diff ↑ | # Attn Heads ↓ |
|--------|--------|----------------|-------|---------------|----------------|
| IOI     | EAP    | 96.74          | 2.447 | -0.181        | 116            |
|         | EP     | 96.16          | 0.360 | 3.210         | 41             |
|         | **Ours** | **96.74**      | **0.600** | **3.203**     | **21**         |
| GP      | EAP    | 93.74          | 0.148 | 2.564         | 106            |
|         | EP     | —              | —     | —             | 106            |
|         | **Ours** | **93.74**      | **0.490** | **2.615**     | **37**         |
| GT      | EAP    | 95.95          | 0.086 | 0.374         | 113            |
|         | EP     | 96.69          | 0.039 | 0.389         | 74             |
|         | **Ours** | **95.95**      | **0.006** | **0.391**     | **28**         |

🔍 **核心优势体现**：
- 在 **IOI 和 GP 任务中，注意力头保留数量减少超过 60%**（如 IOI 从 41→21）。
- 尽管保留更少 heads，**KL 更低或相当，任务性能持平或更好**。
- **GT 任务中 KL 最低（0.006）且性能最高（0.391）**，表明剪枝可能去除了干扰信号。

### 电路稀疏性分析（Table 1）
| Granularity | IOI Sparsity (%) | GP Sparsity (%) | GT Sparsity (%) |
|------------|------------------|------------------|------------------|
| Attention Heads | 85.4 | 74.3 | 80.6 |
| Attention Neurons | 90.2 | 81.5 | 81.5 |
| MLP Hidden Neurons | 33.4 | 96.4 | 87.6 |
| MLP Output Neurons | 14.4 | 84.7 | 61.8 |

💡 发现：
- **MLP 输出神经元在第一层（Keys）比第二层（Values）更容易压缩**，符合 Geva et al. (2020) 的 KV-cache 观察。
- **IOI 任务中几乎所有 MLP blocks 都被保留**（sparsity 0%），而 **GP 和 GT 中大部分可被剪除**，说明任务机制差异显著。

### 内存与计算效率
- 本方法仅增加约 **55K 可训练 mask 参数**（相对于 124.5M 模型参数可忽略）。
- 所需内存为 **6,270 MB**（batch size=32）。
- 相比之下：
  - EAP 需要 **72,794 MB**
  - EP 需要 **33,354 MB**

👉 内存开销降低 **5–10倍**，极具部署优势。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Node-level pruning 是可行且高效的**：首次实现跨 block/head/neuron 多粒度联合剪枝，无需迭代。
2. ✅ **许多被 coarse 方法认为重要的组件内部存在大量冗余神经元**：真正关键的是少数特定神经元。
3. ✅ **不同任务具有不同的电路结构偏好**：
   - IOI：依赖广泛 MLP 活动 + 后几层 attention heads。
   - GP/GT：集中在特定 layer 的 sparse attention。
4. ✅ **剪枝不仅不损害性能，有时还能轻微提升**（如 GT 任务），可能因去除噪声路径增强信号提取。

### 方法的局限性
- ❌ **不恢复交互结构**：mask 能识别必要节点，但不能揭示节点间的连接关系（即“谁连谁”）。
- ❌ **缺乏方向性和动态性建模**：假设静态重要性，未捕捉运行时动态激活模式。
- ❌ **潜在安全风险**：若恶意使用，可能移除安全相关的 moderation pathways，导致 jailbreak 行为。

### 未来工作方向
- 🔮 探索 **hybrid node-edge pruning**：结合 node-level 效率与 edge-level 结构精细控制。
- 🔮 开发能推断 **interaction topology** 的方法，在 node selection 基础上重建通信路径。
- 🔮 将框架扩展至更大模型（如 Llama、Qwen）和其他架构（如 MoE）。
- 🔮 研究如何利用发现的电路进行可控编辑、干预或知识注入。

--- 

> 📌 总结：本文提出的 **multi-granular node pruning** 框架是 circuit discovery 领域的重要进展，它以更低的成本、更高的粒度发现了更紧凑、更忠实的任务专用子网络，推动了 LLM 可解释性的实用化进程。

</details>

---

### 15. [AgriGPT-Omni: A Unified Speech-Vision-Text Framework for Multilingual Agricultural Intelligence](https://arxiv.org/abs/2512.10624)

**Authors**: Bo Yang, Lanfei Feng, Yunkui Chen, Yu Zhang, Jianyu Zhang, Xiao Xu, Nueraili Aierken, Shijian Li  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.10624v1  

#### Abstract
Despite rapid advances in multimodal large language models, agricultural applications remain constrained by the lack of multilingual speech data, unified multimodal architectures, and comprehensive evaluation benchmarks. To address these challenges, we present AgriGPT-Omni, an agricultural omni-fram...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AgriGPT-Omni: A Unified Speech-Vision-Text Framework for Multilingual Agricultural Intelligence

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前农业领域的多模态人工智能系统存在三大瓶颈：
- **缺乏多语言语音数据**：现有农业AI模型主要依赖文本和图像，缺少真实场景下的语音输入支持，尤其在方言和低资源语言中更为稀缺。
- **缺乏统一的多模态架构**：大多数农业模型仅限于视觉-语言（Vision-Language）任务，未实现语音、图像、文本三者的深度融合。
- **缺乏标准化的全模态评估基准**：缺少覆盖语音、图像、文本的综合性农业评测集，难以公平比较不同模型的跨模态能力。

### 🚀 提出的新方法与创新
本文提出 **AgriGPT-Omni**，是首个面向农业领域的**统一语音-视觉-文本（Speech-Vision-Text）全模态框架**，其核心贡献包括：

#### （1）构建最大规模的多语言农业语音数据集
- 通过**合成+人工采集**混合流水线，生成：
  - **492K 合成语音样本**（基于 Agri-342K 和 AgriVL-150K 数据翻译后由 CosyVoice-0.5B 合成）
  - **1.4K 真实人类录音训练样本**
  - **586 高质量真实语音评测样本**
- 覆盖六种语言：中文、四川话、粤语、英语、日语、韩语
- 支持多种任务形式：问答（QA）、选择题、语音转录等

#### （2）设计三阶段训练范式（Three-stage Training Pipeline）
1. **Text Knowledge Injection**  
   - 继续预训练（2.2B 农业领域token）
   - 文本指令微调（342K QA）
   - 语音-文本对齐微调（342K “spoken question → textual answer”）

2. **Multimodal Knowledge Learning**
   - 冻结主干网络，分别进行视觉-语言、语音-语言适配器训练
   - 解冻后联合优化 800K 多模态 QA、50K 高质量 GPT-4o 样本、140K 语音+图像 QA

3. **GRPO-based Reinforcement Learning**
   - 使用 **Group Relative Policy Optimization (GRPO)** 进行偏好优化
   - 利用 2.5K 语音 QA、5K 语音+图像 QA、1.4K 人工录音转录数据提升输出稳定性与决策一致性

#### （3）发布首个农业全模态评测基准：AgriBench-Omni-2K
- 包含 **4 类任务**：
  - Audio QA（语音提问 → 文本回答）
  - Audio+Text Multiple Choice（语音提问 + 文本选项 → 选择答案）
  - Multimodal QA（语音 + 图像 → 文本回答）
  - Multimodal Multiple Choice（语音 + 图像 + 文本选项 → 选择答案）
- 覆盖 **6 种语言**，共 1,500 测试样本（100×6×4）
- 包含真实人类录音（586 条），用于评估现实鲁棒性
- 提供标准化评分协议与可复现评估脚本

### 🔍 相比现有方法的优势
| 方面 | AgriGPT-Omni | 现有农业模型（如 AgriGPT-VL、AgroLLM） |
|------|--------------|----------------------------------------|
| 模态完整性 | ✅ 语音+视觉+文本统一建模 | ❌ 多为单模态或双模态（仅文本/图像） |
| 语言多样性 | ✅ 支持6种语言（含方言） | ❌ 主要集中于英文或标准中文 |
| 数据真实性 | ✅ 含真实农民口音与噪声环境录音 | ❌ 多依赖合成或理想化数据 |
| 评估体系 | ✅ 全模态、多任务、真实语音评测 | ❌ 缺乏统一语音评测标准 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据类型 | 名称 | 规模 | 描述 |
|--------|------|-----|------|
| 文本数据 | Agri-342K | 342K QA 对 | 多语言农业知识问答 |
| 图像-文本对 | AgriVL-150K / Agri-VL-3M | ~150K–3M 对 | 图像描述与农事关联 |
| 合成语音 | 自建合成数据 | 492K 条 | 使用 Qwen2.5-72B 翻译 + CosyVoice 合成 |
| 真实语音 | Human-recorded Speech | 1.4K 训练 + 586 测评 | 多语言志愿者录制，模拟真实田野条件 |
| 强化学习数据 | GRPO Subset | 8,931 样本 | 结构清晰的选择题与转录任务，用于奖励建模 |

### ⚙️ 实验设置与评估指标

#### 模型架构基础
- 基座模型：**Qwen2.5-Omni**
- 参数量：8B（主版本）
- 模块组成：
  - Language Backbone（Qwen2.5）
  - Vision Encoder（CLIP-style）
  - Audio Encoder（Whisper-style）
  - Cross-modal Adapters（Vision-Language, Speech-Language）

#### 评估任务与指标
| 任务类型 | 输入 | 输出 | 主要指标 |
|--------|------|------|---------|
| Audio QA | 语音问题 | 自由文本回答 | Win Rate（vs 基线） |
| Audio MC | 语音问题 + 文本选项 | 选择答案 | Accuracy |
| Multimodal QA | 语音 + 图像 | 自由文本回答 | Win Rate |
| Multimodal MC | 语音 + 图像 + 文本选项 | 选择答案 | Accuracy |
| 通用能力测试 | 文本/图像/音频输入 | 多样输出 | MMLU, ARC, OpenBookQA, MMBench, Sample Audio 等 |

#### 基线方法对比
对比了以下代表性模型：
- 开源通用 MLLMs：`InternVL-3`, `LLaVA-1.5`, `MiniCPM-V`, `Yi-VL`, `Qwen2.5-VL`, `DeepSeek-VL2`
- 通用 Omni-models：`Qwen2.5-Omni-3B/7B`, `Megrez-Omni-3B`, `Step-Audio-2-mini`
- 商业模型：`Gemini 2.5 Flash/Pro`

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Tables 4–7）

#### （1）开放式语音问答（Audio QA）——Pairwise Win Rate
| 对比模型 | 平均胜率 |
|--------|--------|
| vs Qwen2.5-Omni-3B | **79.60%** |
| vs Qwen2.5-Omni-7B | **75.77%** |
| vs Megrez-Omni-3B | **98.74%** |
| vs Qwen2-Audio-7B | **97.85%** |
| vs Step-Audio-2-mini | **96.71%** |

> 在所有语言中均显著领先，尤其在方言（如四川话、粤语）上优势明显。

#### （2）语音选择题（Audio MC）——Accuracy
| 模型 | 准确率（ACC） |
|------|-------------|
| Qwen2.5-Omni-7B | 83.83% |
| Step-Audio-2-mini | 80.83% |
| **AgriGPT-Omni (ours)** | **92.67%** |

> **平均高出约 9–12 个百分点**，表明领域专业化显著增强理解能力。

#### （3）多模态语音问答（Speech + Image → Text）——Win Rate
| 对比模型 | 平均胜率 |
|--------|--------|
| vs Qwen2.5-Omni-3B | **94.85%** |
| vs Qwen2.5-Omni-7B | **89.89%** |
| vs Megrez-Omni-3B | **94.46%** |

#### （4）多模态选择题（Speech + Image + Text → Answer）——Accuracy
| 模型 | 准确率（ACC） |
|------|-------------|
| Qwen2.5-Omni-7B | 59.17% |
| Megrez-Omni-3B | 50.83% |
| **AgriGPT-Omni (ours)** | **75.67%** |

> **绝对提升超过 16 个百分点**，体现强大的跨模态融合能力。

---

### 🔍 消融实验结果（Ablation Study, Figure 5）

| 阶段 | 相比前一阶段的提升趋势 |
|------|-----------------------|
| Phase 1（文本注入） | 显著提升语言理解和指令遵循能力 |
| Phase 2（多模态对齐） | 明显改善视觉与语音信息融合效果 |
| Phase 3（GRPO强化学习） | 最大增益，特别是在选择题准确率和输出稳定性方面 |

> 所有任务上的性能呈**单调上升趋势**，验证三阶段训练的有效性和互补性。

---

### 🧪 真实语音鲁棒性评估（Table 3）
| 输入类型对比 | 胜 | 平 | 负 |
|-------------|----|----|----|
| 合成语音 vs 真实语音 | 234 | 111 | 232 |

> 模型在真实录音上的表现与合成语音相当（接近平局），说明具备良好的**现实部署鲁棒性**，不依赖高质量录音设备或标准发音。

---

### 🌐 通用能力保留测试（Table 8）
| 基准 | Qwen2.5-Omni-7B | AgriGPT-Omni-7B |
|------|------------------|------------------|
| MMLU (Text) | 49.97% | **62.81%** (+12.8) |
| OpenBookQA (Text) | 60.34% | **80.74%** (+20.4) |
| ARC (Text) | 81.69% | 82.19% |
| MMBench (Vision) | 83.45% | 82.51% |
| Sample Audio (Audio) | 75.57% | 74.49% |

> 表明：**领域专用训练不仅没有损害通用能力，反而提升了部分文本推理性能**，证明模型具有强泛化性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **农业全模态智能必须整合语音接口**：语音是最自然的农民交互方式，现有研究严重忽视该模态。
2. **合成数据结合少量真实录音即可有效训练鲁棒模型**：通过高质量TTS合成大规模数据，并辅以少量真实录音进行GRPO优化，能显著提升现实适应能力。
3. **三阶段训练范式有效解耦复杂学习过程**：
   - 先打牢语言基础
   - 再建立跨模态映射
   - 最后通过强化学习稳定输出
4. **领域专业化可同时提升专业与通用能力**：针对性训练反而增强了模型在开放域的理解力（如 MMLU 提升 12.8%），挑战“过拟合”假设。

### ⚠️ 局限性
- 当前语音数据仍偏少（尤其是真实录音），未来需扩大田野采集规模。
- 尚未支持语音输出（TTS 回答），仅实现语音输入理解。
- 方言识别虽优于基线，但仍存在误识别现象（如四川话与普通话混淆）。
- GRPO 依赖精确答案结构，在开放式农业咨询任务中应用受限。

### 🔮 未来工作方向
- 构建端到端语音对话系统（支持语音输入+语音输出）
- 扩展至更多低资源语言与地方方言
- 接入实地传感器数据（温湿度、土壤pH等）形成感知-决策闭环
- 推动开源生态建设，鼓励社区共建农业多模态数据集与工具链

---

## 📢 总结
**AgriGPT-Omni 是农业AI迈向“全模态交互”的里程碑工作**。它首次实现了语音、图像、文本在农业场景中的统一建模，提出了完整的“数据-模型-评测”三位一体解决方案。实验表明，该模型在多语言语音理解、跨模态推理、真实环境鲁棒性等方面全面超越通用模型，且保持甚至提升了通用能力。作者承诺将**开源全部模型、数据与评测工具**，有望推动全球低资源地区农业智能化发展。

</details>

---

### 16. [Computational emotion analysis with multimodal LLMs: Current evidence on an emerging methodological opportunity](https://arxiv.org/abs/2512.10882)

**Authors**: Hauke Licht  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.10882v1  

#### Abstract
Emotions are central to politics and analyzing their role in political communication has a long tradition. As research increasingly leverages audio-visual materials to analyze the display of emotions, the emergence of multimodal generative AI promises great advances. However, we lack evidence about ...

---

### 17. [Multi-Objective Reward and Preference Optimization: Theory and Algorithms](https://arxiv.org/abs/2512.10601)

**Authors**: Akhil Agnihotri  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.10601v1  

#### Abstract
This thesis develops theoretical frameworks and algorithms that advance constrained reinforcement learning (RL) across control, preference learning, and alignment of large language models. The first contribution addresses constrained Markov Decision Processes (CMDPs) under the average-cost criterion...

---

### 18. [User-Feedback-Driven Continual Adaptation for Vision-and-Language Navigation](https://arxiv.org/abs/2512.10322)

**Authors**: Yongqiang Yu, Xuhui Li, Hazza Mahmood, Jinxing Zhou, Haodong Hong, Longtao Jiang, Zhiqiang Xu, Qi Wu, Xiaojun Chang  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10322v1  

#### Abstract
Vision-and-Language Navigation (VLN) requires agents to navigate complex environments by following natural-language instructions. General Scene Adaptation for VLN (GSA-VLN) shifts the focus from zero-shot generalization to continual, environment-specific adaptation, narrowing the gap between static ...

---

### 19. [CloudFix: Automated Policy Repair for Cloud Access Control Policies Using Large Language Models](https://arxiv.org/abs/2512.09957)

**Authors**: Bethel Hall, Owen Ungaro, William Eiers  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09957v1  

#### Abstract
Access control policies are vital for securing modern cloud computing, where organizations must manage access to sensitive data across thousands of users in distributed system settings. Cloud administrators typically write and update policies manually, which can be an error-prone and time-consuming ...

---

### 20. [ESS: An Offload-Centric Latent-Cache Management Architecture for DeepSeek-V3.2-Exp](https://arxiv.org/abs/2512.10576)

**Authors**: Xinhang Chen, Chao Zhang, Jiahuan He, Wei Liu, Jianming Zhang, Wenlong Zhou, Xiao Li, Pai Zeng, Shiyong Li, Yuanpan Qian, Dong Li, Zhaogeng Li  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10576v1  

#### Abstract
DeepSeek-V3.2-Exp introduces a sparse attention mechanism that significantly reduces inference latency in long-context scenarios. Although the overall throughput has improved greatly, the Decode-stage of PD disaggregation remains to be a major bottleneck. This bottleneck primarily stems from the con...

---

### 21. [\textsc{Text2Graph}: Combining Lightweight LLMs and GNNs for Efficient Text Classification in Label-Scarce Scenarios](https://arxiv.org/abs/2512.10061)

**Authors**: Jo\~ao Lucas Luz Lima Sarcinelli, Ricardo Marcondes Marcacini  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10061v1  

#### Abstract
Large Language Models (LLMs) have become effective zero-shot classifiers, but their high computational requirements and environmental costs limit their practicality for large-scale annotation in high-performance computing (HPC) environments. To support more sustainable workflows, we present \textsc{...

---

### 22. [Murmur2Vec: A Hashing Based Solution For Embedding Generation Of COVID-19 Spike Sequences](https://arxiv.org/abs/2512.10147)

**Authors**: Sarwan Ali, Taslim Murad  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10147v1  

#### Abstract
Early detection and characterization of coronavirus disease (COVID-19), caused by SARS-CoV-2, remain critical for effective clinical response and public-health planning. The global availability of large-scale viral sequence data presents significant opportunities for computational analysis; however,...

---

### 23. [Unlocking the Address Book: Dissecting the Sparse Semantic Structure of LLM Key-Value Caches via Sparse Autoencoders](https://arxiv.org/abs/2512.10547)

**Authors**: Qingsen Ma, Dianyun Wang, Jiaming Lyu, Yaoye Wang, Lechen Ning, Sujie Zhu, Zhenbo Xu, Liuyu Xiang, Huining Li, Huijia Wu, Zhaofeng He  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10547v1  

#### Abstract
The Key-Value (KV) cache is the primary memory bottleneck in long-context Large Language Models, yet it is typically treated as an opaque numerical tensor. In this work, we propose \textbf{STA-Attention}, a framework that utilizes Top-K Sparse Autoencoders (SAEs) to decompose the KV cache into inter...

---

### 24. [Uncertainty-Preserving QBNNs: Multi-Level Quantization of SVI-Based Bayesian Neural Networks for Image Classification](https://arxiv.org/abs/2512.10602)

**Authors**: Hendrik Borras, Yong Wu, Bernhard Klein, Holger Fr\"oning  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.10602v1  

#### Abstract
Bayesian Neural Networks (BNNs) provide principled uncertainty quantification but suffer from substantial computational and memory overhead compared to deterministic networks. While quantization techniques have successfully reduced resource requirements in standard deep learning models, their applic...

---

### 25. [Interpretable Embeddings with Sparse Autoencoders: A Data Analysis Toolkit](https://arxiv.org/abs/2512.10092)

**Authors**: Nick Jiang, Xiaoqing Sun, Lisa Dunlap, Lewis Smith, Neel Nanda  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10092v1  

#### Abstract
Analyzing large-scale text corpora is a core challenge in machine learning, crucial for tasks like identifying undesirable model behaviors or biases in training data. Current methods often rely on costly LLM-based techniques (e.g. annotating dataset differences) or dense embedding models (e.g. for c...

---

### 26. [Investigating The Functional Roles of Attention Heads in Vision Language Models: Evidence for Reasoning Modules](https://arxiv.org/abs/2512.10300)

**Authors**: Yanbei Jiang, Xueqi Ma, Shu Liu, Sarah Monazam Erfani, Tongliang Liu, James Bailey, Jey Han Lau, Krista A. Ehinger  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10300v1  

#### Abstract
Despite excelling on multimodal benchmarks, vision-language models (VLMs) largely remain a black box. In this paper, we propose a novel interpretability framework to systematically analyze the internal mechanisms of VLMs, focusing on the functional roles of attention heads in multimodal reasoning. T...

---

### 27. [Remember Me, Refine Me: A Dynamic Procedural Memory Framework for Experience-Driven Agent Evolution](https://arxiv.org/abs/2512.10696)

**Authors**: Zouying Cao, Jiaji Deng, Li Yu, Weikang Zhou, Zhaoyang Liu, Bolin Ding, Hai Zhao  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10696v1  

#### Abstract
Procedural memory enables large language model (LLM) agents to internalize "how-to" knowledge, theoretically reducing redundant trial-and-error. However, existing frameworks predominantly suffer from a "passive accumulation" paradigm, treating memory as a static append-only archive. To bridge the ga...

---

### 28. [Decoding Student Minds: Leveraging Conversational Agents for Psychological and Learning Analysis](https://arxiv.org/abs/2512.10441)

**Authors**: Nour El Houda Ben Chaabene, Hamza Hammami, Laid Kahloul  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10441v1  

#### Abstract
This paper presents a psychologically-aware conversational agent designed to enhance both learning performance and emotional well-being in educational settings. The system combines Large Language Models (LLMs), a knowledge graph-enhanced BERT (KG-BERT), and a bidirectional Long Short-Term Memory (LS...

---

### 29. [Local LLM Ensembles for Zero-shot Portuguese Named Entity Recognition](https://arxiv.org/abs/2512.10043)

**Authors**: Jo\~ao Lucas Luz Lima Sarcinelli, Diego Furtado Silva  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10043v1  

#### Abstract
Large Language Models (LLMs) excel in many Natural Language Processing (NLP) tasks through in-context learning but often under-perform in Named Entity Recognition (NER), especially for lower-resource languages like Portuguese. While open-weight LLMs enable local deployment, no single model dominates...

---

### 30. [Assessing Neuromorphic Computing for Fingertip Force Decoding from Electromyography](https://arxiv.org/abs/2512.10179)

**Authors**: Abolfazl Shahrooei, Luke Arthur, Om Patel, Derek Kamper  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.10179v1  

#### Abstract
High-density surface electromyography (HD-sEMG) provides a noninvasive neural interface for assistive and rehabilitation control, but mapping neural activity to user motor intent remains challenging. We assess a spiking neural network (SNN) as a neuromorphic architecture against a temporal convoluti...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
