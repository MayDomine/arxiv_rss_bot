# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-24 06:00:44 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ActionFlow: A Pipelined Action Acceleration for Vision Language Models on Edge](https://arxiv.org/abs/2512.20276)

**Authors**: Yuntao Dai, Hang Gu, Teng Wang, Qianyu Cheng, Yifei Zheng, Zhiyong Qiu, Lei Gong, Wenqi Lou, Xuehai Zhou  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.20276v1  

#### Abstract
Vision-Language-Action (VLA) models have emerged as a unified paradigm for robotic perception and control, enabling emergent generalization and long-horizon task execution. However, their deployment in dynamic, real-world environments is severely hin dered by high inference latency. While smooth rob...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《ActionFlow: A Pipelined Action Acceleration for Vision Language Models on Edge》核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

Vision-Language-Action (VLA) 模型在机器人感知与控制中展现出强大的泛化能力和长程任务执行潜力，但其**高推理延迟**严重阻碍了在动态真实环境中的部署。当前主流 VLA 模型在边缘设备上仅能实现 3–5 FPS 的推理速度，远低于机器人实时交互所需的 20–30 Hz 控制频率。

该延迟主要源于 LLM 背骨的**自回归解码（autoregressive decoding）过程**，其为内存受限（memory-bound）操作，硬件利用率低。现有优化方法存在以下不足：
- **通用 LLM 推理引擎**（如 Continuous Batching）面向多用户服务器场景，不适用于单用户、低延迟的机器人控制。
- **边缘端优化**（如量化、蒸馏）虽减小模型规模，但未改变解码阶段的串行瓶颈。
- **算法级 VLA 加速**（如并行解码、扩散模型）常需重新训练，可能牺牲精度或引入动作不连续性。

---

### **提出了什么新方法或新思路**

本文提出 **ActionFlow** ——一种专为资源受限边缘平台设计的系统级推理框架，其核心创新如下：

#### ✅ **Cross-Request Pipelining 策略**
- 将单个 VLA 请求内部的生成过程（一次 Prefill + 多次 Decode）视为一个“微请求流水线”（micro-request pipeline）。
- 在时间维度上重叠多个连续请求的计算：将当前帧的 **Prefill 阶段**（计算密集型）与前几帧的 **Decode 阶段**（内存密集型）进行跨请求批处理（batching），形成宏流水线（macro-pipeline）。
- 实现了**单用户场景下的内部批处理机会挖掘**，显著提升 GPU 利用率。

#### ✅ **Cross-Request State Packed Forward 算子**
- 设计了一个融合算子，将多个独立的 Decode 阶段中的小规模矩阵-向量乘法（GEMV）聚合为一个大规模矩阵-矩阵乘法（GEMM）。
- 显著提高**算术强度**（arithmetic intensity），使工作负载从 memory-bound 转向 compute-bound，更充分地利用 GPU 峰值算力。

#### ✅ **Unified KV Ring Buffer**
- 所有活跃请求的 KV Cache 统一存储在一个物理连续的环形缓冲区中。
- 支持 Variable-Length Attention（Varlen-Attention）高效访问不同长度的历史上下文。
- 通过 `FUSEDROPEANDWRITEKV` 和 `INPLACESHIFTKV` 内核融合技术，避免 CPU-GPU 同步开销和动态内存拷贝。

---

### **相比现有方法的优势**

| 维度 | ActionFlow |
|------|-----------|
| **是否需要重训练** | ❌ 不需要，纯系统级优化 |
| **是否依赖外部多用户请求** | ❌ 否，适用于单用户机器人场景 |
| **对硬件利用率提升机制** | ✅ 通过内部微请求批处理提升 |
| **与其它优化正交性** | ✅ 可与量化、Speculative Decoding 等结合 |
| **功能正确性保障** | ✅ 输出等价于原生自回归推理 |

---

## 2. 核心实验方法和设置

### **使用的模型与平台**

- **模型**：OpenVLA-7B（70亿参数）
- **硬件平台**：
  - **NVIDIA Jetson AGX Orin (64GB)**：代表资源受限的嵌入式机器人设备
  - **NVIDIA RTX 5090**：代表高性能边缘工作站
- **软件栈**：PyTorch 2.6.0, Transformers 4.49.0, CUDA 12.6

---

### **实验设置与评估指标**

#### **评估指标**
- **FPS**（Frames Per Second）：核心吞吐量指标，衡量每秒可完成的推理帧数。
- **Speedup**：相对于 Baseline 的加速比。
- **Success Rate**：在 LIBERO 基准上的任务成功率，验证功能无损。

#### **对比方法**
| 方法 | 描述 |
|------|------|
| **Baseline** | 原始 OpenVLA-7B 自回归推理，无任何系统优化 |
| **Naive Pipe** | 实现了 Cross-Request Pipelining，但使用动态 KV 缓存拼接和 CPU 协调批处理（无内核融合） |
| **ActionFlow** | 完整方案：包含 Cross-Request Pipelining + Cross-Request State Packed Forward + Unified KV Ring Buffer |

#### **敏感性分析**
- 变化 **Prefill 长度**（文本指令长度 + 256 视觉 token）
- 变化 **Decode 长度 K ∈ {7, 16, 24, 32}**，模拟不同复杂度的动作序列生成任务

#### **功能验证基准**
- **LIBERO Benchmark Suite**：涵盖空间操作、物体操作、目标达成、长程任务四大类，用于测试任务成功率。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| Method | Platform | FPS | Speedup |
|--------|----------|-----|---------|
| Baseline | AGX Orin | 1.25 ± 0.01 | 1.00x |
| Naive Pipe | AGX Orin | 2.70 ± 0.19 | 2.16x |
| **ActionFlow** | **AGX Orin** | **3.20 ± 0.17** | **2.56x** |
| Baseline | RTX 5090 | 7.62 ± 0.14 | 1.00x |
| Naive Pipe | RTX 5090 | 15.60 ± 0.08 | 2.04x |
| **ActionFlow** | **RTX 5090** | **19.45 ± 0.22** | **2.55x** |

> ✅ **结论**：ActionFlow 在两类平台上均实现约 **2.55x 的稳定加速**，且无需模型修改。

---

### **消融实验结果**

- **ActionFlow vs. Naive Pipe**：
  - 在 AGX Orin 上带来额外 **18.5%** 性能提升（3.20 vs 2.70 FPS）
  - 在 RTX 5090 上带来额外 **24.7%** 提升（19.45 vs 15.60 FPS）
- **说明**：`Cross-Request State Packed Forward` 和 `Unified KV Ring Buffer` 的内核融合设计至关重要，有效消除了 CPU-GPU 同步气泡和内存拷贝开销。

---

### **敏感性分析（Figure 5）**

- **重负载下优势更明显**：
  - 当 K=32 且 Prefill 较长时：
    - RTX 5090：Baseline 下降至 2.36 FPS，ActionFlow 仍达 9.58 FPS → **4.06× 加速**
    - AGX Orin：Baseline 降至 0.30 FPS，ActionFlow 保持 1.31 FPS → **4.36× 加速**
- **对 Decode 长度鲁棒性强**：
  - Baseline 在 K 从 7 增至 32 时，RTX 5090 吞吐下降 **72%**
  - ActionFlow 仅下降 **25%**，表明其成功摊销了自回归的串行开销。

---

### **功能正确性验证（Table 2）**

| Method | Platform | Spatial | Object | Goal | Long |
|--------|----------|--------|--------|------|------|
| OpenVLA (Baseline) | RTX 5090 | 84.4% | 73.8% | 74.4% | 51.4% |
| ActionFlow | RTX 5090 | 84.3% | 71.2% | 78.6% | 53.3% |
| ActionFlow | AGX Orin | 83.4% | 68.8% | 75.6% | 49.0% |

> ✅ **结论**：ActionFlow 在所有任务类别中均保持与 Baseline 相当的成功率，波动在统计误差范围内，**功能无损**。

---

## 4. 关键结论和发现

### **主要发现**

1. **VLA 推理瓶颈本质是 Decode 阶段的低效性**：其低算术强度导致 GPU 计算单元闲置，形成“Decode Bubble”。
2. **单用户场景也可创造批处理机会**：通过将连续请求的 Prefill 与历史 Decode 流水线化，可在边缘设备上实现高效的内部批处理。
3. **系统级优化可独立于算法改进**：ActionFlow 是完全正交于量化、蒸馏、并行解码等方法的纯系统优化，可叠加使用。
4. **内核融合是释放性能的关键**：`Unified KV Ring Buffer` + `FUSEDROPEANDWRITEKV` + `INPLACESHIFTKV` 共同消除同步与内存管理开销，是实现接近理论加速上限的基础。

---

### **方法的局限性**

- **依赖固定动作序列长度 K**：当前设计假设每个请求生成固定数量的动作 token（K 步）。若任务需动态终止（early exit），需扩展支持变长输出。
- **初始延迟仍存在**：首个动作仍需完整 Prefill + K 步 Decode，仅后续帧受益于流水线。
- **KV Cache 内存占用增加**：由于维护 K 个并发请求的状态，KV Cache 总量约为原始的 K 倍，在极端内存受限场景可能成为瓶颈。

---

### **未来工作方向**

1. **支持动态动作序列长度**：结合 early-exit 或流式生成机制，进一步降低首帧延迟。
2. **与算法级优化协同设计**：探索与 Speculative Decoding、Action Chunking 等方法的联合优化。
3. **扩展至多模态输入流**：支持音频、触觉等更多感知模态的实时融合推理。
4. **部署到真实机器人系统**：在移动机器人、机械臂等实际平台上验证端到端控制稳定性与响应能力。

---

> 🔗 **项目地址**：https://anonymous.4open.science/r/ActionFlow-1D47

</details>

---

### 2. [MoE-DiffuSeq: Enhancing Long-Document Diffusion Models with Sparse Attention and Mixture of Experts](https://arxiv.org/abs/2512.20604)

**Authors**: Alexandros Christoforos, Chadbourne Davis  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.20604v1  

#### Abstract
We present MoE-DiffuSeq, a mixture of experts based framework for enhancing diffusion models in long document generation. Existing diffusion based text generation models, such as DiffuSeq, suffer from high computational cost and memory overhead when applied to extended sequences. To address these ch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-DiffuSeq: Enhancing Long-Document Diffusion Models with Sparse Attention and Mixture of Experts  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
传统 Diffusion 模型（如 DiffuSeq）在生成长文本时面临严重的**计算效率低下**和**内存消耗过高**的问题，主要源于 Transformer 架构中自注意力机制的 $O(n^2)$ 时间复杂度。此外，现有模型在处理科学文章、代码仓库或长对话等需要长上下文理解的任务时，往往因序列长度限制而表现不佳。

### **提出了什么新方法或新思路**
本文提出 **MoE-DiffuSeq**，一种结合 **Mixture of Experts (MoE)** 与 **DiffuSeq** 框架，并引入 **Sparse Attention** 和 **Soft Absorbing State** 的新型扩散语言模型。其核心创新包括：

- **MoE + Diffusion 融合架构**：首次将 MoE 动态专家选择机制集成到 Diffusion 序列生成框架中，实现对不同文本段落按需激活最相关专家，提升模型容量的同时控制计算开销。
- **定制化稀疏注意力机制**：采用 Longformer 风格的滑动窗口注意力（Sliding Window Attention），并引入**空洞滑窗（Dilated Sliding Window）** 扩展感受野，在不增加计算量的前提下捕捉长距离依赖。
- **全局注意力机制**：为关键 token（如 [CLS]）保留全局注意力能力，确保整体语义一致性。
- **软吸收状态（Soft Absorbing State）**：改进扩散过程中的噪声建模，优化反向去噪路径，加速序列重建并提高精度。
- **集成 DPM-solver++ 加速推理**：显著减少所需 diffusion steps，提升采样速度而不牺牲质量。

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **效率** | 显著降低训练和推理时间，尤其适用于长序列（>8k tokens）场景 |
| **可扩展性** | MoE 设计支持更大参数规模，同时保持高效计算资源利用 |
| **生成质量** | 在多个任务上优于 DiffuSeq 和 Longformer，语义连贯性和多样性更高 |
| **适用性** | 特别适合科学文献生成、代码生成、多轮对话等长文档任务 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
实验涵盖四类典型长文本生成任务：
- **Arxiv Abstract Dataset**：用于评估科学文本摘要生成能力。
- **HotpotQA**：测试多跳问答与跨文档推理能力。
- **Commonsense Conversation Dataset**：衡量对话系统的情境理解和响应生成质量。
- **Quora Question Pairs (QQP)**：评估语义保持下的文本改写（paraphrasing）准确性。

### **实验设置**
- **模型结构**：12层 Transformer，每层12个 attention heads。
- **稀疏注意力配置**：滑动窗口大小为512，部分实验尝试256/1024；使用 dilated attention 扩展上下文感知范围。
- **MoE 设置**：每层配备多个 expert network，通过 gating function 动态选择 top-k 专家。
- **扩散步骤**：默认使用 2048 步，部分实验调整至 1024 或 4096 进行对比。
- **训练策略**：分阶段增大窗口尺寸和序列长度，采用平方根噪声调度（square-root noise schedule）。
- **硬件平台**：NVIDIA A100 GPU。

### **评估指标**
| 指标 | 用途说明 |
|------|----------|
| **BLEU** | 衡量生成文本与参考文本之间的 n-gram 匹配精度 |
| **ROUGE (R1/R2/RL)** | 基于召回率评估内容覆盖度，常用于摘要任务 |
| **BERTScore** | 利用 BERT 上下文嵌入计算语义相似性，反映语义保真度 |
| **Answer EM/F1 & Support EM/F1** | HotpotQA 专用指标，分别评价答案精确匹配/模糊匹配及证据句子识别效果 |
| **Accuracy** | QQP 数据集上的分类准确率（是否为同义句） |
| **Inference Time / 2-gram Novelty** | 推理延迟与生成文本新颖性平衡分析 |

### **基线方法对比**
- **DiffuSeq (Gong et al., 2023)**：基础扩散语言模型，作为直接对照。
- **Longformer (Beltagy et al., 2020)**：经典稀疏注意力模型，擅长处理长文本。
- **GPT-4 (Achiam et al., 2023)**：通用大模型标杆，提供高性能上限参考。

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

#### ✅ Arxiv Abstract Dataset（ROUGE 分数）
| Model | R1 | R2 | RL |
|-------|----|----|-----|
| Longformer | 41.44 | 17.52 | 38.70 |
| DiffuSeq | 39.12 | 16.43 | 37.88 |
| **MoE-DiffuSeq** | **44.41** | **18.73** | **39.89** |

> **结论**：MoE-DiffuSeq 在所有 ROUGE 指标上均显著领先，表明其在科学摘要生成中具有更强的信息提取与组织能力。

#### ✅ HotpotQA Dataset
| Model | Answer EM/F1 | Support EM/F1 |
|--------|----------------|----------------|
| Longformer | 71.21 / 82.42 | 65.11 / 89.50 |
| DiffuSeq | 70.91 / 81.43 | 64.60 / 88.51 |
| **MoE-DiffuSeq** | **72.88 / 85.42** | **66.69 / 90.40** |

> **结论**：在多跳推理和支撑句识别方面全面超越基线，体现其强大的长程依赖建模能力。

#### ✅ Commonsense Conversation Dataset
| Model | BLEU | ROUGE-L | BERTScore |
|--------|--------|-----------|-------------|
| Longformer | 0.030 | 0.139 | 0.602 |
| DiffuSeq | 0.022 | 0.119 | 0.501 |
| **MoE-DiffuSeq** | **0.049** | **0.233** | **0.628** |

> **结论**：在对话生成任务中展现出更高的语言多样性和语义准确性。

#### ✅ QQP Dataset（Paraphrase Accuracy）
| Model | Accuracy |
|--------|----------|
| Longformer | 92.3 |
| DiffuSeq | 91.7 |
| **MoE-DiffuSeq** | **95.3** |

> **结论**：在语义不变前提下的文本重构任务中表现最优，说明其具备优秀的语义理解与表达能力。

---

### **消融实验结果（Ablation Study）**
在 Arxiv 数据集上进行组件分析（见 Table 5）：

| Configuration | Attention Type | Diffusion Steps | Window Size | BLEU/ROUGE/BERTScore |
|---------------|----------------|------------------|--------------|----------------------------|
| Full Model (Baseline) | Sparse | 2048 | 512 | **44.41 / 18.73 / 39.89** |
| No Sparse Attention | Standard | 2048 | 512 | 42.52 / 17.99 / 38.41 |
| Reduced Steps | Sparse | 1024 | 512 | 43.11 / 18.03 / 39.26 |
| Increased Steps | Sparse | 4096 | 512 | — |
| Smaller Window | Sparse | 2048 | 256 | 44.40 / 18.66 / 39.92 |
| Larger Window | Sparse | 2048 | 1024 | — |

> **发现**：
- 移除 **Sparse Attention** 导致性能明显下降 → 验证其对长序列处理的关键作用。
- 减少 diffusion steps 对性能影响较小，但进一步增加收益有限 → 支持使用 DPM-solver++ 加速推理的合理性。
- 窗口大小变化影响较弱，说明 **dilated attention** 已有效扩展上下文感知能力。

---

### **推理效率与新颖性权衡（Figure 3）**
- MoE-DiffuSeq 在 **2-gram Novelty** 上显著高于 DiffuSeq 和 Longformer，说明生成内容更具多样性。
- 推理时间略慢于 Longformer，但远快于 DiffuSeq，且新颖性远超两者。
- 实现了“高新颖性”与“低延迟”的良好平衡。

---

## 4. 关键结论和发现

### **主要发现**
1. **MoE 与 Diffusion 的融合是可行且高效的**：MoE-DiffuSeq 成功将 MoE 的动态计算分配优势引入扩散模型，提升了长文本生成的效率与质量。
2. **Sparse Attention 是处理长序列的核心技术**：滑动窗口 + dilated attention + global attention 的组合有效缓解了 $O(n^2)$ 复杂度瓶颈。
3. **模型在多种任务上全面超越主流基线**：无论是在摘要、问答、对话还是 paraphrasing 任务中，MoE-DiffuSeq 均取得 SOTA 或接近 SOTA 的表现。
4. **兼顾生成速度与质量**：通过减少 diffusion steps 和优化 attention 结构，实现了快速采样与高质量输出的统一。

### **方法的局限性**
- 当前模型仍依赖大量 GPU 资源训练（A100），部署成本较高。
- MoE 的负载均衡问题未深入探讨，可能存在专家利用率不均的情况。
- 尚未在图像-文本等 multimodal 场景中验证泛化能力。

### **未来工作方向**
- 探索 MoE-DiffuSeq 在 **code generation** 和 **scientific paper writing** 中的实际应用。
- 引入 **multimodal inputs**（如图表、公式）以增强科学文本生成能力。
- 优化 MoE routing 策略，提升专家利用率与训练稳定性。
- 进一步压缩 diffusion steps，探索 **real-time generation** 可能性。

---

> 📌 **总结一句话**：  
> **MoE-DiffuSeq 通过融合 Mixture of Experts 与稀疏注意力机制，构建了一个高效、可扩展且高质量的长文本扩散生成模型，在多项任务中显著优于 DiffuSeq 和 Longformer，为下一代长文档生成系统提供了新范式。**

</details>

---

### 3. [SHIRO: Near-Optimal Communication Strategies for Distributed Sparse Matrix Multiplication](https://arxiv.org/abs/2512.20178)

**Authors**: Chen Zhuang, Lingqi Zhang, Benjamin Brock, Du Wu, Peng Chen, Toshio Endo, Satoshi Matsuoka, Mohamed Wahib  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.20178v1  

#### Abstract
Distributed Sparse Matrix-Matrix Multiplication (SpMM) is a fundamental operation in numerous high-performance computing and deep learning applications. The major performance bottleneck in distributed SpMM lies in the substantial communication overhead, which limits both performance and scalability....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SHIRO: Near-Optimal Communication Strategies for Distributed Sparse Matrix Multiplication

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

分布式稀疏矩阵乘法（**Distributed SpMM**）是高性能计算和深度学习（如 **GNNs**）中的核心操作，其性能瓶颈主要在于**通信开销过大**。现有方法在两个层面存在通信效率低下的问题：

1. **通信策略冗余**：
   - **Sparsity-oblivious** 方法（如 CAGNET、BCL）传输整个稠密块，忽略稀疏性，导致大量无用数据传输。
   - **Sparsity-aware** 方法（如 SPA、CoLa）虽基于列索引或行索引进行优化，但仍只利用单维度稀疏信息，未能充分挖掘稀疏模式。

2. **忽略层次化网络拓扑**：
   - 多数方法采用扁平化的 `all-to-all` 通信，未考虑现代 GPU 集群中**节点内高速链路（NVLink）与节点间低速链路（InfiniBand）的带宽差异**，导致跨节点重复传输相同数据。

---

### **提出了什么新方法或新思路**

作者提出 **SHIRO** —— 一个通信高效的分布式 SpMM 框架，从两个层面联合优化通信：

#### ✅ 联合行列稀疏感知通信策略（Joint Row-Column Sparsity-Aware Communication）
- 将每个非零元的通信决策建模为**最小加权覆盖问题（Minimum Weighted Covering Problem）**。
- 构造二分图，将行索引和列索引作为顶点，非零元作为边，通过 **Max-Flow / Min-Cut** 算法求解最优通信方案。
- 动态选择对每个非零元使用 **row-based**（发送部分 C 结果）还是 **column-based**（请求对应 B 行）策略，实现全局通信量最小。

#### ✅ 层次化通信策略（Hierarchical Communication Strategy）
- 针对多级网络架构（如节点内 NVLink + 节点间 InfiniBand），设计两级通信机制：
  - **Column-based**：采用“源组聚合 → 跨组传输 → 组内分发”三步法，避免重复跨节点拉取 B 行。
  - **Row-based**：采用“组内预聚合 → 跨组传输”两步法，减少跨节点发送的部分 C 结果数量。
- 设计**重叠调度策略**（Overlapping Scheduling）：利用 row-based 和 column-based 通信在不同层级链路上互补的特点，实现双层链路同时高效利用。

---

### **相比现有方法的优势**

| 优势维度 | 具体表现 |
|--------|---------|
| **通信体积更小** | 利用完整稀疏模式，显著降低总通信量（最高减少 96.3%） |
| **网络利用率更高** | 减少跨节点冗余通信，提升慢速链路利用率 |
| **可扩展性更强** | 在 128 个 GPU 上仍保持良好强扩展性 |
| **通用性强** | 可适配任意分区策略（文中以 1D row-partition 为主） |

---

## 2. 核心实验方法和设置

### **使用的数据集**

来自 **SuiteSparse Matrix Collection** 的真实世界稀疏矩阵，涵盖社交网络、网页图、科学计算等领域：

| 数据集 | 规模（行/列） | 非零元数 | 密度 |
|-------|-------------|----------|------|
| `com-YT`, `soc-LJ`, `Pokec`, `Orkut` | 百万级 | 数千万至数十亿 | ~1e-6 ~ 1e-5 |
| `webbase-2001`, `GAP-web` | 上亿级 | 超十亿 | < 1e-7 |
| `arabic-2005`, `uk-2002` | 数千万级 | 数亿至十亿 | ~1e-6 |

共 13 个数据集，具有多样化的稀疏模式（行偏斜、列偏斜、均匀、混合等）。

---

### **实验设置和评估指标**

- **硬件平台**：TSUBAME 4.0 超算
  - 单节点：双 AMD EPYC 9654 CPU + 4 × NVIDIA H100 SXM5（94GB HBM2e）
  - 节点内互联：NVLink 4.0（450 GB/s）
  - 节点间互联：InfiniBand NDR200（200 Gbps）
- **软件栈**：
  - 单节点 SpMM：PyTorch (`torch.sparse.mm`) → cuSPARSE CSR kernel
  - 多节点通信：PyTorch Distributed + NCCL backend（`alltoall`）
- **评估指标**：
  - 端到端运行时间（End-to-end runtime）
  - 总通信量（Total communication volume）
  - 跨节点通信量（Inter-node communication volume）
  - 加速比（Speedup）
  - 强扩展性（Strong scaling up to 128 GPUs）

---

### **基线方法对比**

| 基线方法 | 分区策略 | 是否稀疏感知 | 是否层次感知 | 后端 |
|--------|---------|------------|------------|------|
| **CAGNET** | 1.5D | ❌（block-based） | ❌ | NCCL |
| **SPA** | 1.5D | ✅（column-based） | ❌ | NCCL |
| **BCL** | 2D | ❌ | ❌ | NVSHMEM |
| **CoLa** | 1D | ✅（column-based） | ✅ | NVSHMEM |

SHIRO 在所有维度上均优于这些方法，尤其是在大规模（>8 GPUs）场景下。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

- **最大通信量减少**：高达 **96.3%**（在 `mawi` 数据集上，相比 column-based 策略）。
- **几何平均加速比（128 GPUs）**：
  - 相比 CAGNET：**221.5×**
  - 相比 SPA：**56.0×**
  - 相比 BCL：**23.4×**
  - 相比 CoLa：**8.8×**

> 注：在 ≤4 GPUs 时，由于 CoLa 的细粒度 RDMA 优化占优，SHIRO 略慢；但在 ≥8 GPUs 后全面反超。

---

### **与基线方法的对比结果**

| 对比项 | 结果 |
|------|------|
| **vs CAGNET/BCL** | 显著胜出，因其完全无视稀疏性，通信量巨大 |
| **vs SPA** | 更优，因 SPA 仅用 column-based，无法进一步压缩通信 |
| **vs CoLa** | 在小规模略逊，但在大规模（≥8 GPUs）显著领先，证明层次化优化的有效性 |

> 图 7 显示，在多数数据集上，SHIRO 的执行时间随 GPU 数增加而下降（良好扩展性），而其他方法在超过 8 GPU 后性能恶化。

---

### **消融实验结果**

#### （1）通信体积减少（图 8）
- **Joint row-col vs Col-based**：
  - 平均通信量减少 30–70%，在 `mawi` 上达 96%
  - 通信模式更均衡，消除热点通信对（见图 9）

- **加入 Hierarchical 策略后**：
  - 进一步减少跨节点通信量（inter-node volume）
  - 在 `com-LJ`, `Orkut`, `sx-SO` 上效果最明显

#### （2）逐步优化效果（图 10）
- 三阶段消融：
  1. Base（col-based）
  2. + Joint row-col
  3. + Hierarchical
- 所有数据集上均有持续性能提升，验证两个模块的正交性和有效性。

> 唯一例外是 `del24`，因其通信极度不均衡，层次化拆分反而降低链路利用率。

#### （3）不同稠密列数 N 的影响（图 11）
- 多数数据集：运行时间随 N 线性增长 → **通信吞吐受限**
- 少数（如 `arabic`）：N 增大后增速放缓 → **通信延迟受限**

---

## 4. 关键结论和发现

### **主要发现**

1. **单维度稀疏感知不足以最大化通信效率**：
   - 联合利用行和列稀疏模式可显著减少通信量，尤其在非零元分布复杂的矩阵上。

2. **层次化网络必须显式建模**：
   - 忽视 NVLink/InfiniBand 差异会导致严重冗余；
   - 通过组内聚合 + 跨组传输可大幅减少慢链路负载。

3. **两种优化可叠加且互补**：
   - 先做联合优化降低总量，再做层次优化降低跨节点流量；
   - 重叠调度进一步提升带宽利用率。

4. **SHIRO 具备强扩展性**：
   - 在 128 个 H100 GPU 上仍能有效扩展，远超现有方法（通常只能扩展到 8 GPU）。

---

### **方法的局限性**

1. **预处理开销**：
   - 最小顶点覆盖求解需离线进行，虽然可复用，但对动态图或频繁变化的稀疏模式可能不适用。

2. **极端不均衡通信模式下性能退化**：
   - 如 `del24`，层次化拆分可能导致负载不均，影响性能。

3. **当前实现基于 1D 分区**：
   - 虽然可推广，但未在 2D 或 1.5D 分区上系统验证。

---

### **未来工作方向**

1. **支持动态稀疏模式**：
   - 开发轻量级在线优化器，适应图结构动态变化的应用（如动态 GNN）。

2. **结合图分区技术**：
   - 与 hypergraph partitioning 或 Arrow decomposition 联合优化，进一步减少通信边界。

3. **扩展至 SpGEMM**：
   - 将类似思想应用于稀疏-稀疏矩阵乘法（SpGEMM）。

4. **异构集群优化**：
   - 支持 CPU-GPU 混合集群或多级存储架构。

---

> **总结**：SHIRO 通过**联合行列稀疏感知**与**层次化通信调度**，实现了近最优的分布式 SpMM 通信效率，在真实数据集和大规模 GPU 集群上展现出显著性能优势和卓越可扩展性，为 GNN、图计算等应用提供了强有力的底层支撑。

</details>

---

### 4. [EdgeFlex-Transformer: Transformer Inference for Edge Devices](https://arxiv.org/abs/2512.19741)

**Authors**: Shoaib Mohammad, Guanqun Song, Ting Zhu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.19741v1  

#### Abstract
Deploying large-scale transformer models on edge devices presents significant challenges due to strict constraints on memory, compute, and latency. In this work, we propose a lightweight yet effective multi-stage optimization pipeline designed to compress and accelerate Vision Transformers (ViTs) fo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《EdgeFlex-Transformer: Transformer Inference for Edge Devices》核心总结**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Vision Transformers (ViTs) 在计算机视觉任务中表现优异，但由于其高内存占用、计算密集性和延迟问题，在资源受限的边缘设备（如移动设备、IoT传感器）上部署面临巨大挑战。传统压缩方法往往依赖重训练、复杂的架构搜索或产生硬件不友好的稀疏模式，难以在真实边缘场景中高效部署。

本文旨在解决以下核心矛盾：
- 如何在**不牺牲精度的前提下**，显著降低 ViT 模型的**峰值内存使用量和推理延迟**；
- 如何设计一种**无需重训练、轻量级且模块化**的优化流程，适用于多种 ViT 架构和异构边缘平台。

---

### **提出了什么新方法或新思路**
作者提出了一种名为 **EdgeFlex-Transformer** 的多阶段优化流水线，结合了四种关键技术，形成一个渐进式、训练免费（training-free）的压缩与加速框架：

1. **Activation Profiling（激活分析）**  
   利用前向钩子（forward hooks）在 MLP 层收集激活统计信息（如平均绝对激活值），识别低重要性通道。

2. **Memory-Aware Structured Pruning（内存感知结构化剪枝）**  
   基于激活强度对 MLP 输出通道进行排序，并剪除最不活跃的通道（例如最低 10%），实现结构化稀疏，兼容现代硬件执行单元。

3. **Selective Mixed-Precision Execution（选择性混合精度执行）**  
   对部分子模块启用 FP16 精度（通过 PyTorch AMP），减少内存并提升吞吐，同时保留关键层为 FP32 以维持数值稳定性。

4. **Activation-Aware Quantization (AWQ)**  
   将权重和激活量化至 INT8，利用每通道缩放因子（channel-wise scaling）保护敏感通道，最小化量化带来的精度损失。

> ✅ 整个流程无需模型微调或重新训练，具备高度可移植性和实用性。

---

### **相比现有方法的优势**

| 方法 | 缺陷 | EdgeFlex 的优势 |
|------|------|------------------|
| Unstructured Pruning | 产生不规则稀疏，硬件难加速 | 结构化剪枝，硬件友好 |
| Naive Quantization | 易导致显著精度下降 | AWQ 基于激活敏感性保护关键通道 |
| Knowledge Distillation | 需复杂训练流程，泛化差 | 完全无需训练 |
| NAS | 搜索成本极高，不可扩展 | 轻量级、快速适配不同设备 |
| 单一优化技术 | 改进有限 | 多阶段协同优化，效果叠加 |

> 🔑 **核心创新在于将 activation profiling 作为统一指导信号，贯穿剪枝与量化全过程，实现了“感知激活 → 决策剪枝 → 精细量化”的闭环优化策略**。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **CIFAR-10**：用于激活校准（calibration）和最终测试。
  - 校准集：32 个样本（极小规模，体现轻量性）
  - 测试集：全部 10,000 张图像

---

### **实验设置**
- **基础模型**：ViT-Huge（32 层，隐藏维度 1280，MLP 维度 5120，总参数约 6.32 亿）
- **硬件平台**：NVIDIA A100 GPU（来自 Ohio Super-Computing Center 的 Ascend 集群），启用了内存剖析功能
- **批大小（batch size）**：32
- **实现工具**：PyTorch + Hugging Face Transformers 库

---

### **评估指标**
| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | Top-1 准确率 |
| **Average Batch Latency (s)** | 单批次平均推理时间 |
| **Total Inference Time (s)** | 推理完整测试集所需总时间 |
| **Peak Memory Usage (MB)** | 推理过程中最大内存占用 |

---

### **基线方法对比**
共比较了四个模型变体：

| 变体 | 说明 |
|------|------|
| **Original FP32** | 原始未修改的 ViT-Huge 模型 |
| **Pruned FP32** | 经过结构化剪枝后的 FP32 模型 |
| **Pruned + FP16** | 剪枝后启用混合精度（FP16） |
| **Pruned + AWQ** | 剪枝后应用 AWQ 量化至 INT8 |

> ⚠️ 所有优化均无 fine-tuning 或 retraining。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Fig. 3）**

| Variant | Accuracy (%) | Avg Batch Latency (s) | Total Time (s) | Peak Memory (MB) |
|--------|---------------|------------------------|----------------|------------------|
| Original FP32 | 8.56 | 0.194 | 25.07 | 2613.45 |
| Pruned FP32 | 7.78 | 0.198 | 25.68 | ~2528 |
| Pruned + FP16 | **11.67** | **0.028** | **4.15** | **1356.10** |
| Pruned + AWQ | 11.28 | 0.199 | 25.79 | **623** |

---

### **与基线方法的对比结果**

#### ✅ **内存效率**
- **Pruned + AWQ** 实现 **76.2% 的峰值内存下降**（从 2613 MB → 623 MB），远低于典型边缘设备 <100MB 的限制目标（注：此处可能为报告单位误差或仅指模型静态加载内存以外的部分；实际仍偏高，但趋势明确）。
- **Pruned + FP16** 内存减半（↓48.1%），适合中等资源设备。

#### ✅ **推理速度**
- **Pruned + FP16** 达到 **6.9× 加速**（总时间从 25.07s → 4.15s），平均批延迟降低 **6.9×**（0.194 → 0.028s），是所有变体中最优。
- AWQ 版本虽未提速（因未使用专用 INT8 kernel），但为未来硬件加速留出空间。

#### ✅ **精度表现**
- 所有优化版本均未出现严重精度退化。
- **令人意外的是，Pruned + FP16 和 Pruned + AWQ 的准确率反而高于原始模型（~11.3% vs 8.56%）**，推测原因可能是：
  - FP16 提升了训练/推理一致性（尽管未 fine-tune）
  - 剪枝去除了噪声通道，起到正则化作用
  - 数据分布与 ViT 先验更匹配（CIFAR-10 上 ViT 通常需 fine-tune 才能发挥潜力，此处原始性能偏低属正常现象）

---

### **消融实验结果**
虽然文中未明确标注“ablation study”章节，但从不同变体的结果可视为天然的消融分析：

| 优化步骤 | 内存变化 | 延迟变化 | 精度影响 | 贡献总结 |
|--------|---------|----------|-----------|------------|
| 剪枝（Pruning） | ↓~3.3% | ↑轻微 | ↓0.78% | 贡献有限，但为后续打基础 |
| + FP16 | ↓48% | ↓6.9× | ↑+3.1% | 最大加速来源，精度反升 |
| + AWQ | ↓76% | ≈持平 | ↑+2.7% | 极致内存压缩，适合边缘 |

> 📌 **结论：单一剪枝效果有限，真正的性能飞跃来自于“剪枝 + 混合精度”或“剪枝 + AWQ”的组合效应**。

---

## **4. 关键结论和发现**

### **主要发现**
1. **多阶段协同优化优于单一技术**：单独剪枝收益较小，但作为前置步骤可为量化/混合精度提供更好起点。
2. **无需重训练也能保持甚至提升精度**：得益于 activation-aware 设计，尤其是 AWQ 和 FP16 的数值稳定性和潜在正则化效应。
3. **EdgeFlex 实现了显著的资源节约**：
   - 内存最高减少 **76%+**
   - 推理延迟最多降低 **6.9×**
   - 总推理时间缩短 **83.4%**
4. **AWQ 在 INT8 下表现出色**：即使没有专用 INT8 加速器，也能在几乎不损精度的情况下实现高压缩比，具备良好扩展前景。

---

### **方法的局限性**
1. **当前实验基于 A100 GPU**，尚未在真实边缘芯片（如手机 NPU、MCU）上验证端到端性能。
2. **CIFAR-10 上 ViT 原始性能偏低**（8.56%），表明模型未经预训练或迁移学习，可能影响结论普适性。
3. **AWQ 推理未加速**：由于缺乏 INT8 kernel 融合支持，延迟未改善，依赖底层运行时优化。
4. **仅应用于 ViT-Huge**，未验证在其他 ViT 变体（如 ViT-Base/Tiny）或其他模态（如 NLP）中的通用性。

---

### **未来工作方向（原文 IX 节）**
1. **扩展至 Mixture-of-Experts (MoE) 架构**：探索动态稀疏专家网络的剪枝与量化策略。
2. **Task-Aware Pruning**：引入下游任务反馈机制，指导剪枝优先级。
3. **Layer-wise Dynamic Precision Control**：按层动态切换 FP32/FP16/INT8，进一步节省能耗。
4. **集成实时调度系统**：与边缘操作系统联动，实现动态资源分配与推理调度。

---

## **总结**
> **EdgeFlex-Transformer 提供了一个实用、模块化、无需重训练的 ViT 压缩框架，通过 activation profiling 驱动的“剪枝 + 混合精度 + AWQ”三步走策略，在 CIFAR-10 上实现了高达 76% 的内存压缩和近 7× 的延迟降低，且精度不降反升。该工作为大规模 Transformer 在边缘设备上的高效部署提供了可行路径，并为未来动态稀疏与绿色 AI 计算奠定基础。**

🔗 **代码开源地址**：[https://github.com/Shoaib-git20/EdgeFlex.git](https://github.com/Shoaib-git20/EdgeFlex.git)

</details>

---

### 5. [Multi-hop Reasoning via Early Knowledge Alignment](https://arxiv.org/abs/2512.20144)

**Authors**: Yuxin Wang, Shicheng Fang, Bo Wang, Qi Luo, Xuanjing Huang, Yining Zheng, Xipeng Qiu  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.20144v1  

#### Abstract
Retrieval-Augmented Generation (RAG) has emerged as a powerful paradigm for Large Language Models (LLMs) to address knowledge-intensive queries requiring domain-specific or up-to-date information. To handle complex multi-hop questions that are challenging for single-step retrieval, iterative RAG app...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Multi-hop Reasoning via Early Knowledge Alignment*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Iterative RAG** 系统在处理复杂多跳推理任务时，通常在没有利用检索语料库信息的情况下进行问题分解和初始规划（planning），导致：
- **计划失败（plan failure）**：初始思考步骤缺乏上下文支撑，产生误导性假设；
- **级联错误（cascading errors）**：错误的检索路径引发后续推理偏差；
- **低效探索（inefficient exploration）**：在强化学习（RL）训练中，模型盲目探索，增加噪声。

### 提出的新方法：Early Knowledge Alignment (EKA)
作者提出 **Early Knowledge Alignment (EKA)**，一种简单但有效的模块，其核心思想是：
> 在 LLM 开始推理前，先从检索系统获取初步相关知识（early knowledge），并将其作为初始上下文输入，从而实现“知识对齐”。

#### 具体流程：
1. 给定问题 $ q $，首先执行一次 `Retrieve(q, D, k)` 获取 top-k 文档 $ P_0 $；
2. 将 $ P_0 $ 注入提示词中（置于 `<knowledge>...</knowledge>` 内）；
3. LLM 基于该知识开始首次 `Think` 步骤，后续按标准迭代流程进行 `Search` 和 `Think`。

### 相比现有方法的优势
- ✅ **更强的推理基础**：通过早期知识提供上下文锚点，减少无依据猜测；
- ✅ **降低熵值（entropy）**：使模型在 RL 探索过程中更聚焦、更确定；
- ✅ **提升检索精度与效率**：减少无效查询，提高 R-S 分数；
- ✅ **无需训练的即插即用策略**：可作为 inference-time 模块直接应用于大模型；
- ✅ **通用性强**：兼容不同 RL 算法（如 GRPO、PPO）、不同 backbone 模型和检索架构。

---

## 2. 核心实验方法和设置

### 使用的数据集
在两个主流 RAG 实验设置下验证：
- **Graph-R1 设置**：使用 6 个标准 QA 数据集
  - 2WikiHop
  - HotpotQA
  - Musique
  - NQ (Natural Questions)
  - PopQA
  - TriviaQA
- **Search-R1 设置**：额外加入 **Bamboogle**，区分 in-domain (IND) 与 out-of-domain (OOD) 测试。

### 实验设置
- **Backbone 模型**：
  - Qwen2.5-7B-Instruct
  - Qwen2.5-14B-Instruct
  - GPT-4o-mini（用于部分基线）
- **RL 训练框架**：
  - GRPO（Group Relative Policy Optimization）
  - PPO（Proximal Policy Optimization）
- **最大推理轮次**：默认为多轮交互式检索生成。

### 评估指标
| 指标 | 含义 |
|------|------|
| **EM** | Exact Match，答案完全匹配率 |
| **F1** | 生成答案与真实答案的 token 级 F1 分数 |
| **R-S (Retrieval Similarity)** | 检索到的内容与“黄金”上下文之间的语义相似度（基于嵌入向量的余弦相似度） |

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **非训练方法** | NaiveGeneration, StandardRAG, GraphRAG, LightRAG, PathRAG, HippoRAG2, HyperGraphRAG |
| **监督微调（SFT）** | SFT |
| **RL 方法** | R1, R1-Searcher, Search-R1, Search-R1-PPO, Graph-R1 |
| **其他推理策略** | CoT, IRCoT, Search-o1, Rejection Sampling |

---

## 3. 主要实验结果和性能指标

### 性能提升显著（以 F1 为主）

#### 在 Graph-R1 设置下的平均提升（Qwen2.5-7B）：
| 方法 | +EKA 后 F1 提升 |
|------|----------------|
| Graph-R1 | **+2.83** |
| Search-R1 | **+11.64** |
| Search-R1-PPO | **+7.67** |

> 表明 EKA 对弱初始规划的方法（如 Search-R1）增益更大。

#### 在 Search-R1 设置下的表现：
| 方法 | 平均 F1 提升 |
|------|-------------|
| Search-R1 | **+6.3** |

即使在大规模 Fullwiki 检索集中，EKA 仍带来稳定增益，说明其对检索规模不敏感。

#### 大模型上的训练免费效果（Training-free Inference）
| 模型 | +EKA 平均 F1 提升 |
|------|------------------|
| Qwen2.5-32B-Instruct | +4.0 ~ +6.0 |
| Qwen3-235-A30B-Instruct | +5.0 ~ +8.0 |

> 验证了 EKA 可作为 plug-and-play 模块用于超大模型，避免昂贵的 RL 微调。

### 检索质量提升（R-S 指标）
| 方法 | Avg. R-S (+EKA) |
|------|----------------|
| Graph-R1 | 60.40 → **64.90** |
| Search-R1 | 53.06 → **65.02** |

> 显示 EKA 显著提升了检索的相关性和效率。

### 消融实验结果

#### Q1: 为什么 EKA 更好？——熵分析（Entropy Analysis）
- 图 3 显示，在 `<think>`、`<query>`、`<answer>` 阶段，EKA 的 token 熵普遍更低；
- 说明模型在 EKA 下推理更具确定性，探索方向更集中；
- 符合理论推导：EKA 能获得更高的互信息 $ I(A^*; H_T|Q) $，从而降低最终错误概率。

#### Q2: 是否缩短推理步数？
| 方法 | 平均推理轮次 |
|------|--------------|
| Graph-R1 | 3.26 |
| +EKA | **2.22** |

> 减少约 1 轮，意味着更少噪声、更快收敛。

#### Q3: 是否影响泛化能力？
- 在 OOD 数据集上测试，EKA 不仅未损害泛化，反而平均提升 **+4.64 F1**；
- 即使使用 noisy early knowledge（EKA-wiki），性能仍优于 baseline；
- 使用不同 retriever（BGE vs E5）也保持增益，证明 **retriever-agnostic**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **初始知识对齐至关重要**：在推理开始前引入 early knowledge，能有效防止“盲猜”，建立稳健的推理起点。
2. ✅ **EKA 提高 RL 效率**：通过降低探索熵，加速学习过程，减少无效动作。
3. ✅ **即插即用且可扩展**：无需额外训练即可部署于大型 LLM，适用于资源受限场景。
4. ✅ **全面性能提升**：在 EM、F1、R-S 等多个维度均取得 SOTA 或显著改进。
5. ✅ **理论支持充分**：从信息论角度证明 EKA 可带来更高信息增益和更低误差上限。

### 局限性（Limitations）
- 当前仅验证于多跳问答任务，尚未扩展至更复杂的 **Deep Research** 场景（如科研文献综述、长期规划等）；
- 初始检索质量依赖于 retriever 性能，若 $ P_0 $ 完全无关可能引入误导（尽管实验显示鲁棒性较强）。

### 未来工作方向
- 将 EKA 扩展到更复杂的 agent workflows（如自动实验设计、跨模态推理）；
- 动态调整 early knowledge 的粒度与数量（adaptive k）；
- 结合 memory 或 meta-learning 进一步优化初始知识选择策略。

---

> 🔚 **总结一句话**：  
> **Early Knowledge Alignment (EKA)** 通过“先看再想”的机制，从根本上改善了 Iterative RAG 的初始推理质量，是一种高效、通用、无需训练的增强范式，推动了 RAG 系统从“先计划后检索”向“先对齐再推理”的范式转变。

</details>

---

### 6. [FastMPS: Revisit Data Parallel in Large-scale Matrix Product State Sampling](https://arxiv.org/abs/2512.20064)

**Authors**: Yaojian Chen, Si-Qiu Gong, Lin Gan, Yanfei Liu, An Yang, Yinuo Wang, Chao-yang Lu, Guangwen Yang  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.20064v1  

#### Abstract
Matrix Product State (MPS) is a versatile tensor network representation widely applied in quantum physics, quantum chemistry, and machine learning, etc. MPS sampling serves as a critical fundamental operation in these fields. As the problems become more complex, the scale of MPS is rapidly increasin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FastMPS: Revisit Data Parallel in Large-scale Matrix Product State Sampling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Matrix Product State (MPS) 采样是量子物理、量子化学和机器学习等领域中的基础操作。随着问题规模扩大，MPS 的张量数量（sites $M$）、样本数（$N$）和 bond 维度（$\chi$）急剧增长，导致传统并行策略面临严重瓶颈：
- **Data Parallelism**：受限于内存和 I/O 压力，在大规模 MPS 中效率低下，频繁从磁盘加载张量成为性能瓶颈。
- **Model Parallelism**（如每进程处理一个 site）：虽然可扩展至高 $\chi$（如 $10^4$），但存在**刚性进程绑定**（process binding）、启动延迟大、通信开销重、负载不均衡等问题。

此外，MPS 采样本质上是顺序依赖的（每个 site 的测量依赖前一个 site 的收缩结果），限制了并行化潜力。

---

### 提出的新方法与创新思路
本文提出 **FastMPS** —— 一种多级并行框架，重新激活大规模 MPS 采样中的 **Data Parallelism**，通过结合 **Tensor Parallelism** 和一系列优化技术实现高效可扩展模拟。

#### 核心创新点：
1. **Multi-level Parallel Framework**
   - **Task-level Data Parallelism**：在独立样本之间进行数据并行，提升灵活性和可扩展性。
   - **Tensor-level Tensor Parallelism**：沿 bond 维度对 MPS 张量进行切分，支持跨设备分布大张量，缓解单设备内存压力。
   - 支持灵活配置：$p = p_1 \times p_2$ 进程分为 $p_1$ 组，每组 $p_2$ 个进程执行 tensor parallelism。

2. **Single-site vs Double-site Tensor Parallelism**
   - **Single-site**：使用 `ReduceScatter` 更新左环境（left environment），适合带宽主导场景。
   - **Double-site**：合并两个相邻 site 处理，用 `AllReduce` 替代 `ReduceScatter`，减少通信次数，降低延迟影响，更适合高延迟网络（如 PCIe）。

3. **Adaptive Mixed Precision Strategy**
   - 在计算中采用 **TF32** 加速 tensor core 利用率，同时通过**逐样本动态缩放**（sample-wise max scaling）维持数值稳定性，避免下溢（underflow）。
   - 存储使用 **FP16** 表示 MPS 张量和中间状态，显著降低 I/O、host-device 传输和广播开销。

4. **Customized Optimizations for GBS**
   - **Displacement Operator Acceleration**：利用结构稀疏性将矩阵指数分解为三角矩阵乘法，加速 GPU 上的 `expm` 计算，提速超 10×。
   - **Dynamic Bond Dimension Allocation**：基于纠缠熵分布动态调整各 site 的 bond 维度，边缘区域使用较小 $\chi$，中心区域保留高精度，大幅减少无效计算。

5. **Overlap & Memory Management**
   - 使用双缓冲（double buffering）和额外 CPU 线程实现 **I/O、计算、通信三重重叠**。
   - 将宏批次（macro batch）进一步划分为微批次（micro batch），降低峰值内存需求。

---

### 相比现有方法的优势
| 方面 | 传统方法 [19] | FastMPS |
|------|----------------|---------|
| 可扩展性 | 固定 $p = M$（进程数=站点数），不可伸缩 | 支持任意进程数，强弱扩展均高效 |
| 内存压力 | 单设备需容纳整个 MPS 张量 | 张量被切分，支持更大 $\chi$ |
| I/O 效率 | 所有进程集中读取，易引发磁盘争用 | 主控进程统一读取并广播，支持 I/O 与计算重叠 |
| 数值精度 | 需 FP64 保证稳定 | TF32 + 动态缩放即可稳定运行 |
| 性能 | 受限于通信和启动延迟 | 多级并行 + 混合精度 + 定制优化，性能提升 >10× |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Gaussian Boson Sampling (GBS)**：作为代表性且高要求的应用基准。
- 具体实验包括：
  - **Jiuzhang 系列**（中国科大光量子计算机）
  - **Borealis**（Xanadu 光子芯片）
  - 自定义大规模模拟数据：**8176 sites**, bond dimension $\chi = 10^4$, 实际压缩光子数 16.54

这些数据具有标准化格式，易于获取，并广泛用于验证量子优势。

---

### 实验设置与评估指标

#### 硬件平台
- **GPU**：NVIDIA A100（8 GPUs 构成 2×4 数据+张量并行拓扑）
- **CPU**：Intel Xeon Gold 6230R @ 2.10GHz
- **超算系统**：
  - **Tianhe-3**：测试到 15 节点 375 核
  - **Sunway TaihuLight**：测试到 500 进程 32500 核

#### 参数设置
- 物理维度 $d = 3$ 或 $4$
- bond dimension $\chi$: 最高达 $10^4$
- 样本数 $N$: 高达 $10^7$
- micro batch size $N_2 = 5000$（平衡 GEMM 强度与内存）

#### 评估指标
- **运行时间（Time-to-solution）**
- **Speedup**（相对于基线方法）
- **强/弱扩展效率**
- **截断误差（truncation error）**
- **相关函数一致性**（first/second-order correlation slope）

---

### 基线方法对比
- 主要对比文献 **[19]**（Oh et al., Nature Physics 2024），即当前最先进的 MPS-based GBS 模拟器。
- 包括 GPU 和 CPU 平台上的直接性能比较。
- 同时参考 Roofline 模型分析计算/通信比（CCR），判断是否达到计算瓶颈。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 结果 |
|------|------|
| 最大模拟规模 | **8176 sites**, $\chi = 10^4$, $N = 10^7$ |
| 单卡性能（A100） | 生成 0.4M 样本耗时 ~2000s（Borealis-288），推算 10M 样本约 14 小时 |
| 弱扩展效率 | >95%（Tianhe-3 和 Sunway） |
| 强扩展效率 | >95%（最高达 32500 核） |

---

### 与基线方法的对比结果

#### GPU 性能对比（Table 2）
| GBS 实例 | [19] 时间（分钟） | Fast-MPS-8（分钟） | 加速比 |
|----------|--------------------|---------------------|--------|
| Jiuzhang2 | 62（使用 144 GPUs） | 38.57（仅 8 GPUs） | **1.6× 更快 + 显著更少资源** |
| B-M288 | 62（使用 288 GPUs） | 247.43（8 GPUs） | 推算等效加速 **>10×**

> 注：Fast-MPS 仅用 8 张 GPU 即可在合理时间内完成此前需数百 GPU 的任务。

#### CPU 性能对比（Table 3）
| 实例 | [19] 时间 | Fast-MPS 时间 | Speedup |
|------|-----------|---------------|---------|
| Jiuzhang2-P65-1 | 17.72h | 1.76h | **10.06×** |
| B-M288 | 36.44h | 4.504h | **8.09×**

---

### 消融实验结果（Figure 11）
在 A100 上对三大优化进行消融测试（参数：$d=4,\chi=10^4$, 400K samples）：

| 优化项 | 移除后性能下降（相对全优化版本） |
|--------|-------------------------------|
| Dynamic Bond Dimension | 下降 1.9× |
| Optimized expm | 下降 2.6× |
| Mixed Precision (TF32+FP16) | 下降 5.1× |

👉 **混合精度带来最大收益**，说明其在释放 tensor core 性能方面至关重要。

---

## 4. 关键结论和发现

### 主要发现
1. **Data Parallelism 可在大规模 MPS 中复活**：通过压缩、切分和重叠机制，克服了传统 data parallel 的 I/O 和内存瓶颈。
2. **Tensor Parallelism 有效打破 memory wall**：split-K GEMM + collective communication 设计使大 $\chi$ 张量可在多设备间分布。
3. **混合精度可行且必要**：通过 sample-wise 动态缩放，TF32 可替代 FP64，结合 FP16 存储，显著提升吞吐。
4. **定制化优化效果显著**：displacement operator 分解和 dynamic bond dimension 分配分别带来 2× 和近 80% 的复杂度降低（见 Table 1）。
5. **极高可扩展性**：在超算上实现 >95% 扩展效率，支持数千进程协同工作。

---

### 方法的局限性
1. **仍受限于 GBS 特性**：部分优化（如 displacement 分解）针对 GBS 特殊结构设计，通用性有待验证。
2. **通信敏感**：tensor parallelism 对 AllReduce/ReduceScatter 带宽高度依赖，PCIE 连接下性能可能下降。
3. **尚未完全启用 FP16/BF16**：因 ComplexHalf 支持不足及舍入误差风险，目前未全面启用半精度训练级精度。
4. **启动阶段仍有 I/O 集中问题**：虽由单一进程负责读取，但在极端大规模文件下仍可能成为瓶颈。

---

### 未来工作方向
1. **探索 AI 与科学计算共通性**：作者指出 MPS 可视为“大型模型”（如 8176 sites × $\chi^2$ ≈ 2452B 参数），未来可借鉴 LLM 推理中的 pipeline parallelism、offloading 等技术。
2. **异构计算支持**：扩展至更多硬件平台（如国产加速器、TPU）。
3. **全自动并行策略选择**：基于硬件探测自动选择 single/double-site 模式。
4. **支持更多 tensor network 应用**：将 FastMPS 框架推广至 PEPS、TTNS 等其他 TN 结构。

---

> ✅ **总结一句话**：  
> FastMPS 通过融合 **multi-level parallelism**、**adaptive mixed precision** 和 **application-specific optimizations**，实现了超过 **10× 的端到端加速**，首次在单 GPU 或小型集群上完成了此前需数百 GPU 才能完成的大规模 GBS 模拟，极大降低了高性能 MPS 仿真的门槛。

</details>

---

### 7. [MolAct: An Agentic RL Framework for Molecular Editing and Property Optimization](https://arxiv.org/abs/2512.20135)

**Authors**: Zhuo Yang, Yeyun chen, Jiaqing Xie, Ben Gao, Shuaike Shen, Wanhao Liu, Liujia Yang, Beilun Wang, Tianfan Fu, Yuqiang Li  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20135v1  

#### Abstract
Molecular editing and optimization are multi-step problems that require iteratively improving properties while keeping molecules chemically valid and structurally similar. We frame both tasks as sequential, tool-guided decisions and introduce MolAct, an agentic reinforcement learning framework that ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MolAct: An Agentic RL Framework for Molecular Editing and Property Optimization — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
分子编辑（Molecular Editing）和分子优化（Molecular Optimization）是药物设计中的核心任务，传统方法通常采用单步生成或静态指令微调方式处理，存在以下问题：
- 缺乏对**多步决策过程**的建模；
- 无法有效利用外部化学工具（如有效性、相似性、性质预测器）进行**实时反馈与验证**；
- 难以保证生成分子的**化学有效性**和**结构一致性**。

### 提出的新方法与创新思路
本文提出 **MolAct**，首个将分子设计形式化为 **Agentic Reinforcement Learning（代理式强化学习）** 框架的工作，其核心创新包括：

- **两阶段训练范式（Two-stage Training Paradigm）**：
  - **Stage 1（MolEditAgent）**：在分子编辑任务上预训练，学习基本操作（Add/Delete/Substitute），掌握工具调用与终止策略；
  - **Stage 2（MolOptAgent）**：基于 Stage 1 初始化，在分子优化任务上继续训练，实现属性驱动的渐进式优化。
- **工具增强的代理架构（Tool-Augmented Agent）**：
  - LLM Agent 可主动调用多种化学工具（validity checker, similarity calculator, property oracle），并根据反馈调整后续动作；
  - 支持“思考 → 工具调用 → 观察 → 再思考”的多轮交互机制。
- **Group-Relative Policy Optimization（GRPO）**：
  - 在相同输入提示下并行运行多个 rollout 链，组内相对优势估计降低方差，提升长程信用分配稳定性。

### 相比现有方法的优势
| 维度 | 传统方法（如VAE、RL、LLM单步生成） | MolAct |
|------|-------------------------------|--------|
| 决策模式 | 单步生成或固定流程 | 多步、可迭代的序列决策 |
| 工具集成 | 被动使用或无显式反馈 | 主动调用 + 显式反馈闭环 |
| 化学有效性 | 常出现无效结构（hallucination） | 高有效性（95–100%） |
| 可解释性 | 黑箱生成 | 可追踪每一步推理与工具响应 |
| 泛化能力 | 固定目标优化 | 支持多样化属性目标（LogP、solubility、bioactivity等） |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **ChemCoTDatasets (Li et al., 2025)** 构建训练数据，该数据集包含带链式思维标注的多步分子修改样本；
- 移除原始 CoT 推理文本，仅保留：
  - 输入：源分子 SMILES + 编辑指令 / 优化目标；
  - 输出：目标分子 SMILES；
- 所有分子经标准化（canonicalization）和有效性校验。

### 实验设置
- **模型家族**：
  - MolEditAgent：用于 Stage 1 分子编辑训练；
  - MolOptAgent：从 MolEditAgent 初始化，用于 Stage 2 分子优化；
  - 不同规模变体：基于 Qwen-2.5-3B 和 Qwen-2.5-7B backbone。
- **交互机制**：
  - 最大回合数（max_turns）设为 16；
  - 每个 prompt 并行执行 K 条 rollout（group-relative setting）；
  - 仅对 agent 生成 token 进行梯度更新（masked update）。
- **工具接口支持的操作**：
  - 编辑操作：`add`, `delete`, `substitute` 功能团；
  - 查询操作：`validity`, `Tanimoto similarity`, `Murcko scaffold`, `property oracle`（LogP, solubility, QED, DRD2, JNK3, GSK3β）；
  - 终止信号：`terminate`。

### 评估指标
| 任务类型 | 指标 | 含义 |
|--------|------|------|
| **分子编辑** | Pass@1 | 是否成功完成指定编辑操作 |
|             | Validity (%) | 生成分子是否化学有效 |
| **分子优化** | Δ（Delta） | 属性改善均值（越高越好；负值表示退化） |
|             | SR%（Success Rate） | 成功提升属性的比例（>0） |

### 基线方法对比
涵盖两大类主流模型：
- **With Thinking（具备推理能力）**：
  - Gemini-2.5-pro-think, Claude3.7-sonnet-think, DeepSeek-R1, Qwen3-think 系列
- **Without Thinking（标准指令微调模型）**：
  - GPT-4o, Deepseek-V3, Qwen2.5-Instruct 系列, Llama-3.1-70B-Instruct 等

---

## 3. 主要实验结果和性能指标

### 分子编辑任务表现（Table 1 & 2）

#### 准确率（Accuracy %）
| 模型 | Add | Delete | Substitute |
|------|-----|--------|----------|
| **MolEditAgent-7B** | **90.0** | **80.0** | **78.3** |
| Gemini-2.5-pro-think | 100.0 | 85.0 | 81.7 |
| Claude3.7-sonnet-think | 85.0 | 80.0 | 83.4 |
| GPT-4o | 80.0 | 80.0 | 65.0 |
| **MolEditAgent-3B** | 80.0 | 70.0 | 16.7 |

> ✅ MolEditAgent-7B 在 Delete 和 Sub 上优于多数闭源 thinking 模型，Add 排名第二；  
> ⚠️ MolEditAgent-3B 在 Sub 任务上显著落后，表明复杂替换需更强模型容量。

#### 分子有效性（Validity %）
| 模型 | Add | Delete | Substitute |
|------|-----|--------|----------|
| **MolEditAgent-7B** | **100.0** | **95.0** | **98.0** |
| Qwen2.5-7B-Instruct | 75.0 | 70.0 | 65.0 |
| **MolEditAgent-3B** | 95.0 | 80.0 | 71.7 |
| Qwen-2.5-3B-Instruct | 60.0 | 55.0 | 65.0 |

> ✅ MolAct 显著提升化学有效性，尤其在 7B 模型上接近完美（95–100%）。

---

### 分子优化任务表现（Table 3）

| 模型 | LogP Δ (SR%) | Solubility Δ (SR%) | DRD2 Δ (SR%) | GSK3β Δ (SR%) |
|------|--------------|--------------------|---------------|----------------|
| **MolOptAgent-7B** | **0.89 (92%)** | **1.42 (84%)** | 0.02 (38%) | **0.04 (36%)** |
| Gemini-2.5-pro-think | -0.28 (81%) | 1.91 (92%) | **0.35 (74%)** | 0.04 (68%) |
| Claude3.7-sonnet-think | 0.41 (81%) | 0.59 (77%) | 0.18 (66%) | 0.01 (57%) |
| DeepSeek-R1 | 0.36 (74%) | 1.48 (97%) | 0.10 (62%) | -0.02 (41%) |
| **MolOptAgent-3B** | -0.24 (12%) | -0.021 (8%) | -0.009 (7%) | -0.0026 (10%) |

> ✅ MolOptAgent-7B 在 **LogP 优化上大幅领先所有基线**（Δ=0.89 vs 第二名 0.41）；  
> ✅ 在 **Solubility 上仅次于 Gemini 和 DeepSeek-R1**，仍保持高成功率（84%）；  
> ❗ 在生物活性任务（如 DRD2）上仍有差距，说明需要领域知识增强。

---

### 消融实验结果（Ablation Study）

#### 一阶段 vs 两阶段训练（Table 4）
| 模型 | 训练方式 | LogP SR% | Solubility SR% | QED SR% |
|------|---------|----------|----------------|---------|
| Qwen-2.5-7B-Instruct | One-stage（直接优化） | 0% | 0% | 12% |
| **MolOptAgent-7B** | **Two-stage** | **92%** | **84%** | **35%** |
| Qwen-2.5-3B-Instruct | One-stage | 0% | 0% | 0% |
| **MolOptAgent-3B** | **Two-stage** | **12%** | **8%** | **5%** |

> 🔍 关键发现：即使拥有工具访问权限和 RL 训练，**缺少编辑预训练的一阶段方法几乎完全失败**，证明两阶段设计至关重要。

#### 模型容量影响（Figure 4–6）
- **7B 模型**：响应长度稳定，工具调用高效，奖励能转化为实际成功；
- **3B 模型**：最大响应长度可达 **5000 tokens**，出现冗长无效推理，导致超预算无法终止；
- 尽管验证奖励相近，但 **3B 成功率远低于 7B**，揭示“奖励拟合 ≠ 政策可执行”。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **多步、工具增强的决策框架显著优于单步生成**：通过显式引入化学工具反馈，实现了更可靠、可解释的分子修改；
2. ✅ **两阶段训练是成功的关键**：先掌握编辑技能再进行优化，使模型学会如何正确使用工具和适时终止；
3. ✅ **更大的模型容量有助于政策执行效率**：7B 模型在有限回合内更有效地组织工具调用，而小模型易陷入低效循环；
4. ✅ **MolAct 在关键物理化学属性（LogP, solubility）上达到 SOTA 表现**，且维持高化学有效性（95–100%）；
5. ✅ **Agentic RL 是通往自动化药物设计的重要路径**：模拟真实药物化学家“试错+验证”工作流。

### 方法的局限性
1. 🚫 **对特定生物靶点（如 JNK3）优化效果一般**：可能缺乏足够的领域知识或专用工具；
2. 🚫 **未考虑合成可行性（synthetic accessibility）**：部分生成分子虽有效但在现实中难以合成；
3. 🚫 **依赖高质量外部工具**：性能受限于 property oracle 和 validity checker 的准确性；
4. 🚫 **小模型（3B）执行力弱**：尽管可以学到奖励模式，但难以在预算内完成任务。

### 未来工作方向
1. ➕ **引入反应路径与合成约束**：结合 retrosynthesis 工具，确保生成分子具备可合成性；
2. 🔁 **开发错误恢复机制**：当工具返回失败时，允许模型回溯或修正策略；
3. 🧩 **探索更复杂的课程学习策略**：超越两阶段，构建渐进式任务难度曲线；
4. 📊 **提出“政策可执行性”评估指标**：不仅看奖励高低，还需衡量是否能在交互预算内达成目标；
5. 🔬 **扩展至材料科学、催化剂设计等领域**：验证 MolAct 框架的通用性。

---

> 💡 **总结一句话**：  
> MolAct 首次将分子设计建模为 **Agentic RL 问题**，通过 **两阶段训练 + 工具增强代理** 实现了高有效性、可解释、高性能的分子编辑与优化，为 AI 驱动药物发现提供了新范式。

</details>

---

### 8. [SpidR: Learning Fast and Stable Linguistic Units for Spoken Language Models Without Supervision](https://arxiv.org/abs/2512.20308)

**Authors**: Maxime Poli, Mahi Luthra, Youssef Benchekroun, Yosuke Higuchi, Martin Gleize, Jiayi Shen, Robin Algayres, Yu-An Chung, Mido Assran, Juan Pino, Emmanuel Dupoux  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20308v1  

#### Abstract
The parallel advances in language modeling and speech representation learning have raised the prospect of learning language directly from speech without textual intermediates. This requires extracting semantic representations directly from speech. Our contributions are threefold. First, we introduce...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SpidR: Learning Fast and Stable Linguistic Units for Spoken Language Models Without Supervision

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前用于**Spoken Language Modeling (SLM)** 的自监督语音表示模型（如 wav2vec 2.0、HuBERT）存在以下问题：
- **语音单元质量不稳定**：在线聚类（online clustering）过程容易发生“码本坍缩”（codebook collapse），导致离散语音单元（discrete units）质量下降。
- **训练效率低**：HuBERT 需要多轮迭代训练（交替进行聚类和预训练），耗时长达一周；DinoSR 虽为单阶段训练，但仍需约180小时。
- **缺乏对 SLM 的针对性优化**：大多数 SSL 模型以 ASR 为主要目标设计，其表示可能不适合直接用于语言建模。

### 提出的新方法：SpidR
SpidR 是一种新的自监督语音表示学习框架，专为高效、稳定地生成高质量语音单元而设计，适用于无文本的口语语言建模（textless SLM）。其核心创新包括：

#### （1）改进的自蒸馏与在线聚类机制
- 延续 DinoSR 的架构，采用 **self-distillation + online clustering** 框架。
- **关键改进**：学生网络在多个中间层（last K layers）预测教师网络对应层的码本分配，而非仅用学生最后一层预测所有教师层。
  > 即：学生第 $k$ 层 → 预测教师第 $k$ 层的 nearest neighbor assignment。
- 这种“对齐式预测”增强了训练稳定性，显著缓解了码本坍缩问题。

#### （2）更平滑的教师动量更新策略
- 将教师网络的 EMA decay 参数 $\beta$ 设计为指数增长函数：
  $$
  \beta_t = 1 - (1 - \beta_0)\exp(-t / T)
  $$
  其中 $\beta_0=0.999$, $T=10000$。
- 相比传统恒定或分段常数调度，该策略使教师更快收敛且更稳定。

#### （3）高效的纯 PyTorch 实现
- 开发了一个轻量级、原生 PyTorch 的代码库，兼容 `torch.compile`，大幅减少通信开销。
- 支持快速迭代与实验探索。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **性能** | 在 sWUGGY、sBLIMP、tSC 等零样本 SLM 任务上全面优于 wav2vec 2.0、HuBERT、WavLM 和 DinoSR |
| **稳定性** | 显著降低码本和预测头的困惑度（perplexity），避免 codebook collapse |
| **速度** | 仅需 **1天** 完成预训练（16 A100 GPUs），相比 HuBERT（62小时）、DinoSR（180小时）提速明显 |
| **简洁性** | 单阶段训练，无需外部特征提取或 K-means 聚类步骤 |

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：LibriSpeech 960 小时音频（train-other-960）
- **下游语言模型训练数据**：Libri-Light 子集（600h / 6k / 60k 小时）
- **评估数据**：LibriSpeech dev-clean/dev-other 用于 ABX、MAP、SLM 评估

### 实验设置
- **模型结构**：
  - BASE Transformer：L=12 层，d=768，FFN=3072，12 头注意力
  - 下采样模块：7 层卷积，输出 50Hz 特征
  - 码本数量：K=8 个（每层一个），每个码本大小 V=256
- **优化器**：AdamW，峰值学习率 5e-4，warmup 12k 步
- **硬件**：16×A100 GPUs，总批大小 63 分钟音频

### 评估指标
| 类别 | 指标 | 描述 |
|------|------|------|
| **连续表示质量** | ABX discriminability | 判断 triphone 中心音素是否可区分（within/across speaker） |
| | MAP words | 检索相同词例的能力（Mean Average Precision） |
| **离散单元质量** | ABX (on one-hot) | 对量化后单元计算 ABX |
| | PNMI | Phone Normalized Mutual Information，衡量单元与真实音素的相关性 |
| **下游 SLM 性能** | sWUGGY | 区分真词 vs 伪词（lexical） |
| | sBLIMP | 判断句子语法正确性（syntactic） |
| | tSC (topic version) | 故事延续合理性判断（semantic） |

### 基线方法对比
- wav2vec 2.0
- HuBERT
- WavLM BASE
- data2vec / data2vec 2.0
- DinoSR（原版及作者复现）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）

| Model | sWUGGY (all) ↑ | sWUGGY (in-vocab) ↑ | sBLIMP ↑ | tSC ↑ |
|-------|----------------|--------------------|----------|--------|
| wav2vec 2.0 | 62.29 | 68.50 | 53.34 | 65.97 |
| HuBERT | 65.50 | 73.67 | 55.60 | 68.75 |
| WavLM BASE | 69.74 | 79.88 | 56.60 | 70.35 |
| DinoSR (codebook) | 60.10 | 64.56 | 57.04 | 69.44 |
| **SpidR (codebook)** | **69.78** | **79.98** | **58.10** | **70.14** |
| **SpidR (K-means)** | **71.89** | **82.46** | **59.48** | **70.46** |

> ✅ SpidR 在所有指标上均达到最佳或次佳表现，尤其在词汇层面提升显著。

### 与基线方法的对比结果
- SpidR 在 **ABX** 和 **MAP words** 上也优于其他模型（见 Table 2）：
  - ABX within speaker (dev-clean): **3.32%**（SpidR）vs 3.38%（HuBERT）vs 4.05%（DinoSR）
  - MAP words (dev-clean): **66.50%**（SpidR）vs 46.07%（HuBERT）vs 63.02%（DinoSR）

- 数据扩展分析显示，SpidR 在不同训练数据量下始终优于 HuBERT，具有稳定的增益。

### 消融实验结果（Table 9）
验证了两个关键组件的有效性：

| 变体 | ABX ↓ | MAP ↑ | Discrete ABX ↓ | PNMI ↑ |
|------|-------|-------|---------------|--------|
| DinoSR (reimpl.) | 5.91 | 42.55 | 7.73 | 0.614 |
| + Heads (intermediate pred.) | 4.22 | 63.39 | 7.87 | 0.610 |
| + Exp. EMA | 5.86 | 51.85 | 8.77 | 0.609 |
| **Full SpidR** | **3.92** | **60.88** | **6.31** | **0.602** |

> 🔍 结论：引入中间层预测头是性能提升主因；平滑 EMA 调度有助于训练稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **语音单元质量直接影响 SLM 表现**：
   - ABX 和 PNMI 与下游 SLM 性能高度相关（Pearson r ≈ -0.8 ~ 0.8），可作为可靠代理指标。
2. **SpidR 显著提升了训练稳定性**：
   - 困惑度曲线平稳，未出现 codebook collapse。
3. **SLM 与 ASR 的目标存在差异**：
   - SpidR 在 SLM 上表现优异，但在 SUPERB ASR 和 phoneme recognition 上不如 DinoSR 或 data2vec。
   - 表明“适合语言建模”的表示 ≠ “适合语音识别”的表示。

### 方法的局限性
- **专注于英语和朗读书籍语音**：未验证在多语种、噪声环境或对话场景下的泛化能力。
- **未解耦非语言信息**：当前模型侧重于语言内容，缺乏对韵律、情感、说话人等属性的显式建模。
- **离散单元仍具上下文依赖性**：尽管改进，单元仍未完全达到音素级别的抽象。

### 未来工作方向
- 扩展至多语言和生态化语音（ecological speech）场景。
- 探索如何在同一个编码器中同时学习语言、韵律、说话人等解耦表示。
- 构建端到端的 textless 大模型，支持生成、理解、对话等多种任务。
- 探索更适合 SLM 的新型架构与目标函数，而非沿用 ASR 导向的设计。

---

> 📦 **开源信息**：作者已将训练代码和模型检查点开源：  
> [https://github.com/facebookresearch/spidr](https://github.com/facebookresearch/spidr)

</details>

---

### 9. [Can LLMs Predict Their Own Failures? Self-Awareness via Internal Circuits](https://arxiv.org/abs/2512.20578)

**Authors**: Amirhosein Ghasemabadi, Di Niu  
**Category**: cs.CL  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20578v1  

#### Abstract
Large language models (LLMs) generate fluent and complex outputs but often fail to recognize their own mistakes and hallucinations. Existing approaches typically rely on external judges, multi-sample consistency, or text-based self-critique, which incur additional compute or correlate weakly with tr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Can LLMs Predict Their Own Failures? Self-Awareness via Internal Circuits

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）虽然在生成任务上表现出色，但**缺乏对自身错误的识别能力**，常常产生自信但错误的回答（即“幻觉”）。现有的自检方法依赖外部评判器、多采样一致性或基于文本的自我批判，这些方法存在以下问题：
- 额外计算开销大；
- 与真实正确性相关性弱；
- 无法在推理过程中实时检测失败。

本文提出一个根本性问题：**LLMs 能否通过观察其内部状态来预测自身的失败？**

### 提出的新方法：Gnosis
作者提出了 **Gnosis** —— 一种轻量级的**内在自我验证机制**，使冻结的 LLM 能够通过分析其推理过程中的隐藏状态（hidden states）和注意力模式（attention patterns）来自我判断输出的正确性。

#### 核心创新点：
- **Intrinsic Self-Awareness（内在自我意识）**  
  不依赖外部模型或额外生成样本，而是直接从 LLM 内部动态中提取可靠性信号。
- **Dual-Stream Introspection（双流内省架构）**  
  同时建模：
  - **Hidden-State Circuit**：捕捉表示演化的轨迹特征；
  - **Attention Circuit**：分析注意力路由的稳定性与结构性。
- **Fixed-Budget Compression（固定预算压缩）**  
  将变长的内部轨迹压缩为固定大小的描述符，使得 Gnosis 的推理成本与序列长度无关，仅增加约 **5M 参数** 和极低延迟（~25ms）。
- **Zero-Shot Generalization（零样本泛化）**  
  支持跨模型尺度迁移（如在 1.7B 上训练可判断 8B 模型）、早期失败检测（partial generation 判断），实现计算感知控制。

### 相比现有方法的优势
| 方法类型 | 典型代表 | 缺陷 | Gnosis 的优势 |
|--------|---------|------|-------------|
| 外部评判器 | SkyworkRM, Gemini 2.5 Pro | 成本高、需标注数据、部署复杂 | 更小（~5M vs 8B）、无需微调、零额外世界知识 |
| 文本自评 | Logit Entropy, Self-Critique | 只反映流畅度而非逻辑正确性 | 基于内部表征，更贴近真实错误机制 |
| 多样本一致性 | Self-Consistency | 推理成本随样本数线性增长 | 单次前向即可完成判断 |
| 单token探针 | MLP-Probe | 忽略生成全过程的时空结构 | 利用完整生成轨迹的 spatiotemporal 结构 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**（混合领域）：
  - 数学推理：DAPO-Math-17k（约14k竞赛题）
  - 开放域问答：TriviaQA 子集（40k问题）
- **测试基准**（三个独立领域）：
  1. **Math-Reasoning**：AMC12、AIME24/25、HMMTFeb2025（多步符号推理）
  2. **Open-Domain QA**：TriviaQA（held-out 18k，短事实答案）
  3. **Academic Knowledge**：MMLU-Pro（14个学科，out-of-distribution 泛化）

所有数据均自动标注（对比 GT 判断正误），无需人工标注。

### 实验设置
- **Backbone 模型**（全部冻结）：
  - Qwen3 系列：1.7B-Hybrid, 4B-Thinking, 4B-Instruct, 8B-Hybrid
  - OpenAI gpt-oss-20B（MoE 架构）
- **输入信号**：
  - 最后一层 `hidden states` $ H_{\text{last}} \in \mathbb{R}^{S \times D} $
  - 所有层的 `attention maps` $ A_{l,h} \in \mathbb{R}^{S \times S} $
- **压缩方式**：
  - Hidden States：自适应池化至固定长度 $ K_{\text{hid}} = 192 $
  - Attention Maps：下采样为 $ k \times k = 256 \times 256 $ 网格

### 评估指标
将正确性预测视为二分类任务，报告：
- **AUROC**：区分正确/错误生成的能力
- **AUPR-c / AUPR-e**：分别以“正确”或“错误”为正类的精确率-召回曲线下面积
- **Brier Skill Score (BSS)**：校准质量（>0 表示优于先验基线）
- **Expected Calibration Error (ECE)**：越低越好，衡量预测概率与实际准确率的一致性

### 基线方法对比
| 类别 | 基线方法 |
|------|--------|
| 内部统计指标 | Logit Entropy, Mean Token Prob, Attn Eigenvalue Score |
| 跨层动态分析 | CoE-R, CoE-C |
| 外部评判器 | SkyworkRM-Llama3.1-8B, SkyworkRM-Qwen3-8B, Gemini 2.5 Pro |
| 可学习探针 | MLP-Prob（仅最终token隐藏态） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1 & 2）

| 方法 | Math-Reasoning (AUROC) | TriviaQA (AUROC) | MMLU-Pro (AUROC) |
|------|------------------------|------------------|------------------|
| **Gnosis (Ours)** | **0.95–0.96** | **0.87–0.89** | **0.80–0.82** |
| SkyworkRM-Qwen3-8B | 0.90–0.93 | 0.72–0.84 | 0.73–0.76 |
| Gemini 2.5 Pro | 0.88–0.92 | 0.79–0.83 | 0.70–0.78 |
| Logit Entropy | ~0.79 | ~0.64 | ~0.73 |

> ✅ Gnosis 在所有领域均显著超越外部评判器（包括百亿美元级 Gemini），且参数量仅为后者的 **1/1000**。

### 与基线方法的对比结果
- **相比内部指标**：
  - AUROC 提升约 15–20 pts（如从 0.79 → 0.95）
  - BSS 从负值转为正值（表明校准质量大幅提升）
- **相比 MLP-Prob 探针**（Table 2）：
  - 在 Math 上 AUROC 从 0.86 → 0.95（+9 pts）
  - 证明**整个生成轨迹的信息远超单一 token**
- **效率对比**（Table 4）：
  - 当响应长度达 24k tokens 时，Gnosis 比 SkyworkRM 快 **99倍**（25ms vs 2465ms）
  - 延迟几乎不随长度增长

### 消融实验结果（Ablation Studies）

#### （1）双流架构有效性（Table 5）
| 模型变体 | Math AUROC | TriviaQA AUROC | MMLU-Pro AUROC |
|---------|------------|---------------|----------------|
| Hidden-Only | 0.92 | 0.87 | 0.78 |
| Attention-Only | 0.92 | 0.78 | 0.80 |
| **Full Gnosis (Fused)** | **0.95** | **0.87** | **0.80** |

> 🔍 发现：隐藏态主导短事实任务（TriviaQA），注意力流在长推理任务中更重要，融合效果最佳。

#### （2）注意力特征提取器比较（Table 6）
| 提取方式 | Math AUROC | TriviaQA AUROC | MMLU-Pro AUROC |
|----------|-----------|----------------|----------------|
| CNN-only | 0.92 | 0.79 | 0.79 |
| Stats-only | 0.92 | 0.75 | 0.76 |
| **CNN+Stats (hybrid)** | **0.92** | **0.78** | **0.80** |

> ✅ Hybrid 设计最稳定，兼具可解释性与表达力。

#### （3）其他关键设计选择
- **移除局部时间卷积（Phase 1）**：AUROC ↓ 0.03（说明局部动态建模重要）
- **替换 PMA 为平均池化**：AUROC ↓ 0.07（说明 learnable prototype 更有效）
- **简化为 Pooled-MLP 基线**：AUROC ↓ 0.10（证明全局交互必要）

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLMs 的内部状态蕴含丰富的可靠性信号**  
   正确与错误生成在隐藏态和注意力分布上有明显差异，可通过轻量模块有效解码。
2. ✅ **Gnosis 是首个真正高效的 intrinsic self-verifier**  
   仅添加 ~5M 参数，在多个 backbone 上一致优于数十亿参数的外部评判器。
3. ✅ **支持 zero-shot 跨模型判断（Sibling Modeling）**  
   在 1.7B 上训练的 Gnosis 可直接用于判断 4B/8B 模型输出，AUROC 达 0.93–0.97，甚至超过专用 reward model。
4. ✅ **支持 early failure detection**  
   观察到 40% 的生成路径后即可达到接近完整的判断性能，可用于提前终止无效推理链，节省算力。

### 方法的局限性
- ❗ **非通用 reward model**：适用于同一家族模型（如 Qwen3 系列），难以迁移到架构或生成风格迥异的模型（如 Thinking → Instruct）。
- ❗ **依赖高质量训练信号**：目前依赖自动标注（GT 对比），若 GT 不可用则需引入人类反馈或其他代理信号。
- ❗ **未处理 non-response 或格式错误**：实验中已过滤无有效答案的情况。

### 未来工作方向
- 🔄 将 Gnosis 集成到 test-time scaling 中，实现动态计算分配；
- 🔁 用于强化学习中的过程奖励建模（Process Reward Modeling）；
- 🧠 探索更细粒度的“元认知”机制，例如定位具体哪一步出错；
- 🌐 扩展至多模态模型的自我监控。

---

> 💡 **一句话总结**：  
> Gnosis 证明了 LLMs 可以通过“内观”自己的隐藏态和注意力模式来可靠地预测失败，实现了高效、可扩展、无需外部监督的自我意识机制，为构建安全、可控的语言系统提供了新范式。

</details>

---

### 10. [WOC: Dual-Path Weighted Object Consensus Made Efficient](https://arxiv.org/abs/2512.20485)

**Authors**: Tanisha Fonseca, Gengrui Zhang  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20485v1  

#### Abstract
Modern distributed systems face a critical challenge: existing consensus protocols optimize for either node heterogeneity or workload independence, but not both. For example, Cabinet leverages weighted quorums to handle node heterogeneity but serializes all operations through a global leader, limiti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：WOC: Dual-Path Weighted Object Consensus Made Efficient

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代分布式系统中的共识协议通常只能优化**节点异构性**（node heterogeneity）或**操作独立性**（workload independence）中的一个维度，无法兼顾两者：
- **Cabinet** 使用加权多数（weighted quorums）处理节点性能差异，但所有操作都通过单一全局 leader 序列化，限制了并行性。
- **EPaxos** 支持独立操作的并行执行，但假设所有副本能力相同，忽略了节点间的性能差异。

这种割裂导致在真实异构环境中难以同时实现高吞吐与低延迟。

### 提出的新方法与思路
作者提出 **WOC**（Weighted Object Consensus），一种**双路径共识协议**（dual-path consensus protocol），其核心思想是：
- 根据操作的访问模式动态路由到两条路径：
  - **Fast Path（快速路径）**：用于**独立对象**（Independent Objects），采用**对象粒度加权共识**（object-weighted consensus），无需 leader，完成于**一次网络往返**（1 RTT）。
  - **Slow Path（慢速路径）**：用于**共享或冲突对象**，由 leader 协调，使用**节点加权共识**（node-weighted consensus），保证正确性和线性一致性（linearizability）。

#### 创新点
1. **Dual-Path 架构设计**  
   首次将“对象级并行”与“节点级权重”结合，实现对**工作负载特征**和**节点性能差异**的双重自适应。

2. **Object-Weighted Consensus**  
   将传统的节点加权扩展至**对象粒度**，每个对象拥有独立的权重向量 `w_O`，优先选择响应快的副本形成小而快的 quorum。

3. **几何权重分配机制**（Geometric Weight Assignment）  
   引入可调陡度参数 `R ∈ [1.0, 2.0]` 控制权重分布：
   - `R ≈ 1.3`：更均匀 → 更强容错
   - `R ≈ 2.0`：更陡峭 → 更快 quorum 形成（仅需 top 几个节点）

4. **动态对象分类与路由机制**  
   运行时持续监控操作频率、冲突率、延迟等指标，将对象分为：
   - **Independent Objects** → Fast Path
   - **Common Objects** / **Hot Objects** → Slow Path

### 相比现有方法的优势
| 维度 | WOC | Cabinet | EPaxos |
|------|-----|--------|--------|
| 节点异构支持 | ✅（Fast & Slow Path） | ✅（Node-weighted） | ❌（Equal voting） |
| 操作并行性 | ✅（Fast Path 并行提交） | ❌（全部序列化） | ✅（依赖分析） |
| 吞吐量（低冲突） | ⬆️⬆️⬆️ | 中等 | 高（但无视节点差异） |
| 正确性保障 | ✅（跨路径一致性） | ✅ | ✅ |

> ✅ WOC 在保持高争用下等效性能的同时，在低争用场景下显著优于 Cabinet。

---

## 2. 核心实验方法和设置

### 实验环境
- **平台**：Compute Canada 云基础设施
- **部署配置**：
  - 默认：5 个 replica 服务器 + 2 个 client VM
  - 容错阈值：容忍 `f=2` 故障（即 `n ≥ 5`）
- **实现语言**：Go

### 工作负载设置
- **默认混合负载分布**：
  - 90% **Independent Objects**（适合 Fast Path）
  - 5% **Common Objects**（偶尔冲突）
  - 5% **Conflicting Objects**（热点对象，走 Slow Path）
- **消息大小**：512 字节
- **客户端行为**：Open-loop 请求生成，最多 5 个 in-flight 请求

### 评估维度
1. **Batch Size Scaling**：批大小从 1 到 4000
2. **Conflict Rate Sensitivity**：冲突率从 0% 到 100%
3. **Client Concurrency**：并发客户端数从 2 到 9
4. **Server Scalability**：副本数量从 3 到 9

### 评估指标
- **Throughput**（Tx/sec）
- **P50 Latency**
- **Average Latency**

### 基线方法对比
- **Cabinet**：当前最先进的 weighted consensus 协议，作为主要对比基准
- （隐含参考 EPaxos 设计理念，但未直接比较）

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 场景 | WOC 性能 | Cabinet 性能 | 提升倍数 |
|------|----------|-------------|---------|
| **Batch Size = 1000** | 220.8k Tx/sec | 77.8k Tx/sec | ~2.8× |
| **Batch Size = 4000** | 390k Tx/sec | 161k Tx/sec | ~2.4× |
| **Conflict Rate = 0–10%** | 55.9k–57.1k Tx/sec | 14.9k–15.7k Tx/sec | ~3.8× |
| **Conflict Rate = 50%** | 27.3k Tx/sec | 15.9k Tx/sec | ~1.7× |
| **Conflict Rate = 75–100%** | 11.2k–12.3k Tx/sec | 15.9k–16.2k Tx/sec | ❌（落后） |
| **Clients = 9** | 144.1k Tx/sec | 16.3k Tx/sec | ~8.8× |
| **Servers = 9** | 92.4k Tx/sec | 15.7k Tx/sec | ~5.9× |

> 🔺 **最高提升达 4× 吞吐量**（原文摘要称 up to 4×）

### 与基线方法对比结果
- **低冲突场景（<70%）**：
  - WOC 吞吐远超 Cabinet（3–4×）
  - 所有独立操作可通过 Fast Path 并行提交
- **高冲突场景（>75%）**：
  - WOC 性能下降明显，被 Cabinet 反超
  - 因为大部分操作进入 Slow Path，且 Fast Path 的协调开销成为负担
- **客户端扩展性**：
  - WOC 几乎线性增长（2→7 clients），而 Cabinet 几乎不变（leader 成瓶颈）
- **服务器扩展性**：
  - WOC 随节点增加持续提升吞吐（+66% from 3→9 nodes）
  - Cabinet 仅微幅提升（+10%），受限于中心化架构

### 消融实验与分析（隐含）
虽然没有显式的消融实验表格，但通过多维测试可推断以下结论：
- **Fast Path 是性能优势来源**：当独立对象占比高时，WOC 充分利用并行性。
- **Object-weighted quorum 有效降低延迟**：Top 加权节点即可达成 quorum，避免等待慢节点。
- **Dynamic routing 机制合理**：冲突检测与路径切换确保了安全性与效率平衡。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **双路径设计显著提升低争用场景下的吞吐量**，相比 Cabinet 最高达 **4×**。
2. ✅ **对象粒度加权共识**（object-weighted consensus）能有效利用局部访问模式，加速 quorum 形成。
3. ✅ **动态路由机制**实现了对 workload 和 replica performance 的双重自适应。
4. ⚠️ **高冲突场景下性能劣于 Cabinet**，说明 Fast Path 开销在极端情况下不可忽略。
5. 📈 **良好的横向扩展能力**：无论是 client 还是 server 数量增加，WOC 均表现出良好可扩展性。

### 方法的局限性
1. **高冲突负载不适用**：当 >75% 操作存在冲突时，WOC 不再具有优势。
2. **额外元数据开销**：需要维护 per-object 权重、in-flight map、分类状态等，带来内存和计算成本。
3. **目前基于 CFT 模型**：尚未支持 Byzantine Fault Tolerance（BFT），限制了在开放网络中的应用。
4. **权重更新策略依赖响应时间**：可能受瞬时抖动影响，需更鲁棒的反馈机制。

### 未来工作方向
1. **自适应阈值调节**（adaptive threshold tuning）：根据实时负载自动调整 `R` 参数。
2. **集成 BFT 支持**：探索在 WOC 中引入 Byzantine fault tolerance。
3. **地理分布式部署验证**：在跨区域（geo-distributed）环境下评估性能收益。
4. **更精细的对象分类模型**：引入机器学习预测冲突概率，优化路径决策。

---

## 总结
WOC 是一种面向现代异构分布式系统的高效共识协议，通过**双路径架构 + 对象级加权投票 + 动态路由**，成功解决了传统协议在“节点异构”与“操作独立”之间的权衡难题。它在**低到中等冲突的工作负载下表现卓越**，展现出强大的吞吐能力和良好的扩展性，为下一代高性能 SMR 系统提供了新的设计范式。

</details>

---

### 11. [Hard Negative Sample-Augmented DPO Post-Training for Small Language Models](https://arxiv.org/abs/2512.19728)

**Authors**: Haocheng Lu, Minjun Zhu, Henry Yu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19728v1  

#### Abstract
Large language models (LLMs) continue to struggle with mathematical reasoning, and common post-training pipelines often reduce each generated solution to a binary outcome: correct or incorrect. This perspective is limiting in practice, as failures in chain-of-thought (CoT) reasoning are frequently s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Hard Negative Sample-Augmented DPO Post-Training for Small Language Models*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大语言模型（LLMs）在数学推理任务中仍表现不佳，而主流的后训练（post-training）方法存在以下问题：
- **评价方式过于二元化**：仅将解题结果分为“正确”或“错误”，忽略了链式思维（Chain-of-Thought, CoT）中常见的**结构性错误**（如逻辑跳跃、代数错误、数值偏差等）。
- **依赖高成本判别机制**：如使用大型奖励模型（PRM）或 LLM-as-a-judge 进行偏好标注，带来高昂计算开销、误差传播和判断不稳定性。

### 🚀 提出的新方法与创新思路
作者提出了一种**轻量级、可扩展的小型语言模型（SLM）后训练框架**，其核心是：
1. **MathVerifier**：一个轻量化的双通道验证器，对候选解进行六维错误分析，并生成可解释的 `wrongness` 和 `absurdity` 分数。
2. **Hard Negative Mining**：利用 verifier 信号自动挖掘“看似合理但结构错误”的**困难负样本**（hard negatives），即模型自信输出但存在细微逻辑/数值缺陷的解。
3. **Verifier-Guided Weighted DPO**：将这些 hard negatives 构造成高质量的 preference pairs，并引入基于 verifier 输出的**样本重要性权重**，用于加权 DPO（Direct Preference Optimization）目标函数。

### 🔍 相比现有方法的优势
| 维度 | 本文方法 | 传统方法（如 PRM 或 LLM-as-a-judge） |
|------|----------|-------------------------------|
| **成本** | 轻量、无需训练大型 reward model | 高推理与训练成本 |
| **稳定性** | 程序化检查 + 语义一致性，减少主观偏差 | 易受 judge 模型偏见影响（位置偏置、自偏好等） |
| **可解释性** | 错误类型可分解为六维度，便于调试 | 黑箱评分，难以归因 |
| **效率** | 支持完全离线训练，适合资源受限场景 | 多需在线采样或复杂迭代 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **SFT阶段**：使用 **MetaMathQA**（约39.5万条带CoT的数学问答对），源自 GSM8K 和 MATH 数据集的AI增强版本。
- **DPO偏好构建阶段**：从 `HuggingFaceH4/numina-deepseek-r1-qwen-7b` 数据集中采样约60万条CoT轨迹，经 MathVerifier 过滤后保留约1万条高质量 preference pairs。

### ⚙️ 实验设置
- **基础模型**：`Qwen2.5-1.5B-Instruct`（1.5B参数）
- **微调方式**：
  - SFT 使用 **QLoRA**（4-bit NF4量化 + rank-16 LoRA适配器）
  - DPO 阶段冻结主干，仅更新 LoRA 参数
- **训练流程**：
  1. 在 MetaMathQA 上进行 SFT 得到初始策略 $\pi_{\text{sft}}$
  2. 用该策略生成多解 → 输入 MathVerifier 打分
  3. 构建 preference pairs $(y^+, y^-)$，其中 $y^+$ 是正确且结构良好的解，$y^-$ 是 verifier 识别出的 hard negative
  4. 应用 **加权 DPO 损失**，样本权重由 `wrongness`, `confidence`, `perplexity` 共同决定

### 📊 评估指标
- **Exact Match Accuracy** on：
  - **GSM8K**（小学应用题）
  - **MATH**（高中竞赛级数学题）
- 所有模型均采用相同 prompt 模板（`qwen2-boxed`）、greedy decoding（temp=0）和正则提取答案，确保公平比较。

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **Base** | 仅 SFT 后的模型 |
| **Random-uniform DPO** | 随机选择 preference pairs，统一权重（w=1） |
| **Verifier-guided Weighted DPO** | 本文方法：基于 verifier 挖掘 hard negatives 并加权训练 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Exact Match %）

| Model | MATH | GSM8K | 备注 |
|-------|------|--------|------|
| Qwen2.5-1.5B-Instruct | 55.3 | 71.9 | Base |
| + Random DPO | 54.5 (-0.8) | 72.6 (+0.7) | MATH 下降 |
| + **Hard-neg DPO** | **57.2 (+1.9)** | **74.2 (+2.3)** | ✅ 本文方法 |
| Qwen2.5-1.5B-SFT | 65.2 | 76.3 | SFT 基线 |
| + Random DPO | 64.1 (-1.1) | 77.1 (+0.8) | MATH 退化 |
| + **Hard-neg DPO** | **67.1 (+1.9)** | **80.1 (+3.8)** | ✅ 显著提升 |
| Qwen2.5-Math-1.5B-Instruct | 76.2 | 83.4 | 专用数学模型 |
| + Random DPO | 75.3 (-0.9) | 84.0 (+0.6) | MATH 反而下降 |
| + **Hard-neg DPO** | **77.7 (+1.5)** | **86.2 (+2.8)** | ✅ 持续增益 |

> 注：括号内为相对于对应 base 模型的绝对提升（+）或下降（-）

### 🔬 消融实验与关键发现
- **Hard negatives 至关重要**：相比随机采样的 preference pairs，由 verifier 引导选出的 hard negatives 更能促使模型区分“表面合理但逻辑错误”的解。
- **加权机制有效**：引入 `wrongness + confidence + perplexity` 的组合权重显著提升了学习效率，尤其是在处理“数值接近但逻辑错误”的案例时。
- **MATH 更敏感于结构错误**：Random DPO 在 MATH 上常导致性能下降，说明盲目添加偏好数据可能破坏已有能力；而 verifier-guided 方法能稳定提效。
- **训练动态更优**：Verifier-guided DPO 达到更低的最终 loss，且 reward 分布集中在高信息密度样本上（图5b），表明学习更聚焦于关键错误模式。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **结构化错误分析比二值判断更有价值**：通过六维 error profile（语义、结构、顺序、逻辑、符号、答案一致性）可精准定位失败原因，支持更有针对性的优化。
2. **Hard negatives 是 DPO 成功的关键**：真正有价值的 negative 不应是胡言乱语，而是那些“看起来很像对但实际上错”的解，这类样本迫使模型深入理解推理过程。
3. **Verifier 可作为低成本替代方案**：轻量级 MathVerifier 能有效替代昂贵的 PRM 或 LLM-as-a-judge，在有限算力下实现高效、稳定的 post-training。
4. **加权 DPO 提升信息利用率**：通过 verifier 输出的重要性权重，可在 batch 内动态调整梯度贡献，避免噪声样本主导训练。

### ⚠️ 局限性
- **启发式设计较多**：六维评分的聚合方式、权重公式均为经验设定，缺乏理论最优保证。
- **纯离线流程**：未实现闭环反馈（如 online RL），verifier 固定不变，无法随 policy 演进而进化。
- **规模受限**：目前仅在 1.5B 模型上验证，尚未测试更大模型或更多样化任务（如代码、形式证明）。

### 🔮 未来工作方向
1. 将 verifier 权重机制形式化为 **active learning 或 importance sampling** 框架，提升理论基础。
2. 探索 **co-training verifier 与 policy**，或将 verifier 蒸馏为紧凑 PRM，嵌入在线 RL 流程。
3. 将本框架推广至其他需要结构化推理的任务领域，如 **code generation、formal reasoning、科学问答** 等。

---

## 总结
本文提出了一种**以 hard negative 为核心的轻量级 DPO 后训练范式**，通过 **MathVerifier** 实现细粒度错误诊断与高价值样本挖掘，在不依赖大型 reward model 或人工标注的前提下，显著提升了小型语言模型在数学推理任务上的表现。其核心洞见是：**改进模型的关键不在更多数据，而在更聪明地选择“最有教育意义”的训练样本**。这一思想对低资源场景下的 LLM 对齐具有重要实践意义。

</details>

---

### 12. [OpComm: A Reinforcement Learning Framework for Adaptive Buffer Control in Warehouse Volume Forecasting](https://arxiv.org/abs/2512.19738)

**Authors**: Wilson Fung, Lu Guo, Drake Hilliard, Alessandro Casadei, Raj Ratan, Sreyoshi Bhaduri, Adi Surve, Nikhil Agarwal, Rohit Malshe, Pavan Mullapudi, Hungjen Wang, Saurabh Doodhwala, Ankush Pole, Arkajit Rakshit  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.19738v1  

#### Abstract
Accurate forecasting of package volumes at delivery stations is critical for last-mile logistics, where errors lead to inefficient resource allocation, higher costs, and delivery delays. We propose OpComm, a forecasting and decision-support framework that combines supervised learning with reinforcem...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*OpComm: A Reinforcement Learning Framework for Adaptive Buffer Control in Warehouse Volume Forecasting*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在**最后一公里物流**（last-mile logistics）中，配送站点的包裹量预测不准确会导致资源分配不当、成本上升和交付延迟。传统方法依赖启发式规则，难以捕捉站点间的异质性和动态需求变化，尤其在缓冲区管理（buffer control）方面缺乏自适应能力。

### 🚀 提出的新方法与创新思路
作者提出 **OpComm** —— 一个结合监督学习、强化学习与生成式AI的决策支持框架，用于仓库级别的体积预测与自适应缓冲控制。其核心创新包括：

- **多模块集成架构**：
  - 使用 **LightGBM** 进行站点级需求预测，提供上下文输入；
  - 引入基于 **Proximal Policy Optimization (PPO)** 的强化学习代理，从离散动作集中选择最优缓冲水平；
  - 设计**非对称奖励函数**，对“缓冲不足”（under-buffering）施加更高惩罚，反映真实业务中未满足需求的风险远高于资源浪费；
  - 加入**蒙特卡洛更新机制**，实现策略的持续在线适应；
  - 集成**生成式AI层**，利用SHAP特征归因生成可解释的执行摘要和情景分析，提升决策透明度。

- **将缓冲控制建模为上下文强化学习问题**（contextual reinforcement learning），使策略能根据预测需求动态调整。

### 🔍 相比现有方法的优势
| 维度 | OpComm优势 |
|------|-----------|
| 准确性 | 显著降低WAPE误差（↓21.65%） |
| 决策质量 | 减少under-buffering事件，降低运营风险 |
| 自适应性 | 蒙特卡洛反馈机制支持持续学习与策略演化 |
| 可解释性 | 通过生成式AI输出自然语言报告，促进管理者采纳 |
| 实用性 | 平衡统计严谨性与实际操作可行性，在高 stakes 物流场景中更具落地价值 |

---

## 2. 核心实验方法和设置

### 📊 数据集
- 来源：北美地区 **400+个仓库配送站点** 的历史运营数据；
- 包含时间序列信息、站点特征、容量利用率、季节性模式等；
- 排除历史数据不足的站点以防止过拟合。

### ⚙️ 实验设置
- **模型结构**：
  - LightGBM 回归模型独立训练于每个站点，采用时序划分（temporal train-test split）；
  - PPO Agent 动作空间为离散缓冲等级 `{b₁, b₂, ..., bk}`，作用于 LightGBM 输出的预测值上；
  - 奖励函数设计如下：
    $$
    r_t = -\alpha \cdot \max(0, D_t - D_t^{\text{forecast}} - b_t) + \beta \cdot \max(0, b_t + D_t^{\text{forecast}} - D_t)
    $$
    其中 $\alpha > \beta$，强调对 under-buffering 的惩罚。
- **策略优化**：使用 PPO 的裁剪目标函数进行稳定训练，配合 Monte Carlo 回报估计计算优势函数（Advantage Estimator）。

### 📈 评估指标
- 主要指标：
  - **WAPE**（Weighted Absolute Percentage Error）：衡量预测准确性；
  - **Under-buffering (%)** 和 **Over-buffering (%)**：评估缓冲策略的有效性；
  - **WAPE标准差**：反映区域间稳定性。
- 对比方式：OpComm vs. 手工预测（manual forecasting）基线。

### 🆚 基线方法
- **Manual Forecasting**：当前行业常用的启发式人工预测方法，作为主要对比基准。

> 注：文中未提及与其他ML或RL方法（如DQN、TRPO）的直接比较，也无消融实验。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）
| 区域 | 方法 | WAPE (%) | WAPE Std. Dev. (%) | Under-buffering (%) | Over-buffering (%) |
|------|------|----------|---------------------|-----------------------|---------------------|
| North-East | Manual | 4.95 | 1.20 | 18.3 | 12.5 |
|            | OpComm | **3.85** | **0.95** | **10.2** | 15.1 |
| Mid-West   | Manual | 5.12 | 1.35 | 20.1 | 13.0 |
|            | OpComm | **3.90** | **1.05** | **11.0** | 14.2 |
| South      | Manual | 4.88 | 1.18 | 16.7 | 11.9 |
|            | OpComm | **3.70** | **0.90** | **9.5**  | 13.5 |
| West       | Manual | 4.99 | 1.22 | 17.9 | 12.7 |
|            | OpComm | **3.80** | **0.92** | **10.0** | 14.0 |

### 🔍 总体表现
- **WAPE平均下降 21.65%**（从 4.95% → 3.85%）；
- 在 **93.7% 的站点** 上提升了预测精度；
- **Under-buffering 平均减少约 8 个百分点**，显著降低缺货风险；
- 尽管 over-buffering 略有上升（+2~3%），但整体更偏向保守策略，符合业务偏好；
- WAPE 标准差下降，说明模型跨区域表现更稳定。

> ❗ 注意：虽然 over-buffering 上升，但由于 reward 设计中 $\alpha > \beta$，系统优先避免更严重的 under-buffering 成本，属于合理权衡。

### 🔍 消融实验
- 文中**未提供消融实验**（ablation study），无法量化各模块（如LightGBM、PPO、GenAI）的具体贡献。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **OpComm显著优于手工预测**，不仅提高预测准确性，还优化了缓冲决策；
2. **非对称奖励设计有效引导策略偏向保守**，降低了运营中断风险；
3. **PPO + Monte Carlo 更新机制适合非平稳环境下的持续学习**；
4. **生成式AI增强了解释性与可用性**，有助于推动算法在实际运营管理中的采纳；
5. 该框架成功实现了**预测建模与决策支持的闭环整合**，适用于高风险、高复杂度的物流场景。

### ⚠️ 局限性
- 缺乏与其他先进RL算法（如SAC、DQN）或预测模型（如Transformer、LSTM）的横向对比；
- 未进行模块级消融实验，难以判断各组件的重要性；
- 所有站点独立建模，尚未考虑站点之间的网络协同效应；
- 实验仅限北美地区，泛化能力有待验证。

### 🔮 未来工作方向
- 探索 **meta-learning** 用于动态特征表示学习；
- 尝试其他 **policy optimization 算法** 替代 PPO；
- 扩展至 **网络层级协调**（network-level coordination across stations）；
- 开展 **可解释性方法的形式化评估**，研究生成式AI输出对人类决策的影响。

---

## 总结
**OpComm** 是一个面向实际应用的端到端决策框架，巧妙融合了 LightGBM、PPO 和 Generative AI，在真实仓储物流环境中实现了预测精度与运营决策质量的双重提升。它不仅是技术上的集成创新，更是**连接机器学习与运营管理实践的重要桥梁**，为智能供应链系统的构建提供了可复制的范式。

</details>

---

### 13. [HGAN-SDEs: Learning Neural Stochastic Differential Equations with Hermite-Guided Adversarial Training](https://arxiv.org/abs/2512.20272)

**Authors**: Yuanjian Xu, Yuan Shuai, Jianing Hao, Guang Zhang  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.20272v1  

#### Abstract
Neural Stochastic Differential Equations (Neural SDEs) provide a principled framework for modeling continuous-time stochastic processes and have been widely adopted in fields ranging from physics to finance. Recent advances suggest that Generative Adversarial Networks (GANs) offer a promising soluti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# HGAN-SDEs: Learning Neural Stochastic Differential Equations with Hermite-Guided Adversarial Training —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **Neural SDEs**（神经随机微分方程）在建模连续时间随机过程方面具有强大能力，广泛应用于物理、金融等领域。
- 然而，训练 Neural SDEs 面临挑战：**精确似然不可计算**，传统基于变分的方法受限于近似假设。
- 使用 GAN 进行对抗训练是一种可行的替代方案，但现有方法（如基于 Neural CDE 的判别器）存在两大瓶颈：
  - **计算成本高**
  - **对抗训练不稳定**

### 🚀 提出的新方法：HGAN-SDEs
作者提出 **HGAN-SDEs**，一种基于生成对抗网络（GAN）的新框架，其核心创新在于：
- 将 **Neural Hermite 函数** 引入作为 GAN 中的判别器（discriminator），用于高效且稳定地捕捉路径级动态特征。

#### 创新点详解：
1. **Hermite 函数作为判别器基础**
   - 利用 Hermite 函数构成的正交多项式基来逼近 SDE 的转移密度（transition density）。
   - 具备数学上的完备性和正交性，能以较少项数实现高精度近似。

2. **轻量级与高效性**
   - 相比复杂的 Neural CDE 判别器，Hermite-based 判别器结构更简单，参数更少，显著降低运行时间和内存消耗。

3. **提升训练稳定性**
   - Hermite 函数良好的统计性质有助于缓解 GAN 训练中的梯度不稳定问题，提高收敛鲁棒性。

4. **理论保障**
   - 证明了该框架具备 **通用逼近能力**（universal approximation property），可在适当条件下逼近任意由 SDE 驱动的概率分布。
   - 给出了 Hermite 展开在 L² 空间下的收敛性定理（Theorem 2）以及判别能力分析（Theorem 3）。

### 🔍 相比现有方法的优势
| 方面 | HGAN-SDEs | 传统方法（如 GAN-SDEs + CDE） |
|------|-----------|-------------------------------|
| 计算效率 | ⬇️ 极低（仅需 1.2 GPU-hour） | ⬆️ 高（高达 10.0 GPU-hour） |
| 训练稳定性 | ⬆️ 高（收敛快、波动小） | ⬇️ 低（易崩溃） |
| 表达能力 | ⬆️ 强（正交基保证表达力） | ⬆️ 强但代价高 |
| 实现复杂度 | ⬇️ 简单（MLP 参数化即可） | ⬆️ 复杂（需解控制微分方程） |

---

## 2. 核心实验方法和设置

### 📚 数据集

#### 合成数据集（Simulation Datasets）
遵循 Yacine [30] 的设定，构建四类经典 SDE 模型的数据：
- **GBM**（Geometric Brownian Motion）
- **OU**（Ornstein-Uhlenbeck Process）
- **CIR**（Cox-Ingersoll-Ross Model）
- **Polynomial Drift SDE**

每组含 20,000 训练样本 + 6,000 测试样本，输入为前 100 时间步，预测后 50 步。

#### 真实世界数据集（Real-world Datasets）
- **Stock-AAL / Stock-ADBE**：美股五分鐘交易价格（2017–2018），体现高频金融市场的随机波动。
- **Traffic**：北京交通流量记录（2022年，共21,600条），具强周期性。

### 📊 评估指标
采用四个互补指标全面评价模型性能：
| 指标 | 全称 | 描述 |
|------|------|------|
| **MISE** | Mean Integrated Squared Error | 路径整体拟合误差，越小越好 |
| **TD** | Tail Difference | 尾部（>5% 分位数）差异，衡量极端事件建模能力 |
| **MSE** | Mean Squared Error | 点对点预测误差 |
| **MMD** | Maximum Mean Discrepancy | 真实路径与生成路径之间的分布距离 |

> 所有指标均为“越低越好”。

### 🧪 基线方法对比
涵盖主流时序建模范式：
- **线性模型**：DLinear
- **卷积模型**：MICN, TimesNet
- **注意力机制**：Transformer, Informer, Autoformer
- **RNN 模型**：SegRNN
- **状态空间模型**：Mamba
- **SDE-based 模型**：
  - Latent-SDEs
  - GAN-SDEs（使用 CDE 判别器）

所有生成器统一使用相同结构的 Neural SDE，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 性能对比（见 Table 1 & Table 3）

#### 在合成数据上（Table 1）：
| 模型 | GBM-MISE ↓ | OU-MISE ↓ | CIR-MISE ↓ | PolyDrift-MISE ↓ |
|------|------------|-----------|------------|------------------|
| DLinear | 6.00 | 61.61 | 196.79 | 5.18 |
| Transformer | 0.74 | 1.22 | 2.46 | 9.47 |
| Latent-SDEs | 0.44 | 101.40 | 10.00 | 6.81 |
| GAN-SDEs | 0.38 | 214.54 | — | 0.27 |
| **HGAN-SDEs** | **0.17** ✅ | **0.89** ✅ | **0.18** ✅ | **0.04** ✅ |

> ✅ 在所有任务中均取得最优表现，尤其在非线性扩散系统（如 CIR 和 Polynomial Drift）上优势明显。

#### 在真实数据上（Table 3）：
| 模型 | Stock-AAL-MISE ↓ | Stock-ADBE-MISE ↓ | Traffic-MISE ↓ |
|------|------------------|--------------------|----------------|
| Transformer | 59.80 | 485.93 | 1885.06 |
| Latent-SDEs | 35.52 | 34.67 | 32.17 |
| GAN-SDEs | 38.00 | 37.89 | 35.79 |
| **HGAN-SDEs** | **2.89** ✅ | **8.05** ✅ | **0.22** ✅ |

> ✅ 在金融和交通场景下均大幅领先，说明其对真实复杂随机系统的适应能力强。

### 🔍 消融实验与进一步探索（Section 4.3）

#### (1) Hermite 函数项数的影响（Figure 2）
- 随着 Hermite 项数增加，MISE 先下降后趋于平稳。
- **3–4 项已足够达到最佳性能**，更多项反而可能导致过拟合并增加训练负担。
- 结论：**少量 Hermite 项即可实现高质量路径判别**，验证了方法的高效性。

#### (2) 计算效率对比（Table 4）
| 判别器类型 | MISE ↓ | GPU-hours ↓ |
|----------|--------|-------------|
| LSTM | 1.12 | 6.5 |
| MLP | 0.95 | 4.8 |
| CDE（GAN-SDEs） | 0.11 | 10.0 |
| **Hermite（HGAN-SDEs）** | **0.04** ✅ | **1.2** ✅ |

> ✅ HGAN-SDEs 不仅性能最好，而且训练速度最快、资源消耗最低，适合大规模或资源受限场景。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Hermite 函数是建模 SDE 路径分布的理想工具**：
   - 数学上严谨，具备正交性、完备性；
   - 可有效逼近 SDE 的转移密度，尤其适用于稳态分析。

2. **HGAN-SDEs 显著优于现有方法**：
   - 在合成与真实数据上均取得 SOTA 性能；
   - 特别擅长处理重尾、非线性扩散等复杂随机动力系统。

3. **兼顾效率与稳定性**：
   - 训练速度快（平均 1.2 GPU-hour）；
   - 收敛稳定，不易出现模式崩溃（mode collapse）。

4. **轻量化设计有效**：
   - 仅需 3–4 个 Hermite 项即可获得高性能；
   - 判别器可用标准 MLP 实现，无需复杂架构。

### ⚠️ 局限性
- 当前方法主要针对一维 SDE 设计，扩展到高维系统可能面临“维度灾难”。
- 对非平稳过程（non-stationary dynamics）的建模能力尚未充分验证。
- Hermite 展开依赖于密度可积性假设，在某些奇异扩散情形下可能失效。

### 🔮 未来工作方向
1. 推广至多变量（multivariate）SDE 建模，结合张量积形式的高维 Hermite 基。
2. 结合自适应截断策略，动态选择 Hermite 项数以平衡精度与效率。
3. 应用于更多实际领域，如期权定价、风险建模、气候模拟等。
4. 探索与其他无监督学习框架（如 Score Matching 或 Flow-based Models）融合的可能性。

---

## ✅ 总结
**HGAN-SDEs** 是一个将 **Neural SDE** 与 **Hermite 函数理论**巧妙结合的新型 GAN 框架。它通过引入正交、轻量、可解释的 Hermite 基函数作为判别器，解决了传统方法在计算效率和训练稳定性方面的痛点，在多种 SDE 系统中实现了**更高精度、更快收敛、更强泛化能力**的表现，为神经随机微分方程的学习提供了新的高效路径。

</details>

---

### 14. [Scaling Reinforcement Learning for Content Moderation with Large Language Models](https://arxiv.org/abs/2512.20061)

**Authors**: Hamed Firooz, Rui Liu, Yuchen Lu, Zhenyu Hou, Fangzhou Xiong, Xiaoyang Zhang, Changshu Jian, Zhicheng Zhu, Jiayuan Ma, Jacob Tao, Chaitali Gupta, Xiaochang Peng, Shike Mei, Hang Cui, Yang Qin, Shuo Tang, Jason Gaedtke, Arpit Mittal  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.20061v1  

#### Abstract
Content moderation at scale remains one of the most pressing challenges in today's digital ecosystem, where billions of user- and AI-generated artifacts must be continuously evaluated for policy violations. Although recent advances in large language models (LLMs) have demonstrated strong potential f...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Scaling Reinforcement Learning for Content Moderation with Large Language Models*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文聚焦于**大规模内容审核（content moderation）中的核心挑战**，即在标签稀疏、政策定义动态变化、且需要复杂语义推理的场景下，如何高效训练大型语言模型（LLMs）以实现专家级准确度的内容分类。

传统方法如 **Supervised Fine-Tuning (SFT)** 在高质量标注数据稀缺时表现受限，而简单的基于最终标签匹配的 **Reinforcement Learning (RL)** 容易导致奖励黑客（reward hacking）、推理链崩溃（reasoning collapse）等问题。本文系统地探索并优化了 RL 在工业级内容审核任务中的可扩展性与稳定性。

---

### **提出了什么新方法或新思路**

1. **多维度奖励塑造（Reward Shaping）框架**  
   提出结合四种奖励信号的综合奖励函数：
   - `R_acc`：最终预测准确性（verifiable reward）
   - `R_fmt`：输出格式一致性
   - `R_len`：鼓励合理长度的推理链，防止“长度坍缩”
   - `R_rub`：基于 rubric 的 LLM-as-Judge 奖励，对推理过程进行细粒度评估（如事实性、忠实性、政策遵循）

   综合奖励形式为：
   $$
   R_{\text{total}} = \alpha_{\text{acc}} R_{\text{acc}} + \alpha_{\text{fmt}} R_{\text{fmt}} + \alpha_{\text{len}} R_{\text{len}} + \alpha_{\text{rub}} R_{\text{rub}}
   $$

2. **Reflection-Aided Prompting**  
   引入三阶段提示机制：
   - 初始判断（first decision）
   - 子标签反思（sub-label reflection）
   - 最终决策（last decision）  
   通过让模型自我反思其推理路径，显著改善置信度分布的双峰问题（bimodal distribution），提升阈值决策稳定性。

3. **Monte-Carlo Score Aggregation**  
   对同一输入采样多个推理路径，利用期望公式估计更稳定的概率输出：
   $$
   P_\theta(y|q) = \mathbb{E}_{r \sim P_\theta(r|q)}[P_\theta(y|r,q)]
   $$
   有效缓解因单次推理主导而导致的极端置信度问题。

4. **Disagreement Filtering 数据筛选策略**  
   利用预训练模型多次生成结果，识别“分歧样本”（disagreement examples），即不同温度下预测不一致的样本。这类样本被认为具有更高学习价值，用于提升 RL 的数据效率。

5. **工业级 RL 训练 Recipe 设计**  
   提供了一套完整的训练指南，涵盖 GRPO 算法选择、sequence-level reward normalization、KL系数设为0、effective batch size 至少1024等关键配置。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **数据效率** | RL-Only 在仅数百样本上即可媲美 SFT 数万样本性能，**实现 10×–100× 更高的数据效率** |
| **泛化能力** | 能处理复杂、条件性强、上下文依赖的政策逻辑，超越浅层模式匹配 |
| **稳定性** | 多重干预（rubric reward, reflection, MC sampling）显著降低 reward hacking 和 reasoning collapse 风险 |
| **可扩展性** | 展示了 RL 性能在数据量、rollout 数、优化步数上的 **sigmoid-like scaling behavior**，便于资源规划 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- 来源于 **Meta Platforms, Inc.** 的三个真实生产环境中的内容审核任务（Task1, Task2, Task3），均为二分类任务（是否违反政策）。
- 数据包含文本及部分多模态信息（如账户资料、生物描述、国家、粉丝数、历史内容等）。
- 每个任务仅有数百至数千条高质专家标注数据，体现典型的**标签稀缺场景**。

---

### **实验设置**

- **模型架构**：使用 Qwen2.5-VL-7B 和 Qwen-3-8B 等开源 LLM 作为基础模型。
- **RL 算法**：采用 **Group Relative Policy Optimization (GRPO)**，替代传统 PPO，无需显式 value network，计算更高效。
- **训练框架对比**：比较 HuggingFace TRL 与 **VeRL**，后者基于 HybridFlow 实现更高吞吐（最高达 2.5×）。
- **奖励设计**：综合 accuracy、format、length、rubric 四类 reward。
- **KL 控制**：β = 0，允许更强探索。
- **Normalization**：采用 **sequence-level reward normalization** 提升长推理任务稳定性。

---

### **评估指标**

| 指标 | 含义 |
|------|------|
| **R@P90** | Recall at Precision ≥ 90%，强调高精度下的召回能力（工业常用） |
| **PRAUC** | Precision-Recall 曲线下面积，适用于类别不平衡任务 |
| **F1 Score** | 精确率与召回率的调和平均 |
| **Faithfulness / Factuality** | 使用 Gemini-2.5-Pro 和 HHEM 模型作为 judge，评估推理是否忠于指令与事实 |
| **Confidence Calibration** | 分析输出概率分布是否合理（避免双峰） |

---

### **基线方法对比**

| 基线方法 | 描述 |
|--------|------|
| **SFT-COT** | 基于 Chain-of-Thought 的监督微调 |
| **Zero-shot SFT** | 无微调直接推理 |
| **RL-Only** | 直接在 base model 上应用 RL |
| **SFT → RL** | 先 SFT 再 RL 微调 |
| **maj@N / pass@N** | 多 rollout 下多数投票正确率 / 至少一个正确率，用于分析响应选择机制 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ 数据效率对比（Figure 6）

- 在 Task1 中，**RL-Only（661样本） ≈ SFT（60K样本）**
- 在 Task3 中，**RL-Only（200样本） > SFT（20K样本）**
- 表明 RL 可实现 **10×–100× 数据效率提升**

#### ✅ Reward Shaping 效果（Table 6）

| 模型 | Reward Setup | F1 |
|------|--------------|-----|
| Qwen-38B | R_acc + R_fmt | 0.49 |
| Qwen-38B | + R_len | 0.49（未提升） |
| Qwen-38B | + R_rub | **0.61**（↑24.5%） |

> **Rubric-based reward 贡献最大增益**

#### ✅ Disagreement Filtering 结果（Table 8）

| 数据子集 | 样本数 | F1 | PRAUC |
|---------|-------|-----|--------|
| All | 677 | 0.87 | 0.85 |
| Easy Only | 566 | 0.79 | 0.80 |
| Disagreement Only | **61** | **0.88** | **0.90** |

> 仅用 **61个分歧样本训练 RL** 即达到甚至超过全量数据效果 → **有效数据效率提升约 100×**

#### ✅ Monte Carlo Sampling 效果（Table 2）

| N（rollouts） | Gain on R@P90 |
|-------------|----------------|
| 1 → 4 | ↑0.05 |
| >4 | 收益递减 |

> 推荐 N=4 作为性价比最优选择

#### ✅ Effective Batch Size 影响（Table 7）

| Batch Size | R@P90 |
|-----------|--------|
| 128 | 0.18 |
| 1024 | 0.81 |
| ≥2048 | 0.85（饱和） |

> 推荐 minimum effective batch size = **1024**

#### ✅ GRPO Rollout 数影响（Figure 8 & Table 5）

- 随着 rollout 数增加，性能先升后降（存在最优区间）
- Task2 在 N=32 时达到峰值（R@P90=0.63）
- 过大 rollout 数受 LLM judge 容量限制

---

### **消融实验结果**

| 实验 | 发现 |
|------|------|
| **RL-Only vs SFT→RL** | RL-Only 数据效率高；SFT→RL 更稳定但可能引入 factuality hallucination |
| **With/Without Reflection**（Table 4） | 使用 reflection 后 PRAUC 从 0.77 → 0.89，R@P90 从 0.05 → 0.59 |
| **With/Without MC Sampling** | 显著缓解双峰分布，提升置信度校准 |
| **Remove Hard Examples** | 移除 hard examples 反而提升性能，说明噪声标签有害 |
| **Varying Temperature in GRPO** | 最佳 T ∈ [0.7, 1.0]；T > 1.0 导致解析错误增多 |

---

## 4. 关键结论和发现

### **主要发现**

1. 🔁 **RL 具有 sigmoid-like scaling behavior**  
   性能随数据量、rollout 数、compute 投入平滑上升并逐渐饱和，提供清晰的资源分配蓝图。

2. 💡 **RL 比 SFT 高出 10×–100× 数据效率**  
   尤其适合专家标注昂贵、获取缓慢的内容审核领域。

3. 🛠️ **单纯 accuracy reward 不足，需多维 reward shaping**  
   特别是 `R_rub`（rubric-based）和 `R_len` 对提升推理质量和稳定性至关重要。

4. 🧠 **RL 更像是 response selector 而非 reasoning enhancer**  
   分析表明 RL 主要作用是提高生成正确答案的概率，而非根本增强推理能力（见 maj@N vs pass@N 分析）。

5. ⚖️ **存在 faithfulness 与 factuality 的权衡**  
   - SFT→RL 提升格式一致性（faithfulness）
   - 但也更容易产生“看似合理但错误”的解释（factuality hallucination）

6. 📊 **Monte Carlo + Reflection 显著改善 confidence calibration**  
   解决了 reasoning model 输出概率极端极化的问题，使阈值决策更可靠。

7. 🔍 **Disagreement Filtering 是高效数据筛选利器**  
   自动识别高信息量样本，进一步放大 RL 的数据效率优势。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **依赖 LLM-as-Judge** | `R_rub` 依赖外部 judge 模型（如 Gemini），带来额外成本与潜在偏见 |
| **compute 开销大** | GRPO 需大量 rollout 和 high effective batch size，对算力要求高 |
| **难以完全消除 reward hacking** | 尽管有多种防御手段，仍可能出现策略性短路行为 |
| **泛化到新政策需重新训练** | 当政策频繁更新时，需持续迭代训练流程 |
| **多模态支持有限** | 当前实验主要集中在文本为主任务，图像理解能力有待加强 |

---

### **未来工作方向**

1. **自动化 rubric 构建**：探索从政策文档中自动提取评估标准，减少人工设计成本。
2. **轻量化 RL 推理**：研究 test-time scaling 与 distillation 方法，降低部署开销。
3. **动态政策适应机制**：构建能快速适应政策变更的持续学习框架。
4. **多 agent debate for moderation**：引入多个 agent 辩论机制提升决策鲁棒性。
5. **端到端 joint SFT+RL training**：探索联合优化方案，避免两阶段训练的信息损失。

---

> **总结一句话**：  
> 本文证明了 **Reinforcement Learning 是通往专家级内容审核系统的可行路径**，通过精心设计的 reward shaping、reflection prompting 和 data filtering 策略，可在极低标注成本下实现远超 SFT 的性能，并揭示了 RL 在工业场景中的可预测 scaling 规律。

</details>

---

### 15. [Scaling Point-based Differentiable Rendering for Large-scale Reconstruction](https://arxiv.org/abs/2512.20017)

**Authors**: Hexu Zhao, Xiaoteng Liu, Xiwen Min, Jianhao Huang, Youming Deng, Yanfei Li, Ang Li, Jinyang Li, Aurojit Panda  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.20017v1  

#### Abstract
Point-based Differentiable Rendering (PBDR) enables high-fidelity 3D scene reconstruction, but scaling PBDR to high-resolution and large scenes requires efficient distributed training systems. Existing systems are tightly coupled to a specific PBDR method. And they suffer from severe communication o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scaling Point-based Differentiable Rendering for Large-scale Reconstruction

## 1. 论文的主要贡献和创新点

### 解决的问题
- **现有系统缺乏通用性**：当前的分布式训练系统（如 gsplat [90] 和 Grendel [96]）仅针对特定的 PBDR 算法（如 3D Gaussian Splatting, 3DGS），无法支持不断涌现的新算法。
- **通信开销严重**：由于模型状态（即点云）和图像渲染任务在 GPU 上随机分布，导致每个 GPU 在渲染时需要从其他 GPU 获取大量远程数据，造成严重的通信瓶颈。实验显示，在现有系统中，通信时间占整个训练步骤的 **70–85%**。

### 提出的新方法与思路
作者提出了 **Gaian** ——一个通用且高效的分布式 PBDR 训练系统，其核心创新包括：

#### （1）统一的编程接口（API）
- 设计了一个抽象接口 `PBDR_program`，将所有 PBDR 算法分解为三个可自定义函数：
  - `pts_culling(view, PC)`：剔除视锥外的点
  - `pts_splatting(view, PC, infrustum_ids)`：将点投影为视图相关的“splat”
  - `image_render(view, SP)`：从前到后合成最终图像
- 这种设计既隐藏了底层分布式系统的复杂性，又显式暴露了 **数据访问模式**（例如哪个相机视角访问哪些点），从而为优化提供信息基础。

#### （2）两阶段局部性优化策略
Gaian 利用 PBDR 中独特的空间局部性特征，提出结合离线与在线优化的方法来最小化跨 GPU 数据传输：

- **离线点分配（Offline Point Placement）**
  - 将点与图像之间的访问关系建模为二分图（bipartite graph）
  - 使用 METIS 工具进行图划分，目标是使同一 GPU 存储经常被同一批图像访问的点，减少跨 GPU 边界的数据移动
  - 引入“点组”（point group）机制对点进行粗粒度分组（每组 1024–4096 个点），以降低图规模并加速划分过程
  - 支持层次化划分：先按机器划分，再在机器内部分配给本地 GPU，以区分高速（NVLink）和低速（Ethernet）通信链路

- **在线图像渲染调度（Online Image Rendering Placement）**
  - 每轮训练动态决定如何将 batch 内的图像分配给不同 GPU 渲染
  - 构造访问矩阵 $A_{j,k}$ 表示第 $j$ 张图像需从第 $k$ 个 GPU 获取多少点
  - 将调度问题形式化为整数线性规划（ILP），目标是最小化通信量和负载不平衡
  - 提出基于 **Linear Sum Assignment (LSA)** + **迭代局部搜索（Iterative Local Search）** 的快速求解器，确保低延迟在线决策
  - 支持图像分块（patch-based assignment），进一步提升调度灵活性和负载均衡能力

### 相比现有方法的优势
| 维度 | Gaian | 现有系统（如 gsplat） |
|------|-------|------------------|
| **通用性** | ✅ 支持多种 PBDR 算法（3DGS, 2DGS, 3DCX, 4DGS） | ❌ 仅支持 3DGS |
| **通信效率** | ✅ 最多减少 **91%** 的跨机通信 | ❌ 随机分布导致高通信开销 |
| **训练吞吐** | ✅ 提升 **1.50–3.71×** | ❌ 受限于通信瓶颈 |
| **扩展性** | ✅ 在 128 GPU 下实现 **12.7×** 加速 | ❌ 扩展性差 |

---

## 2. 核心实验方法和设置

### 使用的数据集
共使用 **6 个公开大规模数据集**，涵盖空中与街景场景，具有高分辨率（最高达 6K）和大图像数量（最多 60,000 张）：

| 数据集 | 类型 | 图像数 | 分辨率 | 点云大小 |
|--------|------|--------|--------|----------|
| Rubble | Aerial | 1,600 | 4K | 40M |
| Ithaca365 | Street | 8,200 | 1K | 48M |
| Campus | Street | 20,000 | 1K | 56M |
| BigCity Street | Street | 34,000 | 1K | 80M |
| Sci-Art | Aerial | 3,600 | 6K | 200M |
| BigCity Aerial | Aerial | 60,000 | 1080P | 100M |

> 注：“BigCity” 来自 MatrixCity [49] 数据集。

### 实验设置
- **硬件平台**：最多使用 32 台服务器，每台配备 4×40GB NVIDIA A100 GPU，通过 NVLink（节点内）和 88 Gbps Ethernet（节点间）连接
- **训练配置**：
  - Batch Size: 16–64
  - 最多使用 **128 GPUs**
  - 使用 PyTorch + NCCL 实现分布式通信
- **评估指标**：
  - **训练吞吐量（Throughput）**：单位时间内处理的图像数（images/s）
  - **通信体积（Communication Volume）**：每次迭代跨机器传输的点的数量
  - **PSNR**：重建质量评价指标（越高越好）
  - **强/弱可扩展性（Strong/Weak Scalability）**

### 基线方法对比
- **Baseline**: gsplat [90]（v1.4.0），目前最先进的 3DGS 分布式训练系统
- **改进后的 baseline**：
  - 添加了 Gaian 的高效数据加载机制（预解码 + CPU pinned memory）
  - 手动为其添加 2DGS 和 3DCX 的分布式支持（繁琐且易错）
- **对比维度**：训练速度、通信量、扩展性、重建质量

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）训练吞吐显著提升
在六大数据集上平均：
- **3DGS**：提速 **2.30×**（范围 1.54–3.56×）
- **2DGS**：提速 **2.50×**（范围 1.55–3.71×）
- **3DCX**：提速 **2.23×**（范围 1.50–3.01×）

> ![Figure 10](#) 显示 Gaian 在所有场景下均优于 baseline，尤其在高分辨率 Aerial 数据集上表现更优。

#### （2）通信量大幅下降
- 跨机器通信体积减少 **53.8% – 91.4%**
- 最高减少 **91.4%**（3DCX on BigCity Aerial）

| 场景 | 3DGS ↓ | 2DGS ↓ | 3DCX ↓ |
|------|--------|--------|--------|
| Rubble | 82.8% | 82.7% | 83.2% |
| Ithaca365 | 89.2% | 84.0% | 80.8% |
| Campus | 54.5% | 58.1% | 53.8% |
| BigCity Street | 57.7% | 55.5% | 54.8% |
| Sci-Art | 89.3% | 89.1% | 89.9% |
| BigCity Aerial | 89.9% | 89.9% | **91.4%** |

> 通信减少程度与吞吐提升正相关，验证了通信是主要瓶颈。

#### （3）良好的扩展性
- **弱扩展性**（固定每 GPU batch size）：
  - 从 8 到 128 GPUs，吞吐从 226.9 img/s 提升至 **2898.5 img/s**（**12.7×**）
- **强扩展性**（固定全局 batch size = 64）：
  - 吞吐从 434.7 img/s 提升至 1402.8 img/s（约 3.2×）

> Gaian 在更大规模下仍保持良好扩展趋势。

#### （4）支持超大模型训练
- 成功训练迄今为止最大的 PBDR 模型：
  - **500 million points**（29.5 billion parameters）
  - 使用 64×A100 GPUs 完成 MatrixCity Street 场景训练
- 实现 **state-of-the-art 重建质量**：
  - BigCity Aerial：测试集 PSNR 达 **26.75**
  - 如 Figure 14 所示，PSNR 随模型规模增大而持续提升

#### （5）支持复杂动态场景（3D Video）
- 实现并评估 **4D Gaussian Splatting (4DGS)** 动态重建算法
- 在 Corgi 数据集上：
  - 从 8 到 64 GPUs，吞吐提升 **4.42×**
  - 相比随机分配，Gaian 减少 **48%** 通信，提升 **26%** 吞吐

---

### 消融实验结果

#### （1）联合优化必要性（Figure 13）
- **禁用点分配（w/o Point Placement）**：
  - 吞吐下降 33%–52%
  - 即便启用图像调度也无法弥补
- **禁用图像调度（w/o Rendering Placement）**：
  - 性能退化至接近 baseline 水平
- ✅ 结论：**必须同时优化点放置和图像调度才能发挥最大效益**

#### （2）其他组件消融（Table 4）
| 组件移除 | 平均性能损失 |
|---------|-------------|
| 层次化分配（w/o Hier） | 16%–35% ↓ |
| 图像分块（w/o Patch） | 10%–52% ↓（高分辨率影响更大） |
| 负载均衡（w/o Load Bal） | 1%–14% ↓ |

> 即使去掉任一组件，Gaian 依然显著优于 baseline，说明其核心框架有效。

---

## 4. 关键结论和发现

### 主要发现
1. **PBDR 存在强空间局部性**：相机视角倾向于观察局部区域内的点，这一特性可以被显式建模并通过图划分加以利用。
2. **通信是主要瓶颈**：现有系统因随机分布导致高达 85% 的训练时间用于通信；Gaian 通过局部性感知分配将通信减少最多 **91%**。
3. **通用性与性能可兼得**：通过抽象 API 分离算法逻辑与分布式执行，Gaian 不仅支持多种 PBDR 算法，还能实现比专用系统更高的性能。
4. **超大规模训练成为可能**：Gaian 成功训练含 **5亿点** 的模型，达到当前最优重建质量（PSNR 26.75），推动了 large-scale 3D reconstruction 的边界。

### 方法的局限性
- **离线图划分依赖完整数据集**：METIS 划分需遍历全部图像构建二分图，在数据流或增量学习场景中不适用。
- **对拓扑复杂的街景效果略弱**：Street Datasets 因视野跨越远近物体，局部性较差，Gaian 的增益相对较小（平均 1.80× vs Aerial 的 2.89×）。
- **实现复杂度较高**：相比简单随机分布，Gaian 需要额外开发调度器、图划分模块等，增加了系统复杂性。

### 未来工作方向
- 支持更多新型 PBDR 算法（如 Deformable Beta Splatting、Triangle Mesh Splatting）
- 探索在线自适应划分机制，应对动态变化的点云结构
- 结合硬件加速器（如 GScore [45]、GauSPU [84]）进一步释放性能潜力
- 应用于 real-world autonomous driving simulator 和 World Models 构建

---

> ✅ **一句话总结**：  
> Gaian 是首个通用、高效、可扩展的分布式 PBDR 训练系统，通过揭示并利用 PBDR 中的几何可见性局部性，实现了高达 **3.71×** 的训练加速和 **91%** 的通信削减，并成功训练了迄今最大的 **5亿点云模型**，为大规模 3D 重建提供了坚实基础设施。

</details>

---

### 16. [Resilient Packet Forwarding: A Reinforcement Learning Approach to Routing in Gaussian Interconnected Networks with Clustered Faults](https://arxiv.org/abs/2512.20394)

**Authors**: Mohammad Walid Charrwi, Zaid Hussain  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.20394v1  

#### Abstract
As Network-on-Chip (NoC) and Wireless Sensor Network architectures continue to scale, the topology of the underlying network becomes a critical factor in performance. Gaussian Interconnected Networks based on the arithmetic of Gaussian integers, offer attractive properties regarding diameter and sym...

---

### 17. [Learning to Design City-scale Transit Routes](https://arxiv.org/abs/2512.19767)

**Authors**: Bibek Poudel, Weizi Li  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.19767v1  

#### Abstract
Designing efficient transit route networks is an NP-hard problem with exponentially large solution spaces that traditionally relies on manual planning processes. We present an end-to-end reinforcement learning (RL) framework based on graph attention networks for sequential transit network constructi...

---

### 18. [Adaptive Financial Sentiment Analysis for NIFTY 50 via Instruction-Tuned LLMs , RAG and Reinforcement Learning Approaches](https://arxiv.org/abs/2512.20082)

**Authors**: Chaithra, Kamesh Kadimisetty, Biju R Mohan  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.20082v1  

#### Abstract
Financial sentiment analysis plays a crucial role in informing investment decisions, assessing market risk, and predicting stock price trends. Existing works in financial sentiment analysis have not considered the impact of stock prices or market feedback on sentiment analysis. In this paper, we pro...

---

### 19. [Offline Safe Policy Optimization From Heterogeneous Feedback](https://arxiv.org/abs/2512.20173)

**Authors**: Ze Gong, Pradeep Varakantham, Akshat Kumar  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.20173v1  

#### Abstract
Offline Preference-based Reinforcement Learning (PbRL) learns rewards and policies aligned with human preferences without the need for extensive reward engineering and direct interaction with human annotators. However, ensuring safety remains a critical challenge across many domains and tasks. Previ...

---

### 20. [An Adaptive Distributed Stencil Abstraction for GPUs](https://arxiv.org/abs/2512.19851)

**Authors**: Aditya Bhosale, Laxmikant Kale  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.19851v1  

#### Abstract
The scientific computing ecosystem in Python is largely confined to single-node parallelism, creating a gap between high-level prototyping in NumPy and high-performance execution on modern supercomputers. The increasing prevalence of hardware accelerators and the need for energy efficiency have made...

---

### 21. [Predictive-LoRA: A Proactive and Fragmentation-Aware Serverless Inference System for LLMs](https://arxiv.org/abs/2512.20210)

**Authors**: Yinan Ni, Xiao Yang, Yuqi Tang, Zhimin Qiu, Chen Wang, Tingzhou Yuan  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.20210v1  

#### Abstract
The serverless computing paradigm offers compelling advantages for deploying Large Language Model (LLM) inference services, including elastic scaling and pay-per-use billing. However, serving multiple fine-tuned LLMs via Low-Rank Adaptation (LoRA) in serverless environments faces critical challenges...

---

### 22. [Exploring Deep-to-Shallow Transformable Neural Networks for Intelligent Embedded Systems](https://arxiv.org/abs/2512.19731)

**Authors**: Xiangzhong Luo, Weichen Liu  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.19731v1  

#### Abstract
Thanks to the evolving network depth, convolutional neural networks (CNNs) have achieved remarkable success across various embedded scenarios, paving the way for ubiquitous embedded intelligence. Despite its promise, the evolving network depth comes at the cost of degraded hardware efficiency. In co...

---

### 23. [Field-Space Attention for Structure-Preserving Earth System Transformers](https://arxiv.org/abs/2512.20350)

**Authors**: Maximilian Witte, Johannes Meuer, \'Etienne Pl\'esiat, Christopher Kadow  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.20350v1  

#### Abstract
Accurate and physically consistent modeling of Earth system dynamics requires machine-learning architectures that operate directly on continuous geophysical fields and preserve their underlying geometric structure. Here we introduce Field-Space attention, a mechanism for Earth system Transformers th...

---

### 24. [Population Protocols Revisited: Parity and Beyond](https://arxiv.org/abs/2512.20163)

**Authors**: Leszek G\k{a}sieniec, Tytus Grodzicki, Tomasz Jurdzi\'nski, Jakub Kowalski, Grzegorz Stachowiak  
**Category**: cs.DC  
**Published**: 2025-12-24  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.20163v1  

#### Abstract
For nearly two decades, population protocols have been extensively studied, yielding efficient solutions for central problems in distributed computing, including leader election, and majority computation, a predicate type in Presburger Arithmetic closely tied to population protocols. Surprisingly, n...

---

### 25. [Mixture-of-Experts with Gradient Conflict-Driven Subspace Topology Pruning for Emergent Modularity](https://arxiv.org/abs/2512.20291)

**Authors**: Yuxing Gan, Ziyu Lei  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.20291v1  

#### Abstract
Mixture-of-Experts (MoE) architectures achieve parameter efficiency through conditional computation, yet contemporary designs suffer from two fundamental limitations: structural parameter isolation that causes catastrophic forgetting, and instruction-overfitting that degrades performance in instruct...

---

### 26. [Explainable time-series forecasting with sampling-free SHAP for Transformers](https://arxiv.org/abs/2512.20514)

**Authors**: Matthias Hertel, Sebastian P\"utz, Ralf Mikut, Veit Hagenmeyer, Benjamin Sch\"afer  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.20514v1  

#### Abstract
Time-series forecasts are essential for planning and decision-making in many domains. Explainability is key to building user trust and meeting transparency requirements. Shapley Additive Explanations (SHAP) is a popular explainable AI framework, but it lacks efficient implementations for time series...

---

### 27. [A Branch-and-Price Algorithm for Fast and Equitable Last-Mile Relief Aid Distribution](https://arxiv.org/abs/2512.19882)

**Authors**: Mahdi Mostajabdaveh, F. Sibel Salman, Walter J. Gutjahr  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.19882v1  

#### Abstract
The distribution of relief supplies to shelters is a critical aspect of post-disaster humanitarian logistics. In major disasters, prepositioned supplies often fall short of meeting all demands. We address the problem of planning vehicle routes from a distribution center to shelters while allocating ...

---

### 28. [Enhancing Zero-Shot Time Series Forecasting in Off-the-Shelf LLMs via Noise Injection](https://arxiv.org/abs/2512.20140)

**Authors**: Xingyou Yin, Ceyao Zhang, Min Hu, Kai Chen  
**Category**: cs.AI  
**Published**: 2025-12-24  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.20140v1  

#### Abstract
Large Language Models (LLMs) have demonstrated effectiveness as zero-shot time series (TS) forecasters. The key challenge lies in tokenizing TS data into textual representations that align with LLMs' pre-trained knowledge. While existing work often relies on fine-tuning specialized modules to bridge...

---

### 29. [Spatio-Temporal Graph Neural Networks for Dairy Farm Sustainability Forecasting and Counterfactual Policy Analysis](https://arxiv.org/abs/2512.19970)

**Authors**: Surya Jayakumar, Kieran Sullivan, John McLaughlin, Christine O'Meara, Indrakshi Dey  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.19970v1  

#### Abstract
This study introduces a novel data-driven framework and the first-ever county-scale application of Spatio-Temporal Graph Neural Networks (STGNN) to forecast composite sustainability indices from herd-level operational records. The methodology employs a novel, end-to-end pipeline utilizing a Variatio...

---

### 30. [LoFT-LLM: Low-Frequency Time-Series Forecasting with Large Language Models](https://arxiv.org/abs/2512.20002)

**Authors**: Jiacheng You, Jingcheng Yang, Yuhang Xie, Zhongxuan Wu, Xiucheng Li, Feng Li, Pengjie Wang, Jian Xu, Bo Zheng, Xinyang Chen  
**Category**: cs.LG  
**Published**: 2025-12-24  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.20002v1  

#### Abstract
Time-series forecasting in real-world applications such as finance and energy often faces challenges due to limited training data and complex, noisy temporal dynamics. Existing deep forecasting models typically supervise predictions using full-length temporal windows, which include substantial high-...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
