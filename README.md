# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-11 05:57:41 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Training-free Context-adaptive Attention for Efficient Long Context Modeling](https://arxiv.org/abs/2512.09238)

**Authors**: Zeng You, Yaofo Chen, Shuhai Zhang, Zhijie Qiu, Tingyu Wu, Yingjian Li, Yaowei Wang, Mingkui Tan  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.09238v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities across a wide range of natural language processing tasks. These capabilities stem primarily from the self-attention mechanism, which enables modeling of long-range dependencies. However, the quadratic complexity of self-attention...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Training-free Context-adaptive Attention for Efficient Long Context Modeling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）依赖 **self-attention** 机制建模长距离依赖，但其计算复杂度为 $O(L^2)$，在处理超长上下文（如 128K tokens）时面临严重的 **计算瓶颈** 和 **KV Cache 内存开销**。此外，输入中存在大量冗余 token，导致注意力资源浪费、效率下降。

现有方法存在以下局限：
- **静态稀疏注意力**（如 BigBird、Longformer）：使用固定模式，缺乏对输入内容的适应性。
- **仅加速 prefilling 阶段** 的动态方法（如 MInference、FlexPrefill）：无法缓解 decoding 阶段的 KV Cache 增长。
- **仅压缩 KV Cache** 的方法（如 SnapKV、CAKE）：不减少 prefilling 计算量。
- **统一框架**（如 DuoAttention、Lserve）：需要额外训练或系统级修改，部署成本高。

### 提出的新方法：TCA-Attention
本文提出 **Training-free Context-adaptive Attention (TCA-Attention)**，一种无需训练、可自适应输入内容的稀疏注意力机制，用于高效长上下文推理。

#### 核心设计思想
TCA-Attention 包含两个轻量级阶段：
1. **Offline Sparsity Configuration（离线配置阶段）**
   - 在小规模校准数据集上通过一次前向传播，估计每个 attention head 的冗余程度。
   - 使用 **log-Gaussian 采样策略** 生成候选稀疏配置，并选择能保留足够注意力质量（≥阈值 $T$）的最稀疏配置。
   - 结果是每个 head 获得一个个性化的 **sparsity budget**（即应保留多少 token）。

2. **Online Core Context Selection（在线核心上下文选择阶段）**
   - 推理时，根据离线确定的 sparsity budget 动态选择关键 token。
   - 引入轻量级 **redundancy metric** 来衡量 block 级别的信息密度：
     $$
     h_j = (1-\alpha)\sum_{i \in B_j} s_i + \alpha \cdot \text{HHI}(s_i)
     $$
     其中第一项表示总注意力得分，第二项是 Herfindahl-Hirschman Index 变体，反映注意力集中度。
   - 将序列划分为 block，按 $h_j$ 排序后分配 token 配额，优先保留信息密集 block 中的重要 token。
   - 同时保留最近 $w$ 个局部 token（local subset），以捕捉细粒度上下文。

最终注意力计算基于 **global subset + local subset** 的拼接，公式如下：
$$
\text{Att} = \text{Softmax}\left(\frac{Q[K_G; K_L]}{\sqrt{d_h}}\right)[V_G; V_L]
$$

### 相比现有方法的优势
| 特性 | TCA-Attention | MInference | FlexPrefill | XAttention | SnapKV | DuoAttention |
|------|---------------|------------|-------------|-------------|---------|----------------|
| **Training-free** | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| **Dynamic Adaptation** | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| **Accelerate Prefilling** | ✅ | ✅ | ✅ | ✅ | ❌ | ⚠️（有限） |
| **Accelerate Decoding** | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ |
| **Reduce KV Cache** | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ |

> ✅ 表示支持，❌ 不支持，⚠️ 表示部分支持

**优势总结**：
- **真正统一加速**：同时优化 prefilling 和 decoding 阶段。
- **完全免训练**：无需参数更新、架构修改或复杂调优，实现 **plug-and-play** 部署。
- **头感知 + 上下文自适应**：每个 attention head 有独立稀疏策略，且 token 选择随输入动态变化。
- **理论保障**：证明近似误差有界，且由保留的注意力质量控制。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 任务描述 |
|--------|------|----------|
| **LongBench-E** | 多任务长文本理解 | 包含单文档问答、多文档问答、摘要、代码生成等 14 个任务 |
| **RULER** | 合成基准测试 | 基于 “needle-in-a-haystack” 范式，评估模型从极长上下文中提取关键信息的能力 |
| **MMLU** | 知识理解 | 多学科知识问答 |
| **GSM8K** | 数学推理 | 小学数学应用题 |
| **HumanEval** | 代码生成 | 函数级代码补全 |
| **OlympiadBench** | 科学推理 | 奥赛级别数理难题 |
| **MT-Bench-101** | 多轮对话 | 评估多轮交互中的连贯性、推理能力等 |

### 实验设置和评估指标
- **模型**：
  - `LLaMA-3.1-8B-Instruct`（128K context）
  - `Qwen2.5-7B-Instruct`（128K context）
- **硬件**：NVIDIA A800 GPU（80GB VRAM）
- **评估指标**：
  - **任务性能**：各数据集上的准确率、F1、平均分等。
  - **效率指标**：
    - Attention computation latency（prefilling 时间）
    - Inter-token latency (ITL)（decoding 速度）
    - KV Cache 内存占用
  - 所有基线均基于 **FlashAttention-2** 实现，确保公平比较。

### 基线方法对比
- **MInference [20]**：训练免费，动态稀疏，仅加速 prefilling。
- **FlexPrefill [21]**：训练免费，context-aware，仅加速 prefilling。
- **XAttention [22]**：块稀疏 + 反对角评分，训练免费，仅加速 prefilling。
- **SnapKV [23]** / **CAKE [24]**：KV Cache 压缩方法，仅优化 decoding。
- **DuoAttention [25]**：统一框架，需训练，支持双阶段加速。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（128K context）

#### ✅ 在 RULER 上的表现（Table II）
| 方法 | 平均得分 | 推理延迟 (ms) | 加速比 |
|------|--------|--------------|-------|
| Full Attention | 87.52 (LLaMA), 82.63 (Qwen) | 1282.05 / 1101.01 | 1× |
| **TCA-Attention (Ours)** | **88.72 / 82.49** | **453.22 / 396.72** | **~2.8×** |

> TCA-Attention 在显著加速的同时保持甚至略微提升性能。

#### ✅ 在 LongBench-E 上的表现（Table I）
| 方法 | 平均得分 | 推理延迟 (64K) |
|------|--------|--------------|
| Full Attention | 53.19 / 51.16 | 316.14 / 268.55 ms |
| **TCA-Attention (Ours)** | **53.51 / 51.51** | **120.96 / 105.93 ms** |

> 实现 **2.5–2.6× 加速**，性能持平或略优。

#### ✅ KV Cache 压缩效果（Figure 3）
- 在 128K context 下，KV Cache 内存使用从 **8.00 GB** 降至 **3.12 GB**，**减少 61%**。
- 显著降低显存压力，使长上下文推理可在消费级 GPU 上运行。

#### ✅ 多轮对话表现（MT-Bench-101）
| 方法 | 平均分 |
|------|--------|
| Full Attention | 8.90 |
| FlexPrefill | 8.90 |
| **TCA-Attention** | **8.97** |

> 在复杂对话场景中仍能更好保持上下文连贯性和推理能力。

#### ✅ 综合加速能力（Figure 3）
- **Prefilling latency**: 2.8× speedup
- **Decoding ITL**: 2.1× faster
- **端到端推理效率全面提升**

### 消融实验结果

#### 🔹 Block Size $b$（Figure 4a）
- 最佳 block size 为 **128**，过小无法有效识别冗余，过大则可能丢失关键信息。

#### 🔹 Local Window Size $w$（Figure 4b）
- 性能随 $w$ 增大而提升，在 $w=4096$ 时达到峰值（51.51）。
- 即使大幅增加窗口，延迟仅轻微上升（38.8 → 41.5ms），说明局部保留代价低。

#### 🔹 Threshold $T$（Figure 4c & 5）
- $T=0.9$ 时性能最佳，表明保留更多上下文有助于全局推理任务（如 Multi-Document QA、Code）。
- 对局部任务（如 Single-Document QA）容忍更高压缩率（$T=0.3$ 时性能下降 <1pt）。
- 提供实用指导：**保守阈值用于全局任务，激进压缩用于局部任务**。

#### 🔹 Balance Parameter $\alpha$（Table VIII）
| $\alpha$ | 0.1 | 0.3 | **0.5** | 0.7 | 0.9 |
|---------|-----|-----|--------|-----|-----|
| Average Score | 51.14 | 51.25 | **51.51** | 51.24 | 51.13 |

> 方法对 $\alpha$ 高度鲁棒，最优值出现在中间（$\alpha=0.5$），适合默认使用。

#### 🔹 Calibration Dataset（Table IX）
| 数据集 | 领域 | 平均分 |
|--------|------|--------|
| SlimPajama | 通用网页 | 51.54 |
| GovReport | 政府文档 | 51.56 |
| McEval | 编程代码 | 51.55 |

> 不同领域校准数据下性能稳定，且**单条样本即可完成有效校准**，极具实用性。

---

## 4. 关键结论和发现

### 主要发现
1. **TCA-Attention 是首个真正实现“训练免费 + 输入自适应 + 统一加速 prefilling/decoding”的稀疏注意力机制**。
2. 通过 **head-specific sparsity configuration + online redundancy-aware token selection**，实现了细粒度、高效的上下文压缩。
3. 在 128K context 下实现 **2.8× 加速** 和 **61% KV Cache 压缩**，同时性能**持平甚至优于 full attention**。
4. 方法具有强鲁棒性，对超参数和校准数据不敏感，适合实际部署。

### 方法的局限性
- 当前方法基于 block-level 划分，可能忽略跨 block 的重要 token。
- 对 extremely long context（>1M tokens）的扩展性尚未验证。
- 虽然免训练，但仍需一次离线校准过程（尽管成本极低）。

### 未来工作方向
- 将 TCA-Attention 应用于 vision-language models 或 multimodal contexts。
- 探索更精细的 token selection 策略（如 hierarchical selection）。
- 结合其他高效技术（如 MoE、quantization）构建全栈优化推理系统。
- 研究如何将该机制反向用于模型解释或注意力可视化。

---

> **总结**：TCA-Attention 提出了一种简洁、高效、理论可靠且易于部署的长上下文优化方案，为 LLMs 在真实场景中的大规模应用提供了强有力的支撑。其“免训练 + 自适应 + 统一加速”的设计理念有望成为未来高效 attention 设计的标准范式之一。

</details>

---

### 2. [HPM-KD: Hierarchical Progressive Multi-Teacher Framework for Knowledge Distillation and Efficient Model Compression](https://arxiv.org/abs/2512.09886)

**Authors**: Gustavo Coelho Haase, Paulo Henrique Dourado da Silva  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09886v1  

#### Abstract
Knowledge Distillation (KD) has emerged as a promising technique for model compression but faces critical limitations: (1) sensitivity to hyperparameters requiring extensive manual tuning, (2) capacity gap when distilling from very large teachers to small students, (3) suboptimal coordination in mul...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# HPM-KD: Hierarchical Progressive Multi-Teacher Framework for Knowledge Distillation and Efficient Model Compression  
**论文核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对传统 **Knowledge Distillation (KD)** 方法中存在的四大关键挑战提出解决方案：
1. **超参数敏感性**：传统KD依赖大量手动调参（如温度 $T$、损失权重 $\alpha$），缺乏自适应能力。
2. **容量差距（Capacity Gap）**：当教师模型远大于学生模型时，单步蒸馏难以有效传递知识。
3. **多教师协调不佳**：现有多教师蒸馏方法采用固定或均匀加权策略，无法动态适应不同样本下各教师的专业优势。
4. **资源利用效率低**：重复实验导致计算冗余，缺乏跨实验的知识复用机制。

---

### 提出的新方法：HPM-KD 框架
作者提出了 **HPM-KD**（Hierarchical Progressive Multi-Teacher Knowledge Distillation），一个集成六项协同组件的综合性框架：

| 组件 | 功能 |
|------|------|
| **Adaptive Configuration Manager (ACM)** | 基于 meta-learning 自动预测最优超参数配置，消除人工调参 |
| **Progressive Distillation Chain (PDC)** | 构建自动确定长度的中间模型链，逐步缩小师生容量差距 |
| **Attention-Weighted Multi-Teacher Ensemble (AWMT)** | 引入注意力机制为每个样本动态分配教师权重 |
| **Meta-Learned Temperature Scheduler (MTS)** | 动态调整训练过程中的温度 $T$，提升软标签校准效果 |
| **Parallel Processing Pipeline (PPP)** | 并行化多教师与渐进阶段任务，显著缩短训练时间 |
| **Shared Optimization Memory (SOM)** | 缓存历史实验结果，支持配置重用与 warm-start |

---

### 相比现有方法的优势
- ✅ **无需手动调参**：通过 meta-learning 实现全自动配置选择。
- ✅ **更强压缩能力**：在高压缩比场景（>10×）表现更优。
- ✅ **更高的灵活性与鲁棒性**：多教师动态融合增强对噪声和类别不平衡的容忍度。
- ✅ **高效资源利用**：并行处理 + 缓存机制减少 30–40% 训练时间。
- ✅ **模块化设计**：可灵活扩展至其他任务或架构。

---

## 2. 核心实验方法和设置

### 数据集
实验覆盖图像分类与表格数据两类任务：

#### 图像分类
- **CIFAR-10**：60K 图像，32×32，10 类
- **CIFAR-100**：60K 图像，32×32，100 类

#### 表格数据（来自 UCI ML Repository）
- **Adult**：收入预测（48,842 条记录）
- **Credit**：信用评估（1,000 条）
- **Wine Quality**：葡萄酒质量评分（6,497 条）

---

### 模型架构
| 任务 | 教师模型 | 学生模型 | 压缩比（CR） |
|------|----------|----------|-------------|
| CIFAR-10/100 | ResNet-56 (~0.85M 参数) | ResNet-20 (~0.27M 参数) | 3.1× |
| 表格数据 | MLP [256,128,64] (~0.5M) | MLP [64,32] (~0.05M) | 10× |

---

### 评估指标
| 指标 | 定义 |
|------|------|
| **Student Accuracy (Acc_s)** | 学生模型最终准确率 |
| **Retention Rate (%)** | $\frac{Acc_s}{Acc_{\text{teacher}}} \times 100\%$ |
| **Compression Ratio (CR)** | 参数量比 $\frac{|\theta_t|}{|\theta_s|}$ |
| **Training Time** | 总墙钟时间（wall-clock time） |
| **Efficiency (acc/min)** | 准确率 / 训练分钟数 |
| **Silhouette Score** | 表征分离性衡量 |

---

### 基线方法对比
比较了以下五种主流方法：
1. **Direct Training**：无蒸馏的学生直接训练
2. **Traditional KD** [Hinton et al., 2015]
3. **FitNets** [Romero et al., 2015]
4. **Attention Transfer (AT)** [Zagoruyko & Komodakis, 2017]
5. **TAKD** [Mirzadeh et al., 2020]：引入 Teaching Assistant 的渐进蒸馏方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（CIFAR-10 结果汇总）

| 方法 | Acc (%) | Retention (%) | CR | Training Time (s) |
|------|---------|---------------|----|------------------|
| Teacher (ResNet-56) | 79.28 | 100.0 | 1.0× | 595.2 |
| **HPM-KD (Ours)** | **67.74±0.50** | **85.4%** | 3.1× | 603.1 |
| Direct Training | 68.10±0.48 | 85.9% | 3.1× | 593.7 |
| Traditional KD | 67.12±0.82 | 84.7% | 3.1× | 578.0 |
| FitNets | 62.66±0.76 | 79.0% | 3.1× | 592.4 |
| TAKD | 67.44±0.40 | 85.1% | 3.1× | 655.5 |

> 💡 在 moderate compression ratio（3.1×）下，**Direct Training 反而略优于所有蒸馏方法**，说明蒸馏并非总是必要。

---

### 更高压缩比下的表现（表格数据）
- 实现 **10×–15× 压缩**，同时保持 **≥85% 的准确率保留率**
- 显著优于 Traditional KD、FitNets 和 AT
- 在 tabular 数据上验证了通用性和有效性

---

### 与基线方法对比结果
- **相比 Traditional KD**：HPM-KD 提升 +0.62 pp（CIFAR-10）
- **相比 FitNets**：提升 +5.08 pp
- **相比 TAKD**：精度相当（67.74 vs 67.44），但训练时间更稳定（方差更低），且无需手动设计 Teaching Assistant
- **效率方面**：通过 PPP 实现 **30–40% 时间节省**，4 worker 下达 2.78× 加速

---

### 消融实验结果（Ablation Study on CIFAR-100）

| 配置 | Acc (%) | Δ (pp) | Retention (%) |
|------|--------|--------|--------------|
| Full HPM-KD | 36.28±0.52 | 0.00 | 100.0 |
| w/o MultiTeach | 35.30±0.85 | -0.98 | 97.3 |
| w/o MetaTemp | 35.63±0.41 | -0.65 | 98.2 |
| w/o Memory / Parallel | 35.67±0.51 | -0.61 | 98.3 |
| w/o ProgChain | 36.06±0.31 | -0.22 | 99.4 |
| w/o AdaptConf | 36.18±0.38 | -0.10 | 99.7 |

> 🔍 **Multi-Teacher Ensemble 是最关键组件**（影响最大），其次是 Meta-Temperature Scheduler 和并行/缓存系统。

---

## 4. 关键结论和发现

### 主要发现
1. **HPM-KD 成功整合六大组件，在多个维度超越现有方法**：
   - 实现自动化、高效、鲁棒的知识蒸馏流程
   - 在高压缩比（>10×）和复杂场景下更具优势

2. **蒸馏不一定优于直接训练**：
   - 当压缩比适中（如 3.1×）、学生模型具备足够容量时，**Direct Training 可能更简单有效**
   - 蒸馏的价值体现在：大容量差距、多教师可用、需抗噪/抗不平衡等场景

3. **多教师注意力机制至关重要**：
   - 不同教师在不同类别上表现出专长
   - 动态加权显著提升泛化能力和稳定性

4. **对噪声和类别不平衡具有强鲁棒性**：
   - 即使在 **30% 标签噪声** 或 **100:1 类别不平衡** 下，性能下降有限（<1pp）
   - 推测原因：教师的 soft targets 具有正则化作用，可过滤噪声

5. **并行与缓存带来显著效率增益**：
   - 并行化实现近线性加速（理论可达 K×）
   - 缓存命中率达 30–40%，避免重复计算

---

### 方法的局限性
| 局限 | 描述 |
|------|------|
| **验证范围有限** | 主要在 CIFAR 和小型表格数据上测试，尚未在 ImageNet、NLP 或医疗影像等大规模任务验证 |
| **压缩比中等** | 当前实验集中在 3.1×–10×，未充分探索极端压缩（如 50–100×）场景 |
| **理论分析缺失** | 缺乏对收敛性、泛化界或 PAC 框架的形式化证明 |
| **冷启动问题** | ACM 需要约 5 次历史运行才能准确预测配置，在全新领域可能失效 |
| **计算开销增加** | 相比 Traditional KD，因多教师和渐进链导致总计算量上升 30–40% |

---

### 未来工作方向
1. **扩展到 Large Language Models (LLMs)**：用于压缩 GPT 类千亿参数模型
2. **Cross-Modal Distillation**：从 CLIP、Flamingo 等多模态模型蒸馏到单模态专用模型
3. **Theoretical Analysis**：推导 progressive distillation 的泛化边界
4. **Distillation-Aware Architecture Search (DAAS)**：联合优化网络结构与蒸馏策略
5. **Federated Knowledge Distillation**：支持分布式设备上的隐私保护蒸馏
6. **Continuous Distillation**：支持教师持续更新下的增量学生学习（lifelong learning）

---

## 总结
HPM-KD 是一个面向生产部署的、高度自动化且高效的多教师知识蒸馏框架。它通过 **meta-learning、progressive chain、attention-weighted ensemble、dynamic temperature scheduling、parallelization 与 caching** 六大组件协同工作，解决了传统 KD 中的手动调参、容量断层、资源浪费等问题。

尽管在轻度压缩场景下不如 Direct Training 简洁高效，但在 **高压缩比、多教师、噪声环境或需要快速迭代的 MLOps 场景中展现出巨大潜力**。其开源实现（DeepBridge 库）降低了工业落地门槛，推动了 AI democratization 进程。

</details>

---

### 3. [SCOPE: Language Models as One-Time Teacher for Hierarchical Planning in Text Environments](https://arxiv.org/abs/2512.09897)

**Authors**: Haoye Lu, Pavan Seshadri, Kaheer Suleman  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09897v1  

#### Abstract
Long-term planning in complex, text-based environments presents significant challenges due to open-ended action spaces, ambiguous observations, and sparse feedback. Recent research suggests that large language models (LLMs) encode rich semantic knowledge about the world, which can be valuable for gu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SCOPE: Language Models as One-Time Teacher for Hierarchical Planning in Text Environments

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在复杂的文本环境（text-based environments）中进行长期规划（long-term planning）面临三大挑战：
- **开放的动作空间**（open-ended action spaces）
- **模糊的观测状态**（ambiguous observations）
- **稀疏的奖励反馈**（sparse feedback）

现有基于 **Large Language Models (LLMs)** 的分层规划方法虽然能利用 LLM 的语义知识指导智能体进行高层推理，但存在两个显著缺陷：
1. **计算成本高**：需要在训练和推理阶段反复调用 LLM，导致延迟高、部署困难。
2. **缺乏适应性**：LLM 通常作为冻结模型（frozen）使用，无法针对目标任务进行参数更新。

---

### 🚀 提出的新方法：SCOPE
作者提出 **SCOPE**（Subgoal-COnditioned Pretraining for Efficient planning），一种高效的**一次性教师框架**（one-time teacher framework），其核心思想是：

- **仅在初始化阶段使用一次 LLM**，从人类提供的次优示范轨迹（suboptimal demonstration trajectories）中提取子目标（subgoals）。
- 利用这些 LLM 生成的子目标对一个轻量级学生模型（lightweight student model）进行预训练。
- 后续通过 **Reinforcement Learning (RL)** 在世界模型（world model）上进一步微调，实现独立运行。

#### 创新点：
1. **去依赖化 LLM 推理**：不再在训练或推理过程中重复查询 LLM，极大提升效率。
2. **高效的知识蒸馏方式**：不同于传统“在线蒸馏”（online distillation），SCOPE 采用“离线蒸馏”——直接从示范轨迹中解析出子目标序列。
3. **双层级架构设计**：
   - **Manager Agent**：负责提出高层子目标（high-level planning）。
   - **Employee Agent**：负责执行具体动作以完成子目标（low-level execution）。
4. **可扩展性强**：适用于纯文本环境，无需视觉或物理交互建模。

---

### 🔍 相比现有方法的优势
| 维度 | ADaPT (Prasad et al., 2024) | SCOPE (本文) |
|------|----------------------------|-------------|
| 是否依赖 LLM 推理 | 是（每次决策都调用 GPT-3.5） | 否（仅初始化时用一次） |
| 参数量 | 175B（GPT-3.5） | 11.04M（小型神经网络） |
| 推理时间 | 164.4 秒 | **3.0 秒** |
| 成功率（success rate） | 0.52 | **0.56** |
| 部署成本 | 高（API 调用 + 网络延迟） | 极低（本地 GPU 可运行） |

> ✅ **优势总结**：**更高性能 + 更低成本 + 更快推理 + 更易部署**

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用 **TextCraft** 环境（Prasad et al., 2024 提出），这是一个受 Minecraft 启发的简化版纯文本合成任务环境。
- 智能体需根据目标物品（如“lime stained glass pane”）和可用配方指令，通过 `craft` 和 `get` 命令逐步收集基础材料、制造中间品，最终合成目标物品。
- 观测为文本描述的状态（inventory 字典），动作为自然语言命令。

#### 数据生成：
- 人工模拟 **50万条次优轨迹**（suboptimal trajectories），其中包含约 10% 的随机噪声动作，以模仿人类探索行为。
- 其中 1K 条用于验证和测试。

---

### ⚙️ 实验设置与评估指标

#### 主要组件：
- **LLM 子目标生成器**：使用 LLM 分析示范轨迹，输出 `fac(T)` 函数自动分解子目标序列。
- **Manager Agent**：学习在当前状态下选择下一个子目标。
- **Employee Agent**：学习如何完成给定子目标。
- **World Model**：基于示范数据训练的环境模拟器，用于 RL 微调阶段避免真实环境交互开销。

#### 训练流程：
1. **预训练阶段**：
   - Manager：学习重现 LLM 生成的子目标序列。
   - Employee：模仿示范轨迹中的动作序列。
2. **RL 微调阶段**：
   - 使用 **Cross-Entropy Method (CEM)** 算法，在世界模型中采样成功轨迹并加权更新策略。
   - 目标：最大化最终目标达成率（goal achievement rate）。

#### 评估指标：
- **Success Rate**：成功完成目标任务的比例。
- **Inference Time**：单次游戏平均耗时（秒）。
- **#Parameters**：模型总参数量。
- **消融实验**：验证不同子目标质量的影响。

---

### 🆚 基线方法对比
- **ADaPT (Prasad et al., 2024)**：基于 LLM 的分层规划器，使用 GPT-3.5 动态生成子目标。
- 多种 LLM 后端版本对比（见 Table 4）：
  - GPT-3.5、GPT-4o、GPT-4o mini
  - Mistral Small 3、Claude-3 Haiku、DeepSeek-R1-Distill-Qwen-32B

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 3）

| 方法 | Success Rate | #Parameters |
|------|--------------|------------|
| ADaPT (GPT-3.5) | 0.52 | 175B |
| **SCOPE (ours)** | **0.56** | **11.04M** |
| SCOPE (hand-engineered subgoal) | 0.58 | 11.04M |
| SCOPE (without manager RL-finetuning) | 0.24 | 11.04M |

> ✅ **核心结果**：SCOPE 在仅使用 **11M 参数**的情况下，**超越了 175B 参数的 ADaPT**，成功率提升至 **0.56**。

---

### ⏱️ 效率对比
- **SCOPE**：平均 **3.0 秒** 完成游戏（NVIDIA A10 GPU）
- **ADaPT**：平均 **164.4 秒**（GPT-3.5 API，理想网络条件下）

> 💡 **速度提升超过 50 倍**，且不依赖外部 API。

---

### 🔍 消融实验结果

#### （1）子目标来源的影响（Table 3）
- 使用手工设计的可解释子目标（hand-engineered subgoals）可将成功率进一步提升到 **0.58**。
- 表明 LLM 生成的子目标虽不够透明，但仍提供了有效引导。

#### （2）是否进行 Manager RL 微调
- 若移除 Manager 的 RL 微调阶段，成功率骤降至 **0.24**。
- 说明 **RL 微调对于补偿 Employee 不完美执行至关重要**。

#### （3）子目标模糊性影响（Figure 8a）
- 移除数量信息（`SCOPE (no quantity)`）后性能下降。
- 非分层模型（Non-Hierarchical）表现最差，表明**子目标结构本身具有价值**。

#### （4）子目标与环境错配实验（Figure 8b）
- 当随机重命名子目标中物品名称比例增加（p=0→1.0）：
  - p=0.25 时，成功率已降至 0.09
  - p=1.0 时，几乎完全失败（0.02）
- 结论：**子目标必须与真实环境因果对齐**，否则会误导智能体。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **即使次优的 LLM 生成子目标也能提供强大先验知识**：
   - 尽管 LLM 未与环境交互，其生成的子目标仍可作为有效的分层规划起点。
2. **一次性的 LLM 教学足以支持高性能智能体训练**：
   - 不需要持续调用 LLM，即可获得优于实时 LLM 推理的效果。
3. **分层结构 + RL 微调是关键**：
   - Manager 能通过 RL 学习动态调整计划，弥补 Employee 执行误差。
4. **效率与性能兼得**：
   - SCOPE 在更小模型、更低延迟下实现了更高成功率，展示了极高的参数效率（parameter efficiency）。

---

### ⚠️ 局限性
1. **子目标不可解释性**：
   - LLM 生成的子目标缺乏语义清晰性，难以调试和分析。
2. **依赖高质量示范轨迹**：
   - 若示范轨迹过于混乱或偏离主线，LLM 提取的子目标可能严重失真。
3. **当前仅验证于 TextCraft**：
   - 虽然该环境具备代表性，但在更复杂、动态变化的任务中泛化能力有待验证。
4. **世界模型精度限制**：
   - 所有 RL 微调均在世界模型中进行，若世界模型建模不准，会影响最终性能。

---

### 🔮 未来工作方向
1. **改进子目标生成的可解释性**：
   - 引入约束机制或提示工程，使 LLM 输出更具逻辑性和可读性的子目标。
2. **结合在线 LLM 查询进行自适应修正**：
   - 在关键节点引入少量 LLM 查询，平衡效率与灵活性。
3. **扩展到多模态环境**：
   - 将 SCOPE 思路应用于 Vision-Language-Action 场景（如 ALFWorld、Minecraft GUI）。
4. **探索更强大的轻量级学生模型架构**：
   - 如使用 MoE 或 TinyLLM 替代当前 LSTM 架构，进一步提升性能边界。

---

## ✅ 总结
**SCOPE** 提出了一种新颖且高效的范式：将 LLM 作为“一次性教师”，仅在训练初期提供子目标指导，随后由轻量级学生模型自主学习并优化。实验证明，这种方法不仅在性能上超越了依赖实时 LLM 推理的先进系统（如 ADaPT），还在推理速度和部署成本上取得巨大优势。它为构建**高效、可扩展、低延迟的文本环境智能体**提供了重要思路，推动了 LLM 与强化学习融合研究向实用化迈进。

</details>

---

### 4. [Scalable Construction of Spiking Neural Networks using up to thousands of GPUs](https://arxiv.org/abs/2512.09502)

**Authors**: Bruno Golosio, Gianmarco Tiddia, Jos\'e Villamar, Luca Pontisso, Luca Sergi, Francesco Simula, Pooja Babu, Elena Pastorelli, Abigail Morrison, Markus Diesmann, Alessandro Lonardo, Pier Stanislao Paolucci, Johanna Senk  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09502v1  

#### Abstract
Diverse scientific and engineering research areas deal with discrete, time-stamped changes in large systems of interacting delay differential equations. Simulating such complex systems at scale on high-performance computing clusters demands efficient management of communication and memory. Inspired ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本论文旨在解决**大规模脉冲神经网络（Spiking Neural Networks, SNNs）在多GPU集群和下一代Exascale超级计算机上高效构建与模拟的挑战**。具体而言，传统方法在网络构建阶段存在以下瓶颈：
- 需要跨MPI进程通信来生成远程连接，导致高延迟；
- 数据结构管理效率低，影响内存占用和通信开销；
- 缺乏对GPU内存使用的灵活优化机制。

这些问题限制了SNN仿真在脑科学等需要亿级神经元、万亿级突触规模场景下的可扩展性。

---

### 提出的新方法与创新思路
作者提出了一种**基于MPI的新型网络构建算法**，其核心创新包括：

#### （1）**全GPU端（onboard）网络构建**
- 所有神经元和连接（包括本地和远程）均直接在GPU内存中完成创建，无需从CPU传输数据。
- 每个MPI进程独立构建局部连接并准备通信所需的数据结构，避免了构建阶段的MPI通信开销。

#### （2）**代理神经元（proxy neuron）与通信映射结构**
- 引入“图像神经元”（image neuron / proxy）表示远程源神经元在其目标进程中的虚拟副本。
- 构建高效的映射表（如 `R`, `L`, `S` 数组），将远程源神经元索引与其在目标进程中的本地图像索引关联，支持快速路由。

#### （3）**双模式MPI通信支持**
- 支持两种通信范式：
  - **Point-to-point communication**：适用于非均匀连接、异构活动模式（如Multi-Area Model）；
  - **Collective communication**（如MPI_Allgather）：适用于负载均衡的大规模随机网络。
- 用户可根据模型特性选择最优通信策略。

#### （4）**四级优化层级（Optimization Levels 0–3）**
通过控制通信相关数据结构的存储位置（CPU vs GPU），实现**GPU内存使用与运行时间之间的权衡**：
| Level | 存储于GPU的内容 |
|-------|----------------|
| 0     | 仅连接参数 |
| 1     | 图像映射 + 出边起始索引 + 数量 → 存于CPU |
| 2     | 图像映射 + 起始索引 → GPU；数量动态计算 |
| 3     | 全部存于GPU（最快） |

---

### 相比现有方法的优势
| 方法 | 局限性 | 本文改进 |
|------|--------|----------|
| NEST CPU/GPU (offboard) | 网络构建在CPU进行，需大量CPU-GPU数据拷贝 | 完全on-GPU构建，减少IO开销 |
| GeNN | 单GPU或小规模，缺乏分布式支持 | 支持数千GPU集群 |
| CARLsim | 多GPU但局限于单机 | 支持跨节点大规模分布式 |
| Digital Brain | 不保存个体突触信息 | 保留完整突触级精度 |

> ✅ **优势总结**：首次实现了**突触级精度、全GPU内存构建、支持万级GPU扩展**的SNN仿真框架。

---

## 2. 核心实验方法和设置

### 使用的数据集与模型
#### （1）**Multi-Area Model (MAM)**  
- 模拟猕猴视觉皮层32个区域；
- 总计：4.13×10⁶ 神经元，2.42×10¹⁰ 突触；
- 生物学真实结构，具有分层组织和复杂长程连接；
- 运行于**metastable state**（类麻醉静息态）；
- 采用 **point-to-point MPI communication**。

#### （2）**Scalable Balanced Network**  
- 随机平衡网络（excitatory/inhibitory）；
- 固定输入度（Kin = 11,250），连接规则为 `random, fixed in-degree`；
- 规模随MPI进程数线性增长（weak scaling）；
- 使用 **collective MPI communication**（MPI_Allgather）；
- 每GPU节点规模：2.25×10⁵ 神经元，2.53×10⁹ 突触（scale=20）。

---

### 实验平台硬件配置
| 平台 | CPU | GPU | 数量/节点 | 内存 |
|------|-----|-----|-----------|------|
| **JUSUF** | AMD EPYC 7742 ×2 | NVIDIA V100 | 1 | 16 GB HBM2e |
| **Leonardo Booster** | Intel Xeon Platinum 8358 | NVIDIA A100 ×4 | 4 | 64 GB HBM2 |

> 注：Leonardo用于强扩展测试（area packing），JUSUF用于MAM基准对比。

---

### 评估指标
| 指标 | 定义 |
|------|------|
| **Network Construction Time** | 包括初始化、神经元/设备创建、连接生成、模拟准备等子任务耗时 |
| **State Propagation Time** | 动态演化阶段耗时，以 **Real-Time Factor (RTF)** 衡量：<br>$$ \text{RTF} = \frac{T_{\text{wall}}}{T_{\text{model}}} $$ |
| **Peak GPU Memory Usage** | 每GPU峰值内存消耗，决定可扩展上限 |
| **Earth Mover’s Distance (EMD)** | 验证新旧版本放电统计分布一致性 |

---

### 基线方法对比
- **Offboard vs Onboard**：比较原NEST GPU（CPU构建后传GPU）与本文提出的全GPU构建方法；
- **不同Optimization Levels**：分析内存与性能权衡；
- **Scaling Behavior**：从32到256节点（即128–1024 GPUs）进行弱扩展测试。

---

## 3. 主要实验结果和性能指标

### （1）Multi-Area Model 结果（vs Offboard）
| 指标 | Offboard | Onboard | 加速比 |
|------|---------|--------|--------|
| **总构建时间** | 686.0 ± 1.5 s | 55.5 ± 0.1 s | **~12.4×** |
| - 连接创建（local） | — | — | **20×** |
| - 连接创建（remote） | — | — | **9×** |
| - 神经元/设备创建 | ~1.9 s | ~0.005 s | **~350×** |
| - 模拟准备 | ~1.7 s | ~0.03 s | **~50×** |
| **状态传播 RTF** | 16.0 ± 3.0 | 15.0 ± 1.7 | 相当 |

> 🔍 图2显示：**网络构建成为主要瓶颈，而onboard方法几乎完全消除该瓶颈**。

---

### （2）Scalable Balanced Network 弱扩展结果
#### 网络规模（scale=20）
| 节点数 | GPU数 | 神经元总数 | 突触总数 |
|--------|-------|------------|-----------|
| 32 | 128 | 2.88×10⁷ | 3.24×10¹¹ |
| 256 | 1024 | 2.30×10⁸ | 2.59×10¹² |

#### 性能趋势（图3）
- **随着节点增加，state propagation time保持稳定（RTF ≈ 1.5–2.5）**，表明良好弱扩展性；
- **Optimization Level 3 最快**，相比Level 0提速约2倍；
- **关闭spike recording可进一步降低RTF约20%**。

#### 内存使用（图4）
- **Level 0 可扩展至4096节点（≈16,384 GPUs）不超限**；
- Level 3 在 ~1024 GPUs 达到A100内存极限（64GB）；
- 内存峰值由远程连接映射结构主导。

---

### （3）消融实验：Optimization Levels 影响
| Level | 构建速度 | 传播速度 | GPU内存 |
|-------|----------|----------|--------|
| 0 | 慢（需频繁CPU-GPU访问） | 慢 | 最低 |
| 1 | 中等 | 中等 | 低 |
| 2 | 快 | 快 | 中 |
| 3 | 最快（全GPU操作） | 最快 | 最高 |

> ⚖️ **结论**：Level 3适合追求极致性能，Level 0适合内存受限场景。

---

### （4）Area Packing 实验（附录B）
- 利用A100大内存，在Leonardo上实现多个MAM区域打包到同一GPU；
- 最少可用 **8 GPUs（2 nodes）运行完整MAM**；
- 尽管构建时间略有上升，但整体可行且节省资源。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **全GPU网络构建显著加速构建过程**：相比传统offboard方法，**构建时间缩短超过10倍**，尤其在连接生成和模拟准备阶段表现突出。
2. **支持大规模弱扩展**：在1024 GPUs上成功运行含 **2.3亿神经元、2.6万亿突触** 的平衡网络，RTF接近实时。
3. **灵活通信机制适应不同模型需求**：
   - Point-to-point 更适合生物真实、非均匀连接模型（如MAM）；
   - Collective 更适合高度互联、负载均衡的大规模随机网络。
4. **内存-性能权衡可控**：通过4级优化策略，可在实际部署中按需调节。
5. **具备Exascale潜力**：预计在JUPITER超算上可达 **2×10¹⁰ 神经元、10¹⁴ 突触** 规模，接近人类大脑突触数量级（~10¹⁵）。

---

### ❗ 局限性
1. **当前实现依赖用户手动负载均衡**，未自动考虑硬件拓扑；
2. **尚未启用GPUDirect RDMA**，仍有进一步降低通信延迟的空间；
3. **集体通信假设同构流量**，对极端异构模型可能不适用；
4. **验证集中在统计一致性**，未深入探讨功能层面的行为差异。

---

### 🚀 未来工作方向
1. **自动化分区与映射策略**：结合神经网络拓扑与HPC硬件拓扑进行智能分配；
2. **混合通信协议设计**：例如局部用point-to-point，全局用collective；
3. **集成NCCL或其他GPU专用通信库**提升带宽利用率；
4. **支持更多突触可塑性规则与动态连接更新**；
5. **向JUPITER等Exascale系统迁移与实测验证**。

---

## 总结
该论文提出了一套**面向Exascale的、可扩展的SNN构建与仿真框架**，通过**全GPU内存构建、代理神经元机制、双模式MPI通信与四级内存优化**，实现了前所未有的规模与效率。实验表明其在MAM和随机平衡网络上均取得显著性能提升，并展现出逼近人脑规模仿真的潜力，是迈向“数字大脑”的关键技术一步。

</details>

---

### 5. [Learning Unmasking Policies for Diffusion Language Models](https://arxiv.org/abs/2512.09106)

**Authors**: Metod Jazbec, Theo X. Olausson, Louis B\'ethune, Pierre Ablin, Michael Kirchhof, Joao Monterio, Victor Turrisi, Jason Ramapuram, Marco Cuturi  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09106v1  

#### Abstract
Diffusion (Large) Language Models (dLLMs) now match the downstream performance of their autoregressive counterparts on many tasks, while holding the promise of being more efficient during inference. One particularly successful variant is masked discrete diffusion, in which a buffer filled with speci...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Unmasking Policies for Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
扩散语言模型（dLLMs）在生成时通过逐步“去掩码”（unmasking）一个初始全为掩码 token 的序列来生成文本。传统的采样策略（如随机或基于置信度的启发式方法）虽然能提升效率，但存在以下问题：
- **手动调参困难**：例如阈值 `λ` 或块长度 `BL` 需要针对不同任务和模型进行调整；
- **泛化能力差**：在非半自回归（non-semi-AR）设置下性能显著下降；
- **难以平衡效率与质量**：固定规则无法动态适应不同输入。

本文提出：**将 unmasking 策略的学习形式化为一个强化学习（RL）问题**，以自动发现高效的采样策略。

---

### 提出的新方法与创新思路

#### （1）将采样过程建模为马尔可夫决策过程（MDP）
- **状态（State）**：当前部分去掩码的序列 `y_t` 和原始提示 `x`；
- **动作（Action）**：每个位置是否被去掩码（二值向量 `u ∈ {0,1}^L`）；
- **奖励（Reward）**：最终答案正确性 × 效率惩罚项（`r(y, y*) × (1 - α/(T−T_final))`），采用乘法形式避免错误样本因速度快而获得正优势；
- **环境**：固定的预训练 dLLM（如 LLaDA、Dream），不更新其参数。

#### （2）设计轻量级 Transformer 策略网络（Lightweight Policy）
- 输入：各位置 token 的预测置信度 `c_k = max_v p(v)`、掩码状态 `m_k`、时间步 `t`；
- 网络结构：单层 Transformer，总参数仅约 300K（<0.01% of LLaDA-8B），计算开销极低；
- 输出：每个位置的 unmasking 得分，通过 Bernoulli 分布采样决定是否去掩码。

#### （3）使用 Group Relative Policy Optimization（GRPO）进行训练
- 每组采样多个轨迹，计算组内相对优势（group-mean normalized advantage）；
- 使用重要性采样和 clip 机制稳定训练；
- 移除 KL 正则项（因从零开始训练）。

---

### 相比现有方法的优势

| 方面 | 启发式方法（如 Fast-dLLM） | 本文方法（RL Policy） |
|------|--------------------------|------------------------|
| **调参难度** | 需手动调节 `λ` 或 `K` | 自动学习策略，无需在线调参 |
| **灵活性** | 固定规则，难以适应复杂模式 | 可学习上下文相关的动态策略 |
| **非 semi-AR 性能** | 在 full diffusion 下性能骤降 | 显著优于启发式方法 |
| **效率-准确性权衡** | 通过 `λ` 平滑控制 | 虽不如启发式平滑，但可通过 `α` 和 `T_policy` 调节 |

---

## 2. 核心实验方法和设置

### 数据集
- **数学推理**：
  - **GSM8k**（小学数学题）
  - **MATH**（高中竞赛级数学题）
- **代码生成**：
  - **HumanEval**
  - **MBPP**
- **训练数据混合**：GSM8k + MATH 按比例混合，共 ~15,000 条样本；
- **迁移测试**：在 HumanEval / MBPP 上测试跨域迁移能力。

---

### 实验设置

| 组件 | 设置 |
|------|------|
| **基础模型** | LLaDA-8B-Instruct、Dream-7B-Instruct |
| **策略网络架构** | 单层 Transformer，隐藏维度 128，注意力头数 2 |
| **训练算法** | GRPO，batch size=16，learning rate=3e-5，cosine 调度器 |
| **训练目标** | 多任务混合数据上训练 1 个 epoch |
| **温度设置** | dLLM 温度 T=0（greedy decoding），策略温度 `T_policy` 在测试时可调 |
| **评估指标** |
| - **准确性**：任务特定正确率（如 pass@1） |
| - **效率**：平均 NFEs（Network Function Evaluations），即采样步数 |

---

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **Random** | 随机选择 K 个位置去掩码 |
| **High Confidence** | 选择置信度最高的 K 个位置（K ∈ {8,...,256}） |
| **Fast-dLLM** | 置信度超过阈值 `λ` 的位置全部去掩码（`λ ∈ [0.1,1.0]`） |
| **Ours (RL)** | 本文提出的 RL 学习策略 |
| **Ours (w/ ES)** | 引入 Expert Steering 的变体，引导探索 semi-AR 行为 |

---

## 3. 主要实验结果和性能指标

### （1）在 semi-AR 设置下（BL=32）匹配最先进启发式方法

- 在 GSM8k 和 MATH 上，RL 策略的 Pareto 前沿几乎完全贴合 Fast-dLLM；
- 特别是 `α=10` 的策略，在极低 NFE（~10）时表现最优，说明其擅长极致加速；
- 但 `α` 参数对行为的控制不如 `λ` 平滑（见消融实验）。

> ✅ 结论：**在 semi-AR 场景下，RL 策略能达到与最佳启发式相当的性能。**

---

### （2）在 full diffusion 设置下（BL=256）显著超越启发式方法

| 方法 | GSM8k @ ~12 NFEs | MATH @ ~12 NFEs |
|------|------------------|------------------|
| Random / High Confidence | ≤30% | ≤15% |
| Fast-dLLM | ≤30% | ≤15% |
| **Ours (RL)** | **~50%** | **~25%** |

- 所有方法性能均下降，但 RL 策略下降幅度最小；
- 表明 RL 能有效应对长序列、无块约束的挑战；
- 加入 **Expert Steering (ES)** 后，在中高 NFE 区间进一步逼近 semi-AR 最优性能（如 GSM8k 达到 ~80%）。

> ✅ 结论：**在 full diffusion 场景下，RL 策略明显优于启发式方法，尤其在低 NFE 极限效率场景。**

---

### （3）消融实验结果

#### ▶ 乘法 vs 加法奖励函数
| 奖励形式 | 训练稳定性 | 是否出现 reward hacking |
|---------|------------|------------------------|
| 加法：`r − α×steps` | 差 | 是（模型学会一次性去掩码所有 token） |
| **乘法：`r × (1−α/steps)`** | **好** | **否** |

> ✔️ 乘法奖励更合理：只有正确答案才能获得正优势，防止“快但错”的策略占优。

#### ▶ 不同策略输出分布
| 方法 | 控制性 | 实现复杂度 | 性能 |
|------|--------|------------|-------|
| **Bernoulli**（默认） | 一般 | 简单，高效 | ≈ Fast-dLLM |
| **Dynamic Plackett-Luce (DPLS)** | 更好（`α` 影响更平滑） | 复杂，需处理 STOP token | ≈ Bernoulli |

> ✔️ DPLS 提供更好可控性，但收益有限；Bernoulli 更实用。

#### ▶ 输入特征设计
| 输入 | 性能 | 备注 |
|------|------|------|
| **Top-1 置信度 `c_k`** | **最佳** | 简洁有效 |
| Top-50 置信度 | 略差 | 未带来增益 |
| **Hidden States**（300M 参数） | 更差 | 过大且不稳定 |

> ✔️ **简单的置信度输入已足够**，语义信息反而干扰策略学习。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Unmasking 策略可以被有效学习**：通过 RL 可自动发现高质量的 adaptive sampling 策略；
2. ✅ **在 full diffusion 场景下优于启发式方法**：尤其在低 NFE 极限效率场景，RL 策略展现出更强潜力；
3. ✅ **策略具有良好的迁移能力**：
   - **跨模型**：在 LLaDA 上训练的策略可在 Dream 上取得接近原生训练的效果；
   - **跨长度**：在 L=256 训练的策略可直接用于 L=512，性能稳定；
4. ❌ **跨领域迁移受限**：在数学数据上训练的策略在代码任务（HumanEval/MBPP）上表现不佳，需领域适配。

---

### 方法的局限性
| 局限 | 描述 |
|------|------|
| **可控性不足** | `α` 参数无法像 `λ` 那样平滑调节准确率-效率权衡 |
| **训练不稳定性** | 尤其在 `α=10` 或引入 Expert Steering 时易崩溃 |
| **依赖高质量基础模型输出** | 若 dLLM 本身性能弱，RL 无法弥补 |
| **仍需为每个 `α` 单独训练策略** | 无法在一个模型中连续调节 trade-off |

---

### 未来工作方向
1. **改进策略可控性**：
   - 探索联合学习 `T_policy` 或使用条件生成方式统一多 `α` 策略；
   - 设计更鲁棒的 likelihood 结构（如 DPLS）。
2. **增强跨领域泛化**：
   - 在多领域混合数据上训练通用 unmasking 策略；
   - 引入轻量级适配模块（adapter）实现 zero-shot 迁移。
3. **扩展功能**：
   - 支持 remasking（回退修正）操作；
   - 动态调整生成长度（类似 Li et al., 2025a）；
   - 应用于 multimodal diffusion models。
4. **理论分析**：
   - 分析 RL 策略为何能在 full diffusion 下优于启发式；
   - 探索最优 unmasking 序列的结构性质。

---

> 🔚 **总结**：本文首次系统地将 RL 引入 dLLM 的采样策略学习，证明了 learned unmasking policy 在效率与质量之间可达到甚至超越人工设计启发式的性能，特别是在非 semi-AR 场景下展现巨大潜力，为未来高效、智能的文本生成提供了新范式。

</details>

---

### 6. [TinyD\'ej\`aVu: Smaller Memory Footprint & Faster Inference on Sensor Data Streams with Always-On Microcontrollers](https://arxiv.org/abs/2512.09786)

**Authors**: Zhaolan Huang, Emmanuel Baccelli  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09786v1  

#### Abstract
Always-on sensors are increasingly expected to embark a variety of tiny neural networks and to continuously perform inference on time-series of the data they sense. In order to fit lifetime and energy consumption requirements when operating on battery, such hardware uses microcontrollers (MCUs) with...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TinyDejaVu: Smaller Memory Footprint & Faster Inference on Sensor Data Streams with Always-On Microcontrollers

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在资源极度受限的 **Microcontroller Unit (MCU)** 上运行基于时间序列传感器数据流的神经网络推理时，面临两大挑战：
- **高 RAM 占用**：传统模型对每个滑动窗口输入都重新计算中间激活值，导致内存峰值极高。
- **冗余计算**：由于滑动窗口高度重叠（overlap），大量计算是重复执行的。

这些问题严重影响了电池供电设备的能效与实时性，限制了 TinyML 在长期、连续感知场景中的应用。

---

### 提出的新方法与创新思路
作者提出 **TinyDejaVu** —— 一种全新的嵌入式软件框架，通过将时间算子建模为 **State-Space Model (SSM)** 来优化推理过程。其核心思想包括：

#### ✅ 创新点 1：将 Temporal Operators 转换为 SSM
- 将卷积（Conv）、池化（Pooling）等具有因果结构的时间算子形式化为 SSM：
  $$
  h_t = A h_{t-1} + B x_t,\quad y_t = g(h_t)
  $$
- 这使得只需维护固定大小的隐藏状态缓冲区（而非完整输入序列），实现常量空间复杂度。

#### ✅ 创新点 2：引入 Graph Partitioning 机制
- 将神经网络划分为两个子图：
  - **SSM-subgraph**：局部时间算子 → 可转换为 SSM 流式处理；
  - **GTA-subgraph**（Global Temporal Aggregator）：如全局池化、Attention → 需要全局上下文，作为边界。
- 在滑动窗口更新时，仅更新受影响的隐藏状态，避免全图重算。

#### ✅ 创新点 3：支持 Overlapping Sliding Window 的高效处理
- 设计算法识别哪些隐藏状态需要更新，并动态调整 GTA 层的 stride，减少不必要的计算。

#### ✅ 创新点 4：优化 Global Pooling 实现
- 提出两级 SSM 结构替代原始全局池化操作，将 RAM 复杂度从 $O(N)$ 降至 $O(N/s)$，显著降低内存占用。

#### ✅ 创新点 5：可选 BF16 支持
- 使用 **BF16**（Brain Floating Point）格式存储隐藏状态，在精度损失极小的情况下进一步压缩内存使用（相比 FP32 减少 50% RAM）。

---

### 相比现有方法的优势
| 方面 | 现有方法（如 FastWaveNet、StreamiNNC） | TinyDejaVu |
|------|----------------------------------------|-----------|
| 支持算子范围 | 主要限于 1D Convolution | 支持 Conv、Pooling、Global Pooling、混合架构 |
| 是否支持通用模型 | 否，针对特定结构设计 | 是，适用于多种 Hybrid 架构 |
| 是否端到端代码生成 | 否 | 是，基于 microTVM 自动生成平台无关 C 代码 |
| 内存优化程度 | 中等 | 最高达 **99% RAM 节省** |
| 推理加速能力 | 有限 | 最高可达 **200× 加速**（streaming 阶段） |

> ✅ **优势总结**：TinyDejaVu 是首个支持多类时间算子、通用模型架构、具备自动编译流水线的 SSM 化推理优化框架。

---

## 2. 核心实验方法和设置

### 使用的模型（非传统“数据集”，而是典型 TinyML 时间序列模型）
实验未使用标准数据集，而是选取了多个代表性的时间序列模型进行基准测试，涵盖不同架构类型：

| 模型 | 类型 | 特征 |
|------|------|------|
| **WaveNet** | 纯时间卷积生成模型 | Dilated 1D Conv 栈，用于语音生成 |
| **TC-CNN**, **TEMPONet**, **ResTCN** | CNN + Dense 分类/生成模型 | 使用卷积和池化提取特征 |
| **CET-S**, **TC-TFM** | Transformer-based 模型 | 包含 Attention 层，最终分类 |

所有模型均适配至可在 MCU 上运行的小规模版本。

---

### 实验设置
- **硬件平台**：
  - 主测试平台：**STM32F767ZI MCU**
    - RAM: 512 KB
    - Flash: 2 MB
    - CPU: ARM Cortex-M7 @ 216 MHz
  - 其他验证平台：ESP32、RISC-V、x86（验证可移植性）
- **软件栈**：
  - 前端：PyTorch v2.3.0
  - 编译器后端：microTVM v0.16.0
  - 部署环境：RIOT-ML（轻量级物联网操作系统集成）

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **Peak RAM Usage** | 推理过程中最大瞬时内存占用 |
| **Compute Latency** | 单样本推理延迟（分 Preheat 和 Streaming 阶段） |
| **Overlap Rate** | 滑动窗口重叠率：$ \text{Toverlap} = 1 - s/l $，其中 $s$: stride, $l$: window length |
| **Relative RMSE** | BF16 与 FP32 输出之间的相对均方根误差，衡量精度影响 |

---

### 基线方法对比
- **Vanilla Inference**：标准逐窗口重计算方式（无任何优化）
- **FastWaveNet**：仅针对 WaveNet 的缓存优化方案
- **StreamiNNC**：利用卷积平移不变性跳过冗余计算
- **microTVM baseline**：直接部署未经 SSM 转换的模型

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 📊 表 2：WaveNet 实验结果（生成 10,000 个样本）
| 方法 | RAM Usage | Preheat (ms) | Streaming (ms/sample) |
|------|------------|---------------|--------------------------|
| Vanilla WaveNet | 178.5 MB (**OOM**) | OOM | – |
| TinyDejaVu | **142.6 kB** | 2776.1 | **14.1** |
| + BF16 | **108.3 kB** (-24%) | 3157.1 | 16.0 (+13%) |

> ⚡️ **结论**：
> - RAM 使用仅为原版的 **0.08%**
> - Streaming 阶段比 Preheat 快近 **200 倍**
> - BF16 再降 31% RAM，代价仅为 13% 计算开销

---

#### 📈 图 4：各类模型 RAM 使用对比（归一化）
- 所有模型在 TinyDejaVu 下实现 **至少 60% RAM 节省**
- 最佳情况（TC-TFM、TC-CNN）接近 **>99% RAM 减少**
- 启用 BF16 后进一步下降（除 CET-S 外）

> 💡 原因分析：TC-TFM 输入窗口大（48K），但感受野小（kernel=3），因此 SSM 转换收益巨大。

---

#### 📉 图 5：不同重叠率下的 Streaming 推理延迟（归一化）
- 随着 **Overlap Rate ↑**，延迟 **线性下降**
- 表明 TinyDejaVu 成功消除冗余计算
- ResTCN 和 TEMPONet 在低重叠率下仍保持较低延迟 → 深层 SSM 更好地保留历史信息

---

#### 🔍 精度影响（BF16）
- 所有模型输出的 **Relative RMSE 在 1%~3% 之间**
- 精度下降可忽略
- 建议：若追求零精度损失，可在 BF16 下重新训练模型

---

### 消融实验（隐含在分析中）
虽然没有明确列出消融表，但从以下方面体现了模块有效性：
| 组件 | 效果 |
|------|------|
| SSM 转换 | RAM 降低 $T/N$ 倍（$T$: 感受野，$N$: 序列长度） |
| Global Pooling SSM 化 | RAM 从 $O(N)$ → $O(N/s)$ |
| BF16 存储 | 隐藏状态 RAM 占用减半 |
| Graph Partitioning | 仅更新必要部分，提升计算效率 |

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SSM 是统一表达时间算子的有效范式**  
   卷积、池化等均可转化为 SSM，实现流式处理，打破“必须缓存整个窗口”的思维定式。

2. ✅ **TinyDejaVu 显著降低 MCU 上的时间序列推理成本**  
   - 最高节省 **99% RAM**
   - Streaming 阶段提速最高达 **200×**

3. ✅ **支持多样化模型架构**  
   不仅适用于纯 CNN/TN 模型，也兼容含 Attention 的 Transformer 模型。

4. ✅ **具备良好可移植性和实用性**  
   基于 microTVM 生成平台无关 C 代码，已在多种 MCU 上验证可用。

5. ✅ **Overlap Rate 是控制优化程度的关键参数**  
   用户可通过调节滑动窗口步长来权衡实时性与资源消耗。

---

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **GTA 层仍是瓶颈** | 如 Attention、Global Pooling 若位于深层，仍需较大内存，无法完全避免 |
| **初始化阶段（Preheat）较慢** | 第一个窗口需完整计算，不适合冷启动要求极高的场景 |
| **不适用于非因果模型** | 若模型依赖未来信息（非 causal），则不能使用 SSM 流式处理 |
| **精度微损（BF16）** | 虽然 RMSE 很小，但在敏感任务中可能需重新训练 |

---

### 未来工作方向
1. **扩展更多算子支持**  
   当前主要覆盖 Conv、Pooling、Linear，未来可加入更复杂的 temporal blocks。

2. **自动化模型重构工具链**  
   开发 GUI 工具或 CLI 插件，让用户一键完成 SSM 转换与部署。

3. **结合量化与剪枝**  
   将 SSM 优化与模型压缩技术（如 INT8 Quantization）结合，进一步降低资源需求。

4. **在线自适应 overlap 控制**  
   动态调整滑动窗口 stride，根据输入信号变化智能平衡精度与效率。

5. **支持更多硬件加速器**  
   集成到 GAP8、Greenwaves 等专用 TinyML 芯片中，发挥更大效能。

---

## 总结
> **TinyDejaVu 是一项面向 MCU 上长时间序列流式推理的重大进步**。它通过将时间算子统一建模为 SSM，从根本上解决了滑动窗口带来的内存爆炸与计算冗余问题。其实验结果证明，在真实 MCU 平台上实现了 **两个数量级的 RAM 节省** 和 **百倍以上的推理加速**，同时保持模型准确性。该工作不仅提出了新理论视角，还提供了开源、可复现、可部署的完整工具链，为下一代低功耗、始终在线的智能传感系统奠定了基础。

</details>

---

### 7. [d-TreeRPO: Towards More Reliable Policy Optimization for Diffusion Language Models](https://arxiv.org/abs/2512.09675)

**Authors**: Leyi Pan, Shuchang Tao, Yunpeng Zhai, Zheyu Fu, Liancheng Fang, Minghua He, Lingzhe Zhang, Zhaoyang Liu, Bolin Ding, Aiwei Liu, Lijie Wen  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09675v1  

#### Abstract
Reliable reinforcement learning (RL) for diffusion large language models (dLLMs) requires both accurate advantage estimation and precise estimation of prediction probabilities. Existing RL methods for dLLMs fall short in both aspects: they rely on coarse or unverifiable reward signals, and they esti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：d-TreeRPO: Towards More Reliable Policy Optimization for Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前针对 **diffusion large language models (dLLMs)** 的强化学习（RL）方法在以下两个方面存在显著缺陷：
- **Reward 设计缺乏细粒度和可验证性**：多数方法依赖稀疏的最终结果奖励（outcome rewards），忽略中间去噪步骤；或使用未经验证的过程信号（如均匀广播奖励、模型预测状态奖励），易导致 **reward hacking**。
- **Action log-probability 估计不准确**：由于 dLLMs 支持任意顺序解码（any-order decoding），无法像 auto-regressive (AR) 模型那样通过链式法则精确计算概率。现有方法（如基于 ELBO 的估计）存在未分析的偏差，且多需多次前向传播，效率低。

### 提出的新方法：d-TreeRPO
为解决上述问题，作者提出 **d-TreeRPO** ——一种更可靠的 dLLM 策略优化框架，其核心创新包括：

#### （1）树结构 Rollout 与自底向上优势计算（Tree-Structured Rollout & Bottom-Up Advantage）
- 将 rollout 组织成一棵树结构，每个节点代表一个部分解码状态。
- 叶子节点对应完整生成结果，并赋予 **可验证的结果奖励**（如数独是否正确）。
- 奖励从叶节点向上聚合：父节点奖励 $ R_p = \frac{1}{|C_p|} \sum_{c \in C_p} R_c $
- 子节点相对于父节点的优势定义为：$ A_c = R_c - R_p $

👉 这种机制提供了**密集、可验证、基于实际采样结果的逐步骤奖励信号**，避免了 reward hacking 风险。

#### （2）单次前向传递的概率估计 + 自蒸馏损失提升置信度
- 使用单次前向传递（single-time forward pass）估算父子节点间的转移对数概率（log-probability）。
- 理论证明：该估计误差随模型预测置信度增加而减小（即确定性越高，估计越准）。
- 引入 **time-scheduled self-distillation loss**：
  - 早期训练阶段放松约束，鼓励探索；
  - 后期逐步增强约束，提高策略确定性，从而降低概率估计误差并促进收敛。

### 相比现有方法的优势
| 方面 | 现有方法 | d-TreeRPO |
|------|--------|----------|
| Reward Granularity & Verifiability | 稀疏或不可验证过程奖励 | 密集、可验证、基于采样的逐步奖励 |
| Log-Probability Estimation | 偏差大、需多轮前传、无理论保障 | 单次前传 + 理论误差界 + 自蒸馏提升精度 |
| 探索-利用权衡 | 固定策略 | 动态调度：先探索后聚焦 |

---

## 2. 核心实验方法和设置

### 数据集
实验涵盖四类推理任务：
- **Sudoku**：4x4 数独求解（来自 Diffu-GRPO 提供的数据集）
- **Countdown**：数字拼凑游戏（3-to-4 Countdown 任务）
- **GSM8K**：小学数学应用题
- **Math500**：中等难度数学问题

所有任务采用零样本（zero-shot）评估，pass@1 作为评分标准。

### 实验设置
- **基础模型**：LLaDA-8B-Instruct（已完成监督微调，未进行 RL）
- **训练方式**：
  - 使用 LoRA（rank=128, α=64）
  - 最大生成长度 256 tokens，块长度 32，共 128 步去噪
  - Tree 结构参数：高度 $ H=2 $，分支因子 $ B=4 $
- **评估设置**：
  - 两种生成长度：256 和 512 tokens
  - 去噪步数为生成长度的一半
  - 温度为 0.0（确定性采样）

### 基线方法对比
对比了多个现有的 dLLM RL 方法：
- **Diffu-GRPO**（Zhao et al., 2025）
- **VRPO**（Zhu et al., 2025a）
- **wd1**（Tang et al., 2025）
- **SAPO+**（Xie et al., 2025a）
- **d2-stepMerge**（Wang et al., 2025a）
- **TraceRL**（Wang et al., 2025b）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（256-token setting）
| 方法 | Sudoku | Countdown | GSM8K | Math500 |
|------|--------|-----------|-------|---------|
| Base Model (LLaDA-8B-Instruct) | 6.7 | 19.5 | 76.7 | 32.4 |
| **d-TreeRPO (Ours)** | **92.9** (+86.2) | **71.1** (+51.6) | **81.2** (+4.5) | **37.7** (+5.3) |

> ✅ 在所有任务上均取得 SOTA 表现，尤其在 Sudoku 上实现巨大飞跃（+86.2）。

### 与基线方法对比
- 在 Sudoku 上，d-TreeRPO 显著优于最强基线 d2-stepMerge（76.1 → 92.9）
- 在 Countdown 上超越所有基线（最高达 71.1）
- 在 GSM8K 和 Math500 上也稳定领先，说明其泛化能力强

### 消融实验结果
进行了三项关键消融研究：

#### （1）移除 self-distillation loss
| 方法 | Sudoku | Countdown | GSM8K | Math500 |
|------|--------|-----------|-------|---------|
| d-TreeRPO (Full) | 92.9 | 71.1 | 81.2 | 37.7 |
| w/o self-distillation | 89.8 | 66.4 | 80.9 | 36.1 |

👉 移除后性能全面下降，验证了 self-distillation 对收敛和性能的重要性。

#### （2）替换为“多样性促进”损失（diversity-promoting loss）
设计了一个反向目标：故意拉低高优势动作的置信度，增加后期不确定性。
- 结果显示该变体不仅最终性能最差（Sudoku: 84.2），且训练不稳定。
- 支持了“后期应增强确定性”的设计理念。

#### （3）时间调度策略分析（reverse schedule）
将 self-distillation 的权重 $ \lambda(t) $ 和温度 $ \tau(t) $ 调度反转（初期强蒸馏，后期弱）：
- 初期提升更快，但中期开始性能回落（reward 从 ~0.75 下降）
- 说明过早固化策略会损害泛化能力

---

## 4. 关键结论和发现

### 主要发现
1. **细粒度、可验证的奖励信号至关重要**：通过树结构 rollout 和 bottom-up 奖励传播，实现了真正意义上的 step-wise 优势估计，提升了 RL 优化可靠性。
2. **预测置信度直接影响概率估计准确性**：理论证明了 single-pass 概率估计误差随模型确定性升高而降低。
3. **动态调度的 self-distillation 是平衡探索与利用的有效手段**：前期鼓励探索，后期提升确定性，兼顾学习效率与最终性能。
4. **d-TreeRPO 在多个复杂推理任务上显著超越现有方法**，尤其是在逻辑严密的任务（如 Sudoku）上表现突出。

### 方法的局限性
- **计算开销较高**：tree rollout 的计算复杂度随 $ H $ 和 $ B $ 指数增长，限制了更大规模 tree 的使用（文中 $ H=2, B=4 $ 已接近资源极限）。
- **依赖可验证的最终奖励**：适用于有明确答案的任务（如数学、谜题），对开放生成任务（如创意写作）适用性有限。
- **当前实现仍基于 block-wise decoding**，尚未完全释放任意顺序解码的潜力。

### 未来工作方向
- 扩展到更多类型的下游任务（如代码生成、对话系统）
- 探索更高效的 tree 构建与剪枝策略以降低计算成本
- 将 tree 结构与 value-based RL 方法结合，进一步提升稳定性
- 研究如何在非确定性奖励场景下应用类似思想

---

> 🔚 **总结**：  
> d-TreeRPO 是首个系统性解决 dLLM 强化学习中 **奖励可信度** 与 **概率估计偏差** 两大挑战的工作。它通过 **树结构 rollout + 自底向上奖励传播 + 时间调度自蒸馏** 的组合设计，在理论与实践层面都展现出卓越效果，为 diffusion language models 的可靠强化学习开辟了新路径。

</details>

---

### 8. [Semantic-Aware Cooperative Communication and Computation Framework in Vehicular Networks](https://arxiv.org/abs/2512.09621)

**Authors**: Jingbo Zhang, Maoxin Ji, Qiong Wu, Pingyi Fan, Kezhi Wang, Wen Chen  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09621v1  

#### Abstract
Semantic Communication (SC) combined with Vehicular edge computing (VEC) provides an efficient edge task processing paradigm for Internet of Vehicles (IoV). Focusing on highway scenarios, this paper proposes a Tripartite Cooperative Semantic Communication (TCSC) framework, which enables Vehicle User...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Semantic-Aware Cooperative Communication and Computation Framework in Vehicular Networks

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
本文聚焦于**高速公路场景下的车联网（Internet of Vehicles, IoV）中任务卸载延迟高、通信效率低**的问题。传统基于比特级通信（bit-level communication）的任务卸载机制存在冗余高、难以满足超低时延和高可靠性需求的缺陷。此外，现有研究大多仅考虑 **Vehicle-to-Infrastructure (V2I)** 链路，未能充分利用 **Vehicle-to-Vehicle (V2V)** 协作带来的资源灵活性。

### 🚀 提出的新方法与创新思路
作者提出了一种名为 **Tripartite Cooperative Semantic Communication (TCSC)** 的框架，并设计了结合强化学习与优化理论的联合算法：

- **TCSC 框架**：构建了一个三元协同语义通信架构，支持车辆用户（VUs）通过 **V2I 和 V2V 双链路** 将语义任务卸载至路边单元（RSU）或服务车辆（SVs），实现更灵活高效的边缘计算协作。
  
- **MAPPO-PDN + LP 联合优化方法**：
  - **MAPPO-PDN**：提出一种基于参数分布噪声的多智能体近端策略优化算法（Multi-Agent Proximal Policy Optimization with Parametric Distribution Noise），用于优化离散变量——每个词传输的语义符号数量 $k_{i,j}$。
    - 创新地将策略网络参数建模为高斯分布，引入 **Parametric Distribution Noise (PDN)** 正则项，提升模型在非平稳环境中的泛化能力和鲁棒性。
  - **Linear Programming (LP)**：利用线性规划求解连续变量——任务卸载比例 $p$，降低整体优化复杂度并提高精度。

该方法实现了对语义符号数 $k$ 和卸载比率 $p$ 的联合优化，在保证语义相似度的前提下最小化系统总延迟。

### 🔍 相比现有方法的优势
| 对比维度 | 本文方法（TCSC + MAPPO-PDN + LP） | 现有典型方法 |
|--------|-------------------------------|-------------|
| 通信范式 | 语义通信（Semantic Communication, SC） | 传统比特级通信 |
| 卸载方式 | 支持 V2I + V2V 协同卸载 | 多数仅支持 V2I |
| 学习机制 | 引入 PDN 建模策略不确定性，增强探索多样性 | 传统 MARL 易过拟合、收敛不稳定 |
| 优化结构 | 分解 MINLP 问题，MAPPO-PDN 处理离散变量，LP 处理连续变量 | 端到端 MARL 或单一优化方法，复杂度高 |

> ✅ **优势总结**：所提方案在动态车联网环境中具有更强的适应性、更低的延迟、更高的资源利用率和更好的语义保真度。

---

## 2. 核心实验方法和设置

### 📊 数据集与仿真环境
- **未使用真实公开数据集**，而是基于模拟器生成符合实际交通特征的数据。
- 场景设定为一条 **400米长的双车道高速公路**，部署一个连接 MEC 服务器的 RSU。
- 车辆分为两类：
  - **Vehicle Users (VUs)**：共 20 辆，产生计算密集型语义任务；
  - **Service Vehicles (SVs)**：固定 5 辆，可作为边缘节点提供计算服务。

### ⚙️ 实验设置
| 参数 | 设置值 |
|------|-------|
| 任务生成 | Poisson 分布，每时隙 0.8 Mbit |
| 计算需求 C | 1000 cycles/bit |
| 传输功率（V2I / V2V） | 23 dBm / 15 dBm |
| 噪声功率 | -114 dBm |
| 带宽 B | 540 kHz |
| 计算能力（RSU / VU / SV） | 6 GHz / 1 GHz / 3 GHz |
| 句子平均长度 $L_{i,j}$ | 20 words |
| 语义转换因子 H | 1200 bits/sentence |
| 语义相似度阈值 $\theta_{th}$ | 0.9 |
| PDN 上限 $\Omega_{max}$ | 20 |

### 📈 评估指标
- **Average Task Delay**：任务从生成到完成的总延迟（含传输 + 计算）
- **Transmission Delay**
- **Reward Convergence**（训练过程奖励曲线）
- 不同 VU 数量下的性能变化趋势

### 🔁 基线方法对比
1. **MAPPO-PDN + LP**（本文方法）
2. **MAPPO-PDN + Ivy**（Ivy 是一种启发式优化算法 [26]）
3. **MAPPO + LP**
4. **MADQN + LP**
5. **Linear K-Selection + LP**（线性选择 $k$ 值）
6. **Traditional + LP**（传统通信 + LP 卸载）
7. **Traditional + Ivy**
8. **Traditional (No LP)**（固定卸载比例的传统通信）

---

## 3. 主要实验结果和性能指标

### 📉 关键性能表现（见 Fig. 3 & Fig. 4）

#### （1）奖励收敛性（Fig. 2）
- **MAPPO-PDN 方法在训练过程中持续获得更高奖励**，且收敛更快、更稳定。
- 传统 MARL 方法（如 MAPPO、MADQN）易出现波动或早停滞，表明其在非稳态多智能体环境下鲁棒性较差。
- **PDN 正则化有效提升了策略探索能力与泛化性能**。

#### （2）传输延迟 vs 车辆数量（Fig. 3）
- 当 VU 数量从 15 增加到 40：
  - 所有方法延迟均上升（资源竞争加剧）；
  - 在 25–35 辆之间出现“凹形”下降趋势：因车距缩短 → 信道增益提升 → 传输速率增加 → 延迟短暂降低；
  - 超过一定数量后（如 40 辆），频谱复用干扰严重，延迟回升。
- **本文方法（Our-Method + LP）始终优于其他方法**，尤其在高密度场景下优势明显。

#### （3）平均任务延迟（Fig. 4）
- 随着车辆增多，**计算延迟成为主导因素**（边缘节点资源被摊薄）。
- **本文方法显著降低了整体任务延迟**：
  - 相比 “Traditional + LP”，延迟减少约 **30%~40%**；
  - 相比 “MAPPO + LP”，延迟降低约 **20%**；
  - 相比 “MADQN + LP”，性能提升更为显著。

#### （4）消融实验分析（隐含于对比中）
虽然文中未明确列出“ablation study”小节，但从以下对比可推断关键组件作用：

| 组件 | 性能影响 |
|------|---------|
| **PDN 正则化**（MAPPO-PDN vs MAPPO） | 提升训练稳定性与最终性能 |
| **LP 优化卸载率**（+LP vs +Ivy/no-LP） | 显著降低延迟，说明精确控制卸载比例的重要性 |
| **语义通信（vs Traditional）** | 同等条件下大幅减小需传输的信息量，从而降低延迟与能耗 |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **语义通信 + VEC 是满足 IoV 超低时延需求的有效路径**：相比传统通信，SC 显著减少了信息冗余，提高了传输效率。
2. **V2I/V2V 协同卸载优于单链路模式**：TCSC 框架充分利用移动 SVs 的空闲算力，增强了系统的弹性与可扩展性。
3. **MAPPO-PDN 在非稳态多车环境中表现出优越的鲁棒性和收敛性**：通过建模策略参数的不确定性，避免了传统 MARL 的过拟合问题。
4. **分解优化策略（MAPPO-PDN + LP）优于端到端学习**：分别处理离散与连续变量，兼顾精度与效率。

### ⚠️ 局限性
- **依赖预训练 DeepSC 模型**：语义相似度映射表 $ \delta_{i,j} = f(k_{i,j}, \text{SINR}) $ 来自 AWGN 信道下的训练结果，可能不适用于复杂真实信道。
- **假设车辆速度恒定**：忽略了加减速、变道等动态行为对信道和拓扑的影响。
- **未考虑多模态任务**（如图像、语音）：当前模型主要针对文本类语义任务。
- **仿真环境理想化**：缺乏真实道路流量、信号遮挡、突发干扰等因素。

### 🔮 未来工作方向
- 探索 **多模态语义通信**（Multimodal Semantic Communication）在 IoV 中的应用；
- 引入 **Reconfigurable Intelligent Surface (RIS)** 或 **AI-empowered channel prediction** 提升语义传输质量；
- 研究 **去中心化联邦语义学习框架**，保护隐私的同时实现跨车知识共享；
- 结合 **joint communication-computation coding** 进一步压缩语义负载。

---

## 总结

✅ 本文提出了面向高速公路场景的 **TCSC 框架**，融合 **语义通信、V2I/V2V 协同卸载与 MAPPO-PDN+LP 联合优化算法**，在保障语义准确性的前提下显著降低了任务延迟。实验证明该方法在多种交通密度下均优于传统及主流 MARL 方法，为下一代智能车联网提供了高效可靠的语义级计算卸载解决方案。

</details>

---

### 9. [RIFT: A Scalable Methodology for LLM Accelerator Fault Assessment using Reinforcement Learning](https://arxiv.org/abs/2512.09829)

**Authors**: Khurram Khalil, Muhammad Mahad Khaliq, Khaza Anuarul Hoque  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09829v1  

#### Abstract
The massive scale of modern AI accelerators presents critical challenges to traditional fault assessment methodologies, which face prohibitive computational costs and provide poor coverage of critical failure modes. This paper introduces RIFT (Reinforcement Learning-guided Intelligent Fault Targetin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RIFT: A Scalable Methodology for LLM Accelerator Fault Assessment using Reinforcement Learning

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代大规模 **Large Language Models (LLMs)** 在专用 AI 加速器（如 GPU、TPU）上运行时，面临硬件故障导致功能失效的风险。由于模型参数规模达到数十亿级别，传统故障评估方法（如 Random Fault Injection, RFI）存在以下问题：

- **计算成本极高**：单比特故障位置可达 $ m \times n \times 10^9 $ 量级（例如 8B 参数 × 8bit），组合故障空间呈指数爆炸。
- **覆盖率低且效率差**：随机注入难以发现稀疏但高影响的“最坏情况”故障组合。
- **缺乏可扩展性**：现有基于静态分析或进化算法的方法无法有效扩展到百亿参数模型。

因此，如何高效、系统地搜索出能引发灾难性失败的最小高影响故障集合，成为设计自动化（EDA）领域的关键挑战。

---

### **提出了什么新方法或新思路**

本文提出 **RIFT**（Reinforcement Learning-guided Intelligent Fault Targeting），一个可扩展的、基于强化学习的故障评估框架，其核心创新包括：

1. **将故障搜索建模为序列决策问题**  
   将寻找最小高影响故障集的问题转化为 **Markov Decision Process (MDP)**，由 RL Agent 通过试错逐步构建最优故障组合。

2. **三阶段分层优化架构**
   - **Phase I: Vulnerability Profiling**  
     使用混合敏感度评分（hybrid sensitivity score），结合参数的静态重要性（magnitude）和动态梯度信息（gradient w.r.t. loss），对所有参数进行排序。
   - **Phase II: Candidate Set Initialization**  
     从排序列表中选取 top-$ p\% $ 最敏感参数作为候选集 $ P_{\text{crit}} $，大幅缩小搜索空间。
   - **Phase III: RL-Powered Test Vector Generation**  
     在候选集上运行 Q-learning 算法，以准确率下降和故障数量为奖励函数，自动探索最优故障组合。

3. **生成 UVM-compliant 测试激励**
   自动输出符合工业标准的 **UVM testbench**，支持直接集成进商业 RTL 验证流程，实现“push-button”式故障注入。

4. **指导硬件保护策略设计**
   利用识别出的关键脆弱位点，提出 **RIFT-guided selective ECC**，在极低开销下实现高性价比的容错设计。

---

### **相比现有方法的优势**

| 方面 | RIFT 的优势 |
|------|-------------|
| **效率** | 相比 RFI 提速 7.5×，相比 GenBFA 提速 2.2× |
| **测试向量规模** | 减少 >99% 的 test vectors（仅需 ~847 个） |
| **覆盖率** | 达到 91.7% 故障覆盖率，显著高于其他方法 |
| **实用性** | 输出 UVM 兼容代码，易于工程部署 |
| **设计指导能力** | 支持可靠性感知的 DSE（Design Space Exploration） |

---

## 2. 核心实验方法和设置

### **使用的数据集与目标模型**

- **基准测试集**：
  - **MMLU**：57 个学科，15,000+ 多选题，用于评估通用知识理解。
  - **MMLU-Pro**：更具挑战性的变体，14 学科，12,000 问题。
- **目标模型（DUT）**：
  - GPT-2 Large (~30.5% baseline acc)
  - LLaMA 3.1 8B (~69.9% baseline acc)
  - DeepSeek-V2 7B MoE (~71.3% baseline acc)
- 所有模型采用 **8-bit integer quantization**，反映实际部署场景。

---

### **实验设置和评估指标**

#### **故障模型**
- 注入方式：翻转选定参数的 **Most Significant Bit (MSB)**，模拟最严重的数值偏差。
- 灾难性失败定义：模型准确率下降超过 90%。

#### **评估指标**
| 指标 | 定义 |
|------|------|
| **Coverage (Cov)** | 成功识别的关键故障比例（导致 >90% 性能退化） |
| **Time (hrs)** | 在 1000 CPU 小时预算内的运行时间 |
| **Test Vectors (TV)** | 生成的故障注入测试用例数 |
| **Efficiency (Eff)** | Coverage / Time（每小时覆盖率） |
| **Cost-Effectiveness (CE)** | Fault Coverage / Area Overhead（单位面积覆盖） |

#### **统计方法**
- 所有实验重复 15 次，报告均值 ± 标准差。
- 显著性检验使用 Welch’s t-test + Bonferroni 校正。

---

### **基线方法对比**

| 基线方法 | 类型 |
|--------|------|
| **Random Fault Injection (RFI)** | 工业标准，随机采样 |
| **Magnitude Ranking** | 静态分析（类似 PrisonBreak） |
| **Gradient Selection** | 动态分析（类似 DeepHammer） |
| **GenBFA [17]** | 当前最先进的进化算法（evolutionary search） |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table I）**

| 方法 | Coverage (%) | 时间 (hrs) | Test Vectors | Efficiency (Cov/hr) | Speedup vs RFI |
|------|--------------|-----------|----------------|-----------------------|----------------|
| RFI | 65.3±4.1 | 1000±52 | 1.2×10⁵ | 0.065 | 1.0× |
| Magnitude Ranking | 73.8±3.7 | 245±18 | 8.4×10⁴ | 0.301 | 4.6× |
| Gradient Selection | 79.2±2.9 | 198±15 | 6.1×10⁴ | 0.400 | 6.2× |
| GenBFA [17] | 84.6±3.2 | 388±24 | 4.7×10³ | 0.218 | 3.4× |
| **RIFT (Ours)** | **91.7±2.1** | **187±12** | **847±73** | **0.490** | **7.5×** |

> ✅ RIFT 实现最高覆盖率、最短时间和最少测试向量。

---

### **与基线方法的对比结果**

- **速度提升**：
  - 相比 RFI：**7.5× 更高效**
  - 相比 GenBFA：**2.2× 更快**
- **测试向量压缩**：
  - 相比 RFI 减少 **>99%** 的 test vectors（从 12万 → 847）
- **资源消耗更低**：
  - 在更短时间内完成更高覆盖率任务，适合工业级快速验证。

---

### **消融实验结果（Ablation Studies）**

#### **A. 混合敏感度指标的有效性（Figure 3）**

| α（mixing coefficient） | 平均所需故障数 |
|--------------------------|----------------|
| α = 0.0（纯 magnitude） | 7.1±0.8 |
| α = 1.0（纯 gradient）   | 6.4±0.6 |
| **α = 0.5（hybrid）**    | **5.0±0.4** |

> ✅ 混合指标减少 29% 的故障需求，证明动静结合更优。

#### **B. 三阶段架构必要性（Table V）**

| 配置 | 找到的关键故障数 | 收敛所需 Episodes |
|------|--------------------|---------------------|
| RL-Only（同预算 50ep） | 47.3±8.2 | 50（未收敛） |
| RL-Only（至收敛）       | 5.2±0.6 | 890±67 |
| **完整 RIFT**           | **5.0±0.4** | **50±3** |

> ✅ Phase I & II 提供先验知识，使 RL 收敛速度快 **17× 以上**。

#### **C. 超参数鲁棒性（Figure 4）**

- **训练 episode 数**：在 50 轮后趋于饱和，无需精细调参。
- **探索率 ε ∈ [0.05, 0.30]**：性能波动 <5%，表明算法稳定。

#### **D. 架构泛化能力（Table VI）**

| DUT 模型 | Hybrid Metric 提升 | 3-Phase 架构增益 |
|---------|--------------------|------------------|
| GPT-2 Large | 24% | 3.8× |
| LLaMA 3.1 8B | 29% | 4.2× |
| DeepSeek-V2 7B | 31% | 4.6× |

> ✅ RIFT 在不同 LLM 架构上均表现一致优越。

---

## 4. 关键结论和发现

### **主要发现**

1. **LLM 存在高度稀疏的脆弱点**  
   平均只需 **5.4±0.8 个比特翻转** 即可导致功能崩溃（accuracy 下降 >99%）。

2. **脆弱性集中在特定模块**  
   - 88.5% 的关键故障位于：
     - Attention Mechanisms: 47.3%
     - Normalization Layers: 41.2%
   - Feed-Forward Networks 相对鲁棒。

3. **RIFT 可提供可靠性感知的设计指导**
   - **RIFT-guided selective ECC**：
     - 区域开销仅 **13.8%**
     - 覆盖 88.5% 关键故障
     - 成本效益达 **6.4×**（Coverage/Area）
   - 对比 Uniform TMR：
     - 开销高达 **205%**
     - 成本效益仅为 **0.5**
     - ➜ RIFT 方案 **12.8× 更 cost-effective**

4. **具备良好的一致性与可扩展性**
   - 统计测试显示结果高度稳定（p < 0.001, Cohen's d > 2.5）
   - 运行时间随候选集大小线性增长（$ R^2 > 0.99 $）
   - 内存占用为 $ O(k^{1.3}) $，适用于下一代更大模型。

---

### **方法的局限性**

1. **依赖代表性数据集 $ D_{\text{rep}} $**  
   若 $ D_{\text{rep}} $ 不能代表真实负载分布，可能遗漏某些故障模式。

2. **当前仅支持永久性内存故障（bit-flip）**  
   未涵盖 transient faults、timing errors 或逻辑门级故障。

3. **Q-learning 表格法限制规模**  
   当前使用 tabular Q-learning，适用于数千参数级别的候选集；超大规模需引入 DQN 或 Policy Gradient。

4. **GPU 模拟环境非完全真实硬件反馈**  
   实际芯片中的物理效应（如电压噪声、温度漂移）尚未纳入建模。

---

### **未来工作方向**

1. **扩展至更多模型类型**  
   应用于 Vision Transformers、Diffusion Models 等大型架构。

2. **支持更复杂的故障模型**  
   引入 transient faults、crosstalk、timing violations 等物理层面错误。

3. **集成到自动化综合流程**  
   将 RIFT 探索引擎嵌入 RTL-to-GDSII 流程，实现 **reliability-aware synthesis**。

4. **发展分布式并行版本**  
   利用多 GPU/TPU 并行加速 DUT 评估环节，进一步缩短周期。

5. **结合形式化方法进行验证增强**  
   用 SAT/SMT 工具验证 RL 发现的故障路径是否可达。

---

> 🔚 **总结**：RIFT 是首个将 **Reinforcement Learning** 成功应用于 **LLM 加速器故障评估** 的框架，实现了从“盲目搜索”到“智能定向攻击”的范式转变，不仅极大提升了故障评估效率，还为可靠性驱动的硬件设计提供了量化依据，具有重要的理论价值和工业应用前景。

</details>

---

### 10. [Luxical: High-Speed Lexical-Dense Text Embeddings](https://arxiv.org/abs/2512.09015)

**Authors**: DatologyAI,  :, Luke Merrick, Alex Fang, Aldo Carranza, Alvin Deng, Amro Abbas, Brett Larsen, Cody Blakeney, Darren Teh, David Schwab, Fan Pan, Haakon Mongstad, Haoli Yin, Jack Urbanek, Jason Lee, Jason Telanoff, Josh Wills, Kaleigh Mentzer, Paul Burstein, Parth Doshi, Paul Burnstein, Pratyush Maini, Ricardo Monti, Rishabh Adiga, Scott Loftin, Siddharth Joshi, Spandan Das, Tony Jiang, Vineeth Dorma, Zhengping Wang, Bogdan Gaza, Ari Morcos, Matthew Leavitt  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09015v1  

#### Abstract
Frontier language model quality increasingly hinges on our ability to organize web-scale text corpora for training. Today's dominant tools trade off speed and flexibility: lexical classifiers (e.g., FastText) are fast but limited to producing classification output scores, while the vector-valued out...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Luxical: High-Speed Lexical-Dense Text Embeddings

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前在大规模语言模型（LLM）训练中，**文本语料库的组织与质量控制**成为关键瓶颈。现有的文本嵌入方法面临以下权衡：
- **Transformer-based embedding models**（如 MiniLM、Qwen）虽能生成高质量的 dense embeddings，适用于聚类、检索、分类等多种任务，但计算开销大，难以在万亿级 token 规模下高效部署。
- **Lexical classifiers**（如 FastText）运行速度快，适合大规模处理，但输出仅为单一分类得分，缺乏向量表示能力，无法支持聚类或对称检索等任务。

Luxical 旨在解决这一“速度 vs. 灵活性”的矛盾，提出一种兼具高效率与表达力的中间方案。

---

### 提出的新方法：Luxical 架构
Luxical 是一个融合 **sparse lexical 特征** 与 **轻量神经网络** 的混合型“lexical-dense”文本嵌入框架，其核心设计如下：

- **Sparse Lexical Featurization**：
  - 输入文本被分词后构建 **ngram-level TF-IDF 向量**（基于固定大小的 5-gram 词汇表）。
  - 使用精确匹配而非哈希技巧（no hashing trick），保留完整 term 统计信息。
  
- **Dense Neural Transformation**：
  - 将稀疏 TF-IDF 向量输入一个小型 ReLU 网络（MLP），输出归一化的 dense embedding。
  - 利用 **sparse-by-dense matrix multiplication** 实现高效前向传播，显著降低计算成本。

- **Knowledge Distillation 训练目标**：
  - 采用 **Gram-matrix 蒸馏损失函数**（Gram-matrix distillation objective）：
    $$
    \mathcal{L}_{\text{distill}} = T^2 \cdot \text{KLDiv}(G_s / T, G_t / T)
    $$
    其中 $G_s = SS^\top$ 和 $G_t = TT^\top$ 分别为学生（Luxical）与教师模型（如 snowflake-arctic-embed-m-v2.0）的相似度矩阵。
  - 目标是让 Luxical 学习教师模型的 **pairwise 相似性结构**，从而逼近其语义几何。

---

### 相比现有方法的优势
| 方法 | 优势 |
|------|------|
| **vs. Transformer Embedders**（MiniLM, Qwen） | 推理速度提升 **3x ~ 100x**，可在 CPU 上实现极高吞吐，适合大规模预处理流水线 |
| **vs. Lexical Models**（FastText） | 输出为 dense vector，支持聚类、检索、下游分类等多用途任务，灵活性更强 |
| **总体定位** | 在 **compute/quality trade-off 曲线上提供更优平衡点**，特别适用于 web-scale 文本组织场景 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **FineWeb**：用于训练和评估的主要英文网页语料库。
  - 训练集：从 FineWeb 中采样的 **5000 万文档**
  - 测试集：
    - **100,000 完整文档**：用于端到端吞吐测试
    - **50,000 文档拆分为 halves**：构造正样本对，用于 document-half matching 检索任务
    - **600B token 随机子集**：用于数据筛选实验，最终过滤至 60B token 高质量子集

---

### 实验设置与评估指标

#### （1）**Throughput Benchmark**
- **任务**：嵌入 100,000 FineWeb 文档
- **硬件环境**：
  - Apple M4 Max CPU（Luxical-One 主要运行平台）
  - NVIDIA A10G GPU（用于对比 transformer 模型加速效果）
- **评估指标**：
  - Documents per second（文档/秒）
  - MiB/sec（每秒处理的数据量）

#### （2）**Document-Half Matching**
- **任务**：将每个文档切分为两半，通过 embedding 相似度检索原始配对的一半
- **评估指标**：
  - Error-at-k 曲线（即 top-k 检索错误率）
  - 不同 retrieval window 大小下的表现（如 top 0.001%, 0.01%, ..., 50%）

#### （3）**Classifier-Based Data Curation**
- **任务**：使用不同 scorer 对 FineWeb 数据进行质量打分并筛选 top 10%
- **下游验证方式**：训练一个 3B 参数的 LLM，并在多个 zero-shot benchmark 上测试性能
- **评估指标**：
  - Scoring pipeline throughput（文档/秒）
  - 下游 LLM 的平均准确率（ARC, MMLU, OpenBookQA, SciQ）

---

### 基线方法对比
| 模型 | 类型 | 描述 |
|------|------|------|
| **Qwen3-0.6B** | Transformer Encoder | 强大的 dense embedding 模型，参数约 0.444B |
| **MiniLM-L6-v2** | Small Transformer | 轻量级 BERT 变体，常用于 sentence embedding |
| **FastText** | Lexical Classifier | DCLM 项目使用的分类器，仅输出 scalar score |
| **FineWeb-Edu Scorer** | Transformer + Classifier Head | 基于 BERT 的教育质量评分器 |
| **Arctic-2.0-M** | Teacher Model | Luxical-One 的蒸馏来源，embedding 维度 256 |
| **LEAF-MT** | Distilled MiniLM | 与 MiniLM 同架构但经知识蒸馏训练 |

---

## 3. 主要实验结果和性能指标

### （1）**Throughput 性能对比**（图 3）
| 模型 | 平台 | 吞吐量（docs/s） | 相对 Luxical-One 速度 |
|-------|--------|------------------|-------------------------|
| **Luxical-One** | CPU (M4 Max) | **6,803** | 1×（基准） |
| **MiniLM-L6-v2** | CPU | 307 | ~22× 更慢 |
| **MiniLM-L6-v2** | GPU | 2,357 | ~2.9× 更慢 |
| **Qwen3-0.6B** | GPU | 70 | ~97× 更慢 |

> ✅ **结论**：即使在 GPU 加速下，主流 transformer 模型仍远落后于 Luxical-One 的 CPU 推理速度。

---

### （2）**Document-Half Matching 检索性能**（图 4）
- 在 **top-1 检索** 上，Luxical-One 表现弱于大型模型（如 Qwen），但仍优于 MiniLM。
- 随着检索窗口扩大（如 top 0.1% ~ 1%），Luxical-One 快速追平甚至接近 Arctic-2.0-M 和 Mxbai-L-v1 的表现。
- **关键发现**：在粗粒度检索任务中（例如找出最相似的前 1% 文档），Luxical-One 的语义捕捉能力接近先进 transformer 模型。

> 📊 示例：在 top 1% retrieval window 下，Luxical-One 错误率显著低于 MiniLM-L6-v2 和 LEAF-MT。

---

### （3）**数据筛选应用中的综合表现**（图 5）
#### a. **分类流水线吞吐**
| 方法 | 吞吐量（MiB/s） | docs/s |
|------|------------------|--------|
| **Luxical + MLP Scorer** | **23.1** | 6,787 |
| **FastText Scorer** | 19.0 | 5,590 |
| **FineWeb-Edu Scorer** | 1.6 | 478 |

> ⚡ Luxical-based pipeline 比 transformer 基线快 **14.2x**，且略优于 FastText。

#### b. **下游 LLM 性能**
| 过滤方法 | MMLU 准确率 | ARC Easy | OpenBookQA | SciQ | 平均 |
|----------|-------------|-----------|--------------|-------|-------|
| No Filter | 低 | — | — | — | — |
| **All Three Filters** | **36.4%** | ~60% | ~50% | ~70% | ~相近 |

> ✅ 所有三种过滤方法（Luxical、FastText、FineWeb-Edu）得到的训练数据，均能训练出性能相当的 LLM，说明 Luxical 在实际应用中不牺牲质量。

此外，由于 Luxical 的 embedding 步骤可复用，而 MLP 分类极快（<0.25% runtime），允许快速迭代调参，进一步提升工程效率。

---

### （4）消融实验与分析（隐含在文中）
- **蒸馏的有效性**：尽管 Luxical 架构简单（无位置编码、浅层网络），但由于知识蒸馏自强大 teacher，其表现超越同规模 MiniLM 和 LEAF-MT。
- **IDF scaling 分离设计**：将 IDF 权重独立于 MLP 第一层，有助于优化稳定性。
- **tokenization 成为瓶颈**：得益于高效的 sparse projection，实际运行中 **tokenization 时间主导总耗时**，表明架构已高度优化。

---

## 4. 关键结论和发现

### 主要发现
1. **Luxical 成功实现了 lexical 与 dense 方法的优点结合**：
   - 推理速度媲美 FastText，在 CPU 上达到数千文档/秒；
   - 输出 dense embedding 支持聚类、检索、分类等多样化任务；
   - 语义质量在粗粒度任务上接近 transformer 模型。

2. **知识蒸馏是关键推动力**：
   - 即使学生模型结构受限，也能通过学习 teacher 的 Gram 矩阵有效继承其语义空间结构。

3. **适合大规模文本组织流水线**：
   - Luxical 设计为“一次性嵌入 + 多次复用”，非常适合用于 deduplication、clustering、domain slicing 等预处理阶段。

4. **部署友好**：
   - 模型小、依赖少、API 简洁，易于集成进现有系统；
   - 开源地址：[https://github.com/datologyai/luxical](https://github.com/datologyai/luxical)

---

### 局限性
1. **语言与领域限制**：
   - 当前仅发布英文模型 Luxical-One，未验证多语言能力。
   - 训练数据来自 FineWeb，可能偏向特定网页风格。

2. **细粒度任务表现有限**：
   - 在需要高精度排序的任务（如搜索引擎）中，仍不如 full transformer encoders。

3. **评估范围有限**：
   - 实验集中在两个任务（document retrieval 和 data curation），尚未覆盖更多应用场景（如跨模态、问答等）。

4. **未探索变体**：
   - 如更大/更小版本、不同蒸馏策略、动态 vocab 等扩展未研究。

---

### 未来工作方向
- 开发 **multilingual Luxical models**
- 探索 **distilled variants** 或 **quantized versions** 以进一步压缩
- 与 corpus management 工具链深度集成（如 Dolma、DataComp）
- 研究与其他静态 embedding 方法（如 Static Embeddings、model2vec）的关系与融合可能性

---

## 总结
Luxical 提出了一种新颖的 **lexical-dense hybrid embedding 范式**，通过 **TF-IDF + small MLP + knowledge distillation** 的组合，在保持极高推理速度的同时，获得了接近 transformer 模型的语义表达能力。实验证明其在 web-scale text organization 场景中具有显著优势，是连接传统 lexical 方法与现代 dense embedding 的一座实用桥梁。

</details>

---

### 11. [MentraSuite: Post-Training Large Language Models for Mental Health Reasoning and Assessment](https://arxiv.org/abs/2512.09636)

**Authors**: Mengxi Xiao, Kailai Yang, Pengde Zhao, Enze Zhang, Ziyan Kuang, Zhiwei Liu, Weiguang Han, Shu Liao, Lianting Huang, Jinpeng Hu, Min Peng, Qianqian Xie, Sophia Ananiadou  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09636v1  

#### Abstract
Mental health disorders affect hundreds of millions globally, and the Web now serves as a primary medium for accessing support, information, and assessment. Large language models (LLMs) offer scalable and accessible assistance, yet their deployment in mental-health settings remains risky when their ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MentraSuite: Post-Training Large Language Models for Mental Health Reasoning and Assessment

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 Large Language Models（LLMs）在心理健康领域的应用面临以下关键挑战：

- **推理不完整、不一致或缺乏依据**：许多模型仅能生成流畅但表面化的回应，无法进行临床对齐的逐步推理。
- **现有心理领域LLM侧重情感理解或知识回忆**，忽视了心理评估中的核心认知过程，如认知重构、诊断、干预规划、证据抽象和信息验证。
- **缺乏系统性评估框架**：现有基准（如 PsychCounsel-Bench）主要关注任务准确率，忽略了推理链的质量（如逻辑连贯性、幻觉避免、内部一致性等）。

因此，论文旨在解决：如何构建一个能够进行**可靠、可解释、临床对齐的心理健康推理**的 LLM 框架。

---

### **提出了什么新方法或新思路**

论文提出 **MentraSuite**，一个统一的框架，包含三大核心组件：

#### （1）**MentraBench**：首个综合性心理健康推理基准
- 覆盖五大核心临床推理维度：
  - **Appraisal**（认知模式识别）
  - **Diagnosis**（心理状况判断）
  - **Intervention**（干预策略选择）
  - **Abstraction**（研究证据提炼）
  - **Verification**（信息真伪辨别）
- 包含 **6项任务、13个数据集**，涵盖真实对话、社交媒体文本、医学考试题、系统综述等多源数据。
- 引入**五维推理质量评估体系**：
  - Reasoning Conciseness（简洁性）
  - Logical Coherence（逻辑连贯性）
  - Hallucination Avoidance（幻觉避免）
  - Task Understanding（任务理解）
  - Internal Consistency（内部一致性）

#### （2）**Mindora**：后训练优化的心理健康专用模型
- 基于 Qwen3-8B 构建，采用 **Hybrid SFT-RL 框架**（结合监督微调与强化学习）。
- 创新引入 **LLM-based inconsistency-detection reward**，通过辅助模型（Qwen3-32B）动态检测推理链中的事实错误和逻辑矛盾，提升推理保真度。
- 支持多阶段、整合性推理，适用于复杂心理场景。

#### （3）**Reasoning Trajectory Generation (RTG)**：高质量推理轨迹生成策略
- **难度筛选**：仅保留 Llama-3-8B-Instruct 零样本预测错误的样本，确保训练数据具有挑战性。
- **迭代最优路径搜索**：利用 GPT-4o 进行多轮反思与修正（Backtracking / New Path Exploration），生成正确且深度的推理链。
- **结构化格式化**：强制使用 `<think>` 和 `<answer>` 标签，并分模块（如 `###Symptom Analysis`）组织推理步骤，提升可读性和一致性。

---

### **相比现有方法的优势**

| 方法 | 局限性 | MentraSuite 的优势 |
|------|--------|------------------|
| **Psyche-R1** | 侧重共情与知识整合，未覆盖多阶段临床推理 | 明确建模 Appraisal → Diagnosis → Intervention 全流程 |
| **Psy-Interpreter** | 依赖专家标注场景，泛化能力受限 | 通过 RL + inconsistency reward 自动优化推理质量 |
| **PsychCounsel-Bench** | 仅测试知识记忆，形式为选择题 | 引入开放推理任务与多维质量评估 |
| **通用LLM（如GPT-4o）** | 推理冗长、易产生幻觉、缺乏一致性 | Mindora 在所有推理维度上表现更优 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

MentraBench 整合并新建了 **13个数据集**，分为六类任务：

| 任务 | 数据集 | 来源 | 类型 |
|------|--------|------|------|
| **Cognitive Error Identification** | CognitiveReframing, PatternReframe, Therapist Q&A | 合成 + 真实咨询对话 | Appraisal |
| **Mental Health Condition Detection** | DepSign, SWMH, T-SID | Reddit, Twitter | Diagnosis |
| **Counseling Strategy Formulation** | PsyDTCorpusM, AnnoMI | 合成 & MI 视频转录 | Intervention |
| **Psychiatry QA** | MHQA, MedQAM, MedMCQA, PubMedQAM | PubMed, 医学考试 | Multi-step Reasoning |
| **Systematic Review Summarization** | PSRS*（本文新建） | Cochrane Library | Abstraction |
| **Misinformation Identification** | MentalMisinfo | YouTube Shorts, BitChute | Verification |

> 注：带 * 表示本文新建；带 M 表示本文处理过。

---

### **实验设置和评估指标**

#### **评估模型范围广泛**
- **闭源模型**：GPT-4o, GPT-4o-mini, DeepSeek-R1, DeepSeek-V3, Qwen-plus, QwQ-plus
- **开源模型**：LLaMA3.1-8B, Qwen3-8B/14B/32B/72B 及其蒸馏版本
- **心理专用模型**：EmoLLM, Psyche-R1
- **本文提出模型**：Mindora_SFT, Mindora_SFT+RL, Mindora_CHORD

#### **评估方式**
- 所有模型均按 Mindora 格式生成结构化推理链（`<think>...</think><answer>...</answer>`）。
- **任务性能指标**：Micro-F1, Jaccard Score, Recall 等（依任务而定）。
- **推理质量人工评估**：从每个模型中抽样 4 例（2 正确 + 2 错误），由人工按五维标准打分（0/1），最终取平均值作为 **Reasoning Trajectory Score**。

---

### **基线方法对比**

| 基线类别 | 代表模型 | 特点 |
|---------|----------|------|
| 通用聊天模型 | GPT-4o, Qwen-plus | 强语言能力但推理不可控 |
| 推理优化模型 | GPT-4o-mini, DeepSeek-R1 | 使用 RL 提升推理能力 |
| 心理专用模型 | EmoLLM, Psyche-R1 | 聚焦共情与心理知识 |
| 开源基础模型 | Qwen3-8B, LLaMA3.1-8B | 未经专门推理优化 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table 3）**

| 模型 | Avg_all（13项平均） |
|------|--------------------|
| **Mindora_CHORD** | **0.6933** ✅（最高） |
| Mindora_SFT+RL | 0.6548 |
| GPT-4o-mini | 0.6515 |
| DeepSeek-R1 | 0.6350 |
| Psyche-R1 | 0.5943 |
| Qwen3-8B（骨干模型） | 0.5729 |

> Mindora_CHORD 在 **全部13个数据集** 上均优于基线，尤其在 **Intervention** 和 **Abstraction** 任务上提升显著。

---

### **与基线方法的对比结果**

- **超越最强闭源推理模型**：
  - Mindora_CHORD 比 GPT-4o-mini 高出 **+4.18个百分点**。
  - 在 **Therapist Q&A**（真实咨询认知错误识别）上，Mindora 达到 0.5088 vs GPT-4o-mini 的 0.4468。
- **优于心理专用模型 Psyche-R1**：
  - 平均得分高出 **近10个百分点**（0.6933 vs 0.5943），说明其推理机制更有效。
- **小模型胜过大模型**：
  - 8B 参数的 Mindora 超过 70B 的 LLaMA-3.3-70B（0.6933 vs 0.6178），证明**针对性后训练比模型规模更重要**。

---

### **消融实验结果（隐含分析）**

虽然未单独列出消融表，但从不同变体比较可得：

| 模型变体 | Avg_all | 分析 |
|----------|--------|------|
| Qwen3-8B | 0.5729 | 基线性能 |
| Mindora_SFT | 0.6368 | SFT 提升明显 |
| Mindora_SFT+RL | 0.6548 | 加入 RL 进一步提升 |
| **Mindora_CHORD** | **0.6933** | 动态融合 SFT 与 RL，效果最佳 |

> 结果表明：**联合训练（Joint SFT-RL）优于两阶段训练（SFT → RL）**，CHORD 算法能更好平衡模仿与探索。

---

### **推理轨迹质量评估（Table 4）**

| 模型 | R_avg（五维平均分） |
|------|---------------------|
| **Mindora_CHORD** | **0.9731** ✅ |
| DeepSeek-R1 | 0.9827（R3=1.0）但 R1=0.9519 |
| Psyche-R1 | 0.9442 |
| Qwen3-8B | 0.9039 |
| GPT-4o-mini | 0.8731 |

> Mindora 在 **Logical Coherence（R2=0.9519）** 和 **Internal Consistency（R5=0.9808）** 上表现极佳，说明其推理链更连贯、无矛盾。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **结构化推理训练显著提升心理健康任务表现**：
   - 通过 RTG 生成高质量、结构化、经过验证的推理轨迹，是提升模型可靠性的重要前提。

2. ✅ **Hybrid SFT-RL 框架优于纯 SFT 或纯 RL**：
   - CHORD 算法实现动态损失融合，在保持准确性的同时增强泛化能力。

3. ✅ **推理质量比任务准确率更重要**：
   - 即使多个模型输出正确答案，其推理过程仍可能存在幻觉、冗余或不一致；Mindora 在这些维度全面领先。

4. ✅ **小模型经针对性优化可超越大模型**：
   - 8B 级别的 Mindora 超越 70B 模型，表明**领域专用后训练的价值高于单纯扩大参数量**。

5. ✅ **多维度评估揭示模型真实能力**：
   - 传统 Accuracy/F1 无法反映推理缺陷，五维评估体系更贴近临床需求。

---

### **方法的局限性**

1. **依赖 GPT-4o 构建训练数据**：
   - RTG 流程使用 GPT-4o 生成和验证推理链，存在“以强训弱”的依赖风险，可能限制完全开源生态的应用。

2. **数据来源偏重英文与社交媒体**：
   - 多数数据来自 Reddit、Twitter、YouTube，文化多样性与临床严谨性有待加强。

3. **尚未在真实临床环境中部署验证**：
   - 当前评估为离线 benchmark，缺乏与真实患者交互的安全性与有效性测试。

4. **结构化格式可能限制表达灵活性**：
   - 强制 `<think>` 和模块标题可能影响自然语言流畅性，需权衡可解释性与可用性。

---

### **未来工作方向**

1. **扩展多语言与跨文化数据集**，提升全球适用性。
2. **开发完全开源的 verifier 与 trajectory generator**，摆脱对闭源模型的依赖。
3. **开展真实用户研究（User Study）**，评估模型在实际咨询场景中的帮助程度与潜在风险。
4. **集成多模态输入**（如语音、表情），实现更全面的心理状态感知。
5. **探索主动干预与长期陪伴机制**，支持持续心理健康管理。

---

> **总结**：  
> MentraSuite 通过 **MentraBench + Mindora + RTG** 三位一体设计，首次系统性地推动 LLM 在心理健康领域的**可靠推理能力**发展。其实验充分证明：**结构化训练、一致性奖励、多维评估**是构建可信心理AI的关键路径，为“Web for Good”愿景提供了坚实的技术支撑。

</details>

---

### 12. [WarmServe: Enabling One-for-Many GPU Prewarming for Multi-LLM Serving](https://arxiv.org/abs/2512.09472)

**Authors**: Chiheng Lou, Sheng Qi, Rui Kang, Yong Zhang, Chen Sun, Pengcheng Wang, Bingyang Liu, Xuanzhe Liu, Xin Jin  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09472v1  

#### Abstract
Deploying multiple models within shared GPU clusters is promising for improving resource efficiency in large language model (LLM) serving. Existing multi-LLM serving systems optimize GPU utilization at the cost of worse inference performance, especially time-to-first-token (TTFT). We identify the ro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# WarmServe: Enabling One-for-Many GPU Prewarming for Multi-LLM Serving —— 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在多LLM（Large Language Model）服务场景中，现有系统面临**推理延迟高**（尤其是 Time-to-First-Token, TTFT）与**资源利用率低**之间的权衡问题：

- **Autoscaling 系统**：通过动态创建/销毁实例提高 GPU 利用率，但在请求突发时需现场加载模型，导致显著冷启动延迟。
- **GPU Sharing 系统**：将多个模型共置在同一组 GPU 上以提升资源效率，但会限制每个模型的 KV Cache 空间，并引入性能干扰。

根本原因在于这些方法**缺乏对未来工作负载的预知能力**，无法提前准备资源。

---

### **提出了什么新方法或新思路**

论文提出 **WarmServe**，一个基于“**One-for-Many GPU Prewarming**”的新型多LLM服务系统，其核心创新包括：

#### ✅ **Universal GPU Workers（通用GPU工作者）**
- 将空闲 GPU 预先加载多个不同模型的部分权重（如前几层），使其成为“通用GPU工作者”。
- 当某模型出现流量高峰时，可快速将其转换为专用 GPU 工作者（dedicated worker），实现秒级甚至亚秒级冷启动。

> 这种设计实现了“一个GPU为多个模型预热”的目标，极大提升了预热资源的复用率。

#### ✅ **Evict-Aware Model Placement Strategy（驱逐感知的模型放置策略）**
- 为了避免多个预热模型之间因部分重叠而导致“一个命中、其余全废”的干扰问题，提出：
  - 模型间的 GPU 集合必须满足：**完全不相交** 或 **嵌套关系**（即一个集合是另一个的子集）。
- 此策略最小化了预热冲突，保障高优先级模型的预热成功率。

#### ✅ **Proactive Prewarming（主动预热）**
- 在 autoscaler 发出缩容信号后、实例真正终止前的 **grace period** 内，利用该实例尚未使用的 KV Cache 空间，预先加载其他模型的权重。
- 实例释放后，GPU 可立即转为 universal worker，无需额外加载时间。

#### ✅ **Zero-Overhead Memory Switching（零开销内存切换机制）**
- 借助 CUDA VMM API 实现虚拟内存页表的灵活映射。
- 支持在 serving model weights、prewarming weights 和 KV Cache 之间无缝切换，且所有操作与数据传输并行执行，避免引入额外延迟。

---

### **相比现有方法的优势**

| 维度 | Autoscaling 系统 | GPU Sharing 系统 | WarmServe |
|------|------------------|------------------|-----------|
| TTFT 性能 | 高延迟（冷启动） | 中等（无冷启，但排队严重） | 极低（预热完成即刻响应） |
| 资源利用率 | 较高 | 高 | 高（通过 one-for-many 提升复用） |
| 推理性能（TPOT） | 优秀（独占GPU） | 差（共享导致降速） | 优秀（运行时仍独占GPU） |
| 扩展性 | 动态扩展 | 固定配置 | 动态+预测驱动 |

> WarmServe 成功融合了 autoscaling 的高性能与 GPU sharing 的高效资源利用优势。

---

## 2. 核心实验方法和设置

### **使用了哪些数据集**

- **AzureConv** 和 **AzureCode**：来自真实生产环境的 LLM 请求 trace 数据集。
  - 包含输入/输出 token 数量、请求到达时间等信息。
  - 具有明显的周期性和长期可预测性（论文验证预测准确率达 ~93%）。

---

### **实验设置**

#### **硬件平台**
- 两台服务器，每台配备：
  - 8 × GPU（FP16算力约2K TFLOPS）
  - PCIe 5.0 x16（带宽128 GB/s）用于主机到GPU的数据加载
  - NVLink 4.0（400 GB/s）连接同机内GPU
  - RDMA NICs（8×200Gbps）用于跨机通信

#### **模型配置**
| Model | Size (GB) | Required GPUs |
|-------|-----------|---------------|
| Llama2-7B | 12.55 | 1 |
| Llama2-13B | 24.24 | 2 |
| Llama2-70B | 128.49 | 4 |

> 使用 Llama2 系列模型（FP16精度），并复制 Llama2-7B 以增加调度频率。

#### **请求生成方式**
- 请求速率服从幂律分布（power-law distribution），指数参数 α 控制突发程度（α=0.5 表示更突发）。
- 请求总量由 RPS（Requests Per Second）控制。

---

### **评估指标**

| 指标 | 定义 |
|------|------|
| **TTFT**（Time-to-First-Token） | 从提交请求到收到第一个输出 token 的端到端延迟 |
| **TPOT**（Time-Per-Output-Token） | 生成后续每个 token 的平均耗时 |
| **Prewarming Hit Ratio** | 成功从预热状态启动实例的比例 |
| **Prediction Accuracy** | 工作负载预测的相对误差（Relative Error） |

---

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **SLLM-GPU**（ServerlessLLM-GPU） | 基于缓存机制的 autoscaling 系统，在 GPU 中保留已卸载模型的权重 |
| **MuxServe** | GPU sharing 系统，支持时空复用，使用 CUDA MPS 隔离任务 |
| **WarmServe w/o Proac.** | WarmServe 关闭主动预热功能的变体（消融实验） |

> 所有系统均基于 vLLM 实现，确保公平比较。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 🔹 **预热效果分析（Fig. 8）**
- 成功预热下，WarmServe 可将 **TTFT 缩短 29.8×–54.8×**。
- 对于 Llama2-70B 模型，首次 token 生成可在 **~665ms** 内完成（原系统 >30s）。

#### 🔹 **端到端 TTFT 性能（Fig. 9 & 10）**
- 在 RPS=25, α=0.5 场景下：
  - WarmServe 相比 SLLM-GPU：
    - **P95 TTFT 最多降低 10.06×**
    - **P99 TTFT 最多降低 50.79×（≈50.8×）**
  - 相比自身关闭主动预热版本，尾部延迟降低 **1.03×–32.87×**

#### 🔹 **吞吐能力对比**
- WarmServe 能处理 **最多 2.5× 更多请求** 而不造成严重排队，远超 MuxServe。

#### 🔹 **预热命中率（Fig. 11）**
- 在轻负载下命中率接近 100%
- 即使在 RPS=25 的高压场景下，平均命中率仍达 **82%**

#### 🔹 **推理性能（TPOT）**
- MuxServe 因共享导致性能下降明显：
  - 在 RPS=25 下，超过 40% 请求的 TPOT >100ms
  - WarmServe 超过 60% 请求 TPOT <50ms
- 图 15 显示，MuxServe 平均 TPOT 是 WarmServe 的 **3.26×**

#### 🔹 **工作负载预测准确性（Fig. 16）**
- 使用 CSP（Corrective Seasonal Predictor）进行 5 分钟窗口预测：
  - **平均负载预测误差：5.25%**
  - **峰值负载预测误差：7.34%**
- 在更难预测的 AzureCode 上，误差分别为 9.58% 和 11.16%，但仍足够指导预热决策。

---

### **消融实验结果（Ablation Study）**

| 设置 | 结果影响 |
|------|---------|
| **禁用 Evict-Aware Placement** | 导致 0.29× 更少请求在 100ms 内获得首 token |
| **禁用 Proactive Prewarming** | 导致 0.88× 更少请求在 100ms 内获得首 token |
| **窗口大小 W=3min / 40min** | 性能分别仅为默认（5min）的 0.46× 和 0.30× |
| **W=10min** | 性能与 5min 相当（1.02×），说明系统对窗口选择具有一定鲁棒性 |

> 证明两个核心机制都至关重要。

---

## 4. 关键结论和发现

### **主要发现**

1. **LLM 工作负载具有高度周期性和长期可预测性**，可用于指导资源预配置。
2. **One-for-Many GPU Prewarming 是可行且高效的**，能在不牺牲推理性能的前提下大幅降低 TTFT。
3. **Universal GPU Workers 设计有效提升了预热资源的复用率**，显著减少所需预留 GPU 数量。
4. **Proactive Prewarming 利用了 grace period 的闲置资源**，解决了“来不及预热”的难题。
5. **Evict-Aware Placement 显著降低了预热干扰**，提高了整体预热成功率。
6. **WarmServe 在保持独占 GPU 推理优势的同时，兼具高资源利用率和低延迟响应能力**。

---

### **方法的局限性**

1. **依赖历史数据的周期性假设**：若工作负载突变或缺乏规律（如突发事件），预测失效会影响预热效果。
2. **大规模集群扩展性待验证**：当前实验仅在双节点 16 GPU 规模测试，更大规模下的协调开销未充分评估。
3. **模型越大越难预热**：虽然只预热前几层，但对于千亿级以上模型，仍可能受限于 PCIe 带宽和内存容量。
4. **未考虑异构硬件环境**：假设所有 GPU 同构，实际部署中可能存在混合型号问题。

---

### **未来工作方向**

1. **结合在线学习优化预测器**：采用 LSTM、Transformer 等深度模型进一步提升负载预测精度。
2. **支持跨服务器预热**：探索分布式预热机制，打破单机内存限制。
3. **细粒度分层预热策略**：根据不同层计算特性决定预热顺序和范围。
4. **集成成本模型**：在云环境中平衡预热带来的能耗/成本与用户体验增益。
5. **适配 MoE 架构**：研究稀疏激活模型下的预热策略，仅预热常用专家模块。

---

## ✅ 总结

**WarmServe** 是首个将 **one-for-many prewarming** 思想成功应用于多LLM服务系统的方案。它通过 **universal GPU workers + proactive prewarming + evict-aware placement + zero-overhead memory switching** 四大技术支柱，在几乎不影响推理性能的前提下，实现了高达 **50.8× 的 TTFT 改进** 和 **2.5× 的吞吐提升**，为构建高效、低延迟的大规模 LLM 服务平台提供了重要范式。

</details>

---

### 13. [Training One Model to Master Cross-Level Agentic Actions via Reinforcement Learning](https://arxiv.org/abs/2512.09706)

**Authors**: Kaichen He, Zihao Wang, Muyao Li, Anji Liu, Yitao Liang  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09706v1  

#### Abstract
The paradigm of agentic AI is shifting from engineered complex workflows to post-training native models. However, existing agents are typically confined to static, predefined action spaces--such as exclusively using APIs, GUI events, or robotic commands. This rigidity limits their adaptability in dy...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Training One Model to Master Cross-Level Agentic Actions via Reinforcement Learning*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 **Agentic AI** 模型通常被限制在静态、预定义的 **action space** 中，例如仅使用 API 调用、GUI 事件或机器人控制命令。这种刚性设计导致模型在动态环境中适应能力差，无法根据任务上下文灵活选择最优交互粒度。

此外，现有方法往往依赖人工设计的规则来切换不同 action space，缺乏自主决策能力，且难以泛化到复杂、长周期的任务中。

---

### **提出了什么新方法或新思路**

本文提出 **CrossAgent**，一个统一的原生智能体模型，能够：

- **掌握异构 action spaces**（heterogeneous action spaces），如 Motion、Grounding、Raw 等；
- **自主选择每一步最合适的 action space**，无需人工指定规则；
- 在单个轨迹中实现跨层级动作切换，平衡高层效率与底层精度。

为此，作者设计了一个三阶段训练流水线：

1. **Cold-Start Supervised Fine-Tuning (SFT)**  
   在混合多 action space 数据上进行监督微调，使模型具备解码多种动作的能力。

2. **Single-Turn Reinforcement Learning (STRL)**  
   使用 **Group Relative Policy Optimization (GRPO)** 算法优化单步动作空间选择策略，鼓励模型选择能正确执行任务的动作形式。

3. **Multi-Turn Reinforcement Learning (MTRL)**  
   进一步优化整个任务轨迹的成功率，并引入 token 成本惩罚项 $ \lambda \cdot l_e(t) $，促使模型优先选择高效简洁的动作空间（如高阶 API）而非冗长的原始指令。

---

### **相比现有方法的优势**

| 维度 | 传统方法 | CrossAgent |
|------|--------|-----------|
| **Action Space 设计** | 固定单一空间（如仅 GUI 或仅 API） | 动态切换多个异构空间 |
| **切换机制** | 手工规则或流程编排 | 自主学习、上下文感知 |
| **训练方式** | 多为 SFT 或简单 RL | 分阶段强化学习（STRL + MTRL） |
| **泛化能力** | 局限于特定任务类别 | 在 >800 个未见任务上表现优异 |
| **效率与鲁棒性** | 高层动作可能失败，低层动作效率低 | 自动权衡“成功率”与“执行成本” |

> ✅ **核心优势**：将 action space 的选择本身作为可学习的部分，而非固定约束。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **环境平台**：`Minecraft`（版本 1.16.5），通过 `MineRL` 框架提供基于图像的观察输入（RGB 640×360）和键盘/鼠标控制接口。
- **基准测试套件**：`OpenHA benchmark`，包含 **超过 800 个手动设计并验证的任务**，涵盖三大类：
  - **Mine Blocks**：资源采集类任务（如砍树）
  - **Kill Entities**：战斗生存类任务（如杀羊）
  - **Craft Items**：GUI 密集型合成任务（如制作附魔台）

- **训练数据**：
  - 初始 SFT 使用混合 action space 数据集 `D_mix`，结合 VPT 数据与人工标注轨迹；
  - MTRL 阶段仅使用 **30 个训练任务**（每类 10 个），用于在线 RL 微调。

---

### **实验设置和评估指标**

#### **评估指标**

| 指标 | 含义 |
|------|------|
| **Finished Tasks (FT)** ↑ | 至少成功完成一次的任务比例，衡量覆盖广度 |
| **Average Success Rate (ASR)** ↑ | 所有任务上的平均成功率，反映稳定性 |
| **Success Rate on ID/OOD Tasks** | 区分训练内（In-Distribution）与分布外（Out-of-Distribution）性能，检验泛化能力 |

#### **训练配置**

- **基础模型**：`Qwen2-VL-7B-Instruct`，具备强大视觉语言理解能力。
- **训练硬件**：8×NVIDIA A800-SXM4-80GB GPU
- **优化器**：AdamW
- **RL 算法**：**GRPO**（Group Relative Policy Optimization），无需价值网络，利用组内相对表现估计优势函数，更稳定高效。

---

### **基线方法对比**

| 类别 | 基线模型 |
|------|--------|
| **单 action space 模型** | `RawHA`, `MotionHA`, `GroundingHA`, `LatentHA`, `LanguageHA` |
| **Hierarchical Agent** | `OpenHA`, `UI-TARS-1.5`, `Game-TARS` |
| **专用策略模型** | `VPT`, `ROCKET-1`, `STEVE-1`, `JARVIS-VLA` |

这些基线要么局限于某一 action space，要么采用固定结构组合，缺乏动态适应能力。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1）**

| 方法 | Mine Blocks (ASR) | Kill Entities (ASR) | Craft Items (ASR) | All Tasks (ASR) |
|------|------------------|--------------------|------------------|----------------|
| **VPT** | 6.0±11.4 | 3.6±7.7 | 0.8±3.3 | 3.5±8.4 |
| **ROCKET-1** | 18.9±24.3 | 27.9±29.3 | 0.0±0.0 | 15.6±24.9 |
| **JARVIS-VLA** | 30.0±35.4 | 18.5±22.7 | 25.1±23.9 | 24.5±28.4 |
| **OpenHA** | 30.1±13.9 | 32.5±9.2 | 31.9±13.7 | 31.5±12.5 |
| **CrossAgent (w/o STRL)** | 39.0±46.5 | 27.7±43.9 | 58.0±48.4 | 41.6±47.9 |
| **CrossAgent (ours)** | **40.0±48.3** | **45.1±43.5** | **78.8±41.0** | **54.6±47.6** |

> 🔴 表示 SOTA，🔵 表示第二好；CrossAgent 在所有类别均达到或接近最佳。

---

### **与基线方法的对比结果**

- CrossAgent 显著优于所有单 action space 基线，尤其在 **Craft Items** 上提升明显（+47% ASR vs OpenHA）。
- 相比最强的 hierarchical agent（如 OpenHA），CrossAgent 在 **Kill Entities** 和 **Craft Items** 上分别提升 **12.6%** 和 **46.9%** 的 ASR。
- 尽管只在 **30 个任务上进行 RL 微调**，却能在 **800+ 个 OOD 任务上泛化良好**，证明其强大的迁移能力。

---

### **消融实验结果**

#### **(1) 是否包含 STRL 阶段的影响（Figure 4 & Table 1）**

- 移除 STRL 阶段后（即直接从 SFT 进入 MTRL），训练收敛速度变慢，最终性能下降显著：
  - Craft Items ASR 从 **78.8% → 58.0%**
  - All Tasks ASR 从 **54.6% → 41.6%**
- 结论：**STRL 是有效的“热启动”机制**，帮助模型建立初步的 action space 偏好，加速后续 MTRL 收敛。

#### **(2) 是否使用混合 action space（Figure 3）**

- 对比仅使用 `GroundingHA` 或 `MotionHA` 子集训练的模型：
  - CrossAgent 收敛更快、最终成功率更高；
  - 单 space 模型容易陷入局部最优，无法应对多样化任务需求。
- 结论：**异构 action space 提供更大灵活性，提升数据效率和泛化能力**。

#### **(3) OOD 泛化能力分析（Table 2）**

| 方法 | Craft Items (OOD) | All Tasks (OOD) |
|------|------------------|----------------|
| RawHA-RL | 69.8±44.4 | 42.4±45.1 |
| GroundingHA-RL | 57.2±48.1 | 39.4±44.3 |
| **CrossAgent (w/o STRL)** | 58.0±48.4 | 39.7±48.1 |
| **CrossAgent (full)** | **78.8±41.0** | **49.1±46.6** |

- CrossAgent 在 OOD 任务上仍保持高成功率，而其他 RL 模型出现严重过拟合（ID 高但 OOD 低）；
- **CrossAgent 的 generalization gap 更小**，说明其学到的是通用策略而非记忆训练任务。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **动态 action space 切换是提升 agentic 模型性能的关键**：  
   CrossAgent 能根据任务阶段自动选择最适合的交互粒度（如探索时用 Motion，交互时用 Grounding 或 Raw），从而兼顾效率与精度。

2. ✅ **强化学习（尤其是 MTRL）对 OOD 泛化至关重要**：  
   尽管 RL 容易过拟合，但在合理设计奖励函数（成功率 + 成本惩罚）下，反而能促进策略抽象和迁移。

3. ✅ **STRL 阶段虽轻量但不可或缺**：  
   它为 MTRL 提供了一个高质量的初始策略分布，避免盲目探索，显著提升训练效率。

4. ✅ **CrossAgent 展现出类人行为模式**：  
   案例研究表明其动作切换具有语义连贯性，例如“导航→接近→交互”三阶段中逐步细化控制方式，模仿人类操作逻辑。

---

### **方法的局限性**

1. **计算开销大**：  
   MTRL 需要大量在线环境交互（>80 轮迭代，每轮 >6,400 次交互），训练成本较高。

2. **依赖高质量解析器 `g(·)`**：  
   动作语义一致性判断依赖于确定性 parser，若 parser 不准确会影响 STRL 效果。

3. **尚未迁移到物理世界**：  
   当前实验全部在 Minecraft 中完成，真实机器人场景面临安全、延迟等挑战。

4. **action space 数量有限**：  
   当前支持 3–5 种预定义空间，未来需研究如何自动发现或生成新的 action abstraction。

---

### **未来工作方向**

1. 探索更高效的 RL 算法（如 offline RL 或 sample-efficient RL）以降低训练成本；
2. 将 CrossAgent 架构迁移到真实机器人系统，解决 sim-to-real gap；
3. 引入自监督学习自动构建 latent action space，减少人工设计；
4. 扩展至多模态工具调用（API + GUI + Robot）的通用计算机助手。

---

> 📌 **一句话总结**：  
> CrossAgent 首次实现了在一个统一模型中**自主学习何时使用何种粒度的动作空间**，并通过三阶段 RL 训练框架，在 Minecraft 中实现了 SOTA 性能与卓越的 OOD 泛化能力，标志着向真正通用智能体迈出关键一步。

</details>

---

### 14. [Closing the Train-Test Gap in World Models for Gradient-Based Planning](https://arxiv.org/abs/2512.09929)

**Authors**: Arjun Parthasarathy, Nimit Kalra, Rohun Agrawal, Yann LeCun, Oumayma Bounou, Pavel Izmailov, Micah Goldblum  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09929v1  

#### Abstract
World models paired with model predictive control (MPC) can be trained offline on large-scale datasets of expert trajectories and enable generalization to a wide range of planning tasks at inference time. Compared to traditional MPC procedures, which rely on slow search algorithms or on iteratively ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Closing the Train-Test Gap in World Models for Gradient-Based Planning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**世界模型（World Models）在梯度规划（Gradient-Based Planning, GBP）中的训练-测试不一致问题（train-test gap）**。具体表现为：
- **训练目标**：世界模型通常以“下一状态预测”为目标，在专家轨迹数据上进行训练。
- **测试用途**：在推理时却用于优化动作序列以达成目标，即解决一个基于模型的规划问题。

这种目标错配导致两个核心挑战：
1. **分布外误差累积**：GBP 在优化过程中可能生成训练数据分布之外的动作序列，使模型进入未见过的状态区域，预测误差迅速累积。
2. **非光滑优化景观**：由世界模型诱导的动作损失曲面可能存在大量局部极小值或平坦区域，阻碍有效的梯度优化。

### 提出的新方法
为解决上述问题，作者提出两种新的微调策略：

#### ✅ **Online World Modeling (OWM)**
- **思想**：借鉴 DAgger 思路，通过在线交互扩展训练数据。
- **流程**：
  1. 使用 GBP 生成动作序列；
  2. 将这些动作在真实环境模拟器中执行，获得“修正轨迹”（corrected trajectory）；
  3. 将修正后的轨迹加入训练集并微调世界模型。
- **作用**：让世界模型学习到 GBP 实际访问的状态区域，缓解分布偏移。

#### ✅ **Adversarial World Modeling (AWM)**
- **思想**：通过对抗训练增强模型对扰动的鲁棒性，平滑输入空间的损失曲面。
- **流程**：
  1. 对状态 $z_t$ 和动作 $a_t$ 施加最大化预测误差的小扰动（使用 FGSM）；
  2. 在这些“对抗样本”上继续训练世界模型。
- **作用**：提升模型在关键决策边界附近的稳定性，改善 GBP 的收敛性和可靠性。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | GBP + AWM 在性能上匹配甚至超越 CEM，但计算时间减少 **10倍以上**。 |
| **泛化能力** | 显著缩小 train-test gap，提升长时域规划的准确性。 |
| **通用性** | 可应用于多种世界模型架构（如 DINO-WM、IRIS），不依赖特定结构。 |
| **无需额外采样** | 不像 CEM/MPPI 需要大量 rollout，GBP 直接反向传播优化动作。 |

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
所有实验基于以下三个控制任务，均从图像观测中学习动态模型：

| 任务 | 描述 |
|------|------|
| **PushT** | 控制机械臂推动 T 形物体至目标位置（操作任务） |
| **PointMaze** | 控制小球在迷宫中导航至目标点（导航任务） |
| **Wall** | 在双房间环境中穿门导航（复杂路径规划） |

此外还测试了更复杂的操作任务：
- **Rope**: 推动绳子到指定形状
- **Granular**: 推动颗粒群形成目标构型

> 数据来源：均为离线专家演示轨迹（imitation learning dataset），来自 DINO-WM (Zhou et al., 2025)

### 实验设置
- **基础模型**：DINO-WM —— 基于 DINOv2 视觉编码器的潜变量世界模型（latent world model）
- **编码器**：冻结的 DINOv2，提取图像特征作为 latent state $z$
- **过渡模型**：ViT 架构建模 $f_\theta(z_t, a_t) \rightarrow z_{t+1}$
- **训练方式**：最小化 latent space 中的 $\ell_2$ 预测误差

### 评估指标
| 指标 | 定义 |
|------|------|
| **Success Rate (%)** | 规划动作序列能否成功到达目标状态（开环 / MPC） |
| **Wall Clock Time** | 单次规划耗时（衡量计算效率） |
| **World Model Error (Training vs Planning)** | 比较模型在专家轨迹 vs 规划轨迹上的预测误差差异，反映 train-test gap |
| **Chamfer Distance** | 在 Rope/Granular 上衡量关键点集合之间的几何距离 |

### 基线方法对比
| 方法 | 类型 | 是否可微 |
|------|------|---------|
| **DINO-WM (Baseline)** | Latent world model + next-state prediction | 是 |
| **CEM (Cross-Entropy Method)** | 搜索式、梯度自由规划器 | 否 |
| **MPPI** | 基于采样的随机最优控制 | 否 |
| **GradCEM** | 结合梯度更新的 CEM 变体 | 是 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| 方法 | PushT (MPC) | PointMaze (MPC) | Wall (MPC) |
|------|-------------|------------------|------------|
| DINO-WM + CEM | 92% | 90% | 82% |
| **Adversarial WM + Adam GBP** | **92%** | **98%** | **94%** |
| **Speed-up** | ⬇️ >10× faster than CEM | ⬇️ >10× faster | ⬇️ >10× faster |

> 注：GBP 使用 Adam 优化器即可达到或超过 CEM 性能，且速度快一个数量级。

### 与基线方法的对比结果
- **Open-loop 设置下**：
  - AWM 平均提升成功率 +18% ~ +30%
- **MPC 设置下**：
  - AWM 超越原始 DINO-WM + CEM 在 PointMaze 和 Wall 上的表现
  - 在 PushT 上持平 CEM，但速度显著更快
- **效率方面**（Figure 3）：
  - CEM 需数百分钟完成一次规划
  - GBP + AWM 仅需几分钟内完成，实现 **实时可用**

### 消融实验结果

#### ✅ Train-Test Gap 分析（Figure 4 & 6）
- **DINO-WM**：planning 时的世界模型误差显著高于 training 时（负值），说明存在严重 gap
- **OWM / AWM**：该差距被逆转或消除，表明模型在规划路径上也能准确预测

#### ✅ 初始化网络影响（Table 6）
- 使用专门训练的初始化网络（initialization network）并未带来明显增益
- **随机初始化表现更好或相当**，说明 AWM 已有效平滑了优化地形

#### ✅ 不同对抗攻击方式比较（Table 11）
| 攻击方式 | Backward Passes/Epoch | MPC Success Rate (Wall) |
|----------|------------------------|--------------------------|
| FGSM (1-step) | 2 | 94% |
| 2-step PGD | 3 | 90% |
| 3-step PGD | 4 | 94% |

➡️ **FGSM 效率最高，性能不逊于更强攻击方法**

#### ✅ 不同世界模型架构验证（Table 8）
在 IRIS 模型上也观察到：
- OWM 失效（success rate 下降）
- **AWM 显著提升 GBP 性能（从 0 → 8%）**
➡️ 表明 AWM 更具普适性和稳定性

#### ✅ 长时域规划（Table 9a）
- Horizon 扩展至 50 步后，AWM 仍优于 baseline，体现其长期一致性优势

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Train-test gap 是 GBP 表现不佳的根本原因**  
   - 模型训练目标（next-state prediction）与使用目标（action optimization）不一致。
   
2. ✅ **Adversarial World Modeling 最有效**
   - 通过对抗扰动显式地“暴露”模型脆弱区域，显著平滑了动作优化曲面（Figure 2 展示更宽广的最优盆地）。
   - 不依赖模拟器回滚，适用于高成本或不可及的仿真场景。

3. ✅ **Online World Modeling 有效但受限**
   - 能覆盖 GBP 实际访问的状态流形，但在某些任务上效果不稳定（如 IRIS 架构失败）。
   - 依赖 simulator 进行轨迹纠正，增加计算开销（Table 10 显示 simulator rollout 比 WM rollout 慢 30–150 倍）。

4. ✅ **GBP 可以高效替代 CEM**
   - 经过 AWM 微调后，GBP 不仅能达到 CEM 级别的成功率，而且**运行速度快一个数量级以上**。
   - 特别适合需要快速响应的真实机器人系统。

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **依赖高质量预训练视觉编码器** | 当前方法建立在 DINOv2 等强大 SSL 特征之上，若编码器表达能力弱，效果会下降 |
| **对抗扰动尺度敏感** | 若扰动过大（如 $\lambda_z > 0.5$），可能破坏语义信息，反而损害性能（Figure 9） |
| **尚未在真实硬件上验证** | 所有实验均在模拟器中进行，现实中的噪声、延迟等因素未考虑 |
| **OWM 计算成本较高** | 每轮需调用 simulator rollout，限制其在资源紧张场景的应用 |

### 未来工作方向
1. **真实世界部署**：将 AWM 应用于真实机器人平台，研究其对传感器噪声和动力学不确定性的鲁棒性。
2. **结合策略学习**：将稳健的世界模型用于端到端强化学习，探索其在 long-horizon 决策中的潜力。
3. **多尺度/分层规划**：拓展至 hierarchical world models，在不同抽象层级应用 AWM 提升整体规划稳定性。
4. **防御环境对抗扰动**：利用 AWM 提升模型对物理扰动（如风力、摩擦变化）的适应能力。

---

> 🔗 **代码开源地址**：[github.com/nimitkalra/robust-world-model-planning](https://github.com/nimitkalra/robust-world-model-planning)

</details>

---

### 15. [Visual Categorization Across Minds and Models: Cognitive Analysis of Human Labeling and Neuro-Symbolic Integration](https://arxiv.org/abs/2512.09340)

**Authors**: Chethana Prasad Kabgere  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09340v1  

#### Abstract
Understanding how humans and AI systems interpret ambiguous visual stimuli offers critical insight into the nature of perception, reasoning, and decision-making. This paper examines image labeling performance across human participants and deep neural networks, focusing on low-resolution, perceptuall...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Visual Categorization Across Minds and Models: Cognitive Analysis of Human Labeling and Neuro-Symbolic Integration*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决 **人类与AI在视觉分类任务中处理模糊刺激时的认知机制差异**。具体而言，研究聚焦于低分辨率、感知退化的图像（如CIFAR-10中的小尺寸图像），探讨：
- 人类如何基于有限视觉信息进行分类决策；
- 深度神经网络（DNN）如何依赖特征提取做出预测；
- 两者在表示、推理和置信度校准上的异同。

这一问题对于构建更可解释、认知对齐（cognitively aligned）的AI系统至关重要。

### 提出的新方法与新思路
- **跨模态认知分析框架**：结合人类被试的行为数据（标签、信心、策略描述、注意力焦点等）与AI模型的内部可视化（Grad-CAM），从 Marr 的三层次理论（computational, algorithmic, implementation）出发，系统比较人脑与人工系统的视觉分类机制。
- **认知科学驱动的对比范式**：将人类行为置于 **bounded rationality**、**analogical reasoning**、**embodied cognition** 和 **distributed cognition** 等经典认知理论下进行建模，并与连接主义AI进行对照。
- **推动 neuro-symbolic integration 的设计路径**：提出未来应融合符号推理模块（如形状规则验证）与神经网络激活信号（如Grad-CAM），以提升AI的鲁棒性和可解释性。

### 相比现有方法的优势
| 维度 | 传统方法局限 | 本文优势 |
|------|--------------|---------|
| 分析视角 | 多关注性能指标（准确率）或单一可解释性工具 | 引入多维认知维度（信心、负荷、情绪、信任、解释）进行深度行为分析 |
| 可解释性 | Grad-CAM仅提供热图，缺乏语义解释 | 将AI注意力图与人类语言策略直接对比，揭示“为何”不同 |
| 理论基础 | 缺乏统一认知框架 | 明确依托 Marr’s tri-level hypothesis、Simon’s bounded rationality、PDP 框架等建立分析体系 |
| 架构启示 | 多数为纯连接主义或后期符号后处理 | 提出**实时 neuro-symbolic 集成架构**设想，实现动态反馈与逻辑校验 |

---

## 2. 核心实验方法和设置

### 数据集
- **主数据集**：`CIFAR-10`，选取其中10张低分辨率（32×32）图像作为测试样本，涵盖清晰到模糊的不同难度等级。
- 图像类别包括：airplane, automobile, bird, cat, deer 等。

### 实验设置
#### 人类参与者
- **人数**：12名参与者（便利抽样，来自课堂及同事）
- **任务流程**：
  1. 观察每张图像并给出分类标签（10类之一）
  2. 提供 **confidence rating**（1–5李克特量表）
  3. 描述所用 **strategy**（如“看起来像翅膀”、“类似停着的车”）
  4. 报告 **cognitive load**（1–5）、**focus area**（中心/顶部/底部等）、**emotion**、**trust in AI**、**enjoyment**
- 所有参与者完成CITI伦理培训。

#### AI模型
- **模型架构**：ResNet-18（针对CIFAR-10调整输入层：3×3卷积，无初始max pooling）
- **训练方式**：
  - 从零开始训练，5个epoch
  - 使用Adam优化器（lr=0.001），交叉熵损失
  - 数据增强：随机裁剪、水平翻转、归一化
- **输出内容**：
  - 预测类别
  - Softmax置信度
  - Grad-CAM热力图（用于可视化注意力区域）

### 评估指标
| 类型 | 指标 |
|------|------|
| 性能 | 准确率（Accuracy）、Softmax置信度 |
| 可解释性 | Grad-CAM注意力分布 vs. 人类报告的 focus area 和 strategy |
| 认知一致性 | 人类信心 vs. AI置信度；策略类型 vs. 注意力模式 |
| 理论映射 | 是否体现 bounded rationality、analogical reasoning、embodied cognition 等原则 |

### 基线方法对比
- **AI Baseline**：标准ResNet-18 + Grad-CAM
- **人类 Baseline**：自身行为作为“认知合理”的参照系
- 对比重点不是“谁更准”，而是“为何不同”——即机制层面的差异。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | 数值 |
|------|------|
| **人类准确率** | **100%**（所有10张图像全部正确标注） |
| **人类平均信心** | 4.42 – 4.92 / 5（极高，尤其对清晰图像） |
| **AI测试准确率** | ~70.7%（ResNet-18 on CIFAR-10） |
| **AI置信度范围** | 0.329 – 0.957（波动大，部分错误预测仍具中等置信） |

### 与基线方法的对比结果
| 对比维度 | 人类表现 | AI表现 | 差异说明 |
|--------|--------|-------|--------|
| **分类准确性** | 完美（100%） | 中等（~70.7%） | 人类利用先验知识补偿低分辨率缺陷 |
| **置信度稳定性** | 高且稳定（SD ≤ 0.53） | 波动较大 | 人类受“感知稳定性”影响，AI受局部纹理扰动影响 |
| **推理策略** | 形状主导（Shape, Familiarity） | 局部纹理敏感（texture-driven） | 人类使用 analogical reasoning 映射原型；AI易被误导性边缘欺骗 |
| **注意力焦点** | 全局结构（如“耳朵”、“角”） | 局部高频特征（如斜边、斑点） | Grad-CAM显示AI关注非语义区域（如deer_00.png误判为airplane因顶部斜线） |
| **不确定性应对** | 启用Type 2（分析型）思维，增加认知负荷 | 无显式元认知调节机制 | 人类表现出 metacognitive monitoring 能力 |

### 典型案例分析
- **deer_00.png**：
  - AI预测为“airplane”（conf=0.369），Grad-CAM高亮顶部斜纹（误认为机翼）
  - 人类普遍识别为“deer”，依据“antler-like shape”或“forest-like fuzz”
  - 表明AI缺乏上下文与身体经验支持的推理能力
- **bird_01.png**：
  - AI正确但低置信（0.358），Grad-CAM分散
  - 人类虽也降低信心（均值4.42），但仍能通过“beak-like outline”判断
  - 显示人类具备更强的结构归纳能力

### 消融实验（间接体现）
虽然未进行正式消融实验，但通过以下方式验证机制：
- 改变图像清晰度 → 观察信心与负荷变化 → 验证 bounded rationality
- 分析 strategy 类型分布 → 验证 analogical reasoning 占主导
- 对比 Grad-CAM 与 human focus → 揭示 attention misalignment

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **人类在模糊图像分类中展现出高度稳健性与一致性**，依赖 **shape-based recognition** 和 **analogical reasoning**，而非像素级分析。
2. ❌ **AI（ResNet）严重依赖局部纹理线索**，容易因几何相似性产生语义无关的误判（如将鹿误作飞机），缺乏高层结构理解。
3. 🔍 **Grad-CAM 与人类注意力显著不一致**：AI关注局部高频特征，人类关注整体轮廓与关键部件（ears, wings, antlers）。
4. 🧠 **人类行为符合认知科学理论**：
   - **Bounded rationality**：使用启发式快速决策，节省认知资源
   - **Dual-process theory**：清晰图像触发Type 1（直觉），模糊图像启用Type 2（分析）
   - **Embodied/distributed cognition**：借助背景、常识、环境线索辅助判断
5. ⚖️ **AI缺乏元认知能力**：无法像人类那样动态调节信心或切换策略，导致在不确定情境下可靠性下降。

### 方法的局限性
| 局限 | 说明 |
|------|------|
| 样本规模小 | 仅12名参与者，可能影响统计泛化性 |
| 图像数量少 | 仅10张图像，难以覆盖全面视觉场景 |
| 模型复杂度低 | ResNet-18为轻量级模型，高级模型（如ViT）可能表现不同 |
| 缺乏控制变量 | 未严格控制图像模糊程度、类别距离等因素 |
| 语言策略主观性强 | 自由文本描述需人工编码，存在解释偏差 |

### 未来工作方向
1. **Neuro-symbolic System Development**
   - 设计混合架构：将 Grad-CAM 激活作为输入，触发 symbolic rule engine（例如：“若检测到翅膀 → 验证是否有机身”）
   - 参考 Feature-CAM 等方法改进 attention alignment

2. **Scalability and Representation Alignment**
   - 探索 graph-based embeddings 统一神经特征空间与符号原型
   - 在 CLEVR、GQA 等需要组合推理的数据集上验证抽象能力

3. **Trust, Transparency, and Human-AI Collaboration**
   - 开展用户研究，评估 neuro-symbolic 输出（含 symbolic trace）对 human trust、reliance 和协作效率的影响
   - 特别关注低质量图像下的交互决策场景

4. **Towards Neuromorphic and Context-Aware AI**
   - 引入多感官输入与经验先验（experiential priors）
   - 构建更具“embodiment”的AI系统，减少对静态纹理的过度依赖

---

> **总结一句话**：  
> 本论文通过认知科学视角揭示了人类与AI在视觉分类中的根本差异——前者基于**结构类比与具身经验**，后者陷于**局部纹理统计**，进而呼吁发展融合 symbolic reasoning 与 connectionist perception 的 **neuro-symbolic AI**，迈向更可解释、更类人的智能系统。

</details>

---

### 16. [An End-to-end Planning Framework with Agentic LLMs and PDDL](https://arxiv.org/abs/2512.09629)

**Authors**: Emanuele La Malfa, Ping Zhu, Samuele Marro, Sara Bernardini, Michael Wooldridge  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09629v1  

#### Abstract
We present an end-to-end framework for planning supported by verifiers. An orchestrator receives a human specification written in natural language and converts it into a PDDL (Planning Domain Definition Language) model, where the domain and problem are iteratively refined by sub-modules (agents) to ...

---

### 17. [Targeting Misalignment: A Conflict-Aware Framework for Reward-Model-based LLM Alignment](https://arxiv.org/abs/2512.09212)

**Authors**: Zixuan Liu, Siavash H. Khajavi, Guangkai Jiang, Xinru Liu  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09212v1  

#### Abstract
Reward-model-based fine-tuning is a central paradigm in aligning Large Language Models with human preferences. However, such approaches critically rely on the assumption that proxy reward models accurately reflect intended supervision, a condition often violated due to annotation noise, bias, or lim...

---

### 18. [Mitigating Social Bias in English and Urdu Language Models Using PRM-Guided Candidate Selection and Sequential Refinement](https://arxiv.org/abs/2512.09854)

**Authors**: Muneeb Ur Raheem Khan  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09854v1  

#### Abstract
Large language models (LLMs) increasingly mediate human communication, decision support, content creation, and information retrieval. Despite impressive fluency, these systems frequently produce biased or stereotypical content, especially when prompted with socially sensitive language. A growing bod...

---

### 19. [Efficient MoE Serving in the Memory-Bound Regime: Balance Activated Experts, Not Tokens](https://arxiv.org/abs/2512.09277)

**Authors**: Yanpeng Yu, Haiyue Ma, Krish Agarwal, Nicolai Oswald, Qijing Huang, Hugo Linsenmaier, Chunhui Mei, Ritchie Zhao, Ritika Borkar, Bita Darvish Rouhani, David Nellans, Ronny Krashinsky, Anurag Khandelwal  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09277v1  

#### Abstract
Expert Parallelism (EP) permits Mixture of Experts (MoE) models to scale beyond a single GPU. To address load imbalance across GPUs in EP, existing approaches aim to balance the number of tokens each GPU processes. Surprisingly, we find that this objective degrades performance rather than improving ...

---

### 20. [Passing the Baton: High Throughput Distributed Disk-Based Vector Search with BatANN](https://arxiv.org/abs/2512.09331)

**Authors**: Nam Anh Dang (Cornell University), Ben Landrum (Cornell University), Ken Birman (Cornell University)  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09331v1  

#### Abstract
Vector search underpins modern information-retrieval systems, including retrieval-augmented generation (RAG) pipelines and search engines over unstructured text and images. As datasets scale to billions of vectors, disk-based vector search has emerged as a practical solution. However, looking to the...

---

### 21. [Comparing AI Agents to Cybersecurity Professionals in Real-World Penetration Testing](https://arxiv.org/abs/2512.09882)

**Authors**: Justin W. Lin, Eliot Krzysztof Jones, Donovan Julian Jasper, Ethan Jun-shen Ho, Anna Wu, Arnold Tianyi Yang, Neil Perry, Andy Zou, Matt Fredrikson, J. Zico Kolter, Percy Liang, Dan Boneh, Daniel E. Ho  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.09882v1  

#### Abstract
We present the first comprehensive evaluation of AI agents against human cybersecurity professionals in a live enterprise environment. We evaluate ten cybersecurity professionals alongside six existing AI agents and ARTEMIS, our new agent scaffold, on a large university network consisting of ~8,000 ...

---

### 22. [Contrast transfer functions help quantify neural network out-of-distribution generalization in HRTEM](https://arxiv.org/abs/2512.09067)

**Authors**: Luis Rangel DaCosta, Mary C. Scott  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.09067v1  

#### Abstract
Neural networks, while effective for tackling many challenging scientific tasks, are not known to perform well out-of-distribution (OOD), i.e., within domains which differ from their training data. Understanding neural network OOD generalization is paramount to their successful deployment in experim...

---

### 23. [Improved Physics-Driven Neural Network to Solve Inverse Scattering Problems](https://arxiv.org/abs/2512.09333)

**Authors**: Yutong Du, Zicheng Liu, Bo Wu, Jingwei Kou, Hang Li, Changyou Li, Yali Zong, Bo Qi  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.09333v1  

#### Abstract
This paper presents an improved physics-driven neural network (IPDNN) framework for solving electromagnetic inverse scattering problems (ISPs). A new Gaussian-localized oscillation-suppressing window (GLOW) activation function is introduced to stabilize convergence and enable a lightweight yet accur...

---

### 24. [Physics-Aware Heterogeneous GNN Architecture for Real-Time BESS Optimization in Unbalanced Distribution Systems](https://arxiv.org/abs/2512.09780)

**Authors**: Aoxiang Ma, Salah Ghamizi, Jun Cao, Pedro Rodriguez  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.09780v1  

#### Abstract
Battery energy storage systems (BESS) have become increasingly vital in three-phase unbalanced distribution grids for maintaining voltage stability and enabling optimal dispatch. However, existing deep learning approaches often lack explicit three-phase representation, making it difficult to accurat...

---

### 25. [Knowledge Diversion for Efficient Morphology Control and Policy Transfer](https://arxiv.org/abs/2512.09796)

**Authors**: Fu Feng, Ruixiao Shi, Yucheng Xie, Jianlu Shen, Jing Wang, Xin Geng  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.09796v1  

#### Abstract
Universal morphology control aims to learn a universal policy that generalizes across heterogeneous agent morphologies, with Transformer-based controllers emerging as a popular choice. However, such architectures incur substantial computational costs, resulting in high deployment overhead, and exist...

---

### 26. [Gaussian Process Aggregation for Root-Parallel Monte Carlo Tree Search with Continuous Actions](https://arxiv.org/abs/2512.09727)

**Authors**: Junlin Xiao, Victor-Alexandru Darvariu, Bruno Lacerda, Nick Hawes  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.09727v1  

#### Abstract
Monte Carlo Tree Search is a cornerstone algorithm for online planning, and its root-parallel variant is widely used when wall clock time is limited but best performance is desired. In environments with continuous action spaces, how to best aggregate statistics from different threads is an important...

---

### 27. [A Distributed Framework for Privacy-Enhanced Vision Transformers on the Edge](https://arxiv.org/abs/2512.09309)

**Authors**: Zihao Ding, Mufeng Zhu, Zhongze Tang, Sheng Wei, Yao Liu  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.09309v1  

#### Abstract
Nowadays, visual intelligence tools have become ubiquitous, offering all kinds of convenience and possibilities. However, these tools have high computational requirements that exceed the capabilities of resource-constrained mobile and wearable devices. While offloading visual data to the cloud is a ...

---

### 28. [PHWSOA: A Pareto-based Hybrid Whale-Seagull Scheduling for Multi-Objective Tasks in Cloud Computing](https://arxiv.org/abs/2512.09568)

**Authors**: Zhi Zhao, Hang Xiao, Wei Rang  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.09568v1  

#### Abstract
Task scheduling is a critical research challenge in cloud computing, a transformative technology widely adopted across industries. Although numerous scheduling solutions exist, they predominantly optimize singular or limited metrics such as execution time or resource utilization often neglecting the...

---

### 29. [SynthPix: A lightspeed PIV images generator](https://arxiv.org/abs/2512.09664)

**Authors**: Antonio Terpin, Alan Bonomi, Francesco Banelli, Raffaello D'Andrea  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.09664v1  

#### Abstract
We describe SynthPix, a synthetic image generator for Particle Image Velocimetry (PIV) with a focus on performance and parallelism on accelerators, implemented in JAX. SynthPix supports the same configuration parameters as existing tools but achieves a throughput several orders of magnitude higher i...

---

### 30. [GS-KAN: Parameter-Efficient Kolmogorov-Arnold Networks via Sprecher-Type Shared Basis Functions](https://arxiv.org/abs/2512.09084)

**Authors**: Oscar Eliasson  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.09084v1  

#### Abstract
The Kolmogorov-Arnold representation theorem offers a theoretical alternative to Multi-Layer Perceptrons (MLPs) by placing learnable univariate functions on edges rather than nodes. While recent implementations such as Kolmogorov-Arnold Networks (KANs) demonstrate high approximation capabilities, th...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
