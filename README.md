# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-29 06:01:44 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Accelerate Speculative Decoding with Sparse Computation in Verification](https://arxiv.org/abs/2512.21911)

**Authors**: Jikai Wang, Jianchao Tan, Yuxuan Hu, Jiayu Qin, Yerui Sun, Yuchen Xie, Xunliang Cai, Juntao Li, Min Zhang  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.21911v1  

#### Abstract
Speculative decoding accelerates autoregressive language model inference by verifying multiple draft tokens in parallel. However, the verification stage often becomes the dominant computational bottleneck, especially for long-context inputs and mixture-of-experts (MoE) models. Existing sparsificatio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerate Speculative Decoding with Sparse Computation in Verification

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对 **Speculative Decoding** 中的**验证阶段（verification stage）成为计算瓶颈**的问题。尽管 Speculative Decoding 能通过并行验证多个 draft token 加速生成，但当输入上下文变长或使用 MoE 模型时，验证过程中的全注意力、密集 FFN 和多专家计算开销巨大，导致效率提升受限。

此外，现有的稀疏化方法（如 Sparse Attention、Sparse FFN、MoE Skipping）主要面向标准自回归解码设计，未考虑多 token 并行验证的特性，直接应用可能导致精度下降或冗余未被充分消除。

---

### **提出了什么新方法或新思路**

作者提出了一套**统一的稀疏验证框架（sparse verification framework）**，在验证阶段对三个关键模块进行联合稀疏化，并引入跨层重用机制以进一步减少冗余：

1. **Sparse Attention**  
   - 基于第一个 draft token 的 query 进行重要性评分，选择 top-N KV blocks 参与 attention。
   - 引入 **piecewise budget control**：短序列不剪枝，长序列按比例控制保留块数，平衡效率与稳定性。
   - 保留首尾 blocks 以防止丢失关键上下文（attention sink & locality bias）。

2. **Inter-layer Retrieval Reuse**  
   - 观察到中深层 Transformer 层间 block 检索高度相似。
   - 设计 **anchor layers**，仅在这些层执行检索，其余层复用最近前驱 anchor 的结果，显著减少重复检索开销。

3. **Sparse Feedforward Network (SFFN)**  
   - 在验证时跳过激活值低于阈值 $T$ 的 FFN 通道。
   - 仅对活跃通道执行 up-projection 和 down-projection，降低矩阵乘法量。

4. **Sparse Mixture-of-Experts (SMoE)**  
   - 扩展动态专家跳过机制至任意激活专家数 $k > 2$。
   - 定义每层可跳过的最大专家数 $m$，基于路由权重比值设定跳过阈值 $\beta_m$，实现自适应跳过低贡献专家。

5. **Hybrid Sparse Method**  
   - 将上述三种稀疏策略组合，在 attention、FFN、MoE 三个正交维度同时稀疏化，形成综合加速方案。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **适用性** | 不需重新训练或修改模型结构，纯推理时优化，适用于 off-the-shelf LLMs |
| **针对性** | 针对 speculative decoding 多 token 验证场景定制，而非简单移植标准稀疏方法 |
| **系统性** | 同时覆盖 attention、FFN、MoE 三大计算瓶颈，提供统一框架 |
| **高效性** | 引入 inter-layer reuse 显著减少冗余检索，无需额外参数 |
| **灵活性** | 支持不同稀疏强度调节（如 `Lo`, `T`, `m`），可在精度与效率间灵活权衡 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验基于 **LongBench** 基准，涵盖多种任务类型：

- **Summarization**: GovReport
- **Question Answering**: 2WikiMQA, HotpotQA
- **Code Editing**: LCC, RepoBench-P
- **Mathematical Reasoning**: GSM8K, Math, CollegeMath

---

### **实验设置**

| 项目 | 设置说明 |
|------|---------|
| **Target Models** | Llama3.1-8B-Instruct（SA）、Qwen3-30B-A3B（SFFN）、Deepseek-R1（SMoE & Hybrid） |
| **Draft Model** | EAGLE-3 或 MTP heads |
| **Draft Structure** | Tree-structured，共 60 个候选 token |
| **硬件平台** | 8 × NVIDIA H800-80G GPUs |
| **稀疏参数搜索范围** | <br>- SA: `Lo ∈ {1K, 2K, 4K}`<br>- SFFN: `T ∈ {0.01, 0.05, 0.1}`<br>- SMoE: `m ∈ {2,3,4}` |

---

### **评估指标**

| 指标 | 含义 |
|------|------|
| **ROUGE / F1 / Acc.** | 任务性能指标（summarization, QA, math） |
| **Sparsity (sa/sf/se)** | 注意力/FFN/MoE 的平均稀疏度 |
| **Mean Acceptance Length ($\bar{o}$)** | 衡量 draft 与 target 对齐程度的关键效率指标 |
| **FLOPs Reduction** | 理论计算量下降（见 Table 1） |

---

### **基线方法对比**

- **Strict**: 标准 speculative decoding，无任何稀疏化（full attention + dense FFN + full MoE）
- **SA / SFFN / SMoE**: 单独启用对应稀疏模块
- **SA***: SA + inter-layer retrieval reuse
- **Hybrid**: 三者联合稀疏（SA + SFFN + SMoE）

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

#### ✅ **Sparse Attention (Table 2)**

| 方法 | Sparsity (sa) | GovReport (ROUGE) | 2WikiMQA (F1) | HotpotQA (F1) |
|------|-------------|------------------|--------------|--------------|
| Strict | 0 | 34.18 | 39.20 | 47.66 |
| SA (Lo=4K) | 0.34 | 33.50 (-0.68) | 39.32 (+0.12) | 47.43 (-0.23) |
| SA* (Lo=4K) | 0.34 | 33.28 (-0.90) | 35.78 (-3.42) | 44.78 (-2.88) |

> 🔍 发现：适度稀疏下性能几乎不变；加入 inter-layer reuse 后 QA 类任务略有下降，但长文本摘要更稳定。

---

#### ✅ **Sparse FFN (Table 3)**

| 方法 | Sparsity (sf) | GovReport (ROUGE) | 2WikiMQA (F1) | GSM8K (Acc.) |
|------|-------------|------------------|--------------|-------------|
| Strict | 0 | 32.67 | 43.93 | 90.0 |
| SFFN (T=0.1) | 0.64 | **33.51 (+0.84)** | 41.96 (-1.97) | 91.0 (+1.0) |

> 🔍 发现：即使稀疏率达 64%，性能仍保持稳健，部分任务甚至轻微提升，表明验证阶段存在大量冗余激活。

---

#### ✅ **Sparse MoE (Table 4)**

| 方法 | Sparsity (se) | 2WikiMQA (F1) | Math (Acc.) | CollegeMath (Acc.) |
|------|-------------|--------------|------------|-------------------|
| Strict | 0 | 77.39 | 82.0 | 58.0 |
| SMoE (m=3) | 0.16 | **78.52 (+1.13)** | **87.0 (+5.0)** | 57.0 (-1.0) |
| SMoE (m=4) | 0.22 | 78.88 (+1.49) | 75.0 (-7.0) | 52.0 (-6.0) |

> 🔍 发现：适度跳过专家（m=3）可去噪提效；过度跳过（m=4）损害复杂数学推理能力。

---

#### ✅ **Hybrid 方法 (Table 5)**

| 方法 | ROUGE (GovReport) | F1 (2WikiMQA) | Acc. (GSM8K) | $\bar{o}$ (均接受长度) |
|------|------------------|--------------|-------------|------------------------|
| Strict | 26.90 | 77.39 | 98.0 | 2.44 |
| Hybrid | **27.40 (+0.5)** | **79.82 (+2.43)** | 96.0 (-2.0) | 2.42 (-0.02) |

> 🔍 发现：三重稀疏叠加后，多数任务性能持平或提升，接受长度仅微降，说明分布偏移可控。

---

### **消融实验结果（隐含分析）**

- **Inter-layer reuse**：减少约 50% 检索操作（30 anchor layers / 61 total），QA 性能略损，但长文本更优。
- **Piecewise Budget Control**：避免短序列因过度剪枝导致随机性上升，保障稳定性。
- **Threshold Selection (T)**：T=0.05 是 SFFN 最佳折中点，兼顾稀疏度与精度。
- **Expert Skipping Limit (m)**：m=3 在 MoE 中表现最佳，m=4 导致 CollegeMath 显著退化。

---

## 4. 关键结论和发现

### **主要发现**

1. **验证阶段存在显著结构性冗余**  
   在 attention、FFN 激活、expert 利用等多个维度均可安全稀疏化，且不影响最终输出分布一致性。

2. **多 token 验证具有高度共享性**  
   同一步骤内多个 draft tokens 检索的 KV blocks 高度重叠（>80%），支持以首个 token 为代表进行统一检索。

3. **跨层检索模式相似性强**  
   中深层 Transformer 层间 block 选择高度一致，支持 inter-layer reuse，大幅削减冗余计算。

4. **稀疏验证可在高稀疏度下维持性能**  
   - FFN 稀疏度达 64% 时性能无明显下降
   - MoE 跳过 16% 专家反而提升部分任务表现（去噪效应）
   - Hybrid 方法实现三重压缩，总体性能稳定

5. **接受长度影响极小**  
   所有稀疏方法引起的 mean acceptance length 下降均小于 0.05，对整体吞吐影响可忽略。

---

### **方法的局限性**

| 局限 | 说明 |
|------|------|
| **对精细符号推理敏感** | 如 CollegeMath 在高稀疏下性能下降明显，表明复杂逻辑链易受干扰 |
| **依赖 KV 缓存组织方式** | block-wise 分块假设可能不适用于所有架构（如 sliding window attention） |
| **缺乏端到端训练适配** | 当前为 inference-only 方法，若结合训练可能进一步释放潜力 |
| **inter-layer reuse 可能引入误差传播** | 错误的 anchor layer 检索会影响后续所有非 anchor 层 |

---

### **未来工作方向**

1. **动态稀疏度调控机制**  
   根据输入长度、任务类型自动调整 `Lo`, `T`, `m` 参数，实现自适应稀疏。

2. **结合训练阶段稀疏化**  
   探索 joint training + sparse verification 协同优化，缓解 draft-target 分布偏移。

3. **扩展至更多模型结构**  
   应用于 vision-language models 或 decoder-only 架构中的 cross-attention 验证。

4. **硬件友好稀疏实现**  
   结合 NSA、SnapKV 等硬件对齐稀疏模式，提升实际部署效率。

5. **理论分析稀疏边界**  
   建立稀疏程度与 acceptance rate、验证准确率之间的理论关系模型。

---

> 📌 **总结一句话**：本文首次系统地将稀疏计算引入 speculative decoding 的验证阶段，提出一个无需训练、多维度协同的稀疏框架，在显著降低 FLOPs 的同时保持高性能与稳定接受长度，为大规模语言模型高效推理提供了新范式。

</details>

---

### 2. [LIME:Accelerating Collaborative Lossless LLM Inference on Memory-Constrained Edge Devices](https://arxiv.org/abs/2512.21835)

**Authors**: Mingyu Sun, Xiao Zhang, Shen Qu, Yan Li, Mengbai Xiao, Yuan Yuan, Dongxiao Yu  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.21835v1  

#### Abstract
Large language models (LLMs) have emerged as a powerful foundation for intelligent reasoning and decision-making, demonstrating substantial impact across a wide range of domains and applications. However, their massive parameter scales and substantial resource demands pose critical challenges for ef...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LIME: Accelerating Collaborative Lossless LLM Inference on Memory-Constrained Edge Devices

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在边缘设备上的部署面临**内存受限、计算能力弱、网络带宽低**等挑战。传统方法如量化（quantization）、蒸馏（distillation）和剪枝（pruning）虽然能降低资源消耗，但会带来**模型精度损失**，这在金融风控、医疗诊断等高精度要求场景中不可接受。

此外，现有的分布式推理框架（如 PipeEdge、Galaxy、TPI-LLM）通常假设每个设备有足够的内存来存储其分配的模型分片和推理过程中生成的 KV cache，但在真实边缘环境中，这一假设往往不成立。

### 提出的新方法与核心思路
作者提出 **LIME** —— 一种面向内存受限边缘设备的**协作式无损大模型推理加速系统**，其核心设计包括：

- **Interleaved Pipeline Parallelism（交错式流水线并行）**  
  将传统流水线与动态卸载（offloading）深度融合，通过多阶段交错执行，实现**卸载时间与计算和通信时间的高度重叠**，从而减少整体延迟。

- **Fine-grained Offline Allocation Scheduler（细粒度离线调度器）**  
  基于异构设备的计算、内存和加载能力，构建一个**以延迟最小化为目标的成本模型**，采用动态规划算法进行高效层分配，最大化资源利用率。

- **Online Memory Adaptation Strategy（在线内存自适应策略）**  
  包含两个子模块：
  - **Memory-aware Planner**：根据 KV cache 增长动态触发 MHA 或 MLP 模块级卸载，避免内存饱和。
  - **Bandwidth-sensitive KV Cache Transfer Protocol**：利用高内存余量设备作为目标节点（dtarget），跨设备转移 KV cache，缓解瓶颈设备压力，并适应网络波动。

- **Block-level Fine-grained Offloading（块级细粒度卸载）**  
  不再以整层为单位卸载，而是将每层拆分为 MHA 和 MLP 模块分别管理，显著减少不必要的参数传输开销。

### 相比现有方法的优势
| 维度 | LIME 的优势 |
|------|-------------|
| **准确性** | 实现 **lossless inference**，无精度损失 |
| **内存效率** | 支持设备内存不足以容纳单个模型分片 + KV cache 的极端场景 |
| **延迟优化** | 通过交错流水线和细粒度调度，显著提升计算/通信/卸载的重叠率 |
| **鲁棒性** | 在网络带宽波动和 KV cache 动态增长下仍保持高性能 |

---

## 2. 核心实验方法和设置

### 使用的模型
实验基于三个主流 LLMs，覆盖从 13B 到 70B 参数规模：
- **Llama2-13B-Instruct**
- **Qwen3-32B**
- **Llama3.3-70B-Instruct**

详细配置见 Table III。

### 实验平台与硬件环境
使用三种 NVIDIA Jetson 边缘设备构建异构测试床：
- **Jetson Xavier NX (16GB)**
- **Jetson AGX Orin (32GB)**
- **Jetson AGX Orin (64GB)**

共搭建三个实验环境（E1–E3），设备组合详见 Table IV。所有设备通过交换机连接，总带宽 1000 Mbps，使用 Linux TC 工具模拟 **100 Mbps 和 200 Mbps** 的边带宽条件。

### 请求模式
考虑两种典型边缘请求模式：
- **Sporadic Request Pattern**：微批次大小为 1，模拟零星请求（如语音助手）
- **Bursty Request Pattern**：多个请求同时提交，模拟批量处理

### 基线方法对比
共比较 6 种基线：
1. **Pipeline Parallelism**
2. **Pipeline + Offloading**
3. **EdgeShard**（基于 PP 的高效边缘推理框架）
4. **Galaxy**（基于 TP 的分布式推理）
5. **TPI-LLM**（专为内存受限设计的 TP 框架）
6. **TPI-LLM + Offloading**

部分基线无法运行时标记为 **OOM（Out-of-Memory）** 或 **OOT（Out-of-Time，延迟超阈值）**。

### 评估指标
- **Inference Latency (ms/token)**：主要性能指标
- **Speedup**：相对于基线的加速比
- **Ablation Study**：验证各组件有效性

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Llama3.3-70B-Instruct，E3 环境）
在四台异构 Jetson 设备上运行 LLaMA3.3-70B-Instruct 模型：

| 请求模式 | 网络带宽 | LIME 推理延迟 | 相对 SOTA 加速比 |
|----------|-----------|----------------|--------------------|
| Sporadic | 100/200 Mbps | **1501.9 ms/token** | **1.7×** |
| Bursty   | 100/200 Mbps | **423.2 ms/token**  | **3.7×** |

> 注：SOTA 基线指 Pipeline+Offloading 和 EdgeShard。

### 与其他方法的对比结果
#### 在常规内存条件下（Fig. 12–14）：
- **相比 TP-based 方法（Galaxy, TPI-LLM）**：
  - Burst 场景下加速 **3.4× ~ 28×**
  - Sporadic 场景下加速 **2.5× ~ 10.8×**
  - 原因：TP 对通信依赖强，在低带宽边缘环境下通信开销巨大。

- **相比 PP-based 方法（Pipeline+Offloading, EdgeShard）**：
  - Burst 场景下加速 **1.9× ~ 10.2×**
  - Sporadic 场景下加速 **2.5× ~ 7.6×**
  - 原因：LIME 的交错流水线和细粒度调度更优地掩盖了卸载延迟。

#### 在极低内存场景（Setting 1–3，Fig. 15–17）：
- 多数基线出现 **OOM 或 OOT**（延迟 >40s/token 或 >15s/token）
- LIME 仍可稳定运行，且延迟远低于可运行基线（如 TPI-LLM 在 sporadic 下达 OOT）
- 表明 LIME 具备更强的**内存弹性与鲁棒性**

### 消融实验结果（Table V）
验证了 LIME 各组件的重要性：

| 方法 | Sporadic 延迟 | Speedup | Bursty 延迟 | Speedup |
|------|----------------|---------|--------------|---------|
| LIME（完整） | **1501.9 ms** | 1.00× | **423.2 ms** | 1.00× |
| 缺少 KV Cache Transfer | 1748.2 ms | 0.86× | 489.0 ms | 0.87× |
| 缺少 Memory-aware Planner | 2251.7 ms | 0.67× | 614.8 ms | 0.69× |

> 结论：两个在线策略均显著影响性能，尤其是 memory-aware planner 贡献最大。

---

## 4. 关键结论和发现

### 主要发现
1. **交错式流水线（Interleaved Pipeline）是实现高效卸载重叠的关键机制**，有效解决了传统流水线中“卸载延迟无法被掩盖”的问题。
2. **块级细粒度卸载（MHA/MLP 分离）显著降低了加载开销**，优于传统的整层卸载。
3. **在线内存自适应策略使系统具备应对 KV cache 增长和网络波动的能力**，提升了实际部署中的稳定性。
4. LIME 成功实现了在**内存严重不足的边缘设备集群上运行 70B 级别 LLM 的无损推理**，且性能优于所有现有方案。

### 方法的局限性
- **依赖 SSD 存储性能**：频繁的 offloading 对本地 SSD 的读写速度有较高要求，在低端设备上可能成为瓶颈。
- **调度复杂度增加**：离线调度器虽高效，但仍需预估序列长度；在线策略增加了控制逻辑复杂性。
- **未支持训练**：当前仅针对推理场景，尚未扩展到协同训练。

### 未来工作方向
- 扩展至更多类型的并行范式（如 SP + Offloading）。
- 支持动态输入长度的更精准调度预测。
- 探索在联邦学习或移动设备群上的轻量化部署版本。
- 集成更先进的 KV cache 压缩技术（如 KVQuant）进一步降低存储压力。

---

> ✅ 总结：LIME 是首个系统性解决“**内存极度受限 + 网络带宽有限 + 无损推理**”三难困境的协作推理框架，通过**交错流水线 + 细粒度调度 + 在线自适应**三大核心技术，在真实边缘平台上实现了高达 **3.7× 的端到端加速**，为大模型落地边缘提供了可行路径。

</details>

---

### 3. [FUSCO: High-Performance Distributed Data Shuffling via Transformation-Communication Fusion](https://arxiv.org/abs/2512.22036)

**Authors**: Zhuoran Zhu, Chunyang Zhu, Hao Lin, Xu Fu, Yiming Zhou, Quanlu Zhang, Zhenhua Li, Feng Qian, Chao Yu, Boxun Li, Guohao Dai, Yu Wang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.22036v1  

#### Abstract
Large-scale Mixture-of-Experts (MoE) models rely on \emph{expert parallelism} for efficient training and inference, which splits experts across devices and necessitates distributed data shuffling to route each token to its assigned experts. However, existing communication libraries handle this shuff...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FUSCO: High-Performance Distributed Data Shuffling via Transformation-Communication Fusion

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型 **Mixture-of-Experts (MoE)** 模型在训练和推理过程中依赖 **expert parallelism**，即将专家（experts）分布在多个设备上。这导致每个 token 必须被路由到其对应的专家所在的设备，从而引发大规模的分布式数据重排（data shuffling）。然而，现有的通信库（如 NCCL、DeepEP）将数据变换（transformation）与通信（communication）分离处理，造成大量冗余的数据复制和内存重排操作，使得数据 shuffling 开销占端到端运行时间的 **22%-61%**，成为性能瓶颈。

### 提出的新方法与核心思想
提出 **FUSCO** —— 一种面向 MoE 的高效通信库，其核心思想是：**融合数据变换与通信（Transformation-Communication Fusion）**。

#### 主要创新点：
- **统一的数据抽象：Segment Descriptor**
  - 将待传输的数据（如 tokens）建模为一系列逻辑上的“段”（segments），并通过 `Segment Descriptor` 描述每个段的源地址、目标地址和大小。
  - 该描述符直接编码了数据布局转换信息，使通信过程本身就能完成数据重排，无需额外的预/后处理步骤。

- **数据融合通信引擎（Data-Fused Communication Engine, dComm）**
  - 基于 descriptor 驱动的流水线设计，在数据从 GPU 内存拷贝到 NIC 发送缓冲区的过程中，**内联执行数据重排**。
  - 利用 GPUDirect P2P 和 RDMA 技术实现高效的 intra-node 和 inter-node 数据移动，并通过切片（slice）机制隐藏 descriptor 处理开销。

- **两层通信规划器（Communication Planner）**
  - 构建 **node-level forwarder + expert-level distribution** 的两级 descriptor 结构，支持跨节点去重（deduplication）：同一 token 被多个同节点内的专家使用时，仅需跨节点发送一次。

- **在线负载均衡器（Online Load Balancer）**
  - 动态分配 forwarder GPU，避免热点，提升带宽利用率。
  - 使用轻量级贪心算法进行分组调度，复杂度低，适合毫秒级通信场景。

### 相比现有方法的优势
| 维度 | NCCL / DeepEP | FUSCO |
|------|----------------|--------|
| 数据重排 | 显式多次内存拷贝（pre/post-rearrangement） | 完全消除，融合进通信路径 |
| 跨节点通信 | 不感知 token 重复，可能多次发送相同数据 | 支持跨节点去重，减少冗余流量 |
| 通信模式 | 固定拓扑无关的 all-to-all | 拓扑感知、层级路由优化 |
| 性能可扩展性 | 随 expert parallelism 增加，shuffle 占比上升 | 显著降低 shuffle 时间占比 |
| 易集成性 | 需框架手动管理重排流程 | 提供对 NCCL 的 drop-in 替代接口 |

---

## 2. 核心实验方法和设置

### 实验平台
- **硬件配置**：8 节点集群，每节点配备：
  - 8 × NVIDIA H100 GPU（80GB HBM3）
  - NVLink（intra-node，~480 GB/s）
  - 10 × 400Gbps Mellanox ConnectX-7 NICs（inter-node，RoCE）
- **软件栈**：Ubuntu 24.04, CUDA 12.9, NCCL 2.26.3, PyTorch 2.7.0

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **NCCL** | 通用通信库 | 广泛使用的 all-to-all 实现，无 MoE 专用优化 |
| **DeepEP** | MoE 专用通信库 | 基于 NVSHMEM，支持部分去重和流水线，当前 SOTA |

### 评估任务与数据集
- **通信基准测试（Communication Benchmarks）**
  - 使用真实 MoE 推理流量（来自 DeepSeek-V3 在 ShareGPT 数据集上的 trace）
  - 控制变量构造两种极端情况：
    1. **Single-node routed**：一个 token 的所有 top-k 专家均位于同一节点 → 测试去重能力
    2. **Load-imbalanced**：人为制造 GPU 间通信负载不均 → 测试负载均衡有效性
  - 序列长度：4k, 8k, 16k, 32k
  - MoE 参数见 Table 2：EP=64, Hidden Dim=7168, Top-k=8, Num Experts=256

- **端到端任务**
  - **训练**：集成至 Megatron-LM，使用 Qwen3 和 DeepSeek-V3 模型
  - **推理**：集成至 SGLang，测量 **Time-to-First-Token (TTFT)**

### 评估指标
- 通信延迟（latency）分解：preprocessing, rearrangement, communication
- 端到端性能：
  - 训练：每 iteration 时间（speedup）
  - 推理：TTFT 加速比
- 消融实验：分别关闭 dComm、Planner、Balancer 模块

---

## 3. 主要实验结果和性能指标

### 通信基准测试结果
| 场景 | 对比对象 | 加速比（Speedup） |
|------|----------|------------------|
| Real-world traffic | vs NCCL | **1.60× ~ 1.66×** |
| | vs DeepEP | **1.13× ~ 1.34×** |
| Single-node routed | vs NCCL | **3.47× ~ 3.84×** |
| | vs DeepEP | **1.95× ~ 2.01×** |
| Load-imbalanced | vs NCCL | **1.99× ~ 2.24×** |
| | vs DeepEP | **1.29× ~ 1.42×** |

> 🔍 **关键观察**：
> - 在 single-node routed 场景下，FUSCO 表现出最大优势，说明其 **去重机制非常有效**。
> - DeepEP 也有一定去重能力，但不如 FUSCO 的两级 descriptor 设计灵活高效。
> - 小序列长度（如 4k）下 FUSCO 优势较小，因其有固定 preprocessing 开销。

### 端到端性能
| 任务 | 对比对象 | 加速比 |
|------|----------|--------|
| **Training**（Qwen & DeepSeek-V3） | vs NCCL | **1.17× ~ 1.39×** |
| | vs DeepEP | **1.10× ~ 1.19×** |
| **Inference TTFT** | vs NCCL | **1.09× ~ 1.25×** |
| | vs DeepEP | **1.06× ~ 1.16×** |

> ✅ **趋势分析**：模型规模越大，通信占比越高，FUSCO 的收益越明显。

### 消融实验（Ablation Study）
在 sequence length = 16k 下关闭各模块的结果（单位：ms）：

| 场景 | FUSCO（完整） | dComm-off | Planner-off | Balancer-off |
|------|---------------|-----------|-------------|--------------|
| Real-world | 86.8 | 119.5 (-37.7%) | 124.4 (-43.2%) | 95.1 (-8.7%) |
| Single-node routed | 40.99 | 61.47 (-33.3%) | 125.42 (-67.3%) | 42.36 (-3.2%) |
| Imbalanced | 151.3 | 219.8 (-31.2%) | 207.5 (-27.1%) | 181.5 (-16.6%) |

> 📌 **结论**：
> - **dComm 是最核心组件**，平均带来 ~30% 性能提升，主要源于消除数据重排。
> - **Planner 至关重要**，尤其在 single-node 场景下，缺少它会导致跨节点通信暴增。
> - **Balancer 在负载不均时作用显著**（最高改善 16.6%），但在均衡场景中影响小。

---

## 4. 关键结论和发现

### 主要发现
1. **MoE 中的数据 shuffling 开销主要来自“非通信”部分**：本地数据重排（rearrangement）可占总延迟的 **25%-69%**，远超传统认知。
2. **通信不应只是“搬运工”**：FUSCO 证明，通过将数据布局语义嵌入通信原语，可以在通信过程中自然完成数据重排，大幅减少冗余操作。
3. **融合优于分治**：将 transformation 与 communication 融合，不仅能减少内存拷贝，还能更好地利用现代 GPU-NIC 架构中的带宽差异和并行潜力。
4. **系统级协同设计更有效**：FUSCO 的 success 来自于 descriptor 抽象、pipelined engine、topology-aware planner 和 online balancer 的协同工作。

### 方法的局限性
- **依赖底层通信库（NCCL）**：虽然构建在 NCCL 上提升了可移植性，但也限制了对更底层协议的深度定制空间。
- **descriptor 管理有一定元数据开销**：对于极小粒度或动态变化剧烈的 routing pattern，descriptor 构造成本可能不可忽略。
- **目前主要针对 MoE 场景优化**：尽管作者认为可推广至 Vision Transformer 等结构化数据重排场景，但尚未验证其他 workload。

### 未来工作方向
- 扩展至更多分布式模式（如 tensor parallelism + expert parallelism 混合）
- 支持动态 routing（例如基于 content-aware routing 的实时调整）
- 进一步优化 descriptor 编码方式以降低元数据开销
- 探索在 CPU-GPU 异构系统或边缘设备上的轻量化部署版本

---

> 💡 **总体评价**：  
> FUSCO 提出了一种全新的“通信即计算”的视角，重新定义了分布式数据重排的设计范式。其实验充分、工程扎实，不仅在性能上显著超越 NCCL 和 DeepEP，更重要的是提出了一个**通用的融合通信架构思想**，有望启发后续在分布式 DNN 系统中的更多“fused primitive”设计。

</details>

---

### 4. [MAD-NG: Meta-Auto-Decoder Neural Galerkin Method for Solving Parametric Partial Differential Equations](https://arxiv.org/abs/2512.21633)

**Authors**: Qiuqi Li, Yiting Liu, Jin Zhao, Wencan Zhu  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.21633v1  

#### Abstract
Parametric partial differential equations (PDEs) are fundamental for modeling a wide range of physical and engineering systems influenced by uncertain or varying parameters. Traditional neural network-based solvers, such as Physics-Informed Neural Networks (PINNs) and Deep Galerkin Methods, often fa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MAD-NG: Meta-Auto-Decoder Neural Galerkin Method for Solving Parametric Partial Differential Equations 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**参数化偏微分方程**（parametric PDEs）在高维、长时间演化场景下的求解难题，尤其是传统基于神经网络的方法（如 PINNs 和 Deep Galerkin Methods）存在的以下问题：
- **泛化能力差**：对新参数配置需要重新训练；
- **时间因果性丢失**：全局时空逼近导致长期预测误差累积；
- **计算成本高**：全参数更新带来高昂的训练开销。

### 提出的新方法与思路
作者提出了一种新型框架——**Meta-Auto-Decoder Neural Galerkin Method (MAD-NGM)**，并进一步扩展为 **MAD-RSNGS**（Randomized Sparse 版本），其核心思想包括：

#### （1）两阶段求解框架（Two-stage MAD-NGM）
- **初始近似阶段**：采用 **Meta-Auto-Decoder (MAD)** 范式，利用多个样本构建非线性试函数流形（nonlinear trial manifold），通过少量迭代即可快速推断新参数下的初始网络参数。
- **时间演化阶段**：基于 NGM 框架，从已获得的初始参数出发，逐步推进时间演化，实现任意时刻的状态预测。

#### （2）随机稀疏更新策略（Randomized Sparse Updates）
- 在每个时间步仅随机选择一部分网络参数进行更新（sparse subset），显著降低每步计算量。
- 保留了物理一致性的同时提升了效率，避免了全参数更新带来的冗余计算和过拟合风险。

#### （3）空间-时间解耦机制
- 利用 **Neural Galerkin Method** 中的时间变分原理（Dirac-Frenkel variational principle），将空间逼近与时间积分分离，增强了时间演化的稳定性和物理一致性。

### 相比现有方法的优势
| 方法 | 缺陷 | MAD-NGM/MAD-RSNGS 改进 |
|------|------|------------------------|
| **PINNs / MAD-PINN** | 全局时空训练，易积累时间误差；需密集采样 | 时间解耦，逐时推进，保持因果性；减少采样需求 |
| **传统 NGM** | 每个新参数需重训练初始参数 | 引入 MAD，预训练后仅微调 latent vector 即可适应新参数 |
| **Operator Learning (e.g., FNO, DeepONet)** | 难以处理复杂几何或动态边界 | 可灵活嵌入 positional encoding 处理周期/变化域 |

> ✅ **优势总结**：兼具**高效性**（低计算开销）、**强泛化能力**（快速适应新参数）、**长时稳定性**（物理一致的序列预测）。

---

## 2. 核心实验方法和设置

### 使用的数据集与PDE模型
实验涵盖四类典型参数化 PDEs：
1. **Korteweg-de Vries (KdV) 方程**（1D，非线性色散波）
2. **Burgers 方程**（1D，含激波形成）
3. **Allen-Cahn 方程**（1D & 2D，相分离过程）
   - 包括**随时间/空间变化的参数项**
   - 涉及**随机定义域**（stochastic domain）

所有问题均引入参数不确定性（如初始条件服从随机分布、区域边界扰动等）。

### 实验设置
- **训练样本数**：80–200 个不同参数配置的样本用于预训练。
- **测试方式**：选取未见参数配置的新样本，执行 fine-tuning 后进行时间演化预测。
- **网络架构**：
  - 使用全连接神经网络（FCN），层数 1–8 不等，宽度 20–30。
  - Latent 维度：5–80（依任务而定）。
- **优化器**：
  - 预训练阶段：L-BFGS（精确收敛）
  - 微调阶段：Adam 或 L-BFGS
- **时间离散化**：Forward Euler 或 4阶 Runge-Kutta
- **稀疏更新设置**（MAD-RSNGS）：
  - 每步随机更新 450–1500 参数（总参数远大于此）

### 评估指标
- **MSE（Mean Squared Error）**：
  $$
  \text{MSE} = \frac{1}{N_{\text{test}} N} \sum_{i=1}^{N_{\text{test}}} \sum_{j=1}^{N} (\hat{u}_i(x_j,t_k) - u_i^{\text{true}}(x_j,t_k))^2
  $$
- 对比不同 fine-tuning 迭代次数（100, 400, 4000）下的精度提升。
- 报告各时间点（如 t=0.2, 0.5, 1.0）的 MSE。

### 基线方法对比
- **MAD-PINN**：基于物理信息的元学习 PINN，使用全时空残差损失。
- **MAD-NGM**：本文完整版本（全参数更新）。
- **MAD-RSNGS**：本文稀疏更新变体（不同 s 值）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ KdV 方程（Table 1）
| 数据集 | 阶段 | t=0.2 | t=0.5 | t=1.0 |
|-------|------|--------|--------|--------|
| **训练集** | 预训练 | ~6.5e-7 | ~1.5e-6 | ~1.0e-5 |
|        | 微调4000次 | **5.7e-7** | **1.2e-6** | **3.7e-5** |
| **测试集** | 预训练 | 4.95e-4 | — | — |
|        | 微调4000次 | **1.2e-6** | **1.7e-6** | **9.0e-6** |

> ✔️ 测试集误差下降约 4 个数量级，表明强大泛化能力。

#### ✅ Burgers 方程（Table 2）
| 方法 | t=0.2 | t=0.5 | t=1.0 | t=1.1 |
|------|--------|--------|--------|--------|
| **MAD-NGM** | 1.18e-6 | 3.91e-6 | 4.11e-6 | 4.33e-6 |
| **MAD-RSNGS (s=3000)** | 1.51e-6 | 6.34e-6 | 1.21e-5 | 1.41e-5 |
| **MAD-PINN** | 2.75e-4 | 5.76e-4 | 5.89e-4 | 5.94e-4 |

> 🔺 MAD-PINN 误差高出 **两个数量级以上**，且随时间增长明显（见 Figure 9）。

#### ✅ Allen-Cahn 方程（Table 4）
| 参数类型 | t=0.4 | t=1.0 | t=2.0 |
|---------|--------|--------|--------|
| 时间无关 | 2.68e-5 | 5.18e-5 | 7.35e-5 |
| 时间相关 | 5.35e-5 | 6.98e-5 | 6.78e-5 |

> ✔️ 即使参数随时间和空间变化，仍能保持良好精度。

### 与基线方法的对比结果
- **精度方面**：
  - MAD-NGM > MAD-RSNGS >> MAD-PINN
  - MAD-PINN 因忽略时间因果性，在长时预测中误差迅速累积（~10⁻⁴），而 MAD-NGM 维持在 ~10⁻⁶。
- **效率方面**（Table 3）：
  - **MAD-RSNGS 时间演化耗时仅为 MAD-NGM 的 ~10%**（例如 3.5 min vs 46.47 min）
  - 预训练和微调时间基本相同，说明加速集中在时间推进阶段。
- **内存与采样需求**：
  - MAD-PINN 需要在整个时空域采样上万点（13,029），GPU 内存压力大；
  - MAD-RSNGS 每步只需局部采样，更适用于大规模问题。

### 消融实验结果（隐含分析）
- **稀疏更新规模影响**（Table 2）：
  - 当 `s` 从 600 增加到 3000，MAD-RSNGS 精度持续提高；
  - 表明更多参数参与更新有助于逼近完整 NGM 性能。
- **fine-tuning 迭代的影响**（Table 1）：
  - 少量迭代（100次）即可大幅降低误差；
  - 达到一定迭代后趋于饱和，体现“快速适应”特性。

---

## 4. 关键结论和发现

### 主要发现
1. **MAD-NGM 成功实现了参数化 PDE 的快速泛化求解**：
   - 通过 MAD 构建共享潜流形，新样本仅需微调 latent vector 即可启动。
2. **时间解耦 + 稀疏更新 = 高效长时预测**：
   - NGM 结构保障了物理一致性；
   - 随机稀疏更新大幅降低计算负担而不牺牲太多精度。
3. **优于端到端 PINN 类方法**：
   - 特别是在长时、强非线性问题中，MAD-NGM 显著抑制误差传播。
4. **具备处理复杂几何与随机域的能力**：
   - 通过 positional embedding 和 master domain 设计，支持拓扑变化的求解域。

### 方法的局限性
- **依赖高质量预训练数据**：若训练样本覆盖不足，泛化可能受限。
- **超参数敏感性**：如 latent 维度、稀疏比例 `s`、学习率等需仔细调整。
- **理论保证有限**：当前缺乏关于稀疏更新下误差传播的严格数学分析。
- **尚未验证极端高维问题**（如 >10 维），实际扩展性有待检验。

### 未来工作方向
1. **自适应网络设计**：根据问题复杂度自动调节网络宽度/深度。
2. **多层级优化策略**：结合 coarse-to-fine 时间步长与参数更新机制。
3. **实时仿真应用**：部署于数字孪生、控制反馈系统等工业场景。
4. **与其他算子学习方法融合**：探索 MAD 与 FNO/CNO 的结合潜力。
5. **不确定性量化增强**：集成贝叶斯推理或生成模型以输出置信区间。

---

> 📌 **总体评价**：  
> 本文提出的 **MAD-NGM** 是一种极具前景的参数化 PDE 求解范式，它巧妙地融合了 **meta-learning、space-time decoupling 与 randomized sparse update**，在精度、效率与泛化之间取得了良好平衡，为科学机器学习中的动态系统建模提供了新工具。

</details>

---

### 5. [nncase: An End-to-End Compiler for Efficient LLM Deployment on Heterogeneous Storage Architectures](https://arxiv.org/abs/2512.21571)

**Authors**: Hui Guo, Qihang Zheng, Chenghai Huo, Dongliang Guo, Haoqi Yang, Yang Zhang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.21571v1  

#### Abstract
The efficient deployment of large language models (LLMs) is hindered by memory architecture heterogeneity, where traditional compilers suffer from fragmented workflows and high adaptation costs. We present nncase, an open-source, end-to-end compilation framework designed to unify optimization across...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：nncase: An End-to-End Compiler for Efficient LLM Deployment on Heterogeneous Storage Architectures

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在异构存储架构上的高效部署面临三大挑战：
- **内存墙**：计算能力年增长100%，而内存带宽仅增长20%，数据移动成为瓶颈。
- **存储层次复杂**：从SRAM到HBM/DRAM的多级内存体系要求精细的数据复用策略。
- **计算单元异构**：标量、向量（如AVX-512）、矩阵单元（如Intel AMX）共存，传统编译器难以统一优化。

现有编译框架（如MLC LLM、TVM）通常为特定架构设计独立优化流程，导致**碎片化工作流**和**高昂适配成本**，且难以进行全局优化。

---

### **提出的新方法与创新思路**

nncase 是一个开源的端到端编译框架，其核心是“**统一分布式编译范式**”（unified distributed compilation paradigm），通过以下三大模块实现跨异构架构的高效优化：

#### ✅ 创新点一：基于 e-graph 的 Term Rewriting 引擎
- 采用 **equality saturation** 技术构建非破坏性重写引擎，避免传统编译器的 **phase ordering problem**。
- 在 e-graph 中同时探索多种等价程序表示，最终通过 **Weighted Partial MaxSAT (WPMAXSAT)** 求解器提取最优方案。
- 集成 **Roofline 模型** 进行多目标优化（延迟、内存占用、通信开销）。

#### ✅ 创新点二：三大自动化优化模块
1. **Auto Vectorize**
   - 提出 `MetaPackOperation` 和 `FoldNopPack` 规则，在 e-graph 内生成并融合多种张量布局候选。
   - 动态调整打包因子，消除冗余 layout 转换，平衡数据格式转换与计算单元饱和度。

2. **Auto Distribution**
   - 基于 **SBP (Split, Broadcast, Partial)** 抽象建模分布式策略。
   - 将分布策略搜索空间嵌入 e-graph，支持拓扑无关的并行化。
   - 提取时以设备内存容量为硬约束，确保策略可行性。

3. **Auto Schedule**
   - 引入 **nncase Tensor Template (NTT) Library**，封装高性能微内核（ukernels）作为调度原子单元。
   - 分层优化：
     - 结构优化（loop fusion/order） → 使用 **Monte Carlo Tree Search (MCTS)**
     - 参数优化（tiling size / buffer placement） → 使用 **Mixed-Integer Nonlinear Programming (MINLP)**

#### ✅ 创新点三：缓冲感知代码生成（Buffer-aware Codegen）
- 支持别名分析（Alias Analysis）减少内存拷贝。
- 使用 SAT 求解器解决 bin packing 问题，最大化中间变量内存复用。
- 常量预分片并绑定至本地存储，提升访问效率。

---

### **相比现有方法的优势**

| 维度 | nncase | 传统方法（如MLC LLM, IPEX） |
|------|--------|-----------------------------|
| 架构适应性 | 统一处理均匀/非均匀内存架构（NUMA抽象） | 需为不同架构定制流水线 |
| 全局优化能力 | e-graph 支持联合优化 layout、distribution、schedule | 各阶段割裂，易陷入局部最优 |
| 分布式策略 | 自动搜索 + 内存约束保障可行性 | 依赖人工标注或启发式 |
| 编译效率 | MCTS + MINLP 显著降低搜索时间 | 学习型方法（如Ansor）编译耗时长 |
| 性能上限 | 接近手调库（如llama.cpp） | 通常低于手工优化 |

---

## 2. 核心实验方法和设置

### **使用的模型与平台**

- **模型**：Qwen3 系列
  - Qwen3-0.6B（F32/F16）
  - Qwen3-1.7B（F16）
- **硬件平台**：AMD Ryzen 9 5900X（12核），128GB DDR4-3600
- **操作系统**：Ubuntu 24.04 LTS，GCC 14.2
- **指令集支持**：AVX2

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|-------|------|
| 批大小（batch size） | 1 |
| 输入长度 | 8-token prompt |
| 并发配置 | 单核（1T）、4线程（4T）、8线程（8T） |
| 重复次数 | 每个测试运行100次取平均值 |
| 主要指标 | **Token 吞吐量（tokens/s）**，衡量解码阶段总耗时 |

---

### **基线方法对比**

| 框架 | 类型 | 版本 |
|------|------|------|
| **llama.cpp** | 手工优化库 | Tag: b5753 |
| **Intel IPEX** | 工业级PyTorch扩展 | v28.0 |
| **MLC LLM** | 主流DL编译框架 | Commit: 862a7311 |

> 注：所有框架均启用默认优化及最高可用指令集（AVX2）。

---

## 3. 主要实验结果和性能指标

### **单核场景（1T）性能对比**

| 模型 | nncase | llama.cpp | IPEX | MLC LLM |
|------|--------|-----------|------|---------|
| Qwen3-0.6B (F32) | **8.7 tokens/s** | 10.61 tokens/s | 7.58 tokens/s | 0.2 tokens/s |
| Qwen3-0.6B (F16) | **13.87 tokens/s** | 17.21 tokens/s | 10.22 tokens/s | — |
| Qwen3-1.7B (F16) | **5.09 tokens/s** | 6.3 tokens/s | 4.2 tokens/s | — |

- **结论**：nncase 在单核下显著优于 IPEX 和 MLC LLM，虽略逊于手工优化的 llama.cpp（差距约18%-19%），但已接近其性能边界。

---

### **多核场景（4T / 8T）性能对比**

| 模型 | 配置 | nncase | llama.cpp | IPEX |
|------|------|--------|-----------|------|
| Qwen3-0.6B-F16 | 4T | **23.5 tokens/s** | 23.2 tokens/s | 15.52 tokens/s |
| Qwen3-0.6B-F16 | 8T | **23.98 tokens/s** | ~23.5 tokens/s | ~16 tokens/s |
| Qwen3-1.7B-F16 | 4T | **8.85 tokens/s** | 8.34 tokens/s | 6.93 tokens/s |

- **扩展效率**：
  - Qwen3-1.7B 下，nncase 从 1T 到 4T 提升 **74%**，而 llama.cpp 仅提升 **32%**。
- **优势来源**：
  - nncase 使用 **静态任务划分 + 编译期核心映射**，避免 OpenMP 动态调度开销。
  - 将多核视为“多节点”，通过 Auto Distribution 优化粒度，减少同步与通信。

---

### **消融实验（隐含分析）**

尽管未明确列出消融表，文中通过模块设计揭示了各组件作用：

- **Auto Vectorize**：在 Attention 子图中成功消除中间 Pack/Unpack 操作，实现“pass-through”布局，降低内存访问。
- **Auto Distribution**：引入 Resharding Boxing 和 SBP signature 约束，保证策略合法性。
- **Auto Schedule + NTT**：MINLP 精确建模缓存层级与寄存器复用，避免 spill/fill 开销。

---

## 4. 关键结论和发现

### **主要发现**

1. **统一编译范式可行且高效**  
   nncase 成功将 NUMA 抽象应用于单机多核与分布式系统，实现了“**compile once, adapt everywhere**”。

2. **自动化可逼近手工优化性能**  
   在单核场景下，nncase 达到 llama.cpp 的 **~80%-85%** 性能，证明自动编译在 LLM 部署中的实用性。

3. **多核扩展性超越手工库**  
   得益于静态调度与分布式思维，nncase 在多核环境下反超 llama.cpp，验证了其对现代CPU架构的更好适配。

4. **e-graph 支持联合优化**  
   将 layout、distribution、schedule 统一在 e-graph 中表达，解决了传统方法割裂优化的问题。

---

### **局限性**

1. **当前主要面向 CPU 架构**  
   尚未支持 GPU/SIMT 架构，无法直接用于大规模集群训练。

2. **缺乏对稀疏性与量化联合优化的支持**  
   实验集中在 F32/F16，未涉及 INT4/GGUF 等主流推理格式。

3. **编译时间未详细报告**  
   虽声称比学习型方法快，但缺少与 Ansor/TVM MetaSchedule 的直接对比。

---

### **未来工作方向**

1. **全栈性能表征**  
   扩展评估范围至更多模型规模、硬件平台，分析编译开销与内存占用。

2. **跨架构适配（SIMT 支持）**  
   扩展后端以支持 GPU，实现真正的跨平台可移植性。

3. **计算-通信重叠优化**  
   自动融合通信与计算内核，通过流水线隐藏延迟，提升分布式吞吐。

---

> **源码地址**：https://github.com/kendryte/nncase

</details>

---

### 6. [Rethinking Output Alignment For 1-bit Post-Training Quantization of Large Language Models](https://arxiv.org/abs/2512.21651)

**Authors**: Dung Anh Hoang, Cuong Pham, Cuong Nguyen, Trung le, Jianfei Cai, Thanh-Toan Do  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.21651v1  

#### Abstract
Large Language Models (LLMs) deliver strong performance across a wide range of NLP tasks, but their massive sizes hinder deployment on resource-constrained devices. To reduce their computational and memory burden, various compression techniques have been proposed, including quantization, pruning, an...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Rethinking Output Alignment For 1-bit Post-Training Quantization of Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于 **1-bit Post-Training Quantization (PTQ)** 在大语言模型（LLMs）中的应用挑战。尽管 sub-4-bit PTQ 已取得显著进展，但 **1-bit 量化**（将浮点权重压缩为 ±1）仍面临严重性能退化问题。

现有方法大多采用 **weight alignment**（最小化 $||W - \hat{W}||$），而更直观的 **output alignment**（最小化 $||XW - X\hat{W}||$）在实践中效果不佳。作者系统分析了为何 naive output alignment 失效，并提出改进方案。

### 提出的新方法与创新思路
作者提出了一个 **data-aware 的 1-bit PTQ 框架**，其核心创新包括：

1. **Selective Layer-wise Output Matching**  
   - 不对所有层进行输出对齐，而是**仅在每个 Transformer block 的最后一个全连接层（Final FC）应用 output alignment**，其余层使用 weight alignment。
   - 动机：避免因逐层误差累积导致 block-level 性能下降。

2. **True Target Output Objective**  
   - 将优化目标从 $||\hat{X}W - \hat{X}\hat{W}||$（pseudo target）改为 $||XW - \hat{X}\hat{W}||$（true target），其中 $X$ 是 full-precision 输入。
   - 显式建模并缓解 **activation error accumulation** 问题。

3. **Attention Matrix Preservation (AMP)**  
   - 引入一种注意力感知机制，通过梯度符号构造掩码（mask），保护 token-level 的相似性结构。
   - 防止 output alignment 破坏 attention pattern，尤其在 LLaMA 等架构中至关重要。

### 相比现有方法的优势
- 更贴近量化最终目标（保持输出一致性）
- 显式考虑跨层激活误差传播
- 保留关键注意力行为，防止语义结构退化
- 在多个 LLM 架构上实现一致且显著的性能提升

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据集 |
|------|--------|
| **Calibration Set** | C4（用于量化校准） |
| **Perplexity Evaluation** | C4, WikiText2, PTB |
| **Zero-shot QA Evaluation** | ARC-Easy, ARC-Challenge, PIQA, BoolQ, HellaSwag, WinoGrande, OBQA, LAMBADA |

### 实验设置和评估指标
| 项目 | 设置 |
|------|------|
| **模型范围** | OPT 系列（1.3B–30B）、LLaMA-2（7B/13B）、LLaMA-3（8B） |
| **量化方式** | 1-bit PTQ，block size = 128 |
| **评估指标** | 
| - **Perplexity (PPL)** ↓ | 衡量语言建模能力 |
| - **Zero-shot Accuracy** ↑ | 衡量下游任务泛化能力 |
| - **Ablation Studies** | 分析各组件有效性 |

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Full Precision** | FP16 baseline | 性能上限参考 |
| **PB-LLM** | 1-bit PTQ | Partial binarization，部分参数保留高精度 |
| **BiLLM** | 1-bit PTQ | Hessian-guided 重要性筛选 |
| **ARB-RC** | 1-bit PTQ | Weight alignment 代表方法 |
| **ARB-X** | 1-bit PTQ | Output alignment 代表方法 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 OPT 和 LLaMA 为例）

#### ✅ Table 1 & 2: Perplexity 对比（越低越好）

| Model | Method | C4 (PPL) | WikiText2 (PPL) | PTB (PPL) |
|-------|--------|----------|----------------|-----------|
| OPT-13B | Full Precision | 12.06 | 10.13 | 19.09 |
| OPT-13B | ARB-RC | 15.07 | 13.10 | 19.09 |
| OPT-13B | ARB-X | 17.71 | 15.47 | 23.46 |
| OPT-13B | **Ours** | **14.71** | **12.84** | **18.85** |
| LLaMA-2-7B | Full Precision | 7.26 | 5.47 | 37.91 |
| LLaMA-2-7B | ARB-RC | 14.77 | 12.47 | 197.70 |
| LLaMA-2-7B | ARB-X | 19.82 | 14.86 | 182.10 |
| LLaMA-2-7B | **Ours** | **13.80** | **11.50** | **196.64** |

> 📌 **结论**：在几乎所有配置下，本文方法均优于所有 baseline，尤其在小模型（如 OPT-1.3B）上优势更明显。

#### ✅ Zero-shot QA 准确率（平均准确率 ↑）

| Model | Method | Avg Accuracy (%) |
|-------|--------|------------------|
| OPT-30B | ARB-RC | 57.11 |
| OPT-30B | **Ours** | **57.70** (+0.59%) |
| LLaMA-3-8B | ARB-RC | 47.11 |
| LLaMA-3-8B | **Ours** | **47.71** (+0.60%) |

> 📌 虽然提升幅度不大，但在极端压缩条件下已属显著。

---

### 消融实验结果（Ablation Study）

#### 🔍 Table 3: AMP 掩码的影响（LLaMA-2-7B）

| 设置 | C4 PPL | WikiText2 PPL |
|------|--------|--------------|
| Without AMP | 29.12 | 26.24 |
| With AMP | **19.25** | **15.42** |

> ⚠️ **说明**：AMP 对 LLaMA 至关重要，移除后性能大幅下降（+10 PPL），因其依赖 RMSNorm 导致方向敏感。

#### 🔍 Table 4: 输出误差 vs 激活条件误差

| Objective | C4 PPL (LLaMA-2-7B) |
|----------|---------------------|
| Activation-conditioned Error | 19.97 |
| **True Output Error (ours)** | **19.25** |

> ✅ 显式建模真实输出误差可带来约 0.7 PPL 提升。

#### 🔍 Table 5: 不同层应用 output alignment 的效果

| Layer | C4 PPL (LLaMA-2-7B) |
|-------|---------------------|
| Query | 20.08 |
| Key | 20.80 |
| Value | 21.44 |
| Attn Out | 21.02 |
| **Final FC** | **19.25** ✅ |

> 💡 结论：**仅在 block 的 final FC 层应用 output alignment 效果最佳**，验证 selective strategy 的合理性。

---

## 4. 关键结论和发现

### 主要发现
1. **Naive output alignment 并不能保证 block-level 性能提升**  
   - 即使某层输出重建误差降低，也可能因误差传播导致整体性能恶化。

2. **Activation error accumulation 是 output alignment 失效的关键原因**  
   - 使用 $\hat{X}$（量化后输入）作为条件会引入偏差，应使用原始 $X$ 构造 true target。

3. **Output alignment 可能破坏 attention mask**  
   - 尤其在 LLaMA 中，由于 RMSNorm 对表示方向敏感，token 相似性易被破坏。

4. **Selective + True-target + AMP 组合策略最有效**  
   - 三者协同作用，在不增加推理开销的前提下显著提升性能。

### 方法的局限性
- 当前方法仍基于 ARB 框架扩展，未解决 1-bit 本身的信息瓶颈。
- AMP 机制依赖梯度符号，可能对噪声敏感。
- 所有实验基于 A100 GPU，未测试边缘设备部署效率。

### 未来工作方向
- 探索更精细的 block-level 或 sequence-level output alignment 策略
- 结合轻量级微调（few-step QAT）进一步缩小 gap
- 将 AMP 思想推广到其他结构（如 MoE、vision encoder）
- 研究 1-bit + sparse 架构联合优化

---

> ✅ **总体评价**：该论文不仅提出了一种有效的 1-bit PTQ 方法，更重要的是揭示了 output alignment 在 LLM 量化中的内在机制与陷阱，为后续研究提供了深刻洞见。

</details>

---

### 7. [BLEST: Blazingly Efficient BFS using Tensor Cores](https://arxiv.org/abs/2512.21967)

**Authors**: Deniz Elbek, Kamer Kaya  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.21967v1  

#### Abstract
Breadth-First Search (BFS) is a fundamental graph kernel that underpins a wide range of applications. While modern GPUs provide specialised Matrix-Multiply-Accumulate (MMA) units, e.g., Tensor Cores (TC), with extremely high throughput, they target dense operations, making it non-trivial to exploit ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BLEST: Blazingly Efficient BFS using Tensor Cores

## 1. 论文的主要贡献和创新点

### 解决的问题
现代 GPU 虽然配备了高吞吐量的 **Tensor Cores (TC)**，但这些硬件单元专为密集矩阵运算设计，难以高效支持图算法中典型的稀疏、不规则计算模式。传统的 **Breadth-First Search (BFS)** 在 GPU 上执行时面临以下挑战：
- **负载不均衡**：图的高度不规则性导致不同线程块（warp）处理的工作量差异大。
- **冗余计算**：传统 slice-set 结构采用“frontier-oblivious”调度，即使当前 frontier 为空，仍会分配任务。
- **原子操作开销大**：顶点更新涉及大量分散的原子写操作，导致内存访问不合并（uncoalesced），缓存命中率低。
- **数据布局效率低**：缺乏对图结构感知的重排序策略，影响压缩率和局部性。

### 提出的新方法与创新
论文提出了 **BLEST**（Blazingly Efficient BFS using Tensor Cores），一个基于 Tensor Cores 加速的 BFS 框架，其核心创新包括：

#### （1）Binarised Virtual Slice Sets (BVSS)
- 将传统固定的 slice set 细化为更小的 **Virtual Slice Set (VSS)** 单位。
- 引入 round-robin 分配机制，确保每个 warp 处理的 VSS 数量几乎相等，实现近乎完美的 **warp-level load balancing**。
- 只有当对应 frontier 非零时才将 VSS 加入队列，避免了无效工作。

#### （2）图重排序策略（Graph Reordering）
- 对于 **social-like graphs**（如社交网络），提出 **JACCARDWITHWINDOWS** 算法，利用窗口内 Jaccard 相似性聚类提升 slice 内边的密度，从而提高 **compression ratio**。
- 对于 **non-social graphs**（如道路网络），采用 **RCM (Reverse Cuthill-McKee)** 降低带宽，减少 cache line fetches，改善更新局部性。

#### （3）最优的 TC 计算模式（Optimal TC Layout）
- 设计了一种新的 **bitwise SpMSpV → batched SpMM** 映射方式。
- 利用 m8n8k128 TC tile，通过精心构造 `fragA` 和 `fragB` 输入，使得每次 TC 调用都能充分利用全部 64 个输出条目，无浪费。
- 相比 SOTA 的 16 次 TC 调用处理 128 个节点，BLEST 仅需 **2 次 TC 调用**，减少了 8 倍的 MMA 开销。

#### （4）延迟顶点更新（Lazy Vertex Update）
- 将顶点更新延迟到每层 BFS 结束后统一处理。
- 使用轻量级异步 `REDG` 原子操作进行标记，在最终阶段以完全合并的方式批量应用更新，显著降低原子冲突和 cache miss。

#### （5）核函数融合（Kernel Fusion）
- 利用 CUDA Cooperative Groups 实现设备端同步，将多层 BFS 循环融合进单个持久化 kernel。
- 消除了主机侧（host-side）每层之间的同步开销，特别有利于需要数千层遍历的道路网络。

### 相比现有方法的优势
- **更高的硬件利用率**：充分挖掘 Tensor Cores 的潜力，适用于稀疏图计算。
- **更低的冗余与更高并行效率**：BVSS + lazy update 减少无效计算和原子争用。
- **更强的适应性**：通过图类型自适应选择重排序策略，兼顾压缩与局部性。
- **更好的可扩展性**：核融合减少 kernel launch 开销，适合长路径 BFS。

---

## 2. 核心实验方法和设置

### 数据集
实验在两个基准套件上进行：
- **GAP Benchmark Suite**：包含多种真实世界图，如 `GAP-road`, `GAP-twitter`, `GAP-web`, `GAP-kron` 等。
- **Custom Benchmark**：从 **SuiteSparse** 中选取的大规模图（|V| ≥ 2³⁰），排除重复项后共 14 个图，涵盖社交、网页、科学计算等领域。

代表性图示例：
- `GAP-twitter`: 61M 节点，1.4B 边（social-like）
- `GAP-road`: 23M 节点，57M 边（road network）
- `webbase-2001`: 118M 节点，1.0B 边（web graph）

### 实验设置
- **硬件平台**：NVIDIA H200 GPU（支持 compute capability 8.0+ 及 m8n8k128 指令），配备 141GB 显存。
- **软件环境**：CUDA 13.0，g++ 12.3.0。
- **运行方式**：每张图执行 64 次随机源点 BFS，取平均时间。
- **评估指标**：
  - 总运行时间（milliseconds）
  - 相对于基线的速度提升（speedup）
  - 消融实验分析各优化模块贡献
  - 内存占用、compression ratio、update divergence 等辅助指标

### 基线方法对比
- **GAP CPU BFS**：并行 CPU 版本 BFS
- **Gunrock**：主流 GPU 图分析框架，支持 push/pull 切换与负载均衡
- **GSWITCH**：动态选择优化模式的高性能 GPU BFS
- **BerryBees**：首个使用 Tensor Cores 的 BFS 框架，也是本文最直接的比较对象

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）
| 方法 | 平均速度提升（vs. BerryBees） | 最大速度提升 |
|------|-------------------------------|-------------|
| **BLEST (full)** | **3.58×** | 8.90× (`Spielman_k600`) |

与其他框架相比：
- vs. **GAP CPU**: **13.25×**
- vs. **Gunrock**: **4.64×**
- vs. **GSWITCH**: **4.90×**

> 注：在所有测试图上，BLEST 均优于其他框架，平均提速超过 3.5 倍。

### 消融实验结果（Table 2 中不同变体）
| 变体 | 描述 | 相比 BerryBees 平均加速 |
|------|------|------------------------|
| **BLEST (a)** | 基础版（BVSS + 最优 TC layout） | 1.89× |
| **BLEST (ac)** | (a) + lazy update | 2.71× |
| **BLEST (ab)** | (a) + 重排序（w=2¹⁶） | 1.98× |
| **BLEST (full)** | 完整版本（a + b + c） | **3.58×** |

✅ 所有优化均带来正向收益，组合后效果显著放大。

### 其他重要观察
- **重排序有效性验证**（Table 3）：
  - 在自然顺序不利的图上（如 `GAP-web`），随机打乱后 BLEST(ab) 比 BLEST(a) 快 **2.60×**，证明所提重排序算法有效。
- **window size 影响**（Figure 3）：
  - 增大 `w` 可持续提升 compression ratio（从 0.34 到 0.61），带来 **2.01× BFS 加速**，但存在边际递减效应。
- **lazy update 启发式阈值**：
  - 当 `update divergence` > 25,000 时启用 lazy update 更有利；否则可能因额外 O(n) 扫描而受损（如 `mawi`, `Spielman_k600`）。

### 内存开销（Table 4）
- **BVSS 结构紧凑**：静态结构内存占用合理，总显存消耗可控（多数图在 2–20 GB 范围），可在主流 GPU 上部署。

---

## 4. 关键结论和发现

### 主要发现
1. **Tensor Cores 可用于高效 BFS**：通过 BVSS 和 bitwise TC 编程模型，成功将稀疏图遍历映射到密集 MMA 单元，突破传统限制。
2. **细粒度任务划分 + frontier-aware 调度是关键**：BVSS 实现了近乎完美的负载均衡，并消除了无效工作。
3. **图结构感知的预处理至关重要**：针对不同类型图采用不同重排序策略（Jaccard-based vs RCM），能显著提升压缩率和缓存局部性。
4. **延迟更新 + 核融合大幅提升效率**：lazy update 减少原子开销，kernel fusion 消除同步瓶颈，尤其利于深层 BFS。

### 方法的局限性
- **静态执行策略**：目前未在运行时动态切换优化策略（如是否启用 lazy update），对某些图（如高带宽但低 divergence 的图）可能不是最优。
- **重排序成本**：虽然并行化，但对于超大规模图，JACCARDWITHWINDOWS 的预处理时间仍不可忽略。
- **依赖特定硬件**：必须运行在支持 m8n8k128 的 Ampere 或更新架构 GPU 上（如 A100/H100/H200）。

### 未来工作方向
1. **构建智能决策管道**：基于图特征和运行时状态自动选择最佳配置（如是否 lazy update、window size 等）。
2. **扩展至 Multi-source BFS**：应用于 closeness centrality、diameter 计算等需要多源 BFS 的场景。
3. **跨平台移植**：适配 AMD GPU 的 Matrix Core 或其他支持 MMA 的加速器。
4. **动态调整执行流**：根据当前 frontier 密度、层级深度等动态调整调度与更新策略。

---

> ✅ **总结**：BLEST 是首个系统性地将 Tensor Cores 高效应用于 BFS 的工作，通过 **BVSS 数据结构、图感知重排序、最优 TC 编程、lazy update 和 kernel fusion** 五大技术创新，在广泛的真实图上实现了平均 **3.58×** 于 SOTA TC-based 方法（BerryBees）的性能提升，确立了利用专用硬件加速图算法的新范式。

</details>

---

### 8. [RLLaVA: An RL-central Framework for Language and Vision Assistants](https://arxiv.org/abs/2512.21450)

**Authors**: Lei Zhao, Zihao Ma, Boyu Lin, Yuhe Liu, Wenjun Wu, Lei Huang  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.21450v1  

#### Abstract
We present an RL-central framework for Language and Vision Assistants (RLLaVA) with its formulation of Markov decision process (MDP). RLLaVA decouples RL algorithmic logic from model architecture and distributed execution, supporting researchers in implementing new RL algorithms with minimal code, a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RLLaVA: An RL-central Framework for Language and Vision Assistants 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前强化学习（Reinforcement Learning, RL）在视觉语言模型（Vision-Language Models, VLMs）中的应用日益广泛，但现有的 RL 框架（如 veRL、OpenRLHF）主要面向大规模集群和纯文本大模型（LLMs），存在以下问题：
- **缺乏针对多模态场景的专用框架**：未考虑 VLM 中图像与文本联合处理、环境反馈中视觉响应等特性；
- **算法逻辑与分布式执行耦合紧密**：导致研究人员难以快速实现和测试新的 RL 算法；
- **资源门槛高**：依赖大型 GPU 集群，限制了中小研究团队的参与。

### 提出的新方法与思路
本文提出 **RLLaVA** —— 一个以 RL 为中心的模块化框架，专为语言与视觉助手设计。其核心思想包括：

- **统一的 MDP 建模**：将 VLM 的图文序列决策过程形式化为统一的马尔可夫决策过程（Markov Decision Process, MDP），涵盖状态空间 $S = (V \cup Z)^*$（含图像和文本）、动作空间 $A = V$（生成 token）、转移分布与奖励函数。
- **三重解耦架构设计**：
  - **算法逻辑**（如 PPO、GRPO）
  - **模型架构**（支持 Qwen、Phi、Gemma 等 LLM 和 CLIP、SigLIP 等视觉编码器）
  - **分布式执行引擎**（兼容 FSDP、DeepSpeed、vLLM、SGLang）
- **角色化组件抽象**：定义四个核心角色：
  - `Actor`：策略网络管理与更新
  - `Reward`：轨迹评分（规则奖励 / 奖励模型 / LLM-as-Judge）
  - `Critic`：价值函数估计（可选）
  - `Reference`：参考策略用于 KL 正则化

### 相比现有方法的优势
| 维度 | RLLaVA | 其他框架（如 veRL、OpenRLHF） |
|------|--------|-------------------------------|
| **多模态原生支持** | ✅ 支持图像输入与视觉环境交互 | ❌ 主要面向文本任务 |
| **模块化程度** | ✅ 完全解耦，支持插件式替换 | ⚠️ 耦合较强，修改成本高 |
| **资源效率** | ✅ 单张 24GB GPU 可训练 4B 规模模型 | ❌ 通常需多卡或多节点 |
| **易用性** | ✅ YAML 配置驱动，轻量代码改动即可切换算法 | ❌ 需深入系统级开发 |
| **灵活性** | ✅ 支持混合算法组合（如 OPO + CLIP-COV） | ⚠️ 固定流程为主 |

此外，RLLaVA 借鉴了 TinyLLaVA Factory 的轻量化设计理念，在小规模设备上实现了高效训练。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖五类多模态任务，具体如下：

| 任务类别 | 数据集 | 描述 |
|--------|-------|------|
| **Mathematical Reasoning** | Geometry3K [24] | 图像相关的几何推理题 |
| **Counting** | CLEVR-Count-70k [13] | 复合对象计数任务 |
| **Grounding** | RefCOCO/+/g [14,44,25] | 指代表达理解（定位图像中被描述的对象）<br>LISA [16]：跨域泛化测试集 |
| **Agentic-Search** | MAT-Search [22] | 多轮视觉信息检索（结合网页搜索工具） |
| **Agentic-Coding** | MAT-Coding [22] | 图像操作相关的代码生成任务 |

### 实验设置
- **模型**：
  - Qwen2-VL-2B、Qwen2.5-VL-3B、Qwen2.5-VL-7B
  - 支持 full-parameter fine-tuning 与 LoRA 微调
- **训练配置**：
  - 主要使用 **GRPO**（Group Relative Policy Optimization）作为 RL 算法
  - 每个 prompt 采样 4–8 条响应轨迹（rollout）
  - 分布式训练使用 **FSDP**
  - 推理阶段使用 **vLLM** 加速 rollout
- **评估指标**：
  - Math / Counting：Accuracy
  - Grounding：IoU（Intersection over Union）
  - Agentic Tasks（Search & Coding）：F1 Score

### 基线方法对比
- **Base Model**：未经 RL 微调的原始 VLM（如 Qwen2.5-VL-instruct）
- **Visual-ARFT [22]**：专门针对多模态代理任务设计的 RL 框架，使用 7B 模型进行对比

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| Task | Model | Dataset | Metric | Base | GRPO (RLLaVA) |
|------|-------|--------|--------|------|----------------|
| Math | Qwen2.5-VL-3B | Geometry3K | Accuracy | 35.1 | **39.0** (+3.9) |
| Counting | Qwen2.5-VL-3B | CLEVR-Count | Accuracy | 52.0 | **57.5** (+5.5) |
| Grounding | Qwen2-VL-2B | RefCOCO/+/g | IoU | 51.3 | **63.3** (+12.0) |
| Search | Qwen2.5-VL-3B | MAT-Search | F1 | 4.4 | **27.1** (+22.7) |
| Coding | Qwen2.5-VL-3B | MAT-Coding | F1 | 16.9 | **30.6** (+13.7) |

> 注：表中“Coding”行原文误写为“Owen”，应为“Qwen”。

#### 结果分析：
- 所有任务上，经过 RLLaVA 训练的模型均显著优于 base model；
- **代理类任务提升最大**（Search +22.7 F1，Coding +13.7 F1），表明 RL 对复杂推理与工具调用能力有强增强作用；
- 感知类任务（Counting、Grounding）也有明显改进，说明模型语义理解更精准。

### 泛化能力验证（Table 2）

| Model | RefCOCO | RefCOCO+ | RefCOCOg | LISA (OOD) |
|-------|---------|----------|-----------|------------|
| Base (Qwen2-VL-2B) | 54.79 | 51.48 | 56.75 | 20.78 |
| GRPO (300 steps) | 67.14 | 60.43 | 61.43 | **31.88** |
| Improvement | +12.35 | +8.95 | +4.68 | **+11.10** |

- 在 out-of-domain 的 **LISA** 数据集上提升达 **+11.10 IoU**，远超部分 in-domain 提升；
- 表明 RL 训练增强了模型的**语义推理能力**而非简单记忆或对齐。

### 与其他框架对比（Figure 2）
- 在 **Agentic-Search** 任务中，Qwen2.5-VL-3B + RLLaVA 性能超过 Visual-ARFT-7B（+0.32 All F1）；
- 在 **Agentic-Coding** 上略逊于 Visual-ARFT-7B，但仍大幅超越 base model；
- 表明 RLLaVA 的通用框架已能达到甚至超越专用系统的性能。

### 消融实验（文中虽未单独列出表格，但有隐含分析）
- **资源效率机制有效性**：
  - 启用 **co-located execution**（rollout 与优化共享 GPU 内存）后，单卡 24GB GPU 可完成端到端训练；
  - 使用 **FSDP2 offload** 和 **CPU offloading** 显著降低显存占用（峰值约 21–22GB）；
- **算法组件可组合性**：
  - 支持多种 advantage estimator（GAE、GRPO、RLOO、OPO）与 policy loss（PPO clip、GSPO、DAPO）自由组合；
  - 示例：OPO baseline + CLIP-COV 更新策略，体现高度灵活。

---

## 4. 关键结论和发现

### 主要发现
1. **RLLaVA 成功构建了一个适用于多模态 RL 的通用、模块化框架**，通过 MDP 形式化建模和三重解耦设计，极大降低了算法研发门槛。
2. **在多种多模态任务上，基于 RLLaVA 训练的模型显著优于 base model**，尤其在需要多轮推理与工具使用的 agentic 场景中表现突出。
3. **具备优异的跨任务泛化能力**，在 LISA 等 out-of-domain 数据集上也取得大幅提升，证明 RL 提升的是深层语义理解而非过拟合。
4. **资源效率极高**：支持在单张 24GB GPU 上对 4B 级别模型进行 full-parameter RL 微调，极大促进中小团队参与多模态 RL 研究。

### 方法的局限性
- 当前实验集中在 **静态图像 + 文本输出** 场景，尚未涉及视频、动态 GUI 或具身智能体（embodied agents）；
- 尽管支持多种算法，但某些高级功能（如自动 reward shaping、multi-agent RL）仍需手动实现；
- 对 extremely large models (>7B) 的扩展性未充分验证。

### 未来工作方向
1. 开发 **tool-call 与 multi-turn interaction 模块**，集成基础工具与环境模拟器；
2. 扩展至更多 agentic 场景：pixel-level reasoning、GUI agent、embodied AI；
3. 持续集成新兴 RL 算法（如 REMAX、OPO）与多模态架构；
4. 构建社区生态，鼓励开源贡献与插件开发。

---

> 🔗 **代码地址**：https://github.com/TinyLoopX/RLLaVA  
> 📄 **论文出处**：arXiv:2512.21450

</details>

---

### 9. [TimeBill: Time-Budgeted Inference for Large Language Models](https://arxiv.org/abs/2512.21859)

**Authors**: Qi Fan, An Zou, Yehan Ma  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.21859v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in time-critical systems, such as robotics, autonomous driving, embodied intelligence, and industrial automation, where generating accurate responses within a given time budget is crucial for decision-making, control, or safety-critical tasks. H...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：TimeBill: Time-Budgeted Inference for Large Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在机器人、自动驾驶、具身智能和工业自动化等**时间敏感系统**中应用日益广泛。这些场景通常具有硬实时约束（hard deadlines），要求在给定的时间预算 $ T $ 内完成推理并生成高质量响应。

然而，LLMs 的自回归生成过程导致其端到端执行时间具有高度不确定性，难以建模和预测。此外，现有的高效推理方法（如基于固定比例的 KV Cache 蒸馏或量化）缺乏对动态任务时间和响应长度变化的适应能力，容易出现超时（overrun）或性能下降。

### **提出的新方法：TimeBill 框架**
本文提出了 **TimeBill** —— 一种面向时间预算的 LLM 推理框架，旨在平衡**推理效率**与**响应质量**。其核心创新包括：

- **细粒度响应长度预测器（Fine-grained Response Length Predictor, RLP）**  
  基于小型语言模型（SLM）构建分类型预测器，将响应长度划分为多个“桶”（buckets），实现对目标 LLM 输出长度的高精度预测，优于基于 BERT 的粗粒度分类器。

- **工作负载引导的执行时间估计器（Workload-guided Execution Time Estimator, ETE）**  
  结合 **FLOPs 分析建模** 与 **实际性能剖析（profiling）**，建立预填充阶段（prefill phase）和解码步骤（decoding step）的执行时间模型，并集成 KV Cache 蒸发比例 $ \alpha $ 对解码延迟的影响。

- **时间预算驱动的动态 KV Cache 蒸发机制**  
  根据预测的最坏情况执行时间（WCET）和给定时间预算 $ T $，在线计算最优的 KV Cache 蒸发比例 $ \alpha^* $，以最大化响应性能的同时确保不超时。

### **相比现有方法的优势**
| 方法 | 是否支持时间预算控制 | 是否动态调整 | 对响应性能影响 |
|------|------------------------|---------------|------------------|
| Vanilla Inference | ❌ | ❌ | 高质量但易超时 |
| Fixed-$\alpha$ KV Eviction | ❌ | ❌ | 固定损失，无法权衡 |
| AWQ / Quantization | ❌ | ❌ | 减少内存但不解决时间不确定性 |
| **TimeBill (本文)** | ✅ | ✅ | 动态优化，保持高性能且满足时限 |

> ✅ **优势总结**：TimeBill 是首个显式建模 LLM 端到端执行时间并支持**运行时自适应配置**的推理框架，在多种时间预算下均能显著提升任务完成率和平均响应质量。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **主测试集**：`LongBench` —— 一个双语、多任务的长上下文理解基准，涵盖问答、摘要、数学等多种任务。
- **训练集（用于 RLP）**：`Arena-Human-Preference-100k` 数据集中的提示（prompt），避免在测试集上训练。

### **实验设置**
- **目标 LLM**：`Qwen2.5-7B-Instruct`（context length: 32,768；max generation: 8,192）
- **SLM 构建 RLP**：`Qwen2.5-0.5B-Instruct`
- **硬件平台**：Intel Xeon Platinum 8350C + NVIDIA A40 GPU
- **KV Cache 蒸发实现**：采用 `SnapKV` 技术
- **时间预算范围**：5s ~ 10s（每秒递增）
- **最大蒸发达度 $ \alpha_{\text{max}} $**：95%
- **悲观因子 $ k $**：默认设为 5（用于 WCET 保守估计）

### **评估指标**
- **响应性能评分**：
  - F1 Score
  - ROUGE-L
  - Levenshtein Distance
- **任务完成率（Completion Rate）**：按时完成的任务占比
- **平均响应性能得分（Average Score）**：所有任务的加权平均得分
- **超时策略（Overrun Strategies）**：
  - **Kill**：超时则终止，输出为空
  - **Skip-Next**：跳过后续若干任务直到当前完成

### **基线方法对比**
| 基线方法 | 描述 |
|---------|------|
| **Vanilla** | 不做任何优化，直接推理 |
| **Fixed-$\alpha$ KV Eviction** | 设置固定的 KV Cache 蒸发比例（25%, 50%, 75%, 95%） |
| **AWQ** | 权重 4-bit 量化加速 |
| **TimeBill (Ours)** | 本文提出的动态时间预算感知推理框架 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 响应长度预测器（RLP）效果（Table 1）**
| 方法 | #Buckets | MAE ↓ | RMSE ↓ | R² ↑ |
|------|----------|-------|--------|------|
| ProxyModel (BERT-based) | 5 | 105.72 | 136.79 | 0.152 |
| S3 (DistilBERT-based) | 10 | 108.96 | 148.91 | -0.004 |
| **Ours (Regression)** | – | 64.21 | 103.30 | 0.516 |
| **Ours (Classification, B=64)** | 128 | 48.95 | 87.57 | 0.652 |
| **Ours (B=32)** | 256 | 44.15 | 78.63 | 0.719 |
| **Ours (B=16)** | **512** | **42.71** | **78.13** | **0.723** |

> ✅ **结论**：所提 RLP 显著优于现有方法，且随着 bucket 数增加（即粒度更细），性能持续提升。

#### **(2) 执行时间估计误差**
- **Prefill Phase MAPE**: 1.22%
- **Decoding Step MAPE**: 1.69%
- 图6显示，预测的 $ t_{e2e} $ 和保守估计的 $ t_{WCET} $ 均与实测值高度吻合，且 $ t_{WCET} $ 能有效覆盖真实执行时间。

#### **(3) 平均响应得分 vs 完成率（Fig. 7）**
在不同时间预算（5~10s）和两种超时策略下：

| 方法 | Avg Score ↑ | Completion Rate ↑ |
|------|-------------|--------------------|
| Vanilla | 很低 | < 40% （常超时） |
| Fixed-$\alpha=25\%$ | 中等偏低 | 较低 |
| Fixed-$\alpha=95\%$ | 低（性能差） | 高（接近 TimeBill） |
| **TimeBill** | **最高** | **与 $\alpha=95\%$ 相当甚至更高** |

> ⭐ **核心发现**：TimeBill 在几乎不牺牲完成率的前提下，实现了最高的平均响应质量，显著优于所有 baseline。

#### **(4) 消融实验：悲观因子 $ k $ 的影响（Fig. 8）**
- 当 $ k=1\sim5 $：增大 $ k $ 可提高完成率和平均分（因更保守调度）
- 当 $ k>5 $：过度保守导致 $ \alpha^* $ 过大，响应质量急剧下降
- **最佳 $ k \approx 5 $**，符合硬实时系统设计惯例

---

## **4. 关键结论和发现**

### **主要结论**
1. **LLM 推理必须考虑时间预算约束**：传统“越快越好”（AFAP）或固定优化策略无法兼顾时效性与响应质量。
2. **细粒度响应长度预测至关重要**：基于 SLM 的分类式 RLP 比 BERT 或回归方法更准确，尤其适合长输入场景。
3. **分析建模 + 实测拟合的 ETE 更可靠**：结合 FLOPs 分析与 profiling 的混合建模方式，能精准估计含 KV Cache 蒸发的执行时间。
4. **动态调整 $ \alpha $ 是关键**：TimeBill 能根据每个请求的输入长度、时间预算自动计算最优蒸发达度，在保证不超时的前提下最大化性能。

### **方法的局限性**
- **依赖离线建模**：FLOPs 系数和 ETE 参数需提前在目标硬件上 profiling，迁移至新设备需重新校准。
- **预测误差累积风险**：若 RLP 或 ETE 存在系统性偏差，可能导致 $ t_{WCET} $ 低估，引发超时。
- **仅支持 KV Cache 蒸发**：未整合其他在线优化技术（如 early exiting），仍有扩展空间。

### **未来工作方向**
- 将 TimeBill 与其他优化技术（如 AWQ、prompt compression）联合使用，进一步提升效率。
- 支持多模态 LLM 的时间预算推理。
- 引入反馈机制，根据历史执行结果动态调整悲观因子 $ k $。
- 扩展至分布式或 disaggregated serving 架构下的资源协同调度。

---

> 🔚 **总结**：TimeBill 是首个系统化解决 LLM 时间预算推理问题的框架，通过“预测 → 估时 → 自适应配置”的闭环机制，在真实时间约束下实现了推理效率与响应质量的最佳平衡，为 LLM 在实时系统中的部署提供了重要基础。

</details>

---

### 10. [Robust Federated Fine-Tuning in Heterogeneous Networks with Unreliable Connections: An Aggregation View](https://arxiv.org/abs/2512.22035)

**Authors**: Yanmeng Wang, Zhiwen Dai, Shuai Wang, Jian Zhou, Fu Xiao, Tony Q. S. Quek, Tsung-Hui Chang  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.22035v1  

#### Abstract
Federated Fine-Tuning (FFT) has attracted growing interest as it leverages both server- and client-side data to enhance global model generalization while preserving privacy, and significantly reduces the computational burden on edge devices by avoiding training from scratch. Despite these advantages...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Robust Federated Fine-Tuning in Heterogeneous Networks with Unreliable Connections: An Aggregation View

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Federated Fine-Tuning (FFT)** 在现实异构网络中面临的两大挑战：
- **数据异质性**（Non-i.i.d. 数据分布）：客户端私有数据与服务器公共数据之间、以及不同客户端之间的数据分布差异显著。
- **通信不可靠性**：由于设备故障、信号弱覆盖、电池耗尽等原因，客户端与服务器之间的连接存在频繁且模式各异的中断（如瞬时、间歇性或混合型失败），导致模型聚合偏差。

现有方法通常假设同质网络环境或需要先验的连接失败概率，这在真实商业网络（包含有线、Wi-Fi、4G、5G等多种标准）中不切实际。

---

### 提出的新方法：FedAuto
作者提出了一种名为 **FedAuto** 的新型 FFT 框架，其核心是通过**自适应聚合策略**来缓解上述双重挑战的影响。

#### 创新思路
1. **无需修改基础设施或依赖先验知识**  
   FedAuto 完全在服务器端运行，仅通过调整聚合权重实现鲁棒性，无需对客户端进行任何修改，也无需预知连接失败的概率或模式，具备“即插即用”特性。

2. **双模块自适应聚合机制**
   - **Module 1: Server-Side Compensatory Training（服务器侧补偿训练）**  
     当某些类别的本地更新因连接失败而缺失时，服务器利用其拥有的公共数据子集 $D_{\text{miss}}$ 对这些缺失类别进行额外训练，生成一个补偿模型 $w_{\text{miss}}$，以弥补知识空缺。
   - **Module 2: Aggregation Weight Optimization（聚合权重优化）**  
     将服务器本地模型、补偿模型及成功接收到的客户端模型的聚合权重设为可调变量，并通过求解一个凸的加权最小二乘问题，使每个类的有效贡献比例尽可能接近全局数据分布中的真实比例 $\alpha_{g,c}$，从而消除偏置。

3. **新颖的 per-round aggregation 分析视角**
   - 放弃传统基于期望的收敛分析（需假设失败概率已知），转而采用每轮实际聚合结果的确定性视角。
   - 将连接失败和客户端选择的影响统一嵌入到聚合权重中，实现了对**每一次具体执行过程**（each individual realization）的收敛保证，理论更强。

---

### 相比现有方法的优势
| 方面 | 现有方法局限 | FedAuto 优势 |
|------|---------------|-------------|
| **通信可靠性建模** | 需要估计失败概率（如 TF-Aggregation, FedAWE）或优化物理资源（如 ResourceOpt） | 不需要任何先验知识，适用于复杂多变的真实网络 |
| **部署灵活性** | 物理层优化难以跨标准实施；客户端需配合（如 SCAFFOLD 双向传输） | 仅服务器端操作，兼容现有系统，易于部署 |
| **理论保障** | 多数为期望意义下的收敛 | 提供每次运行都成立的强收敛保证 |
| **性能稳定性** | 在非 i.i.d. + 不可靠连接下表现不稳定甚至崩溃（见 Fig. 2） | 在各种失败模式和数据分布下均保持稳定高性能 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **MNIST**: 10 类手写数字图像
- **CIFAR-10**: 10 类自然图像
- **CIFAR-100**: 100 类细粒度自然图像

每种数据集均测试了两种规模的模型：
- 小模型：CNN / ResNet
- 大模型：ViT（用于 LoRA 微调）

---

### 实验设置
- **客户端数量**：$N = 20$
- **网络异构性模拟**：客户端通过不同通信方式接入服务器：
  - 有线（Ethernet）
  - Wi-Fi（2.4GHz 和 5GHz）
  - 4G
  - 5G
- **连接失败模式**：
  - **Transient**：由信道波动引起的短时中断
  - **Intermittent**：由设备断电等引起的时间较长的持续中断
  - **Mixed**：两者结合
- **参与率**：
  - Full Participation: $K=20$
  - Partial Participation: $K=10$

---

### 评估指标
- **主指标**：测试准确率（Testing Accuracy %）
- **收敛行为**：绘制多个通信轮次下的准确率曲线
- **消融实验**：验证两个模块（补偿训练 vs 权重优化）的独立作用

---

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **中心化训练** | Centralized(Public) |
| **经典 FL** | FedAvg, FedProx, SCAFFOLD |
| **改进聚合** | FedLAW, TF-Aggregation |
| **应对连接失败** | FedAWE |
| **FFT专用** | FedEx-LoRA |
| **资源优化** | ResourceOpt-1（联合优化）、ResourceOpt-2（分标准优化） |
| **理想情况** | FedAvg(Ideal) —— 无连接失败的理想版本 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（摘录自 Tables 1–4）

#### 表格汇总（Mixed Failures, Non-i.i.d., Full Participation, Full-Parameter Fine-Tuning）

| 方法 | MNIST (%) | CIFAR-10 (%) | CIFAR-100 (%) |
|------|-----------|--------------|----------------|
| **FedAuto (Ours)** | **97.96 ± 0.25** | **77.00 ± 0.55** | **51.43 ± 1.49** |
| FedAvg | 93.28 ± 1.18 | 68.35 ± 0.34 | 47.85 ± 1.06 |
| FedProx | 93.45 ± 1.39 | 68.27 ± 0.38 | 47.54 ± 1.01 |
| SCAFFOLD | 97.05 ± 0.48 | 74.58 ± 0.46 | 43.87 ± 0.88 |
| FedLAW | 94.46 ± 1.46 | 10.00 ± 0.00 | 51.40 ± 1.49 |
| FedAWE | 67.97 ± 9.00 | 68.05 ± 1.54 | 34.60 ± 0.59 |
| **FedAvg(Ideal)** | 98.35 ± 0.16 | 77.19 ± 0.77 | 50.57 ± 1.33 |

> ✅ **观察**：FedAuto 在所有任务上均显著优于其他基线，尤其在 CIFAR-10 上提升明显（+8.65%），且接近理想上限。

---

#### LoRA 微调结果（Non-i.i.d., Mixed Failures）

| 方法 | CIFAR-10 (%) | CIFAR-100 (%) |
|------|--------------|----------------|
| **FedAuto (Ours)** | **95.69 ± 0.13** | **85.15 ± 0.29** |
| FedAvg | 93.96 ± 0.45 | 81.82 ± 0.66 |
| FedEx-LoRA | 93.86 ± 0.55 | 80.13 ± 1.76 |
| **FedAvg(Ideal)** | 95.76 ± 0.13 | 86.14 ± 0.21 |

> ✅ **观察**：FedAuto 在 LoRA 场景下依然领先，且在更复杂的 CIFAR-100 上优势更大（+3.33%）。

---

### 与资源优化方法对比（Fig. 5）
- **ResourceOpt-1**：试图均衡所有客户端失败概率，但受限于最差链路，导致有效连接数下降，性能波动大。
- **ResourceOpt-2**：按通信标准分别优化，仍无法解决跨标准间的不平衡问题。
- **FedAuto**：无需干预底层资源，在各种失败模式下均表现出**更稳定、更高精度**的收敛性能。

---

### 消融实验结果（Table 5）

| 设置 | MNIST | CIFAR-10 | CIFAR-100 |
|------|--------|----------|------------|
| **完整 FedAuto**（补偿 + 权重优化） | **97.96** | **77.00** | **51.43** |
| 仅补偿训练（无权重优化） | 96.68 | 73.83 | 49.75 |
| 仅权重优化（无补偿训练） | 94.06 | 72.21 | 47.88 |
| 无任何增强（原始 FedAvg） | 93.28 | 68.35 | 47.85 |

> 🔍 **结论**：
- 两个模块均有显著贡献；
- **补偿训练**主要用于填补缺失类别；
- **权重优化**进一步精细化平衡各类别贡献；
- 二者协同作用带来最大增益。

---

## 4. 关键结论和发现

### 主要发现
1. **连接不可靠性会加剧数据异质性的负面影响**  
   在非 i.i.d. 数据下，连接频繁中断会导致部分客户端长期无法参与聚合，造成模型偏向活跃客户端的数据分布，严重损害泛化能力（见 Theorem 1 中 term (14b)）。

2. **标签相关异质性（label-related heterogeneity）是主导误差源**  
   实验和理论表明，在非 i.i.d. 场景中，类别分布偏差带来的梯度偏移远大于特征内部差异，因此应优先校正类别贡献比例。

3. **FedAuto 能有效抑制聚合偏差**  
   通过补偿训练和动态权重调整，FedAuto 成功将有效类别分布逼近全局分布（$\hat{\alpha}_c \approx \alpha_{g,c}$），从而使收敛偏差趋近于零。

4. **优于通信资源优化策略**  
   即使与专门设计的物理层资源分配方法相比，FedAuto 也能取得更好或相当的性能，且无需改变网络配置，更具实用性。

---

### 方法的局限性
- **依赖服务器拥有完整的类别覆盖**  
  当前假设服务器的公共数据包含所有客户端可能出现的类别。若存在“全新类别”，则补偿训练无法生效。未来可通过 **class-incremental learning** 或 **diffusion-based data generation** 扩展。
- **计算开销集中在服务器**  
  虽然边缘设备负担轻，但服务器需执行额外训练和优化，对大规模模型可能构成压力。
- **未考虑隐私泄露风险**  
  客户端需共享本地类别分布 $\{\alpha_{i,c}\}$，虽信息量小，但仍存在潜在隐私泄露风险，可结合 **secure multi-party computation** 进一步加固。

---

### 未来工作方向
1. 扩展至 **Personalized FL** 场景，支持个性化模型输出。
2. 探索 **semi-supervised** 和 **time-varying data distributions** 下的鲁棒 FFT。
3. 结合 **class-incremental learning** 与 **generative models**，处理服务器未知类别的场景。
4. 设计更高效的聚合权重求解算法，降低服务器计算负担。

--- 

> ✅ **总体评价**：  
> 本文提出的 **FedAuto** 是一种简洁而强大的 FFT 框架，从**聚合视角**出发，巧妙地解决了异构网络中连接不可靠与数据非独立同分布的耦合难题。其实验充分、理论严谨，兼具实用性和先进性，为联邦学习在真实工业场景中的落地提供了重要参考。

</details>

---

### 11. [DuaDeep-SeqAffinity: Dual-Stream Deep Learning Framework for Sequence-Only Antigen-Antibody Affinity Prediction](https://arxiv.org/abs/2512.22007)

**Authors**: Aicha Boutorh, Soumia Bouyahiaoui, Sara Belhadj, Nour El Yakine Guendouz, Manel Kara Laouar  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.22007v1  

#### Abstract
Predicting the binding affinity between antigens and antibodies is fundamental to drug discovery and vaccine development. Traditional computational approaches often rely on experimentally determined 3D structures, which are scarce and computationally expensive to obtain. This paper introduces DuaDee...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DuaDeep-SeqAffinity: Dual-Stream Deep Learning Framework for Sequence-Only Antigen-Antibody Affinity Prediction

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统抗原-抗体亲和力预测方法严重依赖实验测定的三维（3D）结构，而这类结构获取成本高、耗时长，且在抗体-抗原复合物中（尤其是CDR-H3环）存在高度构象灵活性，导致结构建模困难。这构成了“**结构性瓶颈**”（structural bottleneck），限制了高通量药物筛选和疫苗设计。

### 提出的新方法与创新思路
本文提出 **DuaDeep-SeqAffinity** ——一种**仅基于氨基酸序列**（sequence-only）的双流深度学习框架，用于抗原-抗体亲和力预测。其核心创新包括：

- **Dual-Stream Hybrid Architecture**：采用并行的双分支结构：
  - **Transformer 分支**：捕捉序列中的**全局上下文依赖关系**（long-range global dependencies），理解蛋白质折叠模式和稳定性。
  - **1D CNN 分支**：提取**局部序列模体**（local motifs），识别结合热点等精细特征。
- **多尺度特征融合机制**：通过专门设计的融合模块整合来自Transformer和CNN的互补特征，实现对结合界面物理化学特性的全面建模。
- **完全脱离3D结构输入**：仅使用原始氨基酸序列作为输入，利用预训练的 **ESM-2 蛋白质语言模型**（Protein Language Model, PLM）生成高质量嵌入表示。

### 相比现有方法的优势
- **优于纯序列模型**：相比单一架构（如仅用Transformer或CNN）或其他序列模型（如DG-Affinity、MVSF-AB），DuaDeep在多个指标上取得更优性能。
- **超越结构-序列混合模型**：在AUC指标上甚至超过了需要3D结构输入的 **WALLE-Affinity** 模型，证明高质量序列嵌入足以替代部分结构信息。
- **高效可扩展**：无需结构预测步骤，显著降低计算开销，适用于大规模抗体库的快速筛选。

---

## 2. 核心实验方法和设置

### 数据集
- 使用公开数据集 **AbRank**，包含经实验验证的抗原-抗体配对及其结合亲和力（Kd值）。
- 输入为抗原及抗体重链/轻链的氨基酸序列。
- 经过清洗后，将Kd转换为负对数形式 `pKd = 9 - log10(Kd)` 并进行标准化处理。

### 实验设置
- **数据划分**：随机分为 80% 训练集、10% 验证集、10% 测试集，确保抗原和抗体序列在各集合间无重叠，以评估泛化能力。
- **序列编码**：使用 **ESM-2**（`facebook/esm2_t12_35M_UR50D`）生成固定维度的上下文化嵌入向量。
- **模型冻结策略**：ESM-2 编码器权重在训练过程中保持冻结（frozen），仅微调后续网络参数。

### 评估指标
- 回归任务指标：
  - **RMSE**（Root Mean Square Error）
  - **MAE**（Mean Absolute Error）
  - **R²**（Coefficient of Determination）
  - **Pearson 相关系数**
  - **Spearman 相关系数**
- 分类/排序任务指标：
  - **AUC**（Area Under the ROC Curve）

### 基线方法对比
- **内部基线**：
  - **ESM-T**（ESM-2 + Transformer-only）
  - **ESM-C**（ESM-2 + CNN-only）
- **外部SOTA模型**：
  - **DG-Affinity**（sequence-only, ConvNeXt 架构）
  - **MVSF-AB**（sequence-only, 多视图CNN+MLP）
  - **WALLE-Affinity**（structure + sequence, GNN-based）
  - **MINT** 和 **ESM-2 + AntiBERTy**（均为sequence-only ranking models）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（测试集）

| 模型 | RMSE | R² | Pearson | AUC |
|------|------|-----|---------|-----|
| **DuaDeep (Ours)** | **0.737** | **0.460** | **0.688** | **0.890** |
| ESM-C (CNN-only) | 0.773 | 0.397 | 0.636 | 0.854 |
| ESM-T (Transformer-only) | 0.799 | 0.357 | 0.625 | 0.852 |
| DG-Affinity [44] | – | – | 0.656 | – |
| MVSF-AB [46] | 1.447 | 0.467 | – | – |
| WALLE-Affinity (Ranking) [50] | – | – | – | 0.866 |

> 注：部分外部模型未报告所有指标，表格依据原文引用补充。

### 与基线方法的对比结果
- DuaDeep 在 **所有回归指标上均优于单分支模型**（ESM-C 和 ESM-T）：
  - RMSE 下降 > 4.6%
  - Pearson 提升约 5–6%
- 相比当前最优序列模型 **DG-Affinity**（Pearson=0.656），DuaDeep 达到 **0.688**，显著提升。
- 尽管 **MVSF-AB** 的 R² 接近（0.467 vs 0.460），但其 RMSE 高达 1.447，说明预测误差更大。
- 在 **AUC 上达到 0.890**，不仅超过所有纯序列模型（MINT: 0.775, ESM-2+AntiBERTy: 0.761），还**超越了依赖3D结构的 WALLE-Affinity (0.866)**。

### 消融实验结果
- **双流结构有效性验证**：
  - 单独使用 CNN 或 Transformer 均表现次优，表明两者提取的信息具有互补性。
  - CNN 分支在局部特征（如结合位点）建模上略强于 Transformer；而 Transformer 更擅长捕捉长程依赖。
- **融合策略重要性**：
  - 特征拼接（concatenation）后的联合表示能有效增强最终预测头的表现。
  - 学习曲线显示，双流模型收敛更快、验证损失更低（见 Figure 7），泛化能力更强。

---

## 4. 关键结论和发现

### 主要发现
- **高质量序列嵌入足以支撑高精度亲和力预测**：通过 ESM-2 提供的上下文化表示，结合多尺度特征提取，可以在不依赖任何3D结构的情况下达到甚至超越结构辅助模型的性能。
- **多尺度建模至关重要**：同时捕捉**全局上下文**（via Transformer）和**局部模体**（via CNN）是提升预测准确率的关键。
- **双流架构优于单一流架构**：实验证明，Transformer 与 CNN 的协同作用带来了显著增益，验证了“**global + local = better binding signature**”的设计理念。
- **序列优先范式正在逼近结构模型性能**：本研究表明，随着 PLM 和深度学习架构的发展，**sequence-only 方法已成为结构方法的强大替代方案**。

### 方法的局限性
- 当前模型未显式建模抗原与抗体之间的残基级交互（如cross-attention机制），可能遗漏细粒度相互作用信号。
- 所有实验基于 pKd 回归任务，对于突变效应或相对亲和力排序任务尚未充分验证。
- 模型解释性有限，缺乏可视化工具揭示关键结合残基或驱动因素。

### 未来工作方向
- 引入 **Cross-Attention 机制**，增强抗原-抗体间的交互建模。
- 采用 **pairwise ranking loss** 替代回归损失，提升对实验噪声的鲁棒性。
- 开发 **可解释性模块**，生成残基级别的重要性热图（interaction maps），辅助抗体工程优化。
- 扩展至 **neutralization potency prediction** 或 **variant escape prediction** 等临床相关任务。

---

> ✅ 总结：  
> **DuaDeep-SeqAffinity 成功证明了“从序列到功能”的可行性，在无需3D结构的前提下实现了SOTA级别的抗原-抗体亲和力预测。该工作为高通量抗体发现、个性化免疫治疗和快速响应疫情提供了高效、可扩展的AI解决方案。**

</details>

---

### 12. [Knowledge Reasoning of Large Language Models Integrating Graph-Structured Information for Pest and Disease Control in Tobacco](https://arxiv.org/abs/2512.21837)

**Authors**: Siyu Li, Chenwei Song, Wan Zhou, Xinyi Liu  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21837v1  

#### Abstract
This paper proposes a large language model (LLM) approach that integrates graph-structured information for knowledge reasoning in tobacco pest and disease control. Built upon the GraphRAG framework, the proposed method enhances knowledge retrieval and reasoning by explicitly incorporating structured...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Knowledge Reasoning of Large Language Models Integrating Graph-Structured Information for Pest and Disease Control in Tobacco*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该研究针对烟草病虫害防治领域中，**大型语言模型（LLMs）在进行知识推理时难以有效利用结构化关系信息**的问题。传统方法如 Prompt Engineering 或简单的知识注入往往忽略了实体之间的复杂关联（如“疾病-症状-防治方法”之间的多跳关系），导致推理能力受限，尤其在多跳推理（multi-hop reasoning）和比较推理（comparative reasoning）任务中表现不佳。

### 🚀 提出的新方法/新思路
提出了一种基于 **GraphRAG 框架**的图增强型大语言模型方法，将图结构信息深度融合到 LLM 的推理过程中，具体包括：

- 利用 LLM 辅助构建一个**烟草病虫害领域的知识图谱（Knowledge Graph）**，涵盖疾病、症状、防治方法等实体及其关系。
- 引入 **TransE 进行图嵌入学习（Graph Embedding）**，捕捉实体与关系的语义表示。
- 使用 **图神经网络（GCN）** 对节点进行高阶邻域聚合，生成更丰富的上下文感知的节点表示。
- 将 GCN 学习到的图表示与 **ChatGLM** 的文本输入进行融合，形成增强的上下文输入，指导 LLM 更准确地生成答案。

### 🔍 相比现有方法的优势
| 方法 | 局限性 | 本文优势 |
|------|--------|---------|
| 纯 LLM（如 ChatGLM） | 缺乏外部知识支持，易产生幻觉 | 显式引入结构化知识，提升准确性 |
| KGE + LLM | 仅使用扁平化的实体嵌入，忽略图结构 | 利用 GCN 捕捉局部与全局图结构信息 |
| RAG（Retrieval-Augmented Generation） | 基于文本相似度检索，难以理解复杂关系 | 基于知识图谱结构进行精准的知识检索与推理 |
| 传统机器学习方法 | 难以处理自然语言问答与复杂逻辑推理 | 结合 LLM 的语义理解能力和图结构的逻辑表达能力 |

> **核心创新**：实现了 **结构化知识（Symbolic）与神经语言模型（Neural）的深度协同**，通过 GraphRAG 实现从“局部事实匹配”向“全局关系推理”的跃迁。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 自建了一个**烟草病虫害知识图谱**，包含超过 1,000 个实体和关系。
  - 实体类型：疾病（如 tobacco mosaic disease）、症状、防治方法（如 spraying antiviral agents）、农药成分、致病因素等。
  - 关系类型：`treated_by`, `prevented_by`, `caused_by` 等。
- 构建了配套的**问答数据集**，用于评估模型性能，包含三类任务：
  1. **直接问答（Direct QA）**
  2. **多跳推理（Multi-hop Reasoning）**
  3. **比较推理（Comparative Reasoning）**

数据来源：农业专家知识库、病害管理手册、学术文献。

### ⚙️ 实验设置
- **基础 LLM**：采用 **ChatGLM** 作为主干模型。
- **参数高效微调**：使用 **LoRA**（Low-Rank Adaptation）进行微调，LoRA rank 设为 16。
- **图表示学习**：
  - 图嵌入：**TransE**，维度 100，学习率 0.01。
  - 节点表示学习：两层 **GCN**，ReLU 激活函数。
- **融合机制**：在 GraphRAG 框架下，将 GCN 输出的图嵌入与 Sentence-BERT 生成的查询嵌入拼接后输入 ChatGLM。

### 📊 评估指标
采用标准分类与问答评价指标：
- **Accuracy（准确率）**
- **Precision（精确率）**
- **Recall（召回率）**
- **F1-score（F1 分数）**

测试集中在知识密集型推理任务上的表现。

### 🔁 基线方法对比
| 基线模型 | 描述 |
|--------|------|
| **ChatGLM** | 不依赖任何外部知识的原始 LLM |
| **TransE + ChatGLM** | 使用 TransE 嵌入注入知识 |
| **KGE + ChatGLM** | 基于知识图谱嵌入的方法 |
| **RAG + ChatGLM** | 基于文本检索的标准 RAG 方法 |
| **GraphRAG + ChatGLM** | 本文提出的方法（GCN + TransE + LLM 融合） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）
| Model | Accuracy (%) | Precision (%) | Recall (%) | F1-score (%) |
|-------|--------------|---------------|------------|---------------|
| ChatGLM | 75.2 | 78.5 | 72.1 | 75.2 |
| KGE + ChatGLM | 82.5 | 85.3 | 79.8 | 82.4 |
| RAG + ChatGLM | 85.7 | 87.9 | 83.2 | 85.5 |
| **GraphRAG + ChatGLM** | **90.1** | **92.3** | **88.2** | **90.2** |

> ✅ 所有指标均显著优于所有基线方法，特别是在 **Accuracy 和 F1-score 上提升明显**。

### 🔍 与基线方法的对比分析
- 相比纯 **ChatGLM**，引入结构化知识使 Accuracy 提升近 **15 个百分点**，说明外部知识对农业领域推理至关重要。
- **KGE + ChatGLM** 表现优于纯 LLM，但弱于 RAG 和 GraphRAG，表明仅靠嵌入无法充分建模复杂关系。
- **RAG + ChatGLM** 表现良好，但在涉及多跳或比较逻辑的问题上容易误检无关文本片段。
- **GraphRAG + ChatGLM** 在以下场景表现出更强推理能力：
  - 多跳问题：“如何通过综合措施控制由蚜虫传播的烟草花叶病？”
  - 比较问题：“生物防治与化学防治哪种更适合长期防控？”

### 🔧 消融实验（Ablation Study）分析（文中虽未列详细表格，但有讨论）
- 移除 GCN 后性能下降明显 → 证明 **GCN 对捕获高阶邻居信息至关重要**。
- 若仅使用 TransE 而不接入 GCN → 推理深度不足，尤其影响 multi-hop 任务。
- 若不融合图嵌入 → 回退至普通 RAG 或 KGE 水平，验证了**图-LLM 融合机制的有效性**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **图结构信息能显著增强 LLM 在农业领域的推理能力**，尤其是在需要逻辑链推导的任务中。
2. **GCN 比单纯的 KGE 更适合建模农业知识图谱中的复杂关系网络**，能够聚合多层次上下文信息。
3. **GraphRAG 框架为结构化知识与 LLM 的融合提供了有效路径**，实现了从“检索”到“推理”的升级。
4. 该方法特别适用于**多跳推理**和**比较型决策支持任务**，具备实际应用潜力。

### ⚠️ 方法的局限性
1. 当前构建的知识图谱规模较小（约 1,000 条三元组），限制了模型的泛化能力。
2. 使用固定的图嵌入和特定的 GCN 架构，缺乏动态更新与自适应优化机制。
3. 实验集中于中文烟草领域，跨作物、跨语言的可迁移性有待验证。

### 🔮 未来工作方向
1. 构建更大规模、覆盖更多作物的农业知识图谱。
2. 探索更先进的 GNN 架构（如 GAT、Temporal GNN）以建模动态病害演化过程。
3. 设计更高效的图-LLM 融合策略（如中间层注入、注意力引导）。
4. 将框架扩展至其他知识密集型领域，如 **Healthcare、Intelligent Decision-Making Systems**。

---

## 总结
本论文成功将 **GraphRAG 框架应用于烟草病虫害防治领域**，通过整合 **TransE、GCN 与 ChatGLM**，实现结构化知识与语言模型的深度融合。实验证明，该方法在 Accuracy、F1-score 等关键指标上显著超越多种基线，在多跳与比较推理任务中展现出强大潜力，为智能农业决策系统提供了一条可行的技术路径。

</details>

---

### 13. [Demystifying ARM SME to Optimize General Matrix Multiplications](https://arxiv.org/abs/2512.21473)

**Authors**: Chencheng Deng, Weiling Yang, Jianbin Fang, Dezun Dong  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21473v1  

#### Abstract
General Matrix Multiplication (GEMM) is a critical kernel in high-performance computing and deep learning. While modern architectures like ARM's Scalable Matrix Extension (SME) introduce dedicated hardware for matrix operations, existing linear algebra libraries fail to fully exploit its potential, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Demystifying ARM SME to Optimize General Matrix Multiplications 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代 ARM 架构引入了 **Scalable Matrix Extension (SME)** 来加速矩阵运算，特别是 **General Matrix Multiplication (GEMM)**，这是高性能计算（HPC）和深度学习中的核心操作。然而，现有的开源线性代数库（如 LIBXSMM、OpenBLAS、KleidiAI）未能充分挖掘 SME 在处理大规模矩阵时的潜力，存在以下关键瓶颈：

- **Cache 利用效率低**：简单三层循环设计导致子块大小与共享 L2 Cache 不匹配，造成大量 Cache Miss 和 TLB Miss。
- **内存带宽未充分利用**：现有实现多采用单 Z 寄存器加载，无法发挥 SME2 支持的四 Z 寄存器并行加载高达 900 GB/s 的带宽优势。
- **仅打包一个输入矩阵**：为减少开销，多数库只对一个输入矩阵进行预打包，当另一个大矩阵超出 L2 Cache 容量时，访问变得不连续且低效。

### 提出的新方法与创新点
本文提出了 **MPGEMM** —— 一个面向 ARM SME 架构的开源高性能多精度 GEMM 库，其核心创新包括：

#### ✅ 1. **Cache-Aware 分块算法设计**
- 将传统的三重循环重构为 **六级分块算法**（six-level blocked algorithm），通过解析模型指导 `mc`, `nc`, `kc` 等分块参数的选择。
- 考虑 L2 Cache 大小（8MB）、TLB 容量和内存带宽限制，最大化缓存局部性和计算访存比（CMR）。
- 显著提升数据在 L2 Cache 中的驻留时间，减少 Cache/TLB Miss。

#### ✅ 2. **高效的双矩阵打包策略**
- **On-the-fly Transposition for Matrix A**：利用 SME 的 ZA tile 存储结构，在加载过程中通过水平写入、垂直切片的方式实现零开销转置，避免 gather 操作。
- **First-Round Online Packing for Matrix B**：在第一轮计算中边执行 FMOPA 边打包 B 矩阵，将打包延迟隐藏在计算流水线中，降低总内存开销。

#### ✅ 3. **高吞吐微内核设计**
- 微内核始终使用 **四个 Z 寄存器组（four-Z-register loads）** 进行数据加载，以达到接近 900 GB/s 的峰值内存带宽。
- 充分利用所有可用的 **ZA Tiles**（例如 FP32 下使用全部 4 个 ZA.S tiles），确保计算单元饱和。
- 设计主微内核（main kernel）为 16×64 形状，并配合边缘微内核（edge kernel）处理边界情况，保持高性能。

#### ✅ 4. **支持混合精度 GEMM**
- 扩展至 **FP16→FP32** 和 **INT8→INT32** 等混合精度模式，利用 SME 原生支持的低精度输入、高精度累加特性。
- 针对 FP16 数据设计了基于 ZIP 指令的垂直交错打包方法，保证高效向量化访问。

### 相比现有方法的优势
| 特性 | MPGEMM | LIBXSMM / OpenBLAS / KleidiAI |
|------|--------|-------------------------------|
| 分块策略 | 六级分块，Cache-aware | 三级循环，固定分块 |
| 数据打包 | 双矩阵打包 + 在线打包 | 单矩阵打包或全预打包 |
| 内存加载粒度 | 四 Z 寄存器组加载（~900 GB/s） | 单/双寄存器加载（≤460 GB/s） |
| ZA Tile 利用率 | 全部使用（4 tiles for FP32） | 边界常降为 2 tiles |
| 并行化支持 | 多 SME 单元任务动态分配 | 多数无并行支持 |
| 开源透明性 | 完全开源 | Accelerate 闭源 |

---

## 2. 核心实验方法和设置

### 实验平台
- **硬件**：Apple M4 Pro 芯片
  - P-core 集群：10 核，4.4GHz，每集群共享 16MB L2 Cache 和一个 SME 单元
  - E-core 集群：4 核，2.85GHz，共享 4MB L2 Cache 和一个 SME 单元
  - Streaming SVL = 512 bits → Z 寄存器 = 64 字节，ZA 存储 = 64×64 bytes
- **操作系统**：macOS 15.1
- **编译器**：AppleClang 16

### 工作负载（Workloads）
来自真实大模型的 GEMM 配置：
- **DeepSeek 模型**（ID 1–18）：涵盖不同规模的大矩阵乘法
- **LLaMA 模型**（ID 19–24）：包含“瘦长”（skinny）矩阵场景
- 包括规则尺寸和非整除微内核尺寸的 **不规则矩阵** 测试（M/N ∈ {80,110,140,170,200}, K=25600）

### 评估指标
- **性能指标**：GFLOPS（单精度）、GOPS（整型）、实测内存带宽
- **对比方式**：几何平均速度提升（Geometric Mean Speedup）
- **重复次数**：每个实验运行 5 次取算术均值，含预热轮次

### 基线方法对比
| 库名 | 是否开源 | SME 支持 | 精度支持 | 并行能力 |
|------|----------|-----------|------------|-------------|
| **Apple Accelerate** | ❌ 闭源 | ✅ SME2 | FP32/FP64 | ✅ 多 SME |
| **LIBXSMM** | ✅ 开源 | ✅ SME2 | FP32/small GEMM | ❌ |
| **OpenBLAS** | ✅ 开源 | ✅ SME（非 SME2 向量指令） | FP32 | ❌ |
| **KleidiAI** | ✅ 开源 | ✅ SME2 | FP32/BF16/INT8 | ❌ |

> 注：BLIS 等其他库因缺乏 SME 支持被排除。

---

## 3. 主要实验结果和性能指标

### 单 SME 单元性能（FP32）
- **相比开源库显著领先**：
  - 比 **LIBXSMM** 快 **1.95×**
  - 比 **KleidiAI** 快 **2.34×**
  - 比 **OpenBLAS** 快 **2.85×**
- **超越厂商优化库**：
  - 在行优先存储下比 **Accelerate** 快 **1.21×**
  - 在列优先存储下快 **1.18×**

### 多 SME 单元并行性能
- 动态调度任务到多个 SME 单元，实现近两倍加速。
- 相比 Accelerate：
  - 行优先：**1.24×**
  - 列优先：**1.22×**
- 相比无并行能力的开源库：
  - 比 **LIBXSMM** 快 **3.96×**
  - 比 **KleidiAI** 快 **4.69×**
  - 比 **OpenBLAS** 快 **5.7×**

### 双精度 GEMM（FP64）
- 尽管 SME 上 FP64 吞吐仅为 FP32 的 1/4（峰值 ~501 GFLOPS），MPGEMM 仍能在多核下实现比 Accelerate **1.18×** 的加速。

### 不规则矩阵性能
- 使用 predication 寄存器处理非整除尺寸。
- MPGEMM 凭借优化的边缘微内核（64×16 / 16×64）和高效打包，在 M/N 不是 tile 整数倍的情况下依然稳定领先。

### 混合精度 GEMM 性能
- **FP16→FP32 GEMM**：
  - 比 KleidiAI（BF16）快 **1.7×**
  - 更高的 compute-to-memory ratio 带来实际性能增益
- **INT8→INT32 GEMM**：
  - 峰值可达 **4010 GOPS**（单线程）
  - 实测达到 **7856 GOPS**（ID 14），约为理论峰值的 **94%**
  - 比 FP16 基线快约 **2×**

### 消融实验（Ablation Study）
在单 SME 单元上以 LIBXSMM 为基线逐步添加优化：
| 优化阶段 | 平均提速 |
|---------|----------|
| 基线（LIBXSMM） | 1.0× |
| + Multi-vector load（四寄存器加载） | **1.17×** |
| + Partitioning & Dual Packing（分块+双打包） | **1.62×** |
| + First-round Online Packing | 综合达 **1.95×** |

> 结论：**分块与双打包** 是最大贡献因素；**在线打包** 效果有限（因 FMOPA 周期不足以完全掩盖写回延迟）。

---

## 4. 关键结论和发现

### 主要发现
1. **SME 性能高度依赖 Cache 和内存访问模式**：
   - 子矩阵必须控制在 **8MB 以内** 才能维持高带宽（>900 GB/s）。
   - 数据布局需适配 **multi-vector load** 和 **outer product instruction** 的访存模式。

2. **四 Z 寄存器加载至关重要**：
   - 单寄存器加载仅提供 ~230 GB/s，而四寄存器组可达到 ~900 GB/s，差距巨大。
   - 必须重构微内核以持续使用 `LD1D/4Z` 类指令。

3. **ZA Tile 利用率决定计算吞吐上限**：
   - FP32 下应同时激活全部 4 个 ZA.S tiles。
   - 边缘处理不当会导致 tile 数下降一半，严重拖累性能。

4. **混合精度极具潜力**：
   - INT8→INT32 可达 **94% 峰值性能**，适合低比特推理。
   - 更高的计算密度缓解内存压力，尤其适用于大模型部署。

### 方法的局限性
- **平台依赖性强**：当前参数调优基于 Apple M4 Pro 的 L2 Cache（16MB）、SVL（512bit）等特性，迁移到其他 SME 平台需重新建模。
- **堆内存管理要求高**：需避免栈上分配缓冲区以防 Core 与 SME 内存顺序冲突引发 pipeline stall。
- **尚未支持所有数据类型组合**：如 BFloat16 或更低比特（INT4）暂未覆盖。

### 未来工作方向
- 推广分析模型至更多 SME 架构芯片（如 AWS Graviton、Ampere Altra）。
- 支持更多混合精度格式（如 FP8、INT4）及稀疏矩阵运算。
- 结合编译器自动调优（Auto-tuning）进一步提升跨平台可移植性。
- 扩展至 Convolution、Attention 等更复杂算子优化。

---

> **总结一句话**：  
> MPGEMM 通过深入剖析 ARM SME 架构特性，提出了一套系统性的 GEMM 优化框架——从 cache-aware 分块、双矩阵高效打包到 fully-utilized 微内核设计，首次实现了在开源库中全面释放 SME 的性能潜力，尤其在大规模和混合精度场景下大幅超越闭源方案。

</details>

---

### 14. [A Reinforcement Learning Approach to Synthetic Data Generation](https://arxiv.org/abs/2512.21395)

**Authors**: Natalia Espinosa-Dice, Nicholas J. Jackson, Chao Yan, Aaron Lee, Bradley A. Malin  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.21395v1  

#### Abstract
Synthetic data generation (SDG) is a promising approach for enabling data sharing in biomedical studies while preserving patient privacy. Yet, state-of-the-art generative models often require large datasets and complex training procedures, limiting their applicability in small-sample settings. In th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Reinforcement Learning Approach to Synthetic Data Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Synthetic Data Generation (SDG)** 方法（如 GANs 和 diffusion models）通常依赖大规模训练数据和复杂的训练过程，导致其在**小样本生物医学数据集**上的适用性受限。此外：
- **GANs** 存在训练不稳定、易发生 mode collapse 和梯度消失等问题；
- **Diffusion models** 虽然性能优越，但计算成本高、需要大量数据，且存在**记忆化风险**（memorization），可能泄露隐私。

这些问题限制了它们在真实世界中小型临床研究中的应用。

### 提出的新方法：RLSyn
本文提出 **RLSyn** ——一种基于**强化学习（Reinforcement Learning, RL）** 的新型合成数据生成框架，将 SDG 重新建模为一个 RL 任务：
- 将生成器视为一个**随机策略（stochastic policy）**，输出整个患者记录的概率分布；
- 使用 **Proximal Policy Optimization (PPO)** 算法进行优化；
- 判别器仅作为**奖励信号提供者**（reward estimator），不参与生成器的梯度更新；
- 引入轻量级均值匹配损失（mean-matching penalty）以稳定训练。

该方法属于 **inverse RL** 范畴，通过最大化判别器提供的“真实性”奖励来优化生成策略。

### 相比现有方法的优势
- **更稳定的训练过程**：PPO 的 clipped surrogate objective 避免了 GANs 中常见的对抗失衡问题；
- **更高的数据效率**：在小样本场景下表现优于 GAN 和 diffusion 模型；
- **更好的隐私保护潜力**：避免直接复制训练数据，降低 membership inference 攻击成功率；
- **更强的多样性生成能力**：显式建模输出分布的方差，鼓励多样化采样。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|-------|------|
| **AI-READI** | 小规模糖尿病队列数据，包含 1067 名患者，共 4,537 个 person-day 记录，每条含 108 个特征（72 连续 + 36 分类）。用于测试小样本场景。 |
| **MIMIC-IV** | 大规模重症监护电子病历数据，包含约 18 万住院患者记录，用于评估大样本下的性能。 |

### 实验设置
- 数据划分：
  - AI-READI：90%/10% 训练/测试
  - MIMIC-IV：70%/30% 训练/测试
- 每个模型在 10 种不同的 train-test split 上重复训练并取平均结果；
- 超参数通过 **Optuna** 自动调优（20 trials），目标是最小化 S2R 分类任务的误差。

### 评估指标

#### （1）Privacy（隐私）
- **Membership Inference Attack (MIA)**：
  - 攻击者判断某真实个体是否出现在训练集中；
  - 衡量标准：AUROC（越接近 0.5 表示越安全）。

#### （2）Utility（实用性）
- **Synthetic-to-Real (S2R)** 分类任务：
  - 在合成数据上训练分类器，在真实测试集上评估；
  - AI-READI：预测 type 2 diabetes 类型；
  - MIMIC-IV：预测一年内死亡率；
  - 指标：AUC。

#### （3）Fidelity（保真度）
- **Real-to-Synthetic (R2S)**：在真实数据上训练，在合成数据上测试；
- **Column-wise Correlation Difference (CWC)**：衡量特征间相关性保留程度（越低越好）；
- **Normalized Mutual Information (NMI)**：比较真实与合成数据在 PCA + k-means 聚类后的标签一致性（越低表示分布越相似）；
- **Dimension-Wise Difference (DWD)**：
  - 对分类变量用 **Absolute Prevalence Difference (APD)**；
  - 对连续变量用 **Average Wasserstein Distance (AWD)**；
  - 综合得分越低越好。

### 基线方法对比
| 模型 | 类型 | 特点 |
|------|------|------|
| **EMR-WGAN** | GAN-based | 针对 EHR 设计的 WGAN-GP 架构，广泛用于医疗文本和表格数据 |
| **EHRDiff** | Diffusion-based | 专为结构化 EHR 设计的扩散模型，当前 state-of-the-art |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### （1）Privacy（表 1）
| 模型 | MIMIC-IV (AUROC) | AI-READI (AUROC) |
|------|------------------|------------------|
| Real Data | 0.998 | 1.000 |
| EMR-WGAN | 0.498 ± 0.002 | **0.501 ± 0.026** |
| EHRDiff | 0.499 ± 0.002 | 0.601 ± 0.036 |
| **RLSyn** | **0.499 ± 0.002** | **0.544 ± 0.030** |

> ✅ 所有模型在 MIMIC-IV 上均接近随机猜测（理想情况）；  
> ⚠️ 在 AI-READI 上，EHRDiff 明显更容易被攻击（>0.6），表明其存在记忆化倾向。

#### （2）Utility（图 2）
| 模型 | MIMIC-IV (S2R AUC) | AI-READI (S2R AUC) |
|------|--------------------|---------------------|
| EMR-WGAN | 0.764 | 0.747 |
| EHRDiff | 0.906 | 0.871 |
| **RLSyn** | **0.902** | **0.873** |

> ✅ RLSyn 在两个数据集上均显著优于 GAN，并与 diffusion 模型相当甚至略优。

#### （3）Fidelity（表 2）

| 模型 | MIMIC-IV CWC↓ | MIMIC-IV NMI↓ | MIMIC-IV DWD↓ |
|------|---------------|---------------|---------------|
| EMR-WGAN | 11.852 | 0.320 | 17.642 |
| EHRDiff | 7.848 | 0.003 | 2.797 |
| **RLSyn** | **8.877** | **0.001** | **2.073** |

| 模型 | AI-READI CWC↓ | AI-READI NMI↓ | AI-READI DWD↓ |
|------|---------------|---------------|---------------|
| EMR-WGAN | 1.352 | 0.809 | 77.056 |
| EHRDiff | 0.427 | 0.002 | 16.441 |
| **RLSyn** | **0.750** | **0.001** | **13.352** |

> ✅ RLSyn 在所有 fidelity 指标上全面优于 EMR-WGAN；
> ✅ 在 AI-READI 上，RLSyn 在 NMI 和 DWD 上优于 EHRDiff；
> 🔺 EHRDiff 在 CWC 上略优，但其 R2S 性能超过 R2R 基线，提示过拟合。

#### （4）可视化分析（图 3–4）
- **PCA 可视化** 显示 RLSyn 合成数据分布最接近真实数据；
- **Bigram plots** 显示 EMR-WGAN 明显偏向高频诊断，而 RLSyn 和 EHRDiff 更好地还原了罕见病种；
- **直方图对比** 显示 RLSyn 更好地覆盖了连续变量（如 BMI、血糖）的完整分布范围。

---

## 4. 关键结论和发现

### 主要发现
1. **RLSyn 在小样本场景下显著优于现有方法**：
   - 在 AI-READI 上，RLSyn 在 utility 和 fidelity 上均优于 EHRDiff 和 EMR-WGAN；
   - 同时保持较低的隐私风险（AUROC ≈ 0.54），远低于 EHRDiff（0.601）。

2. **RL 提供了一种更稳定、更高效的数据生成范式**：
   - PPO 的 clipped 更新机制有效缓解了 GAN 的训练不稳定性；
   - 不依赖长序列去噪过程，适合小数据场景。

3. **EHRDiff 存在明显的过拟合与记忆化风险**：
   - R2S 性能高于 R2R 基线 → 暗示模型“记住了”训练数据；
   - Membership inference 攻击成功率较高 → 存在隐私隐患。

4. **EMR-WGAN 表现最差**：
   - 虽然隐私性最好（可能是因未能学好数据分布）；
   - 但在 utility 和 fidelity 上大幅落后，说明其生成质量不足。

### 方法的局限性
1. 当前仅适用于**横断面表格型数据**（cross-sectional tabular data），尚未扩展到时间序列、文本或图像等复杂模态；
2. RLSyn 的优势机制尚不完全明确（是 PPO？还是 policy parameterization？）；
3. 理论基础有待深化，特别是与 inverse RL、energy-based models 的联系需进一步探索；
4. 奖励函数目前仍依赖判别器，未来可尝试无判别器的统计指标奖励。

### 未来工作方向
- 探索 RLSyn 在 **longitudinal EHR、clinical notes、medical imaging** 上的应用；
- 设计更灵活的奖励函数（如基于隐私预算、公平性、因果结构等）；
- 开展理论分析，揭示 RL-based SDG 成功的关键因素；
- 结合 differential privacy 或 federated learning，构建端到端隐私增强框架。

---

> 💡 **总结一句话**：  
> 本论文首次系统验证了 **Reinforcement Learning 可作为合成医疗数据生成的有效替代范式**，提出的 **RLSyn 框架在小样本条件下实现了比 GAN 和 diffusion 更优的隐私-效用权衡**，为资源有限的生物医学研究提供了新的工具路径。

</details>

---

### 15. [Gamayun's Path to Multilingual Mastery: Cost-Efficient Training of a 1.5B-Parameter LLM](https://arxiv.org/abs/2512.21580)

**Authors**: Alexander Podolskiy, Semen Molokov, Timofey Gerasin, Maksim Titov, Alexey Rukhovich, Artem Khrapov, Kirill Morozov, Evgeny Tetin, Constantine Korikov, Pavel Efimov, Polina Lazukova, Yuliya Skripkar, Nikita Okhotnikov, Irina Piontkovskaya, Meng Xiaojun, Zou Xueyi, Zhang Zhenhe  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.21580v1  

#### Abstract
We present Gamayun, a 1.5B-parameter multilingual language model trained entirely from scratch on 2.5T tokens. Designed for efficiency and deployment in resource-constrained environments, Gamayun addresses the lack of research on small non-English-centric LLMs by adopting a novel two-stage pre-train...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Gamayun's Path to Multilingual Mastery: Cost-Efficient Training of a 1.5B-Parameter LLM

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**小规模多语言大模型（SLM）研究不足**的问题，特别是针对非英语中心化的小型模型。现有小型 LLM 多为英语主导（>90% 英语训练数据），导致在其他语言上表现不佳。同时，从零开始训练多语言模型面临“多语言诅咒”（curse of multilinguality）——即加入多语言数据会降低主语言的数据效率。

此外，在资源受限环境下，如何以较低成本训练出高性能、可部署的多语言模型也是一个挑战。

### 提出的新方法与创新思路
作者提出了一个**两阶段预训练策略（two-stage pre-training strategy）**，其核心思想是分阶段优化语言对齐与性能迁移：

1. **第一阶段：平衡多语言训练（Balanced Multilingual Training）**
   - 初始阶段采用均衡的语言混合比例（约 37% 英语），确保不同语言之间的**跨语言对齐（cross-lingual alignment）**。
   - 此阶段有助于建立通用语义空间，避免低质量语言数据干扰高资源语言学习。

2. **第二阶段：高质量英语增强（High-Quality English Enrichment）**
   - 在完成初步对齐后，显著提升高质量英语数据的比例（增至 ~70%），引入富含知识和推理能力的数据（如 STEM、代码、书籍等）。
   - 这些高质量数据带来的能力可以通过已建立的跨语言表示迁移到所有支持的语言中，从而实现“性能转移”。

该策略无需依赖大规模蒸馏或合成数据生成，仅使用公开可用数据即可高效训练。

### 相比现有方法的优势
- **成本效益高**：总训练量仅为 **2.6T tokens**，远低于同类模型（如 Qwen2.5-1.5B 使用 18T，Qwen3 使用 36T）。
- **性能更强**：尽管训练预算更小，但在多数英文和多语言任务上优于 LLaMA3.2-1B 和 Qwen2.5-1.5B。
- **文化理解更深**：相比基于微调的俄语适配模型（如 ruadapt、Vikhr），Gamayun 因直接在俄语数据上预训练，展现出更强的文化常识理解和复杂形态处理能力。
- **架构简洁有效**：未采用 MoE 或 RLHF 等复杂技术，证明通过精心设计的数据调度也能取得 SOTA 表现。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **基础语料库**：`mC4`（多语言版本）作为初始数据源。
- **补充高质量数据**：
  - 图书：`Books`（Project Gutenberg）
  - 科学文献：`PubMed`, `arXiv`, `FineWeb-Edu`
  - 代码：`Starcoder`, `StackExchange`
  - 法律文本：`FreeLaw`
  - 其他高质量语料：`ProofPile`, `Pile`

#### 数据分布变化（见 Figure 1）
| 阶段 | 英语占比 | 主要数据来源 |
|------|--------|-------------|
| 第一阶段 | ~37% | mC4 占主导（64.37%），含多种语言 |
| 第二阶段 | ~70% | 高质量英语数据为主（如 FineWeb-Edu 占 29.3%），mC4 比例降至 16% |

### 实验设置与评估指标

#### 模型架构
- 类似 LLaMA 架构：`SwiGLU`, `RoPE`, `RMSNorm`
- 参数配置：1.5B 参数（24 层，隐藏层大小 2048，注意力头数 16）
- 分词器：采用 `LLaMA 3` 的 tokenizer（平衡词汇量与 fertility）

#### 后训练流程（Post-training）
- **监督微调（SFT）**：使用 `SlimOrca`, `OpenHermes`, `Synthetic MCF`, 数学与代码数据等。
- **偏好对齐（DPO）**：基于 `Tulu-3` 偏好数据集进行 Direct Preference Optimization。

#### 评估基准
| 类别 | 英文任务 | 俄文任务 | 其他语言 |
|------|--------|--------|---------|
| 综合知识 | MMLU, MMLU-Pro | GlobalMMLU-ru | ArabicMMLU, FrenchBench, La Leaderboard |
| 推理能力 | ARC-e/c, Hellaswag, Winogrande, Ifeval | XNLI-ru, XStoryCloze, XWinograd | Belebele（多语言阅读理解） |
| 数学能力 | GSM8K | GSM8K-ru | — |
| 文化理解 | — | **RuBIN**（自建文化常识题）、**MERA**（俄语综合评测） | — |
| 多语言综合 | — | — | **INCLUDE**（跨区域知识与推理） |

#### 基线模型对比
选取参数量相近（1–2B）的先进开源模型：
- `LLaMA3.2-1B`（9T tokens）
- `Qwen2.5-1.5B`（18T tokens）
- `Qwen3-1.7B`（36T tokens）
- `Gemma3-1B`（2T tokens）
- `EuroLM-1.7B`（欧洲多语言）
- `SmolLM2-1.7B`（11T tokens）
- 俄语适配模型：`ruadapt`, `QVikhr`, `VikhrLlama`

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 3 & 4）

#### ✅ 英文任务平均得分（Table 2）
| Model | MMLU | GSM8K | Average |
|-------|------|-------|--------|
| LLaMA3.2-1B | 45.6 | 38.2 | 46.09 |
| Qwen2.5-1.5B | 59.7 | 57.9 | 51.84 |
| **Gamayun-1.5B** | **46.4** | **56.4** | **50.36** |

> 尽管 MMLU 落后于 Qwen2.5，但在数学任务上接近，并且总体平均分非常有竞争力。

#### ✅ 多语言任务平均得分（Table 3）
| Model | Russian | Arabic | Chinese | Average |
|-------|--------|--------|--------|--------|
| Qwen2.5-1.5B | 49.8 | 53.7 | 68.7 | — |
| **Gamayun-1.5B** | **50.7** | **57.2** | **55.6** | **最高或次高** |

> Gamayun 在大多数语言上达到 SOTA 或接近 SOTA 水平，尤其在阿拉伯语和俄语上大幅领先。

#### ✅ 俄语专项评测（Table 4）
| Benchmark | Gamayun | Qwen2.5 | ruadapt | Qwen3 |
|----------|--------|--------|--------|--------|
| **MERA**（俄语综合） | **38.1** | 35.7 | 33.7 | 36.4 |
| **RuBIN**（文化常识） | **35.2** | 32.0 | 31.0 | 32.0 |

> Gamayun 在两个俄语专属评测中均排名第一，表明其在本地文化和语言细节上的优势。

#### ✅ 数学能力分类
- **数学专家级**（>65%）：Qwen3, Olmo2
- **强表现者**（56–58%）：**Gamayun**, Qwen2.5
- **较弱模型**（<45%）：LLaMA3.2, Gemma3

> Gamayun 在数学方面属于“强表现者”，虽不及重度优化的 Qwen3，但显著优于同规模多数模型。

### 消融实验结果（Appendix B & Figure 2）

#### 🔍 两阶段训练有效性验证
- 对比实验显示：若一开始就使用高比例英语（baseline），虽然初期英文表现更好，但最终多语言能力较差。
- 采用“先平衡 → 再聚焦”的两阶段策略后，模型不仅追上了英文 baseline，还在多语言任务上保持领先（见 Figure 2）。

#### 🔍 语言采样策略比较（Table 5）
尝试了 `Temperature Scaling` 和 `UniMax` 策略，发现它们提升了低资源语言性能，但严重损害了高资源语言（如英语、俄语）的表现。因此最终选择按 token 数量成比例采样（proportional sampling）。

#### 🔍 数据质量影响分析（Figure 4）
- 添加更多中文 mC4 数据对中文损失改善有限，反而加剧英文退化。
- 添加俄语数据则能持续改善俄语性能且不明显伤害英文。
> 结论：**mC4 中非英语语言的数据质量参差不齐**，俄语相对较好，而中文存在较多噪声。

---

## 4. 关键结论和发现

### 主要发现
1. **两阶段预训练策略有效缓解“多语言诅咒”**：通过先对齐语言表示，再注入高质量英语知识，实现了跨语言的能力迁移。
2. **2.5T tokens 足以训练高性能多语言 SLM**：即使训练量仅为 Qwen3 的 1/14，Gamayun 仍能在大多数任务上与其媲美。
3. **数据质量比数量更重要**：低质量多语言数据不仅自身无效，还会拖累高质量语言的学习。
4. **从头预训练优于后期适配**：在俄语文化常识（RuBIN）和形态复杂句生成上，Gamayun 显著优于基于 Qwen/Llama 微调的俄语模型（如 ruadapt），说明深层文化知识需在预训练阶段内化。
5. **LLaMA-3 tokenizer 是多语言场景下的良好折衷方案**：在词汇量、fertility 和多语言支持之间取得了良好平衡。

### 方法的局限性
- **高级 STEM 任务落后**：在 MMLU 和数学推理上仍落后于经过专门强化训练的大模型（如 Qwen3）。
- **长文本生成仍会幻觉**：尽管指令微调后有所改善，但在自由生成中仍可能出现事实错误（见 Appendix L 示例）。
- **未使用 RLHF 或 MoE 技术**：可能限制了极限性能上限，但也保证了低成本可复现性。
- **仅支持 12 种语言**：牺牲了语言广度以换取每种语言的深度训练。

### 未来工作方向
- 引入更多 STEM 和数学专项数据以缩小与顶级模型的差距。
- 探索轻量级 RLHF 或过程奖励机制来进一步减少幻觉。
- 扩展至更多语言，同时研究动态语言扩展机制。
- 开发更鲁棒的多语言数据清洗管道，提升非英语语料质量。

---

> **总结**：Gamayun 证明了在有限算力下，通过**合理的两阶段数据调度策略**和**高质量数据筛选**，可以训练出性能超越更大模型的小型多语言 LLM。其成功关键在于“**先对齐，后提效**”的设计哲学，为资源受限场景下的多语言模型开发提供了重要范式。

</details>

---

### 16. [HWL-HIN: A Hypergraph-Level Hypergraph Isomorphism Network as Powerful as the Hypergraph Weisfeiler-Lehman Test with Application to Higher-Order Network Robustness](https://arxiv.org/abs/2512.22014)

**Authors**: Chengyu Tian, Wenbin Pei  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.22014v1  

#### Abstract
Robustness in complex systems is of significant engineering and economic importance. However, conventional attack-based a posteriori robustness assessments incur prohibitive computational overhead. Recently, deep learning methods, such as Convolutional Neural Networks (CNNs) and Graph Neural Network...

---

### 17. [Scaling Adversarial Training via Data Selection](https://arxiv.org/abs/2512.22069)

**Authors**: Youran Ye, Dejin Wang, Ajinkya Bhandare  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.22069v1  

#### Abstract
Projected Gradient Descent (PGD) is a strong and widely used first-order adversarial attack, yet its computational cost scales poorly, as all training samples undergo identical iterative inner-loop optimization despite contributing unequally to robustness. Motivated by this inefficiency, we propose ...

---

### 18. [SWE-RM: Execution-free Feedback For Software Engineering Agents](https://arxiv.org/abs/2512.21919)

**Authors**: KaShun Shum, Binyuan Hui, Jiawei Chen, Lei Zhang, X. W., Jiaxi Yang, Yuzhen Huang, Junyang Lin, Junxian He  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.21919v1  

#### Abstract
Execution-based feedback like unit testing is widely used in the development of coding agents through test-time scaling (TTS) and reinforcement learning (RL). This paradigm requires scalable and reliable collection of unit test cases to provide accurate feedback, and the resulting feedback is often ...

---

### 19. [RefineBridge: Generative Bridge Models Improve Financial Forecasting by Foundation Models](https://arxiv.org/abs/2512.21572)

**Authors**: Anthony Bolton, Wuyang Zhou, Zehua Chen, Giorgos Iacovides, Danilo Mandic  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.21572v1  

#### Abstract
Financial time series forecasting is particularly challenging for transformer-based time series foundation models (TSFMs) due to non-stationarity, heavy-tailed distributions, and high-frequency noise present in data. Low-rank adaptation (LoRA) has become a popular parameter-efficient method for adap...

---

### 20. [Robustness and Scalability Of Machine Learning for Imbalanced Clinical Data in Emergency and Critical Care](https://arxiv.org/abs/2512.21602)

**Authors**: Yusuf Brima, Marcellin Atemkeng  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.21602v1  

#### Abstract
Emergency and intensive care environments require predictive models that are both accurate and computationally efficient, yet clinical data in these settings are often severely imbalanced. Such skewness undermines model reliability, particularly for rare but clinically crucial outcomes, making robus...

---

### 21. [Leash: Adaptive Length Penalty and Reward Shaping for Efficient Large Reasoning Model](https://arxiv.org/abs/2512.21540)

**Authors**: Yanhao Li, Lu Ma, Jiaran Zhang, Lexiang Tang, Wentao Zhang, Guibo Luo  
**Category**: cs.AI  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21540v1  

#### Abstract
Existing approaches typically rely on fixed length penalties, but such penalties are hard to tune and fail to adapt to the evolving reasoning abilities of LLMs, leading to suboptimal trade-offs between accuracy and conciseness. To address this challenge, we propose Leash (adaptive LEngth penAlty and...

---

### 22. [MoRAgent: Parameter Efficient Agent Tuning with Mixture-of-Roles](https://arxiv.org/abs/2512.21708)

**Authors**: Jing Han, Binwei Yan, Tianyu Guo, Zheyuan Bai, Mengyu Zheng, Hanting Chen, Ying Nie  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21708v1  

#### Abstract
Despite recent advancements of fine-tuning large language models (LLMs) to facilitate agent tasks, parameter-efficient fine-tuning (PEFT) methodologies for agent remain largely unexplored. In this paper, we introduce three key strategies for PEFT in agent tasks: 1) Inspired by the increasingly domin...

---

### 23. [Embedding Samples Dispatching for Recommendation Model Training in Edge Environments](https://arxiv.org/abs/2512.21615)

**Authors**: Guopeng Li, Haisheng Tan, Chi Zhang, Hongqiu Ni, Zilong Wang, Xinyue Zhang, Yang Xu, Han Tian  
**Category**: cs.DC  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21615v1  

#### Abstract
Training deep learning recommendation models (DLRMs) on edge workers brings several benefits, particularly in terms of data privacy protection, low latency and personalization. However, due to the huge size of embedding tables, typical DLRM training frameworks adopt one or more parameter servers to ...

---

### 24. [Global-Graph Guided and Local-Graph Weighted Contrastive Learning for Unified Clustering on Incomplete and Noise Multi-View Data](https://arxiv.org/abs/2512.21516)

**Authors**: Hongqing He, Jie Xu, Wenyuan Yang, Yonghua Zhu, Guoqiu Wen, Xiaofeng Zhu  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21516v1  

#### Abstract
Recently, contrastive learning (CL) plays an important role in exploring complementary information for multi-view clustering (MVC) and has attracted increasing attention. Nevertheless, real-world multi-view data suffer from data incompleteness or noise, resulting in rare-paired samples or mis-paired...

---

### 25. [AnchorGK: Anchor-based Incremental and Stratified Graph Learning Framework for Inductive Spatio-Temporal Kriging](https://arxiv.org/abs/2512.21569)

**Authors**: Xiaobin Ren, Kaiqi Zhao, Katerina Ta\v{s}kova, Patricia Riddle  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21569v1  

#### Abstract
Spatio-temporal kriging is a fundamental problem in sensor networks, driven by the sparsity of deployed sensors and the resulting missing observations. Although recent approaches model spatial and temporal correlations, they often under-exploit two practical characteristics of real deployments: the ...

---

### 26. [Dictionary-Transform Generative Adversarial Networks](https://arxiv.org/abs/2512.21677)

**Authors**: Angshul Majumdar  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21677v1  

#### Abstract
Generative adversarial networks (GANs) are widely used for distribution learning, yet their classical formulations remain theoretically fragile, with ill-posed objectives, unstable training dynamics, and limited interpretability. In this work, we introduce \emph{Dictionary-Transform Generative Adver...

---

### 27. [Exploring the Heterogeneity of Tabular Data: A Diversity-aware Data Generator via LLMs](https://arxiv.org/abs/2512.21915)

**Authors**: Yafeng Tang, Xiaoou Ding, Jianzhuo Du, Zishuo Yan, Zhuang Ma, Zheng Liang, Zekai Qian, Hongzhi Wang  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.21915v1  

#### Abstract
Tabular data generation has become increasingly essential for enabling robust machine learning applications, which require large-scale, high-quality data. Existing solutions leverage generative models to learn original data distributions. However, real-world data are naturally heterogeneous with div...

---

### 28. [Context as a Tool: Context Management for Long-Horizon SWE-Agents](https://arxiv.org/abs/2512.22087)

**Authors**: Shukai Liu, Jian Yang, Bo Jiang, Yizhi Li, Jinyang Guo, Xianglong Liu, Bryan Dai  
**Category**: cs.CL  
**Published**: 2025-12-29  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.22087v1  

#### Abstract
Agents based on large language models have recently shown strong potential on real-world software engineering (SWE) tasks that require long-horizon interaction with repository-scale codebases. However, most existing agents rely on append-only context maintenance or passively triggered compression he...

---

### 29. [An Equivariance Toolbox for Learning Dynamics](https://arxiv.org/abs/2512.21447)

**Authors**: Yongyi Yang, Liu Ziyin  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.21447v1  

#### Abstract
Many theoretical results in deep learning can be traced to symmetry or equivariance of neural networks under parameter transformations. However, existing analyses are typically problem-specific and focus on first-order consequences such as conservation laws, while the implications for second-order s...

---

### 30. [Statistical vs. Deep Learning Models for Estimating Substance Overdose Excess Mortality in the US](https://arxiv.org/abs/2512.21456)

**Authors**: Sukanya Krishna, Marie-Laure Charpignon, Maimuna Majumder  
**Category**: cs.LG  
**Published**: 2025-12-29  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.21456v1  

#### Abstract
Substance overdose mortality in the United States claimed over 80,000 lives in 2023, with the COVID-19 pandemic exacerbating existing trends through healthcare disruptions and behavioral changes. Estimating excess mortality, defined as deaths beyond expected levels based on pre-pandemic patterns, is...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
