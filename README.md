# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-10 06:52:33 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [tLoRA: Efficient Multi-LoRA Training with Elastic Shared Super-Models](https://arxiv.org/abs/2602.07263)

**Authors**: Kevin Li, Dibyadeep Saha, Avni Kanodia, Fan Lai  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2602.07263v1  

#### Abstract
As Low-Rank Adaptation (LoRA) becomes the standard approach for efficiently fine-tuning large language models (LLMs), shared clusters increasingly execute many concurrent LoRA training jobs over the same frozen backbone. While recent advances enable batching (co-locating) multiple adapters during se...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：tLoRA: Efficient Multi-LoRA Training with Elastic Shared Super-Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
随着 **LoRA**（Low-Rank Adaptation）成为高效微调大语言模型（LLMs）的标准方法，共享集群中越来越多地出现**并发执行多个 LoRA 训练任务**的场景。尽管已有研究在推理阶段实现了多 LoRA 批处理（如 dLoRA、S-LoRA），但在**训练阶段**，由于任务间存在显著异构性（如不同的 LoRA rank、batch size、资源分配等），直接批处理会导致以下问题：
- **同步阻塞**（synchronization stalls）
- **通信开销增加**
- **个别任务变慢甚至劣于独立运行**

因此，如何在保证各任务进度的前提下，高效地协同训练多个异构 LoRA 任务，是一个尚未被充分探索的关键挑战。

---

### 提出了什么新方法或新思路
本文提出 **tLoRA**，一个支持高效批量训练多 LoRA 任务的框架，其核心创新包括三个层面：

#### （1）**Shared Super-Model (SSM) 抽象**
- 将共享同一骨干模型（backbone）的多个 LoRA 任务融合为一个统一的“弹性共享超模型”（Elastic Shared Super-Model）。
- 该抽象保留了原始训练语义（功能等价），同时暴露更多优化结构，可无缝接入现有分布式训练框架（如 Megatron-LM 或 PyTorch FSDP），实现高效的并行执行计划（如流水线并行）。

#### （2）**Fused LoRA Kernel（融合内核）**
- 设计了一个动态的 LoRA 融合内核，避免显式构造 $ W = AB^T $ 权重矩阵，提升寄存器和共享内存复用。
- 引入 **nano-batch** 抽象，在微批次内部进一步细粒度划分输入样本，通过 **rank-aware 调度**最大化计算与通信的重叠，减少流水线气泡。

#### （3）**Online Residual-Capacity-Aware Scheduler（在线调度器）**
- 动态监控每个任务的剩余资源容量（如未使用的 GPU 计算/内存）和紧迫性（urgency score）。
- 自适应地将资源互补的任务进行分组，最大化整体吞吐量，同时确保不违反单个任务的性能约束（如完成时限、最大减速限制）。

---

### 相比现有方法的优势
| 维度 | tLoRA 优势 |
|------|-----------|
| **效率** | 显著提升训练吞吐量、GPU 利用率，缩短任务完成时间 |
| **兼容性** | 不改变训练语义，与现有分布式训练栈完全兼容 |
| **公平性** | 避免“富任务补贴穷任务”的不公平现象，保障个体进度 |
| **自适应性** | 支持在线动态调整分组策略，适应真实集群负载变化 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- 主要训练任务基于 **GSM8K 数据集**（约 8.5k 条小学数学题），用于模拟实际 LoRA 微调任务。
- 实验中的 LoRA 配置（rank、batch size 等）从真实集群轨迹中采样生成。

---

### 实验设置和评估指标

#### 实验平台
- **硬件测试床**：12 块 NVIDIA A100 GPU
- **大规模仿真环境**：使用生产级分布式训练模拟器 **Sailor**（Strati et al., 2025），误差控制在 3% 以内
- **默认集群规模**：128-GPU 集群
- **模型类型**：Llama-3-8B 和 Qwen-3-8B

#### 工作负载来源
- 使用 **ACMETrace** 中的真实 GPU 集群轨迹，包含任务到达时间、GPU 分配、执行时长等信息。
- 在此基础上注入 LoRA 特征（rank ∈ {2,4,8,16}, batch size ∈ {1,2,4,8}）

#### 评估指标
| 指标 | 定义 |
|------|------|
| **Training Throughput** | 全局吞吐量（samples/sec），所有活跃任务总和 |
| **Job Completion Time (JCT)** | 从提交到训练完成的实际墙钟时间（含排队延迟） |
| **GPU Utilization** | 所有 GPU 的平均利用率（SM 利用率） |

---

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **Megatron** | 独立训练每个 LoRA 任务，无批处理 |
| **mLoRA (Ye et al., 2025)** | 当前最先进的批处理 LoRA 训练系统，仅按内存是否足够决定是否合并 |
| **tLoRA w/o Scheduler** | 移除自适应调度器，采用 mLoRA 的静态分组策略 |
| **tLoRA w/o Kernel Fuser** | 移除融合内核，使用原生 PyTorch 内核 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自真实轨迹评估）
| 指标 | 提升幅度 |
|------|----------|
| **Training Throughput** | **+1.2–1.8×** vs. mLoRA / Megatron |
| **Job Completion Time** | **缩短 2.3–5.4×** |
| **Average GPU Utilization** | **提升 37%**（峰值达 84%） |

> 图5显示，在动态到达的工作负载下，tLoRA 吞吐量比 mLoRA 高出 **41%**，且任务完成时间降低 **5.4×**。

---

### 与基线方法的对比结果
| 对比项 | 结果 |
|--------|------|
| vs. **Megatron** | 即使 Megatron 本身高度优化，tLoRA 仍因资源共享获得显著优势 |
| vs. **mLoRA** | mLoRA 因忽略异构性导致部分组合反而降低性能；而 tLoRA 通过智能分组避免此类退化 |
| vs. FIFO 分组策略 | tLoRA 的调度策略使小任务与大任务更有效互补，而 mLoRA 的高共置比例反而带来“长尾延迟” |

---

### 消融实验结果
#### （1）移除 Kernel Fuser
- 使用原生 PyTorch 内核 → 吞吐下降明显
- 原因：频繁的小张量操作导致 kernel launch 开销高、数据复用差、难以重叠通信

#### （2）移除 Adaptive Scheduler
- 改用 mLoRA 的静态分组 → 完成时间恶化，尤其对资源紧张任务不公平
- 表明**动态感知剩余容量和紧迫性**是实现高效且公平调度的关键

#### （3）Nano-batch 大小影响（图8a）
- 固定 nano-batch 大小无法适应不同网络带宽和硬件配置
- tLoRA 使用 **AIMD 控制器**在线调节 nano-batch 数量，实现最优计算/通信重叠，优于手动调优

#### （4）不同到达模式下的表现（图8b & 图11）
- 在稀疏到达时，tLoRA 更容易找到互补伙伴，完成时间更短
- 在密集突发到达时，仍能维持接近峰值吞吐，表现出强健的负载适应能力

#### （5）集群规模扩展性（图9b & 图13）
- 从小型（32 GPU）到大型（256 GPU）集群，tLoRA 均保持良好扩展性
- 完成时间随资源减少呈线性右移，而非指数增长，说明调度有效避免了资源饥饿

---

## 4. 关键结论和发现

### 主要发现
1. **异构性不是障碍而是机会**：LoRA 任务间的差异（如 rank、batch size）若被合理利用，可通过“资源互补”实现双赢。
2. **静态批处理不可靠**：简单依据内存是否充足来合并任务（如 mLoRA）可能适得其反，必须考虑通信瓶颈与负载均衡。
3. **细粒度执行控制至关重要**：引入 **nano-batch + fused kernel** 可显著提升 GPU 利用率和重叠效率。
4. **在线反馈驱动优于预设策略**：基于运行时信号（utilization、latency）动态调整分组和 nano-batch 大小，才能应对复杂多变的真实负载。

---

### 方法的局限性
1. **假设共享相同 backbone**：目前仅适用于多个 LoRA 共享同一个基础模型的场景，不支持跨模型批处理。
2. **依赖底层分布式框架**：虽然兼容性强，但性能仍受限于所用并行化工具（如 Megatron）的能力边界。
3. **未考虑精度漂移风险**：虽然论文声称“lossless”，但在极端异构组合下是否会影响收敛稳定性有待长期验证。

---

### 未来工作方向
1. **支持跨模型 LoRA 批处理**：拓展 SSM 抽象以融合不同 backbone 的 LoRA。
2. **集成自动超参推荐机制**：结合 AdaLoRA 等技术，联合优化 rank 分配与调度决策。
3. **面向绿色 AI 的能耗优化版本**：在提升吞吐的同时最小化 energy-to-solution。
4. **部署到真实云服务平台**：与 Kubernetes、Ray 等集成，构建端到端的 LoRA 训练即服务（LoRA-as-a-Service）系统。

--- 

> ✅ 总结一句话：  
> **tLoRA 通过“统一建模 + 内核实例融合 + 动态资源调度”三位一体设计，首次实现了高效、公平、实用的多 LoRA 并行训练框架，在真实集群轨迹上实现了高达 1.8× 吞吐提升和 5.4× 更快的任务完成速度。**

</details>

---

### 2. [SpecAttn: Co-Designing Sparse Attention with Self-Speculative Decoding](https://arxiv.org/abs/2602.07223)

**Authors**: Yikang Yue, Yuqi Xue, Jian Huang  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.07223v1  

#### Abstract
Long-context large language model (LLM) inference has become the norm for today's AI applications. However, it is severely bottlenecked by the increasing memory demands of its KV cache. Previous works have shown that self-speculative decoding with sparse attention, where tokens are drafted using a s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SpecAttn: Co-Designing Sparse Attention with Self-Speculative Decoding

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
长上下文 Large Language Model (LLM) 推理面临严重的 **KV Cache 内存带宽瓶颈**。传统的稀疏注意力（Sparse Attention）虽然能减少内存访问，但可能牺牲生成质量；而现有的自推测解码（Self-Speculative Decoding）方法在 **drafting 准确率** 和 **KV selection 开销** 之间存在固有 trade-off。

现有方法将 drafting 和 verification 视为独立过程，忽略了 verification 阶段中全注意力计算所隐含的“免费”KV 条目重要性信息。

---

### 🚀 提出的新方法与核心思想
提出 **SpecAttn** —— 一种 **验证引导的稀疏注意力（verification-guided sparse attention）** 自推测解码机制，其核心创新在于：

- **协同设计（Co-Design）drafting 与 verification 阶段**：
  - 利用 verification 阶段执行 full attention 时产生的中间结果（attention logits），识别出对生成关键的 KV 条目。
  - 将这些“被验证过的重要 KV”用于后续 drafting 阶段的 sparse attention，实现高质量 draft token 生成。

- **低开销 KV 选择算法**：
  - 提出 “**Collect-2-Query**” 机制：仅从第一个 draft token 和 bonus token 收集 attention logits，显著降低日志收集开销，同时保持高准确性。
  - 聚合多个 draft token 的 attention logits 进行 KV 选择，避免仅依赖最后一个接受 token 导致的过拟合问题。

- **系统级优化策略**：
  - 设计三步超参数调优策略，平衡 sparse ratio 与 draft token 数量 $ y $，最大化端到端吞吐量。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 MagicDec + Quest） | SpecAttn |
|------|-------------------------------|--------|
| **KV Selection 准确性** | 查询感知但每步重估，易过拟合 | 多 token 聚合，覆盖更广上下文 |
| **KV Selection 开销** | 高（需每步重新估计） | 极低（利用已有 verification 结果） |
| **drafting 准确率** | 中等（受限于 selection 策略） | 更高（基于真实 full attention 反馈） |
| **是否训练依赖** | 否 | 否（training-free, plug-and-play） |

> ✅ **优势总结**：SpecAttn 在不损失输出质量的前提下，实现了更高的 draft token 接受率和更低的选择开销，从而大幅提升了解码吞吐量。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **AIME25**：数学推理任务，输入短、输出长（平均 ~182 输入 tokens，~19.7K 输出 tokens）
- **CodeElo**：编程代码生成任务，同样具有短输入、长链式推理特点
- **LongBench-v2**：综合性长上下文评测基准，测试输入长度为 **96K–120K tokens**，涵盖问答、摘要、推理等多种任务

---

### ⚙️ 实验设置与评估指标

#### 硬件平台
- 服务器配置：双 NVIDIA H100 NVL GPU（94GB 显存）

#### 模型
- 开源模型族：
  - Qwen3-4B, Qwen3-8B, Qwen3-30B
  - gpt-oss-20b（带 MXFP4 量化）

#### 基线方法（均集成于 vLLM 框架内进行公平比较）
| 方法 | 类型 | KV Selection 方式 |
|------|------|------------------|
| **vLLM (default)** | Vanilla auto-regressive decoding | 全量 KV Cache |
| **MagicDec-Stream** | Self-Speculative + Sparse Attention | Sliding Window（query-agnostic） |
| **MagicDec-Quest** | Self-Speculative + Sparse Attention | Query-aware（动态估计重要性） |
| **SpecExtend** | Self-Speculative + Guidance | 使用 last accepted token 的 attention 权重指导 |

#### 评估指标
- **Decoding Throughput (tokens/s)**：主要性能指标
- **Average Accepted Draft Tokens per Iteration**：衡量 drafting 准确率
- **KV Selection Overhead (%)**：相对于理想迭代延迟的额外开销
- **End-to-End Latency**：整体响应时间

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

| 指标 | 结果 |
|------|------|
| **最高吞吐提升（vs. vLLM）** | **2.81×**（Qwen3-8B on LongBench-v2） |
| **平均吞吐提升（vs. vLLM）** | **1.25× – 2.70×**（跨模型与数据集） |
| **相对 SOTA 提升（vs. MagicDec-Quest / Stream）** | **1.18× – 1.29×** |
| **最大 draft token 接受数/轮次** | 最高达 **8.91**（Qwen3-8B, y=11） |

> 💡 示例：在 LongBench-v2 上，SpecAttn 达到 **2.81×** 于 vanilla vLLM，且比当前最优稀疏推测方法快 **29%**。

---

### 🔁 与基线方法对比结果

#### （1）吞吐量对比（图 9 & 图 11）
- **SpecAttn > MagicDec-Stream > MagicDec-Quest ≈ SpecExtend**
- MagicDec-Quest 因 high KV selection overhead 导致实际速度低于 MagicDec-Stream
- SpecAttn 在所有模型上均取得最佳表现，尤其在长上下文场景下优势明显

#### （2）drafting 准确率（图 10a）
- SpecAttn 平均每轮接受 **6.1–9.3 个 draft tokens**（取决于 y 设置）
- 明显优于 MagicDec-Quest 和 SpecExtend，说明其 KV selection 更有效

#### （3）KV selection 开销（图 10b）
- **SpecAttn 开销仅为 5.9%–9.4%**
- MagicDec-Quest：21.7%
- SpecExtend：11.2%（Qwen3-8B），高达 29.1%（gpt-oss-20b）
- 原因：SpecExtend 必须聚合所有 token 的 logits，而 SpecAttn 仅用两个边界 token 即可代表整体

---

### 🔍 消融实验结果（Ablation Study）

#### （1）KV Selection 策略对比（图 4）
- 仅使用 **last accepted token** 的 attention → 后续 draft token 接受率迅速下降
- 使用 **all draft tokens 的 attention logits 聚合** → 接受率稳定维持高位
- 发现：即使被拒绝的 draft token，其语义仍接近正确路径，保留其 attention 有助于 KV 重要性估计

#### （2）“Collect-2-Query” 有效性（表 1 & 图 7）
- 仅使用 **first draft + bonus token** 收集 logits：
  - 接受 token 数：6.11 vs. all-query 的 6.13（几乎无损）
  - 开销降低至原来的 **1/10 以下**
- 原理：相邻 token 的 top-KV 重叠度高，首尾 token 具有最大位置跨度，最具代表性

#### （3）超参数敏感性分析（图 8 & 附录 B）
- 当 sparse ratio ≥ 7% 时，接受 token 数趋于饱和
- SpecAttn 可使用更大的 $ y $（如 9–11），因其 drafting 更高效准确

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Verification 不应只是“验证”，更是“指导”来源**  
   verification 阶段的 full attention 计算提供了宝贵的 KV 条目重要性信号，可作为 drafting 的“免费 oracle”。

2. **协同设计显著打破 accuracy-overhead trade-off**  
   SpecAttn 成功实现了 **高 drafting accuracy + 低 KV selection overhead**，突破了以往方法的性能天花板。

3. **长上下文下性能增益随 context length 扩展而放大**  
   因为 SpecAttn 的 KV selection 开销增长缓慢，而收益持续增加，在百万级 token 场景更具潜力。

4. **无需训练、即插即用，兼容性强**  
   完全基于 vLLM 实现，支持多种架构（dense、MoE、RoPE 等），适合工业部署。

---

### ⚠️ 局限性
- **依赖 FlashAttention-3 修改能力**：需要修改底层 attention kernel 以导出 attention logits，对某些闭源框架适配难度较高。
- **对 very small models 收益有限**：当 KV cache 本身不大时，优化空间较小。
- **目前仅适用于 self-speculative setting**：尚未扩展至 auxiliary draft model 场景（如 Medusa）。

---

### 🔮 未来工作方向
1. **扩展至多 draft model setting**：将 verification-guided 思想应用于 Medusa 或 Eagle 等结构。
2. **动态 sparse ratio 调整**：根据上下文复杂度自适应调整稀疏程度。
3. **硬件协同优化**：结合定制 kernel 或 FPGA 加速 logits 收集过程。
4. **引入缓存机制**：对历史 critical KV 建立长期记忆池，进一步减少重复计算。

---

> 📢 **Impact Statement 强调**：
> - ✅ **可靠性强**：保证生成质量无损（lossless），适用于医疗、法律等高风险领域。
> - ✅ **环保节能**：通过提升效率直接降低 LLM 推理能耗与碳足迹。
> - ✅ **安全可控**：不改变模型行为，仅加速推理，无新增 bias 或滥用风险。

---

## 总结一句话
> **SpecAttn 通过将 speculative decoding 中的 verification 阶段变为 drafting 的“导师”，首次实现了基于反馈的稀疏注意力协同设计，在无需训练的情况下达成高达 2.81× 的解码吞吐提升，是长上下文 LLM 高效推理的一项重要进展。**

</details>

---

### 3. [MedVerse: Efficient and Reliable Medical Reasoning via DAG-Structured Parallel Execution](https://arxiv.org/abs/2602.07529)

**Authors**: Jianwen Chen, Xinyu Yang, Peng Xia, Arian Azarang, Yueh Z Lee, Gang Li, Hongtu Zhu, Yun Li, Beidi Chen, Huaxiu Yao  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.07529v1  

#### Abstract
Large language models (LLMs) have demonstrated strong performance and rapid progress in a wide range of medical reasoning tasks. However, their sequential autoregressive decoding forces inherently parallel clinical reasoning, such as differential diagnosis, into a single linear reasoning path, limit...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MedVerse: Efficient and Reliable Medical Reasoning via DAG-Structured Parallel Execution

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前基于 **Large Language Models (LLMs)** 和 **Chain-of-Thought (CoT)** 的医疗推理模型存在三大瓶颈：

- **准确性（Accuracy）**：序列化自回归（autoregressive, AR）解码强制将本应并行的临床推理（如鉴别诊断）压缩为单一路径，容易因早期错误假设导致“上下文污染”（contextual pollution），限制了对多种可能性的探索。
- **效率（Efficiency）**：串行生成导致高延迟，尤其在处理复杂多分支推理时，重复计算重叠证据，难以满足实时临床决策支持需求。
- **可解释性（Interpretability）**：传统 CoT 缺乏显式的因果依赖结构，推理过程混乱，不利于医生验证和理解。

### 🚀 提出的新方法与创新思路

作者提出 **MedVerse** —— 一种面向复杂医学推理的新型建模范式，其核心是将医学推理重构为 **有向无环图（Directed Acyclic Graph, DAG）上的并行执行过程**，基于 **Petri Net 理论**实现。

#### 主要创新点：

1. **DAG-Structured Medical Reasoning Framework**
   - 将临床推理形式化为 DAG 结构：
     - **节点（Node）**：表示中间推理状态（如症状、假设、病理机制）
     - **边（Edge）**：表示实体间的条件依赖关系
   - 支持多个假设共享中间证据、汇聚于共同机制，更贴近真实临床思维。

2. **基于 Petri Net 的可执行推理引擎**
   - 引入 **Petri Net** 数学框架，将 DAG 映射为可执行的并发系统：
     - **Place** 对应推理状态（节点）
     - **Transition** 对应推理步骤（边）
     - **Token** 携带语义信息（文本历史 `h` + KV-cache 引用 `k`）
   - 实现真正的 **因果一致的并行推理**，避免信息泄露。

3. **全栈协同设计（Full-Stack Co-Design）**
   - **数据层（Data）**：提出 **MedVerse Curator** 自动化流水线，从医学知识图谱生成 13,904 条高质量、结构化的 DAG 推理轨迹，构建 **MedVerse-14K** 数据集。
   - **算法层（Algorithm）**：提出 **MedVerse Attention**，通过拓扑感知注意力掩码（topology-aware attention mask）和自适应位置索引（adaptive position indices），使 Transformer 支持 DAG 并行推理。
   - **系统层（System）**：开发 **MedVerse Engine**，基于 Multiverse Engine 构建，支持动态提取计划、零拷贝 Fork/Join 执行，实现高效并行解码。

4. **Hybrid Execution Paradigm（线性规划 + 图执行）**
   - 第一阶段：**线性规划（Think-then-Map）**  
     使用标准 AR 模型生成多个线性推理路径，并整合为 `<Plan>` 块，明确依赖关系。
   - 第二阶段：**图并行执行（Graph Execution）**  
     引擎解析 `<Plan>` 构建 Petri Net，识别“启用转换前沿”（enabled-transition frontier），并发执行独立推理分支。

---

### 🔍 相比现有方法的优势

| 维度 | 传统 CoT / AR 模型 | 多数 Parallel Thinking 方法 | **MedVerse** |
|------|---------------------|-------------------------------|---------------|
| **推理结构** | 线性链式（Linear Chain） | 树状或简单 Fork-Join | **DAG 结构，支持复杂依赖** |
| **并行能力** | 无 | 通常靠重复采样（repeated sampling） | **真正结构化并行，因果同步** |
| **训练对齐** | 仅推理时并行 | 缺乏结构化训练数据 | **端到端训练 + 并行推理对齐** |
| **效率提升来源** | 无 | 增加计算资源消耗 | **降低推理深度 O(D) 而非长度 O(N)** |
| **可解释性** | 差 | 中等 | **显式结构依赖，逻辑清晰** |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

实验在五个主流医学推理基准上进行：

- **MedQA**：美国医师执照考试风格选择题
- **MedXpert**：专家级医学问答数据集
- **MedBullets (op4/op5)**：基于临床要点的多选题
- **HLE (Humanity’s Last Exam)**：综合性医学挑战测试
- **PubMedQA**：基于 PubMed 文献的问答

最终用于训练的合成数据集为 **MedVerse-14K**（13,904 条结构化推理样本），由 MedVerse Curator 从上述数据集中构建。

### ⚙️ 实验设置

- **基础模型**：
  - Qwen2.5-7B-Instruct
  - Llama-3.1-8B-Instruct
- **训练方式**：
  - 在 MedVerse-14K 上微调 3 个 epoch
  - 学习率：1e-5，Batch Size：128
  - 使用 4 块 NVIDIA H200 GPU，PyTorch FSDP
- **推理引擎**：基于 **SGLang** 实现的 **MedVerse Engine**

### 🎯 评估指标

| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | 多项选择题的正确率，主评价指标 |
| **Latency (s)** | 完整推理链生成的端到端耗时（wall-clock time） |
| **Throughput (tokens/sec)** | 单位时间内生成的 token 数量，衡量吞吐能力 |
| **Speedup** | 相对于标准 AR 模型的延迟加速比 |

### 🆚 基线方法对比

- **Base Models**：
  - Qwen2.5-7B-Instruct
  - Llama-3.1-8B-Instruct
- **Specialized Medical LLMs**：
  - **MedReason-8B**：基于知识图谱监督 CoT 的医疗专用模型
  - **HuatuoGPT-o1-RL-8B**：具备复杂推理能力的中文医疗大模型

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| Model | HLE | MedBullets (op4) | MedBullets (op5) | MedQA | MedXpert | **Average** |
|-------|-----|------------------|------------------|--------|----------|------------|
| Qwen2.5-7B (Base) | 18.4 | 45.8 | 39.6 | 56.2 | 12.3 | **34.5** |
| + MedReason | 20.8 | 49.7 | 44.2 | 56.2 | 14.5 | **37.1** |
| **+ MedVerse** | **19.6** | **55.2** | **48.0** | **58.6** | **15.3** | **39.3** ✅ |
| Llama-3.1-8B (Base) | 13.6 | 48.7 | 42.5 | 58.7 | 13.2 | **35.3** |
| + MedReason | 20.2 | 57.1 | 51.0 | 63.9 | 18.4 | **42.2** |
| + HuatuoGPT-o1 | 14.6 | 55.8 | 53.9 | 72.4 | 16.8 | **42.7** |
| **+ MedVerse** | **20.6** | **62.3** | **53.6** | **66.4** | **19.3** | **44.2** ✅ |

> ✅ **MedVerse 在两个 backbone 上均达到最高平均准确率**，显著优于原始模型和专业医疗模型。

### ⏱ 效率分析结果（Figure 4）

- **推理延迟（Latency）**：
  - 相比标准 AR 模型，**MedVerse 实现 1.25× ~ 1.33× 的端到端延迟降低**
  - 例如，在 MedXpert 上从 5.1s 降至 **4.0s**
- **生成吞吐量（Throughput）**：
  - 在 2048 长序列生成任务中，峰值吞吐达 **~17.1 tokens/sec**
  - 相比 AR 基线（~10.1 tokens/sec），**提升 69.3%**
- **扩展性优势**：
  - 随着序列增长，MedVerse 吞吐下降缓慢，而 AR 模型受限于内存带宽，性能急剧下降。

### 🔬 消融实验结果（Ablation Study）

#### 表格 2：线性-并行混合策略的有效性（MedXpert, Batch=1）

| Model Variant | Linear Planning | Parallel Execution | Accuracy (%) | Latency (s) |
|---------------|------------------|---------------------|--------------|-------------|
| Autoregressive | √ | × | 18.4 | 5.1 |
| Direct Petri Net | × | √ | 17.4 | 4.5 |
| **MedVerse** | √ | √ | **19.3** | **4.0** |

> 💡 发现：**直接生成 Petri Net 结构效果差**，说明 LLM 难以从零构造可靠图结构；而“先线性规划 + 再并行执行”的混合范式在 **准确性和效率上取得最佳平衡**。

#### 表格 4：训练策略与推理模式联合消融

| Configuration | Training | Inference | Avg Acc |
|---------------|----------|-----------|---------|
| Auto-Ser | AR | Serial | 0.3690 |
| Auto-Par | AR | Parallel | 0.3792 |
| Mask-Ser | MedVerse Attention | Serial | 0.3856 |
| **Mask-Par (Ours)** | MedVerse Attention | Parallel | **0.3934** |

> 🔍 结论：
> - **拓扑感知训练（Mask-Ser）本身就能提点 +1.66%**，说明结构化训练有助于学习因果依赖。
> - **并行推理进一步增益**，证明“训练-推理”结构对齐至关重要。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **临床推理本质上是非线性的 DAG 过程**，强行用线性 CoT 建模会损失表达能力和鲁棒性。
2. **MedVerse 成功将 Petri Net 理论引入 LLM 推理**，实现了结构化、可执行、可并行的医学推理框架。
3. **全栈协同设计（数据 + 算法 + 系统）是关键**：
   - MedVerse Curator 提供结构化训练信号
   - MedVerse Attention 实现拓扑感知建模
   - MedVerse Engine 实现高效并行执行
4. **“线性规划 + 图执行”混合范式最优**：纯端到端图生成不可靠，需借助线性思考辅助图构建。
5. **效率提升源于计算复杂度降维**：从 O(N) 序列长度变为 O(D) 图深度，特别适合长程复杂推理。

### ⚠ 局限性

- **依赖高质量结构化数据生成**：MedVerse Curator 的可靠性直接影响模型表现，目前仍依赖强 LLM（如 GPT-5.1）作为教师模型。
- **Petri Net 建模复杂性**：对非常规或模糊推理路径的建模能力有限，可能无法覆盖所有临床场景。
- **硬件依赖较高**：虽然提升了吞吐，但仍需高性能 GPU（如 H200）才能充分发挥并行优势。
- **尚未在真实临床环境中部署验证**：实验均为离线 benchmark 测试，缺乏医生反馈和实际应用数据。

### 🔮 未来工作方向

- **扩展至多模态医学推理**（如结合医学影像），构建 **Multimodal MedVerse**
- **引入强化学习优化推理路径选择**，实现动态剪枝与聚焦
- **轻量化版本适配边缘设备**，推动临床一线部署
- **构建开放的 DAG 医学知识图谱编辑器**，支持医生参与推理结构标注
- **探索更多 Petri Net 变体**（如 Timed/Colored Petri Nets）以建模时间动态和不确定性

---

> **总结一句话**：  
> **MedVerse 通过将医学推理重构为 DAG 并行执行过程，首次实现了“结构对齐、训练对齐、系统对齐”的高效可靠医疗推理框架，在准确率、延迟、吞吐三方面全面超越现有方法，为下一代临床 AI 决策系统提供了新范式。**

</details>

---

### 4. [BitLogic: Training Framework for Gradient-Based FPGA-Native Neural Networks](https://arxiv.org/abs/2602.07400)

**Authors**: Simon B\"uhrer, Andreas Plesner, Aczel Till, Roger Wattenhofer  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2602.07400v1  

#### Abstract
The energy and latency costs of deep neural network inference are increasingly driven by deployment rather than training, motivating hardware-specialized alternatives to arithmetic-heavy models. Field-Programmable Gate Arrays (FPGAs) provide an attractive substrate for such specialization, yet exist...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# BitLogic: Training Framework for Gradient-Based FPGA-Native Neural Networks 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
当前深度神经网络（DNN）推理的能耗和延迟成本日益成为瓶颈，尤其是在边缘设备部署中。传统的基于浮点或高精度整数运算的模型在通用硬件（如GPU）上效率低下。尽管FPGA因其可编程性和低功耗潜力被广泛用于加速推理，但现有的FPGA神经网络方法存在以下问题：
- 多为将传统模型量化后映射到FPGA资源，未能充分利用FPGA原生逻辑单元（如LUT）；
- 不同研究之间采用异构平台、不同假设（如流水线设计），难以公平比较；
- 缺乏统一框架支持端到端训练与硬件生成。

### 提出的新方法与思路
本文提出 **BitLogic** ——一个**完全梯度驱动、端到端可训练的FPGA原生神经网络框架**，其核心思想是：
- 使用 **Lookup Table (LUT)** 节点替代传统的乘累加（MAC）操作，直接对应FPGA中的基本逻辑单元；
- 所有LUT节点通过**可微松弛技术**实现梯度传播，支持反向传播训练；
- 构建模块化功能API，支持多种架构组合（如CNN、残差连接、注意力机制等）；
- 提供自动化 **Register Transfer Level (RTL)** 导出流程，将PyTorch模型无缝转换为可综合的HDL代码，确保软硬件一致性。

### 相比现有方法的优势
| 维度 | BitLogic优势 |
|------|-------------|
| **硬件适配性** | 原生使用LUT作为计算单元，无需依赖DSP或BRAM，最大化利用FPGA片上逻辑资源 |
| **训练方式** | 完全基于梯度优化，支持end-to-end训练，避免组合搜索或非连续优化 |
| **灵活性与扩展性** | 模块化设计允许灵活构建各种架构；支持编码器（encoders）、头层（heads）等组件定制 |
| **部署便捷性** | 自动化RTL导出 + Vivado项目生成，显著降低从模型到硬件的门槛 |
| **公平评估基础** | 在统一框架下系统评估不同LUT松弛策略、连接模式、节点类型的影响 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖四个标准图像分类基准：
- **MNIST**: 手写数字识别（10类）
- **Fashion-MNIST**: 服饰图像分类（10类）
- **CIFAR-10**: 彩色小图分类（10类）
- **CIFAR-100**: 彩色小图细粒度分类（100类）

### 实验设置与评估指标
#### 模型配置
- **两种主干架构**：
  - Feedforward Network (FFN)：两层Top-K稀疏LUT层
  - Convolutional Network (CNN)：两个步长为2的3×3卷积块 + 全连接层
- **关键参数**：
  - LUT输入维度 $n \in \{2,3,4,5,6\}$
  - 使用 **probabilistic relaxation** 作为默认可微化方法
  - 输出头使用 **GroupSum** 或 **GroupedDSP**

#### 评估指标
| 指标类别 | 具体指标 |
|--------|---------|
| **准确性** | 测试准确率（Test Accuracy %） |
| **硬件效率** | 逻辑门等效数量（Equivalent Binary Gates）、LUT使用量、单样本推理延迟（Latency）、吞吐量（FPS）、能效（Energy per Sample） |
| **可比性** | 报告“等效二进制门数”以跨架构公平比较规模 |

#### 基线方法对比
与多个基于逻辑门或LUT的神经网络进行比较：
- **DiffLogic Net** (Petersen et al., 2022)
- **LogicTreeNet** (Petersen et al., 2024)
- **LILogicNet** (Fojcik et al., 2025)
- **DWN** (Bacellar et al., 2025)

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 数据集 | BitLogic 准确率 | 等效门数 | 推理延迟（FPGA） |
|-------|----------------|----------|------------------|
| MNIST | **99.1%** | 384K | <20ns |
| Fashion-MNIST | **93.8%** | 384K | <20ns |
| CIFAR-10 | **72.3%** | <0.3M | <20ns |
| CIFAR-100 | **23.4%** | 384K | <20ns |

> 注：所有FPGA测试均在 **Xilinx Zynq-7020** 上完成，仅使用LUT资源（未用DSP或BRAM）。

### 与基线方法的对比结果
- 在 **CIFAR-10** 上，BitLogic达到 **72.3%** 准确率，优于大多数轻量级逻辑网络（如LILogicNet-L: 60.98% @ 256K gates），且门数远低于大型CNN变体（如LogicTreeNet-G需61M gates达86.29%）。
- 在 **MNIST** 上，BitLogic以384K门实现 **99.1%** 准确率，接近最优水平，显著高于多数逻辑网（如DiffLogic Net最高98.47% @ 384K）。
- 在 **能效方面**，FPGA实测单样本推理能耗仅为 **3.34nJ**，远低于CPU（5.63mJ）和GPU（130μJ）。

### 消融实验结果
#### （1）组件级消融分析（Table 2）
在Fashion-MNIST上的控制变量实验表明：
- **节点输入维度（Fan-in）影响最大**：从2增至6，准确率从79.3%提升至84.9%，说明更高维LUT增强表达能力。
- **宽度优于深度**：增加层宽比堆叠层数更有效（见Fig 4a），深层网络收益递减。
- **编码方式**：`distributive thermometer` 表现最佳（83.4%），优于one-hot、binary等。
- **节点类型**：`hybrid` 和 `probabilistic` 表现最好（~84%），而`linear`、`warp`较差。
- **头部设计**：`GroupSum` 优于 `GroupedDSP`，后者因引入DSP反而受限。

#### （2）温度超参调节（Fig 4b）
- 引入 **temperature parameter T** 可缓解离散化过拟合问题；
- 最佳T ≈ 50时验证准确率达峰值（85.97%），T=1.0则严重过拟合。

---

## 4. 关键结论和发现

### 主要发现
1. **LUT-based NN可以实现高精度推理**：即使不使用任何MAC或浮点运算，仅靠LUT也能在主流视觉任务上取得具有竞争力的结果。
2. **宽度优先于深度**：对于LUT网络，扩大每层宽度比加深网络更有利于性能提升，这与传统DNN趋势一致，但在逻辑网络中更为明显。
3. **节点输入维度至关重要**：更大的fan-in（即更多输入位）显著提高模型容量，但代价是参数呈指数增长（$2^n$）。
4. **硬件-算法协同设计可行**：通过边界一致的可微松弛 + 自动RTL导出，实现了真正意义上的“训练即部署”闭环。
5. **FPGA极致高效**：BitLogic在Zynq-7020上实现 **<20ns 单样本延迟** 和 **3.34nJ 能耗**，展示了FPGA在极低延迟场景的巨大潜力。

### 方法的局限性
1. **训练稳定性随深度下降**：目前主要适用于浅层网络（≤2层），深层训练面临梯度消失问题。
2. **CNN版本表现不佳**：当前卷积架构未充分调优，性能低于FFN，可能由于滑动窗口带来的序列处理开销。
3. **RTL导出限制**：
   - 当前仅支持全组合逻辑；
   - 不支持复杂时序逻辑（如显式流水线、状态机）；
   - Vivado对常量数组大小有限制，影响大规模模型合成。
4. **内存带宽未优化**：虽然计算高效，但输入输出仍受外部存储访问制约。

### 未来工作方向
1. **提升深度训练稳定性**：
   - 引入归一化机制（Normalization）
   - 改进初始化策略（如Residual Initialization推广）
   - 设计适用于逻辑节点的残差连接
2. **拓展任务范围**：
   - 支持分割、重建、序列建模等任务
   - 开发RNN、Transformer风格模块
3. **完善HDL导出能力**：
   - 支持流水线插入、模块复制、迭代分解
   - 实现流式接口与时钟域管理
4. **硬件感知联合优化（Co-design）**：
   - 将LUT利用率、时序约束纳入损失函数
   - 结合Neural Architecture Search (NAS) 自动探索最优结构
5. **探索新型节点与连接机制**：
   - 动态路由、自适应拓扑学习
   - 更高效的interconnect parametrization（参考Kresse et al., 2025）

---

> ✅ 总结：**BitLogic 是首个实现从PyTorch训练到FPGA部署全流程自动化的梯度驱动LUT神经网络框架**。它不仅在多个基准上取得了领先的逻辑网络性能，更重要的是建立了一个开放、可复现、可扩展的研究范式，推动FPGA-native AI模型的发展进入新阶段。

</details>

---

### 5. [DLLM-Searcher: Adapting Diffusion Large Language Model for Search Agents](https://arxiv.org/abs/2602.07035)

**Authors**: Jiahao Zhao, Shaoxuan Xu, Zhongxiang Sun, Fengqi Zhu, Jingyang Ou, Yuling Shi, Chongxuan Li, Xiao Zhang, Jun Xu  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.07035v1  

#### Abstract
Recently, Diffusion Large Language Models (dLLMs) have demonstrated unique efficiency advantages, enabled by their inherently parallel decoding mechanism and flexible generation paradigm. Meanwhile, despite the rapid advancement of Search Agents, their practical deployment is constrained by a fundam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM-Searcher: Adapting Diffusion Large Language Models for Search Agents

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对当前 **Search Agent** 在实际部署中面临的两大核心挑战：

1. **Latency Challenge（延迟挑战）**：  
   在标准的 ReAct 范式下，Agent 需要串行执行“思考 → 工具调用 → 等待工具响应”流程。由于模型在等待外部工具返回时处于空闲状态，导致端到端推理延迟严重。

2. **Agent Ability Challenge（智能体能力挑战）**：  
   当前的 **dLLMs**（diffusion large language models）虽然具备并行解码优势，但在多步推理、工具调用格式遵循等方面表现较弱，难以胜任复杂 agentic 任务。

---

### **提出了什么新方法或新思路**

为解决上述问题，作者提出 **DLLM-Searcher**，一个专为 dLLMs 设计的搜索智能体优化框架，包含两个核心组件：

#### （1）**两阶段后训练管道（Two-stage Post-training Pipeline）**

- **Agentic SFT（Supervised Fine-Tuning）**  
  利用高性能教师模型（如 Doubao-Seed-1.8）生成高质量的多跳问答轨迹，筛选出格式正确、逻辑完整的样本进行监督微调，提升 dLLM 对 `tool_call` 格式的遵循能力和基础检索推理能力。

- **Agentic VRPO（Variance-Reduced Preference Optimization）**  
  基于 SFT 模型生成胜者/败者轨迹对（winner/loser pairs），通过偏好优化进一步强化模型的推理与信息检索能力。引入 **Agentic ELBO** 和 **Agentic Noising** 技术，确保仅关注 `think` 和 `tool_call` 区域的学习，避免工具响应信息泄露。

#### （2）**新型代理范式：P-ReAct（Parallel-Reasoning and Acting）**

一种无需额外训练的推理策略，利用 dLLM 的非自回归特性实现“边等边想”：

- **Token Pre-filling**：预填充 `<tool_call>` 和 `</tool_call>` 边界标记。
- **Confidence Biasing**：在解码过程中，对边界内的 token 位置增加置信度偏置（bias），引导模型优先解码工具调用指令。

这使得模型能先发出搜索请求，在等待搜索引擎返回结果的同时继续完成“思考”部分的生成，从而显著降低端到端延迟。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **效率** | P-ReAct 实现约 **15% 的推理加速**，打破 ReAct 的串行瓶颈 |
| **性能保持** | 性能媲美主流基于 ARMs 的 Search Agent（如 R1Searcher），无明显精度损失 |
| **可控性高** | P-ReAct 几乎以 **100% 成功率** 保证 `tool_call` 被优先解码 |
| **通用性强** | 方法适用于多种 dLLM 架构（如 SDAR），且不依赖特定训练 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

四个多跳问答（multi-hop QA）基准数据集用于评估：

| 数据集 | 描述 |
|--------|------|
| **HotpotQA** | 多跳事实推理，需从多个段落中整合信息 |
| **2WikiMultiHopQA** | 基于维基百科的复杂多跳问题 |
| **Musique** | 更长链条的多跳推理任务 |
| **Bamboogle** | 含有噪声干扰的网页搜索 QA 任务，用于测试泛化能力 |

- 测试集：从前三者开发集中各采样 500 条，Bamboogle 使用全部 125 条。
- 训练集：从 HotpotQA、2Wiki、Musique 的训练集中共采样 2048 查询，由 Doubao-Seed-1.8 生成轨迹，经过滤后得 3977 条用于 Agentic SFT；Agentic VRPO 使用 R1Searcher 发布的 8k Stage 2 数据生成 2237 个胜/负对。

---

### **实验设置和评估指标**

#### **评估指标**

- **ACCR（Accuracy）**：预测答案是否包含真实答案（golden answer）
- **ACCL（LLM-as-Judge Accuracy）**：使用 Doubao-Seed-1.8 作为裁判模型判断回答正确性，更鲁棒地处理冗长输出

> 注：不采用 EM（Exact Match）因 Agent 输出常含补充说明，精确匹配不合理。

#### **硬件与模型配置**

- **Backbone**：SDAR（Block Diffusion Language Model），block size = 128
- **检索工具**：Google Search API，返回 top-10 结果
- **训练参数**：
  - Agentic SFT：lr=1e-5, batch=32, 3 epochs
  - Agentic VRPO：lr=5e-7, batch=16, 5 epochs
- **推理配置**：128 denoising steps, temperature=1.0, confidence bias α=0.5

---

### **基线方法对比**

分为三类：

#### （1）传统 RAG 方法
- SuRe、Selective-Context、Adaptive-RAG、IRCoT、Iter-RetGen、CR-Planner、ReARTeR

#### （2）基于 ARMs 的 Search Agent
- Search-o1、Search-R1、WebSailor*、R1Searcher*

#### （3）基于 dLLMs 的 Agent（首次系统评估）
- SDAR（vanilla）、Dream、LLaDA
- 所有 dLLM 均使用 Fast-dLLM 加速推理

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1）**

| 模型 | Avg ACCR | Avg ACCL |
|------|----------|----------|
| **R1Searcher***（最强 ARM baseline） | 53.1 | 56.5 |
| **DLLM-Searcher**（本文方法） | **57.0** | **56.6** |
| LLaDA（原始 dLLM） | 34.9 | 32.5 |
| Dream | 10.1 | 10.1 |
| SDAR（未训练） | /（完全失败） | / |

> ✅ **DLLM-Searcher 不仅超越所有原始 dLLM，还全面优于 R1Searcher，在多数数据集上达到 SOTA 水平。**

---

### **与基线方法的对比结果**

- **vs. 传统 RAG**：平均 ACCR 提升超 **37个百分点**（如 ReARTeR 为 45.4 → DLLM-Searcher 为 57.0）
- **vs. ARM-based Agents**：
  - 在 HotpotQA、2Wiki、Bamboogle 上均超过 R1Searcher
  - 仅在 Musique 上略低（29.0 vs 28.2），但仍具竞争力
- **vs. Vanilla dLLMs**：原始 dLLMs 几乎无法生成合法 `tool_call`，而 DLLM-Searcher 成功率接近 100%

---

### **消融实验结果**

#### **RQ1: 两阶段后训练的有效性（Table 2）**

| 方法 | HotpotQA ACCR | 2Wiki ACCR | Bamboogle ACCR | Musique ACCR |
|------|----------------|-------------|------------------|---------------|
| Agentic SFT | 57.2 | 66.4 | 64.6 | 24.4 |
| Agentic VRPO | **60.4 (+3.2)** | **69.8 (+3.4)** | **68.8 (+4.2)** | **29.0 (+4.6)** |

✅ VRPO 在所有数据集上带来稳定增益（+3~5 pts），验证其对推理质量的提升作用。

#### **RQ2: P-ReAct 的推理效率（Figure 3）**

| 数据集 | 推理时间减少 |
|--------|--------------|
| HotpotQA | 14.77% |
| 2Wiki | 21.00% |
| Bamboogle | 22.08% |
| Musique | 12.67% |

➡️ **平均约 15% 的端到端延迟降低**，且 ACCR/ACCL 几乎无损。

#### **RQ3: dLLM 的顺序自由生成优势（Figure 4）**

- 将 P-ReAct 应用于 **Qwen3 系列 ARMs**（通过提示工程强制先输出 `tool_call`）：
  - 导致显著性能下降（最多 -13.3%）
- 而 **DLLM-Searcher + P-ReAct**：
  - 在 HotpotQA 和 Musique 上甚至略有提升
  - 最大降幅仅 4.5%，远小于 ARMs

💡 表明：**dLLMs 可在未显式解码“思考”内容时仍利用双向注意力隐式推理，生成高质量工具调用**。

---

## 4. 关键结论和发现

### **主要发现**

1. **dLLMs 完全可以胜任 Search Agent 角色**，前提是经过针对性的 agentic 后训练（Agentic SFT + VRPO）。
2. **P-ReAct 是一种高效且免训练的推理范式**，充分利用 dLLM 的非自回归特性，实现“行动前思考”的并行化。
3. **dLLMs 在结构控制方面具有天然优势**：即使 `tool_call` 先于 `think` 解码，也能保持高质量输出，而 ARMs 严重依赖 CoT 的显式表达。
4. **DLLM-Searcher 实现了性能与效率的双赢**：既达到甚至超越主流 ARM-based Agent 的准确率，又获得约 15% 的推理加速。

---

### **方法的局限性**

1. **依赖高质量教师模型生成训练数据**：Agentic SFT 需要强 teacher model（如 Doubao-Seed-1.8）提供轨迹，可能限制可扩展性。
2. **当前仅支持单次工具调用优先解码**：P-ReAct 在每轮迭代中聚焦一个 `tool_call`，对于复杂多工具调度场景有待扩展。
3. **评估集中在英文多跳 QA**：尚未在其他语言或多模态 Agent 场景中验证。

---

### **未来工作方向**

1. **构建自动化的轨迹合成 pipeline**，减少对人工标注或强 teacher 的依赖。
2. **将 P-ReAct 扩展至多工具协同调度**，支持更复杂的 agentic workflow。
3. **探索 dLLM 在其他 Agent 范式中的应用**，如 Plan-and-Execute、Tree-of-Thought 等。
4. **结合 RL 进一步优化 Agentic VRPO**，实现端到端奖励驱动的搜索策略学习。

---

> 🔚 **总结一句话**：  
> **DLLM-Searcher 成功将 dLLMs 从“低效弱智体”转变为“高效强智能”，不仅解决了延迟瓶颈，更证明了扩散语言模型作为下一代 Agent Backbone 的巨大潜力。**

</details>

---

### 6. [TEAM: Temporal-Spatial Consistency Guided Expert Activation for MoE Diffusion Language Model Acceleration](https://arxiv.org/abs/2602.08404)

**Authors**: Linye Wei, Zixiang Luo, Pingzhi Tang, Meng Li  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.08404v1  

#### Abstract
Diffusion large language models (dLLMs) have recently gained significant attention due to their inherent support for parallel decoding. Building on this paradigm, Mixture-of-Experts (MoE) dLLMs with autoregressive (AR) initialization have further demonstrated strong performance competitive with main...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TEAM: Temporal-Spatial Consistency Guided Expert Activation for MoE Diffusion Language Model Acceleration

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **MoE 架构在 dLLM 中的推理效率低下**：尽管 Mixture-of-Experts (MoE) 在训练和推理中具有参数稀疏激活的优势，但在 diffusion large language models (dLLMs) 中，由于每个 denoising step 都需对整块 token 并行处理，导致大量专家被激活，而只有少数 token 被接受（unmasked），造成严重的计算资源浪费。
- **专家激活与 token 接受之间的不匹配**：传统 MoE 设计未考虑 dLLM 的 block-wise 并行解码特性，导致实际激活的专家数量远高于理论值，削弱了稀疏性带来的加速优势。

### 🚀 提出的新方法：TEAM
提出 **TEAM**（Temporal-Spatial Consistency Guided Expert Activation）框架，通过利用 dLLM 解码过程中的**时间一致性和空间一致性**，实现更高效的专家激活策略。具体包括三个互补机制：

1. **Delayed Caching for Decoded Tokens (DCD)**  
   对已解码的 token 延迟缓存其 KV 缓存，仅在首次接受后参与一次前向传播，后续迭代直接复用，避免重复激活无关专家。

2. **Speculative Exploration for Hot Tokens (SEH)**  
   将 masked token 分为“热”（likely to be accepted soon）和“冷”两类。对 hot tokens 进行多分支 speculative decoding，提升单步 token 接受率。

3. **Limited Activation for Cold Tokens (LAC)**  
   对 cold tokens 采用双轮路由机制：先基于 hot 和新解码 token 确定必要专家集合，再将 cold tokens 限制在此子集中进行路由，防止引入额外专家开销。

### 🔍 相比现有方法的优势
| 方面 | TEAM 的优势 |
|------|-------------|
| **针对性强** | 是首个专门分析 MoE dLLM 中 expert activation 行为的工作，揭示了 temporal-spatial consistency 特性。 |
| **无需硬件改动** | 完全软件层面优化，即插即用（plug-and-play），适用于现有 MoE dLLM 架构。 |
| **兼顾性能与效率** | 在几乎无损模型准确性的前提下，显著降低专家激活数和推理延迟。 |
| **优于通用加速方案** | 不同于 dKV-Cache 或 dInfer 等通用 dLLM 加速方法，TEAM 明确针对 MoE 结构设计，效果更优。 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在四个主流基准上评估性能：
- **代码生成任务**：
  - `HumanEval`（0-shot）
  - `MBPP`（0-shot）
- **数学推理任务**：
  - `GSM8K`（0-shot）
  - `Math-500`（0-shot）

### ⚙️ 实验设置
- **主模型**：`SDAR 30B-A3B` —— 一个典型的 MoE-based dLLM，采用 block diffusion 范式。
- **硬件平台**：NVIDIA A100 80GB GPU。
- **关键超参**：
  - Unmasking threshold: $ T = 0.95 $
  - Block size: 32 tokens
  - Hot token 判定标准：$ c_k > 0.7 $ 或距离已解码 token ≤ 3 位置
  - Speculative branches: 4 条并行分支
- **评估指标**：
  - `APF`: Activated Experts Per Forward pass
  - `TPF`: Accepted Tokens Per Forward pass
  - `APT`: Activated Experts Per decoded Token（等效每 token 激活专家数）
  - `Speedup`: 相对于 vanilla 模型的端到端推理速度提升倍数
  - `Score`: 任务准确率（Pass@1）

### 🆚 基线方法对比
- **Vanilla SDAR**：原始 MoE dLLM，无任何专家激活优化。
- **dKV-Cache**：周期性刷新已解码 token 的 KV 缓存，用于缓解 bidirectional attention 下的表示漂移。
- **dInfer**：面向云部署的 MoE dLLM 推理框架，侧重 expert-parallel 执行，非针对激活效率优化。

---

## 3. 主要实验结果和性能指标

### 📊 总体性能对比（见 Table 1）

| Benchmark | Method | Score | APF ↓ | TPF ↑ | APT ↓ | Speedup ↑ |
|----------|--------|-------|--------|--------|--------|-----------|
| HumanEval | Vanilla | 79.27 | 53.34 | 2.91 | 18.33 | 1× |
|           | **TEAM** | **79.88** (+0.61) | **34.48** (↓35%) | **5.07** (↑1.74×) | **6.80** (↓63%) | **2.20×** |
| MBPP      | Vanilla | 65.76 | 49.59 | 2.74 | 18.10 | 1× |
|           | **TEAM** | **65.76** | **30.92** (↓38%) | **4.56** (↑1.66×) | **6.78** (↓63%) | **2.08×** |
| GSM8K     | Vanilla | 90.60 | 59.11 | 3.16 | 18.71 | 1× |
|           | **TEAM** | **90.30** (-0.30) | **36.20** (↓39%) | **4.79** (↑1.52×) | **7.56** (↓60%) | **1.83×** |
| Math-500  | Vanilla | 76.00 | 57.90 | 3.74 | 15.48 | 1× |
|           | **TEAM** | **75.40** (-0.60) | **36.31** (↓37%) | **5.57** (↑1.49×) | **6.52** (↓58%) | **1.64×** |
| **Average** | Vanilla | 77.91 | 54.99 | 3.14 | 17.66 | 1× |
|           | **TEAM** | **77.84** (-0.07) | **34.48** (↓37%) | **5.00** (↑1.59×) | **6.92** (↓61%) | **1.94×** |

> ✅ **关键发现**：
> - TEAM 实现平均 **1.94× 的端到端加速**，最高达 **2.2×**（HumanEval）。
> - 每步激活专家数减少 **35–39%**，同时接受 token 数提升 **1.49–1.74×**。
> - 单个 token 解码所需激活专家从 ~17.66 降至 **6.92**，甚至低于名义路由成本（8 experts/token）。

### 🔬 消融实验结果（见 Figure 5 和 Table 2）

#### （1）组件消融（逐步添加 TEAM 模块）
| 阶段 | APT ↓ | Speedup ↑ | 说明 |
|------|--------|------------|------|
| Vanilla | 17.66 | 1× | 基线 |
| + SEH | ~12.5 | ~1.5× | 多分支 speculative decoding 显著提高接受率 |
| + DCD | ~8.3 | ~1.8× | 缓存已解码 token 明显减少冗余专家激活 |
| + LAC | **6.92** | **1.94×** | 冷 token 限流进一步压缩专家调用 |

> ➡️ 三者协同作用明显，LAC 在最后阶段提供边际增益。

#### （2）缓存刷新策略对比（Table 2）
| Refresh Strategy | Avg Speedup | Avg Score |
|------------------|--------------|------------|
| Refresh every 4 steps | 1.29× | 77.84 |
| Refresh every 8 steps | 1.38× | 77.10 |
| **No refresh (Ours)** | **1.47×** | **77.57** |

> ❗ 发现：**无需刷新缓存即可保持精度**，且效率更高 —— 支持“refresh-free”设计的合理性。

#### （3）Hot Token 超参敏感性分析（Table 3）
- 最佳配置：$ T_h = 0.7, L_h = 3 $
- 更宽松（如 $ T_h=0.4 $）会轻微提升分数但增加 APF；更严格则导致性能下降。
- 当前设定在 **accuracy-efficiency trade-off 上最优**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE + dLLM 存在根本性效率矛盾**：虽然两者各自高效，但简单结合会导致专家激活过度，破坏稀疏性优势。
2. **dLLM 具有强 temporal-spatial consistency**：
   - 时间维度：已解码 token 表示稳定，可安全缓存；
   - 空间维度：masked token 路由高度集中，适合共享专家；
   - 局部性：acceptance order 呈现近似 autoregressive 趋势，支持 hot/cold 划分。
3. **TEAM 成功融合稀疏性与并行性**：通过差异化专家调度，在不牺牲性能的前提下大幅提升推理效率。

### ⚠️ 方法的局限性
- **依赖 autoregressive 初始化模型**：TEAM 的有效性部分建立在 dLLM 继承 AR prior 的基础上，若初始化方式不同可能影响 hot token 判断。
- **超参数敏感性存在边界**：虽然 $ T_h=0.7, L_h=3 $ 表现最佳，但极端任务下可能需要重新调优。
- **未扩展至多模态 dLLM**：当前验证集中在文本生成任务，是否适用于 vision-language diffusion model 待探索。

### 🔮 未来工作方向
- 将 TEAM 思路推广至其他 MoE-based dLLM（如 LLaDA 2.0）或更大规模模型。
- 动态调整 hot token 判定阈值，基于上下文自适应划分。
- 结合硬件感知调度，进一步优化专家通信与内存访问模式。
- 探索在 training 阶段引入 consistency-aware routing，增强 inference 可预测性。

---

> 💡 **总结一句话**：  
> TEAM 首次揭示了 MoE dLLM 中的专家激活低效问题，并提出一种基于 **temporal-spatial consistency** 的精细化专家调度框架，在几乎零精度损失下实现了高达 **2.2× 的推理加速**，为 MoE 与 diffusion 架构的深度融合提供了高效可行路径。

🔗 开源地址：[https://github.com/PKU-SEC-Lab/TEAM-MoE-dLLM](https://github.com/PKU-SEC-Lab/TEAM-MoE-dLLM)

</details>

---

### 7. [Parallel Track Transformers: Enabling Fast GPU Inference with Reduced Synchronization](https://arxiv.org/abs/2602.07306)

**Authors**: Chong Wang, Nan Du, Tom Gunter, Tao Lei, Kulin Seth, Senyu Tong, Jianyu Wang, Guoli Yin, Xiyou Zhou, Kelvin Zou, Ruoming Pang  
**Category**: cs.DC  
**Published**: 2026-02-10  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.07306v1  

#### Abstract
Efficient large-scale inference of transformer-based large language models (LLMs) remains a fundamental systems challenge, frequently requiring multi-GPU parallelism to meet stringent latency and throughput targets. Conventional tensor parallelism decomposes matrix operations across devices but intr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Parallel Track Transformers: Enabling Fast GPU Inference with Reduced Synchronization

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在基于 Transformer 的大语言模型（LLMs）的大规模推理中，**多 GPU 并行计算**虽然能提升吞吐量和降低延迟，但传统的 **tensor parallelism** 引入了频繁的跨 GPU 同步操作（如 all-reduce），导致通信瓶颈，限制了系统的可扩展性和推理效率。

随着模型规模增长，这些同步开销成为制约 LLM 推理性能的关键因素。

---

### 🚀 提出的新方法：Parallel Track (PT) Transformer
作者提出了一种新的架构范式——**Parallel Track (PT) Transformer**，其核心思想是：

- 将整个模型划分为多个独立并行运行的子 Transformer 实例，称为“**tracks**”。
- 每个 track 在若干层（定义为一个 track block，深度为 $D$）内独立计算，仅在 block 结束时进行一次跨 track 的同步（all-reduce）。
- 这种设计显著减少了同步次数，并降低了每次同步的数据量（因每 track 维度更低）。

该方法被称为 **track parallelism**，是一种系统层面驱动的并行策略。

---

### 🔍 相比现有方法的优势
| 对比维度 | 传统 Tensor Parallelism | PT Transformer |
|--------|------------------------|---------------|
| 同步频率 | 每一层都需同步 → $2L$ 次（注意力 + FFN） | 每 $D$ 层同步一次 → $L/D$ 次 |
| 同步数据量 | 全维度激活值交换 | 分割后的低维 track 输出 |
| 通信模式 | 高频、小批量通信，易造成阻塞 | 低频、规律性强，利于优化 |
| 可集成性 | 已广泛使用但受限于通信瓶颈 | 可无缝集成到 TensorRT-LLM 和 vLLM |

> ⭐ 最高实现 **16× 的同步操作减少**（当 $D=8$ 时，同步减少 93.75%）

此外，PT 不同于 MoE：
- MoE 是 token-level 路由，引入动态负载不均；
- PT 是所有 token 处理所有 tracks，同步节奏固定，更适合高效推理调度。

---

## 2. 核心实验方法和设置

### 📚 使用的基准测试（Benchmarks）
在以下主流评测集上评估模型质量：
- **ARC-C/E**, **HellaSwag**, **PIQA**, **SciQ**, **WinoGrande**：常识推理
- **TriviaQA (EM)**：问答准确率
- **MMLU (EM)**：多任务语言理解
- **GSM8K (EM)**, **MATH (EM)**：数学推理
- **HumanEval (Pass@1)**：代码生成能力

---

### ⚙️ 实验设置
#### 模型配置
- 模型大小：**6B, 13B, 30B** 参数
- Tracks 数量：统一设为 **n = 8**
- Track block depth $D$：测试 $D = 2, 4, 8$
- 使用 **Grouped Query Attention (GQA)**
- 所有模型预训练 token 数一致（6B: 800B；13B/30B: 400B）
- Dense 与 PT 模型采用相同训练流程，确保公平比较

#### 推理服务栈测试环境
- 硬件平台：**8×H100 GPUs**
- 测试框架：
  - **TensorRT-LLM**
  - **vLLM**
- 测试模式：
  - **Throughput Mode**：最大 batch size = 256，衡量输出吞吐（tokens/sec）
  - **Latency Mode**：batch size = 1，测量：
    - **TTFT**（Time to First Token）
    - **TPOT**（Time Per Output Token）

#### 基线方法
- 标准 dense Transformer（无 track 分割）
- 不同 $D$ 的 PT 变体作为消融实验对照

---

## 3. 主要实验结果和性能指标

### 📊 模型性能（质量保持）
尽管同步大幅减少，PT 模型在多数任务上保持与 dense 模型相当甚至更优的表现：

| 模型 | 最大同步减少 | 性能变化趋势 |
|------|-------------|------------|
| **6B** | 93.75% ($D=8$) | MMLU 明显下降（0.56 → 0.36），小模型敏感 |
| **13B** | 同上 | 几乎无损，部分指标反超（如 ARC-C 提升至 0.538） |
| **30B** | 同上 | 整体稳定，MMLU 微降（0.63 → 0.615），其他持平或略升 |

> ✅ **结论：大模型对 PT 架构容忍度更高，可在几乎不损失性能前提下极大削减同步**

---

### ⏱️ 推理效率提升（关键指标）

#### 在 **TensorRT-LLM** 上的结果：
| 指标 | 改进幅度 |
|------|---------|
| **TTFT**（最长输入 63488） | ↓ **14.7%**（1697ms → 1437ms） |
| **TPOT**（典型场景） | ↓ **2–12%** |
| **Throughput**（最高） | ↑ **up to 31.9%**（如 1024 in / 128 out 场景） |

#### 在 **vLLM** 上的结果：
| 指标 | 改进幅度 |
|------|---------|
| **TTFT**（最长输入 63488） | ↓ **~17.7%**（2981ms → 2453ms） |
| **TPOT**（长上下文） | ↓ **~2–5%** |
| **Throughput** | ↑ **up to ~20%**（短序列场景明显） |

> 💡 注：某些长输出场景（如 1024→4096）出现轻微回退，可能由于 kernel 调度未完全优化。

---

### 🔬 消融实验结果
- **$D=2$ vs $D=8$**：
  - $D=8$ 同步最少，通信开销最低，但对小模型（6B）影响较大。
  - 对 13B/30B 模型，$D=8$ 仍能维持高质量，说明 **block depth 可安全扩大以换取性能增益**。
- **Track 数量选择（n=8）**：
  - 匹配常见多卡部署（如 8×H100），便于工程落地。
  - 更多 tracks 可进一步降低单 track 维度，但需权衡内存与通信拓扑。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **同步是 LLM 推理的关键瓶颈**，尤其是在 tensor parallelism 下高频 all-reduce 操作。
2. **PT Transformer 成功将同步从每层一次降至每 $D$ 层一次**，最多减少 **16× 同步次数**。
3. 在 **13B 及以上规模模型中，PT 可在几乎不影响模型质量的前提下大幅提升推理效率**。
4. PT 架构已成功集成至两大主流推理框架（**TensorRT-LLM**, **vLLM**），实测获得：
   - **15–30% TTFT 降低**
   - **2–12% TPOT 降低**
   - **最高达 31.9% 吞吐提升**
5. PT 与 MoE 正交，后续工作 **PT-MoE** 表明二者可结合，在私有云场景中具有应用前景（见 Apple Foundation Models 2025 报告）。

---

### ⚠️ 方法的局限性
1. **小模型（如 6B）性能波动较明显**，尤其在 MMLU 等复杂任务上随 $D$ 增大而下降。
2. 当前实现依赖定制化支持（如内部版 TensorRT-LLM），开源生态尚未普及。
3. 极端长序列输出场景下，性能增益趋于饱和甚至略有回退，需进一步优化调度器。
4. track 间完全均匀划分假设较强，实际中可能存在负载细微不平衡。

---

### 🔮 未来工作方向
1. **自适应 block depth 控制**：根据输入长度或任务类型动态调整同步频率。
2. **混合并行策略设计**：将 track parallelism 与 pipeline / data / expert parallelism 更深度融合。
3. **硬件感知的 track 分布优化**：考虑 NVLink 拓扑、带宽差异等物理连接特性。
4. **扩展至训练阶段**：探索 track parallelism 在训练中的潜力，提升训练效率与容错性。
5. **轻量化 PT 架构用于边缘设备**：研究低 track 数（如 n=2~4）下的移动端适用性。

---

## ✅ 总结一句话
> **Parallel Track Transformer 通过重构模型结构，将高频同步压缩为周期性融合操作，在几乎不牺牲模型性能的前提下，实现了高达 31.9% 的吞吐提升和显著降低的 TTFT/TPOT，为高性能 LLM 推理提供了全新的系统友好型架构路径。**

</details>

---

### 8. [DLLM Agent: See Farther, Run Faster](https://arxiv.org/abs/2602.07451)

**Authors**: Huiling Zhen, Weizhe Lin, Renxi Liu, Kai Han, Yiming Li, Yuchuan Tian, Hanting Chen, Xiaoguang Li, Xiaosong Li, Chen Chen, Xianzhi Yu, Mingxuan Yuan, Youliang Yan, Peifeng Qin, Jun Wang, Yu Wang, Dacheng Tao, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.07451v1  

#### Abstract
Diffusion large language models (DLLMs) have emerged as an alternative to autoregressive (AR) decoding with appealing efficiency and modeling properties, yet their implications for agentic multi-step decision making remain underexplored. We ask a concrete question: when the generation paradigm is ch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DLLM Agent: See Farther, Run Faster

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大多数 **LLM Agent** 研究默认 **autoregressive (AR)** 解码是唯一生成范式，认为 agent 行为主要由训练数据和 workflow 设计决定，而忽略了 **generation paradigm（生成范式）本身是否系统性影响 agent 的决策行为**。  
本文提出一个根本性问题：  
> 当 agent 框架和监督信号保持不变时，仅将生成范式从 AR 改为 **Diffusion LLM (DLLM)**，是否会引发系统性的规划与工具使用行为差异？这些差异能否转化为端到端效率提升？

### 提出的新方法与新思路
- **首次系统研究 diffusion 范式在多步 agent 决策中的作用**，构建了基于 DLLM 的 **DLLM Agent**。
- 在相同的 **DeepDiver 多智能体框架** 和 **agent-oriented fine-tuning 数据** 下，对 AR 和 DLLM 背骨进行公平比较，控制变量以隔离生成范式的影响。
- 提出两项关键技术改进以适配多轮 agent 场景：
  - **Context-clean corruption**：只对动作段（action span）加噪，保留上下文完整，避免训练-推理不一致。
  - **Span-aware attention alignment**：调整 attention mask，防止 context 与 action 之间的虚假双向信息流动。

### 相比现有方法的优势
- **更高的全局规划能力**：DLLM 通过迭代去噪实现隐式全局协调，能更早收敛到正确行动路径，减少回溯。
- **更少冗余交互**：相比 AR agent 的局部增量决策，DLLM agent 更倾向于一次性制定完整计划，减少重复工具调用和推理步骤。
- **端到端速度显著提升**：在相同准确率下，平均提速超过 30%，极端案例可达 **8× 加速**。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **BrowseComp-zh**：一个多轮中文网页浏览基准，要求模型通过多次检索、推理动态证据来回答复杂查询，天然产生长交互历史。
- **Open-ended qualitative prompts**：一组自定义的开放式多步研究问题，用于定性分析 agent 的探索策略、证据整合与终止行为。

### 实验设置和评估指标
#### 统一 agent 框架
所有 agent 均运行于 **DeepDiver** 框架中，包含 Planner、Information Seeker 和 Writer 角色，支持 think、reflect、web_search、document_qa 等工具。

#### 控制变量
- 相同的工具集、API 接口、prompt 模板、状态序列化方式
- 相同的训练数据轨迹（来自 AR agent 的 rollout）
- 相同的优化预算（5 epochs, lr 5e-6 到 0）
- 相同的交互限制：
  - 最大 context 长度：32K tokens
  - 最大回合数 $ T_{\text{max}} = 15 $
  - 最大 ToolCall 次数限制

#### 评估指标
| 指标 | 描述 |
|------|------|
| **Accuracy (%)** | 最终答案正确率 |
| **Tool Calls ↓** | 平均每 episode 工具调用次数（越低越好） |
| **Turns Used ↓** | 平均使用回合数（反映交互长度） |
| **Invalid Action Rate ↑** | 动作格式错误比例（如 schema 错误、缺失分隔符） |
| **End-to-end Latency ↓** | 完成任务的总耗时（秒） |

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **AR Agent** | 使用 `openPangu-Embedded-7B` 作为 backbone，标准 AR 解码 |
| **DLLM Agent** | 使用 `openPangu-R-7B-Diffusion`，采用迭代去噪生成动作 |
| **消融变体** | 移除 context-clean corruption 或 span-aware attention alignment 的版本 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 和 Figure 2）

| Method | Accuracy (%) | Tool Calls | Turns Used | Invalid Action Rate |
|--------|---------------|------------|-------------|------------------------|
| AR Agent | 15.5 | 7.5 | 14.8 | 1.9% |
| DLLM Agent | 15.5 | **6.7** | **13.0** | **6.4%** |
| w/o context-clean corruption | 14.5 | 6.8 | 15.0 | 6.2% |
| w/o span-aware attention | 14.5 | 7.1 | 14.6 | 7.1% |

> 注：准确率持平，但 DLLM Agent 显著减少工具调用（↓10.7%）和回合数（↓12.2%），体现更高效率。

### 与基线方法的对比结果
- **端到端延迟大幅降低**：
  - 在“查找符合多个约束的科技公司”任务上，DLLM Agent 耗时 **140.95s**，AR Agent 耗时 **1152.68s** → **8.18× 加速**
  - 在“撰写特斯拉财报投资报告”任务上，DLLM Agent 耗时 **490.25s**，AR Agent 耗时 **715.31s** → **1.46× 加速（即快 31.5%）**
- **更简洁的执行轨迹**：
  - DLLM Agent 更少创建中间计划文件（如 todo_v1.md, todo_v2.md）
  - 更少发起并行搜索任务，但每次任务目标更明确
- **更高的 planner hit rate**：更早锁定正确方案，减少无效探索

### 消融实验结果
- 移除任一 masking 技术（context-clean corruption 或 span-aware attention）均导致：
  - 准确率下降约 1%
  - 回合数增加
  - 性能退化，验证了两种 masking 对齐训练与推理分布的重要性

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **生成范式本身显著影响 agent 行为**  
   即使 workflow 和训练数据完全相同，DLLM 与 AR agent 展现出本质不同的决策模式：
   - **DLLM Agent**：全局视角强，先建立高阶计划再填充细节，表现为“contract-driven”执行。
   - **AR Agent**：局部条件依赖强，逐步推进，易出现冗余验证和回溯。

2. ✅ **效率优势不仅来自 token 并行性**  
   DLLM 的加速不仅是由于并行解码，更是因为其 **workflow-level 效率提升**：
   - 更少的工具调用
   - 更短的交互链
   - 更少的中间 artifact 生成

3. ✅ **DLLM 具备更强的全局协调能力**  
   注意力动态分析显示，DLLM 在早期阶段就表现出跨块的全局注意力，支持“先看远，再动手”的规划风格。

4. ⚠️ **部署挑战依然存在**  
   - DLLM 输出结构化动作（如 ToolCall）的稳定性较差（invalid action rate 较高），需加强针对 tool-call 的专项训练。
   - 多轮场景下的训练-推理不一致问题必须通过 masking 对齐解决。

### 方法的局限性
- 实验集中在 **deep research-type 任务**（信息检索+综合），尚未验证在长期规划、具身环境或复杂逻辑推理中的泛化性。
- 所有 DLLM 训练仍基于 AR agent 的轨迹数据，未引入 diffusion-native 的 curriculum 或监督信号。
- 当输出文本量很大时（如长篇报告），DLLM 的效率优势会被稀释。

### 未来工作方向
1. **探索 diffusion-native agent 训练范式**：设计专门面向迭代 refine 的监督目标或课程学习。
2. **混合架构（Hybrid Agents）**：结合 DLLM 的全局规划能力和 AR 的精细执行能力，构建 dual-path agent。
3. **扩展至更复杂场景**：测试在软件工程、机器人控制等长视野、部分可观测任务中的表现。
4. **深入机制分析**：通过因果干预、注意力归因等手段进一步揭示 backbone 特性如何塑造 agent 行为。
5. **提升结构化输出可靠性**：开发针对 ToolCall schema 的约束解码或校验机制，降低 invalid action rate。

---

> **总结一句话**：  
> 本论文证明了 **Diffusion LLM 不仅是一种更快的生成器，更是一种具备“前瞻性思维”的 agent 背骨**，能在相同条件下实现“看得更远、跑得更快”的智能决策。

</details>

---

### 9. [FEM-Informed Hypergraph Neural Networks for Efficient Elastoplasticity](https://arxiv.org/abs/2602.07364)

**Authors**: Jianchuan Yang, Xi Chen, Jidong Zhao  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.07364v1  

#### Abstract
Graph neural networks (GNNs) naturally align with sparse operators and unstructured discretizations, making them a promising paradigm for physics-informed machine learning in computational mechanics. Motivated by discrete physics losses and Hierarchical Deep Learning Neural Network (HiDeNN) construc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FEM-Informed Hypergraph Neural Networks for Efficient Elastoplasticity

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Physics-Informed Neural Networks (PINNs)** 在求解非线性 **elastoplasticity** 问题时面临以下挑战：
- **计算成本高**：依赖 Automatic Differentiation (AD) 导致计算图庞大，训练效率低。
- **精度不足**：在复杂几何、循环加载（cyclic loading）、材料硬化（hardening）等场景下预测误差大。
- **边界条件处理困难**：Dirichlet 和 Neumann 边界条件难以有效施加，易导致梯度失衡。
- **黑箱建模**：网络作为黑盒代理，缺乏物理可解释性。

### 提出的新方法
作者提出了一种 **FEM-Informed Hypergraph Neural Network (FHGNN)**，其核心是将 **有限元法 (FEM)** 的计算流程嵌入到 **Hypergraph Neural Network** 的消息传递层中，构建一个**物理一致、可微分、白盒式的求解器**。

#### 创新点：
- **Node–Element Hypergraph 表示**：采用超图结构表示 FEM 网格，其中节点（nodes）代表网格节点，元素（elements）作为第二类节点，通过有向边连接节点与所属元素，自然编码了 FEM 的拓扑关系。
- **FEM 计算内嵌于 Message Passing**：
  - **Isoparametric Transformation Layer**：在消息传递中实现坐标变换与形函数梯度计算。
  - **Strain–Stress Layer**：基于位移场计算应变与应力，支持 J2 plasticity 模型（含 isotropic/kinematic hardening）。
  - **Global Internal Force Layer**：实现单元内力组装，形成全局内力向量。
- **端到端可微分框架**：整个流程支持反向传播，允许优化任意输入属性（如节点坐标），实现 **r-adaptivity**。
- **无需标签数据**：训练完全由物理驱动，仅需输入网格，无监督学习。

### 相比现有方法的优势
| 方面 | 传统 PINN / PIMLP / PIGCN | FHGNN |
|------|--------------------------|--------|
| **计算效率** | 高阶 AD 导致计算图复杂，训练慢 | 利用 FEM 形函数梯度，稀疏局部计算，GPU 并行加速 |
| **精度** | 位移尚可，但应力/塑性应变误差大 | 应力、应变、塑性变量均高度准确 |
| **边界处理** | 需额外损失项，易梯度失衡 | Dirichlet 条件通过掩码直接施加，Neumann 自然融入变分损失 |
| **可扩展性** | 难以处理复杂几何与非结构化网格 | 天然支持非结构化网格与复杂几何 |
| **物理一致性** | 黑箱模型，操作不可解释 | 白盒模型，每一步对应 FEM 物理操作 |

---

## 2. 核心实验方法和设置

### 数据集与问题设置
所有实验均使用 **Abaqus** 生成的 FEM 解作为参考真值，共设计 **5 个 3D 弹塑性基准问题**：
1. **3D Plastic Footing**：平面应变基础板，完美塑性 von Mises 模型。
2. **3D Linear Hardening Cantilever Beam**：含 **isotropic hardening** 与 **kinematic hardening** 的悬臂梁，验证循环加载响应。
3. **3D Linear Hardening Workpiece**：复杂几何工件，测试对非规则形状的适应能力。
4. **3D Bi-material Plate with a Hole**：双材料带孔板，验证对材料不连续性的捕捉能力。
5. **2D Plastic Footing with Cyclic Loading**：二维循环加载案例，用于对比传统 PINN。

### 实验设置
- **硬件**：FHGNN 在 **NVIDIA RTX 4090 GPU** 上运行；FEM 基线在 **Intel i7-12700H CPU** 上运行。
- **优化器**：统一使用 **L-BFGS**，初始步长 1.0。
- **输入**：FHGNN 输入为 node–element hypergraph，包含节点坐标、形函数梯度、位移等。
- **输出**：节点位移 $ u_j $、高斯点应变 $ \varepsilon_i $、应力 $ \sigma_i $。

### 评估指标
- **Relative L2 Error**：$ L_2 = \frac{\|u - u^*\|_2}{\|u^*\|_2} $
- **Mean Absolute Error (MAE)**：$ \text{MAE} = \frac{1}{N}\sum |u - u^*| $
- **训练时间 (Time)**：收敛至目标精度所需时间。
- **收敛迭代次数**：达到收敛所需的优化步数。

### 基线方法对比
- **PINN**：传统多层感知机（MLP）+ AD + PDE 残差损失。
- **PIMLP**：MLP 预测位移，使用 FEM 形函数梯度构造能量损失（如 [26,31]）。
- **PIGCN**：图神经网络（GNN）学习从坐标到位移的映射，使用 Chebyshev 卷积（如 [24,25]）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 任务 | 方法 | Time (s) | $ L_2(u_x) $ | $ L_2(u_y) $ | MAE($ \bar{\varepsilon}_p $) |
|------|------|----------|---------------|---------------|-------------------------------|
| **3D Plastic Footing** | PIMLP | 210.47 | 1.85E-01 | 6.52E-02 | 2.50E-3 |
| | PIGCN | 452.08 | 1.28E-01 | 3.38E-02 | 2.10E-3 |
| | **FHGNN** | **125.23** | **8.06E-03** | **2.40E-03** | **8.62E-05** |
| **3D Cantilever Beam (Isotropic)** | PIMLP | 737.6 | — | — | — |
| | PIGCN | 2,520.6 | — | — | — |
| | **FHGNN** | **145.51** | 4.80E-04 | 3.19E-04 | 6.10E-06 |
| **3D Workpiece** | PIMLP | 98.42 | — | — | 1.02E-05 |
| | PIGCN | 169.71 | — | — | 2.94E-05 |
| | **FHGNN** | **52.13** | — | — | **1.90E-07** |
| **Bi-material Plate** | PIMLP | 108.14 | 8.89E-02 | 7.07E-01 | 1.47E-03 |
| | PIGCN | 201.49 | 5.31E-02 | 3.05E-01 | 6.61E-04 |
| | **FHGNN** | **59.80** | **5.78E-05** | **1.77E-04** | **1.38E-06** |

### 对比结果
- **速度优势**：FHGNN 在多个任务上实现 **2–3 倍以上加速**，在稠密网格下甚至快于多核 FEM。
- **精度优势**：相比 PIMLP 和 PIGCN，FHGNN 的预测误差**降低两个数量级以上**，尤其在应力和塑性应变场表现优异。
- **收敛性**：FHGNN 收敛更快，且更稳定。

### 消融实验与关键发现
#### （1）**能量损失 vs. Galerkin 损失**
- **能量损失 (Energy Loss)**：推荐使用，收敛快、精度高、对网格密度鲁棒。
- **Galerkin 损失**：在细网格下**难以收敛**，误差随网格加密而增大（见 Fig. 26）。
- **理论解释**：Galerkin 损失的 Hessian 矩阵条件数 $ \kappa(K^TK) = \kappa(K)^2 $，远大于能量损失的 $ \kappa(K) $，导致优化病态。

#### （2）**FEM 形函数梯度 vs. AD**
- FEM 形函数梯度为**稀疏、局部、预计算常数**，避免了 AD 的高阶导数计算。
- 位移梯度计算仅涉及单元内少数节点，而非全网络参数，显著提升效率。

#### （3）**Transfer Learning 加速**
- 利用粗网格解、不同材料/载荷下的解作为初值，可进一步加速收敛（见 Fig. 24）。

#### （4）**Mesh Adaptivity**
- 将节点坐标 $ v_j $ 设为可优化变量，实现 **r-adaptivity**：网格自动在高梯度区加密，低梯度区稀疏化，系统能量进一步降低（从 -6.322 → -6.328）。

---

## 4. 关键结论和发现

### 主要发现
1. **FHGNN 显著优于现有 PINN 变体**：在精度、效率、收敛性方面全面领先，尤其适用于复杂几何、非线性弹塑性问题。
2. **FEM 内嵌机制是关键**：将 FEM 计算流程嵌入 GNN 消息传递，实现了**物理可解释性**与**计算高效性**的统一。
3. **能量损失最优**：对于存在能量泛函的材料（如 J2 plasticity），**variational energy loss** 是首选，收敛更快更稳。
4. **GPU 并行潜力巨大**：在稠密网格下，FHGNN 可超越多核 CPU FEM，具备大规模应用前景。
5. **支持自适应优化**：框架天然支持 r-adaptivity 与参数联合优化，为智能仿真提供新路径。

### 局限性
- 当前仅支持 **closed-form J2 plasticity** 模型，复杂屈服面（yield surface）的能量泛函难推导。
- 超图结构与消息传递设计依赖 FEM 先验，通用性受限于已有数值方法。
- 对极端非线性或动态断裂等问题尚未验证。

### 未来工作方向
1. **扩展至一般塑性模型**：开发兼容 AD 的通用回映算法（return mapping），支持任意屈服准则。
2. **多物理场耦合**：集成 DEM（颗粒材料）、FVM（流体力学）等模块，构建统一可微分框架。
3. **Operator Learning 结合**：融合 FNO、DeepONet 等算子学习方法，解决多尺度问题。
4. **数据同化**：引入实验数据进行校准与验证。
5. **学习更优消息传递模块**：从大数据中学习比标准 FEM 更高效的 message-passing 操作。
6. **Preconditioning 技术**：改善 Galerkin 损失的收敛性，提升其适用范围。

> **总体评价**：该工作成功将 **FEM 的物理严谨性** 与 **GNN 的计算灵活性** 相结合，提出了一种**可扩展、高效、白盒化**的物理驱动学习范式，为非线性固体力学的下一代仿真工具奠定了坚实基础。

</details>

---

### 10. [Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective](https://arxiv.org/abs/2602.08009)

**Authors**: Rui Li, Zeyu Zhang, Xiaohe Bo, Quanyu Dai, Chaozhuo Li, Feng Wen, Xu Chen  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08009v1  

#### Abstract
Multi-agent architectures built on large language models (LLMs) have demonstrated the potential to realize swarm intelligence through well-crafted collaboration. However, the substantial burden of manual orchestration inherently raises an imperative to automate the design of agentic workflows. We fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Towards Adaptive, Scalable, and Robust Coordination of LLM Agents: A Dynamic Ad-Hoc Networking Perspective

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前基于 **Large Language Model (LLM)** 的多智能体系统（**Multi-Agent System, MAS**）在协调机制上存在三大核心挑战：

- **Adaptivity（适应性）不足**：传统方法依赖预设的静态拓扑（如链式、星型结构），无法根据推理过程中的动态消息流调整通信路径。
- **Scalability（可扩展性）差**：当新增或移除智能体时，需要重新进行昂贵的离线优化或全局调度，难以支持开放成员制。
- **Robustness（鲁棒性）弱**：中心化控制架构（如 meta-controller）存在单点故障风险（Single Point of Failure, SPoF），一旦控制器被攻击或出错，整个系统可能崩溃。

这些问题共同构成了一个“三难困境”——如何在不牺牲效率的前提下同时实现 **Adaptivity、Scalability 和 Robustness**。

---

### **提出了什么新方法或新思路**

作者提出 **RAPS**（**Reputation-Aware Publish-Subscribe**），一种受 **动态自组织网络（Dynamic Ad-Hoc Networking）** 启发的去中心化协调范式。

#### **核心思想：将 LLM Agent 协调类比为网络通信问题**
- 将每个 LLM Agent 视为一个 **网络主机（Host）**
- 将自然语言消息视为 **语义数据包（Packet）**
- 将通信模式视为基于意图的 **内容中心网络（Content-Centric Networking）**

#### **RAPS 的三层架构设计**

| 层级 | 组件 | 功能 |
|------|------|------|
| **Substrate（底层基质）** | **Distributed Publish-Subscribe Protocol** | 去中心化解耦生产者（Publisher）与消费者（Subscriber），通过 Broker 进行语义匹配 |
| **Overlay I** | **Reactive Subscription** | 允许 Agent 在运行时动态更新其订阅意图（intent），实现自适应角色演化 |
| **Overlay II** | **Bayesian Reputation** | 每个 Agent 配备本地 **watchdog**，通过贝叶斯估计维护对等体的信誉，实现去中心化信任机制 |

---

### **相比现有方法的优势**

| 维度 | 传统方法缺陷 | RAPS 改进 |
|------|--------------|-----------|
| **Adaptivity** | 固定拓扑，通信与内容无关（communication-agnostic） | 意图驱动，消息路由随上下文动态演化 |
| **Scalability** | 新增 Agent 需重新训练或优化 | 支持即插即用，无需全局重构 |
| **Robustness** | 中心控制器易受攻击，存在 SPoF | 分布式信誉机制自动隔离恶意节点 |
| **训练开销** | 多数需离线搜索或训练协调图 | 完全无需训练，纯推理时协调 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验覆盖五个代表性任务，涵盖三大类别：

| 类别 | 数据集 | 任务描述 | 评估指标 |
|------|--------|----------|----------|
| **General Reasoning** | MMLU | 跨学科多选题（STEM、人文等） | Accuracy |
| **Mathematical Reasoning** | GSM8K, SVAMP, AQuA | 数学应用题求解 | Accuracy |
| **Code Generation** | HumanEval | Python 编程问题生成 | Pass@1 |

> 数据统计详见附录 Table 6。

---

### **实验设置和评估指标**

- **LLM Backbone**：统一使用 `GPT-4o-mini` 作为所有 Agent 的基础模型，确保公平比较。
- **Agent 数量**：默认配置 5 个 Agent。
- **最大通信轮次**：k = 5。
- **Broker 实现**：
  - 默认使用 `text-embedding-3-small` 进行语义相似度匹配（高效）
  - 消融实验中也测试了 LLM-based Broker（更精确但更慢）

---

### **基线方法对比**

共四类基线：

| 类型 | 代表方法 | 特点 |
|------|---------|------|
| **Single-Agent Models** | CoT, ComplexCoT, SC | 单模型链式思维或自洽推理 |
| **Static Multi-Agent Models** | Chain, Star, Tree, Random, LLM-Debate | 固定拓扑结构 |
| **Communication-Agnostic Models** | GPTSwarm, AFlow, G-Designer | 离线搜索最优图结构 |
| **Meta-Controlled Models** | AutoAgents, Puppeteer, MAS-Zero | 使用中央控制器动态调度 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table 1）**

| 方法 | MMLU | GSM8K | SVAMP | AQuA | HumanEval | **Average** |
|------|------|-------|-------|------|-----------|------------|
| G-Designer (SOTA prior) | 86.3 | 93.2 | 90.7 | 79.4 | 90.2 | 88.0 |
| **RAPS (Ours)** | **88.2** | **95.4** | **92.2** | **82.6** | **91.5** | **90.0** |

✅ RAPS 在所有任务上均达到 **state-of-the-art 性能**，平均提升 **+2.0%**。

---

### **与基线方法的对比结果**

#### ✅ **优于静态结构**
- Chain、Star 等固定拓扑在复杂任务（如 SVAMP）上表现不佳（Chain: 82.6 vs RAPS: 92.2）
- 表明刚性路径无法适配多样化推理需求。

#### ✅ **优于离线优化方法**
- GPTSwarm、AFlow、G-Designer 虽经优化，但仍受限于预设图结构。
- RAPS 在 AQuA 上比 G-Designer 高 **+3.2%**，说明其具备更强的推理路径灵活性。

#### ✅ **优于中心化控制**
- Puppeteer、AutoAgents 受限于控制器的认知负荷。
- RAPS 平均高出 **+5.0%**，且无 SPoF 风险。

---

### **消融实验结果（Table 3）**

| 变体 | MMLU | GSM8K | HumanEval |
|------|------|-------|-----------|
| **RAPS (Full)** | 88.2 | 95.4 | 91.5 |
| w/o RS（无 Reactive Subscription） | 85.6 (-2.6) | 93.7 (-1.7) | 89.3 (-2.2) |
| w/o BR（无 Bayesian Reputation） | 86.9 (-1.3) | 94.5 (-0.9) | 90.7 (-0.8) |
| w/o Both | 83.7 (-4.5) | 92.8 (-2.6) | 88.5 (-3.0) |

> ✅ 两个 Overlay 机制均有显著贡献，**Reactive Subscription 对性能影响更大**。

---

### **其他关键实验分析**

#### 🔬 **可扩展性分析（Figure 3a）**
- 随着 Agent 数量增加，Chain 和 Puppeteer 性能下降明显。
- **RAPS 持续提升性能**，验证其良好的可扩展性。

#### ⏱️ **效率分析（Figure 3b）**
- GPTSwarm/G-Designer 的优化开销随 Agent 数指数增长（>2小时 for 20 agents）。
- **RAPS 推理延迟稳定增长**，适合大规模部署。

#### 🛡️ **鲁棒性分析（Table 2）**
- 注入对抗性 Agent（Adversarial Agents）后：
  - G-Designer 在 2T3A 场景下准确率降至 **15.0%**
  - **RAPS 仍保持 83.0%**，远超其他方法。
- Puppeteer-C（攻击中央控制器）直接崩溃至 **13.7%**，凸显 SPoF 风险。

#### 🔄 **成本-性能权衡（Figure 4）**
- RAPS 在不同通信轮次（k=3,5,7,10）下形成 **帕累托前沿（Pareto Frontier）**，在精度与 token 消耗之间取得最佳平衡。

---

## 4. 关键结论和发现

### **主要发现**

1. **动态自组织网络视角是解决 LLM Agent 协调三难困境的有效途径**：
   - 将 Agent 协调建模为 **发布-订阅（Publish-Subscribe）** 通信问题，实现了去中心化、意图驱动的协作。

2. **Reactive Subscription 显著提升适应性**：
   - Agent 可根据上下文动态调整角色（如从“数学专家”细化为“距离-速率-时间分析员”），避免语义错配。

3. **Bayesian Reputation 实现去中心化鲁棒性**：
   - 本地 watchdog 结合一阶与二阶证据（first-hand & second-hand），有效识别并隔离恶意或低质量输出。

4. **RAPS 在性能、可扩展性和鲁棒性上全面超越现有方法**：
   - 不仅在标准任务上 SOTA，还能在对抗环境下维持高可靠性。

---

### **方法的局限性**

1. **依赖基础 LLM 能力**：
   - RAPS 是“智能放大器”，不能弥补基础模型本身的知识或推理缺陷。

2. **冷启动问题（Cold Start）**：
   - 初始阶段信誉系统缺乏历史数据，可能导致短暂误判。

3. **未引入学习机制**：
   - 当前为纯推理时协调，未利用强化学习进一步优化通信策略。

---

### **未来工作方向**

1. **构建可学习的协调协议**：
   - 利用分布式信誉分数作为内在奖励信号，训练更高效的通信策略。

2. **引入网络拥塞控制机制**：
   - 借鉴 TCP/IP 的 **Congestion Control**，防止上下文溢出或 token 浪费。

3. **分层子网划分（Hierarchical Subnetting）**：
   - 面对超大规模 Agent 社会，可借鉴 Kleinrock 的分层路由思想进行功能聚类。

4. **跨会话信誉迁移**：
   - 解决冷启动问题，通过持久化信誉先验或角色风险建模加速收敛。

---

> **总结**：RAPS 不仅是一个新方法，更是开启了一个全新的研究范式——**将经典计算机网络原理系统性迁移到 LLM 多智能体系统设计中**，为构建开放、自组织、可信的 Agent 社会提供了坚实基础。

</details>

---

### 11. [Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System](https://arxiv.org/abs/2602.08335)

**Authors**: Yanming Li, Xuelin Zhang, WenJie Lu, Ziye Tang, Maodong Wu, Haotian Luo, Tongtong Wu, Zijie Peng, Hongze Mi, Yibo Feng, Naiqiang Tan, Chao Huang, Hong Chen, Li Shen  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.08335v1  

#### Abstract
Integrating Large Language Models (LLMs) with external tools via multi-agent systems offers a promising new paradigm for decomposing and solving complex problems. However, training these systems remains notoriously difficult due to the credit assignment challenge, as it is often unclear which specif...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Who Deserves the Reward? SHARP: Shapley Credit-based Optimization for Multi-Agent System

---

## 1. 论文的主要贡献和创新点

### **解决的问题**
在基于 **Large Language Models (LLMs)** 和外部工具的 **multi-agent system (MAS)** 中，尽管多智能体架构能有效分解复杂任务（如规划、执行），但训练过程面临严重的 **credit assignment problem**（信用分配问题）：
- 成功或失败的决策轨迹难以归因于具体的智能体（如 planner 或 worker）。
- 现有方法通常依赖 **sparse reward** 或 **globally broadcast reward**，无法区分不同角色的边际贡献，导致策略更新低效甚至误导。

### **提出的新方法：SHARP**
作者提出了 **SHARP (Shapley-based Hierarchical Attribution for Reinforcement Policy)**，一种基于 **Shapley Value** 的精细化信用分配框架，用于优化多智能体强化学习。

#### **核心创新点**
- **三元奖励机制 (Tripartite Decomposed Reward)**
  1. **Global Broadcast-Accuracy Reward**：全局准确率信号，确保任务最终目标对齐。
  2. **Shapley-based Marginal-Credit Reward**：基于 **Shapley Value** 计算每个 agent 的因果贡献，实现个体级信用量化。
  3. **Tool-Process Reward**：过程级反馈，评估工具调用的有效性和正确性，提升执行效率。

- **Counterfactual Masking 机制**
  - 通过“移除”某个 agent 并观察任务成功率的变化（即 $ R_{\text{acc}}(T) - R_{\text{acc}}(T^{(-m)}) $），计算其 **marginal contribution**。
  - 数学上逼近 **Shapley Value**，实现对 planner 和 worker 的差异化信用赋值。

- **Hierarchical Advantage Normalization**
  - 对每个 agent 的优势函数进行组内标准化（group-relative advantage），降低方差，提升训练稳定性。

### **相比现有方法的优势**
| 方面 | 传统方法 | SHARP |
|------|--------|-------|
| **Credit Assignment** | 全局广播，模糊归因 | 精细化、个体化、基于因果推理 |
| **Reward Signal** | 单一终端奖励 | 多维度：结果 + 贡献 + 过程 |
| **训练稳定性** | 高方差，易震荡 | 低方差梯度，稳定收敛 |
| **可解释性** | 黑箱策略更新 | 可分析各 agent 的实际贡献 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
实验覆盖多个真实世界复杂任务基准：
- **MuSiQue**：多跳问答，需组合多个单跳问题。
- **GAIA-text**：综合 AI 助手评测，涵盖推理、网页浏览、工具使用。
- **WebWalkerQA**：多步网页导航与证据聚合。
- **FRAMES**：端到端检索增强生成的综合性评测。
- **DocMath-Eval**：长文本中的数学推理能力评估（含表格与文本）。

### **实验设置**
- **模型骨干**：主要基于 **Qwen3-8B**，部分对比使用 LLaMA-3.1-8B。
- **训练方式**：采用 **self-play multi-agent modeling**，所有角色共享同一 policy，通过 role-specific prompts 区分 planner 与 worker。
- **优化算法**：基于 **Group Relative Policy Optimization (GRPO)** 改进，引入 SHARP 的 credit 分配机制。
- **超参数**：reward 权重设为 $\alpha=0.9$, $\beta=0.9$, $\gamma=0.1$；batch size 256，训练 180 步。

### **评估指标**
- **主指标**：**Average Match Accuracy**（平均匹配准确率）
- **辅助分析**：
  - Planner Score（planner 的平均 Shapley 值）
  - Useful/Harmful Subagent 比例
  - 推理 token 消耗量
  - MMLU 准确率（检测灾难性遗忘）

### **基线方法对比**
| 类别 | 方法 |
|------|------|
| **Zero-shot / RAG** | LLaMA-3.1-8B RAG, Qwen3-8B RAG |
| **Prompt-based Planning** | Plan-Search |
| **Single-agent RL** | Search-R1, Single-agent GRPO |
| **Multi-agent (无 marginal credit)** | Planner-Worker, G-Designer, CARD, COA, MATPO, AceSearcher |
| **Proposed** | **SHARP** |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**
| 方法 | MuSiQue | GAIA-text | WebWalkerQA | FRAMES | **AVG** |
|------|---------|-----------|-------------|--------|--------|
| Single-agent GRPO | 45.93 | 27.97 | 7.47 | 30.20 | 27.89 |
| MATPO (SOTA MAS) | 47.00 | 31.65 | 7.47 | 37.10 | **30.81** |
| **SHARP (Ours)** | **50.76** | **33.70** | **8.50** | **37.29** | **32.56** |

- **相对提升**：
  - 比 **single-agent** 方法平均提升 **23.66%**
  - 比 **multi-agent** 方法平均提升 **14.05%**

### **消融实验结果（Ablation Studies）**
#### （1）Shapley Credit 的作用（图3）
- **完整 SHARP**（planner + worker credit）：MuSiQue 达 **50.76**
- 仅 planner credit：47.60
- 仅 worker credit：48.00
- 无 Shapley：47.00
- ✅ **联合训练 planner 与 worker 的 credit 显著优于单独优化**

#### （2）Scaling Laws（图4）
- 在 **MuSiQue** 上测试不同模型大小（0.6B → 8B）：
  - SHARP 在 8B 模型上比 single-agent GRPO 高出 **14.41 pts**
  - 表明 SHARP 更能从更大模型中受益

#### （3）训练稳定性（图5）
- 在 GAIA-text 上训练 180 步：
  - SHARP 准确率从 27.53% 单调上升至 33.70%
  - GRPO 和 MATPO 出现明显波动
- ✅ SHARP 具备更强的长期训练稳定性

#### （4）协调性分析（图6）
- **Harmful subagent 比例**：
  - Baseline: 5.48%
  - **SHARP**: **4.40%** ↓
- **Useful subagent 比例**：
  - 从 11.03% ↑ 至 **12.96%**
- **Planner Score (avg. Shapley)**：
  - 从 0.4542 ↑ 至 **0.5084**
- ✅ SHARP 显著改善了 planner-worker 协调质量

---

## 4. 关键结论和发现

### **主要发现**
1. **Marginal Credit 是性能跃迁的关键**
   - 相比架构设计或优化策略，**显式的 marginal credit modeling** 是 SHARP 性能领先的核心驱动力。
2. **Shapley-based Attribution 提升训练效率**
   - 通过 counterfactual 分析隔离个体贡献，显著降低策略更新噪声，避免“搭便车”或“替罪羊”现象。
3. **协同优化带来协同增益**
   - planner 与 worker 的 credit 联合建模产生 **synergistic effect**，最终性能远超各部分之和。
4. **更强的泛化与扩展能力**
   - 在 DocMath-Eval 上跨任务表现最优。
   - 随模型规模增大，SHARP 的优势持续扩大。

### **方法的局限性**
- **计算开销增加**：Shapley 值估算需多次反事实 rollout，训练时间随 agent 数增长。
  - 但可通过 **sparsification**（只对部分 subagent 计算）缓解（见图7）。
- **仍存在冗余调用**：即使在 SHARP 下，**useful subagent 仍占少数**（约13%），表明当前 MAS 存在系统性效率瓶颈。
- **对 planner 的间接影响建模较粗略**：planner 的 credit 通过 worker 的 max(credit) 聚合，可能忽略长期结构影响。

### **未来工作方向**
1. **更高效的 Shapley Approximation**
   - 引入采样或代理模型加速 marginal contribution 估计。
2. **动态 agent Pruning**
   - 基于 credit 信号自动剪枝低效 worker，构建更紧凑的 agent graph。
3. **跨任务迁移 credit 策略**
   - 将 learned credit assignment logic 泛化到新任务或新 agent topology。
4. **结合 value-based 方法**
   - 探索将 Shapley credit 与 critic network 结合，进一步提升样本效率。

---

> **总结一句话**：  
> **SHARP 通过引入 Shapley-based marginal credit，首次实现了 multi-agent LLM system 中“谁该得奖”的精确回答，不仅显著提升了性能与稳定性，也为可解释、高效、可扩展的 agent coordination 提供了新范式。**

</details>

---

### 12. [Scalable Dexterous Robot Learning with AR-based Remote Human-Robot Interactions](https://arxiv.org/abs/2602.07341)

**Authors**: Yicheng Yang, Ruijiao Li, Lifeng Wang, Shuai Zheng, Shunzheng Ma, Keyu Zhang, Tuoyu Sun, Chenyun Dai, Jie Ding, Zhuo Zou  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.07341v1  

#### Abstract
This paper focuses on the scalable robot learning for manipulation in the dexterous robot arm-hand systems, where the remote human-robot interactions via augmented reality (AR) are established to collect the expert demonstration data for improving efficiency. In such a system, we present a unified f...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Scalable Dexterous Robot Learning with AR-based Remote Human-Robot Interactions*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
该论文聚焦于**灵巧机器人臂手系统（dexterous robot arm-hand systems）中的可扩展操作学习问题**。传统方法面临以下挑战：
- **专家示范数据获取成本高且效率低**，尤其是在真实世界中进行teleoperation时；
- 单纯的**行为克隆（Behavior Cloning, BC）易受数据不匹配和误差累积影响**，导致策略退化；
- **强化学习（RL）训练样本效率低、收敛慢**，且在复杂任务中可能出现**策略崩溃（policy collapse）**。

为解决上述问题，论文提出了一种结合**增强现实（AR）远程交互、模仿学习与对比学习增强的强化学习**的统一框架。

---

### **提出了什么新方法或新思路**

论文提出一个**两阶段学习框架**：

1. **第一阶段：基于AR的远程人类-机器人交互用于行为克隆预训练**
   - 构建了一个通用的**AR-based teleoperation system**，利用HoloLens 2捕捉人类专家的手部姿态，并通过Unity平台实现与多种机器人的无线远程连接。
   - 利用该系统高效收集少量高质量专家轨迹（仅15条），用于初始化策略网络（behavior cloning pretraining）。

2. **第二阶段：对比学习增强的Soft Actor-Critic（SAC）算法**
   - 引入一个**projection head**结构，在actor网络训练过程中施加**对比损失（contrastive loss）**，强制智能体的动作表示靠近专家动作的表示空间。
   - 设计了**事件驱动的奖励函数（event-driven augmented reward）** 来提升安全性。
   - 对比学习作为辅助目标，引导RL策略保留专家偏好，防止策略偏离导致崩溃。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **数据采集效率** | AR-based teleoperation支持远程、直观的操作，兼容多设备，显著降低数据采集门槛和时间成本。 |
| **训练效率与稳定性** | 相比标准PPO和SAC，所提方法**训练速度提升约4倍**，并有效避免了policy collapse。 |
| **样本效率** | 仅需**15条专家轨迹**即可完成有效预训练，适合小样本场景。 |
| **性能表现** | 在ball grasping和bottle grasping任务中均达到最高成功率，优于主流RL与BC+RL方法。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
- **自建数据集**：通过AR teleoperation系统采集了**15条专家演示轨迹**，涵盖deformable ball和bottle的抓取任务。
- 所有训练数据均来自作者构建的远程人机交互系统，未使用公开数据集。

---

### **实验设置和评估指标**

#### **仿真环境**
- 使用 **PyBullet** 进行物理仿真；
- 机器人平台为 **CGXi-G6机械臂 + Inspire灵巧手**；
- 视觉感知采用 **YOLOv8** 检测目标物体位置；
- 状态向量维度：20维；动作向量维度：8维；
- 最大轨迹长度：T = 100 time-steps；
- 训练硬件：Intel Xeon Gold 6226R CPU + NVIDIA RTX 4090 GPU；
- 总模拟试验次数：10⁴次。

#### **真实世界实验**
- 在实际机器人平台上部署训练好的策略；
- 使用AR界面远程启动机器人执行抓取任务；
- 每项任务进行50次真实测试。

#### **评估指标**
- **Mean Reward**（平均累计奖励）
- **Mean Success Rate**（平均成功率达到目标的比例）
- **Number of Iterations until Convergence**（收敛所需迭代次数）
- **Time Consumption until Convergence (min)**（收敛耗时）
- **Sim-to-Real Gap** 分析（比较仿真与真实环境的表现差异）

---

### **基线方法对比**

| 基线方法 | 描述 |
|---------|------|
| **PPO** [50] | 经典on-policy RL算法，作为强基准之一 |
| **SAC** [51] | 高效稳定的off-policy最大熵RL算法，广泛用于连续控制 |
| **BC+SAC** | 行为克隆预训练 + SAC微调，用于验证对比学习的有效性（消融实验） |
| **Ours (Proposed Method)** | BC预训练 + SAC + projection head + contrastive loss |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见Table III）**

| 任务 | 指标 | PPO | SAC | BC+SAC | **Ours** |
|------|------|-----|-----|--------|----------|
| **Ball Grasping** | Mean Reward | 584.03 | 1055.16 | 1070.77 | **1145.61** |
|                   | Success Rate | 37.33% | 84.22% | 85.49% | **91.80%** |
|                   | 收敛迭代数 | 150 | 140 | 30 | **30** |
|                   | 收敛耗时(min) | 355 | 300 | 75 | **75** |
| **Bottle Grasping** | Mean Reward | 696.76 | 1052.78 | 1110.51 | **1155.79** |
|                    | Success Rate | 44.18% | 83.93% | 88.66% | **91.83%** |
|                    | 收敛迭代数 | 130 | 120 | 25 | **23** |
|                    | 收敛耗时(min) | 340 | 270 | 60 | **55** |

> ✅ **结论**：所提方法在所有指标上全面领先，尤其在**训练速度（快约4倍）和成功率方面优势明显**。

---

### **消融实验结果**

- **BC预训练的作用**：
  - 显著减少RL阶段的探索空间，使策略快速接近最优区域；
  - 是加速收敛的关键因素（BC+SAC已比纯SAC快4–5倍）。

- **对比学习（contrastive learning）的作用**：
  - 在BC+SAC基础上进一步提升性能（success rate ↑ ~3–6%）；
  - 成功防止**policy collapse**现象（从reward曲线稳定性可看出）；
  - projection head通过拉近agent与expert在隐空间中的表示距离，增强了策略鲁棒性。

> 🔍 图6显示：Ours方法不仅收敛更快，且最终reward更高、波动更小。

---

### **真实世界实验结果（图8）**

| 任务 | PPO | SAC | BC+SAC | **Ours** |
|------|-----|-----|--------|----------|
| Ball Grasping | 32.0% | 40.0% | 66.0% | **72.0%** |
| Bottle Grasping | 70.0% | 72.0% | 86.0% | **90.0%** |

- 尽管存在**sim-to-real gap**（尤其是对形变球体感知不准），但所提方法仍保持最佳表现；
- 瓶子（刚性圆柱体）抓取成功率接近仿真结果，表明方法具备良好迁移能力。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **AR-based teleoperation是高效收集专家示范的有效手段**，特别适用于远程、低成本、跨平台的数据采集。
2. ✅ **行为克隆预训练极大提升了RL的初始策略质量**，显著缩短训练时间。
3. ✅ **引入contrastive learning并通过projection head约束策略空间，能有效防止policy collapse**，提高训练稳定性和最终性能。
4. ✅ 所提方法在**仿真与真实环境中均优于PPO、SAC及BC+SAC**，实现了更高的成功率与更快的收敛速度。
5. ✅ 实现了较小的**sim-to-real gap**，尤其对于几何规则的目标（如瓶子）具有良好的泛化能力。

---

### **方法的局限性**

1. 🚫 **依赖高质量AR姿态估计**：若AR设备对手部追踪不准（如遮挡、光照变化），会影响专家数据质量。
2. 🚫 **仅适用于小规模专家数据场景**：虽然强调“少样本”，但极端情况下（如<5条轨迹）效果可能下降。
3. 🚫 **当前仅针对单臂单手系统**，尚未扩展到双臂协作或多智能体系统。
4. 🚫 **缺乏触觉反馈（haptic feedback）**，限制了对高度动态或柔软物体的精细操控能力。

---

### **未来工作方向**

1. 🔮 **联合多个physics simulator进行策略训练**，以增强系统鲁棒性（参考[54] Polysim）；
2. 🔮 **提升仿真中物体几何重建 fidelity**，缩小sim-to-real gap，例如融合高分辨率触觉建模（参考[53]）；
3. 🔮 **融合2D语义与3D几何信息**（如Visual-Geometry Diffusion Policy [55]），提升空间理解能力；
4. 🔮 探索**视觉+触觉反馈的混合teleoperation**，支持更复杂的deformable object manipulation；
5. 🔮 将本方法扩展至**multi-agent dexterous systems**（如双臂协同操作）；
6. 🔮 在非结构化真实环境中研究**RL与multimodal imitation learning的联合部署**，推动实用化落地。

---

> 💡 **总体评价**：本文提出了一种**高效、稳定、可扩展的灵巧操作学习范式**，将AR交互、模仿学习与对比增强RL有机结合，为未来人机协同机器人学习提供了重要技术路径和实证基础。

</details>

---

### 13. [Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs](https://arxiv.org/abs/2602.08241)

**Authors**: Siqu Ou, Tianrui Wan, Zhiyuan Zhao, Junyu Gao, Xuelong Li  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.08241v1  

#### Abstract
While chain-of-thought (CoT) reasoning has substantially improved multimodal large language models (MLLMs) on complex reasoning tasks, existing approaches largely rely on long textual reasoning trajectories and provide limited mechanisms for learning stable visual attention policies. Our analysis sh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Do MLLMs Really See It: Reinforcing Visual Attention in Multimodal LLMs*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Multimodal Large Language Models (MLLMs)** 在复杂视觉推理任务中虽然具备强大的语言推理能力（如 Chain-of-Thought, CoT），但在**视觉注意力机制上存在严重缺陷**。具体表现为：

- **早期视觉关注错误难以纠正**：一旦模型在推理初期将注意力放在错误的图像区域，后续的 CoT 推理过程很少能修正这一偏差，导致“错误传播”（error propagation）。
- **缺乏有效的信用分配机制**（credit assignment）：现有的训练目标（如答案准确率、格式奖励）未能为视觉注意力行为提供明确的学习信号，导致模型学习不到稳定、可靠的视觉聚焦策略。

该问题限制了 MLLMs 在需要精确视觉定位的任务中的表现，尤其是在高分辨率、多对象、信息密集的场景中。

---

### ✅ 提出了什么新方法或新思路

作者提出了一种基于 **Reinforcement Learning (RL)** 的新型训练框架 —— **SAYO**，其核心是引入一个 **region-level 视觉注意力奖励机制**（Entropy-Based Target Attention Reward）。

#### 主要创新点：

1. **视觉注意力奖励（Visual Attention-Based Reward）**  
   利用带有 **bounding box 标注**的数据，计算模型生成 token 时对目标视觉区域的关注程度，并据此设计奖励函数：
   $$
   r_v = \tanh\left(\log\left(\frac{a + \epsilon}{u + \epsilon}\right)\right)
   $$
   其中 $a$ 是对目标区域的平均注意力权重，$u$ 是对整个图像的平均注意力权重。该奖励鼓励模型在关键决策点更多地关注正确的视觉区域。

2. **熵选择机制（Entropy-Selective Reward）**  
   并非对所有生成 token 都施加注意力奖励，而是仅针对**高熵 token**（即模型不确定性高的 token）进行奖励。这些 token 往往对应推理的关键步骤，此时强制模型“看图验证”（Look-to-Verify）可有效抑制幻觉和语言先验依赖。

3. **无需外部提示或推理时干预**  
   与需要人工设计 visual prompt 或运行时迭代反思的方法不同，SAYO 在训练阶段通过 RL 强化注意力行为，**推理时完全无需额外操作**，保持高效性和通用性。

---

### ✅ 相比现有方法的优势

| 维度 | 现有方法（如 ViP, ReFocus, Reflection-V） | SAYO |
|------|----------------------------------------|------|
| 是否依赖外部工具 | 是（需标注/编辑图像） | 否 |
| 是否依赖推理时提示工程 | 是（如 reflection, look-back） | 否 |
| 是否直接优化注意力行为 | 间接（通过任务性能反馈） | 直接（显式注意力奖励） |
| 泛化能力 | 受限于特定流程 | 跨任务强泛化（数学、图表、一般视觉） |
| 训练效率 | 多数为监督微调 | 基于 GRPO 的强化学习，更适配策略优化 |

> ✅ **优势总结**：SAYO 将视觉注意力建模为可学习的策略，而非依赖后处理或外部干预，实现了端到端、稳定的视觉接地能力提升。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

| 类别 | 数据集 | 描述 |
|------|-------|------|
| **一般视觉推理** | M3CoT, V*, MMStar, MME-RealWorld Lite | 包含多步推理、真实世界复杂图像理解任务 |
| **数学视觉推理** | We-Math, MathVision | 图像中的几何题、数学图表理解 |
| **结构化图表理解** | ChartQA, AI2D, CharXiv | 折线图、柱状图、科学插图等图表问答 |
| **训练数据来源** | GQA + ReFocus_Data | ~20k 样本，用于构建带视觉区域标注的 RL 训练数据 |

---

### ⚙️ 实验设置和评估指标

- **基础模型**：
  - `Qwen3-VL-4B/8B/30B`
  - `InternVL3.5-8B`

- **训练方法**：
  - 使用 **Group Relative Policy Optimization (GRPO)** 进行强化学习训练
  - 训练周期：4 epochs，6×NVIDIA H200 GPU
  - 注意力奖励仅作用于 **top 30% 高熵 token**

- **评估指标**：
  - 主要指标：各 benchmark 上的 **accuracy**
  - 辅助分析指标：
    - **Target Attention Score (TAS)**：衡量模型对目标区域的注意力集中程度
    - **Attention Advantage Score (Ra)**：归一化的注意力优势得分

- **Prompt 设计**：
  - 训练时要求模型在 `<think>` 中进行内部推理，强调关注目标区域
  - 测试时统一使用标准思维链模板

---

### 🔁 基线方法对比

| 类型 | 基线模型 |
|------|---------|
| **闭源模型** | GPT-4o, Gemini 2.5 Pro |
| **开源通用 MLLMs** | Qwen3-VL, InternVL3.5, Kimi-VL-16B |
| **开源推理专用 MLLMs** | OpenVLThinker-7B, Semantic-back-7B, ViGoRL, NoisyRollout-7B, R1-Onevision-7B |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| Model | Avg Score |
|-------|-----------|
| **GPT-4o** | – |
| **SAYO-Qwen-8B** | **64.03** ✅（开源最佳） |
| Qwen3-VL-8B | 59.64 |
| OpenVLThinker-7B | 59.79 |
| NoisyRollout-7B | 61.80 |
| **SAYO-Qwen-4B** | **62.17**（小模型超越大模型） |

> 💡 **亮点突破**：
> - SAYO-Qwen-8B 在 **MMStar** 上超过 GPT-4o 和 Kimi-VL-16B
> - 在 **We-Math** 和 **MathVision** 上显著领先，尽管未使用数学数据训练 → 显示出强大跨域泛化能力

---

### 🔍 与基线方法的对比结果

| 对比维度 | 结果 |
|--------|------|
| vs. 闭源模型 | 在部分任务（如 MMStar）上优于 GPT-4o |
| vs. 开源通用模型 | 平均提升约 **+4.4 pts**（Qwen-8B → SAYO-Qwen-8B） |
| vs. 推理专用模型 | 全面优于 OpenVLThinker、ViGoRL 等，且不牺牲通用性 |
| vs. 不同规模模型 | SAYO-Qwen-4B > 所有其他 7B/8B 模型，接近 30B 表现 |

---

### 🔧 消融实验结果（Table 2 & Table 3）

#### ✅ 注意力奖励的有效性（Table 2）

| 设置 | Avg 提升（vs. Base） |
|------|------------------|
| Only Accuracy Reward | +1.28 |
| Only Attention Reward | **+4.32** ✅ |
| Combined Reward | **+4.63** |

> ❗ 结论：**注意力奖励比答案准确性奖励更重要**，说明当前瓶颈在于“看见”，而非“推理”。

#### ✅ 高熵 token 选择的重要性（Table 3）

| Token Selection | Avg Score (Qwen-8B) |
|------------------|--------------------|
| All tokens | 60.51 |
| **Top 30% high-entropy tokens** | **63.96** ✅ |
| Top 20% / Top 40% | ↓ 性能下降（见 Table 6） |

> ✅ 最佳范围为 **top 30%**，太少会遗漏关键信息，太多则引入噪声。

---

## 4. 关键结论和发现

### 🎯 主要发现

1. **MLLMs 的视觉注意力不稳定是推理失败的主因**  
   分析表明，**注意力错位（misalignment）与错误预测高度相关**，且早期错误几乎不会被后续推理纠正。

2. **现有 RL 方法未能解决视觉注意力学习问题**  
   即使经过强化学习优化，主流模型仍表现出低 Target Attention Score（TAS），说明传统 RL 目标无法有效引导视觉聚焦。

3. **显式的注意力奖励能解锁模型潜在推理能力**  
   当输入信号（即“看到的内容”）正确时，预训练好的语言模型即可完成高质量推理。SAYO 的本质是“**修复输入端的感知瓶颈**”。

4. **高熵 token 是注意力干预的最佳时机**  
   此时模型最不确定，强制其参考视觉证据可实现“**Look-to-Verify**”机制，抑制语言幻觉。

5. **方法具有强泛化性**  
   尽管训练数据不含数学题，SAYO 在 We-Math 和 MathVision 上大幅提升 → 证明其学到的是**通用的视觉解析能力**，而非记忆模式。

---

### ⚠️ 方法的局限性

- **依赖标注数据**：需要 bounding box 级别的视觉标注来定义“目标区域”，限制了在无标注数据上的应用。
- **计算成本较高**：RL 训练比 SFT 更耗资源，不适合轻量级部署。
- **对极低熵 token 关注不足**：某些关键判断可能发生在低不确定性阶段，当前策略可能忽略。

---

### 🔮 未来工作方向

1. **弱监督/自监督注意力奖励设计**  
   探索无需人工标注 bounding box 的方式构建注意力奖励信号（如利用 CLIP 或 SAM 自动生成候选区域）。

2. **动态调整熵选择阈值**  
   根据任务类型自动调节参与奖励的 token 比例，进一步提升训练效率。

3. **扩展至视频或多帧推理场景**  
   将 region-level attention reward 延伸到时空维度，增强对动态内容的理解能力。

4. **与其他认知机制结合**  
   如整合 memory、planning 等模块，形成更完整的“视觉思考”系统。

---

## ✅ 总结一句话

> **SAYO 通过引入基于高熵 token 的 region-level 视觉注意力奖励，在无需推理时干预的前提下，显著提升了 MLLMs 的视觉聚焦能力和跨任务推理性能，揭示了“精准感知”才是释放多模态模型推理潜力的关键瓶颈。**

</details>

---

### 14. [SCOUT-RAG: Scalable and Cost-Efficient Unifying Traversal for Agentic Graph-RAG over Distributed Domains](https://arxiv.org/abs/2602.08400)

**Authors**: Longkun Li, Yuanben Zou, Jinghan Wu, Yuqing Wen, Jing Li, Hangwei Qian, Ivor Tsang  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08400v1  

#### Abstract
Graph-RAG improves LLM reasoning using structured knowledge, yet conventional designs rely on a centralized knowledge graph. In distributed and access-restricted settings (e.g., hospitals or multinational organizations), retrieval must select relevant domains and appropriate traversal depth without ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《SCOUT-RAG: Scalable and Cost-Efficient Unifying Traversal for Agentic Graph-RAG over Distributed Domains》总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Graph-RAG** 系统依赖于集中式的全局知识图谱（Knowledge Graph），但在现实场景中，数据通常分布在多个独立组织之间（如医院、跨国企业），由于隐私、所有权或法规限制，无法共享原始数据。这种**分布式、受限访问**的环境导致：
- 无法构建统一的知识图；
- 跨域检索成本高（延迟、API费用）；
- 难以判断应向哪些领域发起查询。

现有方法（如 supervised domain router）依赖标注数据训练路由模型，在冷启动或隐私敏感场景下不可行。

### 🚀 提出的新方法：SCOUT-RAG
提出 **SCOUT-RAG** —— 一种**分层的 agentic 架构**，用于在分布式知识域上实现可扩展且成本高效的 **Agentic Graph-RAG** 检索。

#### 核心创新点：
1. **无需训练的 domain relevance 估计**  
   利用三种信号进行无监督相关性打分：
   - **Semantic similarity**（语义相似度）
   - **Knowledge richness**（知识丰富度，基于报告数量归一化）
   - **Historical performance**（历史问答质量）

2. **去中心化的多跳检索决策机制**  
   将每个 domain 视为一个部分可观测子图，动态决定是否进行本地探索（local traversal）还是跨域扩展（cross-domain expansion）。

3. **将检索建模为序列决策过程（Sequential Decision Process）**  
   引入四个协作 agent 实现闭环控制：
   - **Domain Relevance Assessment Agent (DRAA)**：初步筛选相关 domain
   - **Partial Answer Generation Agent (PAGA)**：执行局部检索
   - **Overall Answer Synthesis Agent (OASA)**：合成初始答案
   - **Answer Quality Assessment Agent (AQAA)**：评估当前答案并指导后续动作

4. **自适应深度-广度探索策略（Adaptive Depth-Breadth Exploration）**  
   根据答案质量反馈动态选择：
   - **Depth 扩展**：深入 HIGH-relevance domain 补充细节
   - **Breadth 扩展**：激活 POTENTIAL domain 获取新视角
   - **Hybrid 或终止**

5. **多级安全机制保障可靠性**
   - 时间预算强制中断（time-budget enforcement）
   - 最佳答案追踪（best-answer tracking）
   - 并行协调机制防止级联失败

### 🔍 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **部署灵活性** | 支持冷启动、无需标注数据，适用于隐私受限场景 |
| **成本效率** | 显著减少跨域调用次数、token 消耗和延迟 |
| **可扩展性** | 可处理多达 40 个 domain 的复杂查询 |
| **答案质量** | 接近 centralized DRIFT 的性能，远超 naive local/global 方法 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 使用来自 **45 个国家的 Wikipedia 文章**作为模拟 domain 数据源。
- 每个 domain 构建了一个图结构知识库，包含 9–77 个 community reports。
- 设计了 **100 个自然语言查询**，其中 89 个被所有方法有效回答。
- 查询按覆盖 domain 数量分为五类：
  - 单 domain（1 国）
  - 小规模 multi-domain（5 国）
  - 中等（10 国）
  - 大规模（20 国）
  - 超大规模（40 国）

### ⚙️ 实验设置
- **硬件环境**：
  - GPU: NVIDIA RTX 3070 (9GB VRAM)
  - CPU: 16 核 AMD EPYC 7B12
  - 内存: 31 GB
- **时间预算上限**：300 秒
- 所有方法均运行相同环境以保证公平比较

### 📊 评估指标（由 GPT-4o 作为 judge 打分，0–100 分制）
| 指标 | 含义 |
|------|------|
| **Comprehensiveness (Comp.)** | 是否涵盖核心概念与方面 |
| **Diversity (Div.)** | 来源多样性与观点广度 |
| **Empowerment (Emp.)** | 是否提供可操作洞察与进一步探索支持 |
| **Directness (Dir.)** | 回答是否简洁清晰 |
| **Overall Quality** | 上述四项平均得分 |
| **Time (s)** | 总执行时间（秒） |
| **Token Count** | 总处理 token 数量 |

### 🆚 基线方法对比
分为两类部署范式：

#### ✅ Centralized Setting（所有数据合并为单一 domain）
1. **GraphRAGLocal**：仅局部检索（entity-level）
2. **GraphRAGGlobal**：仅全局检索（community summary）
3. **GraphRAGDRIFT-c**：先 global 后两次 refine（每轮 3 次 follow-up）

#### ✅ Decentralized Setting（数据分散在 45 个 domain）
1. **GraphRAGDRIFT-dec**：每个 domain 独立运行 DRIFT，最后融合结果（近乎穷举）
2. **SCOUT-RAG (Ours)**：提出的 agent-based 动态调度框架

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1）

| Method | Comp. | Div. | Emp. | Dir. | **Overall** | **Time (s)** | **Token** |
|--------|-------|------|------|------|-------------|--------------|-----------|
| GraphRAGLocal | 65 | 55 | 35 | 58 | 53 | 34.40 | 11,223 |
| GraphRAGGlobal | 60 | 50 | 30 | 55 | 49 | 45.89 | 640,574 |
| GraphRAGDRIFT-c | 72 | 70 | 45 | 63 | **63** | 231.85 | 693,731 |
| GraphRAGDRIFT-dec | 90 | 88 | 75 | 88 | **85** | 414.88 | 879,911 |
| **SCOUT-RAG (Ours)** | 65 | 60 | 40 | 58 | **56** | **75.32** | **159,169** |

### 🔍 对比分析
- **质量方面**：
  - SCOUT-RAG 达到 **56 分**，虽低于 DRIFT-dec（85），但显著优于 GraphRAGGlobal（49）和 GraphRAGLocal（53）。
  - 与 centralized DRIFT-c（63）相比仅低 7 分，差距较小，说明其在分布式环境下仍能保持较高推理能力。
- **成本效率方面**：
  - 相比 **GraphRAGDRIFT-dec**：
    - 减少 **81.8% 时间**（75.32s vs 414.88s）
    - 减少 **81.9% token**（159k vs 879k）
  - 相比 **GraphRAGDRIFT-c**：
    - 时间减少 **67.5%**
    - Token 减少 **77.1%**
- **多样性表现突出**：
  - SCOUT-RAG 在 **Diversity (60)** 上超过所有 centralized 方法（最高为 DRIFT-c 的 70），接近 decentralized DRIFT-dec（88），得益于层级化 domain 激活机制。

### 📉 成本-性能权衡图（Figure 3）
- SCOUT-RAG 在前 120 秒内快速提升质量，约 180 秒趋于稳定，边际收益递减。
- 在 300 秒预算内达到收敛，适合实际部署。
- DRIFT-c 性能接近但耗时更长；DRIFT-dec 最终性能最强但资源消耗极高。

### 🔍 消融实验与案例研究（Figure 4）
- **案例：“Made in Italy” 认证对时尚产业 SME 的影响**
  - Stage I：准确识别 Italy、Slovenia、Malta 等高相关 domain
  - Stage II：生成初步综合回答，但缺乏具体政策与国际协议细节
  - Stage III：通过两轮 refinement 补充 Slovenia 和 Malta 的认证体系对比，质量从 0.675 提升至 **0.725**
  - 第三轮 breadth 扩展引入噪声，质量下降 → **best-answer tracking 机制成功保留最优版本**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **SCOUT-RAG 实现了高质量与低成本之间的良好平衡**  
   在分布式环境下，以不到 1/5 的资源消耗，达到了接近 centralized DRIFT 的性能水平。

2. **agent-based 架构适合分布式 Graph-RAG 场景**  
   通过将检索视为序列决策问题，系统能够根据中间反馈动态调整策略，避免盲目穷举。

3. **无需训练的相关性评估是可行且有效的**  
   结合 semantic、evidential、behavioral 三类信号可在无标注数据情况下实现可靠 domain ranking。

4. **adaptive depth-breadth 控制显著提升效率**  
   动态切换“深挖”与“拓宽”模式，既能补全关键信息，又能避免无效探索。

5. **best-answer tracking 提高鲁棒性**  
   防止后期因噪声检索导致答案退化，确保输出最优质结果。

### ⚠️ 局限性
- 当 query 所需信息高度分散于多个 low-relevance domain 时，可能遗漏重要证据（retrieval regret）。
- 当前 historical performance 依赖历史查询积累，在全新 domain 上效果有限。
- agent 间通信开销未完全建模，极端并发下可能存在同步瓶颈。

### 🔮 未来工作方向
- 引入轻量级 reinforcement learning 优化策略选择模块；
- 探索跨 domain 的联合 embedding space 以增强语义匹配；
- 支持 streaming updates 与增量学习，使系统持续进化；
- 扩展至非文本型 knowledge graph（如生物医学、金融图谱）。

---

## 总结
> **SCOUT-RAG 是首个将 agentic AI 与分布式 Graph-RAG 深度结合的框架**，它在不牺牲太多性能的前提下，实现了高达 **80%+ 的成本节约**，为隐私敏感、资源受限、多组织协同的知识密集型任务提供了实用解决方案。其设计思想对未来的联邦式 RAG、边缘智能问答系统具有重要启发意义。

</details>

---

### 15. [SparseEval: Efficient Evaluation of Large Language Models by Sparse Optimization](https://arxiv.org/abs/2602.07909)

**Authors**: Taolin Zhang, Hang Guo, Wang Lu, Tao Dai, Shu-Tao Xia, Jindong Wang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.07909v1  

#### Abstract
As large language models (LLMs) continue to scale up, their performance on various downstream tasks has significantly improved. However, evaluating their capabilities has become increasingly expensive, as performing inference on a large number of benchmark samples incurs high computational costs. In...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# SparseEval: Efficient Evaluation of Large Language Models by Sparse Optimization —— 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
随着 **Large Language Models (LLMs)** 规模不断增大，其在各类下游任务上的性能显著提升，但随之而来的**评估成本**也急剧上升。对大规模 benchmark 数据集进行推理评估需要高昂的计算资源，尤其是在模型数量和测试样本量都很大的情况下。

本文旨在解决如何在**大幅降低评估开销**的同时，保持对模型能力的准确估计这一核心挑战。

---

### 🚀 提出的新方法与创新思路

作者提出了 **SparseEval**，一种基于**稀疏优化（sparse optimization）** 的高效 LLM 评估框架，其核心思想如下：

- **重新审视 model-item 性能矩阵**，发现其具有明显的**稀疏性和聚类结构**（sparsity and redundancy），即许多测试样本在模型响应模式上高度相似。
- 将高效评估建模为一个**稀疏优化问题**：从大量测试项中选择少量代表性“锚点”（anchors），并通过学习权重来重构全集上的总体性能。
- 首次将**梯度下降（gradient descent）** 引入 anchor 权重的学习过程，端到端地优化重建损失（reconstruction loss）。
- 设计了一种**任务感知的 anchor refinement 策略**，通过迭代替换低价值 anchor 和高潜力候选，动态优化 anchor 集合。

#### 创新点总结：
| 创新维度 | 内容 |
|--------|------|
| **理论视角** | 形式化定义“评估稀疏性”，并将其转化为稀疏优化问题 |
| **技术实现** | 使用 MLP + 梯度下降直接优化 anchor 权重，优于传统加权平均或 IRT 方法 |
| **anchor 选择机制** | 提出 **Anchor Importance Score (AIS)** 和 **Candidate Importance Score (CIS)** 进行任务感知 refinement |
| **无需额外特征** | 不依赖 prompt embedding 或概率分布等外部信息，仅使用 model-item 正确性矩阵 |

---

### 🔍 相比现有方法的优势

| 方法 | 局限性 | SparseEval 的优势 |
|------|-------|------------------|
| **Anchor Points**, **TinyBenchmark (IRT)** | 依赖静态聚类或 IRT 模型，无法端到端优化；权重固定 | 动态学习权重，支持负权重，扩展优化空间 |
| **TailoredBench** | 自适应但未结合梯度优化 | 引入 refinement 机制，更贴近下游任务目标 |
| **Flash-HELM / Pacchiardi et al.** | 多基于启发式或统计校准 | 建立可训练、可微分的评估代理模型（proxy model） |

> ✅ **优势总结**：更低误差、更强相关性、更高泛化性、更少 anchor 数量需求。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验基于 **Open-LLM Leaderboard** 上超过 **5,000 个 LLMs** 在以下六个主流 benchmark 上的表现构建 model-item 矩阵：

| 数据集 | 任务类型 |
|-------|---------|
| **ARC** | 推理与常识问答 |
| **GSM8K** | 数学应用题求解 |
| **HellaSwag** | 日常情境下的句子补全 |
| **MMLU** | 多学科知识理解（涵盖57个子领域） |
| **TruthfulQA** | 抗误导与事实一致性判断 |
| **Winogrande** | 共指消解与语义推理 |

> ⚠️ 注：相比 TinyBenchmark 中仅使用约 300 个模型，本工作扩大至 **5,000 模型**，极大增强了泛化性验证。

---

### 🧪 实验设置与评估指标

#### 实验流程概览：
1. 从全部模型中随机选取 200 个作为 **validation & test set**（各100）
2. 剩余 ~4800 个用于训练
3. 固定 anchor 数量 $ k \in \{20, 40, 60, 80, 100\} $
4. 使用 **4-layer MLP** 作为聚合函数 $ f $
5. 学习率：6e-4，refinement 步数：10

#### 评估指标：
| 指标 | 含义 |
|-----|------|
| **MAE ↓** | Mean Absolute Error，预测准确率与真实准确率之间的绝对误差（越小越好） |
| **Kendall’s Tau (T) ↑** | 排序一致性系数，衡量模型排名的相关性（越高越好，接近1表示强一致） |

#### 对比的基线方法：
- **Anchor Points** (Vivek et al., 2023)：基于代表性子集的聚类方法
- **gp-IRT** (Polo et al., 2024)：基于 Item Response Theory 的贝叶斯建模
- **TailoredBench** (Yuan et al., 2025)：定制化自适应 benchmark

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 1）

| 方法 / 数据集 | Anchor=100 时 MAE (%) | Anchor=100 时 Kendall’s T |
|--------------|------------------------|----------------------------|
| **SparseEval (平均)** | **~1.3%** | **>0.91** |
| **TailoredBench** | ~2.0% | ~0.89 |
| **gp-IRT** | ~2.3% | ~0.83 |
| **Anchor Points** | >3.0% | <0.80 |

> ✅ **结论**：SparseEval 在所有 benchmark 上均显著优于基线，在仅用 **100 个 anchor** 的情况下即可实现：
- **MAE 低于 1.5%**
- **Kendall’s T 超过 0.90**

---

### 🔁 与基线方法的对比结果

| 对比维度 | 结果 |
|--------|------|
| **估计精度** | MAE 平均比 best baseline 低 **1–2%**，尤其在 MMLU 和 TruthfulQA 上优势明显 |
| **排序一致性** | Kendall’s T 提升达 **+0.1**，表明模型排名高度可信 |
| **anchor 效率** | 仅需 **100 个 anchor** 即可达到其他方法使用 **500+ anchor** 才能达到的效果 → **>5x 成本节省**
| **训练效率** | 相比 gp-IRT（耗时长达 16 分钟），SparseEval 训练更快，更适合实际部署 |

> 💡 图 3 显示：SparseEval 使用 100 个 anchor 的表现 ≈ TailoredBench 使用 500 个 anchor 的表现。

---

### 🔍 消融实验结果（Ablation Study）

#### （1）网络架构影响（Fig. 4）
- 使用 **MLP 替代线性加权** 可显著降低 MAE，说明更强的表示能力有助于捕捉复杂的 anchor 权重关系。
- 线性模型受限于表达能力，难以拟合非线性偏差。

#### （2）训练数据比例影响（Fig. 5）
- 即使只使用 **20% 的训练数据**，SparseEval 仍能维持 MAE < 1%，T > 0.90
- 表明方法对数据规模不敏感，具备良好鲁棒性

#### （3）anchor 初始化策略（Table 2）
| 初始化方式 | MAE (ARC, k=100) | T (ARC, k=100) |
|-----------|------------------|---------------|
| Random | 1.339% | 0.890 |
| k-means | 1.218% | 0.913 |
| **SparseEval (w/ refinement)** | **1.165%** | **0.917** |

> ✅ **refinement 是关键增益来源**，特别是在 anchor 数量较少时效果更显著。

#### （4）AIS/CIS 分布分析（Fig. 6）
- 经过 refinement 后，AIS 和 CIS 分布整体右移，说明选出的 anchors 更具影响力，candidates 更具补充潜力。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **LLM benchmark 存在显著的“评估稀疏性”（Evaluation Sparsity）**
   - model-item 矩阵呈现强 intra-cluster 相似性和 inter-cluster 可预测性
   - 支持使用极小子集（如 100 项）代表整个 benchmark

2. **anchor 不应静态选择，而应任务感知地动态优化**
   - 传统的 k-means 或 IRT 聚类虽有效，但缺乏任务对齐
   - 引入 AIS/CIS 可量化每个 item 的“代表性”和“可替代性”

3. **梯度驱动的稀疏优化优于传统方法**
   - 端到端训练允许模型自动学习最优权重组合，甚至引入负权重以修正系统性偏差

4. **SparseEval 泛化能力强**
   - 在 hold-out 的 DeepSeek 家族模型上测试（Table 3），平均 MAE 仅为 **2.09%**，远低于 gp-IRT（2.66%）和 TailoredBench（4.22%）

---

### ⚠️ 方法的局限性

1. **依赖足够多的模型样本进行训练**
   - 梯度优化需要一定数量的模型（文中使用 ~4800）才能稳定收敛
   - 在仅有少数几个模型时可能失效

2. **当前仅适用于 binary 正确性评分场景**
   - 输入为 {-1, 1} 构成的 model-item 矩阵，尚未拓展至 soft label 或生成质量评分

3. **未利用模型内部表征**
   - 当前方法完全基于 black-box 输出行为，未来可融合 hidden states 或 attention map 提升精度

---

### 🔮 未来工作方向

1. **结合 model-model similarity structure**（Fig. 10）
   - 利用模型间的性能相似性进一步减少所需 anchor 数量

2. **扩展至多模态模型评估**
   - 将稀疏优化思想应用于 VLM、语音模型等领域的 benchmark 设计

3. **在线增量更新机制**
   - 支持新模型加入后快速 re-calibrate anchor 权重，无需重新训练

4. **探索 AIS/CIS 的可解释性**
   - 分析为何某些题目天然更具代表性（如 Fig. 11 中法律/物理典型题）

---

## ✅ 总结

**SparseEval** 是首个将 **稀疏优化 + 梯度下降 + 任务感知 refinement** 融为一体的大模型高效评估框架。它不仅实现了**超低评估成本**（仅需 100 个样本），还保证了**高精度估计**与**强排序一致性**，是迈向绿色 AI 评估的重要一步。

> 🔗 开源地址：[https://github.com/taolinzhang/SparseEval](https://github.com/taolinzhang/SparseEval)

</details>

---

### 16. [VocalNet-MDM: Accelerating Streaming Speech LLM via Self-Distilled Masked Diffusion Modeling](https://arxiv.org/abs/2602.08607)

**Authors**: Ziyang Cheng, Yuhao Wang, Heyang Liu, Ronghua Wu, Qunshan Gu, Yanfeng Wang, Yu Wang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08607v1  

#### Abstract
Recent Speech Large Language Models~(LLMs) have achieved impressive capabilities in end-to-end speech interaction. However, the prevailing autoregressive paradigm imposes strict serial constraints, limiting generation efficiency and introducing exposure bias. In this paper, we investigate Masked Dif...

---

### 17. [Horizon Imagination: Efficient On-Policy Training in Diffusion World Models](https://arxiv.org/abs/2602.08032)

**Authors**: Lior Cohen, Ofir Nabati, Kaixin Wang, Navdeep Kumar, Shie Mannor  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08032v1  

#### Abstract
We study diffusion-based world models for reinforcement learning, which offer high generative fidelity but face critical efficiency challenges in control. Current methods either require heavyweight models at inference or rely on highly sequential imagination, both of which impose prohibitive computa...

---

### 18. [Compiler-Assisted Speculative Sampling for Accelerated LLM Inference on Heterogeneous Edge Devices](https://arxiv.org/abs/2602.08060)

**Authors**: Alejandro Ruiz y Mesa, Guilherme Korol, Moritz Riesteter, Jo\~ao Paulo Cardoso de Lima, Jeronimo Castrillon  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08060v1  

#### Abstract
LLM deployment on resource-constrained edge devices faces severe latency constraints, particularly in real-time applications where delayed responses can compromise safety or usability. Among many approaches to mitigate the inefficiencies of sequential token-by-token generation, Speculative Decoding ...

---

### 19. [Towards Efficient Large Language Reasoning Models via Extreme-Ratio Chain-of-Thought Compression](https://arxiv.org/abs/2602.08324)

**Authors**: Yuntian Tang, Bohan Jia, Wenxuan Huang, Lianyue Zhang, Jiao Xie, Wenxi Li, Wei Li, Jie Hu, Xinghao Chen, Rongrong Ji, Shaohui Lin  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08324v1  

#### Abstract
Chain-of-Thought (CoT) reasoning successfully enhances the reasoning capabilities of Large Language Models (LLMs), yet it incurs substantial computational overhead for inference. Existing CoT compression methods often suffer from a critical loss of logical fidelity at high compression ratios, result...

---

### 20. [Bridging Academia and Industry: A Comprehensive Benchmark for Attributed Graph Clustering](https://arxiv.org/abs/2602.08519)

**Authors**: Yunhui Liu, Pengyu Qiu, Yu Xing, Yongchao Liu, Peng Du, Chuntao Hong, Jiajun Zheng, Tao Zheng, Tieke He  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08519v1  

#### Abstract
Attributed Graph Clustering (AGC) is a fundamental unsupervised task that integrates structural topology and node attributes to uncover latent patterns in graph-structured data. Despite its significance in industrial applications such as fraud detection and user segmentation, a significant chasm per...

---

### 21. [ERIS: Enhancing Privacy and Communication Efficiency in Serverless Federated Learning](https://arxiv.org/abs/2602.08617)

**Authors**: Dario Fenoglio, Pasquale Polverino, Jacopo Quizi, Martin Gjoreski, Marc Langheinrich  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.08617v1  

#### Abstract
Scaling federated learning (FL) to billion-parameter models introduces critical trade-offs between communication efficiency, model accuracy, and privacy guarantees. Existing solutions often tackle these challenges in isolation, sacrificing accuracy or relying on costly cryptographic tools. We propos...

---

### 22. [PTS-SNN: A Prompt-Tuned Temporal Shift Spiking Neural Networks for Efficient Speech Emotion Recognition](https://arxiv.org/abs/2602.08240)

**Authors**: Xun Su, Huamin Wang, Qi Zhang  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.08240v1  

#### Abstract
Speech Emotion Recognition (SER) is widely deployed in Human-Computer Interaction, yet the high computational cost of conventional models hinders their implementation on resource-constrained edge devices. Spiking Neural Networks (SNNs) offer an energy-efficient alternative due to their event-driven ...

---

### 23. [Efficient Post-Training Pruning of Large Language Models with Statistical Correction](https://arxiv.org/abs/2602.07375)

**Authors**: Peiqi Yu, Jinhao Wang, Xinyi Sui, Nam Ling, Wei Wang, Wei Jiang  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.07375v1  

#### Abstract
Post-training pruning is an effective approach for reducing the size and inference cost of large language models (LLMs), but existing methods often face a trade-off between pruning quality and computational efficiency. Heuristic pruning methods are efficient but sensitive to activation outliers, whi...

---

### 24. [MARTI-MARS$^2$: Scaling Multi-Agent Self-Search via Reinforcement Learning for Code Generation](https://arxiv.org/abs/2602.07848)

**Authors**: Shijie Wang, Pengfei Li, Yikun Fu, Kaifeng Liu, Fangyuan Li, Yang Liu, Xiaowei Sun, Zonglin Li, Siyao Zhao, Jian Zhao, Kai Tian, Dong Li, Junqi Gao, Yutong Zhang, Yiqun Chen, Yuqiang Li, Zoe Li, Weinan Zhang, Peng Ye, Shuyue Hu, Lei Bai, Bowen Zhou, Kaiyan Zhang, Biqing Qi  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.07848v1  

#### Abstract
While the complex reasoning capability of Large Language Models (LLMs) has attracted significant attention, single-agent systems often encounter inherent performance ceilings in complex tasks such as code generation. Multi-agent collaboration offers a promising avenue to transcend these boundaries. ...

---

### 25. [Time Series Reasoning via Process-Verifiable Thinking Data Synthesis and Scheduling for Tailored LLM Reasoning](https://arxiv.org/abs/2602.07830)

**Authors**: Jiahui Zhou, Dan Li, Boxin Li, Xiao Zhang, Erli Meng, Lin Li, Zhuomin Chen, Jian Lou, See-Kiong Ng  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.07830v1  

#### Abstract
Time series is a pervasive data type across various application domains, rendering the reasonable solving of diverse time series tasks a long-standing goal. Recent advances in large language models (LLMs), especially their reasoning abilities unlocked through reinforcement learning (RL), have opened...

---

### 26. [iGRPO: Self-Feedback-Driven LLM Reasoning](https://arxiv.org/abs/2602.09000)

**Authors**: Ali Hatamizadeh, Shrimai Prabhumoye, Igor Gitman, Ximing Lu, Seungju Han, Wei Ping, Yejin Choi, Jan Kautz  
**Category**: cs.AI  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.09000v1  

#### Abstract
Large Language Models (LLMs) have shown promise in solving complex mathematical problems, yet they still fall short of producing accurate and consistent solutions. Reinforcement Learning (RL) is a framework for aligning these models with task-specific rewards, improving overall quality and reliabili...

---

### 27. [Attn-GS: Attention-Guided Context Compression for Efficient Personalized LLMs](https://arxiv.org/abs/2602.07778)

**Authors**: Shenglai Zeng, Tianqi Zheng, Chuan Tian, Dante Everaert, Yau-Shian Wang, Yupin Huang, Michael J. Morais, Rohit Patki, Jinjin Tian, Xinnan Dai, Kai Guo, Monica Xiao Cheng, Hui Liu  
**Category**: cs.CL  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.07778v1  

#### Abstract
Personalizing large language models (LLMs) to individual users requires incorporating extensive interaction histories and profiles, but input token constraints make this impractical due to high inference latency and API costs. Existing approaches rely on heuristic methods such as selecting recent in...

---

### 28. [Cerebellar-Inspired Residual Control for Fault Recovery: From Inference-Time Adaptation to Structural Consolidation](https://arxiv.org/abs/2602.07227)

**Authors**: Nethmi Jayasinghe, Diana Gontero, Spencer T. Brown, Vinod K. Sangwan, Mark C. Hersam, Amit Ranjan Trivedi  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.07227v1  

#### Abstract
Robotic policies deployed in real-world environments often encounter post-training faults, where retraining, exploration, or system identification are impractical. We introduce an inference-time, cerebellar-inspired residual control framework that augments a frozen reinforcement learning policy with...

---

### 29. [Pareto-guided Pipeline for Distilling Featherweight AI Agents in Mobile MOBA Games](https://arxiv.org/abs/2602.07521)

**Authors**: Xionghui Yang, Bozhou Chen, Yunlong Lu, Yongyi Wang, Lingfeng Li, Lanxiao Huang, Lin Liu, Wenjun Wang, Meng Meng, Xia Lin, Wenxin Li  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.07521v1  

#### Abstract
Recent advances in game AI have demonstrated the feasibility of training agents that surpass top-tier human professionals in complex environments such as Honor of Kings (HoK), a leading mobile multiplayer online battle arena (MOBA) game. However, deploying such powerful agents on mobile devices rema...

---

### 30. [Enhancing Bandit Algorithms with LLMs for Time-varying User Preferences in Streaming Recommendations](https://arxiv.org/abs/2602.08067)

**Authors**: Chenglei Shen, Yi Zhan, Weijie Yu, Xiao Zhang, Jun Xu  
**Category**: cs.LG  
**Published**: 2026-02-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.08067v1  

#### Abstract
In real-world streaming recommender systems, user preferences evolve dynamically over time. Existing bandit-based methods treat time merely as a timestamp, neglecting its explicit relationship with user preferences and leading to suboptimal performance. Moreover, online learning methods often suffer...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
