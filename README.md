# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-27 05:59:48 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Sparsity-Aware Low-Rank Representation for Efficient Fine-Tuning of Large Language Models](https://arxiv.org/abs/2601.16991)

**Authors**: Longteng Zhang, Sen Wu, Shuai Hou, Zhengyu Qing, Zhuo Zheng, Danning Ke, Qihong Lin, Qiang Wang, Shaohuai Shi, Xiaowen Chu  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.16991v1  

#### Abstract
Adapting large pre-trained language models to downstream tasks often entails fine-tuning millions of parameters or deploying costly dense weight updates, which hinders their use in resource-constrained environments. Low-rank Adaptation (LoRA) reduces trainable parameters by factorizing weight update...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**SALR: Sparsity-Aware Low-Rank Representation for Efficient Fine-Tuning of Large Language Models**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

在大型语言模型（LLM）的高效微调中，主流方法如 **LoRA** 虽然减少了可训练参数，但仍依赖于**密集权重更新**，导致存储和计算开销高。而直接对 LoRA 模型进行剪枝（pruning）虽然能引入稀疏性以压缩模型，但通常会严重损害性能，尤其是当剪枝策略破坏了低秩子空间时。

此外，许多声称“实现稀疏”的方法并未真正实现**模型大小压缩**或**推理加速**，因为缺乏高效的稀疏格式支持和硬件优化。

### 🚀 提出的新方法：SALR

本文提出 **SALR (Sparsity-Aware Low-Rank Representation)**，一种将**稀疏剪枝**与**低秩适应**统一的新型微调范式，其核心思想包括：

1. **静态剪枝冻结的基权重 $W_0$**  
   理论证明：仅对预训练的冻结权重 $W_0$ 应用静态掩码（static mask）可最小化剪枝误差上界（MSE bound），优于动态剪枝策略。

2. **残差信息保留机制（Sparsity Preservation Pruning）**  
   将被剪枝的权重残差 $E = W_0 - \tilde{W}_0$ 通过截断 SVD 分解为一个低秩适配器（low-rank adapter），从而恢复丢失的信息。该操作理论上可将每项 MSE 减少 $(1 - r/\min(d,k))$ 倍。

3. **多适配器融合（Adapter Concatenation）**  
   将原始 LoRA 适配器与多个残差适配器沿秩维度拼接，合并为单个 GEMM 操作，显著降低内核启动开销并提升硬件利用率。

4. **真正的模型压缩与推理加速设计**  
   - 使用 **bitmap 编码** 存储稀疏权重，实现紧凑表示；
   - 设计 **两阶段流水线（decoding + GEMM）**：第一阶段解码稀疏子矩阵，第二阶段由 Tensor Core 执行密集乘法，保持计算密度。

### 🔍 相比现有方法的优势

| 方法 | 性能 | 模型稀疏性 | 推理加速 |
|------|------|------------|----------|
| LoSA (Huang et al., 2025) | 低 | 稀疏 | 是 |
| SparseLoRA (Khaki et al., 2025) | 高 | 密集 | 否 |
| **SALR (Ours)** | **高** | **稀疏** | **是** |

SALR 成功实现了三者兼顾：**高性能、真实稀疏、实际加速**，突破了以往方法只能取其二的瓶颈。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **MetaMath**：用于数学领域监督微调（SFT），适配 MATH 类任务。
- **ARC, MC-TEST, OBQA, RACE 等多选题数据集**：用于跨学科知识评估（MMLU）。
- 微调后在以下基准测试零样本性能：
  - **GSM8K**：小学数学应用题，评估推理能力。
  - **MMLU**：涵盖57个学科的多任务理解能力。

### ⚙️ 实验设置与评估指标

| 项目 | 设置 |
|------|------|
| 模型 | Llama2-7B, Llama3-8B, Mixtral-8x7B, DeepSeek-V2-Lite |
| 剪枝率 | 全局 50% 稀疏度（除非特别说明） |
| LoRA 秩 | $r=64$ |
| 评估方式 | Zero-shot accuracy（GSM8K）、5-shot accuracy（MMLU） |
| 硬件平台 | 1x RTX 4090（推理速度测试） |

### 🆚 基线方法对比

- **Pretrained**：未微调的原始模型
- **LoRA**：标准低秩微调，作为性能上限
- **LoSA**：动态低秩稀疏适配，支持稀疏但性能下降明显
- **SparseLoRA**：上下文感知稀疏，训练快但部署仍为密集模型
- **DeepSparse**：深度稀疏微调方法，性能损失大

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 2）

| 方法 | Llama2-7B (MMLU/GSM8K) | Llama3-8B (MMLU/GSM8K) | Mixtral-8x7B (MMLU/GSM8K) |
|------|------------------------|-------------------------|----------------------------|
| LoRA | 56.0 / 56.8 | 69.2 / 79.5 | 71.0 / 79.2 |
| LoSA | 45.0 / 34.2 | 64.4 / 71.4 | 69.2 / 57.9 |
| SparseLoRA | 56.0 / 37.6 | 69.0 / 72.0 | 70.9 / 78.0 |
| **SALR (Ours)** | **56.0 / 56.7** | **68.2 / 79.5** | **71.4 / 79.1** |

✅ **结论**：SALR 在 **50% 稀疏度下几乎完全匹配 LoRA 的性能**，远超其他剪枝方法。

### ⏱️ 推理速度与系统效率（Table 4）

| 方法（Sparsity） | GSM8K 准确率 | 推理吞吐（tokens/s） | 加速比 |
|------------------|---------------|------------------------|--------|
| LoRA (N/A) | 79.5 | 60.1 | 1.0x |
| LoSA (2:4) | 69.4 | 113.5 | 1.9x |
| **SALR (2:4)** | **78.9** | **104.9** | **1.7x** |

📌 SALR 实现了高达 **1.7× 推理加速**，同时准确率仅轻微下降（79.5 → 78.9），显著优于 LoSA（下降至 69.4）。

### 💾 模型压缩效果（Figure 1 & Table 3）

- 对 Llama3-8B：
  - 原始 LoRA 模型大小：15.5 GB
  - SALR 压缩后：**7.98 GB**（接近 **2× 压缩**）
- Fine-tuning 内存从 LoSA 的 27.1 GB 降至 **19.2 GB**，TFLOPS 提升至 **89.2**（vs. LoSA 的 74.5）

### 🔬 消融实验结果

#### （1）是否更新残差适配器（Table 5）

| 方法 | Llama2-7B (MMLU) | Llama3-8B (MMLU) |
|------|------------------|------------------|
| LoRA | 56.0 | 69.2 |
| SALR（残差冻结） | 54.2 | 66.8 |
| **SALR（残差可训练）** | **56.0** | **68.2** |

➡️ **结论**：允许残差适配器参与微调至关重要，否则性能显著下降。

#### （2）不同稀疏度的影响（Table 7）

| 稀疏度 | GSM8K 准确率 |
|--------|--------------|
| 10% | 79.5 |
| 30% | **80.1** ✅ |
| 50% | 79.5 |

➡️ **发现**：适度稀疏（如 30%）甚至可能起到正则化作用，略微提升性能。

#### （3）结合量化（Table 6）——Quantized SALR (QSALR)

| 方法 | DeepSeek-V2-Lite (Size ↓) | Mixtral-8x7B (Size ↓) | 性能影响 |
|------|----------------------------|------------------------|----------|
| LoRA | 31.8 GB | 93.9 GB | — |
| **QSALR (20% sparsity + NF4)** | **6.5 GB** (~5×) | **19.2 GB** (~5×) | -0.6 或无损 |

➡️ 可进一步与 **NF4 量化** 结合，在极小内存下部署超大规模模型。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **理论指导实践**：静态剪枝冻结权重 $W_0$ 是最优选择，能最小化 MSE 上界。
2. **残差低秩补偿有效**：通过 SVD 提取并微调剪枝残差，可显著恢复性能损失。
3. **硬件意识设计必要**：adapter concatenation 和 bitmap 流水线是实现**真实压缩与加速**的关键。
4. **SALR 实现帕累托前沿突破**：首次在“性能-稀疏-速度”三角中达成全面优势。

### ⚠️ 方法的局限性

- 当前 SALR 主要针对 **magnitude-based pruning**，对结构化或非均匀稀疏的支持有待扩展。
- 多适配器融合虽提升效率，但在极端稀疏场景下可能增加管理复杂度。
- 当前 bitmap 解码依赖特定硬件优化（如 Tensor Core），通用性需进一步验证。

### 🔮 未来工作方向

- 将 SALR 扩展到 **MoE 架构** 和 **长序列建模** 场景。
- 探索 **自动化稀疏度分配**（per-layer 或 per-head）以进一步优化性能。
- 结合 **动态稀疏激活** 机制，在输入层面实现条件稀疏执行。
- 推动 SALR 在移动端、边缘设备等资源极度受限环境中的落地。

---

## 总结

> **SALR 是首个将稀疏剪枝与低秩微调从理论到系统完整统一的方法，在不牺牲 LoRA 性能的前提下，实现了高达 50% 稀疏度、2× 模型压缩和 1.7× 推理加速。它不仅提供了新的理论视角，更通过硬件友好的设计推动了稀疏化在 LLM 部署中的实用化进程。**

</details>

---

### 2. [JetFormer: A Scalable and Efficient Transformer for Jet Tagging from Offline Analysis to FPGA Triggers](https://arxiv.org/abs/2601.17215)

**Authors**: Ruoqing Zheng, Chang Sun, Qibin Liu, Lauri Laatu, Arianna Cox, Benedikt Maier, Alexander Tapper, Jose G. F. Coutinho, Wayne Luk, Zhiqiang Que  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.17215v1  

#### Abstract
We present JetFormer, a versatile and scalable encoder-only Transformer architecture for particle jet tagging at the Large Hadron Collider (LHC). Unlike prior approaches that are often tailored to specific deployment regimes, JetFormer is designed to operate effectively across the full spectrum of j...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：JetFormer: A Scalable and Efficient Transformer for Jet Tagging from Offline Analysis to FPGA Triggers

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在高能物理（HEP）领域，**jet tagging** 是识别由特定粒子（如 Higgs、top quark）引发的喷注的关键任务。然而，现有的高性能模型（如 ParT）虽然在离线分析中表现优异，但由于其**计算复杂度高、参数量大**，难以部署到对延迟要求极高的在线触发系统（如 CMS Level-1 Trigger, L1T），尤其是基于 FPGA 的硬件平台。

此外，当前主流工具链（如 hls4ml）对 Transformer 架构支持有限，进一步阻碍了先进模型的实时部署。

### 提出的新方法与思路
本文提出了 **JetFormer** —— 一种**可扩展且高效的 encoder-only Transformer 架构**，专为从离线分析到 FPGA 触发器的全场景 jet tagging 设计。其核心创新包括：

- **统一架构设计**：同一套模型框架既可用于高精度离线分析，也可通过压缩适配超低延迟（sub-microsecond）的 FPGA 触发系统。
- **硬件感知优化流程**：提出了一套完整的硬件部署流水线，结合 **multi-objective Hyperparameter Optimization (HPO)**、**structured pruning** 和 **1-bit quantization**，实现模型小型化与高效推理。
- **Allo 框架扩展**：针对现有 HLS 工具链不支持 Transformer 的问题，作者扩展了 **Allo** 编译框架，使其能够端到端地将 JetFormer 转换为可在 FPGA 上运行的 HLS C++ 代码。
- **硬件友好型结构改进**：
  - 用 **BatchNorm 替代 LayerNorm**，便于推理时参数固化；
  - 用 **ReLU 替代 SiLU**，避免指数运算，提升硬件执行效率。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **性能 vs 效率** | 在 JETCLASS 数据集上，准确率接近 SOTA 的 ParT（仅差 0.7%），但 **FLOPs 减少 37.4%**，显著更高效。 |
| **通用性** | 支持从小规模（JetFormer-tiny）到大规模模型的无缝缩放，适用于不同延迟与精度需求。 |
| **可部署性** | 成功在 Allo 中实现端到端 FPGA 合成，验证了 Transformer 在 L1T 系统中的可行性。 |
| **压缩能力** | 可通过结构化剪枝和 1-bit 量化大幅压缩模型，仅造成轻微精度损失。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 |
|-------|------|
| **HLS4ML 150P Dataset** | 包含 620K 训练样本，每 jet 最多 150 个粒子，每个粒子有 16 个运动学特征；类别平衡（q, g, W, Z, t）。用于小规模 benchmark 测试。 |
| **JETCLASS Dataset** | 大规模数据集，共 100M jets，涵盖 10 类粒子，每粒子 17 个特征（含 PID 和轨迹位移等）。是目前最复杂的 jet tagging benchmark。本文使用其中 2M 子集进行训练与评估。 |

> ⚠️ 注意：JetFormer **未使用 pairwise interaction features**，而 ParT 使用了这些额外特征，因此 JetFormer 是在“信息更少”的输入下达到相近性能。

### 实验设置与评估指标
| 项目 | 设置 |
|-----|------|
| **训练配置** | 使用 AdamW 优化器，初始学习率 0.001；采用 OneCycleLR 学习率调度（效果最优）；batch size 分别为 256（HLS4ML）和 128（JETCLASS）。 |
| **HPO 方法** | 使用 Optuna 进行多目标超参搜索，目标为最大化 accuracy 并最小化 FLOPs；比较三种 sampler（NSGAIISampler、TPESampler、BoTorchSampler）。 |
| **压缩策略** |
| - Pruning | 结构化剪枝（torch-pruning），全局剪枝比例 50%，分步微调恢复精度。 |
| - Quantization | 采用 BitNet 框架进行 **1-bit 权重量化 + 8-bit 激活量化**，训练阶段使用 QAT（Quantization-Aware Training）。 |
| **硬件部署** | 使用 **Allo** 框架将模型编译为 HLS C++，并在 CPU、sw_emu、hw_emu 上验证功能正确性。 |
| **评估指标** | Accuracy、AUC、FLOPs、Parameters、Model Size、Inference Latency、FPGA Resource Utilization（BRAM, DSP, FF, LUT） |

### 基线方法对比
| 模型 | 类型 | 是否硬件友好 |
|------|------|-------------|
| MLP / Deep Sets (DS) / Interaction Network (IN) | 传统轻量模型 | ✅ 广泛用于 FPGA 部署 |
| JEDI-net / JEDI-linear | GNN-based，当前 FPGA SOTA | ✅ 支持 hls4ml，代表先进水平 |
| ParT | Transformer-based SOTA | ❌ 模型过大，难部署于 L1T |
| ParticleNet | DGCNN-based | ❌ 不适合 FPGA 实时推理 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### （1）在 JETCLASS 数据集上的表现（Table 3）
| Model | Accuracy | AUC | Params | FLOPs |
|-------|----------|-----|--------|-------|
| ParT | 0.836 | 0.9834 | 2.14M | 340M |
| **JetFormer** | **0.829** | **0.9827** | **1.66M** | **213M** |

✅ **结论**：  
- 准确率仅比 ParT 低 **0.7%**，AUC 仅低 0.07%，但 **FLOPs 减少了 37.4%**，参数减少 22.4%，体现出更强的计算效率。

#### （2）在 HLS4ML 150P 数据集上的表现（Table 1 & 2）
| Constituents | Model | Accuracy ↑ | FLOPs ↓ |
|--------------|-------|------------|---------|
| 8 particles | JetFormer | **67.1%** | 933k |
|             | IN (best prior) | 64.9% | 37k |
| 32 particles | JetFormer | **79.9%** | 4M |
|              | IN | 75.8% | 110k |

✅ **结论**：  
- 在所有粒子数配置下，JetFormer **准确率领先 3–4%**，尽管 FLOPs 更高，但可通过压缩解决。

#### （3）HPO 与 JetFormer-tiny 选择（Table 4）
- 选出最优小模型 **JetFormer-tiny**（4 transformer blocks, embed_dim=8, heads=2），FLOPs=26,168，val_acc=0.6525。
- NSGAIISampler 表现最佳，在早期即收敛至高 hypervolume，优于 TPE 和 BoTorch。

#### （4）结构化剪枝结果（Table 5）
| 指标 | 剪枝前 | 剪枝后 | 变化 |
|------|--------|--------|------|
| FLOPs | 26,168 | 13,784 | ↓47.3% |
| Parameters | 3,085 | 1,997 | ↓35.3% |
| Accuracy | 0.656 | 0.653 | ↓0.49% |
| Inference Time (GPU) | 3.517ms | 2.902ms | ↓17.5% |

✅ **结论**：剪枝可**减半计算成本**，精度损失极小（<0.5%），推理速度提升明显。

#### （5）1-bit 量化结果（Table 6）
| Particles | Model Size Before → After | Reduction | Accuracy Drop |
|----------|----------------------------|-----------|----------------|
| 8 | 404KB → 31KB | **92.2%** | 1.49% |
| 16 | 414KB → 41KB | **90.1%** | 2.15% |
| 32 | 451KB → 79KB | **82.6%** | 3.50% |

✅ **结论**：  
- 模型体积压缩达 **82–92%**，适合嵌入资源受限设备；
- 精度下降可控（1.5–3.5%），尤其在输入维度较低时表现更好。

#### （6）FPGA 硬件评估（Table 8）
| 模型 | Batch Size | Latency | BRAM/DSP/LUT 利用率 |
|------|------------|--------|---------------------|
| JetFormer-tiny (original) | 16 | 4.767ms | ~9.5%/1.07%/8.05% |
| JetFormer-tiny (pruned) | 16 | 2.705ms | ~9.8%/0.74%/7.5% |
| JetFormer-tiny (pruned) | **2** | **0.404ms** | **~2.92%/0.61%/6.95%** |

✅ **结论**：  
- 当 batch size 降至 2 时，延迟进入 **亚毫秒级（0.404ms）**，已接近 sub-microsecond 目标；
- 资源利用率仍远低于上限，说明可通过**更深的流水线（pipelining）或并行复制**进一步压低延迟。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Transformer 可以兼顾性能与效率**：JetFormer 在不依赖 pairwise interaction features 的前提下，实现了与 ParT 接近的性能，同时计算开销更低。
2. ✅ **统一架构可行**：一个基础模型可通过缩放与压缩，灵活服务于从离线分析到 FPGA 触发的不同场景。
3. ✅ **硬件压缩有效**：**structured pruning + 1-bit quantization** 可使模型缩小一个数量级以上，精度损失小于 3.5%。
4. ✅ **FPGA 部署路径打通**：借助 Allo 框架扩展，成功实现 JetFormer 在 FPGA 上的功能合成，验证了 Transformer 在 L1T 中的部署潜力。

### 方法的局限性
- **当前延迟尚未完全达标**：尽管已实现 0.4ms 延迟，但仍略高于严格的 **sub-microsecond（<1μs）** 要求。
- **缺乏 fully pipelined 实现**：实验中未手动添加深度流水线或循环展开，导致资源利用不足，存在性能瓶颈。
- **量化策略仍有改进空间**：1-bit 量化在高维输入下误差累积较明显，可能需要更精细的 mixed-precision 或 error compensation 技术。
- **Allo 对量化支持不足**：目前无法直接部署量化后的模型到 Vitis HLS 后端，限制了最终硬件性能测试。

### 未来工作方向
1. **深入硬件优化**：引入自动或手动的 **loop pipelining、unrolling、spatial replication** 等技术，充分挖掘 FPGA 并行能力，降低延迟至 μs 级。
2. **联合建模与硬件协同设计（co-design）**：探索模型结构与硬件架构的一体化优化，例如定制稀疏模式、专用 attention 单元。
3. **扩展至其他 HEP 任务**：将 JetFormer 框架推广至顶夸克重建、MET 估计、异常检测等任务。
4. **开发更鲁棒的低比特训练方法**：研究适用于物理模型的新型 QAT 策略，缓解极端量化下的性能退化。
5. **推动 Allo 生态发展**：完善对量化、动态 shape、复杂 control flow 的支持，增强其作为 HEP 领域 HLS 工具的能力。

---

> 🔗 **开源地址**：https://github.com/walkieq/JetFormer  
> 📌 **一句话总结**：JetFormer 提供了一个**从高性能建模到实际硬件部署的完整闭环方案**，首次系统性地证明了 Transformer 架构在 LHC 实时触发系统中的可行性与优越性。

</details>

---

### 3. [Multi-Agent Deep Reinforcement Learning Under Constrained Communications](https://arxiv.org/abs/2601.17069)

**Authors**: Shahil Shaik, Jonathon M. Smereka, Yue Wang  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.17069v1  

#### Abstract
Centralized training with decentralized execution (CTDE) has been the dominant paradigm in multi-agent reinforcement learning (MARL), but its reliance on global state information during training introduces scalability, robustness, and generalization bottlenecks. Moreover, in practical scenarios such...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《Multi-Agent Deep Reinforcement Learning Under Constrained Communications》总结**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
传统的多智能体强化学习（MARL）普遍采用**集中式训练、去中心化执行**（CTDE）范式，其在训练阶段依赖全局状态信息（global state），导致以下问题：
- **可扩展性差**：大规模系统中难以获取和传输全局信息；
- **鲁棒性弱**：训练时依赖全局信息，而执行时仅能访问局部观测，造成**训练-测试不匹配**（train-test mismatch）；
- **通信受限场景下失效**：在无线网络、越野机器人、搜救等实际应用中，长距离低延迟通信难以维持。

本文旨在设计一种**完全去中心化的MARL框架**，无需任何集中式组件或全局可观测性，仅通过**局部观测和对等通信**（peer-to-peer communication）实现高效协作。

---

### **提出的新方法与新思路**
作者提出了两个核心组件：

#### ✅ **D-GAT（Distributed Graph Attention Network）**
- 一种**分布式图注意力网络**，允许每个智能体独立维护和更新自己的注意力参数；
- 利用**多跳通信**（multi-hop communication）进行**全局状态推理**（global state inference）；
- 引入**共识正则化损失**（consensus loss）和**去中心化随机梯度下降**（D-SGD）机制，促进邻居间表示一致性，提升协调能力。

#### ✅ **DG-MAPPO（Distributed Graph-Attention MAPPO）**
- 一个**完全分布式的MARL框架**，结合D-GAT与MAPPO架构；
- 智能体基于**本地观测 + D-GAT推断的全局状态近似 + 平均团队奖励**来优化策略；
- 所有学习过程（策略、价值函数、图网络参数）均在本地完成，**无中央控制器或共享参数**。

---

### **相比现有方法的优势**
| 维度 | CTDE方法（如MAPPO, HAPPO） | DG-MAPPO |
|------|----------------------------|----------|
| **训练模式** | 集中式训练（需全局状态） | 完全分布式训练（仅局部信息） |
| **通信需求** | 训练期高带宽汇聚，执行期通常“静默” | 训练与执行均使用轻量级多跳通信 |
| **泛化性** | 存在train-test mismatch | 更强鲁棒性，适应动态拓扑变化 |
| **可扩展性** | 受限于中心节点吞吐 | 线性可扩展，适合大团队 |
| **隐私/安全** | 全局信息暴露风险 | 仅交换加密特征向量 |

> 🔥 **首次实现了完全消除对特权集中信息依赖的MARL方法**，真正实现“学与行皆去中心化”。

---

## **2. 核心实验方法和设置**

### **使用的数据集与环境**
在三个主流MARL基准上进行评估：
1. **StarCraftII Multi-Agent Challenge (SMAC)**  
   - 多种战斗任务（如3m, 8m vs 9m, MMM2等）
   - 包含同构/异构团队、不同难度等级
2. **Google Research Football (GFootball)**  
   - 足球对抗任务（Academy 3 vs 1 with Keeper）
   - 连续动作空间，强调实时协作
3. **Multi-Agent MuJoCo (MA-MuJoCo)**  
   - 物理控制任务（如Multi-HalfCheetah）
   - 固定邻接关系，模拟真实机器人关节连接

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|-------|------|
| **观测范围限制** | 在SMAC中将默认视野从9降至4，模拟稀疏通信 |
| **通信模型** | 动态图 $ G=(N,E) $，边由距离决定；支持1-hop到N-hop消息传递 |
| **评估指标** | - 胜率（Win Rate）<br>- 平均回合得分（Average Episode Score）<br>- 收敛速度、稳定性 |
| **训练方式** | 每个agent拥有独立buffer和网络，仅通过D-GAT通信同步表征 |
| **超参数** | 使用标准PPO配置，D-GAT学习率5e-4，consensus loss权重20（见附录） |

---

### **基线方法对比**
与多个SOTA CTDE方法比较：
- **MAPPO**：经典的CTDE-PPO方法
- **HAPPO**：分层注意力PPO，支持异构agent
- **MAT-Dec**：基于序列建模的去中心化变体（但仍为CTDE）

> ⚠️ 注意：DG-MAPPO**仅使用局部观测**，而所有baseline均可访问全局状态。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自Table 1 & Figure 3）**

#### 📊 **SMAC 胜率表现（% ± std）**
| 任务 | MAPPO | HAPPO | DG-MAPPO |
|------|--------|--------|-----------|
| 3m (Easy) | 100.0(0.4) | 100.0(1.2) | **100.0(1.4)** |
| 8m (Easy) | 96.8(2.9) | 97.5(1.1) | **100.0(1.4)** |
| MMM (Easy) | 95.6(4.5) | 81.2(22.9) | **100.0(1.6)** |
| 5m vs 6m (Hard) | 88.2(6.2) | 77.5(7.2) | **88.7(4.7)** |
| 10m vs 11m (Hard) | 96.3(5.8) | 87.5(6.7) | **100.0(1.4)** |
| 25m (Hard) | 100.0(2.7) | 95.0(2.0) | **95.3(3.1)** |
| MMM2 (Hard+) | 81.8(10.1) | 88.8(2.0) | **98.9(1.2)** |
| 6h vs 8z (Hard+) | 88.4(5.7) | 76.2(3.1) | **95.0(2.7)** |

✅ **结论**：DG-MAPPO在绝大多数任务上**持平甚至超越CTDE方法**，尤其在复杂异构任务中优势明显。

---

#### 📈 **稀疏通信下的性能（Table 2, Sight Range=4）**
| 任务 | 1-hop | 3-hops | N-hops |
|------|--------|--------|--------|
| 6h vs 8z | 77.08% | 83.68% | **83.75%** |
| MMM2 | 90.62% | 92.7% | **93.1%** |

📌 即使在极端稀疏通信下（平均度≈3），**仅1-hop即可达到良好性能**，增加hop数带来边际增益。

---

#### 🏃‍♂️ **连续控制任务（MA-MuJoCo）**
- 在 **Multi-HalfCheetah (6×1)** 上：
  - K=1：接近CTDE收敛曲线
  - K=3：几乎重合
  - K=6：**超过CTDE baseline**
- 表明：**少量多跳通信足以支撑高质量协作**

---

### **消融实验结果（Ablation Study）**

#### 🔹 **A. 消息聚合方式对比（Mean vs Attention）**
- 在简单任务（MMM2, 10m vs 11m）中，mean aggregation表现接近attention；
- 在困难任务（6h vs 8z）中，**attention显著优于mean**，尤其是在低hop设置下；
- 结论：**attention机制在挑战性任务中有更强表达力**。

#### 🔹 **B. Hop数量的影响**
- 性能随hop增加而提升，但呈**亚线性增长**；
- **K=1 已可获得较强性能**，K=N/2基本达到饱和；
- 实践建议：从小hop开始，按需扩展以节省通信开销。

#### 🔹 **C. Consensus Loss的作用**
- 在高hop设置下（如6-hop），加入consensus loss显著加快收敛并提高最终性能；
- 在1-hop设置下效果较小但仍有益；
- 结论：该损失有助于稳定训练，尤其在信息传播充分时更有效。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **结构化局部通信足以支持高质量多智能体协作**  
   > 无需全局状态，仅靠D-GAT的多跳注意力通信即可实现媲美甚至超越CTDE的表现。

2. ✅ **DG-MAPPO具有优异的可扩展性和鲁棒性**  
   > 在多达25个agent的任务中仍保持高性能，且在稀疏通信下依然稳健。

3. ✅ **训练与执行的一致性提升了泛化能力**  
   > 由于训练和执行都依赖相同通信机制，避免了CTDE的train-test mismatch问题。

4. ✅ **轻量级设计具备实用潜力**  
   > 少量hop（如1~3）即可取得良好性能，适合部署于资源受限的真实系统（如无人机群、无人车编队）。

---

### **方法的局限性**
- ❗ **通信频率较高**：执行阶段仍需持续通信，不适合“静默部署”场景；
- ❗ **对初始连通性敏感**：若通信图长期不连通，可能影响全局状态推理质量；
- ❗ **D-GAT参数量略增**：每个agent独立维护GAT参数，内存占用高于参数共享版本；
- ❗ **未考虑恶意节点或欺骗行为**：假设所有agent合作诚实。

---

### **未来工作方向**
1. **引入自适应通信机制**：让agent自主决定何时/向谁发送消息（类似ATOC，但去中心化实现）；
2. **探索异步通信与延迟容忍机制**：适用于真实无线网络中的丢包与延迟；
3. **结合通信压缩与量化技术**：降低带宽需求，便于边缘设备部署；
4. **拓展至混合合作-竞争环境**：研究在非完全合作下的稳定性；
5. **硬件原型验证**：在真实机器人平台（UGV/UAV）上部署测试。

---

## ✅ **总结一句话**
> **DG-MAPPO是首个完全摆脱集中式训练依赖的MARL框架，证明了仅凭局部观测与多跳注意力通信，就能在多种复杂任务中实现与CTDE相当甚至更优的协作性能，为现实世界中可扩展、鲁棒的多智能体系统提供了新路径。**

</details>

---

### 4. [Least-Loaded Expert Parallelism: Load Balancing An Imbalanced Mixture-of-Experts](https://arxiv.org/abs/2601.17111)

**Authors**: Xuan-Phi Nguyen, Shrey Pandit, Austin Xu, Caiming Xiong, Shafiq Joty  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.17111v1  

#### Abstract
Mixture-of-Experts (MoE) models are typically pre-trained with explicit load-balancing constraints to ensure statistically balanced expert routing. Despite this, we observe that even well-trained MoE models exhibit significantly imbalanced routing. This behavior is arguably natural-and even desirabl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Least-Loaded Expert Parallelism: Load Balancing An Imbalanced Mixture-of-Experts*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题
- **MoE模型在实际运行中存在严重的专家路由不平衡**（imbalanced routing），即使在预训练阶段引入了负载均衡机制（如auxiliary loss），后训练（post-training）或推理阶段仍会出现大量token集中到少数专家的情况。
- **标准Expert Parallelism（EP）假设负载均衡**，但在极端不平衡场景下会导致：
  - 少数GPU严重过载（compute- and memory-bound）
  - 出现 **Out-of-Memory (OOM)** 错误
  - 整体延迟显著上升
- 现有缓解策略（如降低batch size、复制冗余专家）会牺牲吞吐量或增加内存开销。

### 🚀 提出了什么新方法或新思路
提出 **Least-Loaded Expert Parallelism (LLEP)** —— 一种动态负载均衡的EP算法，其核心思想是：
- **动态重路由**：当某个GPU上的专家负载超过容量阈值时，将超额的tokens及其对应的expert weights转发至当前最空闲的GPU进行“共享计算”。
- **双维度优化**：同时考虑 **compute load** 和 **memory usage**，并权衡通信开销与本地计算成本。
- **精确计算保障**：LLEP是一个**exact MoE computation algorithm**，不改变原始MoE的数学行为（如不修改gate输出或路由逻辑）。
- 支持**前向与反向传播**，可用于训练和推理。

### 🔍 相比现有方法的优势
| 方法 | 是否改变模型行为 | 是否支持训练 | 内存效率 | 极端不平衡下稳定性 |
|------|------------------|-------------|----------|---------------------|
| 标准EP | 否 | 是 | 差（OOM风险高） | ❌ |
| 降低batch size | 否 | 是 | 中（吞吐下降） | ⚠️ |
| 复制专家（EPLB） | 否 | 否（仅推理） | 差（额外副本） | ⚠️（仍可能OOM） |
| LLEP（本文） | **否** | **是** | **优（峰值内存稳定）** | ✅ |

> ✅ LLEP在保持数学等价性的前提下，实现了系统级的动态负载均衡。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **合成不平衡负载模拟**：人工构造不同级别的不平衡场景（30% ~ 95% tokens集中在1/4/16个专家上）
- **真实任务数据**：
  - `Megatron-Math` 数据集（用于end-to-end测试）
  - 对话数据来自 `DeepScaleR`，响应由gpt-oss模型自生成
- 模型本身为预训练好的 **gpt-oss-20b** 和 **gpt-oss-120b**

### ⚙️ 实验设置和评估指标
| 设置项 | 配置 |
|-------|------|
| 硬件平台 | 8× H200 GPUs（P=8） |
| MoE配置 | 覆盖gpt-oss-120b、DeepSeek-V3、Kimi-K2等多种架构 |
| Batch size per GPU | 32K（gpt-oss）、16K（其他） |
| 评估指标 | 
| - Speedup（相对标准EP的加速比） |
| - Peak memory per GPU（峰值显存占用） |
| - Full-model throughput（整模型吞吐） |
| - End-to-end wall-clock time（端到端耗时） |
| - Accuracy vs time（微调收敛速度） |

### 🔁 基线方法对比
- **Standard EP**：传统Expert Parallelism实现（基于All-to-All通信）
- **EPLB**（Expert Parallelism Load Balancer）：通过复制热门专家提升负载能力（仅适用于推理）
- 所有对比均在同一硬件和软件栈下完成（PyTorch + NCCL）

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）MoE层级别性能（图1、图4）
| 场景 | 最大加速比（Speedup） | 峰值内存减少 |
|------|------------------------|--------------|
| 极端不平衡（95% tokens → 1 expert） | **高达6.11×**（gpt-oss-120b） | **最高达5×降低** |
| 平衡情况 | ~1×（无损失） | 显存稳定 |
| 不同模型平均加速 | **4–5×** | 显著抑制显存 spikes |

> 💡 LLEP在越不平衡的情况下优势越明显；而在平衡情况下几乎无额外开销。

#### （2）端到端全模型吞吐（图1c）
| 模型 | LLEP加速比 |
|------|------------|
| gpt-oss-20b | **2.2×** |
| gpt-oss-120b | **1.9×** |

> 即使受非MoE层（如attention）影响，MoE层的优化仍带来显著端到端收益。

#### （3）训练效率（图5）
- 在对gpt-oss-20b进行SFT（Supervised Fine-Tuning）时：
  - LLEP比标准EP **快1.25×**
  - 收敛精度一致（accuracy on AIME’25相同）
  - 受益于更高效的MoE处理，减少了等待时间

---

### 🔍 消融实验结果（Ablation Study）

#### （1）Batch Size 影响（图6a）
- **Batch越大，加速越明显**：大batch更容易触发GPU容量饱和，使得LLEP的负载均衡效果更突出。
- 原因：大batch下通信/计算开销占比更高，LLEP的动态分配更能发挥优势。

#### （2）超参数 $ \alpha $（每GPU最大负载因子）（图6b）
- $ \alpha $ 越小（限制越严格），速度提升越高
- 但太小可能导致频繁通信 → 存在一个**trade-off**

#### （3）自适应阈值 $ \lambda $（图7a）
- 当路由接近平衡时（imbalance ratio < $ \lambda $），自动切换回标准EP
- 设置 $ \lambda = 1.3 $ 可避免不必要的weight transfer开销，在轻度不平衡时更高效

#### （4）Hidden Size 影响（图7b）
- **随着D/H增大，LLEP增益增强**
- 原因：compute效率提升快于communication overhead增长，使得负载均衡带来的收益更大

#### （5）专家数量N的影响（附录图9）
- N越大，LLEP优势越明显
- 更多专家意味着更高的潜在不平衡风险，也提供了更多“空闲设备”供卸载使用

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Imbalanced routing是MoE模型的自然且理想现象**  
   - 表明部分专家已专业化（specialization），而非训练失败。
   - 强行强制均衡（如加loss）可能破坏这种有益的专业化。

2. **系统级负载均衡优于模型级干预**  
   - LLEP在不改动模型的前提下，从**系统调度层面**解决了负载不均问题。
   - 是首个支持**训练+推理通用**、且保证**数学等价性**的动态EP方案。

3. **LLEP实现了“最小集体延迟 + 最低峰值内存”双重目标**  
   - 所有GPU尽可能同时完成任务（workload balancing）
   - 显存使用趋于平稳，避免OOM

4. **适用于大规模部署场景**  
   - 特别适合domain-specific fine-tuning / inference，这些场景天然导致路由偏斜。

---

### ⚠️ 方法的局限性
- **引入额外通信开销**：需要P2P传输expert weights，虽然被设计为仅在“划算时”才触发。
- **依赖运行时负载预测**：需提前获取全局expert load信息，增加了调度复杂性。
- **当前实现基于Python调度**：LLA算法用Python实现，存在一定控制开销，未来可通过kernel fusion进一步优化。
- **多节点环境下需注意跨节点通信代价**：目前未深入优化inter-node spilling策略。

---

### 🔮 未来工作方向
1. **Kernel-level融合实现**  
   - 将dispatch + weight transfer + grouped-GEMM融合为单一kernel（类似DeepEP或Triton-Distributed风格）
2. **支持异构硬件调度**  
   - 根据GPU性能差异动态选择“least-loaded”设备
3. **结合专家缓存与预加载机制**  
   - 对高频专家做缓存，减少重复传输
4. **扩展至Tensor Parallelism + EP混合架构**
5. **自动化超参数调优框架**  
   - 自动学习最优的 $ \alpha, m, \lambda $ 组合以适配不同模型与硬件

---

## 总结一句话
> **LLEP通过动态地将过载专家的任务“卸载”给空闲GPU，在不改变MoE语义的前提下，实现了高达6×加速和5×显存节省，为大规模MoE模型在真实不平衡场景下的高效训练与推理提供了可靠解决方案。**

🔗 代码开源地址：[github.com/SalesforceAIResearch/LeastLoadedEP](https://github.com/SalesforceAIResearch/LeastLoadedEP)

</details>

---

### 5. [Latent-Space Contrastive Reinforcement Learning for Stable and Efficient LLM Reasoning](https://arxiv.org/abs/2601.17275)

**Authors**: Lianlei Shan, Han Chen, Yixuan Wang, Zhenjie Liu, Wei Li  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.17275v1  

#### Abstract
While Large Language Models (LLMs) demonstrate exceptional performance in surface-level text generation, their nature in handling complex multi-step reasoning tasks often remains one of ``statistical fitting'' rather than systematic logical deduction. Traditional Reinforcement Learning (RL) attempts...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Latent-Space Contrastive Reinforcement Learning for Stable and Efficient LLM Reasoning》核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前 Large Language Models（LLMs）在处理复杂多步推理任务时，往往依赖“统计拟合”而非系统性的逻辑推导，导致中间步骤存在**局部合理但全局错误**的现象。传统 Reinforcement Learning（RL）方法（如 GRPO）虽能引入正确性奖励来引导推理，但在**离散 token 空间**中直接进行策略优化面临三大瓶颈：

- **Sample Inefficiency**：每次策略更新需生成完整 token 序列，反馈稀疏且计算代价高昂。
- **High Gradient Variance**：长序列的重要性采样误差累积严重，训练不稳定。
- **Catastrophic Forgetting**：对主模型进行端到端微调易破坏预训练阶段获得的语言与知识能力。

### 提出的新方法：DeepLatent Reasoning（DLR）
作者提出 **DeepLatent Reasoning（DLR）** ——一种基于**连续潜在空间**的双向对比强化学习框架，实现“**先思后言**”（think-before-speak）的高效推理范式。其核心思想是将试错过程从高维离散的 token 空间迁移至语义丰富的连续 latent manifold 中。

#### 核心架构设计：
- **Assistant Model**：轻量级可训练模型，在 latent space 中采样 $K$ 条候选推理轨迹（latent reasoning trajectories），表示为“软思维”（soft thoughts）。
- **Frozen Main Model**：冻结参数的大模型，仅负责将筛选后的高质量 latent 轨迹解码为自然语言输出。
- **Latent Filtering & Selective Decoding**：通过 dual reward 机制在 latent 层面预筛，只解码高价值路径，大幅减少昂贵的前向传播次数。
- **Contrastive Learning Objective**：引入对比损失鼓励 latent 轨迹多样性，避免模式坍缩（mode collapse），实现**定向探索**（directed exploration）。
- **Zero-Forgetting Updates**：梯度更新仅作用于 Assistant 和投影层，主模型完全冻结，从根本上消除灾难性遗忘。

### 相比现有方法的优势
| 维度 | 传统方法（如 GRPO、DeepSeekMath） | DLR |
|------|-------------------------------|-----|
| 探索空间 | Discrete token space | Continuous latent space |
| 采样效率 | 高成本全序列 rollout | 低成本 latent 向量采样 |
| 梯度方差 | 高（随序列长度指数增长） | 显著降低（单 latent 对象建模） |
| 忘记风险 | 存在（主模型微调） | 数学上消除（主模型冻结） |
| 探索质量 | 无方向随机扰动 | 语义上有意义的多样化探索 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **GSM8K**：约 8.5K 小学数学应用题，标准划分：7,473 训练 / 1,319 测试。
- **MATH**：约 12.5K 竞赛级数学问题，采用 7,500 训练 / 5,000 测试的标准拆分。

### 实验设置
- **主模型（Main Model）**：`LLaMA-2-7B`，**全程冻结**。
- **助理模型（Assistant Model）**：`LLaMA-2-1.3B`，用于 latent 推理采样。
- **硬件平台**：8× NVIDIA A100 GPUs。
- **Latent Dimension**：512
- **Group Size (G)**：64
- **最大 latent 步数**：32
- **训练轮次**：3 epochs

### 评估指标
- **Pass@1 Accuracy**：模型一次性生成正确答案的比例，作为主要性能指标。
- **Main Model Forward Pass Ratio**：衡量计算开销的关键效率指标。
- **Qualitative Analysis**：人工检查 100 个 GSM8K 解答中的幻觉中间步骤比例。

### 基线方法对比
- **Base LLaMA-2-7B**：零微调基础模型。
- **GRPO (token-level)**：标准组相对策略优化，操作于 token 空间。
- **DeepSeekMath-RL**：代表性的 GRPO 变体，具备序列级 KL 正则化等改进。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）

| Method | #Forward | GSM8K (Pass@1) | MATH (Pass@1) |
|--------|----------|----------------|---------------|
| Base LLaMA-2-7B | — | 14.8% | 3.9% |
| GRPO (token-level) | 100% | 46.2% | 15.7% |
| DeepSeekMath-RL | 100% | 51.7% | 18.3% |
| **DLR (Ours)** | **18%** | **55.4%** | **22.1%** |

> ✅ **结论**：DLR 在仅使用 **18% 的主模型前向传播**下，仍显著超越所有 baseline，在两个 benchmark 上均达到 **SOTA 性能**。

### 与基线方法的对比结果
- **性能提升**：
  - GSM8K 上比 DeepSeekMath 提升 **+3.7%**；
  - MATH 上提升 **+3.8%**，显示其在更难任务上的泛化优势。
- **效率飞跃**：
  - 主模型计算量下降至 **1/5.6**（见 Section 11），极大降低部署成本。
- **稳定性增强**：
  - 训练收敛更平滑，gradient variance 更低（理论分析支持）。
- **推理质量更高**：
  - 定性分析表明，DLR 将幻觉中间步骤从 **27%（GRPO）降至 9%**，验证 latent 过滤的有效性。

### 消融实验结果（Ablation Studies）

#### （1）冻结主模型的影响（Table 4）
| 设置 | 准确率 | 稳定性 |
|------|--------|--------|
| 主模型可训练 | 56.1% | 低（出现震荡与遗忘） |
| 主模型冻结（DLR） | 55.4% | 高（稳定收敛） |

> 🔍 虽峰值略低，但**稳定性与通用能力保留**远胜过微调方案。

#### （2）移除 Contrastive Loss 的影响
- 在 MATH 上造成 **4.7% 的绝对性能下降**；
- latent 轨迹趋于集中，缺乏多样性，test-time scaling 效果变差。

#### （3）计算成本分析（Equation 13）
- GRPO 成本：$O(G \cdot C_M)$
- DLR 成本：$O(G \cdot C_A + K \cdot C_M)$，其中 $K \ll G$
- 实际 $K/G \approx 0.18$，带来 **5.6× 主模型计算节省**

---

## 4. 关键结论和发现

### 主要发现
1. **Latent Space 是更优的推理优化场所**：相比离散 token 空间，连续 latent manifold 提供更平滑、语义连贯的优化地形，显著降低 gradient variance 并提高 sample efficiency。
2. **Reasoning 与 Generation 应解耦**：将“思考”交给轻量 Assistant 在 latent 空间完成，而“表达”由冻结大模型执行，是一种高效且安全的设计范式。
3. **Contrastive Learning 可驱动有意义的探索**：通过对比损失强制 latent 编码分散，可在无需人工标注的情况下自发涌现出多路径、多角度的推理能力。
4. **Zero-Forgetting 是可行且必要的**：冻结主模型不仅理论上防止 catastrophic forgetting，在实践中也保障了语言通顺性和知识完整性。

### 方法的局限性
- **极端符号化任务适应性有限**：对于形式化证明等要求每一步严格对齐 token 的任务，latent-to-token 解码可能存在粒度不匹配问题。
- **Reward Attribution 较粗粒度**：当前奖励仍基于整个 latent 轨迹打分，缺乏 step-level 的细粒度监督信号。
- **依赖高质量 frozen decoder**：若主模型本身解码能力弱，则 latent 表示再优也无法体现。

### 未来工作方向
- 扩展至其他推理密集型任务：如 **code generation**、**commonsense reasoning**、**formal verification**。
- 探索 **latent space 的 scaling laws**：研究 latent dimension、trajectory length 与性能的关系。
- 引入 **finer-grained auxiliary supervision**：例如 step-wise reward modeling 或 latent consistency constraints。
- 构建 **self-evolution + self-correction** 机制：利用 DLR 框架实现 LLM 自主迭代优化推理策略。

--- 

> 📌 **总体评价**：  
> DLR 不仅是一项技术改进，更是对 LLM 推理范式的根本重构——从“在词海中盲目搜索”转向“在思想空间中规划路径”。它为构建**稳定、高效、可持续进化**的 LLM reasoning 系统提供了坚实的新路径。

</details>

---

### 6. [BrainDistill: Implantable Motor Decoding with Task-Specific Knowledge Distillation](https://arxiv.org/abs/2601.17625)

**Authors**: Yuhan Xie, Jinhan Liu, Xiaoyong Ni, Fei Tan, Icare Sakr, Thibault Collin, Shiqi Sun, Alejandro Rodriguez Guajardo, Demon Fanny, Charles-francois Vincent Latchoumane, Henri Lorach, Jocelyne Bloch, Gregoire Courtine, Mahsa Shoaran  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.17625v1  

#### Abstract
Transformer-based neural decoders with large parameter counts, pre-trained on large-scale datasets, have recently outperformed classical machine learning models and small neural networks on brain-computer interface (BCI) tasks. However, their large parameter counts and high computational demands hin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：BrainDistill: Implantable Motor Decoding with Task-Specific Knowledge Distillation**

---

## **1. 主要贡献和创新点**

### **解决的问题**
当前基于 **Transformer** 的神经解码器虽然在脑机接口（BCI）任务中表现出色，但由于其庞大的参数量和高计算开销，难以部署在功耗受限的**可植入系统**（implantable BCI）中。此外，传统的知识蒸馏（Knowledge Distillation, KD）方法试图完整保留教师模型的特征表示，但在学生模型容量远小于教师时，往往无法有效迁移关键任务信息。

### **提出的新方法与创新思路**
本文提出了 **BrainDistill**，一个面向可植入系统的新型运动意图解码框架，其核心是 **Task-Specific Knowledge Distillation (TSKD)** 和轻量化神经解码器 **Implantable Neural Decoder (IND)**。

#### **主要创新点：**
- **TSKD（任务特定知识蒸馏）**：
  - 不同于传统 KD 中直接对齐教师与学生特征空间的方法，TSKD 通过一个**有监督的投影器**（supervised projector），将教师模型的高维嵌入压缩到一个**任务相关的子空间**。
  - 该方法显式地优先保留对解码任务最关键的特征，从而弥补学生模型容量不足的问题。
  
- **Task-Specific Ratio (TSR)**：
  - 提出了一种新的评估指标 **TSR**，用于量化投影后特征中保留的任务相关信息比例。
  - 该指标可用于选择最优的投影方式，并证明其与下游任务性能高度相关（相关系数 >0.9），而传统指标如重构误差或互信息则几乎无相关性。

- **IND 架构设计**：
  - 设计了一个轻量级、适合硬件部署的 Transformer 解码器，采用 **Continuous Wavelet Transform (CWT)** 进行特征分词，结合 **Linear Attention** 结构，显著降低计算复杂度。
  - 所有线性层仅含权重矩阵（无偏置），有利于后续整数量化。

- **Quantization-Aware Training (QAT) with Learnable Clipping Ranges**：
  - 提出一种支持**整数仅推理**（integer-only inference）的量化感知训练方案。
  - 引入**可学习的激活裁剪范围**（learnable clipping ranges），在训练过程中联合优化，以最小化量化带来的性能损失。

### **相比现有方法的优势**
| 方面 | BrainDistill 的优势 |
|------|---------------------|
| **模型大小与效率** | IND 仅 30K 参数，远小于主流基础模型（如 100M 参数 Transformer） |
| **蒸馏有效性** | 在小样本校准场景下，TSKD 显著优于 KD、SimKD、VkD、TOFD、TED 等基线方法 |
| **硬件友好性** | 支持整数仅推理，功耗低至 **5.66 mW**，满足可植入设备的安全要求（<15–40 mW） |
| **泛化能力** | 可跨模态（ECoG、EEG、spike）、跨任务、跨被试稳定工作 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
| 数据集 | 类型 | 被试数 | 任务描述 |
|--------|------|--------|----------|
| **Human-C** | ECoG | 1 | 多类上肢动作分类（6类：休息、肩屈、肘伸等） |
| **Monkey-R** | ECoG | 1 | 上肢关节轨迹回归（腕部位置预测） |
| **Human-D** | ECoG | 12 | 二分类：手臂移动 vs 静止 |
| **BCIC-2A / BCIC-2B** | EEG | 9 | 运动想象分类（4类 / 2类） |
| **FALCON-M1** | Spikes | 1 | EMG 信号回归（16通道） |

### **实验设置**
- **训练范式**：
  - **Scratch**：直接在小规模校准数据 $ \mathcal{X}_{\text{recalib}} $ 上训练。
  - **Distillation**：从预训练的大模型（teacher）蒸馏知识到 IND。
- **教师模型**：
  - Human-C：100M 参数 Transformer
  - BCIC-2A/B：EEGPT（101M）
  - FALCON-M1：NDT2（3.7M）
- **评估指标**：
  - 分类任务：**F1 Score**、**Average Recall**
  - 回归任务：**R²**
  - 跨被试：**Accuracy**、**AUROC**
  - 硬件：**功耗估计（mW）**

### **基线方法对比**
| 类别 | 对比方法 |
|------|--------|
| **解码器架构** | EEGNet, EEGConformer, ATCNet, CTNet, LaBraM |
| **知识蒸馏方法** | KD (Hinton et al.), SimKD, VkD, RdimKD, TOFD, TED |
| **量化方法** | I-ViT (W8A8) |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) Human-C 多类分类任务（F1 Score）**
| 方法 | 1-1 | 4-4 | 4-5 | 4-6 | 16-17 |
|------|-----|-----|-----|-----|-------|
| **Teacher (100M)** | 77.6 | 73.3 | 74.5 | 77.3 | 80.6 |
| **IND (Scratch)** | 69.1 | 64.9 | 61.5 | 70.6 | 72.3 |
| **IND + TSKD** | **73.9** | **73.3** | **75.0** | **77.3** | **70.4** |
| IND + TSKD(CE) | 73.4 | 68.1 | 65.1 | 71.7 | **69.8** |
| IND + RdimKD | 72.3 | 68.6 | 63.1 | 72.3 | 65.9 |
| IND + TED | 71.2 | 67.9 | 61.5 | 71.9 | 69.1 |

> ✅ **TSKD 在多个会话中达到甚至超过教师模型的表现**，尤其在 4-5 和 4-6 会话中表现最佳。

#### **(2) 跨模态蒸馏效果（F1/AUROC/R²）**
| 数据集 | 方法 | 性能 |
|--------|------|------|
| **BCIC-2A (EEG)** | IND + TSKD | **26.73** (vs. 24.22 scratch) |
| **BCIC-2B (EEG)** | IND + TSKD | **69.78** AUROC (vs. 68.79 scratch) |
| **FALCON-M1 (Spikes)** | IND + TSKD | **43.52** R² (vs. 37.59 scratch) |

> ✅ **TSKD 在 EEG 和 spike 数据上均有效提升性能**，验证了其跨模态通用性。

#### **(3) 硬件部署性能（Human-C）**
| 模型 | F1 (avg) | 功耗 (mW) |
|------|---------|-----------|
| FP32 (Full Precision) | 73.9 | 22.84 |
| I-ViT (W8A8) | 72.4 | 5.66 |
| **Ours (W8A8)** | **73.5** | **5.66** |

> ✅ **量化后性能损失 <3%，功耗仅为全精度模型的 1/4**，且显著优于 I-ViT。

---

### **消融实验结果**

#### **(1) 投影方法对比（Human-C）**
| 投影方式 | F1 (4-5) | Avg Recall (4-5) | TSR |
|----------|---------|------------------|-----|
| **TSKD (ours)** | **75.0** | **60.9** | **0.936** |
| PCA | 72.5 | 56.0 | 0.749 |
| Random Orthogonal | 73.8 | 57.9 | 0.694 |
| Inverse Projection | 71.7 | 55.2 | – |

> ✅ **TSKD 投影显著优于 PCA、随机投影和逆投影**，且 TSR 与性能强相关。

#### **(2) IND 架构消融（Monkey-R）**
| 模型 | R² (Day 5) | R² (Day 15) |
|------|------------|-------------|
| **IND (CWT + Linear Attention)** | **0.6835** | **0.5448** |
| CWT + RNN | 0.5511 | 0.3342 |
| CWT + CNN | 0.5195 | 0.2222 |
| IND (STFT) | 0.3815 | 0.4509 |
| IND (Spectrogram) | -2.276 | -0.0542 |

> ✅ **CWT 特征 + Linear Attention 架构表现最优**，且具有良好的长期稳定性。

#### **(3) 波段重要性分析（Human-D）**
移除不同频率波段后的性能变化（Acc / AUROC）：
| 移除频率 | Acc ↓ | AUROC ↓ |
|--------|------|--------|
| 默认（10–125Hz） | 77.1 | 85.0 |
| /{10Hz,20Hz} | 74.5 | 85.2 |
| /{40Hz,60Hz} | 76.0 | 83.7 |
| /{100Hz,125Hz} | 76.1 | 84.3 |

> ✅ **所有频段（尤其是 30Hz 和高频段）均对解码有贡献**，表明多频带融合的重要性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **TSKD 是一种高效的蒸馏策略**：
   - 在学生模型容量远小于教师时，传统 KD 效果有限，而 TSKD 通过**任务导向的特征压缩**，能更有效地迁移关键信息。
   - **TSR 指标可准确预测蒸馏性能**，为投影器设计提供理论指导。

2. **IND 是一个高效且可部署的解码器**：
   - 尽管参数少（30K），IND 在多种任务上仍能取得接近大模型的性能。
   - CWT 分词 + Linear Attention 架构兼具**可解释性**与**计算效率**。

3. **整数仅推理可行且高效**：
   - 通过可学习裁剪范围的 QAT，实现了 W8A8 下的整数推理，**性能损失极小（<3%）**，功耗降至 **5.66 mW**，完全满足可植入需求。

4. **方法具备强泛化能力**：
   - 成功应用于 ECoG、EEG、spike 多种信号模态。
   - 在跨会话、跨被试任务中保持稳定性能。

### **局限性**
- 当前 TSKD 依赖于一个固定的教师模型，若教师本身性能不佳，则蒸馏效果受限。
- IND 架构目前针对短时序信号设计，在处理长序列任务时可能需进一步优化。
- 实验主要基于离线数据分析，真实在线植入环境下的鲁棒性有待验证。

### **未来工作方向**
- 探索**动态更新的教师模型**机制，实现长期自适应。
- 将 BrainDistill 框架扩展至**闭环神经调控系统**（如脑脊接口）。
- 开发专用 ASIC 芯片，集成 ADC、IND 解码器与刺激模块，构建完整的 SoC 系统。
- 推动临床试验，验证其在瘫痪患者中的实际应用价值。

---

> **总结**：  
> **BrainDistill** 提供了一个从算法到硬件部署的完整解决方案，首次实现了高性能、小体积、低功耗的可植入神经解码系统。它不仅推动了 BCI 基础模型的实用化进程，也为未来智能神经假体的发展奠定了坚实的技术基础。

</details>

---

### 7. [Crystal-KV: Efficient KV Cache Management for Chain-of-Thought LLMs via Answer-First Principle](https://arxiv.org/abs/2601.16986)

**Authors**: Zihan Wang, Cheng Tang, Lei Gong, Cheng Li, Chao Wang, teng wang, Wenqi Lou, Xuehai Zhou  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.16986v1  

#### Abstract
Chain-of-Thought (CoT) reasoning in large language models (LLMs) significantly improves accuracy on complex tasks, yet incurs excessive memory overhead due to the long think-stage sequences stored in the Key-Value (KV) cache. Unlike traditional generation tasks where all tokens are uniformly importa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Crystal-KV: Efficient KV Cache Management for Chain-of-Thought LLMs via Answer-First Principle*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在 **Chain-of-Thought (CoT)** 推理中，LLM 需要生成大量中间“思考”token（think-stage tokens），这些 token 被存储在 **KV Cache** 中以供后续 attention 使用。然而，这些中间 token 大部分对最终答案无直接贡献，却占用了巨大的显存（HBM）资源，导致：
- 内存开销剧增（例如 10GB+）
- 吞吐量下降
- 用户响应延迟升高

传统 KV Cache 压缩方法（如 SnapKV、H2O、StreamingLLM）基于 **token-uniform principle**，即平等对待所有输出 token，这在 CoT 场景下不适用——因为用户只关心 **final answer**。

### 🚀 提出的新方法与核心思想
本文提出 **Crystal-KV**，一个专为 CoT 推理设计的高效 KV Cache 管理框架，其核心是 **Answer-First Principle** —— 只保留那些真正有助于生成正确答案的 KV entries。

#### 主要创新点：

1. **首次识别并区分两种统一注意力模式（Unified Attention Patterns）**：
   - **CrystalKV**：间歇性被关注但在整个推理过程中持续影响最终答案的 KV entries。它们是“关键路径”上的记忆。
   - **SlipKV**：呈现流式注意力模式（streaming pattern），主要用于维持推理流程，但可能引入误导信息。

2. **Attention-based LRFU 算法**：
   - 基于 **Least Recently Frequently Used (LRFU)** 思想，结合 attention 分数动态计算每个 KV entry 的重要性（CRF Score）。
   - 不主动锁定 CrystalKV，而是通过演化视角将新来的 KV 视为 **PotentialKV**，逐步淘汰演变为 SlipKV 的条目，自然留下 CrystalKV。
   - 利用 Top-p 采样构建 hit mask，实现轻量级 attention 捕获。

3. **自适应缓存预算分配算法（Adaptive Budget Allocation）**：
   - 动态调整每层/每头的 KV Cache 预算。
   - 基于 CRF 得分估算各 layer/head 的 **cache utilization**，优先放大高利用率组件的预算，提升整体空间利用效率。

### 🔍 相比现有方法的优势
| 特性 | 传统方法（如 H2O/SnapKV） | R-KV / RaaS | Crystal-KV |
|------|--------------------------|-------------|------------|
| 是否遵循 Answer-First | ❌ | ❌（仍视为 LCG 特例） | ✅ |
| 是否区分 CrystalKV vs SlipKV | ❌ | ❌ | ✅ |
| 是否支持动态预算调整 | ❌ | ❌ | ✅ |
| 在低预算下能否保持甚至超越 FullKV 准确率 | ❌ | ❌ | ✅（可达 105% FullKV accuracy） |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **编程任务**：`CodeForces` benchmark（10K 竞赛级编程题），难度限制在 <1500 rating，避免准确率饱和或归零。
- **数学任务**：`MATH-500`（高级竞赛级数学题），涵盖代数、几何、组合等复杂推理场景。

### ⚙️ 模型与硬件平台
- **模型**：三个开源的 DeepSeek-R1-distilled 模型：
  - Llama-8B
  - Qwen-14B
  - Qwen-32B
- **硬件**：三块 NVIDIA RTX PRO 6000 Blackwell GPU
- **推理长度**：平均 9,350（8K-level） 和 18,700（16K-level）tokens

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Accuracy** | 平均正确率，采用 `k=8` 个样本投票，得分 = $\sum p_i$，其中 $p_i$ 是第 i 个答案是否正确 |
| **HBM Saving (%)** | 显存节省比例 |
| **Throughput (tok/s)** | 单位时间处理 token 数量 |
| **Batch Size (max)** | 最大并发 batch 数 |
| **Response Latency Speedup** | 用户级响应延迟加速比 |

### 🆚 基线方法对比
| 方法 | 类型 | 是否适配 CoT | 是否支持动态预算 |
|------|------|---------------|------------------|
| FullKV | 无压缩 | — | ❌ |
| StreamingLLM | LCG 压缩 | ❌ | ❌ |
| H2O | LCG 压缩 | ❌ | ❌ |
| SnapKV | LCG 压缩 | ❌ | ❌ |
| R-KV | CoT-oriented | ✅（但仍是 LCG 扩展） | ❌ |
| RaaS | CoT-oriented | ✅ | ❌ |
| **Crystal-KV.Lite** | 本文变体（关闭动态预算） | ✅ | ❌ |
| **Ada-SnapKV** | SnapKV + 动态预算（人工增强） | ✅ | ✅（仅此一个可比） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 指标 | Crystal-KV 表现 |
|------|----------------|
| **平均显存节省** | **90.89%** |
| **平均吞吐量提升** | **7.57×** |
| **最大响应延迟加速** | **1.24×** |
| **最低预算下准确率（vs FullKV）** | 达到甚至超过 FullKV（最高达 **105%**） |
| **最大并发 batch 提升** | 最高达 **8.07×**（8K-level） 和 **15.16×**（16K-level） |

### 🔬 详细对比结果（图4 & 表I）

#### ✅ 准确率优势（Fig. 4）
- 在相同 KV budget 下，Crystal-KV 显著优于所有 baseline：
  - **编程任务**：平均高出 7.30% ~ 18.98%
  - **数学任务**：平均高出 7.97% ~ 23.87%
- 存在一个“峰值点”：当 budget ≈ CrystalKV 总量时，准确率**超过 FullKV**，说明去除 SlipKV 可减少干扰。
- 自适应预算（adaptive allocation）进一步提升了极低预算下的表现。

#### 💾 效率与资源利用（Table I）
| 设置 | 方法 | Batch Max | Throughput (tok/s) | Tokens Concurrent |
|------|------|-----------|---------------------|--------------------|
| 8K-level | FullKV | 51 | 458.67 | 476,850 |
| 8K-level | Crystal-KV (fixed-1024) | **379** | **2,297.37** (+5.01×) | **3,543,650** (+7.43×) |
| 8K-level | Crystal-KV (ratio-10%) | **412** | **2,478.39** (+5.40×) | **3,852,200** (+8.07×) |
| 16K-level | FullKV | 25 | 174.95 | 467,500 |
| 16K-level | Crystal-KV (fixed-1024) | **379** | **2,141.44** (+12.24×) | **7,087,300** (+15.16×) |

> 💡 结论：随着序列增长，Crystal-KV 的优势愈发明显，尤其适合长链复杂推理。

### 🔁 消融实验与参数分析（Fig. 5）

- **Decay Rate λ**：
  - 最佳范围：代码任务 [0.6–0.7]，数学任务 [0.5–0.6]
  - λ 过小 → CrystalKV 因 attention gap 被误删
  - λ 过大 → SlipKV 被过度保留，挤占空间
- **Top-p threshold**：
  - 推荐使用 **top-p=0.9**，能更好保留关键上下文
  - p < 0.7 导致 attention 波动剧烈，性能不稳定

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Answer-First Principle 成立**：
   - 最终答案确实只依赖少数间歇性被关注的 KV entries（CrystalKV），而非全部 think-stage 内容。
   - 投影 answer-query 到 think-stage attention map 可有效识别 CrystalKV。

2. **SlipKV 可安全移除**：
   - 尽管 SlipKV 维持短期推理连贯性，但长期来看并非必要；清除后反而可能提高准确率（消除噪声）。

3. **动态预算分配显著增益**：
   - 不同 layer/head 对推理贡献异质性强，静态分配浪费严重。
   - 自适应机制可在极低总预算下最大化关键组件容量。

4. **极致压缩仍可 lossless 甚至 better-than-full**：
   - 仅用 **10% KV budget** 即可达到 FullKV 甚至更高准确率，打破“越多越好”的直觉。

### ⚠️ 局限性
- 当前方法依赖 attention score 近似（via Top-p），未考虑 query evolution 的非线性影响。
- 对极度分散的知识类任务（如某些数学题）效果略逊，因 CrystalKV 分布更广。
- 参数敏感性存在（λ 和 top-p），需合理调参。

### 🔮 未来工作方向
- 形式化建模 CrystalKV 与 SlipKV 的生成机制，界定 KV 压缩的能力边界。
- 探索训练阶段引导模型形成更紧凑的 CrystalKV 结构。
- 将该范式扩展至多跳检索、agent planning 等更复杂的 reasoning pipeline。

---

> ✅ **总结一句话**：  
> *Crystal-KV* 通过 **Answer-First Principle** 重新定义了 CoT 场景下的 KV Cache 管理范式，实现了 **超高压缩比 + 更高准确率 + 极大吞吐提升**，为大规模推理系统的高效部署提供了新路径。

</details>

---

### 8. [CondenseGraph: Communication-Efficient Distributed GNN Training via On-the-Fly Graph Condensation](https://arxiv.org/abs/2601.17774)

**Authors**: Zizhao Zhang, Yihan Xue, Haotian Zhu, Sijia Li, Zhijun Wang, Yujie Xiao  
**Category**: cs.DC  
**Published**: 2026-01-27  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.17774v1  

#### Abstract
Distributed Graph Neural Network (GNN) training suffers from substantial communication overhead due to the inherent neighborhood dependency in graph-structured data. This neighbor explosion problem requires workers to frequently exchange boundary node features across partitions, creating a communica...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：CondenseGraph: Communication-Efficient Distributed GNN Training via On-the-Fly Graph Condensation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
分布式 Graph Neural Network（GNN）训练面临严重的**通信开销瓶颈**，主要源于图数据中的邻域依赖性（neighborhood dependency）。在分布式环境下，图被划分到多个 worker 上，位于分区边界的节点（boundary nodes）需要频繁从远程 worker 获取邻居特征，导致跨节点通信量巨大，占训练总时间的 50–90%。这一“**邻居爆炸**”（neighbor explosion）问题严重限制了训练的可扩展性。

现有方法如静态图划分（如 METIS）无法适应动态网络条件，而通用梯度压缩技术未能充分利用图数据的结构特性。

---

### **提出的新方法与新思路**
论文提出了 **CondenseGraph**，一种面向分布式 GNN 训练的**通信高效框架**，其核心创新包括：

- **On-the-Fly Graph Condensation（运行时图压缩）**  
  在通信前，将边界节点的特征动态聚合成紧凑的“**super node**”（超节点），显著减少传输数据量。该过程是轻量级、实时执行的，不同于离线图蒸馏（graph distillation）方法。

- **Gradient-based Error Compensation（基于梯度的误差补偿机制）**  
  为缓解压缩带来的信息损失，引入误差累积器（error accumulator），将未成功传输的压缩误差反馈到后续迭代中，确保重要梯度信息不丢失，维持收敛性。

---

### **相比现有方法的优势**
| 方面 | CondenseGraph | 现有方法（如 METIS、AdaQP、BNS-GCN） |
|------|---------------|-------------------------------|
| **适应性** | 动态适应网络变化 | 静态策略，无法动态调整 |
| **结构利用** | 利用图同质性（homophily）进行特征压缩 | 通用压缩，忽略图结构 |
| **精度保持** | 通过误差反馈维持高精度 | 压缩易导致精度下降 |
| **兼容性** | 可与图划分、量化等方法结合 | 多为单一优化路径 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验在四个标准 benchmark 数据集上进行：

| 数据集 | 节点数 | 边数 | 特征维度 | 类别数 |
|--------|--------|------|----------|--------|
| **Reddit** | 232,965 | 114.6M | 602 | 41 |
| **ogbn-arxiv** | 169,343 | 1.17M | 128 | 40 |
| **ogbn-products** | 2.45M | 61.9M | 100 | 47 |
| **Flickr** | 89,250 | 899,756 | 500 | 7 |

> 注：Reddit 使用完整版本（约 1.14 亿条边），非简化版。

---

### **实验设置与评估指标**

- **实现平台**：基于 **DistDGL** 构建，使用 8 个 worker，每个配备 NVIDIA V100 GPU。
- **模型架构**：2-layer **GraphSAGE**，隐藏层维度 256。
- **压缩率**：默认设置为 50%（即通信量减少一半）。
- **评估指标**：
  - **通信量（Communication Volume）**
  - **训练时间分解**（通信 vs 计算）
  - **测试准确率（Test Accuracy）**
  - **收敛速度**（Convergence Curve）
  - **消融实验**（Ablation Study）

---

### **基线方法对比**
- **Baseline**：标准分布式 GNN 训练，全精度通信
- **AdaQP**：自适应量化 + 并行化
- **BNS-GCN**：边界节点采样方法

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **通信效率提升**
- 在所有数据集上实现 **40–60% 的通信量减少**。
- 在 `ogbn-products` 上，每 epoch 通信量从 **15.2 GB 降至 6.2 GB**（↓59%）。
- 通信时间占比从 **55% 降至 22%**，显著缓解通信瓶颈。

#### ✅ **模型精度保持**
| 方法 | Reddit | arxiv | products | Flickr |
|------|--------|-------|----------|--------|
| Baseline | 96.2% | 71.5% | 78.3% | 51.8% |
| **Ours (attention)** | **96.1%** | **71.4%** | **78.1%** | **51.6%** |

- 所有变体精度损失均 **< 0.5%**，优于 AdaQP 和 BNS-GCN。
- **Attention-based condensation** 表现最佳，尤其在异构度高的图上。

#### ✅ **收敛性分析**
- 收敛速度与 Baseline 几乎一致，验证理论分析。
- **无误差补偿时**，在 50% 压缩率下精度下降 **2–3%**，说明误差反馈机制至关重要。

---

### **消融实验结果**

| 实验项 | 发现 |
|--------|------|
| **压缩率影响** | 40–60% 区间内精度稳定（损失 <1%）；超过 60% 后性能明显下降 |
| **误差补偿作用** | 移除后精度下降 2.1%（ogbn-products），但存储开销可忽略（<1%） |
| **压缩方式比较** | <ul><li>**Mean**：最简单，精度略低</li><li>**Weighted**：考虑节点度，平衡性好</li><li>**Attention**：精度最高，计算开销 +5%</li></ul> |

---

## **4. 关键结论和发现**

### **主要结论**
1. **CondenseGraph 显著降低通信开销**（40–60%），同时几乎不牺牲模型精度（<0.5% 差距）。
2. **运行时图压缩 + 误差反馈机制** 是实现高效且稳定训练的关键。
3. 所提方法具有**良好通用性**，可与现有图划分、量化等技术结合使用。
4. 实验证明该框架在大规模图（如 ogbn-products）上仍能保持高效与稳定。

---

### **局限性**
1. **边界节点分组策略** 当前为启发式（基于结构与特征相似性），可能因数据集而异，缺乏统一最优策略。
2. 尚未扩展至 **heterogeneous graphs**（异构图），多类型节点的压缩更具挑战。
3. 不同 GNN 架构（如 GAT、GCN、GIN）对压缩的敏感性有待进一步研究。

---

### **未来工作方向**
1. 设计**可学习的边界节点分组策略**（learnable grouping）。
2. 扩展至 **dynamic graphs** 与 **heterogeneous graphs** 场景。
3. 探索与其他压缩技术（如量化、稀疏化）的联合优化。
4. 在真实云环境（带宽波动、延迟变化）中部署并评估自适应能力。

---

> **总结一句话**：  
> **CondenseGraph 通过运行时图压缩与误差反馈，在不牺牲精度的前提下，实现了高达 60% 的通信削减，为大规模分布式 GNN 训练提供了高效、实用的新范式。**

</details>

---

### 9. [A Universal Load Balancing Principle and Its Application to Large Language Model Serving](https://arxiv.org/abs/2601.17855)

**Authors**: Zixi Chen, Tianci Bu, Chendong Song, Xin Lu, Yinyu Ye, Zijie Zhou  
**Category**: cs.DC  
**Published**: 2026-01-27  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.17855v1  

#### Abstract
Load balancing-the allocation of work across parallel resources to reduce delay, energy and cost-is a pervasive challenge in science and engineering, from large-scale simulation and data processing to cloud and manufacturing operations. Motivated by the emerging bottleneck in large language model (L...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Universal Load Balancing Principle and Its Application to Large Language Model Serving

## 1. 论文的主要贡献和创新点

### 解决了什么问题
本文针对 **Large Language Model (LLM) Serving** 中一个日益严重的效率瓶颈——**Decode 阶段的负载不平衡问题**。

在现代 LLM 推理中，解码（decode）阶段采用 **Data Parallelism (DP)**，每个 GPU 工作节点维护其请求批次的 KV Cache。由于 **KV Cache 无法轻易迁移**（sticky assignment），且所有节点必须通过 **Barrier Synchronization** 等待最慢的节点完成，导致“straggler”问题严重。生产环境中的追踪数据显示，**Barrier-induced idle time 超过 40%**，造成了巨大的计算资源和能源浪费。

### 提出了什么新方法或新思路
论文提出了一个通用的负载均衡原则：**Balance Future with Integer Optimization (BF-IO)**。

该方法的核心思想是：
- **不依赖对任务总时长的精确预测**（这在 LLM 中极难实现）。
- 转而利用 **Short Lookahead** 信息，即预测活跃请求在未来几步内是否会完成。
- 在每一步分配新请求时，通过求解一个 **有限时间窗口内的整数优化问题 (Integer Optimization, IO)**，选择能使未来几步内系统总不平衡度最小的分配方案。

### 相比现有方法的优势
- **理论保证强**：在对抗性到达模式下，证明了 BF-IO 相对于 FCFS 等基线策略，能将长期不平衡度降低 $\Omega(\sqrt{B \log G})$ 倍，其中 $B$ 是批大小，$G$ 是 GPU 数量。这意味着**系统规模越大，收益越显著**。
- **普适性强**：该原则不仅适用于 LLM Serving，还被推广到一类更广泛的 **non-decreasing workload drift** 问题。
- **效果显著**：相比 Round-Robin、JSQ 等传统策略，BF-IO 能有效避免因请求长度差异导致的负载倾斜，从根本上解决 Barrier 同步下的效率损失。

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **公开数据集**：`BurstGPT` 数据集，该数据集捕捉了真实生产级别的 LLM 服务模式，特别是输出长度的重尾分布特征。
- **私有工业工作负载**：用于验证方法在实际部署环境中的有效性。

### 实验设置和评估指标
- **模拟器**：构建了一个基于 GPU 的请求处理模拟器，模拟在线调度场景。
- **配置**：默认使用 `G=32` 个 GPU，每个 GPU 批大小 `B=72`，揭示目标 `R=128`。
- **评估指标**：
  - **Average Imbalance**：衡量各 GPU 负载的平均不平衡度。
  - **Throughput**：每秒生成的 token 数量。
  - **Time Per Output Token (TPOT)**：用户感知的延迟，即每个输出 token 的平均耗时。
  - **Energy Consumption**：总能耗（焦耳），通过瞬时功耗对时间积分得到。功耗模型为 $P(mfu) = P_{idle} + (P_{max} - P_{idle}) \cdot (mfu / mfu_{sat})^\gamma$，其中 $P_{idle}=100W$, $P_{max}=400W$, $mfu_{sat}=0.45$, $\gamma=0.7$。

### 基线方法对比
- **FCFS (First-Come-First-Serve)**：先来先服务，作为主要基线。
- **JSQ (Join-Shortest-Queue)**：选择队列长度最短的 GPU，但队列长度不等于实际负载。
- **BF-IO (H=0)**：无前瞻窗口的 BF-IO，仅平衡当前步。
- **BF-IO (H=20)**：使用 20 步前瞻窗口的 BF-IO。

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
| Strategy | Avg Imbalance | Throughput (10⁴ tok/s) | TPOT (10⁻² s/tok) | Energy (kJ) |
| :--- | :--- | :--- | :--- | :--- |
| **FCFS** | 27.9 | 8.00 | 1.42 | 396 |
| **JSQ** | 28.2 | 7.99 | 1.42 | 396 |
| **BF-IO (H=0)** | 2.92 | 9.03 | 1.26 | 386 |
| **BF-IO (H=20)** | **1.65** | **9.13** | **1.25** | **383** |

- **负载不平衡度 (Imbalance)**：BF-IO (H=20) 将平均不平衡度从 27.9 降至 **1.65**，减少了 **17倍**。
- **吞吐量 (Throughput)**：相比 FCFS，BF-IO (H=20) 的吞吐量提升了 **14%**。
- **延迟 (TPOT)**：相比 FCFS，BF-IO (H=20) 的 TPOT 降低了 **13%**。
- **能耗 (Energy)**：相比 FCFS，BF-IO (H=20) 的总能耗从 396kJ 降至 383kJ，实现了 **3.3%** 的节能。

### 消融实验结果
- **H=0 vs H=20**：BF-IO (H=0) 已经取得了巨大提升（不平衡度降至 2.92）。引入 H=20 的前瞻窗口后，不平衡度进一步下降了 **44%**，并带来了吞吐量、延迟和能耗的持续优化。这证明了 **Short Lookahead 信息的有效性**。
- **JSQ 表现不佳**：JSQ 的表现几乎与 FCFS 相同，甚至略差。这是因为 JSQ 使用“队列长度”作为代理，而忽略了请求的实际 KV Cache 大小，因此无法有效平衡真实负载。

## 4. 关键结论和发现

### 主要发现
1. **Decode 阶段的负载不平衡是 LLM Serving 的主要效率瓶颈**，由 **sticky assignment** 和 **barrier synchronization** 共同导致。
2. **BF-IO 原则非常有效**：通过最小化短期未来负载不平衡的整数优化，可以显著提高系统效率。
3. **理论与实践一致**：理论证明的 $\Omega(\sqrt{B \log G})$ 改进因子在实验中得到了验证，且系统规模越大，相对收益越明显。
4. **节能潜力巨大**：即使在单个服务实例上节省几个百分点，考虑到 LLM 服务的庞大规模（如 OpenAI 每天处理 25 亿次请求），也能转化为巨大的能源和成本节约。

### 方法的局限性
- **集中式假设**：最强的理论保证建立在中央等待池（centralized waiting-pool）的假设上。一些服务引擎采用即时分发（instant-dispatch）到每个 GPU 的本地队列，这会削弱 BF-IO 的效果。
- **模型范围**：理论主要覆盖了 **non-decreasing workload drift** 过程。对于负载可能减少或高度非单调的系统（如 aggressive pruning 或自适应压缩），该理论框架尚不适用。
- **多目标优化**：现实部署需要同时优化多个目标（尾延迟、租户公平性、SLO 合规性、能耗上限）。BF-IO 主要针对 Barrier Idle Reduction，如何将其扩展到多目标优化仍需研究。

### 未来工作方向
1. **研究“即时分发”接口**：为路由到每个 GPU 的 FIFO 队列的架构开发完整的理论，并量化短视信号随激活延迟增长而退化的程度。
2. **扩展理论模型**：将最坏情况理论扩展到负载减少或高度非单调的系统，并刻画单调性何时是必需的。
3. **多目标服务优化**：将公平性和 SLO 约束集成到短视优化框架中，同时保持毫秒级的决策预算。
4. **探索更复杂的前瞻模型**：研究如何结合学习模型来提供更准确的短期完成预测，以进一步提升 BF-IO 的性能。

</details>

---

### 10. [Conformal Feedback Alignment: Quantifying Answer-Level Reliability for Robust LLM Alignment](https://arxiv.org/abs/2601.17329)

**Authors**: Tiejin Chen, Xiaoou Liu, Vishnu Nandam, Kuan-Ru Liou, Hua Wei  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.17329v1  

#### Abstract
Preference-based alignment like Reinforcement Learning from Human Feedback (RLHF) learns from pairwise preferences, yet the labels are often noisy and inconsistent. Existing uncertainty-aware approaches weight preferences, but ignore a more fundamental factor: the reliability of the \emph{answers} b...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Conformal Feedback Alignment: Quantifying Answer-Level Reliability for Robust LLM Alignment**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**
当前基于偏好的大语言模型（LLM）对齐方法（如 RLHF、DPO）依赖于成对偏好标签进行训练。然而，这些标签通常存在**噪声和不一致性**，尤其是在使用 AI 生成反馈（RLAIF）时更为严重。现有不确定性感知方法主要关注**偏好层面的不确定性**（即“这个偏好判断是否可靠”），却忽略了更根本的问题：**答案本身的可靠性**。

如果两个被比较的答案本身质量低、置信度差，那么无论怎样建模偏好不确定性，该比较所提供的学习信号都缺乏意义。因此，作者指出一个被忽视的关键维度——**answer-level reliability（答案级可靠性）**。

---

### 🚀 **提出了什么新方法或新思路**
提出 **Conformal Feedback Alignment (CFA)**，一种将 **Conformal Prediction (CP)** 引入 LLM 对齐的新框架，用于量化答案级别的可靠性，并将其转化为训练中的加权信号。

#### 核心思想：
- 利用 **Conformal Prediction** 构建具有统计保证的预测集（prediction sets），衡量每个模型输出的可靠性。
- 定义 **set-wise uncertainty aggregation function**，将两个答案所属的 conformal 集合映射为一个统一的不确定性权重 $ u \in [0,1] $。
- 将此权重应用于 **PPO-style 和 DPO-style** 的训练目标中，形成 **uncertainty-weighted loss**，使模型优先学习来自高置信度答案对的偏好。

#### 技术亮点：
- 支持 **black-box 和 white-box 设置**（分别使用不同 nonconformity score）。
- 第一次将 CP 应用于 AI-generated feedback 场景下的 LLM alignment。
- 权重机制实现了从“硬标签”到“软证据”的转变（soft evidence），提升鲁棒性。

---

### 🔍 **相比现有方法的优势**
| 维度 | 现有方法（如 WPO, IW-DPO） | CFA |
|------|----------------------------|-----|
| 不确定性建模层级 | Preference-level（偏好是否可信） | **Answer-level（答案本身是否可信）** |
| 数据源利用 | 忽略生成答案的质量差异 | 显式建模答案可靠性 |
| 泛化能力 | 多数依赖 reward model 或特定架构 | 适用于 DPO/PPO、黑白盒通用 |
| 学习效率 | 可能放大低质量反馈影响 | 自动抑制低可靠性比较的影响 |

> ✅ **优势总结**：CFA 在源头上过滤不可靠的学习信号，提升了对齐过程的数据效率与鲁棒性。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**
在三个标准偏好对齐数据集上进行验证：
1. **WebGPT Comparisons**：基于网页搜索的回答，强调事实性和信息完整性。
2. **Synthetic Instruct GPT-J Pairwise (Pairwise)**：合成指令遵循数据集，覆盖广泛任务。
3. **Summarize from Feedback (Summarize)**：Reddit 文章摘要任务，侧重内容压缩与语义保留。

---

### ⚙️ **实验设置**
- **模型**：
  - `Llama2-7B`, `Llama3.1-8B`, `Qwen2.5-7B`（主实验）
  - `Qwen3` 系列（0.6B–8B）用于可扩展性分析
- **训练流程**：
  1. 先进行 Supervised Fine-Tuning (SFT)
  2. 再应用 CFA 进行 preference-based alignment
- **CP 设置**：
  - 白盒（white-box）：使用 token-level log-probabilities 作为 nonconformity score
  - 黑盒（black-box）：通过采样频率、相似度等估计 score
  - 覆盖率设定：$ \alpha_1 = 0.5, \alpha_2 = 0.8 $
- **优化器**：AdamW，batch size=1，learning rate=1e-6
- **硬件**：4×NVIDIA A100 GPU

---

### 📊 **评估指标**
采用 **LLM-as-a-Judge** 方法，使用 **GPT-4o** 对生成结果打分，评估四个维度（每项满分100）：
1. **Accuracy (Acc)**：内容准确性
2. **Relevance (Rel)**：主题相关性
3. **Completeness (Comp)**：信息完整性
4. **Expression (Expr)**：表达清晰度

最终报告四项平均得分（Overall Score），prompt 见原文 Figure 5。

---

### 🔀 **基线方法对比**
| 类型 | 方法 |
|------|------|
| 基础训练 | SFT（仅监督微调） |
| 主流对齐 | PPO, DPO（无不确定性加权） |
| 不确定性感知方法 | WPO (Zhou et al., 2024), IW-DPO with `Direct Ask`, `p(true)` |
| 消融实验 | 不同 coverage 参数、黑白盒设置对比 |

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据（见 Table 1）**
在多个模型和数据集上，CFA 均显著优于 baseline：

| Model | Dataset | Base (DPO/PPO) | CFA | Improvement |
|-------|--------|----------------|-----|-------------|
| Llama2-7B | Summarize | 65.68 (DPO) / 65.88 (PPO) | **67.30** / **67.39** | **+1.62 / +1.51** |
| Llama2-7B | Pairwise | 90.88 (DPO) / 91.15 (PPO) | **92.12** / **91.89** | **+1.24 / +0.74** |
| Llama2-7B | WebGPT | 71.68 (DPO) / 72.25 (PPO) | **72.25** / **72.78** | **+0.57 / +0.53** |

> 💡 改进幅度接近甚至超过 base 方法相对于 SFT 的增益（例如 base DPO vs SFT 提升约 +1.77）

---

### 🆚 **与其他不确定性方法对比（RQ3）**
在 `Llama2-7B + Summarize` 上比较：
- **IW-DPO (Direct Ask)**：表现 **低于 base DPO** → 表明 LLM 自我评估的置信度不可靠
- **IW-DPO (p(true))** 和 **WPO**：略有提升，但仍 **不如 CFA**
- **CFA 明显胜出** → 说明 answer-level reliability 更具判别力

> 图4显示：CFA 实现最高 overall score（~67.3），远超其他方法。

---

### 🔍 **消融实验结果**

#### （1）**不同模型规模的影响（Figure 2）**
- 在 Qwen3 系列（0.6B–8B）上测试 CFA + DPO
- 发现：**小模型收益更大** → 表明 CFA 特别有助于资源受限场景下的对齐

#### （2）**不同训练样本量的影响（Figure 3）**
- 使用 Llama2-7B，在 Summarize 数据集中抽取 5%~100% 数据
- 结果：即使在 **5% 数据下，CFA 仍优于 base DPO**
- 随着数据增加，性能差距进一步拉大 → 展现出优秀的 **data efficiency**

#### （3）**黑白盒 CP 对比（Figure 7）**
- 白盒 CP 效果更好（68.04 vs 67.40）→ 因其能更精确捕捉生成概率分布
- 但黑盒也有效 → 证明 CFA 可部署于 API 模型（如 GPT-4）

#### （4）**不同 coverage 参数影响（Figure 8）**
- 最佳 performance 出现在 $ \alpha_2 = 0.7–0.8 $
- 当 $ \alpha_2 = 0.6 $（太接近 $ \alpha_1 = 0.5 $）时效果下降 → 区分度不足导致权重失效
- 但所有设置下 CFA 均优于 base → 方法稳健

---

### 🏆 **胜率对比（Win Rate, Table 3）**
使用 GPT-4o 判断 CFA 与 Base DPO 输出孰优：

| Model       | Win Rate (CFA wins) |
|------------|--------------------|
| Llama2-7B  | 56.18%             |
| Llama3.1-8B| 64.33%             |
| Qwen2.5-7B | 57.61%             |

> ✅ 所有模型上 CFA 均取得多数胜利，尤其在更大模型上优势更明显。

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **Answer-level reliability 是影响对齐质量的关键因素**，不应被忽略。
2. **CFA 成功将 Conformal Prediction 引入 preference learning**，提供了一种 principled 的方式来量化并利用答案可靠性。
3. CFA 在多种模型、数据集、训练范式（DPO/PPO）、访问模式（黑白盒）下均带来稳定增益。
4. 方法特别适合 **低数据、小模型、高噪声反馈（如 RLAIF）** 场景，显著提升 **data efficiency 和 robustness**。
5. **DPO + CFA 效果优于 PPO + CFA** → 推测因 PPO 中 reward model 仅为中间组件，难以完全传递可靠性信号。

---

### ⚠️ **局限性**
1. **依赖 calibration set 的质量**：当校准集较小时，CP 的 coverage 保证可能失效。
2. **未结合 preference-level 与 answer-level 不确定性**：目前仅建模前者或后者，未来可联合建模。
3. **局限于文本模态**：尚未扩展至多模态 LLM（如图文、语音）。
4. **计算开销增加**：需额外生成 conformal 集合并计算 nonconformity scores。

---

### 🔮 **未来工作方向**
1. **联合建模 answer-level 与 preference-level uncertainty**
2. **扩展至 multi-modal feedback alignment**
3. **引入 human-in-the-loop 进行动态 calibration**
4. **探索更高效的 black-box CP approximation 方法**
5. **应用于 real-world 在线对齐系统（online RLHF）**

---

## ✅ 总结一句话
> **CFA 首次将 Conformal Prediction 用于量化 LLM 对齐中的 answer-level reliability，通过构建统计可靠的预测集并加权偏好损失，在多种设置下实现了更鲁棒、更高效的大模型对齐，实验证明其显著优于传统及现有不确定性感知方法。**

</details>

---

### 11. [AttenMIA: LLM Membership Inference Attack through Attention Signals](https://arxiv.org/abs/2601.18110)

**Authors**: Pedram Zaree, Md Abdullah Al Mamun, Yue Dong, Ihsen Alouani, Nael Abu-Ghazaleh  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.18110v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed to enable or improve a multitude of real-world applications. Given the large size of their training data sets, their tendency to memorize training data raises serious privacy and intellectual property concerns. A key threat is the membership inf...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*AttenMIA: LLM Membership Inference Attack through Attention Signals*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在训练过程中可能**过度记忆（memorize）其训练数据**，这带来了严重的隐私和知识产权风险。**成员推断攻击（Membership Inference Attack, MIA）** 是一种关键威胁，旨在判断某个样本是否属于模型的训练集。现有的 MIA 方法主要依赖于输出置信度（如 perplexity、log-likelihood）或嵌入特征，但这些信号往往**脆弱且泛化能力差**，尤其是在低误报率（low-FPR）的关键场景下表现不佳。

### 提出的新方法与新思路
本文提出了 **AttenMIA**，这是首个系统性地利用 **Transformer 模型内部自注意力（self-attention）信号** 进行 LLM 成员推断的白盒（white-box）框架。

其核心创新点在于：
- **从注意力机制中提取特征**：认为注意力模式（如集中度、稳定性、跨层一致性）能反映模型对训练样本的“熟悉度”。
- **设计两类互补的注意力特征**：
  1. **Transitional Features（过渡特征）**：量化注意力模式在不同层（layers）和头（heads）之间的变化，例如相关性（Corr）、Frobenius 距离、KL 散度和质心漂移（barycenter drift）。
  2. **Perturbation-Based Features（扰动特征）**：通过主动修改输入（如删除、替换 token 或添加非成员前缀），观察注意力分布的变化（KL shift）。假设成员样本被扰动后会从“成员”变为“非成员”，导致注意力模式发生更大偏移。
- **结合多头注意力结构**：充分利用 Transformer 的多头并行结构，聚合来自多个 head 和 layer 的细粒度信号，形成更丰富、更具判别力的特征向量。

### 相比现有方法的优势
- **更高的准确性和鲁棒性**：在多个基准上显著超越基于输出置信度的基线方法。
- **在低误报率（low-FPR）下表现卓越**：这对于实际的隐私审计至关重要，因为高误报率会导致大量无效警报。
- **更强的泛化能力**：注意力信号在不同模型架构（LLaMA, Pythia, OPT）和数据集间表现出良好的可迁移性，而许多输出基线需要针对特定设置进行校准。
- **无需影子模型（shadow models）**：是参考无关（reference-free）的方法，降低了计算开销。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **WikiMIA**：一个标准的 MIA 基准，提供已知成员/非成员标签的维基百科文本。
- **MIMIR**：另一个广泛使用的 MIA 数据集，包含多个子集（如 arXiv, GitHub, PubMed, HackerNews 等），用于评估方法在不同领域的泛化能力。

### 实验设置和评估指标
- **目标模型**：在多个开源 LLM 上进行评估，包括 **LLaMA-2** (13B, 30B), **Pythia** (1.4B, 6.9B), **OPT** (6.7B, 66B), **NeoX-20B**。
- **威胁模型**：**白盒攻击**，攻击者可以访问模型的所有内部状态（特别是所有层和头的注意力权重矩阵），但**不访问原始训练数据或优化器状态**。
- **评估指标**：
  - **ROC AUC**：衡量整体的分类区分能力。
  - **TPR@1%FPR**：在 1% 的假阳性率下的真阳性率，这是隐私审计中最关键的指标，强调在极低误报前提下的检测能力。

### 基线方法对比
论文将 AttenMIA 与多种先进的 MIA 基线进行了比较：
- **PPL / Loss**：基于困惑度或损失值。
- **Zlib**：基于压缩熵。
- **Min-K% / Min-K%++**：基于最低预测概率。
- **Neighbor / PETAL**：基于邻近 token 的语义相似性。
- **RECALL**：基于前缀扰动后的条件似然变化。
- **LiRA / Reference**：基于影子模型或参考模型的方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在 **WikiMIA-32** 基准上，使用 **Llama2-13b** 模型：
  - **AttenMIA (Perturbed)** 达到了 **0.996 ROC AUC** 和 **87.9% TPR@1%FPR**，创下了该基准上的新纪录。
- 在 **MIMIR** 基准上，平均性能：
  - AttenMIA 将 ROC AUC 相比 RECALL 提升了 **+55%**，相比 PETAL 提升了 **+27%**。
  - 在 TPR@1%FPR 上，AttenMIA 平均达到 **47.8%**，远超 RECALL (19.6%) 和 PETAL (13.3%)。

### 与基线方法的对比结果
- **全面超越**：在所有测试的模型、数据集和序列长度上，AttenMIA 的两种变体（Transitional 和 Perturbed）在 ROC AUC 和 TPR@1%FPR 上均**一致地优于所有基线方法**。
- **优势显著**：特别是在 **TPR@1%FPR** 指标上，AttenMIA 的优势最为明显，证明了其在实际隐私审计中的巨大潜力。
- **消融实验**：通过 **Hellinger Distance (HD)** 分析，证明了 AttenMIA 的单个注意力特征（如 Consistency-Corr, Consistency-KL）的 HD 值（~0.70）远高于最佳基线（~0.56），表明其特征具有更强的分离性。

### 消融实验结果
- **扰动策略的影响**：单独使用 token dropping、replacement 或 prefix insertion 都能取得良好效果，但**组合使用所有策略**能带来最佳性能，说明它们捕捉了互补的信号。
- **层数的影响**：分析表明，**每一层都包含有用的成员信息**，但性能随层数增加而提升。聚合所有层的特征（All Layers）比仅用单层或分组层效果更好，说明记忆是分布在全网络的。
- **序列长度的影响**：较短的序列（如 32 tokens）通常能产生更强的成员信号，随着序列增长，性能有所下降，但 AttenMIA 仍保持领先。
- **防御有效性**：测试了**训练数据去重（deduplication）** 这一常见防御手段，发现其对 AttenMIA 的攻击性能影响**微乎其微**（AUC 变化 < 0.03），表明该防御不足以抵御基于注意力的高级攻击。

---

## 4. 关键结论和发现

### 主要发现
1. **注意力是强大的隐私泄露源**：Transformer 的自注意力机制不仅是为了解释模型行为，也**无意中放大了隐私风险**。其内部动态（如集中度、稳定性和对扰动的敏感性）是判断成员身份的强大信号。
2. **AttenMIA 性能卓越**：所提出的框架在多个维度上显著超越现有技术，尤其是在低误报率这一关键指标上，证明了其作为先进隐私审计工具的有效性。
3. **记忆是分布式且稳健的**：成员信息并非局限于特定层，而是分布在模型的多个层和头中。此外，简单的去重等防御措施无法有效缓解这种基于内部状态的攻击。
4. **可用于增强数据提取**：将 AttenMIA 用于训练数据提取框架时，其生成的排名与真实记忆内容的相似度（ROUGE-L）的相关性（Pearson r = 0.48）远超基于困惑度等的传统指标（最高 r = 0.32），证明了其在实际攻击中的实用性。

### 方法的局限性
- **白盒假设**：AttenMIA 需要完全访问模型的内部注意力状态，这在商业闭源模型（如 GPT-4）中难以实现。其在黑盒或API访问场景下的适用性有待研究。
- **计算开销**：虽然比训练影子模型便宜，但提取和处理所有层和头的注意力特征仍然比简单的输出统计方法更耗资源。
- **通用性边界**：尽管在多种架构上表现良好，但其在非 Transformer 架构或其他模态（如视觉）模型上的有效性未知。

### 未来工作方向
- **开发针对性的防御机制**：鉴于注意力是新的隐私泄露通道，需要研究如何在不影响模型性能的前提下，对注意力机制进行正则化或扰动以保护隐私（例如，注意力掩码、差分隐私注入）。
- **探索黑盒版本**：研究如何通过 API 查询的输出间接推断注意力模式，或将 AttenMIA 的洞见迁移到黑盒攻击中。
- **理解记忆的根源**：深入分析为何某些注意力头和层对成员信息更敏感，从而更好地理解 LLM 的记忆机制。
- **负责任的审计工具**：将 AttenMIA 发展为开发者可用的工具，用于在模型发布前主动检测和减轻潜在的记忆风险。

</details>

---

### 12. [Elastic Attention: Test-time Adaptive Sparsity Ratios for Efficient Transformers](https://arxiv.org/abs/2601.17367)

**Authors**: Zecheng Tang, Quantong Qiu, Yi Yang, Zhiyi Hong, Haiya Xiang, Kebin Liu, Qingqing Dang, Juntao Li, Min Zhang  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.17367v1  

#### Abstract
The quadratic complexity of standard attention mechanisms poses a significant scalability bottleneck for large language models (LLMs) in long-context scenarios. While hybrid attention strategies that combine sparse and full attention within a single model offer a viable solution, they typically empl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Elastic Attention: Test-time Adaptive Sparsity Ratios for Efficient Transformers

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

标准的 **Full Attention (FA)** 在长上下文场景下具有 $O(n^2)$ 的计算复杂度，严重限制了大语言模型（LLMs）在处理长序列时的可扩展性。虽然现有的 **hybrid attention** 方法通过结合稀疏注意力（Sparse Attention, SA）和全注意力来缓解这一问题，但它们通常采用**静态的稀疏比例**（即固定的 SA 与 FA 头数比例），无法在推理阶段根据任务动态调整。

这导致：
- 对于**稀疏敏感任务**（如问答），过度稀疏会显著降低性能；
- 对于**稀疏鲁棒任务**（如摘要），不必要的全注意力则浪费计算资源。

因此，如何在不牺牲性能的前提下，实现**测试时自适应的稀疏策略**，是当前的关键挑战。

---

### ✅ 提出了什么新方法或新思路

本文提出 **Elastic Attention**，一种**测试时自适应稀疏比率**的方法，其核心思想是：

> **让模型在推理前自动识别任务类型，并据此动态分配注意力头的计算模式（FA 或 SA）。**

具体创新点如下：

1. **引入轻量级的 Attention Router 模块**  
   - 插入到预训练模型中，**不修改主干参数**（backbone-freeze）。
   - 接收输入的 Key 隐藏状态，输出每个 attention head 应使用的计算模式（FA 或 SA）。

2. **基于 Mixture-of-Experts (MoE) 的路由机制**  
   - 类似 MoE 的门控机制，对每个 head 进行二分类决策。
   - 使用 **Gumbel-Softmax + STE (Straight-Through Estimator)** 实现端到端训练，解决离散路由带来的梯度不可导问题。

3. **区分两类任务以简化配置空间**  
   - 将下游任务划分为两类：
     - **Sparsity-Robust Tasks**（如 Summarization）：可容忍高稀疏。
     - **Sparsity-Sensitive Tasks**（如 QA）：需保留更多 FA 头。
   - 模型只需判断任务类别并分配相应稀疏水平，而非为每项任务单独调参。

4. **高效的融合内核（Fused Kernel）设计**  
   - 支持在同一层中混合执行 FA 和 SA head，避免多次 kernel launch 和内存拷贝，提升推理效率。

---

### ✅ 相比现有方法的优势

| 维度 | Elastic Attention | 传统 Hybrid 方法（如 DuoAttention, PruLong） |
|------|-------------------|---------------------------------------------|
| **稀疏策略** | 动态、输入感知（test-time adaptive） | 静态、固定比例（static ratio） |
| **泛化能力** | 自动适配不同任务类型 | 需针对每个任务手动调优 |
| **部署灵活性** | 无需重新训练即可应对新任务 | 新任务可能需要重新微调 |
| **参数开销** | 极低（每层仅增加约 0.27M 参数） | 通常需更新部分主干参数 |
| **训练成本** | 仅需 12 小时（8×A800） | 类似或更高 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **LongBench-E** (Bai et al., 2024)：真实世界长上下文基准，涵盖 6 类任务：
  - 单文档 QA（S-Doc QA）
  - 多跳 QA（M-Doc QA）
  - 摘要（Summarization）
  - 上下文学习（In-Context Learning）
  - 合成检索（Synthetic）
  - 代码补全（Code）

- **RULER** (Hsieh et al., 2024)：用于评估**长度外推能力**，测试从 8K 到 256K tokens 不等的上下文长度。

- **LongBench-v2** (Bai et al., 2025)：更复杂的长上下文推理基准，包含高达 2M words 的上下文。

- **训练数据**：来自 ChatQA2-Long-SFT-data、MuSiQue、CoLT-132K、GovReport、XSum，覆盖稀疏敏感与鲁棒任务，总 token 数约 0.74B。

---

### ⚙️ 实验设置和评估指标

#### 主干模型（Backbone Models）
- Qwen3-4B / Qwen3-8B
- Llama-3.1-8B-Instruct

#### 稀疏注意力模式（SA Patterns）
- **Streaming Sparse Attention (SSA)** (Xiao et al., 2024b)
- **XAttention (XA)** (Xu et al., 2025)

#### 评估指标
- **Perf.**：各项任务的平均得分（越高越好）
- **ΩMsR (Model Sparsity Ratio)**：使用 SA 模式的 attention head 比例（越高越高效）
- **ΩEsR (Effective Sparsity Ratio)**：实际关注 token 的比例（反映有效稀疏程度）
- **Speedup**：推理加速比
- **Avg. Perf.**：跨任务平均性能

#### 训练细节
- 使用 8×A800 GPU，训练时间 ≤12 小时
- Backbone 参数冻结，仅训练 **Attention Router**
- 使用 **Gumbel-Softmax + STE** 进行离散路由训练
- 优化目标包含语言建模损失 + 稀疏正则项（带可学习 Lagrange multiplier）

---

### 🔍 基线方法对比

| 基线方法 | 特点 |
|--------|------|
| **InfLLM-V2** | 密集-稀疏切换机制，依赖上下文长度阈值 |
| **DuoAttention** | 固定比例的 FA + SSA 混合头 |
| **PruLong** | 学习重要 token 并保留 |
| **MoBA / NSA** | 基于 block 的稀疏注意力，有硬件约束 |
| **XAttention** | 无训练的启发式稀疏模式 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & Table 2）

#### 在 **LongBench-E** 上的表现（Qwen3-8B 主干）

| 方法 | Avg. Perf. | ΩMsR |
|------|-----------|-------|
| Qwen3-8B (Full FA) | 52.16 | – |
| + InfLLM-V2 | 49.03 | – |
| + PruLong | 48.66 | 0.70 |
| + Elastic Attention (FA-SSA) | **51.51** | 0.69 |
| + Elastic Attention (FA-XA) | **51.66** | 0.80 |

✅ **结论**：Elastic Attention 在保持甚至超越多数基线性能的同时，实现了更高的稀疏率（ΩMsR 达 0.8），说明其能智能地节省计算。

---

#### 在 **RULER** 上的长度外推表现（最长达 256K tokens）

| 方法 | 256K 性能 | 加速比 |
|------|----------|--------|
| MoBA / NSA | 几乎崩溃（接近 0） | <1.0× |
| InfLLM-V2 | 内存溢出（OOM） | – |
| **Elastic Attention (FA-XA)** | **68.51** | **1.51×** |
| **Elastic Attention (XA-SSA)** | **47.68** | **3.28×** |

✅ **结论**：Elastic Attention 显著优于基线，在极端长度下仍保持可用性，并且可通过配置（FA-XA vs XA-SSA）灵活权衡**精度与速度**。

---

#### 在 **LongBench-v2** 上的结果

| 方法 | Easy | Hard | Avg. Perf. | ΩMsR |
|------|------|------|------------|-------|
| Qwen3-8B | 39.33 | 27.82 | 31.97 | – |
| + Elastic Attention (FA-SSA) | 37.33 | **31.20** | **33.41** | 0.66 |
| + Elastic Attention (FA-XA) | **30.68** | 30.77 | 30.74 | 0.78 |

✅ **结论**：尤其在 Hard 设置下，Elastic Attention 明显优于原始模型和其他基线，证明其在复杂推理任务中的有效性。

---

### 🔬 消融实验结果（Ablation Study）

#### （1）Target Sparsity $t$ 的影响（Figure 7）

- 设定 sparsity-sensitive 任务的目标稀疏度 $t_{sen}=0.7$
- 结果显示模型能自动学习差异化分配：
  - QA 类任务 $\Omega_{MsR} \approx 0.68$
  - Code 类任务 $\Omega_{MsR} \approx 0.85$
- 表明模型成功识别了任务敏感性差异。

#### （2）Attention Router 设计分析（Table 3）

| MLP 隐藏层大小 | Avg. Perf. |
|---------------|-----------|
| 2×d’ | 45.22 |
| 4×d’ (**默认**) | **45.92** |
| 8×d’ | 46.40（略高但参数翻倍） |

➡️ 默认设置已在性能与参数间取得良好平衡。

#### （3）路由行为可视化（Figure 6）

- 发现某些 head 始终被分配为 FA（类似 retrieval heads）
- 部分 head 根据任务动态切换
- 验证了“固定 retrieval head + 动态稀疏”机制的有效性。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **任务天然可分为稀疏鲁棒与稀疏敏感两类**  
   - 只需区分这两类即可实现有效的稀疏控制，无需为每个任务单独调参。

2. **Elastic Attention 能在测试时动态调整稀疏比例**  
   - 通过轻量级 **Attention Router** 实现输入感知的 head 分配。
   - 在推理前 Prefill 阶段完成决策，不影响生成效率。

3. **性能与效率兼得**  
   - 在多个长上下文基准上达到或超过基线性能。
   - 实现高达 3.28× 的推理加速（XA-SSA 配置）。
   - 适用于不同规模模型（4B~8B）和多种 SA 模式（SSA/XA）。

4. **极低训练成本与部署友好**  
   - 仅需 12 小时训练，不修改主干参数。
   - 融合内核设计消除调度开销，适合生产环境。

---

### ⚠️ 方法的局限性

1. **依赖于 retrieval head 的存在假设**  
   - 方法假设模型中存在可用于全局信息提取的 FA head，若主干模型本身不具备此特性，效果可能受限。

2. **对输入截断敏感**  
   - 当前采用边界 pooling（首尾各取 100 token）作为任务表示，若关键指令不在边界区域，可能导致误判。

3. **目前仅支持单层统一路由决策**  
   - 所有层共享相同的 ΩMsR 控制逻辑，未探索逐层差异化路由的可能性。

4. **尚未验证在多模态或非文本任务上的泛化性**  
   - 当前实验集中在纯文本长上下文任务。

---

### 🔮 未来工作方向

1. **扩展至多粒度路由机制**  
   - 如支持按层、按位置、按 token 动态选择稀疏模式。

2. **探索完全免训练的弹性路由**  
   - 利用 prompt engineering 或 probing 技术替代轻量训练。

3. **应用于 MoE 或 State Space Models (SSMs)**  
   - 将 elastic routing 思想迁移到其他高效架构中。

4. **构建通用的 Sparsity Controller API**  
   - 提供可插拔模块，支持任意 LLM 快速集成。

5. **研究稀疏模式与模型幻觉的关系**  
   - 探索是否合理的稀疏策略可以抑制长上下文中因信息丢失引发的错误推理。

---

> **一句话总结**：  
> Elastic Attention 提出了一种**轻量、高效、自适应**的稀疏注意力框架，通过一个可训练的 **Attention Router** 实现测试时动态稀疏控制，在几乎零主干改动的情况下，显著提升了长上下文 LLM 的推理效率与泛化能力。

</details>

---

### 13. [Reflect: Transparent Principle-Guided Reasoning for Constitutional Alignment at Scale](https://arxiv.org/abs/2601.18730)

**Authors**: Henry Bell, Caroline Zhang, Mohammed Mobasserul Haque, Dhaval Potdar, Samia Zaman, Brandon Fain  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18730v1  

#### Abstract
The constitutional framework of alignment aims to align large language models (LLMs) with value-laden principles written in natural language (such as to avoid using biased language). Prior work has focused on parameter fine-tuning techniques, such as reinforcement learning from human feedback (RLHF)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Reflect: Transparent Principle-Guided Reasoning for Constitutional Alignment at Scale*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **alignment** 技术（如 RLHF、DPO）依赖于对模型参数进行微调（parameter fine-tuning），存在以下局限性：
- **计算成本高**：需要大量训练资源。
- **依赖标注数据**：需收集人类或 AI 生成的偏好数据，难以获取且成本高昂。
- **缺乏灵活性**：难以快速适配新的、特定场景下的价值原则（例如不同文化、领域或用户群体的需求）。
- **黑箱化严重**：基于奖励模型的方法缺乏透明度，难以解释为何某个输出被判定为“对齐”。

因此，论文旨在解决如何在**不进行任何参数微调、无需额外训练数据**的前提下，实现对大语言模型（LLM）行为的动态、可解释、多原则的对齐控制。

### 提出的新方法：REFLECT
作者提出 **REFLECT** —— 一种完全在推理时（inference-time）执行的、基于自然语言原则引导的透明推理框架。其核心流程分为四个阶段：

1. **Constitution-Conditioned Base Response (初始响应)**  
   在系统提示中传入一组用自然语言书写的 **principles**（构成一个“宪法” Constitution），指导模型生成初步回应。

2. **Self-Evaluation (自我评估)**  
   模型对自身生成的响应逐条评估是否符合每项原则，并给出 1–5 分的 Likert 评分。

3. **Self-Critique (自我批判)**  
   对得分低于阈值的原则进行深入分析，指出具体违反之处及改进建议。

4. **Final Revision (最终修订)**  
   基于批判内容生成改进后的最终输出。

该过程完全通过 **in-context reasoning** 完成，无需修改模型权重。

### 相比现有方法的优势
| 维度 | 传统方法（如 RLHF/DPO） | REFLECT |
|------|------------------------|-------|
| 是否需要训练 | ✅ 需要参数微调 | ❌ 不需要，纯推理时操作 |
| 是否需要标注数据 | ✅ 需要偏好数据 | ❌ 只需提供原则文本 |
| 灵活性 | 低（固定对齐目标） | 高（可即插即用切换不同宪法） |
| 透明性 | 低（黑箱奖励模型） | 高（显式自然语言推理轨迹） |
| 尾部安全表现 | 平均表现好，尾部风险仍存 | 显著降低严重违规率（tail-case violations） |
| 数据复用能力 | —— | ✅ 自动生成高质量 SFT/DPO 训练数据 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **PKU-Safe-RLHF Dataset**  
  包含 500 个单轮对抗性提示（red-teaming prompts），用于测试安全性对齐。
- **Anthropic HH-RLHF Dataset**  
  包含多轮对话提示，共 500 条，用于评估通用对齐能力。
- **LMSYS-Chat-1M Dataset**  
  从中采样 2000 条真实聊天记录，用于模拟部署并生成自监督微调数据。

### 实验设置
- **模型家族**：
  - 商业先进模型：`GPT-4.1-Mini`, `Claude-Haiku-3.5`
  - 开源较小模型：`Mistral-7B-Instruct-v0.3`
- **评估方式**：采用 **LLM-as-a-judge** 范式，使用更强的 `GPT-4.1` 作为裁判模型，对每个输出按每条 principle 打分（1–5 分）。
- **二元编码**：将得分 1–2 视为“违反原则”，3–5 视为“未违反”。
- **人工验证**：招募 50 名 Prolific 用户进行人工打分，验证 LLM 判官的一致性（human-human MAD ≈ 0.94–1.09；LLM-human MAD ≈ 1.03–1.71）。

### 基线方法对比
- **Baseline**：仅使用 constitution-conditioned prompting（CCBase），无后续反思机制。
- **REFLECT**：完整四步流程。
- **CA_SELFREFINE**：对 Self-Refine 方法进行改造以支持宪法原则，作为替代 self-correction 方法的对照。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）
| Dataset | Model | Avg. Likert Score (CCBase → REFLECT) | Principle Violation Rate ↓ |
|--------|--------|-------------------------------|--------------------------|
| SafeRLHF | GPT-4.1-Mini | 4.55 → **4.65** (+0.098) | 7.7% → **3.45%** (-4.27pp) |
| SafeRLHF | Claude-Haiku-3.5 | 2.89 → **4.15** (+1.26) | 47.3% → **13.3%** (-34.0pp) |
| HH-RLHF | GPT-4.1-Mini | 4.22 → **4.60** (+0.37) | 10.4% → **2.0%** (-8.36pp) |

> 注：所有提升均经 **paired bootstrap test** 验证，p < 0.0001，统计显著。

### 与基线方法对比
- **相比 CCBase（仅提示工程）**：
  - 在所有模型和数据集上均显著提升平均对齐分数。
  - 特别是在原本对齐较差的模型（如 Claude）上效果最明显。
  - 大幅减少“严重违反原则”的案例（tail-case mitigation）。
- **相比 CA_SELFREFINE**（见 Table 5）：
  - 实现相近甚至更好的对齐效果。
  - **Token 开销仅为后者的 1/3 至 1/10**（REFLECT: 3.7–5.0× baseline；CA_SELFREFINE: 28.2–58.2×）。
  - 更高效地跳过已良好对齐的响应（通过 self-evaluation 提前终止）。

### 消融实验结果
#### （1）跨模型规模有效性（Section 4.5）
在 GPT-4.1 系列不同尺寸模型上的测试表明：
- 即使是小型模型（如 GPT-4.1-Nano），REFLECT 也能带来显著提升（+0.51 分）。
- 表明 REFLECT 对模型大小鲁棒，小模型也可受益。

#### （2）事实推理保留能力（Section 4.4）
在 **GSM8K** 和 **MMLU** 上测试 REFLECT 对事实推理的影响：
| Benchmark | Base | CCBase | REFLECT |
|---------|------|--------|--------|
| GSM8K | 94.69% | 95.00% | **95.07%** |
| MMLU (test) | 87.60% | 85.80% | **85.80%** |

> 结论：REFLECT **不影响甚至轻微提升** 事实准确性，说明其不会因强调原则而牺牲任务性能。

#### （3）生成训练数据的有效性（Section 4.3）
利用 REFLECT 过程中产生的 `(base response, revised response)` 对进行微调：
- **SFT 微调模型**：在新数据上达到平均 **4.60** 分，接近完整 REFLECT 流程的表现。
- **DPO 微调模型**：也有一定提升，但不如 SFT 明显。
- 证明 REFLECT 可作为**低成本数据引擎**，推动长期部署中的迭代优化。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **REFLECT 显著提升多原则对齐水平**，尤其在原本未针对这些原则微调过的模型上效果突出。
2. ✅ **特别擅长缓解尾部风险**：即使平均得分变化不大，也能大幅降低严重违规的发生频率，这对安全关键场景至关重要。
3. ✅ **保持事实推理能力不变**：引入复杂原则不会损害模型的基础认知能力。
4. ✅ **具备良好的泛化性和效率**：适用于不同规模模型，且计算开销可控。
5. ✅ **天然生成高质量训练数据**：形成“推理时对齐 → 数据产出 → 参数微调 → 更强基础模型”的正向反馈循环。
6. ❌ **无法用于“越狱”或“去对齐”**：尝试用 “harmfulness” 原则也无法让模型真正有害，说明 REFLECT 本身具有内在稳定性。

### 方法的局限性
- **依赖模型自身的判断力**：self-evaluation 步骤可能误判（false positives/negatives），影响修订质量。
- **不能保证完美合规**：仍有可能在修订过程中引入新的原则违反。
- **评估依赖 LLM Judge**：虽然经过人工校准，但仍非绝对客观标准。
- **仅测试有限原则集合**：尚未探索极端对抗性或模糊伦理情境下的表现。
- **推理链可能误导**：生成的 reasoning trace 可能是 post-hoc rationalization，而非真实决策路径。

### 未来工作方向
1. 设计更可靠的 self-evaluation 机制，减少误判。
2. 探索如何训练专门用于 critique 和 revision 的 reasoning model。
3. 研究 REFLECT 在对抗攻击（adversarial jailbreak）下的鲁棒性。
4. 扩展至更多元的文化价值观空间，支持真正的 pluralistic alignment。
5. 构建端到端的“对齐闭环系统”：将 REFLECT 输出持续用于离线微调，实现自动化演进。

--- 

> **总结一句话**：  
> REFLECT 是一种轻量级、透明、无需训练的推理时对齐框架，它通过显式的 in-context 自我反思机制，在不牺牲事实准确性的前提下，显著提升了 LLM 对复杂多元原则的遵循程度，尤其有效抑制了罕见但严重的对齐失败，同时还能反哺训练数据生产，为构建可持续进化的负责任 AI 提供了一条新路径。

</details>

---

### 14. [FlashMoE: Reducing SSD I/O Bottlenecks via ML-Based Cache Replacement for Mixture-of-Experts Inference on Edge Devices](https://arxiv.org/abs/2601.17063)

**Authors**: Byeongju Kim, Jungwan Lee, Donghyeon Han, Hoi-Jun Yoo, Sangyeob Kim  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.17063v1  

#### Abstract
Recently, Mixture-of-Experts (MoE) models have gained attention for efficiently scaling large language models. Although these models are extremely large, their sparse activation enables inference to be performed by accessing only a fraction of the model at a time. This property opens the possibility...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《FlashMoE: Reducing SSD I/O Bottlenecks via ML-Based Cache Replacement for Mixture-of-Experts Inference on Edge Devices》总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
- **MoE模型在边缘设备上的内存瓶颈**：尽管Mixture-of-Experts (MoE) 模型通过稀疏激活实现了高效推理，但其总参数量可达数百GB，远超典型边缘设备（如桌面级PC）的DRAM容量（通常为16–64GB）。
- **传统offloading方案不适用于SSD层级存储**：现有系统（如Fiddler、DAOP）依赖将专家卸载到DRAM，假设整个模型至少可驻留在主存中，这在实际边缘场景下已不可行。
- **通用缓存策略（LRU/LFU）无法有效捕捉MoE的路由特性**：专家访问具有时间局部性和频率模式，但LRU易造成“Eviction Delay”和“Evicting Hot Experts”，导致高代价的SSD I/O。

### 提出了什么新方法或新思路
- **FlashMoE系统架构**：
  - 将**expert与non-expert权重分离存储**，仅在初始化时加载轻量的non-expert部分（Attention、Normalization、Routing Gate等），显著降低启动延迟。
  - 支持**按需从SSD加载expert**，实现细粒度的layer-level和unit-level专家加载。
- **ML-Based Cache Replacement Policy**：
  - 设计了一个基于**Feed-Forward Network (FFN)** 的缓存替换策略，融合LRU（recency）和LFU（frequency）信号。
  - 输入特征包括归一化的**Recency Score**（上次访问步数倒数）和**Frequency Score**（历史调用次数），输出预测下一个使用距离，指导最优驱逐决策。
  - 利用Belady’s optimal policy生成训练标签，逼近理论最优替换时机。

### 相比现有方法的优势
| 维度 | FlashMoE优势 |
|------|--------------|
| **内存效率** | 不要求全模型驻留DRAM，支持SSD作为last-level cache，适用于内存受限边缘设备 |
| **加载延迟** | 初始化仅加载<2GB non-expert模块，比完整加载快4–6.8× |
| **缓存命中率** | ML-based策略相比LRU提升高达51%，显著减少SSD I/O |
| **端到端性能** | 实现最高达2.6×的推理速度加速 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **TriviaQA [7]**：用于训练ML缓存模型和评估推理性能的大规模问答数据集。
  - 训练集用于提取routing trace以识别“popular experts”并训练ML cache。
  - 测试子集用于解码速度和缓存命中率评估，确保无数据泄露。

### 实验设置
- **硬件平台**（见Table 2）：
  - CPU: AMD Ryzen 9 9600X (Zen 5)
  - GPU: NVIDIA RTX 5070 Ti (16GB GDDR7)
  - SSD: SK hynix P51 NVMe (PCIe 5.0, 读取7.4 GB/s)
  - RAM: 64GB DDR5 (保留约1GB可用，模拟资源紧张环境)
  - 软件栈: Python 3.11 + PyTorch 2.7 + CUDA 12.9

- **评估模型**：
  - **OLMoE-1B-7B**：学术MoE模型
  - **Qwen3-30B-A3B [12]**：工业级大规模MoE模型（总30.3B参数，激活3.3B）

### 评估指标
| 指标 | 描述 |
|------|------|
| **Initial Model Loading Time** | 从磁盘加载非专家模块的时间 |
| **Prefill Latency** | 首个token处理阶段的总延迟（含专家加载） |
| **Cache Hit Rate** | 缓存命中比例，直接影响SSD I/O频次 |
| **Token Generation Speed (Throughput)** | 单位时间内生成的token数量（tokens/sec） |
| **End-to-End Inference Latency** | 完整请求响应时间 |

### 基线方法对比
- **llama.cpp**：传统LLM推理框架，未针对MoE优化
- **Fiddler [8]**：CPU-GPU协同调度MoE推理系统
- **DAOP [13]**：数据感知卸载与预计算框架
- **经典缓存策略**：
  - LRU（Least Recently Used）
  - LFU（Least Frequently Used）
  - LIFO
- **先进学习型缓存**：
  - ARC（Adaptive Replacement Cache）
  - LeCaR（Learning-based Cache Replacement）

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 缓存命中率（Fig. 6）
| 模型 | 方法 | 提升幅度（vs LRU） |
|------|------|------------------|
| OLMoE-1B-7B | FlashMoE (ML Cache) | **+21%** |
| Qwen3-30B-A3B | FlashMoE (ML Cache) | **+51%** |
- 同时优于LFU（+51%）、ARC（+28%）、LeCaR（+21%）
- 显著降低I/O：命中率提升带来**22–35%的I/O减少**

#### ✅ 推理吞吐量（Fig. 7b, 7e）
| 模型 | 方法 | 性能提升（vs LRU） |
|------|------|------------------|
| OLMoE-1B-7B | FlashMoE | **+22%** |
| Qwen3-30B-A3B | FlashMoE | **+7%** |
- 在高负载下仍保持优势，尤其在长序列输入时表现稳定

#### ✅ 系统级端到端加速（Fig. 7c, 7f）
| 对比项 | 加速比 |
|--------|-------|
| vs Fiddler / DAOP | 最高 **2.6× speedup** |
| vs llama.cpp | 最高 **4.1× speedup**（prefill阶段） |

#### ✅ 初始加载与Prefill延迟
| 项目 | FlashMoE优势 |
|------|-------------|
| Initial Load | 比llama.cpp快 **4×**，比Fiddler/DAOP快 **6.8×** |
| Prefill Latency | 即使计入专家加载，仍快 **2.5×（vs llama.cpp）**, **4.1×（vs Fiddler/DAOP）** |
- 原因：仅加载<2GB non-expert模块，而非数十GB完整模型

### 消融实验结果（隐含分析）
- **ML Cache有效性验证**：
  - LRU vs Belady最优策略对比显示：LRU有**34.2%的被驱逐专家在后续5步内被重用**，而Belady仅为0.1%，说明LRU存在严重误驱逐。
  - 在Qwen3上，LRU与LFU选择更优的比例分别为56% vs 44%，表明两者各有优势，**融合二者信息是必要且有效的**。
- **缓存大小影响**（Fig. 6）：
  - 所有策略随缓存增大命中率上升，但FlashMoE在各种缓存配置下均领先，体现其鲁棒性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **MoE模型可在边缘设备运行**：通过将inactive experts offload至SSD，并结合智能缓存管理，可在仅16–64GB RAM环境下高效运行百GB级MoE模型。
2. **文件组织方式至关重要**：将expert与non-expert分离存储，并以`.pt`文件形式分层保存，极大提升了加载灵活性和效率。
3. **传统缓存策略不足以应对MoE路由动态性**：LRU因recency bias导致频繁驱逐即将使用的“hot experts”，而LFU对突发访问不敏感。
4. **轻量级ML模型即可显著提升缓存性能**：
   - 使用每层一个小型FFN（~113KB）即可建模复杂访问模式。
   - 训练开销低：在A100上约2小时内完成全流程（trace收集 → 特征构建 → 训练）。
5. **异步执行掩盖计算开销**：
   - FlashMoE利用SSD加载延迟（~3ms）并行执行ML Cache推理（~0.1ms），完全隐藏额外计算成本。

### 方法的局限性
- **依赖历史路由模式泛化能力**：若测试任务与训练分布差异大（如领域迁移），ML Cache可能失效。
- **需要预先采集routing trace进行训练**：增加了部署前准备步骤，对实时自适应能力有一定限制。
- **当前ML Cache为静态训练模型**：未实现在推理过程中持续在线学习更新。
- **对SSD带宽高度依赖**：若SSD性能下降（如QLC颗粒老化），整体收益会减弱。

### 未来工作方向
- **Online Learning for Cache Policy**：开发可在推理过程中动态调整的自适应ML缓存机制。
- **跨层联合缓存决策**：当前策略为per-layer独立决策，未来可探索全局联合优化。
- **支持更多设备组合**：扩展至移动端SoC（如手机NPU+UFS）、IoT设备等更低功耗平台。
- **集成Prefetching机制**：基于ML预测提前加载潜在被调用的expert，进一步降低miss惩罚。

--- 

> **总结一句话**：  
> FlashMoE首次实现了在普通桌面级硬件上高效运行超大规模MoE模型，其核心在于**结构化模型拆分 + ML驱动的智能缓存**，解决了SSD I/O瓶颈这一关键挑战，为边缘侧部署百亿参数MoE提供了可行路径。

</details>

---

### 15. [HeterCSI: Channel-Adaptive Heterogeneous CSI Pretraining Framework for Generalized Wireless Foundation Models](https://arxiv.org/abs/2601.18200)

**Authors**: Chenyu Zhang, Xinchen Lyu, Chenshan Ren, Shuhan Liu, Qimei Cui, Xiaofeng Tao  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18200v1  

#### Abstract
Wireless foundation models promise transformative capabilities for channel state information (CSI) processing across diverse 6G network applications, yet face fundamental challenges due to the inherent dual heterogeneity of CSI across both scale and scenario dimensions. However, current pretraining ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HeterCSI: Channel-Adaptive Heterogeneous CSI Pretraining Framework for Generalized Wireless Foundation Models

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前无线信道基础模型（Wireless Foundation Model）在处理 **Channel State Information (CSI)** 数据时面临两大挑战：
- **Scale Heterogeneity**：不同场景下 CSI 张量的维度差异巨大（如时间窗口 $T$、子载波数 $K$、天线阵列大小 $A$ 不同），导致训练效率低下。
- **Scenario Heterogeneity**：不同传播环境（如 RMa、UMa、UMi、Indoor）下的统计分布差异大，影响模型泛化能力。

现有方法（如 WiFo）通常采用 **scale- and scenario-isolated training**，即每个 mini-batch 只包含单一尺度或单一场景的数据，限制了跨尺度和跨场景的知识迁移，无法实现真正的通用无线基础模型。

---

### 🚀 提出的新方法与核心思想

作者提出 **HeterCSI** —— 一种通道自适应的异构 CSI 预训练框架，其核心洞察是：

> **Scale heterogeneity 是梯度冲突（gradient conflict）的主要来源，而 scenario diversity 实际上有助于梯度对齐（constructive gradient alignment）。**

基于此，HeterCSI 的设计包含两个关键机制：

#### （1）**Scale-Aware Adaptive Batching Strategy（尺度感知自适应批处理策略）**
- 将所有 CSI 样本按序列长度排序后划分为多个“bucket”（桶）；
- 每个 bucket 内部样本具有相似的尺度（minimize padding overhead）；
- 在训练过程中从不同 bucket 中随机采样 mini-batch，保证全局多样性（preserve scenario diversity）；
- 形成“**桶内同质、桶间异质**”的优化结构。

#### （2）**Double-Masking Mechanism（双重掩码机制）**
- **MAE-style 随机掩码**：用于自监督学习，提升表示能力；
- **Attention Masking**：屏蔽零填充（zero-padding）位置的注意力权重，防止无效 token 干扰有效信号建模。

该框架实现了：
- 跨尺度、跨场景的统一预训练；
- 高效且稳定的优化过程；
- 强大的 zero-shot 泛化能力。

---

### 🔍 相比现有方法的优势

| 方面 | HeterCSI | 现有方法（如 WiFo） |
|------|----------|---------------------|
| 输入灵活性 | 支持任意尺度 CSI 输入 | 多数需固定尺寸或隔离训练 |
| 批处理方式 | 自适应混合多尺度 + 多场景 | 单一尺度/场景隔离训练 |
| 梯度稳定性 | 显著减少 destructive gradient interference | 存在严重梯度冲突风险 |
| 泛化能力 | zero-shot 下优于 full-shot 模型 | 依赖微调或特定场景训练 |
| 训练效率 | 减少约 53% 训练延迟 | padding 开销高，效率低 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **生成工具**：使用符合 3GPP 标准的信道仿真器 **QuaDRiGa** 生成 CSI 数据；
- **系统配置**：MISO-OFDM 架构，基站配备 UPA 天线阵列，终端单天线；
- **频率范围**：涵盖 Sub-6GHz 到 mmWave（最高达 42 GHz）；
- **场景类型**：包括 Indoor、RMa、UMa、UMi 等多种典型传播环境。

#### 训练与测试划分：
- **训练集**：40 个异构数据集，共 480,000 个样本（每个数据集 12,000）；
- **测试集**：12 个**未见过的 zero-shot 场景**（D1–D12），用于评估泛化能力；
- 所有数据添加 20 dB 的复高斯噪声以模拟真实条件。

---

### 📊 实验设置与评估指标

#### 评估任务：
1. **CSI Reconstruction**：从部分观测中恢复完整 CSI；
2. **Time-Domain Prediction**：基于历史时隙预测未来 CSI；
3. **Frequency-Domain Prediction**：基于低频段外推高频段 CSI。

#### 性能指标：
- **NMSE (Normalized Mean Squared Error)**，单位为 dB：
  $$
  \text{NMSE (dB)} = 10 \log_{10} \left( \frac{\| \hat{H} - H \|_F^2}{\| H \|_F^2} \right)
  $$
  数值越小越好。

#### 基线方法对比：
| 类别 | 方法 |
|------|------|
| **Full-shot 模型**（在目标域训练） | LSTM, Transformer, LLM4CP, BERT4MIMO |
| **Zero-shot 模型**（无需目标域训练） | PAD (Prony-based), WiFo (SOTA) |
| **训练范式对比** | Sequential, Alternating, Global Shuffling |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（平均 NMSE 对比）

| 方法 | CSI Recon. ↓ | Time Pred. ↓ | Freq. Pred. ↓ |
|------|---------------|----------------|------------------|
| **WiFo (SOTA zero-shot)** | -14.74 dB | -4.60 dB | -9.34 dB |
| **Proposed (HeterCSI)** | **-15.91 dB** | **-6.18 dB** | **-12.48 dB** |
| **提升幅度** | **+1.17 dB** | **+1.58 dB** | **+3.14 dB** |

> 💡 特别地，相比 SOTA zero-shot 模型 **WiFo**，HeterCSI 在三项任务上分别降低 NMSE 达：
> - **7.19 dB**（重建）
> - **4.08 dB**（时域预测）
> - **5.27 dB**（频域预测）

---

### ⚖️ 与 Full-shot 模型的比较

尽管 HeterCSI 是一个 **zero-shot 模型**（未在测试场景训练），但在平均性能上仍优于大多数 full-shot 模型：

| 指标 | 相比最佳 baseline 提升 |
|------|------------------------|
| CSI Reconstruction | +1.33 dB |
| Time-Domain Prediction | +1.58 dB |
| Frequency-Domain Prediction | +3.14 dB |

> ✅ 在 36 项测试中，HeterCSI 在 **80.6% 的情况下取得最优性能**；
> ✅ 其余情况均进入 top-2，最大差距不超过 1.8 dB，表现出极强的稳定性和鲁棒性。

---

### 🔬 消融实验与关键分析

#### （1）**Bucket Granularity 影响**
- 使用不同数量的 bucket（B=4 vs B=8）进行实验；
- 结果显示：更高的异构性需要更细粒度的 bucket 分割来平衡效率与泛化；
- 当 B=8 时，padding ratio 控制在 13.58%，训练时间仅为 Global 方法的 **47%**。

#### （2）**训练范式对比（Scalability）**
随着训练数据集从 8 增加到 40：
- **Global Shuffling** 出现明显性能下降（尤其在 32 数据集时），归因于梯度冲突；
- **HeterCSI** 表现出单调上升的 scaling law，验证了其可扩展性。

#### （3）**效率分析**
| 方法 | 40 datasets 下训练时间 | Padding Ratio |
|------|-------------------------|----------------|
| Global | 30.55 小时 | 58.91% |
| Alternating | 9.6 小时 | 0% |
| **HeterCSI (B=8)** | **11.61 小时** | **13.58%** |

> ✅ HeterCSI 在保持接近 Alternating 效率的同时，获得了 Global 级别的泛化能力。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Scale heterogeneity 是梯度冲突的根本原因**，而非 scenario diversity；
2. **Scenario diversity 实际促进梯度对齐**，应被保留而非规避；
3. 通过 **scale-aware batching + double masking** 可有效解耦效率与泛化之间的矛盾；
4. HeterCSI 成功构建了一个无需微调即可适用于多种未知场景的 **generalized wireless foundation model**；
5. 模型在 zero-shot 设置下超越 full-shot 模型，标志着向通用无线智能迈出关键一步。

---

### ⚠️ 局限性

1. **依赖高质量合成数据**：目前实验基于 QuaDRiGa 仿真数据，尚未在真实设备采集数据上验证；
2. **bucket 数量为超参数**：需手动设定，缺乏自动调节机制；
3. **未考虑动态拓扑变化**：如移动用户轨迹、突发干扰等复杂动态行为；
4. **计算资源要求较高**：虽然效率提升显著，但仍需多 GPU 支持大规模训练。

---

### 🔮 未来工作方向

1. **引入物理先验知识**：探索 physics-aware positional encoding，融合电磁传播规律；
2. **轻量化架构设计**：开发适用于边缘设备部署的小型化版本；
3. **真实数据闭环验证**：结合实测数据进行端到端系统级测试；
4. **多模态扩展**：集成 GPS、IMU、地图等辅助信息，构建更强大的无线感知基础模型；
5. **在线自适应机制**：研究如何在运行时动态调整 bucket 策略以应对突发流量或新场景。

---

> 📌 **一句话总结**：  
> HeterCSI 首次揭示了 CSI 异构性中“尺度”与“场景”的不同作用，并通过创新的 scale-aware batching 和 double masking 机制，在不牺牲效率的前提下实现了前所未有的 zero-shot 泛化能力，为 6G 时代的通用无线基础模型提供了可行路径。

</details>

---

### 16. [Superlinear Multi-Step Attention](https://arxiv.org/abs/2601.18401)

**Authors**: Yufeng Huang  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.18401v1  

#### Abstract
In this paper, we propose \textbf{Superlinear attention}, a fully trainable multi-step attention architecture that achieves subquadratic complexity for long sequences while preserving \textbf{random context access} (a.k.a.\ structural non-exclusion): no eligible token position is structurally exclud...

---

### 17. [UrduLM: A Resource-Efficient Monolingual Urdu Language Model](https://arxiv.org/abs/2601.17664)

**Authors**: Syed Muhammad Ali, Hammad Sajid, Zainab Haider, Ali Muhammad Asad, Haya Fatima, Abdul Samad  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.17664v1  

#### Abstract
Urdu, spoken by 230 million people worldwide, lacks dedicated transformer-based language models and curated corpora. While multilingual models provide limited Urdu support, they suffer from poor performance, high computational costs, and cultural inaccuracies due to insufficient training data. To ad...

---

### 18. [ConceptACT: Episode-Level Concepts for Sample-Efficient Robotic Imitation Learning](https://arxiv.org/abs/2601.17135)

**Authors**: Jakob Karalus, Friedhelm Schwenker  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.17135v1  

#### Abstract
Imitation learning enables robots to acquire complex manipulation skills from human demonstrations, but current methods rely solely on low-level sensorimotor data while ignoring the rich semantic knowledge humans naturally possess about tasks. We present ConceptACT, an extension of Action Chunking w...

---

### 19. [PUNCH: Physics-informed Uncertainty-aware Network for Coronary Hemodynamics](https://arxiv.org/abs/2601.17192)

**Authors**: Sukirt Thakur, Marcus Roper, Yang Zhou, Reza Akbarian Bafghi, Brahmajee K. Nallamothu, C. Alberto Figueroa, Srinivas Paruchuri, Scott Burger, Maziar Raissi  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.17192v1  

#### Abstract
Coronary microvascular dysfunction (CMD) affects millions worldwide yet remains underdiagnosed because gold-standard physiological measurements are invasive and variably reproducible. We introduce a non-invasive, uncertainty-aware framework for estimating coronary flow reserve (CFR) directly from st...

---

### 20. [Efficient Dilated Squeeze and Excitation Neural Operator for Differential Equations](https://arxiv.org/abs/2601.17407)

**Authors**: Prajwal Chauhan, Salah Eddine Choutri, Saif Eddin Jabari  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.17407v1  

#### Abstract
Fast and accurate surrogates for physics-driven partial differential equations (PDEs) are essential in fields such as aerodynamics, porous media design, and flow control. However, many transformer-based models and existing neural operators remain parameter-heavy, resulting in costly training and slu...

---

### 21. [Split-on-Share: Mixture of Sparse Experts for Task-Agnostic Continual Learning](https://arxiv.org/abs/2601.17616)

**Authors**: Fatema Siddika, Md Anwar Hossen, Tanwi Mallick, Ali Jannesari  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.17616v1  

#### Abstract
Continual learning in Large Language Models (LLMs) is hindered by the plasticity-stability dilemma, where acquiring new capabilities often leads to catastrophic forgetting of previous knowledge. Existing methods typically treat parameters uniformly, failing to distinguish between specific task knowl...

---

### 22. [Just-In-Time Reinforcement Learning: Continual Learning in LLM Agents Without Gradient Updates](https://arxiv.org/abs/2601.18510)

**Authors**: Yibo Li, Zijie Lin, Ailin Deng, Xuan Zhang, Yufei He, Shuo Ji, Tri Cao, Bryan Hooi  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18510v1  

#### Abstract
While Large Language Model (LLM) agents excel at general tasks, they inherently struggle with continual adaptation due to the frozen weights after deployment. Conventional reinforcement learning (RL) offers a solution but incurs prohibitive computational costs and the risk of catastrophic forgetting...

---

### 23. [Multi-Objective Reinforcement Learning for Efficient Tactical Decision Making for Trucks in Highway Traffic](https://arxiv.org/abs/2601.18783)

**Authors**: Deepthi Pathare, Leo Laine, Morteza Haghir Chehreghani  
**Category**: cs.LG  
**Published**: 2026-01-27  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18783v1  

#### Abstract
Balancing safety, efficiency, and operational costs in highway driving poses a challenging decision-making problem for heavy-duty vehicles. A central difficulty is that conventional scalar reward formulations, obtained by aggregating these competing objectives, often obscure the structure of their t...

---

### 24. [When Agents Fail to Act: A Diagnostic Framework for Tool Invocation Reliability in Multi-Agent LLM Systems](https://arxiv.org/abs/2601.16280)

**Authors**: Donghao Huang, Gauri Malwe, Zhaoxia Wang  
**Category**: cs.AI  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.16280v1  

#### Abstract
Multi-agent systems powered by large language models (LLMs) are transforming enterprise automation, yet systematic evaluation methodologies for assessing tool-use reliability remain underdeveloped. We introduce a comprehensive diagnostic framework that leverages big data analytics to evaluate proced...

---

### 25. [LongCat-Flash-Thinking-2601 Technical Report](https://arxiv.org/abs/2601.16725)

**Authors**: Meituan LongCat Team, Anchun Gui, Bei Li, Bingyang Tao, Bole Zhou, Borun Chen, Chao Zhang, Chao Zhang, Chen Gao, Chen Zhang, Chengcheng Han, Chenhui Yang, Chuyu Zhang, Cong Chen, Cunguang Wang, Daoru Pan, Defei Bu, Dengchang Zhao, Di Xiu, Dishan Liu, Dongyu Ru, Dunwei Tu, Fan Wu, Fengcheng Yuan, Fengcun Li, Gang Xu, Guanyu Wu, Guoyuan Lin, Haibin Wang, Hansi Yang, Hao Yang, Haonan Yan, Haoxiang Ma, Haoxing Wen, Hongyan Hao, Hongyin Tang, Hongyu Zang, Hongzhi Ni, Hui Su, Jiacheng Zhang, Jiahong Zhou, Jiahuan Li, Jiaming Wang, Jian Yang, Jianfei Zhang, Jianhao Xu, Jianing Wang, Jiapeng Zhu, Jiaqi Sun, Jiarong Shi, Jiarui Zhao, Jingang Wang, Jinluan Yang, Jinrui Ding, Jinwei Xiao, Jiyuan He, Juncan Xu, Kefeng Zhang, Keheng Wang, Li Wei, Lianhui Ma, Lin Qiu, Lingbing Kong, Lingchuan Liu, Linsen Guo, Mengshen Zhu, Mengxia Shen, Mingyang Zhu, Peiguang Li, Peng Pei, Pengcheng Jia, Pengtao Zhang, Peng Zhao, Qi Gu, Qiong Huang, Qiyuan Duan, Quanchi Weng, Rongxiang Weng, Rongzhi Zhang, Rumei Li, Shanglin Lei, Shengnan An, Shijun Dai, Shuaikang Liu, Shuang Zhou, Shuo Wang, Songyuan Zhao, Tao Liang, Tianhao Hu, Tianze Chen, Wei Liu, Wei Shi, Wei Wang, Weifeng Tang, Wenjie Shi, Wenlong Zhu, Wentao Chen, Wentao Shi, Xi Su, Xiangcheng Liu, Xiandi Ma, Xiangyu Xi, Xiangyuan Liu, Xiangzhou Huang, Xiao Liu, Xiaodong Cai, Xiaolong Chen, Xiaowei Shi, Xiaoyu Li, Xin Chen, Xingchen Liu, Xuan Huang, Xuezhi Cao, Xunliang Cai, Yan Chen, Yang Bai, Yang Liu, Yang Yang, Yang Zheng, Yaoming Wang, Yaoming Zhu, Yaqi Huo, Yanyu Chen, Yaorui Shi, Yerui Sun, Yi Zhang, Yihao Chen, Yi-Kai Zhang, Yifan Lu, Yifan Zhao, Yitao Zhai, Yongjing Yin, Yongwei Zhou, Youshao Xiao, Yuchuan Dai, Yuchen Xie, Yuchen Yu, Yufei Zhang, Yuhuai Wei, Yulei Qian, Yunfan Liang, Yunke Zhao, Yuwei Jiang, Yuxin Bian, Yuxin Chen, Yuxin Liu, Yue Xu, Yueqing Sun, Zeyang Yu, Zhao Yang, Zhengsheng Huang, Zhengyu Chen, Zhijian Liu, Zhikang Xia, Zhimin Lin, Zhiyuan Yao, Zhuofan Chen, Zhuowen Han, Zijian Zhang, Ziran Li, Ziwen Wang, Ziyuan Zhuang  
**Category**: cs.AI  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.16725v1  

#### Abstract
We introduce LongCat-Flash-Thinking-2601, a 560-billion-parameter open-source Mixture-of-Experts (MoE) reasoning model with superior agentic reasoning capability. LongCat-Flash-Thinking-2601 achieves state-of-the-art performance among open-source models on a wide range of agentic benchmarks, includi...

---

### 26. [Revisiting Modality Invariance in a Multilingual Speech-Text Model via Neuron-Level Analysis](https://arxiv.org/abs/2601.17387)

**Authors**: Toshiki Nakai, Varsha Suresh, Vera Demberg  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.17387v1  

#### Abstract
Multilingual speech-text foundation models aim to process language uniformly across both modality and language, yet it remains unclear whether they internally represent the same language consistently when it is spoken versus written. We investigate this question in SeamlessM4T v2 through three compl...

---

### 27. [A System for Name and Address Parsing with Large Language Models](https://arxiv.org/abs/2601.18014)

**Authors**: Adeeba Tarannum, Muzakkiruddin Ahmed Mohammed, Mert Can Cakmak, Shames Al Mandalawi, John Talburt  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.18014v1  

#### Abstract
Reliable transformation of unstructured person and address text into structured data remains a key challenge in large-scale information systems. Traditional rule-based and probabilistic approaches perform well on clean inputs but fail under noisy or multilingual conditions, while neural and large la...

---

### 28. [BoRP: Bootstrapped Regression Probing for Scalable and Human-Aligned LLM Evaluation](https://arxiv.org/abs/2601.18253)

**Authors**: Peng Sun, Xiangyu Zhang, Duan Wu  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.18253v1  

#### Abstract
Accurate evaluation of user satisfaction is critical for iterative development of conversational AI. However, for open-ended assistants, traditional A/B testing lacks reliable metrics: explicit feedback is sparse, while implicit metrics are ambiguous. To bridge this gap, we introduce BoRP (Bootstrap...

---

### 29. [When Domain Pretraining Interferes with Instruction Alignment: An Empirical Study of Adapter Merging in Medical LLMs](https://arxiv.org/abs/2601.18350)

**Authors**: Junyi Zou  
**Category**: cs.CL  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.18350v1  

#### Abstract
Large language models (LLMs) show strong general capability but often struggle with medical terminology precision and safety-critical instruction following. We present a case study for adapter interference in safety-critical domains using a 14B-parameter base model through a two-stage LoRA pipeline:...

---

### 30. [Communication-Avoiding Linear Algebraic Kernel K-Means on GPUs](https://arxiv.org/abs/2601.17136)

**Authors**: Julian Bellavita, Matthew Rubino, Nakul Iyer, Andrew Chang, Aditya Devarakonda, Flavio Vella, Giulia Guidi  
**Category**: cs.DC  
**Published**: 2026-01-27  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.17136v1  

#### Abstract
Clustering is an important tool in data analysis, with K-means being popular for its simplicity and versatility. However, it cannot handle non-linearly separable clusters. Kernel K-means addresses this limitation but requires a large kernel matrix, making it computationally and memory intensive. Pri...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
