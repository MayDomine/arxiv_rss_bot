# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-10 05:55:55 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Chopper: A Multi-Level GPU Characterization Tool & Derived Insights Into LLM Training Inefficiency](https://arxiv.org/abs/2512.08242)

**Authors**: Marco Kurzynski, Shaizeen Aga, Di Wu  
**Category**: cs.DC  
**Published**: 2025-12-10  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.08242v1  

#### Abstract
Training large language models (LLMs) efficiently requires a deep understanding of how modern GPU systems behave under real-world distributed training workloads. While prior work has focused primarily on kernel-level performance or single-GPU microbenchmarks, the complex interaction between communic...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Chopper: A Multi-Level GPU Characterization Tool & Derived Insights Into LLM Training Inefficiency*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前对大语言模型（LLM）训练在多GPU系统上的性能瓶颈缺乏**端到端、多层次的综合分析工具**。已有研究多集中于单个层面（如kernel级或单GPU微基准），难以揭示通信、计算、内存行为与功耗管理之间的复杂交互关系，导致无法准确识别真实训练中的性能瓶颈。

### 提出的新方法与新思路
作者提出 **Chopper** —— 一种**多粒度GPU表征框架**，用于自动化收集、对齐和可视化LLM训练过程中的GPU内核轨迹（kernel traces）和硬件性能计数器（hardware performance counters）。其核心创新在于支持从多个维度进行分析：
- **应用层级粒度**：kernel → operation → layer → phase（forward/backward/optimizer）→ iteration → GPU → 整体workload
- **硬件层级粒度**：node级多GPU系统、单GPU微架构、CPU行为
- **跨软硬件联合洞察**：结合软件执行流与硬件资源利用率（如MFMA utilization、memory bandwidth、frequency、power等）

此外，Chopper提供开源工具，支持AMD Instinct™ MI300X，并可扩展至其他GPU平台。

### 相比现有方法的优势
| 维度 | Chopper | 其他工作（如[33]-[37]） |
|------|--------|-----------------------|
| **粒度覆盖** | ✅ 应用全栈 + 硬件全层 | ❌ 多为单一粒度（仅kernel或operation） |
| **分析深度** | ✅ 软硬协同分析 | ❌ 多聚焦单一视角（仅硬件或仅软件） |
| **工具可用性** | ✅ 开源发布 | ❌ 多无工具或未公开 |
| **系统完整性** | ✅ 支持多GPU + CPU协同分析 | ❌ 多局限于单GPU |

> **优势总结**：Chopper是首个实现**端到端、多粒度、软硬协同**的LLM训练性能分析框架，填补了现有工具链的空白。

---

## 2. 核心实验方法和设置

### 数据集与模型
- **模型**：Llama 3 8B（公开发布的LLM）
- **配置参数**：
  - 层数：32
  - Token长度：4,096
  - 隐藏维度：14,336
  - 注意力头数：32 Query Heads / 8 KV Heads（启用Group Query Attention）

### 实验设置
- **并行策略**：Fully Sharded Data Parallelism (**FSDP**)，包括FSDPv1和FSDPv2版本对比
- **训练框架**：基于PyTorch，使用FSDP + FlashAttention-V2 + BF16数据格式
- **硬件平台**：
  - **GPU**：8× AMD Instinct™ MI300X（每卡峰值1.3 PFLOPS，HBM 192GB，带宽5.3TB/s）
  - **互联**：AMD Infinity Fabric™ 128GB/s双向连接（全互连拓扑）
  - **CPU**：双路AMD EPYC™ 9684X，共2.3TB主机内存
- **Profiling工具**：
  - 运行时追踪：PyTorch Profiler + AMD roctracer
  - 硬件计数器采集：AMD rocprofv3
- **评估配置**：
  - 批次大小（batch size）：1, 2, 4
  - 序列长度（sequence length）：4K, 8K（记作b1s4, b2s4, ..., b2s8）
  - 每轮运行20个iteration，前10个warmup，后10个采样

### 评估指标
- **吞吐量（Throughput）**：tokens/sec
- **各阶段耗时分解**：forward / backward / optimizer
- **操作类型耗时占比**：GEMM、FlashAttention、vector ops等
- **Launch Overhead**：kernel间空泡时间
- **Communication/Computation Overlap Ratio**
- **GPU频率、内存频率、功耗**
- **理论 vs 实际性能差距分解**

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 配置 | 吞吐量（相对b1s4归一化） | Backward占比 | GEMM总耗时占比 |
|------|--------------------------|---------------|----------------|
| b1s4 | 1.0x（基准）              | ~60%          | ~60%           |
| b2s4 | ~1.3x                     | ~65%          | ~60%           |
| b4s4 | 最高吞吐                 | ~70%          | ~60%           |
| b2s8 | 略低于b2s4                | 更高           | 更高           |

> ⚠️ **b1s4吞吐显著偏低（约低30%）**，表明小batch size下严重资源未充分利用。

### 与基线方法对比结果
#### FSDPv1 vs FSDPv2 性能对比
| 指标 | FSDPv1 | FSDPv2 | 提升原因 |
|------|--------|--------|---------|
| **平均GPU频率** | ~1.5 GHz | ~1.8 GHz (+20%) | 内存分配更确定，减少HBM功耗波动 |
| **内存频率稳定性** | 波动大 | 更稳定 | 减少DVFS throttling |
| **总体吞吐** | 较低 | 显著更高 | 更高且稳定的频率支撑 |
| **copy kernel调度** | 并发 | **串行化**（per-parameter sharding引入） | 增加launch overhead但换来更高频率 |

> 📌 尽管FSDPv2因串行化copy kernel增加了launch overhead，但由于**更高的GPU频率**，最终仍实现**更高吞吐**。

### 消融实验与关键分解结果（见Fig. 15）
对GEMM和FlashAttention的操作耗时进行了**理论 vs 实际**的开销分解，量化各项因素贡献：

| 开销类型 | 占比（典型值） | 说明 |
|--------|----------------|------|
| **Frequency Overhead** | **最大项（主导地位）** | 因DVFS导致运行频率低于峰值（MI300X未达1.3GHz） |
| **Utilization Overhead** | 次要 | MFMA core未满载（尤其FlashAttention含大量vector ops） |
| **Overlap Overhead** | 中等 | C3重叠不充分导致资源争用 |
| **Instruction Overhead** | 极小 | padding带来的额外flop极少 |
| **Launch Overhead** | 可观（尤其小batch） | kernel启动延迟影响明显 |

> 🔍 **Frequency Overhead 是理论与实际性能差距的最大来源**，超过MFMA利用率损失、通信/计算重叠不足、kernel launch开销等所有其他因素之和。

---

## 4. 关键结论和发现

### 主要发现（Insights）
1. **💡 Batch Size = 1 导致严重性能下降**
   - 吞吐比b2s4低约30%，主因包括：
     - Backward FlashAttention实现不佳（反而比b2s4慢）
     - Launch overhead占比高
     - 更高的通信/计算重叠引发资源竞争

2. **💡 Backward FlashAttention存在优化缺陷**
   - 在b=1时，尽管计算量更大，但耗时却更短 → 表明实现存在问题（可能未充分利用并行性）
   - 推荐使用Chopper定位并修复该kernel的实现问题

3. **💡 Frequency Overhead 是性能瓶颈的“头号杀手”**
   - 动态电压频率调节（DVFS）导致GPU长期运行在远低于峰值频率的状态
   - FSDPv2通过更**确定性的内存分配行为**减少了HBM功耗波动，从而允许维持更高频率（+20%），这是其性能优于FSDPv1的根本原因

4. **💡 Launch Overhead 主要来自迭代边界而非内部**
   - 准备开销（preparation overhead）集中在iteration开始（f_ie）和结束（opt_step），源于通信pipeline的填充与清空
   - 当前优化多关注intra-iteration launch，应加强inter-iteration优化

5. **💡 CPU严重未被充分利用**
   - 仅约25个逻辑核心活跃，物理核心利用率仅12.5%
   - 存在巨大**power-sloshing**空间：可将CPU闲置功率转移给GPU以提升性能

6. **💡 相同操作因通信重叠不同而耗时差异大**
   - 如`b_attn_n`与`b_mlp_n`计算相同，但前者有~90%通信重叠 → 耗时更长
   - 重叠本身不一定有益，可能导致资源争用

### 方法的局限性
- 当前Chopper主要针对**AMD Instinct™ MI300X**优化，虽可扩展，但在NVIDIA或其他架构上需适配
- 硬件计数器采集需序列化kernel执行，**无法同时获取精确timestamp与counter**，依赖trace alignment技术
- 分析依赖专家经验解读图表，尚未完全自动化生成优化建议

### 未来工作方向
- 将Chopper扩展至更多GPU厂商（如NVIDIA Hopper）和分布式多节点场景
- 结合机器学习自动识别性能反模式（anti-patterns）
- 推动硬件固件层优化：改进DVFS策略，使其对LLM workload更友好
- 探索动态power-sloshing机制，在训练期间实时将CPU功率重定向至GPU
- 集成进主流训练框架（如PyTorch），实现闭环性能调优

---

> ✅ **总结一句话**：  
> Chopper首次实现了对LLM训练全过程的**多粒度、软硬协同性能剖析**，揭示了**frequency overhead**是制约实际性能的最大瓶颈，并指出**FSDPv2通过提升频率稳定性实现超越**，为未来GPU架构设计、功耗管理和训练框架优化提供了关键指导。

</details>

---

### 2. [CogMCTS: A Novel Cognitive-Guided Monte Carlo Tree Search Framework for Iterative Heuristic Evolution with Large Language Models](https://arxiv.org/abs/2512.08609)

**Authors**: Hui Wang, Yang Liu, Xiaoyu Zhang, Chaoxu Mu  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.08609v1  

#### Abstract
Automatic Heuristic Design (AHD) is an effective1 framework for solving complex optimization prob-2 lems. The development of large language mod-3 els (LLMs) enables the automated generation of4 heuristics. Existing LLM-based evolutionary meth-5 ods rely on population strategies and are prone6 to loc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CogMCTS: A Novel Cognitive-Guided Monte Carlo Tree Search Framework for Iterative Heuristic Evolution with Large Language Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现有的基于 **Large Language Models (LLMs)** 的自动启发式设计（**AHD**）方法存在以下局限性：

- 多数采用**种群策略**（如进化算法），容易陷入局部最优；
- 虽然引入了 **Monte Carlo Tree Search (MCTS)** 来平衡探索与利用（如 MCTS-AHD），但缺乏对历史经验的深度整合；
- 反思机制多为单轮或浅层反馈，难以实现多轮认知迭代；
- 节点扩展方式单一，限制了启发式空间的多样性探索。

这些问题导致搜索效率低、解的质量不稳定、优化过程缺乏系统性认知引导。

---

### **提出了什么新方法或新思路**

本文提出了一种全新的框架：**CogMCTS**（Cognitive-guided MCTS），其核心是将 LLM 的**认知能力**与 MCTS 的**搜索机制**深度融合，形成一个闭环的、可迭代的认知增强型启发式演化系统。

#### 主要创新点包括：

1. ✅ **多轮认知指导机制（Multi-round Cognitive Guidance）**
   - 引入“快速认知”（rapid cognition）与“复杂认知”（complex cognition）两个阶段：
     - 快速认知：通过成对比较启发式函数，提取有效设计模式；
     - 复杂认知：融合正向经验（positive knowledge）、负向反馈（negative knowledge）以及当前节点信息，生成结构化提示以指导 LLM 改进启发式。
   - 利用 **Consistency-based Knowledge Validation (CKV)** 动态管理知识库，防止无效经验积累。

2. ✅ **双轨节点扩展策略（Dual-track Node Expansion）**
   - 在每个选定节点上并行执行两类操作：
     - **em1/em2**：基于认知反馈进行启发式组合与改进（exploitation）；
     - **m1/m2**：进行结构突变（structural mutation）和参数调优（parameter tuning）（exploration）；
   - 并结合 **elite heuristic management**，保留高质量启发式，提升稳定性。

3. ✅ **战略突变机制（Strategic Mutation）**
   - m1 和 m2 操作允许在算法形式或参数层面引入变化，显著增强了解的多样性，避免早熟收敛。

4. ✅ **紧密集成架构**
   - 将 LLM 的推理能力嵌入 MCTS 的每一步（选择、扩展、模拟、回传），实现真正的“认知驱动搜索”。

---

### **相比现有方法的优势**

| 维度 | CogMCTS | 传统 LLM-EPS / MCTS-AHD |
|------|--------|--------------------------|
| 探索-利用平衡 | 更优（双轨扩展 + UCT） | 一般（单路径扩展） |
| 历史经验利用 | 多轮反馈 + 正/负知识管理 | 单次或浅层反思 |
| 多样性保障 | 结构突变 + 参数调优 | 依赖种群多样性 |
| 鲁棒性与稳定性 | 显著更高 | 易受初始种子影响 |

> CogMCTS 实现了更高效、稳定且高质量的启发式自动设计，在多个任务中超越现有 SOTA 方法。

---

## 2. 核心实验方法和设置

### **使用的数据集与问题类型**

实验覆盖多种经典的 **NP-hard 组合优化问题（COPs）**，分别在不同求解框架下测试：

| 问题 | 数据规模 | 框架 |
|------|---------|-------|
| **Orienteering Problem (OP)** | N=50, 100 | ACO |
| **Capacitated Vehicle Routing Problem (CVRP)** | N=50, 100 | ACO |
| **Multiple Knapsack Problem (MKP)** | N=100/200, m=5 | ACO |
| **Traveling Salesman Problem (TSP)** | N=20–200 | GLS |
| **Knapsack Problem (KP)** | N=50–500 | Step-by-step Construction |

所有测试集均包含大量独立实例（64–1000），确保统计可靠性。

---

### **实验设置与评估指标**

- **LLM 模型**：GPT-3.5-turbo 和 GPT-4o-mini；
- **评估预算**：T = 1000 次启发式评估；
- **运行次数**：每个配置独立运行 3 次取平均；
- **评估指标**：
  - **目标值（Obj.）**：越小越好（↓）或越大越好（↑）
  - **相对差距（Gap）**：相对于最优或最佳基线的百分比差异；
  - **最优解发现率**：是否达到全局最优；
  - **进化曲线**：展示收敛速度与稳定性。

---

### **基线方法对比**

分为三类：

1. 🔧 **手工设计启发式**
   - ACO, Greedy Construct (GC)

2. 🤖 **神经组合优化（NCO）方法**
   - DeepACO, POMO, KGLS, NeuOpt, GNNGLS, NeuralGLS

3. 🧠 **LLM-based AHD 方法**
   - FunSearch
   - EoH (Evolution of Heuristics)
   - ReEvo (Reflective Evolution)
   - MCTS-AHD（最新 MCTS 类方法）

> 所有 LLM-based 方法使用相同初始种子、训练集和评估流程，保证公平比较。

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

#### 表格 1 & 2：ACO 框架下的 OP、CVRP、MKP 结果（部分摘录）

| 方法 | OP (N=50) Obj. | Gap | MKP (N=200) Obj. | Gap |
|------|---------------|-----|------------------|-----|
| ACO | 13.354 | 2.78% | 40.672 | 4.40% |
| DeepACO | 19.867 | 0.00% | 41.988 | 1.30% |
| EoH | 15.293 | 23.02% | 41.994 | 1.29% |
| ReEvo | 15.224 | 23.37% | 42.416 | 0.30% |
| MCTS-AHD | 15.186 | 23.56% | 42.498 | 0.11% |
| **CogMCTS (Ours)** | **15.370** | **22.63%** | **42.542** | **0.00%** ✅ |

> 在 MKP 上，CogMCTS 达到绝对最优（Gap=0%），显著优于其他方法。

#### 表格 2：GLS 框架下的 TSP 结果（Gap %）

| 方法 | N=20 | N=50 | N=100 | N=200 |
|------|------|------|-------|-------|
| KGLS | 0.004% | 0.017% | 0.002% | 0.284% |
| NeuOpt | 0.000% | 0.000% | 0.027% | 0.403% |
| ReEvo | 0.000% | 0.001% | 0.009% | 0.278% |
| MCTS-AHD | 0.000% | 0.011% | 0.005% | 0.295% |
| **CogMCTS (Ours)** | 0.015% | **0.000%** ✅ | **0.000%** ✅ | **0.242%** ✅ |

> CogMCTS 在多数 TSP 规模上表现最佳，尤其在大尺度问题（N=100,200）上优势明显。

#### 表格 3：Step-by-step 框架下的 KP 结果（Gap %）

| 方法 | N=50 | N=100 | N=200 | N=500 |
|------|------|-------|-------|-------|
| POMO | 0.00% | 0.00% | 0.00% | 0.00% |
| FunSearch | 0.260% | 0.114% | 0.092% | 0.055% |
| MCTS-AHD | 0.205% | 0.089% | 0.078% | 0.051% |
| **CogMCTS (Ours)** | **0.200%** | **0.089%** | **0.075%** ✅ | **0.051%** ✅ |

> 在最大规模（N=500）下仍保持最低 Gap，显示强泛化能力。

---

### **与基线方法的对比结果**

- 在 **所有任务** 中，CogMCTS 均优于或持平于最强基线；
- 相比 **MCTS-AHD**，CogMCTS 在：
  - 更快地找到高质量启发式（见图3 进化曲线）；
  - 更高频率发现全局最优解；
  - 更稳定的性能输出（三次运行方差小）；
- 相比 **ReEvo/EoH**，CogMCTS 避免了早期停滞（如 ReEvo 在 KP 上仅生成一个个体即停止）；

> 图3 显示 CogMCTS 在前 200 次评估内迅速上升，并持续优化，而其他方法增长缓慢或早停。

---

### **消融实验结果（Ablation Study）**

#### 表格 6：移除不同动作的影响（KP 任务，Gap %）

| 变体 | em1 | em2 | m1 | m2 | Gap (%) |
|------|-----|-----|----|----|--------|
| 完整模型 | √ | √ | √ | √ | **0.089%** |
| 移除 em1 | × | √ | √ | √ | 0.117% |
| 移除 em2 | √ | × | √ | √ | 0.097% |
| 移除 em1+em2 | × | × | √ | √ | 0.102% |

> 结论：
- **em1 和 em2 是最关键组件**，负责认知驱动的启发式改进；
- 缺失任一都会导致性能下降；
- 同时缺失则退化严重，说明认知反馈机制不可或缺。

此外，CKV 知识验证机制的最佳窗口长度为 **m-k=2**（即两轮思考周期），过短或过长均降低效果（见表4、5）。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **CogMCTS 显著提升了 LLM-based AHD 的性能上限**：
   - 在多个 COP 任务中达到或逼近最优解；
   - 收敛速度快、稳定性高、鲁棒性强。

2. ✅ **认知机制的有效性被验证**：
   - 多轮反馈 + 正/负知识管理能显著提升启发式生成质量；
   - “快速认知 + 复杂认知” 构成了有效的元学习循环。

3. ✅ **双轨扩展 + 战略突变增强了多样性**：
   - 避免早熟收敛；
   - 实现了探索与利用的动态平衡。

4. ✅ **框架具有良好的通用性和适应性**：
   - 成功应用于 ACO、GLS、Step-by-step 三种主流框架；
   - 可迁移至不同问题类型和规模。

---

### **方法的局限性**

- ⚠️ **计算成本较高**：每次启发式生成需多次调用 LLM，受限于 API 延迟与费用；
- ⚠️ **依赖高质量提示工程**：认知反馈的设计对最终性能有较大影响；
- ⚠️ **CKV 机制假设较强**：要求全局最优解可比较，可能不适用于高度随机环境；
- ⚠️ **未在超大规模问题（如 N > 1000）上验证**，泛化边界尚待探索。

---

### **未来工作方向**

1. 🔍 **提升认知机制效率**：
   - 探索轻量级模拟策略（如 Gumbel-MCTS）加速搜索；
   - 设计更高效的反馈压缩与存储机制。

2. 🧠 **扩展至更复杂的优化场景**：
   - 多目标优化（MO-COPs）；
   - 动态/在线优化问题；
   - 实际工业应用（物流调度、芯片布线等）。

3. 🔄 **构建端到端自动化 pipeline**：
   - 自动化提示生成；
   - 动态调整 MCTS 参数（如 exploration decay）；
   - 支持多 LLM 协同演化。

4. 💡 **理论分析**：
   - 分析 CogMCTS 的收敛性与样本复杂度；
   - 建立认知反馈与搜索效率之间的数学关系。

---

> **总结**：CogMCTS 是首个将 LLM 的**深层认知能力**与 MCTS 的**系统性搜索能力**紧密结合的 AHD 框架。它不仅在性能上全面超越现有方法，更为“语言模型作为优化器”的研究提供了新的范式——从“被动生成”走向“主动思考”。

</details>

---

### 3. [MobileFineTuner: A Unified End-to-End Framework for Fine-Tuning LLMs on Mobile Phones](https://arxiv.org/abs/2512.08211)

**Authors**: Jiaxiang Geng, Lunyu Zhao, Yiyi Lu, Bing Luo  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.08211v1  

#### Abstract
Mobile phones are the most ubiquitous end devices, generating vast amounts of human-authored data and serving as the primary platform for end-side applications. As high-quality public data for large language models (LLMs) approaches exhaustion, on-device fine-tuning provides an opportunity to levera...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MobileFineTuner: A Unified End-to-End Framework for Fine-Tuning LLMs on Mobile Phones 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）依赖高质量公共数据进行训练，但研究表明这类数据可能在2026–2032年间耗尽。与此同时，海量有价值的**私有用户数据**（如聊天记录、笔记等）存储在移动设备上，具有极高的个性化潜力。然而，将这些敏感数据上传至服务器进行 fine-tuning 存在严重的**隐私风险**，并可能违反 GDPR 等法规。

尽管已有研究探索基于联邦学习或个性化学习的 on-device fine-tuning，但大多数工作仍停留在仿真环境、IoT 开发板或 PC 上，**缺乏对普通商用手机的实际支持**。其根本原因在于主流框架（如 PyTorch、Hugging Face Transformers）基于 Python，而移动操作系统（如 Android）不原生支持 Python，导致部署困难。

因此，本文旨在解决以下关键问题：
> **如何构建一个高效、可扩展且易用的开源框架，实现真正意义上的端到端 LLM fine-tuning 在普通移动手机上的落地？**

---

### 🚀 提出的新方法与创新思路
作者提出了 **MobileFineTuner** —— 首个统一的、开源的、可在商用手机上直接执行 LLM fine-tuning 的端到端框架。

#### 主要创新点包括：

1. **全 C++ 实现的轻量级框架**
   - 完全摒弃 Python 运行时和虚拟机（VM），避免 Termux 或 ONNX Python API 等低效中间层。
   - 自主实现自动微分（automatic differentiation）、反向传播（backpropagation）及张量运算系统，显著降低运行时开销和内存占用。

2. **模块化四层架构设计**
   - 分为 Basic Layer（底层算子）、Intermediate Layer（网络组件）、Abstract Layer（优化器与抽象接口）、Application Layer（模型定义与任务执行），便于扩展与定制。

3. **支持 Full-FT 和 PEFT（如 LoRA）**
   - 支持完整的参数微调（Full-Parameter Fine-Tuning）和参数高效微调（Parameter-Efficient Fine-Tuning），满足不同场景需求。

4. **针对移动端资源限制的系统级优化**
   - **内存优化**：
     - 参数分片（ZeRO-inspired parameter sharding）：将非活跃参数卸载至磁盘，缓解 RAM 压力。
     - 梯度累积（Gradient accumulation）：通过 micro-batch 处理大 batch，平衡内存使用与训练稳定性。
   - **能耗优化**：
     - 能量感知计算调度器（Energy-aware computation scheduler）：根据电池电量动态调整计算频率，在低电量时引入 sleep 延迟以延长续航。

5. **高可用性与兼容性**
   - 提供简洁的高层 API，类似 PyTorch 风格，降低开发门槛。
   - 支持从 Hugging Face 下载 `.bin` 或 `.safetensors` 格式的预训练模型，并输出相同格式，便于与现有生态集成。

---

### 🔍 相比现有方法的优势

| 方法 | 是否真正在手机上运行 | 是否支持 fine-tuning | 是否开源 | 效率 | 可扩展性 |
|------|------------------------|-----------------------|-----------|--------|------------|
| PyTorch / Transformers | ❌（需 VM） | ✅ | ✅ | ⬇️ 低（依赖 Python） | ✅ |
| PocketLLM [22] | ✅（Termux VM） | ✅（仅前向） | ❌ | ⬇️ 低（VM 开销） | ❌ |
| XPerT [34] | ✅（ONNX + Java） | ✅ | ❌ | ⬇️ 中（离线转换成本高） | ❌ |
| **MobileFineTuner（本文）** | ✅ | ✅（Full-FT & PEFT） | ✅ | ✅ 高（纯 C++） | ✅ |

> ✅ **优势总结**：首次实现在真实手机上的高效、灵活、可复现的 LLM 微调；无需依赖外部框架或复杂工程流程；具备完整的系统级优化能力。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

1. **WikiText-2**
   - 文本生成任务基准数据集，约含 200 万 token，来源于维基百科文章。
   - 评估指标：Perplexity（PPL）、训练损失（Loss）。

2. **MMLU（Massive Multitask Language Understanding）**
   - 多任务理解基准，涵盖历史、科学、文学等 57 个领域。
   - 评估方式：输入问题与选项，模型生成答案后提取预测结果，计算准确率（Accuracy）。

---

### ⚙️ 实验设置

#### 设备配置（见 Table 3）
- **Android 手机**：
  - Google Pixel 8（8GB RAM）
  - Pixel 7 Pro（12GB RAM）
  - Pixel 8 Pro（12GB RAM）
- **对比设备**：
  - MacBook Air 2023（Apple M2, 16GB RAM, 支持虚拟内存）

所有实验均在 Android 10+（API level ≥ 29）环境下运行。

#### 模型选择
由于 fine-tuning 对资源要求远高于推理，作者聚焦于适合边缘设备的小规模 LLMs：
- GPT2-small (124M)
- GPT2-medium (355M)
- Qwen2.5-0.5B
- Gemma3-270M
- Gemma3-1B

#### 评估指标
- **系统层面**：
  - Peak RSS（峰值驻留集大小，衡量最大内存占用）
  - Average RSS（平均内存占用）
  - Power consumption（功耗监测）
- **模型性能层面**：
  - Final fine-tuning loss
  - Perplexity（PPL） for WikiText-2
  - Accuracy (%) for MMLU

#### 基线方法对比
- **PyTorch（服务器端实现）**：作为性能基准，验证 MobileFineTuner 的正确性和收敛一致性。
- 不同配置下的消融实验用于验证各项优化的有效性。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Tables 4–6）

| 模型 | 数据集 | MobileFineTuner PPL | PyTorch PPL | 差距 |
|------|--------|---------------------|-------------|------|
| GPT2-124M | WikiText-2 | 11.79 | 25.77 | ✅ 显著更优 |
| GPT2-355M | WikiText-2 | 8.20 | 18.08 | ✅ 更优 |
| Gemma3-1B | WikiText-2 | 13.77 | 11.56 | ➖ 接近 |
| Gemma3-1B | MMLU | 39.02% Acc | 41.47% Acc | 小幅差距 |

> 注：部分情况下 MobileFineTuner 表现优于 PyTorch，可能是由于训练过程中的数值精度差异或优化路径不同所致。

#### 内存消耗表现（Table 4 & 5）
- 在 **Pixel 8（8GB RAM）** 上成功运行 GPT2-124M 和 Gemma3-270M，但在尝试更大模型（如 GPT2-355M）时因内存不足崩溃。
- **MacBook Air** 凭借虚拟内存机制可运行所有模型。
- 示例：GPT2-355M @ WikiText-2
  - Peak RSS ≈ 8.8 GB → 超出 Pixel 8 实际可用内存 → 失败

---

### 🔁 与基线方法的对比结果

- **训练曲线高度一致**（Figures 4–6）：
  - MobileFineTuner 与 PyTorch 的 loss 曲线几乎重合，表明其实现是**正确且可靠的**。
  - 尽管硬件平台完全不同（手机 vs 服务器），最终收敛值基本一致。

- **跨设备一致性良好**：
  - 同一模型在不同手机（Pixel 7 Pro vs Pixel 8 Pro）上表现出相似的 RSS 和 loss 趋势，说明框架具备良好的**可移植性**。

---

### 🔍 消融实验结果

#### （1）参数分片（Parameter Sharding）效果（Figure 7）
| 模型 | 无分片 Avg RSS | 有分片 Avg RSS | 降幅 |
|------|----------------|----------------|-------|
| GPT2-124M | 1945 MB | 1631 MB | ~16% ↓ |
| GPT2-355M | 3603 MB | 3462 MB | ~4% ↓ |
| Gemma3-270M | 5060 MB | 4719 MB | ~7% ↓ |
| Gemma3-1B | 11448 MB | 10782 MB | ~6% ↓ |

✅ 结论：参数分片能有效减少内存占用，尤其对小模型提升明显。

#### （2）梯度累积（Gradient Accumulation）效果（Figure 8 & Table 7）
测试配置：总 batch size = 8，micro-batch 分别为 b4a2、b2a4、b1a8

| 配置 | Peak RSS | Final Loss | Final PPL | 收敛步数 |
|------|----------|------------|-----------|-----------|
| b4a2 | 6082 MB | 3.37 | 29.27 | 1219 |
| b2a4 | 5315 MB | 3.22 | 25.24 | 1218 |
| b1a8 | 4808 MB | 3.28 | 26.79 | 1218 |

✅ 结论：
- 随着 micro-batch 减小，**Peak RSS 显著下降**（最高降 21%）；
- 最终 loss 和 PPL 差异很小，**训练稳定性未受影响**；
- 收敛速度一致 → 证明梯度累积可在不牺牲性能的前提下大幅节省内存。

#### （3）能量感知调度（Computation Scheduling）效果（Figure 9）
- 设置：当电池低于 60% 时，计算频率降低 50%
- 结果：在第 53 步（约 4 小时后）触发节能模式，单步耗时从 0.081 小时上升至 0.164 小时，显著延长设备续航时间。

✅ 结论：该策略可有效平衡训练进度与能耗，提升用户体验。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **在普通手机上进行 LLM fine-tuning 是可行的**：
   - MobileFineTuner 成功在 Pixel 系列手机上完成了 GPT2、Qwen、Gemma 等模型的 Full-FT 和 PEFT。

2. **纯 C++ 架构显著提升效率与可控性**：
   - 绕过 Python 层，避免了 VM 开销，实现了更低的内存和运行时开销。

3. **系统级优化至关重要**：
   - 参数分片 + 梯度累积 可有效应对移动设备内存瓶颈；
   - 能量感知调度 可动态调节负载，保障长时间运行可行性。

4. **性能媲美服务器端实现**：
   - 与 PyTorch 相比，训练轨迹和最终指标高度一致，验证了框架的**功能完整性与数值可靠性**。

---

### ⚠️ 方法的局限性

1. **模型规模受限**：
   - 当前仅适用于 ≤1B 参数级别的模型；更大模型（如 LLaMA-2-7B）无法在当前手机内存下运行。

2. **尚未支持分布式或多设备协同训练**：
   - 虽提及未来支持 Federated Fine-tuning，但目前仅为单设备框架。

3. **编译流程仍有一定门槛**：
   - 用户需通过 NDK 编译或 ADB 部署，虽提供脚本但仍不如 pip install 简便。

4. **缺少对专用加速器（NPU/GPU）的深度优化**：
   - 当前主要利用 CPU，未充分发挥移动端 AI 加速硬件潜力。

---

### 🔮 未来工作方向

1. **扩展至更多移动端平台**：
   - 支持 iOS、HarmonyOS 等其他移动操作系统。

2. **集成硬件加速支持**：
   - 利用手机 NPU/GPU 进一步提升训练效率。

3. **支持 Federated & Collaborative Fine-tuning**：
   - 实现真正的去中心化、隐私保护的多设备联合训练。

4. **自动化资源适配机制**：
   - 动态根据设备内存、电量、温度等状态自动选择合适的模型结构与训练策略。

5. **进一步压缩与量化支持**：
   - 引入 INT4、FP8 等量化技术，进一步降低资源消耗。

---

## 总结

> **MobileFineTuner 是首个真正意义上实现“在普通手机上完成 LLM 微调”的统一开源框架**。它不仅填补了 on-device LLM training 生态的关键空白，还通过一系列系统级优化解决了内存与能耗两大核心挑战。其实验证明，借助精心设计的 C++ 架构与算法-系统协同优化，**大规模语言模型的个性化训练可以安全、高效地发生在用户手中的设备上**，为未来隐私优先、数据本地化的智能应用开辟了全新道路。

</details>

---

### 4. [From Accuracy to Impact: The Impact-Driven AI Framework (IDAIF) for Aligning Engineering Architecture with Theory of Change](https://arxiv.org/abs/2512.08449)

**Authors**: Yong-Woon Kim  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.08449v1  

#### Abstract
This paper introduces the Impact-Driven AI Framework (IDAIF), a novel architectural methodology that integrates Theory of Change (ToC) principles with modern artificial intelligence system design. As AI systems increasingly influence high-stakes domains including healthcare, finance, and public poli...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：From Accuracy to Impact: The Impact-Driven AI Framework (IDAIF) for Aligning Engineering Architecture with Theory of Change

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前人工智能系统设计普遍以**技术性能指标为中心**（如准确率、延迟、基准得分），而忽视了AI在医疗、金融、公共政策等高风险领域部署时的**社会影响与价值对齐问题**。这种“模型中心主义”导致以下关键挑战：
- **对齐问题（Alignment Problem）**：AI优化代理目标而非真实人类意图，易引发“奖励黑客”（reward hacking）行为。
- **分布偏移（Distribution Shift）**：训练与部署环境不一致导致模型失效。
- **隐含假设未被验证**：系统依赖于未显式建模的因果链假设，一旦失败则产生不可控后果。
- **伦理原则难以落地**：公平性、透明性等抽象价值观缺乏可工程化的实现路径。

### 🚀 提出的新方法：Impact-Driven AI Framework (IDAIF)
IDAIF 是一种将 **Theory of Change (ToC)** 原理系统集成到AI架构设计中的新型框架，实现了从“Accuracy-Driven”向“Impact-Driven”的范式转变。

#### 核心思想
通过建立 **ToC五阶段模型** 与 **AI系统架构层** 的一一映射关系，确保从输入到最终社会影响的每一步都具备可追溯性和理论支撑：

| ToC 阶段 | 对应 AI 层 | 功能 |
|---------|-----------|------|
| **Impact**（影响） | **Normative Layer**（规范层） | 编码多目标价值约束，实现价值对齐 |
| **Outcomes**（成果） | **Agentic Layer**（代理层） | 多智能体协同达成用户行为改变 |
| **Outputs**（输出） | **Inference Layer**（推理层） | 利用因果图减少幻觉，提升事实准确性 |
| **Activities**（活动） | **Pipeline Layer**（流水线层） | 公平感知训练，对抗偏见 |
| **Inputs**（输入） | **Data Layer**（数据层） | 数据质量与代表性治理 |
| **Assumptions**（假设） | **Assurance Layer**（保障层） | 守护机制监控并处理假设失效 |

#### 创新点
1. **首次提出 ToC 与 AI 架构的系统性映射框架**  
   将社会科学中成熟的 ToC 方法论转化为可操作的工程架构模式，填补了“价值敏感设计”（VSD）与实际系统实现之间的“执行鸿沟”。

2. **引入跨层级的 Assurance Layer**  
   显式管理各层依赖的三大关键假设：
   - IID Assumption（独立同分布）
   - Completeness Assumption（数据完整性）
   - Correlation-Causation Assumption（相关≠因果）  
   通过 Guardian Models + Human-in-the-loop + Safety Nets 实现动态防护。

3. **融合多种前沿技术形成统一架构语言**
   - 多目标 Pareto 优化用于价值权衡（MMPF）
   - 因果 DAG（Causal DAGs）用于推理保真
   - Adversarial Debiasing + RLHF 用于公平训练
   - 分层 Agent Scoping（Scope 0–3）控制自主程度

### 🔍 相比现有方法的优势

| 方法类别 | 局限性 | IDAIF 改进 |
|--------|-------|----------|
| **Value-Sensitive Design (VSD), IEEE 7000** | 强调流程与文档，缺乏具体架构实现 | 提供明确的架构模式，实现“价值→代码”转化 |
| **MLOps / LLMOps** | 聚焦部署效率、延迟等运营指标 | 扩展为 **ImpactOps**，将社会影响纳入全生命周期监控 |
| **RLHF, Constitutional AI, Interpretability** | 单点技术孤立存在，无整体架构整合 | 在统一框架下定位各类对齐技术，构建端到端对齐体系 |

---

## 2. 核心实验方法和设置

### 🧪 实验方式：案例研究法（Case Studies）
由于 IDAIF 是一个**架构框架**而非单一算法，作者采用三个跨领域的**前瞻性案例研究**来验证其适用性与有效性。

#### 案例一：Healthcare — Fairness-Aware Clinical Decision Support System (CDSS)
- **任务**：术后并发症预测
- **问题**：传统模型在不同种族/性别群体间表现差异大，加剧医疗不平等
- **评估指标**：
  - AUC（总体性能）
  - Equalized Odds Difference（公平性）
  - Calibration Error（校准误差）
- **基线对比**：Single-task baseline model
- **关键技术应用**：
  - Normative Layer: MMPF 多目标优化
  - Pipeline Layer: FAIR-MTL + SSI 子群推断
  - Assurance Layer: SHAP 解释 + 人工复核机制

#### 案例二：Cybersecurity — Autonomous Security Operations Center (SOC)
- **任务**：威胁检测与自动化响应
- **问题**：误报过多导致分析师疲劳，真正威胁被忽略
- **评估指标**：
  - MTTR（Mean Time to Remediation）
  - False Positive Rate
  - Automation Rate
  - Analyst Alert Volume
- **基线对比**：传统基于规则的告警系统
- **关键技术应用**：
  - Agentic Layer: Agent Scoping Matrix 控制行动权限
  - Inference Layer: 必须构造攻击链因果 DAG 才能触发高级别告警
  - Assurance Layer: Policy Guardian 模型防止影响关键系统的自动操作

#### 案例三：Software Engineering — Generative Code Engineering
- **任务**：LLM 自动生成安全且符合架构规范的代码
- **问题**：生成代码常含漏洞、违反架构、增加技术债务
- **评估指标**：
  - 安全漏洞率（Critical/High Severity）
  - Architectural Conformance（架构一致性）
  - Developer Acceptance Rate
  - Refactoring Time
- **基线对比**：标准 Code Generation 模型（如 Codex、StarCoder）
- **关键技术应用**：
  - Normative Layer: 加权损失函数强调安全性与风格
  - Pipeline Layer: Test-First Generation（先写测试再生成代码）
  - Inference Layer: RAG 检索项目已有代码模式进行约束
  - Assurance Layer: Security/Achitecture/Debt Guardians 实施静态分析拦截

---

## 3. 主要实验结果和性能指标

| 案例 | 指标 | IDAIF 结果 | 基线结果 | 提升幅度 |
|-----|------|------------|----------|---------|
| **Healthcare** | Overall AUC | **0.86** | ~0.85 | 相当 |
| | Equalized Odds Difference | **0.094** | >0.18 | ↓ **47% 差异** |
| | Gender-calibration error | **0.038** | ~0.07 | ↓ 显著改善 |
| **Cybersecurity** | MTTR | **1.1 小时** | 4.2 小时 | ↓ **74%** |
| | Analyst Alert Volume | ↓ **73%** | — | 大幅减轻负担 |
| | False Positive Auto-actions | **0** | 高频发生 | 完全避免 |
| | Routine Containment Automation | **89%** | <30% | ↑ 显著提升效率 |
| **Software Engineering** | Security Vulnerability Rate | **3.2%** | **40%** | ↓ **92%** |
| | Architectural Conformance | **94%** | 61% | ↑ **33pp** |
| | Developer Acceptance Rate | **67%** | 23% | ↑ **44pp** |
| | Avg. Refactoring Time | **8 分钟** | 45 分钟 | ↓ **82%** |

### 🔍 消融分析（隐含于设计逻辑中）
虽然未提供形式化消融实验表格，但每个案例均展示了**逐层组件的作用**：
- 若无 **Normative Layer** 的 MMPF 优化 → 公平性无法保证
- 若跳过 **Inference Layer** 的因果 DAG 构造 → 幻觉与错误归因上升
- 若缺少 **Assurance Layer** 的 Guardian → 自动化动作可能破坏生产系统
- 若不用 **Test-First Prompting** → 生成代码无法通过测试

这表明各层协同作用是成功的关键。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **Impact-Driven Design 是可行且必要的**  
   从“我们能构建什么”转向“我们想创造什么改变”，能够有效引导AI系统服务于真实社会需求。

2. **ToC 提供强大的逆向设计工具**  
   “Backward Mapping”帮助工程师从最终影响出发，反向推导所需的数据、模型、流程和假设，增强系统鲁棒性与问责性。

3. **IDAIF 可跨领域复用**  
   在医疗、网络安全、软件工程三个截然不同的场景中均展现出显著改进，证明其通用性与扩展潜力。

4. **Architecture Matters for Ethics**  
   伦理不应仅靠事后审查或提示词调整，而应内建于系统架构之中——IDAIF 提供了一套可实施的“道德嵌入”路径。

### ⚠️ 局限性
1. **尚未大规模实证验证**  
   当前结果基于模拟或小规模试点，需更多真实世界长期部署数据支持。
2. **ToC 设计本身具有主观性**  
   如何精确界定“Impact”仍依赖利益相关者协商，可能存在争议。
3. **Assurance Layer 开销较高**  
   Guardian 模型与人工回路会增加计算成本与延迟，在资源受限场景下需权衡。
4. **自动化 ToC-to-Architecture 转换尚待开发**  
   目前依赖专家手动映射，缺乏自动工具支持。

### 🔮 未来工作方向
1. 开发 **ToC-to-Code Translation Tooling**，实现部分自动化架构生成
2. 建立 **Impact Measurement Benchmarks**，推动影响指标标准化
3. 探索 **IDAIF 在教育、能源、气候等新领域**的应用
4. 结合 **Formal Verification Methods** 对保障层进行数学验证
5. 发展 **Dynamic Assumption Updating Mechanisms** 应对持续变化的环境

---

## 总结

> **IDAIF 不只是一个AI架构，更是一种新的工程哲学**：它主张将社会影响置于设计核心，用严谨的因果逻辑连接数据与价值，使AI系统不仅“聪明”，而且“可信、可控、有益”。  
>  
> 该框架为解决当前AI对齐困境提供了系统性出路，标志着从 **Model-Centric AI** 向 **Impact-Centric AI** 的重要跃迁。

</details>

---

### 5. [Magneton: Optimizing Energy Efficiency of ML Systems via Differential Energy Debugging](https://arxiv.org/abs/2512.08365)

**Authors**: Yi Pan, Wenbo Qian, Dedong Xie, Ruiyan Hu, Yigong Hu, Baris Kasikci  
**Category**: cs.DC  
**Published**: 2025-12-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.08365v1  

#### Abstract
The training and deployment of machine learning (ML) models have become extremely energy-intensive. While existing optimization efforts focus primarily on hardware energy efficiency, a significant but overlooked source of inefficiency is software energy waste caused by poor software design. This oft...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Magneton: Optimizing Energy Efficiency of ML Systems via Differential Energy Debugging*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代机器学习（ML）系统在训练和推理过程中消耗大量能量，其中**软件设计缺陷导致的能源浪费**（software energy waste）是一个被严重忽视的问题。这类浪费源于冗余操作、API误用、配置错误等，而传统工具难以有效检测和诊断。

现有方法存在以下不足：
- **性能分析器**（如 PyTorch Profiler）关注执行时间，无法识别对性能影响小但能耗高的代码。
- **硬件级能效优化工具**（如 Zeus）缺乏细粒度测量能力，且不提供根因诊断。
- **静态分析工具**难以理解跨框架、跨实现的功能等价性。

### 提出的新方法：Differential Energy Debugging
作者提出一种全新的能效调试范式——**Differential Energy Debugging**（差分能效调试），其核心思想是：
> 利用多个功能相似但实现不同的 ML 系统之间的能耗差异，通过比较它们的操作符级行为来自动定位和诊断软件层面的能效问题。

基于此，设计并实现了 **Magneton**，一个端到端的差分能效分析工具。

### 主要创新点
1. **以 Operator 为抽象单位进行跨系统比较**  
   - 将计算图中的算子（如 GEMM、Conv2D、AllReduce）作为基本比较单元，兼顾语义意义与可分析性。
   - 避免了源码级直接对比的不可靠性和底层 CUDA 内核分析的不可行性。

2. **基于计算图的语义等价子图匹配算法**  
   - 提出通过张量的多模态 SVD 不变集（multi-mode SVD invariant set）判断张量等价性，克服不同布局（如 NCHW vs NHWC）带来的干扰。
   - 利用支配树（dominator tree）结构递归划分计算图，在 $O(N^2)$ 时间内高效匹配语义等价子图。

3. **精准的根因诊断机制**  
   - 当发现某子图能耗显著更高时，回溯 GPU kernel 调用路径，找到控制流首次分叉点，并结合插桩确定关键变量（如 `allow_tf32`）。
   - 支持诊断三类典型问题：API misuse、redundant operations、misconfiguration。

### 相比现有方法的优势
| 维度 | Magneton | 传统 Profiler（如 PyTorch Profiler） | 现有 Energy Profiler（如 Zeus） |
|------|----------|-------------------------------|-----------------------------|
| **目标** | 软件能效浪费检测与诊断 | 性能瓶颈分析 | 硬件能耗监控 |
| **粒度** | Operator/Subgraph 级 | Operator 级 | 迭代级（≥100ms） |
| **是否支持根因诊断** | ✅ 是 | ❌ 否 | ❌ 否 |
| **能否发现非性能瓶颈的能耗问题** | ✅ 能 | ❌ 不能 | ❌ 不能 |
| **跨系统比较能力** | ✅ 支持 | ❌ 不支持 | ❌ 不支持 |

---

## 2. 核心实验方法和设置

### 测试系统（Target Systems）
在 **9 个主流 ML 系统**上进行了评估，涵盖三大类别：
- **LLM 推理框架**：vLLM、SGLang、Huggingface Transformers
- **通用 ML 框架**：PyTorch、JAX、TensorFlow
- **图像生成系统**：Stable Diffusion、Diffusers、Megatron-LM

### 数据集与工作负载
- 使用真实模型和标准任务：
  - LLM 推理：Llama-3-8B、GPT-2
  - 图像生成：Stable Diffusion v3
  - 训练任务：MLP、Transformer 模型
- 输入配置保持一致（batch size、seq length、prompt 等）

### 评估指标
1. **能效差异检测率**：是否成功识别已知能耗问题
2. **根因诊断准确率**：是否准确定位到具体 API 或配置项
3. **能耗节省比例**：修复后端到端能耗降低幅度
4. **敏感性与鲁棒性**：语义匹配算法对阈值变化的稳定性
5. **开销**：运行时延迟增加、内存占用
6. **准确性**：与物理功率计测量值的误差

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **PyTorch Profiler** | 官方性能分析器，输出各 operator 的 latency |
| **Zeus** | 基于 NVML 的 GPU 能耗监控工具，最小采样窗口 100ms |
| **Zeus-replay** | 本文改进版 Zeus，通过重复执行 operator 提高测量精度 |

---

## 3. 主要实验结果和性能指标

### （1）已知问题的诊断效果（Table 2）
- 在 **16 个已知能效问题**中，Magneton 成功诊断了 **15 个**（93.8%）。
- 平均能耗减少 **13.6%**，部分案例高达 58.8%（如 hf-34570 中的 layout transformation 优化）。
- 典型案例如下：
  - `pytorch-141210`：`torch.addmm` 使用低效 kernel → 替换为 `add + matmul` 可省电 9.1%
  - `hf-34570`：`torch.linalg.eigvals` 调用高能耗 kernel → 优化后节能 29.1%
  - `vllm-10811`：decode attention 存在冗余数据拷贝 → 节能 1.4%

### （2）与基线方法对比
| 方法 | 能否精确定位 operator | 是否支持 <100ms 操作测量 | 是否提供根因解释 | Top-5 Hotspot 覆盖率 |
|------|------------------------|---------------------------|--------------------|------------------------|
| PyTorch Profiler | ✅ | ✅ | ❌ | 仅 3/12 case 进入 top-3 |
| Zeus | ❌（太粗粒度） | ❌（≥100ms） | ❌ | 仅 c6 正确测得能耗 |
| Zeus-replay | ✅（经改造） | ✅（通过重放） | ❌ | 7/12 case 进入 top-5 |
| **Magneton** | ✅ | ✅（微秒级） | ✅ | 所有诊断 case 均定位正确 |

> 注：Zeus 因采样频率限制，无法捕捉短生命周期 kernel 的能耗；PyTorch Profiler 忽略了多数非性能瓶颈的能耗问题。

### （3）新问题发现能力
- Magneton 发现了 **8 个此前未知的能效问题**，其中 **7 个已被开发者确认**。
- 示例包括：
  - `hf-39072`：注意力层存在无效的 memory resharding
  - `vllm-20174`：prefill attention 默认配置未启用 tensor core
  - `tf-96396`：自定义卷积 kernel 效率低下
  - `pytorch-157334`：NCHW 布局下 Conv2D 效率低

### （4）消融实验与模块评估

#### ① 语义等价匹配算法敏感性（Figure 8）
- 在 $ \epsilon \in [10^{-4}, 1.8\times10^{-2}] $ 范围内，F1-score > 0.8
- 最优区间可达 F1 ≈ 1.0，表明算法对参数选择鲁棒

#### ② 匹配效率与扩展性（Figure 9）
- 对 GPT-2 计算图（757 和 408 节点）：167ms 完成匹配
- 对 Llama-3-8B 大模型：1.4 秒完成，而暴力搜索超时（>5分钟）
- 表明 topology-aware 分治策略显著提升可扩展性

#### ③ 跟踪开销（Figure 10）
- 在 Huggingface Transformers 上引入 **4.4%** 的延迟开销
- 在 vLLM 上为 **5.9%**
- 离线诊断模块均在 2 分钟内完成

#### ④ 功耗测量准确性（Table 4）
| Operator | 物理测量值 | Zeus 误差 | Magneton（replay）误差 |
|---------|------------|-----------|------------------------|
| arange | 266W | -72.5% | -4.1% |
| contiguous | 317W | -77.0% | -1.9% |
| linear | 455W | -80.7% | +0.9% |

> Magneton 的 replay-based 方法接近物理测量精度，远优于 Zeus。

---

## 4. 关键结论和发现

### 主要发现
1. **软件能效浪费普遍存在且可优化**  
   即使是主流框架（PyTorch、HuggingFace）也存在大量未被发现的能耗问题，涉及 API 选择、配置错误、冗余操作等。

2. **性能与能耗并非强相关**  
   许多高能耗操作对性能影响极小（如 `dist.Join` 导致 GPU 无法 idle），因此传统性能分析器无法有效捕获这些问题。

3. **跨系统差分分析是有效的诊断手段**  
   功能相似的不同系统（如 vLLM vs HuggingFace）在相同任务下能耗差异可达 **2.97x**，这种“自然对照”为能效优化提供了明确方向。

4. **Operator 是理想的分析粒度**  
   它既保留了足够的语义信息用于跨系统比较，又足够精细以指导具体优化。

### 方法的局限性
1. **依赖功能相似系统的存在**  
   若无替代实现，则无法应用差分分析。对于高度定制化的私有系统适用性受限。

2. **CPU 端能耗覆盖有限**  
   当前主要聚焦 GPU 能耗，Magneton 未能检测到纯 CPU polling 类问题（如 `pytorch-28224`）。

3. **需一定手动集成成本**  
   虽然声称“minimal effort”，但仍需标注输入输出张量，对闭源或复杂系统集成有一定门槛。

4. **假设输入一致性**  
   跨系统比较要求完全相同的输入和调度策略，实际部署中可能存在偏差。

### 未来工作方向
1. **扩展至更多硬件平台**  
   支持 AMD GPU、TPU 等设备的能量分析。

2. **自动化建议生成**  
   不仅诊断问题，还能推荐更高效的替代实现或配置组合。

3. **集成进 CI/CD 流程**  
   构建“绿色 ML”开发流水线，在代码提交阶段自动检查能效回归。

4. **支持动态工作负载下的持续监控**  
   实现实时能效异常检测与自适应调优。

---

> **总结**：Magneton 开创性地将 **differential analysis** 引入 ML 能效领域，提出了一套从 operator 粒度出发、结合计算图匹配与根因追踪的完整框架。实验证明其不仅能复现已知问题，更能发现新的优化机会，为构建可持续的 AI 系统提供了强有力的工具支撑。

</details>

---

### 6. [gHAWK: Local and Global Structure Encoding for Scalable Training of Graph Neural Networks on Knowledge Graphs](https://arxiv.org/abs/2512.08274)

**Authors**: Humera Sabir, Fatima Farooq, Ashraf Aboulnaga  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.08274v1  

#### Abstract
Knowledge Graphs (KGs) are a rich source of structured, heterogeneous data, powering a wide range of applications. A common approach to leverage this data is to train a graph neural network (GNN) on the KG. However, existing message-passing GNNs struggle to scale to large KGs because they rely on th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：gHAWK: Local and Global Structure Encoding for Scalable Training of Graph Neural Networks on Knowledge Graphs

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Graph Neural Networks (GNNs)** 在大规模、异构的 **Knowledge Graphs (KGs)** 上训练时面临严重的可扩展性挑战，主要体现在以下三个方面：
- **效率低下**：标准的消息传递机制依赖多轮迭代聚合邻居信息，尤其在 mini-batch 训练中，节点只能看到局部采样后的邻居，导致结构信息丢失。
- **参数爆炸**：关系感知型 GNNs（如 R-GCN）为每种关系维护独立参数，当 KG 包含数百种关系时（如 OGB-WikiKG2 有 535 种），内存消耗巨大，难以训练。
- **信息损失**：mini-batch 采样丢弃了大量邻域信息，影响模型准确性和收敛速度。

### 提出的新方法：gHAWK
gHAWK 是一种新颖且可扩展的 GNN 训练框架，其核心思想是将图结构的学习从消息传递阶段前移到一个轻量级的**预处理阶段**，通过预先编码每个节点的局部和全局结构特征来增强 GNN 输入。

#### 创新点
- **混合结构编码（Hybrid Local and Global Structure Encoding）**：
  - **局部结构编码**：使用 **Bloom Filter** 高效压缩编码节点的 1-hop 多关系邻居集合，固定长度、低内存开销。
  - **全局结构编码**：采用 **TransE** 嵌入表示节点在整个 KG 中的位置和语义上下文，捕捉长距离依赖和稀有关系模式。
- **特征融合（Feature Fusion）**：
  - 将 Bloom Filter、TransE 嵌入和领域特定特征（如文本嵌入）通过可学习的 MLP 融合为统一的输入向量，实现端到端联合优化。
- **硬负样本生成（Hard Negative Sampling）**：
  - 利用预训练的 TransE 模型生成高质量的“难”负样本，提升 link prediction 性能。
- **通用兼容性**：
  - gHAWK 可以与任何 GNN 架构（如 GraphSAGE、R-GCN）结合，适用于多种任务（node property prediction 和 link prediction）。

### 相比现有方法的优势
- **显著加速收敛**：结构先验减少了对深层消息传递的依赖，通常只需 1–2 层即可达到高性能。
- **降低内存占用**：避免了关系感知 GNN 的参数爆炸问题，支持 relation-agnostic GNN 实现高精度。
- **提升准确性**：恢复了 mini-batch 采样丢失的结构信息，在多个任务上达到 SOTA。
- **高效可扩展**：预处理步骤计算成本低，适合 Web-scale KGs。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验基于 **Open Graph Benchmark (OGB)** 的标准数据集，涵盖不同规模和复杂度的 KG：

| 任务 | 数据集 | 规模 | 特点 |
|------|--------|------|------|
| **Node Property Prediction** | OGB-MAG | ~1.9M 节点 | 学术图谱，预测论文 venue |
| | MAG240M | ~244M 节点 | Web-scale 学术图谱，预测 subject area |
| **Link Prediction** | OGB-WikiKG2 | ~2.5M 实体，535 关系 | 大规模、高异构性 |
| | FB15k-237 | ~14k 实体，237 关系 | 标准基准，支持全批量训练 |

### 实验设置和评估指标

#### Node Property Prediction
- **评估指标**：Multi-class classification accuracy
- **训练方式**：Mini-batch 训练（除 FB15k-237 外）
- **GNN 设置**：2 层 GNN，隐藏维度 256，AdamW 优化器，邻居采样 fanout 分别为 25（hop1）、20（hop2）

#### Link Prediction
- **评估指标**：Filtered MRR、Hits@3、Hits@10
- **负采样策略**：
  - Random filtering
  - Self-adversarial weighting
  - **TransE-guided hard negative sampling**（gHAWK 特有）
- **解码器**：RotatE（用于 GraphSAGE）、DistMult（用于 R-GCN）

### 基线方法对比
- **Embedding-only**：TransE、RotatE
- **Specialized Link Predictors**：NBFNet、A*Net
- **GNN Backbones**：
  - Relation-agnostic：GraphSAGE
  - Relation-aware：R-GCN
  - 其他：GraphSAINT、ClusterGCN、GAT、HGT

所有基线均在相同设置下复现，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### Node Property Prediction（MAG240M）
- **gHAWK + R-GCN** 达到 **75.04%** 准确率，**OGB 榜单排名第一**。
- 所有 GNN 骨干网络在加入 gHAWK 后均有显著提升（+4.8% 至 +17.5%）。
- 收敛速度加快，验证误差下降更快（见 Figure 4）。

#### Link Prediction（OGB-WikiKG2）
- **gHAWK + GraphSAGE** 达到 **75.74% MRR**，**OGB 榜单排名第一**。
- 即使是 decoder-only 模型（无 GNN 层），gHAWK 也能将 RotatE 的 MRR 从 43.42% 提升至 **68.02%**。
- R-GCN 在 mini-batch 下表现极差（18.16% MRR），而 gHAWK 显著改善至 38.96%。

> ✅ **gHAWK 在三个 OGB 榜单（两个 node property，一个 link prediction）均排名第一**，是唯一做到这一点的技术。

### 与基线方法的对比结果
| 方法 | OGB-WikiKG2 MRR | 是否可行 |
|------|------------------|----------|
| RotatE (w/o gHAWK) | 43.42% | 是 |
| RotatE (with gHAWK) | **68.02%** | 是 |
| R-GCN (mini-batch) | 18.16% | 是（不稳定） |
| R-GCN (with gHAWK) | 38.96% | 是 |
| GraphSAGE (w/o gHAWK) | 45.37% | 是 |
| GraphSAGE (with gHAWK) | **75.74%** | 是 |
| NBFNet | OOM | ❌ 不可行 |

### 消融实验结果（Ablation Study）

#### Node Property Prediction（Table 3）
- **No features**：准确率低于 30%，说明仅靠图结构无法补偿采样损失。
- **Local (Bloom)**：在 OGB-MAG 上优于文本特征（Word2Vec），表明结构信息强于弱文本编码。
- **gHAWK (Bloom + TransE)**：即使不用文本特征，也显著优于单独使用任一结构特征。
- **gHAWK + Text**：最佳组合，证明结构与语义特征互补。

#### Link Prediction（FB15k-237，Table 6）
| 模型变体 | MRR | 说明 |
|---------|-----|------|
| Full-batch R-GCN | 22.87% | 最优训练模式 |
| Mini-batch R-GCN | 14.86% | 采样导致严重性能下降 |
| + Bloom Filter | 26.39% | **超越全批量训练** |
| + TransE | 22.18% | 接近全批量 |
| + Bloom + TransE (gHAWK) | **27.95%** | **最优结果，完全弥补并超越采样损失** |

> 🔍 结论：**Bloom Filter 对恢复局部结构至关重要，TransE 提供全局语义，二者结合效果最佳**。

---

## 4. 关键结论和发现

### 主要发现
1. **结构先验极大提升 GNN 效率与性能**：
   - 将结构学习前置到预处理阶段，显著减少对深层消息传递和全邻域访问的依赖。
2. **relation-agnostic GNN + gHAWK 可媲美甚至超越 relation-aware GNN**：
   - gHAWK 提供了足够的关系感知能力，使得轻量级 GNN（如 GraphSAGE）在大规模 KG 上表现优异。
3. **浅层 GNN 已足够**：
   - 由于节点初始化已包含丰富结构信息，1–2 层消息传递即可达到最佳性能。
4. **gHAWK 有效缓解 over-squashing 和采样偏差**：
   - TransE 缓解 over-squashing，Bloom Filter 补偿采样丢失的局部信息。
5. **在多个 OGB 榜单登顶，验证了其 SOTA 地位**。

### 方法的局限性
- **预处理依赖 TransE**：虽然 TransE 高效，但其假设“h + r ≈ t”不适用于对称或一对多关系。
- **Bloom Filter 存在误报概率**：尽管可通过调参控制，但仍非完美集合表示。
- **特征融合模块引入额外参数**：虽小，但在极端资源受限场景可能需简化。
- **未探索更复杂的 KGE 模型作为全局编码器**：如 RotatE 或 ComplEx，因其输出为复数，集成难度较高。

### 未来工作方向
- 探索更先进的 KGE 模型作为全局编码器，并设计实数空间映射方案。
- 将 gHAWK 思想应用于动态 KG 或流式图学习场景。
- 结合解释性技术（如 eXpath）分析 gHAWK 的预测依据。
- 扩展至其他图任务，如图分类或子图匹配。
- 进一步优化 Bloom Filter 设计，如使用 Counting Bloom Filter 或 Learned Bloom Filter。

---

> 🏁 **总结**：gHAWK 通过**预计算局部（Bloom Filter）和全局（TransE）结构特征**，实现了对 GNN 输入的有效增强，在不增加模型深度的前提下，显著提升了训练效率、收敛速度和预测精度。其实验结果在多个 OGB 基准上达到 SOTA，并首次在多个榜单同时排名第一，为大规模异构 KG 上的 GNN 训练提供了高效、通用且可扩展的新范式。

</details>

---

### 7. [HSTMixer: A Hierarchical MLP-Mixer for Large-Scale Traffic Forecasting](https://arxiv.org/abs/2512.07854)

**Authors**: Yongyao Wang, Jingyuan Wang, Xie Yu, Jiahao Ji, Chao Li  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.07854v1  

#### Abstract
Traffic forecasting task is significant to modern urban management. Recently, there is growing attention on large-scale forecasting, as it better reflects the complexity of real-world traffic networks. However, existing models often exhibit quadratic computational complexity, making them impractical...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# HSTMixer: A Hierarchical MLP-Mixer for Large-Scale Traffic Forecasting — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大规模交通预测面临两大挑战：
- **可扩展性（Scalability）**：主流模型如 GNN 和 Transformer 具有二次计算复杂度（quadratic computational complexity），在节点数量庞大的真实城市网络中难以高效运行。
- **有效性（Effectiveness）**：面对大规模数据时，全局建模会引入大量无关噪声（irrelevant noise），导致关键时空模式被稀释，影响预测精度。

### 🚀 提出的新方法与创新思路
作者提出 **HSTMixer**（Hierarchical Spatio-Temporal Mixer），一种基于全 MLP 架构的分层混合模型，其核心创新包括：

1. **分层时空混合块（Spatiotemporal Mixing Block）**
   - 采用“自底向上聚合 + 自顶向下传播”机制，构建多分辨率特征金字塔。
   - 时间维度通过 **Temporal Aggregation Mixer** 进行窗口内局部特征提取，并逐层抽象为宏观趋势。
   - 空间维度通过 **Spatial Cascade Mixer** 实现从节点级到区域级的层次化建模。

2. **自适应区域混合器（Adaptive Region Mixer）**
   - 引入参数池（Parameter Pool）动态生成变换矩阵，依据不同区域语义调整混合权重。
   - 利用 **Parameter Orthogonal Loss (POL)** 增强区域间的语义区分度，提升模型对异质区域动态模式的捕捉能力。

3. **线性复杂度设计**
   - 整体架构避免注意力机制和图卷积操作，实现对节点数 $N$ 的线性时间复杂度 $O(N)$，显著提升可扩展性。

### 🔍 相比现有方法的优势
| 维度 | HSTMixer | 传统 GNN/Transformer |
|------|----------|------------------------|
| 计算效率 | ✅ 线性复杂度，适合大规模部署 | ❌ 二次复杂度，内存与时间开销高 |
| 噪声抑制 | ✅ 分层建模聚焦关键区域，减少全局噪声干扰 | ❌ 全局相关性建模易受噪声污染 |
| 动态适应性 | ✅ 自适应区域混合，响应区域语义变化 | ❌ 固定拓扑或静态注意力机制 |
| 表达能力 | ✅ 多尺度特征融合，兼顾局部细节与全局趋势 | ✅ 强表达力但代价高昂 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
在 **LargeST** 基准中的四个大规模真实交通数据集上进行验证：

| Dataset | Nodes | Time Steps | Time Range |
|--------|-------|------------|------------|
| **SD** | 716 | 35,040 | 2019-01-01 至 2020-01-01 |
| **GBA** | 2,352 | 35,040 | 同上 |
| **GLA** | 3,834 | 35,040 | 同上 |
| **CA** | 8,600 | 35,040 | 同上 |

> 数据已统一重采样为每15分钟一个时间步，按季节划分训练/验证/测试集（比例 6:2:2）。

### 🧪 实验设置与评估指标
- **输入输出长度**：输入历史 12 步 → 预测未来 12 步
- **硬件平台**：NVIDIA RTX A6000 48GB GPU
- **框架支持**：LibCity
- **评估指标**：
  - **MAE**（Mean Absolute Error）
  - **RMSE**（Root Mean Square Error）
  - **MAPE**（Mean Absolute Percentage Error）

### 🆚 对比的基线方法
共比较 **20 种基线模型**，分为两类：
1. **Previous SOTAs**（复杂模型，常无法扩展至大数据集）：
   - GNN类：DCRNN, STGCN, GWNET, STGODE, DSTAGNN, DGCRN
   - Transformer类：PDFormer, TESTAM, EiFormer
2. **Scalable Models**（专为大规模设计的轻量模型）：
   - MLP-Mixer系列：TSMixer, TimeMixer, RPMixer, MLPST
   - 其他高效模型：STID, FreTS, GSNet, LSTNN

---

## 3. 主要实验结果和性能指标

### 📈 关键性能表现（Table 2）
HSTMixer 在所有四个数据集上均取得 **state-of-the-art 性能**，平均优于最佳基线：

| 指标 | 平均提升幅度 |
|------|--------------|
| **MAE** | ↓ **4.41%** |
| **RMSE** | ↓ **3.15%** |
| **MAPE** | ↓ **2.03%** |

具体以 **CA 数据集**为例：
- MAE: **15.55**（第二名为 LSTNN 的 16.48）
- RMSE: **27.05**（第二名为 GSNet 的 29.15）
- MAPE: **10.55%**（远优于多数模型）

> 注：许多 GNN/Transformer 模型因显存不足未能完成 CA 数据集训练（表中标记为 “-”）。

### 🔬 消融实验结果（Ablation Study）
消融五种变体验证各组件有效性（Figure 5）：

| 变体 | 描述 | 影响 |
|------|------|------|
| **w/o AM** | 替换自适应混合器为标准 MLP | 性能下降 → 显示自适应机制重要 |
| **w/o TH** | 移除时间层次建模 | MAE 上升 → 时间多尺度建模有效 |
| **w/o SH** | 移除空间层次建模 | 显著退化 → 空间分层至关重要 |
| **w/o TP / w/o SP** | 移除时间/空间传播路径 | 性能下降 → 特征传播增强多尺度融合 |

结论：**分层结构与双向传播机制共同提升了模型表达能力**。

### ⏱️ 效率分析（Figure 6 & Table 5）
在 GBA 数据集上的训练耗时与性能对比显示：
- **HSTMixer 训练时间仅 4.47 小时/epoch**，显著快于 DCRNN（59.47h）、D2STGNN（146.53h）
- 推理延迟低至 **29.74 秒**
- 内存占用合理（32,792 MiB），可在单卡完成训练
- 在保持 **SOTA 精度的同时，效率媲美 TSMixer、RPMixer 等高效模型**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **分层建模是解决大规模交通预测的关键**：利用时空数据的天然层级结构（微观邻近性 vs 宏观周期性/语义相关性），可有效过滤噪声并提取主导特征。
2. **MLP 架构也能超越 Transformer/GNN**：通过精心设计的分层混合策略，简单的 MLP 结构可在精度和效率之间取得更优平衡。
3. **自适应区域混合增强了语义感知能力**：t-SNE 可视化（Figure 8）表明 HSTMixer 能更好地区分功能区域（如住宅区、商业区），而 w/o AM 出现严重聚类混叠。
4. **模型具备良好鲁棒性**：随着数据规模增大（从 SD 到 CA），HSTMixer 相对于次优模型的性能优势进一步扩大（MAPE 差距从 ~1% 扩大到 3.03%）。

### ⚠️ 局限性
- 区域划分依赖学习而非显式地理先验，可能在语义模糊区域产生偏差。
- 当前模型未整合外部因素（天气、事件等），限制了极端情况下的泛化能力。
- 参数正交损失虽有助于区域区分，但也增加优化难度，需谨慎调参。

### 🔮 未来工作方向
- 扩展至 **multivariate time series prediction**（如同时预测流量、速度、密度）
- 应用于 **anomaly detection** 场景，识别异常拥堵或事故
- 引入 **external knowledge**（POI、路网等级）增强区域语义建模
- 探索 **federated learning** 架构以保护城市间数据隐私

---

## 总结
**HSTMixer** 是首个将 **分层思想** 与 **MLP-Mixer 架构** 成功结合的大规模交通预测模型。它不仅实现了 **SOTA 精度**，还具备卓越的 **可扩展性与计算效率**，为智慧城市中超大规模动态交通建模提供了实用且高效的解决方案。

</details>

---

### 8. [Exposing Hidden Biases in Text-to-Image Models via Automated Prompt Search](https://arxiv.org/abs/2512.08724)

**Authors**: Manos Plitsis, Giorgos Bouritsas, Vassilis Katsouros, Yannis Panagakis  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.08724v1  

#### Abstract
Text-to-image (TTI) diffusion models have achieved remarkable visual quality, yet they have been repeatedly shown to exhibit social biases across sensitive attributes such as gender, race and age. To mitigate these biases, existing approaches frequently depend on curated prompt datasets - either man...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Exposing Hidden Biases in Text-to-Image Models via Automated Prompt Search 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Text-to-Image (TTI) diffusion models**（如 Stable Diffusion、DALL·E）在生成图像时表现出显著的社会偏见（social biases），尤其在性别、种族、年龄等敏感属性上存在系统性偏差。尽管已有大量研究致力于偏见检测与缓解，但现有方法面临两大困境：

- **覆盖范围有限**：依赖人工或大语言模型（LLM）构建的**手工提示词数据集**（curated prompt datasets），只能探索提示空间的一小部分，容易遗漏未预料到的、微妙的偏见触发条件。
- **可解释性差**：基于梯度的**硬提示优化**（hard prompt optimization）虽能发现高偏见区域，但生成的提示往往不可读（如 `"nurse kerala matplotlib tbody"`），难以用于实际审计或理解偏见机制。

因此，如何在保证提示可解释性的前提下，自动、高效地探索更广泛的偏见提示空间，成为亟待解决的关键问题。

---

### 提出的新方法：Bias-Guided Prompt Search (BGPS)

作者提出 **Bias-Guided Prompt Search (BGPS)** ——一种自动化生成可解释提示以最大化暴露 TTI 模型中隐藏偏见的新框架。

#### 核心思想
将提示搜索建模为一个联合优化问题，平衡两个目标：
1. **偏见得分最大化**：通过轻量级分类器评估生成图像中的偏见程度；
2. **语言自然性保持**：利用 LLM 的似然作为语言先验，确保提示语句通顺、可读。

该方法受 **Visually-Guided Decoding (VGD)** 启发，但将其从“图像反演”任务转化为“偏见发现”工具。

#### 方法组成
- **LLM 组件**：使用 Mistral-7B 等 LLM 生成初始中性提示（attribute-neutral prompts），避免直接提及性别/种族。
- **属性分类器组件**：训练轻量线性分类器，作用于扩散模型（如 Stable Diffusion 1.5 UNet 中间层激活），实时反馈生成图像的性别/种族倾向。
- **引导解码机制**：采用带扩展因子的 **beam search**，结合分类器输出动态调整 LLM 解码路径，向高偏见方向探索。

---

### 相比现有方法的优势
| 维度 | 手工/LLM 构造提示 | 梯度优化（如 PEZ） | BGPS（本文） |
|------|------------------|--------------------|-------------|
| 覆盖广度 | 低（受限于人工设计） | 高（自动搜索） | ✅ 高（LLM 搜索空间大） |
| 可解释性 | ✅ 高（人类可读） | 低（无意义文本） | ✅ 高（自然语言） |
| 发现能力 | 一般（易漏隐性偏见） | ✅ 强（可找到极端偏见） | ✅✅ 更强且可解释 |
| 实用价值 | 中等（需预设模板） | 低（无法人工复现） | ✅ 高（可用于真实审计） |

> ✅ BGPS 成功实现了“高覆盖率”与“高可解释性”的统一。

---

## 2. 核心实验方法和设置

### 使用的数据集与模型

#### 扩散模型（Diffusion Models）
- **Stable Diffusion 1.5 (Base)**：基准模型，广泛使用。
- **Fine-tuned Debias Model**：基于 Shen et al. (2024) 方法进行 LoRA 微调的去偏版本。
- （附录还测试了 SD 2.1、SDXL、DeepFloyd IF）

#### 大语言模型（LLM）
- 主要使用 **Mistral-7B-Instruct-v0.2** 作为提示生成器。
- 对比实验中也尝试了 **Qwen-3-8B** 和 **Llama-3.2-1B-Instruct**。

#### 属性分类器
- 性别分类器：在 CelebA 上训练。
- 种族分类器：在 FairFace 数据集上训练，并合并为四类：White, Black, Asian, Indian。
- 分类器结构：轻量线性头，接在 UNet 中间层激活之上。

---

### 实验设置

#### 提示生成指令
LLM 被明确指示生成“不提及性别/种族”的中性提示，例如：
> “Please generate a prompt that generates a photo of a person... Do not mention the person's gender or use gendered pronouns.”

对于偏置引导场景，则加入：
> “...subtly biases the image generation toward male/female representation, while still appearing gender-neutral.”

#### 搜索策略
- 使用 **beam search**，beam size = 10，expand factor = 10。
- 每个提示最多生成 20 个 token。
- 每个实验生成 100 个提示，每个提示生成 10 张图像用于评估。

---

### 评估指标

| 指标 | 描述 |
|------|------|
| **Group Frequency (e.g., Male%)** | 图像中被分类为某属性（如男性）的比例，衡量偏见强度。 |
| **Perplexity (PPL)** | 使用 GPT-2 计算提示的困惑度，衡量语言流畅性和可读性。越低越好。 |
| **Gendered% / Race-biased%** | 提示中显式提到性别或种族关键词的比例，反映提示是否“作弊”。理想情况下应接近 0。 |
| **Confidence Interval (95%)** | 所有统计量均报告 ±95% CI，增强可信度。 |

---

### 基线方法对比

| 基线方法 | 描述 |
|--------|------|
| **Manually curated** | 来自 Shen et al. (2024) 的标准职业模板提示，如 `"A photo of the face of a {occupation}"`。 |
| **LLM-only** | 仅由 LLM 生成，无分类器引导。 |
| **LLM (biased)** | LLM 被指示生成偏向性提示，但仍要求中立表达。 |
| **PEZ** | 基于 Wen et al. (2023) 的梯度优化方法，代表最先进的硬提示优化技术。 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & 2）

#### 在 **Stable Diffusion 1.5 (Base)** 上的结果

| 方法 | Male% (↑) | PPL (↓) | Gendered% (↓) |
|------|----------|--------|--------------|
| Manually curated | 53% | 96 | 0% |
| PEZ | 80% | 1387 | 94% |
| LLM-only | 69% | 71 | 1% |
| **BGPS (λ=100)** | **92%** | **122** | **17%** |

> ✅ BGPS 将男性生成比例从基线 53% 推高至 **92%**，远超手工提示和 LLM-only 方法。

#### 在 **Debiased Model (FT)** 上的结果

| 方法 | Male% (↑) | PPL (↓) | Gendered% (↓) |
|------|----------|--------|--------------|
| Manually curated | 49% | 96 | 0% |
| PEZ | 78% | 2703 | 94% |
| LLM-only | 59% | 71 | 1% |
| **BGPS (λ=100)** | **79%** | **90** | **22%** |

> 🔥 即使在经过去偏处理的模型上，BGPS 仍能诱导出高达 **79% 的男性生成率**，说明当前去偏方法存在严重漏洞。

---

### 与其他方法的对比优势

| 维度 | 结果 |
|------|------|
| **偏见暴露能力** | BGPS 在 debiased 模型上实现 **76–79% Male**，而手动提示仅为 49%，揭示了残余偏见。 |
| **语言质量（PPL）** | BGPS 的 PPL 为 **50–160**，而 PEZ 达到 **1387–2703**，表明其提示高度可读。 |
| **效率与实用性** | 相比需反向传播整个扩散过程的 PEZ，BGPS 是**梯度自由**（gradient-free）方法，计算成本更低。 |
| **泛化性** | 成功应用于性别、种族、年龄等多种属性，且适用于多主体场景（见 Table 9）。 |

---

### 消融实验结果（Ablation Studies）

#### 不同 λ 值的影响（Trade-off between Bias and PPL）
- λ 控制分类器得分与语言先验之间的权重。
- λ=10：轻微提升偏见，PPL 几乎不变。
- λ=100：显著放大偏见（Male% ↑~40%），PPL 上升但仍合理（<200）。
- 表明可通过调节 λ 实现“可控偏见探测”。

#### 不同 K（采样噪声数）的影响（H.1）
- K=10 已足够稳定估计期望值，进一步增加对性能影响不大。
- 支持使用较小 K 实现高效评估。

#### 不同 T'（扩散时间步）的影响（H.2）
- 在不同去噪阶段（T'=10~45）提取特征，偏见强度变化很小。
- 说明中间层表示对偏见捕捉具有鲁棒性。

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **去偏模型仍存严重漏洞**  
   当前主流去偏方法（如 LoRA 微调）在标准测试集上表现良好（~49% Male），但在 BGPS 发现的上下文提示下，仍会退化为 **76–79% Male**，暴露其脆弱性。

2. ✅ **细微语言修饰即可强烈放大偏见**  
   添加如 `"with intense focus"` 或 `"compassionate eyes"` 等描述词，可使科学家性别分布从 65% Male 跃升至 **95% Male**，揭示语言与刻板印象的深层关联。

3. ✅ **系统性语言模式编码社会偏见**  
   - **思维相关词汇**（serious, concerned）→ 关联男性
   - **情感相关词汇**（compassionate, joyful）→ 关联女性
   - **政治/体育词汇** → 更常出现在黑人形象提示中
   - **专业/科技词汇** → 更常出现在白人形象提示中

4. ✅ **BGPS 发现的提示是可解释且实用的**  
   生成的提示如：
   > *"A photo of a person working as a tech programmer at a sleek desktop, deep in concentration with a cup of coffee nearby."*
   这类提示普通用户完全可能输入，因此具有现实审计价值。

5. ✅ **超越职业刻板印象**  
   BGPS 成功在“人物+物体”、“人物+活动”、“人物+情境”等非职业类别中发现偏见（见 Table 4），拓展了偏见研究边界。

---

### 方法的局限性

| 局限 | 说明 |
|------|------|
| **依赖外部分类器** | 属性分类器本身可能引入偏差，且训练需要标注数据。 |
| **属性粒度有限** | 当前仅支持粗粒度性别/种族分类，缺乏更细维度（如非二元性别、具体民族）。 |
| **LLM 指令遵循能力差异** | 小型 LLM（如 Llama-3.2B）更容易违反“不得提及性别”的指令，影响公平比较。 |
| **未考虑文化多样性** | 所有提示为英文，未验证跨语言/文化的适用性。 |

---

### 未来工作方向

1. **构建更精细的属性分类体系**，支持非二元性别、多元文化身份识别。
2. **将 BGPS 用于主动去偏训练**：将发现的高偏见提示加入训练集，提升模型鲁棒性。
3. **开发交互式审计工具**：让非技术人员也能使用 BGPS 探索模型偏见。
4. **扩展至其他模态与任务**：如视频生成、语音合成等领域的偏见探测。
5. **结合因果分析**：探究为何某些词语会触发特定偏见，推动机制可解释性研究。

---

## 总结

> **BGPS 是首个能够在保持提示可解释性的同时，自动化探索 text-to-image 模型中隐藏偏见的框架。它不仅揭示了当前去偏方法的重大缺陷，也为未来模型审计提供了强大而实用的新工具。**

该工作兼具技术深度与社会意义，推动了 AI 公平性研究从“静态评测”走向“动态攻防”，是 bias evaluation 领域的重要进展。

</details>

---

### 9. [Prismatic World Model: Learning Compositional Dynamics for Planning in Hybrid Systems](https://arxiv.org/abs/2512.08411)

**Authors**: Mingwei Li, Xiaoyuan Zhang, Chengwei Yang, Zilong Zheng, Yaodong Yang  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.08411v1  

#### Abstract
Model-based planning in robotic domains is fundamentally challenged by the hybrid nature of physical dynamics, where continuous motion is punctuated by discrete events such as contacts and impacts. Conventional latent world models typically employ monolithic neural networks that enforce global conti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Prismatic World Model: Learning Compositional Dynamics for Planning in Hybrid Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统基于**latent world model**的模型在处理机器人领域的**hybrid dynamics**（混合动力学）时存在严重缺陷。这类系统（如腿部运动、抓取操作）涉及连续运动与离散事件（如接触、碰撞）之间的频繁切换，导致动力学非光滑且高度非线性。

主流的单体神经网络（monolithic neural networks）倾向于学习“平均”动态模式，对物理状态间的**sharp mode transitions**（如飞行 vs. 接触、滑动 vs. 黏连）进行过度平滑（over-smoothing），从而在长视界规划中产生**compounding errors**，使计划不可靠。

### 提出的新方法：Prismatic World Model (PRISM-WM)
作者提出 **PRISM-WM**，一种受棱镜分光启发的结构化世界模型，通过以下三个核心机制解决上述问题：

- **Mode Identification（模式识别）**  
  引入一个上下文感知的 **Gating Network**，隐式识别当前所处的物理模式（如“接触”或“飞行”阶段），实现动态切换。

- **Specialized Dynamics（专业化动力学建模）**  
  采用 **Mixture-of-Experts (MoE)** 架构，每个专家网络专注于特定局部动力学（如行走、平衡），避免单一模型因拟合所有模式而导致的干扰和平滑问题。

- **Orthogonality for Distinct Primitives（正交化以确保多样性）**  
  引入**latent orthogonalization objective**（潜空间正交化目标），通过 Gram-Schmidt 正交化强制专家输出特征线性无关，防止“mode collapse”（模式坍缩），提升表达能力。

### 相比现有方法的优势
- **更准确的长期预测**：显著降低 rollout drift，支持更长的 planning horizon。
- **更强的泛化能力**：在多任务场景下有效缓解负迁移（negative transfer）。
- **更高的样本效率与最终性能**：尤其在高维、复杂任务（如 humanoid 控制）上表现突出。
- **实时推理高效**：尽管参数更多，但由于稀疏激活，推理速度优于或接近单体模型。

---

## 2. 核心实验方法和设置

### 使用的数据集与环境
实验覆盖多个具有挑战性的连续控制基准，涵盖不同物理特性与任务复杂度：

| 类别 | 环境 |
|------|------|
| **DiffRL** | Ant, Anymal, Hopper, Humanoid, SNU-Humanoid（强调接触丰富的运动） |
| **DMControl MT30** | 30个多样化任务，包括 locomotion、balance、manipulation、swimming 等 |
| **Humanoid-Bench** | Run, Slide, Pole, Maze（高维全身 humanoid 控制任务） |

这些任务共同构成对模型处理**异质动力学**（heterogeneous dynamics）能力的全面测试。

### 实验设置与评估指标
- **集成框架**：将 PRISM-WM 替换为 TD-MPC2 和 PWM 中的原始单体 world model，保持其他超参一致。
- **专家数量**：统一使用 $ K=4 $ 个专家。
- **训练步数**：
  - DiffRL / Humanoid-Bench：最多 20M 环境步
  - MT30：3M 环境步后报告归一化得分
- **评估指标**：
  - 平均 episode reward（主指标）
  - 归一化任务得分（MT30）
  - 动力学预测误差（MSE over horizon）
  - 规划 horizon 内的状态保真度
  - 推理延迟、吞吐量、GPU 内存占用

### 基线方法对比
| 基线 | 类型 | 说明 |
|------|------|------|
| **TD-MPC2** | Model-based, online planning | 当前最先进的基于 latent planning 的方法 |
| **PWM** | Model-based, direct policy learning | 结合可微规划的学习策略 |
| **DreamerV3** | Latent dynamics model | 基于 imagination 的强基线 |
| **SAC** | Model-free | 高效的无模型强化学习算法，用于样本效率比较 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### （1）DiffRL 高维运动任务（图4）
- 在 **Humanoid** 和 **SNU-Humanoid** 上，PRISM-WM 显著超越所有基线：
  - 更快收敛（更高样本效率）
  - 最终奖励高出明显幅度
- 在 Ant、Anymal、Hopper 上也表现出稳定优势。

#### （2）MT30 多任务泛化（图5）
- **平均归一化得分：0.531**，相比 TD-MPC2（0.430）提升 **23.5%**
- 在 300 个任务实例中，**266 项胜出**
- 单项最大增益达 +0.31（Cheetah-run-backwards）
- 在已有强基线的任务上性能波动极小（±0.02），表明不会损害已解决问题的表现

#### （3）Humanoid-Bench 复杂控制（图6）
- 在 Run、Slide、Pole、Maze 四项任务上均大幅领先：
  - 学习曲线更平稳，无剧烈震荡
  - TD-MPC2 出现周期性崩溃，而 PRISM-WM 能持续优化
- 特别是在需同时完成行走与次级目标（如持杆、走迷宫）的任务中优势显著

#### （4）动力学预测精度分析（图7–8）
- **长期预测误差更低**：
  - 在预测 horizon=10 时，PRISM-WM 的 latent dynamics MSE 比 MLP 基线低约 50%
- **有效规划视界扩展至 H=30**：
  - 单体模型在 H>5 后迅速发散（出现“地面穿透”等不合理状态）
  - PRISM-WM 维持轨迹合理性直至 H=30，保障价值估计有效性

#### （5）消融实验结果
- **正交化（Orthogonalization）至关重要**：
  - 不带正交化的 MoE（~9M 参数）虽减少 dynamics error，但在 reward prediction 上表现差
  - 带正交化的 PRISM-WM（仅 ~5M 参数）在 dynamics 和 reward 两方面均更优 → **结构优于规模**
- **专家数量敏感性低**（图8）：
  - $ K=2,4,6 $ 均显著优于单体模型
  - $ K=4 $ 表现最佳，$ K=6 $ 有轻微过拟合趋势
  - 性能差异主要来自“分解”本身，而非精细调参

#### （6）实时推理效率（表1）
| 模型 | 吞吐量 (FPS) | 推理延迟 (ms) | GPU内存 (GB) | 相对速度 |
|------|--------------|----------------|---------------|------------|
| MLP (baseline) | 7,499 | 0.13 | 0.09 | 1.00× |
| PRISM-WM (K=4) | **7,712** | 0.13 | 0.11 | **1.03×** |
| PRISM-WM (K=2) | 6,983 | 0.14 | **0.07** | 0.93× |

→ 尽管总参数增加，但因稀疏激活，**实际推理更快**，适合实时机器人控制。

---

## 4. 关键结论和发现

### 主要发现
1. **结构先验优于纯容量扩展**：  
   通过 MoE + 正交化构建的结构性分解，比单纯扩大模型规模更能提升 world model 的保真度。

2. **自动发现语义物理模式**：  
   Gating Network 自发地将任务聚类为有意义的类别（如 locomotion vs. balance），无需显式监督。

3. **显著延长可靠规划视界**：  
   有效抑制 compounding error，使得 H=30 的 long-horizon planning 成为可能，极大增强 agent 的远见能力。

4. **通用增强模块**：  
   PRISM-WM 可作为“drop-in replacement”无缝集成到 TD-MPC、PWM 等主流 MBRL 框架中，同时提升 planning 与 policy learning 效果。

5. **真实扰动下的鲁棒响应**（图9）：  
   外部冲击触发 sharp mode switching，模型能快速切换至 stabilizer 专家并恢复稳定节律，验证其捕捉 hybrid dynamics 的因果能力。

### 局限性
- **专家数量 $ K $ 需手动设定**：缺乏自动确定最优专家数的方法。
- **加权组合限制表达能力**：目前使用简单加权和，未探索更复杂的组合函数（如递归、条件分支）。
- **完全可观测假设**：当前实验基于 fully observable 状态，尚未验证在部分观测（partial observability）下的表现。

### 未来工作方向
- 自动学习或动态调整专家数量（如 growth/shrinking mechanisms）
- 探索更复杂的 composition functions（非线性组合、注意力路由等）
- 扩展至真实机器人场景，尤其是涉及多样物理交互与不确定性环境的应用
- 将“dynamic decomposition”思想推广至 social dynamics 或 multi-agent setting

---

> ✅ **总结一句话**：  
> PRISM-WM 通过引入 **context-aware MoE + latent orthogonalization** 的结构化设计，成功解决了 hybrid systems 中 world model 的 over-smoothing 问题，在多种高维、多任务、接触密集型控制任务中实现了更准确的动力学建模与更可靠的 long-horizon planning，展现出成为下一代 MBRL 基础模型的巨大潜力。

</details>

---

### 10. [Adaptation of Embedding Models to Financial Filings via LLM Distillation](https://arxiv.org/abs/2512.08088)

**Authors**: Eliot Brenner, Dominic Seyler, Manjunath Hegde, Andrei Simion, Koustuv Dasgupta, Bing Xiang  
**Category**: cs.CL  
**Published**: 2025-12-10  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.08088v1  

#### Abstract
Despite advances in generative large language models (LLMs), practical application of specialized conversational AI agents remains constrained by computation costs, latency requirements, and the need for precise domain-specific relevance measures. While existing embedding models address the first tw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Adaptation of Embedding Models to Financial Filings via LLM Distillation —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
尽管大型语言模型（LLMs）在理解金融文本方面表现出色，但由于其高计算成本、长上下文处理能力有限以及延迟要求，难以直接用于生产环境中的高效检索任务。现有的通用 embedding 模型（如 OpenAI 的 text-embedding-ada-002）在**金融领域**的信息检索（Information Retrieval, IR）任务中表现不佳，尤其是在处理复杂的 SEC EDGAR 财务文件时存在语义匹配不准确、召回率低等问题。

因此，本文旨在解决以下挑战：
- 如何在不依赖人工标注的前提下，为金融领域的 RAG（Retrieval-Augmented Generation）系统训练一个**高性能、低成本、低延迟的专用 embedding 模型**。
- 如何提升检索模型对财务报告中表格、技术术语和概念关系的理解能力。

---

### 🚀 提出的新方法与创新点

作者提出了一种基于 **LLM Distillation 的迭代式检索模型微调框架**，其核心思想是利用强大的 LLM 作为“教师”来生成训练信号，指导轻量级 bi-encoder 检索器（“学生”）进行领域适应。

#### 主要创新包括：

1. **LLM-Judged Relevance Distillation Pipeline**
   - 利用开源 LLM（Llama-3.1-70B-Instruct）从无标签的 SEC 财报中自动生成查询（queries），并对候选段落打分（1–4 分），构建高质量的监督信号。
   - 这些评分用于构造 contrastive training triples：`(q, c_rel, c_irrel)`，其中正负样本均来自同一文档，确保模型学习到**细粒度的 within-document 检索能力**。

2. **Hard Example Mining with Iterative Refinement**
   - 引入**交互式挖掘机制**：每轮迭代使用当前学生模型（bi-enc(i)）在大规模语料库中检索 top-K chunks，再由 LLM 对这些 chunks 打分，筛选出更难区分的正负例。
   - 学生模型不断更新后，下一轮能挖掘出更具挑战性的训练样本，形成“越练越强”的闭环。

3. **跨文档类别的统一建模策略**
   - 将 14 种不同类型的财务文件（如 10-K, 10-Q, 8-K 等）分别处理生成查询和相关性判断，但在最终训练阶段将所有数据混合，实现一个**统一的多类别 embedding 模型**。

4. **Scalable Corpus Sampling & Downsampling Strategy**
   - 针对海量语料（约 51.88M chunks），设计了高效的采样策略：
     - 按 filing type 分层抽样；
     - 使用 bi-encoder 先检索潜在相关文档集合 `D(q,K,i)`；
     - 在每个文档内采用非均匀概率分布采样 chunks，优先保留高排名结果以支持 MRR@k 等指标计算。

5. **Synthetic Query Generation with Few-Shot Prompting**
   - 改进 InPars 方法，通过 few-shot prompting 生成更泛化、不过度绑定特定段落的“合成查询”，提高查询多样性。

---

### 🔍 相比现有方法的优势

| 方面 | 本文方法 | 先前工作（如 Anderson et al. [3]） |
|------|--------|-------------------------------|
| 正样本数量 | ~10³ 正例/查询（两轮迭代） | 通常仅 1 个正例/查询 |
| 负样本来源 | 同一文档内的无关段落（hard negatives） | 固定排名位置抽取，未验证无关性 |
| 查询生成方式 | 结合 InPars + Few-shot 合成，增强多样性 | 单纯基于段落生成 |
| 检索粒度 | 支持 within-document 细粒度排序 | 更偏向跨文档检索 |
| 数据效率 | 自动化 LLM 打标，无需人工标注 | 依赖大量人工或半自动标注 |

> ✅ 显著优势：**更高的正负样本密度、更强的领域针对性、更好的 within-document 排序能力**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

- **自建数据集**：爬取 2024 年 1 月 1 日至 7 月 31 日期间发布的 **396,165 份 SEC filings**，涵盖 14 类财务文件。
  - 包括：10-K, 10-Q, 8-K, DEF 14A, 424B3, S-8 等。
  - 总计约 **51.88 million chunks**（chunk size: 500–1000 characters）。
  - 时间划分：
    - **训练/验证集**：Jan 1 – Jun 30
    - **测试集**：Jul 1 – Jul 31

- **公开基准数据集**：
  - **FinanceBench [9]**：用于 out-of-distribution 评估，包含真实金融问答对及其 evidence passages。

---

### ⚙️ 实验设置

| 组件 | 设置说明 |
|------|---------|
| **Teacher LLM** | Llama-3.1-70B-Instruct（open-weight） |
| **Student Model** | gte-large（General Text Embeddings, 335M 参数） |
| **Embedding Type** | Bi-Encoder（query 和 passage 分别编码） |
| **Loss Function** | Triplet Loss：<br>`max(0, α + d(q, c_neg) - d(q, c_pos))`<br>margin α = 0.1（优于默认值 5） |
| **Training Epochs** | 2 |
| **Learning Rate** | 5e-7 |
| **Batch Size** | 128（16/GPU × 8 GPUs） |
| **Optimizer** | AdamW |
| **Triplet 数量** | 训练集：2.52M；验证集：950K |

---

### 📊 评估指标

| 指标 | 定义 |
|------|-----|
| **MRR@5** | Mean Reciprocal Rank @5，衡量第一个正确答案的位置 |
| **mean DCG@5** | 平均 Discounted Cumulative Gain @5，考虑排名顺序的加权得分 |
| **NDCG** | Normalized DCG，归一化后的排序质量 |
| **Cohen’s d** | 效应大小统计量，衡量改进是否显著 |

> 所有指标均基于 LLM 打标的 relevance scores（见 Table II），并在最终评估中使用 **GPT-4o** 作为裁判模型以减少偏差。

---

### 🆚 基线方法对比

- **Base Model**: 原始 gte-large（未经微调）
- **Control Variant**: 相同架构但使用传统方法训练
- **OpenAI Baseline**: OpenAI text-embedding-ada-002（参考 Anderson et al. [3] 中的表现）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Table IV & Abstract）

在 **21,800 query-document pairs** 上平均：

| 指标 | 提升幅度 |
|------|--------|
| **MRR@5** | ↑ **27.7%** |
| **mean DCG@5** | ↑ **44.6%** |

> 在 14 类财务文件上全面超越基线。

---

### 🔬 FinanceBench 上的结果（Table V）

| 类别 | MRR 提升 | uNDCG 提升 |
|------|----------|------------|
| 10-Q | ↑ 1.00 (Cohen’s d) | ↑ 1.100 |
| ECT | ↑ 0.15 | ↑ 0.260 |
| 8-K | ↓ (-0.21) | ↑ 0.190 |
| 10-K | ↓ (-1.30) | ↔ (+0.027) |
| **Overall** | ↓ (-0.63) | ↑ **0.530** |

✅ **关键发现**：
- 在多数类别（尤其是 10-Q 和 ECT）上，fine-tuned 模型显著优于原始 gte-large。
- 尽管 MRR 下降，但 **uNDCG 提升明显**，表明排序质量整体改善。
- 10-K 表现下降可能源于 FinanceBench 查询复杂度更高（见下文分析）。

---

### 🧪 消融实验与定性分析（Section VI-B）

通过对“promoted passages”和“demoted passages”的分析，识别出三种典型改进模式：

| 模式 | 示例 |
|------|------|
| **Table Retrieval** | 成功从表格中提取 CEO 薪酬变化等量化信息 |
| **Technical Exact Match** | 准确匹配“par value”、“interest rate range”等专业术语 |
| **Semantic Understanding** | 理解“raising capital”与“notes payable, lines of credit”的隐含联系 |

> 见 Table VI，展示了多个成功案例。

同时，在 FinanceBench 的 10-K 子集中观察到性能退步的原因包括：
- **Multi-faceted Questions**：需综合多个部分作答
- **Terminologically Dense Queries**：涉及会计概念需外部知识解析
- **Distractor Terms**：干扰项降低 precision
- **Label Noise**：chunk 与 evidence 重叠过小导致误标

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **LLM-Judged Distillation 是有效的领域适配手段**  
   利用 LLM 自动生成 query 和 relevance label，可在无人工标注情况下大幅提升 embedding 模型在金融领域的检索性能。

2. **Iterative Hard Example Mining 显著提升效果**  
   通过学生模型逐步挖掘 harder negatives/positives，使训练信号更加丰富且贴近实际应用场景。

3. **Within-Document Retrieval 能力增强**  
   同文档内正负样本配对的设计使得模型更擅长定位关键信息块（如表格、脚注），这对财务分析至关重要。

4. **第一轮迭代收益最大，后续增益递减**  
   第一次 fine-tuning 带来最大提升，第二次迭代效果趋于饱和，提示可优化训练节奏。

5. **模型具备一定语义推理能力**  
   能捕捉 query 与 passage 之间的概念关联，即使没有词汇重叠。

---

### ⚠️ 局限性

1. **依赖 Teacher LLM 的打标质量**  
   当前方法假设 LLM 打分可靠，但可能存在偏见或错误，尤其面对高度专业或模糊表述。

2. **FinanceBench 数据集规模较小**  
   仅有 150 条测试样本，限制了统计显著性和泛化能力评估。

3. **未覆盖复杂复合查询**  
   对 multi-hop 或 multi-faceted 问题处理能力不足，需结合 agentic 或 graph-based 方法。

4. **chunking 策略影响性能边界**  
   固定长度切片可能导致证据片段被割裂，影响 recall。

---

### 🔮 未来工作方向（Section VIII）

1. **引入 Knowledge Graphs / External KBs**
   - 使用 GraphRAG 或知识图谱辅助 query expansion 和 retrieval refinement。

2. **Agentic Query Planning**
   - 构建具备推理能力的 agent，动态生成更精准的检索锚点，并根据初步结果调整查询策略。

3. **Hybrid Long-Context + Retrieval Systems**
   - 探索 long-context LLM 与 specialized retriever 的协同机制，发挥各自优势。

4. **更鲁棒的 Label Propagation 方法**
   - 改进 FinanceBench 中 evidence-to-chunk 的映射逻辑，减少噪声。

5. **端到端训练框架探索**
   - 联合优化 retrieval 与 generation 模块，进一步提升 RAG 整体性能。

---

## 总结

> 本文提出了一种**可扩展、低成本、无需人工标注**的方法，通过 **LLM distillation + iterative hard example mining**，成功将通用 embedding 模型（gte-large）适配至金融财报场景，在 MRR@5 和 DCG@5 上取得显著提升。该方法不仅适用于金融领域，也为其他专业垂直领域的检索系统提供了范式参考。

📌 **一句话总结**：  
**用 LLM 当老师，教一个小而快的 embedding 模型读懂财报，效果远超通用模型，且全过程自动化。**

</details>

---

### 11. [LUNA: Linear Universal Neural Attention with Generalization Guarantees](https://arxiv.org/abs/2512.08061)

**Authors**: Ashkan Shahbazi, Ping He, Ali Abbasi, Yikun Bai, Xinran Liu, Elaheh Akbari, Darian Salehi, Navid NaderiAlizadeh, Soheil Kolouri  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.08061v1  

#### Abstract
Scaling attention faces a critical bottleneck: the $\mathcal{O}(n^2)$ quadratic computational cost of softmax attention, which limits its application in long-sequence domains. While linear attention mechanisms reduce this cost to $\mathcal{O}(n)$, they typically rely on fixed random feature maps, su...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LUNA: Linear Universal Neural Attention with Generalization Guarantees — 核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

Transformer 模型中的 **softmax attention** 具有 $O(n^2)$ 的计算复杂度，严重限制了其在长序列任务（如长文本、高分辨率图像）中的应用。虽然已有多种 **linear attention** 方法将复杂度降至 $O(n)$，但它们通常依赖于固定的随机特征映射（如 Random Fourier Features）或手工设计的非线性函数，导致模型表达能力受限，精度显著低于标准 softmax attention。

这种“**效率换精度**”的权衡是当前高效注意力机制的核心瓶颈。

---

### **提出了什么新方法或新思路**

本文提出 **LUNA (Linear Universal Neural Attention)**，一种全新的线性注意力机制，其核心思想是：

> **将注意力核函数的特征映射从“固定”变为“可学习”**。

具体创新包括：

- **可学习的核特征映射（Learnable Kernel Feature Map）**：
  - 不再使用固定的 RFF 或 Performer 特征，而是通过一个神经网络（MLP）参数化特征映射 $\phi(x)$。
  - 该映射由两部分组成：
    1. 可学习的输入投影矩阵 $W \in \mathbb{R}^{m \times d}$；
    2. 一组共享的通道函数 $\psi_l: \mathbb{R} \to \mathbb{R}$（每个为小型 MLP），作用于投影后的标量值。
  - 最终特征映射为：  
    $$
    \phi(x) = \frac{1}{\sqrt{m}} \left[ \psi_1(w_1^\top x), \dots, \psi_L(w_m^\top x) \right] \in \mathbb{R}^{mL}
    $$

- **保持线性复杂度**：
  - 利用 kernelized attention 的 streaming 形式，实现 $O(n)$ 时间和内存复杂度。
  - 注意力计算形式为：
    $$
    \text{Attn}(Q,K,V) = \phi(Q) (\phi(K)V) / (\phi(K)\mathbf{1}_n)
    $$

- **理论保障**：
  - 证明所构造的核函数是 **正定（positive-definite, PD）** 的。
  - 推导了基于 **Rademacher 复杂度** 的泛化误差界，表明其假设空间具有良好的泛化能力。
  - 将近似误差分解为 **参数化误差**（parametrization error）与 **采样误差**（sampling error），并给出概率边界。

---

### **相比现有方法的优势**

| 维度 | LUNA | 传统线性注意力（如 Performer, RFF） |
|------|------|-----------------------------|
| **特征映射** | ✅ 可学习，自适应任务分布 | ❌ 固定，数据无关 |
| **表达能力** | ✅ 强，能拟合最优核 | ❌ 弱，受限于先验选择 |
| **计算效率** | ✅ $O(n)$ 时间/内存 | ✅ $O(n)$ |
| **后训练转换（post-hoc conversion）** | ✅ 高效恢复原模型性能 | ⚠️ 性能下降明显 |
| **理论支持** | ✅ 泛化误差界、PD 保证 | ⚠️ 多数缺乏 |

> **核心优势**：LUNA 在不牺牲线性效率的前提下，通过学习任务特定的核函数，显著缩小了与标准 softmax attention 的性能差距，甚至反超。

---

## 2. 核心实验方法和设置

### **使用的数据集**

1. **Long Range Arena (LRA)**  
   - 用于评估长序列建模能力的标准基准。
   - 包含 5 个异构任务：
     - **Text**: IMDb 文本分类（4K tokens）
     - **ListOps**: 结构化列表操作（2K tokens）
     - **Retrieval**: 文档检索匹配
     - **Pathfinder**: 图像路径检测（2K×2K pixels）
     - **Image**: 图像分类（MNIST-like, 1K×1K pixels）
   - 所有序列长度在 1K–16K 范围内。

2. **GLUE Benchmark**  
   - 用于自然语言理解任务。
   - 使用 **BERT-base** 微调后的 checkpoint 进行后训练转换实验。

3. **ImageNet-1K**  
   - 图像分类任务。
   - 使用 **ViT-B/16** 微调后的 checkpoint 进行后训练转换实验。

---

### **实验设置和评估指标**

- **公平比较原则**：
  - 所有方法在相同 backbone 架构、优化器、训练步数、batch size 和近似 FLOPs 下进行对比。
  - 仅替换 attention 模块，其余不变。

- **评估指标**：
  - **LRA**: 各任务测试准确率（accuracy %），以及平均得分。
  - **GLUE**: Dev set 上的综合评分（如 CoLA, SST-2, MRPC 等），报告恢复原始 BERT-FT 性能的百分比。
  - **ImageNet-1K**: Top-1 准确率（%）。

- **后训练转换流程（Two-stage Conversion）**：
  1. **Stage 1: Attention Distillation**  
     冻结主干参数，仅训练 LUNA 的可学习特征映射，使其输出的 attention 分布逼近原始 softmax attention。
  2. **Stage 2: End-to-end Fine-tuning**  
     解冻全部参数，使用任务损失进行轻量微调（通常 1–10 epochs）。

---

### **基线方法对比**

| 方法 | 类型 | 是否可学习 |
|------|------|-----------|
| **Transformer (Softmax)** | 原始二次复杂度 | ✅ |
| **Performer**, **Linear Transformer** | 随机特征线性化 | ❌ |
| **Linformer**, **Nystromformer** | 低秩压缩 | ❌ |
| **Cosformer**, **Skyformer** | 结构化稀疏/核近似 | ❌ |
| **Hedgehog**, **T2R/T2R-HH** | 后训练指数特征线性化 | ❌（固定特征） |
| **LUNA (Ours)** | **可学习核特征映射** | ✅ |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **Long Range Arena (LRA) 结果**

| Model | Text | ListOps | Retrieval | Pathfinder | Image | **Avg.** |
|-------|------|---------|-----------|------------|--------|----------|
| Transformer | 61.55 | 38.71 | 80.93 | 70.39 | 39.14 | 58.14 |
| Skyformer | 64.70 | 38.69 | 82.06 | 70.73 | 40.77 | 59.39 |
| LOTFormer | 71.1 | 38.5 | 80.9 | 69.9 | 54.1 | **62.9** |
| **LUNA** | **73.41** | **38.94** | **81.02** | **69.52** | **64.32** | **65.44** |

> 🔥 **LUNA 在 LRA 上取得 SOTA 平均准确率（65.44%）**，尤其在 **Image** 任务上大幅提升（+10% 以上）。

---

#### ✅ **后训练转换：BERT on GLUE**

| Method | CoLA | SST-2 | MRPC | STS-B | QQP | MNLI | QNLI | RTE | **Recover (%)** |
|--------|------|-------|------|-------|-----|------|------|-----|----------------|
| BERT-FT | 58.8 | 93.2 | 90.2 | 88.8 | 91.0 | 84.7 | 91.3 | 68.2 | 100.0 |
| T2R | 43.6 | 87.7 | 83.0 | 78.6 | 86.7 | 78.9 | 84.6 | 54.1 | 88.9 |
| Hedgehog | 59.2 | 92.6 | 90.1 | 87.4 | 91.0 | 82.6 | 89.6 | 69.3 | 99.3 |
| **LUNA** | **58.8** | **93.4** | **90.1** | **88.5** | **90.7** | **83.5** | **90.6** | **68.8** | **99.5** |

> ✅ LUNA **平均恢复 99.5% 的原始性能**，略优于 Hedgehog，远超 T2R 系列。

---

#### ✅ **后训练转换：ViT-B/16 on ImageNet-1K**

| Method | Top-1 Accuracy (%) |
|--------|--------------------|
| ViT-B/16 (original) | 80.3 |
| T2R-HH | 77.0 |
| Hedgehog | 79.5 |
| **LUNA** | **80.5** |

> ✅ LUNA **不仅恢复性能，还略微超越原始模型（+0.2%）**，显示其更强的表达能力。

---

### **消融实验结果**

#### 🔍 **通道数 $L$ 与投影数 $M$ 的影响（LRA-Image）**

| $L$ (Channels) | 2 | 4 | **8** | 16 |
|----------------|----|----|------|-----|
| Accuracy (%) | 62.62 | 62.02 | **64.32** | 60.89 |

| $M$ (Projections) | 4 | **8** | 16 | 32 |
|-------------------|-----|------|-----|-----|
| Accuracy (%) | 63.06 | **64.32** | 63.88 | 59.15 |

> ✅ 最佳配置为 $M=8, L=8$；过大反而性能下降，说明存在最优表示维度。

---

#### 🔍 **神经核变体消融（LRA-Image）**

| Variant | Kernel Form | Accuracy (%) |
|--------|-------------|--------------|
| RFF-sin,cos (fixed) | $\phi_{\text{sin,cos}}(x)$ | 35.72 |
| Bank+Coef | $\sum_l w_l \phi_l(x)$ | 40.30 |
| Envelope-gated | $h(x) \odot \psi(x)$ | 41.52 |
| **No gate (LUNA default)** | $\psi(x)$ | **64.32** |

> ✅ 学习整个非线性映射（而非加权固定基）至关重要；引入门控反而损害性能。

---

## 4. 关键结论和发现

### **主要发现**

1. **可学习核特征映射是提升线性注意力性能的关键**：
   - 固定特征（如 RFF、exp）无法适应不同任务的数据分布。
   - LUNA 通过端到端学习任务自适应的核函数，在保持 $O(n)$ 效率的同时实现了接近甚至超越 softmax attention 的精度。

2. **LUNA 是强大的“即插即用”模块**：
   - 可无缝替换现有 Transformer 中的 softmax attention。
   - 支持高效的 **post-hoc conversion**，无需从头训练即可部署于生产系统。

3. **理论与实践一致**：
   - 提出的模型满足正定性，具备良好泛化性质。
   - 实验验证了其在多种模态（文本、图像）和任务上的鲁棒性和优越性。

4. **注意力可视化更合理**：
   - 在 SST-2 和 ImageNet 上，LUNA 的 attention map 更聚焦于语义关键区域（如情感词、物体主体），而 Hedgehog 等方法更弥散。

---

### **方法的局限性**

- **额外参数开销**：尽管计算复杂度仍为 $O(n)$，但引入了可学习的 MLP 参数，在极小模型中可能带来负担。
- **对初始化敏感**：需 careful initialization 和 warm-up 阶段（如 distillation）以稳定训练。
- **目前主要用于 encoder-only 模型**：尚未充分验证在 decoder 或生成任务中的表现。

---

### **未来工作方向**

- 扩展至 **decoder 架构** 和 **生成任务**（如机器翻译、文本生成）。
- 探索更高效的参数化方式（如低秩 MLP、共享跨层特征）。
- 将 LUNA 应用于 **多模态大模型**（如 LLM + Vision）中的长上下文建模。
- 理论上进一步分析其动态学习过程与归纳偏置演化。

---

## 总结

> **LUNA 成功打破了“线性注意力必须牺牲精度”的固有认知**，通过引入**完全可学习的核特征映射**，在保持 $O(n)$ 复杂度的同时，实现了对标准 softmax attention 的性能追赶乃至反超。其实验结果在 LRA 和 post-hoc conversion 场景下均达到 SOTA，展示了其作为下一代高效注意力范式的巨大潜力。

</details>

---

### 12. [Wavelet-Accelerated Physics-Informed Quantum Neural Network for Multiscale Partial Differential Equations](https://arxiv.org/abs/2512.08256)

**Authors**: Deepak Gupta, Himanshu Pandey, Ratikanta Behera  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.08256v1  

#### Abstract
This work proposes a wavelet-based physics-informed quantum neural network framework to efficiently address multiscale partial differential equations that involve sharp gradients, stiffness, rapid local variations, and highly oscillatory behavior. Traditional physics-informed neural networks (PINNs)...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Wavelet-Accelerated Physics-Informed Quantum Neural Network for Multiscale Partial Differential Equations

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**多尺度偏微分方程（multiscale PDEs）** 中存在的挑战，特别是当解具有以下特征时：
- 尖锐梯度（sharp gradients）
- 刚性行为（stiff dynamics）
- 快速振荡（highly oscillatory behavior）
- 局部剧烈变化（rapid local variations）

传统 **Physics-Informed Neural Networks (PINNs)** 和其量子版本 **Physics-Informed Quantum Neural Networks (PIQNNs)** 在处理这类问题时面临两大瓶颈：
1. 难以准确捕捉多尺度特征；
2. 依赖自动微分（automatic differentiation）计算残差损失，带来显著的计算开销。

---

### 提出的新方法：WPIQNN
作者提出了一种新型框架——**Wavelet-Accelerated Physics-Informed Quantum Neural Network (WPIQNN)**，结合了小波分析与量子神经网络的优势。

#### 核心创新点：
- ✅ **消除自动微分需求**：通过在**小波基函数空间**中表示解，并利用预存的小波导数矩阵进行解析求导，避免了反向传播中的高成本自动微分。
- ✅ **引入小波多分辨率特性**：利用小波的局部化能力，在不同尺度上同时捕获全局平滑特征和局部尖锐变化，有效应对多尺度问题。
- ✅ **融合量子神经网络（QNN）提升表达效率**：使用参数化的变分量子电路作为非线性映射器，预测小波系数，相比经典网络大幅减少可训练参数数量。
- ✅ **双阶段量子架构设计**：
  - 第一阶段（QNN1）：角编码（angle encoding）提取输入特征；
  - 第二阶段（QNN2）：幅值编码（amplitude encoding）处理特征向量并输出小波系数。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **精度** | 显著优于经典 PINN、PIQNN-I/II 和 W-PINN，尤其在刚性和高频场景下 |
| **参数效率** | 可训练参数仅为经典 W-PINN 的 **<5%** |
| **训练速度** | 较现有 PIQNN 方法实现 **3–5倍加速** |
| **稳定性** | 损失收敛更稳定，无明显震荡或停滞现象 |

---

## 2. 核心实验方法和设置

### 实验数据集 / 测试问题
论文未使用真实世界“数据集”，而是基于多个具有挑战性的**基准 PDE 问题**进行数值实验，涵盖不同类型和复杂度：

1. **高梯度热传导方程（Heat Conduction Problem）**
   - 控制参数 ε 越小，梯度越陡，问题越刚性
   - 用于测试对 sharp gradients 的建模能力

2. **高频 Helmholtz 方程**
   - 典型椭圆型 PDE，广泛应用于波动传播
   - 高频下解高度振荡，考验模型频率捕捉能力

3. **Klein-Gordon 方程**
   - 二阶非线性波动方程，含时间振荡项
   - 参数 a 控制振荡频率，用于验证高频频域适应性

4. **Maxwell 方程（电磁场）**
   - 分为均匀介质与非均匀介质两种情况
   - 后者存在材料界面突变，引入不连续性，测试边界/接口处理能力

---

### 实验设置与评估指标

#### 评估指标：
- **相对 L₂-error**：衡量预测解与真解之间的平均误差
  $$
  L_2 = \frac{\|u_{\text{pred}} - u_{\text{ref}}\|_{L^2(\Omega)}}{\|u_{\text{ref}}\|_{L^2(\Omega)}}
  $$
- **可训练参数数量（Trainable Parameters）**
- **训练时间 / 收敛速度**
- **损失曲线稳定性（variance across runs）**

#### 训练配置：
- 所有实验均采用 **Adam 优化器 + 学习率调度器**
- 初始化方式：Xavier 初始化
- 模拟平台：PennyLane（模拟量子电路）、PyTorch
- 硬件：NVIDIA RTX A4500 GPU
- 每个实验重复 **10 次独立运行**取均值与标准差，确保统计稳健性

---

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Classical PINN** | 经典神经网络 | 基础框架，依赖自动微分 |
| **W-PINN [35]** | 经典小波增强PINN | 使用小波基避免自动微分，但参数量巨大 |
| **PIQNN-I / PIQNN-II [50]** | 量子增强PINN | 当前主流量子物理信息网络架构 |
| **Proposed: WPIQNN** | 本文方法 | 小波+量子联合框架 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 🔹 示例1：热传导方程（ε = 0.15）
| 方法 | L₂-error | Trainable Params |
|-------|----------|------------------|
| Classical PINN | ~0.285 | 15,501 |
| PIQNN-I | ~0.437 | 97 (≈81–115) |
| PIQNN-II | ~0.285 | 115 |
| W-PINN | 3.96×10⁻⁴ | 1,574,692 |
| **WPIQNN** | **2.13×10⁻⁴** | **22,652** |

> ✅ WPIQNN 错误最低，且仅用约 **1.4%** 的 W-PINN 参数。

#### 🔹 示例2：Helmholtz 方程（高频）
| 方法 | L₂-error | Trainable Params |
|-------|----------|------------------|
| Classical PINN | 2.39×10⁻¹ | 15,501 |
| PIQNN-I | 9.12×10⁻¹ | 197 |
| PIQNN-II | 3.12×10⁻¹ | 126 |
| W-PINN | 3.12×10⁻⁴ | 2,227,696 |
| **WPIQNN** | **2.19×10⁻⁴** | **35,611** |

> ✅ 准确率更高，参数仅为 W-PINN 的 **~1.6%**

#### 🔹 示例3：Klein-Gordon 方程（a=10）
| 方法 | L₂-error | Trainable Params |
|-------|----------|------------------|
| Classical PINN | ~0.917 | 61,001 |
| PIQNN-I/II | 发散 / 不收敛 | <200 |
| W-PINN | 4.18×10⁻³ | 3,416,627 |
| **WPIQNN** | **1.72×10⁻³** | **50,837** |

> ✅ WPIQNN 成功收敛，误差更低，参数仅占 W-PINN 的 **~1.5%**

#### 🔹 示例4：Maxwell 方程（异质介质）
| 方法 | E-field L₂ | H-field L₂ | Trainable Params |
|-------|------------|------------|------------------|
| W-PINN | 4.17×10⁻⁴ | 3.17×10⁻⁴ | 4,648,006 |
| **WPIQNN** | **2.17×10⁻⁴** | **1.70×10⁻⁴** | **58,366** |

> ✅ 误差减半以上，参数仅 **~1.25%**

---

### 性能加速结果（图7）
- **训练时间对比（vs PIQNN-I/II）**：
  - 固定层数时，WPIQNN 训练时间缩短 **2–3.5倍**
  - 增加层数后，加速比达 **2–5倍**
- 加速原因归因于：
  - **无需自动微分** → 大幅降低计算图构建与梯度回传开销
  - **小波导数预计算** → 导数计算变为查表操作

---

### 消融实验与可视化分析（支持性证据）
- **损失收敛曲线（Fig. 3）**：
  - PIQNN-I 残差损失下降缓慢且波动大
  - WPIQNN 所有损失项（PDE, IC, BC）均平稳快速下降
- **点对点误差图（Fig. 5, 8, 10, 11, 12）**：
  - WPIQNN 在边界、激波区、接口处误差极低
  - 其他方法在这些区域出现明显偏差
- **极端案例测试（ε=0.11）**：
  - 解从 0 突增至 ~9000，其他方法完全失败
  - WPIQNN 仍保持稳定，L₂-error ≈ 1.17×10⁻⁴

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **WPIQNN 能高效解决传统方法难以处理的多尺度、刚性、高频 PDE 问题**。
2. ✅ **小波基 + QNN 架构实现了精度与效率的双重突破**：
   - 精度超越所有基线方法
   - 参数量仅为经典 W-PINN 的 **2–5%**
3. ✅ **去除自动微分显著提升了训练效率**，带来 **3–5倍速度提升**。
4. ✅ **量子架构并未牺牲稳定性**，反而因结构简化表现出更强鲁棒性。

---

### 方法的局限性
1. ❗ **当前仍在经典硬件上模拟量子电路**：
   - 量子态模拟随 qubit 数指数增长，限制规模扩展
   - 实际量子硬件部署尚需等待 NISQ 设备成熟
2. ❗ **小波基选择固定（Gaussian wavelet）**：
   - 未探索自适应或学习型小波基
   - 对某些特定结构可能不是最优
3. ❗ **幅值编码依赖高效状态准备算法**：
   - 在真实设备中仍是开放难题

---

### 未来工作方向
1. 🔄 探索更多复杂 PDE 应用场景：
   - 流体力学（Navier-Stokes）
   - 非规则几何或多物理场耦合系统
2. 🔮 结合实际量子硬件部署 WPIQNN，验证量子优势
3. 🧠 引入可学习小波参数（如 dilation & translation），进一步提升灵活性
4. ⚙️ 开发专用量子线路优化策略，降低电路深度与噪声敏感性

---

## 总结
该论文提出的 **WPIQNN** 是一个开创性的混合框架，成功将 **小波多分辨率分析** 与 **量子神经网络的参数效率** 相结合，解决了 PINN 和 PIQNN 在多尺度 PDE 上的关键瓶颈。它不仅在精度上取得领先，而且在参数效率和训练速度方面展现出巨大潜力，为未来基于量子技术的科学计算提供了新的范式。

</details>

---

### 13. [Mathematical Foundations of Neural Tangents and Infinite-Width Networks](https://arxiv.org/abs/2512.08264)

**Authors**: Rachana Mysore, Preksha Girish, Kavitha Jayaram, Shrey Kumar, Preksha Girish, Shravan Sanjeev Bagal, Kavitha Jayaram, Shreya Aravind Shastry  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.08264v1  

#### Abstract
We investigate the mathematical foundations of neural networks in the infinite-width regime through the Neural Tangent Kernel (NTK). We propose the NTK-Eigenvalue-Controlled Residual Network (NTK-ECRN), an architecture integrating Fourier feature embeddings, residual connections with layerwise scali...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Mathematical Foundations of Neural Tangents and Infinite-Width Networks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文聚焦于**深度神经网络在有限宽度（finite-width）下的训练动态与泛化能力缺乏理论可解释性**的问题。尽管 Neural Tangent Kernel (NTK) 在无限宽度极限下提供了对网络训练的线性化分析框架，但在实际应用中，网络是有限宽度的，导致 NTK 在训练过程中会发生非平凡演化（nontrivial evolution），从而影响收敛性和泛化性能。

现有研究多集中于理想化的无限宽度理论或纯经验观察，缺乏一个既能进行严格数学分析又能实证验证的桥梁机制。

### 🚀 提出的新方法：NTK-Eigenvalue-Controlled Residual Network (NTK-ECRN)

作者提出了一种新型架构——**NTK-ECRN**，其设计目标是实现对 NTK 演化过程的**精确控制与可分析性**，同时保持实践有效性。该架构整合了以下关键技术组件：

- **Fourier Feature Embeddings**  
  将输入 $x$ 映射为 $\phi(x) = [\sin(2\pi Bx), \cos(2\pi Bx)]$，增强高频频谱表示，缓解标准 MLP 中的 spectral bias，使 NTK 能更有效地学习高频模式。

- **Layerwise Residual Connections with Scaling Coefficients $\alpha_l$**  
  引入逐层缩放系数 $\alpha_l$ 控制残差块的贡献强度，理论上可约束 NTK 最大特征值 $\lambda_{\max}$ 的增长速度，提升训练稳定性。

- **Stochastic Depth（可选）**  
  在训练时以概率 $p$ 随机跳过残差块，引入正则化并降低过拟合风险，同时仍允许对期望 NTK 演化进行解析建模。

### 🔍 相比现有方法的优势

| 方面 | 优势 |
|------|------|
| **理论可分析性** | 架构设计显式支持 NTK 动态、特征值谱演化等的数学推导，弥补了传统 ResNet 或 MLP 缺乏理论工具的问题。 |
| **可控的训练动态** | 通过 $\alpha_l$ 和 Fourier 特征调节 NTK 特征值分布，避免边缘不稳定（edge-of-stability）现象。 |
| **更强的泛化能力** | 实验表明，在合成与真实数据上均优于标准 NTK 模型及经典网络（如 MLP、ResNet-18）。 |
| **桥接理论与实践** | 同时提供 kernel dynamics 分析与任务性能评估，形成“理论指导设计 → 实验验证”的闭环。 |

---

## 2. 核心实验方法和设置

### 📚 数据集使用情况

| 类型 | 数据集 | 描述 |
|------|--------|------|
| **合成数据** | 高频回归任务 | 输入维度 $d=20$，目标函数由 $K=10$ 个随机正弦波叠加构成，加入高斯噪声。用于系统分析 NTK 对高频模式的学习能力。 |
| **合成分类** | 高斯混合模型（GMM） | 5 类，每类服从不同均值的多元正态分布，用于测试分类边界学习能力。 |
| **基准数据集** | UCI Repository | 包括 Boston Housing（回归）、Iris、Wine（分类），标准化预处理后划分训练/验证/测试集（70%/15%/15%）。 |
| **图像任务** | CIFAR-10 子集 | 使用 5,000 张图像的小规模子集，适合作为本科项目级别的视觉实验。 |

### ⚙️ 实验设置

- **模型配置**：
  - 所有模型统一宽度（如 512 units），层数一致（NTK-ECRN 使用 $L$ 层残差块）
  - 初始化方式遵循 NTK 参数尺度（NTK-parametrization）
  - 优化器：全批量梯度下降（synthetic）或小批量 SGD（benchmark）

- **评估指标**

| 任务类型 | 性能指标 | Kernel 动态指标 |
|---------|----------|----------------|
| 回归 | MSE, $R^2$ | $\|\Delta \Theta^{(t)}\|_F$, $\lambda_i(t)$ |
| 分类 | Accuracy, Cross-Entropy Loss | $\lambda_{\max}(t)$, spectral decay trend |

- **NTK 跟踪机制**：
  - 每个 epoch 计算当前 NTK 矩阵 $\Theta^{(t)}(x,x')$
  - 计算偏差 $\Delta \Theta^{(t)} = \Theta^{(t)} - \Theta^{(0)}$
  - 进行特征值分解，监控 $\{\lambda_i(t)\}$ 演化轨迹

### 🆚 基线方法对比

| 基线模型 | 说明 |
|--------|------|
| **MLP (3-layer, 512 neurons)** | 标准前馈网络，代表基础 DNN 结构 |
| **ResNet-18** | 经典残差网络，具备良好实践经验 |
| **Standard NTK** | 基于无限宽度近似的线性化 NTK 模型（核方法） |

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能数据汇总

#### 表 I：合成回归任务表现（Mean ± Std）

| Model | MSE ↓ | $R^2$ ↑ |
|-------|------|--------|
| MLP | 0.085 ± 0.007 | 0.91 ± 0.02 |
| ResNet-18 | 0.072 ± 0.006 | 0.93 ± 0.01 |
| Standard NTK | 0.090 ± 0.008 | 0.90 ± 0.02 |
| **NTK-ECRN (proposed)** | **0.045 ± 0.004** | **0.92 ± 0.01** |

> 💡 观察：NTK-ECRN 在 MSE 上显著优于所有基线，尤其擅长捕捉高频信号。

#### 表 II：合成分类任务（5-class GMM）

| Model | Accuracy (%) ↑ | CE Loss ↓ |
|-------|----------------|----------|
| MLP | 87.3 ± 1.1 | 0.412 ± 0.015 |
| ResNet-18 | 89.5 ± 0.9 | 0.385 ± 0.012 |
| Standard NTK | 85.7 ± 1.3 | 0.425 ± 0.017 |
| **NTK-ECRN** | **93.8 ± 0.7** | **0.312 ± 0.010** |

> 💡 观察：结合 Fourier embedding 与残差缩放，显著提升低/高频特征联合学习能力。

#### 表 III：UCI 数据集综合表现

| Dataset | Task | Model | Metric | Value |
|--------|------|-------|--------|-------|
| Boston Housing | Reg | NTK-ECRN | $R^2$ | **0.89** (vs. 0.87 ResNet) |
| Iris | Cls | NTK-ECRN | Acc | **96.1%** |
| Wine | Cls | NTK-ECRN | Acc | **97.0%** |

> ✅ 全体领先，证明其跨任务鲁棒性。

#### 表 IV：CIFAR-10 子集分类性能

| Model | Accuracy (%) | CE Loss |
|-------|--------------|--------|
| MLP | 74.1 | 0.815 |
| ResNet-18 | 78.4 | 0.712 |
| Standard NTK | 71.5 | 0.843 |
| **NTK-ECRN** | **81.9** | **0.648** |

> 💡 即使在图像任务中也表现出色，说明 Fourier + 控制性残差结构具有迁移潜力。

### 🔬 消融实验与关键分析（来自 Fig. 2 及讨论）

- **NTK 特征值演化分析**：
  - NTK-ECRN 的 $\lambda_{\max}(t)$ 增长平滑且可控；
  - ResNet-18 和 Standard NTK 出现剧烈震荡或快速发散；
  - $\|\Delta \Theta^{(t)}\|_F$ 增长缓慢，表明 kernel 更稳定。

- **消融启示**（文中虽未单独列出表格，但从 ablation 式描述可得）：
  - 移除 Fourier embedding → 高频学习变慢，$R^2$ 下降明显；
  - 移除 $\alpha_l$ 缩放 → $\lambda_{\max}$ 快速上升，出现训练不稳定；
  - 移除 Stochastic Depth → 泛化误差略升，尤其在小样本场景。

---

## 4. 关键结论和发现

### 🎯 主要发现

1. **NTK 演化可控性直接影响训练稳定性与泛化能力**  
   通过设计残差缩放系数 $\alpha_l$ 和 Fourier 特征，可以有效调控 NTK 的特征值谱演化路径，防止最大特征值爆炸，维持“边缘稳定”状态。

2. **Fourier Feature Embeddings 显著缓解 spectral bias**  
   使得网络能够在早期阶段就学习到高频率的数据结构，解决了传统 MLP “先学低频后学高频” 的瓶颈。

3. **Stochastic Depth 不仅正则化，还保留了解析性质**  
   在不牺牲理论可追踪性的前提下增强了泛化能力。

4. **理论与实践可以协同推进**  
   NTK-ECRN 成功实现了从“数学推导 → 架构设计 → 实验验证”的完整链条，展示了 deep learning theory 如何指导实际模型改进。

### ⚠️ 方法的局限性

- **计算开销较大**：全程跟踪 NTK 矩阵需 $O(n^2)$ 内存（$n$: 样本数），限制了在大规模数据上的直接应用。
- **依赖良好初始化与参数调优**：$\alpha_l$、$B$（Fourier 矩阵）等超参数需要谨慎选择。
- **目前主要用于中小规模任务**：尚未在 ImageNet 等大型视觉任务上验证 scalability。

### 🔮 未来工作方向

1. **扩展至 CNN / Transformer 架构**：将 NTK-ECRN 设计原则推广到卷积或注意力模块中。
2. **自适应 $\alpha_l$ 机制**：设计动态调整缩放系数的方法，进一步自动化控制 NTK 演化。
3. **高效 NTK 近似算法**：开发低秩或随机估计方法，降低 kernel tracking 的计算成本。
4. **连接 Feature Learning Regime**：探索超越 NTK 线性化区域的行为，向“feature learning”过渡。

---

## 总结

✅ **NTK-ECRN 是一项将 deep learning theory 转化为可工程实现架构的成功尝试**。它不仅在多个任务上取得了优于主流模型的性能，更重要的是建立了一个**可解释、可控制、可验证**的训练动态分析范式，为下一代“理论驱动”的神经网络设计提供了重要参考。

</details>

---

### 14. [SkipKV: Selective Skipping of KV Generation and Storage for Efficient Inference with Large Reasoning Models](https://arxiv.org/abs/2512.07993)

**Authors**: Jiayi Tian, Seyedarmin Azizi, Yequan Zhao, Erfan Baghaei Potraghloo, Sean McPherson, Sharath Nittur Sridhar, Zhengyang Wang, Zheng Zhang, Massoud Pedram, Souvik Kundu  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.07993v1  

#### Abstract
Large reasoning models (LRMs) often cost significant key-value (KV) cache overhead, due to their linear growth with the verbose chain-of-thought (CoT) reasoning process. This costs both memory and throughput bottleneck limiting their efficient deployment. Towards reducing KV cache size during infere...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SkipKV: Selective Skipping of KV Generation and Storage for Efficient Inference with Large Reasoning Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型推理模型（Large Reasoning Models, LRMs）在执行复杂推理任务（如数学推导、代码生成）时，通常依赖冗长的 Chain-of-Thought（CoT）过程，导致 Key-Value（KV）缓存急剧增长。这带来了两个核心挑战：

- **内存瓶颈**：KV 缓存随推理长度线性增长，占用大量显存，限制了批量处理能力。
- **效率下降**：传统 KV 压缩方法（如基于 token 重要性的剪枝）在多批次（multi-batch）场景下精度显著下降，且可能因上下文丢失引发“过度思考”（overthinking），反而延长生成长度。

此外，现有方法（如 H2O、R-KV）存在以下缺陷：
- 仅在 token 粒度进行剪枝，破坏语义连贯性；
- 忽视句子级冗余，导致重复验证和无效推理；
- 多批次推理中因 padding tokens 导致有效 KV 预算减少，影响稳定性。

---

### **提出了什么新方法或新思路**

论文提出 **SkipKV**，一种无需训练的、面向句子级别的 KV 压缩框架，通过**选择性跳过 KV 存储与生成**来提升推理效率。其核心创新包括：

#### （1）**句子级冗余检测与 KV 存储跳过（Sentence-level Skipping of KV Storage）**
- 引入 **Pairwise Sentence Similarity (PSS)** 指标，基于最后一层隐藏状态计算句子嵌入的余弦相似度。
- 定义冗余句对（PSS ≥ 0.95），优先移除早期高度相似的完整句子，保留语义主干。
- 设计 **KV Cache Range Monitoring Logic**，确保生成空间与缓存空间的句子范围映射一致，避免错位删除。

#### （2）**自适应 KV 引导机制（Adaptive Steering for KV Generation Skipping）**
- 构建 steering vector：通过 MATH 数据集上 execution 与 non-execution 思维的隐藏状态差异得到。
- 动态调整引导强度 $ \alpha_t = \alpha_0 + \gamma \cdot N_o $，其中 $ N_o $ 是非执行思维计数器，越冗余则引导越强，抑制无意义生成。

#### （3）**批处理分组策略（Batch Grouping for Multi-batch Decoding）**
- 将输入样本按 prefill 长度排序后分组，减少同一批次内的长度差异，从而降低 padding tokens 数量。
- 提高有效 KV 预算（valid KV budget），增强多批次下的稳定性和准确性。

---

### **相比现有方法的优势**

| 维度 | SkipKV | 现有方法（如 H2O, R-KV） |
|------|--------|--------------------------|
| **粒度** | 句子级（语义单元） | Token 级（碎片化） |
| **生成长度** | 显著缩短（减少 up to 48%） | 往往更长（因上下文丢失需重验证） |
| **多批次表现** | 稳定，精度保持高 | 精度显著下降 |
| **语义连贯性** | 保持推理流完整性 | 易打断关键步骤（如删数字） |
| **内存效率** | 更高压缩比下维持准确率 | 压缩后精度损失大 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **数学推理任务**：
  - **MATH-500**：500 道高中数学竞赛题。
  - **AIME-24**：美国数学邀请赛题目，难度更高。
  - **GSM8K**：小学数学应用题，测试基础推理。
- **代码生成任务**：
  - **LiveCodeBench**：编程问题，评估代码生成能力。

---

### **实验设置和评估指标**

#### **模型**
- **DeepSeek-R1-Distill-Qwen-7B / 14B**
- **DeepSeek-R1-Distill-Llama-8B**

#### **评估指标**
- **Accuracy (pass@1)**：最终答案正确率。
- **Total Token Length**：生成总 token 数，反映推理效率。
- **Throughput (samples/min)**：每分钟处理样本数，衡量端到端吞吐。
- **KV Cache Budget Ratio**：分配的 KV 缓存大小占 FullKV 平均长度的比例（如 20%, 30%）。

#### **实验配置**
- 批大小（batch size）：10（除非特别说明）
- 最大生成长度：8192–16384 tokens
- 硬件：单张 NVIDIA A100 (40GB)

---

### **基线方法对比**

| 方法 | 类型 | 特点 |
|------|------|------|
| **FullKV** | 基线 | 不压缩 KV 缓存，精度最高但内存开销最大 |
| **H2O** | KV Eviction | 基于累积注意力分数剪枝低重要性 token |
| **R-KV** | KV Eviction | 基于 token 级冗余评分剪枝，当前 SoTA |
| **SEAL** | Steering | 引导隐藏状态以缩短推理路径，不直接压缩 KV |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **精度表现（Accuracy）**
- 在 **AIME-24** 上，SkipKV 使用 **仅 15% KV 预算**即可达到 FullKV 的精度。
- 在 **R1-Qwen-14B** 上，SkipKV 比 R-KV 提升高达 **+26.7% 准确率**（相同压缩预算下）。
- 在 **LiveCodeBench** 上，SkipKV 比 FullKV 提升 **+5.2% 精度**，同时使用 **一半 KV 内存**。

#### ✅ **生成效率（Token Length）**
- 相比 FullKV，SkipKV **减少最多 30% 生成长度**。
- 相比 R-KV，SkipKV 生成 token 数少 **32%~48%**（依模型而定）：
  - R1-Qwen-7B: -32%
  - R1-Qwen-14B: -39%
  - R1-Llama-8B: -48%

#### ✅ **吞吐量（Throughput）**
- 在 GSM8K 上，SkipKV 达到 **9.6× 加速** vs FullKV。
- 相比 R-KV，SkipKV 吞吐提升 **up to 1.7×**，得益于更短生成长度。
- 支持更大 batch size（如 140 vs FullKV OOM），突破内存瓶颈。

#### ✅ **KV 内存节省**
- 实现 **2× 至 6.6× KV 内存压缩**，具体取决于任务和模型。
- 在 AIME-24 上，SkipKV 仅用 **2.8GB KV 缓存**，而 FullKV 需 **18.5GB**。

---

### **消融实验结果**

| 组件 | Accuracy ↑ | Token Length ↓ | 说明 |
|------|------------|----------------|------|
| **R-KV 基线** | 36.7% @20% KV | 12000 | — |
| + **Sentence Scoring** | +3.3% | -6% | 移除冗余句，小幅提升 |
| + **Adaptive Steering** | +10.0% | -26% | 动态抑制无意义生成，大幅减长 |
| + **Batch Grouping** | **+13.3%** | **-30%** | 提高有效预算，全面提升 |

> 结论：三个组件协同作用，实现从局部过滤到全局协调的层次化优化。

---

### **Batch Grouping 效果验证（Table 3）**

| KV Budget | w/o Grouping Acc | w/ Grouping Acc | 提升 |
|-----------|------------------|------------------|------|
| 768 (26%) | 77.8% | **83.4%** | +5.6% |
| 1024 (34%) | 85.2% | **86.4%** | +1.2% |

> 表明 batch grouping 显著缓解 padding 导致的有效预算不足问题。

---

## 4. 关键结论和发现

### **主要发现**

1. **token 级剪枝破坏语义连贯性**：现有方法（如 R-KV）虽能压缩 KV，但常删除关键数值或答案片段，迫使模型反复验证，导致“越压缩越长”的悖论。
2. **句子级冗余是 CoT 推理中的主要浪费源**：错误回答中高相似句子比例高出正确回答 **1.7×**，非执行思维多出 **2.6×**。
3. **SkipKV 实现“更短、更准、更快”**：
   - 通过句子级剪枝保持推理完整性；
   - 自适应 steering 抑制冗余生成；
   - batch grouping 提升多批次鲁棒性。
4. **无需训练即可部署**：SkipKV 为纯推理期优化，兼容现有 LRM，易于集成。

---

### **方法的局限性**

- **依赖句子分割质量**：目前使用简单规则（换行符、标点）切分句子，可能误判复合句结构。
- **steering vector 需离线构建**：需额外使用 MATH 训练集提取 execution/non-execution 差异向量。
- **极端压缩下仍可能丢信息**：当 KV 预算极低（<10%）时，即使 SkipKV 也可能出现精度滑坡。

---

### **未来工作方向**

- 探索动态句子边界识别（如基于语义转折）。
- 将 steering 机制扩展至多层或多头 attention。
- 结合量化（quantization）进一步压缩 KV 存储（作者指出此为正交方向）。
- 应用于其他长推理场景，如法律分析、科学文献理解等。

---

> **总结**：SkipKV 提出了一种语义感知的、句子级的 KV 压缩范式，解决了传统 token 级方法在 CoT 推理中的根本缺陷，在精度、长度、吞吐三方面全面超越 SoTA，为大规模推理模型的高效部署提供了实用解决方案。

</details>

---

### 15. [Beyond Traditional Diagnostics: Transforming Patient-Side Information into Predictive Insights with Knowledge Graphs and Prototypes](https://arxiv.org/abs/2512.08261)

**Authors**: Yibowen Zhao, Yinan Zhang, Zhixiang Su, Lizhen Cui, Chunyan Miao  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.08261v1  

#### Abstract
Predicting diseases solely from patient-side information, such as demographics and self-reported symptoms, has attracted significant research attention due to its potential to enhance patient awareness, facilitate early healthcare engagement, and improve healthcare system efficiency. However, existi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Beyond Traditional Diagnostics: Transforming Patient-Side Information into Predictive Insights with Knowledge Graphs and Prototypes

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**仅基于患者端信息**（如人口统计学特征和自述症状）进行疾病预测所面临的三大挑战：
- **数据分布不平衡**：常见病样本远多于罕见病，导致模型对长尾疾病（long-tailed diseases）预测性能差。
- **缺乏可解释性**：现有模型难以提供临床可信、透明的推理过程，限制了在真实医疗场景中的应用。
- **忽略结构化医学知识**：多数方法依赖浅层文本特征，未有效整合权威医学知识，易产生语义缺失或误分类。

### 提出的新方法：KPI 框架
作者提出 **KPI**（Knowledge graph-enhanced, Prototype-aware, and Interpretable）框架，其核心创新如下：

#### （1）构建统一的疾病知识图谱（Disease Knowledge Graph）
- 利用 **LLM** 从权威医学描述中提取结构化三元组（实体-关系-实体），并通过语义对齐与融合技术构建一个统一、去冗余的知识图谱 $ \mathcal{G}_u $。
- 引入两阶段提示策略：
  - 第一阶段：抽取疾病相关三元组；
  - 第二阶段：标准化关系定义，确保跨疾病的语义一致性。

#### （2）原型感知学习（Prototype-aware Learning）
- 基于知识图谱初始化具有临床意义的**疾病原型**（disease prototypes），作为每类疾病的语义锚点。
- 在训练过程中通过图注意力网络（GAT）动态更新原型嵌入，使其既能保持医学语义一致性，又能适应多样化的患者叙述。

#### （3）对比学习提升长尾疾病识别能力
- 采用 **prototype-based contrastive learning**，将患者表征与正确疾病原型拉近，与其他原型推远。
- 显著提升了对低频/罕见疾病的判别能力。

#### （4）可解释性增强机制
- 结合患者叙述与预测结果，检索个性化的子图（patient-specific subgraph）。
- 利用 **LLM** 生成基于知识图谱证据的、个性化且医学上合理的解释，提升模型透明度和医生信任度。

### 相比现有方法的优势
| 维度 | KPI 的优势 |
|------|-----------|
| **准确性** | 在高度不平衡的数据下仍能准确预测罕见病（如 Cold 类 hit@1 达 0.88） |
| **可解释性** | 提供基于知识图谱的、可追溯的解释，优于黑箱模型或通用 LLM 输出 |
| **泛化性** | 融合结构化知识与自由文本，增强了对噪声和稀疏输入的鲁棒性 |
| **效率** | 推理速度快（17.7 秒完成全测试集），适合实际部署 |

---

## 2. 核心实验方法和设置

### 数据集
使用公开的真实世界医患咨询数据集 **Haodf**，涵盖六种疾病类别：
- Common Cold (Cold)
- Pneumonia (Pneu.)
- Diabetes (Diab.)
- Depression (Depr.)
- Coronary Heart Disease (CHD)
- Lung Cancer (Lung)

| 特征 | 描述 |
|------|------|
| 数据来源 | 中国在线医疗平台“好大夫” |
| 总记录数 | 超过 3 万条 |
| 输入信息 | 自由文本症状叙述 + 自报人口统计信息（年龄、性别等） |
| 标签 | 医生确认诊断结果 |

> 注：该数据集是目前唯一公开支持“患者侧信息→疾病预测”研究的带标注数据集。

### 评估指标
- **Hit Rate**：hit@1, hit@3, hit@10（前 k 名命中率）
- **AUC**：ROC 曲线下面积
- **NDCG**：归一化折损累计增益（无截断）
- 所有指标越高越好。

### 基线方法对比
分为四类共 **17 个基线模型**：

#### （1）传统机器学习
- Decision Tree, KNN, AdaBoost（仅使用结构化字段）

#### （2）通用预训练语言模型（PLMs）
- BERT, RoBERTa, ALBERT, Electra, GIST, Granite, GTE, FinBERT

#### （3）医学领域 PLMs
- BioBERT, BioLORD, MedBERT

#### （4）先进疾病预测模型
- **PoMP**：当前最先进的患者侧预测模型，结合文本与结构化输入
- **LLMs**：GPT-3.5 Turbo, Claude 3, LLaMA 3.1, Gemini, DeepSeek V3, GLM-4 Flash, Qwen Turbo（通过问答提示获取输出）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table III 和 IV）

| 模型 | Cold (hit@1) | Pneu. (hit@1) | CHD (hit@1) | Lung (hit@1) | 平均排名 |
|------|-------------|--------------|------------|-------------|----------|
| KPI | **0.8808** | **0.9269** | **0.6755** | **0.9238** | **1.8** ✅ |
| DeepSeek | 0.5973 | 0.6676 | 0.5809 | 0.7519 | 3.0 |
| Gemini | 0.6166 | 0.6200 | 0.5770 | 0.6837 | 2.6 |
| PoMP | 0.7738 | 0.2402 | 0.3692 | 0.5997 | 5.8 |
| BioBERT | 0.5135 | 0.2457 | 0.3993 | 0.5470 | 11.0 |

> 💡 **亮点**：
> - 在最稀有的 **Cold** 类上，KPI 的 hit@1 是第二名（DeepSeek）的 **1.48 倍**。
> - 在所有类别中，KPI 的 **hit@1 平均领先第二名超过 20%**。
> - **NDCG 和 AUC 同样显著领先**，说明排序质量更高。

### 与 LLM 的对比（Table IV）
尽管主流 LLM（如 GPT、Claude、Gemini）在常识推理上有优势，但在本任务中表现不如 KPI：
- KPI 在 **所有疾病类别上的 hit@1 都显著优于所有 LLM**。
- LLM 缺乏显式的医学知识约束，容易出现“幻觉”或泛化偏差。
- KPI 的设计更专注于**疾病特异性线索捕捉**和**结构化推理**。

### 消融实验结果（Table VI）

| 变体 | Lung (AUC) | Cold (hit@1) | 说明 |
|------|-----------|-------------|------|
| Textual（仅文本） | 0.7943 | 0.6615 | 忽略结构化信息导致性能下降 |
| Textual w/ S（加结构化输入） | 0.7765 | 0.7077 | 改进有限，缺乏知识引导 |
| KPI w/o S.C（无语义一致性损失） | 0.8994 | 0.8462 | 性能下降，证明正则项有效 |
| KPI w/o P.K（随机初始化原型） | 0.8054 | 0.8615 | 医学知识缺失严重影响性能 |
| **完整 KPI** | **0.9137** | **0.8769** | 最优配置 |

> 🔍 发现：
> - **引入知识图谱构建的原型至关重要**，移除后 AUC 下降超 10%。
> - **语义一致性正则项**帮助文本与图嵌入对齐，尤其在长尾疾病中作用明显。

### 效率与可扩展性分析（Table V）
- KPI 在 **仅 30% 训练数据**时即可达到接近全量数据的性能。
- 完整测试集推理耗时仅 **17.72 秒**，优于 PoMP（18.48 秒）和 GTE（20.81 秒）。
- 表明 KPI 不仅准确，而且具备良好的**低资源适应性和部署效率**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **KPI 在患者侧疾病预测任务上全面超越现有方法**，尤其是在长尾疾病（如感冒、抑郁症）上表现出卓越的鲁棒性。
2. ✅ **知识图谱 + 原型学习** 的组合能有效缓解数据不平衡问题，并增强模型的语义理解能力。
3. ✅ **可解释性模块生成的解释获得医生高评分**（平均 >4.1/5），表明其具备临床可用价值。
4. ✅ **对比学习 + 语义一致性正则化** 是提升模型泛化能力和稳定性的重要机制。
5. ✅ 即使面对少量训练样本，KPI 也能快速收敛并保持高性能，适用于现实世界的低资源场景。

### 方法的局限性
- **依赖高质量外部知识源**：若初始医学描述存在错误或偏见，可能影响知识图谱质量。
- **子图检索依赖真实标签**：训练阶段需已知疾病标签以获取 2-hop 子图，限制了完全无监督的应用。
- **尚未集成多模态信号**：当前仅处理文本和结构化字段，未考虑图像、语音等其他患者端数据。

### 未来工作方向
1. **失败案例分析**：深入研究 KPI 预测错误的情况，优化知识图谱覆盖范围。
2. **扩展至多模态输入**：融合语音、手写笔记、家庭监测设备数据等非文本信号。
3. **更大规模疾病域迁移**：验证 KPI 在更多疾病种类（如肿瘤、神经系统疾病）中的适用性。
4. **实时交互式系统开发**：构建支持动态追问、反馈修正的智能分诊助手。

---

> 📌 **总结一句话**：  
> KPI 通过融合 **Knowledge Graph**、**Prototype Learning** 和 **LLM-based Explanation**，实现了高精度、强可解释、面向患者侧信息的疾病预测新范式，在真实世界医疗 AI 应用中展现出巨大潜力。

</details>

---

### 16. [Towards a Science of Scaling Agent Systems](https://arxiv.org/abs/2512.08296)

**Authors**: Yubin Kim, Ken Gu, Chanwoo Park, Chunjong Park, Samuel Schmidgall, A. Ali Heydari, Yao Yan, Zhihan Zhang, Yuchen Zhuang, Mark Malhotra, Paul Pu Liang, Hae Won Park, Yuzhe Yang, Xuhai Xu, Yilun Du, Shwetak Patel, Tim Althoff, Daniel McDuff, Xin Liu  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.08296v1  

#### Abstract
Agents, language model (LM)-based systems that are capable of reasoning, planning, and acting are becoming the dominant paradigm for real-world AI applications. Despite this widespread adoption, the principles that determine their performance remain underexplored, leaving practitioners to rely on he...

---

### 17. [CarBench: A Comprehensive Benchmark for Neural Surrogates on High-Fidelity 3D Car Aerodynamics](https://arxiv.org/abs/2512.07847)

**Authors**: Mohamed Elrefaie, Dule Shu, Matt Klenk, Faez Ahmed  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.07847v1  

#### Abstract
Benchmarking has been the cornerstone of progress in computer vision, natural language processing, and the broader deep learning domain, driving algorithmic innovation through standardized datasets and reproducible evaluation protocols. The growing availability of large-scale Computational Fluid Dyn...

---

### 18. [RaX-Crash: A Resource Efficient and Explainable Small Model Pipeline with an Application to City Scale Injury Severity Prediction](https://arxiv.org/abs/2512.07848)

**Authors**: Di Zhu, Chen Xie, Ziwei Wang, Haoyun Zhang  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.07848v1  

#### Abstract
New York City reports over one hundred thousand motor vehicle collisions each year, creating substantial injury and public health burden. We present RaX-Crash, a resource efficient and explainable small model pipeline for structured injury severity prediction on the official NYC Motor Vehicle Collis...

---

### 19. [Neural Ordinary Differential Equations for Simulating Metabolic Pathway Dynamics from Time-Series Multiomics Data](https://arxiv.org/abs/2512.08732)

**Authors**: Udesh Habaraduwa, Andrei Lixandru  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.08732v1  

#### Abstract
The advancement of human healthspan and bioengineering relies heavily on predicting the behavior of complex biological systems. While high-throughput multiomics data is becoming increasingly abundant, converting this data into actionable predictive models remains a bottleneck. High-capacity, datadri...

---

### 20. [Predicting California Bearing Ratio with Ensemble and Neural Network Models: A Case Study from T\"urkiye](https://arxiv.org/abs/2512.08340)

**Authors**: Abdullah Hulusi K\"ok\c{c}am, U\u{g}ur Da\u{g}deviren, Talas Fikret Kurnaz, Alparslan Serhat Demir, Caner Erden  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08340v1  

#### Abstract
The California Bearing Ratio (CBR) is a key geotechnical indicator used to assess the load-bearing capacity of subgrade soils, especially in transportation infrastructure and foundation design. Traditional CBR determination relies on laboratory penetration tests. Despite their accuracy, these tests ...

---

### 21. [The SMART+ Framework for AI Systems](https://arxiv.org/abs/2512.08592)

**Authors**: Laxmiraju Kandikatla, Branislav Radeljic  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08592v1  

#### Abstract
Artificial Intelligence (AI) systems are now an integral part of multiple industries. In clinical research, AI supports automated adverse event detection in clinical trials, patient eligibility screening for protocol enrollment, and data quality validation. Beyond healthcare, AI is transforming fina...

---

### 22. [Softly Symbolifying Kolmogorov-Arnold Networks](https://arxiv.org/abs/2512.07875)

**Authors**: James Bagrow, Josh Bongard  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.07875v1  

#### Abstract
Kolmogorov-Arnold Networks (KANs) offer a promising path toward interpretable machine learning: their learnable activations can be studied individually, while collectively fitting complex data accurately. In practice, however, trained activations often lack symbolic fidelity, learning pathological d...

---

### 23. [Unveiling Latent Knowledge in Chemistry Language Models through Sparse Autoencoders](https://arxiv.org/abs/2512.08077)

**Authors**: Jaron Cohen, Alexander G. Hasson, Sara Tanovic  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08077v1  

#### Abstract
Since the advent of machine learning, interpretability has remained a persistent challenge, becoming increasingly urgent as generative models support high-stakes applications in drug and material discovery. Recent advances in large language model (LLM) architectures have yielded chemistry language m...

---

### 24. [Robust Agents in Open-Ended Worlds](https://arxiv.org/abs/2512.08139)

**Authors**: Mikayel Samvelyan  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08139v1  

#### Abstract
The growing prevalence of artificial intelligence (AI) in various applications underscores the need for agents that can successfully navigate and adapt to an ever-changing, open-ended world. A key challenge is ensuring these AI agents are robust, excelling not only in familiar settings observed duri...

---

### 25. [SOFA-FL: Self-Organizing Hierarchical Federated Learning with Adaptive Clustered Data Sharing](https://arxiv.org/abs/2512.08267)

**Authors**: Yi Ni, Xinkun Wang, Han Zhang  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08267v1  

#### Abstract
Federated Learning (FL) faces significant challenges in evolving environments, particularly regarding data heterogeneity and the rigidity of fixed network topologies. To address these issues, this paper proposes \textbf{SOFA-FL} (Self-Organizing Hierarchical Federated Learning with Adaptive Clustere...

---

### 26. [Transformers for Multimodal Brain State Decoding: Integrating Functional Magnetic Resonance Imaging Data and Medical Metadata](https://arxiv.org/abs/2512.08462)

**Authors**: Danial Jafarzadeh Jazi, Maryam Hajiesmaeili  
**Category**: cs.LG  
**Published**: 2025-12-10  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.08462v1  

#### Abstract
Decoding brain states from functional magnetic resonance imaging (fMRI) data is vital for advancing neuroscience and clinical applications. While traditional machine learning and deep learning approaches have made strides in leveraging the high-dimensional and complex nature of fMRI data, they often...

---

### 27. [Impact of Data-Oriented and Object-Oriented Design on Performance and Cache Utilization with Artificial Intelligence Algorithms in Multi-Threaded CPUs](https://arxiv.org/abs/2512.07841)

**Authors**: Gabriel M. Arantes, Richard F. Pinto, Bruno L. Dalmazo, Eduardo N. Borges, Giancarlo Lucca, Viviane L. D. de Mattos, Fabian C. Cardoso, Rafael A. Berri  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.07841v1  

#### Abstract
The growing performance gap between multi-core CPUs and main memory necessitates hardware-aware software design paradigms. This study provides a comprehensive performance analysis of Data Oriented Design (DOD) versus the traditional Object-Oriented Design (OOD), focusing on cache utilization and eff...

---

### 28. [The High Cost of Incivility: Quantifying Interaction Inefficiency via Multi-Agent Monte Carlo Simulations](https://arxiv.org/abs/2512.08345)

**Authors**: Benedikt Mangold  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.08345v1  

#### Abstract
Workplace toxicity is widely recognized as detrimental to organizational culture, yet quantifying its direct impact on operational efficiency remains methodologically challenging due to the ethical and practical difficulties of reproducing conflict in human subjects. This study leverages Large Langu...

---

### 29. [Reflecting with Two Voices: A Co-Adaptive Dual-Strategy Framework for LLM-Based Agent Decision Making](https://arxiv.org/abs/2512.08366)

**Authors**: Wentao Zhang, Qunbo Wang, Tao Zhang, Junsheng Wu, Hongping Gan, Yang Liu, Ling Dai, Shizhuang Deng, Shuntong Sun  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.08366v1  

#### Abstract
Large language model (LLM) agents often rely on external demonstrations or retrieval-augmented planning, leading to brittleness, poor generalization, and high computational overhead. Inspired by human problem-solving, we propose DuSAR (Dual-Strategy Agent with Reflecting) - a demonstration-free fram...

---

### 30. [Performance Comparison of Aerial RIS and STAR-RIS in 3D Wireless Environments](https://arxiv.org/abs/2512.08755)

**Authors**: Dongdong Yang, Bin Li, Jiguang He  
**Category**: cs.AI  
**Published**: 2025-12-10  
**Score**: 4.0  
**Type**: new  
**ArXiv ID**: 2512.08755v1  

#### Abstract
Reconfigurable intelligent surface (RIS) and simultaneously transmitting and reflecting RIS (STAR-RIS) have emerged as key enablers for enhancing wireless coverage and capacity in next-generation networks. When mounted on unmanned aerial vehicles (UAVs), they benefit from flexible deployment and imp...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
