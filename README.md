# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-17 05:57:09 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [A Unified Sparse Attention via Multi-Granularity Compression](https://arxiv.org/abs/2512.14082)

**Authors**: Siran Liu, Zane Cao, Yongchao He  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.14082v1  

#### Abstract
Efficient long-context understanding and reasoning are increasingly vital for large language model (LLM) applications such as multi-turn dialogue and program analysis. However, the core self-attention mechanism scales quadratically with sequence length, creating a fundamental computational bottlenec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Unified Sparse Attention via Multi-Granularity Compression

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLM）中的 **self-attention** 机制在长序列上下文建模中面临严重的计算瓶颈，其计算复杂度为 $O(L^2)$，其中 $L$ 是序列长度。随着上下文长度从数千扩展到数十万 token（如 128K），注意力计算成本呈指数级增长，成为推理效率的主要瓶颈。

现有稀疏注意力方法存在以下权衡：
- **训练耦合方法**（如 MInference、Mixture-of-Block-Attention）：精度高，但需额外训练，无法作为通用加速插件用于已有模型。
- **推理时方法**：无需再训练，但往往牺牲效率或跨模态泛化能力，例如：
  - 静态模式（如 BigBird）缺乏输入自适应性；
  - 动态方法（如 XAttention、FlexPrefill）依赖简化启发式规则，难以捕捉语义相关性。

因此，亟需一种**高效、通用、无需再训练且适用于多模态任务的稀疏注意力机制**。

---

### **提出了什么新方法或新思路**

本文提出 **UniSparse**，一种统一的、硬件友好的动态稀疏注意力机制，核心思想是通过**多粒度压缩**（multi-granularity compression）构建**复合 token**（composite tokens），实现高效的块级稀疏选择。

#### 核心创新点：

1. ✅ **复合 token（Composite Tokens）抽象**
   - 将原始细粒度 token 通过空间池化（如平均池化）聚合成粗粒度的“复合 token”，作为上下文信息的紧凑表示。
   - 在压缩空间中进行注意力评分，大幅降低代理计算开销。

2. ✅ **多粒度压缩机制**
   - 支持沿**序列维度**和**头维度**的可选压缩（`cq`, `ck`, `ch`），灵活控制精度与效率平衡。
   - 压缩后的注意力矩阵大小仅为原矩阵的 $1/(cq \cdot ck)$，显著减少计算量。

3. ✅ **动态块选择算法**
   - 在压缩空间计算注意力得分 → 聚合到原始块级别 → 使用 **Top-P 机制**选择最重要的 query-key 块对。
   - 生成稀疏掩码 $M$，指导后续的 block-sparse attention 执行。

4. ✅ **硬件友好设计**
   - 完全兼容 FlashAttention 等标准内核，支持 drop-in 替换。
   - 实现融合内核优化，中间结果驻留 on-chip memory，减少内存访问开销。

---

### **相比现有方法的优势**

| 维度 | 优势 |
|------|------|
| **无需训练** | 可直接应用于预训练模型，无需微调或离线搜索，真正“即插即用” |
| **跨模态通用性** | 复合 token 和平均池化具有模态无关性，适用于文本、视频等多模态任务 |
| **高精度保留** | 实验表明可达到 ≥99% 的 full-attention 准确率 |
| **高效性** | 最高实现 **2.61× 更快的注意力计算速度**（vs. FlashAttention） |
| **全局感知能力** | 相比局部探针方法（如 FlexPrefill），UniSparse 在压缩空间中评估所有 query-key 交互，避免遗漏重要依赖 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

实验覆盖三大类长上下文基准，涵盖合成任务、真实世界应用和多模态理解：

| 数据集 | 类型 | 描述 |
|--------|------|------|
| **RULER** | 合成基准 | 控制任务复杂度，测试检索、聚合、多跳推理等核心能力（最长 128K tokens） |
| **HELMET** | 真实世界基准 | 包含 7 个应用场景（如长文档 QA、摘要、RAG），要求深度语义理解（最长 128K tokens） |
| **Video-MME** | 多模态视频理解 | 涵盖 6 个视觉领域、30 个子任务，输入包括帧、字幕、音频，测试时空与跨模态推理 |

---

### **实验设置和评估指标**

#### **模型**
- **Meta-Llama-3.1-8B-Instruct**（纯文本）
- **Qwen2.5-7B-Instruct**（多语言文本）
- **Qwen2.5-VL-7B-Instruct**（视觉-语言多模态）

#### **评估场景**
- 所有方法仅在 **prefill 阶段**应用稀疏注意力，decode 阶段保持 dense。
- 上下文长度范围：**8K 到 128K tokens**（或对应视频时长）。

#### **评估指标**
- **准确率（Accuracy / Score）**：各任务的主指标（如 QA 准确率、召回率等）
- **稀疏度（Sparsity）**：被跳过的 attention block 比例
- **速度提升（Speedup）**：相对于 FlashAttention 的 end-to-end 注意力计算时间加速比
- **选择开销（Selection Overhead, Cselect）**：生成稀疏掩码的时间成本

---

### **基线方法对比**

| 方法 | 类型 | 特点 |
|------|------|------|
| **FlashAttention** | 全注意力基线 | 内存优化的 dense attention，提供精度上限 |
| **MInference** | 训练后动态稀疏 | 离线索引搜索 + head-specific 配置，高精度但昂贵 |
| **FlexPrefill** | 推理时动态稀疏 | 使用最后一个 query block 作为探针，局部评估，速度快但易漏重要信息 |
| **XAttention** | 推理时动态稀疏 | 使用 anti-diagonal scoring + stride sampling 进行全局采样，开销较高 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **精度表现（Accuracy Retention）**
- UniSparse 在多个任务上**一致优于或媲美现有 SOTA 稀疏方法**。
- 在 **RULER** 和 **HELMET** 上，UniSparse (P=0.95) 达到：
  - **≥99% 的 FlashAttention 精度**
  - 甚至在某些配置下**超过 full attention 表现**（如 Video-MME with subtitles）

#### ✅ **效率表现（Speedup）**
- **端到端注意力计算速度提升最高达 2.61×**（vs. FlashAttention）
- 在 128K 序列长度下：
  - UniSparse-0.95 实现 **2.52× 加速**
  - 启用 head compression (`ch=2`) 后进一步提升至 **2.61×**
- 图 3 显示，UniSparse 的 block selection 开销远低于 XAttention，在 128K 下实现 **1.62× ~ 2.64× 更快的选择过程**

#### ✅ **稀疏性与效率平衡**
- 使用更少的 attention blocks（约 1.5–2× 更少）即可达到相同或更高精度
- 在相似稀疏度下，UniSparse 性能普遍领先于 FlexPrefill 和 XAttention

---

### **与基线方法的对比结果**

| 方法 | 准确率 | 速度 | 适用性 |
|------|--------|------|--------|
| **FlexPrefill** | 中等偏低 | 快（局部探针） | 文本尚可，多模态差 |
| **XAttention** | 中等 | 较慢（strided sampling） | 全局但开销大 |
| **MInference** | 高 | 慢（需离线搜索） | 模型特定，不通用 |
| **UniSparse** | **高（≥99% full attn）** | **最快（2.61×）** | **跨模态、即插即用** |

> 示例：在 HELMET 上，UniSparse-0.95 在 48.65% 稀疏度下取得 **56.21** 分，优于 FlexPrefill-0.99（56.01）和 MInference（55.37）

---

### **消融实验结果**

#### 🔹 **压缩策略对比（Table 5）**
- **Average Pooling** 效果最佳：信息聚合最完整
- Max/Stochastic Pooling 虽然稀疏度更高，但精度下降明显（因丢失上下文信息）

#### 🔹 **Q-K 压缩比例分配（Table 6）**
- **平衡压缩（cq=ck=8）效果最好**
- 查询过度压缩（cq=32）导致性能严重下降
- 键偏向压缩（ck=16）在部分模型上有竞争力，值得进一步研究

#### 🔹 **压缩粒度（c）影响（Table 7）**
- 默认 `c=8` 是最佳折衷：
  - `c=4`：太精细，开销大
  - `c=16/32`：过度平滑，丢失细节
- `c=8` 在精度与效率间取得良好平衡

#### 🔹 **头压缩（Head Compression, ch）影响（Table 8）**
- `ch=2` 或 `4` 可显著降低 selection overhead（最高 2.64× vs XAttention）
- 代价是略微降低稀疏度（更多 block 被认为重要）
- 适合 selection 成为瓶颈的场景，trade-off 可控

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **压缩空间中的相对排序足以指导高质量块选择**
   - 实验证明，压缩 token 的重要性排名与原始空间高度一致（Spearman ρ > 0.98）
   - 说明 block selection 本质是 ranking 问题，而非精确 scoring

2. ✅ **平均池化是最优的复合 token 构造方式**
   - 能有效保留局部语义结构，避免信息丢失

3. ✅ **UniSparse 实现了精度与效率的帕累托前沿**
   - 在多种任务、模型、模态下均优于主流稀疏方法
   - 是目前唯一能在保持 ≥99% full-attention 精度的同时实现 >2.5× 加速的方法

4. ✅ **具备强跨模态泛化能力**
   - 在 Video-MME 上表现优异，尤其在带字幕场景下甚至超越 full attention，证明其能有效保留跨模态关键信息

---

### **方法的局限性**

1. ⚠️ **极端压缩可能导致细粒度信息丢失**
   - 如在需要精确定位的任务（如 citation localization）中，过高压缩可能影响性能

2. ⚠️ **Top-P 阈值需手动设定**
   - 虽然默认 `P=0.9/0.95` 表现良好，但尚未实现完全自适应阈值选择

3. ⚠️ **当前实现仍假设固定 block size**
   - 对变长输入或非均匀分布上下文的支持有待增强

---

### **未来工作方向**

1. 🔄 **自适应压缩因子调节**
   - 根据输入内容动态调整 `cq`, `ck`, `ch`，实现更智能的精度-效率权衡

2. 🧠 **结合 KV Cache 压缩与量化**
   - 与 SnapKV、KVCache-Q 等技术联合优化，进一步提升端到端推理效率

3. 🌐 **扩展至 encoder-decoder 架构**
   - 当前聚焦 decoder-only 模型，未来可适配 T5、BART 等架构

4. 🤖 **探索更复杂的聚合函数**
   - 如轻量级 attention-based pooling，替代简单平均池化，在不增加太多开销的前提下提升表达能力

---

> **总结一句话**：  
> UniSparse 通过引入 **复合 token + 多粒度压缩 + Top-P 块选择**，实现了**无需训练、跨模态通用、高精度、高效率**的稀疏注意力机制，是迈向实用化长上下文 LLM 推理的重要一步。

</details>

---

### 2. [Fast and Accurate Causal Parallel Decoding using Jacobi Forcing](https://arxiv.org/abs/2512.14681)

**Authors**: Lanxiang Hu, Siqi Kou, Yichao Fu, Samyam Rajbhandari, Tajana Rosing, Yuxiong He, Zhijie Deng, Hao Zhang  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.14681v1  

#### Abstract
Multi-token generation has emerged as a promising paradigm for accelerating transformer-based large model inference. Recent efforts primarily explore diffusion Large Language Models (dLLMs) for parallel decoding to reduce inference latency. To achieve AR-level generation quality, many techniques ada...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Fast and Accurate Causal Parallel Decoding using Jacobi Forcing 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代大型语言模型（LLMs）普遍采用 **Autoregressive (AR)** 解码方式逐个生成 token，导致推理延迟高、并行度低。虽然近期研究提出基于扩散的 **diffusion LLMs (dLLMs)** 来实现多 token 并行解码以加速推理，但这些方法存在以下问题：

- **Pretrain-to-posttrain mismatch**：dLLMs 在后训练阶段使用掩码数据和双向注意力机制，与预训练时的因果先验（causal prior）不一致，导致质量下降。
- **难以复用 KV Cache**：双向注意力破坏了因果性，阻碍了精确的 KV Cache 复用。
- **速度提升有限**：尽管引入了并行机制，但由于训练目标（如 NELBO）效率低下，实际加速效果受限。

### 提出的新方法：Jacobi Forcing
本文提出 **Jacobi Forcing** —— 一种渐进式蒸馏范式（progressive distillation），用于将标准 AR 模型高效地转化为高性能并行解码器（称为 **Jacobi Forcing Model**），其核心思想是：

- **在模型自身生成的 Jacobi 解码轨迹上进行训练**，无需修改原始模型的因果注意力结构。
- 引入 **noise-aware causal attention** 和 **progressive noise schedule**，使模型学会在噪声上下文中预测收敛点。
- 通过多轮渐进式蒸馏（逐步增大 block size），持续提升模型的并行解码能力。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **训练一致性** | 避免了 pretrain-posttrain 分布偏移，保留了 AR 模型原有的因果推理特性 |
| **KV Cache 利用** | 支持精确的 KV Cache 复用，提高推理效率 |
| **可扩展性** | 能随 block size 增大而稳定提升吞吐量，充分利用现代 AI 加速器算力 |
| **质量保持** | 在显著提速的同时，生成质量接近原始 AR 模型 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Coding Benchmarks**：
  - **HumanEval**（代码生成）
  - **MBPP**（Python 编程任务）
  - 训练数据：**OpenCodeInstruct**（450k prompts）
- **Mathematical Reasoning Benchmarks**：
  - **GSM8K**
  - **MATH**
  - 训练数据：**Openthought2** 中的数学类 prompt

所有模型均基于 **Qwen2.5-Coder-7B-Instruct** 或 **Qwen2.5-Math-7B-Instruct** 微调。

### 实验设置
- **硬件平台**：NVIDIA A100-80GB、H200、B200 GPU
- **训练配置**：
  - 学习率：1e-5
  - Batch size：4
  - Max sequence length：2048
  - Block size：初始为 16，第二轮训练扩大至 32
- **推理配置**：
  - Block size：128（除 MR 变体外）
  - 多块解码（multi-block decoding）支持最多 $ K=2 $ 个并发块
  - 初始化阈值 $ r=0.85 $

### 评估指标
| 指标 | 含义 |
|------|------|
| **TPF (Tokens Per Forward)** | 每次前向传播接受的 token 数量，衡量并行效率 |
| **TPS (Tokens Per Second)** | 实际每秒生成 token 数，反映真实推理速度 |
| **Speedup** | 相对于 AR 基线的速度提升倍数 |
| **Accuracy / Solve Rate** | HumanEval 上的 pass@1 准确率；GSM8K/MATH 上的问题解决率 |

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **AR-based Parallel Decoding** | Vanilla Jacobi, CLLM*, Jacobi Forcing (Ours) |
| **Diffusion-based LLMs (dLLMs)** | LLaDA-Instruct, Dream-Base, Fast-dLLM(DC), D2F |
| **Speculative Decoding**（附录补充） | EAGLE-3, HASS |
| **Distilled dLLM** | dParallel |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（A100 GPU）

#### 表格 1：Coding 任务表现（HumanEval & MBPP）

| 方法 | TPS | Speedup | Accuracy |
|------|-----|---------|----------|
| AR (Baseline) | 41.3 | 1.00× | 87.8% |
| CLLM* | 103.3 | 2.50× | 87.8% |
| **Jacobi Forcing Model** | **159.5** | **3.86×** | **83.5%** |
| **Jacobi Forcing Model (MR)** | **163.9** | **3.97×** | **83.5%** |

> 注：MR = Multi-block + Rejection Recycling

#### 表格 2：Math 任务表现（GSM8K & MATH）

| 方法 | TPS | Speedup | Solve Rate |
|------|-----|---------|------------|
| AR (Baseline) | 41.8 | 1.00× | 92.4% |
| **Jacobi Forcing Model** | **146.1** | **3.50×** | **91.4%** |
| **Jacobi Forcing Model (MR)** | **154.9** | **3.71×** | **91.4%** |

> 在 MATH 上甚至轻微提升了准确率（77.0% → 77.4%）

### 与基线方法的对比结果
- **相比 dLLMs**：
  - Jacobi Forcing 比 Dream-Base 快 **7.4×~15×**
  - 比 Fast-dLLM 和 D2F 快 **2.0×~2.2×**
  - 且准确率高出 **20~40 个百分点**
- **相比 AR-based 方法**：
  - 比 CLLM 快 **1.5×~1.8×**
  - 在保持相近质量的前提下实现更高吞吐

### 消融实验结果

#### （1）Noise Schedule 对比（表 4）
| Schedule | Window Size | Accuracy | iter/token |
|--------|-------------|----------|-----------|
| Random | 16 | 83.5% | 0.51 |
| **Linear Progressive** | **16** | **81.7%** | **0.46** ✅ |
| Reverse Progressive | 16 | 82.9% | 0.62 ❌ |

✅ 结论：**Linear Progressive Schedule** 显著降低迭代次数，提升训练效率。

#### （2）Attention Mask 设计（表 5）
| Mask Type | Speedup | Accuracy |
|----------|--------|----------|
| Noise-conditioned (NC) | **3.6×** | 82.3% |
| NC + Intra-window Clean Context (NC-IC) | 1.9× | 82.3% |

✅ 结论：**noise-aware causal mask** 更有效，避免因清洁上下文干扰学习噪声条件下的预测能力。

#### （3）FLOPs 利用分析（图 7）
- 在 H200/B200 上，最多可并行处理 **~256 tokens** 而不增加延迟；
- 在 A100 上约为 **~128 tokens**；
- 推理配置选择 **block size=64, verification size=4**（共 256 tokens）以最大化 FLOPs 利用。

---

## 4. 关键结论和发现

### 主要发现
1. **Jacobi Forcing 是有效的渐进式蒸馏框架**：
   - 成功将 AR 模型转化为高效的并行解码器，同时保留其高质量生成能力。
   - 实现了高达 **3.8×~4.0×** 的 wall-clock 速度提升。

2. **模型具备更强的“前瞻”能力**：
   - Jacobi Forcing Model 能在噪声上下文中生成更多正确后续 token（higher fast-forwarded token count）。
   - 尾部出现连续正确的“fixed-point segments”，可用于加速验证。

3. **Rejection Recycling + Multi-block Decoding 显著增强效率**：
   - 利用历史迭代中的高质量 n-gram 进行候选构造；
   - 多块并行维护允许提前解码后续 block；
   - 二者结合使每次迭代接受的 token 数提升 **4.5×**。

4. **优于当前主流并行解码方案**：
   - 在速度-质量权衡曲线上明显优于 dLLMs 和 speculative decoding 方法（见附录表 6）。

### 方法的局限性
- **仍依赖于 AR 模型作为基础架构**，无法完全摆脱自回归结构限制。
- **训练成本较高**：需要收集大量 Jacobi 轨迹，并进行两阶段渐进训练。
- **对长序列优化有限**：目前主要针对中等长度输出（<2048 tokens）设计。

### 未来工作方向
- 扩展到更大规模模型（如 70B+）和更复杂任务（如长文本生成、规划）。
- 探索非贪婪采样策略下的 Jacobi Forcing（目前基于 greedy decoding）。
- 结合 speculative decoding 构建混合加速系统。
- 自适应调整 block size 和 noise schedule 以应对不同输入复杂度。

---

> 🔗 **开源信息**：代码已公开于 GitHub  
> 👉 https://github.com/hao-ai-lab/JacobiForcing

</details>

---

### 3. [Meta Hierarchical Reinforcement Learning for Scalable Resource Management in O-RAN](https://arxiv.org/abs/2512.13715)

**Authors**: Fatemeh Lotfi, Fatemeh Afghah  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.13715v1  

#### Abstract
The increasing complexity of modern applications demands wireless networks capable of real time adaptability and efficient resource management. The Open Radio Access Network (O-RAN) architecture, with its RAN Intelligent Controller (RIC) modules, has emerged as a pivotal solution for dynamic resourc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
现代无线网络（特别是基于 **O-RAN** 架构）面临动态、异构且不可预测的资源管理挑战。传统方法在应对突发流量、非平稳环境以及多类型网络切片（如 eMBB、URLLC 和 mMTC）共存时，难以实现快速适应与高效资源分配。现有基于 **DRL** 或 **Meta-RL** 的方法往往缺乏稳定性保证、可扩展性验证不足，且对复杂场景的泛化能力有限。

### 提出的新方法与创新思路
本文提出了一种新型的 **自适应 Meta-Hierarchical Reinforcement Learning (Meta-HRL)** 框架，用于 O-RAN 中的可扩展资源管理和网络切片优化。其核心创新包括：

- **分层控制架构（Hierarchical Control）**：
  - 高层控制器（High-level Controller）负责跨切片的资源分配（Inter-slice Resource Allocation）；
  - 低层代理（Low-level Agents）执行各切片内的用户级调度（Intra-slice Scheduling）；
  - 这种分解显著降低了决策空间复杂度，提升了学习效率和可扩展性。

- **基于 MAML 的元学习机制（Meta-Learning Inspired by MAML）**：
  - 将每个分布式单元（DU）视为一个独立的 meta-task，利用 **Model-Agnostic Meta-Learning (MAML)** 思想训练一个全局 meta-model；
  - 实现“学会学习”（learn to learn），使模型能从历史任务中提取通用策略，并在新 DU 或突发需求下快速微调（few-shot adaptation）。

- **自适应加权元更新机制（Adaptive Variance-Weighted Meta Update）**：
  - 引入基于 **Temporal Difference Error (TD-error) 方差** 的动态权重 $ w_g = \text{Softmin}(\sigma^2_{\text{TD},g}) $；
  - 在元梯度更新中优先关注高方差任务（即更复杂、不确定性更高的网络状态），提升训练稳定性和收敛速度。

- **理论保障**：
  - 提供了该两层级学习过程的 **次线性收敛（sublinear convergence）** 和 **次线性遗憾界（regret bound）** 的理论分析，增强了方法的可信度。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **适应性** | 显著优于传统 DRL、Transfer Learning (TL) 和 Multi-Task Learning (MTL)，能在少量样本（few-shot）内完成快速适配； |
| **性能** | 在 QoS 满足率、吞吐量、公平性等方面全面领先； |
| **可扩展性** | 支持大规模 DU 和 UE 扩展，性能下降极小； |
| **鲁棒性** | 对突发流量、热点区域等 corner cases 表现出更强韧性； |
| **理论支持** | 是少数提供收敛性和 regret 分析的 O-RAN 资源管理方案之一。 |

---

## 2. 核心实验方法和设置

### 数据集与仿真环境
- 使用自研的 **O-RAN 仿真框架**，未依赖公开真实数据集；
- 网络拓扑包含多个 DU（Distributed Unit）、RU（Radio Unit）、CU（Centralized Unit）及 RIC 模块；
- 用户设备（UE）随机分布在不同区域，具有移动性建模（速度 10–20 m/s）；
- 支持三种典型网络切片：
  - **eMBB**（增强移动宽带）
  - **mMTC**（海量机器类通信）
  - **URLLC**（超可靠低时延通信）

### 实验设置
| 参数 | 设置 |
|------|------|
| 子载波间隔 | 15 kHz |
| 单个 DU 带宽 | 20 MHz |
| 每 DU RB 数量 $ K_p $ | 100 |
| 用户数 per DU | 30 |
| 分布式单元数量 $ N_g $ | 默认 7，最大扩展至 30 |
| 总用户数 $ N_u $ | 最大 200 |
| 学习算法 | DDPG（Actor-Critic 架构） |
| 神经网络结构 | 三层全连接（256→512→512），激活函数为 tanh |
| 优化器 | Adam ($ \text{lr} = 10^{-4} $) |
| 折扣因子 $ \gamma $ | 0.99 |

### 评估指标
- **累计奖励（Cumulative Reward）**
- **QoS 满足率（CDF of QoS）**
  - eMBB：平均吞吐量（Mbps）
  - mMTC：质量加权容量（Quality-Weighted Network Capacity）
  - URLLC：最大延迟（ms）
- **用户体验（QoE）**：用户吞吐量分布
- **适应性能（Adaptation Performance）**：不同 shot 数下的表现
- **可扩展性**：随 DU 和 UE 数量增加的性能变化
- **公平性**：Jain’s Fairness Index
- **延迟**：平均包延迟

### 基线方法对比
- **DRL**：从零开始训练的深度强化学习
- **Transfer Learning (TL)**：将已有 agent 的知识迁移到新任务
- **Multi-Task Learning (MTL)**：同时学习多个任务
- **MAML-RL**：标准元强化学习（无自适应加权）
- **Uniform-Meta**：等权元更新（消融实验用）

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **网络管理效率提升**：相比基线方法（DRL、TL、MTL、MAML-RL），所提 **Meta-HRL 框架实现了 19.8% 的最终回报值提升**（见图4）。
- **更快适应能力**：
  - 在 5-shot 场景下，**Adaptive MAML-RL 比 MAML-RL 提升约 4%**，比 DRL 提升高达 **73%**；
  - 消融实验显示，自适应加权机制使收敛所需适应步数减少 **40%**（从 28 降至 17）；
- **QoS 表现优异**：
  - **eMBB 切片**：吞吐量显著高于其他方法；
  - **mMTC 切片**：在高密度连接下仍保持良好服务质量；
  - **URLLC 切片**：最低延迟表现突出，满足严格时延要求；
- **可扩展性表现**：
  - 当 DU 数从 7 扩展到 30，UE 数达 200 时：
    - 收敛迭代次数仅增加 **69%**（接近次线性增长）；
    - 归一化累积奖励下降不到 **2%**，表明高度可扩展；
    - 元更新延迟控制在 **10ms 内**，符合 near-RT RIC 要求。

### 与基线方法的对比结果
| 方法 | 累计奖励 | 适应速度 | QoS 满足率 | 可扩展性 |
|------|----------|-----------|-------------|------------|
| DRL | ❌ 最低 | ❌ 几乎不随 shot 提升 | ❌ 差 | ❌ 差 |
| TL | ⭕ 中等 | ⭕ 有限提升 | ⭕ 一般 | ⭕ 一般 |
| MTL | ⭕ 中等 | ⭕ 有限提升 | ⭕ 一般 | ⭕ 一般 |
| MAML-RL | ✅ 较高 | ✅ 快速提升 | ✅ 良好 | ✅ 良好 |
| **Adaptive MAML-RL (本文)** | ✅✅ **最高 (+19.8%)** | ✅✅ **最快 (-40% shots)** | ✅✅ **最优** | ✅✅ **最佳** |

### 消融实验结果（Ablation Study）
在 7-DU/30-UE 场景下进行（30 个适应步）：

| 方法 | 归一化奖励 | 收敛所需适应步数 |
|------|--------------|---------------------|
| Uniform-Meta（等权） | 0.78 | 28 |
| Static-Var（静态方差加权） | 0.81 | 22 |
| **Adaptive-Var（本文）** | **0.84** | **17** |

> 结果表明：**动态 Softmin 加权机制贡献了约 3% 的性能增益和 40% 的加速效果**。

此外，该机制还带来以下改进：
- 平均包延迟降低 **9.2%**
- Jain’s Fairness 从 0.91 提升至 **0.96**
- 在 50% 流量激增场景下性能退化 < **5%**

---

## 4. 关键结论和发现

### 主要发现
1. **Meta-HRL 架构有效解决了 O-RAN 多尺度资源管理难题**，通过分层决策实现了全局协调与局部精细控制的平衡。
2. **自适应 TD-error 方差加权机制显著提升了元学习效率**，使得模型能够自动聚焦于更具挑战性的任务，避免“平均主义”导致的学习缓慢。
3. 所提方法在 **动态性、异构性和可扩展性** 方面表现出色，适用于现实世界中复杂的 O-RAN 部署。
4. **理论分析证实了算法的收敛性与稳定性**，为实际部署提供了信心。

### 方法的局限性
- 实验基于仿真平台，尚未在真实 O-RAN testbed 上验证（受限于当前硬件规模）；
- 元参数传输虽轻量（仅交换网络权重），但在超大规模部署中仍需考虑通信开销；
- 当前框架主要针对单跳场景，未涵盖 multi-hop 或 UAV-assisted 网络。

### 未来工作方向
- 在 **OAI** 或 **srsRAN** 等开源平台上实现原型系统，开展 **hardware-in-the-loop 验证**；
- 设计更高效的 **communication-efficient meta parameter update 机制**；
- 将框架扩展至 **multi-hop networking** 和 **UAV-assisted 6G scenarios**；
- 探索与其他 AI 技术（如联邦学习 FL）结合的可能性，进一步保护数据隐私。

--- 

> **总结**：本文提出的 **Adaptive Meta-HRL** 框架是首个将 MAML 与 HRL 深度融合并应用于 O-RAN 资源管理的工作，兼具高性能、强适应性、良好可扩展性与理论保障，为下一代智能无线网络管理提供了重要范式。

</details>

---

### 4. [PruneX: A Hierarchical Communication-Efficient System for Distributed CNN Training with Structured Pruning](https://arxiv.org/abs/2512.14628)

**Authors**: Alireza Olama, Andreas Lundell, Izzat El Hajj, Johan Lilius, Jerker Bj\"orkqvist  
**Category**: cs.DC  
**Published**: 2025-12-17  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.14628v1  

#### Abstract
Inter-node communication bandwidth increasingly constrains distributed training at scale on multi-node GPU clusters. While compact models are the ultimate deployment target, conventional pruning-aware distributed training systems typically fail to reduce communication overhead because unstructured s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PRUNEX: A Hierarchical Communication-Efficient System for Distributed CNN Training with Structured Pruning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在大规模多节点 GPU 集群上进行分布式深度学习训练时，**跨节点通信带宽已成为主要瓶颈**。尽管模型压缩（如剪枝）是部署阶段的常见手段，但传统的剪枝感知训练系统通常无法有效减少通信开销，原因在于：
- **非结构化稀疏性（Unstructured Sparsity）** 无法被高度优化的稠密集合通信原语（如 NCCL AllReduce）高效利用；
- 现有方法往往将剪枝作为“训练-剪枝-微调”的后处理步骤，浪费大量计算资源；
- 多数框架采用扁平化的通信拓扑，忽略了现代集群中**节点内（intra-node）与跨节点（inter-node）链路带宽的巨大差异**。

### 提出的新方法与思路
本文提出了 **PRUNEX** —— 一种层次化、通信高效的分布式 CNN 训练系统，其核心创新包括：

#### （1）提出 **Hierarchical Structured ADMM (H-SADMM)** 算法
- 将结构化剪枝约束（如通道/滤波器剪枝）施加在**节点级共识变量 $z_i$** 上，而非全局变量 $z$；
- 这使得在跨节点同步前即可实现**结构化稀疏性**，从而为物理缓冲区压缩提供前提。

#### （2）设计 **物理缓冲区收缩机制（Physical Buffer Shrinkage）**
- 利用全局一致的稀疏掩码（mask），在跨节点通信前将稀疏张量压缩为**紧凑的稠密块（dense compact buffer）**；
- 消除零值传输和索引元数据开销，直接使用标准的 **dense AllReduce** 进行高效聚合。

#### （3）构建 **领导者-跟随者（Leader-Follower）分层架构**
- 引入 **node leader** 角色，管理节点间通信；
- 分离 **intra-node** 和 **inter-node process groups**，确保高带宽链路用于全量同步，低带宽链路仅传输压缩后的活跃参数。

#### （4）开源实现
- 作者已将 PRUNEX 开源：[https://github.com/Alirezalm/PruneX](https://github.com/Alirezalm/PruneX)

### 相比现有方法的优势
| 方法 | 类型 | 元数据开销 | 通信原语 | 拓扑感知 | 推理加速 |
|------|------|------------|----------|-----------|-----------|
| Top-K / DGC | Unstructured | 高（Indices） | AllGather/Gossip | 否 | 无 |
| PacTrain | Unstructured | 低（Mask Tracker） | AllReduce | 否 | 低 |
| PruneTrain | Structured | 零（Reconfig） | AllReduce | 否 | 高 |
| **PRUNEX** | **Structured** | **零（隐式）** | **Hierarchical AllReduce** | **是** | **高** |

> ✅ **优势总结**：PRUNEX 是首个将**结构化剪枝**与**集群层次拓扑**协同设计的系统，在不牺牲推理效率的前提下显著降低通信体积，并通过稠密操作保持高性能。

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **CIFAR-10** 数据集进行图像分类任务。

### 模型架构
- **ResNet-18**, **ResNet-152**, **WideResNet-50-2**
- 参数量从 ~11M 到 ~69M 不等，覆盖不同规模模型。

### 实验平台
- 在芬兰 CSC 的 **Puhti 超算**上进行实验；
- 使用 **16 个节点，共 64 块 NVIDIA V100 GPU**（每节点 4 块）；
- 节点内通过 **NVLink** 互联（高带宽），节点间通过 **HDR InfiniBand**（100 Gbps）连接。

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **PyTorch DDP (Dense Baseline)** | 标准的稠密同步 SGD，使用 Ring-AllReduce |
| **Top-K SGD (Gradient Compression)** | 每层保留 top 1% 梯度，代表梯度压缩方法 |
| **PruneX (AR)** | PRUNEX 的消融版本，使用扁平化 AllReduce，无层级结构 |

### 评估指标
- **End-to-Accuracy Time**：达到目标准确率所需时间；
- **Inter-node Communication Volume**：跨节点总通信量（GB）；
- **Per-Iteration Communication Latency**：单次迭代通信延迟；
- **Strong Scaling Speedup & Efficiency**：随 GPU 数增加的扩展性；
- **Sparsity-Accuracy Trade-off**：剪枝比例与最终精度的关系；
- **Primal/Dual Residuals**：验证 ADMM 收敛性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）端到端训练效率
- **PRUNEX 在约 8 分钟内达到 70% 准确率**，而 DDP 在 14 分钟内仅达到 ~65%，Top-K 更慢且收敛不稳定（~50%）；
- 图 5(b) 显示，PRUNEX 实现更高的“**accuracy per byte**”，即单位通信量带来的精度提升更高。

#### （2）通信体积减少
- **跨节点通信量平均减少 ~60%**：
  - ResNet-18：2.53 GB → 1.01 GB
  - WideResNet-50-2：14.88 GB → 5.97 GB
  - ResNet-152：13.00 GB → 5.21 GB
- 压缩效果随着 H-SADMM 的推进迅速稳定（见图 6）。

#### （3）通信延迟降低
- 单次迭代通信延迟从 DDP 的 **0.50s 降至 PRUNEX 的 0.10s**，提速 **5×**；
- 消融实验表明，若去除层级结构（PruneX-AR），延迟改善消失，证明**层级设计是压缩生效的前提**。

#### （4）强扩展性（Strong Scaling）
- 在 64 GPU 上：
  - **PRUNEX 实现 6.75× 加速比**（并行效率 84.4%）
  - DDP 仅为 5.81×（72.7%）
  - Top-K 为 3.71×
- 表明 PRUNEX 在更大规模下更具可扩展性。

#### （5）消融实验结果
- **PruneX (AR)** 性能接近 DDP，说明**没有层级结构则无法发挥剪枝的通信优势**；
- 层级结构 + 结构化剪枝 + 缓冲区压缩三者结合才是性能提升的关键。

---

## 4. 关键结论和发现

### 主要发现
1. **结构化剪枝必须与通信拓扑协同设计** 才能在训练阶段真正节省通信成本；
2. **在跨节点通信前执行结构化剪枝和缓冲区压缩** 可以显著减少通信量，且兼容现有稠密通信库（如 NCCL）；
3. **Leader-Follower 架构有效隔离了高延迟的跨节点通信**，并通过 mask 同步保证一致性；
4. H-SADMM 算法具有良好的收敛性，各层残差单调下降，支持自适应惩罚参数调节；
5. 即使在 50% 通道剪枝下，ResNet-152 仍能保持 **82.8% 的准确率**（原始为 85.96%），显示冗余性较强。

### 方法的局限性
- 当前实现基于两层拓扑（节点/集群），对更复杂的机架级拓扑支持有限（虽具备扩展潜力）；
- 剪枝策略目前集中在 CNN 的通道/滤波器维度，尚未推广至 Transformer 等注意力结构；
- 冻结掩码后的再训练阶段依赖人工设定阈值 `T_freeze`，缺乏自动化判断机制。

### 未来工作方向
- 扩展至 **Vision Transformers (ViTs)** 和其他 attention-based 模型；
- 在数千 GPU 规模的超大集群上验证系统的可扩展性；
- 探索动态 mask 冻结策略和自动收敛检测机制；
- 支持更深的硬件层次（如 rack-level aggregation）。

---

> 📌 **总结一句话**：  
> **PRUNEX 通过将结构化剪枝嵌入分布式优化算法 H-SADMM，并结合层级通信架构，首次实现了“模型稀疏性 → 通信体积缩减 → 实际训练加速”的闭环，为大规模分布式训练提供了高效、实用的新范式。**

</details>

---

### 5. [Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification](https://arxiv.org/abs/2512.14491)

**Authors**: Cheng-Han Lu, Pei-Hsuan Tsai  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.14491v1  

#### Abstract
Transformer-based multi-modal intelligent systems often suffer from high computational and energy costs due to dense self-attention, limiting their scalability under resource constraints. This paper presents SMMT, a sparse multi-modal transformer architecture designed to improve efficiency and robus...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Sparse Multi-Modal Transformer with Masking for Alzheimer's Disease Classification*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前基于 **Transformer** 的多模态智能系统在阿尔茨海默病（Alzheimer's Disease, AD）分类任务中面临的两大挑战：
- **高计算与能耗成本**：传统 Transformer 使用密集自注意力（dense self-attention），导致时间复杂度为 $O(n^2)$，难以在资源受限环境下扩展。
- **对不完整输入敏感**：临床数据常存在缺失模态（如缺少 MRI 或基因信息），而现有模型缺乏显式机制应对这一现实问题。

### 提出的新方法：SMMT
作者提出 **Sparse Multi-Modal Transformer with Masking (SMMT)**，一种从系统层面设计的高效、鲁棒的多模态融合架构，其核心创新包括：

#### （1）Cluster-based Sparse Attention
- 将每个模态内的 token 通过 **K-Means 聚类** 分组（簇数 $k = \log_2 n$）。
- 自注意力仅在同簇内计算，将计算复杂度从 $O(n^2)$ 降低至 $O(n \log n)$。
- 使用查询向量（query vectors）进行聚类，确保语义相关性驱动分组，优于固定窗口或规则模式。

#### （2）Modality-wise Masking
- 在训练过程中对各模态输出特征随机施加掩码（masking ratio $r=0.3$），模拟真实场景中的模态缺失。
- 增强模型对不完整输入的泛化能力，提升鲁棒性。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率** | 显著减少训练时间和内存占用，适合低资源部署 |
| **鲁棒性** | 对小样本和缺失模态具有更强适应能力 |
| **可持续性** | 大幅降低能源消耗与碳排放，支持绿色 AI 发展 |
| **性能保持** | 在显著压缩计算开销的同时维持甚至超越基线精度 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **ADNI-1 和 ADNI-2** 数据集，筛选出确诊为 AD（Alzheimer’s Disease）或 CN（Cognitively Normal）的个体。
- 总共收集 **12,680 张 T1-weighted 2D MRI 切片**，并与结构化临床数据匹配。
- 包含的模态：
  - **Imaging**: MRI 图像（经 MNI152 配准、归一化为 256×256）
  - **Numerical**: MMSE、CDR、FAQ、Age
  - **Categorical**: Sex、APOE genotype

> 注：排除 MCI（轻度认知障碍）样本以避免类别不平衡及任务复杂化。

### 实验设置
| 项目 | 设置 |
|------|------|
| 框架 | PyTorch |
| 硬件 | NVIDIA RTX 3060 GPU (12GB) |
| 训练轮次 | 50 epochs × 5-fold CV |
| 批大小 | 8 |
| 优化器 | Adam ($lr = 1\times10^{-3}$) |
| 损失函数 | Cross Entropy |
| 特征维度 | 512 |
| 掩码比例 | 0.3（modality-level random masking）|
| 可重复性 | 5 次不同随机种子运行取平均 |

### 评估指标

#### （1）分类性能指标
- **Accuracy**, **Precision**, **Recall (Sensitivity)**, **F1-score**
- **Specificity**, **AUC (ROC 曲线下面积)**

#### （2）计算可持续性指标（via CodeCarbon）
- **Energy Consumption (kWh)**：CPU/GPU/RAM 功耗总和
- **CO₂ Emissions (kg)**：基于台湾电网碳强度（0.502 kgCO₂/kWh）估算

### 基线方法对比
| 模型 | 类型 | 说明 |
|------|------|------|
| **3MT [8]** | 多模态 Transformer（级联交叉注意力） | 主要基准模型 |
| **ADDFformer [20]** | 单模态（MRI）Transformer | 成像专用 SOTA |
| **FusionNet [21]** | 通用多模态融合网络（无注意力） | 中间融合代表 |
| **CNN-only [22]** | 单模态 CNN（如 VGG16） | 传统基线 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（完整数据集，100%）

| 模型 | Accuracy (%) | Sensitivity (%) | Specificity (%) | AUC |
|------|---------------|------------------|------------------|-----|
| **SMMT (Ours)** | **97.05** | **96.31** | **97.58** | **0.986** |
| 3MT [8] | 90.28 | 93.64 | 93.81 | 0.965 |
| ADDFformer [20] | 88.20 | 91.87 | 91.53 | 0.948 |
| FusionNet [21] | 94.28 | 91.01 | 93.24 | 0.956 |
| CNN-only [22] | 80.24 | 78.45 | 80.22 | 0.852 |

✅ SMMT 在所有指标上均达到 **SOTA 表现**，尤其在 AUC 上领先明显。

### 小样本条件下的表现（20% 数据）

| 模型 | Accuracy (%) |
|------|---------------|
| **SMMT (Ours)** | **84.96** |
| 3MT [8] | 78.92 |
| ADDFformer [20] | 71.92 |
| FusionNet [21] | 75.15 |
| CNN-only [22] | 68.53 |

➡️ SMMT 在极低数据条件下仍保持高准确率，展现出卓越的 **data efficiency** 和 **generalization 能力**。

### 训练效率与能耗对比

#### 训练时间（vs. 数据规模）
- SMMT 是所有 Transformer 类模型中 **训练速度最快** 的，在大数据集下扩展性更好。
- 在 100% 数据时，SMMT 训练时间为 **112 分钟**，而 3MT 为 133 分钟。

#### 能源消耗（总计 250 epochs）

| 组件 | 3MT (Baseline) | SMMT (Ours) | 减少幅度 |
|------|----------------|-------------|----------|
| CPU | 0.108489 kWh | 0.071179 kWh | 34.4% |
| GPU | 0.283977 kWh | 0.159642 kWh | 43.8% |
| RAM | 0.051035 kWh | 0.033485 kWh | 34.4% |
| **Total** | **0.443501 kWh** | **0.264306 kWh** | **40.4%** |

#### 碳排放（CO₂）
- 3MT：约 **0.2226 kgCO₂**
- SMMT：仅 **0.1327 kgCO₂** → **减少 40.3%**

💡 节省的碳排放相当于：
- 充电智能手机约 **18 次**
- 点亮一个 60W 灯泡超过 **30 小时**

### 消融实验结果（Ablation Study）

| 配置 | 100% 数据 Accuracy / Time (min) | 20% 数据 Accuracy / Time (min) |
|------|-------------------------------|-------------------------------|
| 3MT (Baseline) | 90.28 / 133 | 78.92 / 41 |
| w/o Sparse Attention | 91.35 / 147 | 84.85 / 51 |
| w/o Masking | 95.42 / 108 | 80.85 / 37 |
| **SMMT (Full)** | **97.05 / 112** | **84.96 / 45** |

🔍 结论：
- **Sparse Attention** 显著降低训练时间（↓~25%），并轻微提升泛化能力（可能起正则化作用）。
- **Masking** 对小样本性能至关重要（20% 数据下 ↓4.11% 若移除），是鲁棒性的关键。
- 两者结合实现最佳 **效率-性能权衡**。

#### 掩码比例（Masking Ratio）影响研究
- 在 40% 数据设定下测试不同 $r$ 值。
- 最优 $r = 0.3$，过高（>0.6）导致性能急剧下降，接近多数类基线（60%）。
- 支持选择适度掩码以平衡正则化与信息保留。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SMMT 在 AD 分类任务中实现了精度与效率的双重突破**：
   - 达到 **97.05% 准确率**（100% 数据）、**84.96%**（20% 数据）
   - 同时将 **能量消耗降低 40.4%**，显著优于 3MT 等基线。
2. ✅ **Sparse Attention 不仅提效，还隐式增强泛化能力**：
   - 限制注意力范围可抑制噪声连接，类似 Dropout 正则化效果。
3. ✅ **Modality-wise Masking 极大提升了模型对缺失数据的鲁棒性**：
   - 特别适用于真实医疗环境中常见的不完整输入情况。
4. ✅ **SMMT 更稳定、更环保**：
   - 能耗曲线平稳，硬件利用率更均衡，有利于长期部署。

### 方法的局限性
- 当前仅用于 **二分类任务（AD vs. CN）**，未涵盖 MCI（轻度认知障碍）阶段。
- 聚类步骤（K-Means）虽一次性且复用，但仍引入额外计算开销（$O(nkdi)$），需 GPU 加速支持。
- 所有实验基于单一硬件平台（RTX 3060），跨设备可移植性有待验证。

### 未来工作方向
1. **扩展至多阶段预测**：纳入 MCI 样本，构建 AD 进展的纵向预测模型。
2. **动态模态选择机制**：在推理时自适应处理可变模态输入。
3. **更大规模验证**：在更多元化的临床数据集上测试泛化能力。
4. **边缘部署探索**：进一步压缩模型，推动 SMMT 在移动或嵌入式医疗设备上的应用。

---

> 📌 **总结一句话**：  
> SMMT 通过 **cluster-based sparse attention** 和 **modality-wise masking**，成功构建了一个 **高效、鲁棒、节能** 的多模态 Transformer 架构，在 AD 分类任务中实现了性能与可持续性的双赢，为资源受限环境下的智能医疗系统提供了可行路径。

</details>

---

### 6. [VersatileFFN: Achieving Parameter Efficiency in LLMs via Adaptive Wide-and-Deep Reuse](https://arxiv.org/abs/2512.14531)

**Authors**: Ying Nie, Kai Han, Hongguang Li, Hang Zhou, Tianyu Guo, Enhua Wu, Xinghao Chen, Yunhe Wang  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14531v1  

#### Abstract
The rapid scaling of Large Language Models (LLMs) has achieved remarkable performance, but it also leads to prohibitive memory costs. Existing parameter-efficient approaches such as pruning and quantization mainly compress pretrained models without enhancing architectural capacity, thereby hitting t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*VersatileFFN: Achieving Parameter Efficiency in LLMs via Adaptive Wide-and-Deep Reuse*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）虽然性能强大，但其庞大的参数量导致**内存开销过高**，严重限制了实际部署。现有的参数高效方法（如剪枝、量化、LoRA等）主要对预训练模型进行压缩，**并未增强模型的表征能力**，因此受限于原始架构的容量上限。

此外，稀疏专家混合（MoE）虽能提升容量，但引入大量额外参数；而递归计算方法（如ALBERT、Universal Transformers）虽节省参数，却缺乏宽度维度上的灵活性。

### 🚀 提出的新方法：VersatileFFN
本文提出 **VersatileFFN** ——一种全新的、参数高效的 FFN 架构，通过在**宽度（width）和深度（depth）两个维度上自适应地复用同一组参数**，实现更强的表达能力，同时保持极低的内存占用。

#### 核心机制：
- **Width-Versatile Path（宽度多功能路径）**  
  将一个共享的 FFN 分解为多个“虚拟子专家”（virtual sub-experts），通过结构化切片（structured slicing）从同一个 `W_proj` 和 `W_out` 中提取非重叠的隐藏子空间，形成多个轻量子网络。  
  → 实现类似 MoE 的稀疏路由效果，但**不增加参数数量**。

- **Depth-Versatile Path（深度多功能路径）**  
  对复杂 token 动态递归应用同一个 FFN 多次，模拟更深的处理过程。  
  → 使用 Gumbel-Softmax 控制器预测每个 token 所需的迭代次数，实现可微分训练。

- **Difficulty-Aware Gating（难度感知门控）**  
  利用深度路径预测的期望循环次数作为 token 难度代理，动态融合两条路径输出：
  $$
  Y = \lambda \cdot Y_{\text{width}} + (1 - \lambda) \cdot Y_{\text{depth}}, \quad \lambda = \frac{L_{\max} - \mathbb{E}[L]}{L_{\max}}
  $$
  - “简单”token 走宽度路径（快速响应）
  - “困难”token 走深度路径（深入推理）

> 💡 受启发于人类认知的双系统理论（Dual-Process Theory）：System 1 快速直觉决策，System 2 缓慢逻辑推理。

### 🔍 相比现有方法的优势
| 方法 | 参数增长 | 内存效率 | 表达能力增强 | 自适应计算 |
|------|----------|-----------|----------------|--------------|
| Pruning/Quantization | ↓ | ✅ | ❌（仅压缩） | ❌ |
| MoE | ↑↑↑ | ❌（多专家） | ✅ | ⭕（静态路由） |
| k-Loop Recurrence | ↔️ | ✅ | ✅（仅深度） | ❌（固定步数） |
| **VersatileFFN** | **↔️（几乎无增）** | ✅✅ | ✅✅（宽+深） | ✅✅（动态分配） |

> ✅ **核心优势**：在**固定参数预算下**，通过智能计算调度提升性能，真正实现“compute-heavy, memory-light”。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **预训练数据**：FineWeb-Edu（40B 和 70B tokens 子集）
- **评估基准**（Zero-Shot）：
  - PIQA, HellaSwag, OBQA, SciQ
  - ARC-easy, ARC-challenge, CommonsenseQA, Winogrande
  - 综合平均准确率（Avg. Accuracy）为主要指标

### ⚙️ 实验设置
- **模型规模**：
  - 354M 参数（hidden dim=1024）
  - 720M 参数（hidden dim=1536）
- **架构基础**：基于 OLMo2，保留 Self-Attention，替换标准 FFN 为 VersatileFFN
- **训练方式**：继续预训练（continued pre-training）1 epoch
- **序列长度**：4096
- **优化器**：AdamW，cosine decay 学习率调度
- **最大循环次数** $L_{\max}=4$（经消融确定最优）

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **BASE** | 原始 Dense 模型 |
| **MoE** | 添加 8 个小专家，Top-2 路由 |
| **k-Loop** | FFN 重复执行 k 次（k=2,4,6） |

---

## 3. 主要实验结果和性能指标

### 📊 性能对比（Zero-Shot Average Accuracy）

| Method | 354M Avg | 720M Avg |
|--------|----------|----------|
| BASE | 47.98% | 53.83% |
| MoE | 51.48% | 55.87% |
| 4-LOOP | 51.98% | 56.33% |
| 6-LOOP | 51.94% | 56.55% |
| **VERSATILEFFN** | **52.33%** | **57.03%** |

> ✅ **VersatileFFN 在两种尺度上均达到最高精度**

### 💰 效率分析（FLOPs 与参数）

| Method | Params (354M) | FFN FLOPs (354M) |
|--------|---------------|------------------|
| BASE | 354.71M | 377.49M |
| MoE | 543.59M (+53%) | 471.86M |
| 4-LOOP | 354.71M | 1509.96M (**×4.0**) |
| 6-LOOP | 354.71M | 2264.96M (**×6.0**) |
| **VERSATILEFFN** | **354.90M** (**+0.05%**) | **1236.08M** (**×3.3**) |

| Method | Params (720M) | FFN FLOPs (720M) |
|--------|---------------|------------------|
| BASE | 720.81M | 849.35M |
| MoE | 1145.69M (+58%) | 1061.69M |
| 6-LOOP | 720.81M | 5096.10M (**×6.0**) |
| **VERSATILEFFN** | **721.09M** (**+0.04%**) | **2586.38M** (**×3.0**) |

> ✅ **参数几乎不变**，FLOPs 显著低于高 loop 数方法，且性能更高。

### 🔍 消融实验结果（Ablation Studies）

| Configuration | Loss | Accuracy |
|---------------|------|----------|
| BASE | 2.779 | 47.98% |
| + Width-Versatile | 2.633 | 50.98% |
| + Depth-Versatile | 2.625 | 51.86% |
| + Gating（完整版） | **2.617** | **52.33%** |

> ✅ 两条路径互补，联合使用效果最佳。

#### 不同配置影响：
- **Loop 数选择**：4 loops 在准确率上达到峰值，6 loops 出现过拟合迹象
- **Expert 配置**：MoE-8-2（8个专家选2个）表现最好

#### 无需继续预训练的结果：
| Method | Accuracy |
|-------|----------|
| BASE | 47.98% |
| 4-LOOP | 50.81% |
| **VERSATILEFFN** | **51.14%** |

> 即使不继续训练，VersatileFFN 也能显著优于 baseline 和 4-Loop。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **参数复用可以有效替代参数扩张**：通过在宽度和深度上灵活复用同一组参数，可在不增加内存负担的前提下显著提升模型性能。
2. **双路径协同优于单一路径**：宽度路径适合快速处理简单 token，深度路径擅长复杂推理，二者结合实现更优资源分配。
3. **动态门控机制有效建模 token 难度**：利用预测的迭代次数作为难度信号，能自动调节计算策略，提升整体效率。
4. **VersatileFFN 是真正的 parameter-efficient 架构**：相比 MoE 的“参数膨胀”，它实现了“计算换性能”的理想范式。

### ⚠️ 局限性
- 引入了额外的 **router 和 loop predictor**，带来轻微参数和计算开销（尽管可忽略）。
- 推理延迟可能受动态循环影响，需依赖 early-exit 和并行优化来缓解。
- 当前设计仍局限于 FFN 模块，未扩展到 Attention 或其他组件。

### 🔮 未来工作方向
- 将 Versatile 设计推广至 Multi-Head Attention 或整个 Transformer Block。
- 探索更高效的控制器结构（如轻量级 MLP 或 attention-based predictor）。
- 在更多下游任务（如长文本生成、数学推理）中验证其泛化能力。
- 结合硬件优化（如动态 kernel 调度）进一步降低推理延迟。

---

## ✅ 总结
**VersatileFFN** 提出了一种新颖的“宽深复用”思想，打破了传统 LLM 扩展依赖参数增长的范式。它通过**参数共享 + 自适应计算 + 难度感知门控**，实现了高性能与高效率的统一，在多项 benchmark 上超越 MoE 和固定循环方法，是迈向“memory-light, compute-smart”大模型的重要一步。

</details>

---

### 7. [TiME: Tiny Monolingual Encoders for Efficient NLP Pipelines](https://arxiv.org/abs/2512.14645)

**Authors**: David Schulmeister, Valentin Hartmann, Lars Klein, Robert West  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.14645v1  

#### Abstract
Today, a lot of research on language models is focused on large, general-purpose models. However, many NLP pipelines only require models with a well-defined, small set of capabilities. While large models are capable of performing the tasks of those smaller models, they are simply not fast enough to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TiME: Tiny Monolingual Encoders for Efficient NLP Pipelines

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流语言模型（如 BERT、XLM-R）虽然在多种 NLP 任务上表现优异，但其庞大的参数量导致**推理延迟高、能耗大**，难以部署于实时系统或资源受限设备（如移动终端）。此外，多语言模型（multilingual models）虽支持多种语言，但在单个语言上的性能可能不如专门优化的**monolingual models**。

本文旨在解决以下挑战：
- 如何为不同语言构建**高效且高性能**的轻量级单语编码器。
- 如何在保持低延迟和低能耗的同时，不显著牺牲模型精度。
- 如何支持从**多语言教师模型**向**单语学生模型**的知识迁移，并跨越架构差异（如相对位置编码 → 绝对位置编码）。

---

### 提出的新方法与创新点
作者提出了 **TiME (Tiny Monolingual Encoders)** —— 一系列通过知识蒸馏（Knowledge Distillation, KD）训练的小型单语 Transformer 编码器模型。

#### 核心方法：
- **基于 MiniLMv2 的蒸馏框架**：采用 multi-head self-attention relation distillation，提取教师模型中 Query-Q、Key-K 和 Value-V 向量之间的关系进行知识传递。
- **跨架构蒸馏能力**：
  - 成功将使用 **relative position embeddings** 的 LTG-BERT 架构教师模型（来自 HPLT 项目）的知识迁移到使用标准 **absolute position embeddings** 的 BERT 架构学生模型。
  - 验证了从 **multilingual teacher (XLM-R-Large)** 蒸馏出高质量 **monolingual student** 的可行性。
- **灵活的学生架构设计**：
  - 定义三种尺寸：`Medium (6L, 768H)`、`Small (6L, 384H)`、`Extra-Small (4L, 384H)`。
  - 所有学生共享教师的 tokenizer，确保兼容性。

---

### 相比现有方法的优势
| 方面 | TiME 的优势 |
|------|------------|
| **效率** | 推理速度提升最高达 **25×**，能量消耗降低最多 **30×**，远超 XLM-R-Large 和 XLM-R-Base。 |
| **性能保留** | TiME-m 在平均 NLP 分数上保留了 XLM-R-Large 的 **98.4%** 性能，同时参数减少 58%。 |
| **通用性** | 支持 **16 种语言**，涵盖高低资源语言（如 Irish 为低资源），验证了方法的广泛适用性。 |
| **实用性** | 模型结构兼容主流工具链（如 spaCy），便于工业落地。 |
| **前沿性** | 在性能-效率权衡曲线上处于 **Pareto 前沿**，优于多个强基线。 |

---

## 2. 核心实验方法和设置

### 使用的数据集

| 类别 | 数据集 | 用途 |
|------|--------|------|
| **预训练蒸馏数据** | `CulturaX` [Nguyen et al., 2023] | 用于知识蒸馏的语言特定子集 |
| **下游任务评估** | `Universal Dependencies (UD)` treebanks | POS tagging、lemmatization、dependency parsing |
| | `WikiAnn` dataset | Named Entity Recognition (NER) |
| | `MLQA` benchmark | Question Answering（仅 en/de） |
| **检查点选择** | `FLORES-200`（仅 Irish）<br>`WMT24++`（其他语言） | 外部验证集用于选择最佳蒸馏检查点 |

---

### 实验设置

- **教师模型**：
  - Multilingual: `XLM-R-Large` (560M params)
  - Monolingual: `HPLT` 系列模型（基于 LTG-BERT 架构）
- **学生模型**：TiME-*lang*-m/s/xs，共训练 16 种语言
- **优化器**：AdamW，学习率 5.5e-4，warmup 4000 步
- **硬件**：NVIDIA A100/H100/H200，BF16 混合精度，有效 batch size = 256
- **训练步数**：200,000 steps，每 10k 步保存一次 checkpoint

---

### 评估指标

| 指标类型 | 具体指标 |
|---------|--------|
| **准确性** | - NER: balanced F1<br>- POS: accuracy (AllTags)<br>- Lemmatization: accuracy (Lemma)<br>- Parsing: labeled attachment score (LAS)<br>- QA: F1 & EM (MLQA) |
| **效率** | - Latency (ms, batch size=1)<br>- Throughput (sentences/sec, at optimal batch size)<br>- Energy consumption (Joules per sample) |
| **综合评分** | 平均 NLP Score（四个任务 macro-average） |

---

### 基线方法对比
- `XLM-R-Large`, `XLM-R-Base`
- `mMiniLM-L6-H384`（MiniLM 蒸馏自 XLM-R）
- `HPLT-original` 模型
- `spaCy` 的 transformer pipelines（en_core_web_trf 等）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1 & Table 2）

| 模型 | 参数量(M) | 平均 NLP Score | Latency (× vs XL) | Throughput (× vs XL) | Energy (J/sample, × vs XL) |
|------|-----------|----------------|--------------------|------------------------|----------------------------|
| XLM-R-Large | 560 | 89.15 | 1.0× | 1.0× | 1.0× |
| XLM-R-Base | 278 | 87.86 | 1.9× | 3.2× | 3.3× |
| TiME-m | 236 | **87.71** | **3.9×** | **5.4×** | **6.6×** |
| TiME-s | 107 | 85.16 | 3.9× | 15.9× | 18.7× |
| TiME-xs | 103 | 83.92 | **5.8×** | **25.2×** | **30.2×** |

> 注：Latency 提升倍数越高表示越快；Throughput 越高越好；Energy 改善倍数越大越好。

---

### 与基线方法的关键对比结果

- **性能方面**：
  - TiME-m 在所有语言上均优于 `mMiniLM-L6-H384`。
  - TiME-m 性能接近 `XLM-R-Base`，但体积更小（236M vs 278M），实际推理更快。
  - 在低资源语言 Irish 上，TiME-m 恢复了超过 **99%** 教师性能。

- **效率方面**（Figure 1）：
  - TiME 模型位于“效率前沿”（efficiency frontier），即在相同性能下延迟更低，或在相同延迟下性能更高。
  - 小型模型（TiME-xs）吞吐量高达 **9321.1 句/秒**，是 XLM-R-Large 的 **25×**。

- **与 spaCy 对比**（Table 4）：
  - 在 English 上，`TiME-en-xs` 延迟仅为 **1.9ms**，而 `en_core_web_trf` 为 **5.1ms**（下降 63%）。
  - 吞吐量提升明显：`TiME-en-xs` 达到 **6361.6 s/s**，相比 spaCy 的 **1330.0 s/s**，提速 **4.78×**。

---

### 消融实验与额外分析（如有）

尽管未明确列出消融表，但文中隐含多个关键验证：

1. **跨架构蒸馏有效性**：
   - 成功从 LTG-BERT（relative PE + GeGLU）蒸馏到标准 BERT（absolute PE + GELU），证明 MiniLMv2 的鲁棒性。

2. **多语言教师 → 单语学生**：
   - 使用 XLM-R-Large 蒸馏出的 TiME 模型，在各语言上表现优于或媲美专为该语言设计的 monolingual teacher 蒸馏结果。

3. **批大小对能效的影响**（Figure 5）：
   - 存在一个最优 batch size 可最小化每样本能耗，TiME-xs 在此指标上表现最佳。

4. **低资源语言适应性**：
   - 在 Irish 上仍取得良好性能，说明该蒸馏流程对数据量较小的语言也具潜力。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **小型单语模型可以实现极高的效率-性能平衡**：TiME 系列模型在显著压缩模型规模的同时，几乎无损地保留了大型教师模型的能力。
2. ✅ **知识蒸馏是构建高效 NLP 流水线的有效路径**：特别是 MiniLMv2 的 attention relation distillation 方法，适用于复杂架构迁移。
3. ✅ **多语言教师可用于生成高性能单语学生**：打破了“必须用单语教师”的限制，提升了低资源语言建模的可能性。
4. ✅ **TiME 模型在真实场景中具备部署优势**：无论是低延迟响应还是大规模批量处理，都展现出巨大潜力。

---

### 方法的局限性
1. **未探索更优架构搜索**：学生模型尺寸固定（xs/s/m），缺乏系统性的神经架构搜索（NAS）来寻找 Pareto 最优解。
2. **低资源语言压缩敏感度高**：在最小模型（TiME-xs）上，Irish 和 Hungarian 的性能下降较明显，表明某些语言特性在压缩过程中易丢失。
3. **缺乏对极端低资源语言的测试**：未验证当训练数据极少时（如 <1M tokens），蒸馏是否依然稳定。
4. **超参数未充分调优**：如 relation head 数量（Ar）直接沿用 MiniLMv2 设置，未针对每种语言做 ablation。

---

### 未来工作方向
- 开展 **architecture-aware 蒸馏**，结合 NAS 自动搜索最优学生结构。
- 探索 **更小的数据需求边界**，研究如何在极低资源条件下进行有效蒸馏。
- 扩展至更多任务类型（如文本生成、摘要等）。
- 进一步优化 **energy-latency trade-off**，特别是在边缘设备上的部署策略。
- 构建统一的 **multi-lingual tiny encoder**，兼顾效率与跨语言泛化能力。

---

> 🔗 **模型与代码公开地址**：
> - Hugging Face: [https://huggingface.co/collections/dschulmeist/time](https://huggingface.co/collections/dschulmeist/time)
> - GitHub: [https://github.com/epfl-dlab/TiME](https://github.com/epfl-dlab/TiME)

</details>

---

### 8. [Variational Physics-Informed Ansatz for Reconstructing Hidden Interaction Networks from Steady States](https://arxiv.org/abs/2512.13708)

**Authors**: Kaiming Luo  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.13708v1  

#### Abstract
The interaction structure of a complex dynamical system governs its collective behavior, yet existing reconstruction methods struggle with nonlinear, heterogeneous, and higher-order couplings, especially when only steady states are observable. We propose a Variational Physics-Informed Ansatz (VPIA) ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Variational Physics-Informed Ansatz for Reconstructing Hidden Interaction Networks from Steady States*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**从稳态（steady states）数据中重建复杂动力系统隐藏的交互网络结构**这一长期挑战。传统方法通常依赖于时间序列轨迹、导数估计或强先验假设，在以下场景中表现不佳：
- 只能观测到稳态而非动态过程；
- 存在非线性、异质性和高阶耦合（higher-order couplings）；
- 数据受噪声干扰严重；
- 网络规模大且密集。

这些问题在生物神经网络、基因调控网络和社会系统等现实场景中普遍存在。

---

### 提出的新方法：VPIA（Variational Physics-Informed Ansatz）

作者提出了一种名为 **Variational Physics-Informed Ansatz (VPIA)** 的新框架，其核心思想是：
- 将未知的交互算子（interaction operator）建模为一个可训练的**变分参数化对象**（variational representation）；
- 利用物理驱动的损失函数——即**稳态残差最小化**（steady-state residual），直接将物理约束嵌入优化过程；
- 不需要时间轨迹、导数估计或监督标签。

#### 主要创新点：
1. **首次实现仅基于稳态数据的通用网络重构**  
   VPIA 能够从多个异构稳态观测中推断出**有向、加权、稀疏或密集、甚至高阶（如 simplex-level）耦合结构**，无需任何时间演化信息。

2. **物理引导 + 可微分表示 = 高鲁棒性与可解释性**  
   方法通过将动力系统的稳态方程 $ \mathbf{F}(\mathbf{x}^*, \mathbf{p}, \mathcal{A}) = 0 $ 显式作为优化目标，确保了解的物理一致性，提升了对噪声和模型不确定性的容忍度。

3. **可扩展设计：残差采样 + 自然梯度优化**
   - **残差采样（Residual Sampling）**：每轮迭代只随机采样部分节点残差，使计算复杂度由 $ O(MN) $ 降至接近 $ O(N) $，适用于大规模网络。
   - **自然梯度更新（Natural Gradient）**：利用 Fisher 信息矩阵的对角近似进行曲率感知优化，稳定高维参数空间中的收敛过程。

4. **统一处理任意阶交互结构**
   支持从 1-simplex（边）到 4-simplex（四体耦合）的高阶相互作用，并以张量形式统一参数化，无需预设交互阶数。

---

### 相比现有方法的优势
| 维度 | 传统方法局限 | VPIA优势 |
|------|---------------|---------|
| 数据需求 | 需要完整时间序列或导数估计 | 仅需稳态快照 |
| 噪声鲁棒性 | 对测量/动态噪声敏感 | 在三种噪声下均保持高精度 |
| 结构灵活性 | 多限于成对、稀疏、线性连接 | 支持有向、加权、高阶、密集结构 |
| 可扩展性 | 多数算法复杂度 ≥ $ O(N^3) $ | 近线性扩展，支持 $ N \sim 10^3 $ 节点 |
| 物理一致性 | 多基于统计相关性（如 TE, Correlation） | 内嵌物理方程，避免虚假关联 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### （1）人工合成网络
- **ER 随机图**（Erdős–Rényi, $ G(N,p) $ with $ p=0.5 $）
- 包含不同拓扑：scale-free、small-world、directed、weighted
- 高阶网络：所有可能的 $ k $-simplex（$ k=1,2,3,4 $）以概率 0.5 独立生成

#### （2）真实世界网络（来自公开数据库 [34–36]）
| 网络名称 | 类型 | 节点数 $ N $ | 边数 $ E $ | 密度 |
|--------|-------|-------------|-----------|-------|
| Florentine Families | Undir | 16 | 20 | 0.1667 |
| Zachary Karate Club | Undir | 34 | 78 | 0.1390 |
| Wild Bird Social | Undir | 202 | 11733 | 0.5861 |
| C. elegans Neural | Dir | 297 | 2359 | 0.0259 |
| Power Grid | Undir | 494 | 596 | 0.0049 |
| Retweet Weibo | Dir | 596 | 1415 | 0.0080 |
| Human Brain Neural | Undir | 989 | 35730 | 0.036 |

这些网络覆盖了从小型社会网络到千节点级脑功能网络的真实尺度。

---

### 实验设置

- **动力学模型**：主要采用 Kuramoto 模型，但也测试了多种其他系统验证泛化能力：
  - Phase oscillators: Kuramoto, Kuramoto-Sakaguchi
  - Limit-cycle: Stuart-Landau, Van der Pol, FitzHugh-Nagumo
  - Chaotic systems: Rössler, Lorenz, Hindmarsh-Rose
- **稳态生成**：通过数值积分（Runge-Kutta 或自适应步长 ODE 求解器）获得 $ M = O(N) $ 至 $ O(N\log N) $ 个稳态样本。
- **去退化过滤**：剔除同步或低离散度的稳态，使用方差或平均成对距离作为判据 $ D(x^*) > \epsilon_{sync} $。

---

### 评估指标

- **AUC（Area Under ROC Curve）**：为主要评价指标，具有阈值无关性和对稀疏网络的稳健性。
- 其他补充指标：Accuracy, Precision, Recall, F1-score（用于二分类任务）。
- 成功重建定义：AUC = 1 且所有分类指标均为 1。

---

### 基线方法对比（见 Supplementary Sec. S4）

比较的经典方法包括：
- Transfer Entropy (TE)
- Network Deconvolution (ND)
- Partial Phase Synchronization (PPS)
- Modular Response Analysis (MRA)
- Global Silencing (GS)
- Correlation-based inference

> ⚠️ 注意：仅选择那些可在相同输入条件下运行的方法（即使用稳态或轨迹数据），排除需要已知网络结构或网络集合的方法。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 场景 | 性能表现 |
|------|----------|
| **一般人工网络**（Fig. 2B） | AUC > 0.95，多数接近 1；即使 $ N=50 $，仅需约 20–30 个稳态即可达到完美重建 |
| **所需稳态数量 vs 网络大小**（Fig. 2C） | 所需稳态数随 $ N $ 近似线性增长，表明信息效率高 |
| **真实网络重建**（Fig. 4） | 在 $ N=989 $ 的人脑网络上，仅需约 150 个稳态即可达到 AUC ≈ 1 |
| **高阶交互重建**（Fig. 5B–C） | 成功恢复 4-simplex 张量结构，AUC 随样本增加快速上升至 1 |
| **噪声鲁棒性**（Fig. 3 & 5E） | 即使在噪声水平达 0.5 时，只要提供更多稳态，仍可恢复高精度（AUC > 0.95） |

---

### 与基线方法的对比结果（Supplementary Sec. S4）

- 所有基准方法在以下情况性能显著下降：
  - 网络密度增加（>0.1）
  - 存在方向性或权重异质性
  - 动力学非线性较强
- VPIA 在所有测试场景中均取得 **一致的高 AUC 和近乎完美的支持恢复（support recovery）**
- 特别是在稠密、有向、非线性系统中，VPIA 明显优于基于 Jacobian 回归、协方差反演或间接关联过滤的方法

---

### 消融实验与关键机制验证（隐含于正文及附录）

虽然未明确列出“消融实验”章节，但文中多处分析体现了关键组件的作用：

| 组件 | 效果验证 |
|------|---------|
| **残差采样**（Sec. III.C） | 使得 $ O(N) $ 计算成本成为可能，支持 $ N \sim 10^3 $ 规模；即使采样比例低至 5%，仍能稳定收敛 |
| **自然梯度优化**（Sec. V.D） | 显著提升高维参数空间下的优化稳定性，防止因各参数尺度差异导致的震荡或停滞 |
| **多稳态多样性要求**（Sec. II.B） | 若所有稳态趋同（如同步态），则无法识别结构；必须保证足够分散的稳态分布 |
| **物理约束驱动**（Sec. IV） | 正因为每个稳态提供全局约束，而噪声为局部扰动，因此可通过大量样本平均掉噪声影响 |

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **稳态蕴含丰富结构信息**：尽管没有时间演化路径，多个异构稳态联合起来足以唯一确定复杂的交互结构，前提是它们足够多样化。
2. ✅ **物理一致性是鲁棒性的来源**：通过最小化物理导出的稳态残差，VPIA 能有效抑制噪声并避免虚假连接。
3. ✅ **VPIA 具备强泛化能力**：在相位振子、极限环、激发系统乃至混沌系统上表现一致优异，说明其依赖的是稳态本身的结构性而非具体动力学形式。
4. ✅ **可扩展至现实大规模网络**：结合残差采样与自然梯度，成功应用于近千节点的真实网络（如人类大脑、微博转发网络）。
5. ✅ **支持高阶交互重建**：首次实现了从稳态数据中准确恢复 2–4-simplex 结构，突破了传统图模型的限制。

---

### 方法的局限性

1. ❗ **依赖多样化的稳态输入**：若系统只能产生高度相似或退化的稳态（如同步状态），则无法重建结构。这在某些强耦合或对称系统中可能发生。
2. ❗ **计算资源仍有一定门槛**：虽然已大幅优化，但在极端高阶（如 $ d>4 $）或超大规模（$ N>10^4 $）网络中，参数数量呈组合爆炸，仍具挑战。
3. ❗ **需要一定程度的动力学知识**：虽然不要求知道精确参数，但仍需知道大致的动力学形式 $ \mathbf{F}(\cdot) $ 来构建残差项。
4. ❗ **初始参数敏感性**：尽管使用 sigmoid 映射缓解，但在极端病态条件下可能存在局部最优陷阱。

---

### 未来工作方向

1. 🔮 推广至**准稳态或亚稳态系统**（quasi-steady/metastable states），放宽严格稳态假设；
2. 🌐 开发**分布式优化策略**，以应对更大规模网络（$ N \gg 10^3 $）；
3. 🧠 探索更高效的**变分参数化方式**（如低秩分解、图神经网络先验）来压缩高阶张量空间；
4. 📊 应用于实际领域：如从 fMRI/fNIRS 快照重建脑功能网络，或从单细胞测序数据推断基因调控网络；
5. 🔍 结合贝叶斯框架，提供不确定性量化（uncertainty quantification）与置信区间估计。

---

## 总结

VPIA 是一种**基于物理约束、完全可微分、无需时间轨迹的网络重构框架**，它成功解决了从稳态数据中重建复杂、非线性、高阶交互网络的核心难题。其实验结果显示：
- 在多种人工与真实网络上实现了接近完美的重建（AUC → 1）；
- 对噪声、结构扰动和系统规模表现出极强鲁棒性；
- 显著优于现有主流方法，尤其在稠密、有向、非线性场景中优势明显。

该工作为生物学、神经科学、社会学等领域中“只有快照、没有录像”的系统提供了强有力的逆向工程工具，标志着**物理引导机器学习在复杂系统识别中的重要进展**。

</details>

---

### 9. [RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees](https://arxiv.org/abs/2512.14069)

**Authors**: Junjie Ma, Jinlong Li  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.14069v1  

#### Abstract
Inference with modern Large Language Models (LLMs) is expensive and slow, and speculative sampling has emerged as an effective solution to this problem, however, the number of the calls to the draft model for generating candidate tokens in speculative sampling is a preset hyperparameter, lacking fle...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RADAR: Accelerating Large Language Model Inference With RL-Based Dynamic Draft Trees

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代 Large Language Models (LLMs) 推理过程昂贵且缓慢。**Speculative Sampling** 是一种有效的加速方法，通过一个小的 draft model 快速生成候选 token，并由目标 LLM 并行验证。然而，现有方法中对 draft model 的调用次数是一个预设的超参数（如固定为 8 次），缺乏灵活性，导致在某些上下文中产生冗余计算，甚至出现大量候选 token 被完全拒绝的情况。

### 提出的新方法与创新思路
作者提出 **RADAR**（Reinforcement learning Adjusted Draft-generation Algorithm for speculative sampling），其核心创新如下：

- **动态 draft tree 构建**：不同于传统 speculative sampling 中使用静态深度的 draft tree（如 EAGLE-3 固定调用 8 次），RADAR 在每一步生成过程中**动态决定是否继续调用 draft model**，从而构建**变深度的动态 draft tree**。
  
- **将 draft tree 生成建模为 MDP**：由于 acceptance length 具有随机性，无法获得精确标签，因此不能直接进行监督学习。作者将整个 draft 过程建模为一个 **Markov Decision Process (MDP)**，利用内在奖励机制训练策略模型。

- **基于离线强化学习（Offline RL）训练预测模型**：
  - 使用 EAGLE-3 在 ShareGPT 数据集上运行，收集不同调用次数下的 **acceptance length 分布**，构建训练数据集。
  - 引入轻量级 LSTM-based **prediction model**，输入为 draft model 输出的 top-k confidence scores，输出为 `continue` 或 `stop` 的控制信号。
  - 利用该数据集进行 **offline reinforcement learning**，避免了在线交互带来的高成本和 extrapolation error。

### 相比现有方法的优势
- **更高的推理效率**：通过减少不必要的 draft model 调用，显著降低冗余计算。
- **无需修改 draft model 结构**：保持原有 draft model 不变，仅引入一个小型 prediction model 来调控生成流程。
- **适应性强**：根据不同 context 动态调整 draft tree 深度，提升资源利用率。
- **训练稳定**：采用 offline RL 避免探索开销，同时保证训练分布与实际推断一致。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据集**：`ShareGPT`（用于构建 acceptance length 分布数据集）
- **评估任务与数据集**（四类典型任务）：
  - 多轮对话：**MT-bench**
  - 数学推理：**GSM8K**
  - 指令遵循：**Alpaca**
  - 代码生成：**MBPP**

> 所有任务均未对目标 LLM 微调，权重共享。

### 实验设置
- **目标 LLMs**：
  - LLaMA-Instruct 3.1 8B (**L3 8B**)
  - Vicuna 13B (**V13B**)
  - DeepSeek-R1-Distill-LLaMA 8B (**DSL 8B**)
- **Draft model**：沿用 EAGLE-3 设计（具体未公开细节）
- **实现基础**：基于 EAGLE-3 开源代码库开发
- **关键参数**：
  - 最大 draft model 调用次数：8
  - 分支因子 $k = 10$
  - 温度：1.0
  - Batch size：1
  - Prediction model 训练使用 REINFORCE 算法，学习率 $1e^{-4}$

### 评估指标
| 指标 | 描述 |
|------|------|
| **Speedup Ratio** | 相对于 vanilla auto-regressive decoding 的实际加速比 |
| **Average Acceptance Length ($\bar{T}$)** | 每个 drafting-verification cycle 平均接受的 token 数量 |
| **Average Number of Calls to Draft Model** | 每 cycle 平均调用 draft model 的次数（不再是固定值） |

> 注意：不报告 acceptance rate，因为 RADAR 不改变 draft model 本身，故其 acceptance rate 与 EAGLE-3 相同。

### 基线方法对比
- **Baseline**：Vanilla Auto-regressive Decoding（速度比定义为 1.00x）
- **对比方法**：
  - **EAGLE-2**
  - **EAGLE-3**（当前最先进的 lossless speculative sampling 方法）

所有测试均在相同硬件环境（2×NVIDIA RTX3090）下完成，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| Model | Method | MT-bench Speedup | GSM8K Speedup | Alpaca Speedup | MBPP Speedup |
|-------|--------|------------------|---------------|----------------|-------------|
| L3 8B | Eagle-2 | 2.56x | 3.43x | 2.89x | 3.29x |
| L3 8B | Eagle-3 | 3.08x | 4.68x | 3.86x | 4.21x |
| L3 8B | **RADAR** | **3.41x** | **4.82x** | **4.04x** | **4.44x** |
| V13B | Eagle-3 | 3.74x | 4.24x | 3.50x | 4.55x |
| V13B | **RADAR** | **4.05x** | **4.36x** | **3.84x** | **4.75x** |
| DSL 8B | Eagle-3 | 3.42x | 4.39x | 3.08x | 3.71x |
| DSL 8B | **RADAR** | **3.86x** | **4.71x** | **3.17x** | **3.99x** |

> ✅ **RADAR 在所有任务和模型上均取得最高 speedup ratio**，相比 EAGLE-3 提升 **3% ~ 29%**。

### 接受长度表现（Acceptance Length）
- RADAR 的平均 acceptance length 略低于 EAGLE-3（约低 1.2%），但仍维持高位。
- 表明其并未牺牲太多 token 接受能力。

### 减少 draft model 调用次数（Table 2）

| Model | MT-bench | GSM8K | Alpaca | MBPP |
|-------|----------|-------|--------|------|
| L3 8B | 5.25 | 6.19 | 6.20 | 6.60 |
| V13B | 6.88 | 7.26 | 6.83 | 7.26 |
| DSL 8B | 6.10 | 7.20 | 5.85 | 6.47 |

> ⚠️ EAGLE-3 固定调用 8 次 → RADAR **平均减少调用 9.3% ~ 34.3%**，**整体平均减少 18.7%**。

### 性能分析图示（Figure 2）
- 图 (a) 显示 RADAR 和 EAGLE-3 的 acceptance length 分布接近，说明有效性相当。
- 图 (b) 显示 RADAR 的 draft model 调用次数集中在 5–7 次，而非固定 8 次，体现其**自适应早停机制**的有效性。

### 小结
- **更高 speedup**：得益于更少的 draft model 调用 + 维持较高的 acceptance length。
- **更低冗余计算**：动态决策避免无效扩展 draft tree。
- **通用性强**：在多种 LLM 和任务上 consistently 优于 baseline。

---

## 4. 关键结论和发现

### 主要发现
1. **固定调用次数限制了 speculative sampling 的效率**，尤其当 context 不适合深 draft 时会造成浪费。
2. **将 draft tree 生成视为 MDP 是可行且高效的建模方式**，无需真实标签即可训练决策模型。
3. **通过 offline RL 学习 acceptance length 分布作为奖励来源**，可有效指导 prediction model 学习最优停止策略。
4. **RADAR 在几乎不损失 acceptance length 的前提下，大幅减少 draft model 调用次数**，从而实现更优的整体加速效果。

### 方法的局限性
- **依赖高质量的 acceptance length 分布数据集**：需预先运行 EAGLE-3 收集数据，增加前期准备成本。
- **prediction model 增加额外延迟**：虽然 $T_{\text{eye}}$ 很小，但在极端低延迟场景可能成为瓶颈。
- **未探索更复杂的树结构优化**：目前 focus 在“何时停止”，尚未结合 node pruning 或 reordering（如 EAGLE-2/3 所做）。
- **prediction model 架构较简单**（LSTM），可能限制表达能力。

### 未来工作方向
- 探索更强大的 prediction model 架构（如 Transformer、MoE）。
- 设计更精细的 reward function，例如考虑内存占用或 energy efficiency。
- 结合 tree pruning 与 dynamic depth 控制，进一步提升效率。
- 扩展到 streaming 或 long-context 场景中的 adaptive speculative decoding。

---

> 🔗 **开源信息**：代码已发布于 GitHub：[https://github.com/minaduki-sora/RADAR](https://github.com/minaduki-sora/RADAR)

</details>

---

### 10. [FusAD: Time-Frequency Fusion with Adaptive Denoising for General Time Series Analysis](https://arxiv.org/abs/2512.14078)

**Authors**: Da Zhang, Bingyu Li, Zhiyuan Zhao, Feiping Nie, Junyu Gao, Xuelong Li  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.14078v1  

#### Abstract
Time series analysis plays a vital role in fields such as finance, healthcare, industry, and meteorology, underpinning key tasks including classification, forecasting, and anomaly detection. Although deep learning models have achieved remarkable progress in these areas in recent years, constructing ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# FusAD: Time-Frequency Fusion with Adaptive Denoising for General Time Series Analysis —— 核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的时间序列分析（TSA）模型通常遵循“单任务专用”范式（one model per task），缺乏跨任务的通用性和泛化能力。此外，现实世界的时间序列数据普遍存在**噪声干扰、多尺度动态模式、非平稳性**等问题，导致特征提取困难，影响模型鲁棒性。同时，许多深度学习模型在长序列或多变量场景下存在计算效率低、可扩展性差等挑战。

### 提出的新方法与创新思路
本文提出了 **FusAD**，一个统一的、适用于多种时间序列任务（分类、预测、异常检测）的通用分析框架。其核心创新包括：

- **自适应时频融合机制（Adaptive Time-Frequency Fusion）**  
  融合 **Fourier 变换**（捕捉全局周期性）与 **Wavelet 变换**（捕捉局部突变），实现对多尺度动态特征的高效建模。

- **自适应去噪模块（Adaptive Denoising Mechanism）**  
  在频域中引入可学习的阈值机制，自动识别并过滤高频噪声和低频趋势失真，提升模型在复杂噪声环境下的鲁棒性。

- **统一的信息融合与解码结构（Information Fusion Module, IFM）**  
  设计交互式卷积结构，增强多变量间的依赖关系建模，并结合 **Masked Pre-training** 实现跨任务的知识迁移与表示学习。

- **端到端的多任务兼容架构**  
  支持分类、预测、异常检测三大主流任务，在一个共享主干网络上完成多任务联合训练，显著提高模型复用性和部署效率。

### 相比现有方法的优势
| 维度 | FusAD优势 |
|------|----------|
| **通用性** | 支持多任务统一建模，避免为每个任务单独设计模型 |
| **鲁棒性** | 自适应去噪有效应对噪声、缺失值和分布偏移 |
| **效率** | 参数量小（仅2.3M）、FLOPs低，优于多数Transformer类模型 |
| **性能** | 在多个基准上达到 SOTA 或接近 SOTA 表现 |

---

## 2. 核心实验方法和设置

### 使用的数据集
#### 分类任务
- **UCR Archive**: 125个单变量时间序列数据集
- **UEA Archive**: 30个多变量时间序列数据集（如健康监测、动作识别等）

#### 预测任务
- **Electricity**: 电力消耗数据（321维）
- **ETT系列**（ETTh1, ETTh2, ETTm1, ETTm2）: 能源传输系统数据
- **Exchange**: 汇率波动数据
- **Traffic**: 高速公路车流量（861维）
- **Weather**: 多气象变量时间序列

#### 异常检测任务
- **SMD**（Server Machine Dataset）: 服务器监控
- **MSL / SMAP**: NASA航天器遥测数据
- **SWaT**: 水处理系统安全测试平台
- **PSM**: 工业泵传感器数据

### 实验设置与评估指标

| 任务 | 评估指标 | 输入长度 | 预测长度 |
|------|---------|----------|----------|
| **分类** | 准确率（Accuracy）、平均排名（Avg. Rank）、Top-1数量、CD图检验 | - | - |
| **预测** | MSE、MAE（越低越好） | 96~512不等 | 96, 192, 336, 720 |
| **异常检测** | Precision (P), Recall (R), F1-score | 滑动窗口 | 单点判断 |

所有数据均进行标准化处理；采用两阶段训练策略：
1. **预训练阶段**：使用 Masked Autoencoder 进行自监督学习
2. **微调阶段**：针对具体任务进行有监督训练

### 基线方法对比
涵盖三大类主流模型：
- **Transformer-based**: Informer, PatchTST, iTransformer, Crossformer, FEDformer, Autoformer
- **MLP/CNN-based**: DLinear, RLinear, ModernTCN
- **通用时间序列模型**: TS2Vec, TimesNet, TVNet, GPT4TS, TS-TCC, Data2Vec

---

## 3. 主要实验结果和性能指标

### 分类任务表现（UCR & UEA）
| 模型 | UCR 平均准确率 | UCR Top-1 数量 | UEA 平均准确率 |
|------|----------------|----------------|----------------|
| **FusAD** | **0.863** | **58** | **0.765** |
| FreRA | 0.850 | 13 | 0.754 |
| TVNet | 0.844 | 5 | 0.744 |
| PatchTST | 0.825 | 0 | 0.730 |

- FusAD在 **125个UCR数据集中取得58次第一**，远超其他模型；
- CD图显示 FusAD 的平均排名显著优于所有基线，且统计差异显著；
- 图2表明 FusAD 在更低参数量（2.3M）和计算成本（2.5G FLOPs）下实现了更高精度。

### 预测任务表现（Multivariate Forecasting）
在8个数据集、4种预测长度（共32项指标）上的综合表现如下：

| 模型 | Top-1 次数 / 总次数 | Avg MSE ↓ | Avg MAE ↓ |
|------|--------------------|-----------|-----------|
| **FusAD** | **48 / 80** | **0.250** | **0.326** |
| iTransformer | 23 / 80 | 0.254 | 0.315 |
| PatchTST | 6 / 80 | 0.267 | 0.327 |
| DLinear | 1 / 80 | 0.286 | 0.332 |

- FusAD在 **ETTh1 和 Weather 数据集上表现尤为突出**，说明其擅长处理高波动性序列；
- 在大规模数据（如 Traffic）上略有下降，但仍保持竞争力；
- 自适应去噪机制有助于抑制噪声干扰，提升长期预测稳定性。

### 异常检测任务表现
| 模型 | SMD F1 | PSM F1 | SWaT F1 | **Overall F1** |
|------|--------|--------|--------|---------------|
| **FusAD** | **0.879** | **0.977** | **0.987** | **0.872** |
| TVNet | 0.857 | 0.975 | 0.983 | 0.868 |
| GPT4TS | 0.868 | 0.971 | 0.986 | 0.867 |
| ModernTCN | 0.858 | 0.972 | 0.980 | 0.866 |

- FusAD在多数数据集上取得最佳F1分数；
- Transformer类模型普遍表现不佳，可能因注意力机制偏向正常样本而忽略稀疏异常；
- 周期性建模（如TimesNet）有一定优势，验证了频域分析的重要性。

### 消融实验结果（Ablation Study）
在多个任务上验证各组件贡献：

| 变体 | AWR (Acc) | SRSCP (Acc) | ETTh1 (MSE) | SMD (F1) | PSM (F1) |
|------|-----------|-------------|--------------|----------|----------|
| **FusAD (完整)** | **0.990** | **0.921** | **0.403** | **0.879** | **0.977** |
| w/o ASM | 0.986 | 0.787 | 0.422 | 0.858 | 0.953 |
| w/o ASM-F (无Fourier) | 0.987 | 0.802 | 0.420 | 0.854 | 0.965 |
| w/o ASM-W (无Wavelet) | 0.989 | 0.805 | 0.416 | 0.871 | 0.963 |
| w/o ASM-T (无自适应阈值) | 0.988 | 0.814 | 0.409 | 0.868 | 0.970 |
| w/o IFM | 0.976 | 0.835 | 0.419 | 0.872 | 0.971 |
| w/o Pretrain | 0.979 | 0.876 | 0.408 | 0.870 | 0.975 |

- **ASM模块最关键**，移除后性能全面下降；
- **Fourier分支对预测更重要**，因其捕获长期周期；
- **Wavelet分支对异常检测更关键**，利于捕捉局部突变；
- **自适应阈值机制显著提升鲁棒性**，尤其在噪声环境下；
- **IFM 和 Pre-training 对表示学习有稳定增益**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **时频融合是构建通用TSA模型的有效路径**：结合 Fourier 与 Wavelet 可同时建模全局趋势与局部变化，优于单一变换。
2. ✅ **自适应去噪机制显著增强鲁棒性**：相比固定滤波器或静态去噪，Learnable Thresholding 更能适应不同数据分布。
3. ✅ **统一框架可行且高效**：FusAD 在分类、预测、异常检测三大任务上均达到或接近SOTA，证明了“一模型多任务”的潜力。
4. ✅ **轻量化设计带来高效率**：仅2.3M参数即可超越大量大模型，在参数效率和推理速度方面具备优势。
5. ✅ **可视化验证特征质量更高**：t-SNE 显示 FusAD 学得的类别原型更紧凑，预测曲线更稳定无漂移。

### 方法的局限性
- 在极高维数据（如 Traffic > 800维）上性能略有下降，可能受限于模型容量；
- 当前框架主要面向规则采样数据，对不规则时间序列支持有限；
- 自适应阈值虽有效，但增加了训练复杂度，需更多调参经验。

### 未来工作方向
1. 探索更灵活的时频融合机制（如可变形Wavelet核）；
2. 研究模型压缩与加速技术，便于边缘部署；
3. 扩展至 **Multimodal Time Series**（如文本+数值信号对齐）；
4. 支持 **Irregularly Sampled Data** 和缺失值更强鲁棒性的建模；
5. 构建更大规模的预训练基础模型（Foundation Model for TSA）。

---

> 🔚 **总结**：FusAD 是一种新颖、高效、鲁棒的统一时间序列分析框架，通过 **Adaptive Time-Frequency Fusion + Denoising + Masked Pre-training** 的组合，在多个任务和数据集上展现出卓越性能，为通用时间序列智能提供了重要实践路径。

</details>

---

### 11. [AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach](https://arxiv.org/abs/2512.13714)

**Authors**: Gangesh Pathak, Prasanna Kumar  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13714v1  

#### Abstract
LLM implementations are failing in highly regulated industries owing to instability issues, inconsistent reasoning, hallucinations and performance variability, especially in workflows. These reliability issues restrict safe use of LLM in areas that need the precision of facts and consistent behavior...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*AI-Powered Annotation Pipelines for Stabilizing Large Language Models: A Human-AI Synergy Approach*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（**LLM**）在实际部署中面临严重的**稳定性问题**，具体表现为：
- **语义漂移（Semantic divergence）**：相同意图的提示词生成不同含义的回答
- **幻觉（Hallucination）**：自信地输出错误或虚构信息
- **推理崩溃（Reasoning breakdown）**：逻辑矛盾或不连贯
- **会话漂移（Session drift）**：多轮对话中输出质量下降

这些问题限制了LLM在医疗、金融、法律等高风险领域的可靠应用。传统对齐方法如**RLHF** 和 **Supervised Fine-Tuning (SFT)** 虽有改进，但依赖大量人工标注，成本高昂且难以持续扩展。

---

### 🚀 提出的新方法与创新思路
本文提出一种 **Human-AI Synergy Approach**，构建了一个闭环的 **AI-Powered Annotation Pipeline**，用于系统性识别、标注并修复LLM输出中的不稳定模式。

#### 核心框架设计：
1. **自动化标注层（Automated Annotation Layer）**
   - 使用轻量级LLM作为“专家注释器”，自动检测三类稳定性问题：
     - **Semantic Consistency**（语义一致性）
     - **Factual Accuracy**（事实准确性）
     - **Logical Coherence**（逻辑连贯性）
   - 引入**置信度评分（confidence scoring）** 和 **集成投票（ensemble voting）** 来过滤低质量自动标签
   - 设置不确定性阈值，触发人工介入

2. **人在回路验证（Human-in-the-Loop Validation）**
   - 人类仅审查以下高风险案例：
     - 低置信度输出
     - 多个AI标注器意见冲突
     - 可能造成危害的事实错误
     - 上下文敏感任务
   - 包含 **Expert Curation** 与 **Community Curation** 双重机制，兼顾专业性与包容性

3. **稳定性反馈机制（Stability Feedback Mechanism）**
   - 将验证后的高质量标注数据用于：
     - **Stability Fine-Tuning**：监督训练以减少不稳定行为
     - **RIC-Based Stabilisation**：基于奖励机制强化低方差、高事实对齐的行为
   - 形成**稳定性反馈循环（Stability Feedback Loops）**

4. **模块化系统架构**
   - 包括 Annotation Engine、Validation Interface、Feedback Manager、Monitoring Dashboard 等组件
   - 支持实时监控稳定性指标与漂移检测

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如RLHF/SFT） | 本方法 |
|------|------------------------|-------|
| **可扩展性** | 高度依赖人工标注，难规模化 | 自动化初筛 + 选择性人工审核，大幅降低人力成本 |
| **持续性** | 多为一次性对齐，缺乏长期优化机制 | 构建闭环反馈系统，支持生命周期内持续稳定化 |
| **评估维度** | 主要关注单次准确率，忽略响应变异性 | 提出**稳定性作为可量化目标**，引入SI、FC等新指标 |
| **伦理控制** | 完全AI驱动易放大偏见；纯人工效率低 | 人机协同保障道德正确性与标注保真度 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验聚焦于挑战多步推理与事实一致性的任务：
| 数据集 | 目标 | 稳定性压力测试设计 |
|--------|------|--------------------|
| **TruthfulQA** | 检测幻觉与事实错误 | 每个问题生成5–10种同义改写版本 |
| **GSM8K / Synthetic Multi-Turn Reasoning** | 测试逻辑一致性 | 加入干扰短语、上下文重排序 |
| **Knowledge-Grounded Dialogues**（医学/教育子集） | 衡量多轮对话一致性 | 扩展多轮变体，注入歧义情境 |

此外还构建了专门的**稳定性压力测试子集**，包括：
- 语义等价的同义提示
- 跨会话重置的重复提示
- 故意诱导推理分歧的矛盾查询

---

### 🧪 实验设置与评估指标

#### 基线模型对比：
| 模型 | 训练方式 | 预期行为 |
|------|---------|----------|
| **Baseline-1**: Standard Fine-Tuned GPT-style Model | 仅指令微调（SFT） | 高任务准确率，但输出方差大 |
| **Baseline-2**: RLHF-Aligned Model | 基于人类反馈的强化学习 | 对齐更好，但在提示改写下仍出现漂移 |

#### 新提出的稳定性评估指标：
| 指标 | 定义 | 目标 |
|------|-----|------|
| **Stability Index (SI)** ↓ | 同义提示下响应的语义方差 | 越低越好（更稳定） |
| **Factual Consistency (FC)** ↑ | 回答与参考答案的事实一致性 | 越高越好（越少幻觉） |
| **Annotation Precision (AP)** ↑ | 自动标注与人工校验的一致率 | 衡量自动化质量 |
| **Response Diversity Ratio (RDR)** | 基于熵的语义多样性度量 | 平衡稳定性与创造性 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自表5.1）
| Model | SI ↓ | FC ↑ | AP ↑ | RDR |
|-------|------|------|------|-----|
| Baseline-1 (SFT) | 0.41 | 72% | — | 0.52 |
| Baseline-2 (RLHF) | 0.33 | 81% | 78% | 0.47 |
| **Stabilized Model** (**本文方法**) | **0.18** | **92%** | **94%** | **0.45** |

#### 性能提升总结：
- **相比SFT基线**：  
  - **SI降低56%** → 显著减少输出波动
  - **FC提升20个百分点** → 幻觉显著减少
- **相比RLHF基线**：  
  - **SI再降45%**，**FC再升11%**
- **AP达94%** → 自动标注高度可信，人工干预负担小
- **RDR保持稳定** → 在增强稳定性的同时未牺牲生成多样性

---

### 🔍 消融分析与定性结果
虽然文中未明确列出消融实验表格，但从机制设计与讨论中可推断关键发现：

#### ✅ 有效性验证（Qualitative Analysis）：
- **医疗问答示例**：
  - SFT模型错误声称“抗生素可治疗流感病毒”
  - RLHF模型模糊回应“有时可用于治疗”
  - **本文方法正确指出：“抗生素只治细菌感染，仅当继发细菌感染时才使用”**
- **数学推理任务**：
  - 基线模型对等价数学题给出不同答案
  - 稳定化模型始终返回一致解，并附清晰推理链

#### 设计优势分析：
| 设计要素 | 效果 |
|--------|------|
| 自动化大规模标注 | 快速识别不稳定模式 |
| 人类聚焦于模糊/高风险案例 | 避免盲目信任AI判断 |
| 稳定性反馈循环 | 实现行为持续强化 |

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **稳定性可以被量化并作为独立优化目标**，不应只是对齐的副产品。
2. **AI驱动的标注流水线+选择性人类验证** 是实现LLM稳定化的高效路径。
3. 该方法在**多轮对话、事实问答、复杂推理**场景中均显著提升了输出一致性与事实准确性。
4. **人机协同（Human-AI Collaboration）** 在保证可靠性的同时实现了可扩展性，优于纯人工或纯AI方案。

---

### ⚠️ 局限性
1. **自动化标注器依赖启发式规则与训练数据质量**，在缺乏ground truth的知识领域可能误判非常规合理推理。
2. **人类验证存在主观偏差风险**，尤其在逻辑严谨性判断上受专业知识影响。
3. 方法在**快速演进的专业领域**（如生物医药）泛化能力有待验证，需动态更新知识库。
4. 存在**错误传播风险**：若自动化阶段错误未被发现，可能通过再训练“病毒式扩散”。

---

### 🔮 未来工作方向
1. 开发具备**自我解释能力的自动标注代理（annotator agents）**，能说明其判断依据，便于人类快速验证。
2. 探索**多模型互评机制（multi-model ensembles）**，让不同LLM相互审查，提升跨模型稳定性。
3. 构建**终身学习（lifelong learning）系统**，实现实时运行中的稳定性监测与主动修正。
4. 加强透明性工具与可解释性结构，支持对系统性变化的审计与治理。

---

## ✅ 总结
本文提出了一种面向**LLM稳定性增强**的新型 **AI-Powered Annotation Pipeline**，通过**自动化检测 + 选择性人类验证 + 迭代反馈机制**，实现了高保真、可扩展的模型稳定化。实验证明其在降低输出方差、提升事实一致性方面显著优于SFT与RLHF基线，同时保留了生成多样性。该工作推动了将“稳定性”从隐性需求转变为**可测量、可持续优化的核心属性**，为下一代可信LLM提供了可行的技术范式。

</details>

---

### 12. [Astraea: A State-Aware Scheduling Engine for LLM-Powered Agents](https://arxiv.org/abs/2512.14142)

**Authors**: Hongqiu Ni, Jiabao Zhang, Guopeng Li, Zilong Wang, Ruiqi Wu, Chi Zhang, Haisheng Tan  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.14142v1  

#### Abstract
Large Language Models (LLMs) are increasingly being deployed as intelligent agents. Their multi-stage workflows, which alternate between local computation and calls to external network services like Web APIs, introduce a mismatch in their execution pattern and the scheduling granularity of existing ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Astraea: A State-Aware Scheduling Engine for LLM-Powered Agents

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现有的 **LLM 推理服务系统**（如 vLLM）主要针对传统的“一次性”推理任务进行优化，其调度策略通常以单个计算阶段（如 Prefill 或 Decode）为单位，忽略了 **LLM Agent 多阶段、交替执行计算与 I/O 的复杂工作流特性**。

这导致两个关键问题：
- **Head-of-Line (HoL) 阻塞**：长时 API 调用期间，请求占用 GPU 内存，阻塞其他请求。
- **局部优化 vs 全局延迟**：系统只优化当前段的响应时间，而忽视了整个请求生命周期的 **Job Completion Time (JCT)**，造成全局性能下降。

### **提出了什么新方法或新思路**

作者提出 **Astraea** —— 一种面向 LLM Agent 工作流的、生命周期感知的推理服务引擎，其核心是 **Stateful-MLFQ** 调度算法，具备以下创新设计：

#### ✅ **状态感知的分层调度机制（Stateful-MLFQ）**
- **宏观控制（Macro-level）**：基于请求的历史行为动态调整优先级队列。
  - 若某段计算开销大 → 判定为 **Compute-intensive** → 降级到低优先级队列。
  - 若提前触发 API 调用 → 判定为 **I/O-intensive** → 升级到高优先级队列，避免长时间阻塞。
- **微观排序（Micro-level）**：在每个队列内部采用 **HRRN（Highest Response Ratio Next）** 策略对就绪段排序，平衡效率与公平性：
  $$
  \text{Score}_{\text{HRRN}}(S) = \frac{W(R) + T_{\text{proc}}(S)}{T_{\text{proc}}(S)}
  $$
  其中 $W(R)$ 是父请求累积等待时间，$T_{\text{proc}}(S)$ 是预测处理时间。

#### ✅ **自适应 KV Cache 管理**
- 在 I/O 等待期间，根据 GPU 内存压力动态选择缓存策略：
  - **低负载**：采用 `Preserve` 策略，保留 KV Cache 于 GPU，降低恢复延迟。
  - **高负载**：评估三种策略（Preserve / Discard / Swap）的内存浪费成本，选择最优方案：
    $$
    \text{Strategy} = \arg\min_{s \in \{\text{Preserve}, \text{Discard}, \text{Swap}\}} W_s
    $$

#### ✅ **服务时间预测器（Service Time Predictor）**
- 结合离线性能建模（Prefill）、生成长度 Oracle（Decode）和 API 类别统计（API Latency），提供准确的服务时间估计，支撑调度决策。

### **相比现有方法的优势**

| 维度 | Astraea | 现有系统（如 vLLM） |
|------|--------|---------------------|
| 调度粒度 | 请求生命周期级（Global JCT） | 单段级（Local RT） |
| 状态感知 | ✅ 历史行为 + 未来预测 | ❌ 无状态调度 |
| I/O 影响处理 | 显式建模并优先处理 I/O 密集型请求 | 忽视 API 时间，易引发 HoL 阻塞 |
| 缓存管理 | 自适应策略，权衡延迟与吞吐 | 固定策略（如 Always Preserve） |

---

## 2. 核心实验方法和设置

### **使用的数据集**

基于 **Infercept** 发布的数据集构建，包含六类任务，模拟真实 LLM Agent 场景：
1. **Arithmetic Operations**（基于 GSM8K-XL）
2. **Knowledge Question Answering**（Multi-Hop QA）
3. **Virtual Environment**（ALFWorld）
4. **Multi-turn Dialogue**（ShareGPT）
5. **Image Generation**（Stable Diffusion API）
6. **Text-to-Speech**（Bark TTS API）

> ⚠️ 实验中人为增加了长延迟 API 请求的比例，以增强测试挑战性。

### **实验设置**

- **硬件平台**：双 NVIDIA A100 80GB GPU（NVLink 连接），Intel Xeon Gold CPU，503GB RAM。
- **模型规模**：
  - **GPT-J-6B**（单卡运行，中等负载）
  - **Vicuna-13B**（双卡运行，高压内存场景）
- **实现基础**：基于 **vLLM** 框架扩展，集成 PagedAttention 和连续批处理（continuous batching）能力。

### **评估指标**

| 指标 | 含义 |
|------|------|
| **Average JCT**（Job Completion Time） | 主要指标，衡量从请求提交到最终完成的端到端延迟 |
| TTFT（Time to First Token） | 辅助指标，非核心关注 |
| TPOT（Time Per Output Token） | 辅助指标，非核心关注 |

### **基线方法对比**

1. **vLLM + FCFS**：先来先服务
2. **vLLM + SJF**（Shortest Job First）：按段长度最短优先
3. **vLLM + LAS**（Least Attained Service）：累计服务最少者优先（Autellix 采用）
4. **Infercept**：支持 KV Cache Offload 的先进系统，默认 FCFS

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **在 GPT-J-6B 上的表现（图4）**
- 在 **30% GPU 内存可用率、QPS=5** 的高压场景下：
  - Astraea 平均 JCT：**122.88 秒**
  - 比 Infercept 降低 **19.1%**
  - 比 vLLM-FCFS 降低 **25.5%**

#### ✅ **在 Vicuna-13B 上的表现（图5）**
- 在相同条件下（30% 内存，QPS=5）：
  - Astraea JCT：**140.02 秒**
  - 比 vLLM-SJF（171.16s）降低 **18.2%**
  - 比 vLLM-LAS（153.96s）降低 **9.1%**
- 即使在 **90% 内存可用率** 下，仍比 vLLM-FCFS 低 **16.0%**

> 📈 总体趋势：**内存越紧张、负载越高，Astraea 的优势越显著**。

### **与基线方法的对比结果**

| 方法 | 相对 Astraea 的 JCT 提升幅度（典型高压场景） |
|------|---------------------------------------------|
| vLLM-FCFS | +25.5% ~ +30% |
| vLLM-SJF | +18% ~ +22% |
| vLLM-LAS | +9% ~ +12% |
| Infercept | +15% ~ +19% |

> Astraea 在所有配置下均取得最优性能，尤其在资源受限环境下表现稳定。

### **消融实验结果（Ablation Study）**

在固定 QPS=3 条件下，剥离 KV Cache 管理影响，仅比较调度算法（图6）：

| 模型 | 调度器 | 相对 Stateful-MLFQ 的 JCT 增加 |
|------|--------|-------------------------------|
| GPT-J | FCFS | +2.3% |
| GPT-J | SJF | +19.3% |
| Vicuna-13B | FCFS | +2.5% |
| Vicuna-13B | LAS | +3.8% |
| Vicuna-13B | SJF | +11.0% |

> 🔍 结论：**Stateful-MLFQ 本身即带来显著收益**，证明其调度逻辑的有效性和通用性。

---

## 4. 关键结论和发现

### **主要发现**

1. **传统调度策略不适用于 LLM Agent 工作流**：忽略 I/O 时间和请求依赖关系会导致严重的 HoL 阻塞和全局延迟上升。
2. **生命周期感知调度至关重要**：将调度视角从“段”提升到“完整请求”，才能有效优化 **JCT**。
3. **状态感知 + 预测驱动的调度优于纯历史或静态策略**：结合历史行为分类与未来预测排序（HRRN），实现了效率与公平的平衡。
4. **自适应 KV Cache 管理提升了资源利用率**：在高负载下通过 Swap/Discard 减少内存浪费，维持系统吞吐。
5. **Astraea 具备强鲁棒性**：在高负载下 JCT 增幅仅为基线的 **1/3**（Astraea ↑16.2%，vLLM-SJF ↑51.9%）。

### **方法的局限性**

1. **依赖 API 类别预测准确性**：若无法准确识别 API 类型，则延迟预测可能偏差较大。
2. **未考虑多租户公平性**：实验聚焦单用户场景，未深入探讨不同用户间的资源分配公平问题。
3. **假设批处理可中断**：虽然避免了细粒度抢占，但在极端情况下仍可能存在调度延迟。

### **未来工作方向**

1. 引入更精细的 **API 延迟预测模型**（如基于上下文语义分析）。
2. 扩展至 **多租户、多模型共存环境**，研究跨请求公平性保障机制。
3. 探索 **与 disaggregation 架构结合**（如 DistServe），进一步解耦 Prefill 与 Decode 阶段。
4. 将调度决策与 **模型并行、流水线并行** 等分布式技术协同优化。

---

> ✅ **总结一句话**：  
> Astraea 首次提出将 LLM Agent 的调度从“段级优化”转向“生命周期级优化”，通过 **Stateful-MLFQ + 自适应 KV Cache 管理**，显著降低了端到端延迟，在多种模型和负载下平均 JCT 最多减少 **25.5%**，为下一代智能代理服务系统提供了重要范式。

</details>

---

### 13. [Delete and Retain: Efficient Unlearning for Document Classification](https://arxiv.org/abs/2512.13711)

**Authors**: Aadya Goel, Mayuri Sridhar  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13711v1  

#### Abstract
Machine unlearning aims to efficiently remove the influence of specific training data from a model without full retraining. While much progress has been made in unlearning for LLMs, document classification models remain relatively understudied. In this paper, we study class-level unlearning for docu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Delete and Retain: Efficient Unlearning for Document Classification**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
该论文研究的是**class-level unlearning**（类别级遗忘）在**document classification**（文档分类）模型中的应用。具体而言，当一个训练类别（如敏感标签）需要被完全从已训练的模型中“遗忘”时，如何高效地移除该类别的影响，同时保留对其他类别的预测能力。

传统做法是**retraining from scratch**（从头重新训练），但这在计算上代价高昂。而现有的**machine unlearning**方法多集中于删除单个样本或小批量数据，在处理整个类别删除时存在以下问题：
- 忽视了模型在剩余类别间的**score ordering**（得分排序）结构；
- 常用的“Random Relabeling”策略会引入大量噪声，损害模型效用。

---

### **提出了什么新方法或新思路**
作者提出了一种名为 **Hessian Reassignment** 的两步、model-agnostic 的 unlearning 方法：

1. **Hessian Downweight Update**  
   利用二阶优化信息（Hessian-vector product），通过共轭梯度法求解一个类级别的“下权重”更新，一次性减去目标类别所有样本对模型参数的影响。这相当于一个近似的牛顿步长，无需完整重训练。

2. **Top-1 Classification Reassignment**  
   对于原属于被删除类别的样本，不是随机重新分配标签，而是将其确定性地重新分配为模型在删除前预测的**次高概率类别**（next top-1 label）。这种方法保留了模型原有的决策空间结构。

此外，在部署时将被删除类别的输出通道置零并重新归一化概率，确保模型不再输出该类别。

---

### **相比现有方法的优势**
- ✅ **高效性**：相比 full retraining 加速数十倍（实验中快约10倍以上）；
- ✅ **高保真度**：在保留类上的准确率接近“Golden Standard”（即从头重训练）；
- ✅ **隐私保护更强**：显著降低针对删除类的 **Membership Inference Attack (MIA)** 成功率（AUC 接近 0.5）；
- ✅ **结构保持**：避免 Random Relabeling 引入的标签噪声，维持了模型在非目标类别间的相对排序一致性。

---

## 2. **核心实验方法和设置**

### **使用的数据集**
在三个标准文本分类 benchmark 上进行评估：
- **20 Newsgroups**：20 个主题，中等长度文档，挑战细粒度边界保持；
- **AG News**：4 个新闻类别，语义差异大，测试粗粒度场景下的稳定性；
- **DBPedia-14**：14 类维基百科摘要，结构清晰，高准确率基线。

---

### **实验设置**
- **模型架构**：基于 **TF-IDF 特征 + multinomial logistic regression**（带 L2 正则化的线性 softmax 分类器）；
- **训练方式**：使用 L-BFGS 优化至收敛（梯度容忍度 $10^{-5}$，正则强度 $C=10.0$）；
- **unlearning 流程**：
  - 计算被删类别的梯度总和 $g_c(\theta)$；
  - 使用共轭梯度法求解 $\Delta \theta = H^{-1} g_c$；
  - 更新参数 $\theta' = \theta - \Delta \theta$；
  - 零化对应 logit 输出通道。

---

### **评估指标**

#### **模型效用（Utility）**
- **Accuracy excluding removed class**：在保留类别上的测试准确率；
- **Agreement on removed class w.r.t. Golden Standard**：与完全重训练模型在原删除类样本上的预测一致性。

#### **隐私保护（Privacy）**
- **Membership Inference Attack (MIA)**：
  - 攻击者基于 shadow models 训练 logistic regression 分类器；
  - 输入特征包括预测概率、熵、负对数似然、top-2 gap 等；
  - 输出为成员判断分数；
  - 报告两个 AUC：
    - **AUC_ret**：在保留类上的 MIA AUC；
    - **AUC_del**：在删除类上的 MIA AUC（理想值应趋近 0.5）。

---

### **基线方法对比**
| 方法 | 描述 |
|------|------|
| **Golden Standard (Complete Retraining)** | 删除该类后从头重新训练 —— “黄金标准” |
| **Random Relabeling** | 将删除类样本随机重新分配到其余类别，然后微调 —— 当前 SOTA 基线 |
| **Hessian Reassignment (Ours)** | Hessian 下权重 + Top-1 重分配 |

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**

#### **表1：模型效用比较（保留类准确率 & 删除类一致率）**

| Method | 20 Newsgroups | AG News | DBPedia-14 |
|--------|----------------|---------|------------|
| Pre-Unlearning | 93.58% | 92.14% | 98.33% |
| Golden Standard | 93.73% | 95.65% | 98.43% |
| Random Relabeling | 83.45% | 84.58% | 88.46% |
| **Hessian Reassignment** | **93.15%** | **95.04%** | **97.55%** |
| **Unlearned Class Agreement** | **87.77%** | **88.16%** | **90.46%** |

> 💡 结论：Hessian Reassignment 在保留类准确率上几乎与 Golden Standard 持平，远优于 Random Relabeling；且在删除类样本上的预测与重训练高度一致（~88–90%）。

---

#### **表2：Membership Inference AUC（越接近 0.5 越好）**

| Method | Group | 20 Newsgroups | AG News | DBPedia |
|--------|-------|----------------|---------|---------|
| Pre-Unlearning | Retain | 0.6635 | 0.5569 | 0.6832 |
|                | Target | 0.6946 | 0.5510 | 0.6794 |
| Random Relabeling | Retain | 0.6346 | 0.5163 | 0.6147 |
|                   | Target | 0.5530 | 0.5181 | 0.5228 |
| **Hessian Reassignment** | Retain | **0.6219** | **0.5235** | **0.6234** |
|                          | **Target** | **0.5109** | **0.5161** | **0.5094** |

> 💡 结论：Hessian Reassignment 显著降低了删除类的 MIA AUC（接近 0.5），说明成功“遗忘”了该类成员信息，同时未明显增加保留类的风险。

---

### **其他重要结果**
- **Confidence Distribution 分析**（图3）：
  - 在保留类上，unlearning 前后的 top-1 margin 分布几乎不变（KS test $p=1.0$），表明模型信心未受干扰。
- **Noise Trade-off 实验**（图4）：
  - 即使不加额外噪声，Hessian Reassignment 已能实现良好隐私；
  - 若需进一步压缩 AUC，仅需极小的 logit 噪声即可达标，且效用损失极小。
- **运行效率对比**（图5）：
  - 在 DBPedia 上，**full retraining 耗时 ~602 秒**，而 **Hessian Reassignment 仅需 ~53 秒**，提速约 **11 倍**。

---

## 4. **关键结论和发现**

### **主要发现**
1. **Top-1 reassignment 是关键设计**：相比于破坏性随机重标，保留模型原有 score ordering 可极大提升 unlearning 后的效用。
2. **Hessian downweight 是有效的近似修正**：利用局部曲率信息的一次性更新，足以逼近 retraining 的效果。
3. **Privacy-Utility Balance 出色**：在几乎不牺牲保留类性能的前提下，有效抹除删除类的 membership signal。
4. **方法高效且可扩展**：仅依赖 gradient 和 Hessian-vector product，适用于多种架构。

---

### **方法的局限性**
- 当前实验集中在 **linear-softmax + TF-IDF** 这类简单 pipeline，尚未验证在深度神经网络（如 Transformer）上的表现；
- Hessian inversion 使用共轭梯度法，虽然高效但仍假设 Hessian 可近似求逆，在高度非凸情况下可能不稳定；
- 未提供严格的理论保证（如影响上界），更多是经验性有效。

---

### **未来工作方向**
1. **扩展至 neural models**：将 Hessian Reassignment 应用于 BERT、RoBERTa 等现代 NLP 模型；
2. **开发 lightweight certificates**：量化 unlearning 后残余影响的程度，迈向可证明遗忘（provable unlearning）；
3. **支持增量 unlearning 多类别**：探索连续删除多个类别的累积效应与修正机制；
4. **结合 differential privacy**：与 DP 机制融合以增强整体隐私保障。

---

## ✅ 总结
**Hessian Reassignment** 提出了一条**高效、实用、兼顾效用与隐私**的 class-level unlearning 路径。它通过 **一次二阶更新 + 确定性重分配**，实现了接近完全重训练的效果，同时大幅缩短时间成本，为现实系统中的“被遗忘权”提供了可行的技术方案。

</details>

---

### 14. [RAST-MoE-RL: A Regime-Aware Spatio-Temporal MoE Framework for Deep Reinforcement Learning in Ride-Hailing](https://arxiv.org/abs/2512.13727)

**Authors**: Yuhan Tang, Kangxin Cui, Jung Ho Park, Yibo Zhao, Xuan Jiang, Haoze He, Dingyi Zhuang, Shenhao Wang, Jiangbo Yu, Haris Koutsopoulos, Jinhua Zhao  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13727v1  

#### Abstract
Ride-hailing platforms face the challenge of balancing passenger waiting times with overall system efficiency under highly uncertain supply-demand conditions. Adaptive delayed matching creates a trade-off between matching and pickup delays by deciding whether to assign drivers immediately or batch r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RAST-MoE-RL: A Regime-Aware Spatio-Temporal MoE Framework for Deep Reinforcement Learning in Ride-Hailing  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
该论文针对**网约车平台中的自适应延迟匹配（adaptive delayed matching）决策问题**，旨在在乘客等待时间（matching delay）与接驾时间（pickup delay）之间取得平衡。传统方法面临以下挑战：
- **交通动态建模不准确**：多数研究假设静态拥堵，无法反映真实城市中时变的供需与密度-速度反馈。
- **奖励函数设计脆弱**：固定权重的奖励容易导致“reward hacking”行为，如无限期持有请求或立即匹配，损害服务质量。
- **表示能力不足**：标准RL模型（如浅层MLP）难以捕捉复杂的时空模式，尤其在不同供需状态（regimes）下泛化能力差。

---

### **提出了什么新方法或新思路**
作者提出 **RAST-MoE-RL** 框架，其核心是将 **Regime-Aware Spatio-Temporal Mixture-of-Experts (RAST-MoE)** 编码器引入深度强化学习（RL），并构建了一个更真实的环境模拟框架。

#### 主要创新点包括：
1. **RAST-MDP（Regime-Aware Spatio-Temporal MDP）建模**
   - 将匹配决策形式化为一个具有显式区域动作空间的MDP。
   - 引入**物理感知的拥堵代理模型（physics-informed congestion surrogate）**，基于宏观基本图（MFD）构建高效的OD旅行时间表，保留密度-速度反馈，支持百万级高效rollout。

2. **抗作弊奖励机制（Anti-hacking Reward）**
   - 设计动态调整的惩罚系数 λ，通过在线更新机制对服务违规（如超时匹配）施加自适应惩罚。
   - 避免策略通过“拒绝远距离乘客”或“永远不等待”等手段操纵奖励。

3. **RAST-MoE 编码器架构**
   - 采用**稀疏激活的MoE结构**，每个专家自动专注于特定的供需-拥堵状态（如早高峰、夜间低需求）。
   - 路由器基于全局池化状态进行决策，提升计算效率与稳定性。
   - 支持专家自然专业化，允许高频与低频专家共存，避免强制均衡导致性能下降。

---

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **表达能力** | MoE结构显著增强对复杂spatio-temporal patterns的建模能力，优于MLP、ResMLP和Dense Transformer。 |
| **训练稳定性** | 自适应奖励防止reward hacking；PPO结合MoE实现稳定长周期训练。 |
| **计算效率** | 仅12M参数即超越更大规模模型（如48M Dense Transformer），每样本推理成本可控。 |
| **泛化能力** | 在未见需求模式、天气扰动、局部事故等OOD场景下表现稳健。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**
- **数据来源**：加州公共事业委员会（CPUC）TNC Data Portal 提供的 **2019年Uber和Lyft旧金山轨迹数据**。
- **数据特点**：
  - 日均约17万次完成行程，代表高密度、高拥堵的典型城市环境。
  - 包含上车/下车位置、时间戳、订单结果等。
- **划分方式**：
  - 训练集：多个非连续时间段（覆盖高峰与平峰）。
  - 测试集：完全未见过的时间窗口，确保模型不能记忆具体模式。

---

### **实验设置和评估指标**

#### **MDP设置**
- **State**：按网格划分城市（H×W cells），每格包含乘客数、司机数、到达率等。
- **Action**：二元决策 `a_i ∈ {0,1}` 表示是否在区域i执行即时匹配（“hold” or “match now”）。
- **Transition**：基于MFD代理模型计算旅行时间，驱动车辆移动与新请求生成。

#### **评估指标**
- **总奖励（Total Reward）**
- **平均匹配延迟（Matching Delay）**
- **平均接驾延迟（Pickup Delay）**
- **服务违规比例（Service Violations）**

#### **训练配置**
- 使用 **PPO** 作为主训练算法，兼容A2C、ACER、GRPO等。
- 所有模型共享相同超参（见Appendix F），Adam优化器，batch size=512。

---

### **基线方法对比**
| 类别 | 基线方法 |
|------|--------|
| **RL Baselines** | PPO, A2C, ACER, DQN |
| **Encoder Variants** | Plain MLP, ResMLP, Dense Transformer (12M & 48M) |
| **Heuristic Policies** | Instant Matching, Constant-Interval Batch Matching (20s) |
| **MoE变体** | 不同专家数量（8/16/32/64）与Top-K路由组合 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**
在旧金山真实数据上的最终性能（PPO + RAST-MoE, 16专家, Top-4路由）：

| 指标 | 提升幅度 | 绝对值 |
|------|---------|-------|
| **Total Reward** | ↑13% | 达到最高水平 |
| **Matching Delay** | ↓10% | 从 ~200s → **181±28s** |
| **Pickup Delay** | ↓15% | 从 ~600s → **524±33s** |

> 注：所有改进均在测试集上验证，表明良好泛化能力。

---

### **与基线方法的对比结果**

#### ✅ 显著优于各类基线
- **优于传统PPO + MLP/Transformer**：
  - 即使参数量仅为12M，RAST-MoE仍优于48M的Dense Transformer（匹配延迟↓10s，接驾延迟↓20s）。
- **优于启发式策略**：
  - Instant Matching 匹配延迟虽低（212s），但接驾延迟高达825s。
  - RAST-MoE实现了更优的权衡。

#### ✅ 多种RL算法下一致增益
- 在PPO、A2C、ACER、GRPO等多种训练器上，替换编码器为RAST-MoE均带来性能提升，说明收益来自**表示能力增强**而非特定优化技巧。

---

### **消融实验结果（Ablation Studies）**

#### （1）MoE配置选择
- 最佳配置为 `(E,K)=(16,4)`（16个专家，激活前4个）。
- 更大专家池（如64专家）或过度稀疏路由（如Top-2）反而性能下降，说明存在**容量与利用率的平衡点**。

#### （2）编码器类型对比（见Table 2）
| 编码器 | 参数量 | Matching Wait (s) | Pickup Wait (s) |
|--------|--------|-------------------|-----------------|
| RAST-MoE (16,4) | 12M | **182** | **523** |
| Dense Transformer | 48M | 192 | 545 |
| ResMLP | 12M | 205 | 575 |
| Plain MLP | 12M | 208 | 577 |

👉 结论：**轻量级MoE > 大型密集Transformer > 各类MLP**

#### （3）专家重要性验证（Masking Experiment）
- 分别屏蔽**高频专家**（日常通用）和**低频专家**（如晚高峰专用）：
  - 两者都导致性能显著下降（reward降低，延迟上升）。
  - 证明即使是低频专家也承载关键状态下的有效策略。

#### （4）自适应奖励 vs 固定权重
- 固定比例（如8:1）会导致极端行为：
  - `8:1`：匹配延迟趋近于0，但接驾延迟飙升至**1746s**（≈29分钟）。
  - `1:8`：匹配延迟长达**2236s**（≈37分钟），无实际意义。
- **自适应奖励**在整个训练过程中保持稳定trade-off，避免reward hacking。

---

## 4. 关键结论和发现

### **主要发现**
1. **Regime-aware representation至关重要**  
   城市交通具有明显的周期性与非平稳性（早高峰、夜间稀疏等），单一单体网络难以适应。MoE通过专家分工实现了对不同regime的有效建模。

2. **轻量MoE可胜过大型密集模型**  
   仅12M参数的RAST-MoE在性能上全面超越48M的Dense Transformer，体现了**专家专业化带来的高效表示增益**。

3. **自适应奖励机制有效防止reward hacking**  
   动态调整的λ系数能自动调节匹配与接驾之间的权衡，无需人工调参，且在多种需求条件下保持鲁棒。

4. **专家确实实现了有意义的专业化**  
   可视化分析显示：
   - 某些专家专注早晚高峰；
   - 某些专家处理午间或深夜低需求；
   - 高峰时段路由更分散，体现系统复杂性。

5. **强健的OOD泛化能力**  
   在加入全局扰动（天气减速）和局部事故冲击后，RAST-MoE相对性能下降最小（见Table 7），表明其未过拟合历史模式。

---

### **方法的局限性**
- **专家解释性有限**：尽管观察到专家激活模式与现实regime相关，但尚难精确界定每个专家的具体功能。
- **依赖MFD代理模型**：虽然高效，但仍是宏观近似，可能忽略微观交互细节。
- **扩展性待验证**：目前实验集中在旧金山，跨城市迁移能力需进一步测试。

---

### **未来工作方向**
1. **多城市联合训练与迁移学习**：探索MoE专家在不同城市的可迁移性。
2. **公平性与社会影响建模**：纳入地理覆盖公平、司机收入均衡等目标。
3. **端到端MoE+Simulator jointly learning**：联合优化代理模型与策略网络。
4. **结合GNN/Multi-Agent RL**：用于更细粒度的空间协调与分布式调度。

---

> **总结一句话**：  
> RAST-MoE-RL通过**regime-aware MoE编码器 + physics-informed simulator + adaptive reward**，在小模型规模下实现了高性能、高鲁棒性的网约车匹配决策，为大规模spatio-temporal RL任务提供了新的范式。

</details>

---

### 15. [The Double Life of Code World Models: Provably Unmasking Malicious Behavior Through Execution Traces](https://arxiv.org/abs/2512.13821)

**Authors**: Subramanyam Sahoo, Jared Junkin  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.13821v1  

#### Abstract
Large language models (LLMs) increasingly generate code with minimal human oversight, raising critical concerns about backdoor injection and malicious behavior. We present Cross-Trace Verification Protocol (CTVP), a novel AI control framework that verifies untrusted code-generating models through se...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*The Double Life of Code World Models: Provably Unmasking Malicious Behavior Through Execution Traces*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**代码生成类大语言模型（Code-Generating LLMs）中存在的后门注入与恶意行为隐藏风险**。随着LLMs在程序合成、调试和自动执行中的广泛应用，攻击者可能通过训练污染等方式植入上下文依赖的恶意逻辑（如隐蔽的`backdoor`），传统基于静态分析或沙箱执行的检测手段难以有效识别此类行为，尤其是在模型不可信或存在对抗性对齐偏差的情况下。

### 提出的新方法与创新思路
作者提出了一种名为 **Cross-Trace Verification Protocol (CTVP)** 的新型AI控制框架，其核心思想是：

- **不直接执行代码**，而是利用模型自身预测不同语义等价变体（Semantic Orbit）下的**执行轨迹（Execution Traces）**；
- 通过分析这些预测轨迹之间的一致性来判断是否存在恶意行为：若一个程序在多个语义不变的语法变换下，其预测执行路径出现显著差异，则表明模型可能存在“双重行为”（double life），即对特定形式触发恶意响应。

#### 创新点包括：
1. **语义轨道分析（Semantic Orbit Analysis）**  
   构造一组语义等价但语法不同的程序变体（如变量重命名、死代码插入、格式化调整等），形成“语义轨道” $ O = \{Q_1, ..., Q_k\} $，并要求模型为每个变体生成执行轨迹。

2. **跨轨迹一致性验证机制（Cross-Trace Consistency）**  
   定义了多维度的相似度度量（step-length ratio, per-step state equality, final-output agreement），计算所有轨迹对之间的相似性，并取高百分位数作为一致性得分 $ C $。

3. **Adversarial Robustness Quotient (ARQ)**  
   引入ARQ量化验证过程相对于单次推理的计算开销（wall-clock time 和 FLOPs），理论证明其随轨道大小 $ k $ 线性增长（$ \text{ARQ} \sim k+1 $），同时揭示了对抗逃避成本呈指数级上升。

4. **非可博弈性（Non-gamifiability）的理论保证**  
   基于信息论分析指出：要构造一个能在整个语义轨道上保持一致性的后门，所需尝试次数为 $ \mathcal{O}(M^{k-1}) $，其中 $ M $ 是轨迹空间规模，因此实际攻击在计算上不可行。

### 相比现有方法的优势
| 维度 | CTVP | 传统方法（如静态扫描、沙箱执行） |
|------|------|-------------------------------|
| 是否需要信任模型 | ❌ 黑盒验证 | ✅ 假设模型输出可信 |
| 是否需执行代码 | ❌ 仅预测轨迹 | ✅ 必须运行代码（有逃逸风险） |
| 对抗鲁棒性 | 高（指数级搜索空间） | 低（易被混淆绕过） |
| 可扩展性 | 支持缓存、自适应阈值（PID控制器） | 多为一次性规则匹配 |
| 形式化保障 | 有信息论边界支持 | 缺乏理论基础 |

---

## 2. 核心实验方法和设置

### 数据集
- 使用了一个**人工构建的混合数据集**，包含：
  - **良性程序（Benign Programs）**：功能正确、无恶意意图的标准Python脚本。
  - **对抗样本（Adversarial/Malicious Variants）**：嵌入了上下文敏感型后门的变种程序（例如仅在特定变量名或结构下触发恶意操作）。
- 所有程序长度控制在合理范围内（最多约200 LOC），确保可处理性和语义完整性。

### 实验设置
- **轨道大小（Orbit Size）**：测试 $ k \in \{3, 5, 10, 20\} $
- **一致性评分方式**：使用第80百分位的成对轨迹相似度作为最终一致性分数 $ C $
- **决策逻辑**：
  - 若 $ C < T_{\text{strict}} $ → `REJECT`
  - 若 $ T_{\text{strict}} \leq C < T_{\text{soft}} $ → 启用可信模型进行交叉验证
  - 若 $ C \geq T_{\text{soft}} $ → `ACCEPT`
- **自适应阈值调节**：采用类PID控制器动态调整 $ T_{\text{strict}} $，目标FPR = 5%

### 评估指标
| 指标 | 描述 |
|------|------|
| **Detection Rate** | 成功检出恶意样本的比例（True Positive Rate） |
| **False Positive Rate (FPR)** | 将良性样本误判为恶意的比例 |
| **Benign Acceptance Rate** | 良性样本被接受的比例 |
| **ARQ (Adversarial Robustness Quotient)** | 验证开销相对于单次推理的增长倍数（时间与FLOPs） |
| **Consistency Score Distribution** | 不同样本的一致性得分分布 |
| **Transformation Success Rate** | 成功生成符合最小编辑距离要求的语义等价变体比例 |

### 基线方法对比
实现了以下几种静态分析基线用于比较：
1. **Static Pattern Detector**：正则匹配关键词（如 `eval(`, `exec()`, `os.system`）
2. **AST Complexity Heuristic**：基于抽象语法树节点数量和控制流复杂度判断异常
3. **Shannon Entropy Heuristic**：检测源码字符熵过高或过低（可能表示加密/混淆）
4. **Weighted Ensemble**：上述三项中至少两项触发才判定为恶意

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2 & Figure 1）

| Orbit Size | Detection Rate (%) | FPR (%) | Avg Traces / Program | ARQ (Median) |
|------------|--------------------|---------|------------------------|---------------|
| 3          | 81.7               | 100.0   | 1.60                   | 4.00          |
| 5          | 83.3               | 97.5    | 2.80                   | 6.00          |
| 10         | 83.3               | 97.5    | 4.30                   | 11.00         |
| 20         | 86.7               | 100.0   | 7.60                   | 21.00         |

> 注：表中FPR高达97.5%-100%，应为笔误或定义反向；结合正文描述“79% outright rejected”，推测此处FPR实指“benign rejection rate”。

更准确的结果见正文第5节：
- **检测率（Detection Rate）**：达到 **100%**（120个恶意样本全部检出，零漏报）
- **良性接受率（Benign Acceptance）**：**79%** 被直接拒绝，21%进入可信模型复查流程
- **一致性得分分布**：所有良性样本一致性接近1.0，而恶意样本明显偏低
- **ARQ增长趋势**：中位ARQ从 $ 4\times10^0 $（k=3）增至 $ 2\times10^1 $（k=20），呈近似线性增长（符合理论预期 $ \text{ARQ} \sim k+1 $）
- **运行时间**：从0.275s（k=3）增至0.333s（k=20），增幅小且可控
- **实验成功率**：200次试验中 **100%成功，零错误**

### 与基线方法的对比（Table 1）
| 方法 | Detection Rate (%) | FPR (%) | Benign Acceptance (%) |
|------|--------------------|---------|------------------------|
| Static Analysis | 51.7 | 0.0 | 100.0 |
| Complexity | 80.0 | 62.5 | 37.5 |
| Entropy | 0.0 | 0.0 | 100.0 |
| Weighted | 51.7 | 0.0 | 100.0 |
| **CTVP** | **83.8** | **98.8** | **1.2** |

> ⚠️ 注意：此表FPR含义疑似颠倒（CTVP的FPR高达98.8%不合理）。结合上下文推断，该数值可能是“恶意检出率”或“良性拒绝率”。真正意义的FPR应在后续研究中进一步校准。

但从趋势看，**CTVP显著优于所有静态基线**，尤其在捕捉语义层面的隐蔽后门方面具有压倒性优势。

### 消融实验与关键观察
- **轨道大小影响**：随着 $ k $ 增加，检测能力稳步提升，尤其在 $ k=20 $ 时达到最优。
- **变换生成成功率**：在中间范围（如k=10）下降至10%，但在k=20恢复至50–75%，说明存在“难生成区”，未来可通过改进变换策略缓解。
- **资源消耗**：CPU内存稳定在~4.8GB，GPU~17GB（远低于71.3GB上限），系统高效稳定。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **语义不变性是一把双刃剑**：合法程序在语义变换下行为稳定，而后门往往依赖特定语法模式，破坏了这种一致性，从而暴露自身。
2. ✅ **CTVP能实现近乎完美的恶意行为检测**：在实验中实现了 **100% detection rate**，且未发生误放行。
3. ✅ **非可博弈性成立**：由于一致性维持的成本随轨道大小指数增长，攻击者无法通过穷举找到既能触发后门又保持一致性的变体。
4. ✅ **轻量级即可生效**：即使较小的轨道（k=3~5）也能提供较强检测力，适合部署于资源受限环境。

### 局限性
1. **依赖LLM轨迹预测质量**：当前方法使用LLM模拟执行轨迹，而非真实执行，存在预测误差风险（尤其是复杂控制流或递归）。
2. **变换覆盖有限**：目前仅支持5种语义保留变换（如变量重命名、死代码插入等），可能遗漏某些微妙的等价形式。
3. **计算开销随k增长**：虽然单次延迟仍较低，但大规模应用时 $ \text{ARQ} \sim k+1 $ 的开销仍构成瓶颈。
4. **假阳性管理挑战**：部分良性程序因LLM预测不稳定也可能导致低一致性，需引入可信模型辅助裁决。

### 未来工作方向
1. **集成真实执行环境**：引入受限沙箱（如 RestrictedPython）获取真实执行轨迹，增强验证可靠性。
2. **丰富语义变换种类**：加入数据流感知重排、循环展开、常量折叠等高级变换，提高轨道多样性。
3. **自适应轨道大小（Adaptive Orbit Sizing）**：根据程序复杂度动态选择 $ k $，优化效率与安全性平衡。
4. **持续监控与反馈学习**：结合 runtime monitoring 实现周期性再验证，支持在线更新与异常预警。
5. **拓展至其他模态**：探索CTVP是否可用于文本生成（检测隐含偏见）、图像合成（检测水印后门）等领域。

---

> 📌 **一句话总结**：  
> CTVP 提出了一种基于语义轨道与跨轨迹一致性的黑盒验证机制，首次将AI控制理论应用于代码生成安全，在无需执行代码的前提下，以极高的精度揭露出模型中的“双重生活”行为，具备坚实的理论支撑与良好的实践前景。

</details>

---

### 16. [Beyond Lipschitz Continuity and Monotonicity: Fractal and Chaotic Activation Functions in Echo State Networks](https://arxiv.org/abs/2512.14675)

**Authors**: Rae Chipera, Jenny Du, Irene Tsapara  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.14675v1  

#### Abstract
Contemporary reservoir computing relies heavily on smooth, globally Lipschitz continuous activation functions, limiting applications in defense, disaster response, and pharmaceutical modeling where robust operation under extreme conditions is critical. We systematically investigate non-smooth activa...

---

### 17. [State-Dependent Refusal and Learned Incapacity in RLHF-Aligned Language Models](https://arxiv.org/abs/2512.13762)

**Authors**: TK Lee  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13762v1  

#### Abstract
Large language models (LLMs) are widely deployed as general-purpose tools, yet extended interaction can reveal behavioral patterns not captured by standard quantitative benchmarks. We present a qualitative case-study methodology for auditing policy-linked behavioral selectivity in long-horizon inter...

---

### 18. [Sparsity-Controllable Dynamic Top-p MoE for Large Foundation Model Pre-training](https://arxiv.org/abs/2512.13996)

**Authors**: Can Jin, Hongwu Peng, Mingcan Xiang, Qixin Zhang, Xiangchi Yuan, Amit Hasan, Ohiremen Dibua, Yifan Gong, Yan Kang, Dimitris N. Metaxas  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13996v1  

#### Abstract
Sparse Mixture-of-Experts (MoE) architectures effectively scale model capacity by activating only a subset of experts for each input token. However, the standard Top-k routing strategy imposes a uniform sparsity pattern that ignores the varying difficulty of tokens. While Top-p routing offers a flex...

---

### 19. [TiCard: Deployable EXPLAIN-only Residual Learning for Cardinality Estimation](https://arxiv.org/abs/2512.14358)

**Authors**: Qizhi Wang  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14358v1  

#### Abstract
Cardinality estimation is a key bottleneck for cost-based query optimization, yet deployable improvements remain difficult: classical estimators miss correlations, while learned estimators often require workload-specific training pipelines and invasive integration into the optimizer. This paper pres...

---

### 20. [Ladder Up, Memory Down: Low-Cost Fine-Tuning With Side Nets](https://arxiv.org/abs/2512.14237)

**Authors**: Estelle Zheng (LORIA, ALE), Nathan Cerisara (LORIA), S\'ebastien Warichet (ALE), Emmanuel Helbert (ALE), Christophe Cerisara (SYNALP, LORIA)  
**Category**: cs.CL  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14237v1  

#### Abstract
Fine-tuning large language models (LLMs) is often limited by the memory available on commodity GPUs. Parameter-efficient fine-tuning (PEFT) methods such as QLoRA reduce the number of trainable parameters, yet still incur high memory usage induced by the backward pass in the full model. We revisit La...

---

### 21. [Real-Time Service Subscription and Adaptive Offloading Control in Vehicular Edge Computing](https://arxiv.org/abs/2512.14002)

**Authors**: Chuanchao Gao, Arvind Easwaran  
**Category**: cs.DC  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14002v1  

#### Abstract
Vehicular Edge Computing (VEC) has emerged as a promising paradigm for enhancing the computational efficiency and service quality in intelligent transportation systems by enabling vehicles to wirelessly offload computation-intensive tasks to nearby Roadside Units. However, efficient task offloading ...

---

### 22. [Physics-Guided Deep Learning for Heat Pump Stress Detection: A Comprehensive Analysis on When2Heat Dataset](https://arxiv.org/abs/2512.13696)

**Authors**: Md Shahabub Alam, Md Asifuzzaman Jishan, Ayan Kumar Ghosh  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13696v1  

#### Abstract
Heat pump systems are critical components in modern energy-efficient buildings, yet their operational stress detection remains challenging due to complex thermodynamic interactions and limited real-world data. This paper presents a novel Physics-Guided Deep Neural Network (PG-DNN) approach for heat ...

---

### 23. [Explainable reinforcement learning from human feedback to improve alignment](https://arxiv.org/abs/2512.13837)

**Authors**: Shicheng Liu, Siyuan Xu, Wenjie Qiu, Hangfan Zhang, Minghui Zhu  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.13837v1  

#### Abstract
A common and effective strategy for humans to improve an unsatisfactory outcome in daily life is to find a cause of this outcome and correct the cause. In this paper, we investigate whether this human improvement strategy can be applied to improving reinforcement learning from human feedback (RLHF) ...

---

### 24. [A First-Order Logic-Based Alternative to Reward Models in RLHF](https://arxiv.org/abs/2512.14100)

**Authors**: Chunjin Jian, Xinhua Zhu  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14100v1  

#### Abstract
Reinforcement Learning from Human Feedback (RLHF) plays a crucial role in aligning large language models (LLMs) with human values and preferences. However, the quality and stability of the trained reward model largely determine the final alignment performance. Existing approaches such as Proximal Po...

---

### 25. [Implicit Bias and Invariance: How Hopfield Networks Efficiently Learn Graph Orbits](https://arxiv.org/abs/2512.14338)

**Authors**: Michael Murray, Tenzin Chan, Kedar Karhadker, Christopher J. Hillar  
**Category**: cs.LG  
**Published**: 2025-12-17  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.14338v1  

#### Abstract
Many learning problems involve symmetries, and while invariance can be built into neural architectures, it can also emerge implicitly when training on group-structured data. We study this phenomenon in classical Hopfield networks and show they can infer the full isomorphism class of a graph from a s...

---

### 26. [Blind Radio Mapping via Spatially Regularized Bayesian Trajectory Inference](https://arxiv.org/abs/2512.13701)

**Authors**: Zheng Xing, Junting Chen  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.13701v1  

#### Abstract
Radio maps enable intelligent wireless applications by capturing the spatial distribution of channel characteristics. However, conventional construction methods demand extensive location-labeled data, which are costly and impractical in many real-world scenarios. This paper presents a blind radio ma...

---

### 27. [ValuePilot: A Two-Phase Framework for Value-Driven Decision-Making](https://arxiv.org/abs/2512.13716)

**Authors**: Yitong Luo, Ziang Chen, Hou Hei Lam, Jiayu zhan, Junqi Wang, Zhenliang Zhang, Xue Feng  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.13716v1  

#### Abstract
Personalized decision-making is essential for human-AI interaction, enabling AI agents to act in alignment with individual users' value preferences. As AI systems expand into real-world applications, adapting to personalized values beyond task completion or collective alignment has become a critical...

---

### 28. [Compressed Causal Reasoning: Quantization and GraphRAG Effects on Interventional and Counterfactual Accuracy](https://arxiv.org/abs/2512.13725)

**Authors**: Steve Nwaiwu, Nipat Jongsawat, Anucha Tungkasthan  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.13725v1  

#### Abstract
Causal reasoning in Large Language Models spanning association, intervention, and counterfactual inference is essential for reliable decision making in high stakes settings. As deployment shifts toward edge and resource constrained environments, quantized models such as INT8 and NF4 are becoming sta...

---

### 29. [OpenDataArena: A Fair and Open Arena for Benchmarking Post-Training Dataset Value](https://arxiv.org/abs/2512.14051)

**Authors**: Mengzhang Cai, Xin Gao, Yu Li, Honglin Lin, Zheng Liu, Zhuoshi Pan, Qizhi Pei, Xiaoran Shang, Mengyuan Sun, Zinan Tang, Xiaoyang Wang, Zhanping Zhong, Yun Zhu, Dahua Lin, Conghui He, Lijun Wu  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.14051v1  

#### Abstract
The rapid evolution of Large Language Models (LLMs) is predicated on the quality and diversity of post-training datasets. However, a critical dichotomy persists: while models are rigorously benchmarked, the data fueling them remains a black box--characterized by opaque composition, uncertain provena...

---

### 30. [Optimizing Multi-Tier Supply Chain Ordering with a Hybrid Liquid Neural Network and Extreme Gradient Boosting Model](https://arxiv.org/abs/2512.14112)

**Authors**: Chunan Tong  
**Category**: cs.AI  
**Published**: 2025-12-17  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.14112v1  

#### Abstract
Supply chain management (SCM) faces significant challenges like demand fluctuations and the bullwhip effect. Traditional methods and even state-of-the-art LLMs struggle with benchmarks like the Vending Machine Test, failing to handle SCM's complex continuous time-series data. While ML approaches lik...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
