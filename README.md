# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-30 06:18:23 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Causal Autoregressive Diffusion Language Model](https://arxiv.org/abs/2601.22031)

**Authors**: Junhao Ruan, Bei Li, Yongjing Yin, Pengcheng Huang, Xin Chen, Jingang Wang, Xunliang Cai, Tong Xiao, JingBo Zhu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.22031v1  

#### Abstract
In this work, we propose Causal Autoregressive Diffusion (CARD), a novel framework that unifies the training efficiency of ARMs with the high-throughput inference of diffusion models. CARD reformulates the diffusion process within a strictly causal attention mask, enabling dense, per-token supervisi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Causal Autoregressive Diffusion Language Model

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前主流的 **Autoregressive Models (ARMs)** 虽然训练稳定、数据效率高，但其**自回归生成方式导致推理速度慢**，难以满足低延迟应用需求。而现有的 **Text Diffusion Models**（如 MDLM 和 BD3LM）虽然支持并行推理，但在实际部署中面临以下瓶颈：
- **MDLM** 使用双向注意力（Full Attention），无法利用 **KV Cache**，推理效率低下；
- **BD3LM** 引入块级扩散机制，虽恢复部分因果结构，但带来显著的**训练开销**（复杂掩码、输入复制）和**固定块大小限制动态并行性**。

### 提出了什么新方法或新思路
本文提出 **CARD (Causal Autoregressive Diffusion)** 框架，将离散扩散过程重构在严格的**因果注意力架构**下，实现训练效率与推理速度的统一。其核心创新包括：

- **Shifted Causal Attention Mechanism**  
  在训练时，每个位置 $x_n$ 预测原始 token，条件为其前面被噪声污染的历史 $x_{<n}'$。通过单次前向传播即可获得全序列密集监督信号，避免了块级向量化带来的计算冗余。

- **Soft Tail Masking**  
  将噪声集中于序列尾部的一个动态窗口内（而非均匀或严格尾部），既保留了足够的干净历史上下文以保证早期 token 可预测性，又维持局部邻近信息用于语言建模。

- **Context-aware Reweighting**  
  设计基于上下文模糊度的损失重加权机制，从三个维度评估上下文质量：
  - **Quantity**：历史中被掩码的数量
  - **Distance**：最近掩码的距离（指数衰减）
  - **Density**：连续掩码段落长度  
  综合为局部模糊得分 $S_{\text{focal}}$，用于动态降低高熵上下文下的损失权重，提升优化稳定性。

- **Confidence-based Block Inference**  
  支持动态并行解码：根据置信度阈值决定每次迭代更新多少个 [MASK] token，并结合 KV Cache 实现任意长度输出的高效生成。

### 相比现有方法的优势
| 特性 | ARM | MDLM | BD3LM | CARD (Ours) |
|------|-----|-------|--------|-------------|
| 因果结构 | ✅ | ❌ | ⚠️（块间因果） | ✅ |
| KV Cache 支持 | ✅ | ❌ | ❌ | ✅ |
| 并行推理能力 | ❌ | ✅（多步迭代） | ✅（块内并行） | ✅✅（动态多 token/step） |
| 训练效率 | ✅ | ⚠️（1.5× 开销） | ❌（3× 开销） | ✅（与 ARM 相当） |
| 数据利用率 | ✅ | ⚠️（~50%） | ⚠️（重复输入） | ✅（接近 100%） |

> ✅ CARD 成功融合了 ARMs 的训练高效性和扩散模型的并行推理优势，同时解决了传统扩散方法中的信息坍塌（Information Collapse）和优化不稳定问题。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **预训练数据**：FineWeb 的子集，共 **300B tokens**
- **微调与评估任务**：
  - 学科知识理解：ARC-Challenge/Easy, MMLU-redux, SciQ
  - 常识推理：CommonsenseQA, HellaSwag, PIQA
  - 上下文消歧：Winogrande
  - 通用语言建模能力：AG News, arXiv, LAMBADA, LM1B, OpenWebText, PTB, PubMed, WikiText（零样本 PPL 测试）

### 实验设置和评估指标
- **模型规模**：统一为 **1B 参数量级**
- **训练配置**：
  - 序列长度：128
  - 优化器：AdamW + Cosine LR Scheduler（峰值学习率 3e-4）
  - 精度：bfloat16 混合精度
  - 注意力实现：Flash Attention 2
- **评估指标**：
  - 下游任务准确率（Zero-shot）
  - 零样本困惑度（Zero-shot PPL）
  - 推理吞吐量（tokens/sec）
  - GenPPL（生成文本质量评估）

### 基线方法对比
- **Autoregressive Models (ARM)**：标准 GPT 架构
- **MDLM**：简化掩码离散扩散模型（Simplified Masked Discrete Diffusion）
- **BD3LM**：块级扩散语言模型（Block Diffusion）

所有基线均采用最先进的工程优化（如 Flex Attention、torch.compile），确保公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表 1：下游任务平均准确率对比（1B 模型，300B tokens）
| Model | AVG Accuracy |
|-------|--------------|
| ARM | 56.39 |
| BD3LM | 47.45 |
| MDLM | 47.55 |
| **CARD (Ours)** | **53.23** |

> CARD 相比现有扩散基线提升 **+5.7% 绝对增益**，接近 ARM 性能。

#### 表 2：零样本困惑度（PPL）对比（越低越好）
| Model | AVG PPL |
|-------|---------|
| ARM | 38.68 |
| BD3LM | 49.78 |
| MDLM | 49.84 |
| **CARD (Ours)** | **34.40** |

> CARD 在 **8 个领域中有 6 个优于 ARM**，尤其在长程依赖任务（如 LAMBADA）表现更优。

#### 表 3：小模型复现实验（110M 参数，33B tokens，使用 EMA）
| Model | PPL |
|-------|-----|
| ARM | 21.12 |
| BD3LM | 35.06 |
| MDLM | 37.48 |
| **CARD (Ours)** | **21.54** |

> 即使在强平滑（EMA）条件下，CARD 仍能逼近 ARM 的密度估计能力，远超其他扩散模型。

#### 表 5：推理加速效果（GenPPL 分析）
| Decoding Config | Throughput (tok/s) | Speedup | AVG PPL ↓ |
|----------------|--------------------|--------|----------|
| ARM (Baseline) | 3,771 | 1.00× | 11.19 |
| CARD (Block=16, Steps=16) | 6,441 | 1.71× | 12.65 |
| CARD (Block=32, Steps=8) | 15,064 | **4.01×** | 18.38 |

> CARD 实现 **最高达 4× 推理加速**，在适度设置下仅轻微牺牲质量（PPL +1.5），具备实用价值。

### 消融实验结果（Table 4）

| 设置 | AVG Accuracy |
|------|--------------|
| CARD (Ours) | 53.21 |
| w/o Relaxed Window (Strict Tail) | 52.50 |
| w/o Tail Preference (Random) | 51.62 |
| w/o Context-aware Weighting | 51.66 |

> 结果表明：
- **Tail-biased noise 分布至关重要**，随机掩码严重损害性能；
- **Soft Tail Window** 明显优于严格尾部掩码，说明混合状态有助于局部建模；
- **Context-aware Reweighting** 对稳定训练有显著作用，移除后下降约 1.55 pts。

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **CARD 成功弥合了 ARMs 与扩散模型之间的鸿沟**：在保持 ARM 级训练效率的同时，实现了高达 4× 的并行推理加速。
2. ✅ **因果扩散是可行且高效的**：通过 Soft Tail Masking 和 Context-aware Reweighting，有效缓解了“信息坍塌”问题，使纯因果结构下的扩散训练变得稳定。
3. ✅ **更高的数据潜力（Data Potential）**：在多轮训练场景下，CARD 不像 ARM 那样快速饱和，而是持续从重复数据中提取信号，在数据受限场景更具优势（见 Figure 4）。
4. ✅ **更强的语言建模泛化能力**：由于训练目标要求从损坏上下文中重建 token，促使模型捕捉全局结构而非局部统计模式，因此在长程依赖任务上超越 ARM。

### 方法的局限性
- **极端加速设置可能导致退化行为**：当 block size 过大且 step limit 过小时，模型易陷入重复循环（如“applying the gel...”反复出现），需谨慎调节推理参数。
- **当前实现仍依赖经验性超参设计**：如 tail factor $\lambda$、decay $p$、threshold $\tau$ 等尚无理论最优选择。
- **未探索更大规模下的扩展规律**：目前实验集中在 1B 模型，尚未验证在 10B+ 规模的表现。

### 未来工作方向
- 探索 **可学习的噪声调度策略**（adaptive noise placement）
- 引入 **课程学习机制** 控制训练难度曲线
- 扩展至 **多模态生成任务**（如图文生成）
- 研究 **更鲁棒的 confidence scoring 函数** 以进一步提升并行解码质量
- 探索 **完全免训练加速方案**（training-free acceleration）与 CARD 的结合可能性

---

> 🔚 **总结一句话**：  
> **CARD 是首个真正意义上将因果自回归训练效率与扩散模型并行推理优势统一起来的框架，在性能、效率与数据利用之间达到了新的平衡，为下一代高效 LLM 提供了一条坚实路径。**

</details>

---

### 2. [Hybrid Linear Attention Done Right: Efficient Distillation and Effective Architectures for Extremely Long Contexts](https://arxiv.org/abs/2601.22156)

**Authors**: Yingfa Chen, Zhen Leng Thai, Zihan Zhou, Zhu Zhang, Xingyu Shen, Shuo Wang, Chaojun Xiao, Xu Han, Zhiyuan Liu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.22156v1  

#### Abstract
Hybrid Transformer architectures, which combine softmax attention blocks and recurrent neural networks (RNNs), have shown a desirable performance-throughput tradeoff for long-context modeling, but their adoption and studies are hindered by the prohibitive cost of large-scale pre-training from scratc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Hybrid Linear Attention Done Right**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前主流的 **Transformer** 模型依赖 **softmax attention**，其计算复杂度为 $O(T^2)$，在处理 **长上下文（long-context）** 时效率极低。虽然 **RNN-attention hybrid models**（如 Mamba、Jamba）通过混合架构实现了线性复杂度，在长序列上推理更快，但它们通常需要从头大规模预训练，成本高昂，限制了学术界的研究。

此外，现有的将 **Transformer 蒸馏为 hybrid 模型** 的方法存在两大缺陷：
1. **数据需求巨大**：多数方法需数十亿甚至数百亿 tokens 的训练数据（如 Jet-Nemotron 需 400B tokens）。
2. **长上下文性能差**：蒸馏后的模型在短上下文任务上表现尚可，但在长上下文（如 >128K）的 **recall-intensive 任务** 上性能严重下降。

---

### **提出的新方法与创新点**

本文提出了 **HALO**（Hybrid Attention via Layer Optimization）和 **HypeNet**，系统性地解决了上述问题。

#### **(1) HALO：高效跨架构蒸馏流程**
- **目标**：将已有的 **Transformer 模型**（如 Qwen3）高效蒸馏为 **RNN-attention hybrid 模型**。
- **核心步骤**：
  1. **Attention Weight Transfer**：将每个 attention 层的权重初始化为对应的 RNN 层。
  2. **Hidden State Alignment**：独立训练每个 RNN 层，使其输出对齐原 attention 层。
  3. **Attention Layer Selection**：提出基于 **recall vs. CSR 性能变化** 的重要性评分机制，选择最关键的 attention 层保留。
  4. **Knowledge Distillation**：端到端蒸馏，以原始 Transformer 为 teacher，hybrid 模型为 student。
  5. **Finetuning**：在更长上下文上微调以优化长程能力。

> ✅ **优势**：仅需 **2.3B tokens** 即可完成转换，远低于现有方法（如 KL-LS 需 25B，Jet-Nemotron 需 400B）。

#### **(2) HypeNet：高性能 hybrid 架构**
- **核心创新**：
  - **HyPE**（Hybrid Positional Encoding）：
    - **RNN 层用 RoPE**（注入丰富位置信息）
    - **Attention 层用 NoPE**（提升长度外推能力）
    - 结合两者优势，实现更强的 **length generalization**。
  - **Attention Logits Scaling**：引入位置相关的缩放因子 $s_t = \log(t + a)$，缓解注意力熵随长度增长的问题。
  - **其他架构改进**：
    - **QK-Normalization** 在 RNN 层中加入。
    - **GQA to MHA**：解除 RNN 层中 K/V 共享，增强表达力。
    - **Output Gate**：在 RNN 和 attention 层均加入门控机制，提升建模能力。

> ✅ **优势**：HypeNet 在长上下文任务上显著优于现有 hybrid 模型。

---

### **相比现有方法的优势**

| 方法 | 所需 Tokens | 是否开源数据 | 长上下文性能 | 效率 |
|------|-------------|---------------|----------------|--------|
| Jet-Nemotron (Gu et al., 2025) | 400B | 否 | 差（NIAH@256K ≈ 0%） | 中等 |
| KL-LS (Li et al., 2025) | 25B | 否 | 中等（NIAH@256K ≈ 44.3%） | 中等 |
| **HALO + HypeNet (Ours)** | **2.3B** | **是（FineWeb-edu）** | **优（NIAH@256K ≈ 74.3%）** | **高（3.4× prefill speedup）** |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **训练数据**：`FineWeb-edu`（高质量互联网文本子集），从中随机采样：
  - Stage 1: 320M tokens
  - Stage 2: 1B tokens
  - Stage 3: 1B tokens（长上下文微调）
  - **总计：2.3B tokens**
- **层选择评估数据**：用于计算 attention 层重要性的任务集合：
  - **CSR**：HellaSwag, ARC-Easy, ARC-Challenge
  - **Recall**：SQuAD, FDA, SWDE

### **实验设置**
- **基础模型**：Qwen3-1.7B, Qwen3-4B, Qwen3-8B
- **转换后模型**：HypeNet-2B, HypeNet-5B, HypeNet-9B（参数略增 ~10%）
- **RNN Mixer**：默认使用 **Lightning Attention**，也测试了 Mamba2、GLA、GDN、RWKV-7
- **硬件**：单张 NVIDIA A800 GPU，BFloat16 精度
- **Batch Size**：1（公平比较吞吐量）

### **评估指标**
- **Commonsense Reasoning (CSR)**：
  - MMLU, HellaSwag, ARC-Easy/Challenge, WinoGrande, PIQA, LAMBADA
  - 报告 **normalized accuracy**
- **长上下文召回能力**：
  - **Needle-in-a-Haystack (NIAH)**：在超长文本中定位关键信息，测试长度从 4K 到 1M。
- **效率指标**：
  - **Throughput (tokens/s)**：解码速度
  - **Time per Output Token (TPOT)**：生成延迟
  - **Prefill Time**：处理长输入的时间

### **基线方法对比**
- **Teacher Model**：Qwen3（原生 Transformer）
- **SOTA 蒸馏方法**：
  - Jet-Nemotron (Gu et al., 2025)
  - KL-LS (Li et al., 2025)
  - RADLADS (Goldstein et al., 2025)
- **其他 baseline**：
  - 均匀分布 attention 层
  - 后半部分集中 attention 层

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（Qwen3-1.7B → HypeNet-2B）**

#### **表：NIAH 长上下文召回性能对比（最高值加粗）**

| Model | NIAH @ 128K | NIAH @ 256K |
|-------|------------|------------|
| Qwen3 (teacher) | 96.4 | 17.0 |
| Jet-Nemotron | 14.8 | 0.0 |
| KL-LS (GDN) | 24.8 | 11.0 |
| **HypeNet + HALO (ours)** | **79.9** | **74.3** |

> 📌 **结论**：HypeNet 在 256K 上仍保持 **74.3% 准确率**，而 Qwen3 仅为 17%，其他蒸馏方法几乎失效。

#### **效率对比（图1 & 图6）**
- **内存占用更低**：HypeNet 在 1M context 下仍可运行，Qwen3 内存溢出。
- **吞吐量更高**：
  - **解码速度提升 up to 3.0×**
  - **prefill 速度提升 up to 3.4×**（512K context）
- **TPOT 更低**：随着 context 增长，HypeNet 增长缓慢，Qwen3 增长剧烈。

---

### **消融实验结果**

#### **(1) HypeNet 架构消融（Table 3）**
移除任一组件均导致性能下降，尤其：
- 移除 **RNN RoPE**（即不用 HyPE）：NIAH@128K 从 79.9 → 47.9
- 移除 **RNN QK-Norm**：NIAH@128K → 17.3
- 移除 **RNN Output Gate**：NIAH@128K → 74.5

> ✅ **验证了 HyPE 和各模块的有效性**。

#### **(2) Layer Selection 方法对比（Table 4）**
| Method | CSR Score | NIAH @ 128K |
|--------|-----------|-------------|
| Evenly distribute | 54.0 | 61.9 |
| Latter-half only | 55.8 | 39.2 |
| KL-LS | 55.3 | 58.3 |
| **HALO (ours)** | **55.9** | **79.9** |

> ✅ **证明所提 selection 方法最优**。

#### **(3) 不同 RNN Mixer 对比（Figure 5）**
- **Lightning Attention** 表现最佳，优于 Mamba2、GLA、GDN。
- 可能原因：其 **data-independent forget gate** 更利于长度泛化。

---

## **4. 关键结论和发现**

### **主要发现**
1. **HALO 是目前最高效的 Transformer-to-Hybrid 蒸馏方法**，仅需 **2.3B tokens** 即可完成转换。
2. **HyPE 是关键创新**：RNN 用 RoPE + Attention 用 NoPE 的组合显著提升 **length generalization**。
3. **Lightning Attention 是最适合 HypeNet 的 RNN mixer**，简单且高效。
4. **HypeNet 在长上下文场景下兼具高性能与高效率**：
   - 性能接近原生 Transformer
   - 推理速度显著更快
   - 内存占用更低

---

### **局限性**
1. **指令跟随能力可能退化**：由于转换过程未使用 instruction tuning 数据，模型的对齐行为可能不如原模型。
2. **仅适用于 Transformer 架构**：当前流程针对 Transformer 设计，难以直接迁移到其他架构。
3. **不支持 Short Convolution**：实验发现添加 short conv 层无益甚至有害，限制了某些 RNN 变体的应用。

---

### **未来工作方向**
1. **恢复对齐能力**：探索如何在蒸馏后高效恢复 instruction-following 和 safety behavior。
2. **扩展至多模态模型**：将 HALO 应用于 vision-language 模型。
3. **自动化 layer selection**：设计更智能的算法动态决定每层是否使用 attention。
4. **探索更优的 RNN mixer**：结合 state space models 与 linear attention 的优势。

---

> 🔗 **代码与模型**：https://github.com/THUNLP/hybrid-linear-attention

</details>

---

### 3. [Amortized Spectral Kernel Discovery via Prior-Data Fitted Network](https://arxiv.org/abs/2601.21731)

**Authors**: Kaustubh Sharma, Srijan Tiwari, Ojasva Nema, Parikshit Pareek  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.21731v1  

#### Abstract
Prior-Data Fitted Networks (PFNs) enable efficient amortized inference but lack transparent access to their learned priors and kernels. This opacity hinders their use in downstream tasks, such as surrogate-based optimization, that require explicit covariance models. We introduce an interpretability-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Amortized Spectral Kernel Discovery via Prior-Data Fitted Network**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
Prior-Data Fitted Networks (PFNs) 虽然在**amortized Bayesian inference**（摊销贝叶斯推断）中表现出色，能够通过单次前向传播实现快速预测，但其内部学到的先验（如 spectral density 和 covariance kernel）是隐式的、不可解释的。这限制了它们在需要显式协方差模型的任务中的应用，例如 surrogate-based optimization 或科学建模。

本文旨在解决以下两个核心问题：
- **机制透明性缺失**：PFNs 如何编码频谱信息？这些信息是否可被提取？
- **缺乏显式可迁移表示**：能否从预训练 PFN 中解码出显式的 spectral density 和 stationary kernel？

---

### **提出了什么新方法或新思路**
作者提出了一种**基于机制分析的解码框架**，用于从预训练的 PFN 中**摊销地（amortized）发现频谱结构**，并构建显式的 stationary kernel。核心思想如下：

1. **机制性洞察（Mechanistic Insight）**  
   在采用 **Decoupled-Value Attention (DVA)** 架构的 PFN 中，注意力输出 $ H $ 编码了输入函数的频谱结构，而值编码 $ V $ 主要携带信号幅度信息。通过 t-SNE 可视化发现，$ H $ 形成了一个与频率强相关的平滑流形（manifold），相关系数达 0.864。

2. **Filter Bank Decoder 架构**  
   设计了一个双路径解码器，将冻结的 PFN 输出映射为显式的 spectral density：
   - 利用 **Multi-Query Attention (MQA) pooling** 提取 $ H $ 和 $ V $ 的多视角摘要。
   - 采用“滤波器组”（filter bank）形式，在离散频率域上预测：
     - 频率 bin 是否激活（Bin Classifier）
     - 中心频率偏移与带宽（Offset-Bandwidth Regressor）
     - 多 realization 下的相对权重（Weight Regressor）

3. **Bochner’s Theorem 显式核重建**  
   将解码得到的 spectral density 通过傅里叶逆变换（Bochner 定理）转换为 stationary kernel：
   $$
   k(\tau) = \sum_b [\mathbb{I}(p_b > \gamma)] w_b \exp(-2\pi^2 \sigma_b^2 \tau^2) \cos(2\pi \mu_b \tau)
   $$

4. **Analytical Scaling 解决尺度模糊**  
   在 single-realization 场景下，由于归一化导致全局尺度不可识别，引入解析估计器：
   $$
   \hat{\alpha} = \|f\|^2 / \text{tr}(K)
   $$
   来恢复能量一致性，避免网络“幻觉”尺度。

---

### **相比现有方法的优势**
| 方法 | 是否需迭代优化 | 是否输出显式 kernel | 推理速度 | 可解释性 |
|------|----------------|--------------------|----------|----------|
| **DKL / RFF** | 是（每任务优化） | 否（隐式） | 慢（秒级） | 低 |
| **Automatic Statistician** | 是（组合搜索） | 是 | 极慢 | 高 |
| **PFN（原生）** | 否 | 否 | 快（~10⁻³ 秒） | 无 |
| **本文方法（Decoder + PFN）** | **否** | **是** | **快（~10⁻³ 秒）** | **高** |

> ✅ **优势总结**：
> - **零样本（zero-shot）频谱推断**：无需测试时优化。
> - **显式可解释 kernel**：支持下游任务（如优化、控制）。
> - **推理加速三个数量级**：相较 DKL/RFF 从秒级降至毫秒级。
> - **理论保证**：multi-realization 下 spectral weights 可识别；single-realization 下 shape 可恢复。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
全部为**合成数据集**，由 GP 采样生成，涵盖多种 kernel 类型：

- **Spectral Mixture (SM)**：含 1–4 个高斯分量，中心频率 $ \mu \sim U[0.5, 3.0] $，带宽 $ \sigma \sim U[0.01, 0.05] $
- **标准 kernel family**：RBF、Periodic、RBF+Periodic、RBF×Periodic
- **高维扩展**：5D 和 10D additive kernels（仅少数维度活跃）
- **out-of-distribution**：Matérn kernels（heavy-tailed spectral density）

---

### **实验设置和评估指标**

#### **评估场景**
| 场景 | 输入 | 输出目标 |
|------|------|-----------|
| **Multi-Realization** | $ M=16 $ 个独立函数样本 | 完整 spectral density（频率、带宽、权重） |
| **Single-Realization** | 单个函数样本 | 频率与带宽（权重设为均匀）+ 解析缩放 |

#### **评估指标**
- **Spectral Recovery**：
  - Wasserstein 距离（频谱密度匹配度）
- **Kernel Quality**：
  - GP 回归 MSE（使用解码 kernel 作为 covariance）
  - 与 Oracle GP、PFN、DKL、RFF 对比
- **效率**：
  - 推理时间（seconds）

#### **训练策略**
- **Curriculum Learning**：从简单（1 分量）到复杂（4 分量）逐步训练
- **损失函数**：
  - Bin Classification：Binary Cross-Entropy（正样本加权）
  - Parameter Regression：Masked MSE（仅对激活 bin 计算）

---

### **基线方法对比**
| 基线方法 | 描述 |
|---------|------|
| **Oracle GP** | 使用真实生成 kernel 的 GP 回归（理想上限） |
| **PFN** | 原始 PFN 模型直接预测（黑盒） |
| **DKL (Deep Kernel Learning)** | 神经网络 + base kernel，需 marginal likelihood 优化 |
| **RFF (Random Fourier Features)** | 固定频谱采样，需优化特征权重 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **Multi-Realization Setting**
- **图3**：随着函数样本数 $ M $ 增加，Wasserstein 距离单调下降，验证了 Theorem 4.3 的 identifiability。
- **图4 & 表6**：Decoder 成功恢复多峰 spectral mixture，即使在重叠频率下也能准确定位峰值。
- **表6**：在 in-distribution（SM）任务上，Decoder MSE 与 Oracle GP 同阶（~10⁻⁴），显著优于 DKL/RFF。

#### **Single-Realization Setting（Kernel Cookbook）**
| True Kernel | Decoder MSE | PFN MSE | DKL MSE | RFF MSE |
|------------|-------------|--------|--------|--------|
| RBF        | 2.52e-4     | 6.56e-5 | 2.52e-4 | 4.33e-2 |
| Periodic   | 2.78e-2     | 1.01e-2 | 3.80e-2 | 5.51e-2 |
| SM(Q=4)    | 2.54e-3     | 2.46e-4 | 2.70e-2 | 5.72e-2 |

> 🔹 Decoder 性能接近 PFN，且显著优于 DKL 和 RFF，尤其在复杂 kernel 上。
> 🔹 所有任务推理时间：< 0.006 秒 vs. DKL/RFF 的 ~2 秒 → **~1000× 加速**

#### **高维扩展（5D & 10D）**
| Kernel (Additive) | Oracle GP MSE | Amortized MSE |
|-------------------|---------------|----------------|
| RBF(5D)           | 1.3e-4        | 2.9e-4         |
| SM(Q=4,10D)       | 2.06e-4       | 4.56e-4        |
| Periodic(10D)     | 2.71e-4       | 2.32e-1        |

> 🔹 在 additive kernels 上表现良好，误差同阶。
> 🔹 Periodic kernel 挑战较大（需全局结构建模），误差高出约 3 个数量级。

---

### **消融实验结果**

#### **Pooling 方式对比（Table 4, Figure 9）**
| Pooling Method | Easy (1 param) | Hard (4 params) | Very Hard (6 params) |
|----------------|----------------|------------------|------------------------|
| Mean Pooling   | 0.999          | 0.560            | 0.333                  |
| Attention Pooling | 1.000        | 0.610            | 0.404                  |

> ✅ Attention pooling 在复杂任务中带来 +3.8% ~ +5.8% 提升，证明其对多成分分离的有效性。

#### **Representation 探测（Table 3, Figure 7–8）**
- $ H $（latent output）在频率探测上 R² 达 0.998，远超 $ V $（0.21）
- $ H+V $ 联合使用进一步提升权重预测能力
- 验证了 $ H $ 是频谱信息的主要载体

#### **相位不变性分析（Figure 10）**
- $ V $ 对相位变化敏感，$ H $ 更稳定
- 建议联合使用 $ H $ 和 $ V $ 以增强鲁棒性

---

## **4. 关键结论和发现**

### **主要发现**
1. **PFNs 隐式执行频谱估计**  
   DVA-PFN 的 attention 输出 $ H $ 编码了输入函数的频谱结构，形成可解释的 manifold。

2. **摊销频谱发现是可行的**  
   通过设计 decoder，可以从冻结 PFN 中高效提取显式 spectral density 和 stationary kernel。

3. **multi-realization 下 spectral weights 可识别**  
   理论证明并实验验证：多个独立样本允许一致估计 second-order statistics，从而恢复完整 spectral mixture。

4. **single-realization 下仅 shape 可识别**  
   归一化导致全局尺度模糊，但频率与带宽仍可恢复，权重需设为均匀 + 解析缩放。

5. **性能媲美 PFN，速度媲美前向传播**  
   解码 kernel 支持的 GP 回归精度接近 PFN，且比 DKL/RFF 快 ~1000×。

---

### **方法的局限性**
- **依赖 PFN 预训练质量**：若 PFN 未充分学习 spectral 结构，则 decoder 效果受限。
- **仅适用于 stationary kernels**：无法处理非平稳过程。
- **高维组合爆炸**：在 fully interacting 高维 kernel 上表现下降（如 product kernels）。
- **periodic structure 建模困难**：global pattern 难以通过局部注意力完全捕捉。
- **不保证精确重构原始 kernel**：输出的是 surrogate kernel，捕获主导二阶统计量。

---

### **未来工作方向**
1. **集成到下游任务**：将 decoded kernel 用于 surrogate-based optimization、control、物理建模等。
2. **混合架构设计**：结合 amortized inference 与 adaptive context selection，提升大数据场景下的 scalability。
3. **维度感知建模**：改进 PFN 架构以显式编码维度信息，支持 per-dimension spectral decoding。
4. **非平稳 kernel 扩展**：探索时变 spectral density 的摊销发现。
5. **真实世界信号应用**：迁移到 EEG、语音、气候等实际 time series 数据。

--- 

> 📌 **一句话总结**：  
> 本文揭示了 PFN 内部蕴含可解释的频谱结构，并提出首个**摊销频谱核发现框架**，实现了**零样本、高速、显式的 kernel 发现**，为 AutoML 与科学发现提供了新的可解释工具。

</details>

---

### 4. [NetMamba+: A Framework of Pre-trained Models for Efficient and Accurate Network Traffic Classification](https://arxiv.org/abs/2601.21792)

**Authors**: Tongze Wang, Xiaohui Xie, Wenduo Wang, Chuyi Wang, Jinzhou Liu, Boyan Huang, Yannan Hu, Youjian Zhao, Yong Cui  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2601.21792v1  

#### Abstract
With the rapid growth of encrypted network traffic, effective traffic classification has become essential for network security and quality of service management. Current machine learning and deep learning approaches for traffic classification face three critical challenges: computational inefficienc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：NetMamba+: A Framework of Pre-trained Models for Efficient and Accurate Network Traffic Classification

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前网络流量分类面临三大挑战：
- **计算效率低下**：基于Transformer的模型因自注意力机制具有 $O(L^2)$ 复杂度，在处理长序列时计算和内存开销巨大，难以部署于实时系统。
- **流量表征不充分**：现有方法在保留原始字节信息的同时引入偏差（如忽略header特征、不当分块），导致关键语义丢失。
- **对长尾分布数据适应差**：真实场景中类别分布高度不平衡（long-tailed distribution），主流方法对此缺乏有效应对策略。

### 提出的新方法与创新思路
NetMamba+ 是首个将 **Mamba 架构**应用于网络流量分类的预训练框架，提出以下三项核心技术：

#### （1）高效模型架构设计
- 引入 **Mamba**（线性时间复杂度的SSM模型）作为主干，替代传统Transformer，显著降低推理延迟。
- 可选使用集成 **Flash Attention** 的现代Transformer变体（NetTrans），通过IO感知优化提升效率。
- 采用 **pre-normalization** 和 **GeGLU-activated FFN** 提升训练稳定性和准确率。

#### （2）多模态流量表征方案（Multimodal Traffic Representation）
- 同时建模三种输入模态：
  - **Byte Stride**：从包头和载荷提取固定长度字节段，使用一维stride切割避免图像式patch带来的语义偏差。
  - **Packet Size Sequence**
  - **Inter-arrival Time Sequence**
- 通过早期融合（early fusion）整合跨模态信息，并加入位置编码和模态标识符。

#### （3）标签分布感知微调策略（Label Distribution-Aware Fine-tuning, LDA）
- 针对长尾分布，提出结合 **类重加权（class re-weighting）** 和 **大间隔学习（large margin learning）** 的新型损失函数：
  $$
  \mathcal{L}_{\text{LDA}} = -\frac{1-\beta}{1-\beta^{n_y}} \log \frac{e^{z_y - \Delta_y}}{\sum_j e^{z_j - \Delta_j}}
  $$
  其中 $\Delta_j = C / n_j^{1/4}$ 对少数类施加更大margin。

### 相比现有方法的优势
| 维度 | NetMamba+ 优势 |
|------|----------------|
| **性能** | 在多个任务上达到SOTA，F1最高提升达 **6.44%** |
| **效率** | 推理吞吐量比最佳基线高 **1.7×**，GPU内存占用低 |
| **鲁棒性** | 少样本学习能力强，仅用10%标注数据仍优于多数全监督方法 |
| **实用性** | 实现在线系统，实测平均吞吐达 **261.87 Mb/s** |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据集 | 下游任务 | 特点 |
|------|--------|----------|------|
| **预训练** | Browser [2], Kitsune [52] | — | 包含多种加密协议（TLS, QUIC等）和攻击流量 |
| **微调** | CipherSpectrum, CSTNET-TLS1.3, CICIoT2022, USTC-TFC2016, ISCXVPN2016 等共11个 | 应用分类、攻击检测、恶意软件识别、VPN识别 | 覆盖四大典型分类任务，部分为真实企业数据（如Huawei-VPN） |

> 所有微调数据集均按8:1:1划分训练/验证/测试集，并限制每类最大流数以缓解不平衡。

### 实验设置
- **模型实现**：
  - NetMamba+：4层Encoder + 2层Decoder，参数约2.6M（预训练）/1.9M（微调）
  - 实现工具：PyTorch 2.1.1，硬件平台为A100 GPU ×4
- **训练配置**：
  - 预训练：batch_size=128，步数15万，AdamW优化器，学习率$1e^{-3}$
  - 微调：batch_size=64，epoch=120，学习率$2e^{-3}$

### 评估指标
| 场景 | 指标 |
|------|------|
| **In-distribution 分类** | Accuracy (AC), Precision (PR), Recall (RC), weighted F1 Score |
| **Out-of-distribution (OOD) 检测** | AUROC, FPR95 |
| **效率评估** | 推理吞吐量（samples/sec）、GPU内存消耗（MB） |
| **少样本评估** | 不同比例（10%, 40%, 70%, 100%）标注数据下的性能 |

### 基线方法对比
| 类别 | 代表方法 |
|------|--------|
| 传统机器学习 | AppScanner, FlowPrint |
| 深度学习（监督） | Seq2Img, FS-Net, FlowPic, mini-FlowPic, TFE-GNN |
| Transformer预训练模型 | ET-BERT, YaTC, TrafficFormer |
| 架构变体 | NetTransV（vanilla Transformer）、NetTransL（Linear Transformer）、NetMambaB（双向Mamba）、NetMambaC（级联Mamba） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 平均Accuracy | 最高F1提升 | 推理吞吐（相对提升） | 内存使用 |
|------|--------------|------------|-----------------------|-----------|
| **NetMamba+** | **0.8498 ~ 0.9765** | **+6.44%** vs SOTA | **1.7× > YaTC** | 保持低位 |
| NetMamba | 0.8779 ~ 0.9779 | — | 相当 | 更低 |
| NetTrans | 0.8543 ~ 0.9823 | — | 相当 | 略高 |

> ✅ 在所有5个公开数据集上，NetMamba+均取得最优或次优性能。

### 与基线方法的对比结果
- **性能方面**：
  - 在完全加密流量（CipherSpectrum）上，NetMamba+ 准确率达 **96.52%**，远超第二名FS-Net（90.00%）。
  - 在华为真实VPN数据集上，NetMamba+ 达到 **94.5%** 准确率，显著优于ET-BERT（<80%），说明header信息的重要性。
- **效率方面**：
  - 推理速度比ET-BERT快 **47.4倍**，比TrafficFormer快近 **10倍**。
  - GPU内存使用低于大多数深度模型，仅次于轻量级NetTransL和FS-Net。

### 消融实验结果

#### （1）原始字节组件消融（Table VI）
| 移除组件 | 性能下降（ACC） |
|---------|----------------|
| Header | ↓14.86% ~ 47.50% |
| Payload | 影响不稳定（±1~3%） |
| Stride Cutting → Patch Splitting | ↓≤2.27% |
| Pre-training | ↓2.5% ~ 6.5% |

> 结论：Header信息至关重要；stride切割优于patch分割；预训练显著提升泛化能力。

#### （2）多模态输入消融（Table VII）
| 输入模态 | 表现特点 |
|----------|----------|
| Byte Only | 基础强，但在加密流量中受限 |
| Size Only | 在CipherSpectrum表现优异（89.06%） |
| Interval Only | 效果较差 |
| **All (NetMamba+)** | **全面领先**，证明多模态融合有效性 |

#### （3）LDA微调消融（Table VIII）
| 数据集 | 使用LDA后F1提升 |
|--------|------------------|
| Huawei-VPN | +0.89% (NetTrans), +0.90% (NetMamba) |
| CP-Android | +1.75%, +1.89% |
| CP-iOS | +2.97%, +2.40% |

> 结论：LDA微调在长尾数据上持续带来增益，尤其在移动端应用分类中效果明显。

#### （4）少样本学习（Figure 8）
- 即使只使用 **10%标注数据**，NetMamba+ 仍优于多数使用全量数据的监督模型。
- 显著优于非预训练模型（如TFE-GNN）在小样本下性能急剧下降。

#### （5）OOD检测（Table IX）
| 任务 | AUROC (NetMamba+) | FPR95 |
|------|--------------------|--------|
| Unknown Application | **0.9455** | 0.3670 |
| Unknown Attack | **0.9825** | **0.0463** |
| Unknown VPN | **0.9720** | **0.0715** |
| Unknown Malware | **0.9668** | **0.1072** |

> NetMamba+具备强大未知流量识别能力，适合实际安全部署。

---

## 4. 关键结论和发现

### 主要发现
1. **Mamba适用于网络流量建模**：首次成功将Mamba引入网络领域，验证其在线性复杂度下仍能捕捉深层语义。
2. **多模态输入优于单一模态**：融合byte、size、interval三类信号可互补信息，尤其在加密环境下提升显著。
3. **stride切割优于patch分割**：更符合网络流量的顺序特性，减少无关字节关联引入的噪声。
4. **LDA微调有效缓解长尾问题**：无需采样即可提升少数类识别能力，工程友好。
5. **预训练极大增强少样本能力**：利用海量无标签数据学习通用表征，大幅降低对标注数据依赖。

### 方法的局限性
- **对分布偏移敏感**：在时间分布漂移场景下（如CSTNET-TLS1.3），准确率下降达8.47%，需进一步增强鲁棒性。
- **未支持动态流长**：当前输入截断至前$M_b$个包，可能丢失后续重要行为模式。
- **依赖高质量匿名化**：若IP/port未彻底脱敏，存在隐私泄露风险。

### 未来工作方向
- 增强模型在**分布外（OOD）和概念漂移**场景下的适应能力。
- 探索**流级别增量学习**机制，支持无限长流处理。
- 扩展至其他网络任务：如QoS预测、网络性能诊断、LLM-based traffic reasoning。
- 开发端侧轻量化版本，适配边缘设备部署。

> 🔗 代码已开源：[https://github.com/wangtz19/NetMamba](https://github.com/wangtz19/NetMamba)

</details>

---

### 5. [ZipMoE: Efficient On-Device MoE Serving via Lossless Compression and Cache-Affinity Scheduling](https://arxiv.org/abs/2601.21198)

**Authors**: Yuchen Yang, Yaru Zhao, Pu Yang, Shaowei Wang, Zhi-Hua Zhou  
**Category**: cs.DC  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21198v1  

#### Abstract
While Mixture-of-Experts (MoE) architectures substantially bolster the expressive power of large-language models, their prohibitive memory footprint severely impedes the practical deployment on resource-constrained edge devices, especially when model behavior must be preserved without relying on los...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ZipMoE: Efficient On-Device MoE Serving via Lossless Compression and Cache-Affinity Scheduling**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现代 **Mixture-of-Experts (MoE)** 架构虽然显著提升了大语言模型（LLM）的表达能力，但其庞大的内存占用严重阻碍了在资源受限的边缘设备（如手机、Jetson、树莓派等）上的部署。传统方法依赖**有损量化（lossy quantization）** 或**专家卸载（expert offloading）** 来减少内存开销，但存在以下问题：

- **有损量化**会改变模型行为，可能引入安全漏洞（如恶意代码生成、过度拒绝等），且现有评估指标难以捕捉这些风险。
- **现有卸载系统**假设 CPU 和 GPU 内存分离，而主流边缘平台采用 **统一内存架构（UMA）**，导致 I/O 瓶颈加剧，计算资源严重浪费。

因此，论文旨在解决：**如何在不改变 MoE 模型语义的前提下，高效地在移动和边缘设备上进行推理？**

---

### **提出了什么新方法或新思路**
作者提出 **ZIPMoE** —— 一种高效的、语义无损（semantically lossless）的 on-device MoE 推理引擎，其核心创新在于 **“缓存-调度协同设计”（caching-scheduling co-design）**，具体包括：

#### **(1) 基于统计冗余的无损压缩（Lossless Compression）**
- 观察到 BF16 参数中 **指数位（exponent bits）具有高度低熵特性**，而符号位和尾数位接近随机分布。
- 将每个参数张量拆分为 **E-chunks（指数位，可压缩）** 和 **SM-chunks（符号+尾数位，不可压缩）**。
- 对 E-chunks 使用 **ZSTD/LZ4HC** 进行无损压缩，压缩率可达 **68%~74%**，接近香农熵下限。

#### **(2) 分层压缩感知缓存管理（Compression-Aware Cache Management）**
- 设计四类缓存池：
  - `F`：完整张量
  - `C`：已解压的 E-chunk + SM-chunk
  - `S`：仅 SM-chunk
  - `E`：仅压缩后的 E-chunk
- 引入 **基于排名的工作负载建模（rank-based workload modeling）**，通过历史激活频率预测专家访问模式。
- 使用 **动态规划（Dynamic Programming）** 优化各缓存池的内存分配，最小化预期 **makespan**。

#### **(3) 缓存亲和调度器（Cache-Affinity Scheduler）**
- 将每个专家重建任务建模为 **DAG（有向无环图）**，节点表示 I/O、解压、恢复操作。
- 提出 **Type-I / Type-II 任务划分**：
  - Type-I：需加载 SM-chunk（I/O 密集）
  - Type-II：仅需解压 E-chunk（计算密集）
- 调度器通过 **块构造（block construction）** 策略，将 Type-II 任务插入 Type-I 的 I/O 空隙中，实现 **I/O 与 CPU 解压并行化**。
- 理论证明该调度算法的 **makespan 不超过最优值的 (3 - 1/L) 倍**（L 为解压线程数）。

#### **(4) 高效的 GPU 张量恢复内核**
- 使用 **memory-coalesced CUDA kernel** 并行合并 E-chunk 和 SM-chunk，恢复为 BF16 张量。
- 利用 UMA 特性，采用 **zero-copy 范式**，避免冗余数据拷贝。

---

### **相比现有方法的优势**
| 维度 | ZIPMoE | 现有方法（MoE-Infinity, DeepSpeed, Accelerate） |
|------|--------|---------------------------------------------|
| **模型保真度** | ✅ 无损压缩，保持原始行为 | ❌ 依赖有损量化，可能引入安全隐患 |
| **硬件适配性** | ✅ 针对 UMA 架构优化 | ❌ 假设分离内存，不适用于边缘设备 |
| **I/O 效率** | ✅ 压缩后传输 + 并行解压隐藏延迟 | ❌ 大量原始参数传输，I/O 成瓶颈 |
| **CPU 利用率** | ✅ 充分利用多核 CPU 进行并行解压 | ❌ CPU 多处于空闲状态 |
| **缓存策略** | ✅ 分层压缩感知缓存 + 动态规划优化 | ❌ 传统 FIFO/LRU，未考虑压缩状态 |

---

## **2. 核心实验方法和设置**

### **使用的模型与数据集**
- **模型**：
  - **DeepSeekV2-Lite**（decoder-only）
  - **Qwen1.5-MoE**（decoder-only）
  - **SwitchTransformers-Large-128**（encoder-decoder）
- **数据集**：从 **ShareGPT** 中随机采样提示词，确保所有基线系统输入一致。

### **实验平台**
- **边缘设备**：
  - **Jetson AGX Orin 64GB**（Hardware 1）
  - **Jetson AGX Orin 32GB**（Hardware 2）
- **存储**：Samsung 970 EVO NVMe SSD（读速 3.5GB/s）
- **操作系统**：Ubuntu 22.04 + JetPack 6.2.1

### **评估指标**
- **TPOT**（Time Per Output Token）：衡量生成速度
- **TTFT**（Time To First Token）：衡量响应延迟
- **端到端延迟**（End-to-End Latency）
- **吞吐量**（Throughput, token/s）
- **内存占用**（Memory Footprint）

### **基线方法对比**
1. **MoE-Infinity**：基于稀疏性的专家缓存与预取
2. **DeepSpeed**：支持 SSD 卸载的 ZeRO-3
3. **Accelerate**：HuggingFace 提供的轻量级卸载库

所有系统均配置为 SSD 卸载模式，并对齐 DRAM 内存占用以保证公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 指标 | ZIPMoE 表现 |
|------|------------|
| **最大推理延迟降低** | **72.77%** |
| **最高吞吐提升** | **6.76×** |
| **平均 TPOT 降低** | **62.65% ~ 97.97%**（decoder-only） |
| **平均 TTFT 降低** | **53.25% ~ 87.90%** |
| **端到端加速比** | **3.03× ~ 42.49×**（decoder-only） |

---

### **与基线方法的对比结果**
#### **(1) 实时响应性（TPOT & TTFT）**
- 在必须卸载的场景下，ZIPMoE 显著优于所有基线：
  - **TPOT 减少最多达 97.97%**
  - **TTFT 最多减少 87.90%**
- 即使对于 encoder-decoder 模型（如 SwitchTransformers），仍能实现 **81.24% TPOT 降低** 和 **83.45% TTFT 降低**。

#### **(2) 吞吐量（Throughput）**
- 在 batch size = 1~16 下测试：
  - **decoder-only 模型**：ZIPMoE 达到 **1.79× ~ 42.49×** 吞吐提升
  - **encoder-decoder 模型**：达到 **1.31× ~ 5.82×** 提升
- 随着 batch size 增加，ZIPMoE 的并行优势进一步放大。

#### **(3) 端到端延迟**
- 在不同输出长度下，ZIPMoE 始终领先：
  - **decoder-only**：加速 **3.03× ~ 42.49×**
  - **encoder-decoder**：加速 **1.11× ~ 5.64×**

---

### **消融实验结果**
- **移除缓存规划模块**（w/o Cache Planning）：
  - 使用 FIFO/LRU 替代，性能明显下降。
- **替换缓存策略**：
  - FIFO、Marking、LRU 表现相近，均不如 ZIPMoE 自研策略。
- **加入缓存规划后**：
  - 实现 **Pareto 最优**，同时降低延迟并提高吞吐。

> 图 10 显示，ZIPMoE 在相同吞吐下延迟更低，或在相同延迟下吞吐更高。

---

## **4. 关键结论和发现**

### **主要发现**
1. **MoE 参数中存在显著的统计冗余**，特别是 BF16 的 exponent bits 可被高效无损压缩。
2. **边缘设备的 UMA 架构应被主动利用**，而非被动适应。ZIPMoE 通过 zero-copy 和并行解压充分发挥其潜力。
3. **I/O 瓶颈可通过“计算换 I/O”策略突破**：用 CPU 多核并行解压来隐藏 SSD 读取延迟。
4. **分层缓存 + 动态规划调度** 可实现细粒度内存控制，在有限内存下最大化命中率与性能。
5. **ZIPMoE 将 MoE 推理从 I/O-bound 转变为 compute-centric 流程**，释放了边缘设备的真正算力。

---

### **方法的局限性**
1. **依赖特定压缩格式**：目前仅针对 BF16 的 exponent bits 设计，若使用 FP8 或其他格式需重新分析。
2. **对 SSD 性能敏感**：在极低带宽存储设备上，即使压缩后 I/O 仍可能是瓶颈。
3. **调度复杂度较高**：缓存亲和调度需要离线 profiling 支持，增加部署复杂性。
4. **未考虑动态内存竞争**：在多任务环境下，缓存预算可能被其他应用抢占。

---

### **未来工作方向**
1. **扩展至更多量化格式**：研究 FP8、NF4 等格式下的压缩潜力。
2. **支持动态缓存调整**：根据运行时负载自动调节各缓存池大小。
3. **结合轻量级预取机制**：进一步减少冷启动延迟。
4. **跨设备协同推理**：探索 ZIPMoE 在设备-云协同场景中的应用。
5. **硬件协同设计**：定制 FPGA/ASIC 加速解压与张量重组过程。

--- 

> **总结**：ZIPMoE 是首个面向边缘设备、基于**无损压缩**与**缓存-调度协同设计**的 MoE 推理系统，不仅大幅提升性能，更重要的是**保障了模型行为的完整性与安全性**，为安全、高效的 on-device AI 提供了新范式。

</details>

---

### 6. [Snowball: A Scalable All-to-All Ising Machine with Dual-Mode Markov Chain Monte Carlo Spin Selection and Asynchronous Spin Updates for Fast Combinatorial Optimization](https://arxiv.org/abs/2601.21058)

**Authors**: Seungki Hong, Kyeongwon Jeong, Taekwang Jang  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21058v1  

#### Abstract
Ising machines have emerged as accelerators for combinatorial optimization. To enable practical deployment, this work aims to reduce time-to-solution by addressing three challenges: (1) hardware topology, (2) spin selection and update algorithms, and (3) scalable coupling-coefficient precision. Rest...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Snowball: A Scalable All-to-All Ising Machine with Dual-Mode Markov Chain Monte Carlo Spin Selection and Asynchronous Spin Updates for Fast Combinatorial Optimization*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对当前 **Ising Machine** 在解决组合优化问题时面临的三大挑战：
1. **硬件拓扑限制**：稀疏连接的硬件（如 Chimera 拓扑）需要通过 *minor embedding* 映射密集问题图，导致物理自旋数量膨胀、资源浪费和映射失败风险。
2. **收敛行为不佳**：并行更新策略（如同步全自旋更新）容易引发振荡（oscillation）或停滞，破坏详细平衡（detailed balance），影响算法收敛。
3. **耦合系数精度不足**：模拟实现（如量子退火、模拟 CMOS）受限于低比特宽度，导致能量景观失真或需引入辅助自旋，降低可扩展性和解质量。

### 提出的新方法与创新
本文提出了 **Snowball** —— 一种数字、可扩展、全连接（all-to-all）的 Ising Machine 架构，其核心创新包括：

- ✅ **Dual-Mode Markov Chain Monte Carlo (MCMC) 自旋选择机制**
  - 支持两种模式：
    - **Random-Scan Mode**：随机选择单个自旋进行异步更新，保证详细平衡与遍历性（ergodicity），确保收敛到吉布斯分布。
    - **Roulette-Wheel Mode**：并行计算所有自旋翻转概率，按权重抽样选择一个自旋进行确定性翻转，减少拒绝率，加速低能态探索。
  - 双模设计允许在收敛稳定性与求解速度之间灵活权衡。

- ✅ **异步自旋更新（Asynchronous Spin Updates）**
  - 每次仅更新一个自旋，并立即传播其对局部场 $ u_i $ 的影响，避免周期性振荡（如 period-2 dynamics），提升收敛稳定性。

- ✅ **高精度、可扩展的耦合系数表示**
  - 采用 **bit-plane decomposition** 技术将高精度 $ J_{ij} $ 分解为多个符号位平面，在 FPGA 上高效存储与处理，支持宽动态范围且内存增长线性于比特数。

- ✅ **增量式局部场更新（Incremental Local-Field Update）**
  - 利用列主序（column-major）bit-plane 缓冲区，在单个自旋翻转后以 $ O(N) $ 时间完成所有受影响局部场的更新，避免每次重新计算 $ O(N^2) $ 的开销。

- ✅ **片上状态无关 RNG（Stateless RNG）**
  - 使用基于种子和索引的纯函数生成随机数，支持高度并行化且无共享状态争用，适合 FPGA 实现。

### 相比现有方法的优势
| 特性 | Snowball | 典型模拟/专用硬件（如 D-Wave, CIM, SB） |
|------|----------|----------------------------------------|
| 连接性 | 全连接（all-to-all） | 受限拓扑（需 minor embedding） |
| 耦合精度 | 高精度、可配置（bit-plane） | 低精度、易受噪声干扰 |
| 更新方式 | 异步、双模式 MCMC | 同步或连续动力学，可能不满足详细平衡 |
| 可编程性 | 数字架构，灵活调度 | 固定物理过程，灵活性差 |
| 可扩展性 | 基于 FPGA，易于扩展 | 受限于物理器件规模 |

---

## 2. 核心实验方法和设置

### 数据集
- **Gset [59]**：标准组合优化基准套件，包含多种图结构（Erdős–Rényi, Small-world, Torus），用于评估 **解质量（cut value）**。
- **K2000**：人工构造的完全图（complete graph），$ |V|=2000 $，边权 $ J_{ij} \in \{-1, +1\} $ 随机生成，边密度 100%，用于测试大规模全连接场景下的 **time-to-solution (TTS)**。

### 实验设置与评估指标
- **平台**：AMD Alveo U250 FPGA 加速卡，运行频率 300 MHz。
- **评估指标**：
  - **Cut Value**：Max-Cut 问题的目标函数值，越高越好。
  - **Time-to-Solution (TTS)**：达到目标成功率 $ p=0.99 $ 所需的期望运行时间：
    $$
    \text{TTS}(p) = t_a \cdot \frac{\ln(1-p)}{\ln(1-P_a(t_a))}
    $$
    其中 $ t_a $ 是单次运行时间，$ P_a(t_a) $ 是单次成功概率。
- **冷却策略**：采用线性退火（simulated annealing）控制温度下降。

### 基线方法对比
- **Neal [15]**：D-Wave 的 CPU 实现的模拟退火器（Simulated Annealing）。
- **CIM [28]**：相干 Ising Machine（光学系统）。
- **SB [21]**：基于分岔的 FPGA 模拟退火。
- **STATICA [54]**：CMOS 全数字退火处理器（支持一次更新所有自旋）。
- **ReAIM [11]**：基于 ReRAM 的存内计算 Ising Machine（当前最先进之一）。

> 注：部分基线结果来自仿真或小规模原型，而 Snowball 在真实 FPGA 硬件上实现并测量。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 在 Gset 上的解质量（Cut Value）
- Snowball 的 **RWA（roulette-wheel）** 和 **RSA（random-scan）** 模式在多数实例上取得最高 cut value。
- 表明其算法在保持高质量解方面具有竞争力。

#### ✅ 在 K2000 Max-Cut 上的 TTS(0.99)

| Ising Machine | Hardware Type | $ t_a $ [ms] | $ P_a(t_a) $ | **TTS(0.99) [ms]** |
|---------------|----------------|--------------|----------------|--------------------|
| Neal [15]     | CPU            | 4610         | 0.38           | **44413**          |
| CIM [28]      | Optics         | 5            | 0.02           | **1139.74**        |
| SB [21]       | FPGA           | 0.5          | 0.04           | **56.14**          |
| STATICA [54]  | CMOS           | 0.13         | 0.07           | **8.23**           |
| ReAIM [11]    | CMOS (sim)     | 0.15         | 0.47           | **1.11**           |
| **Snowball (seq)** | FPGA       | **0.128**    | **0.99**       | **0.128**          |
| **Snowball (par)** | FPGA       | **0.085**    | **0.99**       | **0.085**          |

> ⚡️ **Snowball 实现了比 ReAIM 快 8× 的 TTS(0.99)，相比 Neal 加速超过 20万倍！**

### 与基线方法的对比结果
- Snowball 的 TTS(0.99) 达到 **0.085 ms（并行模式）**，是目前报告中最优的结果。
- 即使与专为高速设计的 STATICA 和 ReAIM 相比，仍分别快约 **97×** 和 **13×**。
- 成功概率高达 0.99，意味着几乎每次运行都能找到目标解，显著优于其他方法（通常 <0.8）。

### 消融实验与分析（隐含验证）
- **增量更新有效性**：图14显示，“naive”（无增量更新）的端到端延迟远高于实际实现，证明增量更新极大缓解了内存带宽瓶颈。
- **计算绑定特性**：图14中 kernel-only 与 end-to-end 曲线几乎重合，说明系统是 compute-bound 而非 memory-bound，表明架构设计高效。
- **高精度重建能力**：图15展示 Snowball 可在 16-bit 精度下重建 64×64 局部场，像素级准确率达 **99.5%**，验证 bit-plane 方案的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **全连接 + 数字架构是高性能 Ising Machine 的可行路径**：尽管布线复杂，但通过 bit-plane 和行列缓冲策略可在 FPGA 上高效实现。
2. **异步 + 双模式 MCMC 平衡了收敛性与速度**：
   - Random-scan 保证理论收敛；
   - Roulette-wheel 减少拒绝，加快搜索。
3. **增量更新是实现高效全连接的关键**：将每步计算从 $ O(N^2) $ 降至 $ O(N) $，使大规模模拟成为现实。
4. **Snowball 在真实硬件上实现了 SOTA 性能**：相较 ReAIM 实现 **8× 的 TTS 加速**，且解质量更优。

### 方法的局限性
- **依赖 FPGA 资源**：虽然可扩展，但在更大规模（如 $ N > 10^4 $）时仍受限于 BRAM 容量和逻辑单元。
- **未集成 parallel tempering**：文中指出 replica exchange 对大规模系统效率下降，故未采用，可能限制某些粗糙能量景观的穿越能力。
- **尚未多芯片扩展**：当前实现在单块 FPGA 上，未来需研究 chiplet 或多卡互联以进一步扩大规模。

### 未来工作方向
- 探索 **multi-FPGA 或 chiplet-based 扩展架构**，支持百万级自旋。
- 结合 **hybrid annealing strategies**，如 adaptive cooling 或轻量级 replica exchange。
- 将 Snowball 架构应用于更多 NP-hard 问题（如 TSP、SAT、QUBO）的实际部署。
- 开发更高层次的编译工具链，自动将组合优化问题映射到 Snowball 架构。

--- 

> 📌 **总结一句话**：  
> Snowball 通过 **数字全连接架构 + 双模式 MCMC + 增量更新 + bit-plane 精度管理**，在真实 FPGA 上实现了比现有最先进 Ising Machine 快 **8倍以上** 的 time-to-solution，为高性能组合优化硬件提供了新的范式。

</details>

---

### 7. [L$^3$: Large Lookup Layers](https://arxiv.org/abs/2601.21461)

**Authors**: Albert Tseng, Christopher De Sa  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21461v1  

#### Abstract
Modern sparse language models typically achieve sparsity through Mixture-of-Experts (MoE) layers, which dynamically route tokens to dense MLP "experts." However, dynamic hard routing has a number of drawbacks, such as potentially poor hardware efficiency and needing auxiliary losses for stable train...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文《L³: Large Lookup Layers》核心结论与实验结果总结**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现代稀疏语言模型（如 MoE）通过动态路由（context-dependent routing）实现高参数量下的高效计算，但存在以下系统级挑战：
- **硬件效率低**：专家选择依赖上下文，无法提前确定激活参数，导致难以进行参数卸载（offloading）和预取（prefetching）。
- **训练不稳定**：需要辅助损失（auxiliary losses）来平衡负载，防止路由坍缩（routing collapse）。
- **系统开销大**：专家分布不均可能导致设备利用率低下，且难以扩展到大规模部署。

相比之下，传统的 **tokenizer embedding 表** 是一种静态稀疏结构，具有极高的系统友好性（如可预测访问模式），但缺乏上下文感知能力，仅用于初始嵌入。

本文提出：**能否设计一种既具备上下文感知能力、又保持系统友好的新型稀疏架构？**

---

### **提出了什么新方法或新思路**
作者提出了 **Large Lookup Layer (L³)**，这是一种新的稀疏化范式，其核心思想是：
> 将传统 tokenizer embedding 表的概念推广到 decoder 层中，并引入**上下文感知的静态查找机制**。

#### **L³ 的两大核心组件**：
1. **静态路由（Static Routing）**  
   - 每个 token ID 预分配一组固定的 embeddings（称为“lookup table”），这些 embeddings 在训练前就已确定。
   - 路由仅依赖于 token ID，而非隐藏状态，因此可以**在 token 生成时立即知道所需参数**，支持高效的 CPU 卸载与预取。

2. **上下文感知聚合（Context-Aware Aggregation）**  
   - 使用当前 token 的 hidden state 对其对应的 embeddings 进行 attention 操作，从而实现**基于上下文的信息检索**。
   - 形式上类似于 Key-Value Attention，其中 Key 和 Value 来自 lookup table，Query 来自 hidden state。

3. **信息论驱动的 embedding 分配算法**  
   - 提出基于 **LZW 压缩算法** 的 embedding 分配策略，将高频出现的 token 后缀赋予更多 embeddings。
   - 实现方式：扫描语料库，统计最长未见后缀频率，按频次降序为 token 分配 embeddings，上限为 $ k=512 $。

---

### **相比现有方法的优势**
| 维度 | MoE | L³ |
|------|-----|----|
| **路由机制** | 动态（context-dependent） | 静态（token-based） |
| **参数可预测性** | 差（需运行至 router 才知） | 极佳（token 一出即知） |
| **系统友好性** | 差（难 offload，负载不均） | 强（支持零开销 offload） |
| **训练稳定性** | 需要 load balancing loss | 不需要 |
| **稀疏轴正交性** | 参数稀疏主路径 | 新增独立稀疏维度，可与 MoE 共存 |
| **性能提升** | 显著但有启动成本 | 立竿见影，全程领先 |

> ✅ **L³ 并非替代 MoE，而是提供了一条新的、更高效的稀疏扩展路径**。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **训练数据**：FineWeb-Edu（约 350B tokens 子集）
- **评估数据**：
  - WikiText2（用于 PPL 测试）
  - 多项 zero-shot 下游任务：ARC-C, ARC-E, HellaSwag, PIQA, Winogrande

### **实验设置**
- **模型架构基础**：基于 Llama 3 风格的 decoder-only transformer
- **模型规模**：
  - 800M（400M decoder）、1.5B、2.6B **active parameters**
  - 总参数可达 7B（含 L³ embeddings）
- **上下文长度**：2048
- **Tokenizer**：BPE，vocab size = 180K
- **L³ 设置**：
  - 每个 token 最多 $ k=512 $ 个 embeddings
  - 总 embedding 数 $ u = 710K $
  - L³ 层插入位置：decoder 层之间（如第 4、16 层后）

### **评估指标**
- **训练阶段**：训练集 perplexity（PPL）
- **推理阶段**：
  - Zero-shot accuracy on multiple benchmarks
  - 推理吞吐量（tokens/sec）
  - offloading 开销分析

### **基线方法对比**
| 类型 | 模型 |
|------|------|
| **Dense Baseline** | 同 active FLOPs 的全连接模型 |
| **MoE** | Iso-FLOP、iso-depth、iso-sparse MoE（如 22–44 专家/层） |
| **消融实验** | 不同 L³ 层数、embedding 分配策略、weight tying 等 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 训练 perplexity 提升显著**
- 在所有模型尺度下，添加 L³ 层均带来持续且稳定的 PPL 下降：
  - **2.6B 模型**：从 15.43（dense）降至 **14.51**（+2L³），相对下降 **6%**
  - **1.5B 模型**：从 18.83 → 16.72（+2L³）
  - **800M 模型**：从 22.02 → 19.59（+3L³*）

> 图 6 显示 L³ 改进贯穿整个训练过程，无初期性能下降（而 MoE 常因 router 学习困难出现冷启动问题）。

#### **(2) 下游任务全面领先**
| 模型 | ACTIVE PARAMS | TOTAL PARAMS | ARCC ↑ | HellaSwag ↑ | PIQA ↑ | Winogrande ↑ |
|------|----------------|---------------|--------|-------------|--------|--------------|
| Dense 2.6B | 2.6B | 2.6B | 36.35 | 44.20 | 71.22 | 57.30 |
| L³ 2.6B (2 layers) | 2.6B | 7B | **38.21** | **44.95** | **71.71** | **58.80** |

> 所有任务均有提升，尤其逻辑推理类任务（如 ARCC +1.86 pts）。

#### **(3) 推理速度几乎无损**
| 模型 | FIRST LAYER OFFLOADED? | TOKENS/S |
|------|-------------------------|----------|
| Dense 2.6B | – | 334.98 |
| L³ 2.6B (CPU offloaded) | Yes (1st L³) | 302.11 |
| L³ 2.6B | Yes (4th layer) | **332.74** |

> ⚡ **仅需 4 个 decoder 层即可完全掩盖 offloading 延迟**，说明 L³ 的静态路由极大提升了系统效率。

---

### **与基线方法的对比结果**

#### **vs. MoE：L³ 更高效且稳定**
- 图 8 显示，在相同 FLOPs 和稀疏率下：
  - **800M 尺度**：一个 L³ 层优于两个 L³ 等效的 MoE
  - **1.5B 尺度**：一个 L³ 层等效的 MoE 甚至不如 dense baseline
- 结论：**MoE 需要足够大的 token budget 或稀疏率才能发挥优势，而 L³ 效果立竿见影**。

#### **vs. Uniform Allocation**
- 使用 LZW 分配比均匀分配（每个 token 固定 4 个 embeddings）带来 **近两倍的 PPL 改善幅度**（图 7C）。
- 若不限制 $ k $，某些高频 token 可能获得超 20K embeddings，造成 worst-case 内存爆炸；限制 $ k=512 $ 后性能仍接近最优。

---

### **消融实验结果**

| 实验 | 发现 |
|------|------|
| **不同 L³ 层数 vs. embedding 规模**（图 7L） | 2×710K ≈ 1×1.42M，但单一大层不利于 offloading；推荐多小层平衡性能与系统效率 |
| **embedding 分配策略**（图 7C） | LZW > capped LZW (k=512) > k=256 >> uniform；**LZW 是关键** |
| **Weight Tying (Wₖ = Wᵥ)**（图 7R） | 几乎不影响质量，但使有效稀疏率翻倍、传输体积减半 → **强烈建议部署时启用** |
| **L³ 插入位置**（图 9） | 太早（前几层）上下文不足；太晚影响有限；中间层（如第 10 层）最佳 |

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **L³ 成功解锁了一个新的稀疏化维度**：它不依赖动态路由，而是通过“静态查找 + 上下文聚合”实现高效信息缓存。
2. ✅ **L³ 显著优于 iso-FLOP dense 和 MoE 模型**，无论是在训练稳定性、下游性能还是推理效率方面。
3. ✅ **L³ 完全兼容现有系统优化技术**：支持 fast sorting kernels（如 FlexAttention）、zero-overhead CPU offloading、context parallelism。
4. ✅ **Tuned Lens 分析表明**：L³ 层引起 KL divergence 的骤降，说明模型确实在利用 L³ 缓存本需深层计算的信息（图 10）。
5. ✅ **LZW-based allocation 有效模拟了 context-dependent behavior**：高频 token 获得更多 embeddings，KL 分布随 embedding 数平滑上升（图 11）。

---

### **方法的局限性**
- ❗ **L³ 的增益来源于“信息缓存”而非“计算增强”**：它更适合加速重复模式处理，对全新组合泛化帮助有限。
- ❗ **embedding 分配依赖语料先验**：若测试分布偏移严重，可能影响性能。
- ❗ **目前仅作为辅助层插入**：尚未完全取代 MLP 或 MoE，未来需探索端到端集成。
- ❗ **极端 token 的 worst-case 开销仍存在风险**：虽有 $ k $ 限制，但在高并发场景仍需注意。

---

### **未来工作方向**
1. 🔮 **结合 MoE 与 L³**：在同一模型中同时使用两种稀疏机制，探索协同效应。
2. 🔮 **跨任务迁移 L³ embeddings**：受 Cartridges 启发，尝试“swap L³ layers across tasks”。
3. 🔮 **动态调整 embedding 分配**：在线学习 token-specific embedding 数量。
4. 🔮 **应用于 encoder-decoder 或 long-context 模型**：探索在检索、摘要等任务中的潜力。
5. 🔮 **进一步压缩 embedding 存储**：结合量化（quantization）或哈希（hashing）降低内存占用。

---

> 📌 **总结一句话**：  
> **L³ 提出了一种系统友好、性能优越的新稀疏架构，通过“静态查找 + 上下文注意力”的设计，在不牺牲建模能力的前提下，实现了比 MoE 更快、更稳、更易部署的稀疏扩展路径**。

</details>

---

### 8. [Investigating Batch Inference in a Sequential Monte Carlo Framework for Neural Networks](https://arxiv.org/abs/2601.21983)

**Authors**: Andrew Millard, Joshua Murphy, Peter Green, Simon Maskell  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.21983v1  

#### Abstract
Bayesian inference allows us to define a posterior distribution over the weights of a generic neural network (NN). Exact posteriors are usually intractable, in which case approximations can be employed. One such approximation - variational inference - is computationally efficient when using mini-bat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Investigating Batch Inference in a Sequential Monte Carlo Framework for Neural Networks*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统的 Sequential Monte Carlo (SMC) 方法在应用于 Neural Network (NN) 的 Bayesian 推断时，通常需要在整个数据集上计算 likelihood 和 gradient，导致**计算成本极高**，难以扩展到大规模模型和数据集。而虽然 mini-batch 随机估计可以降低开销，但直接将其引入 SMC 容易破坏采样稳定性。

本文旨在解决这一矛盾：如何在保持 SMC 采样质量的同时，显著降低其计算负担。

### ✅ 提出的新方法与新思路
提出并系统研究了多种 **Data Annealing (DA)** 策略，在 SMC 框架中逐步引入 mini-batch 数据进行 likelihood 和 gradient 估计：

- **渐进式数据引入机制**：从一个小的初始 mini-batch 开始训练，随着迭代逐步添加新的 mini-batch，直到使用 full-batch。
- 探索了五种**预定义调度策略**（如 Constant、Linear、Constant-to-refine）以及一种基于 posterior 统计量的自适应策略：
  - **Smooth DA (SDA)**：通过控制目标分布 Shannon entropy 的变化率来自适应决定何时增加新数据。

该方法结合了 mini-batch 的高效性和 full-batch 的高精度优势，在推理早期快速移动粒子至高概率区域，后期精细优化。

### ✅ 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **效率** | 相比 full-batch SMC，训练速度提升最高达 **6×**，同时仅造成极小准确率损失。 |
| **灵活性** | 支持多种 annealing 调度方式，可灵活权衡速度与精度。 |
| **兼容性** | 可与梯度驱动的 Markov Kernel（如 HMC、LD）无缝集成，适用于现代 NN 架构。 |
| **无需变分假设** | 不依赖参数化变分分布（如 VI），避免因分布族选择不当带来的近似偏差。 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **MNIST**：70,000 张手写数字灰度图像，用于 LeNet-5 架构（D=61,706 参数）。
- **FashionMNIST**：70,000 张服装类别灰度图像，更具挑战性；采用更大 CNN 架构（D=96,658 参数）。

### ⚙️ 实验设置
- **框架实现**：基于 JAX 实现，运行于 NVIDIA A100 GPU。
- **迭代次数**：共 200 iterations，重复 5 次随机种子取均值与标准差。
- **Mini-batch 大小**：固定为 $ K = C = 500 $。
- **Proposal 方法**：
  - **Langevin Dynamics (LD)**
  - **Hamiltonian Monte Carlo (HMC)**，使用 $ S=3 $ 步 leapfrog。
- **评估指标**：
  - Test Loss（负对数似然）
  - Test Accuracy (%)
  - Runtime (s)

### 🔁 基线方法对比
比较了以下 **六种 batching scheme**：

| 名称 | 描述 |
|------|------|
| **Constant** | 固定使用 mini-batch（最快但最粗糙） |
| **Full-batch (FB)** | 始终使用全部数据（最精确但最慢） |
| **Constant-to-refine (CTR)** | 前期用 mini-batch，最后阶段切换为 full-batch |
| **Linear** | 每步加一个 mini-batch，线性增长至 full-batch |
| **Automated** | 类似 Linear，但在 90% 迭代处达到 full-batch |
| **Smooth DA (SDA)** | 自适应策略，依据 entropy 变化动态决定是否增大数据 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（摘自 Table 1）

#### 在 MNIST 上的表现（以 HMC + CTR 为例）：
| 指标 | 数值 |
|------|------|
| Test Accuracy | **98.00% ± 0.15%** |
| Runtime | **1265.94 s** |
| 相比 FB 加速比 | ~8.4× |

> 注：FB 的 runtime 为 10,658.68 s，准确率为 98.00%，说明 CTR 几乎达到相同精度但快 8 倍以上。

#### 在 FashionMNIST 上表现最佳方案（HMC + CTR）：
| 指标 | 数值 |
|------|------|
| Test Accuracy | **89.62% ± 0.30%** |
| Runtime | **1531.53 s** |
| 相比 FB 加速比 | ~7× |

---

### 🔍 与基线方法的对比结果

| 方法 | 准确率 | 速度 | 综合表现 |
|------|--------|-------|-----------|
| **Full-batch (FB)** | 最高（≈97.5–98.0%） | 极慢（~8700–10800 s） | 精度最优但不实用 |
| **Constant** | 较低（~92–83%） | 极快（~400–500 s） | 快但欠拟合 |
| **CTR** | 接近 FB（~98.0%, 89.6%） | 中等（~1200–1500 s） | ✅ **最佳折衷方案** |
| **Linear / Automated** | 略低于 CTR | 更长运行时间 | 不如 CTR 高效 |
| **SDA** | 居中 | 成本较高 | 自适应未带来明显收益 |

> 💡 **CTR 表现突出**：它在最后阶段切换为 full-batch，使得最终 gradient estimate 无缩放误差，从而获得接近 FB 的收敛质量，同时节省了前期大量计算资源。

---

### 🔬 消融实验与分析（隐含在不同 scheme 对比中）
- **mini-batch 初期的有效性**：早期使用小 batch 能快速引导粒子进入高密度区域，验证了“先粗后精”策略的合理性。
- **full-batch 收敛的重要性**：仅靠 mini-batch（如 Constant）无法充分收敛，需后期 full-batch refinement 才能达到高性能。
- **自适应 SDA 效果有限**：尽管理论上更智能，但实际中并未超越简单线性或两阶段策略，且实现复杂、计算开销大。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Data Annealing 显著加速 SMC 推理**：
   - 最高实现 **6–8× 的训练加速**，同时保持测试准确率几乎不变（<0.5% 差异）。
2. **Constant-to-refine (CTR) 是最优策略之一**：
   - 前期使用 mini-batch 快速探索，末期 full-batch 精细调整，实现了**速度与精度的最佳平衡**。
3. **HMC 明显优于 LD**：
   - 即使只使用 $ S=3 $ 步 leapfrog，HMC 在所有 setting 下都取得更高准确率，runtime 增加有限。
4. **自适应 SDA 并未显著胜出**：
   - 基于 entropy 控制的 SDA 虽具理论吸引力，但实践中不如手工设计的 schedule 高效。

### ⚠️ 方法的局限性
- **依赖人工设定调度参数**：如 CTR 中的切换点（0.9K）、batch size 等仍需调参。
- **SMC 本身内存消耗大**：需维护多个 particle（即多个 NN 副本），限制了其在超大规模模型上的应用。
- **Smooth DA 实现复杂**：涉及 variance/covariance 估计，对权重稳定性敏感，鲁棒性有待提高。

### 🔮 未来工作方向
1. **自适应 trajectory length**：探索在 HMC proposal 中引入类似 NUTS 的机制，动态调整 leapfrog 步数。
2. **更高效的 L-kernel 设计**：结合 approximately optimal L-kernels [14] 进一步提升采样效率。
3. **分布式 SMC 实现**：利用多设备并行处理 particles，缓解内存压力。
4. **将 DA 思路推广至其他采样器**：如应用于 SGLD、SGHMC 或 SVGD 等随机梯度采样方法。

---

## ✅ 总结一句话
> 本文通过引入 **Data Annealing** 策略，在 SMC 框架中实现了 **高效且准确的 Bayesian NN 推断**，其中 **Constant-to-refine 方案以 6–8 倍加速逼近 full-batch 性能**，为大规模贝叶斯深度学习提供了可行路径。

</details>

---

### 9. [Zonkey: A Hierarchical Diffusion Language Model with Differentiable Tokenization and Probabilistic Attention](https://arxiv.org/abs/2601.21768)

**Authors**: Alon Rozental  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21768v1  

#### Abstract
Large language models (LLMs) have revolutionized natural language processing, yet they remain constrained by fixed, non-differentiable tokenizers like Byte Pair Encoding (BPE), which hinder end-to-end optimization and adaptability to noisy or domain-specific data. We introduce Zonkey, a hierarchical...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《Zonkey: A Hierarchical Diffusion Language Model with Differentiable Tokenization and Probabilistic Attention》总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Large Language Models (LLMs)** 存在以下瓶颈：
- **固定、非可微分的 Tokenizer**（如 BPE）导致无法端到端优化，难以适应噪声文本或特定领域数据；
- **离散 token 化限制了扩散模型（diffusion models）在文本生成中的应用**，因为扩散过程依赖连续空间且对长度敏感；
- 层次化建模中缺乏灵活的可变长度处理机制，通常依赖硬截断（EOS tokens）或固定窗口。

Zonkey 针对上述问题提出了一种全新的 **全可微分、层次化的扩散语言模型框架**。

---

### 🚀 提出的新方法与创新点

#### （1）Differentiable Tokenization via Segment Splitter
- 引入 **Segment Splitter** 作为可学习的、概率性的分词器，直接从字符级输入学习“beginning-of-sequence”（BOS）概率；
- 分割决策基于上下文动态生成（例如空格后更可能为词边界，句号后更可能为句子起始），无需显式标注；
- 利用 **existence probabilities** 和 **existence shares** 实现梯度反向传播，使整个 tokenization 过程可微。

> 💡 *这是首个将分词过程完全纳入端到端训练的语言模型之一。*

#### （2）Probabilistic Attention
- 将序列视为理论上无限长，每个位置 $k$ 具有存在概率 $p_k \in (0,1]$；
- 在注意力计算中引入 **existence ratio $\log(p_k / p_q)$** 调整 attention scores，实现软掩码（soft masking）；
- 支持自然衰减而非强制使用 EOS，允许变量长度输出并保持梯度流动。

> ⚙️ 此机制统一了 padding、truncation 和 variable-length modeling 的处理方式。

#### （3）Hierarchical Diffusion with DDMM
- 提出 **Denoising Diffusion Mixed Model (DDMM)**，结合 DDPM 的稳定性与 DDIM 的高效性；
- 在潜在空间进行去噪重建，支持多层级抽象（character → word-like → sentence-like）；
- 使用 contrastive cosine loss 和 mixed-step objective 提升重建质量与鲁棒性。

#### （4）Differentiable Stitcher
- 设计轻量级、可微的 **Stitcher 模块**，用于无缝拼接重叠段落；
- 利用 cross-attention 对齐重叠区域，纠正去噪误差，增强全局一致性；
- 支持递归层级构建（level-$l$ 输出作为 level-$l+1$ 输入）。

#### （5）End-to-End Trainable Pipeline
- 整个流程从原始字符到文档级表示均可微：  
  `Characters → Segment Splitter → Compressor → DDMM Denoising → Stitcher → Higher Level`
- 所有模块共享损失监督信号，形成闭环优化。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法（如 BPE + Transformer） | Zonkey |
|------|-------------------------------|--------|
| Tokenization | 固定、非可微、OOV 问题严重 | 可学习、可微、自适应分割 |
| 序列长度 | 依赖 EOS 和 padding | 软衰减，支持任意长度 |
| 层次结构 | 多阶段训练或固定规则聚合 | 端到端递归压缩与重建 |
| 扩散模型适用性 | 离散 token 不兼容标准 diffusion | 潜在空间连续 diffusion，支持 denoising |
| 域适应能力 | 难以调整 tokenizer | 动态分割天然适合噪声/领域迁移 |

> ✅ Zonkey 是迈向 **fully gradient-based LLMs** 的重要一步。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 主要训练数据：**Wikipedia 英文语料子集**
- 单 GPU 训练环境，资源受限下验证可行性

### ⚙️ 实验设置
- **层级结构**：实现了两级层次（level 0: 字符 → 类词单元；level 1: 类词 → 类句）
- **最大段长度**（`max_seq_len[l]`）设为 32
- **压缩向量数**（`num_compression_vectors[l]`）设为 4
- **噪声调度**：线性 schedule，从高噪声逐步降至接近零
- **训练目标**：多任务联合优化（见下文损失函数）

### 🎯 评估指标（Qualitative为主）
由于模型无固定 vocabulary、输出为连续 latent 表示，**标准 token-level 指标（如 perplexity）不可用**，因此采用：
- **定性分析**：
  - 生成文本的连贯性（coherence）
  - 是否出现词级/句级结构（emergent hierarchy）
  - 自适应分词是否符合语言直觉（如空格/句号处分割）
- **辅助定量指标**（仅用于训练监控）：
  - Reconstruction loss（contrastive cosine similarity）
  - MLM loss（masked prediction accuracy）
  - Average BOS probability
  - Collapse prevention loss

### 🆚 基线方法对比
未提供与主流 LLM（如 GPT、LLaMA）的直接量化比较，原因如下：
- 架构差异巨大（diffusion vs autoregressive）
- 输出形式不同（latent vectors vs discrete tokens）

但文中指出其设计优于以下方向：
- **ByT5 / CANINE**：虽操作于字节/字符，但无动态分词与层次扩散
- **Byte Latent Transformer (BLT)**：虽有 learnable patching，但以 entropy 平衡为目标，不如 Zonkey 与下游任务对齐
- **Hierarchical Diffusion Language Models (HDLM)**：仍基于离散 token，缺乏全可微性

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能表现（Qualitative Results）

| 指标 | 结果描述 |
|------|----------|
| **文本生成质量** | 可从纯噪声生成语法合理、主题一致的英文句子（如：“Quantum computing harnesses quantum mechanics...”） |
| **层次结构涌现** | Level 0 自动在空格处分割 → 类词单位；Level 1 在句号后分割 → 类句单位，无需监督 |
| **自适应分词可视化** | BOS 概率热图显示在 punctuation 和 space 处显著升高，表明语义感知分割 |
| **变量长度生成** | 可生成不同长度文本，尾部通过 $p_{\text{exist}} < \epsilon$ 自动截断 |
| **infilling 能力** | 支持非顺序补全，如给定前缀 “Bob is a” 和后缀 “player in the NBA”，成功补全为 “basketball” |

### 🔬 消融实验（Ablation Studies）
尽管未列出完整表格，但文中通过模块功能说明了各组件必要性：

| 模块 | 移除后果 |
|------|--------|
| **Probabilistic Attention** | 梯度中断，variable-length modeling 不稳定 |
| **Existence Shares** | Splitter 倾向于集中在简单区域频繁分割，忽略复杂部分 |
| **Mixed-step Objective in DDMM** | 去噪路径保守，收敛慢，难以跳跃恢复 |
| **Stitcher Overlap Losses** | 段间不一致增加，影响高层抽象稳定性 |

> 实验表明：所有模块协同作用，共同促成 emergent linguistic structure。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **可微分 tokenization 是可行的**：Segment Splitter 能自发学习类语言边界的分割策略；
2. **Probabilistic Attention 支持无限序列建模**：软存在概率替代硬 mask/EOS，提升灵活性与可导性；
3. **层次化扩散可在 latent space 成功运行**：DDMM 实现稳定去噪与高质量重建；
4. **emergent hierarchy 无需监督**：词级与句级抽象在 reconstruction pressure 下自然浮现；
5. **end-to-end training 推动全局最优**：各模块（splitter, compressor, denoiser, stitcher）相互激励，形成正反馈循环。

---

### ⚠️ 方法的局限性
1. **当前仅为 proof-of-concept**：
   - 仅训练至 sentence-level coherence（level 0–1）
   - 缺乏 paragraph 或 document 级别生成能力
2. **计算资源限制**：
   - 单 GPU 训练，尚未扩展到更大规模
   - 层级深度受限
3. **缺乏标准化评估**：
   - 无法与主流 LLM 进行公平的 perplexity 或 BLEU 对比
4. **推理速度尚待验证**：
   - 尽管宣称并行生成潜力大，但实际 latency 未测量

---

### 🔮 未来工作方向
1. **Scaling to deeper hierarchies**：
   - 构建 paragraph-level 及以上抽象
   - 实现 long-context generation（>10k tokens）
2. **大规模训练与 benchmarking**：
   - 在更大语料上训练，开展 human evaluation 与 automatic metric 对比
3. **Domain Adaptation 实验**：
   - 测试在 noisy text、code、medical 等领域的适应能力
4. **Integration with Autoregressive Heads**：
   - 探索 hybrid generation（diffusion + AR）以兼顾多样性与精确性
5. **Open-source 生态建设**：
   - 已开源代码（GitHub: [ARozental/Zonkey](https://github.com/ARozental/Zonkey)），鼓励社区复现与改进

---

## 总结

Zonkey 提出了一种革命性的 **全可微分、层次化扩散语言模型架构**，突破了传统 LLM 中 **固定 tokenizer 与离散 token 的限制**。它通过 **Segment Splitter + Probabilistic Attention + DDMM + Stitcher** 四大核心组件，首次实现了从字符到句子的端到端可导 pipeline，并展示了 **emergent linguistic structure** 与 **variable-length diffusion generation** 的潜力。

虽然目前仍处于原型阶段，但其设计理念为下一代 **scalable、adaptive、真正 gradient-based language models** 指明了新方向。

</details>

---

### 10. [ECO: Quantized Training without Full-Precision Master Weights](https://arxiv.org/abs/2601.22101)

**Authors**: Mahdi Nikdan, Amir Zandieh, Dan Alistarh, Vahab Mirrokni  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22101v1  

#### Abstract
Quantization has significantly improved the compute and memory efficiency of Large Language Model (LLM) training. However, existing approaches still rely on accumulating their updates in high-precision: concretely, gradient updates must be applied to a high-precision weight buffer, known as $\textit...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**ECO: Quantized Training without Full-Precision Master Weights**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在大型语言模型（LLM）的量化训练中，尽管前向和反向计算已广泛采用低精度（如 FP8 或 INT4），但大多数现有方法仍依赖**全精度主权重（full-precision master weights）** 来累积梯度更新。这种高精度参数副本（通常为 FP32）带来了显著的内存开销，尤其是在 **Sparse Mixture of Experts (SMoE)** 模型中，主权重和优化器状态共同主导了显存占用。

该问题限制了量化带来的实际内存收益——即使激活值和部分计算被量化，模型权重的内存占用仍接近原始高精度水平。

---

### 🚀 提出的新方法：Error-Compensating Optimizer (ECO)

作者提出 **ECO（Error-Compensating Optimizer）**，一种无需主权重的量化训练方法，其核心思想是：

> **将每次权重量化后产生的误差注入到优化器的动量缓冲区（momentum buffer）中，形成一个“无额外内存开销”的误差反馈（error feedback）机制。**

具体流程如下：
1. 在每个训练步骤中，直接对量化后的参数应用优化器更新（如 SGDM 或 Adam）。
2. 更新后得到临时高精度参数，立即进行量化。
3. 计算量化误差 $ e_{t+1} = \theta_{t+1} - q(\theta_{t+1}) $。
4. 将该误差按比例 $ \alpha = (1-\beta)/\eta $ 注入当前的 momentum buffer 中，使得丢失的更新信息得以保留并在后续步骤中补偿。

这一设计的关键在于：**复用现有的 momentum buffer 存储量化误差，避免引入额外的 error buffer，实现零额外内存开销。**

---

### 🔍 相比现有方法的优势

| 方面 | ECO 的优势 |
|------|-----------|
| **内存效率** | 完全消除主权重存储，静态内存最多可减少 **25%**（尤其在 SMoE 模型中）。 |
| **通用性与兼容性** | 支持主流优化器（SGDM、Adam），可与 FP8、INT4 等多种量化格式结合，适用于 Dense 和 MoE 架构。 |
| **理论保障** | 在标准非凸假设下，证明 ECO 收敛至最优解的一个常数半径邻域，且噪声地板（noise floor）仅比使用主权重的方法差一个常数因子；而朴素移除主权重会导致误差随学习率衰减发散。 |
| **实现简单** | 仅需在每步末尾添加一次 element-wise 的误差注入操作，无需调参或复杂重构。 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **Scaling Law 实验**：使用 `C4` 数据集，训练 token 数为模型参数量的 100 倍（即 5× Chinchilla 最优规模）。
- **Gemma-3 1B 预训练**：基于 `C4` 数据集训练 40B tokens。
- **SMoE 2.1B 预训练**：在 `LM1B` 数据集上训练，token 数为活跃参数数量的 100 倍。
- **DeepSeek-MoE-16B 微调**：在 `OpenAssistant-Guanaco` 数据集上微调 3 轮。

---

### ⚙️ 实验设置
| 项目 | 设置详情 |
|------|--------|
| **模型范围** | 30M–800M 小模型、Gemma-3 1B、SMoE 2.1B、DeepSeek-MoE-16B |
| **量化配置** |  
- FP8 实验：主要针对 Transformer 块中的线性层（排除嵌入层）
- INT4 实验：用于 DeepSeek-MoE-16B 的 weight-only QAT
- 量化方式：支持 **Round-to-Nearest (RTN)** 和 **Stochastic Rounding (SR)** |
| **优化器** | AdamW（$ \beta_1=0.9, \beta_2=0.98 $），学习率采用线性预热 + 余弦退火 |
| **评估指标** |  
- 主要指标：**Validation Loss**
- 下游任务：**Zero-shot Accuracy**（ARC-C, ARC-E, GSM8K, HellaSwag, PIQA, MMLU）
- 内存指标：**Static Memory Usage (MB)**，重点关注峰值内存中主权重占比 |

---

### 🆚 基线方法对比
| 基线名称 | 描述 |
|--------|------|
| **BF16 w/ MW** | FP32 主权重 + BF16 计算，标准混合精度基准 |
| **FP8 w/ MW + RTN/SR** | FP32 主权重 + FP8 前向传播（分别用 RTN/SR） |
| **FP8 w/o MW + RTN/SR** | 无主权重，直接量化更新（朴素方法） |
| **FP8 w/o MW ECO + RTN/SR** | 本文提出的 ECO 方法（带/不带动量误差注入） |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### ✅ 表格 1 & 图 3：验证损失（Validation Loss）对比（越低越好）

| 方法 | 30M | 800M | Gemma-3 1B | SMoE 2.1B | DeepSeek-MoE-16B |
|------|-----|------|------------|-----------|------------------|
| **FP8 w/ MW + SR** | 3.3309 | N/A | ~3.09 | ~2.92 | — |
| **FP8 w/o MW + SR** | 3.4008 | 2.9471 | >3.1 (发散) | >3.1 (发散) | — |
| **FP8 w/o MW ECO + SR** | **3.3317** | **2.5399** | **~2.76** | **~2.76** | — |
| **INT4 w/o MW ECO + SR** | — | — | — | — | **匹配主权重基线** |

> 💡 **观察**：ECO 几乎完全恢复了主权重方法的精度，而朴素移除主权重则严重发散或性能大幅下降。

---

#### ✅ 表格 2：DeepSeek-MoE-16B 零样本评测准确率

| 方法 | ARC-C | ARC-E | GSM8K | HellaSwag | PIQA | MMLU |
|------|-------|-------|--------|-----------|------|------|
| **INT4 w/ MW + SR** | 48.55 | 71.13 | 16.15 | 78.78 | 80.90 | 38.57 |
| **INT4 w/o MW ECO + SR** | **48.55** | **71.17** | **16.00** | **78.84** | **81.50** | **38.41** |

> ✅ **结论**：ECO 在 **INT4 精度下实现了与主权重方法几乎一致的零样本性能**，部分指标甚至略有提升。

---

#### ✅ 内存节省效果（图 1）
- **静态内存减少高达 25%**，特别是在 SMoE 模型中（因专家参数多，主权重占比较大）。
- ECO 显著改善了 **memory vs. validation loss 的 Pareto frontier**，即在相同内存预算下达到更低损失，或在相同性能下使用更少内存。

---

#### 🔬 消融实验与分析
- **误差连续性验证（图 2）**：相邻步骤间的量化误差 $ e_t $ 与 $ e_{t+1} $ 具有高度相似性（cosine similarity > 0.999），支持了 ECO 中“用 $ e_{t+1} $ 近似 $ e_t $”的启发式有效性。
- **SR vs. RTN 对比**：ECO 在使用 **Stochastic Rounding (SR)** 时表现最佳，因其提供无偏估计；而 RTN 引入系统偏差，导致更高噪声地板（与理论一致）。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **主权重并非必需**：通过将量化误差注入 momentum buffer，ECO 成功实现了无需主权重的稳定量化训练。
2. **ECO 实现近无损压缩**：在 FP8 和 INT4 精度下，ECO 可以 **几乎完全匹配主权重基线的训练损失和下游任务性能**。
3. **显著内存节省**：在典型设置下可减少 **~25% 的静态内存占用**，特别有利于大规模 MoE 模型部署。
4. **理论与实践一致**：理论证明 ECO 收敛于常数半径邻域，而朴素方法误差会随学习率衰减趋于无穷，实验证实了这一点。

---

### ⚠️ 局限性
1. **依赖 Stochastic Rounding (SR)**：ECO 的最强理论保证依赖于 SR 提供的零均值误差特性。若硬件仅支持 RTN，则性能上限受限。
2. **RTN 场景仍有差距**：虽然优于朴素方法，但在纯 RTN 设置下仍略逊于主权重 + RTN 的最优组合。
3. **未探索极端低位宽（如 INT2）**：目前实验集中在 FP8 / INT4，更低精度下的稳定性尚待验证。

---

### 🔮 未来工作方向
1. **扩展至其他优化器结构**：如 Adafactor、Lion 等低内存优化器，进一步降低整体训练成本。
2. **结合 optimizer state quantization**：将 ECO 与 4-bit momentum 压缩（如 4-bit Adam）结合，实现端到端极低内存训练。
3. **硬件协同设计**：推动 SR 在 GPU/NPU 上的原生支持，使 ECO 更易落地。
4. **应用于更大规模模型**：如 100B+ 参数的 MoE 模型，在真实分布式训练环境中验证可扩展性。

---

## ✅ 总结一句话
> **ECO 是首个通用、高效、无需主权重的大模型量化训练框架，通过动量缓冲区复用实现误差反馈，在几乎不失效的前提下减少高达 25% 的静态内存，为下一代低精度训练提供了坚实基础。**

</details>

---

### 11. [SENDAI: A Hierarchical Sparse-measurement, EfficieNt Data AssImilation Framework](https://arxiv.org/abs/2601.21664)

**Authors**: Xingyue Zhang, Yuxuan Bao, Mars Liyao Gao, J. Nathan Kutz  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.21664v1  

#### Abstract
Bridging the gap between data-rich training regimes and observation-sparse deployment conditions remains a central challenge in spatiotemporal field reconstruction, particularly when target domains exhibit distributional shifts, heterogeneous structure, and multi-scale dynamics absent from available...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SENDAI: A Hierarchical Sparse-measurement, EfficieNt Data AssImilation Framework

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文旨在解决**在观测极度稀疏（sparse observations）且目标域存在分布偏移（distributional shift）时，如何准确重建时空场（spatiotemporal field）**的问题。这在遥感领域尤为关键，例如卫星影像常因云层遮挡、传感器故障等导致大量数据缺失，而传统方法通常需要密集观测才能实现高质量重建。

此外，现有深度学习方法往往依赖大规模GPU训练、高密度标注数据，难以部署于资源受限或实时监控场景（如农业监测、灾害响应），且其隐空间表示常混合多种物理效应，缺乏可解释性。

---

### 提出了什么新方法或新思路
作者提出了 **SENDAI**（Sparse-measurement, EfficieNt Data AssImilation）框架，一种**分层式稀疏测量数据同化架构**，结合模拟先验与学习型差异校正，从极稀疏传感器观测中重建完整空间状态。

其核心创新包括三个层面：

#### （1）极端稀疏条件下的有效重建（Extreme Sparsity Reconstruction）
- 仅使用 **64个传感器**（覆盖约 **1.56%** 的空间区域）即可完成全图重建。
- 远低于传统方法所需的观测密度阈值，适用于真实世界中传感器布设稀疏的场景。

#### （2）轻量化计算设计（Computational Efficiency）
- 架构轻量，可在标准CPU上运行，单站点训练时间仅需数分钟。
- 支持在边缘设备、低带宽或深空探测等资源受限环境中部署。

#### （3）分层频率剥离策略（Hierarchical Frequency Peeling）
- 将重建任务分解为两个互补路径：
  - **低频路径（LF Pathway）**：基于 **SHRED** 框架，利用 **LSTM Encoder + 浅层MLP Decoder** 学习主导动态，并通过 **Latent GAN 对齐** 实现模拟到真实数据的分布迁移适应。
  - **高频路径（HF Pathway）**：引入**逐层频率剥离机制**，结合 **coordinate-based Implicit Neural Representation (INR)** 分离并建模细粒度结构（如边界、梯度、局部异常）。
- 引入 **spectral constraint** 和 **frequency exclusion mechanism**，确保各层提取不同频段成分，避免模式混叠（mode interference），提升模型稳定性和可解释性。

---

### 相比现有方法的优势
| 维度 | SENDAI优势 |
|------|-----------|
| **数据效率** | 在仅1.56%观测下仍能保持高保真重建，远超传统方法需求 |
| **计算效率** | 可在CPU运行，训练/推理速度快，适合实时应用 |
| **结构保持能力** | 显著优于插值类方法，能保留田块拓扑、土地覆盖断点、空间梯度等诊断相关结构 |
| **可解释性** | 高频修正项具有谱分离特性，便于后续反演间接变量（如土壤湿度、地表温度） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **MODIS 卫星遥感数据**（Terra/Aqua平台）
- 主要状态变量为 **NDVI**（Normalized Difference Vegetation Index）
- 数据来源：Google Earth Engine
- 时间范围：2023年
- 空间分辨率：重采样至统一的 **64×64 像素网格**（对应实际面积15km×15km）

### 实验设置
- **研究地点**：全球六个异质性显著的站点，涵盖多种气候类型：
  - 地中海气候（Central Valley, Guadalquivir Valley）
  - 温带大陆性气候（Corn Belt）
  - 干旱区（Imperial Valley, Tarim Basin）
  - 亚热带气候（Riverina）
- **季节划分策略**：
  - 使用一个季节作为“模拟”数据（simulation），另一个季节作为“真实”观测（ground truth）
  - 利用季节更替模拟 **sim2real domain shift**
- **传感器配置**：
  - 固定使用 **64个随机布置的传感器**（占总像素1.56%）
  - 输入为传感器历史序列（time-delay embedding，长度L=5）

### 评估指标
- **SSIM**（Structural Similarity Index Measure）为主指标，强调对空间结构、纹理、边界的保持能力
- **RMSE** 作为辅助定量误差指标
- 特别指出：某些方法（如Kriging）可能RMSE较低但SSIM极差，说明其过度平滑、破坏拓扑结构

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **SG+IDW** | 传统流程 | Savitzky-Golay滤波 + 反距离加权插值 |
| **HANTS+IDW** | 传统流程 | 谐波分析去云 + IDW插值，显式建模植被季节性 |
| **Kriging** | 地统计学 | 高斯过程回归（RBF核），每帧独立拟合 |
| **MMGN** | 深度学习 | 基于Gabor滤波的隐式神经网络，最新连续场重建方法 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ SENDAI Jr.（简化版，仅低频路径）
适用于低频主导变化场景（如平稳农业区）：

| 站点 | 最佳基线 SSIM | SENDAI Jr. SSIM | 提升幅度 |
|------|----------------|------------------|---------|
| Central Valley | 0.2612 | **0.5747** | **+120%** |
| Corn Belt | 0.1588 | **0.4530** | **+185.3%** |
| Guadalquivir Valley | 0.1849 | **0.3655** | **+97.7%** |

> 💡 在Corn Belt达到 **185% SSIM提升**，是文中最大改进。

#### ✅ 完整SENDIA（含高频剥离路径）
用于复杂地形与高频动态场景：

| 站点 | Cheap2Rich SSIM | SENDAI SSIM | 提升幅度 |
|------|------------------|-------------|----------|
| Imperial Valley | 0.4041 | **0.4668** | +15.5% |
| Tarim Basin | 0.3505 | **0.4777** | **+36.3%** |
| Riverina | 0.2761 | **0.3354** | +21.5% |

> 💡 在 **Tarim Basin** 上相比Cheap2Rich提升达 **36.3% SSIM**，体现高频剥离的有效性。

---

### 与基线方法的对比结果
- 所有基线方法均严重破坏空间结构：
  - **IDW类方法** 出现“牛眼”伪影（bullseye artifacts）
  - **Kriging** 导致过度平滑，完全抹除边界信息（如Tarim Basin的山盆交界）
- MMGN虽为深度学习方法，在极端稀疏条件下表现不佳，SSIM普遍低于0.13
- SENDAI不仅SSIM领先，**RMSE也同步下降**，表明其不是以牺牲精度换取结构保真

---

### 消融实验结果（Ablation Studies）

#### （1）频率剥离 vs 联合发现（Joint Discovery）
- 在合成旅行波系统中测试：
  - 联合发现导致频谱能量扩散，出现非目标模式泄漏
  - 层次剥离则输出频谱纯净，各层分别捕获目标频率（k=5, k=11），无干扰
- 结论：层次结构有助于**物理可解释性**与**模块化扩展**

#### （2）INR解码器 vs 直接MLP映射**
- 直接MLP易产生“点状”伪影（dotted artifacts），局限于传感器位置附近
- INR结合 **Fourier positional encoding** 后，生成空间连贯的平滑插值，显著改善视觉质量

#### （3）传感器数量敏感性分析**
- 图5a显示：SSIM随传感器数量增加单调上升
- 但在 **64传感器（1.56%）** 已接近饱和，边际增益递减
- 表明该配置在实用性与性能之间取得良好平衡

---

## 4. 关键结论和发现

### 主要发现
1. **分层建模优于端到端学习**：将重建任务按频率尺度拆解（LF + HF），能更有效地捕捉多尺度动态，尤其适用于具有尖锐边界和亚季节动态的景观。
2. **结构相似性（SSIM）比RMSE更重要**：对于下游任务（如作物分类、异常检测），保持拓扑结构比降低像素级误差更具价值。
3. **轻量模型也能超越重型DL模型**：尽管不使用Transformer或大规模参数网络，SENDAI凭借结构设计在稀疏条件下全面超越MMGN等先进INR方法。
4. **高频修正具有物理意义**：在Tarim Basin案例中，HF1反映气候驱动的物候偏移，HF2揭示水文持久性控制，HF3对应土壤异质性，验证了分层剥离的可解释潜力。

---

### 方法的局限性
1. **假设空间结构静态**：当前框架假设模拟期与真实期的空间格局不变，无法处理土地覆被变化、人为扰动等情况。
2. **传感器布局随机**：未采用信息论指导的主动采样策略（如基于不确定性或熵最大化），未来可通过优化布点进一步减少所需传感器数量。
3. **跨大陆泛化待验证**：目前为区域独立训练，尚未进行严格的跨洲迁移测试，通用性有待加强。
4. **单变量重建**：当前仅处理NDVI，未考虑多变量联合重建（如NDVI + LST + SM），未能挖掘变量间耦合关系。

---

### 未来工作方向
1. **拓展至非稳态场景**：引入时间演化编码器或动态图模型，应对土地利用变化与管理干预。
2. **信息导向的传感器设计**：结合互信息或贝叶斯实验设计（Bayesian optimal design）优化传感器布局。
3. **跨区域迁移学习**：探索共享latent space或meta-learning策略，实现跨气候区快速适配。
4. **多变量联合重建**：构建multi-task INR框架，利用变量间的物理关联提升整体重建一致性。
5. **应用于其他地球科学任务**：如土壤湿度、地表温度、积雪深度、洪水制图等，推动物理引导的稀疏感知范式。

---

## 总结
**SENDAI** 提出了一种新颖、高效且结构感知的稀疏数据同化框架，成功解决了遥感中“少观测 + 大偏移”的核心挑战。其实验结果证明，在仅 **1.56% 观测密度** 下，仍能实现高达 **185% SSIM 提升**，并在多个异质性地貌中稳健表现。该工作不仅提供了实用工具，也为未来资源受限环境下的智能遥感系统设计指明了方向。

</details>

---

### 12. [Scalable Power Sampling: Unlocking Efficient, Training-Free Reasoning for LLMs via Distribution Sharpening](https://arxiv.org/abs/2601.21590)

**Authors**: Xiaotong Ji, Rasul Tutunov, Matthieu Zimmer, Haitham Bou Ammar  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.21590v1  

#### Abstract
Reinforcement learning (RL) post-training is a dominant approach for improving the reasoning performance of large language models (LLMs), yet growing evidence suggests that its gains arise primarily from distribution sharpening rather than the acquisition of new capabilities. Recent work has shown t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scalable Power Sampling: Unlocking Efficient, Training-Free Reasoning for LLMs via Distribution Sharpening

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前，**Reinforcement Learning (RL)** 后训练是提升大语言模型（LLMs）推理能力的主流方法。然而，越来越多的研究表明，RL 的性能增益主要来自于对生成分布的“**Distribution Sharpening**”（分布锐化），而非真正习得新的推理能力。这意味着，高质量的推理路径其实已经存在于基础模型中，只是概率较低。

尽管已有研究提出通过从 LLM 的 **power distribution** 中采样来实现无需训练的推理增强（如 Karan & Du, 2025），但其依赖 **Markov Chain Monte Carlo (MCMC)** 方法进行采样，计算成本极高，导致推理延迟严重，难以在实际场景中应用。

### 提出了什么新方法或新思路
本文提出了 **Scalable Power Sampling (SPS)**，一种**无需训练、无需验证器（training-free and verifier-free）** 的高效推理算法，旨在解决 MCMC 方法的高延迟问题。

其核心理论创新在于：
- **定理 3.1**：证明了全局的 power distribution 可以被分解为一个**token-level 的缩放低温度分布（scaled low-temperature distribution）**。
- 具体而言，`p_power(x_t | q, x_{<t}) ∝ p(x_t | q, x_{<t})^α * S_t(x_t)`，其中 `S_t(x_t)` 是一个**缩放因子（scaling factor）**，它捕获了选择该 token 后对未来轨迹质量的期望。
- 这意味着，原本需要全局优化的 power sampling，可以通过在每一步对 token 概率进行局部重加权来近似。

### 相比现有方法的优势
- **效率极高**：相比 MCMC 方法，**推理延迟降低了超过 10 倍**，使其具备了实际部署的可行性。
- **性能卓越**：在多个基准测试上，性能**匹配甚至超越了一次性的 GRPO (Group Relative Policy Optimisation)**，而 GRPO 需要复杂的 RL 训练流程和外部奖励信号。
- **完全无训练**：不依赖任何参数更新或外部奖励，仅通过改进推理时的采样策略即可释放基础模型的潜力。
- **保留多样性**：相比 GRPO 等 RL 方法容易导致的“diversity collapse”（多样性崩溃），SPS 在提升 `pass@1` 的同时，更好地保留了生成路径的多样性。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验在三个具有代表性的基准上进行：
- **MATH500**：数学推理任务，评估多步问题解决能力，使用最终答案精确匹配作为指标。
- **HumanEval**：代码生成任务，包含 164 个 Python 编程题，通过功能正确性（functional correctness）评估。
- **GPQA-Diamond**：知识密集型问答任务，包含 198 道由专家编写的生物、物理、化学难题，用于挑战非专家水平。

### 实验设置和评估指标
- **模型**：在四个不同的 LLM 上进行了评估：
  - `Qwen2.5-7B`（通用模型）
  - `Qwen2.5-Math-7B` 和 `DeepSeek-Math-7B`（数学微调模型）
  - `DeepSeek-Math-7B-RL`（经过 GRPO RL 微调的模型）
- **评估指标**：主要使用 `pass@1` 准确率，并辅以 `pass@K` 分析多样性和推理效率（每提示的推理时间，单位：分钟）。
- **超参数**：SPS 方法中，power exponent `α=4`，候选 token 数 `K_t=8`，Monte Carlo rollout 数 `M_t=8`。

### 基线方法对比
- **Base**：基础模型的标准解码。
- **Low-temperature**：低温采样 (`T=0.25`)。
- **Best-of-N**：生成 32 个样本，选择基础模型得分最高的一个。
- **MCMC Power Sampling**：Karan & Du (2025) 提出的基于 MCMC 的 power sampling 方法。
- **GRPO (MATH)**：在 MATH 数据集上进行 RL 微调后的模型，作为性能上限的参照。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
根据 **Table 1** 的 `pass@1` 结果：

| Model / Method | MATH500 | HumanEval | GPQA |
| :--- | :--- | :--- | :--- |
| **Qwen2.5-Math-7B + Ours** | **0.758** | **0.604** | **0.409** |
| Qwen2.5-Math-7B + MCMC | 0.748 | 0.573 | 0.389 |
| Qwen2.5-Math-7B + GRPO | 0.785 | 0.537 | 0.399 |

### 与基线方法的对比结果
- **性能对比**：
  - SPS (**Ours**) 在所有模型和任务上均**显著优于** `Low-temperature` 和 `Best-of-N`。
  - SPS 性能**匹配或超越**了计算成本高昂的 `MCMC Power Sampling`。
  - 最令人瞩目的是，在 `HumanEval` 和 `GPQA` 任务上，SPS **超越了经过 RL 微调的 GRPO 模型**，证明了其强大的泛化能力。
- **效率对比**：
  - 如 **Figure 2** 所示，SPS 的推理时间远低于 MCMC。例如，在 `Qwen2.5-Math-7B` 上处理 MATH500 任务，MCMC 平均耗时 **2.5 分钟/提示**，而 SPS 仅需 **0.22 分钟/提示**，实现了约 **11.4 倍**的速度提升。
- **对已微调模型的有效性**：
  - 如 **Table 2** 所示，将 SPS 应用于已经过 GRPO 微调的 `DeepSeek-Math-7B-RL` 模型，依然能带来小幅但稳定的性能提升，说明 SPS 与 RL 微调是正交且可叠加的技术。

### 消融实验结果
- **超参数敏感性**（Figure 4 & 6）：
  - 性能对 `K_t` 和 `M_t` 相对鲁棒，中等值（如 8）即可取得良好效果。
  - 性能对 `α` 敏感，`α=4` 或 `5` 时效果最佳，`α=8` 时因过度锐化反而性能下降。
- **多样性分析**（Figure 3 & 5）：
  - GRPO 虽然提升了 `pass@1`，但随着 `K` 增大，性能增长迅速饱和，表现出明显的 **diversity collapse**。
  - SPS 和 MCMC 方法则保持了与基础模型相似的 `pass@K` 增长曲线，说明它们在锐化分布的同时，成功保留了生成的多样性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **RL 的增益本质是分布锐化**：论文的实证结果强有力地支持了“RL post-training 的主要作用是分布锐化”的假设。因为一个完全无训练、无奖励的方法（SPS）就能达到甚至超越 RL 模型的性能。
2. **基础模型蕴含巨大潜力**：高质量的推理能力并非 RL “教会”模型的，而是本来就“潜藏”在基础模型中。SPS 通过更智能的采样策略，有效地“解锁”了这些能力。
3. **高效的近似是可行的**：通过理论推导，可以将复杂的全局 power sampling 近似为高效的、自回归的 token-level 操作，从而在保证性能的同时，彻底摆脱 MCMC 的迭代开销。

### 方法的局限性
- **计算开销仍高于标准解码**：虽然比 MCMC 快 10 倍以上，但 SPS 由于需要进行 Monte Carlo rollout，其推理成本仍然显著高于标准的 `low-temperature` 采样。
- **依赖于基础模型的质量**：如果基础模型本身存在严重的安全对齐问题或偏见，SPS 的“锐化”效应可能会放大这些有害行为。
- **理论近似的误差**：尽管有理论支撑，但用有限的 rollout (`M_t`) 来估计 `S_t(x_t)` 仍会引入方差和偏差。

### 未来工作方向
- **降低方差**：探索使用控制变量（control variates）等技术来减少 Monte Carlo 估计的方差。
- **自适应计算预算**：根据生成过程中的不确定性动态调整 `K_t` 和 `M_t`，以实现更好的精度-效率权衡。
- **结合推测解码（speculative decoding）**：利用更快的草稿模型来加速 rollout 过程，进一步摊销成本。
- **扩展到 Agent 场景**：将 SPS 应用于更复杂的基于 LLM 的智能体（agentic settings）决策过程中。

</details>

---

### 13. [MoHETS: Long-term Time Series Forecasting with Mixture-of-Heterogeneous-Experts](https://arxiv.org/abs/2601.21866)

**Authors**: Evandro S. Ortigossa, Guy Lutsker, Eran Segal  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.21866v1  

#### Abstract
Real-world multivariate time series can exhibit intricate multi-scale structures, including global trends, local periodicities, and non-stationary regimes, which makes long-horizon forecasting challenging. Although sparse Mixture-of-Experts (MoE) approaches improve scalability and specialization, th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoHETS: Long-term Time Series Forecasting with Mixture-of-Heterogeneous-Experts 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现实世界中的多变量时间序列通常具有复杂的多尺度结构，如全局趋势、局部周期性和非平稳动态，这使得**长时程预测**（long-horizon forecasting）极具挑战。现有的基于 **sparse Mixture-of-Experts (MoE)** 的方法虽然提升了可扩展性和专业化能力，但其专家网络普遍采用同质化的 **MLP** 结构，难以有效捕捉时间序列中多样化的动态模式。

### 提出的新方法与创新思路
本文提出了 **MoHETS** ——一种基于 **encoder-only Transformer** 的新型时间序列预测模型，其核心是引入 **Mixture-of-Heterogeneous-Experts (MoHE)** 层，实现对异构时间模式的专业化建模。

#### 主要创新点包括：
- **MoHE 架构设计**：
  - 将时间序列切分为 **temporal patches**，并由路由机制将不同 patch 分配给不同的专家网络。
  - **共享专家**（shared expert）：采用 **depthwise-convolution**（DwConvFFN），负责建模序列级的连续性与长期趋势。
  - **路由专家**（routed experts）：采用 **Fourier-based networks**（FA-FFN），在频域中捕捉 patch 级别的周期性结构。
  - 这种“共享 + 路由”的异构专家组合实现了对全局趋势与局部周期性的结构性解耦。

- **外生变量融合机制**：
  - 引入 **multimodal cross-attention** 模块，通过对外生协变量（covariates）进行 patch embedding，并与主序列进行跨模态注意力交互，增强模型对非平稳动态的鲁棒性。

- **轻量化解码器设计**：
  - 替代传统的参数密集型线性投影头（linear projection head），提出 **convolutional patch decoder**，利用轻量卷积结构进行上采样，提升参数效率、减少训练不稳定性，并支持单一模型泛化于任意预测长度。

### 相比现有方法的优势
- **更强的表达能力**：异构专家设计使模型能针对不同时间模式（趋势 vs 周期）使用最适合的算子，避免了 MLP 对复杂信号建模的“一刀切”问题。
- **更高的参数效率与稳定性**：卷积解码器显著减少了输出层参数量，缓解过拟合风险。
- **更好的泛化能力**：支持任意预测长度的自回归滚动预测，无需为每个 horizon 单独训练模型。
- **更强的鲁棒性**：结合外生变量与 Huber loss，有效应对异常值与非平稳性。

---

## 2. 核心实验方法和设置

### 数据集
在 **7 个广泛使用的多变量时间序列基准数据集** 上进行了验证，涵盖多种领域与频率：
- **电力负荷**：ETTh1, ETTh2（小时级）、ETTm1, ETTm2（15分钟级）
- **气象数据**：Weather（10分钟级，21维）
- **用电量**：ECL（小时级，321维）
- **交通流量**：Traffic（小时级，862维）

这些数据集具有不同的维度、频率和可预测性（forecastability），覆盖了从低维到高维、从细粒度到粗粒度的多样化场景。

### 实验设置
- **输入长度**（look-back window）：统一设为 $ L = 672 $（约4周的小时数据），以平衡上下文信息与计算开销。
- **预测长度**（forecasting horizon）：测试 $ H \in \{96, 192, 336, 720\} $ 多个长时程设定。
- **评估指标**：
  - **MSE**（Mean Squared Error）
  - **MAE**（Mean Absolute Error）
- **训练策略**：
  - 使用 **Huber loss** 作为主损失函数，提升对异常值的鲁棒性。
  - 引入 **auxiliary load balancing loss** 防止 MoE 中的路由崩溃（routing collapse）。
  - 采用 **autoregressive rolling forecasting** 进行推理，支持任意长度预测。

### 基线方法对比
共对比了 **15 种先进基线模型**，包括：
- **Transformer 类**：iTransformer, PatchTST, Crossformer, FEDformer, TimeXer, Timer-XL
- **MoE 类**：Time-MoE, Moirai-MoE
- **其他 SOTA 模型**：SOFTS, TimeMixer, TimesNet, TiDE, DLinear
- **大模型/基础模型**：Time-MoE, Moirai, MOMENT, Chronos

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在 **所有 7 个数据集和多个预测长度下**，MoHETS 均取得 **state-of-the-art 性能**。
- 平均 **MSE 相比最强基线降低 12%**（原文称 12.3%）。
- 具体提升示例：
  - 在 **ETTh1** 上，相比 TimeXer，平均 MSE 下降 **12.3%**
  - 在 **Weather** 上，相比 TimeMixer，下降 **10%**
  - 在 **Traffic** 上，相比 SOFTS，下降 **5.1%**

### 与基线方法的对比结果
| 模型 | 平均 MSE | 排名 |
|------|--------|-----|
| **MoHETS** | **0.297** | **1st** |
| SOFTS | 0.334 | 2nd |
| TimeXer | 0.334 | 3rd |
| iTransformer | 0.342 | 4th |
| TimeMixer | 0.353 | 5th |

> MoHETS 在所有数据集上均排名第一，且在强季节性数据（如 ETTh1, ETTm2）上优势尤为明显。

### 消融实验结果
#### （1）Transformer 主干结构
- **Encoder-only** 显著优于 decoder-only 设计：
  - 在 ETTh1 上 MSE 降低 **8.1%**
  - 在 Traffic 上降低 **9.6%**
- 原因：encoder 更适合处理长上下文，且更灵活地支持变长输出。

#### （2）MoHE 专家配置
| 配置 | ETTh1 MSE | Traffic MSE |
|------|----------|-----------|
| All MLP-FFN (标准 MoE) | 0.399 | 0.498 |
| **MoHETS (DwConv + FA-FFN)** | **0.383** | **0.406** |
| All FA-FFN | 0.391 | 0.428 |
| Conv + MLP | 0.398 | 0.435 |

> 异构专家组合在 ETTh1 上相对全 MLP 提升 **4.0%**，在 Traffic 上提升 **18.5%**，验证了其互补性。

#### （3）归一化策略
- **混合归一化**（input/output 使用 GroupNorm，其余用 RMSNorm）表现最佳。
- 相比全 RMSNorm，在 ETTh1 上 MSE 改善 **12.1%**。

#### （4）输出头设计
- **卷积解码器**（conv-based head）显著优于传统 MLP 投影头：
  - 在多数任务上 MSE 更低
  - 参数量减少 **42%**
  - 训练更稳定（loss 曲线更平滑）

#### （5）外生变量影响
- 加入日历等协变量后性能提升，尤其在低维数据（如 ETT）上更显著。
- 表明语义信息（如节假日）对建模非平稳性至关重要。

---

## 4. 关键结论和发现

### 主要发现
1. **异构专家优于同质专家**：将 **depthwise convolution** 用于建模趋势、**Fourier 网络** 用于建模周期性，是一种符合时间序列本质的架构设计，显著优于统一使用 MLP 的 MoE。
2. **MoHE 实现了有效的专业化**：路由机制能够自动将高频 patch 分配给 Fourier 专家，低频/趋势 patch 保留给共享卷积路径，形成“功能分工”。
3. **轻量卷积解码器更高效稳定**：相比线性头，卷积解码器不仅参数更少，还能保持局部结构，防止过拟合。
4. **外生变量 + Huber loss 提升鲁棒性**：二者共同增强了模型对噪声、异常值和分布偏移的适应能力。

### 方法的局限性
- **推理延迟问题**：在高维数据（如 Traffic）和超长预测（H=720）时，推理速度仍较慢。
- **未使用预训练**：当前 MoHETS 是从零训练，未探索在大规模数据上的预训练潜力。
- **对 patch 长度敏感**：最优 patch size 依赖于数据频率，需手动调参。
- **缺乏对缺失协变量的处理机制**：当前假设协变量完整可用。

### 未来工作方向
- **引入 KV Caching**：借鉴 NLP 中的缓存机制，加速长序列自回归推理。
- **多分辨率投影头**：为不同预测长度设计专用解码器，进一步优化精度与效率权衡。
- **自适应协变量处理**：开发能处理缺失或错位外生变量的模块。
- **扩展 MoHE 架构**：尝试引入图神经网络（GNN）或注意力专家，建模更复杂的时空依赖。
- **大规模预训练**：在类似 Time-MoE 或 TimesFM 的海量数据上预训练 MoHETS，探索零样本迁移能力。

--- 

> **总结**：MoHETS 通过 **MoHE**、**cross-attention with covariates** 和 **convolutional patch decoder** 三大创新，在长时程多变量时间序列预测任务中实现了显著突破，不仅性能领先，而且设计理念更贴合时间序列的本质特性，为未来的时间序列建模范式提供了重要启示。

</details>

---

### 14. [TACLer: Tailored Curriculum Reinforcement Learning for Efficient Reasoning](https://arxiv.org/abs/2601.21711)

**Authors**: Huiyuan Lai, Malvina Nissim  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.21711v1  

#### Abstract
Large Language Models (LLMs) have shown remarkable performance on complex reasoning tasks, especially when equipped with long chain-of-thought (CoT) reasoning. However, eliciting long CoT typically requires large-scale reinforcement learning (RL) training, while often leading to overthinking with re...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# TACLer: Tailored Curriculum Reinforcement Learning for Efficient Reasoning 论文总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在复杂推理任务中表现出色，尤其是在采用长链式思维（long Chain-of-Thought, CoT）时。然而，这种方法面临两大挑战：

- **训练效率低**：大规模强化学习（RL）训练需要巨大的计算资源（如数万 A100 GPU 小时），尤其在处理长上下文时。
- **推理冗余（Overthinking）**：模型常生成过长且包含冗余步骤的思考过程，导致推理 token 消耗过高，影响效率。

此外，现有方法在提升效率的同时往往牺牲准确性，或限制用户对推理模式的选择自由。

---

### **提出了什么新方法或新思路**

作者提出 **TACLer**（Tailored Curriculum Reinforcement Learning），一种面向高效推理的新型框架，其核心创新包括：

#### （1）**Tailored Curriculum Learning（定制化课程学习）**
- 不同于传统基于输入长度或人工标注难度的课程学习，TACLer 根据模型自身在训练数据上的表现动态评估“困难度”。
- 将训练样本分为三类：
  - G1：答案正确
  - G2：推理完整但答案错误
  - G3：推理被截断（未完成）
- 在多阶段 RL 训练中，逐步引入更难的数据（从 G1/G2 → G3），实现“由易到难”的渐进式学习，显著提升学习效率。

#### （2）**Hybrid Thinking/NoThinking 推理范式**
- 支持两种推理模式共存：
  - **Thinking Mode**：生成详细 CoT 过程，追求高准确率。
  - **NoThinking Mode**：直接输出简洁解答，追求高效率。
- 通过联合训练两种模式，实现“压缩效应”（compression effect）：Thinking 模式的高质量推理能力被提炼到 NoThinking 模式中，使其在短响应下仍保持高精度。

---

### **相比现有方法的优势**

| 维度 | TACLer 优势 |
|------|-------------|
| **训练效率** | 减少超过 50% 的训练计算量（vs. DeepScaleR/FastCuRL） |
| **推理效率** | 推理 token 使用减少 >42% |
| **准确性** | 平均准确率提升 >9%（vs. 基线 R1-Qwen） |
| **灵活性** | 用户可按需选择 Thinking 或 NoThinking 模式 |
| **通用性** | 方法不依赖绝对难度定义，更具模型自适应性 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **训练数据**：`DeepScaleR-Dataset`，约 40k 来自 AIME 1983–2023 等竞赛的数学题。
- **测试数据集**（四大数学推理基准）：
  - `MATH500`
  - `AMC`
  - `AIME 2024`
  - `AIME 2025`

---

### **实验设置和评估指标**

#### **模型架构**
- 基座模型：`DeepSeek-R1-Distill-Qwen-1.5B`（简称 R1-Qwen）
- 优化算法：`GRPO`（Group Relative Policy Optimization），并移除 KL loss、调整 clip bounds 以增强探索。

#### **训练策略**
- 三阶段训练，每阶段均使用 **8K context length** 和 **rollout=8**，保持轻量。
- Curriculum 调度基于前一阶段模型对全数据集的推理结果进行难度划分。

#### **评估指标**
- **Accuracy (ACC)**：最终答案正确率
- **Response Length ↓**：生成 token 数量（越低越好）
- **Training Compute**：总 GPU 小时消耗（估算）

---

### **基线方法对比**

#### **长 CoT 推理模型（Thinking Mode）**
- `STILL-3`
- `DeepScaleR`
- `FastCuRL`

#### **高效推理模型（Efficient Reasoning / NoThinking Mode）**
- `OverThink`
- `DAST`
- `O1-Pruner`
- `TLMRE`
- `ModelMerging`
- `AdaptThink`
- `AutoThink`

所有对比均基于相同训练数据（DeepScaleR-Dataset），确保公平性。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **Thinking Mode 下的结果（Table 1）**

| 模型 | 平均 ACC ↑ | 平均 Length ↓ | 相对 R1-Qwen |
|------|------------|----------------|---------------|
| R1-Qwen | 70.3 | 9300 | — |
| DeepScaleR | 80.8 | 6120 | +10.5% ACC, -34.1% Length |
| **TACLer (Thinking)** | **81.5** | **5370** | **+11.2% ACC, -42.7% Length** |

> 📌 **TACLer 在三项数据集上达到最高准确率，平均优于所有基线，且响应长度最短。**

---

#### ✅ **NoThinking Mode 下的结果（Table 2）**

| 模型 | 平均 ACC ↑ | 平均 Length ↓ |
|------|------------|----------------|
| R1-Qwen (NoThink) | 57.1 | 5430 |
| AutoThink | 60.4 | 3070 |
| **TACLer (NoThinking)** | **66.6** | **3720** |

> 📌 **TACLer 在 NoThinking 模式下准确率远超所有基线（+9.5% vs. R1-Qwen），同时响应长度仅为一半左右。**

---

### **消融实验结果**

#### 🔍 **Hybrid Mode vs. Pure-NoThinking（Table 3）**
- 单独训练 NoThinking 模式虽在该模式下略准，但响应更长（2325 vs. 1472 tokens on MATH500）。
- TACLer 因联合训练 Thinking 模式，获得“压缩效应”，实现更**简洁且准确**的 NoThinking 输出。

#### 🔍 **Curriculum Learning vs. Direct-Train（Table 4）**
- 直接训练（Direct-Train）在 NoThinking 下响应稍短，但在 Thinking 模式下不如 TACLer 精炼。
- **TACLer 在两种模式下均取得更高准确率**，验证了课程学习的有效性。

#### 🔍 **Oracle Union 分析（Table 5）**
- 定义“Oracle Union”为从 Thinking 和 NoThinking 中选最优结果的理论上限。
- TACLer 平均达到该上限的 **86.3%（NoThinking）和 90.2%（Thinking）**，说明其已接近理论性能边界。

---

## 4. 关键结论和发现

### **主要发现**

1. **Tailored Curriculum Learning 显著提升训练效率**
   - 动态基于模型掌握程度安排训练顺序，避免早期处理过难样本导致的响应截断和计算浪费。
   - 实现 **>50% 的训练计算节省**。

2. **Hybrid Reasoning Mode 实现效率与性能的双赢**
   - Thinking 与 NoThinking 可相互促进：前者提升后者准确性，后者反哺前者的简洁性。
   - 在 NoThinking 模式下仍能超越多数 Thinking 模型的性能。

3. **TACLer 实现 SOTA 性能与极致效率**
   - 在四大数学推理数据集上，**准确率平均提升 >9%，推理 token 减少 >42%**。
   - 同时优于所有 Thinking 和 Efficient Reasoning 基线。

4. **正确推理通常更简洁**
   - 实验发现：**正确回答的响应长度普遍短于错误回答**，支持“过度冗长即错误”的假设。

---

### **方法的局限性**

- **额外推理开销**：在每个训练阶段前需对全数据集进行 inference 以构建 curriculum，带来一定计算开销（约 5 小时/H100 GPU）。
- 当前仅验证于数学推理任务，泛化性有待在其他领域（如代码、常识推理）进一步验证。

---

### **未来工作方向**

- 将 TACLer 框架扩展至更多推理任务（如 Multi-hop QA、Program Synthesis）。
- 探索自动化 curriculum labeling，降低 inference 开销。
- 研究如何将 hybrid reasoning 能力迁移到 zero-shot 场景。
- 构建通用的“Thinking Switch”机制，允许用户动态控制推理深度。

---

> 💡 **Impact Statement**  
> TACLer 通过降低计算成本与提升推理效率，使高性能推理模型更易于部署，推动可持续 AI 发展，并为构建安全、可靠、可控的智能系统提供基础。

</details>

---

### 15. [A Federated and Parameter-Efficient Framework for Large Language Model Training in Medicine](https://arxiv.org/abs/2601.22124)

**Authors**: Anran Li, Yuanyuan Chen, Wenjun Long, Yu Yin, Yan Hu, Hyunjae Kim, Weipeng Zhou, Yujia Zhou, Hongyi Peng, Yang Ren, Xuguang Ai, Zhenyue Qin, Ming Hu, Xiaoxiao Li, Han Yu, Yih-Chung Tham, Lucila Ohno-Machado, Hua Xu, Qingyu Chen  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22124v1  

#### Abstract
Large language models (LLMs) have demonstrated strong performance on medical benchmarks, including question answering and diagnosis. To enable their use in clinical settings, LLMs are typically further adapted through continued pretraining or post-training using clinical data. However, most medical ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
当前医学领域的大语言模型（LLMs）训练普遍面临两大挑战：
- **隐私与合规限制**：由于患者数据敏感，医疗机构间难以共享原始数据，导致多数LLM仅基于单一机构的数据进行训练，泛化能力差。
- **联邦学习（Federated Learning, FL）在LLM上的应用瓶颈**：
  - 传统FL需要传输完整的模型参数，在数十亿参数规模的LLM中通信开销巨大，不适用于临床环境。
  - 医疗数据具有高度异质性（non-IID），如不同人群、疾病分布、标注习惯等，标准FL聚合策略容易导致收敛不稳定和性能偏差。

### 提出的新方法
本文提出 **Fed-MedLoRA** 和其增强版本 **Fed-MedLoRA+**，是首个面向医学领域的**模型无关且参数高效的联邦学习框架**，用于适配大语言模型。

#### 核心技术思路
- **Fed-MedLoRA**：采用 **Low-Rank Adaptation (LoRA)** 技术，客户端只训练并上传低秩适配器（adapter）参数，而非整个模型权重，显著降低通信成本。
- **Fed-MedLoRA+**：在Fed-MedLoRA基础上引入**自适应、数据感知的聚合机制（influence-aware aggregation）**。服务器利用一个小的公共验证集评估每个客户端更新的影响，并据此加权聚合，提升对跨站点异构性的鲁棒性。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **通信效率** | 通信量减少 **98.5%**，从全模型更新的数百GB降至数GB级别，适合带宽受限的医疗网络。 |
| **计算可行性** | 可在消费级GPU（如RTX 4090）上完成8B参数模型的训练，推理可在笔记本电脑（如Apple M3 Pro）运行。 |
| **模型性能** | 显著优于单机构微调、零样本LLM及专用BERT模型，接近集中式训练的“上界”性能。 |
| **实用性** | 支持不完整或不均衡的任务标注（例如某些站点仅有NER无RE标签），更贴近真实多中心协作场景。 |

---

## 2. 核心实验方法和设置

### 数据集
共使用五个真实世界临床文本数据集，涵盖四种实体类别（Problem, Treatment, Test, Drug）和16种关系类型：
- **MIMIC-III**：重症监护电子病历。
- **MTSamples**：公开转录的医疗报告样本。
- **UTP (UT Physicians)**：德克萨斯大学医生笔记。
- **I2B2**：整合生物学与床边信息学的临床文本。
- **YNHH (Yale New Haven Health)**：研究团队手动标注的4份耶鲁纽黑文健康系统的真实患者记录，用于模拟新站点低资源迁移场景。

### 实验设置
分为三种评估场景以全面验证模型能力：

| 场景 | 描述 |
|------|------|
| **In-domain Training & Testing** | 在多个站点（如MIMIC-III + MTSamples）上进行联邦训练，并在对应测试集上评估域内性能。 |
| **External Validation** | 将联邦训练后的模型直接应用于未见过的外部数据集（如I2B2或UTP），检验跨域泛化能力。 |
| **Low-resource New-site Adaptation** | 使用YNHH小样本数据集，测试联邦模型能否帮助一个新加入且标注极少的机构快速启动本地任务。 |

### 评估指标
- **NER (命名实体识别)** 和 **RE (关系抽取)** 任务均报告 **Precision, Recall, F1-score**。
- 采用两种匹配标准：
  - **Strict Match**：预测的实体跨度和类型必须完全一致。
  - **Lenient Match**：允许部分重叠的跨度，只要类型正确即视为正确。

### 基线方法对比
| 类别 | 具体基线 |
|------|--------|
| **LLM-based** | - Zero-shot LLaMA3 / DeepSeek-R1 / GPT-4o<br>- 单站点微调（Single-site fine-tuned）LLaMA3 / DeepSeek-R1 |
| **BERT-based** | - 微调 Bio_ClinicalBERT（医学领域最强BERT之一） |
| **Federated Baseline** | - FedSA-LoRA：一种通用领域的LoRA联邦算法，仅上传低秩矩阵A进行聚合 |
| **理论上限** | - Centralized Training：将所有站点数据合并后集中训练，作为理想但不可行的性能上界 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总
| 指标 | Fed-MedLoRA+ 表现 |
|------|------------------|
| **NER F1 (Strict)** | 最高达 **86.6%**（MTSamples），平均比零样本提升超 **50%** |
| **RE F1 (Strict)** | 最高达 **86.0%**（MIMIC-III），平均比零样本提升约 **65%** |
| **外部验证增益** | 相比基线F1提升 **10–70%** |
| **新站点适应** | 在YNHH低资源场景下取得 **73%严格F1 / 85%宽松F1** |
| **通信成本降低** | 较全参数更新减少 **98.5%**（如从239GB→1.25GB） |
| **训练硬件需求** | 8B模型可在 **单块RTX 4090 (16GB)** 上训练；1B模型可跑在 **RTX 3060 Ti (8GB)** 上 |
| **推理设备支持** | 支持在 **标准笔记本电脑**（如Apple M3 Pro）部署推理 |

### 与基线方法的对比结果
#### ✅ 超越所有基线
- **vs 零样本LLM**：Fed-MedLoRA+使LLM的F1提升高达 **65个百分点**（如LLaMA3在MIMIC-III上从0.203→0.860）。
- **vs 单站点微调LLM**：平均F1提升约 **25%**，尤其在复杂RE任务上优势明显。
- **vs 微调BERT模型**：在RE任务上F1领先超过 **40%**（如0.860 vs 0.434）。
- **vs FedSA-LoRA**：在三站点实验中，Fed-MedLoRA+在NER和RE上分别高出 **~40%** 和 **~70%** F1，证明其自适应聚合的有效性。

#### ✅ 接近集中式训练“上界”
- 在多数任务上，Fed-MedLoRA+性能与Centralized Training差距极小（<2% F1），表明其能有效融合多源信息而不牺牲精度。

#### ✅ 强大的跨域泛化能力
- 在独立外部数据集（I2B2, UTP）上，Fed-MedLoRA+仍保持强劲表现，F1相比单站点微调提升 **9–10%**，说明联邦训练增强了模型鲁棒性。

### 消融实验结果
- **Fed-MedLoRA vs Fed-MedLoRA+**：
  - Fed-MedLoRA+在绝大多数测试集上表现更好，尤其是在数据异构性强的三站点设置中。
  - 证明了**影响感知聚合（influence-aware aggregation）** 对缓解异构性至关重要。
- **不同backbone比较**：
  - LLaMA3 和 DeepSeek-R1 表现相近，LLaMA3略优。
- **不同模型大小的影响（1B vs 8B）**：
  - 使用LLaMA3-1B时，NER性能下降约3%，RE下降最多7%，但训练内存从14GB降至2.15GB，推理可跑在普通笔记本。
  - 提供了**效率-精度权衡选项**，适用于资源受限环境。

---

## 4. 关键结论和发现

### 主要发现
1. **联邦LLM在医学中是可行且有效的**：通过参数高效微调（如LoRA），可以实现跨机构协作训练，同时保护患者隐私。
2. **Fed-MedLoRA+显著提升了联邦训练的稳定性和性能**：其提出的**影响感知聚合机制**有效应对了医疗数据的高度异质性问题。
3. **联邦训练极大增强了模型泛化能力**：相比单机构训练，联邦模型在外部数据和新站点上表现出更强的适应性。
4. **实用性强**：该框架可在消费级硬件上运行，支持不完整标注，具备良好的可扩展性（模拟至10个站点性能仅轻微下降）。

### 方法的局限性
1. **隐私保护尚未完全闭环**：虽然不传输原始数据和完整模型，但仍需研究是否可能通过LoRA参数反推敏感信息，建议未来结合Secure Aggregation或Differential Privacy。
2. **任务范围有限**：本研究聚焦于**临床信息提取（IE）**，尚需探索其他医学任务（如诊断辅助、生成摘要）中的适用性。
3. **依赖少量公共验证集**：服务器需持有小型验证集来计算影响分数，虽合理但在极端隐私要求下可能受限。

### 未来工作方向
1. **加强隐私保障机制**：集成差分隐私（DP）、安全聚合（Secure Aggregation）等技术，构建端到端隐私保护管道。
2. **拓展至更多医学任务**：验证Fed-MedLoRA在问答、诊断、治疗推荐等多样化任务中的有效性。
3. **推动真实临床部署**：开展前瞻性多中心试验，解决实际部署中的网络协调、硬件异构、治理合规等问题。
4. **探索更轻量化架构**：进一步优化小模型（如1B以下）的性能，使其能在边缘设备（如医院终端机）运行。

> 🔗 **代码开源**：所有实现已公开于 GitHub → [https://github.com/Yale-BIDS-Chen-Lab/FLLLMMed](https://github.com/Yale-BIDS-Chen-Lab/FLLLMMed)  
> 📚 **资助来源**：本研究由美国国立卫生研究院（NIH）项目 1R01LM014604 支持。

</details>

---

### 16. [Heterogeneous Vertiport Selection Optimization for On-Demand Air Taxi Services: A Deep Reinforcement Learning Approach](https://arxiv.org/abs/2601.21316)

**Authors**: Aoyu Pang, Maonan Wang, Zifan Sha, Wenwei Yue, Changle Li, Chung Shue Chen, Man-On Pun  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.21316v1  

#### Abstract
Urban Air Mobility (UAM) has emerged as a transformative solution to alleviate urban congestion by utilizing low-altitude airspace, thereby reducing pressure on ground transportation networks. To enable truly efficient and seamless door-to-door travel experiences, UAM requires close integration with...

---

### 17. [Breaking the Regional Barrier: Inductive Semantic Topology Learning for Worldwide Air Quality Forecasting](https://arxiv.org/abs/2601.21899)

**Authors**: Zhiqing Cui, Siru Zhong, Ming Jin, Shirui Pan, Qingsong Wen, Yuxuan Liang  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.21899v1  

#### Abstract
Global air quality forecasting grapples with extreme spatial heterogeneity and the poor generalization of existing transductive models to unseen regions. To tackle this, we propose OmniAir, a semantic topology learning framework tailored for global station-level prediction. By encoding invariant phy...

---

### 18. [OmegaUse: Building a General-Purpose GUI Agent for Autonomous Task Execution](https://arxiv.org/abs/2601.20380)

**Authors**: Le Zhang, Yixiong Xiao, Xinjiang Lu, Jingjia Cao, Yusai Zhao, Jingbo Zhou, Lang An, Zikan Feng, Wanxiang Sha, Yu Shi, Congxi Xiao, Jian Xiong, Yankai Zhang, Hua Wu, Haifeng Wang  
**Category**: cs.AI  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.20380v1  

#### Abstract
Graphical User Interface (GUI) agents show great potential for enabling foundation models to complete real-world tasks, revolutionizing human-computer interaction and improving human productivity. In this report, we present OmegaUse, a general-purpose GUI agent model for autonomous task execution on...

---

### 19. [ASTRA: Automated Synthesis of agentic Trajectories and Reinforcement Arenas](https://arxiv.org/abs/2601.21558)

**Authors**: Xiaoyu Tian, Haotian Wang, Shuaiting Chen, Hao Zhou, Kaichi Yu, Yudian Zhang, Jade Ouyang, Junxi Yin, Jiong Chen, Baoyan Guo, Lei Zhang, Junjie Tao, Yuansheng Song, Ming Cui, Chengwei Liu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21558v1  

#### Abstract
Large language models (LLMs) are increasingly used as tool-augmented agents for multi-step decision making, yet training robust tool-using agents remains challenging. Existing methods still require manual intervention, depend on non-verifiable simulated environments, rely exclusively on either super...

---

### 20. [DynaWeb: Model-Based Reinforcement Learning of Web Agents](https://arxiv.org/abs/2601.22149)

**Authors**: Hang Ding, Peidong Liu, Junqiao Wang, Ziwei Ji, Meng Cao, Rongzhao Zhang, Lynn Ai, Eric Yang, Tianyu Shi, Lei Yu  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22149v1  

#### Abstract
The development of autonomous web agents, powered by Large Language Models (LLMs) and reinforcement learning (RL), represents a significant step towards general-purpose AI assistants. However, training these agents is severely hampered by the challenges of interacting with the live internet, which i...

---

### 21. [Deep Reinforcement Learning for Fault-Adaptive Routing in Eisenstein-Jacobi Interconnection Topologies](https://arxiv.org/abs/2601.21090)

**Authors**: Mohammad Walid Charrwi, Zaid Hussain  
**Category**: cs.DC  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21090v1  

#### Abstract
The increasing density of many-core architectures necessitates interconnection networks that are both high-performance and fault-resilient. Eisenstein-Jacobi (EJ) networks, with their symmetric 6-regular topology, offer superior topological properties but challenge traditional routing heuristics und...

---

### 22. [EWSJF: An Adaptive Scheduler with Hybrid Partitioning for Mixed-Workload LLM Inference](https://arxiv.org/abs/2601.21758)

**Authors**: Bronislav Sidik, Chaya Levi, Joseph Kampeas  
**Category**: cs.DC  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21758v1  

#### Abstract
Serving Large Language Models (LLMs) under mixed workloads--short, latency-sensitive interactive queries alongside long, throughput-oriented batch requests--poses a fundamental scheduling challenge. Standard First-Come, First-Served (FCFS) policies suffer from severe head-of-line blocking, leading t...

---

### 23. [Few-Shot Learning for Dynamic Operations of Automated Electric Taxi Fleets under Evolving Charging Infrastructure: A Meta-Deep Reinforcement Learning Approach](https://arxiv.org/abs/2601.21312)

**Authors**: Xiaozhuang Li, Xindi Tang, Fang He  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21312v1  

#### Abstract
With the rapid expansion of electric vehicles (EVs) and charging infrastructure, the effective management of Autonomous Electric Taxi (AET) fleets faces a critical challenge in environments with dynamic and uncertain charging availability. While most existing research assumes a static charging netwo...

---

### 24. [Theoretically Optimal Attention/FFN Ratios in Disaggregated LLM Serving](https://arxiv.org/abs/2601.21351)

**Authors**: Chendong Song, Meixuan Wang, Hang Zhou, Hong Liang, Yuan Lyu, Zixi Chen, Yuwei Fan, Zijie Zhou  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21351v1  

#### Abstract
Attention-FFN disaggregation (AFD) is an emerging architecture for LLM decoding that separates state-heavy, KV-cache-dominated Attention computation from stateless, compute-intensive FFN computation, connected by per-step communication. While AFD enables independent scaling of memory and compute res...

---

### 25. [Signal-Adaptive Trust Regions for Gradient-Free Optimization of Recurrent Spiking Neural Networks](https://arxiv.org/abs/2601.21572)

**Authors**: Jinhao Li, Yuhao Sun, Zhiyuan Ma, Hao He, Xinche Zhang, Xing Chen, Jin Li, Sen Song  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21572v1  

#### Abstract
Recurrent spiking neural networks (RSNNs) are a promising substrate for energy-efficient control policies, but training them for high-dimensional, long-horizon reinforcement learning remains challenging. Population-based, gradient-free optimization circumvents backpropagation through non-differentia...

---

### 26. [Uncertainty-Aware Data-Based Method for Fast and Reliable Shape Optimization](https://arxiv.org/abs/2601.21956)

**Authors**: Yunjia Yang, Runze Li, Yufei Zhang, Haixin Chen  
**Category**: cs.LG  
**Published**: 2026-01-30  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.21956v1  

#### Abstract
Data-based optimization (DBO) offers a promising approach for efficiently optimizing shape for better aerodynamic performance by leveraging a pretrained surrogate model for offline evaluations during iterations. However, DBO heavily relies on the quality of the training database. Samples outside the...

---

### 27. [ChunkWise LoRA: Adaptive Sequence Partitioning for Memory-Efficient Low-Rank Adaptation and Accelerated LLM Inference](https://arxiv.org/abs/2601.21109)

**Authors**: Ketan Thakkar, Maitreyi Chatterjee, Ramasubramanian Balasubramanian, Achyuthan Jootoo, Rajendra Ugrani  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.21109v1  

#### Abstract
Recent advances in low-rank adaptation (LoRA) have enabled efficient fine-tuning of large language models (LLMs) with minimal additional parameters. However, existing LoRA methods apply static rank configurations uniformly across all input tokens, ignoring variation in token complexity and computati...

---

### 28. [Output-Space Search: Targeting LLM Generations in a Frozen Encoder-Defined Output Space](https://arxiv.org/abs/2601.21169)

**Authors**: Tobias Materzok  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.21169v1  

#### Abstract
We introduce Output-Space Search (OS-Search), which turns LLM generation into endpoint search. An outer loop selects a target z* in a frozen encoder-defined 3D output space Z, and a retrieval-grounded policy trained with sequence-level RL generates outputs whose coordinates land near z* under standa...

---

### 29. [Why Attention Patterns Exist: A Unifying Temporal Perspective Analysis](https://arxiv.org/abs/2601.21709)

**Authors**: Qingyue Yang, Jie Wang, Xing Li, Yinqi Bai, Xialiang Tong, Huiling Zhen, Jianye Hao, Mingxuan Yuan, Bin Li  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.21709v1  

#### Abstract
Attention patterns play a crucial role in both training and inference of large language models (LLMs). Prior works have identified individual patterns such as retrieval heads, sink heads, and diagonal traces, yet these observations remain fragmented and lack a unifying explanation. To bridge this ga...

---

### 30. [VTC-R1: Vision-Text Compression for Efficient Long-Context Reasoning](https://arxiv.org/abs/2601.22069)

**Authors**: Yibo Wang, Yongcheng Jing, Shunyu Liu, Hao Guan, Rong-cheng Tu, Chengyu Wang, Jun Huang, Dacheng Tao  
**Category**: cs.CL  
**Published**: 2026-01-30  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.22069v1  

#### Abstract
Long-context reasoning has significantly empowered large language models (LLMs) to tackle complex tasks, yet it introduces severe efficiency bottlenecks due to the computational complexity. Existing efficient approaches often rely on complex additional training or external models for compression, wh...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
