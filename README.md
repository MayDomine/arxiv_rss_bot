# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-02 06:47:39 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Autonomous Chain-of-Thought Distillation for Graph-Based Fraud Detection](https://arxiv.org/abs/2601.22949)

**Authors**: Yuan Li, Jun Hu, Bryan Hooi, Bingsheng He, Cheng Chen  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22949v1  

#### Abstract
Graph-based fraud detection on text-attributed graphs (TAGs) requires jointly modeling rich textual semantics and relational dependencies. However, existing LLM-enhanced GNN approaches are constrained by predefined prompting and decoupled training pipelines, limiting reasoning autonomy and weakening...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Autonomous Chain-of-Thought Distillation for Graph-Based Fraud Detection*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现有的 **LLM-enhanced GNN** 方法在基于文本属性图（Text-Attributed Graphs, TAGs）的欺诈检测中面临两大瓶颈：

1. **缺乏自主推理能力（Lack of Autonomous Reasoning）**  
   当前方法依赖预定义的提示模板（predefined prompting）来引导 LLM 生成辅助信息（如关键词、情感特征），这种模式限制了 LLM 进行多跳、跨邻域的深层推理，仅停留在浅层模式匹配。

2. **语义-结构对齐弱化（Weakened Semantic-Structural Alignment）**  
   多数方法采用解耦训练范式（decoupled training），即先用 LLM 编码文本，再用 GNN 学习结构，导致语义表示无法与图结构联合优化，削弱了二者之间的协同效应。

---

### **提出了什么新方法或新思路**

本文提出 **FraudCoT** ——一个统一的框架，通过**图感知的链式思维蒸馏**（graph-aware Chain-of-Thought Distillation）与**高效的端到端共训练机制**，提升欺诈检测性能与效率。

#### 主要创新点：

- ✅ **欺诈感知的选择性 CoT 蒸馏机制（Fraud-Aware Selective CoT Distillation）**  
  利用教师 LLM 在节点及其邻居上下文中自动生成多样化的推理路径（包括正确与错误路径），并通过正负样本蒸馏训练学生 LLM，使其学会模仿合理推理、抑制误导性逻辑，从而实现**自由形式的图感知推理**。

- ✅ **高效非对称共训练策略（Efficient Asymmetric Co-training）**  
  在训练时仅对目标节点使用 LLM 编码，而对邻居节点使用缓存的初始嵌入，大幅降低计算开销。该设计将 LLM 推理复杂度从指数级降至常数级，支持端到端优化的同时保持高吞吐量。

- ✅ **CoT 增强的节点表示集成**  
  将蒸馏得到的 CoT 推理路径拼接到原始文本中，作为 GNN 的输入，为下游模型提供富含多跳语义与结构线索的信息。

---

### **相比现有方法的优势**

| 维度 | 现有方法（如 TAPE、FLAG） | FraudCoT |
|------|--------------------------|--------|
| 推理方式 | 预定义模板驱动 | 自主、自由形式的多步推理 |
| 训练范式 | 解耦训练（Decoupled） | 端到端联合优化（End-to-end） |
| 语义-结构对齐 | 弱 | 强（通过共训练实现） |
| 计算效率 | 高（但牺牲性能） | 极高（达 1,066× 加速） |
| 可解释性 | 有限 | 每个预测附带人类可读的推理路径 |

> FraudCoT 成功弥合了“自主推理”与“高效训练”的鸿沟，在性能和效率上均实现突破。

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 类型 | 节点数 | 边数 | 场景说明 |
|-------|------|--------|------|---------|
| **InstantVideo** | 公共基准 | 37,126 | 988,340 | Amazon 视频类商品评论，判断是否“有帮助”（代理欺诈信号） |
| **DigitalMusic** | 公共基准 | 64,706 | 7,732,420 | Amazon 数码音乐评论，同上任务 |
| **PromotionAbuse** | 工业界专有 | 371,464 | 1,388,598 | 字节跳动真实促销滥用图谱，检测虚假行为 |

> 图均为异构图（heterogeneous），含三种边类型：同用户（R-U-R）、同产品（R-P-R）、相似评分+时间（R-S-R）。

---

### **实验设置和评估指标**

#### 实验环境
- 硬件：NVIDIA A100 GPU（80GB），64核CPU，1TB内存
- LLM 骨干：Qwen3-8B
- 微调技术：LoRA（应用于注意力层）
- 优化器：AdamW
- 批大小：128（可扩展至256）

#### 评估指标
- **Macro-F1**：类别不平衡下的综合性能
- **AUROC**：ROC 曲线下面积
- **AUPRC**：精确率-召回率曲线下面积（尤其关注正类表现）

所有结果报告 5 次随机种子的均值 ± 标准差。

---

### **基线方法对比**

涵盖四类主流方法：

| 类别 | 代表方法 |
|------|--------|
| **纯 GNN** | GraphSAGE, HGT, ConsisGAD, PMP, GAAP |
| **图无关模型** | MLP, LLM, LLM-SFT（微调版） |
| **图增强 LLM** | LLaGA, GraphGPT, HiGPT, InstructGLM |
| **LLM-enhanced GNN** | TAPE, FLAG |

> 所有基线使用官方代码复现，确保公平比较。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（摘录自 Table 3）**

| 方法 | InstantVideo (AUPRC↑) | DigitalMusic (AUPRC↑) | PromotionAbuse (AUPRC↑) |
|------|------------------------|------------------------|--------------------------|
| **TAPE** | 79.28 ± 0.58 | 81.99 ± 0.19 | 67.21 ± 0.49 |
| **FLAG** | 41.75 ± 0.05 | 38.59 ± 0.23 | 42.17 ± 0.52 |
| **FraudCoT (Ours)** | **84.10 ± 0.26** | **84.49 ± 0.28** | **73.65 ± 0.41** |

> 💡 **最高提升达 +8.8% AUPRC**（相对于最强基线），且在工业数据集上优势最显著。

此外：
- **AUROC 提升明显**：在 InstantVideo 上达到 90.73%，优于第二名近 2 个百分点。
- **稳定性更强**：标准差更小，表明模型鲁棒性更好。

---

### **与基线方法的对比结果**

- 📌 **超越所有 GNN 和 LLM 基线**：即使是最先进的 ConsisGAD 或 GAAP，也无法充分利用文本深层语义。
- 📌 **优于图增强 LLM**：如 GraphGPT、HiGPT 生成的推理较泛化，难以聚焦具体欺诈模式。
- 📌 **显著优于 LLM-enhanced GNN**：TAPE 和 FLAG 受限于固定模板，无法进行跨邻域推理。
- ✅ **唯一同时实现高性能与高效率的方法**：见下表。

---

### **消融实验结果（Ablation Study）**

移除不同组件后的性能下降（以 AUPRC 为例）：

| 变体 | InstantVideo ↓ | DigitalMusic ↓ | PromotionAbuse ↓ |
|------|----------------|----------------|------------------|
| w/o FASCD（无 CoT 蒸馏） | -4.82 | -2.50 | -4.83 |
| w/o EAC（无非对称共训练） | -3.10 | -2.20 | -3.44 |
| w/o NegDis（无负向蒸馏） | -2.10 | -1.80 | -2.30 |

> 🔍 **关键发现**：
- 移除 **FASCD** 影响最大 → 表明 CoT 蒸馏是性能增益的核心驱动力。
- 移除 **EAC** 导致语义-结构失配 → 验证了端到端训练的重要性。
- 移除 **NegDis** 降低鲁棒性 → 说明抑制错误推理路径对防止过拟合至关重要。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **自主推理优于预定义提示**  
   自由形式的 CoT 推理能捕捉复杂的跨邻域欺诈模式（如“同一用户发布极端好评与差评”），远超模板提取的能力。

2. ✅ **正负蒸馏机制有效提升推理质量**  
   不仅学习“怎么想对”，还学习“怎么想错”，使学生模型更具判别力和抗干扰能力。

3. ✅ **非对称共训练实现效率飞跃**  
   在 DigitalMusic 上实现 **1,066× 训练吞吐加速**，最大批大小提升 **128 倍**，使端到端训练变得可行。

4. ✅ **语义与结构深度融合带来稳定增益**  
   CoT 提供高层语义线索，GNN 实现结构传播，二者通过联合训练动态对齐，形成闭环增强。

---

### **方法的局限性**

- ⚠️ **依赖高质量教师 LLM**：若教师本身推理能力不足，蒸馏效果受限。
- ⚠️ **蒸馏样本需人工标注或筛选**：目前仅在部分节点上生成 CoT，扩展至全图仍有成本。
- ⚠️ **对极稀疏标签场景敏感**：当欺诈样本极少时，负例蒸馏可能引入噪声。

---

### **未来工作方向**

- 🔮 探索 **无需教师的自监督 CoT 生成**，进一步降低成本。
- 🔮 将 FraudCoT 扩展至 **动态图** 和 **多模态图**（如图文混合）。
- 🔮 结合 **强化学习** 优化推理路径选择策略。
- 🔮 开发 **轻量化学生模型**，便于部署至生产系统。

---

> ✅ **总结一句话**：  
> **FraudCoT 通过“选择性 CoT 蒸馏 + 非对称共训练”，首次实现了高效、自主、可解释的图感知欺诈推理，在性能与效率上全面超越现有方法，为 LLM-GNN 协同建模树立了新标杆。**

</details>

---

### 2. [Matterhorn: Efficient Analog Sparse Spiking Transformer Architecture with Masked Time-To-First-Spike Encoding](https://arxiv.org/abs/2601.22876)

**Authors**: Zhanglu Yan, Kaiwen Tang, Zixuan Zhu, Zhenyu Bai, Qianhui Liu, Weng-Fai Wong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.22876v1  

#### Abstract
Spiking neural networks (SNNs) have emerged as a promising candidate for energy-efficient LLM inference. However, current energy evaluations for SNNs primarily focus on counting accumulate operations, and fail to account for real-world hardware costs such as data movement, which can consume nearly 8...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Matterhorn: Efficient Analog Sparse Spiking Transformer Architecture with Masked Time-To-First-Spike Encoding

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前基于 Spiking Neural Networks (SNNs) 的 LLM 推理架构虽然在理论上具有高能效潜力，但其能量评估多依赖于简化的操作计数（如 ACC vs MAC），**忽略了真实硬件中占主导地位的数据移动开销**（data movement）。研究表明，在先进工艺下，**spike 转移（42–55%）和权重访问（27–32%）是主要能耗来源**，而计算仅占 12–20%。

此外，传统 TTFS 编码将“静默状态”（all-zero spike train）分配给最小膜电位值，但由于该值在实际分布中极为罕见，导致节能潜力未被有效利用。

### 提出了什么新方法或新思路
本文提出 **Matterhorn**，一种高效的模拟稀疏脉冲 Transformer 架构，包含两大核心技术：

#### （1）Masked Time-To-First-Spike (M-TTFS) 编码
- **核心思想**：重新定义“零能耗静默状态”，将其从最低膜电位转移到**最频繁出现的膜电位对应的时间步 `Imax`**。
- 引入 **‘Dead Zone’ 策略**：将围绕 `Imax` 的一个时间窗口 `[Imax−k, Imax+k]` 内的所有 spike 抑制为静默状态，从而最大化 sparsity。
- 所有非零输出仍只产生一个 spike，保证 sparsity 且不破坏数据分布结构。

#### （2）Memristive Synapse Unit (MSU)
- 基于 **Compute-in-Memory (CIM)** 技术构建的混合信号单元。
- 利用 **nT1R crossbar + RRAM** 实现权重驻留内存中的模拟积分。
- 消除权重读取开销，并通过 bit-serial VMM 和数字累加器重建完整结果。
- 零输入电流可物理关闭模拟通路，实现**动态功耗门控**。

### 相比现有方法的优势
| 维度 | Matterhorn 优势 |
|------|----------------|
| **算法层面** | M-TTFS 显著降低 spike rate（至 1.65%），减少 spike movement 能耗达 2.46× |
| **硬件层面** | MSU 消除 weight access 成本，模拟 MAC 能耗低至 fJ/spike 级别 |
| **系统协同优化** | M-TTFS 的稀疏性直接转化为 MSU 的物理节能，形成软硬协同增益 |
| **整体性能** | 在 GLUE 上达到 SOTA 准确率（84.64%），同时实现 **2.31× 能效提升** |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- 主要基准：**GLUE benchmark** 包含以下 7 个任务：
  - QQP, MNLI-m, SST-2, QNLI, RTE, MRPC, STS-B
- 具体分析以 **SST-2** 数据集为主进行能耗分解与消融研究。

### 实验设置和评估指标
| 设置项 | 描述 |
|-------|------|
| **模型架构** | BERTbase 作为教师模型，采用知识蒸馏训练；使用 4-bit 量化，对应 T=16 时间步 |
| **部署平台** | 假设空间数据流架构（spatial dataflow），基于 NoC 进行核间通信 |
| **工艺节点** | 数字部分建模于商用 **22nm 工艺**；模拟 CIM 参数来自 Ye et al. (2023) 的 28nm RRAM 宏 |
| **评估指标** | 
  - **准确率 Accuracy / Pearson 相关系数（STS-B）**
  - **总能耗（mJ/block）**：细分为 spike movement、weight access、digital computing、analog computing、leakage |
  - **spike rate (%)**
  - **能效比（Energy Efficiency Improvement）**

### 基线方法对比
| 基线模型 | 类型 | 特点 |
|--------|------|------|
| **Spiking Otters (Yan et al., 2025a)** | SNN (TTFS) | 当前最优脉冲 Transformer，13.4M 参数 |
| **SpikingLM (Xing et al., 2024b)** | SNN (Rate) | 大规模脉冲语言模型 |
| **Sorbet (Tang et al., 2025)** | SNN (Rate) | 高效脉冲 Transformer |
| **SpikingBERT (Bal & Sengupta, 2024)** | SNN (Rate) | 脉冲版 BERT，50M 参数 |
| **Q2BERT / BiT / DistilBERT** | Quantized ANN | 低位宽 ANN 对照组 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 平均准确率 | 最佳准确率（RTE） | 总能耗（mJ/block） | Spike Rate |
|------|------------|-------------------|--------------------|-----------|
| BERTbase | 87.31% | 72.6% | — | — |
| Spiking Otters | 83.22% | 68.95% | 14.21 mJ | ~5% |
| **Matterhorn (k=0)** | **85.87%** | **72.56%** | 8.31 mJ | 2.77% → 34% silence |
| **Matterhorn (k=1)** | **84.64%** | 71.84% | **6.14 mJ** | **1.65%** → 61.2% silence |

> ✅ **Matterhorn (k=1)** 达到 **84.64%** 平均准确率，**超越所有现有 SNN 模型 1.42%**  
> ✅ 能耗降至 **6.14 mJ/block**，相比 Spiking Otters 实现 **2.31× 能效提升**

### 与基线方法的对比结果
| 对比项 | 能效提升倍数 |
|-------|--------------|
| vs. Spiking Otters (k=1, no MSU) | 1.72× |
| vs. Spiking Otters (with MSU) | **2.31×** |
| vs. SpikingLM | ~4.24× |
| vs. Sorbet | ~6.98× |
| vs. SpikingBERT | >13.13× |

> 💡 即使与等效 QNN 模型比较，Matterhorn 也能实现 **2.35× 能效提升**，验证了 SNN + CIM 的本质优势。

### 消融实验结果
#### （1）M-TTFS + Dead Zone 对 spike 分布的影响（SST-2）
| 配置 | 静默神经元比例 | spike movement energy |
|------|----------------|------------------------|
| Standard TTFS | 0.26% | 6.98 mJ |
| M-TTFS (k=0) | 34.0% | 4.75 mJ |
| M-TTFS (k=1) | 61.2% | 2.84 mJ |
| M-TTFS (k=2) | 76.4% | 1.80 mJ |

> 🔍 M-TTFS 将最常见激活映射为静默状态，显著提升 sparsity。

#### （2）能量逐级优化路径（SST-2）
| 方法 | 总能耗 | 相对改进 |
|------|--------|----------|
| Traditional TTFS | 16.80 mJ | Baseline |
| + M-TTFS | 12.24 mJ | ↓27% |
| + Dead Zone (k=1) | 8.31 mJ | ↓50% |
| + MSU | **6.14 mJ** | **↓2.7×** |

> 📈 软件稀疏编码与硬件 CIM 协同带来叠加节能效果。

#### （3）精度-能效权衡分析
- **k=1 是最佳平衡点**：能耗下降 59%，准确率仅轻微下降 0.9%
- **k≥2 时进入激进稀疏区**：准确率明显下降（如 k=4 时降至 83.83%）

---

## 4. 关键结论和发现

### 论文的主要发现
1. **数据移动是 SNN 能耗瓶颈**：在现代硬件上，spike transfer 和 weight access 占总能耗超 70%，远高于计算成本。
2. **编码策略应匹配数据分布**：将静默状态分配给高频值（而非极小值）可大幅提升 sparsity 而无信息损失。
3. **死区策略可显式控制稀疏-精度权衡**：通过调节 `k` 可灵活适应不同资源约束场景。
4. **CIM 是突破冯·诺依曼瓶颈的关键**：MSU 成功消除 weight access 开销，并支持物理级功耗门控。
5. **软硬协同设计实现 SOTA 表现**：Matterhorn 同时实现了最高准确率与最强能效，证明了其工程可行性。

### 方法的局限性
- **依赖预训练 QNN 进行转换**：目前采用 QNN-to-SNN conversion 框架，尚未支持端到端训练。
- **模拟噪声容忍度虽强但仍有限**：尽管单比特权重增强了鲁棒性，但在极端工艺偏差下可能影响稳定性。
- **硬件面积随模型规模增长较快**：每个 transformer block 需约 108 个 MSU 宏，大规模部署需 tile 架构优化。
- **当前评估基于模型估算**：缺乏真实芯片流片后的实测数据支撑。

### 未来工作方向
- 探索 **M-TTFS 支持端到端训练** 的可能性，摆脱对 QNN 的依赖。
- 扩展至 **更大规模 LLMs**（如 Llama、Phi 系列）并研究 long-context 下的 spike 动态特性。
- 设计 **可重构 MSU 架构**，支持动态调整 bit-serial 深度与精度配置。
- 推进 **原型芯片流片与实测验证**，建立更精确的能耗-延迟模型。
- 探索 **光子 CIM 或其他新型器件** 替代 RRAM，进一步提升带宽与能效。

--- 

> ✅ **总结一句话**：  
> Matterhorn 通过 **M-TTFS 编码匹配数据分布以最小化 spike movement**，结合 **MSU 基于 CIM 消除 weight access 开销**，首次在脉冲 Transformer 中实现了 **高精度与超高能效的统一**，树立了新的 SOTA 标杆。

</details>

---

### 3. [TriSpec: Ternary Speculative Decoding via Lightweight Proxy Verification](https://arxiv.org/abs/2601.23180)

**Authors**: Haoyun Jiang, Junqi He, Feng Hong, Xinlong Yang, Jianwei Zhang, Zheng Li, Zhengyang Zhuge, Zhiyong Chen, Bo Han, Junyang Lin, Jiangchao Yao  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.23180v1  

#### Abstract
Inference efficiency in Large Language Models (LLMs) is fundamentally limited by their serial, autoregressive generation, especially as reasoning becomes a key capability and response sequences grow longer. Speculative decoding (SD) offers a powerful solution, providing significant speed-ups through...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文《TriSpec: Ternary Speculative Decoding via Lightweight Proxy Verification》核心总结

---

## 1. 主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）的推理效率受限于其**串行自回归生成机制**，尤其是在复杂推理任务中响应序列更长时，延迟问题尤为严重。虽然现有的 **Speculative Decoding (SD)** 已通过轻量级 drafter 和并行验证显著加速推理，但当前研究大多聚焦于优化 **drafting 效率** 和 **acceptance rate**，而忽略了另一个关键瓶颈——**verification 成本**。

本文指出：**每轮验证中调用大规模 target model 的计算开销已成为新的性能瓶颈**，亟需优化。

---

### 🚀 提出的新方法：TriSpec
提出 **TriSpec** ——一种全新的**三元推测解码框架（ternary speculative decoding）**，引入一个**轻量级代理验证器（lightweight proxy verifier）** 来分担 target model 的验证负担。

#### 核心思想：
- 利用**同家族的小规模模型**（如 Qwen3-1.7B 验证 Qwen3-32B）作为 proxy verifier。
- 该 proxy 能够快速预验证大部分 draft tokens，并在置信度高时直接接受或局部纠错。
- 只有当 proxy 的判断“不可信”时，才将控制权交还给昂贵的 target model 进行权威验证。

#### 关键机制：
- **Margin-based routing criterion**：使用 proxy 输出的 top-1 与 top-2 概率差值（margin）作为可信度判据。若 margin ≥ λ，则认为 proxy 判断可信；否则升级至 target model。
- **Case I（本地纠正）**：proxy 在首次拒绝前仍可信 → 接受部分 token 并用 proxy 的预测进行局部修正。
- **Case II（目标升级）**：proxy 在首次拒绝前已不可信 → 将剩余 token 提交给 target model 验证。

---

### 🔍 相比现有方法的优势
| 维度 | 传统 SD 方法（如 EAGLE-3） | TriSpec |
|------|----------------------------|--------|
| **Focus** | 优化 drafting 效率和 acceptance length (T) | 新增对 verification time (t_v) 的优化 |
| **Verifier** | 始终依赖 full target model | 引入 lightweight proxy 分流简单验证 |
| **成本控制** | 每轮都需完整执行 target model forward pass | 显著减少 target model 调用次数 |
| **灵活性** | 对 draft 分布质量敏感 | 更鲁棒，适用于多种 drafting 架构 |

> ✅ **优势总结**：TriSpec 不是替代而是增强现有 SD 方法（可与 EAGLE-3 等结合），从**验证侧**切入实现进一步加速，在保持准确性的前提下大幅提升吞吐量。

---

## 2. 核心实验方法和设置

### 📚 数据集
主要评估基于以下五类推理基准：
- **数学推理**：`GSM8K`, `MATH500`, `Gaokao-2023-EN (GK23-en)`
- **代码生成**：`HumanEval`, `MBPP`
- **综合与挑战性任务扩展测试**：
  - `SpecBench`（多轮对话、翻译、摘要、QA、数学、RAG）
  - `HotpotQA`（多跳问答）
  - `Polymath`（多语言数学）
  - `AIME24/AIME25`（竞赛级数学）
  - `GPQA-Diamond`（专家级 STEM 多选题）

---

### ⚙️ 实验设置
| 项目 | 设置说明 |
|------|----------|
| **Model Families** | `Qwen3`, `DeepSeek-R1-Distill-Qwen (DSQ)`, `DeepSeek-R1-Distill-LLaMA (DSL)` |
| **Target Models** | Qwen3-32B, DSQ-32B, DSL-70B |
| **Proxy Verifiers** | 同家族小模型：Qwen3-1.7B, DSQ-1.5B, DSL-8B |
| **Drafters** | 使用 HASS 或 EAGLE-3 架构的 single-layer drafter |
| **Draft Tree Config** | Depth=6, Top-k=10, Budget Tokens=60 |
| **Margin Threshold λ** | 默认设为 0.5 |
| **Hardware** | 单张 NVIDIA A100 80GB GPU（推理）；8×A100 训练 |

---

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **Accuracy** | Pass@1 准确率（reasoning）、LLM-judge 得分（SpecBench） |
| **Speedup** | 相对于纯 target model 自回归生成的速度提升倍数 |
| **Throughput (TPS)** | Tokens per second，衡量实际吞吐能力 |
| **Target-invocation ratio (r_t)** | 每生成一个 token 调用 target model 的比例，反映验证开销 |
| **End-to-end Latency (L)** | 总体生成延迟 |

---

### 🔁 基线方法对比
- **Single Model (Target/Proxy)**：仅用 target 或 proxy 自回归生成
- **HASS / EAGLE-3**：代表先进 SD 方法
- **SpecCascade variants**：
  - SC[Chow]：基于 drafter 置信度过滤
  - SC[OPT]：基于分布距离的最优拒绝对策
  - SC[Token]：完全依赖 target 分布决策（不依赖 drafter 输出）

> TriSpec 可与 HASS/EAGLE-3 结合形成 “HASS+TriSpec”、“EAGLE3+TriSpec”

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1 & Table 3）

#### 在 `Qwen3-32B` 上的表现（Temperature=0）：
| 方法 | Avg Accuracy | Speedup | Target Invocation Ratio (r_t) |
|------|-------------|---------|-------------------------------|
| EAGLE-3 | 84.4% | 2.85× | ~22% |
| **EAGLE3+TriSpec** | **84.2%** (-0.2%) | **3.55×** (**↑24.6%**) | **~10%** (**↓55%**) |

> ✅ **最高达 35% 的额外加速**，同时 **target model 调用减少超过一半**

#### 在 `DSQ-32B` 上的表现：
| 方法 | Speedup | r_t |
|------|--------|-----|
| EAGLE-3 | 3.22× | 21.45% |
| **EAGLE3+TriSpec** | **3.67×** (**↑14%**) | **13.25%** (**↓38%**) |

#### 在 `DSL-70B` 上的表现：
| 方法 | Speedup | r_t |
|------|--------|-----|
| EAGLE-3 | 2.48× | 26.88% |
| **EAGLE3+TriSpec** | **3.36×** (**↑35.5%**) | **15.18%** (**↓43%**) |

> 💡 所有模型族均实现 **15%-35% 的相对速度提升**，且 **accuracy 下降 <1%**

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）Draft Training Strategies（Table 5）
| 训练方式 | Speedup (Qwen3) | 说明 |
|--------|----------------|------|
| Joint from scratch | 3.55× | 性能略优，但训练成本更高 |
| Adapter-only finetune | 3.46× | 成本低，适合已有 drafter 权重场景 |

> ✅ 两种策略均可有效支持 TriSpec，提供灵活部署选择。

#### （2）Token Pruning（是否剪枝已验证 token）
| 策略 | Acceptance Length T | Speedup |
|------|--------------------|--------|
| w/o pruning | 4.58 | 3.46× |
| **w/ pruning** | **4.71** | **3.55×** |

> ✅ Token pruning 可进一步提升 T 和整体速度，证明 proxy 判断可靠。

#### （3）Routing Criteria 对比（Figure 5b）
比较不同路由策略：
- Top-1 Probability
- Entropy
- **Top-1 - Top-2 Margin（本文采用）**
- Learned Router (R2R)

> ✅ **Margin-based 路由在 accuracy-speedup trade-off 上表现最佳**，无需额外训练。

#### （4）Draft Length 影响（Figure 5c）
- TriSpec 最佳 draft length 与标准 SD 接近（4~5），表明兼容性强。
- 在多数长度下 TriSpec 均优于 baseline。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Verification cost 是 SD 中被忽视的关键瓶颈**，尤其在 high-efficiency drafter（如 EAGLE-3）普及后更为突出。
2. **同家族小模型具备强 token-level alignment**（Qwen3-1.7B vs 32B 达 82% exact match），是理想的 lightweight proxy verifier。
3. **Margin-based criterion 可有效区分 proxy 是否可信**，实现智能路由，避免盲目升级。
4. **TriSpec 可无缝集成到现有 SD 框架中**，带来显著加速（up to 35%↑ speedup），同时将 target model 调用降低 50% 以上。
5. **即使在非推理模型（LLaMA）上也有效**（Appendix B.5），显示良好泛化性。

---

### ⚠️ 局限性
1. **依赖同家族模型的存在**：若无合适规模的 proxy model，难以构建高效 pipeline。
2. **Margin threshold λ 需调参**：虽默认值 0.5 表现良好，但在极端任务中可能需要调整以平衡 accuracy-speed。
3. **训练成本略增**：需额外训练 adapter 模块（joint training 多耗约 50% 时间）。
4. **对极低质量 draft 更敏感**：若 drafter 本身输出极差，proxy 也可能频繁误判导致升级过多。

---

### 🔮 未来工作方向
1. **自动化 margin threshold tuning**：根据输入动态调整 λ，实现自适应精度-延迟权衡。
2. **探索跨家族 proxy**：研究不同架构间是否也可建立可靠的 proxy-target 对齐关系。
3. **硬件感知调度**：结合内存带宽、KV cache 状态等系统因素优化 routing 决策。
4. **多级 proxy cascade**：构建 hierarchical proxy hierarchy，实现更细粒度的成本分层卸载。

---

## ✅ 总结一句话
> **TriSpec 通过引入 lightweight proxy verifier 和 margin-based routing，在几乎不损失 accuracy 的前提下，将 speculative decoding 的验证成本大幅降低，实现了高达 35% 的端到端加速，为 LLM 高效推理提供了新范式。**

</details>

---

### 4. [Understanding Efficiency: Quantization, Batching, and Serving Strategies in LLM Energy Use](https://arxiv.org/abs/2601.22362)

**Authors**: Julien Delavande, Regis Pierrard, Sasha Luccioni  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22362v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in production, contributing towards shifting the burden in terms of computational resources and energy demands from training to inference. While prior work has examined the energy cost of inference per prompt or per token, we highlight how \emph...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Understanding Efficiency: Quantization, Batching, and Serving Strategies in LLM Energy Use**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
随着 Large Language Models（LLMs）从研究阶段转向大规模生产部署，**推理阶段的能源消耗**已成为AI可持续发展的关键瓶颈。尽管已有研究关注训练阶段的碳足迹，但对**系统级设计选择如何影响推理能效**的理解仍不足。

本文聚焦于以下核心问题：
- 数值精度（如 float32 vs int4）、批处理策略（batching）和服务架构（如请求调度）如何影响 LLM 推理过程中的 **energy consumption 和 latency**？
- 是否存在某些配置在理论上节能但在实际中反而增加能耗？

### **提出了什么新方法或新思路**
本研究并未提出新的模型结构，而是通过**系统级视角**揭示了三个关键因素对 LLM 能源效率的影响机制，并提出了一种“**相位感知的能效分析框架**”（phase-aware energy profiling），将推理分为两个阶段进行独立建模与优化：

1. **Prefill Phase**：处理输入 prompt 的前向传播。
2. **Decode Phase**：自回归生成输出 token。

在此基础上，作者系统地评估了：
- 不同 **numerical precision** 在不同计算范式下的表现；
- **static batching** 对 energy/token 的影响；
- 使用 **Text Generation Inference (TGI)** 服务时，**request arrival shaping** 如何提升 batching 效率并降低能耗。

此外，论文强调了“**the how of inference**”——即推理的服务方式本身（而不仅仅是模型大小或参数量）是决定能效的关键变量。

### **相比现有方法的优势**
| 维度 | 传统做法 | 本文优势 |
|------|--------|---------|
| 分析粒度 | 报告整体 energy/query | 按 **prefill / decode 阶段拆解**，实现细粒度诊断 |
| 量化效果评估 | 默认认为低精度一定更高效 | 揭示其仅在 **compute-bound 场景有效**，memory-bound 下可能适得其反 |
| 批处理分析 | 忽略 padding 开销 | 区分 **effective vs computed tokens**，揭示 padding 浪费 |
| 请求模式 | 假设随机到达 | 引入 **arrival shaping**，展示固定间隔可提升效率百倍 |

> ✅ **核心创新**：首次实证表明，**仅靠调整请求到达时间（arrival shaping）即可实现高达 100× 的 per-request energy reduction**，且无需修改模型或硬件。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- 数据来自一个匿名评审中的研究（Anonymous & Anonymous, 2025），名为 `ultrachat-10k` 子集。
- 包含 **10,000 条礼貌型 prompt**（以 "thank you" 结尾），模拟真实人机对话场景。
- 输入长度范围：**200–4000 tokens**（均值约 1200）
- 输出较短：**10–300 tokens**

> 所有 prompts 已适配各模型的输入格式（如 chat template）

### **实验设置**
#### **硬件平台**
- GPU: **NVIDIA H100 SXM 80GB**
- CPU: **8 × AMD EPYC 7R13 cores**
- 无其他共驻任务（dedicated node）

#### **软件栈**
- 模型加载：`Transformers` + `bitsandbytes`（用于 int8/int4 量化）
- 服务端测试：`Hugging Face Text Generation Inference (TGI)` v3.3.4
- 能耗测量：`CodeCarbon`（集成 NVML 和 pyRAPL）实时监控 GPU/CPU/RAM 能耗
- 内存估算：RAM 能耗基于 heuristic 公式推算

#### **被测模型**
- **Qwen 2.5**: 0.5B, 1.5B, 3B, 7B, 14B
- **Mistral-7B-Instruct-v0.3**
- **LLaMA 3.1-8B-Instruct**
- **LLaMA 3.1-70B-Instruct**（多卡测试）

#### **数值精度设置（5 种 dtype）**
| 类型 | 实现方式 |
|------|--------|
| float32, bfloat16, float16 | PyTorch 原生支持 |
| int8 | LLM.int8()（outlier-aware mixed precision） |
| int4 | NF4 格式 + on-the-fly dequantization |

#### **评估指标**
| 指标 | 定义 |
|------|-----|
| **Energy per request** | 单个请求的总能耗（Wh） |
| **Energy per token** | 分为 input/output token，进一步区分 effective vs padded |
| **Latency** | CUDA kernel 级别记录，平均 10 次运行结果 |
| **Batching Quality** | 平均 batch size、idle time、kernel fusion 程度 |

#### **基线方法对比**
| 基线 | 描述 |
|------|------|
| Baseline 1 | `transformers` 库默认顺序执行（no batching） |
| Baseline 2 | 使用 float32 精度 |
| TGI Baseline | 启用 continuous batching 和 kernel fusion |
| Arrival Pattern | 对比随机延迟 vs 固定间隔（50ms, 300ms, 500ms） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) 数值精度的影响**
| 场景 | 观察结果 |
|------|--------|
| **Prefill Phase**（大模型） | float16/bfloat16 相比 float32 可减少 **~4× GPU energy**（因启用 Tensor Cores） |
| **Prefill Phase**（小模型） | 小模型（如 Qwen-0.5B）为 memory-bound，低精度无益甚至轻微增耗 |
| **Decode Phase** | float32 / float16 / bfloat16 能耗几乎不变；**int8 反而高出 2–3×**（due to dequantization overhead） |
| **int4 表现** | 与 float32 相当，未见显著节能（尤其 decode 阶段） |

> 📉 图1 显示：decode 阶段 energy/token 几乎不随 dtype 改变，说明带宽限制下压缩无效。

#### **(2) 批处理（Batching）的影响**
| 指标 | 发现 |
|------|------|
| **Energy per effective input token (prefill)** | 随 batch size 上升而上升 → 因 **padding 导致冗余计算** |
| **Energy per computed input token (prefill)** | 基本恒定 → 符合 compute-bound 特性 |
| **Energy per output token** | 随 batch size 增加持续下降，**logarithmic trend**，最大节省达 **~65%**（vs b=1） |
| **Optimal batch size** | decode 阶段在 **b=4 达到最优**，更大 batch 收益递减 |

> ⚖️ Trade-off：prefill 受 padding 拖累，decode 受并行收益推动。

#### **(3) TGI + Arrival Shaping 的效果**
| 设置 | LLaMA 8B per-request energy | 提升倍数 |
|------|-------------------------------|----------|
| transformers (float32, no batching) | 1.2 × 10⁻¹ Wh | 1×（baseline） |
| TGI（random arrival） | 9.6 × 10⁻³ Wh | **~12.5× 更优** |
| TGI + fixed 500ms interval | **1.1 × 10⁻³ Wh** | **~100× 更优** |

> 💡 这意味着：**仅通过控制客户端请求节奏（arrival shaping），就能实现百倍节能！**

#### **(4) 大规模扩展验证（LLaMA 70B）**
- 使用 4× H100 GPUs
- TGI 仍可将 energy per request 控制在 **2.4 × 10⁻² Wh**
- **优于 8B 模型在原始设置下的表现**，证明动态 batching 和 traffic shaping 可良好扩展

---

## **4. 关键结论和发现**

### **主要发现**
1. 🔹 **Numerical precision reduction only helps in compute-bound regimes**
   - Prefill 阶段（尤其是大模型）受益明显（Tensor Core 加速）
   - Decode 阶段始终 memory-bound，**aggressive quantization（如 int8/int4）会引入额外 kernel launch 和 idle time，反而增加能耗**

2. 🔹 **Batching improves energy efficiency — but mechanism differs by phase**
   - Prefill：受限于 padding，energy/effective-token 可能恶化
   - Decode：强烈受益于 batching，因共享 KV cache 和 memory access
   - Output-token normalization 是更合理的评估方式

3. 🔹 **Serving infrastructure and request scheduling matter enormously**
   - 从 `transformers` 到 `TGI` 可带来 **12.5× 节能**
   - 再加上 **arrival shaping（固定间隔）可达 100× 节能**
   - “How you serve” 比 “What you serve” 更重要

4. 🔹 **Phase-aware profiling is essential**
   - Prefill 和 decode 具有根本不同的计算特征（compute-bound vs memory-bound）
   - 统一报告 aggregate energy 会掩盖关键瓶颈

5. 🔹 **Idle power is a major contributor**
   - GPU idle power ≈ 120W，即使短暂空闲也消耗可观能量
   - Kernel fragmentation（如 dequantization kernels）加剧此问题

---

### **方法的局限性**
| 局限 | 说明 |
|------|------|
| **Prompt多样性有限** | 使用的是短 prompt + 短 response 场景，未涵盖长上下文或多轮复杂交互 |
| **仅基于 H100 测试** | 结果可能无法直接迁移到 AMD Instinct、AWS Inferentia 或 TPU 等平台 |
| **忽略系统级开销** | 当前 energy 测量集中于 GPU，未充分计入 CPU、内存传输、网络 I/O 等成本 |
| **静态 batching vs 动态 batching** | 实验中 prefill 使用 static batching，未能完全反映 vLLM 或 TGI 的 full potential |

---

### **未来工作方向**
1. **跨硬件平台迁移性研究**  
   扩展至 AMD、Apple Silicon、Google TPU 等设备，建立通用能效预测模型。

2. **全栈能效建模（Full-stack Energy Modeling）**  
   整合 GPU + CPU + RAM + Network 的联合能耗模型，提供 end-to-end footprint 评估。

3. **客户端协同优化（Client-Aware Scheduling）**  
   设计轻量级协议引导用户/前端按最优节奏发送请求（e.g., rate limiting hints）。

4. **动态相位切换优化（Phase-adaptive Serving）**  
   根据当前 workload 自动切换 precision / batching strategy（如 prefill 用 int4，decode 用 bfloat16）。

5. **标准化能效报告规范**  
   推动社区采用 phase-separated、token-normalized、arrival-pattern-aware 的 energy reporting protocol。

---

> ✅ **最终洞见**：  
> “可持续的 LLM 部署不仅取决于模型本身，更取决于整个 serving stack 的协同设计。”  
> —— **Efficiency is not just about the model, but about the orchestration.**

</details>

---

### 5. [Quartet II: Accurate LLM Pre-Training in NVFP4 by Improved Unbiased Gradient Estimation](https://arxiv.org/abs/2601.22813)

**Authors**: Andrei Panferov, Erik Schultheis, Soroush Tabesh, Dan Alistarh  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.22813v1  

#### Abstract
The NVFP4 lower-precision format, supported in hardware by NVIDIA Blackwell GPUs, promises to allow, for the first time, end-to-end fully-quantized pre-training of massive models such as LLMs. Yet, existing quantized training methods still sacrifice some of the representation capacity of this format...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Quartet II: Accurate LLM Pre-Training in NVFP4 by Improved Unbiased Gradient Estimation*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **NVFP4** 的全量化大语言模型（LLM）预训练虽然能显著降低计算成本并提升吞吐量，但在精度上仍明显落后于 FP16/FP8 训练。其主要原因在于：
- **前向传播**中为节省量化开销而采用的“square-block quantization”牺牲了表示能力（representation capacity），导致激活值和权重的量化误差增大；
- **反向传播**中广泛使用的 **Stochastic Rounding (SR)** 虽然保证无偏性（unbiasedness），但引入了过高的方差，显著增加了梯度估计误差。

### 🚀 提出的新方法与思路

#### （1）**MicroScaling EDEN (MS-EDEN)**  
一种全新的、适用于微缩放格式（microscaling formats）如 NVFP4 的**无偏量化原语（unbiased quantization primitive）**：
- 将随机性从单个 FP4 元素转移到更高精度的 **microscale 因子（FP8 scale）** 上；
- 利用 **Randomized Hadamard Transform (RHT)** 进行分布平滑，并通过一个可证明无偏的校正因子 $ S $ 来补偿量化偏差；
- 在 FP8 scale 层面使用 **Stochastic Rounding** 实现对校正因子的无损编码，从而在硬件兼容的前提下保持整体期望无偏。

> 💡 **核心思想**：避免在低精度 FP4 层面引入高方差的随机舍入，转而在相对高精度的 scale 上进行可控的随机化，大幅降低 MSE。

#### （2）**Quartet II：全 NVFP4 线性层训练图**
构建了一个完整的、端到端的 NVFP4 训练方案：
- **前向传播**：使用 **Round-to-Nearest (RTN)** + **FourOverSix (4/6)** scale selection 启发式，最大化表示能力；
- **反向传播**：采用 **MS-EDEN** 进行梯度量化，结合内维度的 RHT 旋转，实现高质量无偏梯度估计；
- 放弃“square-block quantization”，允许权重和激活在反向传播时重新量化，以换取更好的前向表示和更优的整体性能。

### 🔍 相比现有方法的优势

| 维度 | 优势说明 |
|------|--------|
| **量化误差** | MS-EDEN 的 MSE 比 SR 降低 **超过 2 倍**（见 Table 1）； |
| **前向表示能力** | 使用 native 1x16 分组而非 16x16 square-block，保留更多动态范围细节； |
| **梯度质量** | 在保证无偏性的前提下，显著优于 SR 和 TetraJet-v2； |
| **系统效率** | 提供高效 CUDA kernel，支持 Blackwell GPU，实测训练速度达 BF16 的 **4.2x**； |
| **协同优化** | 与 “4/6” 等技术正交且互补，形成更强组合。 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **主实验**：`C4` 数据集，用于 Llama-2 架构风格模型的语言建模任务；
- **大规模验证**：`FineWeb-Edu` 数据集，配合 `Nanochat` 训练流程，包含后续 fine-tuning 阶段；
- 测试基准包括：`ARC`, `GSM8K`, `HumanEval`, `MMLU` 等零样本评测任务。

### ⚙️ 实验设置
- **模型架构**：Transformer-based，遵循 Llama-2 设计；
- **参数规模**：从小型（30M–200M）到大型（560M, 1.9B）不等；
- **训练长度**：最长达 **38B tokens**，覆盖 compute-optimal 至 over-trained 区域；
- **优化器**：AdamW / Muon，cosine 学习率调度；
- **超参一致性**：所有 QAT 实验复用 BF16 基线的超参数（LR、weight decay 等）。

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Validation Loss / Perplexity** | 主要衡量预训练阶段的语言建模能力； |
| **Bits-per-byte (BPB)** | Nanochat 中的关键压缩指标，越低越好； |
| **Zero-shot Accuracy (%)** | 在 ARC、GSM8K 等下游任务上的表现； |
| **Relative Loss Gap vs BF16** | 衡量量化方法相对于全精度训练的退化程度； |
| **Speedup over BF16** | 实际 kernel 层面和端到端训练的加速比。 |

### 🆚 基线方法对比
- **NVIDIA et al. (2025)**：首个端到端 NVFP4 预训练方案，使用 square-block + SR；
- **TetraJet-v2 (Chen et al., 2025b)**：改进版，加入 outlier 控制与振荡抑制；
- **FourOverSix (Cook et al., 2025)**：提出自适应选择 scale 网格以减少 MSE；
- **Stochastic Rounding (SR)**：当前主流的无偏量化手段，作为反向传播基线。

> 注：作者指出 TetraJet-v2 的部分设计（如中间 FP32 scale）难以在 GPU 上高效实现，因此仅比较其“GPU 可行版本”。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

| 指标 | 结果 |
|------|------|
| **MS-EDEN vs SR 的 MSE** | MS-EDEN: **9.8×10⁻³**，SR: **23.5×10⁻³** → **降低 >2x**（Table 1） |
| **最大训练加速比** | 达到 **4.2x over BF16**（Figure 6） |
| **真实训练吞吐提升** | 1.1B 模型上达到 **2.45x** 实际 token/s 提升（Appendix D） |
| **预训练损失缩减** | 相比 TetraJet-v2 平均缩小 **15–25%** 的 BF16 差距（Figure 5） |

### 🔁 与基线方法的对比结果

#### （1）**完整训练性能（Figure 4）**
- Quartet II 在不同参数量（30M–200M）和数据比例下，**始终优于所有基线**；
- 相比 TetraJet-v2，平均 **loss gap 缩小至少 20%**；
- 特别是在高 D/N（over-trained）区域，优势更加明显。

#### （2）**Nanochat 大规模训练（Figure 5 & Table 5）**
- 在 1.9B 参数、38B tokens 设置下：
  - **Val BPB 提升显著**：Quartet II 达到 0.7025，优于 TetraJet-v2 的 0.7044；
  - **相对 BF16 的 BPB 增加最小**：仅 **+1.44%**，而 TetraJet-v2 为 +1.72%，NVIDIA 为 +1.92%；
- Zero-shot 性能差异不显著，可能受限于微调数据量和测试集大小。

### 🔍 消融实验结果

#### （1）**反向传播量化策略分析（Figure 1）**
- 当同时量化两个 GEMM 输入张量时（即 fully quantized backward）：
  - MS-EDEN with weight re-quantization (**e**) 明显优于 SR without re-quantization (**d**)；
  - 证明即使增加 re-quantization 开销，MS-EDEN 仍能提供更优梯度估计。

#### （2）**前向传播策略分析（Figure 2）**
- “4/6” 技术在 native 1x16 分组下效果最佳，相比 square-block 提升约 **两倍**；
- native 分组本身也优于 square-block，说明更好的表示能力至关重要。

#### （3）**无偏性验证（Figure 9）**
- 对平均梯度的收敛行为进行测试：
  - Quartet II、NVIDIA、TetraJet-v2 的误差随 accumulation steps 下降符合 $ \sim 1/B $，表明**真正无偏**；
  - “4/6 + SR” 出现 plateau，说明其在反向传播中**引入系统性偏差**，不适合用于 backward pass。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Stochastic Rounding 不是最优解**：尽管被广泛采用，但在 NVFP4 场景下方差过高，限制了精度上限；
2. **MS-EDEN 更优**：通过将随机性移至 scale 层面，可在保持无偏的同时大幅降低 MSE；
3. **放弃 square-block 是值得的**：虽然需 re-quantize 权重，但换来前向表示能力和整体性能的净增益；
4. **“4/6” 应用于前向传递最有效**：不应与 SR 联合用于 backward，否则破坏无偏性；
5. **软硬协同设计可行**：提出的 kernel 支持已在 Blackwell GPU 上验证，具备实用价值。

### ⚠️ 方法的局限性
- **依赖 RHT 旋转**：要求量化块是旋转块的子集，限制了某些并行策略；
- **额外计算开销**：RHT 和两次 quantization/dequantization 引入一定延迟（但可通过 fused kernel 缓解）；
- **目前仅限线性层**：注意力机制中的其他组件未完全量化；
- **zero-shot 表现差异不大**：可能因微调阶段关闭了 backward quantization 所致。

### 🔮 未来工作方向
- 将 MS-EDEN 推广至 **MXFP4** 或其他微缩放格式；
- 探索 **非均匀分组策略** 以进一步优化 outlier 处理；
- 实现 **完全融合的训练 kernel**，消除 memory bound；
- 将该框架扩展至 **fine-tuning 和推理阶段**，打造统一的 4-bit 训练-部署流水线；
- 探索 **更低比特（如 2-bit）下的类似构造**。

---

> ✅ **一句话总结**：  
> *Quartet II 通过提出新型无偏量化方法 MS-EDEN，解决了 NVFP4 训练中梯度估计误差大的问题，在不牺牲稳定性的前提下显著提升了模型精度，并实现了高达 4.2x 的训练加速，推动了全 4-bit LLM 预训练迈向实用化。*

</details>

---

### 6. [SPLA: Block Sparse Plus Linear Attention for Long Context Modeling](https://arxiv.org/abs/2601.22379)

**Authors**: Bailin Wang, Dan Friedman, Tao Lei, Chong Wang  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.22379v1  

#### Abstract
Block-wise sparse attention offers significant efficiency gains for long-context modeling, yet existing methods often suffer from low selection fidelity and cumulative contextual loss by completely discarding unselected blocks. To address these limitations, we introduce Sparse Plus Linear Attention ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SPLA: Block Sparse Plus Linear Attention for Long Context Modeling**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前在长上下文建模中，**Block-wise Sparse Attention**（如 NSA、InfLLM）虽然提升了推理效率，但仍面临两大核心挑战：
1. **低选择保真度（Low Selection Fidelity）**：基于压缩表示（如均值池化）的块选择机制难以准确识别真正相关的 KV Cache 块，导致关键语义信息被遗漏。
2. **累积上下文丢失（Cumulative Contextual Loss）**：通过硬截断（hard truncation）丢弃未选中的“长尾”块，随着序列增长，这些被忽略的概率质量逐渐累积，导致输出严重偏离全注意力（dense attention）模型。

### **提出了什么新方法或新思路**
本文提出 **SPLA**（Sparse Plus Linear Attention），一种结合**精确稀疏注意力**与**残差线性注意力**（Residual Linear Attention, RLA）的新框架，其核心思想是将上下文划分为两个互补部分：
- **Exact Component**：对高相关性的“峰值”块执行标准的稀疏注意力。
- **Approximate Component**：对剩余的“长尾”块通过 RLA 进行高效压缩并保留其全局影响。

具体创新包括：
- **基于二阶泰勒展开的块选择机制**：利用每个块的均值和协方差统计量构建更精确的选择分数，建立从 token-level 注意力到 block-level 检索的数学桥梁。
- **无需显式访问的残差线性注意力（RLA）**：通过减法形式实现 RLA，即 `o_residual = o_global_linear - o_selected_linear`，避免在推理时显式读取未选块，保持 I/O 效率。
- **零新增投影参数的设计**：共享原始 Q/K/V 投影权重，仅引入一个无参数的指数特征映射（exponential feature map），确保可直接适配预训练模型。

### **相比现有方法的优势**
| 对比维度 | SPLA | NSA / InfLLM |
|--------|------|-------------|
| **选择精度** | 高（利用二阶统计） | 低（依赖一阶均值或启发式） |
| **上下文完整性** | 保留全部上下文（通过 RLA 压缩长尾） | 截断长尾，造成信息损失 |
| **I/O 开销** | 不增加（未选块不访问） | 同等稀疏模式下相当 |
| **适配成本** | 极低（仅增加少量 RMSNorm 参数） | 可能需重新训练或微调 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验围绕 **Continual Pretraining (CPT)** 场景展开，涵盖三类任务：
1. **General Knowledge**：混合数学、代码和通用知识领域，上下文长度为 32k。
2. **Long-Context**：长文档与合成问答对，用于评估 RULER 基准上的长程建模能力（最长至 256k）。
3. **Reasoning**：复杂推理任务，包括 AIME、HMMT、LiveCodeBench、HumanEval、GPQA、MMLU Pro 等。

### **实验设置和评估指标**
- **模型规模**：14B 参数模型（详见 Table 1）
- **训练配置**：
  - 全局 batch size：32M tokens
  - 上下文长度：8,192（预训练阶段）
  - 硬件平台：Cloud TPU v6e（1024 芯片，8×128 切片）
  - 并行策略：FSDP + Sequence Parallelism
  - 优化器：AdamW
- **稀疏设置**：
  - Block size $ B = 128 $
  - Stride $ s = 16 $，Compression window $ C = 32 $
  - Top-$ k = 32 $ 块检索
  - 强制包含初始块（attention sink）和最近 4 个局部块
- **评估指标**：
  - 准确率（Accuracy）：各基准测试集得分
  - 推理延迟（Decoding Latency）：step time speedup %
  - 训练吞吐量（Throughput）

### **基线方法对比**
- **DENSE**：全注意力模型（上限）
- **NSA**（Native Sparse Attention）：多分支结构，存在信息重复计算
- **InfLLM-v2**：基于均值选择 + 硬截断，代表“truncate-only”范式
- **SPA**（消融变体）：SPLA 但移除 RLA 模块，用于验证 RLA 有效性

所有模型统一在 JAX + TPU 上实现，保证公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表 2：通用知识基准（平均得分）**
| Method | Average (0/1-shot) | MMLU (5-shot) | GSM8K (8-shot) |
|-------|--------------------|---------------|----------------|
| DENSE | 67.5               | 77.3          | 90.2           |
| NSA   | 66.5               | 77.4          | 90.5           |
| INF-V2| 67.3               | 77.8          | 90.0           |
| SPA   | 67.3               | 78.1          | 90.2           |
| **SPLA** | **67.7**       | **78.6**      | **90.5**       |

> ✅ SPLA 在多数指标上超越 dense 模型，尤其在 MMLU 上表现突出。

#### **表 3：RULER 长上下文基准（最高为优）**
| Model \ Length | 4k  | 8k  | 16k | 32k | 64k | 128k | 256k |
|----------------|-----|-----|-----|-----|-----|------|------|
| DENSE          | 95.8| 94.9| 93.6| 91.4| 87.1| 83.2 | 69.3 |
| NSA            | 92.3| 91.4| 90.7| 84.6| 83.2| 51.3 | 32.5 |
| INF-V2         | 95.9| 94.1| 92.1| 89.3| 86.7| 61.6 | 42.6 |
| SPA            | 95.9| 94.2| 93.3| 90.4| 87.1| 62.4 | 44.5 |
| **SPLA**       | **95.9**| **94.7**| **94.2**| **91.7**| **88.3**| **85.2** | **72.3** |

> 🔥 **SPLA 在 256k 上达到 72.3，远超 dense 的 69.3 和其他稀疏方法（<45）**，首次实现“越稀疏越强”。

#### **表 4：推理与知识综合任务**
| Metric        | DENSE | NSA  | INF-V2 | SPA  | **SPLA** |
|--------------|-------|------|--------|------|----------|
| AIME 2024     | 77.1  | 73.1 | 76.8   | 77.2 | **78.3** |
| HumanEval    | 85.4  | 78.7 | 86.6   | 86.0 | **86.6** |
| GPQA         | 68.5  | 59.6 | 68.7   | 69.2 | **69.5** |
| MMLU Pro     | 78.9  | 68.8 | 79.3   | 79.1 | **79.3** |

> ✅ SPLA 在复杂推理任务中显著优于 NSA，并小幅领先 INF-V2 和 SPA。

---

### **消融实验结果**
- **SPA vs. InfLLM-v2**：SPA 使用二阶泰勒选择，在相同架构下全面优于 InfLLM-v2 → 表明**选择机制改进有效**。
- **SPLA vs. SPA**：SPLA 在长上下文（尤其是 >64k）持续提升性能 → 明确证明**RLA 模块能缓解长尾发散问题**。
- **NSA 性能下降明显**：在 128k 和 256k 上急剧退化（51.3 → 32.5），因其双分支设计导致分布偏移。

---

## **4. 关键结论和发现**

### **主要发现**
1. **SPLA 成功弥合了稀疏与全注意力之间的性能鸿沟**，在长达 256k 的序列上仍能逼近甚至超越 dense 模型。
2. **残差线性注意力（RLA）是关键**：它以极低成本保留了被传统稀疏方法丢弃的“长尾”信息，防止随长度增长而发散。
3. **基于二阶泰勒展开的选择机制更可靠**：相比仅用均值的方法，能更准确识别重要块，提升 recall。
4. **极低参数开销即可完成适配**：SPLA 几乎无需新增参数（仅 RMSNorm 缩放因子），非常适合将已有 dense 模型无缝升级为高效稀疏版本。

### **方法的局限性**
- 当前实现依赖于 TPU 的大 block size（B=128），在 GPU 上可能需要调整以获得最佳性能。
- RLA 的稳定性依赖于隐藏状态使用 float32 精度积累，对低精度部署有一定要求。
- 虽然训练效率接近 dense 模型，但在小 group size（G=5）时仍有轻微开销，建议使用更大的 GQA 组以提升并行效率。

### **未来工作方向**
- 探索从头训练（training from scratch）场景下的解耦设计，例如为 RLA 分支学习专用投影矩阵。
- 尝试更灵活的 grouping 策略（如 multi-value head 结构）以进一步提升效率。
- 扩展至 prefill 阶段的稀疏优化，实现端到端的高效长上下文处理。

---

> 💡 **总体评价**：  
> SPLA 是一种**原则性强、工程友好、性能卓越**的稀疏注意力新范式。它不仅解决了现有方法的根本缺陷，还提供了一条**平滑演进路径**：**先用 dense attention 完成昂贵的预训练，再通过 SPLA 实现高效的长上下文适配**。这一思路有望成为下一代 Foundation Model 服务的标准组件。

</details>

---

### 7. [DART-ing Through the Drift: Dynamic Tracing of Knowledge Neurons for Adaptive Inference-Time Pruning](https://arxiv.org/abs/2601.22632)

**Authors**: Abhishek Tyagi, Yunuo Cen, Shrey Dhorajiya, Bharadwaj Veeravalli, Xuanyao Fong  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22632v1  

#### Abstract
Large Language Models (LLMs) exhibit substantial parameter redundancy, particularly in Feed-Forward Networks (FFNs). Existing pruning methods suffer from two primary limitations. First, reliance on dataset-specific calibration introduces significant data dependency and computational overhead. Second...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：DART-ing Through the Drift: Dynamic Tracing of Knowledge Neurons for Adaptive Inference-Time Pruning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）存在显著的参数冗余，尤其是在 **Feed-Forward Networks (FFNs)** 中。现有的剪枝方法面临两大挑战：
1. **依赖特定数据集进行校准**（dataset-specific calibration），导致计算开销大且泛化能力差；
2. **静态剪枝策略**无法适应自回归生成过程中上下文动态演化的特性，导致“**知识漂移**”（knowledge drift）——即在生成过程中，原本被剪掉的重要神经元可能变得关键。

### 🚀 提出的新方法：DART（Dynamic Attention-Guided Runtime Tracing）
DART 是一种轻量级、无需训练、运行时动态调整的推理阶段剪枝框架，其核心思想是：
- 利用 **attention 输出分布的变化** 来检测语义上下文的转移；
- 动态更新 **neuron-level masks**，保留当前上下文中重要的知识神经元。

#### 主要模块：
1. **Context-aware Neuron Selector**  
   - 基于每层 FFN 对残差流的几何贡献，自动分配稀疏预算；
   - 不依赖外部数据或辅助模型，实现确定性的逐层剪枝。

2. **Context Switch Detector（知识漂移探测器）**  
   - 监控 attention 层输出向量的分布偏移（cosine similarity 下降）；
   - 当偏移超过阈值时触发 mask 更新，恢复因上下文变化而需要的知识神经元。

### 🔍 相比现有方法的优势
| 维度 | 静态剪枝（如 WANDA, SPARSEGPT） | 动态剪枝（如 DEJAVU, GRIFFIN） | **DART（本文）** |
|------|-------------------------------|------------------------------|------------------|
| 是否需训练 | 否（部分） | 是（DEJAVU） | ❌ 否 |
| 是否依赖校准数据 | 是 | 是 | ❌ 否 |
| 是否感知上下文演化 | ❌ 否 | 有限支持 | ✅ 强感知 |
| 是否处理跨层交互 | ❌ 否 | 有限 | ✅ 显式建模 |
| 内存开销 | 低 | 中高 | **极低（<10MB for Llama-3.1-8B）** |
| FLOPs 开销 | 低 | 中 | **仅 0.1%** |

> ✅ DART 在保持极低资源消耗的同时实现了更强的上下文适应性和准确性。

---

## 2. 核心实验方法和设置

### 📚 数据集
实验覆盖多个任务类型以验证通用性：

#### 零样本任务（Zero-Shot Tasks）：
- **BoolQ**, **RTE**, **HellaSwag**, **Winogrande**, **ARC-E/C**, **OBQA**

#### 领域特定多跳任务（Multi-Shot Domain-Specific）：
- **MMLU**（涵盖52个学科）
- **GPQA**（研究生级别问答）
- **MEDMCQA**（医学领域问答）

#### 长文本生成与摘要任务（Long-Horizon Generation & Summarization）：
- **CNN/DailyMail**
- **Multi-News**
- **GovReport**

此外还构建了一个**自定义多主题提示数据集**（Custom Prompt Dataset, 500 prompts），用于测试跨领域生成中的知识追踪能力。

### ⚙️ 实验设置
- **模型**：LLAMA-3.2-3B, LLAMA-3.1-8B，扩展至 QWEN3, DeepSeek-R1, Mistral-7B 等
- **剪枝比例**：FFN 子层达到 **70% 稀疏度**
- **精度格式**：FP8 计算 / FP16 通信
- **硬件平台**：双 AMD EPYC 9654 CPU + 8×NVIDIA L40S GPU

### 📊 评估指标
| 类型 | 指标 |
|------|------|
| 分类/理解任务 | **Accuracy (%)** |
| 摘要任务 | **ROUGE-L**, **BLEU**, **BERTScore**, **Embedding Similarity**, **Coverage** |
| 动态行为分析 | **Cosine Similarity 轨迹**, **Retrigger 次数统计** |

### 🆚 基线方法对比
| 方法 | 类型 | 是否训练 | 是否动态 |
|------|------|--------|---------|
| **WANDA** | 静态、权重级剪枝 | 否 | ❌ |
| **SPARSEGPT** | 静态、非结构化剪枝 | 否 | ❌ |
| **DLP / OWL** | 静态、基于重要性评分 | 否 | ❌ |
| **DEJAVU** | 动态、需训练预测器 | ✅ | ✅ |
| **GRIFFIN** | 动态、复用预填充激活 | 否 | ✅（有限） |
| **DART (ours)** | **动态、无训练、注意力引导** | ❌ | ✅✅ |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（70% FFN Sparsity）

#### 表格：在 LLAMA-3.1-8B 上的零样本与领域任务准确率对比（单位：%）

| BENCHMARK | DENSE | WANDA | DEJAVU | **DART (Ours)** |
|----------|-------|--------|--------|----------------|
| **BoolQ** | 83.09 | 67.79 | 42.14 | **66.20** |
| **RTE** | 71.12 | 52.70 | 47.65 | **55.23** |
| **HellaSwag** | 79.32 | 44.99 | 26.68 | **64.58** |
| **Winogrande** | 74.51 | 56.43 | 50.19 | **65.98** |
| **ARC-E** | 82.53 | 50.34 | 25.08 | **59.43** |
| **ARC-C** | 54.95 | 28.16 | 26.27 | **38.99** |
| **OBQA** | 45.60 | 27.80 | 29.00 | **29.40** |
| **MMLU (5-shot)** | 66.61 | 29.70 | 25.20 | **34.14** |
| **GPQA (5-shot)** | 31.38 | 25.24 | 24.40 | **27.21** |
| **MEDMCQA (5-shot)** | 56.66 | 25.65 | 28.16 | **29.35** |

> 💡 **DART 平均提升高达 +14.5%~19.6%**，尤其在复杂推理任务（如 HellaSwag, ARC-E）上优势明显。

---

### 🔁 动态剪枝 vs 静态剪枝（摘要任务）

| 模型 | 方法 | ROUGE-L | BLEU | BERTScore | Coverage |
|------|------|---------|-------|-----------|----------|
| Llama-3.1-8B | Dense | 0.42 | 0.28 | 0.85 | 0.91 |
| | Static Pruning | 0.14 | 0.09 | 0.76 | 0.63 |
| | **DART (w/ tracing)** | **0.38** | **0.26** | **0.83** | **0.89** |

> ✅ DART 实现了 **3× 更高的 ROUGE-L 分数**，接近原始密集模型表现。

---

### 🔍 消融实验结果（Ablation Study）

| 设置 | ROUGE-L | EMBED SIM | COVERAGE |
|------|---------|-----------|----------|
| 50% Sparsity（无追踪） | 0.27 | 0.85 | 0.77 |
| **+ Knowledge Tracing** | **0.38** | **0.92** | **0.89** |

> ✅ 加入知识追踪机制后，所有指标显著提升，证明其对缓解知识漂移至关重要。

#### 触发重剪次数统计：
- 在 **45k~98k tokens** 的长文档中，平均仅触发 **~100 次 mask 更新**
- 表明上下文演变平滑，DART 只在必要时刻干预，效率极高。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **知识漂移是动态剪枝的关键失败模式**  
   - 固定 mask 会阻碍模型进入新的知识领域，导致生成内容退化为重复片段或无意义标题（gibberish）。
   
2. **attention 输出分布可作为上下文切换的有效信号**  
   - attention 向量的 cosine similarity 下降能提前预警语义迁移，为 mask 更新提供可靠依据。

3. **DART 实现高效且精准的运行时剪枝**  
   - 仅增加 **0.1% FLOPs 开销** 和 **<10MB 内存占用**，即可大幅提升剪枝模型的表现；
   - 性能逼近原始密集模型，在多种任务上优于现有静态与动态方法。

4. **更大的模型更受益于 DART**  
   - 大模型中知识表示更解耦（disentangled），功能分离更清晰，因此更容易通过选择性激活来维持性能。

---

### ⚠️ 局限性
1. **目前仅剪枝 FFN 层**，未涉及 attention 子层；
2. **mask 更新延迟问题**：若检测到漂移太晚（如 >500 tokens），可能已陷入不可逆的语义陷阱；
3. **超参数敏感性**：漂移检测阈值 `σ` 和窗口大小 `T` 需合理设置；
4. **不适用于极端稀疏场景**（>90%），此时即使动态更新也难以恢复完整表达能力。

---

### 🔮 未来工作方向
1. 将 DART 扩展至 **attention 层剪枝**，探索全模块联合稀疏化；
2. 结合 **KV Cache 压缩** 技术，进一步降低长序列推理内存；
3. 探索 **learnable drift detector**，替代手工设定的统计阈值；
4. 应用于 **边缘设备部署**，推动 LLM 在移动端的实时高效运行；
5. 研究 DART 在 **多模态模型** 中的应用潜力。

---

## ✅ 总结
DART 提出了一种新颖的 **动态注意力引导运行时追踪机制**，有效解决了传统剪枝方法在自回归生成中因“知识漂移”而导致的性能下降问题。它无需训练、不依赖校准数据、资源开销极小，却能在多种任务上显著超越现有静态与动态剪枝方法，是迈向高效、自适应 LLM 推理的重要一步。

> 🔗 代码开源地址：[https://github.com/seeder/research/DART](https://github.com/seeder/research/DART)

</details>

---

### 8. [Continual Policy Distillation from Distributed Reinforcement Learning Teachers](https://arxiv.org/abs/2601.22475)

**Authors**: Yuxuan Li, Qijun He, Mingqi Yuan, Wen-Tse Chen, Jeff Schneider, Jiayu Chen  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22475v1  

#### Abstract
Continual Reinforcement Learning (CRL) aims to develop lifelong learning agents to continuously acquire knowledge across diverse tasks while mitigating catastrophic forgetting. This requires efficiently managing the stability-plasticity dilemma and leveraging prior experience to rapidly generalize t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Continual Policy Distillation from Distributed Reinforcement Learning Teachers**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
该论文聚焦于**Continual Reinforcement Learning (CRL)** 中的核心挑战：
- **灾难性遗忘（Catastrophic Forgetting）**：模型在学习新任务时遗忘旧任务的知识。
- **稳定性-可塑性困境（Stability-Plasticity Dilemma）**：如何在保持已有技能稳定的同时高效学习新技能。
- **样本复杂度高**：传统多任务强化学习（Multi-task RL）需要大量环境交互（如 Meta-World 上需千万级步数），计算成本高昂。

### **提出的新方法与新思路**
作者提出了一种新颖的 **teacher-student 框架**，将 CRL 分解为两个独立过程：
1. **分布式 RL 教师训练**：多个单任务 RL agent 并行训练，作为“专家教师”。
2. **持续策略蒸馏（Continual Policy Distillation）**：将教师策略通过模仿学习（Imitation Learning）逐步蒸馏到一个中心化的通用学生模型中。

#### **关键技术组件**：
- **Transformer-based Mixture-of-Experts (MoE) 架构**：
  - 学生模型采用基于 Transformer 的 MoE 结构，支持稀疏激活和动态扩展。
  - 每个新任务到来时，**增量添加新的 expert**，提升模型容量（plasticity）。
- **Contrastive Task Embedding**：
  - 不依赖 one-hot 任务 ID，而是从状态序列中学习语义丰富的 task embedding，更适用于开放世界设定。
- **混合防遗忘机制**：
  - **Experience Replay + Parameter Regularization** 联合使用：
    - **Diversity-Aware Trajectory Replay**：利用 Determinantal Point Process (DPP) 选择最具代表性和多样性的轨迹存入回放缓冲区（仅占总数据 <10%）。
    - **Hierarchical Parameter Masking**：
      - 第一阶段后冻结共享主干（如 Attention、Embedding 层）。
      - 新专家初始化采用 Net2WiderNet 策略，并设置冷启动偏置（Cold-Start Bias Initialization）防止路由不稳定。

### **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **可扩展性** | 将复杂的在线 RL 学习转化为稳定的监督式策略蒸馏，更适合大规模预训练。 |
| **效率** | 利用并行教师训练避免顺序 RL 的长周期；蒸馏仅需约 70k 步教师经验即可恢复 >95% 教师性能。 |
| **稳定性与可塑性平衡** | MoE 扩展 + 参数掩码有效缓解干扰，实现低遗忘下的持续学习。 |
| **架构兼容性** | 支持现代大模型架构（如 Transformer），而传统 actor-critic 方法难以优化此类结构。 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Meta-World MT25**：包含 25 个不同机器人操作任务（如 `peg-insert`, `door-open`, `button-press` 等），是当前主流的多任务/持续学习 RL 基准。
- 使用 V2 密集奖励函数，每任务最大步数为 500。

### **实验设置**
- **教师训练**：
  - 使用 **PPO** 算法独立训练每个任务的 expert policy。
  - 总共收集约 70,000 步高质量示范轨迹用于蒸馏。
- **学生模型训练**：
  - 输入历史状态-动作对，预测连续动作。
  - 采用两阶段和五阶段蒸馏协议：
    - **Two-stage**：先学 MT10 → 再学剩余 15 个任务。
    - **Five-stage**：每次学习 5 个任务，共 5 阶段。
- **模型结构**：
  - Transformer-MoE，隐藏维度 256，5 层，初始 8 个 experts，每阶段新增 1 个 expert/层。

### **评估指标**
| 指标 | 定义 | 含义 |
|------|------|------|
| **Average Accuracy (Acc)** | $ \frac{1}{K}\sum_{k=1}^{K} a_{K,k} $ | 所有任务上的平均最终成功率 |
| **Backward Transfer (BWT)** | $ \frac{1}{K-1}\sum_{j=1}^{K-1}(a_{K,j} - a_{j,j}) $ | 衡量遗忘程度，正值表示正向迁移，负值表示遗忘 |

> 注：$ a_{k,j} $ 表示第 $ k $ 阶段后任务 $ j $ 的测试性能。

### **基线方法对比**
| 类型 | 方法 | 描述 |
|------|------|------|
| 下界 | **Finetune**, **Trac** | 无任何防遗忘机制的顺序微调 |
| 上界 | **Independent** | 每阶段训练独立模型，无遗忘但无法共享知识 |
| 正则化 | **EWC**, **KL** | 基于 Fisher 信息或 KL 散度约束参数变化 |
| 回放 | **Replay** | 仅使用轨迹回放，无结构扩展 |
| 消融 | **Expert Only**, **Replay Only** | 缺少某一关键模块的变体 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
#### ✅ **两阶段蒸馏结果（MT10 → MT25）**
- 学生模型恢复了超过 **85% 的教师性能**。
- 任务级别遗忘控制在 **<10%**（BWT 接近零甚至为正）。
- 在 MT10 多任务蒸馏中达到 **88.9% 成功率**，接近联合训练上限（90.8%），优于所有 multi-task RL 方法（见下表）。

##### **Table 1: MT10 多任务性能对比（Our vs. Multi-task RL Baselines）**
| 方法 | 平均准确率 |
|------|-----------|
| SAC | 61.9±3.3 |
| MTSAC | 62.9±8.0 |
| Soft-Module | 63.0±4.2 |
| CARE | 76.0±6.9 |
| PaCo | 85.4±4.5 |
| MOORE | 88.7±5.6 |
| **Ours (Central Model)** | **88.9±1.9** |
| **Ours (Avg Teachers)** | **90.8±1.4** |

> ➤ 我们的方法不仅超越所有 RL 方法，且仅用 **3M 蒸馏步数**，远低于 RL 方法所需的 **20M 环境交互步数**。

#### ✅ **五阶段持续学习结果（Table 2）**
| 方法 | Stage 5 Acc | Stage 5 BWT |
|------|-------------|-------------|
| Finetune | 26.1% | -55.3% |
| EWC | 35.9% | -37.9% |
| KL | 21.6% | -24.7% |
| Replay Only | 57.2% | -11.0% |
| Expert Only | 28.0% | -51.3% |
| **Ours** | **69.4%** | **+2.3%** |

> ➤ **Ours 在 Acc 和 BWT 上全面领先**，实现了高准确性与正向迁移能力。

---

### **消融实验结果（Ablation Study）**
| 变体 | Acc ↓ | BWT ↓ | 发现 |
|------|--------|--------|------|
| **No Freeze-Gating** | 64.5% → 69.4% | 0.8% → 2.3% | 冻结门控有助于保留路由路径 |
| **No Shared-Frozen** | 60.0% → 69.4% | -8.0% → 2.3% | 冻结主干至关重要 |
| **No TE (Task Embedding)** | 57.9% | -0.7% | Task embedding 对 MoE 路由至关重要 |
| **Replay Only** | 57.2% | -11.0% | 单靠回放不足以维持稳定性 |
| **Expert Only** | 28.0% | -51.3% | 单靠扩展专家会导致严重遗忘 |
| **Ours (Full)** | **69.4%** | **+2.3%** | **必须结合结构扩展 + 回放 + 参数掩码才能成功** |

> ➤ 结果表明：**三种机制缺一不可**，协同作用才实现最佳性能。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Teacher-Student 框架显著提升 CRL 可行性**：
   - 将困难的 online RL 学习转为稳定的 offline 蒸馏任务，大幅降低优化难度。
2. **MoE + 增量扩展是维持长期可塑性的关键**：
   - 动态增加专家使模型能持续容纳新技能，避免容量饱和。
3. **混合防遗忘策略最有效**：
   - **Experience Replay (DPP)** + **Parameter Masking (Freeze Shared Layers + Two-Phase Training)** 显著抑制灾难性遗忘。
4. **Learned Task Embedding 捕获语义相似性**：
   - PCA 可视化显示相似任务（如按钮按压、抽屉开合）自动聚类，促进正向迁移。

### **方法的局限性**
1. **依赖高质量教师模型**：
   - 若某个教师未能学会任务（如 `disassemble-v3` 成功率为 0），则学生也无法掌握。
2. **仅在 Meta-World 上验证**：
   - 尚未在 Atari 或其他视觉输入环境中测试，泛化性有待验证。
3. **离线蒸馏不适用于非平稳环境**：
   - 当前框架假设教师策略固定，若环境动态变化则需重新设计。

### **未来工作方向**
- 扩展至 **vision-based tasks**（如 DeepMind Control Suite）。
- 引入 **online distillation** 机制，允许教师持续改进。
- 探索 **automatic expert pruning** 以控制模型增长。
- 应用于 **embodied AI** 和 **robotics pre-training** 场景，构建通用智能体。

---

> 💡 **总结一句话**：  
> 本文提出的 **Continual Policy Distillation** 框架通过“**分布式教师训练 + MoE 学生蒸馏 + 混合防遗忘机制**”，在 Meta-World 上实现了高效、稳定、可扩展的持续强化学习，为构建通用智能体提供了可行路径。

</details>

---

### 9. [Pushing the Boundaries of Natural Reasoning: Interleaved Bonus from Formal-Logic Verification](https://arxiv.org/abs/2601.22642)

**Authors**: Chuxue Cao, Jinluan Yang, Haoran Li, Kunhao Pan, Zijian Zhao, Zhengyu Chen, Yuchen Tian, Lijun Wu, Conghui He, Sirui Han, Yike Guo  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.22642v1  

#### Abstract
Large Language Models (LLMs) show remarkable capabilities, yet their stochastic next-token prediction creates logical inconsistencies and reward hacking that formal symbolic systems avoid. To bridge this gap, we introduce a formal logic verification-guided framework that dynamically interleaves form...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Pushing the Boundaries of Natural Reasoning: Interleaved Bonus from Formal-Logic Verification

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在数学和逻辑推理任务中表现出色，但其基于概率的逐词生成机制容易导致**逻辑不一致**（logical inconsistency）和**奖励欺骗**（reward hacking）。具体表现为：
- 推理链中存在中间步骤错误，即使最终答案正确；
- 模型通过表面模式“猜对”答案，而非建立严谨的逻辑基础；
- 现有神经符号系统（neuro-symbolic）方法多为被动的事后验证（post-hoc validation），无法在生成过程中主动纠正错误。

### 提出的新方法
作者提出了一种**形式化逻辑验证引导的框架**（Formal Logic Verification-guided Framework），其核心创新在于**动态交错**（dynamically interleaves）自然语言推理与形式化符号验证过程。

#### 主要创新点：
- ✅ **动态交错验证机制**：不同于传统的静态过滤或事后验证，该框架在推理生成的每一步都引入形式化验证器（如 z3、sympy）进行实时反馈，实现**实时错误检测与修正**。
- ✅ **两阶段训练范式**：
  1. **监督微调（SFT）**：通过一个分层的数据合成管道，生成包含自然语言推理、形式化证明和执行输出的三元组数据，并通过执行验证确保三者一致性。
  2. **策略优化（RL）**：采用 **Group Relative Policy Optimization (GRPO)**，结合复合奖励函数，对结构完整性、逻辑正确性和计算效率进行联合优化。
- ✅ **可扩展的形式化验证**：将形式化验证从特定领域（如数学定理证明）推广到通用逻辑推理领域，适用于数学、逻辑和一般推理任务。

### 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|--------|
| 验证时机 | 事后验证（Post-hoc） | **实时交错验证**（Interleaved） |
| 错误处理 | 被动过滤 | **主动纠正** |
| 验证粒度 | 最终答案 | **中间步骤** |
| 应用范围 | 多限于数学领域 | **跨领域通用**（逻辑、数学、一般推理） |
| 数据质量 | 依赖人工标注或弱监督 | **执行验证保证高对齐性** |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **逻辑推理**：
  - **KOR-Bench**：涵盖多领域的知识正交逻辑推理任务。
  - **BIG-Bench Hard (BBH)**：包含需要多步推理的挑战性任务。
- **数学推理**：
  - **MATH-500**：竞赛级数学问题。
  - **AIME 2024**：奥数级别数学推理挑战。
- **一般推理**：
  - **GPQA-Diamond**：研究生级别的物理、化学、生物等学科推理。
  - **TheoremQA**：测试模型在数学、物理、EE&CS 和金融等领域应用定理的能力。

### 实验设置和评估指标
- **主干模型**：Qwen2.5-7B 和 Qwen2.5-14B。
- **训练流程**：
  1. **SFT阶段**：使用约17k样本进行监督微调。
  2. **RL阶段**：在难度较高的样本（teacher model pass rate < 50%）上进行强化学习。
- **评估方式**：
  - 所有任务使用 **OpenCompass** 进行评估。
  - 除 AIME24 使用 `avg@16` 抽样外，其余均使用贪心解码。
- **评估指标**：准确率（Accuracy）。

### 基线方法对比
| 类别 | 基线模型 |
|------|--------|
| 基础模型 | Qwen2.5-7B/14B |
| 强化学习模型 | SimpleRL-Zoo, General-Reasoner, RLPR |
| 逻辑增强模型 | SynLogic-7B |
| 工具集成推理 | ZeroTIR, SimpleTIR |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）
| 模型 | 逻辑平均 | 数学平均 | 一般平均 | 总体平均 |
|------|---------|---------|---------|---------|
| **Qwen2.5-7B-Base** | 27.6 | 33.4 | 29.7 | 30.2 |
| **Qwen2.5-7B-Instruct** | 53.6 | 42.2 | 39.9 | 45.2 |
| **FLV-SFT (Ours, 7B)** | 58.3 | 48.6 | 42.7 | **49.8** |
| **FLV-RL (Ours, 7B)** | 60.5 | 49.7 | 45.5 | **51.9** |
| **FLV-SFT (Ours, 14B)** | 65.8 | 50.9 | 48.5 | **55.7** |
| **FLV-RL (Ours, 14B)** | 67.5 | 55.8 | 52.5 | **58.6** |

> 📌 **总体提升**：相比当前最优（SOTA）基线，**7B 模型平均提升 10.4%**，**14B 模型平均提升 14.2%**。

### 与基线方法的对比结果
- 在 **AIME 2024** 上，FLV-RL-14B 达到 **30.2%**，远超 General-Reasoner 的 17.5% 和 Base 模型的 3.6%。
- 在 **TheoremQA** 上，14B 模型达到 **63.5%**，领先第二名超过 8 个百分点。
- 在 **KOR-Bench** 上，14B 模型达到 57.0，相比 General-Reasoner 的 41.3 提升 **15.7%**。

### 消融实验结果（Table 2）
| 模型 | KOR-Bench | BBH | MATH-500 | AIME24 | 总体平均 |
|------|----------|-----|---------|-------|---------|
| **Natural-SFT** | 30.4 | 55.9 | 56.6 | 8.5 | 36.5 |
| **FLV-SFT (Ours)** | 48.0 | 68.5 | 77.2 | 20.0 | **49.8** |
| **Natural-RL** | 35.7 | 55.4 | 54.4 | 4.8 | 37.0 |
| **FLV-RL (Ours)** | 51.0 | 70.0 | 78.6 | 20.8 | **51.9** |

> 🔍 **关键发现**：
> - 形式化验证（FLV）带来 **+13.3%** 的平均提升（49.8 vs 36.5）。
> - 在逻辑密集任务（如 KOR-Bench）上提升尤为显著（+17.6）。
> - 自然语言 RL 几乎无提升（37.0 vs 36.5），而 FLV-RL 显著优于 FLV-SFT（+2.1），说明形式化验证提供了更稳定可靠的奖励信号。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **形式化验证能显著提升 LLM 推理能力**：通过实时反馈，模型能够避免 reward hacking，建立真正稳健的逻辑链条。
2. ✅ **从“计算”到“推理”的范式转变**：分析表明，FLV-RL 更多地调用 **Symbolic & Logic** 类工具（如 z3、sympy），而减少对 **Algorithmic & Search**（如 itertools）的依赖，表明模型从“暴力搜索”转向“抽象推理”。
3. ✅ **小数据大效果**：尽管仅使用约 17k 训练样本，性能仍大幅超越依赖大规模数据的基线模型，验证了方法的**数据高效性**（data efficiency）。
4. ✅ **灵活的验证策略更优**：强制每步验证会抑制直接计算能力，降低数学性能；而**灵活验证策略**（decoupling calculation from validation）在保持逻辑严谨的同时提升了数学表现。

### 方法的局限性
1. ⚠️ **计算开销增加**：集成实时形式化验证使 RL 训练时间增加约 **2倍**。
2. ⚠️ **形式化转换挑战**：将模糊或常识性强的自然语言描述自动转化为形式化表示仍存在困难，可能导致错误的验证反馈。
3. ⚠️ **GPQA 数据集可靠性问题**：作者引用研究指出 GPQA 存在事实性错误和参数缺失，可能影响评估公正性。

### 未来工作方向
- 开发更鲁棒的 **auto-formalization** 技术，提升自然语言到形式化逻辑的转换成功率。
- 探索更高效的验证调度机制，平衡推理成本与性能增益。
- 将该框架扩展至开放域、非结构化推理任务，进一步验证其通用性。

> 💡 **总结**：本文提出的 **FLV 框架**成功地将形式化逻辑的严谨性与 LLM 的语言流畅性相结合，通过**动态交错验证**和**两阶段训练**，显著提升了模型在复杂推理任务中的准确性和可靠性，为构建可信 AI 推理系统提供了重要路径。

</details>

---

### 10. [CVeDRL: An Efficient Code Verifier via Difficulty-aware Reinforcement Learning](https://arxiv.org/abs/2601.22803)

**Authors**: Ji Shi, Peiming Guo, Meishan Zhang, Miao Zhang, Xuebo Liu, Min Zhang, Weili Guan  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22803v1  

#### Abstract
Code verifiers play a critical role in post-verification for LLM-based code generation, yet existing supervised fine-tuning methods suffer from data scarcity, high failure rates, and poor inference efficiency. While reinforcement learning (RL) offers a promising alternative by optimizing models thro...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# CVeDRL: An Efficient Code Verifier via Difficulty-aware Reinforcement Learning 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于大语言模型（LLM）的代码生成系统依赖**代码验证器（code verifier）**在推理时对多个候选代码进行筛选，以选出最优解。然而，现有的验证器训练方法主要依赖**监督微调（SFT）**，面临以下三大挑战：
- **数据稀缺**：高质量的单元测试标注数据难以获取；
- **高错误率**：SFT模型生成的单元测试常存在语法错误或逻辑失败；
- **推理效率低**：需采样大量测试用例才能保证可靠性，导致严重效率瓶颈。

尽管强化学习（RL）为无监督训练提供了可能，但**仅使用功能正确性奖励（functionality rewards）的朴素RL方法**无法有效覆盖复杂分支和困难样本，导致验证效果不佳。

---

### 提出的新方法与创新思路
本文提出 **CVeDRL**（Code Verifier via Difficulty-aware Reinforcement Learning），一种基于**难度感知强化学习**的高效代码验证器训练框架，其核心创新如下：

#### （1）理论分析指导多维奖励设计
首次从理论上分析了**单元测试多数投票机制**中，**测试通过率（pass rate）、分支覆盖率（branch coverage）与验证器可靠性之间的关系**，推导出提升验证信心的关键因素，从而为RL中的多维度奖励设计提供理论依据。

#### （2）构建语法-功能复合奖励（Syntax-Functionality Composite Reward）
引入两种互补奖励信号：
- **Syntax Reward**：基于AST规则判断输出是否符合 `unittest` 格式要求；
- **Functionality Reward**：根据执行结果分类为 error、failure 或 pass，并结合**分支覆盖率**作为正向激励。

#### （3）提出“分支-样本”双难度感知机制
- **Branch-Difficulty-aware RL**：采用**指数型奖励塑形（exponential reward shaping）**，放大对稀有分支（如边界条件）的奖励，鼓励探索难覆盖路径。
- **Sample-Difficulty-aware RL**：引入静态代码分析指标作为先验难度估计：
  - **Halstead Complexity**（认知负荷）
  - **Maintainability Index (MI)**（可维护性倒置）
  结合二者几何平均得到综合难度 $D$，用于调节奖励强度。

#### （4）使用 GRPO 进行高效训练
采用 **Group Relative Policy Optimization (GRPO)**，无需价值网络，直接组内比较优势，降低训练开销。

---

### 相比现有方法的优势
| 维度 | CVeDRL 优势 |
|------|-------------|
| **性能** | 在仅 0.6B 参数规模下超越 GPT-3.5 和 CodeRM-8B |
| **效率** | 推理速度比 SFT 模型快 **20倍以上**（token throughput） |
| **质量** | 生成更少冗余断言，更高通过率与分支覆盖率 |
| **泛化性** | 显著提升开源与闭源策略模型的验证效果 |

---

## 2. 核心实验方法和设置

### 使用的数据集
共使用四个基准数据集进行综合评估：
| 数据集 | 描述 |
|--------|------|
| **HuManEval+** | HumanEval 的增强版本，含函数级编程任务 |
| **MBPP+** | MBPP 扩展版，侧重实际场景下的小规模编程问题 |
| **LiveCodeBench** | 包含开发者真实提交代码的真实世界评测集（选取2024年1–9月发布的168个函数式问题） |
| **LeetCode** | 算法题子集，过滤掉系统设计类题目后保留542道纯函数题 |

训练数据来自 **CoDERM** 数据集（>50K 合成问题），确保解决方案正确且适配。

---

### 实验设置与评估指标

#### 验证-编码器性能（Validation-Coder Performance）
- **目标**：衡量不同 reward model 对 policy model 输出的筛选能力。
- **流程**：每个问题生成 N 个候选代码 → 由验证器生成 M 个测试用例 → 执行并统计通过数 → 多数投票选择最优。
- **主指标**：Pass@N（最终选中最优解的比例）

#### 单元测试质量评估（Test Quality Evaluation）
直接运行生成的测试套件，评估其内在质量：
| 指标 | 含义 |
|------|------|
| **Error Rate (ER%)** ↓ | 因语法错误等导致无法执行的概率 |
| **Failure Rate (FR%)** ↓ | 测试成功运行但预期输出错误的概率 |
| **Pass Rate (PR%)** ↑ | 正确通过所有断言的比例 |
| **Branch Coverage (BC%)** ↑ | 覆盖的代码分支/行比例 |
| **Assertion Number (AN)** ↓ | 平均每个测试类中的断言数量（越少越好，表示简洁） |

---

### 基线方法对比
#### 策略模型（Policy Models）
- GPT-3.5, GPT-4o-mini, LLaMA3-8B, LLaMA3-70B

#### 验证器基线（Reward Models）
- **Vanilla**：不重排序，取 top-1
- **MBR-E**：基于执行风险最小化的贝叶斯决策
- **CodeT**：双执行一致性评分
- **CodeRM-8B**：SFT训练的8B参数测试生成器
- **LLaMA3-70B**：直接用大模型作验证器

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1 & 2）

#### ✅ 在 MBPP+ 上的表现（Pass Rate）
| 方法 | GPT-3.5 | GPT-4o-mini |
|------|---------|-------------|
| Vanilla | 49.17% | 71.32% |
| CodeRM-8B | 66.63% | 75.18% |
| **CVeDRL-0.6B** | **66.79%** | **76.93%** |

👉 **相比 GPT-4o-mini + CodeRM 提升 1.75pp，是当前 SOTA**

#### ✅ 在 HuManEval+ 上的表现
| 方法 | GPT-3.5 | GPT-4o-mini |
|------|---------|-------------|
| Vanilla | 53.43% | 82.57% |
| CodeRM-8B | 78.13% | 86.49% |
| **CVeDRL-0.6B** | **78.96%** | **87.05%** |

👉 达到当前最优水平，小幅领先 CodeRM-8B

#### ✅ 单元测试质量（Table 2）
在 MBPP+ 上：
| 模型 | ER% ↓ | FR% ↓ | PR% ↑ | BC% ↑ | AN ↓ |
|------|-------|-------|--------|--------|-----|
| GPT-4o | 3.98 | 29.89 | 66.13 | 96.91 | 6.12 |
| CodeRM-8B | 2.44 | 52.86 | 44.70 | 97.11 | 7.88 |
| **CVeDRL-0.6B** | **0.53** | **15.79** | **83.68** | **97.37** | **3.13** |

✅ **通过率提升高达 17.55% vs GPT-4o-mini，同时减少超过一半的冗余断言**

---

### 消融实验结果（Ablation Study, Table 3）

| 消融配置 | MBPP+ PR% | BC% |
|----------|------------|------|
| Full CVeDRL (0.6B) | **83.68** | **97.37** |
| w/o Sample-Difficulty (SDA) | 71.42 | 96.75 |
| w/o Branch-Difficulty (BDA) | 79.96 | 92.14 |
| w/o Syntax Reward | 51.53 | 85.11 |

👉 **三个组件均有显著贡献，组合使用效果最佳**：
- **指数奖励塑形（BDA）** 显著提升覆盖率（+5.23pp）
- **静态难度感知（SDA）** 极大降低失败率，提高通过率（+13.72pp）
- **语法奖励** 是基础保障，缺失将导致崩溃式下降

---

## 4. 关键结论和发现

### 主要发现
1. **多维RL奖励优于单一功能性奖励**：单纯依赖功能反馈不足以引导高质量测试生成；加入语法约束、覆盖率、难度感知可显著提升验证可靠性。
2. **小模型也能胜任高效验证**：CVeDRL 仅用 **0.6B 参数**即达到甚至超越 GPT-3.5 和 CodeRM-8B 性能，证明**紧凑模型 + 强化学习 > 巨型模型 + SFT**。
3. **推理效率极大提升**：相比传统 SFT 方法（如 CodeRM），CVeDRL 实现 **>20x 的 token throughput 加速**（见 Table 4）。
4. **采样效率更高**：图2显示，CVeDRL 在仅需 **10个测试用例**即可达到性能饱和，远低于其他方法所需的100次采样，大幅缩短验证时间。

---

### 方法的局限性
1. **部分代码支持不足**：目前仅适用于完整函数级别的验证，无法处理代码补全等局部生成任务。
2. **缺乏正确性区分能力**：虽然能有效过滤错误代码，但尚不能精确判断两个“都通过”的实现哪个更优。
3. **语言与生态扩展有限**：当前工作聚焦于 Python 及其标准库，尚未扩展至多语言或多依赖环境。

---

### 未来工作方向
1. 将验证管道扩展至**部分代码（partial code）和库级别测试生成**；
2. 引入**代码变异（mutation testing）或形式化原则**来增强对“伪正确”代码的识别能力；
3. 支持更多编程语言及复杂依赖管理环境；
4. 探索将该框架应用于持续集成（CI）自动化测试生成场景。

---

> 🔗 **代码已开源**：[https://github.com/LIGHTCHASER1/CVeDRL.git](https://github.com/LIGHTCHASER1/CVeDRL.git)

</details>

---

### 11. [HetCCL: Accelerating LLM Training with Heterogeneous GPUs](https://arxiv.org/abs/2601.22585)

**Authors**: Heehoon Kim, Jaehwan Lee, Taejeoung Kim, Jongwon Park, Jinpyo Kim, Pyongwon Suh, Ryan H. Choi, Sangwoo Lee, Jaejin Lee  
**Category**: cs.DC  
**Published**: 2026-02-02  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22585v1  

#### Abstract
The rapid growth of large language models is driving organizations to expand their GPU clusters, often with GPUs from multiple vendors. However, current deep learning frameworks lack support for collective communication across heterogeneous GPUs, leading to inefficiency and higher costs. We present ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*HetCCL: Accelerating LLM Training with Heterogeneous GPUs*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前主流的深度学习框架（如 PyTorch）依赖于厂商专用的集体通信库（CCL），例如 NVIDIA 的 **NCCL** 和 AMD 的 **RCCL**。这些库互不兼容，导致在混合使用不同厂商 GPU（如 NVIDIA 与 AMD）的异构集群中无法进行高效的分布式训练。

尽管组织常通过逐步采购构建异构 GPU 集群以降低成本，但现有技术无法实现跨厂商 GPU 的透明协同，造成硬件资源浪费、训练效率低下。

### **提出了什么新方法或新思路**

本文提出 **HetCCL** —— 首个支持跨厂商 GPU 的集体通信库，能够在不修改任何应用代码的前提下，统一调度 NVIDIA 和 AMD GPU 进行高效的大模型训练。

其核心创新机制包括：

- **Runtime API 抽象层（TACC）**  
  设计了一个统一的运行时接口抽象层 **TACC**（Transparent Accelerator Communication Channel），将 CUDA 和 HIP 的运行时 API 统一为平台无关的调用，屏蔽底层差异。

- **平台特定设备代码编译策略**  
  将 GPU 内核代码解耦为独立的共享库（`.so`），分别使用 `nvcc` 和 `hipcc` 编译，并在运行时动态加载对应平台的内核，实现多厂商二进制共存。

- **基于 RDMA 的跨厂商点对点通信**  
  利用标准 **RDMA** 协议（如 InfiniBand）直接访问对方 GPU 显存，无需修改驱动或固件，实现了零拷贝、高性能的数据传输。

- **GPU 感知的微批次负载均衡（GPU-aware micro-batch sizing）**  
  根据各 GPU 的实测吞吐量（tokens/s）动态分配微批次大小，缓解因性能差异导致的 **straggler effect**。

### **相比现有方法的优势**

| 方面 | 现有方案（NCCL/RCCL/MPI） | HetCCL |
|------|----------------------------|--------|
| 跨厂商支持 | ❌ 不支持 | ✅ 支持 NVIDIA + AMD |
| 性能 | 在同构环境中最优 | 接近原生性能（~97% 效率） |
| 兼容性 | 需要重新编译或修改代码 | ✅ 无需修改应用，支持 `LD_PRELOAD` 注入 |
| 扩展性 | 受限于单一厂商生态 | 可扩展至其他加速器（NEW accelerator） |
| 通信机制 | 多数需主机内存中转 | ✅ 原生支持 GPUDirect RDMA |

---

## 2. 核心实验方法和设置

### **使用的模型与任务**

- **模型**：GPT-125M、GPT-355M、LLaMA-1B、LLaMA-3B
- **任务**：大规模语言模型（LLM）的端到端训练
- **精度**：FP16（性能测试）、BF16（收敛性分析）
- **并行策略**：DeepSpeed 的 **ZeRO-1** 和 **ZeRO-3**（即 FSDP）

### **实验设置**

- **硬件配置**：
  - 4 节点集群，每节点 4 块 GPU
    - 2 节点 × 4 × NVIDIA Tesla V100 (PCIe Gen3)
    - 2 节点 × 4 × AMD Radeon Pro W7800 (PCIe Gen4)
  - 网络互联：Mellanox ConnectX-6 InfiniBand HDR
  - 软件栈：PyTorch + DeepSpeed + ROCm/CUDA

- **评估指标**：
  - 通信带宽（GB/s）
  - 训练吞吐量（tokens/s）
  - 加速比（speedup）
  - 模型收敛曲线与最终 loss
  - 效率（Efficiency） = $ \frac{\text{HetCCL (NVIDIA+AMD)}}{\text{NCCL (NVIDIA-only)} + \text{RCCL (AMD-only)}} $

- **基线方法对比**：
  - **NCCL**：NVIDIA 同构环境基准
  - **RCCL**：AMD 同构环境基准
  - **GPU-aware MPI**：作为跨厂商通信的替代方案进行比较

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **通信性能**

- **点对点通信（RDMA）**：
  - HetCCL 在跨厂商通信中达到接近 NCCL 的带宽水平（见 Figure 8）
  - 对于 1GB 数据，HetCCL(HET) 达到约 **90%** 的 NCCL 峰值带宽

- **集体通信操作（All-Reduce / All-Gather / Reduce-Scatter）**：
  - 在 16 GPU 异构环境下（8N + 8A），HetCCL 实现稳定可扩展的通信性能
  - 性能受限于较慢的一方（通常为 RCCL），但仍保持线性扩展趋势（见 Figure 7）

#### ✅ **端到端训练性能**

- **最大加速比**：
  - 相比仅用 AMD GPU（RCCL）：**最高达 2.97× speedup**
  - 相比仅用 NVIDIA GPU（NCCL）：**最高达 1.48× speedup**

- **系统效率**：
  - 平均效率约为 **90%**
  - 最高可达 **97%**，表明 HetCCL 几乎无额外开销地聚合了异构算力

- **可扩展性**：
  - 从 8 GPU（4A+4N）扩展到 16 GPU（8A+8N）时，吞吐量几乎翻倍，显示良好的近线性扩展能力

#### ✅ **消融实验结果**

- **RDMA 的影响（Appendix F.1）**：
  - 关闭 RDMA 后，通信路径退化为 GPU → CPU → NIC → CPU → GPU
  - 小消息延迟显著上升，大消息带宽下降超过 50%
  - 证明 **RDMA 是实现高性能的关键**

- **负载均衡的影响（Appendix F.2）**：
  - 使用 GPU 感知的微批次分配后，端到端训练速度提升 **1.08× ~ 1.22×**
  - 特别是在大模型上效果更明显（如 LLaMA-3B 提升 1.08×）
  - 分析指出：通信开销限制了进一步提速的空间

- **与 GPU-aware MPI 对比（Appendix E.3）**：
  - 在小消息场景下，MPI 延迟更低
  - 但在大消息和集体操作中，HetCCL 显著优于 MPI（尤其 All-Reduce）
  - 原因：MPI 通常在 CPU 上执行归约操作，而 HetCCL 完全在 GPU 上完成，避免了主机内存拷贝

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **跨厂商 GPU 通信是可行且高效的**  
   无需修改驱动或硬件，仅通过标准 RDMA 协议即可实现异构 GPU 间的高性能通信。

2. ✅ **HetCCL 可无缝集成现有训练栈**  
   通过 `LD_PRELOAD` 替换 NCCL/RCCL 符号，无需更改任何 PyTorch 或 DeepSpeed 代码。

3. ✅ **接近原生性能 + 高效资源利用**  
   在异构环境中，HetCCL 实现高达 **97% 的通信效率**，有效聚合了原本孤立的计算资源。

4. ✅ **显著降低训练成本**  
   通过整合旧有和新型 GPU，减少 GPU-hour 消耗，直接降低训练总成本。

5. ✅ **不影响模型准确性**  
   LLaMA-1B 的训练损失曲线与 NCCL/RCCL 完全一致，最终 loss 差异小于 $7\times10^{-3}$，处于 BF16 数值误差范围内。

---

### **局限性（Limitations）**

1. **节点级异构，非卡级混合**  
   当前设计假设每个节点只有一种厂商 GPU（inter-node heterogeneity），尚未支持单节点内混插（intra-node mixed-vendor）。

2. **静态负载分配**  
   微批次比例基于初始 profiling 确定，未考虑运行时性能波动（如热节流、资源竞争）。未来可引入在线重校准机制。

3. **实验规模有限**  
   实验最多使用 16 块 GPU，模型参数上限为 3B。更大规模集群的表现有待验证。

4. **依赖 RDMA 环境**  
   若无 InfiniBand/RoCE 支持，则退化为主机中继模式，性能大幅下降。

---

### **未来工作方向**

1. **支持 intra-node 混合部署**  
   扩展至单节点内同时运行 NVIDIA 与 AMD GPU 的场景。

2. **动态负载再平衡（Dynamic Load Balancing）**  
   引入 per-step profiling，实时调整 micro-batch size 以应对运行时变化。

3. **扩展至更多厂商**  
   支持 Intel GPU、华为 Ascend、Apple Silicon 等其他架构，打造通用异构通信平台。

4. **与 MoE 架构结合优化**  
   探索在 Mixture-of-Experts 中按专家能力分配不同厂商 GPU 的调度策略。

5. **集成至主流框架默认分发版**  
   推动 PyTorch/TensorFlow 原生支持 HetCCL 类机制，成为异构训练的标准组件。

---

> 📌 **总结一句话**：  
> **HetCCL 成功打破了 GPU 厂商之间的通信壁垒，在不改动任何训练代码的情况下，实现了跨 NVIDIA 与 AMD GPU 的高性能、高效率 LLM 训练，为构建低成本、可持续演进的 AI 基础设施提供了关键技术路径。**

</details>

---

### 12. [BayesFlow: A Probability Inference Framework for Meta-Agent Assisted Workflow Generation](https://arxiv.org/abs/2601.22305)

**Authors**: Bo Yuan, Yun Zhou, Zhichao Xu, Kiran Ramnath, Aosong Feng, Balasubramaniam Srinivasan  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22305v1  

#### Abstract
Automatic workflow generation is the process of automatically synthesizing sequences of LLM calls, tool invocations, and post-processing steps for complex end-to-end tasks. Most prior methods cast this task as an optimization problem with limited theoretical grounding. We propose to cast workflow ge...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：BayesFlow: A Probability Inference Framework for Meta-Agent Assisted Workflow Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **agentic workflow** 设计依赖人工设计和反复试错，过程耗时且需要大量领域专业知识，限制了其在新任务上的可扩展性和适应性。现有自动化方法多将 workflow 生成视为**优化问题**（如基于搜索的策略），缺乏理论基础，通常只产出单一高分方案，多样性不足。

### 提出的新方法与新思路
本文提出 **Bayesian Workflow Generation (BWG)**，将自动 workflow 生成重新定义为一个**贝叶斯后验采样问题**，而非传统优化问题。具体实现为 **BayesFlow**，一种无需训练的推理框架，具有以下核心机制：

- **并行前瞻 rollout (Parallel look-ahead rollouts)**：  
  在每一步生成多个可能的后续路径（rollout），通过其潜在奖励（如验证准确率）对当前前缀进行重要性加权，从而在无中间奖励的情况下引导生成过程。该机制避免了对昂贵的 reward model 或闭源强模型的依赖。

- **顺序循环精炼 (Sequential in-loop refinement)**：  
  在每轮迭代中引入一个全局精炼模块（refiner），利用已有完整 workflow 池进行改进（如通过 MCTS），修正早期错误，提升整体质量。

- **理论保证**：  
  证明在无 refiner 的情况下，加权经验分布**渐近收敛于目标后验分布**（Theorem 1）。即使引入 refiner，也提供了 TV 距离界限，说明其在可控范围内扩展探索（Theorem 2）。

### 相比现有方法的优势
- **理论更扎实**：提供收敛性证明，区别于黑箱搜索方法。
- **多样性更强**：通过采样自然产生多样化高质量 workflow。
- **效率更高**：减少冗余评估，token 使用量显著低于 AFlow。
- **通用性强**：不依赖预定义 agent 模块（如 ENSEMBLE, REVISE），仅暴露轻量级接口（`chat_completion`, `exec_code`），由 meta optimizer LLM 自主设计逻辑。

---

## 2. 核心实验方法和设置

### 使用的数据集
在六个基准数据集上进行评估，涵盖三大类任务：
- **数学推理 (Math Reasoning)**：MATH, GSM8K
- **问答 (Question Answering)**：HotpotQA, DROP
- **专家知识 (Expert Knowledge)**：GPQA, MMLU-Pro

此外，在代码生成任务 **MBPP** 上进行了额外测试。

### 实验设置和评估指标
- **Meta Optimizer LLM**：Claude 3.5-Sonnet
- **Executor LLM**：Claude 3.7-Sonnet 或 Qwen2.5-7B-Instruct
- **超参数**：`K=1`（前瞻 rollout 数）、`N=10`（每轮部分 workflow 数）、`M=10`（精炼 workflow 数）、`T=5`（最大步数）
- **评估方式**：每个选出的 workflow 在测试集上评估 5 次，报告均值与标准差。

#### 评估指标
| 数据集        | 指标         |
|---------------|--------------|
| GSM8K, MATH   | Solve Rate   |
| HotpotQA, DROP| F1 Score     |
| GPQA, MMLU-Pro| Accuracy     |

### 基线方法对比
- **Prompting 基线**：
  - Zero-shot (IO)
  - Zero-shot Chain-of-Thought (CoT)
  - CoT with Self-Consistency (CoT-SC)
- **自动 workflow 生成基线**：
  - ADAS (线性启发式搜索)
  - AFlow (Monte Carlo Tree Search)
  - MaAS (多智能体架构搜索)

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

#### 使用 **Claude 3.7-Sonnet** 作为执行器：
| 方法          | 平均准确率 | 最佳单项表现 |
|---------------|------------|-------------|
| AFlow (SOTA)  | 76.2%      | —           |
| **BayesFlow (Ours)** | **80.8%**  | **MATH: 69.4%**, **GPQA: 69.2%** |

- 在 **MATH** 上超越 AFlow **9.3个百分点**
- 在 **GPQA** 上超越 AFlow **3.9个百分点**
- 所有数据集上均取得**最高平均准确率**

#### 使用 **Qwen 2.5-7B-Instruct** 作为执行器：
| 方法          | 平均准确率 |
|---------------|------------|
| AFlow         | 59.9%      |
| **BayesFlow (Ours)** | **63.4%**  |

- 平均提升 **3.5个百分点**，表明方法对小模型同样有效。

#### 在 MBPP 上的表现：
- **BayesFlow**: **Pass@1 = 85.0%**
- **AFlow**: 75.5%
- 显著优于现有方法。

---

### 与基线方法的对比结果
- 相比 **SOTA workflow 方法 (AFlow)**，BayesFlow 平均提升 **4.6–6.5个百分点**。
- 相比 **zero-shot prompting**，提升高达 **65个百分点**。
- 在 **HotpotQA** 上，AFlow 方差大（偶尔失败），而 BayesFlow 更稳定。

---

### 消融实验结果
#### （1）Token 效率对比（Table 2）
BayesFlow 在多数场景下以**更低的 token 消耗**达到**更高的准确率**，尤其在输入 token 上优势明显（如 DROP 上节省约 87M tokens）。

#### （2）不同 N 和 M 的鲁棒性（Table 3）
固定总预算 `N + M = 20`，调整 `N ∈ {5,10,15}`：
- 性能在不同配置下保持稳定，表明方法对探索-利用权衡不敏感。

#### （3）推理时缩放效果（Table 4）
比较 Top-L workflows 的聚合性能（Best@L, Mean@L, Majority@L）：
- BayesFlow 在 **Best@L** 上优势显著（如 L=8 时 86 vs 81），说明其生成的 workflow **更多样且高质量**，至少有一个能解出问题的概率更高。

---

## 4. 关键结论和发现

### 主要发现
- 将 workflow 生成建模为 **Bayesian posterior sampling** 是一个**原则性更强**的替代方案，优于纯优化视角。
- **并行前瞻 rollout** 能有效估计前缀价值，指导生成，且具备理论收敛保证。
- **循环精炼机制** 虽牺牲严格收敛性，但带来显著实证收益，提升多样性和最终性能。
- BayesFlow 在多种模型和任务上均**一致优于现有 SOTA 方法**，且更高效、更稳定。

### 方法的局限性
- **推理成本较高**：并行 rollout 增加了推理时计算开销。
- **短期记忆**：大部分 rollout 被丢弃，未长期复用高价值轨迹。
- **评估范围有限**：目前仅在 7 个基准和两类执行器上验证，尚未覆盖多模态或更复杂 agent 场景。

### 未来工作方向
- 引入**动态控制器**，根据在线信号自适应调整超参数以平衡成本与精度。
- 设计更强的**记忆机制**，保留并优先重用高价值 workflow 轨迹。
- 将 BWG 框架扩展至**多模态任务**和**更复杂的 agent 协作系统**，进一步验证其通用性与预算-精度权衡能力。

</details>

---

### 13. [Neural-Inspired Posterior Approximation (NIPA)](https://arxiv.org/abs/2601.22539)

**Authors**: Babak Shahbaba, Zahra Moslemi  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.22539v1  

#### Abstract
Humans learn efficiently from their environment by engaging multiple interacting neural systems that support distinct yet complementary forms of control, including model-based (goal-directed) planning, model-free (habitual) responding, and episodic memory-based learning. Model-based mechanisms compu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Neural-Inspired Posterior Approximation (NIPA)

## 1. 论文的主要贡献和创新点

### 解决的问题
贝叶斯推断（Bayesian inference）在复杂高维模型（如 **Bayesian Deep Learning**）中面临严重的计算瓶颈。传统的 **MCMC** 方法（如 HMC）虽然无偏，但计算代价高昂；而 **Variational Bayes (VB)** 虽然高效，却常引入偏差并低估不确定性。如何在保证后验近似质量的同时提升采样效率，是当前大规模贝叶斯学习的核心挑战。

### 提出的新方法与思路
本文提出了一种受神经科学启发的通用框架——**Neural-Inspired Posterior Approximation (NIPA)**，其核心思想是模拟人类大脑中三种互补的学习与决策机制：

- **Model-Based (MB) 模块**：基于目标分布进行精确但缓慢的探索（类比 HMC），确保采样的准确性和灵活性。
- **Model-Free (MF) 模块**：利用历史样本训练一个 **surrogate function**（如 DNN），实现快速、反射式的采样，避免重复计算昂贵的目标函数。
- **Episodic Control (EC) 模块**：通过检索最近邻的历史样本（“记忆”），直接复用其 log-posterior 值，实现“一键式”快速接受/拒绝判断。

NIPA 动态地在三个模块之间切换，依据当前提议状态与已有样本池的距离（标准化距离 $d^*$）决定使用哪个模块：
- 若距离远（$d^* > t_2$）→ 启用 MB 进行稳健探索；
- 中等距离（$t_1 < d^* \leq t_2$）→ 启用 MF 利用学习到的模式；
- 接近已有样本（$d^* \leq t_1$）→ 启用 EC 快速决策。

### 相比现有方法的优势
- **统一视角**：将 MCMC、VB、CES 等方法视为 NIPA 的特例（如仅用 MB 即为标准 MCMC，仅用 MF 类似 CES），提供了一个更广泛的理论框架。
- **高效性**：显著减少对昂贵目标函数（如完整数据集上的 log-posterior）的调用次数，尤其在已探索区域附近通过 EC 和 MF 避免重复计算。
- **准确性**：保留了 MB 模块的精确性，防止因过度依赖近似导致的偏差累积。
- **可扩展性**：适用于高维参数空间（如 BNNs），克服传统 MCMC 在大数据下的可扩展性问题。

---

## 2. 核心实验方法和设置

### 数据集
- **回归任务**：
  - **合成数据集**：基于两层 ReLU 网络生成，含 5,000 样本、100 特征、约 3.5k 参数。
  - **Year Prediction MSD**：真实音乐数据集，预测歌曲发行年份，含 515,345 样本、90 特征、约 310k 参数。
- **分类任务**：
  - **合成二分类数据集**：两层 ReLU 网络生成，20,000 样本、512 特征、约 148k 参数。
  - **MNIST Odd vs. Even**：区分奇偶数字，70,000 样本、784 特征、约 218k 参数。

### 实验设置与评估指标
- **采样器配置**：所有方法生成 2,000 个 posterior samples。
- **评估维度**：
  - **预测性能**：
    - 回归：**RMSE**（均方根误差）
    - 分类：**Accuracy**
  - **不确定性量化质量**：
    - 回归：**CP95**（95% 预测区间的覆盖率，越高越好）
    - 分类：**ECE**（Expected Calibration Error，越低越好）
  - **计算效率与采样质量**：
    - **Wall-clock time (s)**：总运行时间
    - **Effective Sample Size (ESS)**：最小、中位数、最大 ESS，衡量采样链混合速度
    - **minESS/s**：单位时间内的最小 ESS，综合效率指标
    - **Speedup**：相对于 BNN-HMC 基线的加速比

### 基线方法对比
- **MCMC 类**：BNN-HMC（基准）、BNN-SGHMC、BNN-pCN
- **变分类**：BNN-VI、BNN-LASSO
- **近似/集成类**：BNN-MCD（MC Dropout）、SWAG、BNN-RNS（Random Network Surrogate）
- **非贝叶斯参考**：DNN、DNN-Ensemble

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Tables 1 & 2）

| 方法 | 任务 | Speedup (vs HMC) | RMSE / Accuracy | CP95 / ECE | minESS/s |
|------|------|------------------|------------------|-------------|----------|
| **BNN-HMC (Baseline)** | Year Prediction | 1.00 | 8.96 | 85.89 | 0.0053 |
| **NIPA (MB/MF/EC)** | Year Prediction | **6.99** | **8.89** | **83.15** | **0.0370** |
| **BNN-HMC (Baseline)** | MNIST (Class.) | 1.00 | 98.07 | 0.51 | 0.0051 |
| **NIPA (MB/MF/EC)** | MNIST (Class.) | **8.65** | **98.01** | **0.39** | **0.028** |

> 注：NIPA 在保持预测性能（RMSE/Accuracy）与 HMC 相当甚至略优的同时，实现了 **7–9 倍的速度提升**，且不确定性校准更好（更低 ECE 或合理 CP95）。

### 与基线方法的对比结果
- NIPA 显著优于所有其他方法在 **minESS/s** 上的表现，说明其在单位时间内能产生更多有效样本。
- 相比于纯近似方法（如 VI、MCD、SWAG），NIPA 提供了更可靠的不确定性估计（更好的 CP95/ECE）。
- 相比于其他 MCMC 变体（如 SGHMC、pCN），NIPA 在相同任务上实现了更高的加速比。

### 消融实验结果
作者对 NIPA 的三个组件进行了消融研究（见 Table 1 & 2 中的子版本）：

| 变体 | 特点 | 性能表现 |
|------|------|---------|
| **NIPA (MB only)** | 仅使用 HMC，无加速机制 | 速度接近或慢于 HMC（Speedup ≈1），效率最低 |
| **NIPA (MF only)** | 仅使用 surrogate 函数 | 速度快（Speedup 达 7.99–10.60），但 ECE 最差（0.27），表明过度依赖近似可能损害校准 |
| **NIPA (EC only)** | 仅使用记忆检索 | 加速明显（Speedup≈2.5–15.7），但在高维下泛化能力有限 |
| **NIPA (MB/MF/EC)** | 完整三模块协同 | **最佳平衡**：高速度 + 高质量采样 + 良好不确定性量化 |

> 结论：**三者协同至关重要**。单独使用任一模块无法兼顾效率与可靠性，而组合策略实现了最优权衡。

---

## 4. 关键结论和发现

### 主要发现
1. **生物启发机制可有效指导算法设计**：将人脑中的 model-based、model-free 和 episodic 控制机制映射到贝叶斯采样中，能够构建出高效且鲁棒的 posterior approximation 框架。
2. **动态模块切换显著提升效率**：通过简单的距离门控机制，在未充分探索区域使用精确 MB，在已知区域使用快速 MF/EC，大幅减少了昂贵函数评估次数。
3. **NIPA 实现了效率与精度的良好平衡**：在多个高维 BNN 任务上，相比标准 HMC 实现了 **7–9 倍加速**，同时保持甚至提升了不确定性量化质量（更低 ECE、更高 CP95）。
4. **现有方法是 NIPA 的特例**：该框架具有高度通用性，涵盖了从纯 MCMC 到 surrogate-based 方法（如 CES）等多种范式。

### 方法的局限性
- **超参数敏感性**：阈值 $t_1, t_2$ 当前作为手动调参项，缺乏自适应机制，影响泛化能力。
- **surrogate 模型选择**：当前使用 autoencoder + DNN 构建 surrogate，虽有效但非唯一选择，模型容量与训练稳定性需进一步研究。
- **EC 检索机制简单**：仅使用最近邻检索，未考虑局部曲率或多点插值，可能在复杂流形上失效。
- **初始化依赖**：初始样本池由 SGHMC 生成，若初始探索不佳，可能影响后续 surrogate 学习效果。

### 未来工作方向
- **自适应门控机制**：借鉴认知科学中基于“不确定性竞争”的理论，让系统根据各模块的置信度自动分配控制权。
- **更灵活的 surrogate 架构**：尝试其他轻量级模型（如 ELM）或结构化先验（如 GP 与 DNN 结合）。
- **高级 EC 检索策略**：引入 k-NN 插值、局部拟合或注意力机制来增强记忆检索能力。
- **扩展至其他领域**：应用于 **Gaussian Processes**、**Bayesian Inverse Problems** 等同样受限于计算成本的贝叶斯建模场景。
- **理论分析**：建立 NIPA 的收敛性、偏差-方差权衡等理论保障。

---

> **总结**：NIPA 是一种新颖且实用的贝叶斯推理框架，它通过模仿大脑多系统协作机制，成功解决了高维模型中采样效率与准确性之间的矛盾，为大规模 **Bayesian Deep Learning** 的实际应用提供了强有力的新工具。

</details>

---

### 14. [SurrogateSHAP: Training-Free Contributor Attribution for Text-to-Image (T2I) Models](https://arxiv.org/abs/2601.22276)

**Authors**: Mingyu Lu, Soham Gadgil, Chris Lin, Chanwoo Kim, Su-In Lee  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.22276v1  

#### Abstract
As Text-to-Image (T2I) diffusion models are increasingly used in real-world creative workflows, a principled framework for valuing contributors who provide a collection of data is essential for fair compensation and sustainable data marketplaces. While the Shapley value offers a theoretically ground...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SurrogateSHAP: Training-Free Contributor Attribution for Text-to-Image (T2I) Models

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
在 Text-to-Image (T2I) 扩散模型日益广泛应用于创意工作流的背景下，如何公平地对**数据贡献者**（如艺术家、品牌、医院等）进行价值评估成为一个关键挑战。传统的 Shapley value 虽然理论上是公平的归因框架，但在实际应用中面临两大瓶颈：
- **计算成本高**：需要对每个数据子集进行重新训练（retraining），对于大规模扩散模型而言不可行。
- **估计方差大**：由于贡献者之间存在复杂的非线性交互效应，基于采样的 Shapley 估计需要极多样本才能收敛。

### 提出了什么新方法或新思路
本文提出了 **SurrogateSHAP**，一种无需重新训练的高效贡献者归因框架，其核心思想包含两个互补部分：

1. **Training-Free Proxy Game（无训练代理游戏）**  
   创新性地将原始的“重训练游戏”（retraining game）转化为一个**无需重新训练的代理版本**。该方法假设预训练模型的条件生成能力可以作为重训练后模型的代理，通过将子集 $S$ 对应的生成分布建模为多个条件分布的混合（mixture），从而仅需推理即可评估任意子集的效用 $v_g(S)$，避免了昂贵的 retraining。

2. **TreeSHAP-based Surrogate Model（基于树的代理模型）**  
   为了提升 Shapley 值估计的样本效率，引入梯度提升树（Gradient-Boosted Tree, GBT）来学习代理效用函数 $v_g(S)$ 的近似。随后利用 **TreeSHAP** 算法从该树模型中**解析地计算出精确的 Shapley 值**，显著减少了达到稳定估计所需的子集查询数量。

### 相比现有方法的优势
- **免重训练**：完全避免了 retraining 或 fine-tuning，大幅降低计算开销。
- **高样本效率**：相比 KernelSHAP 等采样方法，在相同预算下误差更低，收敛更快。
- **高保真度**：理论证明并实验证实，代理效用 $v_g(S)$ 与真实 retraining 效用 $v(S)$ 之间的误差受控于表示空间漂移（representation drift）。
- **可扩展性强**：适用于大规模 T2I 模型（如 Stable Diffusion, FLUX.1），支持多种下游效用指标（如美学评分、多样性、FID 等）。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个不同领域的数据集上进行，涵盖图像质量、美学和产品多样性任务：

| 数据集 | 描述 | 贡献者单位 |
|--------|------|-----------|
| **CIFAR-20** | CIFAR-100 的 20 类子集，32×32 图像 | 每个类别作为一个贡献者 |
| **ArtBench (Post-Impressionism)** | 后印象派艺术作品，258 位艺术家，256×256 图像 | 每位艺术家作为一个贡献者 |
| **Fashion-Product** | 时尚商品图像，100 个品牌，256×256 图像 | 每个品牌作为一个贡献者 |

此外，在临床案例研究中使用了 **ISIC** 皮肤病学数据集，以医院站点作为贡献者。

### 实验设置和评估指标

#### 主要评估方式：
- **Proxy Game Fidelity**：比较代理效用 $v_g(S)$ 与 retraining 效用 $v(S)$ 的一致性，使用 NMAE、NRMSE 和像素/表示级相似性（MAE, SSIM, RMS-LPIPS, RMS-CLIP）。
- **Linear Datamodel Score (LDS)**：衡量归因方法预测未见子集效用的能力，使用 Spearman 相关系数，越高越好。
- **Counterfactual Evaluation**：通过移除 Top-K% 最重要贡献者，观察模型性能下降程度（越大越好），验证归因准确性。
- **Case Study**：在 ISIC 数据上审计由医院引入的虚假相关性（性别与黑色素瘤诊断间的 spurious correlation）。

#### 训练细节：
- 在 CIFAR-20 上使用 DDPM-CFG 进行全参数微调。
- 在 ArtBench 和 Fashion-Product 上使用 LoRA 微调 Stable Diffusion 和 FLUX.1。
- SurrogateSHAP 使用 XGBoost 构建代理模型，并通过 5 折交叉验证优化超参数。

### 基线方法对比
涵盖了四类主流方法：
1. **Similarity-based**: Pixel/CLIP/GMValuator（基于生成样本与训练样本的相似性）
2. **LOO & Influence Functions**: LOO, Relative IF, Renormalized IF
3. **TRAK-based**: TRAK, Journey-TRAK, D-TRAK, DAS
4. **Shapley-based Retraining Proxies**: Sparsified Fine-Tuning (sFT) + KernelSHAP（来自 Lu et al., 2025）

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ LDS 性能（$\alpha=0.5$，越高越好）
| Method | CIFAR-20 (FID) | ArtBench (Aesthetic) | Fashion-Product (Diversity) |
|--------|----------------|-----------------------|------------------------------|
| **SurrogateSHAP (Ours)** | **51.21±6.78** | **44.14±5.27** | **32.41±5.31** |
| Sparsified-FT Shapley | -14.1±3.51 | 18.85±1.89 | 20.34±12.43 |
| LOO | 34.82±0.92 | -0.18±3.19 | -3.88±3.00 |
| DAS | 13.59±2.71 | -3.53±4.09 | 6.35±6.10 |

> SurrogateSHAP 在所有任务上均显著领先，尤其在复杂效用（如美学、多样性）上优势明显。

#### ✅ 计算效率（每子集查询时间，越低越好）
| Method | CIFAR-20 (min) | ArtBench (min) | Fashion-Product (min) |
|--------|----------------|----------------|------------------------|
| Retraining | 234.36 | 128.83 | 105.24 |
| sFT | 27.42 | 14.15 | 54.33 |
| **SurrogateSHAP (Ours)** | **10.72** | **10.92** | **35.23** |

> SurrogateSHAP 比 retraining 快 **12–23倍**，比 sFT 快 **1.4–2.5倍**。

#### ✅ Counterfactual 移除效果（Top 10% 移除后性能下降）
| Method | CIFAR-20 (FID ↑) | ArtBench (Aesthetic ↓) | Fashion-Product (Diversity ↓) |
|--------|------------------|-------------------------|-------------------------------|
| **SurrogateSHAP (Ours)** | **31.70±2.47%** | **-5.76±0.49%** | **-17.49±8.34%** |
| LOO | 17.48±2.68% | -4.99±1.13% | -7.18±6.23% |
| CLIP Score | 17.53±1.58% | -4.30±1.10% | -6.09±9.24% |

> SurrogateSHAP 识别出的贡献者对模型影响最大，移除后导致最严重的性能退化。

### 消融实验结果
- **合成游戏测试**（Synthetic Games）显示，在具有高阶交互的非线性效用函数中，SurrogateSHAP 的 $l_2$ 估计误差仅为 KernelSHAP 的 **1/8**（0.010 vs 0.085 @ M=512）。
- **Axiomatic Checks** 验证了 SurrogateSHAP 满足 Shapley 值的基本公理（如效率性、零玩家性），且随样本增加趋于收敛。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **代理游戏高度保真**：提出的 training-free proxy game 能够以极小误差逼近 retraining 游戏的效用，尤其是在表示空间漂移较小时。
2. **SurrogateSHAP 显著优于现有方法**：无论是在 LDS 预测能力、counterfactual 影响力还是计算效率方面，SurrogateSHAP 均全面超越包括 sFT-Shapley 在内的所有基线。
3. **适用于多场景归因**：不仅可用于评估生成质量，还能有效定位导致虚假相关性的数据源（如 ISIC 案例中 Hospital 7 引入的毛发伪影与性别关联）。
4. **TreeSHAP 提升估计效率**：使用 GBT + TreeSHAP 可解析计算 Shapley 值，极大提升了估计稳定性与速度。

### 方法的局限性
- **依赖条件标签结构**：要求数据有明确的 prompt metadata（如艺术家名、品牌名）以定义贡献者集合。
- **代理误差仍存在**：虽然误差可控，但在极端数据分布偏移下，代理效用可能偏离真实 retraining 结果。
- **树模型容量限制**：尽管 TreeSHAP 是精确的，但 GBT 对极高阶交互的捕捉能力有限（不过实验表明已足够）。

### 未来工作方向
- 将 SurrogateSHAP 扩展到无显式标签的聚类式贡献者划分。
- 探索更强大的代理模型（如 MLP 或 Transformer）以进一步提升复杂效用的拟合能力。
- 结合多目标归因框架，平衡生成质量与数据多样性/公平性，防止过度修剪少数群体数据。

---

> **总结**：SurrogateSHAP 为 T2I 模型中的数据贡献者提供了一个**高效、准确、免重训练**的归因框架，推动了生成模型的透明化、公平化与安全审计，具有重要的实践意义。

</details>

---

### 15. [Gaussian Process Bandit Optimization with Machine Learning Predictions and Application to Hypothesis Generation](https://arxiv.org/abs/2601.22315)

**Authors**: Xin Jennifer Chen, Yunjin Tong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.22315v1  

#### Abstract
Many real-world optimization problems involve an expensive ground-truth oracle (e.g., human evaluation, physical experiments) and a cheap, low-fidelity prediction oracle (e.g., machine learning models, simulations). Meanwhile, abundant offline data (e.g., past experiments and predictions) are often ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Gaussian Process Bandit Optimization with Machine Learning Predictions and Application to Hypothesis Generation*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**昂贵反馈下的连续空间优化问题**，尤其是在科学假设生成、工程设计等场景中，存在以下挑战：
- **Ground-truth oracle（真实反馈）** 非常昂贵（如人类评估、物理实验），难以频繁调用；
- 存在一个**廉价但有偏的预测模型**（如 LLM、仿真器）可提供快速估计；
- 同时拥有**丰富的离线数据**可用于预训练模型或构建先验。

传统方法如 Vanilla GP-UCB 仅依赖昂贵的真实反馈，样本效率低；而多保真度（multi-fidelity）方法通常需要对不同保真度进行成本权衡，并不适用于“预测近乎免费”的情况。

### 提出的新方法：PA-GP-UCB
作者提出 **Prediction-Augmented Gaussian Process Upper Confidence Bound (PA-GP-UCB)**，一种结合高成本真实反馈、低成本机器学习预测与离线数据的贝叶斯优化算法。

#### 核心思想与创新点：
- **联合建模**：将真实函数 $f$ 和 ML 预测函数 $f_{\text{ML}}$ 视为共享协方差结构的双任务 Gaussian Process（multi-task GP）。
- **两阶段设计**：
  - **Offline 阶段**：利用预测模型在 $\epsilon$-net 上批量查询，获得大量低质量预测数据以构建强先验；
  - **Online 阶段**：每次查询真实值的同时也获取对应点的预测值，用于动态校正偏差。
- **Control-variates 估计器**：引入形如  
  $$
  \hat{\mu}(x) = \mu^{\text{true}}(x) - \frac{\rho(x)\sigma^{\text{true}}(x)}{\sigma^{\text{ML}}(x)} (\mu^{\text{ML}}(x) - \mu^{\text{ML,all}}(x))
  $$
  的偏差校正机制，实现**方差缩减与系统性偏差纠正**。
- **理论保证**：证明其累积后悔（cumulative regret）满足：
  $$
  R_T = O\left(\sqrt{C \beta_T \gamma_T [1 - (1-R)\rho^2]}\right)
  $$
  其中 $R$ 是离线数据带来的不确定性压缩因子，$\rho$ 是预测相关性。相比 Vanilla GP-UCB 的 $O(\sqrt{\beta_T \gamma_T})$，PA-GP-UCB 在常数项上有严格改进。

### 相比现有方法的优势
| 方法 | 缺陷 | PA-GP-UCB 改进 |
|------|------|----------------|
| **Vanilla GP-UCB** | 忽视预测信号，收敛慢 | 利用预测加速探索，显著降低采样次数 |
| **Naive prediction-augmented GP-UCB** | 不纠正偏差，易陷入错误区域 | 显式偏差校正，避免被误导 |
| **Multi-fidelity BO** | 强调成本分配，不适合“预测免费”场景 | 将预测视为零成本辅助信息，更高效 |
| **PPI / MLA-UCB** | 限于静态推断或多臂老虎机（discrete） | 扩展至连续域，支持结构化探索 |

---

## 2. 核心实验方法和设置

### 数据集
1. **合成数据（Synthetic Benchmarks）**
   - 从 RBF 核 GP 中采样真实函数 $f$；
   - 构造相关但带偏的预测函数：$f_{\text{ML}} = \rho f + \sqrt{1-\rho^2}g$，其中 $g$ 为独立噪声 GP；
   - 局部反相关设定：在区间 $[0.4, 0.6]$ 内翻转 $f_{\text{ML}}$ 符号，测试鲁棒性。

2. **真实世界数据（Human Behavioral Dataset）**
   - 来源于 Milkman et al. (2021) 的大规模行为干预实验数据（共 54 种干预措施）；
   - **Ground-truth $f(x)$**：每种干预下用户的平均访问量（通过实地实验测量）；
   - **Predictor $f_{\text{ML}}(x)$**：
     - **有限动作空间**：轻量回归模型基于文本嵌入预测；
     - **连续空间**：使用 OpenAI o4-mini LLM 进行预测，采用三种提示方式：
       - K-shot（K=4 或 12）：提供若干真实示例；
       - Scale-only：只给全局统计信息（均值、最大最小值）；
   - **Embedding 构造**：使用 TF-IDF + UMAP 将文本描述映射到二维语义流形上，构造连续假设空间。

### 实验设置
- **Horizon**：$T = 200$ 轮；
- **重复次数**：50 次独立运行取平均；
- **评估指标**：
  - **Cumulative Regret**: $R_T = \sum_{t=1}^T [f(x^*) - f(x_t)]$；
  - **Optimum Localization Accuracy**：最终推荐点是否接近真实最优；
  - **Posterior Variance Reduction**：比较不确定性下降速度。

### 基线方法对比
- **Vanilla GP-UCB**：标准贝叶斯优化；
- **GP-UCB with offline prediction only**：仅用离线预测初始化先验；
- **GP-UCB with offline + online prediction**：加入所有预测观测但无偏差校正；
- **PA-GP-UCB variants**：消融不同参数（$M$: grid size, $N$: 重复次数, $\rho$: 相关性）的影响。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
#### （1）合成环境结果（图2）
- **PA-GP-UCB 显著优于所有基线**：
  - 在 $T=200$ 时，**累计后悔降低约 30–60%**；
  - 即使预测相关性较低（$\rho=0.5$），仍能取得明显增益；
  - 当 $\rho=0.9$ 时，收敛速度提升近一倍。
- **局部反相关测试**（图1）：
  - Naive 方法因未校正偏差，持续偏向预测偏好区，无法找到真实最优；
  - PA-GP-UCB 成功识别并修正偏差，在后期集中探索真实峰值附近。

#### （2）真实数据实验结果（图3）
##### 有限动作空间（Finite-Arm Setting）
- 预测模型与真实反馈的相关性 $\rho \approx 0.66$；
- PA-GP-UCB 在所有设置下均优于 Vanilla GP-UCB；
- 随着离线重复查询数 $N$ 增加（1 → 100），性能进一步提升，表明**更多离线预测有助于稳定探索**。

##### 连续空间假设生成（Continuous Hypothesis Evaluation）
- 使用 LLM 作为预测器：
  - **Scale-only 提示表现最佳**：虽无具体标签，但提供了可靠的全局尺度信息，防止过拟合小样本；
  - **K=12 > K=4**：更多信息带来更好引导；
- Vanilla GP-UCB 出现近似线性后悔，说明其后验严重误设；
- PA-GP-UCB 实现稳定亚线性收敛，**验证了其对 kernel misspecification 的鲁棒性**。

### 消融实验结果
| 变量 | 发现 |
|------|------|
| **Offline 数据量 $M, N$**（图2c） | 即使 $M=N=1$（极少量离线数据），也能带来性能提升；增大 $M,N$ 继续改善效果 |
| **预测相关性 $\rho$**（图2b） | 性能随 $\rho$ 提高单调上升，但即使 $\rho=0.5$ 也有显著收益 |
| **控制变量有效性** | 若移除 control-variates 结构，性能退化至 naive baseline 水平 |

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **PA-GP-UCB 能有效融合廉价预测与昂贵反馈**，在保持理论保证的前提下显著提高样本效率；
2. ✅ **Control-variates 估计器是关键**，它不仅能减少方差，还能主动纠正系统性偏差；
3. ✅ **即使预测质量一般（$\rho \sim 0.5–0.8$）或仅有少量离线数据，也能带来实质性改进**；
4. ✅ 在真实假设生成任务中，PA-GP-UCB 成功发现了高质量策略：
   > “结合高频小额金钱激励 + 规划提醒机制”的干预最能促进健身房访问行为；
5. ✅ 该框架特别适合**稀疏数据、模型误设、结构复杂**的科学发现场景。

### 方法的局限性
- **理论条件较保守**：定理要求的 $(\epsilon, N)$ 参数远高于实际所需（实验显示即使粗粒度网格也有效）；
- **依赖预测相关性 $\rho > 0$**：若预测完全无关（$\rho \approx 0$），则无增益；
- **需手动设定 $\rho$**：当前使用全局估计值，未来可考虑自适应学习；
- **并行化需求高**：Offline 阶段需大规模并行查询预测模型，可能受限于 API 成本或延迟。

### 未来工作方向
- ❗ **弱化离线数据假设**：发展自适应或非均匀离线采样策略，替代固定 $\epsilon$-net；
- ❗ **动态估计 $\rho(x)$**：允许相关性随输入变化，提升局部适应能力；
- ❗ **Kernel robustness analysis**：研究如何选择或学习更适合语义空间的 kernel；
- ❗ **Cost-aware offline design**：优化离线预算分配，在有限资源下最大化收益；
- ❗ **扩展至大规模科学假设搜索**：应用于药物设计、材料发现等领域，集成 LLM 自动生成新假设。

---

> **Impact Statement**：  
> 本文提出的 PA-GP-UCB 为 **human-AI collaborative discovery** 提供了一个通用且高效的框架，能够在减少昂贵人类评估的同时，系统性地探索创新假设空间，推动科学研究向更高效、可持续的方向发展。

</details>

---

### 16. [DRL-Enabled Trajectory Planing for UAV-Assisted VLC: Optimal Altitude and Reward Design](https://arxiv.org/abs/2601.22512)

**Authors**: Tian-Tian Lin, Yi Liu, Xiao-Wei Tang, Yunmei Shi, Yi Huang, Zhongxiang Wei, Qingqing Wu, Yuhan Dong  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.22512v1  

#### Abstract
Recently, the integration of unmanned aerial vehicle (UAV) and visible light communication (VLC) technologies has emerged as a promising solution to offer flexible communication and efficient lighting. This letter investigates the three-dimensional trajectory planning in a UAV-assisted VLC system, w...

---

### 17. [OSNIP: Breaking the Privacy-Utility-Efficiency Trilemma in LLM Inference via Obfuscated Semantic Null Space](https://arxiv.org/abs/2601.22752)

**Authors**: Zhiyuan Cao, Zeyu Ma, Chenhao Yang, Han Zheng, Mingang Chen  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.22752v1  

#### Abstract
We propose Obfuscated Semantic Null space Injection for Privacy (OSNIP), a lightweight client-side encryption framework for privacy-preserving LLM inference. Generalizing the geometric intuition of linear kernels to the high-dimensional latent space of LLMs, we formally define the ``Obfuscated Seman...

---

### 18. [From Self-Evolving Synthetic Data to Verifiable-Reward RL: Post-Training Multi-turn Interactive Tool-Using Agents](https://arxiv.org/abs/2601.22607)

**Authors**: Jiaxuan Gao, Jiaao Chen, Chuyi He, Wei-Chen Wang, Shusheng Xu, Hanrui Wang, Di Jin, Yi Wu  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22607v1  

#### Abstract
Interactive tool-using agents must solve real-world tasks via multi-turn interaction with both humans and external environments, requiring dialogue state tracking, multi-step tool execution, while following complex instructions. Post-training such agents is challenging because synthesis for high-qua...

---

### 19. [Real-Time Aligned Reward Model beyond Semantics](https://arxiv.org/abs/2601.22664)

**Authors**: Zixuan Huang, Xin Xia, Yuxi Ren, Jianbin Zheng, Xuefeng Xiao, Hongyan Xie, Li Huaqiu, Songshi Liang, Zhongxiang Dai, Fuzhen Zhuang, Jianxin Li, Yikun Ban, Deqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22664v1  

#### Abstract
Reinforcement Learning from Human Feedback (RLHF) is a pivotal technique for aligning large language models (LLMs) with human preferences, yet it is susceptible to reward overoptimization, in which policy models overfit to the reward model, exploit spurious reward patterns instead of faithfully capt...

---

### 20. [$\rho$-$\texttt{EOS}$: Training-free Bidirectional Variable-Length Control for Masked Diffusion LLMs](https://arxiv.org/abs/2601.22527)

**Authors**: Jingyi Yang, Yuxian Jiang, Jing Shao  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22527v1  

#### Abstract
Beyond parallel generation and global context modeling, current masked diffusion large language models (dLLMs) suffer from a fundamental limitation: they require a predefined, fixed generation length, which lacks flexibility and forces an inevitable trade-off between output quality and computational...

---

### 21. [Sparse or Dense? A Mechanistic Estimation of Computation Density in Transformer-based LLMs](https://arxiv.org/abs/2601.22795)

**Authors**: Corentin Kervadec, Iuliia Lysova, Marco Baroni, Gemma Boleda  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22795v1  

#### Abstract
Transformer-based large language models (LLMs) are comprised of billions of parameters arranged in deep and wide computational graphs. Several studies on LLM efficiency optimization argue that it is possible to prune a significant portion of the parameters, while only marginally impacting performanc...

---

### 22. [FIRE: Multi-fidelity Regression with Distribution-conditioned In-context Learning using Tabular Foundation Models](https://arxiv.org/abs/2601.22371)

**Authors**: Rosen Ting-Ying Yu, Nicholas Sung, Faez Ahmed  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22371v1  

#### Abstract
Multi-fidelity (MF) regression often operates in regimes of extreme data imbalance, where the commonly-used Gaussian-process (GP) surrogates struggle with cubic scaling costs and overfit to sparse high-fidelity observations, limiting efficiency and generalization in real-world applications. We intro...

---

### 23. [Gradual Fine-Tuning for Flow Matching Models](https://arxiv.org/abs/2601.22495)

**Authors**: Gudrun Thorkelsdottir, Arindam Banerjee  
**Category**: cs.LG  
**Published**: 2026-02-02  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.22495v1  

#### Abstract
Fine-tuning flow matching models is a central challenge in settings with limited data, evolving distributions, or strict efficiency demands, where unconstrained fine-tuning can erode the accuracy and efficiency gains learned during pretraining. Prior work has produced theoretical guarantees and empi...

---

### 24. [Test-Time Mixture of World Models for Embodied Agents in Dynamic Environments](https://arxiv.org/abs/2601.22647)

**Authors**: Jinwoo Jang, Minjong Yoo, Sihyung Yoon, Honguk Woo  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22647v1  

#### Abstract
Language model (LM)-based embodied agents are increasingly deployed in real-world settings. Yet, their adaptability remains limited in dynamic environments, where constructing accurate and flexible world models is crucial for effective reasoning and decision-making. To address this challenge, we ext...

---

### 25. [Task-Aware LLM Council with Adaptive Decision Pathways for Decision Support](https://arxiv.org/abs/2601.22662)

**Authors**: Wei Zhu, Lixing Yu, Hao-Ren Yao, Zhiwen Tang, Kun Yue  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22662v1  

#### Abstract
Large language models (LLMs) have shown strong capabilities across diverse decision-making tasks. However, existing approaches often overlook the specialization differences among available models, treating all LLMs as uniformly applicable regardless of task characteristics. This limits their ability...

---

### 26. [A Step Back: Prefix Importance Ratio Stabilizes Policy Optimization](https://arxiv.org/abs/2601.22718)

**Authors**: Shiye Lei, Zhihao Cheng, Dacheng Tao  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22718v1  

#### Abstract
Reinforcement learning (RL) post-training has increasingly demonstrated strong ability to elicit reasoning behaviors in large language models (LLMs). For training efficiency, rollouts are typically generated in an off-policy manner using an older sampling policy and then used to update the current t...

---

### 27. [TSPO: Breaking the Double Homogenization Dilemma in Multi-turn Search Policy Optimization](https://arxiv.org/abs/2601.22776)

**Authors**: Shichao Ma, Zhiyuan Ma, Ming Yang, Xiaofan Li, Xing Wu, Jintao Du, Yu Cheng, Weiqiang Wang, Qiliang Liu, Zhengyang Zhou, Yang Wang  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22776v1  

#### Abstract
Multi-turn tool-integrated reasoning enables Large Language Models (LLMs) to solve complex tasks through iterative information retrieval. However, current reinforcement learning (RL) frameworks for search-augmented reasoning predominantly rely on sparse outcome-level rewards, leading to a "Double Ho...

---

### 28. [MulFeRL: Enhancing Reinforcement Learning with Verbal Feedback in a Multi-turn Loop](https://arxiv.org/abs/2601.22900)

**Authors**: Xuancheng Li, Haitao Li, Yujia Zhou,  YiqunLiu, Qingyao Ai  
**Category**: cs.AI  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22900v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) is widely used to improve reasoning in multiple domains, yet outcome-only scalar rewards are often sparse and uninformative, especially on failed samples, where they merely indicate failure and provide no insight into why the reasoning fails. In ...

---

### 29. [SSL: Sweet Spot Learning for Differentiated Guidance in Agentic Optimization](https://arxiv.org/abs/2601.22491)

**Authors**: Jinyang Wu, Changpeng Yang, Yuhao Shen, Fangzhi Xu, Bolin Ni, Chonghua Liao, Yuchen Liu, Hongzhen Wang, Shuai Nie, Shuai Zhang, Haoran Luo, Jiaming Xu  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22491v1  

#### Abstract
Reinforcement learning with verifiable rewards has emerged as a powerful paradigm for training intelligent agents. However, existing methods typically employ binary rewards that fail to capture quality differences among trajectories achieving identical outcomes, thereby overlooking potential diversi...

---

### 30. [RASST: Fast Cross-modal Retrieval-Augmented Simultaneous Speech Translation](https://arxiv.org/abs/2601.22777)

**Authors**: Jiaxuan Luo, Siqi Ouyang, Lei Li  
**Category**: cs.CL  
**Published**: 2026-02-02  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.22777v1  

#### Abstract
Simultaneous speech translation (SST) produces target text incrementally from partial speech input. Recent speech large language models (Speech LLMs) have substantially improved SST quality, yet they still struggle to correctly translate rare and domain-specific terminology. While retrieval augmenta...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
