# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-13 05:57:11 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [ARCQuant: Boosting NVFP4 Quantization with Augmented Residual Channels for LLMs](https://arxiv.org/abs/2601.07475)

**Authors**: Haoqian Meng, Yilun Luo, Yafei Zhao, Wenyuan Liu, Peng Zhang, Xindian Ma  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2601.07475v1  

#### Abstract
The emergence of fine-grained numerical formats like NVFP4 presents new opportunities for efficient Large Language Model (LLM) inference. However, it is difficult to adapt existing Post-Training Quantization (PTQ) strategies to these formats: rotation-based methods compromise fine-grained block isol...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：ARCQuant: Boosting NVFP4 Quantization with Augmented Residual Channels for LLMs**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前在大型语言模型（LLM）推理中，**NVFP4** 等细粒度数值格式虽能提升效率，但直接应用于 **W4A4（权重和激活均为4位）** 场景时面临三大挑战：
- **旋转类方法**（如QuaRot）破坏了NVFP4的块级隔离特性，导致局部动态范围扩大；
- **平滑技术**（如SmoothQuant）难以应对4位量化带来的显著误差；
- **混合精度方法**（如Atom）因硬件要求统一精度计算而无法高效运行。

这些限制使得现有PTQ（Post-Training Quantization）策略难以充分发挥NVFP4的潜力。

---

### **提出的新方法：ARCQuant**
作者提出了 **ARCQuant**（Augmented Residual Channels Quantization），一种专为NVFP4优化的新型PTQ框架，其核心思想是：
- **通过增广残差通道进行误差补偿**，而非依赖混合精度或全局变换。
- 在输入张量中识别出主导异常值的通道，并将其量化后的残差作为“增强通道”拼接到原始激活矩阵中。
- 整个补偿过程被映射到矩阵乘法的扩展规约维度上，从而可直接使用标准、高度优化的 **GEMM kernel**（如CUTLASS）执行，无需修改底层计算逻辑。

该设计实现了：
- **严格保持统一的NVFP4格式**，兼容硬件加速；
- 将误差补偿集成进常规矩阵运算，实现高吞吐与低延迟。

---

### **相比现有方法的优势**
| 维度 | ARCQuant | 其他方法（如Atom、QuaRot） |
|------|----------|-----------------------------|
| **硬件兼容性** | ✅ 保持统一NVFP4格式，支持Tensor Core加速 | ❌ 混合精度破坏统一性，无法高效利用硬件 |
| **块隔离保护** | ✅ 显式隔离异常通道，保留细粒度优势 | ❌ 旋转操作传播异常值，破坏隔离 |
| **实现复杂度** | ✅ 利用标准GEMM kernel，部署简单 | ❌ 需定制kernel或复杂内存对齐 |
| **精度表现** | ✅ 接近W4A8水平，优于所有W4A4基线 | ⚠️ 多数仅接近或低于W4A4 RTN |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **校准数据**：`WikiText2`（128个样本，序列长度2048）
- **评估任务**：
  - **通用能力**：`ARC-C`, `HellaSwag`, `PIQA`, `Winograde`, `LAMBADA`
  - **多任务理解**：`MMLU`（5-shot）
  - **代码生成**：`HumanEval`, `MBPP`
  - **数学推理**：`GSM8K`, `CMATH`
  - **困惑度**：`WikiText2 PPL`

### **实验设置与评估指标**
- **模型**：`Llama3.1-8B`, `Qwen2.5-7B`, `Qwen2.5-32B`, `Qwen2.5-Coder-7B-Instruct`, `Qwen2.5-Math-7B-Instruct`
- **量化配置**：目标为 **W4A4**，采用 **NVFP4** 格式（block size=16）
- **评估指标**：
  - 准确率（zero/few-shot）
  - 困惑度（PPL）
  - 推理延迟（prefill latency）
  - 内存占用
  - 速度提升（speedup）

### **基线方法对比**
| 类型 | 方法 |
|------|------|
| 基础量化 | `FP16`, `W4A4 RTN`（INT4/MXFP4/NVFP4）, `W4A8+RTN` |
| 高级PTQ | `FlatQuant`, `Atom` |
| 可适配方法 | `SmoothQuant`, `QuaRot`（adapted to NVFP4） |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **表1 & 表2：主流模型上的准确率与PPL表现**
| 模型 | 方法 | PPL ↓ | 平均准确率 ↑ |
|------|------|--------|--------------|
| Llama3.1-8B | FP16 | 6.24 | 72.56 |
| | W4A8+RTN | 7.07 | 70.59 |
| | **ARCQuant** | **6.87** | **70.90** |
| Qwen2.5-7B | FP16 | 6.85 | 70.97 |
| | W4A8+RTN | 7.44 | 69.15 |
| | **ARCQuant** | **7.28** | **70.28** |

> ✅ **ARCQuant在多个模型上超越W4A8基线，在PPL和下游任务上逼近FP16性能。**

#### **表3：代码生成性能（Qwen2.5-Coder-7B-Instruct）**
| 方法 | HumanEval @1 | MBPP @1 |
|------|---------------|---------|
| FP16 | 84.1 | 80.4 |
| Atom | 80.5 | 74.5 |
| **ARCQuant** | **86.0** | **79.9** |

> ✅ **ARCQuant不仅未损失性能，反而在代码生成任务上超过FP16！**

---

### **与基线方法的对比结果**
- 在所有W4A4方法中，**ARCQuant全面领先**，尤其在处理激活异常值密集层时效果显著（见Figure 3）。
- 相比之下：
  - `QuaRot`（基于Hadamard变换）在NVFP4上表现退化，验证了旋转会破坏块隔离；
  - `SmoothQuant` 改进有限，受限于4位权重表达能力；
  - `Atom` 因混合精度不兼容Blackwell架构，性能下降明显。

---

### **消融实验结果**

#### **1. 泛化性测试（INT4 / MXFP4）**
| 方法 | INT4 PPL ↓ | MXFP4 PPL ↓ |
|------|------------|-------------|
| RTN | 8.84 | 7.86 |
| **ARCQuant** | **7.95** | **7.50** |

> 即使不在NVFP4下，ARCQuant仍能带来显著收益，说明其残差补偿机制具有普适性。

#### **2. 校准鲁棒性**
使用不同领域数据（`C4`, `WikiText2`, `HumanEval`）进行校准，ARCQuant性能波动极小（PPL变化 < 0.03），表明其对校准数据选择不敏感。

#### **3. 效率分析**
- **GEMM延迟增长线性于增强通道数S**，但在典型范围（S ≤ 512）内开销极低；
- 相比FP16：
  - **内存降低1.5×–2.8×**
  - **预填充速度提升2.0×–3.5×**（RTX 5090上Llama3.1-8B达3.5×）
- 相比无补偿的NVFP4，延迟仅增加3%–9%，证明融合kernel高效。

---

## **4. 关键结论和发现**

### **主要发现**
1. **NVFP4的潜力需专用算法释放**：传统PTQ方法（旋转、混合精度）与NVFP4硬件特性冲突，无法有效利用其细粒度优势。
2. **残差通道增广是一种硬件友好的补偿范式**：将误差补偿转化为输入维度扩展，可在不牺牲吞吐的前提下实现高保真重建。
3. **理论误差界媲美MXFP8**：双阶段量化机制的最坏情况误差上界与标准8位格式相当，解释了其高精度表现。
4. **实际部署高效可行**：通过融合kernel（重排序 + RMSNorm + 量化 + 残差计算），实现端到端仅4.9%额外开销。

---

### **方法的局限性**
| 局限 | 说明 |
|------|------|
| **依赖离线校准** | 通道重排序和S的选择基于校准数据，假设分布稳定；极端OOD输入可能影响效果 |
| **硬件依赖性强** | 当前针对NVIDIA Blackwell架构设计，依赖NVFP4原生支持；旧架构只能模拟性能 |
| **权重量化较基础** | 权重采用RTN策略，未结合GPTQ/AWQ等先进方法，仍有提升空间 |

---

### **未来工作方向**
- 扩展至 **sub-4-bit格式**（如INT2/NVFP2）
- 结合 **先进的权重量化算法**（如GPTQ、AWQ）构建全模型混合补偿方案
- 集成至生产级推理引擎（如 **vLLM**）以验证真实场景下的效率
- 探索 **动态自适应S选择机制**，提升对输入变化的鲁棒性

---

> 🔗 **开源地址**：https://github.com/actypedef/ARCQuant  
> 📌 **一句话总结**：ARCQuant通过“用少量计算维度换高保真度”的思想，在严格遵守NVFP4硬件约束的同时，实现了W4A4量化下的SOTA精度与显著推理加速。

</details>

---

### 2. [d3LLM: Ultra-Fast Diffusion LLM using Pseudo-Trajectory Distillation](https://arxiv.org/abs/2601.07568)

**Authors**: Yu-Yang Qian, Junda Su, Lanxiang Hu, Peiyuan Zhang, Zhijie Deng, Peng Zhao, Hao Zhang  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.07568v1  

#### Abstract
Diffusion large language models (dLLMs) offer capabilities beyond those of autoregressive (AR) LLMs, such as parallel decoding and random-order generation. However, realizing these benefits in practice is non-trivial, as dLLMs inherently face an accuracy-parallelism trade-off. Despite increasing int...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：d3LLM: Ultra-Fast Diffusion LLM using Pseudo-Trajectory Distillation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
**dLLM**（diffusion large language models）虽然具备并行解码、错误纠正和随机顺序生成等超越传统**AR**（autoregressive）模型的能力，但在实践中面临一个根本性的**accuracy-parallelism trade-off**（准确率-并行度权衡）：
- 追求高并行度往往导致准确率下降；
- 维持高准确率则限制了解码速度。

现有方法通常只侧重于效率或性能之一，难以兼顾。此外，标准训练采用**random masking**，缺乏对“哪些token可以安全早解码”的指导；推理时多块解码易因上下文不完整而降低质量。

---

### 提出的新方法与创新思路
作者提出 **d3LLM**（pseuDo-Distilled-Diffusion LLM），通过**训练**与**推理**两阶段优化，在准确率与并行度之间取得平衡：

#### （1）训练阶段：Pseudo-Trajectory Distillation（伪轨迹蒸馏）
- 利用教师 dLLM 自身的解码轨迹作为“伪真实路径”，即使其最终输出不完全正确。
- 将该轨迹与真实 prompt-response 对结合，构建带噪声的序列用于训练学生模型，使其学习到更优的 unmasking 顺序。
- 引入**课程学习策略**：
  - **Curriculum Noise Level**：逐步增加 mask ratio（从 0.0 到 0.8），由易到难。
  - **Curriculum Window Size**：逐步扩大解码窗口大小（从 16 到 32），提升上下文适应能力。

#### （2）推理阶段：Entropy-Based Multi-Block Decoding + KV-Cache Refresh
- **基于熵的多块并行解码**：优先解码低熵（高置信度）token，支持跨多个 block 并行处理。
- **KV-Cache Refresh 机制**：定期重新计算已缓存的 KV 状态，缓解误差传播。
- **Early Stopping on EOS**：一旦生成 EOS token 即停止解码，避免冗余计算。

#### （3）提出新评估指标：AUP（Accuracy Under Parallelism）
- 传统指标如 TPS（tokens per second）或 accuracy 孤立看待效率或性能，无法反映 trade-off。
- **AUP** 是加权下的 accuracy-parallelism 曲线下面积，强调在保持高准确率前提下提升并行度。
- 权重函数 $ W(y) = \min(e^{-(1 - y/y_{\text{max}})}, 1) $ 惩罚准确率下降区域。

---

### 相比现有方法的优势
| 方法 | 局限性 | d3LLM 改进 |
|------|--------|-----------|
| Random Masking | 缺乏 unmasking 顺序指导 | 通过伪轨迹提供中间监督 |
| D2F / Fast-dLLM | 牺牲 accuracy 换 parallelism | 在不显著损失 accuracy 下实现更高 parallelism |
| dParallel | 准确率尚可但 parallelism 不足 | 显著提升 TPF 同时维持 accuracy |
| 单一指标评估（如 TPS） | 忽视 accuracy-parallelism trade-off | 提出 AUP 综合衡量 |

> ✅ **核心优势**：首次系统性地同时优化 dLLM 的训练指导信号与推理调度机制，实现了**超高速且高精度**的扩散语言模型。

---

## 2. 核心实验方法和设置

### 使用的数据集
在以下五个代表性任务上进行评估：
- **GSM8K-CoT**：数学推理（零样本）
- **MATH**：竞赛级数学题（四样本）
- **HumanEval**：代码生成（零样本）
- **MBPP**：Python 编程任务（三样本）
- **Long-GSM8K**：长上下文数学推理（五样本，prompt ~1000 tokens）

这些任务覆盖了推理、编码、长文本等多种场景，适合测试并行解码能力。

---

### 实验设置
- **基础模型**：
  - LLaDA-7B
  - Dream-7B
  - Dream-Coder-7B
- **衍生模型**：
  - d3LLM-LLaDA
  - d3LLM-Dream
  - d3LLM-Coder
- **训练细节**：
  - 使用 LoRA 微调，rank=256
  - AdamW 优化器，lr=2e-5，weight decay=0.01
  - 批大小为 64（梯度累积）
  - 在 H100 上训练，bfloat16 精度
- **推理设置**：
  - 单 GPU，batch size=1
  - Block size=32
  - Greedy decoding，temperature=0.0 或 0.1

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **TPF**（Tokens Per Forward） | 每次前向传播生成的 token 数量，衡量算法级并行度 |
| **TPS**（Tokens Per Second） | 实际吞吐量，受硬件影响大 |
| **Accuracy** | Solve Rate / Pass@1 |
| **AUP**（Accuracy Under Parallelism） | 新提出的综合指标，反映 accuracy-parallelism 平衡能力 |

> ⚠️ 特别说明：AUP 基于 TPF 而非 TPS，具有**硬件无关性**，更适合公平比较算法进步。

---

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Vanilla LLaDA / Dream** | 原始 dLLM | 随机掩码，串行解码 |
| **Fast-dLLM** | 推理加速 | 块级 KV Cache，无需训练 |
| **Fast-dLLM-v2** | AR → dLLM 转换 | 微调实现高效 block diffusion |
| **D2F** | 混合蒸馏 | AR-diffusion hybrid，引入 certainty forcing |
| **dParallel** | 蒸馏方法 | certainty-forcing distillation 提升并行度 |
| **EAGLE-3** | Speculative Decoding | 使用 draft model 加速 AR 模型（用于上限参考） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

| 模型 | 最高 AUP | 最高 TPF | TPS (H100) | 相对于 AR 的 speedup |
|-------|---------|----------|------------|------------------------|
| d3LLM-LLaDA | **637.7** (GSM8K-CoT) | **9.11** | **288.9** | **5.0×** |
| d3LLM-Dream | **391.3** (GSM8K-CoT) | **4.94** | **235.3** | **4.1×** |
| d3LLM-Coder | **208.4** (HumanEval) | **2.88** | — | **8× vs Dream-Coder** |

> 💡 **总体表现**：d3LLM 在 **9 out of 10 任务中取得最高 AUP 分数**，证明其在 accuracy-parallelism 曲线上全面领先。

---

### 与基线方法的对比结果

#### 在 LLaDA 基础上的比较（Table 1）
| 方法 | TPF | Acc (%) | AUP |
|------|-----|--------|-----|
| LLaDA | 1.00 | 72.6 | 72.6 |
| Fast-dLLM | 2.77 | 74.7 | 205.8 |
| D2F | 2.88 | 73.2 | 209.7 |
| dParallel | 5.14 | 72.6 | 358.1 |
| **d3LLM (ours)** | **9.11** | **73.1** | **637.7** |

👉 **结论**：d3LLM 的 TPF 是 dParallel 的 1.77 倍，AUP 提升近 **78%**。

#### 在 Dream 基础上的比较（Table 2）
| 方法 | TPF | Acc (%) | AUP |
|------|-----|--------|-----|
| Dream | 1.00 | 83.9 | 83.9 |
| Fast-dLLM-v2 | 2.21 | 77.5 | 156.0 |
| dParallel | 3.02 | 82.1 | 245.7 |
| **d3LLM (ours)** | **4.94** | **81.4** | **391.3** |

👉 **结论**：d3LLM 实现了接近 **5× TPF 提升**，AUP 提升超过 **59%**。

#### 实际吞吐量（TPS）对比（Tables 3 & 4）
| 模型 | H100 TPS | Speedup vs AR |
|------|----------|----------------|
| Qwen-2.5-7B-it (AR) | 57.3 | 1.0× |
| d3LLM-LLaDA | **288.9** | **5.0×** |
| d3LLM-Dream | **235.3** | **4.1×** |
| Vanilla LLaDA | 27.9 | 0.5× |
| Vanilla Dream | 27.6 | 0.5× |

> 🔥 **亮点**：相比原始 dLLM，d3LLM 实现了 **10× 以上的 TPS 提升**！

---

### 消融实验结果（Ablation Study）

#### （1）蒸馏策略消融（Table 5 上半部分）
| 配置 | TPF | Acc | AUP |
|------|-----|-----|-----|
| Baseline（无蒸馏） | 6.41 | 72.2 | 441.4 |
| + Pseudo-Trajectory | 7.55 | 72.1 | 517.7 |
| + Curriculum Noise | 8.46 | 69.8 | 551.3 |
| **Full（+ Curriculum Window）** | **9.11** | **73.1** | **637.7** |

✅ 结论：所有组件均有贡献，尤其是 curriculum window 能恢复 accuracy 并进一步提升 parallelism。

#### （2）解码策略消融（Table 5 下半部分）
| 配置 | TPF | Acc | AUP |
|------|-----|-----|-----|
| Vanilla Block Diffusion | 7.01 | 73.2 | 492.9 |
| + Multi-Block Decoding | 9.07 | 73.1 | 635.0 |
| **+ Early Stopping** | **9.11** | **73.1** | **637.7** |

✅ 结论：multi-block decoding 是主要增益来源，early stopping 进一步优化效率。

#### （3）超参数分析
- **Curriculum Noise Level**（Table 6）：从固定 noise 到 0.0→0.8，AUP 提升 **22.2%**
- **Curriculum Window Size**（Table 7）：从固定 k=32 到 16→32，AUP 提升 **19.0%**

---

## 4. 关键结论和发现

### 主要发现
1. **dLLM 存在固有的 accuracy-parallelism trade-off**，现有方法难以兼顾。
2. **伪轨迹蒸馏**能有效传递教师模型的 unmasking 策略，显著提升并行度。
3. **课程学习**（curriculum learning）对稳定蒸馏过程至关重要，避免 accuracy 崩溃。
4. **多块熵驱动解码 + KV-Cache Refresh** 可在不牺牲 accuracy 的情况下大幅提升推理效率。
5. **AUP 是一个合理且必要的新指标**，能更真实反映 dLLM 的综合性能。
6. d3LLM 实现了：
   - **高达 10× 的 TPS 提升**（vs vanilla dLLM）
   - **3.6×–5× 的 speedup**（vs AR 模型）
   - **几乎无 accuracy 损失**

---

### 方法的局限性
1. **依赖教师模型质量**：若教师 dLLM 本身性能差，则伪轨迹可能误导学生。
2. **仍落后于 speculative decoding**：如 EAGLE-3 在 AUP 上更高（见 Table 9），因其有验证机制保障 accuracy。
3. **未做系统级优化**：当前基于 HuggingFace Transformers，未融合 kernel fusion 或 paged attention。
4. **仅适用于已有的 dLLM 架构**：不能直接用于 AR 模型，需先转换为 dLLM。

---

### 未来工作方向（Future Work）
1. **结合 speculative decoding**：用小型 dLLM 作 draft model，大模型验证，进一步提升效率。
2. **应用于更强的 dLLM 基座**：如 ReFusion、LLaDA 2.0 等，探索更大规模潜力。
3. **引入强化学习**：利用 trajectory-aware RL（如 TraDo）优化解码路径。
4. **系统级优化**：
   - GPU kernel fusion for multi-block attention
   - 支持 vLLM 的 paged attention（适配 bidirectional attention）
   - 更高效的 KV-Cache 管理
5. **扩展至多模态 dLLM**：如 MMaDA 架构，统一处理图文生成。

---

> 📌 **总结一句话**：  
> **d3LLM 通过伪轨迹蒸馏与多块熵解码，在训练与推理两端协同优化，首次实现了 ultra-fast 且 high-accuracy 的 dLLM，推动了 diffusion LLM 向实用化迈进一大步。**

</details>

---

### 3. [TALON: Confidence-Aware Speculative Decoding with Adaptive Token Trees](https://arxiv.org/abs/2601.07353)

**Authors**: Tianyu Liu, Qitan Lv, Yuhao Shen, Xiao Sun, Xiaoyan Sun  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.07353v1  

#### Abstract
Speculative decoding (SD) has become a standard technique for accelerating LLM inference without sacrificing output quality. Recent advances in speculative decoding have shifted from sequential chain-based drafting to tree-structured generation, where the draft model constructs a tree of candidate t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TALON: Confidence-Aware Speculative Decoding with Adaptive Token Trees

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **tree-based Speculative Decoding (SD)** 方法（如 EAGLE）通常采用**固定宽度和深度**的 draft tree 结构，即每层扩展固定的 `K` 个子节点，并生成固定 `D` 层。这种静态策略存在以下问题：
- **在确定性上下文中浪费计算资源**：当模型对预测非常自信时（如“法国的首都是巴黎”），仍会盲目扩展 `K` 个分支，造成冗余（over-exploration）。
- **在不确定性上下文中探索不足**：当模型高度不确定时（如“量子计算可以…”），仅靠 top-K 采样可能无法覆盖目标分布，导致早期拒绝（under-exploration）。
- **无法动态调整生成长度**：固定深度限制了简单任务的加速潜力，同时强迫复杂任务继续生成注定被拒绝的长分支。

### 提出的新方法与思路
作者提出 **TALON**，一种**无需训练、基于预算驱动的自适应树扩展框架**，其核心思想是：
- **以全局 token 预算 `N` 为约束**，而非固定 `K` 和 `D`。
- **迭代构建 draft tree**，直到总节点数达到预算 `N`。
- 采用**混合扩展策略（Hybrid Expansion Strategy）**：
  - **第0层（根层）**：使用 **Top-K 初始化**，确保鲁棒性，避免因 draft model 初始过自信而陷入错误路径。
  - **后续层（d ≥ 1）**：使用 **置信度过滤（Confidence-Gated Expansion）**，只保留概率不低于锚点概率 `m_a` 的 `μ` 倍的候选节点（即 `p(u) ≥ μ · m_a`），实现“边扩展边剪枝”。

该机制使 draft tree 能够自然演化为：
- **“深且窄”（deep-and-narrow）**：在低熵、确定性强的上下文中，最大化生成长度。
- **“浅且宽”（shallow-and-wide）**：在高熵、不确定性强的上下文中，增强覆盖率。

### 相比现有方法的优势
- **更高的灵活性与效率**：动态适应不同上下文难度，优化探索广度与生成深度之间的权衡。
- **无需额外训练**：完全基于推理时决策，可直接集成到现有 tree-based SD 框架中。
- **减少计算浪费**：通过 early stopping 避免在困难 token 上过度投入，在简单 token 上充分扩展。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在 **6 个多样化基准**上进行，涵盖多种任务类型：
- **指令遵循**：Alpaca
- **数学推理**：GSM8K
- **代码生成**：HumanEval
- **多轮对话**：MT-Bench
- **问答**：QA (Natural Questions)
- **摘要生成**：CNN/DM

### 实验设置
- **模型**：在 5 种主流 LLM 上测试，包括：
  - Llama-3.1-8B-Instruct
  - Qwen3-8B / Qwen3-32B
  - DeepSeek-R1-Distill-LLaMA-8B (DSL-8B)
  - Vicuna-13B
- **硬件**：单张 NVIDIA H200 (140GB) GPU，batch size = 1。
- **预算设置**：TALON 与 EAGLE-3 共享相同的全局 token 预算 `N = 60`。
- **温度设置**：主实验使用 `T=0`（greedy decoding），补充实验使用 `T=1` 验证随机采样下的鲁棒性。
- **超参数**：TALON 默认阈值 `μ = 0.03`，初始化 `K = 10`。

### 评估指标
- **Mean Accepted Tokens (MAT)**：每次验证步骤平均接受的 draft tokens 数量，反映 draft 质量。
- **Wall-time Speedup (Spd.)**：相对于标准 auto-regressive 解码的实际端到端加速比，为核心性能指标。
- **Draft Efficiency (δ)**：定义为 `δ = N_d / N_p`，表示每个验证步所消耗的 draft 步数，用于分析成本效益。

### 基线方法对比
- **Speculative Decoding (SD)**：基础链式推测解码。
- **MEDUSA / HYDRA**：基于多头预测的 MLP-based 方法。
- **OPT-Tree**：基于搜索优化的 tree-based 方法。
- **EAGLE-3**：当前最先进的 tree-based SD 方法，作为主要对比基线。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- TALON 在所有 5 个模型和 6 个数据集上均显著优于 EAGLE-3。
- 最高实现 **5.16× 的端到端加速比**（Vicuna-13B + HumanEval）。
- 在 CNN/DM 上达到 **2.30× 加速**（Qwen3-8B），远超 EAGLE-3 的 1.95×。

### 与基线方法的对比结果（摘自 Table 1）
| Model | Method | HumanEval (Spd.) | GSM8K (Spd.) | CNNDM (Spd.) |
|-------|--------|------------------|--------------|---------------|
| Vicuna-13B | EAGLE-3 | 4.77× | 3.87× | 3.43× |
| Vicuna-13B | **TALON** | **5.16×** | **3.99×** | **3.58×** |
| Qwen3-8B | EAGLE-3 | 2.20× | 2.37× | 1.95× |
| Qwen3-8B | **TALON** | **2.57×** | **2.67×** | **2.30×** |

> ✅ 所有场景下 TALON 均取得最佳 speedup。

### 消融实验结果
#### （1）稳健初始化必要性（Top-K 层数 ablation）
- **k=0（无初始化）**：速度下降明显（如 Llama3-8B 上从 263.8 → 261.5 tok/s），验证了根层过自信风险。
- **k≥2（过多初始化）**：延迟进入置信度过滤阶段，增加冗余计算。
- **k=1（默认）**：在所有任务上表现最优，证明单层 Top-K 初始化 + 后续自适应扩展是最优设计。

#### （2）置信度阈值 `μ` 敏感性
- `μ` 控制探索与利用的权衡：
  - 较大 `μ`（如 0.04）→ 更严格过滤 → “深且窄” → 适合高对齐模型（如 DSL-8B）。
  - 较小 `μ`（如 0.01）→ 更宽松 → “浅且宽” → 适合低对齐模型（如 Qwen3-8B）。
- 表明可通过 `μ` 调整策略适配不同模型。

#### （3）预算 `N` 可扩展性
- 在 `N=32` 到 `96` 范围内，TALON 始终优于 EAGLE-3。
- 尤其在资源受限（`N=32`）时优势更明显，说明其能更高效地优先分配预算给高价值路径。

---

## 4. 关键结论和发现

### 主要发现
1. **静态树结构存在系统性低效**：“Acceptance Funnel”现象表明深层节点接受集中在 top-1/2，宽扩展无效；“Static Depth Dilemma”显示最优生成长度随任务波动剧烈。
2. **TALON 实现了真正的动态适应**：通过 budget-driven + confidence-gated 扩展，自动形成“深窄”或“浅宽”结构，显著提升 draft efficiency。
3. **理论与实践一致**：TALON 动态调节 draft cost `δ` 以匹配实际收益 `T`，接近理想 Oracle 曲线，而 EAGLE 因固定成本在困难任务中性能骤降。
4. **通用性强**：在 greedy (`T=0`) 与 stochastic (`T=1`) 解码下均保持领先，适用于多种模型与任务。

### 方法的局限性
1. **批处理扩展性有限**：当前评估集中于 `batch=1` 场景，大规模 batch 下维护多个动态树结构可能导致内存与调度开销上升。
2. **超参数泛化性**：虽然 `μ=0.03`, `N=60` 在多数情况下有效，但在特定领域可能需手动调参。
3. **未解决根本模型对齐问题**：TALON 是 inference-time 优化，不改善 draft model 与 target model 的分布对齐。

### 未来工作方向
- 开发 **auto-tuning 机制**，根据实时 acceptance history 自动调整 `μ` 和 `N`。
- 探索 **large-batch serving 下的高效实现方案**，如共享树结构或近似剪枝。
- 结合 **training-time alignment 技术**（如 HASS、CORAL），进一步提升 draft 质量。

---

> **总结**：TALON 提出了一种简洁而强大的训练免费框架，将 speculative decoding 从刚性的几何约束转向灵活的预算驱动范式。其实验充分验证了自适应树结构在提升 LLM 推理效率方面的巨大潜力，是 tree-based SD 领域的重要进展。

</details>

---

### 4. [MoE-DisCo:Low Economy Cost Training Mixture-of-Experts Models](https://arxiv.org/abs/2601.06857)

**Authors**: Xin Ye, Daning Cheng, Boyang Zhang, Yunquan Zhang  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.06857v1  

#### Abstract
Training large-scale Mixture-of-Experts (MoE) models typically requires high-memory, high-bandwidth GPUs (e.g., A100), and their high cost has become a major barrier to large-model training. In contrast, affordable hardware is low-cost but constrained by memory capacity and bandwidth, making it unsu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-DisCo: Low Economy Cost Training Mixture-of-Experts Models — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大规模 **Mixture-of-Experts (MoE)** 模型的训练严重依赖高内存、高带宽的昂贵硬件（如 NVIDIA A100），导致训练成本极高，成为大模型普及的主要障碍。同时，低成本设备（如消费级 GPU）受限于显存容量和带宽，难以直接用于 MoE 模型训练。

### 🚀 提出的新方法：MoE-DisCo
作者提出 **MoE-DisCo**（Mixture-of-Experts with Disentangled Clustering and Coordination），一种**分阶段、硬件感知的 MoE 训练框架**，其核心思想是将 MoE 模型解耦为多个独立子模型并行训练，最后再融合微调。

#### 主要创新点：
- **模型解耦（Model Decoupling）**  
  将完整的 MoE 模型分解为 `K` 个独立的**密集子模型**（dense submodels），每个子模型包含共享的 Transformer 骨干网络 + 单个专家模块。
- **数据解耦（Data Decoupling）**  
  使用 **K-means 聚类**对训练数据进行无监督语义聚类，生成 `K` 个语义上差异化的子数据集，并与各专家建立“解耦对齐”关系。
- **并行独立训练（Independent Parallel Training）**  
  每个子模型在分配的数据子集上独立训练，无需跨设备通信，可在低功耗、低成本设备（如 RTX 4090）上高效运行。
- **轻量全局微调（Lightweight Global Fine-tune）**  
  所有专家训练完成后，集成成完整 MoE 架构，在全量数据上进行短时间的全局微调（fine-tune），恢复门控机制协调能力，此阶段仅需少量高成本 GPU 时间。

### 🔍 相比现有方法的优势
| 维度 | 传统 MoE 全参数训练 | MoE-DisCo |
|------|---------------------|-----------|
| 硬件需求 | 全程依赖 A100/H800 等高端 GPU | 大部分训练在 RTX 4090 等低成本 GPU 上完成 |
| 通信开销 | 多 GPU 同步通信频繁，存在通信瓶颈 | 子模型完全独立，零通信开销 |
| 内存占用 | 需加载全部专家参数 | 只需单个专家 + 共享骨干，显著降低显存压力 |
| 总体成本 | 高昂（全程使用高价 GPU） | 成本下降 **47.6% ~ 69.5%** |
| 性能表现 | 基准性能 | 匹配甚至优于全参数训练 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **C4**: 大规模英文语料，经过严格清洗过滤
- **WikiText-2**: 来自维基百科的高质量英文文本，常用于语言模型评测
- **OpenWebText**: 开源网页文本集合，模仿 GPT-2 的训练数据构建

### ⚙️ 实验设置
- **模型架构**：
  - **Qwen1.5-MoE-2.7B**：激活约 2.7B 参数，等效性能接近 7B 密集模型（如 Mistral-7B）
  - **LLaMA-MoE-3.5B**：基于 LLaMA 架构的 MoE 版本
  - 两个模型均设置 **4 个专家（K=4）**

- **训练平台与成本估算**：
  - **RTX 4090**：$0.35 / GPU·小时（低成本设备代表）
  - **A100 (80GB)**：$2.28 / GPU·小时（高成本设备代表）

- **评估指标**：
  1. **语言建模性能**：训练损失（loss）、困惑度（PPL）
  2. **下游任务性能**：ARC-e（5-shot）、MMLU（5-shot）、HellaSwag（zero-shot）、PIQA（zero-shot）
  3. **训练效率**：达到目标 loss 所需的训练步数
  4. **经济成本**：总训练费用（美元）、高成本 GPU 使用时间

### 🔁 基线方法对比
- **Full-Parameter Training**：标准的端到端 MoE 全参数训练，全程在 A100 上进行，作为性能与成本基准。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 和 Figure 4–5）

| 模型 | 数据集 | Full-Param 步数 | MoE-DisCo 步数 | 减少比例 |
|------|--------|------------------|----------------|----------|
| Qwen | C4 | 21,150 | 4,100 | ↓ **80.6%** |
| Qwen | WikiText-2 | 12,500 | 3,150 | ↓ **74.8%** |
| Qwen | OpenWebText | 28,600 | 6,650 | ↓ **76.7%** |
| Llama | C4 | 11,750 | 4,150 | ↓ **64.7%** |
| Llama | WikiText-2 | 15,000 | 5,300 | ↓ **64.7%** |
| Llama | OpenWebText | 21,000 | 7,800 | ↓ **62.9%** |

> ✅ MoE-DisCo 在相同 loss 下所需训练步数仅为传统的 **1/4 左右**，收敛速度大幅提升。

#### 困惑度（PPL）对比（示例）：
- Qwen + C4：Full-Param → 230.32 vs MoE-DisCo → **165.86**（↓ 显著改善）
- Llama + OpenWebText：Full-Param → 114.93 vs MoE-DisCo → **106.82**

> 💡 表明 MoE-DisCo 不仅更快，且最终语言建模质量更优。

### 📈 下游任务性能（Table 2）

| 方法 | ARC-e(5) | MMLU(%) | HellaSwag(%) | PIQA(%) |
|------|---------|--------|-------------|--------|
| Full-Param | 27.9 | 23.0 | 27.45 | 52.25 |
| **MoE-DisCo** | **29.1** | **25.3** | **29.45** | **57.03** |

> ✅ MoE-DisCo 在所有下游任务中均**全面超越**全参数训练，尤其在零样本任务（PIQA）提升明显（↑ 4.78%）。

### 💰 经济成本分析（Table 3）

| 模型 | 数据集 | Full-Param 成本 ($) | MoE-DisCo 成本 ($) | 成本降幅 |
|------|--------|-----------------------|--------------------|----------|
| Qwen | C4 | 22.50 | 6.87 | ↓ **69.5%** |
| Qwen | WikiText-2 | 6.93 | 3.34 | ↓ **51.8%** |
| Qwen | OpenWebText | 29.91 | 10.91 | ↓ **63.5%** |
| Llama | C4 | 12.86 | 6.74 | ↓ **47.6%** |
| Llama | WikiText-2 | 16.99 | 8.13 | ↓ **52.2%** |
| Llama | OpenWebText | 32.13 | 15.14 | ↓ **52.9%** |

> 💸 最高节省近 **70%** 的训练成本，且训练时间也大幅缩短（例如 Qwen on C4：从 9.87h → 3.82h）。

### 🔍 消融实验结果

#### （1）聚类策略消融（Figure 6）
- 将 K-means 替换为**随机数据划分**后，MoE-DisCo 的性能退化至与 Full-Parameter 训练相当。
- **结论**：K-means 聚类通过增强专家间的语义分工，提升了模型多样性与收敛效率，是 MoE-DisCo 成功的关键组件。

#### （2）专家数量影响（Figure 7）
- 在 Llama 模型上测试 2 专家 vs 4 专家配置：
  - 4-expert 模型在 MoE-DisCo 框架下仍能快速收敛，且最终 loss 和 PPL 更低。
- **结论**：MoE-DisCo 对不同专家数量具有良好的扩展性和稳定性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE-DisCo 能显著降低 MoE 模型训练成本**（最高降本 69.5%），同时保持甚至提升模型性能。
2. **通过“解耦训练 + 轻量融合”的两阶段设计**，可将大部分计算负载迁移至低成本硬件，极大缓解对 A100 类高端 GPU 的依赖。
3. **语义聚类驱动的数据划分机制** 是实现专家专业化分工的核心，确保各子模型学习互补特征。
4. **最终的短时 fine-tune 阶段足以恢复门控协调行为**，无需长时间端到端训练即可获得高性能 MoE 模型。

### ⚠️ 局限性
- 当前实验未验证在更大规模模型（如 >10B 参数）上的有效性。
- 聚类依赖预训练 embedding，可能引入偏差。
- 专家数量固定，缺乏动态调整机制。

### 🔮 未来工作方向
- 扩展至超大规模 MoE 模型（百亿/千亿参数）
- 引入更多数据划分策略（如 topic modeling、domain-adaptive clustering）
- 探索动态专家数量调整或路由优化策略
- 结合系统层面优化（如通信压缩、异构调度）进一步提升效率

---

## ✅ 总结
**MoE-DisCo** 是一种面向资源受限场景的高效 MoE 训练新范式。它通过 **模型解耦 + 数据聚类 + 并行训练 + 轻量微调** 的四阶段流程，成功实现了在低成本硬件上训练高质量 MoE 模型的目标。实验证明其不仅大幅降低成本（↓47.6%~69.5%），而且在收敛速度、语言建模质量和下游任务表现上均优于传统全参数训练，为学术界和工业界提供了极具实用价值的大模型训练解决方案。

> 🔗 代码地址：[https://anonymous.4open.science/r/MoE-DisCo-4835/](https://anonymous.4open.science/r/MoE-DisCo-4835/)

</details>

---

### 5. [Free-RBF-KAN: Kolmogorov-Arnold Networks with Adaptive Radial Basis Functions for Efficient Function Learning](https://arxiv.org/abs/2601.07760)

**Authors**: Shao-Ting Chiu, Siu Wun Cheung, Ulisses Braga-Neto, Chak Shing Lee, Rui Peng Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.07760v1  

#### Abstract
Kolmogorov-Arnold Networks (KANs) have shown strong potential for efficiently approximating complex nonlinear functions. However, the original KAN formulation relies on B-spline basis functions, which incur substantial computational overhead due to De Boor's algorithm. To address this limitation, re...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Free-RBF-KAN: Kolmogorov-Arnold Networks with Adaptive Radial Basis Functions for Efficient Function Learning**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
原始的 **Kolmogorov-Arnold Network (KAN)** 虽然在函数逼近方面表现出色，但其依赖于 **B-spline** 基函数，需通过 **De Boor 算法**进行计算，并且训练过程中需要动态调整域范围，导致显著的**计算开销**。此外，标准的 **RBF-KAN** 虽然提升了效率，但在精度上通常不如原始 KAN。

本文旨在解决以下问题：
- 如何在保持高逼近精度的同时，降低 KAN 的计算复杂度？
- 如何提升 RBF-KAN 的表达能力以弥补其相对于 B-spline KAN 的性能差距？

---

### **提出的新方法与创新思路**
作者提出了 **Free-RBF-KAN**，一种基于自适应径向基函数（RBF）的新型 KAN 架构，核心创新如下：

#### ✅ **1. 自由可学习的 RBF 形状参数**
- 引入**可训练的网格点（centroids）** 和 **平滑度参数（smoothness factors）**，使 RBF 的形状在训练中动态调整。
- 通过 `tanh` 映射约束网格点在预设区间内，确保梯度优化稳定。

#### ✅ **2. 自适应平滑机制**
- 将 RBF 的平滑度（如高斯核中的 $\sigma$）作为网络参数联合优化，而非固定值。
- 使用 `exp()` 映射保证平滑度始终为正。

#### ✅ **3. 统一的通用逼近理论证明**
- 首次为 **RBF-KAN 家族**提供了形式化的**通用逼近定理（universal approximation theorem）**。
- 证明了 RBF-KAN 不仅是计算优化手段，更是具有强大表达能力的函数逼近框架。

#### ✅ **4. 多任务高效架构设计**
- 在物理信息机器学习（Physics-Informed ML）、算子学习等任务中验证其有效性。
- 可无缝集成到 DeepONet 中作为 trunk network，提升算子学习性能。

---

### **相比现有方法的优势**
| 方法 | 缺点 | Free-RBF-KAN 的优势 |
|------|------|---------------------|
| **B-spline KAN** | 计算昂贵（De Boor 迭代），无法灵活调整平滑度 | 更快训练/推理，支持动态网格和平滑度 |
| **Standard RBF-KAN** | 固定中心和平滑度，表达能力受限 | 动态对齐激活模式，显著提升精度 |
| **MLP / PINN** | 存在谱偏差（spectral bias），难以捕捉高频特征 | 无谱偏差，适合多尺度建模 |
| **FreeKnots-KAN** | B-spline 网格必须有序，优化复杂 | RBF 无需排序，自由移动更灵活 |

> 💡 **核心优势总结**：  
> **Free-RBF-KAN 实现了精度与效率的平衡** —— 在逼近能力上媲美甚至超越原始 KAN，在训练速度上远超 B-spline KAN，同时保留了 RBF 的灵活性和可微性。

---

## **2. 核心实验方法和设置**

### **使用的数据集与任务类型**
实验覆盖多个领域，体现方法的广泛适用性：

| 任务类别 | 数据集/问题描述 |
|--------|----------------|
| **函数逼近** | 合成非光滑函数：$f(x,y)=\cos(4x) + \sin(y) + \sin(2\pi y) + |\sin(3\pi y)|$ |
| **谱偏差分析** | 多尺度函数：$f(x)=0.1\sin(50\pi x)+\sin(2\pi x)$ |
| **高维回归** | **MNIST 图像分类**（视为回归任务） |
| **物理信息学习（PINN）** | <br>• 2D 热传导方程（含高频源项）<br>• 2D Helmholtz 方程（正弦源） |
| **算子学习（DeepONet）** | 1D 反应扩散方程的解算子学习：<br>$u_t = D u_{xx} + k u + f(x)$ |

---

### **实验设置与评估指标**

| 设置项 | 描述 |
|-------|------|
| **模型结构** | 所有 KAN 类模型统一层数与节点数，公平比较（如 `[2,5,1]`, `[28×28,64,10]`） |
| **RBF 类型** | 主要使用 **Gaussian RBF**，部分使用 Matérn 核 |
| **优化器** | Adam 或 LBFGS，学习率 $10^{-3}$ 或 1.0 |
| **评估指标** | <br>• **Test MSE / L² Loss**（函数逼近）<br>• **Relative L2 Error**（算子学习）<br>• **Training Time (sec)**<br>• **NTK Eigenvalue Spectrum**（谱偏差分析） |

---

### **基线方法对比**
| 基线模型 | 简要说明 |
|--------|---------|
| **MLP** | 标准全连接神经网络，Tanh/SiLU 激活 |
| **KAN** | 原始 B-spline KAN（三次样条） |
| **FreeKnots-KAN** | 支持自由节点的 B-spline KAN |
| **RBF-KAN** | 固定中心和平滑度的 RBF 版本 |
| **PINN** | Physics-Informed Neural Network 基线 |
| **DeepONet** | 分支-主干结构的标准算子学习模型 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

#### 🔹 **表1：非光滑二维函数逼近（Table 2）**
| Model | #Param | Test MSE |
|-------|--------|----------|
| MLP | 261 | 5.26e-1 |
| KAN | 195 | 3.96e-3 |
| FreeKnots-KAN | 307 | 2.57e-4 |
| RBF-KAN | 120 | 6.05e-4 |
| **Free-RBF-KAN** | **290** | **2.39e-4** ✅ |

> ➤ Free-RBF-KAN 达到最低误差，优于 FreeKnots-KAN，且参数少于后者。

---

#### 🔹 **表2：MNIST 高维回归（Table 3）**
| Model | #Param | Test Loss | Training Time (s) |
|-------|--------|-----------|-------------------|
| MLP | 509,410 | **6.70e-2** | **81.58** |
| KAN | 762,240 | 1.17e-1 | 97.95 |
| RBF-KAN | 508,160 | 2.02e-1 | 82.27 |
| **Free-RBF-KAN** | **525,120** | **8.79e-2** | **85.81** |

> ➤ 尽管 MLP 最优，但 Free-RBF-KAN 显著优于其他 KAN 变体，接近 MLP 表现。

---

#### 🔹 **表3：2D 热传导 PDE（Table 4）**
| Model | #Param | L²-loss | Training Time (s) |
|-------|--------|--------|-------------------|
| MLP | 5,081 | 1.60e-1 | 1160 |
| KAN | 1,400 | 6.52e-3 | 267 |
| RBF-KAN | 1,280 | 2.78e-3 | 124 |
| **Free-RBF-KAN** | **2,000** | **2.41e-3** ✅ | **138** |

> ➤ Free-RBF-KAN 精度最高，训练时间仅为 KAN 的 **~50%**。

---

#### 🔹 **表4：2D Helmholtz PDE（Table 5）**
| Model | #Param | L²-loss | Training Time (s) |
|-------|--------|--------|-------------------|
| MLP | 50,049 | 4.15e-2 | 39 |
| KAN | 600 | 1.58 | 153 ❌ |
| RBF-KAN | 400 | 3.67e-1 | 49 |
| **Free-RBF-KAN** | **640** | **3.35e-2** ✅ | **62** |

> ➤ KAN 完全失败；Free-RBF-KAN 精度超过 MLP，训练时间可控。

---

#### 🔹 **表5：DeepONet 算子学习（Table 6）**
| Model (Trunk) | #Param | Rel. L2 Error | Training Time (s) |
|---------------|--------|----------------|--------------------|
| MLP | 18,921 | 2.08e-2 | 78 |
| KAN | 11,945 | 6.15e-2 | 96 |
| RBF-KAN | 10,625 | 3.70e-2 | 84 |
| **Free-RBF-KAN** | **11,185** | **1.94e-2** ✅ | **88** |

> ➤ Free-RBF-KAN 实现**最小相对误差**，优于 MLP，且参数更少。

---

### **消融实验结果**
- **自适应网格 vs 固定网格**：移除可学习 centroid 导致误差上升约 30%-50%。
- **自适应平滑度**：固定 $\sigma$ 会限制模型对局部复杂性的响应能力。
- **NTK 分析显示**：Free-RBF-KAN 的 NTK 特征值衰减更慢，表明其具备更强的多频率表示能力，**无谱偏差**。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **Free-RBF-KAN 在多种任务中一致优于原始 KAN 和标准 RBF-KAN**，尤其在物理驱动的多尺度问题中表现突出。
2. ✅ **训练效率显著提升**：相比 B-spline KAN，训练时间减少 **30%-70%**，得益于避免 De Boor 迭代。
3. ✅ **具备理论保障**：首次建立 RBF-KAN 的通用逼近定理，证明其不是近似替代品，而是独立强大的函数逼近框架。
4. ✅ **无谱偏差特性保留**：NTK 分析证实 Free-RBF-KAN 与原始 KAN 一样，能有效学习高频成分。
5. ✅ **适用于复杂科学计算场景**：在 PINN 和 DeepONet 中均取得 SOTA 性能，适合 PDE 求解与算子学习。

---

### **方法的局限性**
- ❌ 在**高度非结构化数据**（如图像分类 MNIST）上仍落后于 MLP，说明 KAN 类方法更适合结构化、物理规律明确的任务。
- ❌ 参数量虽低于原始 KAN，但仍高于轻量级 RBF-KAN，存在进一步压缩空间。
- ❌ 当前实现依赖自动微分（AD），若用于大规模部署，可能需进一步优化 RBF 求导效率。

---

### **未来工作方向**
1. **将 Free-RBF-KAN 重构为等效 MLP**：借鉴 Actor et al. [2025] 的思想，将其转换为带高斯激活的 MLP，从而利用成熟的 MLP 优化工具链。
2. **探索稀疏化与低秩近似**：降低高维输入下的 RBF 计算负担。
3. **扩展至时间序列与图结构数据**：结合 KAN 的可解释性优势，应用于时空建模。
4. **硬件加速与编译优化**：针对 RBF 运算特点设计专用推理引擎。

---

## **总结**
> 🏁 **Free-RBF-KAN 是一次成功的架构革新**：它通过引入**可学习的 RBF 网格与平滑度**，解决了传统 KAN 计算昂贵、RBF-KAN 表达不足的问题，在**精度、效率、理论完备性**三者之间取得了优异平衡。该方法特别适用于**科学机器学习、PDE 求解、算子学习**等强调多尺度建模与物理一致性的任务，是当前 KAN 发展路线中极具潜力的方向之一。

</details>

---

### 6. [Conditional Memory via Scalable Lookup: A New Axis of Sparsity for Large Language Models](https://arxiv.org/abs/2601.07372)

**Authors**: Xin Cheng, Wangding Zeng, Damai Dai, Qinyu Chen, Bingxuan Wang, Zhenda Xie, Kezhao Huang, Xingkai Yu, Zhewen Hao, Yukun Li, Han Zhang, Huishuai Zhang, Dongyan Zhao, Wenfeng Liang  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.07372v1  

#### Abstract
While Mixture-of-Experts (MoE) scales capacity via conditional computation, Transformers lack a native primitive for knowledge lookup, forcing them to inefficiently simulate retrieval through computation. To address this, we introduce conditional memory as a complementary sparsity axis, instantiated...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Conditional Memory via Scalable Lookup: A New Axis of Sparsity for Large Language Models — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Large Language Models (LLMs)** 主要依赖 **Mixture-of-Experts (MoE)** 实现条件计算（conditional computation），通过稀疏激活参数提升模型容量。然而，Transformer 架构缺乏原生的“知识查找”机制，导致模型必须用昂贵的动态计算（如 Attention 和 FFN）来模拟静态知识检索任务（如命名实体识别、固定短语匹配等）。这种做法浪费了宝贵的计算资源和网络深度。

### 提出的新方法与创新思路
本文提出 **conditional memory** 作为 MoE 条件计算之外的**第二条稀疏性轴线**，并设计了一个具体实现模块——**Engram**。

- **Engram** 是一种基于现代改进的 **N-gram embedding** 的条件记忆模块，支持 **O(1) 常数时间查找**。
- 它将局部、静态的语言模式（如 “Alexander the Great”、“Princess of Wales”）存储在大规模嵌入表中，通过输入上下文生成哈希键进行检索。
- 检索到的嵌入向量通过 **context-aware gating** 机制融合回主干网络，确保语义一致性。

#### 核心创新点：
1. **引入“条件内存”作为新的稀疏性维度**：区别于 MoE 的“条件计算”，Engram 实现的是“条件查找”，形成互补。
2. **系统级效率设计**：
   - 利用确定性哈希地址，支持运行时预取（prefetching），可将嵌入表卸载至主机内存（host memory），缓解 GPU 显存压力。
   - 支持多级缓存（Multi-Level Cache Hierarchy），利用 N-gram 的 Zipf 分布特性优化访问延迟。
3. **架构集成优化**：
   - 支持与 **multi-branch 架构**（如 mHC）结合，实现分支特异性门控。
   - 引入 **tokenizer compression**，合并语义等价的子词 token（如 `Apple` vs `_apple`），提升语义密度。

### 相比现有方法的优势
| 对比项 | 传统 MoE | Engram |
|--------|----------|--------|
| 稀疏性类型 | 条件计算（conditional computation） | 条件内存（conditional memory） |
| 静态知识处理方式 | 多层 Attention + FFN 重建 | O(1) 查找 + 融合 |
| 内存扩展性 | 受限于 GPU HBM | 可卸载至 host memory / SSD |
| 推理效率 | 动态路由带来通信开销 | 确定性寻址支持预取 |
| 表达能力 | 动态推理强 | 静态模式记忆高效 |

---

## 2. 核心实验方法和设置

### 数据集
- **预训练数据**：262B tokens 的混合语料（未明确列出，但提及使用 DeepSeek-v3 tokenizer）
- **评估基准**：
  - **知识与推理**：MMLU, MMLU-Redux, MMLU-Pro, CMMLU, C-Eval, AGIEval, ARC-Easy/Challenge, TriviaQA, PopQA, BBH
  - **阅读理解**：DROP, RACE-Middle/High, C3
  - **代码与数学**：HumanEval, MBPP, CruxEval, GSM8K, MGSM, MATH
  - **长上下文能力**：LongPPL（书籍、论文、代码、CoT）、RULER（含 NIAH、Variable Tracking 等子任务）

### 实验设置
- **模型配置**：
  - 统一 backbone：30 层 Transformer，hidden size 2560，MLA 注意力，mHC 多分支连接
  - 所有模型保持 **activated parameters ≈ 3.8B**，严格控制 FLOPs 和训练 token 数（262B）
- **对比模型**：
  - **Dense-4B**：全密集 FFN，4.1B 总参
  - **MoE-27B**：72 个路由专家，总参 26.7B
  - **Engram-27B**：55 个路由专家 + 5.7B 参数 Engram 模块，总参 26.7B（iso-parameter）
  - **Engram-40B**：同上 backbone，Engram 扩展至 18.5B 参数，总参 39.5B

### 评估指标
- **语言建模**：Pile 测试集 loss、验证集 loss
- **任务性能**：准确率（Acc.）、F1、Exact Match（EM）、Pass@1
- **长上下文**：LongPPL（困惑度↓）、RULER 各子任务准确率
- **消融实验**：验证损失（Validation Loss）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| 任务 | MoE-27B | Engram-27B | 提升 |
|------|---------|------------|------|
| **MMLU (Acc.)** | 57.4 | **60.4** | +3.0 |
| **MMLU-Pro (Acc.)** | 28.3 | **30.1** | +1.8 |
| **CMMLU (Acc.)** | 57.9 | **61.9** | +4.0 |
| **BBH (EM)** | 50.9 | **55.9** | +5.0 |
| **ARC-Challenge (Acc.)** | 70.1 | **73.8** | +3.7 |
| **DROP (F1)** | 55.7 | **59.0** | +3.3 |
| **HumanEval (Pass@1)** | 37.8 | **40.8** | +3.0 |
| **GSM8K (EM)** | 58.4 | **60.6** | +2.2 |
| **MATH (EM)** | 28.3 | **30.7** | +2.4 |

> ✅ **关键发现**：提升不仅出现在知识密集型任务（如 MMLU），更显著体现在**通用推理与数学/代码任务**。

### 长上下文表现（Table 2）
在 **RULER** 的复杂检索任务中：
- **Multi-Query NIAH**：84.2 → **97.0**（+12.8）
- **Variable Tracking**：77.0 → **89.0**（+12.0）
- 即使在仅 82% 预训练 FLOPs 下，Engram-27B 仍优于完整训练的 MoE-27B

### 消融实验结果（Figure 5）
- **最优分配比例**：约 75–80% 稀疏预算用于 MoE，其余用于 Engram（U-shaped scaling law）
- **组件重要性排序**：
  1. **multi-branch integration**（移除后性能大幅下降）
  2. **tokenizer compression**（提升语义一致性）
  3. **context-aware gating**（抑制噪声和歧义）
- **插入位置敏感性**：早期层（如 Layer 2）效果最佳，符合“尽早卸载局部重建”的假设
- **N-gram 大小**：2/3-gram 最优；4-gram 在固定预算下略差（稀释高频模式容量）

### 系统效率测试（Table 4）
- 将 **100B 参数 Engram 表卸载至 CPU 内存**，在 Dense-8B 模型上仅造成 **2.8% 吞吐下降**
- 证明其具备 **infrastructure-aware efficiency**，可突破 GPU 显存限制

---

## 4. 关键结论和发现

### 主要发现
1. **条件内存是有效的第三范式**：
   - Engram 与 MoE 形成互补：MoE 处理动态推理，Engram 处理静态模式。
   - 发现 **U-shaped scaling law**：纯 MoE 或纯 Engram 均非最优，混合分配可达最佳性能。

2. **性能增益源于架构效率提升**：
   - **释放早期层计算负担**：Engram 替代了原本需多层 FFN/Attention 完成的本地模式重建（见 Table 3）。
   - **增加有效深度**：LogitLens 和 CKA 分析显示，Engram-27B 第 5 层 ≈ MoE-27B 第 12 层的表示能力。
   - **增强注意力全局聚焦能力**：局部依赖由查找解决，注意力可专注于长程上下文建模。

3. **广泛适用性**：
   - 不仅提升知识检索任务，还在 **BBH、ARC、MATH、HumanEval** 等需要深层推理的任务上取得更大收益。
   - 在 **长上下文场景** 中表现尤为突出，显著优于 MoE 基线。

### 方法的局限性
- **静态知识边界模糊**：某些“看似静态”的表达可能具有上下文敏感含义（polysemy），依赖 gating 缓解但仍存在风险。
- **训练-推理不一致风险**：若预训练时依赖 Engram 存储关键事实，而推理时因哈希冲突或未覆盖导致检索失败，可能引发崩溃（见 Figure 6：TriviaQA 保留率仅 29%）。
- **对 tokenizer 设计敏感**：虽引入 tokenizer compression，但仍依赖良好分词策略。

### 未来工作方向
- 探索 **动态更新机制**：允许 Engram 表在部署后增量学习新知识。
- 结合 **非参数化检索**（如 RETRO、REALM）构建混合记忆系统。
- 进一步扩大 Engram 规模，探索 **TB 级嵌入表**下的训练与推理效率极限。
- 将 Engram 应用于 **多模态模型**，实现跨模态的快速模式召回。

---

> 🔚 **总结**：  
> 本文提出了 **conditional memory** 作为一种全新的稀疏性范式，并通过 **Engram** 模块实现了高效、可扩展的知识查找。实验证明，它不仅能提升知识检索能力，更能通过“解放网络深度”显著增强模型的**通用推理与长上下文理解能力**，为下一代稀疏化 LLM 提供了重要的架构思路。

</details>

---

### 7. [Physics-Informed Tree Search for High-Dimensional Computational Design](https://arxiv.org/abs/2601.06444)

**Authors**: Suvo Banik, Troy D. Loeffler, Henry Chan, Sukriti Manna, Orcun Yildiz, Tom Peterka, Subramanian Sankaranarayanan  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.06444v1  

#### Abstract
High-dimensional design spaces underpin a wide range of physics-based modeling and computational design tasks in science and engineering. These problems are commonly formulated as constrained black-box searches over rugged objective landscapes, where function evaluations are expensive, and gradients...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

**论文标题**: *Physics-Informed Tree Search for High-Dimensional Computational Design*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对科学与工程中的**高维计算设计问题**，特别是那些具有以下挑战的场景：
- **昂贵的函数评估**（如 DFT、FEM、多物理场模拟）；
- **无梯度或梯度不可靠**；
- **目标函数为非凸、多模态、崎岖的黑盒景观**；
- **存在物理约束**（如对称性、化学有效性、力学可行性）；
- **维度灾难**导致传统优化器效率低下。

这些问题在材料设计、晶体结构搜索、势能模型拟合和连续体工程设计中普遍存在。

### 提出的新方法与创新
作者提出了一种名为 **Physics-Informed Monte Carlo Tree Search (MCTS)** 的框架，将基于策略的强化学习思想扩展到连续、高维的科学优化任务中。其核心创新包括：

1. **方向性采样机制（Logistic Surrogate Sampler）**  
   引入一个基于逻辑回归的代理模型，从节点的历史搜索轨迹中学习“成功方向”，从而在高维空间中进行有偏采样，显著提升采样效率。

2. **深度依赖的窗口缩放（Depth-based Window Scaling）**  
   随着树深度增加，动态缩小采样半径 $ r_{\text{max}} $，实现从全局探索到局部精细优化的平滑过渡。

3. **分层树批处理（Hierarchical Batching of Trees）**  
   - **全局批次（Global Batch）**：多个独立初始化的 MCTS 树并行运行，确保多样化探索；
   - **局部批次（Local Batch）**：从全局阶段选出的优秀候选解出发，启动更深层次的局部优化树，增强收敛能力。

4. **物理信息集成**  
   在搜索过程中显式嵌入物理约束模块（如晶格对称性、原子间距限制），确保所有生成的设计方案均满足物理合理性。

### 相比现有方法的优势
- **优于传统全局优化器**：在高维、多模态问题上表现优于 PSO、WOA、GA 等元启发式算法；
- **超越贝叶斯优化（BO）**：无需构建高成本的高维代理模型，在评估次数更少的情况下达到更高精度；
- **克服标准 MCTS 的局限**：解决了经典 MCTS 在连续空间中因固定动作空间而导致的低效问题；
- **可解释性强**：决策过程通过树结构可视化，便于分析搜索路径和物理规律挖掘。

---

## 2. 核心实验方法和设置

### 数据集与测试问题
实验涵盖三大类真实世界科学设计任务及一组标准基准函数：

#### （1）基准测试函数（Benchmark Functions）
共使用 23 个标准测试函数，分为三类：
- **单峰函数（Unimodal, F1–F7）**：如 Sphere、Rosenbrock，用于评估收敛速度；
- **多峰函数（Multimodal, F8–F13）**：如 Rastrigin、Ackley、Schwefel，测试逃逸局部最优的能力；
- **固定维度复合函数（Fixed-Dimensional, F14–F23）**：如 Shekel、Six-Hump Camelback，检验精细搜索能力。

#### （2）实际科学应用案例
- **晶体结构优化（Crystal Structure Design, CSD）**
  - Au₃₅ 纳米团簇（105D）
  - 二维硅烯（Silicene）不同相态（9–24D）
  - 体相硅（Bulk Si, 30D）
- **原子间势能模型拟合（Interatomic Potential Model, IAP）**
  - 使用 Tersoff 势拟合 Al 纳米团簇的 DFT 能量与力数据（13D 参数空间）
- **连续尺度工程设计**
  - 焊接梁优化（Welded Beam, 4D）
  - 压力容器设计（Pressure Vessel, 4D）

### 实验设置与评估指标
- 所有实验进行 **30 次独立运行**以评估鲁棒性和方差；
- 主要评估指标：
  - **最优目标值（Best Objective Value）**
  - **平均值与标准差（Mean ± Std）**
  - **收敛曲线（Objective vs. Evaluations）**
  - **参数误差（Lattice, Coordinates, Angles）**
  - **能量/力预测误差（MAE, RMSE）**

### 基线方法对比
- **Metaheuristics**: PSO、WOA、GA、GSA
- **其他 MCTS 变体**：Hypersphere MCTS（等向采样）、Random Search
- 文献报道的经典结果（如 Deb’s GA、Lee & Geem’s HS）

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（见 Table 1）

| 函数类别 | 示例函数 | MCTS 最优结果 | 最佳基线方法 | 结果对比 |
|--------|--------|---------------|--------------|----------|
| Unimodal | F1 (Sphere) | **0.00000E+00** | Hypersphere MCTS (1.72E-22) | ✅ 显著更优 |
| Unimodal | F4 (Max) | **3.65E-14** | WOA (0.0726) | ✅ 远超 |
| Multimodal | F8 (Schwefel) | **-7.98E+03** | Hypersphere (-7.62E+03) | ✅ 更深极小值 |
| Multimodal | F11 (Griewank) | **7.68E+00** | WOA (7.40) | ✅ 更稳定 |
| Fixed-Dim | F14 (Shekel) | **0.99800** | WOA (0.9994) | ✅ 接近最优 |
| Fixed-Dim | F16 (Camelback) | **-1.03163** | WOA (-1.03163) | ✅ 达到理论最优 |

> 注：加粗表示在该函数上取得最低平均值。

### 与基线方法的对比结果
- 在 **21 out of 23** 基准函数上，MCTS 表现优于或等于所有基线；
- 在 **F1 Sphere** 上达到数值零（远超 Hypersphere MCTS 的 10⁻²²）；
- 在 **F8 Schwefel** 上找到更深的能量盆地（-7981 vs -7620），显示更强的全局探索能力；
- 在 **F14–F23 固定维度函数**中，多次达到理论全局最优，且方差极小；
- 在焊接梁和压力容器问题中，分别获得 **1.697958** 和 **5898.135917** 的最优成本，优于文献所有已知解。

### 消融实验结果（隐含于方法组件分析）
尽管未明确列出消融表，但从方法描述和图示可推断各组件作用：
- **Logistic Surrogate**：相比各向同性 Hypersphere 采样，在 F1/F8 上显著加快收敛；
- **Window Scaling**：使早期广域探索与后期精细调优无缝衔接（见 Figure 3a-c）；
- **Hierarchical Batching**：有效避免早熟收敛，降低跨运行方差，提升鲁棒性；
- **Population of Trees**：允许多区域同步探索，提高发现新可行解的概率（见 Figure 6c/f）。

---

## 4. 关键结论和发现

### 主要发现
1. **MCTS 可成功推广至高维连续空间**：通过引入方向学习、窗口缩放和群体树机制，克服了传统 MCTS 在连续域的瓶颈。
2. **物理引导显著提升效率**：结合物理约束和局部优化器，使得搜索始终聚焦于有意义的区域，减少无效评估。
3. **通用性强**：同一框架适用于从原子级（CSD, IAP）到宏观工程（Welded Beam, Pressure Vessel）的不同尺度设计任务。
4. **高效且稳健**：在有限评估预算下（如 <15,000 次），仍能稳定收敛至高质量解，尤其适合昂贵仿真场景。

### 方法的局限性
- **完全黑箱设定**：未利用任何多保真度信息或多尺度建模优势；
- **无跨任务知识迁移**：每次运行独立训练，无法共享历史经验；
- **约束处理依赖惩罚项**：尚未集成符号化约束生成或可行性先验模型；
- **计算开销较高**：维护大量树结构和 surrogate 模型带来额外内存负担。

### 未来工作方向
1. **Foundation-guided Search**：引入预训练物理编码器作为探索先验；
2. **Multi-Agent Tree Ensembles**：支持树间记忆共享与知识迁移；
3. **Multi-fidelity Integration**：融合低精度快速评估与高精度验证；
4. **闭环自主实验系统**：将本框架集成至机器人实验平台，实现 AI-driven 自主发现。

---

## 总结
本文提出的 **Physics-Informed MCTS** 是一种面向高维科学设计的强大优化范式。它不仅在数学基准上全面超越主流元启发式算法，还在晶体结构预测、势能拟合和工程设计等复杂物理问题中展现出卓越性能。该方法标志着从“预测模型”向“决策引擎”的转变，为构建下一代 AI-native 科学设计基础设施提供了坚实基础。

</details>

---

### 8. [Segmental Advantage Estimation: Enhancing PPO for Long-Context LLM Training](https://arxiv.org/abs/2601.07320)

**Authors**: Xue Gong, Qi Yi, Ziyuan Nan, Guanhua Huang, Kejiao Li, Yuhao Jiang, Ruibin Xiong, Zenan Xu, Jiaming Guo, Shaohui Peng, Bo Zhou  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.07320v1  

#### Abstract
Training Large Language Models (LLMs) for reasoning tasks is increasingly driven by Reinforcement Learning with Verifiable Rewards (RLVR), where Proximal Policy Optimization (PPO) provides a principled framework for stable policy updates. However, the practical application of PPO is hindered by unre...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Segmental Advantage Estimation: Enhancing PPO for Long-Context LLM Training*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在**Reinforcement Learning with Verifiable Rewards (RLVR)** 范式下训练大语言模型（LLM）进行长上下文推理任务时，传统的 **Proximal Policy Optimization (PPO)** 面临一个关键挑战：**不可靠的优势估计（advantage estimation）**。

具体而言，由于 RLVR 中奖励信号稀疏（仅在最终输出给出二元反馈），导致价值函数（value function）对中间 token 的预测不准确。而标准的 **Generalized Advantage Estimation (GAE)** 在每个 token 上进行 bootstrapping 并聚合多步优势，这种细粒度的 token-level 估计会放大由错误 value prediction 引入的偏差，从而损害策略更新的质量，尤其在长序列推理中表现更差。

### 提出的新方法：Segmental Advantage Estimation (SAE)
作者提出 **Segmental Advantage Estimation (SAE)**，一种新型的优势估计框架，旨在缓解 GAE 在 RLVR 场景下的高偏差问题。

#### 核心思想：
- **放弃 token-level 的密集 bootstrapping**，转而采用 **segment-level 的稀疏优势估计**。
- 将生成的序列划分为语义连贯的子段（segments），仅在这些“语义边界”处计算并聚合优势值。
- 利用 **低概率 token** 作为启发式分割边界（low-probability tokens indicate high surprise → 可能是推理步骤的转折点）。

#### 方法流程：
1. **基于概率的分段（Probability-based Segmentation）**  
   定义分割函数 $ f_s(t) = 1 $ 当且仅当当前 token 的生成概率低于阈值 $ p $，即 $ P_{\text{model}}(s_t | s_{<t}) < p $。
2. **选择性优势集成（Selective Advantage Integration）**  
   仅在分段边界（及序列末尾）计算 n-step 优势，并通过类似 GAE 的加权方式组合成最终优势估计。
3. **递归形式实现高效计算**  
   推导出与 GAE 类似的递归公式，其中折扣因子 $ \lambda $ 动态调整：在段内无衰减，在跨段时应用指数衰减。

### 相比现有方法的优势
| 方法 | 局限性 | SAE 的改进 |
|------|--------|-----------|
| **GRPO** | 无需 value model，缺乏细粒度信用分配 | 保留 value model 提供的精细信号 |
| **PPO (λ=1)** | 使用 Monte Carlo 回报，方差大、训练不稳定 | 减少噪声估计点，降低方差与偏差 |
| **PPO (adaptive λ)** | 仍为 token-level bootstrapping，继承 GAE 偏差 | 从根本上减少 bootstrapping 次数，控制偏差来源 |

> ✅ **SAE 的核心优势在于：通过语义感知的稀疏化优势估计，显著降低估计偏差，同时保持计算效率和稳定性。**

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：DAPO-Math-17k（17,000 个高质量数学题）
- **测试数据（out-of-distribution）**：
  - AIME'24, AIME'25（美国数学邀请赛）
  - AMC（美国数学竞赛）
  - BeyondAIME（更具挑战性的推理任务集合）

### 模型与实现细节
- **主干模型**：Qwen3 系列（4B / 8B / 14B）
- **训练配置**：
  - 每步采样 4096 条 rollout（来自 512 prompts，每 prompt 8 条）
  - 最大响应长度：8192 tokens
  - Actor 学习率：1e-6，Value model 学习率：1e-5
  - 不使用 KL 正则或熵损失
  - SAE 分割阈值 $ p = 0.2 $（统一设置，未调参）

### 评估指标
- **主要指标**：零样本准确率（zero-shot accuracy）在四个 OOD 测试集上的平均得分
- **辅助分析指标**：
  - 训练稳定性（loss / entropy 曲线）
  - 样本效率（early-stage performance）
  - 与近似真实优势（Monte Carlo 估计）的相关性（Pearson correlation）

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **GRPO*** | 无 value model，group-level 优势估计 |
| **PPO (λ=1)** | GAE 中 λ=1，等价于 MC 回报 |
| **PPO (adaptive λ)** | VAPO 方法，λ 随生成长度自适应调整 |

> *注：GRPO 因训练不稳定，在 400 步后停止评估。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table 1）
所有方法均在 **Qwen3-8B-base** 上训练，评估平均分数如下：

| 方法 | AIME'24 | AIME'25 | AMC | BeyondAIME | **Average** |
|------|---------|---------|-----|------------|-------------|
| GRPO* | 35.42 | 27.50 | 74.10 | 15.31 | 38.08 |
| PPO (λ=1) | 34.79 | 29.17 | 74.85 | 16.25 | 38.76 |
| PPO (adaptive λ) | 35.42 | 25.42 | 78.39 | 16.33 | 38.89 |
| **SAE (ours)** | **38.54** | **30.21** | **77.56** | **17.62** | **40.98** |

✅ **SAE 以 40.98% 的平均分领先最强基线（PPO adaptive λ）2.09 个百分点**。

### 与基线方法的对比结果
- **性能提升全面**：在所有四个测试集上均优于或持平于最佳基线。
- **训练更稳定**：
  - GRPO 在 ~400 步后出现性能崩溃；
  - 所有 PPO 变体（含 SAE）均稳定收敛。
- **样本效率更高**：从早期训练阶段起，SAE 即表现出更快的性能增长（见 Figure 2）。
- **跨模型规模一致有效**：
  - 在 Qwen3-4B / 8B / 14B 上均持续超越 GRPO 和 PPO(λ=1)（Figure 3）。
- **跨领域泛化能力强**：
  - 在 CODE（APPS, Codeforces 等）和 STEM（GPQA-Diamond）领域也显著优于基线（Figure 4）。

### 消融实验结果

#### （1）与近似真实优势的相关性（Figure 5）
- 构造基于多次 rollout 的 Monte Carlo 估计作为近似 ground truth $ A^* $
- **SAE 的优势估计与 $ A^* $ 的 Pearson 相关系数最高**，表明其估计更接近真实优势。
- 即使最优 λ 下的 GAE，其相关性也始终低于 SAE。

#### （2）对分割阈值 $ p $ 的鲁棒性（Figure 6a）
- 测试 $ p \in \{0.05, 0.2, 0.5, 0.9\} $
- 所有设置下 SAE 均优于基线，说明性能增益源于方法本身而非参数微调。

#### （3）优势质量与最终性能的相关性（Figure 6b）
- 各方法的 **Corr($ \hat{A}, A^* $)** 与其最终测试得分高度正相关。
- 表明更好的优势估计直接带来更强的策略学习能力。

#### （4）不同分段策略比较（Figure 8）
| 分段方式 | 性能 |
|--------|------|
| Uniform segmentation (M=200) | 优于 PPO，但弱于 SAE |
| Newline-based segmentation (\n) | 优于 PPO，但仍不如 SAE |
| **Probability-based (SAE)** | **最佳性能** |

👉 证明 **基于模型不确定性（低概率 token）的动态分段是最有效的策略**。

---

## 4. 关键结论和发现

### 主要发现
1. **Token-level GAE 在 RLVR 中引入过多偏差**，尤其是在长序列稀疏奖励场景下。
2. **语义分段可自然捕捉推理过程的关键转折点**，适合作为优势估计的锚定点。
3. **SAE 显著提升了 PPO 在长上下文推理中的有效性**，体现在：
   - 更高的最终性能
   - 更强的训练稳定性
   - 更好的样本效率
   - 更优的优势估计精度（与 $ A^* $ 更相关）
4. **SAE 的收益具有普适性**：跨越不同模型规模（4B–14B）、任务领域（Math, Code, STEM）均成立。

### 方法的局限性
- **依赖生成序列的概率分布**：若模型整体置信度过高或过低，可能影响分段质量。
- **启发式设计**：目前使用固定阈值 $ p $，尚未实现动态自适应调整。
- **理论分析基于均匀分段假设**：实际使用的概率驱动分段更复杂，理论与实践存在一定差距。

### 未来工作方向
- 探索更高级的分段策略（如基于语义聚类、注意力模式、外部解析器）。
- 设计可学习的、端到端的分段机制。
- 动态调节分割阈值 $ p $，例如最大化 $ \text{Corr}(\hat{A}, A^*) $。
- 将 SAE 应用于其他需要长程信用分配的任务（如规划、对话、代码调试）。

---

> 🔚 **总结一句话**：  
> **SAE 通过将优势估计从“逐 token”升级为“按语义段”，有效抑制了 GAE 在 RLVR 中的偏差累积，为 PPO 在长上下文 LLM 训练中提供了更可靠、更高效的优化路径。**

</details>

---

### 9. [Efficient and Reliable Estimation of Named Entity Linking Quality: A Case Study on GutBrainIE](https://arxiv.org/abs/2601.06624)

**Authors**: Marco Martinelli, Stefano Marchesin, Gianmaria Silvello  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.06624v1  

#### Abstract
Named Entity Linking (NEL) is a core component of biomedical Information Extraction (IE) pipelines, yet assessing its quality at scale is challenging due to the high cost of expert annotations and the large size of corpora. In this paper, we present a sampling-based framework to estimate the NEL acc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient and Reliable Estimation of Named Entity Linking Quality: A Case Study on GutBrainIE

## 1. 论文的主要贡献和创新点

### 解决的问题
- **大规模 NEL 质量评估的挑战**：Named Entity Linking (NEL) 是生物医学信息抽取（IE）中的关键环节，但其质量评估面临两大难题：
  - 专家标注成本高昂；
  - 生物医学语料规模庞大，全面人工审核不可行。

### 提出的新方法与思路
- **将 NEL 准确率估计建模为带约束的优化问题**：
  - 目标是在满足目标 **Margin of Error (MoE)** 的前提下，最小化人工标注成本。
- **引入并适配 Stratified Two-stage Weighted Cluster Sampling (STWCS) 到 NEL 场景**：
  - 结合分层抽样（Stratification）与两阶段加权聚类抽样（TWCS），提升采样效率。
  - 分层依据：基于实体标签（entity label）定义5个语义连贯且大小均衡的 **strata**。
  - 聚类依据：基于归一化的 mention 表面形式（surface form）构建 cluster，减少上下文切换开销。

### 相比现有方法的优势
- **相比 Simple Random Sampling (SRS)**：
  - 显著降低上下文切换频率，从而减少认知负担和标注时间。
  - 在相同样本量下，实现约 **29% 的标注时间节省**。
- **避免循环依赖**：
  - 所有分层与聚类策略均基于 NEL 输出之外的信息（如 entity label 和 mention 文本），不依赖 NEL 自身结果，确保评估无偏。
- **可扩展性强**：
  - 框架通用，适用于其他 NEL benchmark 或 IE pipeline 的质量评估。

---

## 2. 核心实验方法和设置

### 数据集
- **GUTBRAINIE**：
  - 一个聚焦于 **gut-brain axis** 领域的大规模生物医学语料库。
  - 包含来自 PubMed 的 1,647 篇摘要。
  - 涉及 13 种实体类型（如 bacteria, drug, microbiome 等）和 17 种关系谓词。
  - 实体链接至 6 个标准生物医学词汇表（如 UMLS, CHEBI, NCIT）及自定义本体 GBIE。
  - 本文仅使用专家标注的 Platinum 与 Gold 折叠，共包含 **11,184 条 NEL triples**。

### 实验设置与评估指标
- **评估目标**：估算整个语料库级别的 NEL 准确率 $ \mu(T) $，即正确链接 triple 的比例。
- **抽样设计**：
  - 采用 **STWCS** 进行迭代抽样，直到达到预设 MoE ≤ 0.05。
  - 第一阶段：按规模概率有放回地选择 stratum（PPS-WR）。
  - 第二阶段：在选定 stratum 内按 cluster 大小无放回抽样（PPS-WOR）。
  - 第三阶段：每个 cluster 最多标注 m=5 个 triples。
- **终止条件**：当估计的 MoE ≤ 0.05 时停止抽样。
- **成本模型**：
  - 引入 **context switch** 概念：连续两个被标注 triple 属于不同 surface-form cluster 视为一次切换。
  - 时间成本模型：`time(Ts) = n * t_base + n_sw * Δt_switch`

### 基线方法对比
- **Simple Random Sampling (SRS)**：
  - 作为基准，模拟对同一组 2,749 个 triples 使用 SRS 抽样的标注时间和上下文切换次数。
  - 通过 1,000 次随机排列仿真，并进行 bootstrap 分析以获得稳健估计。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **总体准确率估计**：
  - 在手动标注 **2,749 个 triples（占总数 24.6%）** 后收敛。
  - 得到的 NEL 准确率为：  
    $$
    \hat{\mu}_{STWCS} = 0.915 \pm 0.0473 \quad (\text{MoE} = 0.047 < 0.05)
    $$
  - 对应 95% 置信区间：**[0.868, 0.963]**。

### 与基线方法的对比结果
- **标注时间对比**：
  - STWCS 实际耗时：**797 分钟（13h17m）**
  - SRS 仿真平均耗时：**1,124.57 分钟（18h44m）**
  - **节省时间：约 327.6 分钟（5h28m）**
  - **效率提升：约 29% 更快**（效率比 ~0.71）
- **上下文切换对比**：
  - STWCS 中发生 **1,050 次 context switch**
  - SRS 仿真中平均发生 **2,745 次 context switch**
  - STWCS 将 context switch 数量减少了近 **62%**

### 消融实验 / 分层分析结果
- **各 stratum 的准确率估计**（见 Table 3）：
  | Stratum | 准确率估计 | MoE |
  |---|---|---|
  | DDF | 0.883 | 0.093 |
  | Microbiome + Bacteria | 0.979 | 0.108 |
  | Human + Animal + Anatomical Location | 0.976 | 0.060 |
  | Chemical + Gene | 0.851 | 0.087 |
  | Drug + ... + Statistical Technique | 0.898 | 0.154 |

- 发现：
  - **Microbiome + Bacteria** 和 **Human + Animal + Anatomical Location** 类别的 NEL 准确率最高（>0.97），表明系统在这些类别上表现优异。
  - **Drug + Dietary Supplement + Food + Biomedical/Statistical Technique** 类别的 MoE 最大，说明需要更多采样才能精确估计其质量。
  - 所有 strata 的保守下界（p - MoE）均 ≥ 0.74，表明整体 NEL 质量可靠。

---

## 4. 关键结论和发现

### 主要发现
- **STWCS 可高效、可靠地估计大规模 NEL 系统的质量**：
  - 仅需标注不到 25% 的数据即可获得具有统计保证的准确率估计（MoE ≤ 0.05）。
- **结构化抽样显著提升标注效率**：
  - 通过减少 context switch，STWCS 相比 SRS 节省约 29% 的专家时间。
- **框架具备可解释性和指导意义**：
  - 分层估计揭示了不同实体类别的 NEL 表现差异，可用于指导后续针对性修正或资源分配。

### 方法的局限性
- **依赖高质量的 entity labeling**：
  - 当前框架假设 entity mention detection 和 typing 已完成且准确；若存在误标，可能影响 stratum 划分。
- **cluster 定义较简单**：
  - 当前 cluster 基于表面形式归一化（lowercase + whitespace removal），未考虑语义相似性或拼写变体。
- **固定 m=5 的限制**：
  - 虽然文献支持 m=5 为近似最优，但在某些高方差 cluster 中可能不足以充分估计局部准确率。

### 未来工作方向
- **引入多标注者机制**：
  - 采用多数投票（majority voting）等方式聚合多个专家判断，提高标注可靠性。
- **基于下游任务影响的 stratification 设计**：
  - 优先抽样那些参与大量 relation instances 或在 induced KG 中具有高中心性的 concepts。
- **动态调整抽样策略**：
  - 根据已观测到的误差分布，动态调整 stratum 或 cluster 的抽样权重，进一步优化收敛速度。
- **扩展至端到端 IE pipeline 评估**：
  - 将该框架应用于 Relation Extraction 或 Event Extraction 等更复杂任务的质量估计。

> ✅ **总结一句话**：本文提出了一种基于 **STWCS** 的采样框架，在 **GUTBRAINIE** 上实现了高效可靠的 NEL 质量估计——**用 24.6% 的标注量达成 MoE ≤ 0.05，并比 SRS 节省 29% 的专家时间**，为大规模生物医学 IE 系统的评估提供了实用且可推广的解决方案。

</details>

---

### 10. [SourceNet: Interpretable Sim-to-Real Inference on Variable-Geometry Sensor Arrays for Earthquake Source Inversion](https://arxiv.org/abs/2601.06320)

**Authors**: Zhe Jia, Xiaotian Zhang, Junpeng Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.06320v1  

#### Abstract
Inferring high-dimensional physical states from sparse, ad-hoc sensor arrays is a fundamental challenge across AI for Science, as they are complicated by irregular geometries and the profound Sim-to-Real gap in physical modeling. Taking earthquake source characterization as a representative challeng...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：SourceNet: Interpretable Sim-to-Real Inference on Variable-Geometry Sensor Arrays for Earthquake Source Inversion**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
该论文针对**地震震源机制反演**中的两个核心挑战：
- **几何不规则性**：真实世界中的传感器网络是稀疏、数量可变且空间分布不规则的，传统基于固定网格的模型（如CNN）难以处理。
- **Sim-to-Real Gap**：物理仿真数据与真实观测之间存在显著差异（如未建模的介质异质性、噪声、散射等），导致在合成数据上训练的模型难以泛化到真实场景。

### **提出的新方法与创新思路**
作者提出了 **SourceNet**，一个基于Transformer的端到端可解释框架，具备以下三大创新：

#### **(1) Physics-Structured Domain Randomization (PSDR)**
- 不同于传统的特征对齐或对抗性域适应方法，PSDR通过**随机化物理过程本身**来桥接仿真与现实之间的鸿沟。
- 具体包括：
  - **地球结构随机化**：从CRUST1.0中采样17种不同的1D速度模型，防止模型记忆特定传播路径。
  - **真实噪声注入**：使用实际环境噪声记录叠加到合成波形上。
  - **信号失真建模**：引入时间偏移、振幅扰动和模拟散射尾波（scattering coda）。
  - **传感器可用性随机丢弃（M）**：模拟现实中传感器故障或缺失的情况。

> ✅ **优势**：迫使模型学习对未建模异质性具有不变性的表示，提升现实迁移能力。

#### **(2) Set Transformer 架构设计**
- 将传感器阵列视为**无序集合（set）**，采用Set Transformer实现排列不变性。
- 引入**多模态编码器**：
  - **P-Wave Tower** 和 **S-Wave Tower**：分别提取P波和S波的时间域与频谱特征。
  - **Scalar Tower**：显式编码台站方位角、距离、坐标、振幅比等物理元数据，用于解耦辐射模式与衰减效应。
- 利用**Self-Attention机制**建模台站间的成对关系，捕捉波场中的相对相位与振幅信息。
- 使用**Attention Pooling**动态加权不同台站的重要性，实现事件自适应的信息聚合。

> ✅ **优势**：相比DeepSets/GNN/CNN，能更有效地利用稀疏、不规则传感器网络中的全波形信息。

#### **(3) 可解释性驱动的科学智能行为发现**
- 通过XAI分析（Grad-CAM + 注意力权重可视化），发现模型展现出类似“科学代理”的行为：
  - 自主关注P波后的**coda相位和早期散射波**，相当于隐式执行**full-waveform inversion**。
  - 注意力策略表现出系统性各向异性：优先选择东西向（E-W）台站，而抑制南北密集但冗余的观测。
  - 这一策略本质上恢复了**最优实验设计（Optimal Experimental Design, OED）** 原则，即最大化Fisher信息矩阵行列式（D-Optimality）。

> ✅ **优势**：不仅预测准确，还能从数据中自主发现物理观测的最佳策略，增强科学可信度。

---

## **2. 核心实验方法和设置**

### **数据集**
| 数据集 | 描述 |
|--------|------|
| **Synthetic Pre-training Dataset** | 生成了100,000个合成地震事件：<br>- 震源机制均匀采样于所有可能的double-couple源空间；<br>- 使用Southern California的真实台网布局与震中位置；<br>- 应用PSDR增强（速度模型变化、噪声注入、台站丢弃等）。 |
| **Real-world Fine-tuning Dataset** | 2,544个M > 3.0的真实地震事件，来自Yang et al. (2012)提供的Southern California目录。<br>- 要求至少5个台站覆盖，质量为A/B级。 |

### **实验设置**
- **两阶段训练流程**：
  1. **预训练**：在10万合成数据上训练150轮（AdamW, lr=2e-4）。
  2. **微调**：在真实数据上进行少样本微调（lr=2e-6），使用**加权随机采样器**平衡断层机制类别（避免strike-slip主导）。
- **输入形式**：每个台站为一个元组 $(w_i, s_i)$，其中：
  - $w_i \in \mathbb{R}^{C\times T}$：时频域波形；
  - $s_i$：标量元数据（方位角、距离、振幅比等）。

### **评估指标**
| 指标 | 含义 |
|------|------|
| **Moment Tensor Components MAE** | 归一化矩张量分量（Mxx, Mxy, ..., Mzz）的平均绝对误差 |
| **Magnitude MAE** | 预测矩震级 $M_w$ 的MAE |
| **Kagan Angle Error** | 预测与真实震源机制之间的角度偏差（越小越好），是衡量震源机制一致性的标准指标 |

### **基线方法对比**
- **经典目录**：Southern California官方手动解（Yang et al., 2012）
- **深度学习基线**：
  - FM-Net（CNN-based）
  - GNN-based 方法（Zhang et al., 2022）
  - TEAM（Transformer-based, 有离散位置编码）
- **Pooling-based Baseline**：DeepSets风格的全局池化架构

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
| 模型 | Kagan Angle (Mean/Median) | Magnitude MAE | Tensor MAE |
|------|----------------------------|----------------|-------------|
| **SourceNet (Full)** | **26.18° / 19.37°** | **0.11** | ~0.16–0.19 |
| 手动目录（Quality B） | ~35°不确定性上限 | — | — |
| FOCONET (Song et al., 2025) | ~20°–30°（需高质量/密集台网） | — | — |
| DiTing (Zhao et al., 2023) | ~20°–30°（Quality A子集） | — | — |

> 🔹 **结论**：SourceNet在**稀疏、不规则的真实操作数据**上达到SOTA精度，其误差已接近人工标注的不确定性下限（label noise floor），表明其可靠性媲美人类专家。

### **与基线方法的对比结果**
- 在相同测试集上，SourceNet显著优于先前DL方法（如Ross et al., 2018; Cheng et al., 2023），后者Kagan角偏差普遍超过30°。
- 即使与最新Transformer模型（如FOCONET、DiTing）相比，SourceNet在**非理想条件下仍保持竞争力**，且无需依赖密集台网或高质量筛选数据。

### **消融实验结果（Ablation Study）**
| 模型变体 | Magnitude MAE | Tensor MAE | Kagan Angle (Mean) |
|---------|----------------|------------|--------------------|
| **Full SourceNet** | **0.07** | **0.06** | **10.2°** |
| w/o Scalar Tower | 0.18 (+157%) | 0.09 | 12.5° |
| w/o Self-Attention (Pooling) | 0.10 | 0.17 (+183%) | 28.4° (+178%) |

> 🔹 **结论**：
- **Scalar Tower至关重要**：缺少几何元数据会导致震级估计严重退化，说明仅靠波形无法解耦能量与路径衰减。
- **Self-Attention不可替代**：替换为全局池化后机制估计大幅下降，验证了波动物理的**关系本质**——必须比较台站间相对响应。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Architecture Should Follow Physics**  
   对于波动物理反演任务，**Set Transformer + Self-Attention** 显著优于CNN、GNN和DeepSets，因其能够建模台站间的成对依赖关系，有效融合全波形信息（包括传统上被忽略的coda相位）。

2. **PSDR 是 Sim-to-Real 成功的关键**  
   t-SNE可视化显示，未经PSDR训练的模型在真实数据上出现流形错位（manifold misalignment），而PSDR使模型学习到跨域不变的物理算子，实现完美流形对齐。

3. **Interpretability Leads to Discovery**  
   SourceNet展现出类科学代理的行为：
   - 自主识别传感器网络的**几何信息瓶颈**（N-S密集、E-W稀疏）；
   - 学习注意力策略以优先选择正交方向的观测，**隐式实现了D-Optimal实验设计原则**；
   - 表明AI不仅能拟合函数，还能从数据中归纳出优化观测的物理策略。

4. **Amortized Inference 实现实时处理**  
   相比传统迭代反演（O(K)复杂度），SourceNet通过训练一次性学习逆算子，推理时间为常数（O(1)），适用于实时地震预警系统。

### **方法的局限性**
- 当前框架假设仿真支持空间覆盖真实物理范围，对于**out-of-distribution事件**（如多断层破裂、慢滑移事件）尚未建模。
- 依赖于Green函数仿真生成合成数据，若底层物理模型严重偏离现实，仍可能存在根本性偏差。
- 当前输出为静态震源参数，未建模断层滑移的时空演化过程。

### **未来工作方向**
- 扩展至**动态震源成像**：结合Neural Surrogate Models，直接从观测数据约束断层滑移的时间-空间演化。
- 探索**无监督或弱监督学习范式**，减少对精确震源标签的依赖。
- 推广至其他物理系统：如宇宙射电暴定位、气候异常溯源等涉及稀疏传感器阵列的科学反演问题。
- 构建**物理Foundation Model**：通过大规模物理随机化训练通用逆算子，支持多种地球物理任务的零样本迁移。

---

> 📌 **总结一句话**：  
> **SourceNet 不仅是一个高精度地震反演模型，更是一个能从数据中自主发现最优观测策略的“科学智能体”，为AI for Science提供了可解释、可泛化、可迁移的新范式。**

</details>

---

### 11. [Active Learning Strategies for Efficient Machine-Learned Interatomic Potentials Across Diverse Material Systems](https://arxiv.org/abs/2601.06916)

**Authors**: Mohammed Azeez Khan, Aaron D'Souza, Vijay Choyal  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.06916v1  

#### Abstract
Efficient discovery of new materials demands strategies to reduce the number of costly first-principles calculations required to train predictive machine learning models. We develop and validate an active learning framework that iteratively selects informative training structures for machine-learned...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Active Learning Strategies for Efficient Machine-Learned Interatomic Potentials Across Diverse Material Systems*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究旨在解决**机器学习势函数（MLIPs）训练中对高成本第一性原理计算（如DFT）依赖过重**的问题。在材料发现中，DFT计算代价高昂（每结构需 $10^1$–$10^3$ CPU小时），因此如何以最少的标注样本实现高精度模型训练成为关键挑战。

### 提出的新方法与新思路
作者提出并验证了一个**系统性的主动学习（Active Learning, AL）框架**，用于从大规模异构材料数据库（Materials Project 和 OQMD）中高效选择最具信息量的结构来训练 MLIPs。其核心创新包括：

- **统一比较四种AL策略**：首次在同一实验设置下系统性地对比了随机采样、不确定性采样（uncertainty-based）、多样性采样（diversity-based）以及混合策略（hybrid）在多个材料体系中的表现。
- **集成Query-by-Committee进行不确定性量化**：采用神经网络集成（ensemble）模型结合 Query-by-Committee（QBC）范式实现实时的epistemic uncertainty估计。
- **基于k-means聚类的多样性采样优化**：利用17维描述符空间中的k-means聚类（配合最远点细化）实现结构多样性的主动探索。
- **轻量化可复现流程设计**：整个管道可在 Google Colab 上运行（<8GB RAM，<4小时/系统），显著降低研究门槛。

### 相比现有方法的优势
- **多系统、多策略、统计严谨的基准测试**：填补了领域内缺乏跨材料系统的综合性AL比较研究的空白。
- **更高的数据效率**：相比随机采样，智能选择策略可减少5–13%的标注样本达到相同精度。
- **开源与可访问性强**：代码公开，支持全球资源受限的研究者参与MLIP开发。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 来源于两大开放材料数据库：
  - **Materials Project (MP)**：通过 `mp-api` 获取最多500个结构/系统。
  - **Open Quantum Materials Database (OQMD)**：通过 `qmpy` 客户端获取最多100个结构/系统。
- 覆盖四个代表性材料系统：
  - 元素碳（Carbon）
  - 硅（Silicon）
  - 铁（Iron）
  - 钛氧化物（Ti-O）

#### 数据预处理
- 过滤条件：原子数2–50；形成能和带隙有效；去除重复项（ΔE < 1 meV）
- 特征工程：构建17维特征向量（8维组分特征 + 9维性质特征），标准化处理
- 划分方式：80%作为训练/查询池，20%为固定测试集

| System     | Total Structures | Pool / Test Size |
|------------|------------------|------------------|
| Carbon     | 600              | 480 / 120        |
| Silicon    | 571              | 457 / 114        |
| Iron       | 532              | 426 / 106        |
| Ti-O       | 509              | 407 / 102        |

### 实验设置
- **主动学习循环**：
  - 初始化：30个已标注样本
  - 迭代次数：6轮
  - 每轮新增样本数：15个
  - 总标注样本：从30增至105
- **模型架构**：
  - 使用由5个成员组成的**前馈神经网络集成模型**（M=5）
  - 每个网络结构：输入层（17维）→ 两层隐藏层（128 ReLU）→ 输出层（1，预测形成能/原子）
  - 优化器：Adam（lr=1e-3），损失函数：MSE
- **不确定性估计**：使用集成方差（ensemble variance）作为 epistemic uncertainty 度量

### 查询策略对比
| 策略 | 描述 |
|------|------|
| **Random Sampling** | 基线，随机选取15个未标注样本 |
| **Uncertainty Sampling** | 选择集成方差最大的15个样本 |
| **Diversity Sampling** | 在17维描述符空间上执行k-means（k=15），取各簇中心最近结构 |
| **Hybrid Sampling** | 加权组合：$ \alpha U_{\text{norm}} + (1-\alpha) D_{\text{norm}} $，其中 $\alpha = 0.6$ |

### 评估指标
- 主要指标：
  - **MAE**（Mean Absolute Error，单位：eV/atom）
  - **R²**（Coefficient of Determination）
- 统计分析：
  - 每种配置运行 **5次不同随机种子**
  - 报告均值 ± 标准差
  - 使用**配对t检验**（paired t-test）判断显著性（α=0.05）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 2）

| System | Strategy | MAE (eV/atom) | R² | p-value vs Random |
|--------|----------|---------------|-----|--------------------|
| Carbon | Random | 0.262±0.012 | 0.886±0.011 | — |
|        | Diversity | **0.261±0.008** | 0.879±0.010 | 0.823 (Better) |
| Silicon | Random | 0.235±0.006 | 0.928±0.004 | — |
|         | Hybrid | 0.238±0.006 | **0.941±0.004** | 0.289 (Better) |
| Iron | Random | 0.233±0.009 | 0.803±0.015 | — |
|      | Diversity | **0.223±0.011** | 0.796±0.016 | 0.216 (Better) |
| Ti-O | Random | 0.912±0.041 | -0.407±0.089 | — |
|      | **Diversity** | **0.813±0.035** | **-0.072±0.076** | **0.008*** (Better) |

> 注：*表示统计显著（p < 0.05）

### 与基线方法的对比结果
- **Diversity Sampling 表现最优或具竞争力**：
  - 在所有系统中均达到最低或接近最低 MAE
  - 在复杂系统 Ti-O 中实现 **10.9% 的 MAE 改进**（从 0.912 → 0.813 eV/atom），且差异高度显著（p=0.008）
- **Uncertainty Sampling 效果不佳甚至更差**：
  - 在 Carbon、Fe 和 Ti-O 上表现劣于随机采样（p=0.042, 0.031, 0.156）
  - 反映出“只关注高不确定区域”可能导致采样偏差
- **Hybrid 方法未达预期**：
  - 尽管理论上有平衡优势，但在多数系统中表现平庸，在 Ti-O 上反而最差（MAE=0.982）

### 学习曲线分析（Figure 2）
- **Carbon**：Diversity早期收敛更快，最终误差最低
- **Silicon**：所有方法性能接近，反映任务简单、数据同质性强
- **Iron**：Diversity持续优于Random
- **Ti-O**：策略间差距最大，Diversity明显领先，体现其在复杂化学环境下的优越性

### 消融实验与交叉数据库验证（Figure 3）
- **Cross-Database Validation（MP ↔ OQMD）**：
  - MP → OQMD：随着标签增加，迁移性能逐步提升，说明MP数据更具泛化能力
  - OQMD → MP：迁移效果较差，揭示数据库之间存在明显 domain shift
- 表明：**训练数据来源影响模型泛化能力**，强调数据一致性的重要性

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **Diversity-based sampling 是最稳健有效的AL策略**：
   - 尤其适用于结构和组成复杂的材料系统（如 Ti-O）
   - 通过探索特征空间未覆盖区域，提升模型泛化能力
2. ⚠️ **Uncertainty-based sampling 并不总是有效**：
   - 在某些系统中反而导致性能下降，可能因过度聚焦局部高熵区域而忽略全局分布
3. 🔍 **系统复杂度决定AL收益大小**：
   - 简单系统（如Si）各策略差异小
   - 复杂系统（如Ti-O）中，智能采样带来显著增益（10.9% MAE下降）
4. 💡 **数据效率提升明显**：
   - 智能AL策略可在达到目标精度时节省 **5–13% 的DFT计算成本**
5. 🌍 **全流程具备高度可访问性**：
   - 可在 Google Colab 上完成（<8GB RAM，<4小时），推动MLIP democratization

### 方法的局限性
- **描述符表达能力有限**：
  - 当前使用的17维手工特征缺乏局部结构细节（如原子配位、键角、对称性等）
  - 未使用SOAP、symmetry functions 或 GNN 自动生成的表示
- **仅预测形成能**：
  - 未扩展至其他重要属性（如带隙、弹性模量、磁性等）
- **静态加权混合策略**：
  - Hybrid 方法使用固定权重 α=0.6，未能动态适应不同系统或训练阶段的需求

### 未来工作方向
- 🔄 **动态调整 uncertainty/diversity 权重**：根据系统复杂度或训练进展自适应调节采样偏好
- 🧱 **引入更先进的结构感知描述符**：整合 SOAP、Behler-Parrinello symmetry functions 或 E(3)-equivariant GNNs（如 NequIP）
- 📈 **多任务主动学习**：同时优化多种材料属性的联合采样策略
- 🤝 **结合生成模型**：将AL与结构生成模型结合，探索未知化学空间
- 🚀 **集成最新架构**：借鉴 Choyal et al. [31] 的工作，将AL与高性能MLIP架构深度融合，进一步压缩数据需求

---

> **总结一句话**：  
> 本研究表明，在训练机器学习势函数时，**基于多样性的主动学习策略（diversity sampling）比传统的不确定性采样更为有效且稳定**，尤其在复杂材料系统中能显著提升数据效率，且整套流程可在普通计算资源上运行，为全球研究者提供了低门槛、高复现性的MLIP开发范式。

</details>

---

### 12. [HAS-VQ: Hessian-Adaptive Sparse Vector Quantization for High-Fidelity LLM Compression](https://arxiv.org/abs/2601.06959)

**Authors**: Vladimer Khasia  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.06959v1  

#### Abstract
Post-training quantization is essential for deploying Large Language Models (LLMs) on resource- constrained devices. However, standard integer quantization (e.g., INT4) fundamentally degrades per- formance by imposing a uniform grid on the heavy-tailed distribution of weight parameters, particularly...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HAS-VQ: Hessian-Adaptive Sparse Vector Quantization for High-Fidelity LLM Compression

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **标准整数量化（如 INT4）在小规模 LLM 上性能退化严重**：由于 Transformer 权重具有重尾分布（heavy-tailed distribution），均匀的整数量化网格无法有效建模这种非高斯分布，导致量化噪声集中在敏感参数上，引发 perplexity 崩溃。
- **传统 Vector Quantization（VQ）受异常值干扰**：标准 VQ（如 K-Means）对权重中的高幅值 outlier 敏感，容易使聚类中心偏移，降低整体重建质量。

### 🚀 提出的新方法：HAS-VQ
提出了一种名为 **HAS-VQ（Hessian-Adaptive Sparse Vector Quantization）** 的后训练压缩框架，其核心思想是通过二阶敏感性分析将高敏感度的“异常值”从主体权重中解耦，并进行差异化处理。

#### 主要创新点：
1. **Hessian-Masked Decoupling（基于 Hessian 的掩码解耦）**
   - 利用对角 Hessian（近似 Fisher 信息矩阵）计算每个权重的重要性得分 $ I = |W_{\text{norm}}| \odot \sqrt{H} $
   - 根据重要性排序，识别出 top-p% 的高敏感参数作为 outliers，并将其从待量化的“主体”中屏蔽（置零），防止其影响 VQ 聚类过程。

2. **Residual Sparse Feedback（残差稀疏反馈机制）**
   - 不直接存储 outliers 的原始值，而是计算它们在量化后的重建误差（即残差）$ S_{ij} = W_{\text{norm},ij} - Q(B)_{ij} $，仅存储这些残差。
   - 在推理时通过 $ \hat{W} = Q(B) + S $ 实现对敏感维度的**精确重构**（up to FP16 precision），从而消除关键路径上的量化噪声。

3. **Robust Statistical VQ**
   - 对去除了 outliers 的密集主体（dense body）应用鲁棒的 VQ，采用稳定性采样与“死亡单元复活”机制（Dead Unit Revival）提升聚类收敛性和代码本利用率。

### 🔍 相比现有方法的优势
| 方面 | HAS-VQ | 传统 INT4 / RTN | 标准 VQ |
|------|--------|------------------|---------|
| 噪声控制 | 显式抑制高 Hessian 方向噪声 | 均匀噪声分布，易破坏优化轨迹 | 易被 outliers 拉偏 |
| 存储效率 | 动态分配比特，密度匹配 | 固定整数网格，低效编码重尾分布 | 可能因 outlier 失效 |
| 保真度 | 支持 near-lossless 压缩（~7 BPP） | 显著性能下降（PPL ↑） | 中等保真，不稳定 |
| Pareto 性能 | 在更小 BPP 下实现更低 PPL | 需更高 bit 数维持性能 | 通常不如 HAS-VQ |

---

## 2. 核心实验方法和设置

### 📊 数据集
- **校准数据（Calibration Data）**：用于估计 Hessian 和训练 VQ codebook（具体未详述，应为典型语言建模语料）
- **评估数据集**：**WikiText-2**，用于测试模型压缩后的 **Perplexity (PPL)**

### ⚙️ 实验设置
- **模型**：SmolLM2-1.7B-Instruct（1.7B 参数规模的小型 LLM）
- **压缩目标**：Post-Training Quantization（PTQ），无需微调
- **评估指标**：
  - **Perplexity (PPL)**：衡量语言建模能力保留程度
  - **Effective Bits-Per-Parameter (BPP)**：综合考虑 codebook、index、scale、sparse residual 的平均比特开销
  - **Compression Ratio (CR)**：相对于 FP16 的压缩倍数（CR = 16.0 / BPP）

### 🆚 基线方法对比
| 方法 | 类型 |
|------|------|
| FP16 | Oracle（无损基准） |
| INT4 (RTN) | 标准整数量化基线（Round-To-Nearest） |
| HAS-VQ (Mid) | 平衡配置（追求高效） |
| HAS-VQ (High) | 高保真配置（追求精度） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| Method | Configuration | PPL | BPP | Ratio (vs FP16) | Regime |
|--------|---------------|-----|-----|------------------|--------|
| FP16 | Oracle | 10.04 | 16.00 | 1.0x | Lossless |
| INT4 | RTN Baseline | 20.03 | 4.71 | 3.4x | Lossy (Degraded) |
| **HAS-VQ** | Mid (Balanced) | **14.23** | **4.23** | **3.8x** | Efficient Dominance |
| **HAS-VQ** | High (Fidelity) | **10.12** | **7.03** | **2.3x** | Near-Lossless |

### 🔬 对比结果分析
1. **Pareto Dominance over INT4**
   - 在 **更低 BPP（4.23 vs 4.71）** 下，PPL 从 20.03 降至 **14.23**，性能提升约 **29%**
   - 同时节省 **11% 存储空间**，实现“更小更快更好”

2. **High-Fidelity Compression 成果**
   - 在 7.03 BPP 下，PPL 达到 **10.12**，与 FP16 的 10.04 **统计上无显著差异**
   - 实现 **2.3× 模型压缩**，可视为“近无损压缩”（near-lossless）
   - 特别适用于带宽受限场景下的边缘部署

3. **消融实验（隐含于设计逻辑中）**
   - 虽未明确列出 ablation study 表格，但文中强调：
     - 若不使用 residual feedback，则无法保证敏感维度的精确恢复
     - 若不进行 Hessian-masked decoupling，VQ codebook 会被 outliers 扭曲
     - Stability-Bounded Sampling 和 Dead Unit Revival 提升了 codebook 利用率和稳定性

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **重尾分布不适合均匀整数量化**：小型 LLM 缺乏冗余吸收噪声，INT4 导致显著性能退化（PPL +100%）。
2. **Hessian 指导的解耦至关重要**：通过二阶敏感性分离 outliers 可显著提升 VQ 效果。
3. **残差稀疏反馈实现“精准修复”**：只在最关键的位置施加补偿，以极低成本换取大幅性能增益。
4. **存在两个优势操作点**：
   - **高效主导模式（Mid）**：优于 INT4，兼具更小体积与更高精度
   - **高保真模式（High）**：媲美 FP16 表现，适合不能牺牲准确性的场景

### ⚠️ 局限性
- **依赖 Hessian 近似**：需要在校准数据上估计对角 Hessian，增加了预处理复杂度
- **存储稀疏残差带来额外元数据开销**：虽然总量小，但需管理 index 和 sparse structure
- 当前验证集中于 **1.7B 规模模型**，更大模型（如 7B+）的效果尚待验证
- 推理端需支持 **VQ lookup + sparse add** 操作，可能增加工程实现难度

### 🔮 未来工作方向
- 扩展至 **Mixture-of-Experts (MoE)** 和 **CNN-based 架构**
- 探索 **动态 sparsity ratio selection**（根据层自适应调整 p）
- 结合 **quantization-aware fine-tuning (QAT)** 进一步压榨性能边界
- 开发专用硬件友好版本（如支持 HAS-VQ 的 kernel fusion）

---

## ✅ 总结
HAS-VQ 是一种面向小型 LLM 的高性能 PTQ 方法，通过 **Hessian-driven outlier isolation + residual sparse feedback + robust VQ** 的三段式设计，在 **4.23 BPP** 下超越 INT4 基线，同时可在 **7.03 BPP** 实现近乎无损压缩。它打破了传统整数量化在小型模型上的性能瓶颈，为边缘设备部署提供了新的“高效且高保真”的压缩范式。

</details>

---

### 13. [Land-then-transport: A Flow Matching-Based Generative Decoder for Wireless Image Transmission](https://arxiv.org/abs/2601.07512)

**Authors**: Jingwen Fu, Ming Xiao, Mikael Skoglund, Dong In Kim  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.07512v1  

#### Abstract
Due to strict rate and reliability demands, wireless image transmission remains difficult for both classical layered designs and joint source-channel coding (JSCC), especially under low latency. Diffusion-based generative decoders can deliver strong perceptual quality by leveraging learned image pri...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Land-then-transport: A Flow Matching-Based Generative Decoder for Wireless Image Transmission*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统无线图像传输面临以下挑战：
- **分层设计**（如 JPEG2000 + LDPC）在有限码长、低延迟场景下性能不佳，且对信道失配敏感。
- **基于扩散模型的生成式解码器**（如 CDDM）虽然能提升感知质量，但依赖多步随机去噪过程，导致**高解码延迟**，难以满足实时通信需求。

本文旨在解决如何在保证高质量重建的同时，实现**低延迟、确定性、信道自适应**的无线图像生成式解码。

---

### 提出的新方法与创新思路
作者提出了一种名为 **Land-then-Transport (LTT)** 的新范式，并基于 **Conditional Flow Matching (CFM)** 构建了一个新型生成式解码器：

#### （1）LTT 范式：将物理信道嵌入概率流
- 将无线信道效应视为连续时间概率路径上的“着陆”操作。
- 接收信号 $Y$ 被解释为在某个**有效着陆时间 $t^*$** 处的概率路径状态，该时间由信道噪声水平决定。
- 解码即是从 $t^*$ 到 $t=1$（干净图像）的**确定性 ODE 积分过程**，无需从纯噪声开始采样。

#### （2）Flow Matching-based Decoder 设计
- 针对 AWGN 信道构造一个**高斯平滑路径**（Gaussian smoothing path），其噪声调度函数单调映射信道噪声强度。
- 推导出该路径下的**闭式解析教师速度场**（analytical teacher velocity field）。
- 使用深度神经网络学习学生向量场 $v_\theta(x,t)$，通过 **CFM 损失函数**进行训练，避免模拟前向扩散过程。

#### （3）跨信道通用性设计
- 对于 **Rayleigh 衰落** 和 **MIMO 信道**，通过 **Linear MMSE Equalization** 和 **SVD 域处理**，将其转换为等效 AWGN 信道。
- 利用相同的 AWGN 训练好的速度场和概率路径，仅需调整着陆时间 $t^*$，即可实现**无需重新训练**的跨信道解码。

---

### 相比现有方法的优势
| 特性 | 传统分层方法 | DeepJSCC | 扩散模型（如 CDDM） | 本文 LTT |
|------|---------------|----------|------------------------|---------|
| **延迟** | 中等 | 低 | 高（百级采样步） | **极低**（10 步内） |
| **重建质量** | 像素级好，感知差 | 一般 | 感知质量高 | **感知质量更高** |
| **信道适应性** | 差（cliff effect） | 较好 | 通常需重训练 | **强（统一路径）** |
| **解码确定性** | 是 | 是 | 否（随机） | **是（ODE）** |
| **计算效率** | 高 | 高 | 低 | **高（线性复杂度）** |

---

## 2. 核心实验方法和设置

### 数据集
- **MNIST**：28×28 灰度手写数字，用于低分辨率验证。
- **Fashion-MNIST**：28×28 灰度服饰图像，测试多样性。
- **DIV2K**：256×256 自然图像，作为高分辨率基准。

### 实验设置
- **信道模型**：AWGN、Rayleigh Fading、2×2 MIMO。
- **带宽效率一致**：所有方法使用相同 channel uses per pixel，确保公平比较。
- **训练细节**：
  - 使用 U-Net 结构实现学生速度场 $v_\theta(x,t)$。
  - 最大噪声水平 $\sigma_{\text{max}} = 1.0$，对应 SNR > 0 dB。
  - Batch size：64（MNIST/Fashion-MNIST），32（DIV2K）。
  - ODE 步数默认设为 10。

### 评估指标
| 指标 | 描述 |
|------|------|
| **PSNR** | 像素级保真度，越高越好 |
| **MS-SSIM** | 多尺度结构相似性，反映视觉保真度 |
| **LPIPS** | 学习型感知距离，在特征空间衡量差异，越低越好 |
| **$\Delta$PSNR** | 相对于接收信号的 PSNR 提升，体现解码增益 |

### 基线方法对比
1. **JPEG2000 + LDPC**：经典分离式编解码方案。
2. **DeepJSCC**：端到端联合信源信道编码。
3. **CDDM**：基于扩散模型的生成式解码器。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 DIV2K 在 AWGN, SNR=20dB 为例）
| 方法 | PSNR (dB) | MS-SSIM | LPIPS |
|------|-----------|---------|-------|
| **LTT (Ours)** | **36.7** | **24.9** | **0.061** |
| CDDM | ~28.9 (-26.6%) | ~16.2 (-53.2%) | — |
| DeepJSCC | ~28.6 (-28.3%) | ~15.6 (-59.6%) | — |
| JPEG2000+LDPC | ~31.5 (-16.9%) | ~20.0 (-24.9%) | — |

> 注：文中未直接给出 20dB 的表格值，但从 Fig. 4 推算得出上述趋势。

在 **SNR=15dB** 下（见 Table II）：
- **AWGN**: PSNR = 33.83 dB, MS-SSIM = 20.20 dB
- **Rayleigh**: PSNR = 31.92 dB
- **MIMO**: PSNR = 33.41 dB

---

### 与基线方法的对比结果
- 在所有信道和数据集上，LTT 均显著优于所有基线：
  - 相比 **CDDM** 和 **DeepJSCC**，PSNR 提升约 **25–30%**，MS-SSIM 提升超 **50%**。
  - 相比 **JPEG2000+LDPC**，无 cliff effect，低 SNR 下仍可解码，高 SNR 下仍有明显增益（PSNR +16.9%, MS-SSIM +24.9% @ AWGN）。
- 视觉效果更优：边缘更锐利、纹理更清晰、几何结构更忠实（见 Fig. 5）。

---

### 消融实验结果
#### （1）ODE 步数影响（Table V）
| 步数 | PSNR (dB) | MS-SSIM | 单样本推理时间 (s) |
|------|-----------|---------|---------------------|
| 2 | 30.30 | 16.83 | 0.18 |
| 5 | 30.12 | 16.62 | 0.29 |
| **10** | **30.52** | **16.60** | **0.46** |
| 20 | 30.10 | 16.58 | 0.78 |
| 50 | 30.19 | 16.53 | 1.80 |

- **仅需 10 步即可达到最佳性能**，更多步数带来边际收益递减。
- 推理时间随步数线性增长，10 步为精度与效率的最佳平衡点。

#### （2）噪声调度分析（Table VI）
| SNR (dB) | 0 | 3 | 5 | 10 | 15 |
|----------|----|----|----|-----|-----|
| $t^*$ | 0.463 | 0.328 | 0.261 | 0.147 | 0.082 |

- $t^*$ 随 SNR 单调增加：**SNR 越高，着陆点越接近干净端**，积分区间越短。
- 表明 $t^*$ 可作为信道条件与生成动力学之间的**可解释桥梁**。

---

## 4. 关键结论和发现

### 主要发现
1. **LTT 范式成功将物理信道融入生成流**，实现了“信道即生成路径一部分”的理念。
2. 基于 **CFM 的确定性 ODE 解码器**可在 **10 步内完成高质量重建**，显著降低延迟。
3. 通过 **MMSE+SVD 预处理**，AWGN 训练的模型可无缝迁移至 Rayleigh 和 MIMO 信道，具备强大泛化能力。
4. 在多个数据集和信道条件下，LTT 在 **PSNR、MS-SSIM、LPIPS** 上全面超越主流基线，尤其在感知质量上优势明显。

---

### 方法的局限性
- 当前框架依赖于信道可被转化为 AWGN 等效形式（适用于线性高斯信道），对非线性或严重非高斯干扰的扩展尚不明确。
- 虽然 ODE 步数少，但仍需数值求解，相比 DeepJSCC 的单次前传略有开销。
- 模型容量仍较大（U-Net），在极端资源受限设备部署可能受限。

---

### 未来工作方向
- 扩展至 **语义通信** 场景，结合语义先验进一步压缩传输信息。
- 探索 **一致性模型**（Consistency Models）替代 ODE 求解，实现一步生成。
- 支持 **动态带宽调整** 和 **多用户 MIMO** 场景下的自适应传输。
- 进一步优化网络结构，提升轻量化与部署效率。

---

> ✅ **总结一句话**：  
> 本文提出的 **LTT 框架**通过将无线信道嵌入 Flow Matching 的概率流中，构建了一个**确定性、低延迟、跨信道通用**的生成式图像解码器，在保持高感知质量的同时，解决了传统扩散模型在通信系统中的高延迟瓶颈。

</details>

---

### 14. [Adaptive Layer Selection for Layer-Wise Token Pruning in LLM Inference](https://arxiv.org/abs/2601.07667)

**Authors**: Rei Taniguchi, Yuyang Dong, Makoto Onizuka, Chuan Xiao  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.07667v1  

#### Abstract
Due to the prevalence of large language models (LLMs), key-value (KV) cache reduction for LLM inference has received remarkable attention. Among numerous works that have been proposed in recent years, layer-wise token pruning approaches, which select a subset of tokens at particular layers to retain...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Adaptive Layer Selection for Layer-Wise Token Pruning in LLM Inference

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **layer-wise token pruning** 方法在进行 **KV cache reduction** 时，通常采用预定义的固定层（称为 *selection layer*）来选择保留的 tokens。这种设计存在显著缺陷：
- **任务不感知（task-agnostic）**：不同任务对语义理解的深度需求不同，简单任务（如 QA）可能早期即可定位关键信息，而复杂任务（如 KV retrieval）需要更深层才能聚焦。
- **性能波动大**：在困难任务上，过早的 token selection 会导致关键信息丢失，严重损害 accuracy；为保证性能，只能推迟 selection layer 或增加 KV budget，牺牲内存效率。

### 提出的新方法：ASL (Adaptive Selection Layer)
本文提出 **ASL**，一种无需训练、任务感知的自适应层选择方法，其核心思想是：
- **监控注意力得分排序的方差（variance of token ranks）**：在推理过程中，计算连续若干层中 tokens 按注意力得分排序后的排名方差。
- **动态确定 selection layer**：当该方差低于一个用户指定的阈值 `T` 时，表明注意力已稳定聚焦于一小部分 tokens，此时即为执行 token selection 的最佳时机。

### 相比现有方法的优势
- **任务自适应性强**：能根据不同任务的难度自动调整 selection layer，简单任务选浅层以提升速度，困难任务选深层以保证精度。
- **精度更高**：在困难任务（如 KV retrieval, multi-key NIAH）上显著优于固定 selection layer 的方法（如 FastKV, GemFilter）。
- **灵活性高**：可在 **prefilling 阶段**运行，并可无缝集成现有方法（如与 SnapKV 结合优化 decoding，与 GemFilter 结合实现 two-pass 策略）。
- **开销小**：仅需存储少量最近几层的 pooled attention scores，峰值内存和计算开销极低。

---

## 2. 核心实验方法和设置

### 数据集
在三个长上下文基准上进行了评估：
- **InfiniteBench**：平均上下文长度约 214k，包含 10 个多样化任务（如摘要、问答、代码调试、KV 检索等）。
- **RULER**：合成基准，支持配置上下文长度（4k 到 128k），包含 13 个任务，涵盖 NIAH（Needle in a Haystack）变体、词频统计、变量追踪等。
- **NIAH (Needle in a Haystack)**：经典检索能力测试，上下文长度从 1k 扩展到 256k。

### 实验设置和评估指标
- **模型**：
  - `Llama-3.1-8B-UL` (32 layers)
  - `Qwen2.5-7B` (28 layers)
- **KV Budget**：默认为 2048。
- **评估指标**：
  - **Accuracy (%)**：任务正确率。
  - **TTFT (Time to First Token)**：衡量 prefilling 效率。
  - **TPOT (Time Per Output Token)**：衡量 decoding 效率。
  - **Throughput**：吞吐量（queries/s）。
  - **Memory Usage**：显存占用。

### 基线方法对比
- **SnapKV**：基于启发式的 KV cache 压缩，不优化 TTFT。
- **FastKV**：one-shot 方法，固定 selection layer（Llama-3.1-8B-UL 为第 15 层）。
- **GemFilter**：two-pass 方法，固定 selection layer（Llama-3.1-8B-UL 为第 13 层）。
- **PyramidInfer**：渐进式 token pruning 方法。

本文提出的 ASL 包含两种变体：
- **ASL**：one-pass 方案，与 SnapKV 结合。
- **ASL_2pass**：two-pass 方案，与 GemFilter 结合。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### 在 InfiniteBench 上的准确性
- 对于 `Llama-3.1-8B-UL`，**ASL_2pass** 在 KV budget=2048 下取得了最高的平均准确率 (**37.8%**)，优于 FastKV (36.4%) 和 GemFilter (37.0%)。
- 当 pre-selection 使用 full KV 时，**ASL** 在困难任务（如 `kv_retrieval`）上的表现接近 full KV，而 FastKV 性能大幅下降（ASL: 15.4% vs FastKV: 3.2%）。

#### 在 RULER 上的准确性
- 对于 `Llama-3.1-8B-UL`，**ASL** 在所有上下文长度下均优于 FastKV。
- 对于 `Qwen2.5-7B`，当上下文长度 ≥16k 时，**ASL** 显著优于 FastKV，且优势随长度增加而扩大。
- 在 128k 上下文下，**ASL** 平均准确率为 **66.4%**，高于 FastKV (59.1%) 和 GemFilter (56.7%)。

#### 在 NIAH 上的表现
- **ASL** 和 **ASL_2pass** 在所有上下文长度上均达到 **100%** 准确率，与 full KV 相当。
- SnapKV 和 FastKV 在 148k 长度时几乎无法检索到 needle。
- GemFilter 表现平庸，尤其在长上下文下。

#### 效率指标 (RULER, 128k)
| 方法 | TTFT (相对值) | TPOT (相对值) | Throughput (mean) |
| :--- | :--- | :--- | :--- |
| **FastKV** | 0.50 | 0.27 | 3.07 |
| **ASL** | 0.79 | 0.28 | 2.27 |
| **GemFilter** | 0.44 | 0.25 | 2.36 |

- **ASL 的 TTFT 较高**：因其 selection layer 通常更深（平均 L=23.9 vs FastKV 的 L=15），prefilling 时间更长。
- **ASL 的 TPOT 与基线相当**：解码阶段效率相近。
- **Throughput**：ASL 约为 FastKV 的 74%，但考虑到大多数实际应用中输出 token 数远多于输入，解码时间占主导，实际差距会缩小。

#### 内存使用
- 所有 KV cache reduction 方法（包括 ASL）的内存使用几乎相同（约 0.3GB for Llama-3.1-8B-UL），远低于 full KV (17.1GB)，证明 ASL 的额外开销可忽略。

### 消融实验结果
- **相对方差阈值 `T` 的影响**（Table 9）：
  - `T=0.3` 是精度与 TTFT 的良好权衡点。
  - `T` 越小（要求方差更低），selection layer 越深，accuracy 更高但 TTFT 更长。
  - `T` 越大，selection layer 越早，TTFT 更短但 accuracy 可能下降，尤其在困难任务上。
- **Lmin 和 Lobs 的选择**（Appendix D.2）：
  - 推荐 `Lmin ≈ L_model / 3`（如 Llama-3.1-8B-UL 用 10）。
  - `Lobs=8` 能有效平衡敏感性和稳定性。

---

## 4. 关键结论和发现

### 主要发现
1. **固定 selection layer 是现有方法的瓶颈**：其性能高度依赖任务难度，在困难任务上表现不佳。
2. **注意力得分排序的方差是有效的自适应信号**：当方差降低时，表明模型已稳定聚焦，是进行 token pruning 的理想时机。
3. **ASL 实现了更好的精度-效率权衡**：通过自适应选择，ASL 在保持与基线方法相近的 decoding 速度和内存占用的同时，显著提升了 accuracy，尤其是在长上下文和困难任务上。
4. **ASL 具有良好的兼容性**：可作为通用模块，与 SnapKV、GemFilter 等方法结合，进一步优化整体推理流程。

### 方法的局限性
1. **主要针对 one-shot 方法**：目前仅应用于 FastKV 和 GemFilter 类型的方法，如何将其扩展到 progressive 或 multi-shot 方法（如 LazyLLM）尚待研究。
2. **未与非 layer-wise 方法全面比较**：文中未与量化（quantization）、CPU 卸载（offloading）等其他 KV cache reduction 范式进行深入对比。
3. **模型覆盖有限**：仅在两个模型上验证，且在 `Llama-3.1-8B-UL` 上与 GemFilter 结合时表现不如预期。
4. **引入了超参数 `T`**：虽然 `T=0.3` 是推荐值，但仍需用户根据具体场景微调。

### 未来工作方向
- 探索将 ASL 应用于 progressive token pruning 方法。
- 设计更复杂的自适应策略，例如使用多个阈值或动态衰减的阈值。
- 进行更广泛的模型和方法对比，明确 ASL 在整个 KV cache reduction 领域中的定位。
- 研究如何减少对超参数 `T` 的依赖，实现完全自适应。

</details>

---

### 15. [Learning Minimally-Congested Drive Times from Sparse Open Networks: A Lightweight RF-Based Estimator for Urban Roadway Operations](https://arxiv.org/abs/2601.06124)

**Authors**: Adewumi Augustine Adepitan, Christopher J. Haruna, Morayo Ogunsina, Damilola Olawoyin Yussuf, Ayooluwatomiwa Ajiboye  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.06124v1  

#### Abstract
Accurate roadway travel-time prediction is foundational to transportation systems analysis, yet widespread reliance on either data-intensive congestion models or overly na\"ive heuristics limits scalability and practical adoption in engineering workflows. This paper develops a lightweight estimator ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**交通工程领域中旅行时间预测方法在准确性与实用性之间的鸿沟**。当前主流方法存在两个极端：
- **数据密集型模型**（如基于大规模GPS、传感器或深度学习的方法）虽然精度高（MAPE通常为3%-17%），但依赖昂贵数据和强大算力，难以普及；
- **简化启发式方法**（如基于欧氏距离或速度限制的最短路径）虽易于实施，却因忽略信号灯、转向延误等因素导致系统性低估实际旅行时间（MAPE > 20%）。

因此，缺乏一种既准确又轻量、适用于资源受限场景的中间方案。

### 提出的新方法与思路
作者提出了一种**基于Random Forest的轻量级估计器（Lightweight RF-Based Estimator）**，用于预测城市道路网络中的最小拥堵驾驶时间。其核心创新在于：
- 构建一个**融合开放路网数据与稀疏控制特征的机器学习框架**，以校正最短路径遍历时间（`tnaive`）的偏差；
- 利用OpenStreetMap等开源地理数据提取关键操作特征（traffic controls 和 turning movements），作为模型输入；
- 在仅需少量高质量参考时间（reference travel times）的情况下训练回归集成模型，实现对未见OD对的有效泛化。

### 相比现有方法的优势
- ✅ **高准确性**：性能接近state-of-the-art方法（MAPE降至8.41%），显著优于传统简化方法；
- ✅ **低资源需求**：不依赖专有数据或实时拥堵流，仅使用公开可用数据（如OSM、Google Maps API采样）；
- ✅ **可扩展性强**：可在标准硬件上进行大都市尺度分析，支持规划级应用；
- ✅ **透明可复现**：完全基于open data和open-source工具链，提升方法论透明度；
- ✅ **实用导向**：特别适合于基础设施评估、可达性分析、应急响应等低流量运行条件下的工程应用场景。

---

## 2. 核心实验方法和设置

### 数据集
| 数据来源 | 内容说明 |
|--------|-------|
| **OpenStreetMap (OSM)** | 使用OSMnx提取洛杉矶城区道路网络，保留强连通分量；包含63,359个标注的交通控制元素（stop signs, traffic signals, crossings等） |
| **Uber Movement 数据** | 用于筛选真实存在的OD对，确保生成的起点-终点组合具有现实意义 |
| **Google Maps Routes API** | 获取参考旅行时间（response variable），采用 `"BEST_GUESS"` 模型在凌晨3:00 AM调用，模拟最小拥堵状态；共成功获取41,360条有效记录 |

### 实验设置
- **研究区域**：洛杉矶县及周边城市化核心区（基于Global Human Settlement Layer界定）
- **OD样本生成**：从500万随机节点对中，通过Uber数据过滤出符合真实出行模式的41,378个OD对
- **特征工程**：
  - **Baseline特征**：`tnaive` —— 基于Dijkstra算法计算的速度限制加权最短路径时间
  - **Traffic Control 特征**（5类计数）：stop signs, traffic signals, pedestrian crossings, give way signs, mini roundabouts
  - **Turning Movements 特征**（5类计数）：left turns, slight left, right turns, slight right, U-turns（straight movement作为基准类别省略）
  - 总共构建 **11维特征向量**
- **模型选择流程**：
  - 对比四种算法：Decision Tree (DT), Random Forest (RF), Gradient Boosting (GB), AdaBoost
  - 采用80/20训练测试划分 + 5折交叉验证
  - 超参数优化使用randomized grid search，目标是最小化MAE

### 评估指标
| 指标 | 全称 | 用途 |
|-----|------|-----|
| **MAPE** | Mean Absolute Percentage Error | 衡量相对误差 |
| **MAE** | Mean Absolute Error (seconds) | 衡量绝对误差 |
| **MSE** | Mean Squared Error | 反映误差方差 |
| **Bias (δ)** | 平均预测偏差（预测值 - 真实值） | 判断是否存在系统性高估或低估 |
| **R²** | Explained Variance Score | 衡量模型解释能力 |
| **APR** | Average Pairwise Ratio | 预测/真实时间平均比率，理想值为1 |

### 基线方法对比
- **Naive Baseline**：仅基于Dijkstra最短路径的时间估计（`tnaive`）
- **其他ML模型**：DT, GB, AdaBoost 作为候选替代方案，用于验证RF的优越性

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Out-of-Sample Evaluation）

| Model | MAPE (%) | MAE (s) | δ (s) | R² |
|-------|----------|--------|--------|-----|
| Naive TT | 21.2 | 184 | -182.9 | 0.74 |
| **RF (Ours)** | **8.41** | **75** | **+0.38** | **0.93** |
| GB | 7.86 | 72 | -19.2 | 0.93 |
| DT | 9.00 | 80 | +0.13 | 0.93 |
| AdaBoost | 8.20 | 74 | -9.76 | 0.93 |

> 注：表格来自原文Table 1，数值已四舍五入

### 与基线方法的对比结果
- ✅ **MAPE降低超过60%**：从21.2%下降至8.41%，进入state-of-the-art范围（3%-17%）
- ✅ **MAE减少约60%**：从184秒降至75秒，误差缩小近三分之二
- ✅ **MSE下降4倍**：从48,214降至12,155 s²，表明整体预测稳定性大幅提升
- ✅ **消除系统性偏差**：Naive方法严重低估（平均少183秒），而RF模型无显著bias（δ = +0.38s, p=0.76）
- ✅ **APR改善明显**：从0.79（低估21%）提升至1.01（几乎无偏）
- ✅ **R²提高至0.93**：说明模型能解释93%的方差，远高于baseline的74%

### 消融实验与特征重要性分析（Feature Importance）
尽管未严格设计“消融实验”，但通过RF内置的重要性评分揭示了各特征贡献：
- **`tnaive`（基础遍历时间）**：贡献约68%的R²，是主导因素，证明基础网络结构仍至关重要
- **Turning Movements**：合计贡献约19%，其中左转与右转影响相当，U-turn因罕见而权重低
- **Traffic Controls**：
  - **Traffic Signals** 最具影响力，其次为Stop Signs 和 Crossings
  - Roundabouts 与 Give Way Signs 权重较低，可能因其在网络中分布稀疏所致

👉 结论：即使标签不完整、数据稀疏，这些operational features仍提供了足够信号来显著改进预测效果。

---

## 4. 关键结论和发现

### 主要发现
1. **轻量级RF模型可在稀疏开放数据下实现高性能旅行时间预测**，在MAPE、MAE、R²等多个指标上显著超越传统方法；
2. **结合Dijkstra baseline与sparse operational features（信号、转向）可有效纠正系统性低估问题**；
3. **Random Forest在bias-variance trade-off方面表现最优**：相比GB和AdaBoost更少出现under-prediction bias，相比单棵决策树更具鲁棒性；
4. **方法具备良好的k-fold稳定性**：5次CV的MAE分别为75.3, 74.5, 73.6, 73.2, 74.7秒，表明无过拟合；
5. **适用于metropolitan-scale分析**，且无需专有数据或高性能计算资源，适合广泛部署于交通规划实践。

### 方法的局限性
- ❗ **仅适用于最小拥堵条件**（free-flow scenarios）：未建模高峰时段的动态拥堵效应，预测结果在高峰期可能偏低；
- ❗ **依赖OSM数据质量**：不同地区traffic control tagging完整性差异较大，可能影响迁移性能；
- ❗ **泛化能力有待验证**：模型参数是否适用于其他城市尚需进一步测试；
- ❗ **未显式建模时间变化**：缺乏对日周期、周模式等temporal variation的支持；
- ❗ **路线不匹配问题**：当OSM路径与Google实际推荐路径不一致时，模型隐含假设两者delay特征相似，存在一定误差源。

### 未来工作方向
1. **扩展至多时段建模**：收集跨时间段的reference data，区分peak vs off-peak，引入粗粒度时间变量；
2. **融合更多open data源**：如crowdsourced traffic reports、public transit schedules、weather data等，增强预测维度；
3. **空间插补与缺失数据处理**：利用spatial statistics或ML方法填补traffic control标签空白区域；
4. **Transfer Learning for Regional Calibration**：在数据丰富区域训练后，用少量本地样本微调迁移到新城市；
5. **集成到real-time routing系统**：将本方法作为accurate yet lightweight baseline，配合轻量级congestion proxy实现高效动态导航；
6. **推动open & reproducible transportation modeling ecosystem**：鼓励社区协作提升OSM数据质量并共享模型参数。

---

> 📌 **总结一句话**：  
> 本文提出了一种基于Random Forest的轻量级旅行时间预测框架，在仅使用OpenStreetMap等开放数据的前提下，显著提升了最小拥堵条件下城市驾驶时间估计的准确性，填补了高精度模型与简化方法之间的实用空白，为资源受限环境下的智能交通规划提供了可行解决方案。

</details>

---

### 16. [A Fast and Effective Method for Euclidean Anticlustering: The Assignment-Based-Anticlustering Algorithm](https://arxiv.org/abs/2601.06351)

**Authors**: Philipp Baumann, Olivier Goldschmidt, Dorit S. Hochbaum, Jason Yang  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.06351v1  

#### Abstract
The anticlustering problem is to partition a set of objects into K equal-sized anticlusters such that the sum of distances within anticlusters is maximized. The anticlustering problem is NP-hard. We focus on anticlustering in Euclidean spaces, where the input data is tabular and each object is repre...

---

### 17. [Mosaic: Unlocking Long-Context Inference for Diffusion LLMs via Global Memory Planning and Dynamic Peak Taming](https://arxiv.org/abs/2601.06562)

**Authors**: Liang Zheng, Bowen Shi, Yitao Hu, Jiawei Zhang, Ruofan Li, Sheng Chen, Wenxin Li, Keqiu Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.06562v1  

#### Abstract
Diffusion-based large language models (dLLMs) have emerged as a promising paradigm, utilizing simultaneous denoising to enable global planning and iterative refinement. While these capabilities are particularly advantageous for long-context generation, deploying such models faces a prohibitive memor...

---

### 18. [Is Sanskrit the most token-efficient language? A quantitative study using GPT, Gemini, and SentencePiece](https://arxiv.org/abs/2601.06142)

**Authors**: Anshul Kumar  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.06142v1  

#### Abstract
Tokens are the basic units of Large Language Models (LLMs). LLMs rely on tokenizers to segment text into these tokens, and tokenization is the primary determinant of computational and inference cost. Sanskrit, one of the oldest languages, is hypothesized to express more meaning per token due to its ...

---

### 19. [Bringing Computation to the data: Interoperable serverless function execution for astrophysical data analysis in the SRCNet](https://arxiv.org/abs/2601.07308)

**Authors**: Manuel Parra-Roy\'on, Juli\'an Garrido-S\'anchez, Susana S\'anchez-Exp\'osito, Mar\'ia \'Angeles Mendoza, Rob Barnsley, Anthony Moraghan, Jes\'us S\'anchez, Laura Darriba, Carlos Ru\'iz-Monje, Edgar Joao, Javier Mold\'on, Jes\'us Salgado, Lourdes Verdes-Montenegro  
**Category**: cs.DC  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.07308v1  

#### Abstract
Serverless computing is a paradigm in which the underlying infrastructure is fully managed by the provider, enabling applications and services to be executed with elastic resource provisioning and minimal operational overhead. A core model within this paradigm is Function-as-a-Service (FaaS), where ...

---

### 20. [TimeGNN-Augmented Hybrid-Action MARL for Fine-Grained Task Partitioning and Energy-Aware Offloading in MEC](https://arxiv.org/abs/2601.06191)

**Authors**: Wei Ai, Yun Peng, Yuntao Shou, Tao Meng, Keqin Li  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.06191v1  

#### Abstract
With the rapid growth of IoT devices and latency-sensitive applications, the demand for both real-time and energy-efficient computing has surged, placing significant pressure on traditional cloud computing architectures. Mobile edge computing (MEC), an emerging paradigm, effectively alleviates the l...

---

### 21. [One-Shot Hierarchical Federated Clustering](https://arxiv.org/abs/2601.06404)

**Authors**: Shenghong Cai, Zihua Yang, Yang Lu, Mengke Li, Yuzhu Ji, Yiqun Zhang, Yiu-Ming Cheung  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.06404v1  

#### Abstract
Driven by the growth of Web-scale decentralized services, Federated Clustering (FC) aims to extract knowledge from heterogeneous clients in an unsupervised manner while preserving the clients' privacy, which has emerged as a significant challenge due to the lack of label guidance and the Non-Indepen...

---

### 22. [ArenaRL: Scaling RL for Open-Ended Agents via Tournament-based Relative Ranking](https://arxiv.org/abs/2601.06487)

**Authors**: Qiang Zhang, Boli Chen, Fanrui Zhang, Ruixue Ding, Shihang Wang, Qiuchen Wang, Yinfeng Huang, Haonan Zhang, Rongxiang Zhu, Pengyong Wang, Ailin Ren, Xin Li, Pengjun Xie, Jiawei Liu, Ning Guo, Jingren Zhou, Zheng-Jun Zha  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.06487v1  

#### Abstract
Reinforcement learning has substantially improved the performance of LLM agents on tasks with verifiable outcomes, but it still struggles on open-ended agent tasks with vast solution spaces (e.g., complex travel planning). Due to the absence of objective ground-truth for these tasks, current RL algo...

---

### 23. [PRPO: Aligning Process Reward with Outcome Reward in Policy Optimization](https://arxiv.org/abs/2601.07182)

**Authors**: Ruiyi Ding, Yongxuan Lv, Xianhui Meng, Jiahe Song, Chao Wang, Chen Jiang, Yuan Cheng  
**Category**: cs.LG  
**Published**: 2026-01-13  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.07182v1  

#### Abstract
Policy optimization for large language models often suffers from sparse reward signals in multi-step reasoning tasks. Critic-free methods like GRPO assign a single normalized outcome reward to all tokens, providing limited guidance for intermediate reasoning . While Process Reward Models (PRMs) offe...

---

### 24. [Overcoming Joint Intractability with Lossless Hierarchical Speculative Decoding](https://arxiv.org/abs/2601.05724)

**Authors**: Yuxuan Zhou, Fei Huang, Heng Li, Fengyi Wu, Tianyu Wang, Jianwei Zhang, Junyang Lin, Zhi-Qi Cheng  
**Category**: cs.AI  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05724v1  

#### Abstract
Verification is a key bottleneck in improving inference speed while maintaining distribution fidelity in Speculative Decoding. Recent work has shown that sequence-level verification leads to a higher number of accepted tokens compared to token-wise verification. However, existing solutions often rel...

---

### 25. [TeleMem: Building Long-Term and Multimodal Memory for Agentic AI](https://arxiv.org/abs/2601.06037)

**Authors**: Chunliang Chen, Ming Guan, Xiao Lin, Jiaxu Li, Qiyi Wang, Xiangyu Chen, Jixiang Luo, Changzhi Sun, Dell Zhang, Xuelong Li  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.06037v1  

#### Abstract
Large language models (LLMs) excel at many NLP tasks but struggle to sustain long-term interactions due to limited attention over extended dialogue histories. Retrieval-augmented generation (RAG) mitigates this issue but lacks reliable mechanisms for updating or refining stored memories, leading to ...

---

### 26. [Value of Information: A Framework for Human-Agent Communication](https://arxiv.org/abs/2601.06407)

**Authors**: Yijiang River Dong, Tiancheng Hu, Zheng Hui, Caiqi Zhang, Ivan Vuli\'c, Andreea Bobu, Nigel Collier  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.06407v1  

#### Abstract
Large Language Model (LLM) agents deployed for real-world tasks face a fundamental dilemma: user requests are underspecified, yet agents must decide whether to act on incomplete information or interrupt users for clarification. Existing approaches either rely on brittle confidence thresholds that re...

---

### 27. [Solar Open Technical Report](https://arxiv.org/abs/2601.07022)

**Authors**: Sungrae Park, Sanghoon Kim, Jungho Cho, Gyoungjin Gim, Dawoon Jung, Mikyoung Cha, Eunhae Choo, Taekgyu Hong, Minbyul Jeong, SeHwan Joo, Minsoo Khang, Eunwon Kim, Minjeong Kim, Sujeong Kim, Yunsu Kim, Hyeonju Lee, Seunghyun Lee, Sukyung Lee, Siyoung Park, Gyungin Shin, Inseo Song, Wonho Song, Seonghoon Yang, Seungyoun Yi, Sanghoon Yoon, Jeonghyun Ko, Seyoung Song, Keunwoo Choi, Hwalsuk Lee, Sunghun Kim, Du-Seong Chang, Kyunghyun Cho, Junsuk Choe, Hwaran Lee, Jae-Gil Lee, KyungTae Lim, Alice Oh  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.07022v1  

#### Abstract
We introduce Solar Open, a 102B-parameter bilingual Mixture-of-Experts language model for underserved languages. Solar Open demonstrates a systematic methodology for building competitive LLMs by addressing three interconnected challenges. First, to train effectively despite data scarcity for underse...

---

### 28. [Document-Level Zero-Shot Relation Extraction with Entity Side Information](https://arxiv.org/abs/2601.07271)

**Authors**: Mohan Raj Chanthran, Soon Lay Ki, Ong Huey Fang, Bhawani Selvaretnam  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.07271v1  

#### Abstract
Document-Level Zero-Shot Relation Extraction (DocZSRE) aims to predict unseen relation labels in text documents without prior training on specific relations. Existing approaches rely on Large Language Models (LLMs) to generate synthetic data for unseen labels, which poses challenges for low-resource...

---

### 29. [PlaM: Training-Free Plateau-Guided Model Merging for Better Visual Grounding in MLLMs](https://arxiv.org/abs/2601.07645)

**Authors**: Zijing Wang, Yongkang Liu, Mingyang Wang, Ercong Nie, Deyuan Chen, Zhengjie Zhao, Shi Feng, Daling Wang, Xiaocui Yang, Yifei Zhang, Hinrich Sch\"utze  
**Category**: cs.CL  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.07645v1  

#### Abstract
Multimodal Large Language Models (MLLMs) rely on strong linguistic reasoning inherited from their base language models. However, multimodal instruction fine-tuning paradoxically degrades this text's reasoning capability, undermining multimodal performance. To address this issue, we propose a trainin...

---

### 30. [Employ SmartNICs' Data Path Accelerators for Ordered Key-Value Stores](https://arxiv.org/abs/2601.06231)

**Authors**: Frederic Schimmelpfennig, Jan Sass, Reza Salkhordeh, Martin Kr\"oning, Stefan Lankes, Andr\'e Brinkmann  
**Category**: cs.DC  
**Published**: 2026-01-13  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.06231v1  

#### Abstract
Remote in-memory key-value (KV) stores serve as a cornerstone for diverse modern workloads, and high-speed range scans are frequently a requirement. However, current architectures rarely achieve a simultaneous balance of peak efficiency, architectural simplicity, and native support for ordered opera...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
