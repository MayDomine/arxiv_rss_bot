# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-03 06:35:30 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Out of the Memory Barrier: A Highly Memory Efficient Training System for LLMs with Million-Token Contexts](https://arxiv.org/abs/2602.02108)

**Authors**: Wenhao Li, Daohai Yu, Gen Luo, Yuxin Zhang, Fei Chao, Rongrong Ji, Yifan Wu, Jiaxin Liu, Ziyang Gong, Zimu Liao  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2602.02108v1  

#### Abstract
Training Large Language Models (LLMs) on long contexts is severely constrained by prohibitive GPU memory overhead, not training time. The primary culprits are the activations, whose memory footprints scale linearly with sequence length. We introduce OOMB, a highly memory-efficient training system th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Out of the Memory Barrier: A Highly Memory Efficient Training System for LLMs with Million-Token Contexts*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
训练具有**百万级 Token 上下文长度**的大型语言模型（LLMs）面临严重的 **GPU 内存瓶颈**，尤其是由 **Activations** 和 **KV Cache** 引起的内存开销随序列长度线性增长。传统方法如 ZeRO-3、Tensor Parallelism 在长上下文场景下仍需大规模集群，成本高昂。

该论文旨在解决：**如何在极低硬件资源下高效训练超长上下文 LLMs**，突破“内存墙”限制。

---

### 🚀 提出的新方法：OOMB
作者提出 **OOMB**（Out Of the Memory Barrier），一个高度内存高效的训练系统，其核心思想是 **Chunk-Recurrent Training Framework**，并集成多项协同优化技术：

#### 主要创新点：
1. **Chunk-wise Recurrent Training + On-the-fly Activation Recomputation**
   - 将长序列分块处理，前向传播后立即丢弃中间激活值（activations），反向传播时按需重新计算。
   - 实现 **O(1)** 的 activation 内存复杂度，彻底消除 activation 随长度增长的内存负担。

2. **Paged Memory Manager for KV Cache and Gradients**
   - 受 PagedAttention 启发，设计了支持反向传播的 **分页 KV Cache 管理器**。
   - 消除内存碎片化和频繁拷贝操作，提升内存利用率。
   - 自定义 Triton kernel 绕过 PyTorch Autograd，实现梯度原地累加（atomic_add），进一步降低内存开销。

3. **Asynchronous CPU Offloading for KV Cache**
   - 将不断增长的 KV Cache 和其梯度异步卸载到 CPU 内存中。
   - 利用 **pinned memory** 和 **CUDA streams** 实现真正异步传输，通过计算掩盖通信延迟（latency hiding），端到端开销 < 5%。

4. **Page-level Sparse Attention**
   - 支持两种稀疏注意力模式：
     - 对于 Qwen2.5 等 dense attention 模型：采用 **Top-K 页面检索机制** 近似全注意力。
     - 对于 GPT-OSS 等原生稀疏模型：直接利用其局部注意力结构。
   - 显著降低计算复杂度和通信开销，同时保持训练有效性。

---

### 🔍 相比现有方法的优势

| 方法 | 激活内存 | KV Cache 管理 | 是否需要多卡 | 最大上下文 |
|------|----------|----------------|---------------|------------|
| Standard Parallel | O(N) | 无优化 | 单卡受限 | ~32K–128K |
| LongLoRA / SeCO | O(N) 或 O(1) | 无有效管理 | 单卡 | ≤128K |
| Context Parallelism (e.g., Ring Attention) | O(N/P) | 分布式存储 | 多卡（8+） | 256K–1M |
| **OOMB (本工作)** | **O(1)** | **分页 + 异步卸载 + 稀疏注意力** | **单卡即可** | **4M+** |

> ✅ **核心优势**：  
> - **单张 H200 GPU 即可训练 4M-token 上下文的 Qwen2.5-7B 模型**，而同类任务通常需数百 GPU 的集群。  
> - 每增加 10K tokens，端到端训练内存仅增加 **约 10MB**，接近常数增长。  
> - 支持灵活配置（chunk size、sparse budget），兼顾效率与精度。

---

## 2. 核心实验方法和设置

### 📚 数据集
- **arXiv dataset**（Soboleva et al., 2023）：用于构建长上下文训练样本。
- 通过对原始文档进行拼接，构造不同长度（从 32K 到 8M tokens）的输入序列。

---

### ⚙️ 实验设置
- **模型**：`Qwen2.5-7B`（Yang et al., 2025）
- **硬件平台**：NVIDIA H200 / A100 GPU，部分实验使用 4×H200 + Tensor Parallelism
- **精度**：bfloat16
- **优化器**：Adam (`lr=5e-5`, `betas=(0.9, 0.98)`）
- **Batch Size**：每 GPU 1 个样本（unit batch）
- **Page Size**：128 tokens
- **Chunk Sizes 测试范围**：512 ~ 4096 tokens

---

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Peak GPU Memory Usage** | 单设备峰值显存占用（MB/GB） |
| **Per-iteration Latency** | 单次迭代耗时（秒） |
| **Throughput (tokens/sec)** | 每秒处理 token 数量 |
| **Gradient Approximation Error (L2 Norm)** | 稀疏注意力带来的梯度近似误差 |
| **Training Loss Curve** | 不同配置下的收敛行为 |

---

### 🔁 基线方法对比
1. **Standard Parallel Training**
   - 使用 FlashAttention + Layer-wise Gradient Checkpointing
2. **Ring Flash Attention (RFA)**（Lin, 2025）
   - 当前最先进的 Context Parallelism 实现
3. **SeCO**（Li et al., 2025）
   - 早期 chunk-wise 训练尝试，缺乏底层优化

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### ✅ 极致内存效率
- **每增加 10K context tokens，内存开销仅上升 ~10MB**
- 在 **4M-token 上下文** 下：
  - 使用稀疏注意力 + CPU 卸载 → 峰值显存稳定在 **~34GB 左右**
  - 而标准并行训练在 128K 就已 OOM

#### ⏱️ 推理与训练速度
| Context Length | OOMB + Dense Attn | OOMB + Sparse Attn | 加速比 |
|----------------|--------------------|---------------------|--------|
| 64K            | ~70s/iter          | ~22s/iter           | **~3.7×** |
| 256K           | ~900s/iter         | ~97s/iter           | **~9.3×** |

> 💡 稀疏注意力显著加速长序列训练，尤其在 >64K 场景下优势明显。

#### 📈 吞吐量对比（vs Context Parallelism）

| Context | RFA (8×A100) | OOMB-Dense (1×H200) | OOMB-Sparse (1×H200) |
|--------|---------------|----------------------|------------------------|
| 64K    | —             | 936.22 t/s           | **1560.38 t/s**        |
| 128K   | —             | 504.12 t/s           | **1394.16 t/s**        |
| 256K   | 49 t/s        | 266.13 t/s           | **1301.60 t/s**        |

> ✅ **OOMB 在单卡上实现了远超多卡 Context Parallelism 的 per-device throughput**

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）Chunk Size 影响（图9）
- 更大的 chunk size（如 4096）能更好利用 GPU 并行能力，提高吞吐。
- 但收益递减，且会轻微增加 activation 内存。
- **推荐默认值：4096**，平衡效率与内存。

#### （2）Sparse Retrieval Budget 影响
- 增大 retrieval budget（如从 4K+512 到 4K+32768）可降低梯度近似误差（L2 error ↓）
- 但在超过原生 context window（如 128K）后，误差差异趋于平缓。
- 表明 **适度稀疏即可维持良好训练质量**

#### （3）CPU Offloading 开销
- 异步卸载机制将传输延迟有效隐藏，**端到端延迟增加 < 5%**
- 使用 pinned memory + DMA 引擎确保高带宽低延迟

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **OOMB 成功将 activation 内存复杂度降至 O(1)**，解决了长上下文训练中最关键的内存瓶颈之一。
2. **通过 paged KV cache + 异步卸载 + page-level sparse attention 的协同设计**，实现了对 KV cache 的高效管理。
3. **首次实现在单张 H200 GPU 上训练 4-million-token 上下文的 LLM**（Qwen2.5-7B），无需依赖大规模集群。
4. **稀疏注意力在训练中是可行且有效的**，在合理 budget 下几乎不损害模型收敛性和最终 loss。
5. **相比 Context Parallelism，OOMB 具有更高的 per-device 效率和更低的部署门槛**，更适合资源受限的研究者。

---

### ⚠️ 局限性
1. **串行 chunk 处理引入额外延迟**，不适合对吞吐极度敏感的大规模预训练。
2. **稀疏注意力是一种近似方法**，可能影响需要全局密集交互的任务表现（如复杂推理）。
3. **异步卸载性能依赖 CPU-GPU 带宽**，在 PCIe 3.0 或低带宽系统中效果下降。
4. 当前验证集中在 fine-tuning 场景，full pretraining 的扩展性有待验证。

---

### 🔮 未来工作方向
- 探索更智能的 **adaptive sparse attention 策略**，动态调整 retrieval budget。
- 结合 **compression 技术**（如量化 KV Cache）进一步减少传输量。
- 扩展至 **multi-modal long-context training** 场景。
- 开发 **自动调优系统**，根据硬件自动选择最优 chunk size、page size 和 sparse budget。

---

> 🔗 **开源信息**：作者已公开源码（GitHub Repository），便于复现与社区拓展。  
> 🌱 **意义**：OOMB 极大地降低了长上下文 LLM 训练的技术与经济门槛，推动了“可持续 AI”与“民主化研究”的发展。

</details>

---

### 2. [Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design](https://arxiv.org/abs/2602.00608)

**Authors**: Wei Zeng, Xuchen Li, Ruili Feng, Zhen Liu, Fengwei An, Jian Zhao  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.00608v1  

#### Abstract
Real-time generative game engines represent a paradigm shift in interactive simulation, promising to replace traditional graphics pipelines with neural world models. However, existing approaches are fundamentally constrained by the ``Memory Wall,'' restricting practical deployments to low resolution...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scalable Generative Game Engine: Breaking the Resolution Wall via Hardware-Algorithm Co-Design

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的**Generative Game Engine**（生成式游戏引擎）虽然在理论上能够通过神经网络直接“生成像素”来模拟游戏世界，但其实际部署受到严重的“**Memory Wall**”（内存墙）限制。现有系统如 Diamond 和 GameNGen 只能在极低分辨率下运行（如 64×64 或 320×240），难以支持标准清晰度（720×480）下的实时交互。

根本瓶颈在于：
- **World Model**（如 DiT）是计算密集型（compute-bound）
- **Decoder**（如 VAE）是内存密集型（memory-bound）
两者资源需求不匹配，导致传统同构硬件架构无法高效扩展。

---

### 🚀 提出的新方法与创新思路

本文提出了一种**可扩展的硬件-算法协同设计框架**（Hardware-Algorithm Co-Design），从系统级解决高分辨率生成难题，三大核心创新如下：

#### （1）异构计算架构与非对称资源分配（Heterogeneous Architecture & Resource Allocation）

- 将 World Model（DiT）和 Decoder（VAE）解耦，分别部署于不同的 AI 加速器上。
- 对 DiT 使用 **Sequence Parallelism**（Ulysses），对 VAE 使用 **Spatial Parallelism**，以适配各自计算特性。
- 建立理论模型优化设备分配比例，在 8 卡集群中得出最优配置为 **5:3**（5 张用于 DiT，3 张用于 VAE），实现吞吐最大化。

> ✅ 优势：避免单一组件成为瓶颈，显著提升整体 pipeline 效率。

#### （2）面向内存的算子融合优化（Memory-Centric Operator Fusion）

- 针对 VAE 解码阶段的“HBM Ping-Pong”问题，提出 **Vertical Fusion**：将 Upsample → Conv2d → GroupNorm → SiLU 融合为单个 kernel，利用片上 SRAM 缓存中间结果，减少 75% 的 off-chip 内存访问。
- 针对 DiT 中 AdaLN 层的小矩阵乘法开销，提出 **Horizontal Fusion**：合并多个小权重矩阵进行一次性大矩阵运算，提升计算密度（arithmetic intensity）至 >85% 峰值性能。

> ✅ 优势：打破“内存墙”，大幅降低延迟并提高带宽利用率。

#### （3）流形感知潜变量外推机制（Manifold-Aware Latent Extrapolation）

- 利用连续帧之间的高时间相关性，在动作稳定时跳过部分 DiT 推理步骤。
- 基于潜空间中的线性运动假设 $ z_{t+Δt} ≈ z_t + Δt \cdot v_t $ 进行预测，仅当输入变化超过阈值时才触发完整推理。
- 结合 **Speculative Action Prefetching**（基于轻量 LSTM 预测用户行为），进一步掩盖 I/O 延迟。

> ✅ 优势：有效解耦感知响应速度与实际生成频率，实现超低感知延迟。

---

### 🔍 相比现有方法的优势

| 维度 | 本工作 | 现有方法（如 GameNGen, Diamond） |
|------|--------|-------------------------------|
| 分辨率 | 支持 **720×480**（SD） | 最高仅支持 320×240 或更低 |
| 实时性 | 达到 **26.4 FPS**（连续域）和 **48.3 FPS**（离散域） | 多数低于 20 FPS |
| 架构灵活性 | 异构并行 + 显式内存管理 | 同构 GPU 平台，依赖隐式缓存 |
| 延迟控制 | 摊销 **Motion-to-Photon Latency = 2.7ms** | 通常 >50ms |
| 扩展能力 | 可扩展至商用加速器集群 | 依赖专用硬件（如 TPU） |

---

## 2. 核心实验方法和设置

### 🧪 数据集与测试环境

使用两个代表性基准构成“连续-离散对偶测试床”（Continuous-Discrete Duality Benchmark）：

| 类型 | 名称 | 描述 |
|------|------|------|
| **连续域** | **The Matrix [9]** | 高保真 3D 赛车模拟器（720×480），强调物理动力学、摩擦力、动量等连续动态 |
| **离散域** | **Playable Game Generation (PGG) [10]** | 2D 平台跳跃游戏（256×256），强调布尔逻辑、碰撞检测、状态跃迁 |

> ⚠️ 注：两个任务均要求模型在无显式规则引擎的情况下维持长期一致性。

---

### 🛠 实验设置

- **硬件平台**：8× Huawei Ascend 910C NPU（每卡 64GB HBM，FP16 算力 ~752 TFLOPS），通过 HCCS 高速环形互连（30 GB/s）
- **软件栈**：CANN 8.0, PyTorch 2.5.1, xfuser（支持 Ulysses 并行）
- **模型结构**：
  - World Model：Diffusion Transformer (DiT)
  - Decoder：Variational Autoencoder (VAE)

---

### 📊 评估指标

| 指标类别 | 具体指标 | 说明 |
|---------|--------|------|
| **性能** | FPS, Motion-to-Photon Latency (M2P) | 衡量实时性和响应速度 |
| **视觉质量** | FID ↓, PSNR ↑, SSIM ↑, LPIPS ↓ | 衡量生成图像的真实感与细节保留 |
| **物理/逻辑一致性** | Control Sensitivity Analysis (CSA), Discrete Logic Boundary (DLB) Score | 检查是否符合物理规律或游戏逻辑 |
| **效率** | Normalized Efficiency (FPS / 100 TFLOPS) | 消除硬件差异后的相对效率比较 |

---

### 🆚 基线方法对比

| 基线系统 | 硬件 | 分辨率 | FPS | Latency |
|--------|------|-------|-----|--------|
| **Diamond [6]** | RTX 3090 | 64×64 | 10.0 | 100 ms |
| **GameNGen [4]** | TPU v5 | 320×240 | >20 | ~50 ms |
| **PGG Baseline*** | RTX 5090 | 256×256 | 29.9 | 33.5 ms |

> ✅ 本文方法在更高分辨率下仍实现更优性能。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

| 指标 | 连续域（Matrix） | 离散域（PGG） |
|------|------------------|--------------|
| **分辨率** | 720×480 | 256×256 |
| **FPS** | **26.4** | **48.3** |
| **摊销 M2P 延迟** | **2.7 ms** | **2.7 ms** |
| **FID** | 42.3 | **28.5** |
| **LPIPS** | 0.087 | **0.052** |
| **DLB Score（逻辑正确率）** | — | **100.0%**（训练分布内） |

> 💡 在连续域中，Yaw Rate 控制敏感性分析显示模型具备“**涌现惯性**”——转向后角速度呈指数衰减，表明其已学习到基本物理动量。

---

### 🔬 消融实验结果（Ablation Study）

| 阶段 | FPS | Speedup |
|------|-----|--------|
| Baseline（单卡顺序执行） | 2.1 | 1.0x |
| + Operator Fusion | 4.5 | 2.1x |
| + Ulysses (3:5) | 16.6 | 7.9x |
| + Optimal 5:3 Partition | 19.4 | 9.2x |
| + Latent Extrapolation | 26.4 | **12.6x** |
| + Speculative Prefetching (93% hit) | 26.4 | **有效延迟降至 2.7ms** |

> ✅ 总体性能提升达 **12.6 倍**，其中：
- 算子融合带来 2.1x 加速
- 异构并行 + 最优资源配置贡献额外 4.4x
- 算法级外推再提速 1.35x

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **“Memory Wall”可通过硬件-算法协同设计突破**  
   单纯软件优化无法解决高分辨率下的带宽瓶颈；必须结合显式内存管理（SRAM）、异构并行和定制化 kernel 设计才能实现可扩展性。

2. **异构架构优于同构方案**  
   将 compute-bound 与 memory-bound 模块分离，并针对性地应用不同并行策略（Sequence vs Spatial），可最大化集群利用率。

3. **高保真与低延迟可以兼得**  
   通过 **Manifold-Aware Latent Extrapolation** 和 **Speculative Prefetching**，系统实现了 **26.4 FPS @ 720×480** 且摊销延迟仅 **2.7ms**，满足人类感知流畅交互的需求。

4. **神经世界模型能自发学习物理规律**  
   在无显式物理求解器的情况下，DiT 成功建模了车辆转向时的惯性效应，验证了“神经物理”的可行性。

5. **逻辑一致性可在训练分布内严格保证**  
   PGG 测试中达到 **100% DLB 正确率**，证明该架构有能力维护复杂的游戏逻辑边界。

---

### ⚠️ 局限性

1. **依赖大规模加速器集群**  
   当前方案需 8 张高端 NPU 才能运行，限制了边缘设备或本地部署的可能性。

2. **Out-of-Distribution（OOD）行为处理不足**  
   虽然训练分布内表现完美，但在面对未见过的操作（如穿墙指令）时可能出现“幻觉”（hallucination），缺乏硬性规则约束。

3. **资源配置依赖模型超参**  
   最优的 5:3 分配基于特定 attention head 数量（H=30），换模型需重新调优。

---

### 🔮 未来工作方向

1. **构建 Hybrid Neuro-Symbolic Engine**  
   引入轻量级符号逻辑层作为“游戏规则监督者”，确保 OOD 场景下的安全性与合法性。

2. **多模态控制接口探索**  
   利用 Transformer 的 cross-attention 能力，支持自然语言、语音等新型输入方式，提升交互沉浸感。

3. **跨平台通用化与编译器支持**  
   将当前优化迁移至 OpenAI Triton 等跨厂商编译器，实现在 NVIDIA H100/NVLink 等平台上的部署。

4. **端侧量化与压缩技术研究**  
   探索 4-bit 权重量化 + 激活平滑等技术，推动高保真神经游戏引擎向消费级 PC 和移动设备下沉。

---

## ✅ 总结

本文首次实现了在 **720×480 分辨率下实时运行的生成式游戏引擎**，通过 **Hardware-Algorithm Co-Design** 成功打破了长期制约该领域的“Memory Wall”。其三大核心技术——**异构架构设计、内存中心化算子融合、流形感知潜变量外推**——共同支撑起一个兼具高保真、低延迟、强一致性的神经仿真系统。

> 🔥 **这不仅是性能的飞跃，更是范式的转变：从“渲染几何”走向“生成世界”**。  
> 未来的游戏或将不再由程序员编写代码构建，而是由神经网络“梦见”而成。

</details>

---

### 3. [Focus-dLLM: Accelerating Long-Context Diffusion LLM Inference via Confidence-Guided Context Focusing](https://arxiv.org/abs/2602.02159)

**Authors**: Lingkun Long, Yushi Huang, Shihao Bai, Ruihao Gong, Jun Zhang, Ao Zhou, Jianlei Yang  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.02159v1  

#### Abstract
Diffusion Large Language Models (dLLMs) deliver strong long-context processing capability in a non-autoregressive decoding paradigm. However, the considerable computational cost of bidirectional full attention limits the inference efficiency. Although sparse attention is promising, existing methods ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Focus-dLLM: Accelerating Long-Context Diffusion LLM Inference via Confidence-Guided Context Focusing**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
Diffusion Large Language Models (dLLMs) 虽然在非自回归解码范式下展现出强大的长上下文处理能力，但由于其**双向全注意力机制**（bidirectional full attention），推理过程计算开销巨大，尤其在长上下文场景下效率严重受限。

现有加速方法存在以下不足：
- **Approximated KV cache**：虽能复用缓存状态，但仍需对完整上下文执行注意力计算。
- **Sparse attention**：依赖当前已解码 token 作为 query 来估计重要性，但在 dLLM 中，待解码位置（即 unmasked 位置）是动态且未知的，导致稀疏策略不准确、效果不佳。

因此，如何**准确预测即将被解码的位置并据此高效剪枝冗余注意力计算**，成为提升 dLLM 推理效率的关键挑战。

---

### **提出的新方法与创新思路**
本文提出了 **Focus-dLLM** —— 一种无需训练的（training-free）、基于置信度引导的上下文聚焦框架，用于加速长上下文 dLLM 推理。

#### **核心创新点：**

1. ✅ **Past Confidence-Guided Indicator（过去置信度引导指示器）**
   - 发现：相邻去噪步骤中，token 的 confidence score 具有强正相关性。
   - 思路：利用前一步骤中高置信度的 masked token 位置，来预测当前步骤将被 unmask 的位置。
   - 实现：选取上一步 top-k 高置信度 token 构成 `Lfocus`，并通过局部窗口扩展形成活跃查询集 `Lactive`，从而缩小注意力计算范围。

2. ✅ **Sink-Aware Pruning Strategy（关注注意力 sink 的剪枝策略）**
   - 发现：dLLM 中存在显著的 **attention sink**（对语义连贯性至关重要的 token），且这些 sink 在不同层之间具有高度一致性（cross-layer consistency）。
   - 思路：
     - 在浅层（dense layers）识别 attention sink，并在整个深层推理过程中复用其位置。
     - 结合 block-wise pruning，仅保留最相关的 prompt 块和所有响应 token，实现高效的稀疏注意力。

3. ✅ **跨层共享 sink 以降低开销**
   - 利用 sink 的跨层稳定性，在中间层识别一次后即可复用于后续各层，避免重复检测带来的额外计算。

---

### **相比现有方法的优势**
| 特性 | Focus-dLLM | Fast-dLLM | Sparse-dLLM | SparseD |
|------|------------|-----------|-------------|---------|
| 是否需要训练 | ❌ No | ❌ No | ❌ No | ❌ No |
| 能否精准定位 unmasked 位置 | ✅ 是（通过 past confidence） | ❌ 否 | ❌ 粗粒度块级估计 | ❌ 固定模式重用 |
| 是否保留 attention sink | ✅ 显式识别并保留 | ⚠️ 未考虑 | ❌ 忽略 | ⚠️ 不显式建模 |
| 是否支持动态稀疏 | ✅ 动态每步更新 | ⚠️ 缓存复用 | ✅ 动态缓存驱逐 | ❌ 早期固定模式 |
| 效率增益 | **最高达 29.6×** | ~9–12× | ~12× | ~1.5× |

> ✅ **优势总结**：Focus-dLLM 实现了更精确的 query 定位 + 更智能的历史 token 保留机制，在几乎无损甚至提升性能的前提下，实现了远超现有方法的推理加速。

---

## **2. 核心实验方法和设置**

### **使用的模型**
- **UltraLLaDA** (He et al., 2025)：专注于长上下文扩展的 dLLM。
- **Dream-7B-Instruct** (Ye et al., 2025)：从自回归模型迁移而来的 dLLM。

### **数据集与基准测试**
- 主要评测基准：**LongBench** (Bai et al., 2024)，涵盖多任务长文本理解：
  - 单文档问答（Qasper, HotpotQA）
  - 多文档问答（MultiFieldQA-en, 2WikiMQA）
  - 摘要生成（GovReport, QMSum）
  - 少样本学习（TREC, TriviaQA）
  - 合成任务（PassageRetrieval）
  - 代码补全（LCC, RepoBench-P）

详细配置见附录 Table 4。

### **评估指标**
| 类型 | 指标 |
|------|------|
| **准确性** | F1, Rouge-L, Accuracy, Edit Sim 等任务特定指标；LongBench 平均得分（Ave. Score） |
| **效率** | 解码吞吐量（Throughput, tokens/s）<br>相对 Vanilla 的 speedup ratio |
| **综合表现** | 准确性 vs. 吞吐量的帕累托前沿（Pareto frontier） |

### **基线方法对比**
| 方法 | 类型 | 关键技术 |
|------|------|----------|
| **Vanilla** | 原始推理 | 无任何优化，全注意力计算 |
| **Fast-dLLM** | KV Cache | 块级近似 KV 缓存复用 |
| **Sparse-dLLM** | Sparse Attention | 动态缓存驱逐 + 块级稀疏 |
| **SparseD** | Sparse Attention | 预计算稀疏模式并跨步重用 |

所有方法统一采用 semi-autoregressive remasking 策略，block size = 32。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### 🔹 **在 32K 上下文长度下的推理速度提升**
- **Focus-dLLM 达到超过 29× 的 lossless 加速**（相对于 Vanilla）。
- 在 UltraLLaDA 上，**比 Fast-dLLM 快 2.05×**（图5左）。
- 随着上下文增长，加速比持续扩大：
  - 8K → 9.4× speedup
  - 32K → **29.6× speedup**

> 💡 原因：越长序列中冗余注意力越多，Focus-dLLM 的剪枝收益越大。

---

#### 🔹 **准确率表现（LongBench 平均得分）**

| 方法 | UltraLLaDA | Dream-7B-Instruct |
|------|------------|-------------------|
| Vanilla | 44.90 | 43.03 |
| Fast-dLLM | 44.74 | 42.75 |
| Sparse-dLLM | 44.86 | 42.78 |
| SparseD | 44.70 | **43.59** |
| **Focus-dLLM** | **45.14** ✅ | 42.82 |

- 在 **UltraLLaDA 上超越所有基线**，包括 Vanilla，说明不仅没损失精度，反而略有提升。
- 在 Dream-7B 上略低于 SparseD，但**换来了高达 19.95× 的加速**，性价比更高。

---

#### 🔹 **Niah 实验（needle-in-a-haystack 检索能力）**
- 在 UltraLLaDA 上进行最大 32K 上下文的检索任务。
- **Focus-dLLM 在深层取得高于 Vanilla 的准确率**，表明其有效保留了关键上下文信息。
- 显著优于 Fast-dLLM 和 Sparse-dLLM。

---

#### 🔹 **消融实验结果（Ablation Study）**

##### 表2：组件消融（UltraLLaDA, 16K context）

| 方法 | Avg. Score | Throughput (tokens/s) |
|------|------------|------------------------|
| Fast-dLLM | 44.74 | 11.03 |
| + PCGI（置信度引导） | 44.23 | 11.37 |
| + Sink-Aware Sparse Attn | 44.84 | **17.68** |
| **Focus-dLLM（完整）** | **45.14** | **17.71** |

- **PCGI 单独使用轻微降准但提速有限**：说明仅筛选 query 不足以大幅提升效率。
- **Sink-aware 稀疏注意力带来显著加速与提准**：证明保留关键历史 token 对性能至关重要。
- **两者结合实现协同增益**：精准 query + 精细 KV 剪枝 = 最佳平衡。

##### 表3：是否保留 attention sink（Dream-7B）

| 子集 | w/o sink | w/ sink | Δ |
|------|----------|---------|----|
| hotpotqa | 37.17 | 38.96 | +1.79 |
| 2wikimqa | 37.68 | 38.56 | +0.88 |
| trec | 69.50 | 70.00 | +0.50 |
| **Avg.** | **41.47** | **42.82** | **+1.35** |

✅ 明确验证：**显式保留 attention sink 可系统性提升性能**。

---

## **4. 关键结论和发现**

### **主要发现**
1. 🧠 **Token confidence 在相邻去噪步间高度相关** → 可用于可靠预测下一步将被解码的位置。
2. 🌀 **Attention sink 在 dLLM 中普遍存在且具有跨层一致性** → 可在浅层识别并在深层复用，大幅减少重复计算。
3. 🎯 **结合 past confidence 引导的 query selection 与 sink-aware KV pruning，可在几乎无损情况下实现极端加速**。
4. 📈 **加速比随上下文长度增长而指数上升**，特别适合超长文本场景（如 32K+）。

---

### **方法的局限性**
1. ⚠️ **目前仅适用于纯文本任务**，尚未拓展至 multimodal（多模态）场景。
2. ⚙️ **超参数为手动设定**（如 `p`, `w`, `ldense`, `α`），缺乏自适应调节机制。
3. 🔄 **依赖 semi-autoregressive remasking 调度器**，可能限制在其他调度策略下的泛化性。

---

### **未来工作方向**
1. 🔮 开发 **完全自适应的参数调整机制**，根据不同输入动态优化 hyperparameters。
2. 🌐 扩展至 **multimodal diffusion models**，探索视觉-语言联合推理中的上下文聚焦。
3. 🤖 探索 **learnable confidence predictor** 或轻量微调版本，进一步提升预测精度。
4. 🧩 将本方法的思想迁移到其他非自回归生成模型（如 insertion-based models）中。

---

> ✅ **总体评价**：  
> Focus-dLLM 是首个成功将 **confidence dynamics** 与 **attention sink consistency** 结合用于 dLLM 推理加速的工作。它以极简设计实现了前所未有的效率突破（>29× speedup），同时保持甚至提升生成质量，为长上下文 dLLM 的实际部署提供了强有力的技术支撑。

🔗 **代码开源地址**：[https://github.com/Longxmas/Focus-dLLM](https://github.com/Longxmas/Focus-dLLM)

</details>

---

### 4. [STILL: Selecting Tokens for Intra-Layer Hybrid Attention to Linearize LLMs](https://arxiv.org/abs/2602.02180)

**Authors**: Weikang Meng, Liangyu Huo, Yadan Luo, Jiawen Guan, Jingyi Zhang, Yingjian Li, Zheng Zhang  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.02180v1  

#### Abstract
Linearizing pretrained large language models (LLMs) primarily relies on intra-layer hybrid attention mechanisms to alleviate the quadratic complexity of standard softmax attention. Existing methods perform token routing based on sliding-window partitions, resulting in position-based selection and fa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**STILL: Selecting Tokens for Intra-Layer Hybrid Attention to Linearize LLMs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前对预训练大语言模型（LLMs）进行线性化（linearization）以降低自注意力机制的 $O(N^2)$ 复杂度时，存在两大核心挑战：
1. **Token 路由策略粗糙**：现有 **intra-layer hybrid attention** 方法（如 LoLCATs、Liger）依赖滑动窗口（sliding window）进行位置固定的 token 分流，将局部 token 送入 Softmax Attention（SA），其余送入 Linear Attention（LA）。这种**基于位置的路由**忽略了 token 内容的重要性，导致关键长距离依赖被错误地送入低保真的 LA 分支。
2. **特征分布偏移（distribution shift）**：Linear Attention 中引入的可学习 feature map（如 MLP）会改变预训练模型中的向量范数（norm），破坏了原始模型“norm-aware”的注意力机制，影响表示一致性。

### 🚀 提出的新方法：STILL
作者提出 **STILL**（Selecting Tokens for Intra-Layer Hybrid Linearization），一种高效的 LLM 线性化框架，包含三大创新：

#### （1）Self-Saliency Score（自显著性评分）
- 一种基于局部滑动窗口计算、但能反映全局重要性的 token 重要性评分机制。
- 利用滑动窗口内是否包含自身 token 的两个注意力分布差异（`diag` vs `nodiag`）来衡量其“自我依赖”程度。
- 高分 token 被判定为“显著”，即使在窗口外也应进入 SA 分支，从而实现**内容感知的 token 路由**。
- 实验证明该分数具有强**局部-全局一致性**，仅用局部计算即可可靠预测全局重要性。

#### （2）Norm-Preserved Feature Map（NP-Map）
- 为解决 LA 中 feature map 扰动预训练 norm 的问题，设计 NP-Map。
- 显式解耦特征的方向与模长：先通过 MLP 变换方向，再将原始输入的 norm 注入回变换后的特征。
- 形式为：  
  $$
  \phi_{\text{NP}}(x) = [\text{softmax}(u) \parallel \text{softmax}(-u)], \quad u = f(x) \cdot \frac{\|x\|}{\|f(x)\|}
  $$
- 保证线性注意力在非负映射的同时，保留预训练模型的表示强度。

#### （3）延迟选择与块级并行（Delayed Selection & Chunk-Wise Parallelization）
- 在推理阶段采用**延迟选择策略**：每处理完一个 chunk 后才统一决定其中哪些 token 进入 SA 缓存。
- 支持训练与推理统一架构，提升硬件效率与并行性。
- 实现真正的线性复杂度 $O(N)$。

### ⭐ 相比现有方法的优势
| 方面 | 传统方法（LoLCATs/Liger） | STILL |
|------|--------------------------|-------|
| Token 路由 | 固定滑动窗口（位置驱动） | 动态选择（内容驱动，Self-Saliency Score） |
| Norm 保持 | 无保护，MLP 改变 norm | 显式保留原始 norm（NP-Map） |
| 架构效率 | 逐 token 路由，低并行 | 块级路由，高并行，训练推理一致 |
| 性能恢复能力 | 长上下文任务严重退化 | 接近甚至超越原模型 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **常识与通用推理任务**：
  - PIQA, ARC-Easy/Challenge, HellaSwag, WinoGrande, MMLU（5-shot）
- **长上下文理解任务**：
  - **RULER**：单针/多针检索（S-NIAH）、多键值检索（MK/MQ/MV）、问答（HQA/SQA）、变量追踪（VT）等，最长达 4K tokens。
  - **BABILong**：bAbI 任务的长上下文扩展，嵌入大量无关文本，测试远距离证据整合能力，最长至 4K tokens。
- **训练数据**：Cleaned Alpaca 数据集（20M tokens，序列长度 1024）

### 🧪 实验设置与评估指标
- **教师模型**：Llama 3 8B / Llama 3.1 8B / Llama 3.2 1B&3B / Mistral 7B
- **训练流程**（两阶段）：
  1. **Attention Transfer**：冻结教师参数，最小化 MSE 损失对齐注意力图。
  2. **Low-Rank Linearization**：使用 LoRA 微调 query/key/value/output 投影及门控参数。
- **评估方式**：zero-shot 或标准 few-shot（如 MMLU 5-shot），使用 `lm-evaluation-harness` 统一评测。
- **硬件效率测试**：在 NVIDIA H200 上测量不同序列长度（1K–64K）下的峰值内存与端到端延迟。

### 🔁 基线方法对比
| 类别 | 方法 |
|------|------|
| **Subquadratic from Scratch** | Mamba, Mamba2, RWKV-6, Hawk, Griffin |
| **Inter-layer Hybrid** | StripedHyena, Zamba, RecurrentGemma |
| **Linearized from Pretrained** | LoLCATs, Liger-GLA, Hedgehog-LoLCATs, SUPRA, T2R |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）常识与通用推理（Llama 3.1 8B 教师）
| 方法 | MMLU (5-shot) | 平均得分（含 MMLU） |
|------|---------------|--------------------|
| 原始 Llama 3.1 8B | 68.0 | 74.2 |
| LoLCATs | 50.8 | 69.9 |
| Liger-GLA | 46.9 | 68.3 |
| **STILL (Ours)** | **61.3** | **72.5** |

> STILL 在不重新预训练的前提下，**接近原始模型性能**，且显著优于所有线性化基线。

#### （2）长上下文任务（4K context）

##### RULER - S-NIAH-1（单针检索）
| 方法（cache ≤512） | 准确率 @4K |
|------------------|-----------|
| LoLCATs | 8.8 |
| Liger-GLA | 0.0 |
| **STILL** | **86.2** |
| Full Attention | 100 |

> STILL 达到 full attention 的 **86.2% 性能**，远超基线。

##### RULER - Extended（综合记忆与状态追踪）
| 方法 | 平均得分 |
|------|---------|
| Mamba2 | 44.7 |
| LoLCATs | 7.2 |
| Liger-GLA | 2.1 |
| **STILL** | **47.9** |

> 表明 STILL 具备更强的**长程状态跟踪与上下文整合能力**。

##### BABILong - QA5（复杂推理）
| 方法（cache=256） | 准确率 @4K |
|------------------|-----------|
| SWA / LoLCATs | 6 |
| **STILL** | **24** |

> 提升高达 **300%**，显示其在复杂推理中有效聚合远距离证据。

#### （3）效率指标
- **训练成本**：仅需 **0.04B 训练 tokens**（相比从头训练 >1000B）。
- **内存减少**：超过 8K tokens 时，平均 **降低 45% 内存**。
- **解码加速**：相同条件下实现 **28% 解码速度提升**。
- **可扩展性**：支持最长 **64K tokens** 推理，而标准 Softmax 在 32K 即 OOM。

### 🔍 消融实验（Ablation Study）
在 BABILong（1K context）上验证模块贡献：

| 配置 | 准确率 | 下降 |
|------|--------|------|
| 完整 STILL | 27.5 | — |
| 移除 Gate | 21.7 | -5.8 |
| 移除 NP-Map | 22.4 | -4.9 |
| 移除两者 | 19.2 | -8.3 |
| 移除 Self-Saliency Score | 12.5 | **-15.0** |

> 结论：**Self-Saliency Score 是最关键组件**，各模块互补，共同提升性能。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **内容感知路由优于位置固定路由**：通过 Self-Saliency Score 实现的动态 token 选择，能更准确识别对推理至关重要的 token（如否定词 not/no/never、情态动词 will/may、量化词 all/any），显著提升长上下文建模能力。
2. **Norm 保持至关重要**：预训练模型的“norm-aware”特性是高性能的关键，NP-Map 成功保留这一属性，避免 LA 引起的表示漂移。
3. **高效线性化可行**：STILL 仅用极少量训练（0.04B tokens），即可将预训练 LLM 高效线性化，在性能、内存、速度之间取得优越平衡。
4. **长上下文能力大幅恢复**：在 RULER 和 BABILong 上，STILL 恢复了以往线性化方法丢失的长程依赖建模能力，达到接近 full attention 的水平。

### ⚠️ 局限性
- 依赖预训练模型的注意力图进行蒸馏，若教师模型本身注意力质量差，则可能限制性能上限。
- Self-Saliency Score 的设计基于注意力对角项敏感性，其理论解释仍有深化空间。
- chunk-wise 设计虽提高效率，但在极端稀疏选择下可能存在缓存管理开销。

### 🔮 未来工作方向
- 将 STILL 扩展至 encoder-decoder 架构（如 T5、BART）。
- 探索更轻量化的 saliency 估计器，进一步降低推理延迟。
- 结合其他高效注意力机制（如 FlashAttention、PagedAttention）优化实际部署。
- 探索 saliency score 在模型压缩、剪枝、激活分析中的通用价值。

---

> **总结一句话**：  
> STILL 通过 **Self-Saliency Score** 实现内容感知的 token 选择，结合 **NP-Map** 保持预训练 norm 结构，并采用 **chunk-wise 并行架构**，实现了高效、高性能的 LLM 线性化，在长上下文任务上显著超越现有方法，为大规模语言模型的实际部署提供了强有力的新方案。

</details>

---

### 5. [Grappa: Gradient-Only Communication for Scalable Graph Neural Network Training](https://arxiv.org/abs/2602.01872)

**Authors**: Chongyang Xu, Christoph Siebenbrunner, Laurent Bindschaedler  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.01872v1  

#### Abstract
Cross-partition edges dominate the cost of distributed GNN training: fetching remote features and activations per iteration overwhelms the network as graphs deepen and partition counts grow. Grappa is a distributed GNN training framework that enforces gradient-only communication: during each iterati...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Grappa: Gradient-Only Communication for Scalable Graph Neural Network Training 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
分布式 GNN 训练面临严重的**通信瓶颈**：随着图规模增大和模型加深，跨分区边（cross-partition edges）导致每轮迭代都需要频繁地从远程节点获取特征和激活值（feature/activation fetches），这在网络带宽上造成巨大压力，严重限制了训练的可扩展性。

现有解决方案如 METIS 分区、NVLink 高速互联、缓存机制等，要么预处理开销大，要么依赖昂贵硬件，且无法从根本上消除通信开销。

### 提出的新方法与创新思路
Grappa 提出了一种全新的分布式 GNN 训练框架，其核心是 **Gradient-Only Communication（梯度仅通信）** 范式：

- **隔离训练（Isolated Training）**：每个分区在每次迭代中独立训练，不进行任何跨分区的特征或激活交换，仅在全局更新阶段同步梯度。
- **动态重分区（Dynamic Repartitioning）**：通过周期性地对图进行重新分区（形成“超级轮次” super-epochs），使每个节点有机会看到不同的邻居集合，从而恢复全局信息覆盖。
- **覆盖率校正梯度聚合（Coverage-Corrected Gradient Aggregation）**：引入基于重要性采样（importance sampling）的轻量级批级别（batch-level）梯度加权机制，补偿因局部训练带来的采样偏差。

### 相比现有方法的优势
- **极致降低通信开销**：完全消除每轮迭代中的跨分区 neighbor traffic，仅保留梯度同步。
- **无需特殊硬件**：不依赖高带宽互联（如 NVLink/RDMA）或复杂缓存系统。
- **模型无关性（model-agnostic）**：支持任意 GNN 架构（如 GraphSAGE, GCN, GAT）。
- **灵活执行模式**：支持 phase-parallel 执行，在单机上也能训练万亿边级别的超大规模图。
- **理论保障**：证明所提出的批级别校正估计器是渐近无偏的，并最小化均方误差。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 节点数 | 边数 | 特征维度 |
|-------|------|--------|--------|----------|
| OGBN-Ar | 学术引用图 | 0.16M | 1.11M | 128 |
| Reddit | 社交网络 | 0.22M | 109.3M | 602 |
| OGBN-Pr | 商品购买图 | 2.34M | 58.99M | 100 |
| OGBN-Pa | 论文引用图 | 105.92M | 1.51B | 128 |
| RMAT-X | 合成图（幂律分布） | $2^X$ | $2^{X+4}$ | 128 |

其中 RMAT-36 包含约 **1.1 万亿条边**，用于测试极限规模下的可行性。

### 实验设置与评估指标
- **硬件环境**：
  - 分布式集群：8 台机器，每台 2×V100 GPU（PCIe），764GB 内存，10Gb/s 网络。
  - 单节点高性能平台：8×H100 GPU（全 NVLink 连接）。
- **训练配置**：
  - Batch size: 1000
  - Hidden dimension: 128
  - Learning rate: 0.003
  - Dropout: 0.5
  - Epochs: 500
- **评估指标**：
  - **系统层面**：平均 epoch 时间、吞吐量、可扩展性、内存占用。
  - **模型层面**：测试准确率（test accuracy）、收敛速度。

### 基线方法对比
| 基线系统 | 类型 | 特点 |
|---------|------|------|
| **DGL** | 分布式采样训练 | 支持 full-graph 和 mini-batch，典型代表 |
| **MGG** | 单节点全图训练 | 利用 NVLink 加速，但无法分布式扩展 |
| **Cluster-GCN** | 预聚类采样 | 通过 METIS 预分组减少通信，但仅适用于 GCN |

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **平均加速比**：相比当前最先进的系统（如 DGL），Grappa 平均实现 **4× 更快** 的训练速度，最高可达 **13× 加速**。
- **可扩展性**：在最多 64 个分区下仍保持近乎线性的扩展效率。
- **超大规模训练能力**：
  - 在单个 V100 机器上以 phase-parallel 模式完成 **RMAT-36（1.1 万亿边）** 上的一轮 Sage-3 训练，耗时 **3.6 小时/epoch**。
  - 表明可在商品化硬件上训练传统方法无法容纳的巨型图。

### 与基线方法的对比结果
#### ✅ vs DGL（分布式训练）
| 指标 | 结果 |
|------|------|
| Epoch time | Grappa 比 DGL 快 1–13×（平均 4×） |
| Test accuracy | 对于深层模型（3–4 层），Grappa 显著优于 DGL（例如 GCN-4 准确率高出 ~20%） |
| 可扩展性 | Grappa 接近线性扩展；DGL 因通信开销增长而扩展性差 |

> **原因**：Grappa 避免了每轮迭代中高昂的远程 fetch 开销。

#### ✅ vs MGG（单节点全图训练）
| 指标 | 结果 |
|------|------|
| 小图性能 | MGG 在小图上更快（利用 NVLink） |
| 大图支持 | MGG 在 OGBN-Pa 上因显存不足崩溃；Grappa 成功运行 |
| 内存效率 | Grappa 不要求将整个图加载进 HBM |

> **结论**：Grappa 更适合多节点、大图场景，不受限于 GPU 显存。

#### ✅ vs Cluster-GCN（预聚类采样）
| 指标 | 结果 |
|------|------|
| Epoch time | Cluster-GCN 更快（约一半时间） |
| Test accuracy | Grappa 更高（+2.7%） |
| 通用性 | Cluster-GCN 仅支持 GCN；Grappa 支持所有主流 GNN 模型 |
| 总体时间 | 若计入预聚类耗时（881 秒），Grappa 总体更优 |

> **意义**：验证了“低通信”设计路径的有效性，同时强调通用性优势。

### 消融实验结果（Ablation Study）
在 Reddit 数据集上对 Grappa 的三个关键组件进行消融分析（Sage-3, 16 分区）：

| 方法 | Test Accuracy |
|------|----------------|
| **Grappa（完整版）** | **0.9589** |
| Grappa-UW（无覆盖率校正） | 0.9636 |
| Grappa-FP（固定分区） | 0.9556 |
| Grappa-50S（50% 远程采样） | 0.9635 |

> ⚠️ 注意：虽然去掉校正后精度略升，但这源于过拟合风险增加。作者指出 Grappa 的隔离训练具有类似 **dropout 的正则化效果**，有助于泛化，尤其在深层模型中表现更佳。

此外，重分区切换开销仅为总训练时间的 **4%–12%**，远小于通信节省的成本。

---

## 4. 关键结论和发现

### 主要发现
1. **梯度仅通信可行且高效**：通过隔离训练 + 动态重分区 + 梯度校正，可以安全移除所有跨分区中间状态通信，极大提升训练效率。
2. **隔离训练有益泛化**：局部训练行为类似于输入层 dropout，能有效防止深层 GNN 过拟合，反而提升测试准确率。
3. **批级别校正足够有效**：提出的 batch-level importance weighting 方法在保持与理想 node-level 修正最小 MSE 偏差的同时，兼容 PyTorch 等主流框架。
4. **phase-parallel 模式突破容量限制**：允许在单机上顺序训练多个分区，实现了 **trillion-edge-scale 图的端到端训练**。

### 方法的局限性
- **重分区带来额外开销**：尽管可控，但在极短 epoch 场景下可能影响效率。
- **依赖 CPU-GPU 数据搬运**：目前采样仍在 CPU 完成，I/O 可能成为潜在瓶颈。
- **理论假设依赖长期覆盖概率为正**：极端不平衡分区可能导致某些邻居长期未被覆盖。

### 未来工作方向
- 设计更智能的 **repartitioning switching heuristic**，根据实际覆盖情况动态调整切换时机。
- 探索 **异步梯度聚合** 或 **联邦学习结合**，进一步降低同步成本。
- 将 phase-parallel 思想推广至其他大规模 ML 模型训练中。

---

> 📌 **总结一句话**：  
> **Grappa 通过“梯度仅通信 + 动态重分区 + 覆盖率校正”的三重机制，在不牺牲模型精度的前提下，将分布式 GNN 训练速度提升 4× 以上，并首次实现在单机上训练万亿边图的可行性，为超大规模图学习提供了新的工程范式。**

</details>

---

### 6. [Dynamic Expert Sharing: Decoupling Memory from Parallelism in Mixture-of-Experts Diffusion LLMs](https://arxiv.org/abs/2602.00879)

**Authors**: Hao Mark Chen, Zhiwen Mo, Royson Lee, Qianzhou Wang, Da Li, Shell Xu Hu, Wayne Luk, Timothy Hospedales, Hongxiang Fan  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2602.00879v1  

#### Abstract
Among parallel decoding paradigms, diffusion large language models (dLLMs) have emerged as a promising candidate that balances generation quality and throughput. However, their integration with Mixture-of-Experts (MoE) architectures is constrained by an expert explosion: as the number of tokens gene...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Dynamic Expert Sharing: Decoupling Memory from Parallelism in Mixture-of-Experts Diffusion LLMs**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**
- 在 **Mixture-of-Experts (MoE)** 架构与 **diffusion LLMs (dLLMs)** 结合的并行解码场景中，存在“**专家爆炸 (expert explosion)**”问题：
  - 随着并行生成的 token 数量 $N$ 增加，每个 token 独立路由导致激活的**唯一专家 (unique experts)** 数量近乎线性增长。
  - 这带来了巨大的 **HBM 到 SRAM 的权重加载开销**，使推理进入 **memory-bound regime**，抵消了 MoE 和并行解码带来的效率增益。

### 🆕 **提出了什么新方法或新思路**
提出 **Dynamic Expert Sharing (DES)**，一种全新的 MoE 优化范式，核心思想是：
- 将优化目标从 **token-level 的稀疏计算** 转向 **sequence-level 的专家共享 (coreset selection)**。
- 动态识别一个紧凑、高实用性的 **专家共用子集 (shared coreset)** 来服务整个并行 token 序列，从而最大化专家复用、最小化内存流量。

具体实现两种策略：
1. **DES-Seq (Intra-Sequence Sharing)**  
   - 对 block 内所有 token 的 Top-k 专家取并集作为共用 coreset。
   - 简单有效，支持向量化执行。

2. **DES-Vote (Saliency-Aware Voting)**  
   - 引入基于 **router 权重聚合投票机制**，让 tokens “集体选举”最重要的专家。
   - 更智能地捕捉序列级语义复杂性，优先保留对整体任务最关键的专家。

### ⚡ **相比现有方法的优势**
- **超越传统 token-centric 方法**：如 NAEE、MC-MoE 等动态跳过方法，在 dLLM 上表现差（准确率下降严重），因其未考虑跨 token 的冗余。
- **真正缓解 memory bottleneck**：通过减少 unique expert load，显著降低 HBM 流量，而非仅节省 FLOPs。
- **解耦内存开销与并行度**：首次实现 memory overhead 不随 block size 显著增长，为大规模并行提供可扩展基础。

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**
在四个需要长文本生成和多样化推理能力的基准上评估：
- **HumanEval**：代码生成
- **MBPP**：Python 编程任务
- **GSM8K**：小学数学应用题
- **MATH500**：高中及以上难度数学问题

### ⚙️ **实验设置**
- **模型**：
  - `LLaDA2.0-Mini` (16B, MoE)
  - `LLaDA-MoE-7B` (7B, MoE)
- **硬件平台**：
  - NVIDIA B200 GPU + Intel Xeon 6960P CPU
  - 使用 CUDA 13.1 和 **Nsight Systems** 进行性能剖析
- **框架**：
  - 基于 `dInfer` 框架，采用 `Fast-dLLM` 的 KV cache 方法
  - 并行 block size 设为 32（含 16 prefix + 16 suffix tokens）

### 🎯 **评估指标**
| 指标 | 描述 |
|------|------|
| **Unique Activated Experts (T)** | 每层平均激活的独特专家数（越低越好） |
| **MoE Kernel Latency** | MoE 层前向传播延迟（ms） |
| **End-to-End GPU Kernel Time** | 整体 GPU 执行时间 |
| **Relative Accuracy (R.Acc %)** | 相对于 vanilla 模型的性能百分比 |
| **Memory Footprint (GB)** | MoE 组件的平均内存占用 |

### 🔁 **基线方法对比**
- **Vanilla MoE dLLM**：标准实现，无优化
- **Top-K (k=4)**：直接减少每 token 激活专家数
- **NAEE**：基于累积概率阈值跳过低分专家
- **MC-MoE**：保留重要 token 的专家
- *注：所有 baseline 均适配至 dLLM 设置以确保公平比较*

---

## 3. **主要实验结果和性能指标**

### 📊 **关键性能数据（来自 Table 1 和 Figure 5）**

#### 在 `LLaDA2.0-Mini` 上的结果：
| 方法 | Avg. T ↓ | Mem. (GB) ↓ | R.Acc (%) ↑ |
|------|--------|------------|-------------|
| Vanilla | 84 | 0.98 | 100.0 |
| NAEE (β=0.6) | 65 | 0.76 | 46.6 |
| DES-Seq (k=3) | 45 | 0.53 | 97.2 |
| **DES-Vote (β=0.15)** | **38** | **0.45** | **99.5** |

#### 在 `LLaDA-MoE-7B` 上的结果：
| 方法 | Avg. T ↓ | Mem. (GB) ↓ | R.Acc (%) ↑ |
|------|--------|------------|-------------|
| Vanilla | 59 | 0.35 | 100.0 |
| MC-MoE (β=0.6) | 53 | 0.31 | 81.5 |
| **DES-Vote (β=0.6)** | **38** | **0.22** | **97.6** |

### 📈 **性能提升总结**
- **专家数量减少**：DES-Vote 最多减少 **55% 的 unique expert 激活**。
- **延迟降低**：
  - MoE kernel latency 下降高达 **38.0%**（LLaDA2.0-Mini）
  - 总 GPU kernel 时间减少 **8.2–14.3%**
- **精度保持极佳**：DES-Vote 保持 **99%+ 的 vanilla 准确率**，部分任务甚至略有提升（可能因正则化效应）。
- **优于所有 baseline**：NAEE/MC-MoE 导致准确率暴跌至 ~46%，而 DES 显著优于 DES-Seq，证明 voting 机制更优。

### 🔍 **消融实验结果**
#### （1）**Coreset Size 影响（Figure 7a）**
- 准确率随 coreset size 减小而缓慢下降。
- DES-Vote 在相同大小下始终优于 DES-Seq，且可通过连续参数 $\beta$ 灵活调节，突破 DES-Seq 的“每 token 至少一专家”限制。

#### （2）**Block Size 鲁棒性（Figure 7b）**
- DES-Vote 在 block size 从 8 到 64 变化时，准确率波动极小。
- 表明其能有效泛化到不同并行度，实现了 **memory overhead 与 parallelism 的解耦**。

#### （3）**Per-Token Computation 影响（Figure 8）**
- 即使 unique expert 数相同，将 per-token K 从 8 降到 4 会导致明显准确率下降。
- 说明 **re-activating 共享专家中的非 top-k 成员仍有助于恢复性能**，验证了 coreset 内再分配的价值。

#### （4）**Custom Kernel 加速效果（Figure 6）**
- 自定义融合 kernel 相比 PyTorch 实现，在 coreset selection 上实现 **6× 速度提升**，消除调度与内存瓶颈。

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **“专家爆炸”是 MoE dLLM 的根本瓶颈**：独立 token 路由导致 unique expert 数随 block size 线性增长，引发严重 memory-bound 问题。
2. **Sequence-level 共享优于 Token-level 稀疏化**：DES 通过构建共享 coreset，显著减少 HBM 流量，带来真实端到端加速。
3. **Saliency-Aware Voting 更优**：DES-Vote 利用 router 权重进行加权投票，比简单取并集（DES-Seq）更能捕获关键专家，实现更高准确率与更低负载。
4. **成功解耦 memory 与 parallelism**：DES 使得内存开销不再随 block size 增长，为未来更大规模并行解码铺平道路。

### ⚠️ **方法的局限性**
- **依赖 router 分布一致性假设**：若 block 内 tokens 语义差异极大（如多任务混合），共享 coreset 可能失效。
- **引入额外控制逻辑开销**：虽然 custom kernel 已优化，但在极小 batch 或低并行度场景下收益可能有限。
- **需调参（如 β 或 k）**：最优 coreset 大小依赖任务和模型，需经验或搜索确定。

### 🔮 **未来工作方向**
- 探索 **adaptive coreset sizing**：根据输入复杂度自动调整 Mcore。
- 扩展至 **multi-modal MoE models**：研究视觉-语言等跨模态场景下的 expert sharing。
- 结合 **offline pruning + online sharing**：形成联合优化 pipeline。
- 推广至其他 **non-diffusion parallel decoding models**，如 speculative decoding 中的 draft model。

---

> **一句话总结**：  
> **DES 提出了一种面向 MoE dLLM 的 sequence-level 动态专家共享机制，通过 DES-Seq 和 DES-Vote 实现专家共用子集选择，在几乎不损失准确率的前提下，最多减少 55% 的 unique expert 激活和 38% 的 MoE 延迟，从根本上缓解了并行解码中的 memory bottleneck。**

</details>

---

### 7. [HyLRA: Hybrid Layer Reuse Attention for Efficient Long-Context Inference](https://arxiv.org/abs/2602.00777)

**Authors**: Xuan Ai, Qingqing Yang, Peng Wang, Lei Deng, Lin Zhang, Renhai Chen, Gong Zhang  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00777v1  

#### Abstract
Long-context inference in Large Language Models (LLMs) is bottlenecked by the quadratic computation complexity of attention and the substantial memory footprint of Key-Value (KV) caches. While existing sparse attention mechanisms attempt to mitigate this by exploiting inherent sparsity, they often r...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HyLRA: Hybrid Layer Reuse Attention for Efficient Long-Context Inference

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在长上下文推理中面临两大瓶颈：
- **Attention机制的二次计算复杂度**（quadratic computation complexity）
- **Key-Value (KV) cache 的巨大内存开销**

现有的稀疏注意力（sparse attention）方法虽然试图通过仅处理高分token来缓解这些问题，但通常存在以下缺陷：
- 采用固定模式（如滑动窗口）或激进剪枝策略，容易丢失关键信息；
- 忽略不同Transformer层之间的结构差异，统一应用相同的稀疏化策略；
- 每一层独立进行重要性评分与排序，带来重复计算开销。

### 提出的新方法与思路
本文提出 **HyLRA (Hybrid Layer Reuse Attention)**，一种基于**层间相似性分析**的混合注意力框架。其核心思想来源于对注意力行为的实证分析，发现了两个关键特性：

- **Intra-layer sensitivity（层内敏感性）**：某些层对近似误差高度敏感，必须保留 full attention 以防止特征失真。
- **Inter-layer similarity（层间相似性）**：相邻层之间共享大量关键token，表明注意力分布具有局部稳定性。

基于此，HyLRA 设计了一种**离线动态规划（offline dynamic programming）算法**，为每一层生成最优执行策略：
- 在“敏感层”执行标准 full attention；
- 在“容忍层”直接复用前一个敏感层的 top-k token 索引，跳过冗余的 token selection 和 softmax 计算。

该方法实现了：
- **计算效率提升**：避免每层重复计算 attention scores；
- **精度保障**：在关键层保持 full precision；
- **灵活性**：支持 token-level 和 block-level sparsity。

### 相比现有方法的优势
| 方面 | HyLRA | 其他方法（如 Jump3、H2O、StreamingLLM） |
|------|-------|----------------------------------------|
| 层感知能力 | ✅ 显式建模层异质性 | ❌ 多数为 layer-agnostic |
| 冗余计算消除 | ✅ 利用 inter-layer 相似性复用索引 | ❌ 每层独立选择 top-k |
| KV Cache 安全性 | ✅ 不丢弃任何 KV 缓存项 | ❌ 部分方法永久删除 token 导致上下文丢失 |
| 性能-效率权衡 | ⭐ 达成更优的 Pareto frontier | ⚠️ 往往牺牲显著准确率换取速度 |

---

## 2. 核心实验方法和设置

### 数据集
- **LongBench v2**（Bai et al., 2024）：综合性长上下文评测基准，涵盖六类任务：
  - Single-document QA
  - Multi-document QA
  - Long-dialogue History Understanding
  - Long Structured Data Understanding
  - Long In-context Learning
  - Code Repository Understanding
- 包含多种难度等级（Easy / Hard）和上下文长度（Short / Medium / Long），最大可达 **60K tokens**

### 模型
- **DeepSeek-R1**：高性能推理导向模型
- **QWQ-32B-W8A8**：Qwen 模型的量化版本（8-bit weights & activations），用于测试内存受限场景

### 实验设置
- 所有实验运行于配备 **8块加速器设备** 的高性能节点，总显存达 **768GB HBM3**
- 上下文长度范围：从 **8K 到 60K tokens**
- Top-k 预算固定为 **2,048 tokens**

### 评估指标
| 指标 | 描述 |
|------|------|
| **Performance (Score)** | 各任务平均得分，衡量生成质量 |
| **Time to First Token (TTFT)** | 首个 token 生成延迟 |
| **Generation Throughput** | 单位时间生成 token 数量（tokens/s） |
| **Overall Throughput** | 包括预填充和解码阶段的整体吞吐量 |
| **Speedup** | 相对于 full attention 的加速比 |

### 基线方法对比
- **Full Attention**：标准 dense attention，作为性能上限
- **Jump3**：静态跳跃策略，每3层执行一次 full attention，其余层复用索引（HyLRA 的非自适应变体）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 3）

#### 📊 综合性能比较（LongBench v2 平均得分）
| Model | Method | Overall Score |
|-------|--------|----------------|
| DeepSeek-R1 | Full | 44.33 |
| DeepSeek-R1 | Jump3 | 42.54 |
| DeepSeek-R1 | **HyLRA** | **46.32** ✅ |
| QWQ-32B-W8A8 | Full | 45.73 |
| QWQ-32B-W8A8 | Jump3 | 40.76 |
| QWQ-32B-W8A8 | **HyLRA** | **45.03** ✅ |

> 💡 **结论**：HyLRA 不仅优于所有 sparse 方法，甚至在多个任务上**超过 full attention**，说明其能有效过滤噪声并聚焦关键上下文。

#### ⏱ 效率表现（Sequence Length = 60K）
| 指标 | Full | Jump3 | **HyLRA** |
|------|------|--------|----------|
| TTFT (ms) | 57.69 | 53.98 | 56.62 |
| Generation Throughput | 177 | 273 | **258** |
| Overall Throughput | 2835 | 4730 | **4128** |
| Speedup vs Full | 1.00x | 1.54x | **1.45x** |

> 🔺 **加速效果随序列增长而增强**：在 8K 时仅快 6%，但在 60K 时达到 **1.45× 整体加速**

### 与基线方法对比结果
- **相比 Jump3**：
  - 在相同 top-k 预算下，HyLRA **平均高出 3–5 分**；
  - 虽然 Jump3 吞吐更高（因更频繁跳过 full attention），但**代价是显著性能下降**；
  - HyLRA 通过动态规划找到更优路径，在**效率与准确性间取得更好平衡**。
- **相比 full attention**：
  - 几乎无损性能（<1% accuracy degradation）；
  - 实现高达 **46% 的推理吞吐提升**（见摘要）。

### 消融实验（隐含分析）
尽管未明确列出消融表，文中通过以下方式验证设计有效性：
- **敏感性分析图（Figure 1）**：证明 Layer 0 对稀疏化极度敏感（RNMSE=0.25），而 Layer 7 可安全稀疏（RNMSE=0.016）→ 支持 layer-adaptive 策略必要性。
- **层间重叠热力图（Figure 2）**：显示连续层间 top-k token 重叠率高 → 支持 index reuse 合理性。
- **动态规划路径可视化（Figure 3）**：展示如何通过 reset jump 和 vertical move 构建最优策略。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM 各层对注意力稀疏化的容忍度存在显著差异**：
   - 浅层（如 Layer 0）通常是敏感层，需 full attention；
   - 深层多为容忍层，可高效复用前层索引。

2. **注意力模式在相邻层间具有强一致性**：
   - top-k token 集合高度重叠，支持跨层索引复用。

3. **统一稀疏策略是次优的**：
   - 固定间隔跳过（如 Jump3）无法适应模型内部异构性；
   - HyLRA 的 profile-driven 动态策略能实现全局最优配置。

4. **无需丢弃 KV cache 即可实现高效推理**：
   - 传统方法依赖 cache eviction，风险高；
   - HyLRA 保留完整 KV cache，仅减少访问量，更安全可靠。

### 方法的局限性
- **依赖离线 profiling**：需要预先运行 full attention 获取各层 attention 分布，增加部署前成本；
- **假设 attention pattern 稳定**：若输入引起剧烈变化，reuse 可能失效；
- **不适用于极短上下文**：在 8K 以下优势不明显，更适合超长上下文（>30K）场景。

### 未来工作方向
- **在线自适应策略**：结合运行时反馈动态调整 reuse 策略，减少对离线 profile 的依赖；
- **扩展至 vision-language 模型**：探索多模态场景下的跨层注意力复用；
- **硬件协同优化**：进一步结合 GPU memory hierarchy，提升 block-level reuse 的带宽利用率；
- **支持训练阶段应用**：研究是否可在训练中引入类似机制以降低开销。

---

> ✅ **总结一句话**：  
> **HyLRA 通过洞察 LLM 中 attention 的层内敏感性与层间相似性，提出一种 hybrid layer-wise reuse 机制，在几乎不损失精度的前提下，实现高达 1.45× 的长上下文推理加速，显著优于现有 sparse attention 方法。**

</details>

---

### 8. [PROBE: Co-Balancing Computation and Communication in MoE Inference via Real-Time Predictive Prefetching](https://arxiv.org/abs/2602.00509)

**Authors**: Qianchao Zhu, Xucheng Ye, Yuliang Liu, Haodong Ouyang, Chengru Song  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2602.00509v1  

#### Abstract
Mixture-of-Experts models have become a dominant architecture for scaling Large Language Models by activating only a sparse subset of experts per token. However, latency-critical MoE inference faces a fundamental tension: while expert parallelism improves memory efficiency, it also amplifies executi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：PROBE: Co-Balancing Computation and Communication in MoE Inference via Real-Time Predictive Prefetching**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**

Mixture-of-Experts (MoE) 模型在推理阶段面临严重的**计算与通信双重瓶颈**，尤其是在延迟敏感的在线服务场景中：

- **空间不平衡（Spatial Imbalance）**：由于输入语义差异，某些专家被频繁激活，形成“热点”（hotspots），导致部分 GPU 负载过重。
- **时间波动性（Temporal Volatility）**：连续批处理（continuous batching）下请求动态加入和退出，使得专家负载分布快速变化，静态或基于历史统计的平衡策略失效。
- **双惩罚效应（Double Penalty）**：热点 GPU 同时承受**计算负载过高**和**All-to-All 通信拥塞**，严重拖慢整体推理速度。

现有方法如专家复制（expert replication）、动态卸载（dynamic offloading）等存在以下缺陷：
- 内存开销大，与 KV Cache 竞争；
- 反应式调整滞后于负载突变；
- 控制开销暴露在关键路径上，破坏 CUDA Graph 加速；
- 需要训练时修改路由机制，违反语义正确性。

---

### 🚀 **提出了什么新方法或新思路**

作者提出 **PROBE** —— 一种面向 MoE 推理的实时协同优化系统，其核心是 **Continuous Lookahead Pipelining** 架构，将控制流（预测、规划、预取）完全隐藏在主执行流之外。

#### 主要创新点包括：

1. **Gate-Initialized Lookahead Predictor（门控初始化前向预测器）**
   - 利用目标层冻结的 router 参数作为先验，结合轻量级可训练残差 MLP，基于前一层隐藏状态预测下一层专家激活模式。
   - 实现约 **90% 的 Top-K 准确率**，且开销极低。

2. **Hardware-Aware Balance Planning Solver（硬件感知负载均衡规划器）**
   - 将专家复制决策建模为资源分配问题，在满足“隐藏窗口”约束的前提下，动态决定是否复制及如何分配 token。
   - 引入贪心再平衡算法（Greedy Rebalancing），确保传输时间不超过当前层计算时间，避免阻塞关键路径。

3. **Phase-Locked Co-Scheduling Policy（相位锁定协同调度策略）**
   - 设计双轨执行架构（Deterministic Track + Control Plane）：
     - **预测与规划阶段**：在 All-to-All 通信期间并行运行；
     - **专家预取阶段**：通过 split-phase transmission 技术，利用 MoE 计算和 Attention 阶段的带宽空窗进行 P2P 权重传输，避开 All-to-All 通信高峰。
   - 完全消除控制开销对主流程的影响，并保证与 CUDA Graph 兼容。

---

### 🔍 **相比现有方法的优势**

| 维度 | 现有方法（如 DeepSeek-EPLB, Grace-MoE） | PROBE |
|------|----------------------------------------|-------|
| **响应方式** | 反应式（依赖历史统计） | **前向式（实时预测）** |
| **更新频率** | 单次或低频重平衡 | **每层持续预测与更新** |
| **内存效率** | 固定冗余槽位，浪费显存 | **动态复用 slot，循环利用** |
| **关键路径影响** | 存在暴露开销，破坏图捕获 | **零暴露开销，兼容 CUDA Graph** |
| **适应能力** | 对突发语义漂移不鲁棒 | **高动态负载下仍稳定高效** |

---

## 2. **核心实验方法和设置**

### 📚 **使用的数据集**

- **Chinese**：来自 Alpaca-zh 等开源中文语料；
- **Code**：来自 CodeAlpaca20k 和 OpenAI-HumanEval；
- **Repeat（合成数据集）**：重复少量 prompt 以模拟极端专家倾斜场景，用于压力测试。

---

### ⚙️ **实验设置**

- **硬件平台**：8×NVIDIA Hopper GPU（H100级别），NVSwitch 互联（900 GB/s）；
- **模型配置**：
  - **GPT-OSS-120B**：128 个专家，Top-4 激活，BF16；
  - **Qwen3-MoE-235B**：128 个专家，Top-8 激活，BF16；
- **并行策略**：Attention 使用 DP，MoE 层使用 EP（Expert Parallelism），`ep=8`；
- **实现框架**：基于 SGLang + DeepEP 构建，支持 CUDA Graph；

---

### 📊 **评估指标**

| 指标 | 描述 |
|------|------|
| **Prefill Latency (TTFT)** | Time-To-First-Token，衡量首 token 延迟 |
| **Decoding Throughput** | 解码阶段平均吞吐量（tokens/s） |
| **Imbalance Ratio (IR)** | 最大负载 / 平均负载，反映负载偏斜程度 |
| **End-to-End Latency Breakdown** | 微操作级时间线分析（Dispatch, Compute, Combine, Prefetch 等） |
| **Predictor Accuracy** | Top-K、Top-Half-K Hit Rate、Recall@2× 等 |

---

### 🆚 **基线方法对比**

| 基线 | 特点 |
|------|------|
| **SGLang (Default EP)** | 静态分片放置，无动态平衡 |
| **DeepSeek-EPLB** | 基于历史统计的负载均衡，每层预留 2 个冗余 slot，一次重平衡 |

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据**

#### ✅ **Prefill 阶段加速效果**

- 在 `GPT-OSS-120B` 上，PROBE 相比 SGLang 最多降低 **32% 的 TTFT**（即 **1.32× 更快**）；
- 在高偏斜的 `Repeat` 数据集上优势更明显，因能及时应对突发热点；
- EPLB 未参与此比较，因其在 Prefill 阶段易触发 OOM 或无法完成重平衡。

#### ✅ **Decoding 阶段吞吐提升**

- 相比 EPLB，在相同 batch size 下最高提升 **26% 吞吐量**（**1.26×**）；
- 在 `Repeat` 数据集中，EPLB 出现显著延迟尖峰，而 PROBE 保持平稳；
- 在 `Chinese` 和 `Code` 切换的压力测试中，PROBE 无需 warm-up 即维持高性能。

#### ✅ **负载均衡改善**

- 平均 Imbalance Ratio 从 **2.13 → 1.09**；
- 计算延迟的最大/平均比从 **2.27 → 1.18**，显著减少同步等待时间；
- Combine 阶段延迟下降主要源于 straggler 消除，而非通信本身加速。

#### ✅ **预测器精度**

- Top-K 准确率达 **87–94%**，远高于仅使用冻结 router 的 ~75%；
- Top-Half-K Hit Rate 和 Recall@2× 接近 **100%**，表明预测高度可靠，极少漏报重要专家。

#### ✅ **控制开销隐藏验证**

- 图 11 显示：**Predict、Plan、Prefetch 全部被成功隐藏**在非关键资源周期内：
  - Predict + Gather 与 Dispatch 并行；
  - Planner 与 MoE Compute 重叠；
  - Prefetch 分两阶段嵌入 MoE Compute 和下一层 Attention 中；
- 整体未引入额外 idle 时间。

---

### 🔬 **消融实验与关键设计验证**

虽然文中未明确列出独立消融表格，但从多个实验可推断出各组件必要性：

| 组件 | 移除后果 |
|------|---------|
| **Lookahead Predictor** | 若依赖历史统计（如 EPLB），在语义切换后性能骤降（见 Fig.9） |
| **Hardware-Aware Planning** | 若不限制 transfer volume，会超出 hiding window，造成阻塞 |
| **Split-Phase Prefetching** | 若在 All-to-All 期间传输权重，将加剧网络拥塞 |
| **Online Distillation** | 仅用冻结 router 准确率下降约 15%，导致无效预取增多 |

---

## 4. **关键结论和发现**

### ✅ **主要发现**

1. **MoE 推理瓶颈本质是“时空耦合”的负载失衡**：
   - 不仅是静态偏斜，更是**语义驱动下的高频迁移热点**；
   - 必须同时解决空间 straggler 和时间 volatility。

2. **前向预测优于反应式调整**：
   - 利用 Transformer 层间隐藏状态连续性，可以高精度预测下一专家分布；
   - “预测 + 规划 + 预取”流水线可在不影响主流程前提下实现近实时重配置。

3. **真正的零开销平衡需软硬协同设计**：
   - 必须结合硬件特性（compute/bandwidth ratio）来限制复制规模；
   - 调度必须与执行相位对齐，才能实现完全隐藏。

4. **CUDA Graph 兼容性至关重要**：
   - 动态逻辑若引入 host-device 同步或变量控制流，将破坏图捕获；
   - PROBE 通过 GPU-native solver 和固定 kernel pattern 成功规避。

---

### ⚠️ **方法的局限性**

1. **依赖 router 可导出与冻结**：
   - 需访问原始模型的 gate 参数，可能不适用于黑盒部署场景。

2. **预测误差仍存在**：
   - 虽然 Top-K 准确率高，但在极端分布漂移下仍有误判风险；
   - 当前方案未引入 fallback 机制处理预测失败。

3. **扩展到更大规模集群尚待验证**：
   - 实验限于单节点 8 GPU，跨节点 P2P 传输延迟更高，可能压缩 hiding window。

4. **仅针对推理优化，未覆盖训练场景**。

---

### 🔮 **未来工作方向**

1. **将 lookahead 执行推广至其他子系统**：
   - 如提前预加载 KV Cache 分布、预分配内存块等；
   - 构建统一的“预测驱动执行”范式。

2. **结合 disaggregation 架构**：
   - 在专家分离存储（disaggregated storage）系统中，提前拉取远程专家参数。

3. **增强预测鲁棒性**：
   - 引入不确定性估计或自适应校准模块，动态调整复制激进程度。

4. **支持更多 MoE 变体**：
   - 如 Hierarchical MoE、Soft MoE、Routing Networks with Memory 等复杂路由结构。

---

> **总结一句话**：  
> PROBE 通过 **real-time predictive prefetching + phase-locked co-scheduling**，首次实现了在不影响关键路径的前提下，对 MoE 推理中的计算与通信进行**实时协同平衡**，显著提升了延迟和吞吐性能，尤其在高动态负载下表现卓越，为万亿参数 MoE 模型的高效部署提供了新范式。

</details>

---

### 9. [Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization](https://arxiv.org/abs/2602.01090)

**Authors**: Yang Liu, Chuan Zhou, Yancheng Chen, Shuai Zhang, Xixun Lin, Xiaoqing Wang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01090v1  

#### Abstract
Large language models (LLMs) have emerged as promising general-purpose solvers for combinatorial optimization (CO), yet they fundamentally lack mechanisms to guarantee solution feasibility which is critical for real-world deployment. In this work, we introduce FALCON, a framework that ensures 100\% ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Hard Constraints Meet Soft Generation: Guaranteed Feasibility for LLM-based Combinatorial Optimization**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
大型语言模型（LLMs）在组合优化（Combinatorial Optimization, CO）任务中展现出强大的模式识别和序列生成能力，但其本质是**无约束的生成模型**，无法保证输出满足硬性约束条件（如容量限制、哈密顿路径、独立集等）。这导致生成的解常常不可行（infeasible），严重制约了其在物流、制造、应急调度等安全敏感领域的实际部署。

现有方法通常通过奖励塑形（reward shaping）或后处理采样（rejection sampling）来“鼓励”可行性，但这些方法只能提供概率性保障，**无法实现100%可行性**。

### **提出的新方法：FALCON**
本文提出了 **FALCON**（Feasibility-Aware Language-based Combinatorial Optimization with Adaptive Inference），首个能**理论保证100%可行性**的LLM-based CO框架，其核心创新在于三层协同机制：

1. **Grammar-Constrained Decoding（语法约束解码）**  
   - 使用**上下文无关文法**（CFG）定义每个CO问题的有效输出格式（如括号匹配、节点索引范围）。
   - 在解码时通过**下推自动机**（PDA）动态屏蔽非法token，确保输出**语法正确**（syntactically valid）。

2. **Feasibility Repair Layer（可行性修复层）**  
   - 设计**问题特定的修复算子**（repair operator），将任何违反语义约束的解映射到可行域。
   - 修复算子满足三大性质：
     - **可行性**（Feasibility）：输出必为可行解。
     - **幂等性**（Idempotence）：输入已可行则不修改。
     - **有界局部性**（Bounded Locality）：修复代价与违反程度成正比。
   - 例如：TSP中移除重复节点并插入缺失节点；CVRP中拆分超载路线。

3. **Adaptive Best-of-N Sampling（自适应多采样）**  
   - 动态调整采样数量 $N$，基于**解一致性**（solution consistency）估计实例难度。
   - 高一致性（模型自信）→ 少采样早终止；低一致性（模型不确定）→ 多采样探索。
   - 显著提升推理效率。

此外，作者还提出了新的训练算法 **BOPO**（Best-anchored Objective-guided Preference Optimization）：
- 利用**目标值差距**（objective gap）加权偏好对，提供更密集的监督信号。
- 优于传统RLHF或GRPO方法，收敛更快且质量更高。

### **相比现有方法的优势**
| 维度 | 现有方法 | FALCON |
|------|--------|--------|
| **可行性保障** | 软约束，<100% | **理论保证100%** |
| **解码控制** | 无或简单mask | **CFG + PDA精确控制** |
| **训练信号密度** | 稀疏奖励或平均优势 | **目标差距加权，信息更丰富** |
| **推理效率** | 固定采样数 | **自适应分配计算资源** |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在**七个NP-hard CO问题**上进行评估，覆盖三大领域：

| 问题 | 类型 | 规模 | 分布 | 参考求解器 |
|------|------|------|------|------------|
| TSP | 路由 | 10–100节点 | Uniform, GM | LKH-3 |
| OP (Orienteering Problem) | 路由 | 10–100节点 | Uniform, GM | COMPASS |
| CVRP | 路由 | 10–100节点 | Uniform, GM | LKH-3 |
| MIS | 图论 | 50–500节点 | ER, BA | Gurobi |
| MVC | 图论 | 50–500节点 | ER, BA | Gurobi |
| PFSP | 调度 | 10–100作业 | Taillard | QIG |
| JSSP | 调度 | 6–30作业 | Taillard | OR-Tools |

- **训练集**：各问题50万实例。
- **测试集**：各问题100个实例。

### **实验设置和评估指标**

#### **评估指标**
1. **可行性率**（Feasibility Rate, Fea.%）：满足所有约束的解的比例。
2. **最优性差距**（Optimality Gap, Gap%）：$(f(\hat{x}) - f(x^*)) / f(x^*) \times 100\%$，越低越好。
3. **推理时间**（Time/s）：平均每实例耗时。

#### **FALCON配置**
- **最小采样数** $N_{\min}=8$
- **最大采样数** $N_{\max}=64$
- **置信阈值** $T=0.85$
- **采样温度** $\tau=0.7$

#### **基线方法对比**
分为四类：

1. **通用LLM（零样本）**  
   - GPT-4o, GPT-4o-mini, Claude-3.5-Sonnet/Haiku, DeepSeek-V3, Llama-3.3-70B, Qwen2.5-72B

2. **推理增强LLM**  
   - GPT-o3-mini, GPT-o1, DeepSeek-R1

3. **LLM优化方法**  
   - OPRO, LMEA, PHP, SGE

4. **神经CO求解器**  
   - SFT Only, GRPO, LLMCoSolver (N=1/N=8)

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（来自Table 2）**

| 方法 | 平均可行性 | 平均Gap% | 最优性 |
|------|-----------|---------|-------|
| **FALCON (Adaptive)** | **100%** | **0.92%** | ✅ 最佳 |
| FALCON (N=8) | 100% | 0.95% | — |
| LLMCoSolver (N=8) | 94–100% | 1.07–8.20% | — |
| GRPO | 91–98% | 1.34–4.25% | — |
| GPT-4o | 6–88% | 20.57–306% | ❌ |

> **FALCON在所有7个问题上均达到100%可行性，且最优性差距显著优于所有基线。**

### **与基线方法的对比结果**
- **相比通用LLM**：FALCON的Gap降低**数十倍**（如TSP从33.79%降至0.92%），可行性从<100%提升至**确定性100%**。
- **相比神经求解器**：在CVRP、MIS等复杂问题上，FALCON的Gap仍优于LLMCoSolver (N=8)，同时保证100%可行性。
- **与领域启发式对比**（Table 3）：
  - FALCON在大规模TSP上表现稳定（Gap=1.08%），而ACO退化至36.69%。
  - 在JSSP上，FALCON (Adaptive) Gap=6.98%，远优于FIFO（39.00%）。

### **消融实验结果（Table 4）**

| 配置 | TSP Gap | CVRP Gap | 可行性 |
|------|--------|---------|--------|
| **FALCON (Full)** | 0.92% | 3.52% | 100% |
| w/o Grammar | 0.95% | 3.67% | 100% |
| **w/o Repair** | 0.89% | 3.41% | **94%/85%** |
| w/o Adaptive | 0.91% | 3.48% | 100% |
| **w/o BOPO** | 1.85% | 5.89% | 100% |
| SFT Only | 2.30% | 6.02% | 89%/59% |

> **关键发现**：
> - **修复层**是实现100%可行性的**关键**，移除后可行性骤降。
> - **BOPO**对解的质量影响最大，移除后Gap翻倍。
> - 自适应采样节省40%以上计算量，几乎无质量损失。

---

## **4. 关键结论和发现**

### **主要发现**
1. **100%可行性可被严格保证**：通过**语法约束 + 修复算子**的两阶段设计，FALCON首次实现了LLM-based CO的**确定性可行性**。
2. **可行性与高质量可兼得**：尽管引入修复可能略微劣化解，但**BOPO训练出的模型本身高度可行**，修复频率低（2.8%–18.5%），因此质量损失极小。
3. **自适应采样高效智能**：基于一致性（consistency）的难度估计能有效分配计算资源，**平均减少58%采样数**，推理时间下降55%。
4. **BOPO优于GRPO**：目标差距加权使梯度更新更具信息量，在困难问题（如CVRP、MIS）上提升显著（图2显示最高29% Gap下降）。

### **方法的局限性**
1. **修复算子需问题定制**：虽然框架通用，但修复逻辑需针对每类问题手工设计，**泛化到全新问题类型需额外开发**。
2. **依赖高质量训练数据**：SFT阶段依赖专家求解器（如LKH、Gurobi）生成的高质量解，若参考解质量差，会影响最终性能。
3. **修复可能引入次优性**：虽然理论上界存在，但在极端情况下修复可能导致较大质量损失（尽管实践中罕见）。

### **未来工作方向**
1. **自动化修复算子生成**：探索如何从问题描述自动合成修复逻辑，提升框架通用性。
2. **端到端可微修复**：研究是否可将修复过程嵌入模型内部，实现可微训练。
3. **扩展至带不确定性或动态CO问题**：如动态车辆路径问题（DVRP）、随机调度等。
4. **结合形式验证**：利用FALCON的确定性特性，进一步集成形式化方法以提供更强的安全保障。

---

> **总结**：FALCON通过“**硬约束 + 软生成**”的分离思想，成功解决了LLM在组合优化中**可行性不可控**的根本难题，为LLM在现实世界高可靠性场景中的部署提供了坚实基础。

</details>

---

### 10. [AREAL-DTA: Dynamic Tree Attention for Efficient Reinforcement Learning of Large Language Models](https://arxiv.org/abs/2602.00482)

**Authors**: Jiarui Zhang, Yuchen Yang, Ran Yan, Zhiyu Mei, Liyuan Zhang, Daifeng Li, Wei Fu, Jiaxuan Gao, Shusheng Xu, Yi Wu, Binhang Yuan  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.00482v1  

#### Abstract
Reinforcement learning (RL) based post-training for large language models (LLMs) is computationally expensive, as it generates many rollout sequences that could frequently share long token prefixes. Existing RL frameworks usually process these sequences independently, repeatedly recomputing identica...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AREAL-DTA: Dynamic Tree Attention for Efficient Reinforcement Learning of Large Language Models

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在基于 **Reinforcement Learning (RL)** 对 **Large Language Models (LLMs)** 进行后训练（post-training）时，系统会生成大量 rollout 序列。这些序列通常共享很长的前缀（如相同的 prompt 或初始对话），但传统 RL 框架将每个序列独立处理，导致对相同前缀进行重复的 **forward 和 backward 计算**，造成严重的计算和内存浪费。

此外，虽然前缀共享天然形成一棵 **prefix tree**，但现有的 **tree attention** 方法需要完全物化（fully materialize）整个 attention mask，其计算和内存开销随总 token 数量呈 **二次方增长**，难以扩展到大规模 RL 场景。

---

### 🚀 提出的新方法与创新思路

论文提出 **AREAL-DTA**（Dynamic Tree Attention over AReaL），一种高效利用前缀共享、提升 RL 训练效率的新框架，核心创新如下：

#### **Contribution 1: DFS-based 动态树遍历机制**
- 将所有 rollout 序列组织成一棵 **prefix tree**。
- 采用 **Depth-First Search (DFS)** 策略动态遍历该树，在 **forward 和 backward 过程中仅维护一条从根到叶的路径**（包括 token 和 KV Cache）。
- 在访问叶节点时立即计算 loss 并反向传播，随后弹出分支，释放非共享部分的激活状态。
- **优势**：
  - 内存占用仅与最长序列长度成正比（而非所有 token 总数），避免了内存爆炸。
  - 共享前缀的计算和梯度仅执行一次，实现高效复用。

#### **Contribution 2: 负载均衡的分布式批处理机制（Load-Balanced Distributed Batching）**
- 在多 GPU 异步训练场景下，设计了一种 **基于 DFS 顺序的连续分组策略**，将 rollout 分配到多个 trainer GPU。
- 通过 **binary search + 贪心扫描** 实现最小化最大组成本的划分，确保各 GPU 负载均衡。
- **优势**：
  - 最大限度保留前缀共享（减少跨 GPU 前缀复制）。
  - 减少 GPU 空闲时间，提升整体吞吐。

#### **Contribution 3: 多项系统级优化**
- **Chunked Backpropagation**：对长序列分块反向传播，进一步控制内存。
- **Avoid KV Cache for Leaf Nodes**：不为叶节点缓存 KV，减少冗余计算。
- **Greedy DFS 遍历顺序优化**：优先处理更长分支，提升内存和计算效率。

---

### 🔍 相比现有方法的优势

| 方法 | 缺陷 | AREAL-DTA 的改进 |
|------|------|------------------|
| Vanilla RL Training | 无前缀共享，重复计算严重 | 显著减少冗余计算 |
| Tree Training [5] | 需存储整棵树的物化结构，内存开销大 | 动态 DFS 遍历，仅维护当前路径，内存低 |
| Full Tree Attention | attention mask 二次增长，不可扩展 | 无需完整 mask，计算轻量 |

> ✅ **AREAL-DTA 在保持正确梯度累积的同时，实现了内存与计算的双重高效性**。

---

## 2. 核心实验方法和设置

### 📚 数据集与任务
- 使用 **T2-bench [27]** 作为 RL 训练工作负载基准。
- 在 **TauBench** 数据集上评估模型性能（用于 reward 曲线分析）。
- 任务类型：面向 **语言模型推理能力增强** 的 RL 微调，涵盖数学推理、程序合成等复杂任务。

---

### ⚙️ 实验设置
- **模型架构**：基于 **QWEN-3** 系列模型，测试参数规模包括 **1.7B, 4B, 8B, 14B**。
- **上下文长度（context length）**：16K。
- **RL 算法**：PPO（Proximal Policy Optimization）。
- **训练框架**：基于 **AREAL [2]** 构建，支持异步 rollout 生成与模型更新。
- **硬件环境**：每台机器配备 **8 块 NVIDIA H800 GPU**。

---

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **End-to-End Throughput** | 完整 RL 训练流程的加速比（以单位时间内完成的训练步数衡量） |
| **Training Throughput** | 单个训练 worker 的吞吐提升倍数 |
| **Peak GPU Memory Usage** | 反向传播阶段的最大显存占用 |
| **Reward Curve** | 模型在验证集上的奖励变化趋势，用于评估训练稳定性 |
| **Ablation Study** | 各组件对性能的影响（如 DFS、chunking、load balancing） |

---

### 🆚 基线方法对比
- **AREAL**：当前最先进的异步 RL 训练系统，作为主基线。
- **Dense + CKPT**：标准密集训练 + Activation Checkpointing（应对长序列 OOM）。
- **Tree Training [5]**：已有尝试利用前缀共享的方法，但静态打包、扩展性差。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 端到端训练吞吐提升
| 模型 | 相比 AREAL 的 E2E 加速比 |
|------|------------------------|
| Qwen3-1.7B | **1.28×** |
| Qwen3-8B | **2.28×** |

> 💡 提速主要来源于：更高的训练吞吐允许将更多 GPU 分配给 rollout 生成阶段，从而整体加速 pipeline。

#### ✅ 单 worker 训练吞吐提升
- **最高达 8.31×** 的单卡训练吞吐提升（在 2-bench 上）。
- 集群整体训练吞吐提升 **6.20×**。

#### ✅ 显存使用显著降低
- **峰值 GPU 内存下降超过 50%**。
- 因此 **无需启用 Activation Checkpointing**，避免了额外的 recomputation 开销。

---

### 🔬 消融实验结果（Ablation Studies）

#### （1）反向传播优化组件效果（Figure 4 & 5）
| 方法 | 相对 Dense+CKPT 的平均加速比 |
|------|----------------------------|
| Vanilla Tree 方法 | 6.59× |
| + Cut Tail（不缓存叶节点 KV） | 7.53× |
| + DFS 遍历顺序优化 | 7.74× |
| + Larger Backward Chunk Size (LB) | **8.31×** |

> ✅ 所有优化均带来正向增益，且可叠加。

#### （2）负载均衡算法对比（Figure 6–8）
- 对比两种 baseline：
  - `Greedy FFD` 按 token 数分配
  - `FFD by ntree tokens` 按树 token 成本分配
- **AREAL-DTA 的 DFS-order + contiguous partitioning** 表现最优。
- 若关闭该机制，系统性能下降 **11.93%**。

#### （3）前缀合并收益
- 即使只是简单合并完全包含的前缀，也能带来 **1.67×** 的加速。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **前缀共享是 RL 训练中的重大优化机会**：数百条 rollout 共享前缀的现象普遍存在，合理利用可大幅节省计算资源。
2. **动态 DFS 遍历优于静态树打包**：AREAL-DTA 通过运行时动态构建计算图，避免了全树物化的高开销，实现了真正的可扩展性。
3. **内存效率直接转化为速度优势**：更低的显存占用使得更大 batch size 和更多 rollout 成为可能，同时免去了 activation checkpointing 的开销。
4. **负载均衡对多 GPU 扩展至关重要**：合理的分组策略不仅能平衡负载，还能最大限度保留前缀共享结构。

---

### ⚠️ 方法的局限性
- **依赖 rollout 的高前缀重合率**：若不同 rollout 差异极大、共享前缀短，则优化空间有限。
- **DFS 遍历引入一定调度开销**：虽然总体收益显著，但在极短序列场景下可能不如标准 batching 简洁。
- **目前集成于 AREAL 框架**：通用性依赖于异步 RL 架构的支持，同步训练需适配。

---

### 🔮 未来工作方向
1. **扩展至其他 RL 算法**：如 GRPO、SAC 等是否同样受益。
2. **支持动态树剪枝与优先级采样**：结合 reward 信号动态调整 rollout 生成与训练优先级。
3. **跨节点大规模扩展**：探索在万卡级别集群中的通信与调度优化。
4. **与 speculative decoding 结合**：联合优化 inference-time 与 training-time 的 tree 结构利用。

---

## ✅ 总结
**AREAL-DTA 是首个在大规模 RL 训练中高效利用 prefix sharing 的系统级解决方案**。它通过 **DFS-based 动态树注意力机制** 和 **负载均衡的分布式批处理**，实现了高达 **8.31× 的训练吞吐提升** 与 **超 50% 的显存节省**，为 LLM 的高效 RL 训练提供了新的范式。

</details>

---

### 11. [Grad2Reward: From Sparse Judgment to Dense Rewards for Improving Open-Ended LLM Reasoning](https://arxiv.org/abs/2602.01791)

**Authors**: Zheng Zhang, Ao Lu, Yuanhao Zeng, Ziwei Shan, Jinjin Guo, Lufei Li, Yexin Li, Kan Ren  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2602.01791v1  

#### Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has catalyzed significant breakthroughs in complex LLM reasoning within verifiable domains, such as mathematics and programming. Recent efforts have sought to extend this paradigm to open-ended tasks by employing LLMs-as-a-Judge to provide sequen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Grad2Reward: From Sparse Judgment to Dense Rewards for Improving Open-Ended LLM Reasoning  
**—— 核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前在开放域任务（如医学咨询、创意写作）中，**Reinforcement Learning with Verifiable Rewards (RLVR)** 难以直接应用，因为这些任务缺乏明确的“正确答案”，评价具有主观性。现有方法通过引入 **LLM-as-a-Judge** 提供序列级（sequence-level）奖励进行策略优化，但仍面临两大瓶颈：

1. **稀疏奖励（Sparse Rewards）**：仅在生成结束时提供整体反馈，无法指导复杂长文本推理过程中的细粒度优化。
2. **黑盒评判（Black-box Judge）**：将 Judge 视为黑箱，忽略了其内部丰富的中间评估信号。

这导致训练效率低、信用分配不精确，尤其不利于对过程质量要求高的任务（如医疗建议需每一步都科学严谨）。

---

### 🚀 提出的新方法：GRAD2REWARD
作者提出 **GRAD2REWARD**，一种从 Judge 模型推理过程中提取**密集过程奖励（dense process rewards）** 的新框架，核心思想如下：

- **梯度归因（Gradient-based Attribution）**：利用单次反向传播计算每个生成 token 对 Judge 决策的影响程度，得到 token-level 的贡献分数。
- **Token-level Reward 构建**：
  $$
  b_t = g_t \cdot e_t,\quad r_t = \alpha_t \cdot r(x,o)
  $$
  其中 $g_t$ 是 log-probability 关于 token embedding 的梯度，$e_t$ 是对应嵌入，$b_t$ 经 Softmax 归一化后加权分配原始序列级奖励。
- **自评判机制（Self-judging Mechanism）**：Judge 固定为初始策略模型副本，无需依赖更强的外部 Judge，实现自我监督下的持续改进。

---

### 🔍 相比现有方法的优势
| 特性 | 传统方法（如 Vanilla-GRPO, RuscaRL） | GRAD2REWARD |
|------|-------------------------------|-------------|
| 奖励密度 | 稀疏（仅序列级） | 密集（token-level） |
| Judge 使用方式 | 黑盒输出决策 | 白盒挖掘梯度信号 |
| 是否需要外部强 Judge | 是（通常用更大模型） | 否（self-judging） |
| 是否需训练专用 PRM | 是（如 PURE, PQM） | 否（零成本构建 dense reward） |
| 适用范围 | 多限于可验证领域（如数学） | 支持开放域 + 可验证域 |

> ✅ **优势总结**：更高效训练、更高推理质量、更强泛化能力、更低部署门槛。

---

## 2. 核心实验方法和设置

### 📚 数据集
涵盖多个开放域任务，分为两类：

#### 开放式任务（Open-ended Tasks）
| 数据集 | 任务描述 |
|--------|----------|
| **HealthBench** | 医疗健康问答，强调安全性与准确性 |
| **RaR-Medicine** | 医学咨询，含多维度评分标准（rubrics） |
| **ResearchQA** | 学术研究类长文本问答，跨75个学科 |
| **RaR-Science** | 科学问题解答，注重逻辑与事实一致性 |

#### 可验证任务（Verifiable Domains，用于扩展分析）
| 数据集 | 描述 |
|--------|------|
| **MATH500**, **Minerva Math**, **OlympiadBench**, **AIME24/25**, **AMC23** | 数学推理基准，有标准答案 |
| **GPQA-Diamond** | 高难度科学问答，支持自动验证 |

---

### ⚙️ 实验设置
- **模型家族**：
  - Qwen2.5 系列：1.5B, 3B, 7B
  - Llama-3.1/3.2-8B
  - 测试用更强模型（如 Qwen3-30B, Mistral-Small-24B）作为 **test grader**
- **训练方式**：
  - 全参数微调（full-parameter fine-tuning）
  - 使用 **veRL** 框架实现 RL 优化
  - Batch size: 32 prompts, 每 prompt 采样 8 条响应
- **评估协议**：
  - 使用 **OpenAI Simple-Evals** 工具包
  - Test grader 基于 rubric 打分，报告平均得分
  - 训练与测试 prompt 模板不同，防止过拟合

---

### 🆚 基线方法对比
| 方法 | 简介 |
|------|------|
| **Vanilla-GRPO** | 使用 LLM-as-a-Judge 的 sequence-level reward 进行 GRPO 优化 |
| **RuscaRL** | 当前 SOTA 方法，利用 rubric 引导探索空间 |
| **DAPO / RLOO** | 其他 RLHF 框架，用于验证通用性 |
| **PRM / PURE / PQM** | 在数学领域训练的 Process Reward Model，代表 dense reward 基线 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（Table 1）

| Model | HealthBench ↑ | RaR-Medicine ↑ | ResearchQA ↑ | RaR-Science ↑ |
|-------|----------------|----------------|---------------|----------------|
| Qwen2.5-1.5B-Instruct (vanilla) | 32.2 | 27.7 | 41.2 | 36.2 |
| → +Vanilla-GRPO | 39.5 | 32.7 | 53.1 | 41.6 |
| → +RuscaRL | 40.7 | 34.3 | 53.9 | 44.1 |
| → **+GRAD2REWARD (Ours)** | **44.5** | **35.5** | **55.0** | **43.5** |

> ✅ 小模型上提升显著：+3.8 pts vs RuscaRL on HealthBench；+1.1 on ResearchQA

| Llama-3.1-8B-Instruct | HealthBench | RaR-Medicine | ResearchQA | RaR-Science |
|------------------------|--------------|---------------|-------------|---------------|
| Vanilla-GRPO | 47.8 | 56.7 | 65.9 | 54.5 |
| RuscaRL | 48.6 | 61.1 | 67.0 | 56.2 |
| **GRAD2REWARD** | **51.1** | **62.1** | **68.9** | **56.7** |

> ✅ 大模型仍能稳定增益，尤其在 HealthBench 上领先 2.5 pts

---

### 🔁 泛化性与鲁棒性测试
- **换 test grader（Mistral-Small-24B）**：性能趋势一致，证明无过拟合
- **跨数据集迁移（Table 6）**
  - RaR-Medicine → HealthBench：**42.1** vs 37.6 (Vanilla), 37.3 (RuscaRL)
  - RaR-Science → GPQA-Diamond（可验证）：**26.1** vs 24.7 / 25.6
  > ✅ 显示出良好的跨任务、跨格式泛化能力

---

### 🔍 消融实验（Ablation Study, Table 2）
比较不同梯度归因方式：

| 方法 | HealthBench | RaR-Science |
|------|-------------|------------|
| Vanilla-GRPO | 32.2 | 36.2 |
| L1 norm (grad only) | 38.7 | 37.8 |
| L2 norm (grad only) | 38.0 | 40.0 |
| **GRAD2REWARD (grad × embed)** | **44.5** | **43.5** |

> ✅ 说明 **Gradient × Embedding** 设计优于单纯梯度范数，因其保留方向信息，理论更合理。

---

### ⏱️ 训练效率分析（Figure 2）
- **收敛速度加快 1.7× ~ 1.9×**
  - 达到相同性能所需训练步数减少近一半
- **渐近性能更高**
  - 最终得分相对提升达 **12–13%**

> 💡 原因：密集奖励提供更精细的 credit assignment，加速策略更新。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **密集奖励显著提升开放域推理质量**：相比稀疏奖励，token-level supervision 能有效优化生成过程的每一个环节。
2. **Judge 内部蕴含丰富过程反馈**：通过梯度归因可“解码”其隐式评估逻辑，无需额外标注或训练 PRM。
3. **Self-judging 机制可行且高效**：即使不用更强 Judge，也能实现优异性能，降低部署成本。
4. **方法具备强泛化性**：
   - 跨模型规模（1.5B → 8B）
   - 跨任务类型（医疗、科研、科学）
   - 跨领域（开放域 + 可验证数学）
5. **优于专门训练的 PRM**：在数学推理任务上（Table 5），GRAD2REWARD 在 5/6 基准上超越 PURE/PQM/PRM。

---

### ⚠️ 局限性
1. **依赖 Judge 的判别能力**：若初始策略本身判断力弱，可能提供噪声反馈。
2. **梯度归因假设线性近似成立**：高阶非线性影响未被建模。
3. **计算开销略增**：虽只需一次 backward pass，但在长序列下仍有延迟。
4. **安全风险控制需外接机制**：不能完全替代人工审核，尤其在医疗、法律等高危场景。

---

### 🔮 未来工作方向
1. **扩展至 Agent-based Long-horizon Tasks**：应用于多步决策、工具调用等复杂 AI Agent 场景。
2. **结合 Safety Guardrails**：集成可控解码、毒性过滤等模块，确保生成内容合规。
3. **动态调整 Temperature 或 Sampling Strategy**：基于 attribution score 动态聚焦关键 token。
4. **探索多 Judge Ensemble**：融合多个 Judge 的梯度信号以提高稳定性。
5. **理论深化**：建立 attribution-based reward 与 MDP 最优策略之间的形式化联系。

---

## 总结一句话
> **GRAD2REWARD 成功将 LLM-as-a-Judge 的“黑盒判决”转化为“白盒过程指导”，实现了无需外部强模型、无需标注数据、无需训练 PRM 的高质量密集奖励生成，在开放域与可验证任务中均展现出卓越性能与广泛适用性。**

</details>

---

### 12. [Position: Agentic Evolution is the Path to Evolving LLMs](https://arxiv.org/abs/2602.00359)

**Authors**: Minhua Lin, Hanqing Lu, Zhan Shi, Bing He, Rui Mao, Zhiwei Zhang, Zongyu Wu, Xianfeng Tang, Hui Liu, Zhenwei Dai, Xiang Zhang, Suhang Wang, Benoit Dumoulin, Jian Pei  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.00359v1  

#### Abstract
As Large Language Models (LLMs) move from curated training sets into open-ended real-world environments, a fundamental limitation emerges: static training cannot keep pace with continual deployment environment change. Scaling training-time and inference-time compute improves static capability but do...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Position: Agentic Evolution is the Path to Evolving LLMs**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决了什么问题

当前的 **Large Language Models (LLMs)** 在部署到开放、动态的真实世界环境时面临一个根本性挑战：**train-deploy gap**（训练-部署环境差距）。  
- 模型在静态、有限的数据上训练，无法应对部署后不断变化的API、格式、用户需求等。
- 现有的适应方法（如在线微调或记忆积累）要么不稳定（如灾难性遗忘），要么效率低下（如上下文饱和、重复推理）。

### 🚀 提出了什么新方法或新思路

提出 **Agentic Evolution（代理式进化）** 范式，并构建通用框架 **A-Evolve**，将模型的部署期改进视为一个**目标导向、自主决策的优化过程**。

#### 核心思想：
- 将传统的固定更新规则 $ F_{\text{Evolve}} $ 替换为一个显式的 **Evolver Agent**（进化代理），该代理具备以下能力：
  - **Goal-Oriented Principle**（目标导向）：主动诊断失败原因，定位可修复的组件（如工具、知识、验证逻辑）。
  - **Autonomy Principle**（自主性）：决定何时触发进化，是否提交更新，避免盲目修改。
  - **Compositional Principle**（组合性）：生成模块化、结构化的改进（如可执行工具、测试用例），并通过验证机制确保安全性。

#### 框架 A-Evolve 包含三大组件：
1. **Persistent Artifact State $ \mathcal{T}_s $**：非参数化状态，包括：
   - **Knowledge Registry (K)**：结构化知识（如API规范）
   - **Tool Registry (T)**：可执行函数
   - **Validation Registry (V)**：单元测试、回归检查
2. **Solve-Evolve Loop**：分离任务求解（Solve）与系统进化（Evolve），确保进化是受控过程。
3. **Evolver Agent**：由四个协作模块组成：
   - **Diagnose** → **Plan** → **Update** → **Verify**

### ⭐ 相比现有方法的优势

| 方法类型 | 代表 | 缺陷 | A-Evolve 的优势 |
|--------|------|------|----------------|
| **Parametric Fine-tuning** | Test-time training, LoFiT | 灾难性遗忘、不可审计、权重更新不透明 | 更新显式、可验证、保留原始模型稳定性 |
| **Non-parametric Heuristic** | APE, AWM, Memory Append | 上下文饱和、检索噪声、无因果理解 | 结构化知识管理，支持复用与治理 |
| **Static Pipelines** | 固定提示工程、搜索式优化 | 无法处理复杂结构性变更 | 支持多步规划与跨组件协调 |

> ✅ **本质区别**：传统方法是“机械式响应”，而 A-Evolve 是“智能体级决策”。

---

## 2. 核心实验方法和设置

### 📚 使用了哪些数据集

- **AppWorld** (Trivedi et al., 2024)：一个用于评估交互式编码智能体的基准平台。
  - 模拟真实应用生态（如 Amazon, Spotify, Venmo, Gmail）
  - 提供 457 个 API 和约 750 个任务
  - 包含 **test-normal** 和 **test-challenge** 分割，后者要求使用训练未见的应用

- 实验设置：
  - **训练集**：50 个任务（用于进化学习）
  - **测试集**：50 个任务（评估泛化能力）

### 🧪 实验设置和评估指标

#### 评估协议：
- 固定 **solve-time compute budget**（最大工具调用次数、步骤数、token 数）
- 固定 **evolve-time compute budget**（每轮进化的 token 和工具调用上限）
- 所有方法在此预算下公平比较

#### 主要评估指标：
| 指标 | 公式 | 含义 |
|------|------|------|
| **TGC (Task Goal Completion)** | $ \frac{1}{N} \sum_{i=1}^N \mathbb{I}(s_i = 1) $ | 完全成功完成的任务比例（严格标准） |
| **APT (Average Passed Tests)** | $ \frac{1}{N} \sum_{i=1}^N s_i $ | 平均通过的单元测试比例（反映渐进改进） |

### 🔁 基线方法对比

| 基线方法 | 类型 | 描述 |
|---------|------|------|
| **Vanilla** | 无进化 | 直接调用 solver，无任何持久化改进 |
| **APE** (Zhou et al., 2022) | 非参数启发式 | 基于搜索的提示词进化，通过任务评分选择指令 |
| **AWM** (Wang et al., 2024) | 非参数启发式 | 从历史轨迹中提取可复用的工作流作为记忆 |

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1）

| Method | Solver | TGC (%) | APT (%) |
|--------|--------|--------|--------|
| Vanilla | Haiku 4.5 | 32 | 51.16 |
| APE | Haiku 4.5 | 30 | 56.00 |
| AWM | Haiku 4.5 | 46 | 65.76 |
| **A-Evolve** | **Haiku 4.5** | **64** | **84.31** |
| Vanilla | Gemini 3 Flash | 56 | 80.45 |
| AWM | Gemini 3 Flash | 52 | 87.75 |
| **A-Evolve** | **Gemini 3 Flash** | **82** | **92.05** |

> ✅ **结论**：A-Evolve 在所有 solver 上显著优于基线，尤其对小模型提升巨大（Haiku 4.5 的 TGC 提升 +32pts）。

### 🔍 与基线方法的对比结果

- **能力放大效应（Capability Multiplier）**：
  - A-Evolve 能将较小模型（如 Haiku 4.5）的能力提升至接近甚至超过更大模型（如 Sonnet 4）的原始水平。
  - 表明：**持久化程序性改进可以媲美模型规模扩展带来的收益**。

- **缩小容量差距（Narrowing the Capacity Gap）**：
  - Haiku 4.5 + A-Evolve → 64% TGC
  - Vanilla Sonnet 4 → 42% TGC
  - **小模型 + 进化 > 大模型 + 静态推理**

### 🔧 消融实验结果（Ablation Study）

在 **Fig. 3** 中对 A-Evolve 进行模块移除分析：

| 变体 | 描述 | 性能影响 |
|------|------|----------|
| **A-Evolve/D** | 移除 Diagnose | 无法识别根本原因，仅做表面修补 |
| **A-Evolve/A** | 移除 Analysis Tools | 依赖单次轨迹推断，错过跨任务模式 |
| **A-Evolve/P** | 移除 Plan | 无法协调多组件更新，导致不一致 |
| **A-Evolve/V** | 移除 Verify | 提交缺陷工具，污染上下文，引发退化 |

> ❗ 最严重的是 **移除验证（A-Evolve/V）**，导致性能大幅下降，说明 **Verification 是稳定长期进化的关键守门员**。

---

## 4. 关键结论和发现

### 🎯 论文的主要发现

1. **Agentic Evolution 是解决 train-deploy gap 的必然路径**：
   - 静态训练和单纯扩大推理计算无法应对持续变化的现实环境。
   - 必须引入**自主、目标驱动的进化机制**来实现可持续适应。

2. **进化本身应被视为可扩展的第三维度**：
   - 提出 **Evolution-Scaling Hypothesis**：
     > “适应能力”随着分配给进化过程的计算资源（$ C_{\text{evolve}} $）增加而系统性提升。
   - 实验验证：增加进化步数或使用更强 evolver（如 Opus > Sonnet > Haiku），性能单调上升。

3. **结构化、可验证的改进优于黑箱微调或文本记忆**：
   - A-Evolve 通过生成 **verified tools, reusable workflows, structured knowledge**，实现了**推理成本的摊销**（amortization）。
   - 成功将“每次都要重新思考”的问题转化为“一次解决，永久复用”。

### ⚠️ 方法的局限性

- **计算开销较高**：维护 Evolver Agent 和验证流程需要额外 compute，短期不如 heuristic 方法高效。
- **依赖高质量反馈信号**：若环境缺乏明确错误或测试用例，诊断可能失效。
- **当前实现在特定领域（如 API 调用）更有效**，通用性有待进一步验证。

### 🔮 未来工作方向

1. **Benchmark Development**：
   - 设计专门衡量“演化能力”的新基准，关注**改进的持久性、复用率、抗分布偏移能力**。

2. **Framework Optimization**：
   - 提升 Diagnose、Plan、Verify 模块的效率与鲁棒性，降低 $ C_{\text{evolve}} $ 开销。

3. **理论建模**：
   - 建立 agentic evolution 的形式化理论，例如将其建模为组合程序空间上的优化问题。
   - 探索其相对于非代理方法的 regret bounds。

4. **安全与对齐机制强化**：
   - 强化 Validation Gate，引入人类审核 hook 或 alignment constraints，防止目标漂移（goal drift）。

---

## ✅ 总结一句话

> **Agentic Evolution 将 LLM 的部署期适应从“被动反应”升级为“主动进化”**，通过 A-Evolve 框架实现**可治理、可扩展、可持续的智能体成长路径**，是迈向真正开放环境中 AGI 的关键一步。

</details>

---

### 13. [Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach](https://arxiv.org/abs/2602.01131)

**Authors**: Yue Zhong, Jiawen Kang, Yongju Tong, Hong-Ning Dai, Dong In Kim, Abbas Jamalipour, Shengli Xie  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.01131v1  

#### Abstract
With the rapid expansion of the low-altitude economy, Unmanned Aerial Vehicles (UAVs) serve as pivotal aerial base stations supporting diverse services from users, ranging from latency-sensitive critical missions to bandwidth-intensive data streaming. However, the efficacy of such heterogeneous netw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Lyapunov Stability-Aware Stackelberg Game for Low-Altitude Economy: A Control-Oriented Pruning-Based DRL Approach*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**低空经济**（Low-Altitude Economy, LAE）中无人机**（UAV）**作为空中基站时面临的资源分配与物理控制稳定性之间的矛盾问题。传统通信系统多以吞吐量为中心，但在紧急救援等安全关键任务中，**通信延迟直接影响控制系统的稳定性**（如位置跟踪误差），而现有研究大多将通信与控制解耦处理，缺乏闭环协同设计。

### 提出的新方法与创新思路
1. **构建了 Sensing-Communication-Computing-Control (SC³) 闭环框架**  
   首次在LAE场景中显式建模了从感知到控制的端到端延迟传播机制，将物理层的控制稳定性需求与逻辑层的通信资源分配深度融合。

2. **基于 Lyapunov 稳定性理论进行控制-通信映射**  
   利用 Lyapunov 函数对 UAV 的状态误差进行建模，推导出满足系统稳定性的**最大允许通信延迟边界**（`T_budget`），从而将抽象的“稳定性要求”转化为可量化的通信资源约束。

3. **提出 Stackelberg 博弈模型实现分层资源管理**  
   将 UAV 设为领导者（Leader），动态定价带宽；用户作为跟随者（Follower），根据价格和自身紧急程度优化带宽请求。该机制实现了负载均衡与稳定性保障的统一。

4. **设计轻量级 Pruning-based PPO 算法用于边缘部署**  
   针对标准 DRL 模型参数量大、能耗高的问题，提出一种集成**动态结构化剪枝**（dynamic structured pruning）的 PPO 算法，在训练过程中逐步压缩网络规模，显著降低推理延迟和能耗。

### 相比现有方法的优势
| 维度 | 本文方法 | 现有方法 |
|------|---------|--------|
| **系统视角** | SC³ 闭环，联合优化控制与通信 | 多为通信单目标优化 |
| **稳定性保障** | 显式引入 Lyapunov 条件作为硬约束 | 忽略或弱化控制稳定性 |
| **激励机制** | 定价策略由稳定性驱动，具备跨域调控能力 | 仅基于资源稀缺性定价 |
| **算法效率** | 动态剪枝实现轻量化，适合 UAV 边缘部署 | 使用全连接重型网络，难以实时运行 |

---

## 2. 核心实验方法和设置

### 实验环境与仿真设置
- **仿真平台**：自定义 Python/MATLAB 联合仿真环境，模拟多 UAV 与多用户交互。
- **场景设定**：地面基础设施瘫痪下的灾后应急响应场景，UAV 作为空中基站提供通信与控制服务。
- **系统参数**（部分关键值见 Table I）：
  - UAV 数量：3 或 12
  - 用户数量：5 ~ 20
  - 采样周期 `ε_n`：0.5s
  - 控制时间常数 `τ_n`：0.005s
  - Lyapunov 衰减率 `ρ_n`：0.95
  - 总带宽范围：[15, 25] MHz
  - 数据包大小：[40, 64] kbits

### 评估指标
| 指标 | 描述 |
|------|------|
| **Test Reward** | UAV 的平均效用（Utility），综合考虑收益与成本 |
| **Bandwidth Utilization** | 带宽使用效率及负载均衡情况 |
| **Latency Distribution** | 端到端 SC³ 延迟分布，是否满足稳定性要求 |
| **Convergence Speed** | 算法达到稳定策略所需的迭代次数 |
| **Inference Latency / Model Size** | 剪枝前后模型的计算开销对比 |

### 基线方法对比
- **Standard PPO**：未剪枝的标准多智能体 PPO 算法
- **Greedy Algorithm**：UAV 固定高价或按负载调整价格，用户贪心申请
- **Random Algorithm**：随机定价与资源请求

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
#### （1）学习性能与收敛性（Fig. 3）
- **Pruning-based PPO** 在约 400 轮后收敛，最终测试奖励达 **~7.8**
- 标准 PPO 收敛较慢且波动更大，最终奖励约为 **7.6**
- Greedy 和 Random 算法停滞在低水平（< 4.0）
> ✅ **优势**：剪枝不仅未损害性能，反而因正则化效果提升了泛化能力。

#### （2）不同剪枝起始时机的影响（Fig. 4）
- 最优设置为 **pruning start epoch `t₀ = 50`**（早期剪枝）
- 此时模型能从一开始就适应稀疏结构，避免后期结构调整带来的震荡
- `t₀ = 200` 时性能最差，说明中期剪枝会破坏正在形成的策略惯性

#### （3）不同用户/UAV 规模下的表现（Fig. 6）
- 当用户数增加（固定 UAV=3）时，UAV 效用显著上升 → 表明机制能有效激励服务更多用户
- 当 UAV 数增加（固定用户=15）时，**单个 UAV 平均效用下降** → 反映市场饱和效应，符合 Stackelberg 经济规律

#### （4）资源分配合理性验证（Fig. 2）
- 高优先级用户（高 urgency weight）获得更高带宽配额
- 信道质量好（高 SNR）的用户也获得更多资源
- 所有用户的实际分配带宽均高于由 Lyapunov 推导出的最小需求（`K_min`），确保了稳定性底线

#### （5）模型压缩效果（隐含于算法描述）
- 通过动态结构化剪枝，神经网络参数量减少 **>50%**
- 推理延迟降低，更适合部署在能源受限的 UAV 平台

---

## 4. 关键结论和发现

### 主要发现
1. **控制稳定性可通过 Lyapunov 理论精确转化为通信资源约束**，为“控制-通信”联合设计提供了理论基础。
2. **Stackelberg 博弈是建模 UAV 与用户间资源博弈的有效工具**，其均衡点自然实现了紧急任务优先、非紧急任务低成本接入的分层服务机制。
3. **动态结构化剪枝可在不牺牲性能的前提下大幅压缩 DRL 模型**，解决了重型 DRL 难以在边缘设备部署的问题。
4. **早期剪枝优于晚期剪枝**，表明“边训练边压缩”的策略更有利于策略稳定性和模型轻量化。

### 方法的局限性
- **假设线性控制模型**：基于 LTI 系统建模，可能无法完全反映复杂非线性飞行动力学。
- **集中式训练分布式执行**（CTDE）依赖全局信息回传，对通信可靠性有一定要求。
- **未考虑能量动态变化**：当前模型假设 UAV 能量充足，未建模电池衰减过程。
- **静态拓扑假设**：用户移动性建模有限，未深入分析高速移动下的鲁棒性。

### 未来工作方向
1. **联合建模能量与时变带宽资源**，研究复合资源波动下的端到端延迟控制。
2. **深化双向协同设计**：探索控制指令反馈如何反向影响通信调度决策。
3. **扩展至异构 UAV 网络**：支持不同类型（固定翼 vs 多旋翼）、不同能力的混合编队。
4. **引入生成式 AI 辅助决策**：利用 LLM 或扩散模型增强不确定性环境下的策略生成能力。

---

> **总结一句话**：  
> 本论文开创性地将 **Lyapunov 稳定性理论** 引入 LAE 中的资源分配问题，构建了首个 **SC³ 闭环控制-通信协同框架**，并通过 **轻量化的剪枝型 PPO 算法** 实现了高效稳定的 Stackelberg 博弈求解，为安全关键型低空应用提供了坚实的理论与技术支撑。

</details>

---

### 14. [MiTA Attention: Efficient Fast-Weight Scaling via a Mixture of Top-$k$ Activations](https://arxiv.org/abs/2602.01219)

**Authors**: Qishuai Wen, Zhiyuan Huang, Xianghan Meng, Wei He, Chun-Guang Li  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.01219v1  

#### Abstract
The attention operator in Transformers can be viewed as a two-layer fast-weight MLP, whose weights are dynamically instantiated from input tokens and whose width equals sequence length $N$. As the context extends, the expressive capacity of such an $N$-width MLP increases, but scaling its fast weigh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MiTA Attention: Efficient Fast-Weight Scaling via a Mixture of Top-$k$ Activations

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统的 **Transformer** 中的 **attention** 操作具有 $O(N^2)$ 的计算和内存复杂度（$N$ 为序列长度），这严重限制了其在长序列任务中的扩展能力。尽管已有大量高效 attention 方法被提出，但缺乏一个统一的视角来系统理解这些方法。

本文从 **fast-weight scaling** 的角度出发，指出 full attention 可以被视为一个宽度为 $N$ 的两层 fast-weight MLP，随着上下文增长，其表达能力增强，但动态权重（即 key-value 对）的扩展代价高昂。

### ✅ 提出的新方法与新思路
作者提出了 **MiTA (Mixture of Top-$k$ Activations)** 注意力机制，其核心思想是将两种高效的 fast-weight scaling 策略——**routing** 和 **compression**——结合起来：

- **Compression（压缩）**：通过一组少量的 **landmark queries** 将完整的 key-value 缓存压缩成一个紧凑的全局模块（shared expert），提供对上下文的粗略但全局的摘要。
- **Routing（路由）**：每个 landmark query 动态地收集与其最相关的 top-$k$ key-value 对，构建 **deformable fast-weight experts**，实现细粒度、语义感知的稀疏访问。

最终，每个查询同时访问：
1. 共享的压缩专家（来自 landmark queries）
2. 被路由到的一个或多个变形专家（top-$k$ 激活对）

该设计实现了 **“全局摘要 + 局部精确检索”** 的结合。

### ✅ 相比现有方法的优势
| 方法 | 缺陷 | MiTA 如何改进 |
|------|------|---------------|
| **Linear/Memory-based Attention**（如 Linear Attention, Agent Attention） | 仅压缩，丢失细节信息，表达能力受限 | 引入 routing 分支保留局部高激活区域，提升表达力 |
| **Sparse/MoE Attention**（如 MoBA, NSA） | 固定块划分或静态模式，缺乏语义适应性 | 构建 **query-aware 的 deformable experts**，更灵活精准 |
| **Top-$k$ Attention**（如 Spark Attention） | 每个 query 构建独立专家，共 $N$ 个专家，硬件不友好 | 使用固定数量 $m$ 个共享专家，配合 routing，显著降低开销 |

> 🔑 **核心优势**：**首次将 compression 与 routing 统一于 fast-weight scaling 框架下，并构造可调数量的 deformable experts，兼顾效率、表达能力和硬件友好性。**

---

## 2. 核心实验方法和设置

### 📚 数据集
- **ImageNet-1K**：用于图像分类任务，评估模型从零训练和微调性能。
- **ADE20K**：用于语义分割任务，测试在长序列密集预测下的表现。
- **Long Range Arena (LRA)**：五个长程依赖任务（ListOps, Text, Retrieval, Image, Pathfinder），专门用于评测长序列建模能力。
- 合成数据集：用于测量极端长序列（最高达 $2^{20} \approx 1M$ tokens）下的推理吞吐量。

### ⚙️ 实验设置与评估指标
| 任务 | 设置 | 主要指标 |
|------|------|---------|
| 图像分类 | ViT-T/S/B/L 架构；输入分辨率 224×224；训练 300 epoch 或微调 50 epoch | Top-1 Accuracy, MACs, #Params |
| 语义分割 | ViT 编码器 + MiTA 解码器；分辨率 512×512；与 Segmenter 对比 | mIoU (Single Scale / Multi Scale) |
| 长序列建模 | LRA benchmark；无 DWC 辅助；公平比较 | 各子任务准确率及平均得分 |
| 推理速度 | 三层数 Transformer；RTX 4090 GPU；batch=1 | Inference Throughput (samples/sec) vs $N$ |

### 🔁 基线方法对比
- **Full Attention (ViT)**：标准 Transformer 注意力。
- **Agent Attention**：典型 compression 方法，仅保留 landmark 查询。
- **Linear Attention**：基于核函数近似的线性注意力。
- **Reformer, Linformer, Performer, NystromFormer**：主流高效 attention 方法。
- **MoBA, NSA**：代表 routing 类方法（block-based MoE）。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据汇总

#### （1）ImageNet-1K 分类性能（从头训练）
| 方法 | Top-1 Acc (%) | MACs (G) | #Params (M) |
|------|----------------|----------|-------------|
| ViT-T | 73.1 | 1.2 | 5.7 |
| **MiTA-ViT-T (25+25)** | **72.9 → 74.8*** | **1.1** | **5.7** |
| ViT-S | 77.9 | 4.6 | 22.1 |
| **MiTA-ViT-S (25+25)** | **78.5 → 79.3*** | **4.4** | **22.1** |

> *注：加 △ 表示引入 depth-wise convolution；加 Bias 指使用 Agent bias。MiTA 在更低 MACs 下接近甚至超越原生 ViT 性能。*

#### （2）ImageNet-1K 微调性能（预训练自 ImageNet-21K）
| 方法 | Tiny | Small | Base | Large |
|------|------|--------|-------|--------|
| ViT | 76.9 | 81.2 | 84.4 | 85.9 |
| Agent-ViT (m=49) | 74.6 | 79.7 | 81.4 | 83.5 |
| **MiTA-ViT (25+25)** | **75.6** | **80.9** | **82.8** | **85.5** |

> ✅ MiTA 显著优于 Agent Attention，且与 full attention 更兼容，迁移效果更好。

#### （3）ADE20K 语义分割性能
| 方法 | mIoU (SS/MS) | #Params (M) |
|------|--------------|------------|
| Segmenter-T | 38.1 / 38.8 | 7.0 |
| **SEG-T-MiTA (36+36)** | **38.8 / 39.6** | **6.0** |
| Segmenter-B | 48.8 / 50.0 | 106 |
| **SEG-B-MiTA (36+36)** | **48.8 / 49.9** | **96** |

> ✅ MiTA 在更少参数下达到相当甚至略优性能，验证其在长序列密集任务的有效性。

#### （4）Long Range Arena (LRA) 平均准确率
| 方法 | Avg Accuracy (%) |
|------|------------------|
| Standard Transformer | 59.37 |
| Agent Attention | 58.40 |
| **MiTA Attention (m=k=16)** | **59.26** |

> ✅ MiTA 几乎追平 full attention，在所有五项任务中保持稳健表现，远超其他高效 attention。

#### （5）推理吞吐量（极长序列）
- 在 $N = 2^{20}$（约 100 万 tokens）时，**MiTA 的吞吐量比 fused full attention 高出两个数量级**。
- 即使没有定制 CUDA kernel 优化，MiTA 已展现出显著的 wall-clock speedup。

---

### 🔍 消融实验结果

#### （1）Generalization across $m$ 和 $k$
- 固定训练 $(m, k) = (25,25)$，在推理时调整 $m, k$：
  - **增大 $m$ 或 $k$**：性能持续提升，可达原 baseline 的 100%+；
  - **减小 $m$ 或 $k$**：性能下降明显。
- 结论：**可在训练时用较小 $m,k$ 提升效率，推理时放大以获得更高精度**，具备良好的可扩展性。

#### （2）Checkpoint Swapping（跨 attention 机制泛化）
- 使用 MiTA 训练的 checkpoint，在推理时切换为 full attention，性能损失小；
- 反之，full attention 训练的模型换为 MiTA，性能大幅下降。
- 结论：**MiTA 学习到的 slow weights 更通用，向 full attention 泛化能力强**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Fast-weight scaling 是理解高效 attention 的统一框架**：
   - 所有高效 attention 方法均可归类为 **scaling by routing** 或 **scaling by compression**。
   - 多数方法只采用其一，而 **MiTA 成功融合两者**。

2. **Deformable Experts + Fixed Number + Routing = 更优权衡**：
   - 使用 landmark queries 构造 content-aware 的 deformable experts，比固定块更灵活；
   - 控制专家数量 $m \ll N$，比每 query 一个专家更高效；
   - 利用 routing 实现组合式稀疏模式，提升表达能力。

3. **MiTA 在多种任务上媲美 full attention，且具备更强泛化性**：
   - 在分类、分割、长序列建模中均取得优异结果；
   - 支持训练-推理间 $m,k$ 调整，适合弹性部署；
   - checkpoint 更易迁移到 full attention，说明其学习质量更高。

### ⚠️ 方法的局限性
- **gather 操作存在内存瓶颈**：top-$k$ gather 导致不规则内存访问，可能成为实际部署中的性能瓶颈（虽可通过优化缓解）。
- **目前仅实现 $s=1$ 路由**：即每个 query 最多访问一个额外 expert，进一步增加 $s$ 可能带来收益但实现更复杂。
- **未在超大规模语言模型上验证**：当前实验集中于视觉任务，需更多 NLP 验证。

### 🔮 未来工作方向
1. **优化 top-$k$ gather 的 CUDA 实现**，提升端到端推理效率。
2. **探索 $s > 1$ 的多专家路由策略**，研究组合专家带来的表达增益。
3. **将 MiTA 应用于 LLM 预训练与长文本生成任务**，验证其在自然语言领域的潜力。
4. **结合位置编码或 RoPE**，进一步增强其外推（extrapolation）能力。

---

> 💡 **总结一句话**：  
> MiTA 通过 **compress-and-route** 策略，在 fast-weight scaling 视角下统一了高效 attention 设计范式，提出了一种兼具 **全局概括能力** 与 **局部精细检索能力** 的新型 attention 机制，在多项任务中逼近 full attention 性能的同时大幅提升效率，为下一代高效 Transformer 架构提供了新思路。

</details>

---

### 15. [Prism: Efficient Test-Time Scaling via Hierarchical Search and Self-Verification for Discrete Diffusion Language Models](https://arxiv.org/abs/2602.01842)

**Authors**: Jinbin Bai, Yixuan Li, Yuchen Zhu, Yi Xin, Qingyu Shi, Aosong Feng, Xiaohong Liu, Molei Tao, Jianru Xue, Xiangtai Li, Ming-Hsuan Yang  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2602.01842v1  

#### Abstract
Inference-time compute has re-emerged as a practical way to improve LLM reasoning. Most test-time scaling (TTS) algorithms rely on autoregressive decoding, which is ill-suited to discrete diffusion language models (dLLMs) due to their parallel decoding over the entire sequence. As a result, developi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：PRISM: Efficient Test-Time Scaling via Hierarchical Search and Self-Verification for Discrete Diffusion Language Models**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前主流的 Test-Time Scaling (TTS) 方法大多基于 **autoregressive (AR) decoding** 范式设计，依赖于从左到右的逐步生成与回溯机制。然而，**discrete diffusion language models (dLLMs)** 采用并行、非自回归的迭代去噪机制，在整个序列上进行全局双向建模，这使得传统 TTS 方法难以直接应用。

具体挑战包括：
- **宽度扩展（width scaling）成本高昂**：朴素的 Best-of-N 方法需要对 N 条轨迹执行完整的 T 步去噪，导致计算复杂度为 $O(NT)$，效率低下。
- **外部验证器开销大**：引入额外的 reward model 或 verifier 会显著增加 GPU 内存占用和系统复杂性。
- **中间状态不可解释**：dLLMs 在去噪过程中存在大量 `[MASK]`，破坏了标准 PRM（Process Reward Model）所依赖的“前缀结构”，使其无法有效打分。

因此，如何在不依赖外部模型的前提下，高效地实现 dLLMs 的 test-time compute 扩展，是一个尚未充分探索的问题。

---

### **提出了什么新方法或新思路**
作者提出 **PRISM**（Pruning, Remasking, and Integrated Self-verification Method），一个专为 dLLMs 设计的高效 TTS 框架，包含三大核心技术组件：

#### **(1) Hierarchical Trajectory Search (HTS)**
- 将去噪过程划分为三个阶段：
  - **Stochastic Exploration**（高噪声阶段）：保留 N 条独立轨迹，进行多样化探索。
  - **Progressive Thinning**（中低噪声阶段）：通过几何衰减策略动态剪枝，将活跃轨迹数从 N 快速收缩至 K。
  - **Final Refinement**（精细优化阶段）：仅对 K 条高质量轨迹完成剩余去噪。
- 实现近似线性的计算增长：实际复杂度约为 $O(N + KT)$，远优于 Best-of-N 的 $O(NT)$。

#### **(2) Local Branching via Partial Remasking**
- 在剪枝后，对高分种子轨迹进行局部分支（local branching）：
  - 利用预测熵识别低置信度 token。
  - 随机重遮蔽部分位置，保留高置信度的“逻辑骨架”（logic skeleton）。
  - 在相同解题框架下探索多样化的实现路径。
- 相比完全重启，该操作复用当前状态，控制额外开销。

#### **(3) Self-Verified Feedback (SVF)**
- 引入轻量级自验证机制，**复用同一个 dLLM 自身作为二分类验证器**。
- 构造专用的 Yes/No 自我评估 prompt，询问：“该中间完成是否可能导向正确答案？”
- 输出 SVF score 用于指导剪枝与选择，无需加载外部 verifier。

---

### **相比现有方法的优势**
| 维度 | PRISM | Best-of-N | 外部 Verifier 方法 |
|------|-------|----------|------------------|
| **计算效率** | ✅ 近线性扩展 $O(N + KT)$ | ❌ 二次增长 $O(NT)$ | ❌ 同样 $O(NT)$ |
| **内存开销** | ✅ 无额外模型 | ✅ 无 | ❌ 需加载额外 verifier |
| **兼容性** | ✅ 适配 dLLMs 中间状态 | ⚠️ 不适配 | ❌ 通常不适配 |
| **部署简易性** | ✅ 单模型端到端 | ✅ | ❌ 多模块耦合 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
涵盖数学推理与代码生成两大任务类别：

| 类别 | 数据集 | 描述 |
|------|--------|------|
| **Math Reasoning** | GSM8K | 小学算术文字题，需多步符号推理 |
| | MATH-500 | 竞赛级数学难题，共 500 道 |
| **Code Generation** | HumanEval | 手写 Python 函数问题，docstring 描述需求 |
| | MBPP | 日常编程任务，含自然语言提示与单元测试 |

---

### **实验设置和评估指标**

#### **模型**
使用三种主流 dLLMs：
- `LLaDA-8B-Instruct`
- `Dream-7B-Instruct`
- `LLaDA-2.0-mini`

#### **评估指标**
- 数学任务：**Accuracy**
- 编程任务：**Pass@1**
- 计算成本：**Number of Function Evaluations (NFE)**，即去噪步数总和

#### **基线方法对比**
- **Single-trajectory decoding (N=1)**：基础 baseline
- **Best-of-N (N=4,8,16)**：采样 N 条独立轨迹，通过 majority voting 选出最优

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（以 LLaDA-8B-Instruct 为例）**

| 方法 | GSM8K Acc (%) | NFE | HumanEval Pass@1 (%) | NFE |
|------|---------------|-----|------------------------|-----|
| N=1 (baseline) | 67.58 | 256 | 54.88 | 512 |
| Best-of-4 | 69.32 | 1024 | 77.44 | 2048 |
| Best-of-8 | 82.73 | 2048 | 81.71 | 4096 |
| Best-of-16 | 87.50 | 4096 | 82.32 | 8192 |
| **PRISM (K=8)** | **85.30** | **1048** | **79.27** | **2480** |

> 注：PRISM 在 **仅 1/4 的 NFE 开销下** 达到了接近 Best-of-16 的性能。

---

### **与基线方法的对比结果**
- **效率优势显著**：
  - 在 GSM8K 上，PRISM (K=8) 以 **1048 NFE** 达到 85.3%，而 Best-of-16 需 **4096 NFE** 才达 87.5%，节省 **超 4× 计算量**。
  - 在 MATH 和 MBPP 上，PRISM 常能在 **不到 1/3 推理预算** 下匹配甚至超越 Best-of-16。
- **性能稳定提升**：
  - 在所有三个模型和四个任务上，PRISM 均显著优于 N=1 baseline。
  - 例如在 LLaDA-8B 上，PRISM(K=8) 将 GSM8K 提升 **+17.72pp**，MATH500 提升 **+16.4pp**。

---

### **消融实验结果**

#### **目标宽度 K 的影响**
- 性能随 K 增加单调上升，但边际收益递减。
- **K=8 是最佳平衡点**：在 HumanEval 上达到 79.27%（vs Best-of-16 的 82.32%），速度提升 **3.3×**。

#### **剪枝窗口（Pruning Window）**
- 最佳区间为 **[0.1T, 0.6T]**（早期到中期去噪阶段）。
- 过早剪枝丢失多样性；过晚则浪费计算资源。

#### **剪枝频率（Pruning Interval i）**
- **i=3** 表现最佳：每 3 步执行一次 SVF 打分与分支。
- 过于频繁（i=1）易误删潜力轨迹；过于稀疏（i=5）削弱自适应能力。

#### **衰减速率 d 与幸存者数量 S**
- **d=1.8**（中等衰减）效果最好。
- **S=4** 平衡多样性与聚焦性，S 过小限制探索，过大稀释分支质量。

#### **Self-Verified Feedback (SVF) 的有效性**
- SVF 调用次数占 NFE 不足 10%，开销极低。
- 替换为外部 verifier（如 Qwen-7B/Qwen3-8B）虽可略提性能（如 87.35% vs 85.30%），但需额外加载大模型，超出单卡容量（>40GB）。

---

## **4. 关键结论和发现**

### **主要发现**
1. ✅ **Hierarchical Search 显著提升效率**：将计算集中在“逻辑骨架形成”的关键阶段，避免均匀分配。
2. ✅ **Partial Remasking 实现可控多样性**：在保留高置信结构的同时探索替代实现，优于随机重启。
3. ✅ **Self-Verification 可行且高效**：同一 dLLM 可充当轻量 verifier，无需外部模型即可实现可靠打分。
4. ✅ **PRISM 实现强性能-效率权衡**：在大幅降低 NFE 的同时，逼近甚至超越 Best-of-N 的性能上限。

---

### **方法的局限性**
- **依赖良好的 SVF prompt 设计**：若自我判断本身有偏见或过度自信，可能导致错误剪枝。
- **对去噪调度敏感**：HTS 效果依赖于合适的剪枝窗口设定，不同任务可能需调参。
- **未解决长序列扩展问题**：目前 focus 在 width scaling，length scaling 仍受限于固定长度生成。

---

### **未来工作方向**
- 探索 **length-aware HTS**：结合长度扩展与宽度搜索。
- 引入 **learnable pruning policy**：用强化学习自动决定何时剪枝、保留谁。
- 扩展至 **multi-modal dLLMs**：应用于文本-图像联合生成场景。
- 研究 **zero-shot SVF generalization**：使 self-verification 能跨任务迁移。

---

> 🔗 **代码开源地址**：[https://github.com/viiika/Prism](https://github.com/viiika/Prism)

</details>

---

### 16. [Low-latency Federated LLM Fine-tuning Over Wireless Networks](https://arxiv.org/abs/2602.01024)

**Authors**: Zhiwen Pang, Kang Wei, Long Shi, Zhe Wang, Jun Li, Feng Shu  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.01024v1  

#### Abstract
Recently, federated large language models (LLMs) have drawn significant attention thanks to coupled capabilities of LLMs and federated learning (FL) that address privacy concerns in collaborative fine-tuning. However, due to large-scale parameters of LLMs, existing federated LLM fine-tuning framewor...

---

### 17. [SFMP: Fine-Grained, Hardware-Friendly and Search-Free Mixed-Precision Quantization for Large Language Models](https://arxiv.org/abs/2602.01027)

**Authors**: Xin Nie, Haicheng Zhang, Liang Dong, Beining Feng, Jinhong Weng, Guiling Sun  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.01027v1  

#### Abstract
Mixed-precision quantization is a promising approach for compressing large language models under tight memory budgets. However, existing mixed-precision methods typically suffer from one of two limitations: they either rely on expensive discrete optimization to determine precision allocation, or int...

---

### 18. [LRAgent: Efficient KV Cache Sharing for Multi-LoRA LLM Agents](https://arxiv.org/abs/2602.01053)

**Authors**: Hyesung Jeon, Hyeongju Ha, Jae-Joon Kim  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.01053v1  

#### Abstract
Role specialization in multi-LLM agent systems is often realized via multi-LoRA, where agents share a pretrained backbone and differ only through lightweight adapters. Despite sharing base model weights, each agent independently builds and stores its own KV cache for the same long, tool-augmented tr...

---

### 19. [SNIP: An Adaptive Mixed Precision Framework for Subbyte Large Language Model Training](https://arxiv.org/abs/2602.01410)

**Authors**: Yunjie Pan, Yongyi Yang, Hanmei Yang, Scott Mahlke  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2602.01410v1  

#### Abstract
Training large language models (LLMs) efficiently while preserving model quality poses significant challenges, particularly with subbyte precision supported by state-of-the-art GPUs. Current mixed-precision training approaches either apply uniform precision to all GEMM operations or rely on heuristi...

---

### 20. [Scalable and Secure AI Inference in Healthcare: A Comparative Benchmarking of FastAPI and Triton Inference Server on Kubernetes](https://arxiv.org/abs/2602.00053)

**Authors**: Ratul Ali  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00053v1  

#### Abstract
Efficient and scalable deployment of machine learning (ML) models is a prerequisite for modern production environments, particularly within regulated domains such as healthcare and pharmaceuticals. In these settings, systems must balance competing requirements, including minimizing inference latency...

---

### 21. [SEISMO: Increasing Sample Efficiency in Molecular Optimization with a Trajectory-Aware LLM Agent](https://arxiv.org/abs/2602.00663)

**Authors**: Fabian P. Kr\"uger, Andrea Hunklinger, Adrian Wolny, Tim J. Adler, Igor Tetko, Santiago David Villalba  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00663v1  

#### Abstract
Optimizing the structure of molecules to achieve desired properties is a central bottleneck across the chemical sciences, particularly in the pharmaceutical industry where it underlies the discovery of new drugs. Since molecular property evaluation often relies on costly and rate-limited oracles, su...

---

### 22. [Resource-Efficient Reinforcement for Reasoning Large Language Models via Dynamic One-Shot Policy Refinement](https://arxiv.org/abs/2602.00815)

**Authors**: Yunjian Zhang, Sudong Wang, Yang Li, Peiran Xu, Conghao Zhou, Xiaoyue Ma, Jianing Li, Yao Zhu  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00815v1  

#### Abstract
Large language models (LLMs) have exhibited remarkable performance on complex reasoning tasks, with reinforcement learning under verifiable rewards (RLVR) emerging as a principled framework for aligning model behavior with reasoning chains. Despite its promise, RLVR remains prohibitively resource-in...

---

### 23. [Small Generalizable Prompt Predictive Models Can Steer Efficient RL Post-Training of Large Reasoning Models](https://arxiv.org/abs/2602.01970)

**Authors**: Yun Qu, Qi Wang, Yixiu Mao, Heming Zou, Yuhang Jiang, Weijie Liu, Clive Bai, Kai Yang, Yangkun Chen, Saiyong Yang, Xiangyang Ji  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.01970v1  

#### Abstract
Reinforcement learning enhances the reasoning capabilities of large language models but often involves high computational costs due to rollout-intensive optimization. Online prompt selection presents a plausible solution by prioritizing informative prompts to improve training efficiency. However, cu...

---

### 24. [Scalable Random Wavelet Features: Efficient Non-Stationary Kernel Approximation with Convergence Guarantees](https://arxiv.org/abs/2602.00987)

**Authors**: Sawan Kumar, Souvik Chakraborty  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.00987v1  

#### Abstract
Modeling non-stationary processes, where statistical properties vary across the input domain, is a critical challenge in machine learning; yet most scalable methods rely on a simplifying assumption of stationarity. This forces a difficult trade-off: use expressive but computationally demanding model...

---

### 25. [EvoOpt-LLM: Evolving industrial optimization models with large language models](https://arxiv.org/abs/2602.01082)

**Authors**: Yiliu He, Tianle Li, Binghao Ji, Zhiyuan Liu, Di Huang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01082v1  

#### Abstract
Optimization modeling via mixed-integer linear programming (MILP) is fundamental to industrial planning and scheduling, yet translating natural-language requirements into solver-executable models and maintaining them under evolving business rules remains highly expertise-intensive. While large langu...

---

### 26. [Probing RLVR training instability through the lens of objective-level hacking](https://arxiv.org/abs/2602.01103)

**Authors**: Yiming Dong, Kun Fu, Haoyu Li, Xinyuan Zhu, Yurou Liu, Lijing Shao, Jieping Ye, Zheng Wang  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01103v1  

#### Abstract
Prolonged reinforcement learning with verifiable rewards (RLVR) has been shown to drive continuous improvements in the reasoning capabilities of large language models, but the training is often prone to instabilities, especially in Mixture-of-Experts (MoE) architectures. Training instability severel...

---

### 27. [FutureMind: Equipping Small Language Models with Strategic Thinking-Pattern Priors via Adaptive Knowledge Distillation](https://arxiv.org/abs/2602.01222)

**Authors**: Shaoxiong Yang, Junting Li, Mengyuan Zhang, Chao Li, Wei Liu, Jian Luan  
**Category**: cs.AI  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01222v1  

#### Abstract
Small Language Models (SLMs) are attractive for cost-sensitive and resource-limited settings due to their efficient, low-latency inference. However, they often struggle with complex, knowledge-intensive tasks that require structured reasoning and effective retrieval. To address these limitations, we...

---

### 28. [WorldCup Sampling for Multi-bit LLM Watermarking](https://arxiv.org/abs/2602.01752)

**Authors**: Yidan Wang, Yubing Ren, Yanan Cao, Li Guo  
**Category**: cs.CL  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.01752v1  

#### Abstract
As large language models (LLMs) generate increasingly human-like text, watermarking offers a promising solution for reliable attribution beyond mere detection. While multi-bit watermarking enables richer provenance encoding, existing methods largely extend zero-bit schemes through seed-driven steeri...

---

### 29. [Hierarchical Federated Learning with SignSGD: A Highly Communication-Efficient Approach](https://arxiv.org/abs/2602.02355)

**Authors**: Amirreza Kazemi, Seyed Mohammad Azimi-Abarghouyi, Gabor Fodor, Carlo Fischione  
**Category**: cs.DC  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.02355v1  

#### Abstract
Hierarchical federated learning (HFL) has emerged as a key architecture for large-scale wireless and Internet of Things systems, where devices communicate with nearby edge servers before reaching the cloud. In these environments, uplink bandwidth and latency impose strict communication limits, there...

---

### 30. [Generation Order and Parallel Decoding in Masked Diffusion Models: An Information-Theoretic Perspective](https://arxiv.org/abs/2602.00286)

**Authors**: Shaorong Zhang, Longxuan Yu, Rob Brekelmans, Luhan Tang, Salman Asif, Greg Ver Steeg  
**Category**: cs.LG  
**Published**: 2026-02-03  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.00286v1  

#### Abstract
Masked Diffusion Models (MDMs) significantly accelerate inference by trading off sequential determinism. However, the theoretical mechanisms governing generation order and the risks inherent in parallelization remain under-explored. In this work, we provide a unified information-theoretic framework ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
