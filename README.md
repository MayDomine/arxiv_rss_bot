# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-02-19 06:44:07 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Distributed physics-informed neural networks via domain decomposition for fast flow reconstruction](https://arxiv.org/abs/2602.15883)

**Authors**: Yixiao Qian, Jiaxu Liu, Zewei Xia, Song Chen, Chao Xu, Shengze Cai  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2602.15883v1  

#### Abstract
Physics-Informed Neural Networks (PINNs) offer a powerful paradigm for flow reconstruction, seamlessly integrating sparse velocity measurements with the governing Navier-Stokes equations to recover complete velocity and latent pressure fields. However, scaling such models to large spatiotemporal dom...

---

### 2. [MoE-Spec: Expert Budgeting for Efficient Speculative Decoding](https://arxiv.org/abs/2602.16052)

**Authors**: Bradley McDanel, Steven Li, Sruthikesh Surineni, Harshit Khaitan  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2602.16052v1  

#### Abstract
Speculative decoding accelerates Large Language Model (LLM) inference by verifying multiple drafted tokens in parallel. However, for Mixture-of-Experts (MoE) models, this parallelism introduces a severe bottleneck: large draft trees activate many unique experts, significantly increasing memory press...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MoE-Spec: Expert Budgeting for Efficient Speculative Decoding 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在 **Mixture-of-Experts (MoE)** 架构的大语言模型中，**Speculative Decoding**（推测解码）虽然能通过并行验证多个候选 token 来加速推理，但其效率受到严重限制。原因在于：
- 在验证阶段，每个 token 独立路由到不同的专家子集；
- 验证整个 **draft tree** 时，目标模型必须加载所有被激活的专家的并集；
- 随着 draft tree 规模增大，激活的专家数量迅速接近总专家数（即稀疏性失效），导致内存带宽压力剧增，抵消了推测带来的加速收益。

这一瓶颈使得传统 speculative decoding（如 EAGLE）在 MoE 模型上难以实现理想吞吐提升。

---

### 🆕 提出的新方法：MoE-Spec
作者提出 **MoE-Spec** —— 一种无需训练、仅在验证阶段生效的 **expert budgeting** 方法，核心思想是：
- **在每层强制设定专家容量上限 B（expert budget）**；
- 聚合 draft tree 中所有 token 的 routing probability，选择得分最高的前 B 个专家构成短名单（shortlist）；
- 只加载这些高贡献专家进行验证，忽略长尾中低频使用的专家。

该方法实现了 **speculation depth 与 memory cost 的解耦**：无论 draft tree 多大，每层最多只加载 B 个专家。

#### 支持三种 ranking 策略：
| 方法 | 描述 | 特点 |
|------|------|------|
| **Static Ranking** | 基于校准数据统计专家被选中的频率，固定排序 | 零运行开销，适合中等预算 |
| **Router-Based Ranking**（主推） | 对当前 draft tree 内所有 token 的 routing probability 求和，动态选出 top-B | 自适应输入，无额外模型计算 |
| **Oracle Ranking** | 贪心最小化重构误差，理论上限 | 不实用但用于分析潜力 |

#### 两种处理缺失专家的方式：
- **Truncation**：保留原始 top-k 路由权重，未命中专家直接置零；
- **Substitution**：从 shortlist 中选取替代专家，确保每个 token 仍使用 k 个专家（默认策略）。

---

### 🔍 相比现有方法的优势
| 方面 | MoE-Spec vs. 现有方法 |
|------|------------------------|
| **无需修改架构或训练** | 区别于需要训练 drifter 或调整结构的方法（如 EAGLE-3），MoE-Spec 完全 training-free，可插拔集成进现有 pipeline |
| **显著降低内存带宽开销** | 将验证阶段加载的专家数从近乎全部（如 54/64）压缩至固定预算（如 B=32） |
| **更高吞吐 + 更好扩展性** | 在大 draft tree 下持续增益，而 EAGLE 类方法因“straggler 专家”出现性能下降 |
| **灵活的质量-延迟权衡** | 通过调节 B 实现连续的 Pareto 曲线，适配不同应用场景需求 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
涵盖三大任务类别，共 **5 个基准**：
- **数学推理**：GSM8K（多步算术）、MATH500（竞赛级数学题）
- **代码生成**：HumanEval、MBPP（函数合成任务，pass@1 指标）
- **摘要生成**：CNN/DailyMail（抽象式摘要，ROUGE-L F1）

---

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| **模型** | OLMoE-1B-7B（64 experts, k=8）<br>Qwen3-30B-A3B（128 experts, k=8）<br>Mixtral-8x7B（8 experts, k=2） |
| **Speculative Decoder** | EAGLE-3（OLMoE/Qwen3）、EAGLE-1（Mixtral） |
| **Draft Tree Size** | 主实验为 63；消融实验扫描 3–511 |
| **温度** | T=0（greedy decoding）、T=1（sampling） |
| **评估指标** | Speedup（相对 autoregressive 的加速比）<br>Mean Accept Length（平均接受长度）<br>Task Accuracy / Pass@1 / ROUGE-L |
| **硬件平台** | NVIDIA A100（80GB） |
| **精度** | FP16 |

> 所有方法均启用 **batched MoE computation** 以公平比较（详见 Appendix C）

---

### 🆚 基线方法对比
| 方法 | 描述 |
|------|------|
| **Autoregressive (AR)** | 逐 token 生成，无 speculation，作为速度基准 |
| **EAGLE / EAGLE-3** | 当前最先进的 tree-based speculative decoding 方法，代表 state-of-the-art |
| **MoE-Spec**（本文） | 在 EAGLE 基础上加入 expert budgeting 机制 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1 和 Figure 3）

| 模型 | 方法 | 平均 Speedup 提升（vs. EAGLE） | 示例表现 |
|------|------|-------------------------------|----------|
| **Mixtral-8x7B** | MoE-Spec | **+27%** | MBPP 上达 2.3× vs. EAGLE 的 1.7×（+35%） |
| **Qwen3-30B-A3B** | MoE-Spec | **+16%** | GSM8K 上达 2.4× vs. 1.9× |
| **OLMoE-1B-7B** | MoE-Spec | **+6%** | 提升较小，但仍优于基线 |

> 总体：在 **30 个 model-benchmark 组合中有 27 个实现更高 throughput**，且质量相当。

---

### 🔁 与基线方法对比结果
- **Speedup 显著领先**：
  - MoE-Spec 在大 draft tree 下持续受益，而 EAGLE 在 tree size >31 后性能回落（因专家加载成本超过收益）；
  - 在 tree size=255 时，EAGLE 加载 ~54/64 专家仅得 1.85× 加速，MoE-Spec（B=32）仅加载 32 专家却达到 **2.1×**。
- **Acceptance Length 几乎不变**：
  - 平均仅减少 **1.4%**，说明验证准确性保持良好；
  - 加速主要来自 **verification cost 降低**，而非牺牲接受率。

---

### 🔧 消融实验结果（Ablation Studies）

#### （1）Ranking 方法比较（Figure 5a）
| 方法 | 表现 |
|------|------|
| **Router-Based** | 在各种预算下稳定跟踪 Oracle 上限，在 B≥32 时接近 AR 质量 |
| **Static** | 在低预算（B≤16）下崩溃（如 GSM8K 准确率为 0），无法适应输入变化 |
| **Oracle** | 即使在 B=24（37.5% 专家）也能接近 AR 质量，显示改进空间 |

> 结论：**router-based 是最佳实践折衷方案**

#### （2）Coverage Policy 比较（Figure 5b）
- **Truncation vs. Substitution**：两者性能几乎一致；
- 默认采用 **Substitution** 以维持每个 token 使用 k 个专家的一致性。

#### （3）Reconstruction Error 分析（Figure 6）
- Oracle 在 **少用 25% 专家的情况下达到相同重构误差**；
- 原因：Oracle 考虑专家间的相关性（co-activation），避免冗余选择；
- Router 独立打分，易重复选择强相关专家 → 浪费预算。

> 发现：某些 expert pairs 共现频率高达随机预期的 **10–30×**（平均 23×）

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **MoE 验证过程中的专家激活呈重尾分布**：
   - 前 32/64 专家占据 **93% 的 aggregate routing weight**；
   - 长尾专家对输出影响小但带来高昂带宽代价。

2. **expert budgeting 可有效解耦验证成本与 draft tree 复杂度**：
   - 固定 B 后，memory cost 不再随 tree size 增长；
   - 实现更激进的 speculation 而不引发性能退化。

3. **MoE-Spec 实现 10–30% 吞吐提升**：
   - 在三个主流 MoE 模型上全面超越 EAGLE-3；
   - 尤其在较大模型（如 Mixtral）上增益更明显。

4. **quality-speedup tradeoff 可控性强**：
   - 通过调节 B 得到一条连续的 Pareto 曲线；
   - 应用可根据场景选择操作点（保守 or 激进）。

---

### ⚠️ 局限性
| 问题 | 说明 |
|------|------|
| **需手动设置 expert budget B** | 缺乏自动调参机制，部署时需根据任务校准 |
| **router-based ranking 忽略专家间依赖** | 存在约 25% 的优化空间（相比 Oracle） |
| **小 draft tree 下可能无收益** | routing aggregation 开销占 ~2–3%，极小树时可能抵消节省 |
| **静态 batching 场景增益有限** | 多请求共享专家时边际成本下降，budgeting 效益减弱 |
| **依赖 softmax routing** | 对 DeepSeek-V3 等 sigmoid-based routing 需适配 |

---

### 🔮 未来工作方向
1. **设计更好的 expert selection 策略**：
   - 引入建模专家 co-activation 模式的轻量预测器；
   - 利用 clustering 或图结构捕捉 expert group 结构。

2. **分层自适应 budgeting**：
   - 不同 MoE layer 设置不同 B（浅层宽松，深层紧缩）。

3. **结合压缩技术**：
   - 与 MoE pruning、quantization、offloading 等正交方法叠加使用。

4. **动态 budget 控制**：
   - 根据输入类型（code vs. math）实时调整 B。

---

> 💡 **一句话总结**：  
> **MoE-Spec 揭示了 MoE 推理中“验证即瓶颈”的本质，并通过简单的 expert budgeting 机制，在不损失质量的前提下将 speculative decoding 的吞吐提升了 10–30%，为高效 MoE 推理提供了新的基础设施级优化思路。**

</details>

---

### 3. [Supercharging Agenda Setting Research: The ParlaCAP Dataset of 28 European Parliaments and a Scalable Multilingual LLM-Based Classification](https://arxiv.org/abs/2602.16516)

**Authors**: Taja Kuzman Punger\v{s}ek, Peter Rupnik, Daniela \v{S}irini\'c, Nikola Ljube\v{s}i\'c  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16516v1  

#### Abstract
This paper introduces ParlaCAP, a large-scale dataset for analyzing parliamentary agenda setting across Europe, and proposes a cost-effective method for building domain-specific policy topic classifiers. Applying the Comparative Agendas Project (CAP) schema to the multilingual ParlaMint corpus of ov...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Supercharging Agenda Setting Research: The ParlaCAP Dataset of 28 European Parliaments and a Scalable Multilingual LLM-Based Classification*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Comparative Agendas Project (CAP)** 研究依赖大量人工标注政策议题标签，限制了跨国家、长时间跨度的议会话语比较研究。现有自动分类模型多基于**非领域内（out-of-domain）** 数据训练（如新闻或行政文件），在处理议会语境时表现不佳。

本研究旨在解决以下挑战：
- 如何高效、低成本地为大规模多语言议会语料（如 ParlaMint）进行 CAP 政策主题标注；
- 如何构建一个**领域适配性强、高精度、可扩展的多语言政策话题分类器**。

### 🚀 提出的新方法与创新
提出了一种 **LLM teacher-student 框架**，用于构建领域专用的多语言政策话题分类器：
1. **Teacher 阶段**：使用强大的闭源 LLM（GPT-4o）自动标注训练数据；
2. **Student 阶段**：用 LLM 标注的数据微调轻量级多语言 BERT 模型（如 XLM-RoBERTa 和 XLM-R-Parla），实现高效推理。

此外，引入了针对稀有类别（如 *Public Lands*）的**基于关键词检索 + LLM 过滤的数据增强策略**，有效缓解数据不平衡问题。

### ⭐ 相比现有方法的优势
| 维度 | 本文方法（ParlaCAP） | 传统方法（如 CAP Babel Machine） |
|------|------------------------|-------------------------------|
| **训练数据来源** | 领域内（in-domain）议会语料（ParlaMint） | 跨领域（新闻、预算文件等） |
| **标注方式** | LLM 自动生成（成本低、可扩展） | 人工标注（昂贵、耗时） |
| **模型适用性** | 专为议会话语优化 | 泛化于多种文本类型 |
| **性能表现** | 在议会数据上显著更优 | 在目标领域外泛化能力有限 |

> 💡 **核心创新**：首次将 LLM 作为高质量“虚拟标注员”应用于 CAP 政策分类任务，并验证其可靠性接近人类水平。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 类型 | 名称 | 描述 |
|------|------|------|
| **训练数据** | `ParlaMint 4.1` | 来自 29 个欧洲国家/地区的议会语料，共 34,800 条演讲；经 GPT-4o 自动标注后扩充至 35,579 条（含 Public Lands 增强） |
| **测试数据** | `ParlaCAP-test` | 四种语言的人工标注测试集：<br>- 英语（EN, 876 条）<br>- 克罗地亚语（HR, 869 条）<br>- 塞尔维亚语（SR, 874 条）<br>- 波斯尼亚语（BS, 824 条）<br>每类约平衡采样，排除 "do not know" 和 "Other"/"Mix" |
| **最终应用数据** | `ParlaMint 5.0` | 包含超过 **800 万条**来自 28 个议会的演讲，用于生成完整的 **ParlaCAP dataset** |

### 🧪 实验设置与评估指标
- **标签体系**：采用 CAP 的 21 个主类别 + 新增 "Other" 类别 → 共 **22 类**。
- **模型架构**：
  - Student 模型候选：`XLM-RoBERTa-large` 和 `XLM-R-Parla`（后者额外预训练于议会语料）
  - Teacher 模型：`GPT-4o`（仅用于标注，不直接部署）
- **微调配置**：
  - 学习率：1e-5
  - Epochs：3
  - Batch size：16
  - 三次重复训练以支持显著性检验
- **评估指标**：
  - 主要：**macro-F1**, **micro-F1**, **accuracy**
  - 可靠性分析：**Krippendorff’s alpha**（衡量人-人、人-LLM 一致性）

### 🔁 基线方法对比
对比了 Sebok et al. (2024) 开发的多个公开可用的 CAP 分类模型，均基于 `xlm-roberta-large` 微调，但在不同领域数据上训练：
- `party-cap`, `parlspeech-cap`, `execspeech-cap`, `media-cap`, `budget-cap`, `social-cap` 等系列模型

这些模型代表当前主流做法——使用手动标注但非议会原生数据进行训练。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见 Table 2 & Table 3）

#### 表：各模型在四语言测试集上的 macro-F1 表现（Table 2）
| Model | EN | HR | SR | BS |
|-------|-----|-----|-----|-----|
| **ParlaCAP (XLM-R-Parla + PL)** | **0.72** | **0.68** | **0.71** | **0.64** |
| XLM-R-Parla (w/o PL) | 0.70 | 0.66 | 0.68 | 0.62 |
| XLM-RoBERTa (baseline) | 0.68 | 0.65 | 0.67 | 0.63 |
| GPT-4o（上限参考） | 0.74 | 0.68 | 0.72 | 0.63 |

✅ **结论**：  
- 使用 **XLM-R-Parla** 并加入 **Public Lands 数据增强**的模型表现最佳；
- 性能已接近 GPT-4o 上限，且远优于基础 XLM-RoBERTa；
- 在所有语言上均取得最优结果。

#### 加入置信度过滤后的最终性能（Table 3）
设定预测置信度阈值为 **0.60**，低于该值标记为 "Mix"（不确定），其余保留：

| Dataset | Micro-F1 | Macro-F1 | Accuracy |
|--------|----------|----------|----------|
| EN | 0.76 | 0.76 | 0.76 |
| HR | 0.72 | 0.73 | 0.72 |
| SR | 0.75 | 0.74 | 0.75 |
| BS | 0.69 | 0.68 | 0.69 |

📌 说明：通过过滤低置信样本，可在 **约 90% 数据上实现高达 0.76 的 F1 分数**，具备实际应用价值。

### 🔍 与现有 CAP 模型的对比（Table 4）
| Model Type | EN | HR | SR | BS |
|-----------|-----|-----|-----|-----|
| **ParlaCAP** | **0.72** | **0.69** | **0.71** | **0.65** |
| CAP Babel Machine variants | 0.52–0.64 | 0.44–0.63 | 0.48–0.62 | 0.46–0.57 |

✅ **关键发现**：  
> 所有基于其他领域数据微调的 CAP 模型，在议会数据上的表现**全面落后于 ParlaCAP**，即使它们也使用相同的 backbone 模型。

这证明：**领域对齐的训练数据比标注质量本身更重要** —— 即使是 LLM 自动生成的标注，只要来自目标领域，也能训练出更优模型。

### 🔬 消融实验与关键验证

#### （1）LLM 是否可靠？—— 与人类标注者的一致性
- 三位人类标注员之间的 Krippendorff’s alpha：**0.59–0.68**
- 人类 vs GPT-4o 的一致性：**0.60–0.64**

👉 结论：**GPT-4o 的标注质量与人类相当**，支持其作为“教师”的合理性。

#### （2）数据增强效果（Public Lands）
- 原始训练集中 Public Lands 仅有 ~145 条；
- 经关键词检索 + GPT-4o 过滤后新增 779 条；
- 对应类别 F1 分数从 **0.30 提升至 0.80**！

✅ 显著改善长尾类别性能，验证了“find the needle in a haystack”策略的有效性。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **LLM 可替代人工标注**：GPT-4o 在 CAP 政策分类任务中的表现与人类标注员一致，可用于低成本生成高质量训练数据。
2. **领域适配至关重要**：在议会语料上训练的模型（即使使用 LLM 标注）显著优于在其他领域（如媒体、预算）上训练的传统模型。
3. **Teacher-Student 框架高效可行**：利用 LLM 构建训练集 + 微调小型 BERT 模型，实现了高性能与高效率的统一。
4. **ParlaCAP 数据集具有重大研究价值**：提供覆盖 28 国、800 万条演讲的结构化数据，包含：
   - CAP 政策主题标签
   - ParlaSent 情感分析结果（正/中/负）
   - 丰富的 speaker 和 party metadata（政党身份、性别、职位等）

### ⚠️ 局限性
- 测试集未公开发布（防止污染 LLM 预训练），可能影响复现与外部验证；
- 当前模型仍依赖英文翻译版本（ParlaMint.en-ana）进行关键词检索，存在翻译误差风险；
- “Other” 类占比高（尤其在 BS 数据中达 10.4%），反映部分议会发言高度程序化或情绪化，降低议题识别覆盖率。

### 🔮 未来工作方向
- 将框架扩展至更多政策编码体系（如 UN Sustainable Development Goals）；
- 探索完全开源 LLM（如 Llama 系列）替代闭源 GPT-4o，提升可复制性；
- 结合动态注意力机制，实现细粒度句子级议题追踪；
- 利用 ParlaCAP 数据开展跨国民主代表性、议程竞争、性别政治等深层社会科学研究。

---

## 📦 附加资源
- **ParlaCAP 分类器**：[Hugging Face](https://www.doi.org/10.57967/hf/6684)
- **ParlaCAP 数据集**：[CROSSDA Repository](https://doi.org/10.23669/1ZTELP)
- **代码与教程**：[GitHub - ParlaCAP Analysis Tutorials](https://github.com/clarinsi/ParlaCAP-Analysis-Tutorials)

> 本文标志着 NLP 技术赋能政治科学的大规模实证研究迈入新阶段，为 agenda setting 理论提供了前所未有的数据基础和技术工具。

</details>

---

### 4. [DistributedEstimator: Distributed Training of Quantum Neural Networks via Circuit Cutting](https://arxiv.org/abs/2602.16233)

**Authors**: Prabhjot Singh, Adel N. Toosi, Rajkumar Buyya  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2602.16233v1  

#### Abstract
Circuit cutting decomposes a large quantum circuit into a collection of smaller subcircuits. The outputs of these subcircuits are then classically reconstructed to recover the original expectation values. While prior work characterises cutting overhead largely in terms of subcircuit counts and sampl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DistributedEstimator: Distributed Training of Quantum Neural Networks via Circuit Cutting**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
当前量子机器学习（QML）中的 **Variational Quantum Algorithms (VQAs)** 和 **Quantum Neural Networks (QNNs)** 在训练过程中面临硬件限制：实际可用的量子设备（如NISQ设备）在宽度（qubit数量）、深度和保真度上无法直接执行大规模量子电路。虽然 **Circuit Cutting** 技术可以将大电路分解为可执行的子电路，但其系统级开销（如调度、重构时间、协调成本）尚未被充分量化。

现有研究多从算法角度分析切割的理论开销（如子电路数量、采样复杂度），但缺乏对 **end-to-end训练流程中真实运行时开销** 的测量，尤其是在分布式并行执行下的性能瓶颈。

### **提出了什么新方法或新思路**
本文提出 **DistributedEstimator** —— 一种**显式建模Circuit Cutting为分阶段分布式工作流**的执行框架，并引入**细粒度仪器化（instrumentation）机制**，将每个Estimator查询分解为以下四个阶段进行监控：

1. **Partitioning**：选择切割位置，生成子电路划分方案  
2. **Subexperiment Generation**：生成所有需执行的子实验及其重建系数  
3. **Parallel Execution**：在分布式worker池中并行执行子任务  
4. **Classical Reconstruction**：聚合结果以恢复原始期望值  

该框架首次将Circuit Cutting视为一个典型的 **staged distributed pipeline**（类似MapReduce），从而可以从系统层面分析其性能特征。

### **相比现有方法的优势**
- **系统视角创新**：不同于以往仅关注“能切”或“多少次采样”，本工作聚焦于“**墙钟时间（wall-clock time）去哪了**”，揭示了真实训练中的性能瓶颈。
- **端到端可测量性**：通过日志记录各阶段耗时，支持对开销来源的精确归因（overhead attribution）。
- **支持调度策略评估**：允许实现不同的任务调度策略（如staggering），用于缓解straggler效应。
- **联合评估模型质量**：不仅看速度，还同步追踪准确率（accuracy）和鲁棒性（robustness），确保性能提升不以牺牲模型质量为代价。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **Iris**：经典三类鸢尾花数据集，二分类版本用于测试（`iris_binary_pml`）
- **MNIST**：手写数字数据集，构建为二分类任务（如0 vs 非0）

### **实验设置**
- **模型结构**：
  - Feature Map: `ZFeatureMap`
  - Ansatz: `RealAmplitudes`（重复次数 reps=1）
  - Observable: Pauli-Z tensor product（`SparsePauliOp`）
- **训练协议固定**：
  - Shot预算统一为 **1024 shots per (sub)experiment**
  - 使用相同优化器（PyTorch SGD）和训练轮数（budget）
  - 种子（seed）控制变量，保证可比性
- **切割配置**：
  - 对比不同切割数量：`NO_CUT`, `1-cut`, `2-cut`, `3-cut`
  - 切割由 `qiskit-addon-cutting` 自动完成
- **分布式执行环境**：
  - 基于线程池模拟worker池（`subexp_workers`）
  - worker数量从1到16变化，用于分析扩展性
- **注入straggler控制**：
  - 模拟异构延迟：部分子任务随机延迟0.1秒（概率p=0.2），用于测试尾部延迟敏感性

### **评估指标**
| 类别 | 指标 |
|------|------|
| **系统性能** | `T_total`, `T_part`, `T_gen`, `T_exec`, `T_rec`, Speedup, Reconstruction占比 (`T_rec / T_total`) |
| **扩展性** | 不同worker数下的加速比 |
| **容错性** | 注入straggler后的相对慢化（slowdown ratio） |
| **学习效果** | Test Accuracy（测试准确率） |
| **鲁棒性** | 在Gaussian噪声和FGSM攻击下的平均准确率 |

### **基线方法对比**
- **Baseline**: 无切割（`NO_CUT`）的原始电路执行
- **Cut Variants**: 同一模型下启用1~3次circuit cutting
- 所有比较均在**相同shot预算、训练迭代数、种子、硬件模拟器（Aer）** 下进行，确保公平

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **RQ1: Cutting引入显著端到端开销**
- 图4显示，在Iris和MNIST上，随着cut数增加，总训练时间急剧上升。
- 例如，在Iris上，3-cut比no-cut训练时间高出数倍，且增长非线性，表明开销随切割程度加剧。

#### ✅ **RQ2: Reconstruction成为关键路径瓶颈**
- 表I显示，随着cut数增加，**reconstruction阶段占单次query总时间的比例持续升高**：
  | #cuts | median T_rec/T_total | p95 T_rec/T_total |
  |-------|------------------------|--------------------|
  | 1     | 43.0%                  | 47.4%              |
  | 2     | 48.9%                  | 53.3%              |
  | 3     | 53.0%                  | 58.0%              |
- 即使子电路可高度并行执行，串行的reconstruction仍限制整体加速。
- 图5显示：**即使worker从1增至16，speedup接近1甚至低于1**，说明并行化收益被协调与聚合开销抵消。

#### ✅ **RQ3: Straggler敏感性依赖于瓶颈所在阶段**
- 图6显示，在注入straggler后，某些配置下训练时间显著延长。
- 但敏感性取决于当前系统的瓶颈：
  - 若已处于**reconstruction主导状态** → execution侧延迟影响较小
  - 若仍处于**execution主导状态** → straggler会显著拖慢整体进度
- 这表明：仅优化执行调度不足以改善性能，必须协同考虑reconstruction设计。

#### ✅ **RQ4: 准确率未下降，部分情况下反而提升**
- 图7显示：
  - **Iris**: 所有cut配置达到完全相同的test accuracy（≈100%）
  - **MNIST**: 多个cut配置的accuracy与baseline相当，个别甚至更高
- 表明：尽管estimator因切割而改变（更多采样+重构），但**最终学习结果未受损**，且可能因噪声模式变化带来有益扰动。

#### ✅ **RQ5: 鲁棒性保持稳定，部分配置更优**
- 图8显示，在Gaussian和FGSM扰动下：
  - Iris：robustness与clean accuracy一致，无退化
  - MNIST：多个cut配置表现出与baseline相当或更高的鲁棒性
- 说明：**切割未损害模型泛化能力和抗攻击能力**

---

## **4. 关键结论和发现**

### **主要发现**
1. **Circuit Cutting本质上是一个分布式流水线问题**，不能仅当作编译优化处理。
2. **Reconstruction是主要性能瓶颈**，其串行特性严重制约并行扩展潜力，即使拥有大量worker也无法有效提速。
3. **端到端开销随cut数快速增长**，主要来自子任务数量膨胀和聚合成本，而非切割规划本身。
4. **Straggler的影响具有上下文依赖性**：当系统已被reconstruction绑定时，execution优化收效甚微。
5. **模型性能（accuracy + robustness）在实测范围内得以保留**，甚至部分cut配置表现更好，说明切割带来的estimator变化未必有害。

### **方法的局限性**
- 当前实验基于**模拟器（Aer）和线程池**，未涉及真实QPUs的排队、网络延迟、批处理等现实因素。
- Reconstruction目前为**集中式串行聚合**，未尝试分布式或增量式重构。
- 调度策略较简单（如staggering），尚未集成更复杂的speculative execution或work-stealing机制。
- 数据集规模有限（Iris/MNIST二分类），尚需验证在更大、更深模型上的普适性。

### **未来工作方向**
1. **分布式/增量式Reconstruction**：
   - 设计树状reduce或流式聚合，减少等待时间，实现execution与reconstruction重叠。
2. **方差感知调度（Variance-aware Scheduling）**：
   - 根据子实验对估计误差的贡献动态分配shots或优先级，提高效率。
3. **显式尾延迟缓解机制**：
   - 引入speculative execution、task replication等策略，并作为一级参数记录与评估。
4. **硬件真实部署验证**：
   - 在混合环境中（simulator + real QPU）测试，纳入queueing delay、network jitter等因素。
5. **更广泛的workload覆盖**：
   - 扩展至更深ansatz、不同feature map、多分类任务，建立预测性性能模型。

---

> **总结一句话**：  
> 本文揭示了 **Circuit Cutting 的真正瓶颈不在量子执行，而在经典重构与协调**；要实现可扩展的分布式QNN训练，必须将 **reconstruction 视为关键系统组件进行优化**，而不仅仅是“后处理步骤”。

</details>

---

### 5. [Toward Scalable Verifiable Reward: Proxy State-Based Evaluation for Multi-turn Tool-Calling LLM Agents](https://arxiv.org/abs/2602.16246)

**Authors**: Yun-Shiuan Chuang, Chaitanya Kulkarni, Alec Chiu, Avinash Thangali, Zijie Pan, Shivani Shekhar, Yirou Ge, Yixi Li, Uma Kona, Linsey Pang, Prakhar Mehrotra  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16246v1  

#### Abstract
Interactive large language model (LLM) agents operating via multi-turn dialogue and multi-step tool calling are increasingly used in production. Benchmarks for these agents must both reliably compare models and yield on-policy training data. Prior agentic benchmarks (e.g., tau-bench, tau2-bench, App...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Toward Scalable Verifiable Reward: Proxy State-Based Evaluation for Multi-turn Tool-Calling LLM Agents  
**论文核心总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前用于评估多轮、多步 **Tool-Calling LLM Agents** 的基准（如 T-bench、AppWorld）依赖于**完全确定性的后端数据库**（deterministic backend），以实现基于最终状态的评估（state-based evaluation）。然而，构建和维护这类系统成本高昂（例如 AppWorld 报告约 60K LOC 引擎代码 + 40K LOC 测试代码），严重限制了迭代速度和工业部署的可扩展性。

该论文旨在解决这一工程瓶颈：  
> **能否在不构建重型确定性后端的前提下，保留 state-based evaluation 的优势？**

---

### 🚀 提出的新方法：Proxy State-Based Evaluation
提出了一种名为 **Proxy State-Based Evaluation** 的新型仿真评估框架，其核心思想是：
- **用 LLM 驱动的状态追踪器（LLM state tracker）从完整的交互轨迹中推断出一个“代理状态”（proxy state）**，替代真实数据库；
- 利用 LLM judges 对比代理最终状态与预期目标，判断任务是否完成；
- 整个环境由多个协作的 LLM 组件构成，形成一个轻量级、可快速搭建的“benchmark-as-environment”。

#### 关键组件（5个协同 LLM 模块）：
| 组件 | 功能 |
|------|------|
| **Reasoning Agent (RA)** | 被评测的智能体模型，执行 ReAct-style 推理与工具调用 |
| **User Simulator (`fuser`)** | 模拟用户行为，生成符合 persona 的查询 |
| **Tool Simulators (`ftool`)** | 模拟工具响应，读写 proxy state |
| **State Tracker (`fstate`)** | 从对话+工具调用历史中推理结构化 proxy state |
| **LLM Judge (`J`)** | 判断目标是否达成，并检测 hallucination |

#### 场景定义（Scenario Schema）
每个任务通过一个 `scenario z` 明确指定：
- 用户目标 `g(z)`
- 用户事实 `u(z)` 和系统事实 `so(z)`
- 预期最终状态 `s*(z)`
- 预期代理行为 `b*(z)`

评估不再关注路径匹配（trajectory matching），而是看最终输出是否满足 `s*(z)` 和 `b*(z)`。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 AppWorld） | 本文方法（Proxy State-Based Eval） |
|------|------------------------|-------------------------------|
| 后端依赖 | 必须有确定性数据库 | 无需真实 DB，仅需 LLM 推理 proxy state |
| 工程开销 | 极高（数万行代码 + 单元测试） | 极低，场景文件即可驱动整个环境 |
| 可扩展性 | 迭代慢，难以随产品演进 | 快速搭建，支持敏捷开发 |
| 数据生成 | 支持 on-policy rollout | 支持 on/off-policy rollout，可用于 SFT/RFT 训练 |
| 评估可靠性 | 高（基于真实状态） | 高（human-LLM judge agreement >90%） |
| 成本 | 高昂 | 显著降低 |

✅ **本质创新**：将“确定性后端”替换为“LLM-inferred proxy state”，实现了 **scalable + verifiable reward** 的统一。

---

## 2. 核心实验方法和设置

### 📚 数据集
- 自建合成数据集，共 **|Z|=208 个场景**，涵盖电商（Commerce）与账户管理（Account Management）两大领域。
- 分为训练集 `Ztrain=157` 和测试集 `Ztest=51`。
- 所有场景均人工设计，确保内部一致性（逻辑自洽）、覆盖多样化 workflow。

---

### ⚙️ 实验设置

#### 领域与工具集（Two Tool Families）
- **Commerce Tools**: Product Discovery, Checkout, Cart Management, Product Q&A, Offers & Promotions
- **Account Tools**: Wallet & Funding, Payment & Transfer, Dispute & Refund, Security & Fraud, Transaction Inquiry

#### 被测模型（Reasoning Agent, RA）
涵盖多种主流 LLM 家族：
- **GPT-5**（不同 reasoning effort: minimal → high）
- **GPT-4o / GPT-4o-mini**
- **Gemini-2.5-pro / flash**
- **Qwen3-235B / Qwen3-30B-A3B-Thinking-2507**

所有 rollout 使用 temperature=1。

#### 模拟器与裁判模型
除非特别说明：
- `fuser`, `ftool`, `fstate`, `J` 均使用 **GPT-5o**（medium reasoning effort）
- 默认 persona 为 **power user**（避免用户侧错误干扰）

#### 用户 Persona 设置
三种 persona 控制用户行为复杂度：
- **Power user**: 清晰表达需求，高效交互
- **Ambiguous user**: 初始信息模糊，需澄清
- **Confused user**: 不理解流程，易误解系统反馈

---

### 📊 评估指标
对任意二值指标 $ x(z) $，定义其 rate 为：

$$
\text{Rate}(x) = \frac{1}{|Z_{\text{test}}|} \sum_{z \in Z_{\text{test}}} x(z)
$$

主要报告以下指标（均在 `Ztest` 上计算）：
| 指标 | 含义 |
|------|------|
| **GC (Goal Completion Rate)** | 目标成功完成的比例 |
| **ER_agent** | 因 agent 错误导致失败的比例 |
| **ER_user** | 因 user 错误导致失败的比例 |
| **HR_tool** | Tool hallucination rate（工具虚构信息） |
| **HR_user** | User hallucination rate（用户虚构信息） |

此外还进行消融实验与敏感性分析。

---

### 🔁 训练范式（Post-Training Support）
验证该环境也可用于训练：
- **On-policy Training (RFT)**: 当前 RA 与环境交互，只保留成功轨迹（c=1）用于 rejection-sampling fine-tuning
- **Off-policy Training (SFT)**: 使用更强 teacher 模型生成成功轨迹，微调 base RA

---

## 3. 主要实验结果和性能指标

### 📈 基线模型比较（Fig. 3）
| 模型 | GC (%) |
|------|--------|
| GPT-5 (high RE) | **85.76** |
| GPT-5 (medium RE) | 84.47 |
| GPT-5 (low RE) | 83.21 |
| GPT-4o | 80.53 |
| GPT-4o-mini | 76.08 |
| Qwen3-235B | 74.59 |
| Qwen3-30B (base) | **65.64** |

✅ **关键发现**：
- GC 随模型规模和推理努力（reasoning effort）单调上升；
- 不同家族间排序合理，表明评估具有良好的区分能力和稳定性。

---

### 🛠️ 训练提升效果（Fine-tuning Results）
在 Qwen3-30B-A3B-Thinking-2507 上进行训练：
| 方法 | GC (%) | 提升幅度 |
|------|--------|---------|
| Base Model | 65.64 | — |
| RFT (on-policy) | 67.11 | +1.47 |
| SFT (off-policy) | **77.34** | **+11.70** |

✅ 表明该环境能有效生成高质量训练数据，且监督信号可迁移到未见场景。

---

### 🔍 消融实验（Ablation Studies）

#### （1）State Tracker 模型强度影响
- 将默认 `GPT-5o` 替换为较弱的 `GPT-4o`
- 结果：**tool hallucination rate 从 1.33% ↑ 至 3.61%**
- 原因：state tracker 推理不准 → proxy state 出错 → tool simulator “读取”错误状态 → 输出失真

➡️ 说明 **accurate state tracking 是稳定评估的关键**。

#### （2）Scenario Fact 完整性消融（Fig. 4）
随机移除部分系统事实或用户事实：
- 移除越多 → **tool hallucination 和 user hallucination 率显著上升**
- 完全移除时可达 25%+ 的 hallucination 率

➡️ 证明：**精心设计的 scenario 文件至关重要**，它锚定了模拟的真实性边界。

---

### 👥 用户 persona 敏感性分析（Fig. 5）
| Persona | ER_user (%) | HR_user (%) |
|--------|-------------|------------|
| Power user | 3.55 | 0.67 |
| Ambiguous user | 5.50 | 2.75 |
| Confused user | 5.14 | 1.87 |

✅ 发现：
- 更复杂的 persona 导致更高的用户侧错误和幻觉；
- 但默认使用 power user 可控地将用户误差压制在低位（<4%），使评估聚焦于 RA 性能。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **Proxy State-Based Evaluation 是可行且可靠的替代方案**：
   - 在无真实数据库的情况下，仍能实现稳定的 state-based evaluation；
   - human-LLM judge agreement > **90%**，支持自动化评估的可信度。

2. **具备良好模型区分能力**：
   - GC 指标随模型大小和推理努力单调增长，排序符合预期；
   - 支持跨模型 family 的公平比较。

3. **支持高效训练数据生成**：
   - 可生成带 reward 标签的 on-policy 和 off-policy rollout；
   - 微调后性能显著提升（SFT 提升超 11% GC），并具泛化能力。

4. **高度可配置与可扩展**：
   - 支持 persona 控制、工具 schema 演进；
   - 场景即代码，易于随产品迭代更新。

---

### ⚠️ 局限性
1. **依赖高质量 LLM 组件**：
   - 若 state tracker 或 tool simulator 能力不足，可能引入 cascading errors；
   - 当前依赖 GPT-5o 级别模型才能维持低 hallucination。

2. **合成场景的真实性边界**：
   - 场景虽多样但仍为人工构造，可能存在分布偏差；
   - 需持续注入真实用户行为数据以增强 realism。

3. **计算成本转移而非消除**：
   - 虽省去工程开发成本，但多次 LLM 调用带来推理开销；
   - 大规模评估时需权衡 cost-efficiency。

---

### 🔮 未来工作方向
1. **降低对强 LLM 的依赖**：
   - 探索更小模型 + prompt engineering 实现可靠 state tracking；
   - 引入 retrieval augmentation 提升 factual consistency。

2. **动态场景生成（Dynamic Scenario Generation）**：
   - 利用 LLM 自动生成新场景，扩大覆盖范围；
   - 支持 adversarial testing 和 stress-testing。

3. **与真实系统联调验证**：
   - 将 proxy evaluation 与 production 系统结果对齐，建立 external validity。

4. **开放基准生态建设**：
   - 开源 scenario schema 与 evaluator，推动 community-wide adoption。

---

## ✅ 总结一句话
> 本文提出的 **Proxy State-Based Evaluation** 框架成功解耦了 agentic benchmark 与 heavy deterministic backend 的强绑定，通过 LLM-driven simulation 实现了 **scalable、reliable、trainable** 的多轮工具调用智能体评估体系，为工业级 LLM agent 的快速迭代提供了实用解决方案。

</details>

---

### 6. [How Reliable is Your Service at the Extreme Edge? Analytical Modeling of Computational Reliability](https://arxiv.org/abs/2602.16362)

**Authors**: MHD Saria Allahham, Hossam S. Hassanein  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2602.16362v1  

#### Abstract
Extreme Edge Computing (XEC) distributes streaming workloads across consumer-owned devices, exploiting their proximity to users and ubiquitous availability. Many such workloads are AI-driven, requiring continuous neural network inference for tasks like object detection and video analytics. Distribut...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*How Reliable is Your Service at the Extreme Edge? Analytical Modeling of Computational Reliability*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对 **Extreme Edge Computing (XEC)** 环境下，由消费者拥有的边缘设备（如手机、平板、可穿戴设备）构成的分布式推理（Distributed Inference, DI）系统所面临的**计算可靠性建模难题**。

传统可靠性模型通常基于节点“正常/失效”的二元假设，而XEC设备具有高度动态的计算能力（受后台应用、电池管理、热节流等影响），其性能是连续波动而非完全宕机。因此，现有方法无法有效量化：**一个设备或设备组在给定时刻能否满足流式AI服务（如实时目标检测）的QoS需求（如延迟、吞吐量）**。

---

### **提出了什么新方法或新思路**

论文提出了一套**形式化的分析框架**，用于建模和量化XEC环境下的 **computational reliability（计算可靠性）**，定义为：

> 在指定的 **Quality of Service (QoS)** 阈值下，设备可用计算容量 $C(t)$ 满足服务需求 $\Delta(t)$ 的概率：
> $$
> R(t) = P\left(\frac{C(t)}{\Delta(t)} \geq \Theta\right)
> $$
> 其中 $\Theta$ 是QoS阈值（例如，$\Theta=3$ 表示设备需提供至少3倍于需求的算力以应对波动）。

该框架包含两个信息层级的建模方法：

1. **Minimal Information (MI) 模型**：
   - 仅依赖设备声明的算力范围 $[C_{\min}, C_{\max}]$ 和服务的需求范围 $[\Delta_{\min}, \Delta_{\max}]$。
   - 假设 $C(t)$ 和 $\Delta(t)$ 在其范围内服从均匀分布，推导出闭式解（closed-form expression）的可靠性表达式（见 Lemma 1）。
   - 适用于无历史交互的新设备或新服务场景。

2. **Historical Data 模型**：
   - 利用历史观测数据，通过 **Maximum Likelihood Estimation (MLE)** 估计 $C(t)$ 和 $\Delta(t)$ 的分布参数。
   - 采用截断正态分布（Truncated Normal Distribution）以保证物理边界约束。
   - 推导出更精确的可靠性表达式（见 Lemma 2），并证明其随数据积累收敛到真实可靠性。

此外，框架扩展至多设备系统，支持以下配置：

- **Series Configuration**：所有设备必须成功（可靠性相乘）。
- **Parallel Configuration**：任一设备成功即可（可靠性互补相乘）。
- **Work Partitioning**：任务按比例分配，各设备承担部分负载。

并提出：

- **最优任务分配规则（Lemma 3）**：最大化系统可靠性的分配应满足“边际对数可靠性相等”条件。
- **设备选择分析边界（Lemmas 4 & 5）**：给出系列部署最大可行设备数、并行冗余最小所需设备数的解析表达式。

---

### **相比现有方法的优势**

| 方面 | 本文优势 |
|------|---------|
| **可靠性定义** | 超越“节点是否存活”，聚焦“**持续性能是否达标**”，更贴合XEC实际挑战。 |
| **建模灵活性** | 支持从“零先验”到“有历史数据”的平滑过渡，适应不同部署阶段。 |
| **分析可扩展性** | 提供**闭式解**，便于实时决策和资源编排（orchestration），无需复杂仿真。 |
| **系统级洞察** | 给出**最优分配策略**和**设备选择边界**，为系统设计提供理论指导。 |

---

## 2. 核心实验方法和设置

### **使用了哪些数据集**

论文未使用公开数据集，而是构建了一个**基于仿真的实验环境**，以 **YOLO11m** 模型处理视频流作为代表性 **Distributed Inference (DI)** 工作负载。

- **模型**：YOLO11m（单阶段目标检测器）
- **输入**：动态分辨率的视频帧（通过缩放因子 `scale` 控制计算需求）
- **平台**：基于 **Docker 容器**模拟多个 XED（Extreme Edge Devices）

---

### **实验设置和评估指标**

#### **仿真架构**
- **容量控制**：通过动态调整容器分配的 **CPU线程数**（2–12线程）来模拟 $C(t)$ 的波动。
- **需求控制**：通过改变输入帧的 **空间分辨率**（scale ∈ [0.1, 0.9]）来模拟 $\Delta(t)$ 的变化（计算成本与像素数成正比）。
- **QoS阈值 $\Theta$**：直接对应目标帧率（FPS）。例如 $\Theta = 3$ 表示需达到3 FPS。

#### **评估流程**
1. **Profiling**：预先测量不同线程数和缩放因子组合下的推理时间，建立“线程→算力”、“缩放→需求”的映射表。
2. **动态采样**：每30帧随机采样一次线程数和缩放因子，模拟真实波动。
3. **记录**：每帧记录：线程数、缩放因子、推理时间、是否满足QoS。
4. **计算**：
   - **Empirical Reliability**：满足QoS的帧占比。
   - **Analytical Prediction**：使用MI和Historical Data模型计算 $R(t,\Theta)$。
   - **Monte Carlo Sampling**：用于验证解析公式的准确性。

#### **评估指标**
- **Reliability $R(t,\Theta)$**：核心指标。
- **Throughput (FPS)** 和 **Latency (ms)**：用于验证系统性能提升。
- **Convergence of MLE Estimates**：历史数据模型随样本量增加的预测精度提升。

---

### **基线方法对比**

- **Analytical Models**：
  - **MI Model (Uniform Assumption)**：仅用边界信息。
  - **Historical Model (MLE + Truncated Normal)**：使用历史数据拟合。
- **Ground Truth**：
  - **Monte Carlo Simulation**：大量采样验证解析积分。
  - **Empirical Measurement**：实际仿真运行结果。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **MI模型验证（Fig. 3）**
- 在 $C \in [55,152]$ GFLOPS, $\Delta \in [55,278]$ GFLOPS, $\Theta \in [1,4]$ FPS 下：
  - **Analytical**, **Monte Carlo**, 和 **Empirical** 曲线高度一致。
  - 证明 **Lemma 1 的闭式解准确可靠**。

#### ✅ **Historical Data 模型验证（Fig. 4）**
- 使用 MLE 拟合截断正态分布后，**Analytical**, **MC**, **Simulation** 结果依然高度吻合。
- **MLE 收敛性（Fig. 5）**：
  - 仅需约 **50个独立样本**，MLE估计的可靠性即收敛到真实值。
  - 相比MI模型（初始估计 $R \approx 0.55$），最终收敛到 $R \approx 0.82$，**绝对提升达 0.27**。

#### ✅ **系统级配置验证（Fig. 6 & 7）**
- **Series System（Fig. 7）**：
  - 单设备：1.9 FPS，520 ms 延迟。
  - 4设备分区并行：**5.3 FPS（2.8×提升）**，**207 ms 延迟（2.5×降低）**。
  - 可靠性：单设备 $R > 0.8$，但系统可靠性因乘积效应降至 **~0.5 @ 5 FPS**。
- **Device Selection Bounds（Fig. 6）**：
  - **Series**：高可靠设备（$R=0.99$）最多支持10台串联；低可靠（$R=0.95$）仅支持2台。
  - **Parallel**：为达到 $e=0.99$，$R=0.9$ 设备需2台，$R=0.5$ 设备需9台。
  - 历史数据模型预测与仿真结果匹配良好。

---

### **与基线方法的对比结果**

| 对比项 | 结果 |
|--------|------|
| **MI vs Empirical** | MI模型提供**保守下界**，略低于实测值（因假设均匀分布，低估集中趋势）。 |
| **Historical vs MI** | Historical模型显著**更接近实测值**（如在 $\Theta=3$ 时，MI预测 $R=0.69$，Historical预测 $R=0.85$，实测 $R=0.87$）。 |
| **Analytical vs Monte Carlo** | 解析结果与蒙特卡洛采样几乎完全重合，验证公式正确性。 |

---

### **消融实验结果**

- **容量影响（Fig. 3b）**：更高线程数（更高 $C$）使可靠性曲线右移，可在更高QoS下维持高可靠性。
- **需求影响（Fig. 3c）**：低需求（小scale）下可靠性接近1；高需求下迅速下降。
- **MLE样本量影响（Fig. 5）**：可靠性估计随样本量单调收敛，前50个样本带来最大提升。

---

## 4. 关键结论和发现

### **主要发现**

1. **Computational Reliability 是XEC的核心挑战**：设备性能波动是常态，需从“持续性能达标”角度重新定义可靠性。
2. **MI模型实用性强**：即使无历史数据，仅凭声明边界也能提供**保守但可用的可靠性估计**，支持快速决策。
3. **Historical Data 显著提升精度**：少量历史数据即可通过MLE大幅提升预测准确性，实现**渐进式可靠性感知**。
4. **系统配置权衡明确**：
   - **Series**：性能提升明显，但可靠性呈指数衰减，需严格筛选高可靠设备。
   - **Parallel**：牺牲算力换可靠性，适合关键任务。
5. **最优分配非均等**：应将更多负载分配给“可靠性随负载增长更平缓”的设备。

---

### **方法的局限性**

1. **假设独立性**：模型假设设备间计算成功事件相互独立，忽略了可能的共因故障（如区域网络中断）。
2. **忽略通信开销**：当前框架聚焦**计算可靠性**，未显式建模传输延迟和带宽波动对端到端QoS的影响。
3. **静态QoS阈值**：$\Theta$ 被视为常量，未考虑动态调整的可能性。
4. **分布假设**：虽支持一般化，但MLE实验中默认使用截断正态分布，未必适用于所有场景。

---

### **未来工作方向**

1. **扩展至通信可靠性**：将网络延迟、丢包等纳入联合可靠性模型，构建 **End-to-End Reliability Framework**。
2. **动态QoS与自适应调度**：结合可靠性预测，实现动态负载调整和设备重选。
3. **跨层优化**：联合优化计算、通信、缓存资源，最大化系统效用。
4. **真实世界部署验证**：在真实XED集群上测试框架的有效性和开销。
5. **安全与激励机制**：在可靠性模型中引入设备可信度和激励机制，防止恶意或懒惰节点影响系统。

--- 

> **总结**：该论文首次为XEC环境下的流式AI服务提供了**可计算、可扩展、可操作的可靠性分析框架**，填补了从“资源可用性”到“服务质量保障”之间的理论空白，为构建真正可靠的极端边缘智能系统奠定了重要基础。

</details>

---

### 7. [LLM-Driven Intent-Based Privacy-Aware Orchestration Across the Cloud-Edge Continuum](https://arxiv.org/abs/2602.16100)

**Authors**: Zijie Su, Muhammed Tawfiqul Islam, Mohammad Goudarzi, Adel N. Toosi  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16100v1  

#### Abstract
With the rapid advancement of large language models (LLMs), efficiently serving LLM inference under limited GPU resources has become a critical challenge. Recently, an increasing number of studies have explored applying serverless computing paradigms to LLM serving in order to maximize resource util...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM-Driven Intent-Based Privacy-Aware Orchestration Across the Cloud-Edge Continuum

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在 **cloud-edge continuum**（云边协同）环境中，随着边缘计算和物联网（IoT）设备的普及，大量敏感数据（如医疗、金融、个人身份信息）在边缘生成并需要处理。然而，传统编排系统（如 Kubernetes 和 SDN 控制器）存在以下挑战：

- **隐私合规性难以保障**：数据必须满足数据驻留法规（如 GDPR），即特定数据只能在指定司法管辖区（如欧盟）内存储和传输。
- **配置复杂且依赖专家**：将高层级的隐私意图（如“所有个人数据不得离开欧盟”）转化为底层的 Kubernetes 节点选择规则和 SDN 流控策略，需要领域专家手动编写数十条低级策略。
- **跨层协调困难**：现有方案通常只关注计算层（placement）或网络层（routing），缺乏对 **compute + network** 的联合控制。

### 🚀 提出的新方法与创新思路
本文提出了一种 **LLM 驱动的 intent-based 框架**，实现自然语言隐私意图到可执行策略的自动化转换与部署：

- **LLM 作为自然语言接口**：使用 GPT-4o 将用户输入的自然语言隐私意图（如“确保所有健康数据仅在高安全性的云端节点运行”）解析为结构化策略。
- **跨层联合编排**：
  - 在 **计算层**，生成 Kubernetes 的 `nodeSelector` 或亲和性规则，约束 Pod 只能调度到符合隐私要求的节点上。
  - 在 **网络层**，生成 SDN（ONOS）的流规则，确保数据包仅通过合规路径传输（如避开不受信交换机）。
- **闭环验证机制**：构建了一个自动化的验证流水线，在真实 Kubernetes + ONOS 测试平台上部署策略，并检查运行时状态是否满足原始意图。

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| **意图表达方式** | DSL 或预定义策略模板 | 自然语言输入，非专家也可用 |
| **覆盖范围** | 单一层（计算或网络） | 跨层联合控制（compute + network） |
| **自动化程度** | 手动配置或半自动 | 全流程自动化（解析 → 编译 → 部署 → 验证） |
| **评估手段** | 手动验证或有限测试用例 | 构建了含 90 条意图的标准 benchmark + 自动化 validator |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 构建了一个包含 **90 条多样化隐私意图** 的 benchmark 数据集，分为三类：
  - **Computing-only**（30 条）：仅涉及计算资源调度（如“将 PHI 处理服务部署在 EU 节点”）
  - **Networking-only**（30 条）：仅涉及网络路径控制（如“流量必须经过备份交换机 s8”）
  - **Hybrid**（30 条）：同时包含计算与网络约束（如“预约服务部署在高安全云节点，且通信需绕经 s8”）
- 每条意图进一步按复杂度分类：
  - **Simple**（38 条）：单条件
  - **Complex**（52 条）：多条件或例外情况

### ⚙️ 实验设置
- **平台架构**：
  - **Orchestration Core**：K3s（轻量级 Kubernetes）用于容器编排
  - **Network Controller**：ONOS + Mininet + Open vSwitch 模拟 SDN 网络
  - **LLM 接口**：调用 GPT-4o API 进行意图解析
- **基础设施标签体系**：
  - 节点标注 `location=EU`, `security=high`, `zone=cloud/edge` 等
  - 网络设备标注 `mfr=CISCO/Huawei`, `location`, `trusted=yes/no` 等
- **应用负载**：模拟医院信息系统，包含 `PHI-DB`, `Patient`, `Appointment` 等微服务，具有不同敏感级别

### 🎯 评估指标
| 指标 | 定义 |
|------|------|
| **End-to-end Success Rate** | 意图被正确解析、部署并通过 validator 检查的比例 |
| **Avg. Completion Time** | 从输入意图到完成验证的端到端延迟（秒） |
| **Token Usage** | LLM 输入输出总 token 数 |
| **Avg. Checks / Task** | 每个意图触发的原子验证断言数量（反映策略复杂度） |
| **Failure Mode Analysis** | 对失败案例进行归因分析 |

### 🔀 基线方法对比
比较了三种主流 LLM 在相同 prompt 模板下的表现：
- **GPT-4o**（OpenAI）
- **Claude 3.5 Haiku**（Anthropic）
- **DeepSeek-V3 (671B)**（开源大模型）

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（基于 GPT-4o）

| 指标 | 数值 |
|------|------|
| **总体成功率** | **95.6%**（86/90 成功） |
| **平均端到端耗时** | **20.97 秒** |
| **平均每任务 token 数** | 15,133 |
| **平均每任务验证断言数** | 3.7 个 |

### 🆚 与其他 LLM 的对比结果

| Model | Success Rate | Avg. Time (s) | Notes |
|-------|--------------|----------------|-------|
| **GPT-4o** | **95.6%** | 20.97 | 最高准确率，响应快 |
| **Claude 3.5 Haiku** | 86.7% | ~20 | 计算任务满分，混合任务下降明显 |
| **DeepSeek-V3** | 77.8% | ~88 | 响应极慢，准确率最低 |

#### 分域成功率（GPT-4o）：
- **Computing**: 100.0%
- **Hybrid**: 96.7%
- **Networking**: 90.3%

> 💡 **说明**：网络层策略更易出错，因其依赖动态拓扑和精确的 `<src, dst, must_go>` 三元组，模糊描述会导致“no-op policy”。

#### 按复杂度划分：
- **Simple intents**: 96.2% 成功率
- **Complex intents**: 94.9% 成功率  
→ 差异不显著，表明该框架对复杂意图具备良好鲁棒性。

### 🔍 消融实验与失败模式分析
通过对失败的 4 个案例进行后验分析，识别出四大系统性错误来源：

| 失败模式 | 描述 | 示例 |
|--------|------|------|
| **Semantic Congestion** | 多条件融合句中，LLM 仅满足第一个子句 | “A 且 B 且 C” → 只处理 A |
| **Ambiguous Path Specification** | 缺少明确源/目的地址 | “与 host4 通信的流量…” → 未指定具体主机对 |
| **Hallucinated Identifiers** | 生成不存在的标签 | 输出 `region: eu_region`，但实际是 `location=EU` |
| **Partial Topology Awareness** | 忽略中间传输设备的位置标签 | 排除 `region-b`，但路径仍经过其聚合交换机 |

✅ **缓解措施有效**：对失败意图进行 **句子分解** 或 **显式枚举约束** 后，重试均成功。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **GPT-4o 能高效将自然语言隐私意图转化为可执行策略**，在 90 个测试意图中达到 **95.6% 的端到端成功率**，平均延迟约 **21 秒**，具备实用价值。
2. **跨层联合控制是可行且必要的**：单一计算或网络策略不足以保证隐私合规；本文首次实现了 compute + network 的统一 intent 编译与验证。
3. **自动化验证至关重要**：人工验证成本高昂（可达数小时），而本文的 validator 可在几秒内完成闭环检查，极大提升可靠性。
4. **Hybrid intents 是最难处理的类别**：虽然成功率仍高达 96.7%，但其平均耗时达 **39.2 秒**，token 消耗超 28k，建议采用 **轻量级意图分解** 策略优化。

### ⚠️ 局限性
- **依赖标签完整性与一致性**：若节点或网络设备未正确标注（如缺失 `location` 字段），则无法正确执行策略。
- **外部 LLM 存在隐私泄露风险**：用户意图可能包含敏感上下文，需考虑数据脱敏或本地化部署。
- **当前 success rate 不足以支持完全无人干预部署**：95.6% 虽高，但在高关键性场景（如医疗、金融）仍需 human-in-the-loop 审核。
- **测试环境为受控仿真平台**：尚未在大规模、多租户生产环境中验证。

### 🔮 未来工作方向
1. **自动意图分解（Automatic Intent Decomposition）**：将复杂 hybrid intent 拆解为多个简单子任务，降低 LLM 解析难度。
2. **领域适配与微调（Domain Adaptation/Fine-tuning）**：训练专用 LLM 以减少幻觉和标签误用。
3. **增强 label schema portability**：设计跨云厂商通用的标签标准，提升可移植性。
4. **实证研究（Empirical Study）**：在真实行业场景（如 healthcare IoT、regulated finance）中评估有效性，并与人工配置基线对比。
5. **fail-closed 部署机制**：对于关键意图，引入分阶段 rollout 和审批流程，防止错误策略上线。

---

> 🧠 **最终愿景**：让用户只需“说出隐私偏好”，AI 即可自动将其转化为 Kubernetes + SDN 中的 enforceable policies —— 实现 **human intent 与 machine execution 的无缝对齐**。

</details>

---

### 8. [push0: Scalable and Fault-Tolerant Orchestration for Zero-Knowledge Proof Generation](https://arxiv.org/abs/2602.16338)

**Authors**: Mohsen Ahmadvand, Rok Pajni\v{c}, Ching-Lun Chiu  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16338v1  

#### Abstract
Zero-knowledge proof generation imposes stringent timing and reliability constraints on blockchain systems. For ZK-rollups, delayed proofs cause finality lag and economic loss; for Ethereum's emerging L1 zkEVM, proofs must complete within the 12-second slot window to enable stateless validation. The...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：push0: Scalable and Fault-Tolerant Orchestration for Zero-Knowledge Proof Generation**

---

## 1. **论文的主要贡献和创新点**

### **解决了什么问题**
零知识证明（ZKPs）在区块链系统中扮演着关键角色，尤其在 **ZK-Rollups** 和以太坊即将推出的 **L1 zkEVM** 中。然而，ZK 证明生成面临以下挑战：
- **严格的时序约束**：L1 zkEVM 要求在 12 秒的 slot 内完成证明生成（P99 < 10s），否则无法参与共识。
- **链头顺序一致性**：必须按区块顺序生成证明，避免“链停滞”（chain halt）。
- **容错与任务重分配**：硬件故障、GPU 内存溢出等常见问题需自动恢复。
- **异构 zkVM 支持**：不同证明系统（如 STARK、SNARK、递归聚合）流程差异大，难以统一调度。
- **缺乏标准化的编排框架**：现有方案要么是中心化、封闭系统，要么缺乏严格排序和容错机制。

### **提出了什么新方法或新思路**
作者提出 **push0** —— 一个云原生、事件驱动的 **ZK 证明编排系统**，其核心设计包括：

#### ✅ **Dispatcher-Collector 架构**
- **Dispatcher**：从优先级队列拉取任务，调用 prover 二进制文件，无状态，可水平扩展。
- **Collector**：接收多个输入流，通过可插拔的 **collection strategy**（如 Match、Sequential）进行聚合，实现屏障同步（barrier synchronization）。
- **消息总线**（Message Bus）：基于持久化优先级队列（如 NATS JetStream），确保任务不丢失，并支持 at-least-once 语义。

#### ✅ **隐式工作流建模**
- 工作流（workflow DAG）通过 **队列拓扑和收集策略** 隐式定义，无需显式 DAG 数据结构。
- 支持多队列并行流水线（multi-queue pipeline），实现 **提案流（proposer flow）** 与 **证明流（proving flow）** 解耦。

#### ✅ **分区亲和路由（Partition-Affine Routing）**
- 所有属于同一聚合组（如 block_num）的消息被路由到同一个 Collector，解决 **barrier fragmentation** 问题。
- 路由公式：`partition(m) = h(g) mod C`，其中 `g` 是分组键（如 block_num），`C` 是 Collector 数量。

#### ✅ **Prover-Agnostic 设计**
- 支持两种集成模式：
  - **JSON Executor**：适用于 CLI 工具，通过 `--input-path` 和 `--output-path` 接口调用。
  - **Composer Executor**：适用于 Rust 库，直接调用 `prove()` 函数，零拷贝共享内存。
- 编排层完全与 prover 解耦，只需在调用边界适配。

### **相比现有方法的优势**

| 特性 | push0 | DIZK / SHARP | Decentralized Markets (Snarktor, zkCloud) | Kubernetes |
|------|-------|---------------|------------------------------------------|------------|
| **Head-of-chain ordering** | ✅ 严格保证 | ❌ 依赖中心化控制 | ❌ 无保障 | ❌ 无内置机制 |
| **Fault tolerance** | ✅ 自动重试、心跳、红队交付 | ⚠️ 有限 | ⚠️ 依赖经济激励 | ✅ 但需自定义 Operator |
| **Prover agnosticism** | ✅ 完全支持异构 zkVM | ❌ 绑定特定系统 | ✅ 但调度不可控 | ✅ 但无编排语义 |
| **Horizontal scalability** | ✅ 无状态 Dispatcher 可扩展 | ✅ 但同构 | ✅ | ✅ |
| **Observability** | ✅ Prometheus + OpenTelemetry | ❌ | ❌ | ✅ 但需额外开发 |
| **Barrier synchronization** | ✅ Collector + 分区亲和 | ⚠️ 手动管理 | ❌ | ❌ |

> **核心优势**：push0 是首个同时满足 **六个生产级要求** 的 ZK 编排系统：
> 1. Head-of-chain ordering  
> 2. Bounded latency  
> 3. Fault tolerance  
> 4. Prover agnosticism  
> 5. Horizontal scalability  
> 6. Observability

---

## 2. **核心实验方法和设置**

### **实验环境**
- **主实验**：3 节点 AWS Kubernetes 集群（t3.medium，2 vCPU，4GB RAM），使用 **Synadia NGS**（云托管 NATS）作为消息总线。
- **验证实验**：单机 Docker Compose（Apple M3 Max，36GB RAM），本地 NATS JetStream，消除网络波动影响。

### **模拟 Prover**
- 使用 **echo executor** 模拟不同延迟的证明任务（0ms ~ 5s），隔离纯编排开销。
- 真实 workload 使用 **SP1 GPU prover** 进行生产部署验证。

### **工作负载生成**
- Python 脚本向 NATS 发布 JSON 消息，包含 `block_num`、`task_id` 等字段。
- 任务数量随 Dispatcher 数量线性增长（`N = 10 × D`），确保负载均衡。

### **评估指标**
| 指标 | 描述 |
|------|------|
| **Orchestration Overhead** | 从任务发布到结果接收的时间（不含 prover 执行时间） |
| **P50 / P99 Latency** | 编排延迟的中位数和尾部延迟 |
| **Scaling Efficiency** | `S/D`，即加速比除以 Dispatcher 数量，衡量并行效率 |
| **Memory Consumption** | 各组件内存占用 |
| **Fault Recovery Time** | 组件崩溃后任务恢复时间 |
| **Barrier Completion Rate** | 多输入 Collector 成功聚合的比例 |

### **基线方法对比**
- **Linear Pipeline**：串行处理，每阶段阻塞前一阶段。
- **Round-Robin Routing**：Collector 间轮询分配消息，无分区亲和。
- **No Heartbeat**：不启用心跳机制，测试长任务超时行为。

---

## 3. **主要实验结果和性能指标**

### **关键性能数据**

#### ✅ **极低的编排开销**
| 环境 | 注入速率 (tasks/s) | P50 (ms) | P99 (ms) |
|------|---------------------|----------|----------|
| Kubernetes + Synadia NGS | 10–100 | **5.2–5.3** | **8.0–8.5** |
| Docker + Local NATS | 100 | **2.7** | **4.9** |

> - 编排开销仅占典型证明时间（7+ 秒）的 **<0.1%**。
> - 即使在高负载下仍保持稳定。

#### ✅ **近乎完美的水平扩展效率**
| Prover 延迟 | 1 Dispatcher | 32 Dispatchers | 效率 |
|-------------|--------------|----------------|------|
| 0ms（压力测试） | 100% | 14% | 低（协调开销主导） |
| 100ms | 100% | 55% | 中等 |
| **1s** | 100% | **99%** | ✅ 几乎完美 |
| **5s** | 100% | **100%** | ✅ 完美线性扩展 |

> - 对于真实 ZK workload（1s+），push0 实现 **99–100% 扩展效率**。

#### ✅ **分区亲和路由显著提升屏障完成率**
| 路由策略 | Collector 数 | Barrier 输入数 | 完成率 |
|--------|-------------|----------------|--------|
| Round-Robin | 4 | 4 | **5%**（理论值 1.56%） |
| **Partition-Affine** | 4 | 4 | **100%** |

> - 朴素负载均衡导致 **95% 的屏障失败**，而分区亲和路由彻底解决此问题。

#### ✅ **多队列架构显著提升容错能力**
| 架构 | 总耗时 (s) | 最终性偏移（blocks） | 说明 |
|------|-----------|----------------------|------|
| **Multi-Queue (push0)** | **23.9** | 25 | 提案流继续推进 |
| Linear Pipeline | 101.5 | 0（阻塞） | 全链阻塞 |

> - 在注入一个 15 秒“毒任务”时，push0 仍能持续提交提案，**提速 4.2×**。

#### ✅ **容错恢复表现优异**
| 故障类型 | 恢复场景 | MTTR |
|--------|----------|------|
| Dispatcher Crash | 存活 Dispatcher 存在 | **~0.5s** |
| Dispatcher Crash | 所有 Dispatcher 崩溃 | = ACK Timeout（默认 30s） |
| Collector Crash | 重启后 | **0.5s**，所有 barrier 完成 |

> - 利用心跳机制，长任务不会因超时被误重发。

---

## 4. **关键结论和发现**

### **主要发现**
1. **编排层是 ZK 系统可靠性的瓶颈**：生产中大多数故障来自编排系统而非 prover 本身。
2. **Dispatcher-Collector 架构有效解耦顺序与并行性**：通过优先级队列保证区块顺序，通过 Collector 实现块内并行。
3. **分区亲和路由对屏障同步至关重要**：避免 barrier fragmentation，是实现高可用聚合的前提。
4. **Prover-Agnostic 设计极大提升可维护性**：从 Halo2 迁移到 SP1 仅需配置变更和少量胶水代码。
5. **生态有效性已验证**：自 2025 年 3 月 4 日起在 Zircuit zkRollup 上运行，处理 **1400 万+ 主网区块**，零编排故障。

### **方法的局限性**
1. **消息总线为信任根**：当前假设消息总线可信，不支持拜占庭容错。
2. **未提供去中心化激励机制**：适用于中心化运营商或联盟链，尚未集成代币激励。
3. **保密性需应用层保障**：证明输入未加密，需外部机制保护隐私。
4. **静态拓扑配置**：队列拓扑和 Collector 策略需预先配置，动态变更需重启。

### **未来工作方向**
1. **去中心化编排层**：结合共识机制实现抗审查的任务分配与结果验证。
2. **激励兼容的 Prover 市场**：集成经济模型，支持开放参与的 proving 网络。
3. **自动化工作流优化**：基于历史数据动态调整 Collector 数量、超时参数等。
4. **跨链证明编排**：支持多链并行证明生成与聚合。

---

> **总结**：  
> push0 是首个面向生产级 ZK 系统的 **通用、可扩展、容错** 的编排框架。它不仅解决了 L1 zkEVM 和 ZK-Rollups 的实时性与可靠性挑战，还为未来去中心化 proving 网络提供了基础架构。其核心思想——**通过事件驱动 + 持久队列 + 分区亲和** 实现严格顺序与高度并行的统一——对分布式系统设计具有广泛借鉴意义。

</details>

---

### 9. [Adaptive Semi-Supervised Training of P300 ERP-BCI Speller System with Minimum Calibration Effort](https://arxiv.org/abs/2602.15955)

**Authors**: Shumeng Chen, Jane E. Huggins, Tianwen Ma  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.15955v1  

#### Abstract
A P300 ERP-based Brain-Computer Interface (BCI) speller is an assistive communication tool. It searches for the P300 event-related potential (ERP) elicited by target stimuli, distinguishing it from the neural responses to non-target stimuli embedded in electroencephalogram (EEG) signals. Conventiona...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Adaptive Semi-Supervised Training of P300 ERP-BCI Speller System with Minimum Calibration Effort*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统基于 P300 ERP 的 BCI 拼写系统依赖**大量标注数据进行校准（calibration）**，这一过程耗时长、易引起用户疲劳，限制了系统的实时性和实用性。尤其是在实际应用中，获取高质量标注 EEG 数据困难，严重影响系统部署效率。

### 🚀 提出的新方法与创新思路
本文提出了一种**自适应半监督学习框架（adaptive semi-supervised EM-GMM）**，其核心思想是：
- 利用少量标注数据初始化模型；
- 在后续使用过程中持续利用未标注的实时 EEG 输入数据，通过 Expectation-Maximization 算法结合 Gaussian Mixture Model（EM-GMM）动态更新分类器参数；
- 实现“边拼写、边训练”的在线自适应机制，显著减少初始校准负担。

该方法直接对 EEG 特征向量建模（而非仅作用于内积），并假设目标与非目标 ERP 共享协方差矩阵，提升了模型稳定性与泛化能力。

### ⚖️ 相比现有方法的优势
| 方面 | 传统方法（如 SVM, LDA, CNN） | 本文方法 |
|------|-------------------------------|--------|
| 数据需求 | 需要大量标注数据用于离线训练 | 仅需极小量标注数据即可启动，后续无需人工标注 |
| 校准时间 | 长（通常需拼写完整句子） | 极短（约10个字符即可开始自由输入） |
| 学习范式 | 完全监督学习 | 半监督 + 自适应在线学习 |
| 实用性 | 受限于用户耐力和注意力波动 | 更适合长期、实时交互场景 |

> 🔍 **关键优势总结**：在保证高拼写准确率的前提下，将校准努力最小化，提升 BCI 系统的整体可用性（utility）和用户体验。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **真实数据来源**：来自密歇根大学 Direct Brain Interface Lab（UM-DBI）的公开 EEG 数据集。
- **参与者数量**：共15名被试（participants），每人完成一个训练任务（TRN）和最多三个自由拼写测试任务（FRT）。
- **EEG 设置**：
  - 16通道 EEG 电极（F3, Fz, F4, C3, Cz, C4, P3, Pz, P4, Oz 等）；
  - 采样频率 256 Hz，经 notch filter（60Hz）、band-pass filter（0.5–6 Hz）处理，并下采样至 32 Hz；
  - 使用 xDAWN 进行空间滤波降维，提取前两个正交成分作为特征。

### 🧪 实验设置
- **刺激范式**：采用 Row-Column Paradigm（RCP），6×6 虚拟键盘，每轮闪烁12次（6行+6列）；
- **序列定义**：每个字符对应一个 super-sequence，包含15个 sequences（共180次闪烁）；
- **训练阶段**：要求被试拼写 “THE QUICK BROWN_FOX”（共19字符，含空格）；
- **测试阶段**：允许自由拼写，系统根据预测概率动态停止输出字符（dynamic stopping criterion，阈值 θ=0.9）；

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Character-level Accuracy (p)** | 测试集中正确识别字符的比例，要求 ≥0.7 才具实用价值 |
| **Information Transfer Rate (ITR)** | 衡量通信效率（单位：bits/min），综合考虑准确性与速度 |
| **BCI Utility** | 综合 ITR 和准确性的加权指标，当 p < 0.5 时为0，更贴近实际可用性 |

### 🔁 对比基线方法
- **Offline EM-GMM**：使用全部训练数据一次性训练模型，作为性能上限基准；
- **Adaptive EM-GMM**：本文提出的在线自适应方法，每2个 sequences 更新一次参数；
- 两者均基于相同的 GMM 结构和初始化策略，确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）仿真分析结果（Simulation Study）
- 在200次蒙特卡洛模拟中：
  - **Offline 方法**：使用全部285个训练 sequences，平均字符准确率达 **91%**；
  - **Adaptive 方法**：仅用 **75个 sequences**（约减少74%数据量），平均准确率仍超过 **80%**；
  - 表明所提方法在低数据条件下仍保持良好性能。

#### （2）真实数据分析结果（Real Data Analysis）
- 在15名被试中：
  - **9人**达到了最低可用标准（accuracy > 0.7）；
  - 其中 **7人**在 adaptive 设置下的表现优于 offline 基线；
- 平均性能（基于 ~150 sequences / 10 characters 初始化）：
  - Adaptive 方法在 **Accuracy、ITR、BCI Utility 三项指标上均高于 offline 基线**；
  - Interquartile range 更窄 → 性能更稳定。

#### （3）典型个体趋势（Figure 6 & 7）
- **K117、K183**：adaptive 曲线快速上升，在约150 sequences 后超越 offline 性能；
- **K171**：虽起始较低，但呈稳步增长趋势；
- 所有被试的 **ITR 与 BCI Utility 均随训练序列增加而上升**，体现自适应优势。

### 🔍 与基线方法对比总结
| 维度 | 对比结果 |
|------|---------|
| 准确性 | 多数用户（7/9）adaptive > offline；整体均值更高 |
| 效率 | ITR 和 BCI Utility 持续提升，后期优于固定模型 |
| 数据效率 | 仅需 ~10字符（150 seq）即可达到实用水平，相比 offline 的19字符大幅降低门槛 |

> ❗ 注：无明确消融实验（ablation study），但文中强调了参数初始化和共享协方差假设的重要性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **最小化校准可行**：只需少量标注数据（如10个字母）即可成功初始化模型，进入自适应模式；
2. **自适应机制有效**：利用未标注数据持续优化分类器，能够在真实使用中不断提升性能；
3. **整体效能提升**：不仅提高 accuracy，还显著改善 ITR 和 BCI Utility，尤其适用于资源受限或用户疲劳敏感的应用场景；
4. **优于传统离线训练**：多数用户最终表现超过一次性训练的基准模型，验证了“越用越好”的潜力。

### ⚠️ 方法的局限性
1. **对初始化敏感**：若初始标注数据质量差或代表性不足，可能导致模型收敛缓慢甚至失败；
2. **不适用于异步控制**：当前框架假设用户始终处于拼写状态，无法检测分心或终止意图；
3. **噪声鲁棒性待加强**：EEG 易受眼动、肌电等干扰，缺乏显式的去噪模块可能影响长期稳定性；
4. **未引入语言先验**：未融合上下文信息（如 n-gram 或 Transformer），错失进一步纠错机会。

### 🔮 未来工作方向
1. **改进初始化策略**：探索跨被试迁移学习（transfer learning）或元学习（meta-learning）以加速冷启动；
2. **增强噪声抑制**：集成先进去噪算法（如 ICA、深度去噪网络）提升信号质量；
3. **引入上下文建模**：结合语言模型进行 next-character prediction，实现 context-aware 拼写修正；
4. **优化刺激选择**：采用强化学习动态调整 RCP 闪烁顺序，提升信息增益；
5. **推动临床落地**：在 ALS 等患者群体中验证系统的长期可用性和可靠性。

---

> 💡 **总体评价**：  
本论文提出了一种极具实用价值的自适应半监督训练框架，成功解决了 P300 BCI 拼写系统中“高精度 vs. 低校准成本”的矛盾。其实验设计严谨，结果充分支持核心主张——**以最小校准代价实现高效、可持续进化的 BCI 交互体验**，为未来迈向“即插即用”型脑机接口提供了重要技术路径。

</details>

---

### 10. [Training-Free Adaptation of Diffusion Models via Doob's $h$-Transform](https://arxiv.org/abs/2602.16198)

**Authors**: Qijie Zhu, Zeqi Ye, Han Liu, Zhaoran Wang, Minshuo Chen  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2602.16198v1  

#### Abstract
Adaptation methods have been a workhorse for unlocking the transformative power of pre-trained diffusion models in diverse applications. Existing approaches often abstract adaptation objectives as a reward function and steer diffusion models to generate high-reward samples. However, these approaches...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Training-Free Adaptation of Diffusion Models via Doob's $h$-Transform

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
该论文旨在解决**预训练 diffusion model 在下游任务中的适应性问题**，尤其是在需要最大化非可微奖励函数（non-differentiable reward）的情况下。传统方法通常依赖于额外的网络训练（如 RL fine-tuning 或 guidance learning），这带来了高昂的计算成本，并且对奖励函数的可微性有严格要求。

此外，许多现有的推理时（inference-time）适应方法（如 SMC 或搜索类方法）存在样本坍缩（sample collapse）、推理效率低等问题。

### 提出了什么新方法或新思路
作者提出了 **DOIT (Doob-Oriented Inference-time Transformation)** ——一种无需训练、适用于通用（包括非可微）奖励函数的推理时适应算法。

其核心思想是将扩散模型的适应过程建模为一个**测度输运问题**（measure transport），即从原始生成分布 $P_\theta$ 转移到高奖励区域的条件分布 $P_\theta(\cdot \mid \mathcal{E}_{x_0})$。为此，作者引入了 **Doob's $h$-transform** 来实现这一转移。

- **Doob's $h$-transform** 通过在反向采样过程中添加一个动态修正项 $\nabla_x \log h(x,t)$ 来引导轨迹朝向目标事件 $\mathcal{E}_{x_0}$（例如 $r(x_0) \geq r_0$）。
- 该修正项不修改预训练模型参数，仅在推理阶段进行调整，从而实现了完全的 **training-free adaptation**。
- 为了处理 $h(x,t) = \mathbb{P}(\mathcal{E}_{x_0} \mid X_t = x)$ 难以解析计算的问题，提出了一种基于蒙特卡洛（MC）模拟的近似方法来估计 $\nabla \log h(x,t)$。

### 相比现有方法的优势
| 维度 | DOIT | 现有方法 |
|------|------|--------|
| 是否需要训练 | ❌ 否（training-free） | ✅ 多数需额外训练（如 RL fine-tuning） |
| 支持非可微奖励 | ✅ 是 | ❌ 多数依赖梯度（如 gradient-based guidance） |
| 推理效率 | ⏱️ 高效（仅增加少量 NFE） | 🐢 较低（如 SMC / tree search 增加大量采样） |
| 理论保障 | ✅ 提供高概率收敛保证 | ❌ 多数缺乏理论分析 |
| 易于集成 | ✅ 可与其他方法（如 BoK, BFS）结合 | — |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **文本到图像生成任务**：使用 **Stable Diffusion v1.5** 模型，在 LAION 数据集上训练，评估其在提升美学评分（aesthetic score）上的表现。
- **离线强化学习（Offline RL）任务**：采用 **D4RL benchmark**，包含以下环境与数据组合：
  - Environments: `HalfCheetah`, `Hopper`, `Walker2d`
  - Dataset types: `Medium-Expert`, `Medium`, `Medium-Replay`

### 实验设置和评估指标
#### 文本到图像任务
- **奖励函数**：LAION Aesthetic Score 和 ImageReward
- **采样器**：Euler ancestral sampler（$L=20$ 步）
- **评估方式**：生成 $K=32$ 张图像，报告美学分数的均值、最小值、最大值及分位数（violin plot）
- **消融实验**：测试不同温度 $T$ 与修正强度 $\gamma$ 的影响

#### 离线 RL 任务
- **模型**：使用 Lu et al. [2023] 提供的预训练 diffusion policy
- **奖励信号**：使用 ground-truth Q-function 作为 reward oracle
- **采样器**：DDIM ($L=15$ 步)
- **评估指标**：标准 return（回报），越高越好
- **对比基线**：
  - **Training-based**: IQL, Diffuser, D-QL, QGPO
  - **Inference-time**: TFG, DAS, TTS

### 基线方法对比
| 类型 | 方法 |
|------|------|
| Training-based | IQL, Diffuser, D-QL, QGPO |
| Inference-time | TFG, DAS, TTS, FK-Steering, TreeG, SVDD |
| 组合方法 | BoK, BFS |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（D4RL 平均得分）
| 方法 | 平均 Return |
|------|------------|
| **DOIT (Ours)** | **87.6** ✅ |
| TTS (SOTA inference-time) | 86.1 |
| QGPO (training-based) | 86.6 |
| DAS | 80.2 |
| TFG | 82.1 |

> DOIT 在 **9个任务中的8个** 上优于所有 inference-time 基线，平均得分最高。

### 与基线方法的对比结果
- 在 `Hopper-medium` 上，DOIT 达到 **101.0 ± 0.3**，显著超过 TTS 的 99.5 和 DAS 的 71.3。
- 在 `Walker2d-medium-expert` 上达到 **113.2 ± 0.7**，优于所有 baseline。
- 即使在 `Medium-Replay` 这类多样性高的数据集上也表现出色，说明方法鲁棒性强。

### 消融实验结果
#### （1）Surrogate vs Full Trajectory Simulation
| 方法 | Mean Score | Runtime (sec/image) |
|------|-----------|---------------------|
| Pre-trained | 6.372 | 1.260 |
| Surrogate (ours) | 6.726 | **1.712** |
| Full Simulation | 6.714 | 39.584 |

> 使用 **one-step lookahead surrogate**（公式 (10)）即可接近完整轨迹模拟的效果，同时速度提升 **20倍以上**。

#### （2）与 Resampling 方法结合
| 方法 (K=4) | ImageReward Score |
|----------|------------------|
| BoK | 0.702 |
| BoK + DOIT | **0.875** (+24.6%) |
| BFS | 0.882 |
| **BFS + DOIT** | **0.950** ✅ |

> DOIT 可作为“先验优化”模块，显著增强 BoK/BFS 等 resampling 方法的表现。

#### （3）超参数敏感性分析（T 和 γ）
- 发现存在 trade-off：低温 $T$ 需较小 $\gamma$ 以保持稳定；高温下可使用更大 $\gamma$ 加速优化。
- 最佳配置范围：$T \in [0.3, 0.4]$, $\gamma \in [4.0, 8.0]$（具体见附录 Tables 4–8）

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **Doob's $h$-transform 是实现 training-free adaptation 的有效工具**：它提供了一个理论严谨的概率框架，用于将生成分布导向高奖励区域。
2. ✅ **无需训练也能高效适配非可微奖励**：DOIT 成功绕过了对奖励梯度的需求，适用于黑箱 reward（如分子设计、机器人控制）。
3. ✅ **兼具高性能与高效率**：相比其他 inference-time 方法，DOIT 在几乎不牺牲采样效率的前提下实现了更优性能。
4. ✅ **可组合性强**：DOIT 可作为插件式模块，与 BoK、BFS 等搜索策略无缝集成，进一步提升最终输出质量。

### 方法的局限性
- **对高奖励区域稀疏敏感**：当目标事件 $\mathcal{E}_{x_0}$ 在原始分布中概率极低时（$P(\mathcal{E}_{x_0}) \to 0$），MC 估计方差增大，导致 $\nabla \log h$ 近似不稳定。
- **依赖预训练模型的质量**：若 $P_\theta$ 本身难以覆盖目标区域，则即使应用 DOIT 也无法生成高质量样本。
- **超参数调优需求**：$\gamma$, $T$, $l^*$ 等需根据任务调整，缺乏自适应机制。

### 未来工作方向
- 设计 **variance-reduced estimator** for $\nabla \log h$，以应对极端稀疏 reward 场景。
- 探索 **adaptive scheduling** of Doob correction（如自动调节 $\gamma(t)$）。
- 将 DOIT 扩展至 **flow models**, **autoregressive models** 等其他生成架构。
- 结合 **active exploration** 或 **reward shaping** 来缓解初始分布与目标之间的巨大 gap。

---

> 🔗 **代码开源地址**：https://github.com/liamyzq/Doob_training_free_adaptation

</details>

---

### 11. [Quality-constrained Entropy Maximization Policy Optimization for LLM Diversity](https://arxiv.org/abs/2602.15894)

**Authors**: Haihui Pan, Yuzhong Hong, Shaoke Lv, Junwei Bao, Hongfei Jiang, Yang Song  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.15894v1  

#### Abstract
Recent research indicates that while alignment methods significantly improve the quality of large language model(LLM) outputs, they simultaneously reduce the diversity of the models' output. Although some methods have been proposed to enhance LLM output diversity, they often come at the cost of redu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Quality-constrained Entropy Maximization Policy Optimization for LLM Diversity*

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前主流的 **LLM 对齐方法**（如 SFT、RLHF、DPO）虽然显著提升了输出的 **质量（quality）**、有用性和安全性，但普遍观察到其严重抑制了模型输出的 **多样性（diversity）**。这种多样性下降表现为输出趋于重复、模式化、缺乏创造性，尤其在需要开放性生成的任务（如创意写作）中表现不佳。

已有方法在提升多样性时往往以牺牲质量为代价，导致生成内容变得无意义或不连贯。因此，如何在 **保证输出质量的前提下提升多样性**，成为一个亟待解决的问题。

### 提出了什么新方法或新思路
本文提出了一种新的对齐优化框架：**Quality-constrained Entropy Maximization Policy Optimization (QEMPO)**，其核心思想是：

- 将对齐任务分解为两个目标：**最大化质量** 和 **最大化多样性**。
- 明确将 **policy entropy** 作为多样性的代理指标进行最大化。
- 同时引入 **quality constraint** 来确保输出满足人类偏好，防止熵增导致质量崩溃。
- 在此基础上进一步提出了变体 **QEMPO-KL**，额外加入 KL 散度约束以稳定训练。

该方法从理论层面重新审视了 RLHF 的本质，并指出其等价于一个在质量约束下的 KL 最小化问题，而 **并未显式鼓励多样性**。

### 相比现有方法的优势
| 方法 | 是否显式优化多样性 | 是否保持高质量 | 是否依赖 reference model |
|------|------------------|---------------|------------------------|
| **RLHF / DPO** | ❌ | ✅ | ✅ |
| **SPL (Soft Preference Learning)** | ✅ | ⚠️（有时低于 RLHF） | ✅ |
| **QEMPO** | ✅ | ✅（优于或等于 RLHF） | ❌（可省略） |
| **QEMPO-KL** | ✅ | ✅ | ✅ |

- **理论优势**：首次形式化证明了对齐任务应同时优化质量和多样性；并证明 QEMPO/QEMPO-KL 的最优策略具有比 RLHF 更高的 entropy。
- **实践优势**：在多个模型和任务上验证了 QEMPO 能 **显著提升多样性**（lexical, syntactic, semantic），同时 **维持甚至略微提升质量**。
- **灵活性高**：支持 offline 和 online 两种训练模式，且 QEMPO 可完全去除 reference model，降低内存开销。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
- **Offline 实验**：
  - **UltraFeedback Binarized Dataset**：基于人类反馈标注“chosen”和“rejected”的响应对，用于 preference learning 类方法训练。
- **Online 实验**：
  - **GSM8K** 和 **MATH500**：数学推理数据集，共约 12,000 个样本，用于测试模型在复杂任务上的多路径求解能力。

### 实验设置和评估指标

#### 模型规模
- 小模型：`Qwen2.5-1.5B-Instruct`, `Llama-3.2-1B-Instruct`
- 大模型：`Qwen2.5-7B-Instruct`, `Llama-3.1-8B-Instruct`

#### 评估指标
| 维度 | 指标 | 说明 |
|------|------|------|
| **多样性（Diversity）** | Lexical / Syntactic / Semantic Diversity | 使用 Guo et al. (2024) 提出的框架，从词法、句法、语义三个层面量化输出差异性 |
| **质量（Quality）** | MT-Bench Score（由 gpt-4o 打分） | 衡量回答的准确性、逻辑性和有用性 |
| **综合性能（Online）** | **pass@k**（k=1, 4, 8, 16, 32） | 衡量在多次采样下至少有一次正确解答的概率，兼顾质量与多样性 |

#### 基线方法对比
- **RLHF**：标准强化学习对齐方法
- **SPL (Soft Preference Learning)**：近期提出的多样性增强方法
- **Base Model**：未对齐的基础模型

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| 方法 | 平均多样性 | MT-Bench 质量 |
|------|------------|----------------|
| **QEMPO (平均)** | **42.83** ↑↑ | **7.05** ≈ |
| **QEMPO-KL (平均)** | **42.42** ↑ | **7.04** ≈ |
| **SPL (平均)** | 40.43 → | **6.93** ↓ |
| **RLHF (平均)** | 39.28 | **7.02** |

> 注：数值越高越好；↑ 表示优于 RLHF，↓ 表示劣于 RLHF

#### 典型结果亮点：
- 在 `Llama-3.2-1B-Instruct` 上，QEMPO-KL 的 **lexical 多样性达 48.36**，远超 RLHF 的 43.85。
- 在 `Qwen2.5-1.5B-Instruct` 上，QEMPO 的 **MT-Bench 得分为 6.54**，高于 RLHF 的 6.30，实现“双升”。
- 在 **online 数学任务** 中（Figure 3），随着 `k` 增大（即允许更多尝试），QEMPO 和 QEMPO-KL 的 **pass@k 显著超越 RLHF**，表明其生成更多有效解路径。

### 与基线方法的对比结果
- ✅ **QEMPO 在所有实验中均优于或持平 RLHF 的质量，且显著提升多样性**。
- ⚠️ **SPL 虽能提升多样性，但在部分模型（如 Llama-3.1-8B）上质量明显下降，甚至不如 base model**，显示其稳定性差。
- ✅ **QEMPO-KL 性能接近 RLHF，在 Qwen 系列上有增益，在 Llama 上略有波动**，但仍优于 SPL。

### 消融实验结果（Ablation Study）
- 使用 `Qwen2.5-1.5B-Instruct` 测试不同超参数（λ）的影响（Table 3）：
  - 不同 λ 设置下，**多样性与质量变化平缓**，说明方法鲁棒性强。
  - 即使调整参数，**所有配置的平均多样性仍高于 RLHF 的 40.36**。
  - 所有 QEMPO 配置的质量得分 **不低于 RLHF 的 6.30**，体现质量约束的有效性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. ✅ **多样性与质量是对齐任务中两个不可偏废的核心维度**，仅优化其一会导致次优策略。
2. ✅ **Policy Gradient 类方法本质上只优化质量**，无法保障多样性（Corollary 3.1）。
3. ✅ **QEMPO 和 QEMPO-KL 的理论最优策略具有更高的 entropy**，从而支持更丰富的输出分布。
4. ✅ 实验验证：**QEMPO 能在不牺牲质量的前提下显著提升 LLM 输出多样性**，尤其适用于需要创造性或多路径探索的任务。
5. ✅ **online 实验表明，多样性提升有助于解决困难问题**（pass@k 随 k 增长更快），说明多样性本身可转化为性能增益。

### 方法的局限性
- 当前方法依赖 reward model 或人工设计的 reward signal，若 reward 不准确可能导致错误的方向优化。
- 在 extremely low-quality outputs 上直接增加 entropy 会导致训练不稳定，需通过 **conditional variance masking** 技巧缓解（仅在正确输出上计算 var(log π)）。
- 超参数（如 λ）的选择虽整体稳健，但仍需一定调参经验。

### 未来工作方向
- 探索 **自适应 entropy 控制机制**，根据任务难度动态调节多样性强度。
- 结合 **intrinsic motivation**（如 curiosity）进一步激发模型探索能力。
- 将 QEMPO 框架扩展至 **多模态生成模型** 的对齐任务中。
- 研究如何在 **zero-reference setting** 下更高效地训练 QEMPO，减少对外部 reward model 的依赖。

---

> 🔍 **一句话总结**：  
> 本论文提出 **QEMPO** ——一种在严格质量约束下最大化策略熵的新对齐范式，实现了 **高质量与高多样性的统一**，为构建更具创造力和泛化能力的 LLM 提供了新的理论视角与实用工具。

</details>

---

### 12. [Muon with Spectral Guidance: Efficient Optimization for Scientific Machine Learning](https://arxiv.org/abs/2602.16167)

**Authors**: Binghang Lu, Jiahao Zhang, Guang Lin  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.16167v1  

#### Abstract
Physics-informed neural networks and neural operators often suffer from severe optimization difficulties caused by ill-conditioned gradients, multi-scale spectral behavior, and stiffness induced by physical constraints. Recently, the Muon optimizer has shown promise by performing orthogonalized upda...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Muon with Spectral Guidance: Efficient Optimization for Scientific Machine Learning*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在**Scientific Machine Learning**（如 **Physics-Informed Neural Networks**（PINNs）和 **Neural Operators**）中，优化过程面临严重挑战，主要包括：
- **Ill-conditioned gradients**（梯度病态）
- **Multi-scale spectral behavior**（多尺度谱行为）
- **Stiffness**（刚性）由物理约束引入
- 传统优化器（如 Adam、AdamW）在这些任务中收敛慢、不稳定，且对超参数敏感。

尽管 **Muon** 优化器通过在梯度的奇异向量基上进行正交化更新，改善了几何条件，但其“单位奇异值”更新可能导致步长过于激进，缺乏能量稳定性保证。

### 提出的新方法：SpecMuon
本文提出 **SpecMuon**，一种结合了 **Muon 的几何正交化** 和 **Relaxed Scalar Auxiliary Variable**（RSAV）机制的新型优化器。其核心思想是：
- 将矩阵梯度分解为**主导奇异模式**（singular modes）
- 在每个模式上独立应用 **RSAV 更新**
- 通过辅助变量动态调节步长，实现对刚性谱分量的选择性阻尼

### 创新点
- **统一几何与能量控制**：首次将 Muon 的正交化几何与 SAV 的能量稳定性机制融合，形成“多模态梯度流”视角。
- **模式自适应调节**：通过 per-mode RSAV 机制，使不同频谱方向的更新步长能根据全局损失能量自适应调整。
- **保留 Muon 的尺度平衡特性**：在稳定能量的同时，不牺牲 Muon 对多尺度参数的均衡处理能力。

### 相比现有方法的优势
| 方法 | 优势 |
|------|------|
| **vs Adam / AdamW** | 更快收敛、更低训练损失、更强稳定性，尤其在刚性和多尺度问题中 |
| **vs Muon** | 避免过激进的更新，提供明确的能量耗散律和稳定性保证，收敛更平滑 |

---

## 2. 核心实验方法和设置

### 使用的数据集与模型任务
实验覆盖多个典型科学机器学习任务，构成一个从简单到复杂的层次结构：

1. **Linear Regression**（线性回归）  
   - 用于验证在凸问题中的谱效应
   - 数据：合成数据 $ \mathbf{W}\mathbf{X} - \mathbf{Y} $

2. **PINN for 1D Burgers’ Equation**（粘性伯格斯方程）  
   - 非线性 PDE，经典 PINN 测试基准
   - 物理方程：$ u_t + uu_x = \nu u_{xx} $
   - 初始条件：$ u(x,0) = -\sin(\pi x) $

3. **DeepONet for Burgers’ Equation**  
   - 算子学习框架，学习从初始条件到解的映射
   - 架构包含 branch 和 trunk 网络

4. **fPINN-DeepONet for 2D Heat Equation**（分数阶热传导方程）  
   - 更复杂的分数阶 PDE 问题
   - 分析解已知，便于定量评估误差

### 实验设置
- **硬件平台**：NVIDIA H100 80GB GPU
- **训练轮次**：10,000 epochs（Burgers）、2,000 epochs（Heat Equation）
- **超参数调优**：对所有优化器进行网格搜索（grid search），选择最优 learning rate、momentum 等
- **SpecMuon 特有参数**：
  - `rtop`：保留的主导奇异方向数（2–10）
  - `η_sav`：SAV 平滑因子（默认 0.2）

### 评估指标
| 指标 | 描述 |
|------|------|
| **Final Training Loss** | 最终训练损失（log scale） |
| **Mean Squared Error (MSE)** | 预测解与真解之间的均方误差 |
| **Relative L2 Error** | 相对 L2 范数误差 |
| **Training Time** | 总训练时间（秒） |
| **Solution Smoothness & Accuracy** | 可视化解的空间分布与误差图 |

### 基线方法对比
- **Adam**
- **AdamW**
- **Muon**（原始版本）

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 表 1：Burgers’ PINN 实验结果（Table 2）
| Optimizer | Final Training Loss | MSE | Training Time (s) |
|----------|---------------------|------|------------------|
| Adam     | $5.88 \times 10^{-4}$ | $1.42 \times 10^{-3}$ | 120 ± 12 |
| AdamW    | $7.46 \times 10^{-4}$ | $1.50 \times 10^{-3}$ | 117 ± 11 |
| Muon     | $3.39 \times 10^{-4}$ | $1.25 \times 10^{-3}$ | 149 ± 15 |
| **SpecMuon** | $\mathbf{1.55 \times 10^{-4}}$ | $\mathbf{5.06 \times 10^{-4}}$ | 180 ± 18 |

> ✅ **SpecMuon 达到最低训练损失和最小 MSE**

#### 表 2：DeepONet 实验结果（Table 4）
| Optimizer | Final Training Loss | MSE |
|----------|---------------------|------|
| Adam     | $1.89 \times 10^{-3}$ | 0.0014 |
| AdamW    | $4.71 \times 10^{-5}$ | 0.0013 |
| Muon     | $6.38 \times 10^{-4}$ | 0.0011 |
| **SpecMuon** | $\mathbf{4.82 \times 10^{-5}}$ | $\mathbf{0.0010}$ |

> ✅ **SpecMuon 在算子学习任务中仍保持领先**

#### 表 3：fPINN-DeepONet 实验结果（Table 6）
| Optimizer | Training Loss @500ep | MSE |
|----------|------------------------|------|
| Adam     | $3.72 \times 10^{-2}$ | 0.0268 |
| AdamW    | $3.64 \times 10^{-2}$ | 0.0275 |
| Muon     | $3.52 \times 10^{-2}$ | 0.0272 |
| **SpecMuon** | $\mathbf{2.31 \times 10^{-2}}$ | $\mathbf{0.0262}$ |

> ✅ **即使在分数阶 PDE 中，SpecMuon 依然显著优于基线**

### 与基线方法的对比结果
- **收敛速度**：SpecMuon 在所有任务中均表现出更快的初始下降速度和更稳定的后期收敛。
- **稳定性**：避免了 Adam 的震荡和 Muon 的过冲现象。
- **精度提升**：MSE 普遍降低 **30%~60%**，尤其是在 DeepONet 和 fPINN 设置下。
- **计算开销**：由于 SVD 和 RSAV 开销，SpecMuon 训练时间略高（约增加 20–50%），但精度收益远超成本。

### 消融实验结果（Ablation Study）
- **rtop 参数影响**（图 2b, 5b, 6b）：
  - 当 `rtop = 6~8` 时性能最佳
  - 过小（如 2）无法捕捉足够谱结构；过大则引入噪声并增加计算负担
- **SAV 机制作用**：
  - 移除 RSAV 后退化为普通 Muon，出现不稳定更新
  - 保留 RSAV 显著增强鲁棒性和收敛一致性

---

## 4. 关键结论和发现

### 主要发现
1. **谱结构在科学机器学习优化中至关重要**：即使是线性回归这样的凸问题，谱不平衡也会影响收敛效率。
2. **几何正交化 + 能量控制 = 更优优化路径**：SpecMuon 成功将 Muon 的几何优势与 SAV 的能量稳定性结合，实现了“既快又稳”的优化。
3. **多模态梯度流视角有效**：将优化视为各奇异模式上的独立演化，允许对刚性高频分量进行选择性阻尼，同时保留低频快速更新。
4. **理论保障强**：作者建立了严格的理论性质，包括：
   - 修改后的能量耗散律（modified energy dissipation law）
   - 辅助变量的正定性与有界性
   - 在 **Polyak-Łojasiewicz 条件** 下的线性收敛速率

### 方法的局限性
- **计算复杂度较高**：依赖 SVD 分解，在大规模参数矩阵上可能成为瓶颈（尽管采用截断策略缓解）
- **目前仅适用于全批量或大批次设置**：尚未扩展至 mini-batch 或随机设定
- **需手动设置 `rtop`**：虽然可通过消融确定，但缺乏自动选择机制

### 未来工作方向
1. **扩展至随机优化场景**：研究 SpecMuon 在 mini-batch SGD 中的表现，并结合方差缩减技术。
2. **自适应谱截断策略**：设计自动选择 `rtop` 的机制，根据梯度谱动态调整保留模式数量。
3. **更广泛的科学问题应用**：应用于多物理场耦合系统、反问题、不确定性量化等更具挑战性的场景。
4. **理论深化**：探索更弱假设下的收敛率，分析谱演化跨迭代的动力学行为。

---

> **总结一句话**：  
> **SpecMuon 通过将 Muon 的奇异向量正交化与 RSAV 的能量调控机制相结合，提出了一种面向科学机器学习的新型谱感知优化器，在多种 PINN 与 Neural Operator 任务中实现了更快、更稳、更准的训练效果，兼具坚实的理论基础与显著的实证优势。**

</details>

---

### 13. [Fast KV Compaction via Attention Matching](https://arxiv.org/abs/2602.16284)

**Authors**: Adam Zweiger, Xinghong Fu, Han Guo, Yoon Kim  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2602.16284v1  

#### Abstract
Scaling language models to long contexts is often bottlenecked by the size of the key-value (KV) cache. In deployed settings, long contexts are typically managed through compaction in token space via summarization. However, summarization can be highly lossy, substantially harming downstream performa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Fast KV Compaction via Attention Matching**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
现代大语言模型（LLMs）在处理长上下文时面临 **Key-Value (KV) 缓存** 内存瓶颈。随着上下文长度增加，KV cache 可能占用数 GB 内存，严重影响推理效率。传统方法如 **token summarization** 或 **token eviction** 虽然快速，但在高压缩比下损失严重，导致下游任务性能大幅下降。

现有方法如 **Cartridges (Eyuboglu et al., 2025)** 能在潜在空间中训练高度紧凑的 KV cache，实现高质量压缩（如 50×），但依赖端到端梯度优化，耗时长达数小时，难以实用。

本文旨在解决 **“如何在保证高质量的前提下，实现极快的 KV cache 压缩”** 这一关键挑战。

---

### **提出了什么新方法或新思路**
提出 **Attention Matching (AM)** 框架，一种基于注意力行为匹配的 **非梯度、闭式解（closed-form）** 的 KV 压缩方法。

#### **核心思想**
不通过端到端优化输出分布，而是直接优化压缩后的 `(Ck, Cv)`，使其满足两个关键条件：
1. **Attention Output Matching**：压缩块对参考查询 `q` 的注意力输出应与原始 KV 块一致。
2. **Attention Mass Matching**：压缩块的注意力质量（mass）应与原始块匹配，以确保在拼接未来 token 时行为一致。

为此引入 **per-token scalar bias β**，用于调整每个保留 key 的注意力权重，从而补偿因数量减少导致的质量损失。

#### **方法流程**
1. **生成参考查询 `Qref`**：通过 `repeat-prefill` 或 `self-study` 生成模型可能产生的查询向量。
2. **选择压缩 keys `Ck`**：采用启发式（如最高注意力 key）或 OMP（正交匹配追踪）选择子集。
3. **拟合 bias `β` 和 values `Cv`**：
   - `β` 通过 **非负最小二乘 (NNLS)** 拟合，使 attention mass 匹配。
   - `Cv` 通过 **普通最小二乘 (OLS)** 拟合，使 attention output 匹配。

该框架将复杂优化分解为多个可高效求解的子问题，避免了耗时的梯度下降。

---

### **相比现有方法的优势**
| 特性 | Cartridges | Token Pruning (H2O, KVzip) | **Attention Matching (本文)** |
|------|----------|----------------------------|------------------------------|
| **压缩质量** | 高 | 低（尤其高比例时） | **接近 Cartridges** |
| **压缩速度** | 极慢（小时级） | 快（秒级） | **极快（秒级），比 Cartridges 快 100×** |
| **是否需训练** | 是（per-context 优化） | 否 | 否（post-hoc） |
| **是否支持 chunking** | 是（跨 chunk 优化） | 是 | 是（独立 chunk 处理） |
| **是否引入 bias** | 否 | 否 | **是（关键设计）** |

> ✅ **核心优势**：在 **50× 压缩比** 下，**质量接近 Cartridges，速度提升 2 个数量级**，首次实现了高质量与高速度的帕累托前沿突破。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **QuALITY**：长文本理解基准，每篇 5–8k tokens，含 15–20 个多项选择题，共 50 篇文章（894 问）。
- **LongHealth**：临床病历问答任务，每上下文约 60k tokens（5 名患者记录），信息密集，更具挑战性。

---

### **实验设置和评估指标**
- **模型**：Qwen3-4B, Llama3.1-8B, Gemma3-12B。
- **压缩比范围**：10× 到 100×。
- **评估方式**：
  - 一次性压缩上下文，然后用压缩 cache 回答所有问题。
  - 报告 **下游 QA 准确率** 和 **log-perplexity**（衡量 token-level 行为保真度）。
- **硬件**：单张 H100/H200 GPU，报告 **wall-clock compaction time**。

---

### **基线方法对比**
| 基线 | 类型 | 说明 |
|------|------|------|
| **Cartridges** | 梯度优化 | 端到端训练 latent cache，高质量但极慢 |
| **H2O+, SnapKV, PyramidKV, KVzip** | Token Pruning | 基于 attention score 选择 token，速度快但质量差 |
| **Summarization** | Token Space | 文本摘要，严重丢失细节 |
| **No Context** | 控制组 | 完全无上下文 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- 在 **Qwen3-4B + QuALITY** 上，**50× 压缩**：
  - **AM-OMP**：准确率 **~0.65**，压缩时间 **~10 分钟**。
  - **AM-HighestAttnKeys-fast**：准确率 **~0.60**，压缩时间 **< 1 分钟**。
  - **Cartridges**：准确率 **~0.62**，压缩时间 **~5 小时**。
  - **Summarization**：准确率 **~0.45**。
- **速度提升**：AM 方法比 Cartridges **快 50–100×**，同时质量更高。

> 📈 图 1 显示 AM 方法在 **准确率 vs. 压缩时间** 曲线上形成新的帕累托前沿。

---

### **与基线方法的对比结果**
- **优于所有 token pruning 方法**（H2O, SnapKV, KVzip）在高压缩比下。
- **在 50× 时质量超过 Cartridges**，在 100× 时略逊（因梯度方法搜索空间更大）。
- **KVzip** 在某些设置下表现接近 AM，归因于其非均匀预算分配。
- **Summarization** 在 LongHealth 上几乎退化到 “No Context” 水平。

---

### **消融实验结果**
- **移除 nonuniform head budgets**：性能显著下降 → 证明不同 attention head 对压缩敏感度不同。
- **移除 bias β**：性能下降，尤其在高比例压缩时 → 证明 bias 对 mass matching 至关重要。
- **移除 self-study 查询**：性能轻微下降 → `repeat-prefill` 已足够有效。
- **使用 on-policy queries**：带来稳定小幅提升。
- **OMP vs. Highest Attention Keys**：OMP 更优但更慢，fast variant 平衡良好。

> 🔍 消融验证了 **bias、value fitting、nonuniform budget、query quality** 的必要性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Attention Matching 是有效的压缩范式**：通过匹配 attention output 和 mass，可在不依赖梯度的情况下实现高质量压缩。
2. **引入 scalar bias β 是关键**：解决了压缩后 attention mass 不足的问题，使行为在拼接时保持一致。
3. **非均匀压缩（nonuniform compaction）更优**：不同 attention head 对容量敏感度差异大，预计算 head budget 可显著提升效率。
4. **chunked compaction 支持超长上下文**：可独立处理各 chunk 并合并，适用于 60k+ token 场景。
5. **AM 可与 summarization 结合**：先摘要再 AM 压缩，实现 **200× 总压缩比**，性能仍接近纯摘要。

---

### **方法的局限性**
1. **OMP + self-study 变体仍需数分钟**：虽比 Cartridges 快，但仍不够实时。
2. **在极端压缩比（如 100×）下，Cartridges 仍略优**：因其可搜索更广的表示空间（不限于原 key 子集）。
3. **未探索量化或稀疏化**：当前使用 FP32/BF16，进一步压缩潜力未开发。
4. **online compaction 初步验证有效，但策略简单**：如始终压缩整个上下文，未冻结历史部分。

---

### **未来工作方向**
1. **加速 query generation 和 key selection**：如使用轻量模型生成查询。
2. **超越子集选择**：直接优化 compact keys（非从原 key 中选），扩大搜索空间。
3. **集成到 inference engine**：如 RadixAttention、varlen KV packing、disaggregated compaction。
4. **结合 token-space 与 latent-space 方法**：如 retrieval + summarization + AM，构建混合记忆系统。
5. **支持在线压缩（online compaction）**：在长程推理中动态压缩，支持无限长度生成。
6. **模型架构层面支持 compaction**：训练时显式设计可压缩的 KV 结构。

---

> ✅ **总结**：本文提出的 **Attention Matching** 框架，通过 **行为匹配 + 闭式求解**，在 **速度与质量之间取得了前所未有的平衡**，为长上下文 LLM 推理提供了实用且高效的 KV 压缩方案。

</details>

---

### 14. [Towards Efficient Constraint Handling in Neural Solvers for Routing Problems](https://arxiv.org/abs/2602.16012)

**Authors**: Jieyi Bi, Zhiguang Cao, Jianan Zhou, Wen Song, Yaoxin Wu, Jie Zhang, Yining Ma, Cathy Wu  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.16012v1  

#### Abstract
Neural solvers have achieved impressive progress in addressing simple routing problems, particularly excelling in computational efficiency. However, their advantages under complex constraints remain nascent, for which current constraint-handling schemes via feasibility masking or implicit feasibilit...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
- 当前神经求解器（Neural Solvers）在处理**简单路径规划问题**（如 CVRP）时表现出色，但在面对**复杂约束**（如时间窗、回程、容量等多重硬约束）时表现不佳。
- 现有约束处理机制存在两大瓶颈：
  - **可行性掩码（Feasibility Masking）**：在复杂问题中计算掩码本身是 NP-hard（例如 TSPTW），导致不可行或效率低下。
  - **隐式可行性感知（Implicit Feasibility Awareness）**：通过奖励塑形或特征增强引导策略，但难以保证严格可行性，且仍可能导致高不可行率。

### **提出了什么新方法或新思路**
提出 **Construct-and-Refine (CaR)** 框架，首个基于**显式学习型可行性精炼**（explicit learning-based feasibility refinement）的通用高效约束处理框架。

#### **核心创新点**：
1. **联合训练构造与精炼模块**：
   - 构造模块生成多样且高质量的初始解。
   - 精炼模块在极少数步骤内（如 10 步 vs. 先前工作的 5k 步）进行轻量级改进。
   - 两者通过端到端联合训练协同优化，构造模块被引导生成“易于精炼”的解。

2. **显式可行性精炼取代严格掩码**：
   - 放弃在构造阶段强制执行严格掩码，允许生成部分不可行解。
   - 由专门的精炼模块在后续步骤中主动修正不可行性，实现更灵活高效的探索。

3. **跨范式表示共享（Cross-Paradigm Representation Learning）**：
   - 首次在构造与精炼模块间共享编码器（Shared Encoder）。
   - 实现知识迁移，提升模型在复杂约束下的泛化能力与性能。

4. **定制化损失函数设计**：
   - 引入**多样性损失**（diversity loss）鼓励策略探索。
   - 设计**监督信号损失**（supervised loss），用精炼后的优质解反向指导构造策略更新。

### **相比现有方法的优势**
- **更广适用性**：首次能有效处理掩码不可计算（如 TSPTW）和掩码过度限制（如 CVRPBLTW）的复杂 VRP。
- **更高效率**：精炼仅需 5–20 步，推理时间从小时级降至秒级（如 8× 速度提升）。
- **更优性能**：在可行性、最优性差距（Gap）、解质量上全面超越经典求解器（LKH-3, OR-Tools）和前沿神经求解器（POMO, NeuOpt-GIRE）。
- **更强鲁棒性**：对多约束耦合场景更具适应性，避免因严格掩码导致搜索空间坍塌。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **TSPTW**（Traveling Salesman Problem with Time Windows）：
  - 包含不同难度等级（Medium, Hard），其中 Hard 版本掩码计算为 NP-hard。
- **CVRPBLTW**（Capacitated VRP with Backhaul, Duration, and Time Window）：
  - 多重硬约束叠加，掩码虽可计算但过于严格，严重限制搜索空间。
- **其他验证任务**：
  - SOP（Sequential Ordering Problem）
  - TSPDL（TSP with Draft Limit）
  - CVRP 及真实世界基准 **CVRPLIB**

所有实例均在线生成（on-the-fly），测试集来自公开基准以确保公平比较。

### **实验设置和评估指标**

#### **实验设置**
- **模型规模**：问题规模 `n=50` 和 `n=100`。
- **训练配置**：
  - 训练 5000 轮，每轮 20,000 实例，batch size=128。
  - 使用单张 NVIDIA RTX 4090 GPU。
  - 精炼步数 `TR=5` 用于训练，推理时测试 `TR=5,10,20`。
- **推理解码**：
  - 使用 8× 数据增强（augmentation）生成初始解。
  - 对于 CVRPBLTW，最终添加一次带掩码的重构以确保完全可行。

#### **评估指标**
| 指标 | 含义 |
|------|------|
| **Obj. ↓** | 平均可行解的目标值（越小越好） |
| **Gap ↓** | 相对于最强经典求解器（LKH-3 或 OR-Tools）的平均最优性差距 |
| **Infsb% ↓** | 不可行解比例（越低越好） |
| **Time** | 求解 10,000（TSPTW）或 1,000（CVRPBLTW）个实例的总推理时间 |

### **基线方法对比**
#### **经典求解器**
- **LKH-3**：强启发式求解器，支持多次 trial。
- **OR-Tools**：Google 开源工具包，使用引导局部搜索。
- **Greedy Heuristics**：最近邻或最早截止时间优先。

#### **神经求解器**
- **构造类**：POMO, PIP, UDC, MVMoE
- **改进类**：NeuOpt-GIRE
- **混合类**：NCS, LCP
- **后搜索增强**：POMO+EAS+SGBS

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **TSPTW-100 结果（Hard）**
| 方法 | Obj. | Gap ↓ | Infsb% ↓ | Time |
|------|------|--------|-----------|-------|
| LKH-3 (100 trials) | 46.625 | 0.103% | 31.05% | 27m |
| NeuOpt-GIRE* (5k steps) | 46.913 | 0.123% | 0.02% | 30m |
| **CaR-PIP (TR=20)** | **46.923** | **0.146%** | **0.02%** | **2.4m** |

> ✅ **CaR 在 1/12 时间内达到与 NeuOpt 相当甚至更优的性能，且首次实现 <0.2% 不可行率。**

#### **CVRPBLTW-100 结果**
| 方法 | Obj. | Gap ↓ | Infsb% ↓ | Time |
|------|------|--------|-----------|-------|
| POMO* | 27.049 | 7.004% | 0.00% | 4s |
| NeuOpt-GIRE* (5k steps) | 24.038 | -1.541% | 39.10% | 12.7m |
| **CaR(R&R, TR=20)** | **24.400** | **-2.448%** | **0.00%** | **19s** |

> ✅ **CaR 显著优于所有基线，在保持 0% 不可行的同时，目标值远超 POMO，并击败耗时更长的 NeuOpt。**

#### **效率对比（TSPTW-100）**
- **CaR 较 NeuOpt-GIRE 实现约 8× 速度提升**（见 Figure 3），在 10 分钟预算内取得更优 Pareto 前沿。

---

### **消融实验结果**

#### **(1) 联合训练的重要性（Table 5）**
| 构造模块 | 精炼模块 | Gap ↓ | Infsb% ↓ |
|---------|----------|--------|-----------|
| Random | LKH-3 | 0.011% | 60.66% |
| Pretrained PIP | Pretrained NeuOpt* | 0.172% | 2.59% |
| **Trainable PIP + Trainable NeuOpt* (CaR)** | **0.005%** | **0.00%** |

> 🔍 单纯组合预训练模型效果有限，**联合训练带来显著增益**，证明模块间协同优化的关键作用。

#### **(2) 多样性损失 $L_{div}$ 的影响（Table 6）**
| 问题 | 是否使用 $L_{div}$ | Gap ↓ | Infsb% ↓ |
|------|---------------------|--------|-----------|
| TSPTW | 否 | 0.421% | 0.58% |
| TSPTW | 是 | **0.014%** | **0.01%** |

> ✅ 添加多样性损失显著提升性能，尤其在复杂约束下。

#### **(3) 监督信号损失 $L_{SL}$ 的影响（Table 7）**
- 对弱构造骨干（POMO*）：Gap 从 0.136% → 0.014%，Infsb% 从 0.29% → 0.01%
- 对强骨干（PIP）：仍有边际提升（0.006% → 0.005%）

> 🔍 表明监督信号对提升构造-精炼协同至关重要，尤其当构造模块较弱时。

#### **(4) 共享表示的影响（Table 8）**
| 是否共享编码器 | #Params | TSPTW-50 Gap ↓ | Infsb% ↓ |
|----------------|--------|----------------|------------|
| 否 | 2.8M | 0.199% | 0.68% |
| **是** | **1.6M** | **0.014%** | **0.01%** |

> ✅ 共享编码器不仅提升性能，还减少参数量（1.7×），增强知识迁移。

---

## **4. 关键结论和发现**

### **主要发现**
1. **严格掩码在复杂 VRP 中有害无益**：
   - 掩码不可计算（TSPTW）或过度限制（CVRPBLTW）会阻碍 RL 收敛。
   - 放松掩码 + 显式精炼是更优路径。

2. **Construct-and-Refine 是高效约束处理的新范式**：
   - 构造提供高质量候选，精炼快速修复不可行性。
   - 联合训练使两模块形成正向循环。

3. **共享表示极大促进跨范式学习**：
   - 统一编码器实现构造与精炼间的知识共享，尤其在复杂场景下优势明显。

4. **CaR 是首个真正适用于多种复杂约束的神经求解器**：
   - 在 TSPTW、CVRPBLTW、SOP 等任务上均取得 SOTA 性能。
   - 效率远超传统改进方法（如 NeuOpt）。

### **方法的局限性**
- **依赖初始构造质量**：若构造模块完全失效，精炼难以挽救。
- **当前仅限单任务**：未探索多任务统一建模。
- **理论分析不足**：缺乏对收敛性、泛化界的严格证明（作者指出为未来方向）。

### **未来工作方向**
1. 集成更多 backbone 求解器（如 AM, PolyNet）。
2. 扩展至更广泛的 COPs（如调度问题）。
3. 提升可扩展性以处理更大规模实例（如 n > 1000）。
4. 理论研究：学习性（learnability）、收敛性、泛化界。
5. 构建基于 CaR 的 **NCO Foundation Model**，利用跨范式表示学习实现通用路由求解。

> 📌 **代码、预训练模型与数据集已开源**：[https://github.com/jieyibi/CaR-constraint](https://github.com/jieyibi/CaR-constraint)

</details>

---

### 15. [Framework of Thoughts: A Foundation Framework for Dynamic and Optimized Reasoning based on Chains, Trees, and Graphs](https://arxiv.org/abs/2602.16512)

**Authors**: Felix Fricke, Simon Malberg, Georg Groh  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.16512v1  

#### Abstract
Prompting schemes such as Chain of Thought, Tree of Thoughts, and Graph of Thoughts can significantly enhance the reasoning capabilities of large language models. However, most existing schemes require users to define static, problem-specific reasoning structures that lack adaptability to dynamic or...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Framework of Thoughts: A Foundation Framework for Dynamic and Optimized Reasoning based on Chains, Trees, and Graphs

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

现有的 **prompting schemes**（如 Chain of Thought, Tree of Thoughts, Graph of Thoughts）存在三大关键局限性：

1. **静态图结构**：大多数方案依赖人工定义的、固定的推理结构，无法适应动态或未见过的问题类型。
2. **优化不足**：提示（prompts）和超参数（hyperparameters）通常未经系统优化，限制了性能潜力。
3. **执行效率低**：多数方案串行执行 LLM 调用，重复计算多，导致高延迟和高成本。

### 提出了什么新方法或新思路

作者提出了 **Framework of Thoughts (FoT)** ——一个通用的、基础性的框架，用于构建和优化基于链、树和图的动态推理方案。

FoT 不是一个具体的推理方法，而是一个支持多种 prompting schemes 实现和自动优化的**元框架**。

### 相比现有方法的优势

FoT 具备以下四大核心优势：

- **(a) 动态图结构（Dynamic graph structures）**  
  支持在运行时自动演化图结构，允许推理路径根据问题实例动态调整，提升泛化能力。

- **(b) 并行化执行（Faster parallelized execution）**  
  通过智能调度器实现安全的并行操作执行，显著减少等待时间。

- **(c) 持久缓存机制（Cost savings through persistent caching）**  
  缓存操作结果（进程级和持久级），避免重复调用 LLM，大幅降低成本。

- **(d) 内建优化工具（Optimized hyperparameters and prompts）**  
  集成 **Optuna** 进行超参数优化，以及 **DSPy/COPRO** 进行提示词优化，帮助开发者挖掘方案的最大潜力。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

实验覆盖五个任务，分别来自三个经典 prompting schemes 的原始研究：

| 任务 | 数据集 | 来源 |
|------|--------|------|
| 数学推理 | Game of 24 (**Go24**) | Yao et al. (2023) |
| 排序任务 | Sorting (128个整数排序) | Besta et al. (2024a) |
| 文档合并 | Document Merging (**DM**) | Besta et al. (2024a) |
| 多跳问答 | **HotpotQA** | Yang et al. (2018) |
| 多跳问答 | **MuSiQue** | Trivedi et al. (2022) |

### 实验设置和评估指标

#### 实验设置：
- 将三种主流方案（**ToT**, **GoT**, **ProbTree**）重新实现在 FoT 框架中。
- 对比不同配置下的性能：
  - 是否启用 **并行执行**（Parallel vs Sequential）
  - 是否启用 **缓存**（No cache / Process cache / Persistent cache）
  - 是否进行 **超参数与提示优化**

#### 评估指标：
| 任务 | 主要指标 |
|------|---------|
| Go24 | 准确率（Accuracy） |
| Sorting | 错误数量（Mistakes，越少越好） |
| DM | F1 分数（基于冗余与保留信息） |
| HotpotQA / MuSiQue | F1 分数 |
| 所有任务 | 单实例平均运行时间（秒）、成本（USD cents） |

#### 基线方法对比
- 原始方案作为基准（如 Yao et al. 的 ToT、Besta et al. 的 GoT）
- 在相同模型下复现实验（如 GPT-4o 替代 GPT-4，GPT-3.5-Turbo 用于排序等）
- 对比不同配置组合（S/P + 缓存策略）

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 表格 3：平均运行时间和成本（关键结果汇总）

| 方案 | 设置 | 平均运行时间（秒） | 加速比 | 成本（美分） |
|------|------|------------------|--------|-------------|
| ToT (Go24) | Sequential + No cache | 782 | 1.0x | 29.6 |
| → | Parallel + Persistent cache | **22** | **35.4x** | **16.1** |
| GoT (Sorting) | Sequential + No cache | 259 | 1.0x | 5.0 |
| → | Parallel + Persistent cache | **30** | **8.5x** | 5.0 |
| ProbTree (MuSiQue) | Sequential + No cache | 21.6 | 1.0x | 0.8 |
| → | Parallel + Persistent cache | **8.9** | **2.4x** | **0.7** |

> ✅ **总体加速达 10.7x**，最大提速 **35.4x**（ToT on Go24），成本降低最高达 **46%**。

#### 表格 4：优化后任务得分提升

| 方案 | 任务 | 原始得分 | 优化后得分 | 变化 |
|------|------|----------|------------|------|
| ToT | Go24 (准确率) | 63.0% | **66.0%** | ↑3.0% |
| ToT | Sorting (错误数) | 18.4 | **18.2** | ↓0.2 |
| GoT | Sorting (错误数) | 12.7 | **12.1** | ↓0.6 |
| GoT | DM (F1) | 8.4 | **8.8** | ↑0.4 |

> ✅ 所有方案在优化后均实现了**更高任务得分 + 更低成本**。

#### 优化过程效率提升（消融实验）

| 设置 | 总优化耗时（分钟） | 加速比 |
|------|------------------|--------|
| Sequential + No cache | 最高达 39,596 min (~27.5天) | 1.0x |
| Parallel + Persistent cache | **最低 788 min (~13小时)** | **50.2x** |

> ⚠️ 无缓存时优化成本极高；**引入缓存使优化成本降至原来的 9–36%**，成为可行工程实践。

---

## 4. 关键结论和发现

### 主要发现

1. **动态图结构是未来方向**  
   FoT 支持运行时图演化，为应对未知或复杂问题提供了更强的适应性。

2. **并行 + 缓存 = 效率革命**  
   并行执行结合智能缓存可将推理速度提升一个数量级（10.7x），同时显著降低成本。

3. **优化必须与高效执行共存**  
   超参数和提示优化本身开销巨大，只有在具备缓存和并行能力的前提下才具有实际可行性。

4. **FoT 是通用增强平台**  
   即使是对已有方案（如 ToT、GoT）的“移植”，也能通过 FoT 的基础设施获得显著性能增益。

### 方法的局限性

- 当前验证集中在 **半自动** 和 **手动** prompting schemes（如 ToT、GoT），尚未充分展示**完全自动化动态图**的优势。
- 实验未涵盖更多复杂外部工具集成场景（如数据库查询、代码执行闭环）。
- 图修改的安全约束较保守，可能限制某些高度并发的动态行为。

### 未来工作方向

- 开发新的 **Fully Automatic** prompting schemes，充分利用 FoT 的动态图能力。
- 支持更灵活的图修改规则，在保证安全的同时提升并发度。
- 实现 **联合优化**：同时优化 prompts 和 hyperparameters。
- 引入更多 prompt optimization 技术（如 GEPA）进行多策略探索。
- 提供更高层次的抽象接口，降低使用门槛。

---

> 🔗 **开源声明**：作者已公开完整代码库（[GitHub](https://github.com/fjfricke/framework-of-thoughts)），支持复现与扩展。

> 🛑 **伦理提醒**：LLM-based prompting schemes 可能生成看似合理但错误的答案，高风险场景需人工校验输出。

</details>

---

### 16. [Gated Tree Cross-attention for Checkpoint-Compatible Syntax Injection in Decoder-Only LLMs](https://arxiv.org/abs/2602.15846)

**Authors**: Xinyu Gao, Shaonan Wang, Nai Ding  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.15846v1  

#### Abstract
Decoder-only large language models achieve strong broad performance but are brittle to minor grammatical perturbations, undermining reliability for downstream reasoning. However, directly injecting explicit syntactic structure into an existing checkpoint can interfere with its pretrained competence....

---

### 17. [CLAA: Cross-Layer Attention Aggregation for Accelerating LLM Prefill](https://arxiv.org/abs/2602.16054)

**Authors**: Bradley McDanel, Steven Li, Harshit Khaitan  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.16054v1  

#### Abstract
The prefill stage in long-context LLM inference remains a computational bottleneck. Recent token-ranking heuristics accelerate inference by selectively processing a subset of semantically relevant tokens. However, existing methods suffer from unstable token importance estimation, often varying betwe...

---

### 18. [Reinforced Fast Weights with Next-Sequence Prediction](https://arxiv.org/abs/2602.16704)

**Authors**: Hee Seung Hwang, Xindi Wu, Sanghyuk Chun, Olga Russakovsky  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2602.16704v1  

#### Abstract
Fast weight architectures offer a promising alternative to attention-based transformers for long-context modeling by maintaining constant memory overhead regardless of context length. However, their potential is limited by the next-token prediction (NTP) training paradigm. NTP optimizes single-token...

---

### 19. [EnterpriseGym Corecraft: Training Generalizable Agents on High-Fidelity RL Environments](https://arxiv.org/abs/2602.16179)

**Authors**: Sushant Mehta, Logan Ritchie, Suhaas Garre, Nick Heiner, Edwin Chen  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.16179v1  

#### Abstract
We show that training AI agents on high-fidelity reinforcement learning environments produces capabilities that generalize beyond the training distribution. We introduce \corecraft{}, the first environment in \textsc{EnterpriseGym}, Surge AI's suite of agentic RL environments. \corecraft{} is a full...

---

### 20. [Multi-source Heterogeneous Public Opinion Analysis via Collaborative Reasoning and Adaptive Fusion: A Systematically Integrated Approach](https://arxiv.org/abs/2602.15857)

**Authors**: Yi Liu  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15857v1  

#### Abstract
The analysis of public opinion from multiple heterogeneous sources presents significant challenges due to structural differences, semantic variations, and platform-specific biases. This paper introduces a novel Collaborative Reasoning and Adaptive Fusion (CRAF) framework that systematically integrat...

---

### 21. [Towards Fair and Efficient De-identification: Quantifying the Efficiency and Generalizability of De-identification Approaches](https://arxiv.org/abs/2602.15869)

**Authors**: Noopur Zambare, Kiana Aghakasiri, Carissa Lin, Carrie Ye, J. Ross Mitchell, Mohamed Abdalla  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15869v1  

#### Abstract
Large language models (LLMs) have shown strong performance on clinical de-identification, the task of identifying sensitive identifiers to protect privacy. However, previous work has not examined their generalizability between formats, cultures, and genders. In this work, we systematically evaluate ...

---

### 22. [VDLM: Variable Diffusion LMs via Robust Latent-to-Text Rendering](https://arxiv.org/abs/2602.15870)

**Authors**: Shuhui Qu  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15870v1  

#### Abstract
Autoregressive language models decode left-to-right with irreversible commitments, limiting revision during multi-step reasoning. We propose \textbf{VDLM}, a modular variable diffusion language model that separates semantic planning from text rendering. VDLM applies LLaDA-style masked diffusion over...

---

### 23. [P-RAG: Prompt-Enhanced Parametric RAG with LoRA and Selective CoT for Biomedical and Multi-Hop QA](https://arxiv.org/abs/2602.15874)

**Authors**: Xingda Lyu, Gongfu Lyu, Zitai Yan, Yuxin Jiang  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15874v1  

#### Abstract
Large Language Models (LLMs) demonstrate remarkable capabilities but remain limited by their reliance on static training data. Retrieval-Augmented Generation (RAG) addresses this constraint by retrieving external knowledge during inference, though it still depends heavily on knowledge base quality. ...

---

### 24. [Quecto-V1: Empirical Analysis of 8-bit Quantized Small Language Models for On-Device Legal Retrieval](https://arxiv.org/abs/2602.16640)

**Authors**: Subrit Dikshit  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.16640v1  

#### Abstract
The rapid proliferation of Large Language Models (LLMs) has revolutionized Natural Language Processing (NLP) but has simultaneously created a "resource divide." State-of-the-art legal intelligence systems typically rely on massive parameter counts (7B+) and cloud-based inference, rendering them inac...

---

### 25. [Distributed Order Recording Techniques for Efficient Record-and-Replay of Multi-threaded Programs](https://arxiv.org/abs/2602.15995)

**Authors**: Xiang Fu, Shiman Meng, Weiping Zhang, Luanzheng Guo, Kento Sato, Dong H. Ahn, Ignacio Laguna, Gregory L. Lee, Martin Schulz  
**Category**: cs.DC  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.15995v1  

#### Abstract
After all these years and all these other shared memory programming frameworks, OpenMP is still the most popular one. However, its greater levels of non-deterministic execution makes debugging and testing more challenging. The ability to record and deterministically replay the program execution is k...

---

### 26. [SEMixer: Semantics Enhanced MLP-Mixer for Multiscale Mixing and Long-term Time Series Forecasting](https://arxiv.org/abs/2602.16220)

**Authors**: Xu Zhang, Qitong Wang, Peng Wang, Wei Wang  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.16220v1  

#### Abstract
Modeling multiscale patterns is crucial for long-term time series forecasting (TSF). However, redundancy and noise in time series, together with semantic gaps between non-adjacent scales, make the efficient alignment and integration of multi-scale temporal dependencies challenging. To address this, ...

---

### 27. [A Scalable Approach to Solving Simulation-Based Network Security Games](https://arxiv.org/abs/2602.16564)

**Authors**: Michael Lanier, Yevgeniy Vorobeychik  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.16564v1  

#### Abstract
We introduce MetaDOAR, a lightweight meta-controller that augments the Double Oracle / PSRO paradigm with a learned, partition-aware filtering layer and Q-value caching to enable scalable multi-agent reinforcement learning on very large cyber-network environments. MetaDOAR learns a compact state pro...

---

### 28. [Knowledge-Embedded Latent Projection for Robust Representation Learning](https://arxiv.org/abs/2602.16709)

**Authors**: Weijing Tang, Ming Yuan, Zongqi Xia, Tianxi Cai  
**Category**: cs.LG  
**Published**: 2026-02-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2602.16709v1  

#### Abstract
Latent space models are widely used for analyzing high-dimensional discrete data matrices, such as patient-feature matrices in electronic health records (EHRs), by capturing complex dependence structures through low-dimensional embeddings. However, estimation becomes challenging in the imbalanced re...

---

### 29. [Multi-agent cooperation through in-context co-player inference](https://arxiv.org/abs/2602.16301)

**Authors**: Marissa A. Weis, Maciej Wo{\l}czyk, Rajai Nasser, Rif A. Saurous, Blaise Ag\"uera y Arcas, Jo\~ao Sacramento, Alexander Meulemans  
**Category**: cs.AI  
**Published**: 2026-02-19  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.16301v1  

#### Abstract
Achieving cooperation among self-interested agents remains a fundamental challenge in multi-agent reinforcement learning. Recent work showed that mutual cooperation can be induced between "learning-aware" agents that account for and shape the learning dynamics of their co-players. However, existing ...

---

### 30. [TabAgent: A Framework for Replacing Agentic Generative Components with Tabular-Textual Classifiers](https://arxiv.org/abs/2602.16429)

**Authors**: Ido Levy, Eilam Shapira, Yinon Goldshtein, Avi Yaeli, Nir Mashkif, Segev Shlomov  
**Category**: cs.CL  
**Published**: 2026-02-19  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2602.16429v1  

#### Abstract
Agentic systems, AI architectures that autonomously execute multi-step workflows to achieve complex goals, are often built using repeated large language model (LLM) calls for closed-set decision tasks such as routing, shortlisting, gating, and verification. While convenient, this design makes deploy...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
