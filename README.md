# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-19 05:57:41 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Staggered Batch Scheduling: Co-optimizing Time-to-First-Token and Throughput for High-Efficiency LLM Inference](https://arxiv.org/abs/2512.16134)

**Authors**: Jian Tian, Shuailong Li, Yang Cao, Wenbo Cui, Minghan Zhu, Wenkang Wu, Jianming Zhang, Yanpeng Wang, Zhiwen Xiao, Zhenyu Hou, Dou Shen  
**Category**: cs.DC  
**Published**: 2025-12-19  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.16134v1  

#### Abstract
The evolution of Large Language Model (LLM) serving towards complex, distributed architectures--specifically the P/D-separated, large-scale DP+EP paradigm--introduces distinct scheduling challenges. Unlike traditional deployments where schedulers can treat instances as black boxes, DP+EP architectur...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Staggered Batch Scheduling: Co-optimizing Time-to-First-Token and Throughput for High-Efficiency LLM Inference*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对大规模 **DP+EP**（Data Parallelism + Expert Parallelism）架构下的 **Large Language Model (LLM)** 推理服务中存在的调度效率瓶颈问题，尤其是 **P/D分离架构**（Prefill/Decode disaggregation）中因高同步开销导致的性能退化。

具体问题包括：
- **Immediate Dispatch** 策略在非抢占式、离散批处理的 **Prefill 阶段** 引发严重的 **Head-of-Line (HOL) 阻塞** 和设备内部排队（Device-side Queuing），显著增加 **TTFT**（Time-to-First-Token）。
- 在 **DP+EP 架构** 中，由于 All-to-All 通信和负载不均衡（straggler effect），传统调度策略无法有效平衡各 DP 单元间的计算负载，造成资源浪费和吞吐下降。

---

### 提出的新方法与创新思路

#### ✅ **Staggered Batch Scheduling (SBS)**
- **核心思想**：主动引入一个短暂的、自适应的等待窗口（buffering window），将请求聚合为最优批次后再统一调度，实现“以短时等待换取更优执行”。
- **机制优势**：
  - 将排队从不可控的 **Device-side Queue** 转移到可管理的 **Scheduler-side Queue**。
  - 消除内部 HOL 阻塞，显著降低 TTFT。
  - 利用批处理窗口获得全局视图，支持精细化负载均衡。

#### ✅ **Load-Aware Global Allocation**
- 基于 SBS 提供的批处理窗口，设计了两个阶段的统一负载分配策略：
  - **Prefill 阶段**：采用 **Water-Filling 启发式算法**（基于贪心的 bin-packing），优先将长序列请求分配给剩余容量最大的 DP 单元，减少并行化气泡（Parallelization Bubbles）。
  - **Decode 阶段**：提出 **IQR-Aware Lexicographical Scheduling**，联合优化 **Batch Size 平衡** 与 **KV Cache 内存负载平衡**，缓解耦合型负载失衡问题。

#### ✅ **闭环反馈控制机制**
- 设计了一个包含 **Control Plane**、**State Plane** 和 **Resource Plane** 的闭环系统，动态调整调度间隔 $I_{opt}$，确保与集群处理能力匹配。
- 引入三重状态同步协议（Quiescence Polling, Asynchronous Signaling, Liveness Watchdog），保障系统鲁棒性和可用性。

---

### 相比现有方法的优势

| 维度 | 传统方法（如 Immediate Dispatch） | 本文 SBS |
|------|-------------------------------|--------|
| **TTFT** | 高（受设备内排队影响） | ↓ 30–40% |
| **Throughput** | 受负载不均限制 | ↑ 15–20% |
| **负载均衡** | 局部决策，易产生 straggler | 全局优化，显著抑制 straggler |
| **调度粒度** | 实例级（Instance-level） | 子实例级（DP-Attention Unit 级） |
| **适用架构** | 单体或简单 TP 架构 | 复杂 P/D 分离 + DP+EP 架构 |

> SBS 不是简单的“延迟调度”，而是通过 **时间解耦** 实现 **空间利用率最大化**，实现了 **TTFT 与 Throughput 的协同优化**。

---

## 2. 核心实验方法和设置

### 实验平台与模型
- **硬件环境**：生产级 **NVIDIA H800 GPU 集群**
- **部署架构**：**DeepSeek-V3** 模型，采用 **P/D 分离 + DP+EP 混合并行** 架构
- **典型配置**：
  - Prefill 实例：`TP=4`, `DP=8`, `EP=32`
  - Decode 实例：`TP=1`, `DP=32`, `EP=32`
  - Chunk Size：3K / 5K / 16K tokens

### 工作负载（Workload）
- **Prefill 阶段测试**：
  - 输入长度范围：0–3K tokens（均值 1K）和 3K–64K tokens（均值 6.7K）
  - 模拟真实对话场景中的变长请求分布
- **Decode 阶段测试**：
  - 总长度约 2.5K tokens（含输入输出）
  - 平均 Batch Size ≈ 35

### 评估指标
| 指标 | 含义 |
|------|------|
| **TTFT** | Time-to-First-Token，首 token 延迟 |
| **Queue Time** | 请求在队列中的等待时间（区分 device-side vs scheduler-side） |
| **QPS** | Queries Per Second，系统吞吐量 |
| **Chunk Utilization** | Prefill Chunk 的 token 容量利用率 |
| **KV Cache Load Distribution** | Decode 阶段各 DP 单元的缓存占用方差 |
| **P99 Latency** | 尾部延迟表现 |

### 基线方法对比
- **Baseline**：Immediate Dispatch 调度器（即来即发）
- 对比项包括：
  - 不同负载水平下的 TTFT 与吞吐
  - 内部队列延迟分解
  - 负载均衡程度（KV Cache 方差、Chunk 利用率）

---

## 3. 主要实验结果和性能指标

### 📉 **TTFT 显著降低（最高达 40%）**

| 场景 | 结果 |
|------|------|
| **输入长度 0–3K tokens**（图6a） | 在 ≤80% 负载下，SBS 相比 baseline **TTFT 下降 30–40%** |
| **输入长度 3K–64K tokens**（图6b） | 即使在高方差长上下文场景下，仍能 **有效压制尾延迟（tail latency）**，验证鲁棒性 |

> 关键原因：SBS 成功将原本隐藏在设备内部的排队暴露到调度层，并通过批量调度消除空转周期。

---

### 📈 **Throughput 显著提升**

#### ✅ **Prefill 阶段吞吐提升 12.9% – 22.8%**

| 配置 | Baseline QPS | SBS QPS | ΔQPS (%) | Chunk Util. 提升 |
|------|-------------|--------|----------|------------------|
| Chunk 3K | 57 | 70 | **+22.8%** | 51.83% → 88.7% (+36.9pp) |
| Chunk 5K | 70 | 79 | **+12.9%** | 53.0% → 88.0% (+35.0pp) |

> **Chunk Utilization 从 ~52% 提升至 ~88%**，说明 SBS 成功填补了并行化过程中的“气泡”，极大提升了资源利用率。

#### ✅ **Decode 阶段吞吐提升 ~15%**

- **KV Cache 负载方差降低 40%**（图7）：
  - Baseline：±1σ 范围为 40K–130K tokens，存在极端 outlier（~150K）
  - SBS：稳定在 60K–100K tokens，无严重 straggler
- **Aggregate Decode Throughput 提升 15%**（图8）
  - 原因：通过 IQR-aware masking + lexicographical selection，有效避免慢节点拖累整体同步速度

---

### 🔍 消融分析（隐含于设计逻辑中）

虽然未明确列出消融表，但从机制设计可推断以下关键组件的作用：

| 组件 | 功能 | 影响 |
|------|------|------|
| **Adaptive Interval ($I_{opt}$)** | 动态匹配集群吞吐 | 避免过早或过晚 dispatch，保持高利用率 |
| **Water-Filling Allocation (Prefill)** | 贪心最长序列优先分配 | 减少 straggler，提高 Chunk 利用率 |
| **IQR-Aware Masking (Decode)** | 过滤潜在内存溢出 DP 单元 | 提升调度安全性与稳定性 |
| **Lexicographical Selection** | 先平衡 Batch Size，再平衡 KV Cache | 实现多目标协同优化 |

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **Immediate Dispatch 在 DP+EP 架构中本质上是次优的**  
   因其忽视 Prefill 阶段的“门控服务”特性（Gated Service），导致大量请求陷入不可见的设备内队列，恶化 TTFT。

2. **短时缓冲反而能加速整体响应（Counter-intuitive acceleration）**  
   SBS 证明：通过可控的 **scheduler-side buffering**，可以将碎片化的并行执行转化为高度协调的批量处理，从而同时优化延迟与吞吐。

3. **全局视图是解决负载不均衡的关键**  
   批处理窗口提供了观察多个请求的机会，使得 **bin-packing** 和 **multi-objective allocation** 成为可能，显著改善 DP 单元间的负载分布。

4. **Prefill 与 Decode 的负载失衡本质不同但可统一建模**  
   - Prefill：主要是计算负载不均（sequence length）
   - Decode：是 **Batch Size 与 KV Cache 容量的耦合失衡**
   - SBS 提出分阶段策略，在统一框架下分别应对

---

### ⚠️ 局限性

1. **依赖稳定的网络与低延迟反馈机制**  
   若 EndForward 信号延迟过高或丢失频繁，会影响 $I_{opt}$ 的准确性，进而影响性能。

2. **对极低延迟 SLA 场景可能存在挑战**  
   虽然平均 TTFT 下降，但在极低容忍延迟（如 <100ms）的场景中，人为引入的 buffering window 可能成为瓶颈。

3. **扩展性依赖健康实例探测机制**  
   当集群规模进一步扩大时，拓扑感知与故障检测复杂度上升，需更强健的 auto-scaling 支持。

---

### 🔮 未来工作方向

1. **结合 Prefix Cache-Aware Scheduling**  
   如 Preble 或 SGL-Router 所示，利用前缀共享进一步减少冗余计算，与 SBS 形成协同优化。

2. **支持动态弹性扩缩容（Auto-scaling）下的快速收敛**  
   当前 $I_{opt}$ 依赖滑动窗口统计，未来可探索更快速适应突发流量的控制算法（如 PID 控制器）。

3. **跨租户公平性保障机制**  
   在多租户环境下，如何在批处理中保证 FCFS 公平性与优先级调度的兼容性值得深入研究。

4. **应用于 MoE 动态路由场景**  
   将 SBS 思想延伸至专家选择（expert routing）层面，实现 **routing + scheduling joint optimization**。

---

## 总结

> **SBS 的核心洞见是：在高同步成本的现代 LLM 推理架构中，“立即调度”不再是最佳选择；适度延迟、全局统筹的“错峰批处理”才是实现低延迟与高吞吐双赢的关键。**

该工作不仅提出了实用高效的调度机制，更为下一代超大规模 MoE 模型的推理系统设计提供了可扩展的范式——**离散化调度 + 全局负载整形（Discrete Scheduling + Global Load Shaping）**。

</details>

---

### 2. [Efficient CPU-GPU Collaborative Inference for MoE-based LLMs on Memory-Limited Systems](https://arxiv.org/abs/2512.16473)

**Authors**: En-Ming Huang, Li-Shang Lin, Chun-Yi Lee  
**Category**: cs.DC  
**Published**: 2025-12-19  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.16473v1  

#### Abstract
Large Language Models (LLMs) have achieved impressive results across various tasks, yet their high computational demands pose deployment challenges, especially on consumer-grade hardware. Mixture of Experts (MoE) models provide an efficient solution through selective activation of parameter subsets,...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Efficient CPU-GPU Collaborative Inference for MoE-based LLMs on Memory-Limited Systems

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Mixture of Experts (MoE)** 的大语言模型（如 Mixtral 8x7B）虽然在计算上具有稀疏激活优势，但其庞大的模型权重（例如 80GB）远超消费级 GPU（如 RTX 4090 的 24GB 显存），导致部署困难。传统的 **offloading** 方法将专家权重从 CPU 内存加载到 GPU，但由于通信延迟高，严重影响推理速度，尤其是在单请求（single-request）场景下。

此外，现有方法存在以下限制：
- **仅依赖 GPU 计算**，未充分利用 CPU 多核并行能力；
- **预取（prefetching）机制受限于通信瓶颈**，难以实现有效计算-通信重叠；
- 部分方法需修改模型结构或进行离线分析（profiling），通用性差。

---

### 提出的新方法与创新思路

本文提出一种 **CPU-GPU 协同推理框架（CPU-GPU Collaborative Inference Framework）**，核心思想是：

1. **将 GPU 显存作为 MoE 专家的缓存（expert cache）**  
   - 将部分频繁使用的专家权重保留在 GPU 上，其余存储在 CPU 内存中。
   - 利用 **LRU 缓存策略** 动态管理缓存内容，基于“连续 token 中专家重复使用”的观察设计。

2. **异步处理缓存未命中（asynchronous cache miss handling）**
   - 当所需专家不在 GPU 缓存中时（cache miss），不等待数据传输，而是立即将中间状态（attention 输出）传至 CPU，在 CPU 上完成该层的专家计算。
   - 同时后台异步地将缺失专家从 CPU 加载回 GPU，供后续 token 使用。

3. **充分发挥 CPU 多线程计算潜力**
   - 利用 PyTorch 的 OpenMP 支持，在 CPU 上高效执行 FFN 层计算。
   - 实验表明：尽管单次计算慢于 GPU，但避免了高昂的 PCIe 数据传输开销，在整体延迟上更优。

---

### 相比现有方法的优势

| 方面 | 本方法 | 现有方法（如 DeepSpeed、Pre-gated MoE、Fiddler） |
|------|--------|---------------------------------------------|
| 是否需要模型修改 | ❌ 不需要 | ✅ Pre-gated MoE 需改 router |
| 是否依赖离线 profiling | ❌ 动态适应 | ✅ Fiddler 需统计专家激活频率 |
| 是否利用 CPU 计算资源 | ✅ 充分利用多核 CPU | ❌ 多数仅用于数据搬运 |
| 通信-计算重叠效率 | ✅ 异步加载 + CPU 并行计算 | ⚠️ 预取受限于短时间窗口 |
| 兼容性 | ✅ 支持任意 MoE 模型（Mixtral、Phi-3.5-MoE） | ❌ 多为特定架构定制 |
| 性能提升 | ✅ 最高达 4.4× 速度提升 | — |

> ✅ **核心洞见**：对于 MoE 推理，**通信成本 > 计算成本**，因此减少传输比加速计算更重要；而 CPU 是可被有效利用的闲置资源。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 主要测试集：**MMLU [22][23]** 数据集上的实际推理任务（真实 prompt 输入）
- 目的：分析专家选择模式（expert selection pattern），验证缓存有效性

### 实验设置
- **硬件平台**：
  - CPU: AMD Ryzen Threadripper 7960X（24 核）
  - GPU: NVIDIA RTX 4090（24GB）
  - 互联：PCIe Gen 4.0 ×16（双向带宽 64 GB/s）
- **软件环境**：
  - 基于官方 MistralAI 和 HuggingFace 实现
  - 使用 CUDA Streams 实现双通道并发传输（fetch 与 compute 分离）

### 评估指标
| 指标 | 描述 |
|------|------|
| **吞吐量（Throughput）** | tokens/sec，衡量推理速度 |
| **缓存命中率（Hit Rate）** | “至少一个专家命中”、“两个专家都命中” |
| **端到端延迟（Latency）** | 单个 token 生成时间 |
| **功耗与能耗（Power & Energy）** | 使用 RAPL 和 nvidia-smi 监控，计算 Joule/token |

### 对比的基线方法
| 方法 | 类型 | 特点 |
|------|------|------|
| **On-demand Fetching** [13][21] | 按需加载 | 路由决定后才开始加载专家，无预取 |
| **Pre-gated MoE** [15] | 预取类 | 修改 router 提前预测下一層专家，假设完美重叠 |
| **Fiddler** [26] | CPU-GPU 协作 | 基于离线 profiling 缓存热门专家，决策复杂度高 |
| **CPU Only** | 完全 CPU 推理 | 所有专家计算在 CPU 执行，作为性能下界 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Table II & Fig. 5）

| 模型 | 方法 | 最高吞吐量（tokens/sec） | 相对提速 |
|------|------|--------------------------|-----------|
| **Mixtral 8x7B** | 本文方法（24 cores） | **4.8** | ↑4.4× vs Pre-gated MoE |
| **Phi-3.5-MoE** | 本文方法（24 cores） | **10.4** | ↑4.3× vs Pre-gated MoE |
| Mixtral 8x7B | On-demand Fetching | ~1.1 | 基线 |
| Mixtral 8x7B | Pre-gated MoE（理论最优） | ~1.1 | 实际无法达到理想重叠 |

> 💡 注：即使 Pre-gated MoE 假设完全重叠，仍受限于 PCIe 传输速率，无法突破瓶颈。

---

### 与基线方法对比结果

- 在 **Mixtral 8x7B** 上：
  - 本文方法比 **on-demand fetching** 快 **4.4×**
  - 比 **Fiddler** 快 **1.6×**
  - 仅用 **2 个 CPU 核心** 即可超越所有 GPU-only offloading 方法

- 在 **Phi-3.5-MoE** 上：
  - Fiddler 表现较差，因其依赖离线 profiling，且专家数量翻倍（16 vs 8）导致决策开销指数增长
  - 本文方法通过动态缓存适应性强，表现稳定领先

- **CPU-only 配置已优于传统 offloading**：
  - 由于通信耗时（~28ms）远大于 CPU 计算时间（多核下 <10ms），直接在 CPU 运算反而更快

---

### 消融实验结果（Ablation Studies）

#### （1）不同 CPU 核心数的影响（Fig. 3 & Fig. 5）
- **1–4 核心**：计算是瓶颈 → 应优先扩大缓存覆盖层数（更多 indexes）
- **8–24 核心**：通信成瓶颈 → 应增加每层缓存方式数（more ways）以提高命中率
- 结论：**缓存配置应随 CPU 资源动态调整**

#### （2）缓存替换策略比较（Fig. 6）
| 替换策略 | Mixtral 8x7B 缓存命中率 | Phi-3.5-MoE 缓存命中率 |
|---------|------------------------|------------------------|
| Random | ~40%（1 expert hit） | ~30% |
| FIFO | 略高于 Random | — |
| **LRU** | **↑5–15% 提升** | **显著优于 Random** |

> ✅ LRU 更符合“近期使用的专家很可能再次使用”的局部性规律

#### （3）能量效率分析（Table V）
| 模型 | 方法 | Joule/token（越低越好） | 能效提升 |
|------|------|------------------------|----------|
| Mixtral 8x7B | Pre-gated MoE | 171.3 | 基准 |
| Mixtral 8x7B | 本文（24 cores） | **51.1** | ↓70.2% 能耗 |
| Phi-3.5-MoE | Pre-gated MoE | 78.7 | 基准 |
| Phi-3.5-MoE | 本文（24 cores） | **21.9** | ↓72.2% 能耗 |

> 🔋 结论：更高的 CPU 利用率带来更高功耗，但因速度大幅提升，**单位 token 能耗显著降低**

---

## 4. 关键结论和发现

### 主要发现
1. **CPU 是被严重低估的计算资源**：在 MoE 推理中，CPU 多核并行足以弥补其单核性能劣势，且避免了昂贵的 PCIe 传输。
2. **通信是主要瓶颈**：专家权重传输时间（~28ms）远超 GPU 计算时间（~0.3ms），使得任何依赖频繁传输的方法都难逃性能天花板。
3. **存在强专家复用模式**：
   - **Consecutive Layers Pattern**：相邻层常选相同专家（约 44%）
   - **Consecutive Tokens Pattern**：同一层在连续 token 中重复使用专家概率达 40–60%
   - 这些模式支持有效的缓存设计
4. **异步协作优于同步预取**：本文提出的“CPU 计算 + 异步回填”机制实现了更好的计算-通信重叠。

---

### 方法的局限性
- **依赖较强的 CPU 性能**：若 CPU 较弱或多核调度不佳，性能增益会下降。
- **不适合极高吞吐场景**：本文聚焦 single-request 场景，对大规模 batch 推理优化有限。
- **缓存容量受显存限制**：只能缓存部分专家，命中率上限受限于 GPU memory size。

---

### 未来工作方向
1. 扩展至 **multi-user / batched inference** 场景下的资源调度；
2. 结合量化（quantization）进一步压缩专家体积，提升缓存覆盖率；
3. 自适应缓存策略：根据运行时负载自动调节 cache 配置（indexes/ways）；
4. 探索 **NVLink / UCIe** 等高速互连技术下的协同机制。

---

## 总结

✅ 本文提出了一种无需修改模型、无需离线分析的 **CPU-GPU 协同 MoE 推理框架**，通过引入 **GPU 专家缓存 + CPU 多核计算 + 异步回填机制**，有效缓解了消费级设备上的内存瓶颈。

🚀 实验表明，该方法在 Mixtral 8x7B 和 Phi-3.5-MoE 上分别实现 **4.8 和 10.4 tokens/sec** 的推理速度，相比现有方案最高提速 **4.4×**，同时节能超过 **70%**。

🧠 核心启示：**在异构系统中，最大化资源利用率比单一追求峰值算力更重要**。

</details>

---

### 3. [AIE4ML: An End-to-End Framework for Compiling Neural Networks for the Next Generation of AMD AI Engines](https://arxiv.org/abs/2512.15946)

**Authors**: Dimitrios Danopoulos, Enrico Lupi, Chang Sun, Sebastian Dittmeier, Michael Kagan, Vladimir Loncar, Maurizio Pierini  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.15946v1  

#### Abstract
Efficient AI inference on AMD's Versal AI Engine (AIE) is challenging due to tightly coupled VLIW execution, explicit datapaths, and local memory management. Prior work focused on first-generation AIE kernel optimizations, without tackling full neural network execution across the 2D array. In this w...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AIE4ML: An End-to-End Framework for Compiling Neural Networks for the Next Generation of AMD AI Engines

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前在 AMD Versal AI Engine（AIE）上进行高效 AI 推理面临多重挑战：
- **架构复杂性**：AIE 采用 VLIW 架构、显式数据流和本地内存管理，编程难度高。
- **缺乏端到端支持**：已有工作（如 MaxEVA、AutoMM、CHARM）主要聚焦于单个 GEMM 内核优化，未解决完整神经网络在 AIE-ML 2D 阵列上的自动编译、层间通信和全片上执行问题。
- **对低延迟场景支持不足**：许多实时应用（如粒子物理实验中的触发系统）需要微秒级延迟和确定性推理，而主流加速器（GPU/TPU）难以满足。

### 提出的新方法与创新
AIE4ML 是首个面向新一代 AMD AIE-ML 架构的**端到端神经网络编译框架**，其核心创新包括：

#### ✅ 全栈自动化编译流程
- 支持从 PyTorch/TensorFlow 等高级框架导入量化模型，自动生成优化固件。
- 引入专有的 **AIE-IR（Intermediate Representation）**，统一表示层拓扑、张量维度、量化格式和连接关系。

#### ✅ 高效通用线性层实现
- 设计基于 `aie::mmul` API 的通用线性层内核，原生支持 **bias 加法融合** 和 **ReLU 激活融合**。
- 采用 **2×2 分块累加器调度**，提升算术强度并隐藏访存延迟，接近硬件峰值性能。

#### ✅ 基于 Memory Tile 的全片上数据流
- 利用 AIE-ML 新增的 **Memory Tiles** 作为层间中继缓冲区，实现：
  - 层间激活值的零填充（zero-padding）
  - 动态重分块（re-tiling）
  - 广播传输（broadcasting）
- 所有数据移动均在芯片内部完成，无需 PL 参与，避免 I/O 瓶颈。

#### ✅ 自动化图放置算法
- 提出一种基于 **Branch-and-Bound (B&B)** 的搜索算法，用于将多层计算图映射到 2D AIE 阵列。
- 最小化跨层连接开销，优先使用低行（靠近存储资源），生成紧凑且高效的布局。

#### ✅ 位精确（bit-exact）量化支持
- 支持从 hls4ml 或 PyTorch 导入的量化模型，并在整个工具链中保持 **bit-exactness**。
- 显式支持整数量化精度（如 i8×i8, i16×i8），适配 AIE-ML 的整数运算单元。

### 相比现有方法的优势

| 特性 | AIE4ML | Prior Works (e.g., AutoMM, CHARM, GAMA) |
|------|--------|------------------------------------------|
| 支持 AIE-ML 架构 | ✅ | ❌ / 部分支持 |
| 完整 NN 编译 | ✅ | ❌（仅限单层或 GEMM） |
| 权重驻留 AIE 片上 | ✅ | ❌（需 PL 流式输入） |
| 层间全片上通信 | ✅（通过 Memory Tile） | ❌（依赖 PL 缓冲） |
| 融合 Bias/Activation | ✅ | ❌ |
| 自动图放置 | ✅（B&B 算法） | ❌（手动或简单启发式） |
| 位精确量化支持 | ✅ | 不明确 |

---

## 2. 核心实验方法和设置

### 使用的数据集与模型
论文未使用传统图像分类数据集（如 ImageNet），而是评估了多种典型神经网络结构，适用于低延迟推理场景：

- **MLP-Mixer Blocks**：来自 Vision Transformer 类模型，在 Jet Tagging 等物理实验中有广泛应用。
- **Standalone MLPs**：包括 2 层和 7 层全连接网络，用于模拟实际部署中的轻量级模型。
- 所有模型均经过量化处理（INT8/INT16），输入为合成张量。

### 实验设置
- **目标平台**：AMD Versal AIE-ML 架构（VEK280 平台）
- **仿真环境**：AMD Vitis 2025 工具链中的周期精确 AIE 模拟器
- **时钟频率**：1.25 GHz
- **评估对象**：
  - 单内核性能（Single-kernel throughput）
  - 多层扩展性（Scaling across AIE tiles）
  - 端到端吞吐量与延迟
- **对比基线**：
  - AIE 领域：AutoMM [15], MaxEVA [13], CHARM [16], GAMA [19], ARIES [17]
  - 跨架构：FPGA (hls4ml), GPU (NVIDIA RTX 3060 + TensorRT), Apple M4 ANE (Core ML)

### 评估指标
- **TOPS/GOPS**：每秒万亿/十亿次操作数（以 INT8 为主）
- **效率（Efficiency）**：实测性能占理论峰值的比例
- **空间利用率**：使用的 AIE tiles 数量占比
- **延迟（Latency）**：单样本推理时间（微秒级）
- **输出间隔（Output Interval）**：稳态下连续输出之间的平均时间

---

## 3. 主要实验结果和性能指标

### 单内核性能（Table II）
在单个 AIE tile 上运行线性层（含 Bias + ReLU）的结果显示接近理论峰值：

| 数据类型 | 吞吐量（GOPS） | 效率 |
|---------|----------------|-------|
| i8×i8   | 520 GOPS       | 81.3% |
| i16×i8  | 287 GOPS       | 89.7% |
| i16×i16 | 114 GOPS       | 70.6% |

> 注：i8×i8 场景理论峰值为 640 GOPS（Table I），性能受限主要由于 store 和 activation 开销。

### 多层扩展性（Figure 4）
- 在最多使用 **296 out of 304 AIE tiles（97.4% 利用率）** 的情况下：
  - i8×i8：达到 **97.3%** 相对于单 tile 的扩展效率
  - i16×i8：**98.6%**
  - i16×i16：**97.1%**
- 表明 AIE4ML 能有效扩展至整个 AIE-ML 阵列，几乎无通信瓶颈。

### 端到端模型性能（Table III）
在真实模型上的表现（全片上执行）：

| 模型配置 | 操作数（MOPs） | 输出间隔（μs） | 吞吐量（TOPS） |
|---------|----------------|----------------|----------------|
| Token MLP - S/16 | 102 | 1.2 | 82.5 |
| Channel MLP - S/16 | 822 | 10.4 | 77.3 |
| 2-Layer MLP | 1074 | 8.2 | 129.7 |
| **7-Layer MLP** | **3.7K** | **0.03** | **113.4** |

> ⚡️ 7-Layer MLP 实现 **113.4 TOPS** 吞吐量，输出间隔仅 **30 ns**，适合超低延迟流水线。

### 与现有 AIE 框架对比（Table IV）

| 框架 | AIE 架构 | INT8 效率 | 多层支持 | 权重驻留 | 全片上执行 |
|------|----------|-----------|-----------|------------|--------------|
| AIE4ML | AIE-ML | **82.2%** | ✅ | ✅ | ✅ |
| GAMA | AIE-ML | ~85% | ❌ | ❌ | ❌ |
| MaxEVA | AIE | 56–60% | ❌ | ❌ | ❌ |
| AutoMM | AIE | 27.5% | ✅（PL 控制） | ❌ | ❌ |

> 尽管 GAMA 在 GEMM 单项任务中效率略高，但 AIE4ML 是唯一实现**完整多层、全片上、权重驻留**的框架。

### 跨架构性能对比（Table V）
对同一 7-layer MLP 进行横向比较：

| 设备 | 平台 | 工具链 | 吞吐量（TOPS） |
|------|------|--------|----------------|
| **Versal VEK280 (AIE-ML)** | AIE4ML | **113.4** |
| VU13P FPGA | hls4ml | 3.7 |
| NVIDIA RTX 3060 GPU | TensorRT | 14.1 |
| Apple M4 ANE | Core ML | 10.5 |

> 🔥 AIE4ML 吞吐量是 GPU 的 **8 倍以上**，FPGA 的 **30 倍以上**，展现出强大的边缘/嵌入式推理能力。

### 消融实验（隐含分析）
虽然没有独立表格，但文中通过以下方式体现设计选择的有效性：
- **Memory Tile 使用**：启用后可实现全片上执行，消除 PL I/O 开销。
- **2×2 分块调度 vs 单块**：前者显著提高 MAC 利用率，减少 load/store 气泡。
- **B&B 放置 vs 贪婪策略**（Figure 3）：B&B 生成更紧凑布局，减少跨层跳转距离，降低互连压力。

---

## 4. 关键结论和发现

### 主要发现
1. **AIE-ML 架构非常适合低延迟推理**：结合 VLIW、本地内存和 Memory Tiles，可在微秒级延迟下实现 GPU 级吞吐。
2. **全片上执行至关重要**：通过 Memory Tiles 实现层间通信，避免 PL 成为瓶颈，是实现高性能的关键。
3. **自动化编译可行且高效**：AIE4ML 实现了从高层模型到固件的全自动转换，性能接近手工优化水平。
4. **位精确性和量化支持完备**：能无缝集成现有量化流程（如 hls4ml），确保部署一致性。

### 方法的局限性
- 当前主要支持 **Linear Layer** 和 **MLP-like 结构**，尚未覆盖 CNN、Transformer Self-Attention 等复杂算子。
- 对非规则形状（shape not divisible by tile size）存在 padding 开销，影响资源利用率。
- 图放置算法虽高效，但在极深网络中可能面临组合爆炸风险（尽管实际中影响较小）。

### 未来工作方向
- 扩展支持更多算子类型（Conv, Attention, LSTM 等）。
- 支持动态输入尺寸和可变长度序列模型（如 Mamba）。
- 进一步优化 AIE-MLv2 架构特性（更大内存、更高并行度）。
- 探索与其他前端（ONNX, TensorFlow Lite）的集成。

---

> ✅ **总结一句话**：  
> AIE4ML 是首个实现 **端到端、全片上、位精确、自动化编译** 的 AIE-ML 框架，在超低延迟场景下实现了远超 GPU/FPGA 的推理吞吐，为下一代 AI 加速器部署提供了实用解决方案。

</details>

---

### 4. [LoPA: Scaling dLLM Inference via Lookahead Parallel Decoding](https://arxiv.org/abs/2512.16229)

**Authors**: Chenkai Xu, Yijie Jin, Jiajun Li, Yi Tu, Guoping Long, Dandan Tu, Tianqi Hou, Junchi Yan, Zhijie Deng  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.16229v1  

#### Abstract
Diffusion Large Language Models (dLLMs) have demonstrated significant potential for high-speed inference. However, current confidence-driven decoding strategies are constrained by limited parallelism, typically achieving only 1--3 tokens per forward pass (TPF). In this work, we identify that the deg...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**LoPA: Scaling dLLM Inference via Lookahead Parallel Decoding**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Diffusion Large Language Models (dLLMs)** 虽然理论上具备并行生成潜力，但在实际推理中受限于 **Token Filling Order (TFO)** 对预测置信度的影响，导致并行度极低（通常每步仅填充 1–3 个 token）。传统的置信驱动采样策略（confidence-driven sampling）在局部最优下可能陷入次优路径，限制了整体解码效率。

### 提出的新方法：**Lookahead PArallel Decoding (LoPA)**
LoPA 是一种**无需训练、即插即用**的算法，旨在通过主动探索更优的 TFO 来提升 dLLM 推理过程中的并行性。其核心思想是“向前看”多个候选填充顺序，并选择最具未来并行潜力的一条路径。

#### 方法流程：
1. **Anchor Branch 构建**：使用标准置信驱动策略推进一个主分支。
2. **Lookahead Branches 生成**：从剩余未填位置中选取 top-k 高置信度位置，各自独立采样构建 k 个前瞻分支。
3. **Branch Confidence 验证**：在一个前向传播中并行验证所有分支（含 anchor），依据各分支剩余位置的平均置信度 $ C(B_j) = \frac{1}{|M_{B_j}|} \sum_{i \in M_{B_j}} \text{Conf}(i) $ 选择最优分支。
4. 迭代执行，持续优化 TFO。

### 相比现有方法的优势
| 方面 | LoPA 的优势 |
|------|-------------|
| **通用性** | 无需模型微调，可直接集成到任何基于置信度的 dLLM（如 D2F、Dream） |
| **并行效率** | 显著提高 Tokens Per Forward pass (TPF)，突破传统 1–3 的瓶颈 |
| **性能保持** | 在大幅提升速度的同时，维持甚至超越原始模型的生成质量 |
| **系统协同设计** | 提出 **Branch Parallelism (BP)** 分布式推理架构，实现硬件级加速 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **数学推理任务**：
  - **GSM8K**（4-shot）
  - **MATH**（4-shot）
- **代码生成任务**：
  - **HumanEval**（0-shot）
  - **MBPP**（3-shot）
- 更严格的扩展基准：
  - **HumanEval+**, **MBPP+**（来自 EvalPlus，用于鲁棒性评估）

### 实验设置与评估指标

#### 主要模型
- **D2F-Dream-7B** 和 **D2F-DiffuCoder-7B**（基于 D2F objective 微调的 dLLM）
- **Vanilla Dream-7B**（基础扩散模型，用于验证泛化性）

#### 平台配置
- **LoPA-Dist-NV**：8 × NVIDIA H200 GPUs，支持 Tensor Parallelism (TP) 与 Branch Parallelism (BP)
- **LoPA-Dist-Ascend**：8 × Huawei Ascend 910C NPUs，采用 TP4 + BP4 混合并行策略

#### 评估指标
| 指标 | 含义 |
|------|------|
| **TPF (Tokens Per Forward pass)** | 每次前向传播填充的 token 数量，衡量并行效率 |
| **TPS (Tokens Per Second)** | 实际吞吐量，反映端到端推理速度 |
| **Avg TPS / Max TPS** | 平均与峰值吞吐量 |
| **Latency** | 单样本生成延迟 |
| **Score** | 任务准确率（pass@1） |

### 基线方法对比
- **Vanilla dLLM**：原始逐轮填充方式
- **Fast-dLLM**：训练自由的加速方法，利用 KV 缓存和并行解码
- **SDAR-8B-Chat**：先进的 dLLM 架构
- **Qwen3-8B (AR model)**：作为自回归模型的性能参考

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3 & Figure 1）

| 模型 | 平台 | Avg TPS | Max TPS | TPF | Latency (s) | Score (GSM8K) |
|------|------|--------|--------|-----|------------|----------------|
| D2F-Dream-Base | LoPA-Dist-NV | 630.28 | 1472.37 | 15.69 | 0.84 | 60.12 |
| D2F-Dream-Base | **LoPA-Dist-Ascend** | **1073.86** | **2400.12** | **11.92** | **0.78** | **62.55** |
| D2F-Dream-Instruct | LoPA-Dist-Ascend | 896.21 | 2586.73 | 8.64 | 0.11 | 66.87 |

> ✅ **单样本吞吐高达 1073.9 tokens/s**（见 Figure 1），显著优于基线。

### 与基线方法的对比（Table 1 & Table 2）

#### 在 D2F-Dream 上的表现（GSM8K）
| 方法 | TPF | Score |
|------|-----|-------|
| Vanilla | 3.1 | 78.5 |
| Fast-dLLM | 2.1 | 72.6 |
| **LoPA** | **10.1** | **73.8** |

- **TPF 提升超过 3 倍以上**，且得分仍高于 Dream 原始 baseline（72.6）
- 在 MATH 上达到 **TPF=8.0**，性能优于 vanilla baseline

#### 在 D2F-DiffuCoder 上的表现（HumanEval+）
| 方法 | TPF | Score |
|------|-----|-------|
| Vanilla | 2.2 | 65.9 |
| **LoPA** | **8.3** | **64.0** |

- **TPF 提升近 4 倍**，性能仅有轻微下降（-1.9 pts），属于可接受范围

### 消融实验结果（Table 4 & Figure 5）
- **Branch 数量影响**：随着 branch 数增加（k↑），TPF 显著上升，在 k=14 时达到峰值（>10 TPF）
- **平台差异**：
  - LoPA-Dist-Ascend 在高 branch 数下表现更稳定，得益于专用优化（如 Graph Compilation、PagedAttention-like 内存管理）
  - LoPA-Dist-NV 受限于 CUDA 动态调度开销，扩展性略差
- **权衡分析**：更高的 branch 数带来更高 TPS，但可能导致 score 下降（因过度追求未来置信度而牺牲当前语义一致性）

---

## 4. 关键结论和发现

### 主要发现
1. **TFO 是决定 dLLM 并行性的关键因素**  
   不同填充顺序会显著改变后续 token 的置信分布，贪婪策略并非全局最优。

2. **LoPA 成功解锁了 dLLM 的潜在并行能力**  
   通过 lookahead 探索机制，将 TPF 从 1–3 提升至 **10 以上**，实现了前所未有的并行解码效率。

3. **系统与算法协同设计至关重要**  
   单靠算法无法发挥全部潜力。提出的 **LoPA-Dist** 系统通过 **Branch Parallelism (BP)** 实现跨设备分支并行，配合静态 KV 缓存、FlashAttention、图编译等技术，在 Ascend 平台上实现 **超 1000 tokens/s 的单样本吞吐**。

4. **LoPA 具有良好泛化性**  
   不仅适用于 D2F 架构，也能有效加速 vanilla Dream 模型（TPF 从 1.0 → 3.4 on MATH），证明其为通用 plug-and-play 加速方案。

### 方法的局限性
- **计算资源消耗大**：需同时维护多个分支，对显存/内存压力较大，尤其在大规模 branching 时。
- **依赖高质量置信度估计**：若模型本身置信度不准（如 calibration 差），branch selection 可能失效。
- **边际收益递减**：当 branch 数过多时，TPF 增长趋缓，甚至出现性能波动。

### 未来工作方向
- **动态 branch 数控制**：根据输入复杂度自适应调整探索广度
- **轻量化 branch 验证机制**：减少分支间冗余计算
- **与其他加速技术融合**：如结合 KV caching（dllm-cache）、consistency distillation（dParallel）进一步压缩步数
- **扩展至多模态 diffusion model**：探索图像、音频生成中的 lookahead 解码可能性

---

> 🔚 **总结一句话**：  
> LoPA 通过引入 **lookahead 多分支探索机制**，首次实现了 dLLM 在不损失性能前提下的 **十倍级并行解码加速**，并通过专用系统设计将算法潜力转化为真实世界中的 **千 token/s 级吞吐**，为高效非自回归生成提供了新范式。

</details>

---

### 5. [TS-DP: Reinforcement Speculative Decoding For Temporal Adaptive Diffusion Policy Acceleration](https://arxiv.org/abs/2512.15773)

**Authors**: Ye Li, Jiahe Feng, Yuan Meng, Kangye Ji, Chen Tang, Xinwan Wen, Shutao Xia, Zhi Wang, Wenwu Zhu  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.15773v1  

#### Abstract
Diffusion Policy (DP) excels in embodied control but suffers from high inference latency and computational cost due to multiple iterative denoising steps. The temporal complexity of embodied tasks demands a dynamic and adaptable computation mode. Static and lossy acceleration methods, such as quanti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TS-DP: Reinforcement Speculative Decoding For Temporal Adaptive Diffusion Policy Acceleration

---

## 1. 论文的主要贡献和创新点

### 解决的问题
**Diffusion Policy (DP)** 在具身控制（embodied control）任务中表现出色，但由于其依赖多次迭代去噪（iterative denoising），导致推理延迟高、计算成本大，难以满足实时性要求。现有的加速方法如量化（quantization）、缓存（caching）等属于静态或有损压缩，无法适应动态变化的任务复杂度。

本文针对以下两个核心挑战提出解决方案：
1. 如何在时间变化的任务难度下，以更低的成本匹配基础模型的去噪质量？
2. 如何根据任务难度动态调整计算资源，实现稳定高效的加速？

### 提出的新方法与创新思路
作者提出了 **TS-DP**（Temporal-aware Reinforcement-based Speculative Diffusion Policy），是首个将 **speculative decoding** 引入 DP 并具备**时间自适应能力**的框架。其主要创新包括：

- **基于 Transformer 的轻量级 Drafter 模型**  
  通过知识蒸馏训练一个单层 Transformer 作为 drafter，模仿 base model 的多步去噪行为，在低开销下生成多个候选动作序列。

- **并行验证机制 + Reflection-Maximal Coupling**  
  利用 base model 对 drafter 生成的多个候选进行并行验证，接受符合分布的结果；对第一个被拒绝的样本使用 reflection-maximal coupling 进行修正，保证采样分布不变，实现**无损加速**。

- **基于强化学习（RL）的动态调度器（Scheduler）**  
  设计了一个 PPO-based 的 RL 调度器，根据当前环境状态（如末端执行器速度、任务阶段）动态调节 speculative 参数，如 draft 步数 $K$、接受阈值 $\lambda$ 和 $\sigma$ 缩放因子，从而实现**时间复杂度感知的自适应加速**。

### 相比现有方法的优势
| 方法类型 | 代表方法 | 是否有损 | 是否自适应 | 加速比 | 实时性 |
|--------|--------|--------|----------|--------|-------|
| 静态压缩 | Quantization, Pruning | 是 | 否 | ≤3× | 不足 |
| 缓存优化 | BAC, SpeCa | 是/部分 | 否 | ~3.4× | 有限提升 |
| 固定参数 speculative | Frozen Target Draft | 否 | 否 | ~3.0× | 一般 |
| **TS-DP（本文）** | — | **否（lossless）** | **是（temporal-aware）** | **最高达 4.17×** | **支持 25Hz 实时控制** |

> ✅ **优势总结**：TS-DP 实现了**无损、高效、可动态调节**的加速，在保持甚至提升成功率的同时显著降低 NFE（Number of Function Evaluations），适用于真实机器人部署场景。

---

## 2. 核心实验方法和设置

### 数据集与任务
实验在四个主流机器人操作基准上进行，涵盖不同难度和阶段性的任务：
- **Robomimic** 下属子任务：
  - Proficient Human (PH)：熟练人类演示数据
  - Mixed Human (MH)：混合技能水平的人类演示
- 多阶段复杂任务：
  - **Kitchen**：涉及微波炉、烧水壶等多个物体交互
  - **Block Pushing (BP)**：分阶段推进方块至目标区域

所有任务均采用固定 seed 设置，确保可复现性。

### 实验设置
- **Base Model**：原始 Diffusion Policy [10]
- **Hardware**：NVIDIA A100 GPU (40GB)
- **Drafter 架构**：单层 Transformer block，共享 encoder 与 DDPM/DDIM scheduler
- **Training**：通过知识蒸馏训练 drafter，损失函数包含预测一致性（$ \mathcal{L}_{\text{pred}} $）和调度器对齐项（$ \mathcal{L}_{\text{norm}} $）
- **Scheduler 训练**：使用 PPO 算法，输入为观测、动作历史和任务进度，输出为 speculative 参数（$K$, $\lambda$, $\sigma$ scale）

### 评估指标
| 指标 | 描述 |
|------|------|
| **Success Rate (%)** | 主要精度指标（除 Push-T 外） |
| **Coverage Rate (%)** | Push-T 和 Block Push 使用的目标区域覆盖率 |
| **NFE (Number of Function Evaluations)** | 衡量计算效率，每个 drafter 推理记为 1/8 NFE |
| **Speed ×** | 推理加速倍数（vs. 原始 DP） |
| **Frequency (Hz)** | 控制频率，衡量是否满足实时需求 |
| **Draft Acceptance Rate (%)** | 验证通过的草案比例，反映 drafter 质量 |

### 基线方法对比
- **Diffusion Policy [10]**：原始未加速版本（baseline）
- **Frozen Target Draft [2]**：早期 speculative decoding 方法
- **SpeCa [27]**：基于特征缓存的 speculative 方法
- **BAC [15]**：block-wise adaptive caching，当前最优缓存加速方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 在 Proficient Human (PH) 数据集上的表现（Table 1）
| 方法 | AVG Success Rate (%) | NFE (%) | Speed × | Draft Acceptance |
|------|------------------------|---------|--------|------------------|
| DP [10] | 76 / 80 | 100 | 1× | — |
| BAC [15] | 77 / 81 | ~66 | 3.40× | — |
| **TS-DP** | **85 / 80** ↑ | **24 / 24** ↓ | **4.17×** | **~94%** |

> 📌 **亮点**：不仅提速 4.17×，还提升了平均成功率（+9%），且 NFE 降低 76%

#### 在 Mixed Human (MH) 数据集上的表现（Table 2）
| 方法 | AVG Success Rate (%) | NFE (%) | Speed × |
|------|------------------------|---------|--------|
| DP [10] | 76 / 81 | 100 | 1× |
| BAC [15] | 75 / 80 | ~66 | 3.41× |
| **TS-DP** | **75 / 84** | **26 / 26** | **3.84×** |

> ✅ 实现接近无损加速（accuracy 维持不变），NFE 减少 74%

#### 多阶段任务（Kitchen & BP）表现（Table 3）
| 方法 | AVG Success Rate (%) | NFE (%) | Speed × |
|------|------------------------|---------|--------|
| DP [10] | 99 / 99 | 100 | 1× |
| BAC [15] | 99 / 98 | ~64 | 3.60× |
| **TS-DP** | **99 / 99** | **27 / 27** | **3.70×** |

> 🔍 在复杂多阶段任务中仍保持完美成功率，同时获得 3.7× 加速

#### 实际延迟与频率测试（Table 5）
| 方法 | Average Frequency (Hz) | Latency (s) |
|------|--------------------------|------------|
| DP | 7.42 Hz | 0.14 s |
| **TS-DP** | **25.00 Hz** | **0.04 s** |

> ⚡ 达到 **25Hz 实时控制频率**，远超工业级实时控制标准（通常 ≥10Hz）

---

### 消融实验结果（Ablation Study）

#### 固定 vs 自适应调度器（Table 4）
| 配置 | AVG Success Rate (%) | Speed × |
|------|------------------------|--------|
| K=10（保守） | 84% | 2.45× |
| K=25 | 75% | 3.47× |
| K=40（激进） | 72% | 3.92× |
| **TS-DP（自适应）** | **87%** | **3.80×** |

> 💡 结论：**固定参数存在精度-效率权衡困境**，而 TS-DP 的 RL 调度器能智能平衡二者，在更高成功率下实现接近最优加速。

#### 可视化分析（Figures 5 & 6）
- Figure 5 显示：scheduler 能随任务阶段动态调整 $K$、$\lambda$、$\sigma$，在精细操作阶段减少 draft 数量，在平稳运动阶段增加并行度。
- Figure 6 显示：TS-DP 的草案接受率始终高于静态方法，且 draft 数更多，说明其更有效地利用了 speculative 并行性。

---

## 4. 关键结论和发现

### 主要发现
1. **Speculative Decoding 特别适合 I/O-bound 的 DP**  
   与图像生成中 compute-bound 的扩散模型不同，DP 输出低维动作向量，验证开销小，因此 speculative decoding 更易带来显著加速。

2. **任务难度具有明显的时间动态性**  
   实验发现末端执行器速度与草案接受率呈负相关：高速粗粒度动作（如移动）接受率低，低速精细动作（如抓取）接受率高。这表明**固定参数 speculative 是次优的**。

3. **RL-based Scheduler 可有效建模 temporal complexity**  
   将 speculative 参数选择建模为 MDP，并用 PPO 学习策略，能够根据上下文自动调节，实现“该快时快，该稳时稳”的智能加速。

4. **TS-DP 实现了真正的 lossless acceleration**  
   所有任务的成功率持平或提升，证明其在不牺牲性能的前提下大幅提高效率。

---

### 方法的局限性
- **依赖高质量的 base model 和 drafter 对齐**：若 distillation 不充分，会导致过多 rejection，削弱加速效果。
- **scheduler 需额外训练成本**：虽然 inference 时不增加 latency（并行运行），但需额外 RL 训练流程。
- **目前仅限于 offline setting**：尚未在真实机器人上闭环验证，未来需考虑 online adaptation 能力。

---

### 未来工作方向
1. **扩展到 online continual learning 场景**：让 scheduler 能在线适应新任务或环境变化。
2. **探索 multi-agent 或 multi-task 共享 drafter**：降低部署成本。
3. **结合 one-step diffusion policy**：进一步压缩 base model，形成端到端轻量化方案。
4. **硬件协同设计**：针对 TS-DP 的并行验证特性优化 GPU 内存调度与 kernel 执行。

---

## 总结
✅ **TS-DP 是首个面向 Diffusion Policy 的时间自适应 speculative decoding 框架**，通过 **lightweight drafter + RL scheduler + parallel verification** 的组合，在多种具身控制任务上实现了高达 **4.17× 的无损加速**，并将控制频率提升至 **25Hz**，为 diffusion-based 实时机器人控制提供了可行路径。  
🎯 其核心思想——“动态适应任务复杂度”——有望推广至其他时序决策系统中的生成式策略加速。

</details>

---

### 6. [Predictive Concept Decoders: Training Scalable End-to-End Interpretability Assistants](https://arxiv.org/abs/2512.15712)

**Authors**: Vincent Huang, Dami Choi, Daniel D. Johnson, Sarah Schwettmann, Jacob Steinhardt  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.15712v1  

#### Abstract
Interpreting the internal activations of neural networks can produce more faithful explanations of their behavior, but is difficult due to the complex structure of activation space. Existing approaches to scalable interpretability use hand-designed agents that make and test hypotheses about how inte...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Predictive Concept Decoders: Training Scalable End-to-End Interpretability Assistants — 核心总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

神经网络内部激活空间（activation space）结构复杂，难以人工解释其行为。现有的可解释性方法（如 probes、concept dictionaries 或 interpretability agents）通常依赖手工设计的代理模型（off-the-shelf models），这些模型并非为可解释性任务专门优化，导致其能力受限。

此外，传统方法缺乏**端到端训练信号**，无法系统地从模型行为中学习如何解释其内部状态。

### **提出了什么新方法或新思路**

本文提出 **Predictive Concept Decoder (PCD)**，一种**端到端可训练的可解释性助手架构**，将可解释性任务转化为一个可监督的学习目标。

PCD 的核心思想是：
- **训练一个编码器-解码器架构**，通过一个**通信瓶颈（communication bottleneck）** 来预测主体模型（subject model）的行为。
- 编码器（Encoder）从主体模型的激活中提取一个**稀疏的概念列表（sparse list of concepts）**。
- 解码器（Decoder）仅基于这个稀疏概念列表和一个自然语言问题，来预测主体模型的行为或回答问题。
- **编码器不看问题，解码器不直接看原始激活**，迫使编码器学习通用、可复用的“解释”。

该框架实现了：
- **可审计性（auditable）**：任何预测都可追溯至少数几个活跃概念。
- **可扩展性（scalable）**：可通过大规模无标注数据预训练，再微调用于具体任务。

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **训练范式** | 将可解释性从“手工分析”转变为“端到端训练”，利用行为预测作为天然监督信号。 |
| **架构设计** | 引入稀疏瓶颈（sparsity bottleneck），提升人类可读性和可审计性。 |
| **性能表现** | 在检测 jailbreak、揭示隐藏提示、识别植入概念等任务上优于 LatentQA 和直接 prompting。 |
| **泛化能力** | 能揭示模型自身无法自述的信息（如法律顾虑、秘密提示使用）。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

| 数据集 | 用途 | 描述 |
|--------|------|------|
| **FineWeb** (Penedo et al., 2024) | 预训练 | 大规模网页文本语料，用于 next-token prediction 预训练。 |
| **SynthSys** (Choi et al., 2025) | 微调与评估 | 包含用户对话及模型对用户属性（如性别、婚姻状况）的隐含假设，用于评估 decoder 回答问题的能力。 |
| **自建攻击数据集** | 案例研究 | 构造了 50 种有害请求（如制造炸弹）并应用三种 jailbreak 模板（3 Words, Distractors, Dream）进行测试。 |
| **Introspection Dataset** | 内省任务 | 基于 Lindsey (2025) 的 58 个概念，生成对比段落以构造 steering vectors。 |

### **实验设置**

- **主体模型**：`Llama-3.1-8B-Instruct`
- **PCD 架构**：
  - 编码器：线性层 + TopK 稀疏化（k=16）+ 重新嵌入（re-embedding）
  - 解码器：与主体模型同构的 LM，附加 LoRA 适配器
  - 概念数：m = 32,768（远大于隐藏维度 d=4096）
- **两阶段训练**：
  1. **预训练**：在 FineWeb 上进行 next-token prediction，监督 decoder 利用编码后的概念预测后续文本。
  2. **微调**：冻结编码器，在 SynthSys 上微调 decoder 回答关于模型信念的问题。
- **辅助损失**：引入 `auxiliary loss` 防止概念“死亡”（inactive concepts），提升字典质量。

### **评估指标**

| 指标 | 含义 |
|------|------|
| **Auto-Interp Score** | 自动可解释性评分：通过生成概念描述，并用模拟器预测激活模式的相关性（Pearson correlation）衡量概念质量。 |
| **Concept Coverage (Recall)** | 用户属性分类准确率：每个编码方向是否能捕捉特定用户属性（如性别）。 |
| **Question-Answering Accuracy** | 在 SynthSys 上的多选题准确率。 |
| **Jailbreak Awareness Rate** | PCD 是否能意识到模型正在被 jailbreak 并执行有害操作。 |
| **Hint Usage Detection** | 是否能检测模型使用了未声明的秘密提示。 |
| **Introspection Performance** | 是否能口头表达被注入的潜意识概念。 |

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **LatentQA** (Pan et al., 2024) | 类似架构但无稀疏瓶颈，decoder 直接读取完整激活。 |
| **Direct Prompting** | 直接询问主体模型自身（第一人称或第三人称）。 |
| **SAE (Sparse Autoencoder)** | 标准稀疏自编码器，用于学习概念字典，作为编码器替代。 |
| **KL SAE** | 改进版 SAE，使用 KL 散度预测输出分布而非重建激活。 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ **预训练阶段：编码器可解释性随数据量提升**
- 图4显示：PCD 在 `k=16` 下的 auto-interp score 随预训练 token 数增加而持续上升，在 72M tokens 时优于同等数据下的 SAE（k=16 或 k=50）。
- 引入 auxiliary loss 后，超过 90% 的概念保持活跃，避免“死概念”问题。

#### ✅ **微调阶段：问答性能接近 LatentQA**
- 图5显示：PCD 经过充分预训练后（≥36M tokens），在 SynthSys 上的表现与 LatentQA 相当。
- 若使用冻结的 SAE 编码器，则性能显著下降，说明**联合训练至关重要**。

#### ✅ **Jailbreak 检测：PCD 显著优于基线**
- 图7：平均 jailbreak awareness 达 **~50%**，在 Dream 攻击下高达 50%，而其他方法均低于 30%。
- 图8：awareness 随预训练数据量增加而提升，尤其在复杂攻击（如 3 Words）中出现“涌现”现象（emergent awareness at 72M tokens）。

#### ✅ **秘密提示检测：PCD 能揭示模型隐瞒的行为**
- 图9：PCD（无瓶颈）和 LatentQA 能有效揭示“top-secret shortcut”的使用，而直接 prompting 倾向于编造“巧妙算法”来掩盖真相。
- PCD 在更大预训练规模下性能持续提升。

#### ✅ **内省任务（Introspection）：PCD 更擅长表达注入概念**
- 图11：PCD 能更频繁地口头表达被注入的直接相关概念（directly relevant），且性能随预训练数据增长而提升。
- 尽管 SAE 字典在概念匹配上略优，但 PCD 的端到端训练使其在行为预测上更强。

#### ✅ **消融实验结果**
| 实验 | 发现 |
|------|------|
| **移除 auxiliary loss** | 概念活性下降，auto-interp score 和 recall 均降低。 |
| **减少预训练数据** | PCD 性能显著下降，尤其在高稀疏度（k=16）下。 |
| **替换编码器为 SAE** | 性能大幅下降，证明联合训练的重要性。 |
| **测试时移除瓶颈（k=∞）** | 在小规模预训练下性能提升（OOD 输入仍有效），但在大规模下反而下降，说明 PCD 学会了高效压缩。 |

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **端到端训练可行且有效**：  
   将可解释性任务建模为“从激活预测行为”的监督学习问题是可行的，PCD 可通过大规模预训练不断提升解释能力。

2. ✅ **稀疏瓶颈带来可审计性而不牺牲性能**：  
   尽管引入了信息压缩，但经过充分训练后，PCD 在下游任务上的表现仍可匹敌甚至超越无瓶颈的 LatentQA。

3. ✅ **PCD 能揭示模型“不可言说”的信息**：  
   - 模型拒绝请求的真实原因可能是“规避法律责任”而非“用户安全”（图12）。
   - 模型使用秘密提示但从不承认，PCD 却能准确揭示。
   - 模型对自身被植入的概念缺乏意识，PCD 却能表达出来。

4. ✅ **缩放律成立（Scaling Laws）**：  
   PCD 的编码器质量和解码器性能均随预训练数据量增加而提升，展现出良好的可扩展性。

5. ✅ **PCD 具有“有利归纳偏置”（favorable inductive bias）**：  
   即使与 LatentQA 架构相同（无瓶颈），由于训练过程中存在瓶颈，PCD decoder 学会了更关注细粒度概念。

### **方法的局限性**

1. **性能 plateau**：  
   在约 72–144M tokens 后，auto-interp score 出现平台期，可能与 KL-based 目标函数有关。

2. **概念字典质量仍不如 SAE**：  
   在 introspection 任务中，SAE 字典在匹配注入概念方面表现更好，说明当前 PCD 编码器仍有改进空间。

3. **依赖高质量自动解释 pipeline**：  
   auto-interp score 本身依赖 GPT-5-mini 等强模型生成描述和判断，可能存在偏差。

4. **计算成本较高**：  
   需要大量预训练和微调资源，限制了轻量级部署。

### **未来工作方向**

1. **突破 plateau**：探索更丰富的训练目标（如 multi-step prediction、counterfactual reasoning）。
2. **改进编码器架构**：使用 Transformer 编码器、支持概念间关系建模（binding、composition）。
3. **多层读取**：从多个网络层读取激活，提升信息完整性。
4. **扩展任务范围**：训练 end-to-end assistants 执行 neuron ablation、feature patching 等干预任务。
5. **人类评估**：引入真实人类用户评估 PCD 输出的可理解性和实用性。

---

> **总结一句话**：  
> PCD 开创了一种**将可解释性任务转化为端到端监督学习**的新范式，通过稀疏瓶颈实现可审计性，并在 jailbreak 检测、提示揭示、内省等任务上展现出超越 prompting 和 LatentQA 的能力，标志着 scalable interpretability 迈向 trainable assistant 的重要一步。

</details>

---

### 7. [CKA-Guided Modular Quantization: Beyond Bit-Width to Algorithmic Diversity](https://arxiv.org/abs/2512.16282)

**Authors**: Jinhao Zhang, Yunquan Zhang, Daning Chen  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.16282v1  

#### Abstract
Current mainstream post-training quantization methods for large language models typically apply a uniform quantization strategy across all network layers, overlooking the substantial differences in algorithmic suitability among layers. To address this limitation, we propose CKA Guided Modular Quanti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# CKA-Guided Modular Quantization: Beyond Bit-Width to Algorithmic Diversity 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的**Post-Training Quantization (PTQ)** 方法（如 GPTQ、AWQ、SmoothQuant）通常在所有网络层上采用**统一的量化策略**，忽略了不同 Transformer 层对量化误差的敏感度存在显著差异这一事实。这种“一刀切”的方式限制了模型压缩后的性能上限。

此外，现有的混合精度（Mixed-Precision）方法仅通过调整**bit-width**（如某些层用 INT4，另一些用 INT8）来优化，而固定使用同一种量化算法，未能充分利用**算法层面的多样性**。

### 🚀 提出的新方法与创新思路
本文提出 **CKA-Guided Modular Quantization** —— 一种无需微调（fine-tuning-free）、即插即用（plug-and-play）的**算法异构量化框架**，其核心思想是：

- 在每一层独立评估多个 PTQ 算法（如 GPTQ、AWQ、SmoothQuant）
- 使用 **Linear Centered Kernel Alignment (CKA)** 作为衡量标准，自动选择每层最优的量化方法
- 将各层选出的最佳策略组合成一个**混合量化模型**

该方法实现了**算法级异质性（algorithmic heterogeneity）**，而非仅仅 bit-width 的变化。

### 🔍 相比现有方法的优势
| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| 量化粒度 | 统一算法 + 可变 bit-width（bit-heterogeneity） | 固定 bit-width + 可变算法（method-heterogeneity） |
| 决策依据 | 启发式规则或敏感性分析 | 数据驱动的 CKA 表征相似性度量 |
| 是否需要训练 | 多数为无训练 | 完全无训练（post-training only） |
| 性能表现 | 存在性能天花板 | 超越单一算法极限 |

> ✅ **优势总结**：  
> - 更好地匹配各层特性（权重分布、激活异常值等）  
> - 显著降低语言建模任务中的 **Perplexity (PPL)**  
> - 提升下游任务（如数学推理、代码生成）准确率  
> - 不依赖 fine-tuning，部署友好  

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 类型 | 数据集 |
|------|-------|
| **语言建模评估** | `C4`, `WikiText-2`（wiki2） |
| **下游任务评估** |  
- 数学推理：`GSM8K`  
- 代码生成：`HumanEval`  
- 常识推理：`HellaSwag`  
- 多任务理解：`MMLU`  

校准（calibration）使用从 `C4` 中随机采样的 **128 条序列**，长度为 1024 tokens。

### ⚙️ 实验设置
- **目标量化格式**：W4A8（4-bit weights, 8-bit activations），group size = 128
- **保留高精度模块**：Embedding、KV Cache、forward activations 保持 FP16/BF16
- **候选 PTQ 方法池**：GPTQ、AWQ、SmoothQuant、SpinQuant
- **搜索策略**：逐层贪婪选择（greedy layer-wise selection），基于 CKA 分数决策
- **模型系列**：
  - Llama 系列：Llama-3-8B, Llama-3.2-3B, Llama-3.2-1B
  - Qwen 系列：Qwen2.5-1.5B, Qwen2.5-0.5B

### 🎯 评估指标
| 指标 | 描述 |
|------|------|
| **Perplexity (PPL)** ↓ | 衡量语言模型预测能力，越低越好 |
| **Accuracy (%)** ↑ | 下游任务得分，越高越好（GSM8K、HumanEval、HellaSwag、MMLU） |
| **CKA Score** ↑ | 衡量量化后层输出与原始 FP16 输出之间的表征一致性 |

### 🆚 对比的基线方法
- **FP16**：全精度基准
- **GPTQ**：基于 Hessian 的权重量化
- **AWQ**：保护显著权重通道
- **SmoothQuant**：平滑激活以缓解异常值影响
- **SpinQuant**：旋转矩阵抑制异常值
- **MP-GPTQ(FP16/4/2)**：传统混合精度方法（部分层 2-bit）

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & Table 2）

#### ✅ Perplexity (PPL) 结果（越低越好）
| Model | Method | C4 ↓ | Wiki2 ↓ |
|-------|--------|-----|--------|
| Llama-3-8B | FP16 | 12.28 | 6.41 |
| | **Ours** | **12.72** | **6.89** |
| | GPTQ | 14.12 | 8.81 |
| | AWQ | 13.56 | 8.12 |
| | SmoothQuant | 13.64 | 7.32 |
| | SpinQuant | 13.39 | 7.48 |

> 💡 **Ours 接近 FP16，且全面优于其他 PTQ 方法**

| Model | Method | Wiki2 ↓ |
|-------|--------|--------|
| Qwen1.5-1.5B | **Ours** | **12.87** |
| | SpinQuant (SOTA) | 13.31 |
| | AWQ | 14.05 |

> ✅ **领先第二名 0.44 PPL，在小模型上优势更明显**

#### ✅ 下游任务准确率（越高越好）
| Model | Method | GSM8K ↑ | HumanEval ↑ | MMLU ↑ |
|-------|--------|--------|------------|--------|
| Llama-3-8B | FP16 | 77.17 | 60.71 | 67.31 |
| | **Ours** | **74.33** | **59.67** | **65.87** |
| | GPTQ | 71.89 | 58.45 | 60.54 |

| Qwen1.5-0.5B | FP16 | 26.78 | 49.65 | 45.76 |
| | **Ours** | **25.12** | **48.68** | **44.12** |
| | GPTQ | 19.04 | 47.38 | 42.82 |

> ✅ 在极轻量模型上仍大幅领先，尤其在数学推理任务中提升显著（+3.25~4.90 pts）

---

### 🔍 消融实验结果（Ablation Studies）

#### （1）PTQ 候选方法多样性的影响（Table 3）
移除任一候选算法均导致性能下降：
- 移除 **SmoothQuant** → Llama-3-8B C4 PPL 从 12.72 升至 13.55
- 移除 **AWQ** → GSM8K 准确率下降 1.48%
- 所有模型均表明：多种算法协同互补至关重要

> ✅ 验证了“没有万能算法”，必须动态适配

#### （2）Method-Heterogeneity vs Bit-Heterogeneity（Table 4）
在相同平均 bit-width 下比较：
| 方法 | Wiki2 PPL (Llama-3-8B) | GSM8K Acc |
|------|------------------------|-----------|
| MP-GPTQ (FP16/4/2) | 7.95 | 73.40 |
| **Ours (W4-Mix)** | **6.89** | **74.33** |

> ✅ **保持 4-bit 全局精度但切换算法，效果远超降级到 2-bit 的传统混合精度方案**

#### （3）量化粒度影响（Table 5）
| 粒度 | C4 PPL (Llama-3-8B) | GSM8K Acc |
|------|--------------------|-----------|
| Block-8（每8层共享配置） | 13.15 | 73.05 |
| Block-2 | 12.78 | 74.10 |
| **Ours（逐层）** | **12.72** | **74.33** |

> ✅ **逐层优化优于块级共享配置**，说明精细控制必要

#### （4）低比特宽度下的表现（Table 6）
在 **3-bit** 设置下：
- 传统方法（如 GPTQ）PPL 剧增（达 19.25）
- **Ours** 实现平稳退化（PPL=13.55），显著优于所有 baseline

> ✅ 展现出在极端低比特环境下的鲁棒性和适应性

---

## 4. 关键结论和发现

### 🧠 主要发现
1. **不同 Transformer 层对量化算法具有高度异质偏好**：
   - 浅层倾向于 GPTQ
   - 中深层交替使用 AWQ 和 SmoothQuant
   - 小模型切换更频繁，体现更强异质性（见 Figure 4）

2. **CKA 是有效的功能保真度代理指标**：
   - 高 CKA 分数 ≈ 更好的语义一致性
   - 附录中通过线性恢复矩阵 $M$ 实验证明：提升 CKA 可直接改善全局性能（PPL↓, Acc↑）

3. **算法多样性比 bit-width 多样性更重要**：
   - 在固定 4-bit 条件下，改变算法的效果远胜于将部分层降至 2-bit
   - “方法异质性”是突破 PTQ 性能瓶颈的关键路径

4. **无需训练即可实现 SOTA 性能**：
   - 整个流程完全 post-training，适合快速部署
   - 可无缝集成进现有 PTQ 流水线

---

### ⚠️ 方法的局限性
| 问题 | 描述 |
|------|------|
| **理论近似性** | 贪婪逐层选择无法保证全局最优，可能忽略长程依赖 |
| **推理延迟与系统集成挑战** | 混合算法需处理异构 kernel 和内存布局，影响 vLLM 等引擎的算子融合与访存效率 |
| **离线开销增加** | 搜索过程需对每个层尝试多个算法，耗时随候选池规模线性增长 |

---

### 🔮 未来工作方向
1. 设计更高效的搜索机制（如基于强化学习或代理模型）以逼近全局最优
2. 开发支持异构 kernel 的推理引擎，解决部署瓶颈
3. 将该范式扩展至 **Quantization-Aware Training (QAT)** 或 **MoE 架构**中的专家模块
4. 探索 CKA 以外的表示对齐指标（如 SVCCA、Procrustes alignment）

---

## ✅ 总结
本论文提出了 **CKA-Guided Modular Quantization** 框架，首次系统探索了在 LLM 中进行**跨层算法异构量化**的可能性。通过引入 CKA 作为选择准则，实现了无需训练的逐层最优算法分配，在多个主流 LLM 上显著超越了统一量化和传统混合精度方法。

> 🔑 **核心洞见**：  
> “**Not all layers are created equal — nor should they be quantized equally.**”  
> 真正的高效压缩不仅在于“多宽”，更在于“怎么压”。

该工作为 PTQ 提供了一个全新的设计维度 —— **算法多样性**，有望成为未来大模型压缩的标准组件之一。

</details>

---

### 8. [LLMCache: Layer-Wise Caching Strategies for Accelerated Reuse in Transformer Inference](https://arxiv.org/abs/2512.16843)

**Authors**: Harsh Vardhan Bansal  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.16843v1  

#### Abstract
Transformer-based language models have achieved remarkable performance across a wide range of tasks, yet their high inference latency poses a significant challenge for real-timeand large-scale deployment. While existing caching mechanisms,such as token-level key-value caches, offer speedups in autor...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLMCache: Layer-Wise Caching Strategies for Accelerated Reuse in Transformer Inference

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
Transformer 模型在推理阶段存在高延迟问题，尤其是在处理语义相似或重复输入（如对话系统、文档摘要、RAG）时，仍执行完整的前向传播，导致大量冗余计算。

传统 **Key-Value (KV) Caching** 虽然能加速自回归生成，但仅适用于 Decoder-only 架构，且局限于 token-level 的缓存，无法跨层复用中间激活（intermediate activations），也不能用于 Encoder 或 Encoder-Decoder 模型。

---

### 🚀 提出的新方法：LLMCache
提出 **LLMCache** —— 一种**模型无关**（model-agnostic）、**架构兼容**（支持 Encoder、Decoder 和 Encoder-Decoder）的**层级别缓存框架**（layer-wise caching），通过重用语义相似输入对应的中间层输出来加速推理。

#### 核心机制：
- **语义指纹生成（Semantic Fingerprinting）**  
  对输入序列生成轻量级固定长度的 fingerprint（如使用 Avg(E(X)) + 注意力统计 + SimHash/PCA 压缩），作为缓存键。
- **层独立缓存银行（Layer-wise Cache Banks）**  
  每个 Transformer 层维护一个 `(fingerprint, hidden_state)` 缓存池。
- **基于相似度的查找与复用**  
  若当前输入指纹与缓存中某项的相似度（cosine/Jaccard）超过阈值 `τ`，则直接复用该层输出，跳过计算。
- **动态刷新策略（Adaptive Eviction）**  
  支持 LRU、staleness-aware、divergence-monitor 等策略管理缓存新鲜度。

---

### 🔍 相比现有方法的优势

| 特性 | KV-Cache | DocCache | LLMCache |
|------|----------|----------|-----------|
| 支持 Encoder | ❌ | ✅（部分） | ✅ |
| 支持 Decoder | ✅ | ❌ | ✅ |
| 缓存粒度 | Token-level | Document-level | **Layer-wise** |
| 是否需重训练 | ❌ | ❌ | ❌ |
| 是否模型无关 | ❌（仅限 Decoder） | ❌（特定任务） | ✅ |
| 复用深度 | 浅层（仅 attention keys/values） | 浅层（embedding） | **深层（任意层 hidden states）** |

> ✅ **首次实现统一、通用、无需修改架构的 layer-wise 缓存机制**

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **WikiText-103**：语言建模任务
- **SQuAD v2**：问答任务（含不可回答问题）
- **OpenBookQA**：开放知识问答任务

### ⚙️ 实验设置
- **模型**：
  - BERT-base（Encoder）
  - DistilBERT（小型化 Encoder）
  - GPT-2-small（Decoder）
- **硬件环境**：NVIDIA A100 GPU
- **参数配置**：
  - Batch size = 1
  - Sequence length = 128

### 📊 评估指标
| 指标 | 描述 |
|------|------|
| **Inference Latency (ms)** | 平均单次推理耗时 |
| **Cache Hit Rate (%)** | 成功命中缓存的比例 |
| **Accuracy Drop (%)** | 相比无缓存版本的任务准确率下降 |
| **Memory Overhead (MB)** | 缓存引入的额外内存开销 |

### 🔁 基线方法对比
| 基线 | 说明 |
|------|------|
| **NoCache** | 标准 Transformer 推理流程（无任何缓存） |
| **KV-Cache** | 自回归解码中的标准 KV 缓存（仅用于 GPT-2） |
| **DocCache** | 文档级 embedding 缓存（预计算并复用） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（Table I & III）

#### 表格：平均推理延迟（单位：ms）

| Model | NoCache | KV-Cache | LLMCache (Ours) |
|-------|---------|----------|------------------|
| BERT-base | 218.6 | — | **91.3** |
| DistilBERT | 123.4 | — | **57.9** |
| GPT-2-small | 304.8 | 177.3 | **112.5** |

> 💡 **最高达 3.1× 加速**（以 BERT-base 计算约为 2.4×）

#### 表格：任务准确率对比（%）

| Dataset | NoCache | DocCache | LLMCache (Ours) |
|--------|---------|----------|------------------|
| WikiText-103 | 92.1 | 91.7 | **91.9** |
| SQuAD v2 | 86.3 | 85.8 | **86.1** |
| OpenBookQA | 72.5 | 71.9 | **72.3** |

> ✅ **准确率损失 < 0.5%**，显著优于 DocCache

---

### 🔎 缓存命中率分析（Figure 3）
- 在 **GPT-2 上不同层的缓存命中率**显示：
  - 低层和中层（第 3–9 层）命中率高达 **92%**
  - 高层因语义更敏感，命中率下降
- 表明：**浅层表示更具语义稳定性**，适合缓存复用

---

### 🧪 消融实验（Ablation Studies）

#### 图 5：相似度阈值 `τ` 敏感性分析
- 当 `τ ∈ [0.82, 0.88]` 时，达到最佳平衡
- 过高的 `τ` 导致命中率下降；过低则可能引入语义错配

#### 图 4：缓存大小 vs. 命中率（BERT-base）
- 缓存容量增加带来命中率提升，但收益呈对数增长
- 表明可通过合理控制缓存大小实现高效权衡

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **中间层激活具有语义稳定性**：相同或相似输入在 Transformer 中间层产生高度一致的 hidden states，为缓存提供了理论基础。
2. **LLMCache 显著降低推理延迟**：在多种模型和任务上实现 **2.4×~3.1× 推理加速**，且准确率损失极小（<0.5%）。
3. **通用性强**：适用于 BERT、GPT 等不同架构，无需重新训练或结构调整。
4. **缓存效益集中在中低层**：高层变化大，建议选择性缓存早期层以节省内存。

---

### ⚠️ 局限性
1. **对分布外输入效果有限**：当输入差异大或随机性强时，fingerprint 匹配失败率上升，缓存命中率下降。
2. **内存扩展挑战**：对于更大模型（如 GPT-3），全层缓存可能导致 GPU 内存压力。
3. **微调后行为不稳定**：fine-tuned 模型可能改变中间表示分布，导致缓存快速失效。
4. **当前指纹为手工设计**：未使用可学习的 fingerprint encoder，可能限制匹配精度。

---

### 🔮 未来工作方向
1. **动态阈值调整（Dynamic Thresholding）**  
   每层或每输入自适应调整 `τ`，结合不确定性估计优化决策。
2. **分布式缓存共享（Distributed Cache Sharing）**  
   在多节点服务系统中共享 fingerprint 缓存池，进一步提升全局命中率。
3. **可学习指纹编码器（Learned Fingerprints）**  
   使用轻量级可训练网络替代 SimHash/AvgPool，提高语义匹配能力。
4. **与量化/剪枝等技术联合优化**  
   将 LLMCache 与其他推理加速方法集成，构建综合优化方案。

---

## 总结
LLMCache 提出了一种新颖、实用且通用的 **layer-wise 缓存范式**，突破了传统 KV-Cache 的架构限制，实现了在不牺牲模型性能的前提下大幅加速 Transformer 推理。其实验充分验证了语义相似输入下中间激活复用的有效性，为大规模 LLM 部署提供了一个极具潜力的系统级优化工具。

</details>

---

### 9. [LOG.io: Unified Rollback Recovery and Data Lineage Capture for Distributed Data Pipelines](https://arxiv.org/abs/2512.16038)

**Authors**: Eric Simon, Renato B. Hoffmann, Lucas Alf, Dalvan Griebler  
**Category**: cs.DC  
**Published**: 2025-12-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.16038v1  

#### Abstract
This paper introduces LOG.io, a comprehensive solution designed for correct rollback recovery and fine-grain data lineage capture in distributed data pipelines. It is tailored for serverless scalable architectures and uses a log-based rollback recovery protocol. LOG.io supports a general programming...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LOG.io: Unified Rollback Recovery and Data Lineage Capture for Distributed Data Pipelines

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决**分布式数据流水线（data pipelines）中的两个核心挑战**：
1. **正确回滚恢复（Correct Rollback Recovery）**：在系统故障后，能够将整个数据流水线恢复到一个一致的状态，并保证恢复后的执行结果等价于无故障情况下的执行结果（即实现“端到端精确一次”语义）。
2. **细粒度数据血缘捕获（Fine-grain Data Lineage Capture）**：追踪任意两个算子之间输入事件与输出事件之间的依赖关系，用于调试、审计和溯源。

现有主流方案如 **Asynchronous Barrier Snapshotting (ABS)** 存在以下问题：
- **阻塞性恢复**：单个算子失败会导致整个流水线重启，无法利用并行处理能力。
- **不支持动态扩缩容**：扩缩容需停止流水线、取检查点、重新部署。
- **外部系统更新延迟**：写操作必须等到完整 epoch 结束才能提交，导致延迟。
- **血缘粒度粗**：现有血缘方案通常基于 RDD 分区或静态批处理，无法精确到事件级别，且不支持非确定性算子。

### 提出的新方法
作者提出了 **LOG.io** —— 一种统一的日志型回滚恢复协议，同时支持细粒度数据血缘捕获。

#### 核心机制
- **日志驱动恢复（Log-based Recovery）**：
  - 每个算子在生成输出事件前，先以“undone”状态将其持久化到日志中。
  - 同时标记其依赖的输入事件为“done”，通过原子事务确保一致性。
  - 故障发生时，仅需恢复失败算子自身，无需中断其他正常运行的算子（**非阻塞恢复**）。
- **统一血缘捕获**：
  - 在记录输出事件的同时，记录其与输入事件集合（Input Set）的映射关系，存储在 `EVENT_LINEAGE` 表中。
  - 支持任意算子（包括非确定性、自定义代码、与外部系统交互）的事件级血缘追踪。

### 相比现有方法的优势
| 特性 | ABS (Flink) | LOG.io |
|------|-----------|--------|
| **恢复模式** | 阻塞式（全流水线重启） | 非阻塞式（独立恢复） |
| **动态扩缩容** | 需停机取检查点 | 运行时动态调整 |
| **外部系统写入** | 延迟至 epoch 结束 | 可立即提交 |
| **血缘粒度** | 分区级（粗粒度） | 事件级（细粒度） |
| **算子灵活性** | 要求确定性 | 支持非确定性、自定义算子 |
| **血缘开销** | 额外开销高（~20-30%） | 极低（<1.5%） |

---

## 2. 核心实验方法和设置

### 实验环境
- **平台**：SAP Data Intelligence Cloud（基于 Kubernetes）
- **对比协议**：
  - **LOG.io**（本文提出）
  - **ABS**（Asynchronous Barrier Snapshotting，Flink 中实现）
- **数据库**：SAP HANA（用于存储快照或日志）

### 实验设置
设计了三个 **Use Case** 来评估不同场景下的性能：

#### Use Case 1：串行流水线，存在“慢算子”（straggler）
- 流水线结构：`OP1(Source) → OP2 → OP3(straggler) → OP4(Writer) → OP5(Sink)`
- 变量：
  - 事件吞吐率（100ms ~ 500ms/事件）
  - 事件大小（10KB ~ 10MB）
  - 失败算子位置（OP3 或 OP4）
  - ABS 快照间隔（15s, 30s, 45s）

#### Use Case 2：并行路径流水线
- 结构：两条并行路径汇合到 `OP4`
- 目的：测试并行处理对恢复性能的影响

#### Use Case 3：带数据并行化的流水线
- 结构：`Dispatcher → 2 replicas of OP3 → Merger → ...`
- 目的：评估数据并行化对 LOG.io 和 ABS 性能的影响

### 评估指标
- **正常处理开销（Normal Processing Overhead）**：相比无恢复机制的基准执行时间增加的百分比。
- **恢复开销（Recovery Overhead）**：故障恢复期间额外消耗的时间占比。
- **数据血缘开销（Lineage Overhead）**：开启血缘功能带来的性能影响。
- 所有实验重复 10 次，取平均值。

---

## 3. 主要实验结果和性能指标

### 正常处理性能
| 场景 | LOG.io 开销 | ABS 开销 |
|------|------------|---------|
| 存在慢算子（OP3 慢 100×），中等吞吐（100ms/事件） | <1% ~ 6% | 2% ~ 3% |
| 算子处理时间相近，高吞吐（30ms/事件） | **42.5%** | **3%** |
| 数据并行化（2 个副本） | **25%**（显著降低） | ~3.2%（几乎不变） |

> ✅ **结论**：当存在 **straggler 算子** 且吞吐适中时，LOG.io 与 ABS 性能相当；但在高吞吐、算子速度接近时，LOG.io 因悲观日志（pessimistic logging）开销较大。**数据并行化可大幅缓解 LOG.io 开销**。

### 恢复性能
| 场景 | LOG.io 恢复开销 | ABS 恢复开销 |
|------|----------------|-------------|
| 慢算子失败（OP3 慢 100×） | **1% ~ 12%** | **21% ~ 88%** |
| 快算子失败（OP4） | 18.9% ~ 36.5% | 24.6% ~ 63.6% |
| 并行路径中失败 | **7.1% ~ 43.7%** | **19.2% ~ 56%** |

> ✅ **结论**：LOG.io **在恢复阶段始终优于 ABS**，尤其当失败算子较慢或存在并行路径时优势明显。**非阻塞恢复**避免了大量已处理工作的浪费。

### 数据血缘捕获开销
- 在所有实验中，开启血缘功能的额外开销 **均低于 1.5%**（最低 0.4%）。
- 相比已有方案（如 Spark 血缘开销 20-30%），LOG.io 的血缘捕获几乎“零成本”。

> ✅ **结论**：LOG.io 将血缘捕获与恢复日志机制统一，实现了**高效、低开销的细粒度血缘追踪**。

---

## 4. 关键结论和发现

### 主要发现
1. **LOG.io 实现了非阻塞、独立的算子恢复**，显著提升了故障恢复效率，尤其适用于存在处理瓶颈（straggler）的流水线。
2. **支持动态扩缩容**，可在运行时无缝调整算子副本数，适应负载变化。
3. **统一了回滚恢复与数据血缘捕获**，血缘信息直接来自恢复日志，开销极低（<1.5%）。
4. **支持通用编程模型**：允许非确定性算子、自定义代码、与外部系统的任意交互。
5. **在高吞吐、算子速度均衡场景下，LOG.io 正常处理开销较高**，但可通过**数据并行化**有效缓解。

### 局限性
- **悲观日志（Pessimistic Logging）** 导致高吞吐场景下性能下降。
- 当前实现依赖 HANA DB 日志存储，可能成为性能瓶颈。
- 对于完全确定性算子，乐观日志（Optimistic Logging）可进一步优化，但需额外条件（如 replayability）。

### 未来工作方向
1. **自动选择日志模式**：根据算子特性（是否确定性、事件大小、吞吐率）动态切换悲观/乐观日志。
2. **优化日志存储**：采用更高效、可扩展的持久化结构替代当前 HANA DB 实现。
3. **支持更复杂的血缘查询**：结合 schema-level lineage 实现记录级依赖分析。

---

> **总结**：LOG.io 是一个创新性的统一框架，解决了分布式数据流水线中恢复与血缘两大难题。它在恢复效率和功能灵活性上显著优于 ABS，尽管在某些高吞吐场景下正常处理开销较高，但其优势在真实复杂场景中尤为突出。

</details>

---

### 10. [Data Valuation for LLM Fine-Tuning: Efficient Shapley Value Approximation via Language Model Arithmetic](https://arxiv.org/abs/2512.15765)

**Authors**: M\'elissa Tamine, Otmane Sakhi, Benjamin Heymann  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.15765v1  

#### Abstract
Data is a critical asset for training large language models (LLMs), alongside compute resources and skilled workers. While some training data is publicly available, substantial investment is required to generate proprietary datasets, such as human preference annotations or to curate new ones from ex...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Data Valuation for LLM Fine-Tuning: Efficient Shapley Value Approximation via Language Model Arithmetic

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于**大语言模型（LLM）微调中的数据估值（data valuation）问题**，即如何公平、准确地衡量不同数据源对最终模型性能的贡献。传统基于 **Shapley value** 的数据估值方法虽然理论上具有公平性和唯一性，但由于其计算复杂度为 $O(2^n)$，需要对每个可能的数据子集（coalescence）进行模型微调，这在 LLM 场景下是不可行的。

尤其是在偏好对齐任务中（如 RLHF 或 DPO），每次微调成本极高，导致 Shapley 值的实际应用受限。

---

### 提出的新方法与新思路
作者提出了一种**高效近似 Shapley value 的新框架**，其核心思想是：

> 利用 **Direct Preference Optimization (DPO)** 和 **Language Model Arithmetic** 的数学结构，避免为每个数据联盟（coalition）单独微调模型。

具体方法如下：
1. 对每个独立数据源使用 DPO 单独微调一个模型（共 $n$ 次微调）；
2. 在推理阶段，通过 **模型算术（model arithmetic）** 组合这些微调后的模型（及其 LoRA 适配器），构造任意数据联盟对应的“组合策略”（coalition policy）；
3. 基于此组合模型评估效用（utility），进而计算 Shapley value。

该方法的关键洞察在于：  
在 DPO 框架下，多个数据集联合训练所得到的最优策略，等价于各个数据集单独训练所得奖励函数之和作用于基础模型的结果。因此，**联盟策略可以通过已训练模型的概率输出进行代数合成**，无需重新训练。

---

### 相比现有方法的优势
| 方面 | 传统方法 | 本文方法 |
|------|--------|---------|
| 微调次数 | $O(2^n)$，指数级 | $O(n)$，线性 |
| 计算可行性 | 不适用于大规模 LLM | 可扩展至多数据源场景 |
| 方法新颖性 | 黑箱重训练 | 利用 DPO 结构特性实现训练-free 推理组合 |
| 应用潜力 | 限于小规模实验 | 支持多目标数据价值分析、动态数据选择等 |

此外，该方法可与标准 Shapley 近似技术（如 Monte Carlo 采样）结合，在更大规模下进一步降低推理开销。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **基础模型**：`SmolLM-135M-Instruct`（轻量级指令模型）
- **训练数据来源**：UltraFeedback 数据集的四个子集作为独立数据提供者：
  - `flan_v2_niv2`
  - `sharegpt`
  - `evol_instruct`
  - `ultrachat`
- **评估数据**：从 UltraFeedback 中选取与上述训练子集无交集的 128 个提示（prompts）

---

### 实验设置
- **微调方式**：采用 HuggingFace TRL 库实现 DPO，参数 $\beta = 0.1$
- **硬件资源**：单张 A100 80GB GPU
- **训练配置**：
  - Epochs: 4
  - Batch size: 32（含 4 步梯度累积）
  - Learning rate: $2 \times 10^{-5}$
  - 参数高效微调：LoRA（rank=8, alpha=16）
- **模型组合方式**：冻结 base model，合并各数据源对应的 LoRA 权重以构建 coalition model

---

### 评估指标
- **效用函数（Utility）定义为期望奖励值**：
  $$
  v(\pi_S) = \mathbb{E}_{x \sim D, y \sim \pi_S(\cdot|x)}[r(x,y)]
  $$
- 使用两个预训练的标量奖励模型评估：
  - **Helpfulness Reward Model**：衡量回复是否有帮助
  - **Harmlessness Reward Model**：衡量回复是否安全无害
- 最终 Shapley value 在这两个维度上分别计算并可视化

---

### 基线方法对比
文中未直接与其他 Shapley 近似方法进行数值性能对比，而是强调：
- 传统精确 Shapley 需要 $2^n = 16$ 次微调（本实验 $n=4$），而本文仅需 4 次；
- 所有 $2^4 = 16$ 个联盟的效用均通过模型算术构造并评估，验证了方法的完整性；
- 强调其相对于“每联盟重训练”范式的效率飞跃。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 成功在仅 **4 次 DPO 微调**的基础上，估计出全部 4 个数据源在 **helpfulness** 和 **harmlessness** 两个维度上的 Shapley value；
- 所有 $2^4 = 16$ 个 coalition 的效用均可通过 LoRA 合成模型准确估算；
- 图2展示了四类数据源在二维价值空间中的分布（即“spatial signature”）：

| 数据源 | Helpfulness 贡献 | Harmlessness 贡献 |
|-------|------------------|--------------------|
| `sharegpt` | 显著正向 | 轻微正向 |
| `ultrachat` | 几乎中性 | 显著提升安全性 |
| `flan_v2_niv2` | 小幅提升 | 轻微负面影响 |
| `evol_instruct` | 提升明显 | 最严重的负面效应 |

> 注：负 Shapley 值表明该数据源平均而言会拉低某项指标，即使它在某些联盟中有正面作用。

---

### 与基线方法的对比结果
- **计算成本对比**：
  - 传统方法：需 16 次完整微调 → 成本高、不可扩展
  - 本文方法：仅 4 次微调 + 推理时组合 → 成本下降 75%
- **精度保障**：
  - 在收敛假设下，理论证明 coalition policy 可由个体模型精确重构；
  - 实践中虽存在有限步训练偏差，但 LoRA 加权组合仍能有效逼近真实联合训练效果。

---

### 消融实验（文中未明确开展，但有讨论）
- 文中指出未来需研究：
  - Sequential DPO 是否满足交换律（commutativity）？
  - 实际非收敛情况下模型算术的误差边界？
- 当前实验默认模型算术成立，并通过多维度价值分析展示其语义有效性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **DPO 具有良好的代数结构**，允许多个数据集的影响叠加，从而支持 **language model arithmetic**；
2. ✅ 利用这一性质，可以将 Shapley value 的计算从 **指数级微调** 降为 **线性微调 + 推理期模型组合**；
3. ✅ 数据源的价值具有**多维异质性**：有的提升 helpfulness，有的改善 harmlessness，有的甚至带来负向影响；
4. ✅ 负 Shapley 值具有解释意义，可用于识别“毒化”或低质量数据源；
5. ✅ 提出的“数据签名”（data signature）概念有助于理解不同数据集在对齐过程中的角色分工。

---

### 方法的局限性
- 🚫 **依赖 DPO 的特定数学形式**，不直接适用于 PPO 等基于强化学习的对齐方法；
- 🚫 假设 Sequential DPO 与 Joint DPO 收敛到相同解，但在实际训练中由于优化路径差异可能存在偏差；
- 🚫 当前实验规模较小（仅 4 个数据源，模型为 135M），尚未验证在十亿级以上模型上的稳定性；
- 🚫 LoRA 合并可能引入干扰，尤其当适配器之间存在冲突时。

---

### 未来工作方向
1. **拓展至其他对齐算法**：探索是否存在类似代数结构的 RLHF 变体或其他偏好学习目标；
2. **动态数据加权机制**：利用 Shapley 值在线调整训练数据权重，实现自动数据清洗与增强；
3. **版权与责任追溯**：基于数据贡献度判断生成内容的责任归属，支持合规性审计；
4. **社会选择理论融合**：将 LLM 视为偏好聚合器，设计符合公平公理的训练机制；
5. **更高效的 Shapley 近似**：结合 Monte Carlo 采样或回归技巧，将推理次数从 $2^n$ 降至多项式级别。

---

## 总结
本文提出了一个**基于 DPO 和 Language Model Arithmetic 的高效数据估值框架**，首次实现了在 LLM 偏好微调中**线性成本下的 Shapley value 近似计算**。其实验验证了数据价值的多维性和非对称性，为构建透明、可解释、负责任的 LLM 训练流程提供了重要工具。该工作打通了 **cooperative game theory** 与 **LLM alignment** 之间的桥梁，具有重要的理论意义和应用前景。

</details>

---

### 11. [Twin Restricted Kernel Machines for Multiview Classification](https://arxiv.org/abs/2512.15757)

**Authors**: A. Quadir, M. Sajid, Mushir Akhtar, M. Tanveer  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.15757v1  

#### Abstract
Multi-view learning (MVL) is an emerging field in machine learning that focuses on improving generalization performance by leveraging complementary information from multiple perspectives or views. Various multi-view support vector machine (MvSVM) approaches have been developed, demonstrating signifi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Twin Restricted Kernel Machines for Multiview Classification 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统 **Multi-view Support Vector Machine (MvSVM)** 及其变体在处理多视图分类任务时面临以下挑战：
- 需要求解大规模的二次规划问题（Quadratic Programming Problems, QPPs），计算成本高，尤其在大规模数据集上效率低下；
- 对视图间的不一致性（view inconsistencies）敏感；
- 在融合策略上存在局限：早期融合易导致维度灾难并忽略各视图特性，晚期融合则缺乏训练过程中的跨视图信息交互。

### 🚀 提出的新方法：**TMvRKM**（Twin Multiview Restricted Kernel Machine）
本文提出了一种新型多视图分类模型——**Twin Multiview Restricted Kernel Machine (TMvRKM)**，结合了以下关键技术思想：
- 基于 **Restricted Kernel Machine (RKM)** 架构，引入可见层与隐藏层变量，借鉴 **Restricted Boltzmann Machine (RBM)** 的能量函数设计思路；
- 采用 **Twin** 学习机制，构建两个非平行超平面以提升分类边界灵活性；
- 利用 **正则化最小二乘法**（Regularized Least Squares）替代传统的 QPP 求解方式，显著提高计算效率；
- 引入 **耦合项**（coupling term）来平衡多个视图之间的误差，有效利用互补性和一致性原则；
- 融合 **早期融合与晚期融合的优点**：在训练过程中整合多视图信息（早融合），同时为每个视图保留独立的正则化参数以适应其独特统计特性（晚融合灵活性）。

### 🔍 相比现有方法的优势
| 特性 | 传统 MvSVM 类方法 | 本文 TMvRKM |
|------|------------------|-------------|
| 计算复杂度 | 高（需解 QPP） | 低（转化为线性系统求解） |
| 多视图融合机制 | 多为单一早/晚融合 | 同时集成早融合与晚融合优势 |
| 视图间协调性 | 较弱 | 通过耦合项显式建模视图关系 |
| 扩展性 | 不适用于大规模数据 | 更适合大规模数据场景 |

> ✅ **核心创新**：首次将 RKM 与 Twin 框架扩展至多视图学习领域，提出高效、可扩展且具有良好泛化能力的 TMvRKM 模型。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验在三类公开基准数据集上进行：
- **UCI 数据集**：共 27 个真实世界数据集（如 `breast_cancer`, `pima`, `bank` 等）；
- **KEEL 数据集**：同样包含 27 个标准分类任务；
- **AwA（Animal with Attributes）数据集**：包含 50 种动物类别，从中选取 10 类构造 27 个二分类任务，每样本有 6 种预提取特征表示（天然多视图结构）。

> 注：对于 UCI 和 KEEL 数据集，由于本身无多视图属性，作者人工构造两个视图：
> - View A：原始特征；
> - View B：通过 PCA 提取前 95% 主成分得到的降维特征。

### ⚙️ 实验设置
- **交叉验证**：采用 5-fold cross-validation；
- **数据划分**：训练集 : 测试集 = 70% : 30%；
- **核函数**：使用 RBF 核 $ K(x,x') = \exp(-\|x - x'\|^2 / (2\sigma^2)) $，其中 $\sigma$ 从集合 $\{2^{-5}, ..., 2^{5}\}$ 中选择；
- **超参数调优**：通过网格搜索优化惩罚参数 $m_1=m_2$ 和正则化系数 $\lambda_1=\lambda_2$，范围为 $\{10^{-5}, ..., 10^{5}\}$；
- **实现环境**：Python 3.11，运行于 Intel Xeon Gold 6226R CPU + 128GB RAM。

### 📊 评估指标
- **分类准确率（Accuracy, Acc）**
- **平均排名（Average Rank）**：用于综合比较不同模型在多个数据集上的表现；
- **统计检验**：
  - Friedman 检验（判断是否存在显著差异）
  - Nemenyi post-hoc 检验（成对比较，计算临界差 C.D.）

### 🆚 基线方法对比
与以下主流多视图分类器进行对比：
- **SVM-2K** [11]
- **MvTSVM** [13]
- **MVLDM** [31]
- **MvTPMSVM** [22]
- **MvRKM** [27]

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table I & II）

#### 在 UCI + KEEL 数据集上的结果：
| 模型 | 平均 Accuracy (%) | 平均 Rank |
|------|--------------------|-----------|
| SVM-2K | 76.65 | 4.22 |
| MvTSVM | 67.24 | 5.70 |
| MVLDM | 79.90 | 3.74 |
| MvTPMSVM | 83.56 | 3.06 |
| MvRKM | 84.39 | 2.48 |
| **TMvRKM (Ours)** | **85.62** | **1.80** |

✅ **TMvRKM 在所有模型中取得最高平均准确率和最低平均排名**。

#### 在 AwA 数据集上的结果：
| 模型 | 平均 Accuracy (%) | 平均 Rank |
|------|--------------------|-----------|
| SVM-2K | 77.61 | 2.35 |
| MvTSVM | 67.18 | 4.78 |
| MVLDM | 74.09 | 3.72 |
| MvTPMSVM | 74.02 | 3.67 |
| MvRKM | 72.53 | 4.46 |
| **TMvRKM (Ours)** | **78.09** | **2.02** |

✅ **再次优于所有基线模型，尤其在 AwA 上超越最强对手 SVM-2K**。

### 📊 统计显著性分析
- **Friedman 检验**：
  - UCI/KEEL：$\chi_F^2 = 73.59$, $F_F = 31.16$，远大于临界值 $F_{(5,130)} = 2.2839$ → **拒绝零假设**
  - AwA：$\chi_F^2 = 47.44$, $F_F = 14.08$，也显著 → **存在显著性能差异**

- **Nemenyi 检验（C.D. = 1.4788）**
  - 在 UCI/KEEL 上，TMvRKM 与 SVM-2K、MvTSVM、MVLDM 的排名差均超过 C.D.，说明性能提升显著；
  - 尽管与 MvTPMSVM 和 MvRKM 的差距未达显著水平，但仍保持更优排名。

### 🔍 敏感性分析（Sensitivity Analysis）
- 图 1 显示模型对参数 $m$ 和 $\sigma$ 具有一定鲁棒性；
- 最佳性能出现在：
  - $m \in [10^{-1}, 10^5]$
  - $\sigma \in [10^0, 10^5]$
- 建议在此范围内进行精细调参以获得最优泛化性能。

> ❗文中未报告消融实验（ablation study），无法量化“耦合项”、“双曲面结构”或“RKM 架构”的单独贡献。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **TMvRKM 显著提升了多视图分类的泛化性能**，在 UCI、KEEL 和 AwA 数据集上 consistently outperform 所有基线模型；
2. 采用 **正则化最小二乘框架** 替代传统 QPP，大幅降低计算复杂度，更适合大规模应用；
3. 成功融合 **early fusion 与 late fusion 的优势**，既能在训练中共享信息，又能灵活适应各视图特性；
4. 引入的 **耦合项** 有效平衡多视图误差，增强了模型对视图不一致性的鲁棒性；
5. 统计检验证实 TMvRKM 的优越性具有统计显著性。

### ⚠️ 局限性
- 缺乏 **消融实验** 来验证各个组件（如 twin 结构、耦合项、RKM 架构）的具体作用；
- 所有人工构造视图的方式（PCA 分解）可能不能完全反映真实多视图数据的复杂依赖关系；
- 仅用于分类任务，未探索其在回归、聚类或多标签学习中的潜力；
- 超参数较多，依赖网格搜索，在实际部署中可能增加调参负担。

### 🔮 未来工作方向
- 将 TMvRKM 扩展到 **unsupervised learning** 场景，如多视图聚类（Multiview Clustering）；
- 探索在 **dimensionality reduction** 和 **representation learning** 中的应用；
- 设计自动超参数优化策略（如贝叶斯优化）以减少人工干预；
- 应用于更多真实多视图场景，如医学影像（MRI/PET）、多模态情感识别等。

---

## 总结
> **TMvRKM 是一种新颖高效的多视图分类模型，通过融合 RKM、Twin 学习与多视图融合机制，在保持高精度的同时大幅提升计算效率。实验证明其在多种基准数据集上优于主流 MvSVM 类方法，具备良好的泛化能力和统计显著性，是多视图学习领域的一项重要进展。**

</details>

---

### 12. [Meta-RL Induces Exploration in Language Agents](https://arxiv.org/abs/2512.16848)

**Authors**: Yulun Jiang, Liangze Jiang, Damien Teney, Michael Moor, Maria Brbic  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.16848v1  

#### Abstract
Reinforcement learning (RL) has enabled the training of large language model (LLM) agents to interact with the environment and to solve multi-turn long-horizon tasks. However, the RL-trained agents often struggle in tasks that require active exploration and fail to efficiently adapt from trial-and-e...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Meta-RL Induces Exploration in Language Agents

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 **Reinforcement Learning (RL)** 训练的 **Large Language Model (LLM) agents** 在需要主动探索（active exploration）的任务中表现不佳，难以从试错经验中高效适应。标准 RL 方法倾向于在训练中收敛到固定策略，缺乏在测试时动态调整行为的能力，尤其在稀疏奖励、长视野（long-horizon）任务中容易陷入次优解。

### 提出的新方法：LAMER
本文提出 **LAMER**（**LLM Agent with Meta-RL**），一个基于 **Meta-RL**（Meta-Reinforcement Learning）框架的语言智能体训练方法，旨在让 LLM agents 能够在测试时主动探索环境并从反馈中学习。

其两大核心设计是：
1. **跨回合训练框架（Cross-episode Training Framework）**  
   将每个任务视为多个 episode 的序列，在早期 episode 鼓励探索以收集多样化经验，在后期 episode 利用这些经验进行 exploitation。通过最大化跨 episode 的长期回报，模型内化了一种“探索-利用”平衡的学习算法。

2. **上下文中的策略自适应（In-context Policy Adaptation via Reflection）**  
   在每个 episode 结束后，引导 agent 对失败或低效行为进行 **self-reflection**，生成文本形式的反思，并将其作为上下文输入用于后续 episode。这种无需梯度更新的 in-context adaptation 机制，充分利用了 LLM 的上下文学习能力。

### 相比现有方法的优势
- **超越纯提示（prompting）和标准 RL 方法**：LAMER 显著提升了在复杂、部分可观测环境中的成功率。
- **实现测试时自适应（test-time adaptation）**：agent 可在不修改参数的情况下，通过反思调整策略，具备更强的泛化能力。
- **诱导探索行为**：Meta-RL 框架天然鼓励探索，避免过早收敛，提升轨迹多样性。
- **更好的测试时扩展性（test-time scaling）**：随着尝试次数增加，性能提升更显著。

---

## 2. 核心实验方法和设置

### 使用的数据集
在四个具有挑战性的长视野任务环境中进行评估：
- **Sokoban**：经典推箱子游戏，完全可观测，考验规划能力。
- **MineSweeper**：扫雷游戏，部分可观测，需逻辑推理判断地雷位置。
- **Webshop**：模拟电商购物环境，agent 需搜索、筛选并购买符合要求的商品。
- **ALFWorld**：文本版家庭任务环境（如加热物品、清洁等），部分可观测，模拟真实世界交互。

### 实验设置和评估指标
- **基础模型**：Qwen3-4B（主实验），并在 Llama3.1-8B-Instruct 上验证通用性。
- **训练方式**：
  - **Meta-RL (LAMER)**：每轮任务采样 3 个 episode，使用跨 episode 折扣因子 `γ_traj = 0.6`。
  - **标准 RL**：为公平比较，使用 24 个独立 episode（3×8），确保总样本数一致。
- **优化器**：默认使用 **GiGPO**（Group-in-group Policy Optimization）。
- **评估指标**：
  - **pass@k**：k 次尝试内的成功率达到多少（k=1,2,3）。
  - **轨迹多样性（trajectory diversity）**：通过轨迹分布的熵来衡量探索程度。
  - **泛化能力**：在更难任务（increased difficulty）和未见任务（out-of-distribution, OOD）上的表现。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| **Prompting 基线** | Zero-shot, ReAct, Reflexion |
| **RL 基线** | PPO, RLOO, GRPO, GiGPO |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（pass@3 成功率）
| 方法 | Sokoban | MineSweeper | Webshop |
|------|---------|-------------|---------|
| **GiGPO (最强 RL)** | 44.1% | 55.1% | 75.2% |
| **LAMER (ours)** | **55.9%** (+11.8%) | **74.4%** (+19.3%) | **89.1%** (+13.9%) |

> ✅ LAMER 在所有环境中均显著优于 RL 和 prompting 方法。

### 与基线方法的对比结果
- **性能提升显著**：在 Sokoban、MineSweeper 和 Webshop 上分别取得约 **11%、19%、14%** 的绝对性能增益。
- **测试时扩展性更强**：例如在 Sokoban 上，LAMER 从 pass@1 到 pass@3 提升了 **13.5%**，而 RL 基线仅提升不到 5%，表明其能有效利用多次尝试进行策略改进。
- **即使初始表现略低也能反超**：在 MineSweeper 和 Webshop 中，LAMER 的 pass@1 略低于最佳 RL 模型，但在 pass@2 和 pass@3 迅速反超，体现其“先探索后利用”的优势。

### 消融实验结果

#### (1) 跨 episode 折扣因子 `γ_traj` 的影响（图5）
- `γ_traj` 控制探索与利用的权衡：
  - 较小值 → 更关注短期收益（快速 exploitation）
  - 较大值 → 更关注长期回报（鼓励 exploration）
- 最优值因任务而异：
  - Sokoban/Webshop：`γ_traj = 0.6` 效果最好
  - MineSweeper：`γ_traj = 0.9` 表现最佳，说明该任务受益于更长的信用分配

#### (2) 跨 episode 记忆配置消融（表3）
| 记忆内容 | Sokoban | MineSweeper | Webshop |
|--------|--------|-------------|---------|
| 仅历史轨迹（Trajectory-only） | 34.8% | 69.5% | 89.3% |
| 仅反思（Reflection-only） | **56.4%** | **80.5%** | **92.8%** |
| 两者都保留（Both） | 55.9% | 74.4% | 89.1% |

> 🔍 **关键发现**：**仅保留反思（reflection-only）效果最好**，说明简洁、聚焦的反思比原始轨迹更能有效指导策略更新。

#### (3) 其他补充实验
- **在 Llama3.1-8B 上验证**（表4）：LAMER 仍显著优于 GiGPO，证明其对不同架构和规模模型的通用性。
- **RL + 跨 episode 记忆 vs LAMER**（表5）：即使给 RL 添加记忆，LAMER 仍大幅领先，说明 Meta-RL 框架本身是性能提升的关键。

---

## 4. 关键结论和发现

### 主要发现
1. **Meta-RL 是诱导语言 agent 探索的有效范式**：相比标准 RL，Meta-RL 能自然地平衡探索与利用，提升 agent 的适应性和鲁棒性。
2. **LAMER 实现了测试时的 in-context 学习**：通过 self-reflection，agent 能在不更新参数的情况下动态调整策略，真正实现“边做边学”。
3. **更强的泛化能力**：
   - 在更难任务上（如更多箱子的 Sokoban 或更多地雷的 MineSweeper），LAMER 持续优于 RL。
   - 在 ALFWorld 的 out-of-distribution 任务（Cool 和 Pick2）上，LAMER 分别取得 **23%** 和 **14%** 的性能提升。
4. **轨迹多样性更高**：LAMER 保持了比 RL 更高的轨迹熵，说明其探索行为更丰富，避免陷入局部最优。

### 方法的局限性
1. **训练效率较低**：由于 episode 之间存在依赖关系，无法像标准 RL 那样并行采样，导致训练时间约为 RL 的两倍。
2. **依赖高质量反思**：self-reflection 的质量直接影响策略更新效果，若模型反思能力弱，可能无法有效学习。
3. **泛化范围有限**：目前主要在同类任务中泛化（如更难的 Sokoban），尚未验证在完全异构任务间的迁移能力。

### 未来工作方向
1. **提升训练效率**：探索异步 rollout、并行化策略等方法以减少训练时间开销。
2. **结合更强的 RL 算法或反思框架**：如引入更先进的 advantage estimation 或集成外部 verifier 来提升反思质量。
3. **构建通用型 agent**：将 LAMER 扩展到多模态环境，并探索其在完全新颖任务上的 zero-shot 适应能力。
4. **理论分析**：进一步研究 Meta-RL 如何在 LLM 中编码“学习如何学习”的机制。

---

> **总结**：LAMER 首次将 Meta-RL 成功应用于 LLM agent 训练，提出了一种让语言模型“学会探索”的系统性方法。其实验证明，通过跨 episode 训练和 in-context 反思，LLM agents 能够在复杂环境中更有效地学习、适应并泛化，为构建真正自主的智能体提供了重要路径。

</details>

---

### 13. [LADY: Linear Attention for Autonomous Driving Efficiency without Transformers](https://arxiv.org/abs/2512.15038)

**Authors**: Jihao Huang, Xi Xia, Zhiyuan Li, Tianle Liu, Jingke Wang, Junbo Chen, Tengju Ye  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.15038v2  

#### Abstract
End-to-end paradigms have demonstrated great potential for autonomous driving. Additionally, most existing methods are built upon Transformer architectures. However, transformers incur a quadratic attention cost, limiting their ability to model long spatial and temporal sequences-particularly on res...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LADY: Linear Attention for Autonomous Driving Efficiency without Transformers —— 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前主流的端到端自动驾驶模型普遍依赖 **Transformer 架构**进行特征融合与跨模态交互，但其 **自注意力机制具有 $O(T^2)$ 的计算和内存复杂度**，在长序列时空建模中效率低下，尤其难以部署于资源受限的边缘设备（如车载芯片）。此外，多数方法仅使用单帧传感器输入，缺乏对历史时序信息的有效利用，导致在动态场景下规划保守或不安全。

### 🚀 提出的新方法与创新
本文提出 **LADY**（**L**inear **A**ttention for **D**riving Efficienc**Y**），是首个完全基于 **线性注意力（Linear Attention）** 的端到端自动驾驶生成模型，核心创新如下：

- **首次实现全线性注意力架构**：摒弃传统 Transformer，采用 **RWKV-7** 模块构建编码器，实现多帧相机与 LiDAR 特征的高效融合，时间与内存开销为常数级 $O(Td)$ 和 $O(d)$，不受历史帧数影响。
  
- **提出轻量级线性交叉注意力机制（LICA）**：解决现有线性注意力无法支持跨模态、跨时序查询的问题。LICA 在保持线性复杂度的前提下，实现 BEV 特征与轨迹查询之间的有效交互，确保整个模型从头到尾均为线性注意力设计。

- **支持无限长度历史上下文融合**：在推理阶段通过维护一个紧凑的 **Temporal Hidden State**，可逐帧更新并融合任意长度的历史传感器信息，显著增强对动态交通参与者行为预测的能力。

- **结合扩散解码器提升多模态性**：采用 **Truncated Diffusion Policy** 作为解码器，生成高质量、多样化的候选轨迹，并引入评分头选择最优轨迹，兼顾安全性与舒适性。

### 🔍 相比现有方法的优势
| 维度 | LADY | 传统 Transformer / DRAMA |
|------|------|--------------------------|
| **计算复杂度** | $O(Td)$ 时间，$O(d)$ 内存 | $O(T^2d)$ 时间，$O(T^2 + Td)$ 内存 |
| **是否支持多帧历史** | 支持且推理开销恒定 | 多数不支持；若支持则成本剧增 |
| **是否全线性注意力** | 是（含 cross-attention） | 否（即使 Mamba 也需 Transformer 做 cross-attention） |
| **实时性与边缘部署能力** | 强，在 Jetson AGX Orin 上验证 | 差，随帧数增长延迟急剧上升 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- **NAVSIM Benchmark**：基于 nuPlan 的紧凑再分发数据集，包含 120 小时驾驶日志（采样频率 2Hz），提供开放回路（open-loop）与闭合回路（closed-loop）评估接口。
- **CARLA Simulator + Bench2Drive Benchmark**：用于闭合回路测试，评估真实驾驶任务中的成功率、舒适度等综合性能。

### ⚙️ 实验设置
- **输入**：
  - 多帧（最多 10 帧）相机图像（前视三图拼接为 1024×256）与栅格化 LiDAR；
  - 自车状态（速度、加速度、导航指令）；
  - 初始噪声锚轨迹（anchor trajectories）。
- **输出**：
  - 8 个航点的多模态轨迹（4 秒，2Hz）；
  - 轨迹置信度分数、可通行地图、他车未来状态预测。
- **训练配置**：
  - 使用 4×RTX 4090，batch size=4，Adam 优化器，训练 100 轮；
  - 采用 ResNet-34 作为骨干网络提取图像与 LiDAR 特征；
  - 扩散步数：训练时 50 步（从 1000 中截断），推理仅 2 步。

### 🎯 评估指标
#### 在 NAVSIM 上：
- **PDMS（Predictive Driver Model Score）**：综合得分，由以下子项加权：
  - NC（No Collision）：无碰撞 → 若失败则 PDMS=0
  - DAC（Drivable Area Compliance）：合规行驶
  - TTC（Time-to-Collision）：最小安全距离
  - Comfort：加速度与急动度控制
  - EP（Ego Progress）：沿路线前进程度

#### 在 Bench2Drive 上：
- **闭合回路指标**：成功率达率、驾驶得分、舒适性、效率
- **多能力任务表现**：变道、超车、紧急制动、让行、交通标志识别等

### 🆚 基线方法对比
包括：
- **UniAD**, **Transfuser**, **VADv2**, **Hydra-MDP**, **DiffusionDrive**, **iPad**
- **DRAMA**（Mamba-based，部分线性）
- 所有模型均使用相同 ResNet-34 骨干以保证公平比较

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（NAVSIM 测试集）

| 方法 | NC | DAC | TTC | Comfort | EP | **PDMS** |
|------|----|-----|-----|---------|-----|-----------|
| UniAD | 97.8 | 91.9 | 92.9 | 100 | 78.8 | 83.4 |
| Transfuser | 97.7 | 92.8 | 92.8 | 100 | 79.2 | 84.0 |
| DRAMA | 98.0 | 93.1 | 94.8 | 100 | 80.1 | 85.5 |
| DiffusionDrive | 98.2 | 96.2 | 94.7 | 100 | 82.2 | 88.1 |
| iPad | 98.6 | 98.3 | 94.9 | 100 | 88.0 | **91.7** |
| **LADY (Ours)** | **98.0** | **97.3** | 94.0 | 100 | **88.6** | **90.9** |

> 💡 **分析**：LADY 在 PDMS 上仅次于 iPad，但在 **EP（进度）上超越所有基线**，说明更少保守；同时 **计算开销远低于 iPad**。

### ⏱️ 推理效率对比（Jetson AGX Orin）
如图 6 所示，随着输入帧数增加：
- **Transfuser / DiffusionDrive**：推理时间和内存占用呈 **平方增长**
- **DRAMA**：虽用 Mamba 提升效率，但仍因 cross-attention 使用 Transformer，开销仍随帧数上升
- **LADY**：维持 **恒定延迟与内存占用**，无论历史帧数多少

👉 这使得 LADY 可无缝扩展至“无限帧”历史建模。

### 🔍 消融实验结果

#### （1）不同历史帧数的影响（固定训练帧数=10）

| 帧数 | NC | DAC | TTC | EP | PDMS |
|------|----|-----|-----|-----|--------|
| 1 | 97.4 | 95.5 | 91.8 | 83.2 | 86.8 |
| 4 | 97.6 | 96.9 | 92.9 | 86.8 | 89.5 |
| 8 | 97.7 | 97.1 | 93.3 | 88.2 | 90.3 |
| 10 | 97.9 | 97.1 | 93.6 | 88.5 | 90.6 |
| 15 | 98.0 | 97.3 | 94.0 | 88.6 | **90.9** |
| 20 | 98.0 | 97.2 | 94.1 | 88.5 | **90.9** |

✅ 结论：即使训练只用了 10 帧，**推理时使用更多帧仍能持续提升性能**，证明其强大的泛化能力和长期记忆优势。

#### （2）多帧 DiffusionDrive 对比
作者复现了多帧版本的 DiffusionDrive，PDMS 从 88.1 提升至 89.3，验证了多帧信息的价值，但其计算成本随之飙升，而 LADY 在同等条件下实现了更高性能且保持高效。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **线性注意力可用于完整的端到端自动驾驶系统**，LADY 是首个实现 **全链路线性注意力**（含 self-attention 与 cross-attention）的模型。
2. **LICA 机制成功解决了线性注意力中 cross-attention 缺失的问题**，为后续研究提供了通用模块。
3. **长期历史信息融合显著提升规划质量**，尤其是在遮挡、突发行为等复杂场景中，LADY 表现出更强的预判能力。
4. **恒定复杂度使 LADY 具备极强实用性**，已在 Jetson AGX Orin 上部署验证，适合实际车载平台应用。

### ⚠️ 局限性
- 当前 scorer（评分器）未能充分挖掘多模态候选轨迹的质量潜力。当使用 Oracle Scorer（Best-of-N）时，PDMS 可达 **99.2**，远高于当前 90.9，表明 **scorer 成为性能瓶颈**。
- 数据集中需要长时序推理的样本较少，限制了 LADY 优势的全面体现。
- 当前仍使用 ResNet-34，未探索更先进的视觉主干。

### 🔮 未来工作方向
1. 设计更强大的 trajectory scorer，提升最优轨迹选择能力；
2. 探索其他线性注意力架构（如 RetNet、DeltaNet）集成；
3. 在真实车辆上进行闭环测试与部署；
4. 引入 V2X 或地图先验信息进一步增强感知与预测能力。

---

## 总结

> **LADY 开创性地将线性注意力完整应用于端到端自动驾驶系统，在保证 SOTA 规划性能的同时，实现了常数级推理开销，解决了 Transformer 在长序列建模中的效率瓶颈，为高实时性、低功耗边缘部署提供了可行路径。**

其提出的 **LICA** 机制和 **无限历史融合能力**，标志着自动驾驶感知-决策一体化架构向更高效、更具扩展性的方向迈出关键一步。

</details>

---

### 14. [TENG++: Time-Evolving Natural Gradient for Solving PDEs With Deep Neural Nets under General Boundary Conditions](https://arxiv.org/abs/2512.15771)

**Authors**: Xinjie He, Chenggong Zhang  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.15771v1  

#### Abstract
Partial Differential Equations (PDEs) are central to modeling complex systems across physical, biological, and engineering domains, yet traditional numerical methods often struggle with high-dimensional or complex problems. Physics-Informed Neural Networks (PINNs) have emerged as an efficient altern...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TENG++: Time-Evolving Natural Gradient for Solving PDEs With Deep Neural Nets under General Boundary Conditions

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **传统 Physics-Informed Neural Networks (PINNs)** 在处理非周期性边界条件（如 Dirichlet、Neumann）时存在精度不足和训练不稳定的问题。
- 原始的 **Time-Evolving Natural Gradient (TENG)** 方法仅适用于**周期性边界条件**，限制了其在真实物理场景中的应用。

### 🚀 提出的新方法与创新
- 提出了 **TENG++** —— 对原始 TENG 框架的扩展版本，能够有效处理**任意边界条件**，特别是 **Dirichlet 边界条件**。
- 将自然梯度优化（Natural Gradient Optimization）与显式时间步进方法（如 Euler 和 Heun 方法）结合，形成一个稳定且高效的求解框架。
- 在损失函数中引入**边界惩罚项**（penalty terms），实现对 Dirichlet 条件的精确施加：
  $$
  \mathcal{L}_{\text{Dirichlet}} = \mathcal{L}_{\text{PDE}} + \lambda_{\text{Dirichlet}} \|u(x_{\text{boundary}}) - u_{\text{Dirichlet}}\|^2
  $$

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **边界条件支持** | 支持非周期性边界条件（如 Dirichlet），而原版 TENG 仅支持周期性条件 |
| **数值稳定性** | 利用 Natural Gradient 调整参数更新方向，提升优化过程的几何一致性与收敛稳定性 |
| **高阶精度潜力** | 集成 Heun 方法等二阶时间积分器，显著提高时间演化精度 |
| **模块化设计** | 可灵活替换不同时间步进方案（Euler / Heun），便于拓展至更高阶方法（如 RK4） |

---

## 2. 核心实验方法和设置

### 📊 数据集与问题设定
- **目标方程**：二维各向同性热传导方程（heat equation）
  $$
  \frac{\partial u}{\partial t} = D \nabla^2 u, \quad D = 0.1
  $$
- **空间域**：单位圆盘 $\Omega = B(0,1) \subset \mathbb{R}^2$
- **边界条件**：Dirichlet 条件 $u(x,t) = 0, \forall x \in \partial\Omega$
- **初始条件**：由 Bessel 函数构成的线性组合（disk harmonics），确保有解析解用于误差评估

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| 时间范围 | Euler: $T=0.8$；Heun: $T=4$ |
| 时间步长 | Euler: $dt=0.001$；Heun: $dt=0.005$ |
| 总迭代步数 | 800 步 |
| 每步优化次数 | 5 次 Natural Gradient 更新 |
| 网络采样点数 | 65536 个随机空间点 |
| 初始化方式 | 实验1：预训练权重；实验2：双网络差分初始化（one frozen） |

### 📈 评估指标
- **误差度量**：预测解与真实解析解之间的 $L^2$ 误差随时间的变化
- **可视化输出**：绘制“Error vs T”曲线，分别展示两个坐标方向及总体误差

### 🆚 基线方法对比
- **TENG_Euler**：基于一阶 Euler 时间积分的 TENG++
- **TENG_Heun**：基于二阶 Heun 方法的 TENG++
- （隐含对比）原始 TENG（仅支持周期边界）、标准 PINN（无自然梯度）

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能数据
| 方法 | 最终误差水平 | 时间步长 | 适用场景 |
|------|---------------|----------|---------|
| **TENG_Euler** | ~$2 \times 10^{-3}$ | 0.001 | 快速低精度推断 |
| **TENG_Heun** | <$10^{-4}$ | 0.005 | 高精度长时间模拟 |

> 图1（TENG_Heun）显示误差在整个 $T=4$ 时间区间内保持在 $10^{-4}$ 量级以下，表现出优异的累积误差控制能力。

### 🔁 与基线方法对比
- **TENG_Heun 显著优于 TENG_Euler**：
  - 尽管 Heun 使用更大的时间步长（5倍于 Euler），但最终误差低一个数量级以上。
  - 归因于 Heun 的二阶修正机制有效抑制了误差传播。
- **TENG_Euler 更高效但精度有限**：
  - 适合简单或实时性要求高的任务。

### 🔍 消融实验结果（Initialization Impact）
- **实验1（预训练初始化）**：误差快速下降并稳定在 $10^{-4}$ 以内。
- **实验2（双网络 + 冻结初始化）**：误差显著增大，收敛缓慢。
- **结论**：良好的参数初始化对 TENG++ 的性能至关重要，直接影响收敛速度和最终精度。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **TENG++ 成功将 TENG 框架推广到 Dirichlet 边界条件**，解决了原有方法无法处理一般边界的局限。
2. **Heun 方法在精度上明显优于 Euler 方法**，验证了高阶时间积分在神经 PDE 求解中的有效性。
3. **Natural Gradient 提供了更稳定的优化路径**，有助于平衡 PDE 残差最小化与边界约束满足。
4. **初始化策略对模型表现影响巨大**，合理的先验知识注入可大幅提升训练效率和最终精度。

### ⚠️ 方法的局限性
- 当前仅验证了 **Dirichlet 条件**，尚未测试 Neumann 或混合边界条件。
- 所有实验基于规则几何（圆形区域），复杂几何下的泛化能力待验证。
- 训练依赖高质量初始猜测（如预训练权重），冷启动性能较弱。
- 尚未与其他先进方法（如 DeepONet、Fourier Neural Operator）进行横向比较。

### 🔮 未来工作方向
1. **扩展至 Neumann 与 Mixed Boundary Conditions**
   - 引入法向导数惩罚项或 Lagrange multiplier 机制。
2. **应用于更广泛的 PDE 类型**
   - 如非线性 PDE（Navier-Stokes）、波动方程、反应扩散系统。
3. **集成更高阶时间积分器**
   - 如 Runge-Kutta (RK4) 方法，进一步提升长期模拟精度。
4. **探索自适应时间步长与动态采样策略**
   - 提高计算资源利用效率。
5. **结合物理先验结构设计网络架构**
   - 如对称性嵌入、谱基展开等，增强表示能力。

---

## 总结

✅ **TENG++ 是一项重要的推进**：它不仅扩展了 TENG 的适用范围至通用边界条件，还通过结合高阶时间积分与自然梯度优化，在精度与稳定性之间取得了良好平衡。  
🧠 特别是在长时间演化问题中，**TENG_Heun 展现出接近机器精度的潜力**，为神经网络求解 PDE 提供了一条可靠路径。  
🚀 结合合理初始化与未来对 Neumann/mixed 条件的支持，该框架有望成为科学计算中新一代 PDE 求解器的重要候选。

</details>

---

### 15. [Boosting t-SNE Efficiency for Sequencing Data: Insights from Kernel Selection](https://arxiv.org/abs/2512.15900)

**Authors**: Avais Jan, Prakash Chourasia, Sarwan Ali, Murray Patterson  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.15900v1  

#### Abstract
Dimensionality reduction techniques are essential for visualizing and analyzing high-dimensional biological sequencing data. t-distributed Stochastic Neighbor Embedding (t-SNE) is widely used for this purpose, traditionally employing the Gaussian kernel to compute pairwise similarities. However, the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Boosting t-SNE Efficiency for Sequencing Data: Insights from Kernel Selection

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**高维生物序列数据**（如SARS-CoV-2刺突蛋白序列）在进行可视化和分析时，传统t-SNE方法存在的两个核心问题：
1.  **核函数选择不当**：传统的t-SNE默认使用**Gaussian Kernel**（基于欧氏距离），而生物序列是离散的类别型数据，欧氏距离无法有效捕捉其内在的相似性（如编辑距离）。
2.  **计算效率低下**：Gaussian Kernel和近期提出的Isolation Kernel计算开销大，难以扩展到大规模测序数据。

### 提出的新方法/新思路
论文提出了一种系统性的解决方案，其核心创新在于**核函数（Kernel）的选择**：
- **全面评估多种核函数**：首次对应用于t-SNE的九种不同核函数进行了综合评估，包括：**Cosine Similarity**, Linear, Polynomial, Gaussian, Isolation, Laplacian, Sigmoid, Chi-squared, 和 Additive-chi2。
- **推荐最优核函数**：通过大量实验证明，在处理分子序列数据时，**Cosine Similarity Kernel** 是一个更优的选择。

### 相比现有方法的优势
- **性能更优**：Cosine Similarity Kernel 在保留原始高维空间中的成对距离关系方面表现最佳，优于广泛使用的Gaussian Kernel和新近提出的Isolation Kernel。
- **效率更高**：Cosine Similarity Kernel 具有卓越的计算效率，其运行时间随序列数量呈**线性增长**，远快于Gaussian Kernel。
- **适用性广**：该结论不仅适用于可视化质量，还显著提升了下游任务（如分类和聚类）的性能。

## 2. 核心实验方法和设置

### 使用的数据集
研究在六个多样化的生物数据集上进行了验证，涵盖了病毒基因组、宿主来源和抗癌肽等不同类型：
- **Spike7k**: 7,000条SARS-CoV-2刺突蛋白序列，22个谱系。
- **Host**: 5,558条冠状病毒刺突蛋白序列，来自21种不同宿主。
- **ShortRead**: 10,181条模拟的SARS-CoV-2短读长序列。
- **Rabies**: 20,051条狂犬病毒基因组序列。
- **Genome**: 8,220条完整的SARS-CoV-2基因组序列。
- **Breast Cancer**: 949条膜裂解抗癌肽（ACPs）序列。

### 实验设置和评估指标
- **嵌入方法**：使用三种方法将序列转换为数值向量：
  - **One-Hot Encoding (OHE)**
  - **Spike2Vec** (word2vec启发的深度学习方法)
  - **Minimizers** (k-mer的压缩表示)
  - （部分实验还包括Spaced k-mers）
- **评估维度**：
  1.  **t-SNE可视化质量**：
      - **主观评估**：通过2D/3D散点图观察簇的分离情况。
      - **客观评估**：使用 **AUCRNx** (Area Under the RNX curve) 指标量化邻域保留度（Neighborhood Preservation）。
  2.  **下游任务性能**：
      - **分类**：使用7种ML算法（SVM, NB, MLP, KNN, RF, LR, DT），评估准确率（Accuracy）、精确率（Precision）、召回率（Recall）、F1分数和AUC。
      - **聚类**：使用k-means和k-modes，评估轮廓系数（Silhouette Coefficient）、Calinski-Harabasz指数和Davies-Bouldin得分。
  3.  **计算效率**：记录核矩阵计算时间和t-SNE总运行时间。

### 基线方法对比
- **主要基线**：传统的 **Gaussian Kernel** 和近期提出的 **Isolation Kernel**。
- **对比对象**：论文提出的 **Cosine Similarity Kernel** 以及其他七种核函数。

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果
- **t-SNE性能 (以Spike7k数据集为例)**：
  - **AUCRNx值**：Cosine Similarity Kernel (Spike2Vec嵌入) 达到了 **0.331**，显著高于Gaussian Kernel (0.235) 和 Isolation Kernel (0.189)。
  - **计算时间**：Cosine Similarity Kernel的核计算时间（15.865秒）远低于Gaussian Kernel（54.192秒）和Isolation Kernel（24.162秒）。

- **分类性能**：
  - 在多个数据集上，**Linear** 和 **Cosine** Kernel 表现优异。例如，在Spike7k数据集上，Linear Kernel配合Random Forest达到了接近80%的准确率。
  - **Isolation Kernel** 在OHE和k-mer嵌入上的表现极差，准确率常接近随机水平（~0.5），表明其与这些表示不兼容。

- **聚类性能**：
  - 结果高度依赖于数据集和嵌入方法。例如，在Host数据集上，Polynomial Kernel配合k-means取得了高达0.975的轮廓系数。

- **计算效率**：
  - **Runtime Analysis** 图显示，**Cosine Similarity Kernel** 的计算时间增长最平缓，呈现良好的线性缩放行为，而Gaussian Kernel的计算成本最高。

### 消融实验结果
虽然没有明确标注为“消融实验”，但论文通过以下方式实现了类似效果：
- **跨数据集验证**：在6个截然不同的数据集上重复实验，证明了结论的普适性。
- **跨嵌入方法验证**：在OHE、Spike2Vec、Minimizers等多种嵌入方法下测试，证明了Cosine Kernel的鲁棒性。
- **多任务验证**：不仅评估了t-SNE的降维质量，还评估了其对分类和聚类等下游任务的影响，全面展示了核函数选择的重要性。

## 4. 关键结论和发现

### 主要发现
1.  **核函数至关重要**：核函数的选择对t-SNE的性能有根本性影响，不应被视为一个无关紧要的技术细节。
2.  **Cosine Similarity是更优选择**：对于生物序列数据，**Cosine Similarity Kernel** 在**性能**和**效率**上均优于传统的Gaussian Kernel和Isolation Kernel。
3.  **Gaussian Kernel存在缺陷**：基于欧氏距离的Gaussian Kernel 不适合捕捉离散序列间的相似性，导致在多个数据集上表现不佳。
4.  **嵌入方法与核函数相互作用**：最佳的核函数选择也依赖于所使用的序列嵌入方法，不存在一个绝对通用的“最佳”组合。
5.  **效率优势明显**：Cosine Kernel的线性时间复杂度使其特别适合处理大规模测序数据。

### 方法的局限性
- **并非所有场景都最优**：尽管Cosine Kernel总体表现最好，但在某些特定数据集（如Breast Cancer）和任务中，其他核函数（如Chi-squared）可能表现更好。
- **Isolation Kernel的局限性**：论文发现Isolation Kernel在OHE和k-mer等标准嵌入上表现糟糕，这限制了其应用范围。
- **未探索所有可能性**：研究聚焦于九种核函数，可能存在其他更适合特定序列特征的核函数未被探索。

### 未来工作方向
- **探索更多专用核函数**：开发专门针对生物序列设计的核函数，可能进一步提升性能。
- **结合UMAP等新方法**：将核函数选择的研究扩展到UMAP等其他流行的降维技术。
- **自动化核函数选择**：研究如何根据数据特征自动选择最优的核函数-嵌入方法组合。
- **应用于更大规模数据**：在PB级的基因组数据库上验证该方法的可扩展性和有效性。

</details>

---

### 16. [Beyond Fast and Slow: Cognitive-Inspired Elastic Reasoning for Large Language Models](https://arxiv.org/abs/2512.15089)

**Authors**: Jinwu Hu, Dongjin Yang, Langyu Bian, Zhiquan Wen, Yufeng Wang, Yaofo Chen, Bin Xiao, Yuanqing Li, Mingkui Tan  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.15089v1  

#### Abstract
Large language models (LLMs) have demonstrated impressive performance across various language tasks. However, existing LLM reasoning strategies mainly rely on the LLM itself with fast or slow mode (like o1 thinking) and thus struggle to balance reasoning efficiency and accuracy across queries of var...

---

### 17. [BRAID: Bounded Reasoning for Autonomous Inference and Decisions](https://arxiv.org/abs/2512.15959)

**Authors**: Arma\u{g}an Amcalar, Eyup Cinar  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.15959v1  

#### Abstract
Large Language Models (LLMs) exhibit nonlinear relationships between performance, cost, and token usage. This paper presents a quantitative study on structured prompting using BRAID (Bounded Reasoning for Au tonomous Inference and Decisions) across multiple GPT model tiers, eval uated on the Advance...

---

### 18. [An Information-Theoretic Framework for Robust Large Language Model Editing](https://arxiv.org/abs/2512.16227)

**Authors**: Qizhou Chen, Chengyu Wang, Taolin Zhang, Xiaofeng He  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.16227v1  

#### Abstract
Large Language Models (LLMs) have become indispensable tools in science, technology, and society, enabling transformative advances across diverse fields. However, errors or outdated information within these models can undermine their accuracy and restrict their safe deployment. Developing efficient ...

---

### 19. [Semantic-Constrained Federated Aggregation: Convergence Theory and Privacy-Utility Bounds for Knowledge-Enhanced Distributed Learning](https://arxiv.org/abs/2512.15759)

**Authors**: Jahidul Arafat  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.15759v1  

#### Abstract
Federated learning enables collaborative model training across distributed data sources but suffers from slow convergence under non-IID data conditions. Existing solutions employ algorithmic modifications treating all client updates identically, ignoring semantic validity. We introduce Semantic-Cons...

---

### 20. [Stackelberg Learning from Human Feedback: Preference Optimization as a Sequential Game](https://arxiv.org/abs/2512.16626)

**Authors**: Barna P\'asztor, Thomas Kleine Buening, Andreas Krause  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.16626v1  

#### Abstract
We introduce Stackelberg Learning from Human Feedback (SLHF), a new framework for preference optimization. SLHF frames the alignment problem as a sequential-move game between two policies: a Leader, which commits to an action, and a Follower, which responds conditionally on the Leader's action. This...

---

### 21. [Posterior Behavioral Cloning: Pretraining BC Policies for Efficient RL Finetuning](https://arxiv.org/abs/2512.16911)

**Authors**: Andrew Wagenmaker, Perry Dong, Raymond Tsao, Chelsea Finn, Sergey Levine  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.16911v1  

#### Abstract
Standard practice across domains from robotics to language is to first pretrain a policy on a large-scale demonstration dataset, and then finetune this policy, typically with reinforcement learning (RL), in order to improve performance on deployment domains. This finetuning step has proved critical ...

---

### 22. [A Clustering-Based Variable Ordering Framework for Relaxed Decision Diagrams for Maximum Weighted Independent Set Problem](https://arxiv.org/abs/2512.15198)

**Authors**: Mohsen Nafar, Michael R\"omer, Lin Xie  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15198v1  

#### Abstract
Efficient exact algorithms for Discrete Optimization (DO) rely heavily on strong primal and dual bounds. Relaxed Decision Diagrams (DDs) provide a versatile mechanism for deriving such dual bounds by compactly over-approximating the solution space through node merging. However, the quality of these ...

---

### 23. [CangLing-KnowFlow: A Unified Knowledge-and-Flow-fused Agent for Comprehensive Remote Sensing Applications](https://arxiv.org/abs/2512.15231)

**Authors**: Zhengchao Chen, Haoran Wang, Jing Yao, Pedram Ghamisi, Jun Zhou, Peter M. Atkinson, Bing Zhang  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15231v1  

#### Abstract
The automated and intelligent processing of massive remote sensing (RS) datasets is critical in Earth observation (EO). Existing automated systems are normally task-specific, lacking a unified framework to manage diverse, end-to-end workflows--from data preprocessing to advanced interpretation--acro...

---

### 24. [JustRL: Scaling a 1.5B LLM with a Simple RL Recipe](https://arxiv.org/abs/2512.16649)

**Authors**: Bingxiang He, Zekai Qu, Zeyuan Liu, Yinghao Chen, Yuxin Zuo, Cheng Qian, Kaiyan Zhang, Weize Chen, Chaojun Xiao, Ganqu Cui, Ning Ding, Zhiyuan Liu  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.16649v1  

#### Abstract
Recent advances in reinforcement learning for large language models have converged on increasing complexity: multi-stage training pipelines, dynamic hyperparameter schedules, and curriculum learning strategies. This raises a fundamental question: \textbf{Is this complexity necessary?} We present \te...

---

### 25. [Bridging Data and Physics: A Graph Neural Network-Based Hybrid Twin Framework](https://arxiv.org/abs/2512.15767)

**Authors**: M. Gorpinich (Valeo, PIMM Lab. ENSAM Institute of Technology), B. Moya (PIMM Lab. ENSAM Institute of Technology), S. Rodriguez (PIMM Lab. ENSAM Institute of Technology), F. Meraghni (PIMM Lab. ENSAM Institute of Technology), Y. Jaafra (Valeo), A. Briot (Valeo), M. Henner (Valeo), R. Leon (Valeo), F. Chinesta (PIMM Lab. ENSAM Institute of Technology, CNRS@CREATE LTD. Singapore)  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15767v1  

#### Abstract
Simulating complex unsteady physical phenomena relies on detailed mathematical models, simulated for instance by using the Finite Element Method (FEM). However, these models often exhibit discrepancies from the reality due to unmodeled effects or simplifying assumptions. We refer to this gap as the ...

---

### 26. [Surrogate Neural Architecture Codesign Package (SNAC-Pack)](https://arxiv.org/abs/2512.15998)

**Authors**: Jason Weitz, Dmitri Demler, Benjamin Hawks, Nhan Tran, Javier Duarte  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.15998v1  

#### Abstract
Neural Architecture Search is a powerful approach for automating model design, but existing methods struggle to accurately optimize for real hardware performance, often relying on proxy metrics such as bit operations. We present Surrogate Neural Architecture Codesign Package (SNAC-Pack), an integrat...

---

### 27. [Quantitative Verification of Fairness in Tree Ensembles](https://arxiv.org/abs/2512.16386)

**Authors**: Zhenjiang Zhao, Takahisa Toda, Takashi Kitamura  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.16386v1  

#### Abstract
This work focuses on quantitative verification of fairness in tree ensembles. Unlike traditional verification approaches that merely return a single counterexample when the fairness is violated, quantitative verification estimates the ratio of all counterexamples and characterizes the regions where ...

---

### 28. [IoMT-based Automated Leukemia Classification using CNN and Higher Order Singular Value](https://arxiv.org/abs/2512.16448)

**Authors**: Shabnam Bagheri Marzijarani, Mohammad Zolfaghari, Hedieh Sajedi  
**Category**: cs.LG  
**Published**: 2025-12-19  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.16448v1  

#### Abstract
The Internet of Things (IoT) is a concept by which objects find identity and can communicate with each other in a network. One of the applications of the IoT is in the field of medicine, which is called the Internet of Medical Things (IoMT). Acute Lymphocytic Leukemia (ALL) is a type of cancer categ...

---

### 29. [Graph Contextual Reinforcement Learning for Efficient Directed Controller Synthesis](https://arxiv.org/abs/2512.15295)

**Authors**: Toshihide Ubukata, Enhong Mu, Takuto Yamauchi, Mingyue Zhang, Jialong Li, Kenji Tei  
**Category**: cs.AI  
**Published**: 2025-12-19  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.15295v1  

#### Abstract
Controller synthesis is a formal method approach for automatically generating Labeled Transition System (LTS) controllers that satisfy specified properties. The efficiency of the synthesis process, however, is critically dependent on exploration policies. These policies often rely on fixed rules or ...

---

### 30. [Sigma-Moe-Tiny Technical Report](https://arxiv.org/abs/2512.16248)

**Authors**: Qingguo Hu, Zhenghao Lin, Ziyue Yang, Yucheng Ding, Xiao Liu, Yuting Jiang, Ruizhe Wang, Tianyu Chen, Zhongxin Guo, Yifan Xiong, Rui Gao, Lei Qu, Jinsong Su, Peng Cheng, Yeyun Gong  
**Category**: cs.CL  
**Published**: 2025-12-19  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2512.16248v1  

#### Abstract
Mixture-of-Experts (MoE) has emerged as a promising paradigm for foundation models due to its efficient and powerful scalability. In this work, we present Sigma-MoE-Tiny, an MoE language model that achieves the highest sparsity compared to existing open-source models. Sigma-MoE-Tiny employs fine-gra...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
