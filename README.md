# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-28 06:00:40 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [DART: Diffusion-Inspired Speculative Decoding for Fast LLM Inference](https://arxiv.org/abs/2601.19278)

**Authors**: Fuliang Liu, Xue Li, Ketai Zhao, Yinxi Gao, Ziyan Zhou, Zhonghui Zhang, Zhibin Wang, Wanchun Dou, Sheng Zhong, Chen Tian  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2601.19278v1  

#### Abstract
Speculative decoding is an effective and lossless approach for accelerating LLM inference. However, existing widely adopted model-based draft designs, such as EAGLE3, improve accuracy at the cost of multi-step autoregressive inference, resulting in high drafting latency and ultimately rendering the ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# DART: Diffusion-Inspired Speculative Decoding for Fast LLM Inference 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
现有的 **speculative decoding** 方法在加速大语言模型（LLM）推理时面临一个核心瓶颈：**drafting latency**（起草阶段延迟）。尽管如 EAGLE3 等模型通过轻量级设计提升了预测准确性（以 **Average Acceptance Length (T)** 衡量），但其仍采用自回归（autoregressive）生成方式，导致起草过程本身成为性能瓶颈，占总推理时间的 20%-40%。

另一方面，基于 N-gram 或检索启发式的方法（如 Lookahead）虽然延迟极低，但预测质量差，T 值很低，无法带来显著端到端加速。

此外，直接将 **diffusion-based LLMs (dLLMs)** 用作 draft model 存在根本性不匹配：
- dLLMs 是双向建模、全序列去噪，而 speculative decoding 要求严格基于前缀的因果预测；
- dLLMs 参数量大，独立部署导致推理开销高；
- 存在 tokenizer 不兼容等问题。

### 提出了什么新方法或新思路
本文提出 **DART**（Diffusion-Inspired Speculative Decoding），一种受扩散模型启发但专为 speculative decoding 定制的新框架，其核心思想是：

- **并行起草机制**：借鉴 dLLMs 并行预测多个掩码位置的能力，但在单次前向传播中并行预测多个未来 token 的 logits，彻底消除 draft model 中的自回归 rollout。
- **轻量化耦合架构**：draft model 复用 target model 的隐藏状态作为输入，仅添加一个轻量级的定制化 Transformer 解码层进行预测，避免引入重型独立模型。
- **Shifted Logits Prediction**：创新地将每个位置输出的 logit 映射为对“下一个 token”的预测，提升早期位置的预测准确率。
- **N-gram 引导的树剪枝算法**：利用 N-gram 模型作为连续性感知的剪枝机制，在由并行 logits 构成的巨大组合空间中高效构建语义连贯的高质量 draft token tree。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **Latency** | 起草前向延迟相比 EAGLE3 最多降低 **6.8×**，相比 SPS 降低 **53.3×** |
| **Throughput** | 端到端解码速度提升 **2.03×–3.44×**，平均比 EAGLE3 快 **30%** |
| **Accuracy** | 保持与 EAGLE3 相当甚至更高的 **T 值**（平均接受长度） |
| **Efficiency** | 设计轻量、无需复杂 KV cache 管理，适合实际部署 |

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
实验覆盖多种任务类型，评估泛化能力：
- **指令跟随与对话**：MT-Bench, Alpaca
- **数学推理**：Math500
- **代码生成**：HumanEval, CodeAlpaca, LiveCodeBench, MBPP

### 实验设置和评估指标

#### 硬件环境
- 服务器配置：8×NVIDIA H20-3e GPUs (141GB), 90 CPU cores, 900GB RAM
- 部分跨平台测试也在单张 A100-40G 上完成

#### 目标模型（Target Models）
- 主要基于 **Qwen3** 系列：Qwen3-1.7B, 4B, 8B, 14B, 32B
- 对比部分方法时使用 **LLaMA2-Chat-7B**

#### 评估指标
| 指标 | 含义 |
|------|------|
| **Throughput / Speedup** | 相对于标准自回归解码的端到端吞吐提升倍数 |
| **Average Acceptance Length (T)** | 每轮验证中被接受的 draft token 数量均值，反映 draft 准确性 |
| **Drafting Latency** | draft model 前向 + tree search 所耗时间（毫秒） |

#### Draft 长度
- DART 和 EAGLE3 均设为 **8**
- SPS 使用 Qwen3-1.7B 作为 draft model，长度为 5

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 模型 | 方法 | 平均 Speedup | 平均 T 值 |
|------|------|-------------|----------|
| Qwen3-14B | EAGLE3 | 2.02× | 3.48 |
| Qwen3-14B | **DART** | **2.77×** | **3.67** |
| Qwen3-32B | EAGLE3 | 2.11× | 3.85 |
| Qwen3-32B | **DART** | **2.42×** | **3.76** |
| LLaMA2-Chat-7B | EAGLE3 | 2.66× | 3.55 |
| LLaMA2-Chat-7B | **DART** | **2.85×** | **4.08** |

> ✅ **最大端到端加速达 3.44×，平均比 EAGLE3 快 30%**

> 🔥 在代码相关任务上表现尤为突出，例如在 CodeAlpaca 上对 Qwen3-14B 的加速比 EAGLE3 提升 **65%**

### 与基线方法的对比结果

| 方法 | 特点 | DART 相对优势 |
|------|------|----------------|
| **SPS** (Standard Speculative Sampling) | 使用小模型自回归 draft | DART drafting forward 快 **53.3×**，整体加速远超 |
| **EAGLE3** | 单层自回归 draft，精度高但延迟仍高 | DART drafting forward 快 **6.8×**，端到端快 **30%** |
| **Medusa / Hydra** | 多头并行预测，非自回归 | DART 在多数任务上 throughput 更高 |
| **Lookahead / PLD** | 无模型启发式 draft | DART 的 T 值更高，带来更稳定显著的加速 |

### 消融实验结果

#### ✅ N-gram 剪枝有效性（Table 2）
| 设置 | HumanEval (T) | Alpaca (T) | CodeAlpaca (T) |
|------|---------------|-----------|----------------|
| 无 N-gram | 3.13 | 2.76 | 3.85 |
| 有 N-gram | **3.63 (+0.5)** | **3.26 (+0.5)** | **4.59 (+0.74)** |

> N-gram 显著提升 T 值，证明其在过滤语义不合理路径上的有效性。

#### ✅ Shifted Logits Prediction（Table 3）
| 位置 | Unshifted (Hit@1) | Shifted (Hit@1) | 提升 |
|------|--------------------|------------------|------|
| Q-1 | 57.7% | **71.1%** | +13.4% |
| Q-2 | 38.1% | **41.0%** | +2.9% |
| Q-3 | 25.1% | **27.6%** | +2.5% |

> “Shifted” 设计大幅提升首个位置预测准确率，这对 speculative decoding 至关重要。

#### ✅ Annealing Coefficient γ 选择（Table 4）
最优设置为 **γ = 0.6**，此时：
- 早期位置（Q-1 ~ Q-3）准确率较高
- 整体 T 值达到最高（3.63 vs baseline 3.48）

> 证明衰减式 KL 目标函数能有效平衡短程与长程预测稳定性。

#### ✅ 更大 batch size 下的表现（Table 5）
即使 batch size 增大至 64，DART 依然优于 EAGLE3：
| Batch Size | EAGLE3 Speedup | DART Speedup |
|------------|----------------|--------------|
| 2 | 1.84× | **2.16×** |
| 32 | 1.28× | **1.48×** |
| 64 | 1.22× | **1.45×** |

> 表明 DART 的优势在不同负载下具有鲁棒性。

---

## 4. 关键结论和发现

### 论文的主要发现
1. **并行 drafting 是可行且高效的**：通过定制化的 diffusion-style 并行预测机制，可在不牺牲准确性的前提下大幅降低 drafting latency。
2. **最小化 drafting overhead 比单纯追求高 T 更重要**：DART 的 T 值略低于或接近 EAGLE3，但由于 drafting latency 极低，最终端到端性能反而更强。
3. **N-gram 应用于 continuity-aware pruning 而非独立 predictor 更有效**：结合模型 logits 与 N-gram 连续性评分可构建高质量 draft tree。
4. **DART 设计具有硬件通用性**：在 H20-3e 和 A100 上均表现出一致的优越性能（见 Appendix F）。

### 方法的局限性
- **内存占用增加**：N-gram 模型需加载约 **100GB CPU RAM**，虽可共享但仍有一定资源门槛。
- **依赖 target model 的中间隐藏状态**：需要访问 target model 的特定层输出，可能限制某些闭源 API 场景的应用。
- **训练成本存在**：需针对每个 target model 单独训练 draft model，尽管训练流程已优化。

### 未来工作方向
- 探索更紧凑的 continuity model 替代大型 N-gram trie，降低内存需求。
- 将 DART 思路扩展至 MoE 模型或其他新型架构。
- 研究 zero-shot 或 prompt-driven draft model，减少训练依赖。
- 结合动态 draft length 控制策略进一步优化性能。

---

> 📌 **总结一句话**：  
> **DART 通过“并行 logits 预测 + N-gram 引导剪枝”的新范式，在几乎不增加 drafting 开销的前提下实现了与最强自回归 draft model 相当的准确性，从而达成当前最优的端到端 speculative decoding 加速效果。**

</details>

---

### 2. [Learning Adaptive Parallel Execution for Efficient Code Localization](https://arxiv.org/abs/2601.19568)

**Authors**: Ke Xu, Siyang Xiao, Ming Liang, Yichen Yu, Zhixiang Wang, Jingxuan Xu, Dajun Chen, Wei Jiang, Yong Li  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.19568v1  

#### Abstract
Code localization constitutes a key bottleneck in automated software development pipelines. While concurrent tool execution can enhance discovery speed, current agents demonstrate a 34.9\% redundant invocation rate, which negates parallelism benefits. We propose \textbf{FuseSearch}, reformulating pa...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Adaptive Parallel Execution for Efficient Code Localization

## 1. 论文的主要贡献和创新点

### 解决的问题
代码定位（code localization）是自动化软件开发流程中的关键瓶颈，当前基于LLM的代理（agent）在该任务上消耗超过50%的计算资源。传统顺序工具调用方式导致“信息饥饿”（information starvation）——在有限的交互轮次（turn budget）下无法获取足够上下文。尽管并行执行可提升信息密度，但现有方法常引入高达34.9%的冗余工具调用，造成资源浪费和噪声干扰。

### 提出的新方法与创新
本文提出 **FuseSearch**，一种通过学习自适应并行执行实现高效代码定位的框架，其核心创新包括：

- **工具效率（tool efficiency）度量**：定义为有效信息增益（novel information gain）与总调用次数的比率，量化每次工具调用的信息新颖性，直接惩罚冗余探索。
- **双目标优化框架**：联合优化定位质量（F1-score）与搜索效率（tool efficiency），通过SFT + RL两阶段训练，使模型学会在保证精度的同时最小化冗余。
- **极简主义工具集**：仅依赖三个语言无关、只读的工具（`grep`, `glob`, `read_file`），无需AST解析器或代码图等复杂基础设施，提升部署通用性。
- **动态并行策略**：不同于固定宽度的并行方案，FuseSearch根据任务上下文动态调整每轮并行工具数量，从广度探索逐步过渡到精细定位。

### 相比现有方法的优势
- **更高效**：显著减少交互轮数、时间与token消耗。
- **更精准**：通过抑制冗余调用，避免上下文过载，提升定位准确率。
- **更轻量**：不依赖预构建的代码图或语言特定解析器，部署成本低。
- **可扩展**：适用于不同规模模型（如4B/30B参数），且能加速下游任务。

---

## 2. 核心实验方法和设置

### 数据集
- **主评估数据集**：**SWE-bench Verified**（Jimenez et al., 2024），一个面向真实GitHub issue修复的仓库级基准测试。
- **过滤条件**：排除补丁中引入全新文件或函数的样本，保留386个实例用于评估。
- **训练数据集**：从233个高质量GitHub仓库中构建约21K个issue-patch对，经筛选后用于SFT和RL训练。

### 实验设置与评估指标
#### 评估维度
- **定位质量**：
  - 文件级与函数级的 Precision、Recall 和 F1-score。
- **搜索效率**：
  - 交互轮数（#Turn）
  - 总耗时（T(s)，wall-clock time）
  - 总token消耗（Tok.(k)）
  - 工具效率（e）

#### 模型配置
- 主干模型：Qwen3-4B-Instruct 和 Qwen3-30B-A3B-Instruct。
- 训练流程：
  1. **SFT阶段**：使用教师模型（Kimi-K2-Instruct）生成轨迹，并采用**双指标过滤**（F1 ≥ 0.6 且 e ≥ 0.6）确保高质量示范数据。
  2. **RL阶段**：基于GRPO算法进行强化学习，奖励函数设计为：
     $$
     R(T) = \alpha \cdot F1(T) + \gamma \cdot (F1(T) \cdot e(T))
     $$
     其中 $\alpha=0.8$, $\gamma=0.2$，强调高F1下的高效率行为。

#### 基线方法对比
| 类别 | 方法 | 特点 |
|------|------|------|
| Workflow-based | Agentless (Xia et al., 2024) | 固定分层策略，无状态维护 |
| Agent-based | LocAgent (Chen et al., 2025) | 基于静态代码图导航 |
| | CoSIL (Jiang et al., 2025) | 动态构建模块调用图 |
| | RepoSearcher (Ma et al., 2025) | 轻量工具套件，无图结构 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（SWE-bench Verified）

| 模型 | 文件F1 | 函数F1 | #Turn | 时间(s) | Token(k) | 模式 |
|------|--------|--------|-------|----------|-----------|------|
| **FuseSearch-4B (train)** | **84.7%** | **56.4%** | **4.78** | **5.43** | **30.9** | Par |
| RepoSearcher (Qwen3-4B) | 38.1% | 21.7% | 14.8 | 85.3 | 99.2 | Seq |
| Agentless (Haiku 4.5) | 54.5% | 31.8% | 2.00 | 7.32 | 10.6 | Seq |
| LocAgent (Qwen3-4B) | 72.3% | 50.6% | 17.3 | 185 | 67 | Seq |

> ✅ **性能提升总结**：
- 相比RepoSearcher，**文件F1提升123%**，**函数F1提升160%**。
- **减少67.7%交互轮数**，**降低93.6%时间开销**，**节省68.9% token消耗**。
- 在同等backbone下超越专有模型（如Haiku 4.5）。

### 与其他基线对比
- **优于LocAgent**：尽管LocAgent使用复杂图结构，FuseSearch仍以更少轮次和更低延迟取得更高F1。
- **超越Agentless**：虽后者为workflow模式，但FuseSearch在recall和precision上全面领先。

### 消融实验结果

#### 并行 vs 顺序执行（Table 3）
| 模式 | 阶段 | 文件F1 | 函数F1 | #Turn | Tok.(k) |
|------|------|--------|--------|-------|---------|
| Sequential | SFT+RL | 78.82 | 50.21 | 7.52 | 59.4 |
| **Parallel** | **SFT+RL** | **84.65** | **56.45** | **5.60** | **30.9** |

> 并行执行不仅提速，还因每步获取更多信息而提升最终F1。

#### SFT数据过滤策略（Table 4）
| 过滤方式 | 文件F1 | 函数F1 | 效率e | Tok.(k) |
|--------|--------|--------|--------|---------|
| 无过滤 | 75.44 | 43.52 | 55.77 | 60.7 |
| F1-only | 78.55 | 45.43 | 56.72 | 73.2 |
| e-only | 76.74 | 42.63 | 60.14 | 61.8 |
| **Joint (F1+e)** | **78.86** | **47.94** | **62.03** | **54.8** |

> 双指标过滤同时优化质量和效率，为后续RL提供最优初始化。

#### 奖励函数设计（Table 5）
| 奖励类型 | 文件F1 | 函数F1 | e | T(s) | Tok.(k) |
|--------|--------|--------|-----|------|---------|
| F1 only | 81.84 | 54.90 | 59.66 | 7.28 | 39.4 |
| F + e (additive) | 79.22 | 51.98 | 66.62 | 9.40 | 45.7 |
| **F + F·e (multiplicative)** | **84.65** | **56.45** | **69.00** | **5.43** | **30.9** |

> 乘法形式奖励（F + F·e）实现了质量与效率的最佳平衡。

---

## 4. 关键结论和发现

### 主要发现
1. **效率感知训练自然提升质量**：通过显式建模工具效率并联合优化，模型自动规避冗余调用，从而减少噪声干扰，反而提升了最终定位精度。
2. **并行执行的价值不止于速度**：并行不仅是降低延迟的技术手段，更是提升决策质量的关键——每轮获取更多上下文有助于做出更优判断。
3. **极简工具集足以胜任复杂任务**：无需代码图或AST解析器，仅靠`grep`/`glob`/`read_file`即可实现SOTA性能，验证了“战略调度”优于“复杂工具”的理念。
4. **自适应并行优于固定宽度**：模型学会在早期广泛探索，后期聚焦精修，体现出智能的搜索策略演化。

### 方法的局限性
1. **评估偏差**：当前benchmark（如SWE-bench）主要覆盖Python项目，对Java/C++等静态语言泛化能力有待验证。
2. **解空间单一**：ground truth基于实际提交的patch，可能忽略其他合理但未被采用的修改路径。
3. **任务范围受限**：目前仅针对issue-driven定位，尚未拓展至代码问答、文档生成等更广泛的code search场景。
4. **依赖高质量训练数据**：SFT阶段需大量高质量轨迹，依赖强教师模型生成。

### 未来工作方向
- 构建跨语言、多任务的综合性代码定位基准（comprehensive localization benchmarks）。
- 探索在非issue驱动场景（如API理解、文档生成）中的应用。
- 将FuseSearch作为通用子模块集成至端到端软件工程代理系统中。
- 研究如何进一步压缩模型规模（如<1B）以支持边缘部署。

> 🔚 **总结**：  
> FuseSearch证明了**效率驱动的学习范式**能够突破传统精度-效率权衡，通过引入**tool efficiency**这一核心概念，结合**SFT+RL双阶段训练**，实现了高性能、低成本、易部署的代码定位解决方案，为构建生产级自动化软件开发系统提供了重要实践路径。

</details>

---

### 3. [Flatter Tokens are More Valuable for Speculative Draft Model Training](https://arxiv.org/abs/2601.18902)

**Authors**: Jiaming Fan, Daming Cao, Xiangzhong Luo, Jiale Fu, Chonghan Liu, Xu Yang  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.18902v1  

#### Abstract
Speculative Decoding (SD) is a key technique for accelerating Large Language Model (LLM) inference, but it typically requires training a draft model on a large dataset. We approach this problem from a data-centric perspective, finding that not all training samples contribute equally to the SD accept...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Flatter Tokens are More Valuable for Speculative Draft Model Training*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对 **Speculative Decoding (SD)** 中 **draft model** 训练效率低下的问题。尽管 SD 能通过 draft model 预测多个 token 并由 target model 并行验证来加速 LLM 推理，但训练一个高质量的 draft model 通常需要在大规模数据集上进行，计算成本高昂。

当前主流方法（如 EAGLE）采用标准的 **Knowledge Distillation (KD)**，对所有 token 和样本一视同仁地训练，忽略了不同训练样本对提升 **acceptance rate** 的贡献差异，导致训练资源浪费。

### **提出了什么新方法或新思路**

作者从 **data-centric** 角度出发，提出了一项关键理论洞察和一种高效的数据选择方法：

- **核心理论洞察**：  
  在 SD 场景下，**目标模型 (target model) 输出分布越平坦 (flatter) 的 token，对提升 acceptance rate 的训练价值越高**。这是因为平坦分布意味着 draft model 有更大的“改进空间”（headroom），单步更新能带来更大的 L1-norm 下降，从而更有效地提升 acceptance rate。

- **提出的指标**：  
  定义了一个新的 **flatness** 指标，通过计算目标模型输出分布 $ p $ 与均匀分布 $ U $ 之间的 **cosine similarity** 来量化其平坦程度：
  $$
  \text{flatness}(t) = \cos(p_t, U)
  $$
  值越大表示分布越接近均匀，token 的训练价值越高。

- **提出的方法**：  
  基于上述指标，提出了 **Sample-level-flatness-based Dataset Distillation (SFDD)** 方法：
  1. 对每个训练样本，计算其所有 token 的 flatness 平均值，得到 **sample-level flatness**。
  2. 根据该分数对所有样本排序，保留分数最高的前 $ k\% $ 样本。
  3. 仅用这些高价值样本来训练 draft model。

### **相比现有方法的优势**

- **高效性**：  
  仅需 50% 的数据即可实现接近全量数据训练的性能，训练时间减少超过 **2×**。
- **有效性**：  
  在相同数据保留率下，SFDD 显著优于基于 **entropy**, **top-1 probability**, **margin**, **Energy Score**, **PPL** 等常见指标的选择方法。
- **正交性**：  
  SFDD 是一种数据选择策略，可与任何基于训练的 SD 框架（如 EAGLE）结合，不增加部署复杂性。
- **离线计算**：  
  flatness 分数仅依赖于固定的 target model，可在训练前一次性离线计算，无额外动态开销。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **训练数据集**：`ShareGPT` 数据集（经过过滤）。
- **评估任务**：在五个下游任务上进行测试：
  - `GSM8K` (数学推理)
  - `Alpaca` (指令遵循)
  - `MT-Bench` (多轮对话)
  - `CNN/DM` (摘要生成)
  - `Natural Questions (NQ)` (问答)

### **实验设置**

- **框架**：基于 **EAGLE-2** 的训练流程。
- **Target Model**：`LLaMA3-8B-Instruct`。
- **Draft Model**：轻量级单层 Transformer。
- **硬件**：NVIDIA H800 GPUs。
- **超参数**：
  - 训练 30 个 epoch
  - 学习率 $5 \times 10^{-5}$
  - Batch size 为 2
  - 解码温度 (decoding temperature) 默认为 1.0，部分实验在 0 下测试。

### **评估指标**

| 指标 | 说明 |
|------|------|
| **Speedup** | 自回归解码耗时 / SD 解码耗时。越高越好。 |
| **Average acceptance length ($\bar{l}$)** | 每次验证周期平均接受的 draft token 数量。越高越好。 |
| **Training time** | 总训练耗时（秒），用于衡量训练效率。 |

### **基线方法对比**

- **No Filter**：使用全量数据训练（基线）。
- **Random Filtering**：随机保留 $k\%$ 数据。
- **其他重要性指标**：
  - **Entropy** (熵)
  - **Top-1 Probability** (最高概率)
  - **Margin** (前两名概率差)
  - **Energy Score**
  - **Perplexity (PPL)**

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

在 **50% 数据保留率** 下，SFDD 取得了接近全量数据训练的性能：

- **平均 Speedup**：**2.41×**（全量数据为 2.49×），性能损失仅 **<4%**。
- **训练速度提升**：训练时间从 58,227 秒降至 28,787 秒，实现 **2.02×** 的训练加速。
- **数据选择开销**：SFDD 的离线数据筛选耗时 2,242 秒，占全量训练时间约 3.85%，可忽略不计。

### **与基线方法的对比结果**

| 方法 | 平均 Speedup | 平均 $\bar{l}$ |
|------|--------------|----------------|
| **No Filter** | 2.49× | 2.78 |
| **Random Filtering** | 2.20× | 2.46 |
| **Entropy** | 2.20× | 2.49 |
| **Top-1 Probability** | 2.23× | 2.49 |
| **SFDD (Ours)** | **2.41×** | **2.56** |

- SFDD 在所有任务上的 **Speedup** 和 **$\bar{l}$** 均显著优于其他基线。
- 在 **70% 数据保留率** 下，SFDD 的性能已基本与全量数据持平，甚至在 `Alpaca` 上略有超越。

### **消融实验结果**

- **不同保留率下的表现**（Table 2）：
  - SFDD 在 **30% ~ 70%** 的保留率下均大幅领先 Random 和 Top-1 Probability。
  - 即使在 **20%** 数据下，SFDD 仍能保持 2.23× 的平均 Speedup。

- **极端低数据场景**（Table 3）：
  - 在 **5%** 数据保留率下，SFDD 仍能实现 **1.82×** 的平均 Speedup，优于 Random 的 1.68×，证明 flatness 指标的鲁棒性。

- **聚合方式消融**（Table 11）：
  - 使用 **median** 聚合 token-level flatness 得到 sample-level flatness，性能与 **mean** 聚合同等，表明方法对聚合策略不敏感。

- **跨模型与跨数据集泛化**（Table 9 & 10）：
  - 在 `Vicuna-7B-v1.3` 和 `GSM8K` 训练集上验证，SFDD 依然有效，证明其通用性。

---

## 4. 关键结论和发现

### **主要发现**

1. **并非所有 token 都同等重要**：  
   目标模型输出分布 **平坦的 token**（flatness 高）是提升 SD acceptance rate 的关键驱动力，而尖锐分布的 token 贡献极小且迅速饱和。

2. **flatness 是有效的价值指标**：  
   基于 cosine similarity 的 flatness 指标能可靠预测 token 的训练潜力，优于 entropy 等传统不确定性度量。

3. **数据蒸馏可大幅提升训练效率**：  
   通过 SFDD 过滤掉低价值样本，可在几乎不损失推理加速效果的前提下，将训练成本减半以上。

4. **训练已成为 SD 不可忽视的成本**：  
   随着 SD 方法向多阶段、RL-style 训练发展（如 EAGLE-3, GTO），训练成本日益增加，优化训练效率变得至关重要。

### **方法的局限性**

- **依赖于目标模型**：flatness 计算依赖于一个固定的 target model，若 target model 发生变化，需重新计算。
- **未考虑上下文动态性**：flatness 是静态评分，未建模 draft model 在训练过程中的动态学习过程。
- **样本级过滤**：目前是样本级过滤，无法在序列内部进行细粒度的 token 级跳过（因当前框架限制）。

### **未来工作方向**

- 将 flatness 指标推广到其他 **LLM 加速架构**（如 Lookahead Decoding, Chunked Decoding）。
- 探索 **动态选择策略**，在训练过程中根据 draft model 的状态调整数据权重。
- 若未来训练框架支持，研究 **token-level flatness filtering** 以进一步提升效率。
- 将该思想应用于 **其他模型压缩技术**，如更高效的 KD 或 pruning。

--- 

> **代码开源**：https://anonymous.4open.science/r/Flatness

</details>

---

### 4. [OWLEYE: Zero-Shot Learner for Cross-Domain Graph Data Anomaly Detection](https://arxiv.org/abs/2601.19102)

**Authors**: Lecheng Zheng, Dongqi Fu, Zihao Li, Jingrui He  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.19102v1  

#### Abstract
Graph data is informative to represent complex relationships such as transactions between accounts, communications between devices, and dependencies among machines or processes. Correspondingly, graph anomaly detection (GAD) plays a critical role in identifying anomalies across various domains, incl...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# OWLEYE: Zero-Shot Learner for Cross-Domain Graph Data Anomaly Detection 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **Graph Anomaly Detection (GAD)** 方法大多遵循“one model for one dataset”范式，难以泛化到未见过的图数据。新兴的“one-for-all”通用模型（如 ARC 和 UNPrompt）虽然尝试跨域检测，但仍面临以下三大挑战：
1. **跨域特征对齐困难**：不同领域图数据的节点特征维度和语义差异大，现有方法（如 PCA/SVD）无法有效对齐且会破坏原始结构模式。
2. **缺乏持续学习能力**：现有模型不支持在不重新训练的情况下增量更新正常/异常模式。
3. **依赖少量标签进行少样本学习**：实际场景中获取标注成本高，零样本（zero-shot）检测更具现实意义。

### 提出的新方法：OWLEYE
作者提出 **OWLEYE**，一个全新的零样本跨域图异常检测框架，具备以下三项核心创新：

#### （1）Cross-Domain Feature Alignment Module
- 利用成对节点距离统计量进行特征归一化，将不同图嵌入到共享输入空间。
- 在对齐过程中保留领域特定语义，避免 UNPrompt 等方法破坏 Normal-Normal 密度关系的问题。

#### （2）Multi-Domain Multi-Pattern Dictionary Learning
- 构建动态字典，存储来自多个源图的属性级（attribute-level）和结构级（structure-level）正常行为模式。
- 支持无缝集成新图的知识，实现**持续学习**（continual learning），无需重训即可提升性能。

#### （3）Truncated Attention-Based Reconstruction
- 设计基于截断注意力机制的重构模块，在无监督情况下鲁棒地识别异常节点。
- 过滤潜在异常节点作为伪支持集，仅利用最代表性的正常节点进行重构，增强推理稳定性。

### 相比现有方法的优势
| 维度 | OWLEYE | ARC / UNPrompt | 传统方法 |
|------|--------|----------------|----------|
| 跨域对齐 | ✅ 保留结构模式 | ❌ 破坏密度关系或分离图 | 不适用 |
| 持续学习 | ✅ 可动态扩展字典 | ❌ 固定模型参数 | ❌ |
| 零样本能力 | ✅ 完全无需目标域标签 | ⚠️ 通常需 few-shot 标签 | ❌ |
| 泛化性 | 强 | 中等 | 弱 |

---

## 2. 核心实验方法和设置

### 数据集
- **训练集 $ \mathcal{T}_{\text{train}} $**: PubMed, CiteSeer, Questions, YelpChi  
  （涵盖社交网络、引用网络、电商评论等多领域）
- **测试集 $ \mathcal{T}_{\text{test}} $**: Cora, Flickr, ACM, BlogCatalog, Facebook, Weibo, Reddit, Amazon  
  （完全未见的图结构与特征分布）

> 所有图均含注入或真实异常，验证跨域迁移能力。

### 实验设置与评估指标
- **任务设定**：Zero-shot 和 10-shot setting
- **评估指标**：
  - **AUPRC**（Area Under Precision-Recall Curve）——重点关注，因异常稀疏
  - **AUROC**（Area Under ROC Curve）
- **报告方式**：5次运行平均值 ± 标准差

### 基线方法对比
| 类别 | 方法 |
|------|------|
| **Supervised (10-shot)** | BWGNN, GHRN |
| **Unsupervised (zero-shot)** | DOMINANT, SLGAD, TAM, CARE |
| **One-for-all (zero-shot)** | ARC, UNPrompt |
| **Finetuned versions** | DOMINANT, TAM, CARE, ARC, UNPrompt, OWLEYE 在 10-shot 下微调 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（AUPRC 平均值）

| 方法 | Average AUPRC (Zero-Shot) | Average AUPRC (10-Shot) |
|------|----------------------------|----------------------------|
| **OWLEYE (Ours)** | **36.17%** | **36.73%** |
| ARC | 30.74% | 31.68% |
| CARE | 28.72% | 30.74% |
| DOMINANT | 24.46% | 26.02% |
| UNPrompt | 12.82% | 13.51% |

> ✅ OWLEYE 在零样本下比最佳基线 **ARC 提升超过 5% AUPRC**

### 与基线方法的关键对比发现
1. **即使提供标签，OWLEYE 仍优于监督方法**：
   - 在 6/8 测试图上超越 BWGNN 和 GHRN（均为 10-shot）。
2. **在 10-shot 设置下达到 SOTA**：
   - 平均 AUPRC 达 36.73%，领先第二名近 5%。
3. **显著优于其他 zero-shot 方法**：
   - 比 UNPrompt 提升超 **23% AUPRC**，显示更强的零样本泛化能力。

### 消融实验结果（Ablation Study）
在 8 个测试图上的平均 AUPRC 对比如下：

| 方法变体 | AUPRC | 相对下降 |
|---------|-------|----------|
| **OWLEYE (Full)** | **36.17%** | — |
| OWLEYE-N（无特征归一化） | 35.13% | ↓1.04% |
| OWLEYE-S（无结构模式） | 35.06% | ↓1.11% |
| OWLEYE-T（标准注意力） | 34.08% | ↓2.09% |

> 🔍 结果表明三个组件均有效，尤其是**截断注意力机制**贡献最大。

此外：
- **字典大小影响**：当 `nsup` ≥ 200 后性能趋于饱和，说明小而精的模式库已足够。
- **多图模式增益**：相比仅用目标图自身模式，引入多源图模式使 AUPRC 提升 **0.32%**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **有效的跨域特征对齐是关键**：简单的 PCA + 基于距离的归一化能更好保持图内结构模式（如 Normal-Normal 密度），优于 ARC 和 UNPrompt。
2. ✅ **结构模式独立建模至关重要**：仅依赖属性可能导致伪装异常（camouflaged anomalies）被误判；结构表示可有效区分角色差异。
3. ✅ **截断注意力提升鲁棒性**：通过过滤低相似度节点，防止异常污染重构过程。
4. ✅ **字典机制支持持续演化**：新增图的模式可直接加入字典，无需重训即提升性能（见 Case Study 1）。
5. ✅ **零样本优于监督方法**：在多数图上，OWLEYE（zero-shot）表现优于 BWGNN/GHRN（10-shot），证明其强大泛化能力。

### 方法的局限性
1. **高度领域偏移时性能下降**：
   - 当训练集中缺失整个领域（如移除 YelpChi 影响 Amazon），AUPRC 下降约 **4.6%**，虽仍可用但提示依赖跨域共性。
2. **极端异常率可能影响效果**：
   - 尽管在合成高异常率下仍优于基线，但极端不平衡场景仍具挑战。
3. **字典规模受限于内存与效率权衡**：
   - 虽然性能随字典增大趋稳，但过大字典可能影响推理速度。

### 未来工作方向
1. **Multi-modal Graph Foundation Models**：
   - 探索融合文本、时间序列、图像等多模态信号的统一图基础模型。
2. **Test-Time Adaptation for Heavy Domain Shift**：
   - 开发无需额外标注的测试时自适应策略，应对训练未见领域。
3. **Dynamic Pattern Evolution Modeling**：
   - 建模正常模式的时间演化，用于动态图中的异常检测。
4. **Explainable Anomaly Reasoning via Attention Maps**：
   - 利用注意力可视化解释为何某节点被判为异常，增强可信度。

---

> 📌 **总结**：OWLEYE 是首个真正实现 **zero-shot + continual learning + robust cross-domain alignment** 的图异常检测框架。其实验充分验证了结构化知识存储（pattern dictionary）与注意力驱动重构的有效性，为构建可扩展、免标注的通用图异常检测系统奠定了坚实基础。

</details>

---

### 5. [Length-Adaptive Interest Network for Balancing Long and Short Sequence Modeling in CTR Prediction](https://arxiv.org/abs/2601.19142)

**Authors**: Zhicheng Zhang, Zhaocheng Du, Jieming Zhu, Jiwei Tang, Fengyuan Lu, Wang Jiaheng, Song-Li Wu, Qianhui Zhu, Jingyu Li, Hai-Tao Zheng, Zhenhua Dong  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.19142v1  

#### Abstract
User behavior sequences in modern recommendation systems exhibit significant length heterogeneity, ranging from sparse short-term interactions to rich long-term histories. While longer sequences provide more context, we observe that increasing the maximum input sequence length in existing CTR models...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Length-Adaptive Interest Network for Balancing Long and Short Sequence Modeling in CTR Prediction**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代推荐系统中的用户行为序列存在显著的**长度异质性**（length heterogeneity）：  
- 长序列用户拥有丰富的历史交互（如数百次点击），可提供丰富上下文；
- 短序列用户（如新用户或低活跃用户）仅有少量行为。

然而，作者通过实证分析发现一个反直觉现象：**当增加最大输入序列长度时，长序列用户的性能提升，但短序列用户的性能反而下降**。这种“**长度不平衡**”（length imbalance）导致模型在训练中偏向于高频、长序列用户，而牺牲了占多数的短序列用户体验。

该问题源于两个关键因素：
- **Attention Polarization（注意力极化）**：在长序列中，softmax 注意力倾向于过度集中在少数显著行为上；而在短序列中，这种集中会放大噪声，导致兴趣表示不稳定。
- **Length Signal Deficiency（长度信号缺失）**：现有 CTR 模型将行为序列视为同质事件集合，未显式利用“序列长度”这一反映用户状态的重要先验信息。

### 🚀 提出的新方法：LAIN（Length-Adaptive Interest Network）
为解决上述问题，作者提出 **LAIN** ——一种轻量级、即插即用的框架，**显式地将序列长度作为条件信号**，自适应调整建模策略。

#### 核心组件：
1. **Spectral Length Encoder (SLE)**  
   将原始长度 $ L $ 映射为连续向量表示，采用可学习的傅里叶基进行编码（inspired by NeRF），避免离散化损失，捕捉平滑的长度语义。

2. **Length-Conditioned Prompting (LCP)**  
   基于长度嵌入生成软提示 token，并前置到长短序列的行为输入中，引导模型注入全局用户状态信息。

3. **Length-Modulated Attention (LMA)**  
   动态调节注意力机制：
   - **Query-Key Conditioning**：将长度嵌入拼接到 query 和 key 中；
   - **Temperature Scaling**：根据长度动态调整 softmax 温度：
     $$
     T = 1 + \text{sigmoid}(-\beta(L - L_0)) \cdot \gamma
     $$
     当 $ L $ 较小时增大温度以平滑注意力分布，防止过拟合；$ L $ 大时降低温度增强聚焦能力。

### 🔍 相比现有方法的优势
| 维度 | LAIN 的优势 |
|------|-------------|
| **通用性** | 可无缝集成到主流 CTR 模型（DIN、DIEN、SIM、SDIM、TWIN等）中，无需架构重构 |
| **公平性** | 显著提升短序列用户预测精度，缓解因数据分布偏斜造成的推荐偏差 |
| **效率** | 引入参数 <1.5%，推理开销仅增加约 2.3%，适合工业部署 |
| **理论动机强** | 从 attention polarization 和 gradient conflict 角度系统诊断问题，设计有针对性解决方案 |

---

## 2. 核心实验方法和设置

### 📊 数据集
在三个真实世界的公开 CTR 数据集上验证方法有效性：

| 数据集 | 领域 | 用户数 | 物品数 | 平均序列长度 |
|--------|------|--------|--------|--------------|
| **EBNeRD-small** | 新闻推荐 | 18,828 | 20,739 | 147 |
| **KuaiVideo** | 短视频推荐 | 10,000 | 3,239,534 | 278 |
| **MicroVideo1.7M** | 微视频推荐 | 10,951 | 1,704,880 | 148 |

所有数据集均呈现明显的**长尾分布**：短序列用户占比高但训练样本少，长序列用户样本密集。

### ⚙️ 实验设置
- **最大序列长度**：统一设为 1000，以支持长序列建模
- **特征维度**：embedding dim = 64
- **优化器**：Adam，lr = 0.001，早停基于验证集 AUC
- **Head 结构**：MLP + ReLU + Dropout(0.2)
- **LAIN 参数配置**：
  - Fourier dimension $ d_f = 32 $
  - Prompt token 数量 $ k = 4 $
  - MLP hidden dim = 512
  - Temperature 初始值 $ \gamma=0.5, \beta=0.01 $

### 📈 评估指标
| 指标 | 含义 |
|------|------|
| **AUC** | 衡量整体排序质量，越高越好 |
| **GAUC** | 分用户计算 AUC 再平均，减少高流量用户主导影响 |
| **LogLoss** | 衡量概率校准程度，越低越好 |

同时按序列长度分组（<100, 100–200, ≥200）进行细粒度分析。

### 🆚 基线方法对比
选择五类代表性 CTR 模型作为 backbone 进行增强比较：
1. **DIN**：target-aware attention
2. **DIEN**：GRU + attention 建模兴趣演化
3. **SIM**：两阶段架构（GSU + ESU）
4. **SDIM**：multi-head attention 扩展 SIM
5. **TWIN**：双塔结构建模超长序列

所有基线均使用 FuxiCTR 框架实现，保证特征工程与超参一致。

---

## 3. 主要实验结果和性能指标

### 📈 整体性能提升（见 Table 3）
LAIN 在所有 backbone 和数据集上均带来稳定增益：

| 指标 | 最大相对提升 |
|------|------------|
| **AUC** | **+1.15%**（TWIN on MicroVideo1.7M） |
| **LogLoss** | **-2.25%**（DIEN on EBNeRD-small） |
| **GAUC** | **+1.19%**（TWIN on EBNeRD-small） |

> ✅ 特别是在 **TWIN** 上效果最显著，说明 LAIN 对两阶段 attention 架构中的长度失衡问题尤为有效。

### 🔍 长短序列用户性能对比（见 Table 4）
对 TWIN 在 MicroVideo1.7M 上进一步分组测试显示：

| 序列长度 | AUC 提升 | LogLoss 下降 |
|---------|----------|-------------|
| **<100（短序列）** | **+1.08%** | **-2.17%** |
| **100–200（中序列）** | +0.50% | -1.26% |
| **≥200（长序列）** | +1.58% | -1.74% |

> 💡 **关键发现**：LAIN 不仅没有牺牲长序列性能，反而同步提升了两类用户的预测准确性，真正实现了“平衡建模”。

### 🧪 消融实验（Ablation Study，见 Table 6）
在 TWIN + MicroVideo1.7M 上移除各模块的结果表明：

| 变体 | AUC | ΔAUC |
|------|-----|-------|
| **LAIN (Full)** | 0.7233 | — |
| w/o LCP（无提示） | 0.7228 | -0.05% |
| w/o Query-Key Conditioning | 0.7195 | -0.38% |
| w/o Temperature Scaling | 0.7212 | -0.21% |
| w/o LMA（完整移除） | 0.7189 | **-0.44%** |
| w/o Short-term Branch | 0.7226 | -0.07% |

> 🔎 **结论**：
- **LMA 贡献最大**，尤其是 query-key conditioning 与 temperature scaling 协同作用明显；
- **LCP 有助于全局状态注入**；
- 即使在短序列分支也需应用 LAIN，体现其普适价值。

### 📉 注意力极化缓解（见 Table 5）
通过 Gini 系数衡量 attention 分布集中度：

| 模型配置 | Gini（短序列） | Gini（长序列） |
|----------|----------------|----------------|
| Baseline (L=1000) | 0.346 | 0.352 |
| **LAIN** | **0.318** | **0.321** |

> ✅ LAIN 显著降低了所有长度组的注意力极化程度（Gini ↓），尤其改善了短序列下的 over-concentration 问题。

### 🔄 超参数敏感性分析（见 Figure 4）
在不同 Fourier dimension、hidden size、prompt 数量组合下，AUC 均稳定优于 baseline（0.7158 → 0.7216~0.7248），证明方法鲁棒性强。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **序列长度不仅是统计量，更是重要用户状态信号**：应被显式建模用于指导 interest extraction。
2. **当前 CTR 模型存在“长度诱导偏差”**（length-induced bias）：训练过程中隐式偏好长序列模式，损害短序列泛化。
3. **LAIN 成功解耦了长短序列的优化冲突**：通过条件化 prompt 和 attention 调制，实现动态适应。
4. **方法具有高度通用性和实用性**：适用于多种 backbone，且几乎无额外部署成本。

### ⚠️ 局限性
- 当前仅考虑**单一长度信号**，未融合其他用户元信息（如注册时间、活跃周期等）；
- Prompt 设计为静态数量（如 k=4），未探索长度自适应 prompt 长度；
- 在极端稀疏场景（如长度 ≤ 5）的效果仍待深入研究。

### 🔮 未来工作方向
- 探索多维度用户状态建模（结合 length + activity level + novelty）；
- 将 LAIN 思路扩展至 item-side 序列建模；
- 在 online learning 场景中动态更新 length-aware 参数；
- 探索更高效的 prompt compression 技术以应对大规模服务延迟要求。

---

## ✅ 总结
本文提出的 **LAIN** 是首个系统性解决 CTR 预测中“长短序列建模失衡”问题的工作。它不仅揭示了 attention polarization 和 length signal deficiency 两大机制，还提供了轻量、高效、可插拔的解决方案，在多个真实数据集上验证了其对整体性能及短序列用户的显著提升，为构建更公平、更具包容性的推荐系统提供了新范式。

</details>

---

### 6. [EPAS: Efficient Training with Progressive Activation Sharing](https://arxiv.org/abs/2601.19089)

**Authors**: Rezaul Karim, Maryam Dialameh, Yang Liu, Boxing Chen, Walid Ahmed  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19089v1  

#### Abstract
We present a novel method for Efficient training with Progressive Activation Sharing (EPAS). This method bridges progressive training paradigm with the phenomenon of redundant QK (or KV ) activations across deeper layers of transformers. EPAS gradually grows a sharing region during training by switc...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：EPAS: Efficient Training with Progressive Activation Sharing

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大模型在训练和推理阶段均面临高计算成本与内存开销问题。尽管已有研究利用 **cross-layer activation sharing**（如共享 QK 或 KV）来提升效率，但这些方法通常存在以下局限：
- 多数仅在推理阶段应用激活共享，无法优化训练效率；
- 直接从头训练激活共享模型易导致显著的精度下降；
- 依赖复杂的知识蒸馏（knowledge distillation）或多轮训练流程，增加实现复杂度。

### 🚀 提出的新方法：EPAS（Efficient training with Progressive Activation Sharing）
EPAS 是一种将 **progressive training** 与 **activation sharing** 结合的统一高效训练框架，其核心思想是：
- 在训练过程中逐步将深层 decoder 层切换至 **activation sharing mode**，形成一个从深到浅逐渐扩展的“共享区域”；
- 使用 **switchable decoder block** 实现热切换（hot-switching），允许模型动态选择是否复用前一层的 QK 或 KV 激活；
- 支持在单次端到端训练中生成一系列可变长度共享配置的子模型（many-in-one model）。

### 🔍 相比现有方法的优势
| 维度 | EPAS | 传统方法 |
|------|------|----------|
| **训练效率** | 显著提升训练吞吐量（up to 11.1%↑） | 多数不优化训练过程 |
| **推理效率** | 推理吞吐最高提升达 29% | 仅在推理时生效 |
| **精度保持** | 几乎无损，loss 曲线与 baseline 对齐 | 蒸馏失败则精度大幅下降 |
| **部署灵活性** | 单一模型支持多种共享层数配置 | 固定架构 |
| **迁移能力** | 可用于 continual pretraining 将预训练模型转为高效结构 | 需重新训练或复杂蒸馏 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 主要使用开源数据集 **SlimPajama-627B** 的子集进行 pretraining 和 continual pretraining；
- 实验聚焦于效率分析而非完整收敛，因此采用小规模 token 数量（例如 1B~4B tokens）以加速验证。

### ⚙️ 实验设置
| 项目 | 设置说明 |
|------|---------|
| **模型范围** | LLaMA 架构系列：125M, 1.1B, 3B, 7B 参数模型；主实验基于 TinyLLaMA-1.1B |
| **硬件平台** | 多设备对比：Nvidia V100 GPU、Ascend 910A NPU、Ascend 910B NPU |
| **训练方式** | 分布式训练（8-device），batch size per device = 8，gradient accumulation steps = 16 |
| **共享策略** | QK sharing（兼容 Flash-Attention），共享区域从最深层开始逐层向前增长 |
| **调度机制** | 每隔 I 步切换 B 层进入 sharing mode，最终目标为最多 S 层参与共享（如 25% 或 50%） |

### 🎯 评估指标
| 类型 | 指标 |
|------|------|
| **训练效率** | Training Throughput (tokens/sec)、Total Training Time、Loss Curve 收敛速度 |
| **推理效率** | Inference Throughput (tokens/sec) |
| **模型性能** | Validation Loss、LM Benchmark Accuracy（PIQA, BoolQ, ARC, OBQA 等） |
| **理论节省** | FLOPs Reduction（理论计算量降低百分比） |

### 🔁 基线方法对比
- **Baseline**: 原始 LLaMA/TinyLLaMA 模型（无 activation sharing）；
- **ShareAttn / Beyond-KV-Cache [10]**: 推理阶段应用 activation sharing 的代表方法；
- **EchoAtt [19]**: 基于 distillation 的 QK sharing 方法；
- **YOLO [20]**: KV sharing 方法；
- **消融实验设计**：比较不同共享策略（单一大块 vs 多个小块）、是否包含最后一层等。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

#### （1）训练效率提升
| 模型 | FLOPs 降低 | 训练吞吐提升（V100） | 总训练时间减少 |
|------|------------|------------------------|----------------|
| 125M | 4.9% | +10.8% | 54min → 51min |
| 1.1B | 4.3% | **+11.1%** | 3h7min → 2h56min |
| 3B  | 7.3% | +10.9% | 6h3min → 5h39min |
| 7B  | 8.1% | +8.6%  | 11h9min → 10h25min |

> ✅ 所有模型最终 validation loss 差异 < 0.05，表明精度几乎无损。

#### （2）跨硬件一致性
| 设备 | 吞吐提升幅度 |
|------|---------------|
| V100 GPU | +11.1% |
| 910A NPU | +9.57% |
| 910B NPU | +7.3% |

> 💡 表明 EPAS 的效率增益具有良好的硬件泛化性。

#### （3）推理效率提升（QK sharing）
| 模型 | Baseline (tok/s) | 25% Sharing | 50% Sharing |
|------|------------------|-----------|-----------|
| 125M | 63.8 | 78.3 (**+22.7%**) | 82.4 (**+29.2%**) |
| 1.1B | 39.1 | 42.9 (+9.7%) | 44.8 (+14.6%) |
| 3B   | 38.1 | 40.9 (+7.3%) | 43.2 (+13.4%) |
| 7B   | 24.46 | 25.27 (+3.3%) | 26.03 (+6.4%) |

> ✅ 最高推理吞吐提升达 **29%**，尤其在小模型上更显著。

#### （4）语言模型任务表现（TinyLLaMA continual pretraining）
| 配置 | Average Accuracy |
|------|------------------|
| w/o EPAS（no sharing） | 56.70 |
| w/ EPAS（no sharing） | **57.07** |
| w/o EPAS（5/22 sharing） | 44.99 |
| w/ EPAS（5/22 sharing） | **54.47**（↑ ~10%） |

> ✅ EPAS 训练的模型在 activation sharing 模式下仍能保持高准确率，而直接应用 sharing 到普通模型会导致严重性能退化。

### 🔍 消融实验结果
| 实验项 | 发现 |
|-------|------|
| **单一大块 vs 多个小块共享** | 单一大块（single large block）优于多个小块（multiple small blocks），简化结构且性能更高（见 Table 8） |
| **是否包含最后一层** | 包含最后一层略优（+~0.5% avg），故推荐纳入共享区 |
| **共享类型** | QK sharing 更利于计算减负；KV sharing 更省内存，但本文以 QK 为主（兼容 Flash-Attention） |

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **冗余存在于深层 attention 激活中**，可通过 progressive activation sharing 安全利用而不牺牲精度；
2. **渐进式训练策略至关重要**：相比一次性启用所有共享层，逐步扩展共享区域可显著提高稳定性与最终性能；
3. **EPAS 实现训练与推理双优**：既加快训练速度（throughput ↑11.1%），又提升推理效率（↑29%），同时保持 loss 和 accuracy 基本不变；
4. **支持灵活部署**：单一 EPAS 训练模型可在推理时选择不同数量的共享层，适应多样化的 compute budget；
5. **可用于 continual pretraining**：无需 distillation，即可将已有预训练模型高效转化为 activation-sharing 架构。

### ⚠️ 方法的局限性
- 当前主要验证基于 **LLaMA/TinyLLaMA** 架构，尚未广泛测试其他结构（如 encoder-decoder）；
- 共享机制目前集中在 decoder 层，未探索 encoder 内部或跨模态场景；
- 虽然支持多种 activation sharing（QK/KV），但实验集中于 QK，对 KV 的优化潜力有待深入挖掘；
- 对 extremely long context 的影响尚未系统评估。

### 🔮 未来工作方向
- 扩展至 vision transformer 和 speech models 中的 activation sharing；
- 探索 post-training adaptation 场景下的 EPAS 应用；
- 动态调整共享区域大小（adaptive scheduling）以响应输入复杂度变化；
- 结合量化、稀疏化等技术构建更全面的高效训练范式；
- 研究 activation sharing 对模型鲁棒性和泛化能力的影响。

---

## 总结
EPAS 提出了一种新颖且实用的 **progressive activation sharing** 框架，成功打通了高效训练与高效推理之间的鸿沟。通过在训练初期保留完整计算路径，并逐步引入 activation sharing，EPAS 不仅提升了训练吞吐和推理速度，还避免了传统方法中的精度崩溃问题。其实验充分验证了该方法在多个模型尺度和硬件平台上的有效性，为构建“many-in-one”的高效大模型提供了强有力的技术路径。

</details>

---

### 7. [GPCR-Filter: a deep learning framework for efficient and precise GPCR modulator discovery](https://arxiv.org/abs/2601.19149)

**Authors**: Jingjie Ning, Xiangzhen Shen, Li Hou, Shiyi Shen, Jiahao Yang, Junrui Li, Hong Shan, Sanan Wu, Sihan Gao, Huaqiang Eric Xu, Xinheng He  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.19149v1  

#### Abstract
G protein-coupled receptors (GPCRs) govern diverse physiological processes and are central to modern pharmacology. Yet discovering GPCR modulators remains challenging because receptor activation often arises from complex allosteric effects rather than direct binding affinity, and conventional assays...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*GPCR-Filter: a deep learning framework for efficient and precise GPCR modulator discovery*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
G蛋白偶联受体（**GPCRs**）是现代药理学中最重要的药物靶点之一，约36%的FDA批准药物作用于GPCRs。然而，发现有效的GPCR调节剂（modulator）仍面临巨大挑战，主要原因包括：
- GPCRs具有高度动态的构象变化和变构效应（allosteric effects），导致**配体结合与功能激活解耦**；
- 传统计算筛选方法（如基于结构的对接）难以捕捉这些复杂的动态关系；
- 实验验证成本高、通量低，且现有AI模型多聚焦于通用的**Drug-Target Interaction (DTI)** 预测，缺乏对GPCR特异性机制的敏感性。

### 🚀 提出的新方法与创新思路
作者提出了 **GPCR-Filter** ——一个专为GPCR调节剂发现设计的深度学习框架，其核心创新包括：

1. **构建高质量、大规模的GPCR-ligand数据集**  
   整合来自 **GPCRdb** 和 **GtoPdb** 的实验验证数据，共收集 **91,396个**人类GPCR-配体相互作用对，涵盖527个独特GPCRs和72,177个不同配体，标准化为氨基酸序列和SMILES格式。

2. **融合蛋白质语言模型与图神经网络的双模态架构**  
   - 使用预训练的 **ESM-3 蛋白质语言模型** 编码GPCR序列，获得残基级（per-residue）表示；
   - 使用 **Graph Neural Network (GNN)** 编码配体的分子图结构；
   - 引入 **注意力机制（cross-attention）** 实现受体-配体特征融合，学习功能性交互模式，而不仅仅是结合亲和力。

3. **专注于“功能调节”而非“简单结合”的预测能力**  
   模型目标是识别能引发下游信号的功能性调节剂（如激动剂），而非仅预测物理结合，更贴近真实药物发现需求。

### 🔍 相比现有方法的优势
| 方面 | GPCR-Filter | 现有DTI模型（如TransformerCPI2.0, ConPLex） |
|------|-------------|----------------------------------------------|
| **任务针对性** | 专为GPCR设计，考虑其功能复杂性 | 通用DTI模型，未针对GPCR优化 |
| **泛化能力** | 在跨靶点（inter-target）场景下表现优异 | 泛化能力弱，尤其在新靶点上接近随机 |
| **可解释性** | 注意力权重集中在已知结合口袋残基 | 缺乏明确生物学解释 |
| **输入依赖** | 仅需序列 + SMILES，无需三维结构 | 部分依赖结构信息或无法处理新靶点 |

---

## 2. 核心实验方法和设置

### 📊 数据集
- **来源**：GPCRdb 和 GtoPdb
- **规模**：91,396个实验验证的GPCR-ligand对
- **GPCRs**：527个（UniProt标准序列）
- **Ligands**：72,177个（标准化SMILES）
- **负样本构造**：通过枚举所有可能组合并去除已知阳性，进行1:1负采样以平衡类别

### ⚙️ 实验设置
采用三种评估协议来全面测试模型的泛化能力，难度递增：

| 设置 | 描述 | 目标 |
|------|------|------|
| **Random Split** | 所有样本按80/10/10随机划分 | 测试分布内性能（in-distribution） |
| **Intra-target Split** | 每个GPCR出现在训练和测试集中，但配体互斥 | 测试对同一靶点的新配体泛化能力 |
| **Inter-target Split** | 训练与测试的GPCR完全不重叠（9:1划分） | 测试对全新靶点的跨靶点泛化能力 |

### 📈 评估指标
- **AUC**（ROC曲线下面积）
- **AP**（Average Precision）
- **ACC**（Accuracy）
- **Precision**
> 所有指标越高越好；AUC和AP为阈值无关指标，更具鲁棒性。

### 🆚 基线方法对比
- **ConPLex**：基于对比学习的DTI模型，使用PLex蛋白编码器
- **TransformerCPI2.0**：结合TAPE-BERT和GNN的序列级DTI模型
- 所有模型均使用相同的输入（GPCR序列 + 配体SMILES）和超参数设置，确保公平比较

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（见Table 1）

| Method | Random AUC | Intra-target AUC | Inter-target AUC |
|--------|------------|------------------|------------------|
| **ConPLex** | 53.03% | 52.89% | **44.91%** |
| **TransformerCPI2.0** | 65.94% | 62.02% | 66.31% |
| **GPCR-Filter (Ours)** | **99.02%** | **97.98%** | **80.35%** |

> 注：AUC < 50% 表示模型表现不如随机猜测。

#### ✅ 主要发现：
- 在**Random Split**下，GPCR-Filter达到近天花板性能（AUC ~99%），显著优于基线；
- 在**Intra-target**设置中，仍保持极高判别力（AUC >97%），表明对新配体的强大泛化能力；
- 在最具挑战性的**Inter-target**任务中，GPCR-Filter以**80.35% AUC**远超TransformerCPI2.0（66.31%）和ConPLex（44.91%，已过拟合）；
- 同时在AP、Precision等指标上也全面领先。

### 🔍 可解释性分析（Interpretability）
- 对两个新解析的复合物（PDB: 9bsb, 9jcl）进行注意力可视化：
  - 模型关注的Top-20残基中有**6–8个位于晶体结构结合口袋内（<5Å）**；
  - 注意力模式在不同训练分割下保持一致，说明学到的是真实的结合位点特征，而非记忆；
  - 支持模型具备**机械可解释性（mechanistic interpretability）**。

### 💡 湿实验验证（Wet-lab Validation）
- 在**5-HT1A receptor**上进行虚拟筛选与实验验证：
  1. 从ChemDiv数据库中对接160万化合物，取前8,705个；
  2. 使用GPCR-Filter进一步筛选，选择97个预测概率 >0.5 的化合物；
  3. 成功采购并测试其中52个；
  4. **单浓度（30 μM）筛选中，4个化合物（D24, D29, D34, D47）表现出强激活效应**；
  5. 多浓度曲线显示均为**微摩尔级（micromolar-level）激动剂**，具有较高最大效应（Emax），虽EC50偏右移但仍具开发潜力。

> ✅ 这是首次通过纯序列+SMILES驱动的AI模型成功发现新型骨架的活性GPCR激动剂。

---

## 4. 关键结论和发现

### ✅ 主要结论
1. **GPCR-Filter显著优于现有DTI模型**，尤其在跨靶点泛化任务中展现出强大能力；
2. 模型不仅能预测结合，还能有效识别**功能性调节剂**，已在湿实验中验证发现多个**新型骨架的5-HT1A激动剂**；
3. 结合**蛋白质语言模型（ESM-3）与GNN + cross-attention**的设计，使模型能够捕捉GPCR特有的序列-功能关系；
4. 数据组织本身存在化学空间上的聚类结构（ligand-profile similarity clustering），为跨靶点迁移提供了潜在基础；
5. 注意力机制可解释性强，聚焦于真实结合口袋残基，支持其生物学合理性。

### ⚠️ 局限性
1. **负样本不可靠**：负例通过未观测到的组合生成，可能包含未知的真实阳性（false negatives）；
2. **Inter-target split中允许配体重用**：可能让某些化学母核提供“捷径信号”，削弱对真正新颖性的考验；
3. 尚未整合**三维结构信息或口袋约束**，未来可结合AlphaFold3或Docking提升精度；
4. 当前模型未区分激动剂/拮抗剂类型，仅预测“是否调节”。

### 🔮 未来工作方向
1. 扩展至其他GPCR家族及其他膜蛋白靶点；
2. 改进负样本策略，例如使用**decoy生成**或**对抗采样**；
3. 引入**结构感知模块**（structure-aware modeling），融合AF3预测结构或显式建模结合口袋；
4. 开发**多任务版本**，同时预测功能类型（agonist/antagonist）、信号通路偏好（G protein vs. arrestin bias）；
5. 探索**主动学习闭环系统**，将GPCR-Filter嵌入自动化药物发现流程。

---

## 总结
> **GPCR-Filter 是首个专为GPCR调节剂发现定制的深度学习框架，在数据质量、模型设计、泛化能力和实验验证四个维度均实现突破。它不仅在多项基准上大幅超越现有DTI模型，还成功指导发现了具有新化学骨架的微摩尔级激动剂，展示了AI在复杂信号系统药物发现中的巨大潜力。**

该工作标志着从“通用DTI预测”向“靶点家族特异性智能筛选”的重要转变，为下一代GPCR药物研发提供了高效、可扩展的计算工具。

</details>

---

### 8. [Variational Quantum Circuit-Based Reinforcement Learning for Dynamic Portfolio Optimization](https://arxiv.org/abs/2601.18811)

**Authors**: Vincent Gurgul, Ying Chen, Stefan Lessmann  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.18811v1  

#### Abstract
This paper presents a Quantum Reinforcement Learning (QRL) solution to the dynamic portfolio optimization problem based on Variational Quantum Circuits. The implemented QRL approaches are quantum analogues of the classical neural-network-based Deep Deterministic Policy Gradient and Deep Q-Network al...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Variational Quantum Circuit-Based Reinforcement Learning for Dynamic Portfolio Optimization**

---

## 1. **论文的主要贡献和创新点**

### ✅ **解决了什么问题**
该论文针对**动态投资组合优化**（Dynamic Portfolio Optimization）这一复杂、高维且非平稳环境下的序贯决策问题，指出传统静态方法（如 MVO）无法适应市场动态变化，而经典强化学习（RL）在大规模状态-动作空间中面临可扩展性和训练效率的挑战。

同时，尽管已有研究将量子计算用于投资组合优化，但大多局限于**静态 QUBO 问题**（如资产选择），缺乏对**动态、连续决策过程**的学习能力。

### ✅ **提出了什么新方法或新思路**
本文提出了一种基于 **Variational Quantum Circuits (VQCs)** 的全量子强化学习框架，实现了以下创新：

- **首次将 Quantum Reinforcement Learning (QRL)** 应用于**真实金融数据上的动态投资组合管理**。
- 设计了量子版本的 **Deep Deterministic Policy Gradient (DDPG)** 和 **Deep Q-Network (DQN)** 算法，其中**Actor 和 Critic 均由 VQC 构建**，形成端到端的量子策略网络。
- 引入**振幅编码**（Amplitude Encoding）结合非线性特征映射（`Z(x) = [x, x⊗², sin(x), cos(x)]`），提升输入表达能力并缓解“贫瘠高原”（barren plateaus）问题。
- 采用**期望值测量 + ℓ¹ 归一化**的方式输出合法的投资组合权重向量，支持多资产、长短仓配置。

### ✅ **相比现有方法的优势**
| 维度 | 优势 |
|------|------|
| **参数效率** | 仅需 **30–60 个可训练参数**即可达到与数万甚至十几万个参数的经典 Deep RL 模型相当甚至更优的风险调整收益表现。 |
| **模型表达力** | 利用量子纠缠和叠加态，在低参数下实现高表达能力，尤其适合处理高维金融时间序列。 |
| **鲁棒性** | 在不同市场周期下表现出更低的 Sharpe Ratio 波动，说明其策略更具稳定性。 |
| **理论前瞻性** | 展示了 QRL 在复杂现实任务中的可行性，为未来在金融及其他动态系统中的应用铺平道路。 |

---

## 2. **核心实验方法和设置**

### 📊 **使用的数据集**
- 包含 **15 种多样化资产**的日频收盘价，涵盖股票（AAPL, MSFT）、ETF（SPY, QQQ）、债券（TLT, LQD）、黄金（GLD）、能源（USO）等。
- 时间跨度：**2011年8月 至 2025年9月**，共 **5049个交易日**。
- 输入状态包含：
  - 过去 **30 天的价格滚动窗口**（lookback window）
  - 通过 **Auto ARIMA 模型预测的未来 7 天价格**（forecast window）

### ⚙️ **实验设置**
- **动作空间**：连续动作，表示每个资产的持仓权重 `w ∈ ℝ^15`，允许做空（short selling ≤ 100%）。
- **奖励函数**：风险调整回报  
  $$
  r_t = \mu_{t-L:t+F+1} - \lambda \cdot \sigma_{t-L:t+F+1}
  $$
  其中 λ 控制风险偏好。
- **交易成本**：每次调仓收取 **0.15%** 的手续费。
- **调仓频率**：每 30 天重新平衡一次。

### 🎯 **评估指标**
- 主要指标：**测试集上的平均 Sharpe Ratio**（已扣除交易成本）
- 辅助指标：
  - Sharpe Ratio 的标准差（衡量跨市场周期的稳健性）
  - 执行时间（inference latency）
  - 参数数量（parameter count）

### 🔁 **交叉验证设计**
采用 **7 折递增滚动窗口时间序列交叉验证**（increasing rolling-window time-series CV）：
- 每一折训练集逐步扩大，确保因果顺序。
- 验证集用于超参调优和早停。
- 测试集完全未见，用于最终评估。

### 🆚 **基线方法对比**
| 类别 | 方法 |
|------|------|
| **静态基准** | - Equal Weights（等权组合）<br> - Mean-Variance Optimization (MVO) |
| **经典 Deep RL** | - DDPG（13.5k / 27k / 160k 参数）<br> - DQN（13.5k / 27k / 160k 参数） |
| **本研究提出的 QRL** | - Quantum DDPG（30 / 60 参数）<br> - Quantum DQN（30 / 60 参数） |

所有模型共享相同的状态定义、奖励函数和环境逻辑，保证公平比较。

---

## 3. **主要实验结果和性能指标**

### 📈 **关键性能数据（来自 Table 5.1 和 Figure 5.1）**

| 模型 | Sharpe Ratio (mean) | Std. Dev. | 参数量 | 推理时间（GPU/QPU） |
|------|---------------------|-----------|--------|-----------------------|
| Equal Weights | 0.4375 | 0.6558 | – | <1s |
| MVO | 0.5919 | 0.9170 | – | <1s |
| Classical DDPG (13.5k) | 0.4287 | 0.6151 | 13.5k | 7s (GPU) |
| Classical DQN (160k) | **0.8237** | 0.5367 | 160k | 27s (GPU) |
| **Quantum DDPG (60 params)** | **0.7281** | **0.7395** | **60** | **<1s circuit exec**, ~23min (cloud QPU) |
| **Quantum DQN (60 params)** | 0.6537 | 0.6155 | 60 | <1s circuit exec, ~43min (cloud QPU) |

> 注：QPU 上报告的时间为“总作业耗时”，包括排队、初始化、多次 shot 测量等云平台开销。

### 🔍 **与基线方法的对比结果**
- **参数效率碾压级优势**：
  - 仅用 **60 个参数**的 QRL 模型，性能接近甚至超过 **160k 参数的经典 RL 模型**。
  - Quantum DDPG (60) 的 Sharpe Ratio 超过 MVO（0.728 vs 0.592），且波动更小。
- **超越小规模经典模型**：
  - 即使是 30 参数的 QRL 模型也优于大多数低容量经典模型（如 13.5k 参数的 DDPG）。
- **稳定性更强**：
  - QRL 模型在各 fold 中的 Sharpe Ratio 变异较小，表明其对市场 regime 变化的适应能力更强。

### ❌ **消融实验（隐含分析）**
虽然未明确列出消融表，但文中通过以下方式进行了有效性验证：
- 对比不同参数量（30 vs 60）的 QRL 模型 → 显示增加参数能显著提升性能。
- 使用不同 RL 架构（DDPG vs DQN）→ 验证 VQC 可灵活适配多种 RL 范式。
- 分析特征映射 `Z(x)` 的作用 → 表明其有助于打破径向对称性，缓解训练困难。

---

## 4. **关键结论和发现**

### ✅ **主要发现**
1. **QRL 在动态投资组合优化中具备竞争力**：
   - 尽管当前硬件受限，但基于 VQC 的 QRL 已能在真实金融数据上学习出有效的自适应交易策略。
2. **极高的参数效率**：
   - 得益于量子叠加与纠缠，VQC 能以极少参数捕捉复杂的市场动态关系，远超经典神经网络的“参数爆炸”模式。
3. **更强的跨市场鲁棒性**：
   - QRL 策略在多个经济周期中表现稳定，Sharpe Ratio 方差低于多数经典模型。
4. **推理速度悖论**：
   - **电路执行本身极快**（毫秒级），但**云端部署延迟极高**（分钟级），主因是多租户排队、重复校准和初始化开销。

### ⚠️ **方法的局限性**
| 局限 | 说明 |
|------|------|
| **当前硬件限制** | 实验只能在模拟器上训练（statevector simulator），实际 QPU 仅用于推理；最大 qubit 数受 GPU 内存限制（约 20 qubits）。 |
| **云平台延迟严重** | 当前 IBM Quantum Cloud 的调度机制导致 end-to-end 推理延迟高达 **23–43 分钟**，不适用于高频交易场景。 |
| **尚无容错能力** | 使用的是 NISQ 设备，噪声影响尚未深入研究（本工作依赖模拟器获得精确梯度）。 |
| **模型规模有限** | 最大仅测试到 60 参数，更大规模潜力未知。 |

### 🔮 **未来工作方向**
1. **专用量子设备部署**：
   - 若 QPU 可专用于某类固定电路（如 RL 推理），则可大幅减少准备开销，释放原生高速潜力。
2. **降低云服务延迟**：
   - 开发面向 QML 的低延迟量子云计算架构，例如批处理、缓存编译、持久化量子态等。
3. **探索更大规模 VQC**：
   - 结合 error mitigation 和 shallow ansatz，尝试更多资产或更长时序输入。
4. **融合经典-量子混合架构**：
   - 如用经典模型提取特征，VQC 做最终决策，兼顾效率与表达力。
5. **扩展至其他金融任务**：
   - 如期权定价、风险管理、算法交易等，进一步验证 QRL 的通用性。

---

## ✅ 总结一句话

> 本文证明了 **VQC-based QRL 是一种极具参数效率和鲁棒性的动态投资组合优化范式**，虽受限于当前量子云基础设施的延迟瓶颈，但在算法层面已展现出媲美甚至超越大型经典 Deep RL 模型的能力，预示着未来在专用硬件支持下可能成为金融智能决策的新前沿。

🔗 **开源代码**：[https://github.com/VincentGurgul/qrl-dpo-public](https://github.com/VincentGurgul/qrl-dpo-public)

</details>

---

### 9. [LoPRo: Enhancing Low-Rank Quantization via Permuted Block-Wise Rotation](https://arxiv.org/abs/2601.19675)

**Authors**: Hongyaoxing Gu, Lijuan Hu, Liye Yu, Haowei Li, Fangfang Liu  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.19675v1  

#### Abstract
Post-training quantization (PTQ) enables effective model compression while preserving relatively high accuracy. Current weight-only PTQ methods primarily focus on the challenging sub-3-bit regime, where approaches often suffer significant accuracy degradation, typically requiring fine-tuning to achi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LoPRo: Enhancing Low-Rank Quantization via Permuted Block-Wise Rotation 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Post-Training Quantization (PTQ)** 方法在低比特（sub-3-bit）量化时面临显著的精度下降问题，尤其是在**无需微调（fine-tuning-free）** 的场景下。尽管已有低秩补偿（Low-Rank Compensation, LoRC）方法尝试通过引入低秩矩阵来恢复量化误差，但仍存在以下挑战：

- **残差矩阵量化困难**：低秩近似后的残差矩阵仍包含难以量化的异常值（outliers），直接应用旋转等技术会破坏低秩分解的有效性。
- **高计算与存储开销**：传统 SVD 分解计算复杂度高（O(N³)），且需要较高秩（如 r=256）才能达到较好效果，导致推理延迟增加。

### 提出的新方法与创新思路
本文提出 **LoPRo (Low-rank Partial Rotation Quantization)**，一种无需微调的高效 PTQ 算法，其核心创新包括：

#### （1）**Permuted Block-Wise Rotation（置换块状部分旋转）**
- **列重排（Column Permutation）**：基于代理 Hessian 矩阵和残差绝对均值，对残差矩阵的列进行重要性排序，将更重要的通道置于前列。
- **部分块状旋转（Partial Block Rotation）**：仅对非重要列应用 **Walsh-Hadamard Transformation (WHT)** 进行块状旋转，而保留最重要列不旋转（使用单位矩阵），从而在提升量化鲁棒性的同时保护关键信息。

> 公式形式：
> $$
> \begin{bmatrix}
> I_{b_f} & 0 \\
> 0 & H_{\text{wal}}
> \end{bmatrix}
> $$

#### （2）**R1SVD：轻量级混合精度低秩分解**
- 提出 **Rank-1 Sketch SVD (R1SVD)**，基于随机草图（randomized sketching）简化 SVD 流程，将时间复杂度从 O(N³) 降至 O(N²)，并支持迭代更新。
- 使用 **fp8 存储 U 和 V，fp16 存储奇异值 Σ**，实现存储开销减半，同时保持高精度。

### 相比现有方法的优势
| 维度 | LoPRo 优势 |
|------|-----------|
| **精度** | 在 2-bit 和 3-bit 下均优于所有 fine-tuning-free 基线，甚至媲美需微调的方法 |
| **效率** | 量化速度快（7B 模型 < 0.5 小时，Mixtral-8x7B < 2.5 小时） |
| **推理延迟** | 额外延迟 < 10%，随 batch size 增大而降低 |
| **内存开销** | 仅增加约 2% 额外存储（r=16） |
| **通用性** | 支持 Dense 和 MoE 架构（如 LLaMA、Mixtral） |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **校准数据集（Calibration Set）**：
  - `c4` 数据集，采样 128 个序列，每个 2048 tokens
- **评估数据集**：
  - **Perplexity**: `WikiText2`
  - **Zero-Shot Accuracy**: 
    - ARC-Challenge (AC), ARC-Easy (AE)
    - PIQA (QA), Winogrande (WI)
    - BoolQ (BQ), Hellaswag (HS)
    - OpenBookQA (OB), MathQA (MQ)

### 实验设置
- **模型家族**：
  - **Dense Models**: LLaMA-2 (7B, 13B), LLaMA-3 (8B)
  - **MoE Model**: Mixtral-8x7B
- **量化配置**：
  - 权重量化为 2-bit / 3-bit，激活保持 16-bit（W2A16 / W3A16）
  - 低秩秩数：r = 16
  - 块大小：$ b_r = b_H = 256 $
- **硬件平台**：
  - 单卡 A100 40GB 或 A800 80GB GPU

### 评估指标
| 指标 | 描述 |
|------|------|
| **PPL (Perplexity)** | WikiText2 上的语言建模困惑度 |
| **Zero-Shot Accuracy (%)** | 多项选择任务准确率 |
| **Quantization Time** | 完整量化耗时（小时/分钟） |
| **Throughput / Latency** | 推理吞吐量（token/s）与延迟 |
| **Compression Ratio** | 平均比特宽度（bits/weight） |

### 基线方法对比
| 类别 | 基线方法 |
|------|---------|
| **Scalar Quantization** | GPTQ, OmniQuant |
| **Vector Quantization** | GPTVQ, AQLM |
| **Rotation-Based** | QuIP#, QuaRot |
| **Low-Rank Compensation** | LQER, SVD-Quant |
| **Fine-Tuning Based** | RILQ, CALDERA |
| **MoE-Specific** | MoEQuant++ |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 2）

#### LLaMA-2 7B 结果（3-bit）
| 方法 | PPL ↓ | Avg Acc ↑ |
|------|--------|------------|
| FP16 | 5.12 | 66.8 |
| GPTQ | 8.06 | 59.2 |
| GPTVQ | 5.61 | 74.1 |
| LQER | 5.72 | 74.2 |
| **LoPRo** | **5.43** | **76.3** |
| **LoPRo-v (VQ)** | **5.45** | **76.7** |

> ✅ **LoPRo 在无需微调下超越所有基线，接近甚至超过需微调方法**

#### Mixtral-8x7B 结果（2.17-bit）
| 方法 | Bit | PPL ↓ | Zero-Shot Acc ↑ |
|------|-----|--------|------------------|
| MoEQuant++ | 3.00 | 4.90 | ~60.1 (AC) |
| **LoPRo** | **2.17** | **4.80** | **55.8 → 80.5** |
| **LoPRo-v** | **2.17** | **4.80** | **55.8 → 80.5** |

> ✅ **以更低比特（2.17 vs 3.0）实现更高精度，压缩率提升显著**

#### 与微调方法对比（Table 2）
在 LLaMA-2 7B 上结合 RILQ 微调后：
- LoPRo-FT @ 2.2-bit 达到 **PPL=6.06**, 超过 CALDERA (6.79) 和 RILQ (6.28)
- 表明 LoPRo 可作为更优的初始化基础，提升微调上限

### 与基线方法的对比结果
| 对比维度 | LoPRo 优势 |
|----------|-----------|
| **vs GPTQ/GPTVQ** | PPL 降低 0.3–0.5，零样本准确率提升 4–8% |
| **vs LQER** | 使用 r=16 vs r=256，精度更高且内存减少 75% |
| **vs QuIP#** | 在 2-bit 下 PPL 降低 >20%，内存增长 <10% |
| **vs MoEQuant** | 在 MoE 模型上完成量化仅需 ~2.5 小时，PPL 降 0.4，准确率升 8% |

### 消融实验结果（Table 5 & Appendix D）

#### 不同组件消融（2-bit LLaMA-2 7B）
| 配置 | PPL ↓ | Avg Acc ↑ |
|------|--------|------------|
| RTN（无优化） | 402 | 44.1 |
| + GPTQ（无旋转） | 8.40 | 53.0 |
| + Full HWT 旋转 | 9.49 | 51.5 |
| **+ Partial HWT + Permutation** | **7.39** | **57.8** |
| **+ Vector Quantization** | **6.53** | **61.2** |

> 🔍 **关键发现**：
> - 全局旋转反而损害性能（破坏重要列结构）
> - **部分旋转 + 列重排** 是性能跃升的关键
> - 向量量化可进一步提升极限

#### 超参数敏感性分析
- **秩 r=16 已足够**：r>16 改进有限，但存储成本上升
- **迭代次数 it=8 最优**：再增加收益饱和
- **块大小 br=bH=256 最佳**：太小则旋转不足，太大则影响重要列

---

## 4. 关键结论和发现

### 主要发现
1. **低秩 + 局部旋转是高效 PTQ 的关键路径**  
   LoPRo 成功解决了“低秩分解”与“旋转增强量化”的兼容性问题，通过**保护重要列 + 旋转次要列**的设计实现了双赢。

2. **极低秩即可获得高性能**  
   仅需 **r=16** 的低秩补偿，在几乎不增加存储的前提下显著提升量化稳定性，远优于需 r=256 的 LQER。

3. **无需微调即可逼近微调性能**  
   在 3-bit 下，LoPRo 的表现已非常接近需微调的 SOTA 方法，验证了其重建能力的强大。

4. **高度可扩展至 MoE 架构**  
   在 Mixtral-8x7B 上成功部署，量化时间短（<2.5h），性能反超更高比特的 MoEQuant++，展现出工业级实用性。

### 方法的局限性
- **目前仅支持权重量化**：未涉及 activation quantization 和 KV-cache quantization。
- **依赖校准集统计特性**：虽然对不同校准集（c4/Pile/WikiText2）鲁棒，但仍需一定代表性数据。
- **理论边界假设较强**：如输入中心化、误差正比于权重等，在极端分布下可能失效。

### 未来工作方向
- 扩展至 **activation 和 KV-cache 量化**，构建全量化 pipeline
- 探索 **rotation 与 LoRA 初始化的联合优化**，加速下游任务微调
- 将 **R1SVD 应用于大规模训练中的梯度压缩**
- 设计 **自适应块大小与秩选择机制**，实现动态资源分配

---

> 📌 **一句话总结**：  
> **LoPRo 通过“置换+局部旋转+轻量低秩分解”，在无需微调的前提下实现了 SOTA 的低比特量化性能，兼具高精度、高速度与低开销，为大模型边缘部署提供了强有力的新工具。**

</details>

---

### 10. [LLM Driven Design of Continuous Optimization Problems with Controllable High-level Properties](https://arxiv.org/abs/2601.18846)

**Authors**: Urban Skvorc, Niki van Stein, Moritz Seiler, Britta Grimme, Thomas B\"ack, Heike Trautmann  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.18846v1  

#### Abstract
Benchmarking in continuous black-box optimisation is hindered by the limited structural diversity of existing test suites such as BBOB. We explore whether large language models embedded in an evolutionary loop can be used to design optimisation problems with clearly defined high-level landscape char...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLM Driven Design of Continuous Optimization Problems with Controllable High-level Properties

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前连续黑盒优化（continuous black-box optimization）领域的基准测试面临**结构性多样性不足**的问题。主流测试套件如 **BBOB** 覆盖的景观特征空间有限，导致：
- 自动化算法选择（AAS）模型泛化能力受限；
- 新算法评估依赖于训练/测试划分方式，缺乏鲁棒性。

该论文旨在通过生成具有**可控高阶景观特性**（high-level landscape properties）的新优化问题，来扩展现有基准集合的覆盖范围。

---

### 🚀 提出的新方法与创新思路

作者提出将 **Large Language Model-driven Evolutionary Algorithm (LLaMEA)** 框架用于**自动设计优化问题本身**，而非传统地用于设计求解算法。

#### 核心方法流程（三步法）：
1. **Feature Model Definition**  
   基于 BBOB 数据集训练 XGBoost 分类器，预测五个高阶属性：
   - Multimodality（多模态）
   - Separability（可分性）
   - Basin-size Homogeneity（吸引域大小均匀性）
   - Search-space Homogeneity（搜索空间均匀性）
   - Global-to-local Optima Contrast（全局与局部最优对比度）

2. **LLM-driven Problem Generation**  
   利用 LLaMEA 框架中的 LLM（如 `gpt5-nano`），在自然语言提示引导下生成 Python 形式的优化函数代码。进化过程中以 ELA 特征预测得分作为 fitness。

3. **Verification & Filtering**  
   使用基于 **basin-of-attraction analysis** 和统计检验的方法验证生成问题是否真正具备目标属性，并进行过滤。

#### 创新点总结：
| 创新维度 | 内容 |
|--------|------|
| **首次应用** | 首次将 LLM 驱动的进化计算用于**生成具有指定高阶特性的连续优化问题** |
| **可解释控制** | 通过自然语言描述控制目标属性（如“生成一个高度多模态且可分的问题”），比纯数值特征匹配更直观 |
| **多样性增强机制** | 引入基于 **ELA-space 的 fitness sharing** 机制，防止种群陷入相似景观，提升生成多样性 |
| **开放资源提供** | 发布了一个大规模、可复现、带标签的新型优化问题库，支持下游任务 |

---

### 🔍 相比现有方法的优势

| 方法类型 | 局限性 | 本文优势 |
|--------|-------|---------|
| Affine combination of BBOB functions [5,28] | 多样性有限，仅是已有函数的线性组合 | 生成全新结构，非仿射变换产物 |
| Tree-based random generators [19,25] | 难以控制特定高阶属性 | 可定向生成指定属性组合 |
| GP-based ELA matching [15] | 数值目标需专家设定，不可读 | 使用语义级属性描述，更具可解释性 |
| LLM-EBG [16]（唯一同类工作） | 以算法性能差异为 fitness，偏向特定 solver | 以景观属性为目标，更通用、公平 |

> ✅ 总结：本方法实现了**可解释、可控、多样化**的问题生成，填补了 BBOB 在某些区域的空白。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **BBOB benchmark suite**：共 24 类标准函数，每类取前 50 个实例（共 1200 个/维度）
- 维度设置：D ∈ {2, 5, 10}
- 采样策略：Latin Hypercube Sampling，样本量 = 250 × D
- 工具包：使用 `pflacco` 提取 ELA 特征（ela_meta, ela_distr, nbc, disp, pca, ic 等组）

---

### ⚙️ 实验设置

| 组件 | 设置说明 |
|-----|----------|
| **LLM 候选模型** | 对比了 5 种 LLM：<br>- `devstral_24b`,<br>- `gemini-2.0-flash`,<br>- `gpt5-nano`（最终选用）,<br>- `qwen2.5-coder_14b`,<br>- `math-stral_7b` |
| **Population 参数** | μ = 8（parent），λ = 16（offspring），采用 (μ, λ)-strategy（无精英保留） |
| **Prompt 设计** | Mutation prompt 明确引用目标属性（如 “make it more multimodal”） |
| **Fitness 定义** | 单属性：对应 XGBoost 模型输出概率<br>双属性：两个属性得分的均值 |
| **Fitness Sharing** | 在 z-standardized ELA 向量空间中使用 Manhattan 距离计算密度惩罚项：<br>$ F_{shared} = F / \sum_j \max(0, 1 - D_{ELA}(f_i, f_j)/\sigma_{share}) $<br>其中 $\sigma_{share}$ 动态适应群体平均距离 |

---

### 🎯 评估指标

| 评估阶段 | 指标 |
|--------|------|
| **在线评估（generation）** | XGBoost 模型对五项高阶属性的预测分数（[0,1] 区间） |
| **离线验证（verification）** | 基于 basin-of-attraction 的定量分析：<br>- 局部最优数量（multimodality）<br>- 最优与平均局部最优的 fitness 差异（global-local contrast）<br>- 最大/最小吸引域比例（basin-size ratio） |
| **可视化分析** | t-SNE 投影到二维空间，观察生成问题是否填补 BBOB 空白区 |
| **统计检验** | Mann-Whitney U test 检验两组分布显著性差异（α=0.01） |

---

### 🔁 基线方法对比
虽然没有直接端到端对比其他生成器，但文中明确指出与以下方法的本质区别：
- **Muñoz et al. [15]**：基于 GP + ELA 数值匹配 → 缺乏语义控制
- **Vermetten et al. [28]**：affine 组合 → 结构受限
- **LLM-EBG [16]**：fitness 基于算法性能差距 → 有偏见导向

> 本文强调的是**范式转变**：从“数值特征拟合”转向“语义属性驱动”。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据

#### （1）LLM 选择实验（图3）
| LLM 模型 | Basin-size Homog. + Separability 成功率（F > 0.5） | Multimodality + Global Structure 成功率 |
|--------|-----------------------------|-------------------------------|
| `gpt5-nano` | **98.7%** ✅ | >80%（Top 4） |
| `qwen2.5-coder_14b` | ~90% | ~75% |

✅ 最终选定 `gpt5-nano`：在性能、成本、并行效率之间达到最佳平衡。

---

#### （2）Fitness Sharing 消融实验（图4）
比较开启/关闭 ELA-space fitness sharing 的效果：

| 模型 | 是否启用 sharing | 近邻 ELA 距离中位数趋势 |
|------|------------------|------------------------|
| `qwen2.5-coder_14b` | 否 | 较低 |
| `qwen2.5-coder_14b` | 是 | ↑ 提升约 30–50% |
| `gpt5-nano` | 否 | 中等 |
| `gpt5-nano` | 是 | ↑↑ 显著右移，峰值明显增大 |

👉 表明 fitness sharing 有效提升了生成景观的多样性。

---

#### （3）属性验证结果（图6）

##### a. **Multimodality**
- 所有被优化为“多模态”的 LLaMEA 问题均检测出 ≥2 个 basin；
- BBOB 中多数为单峰，仅少数（如 Step Ellipsoidal）因 plateau 被误判为多峰；
- p-value = 7.49e-30 << 0.01 → 分布显著不同。

##### b. **Global-to-local Optima Contrast**
- LLaMEA 生成的问题在最优与平均局部最优之间的 fitness 差更大；
- 当 fitness score F ≥ 0.99 时，仅有 12% 出现小 fitness gap → 表明 fitness score 与真实属性强相关；
- p-value = 1.59e-05 < 0.01。

##### c. **Basin-size Homogeneity**
- 优化后的函数 basin size ratio 更接近 1（即更均匀）；
- 尽管高多模态性带来估计困难，但仍能观察到趋势；
- p-value = 1.52e-16 < 0.01。

> ❗例外：Separability 和 Search-space Homogeneity 未做统计检验，前者因数学定义明确，后者缺乏量化指标。

---

#### （4）t-SNE 可视化（图7）
- **Trustworthiness (k=10) ≈ 0.997** → 局部结构保持良好；
- LLaMEA 生成的问题：
  - 并未形成孤立簇；
  - 而是**自然延伸 BBOB 的分布边界**，填充其未覆盖区域；
- “Other” 类 BBOB 问题集中在左下角（主要是简单单峰函数），而 LLaMEA 覆盖更复杂区域。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **LLM 可用于高质量优化问题生成**  
   LLaMEA 框架能够根据自然语言指令生成符合预期高阶属性的连续优化问题，且可通过 ELA 预测器和 basin analysis 验证。

2. **生成问题显著扩展了 BBOB 实例空间**  
   t-SNE 可视化显示新问题填补了原有空白，增强了基准集合的多样性和代表性。

3. **fitness sharing 显著提升多样性**  
   ELA-space 的 niching 机制有效避免模式坍缩，促进探索不同类型的景观结构。

4. **属性组合生成独特问题**  
   支持多属性联合优化（如“既多模又可分”），产生在 BBOB 中罕见甚至不存在的问题类型。

5. **高 fitness score 与真实属性正相关**  
   特别是在 global-local contrast 上，score 越高，实际差异越大，说明反馈机制有效。

---

### ⚠️ 方法的局限性

| 局限 | 说明 |
|------|------|
| **维度限制** | Basin-of-attraction 分析仅在 2D 上执行，高维难以准确识别吸引域 |
| **分离性评估偏差** | Separability 使用数值差分法判断变量交互，可能受采样误差影响 |
| **LLM 不确定性** | 不同 LLM 输出质量差异大，结果对模型选择敏感 |
| **计算开销大** | 尤其是验证阶段需要密集采样和路径追踪，不适合实时使用 |
| **部分属性难量化** | 如 search-space homogeneity 缺乏统一量化标准，依赖视觉判断 |

---

### 🔮 未来工作方向

1. **系统性比较不同问题生成器**  
   将 LLaMEA 与其他方法（GP、affine、RandOptGen）在相同评估框架下横向对比。

2. **研究 LLM 敏感性**  
   探索 prompt engineering、temperature、top-k 等参数对生成质量的影响。

3. **应用于自动化算法选择（AAS）验证**  
   测试这些新生成的问题是否能提升 AAS 模型在未知问题上的泛化能力。

4. **引入约束条件或多目标扩展**  
   将方法推广至 constrained 或 multi-objective optimization 场景。

5. **构建动态演化的问题库**  
   持续运行 LLaMEA 循环，自适应填充当前算法表现最差的区域。

---

### 📦 开源资源
所有生成的问题和代码已公开发布：
🔗 [Zenodo Reproducibility Package](https://doi.org/10.5281/zenodo.18306723)

> 包含易用的 Python 库，便于社区用于 benchmarking、ELA 研究和 AAS 开发。

---

## ✅ 总结一句话
> 本文开创性地利用 LLM + 进化计算框架 **LLaMEA**，实现了**可解释、可控、多样化**的连续优化问题生成，成功扩展了 BBOB 基准集的景观多样性，并提供了首个大规模、带属性标签的 LLM 生成问题库。

</details>

---

### 11. [PROTEUS: SLA-Aware Routing via Lagrangian RL for Multi-LLM Serving Systems](https://arxiv.org/abs/2601.19402)

**Authors**: Amit Singh Bhatti, Vishal Vaddina, Dagnachew Birru  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.19402v1  

#### Abstract
Production LLM deployments serve diverse workloads where cost and quality requirements vary by customer tier, time of day, and query criticality. Model serving systems accept latency SLOs directly. LLM routers do not. They force operators to tune parameters offline and guess what accuracy might resu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PROTEUS: SLA-Aware Routing via Lagrangian RL for Multi-LLM Serving Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前的 **LLM 路由系统**（LLM routers）无法直接接受并满足用户的 **准确性目标（accuracy targets）**，而是依赖于离线参数调优（如设定“30% 查询走 GPT-4”），导致：
- 准确性与参数之间的关系是**间接、非单调且数据集依赖**的；
- 运维人员难以将业务需求（如“达到 90% 准确率”）转化为具体配置；
- 难以支持多级服务（tiered SLA），例如为不同客户群体提供差异化质量保障。

尽管已有模型服务系统（如 Clipper、INFaaS）支持延迟 SLO 或准确率输入，但它们不适用于**异构大语言模型池中的动态路由决策**。

---

### 🚀 提出的新方法：PROTEUS
作者提出 **PROTEUS**（Polymorphic Router for Operational Target Enforcement with Unified SLA），一个能够**在运行时直接接收准确性目标 T 并确保其被满足**的 LLM 路由器。

#### 核心创新点：
1. **T-conditioned Policy Network**  
   将目标准确率 $ T \in [0.85, 0.95] $ 作为策略网络的输入，在推理阶段实现**单模型覆盖全精度谱系**，无需重新训练或参数扫描。

2. **Lagrangian Dual Control with Learned 入**  
   在训练过程中引入可学习的拉格朗日对偶变量 $ \lambda $ 来动态惩罚违反约束的行为（即实际 accuracy < T）。该机制通过反馈回路教会策略网络建立 $ T \rightarrow u $（quality preference）的映射。

3. **连续质量偏好输出（Continuous Quality Preference）**  
   策略输出的是连续值 $ u \in [0,1] $，表示对高质量模型的倾向程度，而非硬性选择某模型，从而实现平滑插值和更灵活的成本-质量权衡。

4. **自适应路由评分函数（Adaptive Routing Score）**  
   设计非线性打分函数：
   $$
   S_i = p_i + \beta \cdot b_i - (1 - p_i)^\gamma \cdot c_i
   $$
   其中 $ \gamma $ 是可学习的成本敏感参数，允许系统根据不同部署环境自动调整成本权重。

---

### 🔍 相比现有方法的优势
| 方面 | 现有方法（如 CARROT、OmniRouter） | PROTEUS |
|------|-------------------------------|--------|
| 是否支持运行时 accuracy 输入 | 否（需预设模式或重训练） | ✅ 是（直接输入 T） |
| 是否保证 floor compliance（acc ≥ T） | ❌ 否（仅 0–22% 达标） | ✅ 是（100% 达标） |
| 参数调节方式 | 手动调参 / 多模型部署 | 单一模型 + 动态 T 输入 |
| 成本节约能力 | 中等（约 70–80%） | ⬆️ 高达 89.8% |
| 适应性 | 固定 trade-off | 可随负载、时间、客户等级动态变化 |

---

## 2. 核心实验方法和设置

### 📚 数据集
使用两个权威的多LLM路由基准测试集：

| 数据集 | 查询量 | 模型数 | 包含模型 | Oracle Accuracy | 成本范围 |
|-------|--------|--------|----------|------------------|---------|
| **RouterBench** | 405K | 11 | Llama-2, Mistral, GPT-3.5/4, Claude-2 | 91.4% | \$0.0001 – \$0.01/q |
| **SPROUT** | 45K | 14 | GPT-4o, Claude-3.5, o3-mini, Llama up to 405B | 98.6% | \$0.0001 – \$0.05/q |

> 特点互补：RouterBench 强调规模与任务多样性；SPROUT 测试前沿模型与极端成本差异。

---

### 🧪 实验设置
- **训练配置**：
  - 编码器：DeBERTa-v3-small（22M 参数）
  - 批大小：32（A100 GPU）
  - 训练步数：10K 步（约 4 小时/数据集）
  - 使用 PPO（Proximal Policy Optimization）进行强化学习训练
  - 拉格朗日乘子 $ \lambda $ 更新频率高于策略网络（“faster dual” heuristics）

- **推理延迟**：
  - 单查询延迟 < 8.7ms（batch=1），批量处理可达 <3ms/query
  - 显著低于 LLM 推理本身（通常 2–7 秒）

---

### 📊 评估指标

#### ✅ 适应性相关指标（Adaptability Metrics）
| 指标 | 定义 |
|------|------|
| **T-u Correlation** | 请求的目标 T 与策略输出的质量偏好 u 的皮尔逊相关系数（理想为 1.0） |
| **Floor Compliance (%)** | 在多少比例的 T 上实现了 $ \text{achieved accuracy} \geq T $ |
| **Tolerance-band Compliance** | 在 ±2% 和 ±5% 容差范围内匹配目标的比例 |

#### 💰 效率与性能指标（Efficiency & Performance）
| 指标 | 定义 |
|------|------|
| **Routing Efficiency (RE)** | $ \frac{\text{Acc} - \text{Acc}_{\text{random}}}{\text{Latency}} $（单位：pp/ms） |
| **Routing Performance Index (RPI)** | 综合考虑 Accuracy、Cost、Latency 的综合得分：<br>$ \text{RPI} = \text{Acc} \times \left(1 - \frac{\text{Cost}}{\text{Cost}_{\max}}\right) \times \left(1 - E_{\text{route}} \times 100\right) $ |

---

### 🆚 基线方法对比
| 类别 | 方法 |
|------|------|
| **静态策略** | Always-Best, Always-Cheapest, Random |
| **学习型路由器** | KNN, MLP, CARROT（三种模式：quality/cost/balanced）, OmniRouter |
| **消融变体** | PROTEUS-ablated（无约束、固定 γ、无 critic） |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据汇总

| 指标 | RouterBench | SPROUT |
|------|-------------|--------|
| **T-u Correlation** | 0.973 | 0.981 |
| **Floor Compliance (acc ≥ T)** | 100% | 100% |
| **±5% Tolerance-band Compliance** | 100% | 67% |
| **PROTEUS Accuracy** | 90.1% | 94.0% |
| **Oracle Accuracy** | 91.4% | 98.6% |
| **与 Oracle 差距** | +1.3 pp | +4.6 pp |
| **相比最佳固定模型成本节省** | 89.8% | 83% |
| **Routing Efficiency (RE)** | 11.1 pp/ms | 9.5 pp/ms |
| **RPI** | 88.5 | 83.5 |

> 注：在 RouterBench 上，PROTEUS 的 RPI 甚至略高于 Oracle（88.5 vs 88.2），说明其在微小 accuracy 损失下大幅降低成本，整体效率更高。

---

### 🔁 动态适应能力验证（Figure 2c）
在四种动态 T 变化场景中（Step、Drift、Cyclic、Realistic），PROTEUS 能**即时响应目标变更**，无适应延迟：
- **零 retraining / tuning 开销**
- 跟踪误差小（overshoot 仅 1.6–5.2%）
- 支持 per-query、per-user 或基于负载的实时调整

---

### 🔍 消融实验结果（Ablation Study）

| 变体 | 影响（△Acc） | 关键发现 |
|------|--------------|-----------|
| **-Constraint (λ)** | ↓0.27% (RB), ↓0.53% (SP) | 最关键组件！移除后 floor compliance 崩溃，策略变得过于 cost-aggressive |
| **-Learnable γ** | ~0 (RB), ↓0.62% (SP) | 对高成本跨度环境（SPROUT 的 9.5×）更重要，否则影响小 |
| **-Critic** | ↓<0.05% | 几乎无影响，因路由为 single-step MDP，critic 非必需 |

> 结论：**Lagrangian dual mechanism 是核心驱动力**，其余模块可根据部署需求裁剪。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **首次实现“按需指定 accuracy”的 LLM 路由器**：Operator 可直接设置 $ T \in [0.85, 0.95] $，PROTEUS 能可靠地满足该目标。
2. **单一模型服务所有 SLA Tier**：无需为每个服务质量等级训练独立模型，极大降低运维复杂度。
3. **强 floor guarantee 能力**：在所有测试 T 下均达成 $ \text{acc} \geq T $，合规率达 100%，远超基线（OmniRouter 仅 0–22%）。
4. **高效率与低成本共存**：接近 oracle 准确率的同时，实现高达 89.8% 的成本节约。
5. **动态适应性强**：可在运行时实时响应 SLA 变更、负载波动、时段定价等场景。

---

### ⚠️ 局限性（Limitations）
1. **依赖 ground-truth label 训练**：目前需要每条 query 的正确答案来监督训练，生产环境中可能不可得，需借助 LLM-as-a-judge 或人工标注。
2. **数据集时效性限制**：RouterBench 使用较旧模型（如 GPT-4、Claude-2），未涵盖最新 frontier models。
3. **压缩的 accuracy 分布**：中档模型之间性能差距小，使得精细控制多个 T 点较难。
4. **静态模型池假设**：未考虑在线增减模型的情况，未来需支持动态注册。

---

### 🔮 未来工作方向（Future Work）
1. **Bandit Feedback Learning**：仅观察所选模型的结果，减少标注开销，支持持续学习。
2. **Latency-aware Routing**：扩展目标函数，联合优化 accuracy、cost 与 response time。
3. **Hierarchical Routing**：先选 tier 再选 model，提升大规模模型池下的决策效率。
4. **Multi-objective T Specification**：同时设置 accuracy floor 和 cost ceiling，更好匹配真实 SLA。
5. **Multi-turn Conversation Support**：考虑上下文累积效应，优化长对话中的路由策略。
6. **更丰富的 benchmark**：构建跨越 $[0.5, 0.99]$ accuracy 范围的多样化路由测试集。

---

> **总结一句话**：  
> PROTEUS 成功将 **SLA-aware 控制理论** 与 **深度强化学习** 相结合，打造了一个真正面向生产的、可编程的 LLM 路由引擎，推动 LLM serving 系统从“经验调参”迈向“目标驱动”的新时代。

</details>

---

### 12. [Identifying and Transferring Reasoning-Critical Neurons: Improving LLM Inference Reliability via Activation Steering](https://arxiv.org/abs/2601.19847)

**Authors**: Fangan Dong, Zuming Yan, Xuri Ge, Zhiwei Xu, Mengqi Zhang, Xuanang Chen, Ben He, Xin Xin, Zhumin Chen, Ying Zhou  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.19847v1  

#### Abstract
Despite the strong reasoning capabilities of recent large language models (LLMs), achieving reliable performance on challenging tasks often requires post-training or computationally expensive sampling strategies, limiting their practical efficiency. In this work, we first show that a small subset of...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Identifying and Transferring Reasoning-Critical Neurons: Improving LLM Inference Reliability via Activation Steering

---

## 1. 论文的主要贡献和创新点

### 解决的问题
尽管当前的 **Large Language Models (LLMs)** 在推理任务上表现出色，但在复杂任务（如数学和编程）中仍存在**推理可靠性不足**的问题。传统提升可靠性的方法依赖于 **post-training** 或 **test-time sampling**（如 self-consistency、prompt engineering），这些方法计算成本高、效率低，且将模型视为黑盒，缺乏对内部机制的理解。

本文提出：能否通过直接干预 LLM 内部的神经元激活来提升推理可靠性？

### 提出的新方法：AdaRAS
作者提出了 **AdaRAS (Adaptive Reasoning Activation Steering)**，一种**无需训练、轻量级、测试时干预**的框架，用于提升 LLM 推理的可靠性。

#### 核心思想：
- 发现 LLM 中存在一小部分 **Reasoning-Critical Neurons (RCNs)**，其激活状态与推理正确性高度相关。
- 通过对比正确与错误推理路径的激活差异，识别这些 RCNs。
- 在推理过程中，仅在检测到可能失败时，选择性地“引导”这些神经元的激活，从而修正错误路径而不破坏已正确的推理。

#### 创新点：
1. **首次系统性证明**：推理正确性可通过少量神经元的激活干预进行预测和改进。
2. **提出 AdaRAS 框架**：
   - **RCN 识别**：基于 **Mean Difference (MD)** 和 **极性感知过滤 (polarity-aware filtering)**，从对比推理轨迹中识别关键神经元。
   - **自适应干预 (Adaptive Intervention)**：引入一个轻量级预测器，在推理前判断是否需要干预，避免对正确样本造成负面影响。
3. **可迁移性与可扩展性**：识别出的 RCNs 可跨数据集、跨任务迁移，并在更强模型（如 Qwen3-4B）上依然有效。

### 相比现有方法的优势
| 方法类型 | 缺点 | AdaRAS 优势 |
|--------|------|------------|
| **Post-training** (SFT, PPO) | 需额外训练，成本高，泛化受限 | **无需训练**，即插即用 |
| **Test-time sampling** (self-consistency) | 多次采样，计算开销大 | **单次推理**，无额外采样成本 |
| **Probing-based steering** | 依赖探针泛化能力，不稳定 | **基于激活差值**，更鲁棒 |
| **黑盒方法** | 缺乏可解释性 | **提供机制性洞察**，揭示推理失败的神经基础 |

---

## 2. 核心实验方法和设置

### 数据集
在 **10 个数学与编程基准**上进行评估，涵盖难度较高的挑战性任务：

| 类别 | 数据集 |
|------|-------|
| **数学** | AIME-24, AIME-25, AIME-Extend, MATH-500, GSM8K, AMC-12 |
| **编程** | HumanEval, HumanEval+, MBPP, MBPP+ |

其中 AIME 系列为高难度数学竞赛题，是主要评估重点。

### 实验设置
- **模型**：以 **Qwen3-1.7B** 为主干模型，部分实验扩展至 **Qwen3-4B**。
- **推理方式**：使用 **greedy decoding**（温度=0），确保可复现性。
- **评估指标**：**Accuracy**（答案正确率）。
- **RCN 识别**：
  - 构造对比样本：对每个输入，通过高温采样生成多个推理路径，按答案正确性分为正负样本对。
  - 计算每层神经元的 **Mean Difference (MD)**：  
    $$
    S(l,i) = \mathbb{E}[a(l,i,p^+)] - \mathbb{E}[a(l,i,p^-)]
    $$
  - 应用 **极性过滤**：仅保留激活均值符号相反的神经元（$ \mathbb{E}[a(p^+)] \cdot \mathbb{E}[a(p^-)] < 0 $）。
  - 选取 top-K（默认 K=50）构建稀疏 steering vector。
- **自适应干预**：
  - 训练一个轻量级分类器，基于早期激活预测是否需要干预。
  - 仅当预测为“失败”时才应用 steering。

### 基线方法对比
| 基线类型 | 具体方法 |
|--------|--------|
| **Zero-shot 基线** | Chain-of-Thought (CoT) |
| **Post-training 模型** | DeepSeek-R1-Distill, Nemotron, OpenThinker |
| **Activation Steering 基线** | Probing-based steering（使用探针权重作为重要性分数） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）
| 数据集 | CoT 准确率 | AdaRAS 准确率 | **提升幅度** |
|--------|------------|---------------|-------------|
| **AIME-24** | 47.83% | **60.87%** | **+13.04%** |
| **AIME-25** | 40.91% | **54.55%** | **+13.64%** |
| **MATH-500** | 84.80% | **86.40%** | +1.60% |
| **HumanEval** | 77.18% | **79.19%** | +2.01% |
| **MBPP** | 68.78% | **72.22%** | +3.44% |

> ✅ **平均提升约 5%**，在最难的 AIME 上提升超 **13%**，显著优于所有开源 post-training 模型。

### 与基线方法对比
- **优于所有 post-training 方法**：在 AIME-24/25 上，AdaRAS 超过最强 post-training 模型（如 Nemotron、OpenThinker）。
- **优于 probing-based steering**：
  - 后者在 AIME-24 上导致 **-4.35%** 性能下降，说明盲目使用探针权重不可靠。
  - AdaRAS 更稳定，因引入了极性过滤与自适应门控。

### 消融实验结果（Table 5）
| 方法变体 | AIME-24 | AIME-25 | 分析 |
|---------|--------|--------|------|
| **Full AdaRAS** | **60.87%** | **54.55%** | 完整方法 |
| w/o MD (改用 probing) | 43.48% | 50.00% | MD 对识别 RCN 至关重要 |
| w/o 极性过滤 (AS) | 52.17% | 45.45% | 极性信息显著提升效果 |
| w/o 自适应干预 (AI) | 56.52% | 45.45% | 干预会损害正确样本 |
| 随机干预 | 34.78% | 27.27% | 无差别干预有害 |

> 🔍 所有组件均不可或缺，验证了设计合理性。

---

## 4. 关键结论和发现

### 主要发现
1. **RCNs 存在且可识别**：LLM 推理正确性与少量神经元的激活状态强相关，可通过对比激活差（MD）有效识别。
2. **AdaRAS 显著提升可靠性**：在不增加训练或采样成本的前提下，实现一致且显著的性能提升，尤其在高难度任务上。
3. **RCNs 具有强可迁移性**：
   - 在 AIME 上识别的 RCNs 可迁移到 MATH、GSM8K、甚至编程任务（HumanEval），带来稳定增益（见 Table 3）。
   - 表明 RCNs 捕获的是**任务无关的通用推理机制**。
4. **可扩展性强**：在更强模型 Qwen3-4B 上仍有效（Table 4），如 AIME-25 从 59.09% → **72.73%**。
5. **机制性解释**：
   - AdaRAS 主要干预**中后层神经元**（图5）。
   - 能**稳定 latent reasoning 轨迹**（降低 Magnitude），但**不改变语义建模**（Angle 不变）（图6）。

### 局限性
1. **模型范围有限**：目前仅在 Qwen3 系列模型上验证，需扩展至其他架构（如 LLaMA、Phi）。
2. **依赖对比数据对**：需同时获得同一输入的正确与错误推理路径，对极端能力强/弱的模型可能难以构造。
3. **仅关注 MLP 激活**：未考虑 attention head 或其他模块的影响。
4. **STEM 导向**：主要在数学与编程任务验证，对开放域或多跳推理的适用性待研究。

### 未来工作方向
1. **扩展模型与任务范围**：应用于 vision-language、multi-hop QA、spatial reasoning 等更复杂场景。
2. **改进数据构造策略**：探索无需显式错误样本的方法（如基于不确定性采样）。
3. **结合其他可解释技术**：融合 **Sparse Autoencoders (SAEs)** 或 **dictionary learning** 进一步解析 RCNs 的语义功能。
4. **动态 steering 强度控制**：根据推理阶段自适应调整干预强度 α。
5. **与 token-level 方法结合**：与 entropy-based guidance 等方法协同，实现多层级干预。

---

> 📌 **总结一句话**：  
> AdaRAS 揭示了 LLM 推理可靠性的“神经开关”，通过识别并引导 **Reasoning-Critical Neurons**，实现了**无需训练、高效、可迁移**的推理增强，为可控推理提供了新范式。

</details>

---

### 13. [Towards Self-Optimizing Electron Microscope: Robust Tuning of Aberration Coefficients via Physics-Aware Multi-Objective Bayesian Optimization](https://arxiv.org/abs/2601.18972)

**Authors**: Utkarsh Pratiush, Austin Houston, Richard Liu, Gerd Duscher, Sergei Kalinin  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.18972v1  

#### Abstract
Realizing high-throughput aberration-corrected Scanning Transmission Electron Microscopy (STEM) exploration of atomic structures requires rapid tuning of multipole probe correctors while compensating for the inevitable drift of the optical column. While automated alignment routines exist, convention...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Towards Self-Optimizing Electron Microscope: Robust Tuning of Aberration Coefficients via Physics-Aware Multi-Objective Bayesian Optimization*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
现代**aberration-corrected Scanning Transmission Electron Microscopy (STEM)** 虽然能够实现亚埃级成像，但其光学参数（如defocus、astigmatism及高阶像差）的调校过程高度依赖人工操作，存在以下瓶颈：
- **效率低下**：传统自动对准采用串行搜索（如Nelder-Mead），样本利用率低；
- **脆弱性强**：热漂移、束流剂量累积和参数耦合导致难以动态维持最优状态；
- **灵活性差**：深度学习方法虽快，但需大量预训练数据，难以适应不同样品或成像条件。

因此，亟需一种**高效、鲁棒且可泛化**的自动化像差校正框架。

---

### 🚀 提出的新方法与创新思路
本文提出了一种基于**Physics-Aware Multi-Objective Bayesian Optimization (MOBO)** 的自优化电子显微镜框架，核心创新如下：

#### （1）将像差校正建模为多目标优化问题（Multi-Objective Optimization）
- 不再使用单一图像质量指标（如contrast最大化），而是同时优化多个物理驱动的reward函数，例如：
  - `R_contrast`：归一化图像方差（Normalized RMS Contrast）
  - `R_fft`：经高通滤波后的FFT谱功率（反映晶格周期性）
- 利用**Pareto Front**揭示不同目标之间的权衡关系，避免“reward hacking”（例如仅增强噪声以提高contrast）。

#### （2）引入贝叶斯优化（Bayesian Optimization, BO）提升采样效率
- 使用**Gaussian Process (GP)** 构建像差空间的概率代理模型（surrogate model），主动选择最具信息量的镜头参数进行下一次测量；
- 结合**Expected Hypervolume Improvement (EHVI)** 作为acquisition function，在多目标场景中有效扩展Pareto前沿。

#### （3）强调“物理感知”与“人类在环”设计
- Reward函数基于物理原理构建（如FFT对应晶格分辨率），确保可解释性和跨平台可迁移性；
- Pareto front可视化使领域专家可在多个非支配解中手动选择最合适的配置，支持“human-in-the-loop”决策。

#### （4）遵循FAIR数据原则
- 所有优化轨迹（action → observation）被系统记录，形成可用于后续分析、数字孪生建模或迁移学习的高质量数据集。

---

### 🔍 相比现有方法的优势

| 方法类型 | 典型代表 | 局限性 | 本工作的优势 |
|--------|--------|------|-------------|
| 传统启发式搜索 | Nelder-Mead, Grid Search | 样本效率低，易陷入局部最优 | 高效探索，利用不确定性引导采样 |
| 单目标BO | 单指标优化（如maximize contrast） | 易受伪影干扰，忽略分辨率损失 | 多目标平衡，防止reward hacking |
| 深度学习 | DeepFocus, Ronchigram NN | 需大规模标注数据，泛化能力弱 | 无需预训练，适用于新样品/仪器 |

> ✅ **核心优势总结**：  
> - 更**robust**（对抗噪声与硬件非理想性）  
> - 更**data-efficient**（< 50次迭代即可收敛）  
> - 更**interpretable & adaptable**（通过Pareto front理解trade-off）

---

## 2. 核心实验方法和设置

### 🧪 数据集与仿真环境
- **合成数据**：基于`abtem`和`pyTEMlib`模拟单层WS₂（Tungsten Diselenide）的STEM图像；
- **真实数据**：在Thermo Fisher Spectra 300 TEM上采集Au纳米颗粒的标准分辨率测试样品图像；
- **噪声注入**：
  - 空间相关高斯噪声（模拟环境扰动）
  - 泊松噪声（模拟有限电子计数下的shot noise）

> 目的：缩小“sim-to-real”差距，验证算法鲁棒性。

---

### ⚙️ 实验设置
| 组件 | 描述 |
|-----|------|
| **优化变量空间 X** | 包括C₁₀（defocus）、C₁₂ₐ/C₁₂ᵦ（two-fold astigmatism）、C₂₁ₐ/C₂₁ᵦ、C₂₃ₐ/C₂₃ᵦ等，维度从3到7不等 |
| **Reward Functions** | <br>• $ R_{\text{contrast}} = \frac{\sigma}{\mu + \epsilon} $<br>• $ R_{\text{fft}} = \langle (\log(1+|\mathcal{F}|) \cdot M) \rangle $，其中M为去除DC分量的高通mask |
| **优化引擎** | BoTorch + GPyTorch实现GP建模与EHVI采样 |
| **硬件控制接口** | CEOS API 控制像差透镜；AutoScript API 控制探测器与样品台 |
| **开源代码** | 已发布至GitHub：<br>- [Aberrations.ipynb](https://github.com/pycroscopy/asyncroscopy/blob/main/notebooks/Aberrations.ipynb)<br>- [stemOrchestrator](https://github.com/pycroscopy/pyAutoMic/tree/main/TEM/stemOrchestrator)<br>- [asyncroscopy](https://github.com/pycroscopy/asyncroscopy) |

---

### 📊 评估指标
| 指标 | 说明 |
|------|------|
| **Pareto Front 形态** | 可视化contrast与resolution间的trade-off，判断是否存在唯一最优解 |
| **Hypervolume Improvement** | 衡量Pareto front随迭代扩展的程度，用于判断收敛性 |
| **Wall-clock Time per Step** | 分解计算开销（GP training + acquisition）与硬件延迟（microscope communication） |
| **最终图像质量** | 是否清晰分辨原子列（atomic columns）与晶格周期性 |

---

### 🔁 基线方法对比
尽管未直接运行其他算法在同一硬件上，文中明确指出所提方法优于以下类别：
- **Serial heuristic search**（如逐轴扫描+峰值检测）
- **Single-objective BO**（仅优化contrast或FFT）
- **Exhaustive grid search**（在高维空间不可行）

并通过消融实验证明MOBO相对于单目标策略的优越性。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### （1）仿真环境中快速收敛（Fig. 4）
- 在3维像差空间（C₁₀, C₁₂ₐ, C₁₂ᵦ）中，**约25次迭代内收敛至全局Pareto front**；
- Hypervolume在前20步迅速上升，表明EHVI能有效探索关键区域；
- 最终获得同时具有高contrast和高FFT power的原子级清晰图像（Fig. 4c(iii)）。

#### （2）真实仪器上的成功部署（Fig. 5）
- 在7维像差空间（含1st & 2nd order aberrations）中完成调优；
- **仅用~40次实验迭代**即找到平衡解；
- 发现：
  - 最大contrast状态（Red star）反而**无法解析晶格结构**（Objective 2 < 11.90）；
  - Pareto最优解（Green star）虽略降contrast，但显著提升分辨率，成功识别Au纳米粒子中的原子列。

> 👉 证明：**单目标优化会误导系统进入“虚假最优”陷阱**。

#### （3）时间成本分析（Fig. 7）
| 参数维度 | 平均每步耗时 | 硬件通信（HW） | 计算开销（GP + Acquisition） |
|---------|--------------|----------------|-------------------------------|
| 3D       | ~8 s         | ~4 s           | ~4 s                          |
| 4D       | ~15 s        | ~4 s           | ~11 s                         |
| 7D       | >100 s       | ~4 s           | >96 s                         |

> 💡 发现：随着维度增加，**计算成为主要瓶颈**，而非硬件响应速度。

---

### 🔍 消融实验与关键发现
- **Grid Search vs. MOBO**（Fig. 3）：
  - 密集网格搜索生成“灰色云团”，绝大多数点为次优；
  - 真正的Pareto front是一条狭窄流形，传统方法极难命中；
- **Single-objective优化风险**：
  - 仅优化contrast → 进入defocused但高信号状态（bright/blury）；
  - 仅优化FFT → 可能过度放大高频伪影；
- **MOBO的优势**：
  - 自动避开极端解，定位“sweet spot”（如Point C/D in Fig. 3）；
  - 提供一组候选方案供人工选择，增强实用性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **像差调优本质上是多目标权衡问题**：
   - “最佳”图像取决于实验目的（contrast vs. resolution），不存在普适最优解；
   - Pareto front提供了物理上合理的trade-off可视化工具。

2. **MOBO显著优于传统方法**：
   - 数据效率高（<50次测量）；
   - 抗噪能力强，能在真实复杂环境下稳定收敛；
   - 支持“自优化显微镜”愿景——在实验过程中动态维持最佳光路状态。

3. **计算开销随维度剧增**：
   - 当前GP建模在>6维时成为瓶颈；
   - 未来需发展更高效的surrogate modeling策略（如稀疏GP、神经过程）。

4. **开启高级束流工程可能性**：
   - 不再追求“圆形对称探针”，而是**主动设计非对称probe wavefunction**；
   - 例如沿特定晶向增强分辨率，用于应变场mapping等任务。

---

### ⚠️ 方法的局限性
| 局限性 | 说明 |
|-------|------|
| **计算复杂度高** | 高维GP训练时间长，限制实时性（尤其>7参数） |
| **依赖合理reward设计** | 若reward函数不能反映真实物理需求，则结果仍可能偏离预期 |
| **未涵盖所有像差类型** | 当前聚焦于低阶与部分二阶像差，更高阶（如C₃）尚未完全集成 |
| **初始点敏感性** | 尽管比梯度法稳健，但仍受初始探索策略影响 |

---

### 🔮 未来工作方向
1. **优化-仪器协同设计（Optimization-Instrumentation Co-Design）**
   - 加速计算端：GPU加速GP推理、轻量化kernel approximation；
   - 优化硬件端：降低通信延迟、提升探测器帧率。

2. **引入任务定制化probe shaping**
   - 利用MOBO调控非对称像差，生成定向拉伸或旋转探针，服务于特定材料表征任务。

3. **构建数字孪生（Digital Twin）系统**
   - 利用历史优化轨迹训练更快的替代模型，实现跨样品/仪器的知识迁移。

4. **扩展至原位动态实验**
   - 在加热、偏压、液体池等动态条件下持续跟踪并修正像差漂移。

---

> 🏁 **总结一句话**：  
> 本文提出的**Physics-Aware MOBO框架**为实现真正意义上的**Self-Optimizing Electron Microscopy**迈出了关键一步——它不仅提升了像差校正的速度与鲁棒性，更重要的是，将“什么是好图像”的定义权交还给物理规律与人类经验，而非黑箱模型或单一指标。

</details>

---

### 14. [Learn and Verify: A Framework for Rigorous Verification of Physics-Informed Neural Networks](https://arxiv.org/abs/2601.19818)

**Authors**: Kazuaki Tanaka, Kohei Yatabe  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.19818v1  

#### Abstract
The numerical solution of differential equations using neural networks has become a central topic in scientific computing, with Physics-Informed Neural Networks (PINNs) emerging as a powerful paradigm for both forward and inverse problems. However, unlike classical numerical methods that offer estab...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learn and Verify: A Framework for Rigorous Verification of Physics-Informed Neural Networks

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **Physics-Informed Neural Networks (PINNs)** 的微分方程求解器虽然在科学计算中广泛应用，但存在两个根本性缺陷：
- 缺乏**严格的误差界**（rigorous error bounds），无法数学上认证其精度；
- 训练过程具有**非确定性**（stochastic optimization），导致结果难以复现和验证。

这使得 PINNs 难以应用于对可靠性要求极高的科学机器学习场景。

### 🚀 提出的新方法：Learn and Verify 框架
作者提出了一种两阶段框架——“**Learn and Verify**”，将深度学习的灵活性与**验证数值计算**（verified numerical computation）的严谨性相结合。

#### 核心思想：
- **Learn 阶段**：训练 PINNs 构造一对 **sub-solution** 和 **super-solution**，即严格包围真实解的上下界函数。
- **Verify 阶段**：利用 **interval arithmetic** 对这些候选函数进行**机器可验证的数学证明**，确保它们在整个连续域上满足所需的微分不等式。

#### 创新点：
1. **首次实现 PINNs 解的严格验证**  
   将 PINNs 输出转化为可被 interval arithmetic 验证的形式，填补了神经网络求解器与传统验证方法之间的鸿沟。

2. **提出 Doubly Smoothed Maximum (DSM) 损失函数**  
   用于平滑地优化 sub/super-solution 的残差约束，克服了传统 max(·,0) 函数带来的梯度消失问题，提升训练稳定性。

3. **引入 Variation Learning 结构**  
   通过构造 $ u_{\theta}(t) \pm v_g(t), w_g(t) $ 的形式显式控制误差边界宽度，并保证非负性（via scaled sigmoid），使误差容忍度 $ \epsilon $ 成为可控参数。

4. **独立于生成方式的验证机制**  
   验证过程完全独立于模型如何生成，即使是“黑箱”PINN 输出也可被严格认证。

### 🔍 相比现有方法的优势
| 方面 | 传统 PINNs / 数值方法 | 本文方法 |
|------|------------------------|---------|
| 误差保证 | 无或仅渐近估计 | **可计算、严格的后验误差界** |
| 验证方式 | 依赖参考解或启发式 | **无需真值，纯数学证明** |
| 数值精度 | 浮点误差未受控 | **interval arithmetic 控制舍入误差** |
| 应用范围 | 一般只处理光滑解 | 支持 **piecewise C¹ 函数**（如含突变导数） |

---

## 2. 核心实验方法和设置

### 📚 实验对象（ODEs）
论文在三个非线性常微分方程（ODEs）上进行了验证实验，涵盖不同挑战类型：

| 问题 | 类型 | 特点 |
|------|------|------|
| **Classical Logistic Equation** | 经典人口增长模型 | 存在解析解，用于定量评估误差 |
| **Generalized Logistic Equation** | 时变系数扩展模型 | 无闭式解，测试框架普适性 |
| **Riccati Equation** | 有限时间爆破（blow-up） | 解趋于无穷，检验奇异性处理能力 |

### ⚙️ 实验设置
- **网络架构**：采用 **SIREN**（正弦激活函数）作为主干网络，共 4 层，每层 30 神经元；
- **训练策略**：
  - Step 1：标准 PINN 训练近似解 $ u_\theta(t) $
  - Step 2：固定 $ u_\theta $，训练偏差网络 $ v_g(t), w_g(t) $ 构造 sub/super-solutions
- **输出约束**：$ v_g, w_g $ 输出层使用缩放 sigmoid $ \epsilon \cdot \sigma(\cdot) $，保证非负且范围可控
- **损失函数**：
  - 主要使用 DSM loss：  
    $$
    \mathcal{L}_{\text{Sub&Sup}} = \text{DSM}_{c_1,c_2}[R(u_\theta - v_g)] + \text{DSM}_{c_1,c_2}[-R(u_\theta + w_g)]
    $$
  - 引入物理正则项 $ \mathcal{L}_{\text{phys}} $ 抑制不稳定行为
- **验证工具**：使用 **INTLAB** 进行 interval arithmetic 验证，结合自适应细分策略（adaptive subdivision）
- **硬件环境**：AMD Ryzen 9 + 128GB RAM，MATLAB 2025b

### 📊 评估指标
- **验证成功率**（Verification Success Rate）：子/超解是否通过 interval arithmetic 验证
- **最大相对误差**（Max Relative Error） vs 解析解（仅 logistic case）
- **误差容忍度 $ \epsilon $** 的影响分析（消融实验）
- **blow-up 时间的上下界**（针对 Riccati 方程）

### 🆚 基线方法对比
本文未直接与其他 PINN 变体比较性能，而是强调：
- **现有方法缺乏可验证性**
- 所提框架提供的是 **“有数学证明保障的误差界”**，而非单纯更小的残差或 MSE
- 因此，对比本质是 **“不可信近似” vs “可信包围”**

---

## 3. 主要实验结果和性能指标

### ✅ 关键性能数据

#### （1）Logistic Equation 实验结果
- 在 $ \epsilon = 2^{-5} $ 时，6 层网络在 300 轮后验证成功率达 **100%**
- 当 $ \epsilon \leq 2^{-8} $ 时，所有配置均失败 → 表明逼近误差下限约为 $ 2^{-7} \sim 2^{-8} $
- 加入物理正则项 $ \mathcal{L}_{\text{phys}} $ 后，成功率从不足 10% 提升至接近 100%

> 图表显示：随着 $ \lambda_{\text{phys}} $ 增大，mean error 显著下降，success rate 呈 S 形上升

#### （2）Generalized Logistic Equation
- 由于无解析解，以高精度 ODE45 数值解为参考
- 即便如此，验证成功率仍随 $ \epsilon $ 减小而降低
- 多层网络表现优于浅层（>3 层必要）
- **4 层网络表现最优**，表明存在表达力与可训练性的平衡

#### （3）Riccati Equation（Blow-up Case）
| $ \epsilon $ | $ c $ | $ u(T) $ | Blow-up 下界 | 上界 |
|--------------|-------|----------|-------------|-----|
| $ 2^{-2} $ | 2.00 | 12.119 | 1.9375 | 2.020 |
| $ 2^{-3} $ | 2.00 | 14.029 | 1.9375 | 2.009 |
| $ 2^{-4} $ | 2.00 | 14.893 | 1.9375 | 2.005 |

- 随着 $ \epsilon $ 减小，上界不断收紧（更精确）
- 成功实现了对 **有限时间爆破时间 $ T_{\text{blow-up}} $** 的**严格包围**
- 证明该框架能处理奇异性问题

### 🔬 消融实验结果
| 因素 | 影响 |
|------|------|
| **减小 $ \epsilon $** | 训练难度显著增加，需更多 epoch 才能收敛 |
| **增加网络层数** | 表达能力增强，但过深（如 6 层）反而不如 4–5 层稳定 |
| **增大 $ \lambda_{\text{phys}} $** | 显著提高训练稳定性与验证成功率 |
| **使用 DSM loss** | 必不可少；尝试直接优化 max(·,0) 导致训练失败 |

> 结论：DSM 提供软间隔（soft margin），即使轻微违反也施加惩罚，避免陷入局部最小

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Learn and Verify 框架可行且有效**  
   成功为多个非线性 ODE 构建了**严格包围真实解的 sub/super-solutions**，并完成机器验证。

2. **误差可以被数学认证而非估计**  
   不再依赖残差大小或与参考解的距离，而是通过 interval arithmetic 给出**可验证的误差界**。

3. **DSM 损失函数对训练至关重要**  
   平滑近似极大提升了优化稳定性，解决了传统方法中因采样随机性导致的最大残差漏检问题。

4. **支持全局解与奇异解的验证**  
   通过 piecewise C¹ 条件，允许连接点处导数不连续，适用于延拓到无限时间域或处理 blow-up 现象。

5. **参数选择可自动化指导**  
   验证结果本身提供了反馈信号，可用于自动调整 $ \epsilon $、网络结构等超参。

### ⚠️ 方法的局限性
- **计算开销集中在训练阶段**：虽然验证仅需几秒，但训练 sub/super-solutions 较慢（3–15 分钟）
- **目前仅适用于 ODEs**：向 PDEs 推广尚待研究
- **依赖良好初始化与正则化**：若初始近似解太差，难以构造有效包围
- **interval arithmetic 的保守性**：可能导致 false negative（实际成立但未能验证）

### 🔮 未来工作方向
1. **扩展至 PDEs 和高维系统**
2. **开发自动调参机制**（如将 $ \epsilon $ 或 $ c $ 设为可学习变量）
3. **结合其他 verified learning 工具**（如 Taylor models）
4. **GPU 加速训练流程**
5. **探索更高效的 sub/super-solution 参数化方式**

---

> **总结一句话**：  
> 本论文提出了首个将 PINNs 与 verified computing 相结合的“**Learn and Verify**”框架，实现了对神经网络解的**数学可证明误差界认证**，为构建**可信的科学机器学习**奠定了基础。

</details>

---

### 15. [Balancing Sustainability And Performance: The Role Of Small-Scale Llms In Agentic Artificial Intelligence Systems](https://arxiv.org/abs/2601.19311)

**Authors**: Anh Khoa Ngo Ho, Martin Chauvin, Simon Gosset, Philippe Cordier, Boris Gamazaychikov  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19311v1  

#### Abstract
As large language models become integral to agentic artificial intelligence systems, their energy demands during inference may pose significant sustainability challenges. This study investigates whether deploying smaller-scale language models can reduce energy consumption without compromising respon...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Balancing Sustainability and Performance: The Role of Small-Scale LLMs in Agentic Artificial Intelligence Systems*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本研究聚焦于**大型语言模型（LLMs）在智能体（agentic）AI系统中推理阶段的高能耗问题**，探讨如何在不牺牲响应速度和输出质量的前提下，通过部署小规模LLMs来提升系统的可持续性。当前，随着LLMs在多智能体架构中的广泛应用，其环境足迹（尤其是能源消耗）日益受到关注。

### 提出的新方法与新思路
- **构建了一个三目标优化框架**：从 **Environmental Impact（环境影响）**、**User Experience（用户体验）** 和 **Output Quality（输出质量）** 三个维度综合评估LLM的部署表现。
- **提出并应用了可复现的基准测试流程**：基于 `ML-Energy Benchmark` 框架，在真实世界多智能体场景下对多种开源权重（open-weights）LLMs进行系统性比较。
- **引入 Overall Metric (OM)**：一个加权综合评分公式，用于统一衡量不同模型在能效、延迟和质量之间的权衡：
  $$
  OM = \left(\frac{Quality_E}{Quality_R}\right)^{W_{Quality}} \times \left(\frac{Latency_R}{Latency_E}\right)^{W_{Latency}} \times \left(\frac{Energy_R}{Energy_E}\right)^{W_{Energy}}
  $$
  其中值大于1表示优于基线模型（GPT-4o）。

### 相比现有方法的优势
- **更全面的评估视角**：不同于以往仅关注单一维度（如准确率或延迟），本文首次在**真实部署环境**中联合分析三个关键目标。
- **强调实际可行性**：不仅评估原始模型，还纳入量化（Quantization）、知识蒸馏（Knowledge Distillation, KD）等压缩技术，并考虑硬件配置（GPU数量）、批处理大小（batch size）的影响。
- **支持开放模型替代闭源模型**：为使用开源LLMs替代GPT-4o类闭源模型提供了实证依据和实用指南。

---

## 2. 核心实验方法和设置

### 使用的数据集
- 数据来源于一个已部署的真实多智能体系统，包含 **1,000个代表性请求**。
- 平均每个请求约 **8,000 tokens**，最长达 **25,500 tokens**。
- 输出平均长度为 **66 tokens**，最大为 **118 tokens**。
- 所有参考标签由 **GPT-4o** 在真实环境中生成，作为评估其他模型的标准。

### 实验设置
- **评估对象**：共测试 **28个开源LLMs** 及其 **7个压缩版本**，涵盖多个模型家族：
  - Qwen 2.5 / Qwen 3（0.5B–72B）
  - Mistral（12B–123B）
  - Gemma 3（4B–27B）
  - Falcon 3（1B–10B）
  - Phi-4（14B）
  - Llama-4-Scout（MoE, ~109B total）
  - 蒸馏模型（如 DeepSeek-R1-Distill-Qwen）
  - 量化模型（GPTQ-Int4/8, AWQ）
- **部署平台**：AWS `p4d.24xlarge` 实例（8×NVIDIA A100 40GB GPU）
- **推理引擎**：`vLLM v0.5.4`，支持高效批处理和服务
- **能量测量工具**：集成 `Zeus` 库，采集稳态下的每token能耗，进而估算每请求能耗
- **批处理大小（Batch Size）变量**：测试了从 2 到 512 的多种最大批处理配置

### 评估指标
| 维度 | 指标 | 说明 |
|------|------|------|
| **Environmental Impact** | Energy per Request (Joules) | 使用 ML-Energy Benchmark 测量GPU推理能耗 |
| **User Experience** | Decode Latency per Request (Seconds) | 解码阶段耗时，反映用户感知响应速度 |
| **Output Quality** | F1-score (Macro) | 分类标签（grounded/ungrounded/small talk）与GPT-4o的一致性 |
| | LLM-as-a-Judge Score (0–1) | 使用更强LLM（GPT-5）按预定义标准打分，评估回复事实准确性 |

### 基线方法对比
- **主基线**：`GPT-4o`（通过API访问）
- GPT-4o的能耗与延迟基于 **jegham*how*2025** 的估算方法推导得出（因无法直接监控硬件）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）

| Model | Energy (J) ↓ | Decode Latency (s) ↓ | F1-score ↑ | LLM-as-a-Judge ↑ |
|-------|--------------|------------------------|------------|------------------|
| **GPT-4o (baseline)** | 1499 ± 287 | 0.58 ± 0.10 | 1.000 | 0.978 ± 0.116 |
| **Qwen3-30B-A3B-Instruct** | **456 ± 97** | **50 ± 10** | 0.917 | 0.956 ± 0.156 |
| **Qwen3-32B** | 2382 ± 725 | 85 ± 26 | 0.918 | **0.992 ± 0.064** |
| **Falcon3-7B-Instruct** | **335 ± 348** | 17 ± 17 | **0.910** | 0.885 ± 0.245 |

> 注：最优值加粗；Qwen3-30B-A3B 是唯一实现 **70% 能耗降低** 且质量接近GPT-4o 的模型。

### 与基线方法的对比结果
- **能效优势显著**：
  - 多个小模型（如 Falcon3-7B, Qwen3-30B-A3B）能耗仅为 GPT-4o 的 **22%-30%**。
  - Qwen3-30B-A3B-Instruct（MoE架构）激活参数仅 **3.3B**，却达到接近 GPT-4o 的输出质量。
- **延迟方面闭源仍占优**：
  - GPT-4o 的 decode latency 最短（~0.58秒），得益于高度优化的服务端部署。
  - 开源模型本地部署普遍延迟更高，部分因输出更长或未充分调优。
- **输出质量可媲美甚至超越**：
  - Qwen3-32B 在 LLM-as-a-Judge 上得分 **0.992 > 0.978**（GPT-4o），表明其回复更具事实性和清晰度。
  - 多数10B以上模型F1-score保持在0.8以上，满足企业级需求。

### 消融实验结果

#### （1）模型尺寸缩放分析（Qwen 2.5系列）
- **并非越大越好**：7B模型的F1-score（0.74）高于14B（0.68）和32B（0.70），说明存在“甜点”规模。
- **能耗随规模指数增长**：72B模型需8×GPU，能耗是7B的数十倍，而性能提升有限。
- **decode latency 近似线性增长**，但受多GPU通信开销影响。

#### （2）压缩技术效果
| 方法 | 模型 | 能耗变化 | 延迟变化 | F1-score |
|------|------|----------|----------|-----------|
| GPTQ-Int4 | Qwen2.5-7B | ↓ ~20% | ↓ ~20% | ↑（优于原版） |
| AWQ | Qwen2.5-7B | 微降 | ↑（变慢） | — |
| KD（蒸馏至1.5B） | DeepSeek-R1 → Qwen | ↓ | ↓ | < 7B原模型 |
| KD（应用于7B） | DeepSeek-R1 → Qwen | ↓ | ↓ | ↓↓（F1下降29%） |

> 结论：**GPTQ-Int4 是最有效的压缩方案**；知识蒸馏对小模型有效，但在同规模上可能损害性能。

#### （3）批处理大小影响（见 Figure 6）
- 存在明显的 **Energy-Latency 权衡曲线（Pareto Frontier）**
- 小模型对 batch size 更敏感：
  - 将 Qwen2.5-1.5B 的 batch size 提升至512，可节能 **81%**，但延迟增加 **>400%**
- 大模型对此调节不敏感，边际效益递减。

#### （4）额外GPU对 Nano-LLMs 的影响（见 Figure 7）
- 当 max batch ≤ 64 时，单GPU即可承载KV cache，增加GPU无益。
- 当 max batch > 64 后，第二块GPU允许更大并发，但会带来额外能耗和延迟。
- 推荐策略：**根据请求频率和KV cache容量动态分配GPU资源**

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **小规模开源LLMs可在保持高质量的同时大幅降低能耗**，是构建可持续AI系统的可行路径。
2. ✅ **模型大小与性能不成正比**：中等规模模型（如7B–14B）常优于更大模型，尤其在特定任务上。
3. ✅ **混合专家（MoE）架构极具潜力**：Qwen3-30B-A3B 以极低激活参数实现高性能与高能效平衡。
4. ✅ **批处理大小是关键调控参数**：合理设置可显著优化能效，但需警惕延迟代价。
5. ✅ **压缩技术有效但需谨慎选择**：GPTQ-Int4 表现最佳；知识蒸馏适用于学生模型训练，不适合直接替换同规模模型。

### 方法的局限性
- **任务特定性**：实验集中在“幻觉检测”这一具体任务，结论未必泛化到规划、工具调用等复杂智能体行为。
- **数据偏差**：提示词针对 GPT-4o 设计，可能导致小模型输出冗长或格式错误。
- **闭源模型依赖估算**：GPT-4o 的能耗与延迟为间接估计，缺乏硬件级验证。
- **缺少人类评估**：LLM-as-a-Judge 可能引入偏见，未来应结合人工标注。
- **基础设施限制**：实验仅在 AWS A100 上进行，未探索H100、TPU或其他云厂商差异。

### 未来工作方向
- 🔄 **扩展至更多任务类型**：将基准推广至多轮对话、任务规划、工具使用等典型 agentic 场景。
- 🔍 **引入多样化数据集**：覆盖不同语言、领域、prompt长度的任务，增强普适性。
- ⚙️ **优化自托管部署栈**：
  - 升级至 H100 等新一代GPU
  - 应用 TensorRT-LLM、speculative decoding 等高级推理优化
  - 探索 disaggregated serving 架构
- ✍️ **加强提示工程研究**：设计轻量、高效的 prompts 以减少 prefill 阶段能耗。
- 🌍 **全生命周期碳足迹建模**：结合电力来源、PUE、冷却水耗等因素，建立更完整的可持续性评估体系。

---

> **最终建议**：对于企业级 agentic AI 系统，推荐优先考虑 **Qwen3-30B-A3B-Instruct** 或 **Falcon3-7B-Instruct** 等高效模型，并结合 **GPTQ量化** 与 **最优batch size配置**，在保障服务质量的同时实现高达70%的能耗节约。

</details>

---

### 16. [Modular Foundation Model Inference at the Edge: Network-Aware Microservice Optimization](https://arxiv.org/abs/2601.19563)

**Authors**: Juan Zhu, Zixin Wang, Shenghui Song, Jun Zhang, Khaled Ben Letaief  
**Category**: cs.DC  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19563v1  

#### Abstract
Foundation models (FMs) unlock unprecedented multimodal and multitask intelligence, yet their cloud-centric deployment precludes real-time responsiveness and compromises user privacy. Meanwhile, monolithic execution at the edge remains infeasible under stringent resource limits and uncertain network...

---

### 17. [GraIP: A Benchmarking Framework For Neural Graph Inverse Problems](https://arxiv.org/abs/2601.18917)

**Authors**: Semih Cant\"urk, Andrei Manolache, Arman Mielke, Chendi Qian, Antoine Siraudin, Christopher Morris, Mathias Niepert, Guy Wolf  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.18917v1  

#### Abstract
A wide range of graph learning tasks, such as structure discovery, temporal graph analysis, and combinatorial optimization, focus on inferring graph structures from data, rather than making predictions on given graphs. However, the respective methods to solve such problems are often developed in an ...

---

### 18. [Accelerated training of Gaussian processes using banded square exponential covariances](https://arxiv.org/abs/2601.19007)

**Authors**: Emily C. Ehrhardt, Felipe Tobar  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19007v1  

#### Abstract
We propose a novel approach to computationally efficient GP training based on the observation that square-exponential (SE) covariance matrices contain several off-diagonal entries extremely close to zero. We construct a principled procedure to eliminate those entries to produce a \emph{banded}-matri...

---

### 19. [E-QRGMM: Efficient Generative Metamodeling for Covariate-Dependent Uncertainty Quantification](https://arxiv.org/abs/2601.19256)

**Authors**: Zhiyang Liang, Qingkai Zhang  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19256v1  

#### Abstract
Covariate-dependent uncertainty quantification in simulation-based inference is crucial for high-stakes decision-making but remains challenging due to the limitations of existing methods such as conformal prediction and classical bootstrap, which struggle with covariate-specific conditioning. We pro...

---

### 20. [SEAFormer: A Spatial Proximity and Edge-Aware Transformer for Real-World Vehicle Routing Problems](https://arxiv.org/abs/2601.19395)

**Authors**: Saeed Nasehi Basharzad, Farhana Choudhury, Egemen Tanin  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19395v1  

#### Abstract
Real-world Vehicle Routing Problems (RWVRPs) require solving complex, sequence-dependent challenges at scale with constraints such as delivery time window, replenishment or recharging stops, asymmetric travel cost, etc. While recent neural methods achieve strong results on large-scale classical VRP ...

---

### 21. [OSIRIS: Bridging Analog Circuit Design and Machine Learning with Scalable Dataset Generation](https://arxiv.org/abs/2601.19439)

**Authors**: Giuseppe Chiari, Michele Piccoli, Davide Zoni  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.19439v1  

#### Abstract
The automation of analog integrated circuit (IC) design remains a longstanding challenge, primarily due to the intricate interdependencies among physical layout, parasitic effects, and circuit-level performance. These interactions impose complex constraints that are difficult to accurately capture a...

---

### 22. [SETA: Statistical Fault Attribution for Compound AI Systems](https://arxiv.org/abs/2601.19337)

**Authors**: Sayak Chowdhury, Meenakshi D'Souza  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19337v1  

#### Abstract
Modern AI systems increasingly comprise multiple interconnected neural networks to tackle complex inference tasks. Testing such systems for robustness and safety entails significant challenges. Current state-of-the-art robustness testing techniques, whether black-box or white-box, have been proposed...

---

### 23. [ComAgent: Multi-LLM based Agentic AI Empowered Intelligent Wireless Networks](https://arxiv.org/abs/2601.19607)

**Authors**: Haoyun Li, Ming Xiao, Kezhi Wang, Robert Schober, Dong In Kim, Yong Liang Guan  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19607v1  

#### Abstract
Emerging 6G networks rely on complex cross-layer optimization, yet manually translating high-level intents into mathematical formulations remains a bottleneck. While Large Language Models (LLMs) offer promise, monolithic approaches often lack sufficient domain grounding, constraint awareness, and ve...

---

### 24. [Algorithmic Prompt-Augmentation for Efficient LLM-Based Heuristic Design for A* Search](https://arxiv.org/abs/2601.19622)

**Authors**: Thomas B\"omer, Nico Koltermann, Max Disselnmeyer, Bastian Amberg, Anne Meyer  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19622v1  

#### Abstract
Heuristic functions are essential to the performance of tree search algorithms such as A*, where their accuracy and efficiency directly impact search outcomes. Traditionally, such heuristics are handcrafted, requiring significant expertise. Recent advances in large language models (LLMs) and evoluti...

---

### 25. [Optimizing Conversational Quality in Spoken Dialogue Systems with Reinforcement Learning from AI Feedback](https://arxiv.org/abs/2601.19063)

**Authors**: Siddhant Arora, Jinchuan Tian, Jiatong Shi, Hayato Futami, Yosuke Kashiwagi, Emiru Tsunoo, Shinji Watanabe  
**Category**: cs.CL  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19063v1  

#### Abstract
Reinforcement learning from human or AI feedback (RLHF/RLAIF) for speech-in/speech-out dialogue systems (SDS) remains underexplored, with prior work largely limited to single semantic rewards applied at the utterance level. Such setups overlook the multi-dimensional and multi-modal nature of convers...

---

### 26. [Vector-Valued Distributional Reinforcement Learning Policy Evaluation: A Hilbert Space Embedding Approach](https://arxiv.org/abs/2601.18952)

**Authors**: Mehrdad Mohammadi, Qi Zheng, Ruoqing Zhu  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.18952v1  

#### Abstract
We propose an (offline) multi-dimensional distributional reinforcement learning framework (KE-DRL) that leverages Hilbert space mappings to estimate the kernel mean embedding of the multi-dimensional value distribution under a proposed target policy. In our setting, the state-action variables are mu...

---

### 27. [Speed is Confidence](https://arxiv.org/abs/2601.19085)

**Authors**: Joshua V. Dillon  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19085v1  

#### Abstract
Biological neural systems must be fast but are energy-constrained. Evolution's solution: act on the first signal. Winner-take-all circuits and time-to-first-spike coding implicitly treat when a neuron fires as an expression of confidence. We apply this principle to ensembles of Tiny Recursive Models...

---

### 28. [Smoothing the Score Function for Generalization in Diffusion Models: An Optimization-based Explanation Framework](https://arxiv.org/abs/2601.19285)

**Authors**: Xinyu Zhou, Jiawei Zhang, Stephen J. Wright  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19285v1  

#### Abstract
Diffusion models achieve remarkable generation quality, yet face a fundamental challenge known as memorization, where generated samples can replicate training samples exactly. We develop a theoretical framework to explain this phenomenon by showing that the empirical score function (the score functi...

---

### 29. [Improving Policy Exploitation in Online Reinforcement Learning with Instant Retrospect Action](https://arxiv.org/abs/2601.19720)

**Authors**: Gong Gao, Weidong Zhao, Xianhui Liu, Ning Jia  
**Category**: cs.LG  
**Published**: 2026-01-28  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.19720v1  

#### Abstract
Existing value-based online reinforcement learning (RL) algorithms suffer from slow policy exploitation due to ineffective exploration and delayed policy updates. To address these challenges, we propose an algorithm called Instant Retrospect Action (IRA). Specifically, we propose Q-Representation Di...

---

### 30. [Neural Theorem Proving for Verification Conditions: A Real-World Benchmark](https://arxiv.org/abs/2601.18944)

**Authors**: Qiyuan Xu, Xiaokun Luan, Renxi Wang, Joshua Ong Jun Leang, Peixin Wang, Haonan Li, Wenda Li, Conrad Watt  
**Category**: cs.AI  
**Published**: 2026-01-28  
**Score**: 4.5  
**Type**: new  
**ArXiv ID**: 2601.18944v1  

#### Abstract
Theorem proving is fundamental to program verification, where the automated proof of Verification Conditions (VCs) remains a primary bottleneck. Real-world program verification frequently encounters hard VCs that existing Automated Theorem Provers (ATPs) cannot prove, leading to a critical need for ...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
