# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-07 05:58:05 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Lil: Less is Less When Applying Post-Training Sparse-Attention Algorithms in Long-Decode Stage](https://arxiv.org/abs/2601.03043)

**Authors**: Junhao Hu, Fangze Li, Mingtao Xu, Feifan Meng, Shiju Zhao, Tiancheng Hu, Ting Peng, Anmin Liu, Wenrui Huang, Chenxu Liu, Ziyue Hua, Tao Xie  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2601.03043v1  

#### Abstract
Large language models (LLMs) demonstrate strong capabilities across a wide range of complex tasks and are increasingly deployed at scale, placing significant demands on inference efficiency. Prior work typically decomposes inference into prefill and decode stages, with the decode stage dominating to...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Lil: Less is Less When Applying Post-Training Sparse-Attention Algorithms in Long-Decode Stage

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文揭示了一个在**Post-Training Sparse-Attention (PTSD)** 算法中被忽视的关键问题，称为 **“Lil” (Less is Less)**。尽管稀疏注意力（sparse attention）能加速单步解码（decode），但由于其在长序列生成过程中丢失上下文信息，导致模型需要生成更长的输出序列来补偿，反而增加了端到端的推理延迟和内存占用。

这一现象挑战了稀疏注意力“提升效率”的直观假设，尤其是在**reasoning-intensive tasks**（如数学推理、链式思维 CoT）中尤为严重。

### 提出的新方法和新思路
为解决 Lil 问题，作者提出了一种名为 **Guardian** 的**早期停止算法**（early-stopping algorithm），其核心思想是：
- 利用 **LZ77 压缩算法**动态监测生成序列的信息增益。
- 当压缩后的字节数增长停滞（即新增 token 带来的信息增益低于设定阈值）时，判定模型已进入冗余生成阶段，主动终止解码。

该方法基于信息论：**低压缩率意味着高冗余，信息熵趋于饱和时应停止生成**。

### 相比现有方法的优势
- **无需重新训练**：Guardian 是一个插件式（plug-and-play）模块，适用于任何预训练 LLM。
- **高效轻量**：LZ77 压缩成本极低（约 34ms 处理 128k tokens），每 250 步检查一次，开销可忽略。
- **通用性强**：不仅适用于稀疏注意力场景，也适用于普通 full attention 下的冗余 CoT 生成。
- **显著节省 token 消耗**：最多减少 90% 的 token 生成，同时保持 <2% 的准确率下降。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个面向复杂推理的 benchmark 上进行评估：
- **GSM8K**：小学数学应用题，需多步算术推理。
- **MATH-500**：高中数学竞赛题，涵盖代数、几何等五级难度。
- **AIME**：美国数学邀请赛题目，挑战顶尖高中生。

每个数据集取前 200 个测试样例。

### 实验设置和评估指标
- **模型**：
  - **DSR**：DeepScaleR-1.5B-Preview
  - **DSL**：DeepSeek-R1-Distill-Llama-8B
  - **Qwe**：Qwen1.5-MoE-A2.7B-Chat
- **硬件环境**：单张 NVIDIA A100-80GB GPU，Ubuntu 20.04，CUDA 12.6。
- **评估指标**：
  - **Token savings**：应用 Guardian 后相比原始生成的 token 节省比例。
  - **Accuracy**：输出与标准答案的数学等价性判断，报告正确率及变化（ΔAccuracy）。

### 基线方法对比
比较了五种主流 PTSD 算法：
- **H2O**、**Sink**：通过保留“重击者”（heavy hitters）并丢弃 KV 缓存来实现稀疏。
- **infLLM**、**Quest**：不丢弃 KV，但限制 attention 范围。
- **Full attention**：完整注意力作为基准。

所有方法均在不同 **cache budget**（128–1024）下测试，并集成 Guardian 进行对比。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
根据 **Table 1** 和 **Figure 1**，核心结果如下：

| 指标 | 结果 |
|------|------|
| **最大 token 节省** | 高达 **90%** |
| **准确率损失** | 小于 **2%**（绝对值） |
| **部分情况下准确率提升** | 最多 +1.5%，因避免了过度验证导致遗忘正确答案 |

### 与基线方法的对比结果
- **Without Guardian**：
  - 所有稀疏注意力算法均导致输出长度增加（最高达 90%），尤其在小 cache budget 下更明显。
  - H2O/Sink 因丢弃 KV 导致信息丢失严重，准确率较低；Quest/infLLM 表现更好但仍存在冗余。
- **With Guardian**：
  - 在所有 PTSD 算法上均显著降低 token 消耗。
  - 对保守配置（如 cache=1024）仍有效，说明即使接近 full attention 也存在可压缩冗余。
  - 在 DSR + Quest + AIME 场景下实现 90% token 节省，仅降 0.5% 准确率。

### 消融实验结果
- **按任务结果分类统计（Table 3）**：
  - **错误案例**：Guardian 主要通过提前终止无限循环生成来节省 token。
  - **正确案例**：防止模型在得出答案后继续“反复验证”，从而忘记答案。
- **在 Full Attention 上的应用（Table 2）**：
  - 即使没有使用稀疏注意力，Guardian 也能节省 9–18% 的 token，表明 CoT 冗余普遍存在。
  - 说明 Guardian 可泛化至非稀疏场景，用于压缩低效推理路径。

---

## 4. 关键结论和发现

### 主要发现
1. **Lil 问题是 PTSD 的“房间里的大象”**：
   - 稀疏注意力虽加快单步解码，但因信息丢失引发重复重建行为，最终延长总生成长度，抵消甚至逆转效率优势。
2. **信息熵可用于检测冗余生成**：
   - 使用 LZ77 压缩比作为信息密度代理，发现生成过程常经历“快速增长 → 平台期”阶段，平台期即为冗余区。
3. **Guardian 显著缓解 Lil 问题**：
   - 在 reasoning-intensive 任务中，平均节省 50–90% token，准确率损失 <2%。
   - 在某些情况还能提升准确率，避免“画蛇添足”。

### 方法的局限性
- **依赖模型能力**：对弱模型（如 Qwen MoE）效果有限，因其本身生成短且错误率高，缺乏足够冗余供压缩。
- **评估范围有限**：
  - 仅测试三种模型和三个数据集，可能影响泛化性。
  - 未覆盖所有稀疏算法（如 ClusterKV、PQ-Cache），但作者认为 Lil 本质源于稀疏性假设，具有普适性。
- **压缩频率参数需调优**：`f=250` 和 `t=20` 在实验中表现良好，但在不同架构或序列长度下可能需调整。

### 未来工作方向
- 探索 Guardian 在 **general prolonged CoT generation** 中的应用，尤其是由训练偏差、奖励黑客等引起的冗余。
- 研究如何将 Guardian 与现有的 CoT 压缩方法（如 ThinkPrune、HALT-COT）结合。
- 扩展至更多模型和更复杂任务（如代码生成、长文本写作）以验证通用性。

---

> **总结一句话**：  
> 本文指出稀疏注意力可能导致“越稀越慢”的 **Lil 陷阱**，并提出基于信息压缩的 **Guardian 早停机制**，在几乎不影响准确率的前提下，将 token 消耗降低高达 90%，为高效长序列推理提供了新范式。

</details>

---

### 2. [LoRA-Drop: Temporal LoRA Decoding for Efficient LLM Inference](https://arxiv.org/abs/2601.02569)

**Authors**: Hossein Rajabzadeh, Maryam Dialameh, Chul B. Park, Il-Min Kim, Hyock Ju Kwon  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.02569v1  

#### Abstract
Autoregressive large language models (LLMs) are bottlenecked by sequential decoding, where each new token typically requires executing all transformer layers. Existing dynamic-depth and layer-skipping methods reduce this cost, but often rely on auxiliary routing mechanisms or incur accuracy degradat...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*LoRA-Drop: Temporal LoRA Decoding for Efficient LLM Inference*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在自回归推理过程中面临严重的**计算瓶颈**，即每个新 token 都需要通过所有 transformer 层进行完整前向传播，导致高延迟和高内存开销。尽管已有动态深度或层跳过方法尝试缓解该问题，但它们通常依赖额外的路由机制，或在跳过层未补偿的情况下造成精度下降。

### 提出的新方法：LoRA-Drop
本文提出 **LoRA-Drop**，一种轻量级、即插即用的高效推理框架，其核心思想是利用 LLM 中隐藏状态的**时间冗余性**（temporal redundancy），在大多数解码步骤中复用前一个 token 的中间层表示，并通过低秩适配（LoRA）进行微调更新。

#### 核心机制：
- **固定子集的可跳过层（droppable layers）**：选择部分中间 transformer 层注入 LoRA 模块。
- **周期性调度策略（periodic schedule）**：
  - **LoRA 模式（轻量步）**：在连续 `k` 步中，这些层跳过完整计算，仅应用 LoRA 对前一步隐藏状态进行修正。
  - **Full 模式（刷新步）**：每 `k+1` 步执行一次完整的模型前向传播，防止误差累积。
- **无需路由网络**：避免引入额外参数或复杂控制逻辑。

### 相比现有方法的优势
| 特性 | LoRA-Drop | Unified Layer Skipping / FlexiDepth | Speculative Decoding |
|------|-----------|-------------------------------------|------------------------|
| 是否需辅助模块 | ❌（无路由） | ⚠️（部分需） | ✅（需 draft model） |
| 是否保留上下文连贯性 | ✅（重用+LoRA校正） | ❌（直接跳过） | ✅（但有接受率问题） |
| 兼容 KV Cache | ✅ | ✅ | ✅ |
| 可降低 KV Cache 大小 | ✅（跳过层不频繁更新 KV） | ⚠️有限 | ❌ |
| 插件式部署 | ✅（仅需少量 fine-tuning） | ✅ | ❌（架构改动大） |

> ✅ LoRA-Drop 在保持精度的同时显著提升推理速度和内存效率，且完全兼容标准训练与推理流程。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多个任务类别，评估泛化能力：

| 类别 | 数据集 |
|------|-------|
| **通用理解与知识** | MMLU, HellaSwag (HS), WinoGrande, ARC-e/c, OpenBookQA, PIQA, RACE |
| **数学与推理** | GSM8K, MATH, Big-Bench Hard (BBH) |
| **代码生成** | HumanEval, MBPP |
| **长文本与多语言** | LongBench, Needle-in-a-Haystack, XNLI, XCOPA |

### 实验设置
- **模型家族**：
  - LLaMA2-7B, LLaMA3-8B
  - Qwen2.5-7B, Qwen2.5-14B
- **LoRA 参数**：
  - Rank `r = 16`, Scaling `α = 16`
  - 仅更新 LoRA 参数，冻结原始权重
- **Fine-tuning**：
  - 在 RefinedWeb 上进行约 15B token 的持续预训练（continual pretraining）
- **调度参数**：
  - Drop ratio `p ∈ {0.25, 0.5, 0.75}`（可跳过层比例）
  - Temporal window `k ∈ {1,2,3,5}`（每隔 `k+1` 步刷新一次）
- **硬件环境**：
  - NVIDIA A100/V100 GPU，使用 BF16 混合精度和 SDPA

### 评估指标
| 指标类型 | 具体指标 |
|--------|---------|
| **准确性** | Zero-shot Accuracy, Pass@1 (HumanEval/MBPP), F1/EM (LongBench) |
| **效率** | Tokens/sec, Speedup (×), Relative FLOPs |
| **内存** | KV Cache 内存占用（MB），节省百分比 |
| **延迟** | Per-token latency (p50, p95) |

### 基线方法对比
- **Full Model**：原始完整模型（无跳过）
- **Unified Layer Skipping** [18]
- **FlexiDepth** [19]

> 注：作者强调其他加速技术（如量化、推测解码）与本方法正交，故未纳入主比较，留待未来组合研究。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（来自 Table I 和 V）

| 模型 | Drop Ratio `p` | Window `k` | 平均准确度变化 ΔAcc | 推理速度提升 | KV Cache 节省 |
|------|---------------|------------|--------------------|--------------|----------------|
| LLaMA2-7B | 0.5 | 3 | -0.1 pp | **1.68×** | ~40% |
| Qwen2.5-7B | 0.5 | 3 | -0.1 pp | **1.73×** | ~45% |
| LLaMA3-8B | 0.5 | 3 | -0.3 pp | **1.70×** | ~42% |
| Qwen2.5-14B | 0.5 | 3 | -0.2 pp | **1.68×** | ~48% |
| 所有模型（激进配置） | 0.75 | 3–5 | ≤ -2.5 pp | **最高达 2.6×** | **45–55%** |

> 🔹 在 `p=0.5, k=3` 配置下，平均性能损失 < **0.5 pp**，属于“安全区”（safe zone）

### 与基线方法对比（Table I）
- LoRA-Drop 在相同速度增益下，**准确率优于 Unified Layer Skipping 和 FlexiDepth**
- 例如，在 LLaMA2-7B 上：
  - Unified Layer Skipping：1.42× speedup, avg acc ↓0.8 pp
  - LoRA-Drop (`p=0.5`)：**1.68× speedup**, avg acc ↓0.1 pp → 更优的 Pareto 前沿

### 消融实验结果（Table III & IV）

#### 不同 `p` 和 `k` 的权衡分析
| 配置 | 准确性影响 | 吞吐提升 | KV 缓存减少 |
|------|----------|---------|-------------|
| `p=0.25, k=3` | +0.2 pp | ~1.27× | ~17% |
| `p=0.50, k=3` | -0.1~0.3 pp | **1.68–1.73×** | **~40–45%** |
| `p=0.75, k=3` | ↓1.8–2.4 pp | **2.35–2.42×** | **~50–55%** |

> 🔹 发现“安全区”：`p ≤ 0.5` 且 `k ≤ 3` 时，精度损失极小（≤0.5 pp），速度提升明显（1.6–1.8×）

#### 延迟分布（Tail Latency）
- 由于周期性刷新机制，token 延迟呈双峰分布：
  - LoRA 步骤：低延迟
  - Refresh 步骤：较高延迟
- p95 延迟由刷新频率决定：当 `k > 19` 时，p95 进入 LoRA 模式，尾部延迟大幅下降

### LoRA 注入位置消融（Table V）
| 注入方式 | 准确性 | 速度增益 |
|--------|--------|---------|
| Attention+MLP LoRA | ≈ Baseline | ~1.37–1.39× |
| **Block-level LoRA（默认）** | ≈ Baseline | **~1.55–1.58×** |

> 🔹 **Block-level 注入**在精度不变前提下获得更高吞吐，为推荐默认配置。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLM 隐藏状态存在显著时间冗余**：
   - 相邻 token 的隐藏状态相似度高达 0.6–0.85（图1）
   - 单一状态可预测未来 3–6 个 token（Future Lens 现象支持）

2. ✅ **LoRA-Drop 可有效利用冗余实现高效推理**：
   - 通过周期性 LoRA 更新替代完整层计算，显著降低计算负担
   - 引入定期 full refresh 抑制误差漂移，保障长期一致性

3. ✅ **在多种模型和任务上表现稳健**：
   - 覆盖 7B–14B 规模、不同架构（LLaMA/Qwen）
   - 在推理、编码、长文本、多语言任务中均保持高质量输出

4. ✅ **KV Cache 显著压缩**：
   - 可跳过层在 LoRA 模式下无需更新 KV，节省 45–55%
   - 尤其适合长序列场景（long-context）

5. ✅ 存在一个清晰的“**效率-质量帕累托前沿**”：
   - `p=0.5, k=3` 是推荐默认配置，平衡速度与精度
   - `p=0.75` 适用于对延迟敏感的应用

### 方法的局限性
- ❗ 当前采用**固定周期调度**，尚未引入基于 token 复杂度的自适应机制（如熵值、logit margin）
- ❗ 极端设置（`p=0.75, k≥5`）可能导致精度明显下降（>2 pp）
- ❗ 仅验证了文本模态，未扩展至多模态或检索增强场景

### 未来工作方向
- 🔄 探索**自适应调度策略**：根据 token 不确定性动态调整是否刷新
- 🌐 扩展至**多模态与 RAG 模型**：应用于视觉-语言、音频-文本等跨模态推理
- 🔗 结合其他优化技术：与量化（Quantization）、KV 压缩、推测解码（Speculative Decoding）联合使用
- 🧠 深入分析层间冗余模式：构建更精细的 drop-layer selection 策略

---

> 💡 **总结一句话**：  
> LoRA-Drop 提出了一种简单而强大的“**周期性轻量更新 + 定期全量刷新**”范式，首次将 LoRA 用于推理阶段的时间维度优化，在几乎不牺牲性能的前提下实现了高达 **2.6× 的推理加速** 和 **超 50% 的 KV Cache 节省**，为大规模语言模型的高效部署提供了实用解决方案。

</details>

---

### 3. [MiMo-V2-Flash Technical Report](https://arxiv.org/abs/2601.02780)

**Authors**: Bangjun Xiao, Bingquan Xia, Bo Yang, Bofei Gao, Bowen Shen, Chen Zhang, Chenhong He, Chiheng Lou, Fuli Luo, Gang Wang, Gang Xie, Hailin Zhang, Hanglong Lv, Hanyu Li, Heyu Chen, Hongshen Xu, Houbin Zhang, Huaqiu Liu, Jiangshan Duo, Jianyu Wei, Jiebao Xiao, Jinhao Dong, Jun Shi, Junhao Hu, Kainan Bao, Kang Zhou, Lei Li, Liang Zhao, Linghao Zhang, Peidian Li, Qianli Chen, Shaohui Liu, Shihua Yu, Shijie Cao, Shimao Chen, Shouqiu Yu, Shuo Liu, Tianling Zhou, Weijiang Su, Weikun Wang, Wenhan Ma, Xiangwei Deng, Bohan Mao, Bowen Ye, Can Cai, Chenghua Wang, Chengxuan Zhu, Chong Ma, Chun Chen, Chunan Li, Dawei Zhu, Deshan Xiao, Dong Zhang, Duo Zhang, Fangyue Liu, Feiyu Yang, Fengyuan Shi, Guoan Wang, Hao Tian, Hao Wu, Heng Qu, Hongfei Yi, Hongxu An, Hongyi Guan, Xing Zhang, Yifan Song, Yihan Yan, Yihao Zhao, Yingchun Lai, Yizhao Gao, Yu Cheng, Yuanyuan Tian, Yudong Wang, Zhen Tang, Zhengju Tang, Zhengtao Wen, Zhichao Song, Zhixian Zheng, Zihan Jiang, Jian Wen, Jiarui Sun, Jiawei Li, Jinlong Xue, Jun Xia, Kai Fang, Menghang Zhu, Nuo Chen, Qian Tu, Qihao Zhang, Qiying Wang, Rang Li, Rui Ma, Shaolei Zhang, Shengfan Wang, Shicheng Li, Shuhao Gu, Shuhuai Ren, Sirui Deng, Tao Guo, Tianyang Lu, Weiji Zhuang, Weikang Zhang, Weimin Xiong, Wenshan Huang, Wenyu Yang, Xin Zhang, Xing Yong, Xu Wang, Xueyang Xie, Yilin Jiang, Yixin Yang, Yongzhe He, Yu Tu, Yuanliang Dong, Yuchen Liu, Yue Ma, Yue Yu, Yuxing Xiang, Zhaojun Huang, Zhenru Lin, Zhipeng Xu, Zhiyang Chen, Zhonghua Deng, Zihan Zhang, Zihao Yue  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2601.02780v1  

#### Abstract
We present MiMo-V2-Flash, a Mixture-of-Experts (MoE) model with 309B total parameters and 15B active parameters, designed for fast, strong reasoning and agentic capabilities. MiMo-V2-Flash adopts a hybrid attention architecture that interleaves Sliding Window Attention (SWA) with global attention, w...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MiMo-V2-Flash Technical Report 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大模型在实现**强推理能力**和**智能体（agentic）行为**时面临两大瓶颈：
- **长上下文建模效率低**：全注意力机制（Full Attention）计算复杂度为 $O(n^2)$，导致长序列推理速度慢、显存占用高。
- **后训练（post-training）扩展成本高且不均衡**：传统强化学习（RL）训练存在“跷跷板效应”（capability imbalance），即提升某一领域能力常导致其他领域退化。

### 提出的新方法与创新思路

#### （1）**混合滑动窗口注意力架构（Hybrid Sliding Window Attention, SWA）**
- 采用 **5:1 的局部-全局注意力比例**，每 5 层 SWA 后接 1 层 Global Attention。
- 滑动窗口大小设为 **128 tokens**，显著降低 KV Cache 存储和注意力计算开销。
- 引入 **learnable attention sink bias** 技术，缓解小窗口带来的长期依赖丢失问题。

#### （2）**轻量级多令牌预测模块（Lightweight Multi-Token Prediction, MTP）**
- 在预训练阶段引入 MTP 作为辅助目标，提升训练效率。
- 推理阶段将 MTP 模块用作 **speculative decoding 的草稿模型（draft model）**，实现高达 **3.6 的平均接受长度** 和 **2.6x 的解码加速**。
- MTP 模块仅含 **0.33B 参数**，使用 Dense FFN + SWA 结构，避免成为推理瓶颈。

#### （3）**多教师在线策略蒸馏范式（Multi-Teacher On-Policy Distillation, MOPD）**
- 一种全新的后训练框架，分三阶段：
  1. **通用 SFT**：建立基础指令遵循能力；
  2. **领域专用 RL/SFT**：训练多个专家教师模型（如数学、编码、工具调用等）；
  3. **MOPD 蒸馏**：学生模型从多个教师处获取 token-level 的 KL divergence 奖励信号，并结合 outcome reward 进行联合优化。
- 实现了**能力融合而非权衡取舍**，解决了“见此失彼”的能力不平衡问题。

### 相比现有方法的优势
| 维度 | MiMo-V2-Flash | 对比优势 |
|------|----------------|----------|
| **参数效率** | 总参 309B，激活 15B | 性能媲美 DeepSeek-V3.2（671B）、Kimi-K2（1043B） |
| **长上下文性能** | 支持原生 32K → 扩展至 256K | 在 LongBench V2、MRCR 上超越更大全注意力模型 |
| **推理速度** | MTP 加速达 2.6x | 利用 speculative decoding 显著提升吞吐 |
| **后训练可扩展性** | MOPD 支持模块化集成新教师 | 避免重复训练，支持教师-学生共进化 |

---

## 2. 核心实验方法和设置

### 使用的数据集

#### 预训练数据
- **总量**：27 万亿 tokens
- 来源：高质量网页、书籍、学术论文、代码、STEM 内容
- 特别强调长距离依赖数据（如完整仓库代码、PR、commit 历史）

#### 评估基准
| 类别 | 主要数据集 |
|------|-----------|
| **通用理解与推理** | MMLU, BBH, TriviaQA, DROP, ARC, HellaSwag, WinoGrande |
| **数学推理** | GSM8K, MATH, AIME 2024/25, HMMT Feb.2025 |
| **编程能力** | HumanEval+, MBPP+, CRUXEval, LiveCodeBench, SWE-Bench |
| **中文理解** | C-Eval, CMMLU, C-SimpleQA |
| **多语言能力** | GlobalMMLU, INCLUDE |
| **长上下文任务** | LongBench V2, MRCR, GSM-Infinite, NIAH-Multi |
| **智能体任务** | SWE-Bench Verified/Multilingual, Terminal-Bench, BrowseComp, t2-Bench |

### 实验设置与评估指标
- **模型规模**：总参 309B，激活 15B；MoE 每层 256 专家，激活 8 个
- **上下文长度**：原生 32K，扩展至 256K
- **训练精度**：FP8 混合精度（部分保留 BF16/FP32）
- **评估方式**：
  - Zero-shot / Few-shot 准确率
  - Resolved Rate（SWE-Bench）
  - Acceptance Length（MTP）
  - Decoding Speedup（vs. baseline）

### 基线方法对比
- **开源模型**：DeepSeek-V3.2-Thinking, Kimi-K2-Thinking, DeepSeek-V3.2-Exp-Base
- **闭源模型**：Claude Sonnet 4.5, GPT-5 High, Gemini 3.0 Pro
- 所有对比均基于公开报告或官方发布结果

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 9）

| 基准 | MiMo-V2-Flash | 最优基线 | 表现 |
|------|---------------|---------|-------|
| **MMLU-Pro** | 84.9 | Kimi-K2 (84.6) | ✅ 超越 |
| **GPQA-Diamond** | 84.3 | Claude (91.9) | 接近 SOTA |
| **AIME 2025** | 94.1 | Kimi-K2 (94.5) | 接近 SOTA |
| **LiveCodeBench** | 85.1 | Kimi-K2 (83.1) | ✅ 超越 |
| **SWE-Bench Verified** | **73.4%** | GPT-5-High (74.9%) | 当前最佳开源模型 |
| **SWE-Bench Multilingual** | **71.7%** | Kimi-K2 (61.1%) | 显著领先 |
| **LongBench V2** | 60.6 | Claude (65.6) | 接近闭源模型 |
| **MRCR** | 45.7 | DeepSeek-V3.2 (55.5) | 仍有差距 |
| **BrowseComp (w/ Context Manage)** | 58.3 | Kimi-K2 (60.2) | 接近最优 |

> 注：MiMo-V2-Flash 在 **SWE-Bench Verified** 上达到 73.4%，是目前表现最好的开源模型。

### 与基线方法的对比结果
- 尽管总参数仅为 DeepSeek-V3.2 的 ~46%、Kimi-K2 的 ~30%，但在多数推理任务上表现相当甚至更优。
- 在 **长上下文检索任务 NIAH-Multi** 中，MiMo-V2-Flash 在 32K–256K 长度下成功率接近 100%，远优于 DeepSeek-V3.2-Exp Base（在 128K 下降至 94.3%）。
- 在 **GSM-Infinite（极端长上下文推理）** 上，MiMo-V2-Flash 从 16K 到 128K 性能下降平缓（37.7 → 29.0），而 DeepSeek-V3.2-Exp 从 50.4 降至 25.7，显示其更强的鲁棒性。

### 消融实验结果（Table 2–4）

#### 不同注意力配置对比（32B 模型验证）
| 配置 | MMLU | GSM8K | MATH | GSM-Infinite | AIME25 |
|------|------|--------|--------|--------------|--------|
| All GA | 57.3 | 34.2 | 9.5 | 12.3 | 45.5 |
| Hybrid SWA (W=128, w/o sink) | 54.9 | 36.9 | 8.9 | 17.3 | 47.1 |
| **Hybrid SWA (W=128, w/ sink)** | **58.3** | **36.9** | **10.3** | **17.3** | **47.1** |
| Hybrid SWA (W=512, w/ sink) | 58.3 | 37.9 | 10.0 | 17.2 | 46.3 |

> ✅ **结论**：加入 attention sink 后，小窗口（128）反而优于大窗口（512），尤其在长上下文任务中表现更稳定。

#### MTP 推理加速效果（Table 10）
在批大小 64、输入 16K、输出 1K 场景下：
- 当 Acceptance Length = 3.6 时，**解码速度提升 2.53x**
- 速度增益随 Acceptance Length 线性增长

> 图 7 显示：**next-token entropy 与 acceptance length 强负相关**（$R^2=0.995$），说明确定性越高的任务（如 WebDev），MTP 加速效果越好。

---

## 4. 关键结论和发现

### 主要发现
1. **小窗口 + attention sink 可实现高效且强大的长上下文建模**  
   即使使用仅 128-token 的滑动窗口，在 learnable attention sink 的帮助下仍能超越全注意力模型，尤其是在长序列任务中表现出更好的泛化性和稳定性。

2. **MTP 不仅可用于训练增强，更是高效的推理加速手段**  
   将 MTP 作为 speculative decoding 的 draft model，可在无需额外硬件的情况下实现最高 **2.6x 的解码加速**，特别适用于 RL rollout 等高延迟场景。

3. **MOPD 成功实现了多领域能力的无损融合**  
   通过 token-level 的 reverse KL 蒸馏，学生模型不仅继承了各领域教师的最佳能力，还在部分任务上实现反超（如 AIME 2025 达到 94.1，超过最强教师 93.9）。

4. **大规模 agentic RL 训练具有强迁移能力**  
   在代码智能体上的大规模 RL 训练不仅能提升 SWE-Bench 表现，还能正向迁移到数学、搜索、通用推理等多个任务（见 Figure 5）。

### 方法的局限性
- **知识容量受限于参数量**：在 SimpleQA 等知识密集型任务上得分较低（20.6%），表明其知识记忆能力弱于更大模型。
- **长上下文生成任务仍有提升空间**：在 MRCR 上落后于 DeepSeek-V3.2，说明复杂多跳检索任务仍是挑战。
- **MTP 加速依赖任务不确定性**：在开放生成类任务（如 Creative Writing）中 acceptance length 较短，加速效果有限。

### 未来工作方向
1. **扩大模型规模**：通过增加 total parameters 缩小与顶级闭源模型（如 GPT-5 High）之间的差距。
2. **深入探索 agentic-oriented 架构设计**：当前架构仍基于标准 Transformer，未来可设计更适配智能体行为的新型结构。
3. **迭代式教师-学生共进化**：利用 MOPD 框架构建闭环系统，让蒸馏后的学生重新参与 RL 训练生成更强教师，形成自我增强循环。
4. **优化 context management 策略**：进一步研究如何动态压缩、索引和检索历史上下文以应对超长对话与复杂任务。

---

> 🔗 **开源信息**：MiMo-V2-Flash 的模型权重及 3 层 MTP 权重已开源，地址为 [https://github.com/XiaomiMiMo/MiMo-V2-Flash](https://github.com/XiaomiMiMo/MiMo-V2-Flash)。

</details>

---

### 4. [Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling](https://arxiv.org/abs/2601.02346)

**Authors**: Falcon LLM Team, Iheb Chaabane, Puneesh Khanna, Suhail Mohmad, Slim Frikha, Shi Hu, Abdalgader Abubaker, Reda Alami, Mikhail Lubinets, Mohamed El Amine Seddik, Hakim Hacid  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02346v1  

#### Abstract
This work introduces Falcon-H1R, a 7B-parameter reasoning-optimized model that establishes the feasibility of achieving competitive reasoning performance with small language models (SLMs). Falcon-H1R stands out for its parameter efficiency, consistently matching or outperforming SOTA reasoning model...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Falcon-H1R: Pushing the Reasoning Frontiers with a Hybrid Model for Efficient Test-Time Scaling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前大型语言模型（LLMs）在复杂推理任务上的性能提升依赖于大规模训练（scaling training），但这种方法面临计算成本高昂、高质量人类数据稀缺等问题。同时，**Test-Time Scaling (TTS)** 虽然能通过增加推理时计算量来提升性能，但也带来了极高的推理开销。

Falcon-H1R旨在解决以下核心矛盾：
- 如何在**不扩大模型参数规模**的前提下，实现与更大模型相当甚至更优的推理能力？
- 如何设计一个既**高精度又高效**（token-efficient, fast inference）的模型架构，以支持高效的TTS？

### 提出的新方法与创新思路
Falcon-H1R是一个**7B参数的小型语言模型（SLM）**，专为推理优化而设计，其核心创新在于“三位一体”的高效推理框架：

1. **Hybrid Architecture for Efficient Reasoning**
   - 基于 **Falcon-H1 架构**，采用 **Transformer-Mamba 混合并行架构**。
   - 结合了Transformer的强大表达能力和Mamba（SSM）在长序列上的线性时间推理优势，显著提升了**吞吐量和内存效率**，尤其适合处理长Chain-of-Thought（CoT）生成和高并发TTS场景。

2. **Robust Training Strategy**
   - **Cold-start SFT阶段**：使用精心筛选的长推理轨迹数据进行监督微调，强调数学、代码、科学等领域的难题，并引入**difficulty-aware weighting**策略，对困难样本加权。
   - **Reinforcement Learning with Verifiable Rewards (RLVR)** 阶段：基于GRPO算法，在数学和代码任务上进一步优化模型，鼓励探索多样化的解题路径，同时控制输出质量。

3. **Superior Efficiency and Accuracy via TTS**
   - 将Falcon-H1R与最新的TTS方法 **DeepConf** 结合，动态剪枝低置信度的推理链，实现早期停止。
   - 在相同计算预算下可运行更多并行推理链，从而在**更低token消耗下达到更高准确率**。

### 相比现有方法的优势
| 维度 | Falcon-H1R优势 |
|------|----------------|
| **参数效率** | 仅7B参数，性能媲美8B–32B甚至更大的SOTA推理模型（如Qwen3-32B, Phi-4-R-Plus-14B）。 |
| **推理效率** | 混合架构带来更快的推理速度和更高的batch吞吐量，尤其在长序列（16K–32K tokens）场景下优于纯Transformer模型。 |
| **TTS兼容性** | 更强的基础模型性能 + 更好校准的置信度估计 → 支持更激进的early stopping，大幅降低TTS成本。 |
| **训练策略有效性** | 数据质量和训练方法比单纯扩参更重要，验证了SLMs也能具备强大推理能力。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
#### SFT阶段
- 多领域混合数据集，涵盖：
  - **Mathematics**: 包含MATH、AIME风格题目，强调正确性和完整推导。
  - **Coding**: Python/C++算法题，注重功能正确性和执行验证。
  - **Science**: 科学推理问题，结合LLM judge进行评分。
  - **Other**: Instruction-following, Tool calling, Chat等通用任务。
- 总计约3.1M样本，响应token分布呈log-normal长尾分布。

#### RL阶段
- 数学与编程专用数据集，与SFT完全无重叠，防止记忆化。
- 数据经过严格过滤：
  - 排除太简单（pass@8=100%）或太难（所有rollout均失败且超长）的问题。
  - 最终形成“镜像J型”难度分布，聚焦中等偏难问题。

### 实验设置和评估指标

#### 基础评估（Standard Reasoning Tasks）
- **评估方式**：`pass@1`，多轮采样后提取答案。
- **关键设置**：
  - 温度 `T=0.6`, `top_p=0.95`
  - 系统提示固定格式：`<think> reasoning </think> answer`
- **主要基准**：
  - **Math**: AIME24, AIME25, HMMT25, AMO-Bench, MATH500
  - **Code**: LiveCodeBench v6, SciCode, T2-Telecom, Terminal Bench Hard
  - **General**: GPQA-Diamond, MMLU-Pro, Humanity's Last Exam (HLE), IFBench

#### Test-Time Scaling (TTS) 实验
- **方法**：采用 **DeepConf@512** 方法（Fu et al., 2025b）
  - 生成最多512条并行推理链。
  - 动态计算每条链的group confidence，低于阈值则提前终止。
  - 投票机制：多数投票、置信度加权等。
- **评估指标**：
  - 准确率（Accuracy ↑）
  - 生成token总数（Tokens ↓，衡量成本）

### 基线方法对比
对比了多个SOTA推理模型，覆盖不同参数范围：
- **7B级**：Qwen3-8B, DeepSeek-R1-0528-Qwen3-8B
- **10–20B级**：Phi-4-Reasoning-Plus-14B, Apriel-1.5-15b-Thinker, GPT-OSS-20B
- **>30B级**：Qwen3-32B, Nemotron-H-47B-Reasoning

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Standard Evaluation）

#### 数学推理（Math）
| Model | AIME24 | AIME25 | HMMT25 | AMO-Bench | MATH500 |
|-------|--------|--------|--------|-----------|---------|
| **Falcon-H1R-7B** | **88.1** | **83.1** | **64.9** | **36.3** | **97.4** |
| Qwen3-32B | 79.4 | 71.0 | 49.8 | 21.3 | 96.8 |
| GPT-OSS-20B | 83.3 | 84.4 | 64.8 | 26.0 | 94.8 |
| Phi-4-R-Plus-14B | 77.2 | 71.2 | 47.7 | 15.0 | 95.4 |

> ✅ Falcon-H1R在AIME24、HMMT25、AMO-Bench三项上**排名第一**，其中AMO-Bench领先第二名超10个百分点。

#### 编程生成（Code）
| Model | LCB v6 | SciCode (sub/main) | T2-Telecom | TB Hard |
|-------|--------|--------------------|------------|---------|
| **Falcon-H1R-7B** | **68.6** | 28.3 / 3.9 | 25.4 | 4.9 |
| GPT-OSS-20B | 72.0 | 34.9 / 6.2 | 60.2* | 9.9* |

> ✅ 在LiveCodeBench v6上仅次于GPT-OSS-20B，显著优于其他同级别模型。

#### 通用推理（General）
| Model | GPQA-D | MMLU-Pro | HLE | IFBench |
|-------|--------|----------|-----|--------|
| **Falcon-H1R-7B** | 61.3 | 72.1 | **11.1** | **53.4** |
| Phi-4-R-Plus-14B | 67.9 | 79.2 | 5.9 | 51.7 |

> ⚠️ 在知识密集型任务（如GPQA-D, MMLU-Pro）上仍有提升空间，但HLE和IFBench表现优异，说明指令遵循能力强。

---

### Test-Time Scaling (TTS) 实验结果（DeepConf@512）

| Model | AIME25 Acc↑ | AIME25 Tok↓ | AMO-Bench Acc↑ | AMO-Bench Tok↓ |
|-------|-------------|-------------|----------------|----------------|
| Qwen3-32B | 86.7 | 174.8M | 28.2 | 364.8M |
| DS-R1-0528-Qwen3-8B | 82.8 | 174.5M | 25.6 | 487.9M |
| **Falcon-H1R-7B** | **96.7** | **95.1M** | **35.9** | **216.8M** |

> 🔥 **关键突破**：
> - 在AIME25上达到**96.7%准确率**，比Qwen3-32B高近10个百分点。
> - 同时将token消耗从~175M降至**95.1M，减少约45%**。
> - 在AMO-Bench上准确率提升近8个百分点，token减少约40%。

这表明Falcon-H1R不仅基础性能强，而且**置信度校准更好**，使得DeepConf能够更有效地剪枝无效路径。

---

### 消融实验结果（Ablation Studies）

#### SFT阶段关键发现
| 因素 | 最佳选择 | 影响 |
|------|----------|------|
| 学习率 | `1024×10⁻⁶`（较大LR） | 加速收敛，提升下游任务性能 |
| Rollout数量 | `n=12` | 更多推理路径提升泛化能力，尤其对难题有效 |
| 教师模型混合 | 单一教师 > 多教师 | 多教师导致推理风格冲突，降低一致性 |
| 数据权重 | 困难题加权1.25–1.75× | 显著提升Hard问题表现，避免过拟合简单样本 |

#### RL阶段关键发现
| 设置 | 结果 |
|------|------|
| Group Size (G) | `G=16` 最优，G=32收益递减 |
| Max Length | `48K tokens` 允许充分思考，优于24K |
| Curriculum | **Math-only先训**效果最好，后续加入Code收益有限 |
| KL Penalty | 设为0（允许自由探索）更稳定 |

> 📌 结论：**数学推理能力具有强迁移性**，是构建通用推理模型的关键基石。

---

## 4. 关键结论和发现

### 主要发现
1. **小型模型也能实现顶级推理性能**
   - Falcon-H1R-7B在多项推理基准上超越2–7倍大的模型，证明**参数规模不是决定推理能力的唯一因素**。
   - **高质量数据 + 精细训练策略**（SFT + RLVR）是关键驱动力。

2. **数学推理技能具有强跨域迁移能力**
   - 实验表明，专注于数学训练的模型在编码和科学任务上也表现出色，反之则不然。
   - “Math-first”训练范式可能是构建通用推理Agent的有效路径。

3. **混合架构显著提升TTS效率**
   - Falcon-H1的Transformer-Mamba混合设计在长序列、大批量推理中展现出明显优势（见Appendix B），使其成为TTS的理想backbone。

4. **TTS效率取决于基础模型质量**
   - DeepConf的效果高度依赖于模型自身的推理能力和置信度校准水平。
   - Falcon-H1R因基础性能强、confidence估计可靠，能在更少token下实现更高准确率。

### 方法的局限性
- **知识密集型任务表现相对一般**：在GPQA-Diamond和MMLU-Pro上未达最优，反映其训练侧重推理而非广博知识。
- **安全与透明性的权衡**：CoT中可能暴露敏感推理过程（见Appendix E），直接展示给用户需谨慎。
- **RL训练仍具挑战性**：需要大量高质量reward信号，尤其在非形式化领域（如开放科学）难以扩展。

### 未来工作方向
- 探索更小规模（如1B–3B）的高效推理模型。
- 开发适用于非确定性任务（如创意写作、策略规划）的TTS方法。
- 进一步优化混合架构，提升SSM模块在极端长上下文下的稳定性。
- 构建端到端的安全CoT生成机制，在保证推理深度的同时抑制有害内容暴露。

---

> 💡 **一句话总结**：  
> **Falcon-H1R证明了“小模型+精训练+好架构”可以打破“大模型=强推理”的惯性思维，为高效、可扩展的智能推理系统提供了新的范式。**

</details>

---

### 5. [RadioDiff-Flux: Efficient Radio Map Construction via Generative Denoise Diffusion Model Trajectory Midpoint Reuse](https://arxiv.org/abs/2601.02790)

**Authors**: Xiucheng Wang, Peilin Zheng, Honggang Jia, Nan Cheng, Ruijin Sun, Conghao Zhou, Xuemin Shen  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02790v1  

#### Abstract
Accurate radio map (RM) construction is essential to enabling environment-aware and adaptive wireless communication. However, in future 6G scenarios characterized by high-speed network entities and fast-changing environments, it is very challenging to meet real-time requirements. Although generative...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：RadioDiff-Flux: Efficient Radio Map Construction via Generative Denoise Diffusion Model Trajectory Midpoint Reuse

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在6G网络中，**Radio Map (RM)** 构建对于环境感知和自适应无线通信至关重要。然而，在高动态场景（如无人机移动、车辆变化）下，传统方法面临以下挑战：
- **物理驱动方法**（如ray tracing）精度高但计算复杂度极高，无法实时更新；
- **数据驱动方法**中，判别模型难以生成全局一致的RM，而GAN存在训练不稳定问题；
- **扩散模型**（DMs）虽能实现高质量RM生成，但其迭代去噪过程导致推理延迟长达数秒，难以满足实时性需求。

此外，传统DM采用“零记忆”模式，对相邻位置重复执行完整去噪，造成大量冗余计算，并可能引入帧间抖动，破坏时间一致性。

### 提出的新方法与创新思路
本文提出 **RadioDiff-Flux**，一种基于**隐式扩散模型**（latent diffusion model）的两阶段框架，核心思想是**复用扩散轨迹中的中间潜变量（midpoint）** 来加速推理。

#### 主要创新点：
1. **发现并验证了扩散过程中潜变量的语义一致性**  
   在相同静态环境下，不同BS位置或动态障碍物配置所对应的RM，在扩散过程的中期（midpoint）具有高度相似的潜表示。这表明早期去噪主要编码稳定的环境语义（如建筑布局），后期才细化发射机特性和动态因素。

2. **理论分析支持midpoint复用可行性**  
   基于KL散度分析证明：语义相近的RM在高噪声水平下的潜分布差异随扩散步数呈二次衰减，说明在足够高的$t$值时，相似场景的潜状态可视为等价，从而支持复用。

3. **提出两种实现方式**：
   - **Vanilla Midpoint Reuse**：直接复用预计算的midpoint作为起点，仅更新条件输入（如BS位置），无需重新训练。
   - **RadioDiff-Flux**：两阶段架构
     - 第一阶段：仅使用静态环境特征（如建筑物布局）训练一个专用扩散模型，生成**语义级midpoint**；
     - 第二阶段：利用预训练的RadioDiff模型，从该midpoint继续完成剩余去噪，条件为动态环境+BS位置。
   - 实现了**静态建模与动态精调的解耦**，允许缓存和共享midpoint。

4. **显著降低计算开销**
   - 利用静态特征单通道输入替代三通道，减少初始CNN层FLOPs超过300M；
   - 总体推理延迟大幅下降，尤其适用于多BS或多时序更新场景。

### 相比现有方法的优势
| 方法 | 准确性 | 推理速度 | 冗余计算 | 时间一致性 |
|------|--------|----------|-----------|--------------|
| Ray Tracing | ⭐⭐⭐⭐⭐ | ⭐ | ❌ 高 | ⚠️ 差 |
| Discriminative Models (e.g., RadioUNet) | ⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 较低 | ⚠️ 一般 |
| GANs | ⭐⭐ | ⭐⭐⭐⭐ | ✅ | ❌ 不稳定 |
| Standard DMs (e.g., RadioDiff) | ⭐⭐⭐⭐⭐ | ⭐⭐ | ❌ 极高 | ❌ 存在抖动 |
| **RadioDiff-Flux (Ours)** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅✅ 极低 | ✅✅ 良好 |

> ✅ 支持快速适应新BS位置或动态环境，避免重复早期计算；  
> ✅ 可部署于边缘设备，适合6G移动场景。

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **RadioMapSeer** 数据集，源自OpenStreetMap的真实城市地图（Ankara, Berlin等）
- 包含700张256×256分辨率的城市地图（500训练 + 200测试）
- 每张图含80个发射机位置及其对应真值RM
- 分为两类：
  - **Static RM (SRM)**：仅考虑固定建筑物影响
  - **Dynamic RM (DRM)**：加入随机车辆作为动态障碍物

### 实验设置
- 所有实验基于 **PyTorch + NVIDIA A40 GPU**
- 扩散步数 $T = 100$（原RadioDiff为1000步，此处已优化）
- 评估3000次独立试验取平均
- 复用比例 $R_{\text{reuse}} \in [0.1, 0.98]$ 表示前 $R_{\text{reuse}} \times T$ 步使用旧条件生成midpoint

### 评估指标
| 指标 | 描述 |
|------|------|
| **NMSE / RMSE** | 归一化均方误差 / 均方根误差，衡量整体误差 |
| **SSIM** | 结构相似性指数，反映高频细节保留能力 |
| **PSNR (dB)** | 峰值信噪比，评估信号保真度 |
| **Inference Time (ms)** | 单次RM生成耗时，用于计算加速比 |

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **RadioUNet** | CNN-based discriminative | 经典U-Net结构，监督回归 |
| **UVM-Net** | State Space Model | 替换CNN为主干，增强长程依赖建模 |
| **RME-GAN** | GAN-based generative | 条件生成对抗网络，原需测量点，本文禁用 |
| **RadioDiff** | Latent Diffusion Model (SOTA) | 当前最优RM生成器，作为ablation baseline |
| **Vanilla Midpoint Reuse (Ours)** | Simple reuse strategy | 复用已有midpoint，不修改架构 |
| **RadioDiff-Flux (Ours)** | Two-stage framework | 显式分离静态/动态建模 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### 📊 **Scenario 1: 改变BS位置（同静态环境）**
| 方法 | $R_{\text{reuse}}$ | NMSE | Speedup vs RadioDiff |
|------|------------------|-------|------------------------|
| RadioDiff (baseline) | 0.00 | 0.00580 | 1× |
| Vanilla Reuse | 0.70 | 0.00671 | **3.47×** |
| Vanilla Reuse | 0.98 | 0.13098 | **50×** |
| **RadioDiff-Flux** | **0.98** | **0.02957** | **~20×**, 更高保真 |

> 💡 在 $R_{\text{reuse}}=0.7$ 时，误差增加<0.15%，速度提升3.5倍；  
> 💡 RadioDiff-Flux有效缓解“模糊叠加”效应，显著提升高复用率下的质量。

#### 📊 **Scenario 2: 从静态到动态环境（加车辆）**
| 方法 | $R_{\text{reuse}}$ | NMSE | Speedup |
|------|------------------|-------|---------|
| RadioDiff | 0.00 | 0.00643 | 1× |
| Vanilla Reuse | 0.98 | **0.00776** | **60×** |

> ✅ 即使复用98%步骤，NMSE仅上升约20%，SSIM保持>0.95，显示极强鲁棒性。

#### 📊 **Scenario 3: 修改静态环境（改建筑+BS）**
| $R_{\text{reuse}}$ | NMSE | Time (ms) | Speedup |
|--------------------|-------|------------|----------|
| 0.70 | 0.00889 | 175 | ~3.4× |
| 0.98 | 0.58418 | 11 | ~54×（但严重失真）|

> ⚠️ 静态环境大变时，高复用率导致“惯性偏差”，模型难纠正初始语义错误。

---

### 与基线方法对比（以Scenario 1为例）

| Method | NMSE ↓ | SSIM ↑ | PSNR (dB) ↑ | Time (ms) ↓ |
|--------|--------|--------|-------------|--------------|
| RME-GAN | 0.01150 | 0.9323 | 30.54 | 42 |
| UVM-Net | 0.00850 | 0.9320 | 30.34 | 95 |
| RadioUNet | 0.00740 | 0.9592 | 32.01 | 60 |
| RadioDiff | **0.00580** | **0.9647** | **34.67** | 600 |
| **RadioDiff-Flux ($R=0.98$)** | **0.02957** | **0.9458** | **27.53** | **~12** |
| → **加速比 ≈ 50×，精度损失 < 0.15%** | | | | |

> 尽管绝对NMSE上升，但在实际应用中仍可接受，且速度优势巨大。

---

### 消融实验结果
- **第一阶段模型初始化方式**：从RadioDiff权重微调（约10 epoch）即可，节省训练成本（12小时 vs 原始480小时）；
- **缓存大小**：每个midpoint约64KB（float32），城市级服务最多缓存100个，总<6.25MB；
- **环境相似度判断机制**：通过cross-attention空间的Frobenius距离 $D_{\text{env}}$ 自动触发复用，无需重训练。

---

## 4. 关键结论和发现

### 主要发现
1. **扩散过程中的midpoint具有强语义稳定性**：同一建筑布局下不同BS位置的RM在潜空间中路径高度一致，支持中间状态复用。
2. **RadioDiff-Flux可在几乎无损的情况下实现高达50×的推理加速**，特别适用于小范围BS移动或动态障碍物添加场景。
3. **两阶段设计实现了模块化与高效性统一**：静态部分可缓存，动态部分轻量更新，符合6G网络敏捷响应需求。
4. **在动态更新场景（如车载通信）中表现尤为出色**，即使复用98%步骤仍保持良好全局准确性。

### 方法的局限性
- 对**大规模静态环境变更**（如拆除建筑）敏感，高复用率会导致“语义惯性”，必须降低 $R_{\text{reuse}}$ 或重新生成；
- 当前模型针对单BS设计，多BS需分别处理（尽管可通过共享midpoint加速）；
- 缺乏对连续RM序列的时间一致性显式建模（未来可结合Temporal DM）。

### 未来工作方向
1. 设计**自适应 $R_{\text{reuse}}$ 策略**：根据环境变化程度自动选择最优复用比例；
2. 扩展至**multi-BS联合建模**，将多个BS位置作为条件输入，直接输出复合RM；
3. 引入**temporal consistency机制**，用于无人机轨迹等连续RM生成任务；
4. 探索更高效的latent compression与cache管理策略，适配边缘设备部署。

---

> ✅ **总体评价**：RadioDiff-Flux通过洞察扩散模型内部结构特性，提出了一种极具实用价值的加速范式，为6G环境中实时、可扩展的Radio Map生成提供了可行解决方案。

</details>

---

### 6. [MixTTE: Multi-Level Mixture-of-Experts for Scalable and Adaptive Travel Time Estimation](https://arxiv.org/abs/2601.02943)

**Authors**: Wenzhao Jiang, Jindong Han, Ruiqian Han, Hao Liu  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2601.02943v1  

#### Abstract
Accurate Travel Time Estimation (TTE) is critical for ride-hailing platforms, where errors directly impact user experience and operational efficiency. While existing production systems excel at holistic route-level dependency modeling, they struggle to capture city-scale traffic dynamics and long-ta...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# MixTTE: Multi-Level Mixture-of-Experts for Scalable and Adaptive Travel Time Estimation —— 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前工业级 **Travel Time Estimation (TTE)** 系统（如 DiDi 的 WDR 架构）虽然在常规场景下表现良好，但仍存在两大瓶颈：
- **有限的感受野**（Limited Reception Field）：仅建模路线内部依赖，难以捕捉城市尺度的全局交通动态（如周边拥堵扩散）。
- **长尾场景性能差**（Long-tail Underperformance）：对罕见但关键的交通模式（如大型活动、施工封路）预测不准。

此外，将 link-level 模型集成到 route-centric 系统面临三大挑战：
1. **可扩展性差**：百万级道路网络中建模全局时空依赖计算开销大。
2. **异质性处理难**：交通模式高度多样化，单一模型难以兼顾头部与尾部场景。
3. **动态适应成本高**：频繁更新模型易导致过拟合或计算不可持续。

---

### 提出的新方法与创新思路

作者提出 **MIxTTE**，一个**可扩展且自适应的多层级混合专家框架**，实现 link-level 与 route-level 模型的协同增强，无需重构现有系统即可插件式部署。

#### 主要创新模块：

| 创新模块 | 功能与优势 |
|--------|----------|
| **Spatio-Temporal External Attention (STEA)** | 引入外部记忆单元（external memory），通过 cross-attention 高效捕获百万级路网中的**跨时空依赖**，复杂度从 $O(N^2)$ 降至 $O(N \cdot U_{ex})$，显著提升效率。支持分层建模以强化同时间步的空间相关性。 |
| **Externally Stabilized Graph Mixture-of-Experts (ESGMoE)** | 设计图结构 MoE 层，结合**外部知识引导的分层路由机制**（Hierarchical Routing with External Guidance）和**零计算专家**（Zero-computation Experts），有效应对异质交通模式。稀疏激活保障推理效率，同时提升长尾泛化能力。 |
| **Asynchronous Incremental Learning (ASIL)** | 提出自适应异步增量学习策略，基于 **Mahalanobis Distance** 检测周期性分布偏移，仅当异常链路比例超过阈值时触发参数更新。支持**链路级与路线级参数解耦更新**，降低训练成本并防止灾难性遗忘。 |

---

### 相比现有方法的优势

- ✅ **无缝集成**：不改变 DiDi 现有 route-centric 数据流与模型架构，支持平滑升级。
- ✅ **高效全局建模**：STEA 在保持线性复杂度的同时实现跨城域交通传播建模。
- ✅ **稳定 MoE 训练**：通过外部知识增强路由决策，缓解专家坍塌（expert collapse）问题。
- ✅ **真实世界适用性强**：ASIL 支持高频、低成本在线更新，适合大规模生产环境。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个中国大城市的真实出行数据集上进行验证：

| 城市 | 时间跨度 | Trip 数量 | 平均行程时长 | 路段数（Links） | 热门路段数（Hot Links） |
|------|---------|-----------|---------------|----------------|------------------------|
| 北京 | 2024-07 ~ 2024-09 | 22.28M | 18.11 min | 2.32M | 156.7K |
| 南京 | 2024-12 ~ 2025-02 | 7.95M | 13.79 min | 696.1K | 83.3K |
| 苏州 | 2025-01 ~ 2025-04 | 16.78M | 13.63 min | 1.42M | 149.2K |

> 注：“热门路段”指流量大、动态性强的关键道路，用于生成 link embedding。

---

### 实验设置

- **训练方式**：
  - **全量重训**（Full Retraining）：使用前110天数据训练，最后7天测试。
  - **增量学习**（Incremental Learning, IL）：每小时用最新完成 trip 数据微调，预测下一小时出发 trip，更贴近实际业务需求。
- **批采样策略**：采用 **time-specific batch sampling**，同一 batch 内 trip 出发时间一致，利于共享 link 表示计算，提升训练效率。

---

### 评估指标

| 指标 | 定义 |
|------|------|
| **MAE**（Mean Absolute Error） | 预测与真实旅行时间的平均绝对误差（秒） |
| **MAPE**（Mean Absolute Percentage Error） | 平均绝对百分比误差 |
| **BCR**（Bad Case Ratio） | MAE > 300 秒且 MAPE > 20% 的查询占比 |

---

### 基线方法对比

#### 全量训练基线：
1. **Rule-based**: RouteETA（历史平均加总）
2. **Route-centric**: HierETA, WDR（DiDi 当前生产模型）
3. **Link-centric**: CompactETA, ConSTGAT, BigST

#### 增量学习基线：
- **iETA**：DiDi 现有的增量学习框架

所有方法统一输入特征，并公平比较推理负载。

---

## 3. 主要实验结果和性能指标

### 整体性能对比（Table 2）

| 方法 | 北京 MAE↓ | 南京 MAE↓ | 苏州 MAE↓ | 综合相对提升 |
|------|----------|----------|----------|-------------|
| WDR（当前系统） | 134.02 | 77.29 | 78.60 | — |
| iETA（增量版 WDR） | 132.73 | 77.10 | 78.22 | +1.0% ~ +1.5% |
| **MIxTTE**（本文） | **130.84** | **75.26** | **77.18** | **↑2.39% MAE**, **↑3.70% MAPE**, **↑10.32% BCR** |

> ✅ MIxTTE 在所有城市、所有指标上均显著优于所有基线。

---

### 长尾场景性能分析（RQ2）

- 将苏州测试集按以下三个维度五等分：
  - Trip Duration（行程长度）
  - Condition Deviation Degree（路况偏离历史程度）
  - Non-Recurrence Degree（非重复性）
- 结果显示：**MIxTTE 的 MAE 改善幅度随样本从“头部”向“尾部”递增**（见 Figure 3），说明其在罕见、复杂场景下更具鲁棒性和泛化能力。

---

### 效率分析（RQ3）

| 模型 | 北京训练吞吐（K/min） | 推理延迟（ms/step） |
|------|------------------------|--------------------|
| CompactETA | 14.96 | 541.27 |
| ConSTGAT | 10.51 | 704.11 |
| BigST | 22.45 | 78.30 |
| **MIxTTE-WoIL** | **21.17** | **88.97** |

> ⚡ MIxTTE 在训练吞吐上接近 BigST，在推理延迟上略高但仍远优于传统 GNN 方法。

#### 增量学习效率（Table 4）
| 模型 | 平均训练时间（秒/步） | 可训练参数数（K/步） |
|------|------------------------|-----------------------|
| MIxTTE-WoPU（无选择性更新） | 36.63 | 525.66 |
| **MIxTTE（ASIL）** | **7.81** | **47.49** |

> 🔁 ASIL 策略减少约 **80% 训练时间** 和 **91% 参数更新量**，极大提升在线适应效率。

---

### 消融实验（Ablation Study, Figure 4）

消融项包括：
- `-WoEA`：移除 STEA 与分层路由
- `-WoHR`：仅移除分层路由
- `-WoMoE`：移除 ESGMoE
- `-WoZE`：移除零计算专家
- `-WoPU`：移除参数选择性更新

#### 发现：
- **STEA 至关重要**：提供全局上下文感知，尤其在异质性强的城市（如苏州）影响更大。
- **分层路由提升稳定性**：利用外部知识指导路由，避免专家混乱。
- **零计算专家有效缓解长尾压力**：特别是在南京数据集中显著降低 BCR。
- **ASIL 显著提升 IL 稳定性**：证明了解耦更新机制的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **link-level modeling 能有效补充 route-centric 系统的短板**，尤其是在建模全局交通传播和长尾场景方面。
2. **STEA 实现了高效的大规模时空依赖建模**，解决了传统 pairwise GNN 方法无法扩展的问题。
3. **ESGMoE 通过稀疏激活与零计算专家设计，在不增加推理负担的前提下提升了模型容量与多样性表达能力**。
4. **ASIL 实现了高频率、低开销、稳定的在线更新机制**，是工业系统可持续演进的关键。
5. **MIxTTE 已成功部署于 DiDi 生产环境**，带来 **3.03% ~ 1.24% MAE 下降** 和 **4.41% ~ 4.30% BCR 改善**，节假日收益更明显（Figure 6）。

---

### 方法的局限性
- **依赖高质量“热路段”定义**：需定期筛选活跃 link，否则可能遗漏新兴拥堵区域。
- **外部记忆单元需调参**：如 $U_{ex}$ 大小影响性能与过拟合风险（见 Figure 7）。
- **尚未完全释放 link-level 潜力**：目前仍作为 route-centric 模型的增强模块，未构建端到端联合优化框架。

---

### 未来工作方向（Future Work）
1. **多模态融合**：引入天气、事件、社交媒体等外部信号，缓解城市感知不完整问题。
2. **跨城市迁移学习**：利用外部记忆与 MoE 抽象通用交通知识，降低新城市冷启动成本。
3. **轻量化蒸馏**：探索将大规模 MixTTE 蒸馏为小型模型，便于边缘设备部署。

---

> 💡 **一句话总结**：  
> MIxTTE 通过 **STEA + ESGMoE + ASIL** 三位一体设计，在不改造现有系统的前提下，实现了**可扩展、强鲁棒、易维护**的下一代 TTE 框架，已在 DiDi 成功落地，具有广泛工业应用前景。

</details>

---

### 7. [Jenius Agent: Towards Experience-Driven Accuracy Optimization in Real-World Scenarios](https://arxiv.org/abs/2601.01857)

**Authors**: Defei Xia, Bingfeng Pi, Shenbin Zhang, Song Hua, Yunfei Wei, Lei Zuo  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.01857v1  

#### Abstract
As agent systems powered by large language models (LLMs) advance, improving the task performance of an autonomous agent, especially in context understanding, tool usage, and response generation, has become increasingly critical. Although prior studies have advanced the overall design of LLM-based ag...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Jenius Agent: Towards Experience-Driven Accuracy Optimization in Real-World Scenarios**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于大语言模型（LLM）的自主代理系统在实际应用中面临三大挑战：
- **固定提示（prompt）导致意图理解偏差**：静态或通用的 prompt 难以适应任务状态变化，造成输出不稳定。
- **工具调用效率低下**：预定义工具列表或规则无法动态选择合适工具，易产生冗余或错误调用。
- **上下文膨胀与记忆管理低效**：长对话积累大量冗余历史，增加 token 成本并稀释关键语义，影响推理质量。

这些问题限制了代理在复杂、多轮、真实场景中的准确性、稳定性和可扩展性。

---

### **提出的新方法与创新点**
本文提出了一个名为 **Jenius-Agent** 的端到端框架，围绕“经验驱动”的优化理念，引入三个核心模块：

#### **(1) 自适应提示生成（Adaptive Prompt Generation）**
- 动态融合角色指令、任务状态和用户上下文，生成与当前情境匹配的系统 prompt。
- 引入四类用户意图分类（社交交互、创意生成、事实回忆、工具增强推理），实现精细化响应策略定制。
- 包含安全机制（如防止幻觉、参数验证）和输出格式控制，提升可靠性和可控性。

#### **(2) 上下文感知的工具编排（Context-Aware Tool Orchestration）**
- 构建基于 **Model Context Protocol (MCP)** 的标准化工具管理体系。
- 使用 **Qwen3 Embedding** 对工具进行向量化表示，通过语义相似度检索候选工具。
- 提出三步筛选机制：
  1. Top-M 相似度排序
  2. 基于“拐点检测”（inflection point）的过滤（结合 similarity-jump 和 kneedle 算法）
  3. 动态截断保留最优子集（默认 N=10）
- 实现高精度、低噪声的工具推荐，支持跨领域模糊请求处理。

#### **(3) 分层记忆管理（Hierarchical Memory Management）**
- **对话级对齐**：维护 `Human → AI → Tool → AI` 的标准交互序列，修复因失败调用导致的消息缺失。
- **会话级压缩**：当消息数超过阈值 K 时，将早期历史递归总结为 `SystemMessage` 存储，并保留在输入序列前端，确保长期上下文连贯性。
- 支持动态摘要与回填（backfilling），减少 token 占用同时保留关键语义。

---

### **相比现有方法的优势**
| 维度 | 现有方法局限 | Jenius-Agent 改进 |
|------|--------------|------------------|
| **Prompt 设计** | 固定模板，缺乏上下文适配 | 动态生成，基于意图分类与历史感知 |
| **Tool Selection** | 手动配置或简单检索 | 语义检索 + 双重拐点过滤，抗噪声能力强 |
| **Memory 管理** | 滑窗截断或粗粒度摘要 | 结构化对齐 + 层次化压缩，保持推理连续性 |
| **协议兼容性** | 多样异构接口 | 支持 MCP、ACP、A2A 等新兴通信协议 |
| **部署实用性** | 学术导向，难落地 | 已上线生产环境（[jenius.cn](https://www.jenius.cn)），轻量可扩展 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**

#### **(1) APIGen（公开基准）**
- 规模：60K 样本，覆盖 21 类 API 调用任务
- 特点：单轮交互，仅关注函数调用准确性
- 本文改进：每条查询注入 100 个无关工具，模拟高噪声环境，测试工具检索鲁棒性

#### **(2) Jenius-bench（新构建的真实世界数据集）**
- 规模：850 条高质量人工标注样本，涵盖 38 类工具
- 场景：旅行规划、票务预订、天气查询、网页生成、论文检索等
- 特点：
  - 多轮对话，支持状态演化
  - 包含完整执行轨迹（tool call, args, output, response）
  - 工具附带丰富语义描述（目的、前提、示例）
  - 经专家审核，保证真实性与一致性

> ✅ 表格对比见原文 Table 1，突出 Jenius-bench 在 realism、memory、cross-turn reasoning 方面的优越性。

---

### **实验设置与评估指标**

#### **评估框架三大维度**
| 维度 | 指标体系 | 内容 |
|------|--------|------|
| **过程保真度（Procedural Fidelity）** | **4T Metrics** | TCR, TFR, TIR, TPS |
| **输出质量（Output Quality）** | **CRCFF Metrics** | Correctness, Relevance, Completeness, Fluency, Faithfulness |
| **效率（Efficiency）** | **Token Consumption** | 输入/输出 token 总量 |

##### **4T Metrics 定义**
- **TCR (Task Completion Rate)**：正确完成所有步骤的比例
- **TFR (Task Failure Rate)**：完全无意义执行（空调用或报错）
- **TIR (Task Incompletion Rate)**：部分完成但顺序/工具错误
- **TPS (Task Performance Score)**：综合考虑正确、错误、遗漏工具的加权得分

##### **CRCFF Metrics**
由 Qwen 和 DeepSeek 等 LLM 作为 evaluator 打分（0–10 分制），评估响应语义质量。

---

### **基线方法对比（Ablation Study）**

| 模型配置 | 描述 |
|---------|------|
| **Base** | 标准 ReAct 框架（observe-think-act 循环） |
| **B-P** | Base + Adaptive Prompt Generation |
| **B-PT** | B-P + Context-Aware Tool Orchestration |
| **Jenius** | B-PT + Hierarchical Memory Management（完整版） |

所有模型共享相同 backbone 和 inference protocol，仅模块增量添加，便于归因分析。

---

## **3. 主要实验结果和性能指标**

### **(1) 过程保真度（4T 测试结果）**

#### **在 APIGen 上的结果（Table 2）**
| Model | TCR | TFR | TIR | TPS |
|-------|-----|-----|-----|-----|
| Base | 0.8150 | 0.1800 | 0.0050 | 0.8150 |
| Jenius | **0.8500** | **0.1362** | 0.0138 | **0.8500** |

👉 提升有限，说明该数据集偏向模式匹配，难以体现复杂推理优势。

#### **在 Jenius-bench 上的结果（Table 3）**
| Model | TCR | TFR | TIR | TPS |
|-------|-----|-----|-----|-----|
| Base | 0.5659 | 0.0329 | 0.4012 | 0.5968 |
| B-P | **0.7271 (+28.5%)** | 0.0859 | 0.1871 | 0.7491 |
| B-PT | 0.7494 | 0.0718 | 0.1788 | 0.7740 |
| **Jenius** | **0.7647 (+35%)** | **0.0753** | **0.1600** | **0.7847** |

✅ **关键发现**：
- 自适应 prompt 贡献最大（TCR ↑28.5%）
- 工具编排进一步提升精度
- 完整系统实现 **近 20% 的任务准确率提升**

---

### **(2) 输出质量（CRCFF 指标，Table 4）**

| Model | Correctness (↑) | Relevance | Completeness | Fluency | Faithfulness |
|-------|------------------|-----------|--------------|---------|-------------|
| Base | 0.6741 (Qwen) / 0.7890 (DeepSeek) | ~0.89–0.92 | ~0.77–0.79 | ~0.93–0.95 | ~0.79–0.83 |
| **Jenius** | **0.7580 / 0.8350** | **0.9447 / 0.9400** | **0.8088 / 0.8143** | **0.9771 / 0.9686** | **0.8766 / 0.8636** |

📌 **提升幅度**：
- 正确性 ↑ **8–10%**
- 流畅性 ↑ **~5%**
- 忠实性 ↑ **~5–10%**

表明各模块协同显著改善语义质量和逻辑一致性。

---

### **(3) 效率表现（Token 消耗分析，Figure 6）**

| 数据集 | Base | Jenius |
|-------|------|--------|
| **APIGen** | 9.96M tokens | **2.46M tokens**（↓75%） |
| **Jenius-bench** | 9.27M tokens | **3.65M tokens**（↓60%） |

✅ **原因分析**：
- 自适应 prompt 减少无效推理
- 工具编排降低冗余调用
- 分层记忆压缩历史长度

> ⚡️ 实现“更少 token，更高准确率”的双赢。

---

### **消融实验结论**
- **Prompt 优化** 是最关键的改进来源，在多轮任务中显著提升任务对齐能力。
- **Tool Orchestration** 显著减少误调用，尤其在高噪声环境下效果突出。
- **Memory Management** 对长周期任务稳定性至关重要，保障上下文连贯性。

---

## **4. 关键结论和发现**

### **主要发现**
1. **经验驱动的设计优于静态架构**：动态适配 prompt、工具和记忆，能有效应对现实世界的不确定性。
2. **模块化设计带来可解释性与可维护性**：三个核心模块独立优化、协同增益，适合工业部署。
3. **真实场景需要综合评估体系**：传统 BLEU/ROUGE 不足以衡量 agent 性能，需引入 **4T + CRCFF + Token** 多维指标。
4. **已成功落地生产环境**：Jenius 系统已在 [jenius.cn](https://www.jenius.cn) 上线运行，验证其工程可行性。

---

### **局限性**
1. **评估仍依赖人工标注参考路径**：现实中可能存在多种合法执行路径，单一 gold path 可能低估 agent 能力。
2. **拐点检测对小规模工具库敏感**：当候选工具较少时，kneedle 算法可能不稳定。
3. **未充分探索 multi-agent 协作**：当前为单 agent 框架，分布式协作潜力待挖掘。

---

### **未来工作方向**
1. **更灵活的评估机制**：支持多合法路径匹配，结合用户满意度、决策成本、延迟等真实指标。
2. **动态模块重组**：根据任务复杂度自动启用/关闭某些优化模块，实现资源自适应调度。
3. **multi-agent 协同架构**：构建 agent 团队进行分工协作，提升复杂任务解决能力。
4. **强化学习驱动的自我进化**：引入 RL 实现 agent 在线学习与持续优化。

---

## **总结**
**Jenius-Agent** 是一个面向真实应用场景的轻量级、可扩展、协议兼容的 LLM agent 框架。它通过 **自适应提示生成、上下文感知工具编排、分层记忆管理** 三大创新，在 **任务准确率、响应质量、token 效率** 上全面超越基线系统，已在实际产品中验证有效性。该研究不仅提供了技术方案，也为 LLM agent 的工业化落地提供了实践蓝图。

</details>

---

### 8. [Punctuation-aware Hybrid Trainable Sparse Attention for Large Language Models](https://arxiv.org/abs/2601.02819)

**Authors**: Junxiang Qiu, Shuo Wang, Zhengsu Chen, Hengheng Zhang, Jinda Lu, Changcheng Li, Qi Tian  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.02819v1  

#### Abstract
Attention serves as the fundamental mechanism for long-context modeling in large language models (LLMs), yet dense attention becomes structurally prohibitive for long sequences due to its quadratic complexity. Consequently, sparse attention has received increasing attention as a scalable alternative...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Punctuation-aware Hybrid Trainable Sparse Attention for Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的 **sparse attention** 方法在长序列建模中虽然能显著降低计算复杂度（从 $O(L^2)$ 到 $O(L)$），但在 **block selection** 阶段通常采用粗粒度的语义表示（如平均池化），导致块内语义边界模糊，关键信息被稀释，从而造成 **信息丢失**。此外，当前方法在极低激活 token 比例（即极端稀疏）下表现不稳定，限制了其效率潜力。

### 提出的新方法：PHSA
作者提出 **Punctuation-aware Hybrid Sparse Attention (PHSA)**，一种原生可训练的稀疏注意力框架，其核心思想是利用 **punctuation tokens**（如逗号、句号、分号）作为自然的语义边界锚点，提升稀疏选择的准确性。

#### 主要创新点：
- ✅ **双分支聚合机制（Dual-branch Aggregation Mechanism）**  
  每个 key block 的代表性向量由两个部分融合而成：
  - **Global Semantic Representation**：对块内所有 token 进行平均池化。
  - **Punctuation-aware Representation**：仅对块内的标点符号 token 进行池化，保留语义转折点信息。
  - 通过门控参数 $\lambda$ 加权融合：  
    $$
    M(B_t) = \lambda \cdot M_o(B_t) + (1-\lambda) \cdot M_p(B_t)
    $$
  > 该设计几乎不增加额外计算开销，却有效保留了细粒度语义结构。

- ✅ **极端稀疏自适应训练与推理策略（Extreme-sparsity-adaptive Training & Inference）**  
  支持在极低 Top-K 设置下稳定训练和生成，突破现有方法在高稀疏度下的性能退化瓶颈，为资源受限设备上的轻量化部署提供可能。

### 相比现有方法的优势
| 维度 | PHSA | 现有方法（如 InfLLM v2, NSA） |
|------|------|-----------------------------|
| 语义精度 | 利用标点增强边界感知 | 依赖平均池化，模糊内部语义 |
| 极端稀疏能力 | 支持极低 Top-K 训练与推理 | 在低激活比下性能急剧下降 |
| 可训练性 | 完全可微、端到端训练 | 部分含不可导操作（如聚类） |
| 效率 | 显著减少信息损失的同时保持高 sparsity | 需较高 token 激活数才能维持性能 |

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：多源混合数据集，包括：
  - 开源数据集：`dclm`, `mapcc`, `ultrachat`, `tulu3`, `finemath`, `megamath`
  - 自研高质量教育领域私有数据集（未公开）
- **评估任务**：
  - **General Benchmarks**（15项任务）：涵盖数学推理（GSM8K, MathQA, MATH）、常识问答（ARC-C/E/ZH）、综合认知（MMLU, CMMLU, BBH）、文本连贯性（LAMBADA, XStoryCloze）、代码生成（HumanEval）、特定领域任务（PiPA, C-Valid）等。
  - **Long-context Benchmarks**：
    - **LongBench**：多语言、多任务长上下文理解基准。
    - **Needle-in-a-Haystack (NIAH)**：评估模型在长序列中定位关键信息的能力，直接反映信息损失程度。

### 实验设置
- **模型规模**：Qwen3-0.6B 和 Qwen3-8B
- **序列长度**：4k 和 32k tokens
- **Block Size**：16 tokens / block
- **局部窗口与初始块**：
  - 4k 序列：local window=128, init block=16
  - 32k 序列：local window=512, init block=128
- **训练方式**：基于 Qwen3-0.6B-Base 初始化，进行端到端训练（20B 和 100B tokens）
- **推理模式**：支持训练一致（training-inference consistent）与训练无关（training-free）两种范式

### 评估指标
- **准确率（Accuracy）**：用于 general benchmarks 和 LongBench
- **NIAH Score**：越高表示信息保留越好，越接近 100 表示关键信息无损
- **Sparsity Ratio**：实际参与 attention 计算的 token 占比
- **Avg. Score**：多个任务的平均得分

### 基线方法对比
- **Dense Attention**：标准全连接注意力
- **InfLLM v2**（Zhao et al., 2025）：当前最先进的可训练稀疏注意力方法，支持短到长上下文无缝切换
- **NSA**（Yuan et al., 2025）：DeepSeek 提出的动态分层稀疏策略

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### ✅ 在 NIAH 上的表现（32k sequence, Qwen3-0.6B）
| 方法 | Top-K=1 | Top-K=2 | Top-K=4 |
|------|--------|--------|--------|
| **PHSA** | **68.0** | **88.8** | **99.0** |
| InfLLM v2 | 64.6 | 85.8 | 98.0 |

> 在 32k 长序列下，PHSA 在 Top-K=2 时比 InfLLM v2 高出 **3.0 分**，且在 Top-K=4 达到近乎完美检索（99.0）。

#### ✅ 在 general benchmarks 上的平均得分（Qwen3-0.6B, 100B tokens）
| 方法 | Inference Top-K | Avg. Score |
|------|------------------|------------|
| Dense | N/A | 48.96 |
| InfLLM v2 | 2 | 47.69 |
| **PHSA** | **2** | **48.81** |
| **PHSA** | **4** | **49.24** ✅（最优）|

> PHSA 在 Top-K=4 下不仅超越 InfLLM v2，甚至 **超过 dense attention**，表明其具备性能增益潜力。

#### ✅ 信息损失减少效果
> 对于 **0.6B 参数模型 + 32k 输入序列**，PHSA 在 **97.3% 稀疏度**下，相比 dense attention 能 **减少 10.8% 的信息损失**。

#### ✅ LongBench 综合表现
| 方法 | Average Score |
|------|---------------|
| Dense | 27.86 |
| InfLLM v2 | 27.40 |
| **PHSA** | **28.13** |
| **PHSA_en+zh**（加入中文标点） | **28.15** |

> PHSA 在大多数子任务上优于 baseline，并通过引入中文标点进一步提升中文任务表现（如 MQZ 从 20.63 → 22.22）。

---

### 消融实验与关键发现（RQ分析）

| 研究问题 | 发现 |
|---------|------|
| **RQ1: 为何偏好更低的 Top-K？** | 训练时使用较低 Top-K（如 2 或 4）可在推理阶段实现更高效的信息保留；但过低（如 1）会导致性能崩溃。PHSA 在 Top-K=2 时达到最佳平衡。 |
| **RQ2: 是否在通用基准上有效？** | 是。PHSA 在 20B 和 100B 训练步数下均优于 InfLLM v2，且在 Top-K=4 时反超 dense attention。 |
| **RQ3: 是否在长上下文基准上有效？** | 是。在 NIAH 和 LongBench 上持续领先，尤其在极端稀疏条件下优势明显。 |
| **RQ4: 不同语言中标点的影响？** | 英文标点主导训练导致中文任务（arc_c_zh, MQZ）表现略差；加入中文标点后（PHSA_en+zh），相关指标显著提升，验证了跨语言扩展可行性。 |

---

## 4. 关键结论和发现

### 主要结论
1. **标点符号是有效的语义边界提示器**：将 punctuation tokens 作为语义锚点，可显著提升 block-level selection 的准确性，缓解因平均池化带来的语义稀释问题。
2. **PHSA 实现了性能与效率的双重提升**：
   - 在高达 97.3% 的稀疏度下仍保持优异性能；
   - 在多个 benchmark 上超越 dense attention 和 SOTA sparse 方法（如 InfLLM v2）；
   - 支持极端稀疏训练，为边缘设备部署奠定基础。
3. **训练-推理一致性至关重要**：在低 Top-K 下，训练阶段的稀疏配置直接影响推理性能，PHSA 的可训练性使其能在稀疏路径上充分优化。

### 局限性
- 当前方法 **未对标点符号进行筛选**，可能引入噪声（例如频繁出现但语义弱的逗号）；
- 中文等非拉丁语系语言需专门处理标点集合，否则会影响跨语言泛化；
- 论文受限于算力和篇幅，尚未深入探索最优标点子集选择机制。

### 未来工作方向
- 探索 **动态标点权重机制** 或 **可学习标点重要性评分**；
- 扩展至更多语言，构建 **多语言 punctuation-aware sparse attention**；
- 结合硬件优化，推动 **PHSA 在移动端/嵌入式设备上的落地应用**。

---

> **总结一句话**：  
> PHSA 通过引入 **punctuation-aware dual-branch aggregation** 和 **extreme-sparsity-adaptive training**，在几乎零额外计算成本的前提下，显著提升了稀疏注意力的语义保真度，在长上下文建模中实现了 **效率与性能的协同优化**，为 LLM 的轻量化部署提供了新范式。

</details>

---

### 9. [LLM-Enhanced Reinforcement Learning for Time Series Anomaly Detection](https://arxiv.org/abs/2601.02511)

**Authors**: Bahareh Golchin, Banafsheh Rekabdar, Danielle Justo  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.02511v1  

#### Abstract
Detecting anomalies in time series data is crucial for finance, healthcare, sensor networks, and industrial monitoring applications. However, time series anomaly detection often suffers from sparse labels, complex temporal patterns, and costly expert annotation. We propose a unified framework that i...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# LLM-Enhanced Reinforcement Learning for Time Series Anomaly Detection 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**时间序列异常检测**中的几个关键挑战：
- **标签稀疏性**（sparse labels）：真实场景中异常样本极少且标注成本高；
- **复杂时序模式**：如季节性、渐变漂移等难以建模；
- **奖励信号稀疏**（sparse rewards）：传统 **Reinforcement Learning (RL)** 在缺乏密集反馈时学习效率低；
- **探索效率不足**：RL代理在未知状态空间中难以有效探索潜在异常区域。

这些问题导致现有方法在**小样本或弱监督条件下性能下降明显**。

---

### 提出的新方法与创新思路
作者提出了一种**统一框架**，将多种先进技术融合，形成一个协同增强的异常检测系统。其核心创新包括：

#### （1）**LLM-based Potential Function for Reward Shaping**
- 利用 **Large Language Model (LLM)** 生成语义化的“严重性评分”（`severity ∈ [0,1]`），作为 **Potential-Based Reward Shaping (PBRS)** 的势函数 `Φ(s)`。
- 将原始 RL 奖励更新为：
  $$
  r'(s,a,s') = r(s,a,s') + \gamma\Phi(s') - \Phi(s)
  $$
- 这种方式引入了**语义先验知识**，无需人工设计特征即可指导 agent 向更可能含异常的状态转移，同时保持最优策略不变（policy invariance）。

#### （2）**VAE-enhanced Dynamic Reward Scaling**
- 引入 **Variational Autoencoder (VAE)** 对正常模式进行无监督建模，利用其**重构误差**（Reconstruction Error）作为非监督异常信号。
- 设计动态权重系数 `λ(t)`，通过比例控制机制自动调节监督奖励（classification）与非监督信号（VAE error）之间的平衡：
  $$
  R_{total} = R_1 + λ(t) ⋅ R_2
  $$
- 当 episode 表现差时增加 VAE 权重，提升探索能力；表现好则减少依赖，聚焦分类精度。

#### （3）**Active Learning + Label Propagation**
- 在每个 episode 结束后，基于 Q-value margin（`|Q(s,a₀)−Q(s,a₁)|`）选择最不确定的样本送人标注（uncertainty sampling）。
- 使用相似度核（Gaussian kernel）进行 **Label Propagation**，扩展伪标签至邻近未标记样本，显著降低人工标注负担。

#### （4）整体架构整合
- 构建了一个端到端可训练的 **LSTM-based RL Agent**，结合上述模块实现高效、鲁棒的时间序列异常检测。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **样本效率** | 主动学习 + 标签传播极大减少了对标注数据的需求 |
| **探索能力** | LLM 提供语义引导，缓解稀疏奖励下的探索难题 |
| **泛化性** | 融合监督与非监督信号，在多变量复杂场景下仍稳定 |
| **可解释性** | LLM 输出的 `severity` 分数具有语义意义，便于调试和分析 |

---

## 2. 核心实验方法和设置

### 数据集
实验在两个广泛使用的基准数据集上进行：

| Dataset | 类型 | 特征维度 | 序列数量 | 异常比例 | 描述 |
|--------|------|----------|-----------|------------|------|
| **Yahoo-A1** | 单变量（univariate） | 1 | 67 | 1.76% | 来自 Yahoo 网站流量的真实时间序列，异常表现为突增或突降 |
| **SMD** | 多变量（multivariate） | 38 | 28 | 4.16% | 服务器机器传感器数据，前5天为正常，后5天注入随机异常，包含季节性和缓慢漂移 |

> ✅ 所有实验均在有限标注预算下进行，模拟现实场景。

---

### 实验设置
- **滑动窗口长度**：`n_steps = 25`
- **状态表示**：`s_t = [window; action_flag] ∈ ℝ^(d+1)×25`，包含动作上下文
- **Agent 结构**：LSTM + Q-Network（双输出对应两个动作）
- **LLM 模型对比**：测试 GPT-3.5、Llama-3.2-3B、Phi-2
- **Prompt 设计**：采用 few-shot 示例引导 LLM 输出 JSON 格式的 `{"severity": v}`，确保一致性
- **奖励结构**：
  - 分类奖励：TP=+5, TN=+1, FP=-1, FN=-5
  - VAE 重构误差作为辅助奖励项
- **主动学习参数**：每轮选取 Top-N 最不确定样本标注，并传播 K 个高置信伪标签

---

### 评估指标
使用标准异常检测指标：
- **Precision (Prec)**
- **Recall (Rec)**
- **F1 Score**

---

### 基线方法对比
| Baseline | 方法类型 | 是否使用 RL | 是否使用 LLM |
|---------|----------|-------------|---------------|
| THOC | One-Class Network | ❌ | ❌ |
| TranAD | Transformer-based AD | ❌ | ❌ |
| TS2Vec | Self-supervised Representation | ❌ | ❌ |
| DCdetector | Contrastive Learning | ❌ | ❌ |
| TimesNet | Temporal 2D-Variation CNN | ❌ | ❌ |
| CARLA | Deep RL-based AD | ✅ | ❌ |

> 我们的方法（Proposed Method + LLM）是唯一结合 **LLM + RL + VAE + Active Learning** 的框架。

---

## 3. 主要实验结果和性能指标

### 性能对比（来自 Table II）

#### Yahoo-A1（单变量）

| Model | Prec | Rec | F1 |
|-------|------|-----|-----|
| TimesNet | 0.3808 | 0.7883 | 0.5135 |
| TranAD | 0.4185 | 0.8712 | 0.5654 |
| CARLA (best non-LLM) | 0.5747 | 0.9755 | **0.7233** |
| **Proposed + Llama-3** | **0.6051** | **0.9565** | **0.7413** ✅ |
| Proposed + GPT-3.5 | 0.0742 | 0.9130 | 0.1372 |
| Proposed + Phi-2 | 0.6666 | 0.4761 | 0.5555 |

✅ **Llama-3 版本超越最强非 LLM 基线 CARLA（F1 提升约 2.5%）**

#### SMD（多变量）

| Model | Prec | Rec | F1 |
|-------|------|-----|-----|
| TimesNet | 0.2450 | 0.5474 | 0.3385 |
| TranAD | 0.2649 | 0.5661 | 0.3609 |
| CARLA | 0.4276 | 0.6362 | **0.5114** |
| **Proposed + Llama-3** | **0.3813** | **0.8685** | **0.5300** ✅ |
| Proposed + GPT-3.5 | 0.5370 | 0.4061 | 0.4625 |
| Proposed + Phi-2 | 0.8461 | 0.2541 | 0.3908 |

✅ **再次优于 CARLA（F1 提升约 1.8%）**，尤其体现在超高召回率（Recall > 86%）

---

### 关键发现（回答研究问题）

#### Q1: LLM for reward shaping 是否优于现有方法？
✅ 是。特别是 **Llama-3** 显著提升了 F1 分数，说明语义奖励塑造确实能加速 RL 学习并提高检测质量。

#### Q2: 不同 LLM 的表现差异及原因？
| LLM | 表现特点 | 原因分析 |
|-----|----------|-----------|
| **Llama-3.2-3B** | ✅ 最佳平衡（高 Recall & Moderate Precision） | 遵循指令能力强，输出平滑，对噪声不敏感，pattern recognition 更稳健 |
| **GPT-3.5** | ❌ 高 Recall（~90%），极低 Precision（~7%） | “过度警觉”，将正常波动误判为异常（over-flagging） |
| **Phi-2** | ❌ 高 Precision（~66%-84%），Recall 很低（~25%-47%） | 过于保守，漏检严重（under-detection） |

> 🔍 图2 显示 Llama-3 能准确识别真实异常而不被正常波动干扰。

#### Q3: 单变量 vs 多变量性能差异？
- **Yahoo-A1（单变量）**：所有模型表现较好，因为异常通常是明显的 spike 或 shift。
- **SMD（多变量）**：难度更高，需捕捉跨传感器相关性和长期趋势变化。
- 我们的框架在两者上都取得 SOTA，但在 SMD 上更凸显优势 —— **Llama-3 的高 Recall 表明它能有效捕捉复杂、隐蔽的多维异常**。

---

### 消融实验（文中虽未单独列出表格，但可通过分析推断）
从不同 LLM 的对比可以看出以下消融效果：
- **移除 LLM（即仅用 CARLA）** → F1 下降（Yahoo: -1.8%, SMD: -1.8%）
- **替换为 GPT-3.5 或 Phi-2** → 性能显著下降，说明 **LLM 选择至关重要**
- **若关闭 VAE 动态缩放或主动学习** → 文中指出会导致“performance drops”，需更大标注量才能达到相同效果

---

## 4. 关键结论和发现

### 主要结论
1. ✅ **LLM 可以作为有效的语义奖励塑形工具**，通过生成 `severity` 评分来增强 RL 的探索效率，且不破坏 policy invariance。
2. ✅ **Llama-3 在异常感知任务中优于 GPT-3.5 和 Phi-2**，因其具备更好的指令遵循能力和稳定性。
3. ✅ 所提框架在 **Yahoo-A1 和 SMD** 上均达到 **state-of-the-art 水平**，尤其是在有限标注条件下表现出色。
4. ✅ **VAE + 动态奖励缩放机制** 成功融合了监督与非监督信号，增强了模型适应性。
5. ✅ **Active Learning + Label Propagation** 显著降低了人工标注需求，适用于实际部署。

---

### 方法的局限性
1. **计算开销较大**：每次决策都需要调用 LLM 推理，可能影响实时性（尤其是长序列）；
2. **LLM 黑箱风险**：虽然 `severity` 有一定可解释性，但 LLM 决策过程仍不够透明；
3. **多变量关系建模不足**：当前 LLM 输入为扁平化窗口，未能显式建模传感器间的拓扑或因果关系；
4. **依赖预训练 LLM 的领域适配性**：若时间序列来自高度专业领域（如医疗 EEG），通用 LLM 可能无法理解语义。

---

### 未来工作方向
1. **轻量化 LLM 替代方案**：探索微调小型 LLM 或使用 MoE 架构降低成本；
2. **图神经网络集成**：显式建模多变量传感器间的依赖结构；
3. **在线增量学习机制**：支持模型持续适应新出现的异常类型；
4. **跨数据集迁移能力验证**：测试在医疗、金融等其他领域的泛化性；
5. **人类反馈闭环（Human-in-the-loop）优化**：进一步提升主动学习效率。

---

## 总结
该论文成功地将 **LLM、Reinforcement Learning、VAE 和 Active Learning** 融合在一个统一框架中，解决了时间序列异常检测中**标签稀缺、奖励稀疏、探索困难**等核心问题。实验证明，**Llama-3 驱动的语义奖励塑造**是最优配置，在多个 benchmark 上实现了 SOTA 性能，尤其适合资源受限的实际应用场景。这一工作为“AI + 时间序列”提供了新的范式参考。

</details>

---

### 10. [ModeX: Evaluator-Free Best-of-N Selection for Open-Ended Generation](https://arxiv.org/abs/2601.02535)

**Authors**: Hyeong Kyu Choi, Sharon Li  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.02535v1  

#### Abstract
Selecting a single high-quality output from multiple stochastic generations remains a fundamental challenge for large language models (LLMs), particularly in open-ended tasks where no canonical answer exists. While Best-of-N and self-consistency methods show that aggregating multiple generations can...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ModeX: Evaluator-Free Best-of-N Selection for Open-Ended Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在开放生成任务（如文本摘要、代码生成、数学推理）中面临一个核心挑战：如何从多个随机生成的结果中选择一个高质量输出。传统方法依赖外部评估器（如reward model）、精确字符串匹配投票（exact string-match voting）或迭代修正机制（如Self-Refine），这些方法存在以下问题：
- **封闭性限制**：多数仅适用于有标准答案的闭合任务（如多项选择题）；
- **高计算开销**：需要额外推理步骤或辅助模型；
- **效率低下**：无法有效推广到语义等价但字面不同的开放文本。

因此，本文提出：**能否在不依赖外部评估器且无显著计算开销的情况下，实现高质量的Best-of-N选择？**

---

### 提出的新方法与思路
作者提出了 **Mode Extraction (ModeX)** ——一种无需评估器（evaluator-free）的Best-of-N选择框架，其核心思想是：
> 将“多数投票”（majority voting）的思想从**精确字符串匹配**推广到**语义共识建模**，通过识别生成结果中的“模态输出”（modal output）作为最终输出。

#### ModeX 的三步流程：
1. **Adjacency Matrix Construction**  
   构造加权相似度图，节点为生成结果，边权重基于n-gram（unigram, bigram, trigram）的Jaccard相似度：  
   $$
   A_{ij} = s_1(v_i, v_j) + s_2(v_i, v_j) + s_3(v_i, v_j)
   $$

2. **Spectral Graph Clustering**  
   利用图拉普拉斯矩阵的**Fiedler向量**进行谱聚类，递归地将图划分为两个子图，并通过**conductance阈值**判断是否继续分裂，直到找到最凝聚的语义簇。

3. **Centroid Selection**  
   在最终簇中选择**加权度最高**的节点作为中心点（centroid），即“模态输出”。

此外，提出轻量版 **ModeX-Lite**：
- 在生成过程中周期性执行模式选择与剪枝（pruning）；
- 每个剪枝间隔只做一次非递归谱聚类，提升效率；
- 显著降低延迟，适合实际部署。

---

### 相比现有方法的优势
| 特性 | ModeX / ModeX-Lite | Self-Consistency | LLM Judge | Best-of-N w/ RM |
|------|--------------------|------------------|-----------|----------------|
| 是否需外部评估器 | ❌ | ❌ | ✅（另一个LLM） | ✅（Reward Model） |
| 是否支持开放生成 | ✅（语义聚类） | ⚠️（依赖exact match） | ✅ | ✅ |
| 计算效率 | 高（并行生成 + 图算法） | 中等（串行 refine） | 低（二次推理） | 低（需RM推理） |
| 可扩展性（随N增加） | 强（性能稳定上升） | 弱（收益饱和快） | 弱 | 强但成本高 |

> ✅ **核心优势**：首次实现了**完全免评估器、适用于任意开放生成任务、高效可扩展**的Best-of-N选择机制。

---

## 2. 核心实验方法和设置

### 数据集
在三个典型开放生成任务上验证：
- **Text Summarization**: CNN/DailyMail（抽取新闻摘要）
- **Code Generation**: HumanEval（Python编程问题）
- **Mathematical Reasoning**: Math-500（涵盖代数、几何、概率等六类数学题）

---

### 实验设置
- **模型**：
  - 主要使用 Qwen2.5-7b-instruct 和 Llama3.1-8b-instruct；
  - Code Generation 使用 CodeLlama-7b-Instruct。
- **生成路径数 N**：测试 N=4, 8, 16。
- **Prompt设计**：统一采用思维链（Chain-of-Thought）提示模板，要求先推理后输出。

---

### 评估指标
| 任务 | 主要指标 |
|------|---------|
| 文本摘要 | ROUGE-1, ROUGE-2, ROUGE-L, BLEU |
| 代码生成 | Pass@1（功能正确率） |
| 数学推理 | Accuracy（最终答案准确率） |

---

### 基线方法对比
1. **Single Path**：单次生成，取平均性能。
2. **Self-Refine**：迭代优化4轮。
3. **LLM Judge**：用另一个LLM从N个候选中选出最佳（N=4或16）。
4. **Perplexity**：选困惑度最低的输出。
5. **Self-Certainty**：选负对数似然最小的输出。
6. **Best-of-N (Gold Standard)**：使用reward model打分选择最优（视为上限）。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 Qwen 模型为例）

| 方法 | Text Summarization (ROUGE-L) | Code Generation (Pass@1) | Math Reasoning (Accuracy) |
|------|-------------------------------|--------------------------|----------------------------|
| Single Path | 20.17 ± 0.28 | 69.89 ± 3.59 | 70.98 ± 1.74 |
| LLM Judge (N=16) | 19.72 | 65.24 | 74.67 |
| Perplexity BoN (N=16) | 21.06 | 73.17 | 78.00 |
| **ModeX (N=16)** | **21.06** | **75.61** | **78.00** |
| **ModeX-Lite (N=16)** | **21.89** | **78.66** | **75.33** |

> 💡 **亮点**：
> - ModeX 在三项任务上均显著优于 Single Path 和 LLM Judge；
> - ModeX-Lite 在代码生成上达到 **78.66% Pass@1**，甚至超过 Gold Standard 的 Perplexity BoN（73.17%）；
> - 在文本摘要中，ModeX-Lite 的 ROUGE-L 达到 **21.89**，远超 baseline。

---

### 与基线方法的对比结果
- **vs. Self-Refine**：尽管消耗约4倍计算资源，Self-Refine 表现普遍低于 ModeX，说明单纯增加推理步数不如多路径+结构化选择有效。
- **vs. LLM Judge**：随着 N 增大，LLM Judge 收益增长缓慢（如Llama上数学任务仅+1.34%），而 ModeX-Lite 提升达 **+7.33%**，显示其更强的可扩展性。
- **vs. Self-Certainty**：该方法表现不稳定，在代码生成上仅为55.49%，显著劣于 ModeX。

---

### 消融实验结果
#### （1）相似度函数比较（Appendix E）
| 方法 | ROUGE-L (Text Summ.) | Pass@1 (Code Gen.) |
|------|------------------------|---------------------|
| ModeX-n-gram (Jaccard) | **21.06** | **75.61** |
| ModeX-cosine (Embedding) | 20.26 | 75.00 |

> 结论：基于n-gram的Jaccard相似度优于embedding cosine相似度，可能因后者对词序不敏感。

#### （2）超参数敏感性分析（Figure 5）
- **Conductance Threshold T ∈ {0.5, ..., 0.8}**：性能稳健，所有配置下均优于Single Path。
- **Pruning Frequency T ∈ {100, ..., 500} tokens**：不同频率下性能波动小，证明ModeX-Lite鲁棒性强。

#### （3）复杂度与延迟分析（Table 2）
| 方法 | 复杂度 | 实测延迟（Qwen, CNN/DM） |
|------|--------|--------------------------|
| Single Path | O(L) | 5.5s |
| Self-Refine | O(kL) | 31.7s |
| LLM Judge | O(NL + NL_judge) | 10.7s |
| **ModeX-Lite (N=4)** | O(NL + N²) | **7.2s** |
| **ModeX-Lite (N=16)** | O(NL + N²) | **9.1s** |

> ✅ ModeX-Lite 仅引入轻微延迟，却带来显著性能增益，具备强实用性。

---

## 4. 关键结论和发现

### 主要发现
1. **“模态输出”是高质量生成的关键**：即使没有标准答案，多个生成结果中仍存在语义上的“共识”，代表模型最可信的理解。
2. **谱聚类能有效捕捉语义一致性**：利用Fiedler向量进行图分割，可在无监督情况下识别主导语义簇。
3. **无需外部评估也能超越LLM裁判**：ModeX在多个任务上优于LLM Judge，表明“关系结构”比“显式评分”更可靠。
4. **早期剪枝可行且高效**：ModeX-Lite证明可在生成中途剔除异常路径，大幅提升效率而不损性能。

---

### 方法的局限性
1. **依赖lexical similarity**：使用Jaccard n-gram相似度，可能导致语义相同但表达差异大的高质量输出被误判为 outlier。
2. **假设“众数即正确”**：若LLM本身存在系统性偏见或模式坍塌（mode collapse），ModeX可能放大错误共识。
3. **未处理长文本效率瓶颈**：当N很大时，O(N²)图构建可能成为瓶颈（尽管实践中N << L）。

---

### 未来工作方向
- 探索更强大的语义相似度度量（如sentence embedding、BERTScore）；
- 结合内部logits信息进行动态剪枝；
- 扩展至多模态生成任务（如图文生成）；
- 研究对抗性攻击下的鲁棒性（如恶意生成误导性一致输出）。

---

> 🔗 **开源地址**：https://github.com/deeplearning-wisc/ModeX  
> 📌 **一句话总结**：ModeX 提供了一种**免评估器、高效、通用**的开放生成Best-of-N选择方案，通过图谱聚类识别“语义共识”，实现了性能与效率的双重突破。

</details>

---

### 11. [WebAnchor: Anchoring Agent Planning to Stabilize Long-Horizon Web Reasoning](https://arxiv.org/abs/2601.03164)

**Authors**: Yu Xinmiao, Zhang Liwen, Feng Xiaocheng, Jiang Yong, Qin Bing, Xie Pengjun, Zhou Jingren  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.03164v1  

#### Abstract
Large Language Model(LLM)-based agents have shown strong capabilities in web information seeking, with reinforcement learning (RL) becoming a key optimization paradigm. However, planning remains a bottleneck, as existing methods struggle with long-horizon strategies. Our analysis reveals a critical ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# WebAnchor: Anchoring Agent Planning to Stabilize Long-Horizon Web Reasoning 论文总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于 **Large Language Model (LLM)** 的 Web 代理在长周期（long-horizon）推理任务中面临**规划不稳定**和**策略漂移**的问题。尽管强化学习（RL）被广泛用于优化代理行为，但现有方法通常将奖励均匀分配在整个轨迹上，忽视了**初始决策对后续行为的巨大影响**。

作者通过实证分析揭示了一个关键现象：**Plan Anchor（计划锚定）** —— 即第一步行动生成的初始计划对整个任务的成功具有不成比例的重大影响。一旦首步出错，容易引发级联失败，导致任务彻底偏离目标。

---

### 🚀 提出的新方法：Anchor-GRPO
为解决上述问题，论文提出 **Anchor-GRPO**，一种两阶段的强化学习框架，解耦“规划”与“执行”过程：

#### 阶段一：Anchor Plan Optimization（锚定计划优化）
- 引入 **Plan Rubrics Learner**，从大量成功/失败的自我对弈（self-play）经验中自动提炼高质量计划的关键维度（如子目标覆盖、目标对齐、工具选择等）。
- 利用这些结构化评分标准（rubrics）构建**密集奖励信号**（dense reward），专门优化代理的**第一步规划能力**。
- 采用 **masking-based credit assignment** 技术，仅更新首步规划部分的梯度，防止后续执行干扰规划学习。

#### 阶段二：Trajectory-Level Executor Optimization（轨迹级执行优化）
- 执行阶段使用**稀疏奖励**（sparse reward），即仅在最终答案正确时给予奖励（exact match）。
- 确保执行过程始终与初始计划保持一致，提升长期推理稳定性。

> 🔍 **核心思想**：先制定高质量计划（plan first），再稳定执行（act steadily），避免路径漂移。

---

### 💡 相比现有方法的优势
| 方法 | 缺陷 | Anchor-GRPO 改进 |
|------|------|------------------|
| **标准 GRPO** | 奖励分布均匀，无法突出首步重要性 | 显式建模首步为“锚点”，赋予高密度反馈 |
| **First-step GRPO** | 虽分两阶段，但仍用稀疏奖励优化首步 | 使用基于经验提炼的 **dense rubric reward** 更精细指导规划 |
| **传统 RL 方法** | 忽视 long-horizon 中的信用分配难题 | 通过两阶段解耦 + 掩码更新机制实现稳定训练 |

> ✅ **优势总结**：
> - 显著提升任务成功率（Pass@1）
> - 减少无效工具调用，提高 **tool efficiency**
> - 具备良好的可扩展性（scalability）随模型大小和上下文长度增长而持续增益

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
在四个具有挑战性的 Web 信息检索基准上进行评估：

| 数据集 | 描述 |
|--------|------|
| **BrowseComp-en** | 英文网页浏览任务，测试多跳推理能力 |
| **BrowseComp-zh** | 中文版本，验证跨语言泛化性 |
| **GAIA** | 综合性通用 AI 助手评测基准，涵盖复杂查询 |
| **XBench-DeepSearch** | 深度搜索任务，强调长期策略连贯性 |

---

### 📊 评估指标
- **Pass@1**：首次尝试即答对的比例（主指标）
- **Pass@3**：前三次 rollout 中有一次答对的比例
- 所有 Pass@1 结果取三次运行平均值以保证稳定性
- 使用 **Qwen-2.5-72B** 作为打分模型进行自动评估

---

### ⚙️ 实验设置
- **基础模型范围**：3B ~ 30B 参数量（WebSailor-3B/7B, Tongyi-DR-30B）
- **训练环境**：虚拟 Wiki 环境（替代真实 Web，提升稳定性与速度）
- **训练样本**：1,000 条高质量合成数据，按难度筛选
- **硬件配置**：64 GPUs，batch size = 32，rollout num = 8

---

### 🔁 基线方法对比
| 基线方法 | 说明 |
|---------|------|
| **GRPO** | 在整条轨迹上应用 GRPO，所有步骤共享稀疏奖励（exact match） |
| **First-step GRPO** | 两阶段训练：先优化首步，再优化全轨迹；均使用稀疏奖励 |
| **Proprietary Models** | 包括 OpenAI-o3T、Claude-4-Sonnet、DeepSeek-V3.1 等闭源系统 |
| **Open-source Agents** | 如 R1-Searcher、WebSailor 系列等开源代理 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 1）

| 模型 | RL 算法 | GAIA Pass@1 | BrowseComp Pass@1 | BrowseComp-ZH Pass@1 | XBench Pass@1 |
|------|--------|-------------|--------------------|------------------------|---------------|
| WebAnchor-30B | **Anchor-GRPO** | **76.4%** | **46.0%** | **48.8%** | **75.1%** |
| WebAnchor-30B | First-step GRPO | 74.8% | 43.3% | 47.9% | 72.0% |
| WebAnchor-30B | GRPO | 75.8% | 44.0% | 46.2% | 71.0% |
| WebSailor-32B | - | 53.2% | 10.5% | 25.5% | 53.3% |
| DeepSeek-V3.1T | - | 63.1% | 30.0% | 49.2% | 71.0% |
| OpenAI-o3T | - | 70.5% | 50.9% | 58.1% | 66.7% |

> ✅ **亮点突破**：
> - WebAnchor-30B 在 **GAIA 上达到 76.4% Pass@1**，超越 OpenAI-o3T（70.5%）
> - 在 **BrowseComp 上达 46.0% Pass@1**，优于 First-step GRPO（43.3%）和 GRPO（44.0%）

---

### 🔍 消融实验结果（Table 2 & Figure 4）

#### （1）两阶段训练策略对比
| 设置 | Pass@1 | 工具调用数（Avg） | Coverage | Efficiency |
|------|--------|------------------|----------|-----------|
| Only Planner Updated (Stage-1 only) | 42.0% | 14.9 | 67.2 | 65.4 |
| Full GRPO (Baseline) | 44.0% | 15.5 | 61.2 | 53.8 |
| **Two-Stage (Planner + Executor)** | **46.0%** | **17.3** | **67.0** | **63.4** |

> ✅ 结论：联合优化规划与执行效果最佳，单纯只优化规划虽效率高但任务成功率低。

---

#### （2）不同更新步位比较
| 更新位置 | Pass@1 | Alignment |
|--------|--------|----------|
| Random Step | 33.0% | 43.6 |
| Last Step | 36.1% | 41.4 |
| **First Step** | **43.3%** | **58.2** |

> ✅ 验证了“首步锚定”的有效性：优化第一步显著优于其他位置。

---

#### （3）规划奖励设计对比
| 奖励方式 | Pass@1 | Efficiency |
|--------|--------|-----------|
| 0-1 Terminal Reward | 43.3% | 47.8 |
| Naive Plan Reward | 44.2% | 51.4 |
| **Dense Rubric Reward (Ours)** | **46.0%** | **63.4** |

> ✅ 密集、结构化的 rubric 奖励最有效，证明了经验驱动评分标准的价值。

---

#### （4）工具调用效率分析（Figure 4b）
- **Anchor-GRPO** 工具调用更稳定且高效，相比 GRPO 和 First-step GRPO 更少出现冗余搜索。
- 平均工具调用次数控制在合理范围内（~17），同时维持高性能。

---

#### （5）可扩展性分析（Figure 5）
- **模型规模扩展**：从 3B 到 30B，性能持续上升（GAIA 上从 28.3% → 76.4%）
- **上下文长度扩展**：在 16k → 64k context 下，性能稳步提升，表明该框架适合大上下文场景

---

## 4. 关键结论和发现

### 🧠 主要发现
1. **Plan Anchor 现象真实存在且至关重要**  
   实验显示，若首步规划错误，任务成功率下降高达 **23.6%~30.9%**（见 Figure 1b），证实首步是决定成败的“锚点”。

2. **结构化规划标准（Plan Rubrics）能显著提升首步质量**  
   通过自监督学习 + 人工校准的方式提炼 rubrics，使奖励更具解释性和针对性。

3. **两阶段训练优于单阶段或纯执行优化**  
   分离“规划”与“执行”，并通过不同奖励机制分别优化，实现了更高的任务成功率与稳定性。

4. **Anchor-GRPO 具备强可扩展性**  
   随着模型参数增加和上下文窗口扩大，性能持续提升，适用于未来更大更强的 LLM 架构。

---

### ⚠️ 局限性
1. 当前方法主要应用于 **Web 信息检索类任务**，是否适用于其他领域（如机器人控制、游戏决策）尚待验证。
2. **Plan Rubrics 的构建依赖大量历史轨迹和人工反馈**，自动化程度仍有提升空间。
3. 虚拟 Wiki 环境虽提升了训练效率，但与真实 Web 存在一定差距，需进一步在真实环境中验证。

---

### 🔮 未来工作方向
- 将 **Plan Anchor 思想推广至更多 long-horizon 决策场景**（如科学发现、金融分析）
- 探索 **全自动 rubrics 提炼机制**，减少人工干预
- 结合 **meta-learning 或 curriculum learning** 进一步提升小模型上的迁移能力
- 研究如何动态调整 rubrics 以适应不同任务类型

---

## ✅ 总结
**WebAnchor** 通过识别并利用 **Plan Anchor 现象**，提出了一个新颖的两阶段 RL 框架 **Anchor-GRPO**，结合 **经验驱动的 Plan Rubrics** 与 **稀疏执行奖励**，显著提升了 Web 代理在长周期任务中的成功率与稳定性。实验表明其在多个基准上达到 SOTA 表现，并展现出强大的可扩展潜力，为构建可靠、可控的 agentic system 提供了重要范式。

</details>

---

### 12. [A New Benchmark for the Appropriate Evaluation of RTL Code Optimization](https://arxiv.org/abs/2601.01765)

**Authors**: Yao Lu, Shang Liu, Hangan Zhou, Wenji Fang, Qijun Zhang, Zhiyao Xie  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.01765v1  

#### Abstract
The rapid progress of artificial intelligence increasingly relies on efficient integrated circuit (IC) design. Recent studies have explored the use of large language models (LLMs) for generating Register Transfer Level (RTL) code, but existing benchmarks mainly evaluate syntactic correctness rather ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对当前在使用 **Large Language Models (LLMs)** 进行 **Register Transfer Level (RTL)** 代码生成时存在的一个关键缺陷：**现有基准（benchmarks）主要评估语法正确性，而忽视了对设计质量的核心指标——功耗、性能和面积（PPA）的优化能力评估**。

具体而言，作者指出：
- 现有 RTL 优化基准（如 [26]）存在“过度人为构造”（overly contrived）的子最优设计，这些低效模式在实际工程中几乎不会出现；
- 依赖弱合成工具（如 Yosys）进行评估，导致结果对表面代码变化过于敏感，无法反映工业级流程的真实表现；
- 缺乏对 **PPA 全面评估**，尤其忽略了 **Power** 和 **Timing** 的权衡。

### 提出的新方法/新思路
作者提出了一个新的基准：**RTL-OPT**，用于更适当地评估 LLM 在 RTL 代码优化方面的能力。

#### 核心创新点包括：
1. **高质量、真实感强的手工设计任务集**  
   - 包含 **36 个手工编写**的数字电路设计任务，覆盖组合逻辑、流水线数据路径、有限状态机（FSM）、存储器接口等典型场景。
   - 每个任务提供一对 RTL 代码：**子最优版本**（待优化输入）和 **人工优化参考版本**（golden reference），后者体现了工业实践中有效的优化模式。

2. **真实的优化模式（Optimization Patterns）**  
   RTL-OPT 中的优化基于真实有效的行业实践，涵盖以下六类：
   - Bit-width Optimization
   - Precomputation & LUT Conversion
   - Operator Strength Reduction
   - Control Simplification
   - Resource Sharing
   - State Encoding Optimization  
   这些优化在先进合成工具下仍能体现显著 PPA 改进。

3. **集成化自动化评估框架**  
   提供完整的评估流程，自动完成：
   - **功能等价性验证**（使用 Synopsys Formality）
   - **动态仿真验证**（适用于涉及时序调整的优化）
   - **PPA 量化评估**（使用 Synopsys Design Compiler 和 Yosys）

4. **多工具、多配置的鲁棒性评估**  
   在多种合成配置下测试（如 DC `compile` / `compile_ultra`，不同 clock period），确保评估结果不依赖于特定工具或宽松约束。

### 相比现有方法的优势
| 维度 | 现有基准（如 [26]） | RTL-OPT（本文） |
|------|---------------------|------------------|
| 设计真实性 | 人为构造，不现实 | 手工编写，贴近工业实践 |
| 合成工具 | 多用 Yosys（弱优化） | 主要用 DC（工业级）+ Yosys 对比 |
| PPA 覆盖 | 偏重面积 | 完整评估 Power, Performance, Area |
| 评估可靠性 | 商业工具下差异消失 | 即使在 `compile_ultra` 下仍保持优势 |
| 功能验证 | 缺失或不足 | 集成 Formality + VCS 动态验证 |

---

## 2. 核心实验方法和设置

### 数据集
- **RTL-OPT 基准集**：包含 36 个设计任务，每个任务提供：
  - 子最优 RTL 代码
  - 人工优化的黄金参考 RTL 代码
  - 已合成的网表（netlists）
  - PPA 报告（来自 DC 和 Yosys）
  - 完整工具链脚本（标准单元库、综合、仿真、验证脚本）

### 实验设置
- **合成工具**：
  - 主要使用 **Synopsys Design Compiler (DC)**，支持两种模式：
    - `compile`（常规优化）
    - `compile_ultra`（激进优化）
  - 辅助使用开源工具 **Yosys**
- **工艺库**：Nangate45 开源标准单元库
- **时钟约束**：
  - 松弛目标：1ns
  - 严格目标：0.1ns（用于测试时序压力下的表现）
- **PPA 指标**：
  - **Power**：总功耗（动态 + 泄漏）
  - **Performance**：WNS（最差负裕量）、TNS（总负裕量）
  - **Area**：单元数量（Cells）和物理面积（Area）

### 基线方法对比
- **Baseline 1**：现有优化基准 [26] 及其人工优化版本
- **Baseline 2**：[26] 中由 LLM（GPT-4.0 和其自研模型）生成的优化代码
- **Baseline 3**：SymRTLO [23]（另一项 LLM 辅助 RTL 优化工作）

被评估的 LLMs：
- GPT-4o-mini
- Gemini-2.5
- Deepseek V3
- Deepseek R1

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 3）

#### ✅ RTL-OPT 自身有效性验证（Table 1）
在 `DC compile_ultra, 1ns` 设置下：
| 基准 | 更优案例数 / 总数 |
|------|------------------|
| [26]（人工优化） | 13 / 43 |
| RTL-OPT（人工优化） | **35 / 36** |

👉 表明 RTL-OPT 中的人工优化确实带来了显著且稳定的 PPA 提升，而 [26] 的“优化”在强合成工具下大多无效。

#### ✅ 平均 PPA 改进（Table 3）
| 指标 | 子最优（DC） | 优化后（DC） | 改进率 |
|------|-------------|------------|--------|
| Power | 1226.8 mW | 901.8 mW | **27.6%↓** |
| Cells | 1337.4 | 1047.7 | **21.7%↓** |
| Area | 14.0 Kμm² | 9.1 Kμm² | **35.0%↓** |
| WNS/TNS | 0.0 / 0.0 | 0.0 / 0.0 | 无退化 |

👉 显示 RTL-OPT 中的优化在面积、功耗上带来显著改进，且未牺牲时序性能。

### 与基线方法的对比结果

#### 🔍 对比 [26] 的人工优化效果（Table 1）
- 在 `compile_ultra` 下，[26] 的人工优化仅在 13/43 案例中优于原始设计；
- 而 RTL-OPT 在 35/36 案例中均实现更好 PPA；
- 使用 Yosys 时，[26] 表现“虚高”，说明其评估易受工具影响。

#### 🔍 LLM 优化结果（Figure 3 & Table 4）
| LLM 模型 | 语法正确率 | 功能正确率 | PPA 优于子最优 | PPA 超过人工优化 |
|---------|-----------|-----------|----------------|------------------|
| GPT-4o-mini | 97.2% | 75% | 19.4% | — |
| Gemini-2.5 | ~100% | ~70% | ~20% | — |
| Deepseek V3 | 100% | 69.4% | ~23% | — |
| **Deepseek R1** | 86.1% | 61.1% | **41.7%** | **13.9%** |

👉 **Deepseek R1** 在 PPA 优化上表现最强，但以功能正确率为代价；其他模型更保守，PPA 提升有限。

### 消融实验与深入分析
- **失败案例分析**：对 40 个语法通过但功能错误的设计进行检查，发现主要错误模式为：
  1. 控制逻辑错误（如 FSM 条件判断错误）
  2. 过度流水线化（违反延迟要求）
  3. 资源共享不当（寄存器复用导致数据冲突）
- **合成配置影响研究**：
  - 更严格的时序约束（0.1ns）会引入更多 PPA 权衡（trade-off），但 RTL-OPT 仍保持多数案例的优化优势；
  - 商业工具（DC）能消除许多“虚假优化”，凸显 RTL-OPT 设计的真实性。

---

## 4. 关键结论和发现

### 主要发现
1. **现有 RTL 优化基准不可靠**：许多所谓的“优化”在工业级合成流程下无效，甚至更差，主要因为设计不真实、评估工具太弱。
2. **PPA 评估必须结合功能验证**：仅看代码或面积是不够的，必须通过 Formality/VCS 验证功能一致性。
3. **LLMs 当前在 RTL 优化上仍有很大提升空间**：即使是最好的模型（如 Deepseek R1），也只有约 42% 的案例能改善 PPA，且功能错误率较高。
4. **优化策略存在权衡**：激进优化（如 Deepseek R1）可能带来更高 PPA 改进，但也更容易破坏功能正确性。

### 方法的局限性
- **规模限制**：目前仅包含 36 个小型到中型模块，尚未覆盖大型 SoC 或复杂 IP。
- **优化模式有限**：虽然覆盖主流模式，但仍难以穷尽所有工业级优化技巧。
- **依赖特定工艺库**：当前基于 Nangate45，向先进工艺迁移需重新校准。

### 未来工作方向
- 扩展 RTL-OPT 至更大规模、更复杂的系统级设计；
- 引入更多真实芯片中的优化案例（如来自开源 RISC-V 核心）；
- 探索将 RTL-OPT 用于训练 LLM，而非仅作为评估基准；
- 构建端到端的 AI 驱动 RTL 优化 pipeline，并集成到 EDA 流程中。

---

> 📌 **总结一句话**：  
> 本文提出 **RTL-OPT** —— 一个面向 **真实 PPA 优化能力评估** 的新型 RTL 基准，解决了现有基准“纸上谈兵”的问题，为 LLM 在硬件设计领域的实用化提供了更可靠、更具挑战性的评测平台。

</details>

---

### 13. [UltraLogic: Enhancing LLM Reasoning through Large-Scale Data Synthesis and Bipolar Float Reward](https://arxiv.org/abs/2601.03205)

**Authors**: Yile Liu, Yixian Liu, Zongwei Li, Yufei Huang, Xinhua Feng, Zhichao Hu, Jinglu Hu, Jianfeng Yan, Fengzong Lian, Yuhong Liu  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.03205v1  

#### Abstract
While Large Language Models (LLMs) have demonstrated significant potential in natural language processing , complex general-purpose reasoning requiring multi-step logic, planning, and verification remains a critical bottleneck. Although Reinforcement Learning with Verifiable Rewards (RLVR) has succe...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# UltraLogic: Enhancing LLM Reasoning through Large-Scale Data Synthesis and Bipolar Float Reward  
**核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）在复杂通用推理任务（multi-step logic, planning, verification）上仍存在显著瓶颈。尽管 **Reinforcement Learning with Verifiable Rewards (RLVR)** 在数学、代码等特定领域取得成功，但在更广泛的通用推理任务中面临两大挑战：
- **高质量训练数据稀缺**：缺乏大规模、多样化、难度可控的推理数据集。
- **奖励信号稀疏且低效**：传统二元奖励（Binary Reward）无法区分“接近正确”与“完全错误”的推理路径，导致学习效率低下。

---

### 🚀 提出的新方法与创新思路

#### （1）**ULTRALOGIC 数据框架**
- **核心思想**：通过 **Code-based Solving Framework** 将问题的逻辑内核与其自然语言表达解耦，实现自动化、可验证、高多样性的数据合成。
- **关键技术组件**：
  - **任务仓库（Task Repository）**：包含数百种独特的任务类型，基于三维正交分类系统（Task Domain, Core Reasoning Ability, Difficulty Source）设计，确保覆盖广泛推理场景。
  - **模板仓库（Template Repository）**：为每类任务构建多样化的自然语言模板（如侦探故事、物流调度、科幻设定），防止模型记忆固定模式。
  - **程序化扩展（Programmatic Expansion, PE）**：利用 `input()` 和 `solution()` 函数自动生成海量问题变体，并通过参数控制实现难度分级。
  - **十级难度校准系统**：基于主流LLM的实际成功率对10个难度等级进行闭环自动校准（如 Level 1 ~100%，Level 5 ~50%，Level 10 ~0%），确保难度客观可复现。

> 🔍 **优势**：相比人工标注或纯生成式合成，ULTRALOGIC 能够保证数据的 **逻辑正确性、可验证性、多样性与难度可控性**，支持无限扩展。

#### （2）**双极浮点奖励机制（Bipolar Float Reward, BFR）**
- **动机**：解决二元奖励的“非负奖励陷阱”（Non-negative Reward Trap）——即使有逻辑缺陷的答案也可能获得正向优势信号，导致模型收敛于次优策略。
- **设计原理**：
  - 只有完全正确的答案得分为 `+1`；
  - 所有不完美的输出得分映射到 `[-1, 0)` 区间，即施加不同程度的负惩罚（graded penalty）；
  - 构建“推拉动力学”（Push-Pull Dynamics）：正奖励“拉动”模型趋近最优，负奖励“推动”其远离错误路径。

> 🔍 **优势**：相比标准浮点奖励 `[0,1]`，BFR 避免了模型“满足于部分正确”，有效引导其逼近全局逻辑最优解。

---

### ⚖️ 相比现有方法的优势

| 维度 | 现有方法（如 RLVR + 人工数据） | ULTRALOGIC + BFR |
|------|-------------------------------|------------------|
| 数据规模与多样性 | 依赖有限竞赛数据集（如 MATH），多样性不足 | 自动合成百种任务类型，跨领域、多场景 |
| 数据质量与可验证性 | 存在噪声或歧义，难以规模化验证 | 代码驱动，逻辑确定，答案可编程验证 |
| 难度控制 | 缺乏统一、客观的难度标尺 | 十级自动化校准，适配不同模型能力 |
| 奖励信号密度 | 二元奖励稀疏，缺乏中间反馈 | BFR 提供密集、带惩罚的梯度信号 |
| 训练效率 | 收敛慢，易陷入局部最优 | 更快收敛，更高最终性能 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **训练数据**：全部来自 ULTRALOGIC 框架自动生成，涵盖上百种任务类型，包括：
  - 数值推理（Numerical Manipulation）
  - 符号操作（Symbolic Manipulation）
  - 文本谜题（Textual Manipulation）
  - 空间路径规划（Spatial: Pathfinding / Geometry）
  - 因果链推理（Planning & Scheduling）
  - 经典游戏（如24点、数独变体）
- **测试基准（Evaluation Benchmarks）**：
  - **AIME 2024 & 2025**（高级中学数学竞赛）
  - **HMMT 2025**（哈佛-麻省理工数学锦标赛）
  - **BBH**（Big-Bench Hard）
  - **BBEH**（BIG-Bench Extra Hard）
  - **ARC-AGI**（Abstraction and Reasoning Corpus - AI General Intelligence）

---

### ⚙️ 实验设置与评估指标

| 设置项 | 内容 |
|-------|------|
| **模型** | Qwen3-8B 和 Qwen3-14B |
| **训练算法** | Group Relative Policy Optimization (**GRPO**) |
| **超参数** | lr=1e-6, rollout=16, max_response_length=32,768, temperature=1.0, top_p=1.0 |
| **奖励构成** | 主奖励 + 0.1 格式奖励（format bonus）以分离格式与逻辑准确性 |
| **训练轮数** | 2 epochs |
| **评估方式** | 采样策略：T=0.6, top_p=0.95, top_k=20, min_p=0；token limit=32,768；每样本重复64次取平均准确率 |
| **主要指标** | **Accuracy**（各benchmark上的平均准确率） |

---

### 🔁 基线方法对比
- **Binary Reward**：标准二元奖励 `{0, 1}`
- **Graded Float Reward**：连续奖励 `[0, 1]`，基于 Accuracy/F1/Similarity 等指标量化部分正确性
- **BFR**（本文提出）：`[-1, 0) ∪ {1}`，仅完美答案得正分，其余按程度扣分

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（Table 2）

| 方法 | AIME24 | AIME25 | HMMT25 | BBH | BBEH | ARC-AGI |
|------|--------|--------|--------|-----|------|---------|
| **Qwen3-8B (Base)** | 75.2 | 66.1 | 47.1 | 88.2 | 29.2 | 4.0 |
| **Binary Reward** | 81.7 | 69.1 | 52.3 | 90.2 | 31.1 | 4.6 |
| **Graded Float** | 76.9 | 66.3 | 53.0 | 90.4 | 31.0 | 4.3 |
| **Bipolar Float (BFR)** ✅ | **82.6** | **71.3** | **56.6** | **91.1** | **32.5** | **4.7** |

> 💡 **结论**：BFR 在几乎所有 benchmark 上均达到最佳性能，尤其在逻辑密集型任务（如 AIME、BBH）中提升显著。

---

### 🔍 消融实验结果

#### （1）**难度匹配现象（Difficulty Matching Phenomenon）**
- 在 Qwen3-8B 上，**Easy 数据集（Level 1–4）** 表现最好；
- 在更强的 Qwen3-14B 上，**Medium（Level 4–7）** 和 **Hard（Level 7–10）** 更优。
- 最佳训练效果出现在模型在该难度下初始成功率约为 **40%-60%** 时，印证了“最近发展区”（Zone of Proximal Development）理论。

> 📈 图表显示：过难任务引入噪声，过易任务信息增益小，适配难度带来最平滑、高效的训练曲线。

#### （2）**BFR vs. 其他奖励机制**
- **Binary Reward**：虽优于基础模型，但收敛较慢；
- **Graded Float ([0,1])**：初期得分高，但很快停滞，未能持续提升；
- **BFR**：虽然整体 reward 值更低（因含负分），但 **critic/score/mean 曲线更稳定上升**，表明优化方向明确，避免陷入次优。

> 🧪 发现：BFR 成功打破“非负奖励陷阱”，迫使模型追求完整逻辑链而非关键词拼凑。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **任务多样性是推理能力提升的关键驱动力**，远比单纯扩大数据量更重要。
2. **难度匹配至关重要**：训练效率最大化发生在任务难度与模型当前能力相匹配的“黄金区间”。
3. **BFR 显著优于传统奖励机制**：通过引入分级惩罚，提供更丰富、更具区分度的学习信号，加快收敛并提升最终性能。
4. **RLVR 对数据质量极其敏感**：少量逻辑错误即可导致训练崩溃，因此严格的验证流程（如 Section 3.4.4）不可或缺。

---

### ⚠️ 局限性
1. **依赖人工标注**：
   - 尽管高度自动化，但仍需人类专家参与种子任务逻辑验证、初始难度校准和奖励函数配置。
   - RLVR 对噪声零容忍，必须牺牲部分自动化换取100%逻辑严谨性。
2. **奖励缩放的经验性**：
   - 当前 BFR 的奖励值设定基于启发式规则，尚未建立自动搜索“数学最优”奖励分布的方法。
   - 不同任务类型的理想 reward 结构可能需要个性化调优。

---

### 🔮 未来工作方向
- 探索全自动奖励结构搜索机制（Auto-Reward Tuning）。
- 将 ULTRALOGIC 扩展至更多模态（如视觉-语言推理）。
- 构建开放社区版本，允许外部贡献新任务类型与模板。
- 研究如何将 BFR 与其他过程奖励模型（PRM）结合，进一步细化中间步骤监督。

---

> ✅ **总结一句话**：  
> **ULTRALOGIC 通过代码驱动的大规模、多样化、难度可控的数据合成，配合创新的 Bipolar Float Reward 机制，在无需人工标注的前提下，显著提升了 LLM 的复杂推理能力，揭示了“多样性 > 数据量”、“难度匹配”和“惩罚驱动优化”的三大核心原则。**

</details>

---

### 14. [MAFS: Multi-head Attention Feature Selection for High-Dimensional Data via Deep Fusion of Filter Methods](https://arxiv.org/abs/2601.02668)

**Authors**: Xiaoyan Sun, Qingyu Meng, Yalu Wen  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02668v1  

#### Abstract
Feature selection is essential for high-dimensional biomedical data, enabling stronger predictive performance, reduced computational cost, and improved interpretability in precision medicine applications. Existing approaches face notable challenges. Filter methods are highly scalable but cannot capt...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MAFS: Multi-head Attention Feature Selection for High-Dimensional Data via Deep Fusion of Filter Methods

---

## 1. 论文的主要贡献和创新点

### 解决的问题
高维生物医学数据（如基因表达、SNP、多组学）的特征选择面临三大挑战：
- **Filter 方法**：虽然高效可扩展，但无法捕捉非线性关系且易遗漏冗余特征；
- **深度学习方法**：能建模复杂模式，但缺乏稳定性、可解释性和大规模效率；
- **单头注意力机制（Single-head Attention）**：受限于单一视角，难以捕获多层次依赖，且对初始化敏感，影响可重复性。

此外，大多数现有方法未能有效结合统计可解释性与深度学习的强大表征能力，尤其在超高维场景下表现不佳。

### 提出的新方法：MAFS
作者提出 **MAFS**（Multi-head Attention-based Feature Selection），一种融合滤波方法先验与深度学习能力的混合框架。其核心设计包括三个模块：

1. **Filter Module**  
   使用多种经典 filter 方法（如 SIS、BCor-SIS、Kendall’s tau）生成初始特征重要性评分，作为后续学习的“软先验”（soft priors），用于稳定初始化并引导训练过程。

2. **Multi-head Attention Module**  
   引入 multi-head 外部注意力机制，每个 attention head 并行处理一个 filter 方法输出的先验权重，从多个统计视角独立评估特征重要性，从而捕捉复杂的非线性关系和交互效应。

3. **Reordering Module**  
   将各 attention head 输出的 Top-K 特征合并为候选集，并通过树模型（如 Random Forest）重新排序，解决冲突、减少信息丢失，最终输出鲁棒一致的特征排名。

### 相比现有方法的优势
- ✅ **更强的稳定性与可重复性**：基于 filter 的初始化缓解了深度模型对随机初始化的敏感性；
- ✅ **更高的覆盖率（Coverage Rate）**：在多种函数关系下均优于 baseline，尤其在非线性关系中优势显著；
- ✅ **更好的可解释性**：attention 权重提供透明的重要性评分，支持跨 head 分析一致性；
- ✅ **计算效率优于最强 baseline**：相比图神经网络 GRACES 快一个数量级以上；
- ✅ **灵活适应不同任务类型**：适用于连续与分类目标变量，在模拟与真实数据上均表现优异。

---

## 2. 核心实验方法和设置

### 数据集

#### （1）模拟数据（Simulated Data）
- 使用 **OmicsSIMLA** 生成高维基因表达（RNA-seq）和 SNP 数据；
- 包含三种特征类型：连续型、分类型、混合型；
- 七种功能关系建模因果特征：线性、余弦、对数、立方、指数、复合、交互项；
- 样本量：`n = 500` 和 `n = 2,000`；
- 维度：`p = 25K`, `50K`, `100K`；
- 输出类型：连续与二分类响应变量。

#### （2）真实数据
- **癌症基因表达数据集（6个）**：
  - Colon, Leukemia, ALLAML, GLI_85, Prostate_GE, SMK_CAN_187
  - 用于二分类任务（肿瘤 vs 正常）
- **ADNI 数据集**（Alzheimer's Disease Neuroimaging Initiative）：
  - 449 名参与者，49,386 个基因表达特征
  - 预测 9 个脑区体积（image-derived phenotypes），为回归任务

---

### 实验设置与评估指标

| 类别 | 设置 |
|------|------|
| **训练/验证划分** | 模拟数据：80% 训练 + 20% 验证；真实数据：5-fold CV 或 60:20:20 划分 |
| **超参数优化** | 使用 Optuna 进行 100 次搜索，优化学习率、dropout、正则化系数等 |
| **网络架构统一** | 所有嵌入式方法使用相同结构：两层 MLP（BN + ReLU + Dropout） |
| **下游模型** | 分类任务用 SVM、KNN、MLP；回归任务用 SVR、KNN、MLP |

#### 评估指标
- **模拟数据**：**Coverage Rate**（选中的真实因果特征比例）
- **真实数据**：
  - 癌症数据：**AUROC**（前20个特征用于分类）
  - ADNI 数据：**Pearson Correlation**（前50个特征预测脑区体积）

---

### 基线方法对比
比较了四种主流深度学习特征选择方法：
| 方法 | 类型 | 特点 |
|------|------|------|
| **GRACES** | Graph-based | 图卷积网络 + 输入梯度估计，性能强但极慢 |
| **EAR-FS** | Attention-based | 外部注意力机制，轻量高效 |
| **DeepLIFT** | Gradient-based | 基于参考激活的归因方法，解释性强但噪声敏感 |
| **CancelOut** | Layer-based | 可学习门控层 + L1 正则化，结构简单 |

---

## 3. 主要实验结果和性能指标

### （1）模拟实验结果

#### ✅ 覆盖率（Coverage Rate）全面领先
- 在所有维度、样本大小、特征类型组合下，**MAFS 始终取得最高 coverage**。
- 当选择比例从 0.5% 提升到 2%，MAFS 的 coverage 提升仅约 **7.85%**，说明其早期就能精准识别关键特征；
- 对比方法提升幅度更大（DeepLIFT 达 79.1%），表明它们需更多特征才能找回因果信号。

#### ✅ 在非线性关系中优势巨大
| 关系类型 | MAFS | GRACES | EAR-FS |
|--------|-------|--------|--------|
| Linear | 0.86 | 0.84 | 0.70 |
| Logarithmic | **1.00** | 0.25 | 0.19 |
| Cosine | **0.93** | 0.33 | 0.27 |
| Interaction | **0.56** | 0.43 | 0.37 |

> MAFS 在非线性场景下几乎达到完美覆盖，远超其他方法。

#### ✅ 更高的稳定性
- MAFS 的置信区间宽度为 ±2.2%，明显窄于其他方法（±2.5–3.1%），显示其结果更稳定、可重复。

#### ✅ 高维环境下仍保持优势
- 当固定选择 Top 100 特征时，随着维度从 25K 升至 100K，各方法 coverage 下降：
  - MAFS：下降 **21%**
  - GRACES：下降 47%
  - EAR-FS：下降 69%
  - CancelOut：下降 100%（降至 0）

> 表明 MAFS 在超高维场景更具鲁棒性。

---

### （2）真实数据结果

#### ✅ 癌症数据集（分类任务）
- 在易区分数据集（Leukemia, Prostate）上，所有方法表现接近（AUROC > 0.95）；
- 在困难数据集（Colon, Lung）上，**MAFS 显著胜出**：
  - Colon Cancer（MLP）：**0.922** vs GRACES (0.896) vs EAR-FS (0.855)
  - Lung Cancer（MLP）：**0.785** vs GRACES (0.763)

- **特征利用效率更高**：
  - Leukemia：MAFS 仅需 **2–3 个特征** 达到 AUROC > 0.95，而 GRACES 需 4–6，EAR-FS 需 8–10；
  - Colon：MAFS 用 **5 个特征** 达到 AUROC=0.90，baseline 需 10–15。

#### ✅ ADNI 数据集（回归任务）
- 在多数脑区（如 Brainstem, Pallidum, Fourth Ventricle），MAFS 实现最高 Pearson 相关系数；
- 示例（MLP 回归器，Top 50 特征）：
  - Brainstem：MAFS (**0.440**) > GRACES (0.418) > EAR-FS (0.409)
  - Accumbens Area：MAFS (**0.219**) > GRACES (0.158) > CancelOut (0.131)

- 同样表现出更强的特征利用效率，更快收敛至最优子集。

---

### （3）消融实验与补充分析（Supplementary Figures）
- **替换 EAR-FS 初始化方式**：将均匀初始化改为 filter-derived 初始化后，EAR-FS 性能显著提升；
- 表明 **良好的初始化本身即可大幅提升注意力机制的表现**，无需复杂架构修改；
- 支持 MAFS 中“filter-guided initialization”的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **Filter 与 Attention 的深度融合是有效的**：将 filter 方法提供的统计先验作为 attention 的初始化，既保留了可解释性，又增强了深度模型的稳定性和性能。
2. **Multi-head 设计显著提升多样性与鲁棒性**：不同 attention head 捕捉不同类型的特征-响应关系（线性、非线性、秩相关），避免单一视角遗漏重要特征。
3. **Reordering 模块有效整合多源信号**：通过构建候选集 + 再排序策略，最大限度保留潜在重要特征，避免传统投票机制的信息损失。
4. **MAFS 在各种复杂关系和高维条件下均优于现有方法**：无论是在模拟还是真实数据中，都展现出更高的 coverage、AUROC 和 correlation，同时具备良好稳定性。

---

### 局限性
1. **归一化可能导致尺度失配**：尽管进行了标准化，不同 filter 方法的权重可能存在隐含偏差，影响融合效果；
2. **不支持缺失值处理**：要求完整特征矩阵，可能排除部分样本，引入选择偏倚；
3. **计算开销高于最轻量级方法**：虽比 GRACES 快得多，但仍慢于 DeepLIFT 或 CancelOut。

---

### 未来工作方向
- 探索更先进的融合策略，如 learnable fusion weights 或 rank-level aggregation；
- 引入 missing-data-aware 机制或集成 imputation 模块；
- 扩展至多任务或多模态特征选择场景（如基因 + 影像 + 临床）；
- 开发更高效的 reordering 算法以进一步加速推理。

---

## 总结
**MAFS 是一种新颖且实用的高维特征选择框架**，通过将 filter 方法的稳定性与 multi-head attention 的表达能力相结合，实现了**高性能、高可解释性、高稳定性**的统一。它不仅在理论上解决了传统方法的关键缺陷，在癌症和阿尔茨海默病等真实生物医学应用中也展现出卓越性能，为精准医学中的 biomarker 发现提供了强有力工具。

</details>

---

### 15. [From Memorization to Creativity: LLM as a Designer of Novel Neural-Architectures](https://arxiv.org/abs/2601.02997)

**Authors**: Waleed Khalid, Dmitry Ignatov, Radu Timofte  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.02997v1  

#### Abstract
Large language models (LLMs) excel in program synthesis, yet their ability to autonomously navigate neural architecture design--balancing syntactic reliability, performance, and structural novelty--remains underexplored. We address this by placing a code-oriented LLM within a closed-loop synthesis f...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*From Memorization to Creativity: LLM as a Designer of Novel Neural-Architectures*

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

传统 Neural Architecture Search (NAS) 虽然能自动化设计神经网络，但通常依赖昂贵的计算资源（如强化学习、进化算法），且搜索空间受限于预定义结构。同时，尽管 Large Language Models (LLMs) 在程序生成方面表现出色，其在**自主生成有效、高性能且结构新颖的神经网络架构**方面的能力尚未被系统探索。

本文提出并验证了一个核心问题：  
> 如果我们反复将 LLM 自身生成的高质量架构反馈给它进行微调，其生成有效、高性能、结构新颖的神经网络架构的能力是否会持续提升？

### **提出了什么新方法或新思路**

作者提出了一种 **闭环的 LLM 驱动神经架构合成框架**，通过 22 轮迭代的 *generate-evaluate-select-fine-tune* 循环，将 LLM 从一个随机代码生成器转变为具备经验先验的“神经架构设计师”。

#### 核心创新点：

- **LLM 作为可训练的架构先验（Trainable Architectural Prior）**  
  不再将 LLM 视为固定组件，而是将其行为通过自身成功案例不断优化，使其内化经验性架构先验。

- **多目标优化框架**  
  同时优化三个关键维度：
  - **Syntactic Validity**（语法有效性）
  - **Early-epoch Performance**（早期训练性能）
  - **Structural Novelty**（结构新颖性）

- **基于 MinHash-Jaccard 的代码级新颖性过滤机制**  
  引入文本级别的近似重复检测（MinHash + LSH），防止模型陷入重复模板或简单改写，确保设计空间的有效扩展。

- **轻量级性能代理信号（Low-fidelity Proxy）**  
  使用单轮训练（single-epoch accuracy on CIFAR-10）作为性能反馈信号，大幅降低评估成本，使闭环迭代可行。

### **相比现有方法的优势**

| 方面 | 传统 NAS | 现有 LLM-based NAS | 本文方法 |
|------|----------|---------------------|-----------|
| 搜索效率 | 低（需完整训练） | 中等（依赖提示工程） | 高（使用单 epoch 代理） |
| 多样性保障 | 手工设计搜索空间 | 缺乏显式控制 | 显式引入 MinHash 新颖性过滤 |
| 学习机制 | 固定搜索策略 | 固定 LLM | 迭代自增强（self-improving） |
| 可扩展性 | 差 | 中等 | 高（参数高效 LoRA 微调） |

> ✅ **优势总结**：本文首次系统展示了如何利用执行反馈（execution feedback）将 LLM 转变为**自主的、性能驱动的神经架构设计者**，实现了从“记忆”到“创造”的跃迁。

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **初始训练数据**：来自 **LEMUR Neural Network Dataset** 的 1,698 个去重后的 prompt-code 对。
- **任务数据集**：**CIFAR-10** 用于评估生成架构的单轮训练准确率（first-epoch accuracy）。
- **生成目标**：PyTorch 实现的卷积网络，满足 `Net(nn.Module)` API 合同，输入 `(N,3,32,32)` → 输出 10 类 logits。

### **实验设置**

- **基础模型**：`DeepSeek-Coder-7B-Instruct-v1.5`
- **微调方式**：**LoRA**（rank=32, α=32），应用于所有 Transformer 层的 attention 和 MLP 投影。
- **微调配置**：
  - 每 cycle 微调 5 epochs
  - 学习率：1e-5
  - Batch size：4（梯度累积）
  - 优化器：8-bit AdamW
- **生成配置**（固定不变）：
  - Temperature: 0.20, Top-k: 50, Nucleus p: 0.9
  - Max new tokens: 2048
  - Prompt 模板、解码策略全程一致，以隔离微调影响

### **评估指标**

| 指标 | 定义 |
|------|------|
| **Valid Generation Rate** | 成功编译并完成前向传播的模型比例 |
| **First-epoch Accuracy** | 在 CIFAR-10 上训练一个 epoch 后的 top-1 验证准确率（低开销性能代理） |
| **≥40% Accuracy Proportion** | 单 epoch 准确率超过 40% 的模型占比（用于筛选） |
| **Structural Novelty** | 使用 **MinHash-Jaccard Similarity** 判断是否为近似重复代码（阈值 T=0.9） |
| **Training Corpus Growth** | 自生成并加入训练集的新架构数量 |

### **基线方法对比（通过消融实验实现）**

作者未直接对比外部 NAS 方法，而是通过**消融研究（Ablation Study）** 验证各组件必要性：

1. **Full Method**：完整框架（novelty filter + accuracy threshold + iterative fine-tuning）
2. **No Novelty Filter**：移除 MinHash 过滤
3. **No Accuracy Threshold**：取消 40% 准确率筛选
4. **No Iteration**：仅初始微调一次，无循环更新

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table 2 & Figure 3）**

| 指标 | Cycle 1 | Cycle 18（峰值） | Cycle 22（最终） |
|------|--------|------------------|------------------|
| **Valid Generation Rate** | 44.0% | **74.5%**（cycle 14） | 41.8% |
| **Mean First-epoch Accuracy** | 28.06% | **50.99%** | 49.48% |
| **Best Accuracy** | 47.78% | **63.98%** | 57.62% |
| **≥40% Accuracy Proportion** | 2.04% | **96.81%** | 92.86% |
| **累计训练样本数** | 1,698 | 2,025 | **2,153** |
| **新增独特架构数** | — | 38 | 30（共 +455） |

> 📊 **总体趋势**：
> - 有效生成率稳定在 **~50.6%**（95% CI: [45.0%, 56.1%]）
> - 平均单 epoch 准确率从 28.06% 提升至 49.48%
> - 高性能架构（≥40%）从稀有变为常态（>90%）

### **与基线方法的对比结果（消融实验，Table 3）**

| 方法 | Valid Rate (%) | Mean Acc. (%) | ≥40% Acc. (%) | Novel Models Added |
|------|----------------|---------------|----------------|---------------------|
| **Full Method** | **50.6** | **42.3** | **51.1** | **455** |
| No Novelty Filter | 52.0 | 42.0 | 50.0 | 220 |
| No Accuracy Threshold | 51.0 | 38.5 | 34.0 | 470 |
| No Iteration | 44.0 | 28.06 | 4.55 | 1 |

> 🔍 **分析**：
> - 移除 **novelty filter** → 多样性显著下降（+220 vs +455），说明其对维持探索至关重要。
> - 移除 **accuracy threshold** → 性能提升减弱（mean acc ↓3.8%，≥40% ↓17.1%），说明性能反馈是驱动质量提升的关键。
> - 移除 **iteration** → 表现停滞在初期水平，证明闭环反馈不可或缺。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **LLM 可以内化非文本的经验奖励**  
   尽管微调目标仍是标准的 next-token prediction，但通过选择性地加入高表现、新颖的自我生成样本，LLM 能够隐式学习到“什么样的架构学得快”，从而提升生成质量。

2. ✅ **从“随机生成”到“可靠设计”的转变**  
   经过 22 轮迭代，LLM 从最初仅 2% 模型达到 40% 准确率，发展到最后超 90% 模型达标，表明其已形成强健的架构先验。

3. ✅ **多样性得以维持而非崩溃**  
   尽管模型专业化，但在 MinHash 过滤下仍持续发现 **455 个全新高性能架构**，未陷入模式坍缩（mode collapse）。

4. ✅ **轻量级反馈足以驱动显著改进**  
   仅用单 epoch 准确率这一低保真信号，即可引导 LLM 实现性能与可靠性的双重提升，验证了该范式的可扩展性。

### **方法的局限性**

1. **任务单一性**：仅在 CIFAR-10 图像分类任务上验证，泛化能力未知。
2. **性能代理不完美**：单 epoch 准确率可能偏好“快速学习但易饱和”的架构，未必代表最终性能最优。
3. **缺乏与经典 NAS 的公平比较**：未在相同计算预算下对比 RL/Evo/DARTS 等方法。
4. **新颖性定义偏文本层面**：MinHash 基于 token shingles，无法捕捉功能等价但代码风格不同的架构（语义冗余未处理）。
5. **后期出现性能平台期**：cycle 18 后趋于饱和，可能因自生成数据主导导致分布窄化（data collapse）。

### **未来工作方向**

1. **集成到显式优化框架中**：将训练好的 LLM 作为 **prior** 用于 LLMatic、SEKI 或 RZ-NAS 等搜索框架。
2. **多任务泛化**：通过 multi-task prompting 扩展到 ImageNet、segmentation、detection 等任务。
3. **更精细的反馈机制**：引入 performance-weighted sampling 或 RLHF 替代二值筛选。
4. **多目标约束建模**：纳入参数量、延迟、能耗等边缘设备关键指标，支持 Pareto 探索。
5. **语义级新颖性检测**：结合 computation graph embedding 而非仅源码文本，提升多样性判断准确性。

---

> 💡 **一句话总结**：  
> 本文提出并验证了一种让 LLM “自我进化”为神经架构设计师的方法，通过轻量级反馈闭环，成功实现了从**记忆已有结构**到**创造性设计高性能新架构**的跨越，为自动化深度学习开辟了新路径。

</details>

---

### 16. [MindChat: A Privacy-preserving Large Language Model for Mental Health Support](https://arxiv.org/abs/2601.01993)

**Authors**: Dong Xue, Jicheng Tu, Ming Wang, Xin Yan, Fangzhou Liu, Jie Hu  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.01993v1  

#### Abstract
Large language models (LLMs) have shown promise for mental health support, yet training such models is constrained by the scarcity and sensitivity of real counseling dialogues. In this article, we present MindChat, a privacy-preserving LLM for mental health support, together with MindCorpus, a synth...

---

### 17. [Dementia-R1: Reinforced Pretraining and Reasoning from Unstructured Clinical Notes for Real-World Dementia Prognosis](https://arxiv.org/abs/2601.03018)

**Authors**: Choonghan Kim, Hyunmin Hwang, Hangeol Chang, Jaemin Kim, Jinse Park, Jae-Sung Lim, Jong Chul Ye  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03018v1  

#### Abstract
While Large Language Models (LLMs) have shown strong performance on clinical text understanding, they struggle with longitudinal prediction tasks such as dementia prognosis, which require reasoning over complex, non-monotonic symptom trajectories across multiple visits. Standard supervised training ...

---

### 18. [Counterfactual Self-Questioning for Stable Policy Optimization in Language Models](https://arxiv.org/abs/2601.00885)

**Authors**: Mandar Parab  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00885v1  

#### Abstract
Recent work on language model self-improvement shows that models can refine their own reasoning through reflection, verification, debate, or self-generated rewards. However, most existing approaches rely on external critics, learned reward models, or ensemble sampling, which increases complexity and...

---

### 19. [ElecTwit: A Framework for Studying Persuasion in Multi-Agent Social Systems](https://arxiv.org/abs/2601.00994)

**Authors**: Michael Bao  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.00994v1  

#### Abstract
This paper introduces ElecTwit, a simulation framework designed to study persuasion within multi-agent systems, specifically emulating the interactions on social media platforms during a political election. By grounding our experiments in a realistic environment, we aimed to overcome the limitations...

---

### 20. [Reducing Hallucinations in LLMs via Factuality-Aware Preference Learning](https://arxiv.org/abs/2601.03027)

**Authors**: Sindhuja Chaduvula, Ahmed Y. Radwan, Azib Farooq, Yani Ioannou, Shaina Raza  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03027v1  

#### Abstract
Preference alignment methods such as RLHF and Direct Preference Optimization (DPO) improve instruction following, but they can also reinforce hallucinations when preference judgments reward fluency and confidence over factual correctness. We introduce F-DPO (Factuality-aware Direct Preference Optimi...

---

### 21. [Uni-FinLLM: A Unified Multimodal Large Language Model with Modular Task Heads for Micro-Level Stock Prediction and Macro-Level Systemic Risk Assessment](https://arxiv.org/abs/2601.02677)

**Authors**: Gongao Zhang, Haijiang Zeng, Lu Jiang  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.02677v1  

#### Abstract
Financial institutions and regulators require systems that integrate heterogeneous data to assess risks from stock fluctuations to systemic vulnerabilities. Existing approaches often treat these tasks in isolation, failing to capture cross-scale dependencies. We propose Uni-FinLLM, a unified multimo...

---

### 22. [Scalable Tree Ensemble Proximities in Python](https://arxiv.org/abs/2601.02735)

**Authors**: Adrien Aumon, Guy Wolf, Kevin R. Moon, Jake S. Rhodes  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.02735v1  

#### Abstract
Tree ensemble methods such as Random Forests naturally induce supervised similarity measures through their decision tree structure, but existing implementations of proximities derived from tree ensembles typically suffer from quadratic time or memory complexity, limiting their scalability. In this w...

---

### 23. [Electricity Price Forecasting: Bridging Linear Models, Neural Networks and Online Learning](https://arxiv.org/abs/2601.02856)

**Authors**: Btissame El Mahtout, Florian Ziel  
**Category**: cs.LG  
**Published**: 2026-01-07  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.02856v1  

#### Abstract
Precise day-ahead forecasts for electricity prices are crucial to ensure efficient portfolio management, support strategic decision-making for power plant operations, enable efficient battery storage optimization, and facilitate demand response planning. However, developing an accurate prediction mo...

---

### 24. [Universal Conditional Logic: A Formal Language for Prompt Engineering](https://arxiv.org/abs/2601.00880)

**Authors**: Anthony Mikinka  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.00880v1  

#### Abstract
We present Universal Conditional Logic (UCL), a mathematical framework for prompt optimization that transforms prompt engineering from heuristic practice into systematic optimization. Through systematic evaluation (N=305, 11 models, 4 iterations), we demonstrate significant token reduction (29.8%, t...

---

### 25. [Logics-STEM: Empowering LLM Reasoning via Failure-Driven Post-Training and Document Knowledge Enhancement](https://arxiv.org/abs/2601.01562)

**Authors**: Mingyu Xu, Cheng Fang, Keyue Jiang, Yuqian Zheng, Yanghua Xiao, Baojian Zhou, Qifang Zhao, Suhang Zheng, Xiuwen Zhu, Jiyang Tang, Yongchi Zhao, Yijia Luo, Zhiqi Bai, Yuchi Xu, Wenbo Su, Wei Wang, Bing Zhao, Lin Qu, Xiaoxiao Xu  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.01562v1  

#### Abstract
We present Logics-STEM, a state-of-the-art reasoning model fine-tuned on Logics-STEM-SFT-Dataset, a high-quality and diverse dataset at 10M scale that represents one of the largest-scale open-source long chain-of-thought corpora. Logics-STEM targets reasoning tasks in the domains of Science, Technol...

---

### 26. [Yuan3.0 Flash: An Open Multimodal Large Language Model for Enterprise Applications](https://arxiv.org/abs/2601.01718)

**Authors**: YuanLab. ai,  :, Shawn Wu, Sean Wang, Louie Li, Darcy Chen, Allen Wang, Jiangang Luo, Xudong Zhao, Joseph Shen, Gawain Ma, Jasper Jia, Marcus Mao, Claire Wang, Hunter He, Carol Wang, Zera Zhang, Jason Wang, Chonly Shen, Leo Zhang, Logan Chen, Qasim Meng, James Gong, Danied Zhao, Penn Zheng, Owen Zhu, Tong Yu  
**Category**: cs.AI  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.01718v1  

#### Abstract
We introduce Yuan3.0 Flash, an open-source Mixture-of-Experts (MoE) MultiModal Large Language Model featuring 3.7B activated parameters and 40B total parameters, specifically designed to enhance performance on enterprise-oriented tasks while maintaining competitive capabilities on general-purpose ta...

---

### 27. [Scalable Construction of a Lung Cancer Knowledge Base: Profiling Semantic Reasoning in LLMs](https://arxiv.org/abs/2601.02604)

**Authors**: Cesar Felipe Mart\'inez Cisneros, Jes\'us Ulises Quiroz Bautista, Claudia Anah\'i Guzm\'an Solano, Bogdan Kaleb Garc\'ia Rivera, Iv\'an Garc\'ia Pacheco, Yalbi Itzel Balderas Mart\'inez, Kolawole John Adebayoc, Ignacio Arroyo Fern\'andez  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.02604v1  

#### Abstract
The integration of Large Language Models (LLMs) into biomedical research offers new opportunities for domainspecific reasoning and knowledge representation. However, their performance depends heavily on the semantic quality of training data. In oncology, where precision and interpretability are vita...

---

### 28. [Towards Comprehensive Stage-wise Benchmarking of Large Language Models in Fact-Checking](https://arxiv.org/abs/2601.02669)

**Authors**: Hongzhan Lin, Zixin Chen, Zhiqi Shen, Ziyang Luo, Zhen Ye, Jing Ma, Tat-Seng Chua, Guandong Xu  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.02669v1  

#### Abstract
Large Language Models (LLMs) are increasingly deployed in real-world fact-checking systems, yet existing evaluations focus predominantly on claim verification and overlook the broader fact-checking workflow, including claim extraction and evidence retrieval. This narrow focus prevents current benchm...

---

### 29. [Iterative Structured Pruning for Large Language Models with Multi-Domain Calibration](https://arxiv.org/abs/2601.02674)

**Authors**: Guangxin Wu, Hao Zhang, Zhang Zhibin, Jiafeng Guo, Xueqi Cheng  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.02674v1  

#### Abstract
Large Language Models (LLMs) have achieved remarkable success across a wide spectrum of natural language processing tasks. However, their ever-growing scale introduces significant barriers to real-world deployment, including substantial computational overhead, memory footprint, and inference latency...

---

### 30. [LLM-Augmented Changepoint Detection: A Framework for Ensemble Detection and Automated Explanation](https://arxiv.org/abs/2601.02957)

**Authors**: Fabian Lukassen, Christoph Weisser, Michael Schlee, Manish Kumar, Anton Thielmann, Benjamin Saefken, Thomas Kneib  
**Category**: cs.CL  
**Published**: 2026-01-07  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.02957v1  

#### Abstract
This paper introduces a novel changepoint detection framework that combines ensemble statistical methods with Large Language Models (LLMs) to enhance both detection accuracy and the interpretability of regime changes in time series data. Two critical limitations in the field are addressed. First, in...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
