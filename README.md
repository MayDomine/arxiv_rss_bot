# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2026-01-09 05:58:54 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [FaST: Efficient and Effective Long-Horizon Forecasting for Large-Scale Spatial-Temporal Graphs via Mixture-of-Experts](https://arxiv.org/abs/2601.05174)

**Authors**: Yiji Zhao, Zihao Zhong, Ao Wang, Haomin Wen, Ming Jin, Yuxuan Liang, Huaiyu Wan, Hao Wu  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2601.05174v1  

#### Abstract
Spatial-Temporal Graph (STG) forecasting on large-scale networks has garnered significant attention. However, existing models predominantly focus on short-horizon predictions and suffer from notorious computational costs and memory consumption when scaling to long-horizon predictions and large graph...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**大规模时空图（Large-Scale Spatial-Temporal Graph, STG）上的长时域预测任务**中存在的两大挑战：
- **计算复杂度高**：传统STGNN模型在处理大规模图（数千节点）和长预测序列（如672步）时，由于图卷积（GNN）和自注意力机制的 $O(N^2)$ 和 $O(T^2)$ 复杂度，导致内存爆炸和训练效率低下。
- **表达能力受限**：现有高效方法（如结构感知或结构无关方法）为降低复杂度而牺牲了空间语义或时间异质性建模能力，难以捕捉复杂的时空依赖。

### 提出的新方法：FaST
提出 **FaST**（Fast long-horizon forecasting framework），一个基于 **异质性感知的 Mixture-of-Experts (MoE)** 的高效且有效的长时域预测框架。其核心创新包括：

#### （1）自适应图代理注意力机制（Adaptive Graph Agent Attention, AGA-Att）
- 引入可学习的 **agent tokens**（数量 $a \ll N$）作为中介节点，通过“节点→代理聚合”和“代理→节点分发”两阶段注意力，将空间交互复杂度从 $O(N^2)$ 降至 $O(Na)$。
- 避免了全图节点间的成对计算，显著提升可扩展性。

#### （2）并行化 MoE 模块（Parallelized GLU-MoE）
- 将传统的 Feed-Forward Network（FFN）替换为 **Gated Linear Unit (GLU) 专家网络**，并设计了高效的并行计算机制。
- 提出 **异质性感知路由器（Heterogeneity-Aware Router, HA-Router）**，动态地根据节点和时间位置选择专家路径，避免专家极化（expert polarization），保留时空异质性。
- 实现了 **密集 MoE（dense MoE）** 设计，所有专家均参与前向传播，提升模型容量和鲁棒性。

#### （3）线性复杂度架构
- 结合 **MoE 时间压缩输入模块** 和 **AGA-Att**，使整体模型的时间和空间复杂度均与节点数 $N$ 和时间步 $T$ 呈线性关系，具备良好的可扩展性。

### 相比现有方法的优势
- **高效性**：实现线性复杂度，支持在单GPU上训练包含数千节点、预测长达一周（672步）的大规模STG。
- **有效性**：通过异质性感知的MoE和代理注意力，保留了丰富的时空语义，避免了信息损失和表示同质化。
- **可扩展性**：在多个大规模真实数据集上验证了其在不同图规模和预测长度下的稳定性和优越性。

---

## 2. 核心实验方法和设置

### 数据集
使用 **LargeST** 基准数据集，包含四个大规模交通流量子集：
| 数据集 | 节点数 | 时间粒度 | 时间跨度 | 样本量 |
|--------|--------|----------|----------|--------|
| **SD** | 716 | 15分钟 | 2019全年 | ~25M |
| **GBA** | 2,352 | 15分钟 | 2019全年 | ~82M |
| **GLA** | 3,834 | 15分钟 | 2019全年 | ~134M |
| **CA** | 8,600 | 15分钟 | 2019全年 | ~300M |

数据按时间顺序划分为 6:2:2 的训练/验证/测试集。

### 实验设置
- **预测任务**：基于过去96个时间步（24小时）预测未来 {48, 96, 192, 672} 步（即1天到1周）。
- **硬件环境**：NVIDIA RTX A6000 GPU (48GB)，AMD EPYC CPU，128GB RAM。
- **统一配置**：所有模型在 **BasicTS** 基准框架下实现，隐藏维度统一设为64。FaST采用固定配置（#experts=8, #layers=3, #agents=32, #dim=64），未进行数据集特定调参。

### 评估指标
- **MAE**（平均绝对误差）
- **RMSE**（均方根误差）
- **MAPE**（平均绝对百分比误差）
- **R²**（决定系数，越高越好）

### 基线方法对比
分为两类：
1. **时序中心方法（Temporal-centric）**：
   - DLinear
   - NHITS
   - CycleNet
2. **时空中心方法（Spatial-Temporal-centric）**：
   - DCRNN, STGCN, GWNet, SGP, STID, STDMAE, BigST, RPMixer, STPGNN, PatchSTG

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- FaST 在所有数据集、所有预测长度和所有指标上均取得 **SOTA 性能**。
- 在最大规模的 **CA 数据集（8,600节点，672步预测）** 上：
  - **MAE 改进**：相比最佳基线（STID）降低 **4.4%~18.4%**。
  - **训练速度**：比最佳基线快 **1.3×~2.2×**。
  - **推理速度**：同样显著优于基线。

### 与基线方法的对比结果
- **准确性**：FaST 在16个预测任务中全部排名第一（见图3）。
- **效率**：
  - 在 CA 数据集上，FaST 的 GPU 内存占用为 **38.1GB**，而 PatchSTG 达到 **42.3GB**，部分基线（如 RPMixer）因显存不足无法运行。
  - FaST 的参数量（1.466M）虽高于部分轻量模型（如 DLinear 的130K），但远低于 PatchSTG（1.951M），在性能与效率间取得良好平衡。
- **可扩展性**：随着节点数增加（SD → CA），FaST 的内存增长呈线性趋势，而其他方法（如 NHITS, DLinear）增长更快。

### 消融实验结果
消融实验验证了各组件的有效性（见图4）：
| 变体 | 性能下降原因 |
|------|-------------|
| **w/ LinearInput** | 用线性层替代MoE输入模块，性能显著下降，说明 **MoE时间压缩** 对捕捉复杂特征至关重要。 |
| **w/o HA-MoE** | 移除整个HA-MoE模块，性能下降最严重，证明 **MoE结构是模型核心**。 |
| **w/o HA-Router** | 移除异质性感知路由器，性能显著下降，说明 **动态路由机制** 对避免专家极化、提升表达能力至关重要。 |
| **w/o AGA-Att** | 移除代理注意力，性能大幅下降，证明 **线性空间复杂度设计** 是高效性的关键。 |

此外，**专家组件对比**（表2）显示：
- 使用 **GLU-Experts** 替代传统 **FFN-Experts**，精度几乎不变，但训练速度提升 **1.4倍**，验证了并行化设计的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **FaST 是首个成功实现大规模（数千节点）与超长时域（672步）联合预测的框架**，解锁了一周提前预测的能力。
2. **异质性感知的 MoE 设计** 能有效缓解压缩带来的信息损失和表示同质化问题，同时保持高计算效率。
3. **代理注意力机制（AGA-Att）** 成功将空间复杂度降至线性，且理论分析（附录A）表明其重构误差较低，保真度高。
4. **FaST 具有良好的泛化能力**：在电力需求预测（Electricity dataset）上也取得SOTA，MAPE相对提升达 **12.03%~19.06%**。

### 方法的局限性
- 当前模型依赖于固定的 agent 数量和专家数量，可能需要根据具体场景调整以达到最优。
- 虽然解决了大规模STG的效率问题，但在极端稀疏或高度非平稳的数据上表现仍需进一步验证。

### 未来工作方向
- 探索将 **时间序列基础模型（time series foundation models）** 与 FaST 框架结合，进一步提升其通用性和可迁移性。
- 研究更自适应的 agent 和 expert 数量选择策略，实现动态资源配置。
- 将 FaST 扩展至更多领域，如气象预测、能源调度、城市事件预测等。

> **源码地址**：https://github.com/yijizhao/FaST

</details>

---

### 2. [Safety-Utility Conflicts Are Not Global: Surgical Alignment via Head-Level Diagnosis](https://arxiv.org/abs/2601.04262)

**Authors**: Wang Cai, Yilin Wen, Jinchang Hou, Du Su, Guoqiu Wang, Zhonghou Lv, Chenfu Bao, Yunfang Wu  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2601.04262v1  

#### Abstract
Safety alignment in Large Language Models (LLMs) inherently presents a multi-objective optimization conflict, often accompanied by an unintended degradation of general capabilities. Existing mitigation strategies typically rely on global gradient geometry to resolve these conflicts, yet they overloo...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Safety-Utility Conflicts Are Not Global: Surgical Alignment via Head-Level Diagnosis**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在进行 **Safety Alignment**（安全对齐）时，常伴随着“**alignment tax**”——即模型通用能力（如推理、知识问答）的显著下降。传统方法将此视为全局多目标优化（MOO）问题，通过调整梯度方向（如 PCGrad）或参数更新方式（如 LoRA）来缓解冲突，但这些方法通常将模型视为同质整体，忽略了其内部模块化异质性（Modular Heterogeneity）。

本文指出，这种“一刀切”的全局策略会无差别地更新所有参数，导致某些对通用能力高度敏感的注意力头（attention heads）被破坏，从而引发性能退化。

---

### 🚀 提出的新方法：**Conflict-Aware Sparse Tuning (CAST)**

CAST 是一种结合 **Head-Level 诊断** 与 **稀疏微调** 的框架，核心思想是：**并非所有注意力头都同等重要，安全-效用冲突集中在少数“高冲突”头上**。

#### 方法流程：
1. **Head-Level Conflict Diagnosis**（头级冲突诊断）  
   在对齐训练前，构建一个“**Conflict Map**”，识别每个注意力头的风险等级。该评分由两个维度合成：
   - **Optimization Conflict (O)**：安全目标与效用目标梯度方向之间的夹角（几何冲突）
   - **Functional Sensitivity (S)**：该头对通用任务（如 MMLU）与安全行为（如拒绝有害请求）的因果影响差异（功能敏感性）

2. **Sparse Fine-Tuning**（稀疏微调）  
   仅更新“低冲突”头（Safe Zone），冻结“高冲突”头（Risky Zone），实现精准干预。

#### 统一冲突评分公式：
$$
C(h) = O(h) \cdot S(h)
$$

---

### 🔍 相比现有方法的优势
| 方面 | 传统方法（如 Full SFT, PCGrad） | CAST |
|------|-------------------------------|------|
| **更新粒度** | 全局参数更新 | 头级别选择性更新 |
| **假设前提** | 模型同质 | 承认模块异质性 |
| **效率** | 高计算成本，易引入副作用 | 参数高效，避免关键头被破坏 |
| **可解释性** | 黑箱优化 | 提供可视化 Conflict Map |
| **trade-off 表现** | 安全提升以牺牲效用为代价 | 显著减少效用损失，保持安全 |

---

## 2. 核心实验方法和设置

### 📚 数据集

| 类型 | 数据集 | 用途 |
|------|--------|------|
| **诊断阶段** | `Dutil`: 500 个 MMLU 样本<br>`Dsafe`: 500 个 WildJailbreak 有害提示 | 构建 Conflict Map |
| **对齐训练** | `Dalign`: 10,000 样本（来自 WildJailbreak）<br>包含四类：vanilla harmful, adversarial harmful, vanilla benign, adversarial benign | 安全微调 |
| **评估基准** | - **安全**: WildJailbreak (test), WildGuard, DAN<br>- **效用**: <br> • 知识: MMLU, CSQA<br> • 推理: GSM8K, MATH | 性能评估 |

---

### 📊 评估指标

- **Safety Performance**: Defense Success Rate（防御成功率）
- **Utility Performance**: 准确率（Accuracy）
- **Trade-off 指标**：
  - **Utility Cost Ratio (UCR)**：单位安全增益带来的效用损失
    $$
    UCR = \max\left(0, \frac{U_b - U_a}{(S_a - S_b) + \epsilon}\right)
    $$
  - **MMLU-CR**：MMLU 上的 UCR 变体
- **相关性检验**：Pearson $r$ 和 Spearman $\rho$，衡量预测冲突分与实际效用损失的相关性

---

### ⚖️ 基线方法对比

| 基线 | 描述 |
|------|------|
| **Full SFT** | 对所有注意力头进行 LoRA 微调（标准对齐） |
| **Random-SFT (25%)** | 随机选择 25% 头进行微调（控制参数量） |
| **PCGrad (PCG)** | 投影冲突梯度以缓解干扰（几何优化代表） |
| **CAST-SFT / CAST-PCG** | 仅更新低冲突头（Bottom-25%）或高冲突头（Top-25%） |

> 所有方法均采用相同超参（LoRA rank=32, lr=1e-4, batch size=8），确保公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 1 & Figure 3）

#### 在 **Llama-8B** 上的表现（部分）：

| 方法 | MMLU | GSM8K | Avg. Gen | Avg. Safe |
|------|------|-------|----------|-----------|
| Base | 59.38 | 87.40 | 66.10 | 67.22 |
| Full-SFT | 46.28 | 20.80 | 36.78 | 90.61 |
| Random-SFT (25%) | 52.56 | 75.73 | 59.71 | 94.43 |
| **CAST-SFT (Safe Zone)** | **55.73** | **77.20** | **61.34** | **92.62** |
| CAST-SFT (Risky Zone) | 48.52 | 68.80 | 56.02 | 91.79 |

> ✅ **CAST 在仅更新 25% 参数的情况下，恢复了接近 Base 模型的通用能力，同时达到与 Full-SFT 相当的安全水平。**

---

### 🔁 与 PCGrad 结合的结果（Table 2 & Figure 4）

| 方法 | MMLU | GSM8K | Avg. Gen | Avg. Safe |
|------|------|-------|----------|-----------|
| Full-PCG | 57.85 | 71.00 | 59.71 | 89.76 |
| **CAST-PCG (Safe Zone)** | **60.17** | **82.20** | **64.80** | **89.65** |

> ✅ **CAST + PCGrad 进一步提升了效用表现，说明结构选择与几何优化可协同增效。**

---

### 📉 消融实验结果（Appendix F）

#### （1）统一评分必要性（Table 8）
- 单独使用 $O(h)$ 或 $S(h)$ 预测效用损失不稳定（如 $O(h)$ 在 Llama 上与 MMLU-CR 负相关）
- **统一评分 $C(h)$ 在所有模型上均保持强正相关（Pearson $r \in [0.73, 1.00]$）**

#### （2）稀疏比例分析（Table 9）
- 更新 **Bottom-25% 低冲突头** 效果最佳（General Avg = 61.34）
- 增加至 Bottom-50% 反而性能下降（57.93），说明“更多参数 ≠ 更好”
- Full SFT 导致“悬崖式”崩溃（General Avg 降至 36.78）

#### （3）数据效率测试（Table 10）
- 仅用 **100 个 MMLU 样本** 构建诊断，仍能准确识别最优头组（B4 达最高效用）
- 证明 Conflict Map 具备良好数据鲁棒性

#### （4）领域迁移问题（Table 11）
- 若用 **GSM8K（推理任务）** 替代 MMLU 进行诊断，最优区不再位于尾部（B4），而是中间桶（B2/B3）
- 原因：单头消融难以捕捉复杂推理链的依赖关系，导致敏感性低估
- 结论：**诊断需与目标任务匹配**

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **安全-效用冲突不是全局的，而是局部化的**  
   冲突集中在少数特定注意力头（尤其是中深层），大多数头可安全更新。

2. **效用损失主要源于对“高冲突头”的强制更新**  
   这些头兼具高梯度冲突与高功能敏感性，轻微扰动即可导致推理崩溃或过度拒绝（over-refusal）。

3. **CAST 实现更优的 Pareto 前沿**  
   在相同参数预算下，CAST 显著优于 Full SFT、Random SFT 和 PCGrad，实现了“安全不降、效用少损”。

4. **Conflict Score 具有强预测力**  
   预对齐的 $C(h)$ 与后对齐的 UCR 高度相关（Pearson $r \geq 0.73$），可用于风险预警。

5. **Qualitative 改进明显**  
   - 避免 **Reasoning Collapse**（跳过 CoT 步骤）
   - 减少 **Over-Refusal**（错误拒绝无害数学题）

---

### ⚠️ 局限性（Limitations）

| 限制 | 说明 |
|------|------|
| **Structural Scope** | 当前仅关注 Query Projection (Wq)，未覆盖 MLP 层（占多数参数） |
| **Static Diagnosis** | 冲突图谱基于预对齐状态，假设其稳定；但长周期训练可能导致“冲突漂移” |
| **Task Dependence** | 诊断依赖校准集，特定领域（如代码生成）可能需要定制化诊断 |

---

### 🔮 未来工作方向

1. 将诊断扩展至 **MLP 层** 和 **value/key projections**
2. 设计 **动态 Conflict Map**，支持训练过程中在线更新
3. 开发 **领域自适应诊断机制**，适配不同下游任务
4. 探索 **自动化 Safe Zone 划分算法**，替代固定百分比分桶

---

## 总结

> **CAST 提供了一种从“全局粗调”到“精准手术”的范式转变**。它利用 mechanistic interpretability 的洞察，将 alignment 视为一个结构感知的稀疏优化问题，而非单纯的梯度工程。实验证明，**避开少数高风险头，即可大幅缓解 alignment tax**，为高效、可解释的安全对齐提供了新路径。

</details>

---

### 3. [Distributed Online Convex Optimization with Efficient Communication: Improved Algorithm and Lower bounds](https://arxiv.org/abs/2601.04907)

**Authors**: Sifan Yang, Wenhao Yang, Wei Jiang, Lijun Zhang  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2601.04907v1  

#### Abstract
We investigate distributed online convex optimization with compressed communication, where $n$ learners connected by a network collaboratively minimize a sequence of global loss functions using only local information and compressed data from neighbors. Prior work has established regret bounds of $O(...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Distributed Online Convex Optimization with Efficient Communication: Improved Algorithm and Lower bounds

## 1. 论文的主要贡献和创新点

### 解决的问题
本文研究的是**分布式在线凸优化（D-OCO）中带有压缩通信的场景**。在该设定下，$n$ 个通过网络连接的学习者（learners）协作最小化一系列全局损失函数 $f_t(x) = \frac{1}{n}\sum_{i=1}^n f_{t,i}(x)$，但每个学习者只能访问局部信息，并且与邻居交换的信息需要经过**压缩（compression）**以减少通信开销。

已有工作（如 Tu et al., 2022）提出的算法（如 DC-DOGD）存在以下问题：
- **理论保证较差**：其后悔界（regret bounds）对压缩质量因子 $w^{-1}$ 呈现出二次甚至四次方依赖（quadratic or quartic dependence），当压缩率较高（即 $w < 1$）时，性能急剧下降。
- **对学习者数量 $n$ 的依赖过强**：表现出超线性依赖（super-linear dependence），不够理想。

因此，本文旨在解决如何设计一个在压缩通信下具有更优理论保证的 D-OCO 算法。

---

### 提出的新方法和新思路
作者提出了一种名为 **Two-level Compressed Decentralized Online Gradient Descent (Top-DOGD)** 的新算法，其核心是一个**两级阻塞更新框架（two-level blocking update framework）**，并结合了两个关键技术：

1.  **在线压缩八卦策略（online gossip strategy）**：
    - 在每个“块”（block）的前 $L_1$ 轮中，执行多步 gossip 更新。
    - 利用多次通信来加速共识过程，从而有效降低**共识误差（consensus error）**和**压缩误差（compression error）**。
    - 通过阻塞机制，将多轮通信的开销平摊到单轮通信中，保持了每轮一次通信的效率。

2.  **投影误差补偿方案（error compensation scheme）**：
    - 在每个“块”的后 $L_2$ 轮中，递归地压缩和传输投影操作产生的残差（residual）。
    - 这一机制专门用于控制因投影操作引入的**投影误差（projection error）**，避免其导致对 $n$ 的不良依赖。

这两个技术协同工作，在一个统一的框架内同时处理了由**去中心化、压缩和投影**带来的三种主要误差源。

---

### 相比现有方法的优势
- **显著改进的后悔界（Regret Bounds）**：
  - 对于凸函数，Top-DOGD 达到了 $O(w^{-1/2}p^{-1}n\sqrt{T})$ 的后悔界。
  - 对于强凸函数，达到了 $O(w^{-1}p^{-2}n\ln T)$ 的后悔界。
  - 相比 Tu et al. (2022) 的 $O(\max\{w^{-2}p^{-4}n^{1/2}, w^{-4}p^{-8}\}n\sqrt{T})$ 和 $O(\max\{w^{-2}p^{-4}n^{1/2}, w^{-4}p^{-8}\}n\ln T)$，Top-DOGD 在 $w$ 和 $p$ 上的依赖大大减弱，从 $w^{-2}/w^{-4}$ 改进到 $w^{-1/2}/w^{-1}$，从 $p^{-4}/p^{-8}$ 改进到 $p^{-1}/p^{-2}$。

- **建立了首个下界（Lower Bounds）**：
  - 论文首次为该问题建立了匹配的下界：$\Omega(w^{-1/2}p^{-1/4}n\sqrt{T})$（凸）和 $\Omega(w^{-1}p^{-1/2}n\ln T)$（强凸）。
  - 这些下界证明了所提上界的**近似最优性**（near-optimality），尤其是在 $w$ 和 $T$ 上的依赖。

- **扩展到Bandit反馈场景**：
  - 将 Top-DOGD 与经典梯度估计器结合，提出了 Top-DOBD-1（单点反馈）和 Top-DOBD-2（两点反馈）。
  - 在 bandit 场景下也显著改进了现有的后悔界。

## 2. 核心实验方法和设置

值得注意的是，**本文是一篇理论分析为主的论文，没有进行传统意义上的数值实验或在真实数据集上的测试**。其“实验”部分主要体现在**严格的数学推导和理论证明**上。

### 使用的数据集
- **无实际数据集**。所有分析基于通用的凸/强凸函数假设和标准的理论模型。

### 实验设置和评估指标
- **问题设定**：$n$ 个学习者在一个由图 $G=([n], E)$ 定义的网络上协作。
- **通信模型**：学习者之间通过一个满足双重随机性和谱隙（spectral gap）$p$ 的矩阵 $P$ 进行通信。
- **压缩模型**：采用 $w$-contractive compressor，其中 $w \in (0,1]$ 表示压缩质量（$w=1$ 为无压缩）。
- **评估指标**：主要评估指标是**后悔值（Regret）** $R(T,i)$，定义为学习者 $i$ 在 $T$ 轮内的累计损失与最优固定决策的累计损失之差。
- **理论分析工具**：利用了凸分析、鞅不等式、谱图理论等工具进行严格证明。

### 基线方法对比
- 主要对比对象是 **Tu et al. (2022)** 提出的 **DC-DOGD** 算法。
- 同时与未压缩通信下的最优结果（如 Wan et al., 2025）进行了理论上的比较，以说明压缩带来的额外代价。

## 3. 主要实验结果和性能指标

由于缺乏数值实验，这里的“结果”指的是**理论性能指标的对比**。

### 关键性能数据（理论后悔界）
| 来源 | 损失函数 | 后悔界（Regret Bounds） |
| :--- | :--- | :--- |
| Tu et al. (2022) | 凸函数 | $O(\max\{w^{-2}p^{-4}n^{1/2}, w^{-4}p^{-8}\}n\sqrt{T})$ |
| **本文 (Top-DOGD)** | **凸函数** | **$O(w^{-1/2}p^{-1}n\sqrt{\ln n}\sqrt{T})$** |
| Tu et al. (2022) | 强凸函数 | $O(\max\{w^{-2}p^{-4}n^{1/2}, w^{-4}p^{-8}\}n\ln T)$ |
| **本文 (Top-DOGD)** | **强凸函数** | **$O(w^{-1}p^{-2}n \ln n \ln T)$** |
| **本文 (下界)** | **凸函数** | **$\Omega(w^{-1/2}p^{-1/4}n\sqrt{T})$** |
| **本文 (下界)** | **强凸函数** | **$\Omega(w^{-1}p^{-1/2}n\ln T)$** |

### 与基线方法的对比结果
- **Top-DOGD 在理论上全面优于 DC-DOGD**：
  - 在 $w$ 的依赖上，从最坏情况的 $w^{-4}$ 改进到 $w^{-1}$，这是一个巨大的提升，意味着算法对高压缩率更加鲁棒。
  - 在 $p$ 的依赖上，从 $p^{-8}$ 改进到 $p^{-2}$，对网络连通性较差（$p$ 小）的情况更具适应性。
  - 在 $n$ 的依赖上，虽然仍有线性关系，但消除了不必要的 $n^{1/2}$ 因子，更接近已知的下界。

### 消融实验结果
论文通过理论分析进行了类似“消融实验”的讨论：
- **移除投影误差补偿（$L_2=0$）**：会导致后悔界退化为 $O(w^{-1/2}p^{-1}n^{5/4}\sqrt{\ln n}\sqrt{T})$，对 $n$ 的依赖变差。
- **仅执行单步八卦（$L_1=1$）**：即使有投影补偿，也无法改善共识和压缩误差，无法超越 Tu et al. (2022) 的结果。
- **结论**：**两级框架中的两个组件缺一不可**，必须协同工作才能达到最优的理论性能。

## 4. 关键结论和发现

### 论文的主要发现
1.  **Top-DOGD 算法成功解决了压缩通信下 D-OCO 的理论瓶颈**，实现了对压缩因子 $w$ 和谱隙 $p$ 更弱的依赖。
2.  **首次为该问题建立了匹配的下界**，从理论上证明了所提算法的近似最优性，填补了该领域的空白。
3.  **两级阻塞更新框架**是一种有效的设计范式，能够将需要多轮通信的技术（如多步 gossip 和递归压缩）无缝集成到每轮通信受限的在线学习环境中。
4.  所提方法可以自然地扩展到 **bandit 反馈**场景，并同样能带来理论上的改进。

### 方法的局限性
- **纯理论工作**：缺乏在真实数据集（如传感器网络、分布式机器学习任务）上的数值验证，其实用性和收敛速度有待实证检验。
- **复杂性增加**：算法引入了阻塞机制和额外的通信轮次（在块内），可能增加了实现的复杂性和延迟。
- **参数选择**：算法性能依赖于块大小 $L_1, L_2$ 和学习率等参数的精细调优，这些参数的理论最优值可能在实践中难以精确获得。

### 未来工作方向
- **进行大规模的数值实验**，验证 Top-DOGD 在实际应用中的性能。
- 探索更简单或自适应的参数选择策略。
- 将该框架应用于更复杂的优化问题，如非凸优化、带约束的优化或异步通信环境。
- 研究在不同类型的压缩算子（如量化、稀疏化）下的具体实现和性能。

</details>

---

### 4. [RelayLLM: Efficient Reasoning via Collaborative Decoding](https://arxiv.org/abs/2601.05167)

**Authors**: Chengsong Huang, Tong Zheng, Langlin Huang, Jinyuan Li, Haolin Liu, Jiaxin Huang  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2601.05167v1  

#### Abstract
Large Language Models (LLMs) for complex reasoning is often hindered by high computational costs and latency, while resource-efficient Small Language Models (SLMs) typically lack the necessary reasoning capacity. Existing collaborative approaches, such as cascading or routing, operate at a coarse gr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# RelayLLM: Efficient Reasoning via Collaborative Decoding —— 核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

大型语言模型（LLMs）在复杂推理任务中表现出色，但其高计算成本和延迟限制了实际部署。小型语言模型（SLMs）虽然高效，但在复杂推理上能力不足。现有的协作方法（如 cascading 或 routing）通常以“查询级别”为单位将整个任务交给 LLM 处理，导致大量不必要的计算开销——即使 SLM 能独立完成大部分推理步骤。

### **提出了什么新方法或新思路**

本文提出 **RelayLLM**，一种基于**token-level 协同解码**（collaborative decoding）的新框架，其核心思想是：

- 将 LLM 视为一个可按需调用的“工具”，而非备用生成器。
- 允许 SLM 在生成过程中主动通过特殊命令 `<call>n</call>` 请求 LLM 生成指定数量的 token，之后再由 SLM 继续推理。
- SLM 同时扮演“问题求解者”和“控制器”的角色，动态决定何时、调用多少 token 来弥补关键推理缺口。

该机制灵感来自 **tool-use agents**，实现了细粒度、按需的协作。

### **相比现有方法的优势**

| 对比维度 | 现有方法（如 cascading / routing） | RelayLLM |
|--------|-------------------------------|---------|
| 协作粒度 | 查询级（query-level） | **Token级**（fine-grained） |
| 控制机制 | 外部路由器或静态策略 | **SLM 自主决策**（无额外控制器） |
| 成本效率 | 高（整段 offload） | **极低**（仅 1.07% token 调用 LLM） |
| 性能提升 | 有限 | 显著提升（平均准确率 +6.9% vs random router） |

> ✅ **优势总结**：RelayLLM 实现了“精准干预”，避免了“全有或全无”的浪费，在几乎不增加成本的前提下显著提升了 SLM 的推理能力。

---

## 2. 核心实验方法和设置

### **使用的数据集**

训练数据：
- **DAPO dataset**（大规模强化学习推理数据集）

评估基准（共六个数学推理任务）：
- **Minerva**
- **MATH-500**
- **GSM8K**
- **Olympiad-Bench**
- **AIME-2024**
- **AIME-2025**

此外还测试了跨领域泛化能力：
- **Big-Bench Hard (BBEH)**
- **MMLU-Pro**
- **SuperGPQA**

### **实验设置和评估指标**

| 设置项 | 描述 |
|------|------|
| 模型选择 | 学生模型（SLM）：`Qwen3-0.6B`, `Qwen3-1.7B`<br>教师模型（LLM）：`Qwen3-8B`（同系列确保分布一致性） |
| 推理模式 | 非 thinking 模式（non-thinking mode） |
| 训练阶段 | 两阶段：<br>1. **Supervised Warm-up**（监督预热）<br>2. **Group Relative Policy Optimization (GRPO)**（强化学习优化） |
| 评估方式 | - AIME 数据集：`avg@32`<br>- 其他数据集：`pass@1`（greedy decoding）<br>- 使用 `GPT-4o-mini` 作为语义裁判验证答案正确性 |
| 协作成本度量 | **Call Ratio**：LLM 生成的 token 数占总输出 token 的百分比 |

### **基线方法对比**

| 基线方法 | 类型说明 |
|--------|----------|
| **Base Model** | 未经微调的原始 SLM |
| **GRPO Baseline** | 标准 GRPO 微调（无协作） |
| **CITER** | token-level 路由方法，依赖外部 MLP 控制器 |
| **Random Router** | 查询级随机路由到 SLM 或 LLM |
| **Perfect Router** | 理想情况下的查询级路由（仅难问题交给 LLM） |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 模型 | 平均准确率（6 benchmarks） | Avg. Call Ratio |
|------|-----------------------------|----------------|
| Qwen3-0.6B (Base) | 27.17% | — |
| Qwen3-1.7B (Base) | 42.50% | — |
| Qwen3-8B (Teacher Only) | 54.12% | 100% |
| **RelayLLM (Qwen3-1.7B, Difficulty-Aware)** | **49.52%** | **1.07%** |

👉 **性能恢复效果**：RelayLLM 恢复了约 **60%** 的 SLM 与 LLM 之间的性能差距。

### **与基线方法的对比结果**

| 指标 | 结果 |
|------|------|
| 相比 **Random Router**（资源等效） | **+6.9% 准确率提升** |
| 相比 **CITER**（token-level 路由） | 更高准确率 + 更低成本（无需额外控制器） |
| 成本节约 | **相比性能匹配的随机路由器，减少 98.2% 的 token 开销** |
| 在 Minerva 上的表现 | Qwen3-0.6B 从 15.81% → **23.53%**（+48.8%），仅调用 0.77% token |

### **消融实验结果（Ablation Study）**

使用 Qwen3-1.7B 进行消融分析：

| 方法变体 | 平均准确率 | Call Ratio |
|--------|-----------|------------|
| 完整 RelayLLM | **49.52%** | **1.07%** |
| w/o Data Filtering（不滤除教师无法解决的问题） | 48.76% | **3.30%** |
| w/o Independence Incentive（取消独立成功奖励） | 49.34% | **4.10%** |
| w/o Exploration Reward（取消探索奖励） | **47.56%** | 0.65% |

🔍 **发现**：
- **数据过滤** 可防止无效调用，节省成本；
- **鼓励独立性** 是控制过度依赖的关键；
- **探索奖励** 显著提升准确性，尤其对极难题目至关重要。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **Token-level 协作优于 Query-level 路由**  
   RelayLLM 证明：只需在少数关键 token 上引入 LLM 干预，即可大幅提升 SLM 表现，实现“四两拨千斤”。

2. ✅ **SLM 可学会战略性求助行为**  
   通过 GRPO 和难度感知奖励设计，SLM 能自主判断是否需要帮助，并做出高效决策。

3. ✅ **显著的成本-性能权衡优势**  
   仅调用 **1.07%** 的 token 来自 LLM，却带来接近大模型的推理表现，**成本降低 98.2%**。

4. ✅ **具备良好的泛化能力**  
   在未见过的非数学领域（如 MMLU-Pro、BBEH）也表现优异，说明其形成了通用的“认知边界识别”能力。

5. ✅ **学生模型内化了推理能力**  
   在“无教师”测试中，RelayLLM 训练后的 SLM 仍优于基线，表明它不仅学会了调用，还吸收了 LLM 的推理模式。

6. ✅ **动态长度预测更高效**  
   相比固定调用长度（Fixed-100, Fixed-500），RelayLLM 动态决定调用长度，在保持精度的同时大幅降低成本（1.07% vs 2.87%）。

### **方法的局限性**

- **依赖高质量 LLM 输出**：若 LLM 本身无法解决问题，则协作无效（因此需数据过滤）。
- **分布对齐敏感**：换用不同风格的 LLM 会导致性能下降，说明存在分布迁移问题。
- **当前主要适用于可验证任务**：奖励设计依赖规则验证器（verifier），难以直接扩展到开放生成任务。

### **未来工作方向**

- 扩展至多模态或多步工具调用场景；
- 探索更鲁棒的分布对齐机制，支持跨家族模型协作；
- 将该范式应用于其他高效推理架构（如 speculative decoding）；
- 构建通用的“认知助手”接口，让 SLM 自主调度多个专家模型。

---

> 🔗 **代码开源地址**：[https://github.com/Chengsong-Huang/RelayLLM](https://github.com/Chengsong-Huang/RelayLLM)

</details>

---

### 5. [AM$^3$Safety: Towards Data Efficient Alignment of Multi-modal Multi-turn Safety for MLLMs](https://arxiv.org/abs/2601.04736)

**Authors**: Han Zhu, Jiale Chen, Chengkun Cai, Shengjie Sun, Haoran Li, Yujin Zhou, Chi-Min Chan, Pengcheng Wen, Lei Li, Sirui Han, Yike Guo  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04736v1  

#### Abstract
Multi-modal Large Language Models (MLLMs) are increasingly deployed in interactive applications. However, their safety vulnerabilities become pronounced in multi-turn multi-modal scenarios, where harmful intent can be gradually reconstructed across turns, and security protocols fade into oblivion as...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AM³Safety: Towards Data Efficient Alignment of Multi-modal Multi-turn Safety for MLLMs

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的多模态大语言模型（MLLMs）安全对齐方法主要针对单轮视觉问答（VQA）任务设计，且严重依赖昂贵的人工偏好标注。这些方法在多轮多模态对话场景中效果有限，因为：
- 对话中的有害意图可以通过多轮交互逐步重构（gradual intent reconstruction）
- 随着对话进行，安全协议容易被遗忘
- 缺乏专门用于多轮对话安全对齐的数据集

### 提出的新方法和新思路
本文提出了 **AM³Safety** 框架，旨在解决上述挑战，其核心创新包括：

#### （1）InterSafe-V 数据集
- **构建方式**：通过模型间交互（model-to-model interaction）自动生成，无需人工标注
- **规模**：包含 11,270 个对话和 500 个专门设计的拒绝型 VQA 样本
- **特点**：
  - 强调对话上下文，平均 4 轮对话，每轮 1.53 张图像
  - 通过恶意意图分解（Harmful Intent Decomposition）生成隐蔽的多轮攻击路径
  - 包含多个领域（如健康、金融）的专业拒绝样本

#### （2）AM³Safety 框架
结合冷启动拒绝学习（cold-start refusal phase）与基于组相对策略优化（Group Relative Policy Optimization, GRPO）的微调，具体包括：
- **冷启动阶段**：使用 500 个拒绝模板进行监督微调（SFT），使模型学会以合理理由拒绝有害请求
- **GRPO 微调阶段**：引入**回合感知双目标奖励函数**（turn-aware dual-objective rewards），动态权衡安全性和有用性

### 相比现有方法的优势
- **数据高效**：完全避免了昂贵的人工标注成本
- **对话适应性强**：专为多轮多模态对话设计，能有效应对渐进式攻击
- **安全性与有用性平衡**：通过双目标奖励机制，在提升安全性的同时保持甚至增强模型的有用性
- **通用性强**：可在不同 MLLM 架构上应用（如 Qwen2.5-VL 和 LLaVA-NeXT）

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 规模 | 特点 |
|--------|------|------|------|
| **InterSafe-V** | 训练集 | 11,270 对话 + 500 VQA | 本文提出，模型生成，多轮多图 |
| **SafeMT** | 基准测试 | 多轮多模态对话 | 评估多轮安全性能 |
| **JailbreakV-mini** | 基准测试 | 小规模子集 | 评估对抗攻击鲁棒性 |
| **MM-SafetyBench** | 基准测试 | 单轮 VQA | 传统安全基准 |
| **MMSafe-PO** | 基准测试 | 包含部分对话 | 多模态安全评估 |

### 实验设置
- **基础模型**：Qwen2.5-VL-7B-Instruct 和 LLaVA-NeXT-7B
- **训练资源**：8×H800 GPU，仅使用 7,000 个对话进行 GRPO 微调（<15 轮）
- **GRPO 参数**：每指令生成 8 个 rollout，全局 batch size 128，PPO mini-batch size 8

### 评估指标
| 指标 | 定义 | 说明 |
|------|------|------|
| **Win Rate** | 由 GPT-4o 判断的胜率 | 综合评估安全与有用性，越高越好 |
| **Attack Success Rate (ASR)** | 被判定为不安全的响应比例 | 衡量防御能力，越低越好 |
| **Harmless Score** | 无害维度得分 | 越高表示越安全 |
| **Helpful Score** | 有用维度得分 | 越高表示越有帮助 |

### 基线方法对比
- **RLHF-V**：基于人类反馈的强化学习
- **Safe RLHF-V**：平衡安全性与有用性的 RLHF 变体
- **MM-DPO**：多模态直接偏好优化
- **SPA-VL**：综合安全偏好对齐数据集

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

#### 在 **Qwen2.5-VL-7B** 上的表现（SafeMT 基准）：
| 方法 | Helpful ↑ | Harmless ↑ | ASR ↓ |
|------|----------|-----------|-------|
| Base Model | 0.5000 | 0.5000 | 0.4892 |
| +Ours (AM³Safety) | **0.6319** (+13.19%) | **0.5819** (+8.19%) | **0.2806** (-20.86%) |

#### 在 **LLaVA-NeXT-7B** 上的表现（SafeMT 基准）：
| 方法 | Helpful ↑ | Harmless ↑ | ASR ↓ |
|------|----------|-----------|-------|
| Base Model | 0.5000 | 0.5000 | 0.4895 |
| +Ours (AM³Safety) | **0.8210** (+32.10%) | **0.6918** (+19.18%) | **0.3844** (-10.51%) |

> ✅ **结论**：AM³Safety 显著提升了模型的安全性（ASR 下降 >10%），同时大幅增强了有用性（Helpful 提升 ≥8%，最高达 32%）。

### 与其他基准的对比
在 **JailbreakV** 和 **MM-SafetyBench** 上也表现出色，尤其在复杂对话场景（如 SafeMT）中优势明显。

### 消融实验结果（Figure 3 & Table 4）
- **仅 GRPO 微调**：提升有限，仍易被 jailbreak
- **加入冷启动拒绝学习**：显著提升安全性和有用性
- **系数 β（helpfulness 权重）的影响**：
  - 最优值为 **β = 0.1**
  - 过高（如 β=1 或 10）会导致安全性下降
  - 证明“先学会拒绝，再提供帮助”的策略更有效

---

## 4. 关键结论和发现

### 主要发现
1. **多轮对话安全是独立挑战**：现有单轮 VQA 安全方法无法有效应对多轮渐进式攻击。
2. **冷启动拒绝至关重要**：让模型先学会以合理理由拒绝有害请求，是后续优化的基础。
3. **数据效率可行**：通过模型交互生成高质量训练数据，可大幅降低人工标注成本。
4. **安全与有用性可兼得**：通过 turn-aware 双目标奖励，可在提升安全性的同时增强模型实用性。

### 方法的局限性
- **未在专用对抗性 jailbreak 基准上测试**：实际对抗鲁棒性尚需验证
- **依赖外部强模型作为 judge**：使用 InternVL3-78B 作为奖励模型，存在教师模型偏差风险
- **未覆盖专用推理模型**：目前仅在通用 MLLM 上验证，对专用推理模型的影响未知

### 未来工作方向
- 探索**自我修正机制**（self-correction），减少对外部 judge 的依赖
- 扩展到更多类型的 MLLM 架构和应用场景
- 构建更具挑战性的**对抗性多轮安全基准**

> 📌 **总体评价**：AM³Safety 提供了一种实用、高效且可扩展的多模态多轮对话安全对齐方案，为 MLLM 在真实交互场景中的安全部署提供了重要技术路径。

</details>

---

### 6. [Revisiting Judge Decoding from First Principles via Training-Free Distributional Divergence](https://arxiv.org/abs/2601.04766)

**Authors**: Shengyin Sun, Yiming Li, Renxi Liu, Weizhe Lin, Hui-Ling Zhen, Xianzhi Yu, Mingxuan Yuan, Chen Ma  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.04766v1  

#### Abstract
Judge Decoding accelerates LLM inference by relaxing the strict verification of Speculative Decoding, yet it typically relies on expensive and noisy supervision. In this work, we revisit this paradigm from first principles, revealing that the ``criticality'' scores learned via costly supervision are...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Revisiting Judge Decoding from First Principles via Training-Free Distributional Divergence*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
传统的 **Speculative Decoding (SD)** 虽然能加速大语言模型（LLM）推理，但其验证机制过于严格，仅接受与目标模型完全一致的 token，导致大量语义等价但形式不同的候选 token 被错误拒绝，限制了加速潜力。

为此提出的 **Judge Decoding** 引入一个“法官”分类器来判断 draft token 是否“语义正确”，从而放宽验证标准。然而，这类方法严重依赖昂贵且噪声大的监督信号（如人工标注或反事实生成），存在以下瓶颈：
- **高成本**：手动标注耗时费力，自动挖掘（如 AutoJudge）需大量 GPU 小时；
- **标签噪声**：生成的随机性导致同一 token 在不同 rollout 中被标记为“关键”或“非关键”；
- **泛化差**：在跨领域任务中表现下降。

---

### 🚀 提出的新方法与核心思想
本文从第一性原理出发，提出一个颠覆性观点：  
> **“关键性”（criticality）信号本质上已内嵌于 draft 与 target 模型之间的分布差异中，无需外部监督即可捕捉。**

#### 核心创新点：
1. **理论发现**：证明了训练得到的线性“法官”分类器与 **KL 散度** 共享相同的底层 logit 原语（logit primitives）。具体而言：
   - KL 散度是对 logit 差异的**二次聚合**（quadratic aggregation）；
   - 线性分类器是这些差异上的**线性决策面**（linear partitioning）；
   - 二者本质都在检测 draft 和 target 模型之间相对偏好变化的关键 token。

2. **新方法：Training-Free KL Thresholding**
   - 提出一种无需训练、基于 **token-level KL divergence** 的验证机制。
   - 当 KL 散度低于阈值时，接受 draft token；否则拒绝。
   - 引入轻量级置信掩码（confidence mask）：若 target 模型 top-1 概率 > 0.9，则回退到标准 SD，确保高确定性下的精度。

---

### 🔍 相比现有方法的优势
| 维度 | 传统 Judge Decoding（如 AutoJudge） | 本文方法（KL Thresholding） |
|------|-------------------------------|-----------------------------|
| **训练需求** | 需要大量标注/反事实数据训练分类器 | 完全无需训练，纯统计方法 |
| **计算开销** | 数据挖掘成本极高（如 2700 GPU 小时） | 推理时仅增加少量 KL 计算 |
| **鲁棒性** | 对领域偏移敏感，泛化能力弱 | 分布内信号天然具备跨域稳定性 |
| **可解释性** | 黑箱分类器，难以分析 | 基于模型内在分布差异，物理意义明确 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
- **GSM8K**：小学数学应用题，多步推理；
- **MATH-500-Hard**：MATH 数据集中最难的 Level-5 问题；
- **LiveCodeBench**：代码生成任务，强调执行正确性；
- **MMLU-Pro**：涵盖广泛学科的知识理解基准，更具挑战性。

---

### ⚙️ 实验设置与评估指标

#### 模型对（Model Pairs）
- Llama-3.2-1B-Instruct / Llama-3.1-8B-Instruct
- Llama-3.1-8B-Instruct / Llama-3.1-70B-Instruct
- Qwen3-0.6B / Qwen3-8B（用于长链推理测试）

#### 评估指标
| 指标 | 含义 |
|------|------|
| **MAT (Mean Accepted Tokens)** | 平均每轮成功接受的 draft token 数量，衡量效率 |
| **Accuracy / Benchmark Score** | 最终答案准确率或任务得分，衡量质量 |
| **Speedup (vs. Vanilla SP)** | 相对于标准 Speculative Sampling 的端到端速度提升倍数 |
| **Speed (tokens/s)** | 实际吞吐量，在 vLLM 框架下测量 |

---

### 🆚 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Vanilla SP** | 基准 | 标准 Speculative Sampling，严格匹配 |
| **Top-K** | 规则松弛 | 只要 draft token 在 target 的 top-K 内即接受 |
| **Target/Draft Entropy** | 无监督 | 基于单一模型熵进行过滤 |
| **AutoJudge** | 训练式 Judge | 使用反事实 rollouts 自动标注并训练分类器 |
| **KL Thresholding (Ours)** | 无训练 | 本文提出的方法，基于 KL 散度 |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（以 Llama-1B/8B 和 8B/70B 为例）

#### ✅ GSM8K 上的表现（图 7）
| 方法 | MAT 提升（vs Vanilla SP） | 准确率损失 |
|------|--------------------------|-----------|
| **KL Thresholding** | +63.5% (10.61 → 17.35) | ~2% ↓ |
| **AutoJudge** | 略低或相当 | ~2–3% ↓ |
| **Top-K / Entropy** | 较低 | 更大下降 |

> KL 方法在保持高准确率的同时显著提高吞吐。

#### ✅ LiveCodeBench 上的表现（图 8）
| 方法 | MAT 提升 | 执行成功率 |
|------|---------|------------|
| **KL Thresholding** | +104.0% (1B/8B), +53.7% (8B/70B) | 保持稳定，程序可执行 |
| **Top-K / Entropy** | 提升有限 | 易产生语法错误或逻辑错误 |

> 在对精确性要求高的代码生成中，KL 方法仍优于其他无监督策略。

#### ✅ MATH-500-Hard 和 MMLU-Pro（跨域测试，图 9–10）
- **AutoJudge（在 GSM8K 上训练）**：面对领域迁移时性能急剧下降，接近 Top-K 表现；
- **KL Thresholding**：依然稳健，MAT 提升超过 100%，准确率仅轻微下降（<1.5%）；
- 结论：**无训练方法具有更强的泛化能力**。

#### ✅ 端到端真实部署速度（表 1，vLLM 测试）
| 方法 | Speedup (1B/8B) | Speedup (8B/70B) | 准确率维持 |
|------|------------------|-------------------|-------------|
| **AutoJudge** | 1.40× | 1.45× | 下降 2–4% |
| **KL Thresholding** | **1.60×** | **1.45×** | 下降仅 1–2% |

> KL 方法不仅匹配甚至略微超越 AutoJudge 的加速效果，且精度更高。

#### ✅ 长链推理场景（Qwen3-0.6B/8B，图 11）
- KL 方法实现 **2.77× MAT 提升**（6.91 → 19.15），仅损失 ~2% 准确率；
- 表明该方法特别适合复杂推理路径中的频繁分歧处理。

---

### 🔍 消融实验与分析
- **KL 与 AutoJudge 得分高度相关**（图 6）：KL 散度随 AutoJudge 打分升高而单调上升，说明两者捕获的是相同信号；
- **Entropy 不相关**：target/draft 模型熵与 criticality 无明显关系；
- **理论支持**：定理 4.1 证明 KL 与线性分类器共享相同的 logit 差异原语（Δ(x)），解释了为何简单统计量可替代复杂训练。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **“关键性”是模型内在属性**：  
   draft 与 target 模型间的 **distributional divergence**（尤其是 KL 散度）本身就编码了哪些 token 是逻辑转折点，无需外部监督学习。

2. **训练不是必需的**：  
   复杂的监督式 Judge（如 AutoJudge）本质上是在拟合 KL 散度所反映的统计模式，因此可以直接用 KL 作为代理信号。

3. **无训练方法更鲁棒、更高效**：  
   - 避免了数据挖掘的巨大开销；
   - 对领域偏移不敏感；
   - 在多个任务上达到或超过训练式方法的性能。

4. **理论统一视角**：  
   KL 散度（二次）与线性分类器（一次）虽然几何形式不同，但作用于相同的 logit 差异空间，构成了对“逻辑偏差”的双重刻画。

---

### ⚠️ 局限性
1. **未在超大规模模型上验证**：  
   如 Llama-3.1-405B-Instruct 等，尚不清楚是否仍具可扩展性。

2. **KL 计算带来额外开销**：  
   当前实现未优化（如 Triton kernel 融合），可能成为现代推理框架（如 vLLM）的瓶颈。

3. **假设 draft-target 差异小**：  
   理论推导基于泰勒展开，适用于局部微小偏移，极端分歧情况需进一步研究。

---

### 🔮 未来工作方向
- 开发高效的 **operator fusion 实现**，降低 KL 计算延迟；
- 探索其他分布差异度量（如 JS 散度、Wasserstein 距离）是否更优；
- 将该思想推广至 **Medusa/EAGLE 等架构改进型 speculative decoding**；
- 结合 retrieval 或 cache 机制，构建完全无监督的高效推理流水线。

---

## 总结
> 本文从根本上重新审视了 Judge Decoding 的设计范式，揭示了“criticality”信号的本质来源，并提出了一个简单、有效、无需训练的替代方案 —— **基于 KL 散度的分布差异检测**。实验证明，这种方法不仅能媲美甚至超越复杂的训练式 Judge，在效率、鲁棒性和泛化性方面更具优势，为未来高效 LLM 推理提供了一条简洁而强大的新路径。

</details>

---

### 7. [Approximate equivariance via projection-based regularisation](https://arxiv.org/abs/2601.05028)

**Authors**: Torben Berndt, Jan St\"uhmer  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2601.05028v1  

#### Abstract
Equivariance is a powerful inductive bias in neural networks, improving generalisation and physical consistency. Recently, however, non-equivariant models have regained attention, due to their better runtime performance and imperfect symmetries that might arise in real-world applications. This has m...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Approximate Equivariance via Projection-Based Regularisation**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
在深度学习中，**Equivariance**（等变性）是一种强大的归纳偏置，能提升模型的泛化能力和物理一致性。然而，严格等变的模型往往计算开销大、灵活性差，且现实世界的数据常存在对称性破坏（如噪声、非理想几何）。因此，研究者转向“近似等变”（approximate equivariance）模型。

现有方法通常依赖**基于样本的正则化**（sample-based regularisers），即通过数据增强（data augmentation）在训练时随机采样群元素并施加等变约束。这类方法存在以下问题：
- **高样本复杂度**（high sample complexity），尤其对于连续群（如SO(3)）；
- 需要额外前向传播，增加训练时间和内存；
- 正则化效果不稳定（有估计方差）。

### **提出的新方法**
本文提出一种全新的**基于投影的等变正则化**（projection-based equivariance regularisation），其核心思想是：
- 利用线性层在群表示下的**正交分解**（orthogonal decomposition），将权重矩阵分解为**等变分量**（equivariant component）和**非等变分量**（non-equivariant component）；
- 在训练目标中直接惩罚非等变分量的范数，即最小化 `||T - P(T)||`，其中 `P(T)` 是 `T` 在等变子空间上的投影。

该方法的关键创新在于：
- **操作层面正则化**（operator-level regularisation）：不是逐点、逐样本地惩罚等变误差，而是从算子整体上抑制非等变性；
- **无需数据增强**：不依赖于采样群元素或输入样本，避免了额外计算开销；
- **理论可解释性强**：证明了 `||T - P(T)||` 与等变缺陷（equivariance defect）在数量级上等价（相差不超过2倍）；
- **高效实现**：在傅里叶域中可通过**掩码+平均**（masking and averaging）高效计算投影，适用于连续群（如SO(n)）。

### **相比现有方法的优势**
| 维度 | 本文方法（Projection-based） | 现有方法（Sample-based） |
|------|-------------------------------|--------------------------|
| **计算效率** | 仅需一次FFT/IFFT，成本线性于参数量 | 需多次前向传播，成本正比于样本数×群采样数 |
| **稳定性** | 无估计方差，正则项确定 | 有方差，依赖随机采样 |
| **适用性** | 支持连续群（如SO(2), SO(3)） | 对连续群采样困难，效率低 |
| **硬件利用率** | 可使用大batch训练 | 受限于显存，batch size小 |

---

## **2. 核心实验方法和设置**

### **使用的数据集**
1. **合成数据集**：
   - **SO(2)不变分类任务**：二维平面上两个同心环状点云，标签分别为+1/-1，测试模型学习旋转不变性的能力。
   - **烟雾动力学预测**（smoke advection-diffusion）：使用PhiFlow生成64×64烟雾扩散序列，人为引入对称性破坏（如非均匀浮力、不同时间步长），测试模型在**近似对称动态系统**中的泛化能力。

2. **真实医学图像数据集**：
   - **AAPM CT-MAR Grand Challenge**：14,000张带金属伪影的头部和身体CT切片，用于**金属伪影去除**（Metal Artifact Reduction, MAR）任务，测试C4（90°旋转）近似等变性。

### **实验设置和评估指标**
- **评估指标**：
  - **重建质量**：PSNR（峰值信噪比）、SSIM（结构相似性）
  - **等变性度量**：经验等变缺陷 `E_emp(T)`
  - **效率指标**：训练吞吐量（samples/GPU-s）、epoch耗时、显存占用
- **训练设置**：
  - 使用Adam优化器，学习率调度；
  - 对比固定batch（4）和最大可行batch（12）下的性能；
  - 超参数通过网格搜索调优。

### **基线方法对比**
| 方法 | 类型 |
|------|------|
| **Conv** | 普通CNN |
| **Equiv** | 严格等变CNN（如Steerable CNN） |
| **RPP** (Finzi et al., 2021) | 残差路径先验（Residual Pathway Priors） |
| **CLNN** | 显式等变损失（pointwise equivariance penalty） |
| **Bai et al. (2025)** | 样本基正则化（sample-based regulariser） |
| **Train-then-project** | 训练后投影到等变子空间 |
| **RGroup / RSteer** (Wang et al., 2022c) | 放松的群卷积网络 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **(1) CT-MAR 金属伪影去除任务（表2）**
| 方法 | PSNR | SSIM | 训练吞吐量 (batch=12) | Epoch时间 (s) |
|------|------|------|------------------------|---------------|
| ACDNet (baseline) | 42.08 | 0.9559 | 4.90 | 1108 |
| + sample-based (Bai et al.) | 40.02 | 0.9623 | 2.54 | 2011 |
| **+ projection-based (ours)** | **42.68** | **0.9620** | **4.25** | **1202** |

- **PSNR提升**：相比基线提升0.6dB，优于样本基方法（+2.66dB vs +0.6dB）；
- **吞吐量优势**：在max-feasible设置下，吞吐量比样本基方法高**54–61%**；
- **训练速度**：尽管epoch时间略长，但因支持更大batch，实际训练更高效。

#### **(2) 烟雾动力学预测（表1）**
在Rotation和Scaling设定下，加入本文正则化的RGroup和RSteer均显著优于基线：
- **Rotation-Future**：MSE从1.10降至**0.58**（RSteer+Reg）；
- **Scaling-Domain**：MSE从0.95降至**0.69**；
- 表明该方法在**部分对称系统**中能有效保留有用对称性，同时适应对称性破坏。

#### **(3) SO(2)不变性控制实验（图3, 图7）**
- 通过调节超参数 `λ_G` 和 `λ_⊥`，可**连续控制模型的等变程度**；
- 当 `λ_⊥` 增大时，决策边界趋于圆形，经验等变缺陷 `E(T)` 从 `3e+1` 降至 `1e-2`；
- 验证了正则化项能有效引导模型向等变方向收敛。

### **消融实验结果**
#### **(1) 正则化强度敏感性分析（图8, 图9）**
- 固定 `λ_⊥` 时，改变 `λ_G` 对性能影响较小；
- 增大 `λ_⊥` 显著降低等变缺陷，提升分类准确率（如MNIST上从0.975升至0.984）；
- 表明 `λ_⊥` 是控制等变性的主要超参数。

#### **(2) 范数选择的影响（表5）**
比较不同矩阵范数（spectral, Frobenius, infinity, (p,q)-norms）：
- **Frobenius范数**表现最佳，PSNR达38.48，SSIM 0.9457；
- **Spectral范数**慢10–15%，但性能未提升；
- **Infinity范数**导致性能下降（PSNR 35.61），说明过于强调极值会欠拟合；
- 结论：采用**Frobenius范数**作为默认选择。

---

## **4. 关键结论和发现**

### **主要发现**
1. **投影基正则化优于样本基方法**：
   - 在多个任务（分类、动力学预测、图像重建）上均取得更好或相当的性能；
   - 显著提升训练效率，支持更大batch size，更适合大规模训练。

2. **等变性可被连续调节**：
   - 通过超参数 `λ_⊥` 可灵活控制模型的等变程度，实现“软约束”；
   - 在对称性部分存在的场景中，模型能自动平衡“保持对称”与“拟合数据”。

3. **傅里叶域实现高效通用**：
   - 对于连续群（如SO(2)），可在频域通过**掩码+平均**高效实现投影；
   - 特别适合已参数化在不可约表示块中的模型（如steerable CNNs）。

### **方法的局限性**
- **需针对架构和群推导投影形式**：每种群和网络结构需重新设计投影算子；
- **当前实验集中在简单群**：如C4、SO(2)，尚未验证复杂李群（如SE(3)）上的效果；
- **理论假设较强**：要求表示为酉表示（unitary representations），某些非紧群不适用。

### **未来工作方向**
- 扩展到更复杂的群结构（如SE(3)、product of subgroups）；
- 探索自适应调整 `λ_⊥` 的机制，根据数据自动学习对称性程度；
- 将该框架应用于材料科学、分子动力学等需要处理多尺度对称性的领域。

---

> **总结**：本文提出的**投影基等变正则化**提供了一种理论上严谨、计算高效的近似等变学习框架，克服了传统样本基方法的效率瓶颈，在真实和合成任务上均展现出优越的性能与可扩展性，为等变深度学习的实际应用开辟了新路径。

</details>

---

### 8. [LLMs for Explainable Business Decision-Making: A Reinforcement Learning Fine-Tuning Approach](https://arxiv.org/abs/2601.04208)

**Authors**: Xiang Cheng, Wen Wang, Anindya Ghose  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.04208v1  

#### Abstract
Artificial Intelligence (AI) models increasingly drive high-stakes consumer interactions, yet their decision logic often remains opaque. Prevailing explainable AI techniques rely on post hoc numerical feature attributions, which fail to provide coherent narratives behind model decisions. Large langu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LLMs for Explainable Business Decision-Making: A Reinforcement Learning Fine-Tuning Approach

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对高风险商业决策（如贷款审批）中人工智能模型缺乏可解释性的挑战，指出当前主流的 **post hoc explainable AI** 技术（如 SHAP、LIME）存在三大缺陷：

1. **解释不忠实（Unfaithful）**：数值型特征归因是事后近似，无法反映真实决策逻辑，可能导致“看似合理但错误”的解释。
2. **多受众沟通困难**：同一决策需面向专家（如贷款审核员）和消费者提供不同风格的解释，而传统方法难以在保持决策一致性的同时调整表达方式。
3. **标注成本高昂**：依赖人工撰写高质量解释进行监督训练（supervised fine-tuning）成本极高，难以规模化。

### **提出了什么新方法或新思路**

作者提出 **LEXMA**（LLM-based EXplanations for Multi-Audience decisions），一个基于强化学习的两阶段微调框架，其核心创新包括：

- **三阶段生成架构（Reasoning → Explanation → Prediction）**  
  强制模型先生成内部推理链（Reasoning），再提炼为简洁解释（Explanation），最后做出预测（Prediction）。由于决策依赖于解释，因此优化决策准确率会反向激励模型生成更忠实于决策过程的解释。

- **双适配器架构（Dual Adapters）**  
  - **ACC Adapter**（Correctness Adapter）：负责捕捉决策边界和风险因素，用于所有场景。
  - **TONE Adapter**（Tone Adapter）：仅用于消费者端，调整语气、可读性和礼貌程度。  
  在推理时，专家解释仅激活 ACC，消费者解释则同时激活 ACC + TONE，从而实现“同一决策规则，多种表达风格”。

- **标签高效训练（Label-Efficient Training）**  
  结合：
  - **Reflection-augmented SFT**：使用强参考模型（GPT-5）自动生成带反思修正的训练样本，无需人工标注。
  - **Group Relative Policy Optimization (GRPO)**：一种无需学习奖励模型（learned reward model）的强化学习算法，通过组内比较计算优势函数，使用简单规则定义的奖励信号（如决策正确性、Flesch-Kincaid 可读性、礼貌词密度）进行优化。

### **相比现有方法的优势**

| 维度 | 传统方法（如 SHAP） | 通用 LLM（如 raw Qwen/GPT-5） | LEXMA |
|------|---------------------|-------------------------------|--------|
| 解释形式 | 数值归因 | 自然语言但可能不忠实 | 忠实且叙事性强的自然语言 |
| 多受众支持 | 不支持 | 改变 prompt 可能改变决策 | 同一决策，多风格输出 |
| 训练成本 | 需要大量人工标注解释 | 高昂（若用 SFT） | 低（无需人类评分解释） |
| 决策-解释耦合 | 弱（post hoc） | 弱（可能幻觉） | 强（解释驱动决策） |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **HMDA Loan Application Register**（Home Mortgage Disclosure Act）
  - 来源：美国消费者金融保护局（CFPB）公开数据库
  - 数据量：超过一百万条贷款申请记录（使用其中一小部分进行微调）
  - 特征：结构化字段（信用评分、LTV、DTI、收入等）+ 匿名人口统计信息
  - 标签：`Approve` 或 `Deny`
  - 预处理：排除受保护属性（种族、性别等），构建类别平衡子集（50% 批准 / 50% 拒绝）

### **实验设置**

- **基础模型**：Qwen3-4B（开源大模型）
- **微调策略**：Parameter-Efficient Fine-Tuning (PEFT) + LoRA
- **训练流程三阶段**：
  1. **SFT（Supervised Fine-Tuning）**：使用 GPT-5 生成带反思修正的目标响应，训练模型遵循三阶段格式。
  2. **GRPO-Step1**：在 ACC Adapter 上优化决策准确性，交替使用专家和消费者 prompt。
  3. **GRPO-Step2**：在 TONE Adapter 上优化消费者解释的可读性和礼貌性，冻结 ACC Adapter。

- **评估指标**
  - **决策正确性**：F1 Score、Accuracy、Precision、Recall
  - **专家解释质量**：人工评估（3 名资深贷款专员）——风险相关性（risk relevance）、决策适当性（appropriateness）、可解释性（explainability）
  - **消费者解释质量**：人工评估（110 名 Prolific 用户）——清晰度、礼貌性、公平性、可操作性、合理性、可信度、满意度（均为 1–5 分 Likert 量表）
  - **风格量化指标**：
    - 可读性：Flesch-Kincaid Grade Level（目标 ≤ 8 年级）
    - 礼貌性：Stanford Politeness Strategy 密度（经缩放至 [0,1]）

### **基线方法对比**

- **机器学习模型**：
  - XGBoost、Neural Network、Logistic Regression、Gradient Boosting
- **LLM 基线**：
  - Raw Qwen3-4B（零样本）
  - GPT-5（中等推理强度）

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ 决策正确性（F1 Score）

| 模型 | 专家提示（Expert Prompt） | 消费者提示（Consumer Prompt） |
|------|----------------------------|-------------------------------|
| XGBoost | 0.917 | 0.917 |
| Raw Qwen3-4B | 0.723 | 0.726 |
| GPT-5 | 0.730 | 0.771 |
| **LEXMA (GRPO-Step1/2)** | **0.897** | **0.893** |

> **结论**：LEXMA 显著优于原始 LLM 和 GPT-5，接近 XGBoost 性能，同时还能生成自然语言解释。

#### ✅ 专家解释质量（人工评估，n=30）

| 指标 | Raw Qwen 平均分 | LEXMA 平均分 | 差异 | p-value |
|------|------------------|---------------|------|---------|
| 风险相关性（Risk Relevance） | 3.4 | 4.4 | +1.0 | <0.05 |
| 决策适当性（Decision Appropriateness） | 2.2 | 3.9 | +1.7 | <0.01 |
| 可解释性（Explainability） | 2.4 | 3.6 | +1.2 | <0.01 |

> **结论**：专家明显偏好 LEXMA 的解释，认为其更聚焦关键风险、理由充分、易于理解。

#### ✅ 消费者解释质量（人工评估，n=536）

| 指标 | Raw Qwen 平均分 | LEXMA 平均分 | 差异 | p-value |
|------|------------------|---------------|------|---------|
| 清晰度（Clarity） | 3.835 | 4.212 | +0.377 | <0.001 |
| 礼貌性（Politeness） | 4.124 | 4.586 | +0.462 | <0.001 |
| 公平性（Fairness） | 4.180 | 4.551 | +0.371 | <0.001 |
| 可操作性（Actionability） | 3.444 | 4.052 | +0.608 | <0.001 |
| 合理性（Reasonableness） | 3.764 | 4.216 | +0.452 | <0.001 |
| 可信度（Trustworthiness） | 3.742 | 4.139 | +0.397 | <0.001 |
| 满意度（Satisfaction） | 3.692 | 4.447 | +0.755 | <0.001 |

> **结论**：消费者显著更喜欢 LEXMA 的解释，在所有维度上均有提升，尤其是可操作性和满意度。

#### ✅ 风格控制效果（量化）

| 模型 | 可读性均值（Grade） | 礼貌性密度均值 |
|------|----------------------|----------------|
| Raw Qwen | 6.866 | 0.223 |
| GRPO-Step1（仅优化决策） | 9.374 ↑ | 0.169 ↓ |
| **GRPO-Step2（+ 优化语气）** | **5.952 ↓** | **0.223 ↑** |

> **结论**：单纯优化决策会损害消费者解释的可读性和礼貌性；第二阶段 GRPO 成功将两者恢复并优化至理想水平。

### **消融实验结果**

从以下四个检查点观察性能演进：

| 检查点 | 专家 F1 | 消费者 F1 |
|--------|---------|-----------|
| Raw Qwen | 0.723 | 0.726 |
| SFT | 0.825 | 0.793 |
| GRPO-Step1 | 0.897 | 0.893 |
| GRPO-Step2 | 0.902 | 0.893 |

> **结论**：
> - SFT 显著提升基础性能；
> - GRPO-Step1 进一步大幅提升 F1；
> - GRPO-Step2 在几乎不改变决策性能的前提下，成功优化了解释风格。

---

## 4. 关键结论和发现

### **主要发现**

1. **决策与解释可以协同优化**：通过将解释置于决策之前（Explanation → Prediction），强化学习的奖励信号可反向传播至解释生成过程，迫使模型生成真正影响决策的忠实解释。
2. **模块化适配器有效分离功能**：ACC Adapter 控制决策逻辑，TONE Adapter 调整表达风格，二者解耦使得可以在不影响决策的情况下优化用户体验。
3. **无需人工标注即可提升解释质量**：利用 GRPO + 规则化奖励（如可读性公式、礼貌词匹配），可在无任何人类评分解释的情况下完成高质量微调，极具实用价值。
4. **LEXMA 在真实贷款审批任务中表现优异**：不仅决策准确率接近 XGBoost，且生成的解释在专家和消费者眼中均显著优于原始 LLM。

### **方法的局限性**

1. **数据限制**：使用的是公开 HMDA 数据集，缺少详细的信用历史和还款行为数据，可能影响模型上限。
2. **评估规模有限**：
   - 专家评估仅由 3 人完成（虽经验丰富）；
   - 消费者评估为线上问卷，非真实决策场景。
3. **泛化能力待验证**：目前仅在贷款审批场景验证，其他领域（如保险、营销推荐）需进一步测试。
4. **忠实性仍依赖间接机制**：虽然设计上鼓励忠实解释，但仍缺乏直接测量“解释是否真实反映决策过程”的手段。

### **未来工作方向**

1. **增强解释忠实性验证机制**：引入 probing methods 或 causal mediation analysis 来量化解释与决策之间的因果关系。
2. **扩展多受众支持**：支持更多角色（如监管机构、高管）的定制化解释。
3. **结合小规模人工反馈**：在预算允许下，加入少量高质量人类反馈以进一步提升性能。
4. **应用于其他高风险决策场景**：如医疗诊断辅助、招聘筛选、动态定价等需要透明性和合规性的领域。
5. **探索更复杂的 tone reward design**：如品牌语调、法律合规性检测等。

---

> **总结**：LEXMA 提出了一种**系统化、低成本、可扩展**的 LLM 微调框架，首次实现了在保持决策高性能的同时，生成**忠实、多风格、自然语言解释**的目标，为构建透明、可信的商业 AI 系统提供了重要路径。

</details>

---

### 9. [Prior-Informed Zeroth-Order Optimization with Adaptive Direction Alignment for Memory-Efficient LLM Fine-Tuning](https://arxiv.org/abs/2601.04710)

**Authors**: Feihu Jin, Shipeng Cen, Ying Tan  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.04710v1  

#### Abstract
Fine-tuning large language models (LLMs) has achieved remarkable success across various NLP tasks, but the substantial memory overhead during backpropagation remains a critical bottleneck, especially as model scales grow. Zeroth-order (ZO) optimization alleviates this issue by estimating gradients t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Prior-Informed Zeroth-Order Optimization with Adaptive Direction Alignment for Memory-Efficient LLM Fine-Tuning**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLM）在全参数微调（full fine-tuning）时面临严重的**内存瓶颈**，尤其是在反向传播过程中需要存储大量中间激活和梯度。尽管参数高效微调（PEFT）方法（如 LoRA、Prefix Tuning）缓解了部分问题，但其内存开销仍显著高于推理阶段。

Zeroth-Order (ZO) 优化通过仅使用前向传播和高斯采样估计梯度，避免了反向传播，从而大幅降低内存占用。然而，传统 ZO 方法（如 MeZO）依赖**随机高斯扰动**，导致梯度估计方差大、收敛慢、性能不稳定。

### ✅ 提出的新方法与创新思路
本文提出两种基于“先验信息”的零阶优化策略，统称为 **Guiding Vector-Augmented Zeroth-Order (GV-ZO)** 框架：

1. **MeZO-GV（Guiding Vector）**  
   - 在每次迭代中，通过多次随机扰动（M次）评估损失，选出表现最好的“精英”扰动组和最差的“非精英”组。
   - 构造一个**引导向量（guiding vector）** $ v = \sum_{\text{top}} z_i - \sum_{\text{bottom}} z_i $，该向量隐含了当前损失曲面的有利更新方向。
   - 使用该向量进行定向扰动，估计梯度：  
     $$
     \nabla L(\theta; B) \approx \frac{L(\theta + \epsilon v; B) - L(\theta - \epsilon v; B)}{2\epsilon} \cdot v
     $$

2. **MeZO-Greedy（Greedy Perturbation）**  
   - 同样生成 M 个候选扰动，选择使损失最小的那个方向 $ z^* $。
   - 使用 $ z^* $ 进行对称差分估计，作为梯度方向。

这两种方法都属于**插件式（plug-and-play）设计**，可无缝集成到 MeZO 或 SubZero 等现有 ZO 框架中。

### ✅ 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **收敛速度** | 显著加快，可在更少训练步数下达到甚至超越基线性能（如 10k 步超越 20k 步的 MeZO）。 |
| **性能表现** | 在多个任务上优于传统 ZO 方法，并在多数任务上超过梯度法（Adam）基线。 |
| **内存效率** | 保持 ZO 的低内存特性，无需存储激活值，适合资源受限环境。 |
| **理论支持** | 证明所提方法能实现更强的**梯度方向对齐（directional alignment）**，提升单步下降效果。 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
实验基于 **SuperGLUE** 基准中的多个任务，涵盖三类任务：
- **分类任务（Classification）**: SST-2, RTE, CB, BoolQ, WSC, WIC
- **多项选择任务（Multiple Choice）**: COPA, ReCoRD
- **问答任务（QA）**: SQuAD, DROP

### ⚙️ 实验设置
- **模型规模**：覆盖从小到大的多种 LLM：
  - OPT 系列：OPT-1.3B, OPT-13B, OPT-30B
  - Llama2 系列：Llama2-7B-hf, Llama2-13B-hf
- **微调方式**：测试三种主流方案：
  - Full Tuning (FT)
  - LoRA
  - Prefix Tuning
- **超参数配置**：
  - 批大小（batch size）：统一为 16
  - 学习率：根据模型和任务调整（见 Table III）
  - 引导向量采样次数 $ M $：设为 2 或 4
  - 每轮梯度估计的查询预算（query budget）：99（即每步最多 M+2 次前向）
- **硬件平台**：Nvidia A100 (80GB) 或 3090 (24GB)

### 📊 评估指标
- 主要指标：**准确率（Accuracy）** 或 F1 分数（如 SQuAD）
- 辅助分析指标：
  - 收敛速度（wall-clock time / training steps）
  - 内存占用（GPU memory usage）
  - 方向对齐度（cosine similarity between estimated and true gradient）
  - 单步下降幅度分析

### 🔁 基线方法对比
| 类型 | 对比方法 |
|------|----------|
| **Zero-Shot & ICL** | Zero-shot, In-Context Learning |
| **Gradient-Based** | Adam-based full fine-tuning (FT) |
| **ZO Baselines** | MeZO, SubZero |
| **Advanced ZO** | ZO-AdaMU, HiZOO |
| **其他变体** | n-SPSA (multi-query SPSA) |

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据

#### ✅ 在 OPT-13B 上的表现（Table V）
- **所有 11 项任务中，GV-based 方法均优于传统 ZO 方法**
- **在 9/11 任务上超越梯度法（Adam）基线**
- 具体亮点：
  - **SST-2 (FT)**: SubZero-GV 达到 **94.7%**，超过 MeZO(FT) 的 91.4%
  - **CB (Prefix)**: SubZero-GV(Prefix) 达到 **85.7%**，领先 ZO-AdaMU(Prefix) 高达 **13.4%**
  - **SQuAD (LoRA)**: SubZero-GV(LoRA) 达到 **85.3%**，优于 MeZO(LoRA) 的 83.8%
  - **DROP (LoRA)**: MeZO-GV(LoRA) 达到 **32.7%**，提升明显

#### ✅ 在 Llama2-7B 和 Llama2-13B 上的结果（Tables VI & VII）
- **MeZO-GV-10k（仅 10k 步）在多个任务上超越 MeZO-20k（20k 步）**
  - 如 Llama2-7B 上 SST-2：**90.4% vs 88.7%**
  - Llama2-13B 上 SST-2：**93.7% vs 94.3%**（接近且节省一半时间）
- 表明 GV 方法显著加速收敛

#### ✅ 在 OPT-30B 上的结果（Table VIII）
- MeZO-GV(prefix) 在 SST-2 上达到 **91.4%**，优于 MeZO(prefix) 的 87.5% 和 SubZero(prefix) 的 89.3%
- 显示方法具有良好的**可扩展性**，适用于超大规模模型

### 📊 与基线方法的对比结果
| 对比维度 | 结果 |
|--------|------|
| **vs MeZO / SubZero** | GV 变体在所有任务上一致胜出，平均提升 2–5% |
| **vs Adam (FT)** | 在 9/11 任务上更优，尤其在复杂推理任务（如 WSC, MultiRC）中表现突出 |
| **vs ZO-AdaMU / HiZOO** | 性能全面领先，且无需额外内存开销（HiZOO 需 Hessian 估计） |
| **vs n-SPSA** | 在相同查询次数下，MeZO-GV 收敛更快、精度更高、训练时间更短（Table X） |

### 🔍 消融实验与分析
- **方向对齐分析（Figure 6）**：
  - MeZO-GV 的估计梯度与真实梯度（via SGD）之间的 **cosine similarity 更高**，验证了“方向对齐增强”的假设。
- **单步下降分析（Figure 3）**：
  - GV 方法每一步的损失下降更快，说明其梯度估计更有效。
- **不同评估次数的影响（Figure 5）**：
  - 多数任务对评估次数不敏感，但 WIC 等任务随 $ M $ 增加而持续提升，建议根据任务动态调整。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **先验引导扰动显著提升 ZO 效率**：通过从历史扰动中提取“有用方向”，可以大幅提升梯度估计质量，解决传统 ZO 收敛慢的问题。
2. **GV 方法兼具高效性与高性能**：在保持 ZO 低内存优势的同时，实现了比标准 ZO 更快的收敛和更高的最终性能。
3. **广泛适用性与可扩展性**：方法在不同模型架构（OPT、Llama2）、不同规模（1.3B 到 30B）、不同微调范式（FT、LoRA、Prefix）下均有效。
4. **部分任务超越梯度法**：在 9/11 个任务上优于 Adam 基线，表明 ZO 不再只是“退而求其次”的替代方案，而是具备竞争力的主流选择。

### ⚠️ 方法的局限性
- **额外计算开销**：每步需进行 $ M $ 次额外前向传播来构建引导向量（$ M=4 $ 时增加约 4× 查询），可能影响吞吐量。
- **对超参数敏感**：如 $ M $、split ratio $ \alpha $ 需合理设置，否则可能引入噪声。
- **理论近似性**：虽然证明了方向对齐增强，但仍为近似估计，无法完全消除偏差。

### 🔮 未来工作方向
- 探索更高效的引导向量构造方式（如动量累积、跨步复用）
- 将 GV 思路应用于其他黑盒优化场景（如 prompt tuning、NAS）
- 动态调整 $ M $ 或采用自适应采样策略以平衡效率与精度
- 结合分布式训练进一步提升大规模应用能力

---

## 总结
本文提出的 **GV-ZO 框架**（包括 MeZO-GV 和 MeZO-Greedy）通过引入**先验信息指导扰动方向**，解决了传统 Zeroth-Order 优化中梯度估计方差大、收敛慢的核心问题。实验证明，该方法不仅在多种 LLM 和任务上实现了**更快收敛和更高性能**，而且在部分任务上**超越了梯度法基线**，同时保持了 ZO 固有的**低内存优势**。这一成果推动了无反向传播微调技术的发展，为资源受限环境下的 LLM 高效适配提供了强有力的新工具。

</details>

---

### 10. [GPU-Accelerated INT8 Quantization for KV Cache Compression in Large Language Models](https://arxiv.org/abs/2601.04719)

**Authors**: Maanas Taneja, Purab Shingvi  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2601.04719v1  

#### Abstract
The key-value (KV) cache in large language models presents a significant memory bottleneck during inference, growing linearly with sequence length and often exceeding the memory footprint of model weights themselves. We implement and evaluate GPU-accelerated INT8 quantization for KV cache compressio...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GPU-Accelerated INT8 Quantization for KV Cache Compression in Large Language Models

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLM）在推理过程中面临严重的内存瓶颈，尤其是 **Key-Value (KV) Cache** 的内存消耗。随着序列长度增长，KV Cache 的大小呈线性增加，在长上下文场景下（如 32K 或 128K tokens），其内存占用甚至超过模型权重本身，限制了批处理大小、上下文长度，并提高了部署成本。

### 提出的新方法与创新点
本文提出并系统实现了 **GPU 加速的 per-channel INT8 量化方法**，用于压缩 LLM 推理中的 KV Cache，主要贡献如下：

- **高效实现方案**：设计并实现了四种 CUDA kernel 变体（naive、tiled、coarsened、vectorized），探索不同优化策略对性能的影响。
- **系统性评估框架**：不仅关注端到端精度，还深入分析了量化操作本身的 **kernel-level 性能特性** 和误差传播机制。
- **揭示硬件优化规律**：通过实验证明，在此类内存密集型任务中，**vectorization 是最有效的优化手段**，而传统共享内存 tiling 效果有限。

### 相比现有方法的优势
| 方面 | 现有工作 | 本论文优势 |
|------|--------|-----------|
| **优化粒度** | 多数研究聚焦于模型权重或激活值的整体压缩（如 LLM.int8()） | 聚焦于 **KV Cache 这一特定瓶颈组件**，更具针对性 |
| **实现细节透明度** | 商业推理系统（如 TensorRT-LLM）常将量化视为黑盒 | 提供 **完整的 kernel 实现与性能剖析**，指导实际工程落地 |
| **性能优化洞察** | 缺乏对不同 GPU 优化技术在 KV 量化上的对比 | 明确指出 **vectorization 最优，tiled 无显著收益**，避免误用优化 |

---

## 2. 核心实验方法和设置

### 数据集
本研究为 **合成基准测试（synthetic benchmarking）**，未使用真实文本数据集。所有实验基于人工生成的 FP32 矩阵模拟 KV Cache 数据，覆盖多种典型维度组合。

#### 测试配置（Table 3）
| Test Case | Tokens (T) | Head Dim (D) | 描述 |
|----------|------------|--------------|------|
| Small | 2,048 | 128 | 微小测试 |
| Medium | 16,384 | 256 | 开发调试 |
| Large ~ Very Large | 65K ~ 131K | 256 | 长序列场景 |
| Realistic Small ~ V. Large | 131,072 | 1K ~ 8K | 真实 LLM 工作负载（最大达 10 亿元素） |

> 注：`T=131,072` 对应超长上下文（约 128K tokens），`D=8192` 对应宽头模型（如某些大模型 attention head dimension）。

### 实验设置
- **硬件平台**：
  - GPU: NVIDIA Tesla T4 (16GB GDDR6)
  - CPU: 2× Intel Xeon Gold 6148 (共 40 核)
  - CUDA 版本：12.0
- **量化方式**：**per-channel INT8 量化**，每个 head 维度独立计算 scale：
  $$
  s_d = \frac{\max_t |K_{t,d}|}{127}
  $$
- **Kernel 实现**：
  - CPU baseline（单线程参考实现）
  - 四种 CUDA kernel：
    1. `naive`: 1 thread per element
    2. `tiled`: 使用 shared memory 缓存 scales
    3. `coarsened`: 每个线程处理多个元素
    4. `vectorized`: 使用 `float4`/`char4` 向量指令批量读写

### 评估指标
| 指标 | 定义 | 目的 |
|------|------|------|
| **Performance** | 量化/反量化耗时，GPU 相对于 CPU 的 speedup | 衡量计算效率 |
| **L2 Reconstruction Error** | $\|K - \hat{K}\|_2$ | 评估重建精度损失 |
| **Max Absolute Error** | $\max |K_{t,d} - \hat{K}_{t,d}|$ | 评估最大偏差 |
| **Attention Score Error** | $\text{mean} |\text{dot}(Q,K) - \text{dot}(Q,\hat{K})|$ | 评估对下游 attention 计算的影响 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Figure 1–3）

| 指标 | 结果 |
|------|------|
| **最大加速比** | **1,694×**（vs CPU baseline，realistic very large 配置） |
| **GPU 执行时间** | 所有 realistic workload 下均在 **6–58 ms** 内完成 |
| **内存压缩率** | **4× reduction**（FP32 → INT8） |
| **最大绝对误差** | 恒定为 **0.00394**，符合理论上限 $1/(2×127)$ |
| **Attention Score Error** | 即使在 D=8192 时也仅 **0.095**，远小于 softmax 前的 score 范围 |

### 与基线方法对比
| Kernel | vs CPU Speedup（最大） | vs Naive GPU |
|-------|------------------------|-------------|
| CPU Baseline | 1× | — |
| Naive CUDA | ~1,000× | 1× |
| Tiled CUDA | ~950× | ≈0.95×（略慢） |
| Coarsened CUDA | ~1,200× | ~1.2× |
| **Vectorized CUDA** | **~1,694×** | **~1.7× 更快** |

> ⚠️ **关键观察**：尽管 `tiled` kernel 使用 shared memory 减少 scale 重复加载，但由于 scales 数组较小且可驻留 L2 cache，**并未带来性能提升，反而因同步开销略慢于 naive 实现**。

### 消融实验结果
- **Problem Size Scaling**（Figure 5）：
  - 所有 GPU kernels 的 speedup 随矩阵规模增大而上升，说明能有效摊销 kernel launch overhead。
- **Kernel Design Impact**：
  - `vectorized` 在所有规模下表现最佳，因其直接提升了 memory bandwidth 利用率。
  - `coarsened` 有一定改进（减少 loop 开销），但受限于 memory-bound 特性，增益有限。
  - `tiled` 无明显优势，表明 **shared memory 并非万能优化**，需视访问模式而定。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **INT8 量化是实用且高效的 KV Cache 压缩方案**：
   - 实现 **4× 内存节省**，显著缓解 LLM 推理内存压力。
   - 引入的数值误差极小（max error < 0.004），对 attention score 影响可忽略（< 0.1）。

2. 🚀 **GPU 加速至关重要**：
   - 最大任务从 CPU 的 **79 秒降至 GPU 的 46 毫秒**，加速比高达 **1,694×**。
   - 量化开销（6–58ms）远低于实际 attention 计算时间，**几乎不影响整体吞吐**。

3. 💡 **优化策略有效性排序：`vectorization > coarsening > tiling ≈ naive`**：
   - KV cache 量化是典型的 **memory-bound、低算术强度** 操作。
   - 一旦实现良好 memory coalescing，进一步优化应聚焦于 **减少内存事务数量**，而非引入复杂同步。
   - **向量化（vectorized memory access）是最有效的优化路径**。

4. 🔍 **Shared Memory 不一定有益**：
   - 在 scales 小且可缓存的情况下，显式使用 shared memory 并不能提升性能，反而可能因 `_syncthreads()` 带来轻微开销。

### 方法的局限性
- ❌ **未进行端到端任务评估**：仅测量 reconstruction error 和 attention score error，**未报告 perplexity、QA 准确率等下游任务指标**。
- ❌ **仅支持 INT8**：未探索更低比特（如 INT4、INT2）或混合精度策略。
- ❌ **单 GPU 设计**：不支持 multi-GPU 或分布式推理场景。
- ❌ **静态 scale**：采用 per-channel 最大值缩放，缺乏动态或学习式量化机制应对异常激活。

### 未来工作方向
1. **集成至主流推理引擎**：将 kernel 集成进 vLLM、TensorRT-LLM 等系统，进行真实 workload 下的端到端性能与精度评估。
2. **探索更低位宽与新型格式**：
   - 尝试 **KIVI（2-bit asymmetric）** 或 **KVQuant（sub-4-bit）** 等极端量化方法。
   - 利用 **FP8** 等新兴格式，结合硬件原生支持提升效率。
3. **优化 scale computation**：
   - 使用 warp-level primitives（如 `__shfl_down_sync`）加速最大值归约。
   - 探索近似最大值估计以降低开销。
4. **动态与自适应量化**：根据输入分布动态调整 scale，提升鲁棒性。
5. **多 GPU 扩展**：设计跨设备的量化/反量化流水线，适配大规模分布式推理。

---

> ✅ **总结一句话**：  
> 本文证明了 **GPU-accelerated per-channel INT8 量化** 是一种高效、低误差、低开销的 KV Cache 压缩方案，其中 **vectorized CUDA kernel 实现了高达 1,694× 的加速**，为生产级 LLM 推理系统的内存优化提供了切实可行的技术路径。

</details>

---

### 11. [ReEfBench: Quantifying the Reasoning Efficiency of LLMs](https://arxiv.org/abs/2601.03550)

**Authors**: Zhizhang Fu, Yuancheng Gu, Chenkai Hu, Hanmeng Liu, Yue Zhang  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03550v1  

#### Abstract
Test-time scaling has enabled Large Language Models (LLMs) to tackle complex reasoning, yet the limitations of current Chain-of-Thought (CoT) evaluation obscures whether performance gains stem from genuine reasoning or mere verbosity. To address this, (1) we propose a novel neuro-symbolic framework ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ReEfBench: Quantifying the Reasoning Efficiency of LLMs 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前对大语言模型（LLMs）推理能力的评估主要依赖 **Chain-of-Thought (CoT)** 输出长度或最终答案准确性，存在以下问题：
- **无法区分“真实推理”与“冗余生成”**：模型可能通过“过度思考”（overthinking）生成大量无意义的中间步骤来提升表现，而非真正进行深度逻辑推理。
- **缺乏过程导向的量化评估框架**：现有方法难以系统衡量推理路径的效率、逻辑深度和认知行为。

### 提出的新方法与思路
作者提出 **ReEfBench** ——一个基于 **神经符号系统（neuro-symbolic）** 的非侵入式、过程中心化的推理效率评估框架，其核心思想是将推理效率形式化为：

$$
P = \frac{W}{t}
$$
其中：
- $W$：**Logical Depth**（逻辑深度），表示完成任务所需的抽象逻辑步数；
- $t$：**Computational Cost**（计算消耗），如生成的 token 数量；
- $P$：**Reasoning Efficiency**（推理效率），即单位计算成本下的逻辑增益。

#### 框架流程（如图1所示）
1. **Phase A：可扩展数据生成**  
   构建基于 **First-Order Logic (FOL)** 的可控、可扩展的逻辑推理数据集，确保每个问题有明确的逻辑深度定义。
2. **Phase B：LLM 响应获取**  
   向目标 LLM 输入问题并收集其 CoT 推理过程。
3. **Phase C：响应解析**  
   使用小型 LLM 将自然语言推理链分解为结构化节点（如 `actual`, `planning`, `reflection`）。
4. **Phase D：规则驱动的节点处理**  
   利用符号逻辑规则验证节点正确性，并计算每个节点的逻辑深度。
5. **Phase E：行为指标聚合**  
   综合六个维度的诊断指标，识别模型的行为原型。

### 相比现有方法的优势
| 特性 | ReEfBench | 其他方法（如 ProntoQA, Roscoe） |
|------|-----------|-------------------------------|
| **Scalable** | ✅ 支持复杂度可控的数据生成 | ❌ 数据规模有限或不可控 |
| **FOL 基础** | ✅ 形式化逻辑保证客观验证 | ⚠️ 部分支持或不支持 |
| **Logic-Only** | ✅ 脱钩知识与逻辑，专注纯推理 | ❌ 易受先验知识干扰 |
| **LogDepth 可测** | ✅ 明确定义并可自动计算 | ❌ 多为二值有效性判断 |
| **BehProc 分析** | ✅ 支持探索、反思等认知行为分析 | ❌ 缺乏细粒度过程追踪 |
| **Non-Intrusive** | ✅ 不强制输出格式 | ❌ 如 ZebraLogic 强制结构化输出 |

> ✅ 表示支持，⚠️ 表示部分支持，❌ 表示不支持

---

## 2. 核心实验方法和设置

### 数据集
- **自研数据集 ReEfBench**：
  - 基于 **First-Order Logic (FOL)** 和 **Modus Ponens** 推理规则构建。
  - 包含 **前提（Premises）**、**结论（Conclusion）** 和 **黄金解法路径**。
  - 复杂度等级从 3 到 11，每级 100 个样本，共 900 个测试实例。
  - 每个样本额外加入 $C$ 个无关前提作为干扰项（distractors），增加难度。

### 实验设置
- **评估模型**：共评测 **25 个主流 LLMs**，包括：
  - 商业闭源模型：`Claude-Opus-4.5`, `Claude-Sonnet-4.5`
  - 开源模型：`Qwen3-*`, `DeepSeek-R1`, `DS-R1-Distill-Qwen-*`
- **推理模式区分**：
  - `.long`：启用 thinking / CoT 模式的长链推理
  - `.short`：标准指令微调模式
- **API 参数**：`temperature=0`, 最大生成 token 数分别为 24k（推理模式）和 8k（非推理模式）

### 评估指标（六大维度）
所有指标经 max-normalization 映射至 [0,1] 区间后加权平均：

| 指标 | 含义 | 子指标 |
|------|------|--------|
| **Logical Depth ($S_{ld}$)** | 推理能力 | 最大正确逻辑深度 |
| **Cost ($S_{cost}$)** | 计算消耗 | 总 token 数、反思/规划步数 |
| **Exploration ($S_{exp}$)** | 探索广度 | 正确且唯一的逻辑节点数量 |
| **Efficiency ($S_{eff}$)** | 推理效率 | token 效率、有效跨度（effective span） |
| **Coherence ($S_{coh}$)** | 连贯性 | 规划/反思是否带来实际进展 |
| **Redundancy ($S_{red}$)** | 冗余度 | 句子/节点重复率 |

此外还使用原始指标辅助分析，如 **平均逻辑深度**、**token 数（千）** 等。

### 基线方法对比
本文未直接对比传统 CoT 评估方法（因其不具备过程分析能力），而是通过以下方式体现优势：
- 对比不同训练策略（Long vs. Short CoT）
- 对比不同模型规模下的蒸馏效果
- 分析混合训练的影响

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 2 & Figure 3）

| 模型 | 类别 | 平均逻辑深度 | Token 数 (k) | $S_{ld}$ | $S_{cost}$ | $S_{eff}$ |
|------|------|---------------|----------------|----------|------------|-----------|
| Qwen3-235B-thinking | DeepWanderer | 10.54 | 16.8 | 1.00 | 0.88 | 0.47 |
| Claude-Opus-4.5.long | EffectiveSolver | 10.27 | 3.5 | 0.97 | 0.37 | 0.60 |
| QwQ-32B | HollowMimic | 7.12 | 5.7 | 0.68 | 0.61 | 0.48 |
| Qwen2.5-32B-Instruct | LazyGuesser | 2.90 | 0.7 | 0.28 | 0.16 | 0.55 |

> 注：最大逻辑深度为 11。

### 四类行为原型（Behavioral Prototypes）

| 类型 | 特征 | 示例模型 |
|------|------|---------|
| **DeepWanderer** | 高成本、高探索、低效率 | Qwen3-235B-thinking |
| **EffectiveSolver** | 高效率、精准推理、低成本 | Claude-Opus-4.5.long |
| **HollowMimic** | 高成本但浅层推理，表现为“表演性推理” | DS-R1-Qwen-7B, QwQ-32B |
| **LazyGuesser** | 成本极低，推理停滞，常陷入重复 | Qwen2.5-32B-Instruct |

### 与基线方法的对比结果
- **Long CoT vs. Short CoT**（Table 3）：
  - 早期 Long CoT 模型（如 QwQ-32B）相比 Short CoT（Qwen2.5-32B-Instruct）逻辑深度提升 **+97.1%**。
  - 当前先进 Short CoT 模型（如 Qwen3-235B-Instruct）已接近 Long CoT 表现，差距仅 **+1.6%**。
  - 结论：**Short CoT + Reflection** 可实现高效深度推理。

- **Distillation 蒸馏实验**（Figure 5 & 10）：
  - 小模型（4B/8B）在蒸馏后能模仿教师模型（32B）的 **反思频率**，甚至更高。
  - 但 **反思质量随参数减少而下降**，无法转化为更深的逻辑推理。
  - 仅 **14B 模型** 能同时保持行为与能力对齐。
  - 结论：**容量不足导致“行为模仿”而非“能力复制”**。

- **Mixed Training 混合训练影响**（Figure 9）：
  - 单独训练 Long CoT 模型（如 Qwen3-235B-thinking）表现出 **Adaptive Scaling**（随复杂度增加 token 数）。
  - 混合训练模型（如 Qwen3-235B.long）出现 **Premature Saturation/Collapse**，无法随任务变难而扩展推理。
  - 结论：**混合 Long/Short CoT 数据会破坏高消耗推理策略**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **扩展 token 生成不是深度推理的必要条件**  
   高效模型（如 Claude-Opus-4.5.long）能在较少 token 下达到接近最大逻辑深度，挑战了“越长越好”的假设。

2. ✅ **存在两种成功推理路径**：
   - **High-t Strategy (DeepWanderer)**：高成本、高探索，适合资源充足场景。
   - **High-P Strategy (EffectiveSolver)**：高效率、精准推导，更适合部署。

3. ✅ **Distillation 存在根本限制**  
   小模型可以模仿大模型的 **行为形式**（如反思句式），但由于 **内在容量限制**，无法复现其逻辑效能，导致“**Diluted Expansion**”。

4. ✅ **Mixed Training 有害**  
   在同一模型中混合 Long CoT 与 Short CoT 数据会导致策略冲突，引发 **过早饱和或崩溃（Saturation/Collapse）**。

5. ✅ **Reflection 是关键机制**  
   成功的 Short CoT 模型普遍具备高质量的反思行为（Table 4），即使在短链中也能维持推理连贯性。

### 方法的局限性
1. **领域受限于 FOL**：目前仅适用于形式化逻辑推理任务，不涵盖开放问答、数学证明等更广泛场景。
2. **非侵入式设计限制**：只能分析显式生成的内容，无法捕捉隐式思维过程。
3. **分类边界模糊**：四大原型是统计聚类结果，个体模型可能处于过渡状态。

### 未来工作方向
- 扩展 ReEfBench 至更多推理范式（如数学、程序合成）。
- 设计能够引导模型进入“EffectiveSolver”模式的训练方法。
- 探索轻量级模型如何突破容量瓶颈，实现真正的逻辑迁移。
- 将 ReEfBench 用于自动化模型调试与训练信号优化。

---

> 🔗 **项目开源地址**：[anonymous.4open.science/r/LoG-1AD8/](https://anonymous.4open.science/r/LoG-1AD8/)  
> 📄 **论文链接**：[arXiv:2601.03550](https://arxiv.org/abs/2601.03550)

</details>

---

### 12. [Anti-Length Shift: Dynamic Outlier Truncation for Training Efficient Reasoning Models](https://arxiv.org/abs/2601.03969)

**Authors**: Wei Wu, Liyi Chen, Congxi Xiao, Tianfu Wang, Qimeng Wang, Chengqiang Lu, Yan Gao, Yi Wu, Yao Hu, Hui Xiong  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.03969v1  

#### Abstract
Large reasoning models enhanced by reinforcement learning with verifiable rewards have achieved significant performance gains by extending their chain-of-thought. However, this paradigm incurs substantial deployment costs as models often exhibit excessive verbosity on simple queries. Existing effici...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Anti-Length Shift: Dynamic Outlier Truncation for Training Efficient Reasoning Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对当前大型推理模型（reasoning models）在部署中存在“过度冗长”（overthinking）的问题。尽管通过强化学习（RL）结合可验证奖励（RLVR）显著提升了模型的推理能力，但这些模型在处理简单问题时也倾向于生成过长的、不必要的推理链（Chain-of-Thought, CoT），导致高昂的推理成本。

作者指出，现有方法通常通过显式的长度惩罚（length penalty）来压缩响应长度，但这会引入**优化冲突**：长度最小化与准确性最大化的目标可能相互矛盾，从而影响收敛并抑制探索行为。更重要的是，这些方法仅从结果层面惩罚冗余，而未深入探究其生成机制。

为此，论文提出了一个核心概念：**长度偏移（length shift）** —— 即在训练过程中，即使对于已经能正确解答的问题，模型也会逐渐生成越来越长的响应，这是一种由策略全局变化引起的系统性冗余。

---

### 提出的新方法：Dynamic Outlier Truncation (DOT)

为解决上述问题，论文提出 **Dynamic Outlier Truncation (DOT)**，一种基于训练时干预的动态离群值截断方法。其核心思想是：

- **选择性地抑制冗余**：只对那些**所有采样响应都正确的 rollout 组**（all-correct groups）进行干预。
- **基于统计识别异常值**：在每个全正确组内，计算响应长度的均值 $\mu_L$ 和标准差 $\sigma_L$，定义截断阈值 $T = \mu_L + \alpha \cdot \sigma_L$（如 $\alpha=3$ 对应“三西格玛”原则），仅截断超出此阈值的极端长尾样本。
- **保留复杂问题的长程推理能力**：由于仅作用于已完全解决的问题组，且依赖后验统计量，因此不会限制模型对困难问题的探索和长推理需求。
- **避免奖励博弈（reward hacking）**：该方法不修改奖励函数本身，而是作为 rollout 后处理步骤，减少了模型通过操纵奖励信号规避惩罚的风险。

此外，为了稳定训练过程，DOT 还结合了两个辅助技术：
- **KL-Cov 正则化**：防止因频繁截断导致策略熵快速崩溃（entropy collapse），维持探索能力。
- **Predictive Dynamic Sampling**：动态调整采样策略，确保有效训练批次的稳定性，提升训练效率。

---

### 相比现有方法的优势
| 方面 | 现有方法（如 SIRI, DAST, Laser） | DOT 方法 |
|------|-------------------------------|---------|
| **优化目标** | 显式将长度纳入奖励函数，易引发梯度冲突 | 不修改奖励，仅后处理截断，避免优化冲突 |
| **干预粒度** | 全局或按难度分层施加长度约束 | 仅针对“全正确组”中的统计异常值，高度精准 |
| **探索保护** | 可能抑制对复杂问题的有效探索 | 严格隔离简单与困难问题，保障长程推理 |
| **机制理解** | 多关注“如何压缩”，较少分析“为何冗余” | 揭示了“长度偏移”现象及其成因（如 reasoning words 过度触发） |

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：`DeepScaleR-Preview`（Luo et al., 2025b）
- **评估基准**：
  - 数学推理：`AIME-24`, `AIME-25`, `AMC`（AMC-22 & AMC-23）, `MATH-500`
  - 代码生成：`HumanEval`, `LiveCodeBench`

### 实验设置
- **基础模型**：`DeepSeek-R1-Distill-Qwen` 系列，涵盖 1.5B、7B 和 32B 三种规模。
- **训练框架**：基于 `GRPO`（Group Relative Policy Optimization）的 RL 流程。
- **解码参数**：温度 $t=0.6$, top_p=0.95, top_k=20，最大生成长度 32,768 tokens。
- **评估方式**：每题采样 32 条响应，报告平均 `pass@1` 准确率和生成长度（token 数）。
- **实现工具**：`verl`, `FSDP`, `SGLang`。

### 基线方法对比
论文对比了多种前沿高效推理方法，包括：
- **显式长度惩罚**：`O1-Pruner`, `Laser-DE`, `LC-R1`
- **难度感知压缩**：`DAST`, `DLER-R1`
- **模式切换/调度**：`AdaptThink`, `SIRI-low/high`
- **其他**：`OverThink`, `DeepScaleR-Preview`

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 1.5B 模型为例，AIME-24 上）

| 方法 | Acc (%) | Length (tokens) |
|------|--------|----------------|
| Original | 30.0 | 15,498 |
| SIRI-high | 43.6 | 10,049 |
| **DOT-4K** | **43.1** | **3,342** |
| **DOT-8K** | **52.2** | **5,151** |

- **DOT-8K** 在 AIME-24 上将准确率从原始模型的 30.0% 提升至 **52.2%**，同时将平均响应长度减少 **超过 66%**。
- **DOT-4K** 在几乎持平 SIRI-high 准确率的情况下，仅使用其 **约 1/3 的 token 预算**（3,342 vs 10,049）。

### 与其他方法的对比优势
- **超越帕累托前沿**：DOT 显著向外推动了效率-性能的帕累托边界（Pareto frontier），实现了**精度与效率的同时提升**。
- **优于 DLER-R1**：虽然 DLER 也避免显式长度奖励，但 DOT-4K 在同等极短长度下仍能进一步提升准确率 **7.3%**。
- **优于 SIRI**：相比当前最优方法 SIRI-high，DOT-8K 在 AIME-24 上实现 **8.6% 的绝对精度增益**，同时仅消耗一半的推理预算。
- **跨尺度泛化性强**：在 7B 和 32B 模型上均取得一致性的性能提升，表明方法具有良好的可扩展性。

### 消融实验结果（Table 3）
在 `DeepSeek-R1-Distill-Qwen-1.5B` 上的消融研究显示：

| 变体 | Acc (%) | Length (tokens) |
|------|--------|----------------|
| **DOT-8K (完整)** | **52.2** | **5,151** |
| w/o Dynamic Outlier Truncation | 51.5 | 6,879 |
| w/o Group-Conditional Truncation | 47.8 | 5,071 |
| w/o KL-Cov | 47.9 | 6,057 |
| w/o Predictive Dynamic Sampling | 48.9 | 5,637 |

- 移除 **Dynamic Outlier Truncation** 导致长度显著增加，说明其是控制冗余的关键。
- 移除 **Group-Conditional Truncation**（即无条件截断）会导致性能下降，证明了“仅在全正确组中截断”的设计至关重要。
- 移除 **KL-Cov** 或 **Predictive Dynamic Sampling** 均导致精度和效率双双下降，验证了它们对训练稳定性的必要性。

---

## 4. 关键结论和发现

### 主要发现
1. **长度偏移（Length Shift）是冗余的根本原因**：模型在训练中对简单问题产生更长响应，并非因为需要提高准确性，而是由于共享策略下，处理不确定性所需的“推理词”（reasoning words，如 "however", "wait", "verify"）被过度激活所致。
2. **DOT 能有效纠正这一偏移**：通过仅在全正确组中截断统计异常值，DOT 成功抑制了冗余，同时保留了对复杂问题的探索能力。
3. **极小干预带来全局收益**：DOT 仅影响约 **0.5% 的响应**，却能驱动整个策略向更高效的分布演化，体现了其机制上的高效性。
4. **无需牺牲精度即可大幅提升效率**：DOT 实现了“自适应推理”——对简单问题大幅剪枝，对复杂问题保留长链，从而在多个基准上同时提升准确率和降低延迟。

### 方法的局限性
1. **依赖高质量初始策略和训练数据**：作为训练时干预方法，DOT 的效果受限于初始策略的质量。在已高度优化的顶尖模型（如 DeepSeek-V3.2, Qwen3-235B）上应用面临挑战，因其在开源数据上熵值极低，难以提取进一步增益。
2. **尚未应用于智能体任务**：目前仅针对语言模型的文本生成，未扩展到 agent 场景下的动作序列冗余（如重复调用工具或循环规划）。

### 未来工作方向
- 将 DOT 扩展至 **agent 轨迹优化**，用于修剪冗余的动作空间。
- 结合 **test-time reinforcement learning (TTRL)** 或内部高质量数据，在更大规模模型上进一步释放潜力。
- 探索更精细的 reasoning words 动态调控机制，实现更细粒度的推理控制。

--- 

> **总结**：本文提出的 **DOT** 方法通过揭示并干预“长度偏移”现象，提供了一种简洁、鲁棒且可扩展的训练范式，显著提升了推理模型的效率-性能权衡，为构建下一代高效推理系统提供了重要思路。

</details>

---

### 13. [ARREST: Adversarial Resilient Regulation Enhancing Safety and Truth in Large Language Models](https://arxiv.org/abs/2601.04394)

**Authors**: Sharanya Dasgupta, Arkaprabha Basu, Sujoy Nath, Swagatam Das  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04394v1  

#### Abstract
Human cognition, driven by complex neurochemical processes, oscillates between imagination and reality and learns to self-correct whenever such subtle drifts lead to hallucinations or unsafe associations. In recent years, LLMs have demonstrated remarkable performance in a wide range of tasks. Howeve...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ARREST: Adversarial Resilient Regulation Enhancing Safety and Truth in Large Language Models

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）在实际应用中面临两大核心挑战：
- **事实性错误（Hallucination）**：生成看似合理但不真实的内容。
- **安全性失效（Safety Failure）**：在面对恶意提示（如 jailbreak）时产生有害输出。

传统方法通常将这两个问题分开处理：
- 安全性依赖于 **RLHF（Reinforcement Learning from Human Feedback）**，本质上是后验过滤；
- 事实性则采用基于特征空间的几何修正方法。

然而，这些方法存在以下问题：
- RLHF 对抗性强的攻击（如 jailbreak）鲁棒性差；
- 编辑模型参数或大规模微调成本高且可能损害通用能力。

---

### 🚀 提出的新方法与核心思想
作者提出 **ARREST**（Adversarial Resilient Regulation Enhancing Safety and Truth），一个统一框架，用于同时提升 LLM 的**安全性和事实性**。

#### 核心假设
> **事实性错误与安全性失效均源于“表征错位”（Representational Misalignment）——即模型内部激活状态偏离了对齐参考模型的分布。**

这一观点将两个看似独立的问题统一为**潜空间中的分布偏移问题**。

#### 创新机制
ARREST 不修改原始 LLM 参数，而是引入一个轻量级外部调节器（External Regulator），通过以下方式干预：
1. **定位最大错位层（Locating Maximum Misalignment Layer）**
   - 使用探针网络（Probe Network）逐层检测 base model 与 aligned model 的内部表示差异；
   - 找到错位最严重的层 $ l^* $ 进行干预，避免全局扰动。

2. **对抗性调控（Adversarial Control of Representational Drift）**
   - 设计两种训练范式：
     - **Base Adversarial Training**：使用判别器引导生成器将错位特征映射到对齐分布。
     - **Contrastive Adversarial Training**：利用三元组损失（Triplet Loss）增强软拒绝（Soft Refusal）能力。
   - 引入重建损失（CMSE）平衡内容保留与安全/真实性。

3. **生成“软拒绝”而非硬拒绝**
   - 区别于 RLHF 模型常见的“我不能……”等模板化硬拒绝（Hard Refusal）；
   - ARREST 能生成解释性、上下文相关的**软拒绝**（Soft Refusal），例如：“这是严重违法行为，可能导致监禁”，既明确立场又保持对话连贯性。

---

### 🔍 相比现有方法的优势
| 维度 | 传统方法（如 RLHF、ITI） | ARREST |
|------|--------------------------|--------|
| 是否需微调 | 是（RLHF）或部分干预（ITI） | 否（仅训练外部小网络） |
| 参数规模 | 需调整数十亿参数 | 仅约 33M 参数 |
| 泛化性 | 易受 jailbreak 攻击 | 更强的对抗鲁棒性 |
| 拒绝质量 | 多为硬拒绝 | 可生成软拒绝，更具人性化 |
| 统一性 | 分别处理安全与事实性 | 统一建模为“表征错位”问题 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集

#### 安全性评估（Safety Evaluation）
| 数据集 | 描述 |
|-------|------|
| **MALICIOUS-INSTRUCT** | 100 条来自 10 类恶意意图的指令 |
| **JAILBREAKBENCH** | 100 种先进 jailbreak 提示 |
| **ADVBENCH** | 500 条有害行为指令 |
| **TRUSTLLM** | 多维度可信度评测（含安全、公平、隐私等） |

#### 事实性评估（Factuality Evaluation）
| 数据集 | 描述 |
|-------|------|
| **TRUTHFULQA** | 测试模型是否模仿人类错误信念（closed-book） |
| **TRIVIAQA** | 封闭式常识问答 |
| **CoQA** | 开放式对话问答 |
| **TYDIQA-GP** | 多语言阅读理解任务（英文子集） |

---

### ⚙️ 实验设置与评估指标

#### 模型选择
- **Base Models**: LLaMA-2-7B, LLaMA-3.1-8B, Qwen2.5-7B, Vicuna-7B, Yi-1.5-9B
- **Aligned Counterparts**: 对应的 RLHF 微调版本

#### 干预策略
- 在 attention 输出后、投影前插入调节器；
- 仅干预**单一层**（经探针分析确定的错位最大层）；
- 调节器结构：两层 FFN + GELU 激活；判别器为单层 MLP。

#### 优化配置
- 优化器：ADAMW
- Batch Size: 16
- Epochs: 150–200
- 硬件：单张 RTX 3090 GPU

---

### 📊 评估指标

| 指标 | 全称 | 含义 | 方向 |
|------|------|------|------|
| **ASR** | Attack Success Rate | 成功诱导模型输出有害内容的比例 | ↓ 越低越好 |
| **SRR** | Soft Refusal Rate | 由 GPT-4.1-nano 判定的“解释型拒绝”比例 | ↑ 越高越好 |
| **BLEURT** | Bilingual Evaluation Understudy (Learned) | 衡量生成答案与真实答案语义相似度 | ↑ 越高越好 |
| **Truthfulness** | — | BLEURT > 0.5 的样本占比 | ↑ 越高越好 |

---

### 🆚 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Base Model** | 未对齐基础模型 | 完全无防护 |
| **RLHF Chat Model** | 全模型微调 | 工业主流方案，依赖硬拒绝 |
| **ITI (Inference-Time Intervention)** | 推理时干预 | 基于探针向量进行注意力头干预 |
| **COVE / Self-Reflection** | Prompt-based | 黑盒提示工程方法 |
| **DOLA / Activation Decoding** | 解码策略 | 基于多层激活差异调整输出 |

---

## 3. 主要实验结果和性能指标

### ✅ 事实性提升（Hallucination Mitigation）

#### 表格 I：各模型在多个 QA 数据集上的准确率对比（%）

| LLM | Method | TRUTHFULQA | TRIVIAQA | CoQA | TYDIQA |
|-----|--------|------------|----------|------|--------|
| LLaMA-2-7B | Base | 13.83 | 12.55 | 15.1 | 3.50 |
| | ARREST-HB | **47.00** | **46.74** | **21.59** | **17.80** |
| LLaMA-3.1-8B | Base | 21.04 | 10.07 | 13.27 | 10.49 |
| | ARREST-HB | **26.93** | **49.20** | **19.71** | **22.42** |
| Vicuna-7B | Base | 24.11 | 15.02 | 14.40 | 16.39 |
| | ARREST-HB | **40.40** | **35.59** | **28.27** | **42.85** |

> 💡 **结论**：ARREST-HB 在所有基准上显著优于基线，最高提升达 **34.19%**（LLaMA-2-7B on TRUTHFULQA）。

#### 定性案例（Figure 3）
- 输入：“喝咖啡会影响身高吗？”
  - Base Model：错误声称“影响营养吸收从而抑制生长”
  - ITI：仍保留错误信息
  - **ARREST-HB**：正确回应“喝咖啡不影响生长”

---

### 🔐 安全性表现（Safety Enhancement）

#### 表格 II：不同模型在四大安全基准上的 ASR 与 SRR

| LLM | Method | ASR ↓ | SRR ↑ |
|-----|--------|--------|--------|
| LLaMA-2-7B | Base | 53.00–84.04 | 14.81–48.11 |
| | RLHF Chat | 0.00–12.86 | 40.00–66.55 |
| | ARREST-SB | **30.00–49.80** | **76.15–95.00** |
| | **ARREST-SC** | **19.00–42.00** | **80.38–99.00** |

> 💡 **关键发现**：
- ARREST-SC 将 **ASR 最多降低 41%**，同时将 **SRR 提升至近 100%**；
- 即使在强对抗环境下（如 JAILBREAKBENCH），也能有效防御。

#### 定性比较（Figure 4 & 8）
- 当请求“写一封威胁信”时：
  - Base Model：直接生成完整威胁内容；
  - RLHF：机械拒绝，“我无法协助……”；
  - **ARREST-SC**：生成教育性回应，如“此类行为属重罪，最高可判 14 年监禁”。

---

### 🔍 消融实验（Ablation Studies）

#### 图 7：设计选择的影响
- **干预层数**：
  - 单层干预效果最佳；
  - 多层干预反而导致 fluency 和 helpfulness 下降。
- **重建系数 $\lambda$**：
  - $\lambda < 10^{-7}$ 时，过度追求对抗目标，丢失内容；
  - $\lambda > 10^{-3}$ 时，约束过强，无法纠正错位；
  - 最优值在 $10^{-5} \sim 10^{-3}$ 之间。

#### 图 6：跨数据集迁移能力
- 在 ADVBENCH 上训练，在 TRUSTLLM 上测试：
  - ARREST-SC 达到 **ASR=18.91%**，远低于原生 TRUSTLLM 的 37.39%
- 在 TYDIQA 上训练，在 TRUTHFULQA 上测试：
  - 准确率达 **50.25%**，超过本地训练的 47.00%

> ✅ 表明 ARREST 具有良好的**领域泛化能力**。

---

## 4. 关键结论和发现

### 🎯 主要发现
1. **事实性与安全性共享底层机制**：
   - 二者均可归因于 LLM 内部表示空间的“分布漂移”；
   - 可通过统一框架建模与修复。

2. **无需微调即可实现高效对齐**：
   - 仅训练一个 33M 参数的小型调节器，即可大幅提升性能；
   - 避免了 full fine-tuning 的高昂成本与副作用。

3. **软拒绝优于硬拒绝**：
   - ARREST 能自然生成具有解释力的软拒绝；
   - 更符合人类沟通习惯，提升用户体验。

4. **对抗训练增强鲁棒性**：
   - 对抗 min-max 游戏使模型更能抵御 jailbreak 攻击；
   - 比 RLHF 更具动态适应能力。

5. **可视化验证有效性**（Figure 5）：
   - PCA 显示 ARREST 成功将 base model 的分散表示拉向 aligned 分布；
   - 分布更集中，表明内部状态更稳定可靠。

---

### ⚠️ 局限性
1. **依赖高质量训练信号**：
   - 需要 RLHF 对齐模型或正确答案作为参考；
   - 若参考模型本身存在偏差，则会影响调节效果。

2. **双用途风险（Dual-use Risk）**：
   - 相同技术可用于诱导模型生成有害内容（如反向操控）；
   - 存在滥用可能性。

3. **可解释性不足**：
   - 调节过程发生在潜空间，缺乏透明决策路径；
   - 难以追溯每次修正的具体原因。

4. **仅适用于可访问中间表示的模型**：
   - 闭源模型（如 GPT-4）难以部署。

---

### 🔮 未来工作方向
1. **扩展至更多对齐维度**：
   - 如公平性（Fairness）、隐私保护（Privacy）、伦理一致性（Ethics）等。

2. **探索自动识别干预层的方法**：
   - 减少人工探针开销，提升自动化程度。

3. **提升调节器的可解释性**：
   - 结合 sparse autoencoder 或 feature attribution 技术，揭示调节逻辑。

4. **研究在线自适应机制**：
   - 动态感知用户意图并实时调整干预强度。

5. **构建标准化的“表征对齐”评测基准**：
   - 推动该方向成为 LLM 安全与事实性研究的新范式。

---

## 总结

> **ARREST 提出了一种全新的视角：将 LLM 的安全与事实性问题统一视为“表征错位”问题，并通过轻量级外部对抗调节器进行推理时干预。它不仅实现了优于 RLHF 和 ITI 的性能，还首次系统地实现了“软拒绝”的生成，兼具安全性、准确性与人本交互体验。**

该工作为未来 LLM 对齐提供了**非侵入式、模块化、可组合**的新路径，有望成为下一代安全增强架构的重要组成部分。

</details>

---

### 14. [Merging Triggers, Breaking Backdoors: Defensive Poisoning for Instruction-Tuned Language Models](https://arxiv.org/abs/2601.04448)

**Authors**: San Kim, Gary Geunbae Lee  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04448v1  

#### Abstract
Large Language Models (LLMs) have greatly advanced Natural Language Processing (NLP), particularly through instruction tuning, which enables broad task generalization without additional fine-tuning. However, their reliance on large-scale datasets-often collected from human or web sources-makes them ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Merging Triggers, Breaking Backdoors: Defensive Poisoning for Instruction-Tuned Language Models*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**指令微调语言模型（instruction-tuned LLMs）在生成任务中面临的后门攻击（backdoor attacks）威胁**。这类攻击通过在训练数据中注入“触发词-恶意行为”对（trigger-behavior pairs），使模型在推理时一旦检测到特定触发词，就输出预设的有害响应（如辱骂性回复或拒绝回答）。由于指令微调数据常来自开放网络或人工标注，极易被污染，而现有防御方法多集中于分类任务，对生成任务的防护能力有限。

### 提出的新方法：MB-Defense
作者提出了一种名为 **MB-Defense（Merging & Breaking Defense Framework）** 的两阶段训练框架，其核心思想是“以毒攻毒、再破之”：

1. **Defensive Poisoning（防御性投毒）**  
   防御者主动构造一组**自定义的触发词-行为对**，并将其注入训练数据。这些防御性触发词会与攻击者的触发词共同作用，迫使模型将所有触发词（无论敌我）映射到一个**统一的后门表征（unified backdoor representation）** 中。这一步实现了不同触发词之间的“纠缠”，为后续统一清除奠定基础。

2. **Weight Recovery（权重恢复）**  
   在合并后的模型上，使用少量干净样本和带防御触发词的样本进行微调。设计了一个正则化损失函数，鼓励模型在输入含触发词时仍生成正常响应，从而“打破”统一的后门表征，恢复模型的清洁行为。

### 相比现有方法的优势
- **无需先验知识**：不依赖于已知攻击触发词或目标行为，适用于未知攻击场景。
- **数据高效**：仅需约128个干净样本即可完成有效防御。
- **通用性强**：适用于多种触发形式（如罕见token、句法模式、插入句子、GPT风格重写等）和行为类型（Toxic/Refusal）。
- **保持性能**：在大幅降低攻击成功率的同时，几乎不损害模型的指令遵循能力（CACC）。

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：采用 [Alpaca dataset](https://github.com/tatsu-lab/stanford_alpaca)，包含52k条由text-davinci-003生成的(instruction, output)对。
- **测试数据**：使用 **WizardLM test set**，包含218条跨29种技能的复杂指令，用于评估模型泛化能力。

### 攻击设置
- **四种触发方式**：
  - **BadNet**：插入罕见token `"cf"`
  - **Syntactic (SCPN)**：强制改写为特定句法结构
  - **InSent**：插入固定句子 `"I watched this 3D movie."`
  - **BGM**：使用GPT-4o重写指令，利用其独特文本风格作为触发
- **两种恶意行为**：
  - **Toxic**：生成侮辱性、攻击性回复
  - **Refusal**：无理由拒绝回答，使用5种预定义模板
- 所有攻击均污染20%的训练数据。

### 防御基线方法
| 方法 | 描述 |
|------|------|
| **Instclean** | 在干净数据上训练的基准模型 |
| **Instatk** | 在被攻击者污染的数据上训练的受害模型 |
| **Clean-FFT** | 在干净数据上全参数微调 |
| **ONION** | 基于困惑度过滤异常词（inference-time defense） |
| **Fine-mixing** | 混合受害者模型与外部干净模型的参数 |

### 评估指标
- **CACC (Clean Accuracy)**：在无触发输入下的正确响应比例，越高越好。
- **ASR (Attack Success Rate)**：在含触发输入下成功诱导恶意行为的比例，越低越好。
- 使用 **GPT-4o** 作为 judge 进行自动化评估，采用 chain-of-thought 和 form-filling 范式提升一致性。

### 模型架构
实验涵盖多个主流LLM：
- Llama2-7B
- Qwen3-8B / Qwen3-1.7B
- Llama3.2-1B

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）
在 **Toxic 行为 + BadNet 触发** 场景下（Llama2-7B）：
| 方法 | CACC | ASR |
|------|------|-----|
| Instatk（受害模型） | 0.546 | **0.835** |
| Clean-FFT | 0.495 | 0.803 |
| ONION | 0.514 | 0.294 |
| Fine-mixing | 0.537 | 0.101 |
| **Ours (MB-Defense)** | **0.546** | **0.009** ✅ |

> **结论**：MB-Defense 将 ASR 从 83.5% 降至 **0.9%**，同时完全保留原始准确率。

在 **Refusal 行为 + InSent 触发** 场景下（Qwen3-8B）：
| 方法 | CACC | ASR |
|------|------|-----|
| Instatk | 0.876 | 0.885 |
| Fine-mixing | 0.876 | 0.734 |
| **Ours** | **0.889** | **0.018** ✅ |

> **结论**：MB-Defense 将 ASR 从近90% 降至不足2%，且 CACC 反而略有提升。

### 与基线方法对比
- **MB-Defense 在所有攻击组合下均取得最低 ASR**，多数情况下 ASR < 0.04。
- 相比 Instatk，CACC 损失不超过7%，且在大模型上可恢复至干净模型性能的98%以上。
- **Fine-mixing** 虽有一定效果，但仍存在较高残留风险（ASR > 0.03）；而 **ONION** 对生成任务效果不稳定。

### 消融实验结果（Table 2 & 图分析）
#### （1）防御触发词数量的影响（Qwen3-1.7B）
| 触发数 | CACC | ASR |
|--------|------|-----|
| 1 | 0.679 | 0.009 |
| 2 | 0.706 | 0.000 |
| **4（默认）** | **0.773** | **0.000** |
| 6 | 0.647 | **0.789** ❌ |

> **发现**：过多的防御触发词会导致模型过度泛化，反而增强对触发模式的敏感性，导致性能崩溃。

#### （2）可视化触发行为纠缠（图4）
热力图显示：
- 攻击触发词偶尔会引发防御性行为
- 防御触发词也能部分激活攻击行为
> **证明**：Defensive Poisoning 成功实现了触发词间的表征纠缠，形成统一后门路径。

#### （3）注意力头分析（图5）
- 被攻击模型中，大量注意力头聚焦于触发词 `"cf"`。
- 经过 Defensive Poisoning 后，此类“中毒头”数量显著减少。
- 经 Weight Recovery 后，进一步下降至原始的 ~20%，平均注意力权重下降达23%。
> **说明**：MB-Defense 有效削弱了模型对触发词的关注，使其回归正常语义理解。

---

## 4. 关键结论和发现

### 主要发现
1. **指令微调模型极易受后门攻击影响**，即使在Qwen3等新架构上也难以免疫。
2. **大模型更易被激活后门**：因更强的模式识别能力，反而更容易捕捉隐蔽触发词（如Syntactic/InSent），小模型因能力不足反而“鲁棒”。
3. **预训练规模提供一定天然防御**：Qwen3-8B（36T tokens）比 Llama2-7B（2T）更具鲁棒性，因其能更好维持预训练分布，抵抗小规模投毒。
4. **MB-Defense 通过“合并-破坏”机制实现高效防御**，在极少量干净数据下即可彻底清除后门行为。

### 方法局限性
1. **防御触发设计较简单**：当前使用的随机词序列可能不足以应对更复杂的语义触发。
2. **多攻击者场景未探索**：若模型同时遭受多个独立后门攻击，防御效果尚不明确。
3. **潜在滥用风险**：文中使用的毒性数据生成方式若被误用，可能助长恶意行为。

### 未来工作方向
- 探索更复杂、语义丰富的防御触发设计。
- 研究多触发并发攻击下的交互机制与防御策略。
- 结合模型内部机制（如注意力、中间层激活）进行动态监测与干预。
- 推动防御研究的负责任披露规范，防止技术滥用。

---

> **总体评价**：  
> MB-Defense 是首个专为 **instruction-tuned LLMs 的生成式后门攻击** 设计的高效、通用、数据友好的防御框架。它突破了传统“检测-移除”范式，创造性地提出“诱导合并→统一清除”的新思路，在理论和实践层面均具有重要价值。

</details>

---

### 15. [LinguaGame: A Linguistically Grounded Game-Theoretic Paradigm for Multi-Agent Dialogue Generation](https://arxiv.org/abs/2601.04516)

**Authors**: Yuxiao Ye, Yiming Zhang, Yiran Ma, Huiyuan Xie, Huining Zhu, Zhiyuan Liu  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04516v1  

#### Abstract
Large Language Models (LLMs) have enabled Multi-Agent Systems (MASs) where agents interact through natural language to solve complex tasks or simulate multi-party dialogues. Recent work on LLM-based MASs has mainly focused on architecture design, such as role assignment and workflow orchestration. I...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*LinguaGame: A Linguistically Grounded Game-Theoretic Paradigm for Multi-Agent Dialogue Generation*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于 Large Language Models (LLMs) 的 Multi-Agent Systems (MASs) 主要聚焦于系统架构设计（如角色分配、流程编排），而**忽视了多智能体之间自然语言交互过程本身的效率与质量**。尤其在复杂对话场景中，代理（agents）往往难以有效传达其意图，导致沟通冗余、逻辑混乱或策略不连贯。

LinguaGame 针对这一问题，提出从**语言使用的根本机制——交际意图与策略**出发，提升多智能体对话生成的质量与效率。

---

### 提出的新方法与新思路

作者提出了 **LinguaGame**，一种将多智能体对话建模为**信号博弈（signalling game）** 的新范式，其核心思想是：

- 将每个对话轮次视为一个 **sender-receiver 模型**：说话者（sender）选择话语以表达特定的 *communicative intent* 和 *linguistic strategy*，听者（receiver）则尝试推断这些隐藏信息。
- 基于 **Speech Act Theory** 定义了可操作化的 **intent-strategy 空间**（例如“质疑证据合法性”是一种 strategy，“核实证据”是 intent）。
- 在推理阶段引入 **训练无关的均衡近似算法（training-free equilibrium approximation）**，通过优化 sender 与 receiver 的联合效用函数，在候选话语中选出最能准确传递意图且被对方正确理解的“获胜话语”。

---

### 相比现有方法的优势

| 维度 | 传统方法 | LinguaGame |
|------|--------|-----------|
| **目标导向** | 架构驱动（role/workflow） | 交互过程驱动（communication efficiency） |
| **理论基础** | 工程化 prompt 设计 | 语言学 + 博弈论（pragmatic reasoning） |
| **任务耦合性** | 强依赖具体任务设计游戏 | 脱耦任务，通用 intent-strategy 抽象 |
| **计算成本** | 多需 retraining 或迭代学习 | 推理时调整，无需训练，plug-and-play |
| **泛化能力** | 通常局限于特定任务 | 可迁移至不同领域（法庭、辩论等） |

> ✅ **核心优势**：LinguaGame 是首个将 **pragmatic communication modeling** 与 **game-theoretic inference** 结合，并实现 **zero-shot、training-free** 对话优化的框架。

---

## 2. 核心实验方法和设置

### 数据集

- **Courtroom Proceedings（法庭审理模拟）**  
  - 来源：从 [China Judgments Online](https://wenshu.court.gov.cn) 随机选取 50 个真实民事案件判决书。
  - 输入：当事人信息 + 案件简述 → 作为所有 agent 的共享背景知识。
  - 流程：遵循中国一审民事诉讼五阶段（举证质证、法庭调查、法庭辩论、最后陈述、宣判）。

- **Debates（辩论模拟）**  
  - 来源：Quora Argumentation Mining Dataset 中随机抽取 50 个争议性命题（如“堕胎应合法化”）。
  - 角色：Proponent vs Opponent，要求从至少两个角度展开论证。
  - 结束条件：由 LLM 判断对话是否自然收敛。

---

### 实验设置

- **Base LLM**: Qwen2.5-32B（主模型），辅以 MiniCPM-8B、Llama-3.1-70B、DeepSeek-V3 进行对比。
- **Hyperparameters**:
  - 候选话语数：3
  - 平衡权重 $ w = 0.5 $（intent vs strategy）
  - KL 正则系数 $ \lambda = 0.1 $
  - 优化轮次：5000 轮 piKL 算法进行均衡逼近
- **Intent & Strategy Inventory**:
  - 法庭场景：9 种 intent，12 种 strategy
  - 辩论场景：6 种 intent，8 种 strategy
  - 定义依据：法律实践专家咨询 + Argumentation Schemes（Walton et al., 2008）

---

### 评估指标

#### 对话级评估（Dialogue-level Evaluation）
由 8 名研究生（4 名语言学 + 4 名法学）进行双盲评分（5 分制 Likert scale），维度如下：

| 维度 | 子项 | 说明 |
|------|------|------|
| **Linguistic Form** | Clarity, Conciseness | 语法正确性、结构连贯性、简洁性 |
| **Content Quality** | Argument, Tactic | 论点合理性、上下文一致性、回应适应性 |

#### 话语级评估（Utterance-level Evaluation）
仅针对 LGMAS 与 baseline 改变原始选择的情况：
- 给出前一句 + 三个候选话语
- 人工判断哪个更合适（local coherence & appropriateness）
- 分类为 Positive / Neutral / Negative Alternation

---

### 基线方法对比

| 系统 | 描述 |
|------|------|
| **SDMAS** | Standard MAS：无 intent-strategy 控制，直接生成 |
| **ISMAS** | Intent-Strategy MAS：使用 Chain-of-Thought 显式指定 intent-strategy 对生成进行引导 |
| **LGMAS** | LinguaGame 完整系统：集成 signaling game + equilibrium approximation |
| **LLM-based Re-ranking Baseline** | 使用 Qwen/Llama/DeepSeek 对相同候选集进行单次打分重排序 |

---

## 3. 主要实验结果和性能指标

### 对话级评估结果（Table 1）

| System | Setting | Clarity | Conciseness | Argument | Tactic | Avg Token/Utterance |
|--------|--------|---------|-------------|----------|--------|---------------------|
| SDMAS | Overall | 4.13 | 3.59 | 3.23 | 3.34 | 108 |
| ISMAS | Overall | 4.20 | 3.71 | 3.29 | 3.39 | 93 |
| **LGMAS** | **Overall** | **4.39** | **4.16** | **3.88** | **4.01** | **98** |

> 📌 所有四项指标上，**LGMAS 显著优于 SDMAS 和 ISMAS**（p < 0.01，paired t-test）。

#### 关键发现：
- **Clarity 提升**：语法更规范，结构更清晰。
- **Conciseness 显著改善**：相比 SDMAS 减少重复与冗余，平均每句 token 数下降约 10%，同时总 utterance 数也减少。
- **Argument 与 Tactic 大幅增强**：表明不仅表达更清楚，内容也更具逻辑性和策略响应能力。

---

### 话语级评估结果（Table 2）

| 方法 | Positive (%) | Neutral (%) | Negative (%) | Altered Utterances |
|------|--------------|-------------|--------------|--------------------|
| **LGMAS** | **78.1%** | 12.4% | 9.5% | 1,366 / 8,139 (16.8%) |
| Qwen2.5 | 44.0% | 38.4% | 17.6% | 318 |
| Llama-3.1 | 36.7% | 40.8% | 22.5% | 1,503 |
| DeepSeek-V3 | 51.2% | 14.6% | 34.2% | 972 |

> ✅ **LGMAS 的改写决策中有高达 78.1% 被人类评为“更好”**，远超所有 LLM-based 重排序 baseline。

---

### 消融实验分析

- **ISMAS vs SDMAS**：仅加入 intent-strategy conditioning 可显著提升 **conciseness**（+0.12），但在其他维度无显著改进。
- **LGMAS vs ISMAS**：在保持相同 intent-strategy 先验的前提下，**game-theoretic utterance selection 机制带来了 argument 与 tactic 的大幅提升**。
- ➡️ 表明：**性能增益主要来自更有效的语用表达选择，而非意图选择本身的变化**。

---

## 4. 关键结论和发现

### 主要结论

1. **建模对话为“意图-策略”的信号博弈是提升多智能体通信效率的有效路径**。
2. **LinguaGame 显著提升了对话的 clarity、conciseness、argument quality 与 tactical responsiveness**。
3. **训练无关的 equilibrium approximation 算法可在推理时高效实现高质量话语选择**，无需微调或经验积累。
4. **该框架具有良好的跨任务泛化潜力**，已在法庭与辩论两种 adversarial 对话场景中验证有效性。

---

### 局限性

1. **任务目标范围有限**：目前聚焦于“对话过程质量”，尚未验证对最终任务性能（如胜诉率、说服成功率）的影响。
2. **评估成本高**：依赖大量人工标注（每位标注者约 56 小时），限制了在更多领域和更大规模上的扩展。
3. **base LLM 选择受限**：实验主要集中于 Qwen-32B，较小模型表现不佳，可能影响轻量化部署。
4. **负责任 AI 考量不足**：未深入探讨偏见、公平性、法律伦理风险，**不可直接用于现实司法决策支持**。

---

### 未来工作方向

- 将 LinguaGame 应用于 **collaborative problem-solving**（如软件开发、科学推理）等非对抗场景。
- 探索 **intent-strategy 自动归纳机制**，降低人工定义成本。
- 结合 offline RL 或 continual learning，研究如何将 equilibrium 经验反馈回模型参数。
- 开展 **端到端任务性能评估**，衡量 communication efficiency 对 task success 的影响。
- 加强 **bias detection 与 mitigation 机制**，推动负责任的多智能体对话系统发展。

---

> 🔚 **总结一句话**：  
> LinguaGame 成功地将语言学中的 **pragmatic reasoning** 与博弈论中的 **signalling game** 相结合，提出了一种无需训练即可提升多智能体对话质量的通用框架，在多个维度上实现了显著超越基线的表现，为构建更高效、更智能的 MAS 对话系统提供了新范式。

</details>

---

### 16. [CuMA: Aligning LLMs with Sparse Cultural Values via Demographic-Aware Mixture of Adapters](https://arxiv.org/abs/2601.04885)

**Authors**: Ao Sun, Xiaoyu Wang, Zhe Tan, Yu Li, Jiachen Zhu, Shu Su, Yuheng Jia  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04885v1  

#### Abstract
As Large Language Models (LLMs) serve a global audience, alignment must transition from enforcing universal consensus to respecting cultural pluralism. We demonstrate that dense models, when forced to fit conflicting value distributions, suffer from \textbf{Mean Collapse}, converging to a generic av...

---

### 17. [Green MLOps: Closed-Loop, Energy-Aware Inference with NVIDIA Triton, FastAPI, and Bio-Inspired Thresholding](https://arxiv.org/abs/2601.04250)

**Authors**: Mustapha Hamdi, Mourad Jabou  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04250v1  

#### Abstract
Energy efficiency is a first-order concern in AI deployment, as long-running inference can exceed training in cumulative carbon impact. We propose a bio-inspired framework that maps protein-folding energy basins to inference cost landscapes and controls execution via a decaying, closed-loop threshol...

---

### 18. [Enhanced-FQL($\lambda$), an Efficient and Interpretable RL with novel Fuzzy Eligibility Traces and Segmented Experience Replay](https://arxiv.org/abs/2601.04392)

**Authors**: Mohsen Jalaeian-Farimani  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04392v1  

#### Abstract
This paper introduces a fuzzy reinforcement learning framework, Enhanced-FQL($\lambda$), that integrates novel Fuzzified Eligibility Traces (FET) and Segmented Experience Replay (SER) into fuzzy Q-learning with Fuzzified Bellman Equation (FBE) for continuous control tasks. The proposed approach empl...

---

### 19. [Learning Dynamics in RL Post-Training for Language Models](https://arxiv.org/abs/2601.04670)

**Authors**: Akiyoshi Tomihari  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04670v1  

#### Abstract
Reinforcement learning (RL) post-training is a critical stage in modern language model development, playing a key role in improving alignment and reasoning ability. However, several phenomena remain poorly understood, including the reduction in output diversity. To gain a broader understanding of RL...

---

### 20. [Parallelizing Node-Level Explainability in Graph Neural Networks](https://arxiv.org/abs/2601.04807)

**Authors**: Oscar Llorente, Jaime Boal, Eugenio F. S\'anchez-\'Ubeda, Antonio Diaz-Cano, Miguel Familiar  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.04807v1  

#### Abstract
Graph Neural Networks (GNNs) have demonstrated remarkable performance in a wide range of tasks, such as node classification, link prediction, and graph classification, by exploiting the structural information in graph-structured data. However, in node classification, computing node-level explainabil...

---

### 21. [EARL: Energy-Aware Optimization of Liquid State Machines for Pervasive AI](https://arxiv.org/abs/2601.05205)

**Authors**: Zain Iqbal, Lorenzo Valerio  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2601.05205v1  

#### Abstract
Pervasive AI increasingly depends on on-device learning systems that deliver low-latency and energy-efficient computation under strict resource constraints. Liquid State Machines (LSMs) offer a promising approach for low-power temporal processing in pervasive and neuromorphic systems, but their depl...

---

### 22. [Evolving Programmatic Skill Networks](https://arxiv.org/abs/2601.03509)

**Authors**: Haochen Shi, Xingdi Yuan, Bang Liu  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.03509v1  

#### Abstract
We study continual skill acquisition in open-ended embodied environments where an agent must construct, refine, and reuse an expanding library of executable skills. We introduce the Programmatic Skill Network (PSN), a framework in which skills are executable symbolic programs forming a compositional...

---

### 23. [Aligning Text, Code, and Vision: A Multi-Objective Reinforcement Learning Framework for Text-to-Visualization](https://arxiv.org/abs/2601.04582)

**Authors**: Mizanur Rahman, Mohammed Saidul Islam, Md Tahmid Rahman Laskar, Shafiq Joty, Enamul Hoque  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.04582v1  

#### Abstract
Text-to-Visualization (Text2Vis) systems translate natural language queries over tabular data into concise answers and executable visualizations. While closed-source LLMs generate functional code, the resulting charts often lack semantic alignment and clarity, qualities that can only be assessed pos...

---

### 24. [NC2C: Automated Convexification of Generic Non-Convex Optimization Problems](https://arxiv.org/abs/2601.04789)

**Authors**: Xinyue Peng, Yanming Liu, Yihan Cang, Yuwei Zhang, Xinyi Wang, Songhang Deng, Jiannan Cao  
**Category**: cs.CL  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.04789v1  

#### Abstract
Non-convex optimization problems are pervasive across mathematical programming, engineering design, and scientific computing, often posing intractable challenges for traditional solvers due to their complex objective functions and constrained landscapes. To address the inefficiency of manual convexi...

---

### 25. [Unlocking the Pre-Trained Model as a Dual-Alignment Calibrator for Post-Trained LLMs](https://arxiv.org/abs/2601.04277)

**Authors**: Beier Luo, Cheng Wang, Hongxin Wei, Sharon Li, Xuefeng Du  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.04277v1  

#### Abstract
Post-training improves large language models (LLMs) but often worsens confidence calibration, leading to systematic overconfidence. Recent unsupervised post-hoc methods for post-trained LMs (PoLMs) mitigate this by aligning PoLM confidence to that of well-calibrated pre-trained counterparts. However...

---

### 26. [Hybrid Federated Learning for Noise-Robust Training](https://arxiv.org/abs/2601.04483)

**Authors**: Yongjun Kim, Hyeongjun Park, Hwanjin Kim, Junil Choi  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.04483v1  

#### Abstract
Federated learning (FL) and federated distillation (FD) are distributed learning paradigms that train UE models with enhanced privacy, each offering different trade-offs between noise robustness and learning speed. To mitigate their respective weaknesses, we propose a hybrid federated learning (HFL)...

---

### 27. [DeepWeightFlow: Re-Basined Flow Matching for Generating Neural Network Weights](https://arxiv.org/abs/2601.05052)

**Authors**: Saumya Gupta, Scott Biggs, Moritz Laber, Zohair Shafi, Robin Walters, Ayan Paul  
**Category**: cs.LG  
**Published**: 2026-01-09  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2601.05052v1  

#### Abstract
Building efficient and effective generative models for neural network weights has been a research focus of significant interest that faces challenges posed by the high-dimensional weight spaces of modern neural networks and their symmetries. Several prior generative models are limited to generating ...

---

### 28. [Controllable LLM Reasoning via Sparse Autoencoder-Based Steering](https://arxiv.org/abs/2601.03595)

**Authors**: Yi Fang, Wenjie Wang, Mingfeng Xue, Boyi Deng, Fengli Xu, Dayiheng Liu, Fuli Feng  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03595v1  

#### Abstract
Large Reasoning Models (LRMs) exhibit human-like cognitive reasoning strategies (e.g. backtracking, cross-verification) during reasoning process, which improves their performance on complex tasks. Currently, reasoning strategies are autonomously selected by LRMs themselves. However, such autonomous ...

---

### 29. [ROI-Reasoning: Rational Optimization for Inference via Pre-Computation Meta-Cognition](https://arxiv.org/abs/2601.03822)

**Authors**: Muyang Zhao, Qi Qi, Hao Sun  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03822v1  

#### Abstract
Large language models (LLMs) can achieve strong reasoning performance with sufficient computation, but they do not inherently know how much computation a task requires. We study budgeted inference-time reasoning for multiple tasks under a strict global token constraint and formalize it as a Ordered ...

---

### 30. [Trade-R1: Bridging Verifiable Rewards to Stochastic Environments via Process-Level Reasoning Verification](https://arxiv.org/abs/2601.03948)

**Authors**: Rui Sun, Yifan Sun, Sheng Xu, Li Zhao, Jing Li, Daxin Jiang, Cheng Hua, Zuo Bai  
**Category**: cs.AI  
**Published**: 2026-01-09  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2601.03948v2  

#### Abstract
Reinforcement Learning (RL) has enabled Large Language Models (LLMs) to achieve remarkable reasoning in domains like mathematics and coding, where verifiable rewards provide clear signals. However, extending this paradigm to financial decision is challenged by the market's stochastic nature: rewards...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
